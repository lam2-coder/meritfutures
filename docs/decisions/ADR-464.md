## ADR-464: the pricing idiom gets a register and a checker, the population is fifty-five at this head and not forty-one, and the entry that derived the forty-one published a command that misses three of them (2026-09-08, status: proposed)

**Row 464 of the 448s wave. Branch `claude/price-register`, off `origin/claude/wave-464s` at `74eb0fd`, session 659. [ADR-461](ADR-461.md) section 10 item 4 taken, second option.**

**THE POPULATION IS FORTY-EIGHT AT THE BASE AND FIFTY-FIVE AT THIS HEAD, AND BOTH NUMBERS WERE DERIVED HERE.** [ADR-461](ADR-461.md) found forty-one over seven at `3f40646a`, and the difference is not the arithmetic it looks like. The base carries that entry's own seven on top of the forty-one, because [ADR-461](ADR-461.md) prices things and is now in the tree; the head carries this entry's seven on top of that, because section 8 below prices seven things and **an entry that prices is a member of the population it describes.** The number that is a finding is neither of those: **the derivation command [ADR-461](ADR-461.md) section 2 publishes returns thirty-eight over those seven entries and not forty-one.** Its census table is right and the command printed under it is short by three, because the command requires an asterisk against the label and three of the prices it scored are written unbolded. A register transcribed from either that command or that table would have inherited a hole or a stale total, which is the error this whole wave is about, so this row's checker counts the corpus on every run and pins no number anywhere.

**WHAT LANDS IS A REGISTER PLUS A CHECKER AND NEITHER HALF IS THE WHOLE.** [PRICE_REGISTER.md](PRICE_REGISTER.md) lists every labelled price with an obligation, an accuracy and a basis. [`price-register.mjs`](../../packages/tooling/checks/price-register.mjs) derives the population, the anchors and the paths, compares them against that table in both directions, and **refuses to derive a state.** The division is the whole design and section 4 is the argument for it: a script that claimed to know whether a price is still owed would be inventing the reading [ADR-461](ADR-461.md) spent a row producing.

**THE SHAPE IS THE ONE THE DISPATCH SET AND IT WAS NOT ARGUED AGAINST.** One checker under `packages/tooling/checks/`, on [`write-guard-set.mjs`](../../packages/tooling/checks/write-guard-set.mjs)'s precedent, running through `CI-02` by way of its own suite file. **No `CI-06` letter and no `RI-nn` is minted; gates stay at 33 and invariants at 35.** No workflow file is touched.

---

### 1. The container, the base, and everything measured before the first edit

**THE REPOSITORY WAS PRESENT AND `node_modules` WAS NOT.** This breaks the run of eight consecutive rows that arrived with no checkout at all, recorded by [ADR-449](ADR-449.md), [ADR-456](ADR-456.md), [ADR-457](ADR-457.md), [ADR-458](ADR-458.md), [ADR-459](ADR-459.md), [ADR-460](ADR-460.md) and [ADR-461](ADR-461.md). The working tree was on `claude/price-register` at `74eb0fd` with a full refspec and an `origin` that fetched, so none of [ADR-460](ADR-460.md) section 1's shallow-clone remedy was needed. `pnpm install --frozen-lockfile` was the only setup step.

**THE BASE IS `74eb0fdf17e4da2fcffcdd8656f424b7f284ddc5`**, which carries the dispatcher's reservation of rows 464 and 465. Nothing was branched from `main`.

**THE BASE WAS MEASURED BEFORE THE FIRST EDIT.** Suite **346 files PASSED of 346, 8659 PASSED, 11 skipped, 8670 TOTAL, 0 failed**; gates **33 of 33** with a clean working tree after `generate`; invariants **35 of 35**; [`write-guard-set.mjs`](../../packages/tooling/checks/write-guard-set.mjs) **PASS**, 9 guards, 6 builders, 10 declared cells over 3 write guards, 4 `OPEN`. **Every column reproduces [ADR-461](ADR-461.md) section 1's base exactly**, which is the expected result: that entry's row wrote no code file and its merge added no case.

---

### 2. How the population was derived, and the three prices ADR-461's own command cannot see

**THE COMMAND IS STATED SO IT CAN BE DISAGREED WITH.** The derivation is in [`price-register.mjs`](../../packages/tooling/checks/price-register.mjs) and runs on every invocation, but its shell equivalent at this base is:

```
grep -rnoE '(\*\*)?\bPrice( of the [a-z]+)?:' docs/decisions/ADR-*.md
```

with matches inside a backtick code span, a fenced block or a blockquote discarded. **At the base it returns 48 clauses over 8 entries**: [ADR-445](ADR-445.md) 7, [ADR-448](ADR-448.md) 7, [ADR-451](ADR-451.md) 7, [ADR-452](ADR-452.md) 7, [ADR-454](ADR-454.md) 4, [ADR-458](ADR-458.md) 4, [ADR-459](ADR-459.md) 5, [ADR-461](ADR-461.md) 7. **At this head it returns 55 over 9**, the difference being this entry's own section 8.

**THE FIRST SEVEN COLUMNS REPRODUCE [ADR-461](ADR-461.md) SECTION 2's TABLE TO THE ENTRY.** That is the cross-check that matters, and it was run before anything was written down: a derivation that agreed on the total and disagreed on the distribution would mean two different populations of the same size.

**WHAT DOES NOT REPRODUCE IS THE COMMAND THAT ENTRY PUBLISHED.** Section 2 gives it as

```
grep -ohE '\*\*?Price[^*]{0,40}?:' docs/decisions/ADR-*.md
```

and `\*\*?` requires at least one asterisk immediately before the label. **Three of the forty-one prices that entry scored are written unbolded and the command cannot see any of them**: [ADR-445](ADR-445.md):139, item 8.3, scored HOLDS; [ADR-448](ADR-448.md):159, item 8.5, scored HOLDS and discharged; and [ADR-458](ADR-458.md):198, item 11.4, scored HOLDS. Run over those seven entries at this base the command returns **38**. All three files are byte-identical between `3f40646a` and this base, checked with `git diff --stat`, so this is not drift: **the census read forty-one clauses and the command under it reads thirty-eight.** The table is the sound half and the command is the one nobody re-ran.

**THIS IS NAMED AND NOT REPAIRED.** [ADR-461](ADR-461.md) is a dated record and it is outside this row's fence. It is recorded here because a later row that takes the published command as the definition will build a smaller population than the census it thinks it is extending.

**THE IDIOM THIS ROW USES IS WIDER BY EXACTLY THAT AND NARROWER IN THREE PLACES.** Wider: bold is not required, because the corpus does not consistently apply it. Narrower: a match inside a code span, a fenced block or a blockquote is a QUOTATION and not a price. That rule is load-bearing rather than tidy. [ADR-461](ADR-461.md):5 and [ADR-461](ADR-461.md):44 quote the label four times between them while discussing the idiom, this entry quotes it repeatedly, and without the exclusions the population would grow every time somebody wrote about pricing. **THE BLOCKQUOTE ARM WAS ADDED ON A MEASUREMENT AND THE MEASUREMENT WAS THIS ENTRY.** Not one labelled price in the corpus sits on a `>` line, while blockquotes are used throughout it to reproduce another document's words; section 5 below quotes the item this row takes at length, and the first derivation counted that quotation as a forty-ninth price. **The cost is stated rather than hidden: a price WRITTEN inside a blockquote would be invisible, exactly as one written inside backticks is.** **The scope is `docs/decisions/ADR-*.md` and nothing else**: a price quoted into [ALLOCATION](ALLOCATION.md) by a dispatcher or into a session log by the row that wrote it is a quotation of a price set in an entry, not a second price, and both currently exist.

---

### 3. The anchor, and why it is a line rather than a label

**A ROW IS KEYED `ADR-NNN:LINE` AND ITS SECTION AND ITEM ARE DERIVED BESIDE IT.** The section is the nearest preceding `### N.` heading; the item is a leading `N.` **on the price's own line** and nowhere else.

**THE SAME-LINE RULE WAS FORCED BY A CASE AND NOT CHOSEN.** A first derivation carried the last numbered item forward across paragraphs and put [ADR-458](ADR-458.md):104 at `6.3`. [ADR-461](ADR-461.md) cites it as **section 6 with no item**, and reading the entry settles it: the clause sits in a paragraph that FOLLOWS the numbered list, opening `**IT IS NOT REPAIRED HERE AND THE REASON IS THE CONSTRAINT, NOT THE APPETITE.**` With the rule corrected, **the derivation reproduces all forty-one of [ADR-461](ADR-461.md) section 3's section-and-item labels, one for one, including that one.** The join between the two was run on `(entry, where)` and every one of the forty-one matched; the only unmatched anchors are [ADR-461](ADR-461.md)'s own seven, which no census has scored.

**THE ANCHOR IS A LINE BECAUSE A DATED RECORD'S LINES DO NOT MOVE.** [ADR-386](ADR-386.md):169 rules that a pointer in a dated record is named rather than repaired, which means a line in an entry that has been written is stable in the way a heading number is not. It also means the register goes red rather than silently wrong if one ever does move, which is the behaviour [ADR-461](ADR-461.md) section 7 item 1 found missing when [ADR-459](ADR-459.md)'s prose pointer `:270` drifted to `:264` with nothing able to see it.

---

### 4. The state vocabulary, and the line between what a script decides and what a reader does

**[ADR-461](ADR-461.md)'s VERDICT COLUMN IS TWO QUESTIONS IN ONE CELL AND THIS ROW SPLITS THEM.** That entry's table carries `HOLDS`, `HOLDS, discharged`, `WRONG` and `UNCHECKABLE`. `HOLDS, discharged` is a compound because two independent things are being said: the work was taken, and the price described it correctly. They vary independently and the census contains the proof. **[ADR-445](ADR-445.md) 8.1 is WRONG and its work was TAKEN**: the guard that landed is `refuseGeneratedColumn`, in a different place and a different shape from the one priced. **[ADR-445](ADR-445.md) 8.2 is WRONG and half its work is still owed.** One column cannot carry both.

So the register carries two, each with a closed vocabulary and a stated default:

| Column     | Values                                        | Default      | What the default claims |
| ---------- | --------------------------------------------- | ------------ | ----------------------- |
| Obligation | `OPEN`, `DISCHARGED`, `WITHDRAWN`             | `OPEN`       | nothing                 |
| Accuracy   | `HOLDS`, `WRONG`, `UNCHECKABLE`, `UNASSESSED` | `UNASSESSED` | nothing                 |

**`UNASSESSED` IS THE STATE THIS ROW ADDS AND IT IS THE ONE WORTH HAVING.** [ADR-461](ADR-461.md)'s four values have no cell for a price nobody has read, because that entry read all forty-one. Seven prices in this tree have never been checked by anybody: [ADR-461](ADR-461.md)'s own. Without `UNASSESSED` the register would either omit them or score them, and both are worse than saying so. **The register's most useful column on the day it lands is the one that says which load-bearing claims are unread.**

**WHAT THE CHECKER DECIDES**, and it decides nothing else:

1. **Leg A, the population.** Every derived price has a row and every row names a derived price, in both directions, with no anchor listed twice. Derivable, and it is the motivating failure: a price nothing can see is the whole of [ADR-461](ADR-461.md) section 7 item 2.
2. **Leg B, the anchors.** Each row's section and item are the ones the entry gives, derived independently on every run. Derivable, and it is what catches a row copied from another entry's table rather than derived at this base.
3. **Leg C, the paths.** Every repo path a price clause links resolves, and where the link label ends `:NNN` the file is at least that long. Derivable. An unresolved path must be declared with a `SETTLED` or `OPEN` reason, on [`write-guard-set.mjs`](../../packages/tooling/checks/write-guard-set.mjs)'s precedent, because a price may legitimately name a file whose creation is the thing it buys.
4. **Leg D, the states.** Every value is in its vocabulary, and every value other than the two defaults carries a basis naming an entry that exists. **This leg checks that a reading was WRITTEN DOWN and cites somebody. It does not check the reading.**

**WHAT ONLY A READER DECIDES.** Whether a price is still owed is a question about work, not about text: [ADR-458](ADR-458.md) 11.1's expression-shaped guard was answered by a `void` call at [`scoped-db.ts:2211`](../../packages/db/src/scoped-db.ts), which no diff of the clause would reveal. Whether a price describes the tree took [ADR-461](ADR-461.md) a whole row. **A checker that guessed either would be a green check that is not checking anything**, which is the failure `RI-30`'s own header names, and the register would be worth less than the prose it replaced. So the states in [PRICE_REGISTER.md](PRICE_REGISTER.md) for the forty-one are [ADR-461](ADR-461.md)'s verdicts, cited row by row to its section 3, 4, 5 or 6, and the obligations for its ten WRONG prices are read off its section 4 one at a time. **This row ran no second census and claims none.**

**NO TOTAL IS WRITTEN IN THE REGISTER.** The tallies are printed by the checker on every PASS, which is the only place they cannot rot. Section 6 quotes the line.

---

### 5. What ADR-461 section 10 item 4 asked for, and which half of it landed

The item priced two options and this row took the second, as dispatched:

> **Price: a `CI-06` letter or an `RI-nn` asserting that every labelled price names a path that exists, which is the cheapest useful leg and would have caught nothing in this census, or a register span listing all 41 with their state, which would have caught six of the ten.**

**BOTH LANDED, AND THE FIRST IS FREE ONCE THE SECOND EXISTS.** Leg C is the cheapest useful leg, built as a leg of the register's checker rather than as a `CI-06` letter or an `RI-nn`. That placement is the dispatch's fence decision and its reason is arithmetic rather than taste: rows 462, 463 and 465 are each reporting the gate and invariant counts from their own branches, and a row that moved either count would turn three clean integrations into a reconciliation.

**THE ITEM'S OWN CLAIM ABOUT WHAT A REGISTER WOULD HAVE CAUGHT IS NOT ASSESSED HERE AND ITS ROW SAYS `UNASSESSED`.** _"Would have caught six of the ten"_ is a counterfactual about a control that did not exist, and no state of this tree makes it true or false. Scoring it would be exactly the error this entry is built to make harder.

**ITS OBLIGATION IS `DISCHARGED` AND THE BASIS IS THIS ENTRY.** The register describes the tree it ships in, which is the rule that makes it maintainable, and on this branch the register is in the tree.

---

### 6. The control, watched firing on this row's own change

**THE SEEDS WENT INTO THE REGISTER, WHICH IS THIS ROW'S OWN FILE, AND NOT INTO AN ENTRY.** Seeding a missing price by editing [ADR-445](ADR-445.md) would be editing a dated record, which [ADR-386](ADR-386.md):169 forbids and this row's fence forbids twice. Three seeds went in together so that three legs would be named separately in one run:

1. **Leg A**, the row for `ADR-459:188` deleted.
2. **Leg B**, the row for `ADR-459:189` moved from section 9.5 to 9.4.
3. **Leg D**, the basis stripped from `ADR-452:206`, which is marked `WRONG`.

**EACH SEED WAS LOCATED IN THE DIFF AND NOT BY GREP**, with `git diff -U6 -- docs/decisions/PRICE_REGISTER.md`, which showed one deletion at row 40, one cell changed at row 41 and one cell changed at row 23, and nothing else. [ADR-460](ADR-460.md)'s row falsified the wrong line by grepping for a string that occurred elsewhere, and this is that advice acted on.

**THE RED OUTPUT, QUOTED:**

```
FAIL   the price register is not the corpus's prices (3)
       leg A: ADR-459:188 sets a price and the register does not list it. Add a row with its obligation and its accuracy, or say in the register why the idiom matched something that is not a price.
       leg B: the register places ADR-459:189 at section 9.4 and the entry puts it at 9.5. The section is the nearest preceding "### N." heading and the item is a leading "N." on the price's own line; both are derived on every run and neither is read from this table.
       leg D: ADR-452:206 is marked OPEN/WRONG and cites nobody. OPEN and UNASSESSED are the states that claim nothing; every other state is a reading, and a reading with no basis is the thing this register exists to replace.
```

**THREE SUITE CASES WENT RED IN THE SAME STATE, WHICH IS THE EVIDENCE THE CHECK RUNS THROUGH `CI-02`.** `every leg holds on the tree this check ships with` reported `expected 1 to be +0`, and `the corpus prices things, so the check is not passing on an empty set` reported a 47-element array against a 48-element one. Nothing in this tree runs the checker from a workflow, exactly as [`write-guard-set.mjs`](../../packages/tooling/checks/write-guard-set.mjs) is not run from one; the suite file is the wiring and [ADR-458](ADR-458.md) section 11 item 4's owed workflow entry now covers two checkers rather than one.

**THE SEED FOUND A DEFECT IN THIS ROW'S OWN TEST, WHICH IS RECORDED RATHER THAN TIDIED AWAY.** A third case went red that had nothing to do with the seeds: `the vocabularies are closed and are the ones the register documents` asserted a legend row as the literal string `` `| ` `` plus the value plus `` ` |` ``, which passed when it was written and stopped being true the moment Prettier padded the register's table cells to the column width. **It had been green in the only run before the formatter touched the file and would have shipped asserting the formatter rather than the register.** It was rewritten to match the row rather than the padding, and then **watched red on its own seed**: renaming `WITHDRAWN` to `RETRACTED` in the legend produced `the register does not document WITHDRAWN: expected ... to match /^\|\s*`WITHDRAWN`\s*\|/m`, and the legend was restored.

**AND THEN LEG A FIRED WITHOUT A SEED, ON THIS ENTRY.** Writing section 8 below priced seven things under the very idiom this row had just enumerated, and the next run of the checker went RED on seven findings, one per clause, before anybody thought to add a row:

```
FAIL   the price register is not the corpus's prices (7)
       leg A: ADR-464:151 sets a price and the register does not list it. Add a row with its obligation and its accuracy, or say in the register why the idiom matched something that is not a price.
       leg A: ADR-464:153 sets a price and the register does not list it. Add a row with its obligation and its accuracy, or say in the register why the idiom matched something that is not a price.
       leg A: ADR-464:155 sets a price and the register does not list it. Add a row with its obligation and its accuracy, or say in the register why the idiom matched something that is not a price.
```

**THE QUOTED LINE NUMBERS ARE THE ONES THAT RUN PRINTED AND THEY HAVE SINCE MOVED**, because writing this paragraph pushed section 8 down eleven lines. The next run said so, in leg A's other direction, on all seven rows at once: _"the register's row at :132 lists ADR-464:151 and no price is derived there."_ **That is the anchor doing its job on the smallest possible edit**, and it is the whole argument for keying a row to a line rather than to a heading number that would not have moved.

**THAT IS THE MOTIVATING FAILURE HAPPENING RATHER THAN BEING SIMULATED**, and it happened to the entry that built the control, four minutes after the control existed. Seven rows were added, `OPEN` and `UNASSESSED`, which is what a price nobody has read is. **It is also the answer to the obvious objection that a register is a thing people forget to update**: forgetting is what this run did, and the check is what noticed.

**THE RESTORE IS BYTE-IDENTICAL AND WAS PROVED RATHER THAN ASSUMED.** An `md5sum` of the register was taken before the first seed and verified after each restore; `git status --porcelain` reports the tree clean and the checker reports PASS. Leg C is the one leg NOT watched red on the real tree, because seeding it would mean editing a price clause in a dated record. It is watched red four ways on a fabricated corpus in the suite: an unresolved path, an unresolved path declared without a `SETTLED` or `OPEN` class, a cited line past the end of its file, and an `http` link correctly not treated as a repo path.

---

### 7. Every leg, re-run after the last edit

| Leg                                                   | Result                                                                                                                              |
| ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `node scripts/corpus/gates.mjs generate` then `check` | **33 of 33, and the working tree is clean after `generate`**                                                                        |
| `node packages/tooling/checks/repo-invariants.mjs`    | **35 of 35**                                                                                                                        |
| `node packages/tooling/checks/write-guard-set.mjs`    | **PASS, 9 guards, 6 builders, 10 declared cells, 4 `OPEN`**                                                                         |
| `node packages/tooling/checks/price-register.mjs`     | **PASS, 55 prices over 9 entries, 55 rows, 55 anchors, 23 path citations over 15 prices, 9 of them line-numbered and all in range** |
| `pnpm vitest run`                                     | **347 files PASSED of 347; 8692 PASSED, 11 skipped, 8703 TOTAL, 0 failed**                                                          |
| `pnpm run typecheck`                                  | **exit 0**                                                                                                                          |
| `pnpm run lint`                                       | **exit 0**                                                                                                                          |
| `pnpm run format:check`                               | **clean**                                                                                                                           |

**THE SUITE DELTA IS THIS ROW ENTIRE AND IT RECONCILES.** The base was **346 files, 8659 PASSED, 11 skipped, 8670 TOTAL**; the head is **347, 8692, 11, 8703**. That is **one file and 33 cases**, which is [`price-register.test.ts`](../../packages/tooling/test/price-register.test.ts) and nothing else, and **the skip count is unchanged**, which is the cross-check that no case was quarantined to get here.

**GATES AND INVARIANTS ARE UNMOVED AT 33 AND 35**, which is the fence decision's whole purpose: rows 462, 463 and 465 are each reporting those two numbers from their own branches and none of them has to reconcile against this one.

**ONE GATE WENT RED DURING THE RUN AND IT WAS THIS ROW'S FAULT.** `CI-06/table-row-width` reported `docs/sessions/README.md:882: this row has 4 cell(s) where its table's delimiter declares 2`. The session log's `<!--index:` line quoted a markdown table row verbatim, carrying two unescaped pipes into a generated registry row, and a long row has its excess cells **silently discarded on render**. The quotation was rephrased rather than escaped, because the sentence did not need the literal. **This is a gate catching a defect that no reader of the rendered file could have seen**, and it is recorded because the same shape will arrive again the next time an entry quotes a table.

**`pnpm run verify` AND `scripts/corpus/falsify.mjs` WERE NOT RUN.** Both mutate the working tree and both are forbidden to this row.

---

### 8. What is owed

1. **[ADR-461](ADR-461.md)'s OWN SEVEN PRICES HAVE NEVER BEEN CHECKED BY ANYBODY** and the register carries all seven as `UNASSESSED`. That is the register's first useful output on the day it lands: it names, without anybody having to read eight entries, exactly which load-bearing claims are unread. **Price: one row, a census over seven clauses on the model of [ADR-461](ADR-461.md) section 3, which is a seventh of that entry's work because the population is already derived and anchored.**

2. **THE COMMAND [ADR-461](ADR-461.md) SECTION 2 PUBLISHES IS SHORT BY THREE AND IS NOT REPAIRED.** Section 2 above. It is a dated record and [ADR-386](ADR-386.md):169 governs. **Price: none, and there is nothing to take. It is recorded so that a later row reaching for that command reaches for the checker instead.**

3. **[ADR-461](ADR-461.md)'s SUMMARY ARITHMETIC DISAGREES WITH ITS OWN TABLE AND IS NOT REPAIRED.** Its prose reads _"31 HOLDS, 10 WRONG, 1 UNCHECKABLE, of 41"_, which sums to 42; its table parses to **23 `HOLDS` plus 7 `HOLDS, discharged`**, so the figure is **30**. Its heading and summary say **nine** discharged and both its table and its section 5 enumeration give **seven**. **Price: none, same reason as item 2.** The register carries the table's figures because the table is the half that was derived.

4. **NOTHING ASSERTS THAT A PRICE'S STATE IS CURRENT AND NOTHING IN THIS DESIGN CAN.** Section 4. A price marked `OPEN` whose work landed yesterday stays `OPEN` until a reader moves it. **Price: the honest options are a per-row `assessed at` commit and a leg that goes red when the entry's cited files have moved since it, which is a real design and a real row, or nothing. This entry proposes neither and names the hole instead**, because a staleness leg that guessed would put the register back in the position the prose was in.

5. **LEG C REACHES 15 OF THE 55 PRICES AT THIS HEAD.** 23 path citations, all resolving, all in range. A price naming a file in a bare code span with no link is invisible to it, which is [ADR-461](ADR-461.md) section 7 item 1's finding arriving in a second place. **Price: that entry's section 10 item 3, unchanged, which is the citation-set widening nobody owns.**

6. **NO WORKFLOW RUNS EITHER CHECKER DIRECTLY.** [ADR-458](ADR-458.md) section 11 item 4 owes one entry for [`write-guard-set.mjs`](../../packages/tooling/checks/write-guard-set.mjs) and this row adds a second file with the same shape. **Price: that entry's, widened from one step to two, and still one row that owns a workflow file.**

7. **NOTHING HERE ANSWERS [ADR-437](ADR-437.md) SECTION 13**, and nothing here settles [ADR-445](ADR-445.md), [ADR-448](ADR-448.md), [ADR-451](ADR-451.md), [ADR-452](ADR-452.md), [ADR-454](ADR-454.md), [ADR-456](ADR-456.md), [ADR-458](ADR-458.md), [ADR-459](ADR-459.md), [ADR-460](ADR-460.md) or [ADR-461](ADR-461.md), all `proposed` and unsigned. **Price: a founder ruling, which no row can substitute for.**

---

### 9. What this row did NOT do

1. **IT REPAIRED NO PRICE.** The ten [ADR-461](ADR-461.md) proves wrong are RECORDED with their state and their basis and not one clause is edited. Seven sit in dated records.
2. **IT RAN NO SECOND CENSUS AND CLAIMS NONE.** Every accuracy verdict in the register for the forty-one is [ADR-461](ADR-461.md)'s, cited row by row to the section that reached it. The only tree-side fact this row checked itself is [`scoped-db.ts:2211`](../../packages/db/src/scoped-db.ts), read to settle whether [ADR-458](ADR-458.md)'s section 6 and 11.1 obligations are `DISCHARGED`: the call is there and it is the `void` spelling, which is [ADR-460](ADR-460.md)'s and not the expression-shaped one those items priced.
3. **IT MINTED NO `RI-nn`, NO `CI-06` LETTER AND NO GATE.** Invariants stay at 35 and gates at 33.
4. **IT TOUCHED NO WORKFLOW.** Not [`ci.yml`](../../.github/workflows/ci.yml), not [`corpus.yml`](../../.github/workflows/corpus.yml). [ADR-461](ADR-461.md) section 10 item 2 is not this row's and is not taken.
5. **IT TOUCHED NO OTHER ROW'S FILE.** [`gates.mjs`](../../scripts/corpus/gates.mjs), [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs), [`write-guard-set.mjs`](../../packages/tooling/checks/write-guard-set.mjs), `packages/eslint-plugin-merit/**` and [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) were READ AND NOT WRITTEN. No migration was opened.
6. **IT CHANGED NO PROSE IN [STATE.md](../STATE.md).** The one line that moved is the generated `adr_count` span, from 444 to 445, which is `generate`'s output and this entry's own arrival.
7. **IT DID NOT PIN A TOTAL ANYWHERE A READER WOULD TRUST.** Not in the register, not in the checker, and not in the suite, whose real-corpus case asserts the two sets are equal rather than that either is 55. Rows land beside this one and each may price something.
8. **IT DID NOT OPEN A PULL REQUEST, MERGE ANYTHING, OR SIGN AN ADR.**

---

### 10. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.**

**What a founder read adds and this entry cannot.**

**THE FIRST THING IS THAT THE STATE COLUMNS ARE A JUDGEMENT AND THE CHECKER CANNOT DEFEND THEM.** Every accuracy verdict for the forty-one is [ADR-461](ADR-461.md)'s, and every obligation for its ten wrong prices is this row's reading of that entry's section 4, taken one at a time. **The reading that will be argued with is [ADR-458](ADR-458.md) section 6 and 11.1 marked `DISCHARGED` while their accuracy is `WRONG`**: the work was taken and taken in a different shape from the one priced, which is precisely the case the two-column split exists to express and precisely the case a reader may think should have stayed one. Section 4 is where the split would be undone.

**THE SECOND IS THAT A REGISTER WHOSE STATE NOBODY REFRESHES IS WORSE THAN NO REGISTER**, because it reads as checked. Section 8 item 4 names that hole and refuses to close it with a leg that guesses. A founder who wants the guarantee stronger is asking for the `assessed at` design, and it is a row rather than a line.

**THE THIRD IS THAT THIS ENTRY PRICES SEVEN THINGS UNDER THE VERY IDIOM IT HAS JUST FOUND WRONG TEN TIMES IN FORTY-ONE.** [ADR-461](ADR-461.md)'s own fourth reading said the honest statement is that nothing in the tree will tell the next row whether such a price is real. **That is now half false and the half matters**: nothing here checks whether section 8 item 1 really costs one row, but for the first time the claim has a row, an anchor, a state and a place for somebody to write down that it was wrong.

- Approval: `___________________________` (UNSIGNED. **NON-MONEY DIFF, MONEY-ADJACENT SUBJECT, `E2` READ OWED.** No money-path file was opened for writing and no executable line under `packages/db`, `apps/` or `scripts/` changed. **The `E2` read is owed on the register itself rather than on the diff**: [PRICE_REGISTER.md](PRICE_REGISTER.md) carries a state for every price set on the money-path accessor, and a wrong `DISCHARGED` in that table is a debt a later row will believe is paid. It inherits [ADR-437](ADR-437.md)'s, [ADR-445](ADR-445.md)'s, [ADR-448](ADR-448.md)'s, [ADR-451](ADR-451.md)'s, [ADR-452](ADR-452.md)'s, [ADR-454](ADR-454.md)'s, [ADR-456](ADR-456.md)'s, [ADR-458](ADR-458.md)'s, [ADR-459](ADR-459.md)'s, [ADR-460](ADR-460.md)'s and [ADR-461](ADR-461.md)'s unsigned debts and settles none of them. Merging is not signing.)

|             |     |
| ----------- | --- |
| Approved by |     |
| Date        |     |
