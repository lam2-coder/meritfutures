## ADR-468: the budget is sixty and not thirty because two of the four factors measure differently here, the population is sixty-seven cases and not fourteen because it is derived from inputs rather than from a clock, and three of the eight files that walk this tree name `REPO_ROOT` nowhere (2026-09-08, status: proposed)

**Row 468 of the 448s wave. Branch `claude/scan-budgets-everywhere`, off `7f7ba904c7a052a1dd657e119d872425e6f7bc42`, session 663. EIGHT TEST FILES, ONE NEW MODULE HOLDING ONE CONSTANT, NO ASSERTION MOVED.**

**THE SUBJECT WAS SET BY THE DISPATCHER AND IT IS [ADR-466](ADR-466.md) SECTION 10 ITEM 1**, quoted: *"FOURTEEN CASES IN FIVE FILES SCAN THE TREE UNDER THE 5000ms DEFAULT AND ONE OF THEM HAS ALREADY GONE RED."*

**THE RED REPRODUCED ON THIS ROW'S OWN BASE, BEFORE ANY EDIT, AND IT IS THE SAME CASE.** In the second of two full-suite runs under four background CPU spinners at `7f7ba90`, [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts)'s *"RI-35 Every registered absence claim still names something absent"* **ABORTED at 5212ms**. [ADR-466](ADR-466.md) recorded 5402ms for the same case on a different container. **This is a second independent observation of the same failure and not a restatement of the first.**

**THE POPULATION IS SIXTY-SEVEN CASES OVER THIRTY-THREE CALL SITES IN EIGHT FILES, NOT FOURTEEN IN FIVE, AND THE DIFFERENCE IS A METHOD DIFFERENCE RATHER THAN A TREE THAT MOVED.** [ADR-466](ADR-466.md) surveyed by wall-clock: every case outside its own file that exceeded 1000ms under load. That question is *who is near the wall*. This row asks *whose cost grows with the repository*, which is the question a budget is actually about, and the two answers differ in both directions. Section 3 derives it and states the command.

**THREE OF THE EIGHT FILES THAT WALK THIS TREE NAME `REPO_ROOT` NOWHERE, AND ONE FILE THAT NAMES IT WALKS NOTHING IN THREE OF ITS CASES.** `git grep -l REPO_ROOT` over the fence returns **five** files; the true count is **eight**. [`generated-column-writes.test.ts`](../../packages/tooling/test/generated-column-writes.test.ts) reaches the tree through a DEFAULT PARAMETER, [`absence-grammar-census.test.ts`](../../packages/tooling/test/absence-grammar-census.test.ts) through a root it computes from `import.meta.url`, and [`price-register.test.ts`](../../packages/tooling/test/price-register.test.ts) through a default spelled `DECISIONS`, which a search for the identifier misses even in the checkers. Section 3.2.

**THE BUDGET IS 60000ms AND TWO OF [ADR-466](ADR-466.md)'s FOUR FACTORS DO NOT REPRODUCE HERE.** Contention measures **x1.9** across 32 population members where that entry measured **x1.61** from one case, and the per-megabyte cost of the scan **does not fall as the scope grows**, so its stated five-fold horizon buys **x5.0** rather than its x3.2. Its machine factor was rounded DOWN from 2.16 to 2.0 and is carried here at **x2.21** un-rounded, because rounding a slower-machine factor down is the direction that makes the budget too small. **2797 x 1.9 x 2.21 x 5.0 = 58,723, rounded up.** Section 5. **The number was re-derived and it is not 30000, so it was not reused.**

**AND THE GROWTH FRAMING IS CORRECTED IN THE OTHER DIRECTION FROM [ADR-466](ADR-466.md)'s CORRECTION.** That entry falsified [ADR-465](ADR-465.md)'s stated mechanism, the corpus gaining an ADR per row, and it was right about `ri35`: cloning the corpus from 36.6MB to 95.2MB moves `ri35.run` by a factor of **1.10** for a factor of **2.60** of corpus, re-measured here. **But this population is wider than that one and it does hold members whose input IS the corpus**: over the same clone `RI-16` moves 959ms to 1372ms and the absence-grammar census 635ms to 1276ms. **Both mechanisms are live and only one of them sets the number.** Section 5.3.

**THE CONSTANT MOVED OUT OF A TEST FILE AND INTO A MODULE, WHICH IS A DESIGN DECISION AND IS ARGUED RATHER THAN ASSUMED.** One scan is called from two files: `ri35.run(REPO_ROOT)` runs in [`absence-claims.test.ts`](../../packages/tooling/test/absence-claims.test.ts) and again in [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts) through `CHECKS`, and `RI-18` runs in [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts) and again in [`response-shape-copies.test.ts`](../../packages/tooling/test/response-shape-copies.test.ts). **A per-FILE figure hands ONE scan TWO budgets according to which suite called it.** Section 6.

**NO ASSERTION MOVED, AND THE PROOF IS MECHANICAL.** The **63** assertion-bearing lines added and the **63** removed are an identical multiset, and `git diff -w` over the whole fence contains exactly **one** line that is not a comment, an import, a `test(` opener or the budget scaffolding: the deletion of `const CORPUS_SCAN_MS = 30_000;`, which is the constant that moved. Section 4.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `468`, session **663**, branch `claude/scan-budgets-everywhere`, off `7f7ba904`, reserved by the dispatcher at hand-out time with its subject set: [ADR-466](ADR-466.md) section 10 item 1.
- **Fence, BY FILE:** [`packages/tooling/test/`](../../packages/tooling/test) entire (nine files touched: eight suites and one new module, [`scan-budget.ts`](../../packages/tooling/test/scan-budget.ts)), this file, [decisions/README](README.md), [INDEX](../INDEX.md), [sessions/README](../sessions/README.md), [`docs/sessions/2026-09-08-session-663.md`](../sessions/2026-09-08-session-663.md), [ALLOCATION](ALLOCATION.md) at row `468`'s result cell only, and [STATE](../STATE.md) for generated output only. **NO CHECKER WAS OPENED FOR WRITING.** Every module under [`packages/tooling/checks/`](../../packages/tooling/checks) was read and none was written. **NO `packages/eslint-plugin-merit` FILE** (row `469`). **NO MIGRATION. NO GLOBAL VITEST CONFIGURATION**: [`vitest.config.ts`](../../vitest.config.ts) is byte-identical and absent from the diff, which is the whole reason this budget is spelled per case.
- **Money:** **NON-MONEY DIFF.** Test files and one constant. No executable line in any shipped module is added, changed or deleted. This row states no money arithmetic.
- **`PRICE_REGISTER.md` IS UNTOUCHED AND THAT IS DELIBERATE, AND IT IS WHY THIS BRANCH LANDS WITH ONE RED CHECK, EXPECTEDLY.** Section 9 records it. The integrator registers section 10's anchors as `OPEN` / `UNASSESSED`.

---

### 1. The container, the base, and everything measured before the first edit

**THE REPOSITORY WAS PRESENT AND `node_modules` WAS ABSENT.** `pnpm install --frozen-lockfile` ran to completion before any measurement was taken. Base `7f7ba904c7a052a1dd657e119d872425e6f7bc42`, which is the dispatcher's reservation of rows 468 and 469 on top of [ADR-466](ADR-466.md)'s landing and the registration of its seven prices. Container: **4 CPU**, 15GB, Node v22.22.2, pnpm 10.33.0, vitest 4.1.10.

**THE TREE AS THIS ENTRY MEASURES IT.** The denominator is stated because every per-megabyte figure below depends on it and [ADR-466](ADR-466.md) used a different one. This is a walk from the repository root over `.ts`, `.tsx`, `.mts`, `.mjs` and `.js`, skipping `node_modules`, `.git`, `dist`, `build`, `coverage` and `.next`, and it INCLUDES test files, which the checkers' own `shippedSources` scope does not:

| Input | At `7f7ba904` |
| --- | --- |
| source-like files, the growth denominator used here | **789** file(s), **18,084,541** byte(s) |
| non-test source-like files | 416 file(s), 9,881,014 byte(s) |
| `docs/**/*.md` | **1,499** file(s), **36,557,502** byte(s) |
| of those, `ADR-nnn.md` | **450** file(s), 11,725,223 byte(s), which is **26,056** byte(s) per ADR |
| `.sql` | 99 file(s), 1,653,908 byte(s) |

**THE BASE, BEFORE THE FIRST EDIT.** `gates.mjs generate` left a clean tree; **33 of 33** gates; **35 of 35** invariants; `write-guard-set.mjs` PASS; `price-register.mjs` PASS; typecheck exit 0; lint exit 0; `format:check` clean.

| Base full-suite run | Files | PASSED | skipped | TOTAL | failed | wall |
| --- | --- | --- | --- | --- | --- | --- |
| unloaded 1 | 347 of 347 | 8713 | 11 | 8724 | 0 | 82s |
| unloaded 2 | 347 of 347 | 8713 | 11 | 8724 | 0 | 84s |
| loaded 1 | 347 of 347 | 8713 | 11 | 8724 | 0 | 149s |
| **loaded 2** | 346 of 347 | 8712 | 11 | 8724 | **1** | 146s |

**LOADED MEANS FOUR BACKGROUND CPU SPINNERS ON FOUR CORES**, which is [ADR-466](ADR-466.md)'s method, reused so the two entries' contention figures are comparable.

**THE ONE FAILURE IS THE DISPATCHED SUBJECT AND IT IS THIS ROW'S TO FIX.** [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts)'s *"RI-35 Every registered absence claim still names something absent"* aborted at **5212ms** against the 5000ms default. It is generated by a `test.each` whose body is `expect(findings(id, REPO_ROOT)).toEqual([])`, so what it runs is `ri35.run(REPO_ROOT)`: the same scan [ADR-466](ADR-466.md) was commissioned over, reached from a different file.

**AND [ADR-466](ADR-466.md)'s OWN CHANGE IS ALREADY EARNING ITS KEEP IN THIS ROW'S BASE MEASUREMENT.** In loaded run 1, *"the widening moves no verdict over this repository"* reached **5103ms and passed**. At the default it would have been the second red in the same run.

---

### 2. Re-deriving what the dispatch handed me, including the parts it told me not to trust

**THE 5000ms DEFAULT STILL HOLDS AND IT IS STILL A DEFAULT RATHER THAN A CHOICE.** `git grep -n testTimeout` over the tree returns exactly the sites [ADR-466](ADR-466.md) created and nothing else. Neither [`vitest.config.ts`](../../vitest.config.ts) nor [`packages/rules-engine/vitest.config.ts`](../../packages/rules-engine/vitest.config.ts) sets one. **No file in this repository has ever chosen this number.**

**THE `CORPUS_SCAN_MS = 30_000` CONSTANT IS WHERE THE DISPATCH SAID.** [`absence-claims.test.ts`](../../packages/tooling/test/absence-claims.test.ts) declared it and passed it to seven `test()` calls at the base, verified by reading rather than taken from the dispatch.

**"FOURTEEN CASES IN FIVE FILES" IS [ADR-466](ADR-466.md)'s FIGURE AT ITS OWN BASE AND IT IS NOT WRONG; IT ANSWERS A DIFFERENT QUESTION.** Its section 7 states the method in terms: *"the survey is every case OUTSIDE this file that exceeded 1000ms in the pre-edit run under four background CPU spinners"*. That is a threshold on a clock. Section 3 below derives a population from inputs instead, and section 3.3 reconciles the two counts case by case.

**THE 6183ms FROM [ADR-465](ADR-465.md) DID NOT REPRODUCE HERE EITHER, AND IT IS THE ONLY NUMBER IN THIS ENTRY THAT IS NOT THIS CONTAINER'S.** In the same position this container reports **2703ms and 2797ms** across two unloaded runs. 6183 / 2797 = **2.21**, and since 6183 is an ABORT rather than a completion the true factor is worse. **It is used as a machine-variance datapoint and it is not restated as mine.** Section 12 names what a founder should do if that borrowing is unacceptable.

**AND THE DISPATCHER IS NOT A PRIMARY SOURCE EITHER.** Every figure it handed over was checked at source: the constant, the file it sits in, the default, the five files, and the aborted case. All hold. The population count does not, for the reason above, and the correction is upward.

---

### 3. The population, re-derived from inputs, with the command

#### 3.1 The rule

**A CASE BELONGS TO THE POPULATION WHEN IT WALKS A DIRECTORY OF THIS REPOSITORY THAT IT DOES NOT NAME**, so its cost grows as the repository grows. A case that reads files a register names, or imports two modules by path, does not: its cost scales with the register or with the module.

**THIS IS [ADR-466](ADR-466.md)'s OWN STATED PRINCIPLE, *"the budget belongs to the INPUT"*, APPLIED WHERE ITS OWN DISCRIMINATOR STOPS WORKING.** In that file the two groups separated by a factor of twelve, 328ms against 27ms, and sorting by milliseconds gave the same answer as reading the calls. **Over the whole directory it does not.** The measured costs run continuously from 5212ms down to 1ms with no gap anywhere, because [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts) reads ONE kind of input at thirty-five different depths inside a single `test.each`. A clock threshold puts `RI-08` at 1ms and `RI-35` at 5212ms in different populations while both are one invariant over one tree, generated by one call site.

#### 3.2 The command, and why a single grep is not it

**THE NAIVE COMMAND, AND ITS ANSWER, WHICH IS WRONG BY THREE FILES:**

```
git grep -l 'REPO_ROOT' -- 'packages/tooling/test/*.test.ts'
```

returns **five**: `absence-claims`, `dependants`, `repo-invariants`, `response-shape-copies`, `strip-comments`.

**THE DERIVATION ACTUALLY USED IS TWO STEPS, BECAUSE THE ROOT REACHES A CASE IN THREE FORMS AND ONLY ONE OF THEM CARRIES THAT NAME.** Step A enumerates every exported function in the checkers whose parameter DEFAULTS to a real repository path:

```
grep -nE '^export (async )?function [a-zA-Z0-9_]+\([^)]*=' packages/tooling/checks/*.mjs
```

which returns 24 declarations, of which the ones defaulting to a repository path are `absence-grammar-census.mjs:main`, `dependants.mjs:main`, `generated-column-writes.mjs`'s `derivedSet`, `builtStatements`, `refusedWrites` and `literalStatements`, `price-register.mjs`'s `derive`, `readRegister`, `legPaths` and `legStates`, `skipped-tests.mjs`'s `registerFindings`, `countsByFile` and `reportFindings`, and `write-guard-set.mjs`'s `derive` and `legConventionShared`. Step B reads every call site in the fence and classifies it, since a defaulted parameter is only a walk when the function walks.

**THE THREE FORMS, EACH FOUND IN THE FENCE:**

| Form | Where | Visible to a `REPO_ROOT` grep of the SUITE |
| --- | --- | --- |
| the imported `REPO_ROOT` constant, passed | 5 files | yes |
| a root computed locally from `import.meta.url` | [`absence-grammar-census.test.ts:305`](../../packages/tooling/test/absence-grammar-census.test.ts) | **no** |
| a DEFAULT PARAMETER, so the call has no root argument at all | [`generated-column-writes.test.ts`](../../packages/tooling/test/generated-column-writes.test.ts), [`price-register.test.ts`](../../packages/tooling/test/price-register.test.ts) | **no** |

**AND THE THIRD FORM DEFEATS A GREP OF THE CHECKERS TOO, NOT ONLY OF THE SUITES.** `price-register.mjs` spells its default `DECISIONS`, not `REPO_ROOT`, so a search for that identifier in `packages/tooling/checks/` misses `derive(dir = DECISIONS)`, which does `readdirSync` over the whole of `docs/decisions` and reads every entry file it finds. Two cases in `price-register.test.ts` walk the corpus through it and **neither the suite nor the checker names `REPO_ROOT` anywhere on that path.**

**GREP IS ALSO WRONG IN THE OTHER DIRECTION, AND THE FALSE POSITIVES ARE IN THE FILE WITH THE MOST TRUE ONES.** Three cases in [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts)'s runner-report block pass `REPO_ROOT` to a `transcript` helper whose checks are `held`, `violated` and `crashes` fixtures declared `run: (): string[] => []`. **They name the constant and scan nothing.** [`price-register.test.ts`](../../packages/tooling/test/price-register.test.ts)'s *"it takes no argument"* calls `run(['--fix'])`, which returns 2 before reading anything: **the walking function is called and no walk happens.** And [`write-guard-set.test.ts`](../../packages/tooling/test/write-guard-set.test.ts) reaches the real tree in every case and walks none of it, because `derive` and `legConventionShared` default to TWO NAMED FILES; its costliest case is **165ms** and it keeps the default.

#### 3.3 The answer, and how it differs from fourteen

**SIXTY-SEVEN CASES OVER THIRTY-THREE CALL SITES IN EIGHT FILES.** One of the thirty-three is a `test.each` generating thirty-five, so 32 + 35 = 67. Derived at reporting time by counting the third-argument sites in the fence and the generated cases in the run.

| File | sites | cases | reached by |
| --- | --- | --- | --- |
| [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts) | 5 | **39** | `REPO_ROOT`, one `test.each` over `CHECKS` generating 35 |
| [`absence-claims.test.ts`](../../packages/tooling/test/absence-claims.test.ts) | 7 | 7 | `REPO_ROOT`. [ADR-466](ADR-466.md)'s seven, re-pointed at the shared constant |
| [`generated-column-writes.test.ts`](../../packages/tooling/test/generated-column-writes.test.ts) | 7 | 7 | **default parameter, no `REPO_ROOT` in the file** |
| [`dependants.test.ts`](../../packages/tooling/test/dependants.test.ts) | 5 | 5 | `REPO_ROOT` |
| [`strip-comments.test.ts`](../../packages/tooling/test/strip-comments.test.ts) | 3 | 3 | `REPO_ROOT`, two of them through a module-level `sourceTree()` helper |
| [`absence-grammar-census.test.ts`](../../packages/tooling/test/absence-grammar-census.test.ts) | 3 | 3 | **`import.meta.url`, no `REPO_ROOT` in the file** |
| [`price-register.test.ts`](../../packages/tooling/test/price-register.test.ts) | 2 | 2 | **default `DECISIONS`, no `REPO_ROOT` in the file or the checker** |
| [`response-shape-copies.test.ts`](../../packages/tooling/test/response-shape-copies.test.ts) | 1 | 1 | `REPO_ROOT` |
| **total** | **33** | **67** | |

**THE THREE FILES IN THE FENCE THAT GET NOTHING, NAMED SO THE EXCLUSION IS CHECKABLE RATHER THAN IMPLIED:** [`write-guard-set.test.ts`](../../packages/tooling/test/write-guard-set.test.ts) (two named files, 165ms at worst), [`skipped-tests.test.ts`](../../packages/tooling/test/skipped-tests.test.ts) (`existsSync` over the rows its register names, 10ms at worst) and [`api-contract-endpoints.test.ts`](../../packages/tooling/test/api-contract-endpoints.test.ts) (one fixture file, 7ms at worst).

**WHY 67 AND NOT 14, ITEM BY ITEM.** [ADR-466](ADR-466.md)'s fourteen are all inside this sixty-seven; the sixty-seven adds fifty-three, and every one of them is a case whose cost grows with this repository and which its 1000ms threshold could not see:

| Added | Count | Why the clock missed it |
| --- | --- | --- |
| the rest of `repo-invariants.test.ts`'s `test.each` table | **31** | 31 of the 35 generated cases ran under 1000ms loaded. Every one is `CHECKS[i].run(REPO_ROOT)` |
| the seven in `absence-claims.test.ts` | 7 | excluded by [ADR-466](ADR-466.md) as its own file, correctly, and inside this fence |
| `generated-column-writes.test.ts` | 5 more | 2 were over the threshold; 5 more walk and were cheaper |
| four more `repo-invariants.test.ts` cases outside the table | 4 | 1ms to 86ms |
| `price-register.test.ts` | 2 | 216ms and 283ms loaded, a whole FILE the survey never reached |
| `strip-comments.test.ts` | 1 more | 274ms loaded, calling the same `worstVictim()` walk as the case beside it |
| `dependants.test.ts` | 1 more | 22ms, `population(REPO_ROOT)` |
| `response-shape-copies.test.ts` | 1 | 598ms loaded, a whole FILE the survey never reached |
| `absence-grammar-census.test.ts` | 1 more | 3 walk and the survey found 2 |

**[ADR-466](ADR-466.md) SECTION 7's OTHER EIGHT ARE STILL CORRECTLY EXCLUDED AND THEY ARE STILL OUT OF THIS FENCE.** Four are `apps/worker/test/entrypoint.test.ts` spawning a real process, two read one route directory, one reads one module and one is in memory. Their cost does not scale with the repository, so a corpus-sized budget is the wrong instrument for them and none is proposed. **They are also outside `packages/tooling/test/`, so this row could not have given them one.** Section 11 owes them.

---

### 4. That no assertion moved, proved mechanically rather than promised

**A GATE IS A CLAIM ABOUT THE TREE THAT CAN BE FALSE. A TIMEOUT IS NOT ONE.** The argument is [ADR-466](ADR-466.md) section 4's and it is not restated at length. What each of the sixty-seven asserts, what it reads, the scope it reads over and the verdict it computes are byte-identical either side of this diff.

**THE FIRST PROOF: THE MULTISET OF ASSERTION-BEARING LINES IS UNCHANGED.** Every added and removed line of `git diff -U0` over the fence carrying `expect`, `toEqual`, `toBe`, `toContain`, `toHaveLength`, `toThrow`, `toMatchObject`, `toBeGreaterThan`, `toBeGreaterThanOrEqual`, `toBeLessThanOrEqual`, `toBeDefined` or `assert` was collapsed to single spaces, trimmed and sorted:

```
assertion-bearing lines: ADDED 63, REMOVED 63
IDENTICAL: the multiset of assertion lines is unchanged.
```

**63 of them appear on both sides and none appears on one.** Whitespace is the only thing normalised away, and it is the only thing this change does to those lines: re-indenting a body by two columns is what moving it inside a longer argument list costs.

**THE SECOND PROOF IS STRONGER AND IT IS THE ONE TO READ.** `git diff -w -U0` over the whole fence, with comment lines, blank lines, `test(` and `test.each(` openers, title literals, arrow openers, the `},` / `CORPUS_SCAN_MS,` / `);` closers and the seven `import { CORPUS_SCAN_MS }` lines removed, leaves **exactly one line in the entire diff**:

```
-const CORPUS_SCAN_MS = 30_000;
```

which is the constant that moved into [`scan-budget.ts`](../../packages/tooling/test/scan-budget.ts). **There is no other executable change in this branch.**

**THE THIRD: NOT ONE SCAN CALL MOVED.** For each of the eight files, every line mentioning `REPO_ROOT`, a locally computed `REPO`, `derivedSet(`, `refusedWrites(`, `literalStatements(`, `builtStatements(`, `census(`, `sourceFilesUnder(`, `ri35.run(` or `ri18.run(` was extracted, stripped of whitespace, sorted and digested at base and at head. **Seven of the eight digest identically.** The eighth is `absence-claims.test.ts`, whose two extra lines are both COMMENT prose in the rewritten header, and all sixteen of its `REPO_ROOT` call sites are unchanged.

**THE FOUR THINGS THIS ROW REFUSED, EACH OF WHICH WOULD HAVE BEEN WEAKENING A GATE:**

1. **Skipping, disabling, quarantining or deleting a case.** The skip count is **11** before and after, on every run in this entry, and [`packages/tooling/checks/skipped-tests.mjs`](../../packages/tooling/checks/skipped-tests.mjs) is untouched. **No `CI-02` / `CI-03` / `CI-04` step is implicated** ([ADR-435](ADR-435.md), [ADR-439](ADR-439.md)).
2. **Narrowing what a case scans.** No case scans less than it did, and the digest above is how that is checked rather than asserted.
3. **Moving an assertion to a fixture tree.** Every one of the sixty-seven still reads the real repository.
4. **Setting a GLOBAL `testTimeout`**, which would have been a one-line diff and the worst one: it moves all 8,724 cases in this workspace, and it is out of fence besides.

**THE HONEST COST IS NAMED RATHER THAN HIDDEN, AND IT IS BIGGER HERE THAN IT WAS FOR [ADR-466](ADR-466.md).** Sixty-seven cases now have a wall twelve times the default instead of seven. Against a genuine hang, CI waits 60 seconds rather than 5, once, in the run where something is actually wedged. Against a case that has quietly become slow, nothing reports it: **5000ms was never designed as a performance signal and nobody in this tree chose it**, but it was doing that job by accident for sixty-seven cases and it now does it for none of them. That is what section 10 item 2 prices.

---

### 5. The budget, its four factors, and the growth assumption it encodes

**`CORPUS_SCAN_MS = 60_000`.** Every factor is measured on this container at this base except the machine factor, which is flagged in its own row and again in section 12.

| Factor | Value | How it was measured | [ADR-466](ADR-466.md) |
| --- | --- | --- | --- |
| Base | **2797ms** | the costliest member of the population in an unloaded full-suite run: *"RI-35 finds nothing"*, which is `ri35.run(REPO_ROOT)`, the same scan that ABORTED at 5212ms under load at this base | 2858ms |
| Contention | **x1.9** | MEDIAN over 32 population members, each measured unloaded and loaded at this base. p90 x2.14, max x2.22 | x1.61, from one case |
| Machine | **x2.21** | 6183 / 2797, where 6183ms is [ADR-465](ADR-465.md)'s figure in the same position. **NOT rounded down.** 6183 is an ABORT, so it is a lower bound on that container's true cost | x2.0, rounded DOWN from 2.16 |
| Growth | **x5.0** | for five times today's source scope, on a MEASURED exponent of 1.00. Section 5.2 | x3.2, on a falling per-megabyte rate |
| Product | **58,723ms** | 2797 x 1.9 x 2.21 x 5.0 | 29,450 |
| **Chosen** | **60,000ms** | the product rounded up to a number a reader can hold | 30,000 |

**THE TWO DERIVATIONS AGREE, WHICH IS WHY THIS IS A CORRECTION RATHER THAN A DISAGREEMENT.** Take [ADR-466](ADR-466.md)'s own base and its own contention and machine factors and change only the growth exponent to the one measured here: **2858 x 1.61 x 2.0 x 5.0 = 46,014**. Take this row's factors throughout: 58,723. **On its own stated five-fold horizon, ADR-466's constant is under-sized on this container**, and the gap is the growth exponent plus the two rounding choices. The conclusion of that entry is untouched and only its arithmetic moves.

#### 5.1 Contention, measured across the population rather than from one case

Thirty-two members of the population that cost over 200ms unloaded, each taken at its maximum across two unloaded runs and two loaded runs. The distribution:

| | ratio |
| --- | --- |
| minimum | x0.87 (`RI-16`, whose unloaded figure was itself an outlier) |
| median | **x1.89** |
| mean | x1.78 |
| p90 | x2.14 |
| maximum | x2.22 (*"the two modes agree on length and on every newline, over the whole tree"*) |

**`ri35` ITSELF SITS AT x1.76 TO x1.77 IN THREE SEPARATE POSITIONS**, which is close to [ADR-466](ADR-466.md)'s x1.61 and below the population median. **The factor is taken from the population and not from the binding case**, because the constant covers all sixty-seven, and one of them measured x2.22. Rounded to **x1.9**.

**AND THE ABORT MAKES THE TRUE MAXIMUM WORSE THAN x2.22.** `RI-35` in `repo-invariants.test.ts` went 2443ms unloaded to an abort at 5212ms, so its own ratio EXCEEDS x2.13 by an amount the abort hides.

#### 5.2 Growth, measured on two mechanisms separately

**A COPY OF THIS TREE WAS TAKEN OUTSIDE THE REPOSITORY AND SCALED, AND THE SHIPPED SCANS WERE RUN AGAINST IT.** Nothing was reimplemented: `ri35`, `CHECKS` and `census` were imported from the checkers as they ship and handed a different root. Three runs per size, median reported. The copy reproduces the base within noise (`ri35.run` 2271ms in the repository, 2258ms in the copy).

**MECHANISM ONE, SOURCE.** `packages/zzclone<n>/src` directories added from `apps/`:

| Source-like scope | files | bytes | `ri35.run` | `RI-30` | `RI-28` | ms per source-like MB, `ri35` |
| --- | --- | --- | --- | --- | --- | --- |
| this tree | 789 | 18,084,541 | **2258ms** | 978ms | 480ms | **124.9** |
| +2 clones | 1705 | 39,438,799 | 5699ms | 1753ms | 777ms | 144.5 |
| +4 clones | 2621 | 60,793,057 | **7588ms** | 2818ms | 1203ms | **124.8** |

**THE COST IS LINEAR IN SOURCE BYTES AND THE PER-MEGABYTE FIGURE DOES NOT FALL.** 60,793,057 / 18,084,541 = **3.36** and 7588 / 2258 = **3.36**. The exponent is 1.00 to two figures. **So five times today's source scope costs 5.0 times, not 3.2 times.**

**THIS DISAGREES WITH [ADR-466](ADR-466.md) SECTION 5 AND THE DISAGREEMENT IS STATED RATHER THAN SMOOTHED.** That table reports 289.5, 283.3, 206.7 and 164.9 ms per swept megabyte across four sizes, a rate falling by 43 percent, and it took x3.2 for a five-fold horizon on that basis. **The denominators differ**, since it measured against the checker's own swept scope and this measures against a broader source-like walk, but a falling rate is a dimensionless property and it does not reproduce here at any denominator. **This measurement carries the same contamination that entry named against itself**, identical clones warming the page cache and a finding count rising from 0 to 112, and both effects make the larger sizes cheaper than a real tree of that size, so **the flat rate measured here is if anything optimistic and x5.0 is a floor rather than a ceiling.**

**MECHANISM TWO, THE CORPUS.** The source clones were removed and `docs/decisions` was cloned instead:

| `docs/` scope | files | bytes | `ri35.run` | `RI-16` | census | `RI-12` |
| --- | --- | --- | --- | --- | --- | --- |
| this tree | 1499 | 36,557,502 | **2258ms** | 959ms | 635ms | 158ms |
| +450 ADRs | 1949 | 48,282,725 | 2347ms | 1026ms | 725ms | 179ms |
| +2250 ADRs | 3749 | 95,183,617 | **2492ms** | **1372ms** | **1276ms** | **322ms** |

#### 5.3 Both mechanisms are live and only one of them binds

**[ADR-466](ADR-466.md)'s CORRECTION OF [ADR-465](ADR-465.md) IS RE-DERIVED HERE AND IT HOLDS.** Multiplying the corpus by **2.60** moves `ri35.run` by **1.10**. The sweep excludes `docs/` by construction and the corpus is not what will red that scan.

**BUT THAT CORRECTION WAS ABOUT ONE SCAN AND THIS POPULATION HAS SIXTY-SEVEN CASES, SOME OF WHICH READ NOTHING ELSE.** Over the same clone, per corpus megabyte: the absence-grammar census **10.9ms**, `RI-16` **7.0ms**, `RI-12` **2.8ms**. At 26,056 bytes per ADR file that is **about 0.28ms of census per ADR**, so a thousand further ADRs buy the costliest corpus reader about **284ms**, taking it from 635ms to roughly 919ms. **[ADR-465](ADR-465.md)'s stated mechanism is therefore REAL for part of this population and still not the thing that sets the number.** For a corpus reader to become the binding member, `docs/` would have to reach roughly six times its present size while source stood still.

**THE ASSUMPTION, STATED SO IT CAN BE FALSIFIED RATHER THAN ARGUED WITH.** **This budget holds until the source-like scope reaches about five times its 18,084,541 bytes, on a container 2.21 times slower than this one and fully contended.** To falsify it: scale a copy of the tree, time the shipped `ri35.run` against it, and check whether milliseconds per source-like megabyte stay flat at about 125. **One measurement showing that figure rising retires this number**, and so does one container reporting the base above 2797ms under no load.

---

### 6. One shared constant and not a figure per file, argued rather than assumed

**THE DISPATCH ASKED WHETHER ONE CONSTANT OR A PER-FILE FIGURE IS RIGHT, AND THE ANSWER IS ONE, FOR A REASON THE MEASUREMENT PRODUCED RATHER THAN A PREFERENCE.**

**THE MEMBERS DO NOT PARTITION BY FILE, BECAUSE ONE SCAN IS CALLED FROM TWO FILES.**

| Scan | called from | cost, unloaded |
| --- | --- | --- |
| `ri35.run(REPO_ROOT)` | [`absence-claims.test.ts`](../../packages/tooling/test/absence-claims.test.ts) *"RI-35 finds nothing"* | 2797ms |
| the same scan, through `CHECKS` | [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts) *"RI-35 Every registered ..."* | 2443ms |
| `RI-18`, through `CHECKS` | [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts) | 572ms |
| `ri18.run(REPO_ROOT)` | [`response-shape-copies.test.ts`](../../packages/tooling/test/response-shape-copies.test.ts) | 279ms |

**A PER-FILE FIGURE HANDS ONE SCAN TWO DIFFERENT BUDGETS ACCORDING TO WHICH SUITE CALLED IT.** Sized honestly per file, `absence-claims.test.ts` gets 60000 and `response-shape-copies.test.ts` gets about 6000, and the same `RI-18` scan then has a ten-fold difference in its wall depending on the caller. **That is the drift this corpus names first and everywhere**: a second copy of a number is the copy that goes wrong ([ADR-034](ADR-034.md), [ADR-185](ADR-185.md)).

**AND A PER-FILE FIGURE WOULD ALSO HAVE TO BE MAINTAINED EIGHT TIMES.** Eight numbers, each derivable only by re-running the measurement in section 5 against that file's costliest member, each capable of being wrong independently, in exchange for a faster failure in the run where something is genuinely hung. **The thing a smaller budget buys is not correctness, it is 54 seconds in the hang case.**

**WHAT IS NOT DONE, AND WHY IT IS THE SAME ARGUMENT [ADR-466](ADR-466.md) MADE: THIS IS STILL PER CASE AND NOT PER FILE.** A file-wide budget, whether spelled in a config or with `vi.setConfig`, would hand a corpus-sized wall to every case in the file. [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts) holds **268** cases and 39 of them are in the population; [`absence-claims.test.ts`](../../packages/tooling/test/absence-claims.test.ts) holds 107 and 7 are. **For the other 329, bounding a hang is the ONLY thing their timeout does**, and making it twelve times longer makes that job twelve times slower for no return.

**THE COST OF THE PER-CASE FORM IS REAL AND IT IS THIRTY-THREE SITES INSTEAD OF ONE.** A later case that scans the tree can be added without one, and nothing will notice until it reddens. **That is the same failure mode [ADR-466](ADR-466.md) section 10 item 2 priced, and this row does not close it either.** Section 10 item 2.

**WHERE THE CONSTANT LIVES, AND WHY IT IS NOT IN A CHECKER.** [`packages/tooling/test/scan-budget.ts`](../../packages/tooling/test/scan-budget.ts) is a new file inside the fence. It is not matched by any project's include glob (`packages/*/test/**/*.test.ts` and the three suffixed variants), so it adds no test file and no case, which the counts in section 8 confirm. **It is not in `packages/tooling/checks/` because that directory is out of this row's fence**, and a shared constant is not a checker's business in any case: it is a property of how the suite is run.

---

### 7. Per-case timings, before and after, under both loads

**THE POPULATION, MEASURED.** Every figure is from a full-suite run. Unloaded is run 2 of 2; loaded is run 2 of 2 under four CPU spinners. Base is `7f7ba904`, head is this branch. The thirty-five cases the `test.each` generates are summarised in the row beneath the table.

| Case | base unl | base load | head unl | head load |
| --- | --- | --- | --- | --- |
| `absence-claims` :: RI-35 finds nothing | 2797 | 4461 | 2685 | **5480** |
| `repo-invariants` :: RI-35 Every registered absence claim ... | 2418 | **5212 FAILED** | 2455 | 4382 |
| `absence-claims` :: the widening moves no verdict ... | 2703 | 3831 | 2545 | **5214** |
| `absence-claims` :: every census reaches lines on this repository | 1707 | 2688 | 1630 | 3577 |
| `strip-comments` :: the two modes agree on length and on every newline ... | 952 | 2322 | 1179 | 2383 |
| `repo-invariants` :: RI-30 One comment stripper ... | 970 | 2140 | 963 | 2250 |
| `dependants` :: the derivation runs over the real population ... | 1136 | 1897 | 1071 | 2139 |
| `generated-column-writes` :: the real shipped tree names no member ... | 877 | 1394 | 865 | 1892 |
| `dependants` :: this suite is derived as an importing dependant ... | 899 | 1446 | 924 | 1750 |
| `dependants` :: the module reads the one comment stripper ... | 985 | 2126 | 963 | 1703 |
| `generated-column-writes` :: passes over this tree, and says what it derived | 649 | 598 | 674 | 1575 |
| `strip-comments` :: the idiom silently deletes source the scanner keeps ... | 854 | 1205 | 804 | 1332 |
| `absence-grammar-census` :: NARROW nests inside WIDE ... | 660 | 1308 | 655 | 1259 |
| `absence-grammar-census` :: the claim this row was commissioned over ... | 579 | 1250 | 671 | 1260 |
| `absence-grammar-census` :: the floor is not empty ... | 702 | 1394 | 711 | 1123 |
| `dependants` :: the closure reaches this suite ... in two composing steps | 700 | 1329 | 665 | 898 |
| `generated-column-writes` :: the real registry builds an INSERT for every key ... | 585 | 1145 | 624 | 816 |
| `response-shape-copies` :: every RI-18 finding on the real tree ... | 275 | 598 | 331 | 495 |
| `price-register` :: every leg holds on the tree this check ships with | 126 | 283 | 101 | 205 |
| `price-register` :: the corpus prices things, so the check is not passing on an empty set | 98 | 216 | 138 | 99 |
| `strip-comments` :: a local clock read placed inside the phantom span ... | 132 | 274 | 130 | 249 |
| `generated-column-writes` :: accounts for every GENERATED ALWAYS AS ... | 71 | 139 | 83 | 85 |
| `generated-column-writes` :: holds a column no Drizzle table in this tree declares | 47 | 96 | 33 | 46 |
| `repo-invariants` :: it binds `B5`s condition ... | 43 | 86 | 41 | 84 |
| `repo-invariants` :: the real repository holds | 18 | 39 | 29 | 23 |
| `dependants` :: the population names the method it used ... | 9 | 22 | 7 | 30 |
| `repo-invariants` :: every admitted package really declares the accessor | 1 | 5 | 1 | 1 |
| `repo-invariants` :: the shipped admission is doing work ... | 1 | 1 | 1 | 1 |
| the `test.each` table, **35 cases**, sum | 7429 | 13738 | 7421 | 14339 |
| the same table, max (`RI-35`) | 2418 | **5212 FAILED** | 2455 | 4382 |

**WHICH MEMBERS ARE CHEAPER THAN THE ONE THAT SET THE CONSTANT, AND BY HOW MUCH.** Against 2797ms unloaded: **every other member is cheaper**, and the ratios are wide. The second dearest, *"the widening moves no verdict"*, is 1.03 times cheaper. `RI-30` is **2.9** times cheaper, the strip-comments whole-tree comparison **2.9**, the dependants derivation **2.5**, the census cases **4.0 to 4.8**, the response-shape scan **10.2**, the price-register pair **22 to 29**, and the two `RI-08` cases are **2797** times cheaper at 1ms. **Fifty-six of the sixty-seven cost under a third of the constant's base, and nineteen cost under a hundredth of it.** That spread is the case against one constant and section 6 answers it: the spread is real, and it does not track the file, so a per-file figure prices none of it correctly.

**THE CHANGE WATCHED WORKING.** In head loaded run 2, **two cases crossed 5000ms and both PASSED**: *"RI-35 finds nothing"* at **5480ms** and *"the widening moves no verdict over this repository"* at **5214ms**. **Both would have been RED at the default.** In the base loaded run at the same load, one case crossed it and went red.

**AND THE DISCRIMINATING EXPERIMENT, WHICH WOULD HAVE CAUGHT THE CHANGE NOT WORKING.** `pnpm vitest run --testTimeout=100 packages/tooling/test/` sets a 100ms wall over the whole fence:

```
PASSED 598  FAILED 17
BUDGETED cases that ran OVER the 100ms wall: 35, of which failed: 0
```

**Thirty-five budgeted cases ran past a 100ms wall and not one of them aborted**, the dearest at 2647ms. **All seventeen failures are cases with no budget**, the dearest at 221ms, and every one is named in the session log. **If the third argument were not taking effect, those thirty-five would have been red.** The 100ms wall is an experiment and not a state this branch leaves behind: at the default, and at the constant, the suite is green.

---

### 8. Every check re-run after the last edit

**Derived at the moment of reporting, after the final edit and the final `format` pass.**

| Check | Base `7f7ba904` | Head |
| --- | --- | --- |
| `node scripts/corpus/gates.mjs generate` | exit 0, clean tree | exit 0. It rewrites the `gen:adr_count` span in [STATE](../STATE.md) from 450 to 451 and adds one row to each of the two registries. **No prose moves and [INDEX](../INDEX.md) is unchanged** |
| `node scripts/corpus/gates.mjs check` | **33 of 33** | **33 of 33** |
| `node packages/tooling/checks/repo-invariants.mjs` | **35 of 35** | **35 of 35** |
| `node packages/tooling/checks/write-guard-set.mjs` | PASS | PASS |
| `node packages/tooling/checks/price-register.mjs` | PASS | **FAIL at exit 1 on 4 leg-A findings, every one of them this entry's own, and section 9 is why** |
| `pnpm run typecheck` | exit 0 | exit 0 |
| `pnpm run lint` | exit 0 | exit 0 |
| `pnpm run format:check` | clean | clean |

**THE SUITE, FOUR RUNS EACH SIDE, EVERY FIGURE STATED AS PASSED OR TOTAL.**

| Run | Files | PASSED | skipped | TOTAL | failed | wall |
| --- | --- | --- | --- | --- | --- | --- |
| base unloaded 1 | 347 of 347 | 8713 | 11 | 8724 | 0 | 82s |
| base unloaded 2 | 347 of 347 | 8713 | 11 | 8724 | 0 | 84s |
| base loaded 1 | 347 of 347 | 8713 | 11 | 8724 | 0 | 149s |
| base loaded 2 | 346 of 347 | 8712 | 11 | 8724 | **1** | 146s |
| head unloaded 1 | 347 of 347 | 8713 | 11 | 8724 | 0 | 83s |
| head unloaded 2 | 347 of 347 | 8713 | 11 | 8724 | 0 | 83s |
| head loaded 1 | 347 of 347 | 8713 | 11 | 8724 | 0 | 144s |
| head loaded 2 | 347 of 347 | 8713 | 11 | 8724 | 0 | 153s |
| **final 1**, with this entry in the tree | 346 of 347 | 8711 | 11 | 8724 | **2** | 84s |
| **final 2**, with this entry in the tree | 346 of 347 | 8711 | 11 | 8724 | **2** | 84s |

**THE TWO FAILURES IN THE FINAL RUNS ARE THIS ENTRY'S OWN PRICES AND NEITHER IS IN THIS ROW'S CODE.** They are [`price-register.test.ts`](../../packages/tooling/test/price-register.test.ts)'s *"every leg holds on the tree this check ships with"* and *"the corpus prices things, so the check is not passing on an empty set"*, both of which run the checker against the real `docs/decisions` and therefore against section 10. **They are also two of the sixty-seven, so they are the population reporting on the entry that defines it.** The head runs above are the same tree WITHOUT this file, which is the honest way to state what the code change does and what the document does. Section 9.

**OTHERWISE THE SUITE IS IDENTICAL IN EVERY COLUMN EXCEPT THE ONE FAILURE THIS ROW REMOVED.** No file is added to the run, no case is added, and **the skip count is 11 on all eight runs**. `scan-budget.ts` is a module rather than a suite and the file count is how that is checked rather than claimed.

**THE FOUR RUNS PER SIDE ARE DELIBERATE AND THE SECOND LOADED RUN IS WHY.** The whole subject is behaviour under load, and the base's failure appeared in the second loaded run and not the first. **A single loaded run at the base would have reported this row's motivating defect as absent.**

---

### 9. `price-register.mjs` is RED on this branch and that is the check working

**THIS ENTRY SETS PRICES IN SECTION 10, AND [`price-register.mjs`](../../packages/tooling/checks/price-register.mjs) DERIVES ITS POPULATION FROM `docs/decisions/` ON EVERY RUN.** So this entry joins the population it describes the moment it lands, which is [`ADR-464.md:5`](ADR-464.md)'s own sentence coming true: *"an entry that prices is a member of the population it describes."* **The checker is RED BY CONSTRUCTION on any branch that adds a pricing entry**, and it was PASS at this row's base and is FAIL at its head for that reason alone.

**[PRICE_REGISTER.md](PRICE_REGISTER.md) IS OUT OF FENCE AND WAS NOT TOUCHED.** Its `#` column is a sequence that concurrent rows collide on, and row `469` is live in this same wave. **The two ways to make the check green from here were both refused**: editing the register is out of fence, and dropping the `Price:` idiom from section 10 would hide findings to turn a gate green, which is the thing the constitution names first.

**THE INTEGRATOR REGISTERS THE ANCHORS AS `OPEN` / `UNASSESSED` WITH NO BASIS**, which is the routine step [ADR-466](ADR-466.md) established and not an unresolved question. **IT WAS RUN AND ITS OUTPUT IS RECORDED RATHER THAN PREDICTED.** Exit 1, four leg-A findings, one per section 10 price, each of the form *"ADR-468:NNN sets a price and the register does not list it"*. **All four anchors are this entry's own and no finding names any other entry.** The four are section 10 items 1, 2, 3 and 4. The line numbers are derived by the checker on the run and are deliberately not typed here, because a line number written today is stale by integration ([ADR-467](ADR-467.md) section 4).

**AND TWO SUITE CASES ARE RED BESIDE THEM, FOR THE SAME REASON AND IN THIS ROW'S OWN FENCE.** [`price-register.test.ts`](../../packages/tooling/test/price-register.test.ts)'s *"every leg holds on the tree this check ships with"* and *"the corpus prices things, so the check is not passing on an empty set"* both run the shipped checker against the real corpus. **They are also two of the sixty-seven cases this row budgeted**, which is the population and the entry that defines it meeting in one run.

**NO OTHER CHECK IS RED ON THIS BRANCH.** Gates **33 of 33**, invariants **35 of 35**, `write-guard-set.mjs` PASS, typecheck exit 0, lint exit 0 and `format:check` clean, all re-run after the last edit, and the suite is green in every case except those two on both final runs.

---

### 10. What is owed

1. **NOTHING ASSERTS THAT A CASE WHOSE INPUT IS THE TREE CARRIES A BUDGET SIZED AGAINST THE TREE.** Thirty-three call sites now carry one and the thirty-fourth will be added without one, exactly as [ADR-466](ADR-466.md) section 10 item 2 predicted for the eighth. **This row makes that prediction worse rather than better**, because a reader adding a case to any of eleven files in `packages/tooling/test/` now has eight files where the constant is imported and three where it is not, and no signal about which kind their new case is. The population is derivable: section 3.2's two-step method is mechanical, and the classification of a call as a walk or a named read is the only judgement in it. **Price: a checker over `packages/tooling/test/**` that folds the tree-reaching call sites and requires each enclosing `test(` to carry the third argument, plus the roster of default-parameter functions in the checkers that it must read to do so. It is a parser over a test file, which this tree has nowhere yet.**

2. **NOTHING WATCHES THE SCAN GETTING SLOWER, AND AFTER THIS DIFF NOTHING ACCIDENTALLY DOES EITHER.** [ADR-466](ADR-466.md) section 10 item 3 priced a performance gate with a derived expectation and a stated tolerance, and it is unowned. **This row raises the urgency by a factor of about nine**: sixty-seven cases were being watched by a 5000ms accident and now none of them is, and the budget's own growth assumption is a claim about a rate nothing measures on a schedule. **Price: a check that times the shipped scans against the tree, compares them against a stored expectation with a stated tolerance, and reports a regression rather than aborting a case. The hard half is the expectation: a figure written into a check is a second copy of a measurement, so it has to be stored and refreshed deliberately.**

3. **THE MACHINE FACTOR IS THE ONLY BORROWED NUMBER IN THIS DERIVATION AND IT CANNOT BE CHECKED FROM HERE.** [ADR-465](ADR-465.md)'s 6183ms did not reproduce on [ADR-466](ADR-466.md)'s container and did not reproduce on this one, and it is an abort rather than a completion. **Two entries have now carried it forward without either being able to verify it.** **Price: a recorded figure for what the CI runner actually costs on the binding scan, taken from a CI run rather than from a container, which turns x2.21 into an observation instead of a ratio between two containers neither of which is the one that matters.**

4. **[ADR-466](ADR-466.md) SECTION 7's EIGHT NON-SCALING CASES ARE STILL UNOWNED AND STILL OUT OF EVERY FENCE SO FAR.** Four in [`apps/worker/test/entrypoint.test.ts`](../../apps/worker/test/entrypoint.test.ts) spawn a real process and reached 1868ms under load at this base, two read one route directory, one reads one module, one is in memory. **A corpus-sized budget is the wrong instrument for all eight and none is proposed.** But a process spawn under a loaded CI runner is the one class here whose worst case nobody has measured, and 1868ms against 5000ms is a smaller margin than it looks. **Price: a measurement of the worst spawn cost under load on the CI runner, and a decision about whether a process-spawn budget is a different constant or no constant at all.**

**FINDINGS RECORDED RATHER THAN ACTED ON, EACH OF WHICH IS SOMEBODY'S AND NOT A PRICE:**

5. **`RI-32` DOES NOT EXIST.** The `CHECKS` roster runs `RI-01` to `RI-37` with `RI-32` absent, observed in the generated table of thirty-five. `RI-29` asserts that every check has a register row and every row a check, so the gap is consistent rather than broken, but **a reader counting 35 invariants across ids reaching 37 will look for it.** Not this row's to close and not obviously a defect.

6. **`repo-invariants.mjs`'s RUNNER-REPORT BLOCK NAMES `REPO_ROOT` IN A HELPER THAT NEVER READS IT.** `transcript(checks, root = REPO_ROOT)` is only ever called with `held`, `violated` and `crashes` fixtures whose `run` ignores the root. The default is harmless and it is the single most misleading `REPO_ROOT` in this fence for anyone deriving a population by search. **Reported, not repaired**: the change would be to drop the default, which touches no assertion but does touch a case body, and this row's whole argument is that it touches none.

---

### 11. What this row did NOT do

- **NO CASE WAS SKIPPED, DISABLED, QUARANTINED OR DELETED.** Skip count 11 on all eight runs, [`skipped-tests.mjs`](../../packages/tooling/checks/skipped-tests.mjs) untouched, no `CI-02` / `CI-03` / `CI-04` step implicated.
- **NO CASE SCANS LESS THAN IT DID.** No `REPO_ROOT` call site moved, no root argument was narrowed, no assertion was moved to a fixture tree, and section 4's three proofs are how that is established rather than asserted.
- **NO GLOBAL VITEST CONFIGURATION.** [`vitest.config.ts`](../../vitest.config.ts) is byte-identical and absent from the diff.
- **NO CHECKER WAS EDITED.** Everything under [`packages/tooling/checks/`](../../packages/tooling/checks) was read and none of it written, including the runner-report default named in section 10 item 6.
- **NO `packages/eslint-plugin-merit` FILE**, which is row `469`'s.
- **NO `PRICE_REGISTER.md` ROW**, which is why section 9's red exists.
- **NO MIGRATION, NO WORKFLOW, NO `RI-nn` MINTED, NO `CI-06` LETTER SPENT, NO GATE ADDED OR WEAKENED, NO `apps/` FILE, NO `packages/db` FILE, NO LANDED ADR EDITED, NO PULL REQUEST OPENED, NOTHING SIGNED.**
- **`pnpm run verify` AND [`scripts/corpus/falsify.mjs`](../../scripts/corpus/falsify.mjs) WERE NOT RUN.** Both mutate the working tree and both are forbidden to this row.
- **THE MEASUREMENT AND TRANSFORM SCRIPTS WERE WRITTEN IN THE SESSION SCRATCHPAD OUTSIDE THE REPOSITORY AND ARE NOT IN THE DIFF.** The timing extractor, the scaling harness, the assertion-multiset proof and the script that added the third argument all live there. **The scaled tree was built in the scratchpad and the repository was never mutated to measure it.**
- **NO FIGURE FROM [ADR-466](ADR-466.md) WAS CARRIED FORWARD UNCHECKED.** Its constant, its contention factor, its machine factor, its growth factor, its population count and its corpus correction were each re-derived, and three of the six came out different.

---

### 12. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** Inherited as well as owed: [ADR-437](ADR-437.md), [ADR-445](ADR-445.md), [ADR-448](ADR-448.md), [ADR-451](ADR-451.md), [ADR-452](ADR-452.md), [ADR-454](ADR-454.md), [ADR-456](ADR-456.md), [ADR-458](ADR-458.md), [ADR-459](ADR-459.md), [ADR-460](ADR-460.md), [ADR-461](ADR-461.md), [ADR-463](ADR-463.md), [ADR-464](ADR-464.md), [ADR-465](ADR-465.md), [ADR-466](ADR-466.md) and [ADR-467](ADR-467.md) are `proposed` and unsigned, and this row settles none of them.

  **What a founder read adds and this entry cannot.**

  **THE FIRST IS WHETHER SIXTY SECONDS IS A BUDGET OR A CAPITULATION, AND IT IS A SHARPER QUESTION THAN IT WAS FOR [ADR-466](ADR-466.md).** That entry raised seven cases to 30000 and this one raises sixty-seven to 60000. Every factor is measured and section 4 proves nothing else changed, and both of those are true. **A founder may still hold that a test suite whose slowest case reads four hundred files in three seconds has a cost problem, and that a number twelve times the framework default is the sound of nobody looking at it.** The argument against is section 10 item 2: the right instrument for a cost problem is a check with a derived expectation and a stated tolerance, and a framework's default timeout was never that instrument. **The argument FOR the founder's position is in this entry too, at section 4's honest cost: sixty-seven cases were being watched by accident and now none of them is.**

  **THE SECOND IS WHETHER THE MACHINE FACTOR SHOULD BE IN THE DERIVATION AT ALL.** It is the largest single term after growth, it comes from [ADR-465](ADR-465.md), it is an abort rather than a completion, and **neither of the two entries that have now used it could reproduce it.** [ADR-466](ADR-466.md) put this same question to the founder and it is not yet answered. **If the ruling is that a figure from another container may not enter a derivation, the product falls to 2797 x 1.9 x 5.0 = 26,572 and the budget should be 30000**, which is [ADR-466](ADR-466.md)'s number arrived at from different factors, and every remaining term is this container's own. Section 10 item 3 prices the measurement that would replace the borrowing.

  **THE THIRD IS WHETHER THE GROWTH HORIZON IS THE RIGHT ONE.** x5.0 encodes *five times today's source scope*, which is [ADR-466](ADR-466.md)'s horizon kept deliberately so the two entries are comparable, and it is otherwise arbitrary: nothing in this repository says how large P1 and P2 will make it. **A founder who thinks in delivery dates rather than in multiples should set the horizon and the arithmetic will follow**, and the exponent measured in section 5.2 makes that conversion linear and easy.

  **THE FOURTH IS THAT THE MOST DURABLE THING IN THIS ENTRY IS A METHOD RATHER THAN A NUMBER.** Section 3.2's finding, that three of the eight files walking this tree name `REPO_ROOT` nowhere and that one file naming it walks nothing in three cases, is not about timeouts. **Any future derivation over "what reads the repository" in this fence is wrong by 37 percent of files if it is a grep**, and the reason is a language feature rather than an oversight. A founder deciding whether this row was worth a session should read section 3.2 before section 5.

- Approval: `___________________________` (UNSIGNED. **NON-MONEY DIFF. `E2 READ OWED`, ON THE NINE FILES UNDER `packages/tooling/test/` AND THE DOCUMENTS NAMED IN THE FENCE ABOVE.** Eight are test files and one is a test-only module holding one constant; no money-path module, migration, ledger, payout or auth line is added, changed or deleted, so **no new money-path `E2` read is created by this diff**. It inherits the unsigned debts of the entries listed above. Merging is not signing.)

|                 |     |
| --------------- | --- |
| Approved by |     |
| Date        |     |
