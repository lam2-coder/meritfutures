## ADR-447: `live_account_state` is transcribed from `0050`, compared by name because the registry loops cannot reach it, and NOT registered, because `merit_app` holds nothing on it (2026-09-07, status: proposed)

**Row 447. Branch `claude/transcribe-live-account-state`, off `origin/main` at `89b02405`, session 642.**

**THE TRANSCRIPTION IS TAKEN AND IT LANDS UNDER FIVE MECHANICAL COMPARISONS RATHER THAN FOUR.** `live_account_state` was declared nowhere in [`schema.ts`](../../packages/db/src/schema.ts). It is [ADR-020](ADR-020.md) tier 2's live cache, it carries **three** `*_cents` columns, and one of the three is `GENERATED ALWAYS AS (equity_cents - opening_equity_cents) STORED`. It is now declared, and the column-name fold, the TYPE comparison, the NULLABILITY comparison, the DEFAULT comparison and a GENERATION comparison are all run against it by name.

**AND THE DISPATCH'S PREMISE IS FALSE IN TWO PLACES, WHICH IS THIS ROW'S FIRST FINDING.** The dispatch says four comparisons exist and that `ADR-443` added the fourth. **On `origin/main` at `89b02405` there is no `ADR-443.md`, no `ADR-441` through `ADR-446`, and `grep -c GENERATED_ALWAYS_AS` over `packages/db/test/scoped-db.test.ts` returns `0`.** Three comparisons exist at this base, not four. The dispatch also describes the table as carrying _"three `*_cents` columns and a generated `*_cents` column"_, which reads as four; **the derived figure is three in total, of which the third IS the generated one.** Section 5 is the arithmetic and section 6 is the ADR-443 prediction, checked at source and found unverifiable from this base.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `447`, session **642**, branch `claude/transcribe-live-account-state`, off `89b02405`.
- **Fence, BY FILE:** [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts), [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts), this file and the registries. Everything else read only. **No migration, no `scope.ts`, no `scoped-db.ts`, no file under `packages/tooling/checks/`, no workflow, no `apps/` file.**
- **Money:** **MONEY PATH.** The declaration governs three integer-cents columns on the surface a trader watches. **Integer cents on both sides, compared rather than read, and no float enters the declaration or the DDL.** Section 8.

---

### 1. The container, measured rather than assumed

**THE CONTAINER ARRIVED WITH NO REPOSITORY**, which is now the twenty-third consecutive row to report it. `add_repo` with `access: "push"` was called first and the clone followed; the remote was **not** pre-checked. `pnpm install --frozen-lockfile` was needed before any leg could run, because `node_modules` was absent too.

| Fact                      | Value                                   |
| ------------------------- | --------------------------------------- |
| Repository on arrival     | **absent**                              |
| Base commit               | `89b02405` (`origin/main`)              |
| `node_modules` on arrival | **absent**, installed from the lockfile |
| `gh`                      | not used                                |

### 2. The base, reproduced before the first edit, and it IS GREEN

Measured on `origin/main` at `89b02405`, on a clean tree, in this session:

| Leg                                 | Base                                                                  |
| ----------------------------------- | --------------------------------------------------------------------- |
| `gates.mjs generate`                | `every span already matches its query.` and **the tree stayed clean** |
| `gates.mjs check`                   | **33 of 33**                                                          |
| `repo-invariants.mjs`               | **35 of 35**                                                          |
| `pnpm vitest run`                   | **342 files / 8,429 passed / 11 skipped / 0 failed**                  |
| `typecheck`, `lint`, `format:check` | exit 0, exit 0, clean                                                 |

**`pnpm run verify` WAS NEVER RUN AND NEITHER WAS `scripts/corpus/falsify.mjs`**, on the dispatch's standing instruction that both mutate the working tree.

### 3. WHICH MIGRATIONS BUILD THE TABLE, DERIVED AND NOT ASSUMED

`grep -rniI 'live_account_state' packages/db/migrations` matches **exactly one file**: [`0050_live_cache_and_role.sql`](../../packages/db/migrations/0050_live_cache_and_role.sql). No `ALTER TABLE`, no `ADD COLUMN`, no `ALTER COLUMN`, no `RENAME`, in any of the migrations that follow it.

**SO THE `CREATE TABLE` BODY IS THE COLUMN SET AS OF THE LAST MIGRATION, AND THAT IS AN ANSWER RATHER THAN AN ASSUMPTION.** [ADR-094](ADR-094.md)'s rule is that a table is read as of the LAST migration and never as of its `CREATE TABLE`; here the two coincide, and the coincidence is asserted rather than relied on: [`scoped-db.test.ts:4040`](../../packages/db/test/scoped-db.test.ts) re-derives the set of migrations naming the table on every suite run and fails the day a second one does. It is `reserve_coverage_snapshots`' position and `events`' (`schema.ts` header), and it means the fold replays nothing onto this table.

### 4. THE TRANSCRIPTION, COLUMN BY COLUMN

**Ten columns. Every type, nullability and default below was compared against [`0050`](../../packages/db/migrations/0050_live_cache_and_role.sql) mechanically, not read.**

| Column                    | DDL                                                                       | Transcribed                                                                                     |
| ------------------------- | ------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| `account_id`              | `uuid PRIMARY KEY REFERENCES accounts(id) ON DELETE RESTRICT`             | `uuid(...).primaryKey().references(() => accounts.id)`                                          |
| `trading_day`             | `date NOT NULL`                                                           | `date(...).notNull()`                                                                           |
| `sequence`                | `integer NOT NULL`                                                        | `integer(...).notNull()`                                                                        |
| `opening_equity_cents`    | `bigint NOT NULL`                                                         | `bigint(..., { mode: 'bigint' }).notNull()`                                                     |
| `equity_cents`            | `bigint NOT NULL`                                                         | `bigint(..., { mode: 'bigint' }).notNull()`                                                     |
| `intraday_movement_cents` | `bigint GENERATED ALWAYS AS (equity_cents - opening_equity_cents) STORED` | `bigint(..., { mode: 'bigint' }).generatedAlwaysAs(sql\`equity_cents - opening_equity_cents\`)` |
| `feed`                    | `text NOT NULL`                                                           | `text(...).notNull()`                                                                           |
| `as_of_instant`           | `timestamptz NOT NULL DEFAULT now()`                                      | `timestamp(..., { withTimezone: true }).notNull().defaultNow()`                                 |
| `indicative`              | `boolean NOT NULL DEFAULT true`                                           | `boolean(...).notNull().default(true)`                                                          |
| `created_at`              | `timestamptz NOT NULL DEFAULT now()`                                      | `timestamp(..., { withTimezone: true }).notNull().defaultNow()`                                 |

**THE GENERATED COLUMN IS NULLABLE HERE AND THAT IS `rcr_bp`'s POSITION EXACTLY**, not an omission: `0050` declares no `NOT NULL` on it, and drizzle records a generated column in `generated` rather than in `hasDefault`, so both halves of the DEFAULT comparison read `false` and agree. That is [ADR-438](ADR-438.md) section 5's reading of `declaredDefault`, applied rather than widened, at [`scoped-db.test.ts:3151`](../../packages/db/test/scoped-db.test.ts).

**NO ENTRY WAS ADDED TO `TRANSCRIPTION_OMITS_DEFAULT` AND THE ABSENCE IS ASSERTED**, at [`scoped-db.test.ts:4088`](../../packages/db/test/scoped-db.test.ts). ADR-438's register stands at the three columns it names and this table contributes none.

**THE CHECK CONSTRAINTS ARE LEFT TO THE DATABASE**, as every other table's are: `live_account_state_sequence_is_positive` and `live_account_state_is_indicative` are not expressible in `schema.ts`, and `schema.ts`'s own header rules that a constraint claimed there would be a claim nothing in the package checks.

### 5. WHY THE COMPARISONS DID NOT REACH IT, DERIVED AT SOURCE

**EVERY COMPARISON IN THE SUITE ITERATES `DDL_NAMES`, AND `DDL_NAMES` IS DERIVED FROM `TABLE_KEYS`.** [`scoped-db.test.ts:247`](../../packages/db/test/scoped-db.test.ts) filters `ALL_REGISTERED`, which [`:204`](../../packages/db/test/scoped-db.test.ts) builds from `TABLE_KEYS`, which [`scope.ts:1658`](../../packages/db/src/scope.ts) builds from `TABLES`. So the drift fold at [`:1373`](../../packages/db/test/scoped-db.test.ts), the type and nullability comparison at [`:2728`](../../packages/db/test/scoped-db.test.ts) and the DEFAULT comparison at [`:3217`](../../packages/db/test/scoped-db.test.ts) are total over the REGISTRY and stop at its edge. **An unregistered relation is reached by none of them.** The dispatch's word for this is right: they stop at the registry.

**THE COUNTS, DERIVED AT REPORTING TIME BY WALKING THE MODULES RATHER THAN THE TEXT.** `TABLE_KEYS.length` is **116**. The number of drizzle relations exported from `schema.ts` is **117** with this row's addition and was **116** before it. **The set of relations declared in `schema.ts` and absent from `scope.ts` was EMPTY before this row and is now exactly one member, `liveAccountState`.**

**THAT MAKES ONE EXPLANATORY CLAUSE IN THE `schema.ts` HEADER IMPRECISE AND IT IS NAMED RATHER THAN LEFT.** [`schema.ts:6`](../../packages/db/src/schema.ts) reads _"`SCOPE_RULES` is total over the keys of this file"_. It is stated of the declarations in that file and this declaration is outside it. **THE SAFETY PROPERTY THE CLAUSE EXISTS TO EXPLAIN IS UNCHANGED**: `scopedDb`, `firmDb` and `systemDb` all take `TableKey`, which is `keyof typeof TABLES`, so naming this relation at a call site is a **compile error**, exactly as it was when the relation was not declared at all. Section 9 item 1 owes the repair. It is not taken here, and the reason is measured in section 10.

### 6. `ADR-443`'s PREDICTION, CHECKED AT SOURCE, AND THE CORRECTION

The dispatch asks this row to verify `ADR-443` section 11 item 1, which predicts that this transcription _"turns `packages/db/test/scoped-db.test.ts:3825` red on the same commit"_ and that `GENERATED_ALWAYS_AS` **must move with it**.

**THE PREDICTION CANNOT BE VERIFIED FROM THIS BASE AND IS FALSE ABOUT IT, ON THREE DERIVED FACTS.**

1. **`docs/decisions/ADR-443.md` DOES NOT EXIST ON `origin/main` AT `89b02405`.** The highest ADR file in the tree is [ADR-440](ADR-440.md). `ADR-441` through `ADR-446` are absent as well. The entry exists, if it exists, on an unmerged sibling branch; `git ls-remote --heads origin` shows `claude/generated-always-as-compared` among others, and **this branch is off `origin/main` as instructed and is not rebased onto any of them.**
2. **`GENERATED_ALWAYS_AS` DOES NOT EXIST IN THE TEST FILE ON THIS BASE.** `git show origin/main:packages/db/test/scoped-db.test.ts | grep -c GENERATED_ALWAYS_AS` returns **0**. There is nothing to move. This row DEFINES a constant of that name inside its own block, at [`scoped-db.test.ts:4038`](../../packages/db/test/scoped-db.test.ts), which is a first statement rather than a move.
3. **LINE `3825` OF THE BASE FILE IS NOT A TRANSCRIPTION ASSERTION AT ALL.** `git show origin/main:packages/db/test/scoped-db.test.ts | sed -n '3825p'` lands inside `` `SystemReason` is still exactly two members ``, a test about the accessor's reason vocabulary. **It did not go red on this commit and nothing in this row could have made it do so.**

**WHAT IS TRUE IS THE HALF UNDERNEATH THE PREDICTION, AND IT IS WORTH SEPARATING FROM IT.** A transcription of this table without a generation comparison would rest the one fact that makes `intraday_movement_cents` unwritable on nothing at all. **That is why this row wrote the comparison instead of waiting for one**, section 7 case 5. Whether ADR-443's own general comparison over the whole registry is the right home for it is that row's question and not this one's, and if ADR-443 lands, **the block here should be read against it and the narrower half deleted** rather than kept in duplicate. Section 9 item 3.

**ADR-443 IS NOT EDITED BY THIS ROW AND COULD NOT BE**: it is not in the tree, and a landed ADR is a dated record that a commit cannot repair ([`ADR-386.md:169`](ADR-386.md)'s rule). The correction is here, in this row's own entry, which is what the dispatch asked for.

### 7. THE TABLE IS **NOT** REGISTERED IN `scope.ts`, AND THE REASON IS A GRANT

**REGISTERING IT WOULD PUBLISH A DOOR ONTO A RELATION THE DATABASE IS REQUIRED TO REFUSE.** [`0050:233`](../../packages/db/migrations/0050_live_cache_and_role.sql) is `REVOKE ALL ON live_account_state FROM merit_app, PUBLIC;` and [`0050:254`](../../packages/db/migrations/0050_live_cache_and_role.sql) grants `SELECT, INSERT, UPDATE` to `merit_live` alone. `0050`'s own header item 3 says why the revoke is `ALL` and not `UPDATE, DELETE`: **FM-M12-08 is a statement about READING**, the stats run is `apps/worker/src/batch/statistics.ts` and it runs as `merit_app`, so _"the verb that has to go is SELECT"_. Header item 4 adds the consequence in the migration's own words: `client.ts` opens ONE pool from ONE `DATABASE_URL`, so _"the live read path must therefore be a process that connects as `merit_live`"_, and which process that is stays P6-b's and P6-g's.

**EVERY ACCESSOR DOOR IN `packages/db` OPENS ON THAT ONE POOL.** So a key in `TABLES` would be a permission claim this package cannot honour: it would compile, it would satisfy every scope assertion, and the first real read through it would be refused by PostgreSQL, **which is the outcome FM-M12-08 requires.** A registration is therefore not a smaller version of the right answer; it is the wrong one.

**THE REFUSAL IS RECORDED AS AN ASSERTION RATHER THAN AS A COMMENT**, at [`scoped-db.test.ts:4166`](../../packages/db/test/scoped-db.test.ts), on this file's own precedent for the aggregate P7 asked for. It holds three things: that no registered relation carries this SQL name, and that both grant lines are still in `0050` as quoted. A row that registers the key has to come here and read the reason before the suite is green again.

### 8. THE MONEY ACCOUNTING

**MONEY IS INTEGER CENTS AND THE COMPARISON WAS MADE MECHANICALLY, NOT BY READING.** [ADR-437](ADR-437.md) section 8 is the reason this distinction is the whole point of the row: it reports that every `*_cents` column agrees with its DDL and then says of that agreement, in its own words, **"that agreement is one reading taken on one afternoon by one row. It is not a check."** This table's three money columns are not in that afternoon's population at all, because they were not transcribed. They are now, **and they are checked**: [`scoped-db.test.ts:4146`](../../packages/db/test/scoped-db.test.ts) derives the `*_cents` set from the DDL, asserts it is exactly `equity_cents`, `intraday_movement_cents` and `opening_equity_cents`, and asserts `bigint` on **both** sides of each. A TypeScript `bigint` cannot hold `1.5`. **No float enters this path in either direction.**

**NO WRITE PATH IS ADDED AND NONE WAS AVAILABLE TO ADD.** [ADR-437](ADR-437.md) section 7 reasons 1, 2 and 3 refused the write-path narrowing and nobody has answered its section 13. This row adds a **declaration**, and:

- **No column that was unwritable became writable.** Nothing in this diff touches `scoped-db.ts`, `scope.ts` or any accessor. `insert` is not reachable for this relation because the relation is not a `TableKey`.
- **The generated column cannot be written through anything this row adds.** `.generatedAlwaysAs()` is recorded by drizzle in `generated` with `type: 'always'`, which is asserted at [`scoped-db.test.ts:4136`](../../packages/db/test/scoped-db.test.ts), and a `GENERATED ALWAYS AS ... STORED` column refuses a written value at the database regardless.
- **None of the twelve defaulted money columns ADR-437 section 8 counts is on this table**, and this table adds none to that population: its three `*_cents` columns carry **no** `DEFAULT` on either side, which the DEFAULT comparison asserts per column.

**NOT ONE GATE WAS WEAKENED, NOT ONE TEST WAS SKIPPED, DISABLED OR QUARANTINED, AND NOT ONE ASSERTION WAS INVERTED.** The skip count is **11** on the base and **11** on this branch.

### 8.1. THE POSTURE CHANGE, NAMED PLAINLY FOR A READER WHO HAS NOT FOLLOWED THE CHAIN

**THIS SECTION WAS COMMISSIONED TWICE, BY TWO DISPATCHES, AND IS WRITTEN ONCE.** Both asked for the same four items and the second arrived after the first was already landed. **A second copy would be the duplicate-registry defect this estate keeps finding**, so the second commission is answered by this section rather than beside it.

**READ THIS SECTION FIRST IF YOU ARE READING THE DIFF TO DECIDE SOMETHING.** Before this row, `live_account_state` was transcribed NOWHERE. A money table that is in no TypeScript file is FAIL-CLOSED by construction: there is no symbol to name, so no statement can be built against it and no type can be derived from it. That is one instance of [ADR-112](ADR-112.md) foreclosure 4 and it is why [ADR-437](ADR-437.md) section 5 could name the table at all. **This row ends that condition, and a founder should see the change in what is reachable rather than only a transcription.**

**AND THE COMMISSION'S OWN SENTENCE FOR THE CHANGE IS TOO STRONG, WHICH IS WHY EACH ITEM BELOW IS DERIVED.** The correction that asked for this section says the row moves the table *"from FAIL-CLOSED to REACHABLE through the typed accessor"*. **It does not become reachable through the typed accessor.** What it becomes is NAMEABLE through the package's schema barrel, which is a narrower and different thing, and the difference is the whole of item 1.

**1. WHAT BECOMES REACHABLE, AND THROUGH WHICH MEMBERS.**

| Surface | Before | After | Evidence |
| --- | --- | --- | --- |
| `scopedDb(identityId)` | unreachable | **unreachable** | [`scoped-db.ts:289`](../../packages/db/src/scoped-db.ts). Its read verbs take `TableKey`, which is `keyof typeof TABLES` ([`scope.ts:514`](../../packages/db/src/scope.ts)), and this relation has no key |
| `firmDb()` | unreachable | **unreachable** | [`scoped-db.ts:1395`](../../packages/db/src/scoped-db.ts), `FirmTableKey`, same derivation |
| `systemDb(reason)` | unreachable | **unreachable** | [`scoped-db.ts:312`](../../packages/db/src/scoped-db.ts), `TableKey`, same derivation |
| `CATALOG_TABLE_KEYS` | not a member | **not a member** | [`scoped-db.ts:3403`](../../packages/db/src/scoped-db.ts), a closed list this row does not touch |
| `schema` barrel | **no symbol** | **`schema.liveAccountState` exists** | [`index.ts:268`](../../packages/db/src/index.ts), `export * as schema from './schema.ts'` |

**SO EXACTLY ONE SURFACE MOVED AND IT IS THE BARREL.** Naming the relation at any accessor door is a COMPILE error today exactly as it was when the relation did not exist, and [`scoped-db.test.ts:4166`](../../packages/db/test/scoped-db.test.ts) is that fact's runtime shadow. What a caller outside this package gains is the ability to WRITE THE NAME and to derive `$inferSelect` and `$inferInsert` from it.

**2. WHETHER ANY WRITE BECOMES POSSIBLE. NO, AND THE EVIDENCE IS THREE INDEPENDENT REFUSALS RATHER THAN ONE.**

- **NO HANDLE ESCAPES THE PACKAGE.** `client()`, the only function returning a drizzle database, is **deliberately unexported**: [`index.ts:271`](../../packages/db/src/index.ts) says in its own words that *"`client()` itself stays unexported"* and only `closeClient` is exported beside it. `packages/db/package.json` declares one entry point, `./src/index.ts`. **A name with no handle builds no statement.**
- **NO ACCESSOR ADMITS THE KEY**, item 1. And even inside the package, where `client()` is in scope, this row adds no caller: `git diff origin/main --name-only` is two `packages/db` files and the registries.
- **AND THE DATABASE REFUSES IT REGARDLESS**, which is the refusal that does not depend on TypeScript at all. `merit_app` holds no privilege of any kind on this relation ([`0050:233`](../../packages/db/migrations/0050_live_cache_and_role.sql)), and every pool this package opens is `merit_app`. Section 7.

**THE STOP CASE FROM THE DISPATCH DID NOT FIRE.** No column that was unwritable became writable, so there is no finding to record under it and no writer to withhold.

**3. THE MONEY COLUMNS BY NAME, WITH THEIR DDL TYPES.** Three, derived from the DDL and asserted at [`scoped-db.test.ts:4146`](../../packages/db/test/scoped-db.test.ts).

| Column | DDL type | Transcribed type | Generated | Integer on both sides |
| --- | --- | --- | --- | --- |
| `opening_equity_cents` | `bigint NOT NULL` | `bigint`, `mode: 'bigint'` | no | **yes** |
| `equity_cents` | `bigint NOT NULL` | `bigint`, `mode: 'bigint'` | no | **yes** |
| `intraday_movement_cents` | `bigint GENERATED ALWAYS AS (equity_cents - opening_equity_cents) STORED` | `bigint`, `mode: 'bigint'` | **yes** | **yes** |

**THREE IN TOTAL AND THE THIRD IS THE GENERATED ONE.** There is no fourth. The expression is integer subtraction on `bigint` cents, so **no float enters this path at any point**, and a TypeScript `bigint` cannot hold `1.5`. That last claim was watched failing rather than asserted: typing `1.5` onto the write type of `equity_cents` is `error TS2322: Type 'number' is not assignable to type 'bigint'`.

**4. WHETHER THE GENERATED COLUMN CAN BE WRITTEN THROUGH THE TYPE AT ALL. NO, AND IT WAS CHECKED RATHER THAN ASSUMED.** A generated column that `$inferInsert` left writable would be a real defect, because a write type is the one place the refusal could be silently lost. **`'intradayMovementCents'` is NOT a key of `typeof liveAccountState.$inferInsert`, and it IS a key of `$inferSelect`.** Both were compiled as type-level assertions and **both were watched failing**: asserting the column IS writable produced a `tsc` error, and the baseline compiles at exit 0. The two stored money columns are additionally **REQUIRED** rather than optional in that write type, because neither carries a `DEFAULT` on either side, so neither could be omitted from a write and defaulted by a clause nobody compared.

**THIS IS ONE TABLE CHECKED BY HAND AND IT IS EVIDENCE, NOT A CONTROL.** It is [ADR-437](ADR-437.md) section 8's own distinction applied to this row's own work, in that entry's own words: a reading taken once by one row **"is not a check."** **ROW 445 OF THIS WAVE OWNS THE TREE-WIDE CONTROL** for the property that nothing writes a generated column, and **this section does not close it and must not be read as closing it.** What is established above is one table, by hand, on one afternoon. **No control was built here and none should be**: the probe was deleted after it was watched failing rather than landed as a test, precisely so that row 445's control is the first mechanical statement of the property rather than the second.

**5. [ADR-437](ADR-437.md) SECTION 13's WRITE-PATH RULING IS STILL UNANSWERED AND THIS ROW DOES NOT ANSWER IT.** That entry's section 7 reason 1 is that **no ruling in this tree names the write path**: [ADR-303](ADR-303.md) limit 4 names four verbs and every one of them is a READ. Its section 13 asks a founder to say which ruling admits a writer, and nobody has. **This row neither answers that question nor depends on an answer**, because it ships a declaration and no writer. The question is exactly as open after this commit as before it, and **nothing in this entry may be read as answering it**: a transcription that makes a write type DERIVABLE is not a ruling that a write is permitted, and section 8.1 item 2 is that no write is expressible in any case.

---

### 9. What is owed, stated precisely enough to be picked up

1. **THE HEADER CLAUSE AT [`schema.ts:6`](../../packages/db/src/schema.ts)**, _"`SCOPE_RULES` is total over the keys of this file"_, is now imprecise: it is true of `TABLES` and no longer true of the declarations in `schema.ts`. **The repair is a sentence, and its cost is the reason this row did not take it**: 62 distinct lines of `schema.ts` are cited from elsewhere in the tree and **6 of them are inside the header** (lines `4`, `20`, `23`, `49`, `109` and `112`), so any edit above the imports moves every one of the 62. A row that repairs it should either do it line-neutrally on [ADR-440](ADR-440.md)'s precedent or take the citation move deliberately. The correction is kept beside the declaration that causes it, at [`schema.ts:5351`](../../packages/db/src/schema.ts), on `RI-14`'s idiom.
2. **THE DENOMINATOR IN THE SAME HEADER IS STALE AGAIN**, at [`schema.ts:4`](../../packages/db/src/schema.ts), which reads _"ONE HUNDRED AND ELEVEN TABLES OF 115, PLUS ONE VIEW"_. `TABLE_KEYS.length` is **116** as derived in section 5, and the header's own paragraph at [`schema.ts:15`](../../packages/db/src/schema.ts) says the denominator _"has been stale twice and is recomputed here rather than incremented"_. **It is stale a third time and this row did not increment it either**, for item 1's reason. A `<!--gen:-->` span cannot live in a `.ts` file, so the durable repair is an assertion in the suite that recomputes both figures, on the model of the one that already asserts `TABLE_KEYS.length` at [`scoped-db.test.ts:466`](../../packages/db/test/scoped-db.test.ts).
3. **THE GENERATION COMPARISON IS NARROW ON PURPOSE AND SHOULD BE FOLDED INTO A GENERAL ONE IF `ADR-443` LANDS.** It runs on this table only, at [`scoped-db.test.ts:4103`](../../packages/db/test/scoped-db.test.ts). A comparison over every registered relation belongs beside the type, nullability and DEFAULT loops at [`scoped-db.test.ts:3217`](../../packages/db/test/scoped-db.test.ts). **When the general one exists, the narrow one is DELETED rather than kept**, and the deletion is the admission rule ADR-421 section 9 states.
4. **THE LIVE READ PATH IS STILL NOT A PROCESS.** [`0050`](../../packages/db/migrations/0050_live_cache_and_role.sql) header item 4 leaves to P6-b and P6-g the question of which deployable connects as `merit_live`. **Until that exists, this transcription is a type nothing reads**, and that is the honest description of what this row shipped. A row that builds the live reader needs a second pool in `client.ts`, which is a diff in a file this fence does not hold.
5. **THE NON-REGISTRATION ASSERTION WAS NOT INDEPENDENTLY WATCHED RED**, section 11. Adding the key to `TABLES` and `SCOPE_RULES` makes a large part of the suite fail before that case is reached, because `SQL_NAME` at [`scoped-db.test.ts:79`](../../packages/db/test/scoped-db.test.ts) is total by COMPILE error and a runtime key with no entry in it breaks every loop that indexes it. **The fact observed is stronger than the one the case asserts, and it is a different fact**, so the case's own discrimination is owed a seed by a row that can arrange one cheaply.

### 10. Blast radius, DERIVED, and what actually moved

**THE CONSTRAINT IS THAT NO CITED LINE MOVES, WHICH [ADR-386](ADR-386.md) AT [`repo-invariants.mjs:3008`](../../packages/tooling/checks/repo-invariants.mjs) DISTINGUISHES FROM LINE-COUNT NEUTRALITY.** This row satisfies the stronger and simpler thing: **both edited files are PURE APPENDS.**

| Fact                               | `schema.ts`                          | `scoped-db.test.ts`                  |
| ---------------------------------- | ------------------------------------ | ------------------------------------ |
| `wc -l` base / branch              | 5320 / 5427                          | 3984 / 4178                          |
| `git diff --numstat`               | `107  0`                             | `194  0`                             |
| Insertion point                    | line 5321                            | line 3985                            |
| Prefix vs `origin/main`            | **byte-identical through line 5320** | **byte-identical through line 3984** |
| Distinct cited lines into the file | 62, highest **5190**                 | 20, highest real **2789**            |
| **Cited lines moved**              | **0**                                | **0**                                |

**THE CITATION POPULATION WAS DERIVED HERE AND NOT INHERITED FROM ANY DOCUMENT**, which is what `ADR-441` section 11 item 6 was a live notice about. `grep -rhoP 'scoped-db\.test\.ts[:#]\K\d+'` over `.md`, `.ts`, `.mjs` and `.js` returns **20 distinct lines over 21 files**. One of the twenty is `99999`, which is [ADR-440](ADR-440.md) line 139 recording its own seeded red and not a live pointer. **A citation population is not what an edit moves**, and the figure that matters here is the second one: an insertion at line 3985 moves **none** of them.

**THE IMPORT BLOCK WAS NOT TOUCHED AND THAT IS WHY THE BLOCK USES A DYNAMIC IMPORT.** `scoped-db.test.ts`'s imports end at line 49, above every cited line in the file, so adding `liveAccountState` there would have moved all twenty to buy one symbol. It is taken with `await import('../src/schema.ts')` inside the block instead, at [`scoped-db.test.ts:4025`](../../packages/db/test/scoped-db.test.ts).

### 11. The controls added, and the RED watched under each

**SEVEN CASES, ALL IN ONE NAMED BLOCK AT THE END OF THE FILE.** Six were watched failing under a seeded violation in `schema.ts`, each reverted and each verified byte-identical afterwards by `git hash-object` against the value taken before the seed.

| #   | Case                                      | Seed                                                                                          | Result               |
| --- | ----------------------------------------- | --------------------------------------------------------------------------------------------- | -------------------- |
| 1   | `exactly one migration builds this table` | not seeded; seeding it means editing a migration, and migrations are sacred (constitution E2) | see below            |
| 2   | `the column-name set equals the DDL`      | `feed` renamed `feed_name` in the transcription                                               | **RED**, 3 cases     |
| 3a  | `TYPE and NULLABILITY equal the DDL`      | `equity_cents` `bigint` to `integer`                                                          | **RED**, 2 cases     |
| 3b  | `TYPE and NULLABILITY equal the DDL`      | `feed` loses `.notNull()`                                                                     | **RED**, 1 case      |
| 4   | `DEFAULT equals the DDL`                  | `indicative` loses `.default(true)`                                                           | **RED**, 1 case      |
| 5a  | `the GENERATION CLAUSE agrees`            | the generated column becomes a plain writable `bigint`                                        | **RED**, 1 case      |
| 5b  | `the GENERATION CLAUSE agrees`            | the expression written backwards, `opening_equity_cents - equity_cents`                       | **RED**, 1 case      |
| 6   | `every *_cents column is INTEGER CENTS`   | covered by seed `3a` above                                               | **RED**              |
| 7   | `the table is NOT registered`             | key added to `TABLES` and `SCOPE_RULES`                                                       | see section 9 item 5 |

**CASE 1 IS THE ONE THAT PROTECTS THE OTHER SIX AND IT IS NOT SEEDABLE FROM WITHIN THIS FENCE.** It asserts that `0050` is still the only migration naming the table. The day a later migration adds a column, that case goes red **and the transcription above it is stale on the same day**, which is the whole of [ADR-094](ADR-094.md)'s rule expressed for a table the fold cannot reach.

**THE EXPRESSION SEED IS THE ONE WORTH NAMING.** Agreeing about WHICH column is generated says nothing about WHAT it is generated from, and `opening_equity_cents - equity_cents` is a movement figure with the sign written backwards on the surface a trader watches. **It is caught.** This project has recorded a sign written backwards inside an ADR before; `CLAUDE.md` names it among the reconciliation session's three worst errors and rules that the remedy is a mechanical assertion rather than a bigger model.

### 12. Every leg, re-run after the last edit

| Leg                                   | Base (`89b02405`)       | This branch              | Delta  |
| ------------------------------------- | ----------------------- | ------------------------ | ------ |
| `gates.mjs generate`                  | clean, spans matched    | clean, spans regenerated | --     |
| `gates.mjs check`                     | **33 of 33**            | **32 of 33**, `CI-06f` only | see below |
| `repo-invariants.mjs`                 | **35 of 35**            | **35 of 35**             | 0      |
| `vitest run` files                    | **342**                 | **342**                  | **0**  |
| `vitest run` passed                   | **8,429**               | **8,436**                | **+7** |
| `vitest run` skipped                  | **11**                  | **11**                   | **0**  |
| `vitest run` failed                   | **0**                   | **0**                    | **0**  |
| `typecheck` / `lint` / `format:check` | exit 0 / exit 0 / clean | exit 0 / exit 0 / clean  | --     |

**`CI-06f` IS RED AND IT IS REPORTED RATHER THAN REPAIRED**, on the precedent rows `434`, `437` and `440` set. Its six findings are exactly *"`ADR-441` is neither present nor reserved (a hole)"* through `ADR-446`: this row was told to claim ALLOCATION row `447` and **no other cell**, and row `423`'s ruling is that a reservation's subject is *"to be set at dispatch and NOT invented here"*. **Writing six reservations for rows this row does not own would be inventing them.** The six holes close when the sibling rows land. **Every other gate passes and every invariant holds**, and the only two gate findings this row caused itself -- a ragged `ALLOCATION` row from an unescaped pipe and two duplicate first-cell keys in section 11's table -- were repaired rather than registered.

**THE GENERATED SPANS, CHECKED IN BOTH DIRECTIONS BECAUSE A MID-ROW FENCE CORRECTION ASKED FOR IT.** A dispatcher correction widened this row's fence to admit `docs/STATE.md` for `gates.mjs generate` output only, on the ground that granting the ADR file without it was not satisfiable, and asked that any span OTHER than the ADR count moving be reported as a finding. **It was checked by diffing every `<!--gen:-->` span in every file this branch touches against `origin/main`, rather than by reading the one line.** Exactly one span moved anywhere: `adr_count` in [`STATE.md:32`](../STATE.md). **`sql_tables` is 118 on both sides, `sql_triggers` 32, `migration_files` 75, `manifest_changes` 118, `gate_count` 33, `gs_count` 316 and `ec_count` 158**, and [`INDEX.md`](../INDEX.md), which carries three of those, is **byte-identical to `origin/main`**. That is the confirmation the correction asked for: **the transcription touched nothing the generator counts**, which is what a row adding no migration should produce. `generate` is idempotent on the final tree.

**AND THE CORRECTION'S OWN FIGURE IS WRONG ABOUT THIS BASE, WHICH IS THE THIRD INHERITED COUNT THIS ROW HAS HAD TO RE-DERIVE.** It states that `docs/STATE.md:32` carries `<!--gen:adr_count-->424<!--/gen-->` and says the figure was verified at source. `git show origin/main:docs/STATE.md` gives **423** at that line, and this branch gives **424**. **426 is neither side of this row's diff.** It is consistent with a tree on which three of the unlanded sibling rows have landed, which is the same defect as section 6: a wave dispatch describing a sibling's tree as though it were `origin/main`. **The correction's RULING is right and is taken in full** -- the fence as first written was not satisfiable, `STATE.md` had to move, and the `generate` output was committed before any `git checkout`. **Only its number is wrong, and it is re-derived here rather than repeated.**

**THE SEVEN ADDED CASES ARE ATTRIBUTED, AND THE ATTRIBUTION IS THE POINT OF THE ROW.** **Not one of them came from a registry comparison loop.** The drift fold, the type and nullability loop and the DEFAULT loop each emit one case per registered relation, and this row registered no relation, so **all three emitted exactly what they emitted on the base.** All seven come from the named block: one migration-derivation case, one name fold, one type-and-nullability case, one DEFAULT case, one generation case, one money case and one non-registration case. **A row that reported these as registry-loop growth would be reporting a registration that did not happen.**

### 13. What this row did NOT do

- **No migration was written, edited, superseded or numbered.** `packages/db/migrations/` is byte-identical to `origin/main`.
- **`scope.ts` was NOT edited.** It was modified once to seed the red in section 11 and restored byte-identically, verified by `git hash-object` on both sides.
- **`scoped-db.ts` was not opened for edit** (row 444's), and **nothing under `packages/tooling/checks/` was edited** (rows 445's and 446's).
- **No `RI-nn` was minted and no `CI-06` letter was spent.** The comparisons are vitest cases in a file that already holds every other transcription comparison.
- **No general generation comparison over the registry was written.** That is section 9 item 3 and it belongs to the row that owns it.
- **No landed ADR was edited.** ADR-443 is corrected in section 6, in this entry, because it is not in this tree and because a dated record is not repaired by a commit.

### 14. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** Inherited as well as owed: [ADR-303](ADR-303.md), [ADR-416](ADR-416.md), [ADR-421](ADR-421.md), [ADR-426](ADR-426.md), [ADR-430](ADR-430.md), [ADR-432](ADR-432.md), [ADR-437](ADR-437.md), [ADR-438](ADR-438.md), [ADR-439](ADR-439.md) and [ADR-440](ADR-440.md) are `proposed` and unsigned, and this row settles none of them.

**What a founder read adds on the `E2` read, and this entry cannot.**

**THE FIRST IS THE ONLY THING HERE THAT IS A JUDGEMENT RATHER THAN A DERIVATION, AND IT IS SECTION 7.** Every other claim in this entry is a comparison a machine now re-runs. **That one is not.** The reasoning is that `0050`'s revoke makes a `scope.ts` key a permission claim `packages/db` cannot honour, so the table is transcribed and left out of the registry. **A founder may read that the other way**: that a table nothing can reach through an accessor should not be in `schema.ts` at all, and that the right shape is a separate declaration file for the live tier's relations. **This row cannot settle that, because it is a question about where the estate is going and not about what `0050` says.** The consequence of the ruling is stated plainly in section 5: this is the first relation in `schema.ts` with no key in `scope.ts`, and if that is the wrong precedent it is cheapest to say so now, at one table, rather than at the fourth.

**THE SECOND IS THAT THE MONEY COMPARISON THIS ROW MADE IS THE ONE ADR-437 SAID WAS OWED, AND IT IS NOW OWED ON A SMALLER POPULATION.** [ADR-437](ADR-437.md) section 8's _"one reading taken on one afternoon by one row"_ covered every `*_cents` column that was transcribed. **Three more are transcribed today and they are checked rather than read.** What is still uncovered is the rest of that population, and the check that would cover it is the general one section 9 item 3 describes. **A founder deciding what to fund next should know that this row made the money comparison mechanical on exactly three columns and left it a reading on the others.**

**THE THIRD IS SECTION 6, AND IT IS A PROCESS FINDING RATHER THAN A CODE FINDING.** This row was dispatched to verify a prediction in a document that **is not in the tree**, about a constant that **does not exist**, at a line that is **something else entirely**. It checked at source and says so. **The cost of the wave shape is that a dispatch can carry a sibling's unlanded reasoning as settled fact**, and this is the second premise in three rows to fail that way. A founder may want to rule on whether a row should be dispatched against an unmerged sibling's section numbers at all.

**THE FOURTH IS THAT NOTHING READS THIS TABLE AND NOTHING WILL UNTIL A PROCESS CONNECTS AS `merit_live`**, section 9 item 4. This row shipped a checked declaration for a relation with no reader. **That is worth having and it is not worth pretending is more than it is.**

- **Approval:** `___________________________` (UNSIGNED. **MONEY PATH, E2 READ OWED**, on the files named in section 10. **Merging is not signing.**)

|                 |     |
| --------------- | --- |
| **Approved by** |     |
| **Date**        |     |
