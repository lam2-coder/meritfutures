---
status: approved
depends_on: [README.md]
last_updated: 2026-08-17
---

# Number allocation

The three allocation registries, kept in one document because each is read AS A
TABLE: `CI-06f`, `CI-06h` and `CI-06p` call one shared parser over the first cell of
every row. A table split into a file per row is not a table, which is the same reason
`DELTA_MANIFEST` stays single under [ADR-043](ADR-043.md).

**Every table here carries two columns and no third.** The number, and who took it.
The State column that used to sit beside them is deleted, on [ADR-034](ADR-034.md)'s
own remedy, and the ruling is [below](#the-state-column-is-deleted-on-adr-034s-own-remedy).

## Number allocation, and why this table exists

**An ADR number is claimed here before it is written.** Two branches forking from the same `main` both read the registry, both find the same maximum, and both take the next integer. Neither is wrong locally; the corpus is broken the moment the second merges. **[ADR-034](ADR-034.md) rules the allocation, and [CI-06f](../testing/STRATEGY.md) enforces it.**

| Numbers | Claimed by | What it rules |
|---|---|---|
| 001 to 032 | merged to `main` (PR #4, 2026-08-14) | 026 to 030 are the schema-delta fold; 031 and 032 are the `published_statistics` rulings. Their numbers are cited inside merged migration comments, `DELTA_MANIFEST` and `DATA_MODEL`, which is why they were the ones that did not move |
| **033** | merged to `main` (PR #5) | The citation-reviewer ADR, renumbered from `031` on that branch under the ruling below |
| **034** | merged to `main` (PR #6) | The allocation ruling itself, and the COUNT GATE |
| **035** | merged to `main` (PR #9) | The `plan_versions` immutability-trigger defect, ruled at the PR #7 / PR #8 reconciliation. **The fix is `0028`, which merged with it, and the structural fix is `CI-06j`** |
| **036** | merged to `main` (PR #13) | The migration number allocation table below, and `CI-06h`'s allocation half |
| **037** | merged to `main` (PR #15) | A shorthand may not restate a value the config owns |
| **038** | merged to `main` (PR #15) | A CI stage states, in its own output, what it currently proves |
| **039** | merged to `main` (PR #17) | Passwordless auth, and phone as a first-class identity signal |
| **040** | merged to `main` (PR #17) | The payout enforcement window |
| **041** | merged to `main` (PR #17) | Identity-level restriction, and its enforcement surface |
| **042** | merged to `main` (PR #17) | The trading-calendar source discipline, and the wall-clock unit ruling |
| **043** | merged to `main` (PR #18) | The append-only registries become directory-per-entry |
| **044** | `claude/merit-futures-briefing-7auoor`, the review desk | The AI and LLM policy: a permission boundary that adds no scope, a narration boundary on the trader surface, and `CI-06o` |
| **039** | `claude/corpus-workflow-founder-rulings-py70hi`, the [FOLD-01](../plans/FOLD-01-phone-identity.md) fold | Passwordless auth, and phone as a first-class identity signal |
| **040** | `claude/corpus-workflow-founder-rulings-py70hi`, the [FOLD-02](../plans/FOLD-02-enforcement-window-and-suspension.md) fold | The payout enforcement window |
| **041** | `claude/corpus-workflow-founder-rulings-py70hi`, the [FOLD-02](../plans/FOLD-02-enforcement-window-and-suspension.md) fold | Identity-level restriction, and its enforcement surface |
| **042** | `claude/corpus-workflow-founder-rulings-py70hi`, the [S-E](../plans/P1-SE-trading-calendar.md) session | The trading-calendar source discipline, and the wall-clock unit ruling |
| **043** | `claude/corpus-workflow-founder-rulings-py70hi` | The append-only registries become directory-per-entry |
| **044** | `claude/merit-futures-briefing-7auoor`, the review desk (PR #25) | The AI and LLM policy: a permission boundary that adds no scope, a narration boundary on the trader surface, and `CI-06o`. **This row was written by a branch that does not hold that ADR**, because the number is taken and a table that only records what its own branch wrote is the table that let ADR-031 collide |
| **045** | `claude/builder-reviewer-loop-rykvhs` | [ADR-045](ADR-045.md), the `trading_calendar` prior image becomes a trigger. [ADR-042](ADR-042.md) was silent and the founder has ruled it. **Claimed as `044` first and moved**, see below |
| **046** | `claude/trading-calendar-holidays-0032-w8lq6m`, [P2](../plans/P2-rules-engine.md) section 8 | `OQ-P2-01`: `CalendarSlice` is a value, and a lookup miss is a typed refusal |
| **047** | `claude/trading-calendar-holidays-0032-w8lq6m`, [P2](../plans/P2-rules-engine.md) section 8 | `OQ-P2-02`: `rule_states` carries the calendar revision, and Appendix B.4 governs a calendar correction |
| **048** | `claude/trading-calendar-holidays-0032-w8lq6m`, [P2](../plans/P2-rules-engine.md) section 8 | `OQ-P2-03`: CI-03's polarity is derived per fixture from the rules it cites |
| **049** | `claude/ec157-repair-a-and-adr046`, [ADR-049](ADR-049.md) | `CalendarSlice` is a value and a lookup miss is a typed refusal (`OQ-P2-01`). **This ruling was written as `046` and moved here**, because `docs/decisions/ADR-046.md` carried two `## ADR-046` headings for two unrelated rulings. See below |
| **045** | merged to `main` (PR #29) | [ADR-045](ADR-045.md), the `trading_calendar` prior image becomes a trigger. [ADR-042](ADR-042.md) was silent and the founder has ruled it. **Claimed as `044` first and moved**, see below |
| **050** | `claude/corpus-workflow-founder-rulings-py70hi`, the `INV-06` scope ruling | `INV-06` gains a **stated `R-31` exception**: the floor never decreases except at the funded reset, where `R-12` initialises it to `size_cents` minus the **funded** drawdown. It amends a frozen invariant in [M01 section 1.5](../plans/M01-rules-engine.md), which is why it is an ADR and not a commit, and it is what unblocks `RE-P-01`. **Every remote ref was read before this number was taken** and all fourteen maxed at `049`, which is the manual step the `044` collision below exists to teach |
| **046** | `claude/builder-reviewer-loop-rykvhs` | [`OQ-M10-06`](../plans/M10-integrations.md)'s ruling: a contact address is held **reversibly** rather than not at all, a `value_ciphertext` beside the existing `value_hash` under a key only the dispatcher can use, so `INV-M16-03`'s prior-contact notification and [SECURITY §4.8](../architecture/SECURITY.md) leg 2 have a destination. **Every remote ref was read before this number was taken**, which is the manual step the `044` collision below exists to teach |
| **050** | `claude/builder-reviewer-loop-rykvhs`, golden fixtures batch 4 | [ADR-050](ADR-050.md), the founder ruling that `INV-06` gains a stated `R-31` exception, which makes the funded-reset floor citable and unblocks a fixture that crosses the eval pass. **The number was read off this table and off `git ls-remote` before it was taken**, per the `044` lesson below. **`INV-07` is deliberately NOT covered by it** and is a separate open founder item |
| **051** | `claude/merit-futures-briefing-7auoor`, the review desk | [ADR-051](ADR-051.md), `R-32`'s **anchor** and **which column binds**. `phase_eval.max_days` is authoritative and `accounts.expires_on` is derived; the count anchors at `opened_on`, which is ruled to be the first trading day the account is **tradeable** rather than the day it was paid for. Amends [M01 section 2.1](../plans/M01-rules-engine.md)'s `DayInput`, which is why it is an ADR. **No `rule_states` column, no `SD-nn`, and deliberately no migration number: `0037` stays free.** `git ls-remote` was read before the number was taken and every ref maxed at `050` |
| **052** | `claude/test3-3p7a5b`, the `GS-024` floor divergence | [ADR-052](ADR-052.md), `R-15`'s **lock effect**: the locked floor is `floor_lock_floor_at_cents` by assignment, not `max(hwb - drawdown_cents, floor_lock_floor_at_cents)`. Amends [M01 section 3.4](../plans/M01-rules-engine.md)'s binding expression and its `CV-12` redundancy claim, which is why it is an ADR, and it **reverses the reading [`floor.ts`](../../packages/rules-engine/src/day/floor.ts) implements**. `CV-11`'s derivation of `INV-21` is what decides it. **Status `proposed`: this amends a frozen document, changes engine behaviour and is money path, so the signature is the founder's.** `git ls-remote` was read before the number was taken and all twenty-eight refs maxed at `051` |
| **053** | `claude/trading-calendar-holidays-0032-w8lq6m`, the `rule_states` high-water bound | [ADR-053](ADR-053.md), what `rule_states_high_water_bounds_balance` asserts once `R-15` has frozen `hwb`. [`0015`](../../packages/db/migrations/0015_rule_states.sql) checks `high_water_balance_cents >= balance_cents` **unconditionally**, and `R-15` freezes `hwb` permanently at the lock, so **a locked account that makes a new closing high has a state row the database refuses to write**. `GS-016`'s expectation is exactly that row. The ruling is the **predicate that replaces it** and, as much, **what it deliberately stops asserting**. Money path, and the migration is `0037` below. **All twenty-eight remote refs were read before the number was taken**, by file and by claim in this table, and none held `053` or `0037` |
| **054** | the review desk, assigned 2026-08-17; the session holding it is in flight | `GS-064`'s surviving diff: a **three-way disagreement about what `R-35` measures on the row that CLOSES an account.** The fixture pins `20,000`, the engine produces `0`, and `R-35` applied to the pinned closing balance gives `40,000`. **Money path.** **This row is written by a branch that does not hold this ADR**, for the reason the `044` row below states |
| **055** | `claude/test-2r6zai`, the trading-calendar session boundary | **Reserved, unwritten.** `ADR-055` is deliberately NOT linked: the file does not exist yet and `CI-06a` fails on a link to an absent document, which is why a reservation row names a number rather than pointing at one. The **open end of a trade date's session**. `session_rule` computes a trade date's open as 17:00 CT on the prior calendar day, which the all-products artifact confirms exactly on an ordinary day and which is **wrong at the open end after a MID-WEEK holiday**: CME opens trade date `2026-09-08` on **Sunday `09-06`**, not Monday. A holiday does not remove a session, it **pauses the session belonging to the next trade date**, and that session opened before the holiday began. `early_closes` covers the close end and nothing covers the open end, so this is a gap in the FILE'S SHAPE rather than in a value. Also settles what coverage may honestly be declared, amending [ADR-042](ADR-042.md)'s `OQ-SE-02`. **Every one of the twenty-eight remote refs was read before this number was taken and all of them maxed at `052`** |

**Gaplessness is asserted over allocated plus reserved**, so a branch holding a reserved number shows a hole in this file and passes. A branch inventing an unreserved number fails.

**`053`, `054` and `055` were assigned to three parallel sessions within an hour of each other and none of them was reserved here at the time, so gaplessness was broken at every intermediate state and the first of the three to reach a commit paid for all of it.** The session holding `055` found `CI-06f` reporting `053` and `054` as holes, could not invent their subjects, and had to stop and ask. **That is a defect in how numbers are handed out, not in the gate**: `CI-06f` did exactly what [ADR-034](ADR-034.md) built it to do, which is refuse a sequence nobody can account for. The remedy is the one the `044` row below already names, applied at assignment time rather than at commit time: **a number is reserved in this table when it is handed out, by whoever hands it out, before the session that received it writes a line of anything.**

### `044` collided with an open sibling pull request, and this branch moved

**This session claimed `044`, wrote the ADR, the migration that cites it, its probe and eleven documents, and then found `044` already claimed by PR #25.** `main` ends at `043`, this branch is not behind `main`, and **twelve of twelve gates passed against that claim**, because `CI-06f` reads one ref and PR #25's branch is not it. **That is `CI-06f`'s declared coverage gap met in the wild for the second time in three days**, after [FOLD-02](../plans/FOLD-02-enforcement-window-and-suspension.md)'s plan hit it on `038`, and the cross-ref job that would close it is still not written.

**The remedy was a manual step this session skipped and the FOLD-02 session did not.** That session's record says "every remote branch was checked"; this one checked `main`, found `043`, and took `044`. **The table cannot do that step for you**: it is single-ref by construction, it says so in its own words two paragraphs down, and a claim made without reading the siblings is a claim made against a table that was never asked the question.

**It is resolved here rather than at merge**, which is FOLD-02's precedent rather than PR #4 and PR #5's: the founder assigned at merge once, [ADR-034](ADR-034.md) exists because that was expensive, and the branch that discovers a collision is the only one that can act on it. **This branch moved even though [ADR-034](ADR-034.md)'s tiebreak points the other way**, and the reasoning is worth the two lines it costs: the tiebreak renumbers the branch whose number is cited least, and this branch cites it forty-one times against PR #25's twenty-one, **including inside a migration**. But the tiebreak decides which branch *should* move, and only one branch *can*. **PR #25 is open, complete and waiting for a founder who would otherwise resolve a numbering conflict in a frozen registry by hand**; this session was still writing. The number cited inside a migration is the one that matters, and `0033` is unmerged, so the citation is a `sed` rather than a supersession.

### The State column is deleted, on ADR-034's own remedy

**A third column used to sit beside these two and say `allocated` or `reserved, unmerged`. It was wrong eleven times in nine days**, and the rows recorded it against themselves, one repair at a time: `035` four commits after PR #9 merged it, `036` after PR #13, `037` and `038` after PR #15, `039` through `043` after PR #17 and PR #18, and `045` with the migration table's `0033` after PR #29. **[ADR-036](ADR-036.md) already ruled the column prose no gate can check**, [STRATEGY](../testing/STRATEGY.md) already declared it as a coverage gap, and the column went on rotting anyway, because a declared gap is a description of a defect rather than a fix for one.

**[ADR-034](ADR-034.md)'s remedy has exactly two branches: generate the value, or delete it and point at the source.** There is no third, and in particular hand-repairing the column is not one, because that is what was being done, eleven times. This file already applied the remedy once, to the next-free-number sentence in the migration section below. Applied to the column, it splits the old cell in two:

| What the cell claimed | Disposition |
|---|---|
| **Is the artifact in this tree?** `docs/decisions/ADR-045.md` exists here, `0035_rule_states_calendar_revision.sql` exists here | **DERIVED, at check time, from the tree.** `CI-06f` and `CI-06h` already compute exactly this set to assert gaplessness against these tables; `node scripts/corpus/gates.mjs allocation` prints it per row. A derivation that runs is worth more than a cell that agreed with the tree on the day somebody typed it |
| **Has the branch that claimed it merged?** `allocated` against `reserved, unmerged` | **DELETED.** A runner reading one ref cannot tell a reservation from an allocation: the branch holding a reservation and the `main` that has absorbed it both show the number claimed. It is the two-ref problem [ADR-036](ADR-036.md) records, it is not closed here, and a claim no reader and no runner can check is not written down |

**Why the Claimed by column stays while the State column goes, which is the whole distinction.** A claimant is a historical fact and merging cannot falsify it: `merged to main (PR #17)` is true today and true forever, and a branch name records who to ask. **`reserved, unmerged` is false the instant the pull request merges, and nothing in the tree tells the row to change.** The volatile column is the one that was deleted, and it is the only one that ever drifted.

**What a reader loses, stated rather than glossed.** The table no longer says which claims are live. That was never a fact it held: it held a fact somebody entered, and the eleven instances are the measurement of how long that fact survived. What answers the question now is `git ls-remote`, which is the manual step the `044` collision above exists to teach, and `node scripts/corpus/gates.mjs allocation`, which answers the half a single ref can honestly answer.

**The duplicate rows are [`OI-11`](../STATE.md) and are not touched here.** Several numbers appear twice, a branch row beside the row recording that branch's merge, and choosing which of each pair survives is a decision rather than an edit. With the volatile column gone the pairs now differ only in their claimant, which is the form a dedup session will collapse.

## Migration number allocation, and why there is more than one table

**A migration number is claimed here before the file is written.** It is the same race as the one above, on the registry where it is least recoverable. **This heading read "why there are two tables" until a third arrived below it**, which is a hand-maintained count in the file that rules against them, one week old. [ADR-034](ADR-034.md) resolved the ADR collision by **renumbering the branch whose number was cited least**, and that remedy does not exist here: a migration is sacred once merged (constitution E2), so a number that has landed cannot be renamed, only superseded. **[ADR-036](ADR-036.md) rules the allocation and [CI-06h](../testing/STRATEGY.md) enforces it**, by the same rule as `CI-06f` and against this table.

| Numbers | Claimed by | What it installs |
|---|---|---|
| 0001 to 0028 | merged to `main` (PR #4, and `0028` with PR #9) | `0001` to `0027` are the schema-delta fold; **`0028` is [ADR-035](ADR-035.md)'s superseding migration**. `0025` is the marked reserved sequence, three tables created and unused at launch, which reserves *tables* and is an ordinary allocation of a *number* |
| **0029** | `claude/builder-reviewer-loop-rykvhs`, [FOLD-01](../plans/FOLD-01-phone-identity.md) section 4 | [`0029_phone_identity_and_auth.sql`](../../packages/db/migrations/0029_phone_identity_and_auth.sql), [ADR-039](ADR-039.md)'s nine changes, with an `E2 READ: MONEY PATH` header |
| **0030** | `claude/corpus-workflow-founder-rulings-py70hi`, [FOLD-02](../plans/FOLD-02-enforcement-window-and-suspension.md) section 6 | `0030_payout_hold_enum.sql`, the `ALTER TYPE ... ADD VALUE` and nothing else, deliberately without `BEGIN`/`COMMIT`. It is its own file because PostgreSQL refuses to use a new enum value inside the transaction that added it, and every index predicate in `0031` is such a use |
| **0031** | `claude/corpus-workflow-founder-rulings-py70hi`, [FOLD-02](../plans/FOLD-02-enforcement-window-and-suspension.md) section 6 | `0031_payout_hold_and_identity_restriction.sql`, [ADR-040](ADR-040.md)'s hold and [ADR-041](ADR-041.md)'s restriction episode |
| **0032** | `claude/corpus-workflow-founder-rulings-py70hi`, [S-E](../plans/P1-SE-trading-calendar.md) section 10 | `0032_trading_calendar_holidays_coverage_revisions.sql`, [ADR-042](ADR-042.md)'s F-1 through F-4 |
| **0033** | `claude/builder-reviewer-loop-rykvhs`, [ADR-045](ADR-045.md) | [`0033_trading_calendar_revision_required.sql`](../../packages/db/migrations/0033_trading_calendar_revision_required.sql), the trigger that makes [ADR-042](ADR-042.md) F-2's prior image a control rather than a rule the loader follows. **This row was committed and pushed BEFORE the migration file existed**, on the founder's instruction and on the reading below |
| **0034** | `claude/trading-calendar-holidays-0032-w8lq6m`, [ADR-047](ADR-047.md) | `rule_states` gains the calendar revision it was computed under, so replay can scope a calendar correction the way Appendix B.4 scopes an engine upgrade. **Money path: its own session, plan mode, fresh context**, and deliberately NOT written in the session that claimed the number |
| **0033** | merged to `main` (PR #29) | [`0033_trading_calendar_revision_required.sql`](../../packages/db/migrations/0033_trading_calendar_revision_required.sql), the trigger that makes [ADR-042](ADR-042.md) F-2's prior image a control rather than a rule the loader follows |
| **0035** | `claude/trading-calendar-holidays-0032-w8lq6m`, [ADR-047](ADR-047.md) | `0035_rule_states_calendar_revision.sql`, `OQ-P2-02`: `rule_states` gains the calendar revision it was computed under, so replay can scope a calendar correction the way [Appendix B.4](../plans/M01-rules-engine.md) scopes an engine upgrade. **`0034` was this migration's number and it collided**, see below. Money path, own session, claimed BEFORE the file exists |
| **0034** | `claude/builder-reviewer-loop-rykvhs`, `ADR-046` | `0034_reversible_contact_addresses.sql`, [`OQ-M10-06`](../plans/M10-integrations.md)'s ciphertext columns on `contact_channels`, `identity_phones` and `phone_change_requests`, the dispatcher role's grants over them, and the dispatch-record link that makes a notification obligation dischargeable by evidence rather than by a timestamp ([EC-146](../edge-cases/EC-146.md)). **Committed and pushed BEFORE the file existed**, which is `0033`'s reading applied a second time rather than a rule cited once |
| **0036** | `claude/ec157-repair-a-and-adr046`, [EC-157](../edge-cases/EC-157.md) | `0036_supersede_daily_marks_balance_arithmetic.sql`, [EC-157](../edge-cases/EC-157.md)'s **Repair A** as ruled 2026-08-16: `daily_marks_balance_arithmetic` is superseded by `closing = opening + realized_pnl`, because the constraint is the one artifact of five that disagrees with `INV-18`, `R-10`, `EC-034` and `0014`'s own column comment. **Money path.** Committed and pushed BEFORE the file existed, on `0033`'s reading |
| **0037** | `claude/trading-calendar-holidays-0032-w8lq6m`, [ADR-053](ADR-053.md) | `0037_supersede_rule_states_high_water_bounds_balance.sql`. [`0015`](../../packages/db/migrations/0015_rule_states.sql) is merged and is **never edited** (constitution E2), so its unconditional `rule_states_high_water_bounds_balance` is **superseded** here by the scoped form [ADR-053](ADR-053.md) rules, plus a probe wired into the migrations CI job the way [`probe_ledger_constraints.sql`](../../scripts/db/probe_ledger_constraints.sql) is. **Money path**, `E2 READ: MONEY PATH` header, claimed BEFORE the file exists on `0033`'s reading. [ADR-051](ADR-051.md)'s row above left `0037` free and it is still free on every remote ref |

**The next free number is the one after the last row of this table, and this file no longer says which it is.** It said `0029` while two approved plans stated their reservation rows "are written in the same commit as this fold's". They were not, and the sentence claiming nothing was reserved was **the only thing in the repository asserting it**, in the table that exists to end assertions of that kind. [ADR-034](ADR-034.md)'s remedy applies to it exactly: either generate the number or delete it and point at the source. **Deleted.** A session that needs a number adds its row here, and the row is what a sibling branch reads. **That deletion is the precedent the State column was repaired on**, one section up and one week later.

**And it is claimed BEFORE the file is written, in its own commit, which this sentence used to say the opposite of.** It read "in the same commit that creates the file", and that is a claim a sibling branch cannot see until the work is done: the whole window this table exists to close is the one between deciding on a number and having something to show for it, which is exactly the window a same-commit claim leaves open. [ADR-034](ADR-034.md) already rules the order for an ADR number ("claimed here before it is written") and the sentence over the registry where a collision is **unrecoverable** was the looser of the two. **The founder ruled the stricter reading on 2026-08-16** and `0033`'s row above is the first claim made under it: committed and pushed on its own, then the file.

**The same two-ref limit applies.** This table makes a collision visible to the second branch **that reads `main`**; it cannot stop a branch that never looks. The cross-branch assertion, that a pull request may not claim a number already on `main`, needs a job that can see both refs and belongs with `CI-06f`'s identical gap.

### `0034` was claimed twice IN THIS TABLE, and twelve gates read it and passed

**Two rows above both claim `0034`.** [ADR-047](ADR-047.md) claimed it for `rule_states`' calendar revision; `ADR-046` claimed it for the reversible contact addresses, wrote the file, and merged in PR #41. **The number is spent, so this migration is `0035`** and the row above records the move.

**This one was not the two-ref gap.** The ADR-044 collision two headings up was invisible because `CI-06f` reads one ref and the colliding claim lived on another. **This collision was in one file, on one ref, in adjacent rows, and every gate still passed** — because `allocated()` in [`gates.mjs`](../../scripts/corpus/gates.mjs) accumulates claims into a `Set`. Two rows claiming `0034` produce one member. Gaplessness holds, "every number on disk is claimed" holds, and **a table whose entire purpose is to make a duplicate claim visible cannot see one.**

**That is worth more than the collision it let through**, because the ADR remedy does not exist on this registry: [ADR-034](ADR-034.md) renumbers the branch citing the number least, and a merged migration cannot be renumbered at all (constitution E2). **Here the recovery was free only because the second file was still unwritten.**

The check is a handful of lines against a `Set` that already exists, and it is **not written in this session, for a stated reason**: the tables are full of duplicate rows today. The ADR table above claims `039` through `046` **twice each**, a branch row beside the row recording that branch's merge, and this table claims `0033` twice as well as `0034`. So the assertion fails on arrival and what it needs first is a cleanup of the historical record, which is a session and a decision about which of each pair survives rather than a gate. It is [`OI-11`](../STATE.md), raised by the branch that met it, and it is the same shape as `OI-10`: **a duplicate that a gate reading for presence cannot see.**

## CI gate identifier allocation, and why there are now three tables

**A `CI-06` letter is claimed here before the gate is written.** It is the third numbered registry in this repository and it had no table until three folds claimed from it in one week: [FOLD-01](../plans/FOLD-01-phone-identity.md) section 7 claims `k`, [FOLD-02](../plans/FOLD-02-enforcement-window-and-suspension.md) section 4.4 claims `l`, and [S-E](../plans/P1-SE-trading-calendar.md) section 7.1 claims `m`. **Each of the three plans states which letters the other two are taking**, which is three documents hand-maintaining one sequence and is precisely the arrangement [ADR-034](ADR-034.md) and [ADR-036](ADR-036.md) were each written to end. **ADR-038 collided this week for exactly this reason**, on the registry that already had a table, so the argument that a third table is premature has a counterexample three rows above it.

| Letter | Claimed by | What it checks |
|---|---|---|
| `a` to `j` | merged to `main` | The first ten gates in [`gates.mjs`](../../scripts/corpus/gates.mjs), rowed in [STRATEGY section 4.4](../testing/STRATEGY.md) |
| **`k`** | merged to `main` (PR #34), from `claude/corpus-workflow-founder-rulings-py70hi`, [FOLD-01](../plans/FOLD-01-phone-identity.md) section 7 | `CI-06k`, declared authority: every endpoint in the negative-authz matrix carries a required-factor cell, every sensitive action [ADR-039](ADR-039.md)'s C-27 names declares a non-single factor, and no `notification_kinds` class outside the post-identity security and money classes is `rate_limit_exempt` |
| **`l`** | merged to `main`, from `claude/corpus-workflow-founder-rulings-py70hi`, [FOLD-02](../plans/FOLD-02-enforcement-window-and-suspension.md) section 4.4 | `CI-06l`, every expiry has a sweep: each `*_expires_at` column in the migration set either names a release job in [CRON_INVENTORY](../ops/runbooks/CRON_INVENTORY.md) that is itself a row of that document's scheduled table, or sits on its written exemption list with a reason. **Stale entries in either list are findings**, in the [NO-FLOATS list](../../scripts/db/assert_no_floats.sql)'s idiom. Watched failing on a seeded violation and passing on a legitimate exemption whose **control fires**, which is `CI-06k`'s pair one gate over |
| **`m`** | `claude/corpus-workflow-founder-rulings-py70hi`, [S-E](../plans/P1-SE-trading-calendar.md) section 7.1 | `CI-06m`, the calendar's declared counts agree with its own contents, the fixture calendar is regenerated and `git diff --quiet`, and every `date` column in the migration set has a [DATA_MODEL](../architecture/data-model/README.md) row naming its unit |
| **`k`** | `claude/corpus-workflow-founder-rulings-py70hi`, [FOLD-01](../plans/FOLD-01-phone-identity.md) section 7 | `CI-06k`, declared authority: every endpoint in the negative-authz matrix carries a required-factor cell, every sensitive action [ADR-039](ADR-039.md)'s C-27 names declares a non-single factor, and no `notification_kinds` class outside the post-identity security and money classes is `rate_limit_exempt` |
| **`l`** | `claude/corpus-workflow-founder-rulings-py70hi`, [FOLD-02](../plans/FOLD-02-enforcement-window-and-suspension.md) section 4.4 | `CI-06l`, every expiry has a sweep: each expiry column in the migration set either names a release job in [CRON_INVENTORY](../ops/runbooks/CRON_INVENTORY.md) or sits on a written exemption list with a reason |
| **`m`** | `claude/corpus-workflow-founder-rulings-py70hi`, [S-E](../plans/P1-SE-trading-calendar.md) section 7.1, written in S-E session 5 (2026-08-16) | `CI-06m`, three checks: the calendar source's transcription state, exception lists and declared counts agree with each other and with its own contents; every generated calendar is DERIVED and reproduces, and an untranscribed source has no generated file at all; and every `date` column in the migration set has a [design record](../architecture/data-model/README.md) row declaring its unit as `**Unit: <token>**` from the closed vocabulary **trading day \| wall clock \| rail clock**. **The fixture-regeneration half is wired and cannot yet assert the days**, because the CME publication is untranscribed and there is nothing to derive from |
| **`n`** | `claude/corpus-workflow-founder-rulings-py70hi`, [ADR-043](ADR-043.md) | `CI-06n`, the registry-index gate: every entry file in a registry directory has a row in that registry README, and every README row resolves to a file. It is what pays for entry files being exempt from `CI-06c`. **Its parser matches any markdown link rather than a table row**, which is `OI-09` in [STATE](../STATE.md) rather than a note in a transcript |
| **`o`** | `claude/merit-futures-briefing-7auoor`, [ADR-044](ADR-044.md) section 8 | `CI-06o`, the money-path model ban: no module resolving from `packages/rules-engine` or from the payout, ledger or auth paths imports a model SDK or reaches a model endpoint, and a money path added without being added to the gate's scope is itself a finding. **Until it runs and has been watched failing on a seeded violation, [ADR-044](ADR-044.md)'s first prohibition is prose** |
| **`p`** | `claude/trading-calendar-holidays-0032-w8lq6m`, this table's own gate | `CI-06p`, the letter registry read the way the two above it are read: each `CI-06<letter>` heads **at most one** row of [STRATEGY section 4.4](../testing/STRATEGY.md), **every letter the runner implements is claimed by a row here**, and the letters are **gapless over implemented plus reserved**. `o` is reserved and unwritten, so the gaplessness is asserted over allocated PLUS reserved exactly as the migration table does it, and a hole a sibling branch reserved passes |

| **`q`** | `claude/test-2-ont6pi`, the polarity-citation defect of 2026-08-17 | `CI-06q`, **cited authority exists**: every dated reference to a founder ruling, in `docs/`, `scripts/` or `packages/`, resolves to a **registry declaration** — an `ADR-nnn` whose heading parenthetical carries that date, or a file under [`gates/`](gates/) whose filename does. **Resolution is deliberately not "some document mentions the date"**, because the citations would then satisfy each other and the gate would pass on the defect that commissioned it. **It checks that the authority EXISTS and cannot check that the authority SAYS WHAT THE CITATION CLAIMS**, which is stated in its own `covers` line and in [STRATEGY section 4.4](../testing/STRATEGY.md) rather than left for a reader to assume |

**One letter in this file is already spoken for by something that is not a gate, and it is named here so a reader does not mistake it for a reservation.** [ADR-036](ADR-036.md)'s alternatives list rejects "**`CI-06k`, a sibling of `CI-06f`**" by name. That `CI-06k` was never written and never claimed; the letter is FOLD-01's. **This is the failure mode the shared allocation parser was hardened against on the other two tables**, where a three-digit numeral in prose used to reserve a number silently, and it is why the first cell of a table row is the only thing that claims anything here either.

**This table is read by `CI-06p` now, which is the remedy this section named for itself and then went a week without.** It said "the cheap version of the gate is the one to write: uniqueness of the letters in the STRATEGY rows, and gaplessness over allocated plus reserved, which is `CI-06f`'s assertion with a different alphabet". That is what `CI-06p` is, and it reads letters through a parser of its own rather than through `allocated()`, because `allocated()` parses a three-digit or four-digit first cell and **a letter does not parse**, which is why nothing read this table for a week while two gates read the tables above it.

**Its third assertion is the one that keeps the table from going vacuous, and it is `CI-06h`'s.** Gaplessness alone cannot force a row: a letter implemented in the runner already fills its own hole, so the sequence stays gapless while the registry stops being maintained. **Every letter the runner holds must therefore be claimed by a row here**, exactly as every migration number on disk must be claimed by a row above.

---

### `ADR-046` was allocated twice IN ONE FILE, and the gate that checks for it could not run

**`docs/decisions/ADR-046.md` carried two `## ADR-046` headings** for two unrelated rulings: `OQ-P2-01`'s `CalendarSlice` ruling and `OQ-M10-06`'s reversible contact addresses. It was created by merging PR #40 and PR #41, neither of which had checked that the other claimed the number.

**This was not the two-ref gap and it was not a missing assertion either.** `CI-06f` has carried `if (seen.has(n)) findings.push("ADR-nnn appears more than once")` since it was written. **It could never fire**: `adrEntries()` in [`gates.mjs`](../../scripts/corpus/gates.mjs) read each file with a **non-global** `exec`, returning at most one entry per file, so two headings produced one entry and `seen` never collided. Fifteen gates passed over the duplicate.

**`ADR-049` is the one that moved, on a count rather than a guess**: 25 citing lines in 6 files against 70 in 20. And decisively, **37 of the other ruling's citations sit inside [`0034`](../../packages/db/migrations/0034_reversible_contact_addresses.sql), a merged migration constitution E2 forbids editing**, so that ruling was not renumberable at all. [ADR-034](ADR-034.md)'s tiebreak and E2 agree here, which they will not always do: **on this registry the ADR remedy and the migration rule can point in opposite directions**, and when they do, E2 wins because it is the one that cannot be complied with retroactively.

**The parser was tightened only after a seed was watched not firing.** `CI-06f/duplicate-heading` in [`falsify.mjs`](../../scripts/corpus/falsify.mjs) reported `DID NOT FAIL` against the non-global parser and `in scope, failed` after it, in that order, because tightening a parser without one is how 109 phantom anchors happened.
