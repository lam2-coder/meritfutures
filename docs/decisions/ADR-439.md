## ADR-439: the hole ADR-435 named in `golden` is closed in the shape that entry specified, and re-deriving its numbers corrects one of them (2026-09-07, status: proposed)

**Row 439 of the 435s wave. Branch `claude/bind-golden-skip-count`, off `claude/wave-435s` at `69662904`, session 634.**

**Status `proposed`. The approval line in section 11 is UNSIGNED and merging is not signing. `E2` READ OWED.**

**THE ROW WAS DISPATCHED TO CLOSE [ADR-435](ADR-435.md) SECTION 10'S NAMED HOLE IN THE SHAPE THAT ENTRY SPECIFIED, TO REFUSE THE MERGED-STAGE REPAIR IF THAT WERE THE ONLY ONE AVAILABLE, AND TO SHOW THE CONTROL FIRING IN BOTH DIRECTIONS BEFORE PUSHING.** It is closed, the merged-stage repair was never reachable, and both directions are in section 5 verbatim.

1. **THE HOLE REPRODUCES EXACTLY AS [ADR-435](ADR-435.md) DESCRIBED IT.** Two `vitest run` invocations, no reporter that writes a file, no step that reads one. Section 2.
2. **ONE OF ITS NUMBERS DOES NOT REPRODUCE, AND THE CORRECTION IS A UNIT ERROR RATHER THAN A COUNT ERROR.** `golden` is 56 cases as stated, over **2** test files and not 7. **7 is the SUITE count.** Section 2.
3. **AND THE HOLE IS LARGER THAN THE ENTRY THAT NAMED IT KNEW.** `golden.yml`'s SECOND invocation carries a registered skip today, so the unbound surface was never only a set of zeros. Section 3.
4. **THE REGISTER PASSES ON A PROJECT IT HAS NEVER SEEN, DERIVED FROM THE SOURCE AND THEN MEASURED.** No fix is owed in `skipped-tests.mjs` and none was made. Section 4.
5. **RED IN BOTH DIRECTIONS, ON COPIES OF REAL REPORTS THIS BRANCH PRODUCED.** Section 5.
6. **THE MERGED-STAGE REPAIR IS REFUSED AND `ci.yml:247-256` WAS READ AT SOURCE RATHER THAN QUOTED FROM [ADR-435](ADR-435.md).** Section 6.
7. **WHAT REMAINS UNBOUND AFTER THIS ROW, INCLUDING THE SECOND HOLE THIS ROW DOES NOT TOUCH.** Section 8.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `439`, session **634**, off the wave integration branch `claude/wave-435s` rather than off `main`.
- **NON-MONEY DIFF AND NON-MONEY SUBJECT.** Nothing in this row reads or writes a monetary value, no `*_cents` column, rate, threshold or basis point is named by any file it changes, and `git diff --name-only` against the base names no file under `packages/ledger`, `packages/rules-engine`, `packages/db`, `packages/db/migrations` or `scripts/db`. **The subject is how many test cases do not run in one CI stage.**
- **NO GATE ADDED, WIDENED OR WEAKENED. NO `CI-06` LETTER SPENT. NO `RI-nn` MINTED OR RESERVED. NO MIGRATION AND NO MIGRATION NUMBER. NO `.sql` FILE. NO SCHEMA DELTA. NO TEST FILE ADDED, CHANGED, RENAMED OR RETARGETED. NO TEST SKIPPED, DISABLED OR QUARANTINED, AND NOT ONE `skipIf` OR `runIf` SITE TOUCHED. NO REGISTER ROW ADDED, EDITED OR REMOVED. WHAT ANY SUITE RUNS IS UNCHANGED IN EVERY PROJECT.**
- **ONE WORKFLOW FILE IS EDITED AND IT IS THE ONE THIS ROW EXISTS TO EDIT.** [`.github/workflows/golden.yml`](../../.github/workflows/golden.yml). `.github/workflows/ci.yml` was read closely as the model and not changed.
- **`packages/tooling/checks/skipped-tests.mjs` WAS READ IN FULL AND NOT CHANGED**, which section 4 records as a derivation rather than as a courtesy: the question of whether it needed a change was asked and answered against the source.
- **THREE FILES IN THIS DIFF WERE WRITTEN BY `gates.mjs generate` AND NOT BY HAND, AND ALL THREE ARE NAMED HERE RATHER THAN LEFT TO BE FOUND.** [`docs/STATE.md`](../STATE.md) carries `CI-06g`'s `adr_count` span and this row moves it **420 to 421**, the mechanical consequence of adding an ADR file; [`docs/decisions/README.md`](README.md)'s ADR table and [`docs/sessions/README.md`](../sessions/README.md)'s ENTRY table are generated spans under [ADR-088](ADR-088.md) and each gains one row, sourced from this entry's own heading and from the session log's own `<!--index:` line. **No prose in any of the three is touched by hand.**
- **AND ONE ACT OUTSIDE THIS ROW'S LITERAL FENCE IS RECORDED RATHER THAN PASSED OVER: ADR NUMBER `438` IS RESERVED IN [ALLOCATION](ALLOCATION.md).** The fence granted this row's `439` cell only. `CI-06f` is gapless over allocated plus reserved and computes its ceiling from the highest number **on disk**, so writing `ADR-439.md` turned `438`, held by a concurrent row of this wave, into a hole and the gate went red on it. **A reservation is the corpus's own mechanism for exactly that** ([`gates.mjs:857-859`](../../scripts/corpus/gates.mjs): *"the allocation table is the reserved set: any number it names is allowed to be absent, because a sibling branch holds it"*), and it is the narrower of the two available acts: **it asserts nothing whatever about row `438`'s subject, branch or disposition**, and `CI-06f`'s [ADR-065](ADR-065.md) T3 leg turns red if that row is still reading `Reserved, unwritten` once `ADR-438.md` lands. **The alternative was pushing a branch with a red gate**, and [ALLOCATION](ALLOCATION.md)'s own note on `053`, `054` and `055` predicted this cost in advance: three numbers assigned to parallel sessions, none reserved at the time, *"and the first of the three to reach a commit paid for all of it"*. **This row is the first of its three and this is the payment.**

---

### 1. The container and the base, measured before the first edit

**THE CONTAINER ARRIVED WITH NO REPOSITORY.** `/home/user` held `.` and `..` and no third entry. [ADR-416](ADR-416.md) asked for the image to be fixed or the dispatch line deleted; [ADR-421](ADR-421.md), [ADR-424](ADR-424.md), [ADR-428](ADR-428.md), [ADR-429](ADR-429.md), [ADR-434](ADR-434.md) and [ADR-435](ADR-435.md) each recorded that neither happened. **This row records it a seventh time.**

**AND THE PROXY REFUSED THE FIRST PUSH, AS IT REFUSED [ADR-435](ADR-435.md)'s.** `git push` returned *access denied by the git proxy: lam2-coder/meritfutures is not in this session's authorized repository set*, `403`. The documented remedy, `add_repo` with `access: "push"`, was applied and the same push then succeeded with no other change. **That is now seven consecutive rows paying the same two costs out of their own budget.**

| measurement | observed |
|---|---|
| repository on arrival | **none** |
| base checkout | `696629044487c43a6aa6c045c5370ac880eb802f`, the head the dispatch named, on `origin/claude/wave-435s` |
| `pnpm install --frozen-lockfile` | **11s**, no lockfile change |
| gates on the untouched base | **33 of 33** |
| invariants on the untouched base | **35 of 35** |
| suite on the untouched base | **342 files / 8,310 passed / 11 skipped / 0 failed**, 8,321 total |

**`pnpm run verify` was NOT run and `scripts/corpus/falsify.mjs` was NOT run**, on the standing rule that both mutate the working tree and that `verify` chains `falsify`.

---

### 2. The hole, confirmed at source, and the one number that does not reproduce

**THE THREE PARTS OF THE CLAIM WERE CHECKED SEPARATELY AND ALL THREE HOLD ON THE BASE.**

| claim | where it was checked | result |
|---|---|---|
| `golden.yml` runs `CI-03` at `:112` | [`golden.yml:111-112`](../../.github/workflows/golden.yml) on the base, `pnpm exec vitest run --project golden --reporter=verbose` | **holds** |
| a second selection at `:127` | [`golden.yml:126-127`](../../.github/workflows/golden.yml) on the base, `pnpm exec vitest run --project unit packages/golden-loader` | **holds** |
| neither produces a report | neither line carries `--reporter=json` or `--outputFile`; `grep -n "reporter\|outputFile"` over the base file returns four lines, [`:96`](../../.github/workflows/golden.yml), [`:99`](../../.github/workflows/golden.yml), [`:101`](../../.github/workflows/golden.yml) and [`:112`](../../.github/workflows/golden.yml), of which three are comment prose about `--reporter=verbose` and the fourth is the flag itself. **`outputFile` appears nowhere in the file** | **holds** |
| no step reads a report | the job's steps on the base are `checkout`, `setup-node`, `Enable corepack`, `Install`, `Golden fixtures`, `The loader fails on a seeded violation`. **Six, and none of them is a check** | **holds** |
| `golden` reports **0** skipped today | measured, below | **holds** |

**AND THE 56 REPRODUCES WHILE THE 7 DOES NOT.** `pnpm exec vitest run --project golden --reporter=verbose --reporter=json`, read back off the report's own fields:

```
numTotalTestSuites 7 numTotalTests 56 passed 56 pending 0 todo 0 failed 0 success true
testResults files: 2
   packages/golden-loader/test/fixtures.golden.test.ts 54 {"passed":54}
   packages/rules-engine/test/evaluate.golden.test.ts 2 {"passed":2}
```

**[ADR-435](ADR-435.md) SECTION 10 SAYS *"`golden`'s 56 cases over 7 files"*. THE 56 IS RIGHT AND THE FILE COUNT IS 2.** The 7 is `numTotalTestSuites`, which counts the two files' root suites plus the five `describe` blocks in [`fixtures.golden.test.ts`](../../packages/golden-loader/test/fixtures.golden.test.ts) at [`:73`](../../packages/golden-loader/test/fixtures.golden.test.ts), [`:125`](../../packages/golden-loader/test/fixtures.golden.test.ts), [`:171`](../../packages/golden-loader/test/fixtures.golden.test.ts), [`:248`](../../packages/golden-loader/test/fixtures.golden.test.ts) and [`:282`](../../packages/golden-loader/test/fixtures.golden.test.ts). **It is a unit error and not a count error, and it changes nothing about the hole**; it is recorded because this row was told to re-derive rather than trust, and because a file count is what the register is keyed on, so reading a suite count as a file count is the kind of slip that would put a register row in the wrong place.

**THE ZERO IS THE INTERESTING HALF AND IT IS NOT PERMANENT.** [`fixtures.golden.test.ts:248`](../../packages/golden-loader/test/fixtures.golden.test.ts) is `describe.runIf(declaration.holds)`, so the day the declaration stops holding, that block stops running and `golden` acquires skips. **The stage's own file says so at [`golden.yml:38-39`](../../.github/workflows/golden.yml)** on the base: *"the end-to-end assertion is behind `describe.runIf(!stubbed)` and does not run, appearing only as a skip count"*. **A stage whose own header describes its assertions appearing only as a skip count is the last stage in this repository that should have had no reader for that count.**

---

### 3. The hole is larger than the entry that named it knew

**[ADR-435](ADR-435.md) SECTION 10 PRICES THE HOLE AS `golden`'s 56 CASES AND STOPS THERE. `golden.yml` RUNS A SECOND SELECTION, AND THAT ONE CARRIES A REGISTERED SKIP TODAY.**

[`golden.yml:127`](../../.github/workflows/golden.yml) on the base is `pnpm exec vitest run --project unit packages/golden-loader`, measured:

```
suites 18 total 103 passed 102 pending 1 todo 0
   packages/golden-loader/test/compare.test.ts 25 {"passed":25}
   packages/golden-loader/test/determinism.test.ts 10 {"passed":9,"skipped":1}
   packages/golden-loader/test/loader.test.ts 34 {"passed":34}
   packages/golden-loader/test/yaml.test.ts 34 {"passed":34}
```

**That one skip is [`determinism.test.ts`](../../packages/golden-loader/test/determinism.test.ts)'s `test.skipIf(!stubbed)` at [`:191`](../../packages/golden-loader/test/determinism.test.ts), which is the third row of the register** at [`skipped-tests.mjs:150-159`](../../packages/tooling/checks/skipped-tests.mjs). It is covered by `CI-02`, because the file is in the `unit` project and `CI-02` runs that project whole. **It was NOT covered here**, and `golden.yml:122-125` on the base gives the reason this job runs the loader suite twice in its own words: *"a CI-03 whose loader has quietly stopped refusing anything is a green stage that checks nothing"*. **The same sentence applies to a loader that has quietly stopped RUNNING a case, and until this row nothing in this job said so.**

**THE PRACTICAL CONSEQUENCE IS THAT THIS ROW'S SECOND STEP IS NOT A ZERO-ASSERTION.** [ADR-435](ADR-435.md)'s `CI-04` step asserts only that a stage skips nothing; this row's second step exercises the comparison against a non-zero register row, which is the half that has an expected value to be wrong about.

---

### 4. The register on a project it has never seen, derived before it was measured

**THE DISPATCH ASKED WHETHER A REPORT WITH ZERO SKIPS AND NO REGISTER ROW IS A PASS OR AN ERROR, AND ASKED FOR THE ANSWER DERIVED RATHER THAN ASSUMED.** It is a **PASS**, and the derivation is four sites in a file this row may not edit.

1. **Direction 1 defaults an unlisted file to zero rather than refusing it.** [`skipped-tests.mjs:289-292`](../../packages/tooling/checks/skipped-tests.mjs): `const want = row ? {...} : { skipped: 0, unwritten: 0 }`. A `golden` file the register never names is compared against `{0,0}` and matches.
2. **Direction 2 is skipped entirely for a file the report does not contain.** [`:310`](../../packages/tooling/checks/skipped-tests.mjs): `if (!actual.has(row.file)) continue;`. All three register rows are absent from the `--project golden` report, so all three continue and none produces a finding.
3. **The arithmetic refusal compares zero against zero.** [`:334`](../../packages/tooling/checks/skipped-tests.mjs) and [`:341`](../../packages/tooling/checks/skipped-tests.mjs) compare the report's `numPendingTests` and `numTodoTests` against the summed per-case statuses, `0 === 0` in both.
4. **`registerFindings` never reads the report at all.** [`:185-223`](../../packages/tooling/checks/skipped-tests.mjs) takes a root and not a report, so the register's own well-formedness is decided identically whichever slice is handed in.

**THE ONE REFUSAL THAT COULD HAVE FIRED IS THE EMPTY-REPORT THROW AT [`:275-280`](../../packages/tooling/checks/skipped-tests.mjs), AND IT CANNOT HERE**, because `--project golden` matches two files and Vitest exits non-zero on a project that matches none, which [`ci.yml:253-256`](../../.github/workflows/ci.yml) records as the reason `--passWithNoTests` must never be added to a stage's line.

**THEN MEASURED, ON THE TWO REAL REPORTS THIS BRANCH'S OWN WORKFLOW LINES PRODUCE:**

```
=== report from golden.yml:112 (--project golden) ===
PASS   the run's not-run cases match the register: 11 skipped and 0 unwritten across 3 file(s), of which this report ran 0
exit=0

=== report from golden.yml:127 (--project unit packages/golden-loader) ===
PASS   the run's not-run cases match the register: 11 skipped and 0 unwritten across 3 file(s), of which this report ran 1
exit=0
```

**SO NO FIX IS OWED IN `skipped-tests.mjs` AND NONE WAS MADE.** The dispatch's contingency, that the smallest honest fix would belong in a file outside this fence and would have to be reported rather than taken, did not arise. **`of which this report ran 0` is the line that says the shape was already designed for this**, and [ADR-435](ADR-435.md) section 6 had already measured the same thing on `CI-04`'s slice.

---

### 5. Red in both directions, on copies of reports this branch produced

**NEITHER DEMONSTRATION EDITS A TEST FILE, A `skipIf` SITE, A `runIf` SITE OR THE REGISTER.** Each takes a real report this branch's own workflow lines wrote, copies it, and moves one case's status, adjusting the report's summary counters the way a real run of that shape would carry them. **Adjusting the summary is deliberate**: leaving it inconsistent would fire the arithmetic refusal at [`:334`](../../packages/tooling/checks/skipped-tests.mjs) instead of the finding under test, and a demonstration that fires the wrong assertion demonstrates nothing.

**DIRECTION 1, A `golden` CASE STOPS RUNNING.** One case in [`evaluate.golden.test.ts`](../../packages/rules-engine/test/evaluate.golden.test.ts) marked `skipped`, in a project no register row names:

```
FAIL   the skipped-case register and the run disagree (1)
       packages/rules-engine/test/evaluate.golden.test.ts: the run reports 1 skipped and 0 unwritten (`.todo`), the register says 0 and 0. This file has no register row, so it may not skip a case. Add a row, or make the case run

A SKIP IS GREEN, WHICH IS WHY THIS IS RED. Either the change to what runs was intended, in which case the register is edited in the same commit, or it was not, in which case this is the warning that a case stopped running.
```

Exit **1**. **THAT IS THE HOLE CLOSING, STATED AS A MEASUREMENT.** On the base this report was produced by nothing and read by nothing, so the same tampering would have changed no CI outcome at all.

**DIRECTION 2, THE REGISTERED SKIP STARTS RUNNING.** The one skipped case in [`determinism.test.ts`](../../packages/golden-loader/test/determinism.test.ts) marked `passed`, in the second selection:

```
FAIL   the skipped-case register and the run disagree (2)
       packages/golden-loader/test/determinism.test.ts: the run reports 0 skipped and 0 unwritten (`.todo`), the register says 1 and 0. If the run is right, edit the row in packages/tooling/checks/skipped-tests.mjs
       packages/golden-loader/test/determinism.test.ts: the register expects 1 skipped and 0 unwritten and the run reports none at all. The gated block is gone, is no longer gated, or the file failed to import

A SKIP IS GREEN, WHICH IS WHY THIS IS RED. Either the change to what runs was intended, in which case the register is edited in the same commit, or it was not, in which case this is the warning that a case stopped running.
```

Exit **1**, and both sentences fire, which is [`skipped-tests.mjs:305-323`](../../packages/tooling/checks/skipped-tests.mjs)'s deliberate duplication doing its job: **a block that VANISHED reads differently from a block that grew, and the reader of a red build should not have to work out which happened.** The case that moved is the one whose title is *"the golden corpus is SKIPPED while the loader folds the identity stub"*, so **direction 2 here is the exact shape of the day the loader stops being a stub**, which is the event [`skipped-tests.mjs:155-158`](../../packages/tooling/checks/skipped-tests.mjs)'s `why` field predicts in words.

**AND THE PASSING DIRECTION WAS MEASURED ON THE REAL COMMANDS RATHER THAN ON THE TAMPERED COPIES**, with `RUNNER_TEMP` set the way the runner sets it, running the four steps of the edited file verbatim in order: both suites green, both checks `PASS` and exit **0**, and `git status --porcelain` afterwards naming only `.github/workflows/golden.yml`. **The report went to `RUNNER_TEMP` and left nothing in the workspace**, which is the property the placement was chosen for.

---

### 6. The merged-stage repair, refused at the source rather than on [ADR-435](ADR-435.md)'s word

**[`ci.yml:247-256`](../../.github/workflows/ci.yml) WAS OPENED AND READ, AND IT SAYS WHAT [ADR-435](ADR-435.md) SAYS IT SAYS:**

> THE TWO PROJECTS CI-02 OWNS, NAMED. `golden` is CI-03 and `integration` is CI-04, and both are deliberately absent: P1 section 2.2 makes the projects map to CI STAGES rather than to packages precisely so that this line can exist. `vitest run` with no selector would make CI-02 go red for a CI-03 failure and there would be no way to tell them apart.

**SO THE TRADE WAS NEVER AVAILABLE AND THE ROW NEVER APPROACHED IT.** The repair taken is the one [ADR-435](ADR-435.md) section 10 specified, in the stage that already runs the suite, and it costs no stage separation because it adds no project to any selection. **The stage boundary is not merely preserved, it is untouched**: `--project golden` at [`:122`](../../.github/workflows/golden.yml) and `--project unit packages/golden-loader` at [`:167`](../../.github/workflows/golden.yml) are the same selections the base carried, character for character up to the added reporter flags.

**AND `--reporter=verbose` IS KEPT BESIDE `--reporter=json` FOR A REASON STRONGER THAN THE ONE `CI-02` HAD.** [ADR-435](ADR-435.md) kept `--reporter=default` so a log would stay readable. Here the human-readable reporter is load bearing and the file says so at [`golden.yml:99-110`](../../.github/workflows/golden.yml): the stage prints a coverage statement that Vitest's default reporter swallows on a passing run, and *"the job log reads `Tests 13 passed | 3 skipped`, which is the exact shape of green check this ruling is about"*. **Replacing the reporter would have deleted the very output the stage exists to emit in order to buy a check on the very number that output is about.** Verified rather than assumed on this branch: with both named, the run still prints `- **Coverage of the registry is 43 of 316.**` and still prints every case by name, **and** writes the report.

---

### 7. What was built

**FOUR STEPS WHERE THERE WERE TWO, IN ONE FILE, AND NO OTHER FILE EXECUTES.**

- [`golden.yml:120-124`](../../.github/workflows/golden.yml). `Golden fixtures` gains `--reporter=json` and `--outputFile="$RUNNER_TEMP/ci-03-golden-report.json"`, beside the `--reporter=verbose` it already carried.
- [`golden.yml:143-144`](../../.github/workflows/golden.yml). `The skipped-case register still matches the run` runs [`skipped-tests.mjs`](../../packages/tooling/checks/skipped-tests.mjs) over that report.
- [`golden.yml:165-169`](../../.github/workflows/golden.yml). `The loader fails on a seeded violation` gains `--reporter=default --reporter=json` and its own `RUNNER_TEMP` path.
- [`golden.yml:177-178`](../../.github/workflows/golden.yml). `The skipped-case register still matches this selection` runs the check over that one.

**NEITHER CHECK RUNS A SUITE**, so the runtime cost of this control is two Node start-ups on a job that already runs two Vitest processes.

**NO `CI-06` LETTER IS SPENT AND NO `RI-nn` IS MINTED**, on [ADR-435](ADR-435.md) section 8's argument unchanged and not re-litigated here: every `CI-06` gate runs under `gates.mjs check` and every `RI-nn` under `repo-invariants.mjs`, both of which read the tree and neither of which runs a suite, and the number this control needs exists only inside a run. **This row inherits that ruling rather than restating it, and it inherits section 11's first open question with it**: the consequence is a control that `gates.mjs list` and `repo-invariants.mjs list` still do not name, and this row has now doubled the number of stages relying on it without adding a row to either register.

---

### 8. What remains unbound after this row

**THE FIRST HOLE IS CLOSED AND THIS SECTION SAYS SO NARROWLY.** Every Vitest project this repository declares is now compared against the register by the job that runs it: `unit` and `property` by `CI-02`, `integration` by `CI-04`, `golden` by `CI-03`, and `CI-03`'s second `unit` selection by its own step. **Four declared projects, four covered, plus one filtered selection.**

**[ADR-435](ADR-435.md) SECTION 10'S SECOND, SMALLER HOLE IS NOT TOUCHED BY THIS ROW, AND THE ANSWER TO THE DISPATCH'S QUESTION IS NO.** That hole is `RI-03`: it asserts that each of the four stage names is PRESENT in [`vitest.config.ts`](../../vitest.config.ts) and not that there are only four, **so a FIFTH project run by no job, or run by a job with no register step, is outside both `RI-03` and this control**. Nothing this row does changes that. This row adds a register step to an existing job for an existing project; it mints no invariant, widens no invariant, and adds no assertion about how many projects exist. **A fifth project added tomorrow and selected by no workflow would be as invisible after this row as before it**, and a fifth project selected by a workflow whose author does not copy the register step would be invisible in exactly the way `golden` was. **No number is minted for it here either, and it is recorded a second time rather than left to look closed because the first hole beside it now is.**

**AND THE THIRD THING, WHICH IS THE ONE [ADR-435](ADR-435.md) STATED ABOUT ITSELF AND IS STILL TRUE.** Binding the count is not closing the hole [ADR-428](ADR-428.md) section 10 and [ADR-429](ADR-429.md) section 11 both price. **Eleven cases still do not run in CI and this row leaves all eleven not running**, and it touched no `skipIf` or `runIf` site in either direction. What it buys is that the next movement in that number is loud in one more stage than it was.

**ONE THING THAT IS BOUND ONLY BY CONVENTION.** The two report paths are string literals in [`golden.yml`](../../.github/workflows/golden.yml), written twice each, and nothing checks that the path a check reads is the path the run above it wrote. **A typo in one of the four would make the check fail to read its file and exit `2`**, which is red rather than silently green, so the failure mode is the safe one; but the same is true of [ADR-435](ADR-435.md)'s two and no row has minted anything for it.

---

### 9. Counts, derived at the moment of reporting

| runner | result |
|---|---|
| `node scripts/corpus/gates.mjs check` | **33 of 33 gates pass**, exit 0, counted off the runner's own PASS lines |
| `node packages/tooling/checks/repo-invariants.mjs` | **35 of 35 invariants hold** |
| `pnpm vitest run`, this branch | **342 files / 8,310 passed / 11 skipped / 0 failed**, 8,321 total |
| `pnpm vitest run`, the untouched base | **342 files / 8,310 passed / 11 skipped / 0 failed**, 8,321 total |
| `pnpm run typecheck` | exit 0 |
| `pnpm run lint` | exit 0 |
| `pnpm run format:check` | exit 0 |

**STATED AS THE DISPATCH ASKS: base 8,310, mine 8,310, delta 0.** **The suite total was read off THIS BRANCH's own last line after the last edit rather than carried from section 1**, which [ADR-435](ADR-435.md) section 12 records four entries getting wrong; the two figures are equal here because this row adds no test case, and the equality is a measurement rather than a copy. **AND THE SKIPPED COUNT, STATED SEPARATELY: base 11, mine 11, delta 0.** This row adds no test case and removes none; it adds four workflow steps, two of which run a suite this branch did not change and two of which run a check over the reports.

**`docs/STATE.md`'s DIFF IS ONE INTEGER**, `CI-06g`'s `adr_count` span moving by one, written by `generate` and not by hand. No prose in that file is touched.

**NOTHING HERE TOUCHES MONEY**, and that was checked rather than assumed: `git diff --name-only` against the base names no file under `packages/ledger`, `packages/rules-engine`, `packages/db`, `packages/db/migrations` or `scripts/db`, and no file this row wrote contains a `*_cents` identifier, a rate, a threshold or a basis point. **The one money-adjacent file this row's diff so much as names is `packages/rules-engine/test/evaluate.golden.test.ts`, and it is named inside a transcript in section 5 rather than edited.**

---

### 10. What a founder read adds and this entry cannot

**THE FIRST DECISION IS WHETHER THIS CONTROL SHOULD NOW GET A NUMBER.** [ADR-435](ADR-435.md) section 11 left that open with one stage-pair relying on the check. **This row makes it two jobs and four steps, and the argument for leaving it unnumbered gets weaker with each one**: a reader auditing what this repository enforces still finds nothing in `gates.mjs list` or `repo-invariants.mjs list` naming it, and there are now four places where deleting one line silently removes a control. **A thin `RI-nn` asserting that every workflow step running `vitest run` is followed by a step reading its report would be static, would be checkable, and would close the convention-only half section 8 records.** This row did not mint it because minting an `RI-nn` is not in its fence and because the shape of that assertion is a design choice a row should not make silently.

**THE SECOND IS WHETHER `golden` SHOULD HAVE A REGISTER ROW WAITING FOR IT.** [`fixtures.golden.test.ts:248`](../../packages/golden-loader/test/fixtures.golden.test.ts)'s `runIf` means this stage's skip count is expected to move when M01 lands, and on that day this row's own control goes red on a change everybody wanted. **That is the tax [ADR-435](ADR-435.md) section 7 priced and section 11 asked a founder to confirm is the right way round**, and this row has just extended it to the stage where the movement is most predictable. **A founder may reasonably say the tax is now large enough to want the alternative that fires only on a case STOPPING**, and this row's position is that it should not, for [ADR-435](ADR-435.md)'s reason: a control that fires only on bad changes has encoded somebody's idea of intent.

**THE THIRD IS THE CORRECTION IN SECTION 2 AND WHAT IT SAYS ABOUT RE-DERIVATION.** [ADR-435](ADR-435.md)'s *"56 cases over 7 files"* was wrong in its second number, in an entry that is otherwise careful and that derived the 56 correctly in its own section 2. **Nothing would have caught it**, because no gate reads an ADR's prose for a file count, and the next row to trust it would have looked for seven files. **The dispatch's instruction to re-derive rather than trust is what caught it**, and whether that instruction should be standing on every row that cites a sibling's number is a judgement about how much this corpus pays for re-derivation versus what it pays for drift.

---

### 11. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** [ADR-435](ADR-435.md), whose section 10 this row answers, is itself `proposed` and unsigned on this row's base, as are [ADR-428](ADR-428.md), [ADR-429](ADR-429.md), [ADR-436](ADR-436.md) and [ADR-437](ADR-437.md) beside it.

- **Approval:** `___________________________` (UNSIGNED. **ONE WORKFLOW FILE AND THE REGISTRIES. NO CHECK MODULE CHANGED, NO TEST FILE ADDED OR CHANGED, NO REGISTER ROW EDITED, NO MIGRATION, NO `.sql` FILE, NO SCHEMA DELTA, NO GATE, NO `RI-nn`, NO `CI-06` LETTER, NO `skipIf` OR `runIf` SITE, AND NOTHING UNDER `apps/**`, `packages/**` OR `scripts/**`.**)

| | |
|---|---|
| **Approved by** | |
| **Date** | |
