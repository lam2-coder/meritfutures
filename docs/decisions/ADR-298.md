## ADR-298: five citations in the plan every payout row reads. Four line numbers, one backwards range, six sites, and the sixth pointer in the same sentence resolves to nothing and is LEFT  (2026-08-30, status: proposed)

**[ADR-287](ADR-287.md) is the nine-slice payout plan every remaining slice is dispatched from, and five of its citations do not address what they say they address.** Row `298` dispatched their repair. **All five were re-derived at source in this session and all five are stale.** None of [ADR-289](ADR-289.md) section 1.3's numbers was copied, and section 2 records why that instruction was load bearing rather than ceremonial: **two of the six live values that entry published are themselves stale today**, and one of the two is stale because of ADR-289's own ruling.

**THE REPAIR IS FIVE CITATIONS ACROSS SIX SITES, AND THE SIXTH SITE IS THE POINT.** `wiring.test.ts:1337` carries the wired count twice in [ADR-287](ADR-287.md), at its opening claim and at slice 3's stop condition. Repairing one instance of a wrong pointer and leaving the other is the exact shape this row exists to end, so both moved.

**NO FINDING, NO RULING, NO VERDICT AND NO PROSE MOVED.** The diff on [ADR-287](ADR-287.md) is three lines, every changed byte is inside a `file:line` pointer, and section 6 names three substantive claims the tree now contradicts and repairs **none** of them.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `298`, session **490**, branch `claude/five-citations-in-the-plan`. **NON-MONEY. NO `src/` LINE MOVED, NO TEST CHANGED, NO MIGRATION WRITTEN OR RESERVED, NO `RI-nn` MINTED.**

- **CHANGING AN APPROVED DOCUMENT REQUIRES AN ADR AND THIS IS IT.** [ADR-287](ADR-287.md) ships `proposed` with an UNSIGNED approval line and is the plan the money-path rows read. The edit to it lands under this entry, and this entry records what each citation said so the record is amended rather than overwritten.

- **[ADR-065](ADR-065.md) T3 AND `CI-06f` ARE NOT REACHED.** No reservation lands, no ADR number moves, and nothing in [ADR-287](ADR-287.md)'s substance is rewritten, which is the prohibition `CI-06f` enforces around this class of edit.

- **Ships `proposed` with an UNSIGNED approval line. Merging is not signing.** It inherits `E2` debt on [ADR-287](ADR-287.md), which is `proposed` and UNSIGNED and which this row reads as its subject rather than as a premise.

---

### 1. The five, re-derived at source

**Every live value below was derived by opening the file in this working tree and matching on the TEXT, which is [ADR-289](ADR-289.md) section 4.2's own instruction, *"the row that takes this matches on the text and not on the number."*** The "what `:nnn` holds today" column is why each is a false citation rather than an imprecise one: the address resolves, and the line says something else.

| # | [ADR-287](ADR-287.md) said | What that address holds today | Live, derived here | Primary source at the live line |
|---|---|---|---|---|
| 1 | [`payouts.ts:588-566`](../../apps/api/src/routes/payouts.ts) for the declared return type | **Nothing. A backwards range addresses no line** | [**`payouts.ts:588-590`**](../../apps/api/src/routes/payouts.ts) | `insertPayoutRequest(` / `row: PayoutRequestInsert,` / `): Promise<{ readonly eligibilitySnapshotId: string }>;` |
| 2 | [`payouts.ts:1177`](../../apps/api/src/routes/payouts.ts) for the route's destructure | `ordinal: evaluation.ordinal,`, a property of the insert row | [**`payouts.ts:1201`**](../../apps/api/src/routes/payouts.ts) | `const { eligibilitySnapshotId } = await tx.insertPayoutRequest(row);` |
| 3 | [`payouts.ts:1226`](../../apps/api/src/routes/payouts.ts) for the wire field | a comment about `LT-01` and the idempotency key | [**`payouts.ts:1250`**](../../apps/api/src/routes/payouts.ts) | `eligibility_snapshot_id: eligibilitySnapshotId,` |
| 4 | [`payouts.ts:248`](../../apps/api/src/routes/payouts.ts) for the field on `PayoutResponse` | `};`, closing `estimated_settlement` one field earlier | [**`payouts.ts:249`**](../../apps/api/src/routes/payouts.ts) | `readonly eligibility_snapshot_id: string;` |
| 5 | [`wiring.test.ts:1337`](../../apps/api/test/wiring.test.ts) for **ten wired of twenty-four, fourteen blocked** | prose inside a different test, and it is about citation grammar | [**`wiring.test.ts:1432`**](../../apps/api/test/wiring.test.ts) | `}).toStrictEqual({ declared: 24, wired: 10, blocked: 14 });` |

**THE FIFTH IS THE ONE WORTH READING TWICE.** `wiring.test.ts:1337` lands inside that file's own written warning that *"a `file:line` pointer is a claim about THIS tree, so a pointer quoted as HISTORY must not wear the shape that says follow me (ADR-212)."* A stale citation for the wired count came to rest on the sentence explaining why stale citations are dangerous.

**AND THE COUNT ITSELF IS UNCHANGED AND WAS RE-DERIVED RATHER THAN CARRIED.** `{ declared: 24, wired: 10, blocked: 14 }` is asserted at [`wiring.test.ts:1432`](../../apps/api/test/wiring.test.ts), inside the test at [`:1425`](../../apps/api/test/wiring.test.ts) whose name is *"the wired count is reported, so a regression is a number and not a paragraph."* **Ten wired of twenty-four declared, fourteen blocked HOLDS.** Only the address moved.

**SIX SITES FOR FIVE CITATIONS.** Citation 5 appears twice in [ADR-287](ADR-287.md), at its opening claim and in slice 3's stop-condition cell in section 7. Both were repointed. Citations 1 to 4 sit on one line and appear once each.

---

### 2. WHY [ADR-289](ADR-289.md) SECTION 1.3's NUMBERS WERE NOT COPIED, AND WHY THAT WAS NOT PRECAUTIONARY

Row `298` forbade copying them. **The prohibition earned itself: two of the six live values that entry published no longer hold.**

| [ADR-289](ADR-289.md) 1.3 published | Today | Verdict |
|---|---|---|
| return type at `588-590` | `588-590` | **still true**, re-derived here |
| destructured at `:1201` | `:1201` | **still true**, re-derived here |
| wire field at `:1250` | `:1250` | **still true**, re-derived here |
| `PayoutResponse` at `:249` | `:249` | **still true**, re-derived here |
| `API_CONTRACT.md` line 490 **HOLDS** | line 490 is `hold: {`, and the field is gone from the contract | **FALSE TODAY.** Section 3 |
| the live count assertion at `wiring.test.ts:1402-1409` | `:1402` is a factory walk, `:1405-1409` is a comment and a `Set` of adapter names | **FALSE TODAY.** The assertion is at `:1425-1432` |

**A repair that trusted the last line of that table would have replaced one wrong pointer with a different wrong pointer, dated today.** That is the defect wearing a fresh date, and it is what a re-derivation is for.

**THE SECOND FALSE ROW IS NOT DRIFT AND IS MORE INTERESTING THAN THE FIRST.** [ADR-289](ADR-289.md) section 4.1 deleted `eligibility_snapshot_id: string;` from `type PayoutResponse` in [API_CONTRACT](../architecture/API_CONTRACT.md) as its own ruling. **So the entry that recorded "line 490 HOLDS" is the entry that stopped it holding**, in the same commit set. Nothing drifted; a ruling landed, and a live value published beside it aged out the moment it was published.

---

### 3. THE SIXTH POINTER IN THE SAME SENTENCE CANNOT BE RESOLVED TO A PRIMARY SOURCE, SO IT IS NAMED AND LEFT

F2's sentence carries a sixth pointer beyond the four repaired here: *"and required by [API_CONTRACT section 6](../architecture/API_CONTRACT.md) at line **490**."*

**THERE IS NO LINE TO REPOINT IT TO.** [ADR-289](ADR-289.md) removed the field from the contract. `grep -n eligibility_snapshot_id docs/architecture/API_CONTRACT.md` returns **one line, [`:500`](../architecture/API_CONTRACT.md)**, and that line is the note recording the removal: *"This response carried `eligibility_snapshot_id: string` until ADR-289, and the field is REMOVED rather than supplied... **Do not restore it.**"* [`API_CONTRACT.md:490`](../architecture/API_CONTRACT.md) today is `hold: {`.

**REPOINTING IT TO `:500` WOULD BE A SUBSTANCE EDIT WEARING A CITATION'S CLOTHES.** F2's claim is that the contract **requires** the field. The contract now forbids it. A pointer moved from the requirement to the record of its deletion changes what the sentence asserts, and this row has no standing to change what [ADR-287](ADR-287.md) asserts. **The citation is left exactly as written and is recorded here as a finding.** The claim it carries is dispositioned in section 6.

---

### 4. THE CAUSE, DERIVED FROM THE COMMIT RATHER THAN INHERITED

[ADR-289](ADR-289.md) section 1.3 named the cause and got its shape right and its extent wrong. **Both halves are re-derived here from the two commits themselves.**

**[ADR-287](ADR-287.md) WAS AUTHORED WITH CORRECT CITATIONS AND ONE COMMIT FALSIFIED THEM ALL AT ONCE.** At `00cb1bb^`, the parent of [ADR-285](ADR-285.md)'s build, **every** pointer this session checked resolved to the line it named: `PayoutTx` at `539`, `PayoutBackend` at `570`, `transact` at `572`, `listPayouts` at `575`, `idempotency` at `586`, `IdentityStatus` at `323`, `PayoutSubject` at `333`, `HoldFlag` at `469`, `PayoutRequestInsert` at `476`, `PayoutListItem` at `260`, `snapshotOf` at `1046`, `readonly tx: PayoutTx` at `1097`, `unwiredOrThrow` at `1386`. `00cb1bb` added 125 lines above them.

**THE REPAIR PASS THAT FOLLOWED IS NAMED "nine citations" AND MOVED FIVE.** `96be966`, *"Merge origin/main into ADR-287, and repoint nine citations ADR-285 moved"*, changed **five lines of [ADR-287](ADR-287.md)** and repointed **five** pointers: `:548` to `:564`, `:551` to `:575`, `:554` to `:578`, `:564` to `:588`, and `:564-566` to `:588-566`. **The fifth took the new start and kept the old end**, which is the backwards range. Everything else in the file was left where `00cb1bb` had put it.

**SO [ADR-289](ADR-289.md)'s *"that pass repointed section 1's table in full"* IS NOT WHAT THE COMMIT DID.** Section 1's table has seven rows and the pass repointed four of them. Rows `C1`, `C2` and `C3` were not touched and are stale today, which is section 5. **The correction matters because it changes the size of what is owed**: a pass that fully repaired one table and missed one finding leaves three pointers wrong, and a pass that repaired four of seven rows leaves twenty-five sites wrong.

**AND ONE OF THE UNREPOINTED THREE NOW RESOLVES TO A REAL DECLARATION THAT IS THE WRONG ONE.** `C2` cites `listPayouts` at [`payouts.ts:575`](../../apps/api/src/routes/payouts.ts). After `00cb1bb`, **`:575` is `subject(accountId): Promise<PayoutSubject | null>;`**, which is the line `C2`'s own table repointed row 2 to. A reader following `C2` lands on a plausible port member, declared, in the right interface family, and wrong. **A citation that resolves to something believable is worse than one that resolves to a comment**, and no check in this tree reads it.

---

### 5. EVERY OTHER CITATION IN [ADR-287](ADR-287.md), SWEPT, AND THE TWENTY-FIVE SITES THIS ROW DOES NOT REPAIR

**The sweep was mechanical over the whole file and its result is narrow, which is itself the finding.** Every `file:line` pointer in [ADR-287](ADR-287.md) was resolved against this tree. **The pointers into `packages/db/migrations/**`, `packages/db/src/scope.ts`, `packages/db/src/scoped-db.ts`, `packages/rules-engine/**`, `scripts/corpus/gates.mjs`, `apps/api/src/idempotency-store.ts`, `apps/api/src/rule-state-reader.ts`, `docs/architecture/STATE_MACHINES.md` and `docs/legal/TOS_CLAUSES.md` all resolve to the text they name.** So does `wiring.test.ts:147-1240` and `wiring.test.ts:138`.

**THE DRIFT IS CONFINED TO ONE FILE, `apps/api/src/routes/payouts.ts`, PLUS THE ONE `wiring.test.ts` POINTER REPAIRED ABOVE.** That is `00cb1bb`'s blast radius and nothing else.

**TWENTY-FIVE SITES ARE STALE AND THIS ROW REPAIRS NONE OF THEM.** They are listed in full, with the live value derived at source, so the row that takes them has a checklist rather than a judgement, on [ADR-289](ADR-289.md) section 4.2's own idiom.

| # | [ADR-287](ADR-287.md) line | Cited | Names | Live, derived here |
|---|---|---|---|---|
| 1 | 3 | `payouts.ts:539` | `PayoutTx` declares FOUR methods | **555**, `export interface PayoutTx {` |
| 2 | 21 | `payouts.ts:539` | `PayoutTx` | **555** |
| 3 | 21 | `payouts.ts:570` | `PayoutBackend` | **594**, `export interface PayoutBackend {` |
| 4 | 29 | `payouts.ts:572` | `PayoutBackend.transact<T>(session, fn)` | **596** |
| 5 | 30 | `payouts.ts:575` | `PayoutBackend.listPayouts(session)` | **599**. `:575` is `subject()` today, section 4 |
| 6 | 31 | `payouts.ts:586` | `PayoutBackend.idempotency` | **610**, `readonly idempotency: IdempotencyStore;` |
| 7 | 33 | `payouts.ts:572` | `PayoutTx` as a type in `transact` | **596** |
| 8 | 33 | `payouts.ts:1097` | `readonly tx: PayoutTx` | **1121** |
| 9 | 33 | bare `` `:564` `` | the `insertPayoutRequest` declaration | **588**. `:564` is `identityStatus()` |
| 10 | 33 | bare `` `:1177` `` | the `insertPayoutRequest` call | **1201** |
| 11 | 33 | `payouts.test.ts:362` | the only `PayoutTx` fixture | **363**, and section 6 finding S2 |
| 12 | 45 | `payouts.ts:323` | `IdentityStatus` | **324** |
| 13 | 47 | `payouts.ts:541` | the docblock that says why the method takes no account | **556-563**, the sentence at **559** |
| 14 | 47 | `payouts.ts:1106` | `decidePayout` calls it first | **1130** |
| 15 | 55 | `payouts.ts:333` | `PayoutSubject` | **334** |
| 16 | 59 | `payouts.ts:1113` | the call site that states the 404 is the point | **1137** |
| 17 | 69 | `payouts.ts:1386-1387` | `unwiredOrThrow` rethrows what is not `PayoutBackendUnwired` | **1502-1503** |
| 18 | 99 | `payouts.ts:469` | `HoldFlag` | **485** |
| 19 | 138 | `payouts.ts:476` | `PayoutRequestInsert` | **492** |
| 20 | 140 | `payouts.ts:1046` | `snapshotOf` | **1070** |
| 21 | 140 | `` `:1060` `` | `snapshotOf` writes `clamp.reason` | **1084** |
| 22 | 140 | `` `:1063` `` | `snapshotOf` writes `clamp.split_bp` | **1087** |
| 23 | 148 | `payouts.ts:260` | `PayoutListItem` | **261** |
| 24 | 161 | `payouts.ts:471` | `HoldFlag.tosClause` | **487** |
| 25 | 169 | `payouts.ts:534` | `transact` is handed the session and binds once | **550** |

**WHY THIS ROW STOPS AT FIVE, STATED AS A REASON AND NOT AS AN APOLOGY.** Row `298` specified five citations and this entry records five. **Repairing an arbitrary further subset is the failure mode `96be966` already demonstrated on this exact file**: a pass that stops where its author's attention stopped leaves a document that reads as repaired and is not. The choice is therefore between the row's five and all thirty, and thirty is a different row's fence and a different row's re-derivation. **What this row can do without widening is leave the remainder COMPLETE rather than arbitrary**, which is the table above.

**IT SHOULD BE ONE ROW AND IT NEEDS NO NEW RIGHTS.** Row `298`'s own fence, `docs/decisions/ADR-287.md` for citations only, is the fence that repair needs. **The twenty-five are mechanical and depend on nothing.**

---

### 6. THREE SUBSTANTIVE CLAIMS THE TREE NOW CONTRADICTS. NAMED, AND NOT FIXED

**[ADR-287](ADR-287.md) IS THE PAYOUT PLAN AND RE-DECIDING ANY OF ITS SLICES IS A MONEY-PATH RULING.** This row is non-money and has no standing to make one. Each of these is recorded and left exactly as written.

**S1. F2's claim that the contract REQUIRES the field is false, and its own subject already ruled it so.** F2 reads *"required by [API_CONTRACT section 6] at line 490."* [ADR-289](ADR-289.md), taking F2 as its dispatch, ruled the field **out of the frozen contract and out of the port shape**, and [API_CONTRACT:500](../architecture/API_CONTRACT.md) now says *"Do not restore it."* **F2's finding survives its own resolution**: there is still no supplier, which is why the ruling went the way it did. **The sentence is a record of the position that produced the ruling and repointing it would rewrite that record.** It is the citation section 3 leaves alone.

**S2. Section 1's closing paragraph is false in two counts.** It reads *"`grep -rn insertPayoutRequest` over every `apps/*/src` and `packages/*/src` returns **2 lines**... and no third line is an implementation. The only value in this repository satisfying `PayoutTx` is a test fixture."* **That grep returns SIX lines today.** Four are in [`payout-backend.ts`](../../apps/api/src/payout-backend.ts), which [ADR-291](ADR-291.md) landed as slice 3: a comment at `:22`, a comment at `:233`, and the member itself at [`:238-239`](../../apps/api/src/payout-backend.ts), which **rejects with `PayoutBackendUnwired` rather than implementing**. So the claim's spirit holds, no line is an implementation, and its letter does not. **And `postgresPayoutBackend` is a second value satisfying `PayoutTx`**, alongside two fixtures in [`payouts.test.ts`](../../apps/api/test/payouts.test.ts) rather than one. **This is a dated planning record that a later slice overtook, which is the plan working**, and it is not this row's to restate.

**S3. Slice 1's blocker is discharged and section 7 still reads as though it were open.** [ADR-287](ADR-287.md) section 7 blocks slice 6 on the slice 1 ruling, and F2 closes *"SO A RULING IS OWED AND IT IS NOT A TRANSCRIPTION."* **[ADR-289](ADR-289.md) made it**, [ADR-293](ADR-293.md) section 4 specified the resulting edits, and [ADR-297](ADR-297.md) discharged slice 7's separate blocker. **Nothing in [ADR-287](ADR-287.md) says so, because an ADR is a record of a moment and the moment has moved.** Recorded so a reader arriving at section 7 cold does not re-open a settled question.

---

### 7. What this row did not do

- **It did not sign anything.** [ADR-287](ADR-287.md)'s approval line is untouched, its `status: proposed` is untouched, and this entry's own approval line is UNSIGNED.
- **It did not change a finding, a verdict, a ruling, a slice, a stop condition or a sentence of prose in [ADR-287](ADR-287.md).** Every changed byte in that file is inside a `file:line` pointer. `git diff` on it is **three lines**.
- **It did not repair the twenty-five sites in section 5**, and it did not repair the one in section 3 that cannot be resolved.
- **It did not touch `apps/**`, `packages/**`, `scripts/**` or `e2e/**`.** Those trees were read and not written, measured rather than intended.
- **It minted no `RI-nn`.** Section 5's sweep argues for a check and section 8 records why this row does not write it.

---

### 8. `RI-16` DOES NOT READ THIS FILE, BY DESIGN, AND NO INVARIANT IS MINTED HERE

**`RI-15` and `RI-16` are the two checks that bind a citation to a line, and neither one reads [ADR-287](ADR-287.md).** `RI-15` reads six source files and its own `covers` line says it reads `docs/decisions` *"NOT AT ALL"*. `RI-16` reads the whole of `docs/` and excludes, as its exclusion 1, any citation under a dated or session heading, because *"an entry headed `## ... (2026-08-24)` ... IS A RECORD OF A MEASUREMENT MADE THAT DAY"* and rewriting one *"would REWRITE THE RECORD TO SAY SOMETHING IT DID NOT SAY, which is a worse defect than the one being repaired."* **[ADR-287](ADR-287.md) opens with a dated `##` heading, so every pointer in it is out of scope at level 1, and so is every pointer in this entry.**

**THAT EXCLUSION IS CORRECT AND IT IS ALSO EXACTLY WHY THIS ROW EXISTS.** The five stale citations survived a repair pass, a founder-facing plan, and three build rows reading it as their dispatch, with nothing mechanical following one of them. **The remedy `RI-16` already names is an ADR and not a widened check**, and this entry is that ADR: the old pointer is recorded beside the new one in section 1, so the record is amended in the open rather than silently overwritten.

**NO `RI-nn` IS MINTED AND `RI-39` IS LEFT FREE.** Three candidates were considered and each refused. **A check that followed citations inside dated ADR headings would be RED ON LANDING over the twenty-five sites in section 5**, over an unknown number in the other 302 ADRs, and over a corpus rule this row has no standing to overturn. **A check narrowed to [ADR-287](ADR-287.md) alone** would be a reminder wearing a gate's clothes, green the day it lands and silent on the next document to drift. **A check asserting that no citation range runs backwards** is the one candidate that is neither, and it is genuinely small, but it catches one of these five and is silent on the other four, so landing it would report a class as guarded that is not. **The real control is the one section 5 leaves behind: a complete list, in a file a row can be pointed at.**

---

### 9. The files outside the stated fence, and why they moved

**None.** Row `298`'s fence is `ADR-298`, [`docs/decisions/ADR-287.md`](ADR-287.md) for citations only, [ALLOCATION](ALLOCATION.md), [STATE](../STATE.md), [INDEX](../INDEX.md), the registry READMEs and the session log. **Every file this session wrote is inside it, and one file inside it needed no edit and received none.** [INDEX](../INDEX.md) indexes the [decisions registry](README.md) rather than the entries inside it, so a new ADR adds no row to it; the ADR count is a generated span and it lives in [STATE](../STATE.md), where `generate` moved it from 281 to 282. The two registry READMEs, [`docs/decisions/README.md`](README.md) and [`docs/sessions/README.md`](../sessions/README.md), are **GENERATED** by `node scripts/corpus/gates.mjs generate` under `CI-06n` and were not hand-edited.

**AND ONE THING THE ROW REQUIRED COULD NOT BE DONE.** The row ends *"open the pull request, do NOT merge it."* **The branch is pushed and mergeable and the pull request is NOT open**: `gh` is not installed in this container and the GitHub REST API returns **403, *"GitHub access is not enabled for this session"***, on write and on read alike. **It is recorded here rather than reported as done**, and it is the only part of row `298` this session did not complete.

---

### 10. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.**

This is a **repair** row on a **non-money** document that the money path is dispatched from. It builds nothing and decides nothing.

- **Decision:** **Five citations in [ADR-287](ADR-287.md) are repointed across six sites**, each derived at source in this session and each recorded beside what it used to say: the backwards range `588-566` becomes `588-590`, `:1177` becomes `:1201`, `:1226` becomes `:1250`, `:248` becomes `:249`, and `wiring.test.ts:1337` becomes `:1432` in both places it appears. **A sixth pointer in the same sentence, `API_CONTRACT.md` line 490, cannot be resolved to a primary source because [ADR-289](ADR-289.md) deleted the text it addressed, and it is LEFT.** **No finding, verdict, ruling or slice moved.**
- **Consequences:** **[ADR-287](ADR-287.md)'s five load-bearing citations now resolve**, and the wired count it states, ten of twenty-four with fourteen blocked, is confirmed at [`wiring.test.ts:1432`](../../apps/api/test/wiring.test.ts) rather than quoted. **Twenty-five further stale sites in the same file are enumerated with live values in section 5 and are OWED to one row**, which needs no rights beyond the ones row `298` already had. **Three substantive claims the tree contradicts are named in section 6 and left**, each of them a money-path question. **`RI-39` is left free.**

**What a founder read adds and this entry cannot.**

**THE FIRST THING TO DECIDE IS WHETHER STOPPING AT FIVE WAS RIGHT.** Section 5 argues that the choice is between the dispatched five and all thirty, because an arbitrary subset is the failure `96be966` already produced on this file. **A founder who reads that differently should say so, and the answer is one row with the fence row `298` already carried.** The twenty-five are derived and listed; nothing is waiting on analysis.

**THE SECOND IS SECTION 3, WHICH IS THE ONE JUDGEMENT IN THIS ENTRY THAT COULD HAVE GONE THE OTHER WAY.** Leaving a pointer that resolves to the wrong line is uncomfortable, and repointing it to [`API_CONTRACT:500`](../architecture/API_CONTRACT.md) would make the sentence resolve. **It would also make F2 assert that the contract records the field's deletion, which is not what F2 says and not what was true when F2 was written.** If the founder prefers the sentence to resolve over the record to stand, that is a substance edit and it belongs to a money-path row, not to this one.

**THE THIRD IS SECTION 6 S3, THE ONLY PART THAT COMPOUNDS.** [ADR-287](ADR-287.md) section 7 is the dispatch surface for the remaining slices and it still reads as though slice 1 were open. **Every future row reading it cold reads a blocker that three ADRs have discharged.** A record of a moment is right to leave alone; a live dispatch table is not. **Which of the two section 7 is, is a founder call and it is the one this entry most wants overruled if it guessed wrong.**

- **Approval:** `___________________________` (UNSIGNED. **Merging is not signing.**)
