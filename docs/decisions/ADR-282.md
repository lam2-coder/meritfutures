## ADR-282: a generated line is never written twice, and the fence had to reach the generator before the check could say so  (2026-08-30, status: proposed)

**Row `282` said the ceiling is 8 and it does not catch doubling, it catches the eighth doubling. That is right, the eight duplicates it measured reproduce here, all eight are defects, and the sharper discriminator it named survives every test this entry could put to it. THE ROW'S FENCE DOES NOT.** The discriminator the row proposes is grounded in `gates.mjs generate`, and that generator writes into every tracked `.md` in this repository rather than into `docs/`. Measured at this commit, the live population of the defect is fifteen lines over five files, and the fifth file is [`packages/db/DELTA_MANIFEST.md`](../../packages/db/DELTA_MANIFEST.md), which carries **seven** of them and which the fence does not name.

**SO THE SESSION STOPPED AND REPORTED RATHER THAN CHOOSING BETWEEN TWO BAD MOVES, AND THE FENCE WAS EXTENDED TO THE GENERATOR'S OWN SCOPE.** A check scoped to `docs/` would have landed green over 8 of the 15 and would have been a gate shaped to fit a fence, which is the move [ADR-279](ADR-279.md) was written to end. A check scoped to the generator would have landed RED on seven findings in a file the session could not edit. The dispatcher verified the finding at source, granted `packages/db/DELTA_MANIFEST.md` and the generator's scope, and granted nothing else. **`RI-31` IS MINTED AND `check:invariants` REPORTS 30 OF 30.** Section 6 keeps the refusal as the record, because the reason the check is correct is that nobody narrowed it.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `282`, session **473**, branch `claude/a-ceiling-that-catches-the-eighth-doubling`. **NO MIGRATION NUMBER IS TAKEN OR RESERVED** and `packages/db/migrations/**` is untouched. **NOT the money path**: no `src/` line moves, no schema, no ledger, no engine.

- **`RI-12` IS UNCHANGED, AND THIS ENTRY ARGUES THAT ITS NUMBER IS CORRECT.** Section 4 shows what breaks at each candidate value and finds a legitimate line sitting at exactly 8. The ceiling is not loose, it is saturated. **The row leaned toward lowering it and that instinct was wrong**, which is said here plainly because a row's instinct is the thing a later reader inherits.

- **`RI-31` IS MINTED**, over the generator's own scope, with two sentinels that throw, watched RED on the real tree in both halves of that scope, and its register row is in the same commit with the title read out of the live `CHECKS` array. **THE FENCE WAS EXTENDED BY THE DISPATCHER AND NOT BY THIS SESSION**, which is the only reason the check has the scope the property needs.

- **Ships `proposed`, approval line UNSIGNED. Merging is not signing.**

---

### 1. What the row claimed, re-derived rather than inherited

The row hands over a measurement taken on `main` before dispatch. Every figure below was taken again on this branch, before any repair, by a script reading the tracked tree.

| The row's claim | What this branch measures | Verdict |
| --- | --- | --- |
| eight duplicated generated lines | eight, under `docs/` | **reproduces** |
| across four files | four: `architecture/data-model/README.md`, `STATE.md`, `testing/STRATEGY.md`, `sessions/2026-08-16-session-38.md` | **reproduces** |
| the worst at seven copies in `docs/architecture/data-model/README.md` | seven, at that file's lines 474 to 477, 482, 484 and 490 | **reproduces** |
| then five and five | five in the same file at 542 to 546, five in `STATE.md` at 2527, 2560, 2585, 2608 and 2629 | **reproduces** |
| two at two in `STATE.md` | **one** at two in `STATE.md` (1798 and 1800) and **one** at five. The full shape is 7, 5, 5, 4, 2, 2, 2, 2, so **four** lines sit at two copies and they are spread over all four files | **CORRECTED** |
| every one is invisible to `RI-12` | every one. The largest is 7 against a ceiling of 8 | **reproduces** |
| the dispatcher has hand-collapsed this at three separate merges, each time with the gate green | three collapse commits are in the visible history and **all three removed a line carrying a `<!--gen:` span**. Section 3 | **reproduces, and it is sharper than the row knew** |

**THE ONE CORRECTION IS NOT COSMETIC.** The row's shape implies the ceiling would have to fall a long way to reach the tail. The real shape has half the population sitting at exactly two copies, which is the first doubling and nothing more, and a ceiling of any value above one leaves all four of them live.

**AND THE POPULATION IS LARGER THAN THE ROW BELIEVED.** Widening the same measurement from `docs/` to every tracked `.md`, which is what `gates.mjs`'s own `markdownFiles()` reads, gives **fifteen** duplicated generated lines over **five** files. The seven this row could not see are in [`packages/db/DELTA_MANIFEST.md`](../../packages/db/DELTA_MANIFEST.md) at lines 11 to 27, 35 to 38, 480 to 484, 734 to 738, 849 to 852 and 927 to 930. Section 6.

### 2. THE RULING

**RULING 1. A GENERATED SPAN MAY LEGITIMATELY APPEAR MANY TIMES IN ONE FILE. A LINE CARRYING ONE MAY NOT APPEAR TWICE.** The row asked this question first and it is the right one to ask first, because getting it wrong in either direction ruins the check. `gate_count` alone occurs **192** times across `docs/`, twice on a single line wherever a document writes "N of N", and every one of those is correct. The property is therefore about the LINE and never about the span name.

**RULING 2. ON THIS CORPUS, NO LINE CARRYING A GENERATED SPAN IS LEGITIMATELY WRITTEN TWICE, AND THE MEASUREMENT IS STRONGER THAN THE ARGUMENT.** There are **175** lines carrying a `<!--gen:` span under `docs/`, spread over **34** files. The number of those lines that appear byte-identically in **two different files** is **zero**. Not one sentence in this corpus that carries a derived number is written the same way twice anywhere, let alone twice in one document. The row's hypothesis, that one query rendered in two sections would make some of the eight correct, is a real possibility and it has no instance. A second rendering of the same query is written as a different sentence, because it is being said for a different reason.

**RULING 3. ALL FIFTEEN ARE DEFECTS, RULED PER SITE RATHER THAN PER CLASS.** The row required that the ruling come before the repair. It does, and the evidence is per site:

| Site | Copies | Why it is a defect and not a rendering |
| --- | --- | --- |
| `docs/architecture/data-model/README.md:474-492` | 7 and 4 and 2 | One paragraph and one `###` heading interleaved with each other. **A document does not render a heading four times.** The section reads `paragraph, heading, paragraph, heading, paragraph, heading` where it should read `paragraph, heading` |
| `docs/architecture/data-model/README.md:542-546` | 5 | Five adjacent identical lines under one `###` |
| `docs/STATE.md:1798,1800` | 2 | Adjacent identical pair, one blank line apart |
| `docs/testing/STRATEGY.md:271,272` | 2 | Adjacent identical pair |
| `docs/sessions/2026-08-16-session-38.md:4,5` | 2 | Adjacent identical pair, both the first `- **Done:**` bullet |
| `docs/STATE.md:2527,2560,2585,2608,2629` | 5 | **The interesting one.** One sentence in five different session sections, and **four of those five sections already carry their own, different "The position:" line.** Section 2.1 |
| `packages/db/DELTA_MANIFEST.md`, seven lines | 7, 7, 4, 5, 5, 4, 4 | Adjacent identical runs, the first two interleaved with each other exactly as the `data-model` case is. **Outside this fence.** Section 6 |

**RULING 4. THE DISCRIMINATOR IS THE SPAN, THE SCOPE IS ONE FILE, AND THE CEILING IS ONE.** Not a length floor, not a ceiling above one, and not a span name. Sections 3 and 4 measure each of those alternatives rather than asserting against them.

**RULING 5. `RI-12` STAYS EXACTLY AS IT IS.** Its ceiling is not a compromise that a sharper check now makes unnecessary. Section 4 finds a **legitimate** line sitting at 8, so the ceiling could not be lowered by even one without a false positive, and `RI-12`'s scope contains 29 further repeated lines that are not this defect and must not become findings.

#### 2.1 The `STATE.md` splatter, and where the sentence lives

The five copies are not five sessions agreeing. The sentence reports `falsify:ci` at 68 of 68 runnable cases with 3 unrun for missing scanners, and that measurement belongs to one session: [`docs/sessions/2026-08-24-session-148.md:105`](../sessions/2026-08-24-session-148.md) reads _"68 of 68 runnable cases were watched failing on the violation aimed at them, and 3 could not run here."_ The copy at `STATE.md:2527` is the one inside session 148's own section. The other four sit in the sections for sessions 176, 173, 174 and 175, each **immediately below that section's own, different, position line**, and each reporting a test count of 1,512 where its host section reports 1,526. The keep-both merge rule put one session's closing sentence into four other sessions' records, and it is a **false statement about four sessions** rather than a cosmetic repetition. That is the sharpest single argument in this entry for why a duplicated generated line is never harmless.

### 3. The discriminator, measured rather than taken

The row named the discriminator and told this session to measure it rather than take it. Three measurements, each of which could have refuted it.

**FIRST: DOES IT SEPARATE THE DEFECTS FROM THE LEGITIMATE REPETITION?** Inside `RI-12`'s own scope, lines of at least 80 characters repeating within one file, this tree carries **37** distinct such lines. Partitioned by whether the line carries a `<!--gen:` span: **8 carry one and all 8 are defects; 29 do not and the largest of them are legitimate.** The partition is clean in both directions on the whole population, which is the strongest form this claim could take.

**SECOND: WOULD IT HAVE CAUGHT WHAT WAS ACTUALLY MISSED?** The row says the dispatcher hand-collapsed this defect at three merges with the gate green. Three such commits are in the visible history, `ca2f86f`, `ce026dd` and `c3e7293`, and **every line removed by every one of them carries a `<!--gen:` span**. All three collapsed the same STATE.md headline, the one whose first span is named `adr_count`. `RI-12` saw none of the three because each stood at two or three copies. The proposed check is three for three on the only real trials available.

**THIRD: IS THE DEFECT CLASS ALREADY ON THE RECORD AS NEEDING THIS EXACT GATE?** Yes, and it predates `RI-12`. [`docs/STATE.md`](../STATE.md), in session 174's section, records _"A PRE-EXISTING DEFECT IS REPORTED AND NOT REPAIRED, BECAUSE IT IS OUTSIDE THE FENCE AND NEEDS A GATE CHANGE. The `gen:adr_count` line is DUPLICATED VERBATIM in both STATE and INDEX ... `CI-06g` cannot see it: the gate rewrites every span it finds and compares each to its own query, so two identical copies both pass."_ That was written on 2026-08-24. `RI-12` landed on 2026-08-27 and does not close it. **This is the fourth wave to name this gate and the first to specify it.**

**A FOURTH MEASUREMENT THAT CAME BACK NEGATIVE AND IS REPORTED ANYWAY.** Masking fenced code blocks the way `gates.mjs` masks them, so that a documented example of the form is not read as a span, changes **nothing** on this tree: fifteen duplicates either way. The masking belongs in the check because the generator has it and because [STRATEGY](../testing/STRATEGY.md)'s own `CI-06g` section shows the form inside a fence, but it is a correctness choice for a future document rather than a thing that moves today's number, and saying so is cheaper than letting a later reader assume it was load bearing.

### 4. The ceiling, and what breaks at each candidate value

The row forbade lowering 8 to 2 without showing what breaks at each candidate. Two separate questions live here and they have opposite answers.

**QUESTION ONE: WHAT WOULD BREAK IF `RI-12`'s CEILING MOVED?** Its header says _"a zero would be a check nobody could keep green"_ ([`repo-invariants.mjs:1621`](../../packages/tooling/checks/repo-invariants.mjs)) and names short structural tokens as the reason. **The header is right and its reason is understated.** Measured over `RI-12`'s exact scope on this tree:

| Ceiling | Findings on today's tree | What breaks |
| --- | --- | --- |
| 8, as shipped | 0 | Nothing, and it sees none of this row's subject |
| 7 | 1 | [`docs/reviews/2026-08-20-admin-parity-audit.md`](../reviews/2026-08-20-admin-parity-audit.md) heads **eight separate tables** with one 94-character header row, `\| # \| Trader route \| Source \| ... \|`. It is legitimate, it is structural, and **it is 94 characters, so the length floor does not exempt it** |
| 6 | 2 | Adds [`docs/plans/P3-wave3-modules.md`](../plans/P3-wave3-modules.md), whose seven session prompts each close with the same one-line regime declaration |
| 5 | 3 | Adds [`docs/plans/WAVE-04-fixture-backlog-and-gate-inventory.md`](../plans/WAVE-04-fixture-backlog-and-gate-inventory.md), same shape, six session prompts |
| 2 | 14 after this entry's repair | Twenty-three distinct lines sit at exactly two copies in `RI-12`'s scope and only four of them are this defect |
| 0 | 29 after this entry's repair | The header's own claim, and the number is 29 rather than a handful |

**SO `RI-12`'s 8 IS THE TIGHTEST VALUE THAT ADMITS EVERY LEGITIMATE LINE ON THIS TREE, AND IT IS SATURATED.** A ninth parity table in that one review document turns `RI-12` red on a line nobody should touch. That is worth knowing and is reported in section 8; it is not this row's fence and nothing here changes it.

**QUESTION TWO: WHAT CEILING DOES THE NEW CHECK TAKE?** Its scope is different, so the answer is different, and it is derived rather than borrowed.

| Ceiling for the generated-line check | Of the 15 live instances it catches | What breaks |
| --- | --- | --- |
| 8, copied from `RI-12` | 0 | It is `RI-12` again under a second name, which is a second copy of a threshold and nothing else |
| 4 | 5 | Permits three doublings. It lands with ten live defects still on the tree and the check green over them |
| 2 | 6 | **Permits exactly the first doubling, which is the whole failure mode.** It lands with nine live defects still on the tree, six of them at two copies, and reports PASS over every one |
| 1, that is, no repetition at all | 15 | **Nothing measured.** Zero legitimate instances exist to sacrifice: no gen-carrying line repeats within a file except these fifteen, and none repeats across two files anywhere in the corpus |

**THE LENGTH FLOOR GOES TOO, AND FOR THE SAME REASON.** `RI-12` needs its 80-character floor because `---` and `\|---\|---\|` and a bare fence are lines. None of those carries a generated span. Of the 175 gen-carrying lines under `docs/`, exactly **4** are shorter than 80 characters and each is substantive: two are multi-line span openers standing alone on their line, one is a `###` heading, one is a table cell. Keeping a floor would buy nothing and would create a hole exactly where a generated table cell repeats.

### 5. What changed

**ALL FIFTEEN DUPLICATED LINES ARE COLLAPSED, IN TWO COMMITS SEPARATED BY THE FENCE QUESTION.** Fifty-one deletions, zero insertions, and no line was reworded: every repair keeps the first occurrence byte for byte and removes the rest, together with the blank line orphaned in the one case that had one.

| File | Deletions | Shape |
| --- | --- | --- |
| `docs/architecture/data-model/README.md` | 14 | one paragraph and one heading interleaved, plus a five-line run |
| `docs/STATE.md` | 6 | one adjacent pair, one sentence splattered into four other sections |
| `docs/testing/STRATEGY.md` | 1 | adjacent pair |
| `docs/sessions/2026-08-16-session-38.md` | 1 | adjacent pair |
| `packages/db/DELTA_MANIFEST.md` | 29 | two lines interleaved at seven copies each, plus four adjacent runs |

**`RI-31` IS ADDED TO `CHECKS`, ITS REGISTER ROW TO THE `RI-nn` TABLE, AND NINE CASES TO THE SUITE.** The next-free note moves to `RI-32`.

**Nothing else in this repository is edited by this entry.** No `src/` line, no migration, no gate runner, and nothing under `packages/db/migrations/**`: `DELTA_MANIFEST` is a manifest and not a migration, so constitution E2 does not reach it, and the grant said so explicitly.

### 6. THE FENCE, WHY THIS SESSION REFUSED IT, AND WHAT IT WAS EXTENDED TO

**THE ROW'S FENCE FOLLOWS FROM A `docs/`-ONLY MEASUREMENT WHILE THE ROW'S OWN DISCRIMINATOR FOLLOWS FROM THE GENERATOR.** The row grounds the discriminator in one sentence: _"a line carrying a `<!--gen:` span is written by `gates.mjs generate`"_. That generator's file set is `markdownFiles()`, which is `allFiles()` filtered by extension, and `allFiles()` walks the repository rather than `docs/`. So the row's reasoning reaches every tracked `.md`, and the row's fence reaches four documents under `docs/`.

**THAT GAP IS NOT HYPOTHETICAL. IT IS SEVEN LIVE DEFECTS.** [`packages/db/DELTA_MANIFEST.md`](../../packages/db/DELTA_MANIFEST.md) carries seven duplicated generated lines, of the same family as the four repaired here, sharing the `manifest_changes` and `migration_files` spans with them and standing at up to seven copies. That file is `status: review`, it is not frozen, it declares `docs/architecture/data-model/README.md` in its own `depends_on`, and the same merges produced both. It is not in the fence.

**BOTH AVAILABLE MOVES ARE REFUSED, AND THE REFUSALS ARE THE SAME RULE TWICE.**

1. **Scope the check to `docs/`.** It lands green, it reports PASS, and it is silent over 7 of the 15 live instances of the very defect it is named for. That is an absence check over a scope built to make the tree pass, which is what [ADR-279](ADR-279.md) found `RI-28` doing one wave ago. **Weakening a gate to pass it.**
2. **Scope the check to the generator and repair `DELTA_MANIFEST` too.** Correct, and it edits a file the row did not name. **Widening a fence to finish.**

The row's own standing rule says these are the same move, and the session took neither. It reported, stopped, and held the number: `RI-29`'s second leg requires a row to name a check that exists, so a register row for a check nobody had written would have failed it.

**THE FENCE WAS EXTENDED, AND ONLY AS FAR AS THE PROPERTY REACHES.** The grant added `packages/db/DELTA_MANIFEST.md`, to collapse its seven duplicated generated lines ruled per site, and `RI-31` scoped to what `gates.mjs` generates over. It added nothing else, and it said that a file outside those turning out to carry one is another stop-and-report rather than a licence. None does: measured after the repair, over every `.md` in the tree, the count is zero.

**ONE CITATION IN THE GRANT IS CORRECTED, BECAUSE A GRANT IS NOT A SOURCE EITHER.** It located the generator's scope at `scripts/corpus/gates.mjs:166`, which is `isCorpusDocument`, the predicate `CI-06b` and `CI-06c` share for what counts as a corpus document. The generator does not call it. `generate()` iterates `markdownFiles()`, which is `allFiles()` filtered by extension, and `allFiles()` is a walk of the repository. **The correction runs in the same direction as the grant and makes it stronger**: the generator's scope is WIDER than `isCorpusDocument`, which excludes `docs/reviews/` and every registry entry, so a check built on `isCorpusDocument` would have had a second hole in it. `RI-31` reads `markdownFiles()`.

**THE TWO WALKS WERE COMPARED RATHER THAN ASSUMED EQUAL.** `repo-invariants.mjs`'s walk additionally skips `dist`, `build`, `coverage` and `.next`, which the generator's does not. On this tree they enumerate the same 1,158 markdown files and the same 199 gen-carrying lines, and the difference is build output that carries no markdown. That is stated in the check's header rather than left for someone to discover on the day a build directory acquires a `.md`.

**AND IT IS THE SECOND WAVE RUNNING THAT `RI-31` IS RETURNED.** [ADR-281](ADR-281.md) section 6 returned it for a different reason. [ADR-277](ADR-277.md) section 8 already named this pattern, that the durable repair for a tooling defect keeps being deferred to a row nobody has written. **This entry is not deferring the repair to a later row; it is handing over a finished specification and one blocking question.** The distinction matters and this entry would rather be judged on it than hide it.

### 7. The check, as built

Specified before the fence question was answered so that landing it would be transcription rather than design, and transcribed unchanged afterwards. It sits in [`packages/tooling/checks/repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) beside `RI-12`, on `RI-13`'s stated file-boundary precedent: a property of a document's structure that needs no registry.

- **`id`**: `RI-31`.
- **`title`**: `No document repeats a whole line that carries a generated span`.
- **Scope**: every `.md` in the tree, matching `gates.mjs`'s `markdownFiles()` rather than `RI-12`'s `docs/`. Granted, section 6.
- **Masking**: fenced code blocks are masked with the generator's own expression, character for character. `gates.mjs`'s `spansIn` replaces each fenced block with a copy of itself whose every `<` has become a NUL, so a documented example of the form is not read as a span. Copy that expression rather than writing a second one. Measured to change nothing today, section 3.
- **Predicate**: trim each line; skip any line not containing `<!--gen:`; count identical survivors per file; **any count above one is a finding**. No length floor and no ceiling, each justified in section 4.
- **Finding text** names the file, every line number the duplicate sits on, the count, and the first 90 characters, so a reader can act without opening the file. It also names `RI-12` and says why that gate is silent, because the first question anyone asks of this finding is why nothing caught it.
- **SENTINEL ONE**: no `.md` file walked at all is an ERROR, not a pass.
- **SENTINEL TWO, and it is the one that matters**: **zero lines carrying a `<!--gen:` span anywhere is an ERROR.** This check is an absence check and its scope is defined by a comment syntax. If the span form is renamed, or the masking expression is edited into swallowing the whole document, the scope empties and the check reports PASS forever over a corpus it never read. That is [ADR-274](ADR-274.md)'s and [ADR-279](ADR-279.md)'s rule, and `RI-29`'s own sentinel at [`repo-invariants.mjs:7060`](../../packages/tooling/checks/repo-invariants.mjs) is the idiom to copy.
- **Nine cases in [`packages/tooling/test/repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts)**, and the four that matter most are the directions it must stay QUIET in. FIRES: a generated line doubled in one file, and a generated line doubled OUTSIDE `docs/`, which is the leg the row's fence got wrong. SILENT: one span name twice on a single line, one identical generated line in two different files, a doubled line carrying no span, and a doubled span inside a fenced block. THROWS: both sentinels. Plus the clean-tree `test.each` over `CHECKS`. The fixture gains two span-bearing documents in two directories, because sentinel two is an unconditional throw and because the cross-file pair is itself an assertion.
- **Register row**: written in the same commit as the check, with the title read out of the live `CHECKS` array rather than typed, per `RI-29`'s third leg. **`RI-29` enforced that here rather than being trusted to**: the check landed in the working tree first and the suite went red with *"ALLOCATION.md carries no register row for `RI-31`"*, naming the live title, before the row existed.

**WATCHED RED ON THE REAL TREE, IN BOTH HALVES OF ITS SCOPE.** One copy of an existing generated line seeded into `docs/STATE.md` and one into `packages/db/DELTA_MANIFEST.md`. `RI-31` reported both with their line numbers and exited 1; **`RI-12` reported PASS on the same tree**, which is the entire argument for a second check stated as a measurement rather than as a prediction. Both seeds removed, `sha256sum -c` OK on both files, `git status` clean. Both sentinels were watched throwing on scratch trees before the suite cases were written, and a fenced-only duplicate was watched producing no finding.

### 8. Reported rather than repaired

1. **`packages/db/DELTA_MANIFEST.md`'s two non-generated residue lines are left.** Its seven duplicated generated lines are repaired under the extended fence, but `:24` and `:27` carry no span, are welded to the paragraph above them, and each still says *"27 files"* beside a generated 69. They are residue of the same merges and are outside both `RI-31`'s subject and the words of the grant. Reported, not touched.
2. **`RI-12` IS SATURATED AT ITS OWN CEILING BY A LEGITIMATE LINE.** [`docs/reviews/2026-08-20-admin-parity-audit.md`](../reviews/2026-08-20-admin-parity-audit.md) heads eight tables with one identical 94-character row. A ninth parity table turns `RI-12` red on a document doing nothing wrong. **`RI-12` should not be lowered and this is not an argument to raise it**; it is a fact about the gate that nobody had measured, and the remedy when it fires will be to shorten the header rather than to move the number.
3. **Twenty-nine repeated long lines under `docs/` carry no generated span and are untouched.** They are out of this row's subject. Whether any of them is also merge splatter is a separate question this entry did not open, and the honest statement is that it looked at their counts and not at their content.
4. **`CI-06g` remains blind to this by construction and that is correct.** It rewrites every span it finds and compares each against its query, so N identical copies are N passes. It is the count gate and this is not a count defect. Nothing about `CI-06g` should change.

### 9. Where this entry is most likely wrong

**RULING 2 IS AN EMPIRICAL CLAIM ABOUT A CORPUS AND NOT A THEOREM.** "No line carrying a generated span is legitimately written twice" is true of 175 lines over 34 files today. A document could be written tomorrow that renders one query in two places in identically worded sentences, and the check would call it a defect. The cost of being wrong is one reworded sentence and the cost of the alternative is the check, so the trade is heavily one-sided, but the claim is a measurement rather than a proof and it is written down here as one.

**AND THE SECOND IS THAT STOPPING OVER THE FENCE MIGHT HAVE BEEN AN OVERREACTION.** A reader may hold that `docs/` was the right scope on `RI-12`'s precedent, that `DELTA_MANIFEST` was a different row's business, and that a session which stopped for one line cost the wave more than the hole would have. **The counter is what the stop bought**: the check now covers the file that held seven of the fifteen, and the alternative would have shipped a gate whose stated subject is the generator over half the generator's output. The judgement was not this session's to make either way, which is why it was reported rather than taken.

### 10. Approval

**What a founder read adds and this entry cannot.**

**SECTION 6 IS ANSWERED AND IS NO LONGER A QUESTION.** The fence was extended to the generator's scope and the check is minted. What is left for a founder there is whether the STOP was the right call as a matter of process, which section 9 states honestly in both directions. **Nothing in this entry needs a decision before it can be merged.**

**THE FIRST REMAINING JUDGEMENT IS WHETHER RULING 2 IS A RULE OR AN OBSERVATION.** This entry treats "a generated line is never written twice" as a property the corpus should keep true from here, not merely as a description of today. That is a small standing constraint on how future documents are worded, and it is the founder's to accept. If it is only an observation, the check wants a ceiling above one and section 4's second table prices each candidate.

**THE SECOND IS ITEM 2 OF SECTION 8.** `RI-12` is one legitimate table away from red. Leaving it is defensible and is what this entry does, on the row's instruction that `RI-12` stays. A founder may prefer that the review document's table headers be shortened below the floor now rather than on the day the gate fires.

- **Approval:** `___________________________` (UNSIGNED. **Merging is not signing.**)
