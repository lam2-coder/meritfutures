## ADR-449: the `schema.ts` header's census is ASSERTED rather than corrected a fourth time, the header edit is line-neutral because a non-neutral one is not satisfiable inside this fence, and the narrow generation comparison is KEPT because the general one provably cannot reach its table (2026-09-08, status: proposed)

**Row 449. Branch `claude/schema-header-asserted`, off `origin/claude/wave-444s` at `ea353e84`, session 644.**

**TWO OF THE THREE COMMISSIONED ITEMS ARE TAKEN AND THE THIRD IS REFUSED AT SOURCE.** [ADR-447](ADR-447.md) section 9 items 1 and 2 are discharged: the header's totality clause and its denominator are corrected, and the correction is defended by five cases that READ the header text and recompute every figure it states. [ADR-444](ADR-444.md) section 14 item 1 is discharged: the reciprocal back-pointer to limit 1 of `CatalogRow` is written at both sites it names. **`ADR-447` section 9 item 3, the deletion of the narrow generation comparison, is REFUSED**, because the general comparison iterates a population that provably excludes the narrow one's table, and the same file already contains the assertion that says so.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `449`, session **644**, branch `claude/schema-header-asserted`, off `ea353e84`.
- **Fence, BY FILE:** [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts), [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts), this file, the registries, this session's log and [`docs/STATE.md`](../STATE.md) for `gates.mjs generate` output only. **No migration, no `scope.ts`, no `scoped-db.ts`, no file under `packages/tooling/`, no workflow, no `apps/` file.**
- **Money:** **NON-MONEY DIFF IN A MONEY-PATH FILE.** Nothing here declares, reads or computes a cents value. **The refusal in section 7 is money-relevant and it is the reason that section is long**: the comparison this row declined to delete is the only mechanical check on a `GENERATED ALWAYS AS` MONEY column, and deleting it would have removed that check while reporting a fold.

---

### 1. The container and the base, measured rather than assumed

**THE CONTAINER ARRIVED WITH NO REPOSITORY AND NO `node_modules`.** `/home/user` was empty and `git status` reported *"not a git repository"*. The repository was attached and cloned in-session, `pnpm install --frozen-lockfile` was run, and every figure below was derived after that.

**THE BASE IS `origin/claude/wave-444s` AT `ea353e84`**, which is the head of open PR #736 and carries rows 441 to 447. It is not `main`. **The base was reproduced GREEN before the first edit: 344 test files, 8,605 passed, 11 skipped, 0 failed.**

**THE DISPATCH'S PREMISES WERE RE-DERIVED AND TWO OF THEM ARE WRONG ABOUT THIS BASE.** They are corrected in sections 6 and 7 rather than absorbed, on `ADR-446`'s precedent. `ADR-443` HAS landed and is on this base, as the dispatch says; that premise holds.

### 2. The two defects, at source

1. **[`schema.ts:6`](../../packages/db/src/schema.ts) read *"`SCOPE_RULES` is total over the keys of this file"*.** True of `TABLES`, and false of the DECLARATIONS in `schema.ts` from the moment `ADR-447` declared `live_account_state` there with no key.
2. **[`schema.ts:4`](../../packages/db/src/schema.ts) read *"ONE HUNDRED AND ELEVEN TABLES OF 115, PLUS ONE VIEW"*.** Both figures were stale, and the header's own paragraph says the denominator *"has been stale twice and is recomputed here rather than incremented"*. **It was stale a third time.**

**AND THE DEFECT IS WIDER THAN THE TWO ITEMS NAMED IT.** The header's own diagnosis is that `TABLE_KEYS.length` is asserted in the suite *"which is why the staleness could survive here and not there"*, and that the other figures *"are asserted nowhere, which is why they went stale in company."* **That sentence is a correct diagnosis and it named four figure families. All four were stale.** This row treats the family, not the two items.

### 3. THE CENSUS, DERIVED

Every figure below was computed in this session, twice: once by a throwaway probe under `vitest`, and once by the shipped cases, which is what a run repeats.

| Figure | Header read | DERIVED | How |
| --- | --- | --- | --- |
| Relations DECLARED in `schema.ts` | stated nowhere | **117** | `Object.values(schema).filter((v) => is(v, PgTable))` |
| `TABLE_KEYS.length` | 112 | **116** | `scope.ts` |
| Registered TABLES (non-view) | 111 | **115** | `DDL_NAMES.length` |
| Registered VIEWS | 1 | **1** | `VIEW_NAMES.length`, `economic_calendar_current` |
| `CREATE TABLE` in `packages/db/migrations` | 115 | **118** | comments stripped, unqualified names only |
| `firm` rules | 45 | **48** | `SCOPE_RULES` |
| `pair` rules | 3 | **3** | `SCOPE_RULES` |
| Served by `scopedDb` | 64 | **65** | `116 - 48 - 3`, and `1 + 41 + 22 + 1` the other way |
| Classes in `SCOPE_RULES` | 6 | **6** | `root` 1, `owned` 41, `derived` 22, `firm` 48, `pair` 3, `either` 1 |

**THE THREE TABLES NO ACCESSOR REACHES ARE NAMED RATHER THAN COUNTED**, because a count is what went stale: `identity_merges` and `identity_signal_weights` are created by the migrations and transcribed in no TypeScript file at all, and `live_account_state` is transcribed and deliberately unregistered ([ADR-447](ADR-447.md) section 7).

**THE `pgboss` TABLES ARE OUTSIDE THE DENOMINATOR AND THAT IS ASSERTED RATHER THAN LEFT TO A REGEX.** The migration set carries 12 `CREATE TABLE pgboss.<name>` statements in the vendor's own schema. The reader requires the open paren directly after an UNQUALIFIED name, so they do not match, and 118 is the public-schema figure the header has always meant.

### 4. THE REPAIR IS AN ASSERTION, AND THE HEADER TEXT IS CORRECTED BECAUSE THE ASSERTION FORCES IT

**A `<!--gen:-->` SPAN CANNOT LIVE IN A `.ts` FILE**, which is `ADR-447` section 9 item 2's diagnosis and is correct. So the durable form is a case that reads the header as TEXT and recomputes what it states.

**THAT SETTLES THE QUESTION THE DISPATCH LEFT OPEN.** The dispatch asked this row to decide whether to correct the header text as well as assert it. **It is not a free choice: an assertion that recomputes the figures fails on a stale header the moment it is written.** Asserting the figures and leaving them wrong would mean writing a case that asserts the wrong answer, which is the shape of weakening a gate to pass it. **The header is corrected, and the correction is what the new cases defend.**

**FIVE CASES, IN ONE NAMED BLOCK AT THE FOOT OF [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts), starting at [`scoped-db.test.ts:4662`](../../packages/db/test/scoped-db.test.ts).**

| # | Case | Line | Binds |
| --- | --- | --- | --- |
| 1 | `the header is the text above the first import, and it is not empty` | [`:4776`](../../packages/db/test/scoped-db.test.ts) | the slice is real before any figure is read out of it |
| 2 | `the registered-table count, the CREATE TABLE denominator and the view are the derived ones` | [`:4792`](../../packages/db/test/scoped-db.test.ts) | 115, 118, 1, and the 3 unreached |
| 3 | `the declaration count and TABLE_KEYS.length are the derived ones, and they differ` | [`:4832`](../../packages/db/test/scoped-db.test.ts) | 117, 116, and that the gap is exactly 1 |
| 4 | `the class tallies in the header are the ones SCOPE_RULES holds` | [`:4862`](../../packages/db/test/scoped-db.test.ts) | 48, 3, 65, 116, six classes, `either` of one |
| 5 | `the figure the header spells out is the same recomputed count` | [`:4907`](../../packages/db/test/scoped-db.test.ts) | `ONE HUNDRED AND SIXTEEN`, rendered rather than typed |

**CASE 5 EXISTS BECAUSE THE SPELLED FORM WENT STALE TOO.** The header read *"THE ONE HUNDRED AND TWELVE ARE NOT ONE PHASE'S SET"* while `TABLE_KEYS` held 116. A numeral spelled in words is invisible to a reader grepping for digits, so the block carries a renderer and the renderer is itself asserted non-degenerate on three known values before it is used.

**AND CASE 3 IS THE ONE THAT CARRIES ITEM 1'S CONTENT AS A NUMBER.** *"`SCOPE_RULES` is total over the DECLARATIONS in this file"* is false exactly when the declaration count exceeds `TABLE_KEYS.length`. The case asserts that difference is **1**. The day it is 0 the clause is over-cautious rather than wrong, and the day it is 2 a second relation has been declared unregistered without anyone deciding to.

### 5. THE HEADER EDIT IS LINE-NEUTRAL, AND THAT IS FORCED RATHER THAN CHOSEN

**THE CONSTRAINT IS THAT NO CITED LINE MOVES**, which [ADR-386](ADR-386.md) at [`repo-invariants.mjs:3008`](../../packages/tooling/checks/repo-invariants.mjs) rules is neither implied by nor implies line-count neutrality: *"where one must, every pointer in a live file is REPAIRED and every pointer in a DATED RECORD is NAMED."*

**THE POPULATIONS WERE DERIVED HERE AND NOT INHERITED.** `grep -rhoP 'schema\.ts[#:]L?\K[0-9]+'` over `.md`, `.ts`, `.mjs` and `.js` returns **70 distinct lines over 64 files**. `ADR-447` section 9 measured **62 and 6-in-header** on its own base; **the figures on this base are 70 and 8-in-header**, and the two extra header pointers are `ADR-447`'s own, to `:6` and `:15`. The same grep for `scoped-db.test.ts` returns **44 distinct lines**, one of which is `99999`, [ADR-440](ADR-440.md)'s record of its own seeded red rather than a live pointer. `ADR-447` measured 20 there.

**THE HEADER IS LINES 1 TO 114 AND THE FIRST `import` IS LINE 115**, so 8 cited lines sit inside it (`4`, `6`, `15`, `20`, `23`, `49`, `109`, `112`) and **62 sit below it**.

**THE DECIDING FACT IS THAT A NON-NEUTRAL EDIT IS NOT SATISFIABLE INSIDE THIS FENCE, AND IT IS DERIVED RATHER THAN PREFERRED.** Of the 64 files citing `schema.ts` by line, **8 are live source and 56 are dated records.** ADR-386's rule REPAIRS a pointer in a live file. Three of the live citers are [`apps/api/src/routes/accounts.ts`](../../apps/api/src/routes/accounts.ts), [`apps/worker/src/detectors/fills.ts`](../../apps/worker/src/detectors/fills.ts) and [`apps/worker/src/provisioning/vocabulary.ts`](../../apps/worker/src/provisioning/vocabulary.ts), **and this fence holds no `apps/` file.** So an edit that moved the 62 would owe repairs in files this row may not touch. **Line-neutrality is not the aesthetic option here; it is the only one the fence permits**, and that is a stronger reason than [ADR-440](ADR-440.md)'s precedent the dispatch offered.

**WHAT WAS ACTUALLY DONE, AND IT IS CHECKED RATHER THAN CLAIMED.**

| Fact | `schema.ts` | `scoped-db.test.ts` |
| --- | --- | --- |
| `wc -l` base / branch | 5427 / 5458 | 4638 / 4918 |
| `git diff --numstat` | `52 21` | `282 2` |
| Header lines 4 to 23 | **20 replaced by 20** | n/a |
| Prefix vs base | **lines 115 to 5358 byte-identical** | **lines 1 to 4638 differ at exactly 2 lines, both formerly blank** |
| Distinct cited lines into the file | 70, highest **5351** | 44, highest real **4166** |
| **Cited lines moved** | **0** | **0** |

The two lines in `scoped-db.test.ts` are `2725` and `3214`. **Both were blank lines**, and putting the back-pointer on each is what keeps the `describe` at `2726` and at `3215` where [ADR-444](ADR-444.md) section 14 item 1 names them. `git diff --numstat` reads `282 2` and not `280 0` for that reason: two deletions and two insertions of the same lines, at the same line numbers.

**THE `schema.ts` APPEND SITS BELOW LINE 5351, WHICH IS THE HIGHEST LINE ANYTHING CITES.** It begins at [`schema.ts:5360`](../../packages/db/src/schema.ts).

### 6. WHAT THE HEADER EDIT COST, STATED RATHER THAN ABSORBED

**`RI-14` GOVERNS AND THE STRUCK SENTENCES ARE KEPT.** They are kept at [`schema.ts:5360`](../../packages/db/src/schema.ts), beside the declaration that moved them, which is exactly where `ADR-447` put the first half of this record and cited as [`schema.ts:5351`](../../packages/db/src/schema.ts). **This file's own precedent, one row old, reads `RI-14`'s "beside" as beside the DECLARATION rather than beside the sentence**, and the header has no room for four struck figures inside 20 lines.

**ONE PARAGRAPH OF THE ORIGINAL HEADER IS COMPRESSED AND THAT IS NAMED.** Lines 15 to 23 carried the narrative of the two earlier staleness episodes, naming `0049`, `0050`, `0051`, `tradingCalendar` and `0065`. The replacement states that the denominator has been stale three times and where the record now lives. **Content moved; it was not dropped.**

**AND ONE FIGURE IN THAT HEADER IS DERIVED STALE AND DELIBERATELY NOT REPAIRED.** [`schema.ts:81`](../../packages/db/src/schema.ts) reads *"ELEVEN of the 109 below carry later columns"* and names eleven tables. Derived over the migration set by splitting on `;` and matching `ALTER TABLE <t> ... ADD COLUMN`: **FIFTEEN tables carry one**, and the four the sentence does not name are `affiliate_commissions`, `firm_parameters`, `liability_snapshots` and `wallet_withdrawals`. The denominator is **116** declared tables, not 109. **It is NAMED and not repaired, because it is a claim about the FOLD's necessity and not a census entry**, and repairing it means re-deriving whether the `ADR-094` argument the sentence makes holds for each of the four. **This row binds the census and says plainly that it did not bind this.** Section 9 item 1.

### 7. THE DELETION IS REFUSED, AND THE FILE ALREADY CONTAINED THE PROOF

**THE DISPATCH IS WRONG ABOUT THIS BASE IN TWO PLACES AND THE SECOND IS DECISIVE.**

**FIRST, THE LINE NUMBERS.** The dispatch places the narrow comparison at `scoped-db.test.ts:4103` and the general one at `:3217`. On this base **`:4103` is inside an accessor-refusal case about `ledgerHalts`**, and **`:3217` is the DEFAULT loop**, not a generation comparison. The narrow comparison is at [`scoped-db.test.ts:4563`](../../packages/db/test/scoped-db.test.ts) and the general generation block, which `ADR-443` built, opens at [`scoped-db.test.ts:3601`](../../packages/db/test/scoped-db.test.ts). `ADR-447` section 9 item 3's own words are *"belongs beside the type, nullability and DEFAULT loops at `:3217`"*, which is a statement about where a general one BELONGS and was read here as a statement about where one IS.

**SECOND, AND THIS IS THE REFUSAL.** The general block iterates `DDL_NAMES`, defined at [`scoped-db.test.ts:247`](../../packages/db/test/scoped-db.test.ts) as `ALL_REGISTERED` minus the views, and `ALL_REGISTERED` at [`:204`](../../packages/db/test/scoped-db.test.ts) is built from `TABLE_KEYS`. **`live_account_state` is not in `TABLE_KEYS`, which is the whole of `ADR-447` section 7.** So the general comparison cannot reach the narrow one's table, by construction and not by accident.

**THE FILE SAYS SO ITSELF, IN THE GENERAL BLOCK, IN A CASE `ADR-443` WROTE.** `the migration set declares SIX generated columns and the sixth is a MONEY column nothing transcribes` at [`scoped-db.test.ts:3825`](../../packages/db/test/scoped-db.test.ts) asserts `ALL_REGISTERED` does NOT contain `live_account_state`, *"so the sixth generated column is reachable and belongs in `GENERATED_ALWAYS_AS`"* the day it is. **The general comparison's own definition of its blind spot is the narrow comparison's subject.** [`schema.ts:5360`](../../packages/db/src/schema.ts)'s neighbouring paragraph, written by `ADR-447`, states the same thing from the declaration's side.

**SO `ADR-421` SECTION 9's ADMISSION RULE REFUSES THE DELETION RATHER THAN COMMANDING IT.** That rule admits a change where the thing it replaces can be shown **DELETED in the same commit** because something genuinely covers it: *"a row that ... can therefore show a hand-written mapping DELETED in the same commit."* **Here nothing covers it.** Deleting the narrow block would remove the only comparison between `schema.ts`'s transcription of `intraday_movement_cents` and `0050`'s `GENERATED ALWAYS AS (equity_cents - opening_equity_cents) STORED`, on a MONEY column, and would report a fold. **The dispatch's instruction to verify at source before deleting anything is the instruction that caught this.**

**WHAT WOULD MAKE IT TAKEABLE, SO THE NEXT ROW DOES NOT RE-DERIVE IT.** Either `live_account_state` is registered in `scope.ts`, which `ADR-447` section 7 argues against on a grant, or the general loops iterate a population wider than `TABLE_KEYS`. **Both are diffs on `scope.ts` or on the loop headers and both are arguments, not folds.** Section 9 item 2.

### 8. THE RECIPROCAL BACK-POINTER, AND WHY IT IS NOT THE CITING FORM

[ADR-444](ADR-444.md) section 14 item 1: a row editing [`scoped-db.test.ts:2728`](../../packages/db/test/scoped-db.test.ts) *"is not told that a sentence in another file describes what it is about to change"*. **One line at [`:2725`](../../packages/db/test/scoped-db.test.ts) and one at [`:3214`](../../packages/db/test/scoped-db.test.ts) close it**, each reading:

`// DESCRIBED ONCE at limit 1 of `CatalogRow` (`packages/db/src/scoped-db.ts:3466`).`

**IT DELIBERATELY DOES NOT USE `ADR-444` SECTION 5.3's CITING FORM.** That form, *"stated ONCE at limit 1"*, exists so `grep -rn "stated ONCE at limit 1"` enumerates the sites that formerly RESTATED the claim, which is a population of **10 on this base**. **A back-pointer is the opposite direction and putting it in the same grep would corrupt what that grep counts.** `DESCRIBED ONCE` is greppable on its own and returns exactly the two reciprocal sites.

**AND IT RESTATES NO COMPARISON LEG**, which is `ADR-444` section 5.2's clause with teeth. It names the canonical statement and stops. `RI-15` binds it: `CatalogRow` is declared at [`scoped-db.ts:3466`](../../packages/db/src/scoped-db.ts) and the invariant passes.

### 9. What is owed, stated precisely enough to be picked up

1. **THE FOLD PARAGRAPH'S FIGURES ARE STALE AND THIS ROW BOUND THE CENSUS ONLY.** [`schema.ts:81`](../../packages/db/src/schema.ts) reads *"ELEVEN of the 109 below carry later columns"*. Derived here: **FIFTEEN and 116**, the four unnamed being `affiliate_commissions`, `firm_parameters`, `liability_snapshots` and `wallet_withdrawals`. **A row that takes it owes more than the numerals**: the sentence argues that none of the eleven *"could be registered at all before ADR-094"*, and that argument has to be checked against the four before they join the list. The binding leg belongs in the block at [`scoped-db.test.ts:4662`](../../packages/db/test/scoped-db.test.ts) beside the other four families.
2. **THE GENERATION COMPARISON IS STILL TWO COMPARISONS AND SECTION 7 IS WHY.** The narrow one at [`scoped-db.test.ts:4563`](../../packages/db/test/scoped-db.test.ts) survives because the general one at [`scoped-db.test.ts:3601`](../../packages/db/test/scoped-db.test.ts) iterates `DDL_NAMES`. **The fold becomes takeable the day `live_account_state` is registered or the day those loops take a wider population**, and neither is a fold: both are diffs on [`scope.ts`](../../packages/db/src/scope.ts) or on the loop headers with an argument attached.
3. **`ADR-447`'s EIGHT POINTERS INTO `scoped-db.test.ts` ARE ALL OFF BY EXACTLY 460 ON THIS BASE, AND NO ROW'S EDIT DID IT.** `:4025`, `:4038`, `:4040`, `:4088`, `:4103`, `:4136`, `:4146` and `:4166` were derived where its block sat at 3985; on this base the block opens at [`scoped-db.test.ts:4480`](../../packages/db/test/scoped-db.test.ts) and every one of the eight lands on an unrelated REAL line. **This is the failure `ADR-444`'s worked warning names**, reproduced by wave integration rather than by a row: `RI-15` sees pointers landing on blank or brace lines and one of these eight lands on `});` while seven land on live code. **`ADR-447` is a DATED RECORD, so [ADR-386:169](ADR-386.md) NAMES these rather than repairing them**, and they are named here. Its ninth pointer, [`scoped-db.test.ts:466`](../../packages/db/test/scoped-db.test.ts), still lands on its subject because it is above the insertions.
4. **THE OTHER FIGURES IN THE `either` PARAGRAPH ARE UNBOUND.** [`schema.ts:41`](../../packages/db/src/schema.ts) claims *"seven others carry both an identities edge and an accounts edge and every one of the seven declares its identity column NOT NULL"*. This row corrected that sentence's population from 115 to 116 because that is the population case 4 binds, **and it did not derive the seven**. A row that binds it also settles whether the sentence is about registered relations or about the DDL, which this row narrowed without evidence either way.
5. **`ADR-447` SECTION 9 ITEMS 4, 5 AND 6 ARE UNTOUCHED BY THIS ROW.** The live read path is still not a process, the non-registration assertion is still not independently watched red, and the `<!--gen:-->`-inside-inline-code question at [`gates.mjs:1275`](../../scripts/corpus/gates.mjs) is still open. **None of the three is in this fence.**
6. **`ADR-444` SECTION 14 ITEMS 2 AND 4 ARE UNTOUCHED.** The census cannot see `nowhere`, and nothing polices the decision to restate. Item 4 is grammar-keyed and inherits `ADR-444` section 6's objections; a row that wants it argues past them.
7. **`CI-06f` IS RED AT `448` AND IT WENT RED BECAUSE OF THIS ROW.** Adding `449` to [ALLOCATION](ALLOCATION.md) opens exactly one hole, and the finding is *"ADR-448 is neither present nor reserved (a hole)"*. Row `448` is a sibling on this wave holding [`scoped-db.ts`](../../packages/db/src/scoped-db.ts). **It is REPORTED rather than repaired and NO reservation was written for a row this session does not own**, on the precedent rows `434`, `437`, `440` and `444` set and on row `423`'s ruling that a reservation's subject is set at dispatch. **It closes the moment `448` lands**, and no gate was weakened to hide it.
8. **`docs/STATE.md` MOVED BY ONE GENERATED SPAN AND BY NOTHING ELSE.** `gates.mjs generate` incremented the `adr_count` span at [`STATE.md:32`](../STATE.md) because this row adds an ADR. **No prose in that file was touched.** The fence granted that file for `generate` output only, which is `ADR-444` section 14 item 9's correction applied as written rather than re-argued.

### 10. Every leg, re-run after the last edit

- **`node scripts/corpus/gates.mjs generate`: 3 spans rewritten.** In [`STATE.md`](../STATE.md) exactly one moved, `adr_count` from **430** to **431** at [`STATE.md:32`](../STATE.md), and **no prose in that file was touched**; the other two are the generated ADR and session registers. **`check`: 32 of 33 gates pass, and the one failure is `CI-06f` with a single finding, _"ADR-448 is neither present nor reserved (a hole)"_**, which is section 9 item 7 and is reported rather than repaired.
- **`node packages/tooling/checks/repo-invariants.mjs`: 35 of 35 invariants hold**, `RI-14`, `RI-15` and `RI-16` among them.
- **`pnpm vitest run`:** base **344 files / 8,605 passed / 11 skipped / 0 failed**; branch **344 / 8,610 / 11 / 0**. **Delta: +5 passed, +0 files, +0 skipped, +0 failed.** Both measured in this session against `ea353e84`.
- **THE DELETED NARROW COMPARISON IS ACCOUNTED FOR SEPARATELY AND ITS CONTRIBUTION IS ZERO, BECAUSE THE DELETION WAS REFUSED.** Section 7. The +5 is five added cases and no removed one; the narrow block still contributes its **7** cases, unchanged.
- **`pnpm run typecheck && pnpm run lint && pnpm run format:check`: all clean.** `typecheck` was RED once, at the numeral renderer, on `noUncheckedIndexedAccess`; the fix makes the renderer THROW on a missing word rather than casting, so a degraded renderer is loud rather than comparing the header against a shorter string.

### 11. The controls added, and the RED watched under each

**TEN SEEDS, EVERY ONE OF THE FIVE CASES WATCHED RED, AND EVERY SEED REVERTED AND VERIFIED BYTE-IDENTICAL** by `git hash-object` against the value taken before the seed.

| # | Seed | Case reddened |
| --- | --- | --- |
| 1 | numerator `115` to `111` at `schema.ts:4` | 2 |
| 2 | denominator `118` to `115` at `schema.ts:4` | 2 |
| 3 | `The other 3 tables` to `4` | 2 |
| 4 | declaration count `117` to `116` at `schema.ts:8` | 3 |
| 5 | restated keys `116` to `112` at `schema.ts:13` | 3 |
| 6 | `firm` tally `48` to `45` at `schema.ts:26` | 4 |
| 7 | served remainder `65` to `64` at `schema.ts:33` | 4 |
| 8 | spelled count `SIXTEEN` to `TWELVE` at `schema.ts:46` | 5 |
| 9 | the census sentence REWORDED AWAY entirely | **1 and 2** |
| 10 | an extra relation DECLARED in `schema.ts` and registered nowhere | 3 |

**SEED 9 IS THE ONE THAT ANSWERS `ADR-442` SECTION 9's OBJECTION TO GRAMMAR-KEYED GATES**, which is that such a gate is satisfiable by REWORDING. Deleting the sentence rather than changing its figures reddens two cases, not zero.

**SEED 10 IS THE ONE THAT SAYS THE DERIVED SIDE IS LIVE.** Seeds 1 to 9 all move the HEADER. A block that compared the header with itself would pass every one of them and still be worthless. Seed 10 moves the TREE and leaves the header alone: an eleventh relation declared in `schema.ts` with no key reddens case 3 while every figure in the header is untouched.

**ONE SEED WAS REFUSED AND IT IS NAMED.** The `CREATE TABLE` denominator would be seeded by editing a migration, and **migrations are sacred** (constitution E2). `ADR-447` refused the same seed for the same reason. What stands in its place is case 2's own leg asserting that every registered table appears in the derived set, so a reader that stopped matching is red rather than agreeing with a header nobody moved.

### 12. What this row did NOT do

**NO MIGRATION TOUCHED. NO `scope.ts`. NO [`scoped-db.ts`](../../packages/db/src/scoped-db.ts)**, which row `448` holds this wave. **NO FILE UNDER `packages/tooling/`. NO `apps/` FILE. NO WORKFLOW. NO `RI-nn` MINTED. NO `CI-06` LETTER SPENT.** **NO GATE WEAKENED, NO TEST SKIPPED, DISABLED OR QUARANTINED, NO ASSERTION INVERTED, NO TEST DELETED.** **NO LANDED ADR EDITED**, and [ADR-432](ADR-432.md) section 10 item 1's off-by-one citation stays exactly as it is.

**AND NO RESERVATION WAS WRITTEN FOR A ROW THIS SESSION DOES NOT OWN.** Row `449` is claimed and no other cell.

### 13. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** **Merging is not signing.** Inherited as well as owed: [ADR-303](ADR-303.md), [ADR-416](ADR-416.md), [ADR-421](ADR-421.md), [ADR-426](ADR-426.md), [ADR-430](ADR-430.md), [ADR-432](ADR-432.md), [ADR-437](ADR-437.md), [ADR-438](ADR-438.md), [ADR-439](ADR-439.md), [ADR-440](ADR-440.md), [ADR-443](ADR-443.md), [ADR-444](ADR-444.md), [ADR-445](ADR-445.md), [ADR-446](ADR-446.md) and [ADR-447](ADR-447.md) are `proposed` and unsigned, and this row settles none of them.

**What a founder read adds on the `E2` read, and this entry cannot.**

**THE FIRST IS SECTION 7, AND IT IS THE ONLY REFUSAL HERE.** This row was dispatched to delete a comparison and did not. **The derivation is mechanical and is stated so it can be checked in one grep**, but the JUDGEMENT that a dispatch's instruction yields to a fact measured at source is not something a row can settle for itself. **If the founder reads it the other way, the cost of the refusal is one duplicated-looking block and nothing else**, and the cost of having taken it would have been a MONEY column's generation expression compared by nothing.

**THE SECOND IS THAT THE HEADER'S PROSE IS NOW PARTLY MACHINE-BOUND AND THAT IS A NEW KIND OF SENTENCE IN THIS TREE.** Five cases now fail if anyone rewords nine specific sentences in a comment block. **That is deliberate and it is also a tax on whoever next edits that header.** `ADR-442` section 9 rules that a gate over a GRAMMAR is satisfiable by rewording and therefore a liability; this block is keyed to grammar and answers that objection with seed 9 rather than escaping it. **A founder may hold that a comment should never be load-bearing**, in which case the right shape is to move the census out of prose into an exported constant and let the header cite it. That is a bigger diff than this fence allows and it is a real alternative.

**THE THIRD IS THAT THIS ROW NARROWED A CLAIM IT DID NOT VERIFY**, section 9 item 4. [`schema.ts:41`](../../packages/db/src/schema.ts) read *"the only table of that shape in the 115"* and now reads *"in the 116"*, which changes the sentence's population from the DDL set to the registered set. **It was done because 116 is the figure case 4 binds and 118 is not.** It is the smallest unforced judgement in this diff and it is the one most likely to be wrong.

**THE FOURTH IS SECTION 9 ITEM 3, AND IT IS A PROCESS FINDING.** Eight pointers in an ADR that landed yesterday are off by a constant 460 because of a merge, not an edit, and seven of the eight are invisible to `RI-15`. **`ADR-444`'s warning said this would happen and it has, in the next row's entry.** A founder may want to rule on whether a row appending to a hot file should cite line numbers at all, or should cite the case titles that the merge cannot move.

- **Approval:** `___________________________` (UNSIGNED. **E2 READ OWED**, on the files named in section 5. **Merging is not signing.**)

|                 |     |
| --------------- | --- |
| **Approved by** |     |
| **Date**        |     |
