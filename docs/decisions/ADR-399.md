## ADR-399: the eight are CLASS ONE and every one of them is repaired, `G2` holds on all eight at the commit that wrote them, four waves recorded them as blocked on a founder question that was never in the way, and ADR-392's one judgment call dissolves at the right tree (2026-09-07, status: proposed)

**[ADR-392](ADR-392.md) SECTION 11 ITEM 4 NAMED EIGHT POINTERS INTO [`CRON_INVENTORY.md`](../ops/runbooks/CRON_INVENTORY.md) AS *"ALREADY WRONG"*, *"NOT REPAIRABLE UNDER THE RULE"* AND A FOUNDER QUESTION. [ADR-395](ADR-395.md) SECTION 8 ITEM 3 RECORDED THE SAME THING AND COUNTED THE WAVES: FOUR.** This row was given the fence. **The eight are CLASS ONE under [ADR-388](ADR-388.md), all eight are repaired here, and the question four waves referred to the founder did not need answering.**

**THE REASON THE CORPUS GOT THIS WRONG IS ONE SENTENCE LONG AND [ADR-392](ADR-392.md) WROTE IT DOWN ITSELF.** That entry states, in its own section 6: *"The test applied is not the class test... This row asks a question that needs none: was the pointer exact at `c5648f00`, the base this diff edits?"* **It measured drift against its own base and never derived `G2` at all.** `G2` asks whether the cited line held the subject at the commit that WROTE the citing line. Derived that way, on all eight, **the answer is YES, every time, byte for byte.** They are not born wrong. They are the cleanest drift this corpus has produced.

**WHAT LEFT THEM UNREPAIRED WAS NEVER [ADR-388](ADR-388.md)'s RULE. IT WAS [ADR-390](ADR-390.md) SECTION 9's**, which says *"a pointer that was already wrong before this diff did not go stale because of this diff, so it is not this diff's to repair"*. **That is a rule about WHOSE DIFF OWES A REPAIR, not about whether a repair is permitted.** Every wave that reached these eight was a wave doing something else, so every wave correctly declined, and the decline was recorded four times in language that reads as a prohibition. **This is [ADR-381](ADR-381.md)'s mechanism exactly: a finding nobody's fence covers is a finding that does not get fixed, and the record of not fixing it hardens into a reason.**

- **Origin:** [ALLOCATION](ALLOCATION.md) row `399`, branch `claude/the-eight-runbook-pointers`, session **594**, off the wave integration branch `claude/wave-395s` at `c219c0d1` rather than off `main`.
- **NON-MONEY. DATED RECORDS AND ONE OPERATIONS REVIEW. Ships `proposed` with an UNSIGNED approval line. Merging is not signing. `E2` read owed.**
- **NO GATE WEAKENED, NO TEST SKIPPED, DISABLED OR QUARANTINED, NO INVARIANT MINTED, NO `RI-` NUMBER RESERVED, NO `CI-06` LETTER SPENT, NO MIGRATION TAKEN OR RESERVED, NO REGISTER GROWN, NO PORT WIRED, NO JOB SCHEDULED.**
- **NOT ONE EXECUTABLE LINE CHANGED AND NOTHING OUTSIDE `docs/` WAS WRITTEN.** [`scripts/corpus/**`](../../scripts/corpus) and [`packages/tooling/**`](../../packages/tooling) were READ and never opened for writing. **`pnpm run verify` WAS NOT RUN AND [`falsify.mjs`](../../scripts/corpus/falsify.mjs) WAS NOT RUN.**
- **[`CRON_INVENTORY.md`](../ops/runbooks/CRON_INVENTORY.md) IS NOT EDITED BY THIS ROW AND SECTION 8 IS WHY.** Every repair here is in a CITING document, which is what [ADR-395](ADR-395.md) section 8 item 2 predicted and this row confirms by deriving the same predicate independently at **21 against 21**.
- **A GREEN RUN IS NOT EVIDENCE HERE AND THAT IS MEASURED RATHER THAN RECITED.** Section 7 breaks a repaired pointer by 9,803 lines in all three document classes and **nothing goes red**. Gates **33 of 33** and invariants **35 of 35** say nothing whatever about these eight. **Only reading says anything, so every repair was read.**

---

### 1. The container, measured before anything else, because the dispatch was wrong about it twice

**THE DISPATCH SAID THE CONTAINER ALMOST CERTAINLY ARRIVES WITH NO REPOSITORY, AND THAT HALF IS RIGHT.** The stated working directory `/home/user` was **empty**, `git rev-parse` returned *fatal: not a git repository*, and a search to depth 6 from `/` found `.git` directories belonging to `rbenv`, `ruby-build` and `nvm` and **no checkout of this repository anywhere on the container**.

**THE REPOSITORY WAS NOT NAMED IN THE DISPATCH EITHER**, and no source was attached to this session. It was recovered from the account's repository list as `lam2-coder/meritfutures` and cross-checked against the parent session's own `sources` record before a byte was cloned.

| Measured, at the moment of reporting | |
|---|---|
| `git rev-parse --is-shallow-repository` | **`false`** |
| `git rev-list --count c219c0d1` | **4,319** |
| `git rev-list --count main` | 4,310 |

**SO THE CLONE IS FULL AND THE INSTRUCTION TO DEEPEN IT HAD NOTHING TO RUN AGAINST, WHICH IS [ADR-396](ADR-396.md) SECTION 1 REPRODUCING ON A THIRD CONTAINER.** It is stated because [ADR-389](ADR-389.md) section 12 item 6 asked every row deriving a history fact to state it, and **this entry's whole argument is a history fact**: `G2` is decided by reading eight blobs at five commits between 2026-08-22 and 2026-08-29. **A shallow clone would have answered that wrongly rather than refusing**, and [ADR-395](ADR-395.md) section 9 reports `true` on its own container, so the property is per container and not per repository. **Nobody may inherit it in either direction.**

---

### 2. The population, re-derived, and the 21 does not reproduce

A raw grep for `CRON_INVENTORY(\.md)?:NN` over the whole tree finds **20 tokens**, not [ADR-392](ADR-392.md)'s 21.

**THE MISSING ONE IS NOT A MISCOUNT, IT IS A REPAIR THAT LANDED BETWEEN THE TWO BASES.** [ADR-394](ADR-394.md) repaired [P7](../plans/P7-risk-and-abuse.md) line 407, which [ADR-392](ADR-392.md) section 11 item 2 and [ADR-389](ADR-389.md) both left owed. It is now at [P7](../plans/P7-risk-and-abuse.md) line 160 and **it carries no coordinate at all**: that row repaired to the ROW NAME rather than to a number, on the ground that the runbook was being restructured in the same wave. **So the population shrank by one because a pointer stopped being a pointer**, which no count of tokens can see and which is the correct outcome.

| The 20, classified | Tokens | Disposition here |
|---|---|---|
| **The eight this row owns** | **8**, at 7 sites in 5 documents | **ALL EIGHT REPAIRED.** Sections 4 and 5 |
| `:27`, exact today | 4 | **Nothing owed.** Section 6 |
| `:35`, class two | 4 | **LEFT, and re-derived rather than inherited.** Section 6 |
| `:23-40`, transcribed seed output | 4 | **Not pointers.** Editing them would falsify a record of what a checker printed |

**THE "EIGHT" IS A COUNT OF COORDINATES AND NOT OF SITES, AND [ADR-392](ADR-392.md) NEVER SAYS SO.** Seven sites carry them, because [session 166](../sessions/2026-08-24-session-166.md) line 52 writes `:67-68`, which is two coordinates aimed at two adjacent rows with two different subjects. **Read as sites the set is seven and the arithmetic of four waves stops working**; read as coordinates it is eight and [ADR-392](ADR-392.md)'s own *"between four and six lines short"* covers all of them. **That sentence is what fixes the grain**, and it is also what excludes the four `:35` tokens, which were two short.

---

### 3. `G2` derived at the commit that wrote each citing line, never at `git blame -w` and never at this tree

**[ADR-396](ADR-396.md) SECTION 7 RULED THAT `git blame -w` IS NOT SUFFICIENT TO DECIDE `G2`**, because a cosmetic later rewrite blames a line to a commit that did not write it, and it had an instance where the two instruments differed by 304 lines and by a verdict. **Its remedy is `git log -S` over the coordinate token, oldest result**, and that is the instrument used here.

**RUN AGAINST ALL FIVE CITING FILES, THE TWO INSTRUMENTS AGREE ON EVERY ONE.** That is reported as a result and not as a reassurance: [ADR-396](ADR-396.md) measured disagreement on 5 of 46 and a flipped verdict on 1 of 46, so agreement on 5 of 5 is what a small sample of unreflowed lines looks like. **It does not weaken that finding and it does not confirm it.**

| Citing site | Coordinate | Write commit, `git log -S` oldest | The cited line AT that commit |
|---|---|---|---|
| [session 159](../sessions/2026-08-24-session-159.md):120 | `:46` | `a82632f5`, 2026-08-24 | the `ADR-040` clock sentence, **the one the record quotes** |
| [session 166](../sessions/2026-08-24-session-166.md):52 | `:67` and `:68` | `4d57f9f9`, 2026-08-24 | the `offers.expires_at` and `promotional_credit_grants.expires_at` exemption rows, **in that order**. One site, two coordinates, two subjects |
| [session 165](../sessions/2026-08-24-session-165.md):69 | `:74` | `b681a698`, 2026-08-24 | the `otp_send_budget.override_expires_at` exemption row |
| [ADR-080](ADR-080.md):110 | `:89` | `3391f393`, 2026-08-22 | the **Restore drill** (`VG-9`) row |
| [ADR-080](ADR-080.md):132 | `:89` | `3391f393`, 2026-08-22 | the same row |
| [ADR-080](ADR-080.md):211 | `:89` | `3391f393`, 2026-08-22 | the same row |
| [d2 controls audit](../reviews/2026-08-29-d2-controls-audit.md):85 | `:94` | `fc0db621`, 2026-08-29 | the **Secret rotation** row, **the one the record quotes** |

**`G2` HOLDS ON ALL EIGHT. NOT ONE OF THEM WAS EVER WRONG.** Two of the eight quote their subject verbatim, so for those the gate is decided by string equality rather than by reading.

---

### 4. ADR-392's one recorded judgment call dissolves, and it dissolves at the tree rather than by argument

**[ADR-392](ADR-392.md) SECTION 6 CALLED [ADR-080](ADR-080.md):110 *"THE ONE JUDGMENT CALL"* AND RECORDED IT RATHER THAN BURYING IT**, which is why this row could check it. Its reasoning: at `c5648f00`, line 89 of the runbook is the heading `## Calendar work, not cron`, *"which a generous reading could call the subject"*, and under that reading a repair to `:179` is owed. It settled the ambiguity by reading the citing sentence, and it flagged in its own section 12 that a second reader could reasonably disagree.

**THERE IS NO AMBIGUITY. IT IS AN ARTEFACT OF READING THE POINTER AT THE WRONG TREE.**

| At `3391f393`, the commit that wrote [ADR-080](ADR-080.md):110 | |
|---|---|
| Runbook line **89** | the **Restore drill** (`VG-9`) row, **exactly as the sentence names** |
| `## Calendar work, not cron` | line **83**, six lines above |
| At `c5648f00`, [ADR-392](ADR-392.md)'s base, line **89** | `## Calendar work, not cron` |

**THE HEADING ARRIVED AT LINE 89 BY DRIFT, LONG AFTER THE POINTER WAS WRITTEN.** On the day the record was made, the heading was at 83 and the cited line held the row the sentence describes. **A second reader cannot reasonably disagree with a blob.**

**THIS IS [ADR-388](ADR-388.md) SECTION 5's OWN LESSON ARRIVING A THIRD TIME**, and each time in a different disguise. That entry ruled that [ADR-384](ADR-384.md) had measured a pointer at the wrong tree. [ADR-396](ADR-396.md) found the ruling's own remedy handing rows a third tree. **Here the wrong tree does not produce a wrong verdict, it produces a false APPEARANCE OF DIFFICULTY**, and a corpus that records difficulty honestly will carry that appearance forward for as long as nobody re-measures. **Four waves carried it.**

---

### 5. The eight repairs, each with a destination that is byte-identical and unique

**[ADR-397](ADR-397.md)'s PRECONDITION IS APPLIED AND NOT ASSUMED.** *Only the coordinate moves* presumes a coordinate to move to, so the test is whether the named subject still appears in the cited file at all. **It does, for all eight. And a stricter thing is true, which this row derived because section 6 made it necessary: each subject line occurs EXACTLY ONCE in the file today, so every destination is unambiguous.**

| # | Site | Was | Now | Named subject | Destination unique today |
|---|---|---|---|---|---|
| 1 | [session 159](../sessions/2026-08-24-session-159.md):120 | `:46` | **`:141`** | the `ADR-040` clock sentence, **quoted verbatim** | 1 occurrence |
| 2 | [session 166](../sessions/2026-08-24-session-166.md):52 | `:67` | **`:162`** | `offers.expires_at` | 1 occurrence |
| 3 | [session 166](../sessions/2026-08-24-session-166.md):52 | `:68` | **`:163`** | `promotional_credit_grants.expires_at` | 1 occurrence |
| 4 | [session 165](../sessions/2026-08-24-session-165.md):69 | `:74` | **`:170`** | `otp_send_budget.override_expires_at` | 1 occurrence |
| 5 | [ADR-080](ADR-080.md):110 | `:89` | **`:185`** | `VG-9`'s drill row | 1 occurrence |
| 6 | [ADR-080](ADR-080.md):132 | `:89` | **`:185`** | the same | 1 occurrence |
| 7 | [ADR-080](ADR-080.md):211 | `:89` | **`:185`** | the same, named 79 and 101 lines above. Section 5.1 | 1 occurrence |
| 8 | [d2 controls audit](../reviews/2026-08-29-d2-controls-audit.md):85 | `:94` | **`:188`** | the **Secret rotation** row, **quoted verbatim** | 1 occurrence |

**EVERY ONE OF THE SIX DISTINCT SUBJECT LINES IS BYTE-IDENTICAL BETWEEN ITS WRITE COMMIT AND THIS TREE**, compared blob to blob rather than by eye. **So no repair here is a judgement about what a sentence meant. Each is a lookup of where an unchanged line went.**

**THE RETIRED COORDINATE IS KEPT IN BACKTICKS BESIDE THE NEW ONE AND IS NEVER RE-CITED WITH ITS PATH** ([ADR-388](ADR-388.md) section 4, [ADR-367](ADR-367.md) section 7). The form is `` (was `:89`) ``, and **it was derived from the corpus rather than from the dispatch**: 62 instances of a retired coordinate exist under `docs/`, and **62 of 62 are backticked**. The dispatch's written form was unbackticked and is not the house form.

#### 5.1 [ADR-080](ADR-080.md):211 is the one call this row has to make, and the repair rule makes it

**THAT LINE NAMES NO SUBJECT AT ITS POINTER.** It is a summation, *"Every load-bearing claim above was read at its cited line in this session"*, followed by a bare list of thirteen coordinates. Read at the pointer, `G1` fails and the pointer is undecidable.

**READ AT THE RECORD, WHICH IS WHAT [ADR-388](ADR-388.md) ASKS FOR, `G1` HOLDS.** That ruling's gates are *"properties of the record and of `git`"*, and this record names the subject of this exact coordinate **twice above, at lines 110 and 132, both times as `VG-9`'s drill**. Nothing is guessed.

**AND THE DECIDING ARGUMENT IS NOT `G1`, IT IS OUTCOME ONE's OWN GUARANTEE: *"nothing the record asserted becomes untrue"*.** Repairing 110 and 132 and leaving 211 would leave the document saying *"every load-bearing claim above was read at its cited line"* while listing a coordinate that **contradicts the two claims above it**. **The repair would introduce a falsehood the record never carried.** So the summation moves with what it summarises.

**THIS IS [ADR-396](ADR-396.md) SECTION 10 RUNNING BACKWARDS, AND THAT IS THE FINDING WORTH KEEPING.** There, one sentence carried a class-one pointer and a class-two pointer four lines apart, and repairing the honest one alone would have asserted a 332-line separation the record never asserted, **so the repair rule REFUSED a permitted repair**. Here the same constraint **COMPELS** one. **The internal consistency of a record is a two-sided constraint on repair, and until now the corpus had only seen the side that says no.**

---

### 6. The twelve pointers this row does not own, checked anyway, and the mechanism that made four of them born wrong

**NONE OF THESE IS REPAIRED HERE.** They sit in [`digests.test.ts`](../../apps/worker/test/digests.test.ts), [STATE](../STATE.md), [session 323](../sessions/2026-08-28-session-323.md), [`sessions/README.md`](../sessions/README.md), [P7](../plans/P7-risk-and-abuse.md), [session 266](../sessions/2026-08-27-session-266.md) and [ADR-388](ADR-388.md), and every one of those is outside this fence or append-only to it. **They are checked because a row that repairs eight pointers into a file and does not look at the other twelve has not read the file.**

**`:27`, FOUR SITES: EXACT TODAY, NOTHING OWED.** The `Detector runs` scheduled row is still at line 27. **The restructure did not move it because it sits ABOVE the insertion point**, which is why [ADR-392](ADR-392.md)'s only *"nothing to repair"* verdict is still true a wave later.

**`:35`, FOUR SITES: CLASS TWO, LEFT, AND RE-DERIVED RATHER THAN INHERITED.** [ADR-392](ADR-392.md) called them class two on a test that was not `G2`, so the label was right for the wrong reason and had to be checked.

| At the write commit (`a21bfb8e` for the test file, `4b3e8e57` for the three documents) | |
|---|---|
| Runbook line **35** | the **Tier-1 economic calendar staleness check** row |
| The **Scheduled digest delivery** row, which is the subject | line **36**, one line below |
| Occurrences of the phrase the records name, *"It asserts the query, not the job"* | **5**, then and now |

**ANNOTATED BY [ADR-401](ADR-401.md): THE CELL ABOVE IS RIGHT ABOUT *THEN* AND WRONG ABOUT *NOW*.** The phrase is 5 at both write commits, `a21bfb8e` and `4b3e8e57`, exactly as recorded, and it was already **6** in the runbook on `c219c0d1`, this row's own base. **The figure is left standing because it decides `G2` on the *then* half alone**, and it is the same class this corpus keeps finding: a property derived at the record's day and reported as also true of the tree.

**`G2` FAILS. THE VERDICT STANDS AND THE MECHANISM IS NEW.** The cited line held **the named token and not the named subject**, because the token is a repeated house formula that five different jobs carry. **The author landed one row above the right one and the line they landed on read exactly like the claim they were making.**

**SO `G1` AS WRITTEN IS NOT STRONG ENOUGH, AND THIS IS THE ONE THING THIS ROW WOULD ADD TO [ADR-388](ADR-388.md) IF IT WERE ITS FILE TO ADD IT TO.** That gate asks for *"a token a later reader can search for"*. **A token that occurs five times in the cited file is a token a later reader can search for and cannot resolve.** Searching it returns five answers, four of them wrong, and the pointer looks exact from any of them. **The gate should ask for a token that is UNIQUE in the cited file**, and the four `:35` pointers are the instance: born wrong, invisible for a year, and invisible precisely because `G1` passed.

**IT IS NOT PROPOSED AS AN AMENDMENT AND NO CLASS IS MINTED.** [ADR-397](ADR-397.md) ruled six days ago that two gates partition this population into three cells and admit no fourth, and a uniqueness condition on `G1` **sorts nothing differently**: it is a sharper way to reach a verdict `G2` already reaches, and it would move zero of the eight repaired here, each of which was checked against it and passes at exactly 1 occurrence. **It is a strengthening of the CHEAP gate to catch what the EXPENSIVE gate catches anyway, which is worth writing down and is not worth a ruling.** **REFUTED BY [ADR-401](ADR-401.md) AND THE SENTENCE IS LEFT WHOLE: THE CONDITION DOES NOT SORT NOTHING DIFFERENTLY, IT SORTS 442 OF 495.** It fails on 89.3 percent of the named population, and the pointers it would move out of the decidable set include **80 that `G2` calls REPAIRABLE** and **42 that `G2` correctly calls BORN WRONG**, so taking it as a gate loses true verdicts rather than sharpening them. **The eight repaired here do pass it at exactly 1 occurrence, so this row's own claim about its own set holds**; what does not hold is the generalisation past it. That entry takes the observation as a condition on the REPAIR instead. [ADR-388](ADR-388.md) is row `400`'s file this wave and this is recorded for whoever holds it next.

**AND ONE FIGURE IN [ADR-392](ADR-392.md) IS NAMED AS WRONG WITHOUT BEING RE-CITED.** It reports the `:35` sites as **2 short**. They are 2 short at `c5648f00` and **1 short at the commit that wrote them**, because the digest row moved from 36 to 37 in between. **Both numbers are true about different trees and only the second decides `G2`**, which is the whole content of section 3 arriving on a set this row may not touch.

---

### 7. Falsification: not one of these eight is checked by anything, measured in all three document classes

**THE INSTRUCTION IS THAT A GREEN INVARIANT RUN IS NOT EVIDENCE. THIS SECTION IS WHY, ON THIS FENCE, IT IS NOT EVEN WEAK EVIDENCE.**

Every seed was taken on the real files, and **every restore is verified byte-identical with `sha256sum -c` and none was taken with `git checkout --`.**

| Seed | What was done | Gates | Invariants |
|---|---|---|---|
| **A** | [session 159](../sessions/2026-08-24-session-159.md):120's repaired `:141` pointed at **`:9999`**, 9,803 lines past the end of a 196-line file | **33 of 33** | **35 of 35** |
| **B** | The same break in [ADR-080](ADR-080.md):110 and in the [d2 controls audit](../reviews/2026-08-29-d2-controls-audit.md):85, a decision record and an operations review | **33 of 33** | **35 of 35** |

**NOTHING NOTICED, IN ANY OF THE THREE DOCUMENT CLASSES, AT ANY MAGNITUDE OF WRONGNESS.**

**THE MECHANISM WAS DERIVED AND NOT GUESSED.** `RI-16` is *"No **live** document cites a line that does not hold the name beside it"*. Its `documentScope` masks any file whose first `#` or `##` heading matches `RECORD_HEADING`, which is `/\b20\d{2}-[01]\d-[0-3]\d\b|\bsessions?\s+\d{1,4}\b/i`. **All five citing files match it**, the three session logs on their dates, [ADR-080](ADR-080.md) on the `(2026-08-22, ...)` in its title, the audit on the `2026-08-29` in its heading. `RI-15` reads reason comments in source files and none of these is one.

**SO THE ONLY THING STANDING BEHIND THESE EIGHT REPAIRS IS THAT THEY WERE READ, AND THEY WERE.** Each new coordinate was read at its line in the runbook after the edit landed, and each of the six distinct destinations was compared byte for byte against the blob at the write commit. **The 33 of 33 and the 35 of 35 reported in section 10 are true and are not evidence for anything in this entry.**

**AND THAT IS THE STRUCTURAL ANSWER TO WHY FOUR WAVES PASSED OVER THESE.** They are in the one region of the corpus where the citation checks do not look, they are unreachable by any runner, and the only control on them is a person with a fence. **[ADR-388](ADR-388.md) section 16 already ruled that adding history to `RI-15` would go red on 42 records nobody may repair. This row adds the other half of that picture: the checks are also blind to the 76 that anybody MAY repair, so the repairable set and the unrepairable set are equally invisible, and the corpus cannot tell them apart without a row like this one.**

---

### 8. The runbook does not move, and the argument is not convenience

**THE DISPATCH ASKED THIS ROW TO SAY WHY IF IT CONCLUDED THE RUNBOOK MUST MOVE. IT MUST NOT, AND THERE ARE THREE REASONS, ONE OF WHICH IS A MEASUREMENT.**

1. **THE PREDICATE IS SATISFIED AND THAT WAS DERIVED HERE INDEPENDENTLY.** [ADR-395](ADR-395.md) section 8 item 2 reports the gate leg needed no edit to this document at **21 jobs against 21 notes**. Counted again on `c219c0d1` through the document's own two section bounds: **21 scheduled rows, 21 severity notes.** The figure reproduces and nothing is owed to the leg.
2. **EDITING IT WOULD MOVE ALL EIGHT TARGETS THIS ROW JUST REPAIRED.** Every subject sits at or below line 141 of a 196-line file, so **any insertion above them re-breaks the repair in the same diff that made it**. That is [ADR-394](ADR-394.md)'s reasoning for repairing [P7](../plans/P7-risk-and-abuse.md) to a row name rather than a coordinate, and it is why this row's own diff touches no line of the cited file: **a pointer repaired to a coordinate its own wave invalidates is the defect re-armed with a fresher date.**
3. **IT IS A RUNBOOK A HUMAN READS UNDER PRESSURE.** [ADR-389](ADR-389.md) and [ADR-392](ADR-392.md) both carry that sentence as the reason to be careful with this file. **The eight pointers are a problem for readers of the corpus. They are not a problem for an operator at 3am**, who reaches this page through [`runbooks/README.md`](../ops/runbooks/README.md) and reads section headings. **A restructure to make citations more stable would be paid for by the one reader whose time actually matters.**

**AND THE COORDINATES ARE THEREFORE REPAIRED TO NUMBERS RATHER THAN TO NAMES, WHICH IS A DEPARTURE FROM [ADR-394](ADR-394.md) AND IS ARGUED RATHER THAN ASSUMED.** That row repaired to a name because the runbook was being restructured **in its own wave** and any number would drift before it was read. **That restructure is finished**: [ADR-392](ADR-392.md) landed it, [ADR-395](ADR-395.md) confirmed the document needed no further edit, and this row confirms it a third time at 21 against 21. **A number is the more useful repair when it will hold**, because it takes a reader to the line, and the retired coordinate beside it carries the record's own history either way. **If a founder expects the runbook to move again, the eight should be re-repaired to row names and this entry is wrong on exactly that point and on no other.**

---

### 9. Citations this diff moved, derived before the prose was written

**THE EDITS ARE STRICTLY LINE-COUNT NEUTRAL IN EVERY FILE, SO NO CITATION INTO ANY OF THEM MOVED.** Five files, **7 insertions against 7 deletions**, each file's line count identical to its blob at `c219c0d1`: 211, 212, 144, 174, 180.

**TWELVE POINTERS AIM INTO THESE FIVE FILES AND EXACTLY TWO LAND ON A LINE THIS DIFF EDITED.** Both are [ADR-392](ADR-392.md), at its lines 120 and 244, and **both cite [ADR-080](ADR-080.md):110, the one line whose text this diff changes and whose judgment call section 4 overturns.**

**NEITHER GOES STALE, AND THE REASON IS THE CONVENTION ITSELF.** [ADR-392](ADR-392.md):120 reads *"Its cited line 89 at this base is the heading"*. After this repair, line 110 of [ADR-080](ADR-080.md) still carries `` `:89` `` at that exact spot, in backticks, as the retired coordinate. **The record that describes the old pointer still lands on the line that holds it.** This is the first instance in the corpus of [ADR-388](ADR-388.md) section 4's form paying for itself: **had the retired coordinate been deleted rather than kept, this diff would have falsified two sentences in another row's ADR, in a file this row may not edit.**

**NOTHING ELSE IS OWED.** The other ten inbound pointers land at [ADR-080](ADR-080.md):25 and :193 and at the [d2 audit](../reviews/2026-08-29-d2-controls-audit.md):80, all unedited and all line-stable.

**AND THE REGISTRY ROWS THIS DIFF APPENDS MOVE NOTHING**, because [ALLOCATION](ALLOCATION.md)'s ADR table, [`decisions/README.md`](README.md) and [`sessions/README.md`](../sessions/README.md) are appended at their ends and [STATE](../STATE.md) gains a section at its end. **Every pointer into those files sits above every line this diff adds**, which was checked rather than assumed.

---

### 10. Verification, every figure derived at the moment of reporting

**THE BASE WAS REPRODUCED ON `c219c0d1` BEFORE THE FIRST EDIT.**

| | Base, before any edit | After |
|---|---|---|
| Test files | **336** | **336** |
| Passed | **8,109** | **8,109** |
| Skipped | **10** | **10** |
| Failed | **0** | **0** |
| Gates | **33 of 33** | **33 of 33** |
| Invariants | **35 of 35** | **35 of 35** |
| `typecheck`, `lint`, `format:check` | clean | clean |

**DELTA ZERO ON EVERY AXIS, AND THE DELTA ATTRIBUTED BY REMOVAL IS ZERO BECAUSE NOTHING WAS REMOVED.** No test was added, deleted, skipped, disabled or quarantined. **Section 7 is what says these repairs are correct. This table says only that they broke nothing**, and on a diff that changes seven lines of prose in five dated documents that is the weakest possible claim and it is stated as such.

**The gate count is derived from the runner's own closing line and not carried**, on a wave in which a sibling row edits [`gates.mjs`](../../scripts/corpus/gates.mjs). `gates.mjs generate` reports every span already matching its query, so **no generated span moved** and `gate_count` still derives 33.

---

### 11. Where this entry is most likely wrong

- **THE WHOLE ARGUMENT RESTS ON `git log -S` BEING THE RIGHT INSTRUMENT, AND THAT IS SIX DAYS OLD.** [ADR-396](ADR-396.md) section 7 introduced it and named its own open cost: **nothing has re-run [ADR-388](ADR-388.md)'s corpus-wide census of 289 under the corrected instrument**, and that census's 76 against 42 is the figure the whole repairability ruling stands on. **If `git log -S` is itself wrong on some shape nobody has found yet, these eight verdicts move with it.** The mitigation here is weak but real: two of the eight quote their subject verbatim, so those two are decided by string equality at the blob and survive any change of instrument.
- **[ADR-080](ADR-080.md):211 IS A CALL AND A SECOND READER COULD TAKE IT THE OTHER WAY.** Section 5.1 argues `G1` is a property of the record rather than of the line. **A reader who holds `G1` strictly at the pointer gets seven repairs here and one undecidable pointer**, and then owns the consequence that the document's own summation contradicts the two claims it summarises. **That consequence is the argument, and it is not a proof.**
- **THE UNIQUENESS CONDITION IN SECTION 6 IS DERIVED FROM ONE INSTANCE.** Four pointers at one coordinate, all written in two commits two days apart, are close to a single event. **A condition on `G1` proposed from a sample of one event is a hypothesis**, and the honest claim is that it explains this instance and predicts nothing else that has been measured.
- **THE EIGHT ARE REPAIRED TO COORDINATES AND THE CORPUS'S RECENT PRACTICE HAS BEEN TO REPAIR TO NAMES.** Section 8 argues the restructure is finished. **If it is not, this row has re-armed eight pointers with a fresher date and made them harder to spot, because a 2026-09-07 repair reads as more trustworthy than a 2026-08-24 original.** That is the worst outcome available from this diff and it is one insertion above line 141 away.

---

### 12. What is owed, stated precisely enough to be picked up

1. **THE `:35` FOUR ARE STILL WRONG, WILL STAY WRONG, AND THE FINDING IS NOT YET WRITTEN BESIDE THEM.** [ADR-388](ADR-388.md)'s outcome two requires the finding to be recorded at the pointer, and [ADR-394](ADR-394.md) did exactly that for [ADR-337](ADR-337.md) so a fourth row would not spend itself re-deriving it. **Four sites are owed that annotation**: [`digests.test.ts`](../../apps/worker/test/digests.test.ts):60, [STATE](../STATE.md):6095, [session 323](../sessions/2026-08-28-session-323.md):9 and [`sessions/README.md`](../sessions/README.md):556. **The last is not hand-repairable** and must go through the session log's own `<!--index:` line and a `gates.mjs generate`, which [ADR-396](ADR-396.md) section 8 measured. The exact finding to write: *born one line short at the commit that wrote it, on a row carrying the same house phrase as its subject.*
2. **`G1` SHOULD REQUIRE A TOKEN UNIQUE IN THE CITED FILE.** Section 6. [ADR-388](ADR-388.md) is row `400`'s file this wave, so this is recorded and not taken. **It changes no verdict in this corpus that has been measured**, including all eight repaired here.
3. **[ADR-388](ADR-388.md)'s CENSUS OF 289 HAS NEVER BEEN RE-RUN UNDER `git log -S`.** [ADR-396](ADR-396.md) section 7 owed this first and it is still owed. **This row is a second data point that the instrument matters**, since the eight it repairs were called unrepairable by four consecutive waves under a test that was not `G2` at all.
4. **NOTHING BINDS THE SEVERITY NOTES TO THE RUNBOOKS THEY NAME**, which [ADR-392](ADR-392.md) section 11 item 5 raised and which is untouched here. **Three of the 21 notes carry an `RB-nn` link and nothing checks the target exists**, re-derived at reporting time.
5. **[STRATEGY](../testing/STRATEGY.md) SECTION 4.4's `CI-06l` ROW STILL UNDER-DESCRIBES THE GATE**, which [ADR-395](ADR-395.md) section 8 item 1 owed with its exact replacement written out. It is row `398`'s file this wave and is untouched here.

---

### 13. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** Inherited as well as owed: [ADR-357](ADR-357.md) and [ADR-360](ADR-360.md) through [ADR-397](ADR-397.md) are `proposed` and unsigned, two rows run beside this one holding `398` and `400`, and this row settles none of them.

  **What a founder read adds and this entry cannot.**

  **THE FIRST THING TO DECIDE IS WHETHER FOUR WAVES OF "NOT REPAIRABLE" SHOULD HAVE BEEN CAUGHT SOONER, BECAUSE THE EVIDENCE WAS IN THE RECORD THE WHOLE TIME.** [ADR-392](ADR-392.md) states plainly that it is not applying the class test. **Three later entries read that section and carried its conclusion without its caveat**, and the caveat is one sentence long and sits four paragraphs above the table. **The corpus's failure mode here is not a wrong measurement. It is a correct measurement whose stated scope does not survive being cited**, and no gate can catch that.

  **THE SECOND IS SECTION 7, WHICH A FOUNDER SHOULD READ AS A STANDING CONDITION RATHER THAN AS THIS ROW'S RESULT.** Every citation in every dated record in this corpus is unchecked, in both directions, at any magnitude of error. **That is thousands of pointers whose only control is that somebody happened to be given the fence.** [ADR-388](ADR-388.md) ruled that a gate reading history would demand repairs its own ruling forbids, and that ruling holds. **The gap it leaves is the largest unguarded surface this corpus has, and this row is the fourth wave to walk across it and the first to be allowed to fix anything.**

  **AND THE THIRD IS THAT THIS ROW REPAIRED TO NUMBERS AGAINST THE RECENT PRACTICE OF REPAIRING TO NAMES.** Section 8 gives the argument and section 11 gives the way it fails. **The founder question underneath it is not about these eight: it is whether a corpus whose citation habit is a line number in a file that grows by insertion should keep that habit at all.** [ADR-388](ADR-388.md)'s option (b) adopted naming for NEW pointers and refused it as a migration. **Four waves of this exact document suggest the migration is the cheaper of the two, and nobody has priced it.**

- **Approval:** `___________________________` (UNSIGNED. **`E2` READ OWED.** **Merging is not signing.**)

| | |
|---|---|
| **Approved by** | |
| **Date** | |
