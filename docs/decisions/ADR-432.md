## ADR-432: `ADR-303` limit 4 is SPENT on the `breaker` family, the mapping is deleted, and this is the first such row that keeps the refusal its two precedents dropped (2026-09-07, status: proposed)

**Row 432 of the 429s wave. Branch `claude/breaker-port-family`, off `claude/wave-429s` at `9eb52656`, session 627.**

**[ADR-426](ADR-426.md) IS THE WORKED EXAMPLE, [ADR-430](ADR-430.md) IS THE SECOND ROW, AND THIS IS THE THIRD ON THE FAMILY THAT ENTRY NAMED.** Its section 12 item 1 says *"`breaker` IS THE CHEAPEST NEXT FAMILY AND IT COSTS MORE THAN THIS ONE DID"*. **The claim was re-derived rather than inherited and it HOLDS. Every one of its five numbers for this family is EXACT**, which is the first time in this sequence a sibling's table has survived re-measurement whole. Section 3 is the measurement. **Two of its other numbers are wrong and section 3.1 is what they were.**

**THE MAPPING IS DELETED AND IT IS THE WHOLE OF THIS ROW.** [`apps/worker/src/breaker/ports.ts`](../../apps/worker/src/breaker/ports.ts) declared `BreakerRow = Readonly<Record<string, unknown>>` at line `219` of the base tree, and [`apps/worker/src/breaker/evaluate.ts`](../../apps/worker/src/breaker/evaluate.ts) declared `record(value: unknown, where: string): BreakerRow` at line `127` of the base tree to cast back out of it. **The type is gone, the cast is gone, and the barrel's export of the name is gone.** `BreakerTx.rowsWhere` hands back the row the key declares, at [`ports.ts:252`](../../apps/worker/src/breaker/ports.ts).

**AND THE DIFFERENCE FROM BOTH PRECEDENTS IS THE ONE A FOUNDER SHOULD READ FIRST: THE ROW-SHAPE REFUSAL STAYS.** [ADR-426](ADR-426.md) went from nine `throw`s to eight and section 5 of [ADR-430](ADR-430.md) records it *"losing its existence check"*; that entry held its own count at eight only by adding a different refusal elsewhere. **Both deleted the check that says the port handed back something that is not a row. This row keeps it**, as `requireRow` at [`evaluate.ts:147`](../../apps/worker/src/breaker/evaluate.ts), which returns its argument UNCAST. Section 5 is the argument and section 7 is the case that watches it. **Eleven `*_cents` columns cross this family**, so `ADR-299` section 5.1 item 5 is not a formality here.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `432`, session **627**, branch `claude/breaker-port-family`, off `9eb52656`.

- **NON-MONEY DIFF, MONEY-ADJACENT SUBJECT, AND THIS ENTRY SAYS SO EXPLICITLY BECAUSE THE DISPATCH REQUIRED IT.** No DDL, no ledger row, no payout path. **But eleven `*_cents` columns cross the read surface** and the guard this row declined to delete is the one standing between a `number` and the denominator of a loss ratio. The **`E2`** read is **OWED** and section 11 names the files per line. Ships `proposed` with an **UNSIGNED** approval line. **Merging is not signing.** It inherits `E2` debt on [ADR-303](ADR-303.md), [ADR-416](ADR-416.md), [ADR-421](ADR-421.md), [ADR-426](ADR-426.md) and [ADR-430](ADR-430.md), all `proposed` and UNSIGNED.

- **NO MIGRATION NUMBER RESERVED OR TAKEN. `packages/db/migrations/` IS UNTOUCHED.** Nothing here is DDL and `git diff` on that directory is empty.

- **NOT ONE BYTE UNDER `packages/db/**`, AND THIS ROW WAS NOT GRANTED IT.** Section 8. [ADR-430](ADR-430.md) section 12 item 2 predicted the narrower fence would hold, and it did.

- **NO GATE WEAKENED, NO TEST SKIPPED, RENAMED, RELAXED, QUARANTINED OR RETARGETED, NO `RI-nn` MINTED, NO `CI-06` LETTER SPENT, NO EXISTING CASE INVERTED OR DELETED.** Three cases added, section 7.

- **NOT ONE BYTE UNDER `apps/api/**`, `packages/ledger/**`, `packages/tooling/**`, OR ANY `apps/worker/src` SUBTREE OTHER THAN `breaker/`.** The one file outside `breaker/` is `apps/worker/src/index.ts`, the barrel, and the edit to it is the removal of a dead type name, section 11.

---

### 1. The container, measured rather than assumed

**IT ARRIVED WITH NO REPOSITORY, FOR THE EIGHTEENTH CONSECUTIVE ROW.** [ADR-421](ADR-421.md) section 1 declined to mint an ordinal for this and that decision is followed rather than reversed.

| Measured | Command | Result |
| --- | --- | --- |
| Working directory | `ls -la /home/user` | `total 8`, two entries, `.` and `..` |
| Clone | `git clone https://github.com/lam2-coder/meritfutures.git` | Succeeded, NOT shallow |
| Install | `pnpm install --frozen-lockfile` | exit `0` |
| `git push`, first attempt | `git push -u origin claude/breaker-port-family` | **REFUSED**, `403`, *"not in this session's authorized repository set"* |
| `git push`, second attempt | the same command | **REFUSED**, identically |
| `git push`, after `add_repo` with `access: "push"` | the same command | Succeeded, `[new branch]` |

**THE PUSH REFUSAL REPRODUCED EXACTLY AS [ADR-430](ADR-430.md) SECTION 1 RECORDED IT, AND THE DISPATCH AGAIN NAMED THE REMEDY BEFORE IT WAS NEEDED.** The repository was NOT pre-checked with `curl` or `git ls-remote`, per the instruction, and the remedy worked first time. **The cost was one tool call, for the second wave running, because the instruction stayed written down.**

**THE RUNNER PATHS WERE CORRECT AGAIN**, `scripts/corpus/gates.mjs` and `packages/tooling/checks/repo-invariants.mjs`, both ran first time. **The container sentence is now eighteen rows old and four entries have asked for it to be fixed or deleted.**

---

### 2. The base, reproduced before the first edit, and it IS GREEN

Branched from `origin/claude/wave-429s` at `9eb52656`, NOT from `main`. `git status --short` was EMPTY before the first edit and stayed empty after `gates.mjs generate`.

| Leg | Observed on the base |
| --- | --- |
| `gates.mjs generate` | `every span already matches its query.`, tree clean after it |
| `gates.mjs check` | `33 of 33 gates pass.` |
| `repo-invariants.mjs` | `35 of 35 invariants hold.` |
| `pnpm vitest run` | **`Test Files 341 passed (341)`, `Tests 8272 passed \| 11 skipped (8283)`** |
| `typecheck` | exit `0`, `Done` for every package |
| `lint` | exit `0`, silent |
| `format:check` | `All matched files use Prettier code style!` |

**THE TWO RED CASES [ADR-430](ADR-430.md) SECTION 2 RECORDED DID NOT REPRODUCE, WHICH CONFIRMS ITS DIAGNOSIS RATHER THAN CONTRADICTING IT.** That entry derived that `RI-35`'s two timeouts in `packages/tooling/test/` are a load-dependent flake and not a defect in the tree, reproducing on a 153-second run and not on a 76-second one. **This base ran in 124 seconds and both passed.** Nothing was done to those files, which this fence forbids in any case.

**`pnpm run verify` WAS NOT RUN AND `scripts/corpus/falsify.mjs` WAS NOT RUN.** Both are prohibited by the dispatch and both mutate the working tree.

---

### 3. The family, chosen on this row's own measurement

**THE COLUMN COUNTS WERE DERIVED BY WALKING THE `pgTable` BODIES**, brace-matched over a copy of [`schema.ts`](../../packages/db/src/schema.ts) with comments and string literals blanked so a brace inside either cannot be mistaken for structure, counting top-level keys. **The instrument was calibrated against [ADR-430](ADR-430.md) section 3's own two hand-checks before any number below was used**, and it reproduces them exactly: `reportSchedules` 10, `reportDeliveries` 16, `digests` 26. It was then hand-checked once more independently on `plans`, 7 by hand and 7 by the walker.

| Family | Verbs of limit 4 | Read tables | Columns | `*_cents` columns | Consumer modules | Status |
| --- | --- | --- | --- | --- | --- | --- |
| `digests` | none remaining | 2 | **26** | **0** | 2 | **SPENT by [ADR-426](ADR-426.md)** |
| `recon` | none remaining | 3 | 60 | 17 | 1 | **SPENT by [ADR-430](ADR-430.md)** |
| `sweeps` | `rowsWhere`, `lockAt` | 2 | **53** | 6 | 1 | **BLOCKED**, below |
| `withdrawals` | `rowsWhere`, `rowAt`, `lockAt` | 5 | **73** | 4 | 1 | **BLOCKED**, below |
| **`breaker`** | `rowsWhere` | **5** | **95** | **11** | 1 | **TAKEN BY THIS ROW** |
| `detectors` | `rowsWhere` | **13** | **227** | **21** | 3 | last |
| `batch`, `live`, `provisioning` | **none of the four** | n/a | n/a | n/a | n/a | not rows |

**THE FIRST TWO ROWS READ `none remaining` AND THAT IS THE CHEAPEST CONFIRMATION IN THIS ENTRY.** The census instrument matches a verb declaration as `rowsWhere(`, and on `digests` and `recon` it now finds nothing, because both are declared `rowsWhere<K extends ...>(`. **The two spent families report themselves spent to an instrument that was not told about them.**

**`breaker` IS THE CHEAPEST TAKEABLE FAMILY AND THE SIBLING'S CONCLUSION SURVIVES ITS OWN ARITHMETIC.** `sweeps` at 53 columns and `withdrawals` at 73 are both smaller and neither is takeable, for reasons re-derived at source rather than inherited:

1. **`sweeps` DECLARES `lockAt`**, at [`sweeps/ports.ts:178`](../../apps/worker/src/sweeps/ports.ts), and [ADR-421](ADR-421.md) section 13 item 2 reserves those six `FOR UPDATE` sites for a founder's read BEFORE dispatch. **A CONTROL ALSO FREEZES ITS PORT'S SHAPE**: [`apps/worker/test/expiry.test.ts:1177`](../../apps/worker/test/expiry.test.ts) is `it('`ExpiryTx` declares exactly three members and `ledger` is not one of them')` and asserts the member list against the source as text.
2. **`withdrawals` IS BLOCKED BY AN ASSERTION RATHER THAN BY PROSE.** [`apps/worker/test/withdrawal-approvals.test.ts:355`](../../apps/worker/test/withdrawal-approvals.test.ts) is `it('the port imports nothing at all, so ADR-165s one door is untouched')` and matches `/^\s*(?:import|export)\b[^\n]*from '/m` against the source. **A narrowing needs `DeclaredRow` imported to be spelled, so a row taking that family must first decide whether to weaken that case, which is a ruling and not a diff.**

#### 3.1 [ADR-430](ADR-430.md) SECTION 3's TABLE IS RIGHT ABOUT THIS FAMILY AND WRONG ABOUT ONE OTHER

**EVERY NUMBER IT PUBLISHED FOR `breaker` IS EXACT**: five read tables, 95 columns, eleven `*_cents`, one consumer module, one verb. **Its section 12 item 1's supporting detail is exact too**, that `purchases` alone carries 30. This is the first entry in this sequence whose measurement of the NEXT family survived re-measurement without correction, and it is worth recording plainly after [ADR-426](ADR-426.md) section 3 was found low in every column and [ADR-428](ADR-428.md)'s headline was corrected by [ADR-429](ADR-429.md) section 9.

**TWO OF ITS OTHER NUMBERS ARE WRONG AND NEITHER CHANGES THE RANKING.**

1. **`detectors` HAS 13 READ TABLES AND NOT 14.** [`detectors/ports.ts:136-150`](../../apps/worker/src/detectors/ports.ts) is `DETECTOR_READ_TABLES` and its members were counted at source: `detectorDefinitions`, `fills`, `dailyMarks`, `identities`, `accounts`, `purchases`, `identityLinks`, `identitySignals`, `identityPhones`, `payoutTransfers`, `attributions`, `ruleStates`, `economicCalendar`. **[ADR-426](ADR-426.md) section 3 said 10 for the same list and [ADR-430](ADR-430.md) said 14. Both are wrong in opposite directions**, which is what a number nobody derives looks like over two waves.
2. **`detectors` IS NO LONGER `not counted`.** It is **227 columns and 21 `*_cents`**, measured here with the same walker. It is the largest by a factor of 2.4 against this family, and its position as last is unaffected.

**A SIBLING'S MEASUREMENT CAN BE WRONG EVEN WHEN ITS CONCLUSION IS RIGHT, AND THE CONVERSE IS ALSO NOW ON RECORD.** Nothing in `detectors/ports.ts` was touched to correct anything: that file is not this row's, and the defect is in two ADRs rather than in it.

---

### 4. The blast radius, PERFORMED and measured before the mapping was touched

**ON [ADR-418](ADR-418.md)'s INSTRUMENT: measure by performing the change.** The accessor half is already landed on this base, at [`scoped-db.ts:3795`](../../packages/db/src/scoped-db.ts), so the move performed alone was the PORT: the import at [`ports.ts:102`](../../apps/worker/src/breaker/ports.ts) and `rowsWhere` made generic, with **every consumer, adapter, fixture and case untouched**, then `pnpm run typecheck` over the whole workspace.

| Population | Under the port move alone |
| --- | --- |
| `pnpm run typecheck`, whole workspace | **2 errors** |
| Where | [`apps/worker/src/breaker/adapter.ts:255`](../../apps/worker/src/breaker/adapter.ts) and [`apps/worker/test/breaker.test.ts:156`](../../apps/worker/test/breaker.test.ts) of the tree as it then stood |
| Which | `TS2322` at both: the adapter's `rowsWhere` and the suite's fake `BreakerTx` |
| Errors in `apps/api/` | **ZERO** |
| Errors in `packages/` | **ZERO** |
| Errors in any package other than `apps/worker` | **ZERO** |
| **Errors in [`apps/worker/src/breaker/evaluate.ts`](../../apps/worker/src/breaker/evaluate.ts), THE ONLY CONSUMER** | **ZERO** |

**THAT LAST ZERO IS THE FINDING AND IT IS THE WHOLE ARGUMENT FOR DELETING THE MAPPING, AND IT REPRODUCES [ADR-430](ADR-430.md) SECTION 4 ON A DIFFERENT FAMILY.** The consumer did not move because it *could not see the change*: `record(value: unknown, where: string)` accepts anything the port hands it. **A mapping that takes `unknown` absorbs a narrowing completely.** That is [ADR-421](ADR-421.md) section 9 reason 2, *"zero errors is zero readers"*, observed from the inside for the second time. The narrowing reaches the reader only when the mapping is gone.

**AND THE THREE ZEROS ABOVE IT ANSWER [ADR-424](ADR-424.md)'s SEAM AGAIN.** A port whose RETURN narrows is still assignable everywhere a `Promise<unknown[]>` was declared, and the key vocabulary at [`ports.ts:126-132`](../../apps/worker/src/breaker/ports.ts), `BREAKER_READ_TABLES`, is the same closed union of five it always was.

---

### 5. The deletion, which is the deliverable, and the refusal that did NOT go with it

| Before | After |
| --- | --- |
| `export type BreakerRow = Readonly<Record<string, unknown>>;` in `ports.ts` | **deleted** |
| `return value as BreakerRow;`, the whole body of the mapping | **deleted** |
| `BreakerRow` in the barrel's `export type {}` block at `src/index.ts` | **deleted** |
| `BreakerRow` in `evaluate.ts`'s type import | **deleted** |
| `rowsWhere(key: BreakerReadTable, ...): Promise<unknown[]>` | `rowsWhere<K extends BreakerReadTable>(key: K, ...): Promise<DeclaredRow<K>[]>` |
| `foldWindow(purchases: readonly unknown[], payouts: readonly unknown[])` | `readonly DeclaredRow<'purchases'>[]` and `readonly DeclaredRow<'payoutRequests'>[]` |
| `latestPrevious(rows: readonly unknown[], ...)` | `readonly DeclaredRow<'planBreakerState'>[]` |
| `readText`, `readCents`, `readInstant`, `readTradingDay` taking `row: BreakerRow, key: string` | `<R extends object>(row: R, key: keyof R & string, ...)`, **so `tsc` now checks every column NAME against `schema.ts`** |
| `function record(value: unknown, where: string): BreakerRow` | `function requireRow<R extends object>(value: R, where: string): R`, **the refusal without the cast** |

**EVERY REFUSAL SURVIVED AND THE COUNT DID NOT MOVE IN EITHER DIRECTION.** `throw new BreakerRowError(` occurs **10** times in `evaluate.ts` before this row and **10** times after it; `throw ` occurs **11** times before and **11** after. **Unlike [ADR-430](ADR-430.md), this row did not have to add a compensating refusal to hold its count, because it did not drop one.**

**THE ONE THAT WOULD HAVE BEEN CHEAPEST TO LOSE IS THE ROW-SHAPE CHECK, AND IT IS THE ONE BOTH PRECEDENTS LOST.** Under the narrowed port, `requireRow`'s argument is already typed as a row, so `typeof value !== 'object'` reads to a reviewer as dead code. **It is not dead, and the reason is `ADR-112` foreclosure 4: nothing in this tree compares a `schema.ts` column type against the DDL.** `DeclaredRow` is a TRANSCRIPTION. The adapter reaches the accessor through `key as never` at [`adapter.ts:270`](../../apps/worker/src/breaker/adapter.ts), and a driver or a fake that handed back `null` would reach `readCents` at [`evaluate.ts:176`](../../apps/worker/src/breaker/evaluate.ts) and raise a bare `TypeError` on a money column instead of a refusal that names the row. **A narrowing buys a column's EXISTENCE and buys nothing about the row's ARRIVAL**, which is `ADR-299` section 5.1 item 5 applied one level out from where the two precedents applied it.

**AND THE SUITE PROVED THE CHECK IS LOAD-BEARING RATHER THAN THE PROSE ASSERTING IT.** Section 7 records the falsification: with `requireRow`'s `throw` removed, **case 9.3 fires and NOT ONE OTHER CASE IN THE FILE DOES.** A refusal that no other case watches is exactly the refusal a narrowing quietly retires.

**`readCents` STAYS FOR THE SAME REASON AT A HIGHER PRICE.** Its whole content is that a `number` here *"is a driver or a fake that widened the type, and accepting it would put the whole fold on floating point without a single float literal in the diff"*. Under the narrowed port `amountPaidCents` and `approvedCents` ARE `bigint` and the check reads as provably redundant. **It stays, because the type's authority is a transcription and the value it guards is the denominator of a loss ratio.**

**THE COST IS STATED RATHER THAN HIDDEN, AND IT IS ONE CAST AND NOT FIVE.** [ADR-430](ADR-430.md) section 12 item 1 predicted *"five casts in its adapter rather than three"*. **The prediction is wrong and the reason is structural rather than lucky.** `recon/adapter.ts` reaches the accessor through a `switch` with a branch per table, and each branch needs its own cast because the branch proves the key to a reader and not to `tsc`. **`breaker/adapter.ts` has no `switch`.** It reaches the accessor exactly once, at [`adapter.ts:270`](../../apps/worker/src/breaker/adapter.ts), where `key as never` was ALREADY spelled on the base tree; the narrowing adds the same cast on the result and nothing else. **The whole src-side cost of this row is one cast expression**, derived from the diff rather than counted by eye.

---

### 6. The controls that could have fired, and the trap confirmed by deriving it

**ONE GATE WENT RED ON THIS DIFF AND IT WAS RIGHT, AND IT FIRED ON THIS ENTRY'S OWN PROSE RATHER THAN ON ITS CODE.** `CI-06t`, *"Every generated span is closed before the next one opens"*, reported two findings: `docs/decisions/ADR-432.md` and `docs/sessions/2026-09-07-session-627.md` each *"opens and is never closed"* a span named `adr_count`. **Section 10 item 5 had SPELLED the span's opener in order to describe it**, and a document that spells an opener has opened one. The gate's own message carries the remedy, *"Name the span rather than spelling its opener"*, and both sentences now name it. **THE DEFECT WAS REAL AND THE REPAIR IS INSIDE THIS FENCE**: the gate's warning is that such a span is invisible to `CI-06g` for exactly as long as nothing is appended below it, so the next section added to either file would have supplied the closer and quietly acquired a generated span. **A row reporting on a span mechanism is the most likely row in this corpus to write one by accident**, which is worth stating because the next such report will be written the same way.

**NO `RI-nn` WENT RED, AND THAT IS A RESULT RATHER THAN AN ABSENCE OF CHECKING.** [ADR-426](ADR-426.md) section 8 records that `packages/db/src/scoped-db.ts` cannot grow a line near its top from inside a fence like this one, because `RI-15` and `RI-16` bind citations to it from a large population.

**THE POPULATION WAS DERIVED AT THE MOMENT OF WRITING RATHER THAN TAKEN.** Citations of the form `scoped-db.ts:NNN` occur **427** times across **152** files on this base, of which **401** are in `.md` documents. [ADR-426](ADR-426.md) reported 367 over 140 and [ADR-430](ADR-430.md) reported 424 over 150. **The population is still growing and the trap is real. This row did not open that file at all**, so its top was never at risk, which is section 8.

**THE SHARPER FINDING IS THAT THE TRAP HAS A SECOND FORM AND IT LANDS ON THIS ROW'S OWN FILES.** `breaker/ports.ts:NNN` is cited by line from **five** documents and `breaker/evaluate.ts:NNN` from **two**: [ADR-201](ADR-201.md), [ADR-214](ADR-214.md), [ADR-202](ADR-202.md), [ADR-430](ADR-430.md) and the session records for 379, 381 and 383. **This row's edit shifts lines in both files, and not one of those seven documents is inside this fence.**

**IT DID NOT FIRE, AND THE REASON IS A RULE RATHER THAN LUCK.** `RI-16`'s exclusion 1 puts a citation under a DATED OR SESSION HEADING out of scope, on `CI-06/derivable-counts`' own argument that such an entry *"IS A RECORD OF A MEASUREMENT MADE THAT DAY"*. **Every one of the seven is in a dated ADR title or a session log.** And `RI-15` reads source files rather than `docs/decisions`, so what would have caught this is a citation of a `breaker/` line from inside a source file. **There is none: `grep -nEo "[A-Za-z0-9_./-]+\.(ts|sql|md):[0-9]+" apps/worker/src/breaker/*.ts` returns nothing.**

**SO THE COUPLING [ADR-426](ADR-426.md) SECTION 8 REPORTED IS NARROWER THAN IT LOOKS, AND THE BOUNDARY IS NOW NAMED: an edit that shifts lines in a cited file is payable when the citing set is dated, and unpayable when it is source.** That is stated so the next row can check the shape of its citing set rather than the size of it.

---

### 7. The controls added, and the RED watched under each

**THREE CASES WERE ADDED AS SECTION 9 OF [`apps/worker/test/breaker.test.ts`](../../apps/worker/test/breaker.test.ts) AND EVERY ONE WAS WATCHED FAIL.**

| Control | Watched RED under |
| --- | --- |
| `9.1 the READ port hands back the accessor row and no longer re-states unknown` | **YES**, base sources restored under the new suite: `AssertionError: expected 'Promise<unknown[]>' to be 'Promise<DeclaredRow<K>[]>'` |
| `9.2 the hand-written mapping is DELETED from the slice, not merely unused` | **YES**, same run: `expected '...' not to match /export type BreakerRow\b/` |
| `9.3 every VALUE refusal survived the deletion, one for one and by subject` | **PASSES ON THE BASE BY CONSTRUCTION, WHICH IS ITS POINT: it asserts what must NOT change.** Falsified TWICE and fired both times, below |

**CASE 9.1 READS THE MEMBER'S OWN RETURN AND NOT AN `unknown` NEARBY.** This is the defect [ADR-421](ADR-421.md) section 10 records catching in its own fourth case, and the risk here is the same one [ADR-430](ADR-430.md) section 7 names: `BreakerTx` declares `insert` **two lines below** `rowsWhere` at [`ports.ts:254`](../../apps/worker/src/breaker/ports.ts) and it still returns `Promise<unknown[]>`, so a case reading the interface block for the string would pass on the wrong member. `returnOf` walks from the member name to the `)` that closes its parameter list, **matching parentheses so a generic parameter cannot end it early**, and reads what follows. **It also asserts the write path is still `unknown`**, so a later row narrowing `insert` is told it is answering a different question.

**CASE 9.2 SWEEPS OVER `code()` AND NOT OVER THE FILE**, because `evaluate.ts` QUOTES the deleted declaration in the docblock recording why it went. **The paragraph explaining a deletion can never be the thing that satisfies the case asserting it.**

**CASE 9.3 REFUSES A COUNT AS ITS INSTRUMENT**, for [ADR-426](ADR-426.md) section 7's two reasons: a count is a derivable number written down, and it passes just as happily when one reader loses its `throw` and another grows a second. Each refusal is read for its own subject.

**AND ITS FIRST FORM WAS WRONG, WHICH IS THIS ROW'S SHARPEST FINDING ABOUT ITS OWN WORK.** The money refusal was asserted as `toContain("typeof value === 'bigint'")`. **The falsification that was supposed to prove it fires DID NOT FIRE**: widening the guard to `typeof value === 'number' || typeof value === 'bigint'` retires the refusal on a money column while leaving that substring intact and satisfied. **A check that passes on a WIDENED guard watches the wrong half of the defect.** The case now asserts the guard whole, `if (typeof value === 'bigint') return value;`, plus a negative on the widened form, and both falsifications fire:

| Falsification | Result |
| --- | --- |
| `readCents`' guard widened to admit `number` | **9.3 RED**, `expected '...' to contain 'if (typeof value === 'bigint') retu...'`. Existing case `4.4` also fired |
| `requireRow`'s `throw` removed, leaving a pass-through | **9.3 RED**, `expected '...' to contain 'expected a row and received'`. **NOT ONE OTHER CASE IN THE FILE FIRED** |

**THAT SECOND ROW IS THE ARGUMENT FOR SECTION 5 IN ONE MEASUREMENT.** Before this row, deleting the row-shape refusal was invisible to all 52 cases in this suite. It is now watched by one.

---

### 8. `packages/db/**` WAS NOT GRANTED AND WAS NOT NEEDED, WHICH CONFIRMS [ADR-430](ADR-430.md) SECTION 8

**[ADR-430](ADR-430.md) SECTION 12 ITEM 2 SAID THE FENCE FOR THIS ROW COULD BE NARROWER THAN ITS OWN AND THE DISPATCH CUT IT THAT WAY. THE PREDICTION HOLDS AND WAS VERIFIED RATHER THAN ASSUMED.** `DeclaredRow` is declared at [`scoped-db.ts:4581`](../../packages/db/src/scoped-db.ts), exported through `packages/db/src/index.ts` and re-exported through the worker's one door at [`db.ts:149`](../../apps/worker/src/db.ts). **This row imports it from `../db.ts` and never names `@merit/db`**, which is `ADR-165`'s one-door rule and is what [`ports.ts:102`](../../apps/worker/src/breaker/ports.ts) spells.

**THE ACCESSOR WORK IS SPENT ONCE AND DOES NOT RECUR**, so the remaining families need a fence over their own directory and nothing else. **This is now confirmed on a second family rather than predicted from one, and the next dispatch can cut the same fence again.**

---

### 9. What the narrowing made harder, priced explicitly because the dispatch asked

**[ADR-430](ADR-430.md) SECTION 9 NAMED THIS TRADE AND THIS ROW MAKES IT AGAIN, ON A FLOAT THIS TIME.**

Case `4.4`, `a number in a cents column is REFUSED BY NAME rather than converted`, seeds two rows PostgreSQL could not produce, to prove `readCents` refuses rather than coerces:

1. `amountPaidCents: 9_900`, a `number` where the column is `bigint`.
2. **`approvedCents: 1.5`, a FLOAT**, into the numerator of a loss ratio.

**Under the narrowed port both columns ARE `bigint`, so both seeds are compile errors and the input the case exists to watch is no longer expressible without casting past the type.** `tsc` reported exactly these two and nothing else once the fixtures were complete.

**THE CASTS STAY, THE CASES STAY, AND THE REFUSALS STAY.** Deleting a cast would delete the case; deleting a refusal would trade a guard that FIRES for a claim nothing checks. **THE COST IS CHARGED ONCE RATHER THAN AT EVERY SITE THAT PAYS IT**: one named helper, `malformed(row, overrides)` at [`breaker.test.ts:385`](../../apps/worker/test/breaker.test.ts), carries the single cast and the whole argument above it, so a reader meets the trade in one place and a future case cannot quietly acquire a bare `as`.

**AND THE FIXTURE PRICE IS THE LARGEST SINGLE COST OF THIS ROW, EXACTLY AS [ADR-430](ADR-430.md) SECTION 12 ITEM 1 PREDICTED.** That entry paid **41** fixture columns to make its store hold `DeclaredRow`. **This row paid 71.**

| Store | Columns spelled on the base | Columns spelled now |
| --- | --- | --- |
| `plans` | 3 | **7** |
| `planVersions` | 2 | **15** |
| `purchases` | 5 | **30** |
| `payoutRequests` | 5 | **29** |
| `planBreakerState` | 9, across three inline rows | **14**, in one builder |
| **Total** | **24** | **95** |

**THE TOTAL IS 95 AND THAT IS NOT A COINCIDENCE: it is the family's own column count, because the store now spells each of the five tables exactly once and completely.** The walker's count and `tsc`'s demand agree to the column, which is a cross-check neither instrument was built to provide.

**WHAT 71 COLUMNS NOBODY READS BUYS IS THAT `purchase({ amountPaidCentss: 1n })` IS NOW A COMPILE ERROR.** Before this row it was a passing test over a row no migration can produce. **That is the trade in one sentence: 71 lines of fixture nobody reads, against a class of green run that means nothing.**

---

### 10. What this row did NOT do

1. **`insert` STILL RETURNS `unknown`**, and case 9.1 asserts it rather than leaving it implied. [ADR-430](ADR-430.md) section 12 item 4 rules the write path a different question and nothing here changes that. **[`breaker-adapter.test.ts:556`](../../apps/worker/test/breaker-adapter.test.ts) additionally asserts `ports.ts` contains `insert(key: BreakerWriteTable, values: BreakerValues)` verbatim**, so narrowing the write path on this family means deciding about that case first. **It was found before the edit and respected, not discovered by a red run.**
2. **`lockScope` WAS NOT TOUCHED**, on [ADR-426](ADR-426.md) section 10's derivation, which was not re-taken.
3. **`digests/ports.ts`'s HEADER IS NOW FALSE AND WAS NOT REPAIRED.** [`digests/ports.ts:5`](../../apps/worker/src/digests/ports.ts) still reads *"declared structurally, importing nothing"* and [`:12`](../../apps/worker/src/digests/ports.ts) still reads *"with no import in either direction"*, while [`:119`](../../apps/worker/src/digests/ports.ts) is `import type { DeclaredRow } from '../db.ts';`. **[ADR-426](ADR-426.md) left the sentence standing when it added the import.** `recon/ports.ts` carries the same claim at [`:10`](../../apps/worker/src/recon/ports.ts). **Neither file is inside this fence.** This row's own copy of that sentence was corrected rather than deleted, quoting the superseded form whole on `RI-14`'s idiom, at [`breaker/ports.ts:4-11`](../../apps/worker/src/breaker/ports.ts).
4. **THE TWO WRONG `detectors` TABLE COUNTS IN [ADR-426](ADR-426.md) AND [ADR-430](ADR-430.md) WERE NOT EDITED**, section 3.1. Both are dated entries and neither is this row's.
5. **ONE FILE OUTSIDE THE FENCE WAS CHANGED, BY THE GENERATOR THE DISPATCH REQUIRES, AND IT IS REPORTED HERE RATHER THAN LEFT SILENT.** The fence names `ADR-432.md`, `README.md`, `ALLOCATION.md`, `INDEX.md`, `docs/sessions/README.md` and this row's session record. **`node scripts/corpus/gates.mjs generate` also rewrote one span in [`docs/STATE.md`](../STATE.md)**: the span NAMED `adr_count`, from `414` to `415`, because this row adds an ADR file and that span counts them. **It is named here rather than spelled, which `CI-06t` required and taught by firing**: the first draft of this item wrote the opener out in full, and the gate read it as a span opened and never closed, in this file and in the session record both. Its message says *"Name the span rather than spelling its opener"* and that is what this sentence now does. **THE FENCE IS NOT SATISFIABLE WITHOUT IT AND THE GATE SAYS SO**, measured rather than argued: with that one line reverted, `gates.mjs check` reports `32 of 33 gates pass` and `CI-06g` names the span. **This is not a gate weakened to pass it and it is not a file this row had an opinion about**: it is one generated integer, and the alternative was shipping a corpus with a failing gate this row caused. **THE FINDING IS THE FENCE, NOT THE LINE**: any row that adds an ADR moves `adr_count`, so a fence granting `ADR-nnn.md` and not `docs/STATE.md` will hit this every time. It is stated so the next dispatch grants the file or names the span. This is the same shape as [ADR-430](ADR-430.md) section 10 item 5, arriving through a different span.
6. **NO ALLOCATION RESERVATION WAS NEEDED.** [ADR-430](ADR-430.md) section 10 item 5 records that a fence cut to one allocation cell cannot claim a number at the END of the table without opening a `CI-06f` hole below it. **That trap does not bite here and it was checked rather than assumed**: `431` is the highest claimed row and [ADR-431](ADR-431.md) exists, so `432` is the next number and no hole is created. **The finding stands for whoever claims `433`.**

---

### 11. Every leg, re-run after the last edit

| Leg | Observed on this branch |
| --- | --- |
| `gates.mjs generate` | `every span already matches its query.`, tree clean after it |
| `gates.mjs check` | `33 of 33 gates pass.` |
| `repo-invariants.mjs` | `35 of 35 invariants hold.` |
| `pnpm vitest run` | **`Test Files 341 passed (341)`, `Tests 8275 passed`, `11 skipped`, `(8286)`** |
| `typecheck` | exit `0`, `Done` for every package |
| `lint` | exit `0`, silent |
| `format:check` | `All matched files use Prettier code style!` |

**THIS IS THIS BRANCH'S OWN TOTAL AND NOT THE BASE'S.** The base is `(8283)` and this branch is `(8286)`. ****The suite grew by exactly three cases and no file, and the skip count did not move**, which is section 7s three controls and nothing else. Both totals were read off runs performed on this branch, the base before the first edit and this one after the last.**

**THE BARREL LEGS WERE COUNTED AND NOT TYPED.** `grep -o "from '\./[^']*/[^']*'" apps/worker/src/index.ts | sort -u | wc -l` returns **36** on the base and **36** after this row: a type name was removed from an existing leg and no leg was added or lost.

**THE `E2` READ THIS ROW OWES, PER FILE.** [`apps/worker/src/breaker/evaluate.ts`](../../apps/worker/src/breaker/evaluate.ts): the deletion of the cast, the four generic readers, and above all that `requireRow` and `readCents` are unchanged in what they refuse. [`apps/worker/src/breaker/ports.ts`](../../apps/worker/src/breaker/ports.ts): the narrowed member, the one type-only import, and the corrected header. [`apps/worker/src/breaker/adapter.ts`](../../apps/worker/src/breaker/adapter.ts): the single cast section 5 prices. [`apps/worker/test/breaker.test.ts`](../../apps/worker/test/breaker.test.ts): the `malformed` helper, the complete fixture rows, and section 9.

---

### 12. What is owed, stated precisely enough to be picked up

1. **`detectors` IS THE ONLY UNBLOCKED FAMILY LEFT AND IT COSTS ROUGHLY 2.4 TIMES THIS ONE.** Section 3: **13 read tables, 227 columns, 21 of them `*_cents`, three consumer modules**. Its port declares one verb at [`detectors/ports.ts`](../../apps/worker/src/detectors/ports.ts). **Expect its fixture bill to be the dominant cost**, on this row's 71 scaled by column count, and expect three consumers to move rather than one. **Whether one row should take it at all is a dispatch question rather than a technical one.**
2. **`sweeps` AND `withdrawals` STILL NEED A RULING BEFORE EITHER CAN MOVE**, section 3, and `sweeps` additionally wants the founder's read [ADR-421](ADR-421.md) section 13 item 2 reserves for the six `FOR UPDATE` sites. **Neither is a diff.** They are also both CHEAPER than `detectors` by a wide margin, 53 and 73 columns against 227, **so the ruling is worth more than the next row of work is.**
3. **THE FENCE FOR EVERY REMAINING FAMILY IS THIS ROW'S FENCE**, section 8, confirmed twice now.
4. **THE WRITE PATH IS UNTOUCHED ACROSS ALL TWENTY-FIVE FAMILIES**, and on this family a control at [`breaker-adapter.test.ts:556`](../../apps/worker/test/breaker-adapter.test.ts) reads `insert`'s signature as text, section 10 item 1.
5. **TWO ADRs REPORT `detectors` READ-TABLE COUNTS THAT ARE BOTH WRONG**, section 3.1, and neither is this row's to repair.
6. **`digests/ports.ts` AND `recon/ports.ts` CARRY A HEADER SENTENCE THEIR OWN IMPORT FALSIFIES**, section 10 item 3. **Each is a one-line repair inside a fence that already exists for that family.**
7. **A FENCE THAT GRANTS AN `ADR-nnn.md` MUST ALSO GRANT `docs/STATE.md` OR NAME ITS `adr_count` SPAN**, section 10 item 5. **Every row that writes an ADR hits this**, and the next dispatch can close it in one line.
8. **THE CONTAINER SENTENCE IS NOW EIGHTEEN ROWS OLD**, section 1.

---

### 13. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** Inherited as well as owed: [ADR-303](ADR-303.md), [ADR-416](ADR-416.md), [ADR-421](ADR-421.md), [ADR-426](ADR-426.md) and [ADR-430](ADR-430.md) are `proposed` and unsigned, and this row settles none of them.

  **What a founder read adds and this entry cannot.**

  **THE FIRST THING TO DECIDE IS THE ONE [ADR-430](ADR-430.md) SECTION 13 SAID WOULD ARRIVE.** That entry wrote: *"`breaker` is eleven more money columns and the argument does not get stronger by repetition."* **This is that row.** The argument it offers is not repetition: it is that the two precedents each dropped the row-shape refusal when they deleted their mapping, and this one did not, and that the difference is now watched by a case that fires when nothing else does, section 7. **A founder who reads the whole programme as the thin end of a wedge should say so here rather than at `detectors`, which is 227 columns and 21 money columns.**

  **THE SECOND IS THE 71 FIXTURE COLUMNS IN SECTION 9**, up from 41 one family ago. They are the price of a store that cannot hold an impossible row, paid in a file nobody reads for pleasure. **A founder who would rather have a loose store and short fixtures should say so now**, because `detectors` scales this cost by roughly 2.4 and that choice is much less reversible after it.

  **THE THIRD IS THAT `requireRow` LOOKS LIKE DEAD CODE AND IS NOT.** A reviewer who deletes it will get a green suite on every case but one. **The whole of section 5 is the argument for a check that a type checker believes is unreachable**, and it rests on `ADR-112` foreclosure 4 rather than on taste. **If that ruling is ever retired, this check should be retired with it and not before.**

  **THE FOURTH IS THAT THE ONLY THING THAT CAUGHT A REAL DEFECT IN THIS ROW'S OWN WORK WAS A FALSIFICATION, NOT A GATE**, section 7. The first form of case 9.3 was satisfied by a widened guard. **No `RI-nn` and no gate would have found that, and the only reason it was found is that the case was watched failing rather than watched passing.**

- **Approval:** `___________________________` (UNSIGNED. **NON-MONEY DIFF, MONEY-ADJACENT SUBJECT. `E2` READ OWED, ON THE FOUR FILES NAMED IN SECTION 11.** **Merging is not signing.**)

|                 |     |
| --------------- | --- |
| **Approved by** |     |
| **Date**        |     |
