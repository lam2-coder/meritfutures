## ADR-405: `clause 2` is the NAMING convention and [ADR-402](ADR-402.md) ruled on a different clause, so the text awaiting a founder does not carry the rule it was written for; question 3's premise is false at 7 of 42 rather than 42 of 42; and the `CLAUDE.md` home is measured for the first time at 368 of 387 (2026-09-07, status: proposed)

**[ADR-388](ADR-388.md) SECTION 16 QUESTIONS 2 AND 3 HAVE BEEN RECORDED AS OWED THREE TIMES**, by [ADR-400](ADR-400.md) section 17 item 1, by [ADR-401](ADR-401.md) section 13 item 4, and by the dispatch that sent this row. **Both are answered here and one of them is answered by being retired, with the measurement that justifies retiring it.**

**QUESTION 2 IS NOT OPEN IN THE WAY IT LOOKS.** [ADR-402](ADR-402.md) section 9 already ruled option (c) and already wrote the `CLAUDE.md` text a founder is being asked to sign. **This row affirms that ruling and corrects its subject.** That entry names `clause 2` and then states a different convention: it ruled on `clause 1`'s second branch, which is the honesty rule, and the proposed bullet carries the honesty rule and not the naming convention. **So the text now standing in front of a founder does not contain the rule question 2 asks about.** Section 4 derives which clause is which without trusting any summary, this one included.

**QUESTION 3 RESTS ON A CLAIM ABOUT THE SHAPE OF ITS OWN POPULATION AND THE CLAIM IS FALSE.** It says *"Every one of those pointers sits in an entry that a founder either read or will read, in a table cell whose verdict column says the claim was checked."* **Re-derived here over the born-wrong set under the instrument [ADR-396](ADR-396.md) ruled: 42 pointers, of which 13 make a claim of a check at all and 7 sit in a verdict column.** The other 29 are the sentence reporting somebody else's pointer wrong, a declared specimen, a quotation, or a coordinate naming a tree that was deliberately thrown away. **Option (c) was offered as *"the one place it would have caught all 42"*. It reaches 7.**

- **Origin:** [ALLOCATION](ALLOCATION.md) row `405`, branch `claude/adr-388s-last-two-questions`, session **600**, off the wave integration branch `claude/wave-401s` at `1f621d5c` rather than off `main`. The offset of 195 between the session number and the ADR number holds, as it did on rows `388`, `403` and every row between.
- **NON-MONEY. DATED RECORDS AND ONE REGISTRY. Ships `proposed` with an UNSIGNED approval line. Merging is not signing. `E2` read owed.**
- **NO GATE WEAKENED, NO TEST SKIPPED, DISABLED OR QUARANTINED, NO INVARIANT MINTED, NO `RI-` NUMBER RESERVED, NO `CI-06` LETTER SPENT, NO MIGRATION TAKEN OR RESERVED, NO REGISTER GROWN, NO PORT WIRED, NO JOB SCHEDULED.**
- **NOT ONE EXECUTABLE LINE CHANGED, AND NOT ONE FILE OUTSIDE `docs/` WAS OPENED FOR WRITING.** [`scripts/corpus/**`](../../scripts/corpus), [`packages/tooling/**`](../../packages/tooling) and [`CLAUDE.md`](../../CLAUDE.md) were READ and never written. **No line number into any file under `scripts/corpus/` is written anywhere in this diff**, because the row holding that directory this wave may move lines in it.
- **[`CLAUDE.md`](../../CLAUDE.md) IS UNTOUCHED AND THE TEXT IN SECTION 6 IS PROPOSED RATHER THAN TAKEN**, for the reason that file states about itself: changing a frozen document is an ADR and not a commit. Checked rather than asserted, section 13.
- **TWO FIGURES IN [ADR-388](ADR-388.md) SECTION 1 DO NOT REPRODUCE AT THAT ENTRY'S OWN BASE AND ARE ANNOTATED AT THE SITE**, section 3. They were found while validating the reader this row's own census runs on, and they are annotated rather than overwritten on [ADR-397](ADR-397.md) section 7's convention as [ADR-401](ADR-401.md) section 9 scoped it.

---

### 1. The container, measured rather than inherited

**THE DISPATCH STATED NOTHING ABOUT THE CONTAINER AND INSTRUCTED THIS ROW TO MEASURE IT. IT WAS MEASURED FIRST, BEFORE THE CLONE AND AFTER IT.**

| | |
|---|---|
| `/home/user` on arrival | **EMPTY.** `ls -la` shows `.` and `..` and nothing else |
| `git rev-parse --is-inside-work-tree` before the clone | **`fatal: not a git repository`**, exit 128 |
| `git rev-parse --is-shallow-repository`, run immediately after the clone | **`false`** |
| `git rev-list --count HEAD` | **4,329** |

**THE CONTAINER ARRIVES WITH NO REPOSITORY AND THIS IS THE SIXTH CONSECUTIVE ENTRY TO RECORD IT**, after [ADR-389](ADR-389.md), [ADR-394](ADR-394.md), [ADR-400](ADR-400.md) section 17 item 5, [ADR-401](ADR-401.md) section 13 item 6 and [ADR-402](ADR-402.md) section 1. **No ADR can fix it and this entry claims no more about it than the previous five did.**

**AND THE CLONE IS FULL FOR THE SEVENTH CONSECUTIVE ROW.** [ADR-398](ADR-398.md), [ADR-399](ADR-399.md), [ADR-400](ADR-400.md), [ADR-401](ADR-401.md), [ADR-402](ADR-402.md) and [ADR-403](ADR-403.md) each measured `false` and this row measures `false`. **The standing claim that a clone IS shallow was last true at [ADR-396](ADR-396.md) and has been wrong in every dispatch since.** Nothing was deepened, because `false` is the answer that says there is nothing to deepen. **The dispatch that sent this row said nothing about shallowness, which is the first one in this programme to get that right.**

---

### 2. The base, reproduced on `1f621d5c` before the first edit

**RUN IN FULL BEFORE ANY FILE WAS OPENED FOR WRITING.** `pnpm run verify` was NOT run and [`falsify.mjs`](../../scripts/corpus) was NOT run, on the dispatch's absolute instruction; the legs were run individually.

| Leg | On `1f621d5c`, before the first edit |
|---|---|
| `gates.mjs check` | **33 of 33 gates pass** |
| `repo-invariants.mjs` | **35 of 35 invariants hold** |
| `pnpm vitest run` | **336 file(s), 8,109 passed, 10 skipped, 8,119 total** |
| `pnpm run typecheck` | clean |
| `pnpm run lint` | clean |
| `pnpm run format:check` | *"All matched files use Prettier code style"* |

**THE DELTA THIS DIFF MAKES TO THAT BASE IS ZERO ON EVERY LEG**, section 13. No file under `apps/`, `packages/`, `scripts/` or `e2e/` is touched, so the test, typecheck, lint and format legs cannot move, and the gate and invariant counts are re-derived after the diff rather than assumed.

---

### 3. The reader, validated against the checkers' own figures, and the two that do not reproduce

**THE INSTRUMENT IS [ADR-377](ADR-377.md)'s AND THE FENCE FORCED A CHANGE TO ITS METHOD.** That method places an export-added copy of [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) in that checker's own directory so `REPO_ROOT` resolves. **`packages/tooling/**` is another row's this wave, so the copy was placed under `node_modules/` instead**, at the one depth where `resolve(HERE, '../../..')` still lands on the repository root. That directory is in the checker's own `SKIP_DIRS`, so the copy is invisible to `walk` and cannot enter its own census, and it is ignored by `git`. **It was deleted before the diff was committed.**

**THE READER WAS VALIDATED BEFORE IT WAS BELIEVED, AT [ADR-388](ADR-388.md)'s OWN BASE `8fa66104` RATHER THAN AT THIS ONE**, in a `git worktree` carrying its own copy of the instrument, so that base movement cannot explain agreement or disagreement.

| [ADR-388](ADR-388.md) section 1, at its own base | That entry | This row |
|---|---|---|
| `docs/`, backticked, the form both checks read | 8,623 | **8,623** |
| Carry a bindable NAME, over that scope | 363 | **363**, being 73 live and 290 in dated records |
| Named pointers in dated records, section 2's cardinal | 290 | **290** |
| **In a dated record by shape** | **7,242** | **7,528** per-line, **6,749** per-file heading |
| **The vacancy leg over `RI-16`'s live scope** | **101** | **92** |

**THREE FIGURES REPRODUCE TO THE UNIT AND TWO DO NOT, AND THE TWO THAT DO NOT ARE THE ENTRY'S OWN VALIDATION AND ITS OWN DENOMINATOR.**

**THE 101 IS THE FIGURE THAT ENTRY USED TO VALIDATE ITS READER.** Its sentence claims a derivation: *"Run here with `VACANT_LINE` verbatim over `RI-16`'s live scope, the answer is 101."* **Six readings were run at that entry's own base and none produces 101**: live scope with inheritance off gives **92** under `RI-16`'s own resolution and **92** under `RI-15`'s, live scope with inheritance on gives **111**, dated scope gives **366** and **472**, and both scopes together give **458** and **583**. **The figure is dated to [ADR-380](ADR-380.md) in `RI-16`'s own `covers` line, which says in the same breath that none of its counts was re-run; the reasonable reading is that the re-run reported the inherited number.**

**THE 7,242 IS LOAD BEARING FOR THE 8,390 AND THEREFORE FOR THE 8,100.** That table adds 7,242 to 1,148 to reach 8,390, and 8,390 less 290 gives the 8,100 that section 2 states `G1` fails on. **Neither available reading of "in a dated record by shape" produces 7,242 at that base**: the per-line reading the checkers' `documentScope` implements gives 7,528, and the whole-file reading gives 6,749. **This row cannot say which reading the entry took, and says so rather than guessing one.** The 1,148 is that entry's own figure over a form no check reads and is not re-derived here.

**WHAT THIS DOES AND DOES NOT DO TO [ADR-388](ADR-388.md).** It does not touch the ruling, which stands on the three-way split in its section 4, and that split is built on the 290 that reproduces exactly. **It moves the denominator of the "96.6 percent" sentence and it removes the validation the entry offered for its own reader.** Both are annotated at the site, line-count neutrally, and neither is overwritten.

**AND THE HARNESS WAS VALIDATED A SECOND TIME, AGAINST A SIBLING'S ENUMERATED SET RATHER THAN AGAINST A COUNT.** [ADR-401](ADR-401.md) section 4 enumerates eight pointers whose class differs between `git blame -w` and `git log -S`. **Four of the eight are the ones `blame` calls born wrong and `log -S` does not, and this row reproduces exactly those four, independently, at a different base**: [`STATE.md`](../STATE.md) line 10100, [session 478](../sessions/2026-08-30-session-478.md) line 9, [`STATE.md`](../STATE.md) line 5641 and [session 362](../sessions/2026-08-28-session-362.md) line 118. **Nothing else appears in the difference between this row's two legs in that direction, and the `log -S` set is a strict subset of the `blame` set.**

---

### 4. Which clause is `clause 2`, derived rather than inherited

**THE DISPATCH CALLED IT THE NAMING CONVENTION, [ADR-401](ADR-401.md) CALLS IT THE NAMING CONVENTION, AND [ADR-402](ADR-402.md) CALLS IT SOMETHING ELSE. THE DERIVATION BELOW TRUSTS NONE OF THEM.**

[ADR-388](ADR-388.md) section 2 contains two ruling clauses and one numbered list. The list is `G1` and `G2`, the two gates. **The clause numbering is not the list's, and the entry itself says so**: its section 18 writes *"should reject clause 1's second branch"*, and the only thing in section 2 with branches is the three-outcome partition. **So `clause 1` is the two-gate repair ruling together with its three outcomes, and its second branch is the outcome that reads *"G1 holds and G2 fails: REPAIR IS FORBIDDEN"*.** That reading is forced by what the sentence is about: it concerns a founder who reads the born-wrong pointers as clerical, and the born-wrong outcome is exactly the second branch.

**`clause 2` IS THEREFORE THE ONLY OTHER RULING CLAUSE IN THAT SECTION**, the paragraph adopting [ADR-384](ADR-384.md)'s option (b): *"A citation into a document names the ROW, SECTION or SYMBOL it is about, beside the pointer, in the token or in front of it."*

**AND THE QUESTION'S OWN PREDICATE CONFIRMS IT.** Question 2 says of its subject *"It binds nothing and no runner reads it."* **`G2` binds a great deal**: it is one half of the test that decides whether a repair is permitted, and the whole of section 4's third column. **The naming clause is the one the entry itself describes as binding nothing**, in the same paragraph: *"It is NOT proposed as a check"*, and *"repairability is decided by a person with `git blame`, not by a runner"*.

**[ADR-402](ADR-402.md) SECTION 9 STATES THE OTHER ONE.** It writes that clause 2 *"states a convention: a citation records a check that was actually made, so a coordinate written where the named subject was not read is not stale but wrong"*. **That is `clause 1`'s second branch, restated accurately, under the wrong name.**

**THE CONSEQUENCE IS NOT A WORDING QUIBBLE, BECAUSE THE PROPOSED TEXT FOLLOWED THE MISREADING.** The bullet that entry put in front of a founder reads *"Write a line number only where you read the named subject at that line"* and asks for a name only in the branch where the coordinate is dropped. **A pointer written with a coordinate its author did verify and with no name at all satisfies that bullet completely and violates `clause 2` completely.** That pointer is class three, undecidable, and no later reader can ever repair it. **The bullet as it stands closes the born-wrong class and leaves the undecidable class open, and the undecidable class is the larger one under every instrument this corpus has run.**

**TWO ENTRIES ON ONE WAVE READ ONE CLAUSE OF ONE RULING AND DESCRIBED TWO DIFFERENT CONVENTIONS.** That is not an argument this row constructed. It is a fact about the tree, it is checkable in two greps, and section 5 is what it decides.

---

### 5. RULED on question 2: option (c), and the precedent is measured rather than asserted

**RULED: (c). THE CONVENTION BELONGS IN [`CLAUDE.md`](../../CLAUDE.md)'s CONVENTIONS (BINDING) LIST. [ADR-402](ADR-402.md)'s CONCLUSION IS AFFIRMED AND ITS SUBJECT IS CORRECTED.**

**(b) IS REFUSED AND THIS ROW ADDS NOTHING TO THE TWO REASONS ALREADY ON THE RECORD.** [ADR-388](ADR-388.md) called it *"a check advertising a rule it does not enforce"*; [ADR-402](ADR-402.md) section 4 priced the stronger objection, that `covers` already carries two moods with no grammar separating them and a third would make a measured defect worse. **Both hold and neither needed re-deriving.** A third reason is available and is recorded rather than leaned on: the `covers` strings live in a file another row holds this wave, and section 12 of that entry measured what editing that region costs in pointers.

**(a) IS REFUSED, AND FOR THE FIRST TIME THE REFUSAL HAS AN INSTANCE RATHER THAN AN ANALOGY.** [ADR-402](ADR-402.md) argued from [ADR-399](ADR-399.md), where a rule in a dated record was read as a prohibition by four consecutive waves. **The instance now available is this question itself.** `clause 2` shipped as prose in one dated record on 2026-09-06. **Within one wave, the first row dispatched to act on it ruled on a different clause and wrote the wrong rule into the text a founder is being asked to sign, while two sibling entries on the same wave named it correctly.** A convention whose own wave cannot agree on what it says is a convention in the wrong place, and no amount of care fixes that: [ADR-402](ADR-402.md) is a careful entry, and section 4 is not a criticism of it.

**AND (c)'s MECHANISM IS MEASURED, WHICH NOBODY HAD DONE.** [ADR-402](ADR-402.md) called the precedent *"exact"* and named two rules: the em-dash rule and the integer-cents rule. **Only one of the two is the shape `clause 2` is in.** Integer cents is backed by a runner, [`assert_no_floats.sql`](../../scripts/db/assert_no_floats.sql), which `RI-24` requires `corpus.yml` to run. **The em-dash rule is enforced by nothing**: no gate and no invariant reads for the character, and one survives inside a `covers` string in the checker file itself. **So it is the exact analogue of `clause 2`, a rule about how a session writes, obeyed only because it is read at the start of every session.** Derived on this base, over the numbered entry files, counting outside fenced blocks:

| Band | Entries | Entries carrying at least one em-dash |
|---|---|---|
| `ADR-001` to `ADR-100` | 100 | **13** |
| `ADR-101` to `ADR-200` | 91 | **0** |
| `ADR-201` to `ADR-300` | 95 | **2** |
| `ADR-301` to `ADR-380` | 78 | **4** |
| `ADR-381` to `ADR-403` | 23 | **0** |
| **Total** | **387** | **19** |

**368 OF 387 ENTRIES OBEY A RULE NO RUNNER CHECKS, THE VIOLATIONS CONCENTRATE IN THE FIRST HUNDRED, AND THE LAST 23 CONSECUTIVE ENTRIES CARRY NONE.** That is the measured answer to *"does a line in `CLAUDE.md` actually hold a write-convention in this corpus"*, and it is yes. **It is the strongest thing that can be said for (c) and it is stronger than the argument (c) was carried on.**

**THE COMPARISON IS STATED WITH ITS LIMIT.** The em-dash rule is a single character a writer can see; `clause 2` asks for a judgement about what a sentence is about. **The precedent shows the CHANNEL works and does not show that this rule will be obeyed at that rate.**

---

### 6. The consolidated text, PROPOSED and NOT taken, as ONE bullet

**THE DISPATCH ASKED FOR ONE THING TO APPROVE RATHER THAN TWO ENTRIES TO RECONCILE, AND SECTION 4 IS WHY THAT MATTERS: THE TWO ENTRIES DO NOT SAY THE SAME THING.** [ADR-402](ADR-402.md)'s bullet carries the honesty rule. `clause 2` is the naming rule. **They close different populations and the corpus needs both, so the consolidation is one bullet carrying both halves rather than a choice between two drafts.**

**WHY BOTH HALVES, STATED AS THE TWO POPULATIONS THEY REACH.** Section 8's census is the evidence and it separates cleanly:

| Half | What it prevents | The population it reaches on this base |
|---|---|---|
| **Naming**, `clause 2` | A pointer nobody can ever repair, because no reader can tell what it was for | Every citation the record does not name. **7,398 of the 7,692** backticked citations in dated records under `docs/` carry no name the binder reads, and [ADR-400](ADR-400.md) section 12 measured that a person still reads a subject in about 93 percent of those |
| **Honesty**, `clause 1`'s second branch | A pointer that was never right, which cannot be repaired at all without manufacturing a verification | The born-wrong class. **42** on this base under the ruled instrument, of which **13** make a claim of a check |

**THE 13 ALL NAME THEIR SUBJECT ALREADY**, which is what put them in the census at all, so the naming half does not reach one of them. **And the naming half's population is the one the honesty half cannot see.** A bullet carrying either half alone leaves the other population exactly where it is, which is the measured reason this is one bullet and not a choice.

**PROPOSED FOR THE `Conventions (binding)` LIST IN [`CLAUDE.md`](../../CLAUDE.md), AS ONE BULLET, IN THAT LIST'S VOICE. IT IS NOT TAKEN AND THE FILE IS NOT EDITED.**

> `- A citation names its subject, and a line number in one is a claim that you read that subject there. Write the row, section or symbol the pointer is about beside the pointer, in the token or in front of it. Write the line number only where you read the named subject at that line, in the tree you are writing in; otherwise name the subject and leave the coordinate out. A pointer with no name cannot be repaired by anybody later, and a pointer that was never right cannot be repaired at all without manufacturing a verification ([ADR-388](docs/decisions/ADR-388.md) section 2, consolidated by [ADR-405](docs/decisions/ADR-405.md)).`

**THE BULLET IS QUOTED AS LITERAL TEXT AND NOT AS A LIVE LINK**, on [ADR-402](ADR-402.md) section 9's reason, which is correct and is kept: the paths are written relative to the repository root where [`CLAUDE.md`](../../CLAUDE.md) sits, and a live link in this file would resolve from `docs/decisions/` and be wrong in both places at once.

**AND IT NAMES SECTIONS RATHER THAN LINES, WHICH IS THE RULE APPLIED TO ITSELF.** [ADR-402](ADR-402.md) deliberately carried no section at all, because [ADR-388](ADR-388.md) was being amended beside it and a coordinate written that day could be moved. **This row is the amender, its edits to that file are line-count neutral, and a section number is not a line number.** A convention that could not survive naming its own source would be teaching the opposite of what it says.

**[ADR-402](ADR-402.md)'s BULLET IS SUPERSEDED AND IS NOT DELETED.** Its section 9 and its section 11 item 3 are annotated in place, pointing here, on [ADR-397](ADR-397.md) section 7's convention. **One signature discharges both entries; two drafts in front of a founder is the condition this row was sent to end.**

---

### 7. The census for question 3, taken on both instruments and stated with its traversal

**THE POPULATION IS THE ONE BOTH PRIOR CENSUSES USED**, backticked citations under `docs/` that sit in a dated record by `documentScope`'s own reading and that the binder names. **294 on this base, of which 293 resolve to a file long enough to hold the line.** 172 are right today and 121 are wrong today.

**INSTRUMENT ONE IS [ADR-388](ADR-388.md)'s, `git blame -w` on the citing line.** Instrument two is the one [ADR-396](ADR-396.md) section 7 ruled, and [ADR-401](ADR-401.md) section 5 ruled that a row using it states its traversal and its search string or its figure is not reproducible. **This row states both: the DEFAULT traversal, no `--first-parent` and no `--full-history`, taking the oldest result; and the search string is the backticked token AS THE FILE WRITES IT rather than the reader's synthesised `cited` field, which is the leg that entry caught itself getting wrong.**

| Over 293 resolving named pointers in dated records | `git blame -w` | `git log -S` |
|---|---|---|
| True then, true now. Nothing owed | 170 | 166 |
| DRIFTED after being true: REPAIRABLE | 75 | 72 |
| **FALSE ON THE DAY IT WAS WRITTEN: NOT REPAIRABLE** | **46** | **42** |
| False then, true now, by coincidence | 2 | 2 |
| Undecidable, no introducing commit or no tree to read | 0 | 6 |
| Not in a backticked token, so `log -S` has no search string | 0 | 5 |

**THE `log -S` BORN-WRONG SET IS A STRICT SUBSET OF THE `blame` SET, AND THE FOUR IN THE DIFFERENCE ARE THE FOUR [ADR-401](ADR-401.md) SECTION 4 ENUMERATES**, section 3. **Every figure below is taken over the 42, the ruled instrument's set**, and the classification in section 8 was applied to the 46 first and then restricted, so the four that leave are named rather than dropped silently: two were class A and two were class B.

**THE HARNESS WAS FALSIFIED IN BOTH DIRECTIONS BEFORE ITS OUTPUT WAS USED.**

| Seed | What it disables | Result |
|---|---|---|
| **A** | the subject, replaced by a token present in no file in the tree | **282 BORN WRONG, 0 drifted, 0 true-then-true-now.** The name leg decides the class |
| **B** | the history leg, the cited file read at `HEAD` instead of at the write commit | **BORN WRONG rises from 42 to 119 and DRIFTED collapses from 72 to 0.** The `git` leg is doing the work the ruling rests on |

---

### 8. REFUTED: question 3's premise, at 7 of 42

**THE QUESTION STATES A PROPERTY OF ITS WHOLE POPULATION AND THE PROPERTY WAS NEVER MEASURED.** *"Every one of those pointers sits in an entry that a founder either read or will read, in a table cell whose verdict column says the claim was checked. They were not checked, or they were checked somewhere other than where the entry says."*

**THE CRITERION WAS STATED BEFORE THE READING, ON [ADR-400](ADR-400.md) SECTION 12's PRACTICE.** For each of the 42: **does the citing sentence assert, in its own voice, that the named subject is at the cited coordinate?** All 42 were read at their sites. A regular expression over verdict tokens was written first and discarded, because it scored 12 where a reading scores 7 in one direction and misses rows reading `HELD` and `Two of the three are wrong` in the other. **A crude instrument in either direction is worse than none here, and the population is small enough to read.**

| Over the 42 born-wrong pointers | Count | What the record actually says |
|---|---|---|
| **A. A CLAIM OF A CHECK** | **13** | The sentence asserts the subject is there. If the coordinate is wrong the record claims a verification nobody made. **This is question 3's population** |
| **B. THE SENTENCE REPORTS THE POINTER WRONG** | **15** | The citing sentence quotes somebody else's coordinate in order to record it as stale, born wrong or repaired. The record asserts the OPPOSITE of the pointer |
| **C. A DECLARED SPECIMEN OR SEED** | **6** | Deliberately wrong, and the entry says so at the site |
| **D. A QUOTATION OF SOMEBODY ELSE'S TEXT** | **4** | A test's expected string, a source comment's own pointer, another entry's sentence, reproduced verbatim |
| **E. A COORDINATE IN A TREE THAT WAS THROWN AWAY** | **4** | A compile error observed in a seeded tree that was reverted. `G2` tests it against a tree it was never about |

**AND OF THE 13 THAT ARE CLAIMS, 7 SIT IN A TABLE ROW WITH A VERDICT COLUMN. THE OTHER 6 ARE PROSE.**

**SO THE PREMISE IS FALSE TWICE.** 29 of 42 make no claim at all, and 6 of the 13 that do are not in a table. **Question 3's option (c), offered as *"the one place it would have caught all 42"*, reaches 7.** A rule about verdict columns would leave 6 claims of a check standing in prose and would silence 22 records that are behaving correctly.

**CLASS B IS THE FINDING AND IT IS THE LARGEST CELL.** Fifteen of the 42 are a row doing exactly what [ADR-388](ADR-388.md)'s outcome two requires of it: leaving a wrong pointer in place and writing the finding beside it. **The census then reads the finding as a fresh wrong pointer.** [ADR-319](ADR-319.md) section 3 writes *"[ADR-251] section 2 cites `anchorCalendar` at `liability.ts:1605`; the declaration is now at [`liability.ts:1624`]"*, and the census scores `liability.ts:1605` as born wrong in [ADR-319](ADR-319.md). **Repairing that coordinate would make the sentence say the pointer had never been wrong.** The annotation programme this corpus has been running for four waves manufactures born-wrong pointers at the rate it discharges them, and nothing had counted that.

**CLASS E IS SMALL AND IT IS A HOLE IN `G2` RATHER THAN IN A RECORD.** Four pointers name a line in a tree that was seeded, watched red and reverted, which is the evidence `RI-23` requires of a class B approval. **That tree was never committed, so `G2` tests the coordinate against the wrong tree by construction and can only ever return false.** No repair is available and none is owed; the record is correct and the instrument cannot see why.

**THE 13 ARE NOT DISMISSED AND THIS ROW DOES NOT CALL THEM CLERICAL.** They are records that claim a check at a coordinate where the check was not made, and [ADR-388](ADR-388.md) is right that repairing them would manufacture evidence. **The number is smaller than the question assumed and it is a floor for the same reason every figure in this family is one**: [ADR-400](ADR-400.md) section 12 ruled `G1` is a person's read, so the binder's 294 of 7,692 is one twenty-fifth of the subject and the 13 scales with it. **This row bounds the shape and declines to invent the size, which is that entry's own distinction.**

---

### 9. RULED on question 3: retired into question 2, and (b) and (c) refused with the measurement

**RULED: THE `E2` READ IS UNCHANGED, OPTIONS (b) AND (c) ARE REFUSED, AND THE QUESTION IS RETIRED BECAUSE ITS REMEDY IS QUESTION 2's SECOND HALF.**

**(c) IS REFUSED AT 7 OF 42**, section 8. It was offered as the option that catches everything and it catches one in six.

**(b) IS REFUSED ON THE SAME MEASUREMENT AND ONE MORE.** *"Require that a verdict column naming a line be re-derived at the reading"* has the identical coverage, 7 of 42, because it is scoped to the same column. **And it asks the `E2` read to become a thing it structurally is not.** The constitution's `E2` and `C10` put the founder's read on money-path diffs against the plan doc's acceptance criteria, and its own words are that the failure class is *"access control, authorization bypass, and trust boundaries"*. **A coordinate cannot be checked by reading the document it sits in. It is checked against another file at another commit**, which is not a diff read, and section 7's harness is what checking one actually costs.

**(a) IS TAKEN IN ONE SENSE AND REFUSED IN THE OTHER.** The `E2` read is not changed by the 13, which is what the question asks, so the answer to the question as written is no. **But *"accept it as the cost of prose"* is not what this row is saying, because the 13 are not prose. They are claims.** The remedy exists and it is at write time rather than read time: **do not write a coordinate you did not read.** That is [ADR-402](ADR-402.md)'s sentence, it is `clause 1`'s second branch turned into a rule for writers, and it is already the second half of section 6's bullet.

**SO QUESTION 3 IS RETIRED RATHER THAN CARRIED FORWARD.** Its population is 13 rather than 42, its shape is not the one it states, its two active options reach 7 of that population, and its real remedy is a rule this row is already asking a founder to sign for a different reason. **A fifth row re-deriving it would find the same thing, and section 8 is written so that it does not have to.**

**WHAT A FOUNDER MAY REVERSE HERE, IN ONE LINE.** A founder who holds that the 13 claims are serious enough to change the read can require exactly that: that any coordinate in a verdict column be re-derived at the `E2` read. **This row measured that it would reach 7 of 42 and would leave 6 claims standing in prose, and refuses it on the coverage rather than on the principle.** The principle is defensible and the scoping is not.

---

### 10. Falsification

**THE HARNESS IS FALSIFIED IN SECTION 7 AND THE RULINGS ARE FALSIFIED HERE.** Neither ruling mints a check, so there is no runner to seed; what can be falsified is the derivation each rests on.

| Claim | How it would be shown wrong |
|---|---|
| `clause 2` is the naming clause | Produce a reading of [ADR-388](ADR-388.md) section 18's *"clause 1's second branch"* on which `clause 1` is `G1`. `G1` has no branches, and the sentence is about the born-wrong outcome, which is a branch of the partition |
| The proposed bullet does not carry `clause 2` | Write a pointer that satisfies [ADR-402](ADR-402.md)'s bullet and `clause 2` both, with a verified coordinate and no name. It cannot be done, which is the point |
| Question 3's premise fails at 7 of 42 | Read the 42 sites. They are enumerated by file and line in section 8's classification and every one was read at its site |
| 368 of 387 obey the em-dash rule | Re-run the count outside fenced blocks. A different fence-stripping rule moves it; the corpus-wide raw count is 247 and the fenced-block-excluded count is 223 over 58 of 1,372 documents |
| The two [ADR-388](ADR-388.md) figures do not reproduce | Find a seventh reading of the vacancy leg, or a third reading of "in a dated record by shape", that produces 101 or 7,242 at `8fa66104`. Six and two were tried and are listed |

**AND ONE THING THIS ROW COULD NOT FALSIFY.** The classification in section 8 is a reading of 42 sentences by one reader, and no second reader checked it. **[ADR-400](ADR-400.md) had the same limit on its 60 and said so; this row inherits it and does not pretend otherwise.** The cells most open to argument are the four in class D, where a quotation of another file's pointer could be argued to be a claim about that file.

---

### 11. Citations this diff moved, audited under [ADR-388](ADR-388.md)'s own ruling

**ZERO. NO CITATION REPAIR IS FORCED BY THIS DIFF, AND THAT WAS DERIVED RATHER THAN HOPED FOR.**

**THE THREE ANNOTATED FILES ARE EDITED LINE-COUNT NEUTRALLY.** Every annotation extends an existing line; no line is added to or removed from [ADR-388](ADR-388.md), [ADR-400](ADR-400.md) or [ADR-402](ADR-402.md). **`git diff --numstat` reports equal insertions and deletions on each of the three and `wc -l` is unchanged on all three**, section 13.

**AND THE COST OF GETTING THAT WRONG WAS MEASURED RATHER THAN ASSUMED.** The dispatch called [ADR-388](ADR-388.md) heavily cited, which is true by document and not by line.

| Into | Coordinate-token pointers, either reader | Prose positional references |
|---|---|---|
| [ADR-388](ADR-388.md) | **0** | **5**, at two distinct lines: four naming line 28 and one naming line 298 |
| [ADR-400](ADR-400.md) | **0** | **1**, naming line 266 |
| [ADR-402](ADR-402.md) | **0** | **0** |

**NO CHECK IN THIS REPOSITORY READS ANY OF THE SIX.** They are the form [ADR-397](ADR-397.md) section 7 ruled on, `[ADR-N](ADR-N.md) line 92`, which carries no coordinate token and which `citationsIn` cannot see in either the backticked form or the link form. **So the only protection those six have is a row choosing to be neutral**, and the annotation sites here sit at lines 18, 30, 337 and 338 of [ADR-388](ADR-388.md), two of them above line 28. **Neutrality was not a preference on this file. It was the condition of touching section 1 at all.**

**[ADR-400](ADR-400.md) LINE 266 IS ONE OF THAT ENTRY'S TWO DELIBERATELY FALSE SPECIMENS**, and it is class C in section 8's table. It is not repaired, it is not annotated, and this row's only edit to that file is at its section 17.

---

### 12. What is owed, stated precisely enough to be picked up

1. **THE `CLAUDE.md` BULLET IN SECTION 6 IS UNTAKEN AND NEEDS A FOUNDER.** It is a change to a frozen document, which is an ADR and not a commit, and this entry is that ADR. **Nothing in the tree changes until it is signed**, and [ADR-402](ADR-402.md) section 11 item 3 is the same item, now consolidated into this one rather than standing beside it.
2. **[ADR-388](ADR-388.md) SECTION 1's 7,242 IS ANNOTATED AND NOT RESOLVED.** Two readings were tried and neither produces it. **A row that re-derives the corpus-wide dated population should state which reading it takes**, because the 8,390 and the 8,100 both descend from this cell and both move by the same amount.
3. **THE ANNOTATION PROGRAMME MANUFACTURES BORN-WRONG POINTERS AT THE RATE IT DISCHARGES THEM**, section 8 class B, 15 of 42 on this base. **This is reported and no remedy is proposed**, because the alternative to quoting a wrong coordinate while reporting it is not writing the coordinate, and that would make the finding unreadable. **A row deriving a class must separate the two, and this entry is the first to say so.**
4. **CLASS E IS A HOLE IN `G2` AND NOBODY HAS PRICED IT.** Four pointers name a tree that was seeded and reverted, so `G2` is asked a question about a tree that was never committed. **Four is a floor over the binder's slice and the seeded-defect practice is corpus-wide**, so the real figure is larger and is not derivable without reading every `RI-23` evidence record.
5. **[ADR-388](ADR-388.md) SECTION 15 ITEMS 1, 2, 3, 5 AND 7 REMAIN UNTOUCHED**, and none of them was this row's. **With questions 2 and 3 answered, that entry's section 16 is closed**: question 1 was answered by [ADR-400](ADR-400.md) and both remaining questions are answered here.
6. **THE CONTAINER ARRIVES WITH NO REPOSITORY, FOR THE SIXTH CONSECUTIVE RECORDED ENTRY**, section 1. **No ADR can fix it and this is the fifth consecutive entry to say so.**

---

### 13. Verification, every figure derived at the moment of reporting

| Claim | How it was derived, on `1f621d5c` plus this diff |
|---|---|
| Container empty, clone full, 4,329 commits | `ls -la /home/user`, `git rev-parse --is-inside-work-tree`, `git rev-parse --is-shallow-repository`, `git rev-list --count HEAD` |
| Base and delta, 33 of 33 and 35 of 35 | `gates.mjs generate` then `gates.mjs check`, and `repo-invariants.mjs`, run before the first edit and again after the last |
| 8,623 / 363 / 290 reproduce at `8fa66104` | The export-added reader, run in a `git worktree` at that commit carrying its own copy |
| 101 does not reproduce; 92 under the stated reading | Six readings of `VACANT_LINE` over `RI-16`'s scope at `8fa66104`, both resolvers |
| 7,242 does not reproduce; 7,528 and 6,749 | `documentScope`'s per-line `inRecord`, and `RECORD_HEADING` against the first level-1 or level-2 heading |
| 294 population, 293 resolving, 121 wrong today | The reader over `docs/`, `citationsIn(body, false)`, `inRecord === true`, `c.name !== null` |
| 170 / 75 / 46 / 2 and 166 / 72 / 42 / 2 | `git blame -w --porcelain` on the citing line, and `git log -S <token> -- <file>` on the default traversal taking the oldest, with `git show <commit>:<path>` for the then-tree |
| The four instrument disagreements | The set difference of the two born-wrong sets, compared against [ADR-401](ADR-401.md) section 4's enumerated table |
| 13 / 15 / 6 / 4 / 4 and 7 in a verdict column | All 42 sites read at their sites against the criterion stated in section 8 before the reading |
| 19 of 387 entries carry an em-dash | Fenced blocks blanked, the character counted by codepoint (`U+2014`, written here as an escape so this row does not add one to its own count), bands over the numbered entry files |
| 7,692 dated citations and 7,398 unnamed | The same reader, over the dated side of the same scope |
| Zero inbound coordinate tokens, six prose positional references | Both citation readers over the whole tree, plus a regular expression for the `[ADR-N](ADR-N.md) line N` form |
| Line-count neutrality on all three annotated files | `wc -l` before and after, and `git diff --numstat` |
| [`CLAUDE.md`](../../CLAUDE.md) untouched | `git diff 1f621d5c -- CLAUDE.md` is EMPTY |
| The instrument copy left nothing behind | `git status` reports no untracked file under `node_modules/`, which `.gitignore` covers, and the directory was deleted |

---

### 14. Where this entry is most likely wrong

- **SECTION 8's CLASSIFICATION IS ONE READER'S READING OF 42 SENTENCES AND NO SECOND READER CHECKED IT.** The criterion was written before the reading and the sites are enumerated, which is the best available defence and is not a control. **Class D is the softest cell**: a sentence that quotes another file's pointer could be read as asserting that file's content, which would move up to 4 into class A and make the headline 17 of 42 rather than 13.
- **`clause 2` IS DERIVED FROM ONE PHRASE IN SECTION 18 AND FROM THE ABSENCE OF ANY OTHER CANDIDATE.** [ADR-388](ADR-388.md) never numbers its clauses at the site. **If its author meant `G2` by `clause 2`, then [ADR-402](ADR-402.md) is right and this section 4 is wrong**, and the consolidated bullet is still correct because it carries both halves. **That is deliberate: the bullet does not depend on the derivation coming out this way.**
- **THE 42 IS THE BINDER'S AND THE BINDER IS NOT WHO READS `G1`.** [ADR-400](ADR-400.md) section 12 settled that, so 13 is a floor over one twenty-fifth of the subject and the true claim population is larger by an unknown factor. **The ratio 7 of 42 is what refutes question 3's premise, and a ratio measured on a biased slice is a weaker thing than a count.** The bias runs toward pointers written in two particular shapes, and nothing says those shapes are representative of verdict columns.
- **THE EM-DASH PRECEDENT MEASURES A CHARACTER AND `clause 2` ASKS FOR A JUDGEMENT.** Section 5 states this limit and it is the weakest joint in the argument for (c). **A founder who reads the two rules as different in kind should discount the 368 of 387 heavily**, and (c) then rests on the discoverability argument alone, which is where [ADR-402](ADR-402.md) left it.
- **THE VACANCY FIGURE MAY HAVE A SEVENTH READING.** Six were tried at the right base with both resolvers. **The claim is that none of six produces 101, not that no reading does.**
- **THIS ROW READ FIVE ENTRIES AT LENGTH AND RAN ONE READER OVER TWO TREES.** A restatement of `clause 2` in a session log, a review, or a plan is outside everything measured here, and section 4's *"two entries on one wave"* is a count of decision records rather than of documents.

---

### 15. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** Inherited as well as owed: [ADR-357](ADR-357.md) and [ADR-360](ADR-360.md) through [ADR-403](ADR-403.md) are `proposed` and unsigned, and this row settles none of them.

  **What a founder read adds and this entry cannot.**

  **THE FIRST THING TO DECIDE IS THE BULLET IN SECTION 6, BECAUSE IT IS THE ONLY THING HERE THAT CHANGES THE TREE.** Two entries now propose text for the same list and they carry different rules. **This one carries both halves and supersedes the other, and a signature on it discharges [ADR-402](ADR-402.md) section 11 item 3 as well.** A founder who wants only the honesty half should sign [ADR-402](ADR-402.md)'s bullet instead and say so, and the undecidable class then stays open by decision rather than by accident, which is the outcome this row exists to make visible.

  **THE SECOND IS THAT A CAREFUL ENTRY RULED ON THE WRONG CLAUSE AND NOTHING CAUGHT IT.** [ADR-402](ADR-402.md) is not a sloppy record. It read a ruling in a dated record, took the wrong one of two adjacent clauses, and wrote the wrong rule into a proposal, and the wave around it contained two other entries naming the clause correctly. **No gate reaches this and none could.** It is the strongest evidence in this programme that a convention living in a dated record is a convention that does not survive its own wave, and it is the argument for section 6 more than any measurement in section 5 is.

  **THE THIRD IS THAT THIS ROW REFUTED A PREMISE ITS OWN DISPATCH DID NOT HAND IT.** Question 3's premise was written by [ADR-388](ADR-388.md), carried forward by [ADR-400](ADR-400.md) and by [ADR-401](ADR-401.md), and never measured by any of the three. **It is false, and the shape it is false in matters more than the count**: 15 of 42 born-wrong pointers are entries correctly reporting somebody else's wrong pointer, which is the annotation practice this corpus has run for four waves showing up in its own census as a defect. **A founder deciding whether the annotation programme is worth continuing should read section 8 class B before section 9.**

- **Approval:** `___________________________` (UNSIGNED. **`E2` READ OWED.** **Merging is not signing.**)

| | |
|---|---|
| **Approved by** | |
| **Date** | |
