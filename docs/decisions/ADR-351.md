## ADR-351: the eleven migrations `RI-37` still registered as having no landing record get one, all eleven measured against PostgreSQL 16.13 rather than reconstructed, the backlog register that opened at twenty reaches zero and stops being a register, and the manifest's superseded `OI` table is found carrying nine stale STATE cells that its own superseding banner never claimed to supersede (2026-09-05, status: proposed)

**[ADR-334](ADR-334.md) SHIPPED `RI-37` AND A SHRINK-ONLY REGISTER OF TWENTY MERGED MIGRATIONS THAT CARRY NO LANDING RECORD**, and refused to write those twenty sections in the diff that added the check, because *"twenty records nobody measured is the one thing a landing section may not be."* [ADR-335](ADR-335.md) took six, [ADR-336](ADR-336.md) took three. **THIS ROW TAKES THE REST.** All eleven now hold [`DELTA_MANIFEST`](../../packages/db/DELTA_MANIFEST.md) sections `49` to `59`, their eleven register rows are deleted in the same commits as the sections that close them, and `LANDING_RECORD_BACKLOG` is left holding nothing.

**THE COUNT IS ELEVEN OF ELEVEN AND THE LIVE TOTAL WAS DERIVED HERE RATHER THAN INHERITED.** The dispatch said the register had stood at 20, then 14, then 11, and told me not to trust the last figure. Read off the file before the first edit, `LANDING_RECORD_BACKLOG` held **eleven** entries; `RI-37` passing on that same tree is what proves the figure, because leg 1 reports any migration on disk that is neither recorded nor registered and leg 3 reports any registered migration that is already recorded. **Six of the eleven open with an `E2 READ: MONEY PATH` header**, measured with `grep -c` over each file rather than off the register's own markers: `0037`, `0044`, `0050`, `0059`, `0063`, `0074`.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `351`, branch `claude/records-carried-forward-nine-times`, session **530**.
- **NOT THE MONEY PATH BY FILE, AND IT IS THE RECORD OF SIX MONEY-SCHEMA MIGRATIONS AND FIVE MORE BESIDE THEM.** `git diff --stat origin/main -- packages/db/migrations` is **ZERO LINES**, measured at the moment this line was written. `scripts/db` shows **one file**, [`probe_wallet_debit_provenance.sql`](../../scripts/db/probe_wallet_debit_provenance.sql), and the change to it is one comment sentence whose number was wrong. **NO MIGRATION IS EDITED**, which is constitution `E2`: migrations are sacred, superseded by addition and never edited.
- **AN `E2` READ IS OWED ANYWAY**, and eleven rulings are inherited, all `proposed` and all UNSIGNED: [ADR-053](ADR-053.md), [ADR-066](ADR-066.md), [ADR-069](ADR-069.md), [ADR-070](ADR-070.md), [ADR-164](ADR-164.md), [ADR-193](ADR-193.md), [ADR-200](ADR-200.md), [ADR-237](ADR-237.md), [ADR-252](ADR-252.md), [ADR-128](ADR-128.md) and [ADR-169](ADR-169.md). This diff writes down what six money-schema migrations did to `rule_states`, `promotional_credit_grants`, `plan_versions`, `live_account_state`, `ledger_transactions`, `otp_challenges` and `firm_parameters`, and a reader trusting a section that was reconstructed rather than measured would be trusting a copy.
- **A DISPATCH PROMPT IS NOT A SOURCE.** Every figure below is a `psql` run against a database this session built, a `grep` this session ran, or a runner's own last line. **Three claims the dispatch made were re-verified and all three hold**; a fourth thing it did not name did not hold and is section 7.

---

### 1. What was re-derived before the first edit, and what the baseline was

**EVERY ROW BELOW WAS RUN ON `c0d15f0` BEFORE THE FIRST EDIT.**

| Claim | Read at | Held |
|---|---|---|
| `main` carries through [ADR-338](ADR-338.md) | `git ls-tree origin/main --name-only docs/decisions/` after `git fetch origin main` | **YES.** It ends at `ADR-338.md` |
| `339` to `350` are held by branches rather than by `main` | `git cat-file -e "<ref>:docs/decisions/ADR-<n>.md"` over every one of the **556** refs under `refs/remotes/origin/` | **PARTLY, AND THE MEASURED ANSWER IS NARROWER THAN THE DISPATCH'S.** `ADR-339` exists, on `claude/the-letter-that-heads-four-sections`, session **530**. **`ADR-340` to `ADR-350` exist on NO ref this session can see.** Section 6 |
| The register holds eleven and six are money path | The register itself, then `grep -c 'E2 READ: MONEY PATH'` returning 1 on each of six files | **YES**, and the money-path six are the six the dispatch named |
| `0059` appears in the delta manifest **zero times in any form** | `grep -c '0059' packages/db/DELTA_MANIFEST.md` | **YES.** Zero. It is the only one of the eleven for which that is true; the other ten are named somewhere and none of those mentions is a landing record, which is `RI-37`'s own distinction between a claim before the verb and a cross-reference after it |
| [`probe_wallet_debit_provenance.sql:53`](../../scripts/db/probe_wallet_debit_provenance.sql) says `0054` provisions **seven** scoped accounts | The line, then [`0054:127-131`](../../packages/db/migrations/0054_identity_ledger_accounts.sql) and [`0054:163-168`](../../packages/db/migrations/0054_identity_ledger_accounts.sql) | **YES, AND IT IS WRONG.** Both the trigger's `INSERT` and the backfill's `VALUES` list carry **three** codes. Section 5 |
| `OI-06 (payout destinations)` is **open** in [ALLOCATION](ALLOCATION.md) and [`0051:8`](../../packages/db/migrations/0051_payout_destinations.sql) records the table landing under [ADR-169](ADR-169.md) | Both files | **YES, AND THE MANIFEST ALREADY CLOSED IT.** Section 4 |
| PostgreSQL 16 is available for the measurement | `psql --version` -> **16.13**, `pg_isready` accepting after `pg_ctlcluster 16 main start` | **YES** |

**Baseline, reproduced on `c0d15f0` BEFORE the first edit and quoted off each runner's own last line:** invariants **35 of 35**; gates **33 of 33**; `pnpm run typecheck` clean; `pnpm run lint` clean; `pnpm run format:check` *"All matched files use Prettier code style!"*; `pnpm vitest run` **312 files, 7,666 passed, 10 skipped, 0 failed**, exit 0; `git status --porcelain` empty. **Skips are reported separately from passes**, because a skip is not a pass.

---

### 2. The measurement, and it is the whole row

**ONE FORWARD-ONLY LADDER AND TWENTY-THREE MORE DATABASES.** `packages/db/migrations/*.sql` was applied into an empty database under `ON_ERROR_STOP=1`, one file at a time, with the full object counts read after **every** migration on disk; then eleven databases each stopping at one of the eleven, eleven more each carrying every migration **except** one of the eleven, and a clone of the fully applied set per perturbation. Every count is read from `pg_tables`, `pg_indexes`, `pg_constraint`, `pg_trigger`, `pg_proc` and `pg_views`, every constraint body from `pg_get_constraintdef`, every trigger from `pg_get_triggerdef` and every grant from `information_schema.role_table_grants`.

**THE DELTA IS THE NUMBER A READER USES.** Read as the change each file makes to the catalogue at its own landing point:

| | tables | indexes | CHECKs | FKs | uniques | triggers | functions | views |
|---|---|---|---|---|---|---|---|---|
| `0037` | 0 | 0 | **0** | 0 | 0 | 0 | 0 | 0 |
| `0039` | +2 | +7 | +10 | +1 | 0 | 0 | 0 | **+1** |
| `0040` | +2 | +7 | +22 | +1 | 0 | 0 | +1 | 0 |
| `0041` | 0 | **+1** | 0 | 0 | 0 | 0 | 0 | 0 |
| `0043` | 0 | +1 | +2 | +1 | 0 | 0 | 0 | 0 |
| `0044` | +1 | +5 | +3 | +4 | 0 | 0 | 0 | 0 |
| `0050` | +1 | +1 | +2 | +1 | 0 | 0 | 0 | 0 |
| `0059` | 0 | 0 | 0 | 0 | 0 | **+1** | **+1** | 0 |
| `0063` | 0 | 0 | 0 | 0 | 0 | **+1** | **+1** | 0 |
| `0073` | +2 | +7 | +10 | +2 | **+2** | 0 | 0 | 0 |
| `0074` | +1 | +2 | +2 | +1 | 0 | 0 | 0 | 0 |

**`0037`'s ROW IS ALL ZEROES AND IT IS THE MOST CONSEQUENTIAL FILE IN THE SET.** It renames a `CHECK` on `rule_states`, so one constraint leaves and one arrives and every total holds. A session auditing this estate by diffing counts either side of a migration would conclude it did nothing; what it did was decide what a column may hold on every funded account. **No count this catalogue keeps can see a predicate**, and manifest section 49 says so at the migration's own number.

**`0050` CREATES A ROLE AND NO COLUMN OF THAT TABLE COUNTS IT.** `merit_live` is a cluster object. The measurement that shows it is the grant set: `merit_live` reaches exactly two tables on the fully applied schema, `live_account_state` with `INSERT, SELECT, UPDATE` and `platform_account_refs` with `SELECT`, read from `information_schema.role_table_grants`. **The `DELETE` that [`0050:275`](../../packages/db/migrations/0050_live_cache_and_role.sql) revokes from `merit_live` is absent**, which is the one arm of that file a count would never have shown.

**[`assert_no_floats.sql`](../../scripts/db/assert_no_floats.sql) holds at all eleven landing points**, exit 0 each, so no file in the set declares a non-integer numeric column.

#### 2.1 Re-application, and all eleven are refused

Each file re-applied against its own landing schema: **eleven refusals, no acceptances.** [ADR-335](ADR-335.md) found one of six that re-applied clean and recorded the hazard; there is no such file here. Two of the eleven refuse somewhere a reader would not predict, and both are in the sections:

- **`0037` refuses at its FIRST statement**, `constraint "rule_states_high_water_bounds_balance" of relation "rule_states" does not exist`. The `DROP` carries no `IF EXISTS` and the name is already gone, so the refusal never reaches the `ADD`.
- **`0063` refuses at the TRIGGER and not at the function.** [`0063:100`](../../packages/db/migrations/0063_otp_challenge_consumption.sql) is a `CREATE OR REPLACE FUNCTION`, which is idempotent by rerun; the file is not, because [`0063:122`](../../packages/db/migrations/0063_otp_challenge_consumption.sql) is a plain `CREATE TRIGGER`.

---

### 3. THE PROBE COVERAGE, derived by execution rather than claimed, and four of the eleven are covered by nothing

**THE SET WAS DERIVED THREE WAYS AND THEN EXECUTED.** Grep for every object name the eleven files create over `scripts/db/`; then, for each of the eleven, apply every migration on disk **with that file removed** and run all twenty-seven readers under `scripts/db/`; then, where removal is unavailable or inert, perturb the fully applied schema at the exact object and run all twenty-seven again. **All twenty-seven are green on the unperturbed full set**, which is the baseline every row below is a departure from.

| Migration | What goes RED without it | Result |
|---|---|---|
| `0037` | **removal:** [`probe_rule_states_high_water_bound.sql:118`](../../scripts/db/probe_rule_states_high_water_bound.sql), `PROBE FAILED: GS-016 day two is unwritable`. **perturbation:** drop the constraint it installed and the same probe dies at [line 185](../../scripts/db/probe_rule_states_high_water_bound.sql), `an unlocked state row with hwb below balance was admitted` | **covered in BOTH directions**, the only one of the eleven that is |
| `0039` | [`assert_append_only_grants.mjs`](../../scripts/db/assert_append_only_grants.mjs), naming `economic_calendar` and `economic_calendar_loads` | **covered** |
| `0040` | `assert_append_only_grants.mjs`, naming `report_deliveries` | **covered** |
| `0041` | **NOTHING.** Dropping `contact_channels.complained_at` and its index from the fully applied schema leaves every reader exit 0 | **NOT COVERED** |
| `0043` | [`probe_audited_writes.sql:153`](../../scripts/db/probe_audited_writes.sql) and [`probe_rule_state_phase_vocabulary.sql:215`](../../scripts/db/probe_rule_state_phase_vocabulary.sql), both `column "initiative" of relation "admin_actions" does not exist` | **covered twice** |
| `0044` | **NOTHING.** Dropping the idempotency index alone, then the table and the column and the lock together, leaves every reader exit 0 | **NOT COVERED** |
| `0050` | `assert_append_only_grants.mjs`, and the `SELECT`-grant perturbation produces **two** findings on one table | **covered** |
| `0059` | [`probe_ledger_constraints.sql:612`](../../scripts/db/probe_ledger_constraints.sql), `PROBE FAILED: LEDGER-C3 admitted a reversal of a reversal` | **covered** |
| `0063` | **NOTHING.** Removal and trigger-drop both leave every reader exit 0 | **NOT COVERED** |
| `0073` | removal is unavailable, because [`0074:146`](../../packages/db/migrations/0074_firm_parameters.sql) has a foreign key `REFERENCES operators(actor)` and the set will not build. Dropping `admin_actions_actor_is_an_operator` instead: [`probe_audited_writes.sql:944`](../../scripts/db/probe_audited_writes.sql), `REJECTION 15 FAILED: an audit row named an actor in no directory` | **covered, at the constraint rather than at the file** |
| `0074` | **NOTHING.** Removal is unavailable ([`0076:162`](../../packages/db/migrations/0076_firm_parameter_write_control.sql) is an `ALTER TABLE firm_parameters`); dropping `firm_parameters_vocabulary_is_closed` leaves every reader exit 0 | **NOT COVERED** |

**FOUR OF THE ELEVEN ARE COVERED BY NOTHING THIS REPOSITORY RUNS AGAINST A DATABASE, AND TWO OF THE FOUR ARE MONEY PATH BY THEIR OWN HEADERS.** That is the finding this row exists to have produced, and it is larger than the one [ADR-335](ADR-335.md) reported: it found one uncovered file of six and called it *"the one with no probe"*.

**EACH OF THE FOUR WAS THEN WATCHED BEING LIVE, so that "not covered" is never read as "not enforced".** Every case below is an executed statement against the fully applied schema:

| Guard | Executed | Result |
|---|---|---|
| `otp_challenges_consumption_is_write_once` (`0063`) | move `consumed_at` forward on a consumed row | **refused**, `otp_challenges.consumed_at is write once` |
| `otp_challenges_consumption_is_write_once`, un-consume | set `consumed_at` back to `NULL` | **refused**, same message with `<NULL>` in the second position |
| `otp_challenges_consumption_is_write_once`, narrowness | update `attempts` on a consumed row | **accepted.** The trigger is narrow: a consumed row is not frozen, only its consumption is |
| `promotional_credit_grants_fee_back_settlement_uq` (`0044`) | two grants naming one `source_payout_request_id` | **refused**, `duplicate key value ... already exists`. That is the doubled credit the file's own header exists against |
| `plan_versions_fee_back_repeats_locked` (`0044`) | a plan version with `fee_back_repeats = true` | **refused**. With `false`, accepted |
| `firm_parameters_vocabulary_is_closed` (`0074`) | a parameter outside the vocabulary | **refused** |
| `firm_parameters_base_account_cap_is_positive` (`0074`) | `base_account_cap` at `0` | **refused**. At `3`, accepted |
| `contact_channels.complained_at` (`0041`) | nothing to execute | **there is no guard.** The column carries no `CHECK`; five constraints sit on that table and none names it. The control is entirely in the send path |

**THE PERTURBATIONS RAN UNDER `session_replication_role = replica` WHERE A FIXTURE WOULD OTHERWISE HAVE NEEDED HALF THE ESTATE.** That setting suspends triggers and foreign keys and **leaves `CHECK` constraints evaluating**, which is exactly the surface three of these guards live on. It is stated because a reader reproducing them without it will hit a foreign key first and conclude the wrong thing about what refused.

---

### 4. FINDING: the manifest's `OI` table was superseded as an ALLOCATOR and its STATE column was never superseded at all

[`DELTA_MANIFEST` section 16](../../packages/db/DELTA_MANIFEST.md) carries a banner dated 2026-08-23: **"SUPERSEDED AS AN ALLOCATOR"**, `OI` numbers are claimed in [ALLOCATION](ALLOCATION.md), *"This table allocates nothing"*, and **"The rows below stay, as the record of the items THIS MANIFEST opened"** on the distinction *"ALLOCATION carries the number and this table carries the finding"*.

**THE TABLE'S THIRD COLUMN IS HEADED `State`, AND A STATE IS NOT A FINDING.** Nine of its rows still read `Open` while [ALLOCATION](ALLOCATION.md) records them closed: `OI-01`, `OI-03`, `OI-04`, `OI-09`, `OI-11`, `OI-12`, `OI-13`, `OI-24` and `OI-27`. **BOTH SIDES WERE VERIFIED AT THE PRIMARY SOURCE BEFORE EITHER WAS CHANGED**, and none of the nine was taken on [ALLOCATION](ALLOCATION.md)'s word:

| Item | Closure verified at | Held |
|---|---|---|
| `OI-01` | [`0049_reserve_coverage_snapshots.sql:10`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql) says it closes `OI-01`; `reserve_coverage_snapshots` and `liability_snapshots` both exist on the applied schema | **YES** |
| `OI-03` | [`assert_append_only_grants.mjs`](../../scripts/db/assert_append_only_grants.mjs) exists and was run green against the applied schema, and red on three separate seeded grants | **YES** |
| `OI-04` | `pg_proc.prosecdef` on the applied schema returns `rewrite_rule_state`, `supersede_daily_mark` and `suppress_identity_link`. The row says two and the catalogue holds **three** | **YES**, and [ALLOCATION](ALLOCATION.md) already records that the row is imprecise |
| `OI-09` | `CI-06n` passes, its own note reading `0 entry link(s) outside any row` | **YES** |
| `OI-11` | `CI-06w` passes: `338 ADR number(s) over 338 claim(s)`, `82 migration number(s) over 82 claim(s)` | **YES** |
| `OI-12` | [`0048:36`](../../packages/db/migrations/0048_audited_writes_on_append_only_tables.sql) names `CALENDAR-C3` as `OI-12`'s remedy | **YES** |
| `OI-13` | [`0048:35`](../../packages/db/migrations/0048_audited_writes_on_append_only_tables.sql) names `rewrite_rule_state` as `OI-13`'s, and it is one of the three `SECURITY DEFINER` functions above | **YES** |
| `OI-24` | [`M06:65`](../plans/M06-admin-ops-console.md) carries the `m06_delta_count` generated span | **YES** |
| `OI-27` | The `OI-nn` table in [ALLOCATION](ALLOCATION.md) exists, which is what the row asked for | **YES** |

**THE REPAIR IS BOTH, AND THE ARGUMENT MATTERS MORE THAN THE EDIT.** The choice offered was to update the table or to mark it superseded more loudly. **They are not alternatives, because they fix different halves of one defect.**

**Updating alone is wrong** on the same ground the banner already states: the rows are the only copy of several findings, and a `State` cell rewritten to `Closed` with nothing beside it deletes the position the item was opened from. `RI-14`'s rule is to keep corrected text beside its correction, never to replace it.

**Marking it superseded more loudly alone is wrong** because the banner was already loud, and it was obeyed: no session has taken a number from this table since [session 141](../sessions/2026-08-23-session-141.md). **The banner's failure is not volume, it is scope.** It supersedes ONE of the table's three columns and says nothing about the other two, so a reader who correctly stops reading it as an allocator goes on reading it as a state register, which is the most decayable column of the three.

So: **the `State` column is superseded in the banner by name**, each of the nine rows keeps its original words and **gains its live disposition beside them**, and every disposition names the artifact that closed it rather than pointing at [ALLOCATION](ALLOCATION.md). A pointer to a second register is what let nine cells drift while both registers passed every gate.

**AND `OI-06 (payout destinations)` IS THE TENTH, RUNNING THE OTHER WAY.** [ALLOCATION](ALLOCATION.md) marks it **Open**; [`0051:8`](../../packages/db/migrations/0051_payout_destinations.sql) records the table landing under [ADR-169](ADR-169.md), and [`DELTA_MANIFEST` section 28](../../packages/db/DELTA_MANIFEST.md) is headed *"`0051` lands, and `OI-06` closes on one word the recommendation did not contain"*. **Both the migration and the manifest closed it and the allocator never heard.** That row is repaired in [ALLOCATION](ALLOCATION.md) with its original words kept. **The two directions in one row are the point:** a register that is not the source of a state will drift in whichever direction nobody is looking, and here it drifted in both at once.

---

### 5. FINDING: a probe comment states a number that its own migration contradicts, and it is repaired because a probe is not a migration

[`probe_wallet_debit_provenance.sql:53`](../../scripts/db/probe_wallet_debit_provenance.sql) reads *"The identity's seven scoped accounts, `trader_wallet` among them, are provisioned by the `identities_provision_ledger_accounts` trigger"*.

**IT IS THREE.** [`0054:127-131`](../../packages/db/migrations/0054_identity_ledger_accounts.sql) inserts `trader_withdrawable`, `trader_wallet` and `promotional_credit`; [`0054:163-168`](../../packages/db/migrations/0054_identity_ledger_accounts.sql)'s backfill carries the same three. The file's own comment two lines above the `INSERT` says *"The three per-identity classes of ADR-027's seven"*. **Seven is the size of the whole chart of accounts and three is the per-identity subset**, so the sentence took the count from the right ruling and the wrong set.

**THIS ONE IS REPAIRED IN PLACE, AND `0054` IS NOT.** A probe is a live check and its comments are read as instructions to the next session; a merged migration is permanent under constitution `E2` and a false sentence inside one is recorded in the manifest and never fixed in the file. [ADR-335](ADR-335.md) and [ADR-336](ADR-336.md) both hit that rule and both left the migration alone. **The corrected text is written beside the correction rather than over it** (`RI-14`), so the sentence now states three, states that it read seven, and names where seven is right.

**This is the whole of `scripts/db`'s diff on this branch**, and `git diff --stat origin/main -- packages/db/migrations scripts/db` shows that one file and zero lines under `packages/db/migrations`.

---

### 6. The reservation rows, and the measured answer is narrower than the instruction assumed

`CI-06f` reads gaplessness over the ADR files **present** plus the numbers [ALLOCATION](ALLOCATION.md) **names**, so `351` landing beside unnamed numbers below it is a hole the gate reports at numbers nobody lost. The instruction was to write a reservation row for every number from `339` to `350` not present on `origin/main`, **naming the sibling branch holding it**.

**THE BRANCH IS KNOWN FOR EXACTLY ONE OF THE TWELVE AND IS UNKNOWABLE FROM THIS SESSION FOR THE OTHER ELEVEN.** Every ref under `refs/remotes/origin/` was fetched and each was asked, by `git cat-file -e`, whether it carries `docs/decisions/ADR-<n>.md`:

| Number | Held by |
|---|---|
| `339` | `claude/the-letter-that-heads-four-sections`, session **530**, measured |
| `340` to `350` | **no ref this session can see**, out of 556 checked |

So eleven of the twelve rows name no branch, **and they say so rather than naming one**. A reservation row that invented a branch name would be worse than the hole it fills: the dispatcher resolves these by hand at merge, and a wrong name is a resolution instruction pointing somewhere real and wrong. Each row states what was measured, claims nothing, decides nothing, and says its owner amends it in place. **The wording is deliberately non-matching row to row**, on [ADR-065](ADR-065.md) T3 and on row `337`'s own reasoning: identical boilerplate across twelve rows would let `CI-06f`'s second leg pass on a reservation nobody ever replaced.

---

### 7. FINDING: a shrink-only register had to be raised, and the alternative was a landing record forbidden to say what it superseded

`CI-06/retired-constraints` went **RED** on manifest section 49, at three citations of `rule_states_high_water_bounds_balance`. `RETIRED_REGISTER` claimed `7` for that file and the file now holds `10`.

**THE REGISTER PINS ITS COUNT IN BOTH DIRECTIONS AND ITS OWN COMMENT SAYS IT SHRINKS ONLY**, so this is the gate stopping exactly what it was built to stop. It is stopping correct work, and the argument for going through it rather than around it is:

- **The three new sites are the same three kinds the seven registered ones already are**: the sentence stating what the migration dropped, the verbatim `psql` refusal on re-application, and the delta row. The entry's own `why` describes them as *"the two supersession sections"*; section 49 is a third of exactly that shape.
- **The alternative was to write the section without naming the constraint `0037` retired.** A landing record for a supersession that may not say what it superseded is not a smaller record, it is a false one, and `RI-37` requires the record. **That is weakening the record to pass a gate**, which working agreements forbid for the same reason they forbid weakening the gate.
- **What is NOT done: no permitted class is widened.** The manifest stays outside the exempted classes, so the next citation of a retired name in it is still a finding on the commit that adds it. **The raise leaves its ADR number in the entry**, so a raise arriving without a ruling is still visible as one.

**A REGISTER THAT CAN ONLY SHRINK IS A REGISTER THAT EVENTUALLY FORBIDS A CORRECT DIFF, AND THE ANSWER IS A RECORDED RAISE RATHER THAN AN EXEMPTION.** This is the first raise any entry in it has taken.

---

### 8. What `RI-37`'s register looks like now, and what it stops being

`LANDING_RECORD_BACKLOG` opened at twenty and holds **nothing**. Every `nnnn_*.sql` under `packages/db/migrations` now has a landing record in the manifest, and **leg 1 is unconditional from this commit onward**: a migration merged without a section is a finding on the commit that merges it, with no register left to absorb it.

**THE REGISTER IS KEPT AS AN EMPTY MAP RATHER THAN DELETED.** Deleting it would delete leg 3 with it, and leg 3 is what made the register decay: an entry naming a migration that already has a record is itself a finding. An empty register asserts nothing and costs one iteration of a loop; a deleted one takes the mechanism out of the tree, and the next twenty-migration backlog would arrive with nothing to register it in. **The comment above it records that it reached zero, on which rows, and that the zero is the shape it was designed to reach** rather than evidence it was never needed.

**WHAT IT DOES NOT MEAN.** `RI-37` reads no content. That every migration now has a section says nothing about whether the section is measured, and the four uncovered files in section 3 are the standing reason to care: a landing record proves a section exists at a number, and only an `E2` read proves the section is true.

---

### 9. What is owed and is not taken here

- **A probe for `0063`.** A money-path write-once trigger with no committed reader is one migration away from being silently dropped. **It is a probe block rather than a migration**, and this row's fence is `scripts/db`-zero except for the one comment repair, so writing it here would have made this diff a schema-verification row wearing a records row's title.
- **A probe for `0044`'s idempotency index.** The same shape and the higher stake: the index is the only thing between a retried settlement and a doubled credit, and the executed refusal in section 3 is this session's, not a check's.
- **`0041` and `0074`'s uncovered guards**, both smaller, both real.
- **The nine `OI` dispositions are recorded and none of the nine items is re-opened or re-ruled.** Where a row and its closure disagree in substance, as `OI-04`'s two-against-three does, the disagreement is written down and left for a reader rather than resolved by this row.

---

### 10. Approval

**Status: `proposed`. The approval line is UNSIGNED and merging is not signing.** No `status` field anywhere moves and nothing is marked `accepted`. **`E2` READ OWED**, with eleven inherited, all `proposed` and all UNSIGNED, listed at the head of this entry.

- **Approval:** `___________________________` (UNSIGNED. **Merging is not signing.**)

  **What a founder read adds and this entry cannot.**

  **THE FIRST QUESTION IS WHETHER ELEVEN SECTIONS IN ONE ROW IS ONE ROW OR THREE.** I wrote all eleven because I could measure all eleven properly from one ladder and one set of counterfactuals, and because the register reaching zero is worth more than the register reaching four. The opposite argument is real: [ADR-335](ADR-335.md) took six and [ADR-336](ADR-336.md) took three, and both were clusters with a shared subject, where mine is *"whatever was left"*. **If eleven unrelated migrations in one diff is too much surface for one `E2` read, the fence should have been the six money-path files and the other five should have waited.**

  **THE SECOND IS THE `RETIRED_REGISTER` RAISE IN SECTION 7, AND IT IS THE decision in this diff I am least entitled to make alone.** I raised a number in a register whose own comment says it shrinks only. I believe the alternative was worse and I have said why. **But the rule I applied is one I wrote in the same commit**, and a register that admits a raise whenever the raiser argues well is a register with a different property than the one it advertises. The narrower option I did not take: leave `CI-06/retired-constraints` red on this branch and let the founder decide at merge.

  **THE THIRD IS WHETHER SECTION 4's REPAIR SHOULD HAVE MOVED THE ROWS AT ALL.** The banner says the rows stay as the record of what the manifest opened. I read `State` as a column that decayed and repaired it in place beside the original words; the other reading is that the whole table is historical and the honest repair is to strike the column heading and leave every cell exactly as it was written. **That reading is defensible and I did not take it**, because a table with a struck column still shows nine cells saying `Open`.

  **WHERE THIS ENTRY IS MOST LIKELY WRONG, named rather than left to be found.** **My "NOT COVERED" claims are scoped to `scripts/db/` and that scope is a choice.** `plan_size_unlocks`, `firm_parameters`, `complained_at` and `otp_challenges` all appear in TypeScript under `packages/` and `apps/`, and I read enough of [`plan-size-unlocks.test.ts`](../../packages/db/test/plan-size-unlocks.test.ts) to establish that it asserts over DDL text rather than against a database. **I did not read every one of those sites.** If any of them does exercise one of the four guards against a real schema, the word in section 3 should be *"uncovered by the database readers"* and not *"covered by nothing"*, and the four owed probes in section 9 are worth less than I have priced them.
