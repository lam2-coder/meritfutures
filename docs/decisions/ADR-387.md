## ADR-387: the claim site ADR-384 named was one of two, `S3` measured at full population is six of thirteen rather than three of five, and the gate is refused on three measurements rather than on a cost (2026-09-06, status: proposed)

**[ADR-384](ADR-384.md) SECTION 13 ITEM 1 NAMED ONE UNREGISTERED CLAIM SITE AND SAID A NEEDLE WOULD REACH IT *AND NOTHING ELSE*. THE NEEDLE MEASUREMENT REPRODUCES EXACTLY. THE CONCLUSION DOES NOT, BECAUSE THERE ARE TWO SITES.** [`index.ts:449`](../../apps/worker/src/index.ts) states the same absence about the same job in the words `Nothing calls`, which no marker in `RI-35`'s vocabulary reached, so the sweep would have gone green over a second site one file from the first. **That is occurrence 3's shape arriving inside the register written to end it.** Both sites are registered, the needle and the verb land in the same commit, and [ADR-385](ADR-385.md)'s `S3` is measured over its full population rather than a sample.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `387`, branch `claude/the-claim-site-the-register-cannot-see`, session **582**, off the wave integration branch rather than off `main`. ADR number and session number assigned centrally and not derived.
- **MONEY-ADJACENT. ADR-003 STRICT. `E2` READ OWED.** The worker runs ledger and payout work, and the two claim sites registered here are its barrel and its job registry. Ships `proposed` with an **UNSIGNED** approval line. **Merging is not signing.**
- **THE STANDING BAR IS NOT TOUCHED.** No port was wired, no job scheduled, no sink passed, no default parameter added to `expirySweepIo`, and no cron expression, interval, batch size, concurrency, retry count, backoff or timeout was invented. [ADR-382](ADR-382.md)'s bar STANDS with its two exits unchanged, and this row is the fourth consecutive one to leave it standing.
- **NOT ONE LINE UNDER `apps/**` WAS EDITED.** The two registrations quote sentences that already stand there. `git diff --stat` names `apps/` nowhere, which is why no barrel leg needed counting and why no `apps/worker` citation could move.
- **NO MIGRATION WAS TAKEN OR RESERVED, AND NO INVARIANT WAS MINTED.** `git diff --stat origin/main -- packages/db/migrations scripts/db` is **EMPTY**, derived at reporting time. `RI-` numbers stand at **35**, unchanged.
- **EVERY FIGURE BELOW IS DERIVED ON THIS TREE.** The dispatch said its own figures were second-hand, including `:407`, the twelve `S3` sites and the three of five, and told this row to derive its own. **One of the three does not reproduce and section 5 says which.**

---

### 1. What was measured before the first edit

**THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL.** `/home/user` was empty, with no clone and no `node_modules`, and the dispatch's instruction not to prefix commands with a `cd` into the tree describes a tree that was not there. This is the **thirteenth consecutive session** to record it. The repository was attached to this session's authorized set and cloned by hand, and `HEAD` was confirmed to be the `8fa66104` the row names rather than assumed.

**Base, on a clean tree, before anything was touched: 336 files / 8,097 passed / 10 skipped / 0 failed.** Gates **33 of 33**, invariants **35 of 35**, `typecheck`, `lint` and `format:check` all clean. **THESE ARE NOT [ADR-385](ADR-385.md)'s FIGURES**, which are `336 / 8,085 / 10 / 0` measured on `0fc59a22`; rows `383`, `384` and `385` have merged into the wave branch since.

**THE CITATION CENSUS INTO THE TWO FILES THIS DIFF MOVES LINES IN WAS RUN BEFORE THE FIRST EDIT**, because it decides whether a repair is owed. **Exactly one line-numbered citation reaches either file, and it was already wrong**: [`ADR-352:30`](ADR-352.md) cites `absence-claims.mjs:822` for `event-sink-caller`. Nothing cites `absence-claims.test.ts` at all. Section 9 carries the arithmetic and the reason it is reported rather than repaired.

---

### 2. THE RULING

| # | Item | At primary source | Action |
|---|---|---|---|
| 1 | [ADR-384](ADR-384.md)'s needle measurement | **REPRODUCES.** The needle reaches 18 lines and exactly ONE window carries an absence word | **CONFIRMED.** Section 3 |
| 2 | its conclusion, *a needle would reach it and nothing else* | **A SECOND SITE EXISTS** and the vocabulary could not see it | **REGISTERED, AND THE VERB ADDED.** Sections 3 and 4 |
| 3 | should the site be registered or ruled unregisterable | registerable, `live`, against the artifact `384` already declared | **TWO CLAIMS, ONE NEEDLE, ONE MARKER, ONE COMMIT.** Section 3 |
| 4 | [ADR-385](ADR-385.md)'s `S3`, at FULL population | **14 code sites**, of which **13 are decidable and SIX are false** | **MEASURED. Section 5** |
| 5 | its *three of five*, flagged upper-biased by its own section 11 | **UPPER-BIASED AS FLAGGED**, 60 percent against 46. **All four un-repaired per-site verdicts REPRODUCE** | **CONFIRMED IN BOTH DIRECTIONS.** Section 6 |
| 6 | is there a narrower decidable shape that makes the class gateable | **NO**, and the three reasons are measured rather than priced | **REFUSED, AND THE OPEN QUESTION IS RETIRED.** Section 7 |

---

### 3. The registration, and the site the measurement could not see

**THE NEEDLE FIGURE REPRODUCES EXACTLY AND IT IS WORTH SAYING SO FIRST.** Driving the shipped `checkAbsenceClaims` over this tree with `/runReplayAudit/` added to `replay-audit-src-caller` in memory and no new claim, leg 6 reports **one finding**, at [`schedule.ts:407`](../../apps/worker/src/schedule.ts), whose `NO CALLER` is the absence word and whose needle sits one line below at [`:408`](../../apps/worker/src/schedule.ts). That is [ADR-384](ADR-384.md) section 6 confirmed at the character.

**WHAT REPRODUCES IS A STATEMENT ABOUT A NEEDLE. THE TREE WAS NEVER ASKED.** Derived over the swept scope of **398 files**:

| Measurement | Value |
|---|---|
| lines carrying `runReplayAudit` | **18** |
| of those, windows carrying an absence word under the vocabulary as `384` left it | **1**, [`schedule.ts:407`](../../apps/worker/src/schedule.ts) |
| further windows carrying an absence word the vocabulary DID NOT CARRY | **2**, [`index.ts:449`](../../apps/worker/src/index.ts) and [`schedule.ts:408`](../../apps/worker/src/schedule.ts) |

**[`index.ts:449`](../../apps/worker/src/index.ts) READS *STILL NOT SCHEDULED. Nothing calls `runReplayAudit`: there is no cron*.** It is a second claim site for the same artifact, in the same deployable, one file from the first, and no marker in the written vocabulary reaches it: `nothing ... imports` wants the verb `imports`, and `no (module|file|adapter|importer|caller|door)` does not carry `cron` or `producer`. **The measurement that would have priced the registration was itself blind to half of what it was pricing**, because a claim phrased outside the vocabulary is invisible to the sweep that measures the needle.

**THIS IS OCCURRENCE 3's SHAPE INSIDE THE REGISTER WRITTEN TO END IT.** [`absence-claims.mjs`](../../packages/tooling/checks/absence-claims.mjs)'s own header says occurrence 3 is the proof that hand repair does not scale: `ADR-326` repaired a barrel and left an identical sentence one package away, because nothing bound the second site. Here the two sentences are one file apart, about one function, and a row that registered only the named site would have shipped past the other.

**SO THE REGISTRATION IS TWO CLAIMS AND NOT ONE**, both `live` against `replay-audit-src-caller`, plus the needle and the marker in the same commit, which is exactly what [ADR-384](ADR-384.md) section 13 item 1 asked for and one entry more than it knew to ask for.

| site | anchor | why this anchor |
|---|---|---|
| [`schedule.ts`](../../apps/worker/src/schedule.ts) | `` nothing under any `src/` calls `runReplayAudit` `` | **CHARACTER FOR CHARACTER THE RUNBOOK'S OWN ANCHOR.** `384` said the registry row *states the same absence in the same words*; one substring now binds a markdown table cell and a TypeScript string literal, and a case asserts it rather than a reader believing it |
| [`index.ts`](../../apps/worker/src/index.ts) | ``STILL NOT SCHEDULED. Nothing calls `runReplayAudit` `` | the barrel, which is the file occurrence 2 lived in and which carries three of this register's retired claims already |

**BOTH ANCHORS OCCUR EXACTLY ONCE IN THEIR FILES**, which is leg 1's demand, derived at the shipped sites and asserted by a case rather than by a run. **AND THE DISPOSITION IS DEFENSIBLE IN ONE SENTENCE**: the day a `src/` caller lands, all THREE sentences are false in the same commit, and leg 2 reports three lines in three files. A row that had repaired the runbook alone would have left two sentences telling the next session this job has no caller.

**LEG 1 IS WHAT BINDS THE BARREL, NOT THE WIDENING.** A case holds that open: with the needle removed and the marker irrelevant, the barrel claim still goes red on a caller. So a later row that narrowed the vocabulary would lose the SWEEP over that phrasing and keep the BINDING, which is the failure mode worth insuring against.

---

### 4. The verb, priced

**THE VOCABULARY GAINS ONE ENTRY, `nothing[^.]{0,60}\bcalls?\b`, AND IT IS THE SECOND ADDED SINCE THE LIST WAS WRITTEN.** [ADR-330](ADR-330.md)'s was the first and was found the same way: a sweep going green over a site that asserts an absence in words nobody had written down.

**IT IS A TWIN OF AN ENTRY FOUR LINES ABOVE IT.** `nothing ... imports` was taken from occurrence 2's phrasing and reaches a sentence about an IMPORT. The estate's other way of saying a thing is unreached is to say nothing CALLS it. One was reachable and the other was not, **and the difference was a verb**.

**WHAT IT COSTS IS DERIVED AND IT IS ZERO.** Over the 398 swept files the marker reaches **25 lines**. **TWENTY-THREE carry no registered needle within the window and are therefore silent**, which is leg 6's own limit doing the work rather than a narrowing bought to keep a run green: they are true sentences about ports, screens and adapters that no artifact in this register claims. The two it adds are the two registered above.

**THE VOCABULARY'S OWN RULE ADMITS IT AND `ri35.covers` DID NOT SAY SO, WHICH IS REPAIRED HERE.** The list's header rule is *every entry is taken from a sentence that actually went false, or from one standing in the tree today*. The invariant's public description had dropped the second half, so adding an entry taken from a TRUE standing sentence would have made that description false. It now carries the rule the file states.

**AND TWO FURTHER CLAUSES OF THAT DESCRIPTION WERE FALSE BEFORE THIS DIFF AND ARE REPAIRED WITH IT**, both about the mechanism this registration depends on:

1. **It said the check IS LINE SCOPED and that a claim whose needle and whose absence word land on different lines is MISSED.** `SWEEP_WINDOW` is **2** and its docblock is the measurement: occurrence 3 wraps exactly that way, and so does this row's own registration, where the absence word is at `:407` and the needle at `:408`. **The sentence denied the mechanism that makes this entry possible.**
2. **It carried a count of needle-less artifacts, *two*.** Derived at reporting time: **17** of 20 artifacts register no needle. The figure is **DELETED rather than corrected**, on [ADR-034](ADR-034.md)'s own second branch, because the register below is the source and a figure retyped beside it is the drift that entry exists to end.

Both retired clauses are **NAMED rather than re-quoted**, per [ADR-367](ADR-367.md) section 4, whose seed watched a quoting retirement hand a prose-matching case two answers.

---

### 5. `S3`, measured over its full population

**[ADR-385](ADR-385.md) SECTION 5 DEFINED `S3` AS `S1` NARROWED TO THE MECHANICALLY DECIDABLE, PRINTED THE NOUN LIST WITH AN ELLIPSIS, AND GAVE THE POPULATION AS TWELVE.** The shape it used is not recoverable from the entry, so **this row states its own and derives everything from it**:

```
S1  /\bthe only\b[^.]{0,80}\bin (?:this|the) (?:repository|repo|tree|workspace|corpus)\b/i
S3  /\bthe only (?:file|module|copy|one|place|thing|site)\b[^.]{0,80}\bin
      (?:this|the) (?:repository|repo|tree|workspace|corpus)\b/i
```

Line scoped, over every git-tracked non-binary file, split at `docs/`. Derived on `8fa66104` before the first edit: **`S1` 53 code sites and 196 documentary; `S3` 14 code sites and 42 documentary.**

**AND THIS ENTRY IS A MEMBER OF WHAT IT MEASURES, WHICH IS SAID HERE RATHER THAN DODGED.** Re-derived at reporting time the documentary figures are **197** and **43**, and the one added line is this row's own generated registry row in [`docs/sessions/README.md`](../sessions/README.md), which quotes the summary below. This file itself carries **15 `S1` lines and 13 `S3` lines**, 14 and 13 of them in the table below, all of which join the documentary population the moment it is tracked. **The code figures do not move and they are the ones every verdict rests on.** Site 1 below is false precisely because a sentence counted its own file out, so an entry measuring that family and quietly excluding itself would be committing the defect in the document that names it.

**THE SHAPE WAS VALIDATED AGAINST [ADR-385](ADR-385.md)'s OWN TREE RATHER THAN AGAINST ITS PROSE**, by running it over `0fc59a22` through `git ls-tree` and `git show` without checking anything out. It yields **`S3` 14 code sites there too**, and it resolves four of that entry's five sites at their own cited line numbers, `repo-invariants.mjs:4562` included. **The fifth it does not reach**: on `0fc59a22` [`index.ts`](../../apps/worker/src/index.ts) read *the only producer in this repository*, and `producer` is not in the noun list that entry printed. **So one of the five sites `385` says it drew from `S3` does not satisfy `S3` as `S3` is written there.** That is an under-specification rather than an error, and it is the first evidence for section 7: the family is a grep for phrasing, and the phrasing list is not stable inside the entry that defined it.

**ALL FOURTEEN WERE OPENED AT PRIMARY SOURCE. THIRTEEN ARE DECIDABLE AND SIX ARE FALSE.**

| # | site | what it claims | at primary source | verdict |
|---|---|---|---|---|
| 1 | [`apps/admin/package.json:17`](../../apps/admin/package.json) | `e2e/pass.ts:17` is the only file in this repository importing `@playwright/test` | [`playwright.config.ts:53`](../../playwright.config.ts) imports it | **FALSE** |
| 2 | [`apps/admin/test/page.test.ts:246`](../../apps/admin/test/page.test.ts) | the only copy of the old uuid regex in the tree | one copy, and it is this line | **TRUE** |
| 3 | [`rule-state-producibility.test.ts:155`](../../apps/api/test/rule-state-producibility.test.ts) | the writer is the only site in the tree inserting a `rule_states` row | **SIX files under `scripts/db` carry an uncommented `INSERT INTO rule_states`**, and [`0065:67`](../../packages/db/migrations/0065_rule_state_lifetime_and_breach.sql) says so in the corpus already | **FALSE** |
| 4 | [`start-program.test.ts:37`](../../apps/api/test/start-program.test.ts) | two cases are the only thing in the repository holding what they hold | [`start-construction.test.ts:17`](../../apps/api/test/start-construction.test.ts) names them as the guard and disclaims holding it | **TRUE** |
| 5 | [`apps/worker/src/index.ts:594`](../../apps/worker/src/index.ts) | the only event producer in this repository is `apps/api/src/events.ts` | `EVENT_CATALOGUE`, `buildEvent`, `makeEventSink` and `TRANSACTION_EVENT_WRITER` are declared there and nowhere | **TRUE** |
| 6 | [`packages/db/src/client.ts:4`](../../packages/db/src/client.ts) | the only file in the workspace that imports `pg` | [`scoped-db.ts:75`](../../packages/db/src/scoped-db.ts) imports it, and so do **seven** files under `packages/db/test` | **FALSE** |
| 7 | [`scoped-db.ts:4218`](../../packages/db/src/scoped-db.ts) | `client.ts` remains the only file in the workspace importing it **at runtime** | [`date-column-timezone.test.ts:55`](../../packages/db/test/date-column-timezone.test.ts) does `import pg from 'pg'` and calls `pg.types.getTypeParser` six times | **FALSE, AND NEW** |
| 8 | [`chart-of-accounts-kinds.test.ts:133`](../../packages/ledger/test/chart-of-accounts-kinds.test.ts) | the only place in this repository the ruled map is typed | no second TypeScript statement of it; the other is a SQL `CHECK` | **TRUE** |
| 9 | [`pg-boss-queue.ts:4`](../../packages/queue/src/pg-boss-queue.ts) | the only file in the workspace that NAMES `pg-boss` | **37 git-tracked files outside `docs/` name it, and 67 inside it. One file imports it** | **FALSE** |
| 10 | [`surface.test.ts:113`](../../packages/queue/test/surface.test.ts) | the only thing in the repository that would report a second importer | nothing else scans for `from 'pg-boss'` | **TRUE** |
| 11 | [`lt-07.test.ts:125`](../../packages/rail/test/lt-07.test.ts) | two adjacent lines are the only place in this repository a sign is written | true of shipped source, where [`posting.ts:236`](../../packages/ledger/src/posting.ts) is alone. **Four test files write a literal negative `amountCents`** | **FALSE** |
| 12 | [`repo-invariants.mjs:4611`](../../packages/tooling/checks/repo-invariants.mjs) | `.gitignore` is the only one in this repository | `find` returns exactly `./.gitignore` | **TRUE** |
| 13 | [`strip-comments.test.ts:20`](../../packages/tooling/test/strip-comments.test.ts) | the only copy of the naive spelling left in the repository | the guarded spelling occurs once | **TRUE** |
| 14 | [`falsify.mjs:1215`](../../scripts/corpus/falsify.mjs) | the only place in this repository where a literal token is UNAVOIDABLE | a claim about NECESSITY. No probe over a tree reports what is avoidable | **NOT DECIDABLE** |

**THE THREE FALSE ONES THAT ARE NEW ARE 3, 7 AND 11**, and 1 is [ADR-385](ADR-385.md) section 9 item 3 confirmed at primary source rather than carried.

---

### 6. The rate, and what confirming a flagged number looks like

**[ADR-385](ADR-385.md) SECTION 11 SAID ITS *THREE OF FIVE* WAS UPPER-BIASED AND SHOULD NOT BE CARRIED FORWARD AS A RATE. IT WAS RIGHT, AND THE BIAS IS SMALLER THAN THE WARNING IMPLIES.**

| | false | decided | rate |
|---|---|---|---|
| [ADR-385](ADR-385.md), five opened, one handed to it as suspicious | 3 | 5 | **60 percent** |
| this row, full population | **6** | **13** | **46 percent** |

**AND EVERY ONE OF ITS PER-SITE VERDICTS REPRODUCES.** Of the five it opened, one ([`playwright.config.ts`](../../playwright.config.ts)) it repaired, so it leaves `S3` on this tree. The other four were re-derived here from scratch: `client.ts` FALSE, `pg-boss-queue.ts` FALSE, `index.ts` TRUE, `.gitignore` TRUE. **Four for four.** The entry's judgement was sound; only its denominator was small.

**THE MOST INTERESTING RESULT IS AT THE ONE SITE `385` HELD UP AS TRUE.** Its section 5 closing paragraph names [`scoped-db.ts:4218`](../../packages/db/src/scoped-db.ts) as the exemplar: *the truer narrower claim already existed in the corpus*, surviving *because that sentence carries "at runtime" and the lint rule behind it*. **That narrower claim is also false**, and the lint rule does not cover it: [`eslint.config.js:41`](../../eslint.config.js) scopes `merit/no-raw-db-client` to `apps/**/*.ts` and `packages/**/*.ts` and [`:42`](../../eslint.config.js) ignores **`packages/db/**`**, which is the package both sentences live in and the package where a test imports `pg` and calls it. **The mechanism that was credited with holding the sentence up is switched off in exactly the directory the sentence is about.**

---

### 7. Is there a narrower decidable shape? NO, and here are the three measurements

**[ADR-385](ADR-385.md) SECTION 7 REFUSED A GATE ON A COST ARGUMENT AND ITS SECTION 11 SAID THE REFUSAL RESTS ON *no narrower decidable shape existing, which is NOT measured*. IT IS MEASURED NOW, THE ANSWER IS THE SAME, AND THE GROUND IS DIFFERENT.**

**ONE. `S3` IS A GREP FOR PHRASING AND NOT A NARROWING TO THE DECIDABLE.** Of the fourteen members, **four carry a predicate no runner can evaluate**: sites 4, 10 and 14 above, plus 8 on any reading of *typed* a machine has to supply. Sites 4 and 10 assert that no OTHER assertion in the corpus holds the same proposition, which is a claim about the meaning of every test in the tree; site 14 asserts a NECESSITY. **A subfamily selected for decidability that is 29 percent undecidable was selected for its words.** [ADR-385](ADR-385.md)'s own sample crossing its own noun list, section 5 above, is the same finding from the other end.

**TWO. FOR THE TEN THAT ARE DECIDABLE, THE PREDICATE IS PER SITE, SO THE GATE IS A HAND-WRITTEN PROBE PER SITE.** Site 1 needs *who imports a package*; site 3 needs *what executes an `INSERT`*; site 7 needs a VALUE import told from a TYPE import, which is an AST and not a grep; site 9 needs *who names a string*; site 11 needs a definition of *a sign is written*. There is no common predicate. **A check with one hand-written probe per registered sentence is `RI-35`, it already exists, and its header already rules how it may grow**: the register is written and never computed, because a register a session can regenerate is a register nobody decided. **So the decidable shape was never missing. What is missing is a decision per sentence, and that cost is paid by a reader, once, at the sentence.**

**THREE, AND THIS IS THE ONE THAT CHANGES SOMETHING. [ADR-385](ADR-385.md)'s OWN REPLACEMENT RULE DOES NOT DISCRIMINATE.** That entry offered, in place of a gate, a rule a reviewer can apply: *an absolute in prose either NAMES the mechanism that asserts it, or is written as the narrower claim that is true*. **Three of the six false sites name a mechanism.**

| site | the mechanism it names | why the sentence is still false |
|---|---|---|
| [`client.ts:4`](../../packages/db/src/client.ts) | *the ESLint rule `merit/no-raw-db-client` makes that mechanical rather than a convention* | the rule ignores `packages/db/**` |
| [`scoped-db.ts:4218`](../../packages/db/src/scoped-db.ts) | *the suite watches this function returning a pool* | the suite watches a POOL, not the import census |
| [`pg-boss-queue.ts:4`](../../packages/queue/src/pg-boss-queue.ts) | *`test/surface.test.ts` asserts that it does, over the whole tree* | the case asserts IMPORTS and the sentence says NAMES |

**THE PATTERN IS ONE STEP FINER THAN THE RULE.** In all three the mechanism is real, is green, and asserts something NARROWER than the sentence beside it. **Naming a mechanism is cheap; matching its scope to the sentence is the whole of the work.** So the rule collapses either way: read cheaply it passes three false sites, and read strictly (does the named mechanism assert THIS proposition at THIS scope) it costs exactly what opening the file costs, which is what every finding in section 5 came from. **This is offered as a refutation by counterexample and never as a rate.** Thirteen decided sites cannot support a rate claim, and pretending otherwise would be this row committing the error it was sent to measure.

**SO NO GATE IS BUILT, NO INVARIANT IS MINTED AND NO `RI-` NUMBER IS RESERVED. [ADR-385](ADR-385.md) SECTION 7's REFUSAL STANDS ON MEASURED GROUND, AND ITS SECTION 11 ALTERNATIVE, THE `S3` GATE PRICED AT TWELVE SITES, IS REFUSED WITH ITS PRICE DERIVED.** A gate over the shape would land against 14 sites of which **7 carry true sentences and 1 cannot be decided at all**, so 8 of its 14 findings would demand edits to prose that is correct or unknowable. That is the backlog `385` predicted, at a ratio its own numbers could not yet supply.

**WHAT IS RETIRED AND WHAT IS NOT.** The question *is there a narrower decidable shape* is **retired**: the answer is no, on three measurements. The question of whether the founder accepts a fourth consecutive measured refusal is **not this row's** and is section 12's first item.

---

### 8. What is bound mechanically

**UNLIKE [ADR-385](ADR-385.md), THIS ROW BINDS SOMETHING, AND THE DIFFERENCE IS THAT ITS OUTPUT IS AN ARTIFACT AND NOT A CORRECTION.** That entry's output was six prose repairs, and `ADR-324`'s rule as quoted by `RI-35` is that a test pinning prose goes stale in silence. **Two register entries, a needle and a vocabulary entry are values, and a value is bindable.**

**EIGHT CASES LAND IN [`absence-claims.test.ts`](../../packages/tooling/test/absence-claims.test.ts) AND EACH IS A RED OR A PAIR:**

1. three sites are registered `live` against the caller probe, one runbook and two in the deployable;
2. the schedule anchor is the runbook's own substring, character for character;
3. each anchor occurs exactly once in the file it names, leg 1's demand read at the shipped sites;
4. **RED:** one `src/` caller falsifies all three sentences, and the case asserts three findings at three named sites rather than a length;
5. **GREEN:** the counterfactual, the same three sentences with no caller;
6. **RED by discovery:** a FOURTH site naming the entry point with an absence word is reported, which is what the needle buys;
7. **RED and GREEN together:** the new marker reaches `Nothing calls`, and a phrasing outside the vocabulary is still silent, so a later row that widened the vocabulary to match everything fails here;
8. **RED:** leg 1 binds the barrel site with the needle removed, so the binding does not rest on the widening.

**ONE EXISTING CASE WAS AMENDED AND THE REASON IS THE FINDING RATHER THAN THE REPAIR.** [ADR-384](ADR-384.md)'s case *a comment naming the call shape is not a caller* seeded, into a temporary tree, **the exact shape of the second site**, and asserted the whole check EMPTY. It passed because the vocabulary carried no marker reaching `Nothing calls`. **The fixture had written the unregistered sentence down and the suite went green over it**, which is section 3's finding arriving inside `384`'s own test. The case now asserts **at the probe**, which is what it was always about and is stronger than an emptiness two mechanisms had to agree on, and it states what the sweep reports rather than folding it into a zero. **No assertion was deleted, weakened or skipped**, and the case's original subject is asserted more directly than before.

---

### 9. What moved, and the one pointer that did not become repairable

**NOT ONE LINE UNDER `apps/**` CHANGED**, so no `apps/worker` citation could go stale and no barrel leg needed counting. Lines moved in exactly two files, [`absence-claims.mjs`](../../packages/tooling/checks/absence-claims.mjs) and its suite.

**EXACTLY ONE LINE-NUMBERED CITATION REACHES EITHER FILE AND IT WAS ALREADY WRONG BEFORE THIS DIFF EXISTED.** [`ADR-352:30`](ADR-352.md) cites `absence-claims.mjs:822` for `event-sink-caller`. Derived at three points:

| tree | `:822` holds | `key: 'event-sink-caller'` at | short by |
|---|---|---|---|
| `0fc59a22`, per [ADR-384](ADR-384.md) section 10 | the last line of the `gates-importable` probe | `:852` | 30 |
| `8fa66104`, this row's base, derived here | `names:` | `:875` | **53** |
| after this diff | `body,` | `:902` | **80** |

**IT IS NOT REPAIRED, AND THE DISPATCH'S OWN EXCEPTION IS WHY.** That exception covers a citation **this diff makes stale**. This one was thirty lines short of its subject on the day it was written, so nothing this diff does can make it stale; it can only make it further wrong. **[ADR-384](ADR-384.md) section 14 item 3 records the governing question as OPEN** (may a citation into a document that has grown by insertion be corrected at all?), and `ADR-352` is a dated record. **A row that quietly repointed it would be answering a question three entries have left to a founder.** The new offset is written down so a fourth row does not re-derive it and conclude the pointer moved.

**EVERY CITATION THIS DIFF WRITES WAS OPENED AND READ AFTER THE EDIT** rather than inferred from a green invariant run, which is [ADR-380](ADR-380.md)'s warning followed rather than quoted. The four `apps/worker` pointers in the register prose (`schedule.ts:407`, `:408`, `index.ts:449`, `batch/replay.ts:608`) were each read at the line.

---

### 10. Owed outside this fence, stated precisely enough to be picked up

1. **[`apps/admin/package.json:17`](../../apps/admin/package.json) carries the false clause `385` repaired one file over.** Section 5 site 1, confirmed at primary source. **This is the FIFTH consecutive statement of it**, after [ADR-385](ADR-385.md) section 9 item 3. `apps/admin/**` is a concurrent row's fence this wave.
2. **[`packages/db/src/client.ts:4`](../../packages/db/src/client.ts) AND [`scoped-db.ts:4218`](../../packages/db/src/scoped-db.ts) ARE BOTH FALSE, WHICH IS ONE MORE SITE THAN [ADR-385](ADR-385.md) OWED.** Its item 1 offers the repair *add the two words `at runtime`*. **That repair does not work**, because the sentence already carrying those words is false at [`date-column-timezone.test.ts:55`](../../packages/db/test/date-column-timezone.test.ts). The scope that is true is `packages/db/src`, which is also the scope the lint rule does not cover. `packages/db/**` is outside this fence.
3. **[`rule-state-producibility.test.ts:155`](../../apps/api/test/rule-state-producibility.test.ts) says *in the tree* where its own file means the deployable scope.** Six `scripts/db` probes insert the row and [`0065:67`](../../packages/db/migrations/0065_rule_state_lifetime_and_breach.sql) already records that they do. `apps/api/**` is a concurrent row's fence this wave.
4. **[`lt-07.test.ts:125`](../../packages/rail/test/lt-07.test.ts) says *in this repository* where the case two lines down scopes itself to five files under `packages/rail/src`.** `packages/rail/**` is outside this fence.
5. **[`pg-boss-queue.ts:4`](../../packages/queue/src/pg-boss-queue.ts) is unrepaired**, [ADR-385](ADR-385.md) item 2, re-derived here at 37 git-tracked non-documentary naming files against one importer. `packages/queue/**` is outside this fence.
6. **`ri35`'s exported signature and its call site at [`repo-invariants.mjs:92`](../../packages/tooling/checks/repo-invariants.mjs) NEED NO CHANGE**, derived rather than assumed: this row added claims, a needle and a marker, all inside the module, and the `CHECKS` literal imports the same object. **Nothing is owed to row `386` and that file was read and never opened for writing.**
7. **`docs/sessions/README.md:141` is still a malformed table row** and is row `388`'s. Confirmed unmoved: this row appends its entry through `gates.mjs generate`, which writes only inside the `session_entries` span.
8. **Nothing is owed inside `docs/ops/runbooks/**`.** [`CRON_INVENTORY.md`](../ops/runbooks/CRON_INVENTORY.md) was READ, its registered anchor confirmed present exactly once, and the file was never opened for writing.

---

### 11. Verification

Every figure derived at reporting time off each runner's own last line, stated in the [session log](../sessions/2026-09-06-session-582.md).

- **Suite: 336 files / 8,105 passed / 10 skipped / 0 failed**, against a base of **336 / 8,097 / 10 / 0** reproduced on `8fa66104` **BEFORE the first edit**. **Delta: +8 passed, +8 total, 0 files, 0 skipped, 0 failed.** **Attributed by removal**: [`absence-claims.test.ts`](../../packages/tooling/test/absence-claims.test.ts) carried **59** cases on the base and carries **67** now, so all eight are the cases section 8 lists and no case elsewhere moved, was renamed or was removed.
- **Register sizes, derived:** artifacts **20**, unchanged. Claims **31 to 33**. Vocabulary entries **15 to 16**. Artifacts registering a needle **2 to 3**.
- Gates **33 of 33** after `generate`. Invariants **35 of 35**. `typecheck`, `lint` and `format:check` clean.
- **`pnpm run verify` and `scripts/corpus/falsify.mjs` were NOT run**, both forbidden by this row.
- **AND THE SHIPPED REGISTER WAS WATCHED GOING RED ON THIS TREE, not only in a temporary one.** A single untracked caller was written under `apps/worker/src`, `ri35.run` was driven over the repository root, and it reported **THREE findings**, one at each registered site, naming `CRON_INVENTORY.md`, `schedule.ts` and `index.ts`. **That is the whole claim of section 3 observed rather than argued**: one caller, three sentences, and before this row it would have been one. The seed was removed and the three claim-site files plus the checker were verified **byte-identical with `sha256sum -c`**, never with `git checkout --`, and `git status --porcelain` was confirmed to carry only this row's own files.
- **THE SEEDS ARE THE CASES AND THEY ARE RED-AND-GREEN PAIRS**, not a green run. Section 8 item 4 watches three sentences go red on one seeded caller; item 6 watches leg 6 report a fourth site that does not exist on this tree; item 7 watches the marker fire and watches an out-of-vocabulary phrasing stay silent. **Every seed is a temporary tree under `mkdtemp` removed in `afterEach`, no tracked path was written by a test, and `git status --porcelain` was confirmed clean before the gates were run.**
- **No pull request was opened and nothing was merged, by instruction.**

---

### 12. Where this entry is most likely wrong

**SIX OF THIRTEEN IS A COUNT AND NOT A RATE EITHER, AND THIS ROW WILL NOT PRETEND OTHERWISE.** Thirteen decidable sites is a full population of `S3` and a tiny sample of the class. `S1` stands at **53 code sites** and **196 documentary**, and this row opened **none** of the 39 `S1` sites outside `S3`. **So the 46 percent is the rate for one phrasing family and says nothing about the class**, which is [ADR-385](ADR-385.md) section 11's own warning inherited whole rather than dissolved. A claim written in a shape neither `S1` nor `S3` matches is invisible here as it was there.

**AND THE POPULATION DEPENDS ON A REGEX THIS ROW WROTE.** Section 5 states it in full for exactly this reason, and validates it against `0fc59a22` where it resolves four of `385`'s five cited sites at their own line numbers. **It does not reach the fifth**, and a shape that misses one site the entry it is reconstructing counted may miss others. Widening the noun list to all of `S1` is one edit and 39 more files to open; **that is the cheapest way to overturn section 7's first measurement** and it is named here rather than left implicit.

**THREE OF THE SIX FALSE VERDICTS TURN ON WHAT COUNTS AS *THE TREE*, AND A READER MAY DISAGREE WITH ALL THREE.** Site 3 counts committed `scripts/db` probes, site 11 counts test fixtures, site 6 counts a type-only import. Each is defensible and none is forced: a reader who holds that *the tree* means shipped source alone gets **three of thirteen**, 23 percent, and section 7's conclusion is unchanged because it rests on decidability and on the mechanism-naming counterexamples rather than on the rate. **The rate is the softest number in this entry and the one a founder should trust least.**

**THE VOCABULARY WIDENING IS THE ONE CHANGE THAT COULD COST SOMEBODY LATER.** It costs zero registrations today, derived. But `nothing ... calls` reaches 25 lines already, and the day an artifact registers a needle near any of the other 23, that sentence has to be registered or the needle tightened. **This row bought a control with a bill that arrives on somebody else's diff**, which is the same shape `ADR-328` recorded when it shipped leg 6, and it is stated rather than left to be discovered.

**AND THE SECOND SITE MIGHT NOT BE THE LAST ONE.** Section 3's whole finding is that a needle measurement cannot see a claim phrased outside the vocabulary. **That argument does not stop applying now that the vocabulary has one more entry.** A third site for this artifact, phrased in a verb nobody has written down, would be as invisible today as `index.ts:449` was yesterday, and the only thing that would find it is a reader opening the file. **This entry has no measurement for that and does not have one to offer.**

---

### 13. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** Inherited as well as owed: [ADR-357](ADR-357.md) and [ADR-360](ADR-360.md) through [ADR-385](ADR-385.md) are `proposed` and unsigned, and this row settles none of them. **`386` and `388` are RESERVED and not written**, derived by listing `docs/decisions/` at reporting time: two siblings hold them on this wave and neither number is claimed here, and neither reservation carries a citation to a figure it does not own.

  **What a founder read adds and this entry cannot.**

  **THE FIRST THING TO DECIDE IS WHETHER A FOURTH CONSECUTIVE MEASURED REFUSAL IS DISCIPLINE.** [ADR-377](ADR-377.md) refused a gate for the citation form, [ADR-380](ADR-380.md) refused a proximity remedy, [ADR-385](ADR-385.md) refused one for absolutes in prose, and this row refuses the `S3` gate that entry offered as the alternative to its own refusal. **`385`'s approval block already asked this question at three; the honest addition here is that the fourth refusal is the one that closed the escape hatch the third one left open.** There is no measured proposal on the table any more, and a founder who wants a control built has to say so, because no row is going to arrive at one from a measurement.

  **THE SECOND IS THAT THE MECHANISM-NAMING RULE SHOULD PROBABLY BE INVERTED.** Section 7's three counterexamples all name a real, green mechanism whose scope is narrower than the sentence beside it. **The reader instruction that would have caught all three is not *name the mechanism* but *write the sentence at the mechanism's scope***, which is a different discipline: it says do not widen a claim past the glob, the file list or the case that holds it. This row does not rule that, because it is a corpus convention and conventions are the founder's.

  **THE THIRD IS THE PART OF THIS ROW THAT WAS LUCK.** [ADR-384](ADR-384.md) did everything right. It measured its needle, stated the measurement, declined to ship it into another row's fence, and wrote the owed item down precisely enough to be picked up. **The second site was found only because this row ran the marker measurement it had no instruction to run**, and that measurement was one line of curiosity, not a control. **A process that depends on the next session being curious is not a process**, and whether that is worth a rule is a founder's call rather than a measurement anybody can take.

- **Approval:** `___________________________` (UNSIGNED. **MONEY-ADJACENT, `E2` READ OWED.** **Merging is not signing.**)

| | |
|---|---|
| **Approved by** | |
| **Date** | |
