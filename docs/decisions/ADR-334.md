## ADR-334: two merged money-schema migrations had no landing record and four rows in a row reported the absence rather than repairing it. `RI-37` binds every migration on disk to a landing record in the delta manifest, the twenty other gaps are a shrink-only register rather than twenty sections written in one diff, and `0080` and `0081` get sections 38 and 39 measured against PostgreSQL 16.13 rather than reconstructed from their ADRs (2026-09-05, status: proposed)

**`packages/db/DELTA_MANIFEST.md` is where a merged migration's execution lives.** A landing section carries the install from an empty database, the object counts read from `pg_tables`, `pg_indexes`, `pg_constraint` and `pg_trigger`, the probe transcript and the counterfactual that probe was watched failing on. Constitution `E2` makes a merged migration permanent, so that section is the only place the reasoning behind a permanent statement is written down at the migration's own number.

**[`0080`](../../packages/db/migrations/0080_wallet_debit_provenance.sql) ([ADR-322](ADR-322.md)) AND [`0081`](../../packages/db/migrations/0081_purchase_processor_columns.sql) ([ADR-323](ADR-323.md)) HAD NONE, AND THE ABSENCE HAS FOUR RECORDED OCCURRENCES.** [ADR-323](ADR-323.md) landmine 1 named it, [ADR-327](ADR-327.md) carried it forward, [ADR-330](ADR-330.md) carried it forward again, and [ADR-329](ADR-329.md) section 9 finding 5 was still writing *"`0080` and `0081` still carry no `DELTA_MANIFEST` section"* four rows later. **Every one of those is a session writing a sentence because nothing in this repository could write a failure.** Section 37's own row in the manifest's section-number table said the same thing in the same words and moved on.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `334`, branch `claude/two-migrations-with-no-record`, session **525**. `RI-37` taken in the same commit as the check.
- **NOT THE MONEY PATH BY FILE, AND IT IS THE RECORD OF TWO MONEY-SCHEMA MIGRATIONS.** `git diff --stat 9641a972..HEAD -- packages/db/migrations scripts/db` is **EMPTY**, measured at the moment this line was written. No SQL, no migration number, no `SD-nn`, no route behaviour. **`0080` and `0081` ARE NOT EDITED**, which is constitution `E2`: migrations are sacred, superseded by addition and never edited.
- **AN `E2` READ IS OWED ANYWAY**, and two are inherited, both `proposed` and both UNSIGNED: [ADR-322](ADR-322.md) and [ADR-323](ADR-323.md). This diff writes down what two money-schema migrations did to `wallet_entries` and `purchases`, and a reader trusting a section that was reconstructed rather than measured would be trusting a copy.
- **THE BASE IS [PR #687](https://github.com/lam2-coder/meritfutures/pull/687)'s HEAD, `9641a972`, DELIBERATELY.** It carries `main` plus [ADR-333](ADR-333.md). Re-derived before a line was written and not taken from the dispatch: `git ls-tree origin/main --name-only docs/decisions/ | tail` ends at `ADR-332.md`, so `main` carries through [ADR-332](ADR-332.md) and `333` is present beneath this branch. **`334` opens no `CI-06f` hole.**
- **A DISPATCH PROMPT IS NOT A SOURCE.** Every figure below is a `grep`, a `psql` run or a runner's own last line. **Two of the dispatch's claims are amended rather than transcribed** (findings 1 and 6) and **two figures in [ADR-322](ADR-322.md) and [ADR-323](ADR-323.md) disagree with what this tree measures** (section 5.3).

---

### 1. What was re-derived before the first edit, and what the baseline was

**EVERY ROW BELOW WAS RUN ON `9641a972` BEFORE THE FIRST EDIT.**

| Claim | Read at | Held |
|---|---|---|
| `main` carries through [ADR-332](ADR-332.md) and this branch adds `333` | `git ls-tree origin/main --name-only docs/decisions/` against `ls docs/decisions/` | **YES.** No `CI-06f` hole |
| There are 34 invariants and 33 gates on this ref | Both runners' own last lines, read separately | **YES.** `34 of 34` with `RI-36` as the last PASS, and `33 of 33` |
| `RI-36` is the highest live invariant and `RI-37` is free | The live `CHECKS` array, plus [ADR-297](ADR-297.md) and [ADR-304](ADR-304.md), which each RESERVED `RI-37` by name and each LEFT IT FREE in writing | **YES** |
| The `CI-06<letter>` series is closed at `w` | `CLOSED_LETTER_SERIES = 'abcdefghijklmnopqrstuvw'` in [`gates.mjs`](../../scripts/corpus/gates.mjs), enforced by `CI-06/closed-letter-series` over three registries ([ADR-131](ADR-131.md)) | **YES.** A new gate takes a SLUG |
| The manifest's landing sections run `## 35.` `0079`, `## 36.` `0078`, `## 37.` `0082`, and there is no `0080` or `0081` section | `grep -n '^## [0-9]' packages/db/DELTA_MANIFEST.md` | **YES**, verbatim |
| `0080` appears once in the manifest and `0081` twice | `grep -o '\b0080\b'` and `\b0081\b`, counted | **YES.** One and two, and all three are incidental mentions inside other sections' prose |
| No gate or invariant requires a migration to have a landing record | A read of every `CI-06` and `RI-nn` body naming `packages/db/migrations` or `DELTA_MANIFEST` | **YES.** Section 2 finding 4 |
| Section 16's section-number table needs rows | The table, whose maximum is `37` | **YES**, and its `37` row says `0080` and `0081` have no section |
| PostgreSQL 16 is available for the measurement | `psql --version` -> **16.13**, `pg_isready` accepting | **YES** |

**Baseline, reproduced on `9641a972` BEFORE the first edit and quoted off each runner's own last line:** `pnpm vitest run` **311 files, 7,585 passed, 10 skipped, 0 failed**, exit 0; invariants **34 of 34**; gates **33 of 33**; `git status --porcelain` empty. **Skips are reported separately from passes throughout, because a skip is not a pass.**

---

### 2. The findings, re-derived at source, and the two that amend the dispatch

| # | Claim | Verified |
|---|---|---|
| 1 | **THE DISPATCH SAYS 76 MIGRATIONS AND THERE ARE 75** | `ls packages/db/migrations/ \| grep -c '\.sql$'` returns **75**, and `ls` shows no non-`.sql` file in the directory. The gaps are `0058`, `0060` to `0062`, `0069`, `0071` and `0077`, each a reserved-unwritten row of [ALLOCATION](ALLOCATION.md)'s migration table. **A reservation is not a landing and `RI-37` says nothing about one.** Amended rather than transcribed |
| 2 | **The gap is real and it is exactly the two named** | The shipped check, run on the tree as it stood before the repair, reports exactly two findings: `0080` and `0081`. Section 4 |
| 3 | **`0080` and `0081` are on disk, merged, and money schema** | Both files exist, both open with an `E2 READ: MONEY PATH` header, and `git diff --stat 9641a972..HEAD -- packages/db/migrations` is EMPTY so neither is touched by this row |
| 4 | **NOTHING IN THIS REPOSITORY BOUND A MIGRATION TO A RECORD** | `CI-06i` reconciles the design-record TABLE set against the migrations and stops at the name. `data-model-columns.mjs` does the COLUMN set and is unregistered. `RI-26` and `RI-36` fold the DDL. `ADR-026`'s gate binds every `SD-nn` and `U-nn` cited in `docs/` to exactly one manifest ROW, which is a delta identifier and not a migration number. **No check reads the manifest's section structure at all**, which the manifest's own section 16 states in writing under the heading *"What no gate checks, stated rather than implied"* |
| 5 | **The manifest names its own remedy and named it in 2026-08-16** | Section 16: *"The cheap version of the gate is the one to write, and it is two assertions"*, and *"That is `ADR-026`'s manifest-completeness check with a different identifier prefix, and it would have caught both collisions on the day they were written. **It is not written here**, because a gate arrives with a seeded violation it has been watched failing on, and this session's stop condition is `0033`."* **Written on 2026-08-16, four hundred sessions ago, and correct on both counts** |
| 6 | **THE SWEEP FINDS TWENTY MORE GAPS, NOT THREE, AND SIXTEEN OF THEM ARE MONEY PATH** | Section 6. The dispatch says to STOP AND REPORT past about three, and this entry does: the twenty are **registered and reported and NOT written**, because twenty sections reconstructed from twenty ADRs in one diff is the one thing a landing section may not be |

**FINDING 6 IS THE ONE THAT SHAPED THE CHECK.** A check that was simply RED over twenty-two migrations could not land, because the stop condition and CI both require it green; and a check whose twenty-two findings were silenced by an exemption list would be the loosening the row forbids. **The answer is neither**: a shrink-only register on `CI-06/gate-inventory`'s precedent, which absorbs exactly the twenty that exist today, names each one, and turns any twenty-first into a finding on the commit that adds it.

---

### 3. THE RULING, part one: where the check lives, and why it is an invariant

**THERE ARE 34 INVARIANTS AND 33 GATES ON THIS REF**, both derived off their own runners' last lines before the first edit. The check lands as **`RI-37`**, written **inline in [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs)**, so invariants become **35** and gates stay **33**.

#### 3.1 The argument FOR a corpus gate, which is real, and the four things that beat it

**THE SUBJECT IS A CORPUS DOCUMENT AND `gates.mjs` ALREADY OWNS IT.** [`gates.mjs:166`](../../scripts/corpus/gates.mjs) names `packages/db/DELTA_MANIFEST.md` **by hand** in `isCorpusDocument`; [`:982`](../../scripts/corpus/gates.mjs) computes the `manifest_changes` span out of it; [`:2131`](../../scripts/corpus/gates.mjs) is `ADR-026`'s completeness gate, which parses its delta rows. **This check is that gate with a different key**, and the manifest itself says so in finding 5's words. A reader who thinks a check about a corpus document belongs in the corpus runner regardless of anything else is making a maintainability argument, and it is the same argument [ADR-329](ADR-329.md) section 3.1 said it could not refute. **Four things beat it here and the first is decisive.**

1. **A REGISTERED CORPUS GATE IS WATCHED FAILING BY [`falsify.mjs`](../../scripts/corpus/falsify.mjs), WHICH THIS ROW MAY NOT RUN.** That harness is forbidden by the dispatch and it mutates the working tree; its `SEEDS` table holds one violation per gate, and `gates.mjs` records in its own comments that *"`falsify.mjs` runs every gate against the tree as it stands and a gate that cannot pass there is an ERROR"*. **A gate landing here would land with the half that makes it a control unwritten and unrunnable**, which is precisely the decoration the row's falsifiability test exists to refuse. An invariant's falsification harness is a vitest file, which this row may run, and which [ADR-329](ADR-329.md) wrote for `RI-36` one check over.
2. **THE FENCE NAMES `packages/tooling/test/**` AND `packages/db/test/**` AND NOT `scripts/corpus/test/**`.** The one file in this repository that imports `gates.mjs` for testing is [`scripts/corpus/test/gates-runner.test.ts`](../../scripts/corpus/test/gates-runner.test.ts), which is outside this fence; and that file tests the REPORT rather than any gate. So a gate landing here would have had its cases in a directory the row does not hold.
3. **`RI-29` MAKES REGISTRATION MECHANICAL AND NOTHING DOES THAT FOR A SLUG.** `RI-29` reported *"ALLOCATION.md carries no register row for `RI-37`"* with the live title before the row was written, which is the seventh consecutive wave it has done that. **A slug gate is registered by nothing at all**: `CI-06p` reads letters only and `CI-06/closed-letter-series` says in its own words that *"a slug cannot exhaust, so there is no slug series to close"*; measured, **two of the nine live slug gates carry no [STRATEGY](../testing/STRATEGY.md) section 4.4 row**, `CI-06/table-row-width` and `CI-06/derivable-counts`. Registration that is enforced beats registration by discipline, which is [ADR-034](ADR-034.md)'s own rule.
4. **`scripts/corpus/tsconfig.json` SETS `checkJs: false` IN WRITING AND `packages/tooling/tsconfig.json` SETS IT TRUE.** This check carries a `Map<string, string[]>` and a written register through two functions, and it is type-checked here and would be unchecked there. A fifth consideration is smaller and is recorded rather than leaned on: adding a gate moves `GATES.length`, which is the `gate_count` span, which `generate` rewrites in five places under `docs/`; [ADR-296](ADR-296.md) section 6 refused a thirty-fourth gate on exactly that ground.

**THE COST OF CHOOSING THIS RUNNER IS STATED RATHER THAN HIDDEN, AND IT IS [ADR-329](ADR-329.md)'s COST DEEPENED.** That entry accepted that one docs-versus-migrations comparison now sits in two runners. This is the second such check in `packages/tooling`, so a reader asking *"what checks the delta manifest"* now finds `ADR-026`'s gate in `scripts/corpus` and `RI-37` in `packages/tooling`. **Moving it later is a file move plus a register row, not a rewrite**, and the thing that cannot be moved later is a control that was never watched failing.

#### 3.2 `RI-37` is taken and `RI-32` is left exactly as the register describes it

The `RI-nn` table's own note reads *"`RI-32` IS STILL THE NEXT FREE NUMBER"*: row `291` reserved it and [ADR-291](ADR-291.md) section 8 returned it. **It is not backfilled here.** Filling a returned reservation is a renumbering decision about somebody else's row, the paragraph beside it is the record of that return, and the five waves since have each taken maximum-plus-one. `RI-37` was reserved by name by [ADR-297](ADR-297.md) and again by [ADR-304](ADR-304.md), and each **left it free in writing**, so it is claimed here rather than assumed free.

---

### 4. THE FALSIFIABILITY TEST: the check RED on this tree before the repair, GREEN after

**IT IS A COMMIT ORDER RATHER THAN A TRANSCRIPT.** `56f1ceaf` carries the check and its register row and is **RED at `RI-37` by construction**; `c4f8b5ee` is the repair; `95bc12c0` is the suite. The runner's own output on `56f1ceaf`:

```
PASS   RI-36  A design record's nullability is the nullability the migrations leave
FAIL   RI-37  Every migration on disk has a landing record in the delta manifest  (2)
       packages/db/migrations/0080_wallet_debit_provenance.sql is on disk and
       packages/db/DELTA_MANIFEST.md carries no landing record for `0080`: no row of the
       migration sequence in section 1 and no `## <n>.` section naming it. ...
       packages/db/migrations/0081_purchase_processor_columns.sql is on disk and
       packages/db/DELTA_MANIFEST.md carries no landing record for `0081`: ...

33 of 35 invariants hold.
```

**The `33 of 35` is `RI-29` failing beside it**, reporting *"ALLOCATION.md carries no register row for `RI-37`"* in the same run, which is the register doing its job before the row existed.

**AND THE SAME RUN AFTER THE REPAIR:**

```
PASS   RI-37  Every migration on disk has a landing record in the delta manifest

35 of 35 invariants hold.
```

**THE RED WAS RE-DERIVED WITH THE SHIPPED READER, WHICH MATTERS BECAUSE THE READER MOVED AFTER `56f1ceaf`** (section 7). Run against a tree carrying the real migration directory and `git show 56f1ceaf:packages/db/DELTA_MANIFEST.md`, the shipped check reports **the same two findings, byte for byte**.

**AND THE RED IS RECONSTRUCTED IN THE SUITE SO IT SURVIVES THE REPAIR.** [`migration-landing-record.test.ts`](../../packages/db/test/migration-landing-record.test.ts) removes each of sections 38 and 39 from the **live** manifest, asserts the removal changed something, and watches the shipped check report exactly that migration; a third case removes both and requires exactly two findings.

**EVERY RECONSTRUCTION HAS ITS COUNTERFACTUAL, WHICH IS THE HALF A RED RUN CANNOT SHOW.** The counterfactual tree is the same manifest with the MIGRATION taken away as well: the tree on which no record was owed. **A check red on both is catching nothing.**

---

### 5. THE TWO SECTIONS, measured rather than reconstructed

**THE MEASUREMENT IS THE ROW.** Against **PostgreSQL 16.13**, three databases were built by applying `packages/db/migrations/*.sql` forward-only from empty under `ON_ERROR_STOP=1` and stopping at `0079`, `0080` and `0081`. Every count below is read from `pg_tables`, `pg_indexes`, `pg_constraint` and `pg_trigger`, and every constraint body from `pg_get_constraintdef`.

#### 5.1 `0080`, and what one CHECK moved

| | `0001`..`0079` | `0001`..`0080` |
|---|---|---|
| tables | 118 | 118 |
| indexes | 414 | 414 |
| CHECK constraints | 516 | **517** |
| foreign keys | 176 | 176 |
| unique constraints | 17 | 17 |
| non-internal triggers | 32 | 32 |
| `attnotnull` on `wallet_entries.provenance` | `true` | **`false`** |

**ONE OBJECT IS CREATED AND IT IS A CHECK.** `wallet_entries_provenance_check` is still installed and still closes the vocabulary at three members, so `0080` composes rather than replaces. Re-applying `0080` alone is refused at `constraint "wallet_entries_provenance_follows_direction" for relation "wallet_entries" already exists`, and re-applying `0001` at `type "identity_status" already exists`. [`probe_wallet_debit_provenance.sql`](../../scripts/db/probe_wallet_debit_provenance.sql) runs **4 acceptances and 5 rejections, 10 `NOTICE` lines, exit 0** at `0080` and **dies at `ACCEPTANCE 1`, exit 3**, at `0079`. Both transcripts are in manifest section 38.

#### 5.2 `0081`, and the anchor read out of the catalogue

| | `0001`..`0080` | `0001`..`0081` |
|---|---|---|
| tables | 118 | 118 |
| indexes | 414 | 414 |
| CHECK constraints | 517 | **518** |
| foreign keys | 176 | 176 |
| unique constraints | 17 | 17 |
| non-internal triggers | 32 | 32 |
| `attnotnull` on `purchases.psp` and `.psp_reference` | `true`, `true` | **`false`, `false`** |

`purchases_psp_reference_uq` reads `indisunique = true`, **`indnullsnotdistinct = false`**, `indpred` **null**, so the idempotency anchor is whole, unique and NULLS DISTINCT and `0081` neither dropped nor narrowed it. [`probe_purchase_processor_columns.sql`](../../scripts/db/probe_purchase_processor_columns.sql) runs **4 acceptances and 8 rejections, 12 `NOTICE` lines, exit 0** at `0081` and **dies at `ACCEPTANCE 1`, exit 3**, at `0080`. [`assert_no_floats.sql`](../../scripts/db/assert_no_floats.sql) holds over the applied schema and fires in both directions.

**A DETAIL NEITHER ADR RECORDS AND THIS ROW MEASURED.** All three columns get their **first** comment from these two files: `wallet_entries.provenance` has none at `0079`, and `purchases.psp` and `.psp_reference` have none at `0080`. **And `purchases_psp_reference_uq` has no comment of any kind before `0081`**: `0006` named it the idempotency anchor in a `--` comment in the migration text and left the object itself undocumented, so `0081`'s `COMMENT ON INDEX` is the first thing `psql \d+` has ever shown about the anchor.

#### 5.3 TWO FIGURES DISAGREE WITH [ADR-322](ADR-322.md) AND [ADR-323](ADR-323.md), and the cause was executed rather than argued

**[ADR-322](ADR-322.md) section 4 reports 412 indexes at `0001`..`0079` and 412 at `0001`..`0080`. [ADR-323](ADR-323.md) section 4 reports 412 at `0001`..`0080` and 412 at `0001`..`0081`. This tree measures 414 at all four.** Reported as a disagreement rather than quietly overwritten, which is the row's own instruction.

**THE DELTA BOTH ENTRIES RECORD IS REPRODUCED EXACTLY**, `+1` CHECK and nothing else, and tables, CHECK totals and triggers all agree to the unit. **The absolute figure is the one that differs, and the cause is `0078`.** [ADR-322](ADR-322.md) finding 5 records `0077` and `0078` as reserved-unwritten rows and [ADR-323](ADR-323.md) finding 6 records the same, so neither measuring base carried `0078`; manifest section 36 records `0078` moving `public` indexes **412 to 414**. **Applying `0001`..`0080` on this tree with `0078` removed returns 118 tables, 412 indexes, 517 checks, 174 foreign keys, 16 unique constraints and 32 triggers**, which is [ADR-322](ADR-322.md)'s figure exactly.

**SO NEITHER MEASUREMENT IS WRONG AND THE DISAGREEMENT IS ABOUT WHICH MIGRATIONS WERE ON THE MEASURING BASE**, which is what an absolute object count is a fact about. It is the ordinary hazard of sibling branches measuring a shared schema, and it is written into both sections so a reader comparing the numbers does not have to rediscover it. **Neither ADR is amended**: both are dated records and both were true of the database they were run against.

---

### 6. THE SWEEP OVER ALL 75 MIGRATIONS: twenty more gaps, sixteen of them money path

**RUN OVER THE WHOLE DIRECTORY: 75 migrations against the manifest as this row leaves it, 29 migration-sequence rows and 28 landing headings**, which is **55 numbers recorded and 20 not**. Findings beyond the row's own two: **twenty**. `0037`, `0039`, `0040`, `0041`, `0043`, `0044`, `0050`, `0052`, `0053`, `0054`, `0055`, `0056`, `0057`, `0059`, `0063`, `0068`, `0070`, `0072`, `0073`, `0074`.

**SIXTEEN OF THE TWENTY OPEN WITH AN `E2 READ: MONEY PATH` HEADER**, measured by grepping the header out of each file: all but `0039`, `0040`, `0041`, `0043`. **And seven of them appear in the manifest ZERO times in any form**, measured by counting bare occurrences of each number: `0052`, `0053`, `0054`, `0055`, `0056`, `0057` and `0059`, which is the seven-file chart-of-accounts and terminal-withdrawal run from `0052` to `0059`. **The delta manifest has never heard of them.**

**THE DISPATCH SAYS TO STOP AND REPORT PAST ABOUT THREE, AND THIS ENTRY DOES.** Not one of the twenty sections is written. What the row does instead is name them, in a register the check reads, in a file a reviewer sees:

- **The register is `LANDING_RECORD_BACKLOG` in [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs)**, twenty entries, each carrying the migration's file name and the ADR its own header cites as the ruling.
- **It can only shrink.** Leg 3 makes an entry naming a migration that now HAS a record a finding, so a session writing one of those sections deletes its row in the same commit or the runner goes red. **It runs in ONE direction and the other is deliberately not taken**: an entry naming a migration the tree does not carry is INERT, because reporting it would make the register a claim about one migration set rather than a property of the check and constitution `E2` already forbids deleting a merged migration. **A mis-keyed row is caught by leg 1 instead**, since the number it failed to absorb is then reported as unrecorded, and both halves are cases in the suite.
- **It cannot grow without an edit to the runner.** A twenty-first migration with no record is a finding on the commit that adds it, so the backlog is a fixed list of known debt rather than a rule about what may be skipped.
- **It is not a ruling that these migrations need no record.** Every one is owed the section `0080` and `0081` get here, and writing them is a row of its own, **dispatched knowing it is twenty installs and twenty probe transcripts** rather than twenty paragraphs written from twenty ADRs.

**THAT LAST POINT IS THE WHOLE REASON FOR THE SHAPE.** A landing section reconstructed from an ADR instead of measured is exactly what section 5.3 just caught two ADRs being imprecise about, on a figure neither of them was wrong to state. Twenty of those in one diff would be twenty records nobody measured, wearing the clothes of the thing this check exists to require.

---

### 7. Where the two sections go in the numbering, and the reader the suite moved

#### 7.1 Sections 38 and 39, because the sequence is LANDING order

**SECTION NUMBERS IN THIS FILE ARE THE ORDER RECORDS WERE WRITTEN, NOT THE ORDER MIGRATIONS RAN**, and sections 35, 36 and 37 prove it by being `0079`, `0078` and `0082`. Section 16's own rule: *"Sections are append-only records of what landed and when, so the sequence only ever grows and the maximum is the only thing anybody needs."* **`37` is the maximum, so `38` and `39` are the next free numbers.** `0080` takes `38` because it is the earlier migration and because `0081`'s ruling reuses its `CASE`-not-disjunction finding.

**A LETTERED SECTION WAS CONSIDERED AND REFUSED.** `4a` is the file's own escape hatch for a section that belongs in the MIDDLE. There is no middle to belong to: a record written today belongs at the end of a sequence ordered by when records were written, and a `35a` would claim these two records were written between `0079`'s and `0078`'s, which they were not.

**BOTH ROWS ARE IN SECTION 16'S SECTION-NUMBER TABLE, WRITTEN IN THE SAME COMMIT AS THE SECTIONS.** That upholds the table's standing rule rather than trading it: *"a row written on somebody else's behalf is a claim nobody made"*, and these two rows are not on anybody else's behalf.

**AND SECTION 37'S ROW WAS AMENDED IN PLACE BECAUSE IT IS NOW FALSE.** It read *"`0080` and `0081` are on disk with no section here at all ... 37 is the true maximum"*. Both halves stopped being true in commit `c4f8b5ee`. It is amended in place rather than joined by a second row ([ADR-065](ADR-065.md) T3), keeping every word it said and recording what closed it, because a false sentence standing in an allocation table is [ADR-328](ADR-328.md)'s defect class **and no needle in `RI-35`'s register reaches it**: that sweep reads `apps/*/src`, `packages/*/src` and `scripts/`, and this file is a `.md` under `packages/db/`.

#### 7.2 THE SUITE FOUND A HOLE IN THE CHECK AND THE CHECK MOVED, which is the most useful thing in this entry

**The first reader took every backticked four-digit number in a `## ` heading.** Section 39's heading reads *"`0081` lands, and the record that was owed with it is written in the same commit as `0080`'s"*. **So that MENTION counted as a landing record for `0080`**, and the reconstruction case that removes section 38 reported the tree CLEAN with `0080` unrecorded.

**A HEADING NOW CLAIMS ONLY THE NUMBERS BEFORE ITS VERB.** Every landing heading in the file is written that way and this was checked against all 26 rather than assumed: `` `0028` lands ``, `` `0030` and `0031` land ``, `` `0048` and `0049` land ``. A number named after the verb is a cross-reference and claims nothing, and a heading with no `land` in it, like section 15's or section 22's, claims nothing at all.

**THE ALTERNATIVE WAS TO REWORD MY OWN HEADING AND IT WAS REFUSED.** Renaming section 39 would have made the case pass and left the reader admitting any heading that mentions a number, which is a control that decays silently the next time somebody writes a cross-reference. **The check was watched failing on a defect its own author introduced**, which is the only reason the hole was found at all, and the red was then re-derived with the moved reader (section 4) so the transcript in this entry is the shipped one.

---

### 8. The pre-edit grep, and what already pins this subject

**`grep -rn "DELTA_MANIFEST"` over `apps`, `packages`, `scripts` and `.github`, restricted to `.mjs`, `.ts`, `.tsx`, `.js`, `.yml` and `.json` and excluding `node_modules`, returned 51 lines in 17 files.** Four of them pin this subject and were read before a line was written.

| Site | What it pins | Moved? |
|---|---|---|
| [`gates.mjs:166`](../../scripts/corpus/gates.mjs) | `isCorpusDocument` names this file **by hand**, with its own comment saying a by-name list *"is fine at one entry; if it reaches three it needs a rule instead of a list"* | **No.** Still one entry |
| [`gates.mjs:982`](../../scripts/corpus/gates.mjs) | the `manifest_changes` span, which counts `SD-nn` and `U-nn` ROWS. Sections 38 and 39 add delta tables, so **this was the one span at risk** | **No.** Both new rows are keyed `ADR-322` and `ADR-323`, not `SD-nn`, exactly as sections 33 to 37 key theirs; `generate` reports *"every span already matches its query"* |
| [`gates.mjs:8412`](../../scripts/corpus/gates.mjs) | `CI-06/retired-constraints` registers this file at **`sites: 7`**, for the two supersession sections that name a retired constraint in order to retire it | **No.** Neither new section names a retired constraint; the gate holds and the register size is unchanged |
| [`repo-invariants.mjs:7476`](../../packages/tooling/checks/repo-invariants.mjs) and [`repo-invariants.test.ts:392`](../../packages/tooling/test/repo-invariants.test.ts) | `RI-31`, no document repeats a whole line carrying a generated span, whose own comment records that this file *"held SEVEN more of them"* | **No.** Neither new section carries a generated span |

**THE GREP PAID FOR ITSELF ON THE SECOND ROW.** `manifest_changes` counts any table row whose first cell matches `SD-nn` or `U-nn`, and a delta table written with an `SD` key in the first cell would have moved a generated span in [INDEX](../INDEX.md) and [STATE](../STATE.md) as a side effect of writing a landing record. The convention that a supersession's delta row is keyed by its ADR is what makes the two independent, and it is a convention rather than a control.

---

### 9. What this row did not do

- **No SQL, no migration, no `SD-nn`, no `U-nn`.** `git diff --stat 9641a972..HEAD -- packages/db/migrations scripts/db` is **EMPTY**. `0080` and `0081` are not edited, including their comments (constitution `E2`).
- **NO CORPUS GATE.** Gates stay at 33 and the `CI-06` letter series is untouched. Section 3.1 states the argument for a gate rather than pretending there was not one.
- **The twenty other gaps are NOT repaired.** They are registered, reported here and reported in the pull request, and section 6 says why writing them would have been the worse outcome.
- **NO EXEMPTION LIST AND NOTHING LOOSENED.** The register is shrink-only and the direction it declines to run in is stated in the check's own `covers` with its reason rather than left to be discovered from green. What counts as a record is the manifest's own two shapes, and the heading shape was NARROWED after the suite found it too wide rather than widened to accommodate a heading.
- **No dated record was edited and no ADR was amended.** [ADR-322](ADR-322.md) and [ADR-323](ADR-323.md) stand exactly as written, including the index figures section 5.3 disagrees with; both were true of the databases they were run against.
- **`scripts/corpus/data-model-columns.mjs` was not registered and not edited**, and `RI-32` was not backfilled (section 3.2).
- **[`docs/INDEX.md`](../INDEX.md) was NOT edited beyond its generated spans**, on [ADR-322](ADR-322.md)'s and [ADR-323](ADR-323.md)'s reason: an ADR is a registry entry and needs no `INDEX.md` row.
- **[`packages/tooling/test/repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts) was not edited.** `RI-37` is covered there by the `test.each(CHECKS)` clean-tree row, which grows with the array, and it is SILENT on that file's synthetic estate by design because that estate carries neither a migrations directory nor a manifest.

---

### 10. Found and NOT repaired

1. **Twenty migrations have no landing record and sixteen are money path** (section 6). Registered, reported, and owed a row of its own. **Seven of them appear in the manifest zero times in any form.**
2. **Sections `24`, `26` to `30` and `32` to `34` are claimed by headings and carry no row in section 16's section-number table**, which the `31`, `35` and `37` rows each already name. Left standing on that table's own rule that a row written for somebody else is a claim nobody made. **`RI-37` does not read that table**, so the two registries can still disagree; a check that they agree is the second half of the manifest's own section-16 remedy and is not written here.
3. **`RI-37` never reads a landing section's CONTENT.** A section reading `## 40. \`0083\` lands` and nothing else would satisfy it. That limit is stated in the check's `covers` rather than left to be discovered from green, and the thing that actually forces content is the `E2` read.
4. **`ADR-026`'s manifest gate and `RI-37` now read the same document in two runners** for two different keys (section 3.1's stated cost).
5. **Unchanged from the rows before this one**, each re-read and none in this fence: [ADR-305](ADR-305.md) slice 8's stale `worker/src/index.ts:1326-1331` citation; `identity_restriction_episodes.sla_due_at` still invisible to `CI-06l`; [`schedule.ts:220`](../../apps/worker/src/schedule.ts)'s reachability clause; [`data-model-columns.mjs`](../../scripts/corpus/data-model-columns.mjs) still unregistered; and [`purchases.md`](../architecture/data-model/purchases.md)'s *"Why the wallet constraints are three and not one"* against a fourth wallet-keyed constraint.

---

### 11. Fence

[`packages/tooling/checks/repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) (`RI-37`, its reader, its register and the `CHECKS` row), [`packages/db/test/migration-landing-record.test.ts`](../../packages/db/test/migration-landing-record.test.ts) (new), [`packages/db/DELTA_MANIFEST.md`](../../packages/db/DELTA_MANIFEST.md) (sections 38 and 39, two section-number rows and one in-place amendment), this entry, [ALLOCATION](ALLOCATION.md)'s `RI-37` row and row `334`, [the session log](../sessions/2026-09-05-session-525.md), [STATE](../STATE.md) and the two registry READMEs.

**NO CITATION REPAIR WAS NEEDED AND THAT WAS CHECKED RATHER THAN ASSUMED.** `RI-37`'s body sits immediately above the `CHECKS` literal at the foot of `repo-invariants.mjs`, on [ADR-329](ADR-329.md)'s precedent, so the only lines it moved are that literal and the report block beneath it; the new manifest sections are appended and the two section-number rows are inserted inside a table nothing cites by line. `RI-15` and `RI-16` both hold, which is how the absence of a repair is known rather than hoped.

**THE TWO REGISTRY READMEs AND EVERY GENERATED SPAN ARE GENERATED AND NOT HAND-WRITTEN**, rewritten by `node scripts/corpus/gates.mjs generate` from this entry's own first line and the log's `index` comment.

---

### 12. Verification, each command run separately and each exit code read without a pipe

- `node packages/tooling/checks/repo-invariants.mjs` -> **35 of 35 invariants hold**, exit 0, where the baseline was 34 of 34.
- `node scripts/corpus/gates.mjs generate` then `check` -> **33 of 33 gates pass**, exit 0, unchanged: this row adds no gate.
- `pnpm vitest run` -> counts in section 13, exit 0.
- `pnpm run lint`, `pnpm run format:check`, `pnpm run typecheck` -> clean, exit 0 each.
- The migration set applied forward-only from empty under `ON_ERROR_STOP=1` against PostgreSQL **16.13** at `0079`, `0080`, `0081` and at `0080`-without-`0078`; both probes run at their landing point and at their counterfactual.
- **`pnpm run verify` was NOT run and `scripts/corpus/falsify.mjs` was NOT run**, both forbidden by the row, the second because it mutates the working tree.

---

### 13. Counts, derived at the moment they are stated

Suite **312 files / 7,604 passed / 10 skipped / 0 failed**, against a `9641a972` baseline reproduced **before the first edit** of **311 files / 7,585 passed / 10 skipped / 0 failed**; the delta is **+1 file and +19 cases with skips unchanged**: eighteen are this row's own suite and the nineteenth is the `test.each(CHECKS)` row in [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts), which grows with the array by construction. Invariants **35 of 35** where the baseline was 34 of 34, the one new check being `RI-37`. Gates **33 of 33** after `generate`, unchanged. Check sizes read out of the runner at reporting time: **75 migration(s) on disk, 55 recorded by `packages/db/DELTA_MANIFEST.md` across 29 section-1 rows and 28 landing headings, 20 on the backlog register.** The heading count is 28 rather than 26 because sections 14 and 26 each name two migrations and this row's two sections are now among them. `git diff --stat 9641a972..HEAD` against `packages/db/migrations` and `scripts/db` **empty**. Lint, `format:check` and typecheck clean.

---

### Approval

**Status: `proposed`. The approval line is UNSIGNED and merging is not signing.** No `status` field anywhere moves and nothing is marked `accepted`. **`E2` READ OWED**, and two are inherited, both `proposed` and both UNSIGNED: [ADR-322](ADR-322.md) and [ADR-323](ADR-323.md).

- **Approval:** `___________________________` (UNSIGNED. **Merging is not signing.**)

  **What a founder read adds and this entry cannot.**

  **THE FIRST QUESTION IS THE ONE NO CASE IN THIS DIFF CAN ANSWER: IS A TWENTY-ENTRY BACKLOG REGISTER THE RIGHT SHAPE, OR IS IT AN EXEMPTION LIST WITH BETTER MANNERS.** Every leg here can tell you the register shrinks, that a stale entry is a finding, and that a twenty-first migration cannot join it without an edit somebody reviews. **None of them can tell you that shipping a control which is green over twenty known money-path gaps is better than not shipping it.** The alternative reading is real and I want it said out loud: a check that passes while `0052` to `0059` are absent from the delta manifest **entirely** is a check that has normalised the thing it was written to end, and a founder who would rather see twenty-two findings on every CI run until somebody does the work is choosing a sharper instrument than the one I built. **I chose the register because the row's own dispatch forbids writing those sections here and the stop condition requires green**, and because the twenty are now countable, named and in a file a reviewer reads instead of being four sentences scattered across four ADRs. If you want the other shape, it is a one-line deletion in the runner and a red CI until the backlog is worked off.

  **THE SECOND IS WHERE THE CHECK LIVES, AND IT IS THE CALL I AM LEAST SURE OF.** Section 3.1 puts a check about a corpus document into `repo-invariants` while `gates.mjs` names that document by hand in three places and holds `ADR-026`'s completeness gate, which is this check with a different key. **The honest description is that I let the FALSIFICATION HARNESS decide the address**: `falsify.mjs` is forbidden to this row, so a gate could not have been watched failing here, and a control that has never seen its defect is decoration. **A reader who thinks that is a fact about one dispatch rather than about the estate is making an argument I cannot refute.** Moving it is a file move plus a register row.

  **THE THIRD IS THE ONE FIGURE THIS ROW MEASURED AGAINST TWO SIGNED-NOTHING PREDECESSORS.** Section 5.3 reports 414 indexes where [ADR-322](ADR-322.md) and [ADR-323](ADR-323.md) each report 412, and demonstrates the cause is `0078`'s absence from their bases rather than an error in anybody's arithmetic. **I did not amend either entry.** The alternative is to treat a dated record's object counts as maintainable, and this corpus has repeatedly ruled the opposite. **But it means the repository now carries three numbers for one property**, and a reader who finds 412 first will believe it; the only thing standing between them and the wrong number is the paragraph I wrote in each section.

  **WHERE THIS ENTRY IS MOST LIKELY WRONG, named rather than left to be found.** **`RI-37` asserts that a record EXISTS and can say nothing about whether it is TRUE.** A section reading `## 40. \`0083\` lands` and one sentence satisfies it exactly as well as section 38's measured install and probe transcript do, and the pressure this check creates is pressure to write a heading. **The thing that makes a landing section worth anything is the measurement, and the measurement is enforced by nothing but the discipline of the session writing it** and by your `E2` read. If that worries you, the leg worth adding is the cheap one: a landing section must name the migration file that exists AND must contain a probe or install transcript, which is a shape parse rather than a truth claim, and it is perhaps a fifth of the work of the real thing.
