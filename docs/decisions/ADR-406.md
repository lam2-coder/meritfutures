## ADR-406: the census the dispatch says was never re-run was re-run three rows ago and reproduces at 74 against 39, hole one is 43 pointers and not 55 and retires on a corrected key, hole two is CLOSED at a cost of four pointers and no dated one, and a pointer into a dependency is undecidable rather than born wrong (2026-09-07, status: proposed)

**Row 406 of the 404s wave. Branch `claude/the-instruments-two-holes`, off `1f621d5c`, session 601.**

**Status `proposed`. The approval line in section 15 is UNSIGNED and merging is not signing. `E2` read owed.**

**FIVE THINGS ARE HERE AND THE FIRST IS A REFUSAL OF THE TASK AS STATED.**

1. **THE CENSUS WAS RE-RUN BY [ADR-401](ADR-401.md) AND [ADR-403](ADR-403.md) SAYS IT WAS NOT.** The dispatch inherited that sentence from a dated record written the same day the re-run landed. Section 4.
2. **IT REPRODUCES ANYWAY, TWO COMMITS LATER, CELL FOR CELL.** 74 repairable against 39 born wrong, and all ten of that entry's dated cells. Section 5.
3. **HOLE ONE IS 43 POINTERS AND NOT 55, AND THE DIFFERENCE IS COMMA LISTS.** It retires on the corrected key for a better reason than it retired on the old one. Section 6.
4. **HOLE TWO IS CLOSED.** The rule is one clause, it moves four pointers, and **not one of them is in a dated record**, so [ADR-388](ADR-388.md)'s headline is untouched by its own correction. Section 7.
5. **A THIRD HOLE NOBODY NAMED, AND IT IS THE ONE POINTER TWO HARNESSES DISAGREED ON.** Section 8.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `406`, session **601**, off the wave integration branch `claude/wave-401s` rather than off `main`.
- **CONTROL OVER THE CORPUS'S OWN POINTERS. Ships `proposed` with an UNSIGNED approval line. `E2` read owed.**
- **ONE NEW INSTRUMENT, NO GATE.** No `RI-nn` minted, no `CI-06` letter spent, no register grown, no test skipped, no gate added, widened or weakened.
- **EVERY FIGURE THE DISPATCH CARRIED WAS RE-DERIVED. THE 289 IS DISCHARGED, THE 23 REPRODUCES, THE 81 REPRODUCES, AND THE CONTAINER SENTENCE IS TRUE FOR ONCE.** Sections 1, 4, 7 and 9.

---

### 1. The container, measured and reported, because the dispatch declined to state it

**THE CONTAINER ARRIVED WITH NO REPOSITORY, FOR THE SIXTH CONSECUTIVE WAVE.** The stated working directory was empty, `git rev-parse` returned *fatal: not a git repository*, and no checkout existed anywhere on the container. The tree was attached to this session's authorized set and cloned by hand.

**AND THE MEASUREMENT THE DISPATCH ASKED FOR, RUN ON THIS ROW'S OWN CLONE AND REPORTED VERBATIM.**

| | |
|---|---|
| `git rev-parse --is-shallow-repository` | **`false`** |
| `git rev-list --count HEAD` on the default branch | **4,329** |
| `.git/shallow` | **does not exist** |
| Deepening | **NOT performed**, because the rule says to deepen only on `true` |

**THIS IS THE FIRST DISPATCH IN SIX WAVES THAT ASSERTED NOTHING ABOUT THE CLONE, AND IT IS THE RIGHT CHANGE.** [ADR-396](ADR-396.md), [ADR-398](ADR-398.md), [ADR-399](ADR-399.md), [ADR-400](ADR-400.md), [ADR-401](ADR-401.md) and now this row each measured `false`; [ADR-397](ADR-397.md) measured `true` on its own container. **A dispatch that hands forward a measurement is wrong five times in six. A dispatch that asks for one is right every time**, and the cost of asking is the two seconds this section spent.

**THE MISSING REPOSITORY IS THE PART THAT IS STILL BROKEN AND NO ADR CAN FIX IT.** Six rows have now paid the same clone. It is the dispatcher's.

---

### 2. The base, reproduced on `1f621d5c` before the first edit

**A DISPATCH PROMPT IS NOT A SOURCE AND NEITHER IS THE PREVIOUS ROW'S TABLE.** The whole suite ran on the base commit, on a tree `git status --porcelain` reported EMPTY, before this row wrote a character.

| Runner | On `1f621d5c`, before the first edit |
|---|---|
| `node scripts/corpus/gates.mjs check` | **33 of 33** |
| `node packages/tooling/checks/repo-invariants.mjs` | **35 of 35** |
| `pnpm vitest run` | **336 files, 8,109 passed, 10 skipped, 0 failed** |
| `pnpm run typecheck`, `pnpm run lint`, `pnpm run format:check` | clean, clean, clean |

**THE GATE COUNT IS DERIVED AND IT IS 33.** It is read off the runner's own last line and not assumed, which the dispatch asked for and which is worth doing: the count has been 33 since [ADR-396](ADR-396.md) and a row that assumes it would not notice the wave that changes it.

**`pnpm run verify` was NOT run and [`falsify.mjs`](../../scripts/corpus/falsify.mjs) was NOT run.** Both are forbidden to this row and both mutate the tree. Every leg was invoked directly.

---

### 3. The reader, relocated by fence, and the population

**THE CHECKS' OWN READER WAS RELOCATED OUTSIDE THE REPOSITORY WITH ITS `HERE` CONSTANT PINNED TO THE REAL CHECKS DIRECTORY.** That is [ADR-400](ADR-400.md)'s and [ADR-401](ADR-401.md)'s method. **This row owns `packages/tooling/**` and took the relocation anyway**, because adding an export to [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) moves lines in a file carrying 81 inbound pointers to buy nothing a copy does not buy.

**THE COPY WAS VALIDATED BEFORE ITS READER WAS BELIEVED.** Run through it, **34 of 35** checks reproduce their live verdict and the one that does not is **`RI-18`**, which resolves a dependency against its own module location in a way the relocation defeats. **That is [ADR-397](ADR-397.md)'s, [ADR-400](ADR-400.md)'s and [ADR-401](ADR-401.md)'s result reproduced, the same check and the same cause, on a fourth base.**

| Scope, through each check's own input | Citations | Carry a name the binder binds |
|---|---|---|
| `docs/` under a dated or session heading, out of `RI-16`'s scope by shape | 7,692 | **294** |
| `docs/` live, `RI-16`'s scope | 1,100 | **73** |
| Source files, `RI-15`'s scope | 393 | **130** |
| **READ BY THE CHECKS' READER** | **9,185** | **497** |

**RUN AT [ADR-401](ADR-401.md)'s OWN BASE, ALL FOUR ROWS OF THAT TABLE REPRODUCE ITS FIGURES EXACTLY**: 7,676 over 292, 1,099 over 73, 393 over 130, and 9,168 over 495. **That is what licenses reading the 497 as the tree moving and not as a different reader.**

**THE TWO ADDED POINTERS ARE [ADR-401](ADR-401.md)'s OWN FALSIFICATION SEEDS, DERIVED BY SET DIFFERENCE RATHER THAN ASSUMED.** Its section 10 declares seeds A and C at `scope.ts` coordinates it states are wrong on purpose, and the difference of the two populations is exactly those two and nothing else.

| Findings-eligible dated pointers | |
|---|---|
| Dated pointers on `1f621d5c` | **294** |
| Less [ADR-400](ADR-400.md)'s two declared specimens | 292 |
| Less [ADR-401](ADR-401.md)'s two declared seeds | **290** |

**THE FINDINGS-ELIGIBLE DATED POPULATION IS 290 ON BOTH BASES**, which is [ADR-401](ADR-401.md)'s figure arrived at through a different subtraction, and is why the two censuses are comparable at all.

---

### 4. REFUTED: the census WAS re-run, and the record saying otherwise was written after it

**THE DISPATCH SAYS [ADR-388](ADR-388.md)'s CENSUS "HAS STILL NEVER BEEN RE-RUN UNDER `git log -S`", OWED BY THREE ROWS.** The instruction to read [ADR-401](ADR-401.md) first is the instruction that refutes it.

| Record | What it says about the census |
|---|---|
| [ADR-396](ADR-396.md) section 7 | Asks for it, and **already carries the discharge in its own text**: *"DISCHARGED BY [ADR-401](ADR-401.md), AND THE SPLIT MOVES: 74 REPAIRABLE AGAINST 39 BORN WRONG"* |
| [ADR-399](ADR-399.md) section 12 item 3 | Asks for it. **Predates the discharge and is therefore not wrong**, only spent |
| [ADR-401](ADR-401.md) section 4 | **RUNS IT.** 290 findings-eligible dated pointers, 74 against 39, with the eight flips enumerated |
| [ADR-403](ADR-403.md) section 13 item 3 | *"HAS STILL NEVER BEEN RE-RUN UNDER `git log -S`"*, naming [ADR-396](ADR-396.md) and [ADR-399](ADR-399.md) as the askers and [ADR-400](ADR-400.md) as the row that did not do it. **It does not mention [ADR-401](ADR-401.md) at all** |

**[ADR-401](ADR-401.md) IS SESSION 596 AND [ADR-403](ADR-403.md) IS SESSION 598, BOTH DATED 2026-09-07.** The row that declared the item still owed ran two sessions after the row that discharged it, in the same wave, and checked the two entries that asked rather than the one between them. **The dispatch then inherited the sentence, and the instruction that saved it was the one telling this row to read [ADR-401](ADR-401.md) before starting.**

**SO ITEM 1 OF THIS ROW'S TASK IS DISCHARGED BY SOMEBODY ELSE AND WHAT IS LEFT IS THE PART NOBODY HAS DONE**, which is the census under an instrument with the holes closed. That is sections 5 through 8, and it is a smaller claim than the dispatch asked for and a truer one.

**THE PATTERN IS WORTH MORE THAN THE INSTANCE.** [ADR-402](ADR-402.md) section 9 refused option (a) for a convention because *"a convention that lives in one dated record is a convention every later row must find before it can follow"*, and named [ADR-399](ADR-399.md) as the case where four waves misread one. **This is the same failure in the other direction: an item DISCHARGED in a dated record is an item every later row must find before it stops asking for it.** An owed-items list that is only ever appended to grows stale from the bottom.

---

### 5. The census reproduced at this base, three instruments side by side

**DERIVED THROUGH [`g2-census.mjs`](../../scripts/corpus/g2-census.mjs), WHICH THIS ROW WROTE AND SECTION 10 DESCRIBES.** Every figure below is that runner's output at `1f621d5c`, with the four declared specimens and seeds removed.

| Verdict, over the 290 findings-eligible named pointers in dated records | `git blame -w` | `-S` oldest | `-S` **surviving** |
|---|---|---|---|
| True then, true now. Nothing owed | 169 | 168 | **168** |
| **Outcome ONE: DRIFTED after being true, REPAIRABLE** | **75** | **74** | **74** |
| **Outcome TWO: FALSE ON THE DAY IT WAS WRITTEN** | **43** | **39** | **39** |
| False then, true now, by coincidence | 2 | 2 | 2 |
| **Undecidable** | **1** | **7** | **7** |

**ALL TEN CELLS OF [ADR-401](ADR-401.md)'s DATED TABLE REPRODUCE, AND SO DOES ITS CORPUS-WIDE `-S` ROW.** Run at that entry's own base the two `blame` and `-S` columns come back 169 / 75 / 43 / 2 / 1 and 168 / 74 / 39 / 2 / 7, and over 493 the `-S` leg comes back 346 / 85 / 46 / 7 / 9 against that entry's 346 / 85 / 46 / 7 / 9. **Two harnesses built a wave apart from one sentence of prose now agree cell for cell**, which is a stronger result than either could report alone and is the first time in this sequence that has happened.

**AND THE HEADLINE IS UNCHANGED BY THE TREE MOVING.** [ADR-388](ADR-388.md) measured 76 against 42, [ADR-400](ADR-400.md) 75 against 43, [ADR-401](ADR-401.md) 74 against 39, and this base, two merges later, **74 against 39 again**.

**THE THIRD COLUMN IS THE ONE NOBODY HAS RUN AND ITS RESULT IS THAT IT CHANGES NOTHING HERE.** Closing hole two does not move a single dated pointer. Section 7 says what it does move and why the answer is still worth having.

---

### 6. HOLE ONE: priced at 43 and not 55, and it retires on a corrected key

**[ADR-401](ADR-401.md) SECTION 6 RETIRED THIS HOLE AND ITS POPULATION IS WRONG.** That entry keys on the pair *(citing file, token)* and reports 25 pairs carrying 55 of 495 pointers. **A comma list defeats that key.** This corpus writes `` `x.ts:644,649` `` and [ADR-212](ADR-212.md)'s grammar reads it as ONE CITATION PER NUMBER, so one token at one site emits several pointers and the key counts them as a coordinate written twice. They are one site writing two coordinates, which is the opposite shape.

| Over 497 | [ADR-401](ADR-401.md)'s key | Corrected key |
|---|---|---|
| The unit counted | *(citing file, token)* with more than one emitted pointer | the same COORDINATE at more than one distinct LINE of one file |
| Pairs | 25 | **19** |
| Pointers on them | 55, 11.1 percent | **43**, 8.7 percent |
| Pairs where `git blame -w` names more than one commit | 4 | **4** |
| **Pairs whose sites do NOT share one verdict** | ZERO | **ZERO** |

**THE ARITHMETIC CLOSES EXACTLY AND WAS DERIVED RATHER THAN RECONCILED.** There are **6** distinct comma-list *(file, token)* pairs in the population carrying **12** pointers. 25 less 6 is 19 and 55 less 12 is 43. **The whole of the difference is that one shape.**

**THE RETIREMENT SURVIVES THE CORRECTION AND IS BETTER FOR IT.** [ADR-401](ADR-401.md)'s reason was that the sites were written in one diff so `blame` names one commit for all of them. That reason is still true on 15 of the 19, and on the four where `blame` does disagree every site already shares a verdict. **But the corrected key also removes the six pairs that were never instances**, so the claim is now about 43 pointers that really are a coordinate repeated rather than 55 of which twelve were a punctuation artefact. **HOLE ONE IS RETIRED, on a population 22 percent smaller than the one it was retired on.**

**AND THE FOUR SPLIT PAIRS ARE NAMED RATHER THAN COUNTED**, since a retirement resting on four cases should say which: one in [`WAVE-06-admin-console-transport.md`](../plans/WAVE-06-admin-console-transport.md) and three in [`wiring.test.ts`](../../apps/api/test/wiring.test.ts). All four carry one verdict across their sites under both instruments.

---

### 7. HOLE TWO: priced at 23, CLOSED, and the closure moves four pointers and no dated one

**THE HOLE, AS [ADR-396](ADR-396.md) SECTION 13 RECORDED IT.** `git log -S` over the coordinate token, *taking the oldest result*, attributes the surviving line to the earliest introduction. **Where a citation was written, deleted and rewritten, the oldest commit wrote a line that no longer exists.**

| Over 497, at `1f621d5c` | |
|---|---|
| Pointers whose token has MORE THAN ONE `-S` commit over its citing file | **23** |
| Corpus-wide verdict disagreements, `blame` against `-S` oldest | **17** |
| Of those, sitting on a churned token | **8** |
| Of those 8, in DATED records | **1** |

**THE 23 AND THE 8 REPRODUCE [ADR-401](ADR-401.md) EXACTLY AND THE DATED FIGURE DOES NOT.** That entry reports *"2, including [`STATE.md`](../STATE.md) line 10100"*. **Run at that entry's own base and at this one, the answer is 1 both times, and it is [`STATE.md`](../STATE.md) line 10100.** The pointer it names is real; the second one is not there. The eight are byte-stable across the two bases, so this is not the tree moving.

**THE CLOSURE, AND IT IS ONE CLAUSE.**

> `G2` is derived at the SURVIVING introduction: the **newest** commit `git log -S` names for the token over the citing file whose **first parent held that token zero times**. Where the token never went away, that is the oldest commit and the rule changes nothing.

**IT IS THE RIGHT COMMIT BY CONSTRUCTION AND NOT BY TUNING.** A commit whose parent held the token zero times INTRODUCED it. Taking the newest such commit takes the last introduction, and because the token is present today, nothing removed it afterwards. **The line alive today was written by that commit.** Every other `-S` hit is a commit that changed the token's count without introducing it from nothing, or an introduction a later deletion cancelled.

| What the closure costs and buys, over 497 | |
|---|---|
| Churned pointers where surviving names a DIFFERENT commit than oldest | **7 of 23** |
| Of those, pointers whose VERDICT changes | **4** |
| Verdict disagreements against `blame`, before | 17 |
| Verdict disagreements against `blame`, after | **13** |
| **Dated pointers moved** | **ZERO** |

**THE FOUR ARE ENUMERATED BECAUSE FOUR IS A NUMBER A FOUNDER CAN CHECK.**

| Subject | Site | Token | oldest | surviving |
|---|---|---|---|---|
| `subject` | [`ALLOCATION.md`](ALLOCATION.md), the registry's live row | `routes/payouts.ts:554` | false then, true now | **nothing owed** |
| `lockScope` | the SAME line of that file | `routes/payouts.ts:588` | false then, true now | **nothing owed** |
| `principal` | [`wiring.test.ts`](../../apps/api/test/wiring.test.ts) | `routes/admin-writes.ts:277` | false then, true now | **nothing owed** |
| `firm` | [`wiring.test.ts`](../../apps/api/test/wiring.test.ts), a second site | `packages/db/src/scope.ts:1119` | false then, true now | **nothing owed** |

**ALL FOUR MOVE THE SAME WAY AND IT IS THE DIRECTION THAT ARGUES FOR THE RULE.** *"False then, true now, by coincidence"* is [ADR-388](ADR-388.md)'s bucket for a pointer that was wrong when written and that the tree happened to make right. **On these four the pointer was never wrong. The oldest introduction was measuring a sentence somebody deleted**, and the surviving introduction says the author read the line and got it right. **The old instrument was calling four correct citations lucky accidents.**

**AND ONE POINTER ON THE SAME LINE IS NOT FIXED, WHICH IS REPORTED RATHER THAN ROUNDED AWAY.** [`ALLOCATION.md`](ALLOCATION.md) line 279 carries a third citation, `` `transact` `` at `routes/payouts.ts:637`, and it stays *false then, true now* under the surviving rule. **Two of three pointers in one sentence move and the third does not**, which is [ADR-396](ADR-396.md) section 10's finding that `G2` is per pointer and not per sentence, arriving now inside the remedy rather than inside the diagnosis.

**THE HOLE IS REAL AND THE CLOSURE IS DEMONSTRATED RATHER THAN ARGUED.** Section 11 builds the case in a scratch repository and watches the two rules reach opposite verdicts.

**WHAT IS LEFT OPEN, STATED PLAINLY.** The 13 residual disagreements against `blame` are NOT a hole in `-S`; they are the correction [ADR-396](ADR-396.md) section 7 exists to make, and `blame` is the instrument that is wrong on them. **Nothing here proposes a rule for what an undecidable verdict MEANS**, which [ADR-400](ADR-400.md) asked for and [ADR-401](ADR-401.md) repeated. The corrected instrument refuses 9 times corpus-wide where `blame` refuses once, and **that ratio is unchanged by this closure** because the closure never converts a decidable pointer into a refused one.

---

### 8. THE THIRD HOLE: a path this repository has never tracked is undecidable and not born wrong

**THIS ROW'S FIRST HARNESS DISAGREED WITH [ADR-401](ADR-401.md) ON EXACTLY ONE POINTER IN EVERY COLUMN, AND CHASING IT FOUND A HOLE NEITHER ENTRY NAMES.** Fourteen of that entry's fifteen dated cells reproduced; the fifteenth was off by one, in both instrument columns, at both bases. **A residue that size is the kind two rows have previously left unexplained**, and [ADR-396](ADR-396.md) section 13 item 2 says so about its own.

**THE POINTER IS [ADR-274](ADR-274.md) LINE 65**, which cites a line of the `pg` package's own `lib/client.js` for `getStartupConf` while explaining that `pg` is not `libpq`. **That file is a dependency's source. It has never been tracked in this repository, on any ref, at any commit**, which was checked rather than assumed.

| The two readings | |
|---|---|
| This row's first harness | the path resolves to no file in the tree, so the pointer was FALSE on the day it was written: **class two, born wrong** |
| [ADR-401](ADR-401.md) | **undecidable** |

**[ADR-401](ADR-401.md) IS RIGHT AND THE REASON IS [ADR-388](ADR-388.md)'s OWN.** `G2` asks whether the cited line held the named subject at the commit that wrote the citing sentence. **For a path this repository has never held, no commit here can answer.** `getStartupConf` may well have stood at that line in `node_modules` that day, and the author may have opened it and read it. **Deciding it class two convicts a citation of a defect on the evidence that this repository is not the `pg` package**, and class two is the verdict that makes a pointer permanently unrepairable.

**RULED FOR THIS INSTRUMENT: A CITED PATH WITH NO COMMIT ON ANY REF IS REFUSED, NOT DECIDED.** It costs one pointer corpus-wide today and the reason it is worth writing down is that it is the only shape in this census where a census can manufacture a class-two verdict out of its own scope. **The corrected harness reproduces all fifteen of [ADR-401](ADR-401.md)'s cells** and that reproduction is the evidence the rule is the one that entry used.

**IT IS A HOLE IN THE POPULATION AND NOT IN THE INSTRUMENT**, which is why neither previous entry found it: both holes on record are about which COMMIT to read, and this one is about which pointers may be read at all.

---

### 9. `CI-06e`: the price re-derived, the clause NOT written, and why that is the ruling

**[ADR-402](ADR-402.md) SECTION 11 ITEM 2 SAYS THE RIGHT TIME TO WRITE THE CLAUSE IS "THE NEXT DIFF THAT ALREADY HAS TO MOVE THAT REGION FOR ANOTHER REASON". THIS DIFF DOES NOT MOVE IT.** [`gates.mjs`](../../scripts/corpus/gates.mjs) is READ by this row and never opened for writing, and `git diff` against the base names it nowhere. **So the condition has not arrived and the clause is left unwritten.**

**THE PRICE WAS DERIVED AND NOT INHERITED, THROUGH THE REPOSITORY'S OWN [`citation-fan-in.mjs`](../../scripts/corpus/citation-fan-in.mjs).**

| Insertion point in [`gates.mjs`](../../scripts/corpus/gates.mjs) | pointers below | live, to repair | dated, only NAMED |
|---|---|---|---|
| the first `covers:` in the array, which is [ADR-402](ADR-402.md)'s slice | **82** | 17 | **65** |
| **`CI-06e`'s own definition** | **81** | **17** | **64** |
| the last `covers:` alone | **16** | 7 | 9 |
| below every gate definition in the array | **8** | 5 | 3 |

**THREE OF [ADR-402](ADR-402.md)'s FOUR ROWS REPRODUCE EXACTLY AND THE FIRST HAS MOVED BY ONE.** That entry reports 81 / 17 / 64 at the first `covers:` and the same 81 at `CI-06e`; on this base the first `covers:` is **82** because the tree gained one dated pointer between the two bases, and **`CI-06e` is 81 / 17 / 64 exactly.** So its central claim, that one clause at that gate costs what the whole refused slice costs, is now literally true at `CI-06e` and one pointer short at the slice.

**THE 64 IS STILL THE WHOLE ARGUMENT AND IT IS WORSE THAN IT LOOKS FROM THIS ROW'S OWN CENSUS.** [ADR-402](ADR-402.md) calls a manufactured dated pointer *"damage with a tidy motive"*. **Sections 5 and 8 add the reason that framing is exactly right: a dated pointer this diff pushes off its line is not merely unrepairable, it becomes a pointer a later census must classify**, and this corpus's classification of a moved dated pointer is class one or class two depending on a git archaeology that four rows have now got wrong in four different ways. **Sixty-four of those is sixty-four future arguments.**

**AND THE DISPATCH'S CLAIM ABOUT THIS FILE'S RANK WAS CHECKED TOO.** [ADR-402](ADR-402.md) refuted the claim that [`gates.mjs`](../../scripts/corpus/gates.mjs) is among the two most cited files and called it the eighteenth. **On this base it is the SEVENTEENTH, at 112 citations, 18 live and 94 dated**, of 10,777 citation edges over 583 files. The refutation stands and the ordinal moved by one.

---

### 10. What this row built, and why an instrument rather than a paragraph

**ONE NEW FILE, [`g2-census.mjs`](../../scripts/corpus/g2-census.mjs), WHICH MOVES NOTHING.** A file that did not exist has no inbound pointer and no existing line moved to make room for it.

**IT IS AN INSTRUMENT AND NOT A GATE**, on the precedent [`citation-fan-in.mjs`](../../scripts/corpus/citation-fan-in.mjs) and [`covers-census.mjs`](../../scripts/corpus/covers-census.mjs) set in the same directory and for the same stated reason. **`GATES.length` is 33 before and after** and the `gate_count` span did not move. It carries **Rule 2** on its input: an empty population throws rather than reporting a clean corpus.

**THE REASON IT IS KEPT IS THAT FOUR ROWS HAVE BUILT IT AND THROWN IT AWAY.** [ADR-396](ADR-396.md) built the `G2` reader in a session, [ADR-400](ADR-400.md) rebuilt it, [ADR-401](ADR-401.md) rebuilt it and caught itself searching the wrong string with it, and this row rebuilt it again in order to check that entry's figures. **[ADR-402](ADR-402.md) used exactly this argument to keep [`covers-census.mjs`](../../scripts/corpus/covers-census.mjs), in its words *"a third row would have rebuilt it again"*.** A fifth row would have rebuilt this one.

**AND IT DISCHARGES [ADR-401](ADR-401.md) SECTION 5's RULING IN THE ONLY PLACE THAT RULING CAN BE OBEYED.** That entry ruled that a row deriving `G2` by `git log -S` states its traversal and its search string or its figure is not reproducible. **A prose sentence cannot satisfy that ruling, because the ruling exists because a prose sentence did not.** Both halves are now fixed in code: the default walk, and the backticked token as the citing file writes it.

**THE TOKEN RECOVERY IS THE PART THAT WAS HARD AND IT IS WORTH ONE PARAGRAPH.** The reader's `cited` field is target-plus-coordinate and is not the string in the file wherever a pointer sits inside a markdown link, which is [ADR-401](ADR-401.md)'s own recorded mistake. Recovering the real token needs the reader's backtick PAIRING and not a regular expression: a naive sweep mispairs every line this corpus writes as a name and a coordinate together inside a double-backtick span, and there are twelve of those. **A first version of this file got 12 of 497 tokens wrong that way and the arithmetic is what caught it**, exactly as it caught [ADR-401](ADR-401.md): the dated census came back with more undecidables than the validated harness had, and a refinement cannot refuse more often than the thing it refines.

**AND THE TIE IS BROKEN ON THE TARGET RATHER THAN THE TOKEN**, because two citations to DIFFERENT files at the SAME line number on ONE line is a real shape here, twice, and picking the longer token searches the wrong file's history in silence.

**THE INSTRUMENT WROTE TWO WRONG POINTERS INTO THIS TREE AND THEY WERE REMOVED BEFORE THE COMMIT.** Its header comment quoted [ADR-274](ADR-274.md)'s `pg` coordinate and [ADR-401](ADR-401.md)'s seed A coordinate as worked examples. **Both are citations by the checks' own grammar: they entered this row's own census, moved the source population from 130 to 131, and one of them was a coordinate ADR-401 declares WRONG ON PURPOSE.** They are gone and the subjects are named without coordinates, which is [ADR-402](ADR-402.md) section 9's proposed convention obeyed by the row that measured it rather than quoted at somebody else. **A census that changes its own population is a census reading its own footprint.**

---

### 11. Falsification: the hole built on purpose, and the two rules reaching opposite verdicts

**THE SEED IS A SCRATCH REPOSITORY AND NOT THIS TREE, AND THAT IS A CHOICE WITH A REASON.** The thing under test is a rule for choosing a commit. **Seeding this corpus would test whether a gate reddens, and there is no gate here to redden**; what has to be shown is that the two rules disagree on a case the corpus is claimed to contain. So the case was built.

| Commit | What it does | Where `bar` stands in `foo.ts` |
|---|---|---|
| `c1` | writes `` `foo.ts:10` `` for `bar`, and `bar` is at line 10 | **10. The citation is TRUE** |
| `c2` | DELETES the citation from the document | 10 |
| `c3` | moves `bar` to line 20, and REWRITES the same `` `foo.ts:10` `` | **20. The citation is FALSE the day it is rewritten** |

| Rule | Commit it names | Verdict it reaches |
|---|---|---|
| `-S` **oldest**, [ADR-396](ADR-396.md) section 7 as written | `c1` | true then, false now: **ONE, REPAIRABLE** |
| `-S` **surviving**, section 7 of this entry | `c3` | false then, false now: **TWO, BORN WRONG** |

**THE TWO RULES DISAGREE ON THE VERDICT AND NOT MERELY ON THE COMMIT, AND THE OLD ONE FAILS TOWARD THE DANGEROUS ANSWER.** It licenses repairing a coordinate that was wrong the moment it was written, which is [ADR-388](ADR-388.md)'s manufactured verification: the repair would move the pointer to `bar`'s real line and leave a record asserting a check that never happened. **The closed rule refuses it.**

**NOTHING IN THE TRACKED TREE WAS SEEDED, SO NOTHING NEEDED RESTORING.** `git status --porcelain` named only this row's own files at every moment, and no `git checkout --` was run against anything.

---

### 12. Citations this diff moved, audited before the prose was written

**THE THREE FILES CARRYING PROSE ARE NEW OR NEUTRAL.** [`g2-census.mjs`](../../scripts/corpus/g2-census.mjs), this entry and the session log did not exist. **[ADR-396](ADR-396.md)'s annotation is strictly line-count neutral**, one insertion against one deletion, and that file carries **0** inbound citations, so it could not have moved one either way.

**AND THIS ROW MEASURED ITS OWN FOOTPRINT ON ITS OWN CENSUS, WHICH SECTION 10 SAYS IS A THING THAT HAPPENS.** Run against the finished tree, the population is **497** named pointers, exactly what it was at the base. **This entry contributes 9 citations and NONE of them named**, and the session log contributes none at all, because every coordinate in section 7's table sits in a different table cell from its subject and the binder admits at most one glue word between a name and a pointer. **The reader's walk does see both new files**, checked rather than assumed, so this is a population that did not move and not a reader that failed to look.

**THE REGISTRY ROWS ARE THE PART THAT MOVES LINES AND THEY MOVE TEN POINTERS, ALL CLASSIFIED BEFORE THEY WERE WRITTEN.** Three rows are added to [`ALLOCATION.md`](ALLOCATION.md)'s ADR table and everything below shifts by three. **Ten pointers sit below the insertion, at TWO distinct coordinates, over six files, and ZERO of them are live.**

| Cited line | Citing records | Verdict |
|---|---|---|
| `:412` | [ADR-386](ADR-386.md), [ADR-398](ADR-398.md), [ADR-401](ADR-401.md) | **LEFT.** All three name the coordinate as the SUBJECT of a finding about it. **Derived rather than inherited: the line is BLANK on this base**, which is the state those records report |
| `:638` | [ADR-337](ADR-337.md), [ADR-386](ADR-386.md), [ADR-388](ADR-388.md), [ADR-389](ADR-389.md), [ADR-398](ADR-398.md), [ADR-401](ADR-401.md) twice | **LEFT.** Already ruled BORN WRONG and class two by [ADR-388](ADR-388.md) and [ADR-389](ADR-389.md). **The line holds a section HEADING today and not the register row those records name** |

**ZERO CITATION REPAIRS ARE FORCED BY THIS DIFF, AND THAT RESOLVES A FENCE COLLISION RATHER THAN CREATING ONE.** One of the ten is in [ADR-388](ADR-388.md), which is row `405`'s and which this row may not write, and two are in [ADR-401](ADR-401.md). **Because the correct disposition is to LEAVE every one of them, this row needs no edit in any file its fence excludes.** It is recorded so those rows know the shift happened and know it was classified.

**THE OTHER THREE REGISTRIES TAKE APPENDS AND THAT WAS SWEPT FOR RATHER THAN ASSUMED.** [`decisions/README.md`](README.md)'s highest cited line is **378** in a file of **429**, and [`STATE.md`](../STATE.md)'s is **12,814** in a file of **13,863**, so both insertions sit below every pointer into them. **[`sessions/README.md`](../sessions/README.md) was NOT hand-edited**: its row arrives inside the `session_entries` generated span by `gates.mjs generate` reading this row's own index line, which is [ADR-396](ADR-396.md) section 8's finding obeyed rather than rediscovered.

---

### 13. Verification, every figure derived at the moment of reporting

| Runner | Base on `1f621d5c` | After this diff |
|---|---|---|
| `node scripts/corpus/gates.mjs check` | 33 of 33 | **33 of 33** |
| `node packages/tooling/checks/repo-invariants.mjs` | 35 of 35 | **35 of 35** |
| `pnpm vitest run` | 336 / 8,109 / 10 / 0 | **336 / 8,109 / 10 / 0**, delta ZERO on every axis |
| `pnpm run typecheck` / `lint` / `format:check` | clean | **clean, clean, clean** |
| `node scripts/corpus/gates.mjs generate` | | rows added in the two registry READMEs and `adr_count` advanced, all in place |

**THE STANDING BAR IS UNTOUCHED.** No invariant minted, no `RI-` number reserved, no `CI-06` letter spent, no gate added, widened or weakened, no test skipped, disabled or quarantined, no register grown, no migration taken or reserved, no port wired, no job scheduled. **[`gates.mjs`](../../scripts/corpus/gates.mjs) and [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) were READ and never opened for writing**, so both are byte-identical to their base and the reader was obtained by relocating the checks directory OUT of the tree. **`pnpm run verify` and [`falsify.mjs`](../../scripts/corpus/falsify.mjs) were never run.** No line number in [`gates.mjs`](../../scripts/corpus/gates.mjs) or under `docs/ops/runbooks/**` is cited anywhere in this entry: section 9's insertion points are named by what sits at them and carry no coordinate.

**ONE DEVIATION FROM THE DISPATCH IS RECORDED RATHER THAN TAKEN QUIETLY.** It named this row's wave `404s` and its base branch `claude/wave-401s`; the base commit `1f621d5c` is the tip of that branch, verified before branching, and every commit here is dated 2026-09-07, so the session log carries that date.

---

### 14. Where this entry is most likely wrong

1. **THE SURVIVING-INTRODUCTION RULE READS THE FIRST PARENT AND SAYS SO NOWHERE IN ITS OWN CLAUSE.** `git rev-parse <sha>^` is the first parent, and on a merge commit that is a choice. **This is [ADR-401](ADR-401.md) section 5's finding reappearing inside the remedy for a different hole**, and the honest reading is that the closure inherits one unstated parameter while removing another. It is stated here and in the instrument's own comment; it is not stated in the one-sentence clause, and a row quoting only the clause would be as unreproducible as the rule it replaces.
2. **ZERO DATED POINTERS MOVED IS A RESULT ABOUT THIS CORPUS AND NOT ABOUT THE RULE.** The four that move are in a live registry and a test file. **A wave that churns a dated record's coordinates would find dated instances**, and the rule's value would then be measured rather than merely demonstrated. This row cannot say the hole is unimportant, only that it is currently cheap.
3. **SECTION 8's RULING COULD BE READ AS WIDENING "UNDECIDABLE" TO COVER A CENSUS'S OWN BLIND SPOTS.** A cited path outside this repository is one shape; a cited path inside it that a `.gitignore` hides would be another, and nothing here says whether that is the same rule. **The refusal was adopted because it reproduces [ADR-401](ADR-401.md) exactly, which is evidence about what that entry did and not proof about what is right.**
4. **THE COMMA-LIST CORRECTION IN SECTION 6 CHANGES A RETIRED HOLE'S POPULATION AND NOT ITS VERDICT**, so a founder may reasonably ask why it is a section. It is one because 55 is now in a dated record as the price of a hole and the price is 43, and because the same key would misprice any FUTURE measurement over repeated coordinates.
5. **THIS ROW DID NOT REPAIR A SINGLE POINTER.** It measured, ruled and built. [ADR-396](ADR-396.md) section 14 argued the repair work is losing; this row is consistent with that and is not evidence for it, since nothing here was a repair opportunity refused.

---

### 15. What is owed, stated precisely enough to be picked up

1. **THERE IS STILL NO RULE FOR WHAT AN UNDECIDABLE VERDICT MEANS.** [ADR-400](ADR-400.md) asked for one, [ADR-401](ADR-401.md) repeated it, and this row repeats it a third time with the ratio unchanged: the corrected instrument refuses **9** times corpus-wide where `blame` refuses **1**, and **7 to 1** in dated records. **No gate can ship on the corrected instrument until a refusal means something.** The closure in section 7 does not help, by design: it never converts a decidable pointer into a refused one.
2. **`CI-06e` STILL STATES NO LIMITATION.** Section 9 re-derives the price at **81 pointers, 64 of them dated**, and this diff does not move that region, so the clause is not written. **The condition [ADR-402](ADR-402.md) set has not arrived and is restated unchanged.**
3. **[ADR-403](ADR-403.md) SECTION 13 ITEM 3 IS FALSE AND IS A DATED RECORD.** Section 4. **This entry does not propose overwriting it**, and whether a discharged owed-item is annotated at its site is the same open question [ADR-400](ADR-400.md) section 17 item 3 and [ADR-403](ADR-403.md) section 13 item 1 both carry about a false non-numeric claim. **This is the third instance and the first where the false claim is about work that was already done.**
4. **THE OWED-ITEM LISTS ARE APPEND-ONLY AND ROT FROM THE BOTTOM.** Four consecutive entries carry an item three of them inherited without checking the row between them. **This is not a defect in any one entry** and no runner can check it, since an owed item is prose about the future. It is recorded because the remedy is cheap and social: a row that lists an item as owed names the last entry it checked.
5. **THE CONTAINER ARRIVES WITH NO REPOSITORY, SIX WAVES RUNNING.** Section 1. **It is the dispatcher's and no ADR can fix it.**

---

### 16. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** Inherited as well as owed: [ADR-357](ADR-357.md) and [ADR-360](ADR-360.md) through [ADR-403](ADR-403.md) are `proposed` and unsigned, two rows run beside this one holding `404` and `405`, and this row settles none of them.

  **What a founder read adds and this entry cannot.**

  **THE FIRST THING TO DECIDE IS WHETHER THE CLOSURE IN SECTION 7 IS WORTH TAKING AT ALL.** It is correct, it is one clause, it is demonstrated on a built case, and **it moves nothing in the population [ADR-388](ADR-388.md)'s ruling is about**. The argument for taking it is that the four pointers it does move were being called lucky accidents and are not; the argument against is that a rule bought for four live pointers is a rule the next row must state, obey and get right. **This row would take it, and records that the case for it is smaller than the case [ADR-396](ADR-396.md) made for the instrument it refines.**

  **THE SECOND IS THAT THE INSTRUMENT NOW HAS THREE HOLES ON RECORD AND THIS ROW CLOSED ONE, RETIRED ONE AND OPENED ONE.** Section 8's is the interesting one, because it is not about which commit to read. **A census can manufacture a class-two verdict out of nothing but its own scope**, and class two is the verdict that makes a pointer permanently unrepairable. Nobody has asked how many other shapes do that.

  **THE THIRD IS THE ONE THIS ROW WOULD PUT FIRST IF IT COULD ONLY KEEP ONE.** Section 4 is not a measurement error, it is a coordination failure, and it cost a whole row's dispatch. **The corpus has spent five consecutive waves refining an instrument, and this wave's dispatch asked for work that was already finished because the record saying so is one entry deeper than anybody looked.** Sections 5 through 8 are worth having. **They are not worth what was budgeted for them**, and a founder is better placed than a row to decide whether the sixth wave of pointer archaeology should happen at all.

- **Approval:** `___________________________` (UNSIGNED. **TOOLING AND CONTROL OVER THE CORPUS'S OWN POINTERS, `E2` READ OWED.** **Merging is not signing.**)

| | |
|---|---|
| **Approved by** | |
| **Date** | |
