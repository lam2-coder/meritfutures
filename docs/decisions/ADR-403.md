## ADR-403: the four `:35` pointers are CLASS TWO for the second time and the finding is now written beside all four, `CI-06a` has been checking the runbook links all along so nothing binds them because something already does, and `ADR-337:202` is CLASS ONE and repaired to a name (2026-09-07, status: proposed)

**[ADR-399](ADR-399.md) SECTION 12 ITEM 1 OWED AN ANNOTATION AT FOUR SITES, ITEM 4 OWED A RULING ON A BINDING, AND [ADR-398](ADR-398.md) SECTION 13 ITEM 1 OWED ONE REPAIR.** All three are discharged here. **Two of the three inherited premises survive re-derivation and one does not.**

**THE ONE THAT DOES NOT IS ITEM 4, AND IT FAILS AT ITS FIRST WORD.** It reads *"nothing checks the target exists"*. **`CI-06a` checks it, reads `docs/ops/runbooks/**` while doing so, and is one of the 33 gates every row in this programme has been running for weeks.** Section 6 watches it go red on a seeded rename of an `RB-nn` target and name the site. **There is no ruling to make about whether the binding is worth a check, because the check is already there and has been the whole time**, and the four waves that carried this item forward as an open question were carrying an open question that a green line in their own output had already closed.

**THE ONE THAT SURVIVES IS THE HARDER ONE, AND THE DISPATCH WAS RIGHT TO ASK.** It instructed this row to test whether *"these four must stay wrong"* is itself an inherited claim of the kind [ADR-399](ADR-399.md) demolished for the eight. **It is not. It reproduces at the blob, at both write commits, and the mechanism reproduces with it.** Section 4 derives `G2` independently and it FAILS on all four. **The four stay wrong, they are annotated rather than repaired, and this row would have said so and repaired them if the measurement had gone the other way.**

- **Origin:** [ALLOCATION](ALLOCATION.md) row `403`, branch `claude/the-finding-beside-the-pointer`, session **598**, off the wave integration branch `claude/wave-398s` at `28e2b0ee` rather than off `main`. The offset of 195 between the session number and the ADR number holds, as it did on rows `388`, `397`, `399` and `400`.
- **NON-MONEY. DATED RECORDS, ONE DECISION RECORD AND ONE TEST FILE'S HEADER COMMENT. Ships `proposed` with an UNSIGNED approval line. Merging is not signing. `E2` read owed.**
- **NO GATE WEAKENED, NO TEST SKIPPED, DISABLED OR QUARANTINED, NO INVARIANT MINTED, NO `RI-` NUMBER RESERVED, NO `CI-06` LETTER SPENT, NO MIGRATION TAKEN OR RESERVED, NO REGISTER GROWN, NO PORT WIRED, NO JOB SCHEDULED.**
- **NOT ONE EXECUTABLE LINE CHANGED.** The single edit outside `docs/` is a block of comment lines in [`digests.test.ts`](../../apps/worker/test/digests.test.ts). [`scripts/corpus/**`](../../scripts/corpus) and [`packages/tooling/**`](../../packages/tooling) were READ and never opened for writing, and [`CLAUDE.md`](../../CLAUDE.md) was read and not written. **`pnpm run verify` WAS NOT RUN AND [`falsify.mjs`](../../scripts/corpus/falsify.mjs) WAS NOT RUN.**
- **[`CRON_INVENTORY.md`](../ops/runbooks/CRON_INVENTORY.md) IS NOT EDITED BY THIS ROW EITHER**, which is the second consecutive wave to hold that file open and put it down. Section 6 gives this row's own reason, and it is not [ADR-399](ADR-399.md)'s.

---

### 1. The container, measured rather than assumed, because the dispatch asserted the opposite of what this one is

**THE STATED WORKING DIRECTORY `/home/user` WAS EMPTY** and `git rev-parse --show-toplevel` returned *fatal: not a git repository*. **That is the fifth consecutive row to measure no repository on arrival**, after [ADR-394](ADR-394.md), [ADR-396](ADR-396.md), [ADR-397](ADR-397.md) and [ADR-400](ADR-400.md). The repository was not named in the dispatch and no source was attached; it was recovered from the account's repository list as `lam2-coder/meritfutures`.

**THE DISPATCH STATED THAT ITS OWN CONTAINER REPORTS `true` AND INSTRUCTED THIS ROW TO MEASURE RATHER THAN INHERIT. MEASURED, IT IS `false`.**

| Measured at the moment of reporting | |
|---|---|
| `git rev-parse --is-shallow-repository` | **`false`** |
| `git rev-list --count 28e2b0ee` | **4,320** |

**SO THE CLONE IS FULL, AND THIS MATTERS TO EVERY VERDICT BELOW RATHER THAN BEING AN ENVIRONMENT NOTE.** Sections 4 and 8 decide two classes by reading three blobs at three commits between 2026-08-28 and 2026-09-05. **A shallow clone would have answered those questions wrongly rather than refusing to answer them.** [ADR-400](ADR-400.md) section 17 item 5 already ruled that the standing claim that a clone IS shallow is wrong while the standing advice to check it is right, and this row is the next data point for that and not a new finding.

---

### 2. The base, reproduced on `28e2b0ee` before the first edit

| | Base, before any edit | After this diff |
|---|---|---|
| Test files | **336** | **336** |
| Passed | **8,109** | **8,109** |
| Skipped | **10** | **10** |
| Failed | **0** | **0** |
| Gates | **33 of 33** | **33 of 33** |
| Invariants | **35 of 35** | **35 of 35** |
| `typecheck`, `lint`, `format:check` | clean | clean |

**DELTA ZERO ON EVERY AXIS, AND THE DELTA ATTRIBUTED BY REMOVAL IS ZERO BECAUSE NOTHING WAS REMOVED.** No test file was added, no case was added or deleted, and the skip count is unchanged. **Nothing else could be true of a diff whose only non-document edit is fourteen comment lines.**

---

### 3. The population, re-derived, and [ADR-399](ADR-399.md)'s table sums to the right total through two offsetting grain shifts

A raw grep for `CRON_INVENTORY(\.md)?:NN` over the whole tree finds **20 tokens**, which reproduces [ADR-399](ADR-399.md) section 2 exactly. **Its four-row table also sums to 20. The two facts are independent and the table gets there by two departures from the token grain that cancel, neither of which it states.**

| [ADR-399](ADR-399.md) section 2's row | It reports | Raw grep tokens | The departure |
|---|---|---|---|
| The eight it owned | **8** | **7** | [session 166](../sessions/2026-08-24-session-166.md):52 writes one token, `:67-68`, aimed at two adjacent rows with two subjects. Counted as COORDINATES, correctly, because the repair grain is the subject |
| `:27` | 4 | 4 | none |
| `:35` | **4** | **5** | [ADR-388](ADR-388.md):28 carries a fifth `:35` and it is dropped, correctly, because it is a SPECIMEN OF THE FORM and not a pointer |
| `:23-40` | 4 | 4 | none |

**BOTH DEPARTURES ARE INDIVIDUALLY RIGHT AND THE COLUMN IS HEADED "Tokens".** This is a presentation defect and not a wrong verdict, and it is recorded because the next row to re-derive this population will grep, get 20, read a table that says 20, and conclude the two agree at the same grain. **They do not, and the four this row annotates is four for a reason that table does not give.**

**THE FIFTH `:35` IS THE REASON THE FOUR IS FOUR, AND IT IS WORTH NAMING.** [ADR-388](ADR-388.md):28 writes `` `CRON_INVENTORY:35` `` inside the sentence *"as `` `CRON_INVENTORY:35` `` is written in four places"*. **It is a quotation of the shape being counted, not a claim about line 35**, so it names no subject, `G1` cannot be asked of it, and annotating it would be annotating an example. It is also row `401`'s file this wave and outside this fence in both directions. **The corpus contains at least two of these mention-not-use specimens and section 6 finds the other ten**, so this is a shape rather than an instance.

---

### 4. `G2` derived at the blob on all four, and the dispatch's invitation to refute it is declined with the measurement

**THE DISPATCH ASKED THIS ROW TO CHECK WHETHER *"THESE FOUR MUST STAY WRONG"* IS AN INHERITED CLAIM OF THE KIND [ADR-399](ADR-399.md) DEMOLISHED FOR THE EIGHT, AND SAID THAT REPAIRING THEM WOULD BEAT ANNOTATING THEM.** It would have. **The measurement does not permit it.**

The instrument is [ADR-396](ADR-396.md) section 7's, `git log -S` over the coordinate token, oldest result, and `git blame -w` was run beside it rather than instead of it. **The two agree on all four sites.**

| Citing site | Write commit | `git blame -w` |
|---|---|---|
| [`digests.test.ts`](../../apps/worker/test/digests.test.ts):60 | `a21bfb8e`, 2026-08-28 | agrees |
| [STATE](../STATE.md):6095 | `4b3e8e57`, 2026-08-28 | agrees |
| [session 323](../sessions/2026-08-28-session-323.md):9 | `4b3e8e57`, 2026-08-28 | agrees |
| [`sessions/README.md`](../sessions/README.md):556 | `4b3e8e57`, 2026-08-28 | agrees |

**READ AT BOTH WRITE COMMITS, THE RUNBOOK IS THE SAME FILE AT 102 LINES AND THE ANSWER IS THE SAME.**

| At `a21bfb8e` and at `4b3e8e57` | |
|---|---|
| Runbook line **35** | the **Tier-1 economic calendar staleness check** row |
| The **Scheduled digest delivery** row, which is the subject | line **36**, ONE LINE BELOW |
| Occurrences of the house phrase *"It asserts the query, not the job"* | **5**, then and now |

**`G1` HOLDS AND `G2` FAILS, WHICH IS [ADR-388](ADR-388.md) CLASS TWO, AND REPAIR IS FORBIDDEN.** `G1` holds because every one of the four names its subject in a token a reader can search the cited file for: three of them quote the rule and name a `delivered` row, and the fourth names `report_deliveries`. `G2` fails because the cited line held the named TOKEN and not the named SUBJECT.

**AND THE MECHANISM IS NOT MERELY REPRODUCED, IT IS SHARPER AT THE BLOB THAN IN PROSE.** [ADR-399](ADR-399.md) section 6 says the wrong row carried the same house phrase. **Read whole, line 35 at the write commit ends with *"evaluated independently of whether any loader reported success"* and line 36 ends with *"evaluated independently of whether any delivery run reported success"*.** The two rows are not merely similar. **They are the same sentence with one noun changed**, and the records quote the version with the noun that belongs to line 36. **The author landed one row above the right one on a line that differed from their subject by a single word.**

**SO THE FOUR ARE LEFT WRONG AND THE FINDING IS WRITTEN BESIDE THEM**, which is [ADR-388](ADR-388.md) section 2's second outcome doing exactly what it says and what no row had yet done here.

---

### 5. The four annotations, each read back at its own line after the edit

**THE FORM IS [ADR-394](ADR-394.md) SECTION 7's AND IT WAS COPIED RATHER THAN INVENTED**, which is what the dispatch asked for and what [ADR-397](ADR-397.md) section 7 ruled: the finding goes AT THE SITE, the wrong pointer is kept whole, and the retired coordinate is NAMED rather than re-cited with its path.

| Site | Shape | Line-count neutral | Read back |
|---|---|---|---|
| [STATE](../STATE.md):6095 | hand-written prose, appended in place to the sentence carrying the pointer | **yes**, 1 insertion against 1 deletion | the pointer is still wrong, still at line 6095, and the finding sits after it |
| [session 323](../sessions/2026-08-28-session-323.md):9 | the `<!--index:` marker, inserted immediately after the clause carrying the pointer rather than at the line's end | **yes**, 1 against 1, still one well-formed marker line | same |
| [`sessions/README.md`](../sessions/README.md):556 | **GENERATED. Not hand-edited.** Written by `gates.mjs generate` from the marker above | **yes**, 1 against 1, at line 556 exactly | same |
| [`digests.test.ts`](../../apps/worker/test/digests.test.ts):60 | a source comment | **NO. Fourteen lines inserted**, section 9 | the pointer is still at line 60 and still wrong |

**THE THREE DOCUMENT ANNOTATIONS CARRY NO BACKTICKED `path:line` TOKEN OF THEIR OWN**, so no reader in this repository resolves any of them as a citation, and the coordinate is named as the bare word *"coordinate 35"* rather than reproduced in the form it is retired in. **The source comment spells it in words and writes no backticked number at all**, which is [ADR-388](ADR-388.md) section 4's rule for source scope: `RI-15` inherits a path across `CITATION_INHERIT_LINES`, so a bare backticked number within six lines of a path would become a live citation that the correction itself says is retired.

---

### 6. RULED, AND THE PREMISE IS REFUTED FIRST: `CI-06a` has been binding these links all along

**[ADR-399](ADR-399.md) SECTION 12 ITEM 4, RAISED FIRST BY [ADR-392](ADR-392.md) SECTION 11 ITEM 5, READS: *"Three of the 21 notes carry an `RB-nn` link and nothing checks the target exists"*.** The first half reproduces. **The second half is false.**

**THE FIGURES FIRST, ALL DERIVED HERE.** The document holds **21 scheduled rows** between its own table bounds and **21 severity notes** under `## Severity if absent, by job`, which reproduces [ADR-395](ADR-395.md) and [ADR-399](ADR-399.md) at 21 against 21 for the third time. **Three of the 21 notes carry an `RB-nn` link**, at lines 53, 57 and 133. **The document carries SIX such links in total**, the other three in the `## Calendar work, not cron` table at lines 185, 187 and 191, which the owed item does not mention and which a row pricing "the binding" would need. **All six targets exist**, and every distinct `RB-nn` number referenced anywhere in this corpus, `RB-01` through `RB-11`, has a file.

**AND `CI-06a` IS A LINK CHECK OVER EVERY TRACKED MARKDOWN FILE.** Its own `covers` line reads *"relative markdown links and their #anchors, resolved against the tree"*. It resolves each target against the tree and reports `(no such file)` by site. **It was PASSING on this base, which is why nobody looked at it**, and a passing check that nobody looked at is exactly the shape [ADR-399](ADR-399.md)'s own section 13 warns about in the other direction.

**SEED G, WATCHED RED RATHER THAN ARGUED.** `RB-01`'s target in the severity note at line 57 was renamed to a file that does not exist, changing nothing else.

| Seed | Result |
|---|---|
| **G**: `[RB-01](RB-01-nightly-batch-failure.md)` at line 57 pointed at `RB-01-nightly-batch-renamed.md` | Gates **32 of 33**. `CI-06a` reports **`docs/ops/runbooks/CRON_INVENTORY.md:57 -> RB-01-nightly-batch-renamed.md (no such file)`** |

**Restored byte-identically, verified with `sha256sum -c`, and not with `git checkout --`.**

**SO THERE IS NOTHING TO MINT AND NOTHING TO REFUSE, AND THOSE ARE DIFFERENT OUTCOMES.** Eight consecutive waves refused a gate with a measurement and this row is not the ninth. **It did not weigh a proposed check and decide it was not worth the cost. It found the check running.** The honest disposition of the owed item is that it was never open.

**THE COST SIDE WAS PRICED ANYWAY, BECAUSE THE ITEM ASKED FOR IT AND BECAUSE A REFUTED PREMISE CAN STILL SIT ON TOP OF A REAL COST.** It does not here, and the three measurements are worth keeping.

1. **THE HAZARD HAS NEVER OCCURRED.** Over the whole 4,320-commit history, `docs/ops/runbooks/` has taken **15 file additions and ZERO renames and ZERO deletions**. The only event that can dangle an `RB-nn` link has not happened once.
2. **THE SCOPE IS ALREADY CLEAN.** Of **225 relative markdown links** in `docs/ops/runbooks/**`, **zero** fail to resolve.
3. **AND A NAIVE VERSION OF THE CHECK SOMEBODY MIGHT HAVE WRITTEN WOULD HAVE BEEN WORSE THAN NOTHING.** An existence check that does not exempt inline code goes red on **23** targets corpus-wide, and **10 of the 23 are metasyntactic placeholders** such as `path.md`, `ADR-N.md` and `ADR-nnn.md` inside prose describing the link form. **`CI-06a` strips inline code and fenced blocks before it reads a link and reports none of the ten.** That exemption is the whole difference between the gate that exists and a gate that would have demanded ten repairs that must not be made, and it is the same mention-not-use shape section 3 found at [ADR-388](ADR-388.md):28.

**AND THE RUNBOOK IS STILL NOT EDITED, ON A REASON THAT IS THIS ROW'S RATHER THAN [ADR-399](ADR-399.md)'s.** That entry gave three, of which the third is the one that survives contact with this item: **it is a runbook a human reads under pressure.** A note reading *"nothing checks this"* would be false after section 6 and would still be noise to an operator at 3am if it were true. **The finding about a check belongs in the record of the row that took the measurement, and the operator's page carries operations.**

---

### 7. RULED: the annotation convention reaches a GENERATED line by not reaching it, and the site is the source

**[ADR-400](ADR-400.md) SECTION 17 ITEM 3 LEFT [ADR-397](ADR-397.md) SECTION 7's SCOPE UNRULED AND SAID IT WAS NOW A LIVE QUESTION WITH AN INSTANCE. THE DISPATCH PREDICTED THIS ROW WOULD MEET IT. IT DID, ON A DIFFERENT EDGE THAN THE ONE THAT ENTRY NAMED.**

That entry's open edge is whether the convention reaches NON-NUMERIC claims. **This row's edge is whether it reaches a line that is not written by hand.** [`sessions/README.md`](../sessions/README.md):556 is one of the four sites owed an annotation, and its content is generated verbatim from [session 323](../sessions/2026-08-28-session-323.md)'s own `<!--index:` marker. **`gates.mjs generate` does not annotate that line. It replaces it.**

**RULED: THE SITE IS THE SOURCE LINE, AND RE-RENDERING A GENERATED COPY IS NOT OVERWRITING A RECORD.** [ADR-397](ADR-397.md) section 7's prohibition protects a claim somebody made: it forbids editing a sentence so that it reports a derivation nobody performed. **A generated line makes no claim of its own.** It is a rendering of a claim held one file away, its content is not evidence of anything except what the generator read, and the record whose honesty the convention protects is the marker. **So the annotation is written at the marker, by hand, in place; the copy is regenerated; and nothing is overwritten in the sense the rule means.**

**THIS IS NOT A FOURTH CONVENTION AND IT MINTS NOTHING.** It is the existing rule applied to a line that has an author elsewhere, and it changes no verdict in this corpus: the only lines it reaches are generated ones, and no generated line in this corpus has ever been the subject of an annotation before this diff. **It does not answer [ADR-400](ADR-400.md)'s question about non-numeric claims**, which is still open and is still not this row's.

---

### 8. [ADR-337](ADR-337.md):202 is CLASS ONE, derived rather than inherited, and repaired to a NAME

**[ADR-398](ADR-398.md) SECTION 13 ITEM 1 HANDED THIS OVER AS *"the same sentence at `:8412`, the original coordinate, which has been wrong since `c5648f00`"*, repaired in its three siblings by [ADR-393](ADR-393.md) and missed there. That is accurate about the tree and it is not a class.**

**THE CLASS IS DERIVED HERE AND IT IS ONE.** The write commit is `7bfd86f6`, 2026-09-05, and `git log -S` and `git blame -w` agree on it.

| At `7bfd86f6`, the commit that wrote [ADR-337](ADR-337.md):202 | |
|---|---|
| [`gates.mjs`](../../scripts/corpus/gates.mjs) line **8412** | `'packages/db/DELTA_MANIFEST.md',`, the `RETIRED_REGISTER` key for exactly the document this row was editing |
| Two lines below it | `sites: 7` |

**SO BOTH HALVES OF THE TOKEN WERE EXACT ON THE DAY THEY WERE WRITTEN, AND THIS IS THE CLEANEST `G2` PASS AVAILABLE**: the cited line does not merely contain the subject, it IS the subject's name in the register. `G1` holds because the cell names `CI-06/retired-constraints` and the value it read. **`G1` and `G2` both hold and the pointer is wrong today, so repair is PERMITTED** under [ADR-388](ADR-388.md) section 2's first outcome, and [ADR-397](ADR-397.md)'s precondition is satisfied because the register entry is still there to repair to.

**WHERE IT WENT, DERIVED ACROSS THE REFS RATHER THAN INFERRED FROM THE TWO THAT WERE HANDED OVER.**

| Ref | Register key line | Value |
|---|---|---|
| `7bfd86f6` | **8412** | **7** |
| `c5648f00` | 8579 | 10 |
| `ebc21238` | 8579 | 10 |
| `c219c0d1` | 8667 | 10 |
| `28e2b0ee`, this row's base | **8667** | **10** |

**THE COORDINATE AND THE CARDINAL FALSIFY AT THE SAME COMMIT HERE, WHICH IS THE OPPOSITE OF [ADR-398](ADR-398.md) SECTION 5's THREE SIBLINGS AND IT IS WHY THIS ONE WAS MISSED.** Those three had already been condemned on their cardinal, which took the whole token out of a coordinate sweep. **This one was never condemned on anything. It was simply not in a fence**, and [ADR-393](ADR-393.md) repaired the three that were.

**THE REPAIR IS [ADR-398](ADR-398.md) SECTION 6's RULING APPLIED WITHOUT AMENDMENT, IN BOTH OF ITS HALVES.**

- **The cardinal `7` is UNTOUCHED and its tense is dated.** *"registers this document at `sites: 7`"* became *"registered this document at `sites: 7` as the register read on this row's base commit"*. Rewriting 7 to 10 would make [ADR-337](ADR-337.md) claim it measured 10 at a commit where the register read 7, which is [ADR-367](ADR-367.md)'s prohibition and has now been declined by four consecutive rows.
- **The coordinate is DELETED and replaced by the register entry NAMED**, with `` `:8412` `` recorded retired in backticks and without a path. **The reason is not that the number is unknowable. It is 8667 and this row derived it.** It is that [`gates.mjs`](../../scripts/corpus/gates.mjs) is read-only to this whole wave and **row `402` is restructuring 30 of 33 `covers` definitions inside it**, so 8667 is a number known to be wrong before this diff lands. **A coordinate repaired to a value its own wave invalidates is the defect re-armed with a fresher date**, which is [ADR-399](ADR-399.md) section 8's reasoning and [ADR-394](ADR-394.md) section 5's before it.

**AND THE REPAIRED CELL SAYS SOMETHING THE THREE SIBLINGS' CELLS CANNOT.** Theirs record a coordinate that drifted twice and was repaired twice. **This one records that its coordinate was EXACT at the commit that wrote it**, which is the fact that makes it class one rather than class two, and it is written into the cell so the next reader does not derive it a second time.

---

### 9. Citations this diff moved, derived before the prose was written, and the ONE file that could not be neutral

**EVERY ANNOTATION AND EVERY REPAIR IS STRICTLY LINE-COUNT NEUTRAL EXCEPT ONE, AND THE THREE FILES THAT CARRY NOTHING ELSE PROVE IT AT THE FILE LEVEL**: [ADR-337](ADR-337.md) **269 lines before and after**, [ADR-320](ADR-320.md) **153 and 153**, [session 323](../sessions/2026-08-28-session-323.md) **77 and 77**, each **1 insertion against 1 deletion**. **[STATE](../STATE.md) AND [`sessions/README.md`](../sessions/README.md) TAKE THEIR ANNOTATION THE SAME WAY, ONE LINE REPLACED IN PLACE AT 6095 AND AT 556**, and grow only at their tails, by this row's appended session section and session row and by the `adr_count` span at [STATE](../STATE.md) line 32. **Every pointer aimed into either file sits above every line this diff adds to it**, which was checked rather than assumed. **So no citation into any of the five moved.**

**[`digests.test.ts`](../../apps/worker/test/digests.test.ts) COULD NOT BE, AND THE DISPATCH ASKED THIS ROW TO SAY SO RATHER THAN QUIETLY TAKE IT.** The annotation is fourteen comment lines. **The neutral alternative was one comment line of about a thousand characters**, in a file whose longest line is 108 and whose longest comment is 86, under a `printWidth` of 100. Prettier does not reflow comments, so that line would have passed `format:check` and would have been a style discontinuity of exactly the kind Appendix F2 names as a code-level tell. **Neutrality was available and was rejected on the quality of the artifact, which is a judgement and is stated as one.**

**TWO POINTERS AIM INTO THAT FILE AND EXACTLY ONE MOVED.**

| Pointer | Before | After | Disposition |
|---|---|---|---|
| [ADR-384](ADR-384.md):79 | `digests.test.ts:60` | unmoved | **Nothing owed.** The insertion is at line 64, below it. Line 60 still carries the `:35` pointer this row annotates, so the record that describes it still lands on the line that holds it |
| [ADR-320](ADR-320.md):75 | `digests.test.ts:1155` | `:1169` | **REPAIRED HERE**, under the citation-repair exception |

**THE ONE REPAIR IS LISTED IN FULL AND ITS CLASS WAS DERIVED RATHER THAN WAIVED.** [ADR-320](ADR-320.md):75 cites the line beside the words *"case `9.3`"*. `G1` holds on that token. `G2` holds: at `9f04aeff`, the commit that wrote the citing line, line 1155 held `test('9.3 the three lists cover every module under src/, and nothing appears twice', ...)`, and it still held it on this row's base. **It was exact for its whole life and this diff is what moved it**, which is [ADR-390](ADR-390.md) section 9's rule read forwards: a pointer that goes stale BECAUSE of this diff is this diff's to repair. **It is repaired to a number rather than to a name because nothing in this wave touches `apps/worker/test/` again**, and the retired `` `:1155` `` is named beside it without a path.

**AND THE REGISTRY ROWS THIS DIFF APPENDS MOVE NOTHING.** [ALLOCATION](ALLOCATION.md)'s ADR table, [`decisions/README.md`](README.md) and [`sessions/README.md`](../sessions/README.md) grow at the end of their spans and [STATE](../STATE.md) gains a section at its end. **The `sessions/README.md` row for this session is appended BELOW line 556**, so the annotation this row put at 556 is not moved by this row's own registry row, which was checked rather than assumed.

---

### 10. Falsification: three of the four annotations are unreachable by every check in this repository

**[ADR-399](ADR-399.md) SECTION 7 MEASURED THIS FOR THE EIGHT AND THE SAME ANSWER HOLDS FOR THE FOUR, WHICH IS WHY IT IS RE-MEASURED AND NOT CITED.**

| Seed | What was done | Gates | Invariants |
|---|---|---|---|
| **G** | Section 6's `RB-nn` rename in the runbook | **32 of 33**, `CI-06a` naming the site | **35 of 35** |
| **H** | [STATE](../STATE.md):6095's annotated pointer aimed at a coordinate 9,000 lines past the end of the runbook | **33 of 33** | **35 of 35** |

**SEED G IS THE ONLY THING IN THIS DIFF A RUNNER CAN SEE, AND IT IS NOT ONE OF THE ANNOTATIONS.** `RI-16` masks every dated record through `RECORD_HEADING`, `RI-15` reads source comments and no `.md`, and none of the four annotations contains a resolvable citation for either to read. **So the four annotations are held up by nothing except that they were read**, and each was: every one was read back at its own line after the edit landed, and the two class derivations were taken at the blob and not at this tree.

**Every seed was taken on the real file and every restore was verified byte-identical with `sha256sum -c`. None was taken with `git checkout --`.**

---

### 11. Verification, every figure derived at the moment of reporting

- **Suite: 336 files / 8,109 passed / 10 skipped / 0 failed**, against the base's own **336 / 8,109 / 10 / 0** reproduced on `28e2b0ee` **BEFORE the first edit**. **Delta ZERO on every axis.**
- **Gates 33 of 33** after `generate`, **invariants 35 of 35**, `typecheck`, `lint` and `format:check` all clean.
- **`CI-06a` WAS WATCHED RED TWICE AND BOTH REDS WERE THIS ROW'S OWN.** Once on seed G, and once before [ADR-403](ADR-403.md) existed, when the four annotations' forward links to it reported `(no such file)` at exactly the four sites. **That second red is an unplanned confirmation of section 6's whole argument**: the gate named all four annotation sites by file and line, from a standing start, with no seed.
- **The gate count is derived from the runner's own closing line and not carried**, on a wave in which row `402` edits [`gates.mjs`](../../scripts/corpus/gates.mjs). `gates.mjs generate` reports every span already matching its query, so no generated span moved and `gate_count` still derives 33.
- **`adr_count` moves by one, generated rather than typed**, and the decisions registry row is generated too.
- **THE DIFF, DERIVED AT REPORTING TIME FROM `git diff --numstat`.** [ADR-337](ADR-337.md), [ADR-320](ADR-320.md) and [session 323](../sessions/2026-08-28-session-323.md) at **1 and 1** each, the three files this row edits for the annotation or the repair alone. [`digests.test.ts`](../../apps/worker/test/digests.test.ts) **+14/-0**, comment lines only. [ALLOCATION](ALLOCATION.md) **+3**, one row per wave member. [`decisions/README.md`](README.md) **+1**, generated. [`sessions/README.md`](../sessions/README.md) **+2/-1**: the annotation rewritten in place at line 556 and this session's row appended at line 822, both generated. [STATE](../STATE.md) **+26/-2**: the annotation in place at 6095, the `adr_count` span at 32, and a session section appended at the end. **Two new files**, [ADR-403](ADR-403.md) at 266 lines and the session log at 41.
- **`pnpm run verify` WAS NOT RUN AND [`falsify.mjs`](../../scripts/corpus/falsify.mjs) WAS NOT RUN.** Both mutate the working tree. Each leg was invoked directly instead.
- **NO SEED FILE WAS LEFT MODIFIED**, and `sha256sum -c` reported `OK` on each restore.

---

### 12. Where this entry is most likely wrong

- **SECTION 6 REFUTES AN ITEM THAT TWO ENTRIES RAISED, AND THE REFUTATION IS ONE GATE READ.** If `CI-06a`'s `markdownFiles()` excluded some path this row did not test, the conclusion would be right for the six links measured and wrong as a general claim. **Seed G is the mitigation and it is one instance in one file**: it proves the gate reaches `docs/ops/runbooks/CRON_INVENTORY.md` and proves nothing about any directory nobody broke a link in.
- **THE `:35` VERDICT RESTS ON THE SAME INSTRUMENT [ADR-400](ADR-400.md) MEASURED AS UNSETTLED.** That entry found two defensible instruments disagreeing on 23 of 493 verdicts. **These four agree under both**, which is what a small sample of unreflowed lines looks like and is not evidence the instrument is sound.
- **THE FOURTEEN-LINE INSERTION IS A TASTE JUDGEMENT WITH A REAL COST.** It broke a pointer that had been exact for its entire life, and the repair spends a coordinate into a file another row could still touch. **A reader who weights neutrality above comment style would take the thousand-character line and owe nothing**, and that reader is not obviously wrong.
- **SECTION 7 RULES ON A GENERATED LINE FROM ONE INSTANCE.** One generated site, one generator, one annotation. **The ruling is stated as an application of an existing rule precisely because a convention derived from a single observation should not be a new convention**, but it is still a single observation.

---

### 13. What is owed, stated precisely enough to be picked up

1. **[ADR-399](ADR-399.md) SECTION 12 ITEM 4 IS DISCHARGED BY REFUTATION AND ITS SOURCE IS NOT.** [ADR-392](ADR-392.md) section 11 item 5 raised it first and still carries the false clause. **It is a dated record and this entry does not propose overwriting it**; whether it should be annotated at its own site under [ADR-397](ADR-397.md) section 7 is exactly [ADR-400](ADR-400.md) section 17 item 3's open question about non-numeric claims, now with a second instance.
2. **`G1` SHOULD REQUIRE A TOKEN UNIQUE IN THE CITED FILE**, [ADR-399](ADR-399.md) section 12 item 2, still owed and still not taken. **Section 4 is a second receipt for it and a sharper one**: the two candidate lines differ by one noun, so a uniqueness condition on the PHRASE would have caught these four and a condition on any coarser token would not.
3. **[ADR-388](ADR-388.md)'s CENSUS OF 289 HAS STILL NEVER BEEN RE-RUN UNDER `git log -S`**, owed by [ADR-396](ADR-396.md) section 7 and again by [ADR-399](ADR-399.md) section 12 item 3. [ADR-400](ADR-400.md) priced the instrument disagreement corpus-wide but did not re-run the census itself.
4. **[STRATEGY](../testing/STRATEGY.md) SECTION 4.4's `CI-06l` ROW** was row `398`'s and [ADR-398](ADR-398.md) section 8 reports it repaired. **This is recorded as discharged rather than owed**, and it is listed so the fourth consecutive appearance of an item stops looking open.
5. **THE CONTAINER ARRIVES WITH NO REPOSITORY, FIVE WAVES RUNNING.** Section 1. **It is the dispatcher's and no ADR can fix it**, which is [ADR-394](ADR-394.md), [ADR-397](ADR-397.md), [ADR-398](ADR-398.md) and [ADR-400](ADR-400.md) repeated because nothing has changed.

---

### 14. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** Inherited as well as owed: [ADR-357](ADR-357.md) and [ADR-360](ADR-360.md) through [ADR-400](ADR-400.md) are `proposed` and unsigned, two rows run beside this one holding `401` and `402`, and this row settles none of them.

  **What a founder read adds and this entry cannot.**

  **THE FIRST THING TO DECIDE IS WHAT AN OWED ITEM IS WORTH, BECAUSE THIS ROW IS THE THIRD CONSECUTIVE RECEIPT AGAINST THEM.** [ADR-394](ADR-394.md) found a wrong figure in one, [ADR-399](ADR-399.md) found four waves carrying a conclusion without its caveat, and this row found an item that was never open: **a check that would have answered it was passing in every one of those waves' own output.** The owed list exists so a finding can be picked up without re-derivation. **It has now handed forward a wrong figure, a wrong prohibition and a wrong absence, and in all three cases the row that re-derived it was the row that caught it.** The convention is buying speed with accuracy and the exchange rate is getting worse, not better.

  **THE SECOND IS THAT THE CORPUS DOES NOT KNOW WHAT ITS OWN GATES CHECK.** Section 6 is not a story about a careless entry. `CI-06a`'s `covers` line says plainly what it does, it has run in every wave, and two separate entries wrote down that nothing checked a thing it checks. **Row `402` is restructuring 30 of 33 `covers` definitions this wave**, and the question that makes urgent is whether those strings are read by anybody before a row states an absence. **A gate nobody can enumerate is a gate that gets re-proposed**, and this corpus has spent parts of nine rows refusing gates.

  **AND THE THIRD IS THE ONE THIS ROW WOULD PUT LAST AND MEANS MOST.** Four sites now carry a paragraph explaining why a wrong number is deliberately left wrong, and three of them are unreachable by every check in this repository. **The annotation programme's entire product is prose that only a person will ever read, in records only a person will ever open**, and its value is precisely that it stops the fifth person re-deriving what four already have. **That is real and it is also unbounded**: [ADR-388](ADR-388.md) measured 3,536 drifted pointers in dated records, [ADR-400](ADR-400.md) ruled `G2` unenforceable, and at four sites a wave this programme finishes somewhere around never. **The founder question is not whether these four were worth annotating. It is whether the corpus should keep writing coordinates into records it cannot check**, and [ADR-388](ADR-388.md)'s option (b) adopted naming for new pointers and refused it as a migration. **Three consecutive waves have now repaired a pointer to a NAME because a number was known to be unstable. Nobody has priced the migration and it keeps happening one row at a time.**

- **Approval:** `___________________________` (UNSIGNED. **`E2` READ OWED.** **Merging is not signing.**)

| | |
|---|---|
| **Approved by** | |
| **Date** | |
