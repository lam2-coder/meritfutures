## ADR-441: the population under `apps/worker` is NINE and not five, the two ADR-440 missed are line-wraps in its own grep, and "no cited line moves" bought room line-neutrality would have refused (2026-09-07, status: proposed)

**Row 441 of the 438s wave. Branch `claude/five-copies-no-cited-line-moves`, off `claude/wave-438s` at `9847bcd0`, session 636.**

**ALL NINE ARE REPAIRED AND NOT ONE CITED LINE MOVED.** 101 citations reach the six files this row touched, over 44 distinct lines. **39 of those 44 lines hold bytes identical before and after**; the other 5 are the claim sites themselves, still at `:139`, `:387`, `:218`, `:773` and `:684`, each now carrying its own correction. **`RI-14`, `RI-15` and `RI-16` PASS before and after**, and `RI-15` was watched RED on all ten citations this row added, then restored byte-identically (section 6).

**AND THE COMMISSIONED COUNT IS THE FIRST THING THIS ROW HAD TO CORRECT.** [ADR-440](ADR-440.md) section 12 item 1 says **five** copies under `apps/worker/**`. **There are nine live false sites and the count was low for two separate reasons**, both mechanical and both derivable: [ADR-440](ADR-440.md) section 3's grep needle `` compares a `schema.ts` column `` **wraps across a line break** at [`rows.ts:28`](../../apps/worker/src/digests/rows.ts) and [`breaker.test.ts:374`](../../apps/worker/test/breaker.test.ts), and a **fifth wording** it never enumerated, *"`schema.ts` is a transcription and nothing compares it to the DDL"*, stands at [`recon-sweep.test.ts:830`](../../apps/worker/test/recon-sweep.test.ts) and [`digests.test.ts:1458`](../../apps/worker/test/digests.test.ts). Section 3 is the derivation. **That entry's own section 9.1 prices its control at five claim-site rows. The number is nine.**

- **Origin:** [ALLOCATION](ALLOCATION.md) row `441`, session **636**, branch `claude/five-copies-no-cited-line-moves`, off `9847bcd0`.
- **Fence, BY FILE:** `apps/worker/**`, this file, the registries, and [`docs/STATE.md`](../STATE.md) for generator output only. **No `packages/tooling` file, no `packages/db` file, no `apps/api` file, no migration, no workflow.**
- **Money:** **COMMENT-ONLY DIFF IN A MONEY-ADJACENT PACKAGE, AND THE DISPATCH ASKED TO HEAR IT EITHER WAY.** `apps/worker` is money-adjacent and three of the nine sites govern a money read. **Not one executable line changed**: every one of the 110 changed lines is inside a `/** */` block or a `//` run. `pnpm run typecheck` is exit 0 and the suite total is unmoved in both directions. Integer cents, no float, and not one `throw` retired.

---

### 1. The container, measured rather than assumed

**THE DISPATCH'S CONTAINER SENTENCE IS NOW TWENTY-THREE ROWS OLD AND IS STILL RIGHT.** The container arrived with **no repository**; cloning it was the first act of this row. `gh` is absent.

| Fact | Value |
| --- | --- |
| `node` | `v22.22.2` |
| `pnpm` | `10.33.0`, `pnpm install --frozen-lockfile` clean in 10.7s |
| Repository on arrival | **absent** |
| Both runner paths the dispatch names | present, and both ran |

**THE FIRST `git push` WAS REFUSED `403` AND THE DISPATCH'S OWN REMEDY WORKED FIRST TIME**, *"not in this session's authorized repository set"*, then `add_repo` with `access: "push"`, then the same push. **No pre-check was taken against the remote.** Same sequence [ADR-437](ADR-437.md) section 1 and [ADR-440](ADR-440.md) section 1 report, confirmed a third time.

---

### 2. The base, reproduced before the first edit, and it IS GREEN

| Leg | On `9847bcd0`, before any edit |
| --- | --- |
| `pnpm vitest run` | **342 files, 8429 passed, 11 skipped, 8440 total**, 87.19s, 0 failed |
| `pnpm run typecheck` | exit 0 |
| `pnpm run lint` | exit 0 |
| `pnpm run format:check` | clean |
| `node scripts/corpus/gates.mjs check` | **33 of 33 gates pass** |
| `node packages/tooling/checks/repo-invariants.mjs` | **35 of 35 invariants hold**, `RI-14`, `RI-15` and `RI-16` among them |

**`CI-06f` IS GREEN ON THIS BASE AND THAT IS NEW.** [ADR-440](ADR-440.md) section 10 item 2 records `438` and `439` as holes on its own base; both are allocated and both entries are present here, so this row inherits no hole and creates none by claiming `441`.

**`pnpm run verify` WAS NOT RUN AND `scripts/corpus/falsify.mjs` WAS NOT RUN.** Both are prohibited by the dispatch.

---

### 3. The population, DERIVED, and it is NINE

**THE COMMISSION SAYS FIVE. IT IS NINE, AND THE UNDERCOUNT IS AN ARTEFACT OF THE INSTRUMENT RATHER THAN A DISAGREEMENT ABOUT WHAT IS FALSE.** Derived with three greps over `apps/worker`, every hit opened: [ADR-440](ADR-440.md)'s own needle `` compares a `schema.ts` column ``, then `against the DDL`, then `transcription`.

| # | Site, at its BASE line | Wording | In ADR-440's five? |
| --- | --- | --- | --- |
| 1 | [`evaluate.ts:139`](../../apps/worker/src/breaker/evaluate.ts) | *"nothing in this tree compares a `schema.ts` column type against the DDL"* | yes |
| 2 | [`sweep.ts:387`](../../apps/worker/src/recon/sweep.ts) | the same | yes |
| 3 | [`recon-sweep.test.ts:218`](../../apps/worker/test/recon-sweep.test.ts) | *"nothing here compares..."* | yes |
| 4 | [`recon-sweep.test.ts:773`](../../apps/worker/test/recon-sweep.test.ts) | *"nothing in this tree compares..."* | yes |
| 5 | [`digests.test.ts:684`](../../apps/worker/test/digests.test.ts) | *"no check in this tree compares..."* | yes |
| 6 | [`rows.ts:28`](../../apps/worker/src/digests/rows.ts) | *"nothing in this tree compares a"* / *"`schema.ts` column type against the DDL"* | **NO. The needle wraps** |
| 7 | [`breaker.test.ts:374`](../../apps/worker/test/breaker.test.ts) | *"no check in this tree compares a"* / *"`schema.ts` column type against the DDL"* | **NO. The needle wraps** |
| 8 | [`recon-sweep.test.ts:830`](../../apps/worker/test/recon-sweep.test.ts) | *"`schema.ts` is a transcription and nothing compares it to the DDL"* | **NO. FIFTH WORDING** |
| 9 | [`digests.test.ts:1458`](../../apps/worker/test/digests.test.ts) | *"`schema.ts` is a transcription and no check in this tree compares it to the DDL"* | **NO. FIFTH WORDING** |

**SITES 6 AND 7 ARE THE SAME SENTENCE AS SITE 1, BROKEN ONE WORD EARLIER.** [`rows.ts:28`](../../apps/worker/src/digests/rows.ts) ends *"...records that nothing in this tree compares a"* and line 29 opens *"`schema.ts` column type against the DDL."* [ADR-440](ADR-440.md) section 3's grep is a substring match over single lines, so it cannot see either. **This is not a criticism of that entry's reading; it is the cost of the instrument, and it is exactly the objection its own section 9.1 raises against candidate B, arriving inside the measurement that refused candidate B.**

**SITES 8 AND 9 ARE A WORDING [ADR-440](ADR-440.md) SECTION 4 DOES NOT LIST.** It enumerates four: *"no check in this tree compares"*, *"nothing in this tree compares"*, *"nothing here compares"*, *"nothing compares"*. The fifth is *"`schema.ts` is a transcription and nothing compares IT to the DDL"* -- the subject is the whole transcription rather than a column type, which makes it **broader and equally false**. **So the claim has FIVE spellings and not four**, and the survey [ADR-440](ADR-440.md) section 12 item 3 asks for is larger than that entry could see.

**THREE NEARBY CLAIMS ARE TRUE AND WERE LEFT, WHICH IS THE OTHER HALF OF DERIVING A POPULATION.** [`breaker.test.ts:1358`](../../apps/worker/test/breaker.test.ts) says *"nothing in this tree compares a `schema.ts` DEFAULT against the DDL"* -- **true**, section 4. [`breaker.test.ts:1415`](../../apps/worker/test/breaker.test.ts) says NO DEFAULT is *"the half nothing in this tree checks"* -- **true**, same reason. [`breaker.test.ts:1422`](../../apps/worker/test/breaker.test.ts) reads *"ADR-112 foreclosure 4: nothing makes the transcription complete"* -- **true**, and it is the one site in this fence that quotes foreclosure 4 correctly. **A row that greps for the shape and repairs every hit would have falsified all three.**

---

### 4. What the sentence claims, why it is false, and WHEN it became false

**THE COMPARISON EXISTS AND WAS OPENED AT SOURCE RATHER THAN INHERITED FROM [ADR-440](ADR-440.md).**

| What is compared | Where | Population |
| --- | --- | --- |
| Column TYPE | [`scoped-db.test.ts:2734`](../../packages/db/test/scoped-db.test.ts), `tsType(column)` against `ddlType(def ?? '')` at [`:2737`](../../packages/db/test/scoped-db.test.ts) | every column of every registered non-view relation, one named case per relation at [`:2728`](../../packages/db/test/scoped-db.test.ts) |
| Column NULLABILITY | [`scoped-db.test.ts:2739`](../../packages/db/test/scoped-db.test.ts), `column.notNull` against `declaredNotNull(def)` | the same |
| Non-degeneracy | [`scoped-db.test.ts:2754`](../../packages/db/test/scoped-db.test.ts) | asserts the reader produced a real type for every registered column |
| Column DEFAULT | **nowhere** | [`scoped-db.test.ts:2520`](../../packages/db/test/scoped-db.test.ts)'s `TYPE_ENDS_AT` carries `DEFAULT\b` in its lookahead and cuts the text there before the comparison |

**THE DDL SIDE IS THE FOLDED MIGRATION SET AS TEXT AND NOT THE DATABASE**, which is what keeps [ADR-299](ADR-299.md) section 5.1 item 5's ruling standing after the sentence goes: `foldTableDefs(sqlName)` at [`scoped-db.test.ts:2729`](../../packages/db/test/scoped-db.test.ts) reads migration text. **A transcription agreeing with a second transcription settles nothing about the row a driver hands back**, and every repaired block now says so in the place where the false sentence used to sit.

**AND THE MISATTRIBUTION IS IN SEVEN OF THE NINE.** [`ADR-112.md:92`](ADR-112.md) foreclosure 4 reads *"The addressability of a table is decided by `schema.ts` and not by the migrations ... nothing yet makes the transcription complete -- the suite catches a key the transcription INVENTS, and does not require it to be exhaustive."* **It is about ADDRESSABILITY and EXHAUSTIVENESS and says nothing about column types.** Sites 1 through 7 all cite it; sites 8 and 9 cite nothing. Each repaired block now says which.

**THE DATES, DERIVED HERE WITH `git log -S` PER SITE AND `git show <commit>:packages/db/test/scoped-db.test.ts` FOR EACH.**

| Site | Commit that wrote it | Date | `ddlType(def` present in the tree at that commit |
| --- | --- | --- | --- |
| the comparison itself | `600814b7`, *"ADR-094 section 3's comparison: column TYPE and NULLABILITY are asserted"* | **2026-08-25** | n/a |
| sites 1 and 7, `breaker` | `5941d85f` | 2026-09-07 | **yes, 4 occurrences** |
| sites 6 and 5, `digests` | `2a47dccb` | 2026-09-07 | **yes, 4 occurrences** |
| sites 2, 4 and 8, `recon` | `86c2aa92` | 2026-09-07 | **yes, 4 occurrences** |
| site 3, `recon` controls | `919ac085` | 2026-09-07 | **yes, 4 occurrences** |

**ALL NINE WERE WRITTEN THIRTEEN DAYS AFTER THE COMPARISON LANDED, ON ONE DAY, BY FOUR COMMITS.** [ADR-440](ADR-440.md) section 4's finding holds and this row extends it: **nine sentences, five wordings, six files, and not one of them was ever true.** Nothing drifted. The word "stale" is wrong here too.

---

### 5. The constraint, which is NOT line-count neutrality, and the numbers it turns on

**THE RULING WAS READ AT SOURCE BEFORE ANYTHING WAS EDITED.** [`repo-invariants.mjs:3008-3016`](../../packages/tooling/checks/repo-invariants.mjs): *"WHAT THIS GRAMMAR COSTS THE PROSE AROUND IT IS RULED BY ADR-386 AND THE RULING IS NOT LINE-COUNT NEUTRALITY. Nine sessions held a diff to equal insertions and deletions so no pointer would drift, and three entries recorded a clause dropped to fit. That property is neither necessary nor sufficient. THE ONE THAT IS BOTH IS THAT NO CITED LINE MOVES, and where one must, every pointer in a live file is repaired and every pointer in a DATED RECORD is NAMED."* **The dispatch's paraphrase is exact.**

**THE CITATION FIGURES, DERIVED PER FILE AND PER SITE RATHER THAN ASSUMED.** A reader mirroring [`repo-invariants.mjs:3180`](../../packages/tooling/checks/repo-invariants.mjs)'s `citationTargets` was written for this: a markdown `href` beats the token's own path, and a bare spec with no `href` resolves to every file in the tree whose path ends with it.

| Site | File total citations | Files citing | Distinct cited lines in the file | **Citations BELOW the site, which a 1-line insertion MOVES** | Distinct lines below |
| --- | --- | --- | --- | --- | --- |
| [`evaluate.ts:139`](../../apps/worker/src/breaker/evaluate.ts) | 27 | 11 | 13 | **25** | 12 |
| [`sweep.ts:388`](../../apps/worker/src/recon/sweep.ts) | 27 | 13 | 8 | **20** | 4 |
| [`rows.ts:29`](../../apps/worker/src/digests/rows.ts) | **0** | 0 | 0 | **0** | 0 |
| [`breaker.test.ts:375`](../../apps/worker/test/breaker.test.ts) | 19 | 15 | 8 | **17** | 6 |
| [`recon-sweep.test.ts:218`](../../apps/worker/test/recon-sweep.test.ts) | 9 | 4 | 5 | **5** | 3 |
| [`recon-sweep.test.ts:773`](../../apps/worker/test/recon-sweep.test.ts) | 9 | 4 | 5 | **0** | 0 |
| [`digests.test.ts:684`](../../apps/worker/test/digests.test.ts) | 19 | 8 | 10 | **10** | 6 |
| [`digests.test.ts:1458`](../../apps/worker/test/digests.test.ts) | 19 | 8 | 10 | **0** | 0 |

**THE SPREAD IS 0 TO 25 AND THE DISPATCH'S WARNING NOT TO ASSUME IS WHAT THE TABLE VINDICATES.** [`rows.ts`](../../apps/worker/src/digests/rows.ts) carries **no citation at all** -- a grep for `rows.ts:` returns nothing in this tree -- and three of the eight sites have **nothing cited below them**. **Line-count neutrality would have been paid on all eight and was owed on five.**

**SO THE ROOM WAS SPENT WHERE IT WAS FREE, AND THE REPAIR IS BETTER FOR IT.** Five sites are held to their exact block span. Three grow: [`rows.ts`](../../apps/worker/src/digests/rows.ts) by 7 lines, [`recon-sweep.test.ts`](../../apps/worker/test/recon-sweep.test.ts) by 4 at `:773` and 3 at `:830`, [`digests.test.ts`](../../apps/worker/test/digests.test.ts) by 3 at `:1458`. **The three free sites carry the full finding**: the misattribution named as ADDRESSABILITY and EXHAUSTIVENESS, the migration-text-not-database distinction spelled out, and `TYPE_ENDS_AT` cited for why DEFAULT is compared nowhere. **The five held sites carry a compressed form of the same three facts.** That difference is the whole benefit of the weaker constraint and it is stated here rather than left to be inferred.

**AND ONE SITE DOES WHAT [ADR-440](ADR-440.md) SECTION 5 SAYS LINE-NEUTRALITY CANNOT.** That entry names the unpaid cost: *"line-neutrality preserves the pointer without preserving what it lands on"*, and records `scoped-db.ts:3446` now holding the middle of a correction. **At [`recon-sweep.test.ts:773`](../../apps/worker/test/recon-sweep.test.ts) this row kept both.** Lines 770 and 771 are byte-identical to the base, line 772 was rewritten to open a quotation, and **line 773 still reads `` this tree compares a `schema.ts` column type against the DDL. `` -- the exact fragment [ADR-440](ADR-440.md) section 3 points at -- now closed by a quotation mark with `` `RI-14` (ADR-441): IS FALSE `` on the same line and the whole derivation on the four lines below it, which were free to add because nothing in this tree cites past `:773`. **A reader following that pointer meets the sentence AND its correction, which is what the idiom is for.**

**THE CLAUSE COMPRESSED, STATED AS [ADR-440](ADR-440.md) SECTION 5 STATES ITS OWN.** At [`digests.test.ts:684`](../../apps/worker/test/digests.test.ts), the tightest site, `` `RI-14` (ADR-441): IS FALSE `` carries no *"and was false when written"* and the DEFAULT clause reads *"DEFAULT nowhere"*. **That is the cost, and it is the only one: no site dropped the quotation, the citation, or the misattribution finding.**

---

### 6. Proof, not assertion

**NO CITED LINE MOVED, VERIFIED POSITIVELY AND NOT BY THE DIFFSTAT.** All 101 citations reaching these six files were resolved to a line number, and for each of the 44 distinct lines they land on, the base content was compared byte for byte with the working content at the same line.

| | Count |
| --- | --- |
| Citations into the six files | **101**, over **44** distinct lines |
| **Distinct cited lines whose bytes are IDENTICAL before and after** | **39 of 44** |
| Cited lines whose content changed | **5**, and every one is a claim site this row repaired in place |
| Cited lines that MOVED to a different number | **0** |

**THE FIVE ARE `evaluate.ts:139`, `sweep.ts:387`, `recon-sweep.test.ts:218`, `recon-sweep.test.ts:773` AND `digests.test.ts:684`, AND EVERY ONE OF THEM IS CITED ONLY BY [ADR-440](ADR-440.md)**, in section 3's table and section 10 item 1. **[ADR-440](ADR-440.md) is a DATED RECORD**, which [`repo-invariants.mjs:3013`](../../packages/tooling/checks/repo-invariants.mjs) rules is NAMED rather than repaired. They are named here, with what each now holds:

| Pointer | What it held on the base | What it holds now |
| --- | --- | --- |
| [`evaluate.ts:139`](../../apps/worker/src/breaker/evaluate.ts) | the sentence | the correction and `` `scoped-db.test.ts:2728` `` |
| [`sweep.ts:387`](../../apps/worker/src/recon/sweep.ts) | the sentence | `` `RI-14` (ADR-441): IS FALSE `` and the citation |
| [`recon-sweep.test.ts:218`](../../apps/worker/test/recon-sweep.test.ts) | the sentence | the evidence clause, with the quotation two lines above |
| [`recon-sweep.test.ts:773`](../../apps/worker/test/recon-sweep.test.ts) | the sentence | **the same sentence, quoted, plus the verdict** |
| [`digests.test.ts:684`](../../apps/worker/test/digests.test.ts) | the sentence | `` IS FALSE `` and the citation |

**AND THE ONE PLACE THIS ROW DID MOVE CITED LINES IS THE REGISTRY, WHICH IS DERIVED HERE RATHER THAN LEFT FOR THE NEXT ROW TO FIND.** [ALLOCATION](ALLOCATION.md) carries **65 citations over 22 files on 26 distinct lines**, and row `441` is inserted at line 433 because the table is ordered and `440` is the highest claimed number on this base. **Four cited lines sit below it and all four moved down by one**: `:472`, `:523`, `:638` and `:644`, carrying **18 citations between them**. **Every one of the 18 is in an ADR**, which is a DATED RECORD, so [`repo-invariants.mjs:3012-3016`](../../packages/tooling/checks/repo-invariants.mjs) rules them NAMED rather than repaired. They are named here, and their sources are [ADR-337](ADR-337.md), [ADR-386](ADR-386.md), [ADR-388](ADR-388.md), [ADR-389](ADR-389.md), [ADR-398](ADR-398.md), [ADR-401](ADR-401.md), [ADR-404](ADR-404.md) and [ADR-414](ADR-414.md). **No live document cites below line 433 of that file**, which is why nothing was owed a repair.

**`:638` WAS ALREADY WRONG BEFORE THIS ROW AND IT IS STILL WRONG, WHICH IS THE POINT [ADR-337](ADR-337.md) SECTION 70 MAKES ABOUT ITSELF.** On the base, line 638 held the `OI-22` row of a different table entirely; the thirteen citations naming it are about `RI-37`'s title. That entry says so in its own words: *"THE POINTER ABOVE IS WRONG, AND IT IS DELIBERATELY LEFT WRONG"*, on [ADR-388](ADR-388.md) section 2's ruling that moving it *"would convert a record of a check that did not check into a record of one that did"*. **This row moved it by one more and did not repair it, for that ruling's reason and not for convenience.** Every registry row added since `434` moved the same four lines the same way.

**THE TWO GENERATED REGISTRIES MOVED NOTHING.** `gates.mjs generate` appended one row to [`docs/decisions/README.md`](README.md) at line 454 and one to [`docs/sessions/README.md`](../sessions/README.md) at line 861, and rewrote one span in [`docs/STATE.md`](../STATE.md) at line 32. The highest cited line in the first is `:378` and both cited lines there are above the append. The append in the second lands below `:857` and above `:870` -- **`:870`, cited once by [ADR-384](ADR-384.md), is the one pointer the generator moved**, and it too is in an ADR and is NAMED here.

**`RI-14`, `RI-15` AND `RI-16` PASS ON BOTH SIDES.**

| Leg | Before the edit | After the edit |
| --- | --- | --- |
| `RI-14` | **PASS** | **PASS** |
| `RI-15` | **PASS** | **PASS** |
| `RI-16` | **PASS** | **PASS** |
| Whole check | **35 of 35** | **35 of 35** |

**THE RED WAS WATCHED, ON THIS BRANCH, IN TWO PASSES, AND REVERTED BYTE-IDENTICALLY BOTH TIMES.** This row adds **ten** citations, nine of them `` `scoped-db.test.ts:2728` `` and one `` `scoped-db.test.ts:2520` ``. Rewritten to `:99999` and `:99998` against a file of **3985** lines: the three source files gave `RI-15` **FAIL with 4 findings, 34 of 35**, naming `evaluate.ts:139`, `rows.ts:32`, `rows.ts:38` and `sweep.ts:387`; the three test files gave **FAIL with 6 findings, 34 of 35**, naming `breaker.test.ts:375`, `digests.test.ts:684`, `digests.test.ts:1459`, `recon-sweep.test.ts:217`, `recon-sweep.test.ts:774` and `recon-sweep.test.ts:835`. **Restored, and `git diff --stat` is unchanged and the check is 35 of 35.** So `RI-15` reads every citation this row wrote, and not one of them is a pointer nothing checks.

---

### 7. What the repaired text says

**EVERY BLOCK CARRIES THE SAME FOUR THINGS**, on `RI-14`'s idiom at [`repo-invariants.mjs:1887-1894`](../../packages/tooling/checks/repo-invariants.mjs) -- *"A REFUTED CLAIM MAY STAY, AND MUST SAY SO"* -- and on the worked example at [`scoped-db.ts:3442`](../../packages/db/src/scoped-db.ts): the superseded sentence QUOTED, `` `RI-14` (ADR-441): IS FALSE ``, the comparison at [`scoped-db.test.ts:2728`](../../packages/db/test/scoped-db.test.ts) with its population, and what keeps [ADR-299](ADR-299.md) section 5.1 item 5's ruling standing anyway.

**THE RULING SURVIVES IN ALL NINE AND NOT ONE GUARD CAME OFF WITH THE SENTENCE.** What each block now says is that the comparison is against **MIGRATION TEXT and not the database**, that **DEFAULT is compared nowhere**, and that `ADR-112` foreclosure 4 is about **EXHAUSTIVENESS**. **A type derived from a transcription that agrees with a second transcription still buys the column's EXISTENCE and still buys nothing about its VALUE**, so every `throw` below every repaired block is exactly where it was.

**NEITHER THE COMPARISON NOR THE FORECLOSURE WAS TAKEN ON [ADR-440](ADR-440.md)'s WORD.** [`scoped-db.test.ts:2726-2760`](../../packages/db/test/scoped-db.test.ts), [`:2520`](../../packages/db/test/scoped-db.test.ts) and [`ADR-112.md:92`](ADR-112.md) were each opened at source by this row.

---

### 8. Money

**COMMENT-ONLY, IN A MONEY-ADJACENT PACKAGE, AND THE DISPATCH ASKED TO HEAR IT LOUDLY EITHER WAY.** The diff is **110 changed lines over six files, every one of them inside a `/** */` block or a `//` run**. No declaration, no signature, no expression, no export, no test name and no assertion moved. `pnpm run typecheck` is exit 0.

**THREE OF THE NINE SITES SIT ON A MONEY READ AND SAY SO.** [`evaluate.ts`](../../apps/worker/src/breaker/evaluate.ts)'s block guards *"the denominator of a loss ratio"*; [`sweep.ts`](../../apps/worker/src/recon/sweep.ts)'s says *"these rows carry money"*; [`recon-sweep.test.ts`](../../apps/worker/test/recon-sweep.test.ts)'s says the guard traded away *"would be the one standing between a float and a balance"*. **Every one of those sentences is unchanged.** The falsehood was failing safe -- it argued for KEEPING guards on a worse reason than the true one -- and the direction this repair could have gone wrong in is the direction it did not go: **not one refusal was retired, not one `throw` deleted, not one cast removed.** Integer cents throughout.

---

### 9. What this row did NOT do, and why

1. **NO CONTROL WAS BUILT FOR THIS DEFECT CLASS.** [ADR-440](ADR-440.md) section 9.1 prices it at one `absence-claims.mjs` artifact plus one claim-site row per live sentence plus a seed each, in a fence this row does not hold, and section 9.2 rules it **must not be bolted onto a repair** because a control landing in the diff of the defect it would have caught has no counterfactual. **The dispatch names row `442` as its owner. It was left alone.** This row's only contribution to it is section 3: **the price is nine claim-site rows and nine seeds, not five, and the probe must be written against a claim with FIVE spellings.**
2. **THE FOUR ADR COPIES AND [`STATE.md:11282`](../STATE.md) WERE NOT REPAIRED.** [ADR-299](ADR-299.md), [ADR-303](ADR-303.md), [ADR-426](ADR-426.md) and [ADR-432](ADR-432.md) are dated records, [`repo-invariants.mjs:3013`](../../packages/tooling/checks/repo-invariants.mjs) rules a pointer in one is NAMED rather than repaired, and `STATE.md` is granted to this row for generator output only.
3. **THE TWO `scoped-db.ts` COPIES WERE NOT TOUCHED.** They are [ADR-440](ADR-440.md)'s and they are already repaired; `packages/db/**` is outside this fence.
4. **THREE TRUE CLAIMS IN THIS FENCE WERE LEFT STANDING**, section 3. Repairing them would have been the mechanism running backwards.
5. **NO GATE WAS WEAKENED, NO TEST SKIPPED, DISABLED OR QUARANTINED, NO CASE ADDED, INVERTED OR LOOSENED.** The suite total is unchanged in both directions, section 10. No case asserts a comment's text, which is [ADR-440](ADR-440.md) section 9.1's refused candidate B.
6. **NO ALLOCATION NUMBER WAS INVENTED AND NO RESERVATION WAS WRITTEN.** `440` is the highest claimed row on this base and this row claims `441`, so there is no hole and nothing to reserve. The fence grants row `441`'s cell alone.
7. **ONE FILE OUTSIDE THE FENCE'S PROSE WAS CHANGED, BY THE GENERATOR THE DISPATCH REQUIRES.** `node scripts/corpus/gates.mjs generate` rewrote the span NAMED `adr_count` in [`docs/STATE.md`](../STATE.md), because this row adds an ADR file and that span counts them. **No prose in that file was touched.**

---

### 10. Every leg, re-run after the last edit

**COUNTS DERIVED AT THE MOMENT OF REPORTING AND NOT CARRIED FROM AN EARLIER RUN.**

| Leg | Result |
| --- | --- |
| `pnpm vitest run` | **342 files, 8429 passed, 11 skipped, 8440 total, 0 failed.** **Base 8440, mine 8440, delta 0** |
| Skipped | **11 before and 11 after**, stated separately. Nothing in this diff changes what any suite runs |
| `pnpm run typecheck` | exit 0 |
| `pnpm run lint` | exit 0 |
| `pnpm run format:check` | clean |
| `node scripts/corpus/gates.mjs generate` | see section 11 |
| `node scripts/corpus/gates.mjs check` | see section 11 |
| `node packages/tooling/checks/repo-invariants.mjs` | see section 11 |

**`pnpm run verify` WAS NOT RUN AND `scripts/corpus/falsify.mjs` WAS NOT RUN.**

**FILE LENGTHS, `wc -l` ON BOTH SIDES.** [`evaluate.ts`](../../apps/worker/src/breaker/evaluate.ts) 859 and 859; [`sweep.ts`](../../apps/worker/src/recon/sweep.ts) 801 and 801; [`breaker.test.ts`](../../apps/worker/test/breaker.test.ts) 1426 and 1426; [`rows.ts`](../../apps/worker/src/digests/rows.ts) 153 and 160; [`recon-sweep.test.ts`](../../apps/worker/test/recon-sweep.test.ts) 859 and 866; [`digests.test.ts`](../../apps/worker/test/digests.test.ts) 1493 and 1496. **Three files grew and no cited line moved, which is the point of section 5.**

**NO BARREL WAS TOUCHED, SO NO LEG COUNT IS OWED.**

---

### 11. What is owed, stated precisely enough to be picked up

1. **THE CONTROL'S PRICE IS NINE CLAIM-SITE ROWS AND NINE SEEDS, NOT FIVE**, section 3 and section 9 item 1. [ADR-440](ADR-440.md) section 9.1's artifact and probe are unchanged; only the row count and the number of spellings the probe's siblings must survive are. **It is still row `442`'s and it must still not be bolted onto a repair.**
2. **THE CLAIM HAS FIVE SPELLINGS AND NOT FOUR**, section 3. [ADR-440](ADR-440.md) section 4's list is missing *"`schema.ts` is a transcription and nothing compares it to the DDL"*, which is broader than the other four and was live in two files.
3. **A SUBSTRING GREP OVER SINGLE LINES CANNOT SEE A SENTENCE THAT WRAPS**, section 3, and that is how two of the nine were missed. **Any survey commissioned under [ADR-440](ADR-440.md) section 12 item 3 must flatten line breaks before it counts**, or it will undercount by the same mechanism.
4. **NOTHING COMPARES A `schema.ts` DEFAULT AGAINST THE DDL**, [`scoped-db.test.ts:2520`](../../packages/db/test/scoped-db.test.ts)'s `TYPE_ENDS_AT` being why. This is [ADR-437](ADR-437.md) section 12 item 2 and [ADR-440](ADR-440.md) section 12 item 6, it is row `438`'s subject, and it is untouched here. **Three sites in this fence state it and all three are TRUE**, section 3.
5. **[ADR-432](ADR-432.md) SECTION 10 ITEM 1's CITATION IS STILL OFF BY ONE**, [ADR-437](ADR-437.md) section 10 item 3, and it is still not repaired.
6. **THE SIX SITES IN THIS FENCE NOW CARRY TEN NEW CITATIONS INTO [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts)**, which is a file `packages/db/**` owns. **A row that reflows that file above line 2520 will move all ten**, and this entry is the notice.
7. **NOBODY HAS SURVEYED HOW MANY SENTENCES IN THIS TREE CLAIM "NO CHECK DOES X"**, [ADR-440](ADR-440.md) section 12 item 3, and this row's finding that the population was nearly doubled by two line-wraps and one unlisted wording makes that survey more valuable rather than less.

---

### 12. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing. `E2` READ OWED.** Inherited as well as owed: [ADR-299](ADR-299.md), [ADR-303](ADR-303.md), [ADR-426](ADR-426.md), [ADR-432](ADR-432.md), [ADR-434](ADR-434.md), [ADR-436](ADR-436.md), [ADR-437](ADR-437.md) and [ADR-440](ADR-440.md) are `proposed` and unsigned, and this row settles none of them.

  **What a founder read adds and this entry cannot.**

  **THE FIRST THING TO DECIDE IS WHETHER A COUNT IN A SIBLING ENTRY MAY EVER BE SPENT WITHOUT RE-DERIVING IT, BECAUSE THIS IS THE FIFTH CONSECUTIVE ROW WHERE IT COULD NOT.** [ADR-430](ADR-430.md) measured [ADR-426](ADR-426.md)'s counts low, [ADR-429](ADR-429.md) corrected [ADR-428](ADR-428.md)'s headline, [ADR-437](ADR-437.md) found [ADR-432](ADR-432.md)'s citation off by one, [ADR-440](ADR-440.md) corrected its own dispatcher's figure, and **this row found [ADR-440](ADR-440.md)'s population 44% low.** Not one of the five was carelessness. **Every one was an instrument doing exactly what it was written to do**, and here it was a grep meeting a line break. A founder should decide whether the answer is a rule about re-derivation or a rule about instruments.

  **THE SECOND IS THAT THE WEAKER CONSTRAINT WAS THE BETTER ONE AND THE DIFFERENCE IS VISIBLE IN THE DIFF.** Section 5. Three of eight sites had **nothing cited below them** and one file had **no citation at all**; those three carry the full finding and the five held sites carry a compressed form. **Two dispatches in a row commissioned line-count neutrality and [ADR-440](ADR-440.md) section 12 item 5 predicted what it would cost the row that took these nine.** It would have cost exactly this: the same repair, three times, with the reasons cut out of it.

  **THE THIRD IS THAT A POINTER CAN KEEP BOTH THE SENTENCE AND ITS CORRECTION, AND ONE SITE HERE PROVES IT.** [ADR-440](ADR-440.md) section 5 names the unpaid cost of line-neutrality: the pointer survives and what it lands on does not. At [`recon-sweep.test.ts:773`](../../apps/worker/test/recon-sweep.test.ts) the cited line still carries the sentence VERBATIM and the correction begins on the same line. **That is available wherever nothing is cited below the site, which is knowable in one derivation, and it should be the default rather than this row's flourish.**

  **THE FOURTH IS THAT NO GUARD CAME OFF.** Nine blocks lost their evidence and not one lost its ruling. **Three of them stand in front of a money read and all three still say a caller checks the value it read.** A founder reading only the diff should see that this is the one direction it could have gone wrong in and did not.

- **Approval:** `___________________________` (UNSIGNED. **COMMENT-ONLY DIFF IN A MONEY-ADJACENT PACKAGE. `E2` READ OWED, ON THE FILES NAMED IN SECTION 10.** **Merging is not signing.**)

|                 |     |
| --------------- | --- |
| **Approved by** |     |
| **Date**        |     |
