## ADR-397: there is NO fourth outcome, ADR-388's three partition the population and admit no fourth cell, and what ADR-393 found is a missing PRECONDITION on outcome one's remedy that changes not one verdict in 420 (2026-09-06, status: proposed)

**[ADR-393](ADR-393.md) SECTION 9 ITEM 4 ASKED FOR A FOURTH OUTCOME OR AN EXPLICIT STATEMENT THAT THERE IS NONE, AND SAID AMENDING A RULING IS A ROW AND NOT A PARAGRAPH. THIS IS THAT ROW AND THE STATEMENT IS: THERE IS NONE.** [ADR-388](ADR-388.md)'s two gates are a partition and a partition of two gates has three cells. Nothing can sit outside them, and nothing does.

**AND [ADR-393](ADR-393.md) IS NOT WRONG. IT FOUND A REAL DEFECT AND MISNAMED IT.** Outcome one states a PERMISSION and a REMEDY in one breath, and the remedy carries a precondition the permission never stated: *only the coordinate moves* presumes a coordinate to move to. **Where the named subject is no longer anywhere in the cited file there is none.** That is an amendment to outcome one, written into [ADR-388](ADR-388.md) at its own line and keeping that sentence whole, and it is not a class.

**THE MEASUREMENT DECIDES IT AND THE MEASUREMENT IS ZERO.** Over the **420** pointers this corpus can decide, the precondition fails **8 times and all 8 sit where `G2` has ALREADY failed**, so repair was forbidden before the precondition was reached. **It fails NOT ONCE in outcome one.** Nothing moves between classes, and [ADR-388](ADR-388.md) section 4's table reproduces figure for figure on a different base.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `397`, branch `claude/the-fourth-outcome-and-the-header-clause`, session **592**, off the wave integration branch rather than off `main`. ADR number and session number assigned centrally and not derived.
- **MONEY-ADJACENT. ADR-003 STRICT. `E2` READ OWED.** The prose repaired in [`schedule.ts`](../../apps/worker/src/schedule.ts) describes the worker's expiry sweep, which releases held payouts and posts `LT-01`. Ships `proposed` with an **UNSIGNED** approval line. **Merging is not signing.**
- **THE STANDING BAR IS NOT TOUCHED AND THIS IS THE SEVENTH CONSECUTIVE ROW TO LEAVE IT STANDING.** No port wired, no job scheduled, no sink passed, no default parameter added to `expirySweepIo`, and no cron expression, interval, batch size, concurrency, retry count, backoff or timeout invented. [ADR-382](ADR-382.md)'s bar stands with its two exits unchanged, after [ADR-376](ADR-376.md), [ADR-378](ADR-378.md), [ADR-385](ADR-385.md), [ADR-387](ADR-387.md), [ADR-391](ADR-391.md) and [ADR-394](ADR-394.md).
- **NOT ONE EXECUTABLE LINE CHANGED.** The only file outside `docs/` this diff touches is [`schedule.ts`](../../apps/worker/src/schedule.ts) and every edit in it is inside a comment block. `scripts/corpus/**`, `packages/tooling/**` and `.github/workflows/corpus.yml` were READ and never opened for writing. `docs/ops/runbooks/**` was neither read nor written.
- **NO INVARIANT MINTED, NO `RI-` NUMBER RESERVED, NO `CI-06` LETTER SPENT, NO GATE ADDED, WIDENED OR WEAKENED, NO REGISTER GROWN, NO TEST SKIPPED, DISABLED OR QUARANTINED, NO MIGRATION TAKEN OR RESERVED, NO `status` FIELD MOVED.**
- **EVERY FIGURE THE DISPATCH CARRIED WAS DECLARED SECOND-HAND AND EVERY ONE WAS RE-DERIVED.** The 289, the 8,390, the 8,101 and the three classes all reproduce. **The gate count was derived and is 33.** The one figure that does not reproduce is a REASON rather than a count, section 4.

---

### 1. What was measured before the first edit, and the environment it was measured in

**THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL.** `/home/user` was empty, `git rev-parse` failed in the directory the dispatch called the repository root, and no checkout of this repository existed anywhere on the container. The tree was attached to this session's authorized set and cloned by hand. **This is the condition [ADR-382](ADR-382.md) section 1 first recorded and [ADR-393](ADR-393.md) section 1 and [ADR-394](ADR-394.md) section 1 recorded again, and it has not been fixed.**

**THE CLONE WAS SHALLOW AND IT WAS DEEPENED BEFORE ONE HISTORY QUESTION WAS ASKED.** This row rules ON [ADR-388](ADR-388.md)'s class test, and that test is `git blame` plus a read at the blamed commit, which is exactly the question a truncated graph answers WRONGLY rather than refusing ([ADR-389](ADR-389.md) section 7, reproduced there on a real pointer).

| Step | Result |
|---|---|
| `git rev-parse --is-shallow-repository`, run first | **`true`** |
| After `git fetch --depth=1000 origin claude/wave-392s` | **`false`**, `.git/shallow` does not exist |
| Commits reachable from the base | **4,309**, and the root commit `136fbf86` is among them |

**THE BASE WAS REPRODUCED ON `ebc21238` BEFORE THE FIRST EDIT**, on a clean tree, with `HEAD` confirmed to be that commit rather than assumed.

| Runner | On `ebc21238`, before the first edit |
|---|---|
| `node scripts/corpus/gates.mjs check` | **33 of 33**, the count read off the runner's own last line |
| `node packages/tooling/checks/repo-invariants.mjs` | **35 of 35** |
| `pnpm vitest run` | **336 files, 8,109 passed, 10 skipped, 0 failed** |
| `pnpm run typecheck`, `lint`, `format:check` | clean, clean, clean |

**`pnpm run verify` WAS NOT RUN AND [`falsify.mjs`](../../scripts/corpus/falsify.mjs) WAS NOT RUN.** Both are forbidden to this row and both mutate the working tree. Every leg was invoked directly.

---

### 2. THE RULING

**RULED: [ADR-388](ADR-388.md) HAS THREE OUTCOMES AND IT HAS NO FOURTH, AND THE REASON IS STRUCTURAL RATHER THAN EMPIRICAL.** The classification is a function of two gates. `G1` either holds or fails. Where `G1` holds, `G2` either holds, fails, or cannot be measured, and that entry already folds the third into the undecidable bucket in its own words. **Four leaves, three verdicts, no room for a fifth branch to hide in.** A row that added a fourth class would be adding a class that no pair of gate values can reach.

**RULED: WHAT [ADR-393](ADR-393.md) SECTION 5 FOUND IS REAL, REPRODUCES AT PRIMARY SOURCE, AND IS A MISSING PRECONDITION ON OUTCOME ONE'S REMEDY.** That outcome reads *REPAIR IS PERMITTED* and then *only the coordinate moves*. **The second clause presumes a destination and the first does not check for one.** Where the named subject has left the cited file entirely, the permission is empty: there is nothing to move the coordinate TO, and moving it to the nearest surviving thing is the row inventing a reading nobody took, which is the failure `G1` exists to prevent arriving through the gate rather than around it. **That is [ADR-393](ADR-393.md)'s own sentence and this row does not improve on it.**

**THE AMENDMENT IS WRITTEN INTO [ADR-388](ADR-388.md) AT OUTCOME ONE'S OWN LINE, THE ORIGINAL SENTENCE IS KEPT WHOLE, AND THE ADDITION IS APPENDED RATHER THAN SUBSTITUTED** (`RI-14`, [ADR-367](ADR-367.md)). Nothing in that sentence was false; it was incomplete, so nothing is retired and nothing is named as retired. **The disposition where the precondition fails is: the pointer is LEFT, and the finding is written beside it, recorded as a record that was TRUE about its own day.**

**AND THE DISPOSITION IS THE SAME ACTION AS OUTCOME TWO'S AND THE OPPOSITE MEANING, WHICH IS WHY IT IS WORTH WRITING DOWN AT ALL.** A class-two annotation tells a founder that an entry's verdict column recorded a check that did not check. **A precondition-failure annotation tells a founder that the entry was RIGHT and the tree moved on.** An `E2` read cares about that difference and no runner ever will.

**AND NO INVARIANT IS MINTED.** [ADR-388](ADR-388.md) already ruled that repairability is decided by a person with `git blame` and that `G2` needs history the checks say of themselves they never read. The precondition needs the same person and one more read. **A row that minted a number here would be doing the thing six consecutive waves have refused.**

---

### 3. The census, taken through the checkers' own reader, and the copy that never entered the repository

**DERIVED BY IMPORTING `citationsIn`, `documentScope`, `citationReader`, `citationTargets`, `citedReasonFiles` AND `nearestName` FROM AN EXPORT-ADDED COPY OF [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs).** That is [ADR-377](ADR-377.md)'s method and [ADR-388](ADR-388.md)'s: a hand-rolled regex measures a different grammar than the one the checks enforce, and the grammar is the subject here.

**THE COPY WAS PLACED OUTSIDE THE REPOSITORY AND NOT INSIDE IT, WHICH IS A DEPARTURE FROM [ADR-388](ADR-388.md)'s METHOD AND IS FORCED.** That row put its copy in the checker's own directory so `REPO_ROOT` would resolve. `packages/tooling/**` is another row's fence this wave. **So the whole package was copied to a scratch directory outside the tree and the copy's `HERE` constant was pinned to the real checks directory**, which makes `REPO_ROOT` and `OWN_PACKAGE` resolve exactly as the original's do while nothing is written into the repository at any point. `git status --porcelain` was EMPTY at every moment before the first edit and named only this row's own files after it.

**THE COPY WAS VALIDATED BY RUNNING THE CHECKS THROUGH IT BEFORE ITS READER WAS BELIEVED.** `RI-15` and `RI-16`, which are the two checks whose grammar this row borrows, both **PASS** through the copy on this tree, which is their live verdict. **One unrelated check, `RI-18`, does not run in the copy and is named rather than left in a total**: it resolves a path against its own module location in a way the relocation defeats. **So the copy reproduces 34 of 35 and the two that matter are among the 34.**

| Scope | Citations | In a dated record by shape | Carry a bindable NAME |
|---|---|---|---|
| `docs/`, backticked, the form both checks read | **8,764** | **7,665** | **363** |
| Source files, `RI-15`'s scope | **393** | 0 | **130** |
| **Read by the checks' reader** | **9,157** | **7,665** | **493** |

**THREE OF [ADR-388](ADR-388.md)'s FOUR SECTION 1 FIGURES REPRODUCE EXACTLY AND THE TWO THAT MOVED ARE THE TWO THAT MUST.** That entry measured **363** named citations in `docs/`, **393** citations in source files and **130** named among them, on `8fa66104`. **All three are identical here, on a base seven merges later.** Its 8,623 and 7,242 have grown to 8,764 and 7,665, which is the corpus writing pointers faster than it retires them and is that entry's own section 3 finding rather than a disagreement with it.

---

### 4. The classification re-derived, and ADR-388's section 4 table reproducing figure for figure

**FOR EVERY NAMED POINTER IN A DATED RECORD: `git blame -w` the citing line to the commit that last wrote it, read the cited file AT THAT COMMIT with `git cat-file blob`, and run `nearestName` through the same window `RI-15` and `RI-16` use.** That is [ADR-388](ADR-388.md) section 4's method verbatim, re-run rather than inherited.

| Verdict, over **290** named pointers in dated records on `ebc21238` | This row | [ADR-388](ADR-388.md) on `8fa66104` |
|---|---|---|
| True then, true now. Nothing owed | **169** | 169 |
| DRIFTED after being true. `G1` and `G2` both hold: outcome ONE | **76** | 76 |
| FALSE ON THE DAY IT WAS WRITTEN and still false: outcome TWO | **42** | 42 |
| False then, true now, by coincidence | **2** | 2 |

**ALL FOUR REPRODUCE EXACTLY, ON A DIFFERENT BASE, THROUGH AN INDEPENDENTLY BUILT COPY OF THE SAME READER.** This is the strongest confirmation of an inherited figure this row could take, and it is worth more than any of the ones the dispatch asked for: it means the population that would have to MOVE if a fourth outcome existed is measured, stable, and known.

**AND THE 289 REPRODUCES WHILE ITS STATED REASON DOES NOT.** That entry writes that of the 290 pointers passing `G1`, *"on one more the write commit is not recoverable, so 8,101 are undecidable and 289 are not"*. **On this base every one of the 290 has a recoverable write commit and exactly ONE is still undecidable, for a different reason**: [ADR-274](ADR-274.md) line 65 names `getStartupConf` and cites a vendored `pg` path this tree has never held, so no blob exists to read at any commit. **`git blame -w` gives its write commit as `baa352a1`, which is an ancestor of `8fa66104`, and that path resolves to no file at `8fa66104` either.** The count is right, the mechanism is a missing FILE rather than a missing COMMIT, and the difference matters because a missing commit is a property of the clone and a missing file is a property of the corpus. **This is not repaired**, section 6.

---

### 5. The price of the precondition, which is the number this row was dispatched to take

**THE TEST IS ONE READ AND IT NEEDS NO HISTORY: does the named subject appear ANYWHERE in the cited file on this tree?** Where it does, outcome one's remedy has a destination. Where it does not, the permission is empty.

| Population | Pointers | Precondition FAILS |
|---|---|---|
| Outcome ONE, dated records | **76** | **0** |
| Outcome TWO, dated records, including the 2 true-by-coincidence | **44** | **8** |
| Nothing owed, dated records | 169 | 0, by construction |
| Outcome ONE, source files | **3** | **0** |
| Outcome TWO, source files | **4** | **0** |
| Nothing owed, source files | 123 | 0, by construction |
| **Every pointer this corpus can decide** | **420** | **8** |

**EIGHT OF 420, AND ALL EIGHT WERE ALREADY FORBIDDEN.** Every one sits where `G2` has failed, so [ADR-388](ADR-388.md) refuses the repair before the precondition is ever reached. **NOT ONE lands in outcome one.** The eight are three subjects across six documents: `TS2741` and `TS2353` cited into a worker `index.ts` that no longer raises them, `STATEMENTS_UNREACHABLE` cited into `routes/affiliate.ts` at three sites, and `DATA_MODEL` cited into the data-model README.

**SO WHAT MOVES BETWEEN CLASSES IS NOTHING, AND THAT IS THE ANSWER TO THE DISPATCH'S THIRD QUESTION.** The 169, the 76, the 42 and the 2 are unchanged by the amendment. No pointer becomes repairable, none stops being repairable, and no entry in this corpus has to be re-read because of it.

**AND THE 76 DO NOT DRIFT GENTLY.** Their subjects sit between **3** and **401** lines from the coordinate that names them, median **31**. Only **3** of the 76 resolve to more than one candidate file. **A class-one pointer in this corpus is a live subject at a wrong number, every single time, and that is why the amendment costs nothing here.**

---

### 6. ADR-393's two instances, re-derived at primary source, and why the price above cannot see them

**BOTH REPRODUCE EXACTLY AND THIS IS THE SECOND INDEPENDENT DERIVATION.**

| Pointer | Write commit | The cited line AT that commit | On `ebc21238` |
|---|---|---|---|
| [ADR-098](ADR-098.md) line 53, for the `build`-key probe | `9fa12da1` | `hasOwnProperty.call(JSON.parse(read(p)).scripts ?? {}, 'build')`, **EXACT** | **GONE.** The token `'build'` occurs zero times in that runner and `INVENTORY_PROBES` is declared with an empty body |
| [ADR-119](ADR-119.md) line 24, for the register entry `'a demo-world seed script'` | `d835a1a6` | that literal, **EXACT** | **GONE.** Zero occurrences |

**AND NEITHER IS IN THE 420.** Run through the checks' own binder, both citations come back with **no name at all**: each writes its pointer inside a markdown link whose token already carries a path, which is the one shape where `citationsIn` declines to bind a name from inside the token. **So both sit in the 8,101 [ADR-388](ADR-388.md) calls undecidable, while being outcome ONE under that ruling's own words.**

**THAT IS [ADR-388](ADR-388.md) SECTION 17's FIRST CAVEAT ARRIVING WITH TWO INSTANCES ATTACHED.** It wrote that `G1` is measured with the checkers' binder, that `G1` is not the binder, and that 289 is therefore a FLOOR. **This row is the first to hold a member of the gap in its hand.** The corpus can price the amendment exactly at ZERO over everything it can count, and it cannot price it at all over the population where both known instances live, and **the honest figure is: a lower bound of 2, an exact 0 over the decidable set, and nothing derivable in between without reading 8,101 sentences.**

**WHICH IS WHY THE AMENDMENT SHIPS AND THE CLASS DOES NOT.** A class is a thing rows sort into. A class whose entire demonstrated membership is invisible to the census that would populate it is a class nobody can apply. **A precondition on a remedy is applied by the row standing in front of the pointer, which is exactly who [ADR-393](ADR-393.md) was when it found them.**

---

### 7. The convention for a wrong figure inside a dated record, ruled and then applied to this row's own item

**[ADR-394](ADR-394.md) SECTION 12 ITEM 2 HANDED THIS OVER AS A CONVENTION QUESTION IT DID NOT OWN THE FILE TO ANSWER.** [ADR-391](ADR-391.md)'s owed list says `ADR-376 line 92` is a positional reference in FOUR documents and says it was derived there.

**RULED: A WRONG FIGURE IN A DATED RECORD IS ANNOTATED AND NEVER OVERWRITTEN, AND THE ANNOTATION GOES AT THE SITE.** **ITS SCOPE IS RULED BY [ADR-401](ADR-401.md), WHICH [ADR-400](ADR-400.md) SECTION 17 ITEM 3 RECORDED AS OPEN WITH AN INSTANCE: THE CONVENTION REACHES ANY WRONG CLAIM IN A DATED RECORD AND NOT ONLY A NUMERIC ONE.** A sentence that claims its own derivation is annotated for the reason given here; a sentence that states only a consequence is annotated because a reader cannot tell the two apart from the sentence, and the practice is the same either way, **so the distinction is real, it is cheap to apply, and it mints no cell**. The argument is [ADR-388](ADR-388.md) section 2's second outcome carried from a coordinate onto a count, and it is stronger here than there. **The clause says the figure was derived HERE.** Replacing four with nine would leave a sentence claiming a derivation that produced nine, and no derivation ever did. **That is manufacturing evidence in the ruling's own sense: it converts a record of a count that was not taken into a record of one that was.** A coordinate at least has the excuse that nobody claimed to have checked it; a figure in an owed list is a claim to have measured.

**AND THE ANNOTATION GOES AT THE SITE ON [ADR-394](ADR-394.md)'s OWN EVIDENCE RATHER THAN ON TASTE.** That row measured the cost of the opposite practice: three rows derived [ADR-337](ADR-337.md)'s class-two verdict and the site recorded none of them, so a fourth was going to derive it again. **This figure has now been re-derived twice, by [ADR-394](ADR-394.md) and by this row, and until this diff the site carried no trace of either.**

**APPLIED, AND THE FIGURE IS WRONG UNDER EVERY READING RATHER THAN UNDER TWO.** [ADR-394](ADR-394.md) offered two readings and this row took six, three regexes at three commits:

| Reading | At the parent of that entry's commit | At that entry's own commit | On `ebc21238` |
|---|---|---|---|
| Documents, counting the link form `[ADR-376](ADR-376.md) line 92` as well as the bare phrase | **five** | **seven** | **nine** |
| Documents, the bare phrase alone | one | three | seven |
| Occurrences rather than documents, wide reading | seven | nine | sixteen |

**[ADR-394](ADR-394.md)'s FIVE AND SEVEN BOTH REPRODUCE EXACTLY, AND NO READING AVAILABLE PRODUCES FOUR AT ANY OF THE THREE COMMITS.** The figure was not stale and was not rounded. **It was wrong on the day it was written, which is `G2` failing on a count**, and it is left standing with the finding beside it exactly as a class-two pointer would be.

**AND THE THING THE FIGURE WAS ABOUT IS ALREADY DISCHARGED.** [ADR-394](ADR-394.md) repaired the retired sink clause at that site line-count neutrally, so line 92 is still line 92 and all nine documents keep saying something true. **The figure understated the blast radius of a repair that was then taken without one.**

---

### 8. The two `schedule.ts` clauses, repaired, and the neutrality that was derived rather than preferred

**[ADR-394](ADR-394.md) SECTION 4 WROTE THE REPLACEMENT OUT AND SAID THE ROW THAT TOOK IT SHOULD RE-DERIVE THE TWO CLAUSES RATHER THAN PASTE IT.** Both were re-derived at the file and both findings hold.

| Clause | What it asserts | Refuted at |
|---|---|---|
| The `ADR-376` header's exception reserving no value at all, **not even a refusing one**, for `ExpiryEventPort` | that this one port is uninhabited while the other two are | `UNWIRED_EXPIRY_SWEEP_IO` in [`sweeps/ports.ts`](../../apps/worker/src/sweeps/ports.ts) composes a refusing `ExpiryEventPort` **INLINE**, as a member of the same object literal the other refusals live in |
| The same header's later **STILL UNCALLABLE** | that the expiry constructor cannot be called | [`test/expiry-adapter.test.ts`](../../apps/worker/test/expiry-adapter.test.ts) calls it, and one case passes `UNWIRED_EXPIRY_SWEEP_IO.events` to it and asserts the same value back |

**THIS IS THE *PRECISE CLAIM DEGRADED BY RESTATEMENT INTO A FALSE ABSOLUTE* CLASS [ADR-382](ADR-382.md) NAMED AND [ADR-385](ADR-385.md) MEASURED, AND THE SECOND CLAUSE IS THE SHORTER COSTUME OF THE FIRST.** Both corrections keep their original clause whole and NAME what is retired rather than reproducing it, per `RI-14` and [ADR-367](ADR-367.md). **NEITHER VERDICT MOVES.** The header still refuses to wire anything, the bar still stands on what the sweep would DO rather than on what cannot be typed, and case `6.1` still asserts the absence of the default that would lift it.

**THE EDIT IS STRICTLY LINE-COUNT NEUTRAL AND THAT IS A MEASUREMENT, NOT A HABIT.** [ADR-386](ADR-386.md) ruled whole-file neutrality an explicit non-rule, so this row derived whether it was owed here instead of assuming it.

| | |
|---|---|
| Citations resolving to [`schedule.ts`](../../apps/worker/src/schedule.ts), through the checks' own reader | **72** |
| Of those, aimed BELOW the edited block | **28**, over **14** files |
| Distinct lines they aim at | **seven** |
| **Of the seven, exact on this base** | **SEVEN. Every one** |
| Further pointers at the same lines that NO reader in this repository can see, because they sit inside `OWN_PACKAGE` | **5**, in [`absence-claims.mjs`](../../packages/tooling/checks/absence-claims.mjs) and [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) |

**SO A ONE-LINE INSERTION IN THAT HEADER WOULD HAVE BROKEN 33 LIVE, EXACT POINTERS AT SEVEN DISTINCT LINES, AND FIVE OF THEM SIT IN FILES THIS ROW MAY NOT WRITE.** The citation-repair exception could not have covered them. **That is why neutrality was mandatory, and it is the first time this corpus has had to buy it in a SOURCE file rather than in a document.**

**AND IT COST SOMETHING, WHICH IS STATED RATHER THAN HIDDEN.** A markdown line is unwrapped, so [ADR-394](ADR-394.md)'s one-longer-line technique is free there. **A comment block wraps, so neutrality here means the two paragraphs are re-wrapped WIDER**, to at most 122 columns against the file's habit of about 78 and its existing maximum of 101. The file's longest comment line before this diff was 101 and `apps/worker/src/index.ts` already carries one at 120, `format:check` passes, and prettier does not reflow comments. **Both paragraphs keep every word they had. Nothing was compressed to make room and no other paragraph was touched.**

---

### 9. Citations this diff moved, and the six its own registry rows forced

**THE PROSE REPAIRS MOVE NOTHING, AND IT WAS CHECKED AT THE FILES RATHER THAN INFERRED FROM A GREEN RUN.**

- **[`schedule.ts`](../../apps/worker/src/schedule.ts): 16 lines replaced by 16 lines.** All seven cited lines below the edit were read back afterwards and every one is **byte-identical and at the same number**.
- **[ADR-388](ADR-388.md) and [ADR-391](ADR-391.md): one line replaced by one longer line each.** Neither file grew or shrank. **[ADR-392](ADR-392.md) cites [ADR-388](ADR-388.md) line 28 and line 28 has not moved.** No document on the base cites [ADR-391](ADR-391.md) by line at all, derived rather than assumed, and this entry is the first to do so.
- **The generated registries and [STATE](../STATE.md) grow at their tails or in place.** The two generated rows land at the END of their spans, [STATE](../STATE.md)'s section is appended after its last line and its one generated count is rewritten where it stands, and the deepest coordinate aimed into each of those three files is above every insertion, derived before the rows were written.

**AND THREE ROWS APPENDED TO THE ADR ALLOCATION TABLE MOVE EVERY LINE BELOW THEM BY THREE, WHICH IS [ADR-393](ADR-393.md) SECTION 11 ITEM 4's HAZARD ARRIVING AT THIS ROW'S OWN DIFF.** **Twenty pointers on nineteen lines** aim at [`ALLOCATION.md`](ALLOCATION.md) below that point, at **six** distinct coordinates, derived on the base rather than after the fact. **Each was classified before it was touched and only SIX are live.**

| Coordinate | Sites | What it is | This row |
|---|---|---|---|
| the `0078` reservation row | [ADR-312](ADR-312.md), **four pointers on three lines** | LIVE, exact before this diff | **REPAIRED TO THE NAME** |
| the invariant register's *"THE ROWS ARE READ OUT OF ..."* sentence | [ADR-275](ADR-275.md) line 66 and [session 466](../sessions/2026-08-30-session-466.md) line 26 | LIVE, exact before this diff, and both QUOTE the sentence beside the pointer | **REPAIRED TO THE NAME** |
| `:393`, `:523` and `:412` as they appear in [ADR-386](ADR-386.md), [ADR-388](ADR-388.md) and [ADR-389](ADR-389.md) | 10 | RETIRED coordinates and class verdicts recorded inside dated tables, not pointers | **LEFT.** Moving them would falsify records of what was measured |
| the `RI-37` row, `:638` | [ADR-337](ADR-337.md) line 70 and three records that cite it AS a finding | **CLASS TWO**, ruled born wrong by [ADR-388](ADR-388.md), re-derived by [ADR-389](ADR-389.md) and again by [ADR-394](ADR-394.md) | **LEFT WRONG**, for the fourth time |

**SO SIX REPAIRS, ALL SIX OUTSIDE THIS FENCE, ALL SIX FORCED BY THIS ROW'S OWN THREE ROWS, AND ALL SIX LISTED**, which is what the citation-repair exception requires. **None of the three files is row `396`'s**: [ADR-312](ADR-312.md) and [ADR-275](ADR-275.md) are not among the nine [ADR-393](ADR-393.md) section 3 names, and [session 466](../sessions/2026-08-30-session-466.md) carries no pointer into [`gates.mjs`](../../scripts/corpus/gates.mjs) at all, so it is not among the eighteen, derived rather than assumed.

**AND ALL SIX ARE REPAIRED TO A NAME RATHER THAN TO A FOURTH COORDINATE, WHICH ANSWERS [ADR-394](ADR-394.md)'s FIRST APPROVAL QUESTION WITH A MEASUREMENT RATHER THAN AN OPINION.** That row asked whether repairing a coordinate to a coordinate is a ritual that mostly re-arms the defect, and said the measurement that would settle it was not its to take. **It is takeable here, because the pointer carries its own receipt.** [ADR-312](ADR-312.md)'s four tokens read `` (was `:472`, and `:393` before that) `` on this base:

| Wave | The coordinate | Falsified by |
|---|---|---|
| the `0078` row cited at `:393` | exact at `0227ed87` | the allocation table growing |
| [ADR-389](ADR-389.md) repaired it to `:472` | exact on that base | **that same row's own three registry rows, and the next wave's** |
| [ADR-393](ADR-393.md) repaired it to `:475` | exact on this base | **this row's own three registry rows** |
| this row | **no coordinate at all** | nothing |

**THREE CONSECUTIVE WAVES REPAIRED THE SAME SIX POINTERS AND EACH REPAIR WAS FALSIFIED BY THE REPAIRING WAVE'S OWN REGISTRY ROWS.** The retirement list inside the pointer grew by one entry each time, and a fourth repair would have written `` (was `:475`, `:472`, and `:393` before that) ``. **That is not drift and no sibling caused it: the diff that repaired the pointer is the diff that broke it again.** [ADR-394](ADR-394.md) called that shape *the defect re-armed with a fresher date* and could show one instance; this is the same shape with three dated instances and a receipt.

**THE RETIRED COORDINATES ARE KEPT IN BACKTICKS WITH NO PATH AND ARE NEVER RE-CITED**, which is [ADR-388](ADR-388.md) section 4's document form and its seed F, so no reader and no check reads any of the three as a live second pointer.

---

### 10. Verification, every figure derived at the moment of reporting

| Runner | Base on `ebc21238` | After this diff |
|---|---|---|
| `node scripts/corpus/gates.mjs check` | 33 of 33 | **33 of 33** |
| `node packages/tooling/checks/repo-invariants.mjs` | 35 of 35 | **35 of 35** |
| `pnpm vitest run` | 336 / 8,109 / 10 / 0 | **336 / 8,109 / 10 / 0**, delta ZERO on every axis |
| `pnpm run typecheck` / `lint` / `format:check` | clean | **clean, clean, clean** |

- **THE GATE COUNT WAS DERIVED AND NOT ASSUMED.** It is read off the runner's own closing line on both runs and it is 33. The invariant count is read the same way and it is 35.
- **THE SUITE DELTA IS ZERO AND NOTHING ELSE COULD BE TRUE OF THIS DIFF.** No executable line changed, no test file was added, no case was added and the skip count is unchanged. **The delta is that fact rather than a claim about it.**
- **`git diff ebc21238 --name-only` NAMES ONE FILE OUTSIDE `docs/`** and every edit in it is inside a comment block, which was checked by reading the diff rather than by trusting the count.
- **SIX CITATIONS OUTSIDE THIS FENCE WERE MADE STALE BY THIS ROW'S OWN THREE REGISTRY ROWS AND ALL SIX ARE REPAIRED AND LISTED**, section 9. Every destination was read in [`ALLOCATION.md`](ALLOCATION.md) after the three rows had shifted it, and each repair is a NAME rather than a coordinate, so none of the six can be falsified again by a sibling's rows landing in the same table.
- **NO FALSIFICATION SEED WAS RUN AND THAT IS A STATEMENT ABOUT WHAT THIS DIFF IS.** It mints no check, adds no assertion and changes no executable line, so there is nothing here a seed could watch fail. **No seed file was touched, so none needed restoring.** A `sha256sum` manifest of the three files this row was dispatched to repair was taken before the first edit as a record of their pre-edit bytes, and no restore was performed with `git checkout --` or with anything else, because nothing was seeded.
- **THE READER COPY WAS DELETED AFTER THE LAST DERIVATION AND IT NEVER ENTERED THE TREE.** `git status --porcelain` named only this row's own files at every point after the first edit and was EMPTY before it.
- **`pnpm run verify` WAS NOT RUN AND [`falsify.mjs`](../../scripts/corpus/falsify.mjs) WAS NOT RUN.**
- **`395` AND `396` ARE RESERVED IN [ALLOCATION](ALLOCATION.md) so `CI-06f` stays gapless while two siblings run.** Neither reservation names a subject, neither guesses a branch name, neither claims anything and neither carries a citation. **The session numbers in them are DERIVED rather than guessed**: the offset between an ADR number and its session number is 195 on rows `388`, `389`, `391`, `392`, `393` and `394`, and on this row's own pair.

---

### 11. Where this entry is most likely wrong

- **THE ZERO IS A ZERO OVER A POPULATION THAT CANNOT SEE EITHER KNOWN INSTANCE, AND THAT IS THE WHOLE WEAKNESS.** Section 5 measures the precondition over the 420 the binder can decide; section 6 shows both demonstrated failures live outside it. **A founder who reads that as "the measurement missed the phenomenon" is reading it correctly.** The claim this row defends is narrower and it is the one the amendment needs: over everything the corpus can classify, naming a fourth class would re-sort nothing.
- **"THE SUBJECT IS GONE FROM THE CITED FILE" IS TESTED WITH THE BINDER'S NAME AND NOT WITH THE SENTENCE'S SUBJECT.** Where the binder binds a coarser token than the citing sentence names, the test finds the coarse token still present and reports the precondition satisfied. **That is exactly how [ADR-098](ADR-098.md)'s and [ADR-119](ADR-119.md)'s subjects would have been missed had they been in the population, so 8 is a FLOOR on the precondition's failures inside the 420 as well as outside it.**
- **THE STRUCTURAL ARGUMENT IN SECTION 2 IS ONLY AS GOOD AS THE CLAIM THAT `G1` AND `G2` ARE THE ONLY GATES.** A founder who holds that "is there a destination" is a THIRD gate rather than a precondition on a remedy would rule the other way, and would be minting a class whose measured population is zero and whose demonstrated population is two. **The disagreement is about where to put the same sentence, and nothing in this corpus behaves differently under either reading.**
- **SECTION 7's RULING IS ABOUT A FIGURE THAT CLAIMS TO HAVE BEEN DERIVED, AND NOT EVERY WRONG FIGURE MAKES THAT CLAIM.** [ADR-391](ADR-391.md)'s says *derived here* in those words, which is what makes overwriting it evidence-manufacturing. **A figure copied without that claim is a weaker case and this row did not measure how many are which.**
- **THE WIDER COMMENT WRAP IN [`schedule.ts`](../../apps/worker/src/schedule.ts) TOUCHES A CONVENTION THAT IS LOAD-BEARING SOMEWHERE ELSE.** [ADR-328](ADR-328.md)'s *window of two is a measurement and not a choice* rests on comment prose in this repository wrapping near column 78, and it is named by its sentence rather than by a coordinate because that file is not this row's. **This row's two paragraphs carry no citation whose name has to stay inside that window**, checked at the reader, and `RI-15` and `RI-35` were both run after the edit. **But the convention is now visibly broken in one header and the next row to wrap prose in that file should read this paragraph.**
- **THE `ADR-274` FINDING IN SECTION 4 IDENTIFIES [ADR-388](ADR-388.md)'s ONE UNDECIDABLE MEMBER BY ELIMINATION.** It is the only member of the 290 for which `G2` is unmeasurable on this base, and that entry's 290 and 289 both reproduce, so it is almost certainly the same pointer. **This row did not re-run that census at `8fa66104` and cannot prove the identification.**
- **THIS ENTRY DATES ITSELF 2026-09-06 AND THE CONTAINER CLOCK READS 2026-09-07.** The wave dates itself 2026-09-06 and the dispatch named a session log under that date, so the wave was followed rather than the clock, and it is said here rather than left as a discrepancy somebody has to explain.

---

### 12. What is owed, stated precisely enough to be picked up

1. **[ADR-388](ADR-388.md) SECTION 4's TABLE IS NOW A REPRODUCED FIGURE AND ITS SECTION 16 QUESTION 1 IS UNTOUCHED.** Whether `G2` should ever be enforced by a check rather than merely required is still that entry's question and still founder-owed, and this row's re-derivation strengthens the reason it named for refusing: option (c) would go red on **42** dated records nobody may repair, and that 42 is now measured twice. **ANSWERED BY [ADR-400](ADR-400.md), AND NEITHER HALF OF THIS SENTENCE SURVIVES.** The 42 is **43** under this entry's own instrument on `c219c0d1` and **39** under [ADR-396](ADR-396.md)'s corrected one, so it is measured three times and agrees twice; and option (c) could never have gone red on a dated record, because `RI-15` reads 754 source files and no document. **The pointer that moved is [session 478](../sessions/2026-08-30-session-478.md) line 9, re-blamed by row 396's own repair to a different pointer on the same line.**
2. **THE PRECONDITION HAS NO POPULATION AND CANNOT GET ONE WITHOUT A HUMAN `G1`.** Both known instances are invisible to the binder. **Nobody should re-derive the price without first deciding whether `G1` is read by a person or by `citationsIn`**, because the two give 0 and at-least-2 and both answers are honest under their own definition. That decision is the founder's and it is bigger than this row.
3. **[ADR-393](ADR-393.md) SECTION 9 ITEMS 1, 2, 3 AND 5 ARE UNTOUCHED**, and none was this row's: the 63 wrong pointers into [`gates.mjs`](../../scripts/corpus/gates.mjs) over 35 files, the copies of [ADR-330](ADR-330.md)'s five in three append-only files, the class-two pointer in the session 521 log, and its runbook statement.
4. **THE CONTAINER ARRIVES WITH NO REPOSITORY AND HAS NOW DONE SO FOR MANY CONSECUTIVE SESSIONS.** Every row pays the clone, the deepen and the install out of its own budget and every row records it. **It is the only item in this list that no ADR can fix**, which is [ADR-394](ADR-394.md) section 12 item 3 repeated verbatim because nothing about it has changed.

---

### 13. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** Inherited as well as owed: [ADR-357](ADR-357.md) and [ADR-360](ADR-360.md) through [ADR-394](ADR-394.md) are `proposed` and unsigned, two rows run beside this one holding `395` and `396`, and this row settles none of them.

  **What a founder read adds and this entry cannot.**

  **THE FIRST THING TO DECIDE IS WHETHER A REFUSAL WITH A ZERO IN IT IS THE RIGHT DELIVERABLE.** This row was dispatched to decide whether a fourth outcome is real and it shipped an amendment to a sentence instead. **The measurement is what made that the honest answer**: the classification's four buckets reproduce exactly on a different base, the new condition fires eight times in 420, and all eight sit where repair was already forbidden. **A founder who wanted a class can have one in one line, and it would sort nothing that is not already sorted.**

  **THE SECOND IS THAT THE ONLY TWO KNOWN INSTANCES OF THE THING THIS ROW PRICED ARE OUTSIDE EVERYTHING THIS CORPUS CAN COUNT.** [ADR-388](ADR-388.md) said its decidable set was a floor. **This row is the first to show what is standing on the other side of it**, and the gap is not a rounding: it is 8,101 pointers wide, and the two instances that provoked a whole ruling amendment both live in it. **If the corpus wants to price anything about repairability again, the first question is not which class a pointer is in. It is whether `G1` means what a person can read or what `citationsIn` can bind**, and those two have never been the same test.

  **AND THE THIRD IS SECTION 8, WHICH IS A SMALL DIFF WITH A LARGE FINDING UNDER IT.** Repairing two sentences in one comment block required proving that thirty-three pointers at seven lines would survive it, and five of those pointers sit in a file this row was forbidden to open. **The prose in that header is now effectively frozen against any edit that adds a line**, and the only reason this repair was possible at all is that a comment can be re-wrapped wider. **The next false absolute in that file may not be so lucky, and that is a fact about the corpus's citation habit rather than about this row.**

- **Approval:** `___________________________` (UNSIGNED. **MONEY-ADJACENT, ADR-003 STRICT, `E2` READ OWED.** **Merging is not signing.**)

| | |
|---|---|
| **Approved by** | |
| **Date** | |
