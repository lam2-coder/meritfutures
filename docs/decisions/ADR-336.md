## ADR-336: three of the fourteen migrations `RI-37` still registers as having no landing record get one, `0068`, `0070` and `0072` measured against PostgreSQL 16.13 rather than reconstructed from their ADRs, the backlog register shrinks from fourteen entries to eleven in the same commit, and two sentences inside two merged migration headers are found false on today's tree without either file being edited (2026-09-05, status: proposed)

**[ADR-334](ADR-334.md) SHIPPED `RI-37` AND A SHRINK-ONLY REGISTER OF TWENTY MIGRATIONS THAT HAVE NO LANDING RECORD.** [ADR-335](ADR-335.md) took six of them, wrote [`DELTA_MANIFEST`](../../packages/db/DELTA_MANIFEST.md) sections 40 to 45 and left **fourteen**. That register can only shrink, and its own finding text says the row *"comes out in the same commit as the section that closes it. An exemption left standing behind a repair is how an allowlist stops being read."*

**THIS ROW TAKES THREE OF THE FOURTEEN AND THEY ARE THE WITHDRAWAL-APPROVAL RUN.** `0068`, `0070` and `0072` are the dual-control ceiling, the approval columns and the terminal-transition trigger: one `CHECK`, four columns with six `CHECK`s and a constraint trigger, and one column with two `CHECK`s and two more constraint triggers. All three are `MONEY PATH` by their own headers. **This wave has been building on them all day**: [ADR-305](ADR-305.md) slice 8 rests on `0072`, [ADR-325](ADR-325.md) cites [`0072:211-218`](../../packages/db/migrations/0072_terminal_withdrawal_transitions.sql) for `WD-C2`, and [ADR-335](ADR-335.md) section 4 found that `probe_ledger_constraints.sql` cannot run at `0057` because it needs a column `0070` adds. **A reader who went looking for what these three did found nothing.** They now hold **sections 46 to 48** and their three register rows are deleted in the same commit.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `336`, branch `claude/the-approval-chain-unrecorded`, session **527**.
- **NOT THE MONEY PATH BY FILE, AND IT IS THE RECORD OF THREE MONEY-SCHEMA MIGRATIONS.** `git diff --stat 02d298b8..HEAD -- packages/db/migrations scripts/db` is **EMPTY**, measured at the moment this line was written. No SQL, no migration number, no `SD-nn`, no `U-nn`, no route behaviour. **NONE OF THE THREE IS EDITED**, which is constitution `E2`: migrations are sacred, superseded by addition and never edited.
- **AN `E2` READ IS OWED ANYWAY**, and three are inherited, all `proposed` and all UNSIGNED: [ADR-228](ADR-228.md), [ADR-232](ADR-232.md) and [ADR-234](ADR-234.md). This diff writes down what three money-schema migrations did to `account_adjustments` and `wallet_withdrawals`, and a reader trusting a section that was reconstructed rather than measured would be trusting a copy.
- **THE BASE IS [PR #689](https://github.com/lam2-coder/meritfutures/pull/689)'s HEAD, `02d298b8`, DELIBERATELY.** It carries `main` plus [ADR-334](ADR-334.md) and [ADR-335](ADR-335.md), both unmerged but present. Re-derived before a line was written and not taken from the dispatch: `git ls-tree origin/main --name-only docs/decisions/` ends at `ADR-332.md`, so `main` carries through [ADR-332](ADR-332.md) and `333`, `334` and `335` are present beneath this branch. **`336` opens no `CI-06f` hole.**
- **A DISPATCH PROMPT IS NOT A SOURCE.** Every figure below is a `psql` run against a database this session built, a `grep` this session ran, or a runner's own last line. **Two sentences inside two merged migration headers are false on today's tree** and both causes are executed rather than argued (sections 5 and 6). Neither file is edited.

---

### 1. What was re-derived before the first edit, and what the baseline was

**EVERY ROW BELOW WAS RUN ON `02d298b8` BEFORE THE FIRST EDIT.**

| Claim | Read at | Held |
|---|---|---|
| `main` carries through [ADR-332](ADR-332.md); `333`, `334` and `335` sit beneath this branch | `git ls-tree origin/main --name-only docs/decisions/` against `ls docs/decisions/` | **YES.** No `CI-06f` hole |
| There are 35 invariants and 33 gates on this ref | Both runners' own last lines, read separately | **YES.** `35 of 35` with `RI-37` as the last PASS, and `33 of 33` |
| `RI-37`'s backlog register holds FOURTEEN entries and NINE are money path | The live `LANDING_RECORD_BACKLOG` literal, and `grep -c 'E2 READ: MONEY PATH'` over each of the fourteen files | **YES.** Fourteen rows, and the header returns 1 on nine of them |
| The three named migrations are on disk, merged, and money path | `ls`, and `grep -c 'E2 READ: MONEY PATH'` returning 1 on each of the three | **YES** |
| 45 is the maximum section number | `grep -n '^## [0-9]' packages/db/DELTA_MANIFEST.md` | **YES.** `## 45.` is `0057` |
| Section 1's migration-sequence table cannot hold them | It runs `0001` to `0029` and its own text closes the table at 27 | **YES.** A `## <n>.` section is the only available shape |
| PostgreSQL 16 is available for the measurement | `psql --version` -> **16.13**, `pg_isready` accepting | **YES** |
| No name any of the three retires is a `CI-06/retired-constraints` needle | `grep -c 'DROP CONSTRAINT'` over the three | **YES. ZERO on all three.** None of them drops anything, so the gate's `sites: 7` register entry for this document cannot move |

**Baseline, reproduced on `02d298b8` BEFORE the first edit and quoted off each runner's own last line:** `pnpm vitest run` **312 files, 7,618 passed, 10 skipped, 0 failed**, exit 0; invariants **35 of 35**; gates **33 of 33**; `git status --porcelain` empty. **Skips are reported separately from passes throughout, because a skip is not a pass.**

---

### 2. The measurement, and it is the whole row

**NINE DATABASES WERE BUILT.** `packages/db/migrations/*.sql` applied forward-only into an empty database under `ON_ERROR_STOP=1` and stopped at `0067`, `0068`, `0070` and `0072`, plus one carrying every migration on disk, three more carrying every migration **except** one of the three, and two scratch copies of the full set carrying an instrumented trigger function (section 3). Every count below is read from `pg_tables`, `pg_indexes`, `pg_constraint`, `pg_trigger` and `pg_proc`, every constraint body from `pg_get_constraintdef`, and every trigger property from `pg_trigger` and `pg_get_triggerdef`.

| | `0067` | `0068` | `0070` | `0072` |
|---|---|---|---|---|
| tables | 115 | 115 | 115 | 115 |
| indexes | 401 | 401 | 401 | 401 |
| CHECK constraints | 495 | **496** | **502** | **504** |
| foreign keys | 167 | 167 | **168** | 168 |
| unique constraints | 12 | 12 | 12 | 12 |
| non-internal triggers | 26 | 26 | **27** | **29** |
| functions | 112 | 112 | **113** | **114** |
| migration files applied | 63 | 64 | 65 | 66 |

**THE DELTA IS THE NUMBER A READER USES.** `0068` adds one CHECK. `0070` adds six CHECKs, one foreign key, one constraint trigger and one function. `0072` adds two CHECKs, two constraint triggers and one function. **Between `0067` and `0072` the table, index and unique-constraint totals do not move once**, and **not one index is created by any of the three**: `0070` adds four columns to `wallet_withdrawals` and none of them is indexed, including the foreign key.

**`assert_no_floats.sql` holds over all four applied schemas**, exit 0 each, so no file in the run declares a non-integer numeric column. **Money in all three is integer cents**: `dual_control_threshold_cents` is `bigint` on both tables and the ceiling is the integer literal `500000`.

**EVERY FIGURE THE THREE ADRs STATE IS REPRODUCED TO THE UNIT.** [ADR-228](ADR-228.md) section 3's *"all 64 migration files ... giving 115 tables"*; [ADR-232](ADR-232.md) section 8's *"All 65 migrations ... 115 base tables"* and its fence's *"115 tables, 27 triggers, 65 migration files"*; [ADR-234](ADR-234.md) section 6's *"All 66 migrations ... 115 base tables"*. **Nine figures, nine agreements, and the agreement is stated rather than assumed** because a landing record whose numbers were transcribed from its own ADR is a copy and not a record.

#### 2.1 Re-application: all three are refused, and two of them by a column rather than a constraint

| File | Re-applied against its own landing schema |
|---|---|
| `0068` | **refused**, `constraint "account_adjustments_dual_control_threshold_ceiling" for relation "account_adjustments" already exists` |
| `0070` | **refused**, `column "approved_at" of relation "wallet_withdrawals" already exists` |
| `0072` | **refused**, `column "cancelled_at" of relation "wallet_withdrawals" already exists` |

Re-applying `0001` against `0001`..`0072` is refused at `type "identity_status" already exists`, which is the standing check every section in the manifest runs.

**NONE OF THE THREE IS [ADR-335](ADR-335.md)'s `0055`.** That file was accepted on a rerun because it carried no `INSERT` and no `CREATE`; each of these carries an `ALTER TABLE ... ADD`, which is what refuses them. **Two of the three are refused by the COLUMN and not by the control**, which matters for the same reason `0053`'s and `0056`'s refusals mattered one row ago: a reader reasoning about a partial replay from the error message would place the refusal in the wrong statement. `0070`'s six CHECKs and its trigger are never reached on a rerun.

#### 2.2 The constraint bodies and the trigger properties, read out of the catalogue

`account_adjustments_dual_control_threshold_ceiling` reads `CHECK ((dual_control_threshold_cents <= 500000))`, bare, because `0038` declares that column `NOT NULL`. `wallet_withdrawals_dual_control_threshold_ceiling` reads `CHECK (((dual_control_threshold_cents IS NULL) OR ((dual_control_threshold_cents > 0) AND (dual_control_threshold_cents <= 500000))))`, which is `0068`'s bound **plus** `0038`'s `> 0` restated, on a column that is nullable here. **The same ruling has two shapes on two tables and the difference is nullability**, which no ADR states and which a reader comparing the two constraint names would otherwise have to derive.

**AND `0072`'s TWO CONSTRAINT TRIGGERS ARE `NOT DEFERRABLE INITIALLY IMMEDIATE`, WHICH THE FILE DOES NOT SAY.** `CREATE CONSTRAINT TRIGGER` without a `DEFERRABLE` clause defaults to immediate, and `0072` writes no such clause; measured at `pg_trigger`, `tgdeferrable` and `tginitdeferred` are both **false** on `wallet_withdrawals_not_born_terminal` and `wallet_withdrawals_terminal_transition_is_drawn`, and both **true** on `0057`'s `wallet_withdrawals_terminal_obligation_is_zero` and `0070`'s `wallet_withdrawals_dual_control_is_real`. **So `WD-C2` decides at the statement and `WD-C1` decides at the commit, on one table**, and that ordering was watched: `transferring --> failed` with no `LT-09` is refused by `WD-C1` and not by `WD-C2`, which is [ADR-234](ADR-234.md) case `G` reproduced. It is recorded in section 48 because `0057`'s own comment reasons at length about deferrability and a reader carrying that reasoning across to `0072` would get it backwards.

---

### 3. THE PROBE COVERAGE, derived by execution rather than claimed, and NONE of the three is covered

**THE SET WAS DERIVED THREE WAYS AND THEN EXECUTED.** A grep for every object name the three files create over `scripts/db/`, which returns **nothing**; a grep for `wallet_withdrawals`, `account_adjustments`, `dual_control`, `approved_at` and `cancelled_at` over the same directory, which finds five files; and then, for each of the three, applying every migration on disk **with that file removed** and running all 22 probes, [`assert_no_floats.sql`](../../scripts/db/assert_no_floats.sql), [`assert_ledger_scope_agrees.mjs`](../../scripts/db/assert_ledger_scope_agrees.mjs) and [`assert_append_only_grants.mjs`](../../scripts/db/assert_append_only_grants.mjs) against the result.

| Migration | Readers under `scripts/db/` that go RED without it | Result |
|---|---|---|
| `0068` | **NONE.** All 22 probes and all three assert scripts exit 0 with the file removed | **NOT COVERED** |
| `0070` | [`probe_ledger_constraints.sql`](../../scripts/db/probe_ledger_constraints.sql) and [`probe_payout_hold.sql`](../../scripts/db/probe_payout_hold.sql), **both at `column "approved_at" of relation "wallet_withdrawals" does not exist`**, exit 3. Neither reaches an assertion | **NOT COVERED. A DEPENDENCY IS NOT COVERAGE** |
| `0072` | **NONE.** All 22 probes and all three assert scripts exit 0 with the file removed | **NOT COVERED** |

**SO THE HONEST SENTENCE IS THAT NONE OF THE THREE HAS A PROBE**, and for `0070` that is a sharper statement than a green counterfactual would have been. The two files that go red do so because a column is missing, at the `INSERT` that builds their fixture, before any `EXCEPTION WHEN check_violation` block is entered. **A reader taking those two reds as coverage would conclude that `0070`'s dual-control controls are watched.** They are not, and section 3.1 is how that was established rather than inferred.

#### 3.1 The tripwire, which is the half a removal counterfactual cannot show

A removal says whether a reader NOTICES a file. It does not say whether a reader ever ENTERS the code that file installed. **Both trigger functions were replaced, in scratch copies of the fully applied database, with bodies that announce themselves**, and every probe under `scripts/db/` was run against each copy.

- **`assert_withdrawal_dual_control_is_real()` (`0070`) fired ZERO times.** No committed reader ever writes `dual_control_approval_id`, so the trigger's `WHEN (new.dual_control_approval_id IS NOT NULL)` is never satisfied and `WD-DC1` through `WD-DC4` have never been evaluated by anything in this repository.
- **`assert_withdrawal_terminal_transition_is_drawn()` (`0072`) fired FOUR times**, all of them `UPDATE transferring -> failed`, all inside `probe_ledger_constraints.sql` at lines 445, 476, 538 and 584. That is [ADR-234](ADR-234.md)'s *"one drawn arrow in this estate that any fixture actually walks"*, measured. **It is the arrow the trigger PERMITS.** Every arm it refuses is unwatched.
- **`account_adjustments_dual_control_threshold_ceiling` (`0068`) is evaluated on every run of [`probe_wallet_debit_provenance.sql`](../../scripts/db/probe_wallet_debit_provenance.sql) and accepted on every run of it.** That file, at `:126-133`, writes `dual_control_threshold_cents` of **500000**, which is the ceiling's own value, so the constraint is exercised exactly at the boundary and never past it. Measured on the full applied set inside a rolled-back transaction: `500000` reaches the foreign key and `500001` is refused by name.

**THE PATTERN ACROSS ALL THREE IS ONE PATTERN.** Every committed reader in this repository walks the arm these files PERMIT and none walks an arm they REFUSE. That is not a criticism of any probe: `probe_ledger_constraints.sql` exists for the ledger and reaches `wallet_withdrawals` only because `WD-C1` needs a terminal transition. It is the reason this row perturbed sixteen of the seventeen refusals by hand (sections 4.1 and 4.2) and wrote the results into the sections, because a landing record that quoted a green probe transcript here would be quoting a file that never asks the question.

---

### 4. What was perturbed by hand, since nothing under `scripts/db/` does it

**THE THREE FILES INSTALL SEVENTEEN REFUSALS AND SIXTEEN OF THEM WERE REACHED BY HAND ON THE FULL APPLIED SET INSIDE ROLLED-BACK TRANSACTIONS**, alongside nine permitted cases, because a harness that only ever attempts forbidden things passes against a control that rejects everything. **THE SEVENTEENTH IS `WD-DC1` AND IT IS UNREACHABLE**, because the foreign key refuses a missing approval row before the trigger runs, which is exactly what `0070`'s own comment predicts of that branch. Thirty perturbations in all: twenty-one refused and nine reaching the foreign key or committing.

#### 4.1 `0068` and `0070`

[ADR-228](ADR-228.md) section 3.2's four cases reproduce exactly, **and so does the counterfactual that entry calls *"the whole reason this entry exists"***: case A re-run against the set with `0068` removed stops at `account_adjustments_identity_id_fkey`, so a 100000000-integer-cent credit naming a threshold no adjustment can reach clears every `CHECK` on the table. [ADR-232](ADR-232.md) section 8's twelve reproduce exactly, including [ADR-232](ADR-232.md)'s own note that case `K` fires on `..._operator_approval_records_threshold` where `..._unapproved_records_no_approval` would also have refused it. **That second constraint was then ISOLATED rather than left inferred**, by a `requested` row carrying a recorded threshold and no `approved_at`, which it refuses by name. The machine arm is permitted at 1,000,000 integer cents with `approved_by` NULL, which is [ADR-232](ADR-232.md) section 3's ruling executed; an operator approval at exactly the threshold is refused and one cent below it is permitted.

**`WD-DC1` IS REFUSED BY THE FOREIGN KEY AND NOT BY THE TRIGGER**, which is what `0070`'s own comment predicts: the reference is checked first, so the trigger's `IF NOT FOUND` branch is defensive against a path the foreign key already closes. `WD-DC2`, `WD-DC3` and `WD-DC4` each fire under their own label, and a real, approved, self-requested approval commits.

#### 4.2 `0072`

All four `WD-C2` arms and both new CHECKs fire under their own messages: `INSERT` at `settled`, `cancelled --> requested`, `approved --> cancelled`, `approved --> settled`, a `cancelled` row with no `cancelled_at`, and a `cancelled_at` on a `requested` row. `cooling --> cancelled` is permitted, which is the drawn arrow. **That reproduces [ADR-234](ADR-234.md) section 6's cases `C`, `E`, `I`, `J`, `K`, `M` and `N`**, and case `G` besides.

---

### 5. FINDING: `0068`'s header states a grep result that is no longer this tree's, and the cause is `0070`

[`0068`](../../packages/db/migrations/0068_dual_control_threshold_ceiling.sql) header item 1 states that *"`grep -c 'dual_control'` over `apps/api/src/routes/payouts.ts`, `admin-payouts.ts`, `wallet-withdrawals.ts` and `wallet.ts` returns 0, 0, 0 and 0"*, and concludes that *"the founder's threshold is UNDISCHARGED on the path their question named"*.

**MEASURED TODAY WITH THE FILE'S OWN COMMAND: 0, 0, 5 and 0.** `wallet-withdrawals.ts` gained the five when [`0070`](../../packages/db/migrations/0070_withdrawal_approval_and_dual_control.sql) put `dual_control_approval_id` and `dual_control_threshold_cents` on `wallet_withdrawals` two migrations later, and [ADR-232](ADR-232.md) wired the route to them. **The threshold `0068` says is undischarged on the withdrawal path is discharged**; it remains undischarged on `payout_requests`, where all three payout-side counts are still 0.

**THE CAUSE IS THAT A HEADER STATED A PROPERTY OF A TREE.** A grep result is a fact about a working copy on a day, and `0068` is merged and permanent (constitution `E2`), so the sentence cannot be updated and should not be. **It is written into manifest section 46** so that the reader most likely to run that grep, which is the reader checking whether the gap is still open, finds the answer before concluding from a stale zero that nothing has moved.

---

### 6. FINDING: `0070` says it drives no edge and `0072` says its hazard is theoretical, and both are false today

[`0070`](../../packages/db/migrations/0070_withdrawal_approval_and_dual_control.sql) header section 5 item 1 states: *"IT DRIVES NO EDGE. Nothing in this tree writes `wallet_withdrawals.status` after the creation INSERT ... the writers are the INSERT at `apps/api/src/routes/wallet-withdrawals.ts` and one UPDATE at `apps/worker/src/sweeps/expiry.ts`, and the second writes the freeze trio and `updated_at` only."*

[`0072:27-34`](../../packages/db/migrations/0072_terminal_withdrawal_transitions.sql) states of a cancel from `approved`: *"Today it lands harmlessly, because no door drives the approval edge and `LT-06` therefore never posts ... THE DAY THE POSTING LANDS IT STOPS BEING HARMLESS."*

**THERE IS A THIRD WRITER AND IT DRIVES EXACTLY THAT EDGE AND POSTS EXACTLY THAT ENTRY.** [`apps/worker/src/withdrawals/approval-sweep.ts`](../../apps/worker/src/withdrawals/approval-sweep.ts) opens *"THE `LT-06` WITHDRAWAL-APPROVAL DRIVER. THE TRANSITION AND THE POSTING TOGETHER, IN ONE TRANSACTION"*, is ruled by [ADR-316](ADR-316.md) and transcribed by [ADR-325](ADR-325.md) off [ADR-305](ADR-305.md) section 7 slice 7, writes `status: 'approved'` and `approvedAt` at [`:556-557`](../../apps/worker/src/withdrawals/approval-sweep.ts), lists *"6. POST `LT-06`"* as step 6 of its own transaction, and is wired at [`schedule.ts:245`](../../apps/worker/src/schedule.ts) under `entryPoint: 'runWithdrawalApprovals'`.

**THE TWO SENTENCES FAIL DIFFERENTLY AND THE DIFFERENCE IS THE POINT.**

- **`0070`'s is now simply wrong** and a reader taking it as current would conclude the four columns are unwritten in production shape. They are written on every approval.
- **`0072`'s is DISCHARGED RATHER THAN STALE.** It named a condition, said what would happen when the condition was met, and the condition has been met. The arrow it would have opened was already refused, because `0072` merged on 2026-08-29 and the driver landed afterwards. **That is the whole argument for writing a control before the row it controls**, which manifest section 34 recorded under that name one migration set earlier.

**AND THE DRIVER IS ALSO WHY `0070`'s CONTROLS ARE UNREACHED.** [`approval-sweep.ts:563-565`](../../apps/worker/src/withdrawals/approval-sweep.ts) writes `approvedBy`, `dualControlApprovalId` and `dualControlThresholdCents` all `null`, with a comment saying the DDL requires it. `approved_by IS NULL` is the first disjunct of `..._operator_approval_dual_controlled` and the guard on `..._operator_approval_records_threshold`, so **the threshold, the ceiling and all four trigger arms are unreachable from any code path in this tree**, which is exactly what [ADR-232](ADR-232.md) section 3 rules should be true until an operator door exists. Section 3.1's zero firings and this file's `null` triple are the same fact read from two ends.

**NEITHER MIGRATION IS AMENDED**, which is constitution `E2`. Both were true of the tree their authors measured, and both corrections live in manifest sections 47 and 48 and here.

---

### 7. Where the three sections go, and why the order inside them is NOT [ADR-335](ADR-335.md)'s reason

**SECTION NUMBERS IN THIS FILE ARE THE ORDER RECORDS WERE WRITTEN, NOT THE ORDER MIGRATIONS RAN**, which sections 35, 36 and 37 prove by being `0079`, `0078` and `0082`. Section 16's own rule: *"Sections are append-only records of what landed and when, so the sequence only ever grows and the maximum is the only thing anybody needs."* **45 is the maximum, so 46 to 48 are the next three free numbers.**

**WITHIN THE THREE THE ORDER IS MIGRATION ORDER, AND THE REASON IS DIFFERENT FROM THE ROW BEFORE THIS ONE.** [ADR-335](ADR-335.md) ordered its six by migration because they were one supersession chain on one constraint name, and its approval block asked whether that was one rule or two. **These three supersede nothing**: `grep -c 'DROP CONSTRAINT'` returns 0 on all three, and no two of them touch the same object. What binds them is a **citation chain**, each file naming the one before it in its own text: `0070`'s header calls its ceiling *"`0068`'s repair, arriving with the column instead of nine migrations later"*, and `0072`'s column comment reads *"`0011` gave settled its settled_at, `0070` gave approved its approved_at"*. **A reader taking them out of order reads a reference to a file that has not been introduced.**

**SO THE ANSWER TO [ADR-335](ADR-335.md)'s OPEN QUESTION IS THAT ITS RULE HOLDS AND ITS REASON DOES NOT GENERALISE.** Records written in one commit are ordered by their migrations; commits are ordered by when they landed. The supersession chain was one sufficient reason for that ordering and a citation chain is another, and neither is written into section 16 here, because writing an allocation rule on behalf of every future session is what that table's standing rule forbids.

**A LETTERED SECTION WAS CONSIDERED AND REFUSED**, on [ADR-334](ADR-334.md)'s reasoning verbatim: `4a` is the escape hatch for a section that belongs in the MIDDLE, and three records written today belong at the end of a sequence ordered by when records were written. A `31a` through `31c` would claim these three records were written between `0066`'s and `0067`'s, which they were not.

**ALL THREE ROWS ARE IN SECTION 16'S SECTION-NUMBER TABLE, WRITTEN IN THE SAME COMMIT AS THE SECTIONS.** That upholds the table's standing rule rather than trading it: *"a row written on somebody else's behalf is a claim nobody made"*, and none of these three is on anybody else's behalf. **The rows that table still owes are NOT filled in here and are named in the `48` row instead**: 24, 26 to 30 and 32 to 34 are claimed by headings and carry no row, which the `31`, `35`, `37`, `45` and now `48` rows each record, and nothing compares the table to the headings. That is the second half of the manifest's own section-16 remedy and it is owed rather than taken, **by two rows now and this is the third**.

---

### 8. The falsifiability test: the check RED on each of the three, GREEN with the section

**THE REGISTER SHRINKING IS ITSELF THE ASSERTION AND IT IS WRITTEN AS A PAIR.** Before this row `RI-37` was SILENT on all three, because leg 3 absorbed them; the gap was invisible by construction. Removing a register row without writing the section turns leg 1 red; writing the section without removing the row turns leg 3 red. **Both directions were watched.**

**LEG 3 WAS FALSIFIED BY HAND BEFORE THE SUITE WAS WRITTEN**, by putting `0068`'s row back into the live register with section 46 in place:

```
FAIL   RI-37  Every migration on disk has a landing record in the delta manifest  (1)
       RI-37's backlog register holds `0068` (0068_dual_control_threshold_ceiling.sql,
       ADR-228. MONEY PATH) and packages/db/DELTA_MANIFEST.md now carries its landing
       record at packages/db/DELTA_MANIFEST.md:2908 ... The register may only shrink
```

The row was then removed again and the runner returns to `PASS`.

**AND [`migration-landing-record.test.ts`](../../packages/db/test/migration-landing-record.test.ts) ASSERTS BOTH DIRECTIONS FOR EACH OF THE THREE:**

- **Three reconstruction cases.** Each removes one of sections 46 to 48 from the **live** manifest, asserts the removal changed something, and requires exactly one finding naming that migration's file. **These cases can only be red because the register row came out.**
- **Three counterfactuals beside them.** The same manifest with the MIGRATION removed as well, on which no record is owed, watched GREEN. A check red on both is catching nothing.
- **All three at once**, requiring exactly three findings, which is the tree this row was dispatched against with its register rows already gone.
- **A cluster case on the heading shape**, re-run rather than inherited from [ADR-335](ADR-335.md)'s. These three headings are the first in the file where a sibling of the cluster could be named in the heading text, so removing section 46 must report `0068` **and nothing else**, once per section.
- **The size case moved from `toHaveLength(14)` to `toHaveLength(11)`**, and its list of numbers is a deliberate second copy of the register rather than a derivation from it, so a session writing a section goes red beside its register row.

---

### 9. The pre-edit grep, and what already pins this subject

**`grep -rn "DELTA_MANIFEST"` over `apps`, `packages`, `scripts` and `.github`, restricted to `.mjs`, `.ts`, `.tsx`, `.js`, `.yml` and `.json` and excluding `node_modules`, returned 56 lines in 18 files**, which is [ADR-335](ADR-335.md)'s 53 in 18 plus that row's own additions. Four sites pin this subject and each was read before a line was written, and **two further greps were added for this row.**

| Site | What it pins | Moved? |
|---|---|---|
| [`gates.mjs:982`](../../scripts/corpus/gates.mjs) | the `manifest_changes` span, which counts table rows whose FIRST CELL matches `SD-nn` or `U-nn`. Three new sections add **six** delta-table rows, so this was the span at risk | **No.** All six are keyed `ADR-228`, `ADR-232` and `ADR-234`, exactly as sections 33 to 45 key theirs; `generate` reports *"every span already matches its query"* |
| [`gates.mjs:8412`](../../scripts/corpus/gates.mjs) | `CI-06/retired-constraints` registers this document at **`sites: 7`** | **No**, and it was CHECKED rather than assumed. `grep -c 'DROP CONSTRAINT'` returns **0 on all three files**, so no section written here can name a retired constraint. The directory holds **23 lines matching `DROP CONSTRAINT` of which 21 are statements**, the other two being [`0048:273-274`](../../packages/db/migrations/0048_audited_writes_on_append_only_tables.sql)'s comment explaining why that file uses `DROP INDEX` instead; **21 reproduces [ADR-335](ADR-335.md)'s figure exactly** |
| [`gates.mjs:166`](../../scripts/corpus/gates.mjs) | `isCorpusDocument` names this file **by hand**, its comment saying a by-name list *"is fine at one entry; if it reaches three it needs a rule instead of a list"* | **No.** Still one entry |
| [`repo-invariants.mjs:7476`](../../packages/tooling/checks/repo-invariants.mjs) | `RI-31`, no document repeats a whole line carrying a generated span | **No.** None of the three sections carries a generated span |
| **A line-number sweep for the three migrations**, `grep -rno` for a backticked `0068`, `0070` or `0072` followed by a colon and digits, over `docs/`, `apps/`, `packages/` and `scripts/` | **26 line-number citations of the three migrations**, at seven distinct targets: `0068:141`, `0070:151`, `0070:153`, `0070:200-203`, `0072:27-34`, `0072:123-125` and `0072:211-218` | **ALL SEVEN HOLD**, each opened and read. `0068:141` is the `ALTER TABLE`, `0070:151` is the `0011 IS UNTOUCHED` idiom, `0070:153` is `approved_at`, `0072:211-218` is the cancel-from-approved arm [ADR-305](ADR-305.md), [ADR-325](ADR-325.md), [ADR-326](ADR-326.md), [ALLOCATION](ALLOCATION.md), [STATE](../STATE.md) and [`approval-sweep.ts`](../../apps/worker/src/withdrawals/approval-sweep.ts) all cite |
| **`grep -rn "DELTA_MANIFEST\.md:[0-9]"` over `docs/`, `apps/`, `packages/` and `scripts/`** | **the `DELTA_MANIFEST.md:2043` citation [ADR-260](ADR-260.md) and [session 451](../sessions/2026-08-30-session-451.md) carry** | **Already stale before this row and moved further by it**, section 12 finding 4, exactly as [ADR-335](ADR-335.md) reported one row ago |

**THE FIFTH GREP IS THE ONE THAT PAID, AND WHAT IT PAID IS AN ABSENCE.** Twenty-six line-number citations of three frozen migrations, and not one of them has drifted, because constitution `E2` makes the target immovable. **That is the argument for citing a migration by line and against citing a living file by line** in one measurement, and it is why sections 46 to 48 cite `approval-sweep.ts` by line with the caveat that the file is not frozen.

---

### 10. What this row did not do

- **No SQL, no migration, no `SD-nn`, no `U-nn`.** `git diff --stat 02d298b8..HEAD -- packages/db/migrations scripts/db` is **EMPTY**. None of the three is edited, including the two headers sections 5 and 6 find false (constitution `E2`).
- **NO NEW CHECK AND NO NEW GATE.** Invariants stay at 35 and gates at 33; the `CI-06` letter series is untouched and no slug is taken. `RI-37` is [ADR-334](ADR-334.md)'s and this row uses it rather than extending it.
- **NOTHING LOOSENED, AND THE REGISTER SHRANK RATHER THAN BEING TRIMMED.** Three rows came out because three sections went in, in one commit, which is leg 3's stated mechanism. What counts as a landing record is unchanged.
- **The other eleven gaps are NOT repaired.** They are a separate row, which is the dispatch's own instruction and [ADR-334](ADR-334.md) section 6's reasoning.
- **NO PROBE WAS WRITTEN**, and three are now named as owed. `scripts/db/**` shows a zero-line diff.
- **No dated record was edited and no ADR was amended.** [ADR-228](ADR-228.md), [ADR-232](ADR-232.md), [ADR-234](ADR-234.md), [ADR-334](ADR-334.md) and [ADR-335](ADR-335.md) all stand exactly as written.
- **Section 16's owed rows are NOT filled in**: 24, 26 to 30 and 32 to 34 stay named rather than written, on that table's own rule, for the third row running.
- **[`docs/INDEX.md`](../INDEX.md) was NOT edited beyond its generated spans**, on [ADR-322](ADR-322.md)'s, [ADR-334](ADR-334.md)'s and [ADR-335](ADR-335.md)'s reason: an ADR is a registry entry and needs no `INDEX.md` row.
- **ONE EDIT SITS AT THE EDGE OF THE FENCE AND IS DECLARED RATHER THAN QUIET, WHICH THIS ROW'S FENCE ANTICIPATED.** Two sentences ABOVE the register rows state its size, in its doc comment and in `RI-37`'s `covers` string: *"FOURTEEN remain and NINE of them open with an `E2 READ: MONEY PATH` header"* and *"IT OPENED AT TWENTY UNDER ADR-334 AND HOLDS FOURTEEN, NINE of them carrying"*. **Both became false the moment three rows came out.** They are corrected to eleven and six, **counted off the eleven remaining files rather than off the rows**. No leg, no reader and no finding text moved.

---

### 11. Verification, each command run separately and each exit code read without a pipe

- `node packages/tooling/checks/repo-invariants.mjs` -> **35 of 35 invariants hold**, exit 0, unchanged from the baseline: this row adds no check.
- `node scripts/corpus/gates.mjs generate` then `check` -> **33 of 33 gates pass**, exit 0, unchanged: this row adds no gate.
- `pnpm vitest run` -> counts in section 13, exit 0.
- `pnpm run lint`, `pnpm run format:check`, `pnpm run typecheck` -> clean, exit 0 each.
- Four forward-only installs under `ON_ERROR_STOP=1` against PostgreSQL **16.13** stopping at `0067`, `0068`, `0070` and `0072`; one full install; three full-set-minus-one installs; two instrumented scratch copies; three re-application attempts; all 22 probes and all three assert scripts run against every one of them; and thirty hand-written perturbations inside rolled-back transactions, twenty-one refused and nine permitted.
- **`pnpm run verify` was NOT run and `scripts/corpus/falsify.mjs` was NOT run**, both forbidden by the row, the second because it mutates the working tree.

---

### 12. Found and NOT repaired

1. **NONE OF THE THREE HAS A PROBE** (section 3), executed by removal for all three and by tripwire for the two triggers. `0068`'s only committed writer sits exactly on its bound and never past it; `0070`'s trigger has never been entered; `0072`'s trigger is entered only on the arrow it permits. **Closing any of the three is a probe block rather than a migration**, and `scripts/db/**` is a zero-line diff on this row.
2. **`0070`'s OPERATOR ARM IS UNREACHABLE FROM EVERY WRITER IN THIS TREE** (section 6). That is [ADR-232](ADR-232.md) section 3's ruling working as intended and not a defect, and it means six CHECKs and four trigger arms on the money path have never run against a row. The day an operator door lands, they run for the first time in production.
3. **`0068`'s HEADER GREP AND `0070`'s "IT DRIVES NO EDGE" ARE FALSE ON THIS TREE** (sections 5 and 6). Both files are merged and permanent; the corrections live in manifest sections 46 and 47 and here.
4. **THE `DELTA_MANIFEST.md:2043` CITATION IN [ADR-260](ADR-260.md) AND [session 451](../sessions/2026-08-30-session-451.md)** was already stale by 24 lines before [ADR-335](ADR-335.md), was moved six further by that row, and is moved three further by this one's section-16 rows. Both are dated records and one is a merged ADR.
5. **Sections `24`, `26` to `30` and `32` to `34` are claimed by headings and carry no row in section 16's section-number table.** `RI-37` does not read that table, so the two registries can still disagree, and the check that they agree is the manifest's own stated remedy. **Named by three consecutive rows now.**
6. **`RI-37` STILL NEVER READS A LANDING SECTION'S CONTENT.** A section reading `## 49. \`0059\` lands` and one sentence satisfies it exactly as well as section 47 does. That limit is stated in the check's own `covers`.
7. **THE ELEVEN REMAINING BACKLOG ENTRIES**, six of them money path, counted off the files at the moment this line was written: `0037`, `0039`, `0040`, `0041`, `0043`, `0044`, `0050`, `0059`, `0063`, `0073`, `0074`.
8. **Unchanged from the rows before this one**, each re-read and none in this fence: [ADR-305](ADR-305.md) slice 8's stale `worker/src/index.ts:1326-1331` citation; `identity_restriction_episodes.sla_due_at` still invisible to `CI-06l`; [`schedule.ts:220`](../../apps/worker/src/schedule.ts)'s reachability clause; [`data-model-columns.mjs`](../../scripts/corpus/data-model-columns.mjs) still unregistered; [`purchases.md`](../architecture/data-model/purchases.md)'s *"Why the wallet constraints are three and not one"*; `0053`'s missing probe; and [`probe_wallet_debit_provenance.sql:53`](../../scripts/db/probe_wallet_debit_provenance.sql)'s *"seven scoped accounts"* against a measured three.

---

### 13. Counts, derived at the moment they are stated

Suite **312 files / 7,626 passed / 10 skipped / 0 failed**, against a `02d298b8` baseline reproduced **before the first edit** of **312 files / 7,618 passed / 10 skipped / 0 failed**; the delta is **+8 cases in one existing file with skips unchanged and no file added**, all eight in [`migration-landing-record.test.ts`](../../packages/db/test/migration-landing-record.test.ts): three reconstructions, three counterfactuals, the all-three case and the heading-shape cluster case. Invariants **35 of 35**, unchanged. Gates **33 of 33** after `generate`, unchanged. `RI-37`'s own sizes, derived off the shipped reader's rules at reporting time: **75 migration(s) on disk, 64 recorded by `packages/db/DELTA_MANIFEST.md` across 29 section-1 rows and 37 landing headings, 11 on the backlog register**, and 64 plus 11 is 75. `git diff --stat 02d298b8..HEAD` against `packages/db/migrations` and `scripts/db` **empty**. Lint, `format:check` and typecheck clean.

---

### Fence

[`packages/db/DELTA_MANIFEST.md`](../../packages/db/DELTA_MANIFEST.md) (sections 46 to 48 and three section-number rows), [`packages/tooling/checks/repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) (three register rows deleted, and the two size sentences above them corrected with them, which this row's fence names explicitly), [`packages/db/test/migration-landing-record.test.ts`](../../packages/db/test/migration-landing-record.test.ts), this entry, [ALLOCATION](ALLOCATION.md) row `336`, [the session log](../sessions/2026-09-05-session-527.md), [STATE](../STATE.md) and the two registry READMEs.

**NO CITATION REPAIR WAS NEEDED INSIDE THE FENCE AND THAT WAS CHECKED RATHER THAN ASSUMED.** The three manifest sections are appended after section 45, the three section-number rows are inserted inside a table nothing cites by line, and the register rows sit inside a `Map` literal nothing cites by line. `RI-15` and `RI-16` both hold, which is how the absence of a repair is known rather than hoped. The one line-number citation this diff moves is outside the fence and is section 12 finding 4.

**THE TWO REGISTRY READMEs AND EVERY GENERATED SPAN ARE GENERATED AND NOT HAND-WRITTEN**, rewritten by `node scripts/corpus/gates.mjs generate` from this entry's own first line and the log's `index` comment.

---

### Approval

**Status: `proposed`. The approval line is UNSIGNED and merging is not signing.** No `status` field anywhere moves and nothing is marked `accepted`. **`E2` READ OWED**, and three are inherited, all `proposed` and all UNSIGNED: [ADR-228](ADR-228.md), [ADR-232](ADR-232.md) and [ADR-234](ADR-234.md).

- **Approval:** `___________________________` (UNSIGNED. **Merging is not signing.**)

  **What a founder read adds and this entry cannot.**

  **THE FIRST QUESTION IS WHETHER "NOT COVERED" THREE TIMES IS A FINDING OR AN INDICTMENT, AND NO CASE IN THIS DIFF CAN ANSWER IT.** Section 3 says plainly that none of these three money-path migrations has a probe, and section 3.1 goes further: the one trigger a probe does enter, it enters only on the arrow the trigger permits. **So the honest summary of this repository's committed coverage of the withdrawal approval chain is that it checks the schema installs and nothing else.** I think that is worth knowing and I would rather you read it as a work item than as a verdict, because [ADR-232](ADR-232.md) and [ADR-234](ADR-234.md) each executed twelve and seventeen cases by hand on the day they landed and both records are good. **What is missing is not diligence; it is a committed file that re-asks those questions on every push.** Three probe blocks would close it, they are cheap, and this row was forbidden to write them. If you read section 3 and think a landing record that reports "NOT COVERED" without a follow-up row is a report that will be read once and filed, say so and it becomes a row.

  **THE SECOND IS THAT I FOUND TWO MERGED MIGRATION HEADERS SAYING THINGS THAT ARE FALSE TODAY, AND I THINK ONLY ONE OF THEM IS A PROBLEM.** `0070`'s "IT DRIVES NO EDGE" is simply wrong now and could mislead. `0072`'s "the day the posting lands it stops being harmless" is the opposite: it named a condition, the condition was met, and the migration was already there to catch it. **I want you to test whether that distinction is real or whether I am being generous to the file I liked more.** The practical question behind it is whether a migration header should make claims about the surrounding tree at all. `E2` freezes the file, so every such claim has a half-life, and this corpus has now spent three rows discovering that. **The alternative rule is that a migration header states what the migration does and nothing about what else exists**, which would have cost `0068` its best paragraph.

  **THE THIRD IS THE ORDERING RULE, WHICH [ADR-335](ADR-335.md) LEFT OPEN AND I HAVE NOW ANSWERED TWICE WITHOUT WRITING IT DOWN.** That entry ordered six sections by migration because they were a supersession chain and said the general rule it would write if forced was "records written in one commit are ordered by their migrations". **I ordered three by migration for a different reason, a citation chain, and I did not write the rule into section 16 either.** Two rows have now made the same choice on different grounds and neither has bound the next one. **That is either healthy restraint or a convention forming by accident**, and the next session writing four of the eleven will be the third to decide it alone. If you want it settled, section 16 is where it goes and it is one sentence.

  **WHERE THIS ENTRY IS MOST LIKELY WRONG, named rather than left to be found.** **My tripwire measures ENTRY into a trigger function and not evaluation of the constraint it guards**, and those are not the same thing. For `0070` the two coincide, because the `WHEN` clause is the whole guard. For `0068` there is no trigger at all and I substituted a boundary walk, which shows the constraint cuts where it says and not that any committed reader watches it cut. **A reader who thinks "fired zero times" is a stronger claim than I am entitled to make about `0072` is right in one direction**: I measured that the function is entered four times and that all four are the permitted arrow, but I did not prove that no probe exercises `WD-C2` through some path that never reaches the function, because there is no such path. That last clause is reasoning and not measurement, and it is the one sentence in section 3.1 I would want a second reader on.
