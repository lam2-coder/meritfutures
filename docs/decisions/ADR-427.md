## ADR-427: the enumeration kind is given a RETENTION TIER rather than a filter, its precision goes from 1 in 18 to 1 in 2 without one site being dropped, a walker spelling nobody folded turns 171 unresolved reads into 137 and finds a dependant no row has named, and the one-hop default is DECIDED and kept (2026-09-07, status: proposed)

**Row 427 of the 422s wave. Branch `claude/dependants-precision`, off `claude/wave-422s` at `bfd6c400`, session 622.**

**Status `proposed`. The approval line in section 11 is UNSIGNED and merging is not signing. THE DISPATCHER STATES IT HAS NO AUTHORITY TO SIGN AND THIS ENTRY DOES NOT WAIT ON ONE. NO `E2` READ IS WAITED FOR EITHER; section 10 names the files that want one.**

1. **THE PRECISION CAN BE IMPROVED AND IT WAS, AND THE REASON IT DID NOT GO QUIET IS STRUCTURAL RATHER THAN A PROMISE.** [ADR-422](ADR-422.md) section 11 handed on that `enumeration` has a precision of **1 in 18** against the red set on its test target, that the module can not tell REACH from RETENTION, and that modelling the caller's filter *"would be the first place this module could go quiet"*. This row adds a `retention` TIER and not a filter. **The kind still reports 18 files and its own precision is still 1 in 18, because nothing was dropped; the `selected` tier holds 2 files and 1 of them is red, which is 1 in 2.** Section 4.
2. **A TIER CANNOT GO QUIET AND A FILTER CAN, AND THE SUITE WATCHES THE DIFFERENCE RATHER THAN THE ENTRY ASSERTING IT.** One of the eleven RED seeds turns the tier INTO a filter, dropping the sites it marks `excluded`. **Three cases go red.** That seed is the whole argument of this row expressed as something a run can execute. Section 8.
3. **`excluded` IS A NEGATIVE DEPENDENCE AND STILL A DEPENDENCE, WHICH IS WHY THE SITE STAYS IN THE LIST.** A walk reaches the target and the caller filters its answer on content the target does not carry: a DELETION is not felt, and a CHANGE to the target's bytes would be. The assertion under it is green BECAUSE of what the target does not contain, and that is not a property a reader may assume is stable. Section 3.
4. **THE WALKER RECOGNISER READ ONLY THE FIRST ARGUMENT OF A `join`, AND FIXING THAT TURNED 171 UNRESOLVED DIRECTORY READS INTO 137, OF WHICH 5 ARE THIS ROW'S OWN FIXTURES.** It also found a **dependant of the compatibility module that no row has ever named**, and removed one that was never a dependant at all. **The count of files reached by a kind that breaks on deletion is 21 before and 21 after, and its MEMBERSHIP changed by one in and one out.** Section 5, and it is written for row 425 to read.
5. **THE TEST RUNNER'S CONFIGURATION IS NO LONGER UNDECIDED.** ADR-422 recorded that it computes its include lists and that *"nothing in this tree can currently say which files that config reaches"*. Four spellings are folded and everything else is still named UNDECIDED. Section 6.
6. **THE ONE-HOP DEFAULT IS DECIDED AND KEPT, AND THE RUN NOW REPORTS THE SIZE OF WHAT IT IS SHORT BY.** ADR-422 left this open as *"not this row's to decide"*. It is decided here against widening, on the ground that a two-hop entry means something weaker than a one-hop entry, and the honest repair is to make the default's incompleteness DERIVED on every ordinary run rather than inferred to be zero. Section 7.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `427`, session **622**, off the wave integration branch `claude/wave-422s` rather than off `main`.
- **NON-MONEY IN ITS DIFF AND MONEY-ADJACENT IN ITS SUBJECT.** No migration taken or reserved, no SQL, no float introduced, no `_cents` handling changed, no financial value read or written.
- **NO GATE ADDED, WIDENED OR WEAKENED, NO LEG ADDED, NO PROBE CHANGED, NO NEEDLE ADDED OR TIGHTENED, NO CENSUS ADDED OR EDITED, NO MARKER ADDED TO THE ABSENCE VOCABULARY, NO ARTIFACT ADDED TO OR REMOVED FROM ANY REGISTER, NO `RI-nn` MINTED OR RESERVED, NO TEST SKIPPED, DISABLED OR QUARANTINED, NO TRIPWIRE DELETED OR RELAXED, NO MANIFEST EDITED, NO `VG-12` ADMISSION ASKED FOR, NO `CI-06` LETTER SPENT, NO MIGRATION.**
- **THE DIFF IS TWO EXISTING FILES UNDER `packages/tooling` PLUS THE REGISTRIES.** `packages/tooling/checks/dependants.mjs` and `packages/tooling/test/dependants.test.ts`, both created by ADR-422 and both inside this row's fence. **NOTHING UNDER `apps/**`, `packages/db/**`, `packages/ledger/**`, `scripts/**` OR THE REST OF `packages/tooling/**` IS TOUCHED**, and `repo-invariants.mjs` and `absence-claims.mjs` were read and not edited.
- **NO EXISTING EXPORT, TYPE, FUNCTION OR RUNTIME BEHAVIOUR OUTSIDE THIS MODULE CHANGES.** Nothing in the tree imports it except its own suite.
- **THE COMPATIBILITY MODULE IS NOT RETIRED AND NOT ONE BYTE OF IT IS IN THIS DIFF.** The dispatch forbade it and this row did not go near it. Section 5 records the one thing this row changes about what is REPORTED for it.

---

### 1. The container, measured rather than asserted

**THE CONTAINER ARRIVED WITH NO REPOSITORY, AND THAT IS SIXTEEN CONSECUTIVE ROWS.** Every figure below is what the command printed on this row's clone.

| measurement | observed |
|---|---|
| repository present on arrival | **none.** `ls -la /home/user` returned two entries, `.` and `..` |
| `git`, `node`, `pnpm` | present: `2.43.0`, `v22.22.2`, `10.33.0` |
| clone command | `git clone https://github.com/lam2-coder/meritfutures.git`, one invocation, no retry, no depth flag |
| wall clock for the clone | **17.634s real**, measured by `time` |
| shallow | **NO.** No depth flag, so section 8's worktree probe is possible |
| `.git` on disk | **196M** |
| base checkout | `git checkout claude/wave-422s`, resolving to **`bfd6c400`** |
| dispatch's stated base | `claude/wave-422s` at its current head, and this is that head |
| `pnpm install --frozen-lockfile` | **11.656s real**, `git status --porcelain` empty after it |

**THE ASK IS NOW ONE ROW OLDER AGAIN.** [ADR-416](ADR-416.md) asked for the container image to be fixed or the dispatch line deleted, [ADR-421](ADR-421.md) recorded that neither happened, [ADR-422](ADR-422.md) recorded the fifteenth occurrence, and this is the sixteenth. **The cost this row paid is 17.6 seconds**, which is small, and the reason to keep recording it is that it is the only evidence the ask went unanswered.

### 2. The baseline, on a clean tree, before the first edit

| leg | observed on `bfd6c400` |
|---|---|
| `gates.mjs generate` | `every span already matches its query.`, exit 0, `git status --porcelain` empty after it |
| `gates.mjs check` | **33 of 33** gates pass |
| `repo-invariants.mjs` | **35 of 35** invariants hold |
| `pnpm vitest run` | **341 files, 8,246 passed, 10 skipped, 0 failed** |
| `pnpm run typecheck` | exit **0** |
| `pnpm run lint` | exit **0** |
| `pnpm run format:check` | `All matched files use Prettier code style!` |

**THE BASE IS CLEAN AND NOTHING WAS REPORTED TO STOP ON.** The suite figures differ from ADR-422's `340 / 8,241` because this base is two rows further on; they are derived here and not carried. `pnpm run verify` was **NOT** run and `scripts/corpus/falsify.mjs` was **NOT** run, both forbidden by the row.

**EVERY FIGURE ADR-422 HANDED ON WAS RE-DERIVED ON THIS BASE BEFORE ANY OF IT WAS ACTED ON**, because this programme has caught a sibling's measurement wrong three times this week. All of them reproduce exactly.

| ADR-422's figure | re-derived here |
|---|---|
| `import` 1, `path-literal` 9, `enumeration` 20 sites over 18 files, `config-glob` 4 | **identical** |
| `prose` 72, of which 61 dated | **identical** |
| distinct files reached by a kind that breaks on DELETION: 21 | **21** |
| directory reads repo-wide that did not fold: 171 | **171** |
| `tsconfig` `paths` aliases: 0 | **0** |
| closure at 1, 2 and 3 hops: 4, 6, 15 | **4, 6, 15** |
| the deletion: `34 of 35` invariants, 4 files and 9 cases red | **34 of 35, 4 files, 9 cases, the same four files** |

**ADR-422's SECTION 6 MEASUREMENT STANDS ENTIRELY, INCLUDING ITS FIFTH FILE.** The deletion was re-performed in a `git worktree` off the same object store, the worktree removed with `git worktree remove --force`, and the working tree's copy of the subject file confirmed present and 88 lines throughout.

### 3. The tier, and why it is a tier

**THE ONE DESIGN DECISION IN THIS ROW IS THAT NOTHING ADDED HERE MAY REMOVE A SITE, SHORTEN A LIST OR MOVE A COUNT.** `retention` attaches a word to a site that is reported either way, exactly as the `near` tier attaches one to an `undecided` entry without dropping any of the 171. **A tier that is wrong costs a reader one wasted look. A filter that is wrong costs a reader the file**, and that asymmetry is the whole of what ADR-422 was pointing at when it called this the first place the module could go quiet.

| value | what it means |
|---|---|
| `selected` | a CONTENT predicate this file applies to the walk's answer ACCEPTS the target's real bytes, so the target is inside the set the file then asserts over |
| `excluded` | predicates were folded and the target's bytes fail every one. **A deletion is not felt through them and a CHANGE to the target's bytes would be.** Negative dependence, and still dependence |
| `unfiltered` | no predicate this module folds. **The tier that claims the least**, and the one every unrecognised spelling falls into |
| `unreadable` | the target's bytes could not be read, so no filter could be evaluated against them |

**ONLY CONTENT PREDICATES ARE READ, AND THE REASON IS THAT THEY CANNOT BE SELECTION.** A directory walk in this tree selects on an entry's NAME: an extension, a prefix, a skip set. It never selects on an entry's BYTES, because the walk has not opened the file yet. **So a predicate over a file's content is always applied DOWNSTREAM of the walk**, to the walk's answer, which is the one place where reach and retention differ. Reading name predicates as well would need this module to decide which side of the walk each one sits on, and deciding that wrong in the quiet direction is the failure being avoided.

**THE BIAS IS STATED RATHER THAN TUNED AND IT RUNS ONE WAY: ANYTHING UNFOLDABLE COUNTS AS SENSITIVE.** A predicate the module does not recognise, a target it cannot read, a pattern it will not compile: every one yields `unfiltered`. **`excluded` is reachable only when a predicate WAS folded and the target's real bytes were tested against it and failed.** The recogniser for a locally declared reader is bounded to a short declaration body for the same reason: a long walker body that happens to read a file is not a reader, and calling it one is the only route by which this tier could invent a predicate that excludes something nothing excludes.

### 4. What the tier is worth, measured against a red set derived on this base

**THE RED SET IS THE ONE SECTION 2 RE-MEASURED IN A WORKTREE AND NOT ADR-422's, THOUGH THEY ARE THE SAME FOUR FILES.** Of the 18 files the `enumeration` kind reports for the compatibility module, exactly one is in it.

| tier | files | of which red | precision |
|---|---|---|---|
| `selected` | **2** | **1** | **1 in 2** |
| `unfiltered` | **8** | **0** | claims nothing, by design |
| `excluded` | **8** | **0** | |
| **the kind, whole** | **18** | **1** | **1 in 18, unchanged, because nothing was dropped** |

**BOTH HALVES OF THAT TABLE ARE THE RESULT.** The `selected` tier is a ninefold improvement on the figure ADR-422 handed on, and the kind's own precision is deliberately identical to it, because the 16 files outside `selected` are still reported, still counted, and still in the 21 a taker must read. **A reader who ignores the tier is left exactly where ADR-422 left them and never anywhere worse**, which is the property that makes it safe to ship a ranking at all.

**THE TWO `selected` FILES ARE THE SUITE THAT GOES RED AND ONE THAT DOES NOT**, and the one that does not is `selected` because the compatibility module's last line spells a package specifier that file's predicate looks for. That is a true hit on a real predicate and not a defect: the file's assertion genuinely reads the target's bytes and genuinely finds that string in them.

### 5. The walker spelling nobody folded, and what it found

**THE RECOGNISER READ ONLY THE FIRST ARGUMENT OF A `join`.** A walker spelled around `readdirSync(join(ROOT, dir))` over a parameter `dir` was therefore not recognised as a walker at all, its read was counted against the standing figure for what the kind cannot aim, and its call sites were never followed. It now keeps the whole directory EXPRESSION and folds it at the call site with the parameter bound to what was passed, which handles both spellings under one rule. **A walker's DECLARATION is also no longer counted as one of its own call sites**, which was a parameter list being reported as a directory read that did not fold.

| figure | ADR-422 | here |
|---|---|---|
| directory reads repo-wide that did not fold | **171** | **137** |
| of those, sites inside this module's OWN suite | **4** | **5** |
| the figure with this module's own suite set aside | **167** | **132** |
| `enumeration` sites for the compatibility module | 20 | **21** |
| distinct files under `enumeration` | 18 | **18** |
| **distinct files reached by a kind that breaks on DELETION** | **21** | **21** |

**THE HEADLINE COUNT DID NOT MOVE AND ITS MEMBERSHIP DID, AND THIS PARAGRAPH IS WRITTEN FOR ROW 425 TO READ AGAINST ITS OWN RECORD.**

- **`scripts/corpus/gates.mjs` IS IN, AND NO ROW HAS EVER NAMED IT.** Its `walk` takes the directory as a parameter inside a `join` whose first argument is the root constant, and it is called over the whole tree. It genuinely enumerates a directory containing the compatibility module. It tiers `excluded`.
- **`apps/site/src/content/lint-cli.ts` IS OUT, AND IT WAS NEVER A DEPENDANT.** Its walker's inner read joins the root parameter to a SECOND local variable the old recogniser ignored, so the old fold resolved the walk to the repository root and reported the whole tree in its reach. The walk in fact never leaves that deployable's content directory. **It has not been dropped: it is now named in the `undecided` register**, which is where a read this module cannot resolve belongs.
- **The 21 files a taker must READ is unchanged, and one of the names on that list is different.** ADR-419's five to EDIT and ADR-422's sixth to WATCH are untouched by this row, and neither of the two files above is among them.

**THIS ROW DID NOT RETIRE THE COMPATIBILITY MODULE, DID NOT EDIT IT, AND DID NOT EDIT ANY FILE ROW 425 HOLDS.** What changed is what the instrument reports about it, and it changed in the direction of one true positive gained and one false positive returned to `undecided`.

### 6. The configuration that computed its own reach

ADR-422 recorded that the test runner's configuration builds its include lists through a `.map` the module did not fold, that it was reported UNDECIDED on every run, and that **nothing in this tree could say which files it reaches**. Four spellings now fold: an array of literals, a binding holding one, a spread of two, a `.map` to a template over the element, and a call to a bound arrow returning one.

**THE FOLD IS ATTEMPTED FIELD BY FIELD RATHER THAN FILE BY FILE**, because the old reading asked whether ANY include was computed and declared the whole configuration undecided on the answer, losing a list it could read beside one it could not. **A refusal is still an UNDECIDED entry and never an absence**, which is the only property that makes folding safe to attempt: a fold that fails leaves the configuration exactly where ADR-422 left it, named on every run.

It reports **0 undecided entries** for that configuration now, and both the `include` and the `exclude` sides resolve, including the one built by calling a bound arrow. **It answers a question about the compatibility module with `no`, correctly**: that configuration's patterns reach test files and the compatibility module is not one, so this row adds no `config-glob` site to that target and the figure there is still 4.

### 7. The sixth kind, and the default this row was allowed to decide

ADR-422 section 11 handed on that whether the default should ever be more than one hop was *"not this row's to decide"*, because a default returning fifteen files where four are direct teaches its reader to skim. **THIS ROW DECIDES IT AND KEEPS THE DEFAULT AT ONE HOP**, on that argument and on a second one ADR-422 supplies: a two-hop entry means *"red for as long as some other file is unrepaired"*, which is weaker than what a one-hop entry means, and merging them makes every entry mean the weaker thing. **A list that has quietly changed what its entries mean is a list that has gone quiet without one site being dropped.**

**AND THE HONEST REPAIR IS NOT TO THE DEFAULT BUT TO WHAT THE DEFAULT SAYS ABOUT ITSELF.** ADR-422 measured a file going red that is outside the one-hop closure and inside the two-hop one, so a caller reading the default is short by an amount that is DERIVABLE rather than unknowable. Every ordinary run now derives it and names it without listing it:

| target | one hop | two hops | reported as "not listed above" |
|---|---|---|---|
| the compatibility module | **4** | **6** | **2** |

The cost is one further pass over the population, **about 0.8s on this tree**, against **about 1.2s** for the derivation it is added to. **A caller who did not ask for depth is still not handed a list whose entries mean something weaker; they are handed the SIZE of the one they did not ask for.**

### 8. Every mechanism watched RED before any green was trusted

**EACH MECHANISM WAS DISABLED IN TURN, THE SUITE RUN, AND THE FILE RESTORED FROM A COPY VERIFIED WITH `sha256sum -c`.** A `git checkout --` restore was deliberately not used, on [ADR-410](ADR-410.md)'s recorded lesson. The tree was verified after every one.

| mechanism removed | suite |
|---|---|
| the tier stops distinguishing at all | **3 failed, 54 passed** |
| **the tier becomes a FILTER and drops what it marks `excluded`** | **3 failed, 54 passed** |
| a locally declared reader stops being recognised | **1 failed, 56 passed** |
| a regular expression bound to a name stops folding | **1 failed, 56 passed** |
| the chained `includes`/`match` arm of the predicate scan | **2 failed, 55 passed** |
| the `.map` arm of the pattern-list folder | **2 failed, 55 passed** |
| the call arm of the pattern-list folder | **2 failed, 55 passed** |
| a refusal to fold stops being named UNDECIDED | **1 failed, 56 passed** |
| the walker recogniser reads only the first argument again | **1 failed, 56 passed** |
| the declaration is counted as a call site again | **1 failed, 56 passed** |
| the transitive footer on an ordinary run | **1 failed, 57 passed** |

**THE SECOND ROW IS THE ONE THIS ENTRY WOULD MOST LIKE READ.** It is the difference between a tier and a filter written as something a run can execute, and without it every other case in this block could be satisfied by a module that had quietly become the worse thing.

**ONE CASE THIS ROW WROTE WAS CHECKED FOR THE DEFECT ADR-422 SECTION 8 RECORDS.** The eleven seeds above are how it was checked, and no case in this diff survived a seed that should have killed it.

### 9. The constraints the dispatch handed this row, and how each is honoured

| its constraint | how |
|---|---|
| **a quieter instrument that misses a dependant is worse than a noisy one that does not** | the governing decision. A tier, not a filter, watched RED from both ends. Sections 3 and 8 |
| **watch a RED for every mechanism you add** | eleven, section 8 |
| **do NOT retire the compatibility module, row 425 holds it** | not retired, not edited, not opened for writing. Not one byte under `apps/**` is in this diff |
| **if your changes alter what it reports for that target mid-flight, say so** | section 5, written for that row: one true positive gained, one false positive returned to `undecided`, the count of 21 unchanged |
| **derive every count at the moment you report it** | section 10 |
| **a `file:line` citation into a file you do not own is a citation into a moving target** | every file outside this row's fence is named without a line number, and the only anchored commit is this row's base |
| **do the narrower thing and record it as a finding rather than stopping** | section 10 records four |

### 10. Found and not repaired, and to whom

| the item | whose |
|---|---|
| **`scripts/corpus/gates.mjs` enumerates a directory holding the compatibility module and no row has named it.** It tiers `excluded`, so it is a file to READ and is not expected to go red | **a row holding `scripts/**`,** which this one does not. Named, not touched |
| **The `unfiltered` tier holds 8 of the 18 and claims nothing about any of them.** Ranking them further needs the caller's ASSERTION modelled and not just its filter, which is a larger piece of work than this row's and is the next place the module could go quiet | **`packages/tooling`'s.** Named rather than started |
| **137 directory reads repo-wide still do not fold**, down from 171, and **5 of the 137 are this row's own suite fixtures** rather than reads any runner performs. The residue is dominated by reads whose directory is a loop variable over a list, which needs a fold this module does not have | **`packages/tooling`'s** |
| **Retiring the compatibility module is still 21 files to READ and 6 to WATCH**, and this row changes one NAME on the list of 21 and no count. ADR-419's five to edit and ADR-422's sixth to watch are untouched | **still a row holding `packages/tooling/**`, `apps/api/**` and `apps/worker/test`.** ADR-418 asked for it, ADR-419, ADR-422 and this row each record that no row is it |
| **The container has arrived with no repository sixteen consecutive times.** ADR-416 asked for the image or the dispatch line; ADR-421 and ADR-422 recorded that neither happened | **the dispatcher's.** Unchanged and now one row older |
| **`RI-35` still has no leg for a sentence carried twice.** THIS ROW DECLINES IT for ADR-422 section 11's reason, which this row's section 5 strengthens: the binding is one hop from a file no row holds, and this row has now moved a name on that one-hop list | **nobody's this wave** |
| **The `E2` read this row owes.** Nothing in this diff is money-path on this corpus's operative test. The one file a founder should read is `packages/tooling/checks/dependants.mjs`, specifically `retentionOf` and the paragraph above it, and the case in `packages/tooling/test/dependants.test.ts` that asserts an `excluded` site is STILL REPORTED | **the founder's, after merge.** This entry does not wait on it |
| dated pointers into figures this row supersedes | **NOBODY'S.** [ADR-386](ADR-386.md) rules a pointer in a dated record is NAMED and never repaired. ADR-422's `171` and its enumeration membership are correct records of what that row measured on its day |

### 11. Counts at reporting time

Every figure below was derived after the last edit rather than carried forward from section 2.

| leg | base at `bfd6c400` | this branch | delta |
|---|---|---|---|
| gates, after `generate` | 33 of 33 | **33 of 33** | 0 |
| invariants | 35 of 35 | **35 of 35** | 0 |
| suite files | 341 | **341** | 0 |
| suite passed | 8,246 | **8,261** | **+15** |
| suite skipped | 10 | **10** | 0 |
| suite failed | 0 | **0** | 0 |
| typecheck | exit 0 | exit **0** | 0 |
| lint | exit 0 | exit **0** | 0 |
| `format:check` | clean | **clean** | 0 |

| population derived at reporting time | value |
|---|---|
| files in this diff outside the registries | **2**, both existing, both under `packages/tooling` |
| new files outside the registries | **0** |
| kinds in `KINDS` | **5**, unchanged |
| entries in `BLIND` | **7**, unchanged |
| cases in the suite | **58**, up from 43 |
| mechanisms disabled and watched RED | **11** |

**Eleven seeds watched RED, every one restored and verified byte-identical with `sha256sum -c`, tree verified after each.** One measurement taken in a `git worktree` that was removed afterwards, with the working tree's copy of the subject file confirmed present and unchanged throughout. `pnpm run verify` **NOT run**, `scripts/corpus/falsify.mjs` **NOT run**.

### 12. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** [ADR-414](ADR-414.md), [ADR-415](ADR-415.md), [ADR-417](ADR-417.md), [ADR-418](ADR-418.md), [ADR-419](ADR-419.md) and [ADR-422](ADR-422.md), whose findings this row answers, are each `proposed` and unsigned on this row's base.

- **What a founder read adds and this entry cannot.**

  **THE FIRST DECISION IS WHETHER A TIER IS ALLOWED TO EXIST AT ALL IN AN INSTRUMENT WHOSE ARGUMENT IS THAT SILENCE IS THE ENEMY.** This row's answer is that it is, on the condition that it can never remove a site, and it spent a RED seed making that condition mechanical rather than promised. **A founder should decide whether that condition is sufficient**, because the failure it does not prevent is a reader who treats `excluded` as `absent` and stops looking. If it is not sufficient, the tier should go and this estate reads 18 undifferentiated files every time.

  **THE SECOND IS THE ONE SECTION 5 FORCES ON ROW 425.** Two rows are now in flight over the same target, one holding the file and one holding the instrument that prices it, and the instrument's answer changed by one name while the other row was working. **A founder should decide whether an instrument under repair may be the instrument a live retirement is priced with**, because the alternative is freezing it for the duration and the cost of that is that ADR-422's known false positive would have stayed in row 425's list.

  **THE THIRD IS ADR-422's THIRD, WHICH THIS ROW DID NOT ANSWER AND WOULD LIKE TO SEE ANSWERED.** That entry asked whether the kind register may hold entries nobody has paid for, on `config-glob`'s account. **It is now a question with a second instance:** the `unreadable` tier value in this row has never occurred on this tree either.

- **Approval:** `___________________________` (UNSIGNED. **TWO EXISTING FILES UNDER `packages/tooling` PLUS THE REGISTRIES. NO NEW FILE OUTSIDE THE REGISTRIES, NOT ONE BYTE UNDER `apps/**`, `packages/db/**`, `packages/ledger/**`, `scripts/**` OR THE REST OF `packages/tooling/**`. NO GATE OR LEG ADDED, WIDENED OR WEAKENED, NO CENSUS TOUCHED, NO `RI-nn` MINTED OR RESERVED, NO TEST SKIPPED, DISABLED OR QUARANTINED, NO `.sql` FILE, NO MIGRATION, NO SCHEMA DELTA. THE COMPATIBILITY MODULE IS NOT RETIRED AND NOT EDITED.** **NOT THE MONEY PATH** on this corpus's operative test: nothing under `scripts/db`, and no monetary value read or written. **Merging is not signing.**)

| | |
|---|---|
| **Approved by** | |
| **Date** | |
