## ADR-396: there was no clone to be shallow, the fence holds 46 wrong pointers and not 63, 38 are repaired and 8 are refused in four shapes, and `git blame -w` is not sufficient to decide `ADR-388`'s second gate (2026-09-07, status: proposed)

**Row 396 of the 395s wave. Branch `claude/sixty-three-pointers-across-thirty-five-files`, off `ebc21238`, session 591.**

**Status `proposed`. The approval line in section 13 is UNSIGNED and merging is not signing. `E2` read owed.**

**FOUR THINGS ARE HERE AND THE THIRD IS THE ONE THAT MATTERS.**

1. **THE DISPATCH'S ENVIRONMENT WAS NOT WRONG ABOUT THE CLONE, IT WAS WRONG THAT THERE WAS ONE.** Section 1.
2. **THE 63 DOES NOT REPRODUCE AND NEITHER DOES ITS SHAPE.** The fence holds **46** wrong pointers into [`gates.mjs`](../../scripts/corpus/gates.mjs), **18** sit outside it, and two of the 46 are in a form no check in this repository reads. Sections 3 and 4.
3. **`git blame -w` DECIDES `G2` WRONGLY, AND IT DID SO HERE ON A REAL POINTER.** [ADR-393](ADR-393.md) section 11 item 3 named this hazard and could not point to a case. This row has the case, and reading `G2` at the blamed commit would have condemned a class-one pointer as manufactured evidence. Section 7.
4. **[`sessions/README.md`](../sessions/README.md) IS NOT HAND-REPAIRABLE AND THIS ROW WAS TOLD TO HAND-REPAIR IT.** Every wrong pointer in it is a verbatim copy of a session log's `<!--index:` line inside a generated span. Section 8.

---

### 1. There was no clone, so the rule about shallow clones had nothing to run against

**THE DISPATCH'S FIRST RULE WAS THAT `git rev-parse --is-shallow-repository` RETURNS `true` HERE, "CONFIRMED BY THE DISPATCHER", AND THAT THE ROW'S WORKING DIRECTORY WAS THE REPOSITORY ROOT.** The command was run first, as [ADR-389](ADR-389.md) section 7 requires, and it did not return `true`. It returned a fatal error, because the working directory was empty.

| Step, in order | Result |
|---|---|
| `git rev-parse --is-shallow-repository` in the stated working directory | **fatal: not a git repository** |
| Contents of that directory | **empty** |
| Any checkout of this repository anywhere on the container | **none**, searched to depth 6 from `/` |
| Clone made by this session, at full depth | **`is-shallow` = `false`**, **4,301** commits reachable |
| `ebc21238`, the dispatched base, present | **yes** |

**THIS IS THE SECOND CONSECUTIVE WAVE IN WHICH THE SAME SENTENCE WAS FALSE IN THE SAME WAY.** [ADR-393](ADR-393.md) section 1 recorded that its dispatch's *"the repo is already cloned and your cwd is the repo root"* described nothing that existed. **A dispatcher that reports a property of a clone it did not check is reporting a property of a clone that is not there**, and the two failures are one failure: the environment claim is written from a template rather than measured.

**THE CONSEQUENCE FOR THIS ROW IS THE OPPOSITE OF THE ONE THE RULE FEARED.** The rule exists because a shallow clone answers a history question wrongly rather than refusing. This clone was made deep on purpose, since every `G2` verdict below is a history question, and no `--depth` bound was ever imposed on it.

---

### 2. The base, reproduced on `ebc21238` before the first edit

**A DISPATCH PROMPT IS NOT A SOURCE, AND NEITHER IS THE PREVIOUS ROW'S TABLE.** The whole suite was run on the base commit before this row wrote a character.

| Runner | On `ebc21238`, before the first edit |
|---|---|
| `node scripts/corpus/gates.mjs check` | **33 of 33** |
| `node packages/tooling/checks/repo-invariants.mjs` | **35 of 35** |
| `pnpm vitest run` | **336 files, 8,109 passed, 10 skipped, 0 failed** |
| `pnpm run typecheck`, `pnpm run lint`, `pnpm run format:check` | clean, clean, clean |

**THE GATE COUNT IS DERIVED AND IT IS 33.** The suite figure is **8,109** and not the **8,107** [ADR-393](ADR-393.md) and [ADR-394](ADR-394.md) both reproduce, because this base is two merges later than theirs and not because anything moved under a test.

---

### 3. The population, derived through the checks' own reader, and the reader had to be moved out of the repository to get it

**[ADR-388](ADR-388.md)'s METHOD IS TO IMPORT `citationsIn`, `documentScope`, `citationReader` AND `nearestName` FROM AN EXPORT-ADDED COPY OF [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) PLACED IN THAT CHECKER'S OWN DIRECTORY, SO THAT `REPO_ROOT` RESOLVES.** That directory is `packages/tooling/**`, which is another row's this wave, so the copy could not go there. **The whole `checks` directory was copied OUT of the repository instead**, under a `package.json` the checker walks up to find, with `REPO_ROOT` pinned to the real tree. **Two properties of that file made the naive copy fail and both are worth recording**: it refuses to load at all if it sits under no `package.json`, and one of its siblings imports it back, so copying a single file produces two module instances and a temporal-dead-zone crash rather than a reader. **Copying the directory whole is what makes the cycle resolve to one instance.** Nothing inside the repository was written to obtain the reader.

| Derived on `ebc21238`, inheritance OFF, over every tracked `.md` | |
|---|---|
| Citations resolving to [`gates.mjs`](../../scripts/corpus/gates.mjs) | **109** |
| Distinct citing files | **59** |
| Distinct coordinates cited | **56** |
| Cited line no longer holds what it held at the commit that wrote the citing line | **62**, over **37** files |
| Still exact | **47** |
| Carrying no path inside the token, resolved only by the link target beside it | **11** |

**AND THREE POINTERS SIT OUTSIDE THAT GRAMMAR ENTIRELY.** One is in a source file and is exact. **Two are written `[gates.mjs:368](../scripts/corpus/gates.mjs)`, the backtick-free link form [ADR-388](ADR-388.md) section 1 measured at 1,403 corpus-wide and which `citationsIn` cannot see, and BOTH ARE WRONG.** So the corpus-wide figure this row stands behind is **64 wrong pointers over 38 files**, and the two invisible ones are both inside this row's fence, in [`STATE.md`](../STATE.md) and in [session 83](../sessions/2026-08-20-session-83.md), a file no entry before this one names.

**THE PREVIOUS ROW'S 84 WAS RE-RUN AT ITS OWN BASE RATHER THAN ARGUED WITH.** A worktree was cut at `c5648f00` and the same reader run against it: **81** in the `citationsIn` grammar plus the same **2** in the link form is **83**, against [ADR-393](ADR-393.md)'s **84**, and **109** citations against its **115**. **The 83 of 84 is close enough to call the two readers the same reader and the residue is not explained here.** What is worth more than the residue: [`gates.mjs`](../../scripts/corpus/gates.mjs) and [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) are **byte-identical** between `c5648f00` and `ebc21238`, so every coordinate that entry derived is still the right coordinate on this base, and this row's independent derivation agrees with all fourteen of its repairs that overlap.

---

### 4. THE 63 IS REFUTED, AND NOT BY A LITTLE

**THE DISPATCH SAID 63 WRONG POINTERS LIE OUTSIDE [ADR-393](ADR-393.md)'s FENCE OVER 35 FILES.** That figure is that entry's count at its own base, taken **before its own 24 repairs landed**. On `ebc21238`, with those repairs merged, it does not reproduce and neither does the split.

| On `ebc21238` | |
|---|---|
| Wrong pointers into [`gates.mjs`](../../scripts/corpus/gates.mjs), corpus-wide, all three forms | **64** over **38** files |
| **Inside this row's fence** | **46** over **31** files |
| Outside it | **18** over **7** files |

**SO THE DISPATCH UNDERSTATED THIS ROW'S OWN WORK AND OVERSTATED THE REMAINDER.** The 18 outside sit in [ADR-098](ADR-098.md), [ADR-119](ADR-119.md), [ADR-337](ADR-337.md), [`ALLOCATION.md`](ALLOCATION.md), the dated audit under `docs/decisions/gates/`, two plans under `docs/plans/` and [`DELTA_MANIFEST.md`](../../packages/db/DELTA_MANIFEST.md).

**THE NINE ADRs DO NOT CARRY ONE EACH.** They carry **16** between them: two in [ADR-064](ADR-064.md), one in [ADR-188](ADR-188.md), two in [ADR-272](ADR-272.md), one each in [ADR-276](ADR-276.md), [ADR-278](ADR-278.md) and [ADR-279](ADR-279.md), three in [ADR-287](ADR-287.md), three in [ADR-290](ADR-290.md) and two in [ADR-297](ADR-297.md).

**THE EIGHTEEN SESSION LOGS ARE NINETEEN.** The eighteen reproduce exactly in the `citationsIn` grammar. [session 83](../sessions/2026-08-20-session-83.md) is the nineteenth and it is invisible to every check in this repository, which is why no entry has ever named it.

---

### 5. The classification, run per pointer, and the two gates derived rather than inherited

**[ADR-388](ADR-388.md) SECTION 2 WAS APPLIED AND NOT ASSUMED.** `G1` is read off the citing sentence; `G2` is read out of [`gates.mjs`](../../scripts/corpus/gates.mjs) at the commit that wrote the citing line, using the method section 7 argues for rather than the one the ruling names.

| Verdict over the fence's 46 | Count |
|---|---|
| `G1` and `G2` hold, the subject is alive and uniquely locatable today: **REPAIRED** | **38** |
| `G1` and `G2` hold, the subject is **GONE or SUPERSEDED**: refused | **7** |
| `G1` holds, `G2` FAILS, born wrong: refused | **1** |

**36 OF THE 38 WERE VERIFIED BYTE-IDENTICAL AT THEIR NEW COORDINATE AFTER THE EDIT LANDED**, by reading the span out of the file on this base and comparing it to the span the cited line held at the commit that wrote the citing sentence. **The other two are named in section 6 and neither is inferred from an offset.**

---

### 6. The repairs, and the two that are not a byte match

**41 COORDINATE TOKENS ACROSS 24 FILES, CARRYING 42 POINTERS.** The retired coordinate is kept in backticks beside the new one and is **never re-cited with its path**, which is [ADR-388](ADR-388.md) section 4's rule for a document and [ADR-367](ADR-367.md)'s rule about naming a retired figure rather than reproducing it. **That rule was re-derived at the reader rather than inherited: run over the repaired documents with inheritance OFF, not one retired coordinate reads as a citation.**

| Record | Subject, which is what makes the repair permitted | Retired | Repaired to |
|---|---|---|---|
| [`STATE.md`](../STATE.md), two sites | the drift-recogniser's header sentence | `:8417` | `:8899` |
| | `export const GATES = [`, and the guard beside it | `:9118`, `:9324-9327` | `:9304`, `:9510-9513` |
| [ADR-188](ADR-188.md) | `CI-06k`'s header, the line naming the matrix as its input | `:1958` | `:2290` |
| [ADR-272](ADR-272.md) | the two probe filenames in `CI-06h`'s needle list | `:1579`, `:1617` | `:1718`, `:1756` |
| [ADR-276](ADR-276.md) | the closed unit vocabulary's declaration | `:2725` | `:2961` |
| [ADR-278](ADR-278.md) | `dateColumns()` | `:2777` | `:3013` |
| [ADR-279](ADR-279.md) | `stripJsComments` | `:3318` | `:3554` |
| [ADR-287](ADR-287.md) | `CI-06n`, and the `adr_registry` and `session_entries` queries | `:2562`, `:1068`, `:1094` | `:2798`, `:1186`, `:1212` |
| [ADR-290](ADR-290.md) | the same three | `:2562`, `:1068`, `:1094` | `:2798`, `:1186`, `:1212` |
| [ADR-297](ADR-297.md) | the same two queries | `:1068`, `:1094` | `:1186`, `:1212` |
| [session 83](../sessions/2026-08-20-session-83.md) | `strategyGateLetters()` | `:2890` | `:3784` |
| [session 95](../sessions/2026-08-20-session-95.md) | the manifest gate's own reason string, and its row loop | `:1537`, `:1542` | `:2246`, `:2251` |
| [session 157](../sessions/2026-08-24-session-157.md) | `CI06O_SCOPE`, and `MONEY_PATH_WORDS` | `:3015`, `:3025` | `:3537`, `:3547` |
| [session 160](../sessions/2026-08-24-session-160.md) | the `session_entries` query | `:1067` | `:1212` |
| [session 170](../sessions/2026-08-24-session-170.md) | the `generate` subcommand's dispatch line | `:7131` | `:9476` |
| [session 175](../sessions/2026-08-24-session-175.md) | `ACCEPTING_VERDICTS` | `:3664` | `:4314` |
| [session 177](../sessions/2026-08-24-session-177.md) | `VG_INVENTORY` | `:6833` | `:7981` |
| [session 221](../sessions/2026-08-26-session-221.md) | the negative-authz matrix read | `:1995` | `:2389` |
| [session 319](../sessions/2026-08-28-session-319.md) | the span writer's interpolation | `:1219` | `:1337` |
| [session 337](../sessions/2026-08-28-session-337.md) | the drift-recogniser's header sentence | `:8417` | `:8899` |
| [session 463](../sessions/2026-08-30-session-463.md) | the two probe filenames | `:1579`, `:1617` | `:1718`, `:1756` |
| [session 478](../sessions/2026-08-30-session-478.md) | the two generator queries, in one comma token | `:1068,1094` | `:1186,1212` |
| [session 521](../sessions/2026-09-05-session-521.md), two sites | `export const GATES = [`, and the guard | `:9118`, `:9324-9327` | `:9304`, `:9510-9513` |
| [session 525](../sessions/2026-09-05-session-525.md) | the `manifest_changes` span, and the retired-constraints register entry | `:982`, `:8412` | `:1100`, `:8579` |
| [session 528](../sessions/2026-09-05-session-528.md) | the same two, and the `CI-06` register entry for the superseded `OI-nn` table | `:982`, `:8412`, `:6780` | `:1100`, `:8579`, `:6947` |

**FOUR OF THOSE COORDINATES ARE BELOW EVERY READER IN THIS REPOSITORY AND WERE REPAIRED ANYWAY.** A bare `` `:9324-9327` `` with no path and no link of its own is not a citation to `RI-15`, to `RI-16` or to this row's reader. **Each of the four sits in the SAME SENTENCE as a pointer that was repaired**, and leaving it would have produced a sentence in which one coordinate is current and its neighbour is not, with nothing to tell a reader which. **A sentence is the unit a reader trusts, so it is the unit that was repaired.**

**ONE REPAIR CHANGES WHAT A READER FINDS AND IT IS LEFT THAT WAY DELIBERATELY.** [session 157](../sessions/2026-08-24-session-157.md) reads *"`CI06O_SCOPE`, one entry"*. The declaration is still there and it now holds two. **The coordinate is repaired and the figure is NOT**, on [ADR-393](ADR-393.md) section 7's disposition for the `sites: 7` case and [ADR-367](ADR-367.md)'s rule underneath it. A reader following the repaired pointer sees the disagreement, which is the point of repairing it.

**THE OTHER NON-MATCH IS [ADR-188](ADR-188.md)'s AND IT IS THE WEAKEST `G1` IN THE SET.** The record names *"API_CONTRACT section 12's negative-authz matrix"* and the cited line is a line of the gate's header comment describing that input rather than a declaration a reader would grep for. The line's text is unchanged and unique on this base, so the repair moves the coordinate to the same characters; but a founder who reads `G1` strictly would refuse it, and refusing it costs one line.

---

### 7. THE FINDING: `git blame -w` IS NOT SUFFICIENT TO DECIDE `G2`, AND IT FAILED HERE

**[ADR-388](ADR-388.md) SPECIFIES `G2` AS "AT THE COMMIT THAT LAST WROTE THE CITING LINE" AND EVERY ROW SINCE HAS READ THAT AS `git blame -w`.** [ADR-393](ADR-393.md) section 11 item 3 flagged the risk in the abstract, that a reflowed line blames later than its author, and said the corpus has had at least one whitespace sweep. **[session 589](../sessions/2026-09-06-session-589.md) then wrote it down as a landmine in its own words the night before this row ran**, that the `G2` reads *"cannot distinguish an unrelated later rewrite"*. **Two rows named it and neither had an instance. `-w` ignores whitespace. It does not ignore anything else, and a markdown emphasis marker is not whitespace.**

| [`STATE.md`](../STATE.md) line 6614, citing the drift-recogniser's header | |
|---|---|
| What `git blame -w` names | `075ee870`, 2026-09-05 |
| What that commit did to the line | changed `*"` to `_"` and `"*` to `"_`. **Nothing else** |
| Where the cited sentence stood at that commit | **8721**, not 8417. `G2` FAILS, class two, repair FORBIDDEN |
| The commit that actually introduced the coordinate | **`b8ab1eaa`**, 2026-08-28, which wrote the sentence and [session 337](../sessions/2026-08-28-session-337.md)'s copy of it in the same diff |
| Where the cited sentence stood there | **8417, exactly as cited.** `G2` HOLDS, class one, repair permitted |

**READ AT THE BLAMED COMMIT, THIS POINTER IS A RECORD OF A CHECK THAT NEVER HAPPENED AND MUST STAY WRONG. READ AT THE COMMIT THAT WROTE IT, IT IS THE CLEANEST DRIFT IN THIS ROW'S SET.** The two readings differ by 304 lines and by a verdict, and the only thing separating them is a cosmetic rewrite eight days later.

**SO `G2` IS DERIVED HERE AT THE EARLIEST COMMIT THAT INTRODUCED THE CITED COORDINATE INTO THE CITING FILE**, by `git log -S` over the coordinate token, taking the oldest result. **Run against all 46, it disagrees with `git blame -w` on FIVE and changes a verdict on ONE.** The other four move the commit without moving the answer.

**THIS IS [ADR-388](ADR-388.md) SECTION 5's OWN LESSON ARRIVING AT ITS OWN GATE.** That entry ruled that [ADR-384](ADR-384.md) had measured a pointer at the wrong tree, and its rule was: measure at the commit that wrote the sentence, never at the tree you happen to be standing on. **`git blame -w` is a third tree, and it is the one the ruling's own remedy hands you.** The ruling is not wrong. Its instrument is, on any line a later row has restyled.

**THE COST OF THE OLD INSTRUMENT IS NOT ESTIMATED HERE AND IT SHOULD BE.** One in 46 flipped in this fence. **Nothing has re-run [ADR-388](ADR-388.md)'s corpus-wide census of 289 named pointers under the corrected instrument**, and its headline split of 76 repairable against 42 born wrong is the figure the whole ruling rests on.

---

### 8. [`sessions/README.md`](../sessions/README.md) IS NOT HAND-REPAIRABLE, AND THE GATE SAID SO OUT LOUD

**THE DISPATCH GAVE THIS ROW "POINTER REPAIRS INSIDE [`STATE.md`](../STATE.md) AND [`sessions/README.md`](../sessions/README.md)" AS A FENCE EXTENSION ITS SIBLINGS WERE DENIED.** Half of that is a real extension and half of it is not a thing that can be done.

**EVERY WRONG POINTER IN THAT README SITS INSIDE THE `session_entries` GENERATED SPAN**, between that span's opener and its closer, neither spelled here on `CI-06t`'s stated repair, which `documentScope` masks. **They are therefore invisible to `RI-16` and to this row's reader alike**, which is why the file appears in no citation census and why its four wrong pointers are not among the 46. **Each is a verbatim copy of a session log's own `<!--index:` line.**

**THE MECHANISM WAS WATCHED RATHER THAN ASSUMED.** Repairing the `<!--index:` lines of [session 337](../sessions/2026-08-28-session-337.md), [session 463](../sessions/2026-08-30-session-463.md), [session 478](../sessions/2026-08-30-session-478.md) and [session 521](../sessions/2026-09-05-session-521.md) turned `CI-06g` **RED at 32 of 33**, in its own words *"every generated span still matches its query"*. `node scripts/corpus/gates.mjs generate` then rewrote exactly four lines of that README and the gate returned to 33 of 33 on its own. **No gate was weakened, no register grown and no exemption written.**

**SO A HAND EDIT THERE WOULD HAVE BEEN OVERWRITTEN BY THE NEXT `generate` AND WOULD HAVE FAILED `CI-06g` UNTIL IT WAS.** The four pointers are repaired, and not one of them was repaired in the file the dispatch named.

---

### 9. The eight refusals, in four shapes, and only one of them is [ADR-388](ADR-388.md)'s class two

**EXPECTING REFUSALS AND REPORTING THEM AS RESULTS IS THE INSTRUCTION AND THIS IS THE RESULT.** Eight pointers in this fence are wrong today, will stay wrong, and each is unrepairable for a different reason.

| Pointers | Shape | Why the coordinate does not move |
|---|---|---|
| [ADR-064](ADR-064.md) twice, [session 85](../sessions/2026-08-20-session-85.md) once, all at `:218` | **THE SUBJECT WAS REPAIRED, NOT DELETED** | All three quote the sessions registry predicate reading `session-\d{2}\.md` and all three report it as a live defect. The predicate still exists and now reads `\d{2,}`. **Moving the coordinate would make three dated records assert, at a live line, a defect the tree fixed because they reported it.** The corpus holds the bug report and its fix, and the rot is the only thing keeping them apart |
| [session 172](../sessions/2026-08-24-session-172.md) at `:6519` | **THE SUBJECT WAS DELETED** | The `build`-key probe is gone and `INVENTORY_PROBES` is declared with an empty body. This is [ADR-393](ADR-393.md) section 5's fourth outcome, and it is the SAME subject that entry refused in [ADR-098](ADR-098.md), carried by a second document nobody had connected to it |
| [session 220](../sessions/2026-08-26-session-220.md) twice at `:6932` | **THE SUBJECT WAS REPLACED** | The register entry both sites name was replaced, and the entry that replaced it says so in a comment at the coordinate's new neighbourhood. **The pointer and the notice that its subject is gone are now in the same file**, which is [ADR-393](ADR-393.md) section 5's observation about [ADR-182](ADR-182.md) sharpened: there, two documents; here, one |
| [`STATE.md`](../STATE.md) at `:368` | **THE NAME SURVIVED AND THE PROPERTY MOVED** | The record names *"one `allocated()` parser"*. `allocated` still exists and is now a one-line view over `allocatedClaims`, which is the parser and which the record never names. **One candidate coordinate makes the record visibly false and the other answers a question it did not ask**, so there is nowhere to move it to |
| [session 165](../sessions/2026-08-24-session-165.md) at `:2069` | **CLASS TWO, BORN WRONG** | Four lines short on the day it was written: the cited line held a closing brace and the generated-expression read it names was above it. Section 10 |

**AND ONE MORE REFUSAL IS OWED TO THE DISPATCH, WHICH NAMED IT AND WAS RIGHT.** [session 521](../sessions/2026-09-05-session-521.md)'s *"carries the clause verbatim"* claim about [`data-model-columns.mjs`](../../scripts/corpus/data-model-columns.mjs) is class two, **re-derived here rather than inherited**: at the commit that wrote the sentence the cited line read *"IT CAN NOW BE IMPORTED, AND THE BLOCKER THIS PARAGRAPH NAMED IS SPENT"*, the clause it quotes stood 11 lines below as a RETIRED sentence, and that commit never touched the file while its parent reads the same. **It is class two for the same reason it is class two in [ADR-330](ADR-330.md) and it stays wrong.** The retired quotation has since moved further, so it is a class-two pointer growing more wrong, which is the only kind of rot no repair may touch.

---

### 10. ONE SENTENCE, TWO POINTERS, ONE CLASS ONE AND ONE CLASS TWO, AND BOTH ARE LEFT

**[session 165](../sessions/2026-08-24-session-165.md) LINE 62 CITES TWO COORDINATES FOR TWO ADJACENT PARTS OF ONE GATE.** At the commit that wrote it, the first held a closing brace and the second held `POST_IDENTITY_EXEMPT`'s declaration **exactly as named**. **The author was four lines short on one and exact on the other, in one sentence, on one day.**

**THAT EXTENDS [ADR-393](ADR-393.md) SECTION 6's RULE.** That entry found one coordinate class one in one document and class two in another, and concluded `G2` is per pointer and not per subject. **Here both pointers are in the same sentence of the same document, so `G2` is per pointer and not per SENTENCE either.**

**AND THE REPAIRABLE ONE IS LEFT, WHICH NEEDS AN ARGUMENT RATHER THAN A PREFERENCE.** [ADR-388](ADR-388.md)'s permission says the record's claim is preserved and *"nothing the record asserted becomes untrue"*. The two coordinates are four lines apart and the record reads them as neighbouring parts of one gate. **Repairing only the honest one would put 332 lines between them and make the record assert a distance it never asserted.** So the permission does not reach it. **This is the first case in this corpus where repairing a class-one pointer is refused by the repair rule itself rather than by a fence**, and a founder who reads it the other way gets both pointers repaired for the price of one line and a false adjacency.

---

### 11. What this row's own diff moved, derived before it was written

**EVERY ONE OF THE 24 REPAIRED FILES IS LINE-COUNT NEUTRAL.** 26 insertions against 26 deletions, no line added and none removed, so **not one citation into any of them moved** and the second-order rot from the repairs is zero. That was designed rather than discovered: a coordinate is replaced in place and the retired one is appended to the same line.

**THE REGISTRY ROWS ARE THE PART THAT MOVES LINES, AND THEY MOVE FIVE CITATIONS.** Three rows are added to [`ALLOCATION.md`](ALLOCATION.md)'s ADR table and everything below shifts by three. Five pointers cite lines below that point and **all five are LEFT, none of them by omission**.

| Cited line | Citing records | Verdict |
|---|---|---|
| the `RI-37` register row | [ADR-337](ADR-337.md) | **LEFT. Already ruled BORN WRONG and class two** by [ADR-388](ADR-388.md) and again by [ADR-389](ADR-389.md); this diff changes how wrong it is and not whether it may be repaired |
| the same line | [ADR-386](ADR-386.md), [ADR-388](ADR-388.md), [ADR-389](ADR-389.md) | **LEFT.** All three cite the coordinate as the SUBJECT of a finding about that coordinate. Repairing them would falsify the findings |
| the counts row at `:412` | [ADR-386](ADR-386.md) | **LEFT**, same shape |

**THAT RESOLVES A FENCE COLLISION RATHER THAN CREATING ONE.** One of the five is in [ADR-388](ADR-388.md), which is row 397's and which this row may not write. **Because the correct disposition is to leave it, this row needs no edit there**, and the collision the dispatch's fence would otherwise have forced does not arise. It is recorded so that row 397 knows the shift happened and knows it was classified.

**NOTHING ELSE IN THE TREE MOVED, AND THAT WAS SWEPT FOR RATHER THAN ASSUMED.** The two generated registries take their new rows at the END of their spans and [`STATE.md`](../STATE.md) is appended to at the END, so the highest cited line in each of those three files is above every insertion: **378 against 409, 781 against 816, and 12,814 against 13,617**. [`STATE.md`](../STATE.md) also has one line rewritten in place at line 32, where `generate` moves `adr_count` from 378 to 379, and a one-for-one replacement moves nothing below it. **Re-run over every tracked document after the diff, the citations sitting below any insertion point in this row's four changed registries number exactly five, and they are the five in the table above.** All of it was derived before the rows were written and again after.

---

### 12. Verification, every figure derived at the moment of reporting

| Runner | Base on `ebc21238` | After this diff |
|---|---|---|
| `node scripts/corpus/gates.mjs check` | 33 of 33 | **33 of 33** |
| `node packages/tooling/checks/repo-invariants.mjs` | 35 of 35 | **35 of 35** |
| `pnpm vitest run` | 336 / 8,109 / 10 / 0 | **336 / 8,109 / 10 / 0**, delta ZERO on every axis |
| `pnpm run typecheck` / `lint` / `format:check` | clean | **clean, clean, clean** |
| `node scripts/corpus/gates.mjs generate`, run twice | | **six rows moved in three files**: four session rows after the pointer repairs, then one row in each registry README and `adr_count` from 378 to 379, all in place |

**A GATE WENT RED ONCE AND IT IS SECTION 8's, WHICH IS EVIDENCE RATHER THAN AN INCIDENT.** `CI-06g` reported 32 of 33 in the window between repairing four `<!--index:` lines and regenerating the span they feed. **That red is the proof that [`sessions/README.md`](../sessions/README.md) is generated and not authored**, and it is the reason section 8 is a finding and not an opinion.

**A SECOND GATE WENT RED AND IT WAS THIS ENTRY'S OWN PROSE.** `CI-06t` reported **32 of 33** against section 8 of this file, which had spelled a generated span's closer while explaining that the span exists. **The gate is right and the sentence was wrong**: a document quoting a closer outside a code fence is indistinguishable from a span whose opener was deleted. The sentence now describes the closer instead of spelling it, which is [ADR-287](ADR-287.md)'s stated repair for the opener, and the gate returned to 33 of 33 on its own. **No gate was weakened, no register grown and no exemption written.**

**THE STANDING BAR IS UNTOUCHED.** No invariant minted, no `RI-` number reserved, no `CI-06` letter spent, no gate added, widened or weakened, no test skipped, disabled or quarantined, no register grown, no migration taken or reserved, no port wired, no job scheduled. **`scripts/corpus/**` and `packages/tooling/**` were READ and never opened for writing**, so the file this row repairs pointers INTO is byte-identical to its base, and the reader was obtained by copying that directory OUT of the tree rather than by adding exports inside it. **`pnpm run verify` and `scripts/corpus/falsify.mjs` were never run.** Nothing under `docs/ops/runbooks/**` was read or written, and no line number in that directory or in [`gates.mjs`](../../scripts/corpus/gates.mjs) is cited anywhere in this entry: the coordinates in section 6 are bare numbers anchored to `ebc21238` in the sentence above them.

**ONE DEVIATION FROM THE DISPATCH IS RECORDED RATHER THAN TAKEN QUIETLY.** It named this row's session log `2026-09-06-session-591.md`. The base commit is dated 2026-09-07 and so is every commit here, so the log carries **2026-09-07**. Nothing in the corpus referenced the other name.

---

### 13. Where this entry is most likely wrong

1. **THE `G2` INSTRUMENT IN SECTION 7 IS A HEURISTIC AND IT HAS A KNOWN HOLE.** `git log -S` over the coordinate token finds the commit that first introduced that string into that file. **Where the same coordinate appears twice in one file, the first introduction is attributed to both**, and where a citation was written, deleted and rewritten, the earliest is not the author of the surviving line. Neither case is present in this fence, checked; **neither is ruled out corpus-wide, and the corrected census section 7 asks for would have to handle both.**
2. **THE 83 AGAINST [ADR-393](ADR-393.md)'s 84 IS UNEXPLAINED AND ONE POINTER IS NOT NOTHING.** Two readers that agree on 83 of 84 and on every overlapping repair are probably the same reader, but *probably* is doing work in that sentence. **The residue was not chased, because the operative population is this row's fence and that was derived twice.**
3. **SECTION 9's FIRST SHAPE MAY BE ONE THE CORPUS ALREADY HAS A WORD FOR.** *"The subject was repaired rather than deleted"* is arguably [ADR-393](ADR-393.md)'s fourth outcome with a different cause, and arguably a fifth. **The reason it is written as its own row is that the remedy differs**: a deleted subject has nowhere to point, while this one has a live line that would make the record read as a current complaint. A founder may collapse the two.
4. **THE FOUR CHECK-INVISIBLE COORDINATES IN SECTION 6 WERE REPAIRED ON A RULE THIS ROW MADE UP.** No ruling says a sentence is the unit of repair. **It is argued from the same premise section 10 uses to refuse a repair, and the two conclusions point in opposite directions**: there, coherence forbids a one-sided repair; here, coherence compels a two-sided one. Both follow from *"nothing the record asserted becomes untrue"*, and a founder who thinks that premise is being stretched should look at section 10 first, because that is the one that left a wrong pointer standing.
5. **[ADR-188](ADR-188.md)'s REPAIR RESTS ON THE WEAKEST `G1` HERE**, section 6, and it is the one repair in this set that a strict reading of the subject gate would refuse.

---

### 14. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** Inherited as well as owed: [ADR-357](ADR-357.md) and [ADR-360](ADR-360.md) through [ADR-394](ADR-394.md) are `proposed` and unsigned, two rows run beside this one holding `395` and `397`, and this row settles none of them.

  **What a founder read adds and this entry cannot.**

  **THE FIRST THING TO DECIDE IS WHETHER [ADR-388](ADR-388.md)'s CENSUS SHOULD BE RE-RUN.** Section 7 shows the instrument every row has used to apply that ruling is wrong on any line a later row restyled, and it flipped one verdict in 46 here. **The ruling's headline is 76 repairable against 42 born wrong over 289 named pointers, and that split is what makes option (a) a consequence rather than a choice.** Nobody knows what it is under the corrected instrument. **This row can say the number is unverified; it cannot say it is wrong, and it did not re-run the census because that is a row and not a paragraph.**

  **THE SECOND IS THAT THIS CORPUS NOW HAS FIVE SHAPES OF UNREPAIRABLE POINTER AND [ADR-388](ADR-388.md) NAMES THREE.** Section 9's table is four of them and section 10 adds a fifth, in which the ruling's own permission refuses a repair it grants. **Three consecutive rows have each found a shape the ruling has no class for**, which is either a ruling that needs a section or a category that is not carving anything, and a founder is better placed than a row to say which.

  **THE THIRD IS THE ONE THIS ROW WOULD PUT LAST AND MEANS MOST.** [ADR-393](ADR-393.md) asked whether repairing pointers at this scale is worth doing at all, and this row is the evidence for the sceptical answer: **it repaired 38 pointers in a fence of 46, and the corpus wrote 46.9 new pointers per merged pull request at a 42.1 percent rot rate while it did so.** Two waves have now spent a row each on one file's pointers. **The honest reading is that the repair work is losing, and that the thing worth buying is not more repairs but the form [ADR-388](ADR-388.md) adopted for NEW pointers and refused as a migration**, which is a name beside every coordinate. **This row could not have done any of its work without those names, and the eight it refused are eight cases where the name was there and the tree had still moved out from under it.**

- **Approval:** `___________________________` (UNSIGNED. **TOOLING AND CONTROL OVER THE CORPUS'S OWN POINTERS, `E2` READ OWED.** **Merging is not signing.**)

| | |
|---|---|
| **Approved by** | |
| **Date** | |
