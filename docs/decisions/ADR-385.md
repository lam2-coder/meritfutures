## ADR-385: the two counts ADR-382 left unreconciled are two different sets, both are short, and the class they name is not checkable (2026-09-06, status: proposed)

**[ADR-382](ADR-382.md) named a defect class it could not finish, gave it two numbers, and said in writing that it had not reconciled them.** They are reconciled here. **THEY ARE NOT THE SAME SET, THE DISPATCH'S PREMISE IS CORRECT, AND BOTH NUMBERS ARE SHORT.** The class recurs outside `apps/worker`, the two sites `ADR-382` left are repaired at a citation cost of ZERO, and the question of whether the class is checkable is answered NO with the measurement that makes it no.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `385`, branch `claude/the-claim-that-degraded-into-an-absolute`, session **580**. ADR number and session number assigned centrally and not derived.
- **MONEY-ADJACENT. ADR-003 STRICT. `E2` READ OWED.** The worker runs ledger and payout work and every file this row edits sits in that deployable or in the pass that gates its consoles. Ships `proposed` with an **UNSIGNED** approval line. **Merging is not signing.**
- **THE STANDING BAR IS NOT TOUCHED.** No port was wired, no job scheduled, no sink passed, no default parameter added to `expirySweepIo`, and no cron expression, interval, batch size, concurrency, retry count, backoff or timeout was invented. [ADR-382](ADR-382.md)'s bar STANDS with its two exits unchanged. **This row repairs prose ABOUT the bar and says so, because those are two different acts.**
- **NO MIGRATION WAS TAKEN OR RESERVED.** `git diff --stat origin/main -- packages/db/migrations scripts/db` is **EMPTY**, measured at reporting time.
- **THIS DIFF IS COMMENTS ONLY.** Not one line of executable source changed, which is why section 10's suite delta is **ZERO** and why section 8 binds nothing.
- **EVERY FIGURE BELOW IS DERIVED HERE.** The dispatch said its own numbers were second-hand and told this row to derive its own. **Four of the figures carried forward from [ADR-382](ADR-382.md) are wrong, and section 3 and section 5 say which.**

---

### 1. What was measured before the first edit

**THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL.** `/home/user` was empty, with no clone and no `node_modules`, and the dispatch's own instruction not to prefix commands with a `cd` into the tree describes a tree that was not there. This is the **twelfth consecutive session** to record it. The repository was attached to this session's authorized set and cloned by hand, and `HEAD` was confirmed to be the `0fc59a22` the row names rather than assumed.

**Base, on a clean tree, before anything was touched: 336 files / 8,085 passed / 10 skipped / 0 failed.** Gates **33 of 33**, invariants **35 of 35**, `typecheck`, `lint` and `format:check` all clean. **THESE ARE NOT [ADR-382](ADR-382.md)'s FIGURES**, which are `336 / 8,077 / 10 / 0` measured on `e18ea41f`; rows `380`, `381` and `382` have merged into the wave branch since.

**AND THE CITATION CENSUS WAS RUN BEFORE THE FIRST EDIT, BECAUSE IT DECIDED THE SHAPE OF EVERY REPAIR.** It was derived by driving `repo-invariants.mjs`'s own `citationsIn` and `citationReader` over the whole tree from a scratch copy, so the resolution is the CHECKER'S rather than a grep's. The copy was deleted and `git status` confirmed clean before any gate was run.

```
apps/worker/src/index.ts                 -> 36 citations BELOW :596, 10 distinct lines
apps/worker/src/sweeps/expiry-adapter.ts -> 12 citations, 10 of them at :193
apps/worker/src/schedule.ts              -> 53 citations, 15 distinct lines
apps/worker/src/recon/sweep.ts           -> 10 citations, 8 of them at :614
apps/worker/test/breaker.test.ts         ->  9 citations at :974, :976 and :990
playwright.config.ts                     ->  8 citations, deepest line :90
apps/portal/test/kyc-source.test.ts      ->  3 citations, all at :42
```

**TWO OF [ADR-382](ADR-382.md)'s NUMBERS DO NOT REPRODUCE.** It records *30 citation sites across 7 distinct lines* below `index.ts:596` and *8 citations* at `expiry-adapter.ts:193`. Derived here: **36 across 10**, and **10**. The direction is the same and the price it quoted was real; the figures were carried rather than re-derived, which is the defect one document over from the one this row is about.

---

### 2. THE RULING

| # | Item | At primary source | Action |
|---|---|---|---|
| 1 | [ADR-382](ADR-382.md)'s *three of five* and its *six* | **TWO DIFFERENT SETS.** Intersection **4**, union **7** | **RECONCILED.** Section 3 |
| 2 | whether either number is complete | **NEITHER IS.** Three further sites carry the same claim about the same subject | **NAMED AND COUNTED AT TEN.** Section 3 |
| 3 | the two sites [ADR-382](ADR-382.md) left for a citation budget | the budget is real and the repair does not have to spend it | **BOTH REPAIRED AT A COST OF ZERO CITATIONS.** Section 4 |
| 4 | does the class recur outside `apps/worker` | **YES.** Four sites outside it, three of them derived false at primary source | **ONE REPAIRED, THREE OWED.** Section 5 |
| 5 | [ADR-380](ADR-380.md) section 15 item 4, which of the two wrapped paths misresolves | **NEITHER, under the shipped reader.** The premise holds only of the remedy that entry REFUSED | **RULED, AND A DIFFERENT DEFECT AT THE SAME SITE REPAIRED.** Section 6 |
| 6 | is the class checkable | **NO**, and the two nearest checks are out of scope BY CONSTRUCTION | **REFUSED WITH A MEASUREMENT, NO INVARIANT MINTED.** Section 7 |

---

### 3. The reconciliation, which is the first thing this row was told to do

**[ADR-382](ADR-382.md) STATES BOTH NUMBERS AND NEVER PUTS THEM SIDE BY SIDE.** Its section 4 repairs the clause *at three of five sites*; its section 6 counts the class *at six sites*. Read at primary source, the members are:

| site | in the FIVE? | in the SIX? | state on `0fc59a22` |
|---|---|---|---|
| [`sweeps/expiry-adapter.ts:54`](../../apps/worker/src/sweeps/expiry-adapter.ts) and its `expirySweepIo` docblock | yes | yes | **live**, left by `382` |
| [`schedule.ts:688`](../../apps/worker/src/schedule.ts) registry cell | yes | yes | repaired by `382` |
| [`index.ts:596`](../../apps/worker/src/index.ts) barrel register | yes | yes | **live**, left by `382` |
| [`CRON_INVENTORY`](../ops/runbooks/CRON_INVENTORY.md) line 34 | yes | yes | repaired by `382` |
| [`test/schedule.test.ts`](../../apps/worker/test/schedule.test.ts) case `6.1` | **yes** | **no** | repaired by `382` |
| [`schedule.ts:263`](../../apps/worker/src/schedule.ts) | **no** | **yes** | **live** |
| [`test/breaker.test.ts:974`](../../apps/worker/test/breaker.test.ts) | **no** | **yes** | **live** |

**SO THE INTERSECTION IS FOUR, THE UNION IS SEVEN, AND THE DISPATCH'S SUSPICION IS CORRECT.** The five is a REPAIR ledger and the six is a CLASS census, and they were never the same question. `382` repaired one member of the five that is not in the six, and counted two members of the six it never repaired and never listed as left. **Its section 8 names TWO sites left; FOUR were live when it finished.**

**AND BOTH NUMBERS ARE SHORT, WHICH IS THIS SECTION'S REAL OUTPUT.** Three further sites carry the same claim about the same subject and neither number reaches them:

1. **[`apps/worker/src/index.ts:532`](../../apps/worker/src/index.ts)**, a SECOND register in the file `382` already counted once. Lines `:525` to `:530` state the precise and true claim, that the one event producer is in a deployable this one cannot import; `:531` to `:532` then draw the false consequence, *a blocker that is now a call which does not compile*. **The degradation happens inside one paragraph, seven lines apart**, which is [ADR-034](ADR-034.md)'s last table row arriving on a claim.
2. **[ADR-370](ADR-370.md) line 84**, which carries the absolute AND the consequence and calls them *that row's own words and they are exact*.
3. **[ADR-376](ADR-376.md) line 92**, which carries the absolute and opens with **IT IS STILL TRUE**.

**COUNTED ON `ADR-382`'s OWN CONVENTION, WHICH FOLDS `expiry-adapter.ts`'s header and docblock into ONE ROW, THE CLASS STANDS AT TEN SITES AND NOT SIX.** Three were repaired by `382`, six are repaired here, and one is outside every fence dispatched so far.

---

### 4. The two sites left for a citation budget, repaired for nothing

**[ADR-382](ADR-382.md) PRICED THESE CORRECTLY AND PRICED ONLY ONE WAY OF MAKING THEM.** Its section 8 says the correction *does not fit in the two lines the clause occupies* and that 38 citations sit below the two sites, so it left them. **The unstated assumption is that a repair ADDS lines.** It does not have to.

**EVERY EDIT IN THIS DIFF IS LINE-COUNT NEUTRAL ACROSS ITS OWN REGION, AND THE RESULT IS THAT NOT ONE CITATION IN THE REPOSITORY MOVED.** `git diff --numstat` reports equal insertions and deletions on all five source files, `expirySweepIo` is still declared at `expiry-adapter.ts:193`, and every cited anchor listed in section 1 was **opened and read after the edit** rather than inferred from a green invariant run, which is [ADR-380](ADR-380.md)'s own warning followed rather than quoted.

| site | what was retired | technique | citations moved |
|---|---|---|---|
| [`expiry-adapter.ts:52-54`](../../apps/worker/src/sweeps/expiry-adapter.ts), and the section title at `:57` | the absolute, and a heading that asserted it | 3 lines for 3, 1 for 1 | **0** |
| [`expiry-adapter.ts:176-187`](../../apps/worker/src/sweeps/expiry-adapter.ts) docblock | the absolute and the consequence | 12 lines for 12 | **0** |
| [`index.ts:531-532`](../../apps/worker/src/index.ts) | the consequence | 2 for 2 | **0** |
| [`index.ts:592-597`](../../apps/worker/src/index.ts) barrel register | the absolute | 6 for 6 | **0** |
| [`schedule.ts:261-264`](../../apps/worker/src/schedule.ts) | the absolute, and *the constructor CANNOT BE CALLED* | 4 for 4 | **0** |
| [`test/breaker.test.ts:974-975`](../../apps/worker/test/breaker.test.ts) | the absolute | 2 for 2 | **0** |

**THE RETIRED CLAUSES ARE NAMED AND NOT REPRODUCED, WHICH IS [ADR-367](ADR-367.md) SECTION 4's MEASUREMENT OBEYED RATHER THAN CITED.** That entry seeded a retirement that QUOTED its false sentence and watched a prose-matching case go RED on two matches. So every retirement here says THAT a clause stood and what it denied, and none of them writes the sentence out. The one place a retired FIGURE is named, [`kyc-source.test.ts:48`](../../apps/portal/test/kyc-source.test.ts), writes it as the bare number `824` with no colon and no path, so it cannot parse as a live citation under `RI-15`'s grammar.

**AND ONE CORRECTION IS SHARPER THAN THE CLAUSE IT REPLACES.** [`schedule.ts:261`](../../apps/worker/src/schedule.ts) read *`ExpirySweepIo` HAS a constructor and the constructor CANNOT BE CALLED*. It can. [`test/expiry-adapter.test.ts:599`](../../apps/worker/test/expiry-adapter.test.ts) calls it, passing `UNWIRED_EXPIRY_SWEEP_IO.events`, and that case is GREEN on this tree and on the base. The line now reads **NOTHING CALLS IT**, which is what the caller census in that file actually asserts.

---

### 5. Does the class recur outside `apps/worker`, measured

**[ADR-382](ADR-382.md) REFUTED THE REPORT-LAG CLASS FOR `apps/worker` AND COULD NOT REACH THIS QUESTION. THE ANSWER IS YES.**

**THE SHAPE SWEPT IS STATED SO THE RECALL IS BOUNDED.** Two families were enumerated over the whole tree, and the counts are derived at reporting time:

```
S1  "the only <X> in this repository|tree|workspace|corpus"    38 code sites, 150 in docs/
S2  "nothing in this tree|repository|workspace ..."           136 code sites
S3  S1 narrowed to the MECHANICALLY DECIDABLE
    ("only file|module|copy|one|place|thing|site ...")          12 code sites
```

**FIVE OF THE TWELVE IN `S3` WERE OPENED AT PRIMARY SOURCE. THREE ARE FALSE AS WRITTEN.**

| site | claim | at primary source | verdict |
|---|---|---|---|
| [`playwright.config.ts:91`](../../playwright.config.ts) (pre-repair) | `e2e/pass.ts:17` is *the only file in this repository that imports the package* | **`playwright.config.ts:53` imports `defineConfig` from `@playwright/test`.** The sentence counts its own file out | **FALSE. Repaired here** |
| [`packages/db/src/client.ts:4`](../../packages/db/src/client.ts) | *THE ONLY FILE IN THE WORKSPACE THAT IMPORTS `pg`* | [`packages/db/src/scoped-db.ts:75`](../../packages/db/src/scoped-db.ts) imports it. **And [`scoped-db.ts:4218`](../../packages/db/src/scoped-db.ts) carries the true, narrower claim, *at runtime*, in the very file that falsifies the absolute** | **FALSE as written. OWED** |
| [`packages/queue/src/pg-boss-queue.ts:4`](../../packages/queue/src/pg-boss-queue.ts) | *THE ONLY FILE IN THE WORKSPACE THAT NAMES `pg-boss`*, and one line down, *the vendor's name appears in one module* | **32 files name it. One imports it**, and that narrower claim is what [`packages/queue/test/surface.test.ts:110`](../../packages/queue/test/surface.test.ts) asserts | **FALSE as written. OWED** |
| [`apps/worker/src/index.ts:594`](../../apps/worker/src/index.ts) | the only event producer is `apps/api/src/events.ts` | `EVENT_CATALOGUE`, `buildEvent`, `makeEventSink` and `TRANSACTION_EVENT_WRITER` are declared in that file and nowhere else; every other occurrence is prose | **TRUE** |
| [`repo-invariants.mjs:4562`](../../packages/tooling/checks/repo-invariants.mjs) | `.gitignore` is the only one in this repository | `find` returns exactly `./.gitignore` | **TRUE** |

**AND THE CLASS RECURS ON A DIFFERENT SUBJECT INSIDE `apps/worker`, WHICH REFINES `ADR-382`'s SWEEP RATHER THAN CONTRADICTING IT.** [`recon/sweep.ts:150`](../../apps/worker/src/recon/sweep.ts) read *No module in this tree writes `recon_blocked` in either direction*, and [`recon/sweep.ts:614`](../../apps/worker/src/recon/sweep.ts), in the same file, writes it. **The corpus cites `:614` EIGHT TIMES**, so the refutation is not merely present, it is the most-cited line in the file. The header's bullet at `:28` states the same absolute unscoped while [`test/recon-sweep.test.ts:225`](../../apps/worker/test/recon-sweep.test.ts) pins the scoped version, *outside `src/recon`*. **Both are repaired here, net-zero, and the true claim is that no module CLEARS it.** This one runs in the opposite direction from the sink clause: it makes a GAP look total rather than making a control look strong. **The mechanism is not direction-specific and the entry that named it did not claim otherwise.**

**THE PATTERN ACROSS ALL FIVE DECIDED SITES IS THE FINDING WORTH MORE THAN THE COUNT.** The two absolutes that survived contact with a derivation are the two an independent mechanism asserts: a named case for `pg-boss` containment, an ESLint rule for the `pg` import, an invariant for `.gitignore`. **Every false one is asserted by nothing, and in every false one a truer narrower claim already existed in the corpus**, in a test, in a sibling file, or one import statement up in the same file.

---

### 6. [ADR-380](ADR-380.md) section 15 item 4, settled, and the defect it was standing in front of

**THE ITEM SAYS TWO WRAPPED-PATH SITES EXIST AND *ONE OF THE TWO RESOLVES TO THE WRONG FILE*. UNDER THE SHIPPED READER, NEITHER DOES.** Derived by driving the checker's own resolver:

| site | token the grammar extracts | candidates | reachable at the cited line | resolves to |
|---|---|---|---|---|
| [`playwright.config.ts:90`](../../playwright.config.ts) | `pass.ts` | **1**: `e2e/pass.ts` (113 lines) | 1 | **the intended file** |
| [`kyc-source.test.ts:42`](../../apps/portal/test/kyc-source.test.ts) | `accounts.ts` | **3** | **1**: only `apps/api/src/routes/accounts.ts` (1,023 lines) reaches line 824 | **the intended file** |

**`citationReader` MATCHES BY PATH SUFFIX AND THEN `RI-15` DROPS EVERY CANDIDATE THE LINE NUMBER CANNOT REACH.** `pass.ts` is unique in the tree, so the wrap costs it nothing. `accounts.ts` is not, and it is rescued by an accident: `apps/portal/src/view/accounts.ts` has 304 lines and `packages/ledger/src/accounts.ts` has 152, and the citation is to line 824. **[ADR-380](ADR-380.md)'s premise is true of the NEAREST-BY-DIRECTORY remedy its own section 6 measured and refused**, where the portal file wins; it is not true of what shipped. That entry's section 6 table says exactly this and its section 15 restatement lost the qualifier, **which is this row's class occurring inside the record of this row's class.**

**THE REPAIR IS STILL WORTH TAKING AND THE REASON IS A MARGIN RATHER THAN A FAILURE.** The rescue is 824 against 304, so the citation is one file growth away from silently gaining a second answer. Both paths are now written whole, and each stays on the line [ADR-380](ADR-380.md) cites so its three pointers still land.

**AND THE WRAP WAS THE SMALLER OF TWO DEFECTS AT THAT SITE.** The comment attributes a direct quotation, *"fails closed, because the alternative is reporting somebody verified on the strength of an ordering this table does not declare"*, to `accounts.ts:824`. **That sentence is at `accounts.ts:846-848`.** Line 824 opens a different paragraph, about re-verification chains, in a docblock the quotation is not in. **Twenty-two lines, and nothing was going to find it**: the citation carries no backticked name, so `RI-15` asserts only that the file exists and the line is not blank, which is [ADR-380](ADR-380.md) section 1's own 94.4 percent measured on a live example. The pointer is repointed to `:846-848` and the retired figure is named.

---

### 7. Is the class checkable? NO, and this is the measurement

**[ADR-377](ADR-377.md) RULED THAT THE CITATION FORM CANNOT BE MADE CHECKABLE AND THAT RULING WAS RIGHT. THE SAME ANSWER IS RIGHT HERE, FOR A DIFFERENT AND SHARPER REASON.**

**THE TWO CHECKS THAT LOOK LIKE THEY SHOULD COVER THIS ARE OUT OF SCOPE BY CONSTRUCTION, NOT BY OVERSIGHT.**

1. **`RI-14` cannot see any of these sites and its own header says why.** It reads **EXPORTS** and never behaviour, and it states the exclusion in the exact words this row needs: *a reason saying a thing exists but cannot be CONSTRUCTED is out of scope, that is ADR-172's finding 1, which was true, and a check that failed it would be wrong.* **Every site in section 3 is precisely that shape.** `RI-14` also reads only `SOURCED_CLAIM_FILES`, which is **three files**, none of them a site here. Repairing "under `RI-14`" therefore means obeying its DOCTRINE. **It does not mean a runner will notice.**
2. **`RI-35` binds a sentence to an ARTIFACT whose absence a probe reports, and every artifact here is PRESENT.** The falsehood lives in the QUANTIFIER, not in the existence. Its header already priced the alternative: a check that fails on every absence phrasing *fails on more than twenty sites TODAY and most of them are TRUE*, and *a gate satisfied by deleting honest prose is a gate somebody deletes*.

**AND THIS TREE PRICES IT AGAIN, LARGER.** Section 5's own numbers are the argument: **38 code sites and 150 documentary sites for `S1`, 136 code sites for `S2`.** Of the five decided at primary source, **two were true and were true for a reason a reader can check.** A gate over the shape would arrive with a backlog in the hundreds, over prose that is mostly correct, in a frozen corpus. That is [ADR-380](ADR-380.md) section 4's 101 findings again with a worse ratio.

**SO NO INVARIANT IS MINTED AND NO `RI-` NUMBER IS RESERVED.** The dispatch said not to mint one to look productive, and the honest position is that this class is decided by opening a file, which is what every finding in section 5 came from.

**WHAT A READER SHOULD DO INSTEAD, WHICH IS A RULE RATHER THAN A GATE.** Section 5's five sites separate cleanly and the separator is not subject matter:

> **An absolute in prose either NAMES the mechanism that asserts it, or it is written as the narrower claim that is true.**

`pg-boss` containment survived because its sentence names `test/surface.test.ts`. The `pg` import survived at `scoped-db.ts:4218` because that sentence carries *at runtime* and the lint rule behind it. `.gitignore` survived because an invariant holds it. **The three that failed name nothing, and in all three the narrower true claim was already written somewhere in the corpus.** The failure is not that nobody checked; it is that a sentence was widened past the thing that was holding it up. **A reader reviewing a diff can apply that in one question, and no runner can.**

---

### 8. What is bound mechanically, stated plainly: NOTHING, and that is the honest report

**THIS DIFF ADDS NO TEST CASE AND NO ASSERTION.** [ADR-382](ADR-382.md) bound its repairs in cases `9.1` and `6` because it was making a claim about a VALUE, that a refusing `ExpiryEventPort` exists and both censuses are blind to it, and a value is bindable. **This row's output is a reconciliation and six prose corrections, and `ADR-324`'s rule as quoted by `RI-35` is that A TEST THAT PINS PROSE GOES STALE IN SILENCE.** Pinning these sentences would manufacture exactly the control this entry has just argued cannot exist.

**THE THREE THINGS THAT DO HOLD ARE INHERITED AND WERE RE-RUN RATHER THAN ASSUMED**, and they are named so nobody reads section 4 as having loosened them: the arity pin at [`test/expiry-adapter.test.ts:294`](../../apps/worker/test/expiry-adapter.test.ts), the caller census in section 5 of that same file, and case `9.1` in [`test/schedule.test.ts`](../../apps/worker/test/schedule.test.ts). All three are GREEN on this tree, none was edited, and the bar they hold is unmoved.

**A READER WHO WANTS A NUMBER FOR HOW MUCH THIS ROW IS WORTH SHOULD USE THE SUITE DELTA, WHICH IS ZERO.** That is the correct measurement of a comments-only diff and it is stated rather than dressed up.

---

### 9. Owed outside this fence, stated precisely enough to be picked up

1. **[`packages/db/src/client.ts:4`](../../packages/db/src/client.ts) states an absolute its own sibling qualifies.** The repair is the two words `at runtime`, already written at [`scoped-db.ts:4218`](../../packages/db/src/scoped-db.ts). `packages/db/**` is outside this fence. **The cheapest item in this list and the clearest.**
2. **[`packages/queue/src/pg-boss-queue.ts:4`](../../packages/queue/src/pg-boss-queue.ts) says *NAMES* where its guard asserts *IMPORTS*.** 32 files name the vendor; one imports it. The repair is one word in the header and one in the sentence below it. `packages/queue/**` is outside this fence.
3. **[`apps/admin/package.json:17`](../../apps/admin/package.json) carries the SAME false clause this row repaired in [`playwright.config.ts`](../../playwright.config.ts)**, that `e2e/pass.ts:17` is the only file importing `@playwright/test`. **It is a second copy of one sentence in a second file, which is `RI-35`'s occurrence 3 exactly.** `apps/admin/**` is outside this fence.
4. **[ADR-370](ADR-370.md) line 84 and [ADR-376](ADR-376.md) line 92 carry the retired sink clause**, line 84 with the consequence attached and calling it *exact*. **`ADR-376` is a NEW finding; `ADR-370` is the fifth consecutive row to record that `docs/decisions/ADR-370.md` is in no fence anybody has dispatched**, after [ADR-371](ADR-371.md), [ADR-376](ADR-376.md), [ADR-378](ADR-378.md) and [ADR-382](ADR-382.md).
5. **[ADR-370](ADR-370.md) section 3.1's two cells: STILL UNCORRECTED, confirmed a FIFTH consecutive time**, and its section 9 text still owes the correction [ADR-382](ADR-382.md) could not deliver.
6. **[ADR-380](ADR-380.md) section 15 item 4's own restatement is imprecise** and section 6 above says how. Its section 6 table is right; the item that summarises it dropped the qualifier. `docs/decisions/ADR-380.md` is outside this fence.
7. **The three `docs/` citations into `schedule.ts` that [ADR-382](ADR-382.md) recorded already stale are still stale and are still unmoved**, because every line this diff touches in that file is replaced one for one. *Already broken* is a reason and not a licence, followed rather than revisited.
8. **Nothing is owed inside `packages/tooling/**`, `apps/api/**` or `docs/ops/runbooks/**`.** None of the three was opened for writing. `CRON_INVENTORY` line 34 was READ and confirmed carrying `382`'s repair.

---

### 10. Verification

Every figure derived at reporting time off each runner's own last line, stated in the [session log](../sessions/2026-09-06-session-580.md).

- **Suite: 336 files / 8,085 passed / 10 skipped / 0 failed**, against a base of **336 / 8,085 / 10 / 0** reproduced on `0fc59a22` **BEFORE the first edit**. **Delta ZERO on every axis**, which is the expected result for a comments-only diff and is the attribution: no case was added, none removed, none renamed, and no file moved.
- Gates **33 of 33** after `generate`. Invariants **35 of 35**. `typecheck`, `lint` and `format:check` clean.
- **`pnpm run verify` and `scripts/corpus/falsify.mjs` were NOT run**, both forbidden by this row because both mutate the working tree.
- **NO SEED WAS PLANTED.** Section 8 says why there was nothing to observe failing: this diff changes no executable line, so a seed would have been theatre. The one scratch artifact, a copy of the checks package used to drive the citation reader, was created outside any tracked path's meaning, deleted, and `git status --porcelain` confirmed empty before the gates were run.
- **No pull request was opened and nothing was merged, by instruction.**

---

### 11. Where this entry is most likely wrong

**SECTION 5's RECALL IS NOT MEASURED AND THE SAMPLE IS NOT RANDOM.** Five of twelve `S3` sites were opened, and one of the five, `playwright.config.ts`, was handed to this row by [ADR-380](ADR-380.md) as a known-suspicious site. **So *three of five false* is an upper-biased estimate and should not be read as a rate.** The seven unopened `S3` sites, the remaining `S1` sites and all 136 `S2` sites are unchecked. A claim written in a shape neither family matches is invisible to this sweep, which is the weakness [ADR-382](ADR-382.md) recorded for its own section 6 and it is inherited whole.

**SECTION 7 REFUSES A GATE ON A COST ARGUMENT, AND A COST ARGUMENT IS THE ONE A LATER ROW CAN OVERTURN WITH A BETTER PARSE.** The refusal rests on the shapes being mostly true, which is measured, and on no narrower decidable shape existing, which is **not** measured. `S3` is decidable and is only 12 sites. **A reader who holds that a check over `S3` alone is worth a number is not obviously wrong**, and the argument against it is that `S3` is a grep for a phrasing rather than for a property, so it is satisfied by rewording. That argument is weaker than section 7 makes it sound and it is flagged here rather than left in the ruling.

**AND SECTION 4's TECHNIQUE HAS A COST NOBODY HAS PRICED.** Line-count-neutral editing keeps citations valid by CONSTRAINING PROSE TO A LINE BUDGET. Three of the six repairs dropped a clause to fit: `job.ts`'s wiring detail in the docblock, *a finding for the session log* in `recon/sweep.ts`, and the standalone `@param` wording. **Each is small and each was a choice made by arithmetic rather than by meaning**, which is [ADR-378](ADR-378.md)'s dropped clause arriving by a different route. A corpus where the citation graph decides how long a sentence may be has a problem that neither this row nor `382` has named properly.

---

### 12. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** Inherited as well as owed: [ADR-357](ADR-357.md) and [ADR-360](ADR-360.md) through [ADR-382](ADR-382.md) are `proposed` and unsigned, and this row settles none of them. **`383` and `384` are RESERVED and not written**, derived by listing `docs/decisions/` at reporting time: two siblings hold them on this wave and neither number is claimed here.

  **What a founder read adds and this entry cannot.**

  **THE FIRST THING TO DECIDE IS WHETHER SECTION 7's REFUSAL IS ACCEPTED, BECAUSE IT IS THE SECOND CONSECUTIVE WAVE IN WHICH A ROW HAS MEASURED A REAL DEFECT CLASS AND DECLINED TO GATE IT.** [ADR-377](ADR-377.md) refused for the citation form, [ADR-380](ADR-380.md) refused a proximity remedy, and this row refuses for absolutes in prose. **Three refusals with three good measurements behind them is either discipline or a pattern of declining to build controls**, and only a founder can say which. The alternative on offer is the `S3` gate section 11 flags, priced at 12 sites.

  **THE SECOND IS THAT A REPAIR LEDGER AND A CLASS CENSUS WERE PUBLISHED AS TWO NUMBERS IN ONE ENTRY AND NOBODY NOTICED THEY WERE DIFFERENT SETS.** Section 3. `ADR-382` said in writing that it had not reconciled them, which is why this row exists, and that honesty is the only reason the gap was recoverable. **Whether an entry may ship two counts of overlapping things without stating their relation is a corpus rule that does not exist yet**, and `ADR-034` governs derivable counts rather than this.

  **THE THIRD IS SECTION 11's LAST PARAGRAPH, WHICH IS THE ONE THIS ENTRY WOULD MOST LIKE OVERTURNED.** Six repairs here were shaped by a line budget imposed by the citation graph, and two prior rows paid different prices for the same constraint. **The constraint rather than any row is the thing to rule on**, which is what `ADR-382`'s own approval block said, arriving a second time with the technique that dodges it and the cost of dodging it both measured.

- **Approval:** `___________________________` (UNSIGNED. **MONEY-ADJACENT, `E2` READ OWED.** **Merging is not signing.**)

| | |
|---|---|
| **Approved by** | |
| **Date** | |
