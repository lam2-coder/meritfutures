## ADR-295: slice 6's approval branch LANDED, and the lock ADR-293 ruled is STILL OWED because the fence that granted this file granted it for seven named lines  (2026-08-30, status: proposed)

**Row `295` owns [ADR-287](ADR-287.md) slice 6, the `hold === null` path of `insertPayoutRequest()`, RE-DISPATCHED after row `293`'s fence could not reach the declaration [ADR-289](ADR-289.md) ruled. THE CODE LANDED.** `postgresPayoutBackend.insertPayoutRequest` writes an `approved` row through the scoped door, `PayoutTx.insertPayoutRequest` returns `Promise<void>`, and [ADR-289](ADR-289.md)'s seven owed lines are all seven taken.

**THE ROW'S HARD HALF WAS DONE BY [ADR-293](ADR-293.md) AND IS IMPLEMENTED HERE RATHER THAN REDONE.** That entry proved by compiler that the declaration was the only error and ruled the lock question [ADR-291](ADR-291.md) handed over. **Neither is re-decided.** What this entry adds is the body, the seven lines, the two prose corrections [ADR-293](ADR-293.md) section 4D left in this file's fence, `RI-34`, and **one honest residue**: the lock is ruled and is NOT built, because clauses 2 and 4 of that ruling are lines of [`routes/payouts.ts`](../../apps/api/src/routes/payouts.ts) that this row's fence does not reach.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `295`, session **486**, branch `claude/one-declaration-away`. **NO MIGRATION WAS WRITTEN, NO NUMBER TAKEN AND NONE RESERVED**; `packages/db/migrations/**` was read and never opened for writing. **[`start.ts`](../../apps/api/src/start.ts) WAS NEVER OPENED. THE COUNTS DID NOT MOVE**: `{ declared: 24, wired: 10, blocked: 14 }`, [`wiring.test.ts:1447`](../../apps/api/test/wiring.test.ts), confirmed at source before and after. **`RI-34` WAS MINTED** (section 6). **NO GITHUB ACCESS: no pull request was opened and nothing was merged.**

- **MONEY PATH.** The [**`E2`**](../../MERIT_BUILD_MASTER_PROMPT.md) line-by-line read is **OWED AND HAS NOT HAPPENED.** Ships `proposed` with an **UNSIGNED** approval line. **Merging is not signing.** It inherits `E2` debt on [ADR-293](ADR-293.md), [ADR-291](ADR-291.md), [ADR-289](ADR-289.md), [ADR-287](ADR-287.md), [ADR-285](ADR-285.md), [ADR-283](ADR-283.md), [ADR-281](ADR-281.md), [ADR-268](ADR-268.md) and [ADR-176](ADR-176.md), every one of which is `proposed` and UNSIGNED. **THIS IS THE FIRST ENTRY IN THE CHAIN WITH A `src/` WRITE ON THE MONEY PATH TO READ**, which is what makes the owed read different in kind from the four above it.

- **THE HOLD BRANCH WAS NOT BUILT AND NOT SCHEDULED.** No clause id was invented, no enum of clause names was written and no default string was chosen. It REFUSES BY NAME (section 4).

---

### 1. The three traps, re-derived at their primary sources ON THIS TREE

**Row `295` required this even though session 484 had already done it, and the requirement is right: a body written against an inherited grep is the defect [CLAUDE.md](../../CLAUDE.md) names as the cause of the reconciliation session's three worst errors.** Every command below was run in this session, on this tree, before a character of the body was written. **All three hold.**

#### 1.1 Traps one and two: `splitBp` and `clampReason` have NO COLUMN, and both values are ALREADY STORED

| The claim | Command, and what it returned here |
|---|---|
| `split_bp` has no column | `grep -rn split_bp packages/db/migrations \| wc -l` returned **`0`** |
| `clamp_reason` has no column | `grep -rn clamp_reason packages/db/migrations \| wc -l` returned **`0`** |
| Both are declared on the insert shape anyway | `readonly splitBp: number;` and `readonly clampReason: ClampReason;`, [`payouts.ts:522-523`](../../apps/api/src/routes/payouts.ts) |
| Both are already inside the snapshot | `snapshotOf` ([`payouts.ts:1076`](../../apps/api/src/routes/payouts.ts)) writes `reason: evaluation.clamp.reason` and `split_bp: evaluation.clamp.splitBp` into its `clamp` object |
| And the snapshot IS a column | `eligibility_snapshot     jsonb NOT NULL`, [`0010_payouts.sql:74`](../../packages/db/migrations/0010_payouts.sql) |

**SO THE BODY NAMES NEITHER FIELD AND DROPS NEITHER VALUE, AND THOSE ARE ONE ACTION RATHER THAN TWO.** It names ELEVEN of the shape's FOURTEEN fields plus the derived `planVersionId`; the two values reach the database inside `eligibilitySnapshot`, which is passed through untouched. **A second home for either would state one money fact twice on the money path**, and the second statement is the one that can disagree.

**AND THE RESPONSE STILL CARRIES BOTH, WHICH IS WHY DROPPING THEM WOULD HAVE BEEN SILENT.** `clamp_reason: clamp.reason` and `split_bp: clamp.splitBp` are wire fields read off the evaluation and not off the row, so a body that discarded them would have broken nothing a test could see. **`RI-34` is what makes this a property rather than a paragraph** (section 6).

#### 1.2 Trap three: `plan_version_id` is `NOT NULL`, is not on the insert shape, and IS DERIVED

| The claim | Live source, read here |
|---|---|
| The column is `NOT NULL` on the table being written | `plan_version_id          uuid NOT NULL REFERENCES plan_versions(id) ON DELETE RESTRICT`, [`0010_payouts.sql:68`](../../packages/db/migrations/0010_payouts.sql) |
| `PayoutRequestInsert` does not carry it | The shape's FOURTEEN fields, [`payouts.ts:491-534`](../../apps/api/src/routes/payouts.ts). There is no `planVersionId` |
| The source it is derived from | `plan_version_id        uuid NOT NULL REFERENCES plan_versions(id) ON DELETE RESTRICT`, [`0007_accounts.sql:50`](../../packages/db/migrations/0007_accounts.sql) |
| **And the derivation is SOUND rather than convenient** | [`0007_accounts.sql:12`](../../packages/db/migrations/0007_accounts.sql) states it as a numbered property of the table: *"plan_version_id NEVER CHANGES, for the life of the account"*, and `assert_account_plan_version_pinned` ([`0027_triggers_invariants.sql:269-278`](../../packages/db/migrations/0027_triggers_invariants.sql)) RAISES on `NEW.plan_version_id IS DISTINCT FROM OLD.plan_version_id`. **The database refuses to let this value move**, so a copy taken on the same transaction cannot be stale |
| The read is on the same transaction and is scoped | `accounts` is scope class `owned` on `identity_id` ([`scope.ts`](../../packages/db/src/scope.ts)), so `handle.rowAt('accounts', { id })` reaches the caller's own account and reads `undefined` for anybody else's |

**AND AN ABSENT ACCOUNT ROW IS A REFUSAL RATHER THAN A DEFAULT.** The body throws `PayoutRowError`, which is deliberately NOT a `PayoutBackendUnwired` and is therefore not caught by `unwiredOrThrow`: nothing is down and a retry fixes nothing.

#### 1.3 `identity_id` NEEDS NO DERIVATION, WHICH IS THE HALF THAT LOOKS LIKE A FOURTH TRAP

`payout_requests` is scope class `owned` on `identity_id` ([`scope.ts`](../../packages/db/src/scope.ts)), and **a scoped write STAMPS its tenancy column from the handle: naming it in an insert's values is REFUSED** ([`scoped-db.ts`](../../packages/db/src/scoped-db.ts), *"a scoped write never takes it from the caller"*). So the absence of `identityId` from `PayoutRequestInsert` is the door working, not a field somebody forgot. **The body names it nowhere and `RI-34` leg two holds that.**

#### 1.4 The counts, at source, before and after

`{ declared: 24, wired: 10, blocked: 14 }`, [`wiring.test.ts:1447`](../../apps/api/test/wiring.test.ts). **Unmoved, and it could not have moved: `wired` is the set of ports [`start.ts`](../../apps/api/src/start.ts) CALLS, and `start.ts` was never opened.** `grep -c 'usePayoutBackend(' apps/api/src/start.ts` returned **`0`** after the diff, as it did before it. `CATALOG_TABLE_KEYS` is still five ([ADR-268](ADR-268.md) refused a sixth in writing) and nothing here reads a catalogue.

---

### 2. WHAT LANDED

#### 2.1 [ADR-289](ADR-289.md)'s seven owed lines, all seven, matched on TEXT and not on number

[ADR-289](ADR-289.md) section 4.2 wrote them as a checklist *"rather than a judgement"* and said the row that takes them matches on the text, which is that entry's own section 1.3 finding. **Every one was located by its text and every one is taken:**

| # | Where | Now reads |
|---|---|---|
| 1 | `payouts.ts`, `PayoutResponse` | `readonly eligibility_snapshot_id: string;` **DELETED** |
| 2 | `payouts.ts`, the port docblock | *"Write the request."* plus [ADR-289](ADR-289.md)'s reason, written out |
| 3 | [`payouts.ts:596`](../../apps/api/src/routes/payouts.ts) | `insertPayoutRequest(row: PayoutRequestInsert): Promise<void>;` |
| 4 | [`payouts.ts:1207`](../../apps/api/src/routes/payouts.ts) | `await tx.insertPayoutRequest(row);` |
| 5 | `payouts.ts`, the response object | `eligibility_snapshot_id: eligibilitySnapshotId,` **DELETED** |
| 6 | `payouts.test.ts`, two fixtures | `return Promise.resolve();` in both |
| 7 | `payouts.test.ts`, the tautological assertion | **REPLACED BY ITS OWN INVERSE**, `expect(body).not.toHaveProperty('eligibility_snapshot_id')` |

**LINE 7 IS THE ONE WORTH READING, BECAUSE DELETING IT OUTRIGHT WAS THE OTHER OPTION AND IS WORSE.** The assertion that stood there checked that the route forwarded what a fixture invented, and [ADR-289](ADR-289.md) section 3.4 item 6 called it a tautology. **An absence assertion in its place costs one line and makes the field unrestorable without a red test**, which is the failure mode [ADR-289](ADR-289.md) section 4.1 wrote a contract note against on the document side and left unguarded on the code side.

**AND `apps/api` TYPECHECKS.** `tsc -p apps/api --noEmit` exits **0**, which is [ADR-293](ADR-293.md) section 2.2's transcript with its one error gone.

#### 2.2 The body, in [`payout-backend.ts`](../../apps/api/src/payout-backend.ts)

Exactly the shape [ADR-293](ADR-293.md) section 4C compiled: read `accounts` on the same transaction for `planVersionId`, refuse rather than default if that read returns nothing, insert into `payoutRequests` naming ELEVEN of the shape's fourteen fields plus the derived `planVersionId`, name neither `splitBp` nor `clampReason` nor `identityId`, and return nothing.

**AND ON THIS BRANCH `hold` IS `null`, SO THE FIVE HOLD COLUMNS ARE NAMED NOWHERE**, which is what `payout_requests_hold_is_complete` ([`0031_payout_hold_and_identity_restriction.sql:61-72`](../../packages/db/migrations/0031_payout_hold_and_identity_restriction.sql)) requires: all five together with `held_pending_review`, or none.

**EVERY MONEY FIELD IS PASSED THROUGH AS THE `bigint` IT ARRIVES AS.** `Cents` is `bigint` ([`types.ts:38`](../../packages/rules-engine/src/types.ts)), the four columns are `bigint NOT NULL` in [`0010_payouts.sql`](../../packages/db/migrations/0010_payouts.sql), and there is no `Number()` on this path. **The fixtures are `bigint` literals too**, deliberately: a fixture written in `number` would assert against a row this code never receives.

#### 2.3 The two prose corrections [ADR-293](ADR-293.md) section 4D left inside this file's fence

**BOTH ARE CORRECTED WHERE THEY WERE MADE RATHER THAN DELETED**, because a false sentence deleted leaves nothing for the next reader to check.

| Where | What it said | What it says now |
|---|---|---|
| `payout-backend.ts`, the `insertPayoutRequest` comment | *"`insertPayoutRequest` must return `{ eligibilitySnapshotId }`"* | [ADR-289](ADR-289.md) ruled it returns nothing. The comment names the retired claim, says which half of it was true, and says which ADR retired it |
| `payout-backend.ts`, the `transact` docblock | *"`databaseWithdrawalBackend` calls `tx.lockScope()` first"* | It EXPOSES the member; `decideWithdrawal` calls it. [ADR-293](ADR-293.md) section 3.4, and section 3 below |

**THE THIRD CORRECTION IS NOT TAKEN AND IS STILL OWED.** [ADR-287](ADR-287.md)'s F2 citations are four stale and one a backwards range; [`ADR-287.md`](ADR-287.md) is out of this row's fence, as it has been out of every fence allocated since [ADR-289](ADR-289.md) section 5.2 found them.

#### 2.4 The census, moved rather than left stale

Three files carried the sentence that this member refuses, and all three are inside `apps/api/test/**`:

- [`payout-backend.test.ts`](../../apps/api/test/payout-backend.test.ts): the refusing-member list drops to `subject` and `holdFlag`, and a NEW describe block of nine cases drives the approval branch over `db-recorder.ts`.
- [`rule-state-producibility.test.ts`](../../apps/api/test/rule-state-producibility.test.ts): the census asserts `new PayoutBackendUnwired('insertPayoutRequest')` is **ABSENT** and `new PayoutBackendUnwired('insertPayoutRequest.hold')` is **PRESENT**. **THE NEEDLE IS THE HOLD ARM ON PURPOSE**: a session that "completed" this member by inventing a clause id would have to delete exactly that line.
- [`wiring.test.ts`](../../apps/api/test/wiring.test.ts): `usePayoutBackend`'s entry clause FIVE now names THREE members answering, FOUR refusing, one BRANCH refusing, and the owed lock line of section 3 below.

---

### 3. THE LOCK: [ADR-293](ADR-293.md)'s RULING IS INHERITED AND IS **NOT BUILT**, AND THAT IS THE RESIDUE THIS ENTRY EXISTS TO MAKE LOUD

**IT IS NOT RE-DECIDED. THE PAYOUT PATH LOCKS.** [ADR-293](ADR-293.md) section 3.5 ruled it in five clauses on evidence this session did not re-open, and row `295` says not to re-decide it. **This entry takes the ruling as settled and reports only what it could not build.**

#### 3.1 What the ruling requires, and where each clause lives

| Clause | File | In this fence? |
|---|---|---|
| 1. The payout path takes the per-identity lock | (the ruling itself) | n/a |
| 2. `PayoutTx` GAINS `lockScope(): Promise<void>` | [`routes/payouts.ts`](../../apps/api/src/routes/payouts.ts) | **NO** |
| 3. `postgresPayoutBackend` delegates to `handle.lockScope()` | [`payout-backend.ts`](../../apps/api/src/payout-backend.ts) | YES |
| 4. `decidePayout` calls it as its FIRST statement | [`routes/payouts.ts`](../../apps/api/src/routes/payouts.ts) | **NO** |
| 5. `transact` still takes no lock | [`payout-backend.ts`](../../apps/api/src/payout-backend.ts) | YES, and it is stated as a decision now |

**ROW `295` GRANTED [`routes/payouts.ts`](../../apps/api/src/routes/payouts.ts) FOR THE `insertPayoutRequest` DECLARATION AND [ADR-289](ADR-289.md)'S SEVEN LINES, AND SAID "NOTHING ELSE IN THAT FILE."** Clauses 2 and 4 are neither. **The fence was not widened.**

#### 3.2 Clause 3 IS NOT BUILDABLE ALONE, and that is a fact about TypeScript rather than a preference

An object literal annotated `: PayoutTx` and carrying a `lockScope` the interface does not declare is an excess-property error. **So clause 3 without clause 2 does not compile**, and clause 3 written as a loose helper is [ADR-293](ADR-293.md) section 2.3(b) exactly: money-path code with no caller and no census entry. **There is no partial arm here to take.**

#### 3.3 WHAT LANDING SLICE 6 WITHOUT THE LOCK ACTUALLY COSTS, stated rather than minimised

**THE READ-THEN-WRITE THIS BODY CREATES IS REAL AND IS NOW ON THE TREE.** [ADR-293](ADR-293.md) section 3.1 is unchanged by this diff: `G-NO-IN-FLIGHT` is read through `subject()`, decided in memory, and the row is written against that decision; the clamp is the second one and it is the money one.

**AND THE BACKSTOP IS REAL AND IS NOT THE CONTROL.** `payout_requests_no_in_flight_uq` is `ON payout_requests (account_id) WHERE status IN ('approved', 'frozen', 'held_pending_review')` ([`0031:104-106`](../../packages/db/migrations/0031_payout_hold_and_identity_restriction.sql), which DROPPED and re-created [`0010`](../../packages/db/migrations/0010_payouts.sql)'s two-status version). **It fails in the two ways [ADR-293](ADR-293.md) section 3.3 measured**: it converts a contract-specified `409 conflict` into a **500**, because a unique violation is not a `PayoutBackendUnwired` and `unwiredOrThrow` rethrows it; and it is keyed **per account** where the exposure question the schema denormalises `identity_id` for is **per identity**, so two accounts of one identity pass it simultaneously.

**WHAT MAKES THIS ACCEPTABLE TO LAND RATHER THAN A REASON TO STOP IS THAT NO DEPLOYMENT REACHES IT.** [`start.ts`](../../apps/api/src/start.ts) does not call `usePayoutBackend`, the route answers 503, and slice 9 is a separate money-path session behind a founder `E2` read. **A race needs two requests and there are none.** The residue is therefore a PRECONDITION OF SLICE 9 rather than a live exposure, and it is written into `usePayoutBackend`'s own blocked reason so that the session dispatched to install this port meets it there.

#### 3.4 AND THE DISPATCH DEFECT IS THE SAME ONE, ONE ROW LATER

[ADR-293](ADR-293.md) section 2.4 attributed row `293`'s blocker to the dispatch rather than to a build session, and its section 4 said **"ONE ROW SHOULD TAKE ALL OF IT, AND ITS FENCE MUST NAME [`routes/payouts.ts`](../../apps/api/src/routes/payouts.ts)"**, listing A, B, C and D. **Row `295` named the file and granted it for A and C. B was not granted.** So the entry that diagnosed a specification complete and a scheduling gap was followed by a row that scheduled two thirds of it.

**THIS IS RECORDED AS THE DISPATCHER'S AGAIN AND NOT AS A SESSION FAILURE, AND THE REMEDY IS NOT A BIGGER FENCE.** [ADR-293](ADR-293.md) section 5 already named the real control and refused to build it out of fence: *"every `OWED` edit named in an ADR has an allocation row whose fence reaches it"*, which is a **corpus gate** belonging beside `CI-06f` in [`scripts/corpus/`](../../scripts/corpus/), not an `RI-nn`. **That refusal is inherited here unchanged**, and `scripts/corpus/` is out of this row's fence too. **Reported for the third time, not built.**

---

### 4. THE HOLD BRANCH REFUSES BY NAME, and nothing was invented to make it answer

**ADR-287 SLICE 8 CANNOT BE SCHEDULED.** `HoldFlag.tosClause` ([`payouts.ts`](../../apps/api/src/routes/payouts.ts)) has no value space in this repository; [`docs/legal/TOS_CLAUSES.md`](../legal/TOS_CLAUSES.md) is a drafting brief with **no drafted prose anywhere in it, deliberately**, and `DEP-M7-05` ([`M07`](../plans/M07-risk-abuse.md)) owes the clauses to **counsel**.

**SO A ROW CARRYING A HOLD IS REFUSED HERE**, with `PayoutBackendUnwired('insertPayoutRequest.hold')`, which is a **503** and is the honest answer for an unbuilt member. **No clause id was invented, no enum of clause names was written and no default string was chosen.**

**THE PREDICATE IS TWO SIDED AND THAT IS THE CONSTRAINT'S OWN SHAPE RATHER THAN A CONVENIENCE.** `payout_requests_hold_is_complete` admits `hold` present WITH `status = 'held_pending_review'`, or the five columns absent WITH any other status. The refusal here reads `row.hold !== null || row.status !== 'approved'`, so a row that is half of either is refused at the door instead of reaching a CHECK constraint and coming back as a 500 nobody can diagnose. **`held_pending_review` IS a member of `payout_status`**, added by [`0030_payout_hold_enum.sql`](../../packages/db/migrations/0030_payout_hold_enum.sql) over [`0001`](../../packages/db/migrations/0001_extensions_and_enums.sql)'s four, so the status alone could not have carried the refusal.

**AND THE ARM IS UNREACHABLE FROM `decidePayout` TODAY, WHICH IS WHY IT IS ASSERTED RATHER THAN ASSUMED.** `holdFlag()` still rejects, so this backend never produces a row with a hold; the refusal exists for a caller that is not `decidePayout`, and a case in [`payout-backend.test.ts`](../../apps/api/test/payout-backend.test.ts) drives it directly.

---

### 5. WHAT IS STILL OWED, as a checklist at live text

**ONE ROW SHOULD TAKE ALL OF IT AND ITS FENCE MUST NAME [`routes/payouts.ts`](../../apps/api/src/routes/payouts.ts) WITHOUT A CLAUSE LIMITING IT TO NAMED LINES.** It is a **MONEY-PATH** row under [ADR-003](ADR-003.md) strict. **Match on the text and not on the line number.**

**A. [ADR-293](ADR-293.md) section 3.5 clauses 2 and 4**, which are section 3.1 above:

1. `PayoutTx` gains `lockScope(): Promise<void>`, worded on `WithdrawalTx`'s ([`wallet-withdrawals.ts`](../../apps/api/src/routes/wallet-withdrawals.ts)) and `CheckoutTx`'s ([`checkout.ts`](../../apps/api/src/routes/checkout.ts)), both of which take no argument.
2. `postgresPayoutBackend` implements it as `lockScope: async () => { await handle.lockScope(); }`, which is the whole body. **This is one line inside this row's fence and it lands with clause 1 or not at all** (section 3.2).
3. `decidePayout` calls `await tx.lockScope();` as its FIRST statement, above `gateIdentityStatus`. This does not disturb `ADR-140`: `lockScope()` reads nothing about the account and takes no address.
4. `db-recorder.ts` gains a `lockScope` verb, because the recorder implements the handle and would otherwise throw a `TypeError` the moment the delegation runs. **It is in `apps/api/test/**` and would have been in this row's fence; it is not taken here because a recorder verb nothing calls is a fixture for a member that does not exist.**

**B. [ADR-287](ADR-287.md)'s F2 citations**, four stale and one a backwards range, derived live by [ADR-289](ADR-289.md) section 1.3 and still unrepaired because that file has been out of every fence since.

**C. The corpus gate of [ADR-293](ADR-293.md) section 5**, refused there and refused again in section 3.4 above, on the same ground: it belongs in [`scripts/corpus/`](../../scripts/corpus/) beside `CI-06f` and is out of fence.

---

### 6. `RI-34`, MINTED AND SPENT, with the RED and the GREEN both watched

**ROW `293` RESERVED THIS NUMBER AND [ADR-293](ADR-293.md) SECTION 5 RETURNED IT**, because no `src/` line moved in that session, and it said of this exact candidate: *"When the body lands it is worth taking, and the row that lands it should take it."* **The body landed here.**

**WHAT IT ASSERTS**, title read live from the check: *"Every writer of payout_requests derives plan_version_id and names no column-less field"*. Every `insert` into `payoutRequests` under an `apps/*/src` or `packages/*/src` directory, comments stripped through the one scanner and the values object parsed as a **balanced object literal** rather than matched. **FOUR LEGS**: no writer names `splitBp` or `clampReason` in either spelling; no writer names the tenancy column `identityId`; every writer names `planVersionId`; and a **SPREAD is a finding on its own**, because `{ ...row }` writes all fourteen fields and no key scan can see it.

**WHY IT IS A CHECK RATHER THAN A COMMENT.** [CLAUDE.md](../../CLAUDE.md)'s caution is that the reconciliation session's three worst errors were failures to check a claim against a primary source, and that the remedy is *"a new CI gate over a bigger model whenever the error is checkable"*. **This one is checkable and it fails in the worst direction**: writing `splitBp` breaks nothing any test can see.

#### 6.1 The four legs, five seeds, all on the real tree, RED watched

Each seed was applied to [`payout-backend.ts`](../../apps/api/src/payout-backend.ts), the check run alone, and the file restored byte for byte.

```
SEED 1  splitBp: row.splitBp,  added to the insert
FAIL   RI-34  Every writer of payout_requests derives plan_version_id and names no column-less field  (1)
       apps/api/src/payout-backend.ts:337 names `splitBp` in a `payoutRequests` insert. `payout_requests`
       has NO SUCH COLUMN and the value is already stored inside `eligibility_snapshot`, so a second home
       for it would state one money fact twice on the money path
exit=1

SEED 2  clamp_reason: row.clampReason,  the SQL spelling
FAIL   RI-34  ...  (1)
       apps/api/src/payout-backend.ts:337 names `clamp_reason` in a `payoutRequests` insert. ...
exit=1

SEED 3  identityId: row.accountId,  the tenancy column
FAIL   RI-34  ...  (1)
       apps/api/src/payout-backend.ts:337 names `identityId` in a `payoutRequests` insert. That is the
       tenancy column, `packages/db` stamps it from the handle, and naming it in an insert`s values is
       REFUSED at run time
exit=1

SEED 4  the planVersionId line deleted
FAIL   RI-34  ...  (1)
       apps/api/src/payout-backend.ts:337 writes `payoutRequests` without naming `planVersionId`. The
       column is `NOT NULL` and `PayoutRequestInsert` does not carry it, so it is DERIVED from
       `accounts.plan_version_id` on the same transaction. An omission is a constraint violation on the
       request that approves a payout
exit=1

SEED 5  the eleven fields replaced by `...row`
FAIL   RI-34  ...  (1)
       apps/api/src/payout-backend.ts:337 SPREADS into the `payoutRequests` insert. A spread writes every
       field of `PayoutRequestInsert`, including `splitBp` and `clampReason`, which have NO COLUMN and are
       already stored inside `eligibility_snapshot`. Name the fields
exit=1
```

#### 6.2 The five sentinels, all five watched

**EVERY LEG BUT THE THIRD IS AN ABSENCE, AND AN ABSENCE OVER AN EMPTY SCOPE REPORTS PASS.** That is [ADR-274](ADR-274.md)'s rule and [ADR-279](ADR-279.md)'s whole subject, so each sentinel THROWS rather than returning a finding.

```
SENTINEL ONE   RI-34's own path filter seeded to `src` -> `source`
ERROR  RI-34  ...  (THIS CHECK DID NOT RUN)
       RI-34 found no source file under any `apps/*/src` or `packages/*/src`. Zero means the walk or the
       path filter has moved, at which point every leg is asserting about an empty list and a writer
       anywhere in the tree would report as absent
exit=3

SENTINEL TWO   the table key seeded to a constant, so no writer matches
ERROR  RI-34  ...  (THIS CHECK DID NOT RUN)
       RI-34 found no writer of `payoutRequests` under any `apps/*/src` or `packages/*/src`, on a tree
       whose migrations DO create `payout_requests`. Zero means the module was renamed, the table key
       spelling moved, or the write is no longer an `insert` call, at which point two of this check`s
       three legs are absences asserted over an empty scope and report PASS
exit=3
```

**SENTINELS THREE AND FOUR ARE SCHEMA FACTS AND WERE DRIVEN AGAINST A FIXTURE ROOT RATHER THAN AGAINST THIS TREE, ON PURPOSE.** Seeding them means editing `packages/db/migrations/**` or `packages/db/src/scope.ts`, and **migrations are sacred under constitution `E2` and are OUT of this row's fence**. `run(root)` takes its root as a parameter, so a four-file fixture in a scratch directory exercises them and no file in this repository is opened for writing:

```
SENTINEL THREE   a fixture migration grows `split_bp integer NOT NULL,`
ERROR  RI-34  (THIS CHECK DID NOT RUN)
       RI-34 found `split_bp` in packages/db/migrations, and this check asserts that no writer names it
       BECAUSE THE TABLE HAS NO SUCH COLUMN. If a migration added one, leg one is now asserting a schema
       fact that stopped being true and the check must be re-derived rather than left green. ADR-287
       finding F3 and ADR-295 are the reasoning

SENTINEL FOUR (a)   the fixture's `plan_version_id` loses `NOT NULL`
ERROR  RI-34  (THIS CHECK DID NOT RUN)
       RI-34 cannot find `plan_version_id uuid NOT NULL` in packages/db/migrations/0010_payouts.sql. Leg
       three requires every writer to derive that column BECAUSE it is `NOT NULL` and absent from the
       insert shape. If the column moved, the requirement moved with it

SENTINEL FOUR (b)   the fixture's registry stops calling `payoutRequests` owned on `identity_id`
ERROR  RI-34  (THIS CHECK DID NOT RUN)
       RI-34 cannot find `payoutRequests` registered `owned` on `identity_id` in packages/db/src/scope.ts.
       Leg two forbids a writer naming the tenancy column BECAUSE the handle stamps it. If the scope class
       changed, that is a money-path ruling and this check is asserting against a registry it no longer
       matches
```

#### 6.3 GREEN, on the restored tree

```
PASS   RI-34  Every writer of payout_requests derives plan_version_id and names no column-less field
32 of 32 invariants hold.
exit=0
```

`git diff --stat` reported the same figures for [`payout-backend.ts`](../../apps/api/src/payout-backend.ts) and [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) before the first seed and after the last restore.

#### 6.4 THE ONE ARM THAT PASSES OVER NOTHING, and the gate that found it

**THE FIRST VERSION OF THIS CHECK TOOK ITS SCHEMA SENTINELS UNCONDITIONALLY, AND [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts) WENT RED.** That suite requires every member of `CHECKS` to hold on a SYNTHETIC tree it builds, and that tree has no `0010_payouts.sql`, no payout schema and no writer. Sentinel four threw on it.

**THE REPAIR IS NOT A LOOSER SENTINEL. IT IS A PRECONDITION MOVED TO THE RIGHT FACT.** The check now asks the schema first: if `packages/db/migrations` carries no `CREATE TABLE payout_requests`, there is nothing here to be about, and it returns empty. **Every sentinel keeps its full force above that line**, and sentinel two gained the clause that makes it stronger rather than weaker: it fires on a tree whose schema DOES declare the table and whose source writes it nowhere, which is exactly the renamed-module case and is the only case it was ever for.

```
THE VACUOUS ARM   the fixture's `CREATE TABLE payout_requests` removed
PASS   RI-34
```

**AND IT CANNOT ARISE ON THIS REPOSITORY, WHICH IS WHY IT IS ADMISSIBLE RATHER THAN A HOLE.** A merged migration is never edited and only superseded (constitution `E2`), so the `CREATE TABLE` cannot leave this tree without an ADR, and that ADR is exactly the occasion to re-derive this check. **The alternative was to widen the fence into [`packages/tooling/test/`](../../packages/tooling/test/) and teach the synthetic fixture a payout schema, which this row does not reach and which would have been a fixture edited to pass a check rather than a check corrected.**

**THE SUITE IS THE REASON THIS IS IN THE ENTRY AT ALL.** Nothing else in the tree would have found it: `check:invariants` was green on the real tree at every point, and the defect was a check that could not run on a tree that legitimately lacked its subject.

#### 6.5 What it CANNOT see, stated rather than implied

- **UPDATES.** `planVersionId` is REQUIRED on an insert and FORBIDDEN on an update, where `assert_account_plan_version_pinned`'s sibling reasoning applies, so one id covering both would be two rules wearing one number. **No writer of either kind exists outside [`payout-backend.ts`](../../apps/api/src/payout-backend.ts) on this tree**, so an update leg would today be a leg over an empty scope, which is the shape [ADR-274](ADR-274.md) refuses.
- **A WRITE THAT IS NOT AN `insert` CALL.** A raw `sqlExecutor` write would be invisible; `ScopedTx` refuses one to a request handler, which is `packages/db`'s property and not this check's.
- **WHETHER THE VALUES ARE RIGHT.** It reads KEYS. That `approvedCents` carries the clamped number is `payouts.test.ts`'s and the engine's, and no text scan can be a proof about a value.

**`RI-32` AND `RI-35` STAY FREE.** `RI-32` was returned by [ADR-291](ADR-291.md) section 8 and `RI-35` by [ADR-294](ADR-294.md) section 6, and this session took only the number its own row named.

---

### 7. What this row did NOT do

- **NO `start.ts`.** Slice 9 is founder-gated and that file was never opened. **THE COUNTS DID NOT MOVE**: `{ declared: 24, wired: 10, blocked: 14 }`.
- **`subject()`, `holdFlag()` and `listPayouts()` are untouched and still refuse.**
- **THE HOLD BRANCH WAS NOT BUILT.** No clause id, no enum of clause names, no default string.
- **NO MIGRATION** was written, no number taken, and none reserved. `packages/db/migrations/**` was read and never opened for writing; the two schema sentinels were exercised against a fixture root for exactly that reason.
- **NO DOOR WIDENED.** `CATALOG_TABLE_KEYS` is still five, `PayoutTx` gains no member, and `ScopedTx` was not touched.
- **NO SYNTHESISED DEFAULT, no cast onto an unvalidated row, no fabricated id, no stub and NO FLOAT**, in source or in a fixture: every money value in the new cases is a `bigint` literal.
- **No secret, no credential, and Merit stays passwordless** ([ADR-039](ADR-039.md)).
- **NO GATE WAS WEAKENED AND NO FENCE WAS WIDENED.** The lock lines were left owed rather than taken (section 3).
- **`pnpm run verify` was NOT run**, forbidden by the row. `falsify.mjs` was run in the FOREGROUND and the tree was verified unmutated after it.

---

### 8. The files outside the stated fence, and why they moved

**NONE, except the two GENERATED registry spans.** [`docs/decisions/README.md`](README.md) and [`docs/sessions/README.md`](../sessions/README.md) are rewritten by `node scripts/corpus/gates.mjs generate` from each ADR's own first line and each log's `<!--index:` line, and `CI-06n` fails without them. **They change as a CONSEQUENCE of the fenced files rather than as an edit**, on [ADR-293](ADR-293.md) section 7's, [ADR-289](ADR-289.md) section 7's and [ADR-287](ADR-287.md) section 9's reading.

**SIX CITATIONS IN [ALLOCATION](ALLOCATION.md) AND ONE IN [`wiring.test.ts`](../../apps/api/test/wiring.test.ts) WERE REPOINTED, AND THIS DIFF IS WHAT MADE THEM STALE.** `RI-15` and `RI-16` caught all seven: deleting one line of `PayoutResponse` and rewriting one docblock moved `transact`, `listPayouts`, `idempotency`, `insertPayoutRequest`, `subject` and `snapshotOf` by six or eight lines each. **That is [ADR-212](ADR-212.md) on this file again**, and both files were already in this row's fence.

**[`docs/INDEX.md`](../INDEX.md) IS NOT TOUCHED.** An ADR is a registry entry and needs no INDEX row, and no document's status or title changes here.

---

### 9. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing. THE `E2` LINE-BY-LINE READ IS OWED AND HAS NOT HAPPENED.**

**THERE IS A MONEY-PATH DIFF TO READ THIS TIME, WHICH THE FOUR ENTRIES ABOVE THIS ONE DID NOT HAVE.** It is one insert into `payout_requests`, twelve named fields, and one derived read.

- **Decision (1):** **Slice 6's approval branch LANDED.** `insertPayoutRequest` writes an `approved` row, derives `plan_version_id` from `accounts` on the same transaction, names neither `splitBp` nor `clampReason` nor `identityId`, and returns nothing. [ADR-289](ADR-289.md)'s seven owed lines are all taken.
- **Decision (2):** **The hold branch REFUSES BY NAME**, `PayoutBackendUnwired('insertPayoutRequest.hold')`, on a two-sided predicate matching `payout_requests_hold_is_complete`. Slice 8 stays unscheduled and its supplier is counsel.
- **Decision (3):** **`RI-34` is MINTED**, four legs, five sentinels and one stated vacuous arm, every one of them seeded and restored GREEN.
- **Decision (4):** **[ADR-293](ADR-293.md)'s LOCK RULING IS INHERITED AND IS NOT BUILT.** Clauses 2 and 4 are lines of [`routes/payouts.ts`](../../apps/api/src/routes/payouts.ts) that this row's fence does not reach, clause 3 does not compile without clause 2, and the fence was not widened. Section 5A is the checklist and `usePayoutBackend`'s blocked reason now carries it.
- **Alternatives considered:** widening the fence by two lines to finish [ADR-293](ADR-293.md) section 4B (**refused**: the fence says "nothing else in that file", and a session that widens a fence to finish is the mechanism that made row `293`'s stop necessary); stopping entirely and reporting, as session 484 did (**refused**: 484 could not land its objective at all, and this row's objective IS buildable inside its fence, so stopping would leave slice 6 unbuilt over a residue that is a slice 9 precondition rather than a slice 6 blocker); taking the lock inside `transact` where no interface change is needed (**refused**: [ADR-293](ADR-293.md) section 3.4 refused it on the merits and this entry does not reopen a settled ruling to fit a fence); deleting the tautological assertion rather than inverting it (**refused**, 2.1: an absence assertion costs one line and makes the field unrestorable).
- **Consequences:** slice 6 is done, slice 8 is unchanged and unschedulable, slice 9 now has TWO preconditions rather than one, the second being section 5A. Nothing about the stored snapshot changes and nothing about the wired counts changes. `check:invariants` reports **32** rather than 31.

**WHAT A FOUNDER READ ADDS AND THIS ENTRY CANNOT.**

**THE FIRST QUESTION IS WHETHER LANDING A READ-THEN-WRITE AHEAD OF ITS RULED LOCK WAS RIGHT.** Section 3.3 argues it costs nothing today because no deployment reaches the code, and that the alternative was either widening a fence or leaving a buildable slice unbuilt. **The opposite judgement is defensible**: a money-path write that lands without the serialization its own ADR ruled is a thing a later session can install without ever reading section 3. **This entry's answer to that is to write the residue into `usePayoutBackend`'s blocked reason**, which is the one document the installing session cannot avoid, rather than into an ADR it might not open. A founder who disagrees reverts the body in one commit and the port refuses again.

**THE SECOND IS THE `insertPayoutRequest.hold` REFUSAL NAME.** It is a 503 on a request that is not retryable in any useful sense, because counsel has not drafted the clauses and no retry will change that. **A 500 would be the other honest answer** and would say "this deployment cannot do this" rather than "come back later". This entry chose 503 on the file's own stated rule that an unbuilt member is retryable; the arm is unreachable from `decidePayout` today, so the choice costs nothing yet and is cheap to overturn.

**AND ONE THING THIS ENTRY IS NOT A JUDGEMENT ABOUT.** Section 3.4 records the dispatch defect for the third time and refuses the corpus gate that would catch it, on [ADR-293](ADR-293.md) section 5's reasoning rather than on new reasoning. **Whether that gate is worth a row is a founder's call**, and it is now the only mechanism that has been named three times and built zero times.

- **Approval:** `___________________________` (UNSIGNED. **MONEY PATH, `E2` READ OWED AND NOT PERFORMED**, and the reads on [ADR-293](ADR-293.md), [ADR-291](ADR-291.md), [ADR-289](ADR-289.md), [ADR-287](ADR-287.md), [ADR-285](ADR-285.md), [ADR-283](ADR-283.md), [ADR-281](ADR-281.md), [ADR-268](ADR-268.md) and [ADR-176](ADR-176.md) are still owed under it. **Merging is not signing.**)
