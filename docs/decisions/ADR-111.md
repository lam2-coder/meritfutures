## ADR-111: two of the three homeless shapes get a contract row and the third gets a home it cannot be given here, because a banner that describes a session must arrive on the response that resolved it (2026-08-26, status: accepted)

**[Session 158](../sessions/2026-08-24-session-158.md) found three shapes the tree renders and [API_CONTRACT](../architecture/API_CONTRACT.md) does not declare: `required_factor`, `EconomicCalendarPanelResponse` and `ImpersonationSession`.** API_CONTRACT is `approved`, so a row is an ADR and not a commit ([C7](../../MERIT_BUILD_MASTER_PROMPT.md)), and [P4](../plans/P4-portal-and-site.md) section 8's `P4-h` row is where all three were sent. This entry rules each on its own evidence. **Two land as rows. The third lands as a declared shape whose carrier is an endpoint this slice does not hold**, and saying which is which is the content of the ruling.

- **Origin:** [P4](../plans/P4-portal-and-site.md) section 8's `P4-h` row, session **226**. The number was **reserved before dispatch** and this session chose nothing. `111` is a REUSE of a number reserved for session 225 and returned to the pool unused; [ALLOCATION](ALLOCATION.md)'s row is the record and [ADR-034](ADR-034.md)'s disposition is why the reuse is correct rather than a collision.

- **Not money.** No migration, no `SD-nn`, no schema change, no `packages/db` import, no dependency, no write path. Five read screens and three declarations.

---

### 1. What is here, measured on this tree rather than inherited

Every count below was produced by a command run in this session against this branch's base, `9244c2c`.

| Claim                                                    | How it was checked                                                                                                                                                                                                                                                                            | Result                                                                                                     |
| -------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| The calendar panel is in no contract                     | `grep -c economic_calendar docs/architecture/API_CONTRACT.md`                                                                                                                                                                                                                                 | **0**                                                                                                      |
| The impersonation session is in no contract              | `grep -ic impersonat docs/architecture/API_CONTRACT.md`                                                                                                                                                                                                                                       | **0**                                                                                                      |
| `required_factor` is in no contract                      | `grep -c required_factor docs/architecture/API_CONTRACT.md`                                                                                                                                                                                                                                   | **0**, against **1** prose hit for `required-factor`, which is section 12's column heading and not a field |
| Session 158's finding still holds                        | the three greps above, re-run rather than cited                                                                                                                                                                                                                                               | all three, unchanged in two days                                                                           |
| The wire field was handed here explicitly                | [ADR-093:166](ADR-093.md): _"It is `P4-h`'s, listed there, and moving auth's service does not move it"_                                                                                                                                                                                       | handed, by the branch that ships the field                                                                 |
| `#290` ships `required_factor` on the wire               | `apps/api/src/routes/auth.ts` on that branch: `REQUIRED_FACTORS` at `:154`, `required_factor?: RequiredFactor` on `ProblemDocument` at `:588`, `sendForbidden` writing it at `:622`                                                                                                           | six tokens, spelled as section 12 spells them, space included                                              |
| `#290` does NOT edit API_CONTRACT                        | `git diff --name-only` over that branch: six files, none of them `docs/architecture/API_CONTRACT.md`                                                                                                                                                                                          | **the field is shipped and undeclared**, which is clause 2's whole reason to exist                         |
| `GET /me` has no `impersonation` member on `#290`        | `project()` in `apps/api/src/routes/me.ts`, field by field                                                                                                                                                                                                                                    | eleven members, none of them this one                                                                      |
| The calendar's columns exist                             | [`0039:159`](../../packages/db/migrations/0039_economic_calendar.sql) `economic_calendar`, [`0039:104`](../../packages/db/migrations/0039_economic_calendar.sql) `economic_calendar_loads`, [`0039:276`](../../packages/db/migrations/0039_economic_calendar.sql) `economic_calendar_current` | present, and the portal's type already names them                                                          |
| The banner's columns exist                               | [`0042:57`](../../packages/db/migrations/0042_impersonation_sessions.sql) `admin_user_id`, `:65` `subject_identity_id`, `:76` `reason_code`, `:90` `reason_detail`, `:93` `expires_at`                                                                                                        | five, all `NOT NULL`                                                                                       |
| An impersonation token can never be a trader session row | `IMPERSONATION-C1` at [`0042:183`](../../packages/db/migrations/0042_impersonation_sessions.sql), refusing in both directions                                                                                                                                                                 | enforced by the database, which is clause 7's premise                                                      |

**`#290` and `#293` are UNLANDED and are cited unlinked throughout**, because [`CI-06a`](../testing/STRATEGY.md) fails on a link to an absent document.

---

### 2. THE RULING

> **1. `EconomicCalendarPanelResponse` ENTERS [API_CONTRACT](../architecture/API_CONTRACT.md) AS A NEW SECTION 6.1, `GET /economic-calendar`, `Auth: session`.** The shape is the one already in [`apps/portal/src/api/types.ts`](../../apps/portal/src/api/types.ts), unchanged in a single field, because that type was built column by column from `0039` against `DEP-M4-09` and this ruling has nothing to add to it. **The row is a transcription of the tree in the direction nobody usually runs it**, and the reconciliation `types.ts`'s own header asked for is therefore a no-op rather than a diff.
>
> **2. A NEW TOP-LEVEL SECTION IS REFUSED AND 6.1 IS WHY.** Section 12 is addressed by NUMBER: `CI-06k` reads it, [M04:502](../plans/M04-trader-portal.md) cites it as `API_CONTRACT §12`, and [ADR-039](ADR-039.md) amendment 4 is written against it. **Inserting a section renumbers every section after it and silently breaks every one of those citations**, which is the class of drift [ADR-034](ADR-034.md) exists to end, arrived at through a heading instead of through a count. Section 3.1 is the document's own precedent for a subsection carrying new content.
>
> **3. `Auth: session`, AND THE ARGUMENT IS THE DIRECTION OF THE MISTAKE.** Nothing in the panel is per-trader: the timezone conversion is a rendering (`GS-285`) and the table carries no identity column. **So a public row would work and is still refused.** Widening an authenticated route to public later is a decision somebody makes; narrowing a public route later is a break for every caller that found it. [ADR-110](ADR-110.md) added the corpus's one new public surface deliberately and argued it; this one has no such argument, and "it would also work public" is not one.
>
> **4. `required_factor` ENTERS SECTION 2 AS AN OPTIONAL MEMBER OF `Problem`, WITH SECTION 12's SIX-TOKEN VOCABULARY, ON A `403` AND NOWHERE ELSE.** Section 12 already requires that a `403` _"names the factor required so the client can offer it"_ and section 2's canonical code table is closed, so the code stays `forbidden` and the factor is an RFC 9457 extension member. **This is exactly what `#290` sends**, token for token including the space in `passkey or dual_channel`, so the row makes a shipped field declared rather than ruling against a branch in flight.
>
> **5. CLAUSE 4 DISCHARGES SECTION 12 AND DOES NOT DISCHARGE `DEP-M4-07` OR `INV-M4-14`, AND CONFLATING THEM WOULD CLOSE THE FINDING WITHOUT FIXING IT.** A field that arrives on a refusal cannot render a control disabled BEFORE the refusal. [M04 section 3.7](../plans/M04-trader-portal.md) is written against exactly that: _"A boundary that is only ever discovered by hitting it is indistinguishable from a bug, and users route around bugs."_ **The read-surface half is still missing after this ADR**, and section 4 states where it belongs and why this slice cannot put it there.
>
> **6. `ImpersonationSession` IS DECLARED AS A NAMED SHAPE IN A NEW SECTION 3.2, AND ITS CARRIER IS `Me.impersonation` ON `GET /me`, WHICH THIS SLICE DOES NOT WRITE.** The five fields are the five `NOT NULL` columns of `impersonation_sessions` and nothing else. **The section says in its own text that no endpoint returns it yet and names the one that will**, on [ADR-110](ADR-110.md) clause 7's precedent: a declared surface whose source is unwired is honest, and a declared surface pretending to be wired is not.
>
> **7. THE BANNER'S CARRIER MUST BE THE RESPONSE THAT RESOLVED THE SESSION, WHICH IS WHY A DEDICATED ENDPOINT IS REFUSED RATHER THAN CHOSEN AGAINST.** [M04 section 3.9](../plans/M04-trader-portal.md): the banner _"renders from the session the server resolved, never from client state"_. A second endpoint is a second resolution, and two resolutions can disagree by the width of one request. **That disagreement is `GS-301` exactly**: a session that reaches expiry mid-view _"whose next request is refused, on a page that still looks live"_. A banner assembled from a read other than the one that authorised the page IS client state, wearing a server's name.
>
> **8. NOTHING IN THIS ADR NAMES THE ADMIN DOMAIN AND `ADMIN_ORIGIN` STAYS A PLACEHOLDER.** [ADR-012](ADR-012.md) is unconditional and the shape does not strain it: `admin_user_id` is a `users` row and not an origin, and the banner carries no URL, because [FOLD-04](../plans/FOLD-04-impersonation-and-admin-parity.md)'s explicit exit is a control the shell renders and not a link the server sends.
>
> **9. THE FIVE SCREENS TAKE NO CONTRACT ROW AT ALL AND THAT IS THE MEASURED RESULT RATHER THAN A SCOPE DECISION.** `SC-M4-05`, `SC-M4-06`, `SC-M4-07`, `SC-M4-08` and `SC-M4-09` read `GET /plans/:planId/versions/:version`, `GET /purchases`, `GET /kyc/status`, `GET /accounts/:accountId/certificate` and `GET /affiliate/stats`, all five already declared. **They are transcription, which is what `P4-h` said they were**, and the one field they want that no row carries is section 5's, named in section 6 below and left there.

---

### 3. Clause 4 and 5: what `#290` declares, what it does not, and why the two halves are not the same field

**READ BEFORE THE ROW WAS WRITTEN, AS THE DISPATCH REQUIRED, AND THE TWO DO NOT CONTRADICT.** `#290` (unlanded) declares `required_factor` twice, in two different registers, and only one of them is a wire field.

| What `#290` declares                                       | Where                                                                   | Is it a wire field                                                                               |
| ---------------------------------------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| `requiredFactorTable(specs)`, keyed `METHOD /path`         | `apps/api/src/routes/auth.ts:889`, applied at `:1109` and `me.ts:126`   | **no.** A server-side table derived from the route array, read by a reviewer and by a later gate |
| `required_factor?: RequiredFactor` on the problem document | `apps/api/src/routes/auth.ts:588`, written by `sendForbidden` at `:622` | **yes**, on a `403`                                                                              |
| `RequiredFactor`, six tokens                               | `apps/api/src/routes/auth.ts:154`                                       | the vocabulary, and it is section 12's verbatim                                                  |

**Clause 4 declares the second row and asserts nothing about the first**, which is an implementation's own bookkeeping and belongs in the module that keeps it. **The contract gains the field the client can actually see.**

**WHAT `DEP-M4-07` ASKS FOR IS A THIRD THING, AND THE PLAN SAYS SO IN THE SENTENCE THAT NAMES THE FAILURE.**

> _"Every sensitive endpoint declares its required factor, and the session endpoint reports the session's own factor and elevation | ... | `INV-M4-14` has nothing to render, so the portal either guesses at the boundary or lets the trader hit it, and both are `FM-M4-10`. A declaration the client reads is the only version of this that does not become a second copy of C-27 maintained in the portal."_

Both halves of that sentence are load bearing and only one of them exists. **The session's own factor and elevation ARE reported**: `Me.session` carries `auth_factor`, `elevated` and `elevated_by_factor`, landed at [API_CONTRACT:139](../architecture/API_CONTRACT.md). **The per-action declaration on a read is not reported anywhere.** A `403`'s extension member is a declaration the client reads AFTER acting, and `INV-M4-14`'s component takes `required_factor` as a required prop it must hold BEFORE rendering.

**THE THREE WAYS TO CLOSE IT, AND WHY THIS SLICE TAKES NONE OF THEM.** A per-action map on `GET /me`; a `required_factor` on each sensitive endpoint's SUCCESS response; or a static table in the portal. **The third is refused outright by `DEP-M4-07`'s own last clause** and needs no session to rule it. The first two are both edits to endpoints in [API_CONTRACT](../architecture/API_CONTRACT.md) section 3 and section 6's write half, which are the auth and payout surfaces: `#290` holds section 3's implementation open, and [P4](../plans/P4-portal-and-site.md) section 6 puts auth _"in no phase's contents at all"_ while section 4.4 refuses to invent one for it. **A read-screen slice that added a field to `GET /me` would be writing a requirement into an approved contract for a branch that is in review and a phase that does not exist.** So the gap is named, priced, and left, which is section 6's first row.

---

### 4. Clause 6 and 7: why the banner's shape lands and its endpoint does not

**THE SHAPE IS NOT IN DOUBT AND HAS NOT BEEN SINCE `0042`.** [M04 section 3.9](../plans/M04-trader-portal.md) tabulates the banner field by field against columns, saying the quiet part outright: every field is a column _"rather than a string the portal composes"_. Session 158 found no contract row for it. `0042` has had all five columns since it landed. **The row that was missing was never a design question, which is why clause 6 can declare the shape without ruling anything `ADR-068` already ruled.**

**THE CARRIER IS THE ENTIRE RULING, AND THE THREE CANDIDATES ARE NOT EQUIVALENT.**

| Candidate                                      | What it costs                                                                                                                                                                                                        |
| ---------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Me.impersonation`, on `GET /me`               | **Correct, and not this slice's to write.** `GET /me` is _"the trader portal's first call on every page load"_ (`#290`'s own header), so the banner and the session it describes arrive together and cannot disagree |
| `GET /impersonation/session`, a dedicated read | **Reintroduces `GS-301`.** Two resolutions, one page. The banner can render from a session the page is no longer being served under, which is the precise failure the expiry is displayed to prevent                 |
| Client state held by the shell                 | Refused by section 3.9: _"A banner held in memory is gone on the first hard refresh, which is the ordinary way an operator works"_                                                                                   |

**So the honest outcome is a declared shape with a named carrier and no endpoint**, and the alternative was to invent an endpoint that is wrong for a reason the corpus already wrote down. **`IMPERSONATION-C1` is what makes the carrier interesting rather than obvious**: an impersonation token can never be the token a trader session carries, in both directions, enforced at [`0042:183`](../../packages/db/migrations/0042_impersonation_sessions.sql). So `GET /me` under an impersonation token is resolving something structurally different from `GET /me` under a trader token, and **that resolution is an auth-path ruling and not a view ruling.** It is stated here so the slice that takes it inherits the question rather than discovering it.

**THE PORTAL LOSES NOTHING TODAY.** [`impersonation-banner.ts`](../../apps/portal/src/shell/impersonation-banner.ts) already renders `ImpersonationSession` and [`app-shell.ts`](../../apps/portal/src/shell/app-shell.ts) already carries the band. The type they read is now a declared contract shape instead of an undeclared local one, which is the whole of what session 158 asked for on this row.

---

### 5. Clause 9: the five screens, and the one field they want that nobody has

**FOUR OF THE FIVE ARE PURE TRANSCRIPTION AND THE FIFTH IS ONE VIEW-LEVEL DECISION.**

| Screen                                 | Endpoint it reads                                      | Declared at                                                                                  |
| -------------------------------------- | ------------------------------------------------------ | -------------------------------------------------------------------------------------------- |
| `SC-M4-05` rules, per account          | `GET /plans/:planId/versions/:version`                 | [API_CONTRACT:251](../architecture/API_CONTRACT.md)                                          |
| `SC-M4-06` purchases and the rule diff | `GET /purchases`, plus two `PlanVersionResponse` reads | [API_CONTRACT:325](../architecture/API_CONTRACT.md), [:251](../architecture/API_CONTRACT.md) |
| `SC-M4-07` KYC status                  | `GET /kyc/status`                                      | [API_CONTRACT:517](../architecture/API_CONTRACT.md)                                          |
| `SC-M4-08` certificates                | `GET /accounts/:accountId/certificate`                 | [API_CONTRACT:496](../architecture/API_CONTRACT.md)                                          |
| `SC-M4-09` referrals                   | `GET /affiliate/stats`                                 | [API_CONTRACT:522](../architecture/API_CONTRACT.md)                                          |

**THE RULE DIFF IS RENDERED AND NEVER COMPUTED, AND `INV-M4-01` IS THE REASON THE DESIGN IS WORTH A CLAUSE.** A diff between two plan versions is naturally written as a delta: this cap rose by 50,000c, this gap fell by two days. **Every one of those deltas is a money value computed client side**, which `INV-M4-01` bans without qualification and which a lint over `_cents` and `_bp` arithmetic would catch only if somebody had written the lint. **So the diff renders BOTH sides and no difference**: `was` and `now`, each read straight off its own `PlanVersionResponse`, with a boolean saying they differ. The trader sees the two numbers the two contracts actually carry, which is also the more useful rendering, since _"it went up by 500"_ answers a question nobody asked while _"it was 3,000 and it is 3,500"_ is the fact the acknowledgement is about.

**THE FIELD NOBODY HAS IS `rule_diff_acknowledged_at`, AND IT IS NOT TAKEN HERE.** `SD-M4-02` added the column to `purchases` at [`0006:173`](../../packages/db/migrations/0006_commerce.sql) and `PurchaseListItem` does not expose it. **This ADR's subject is the three shapes and a fourth row would be reaching**, which the dispatch named as the failure mode to avoid. More decisively: the acknowledgement is a WRITE, `AS-M3-05` is [M03](../plans/M03-billing-checkout.md)'s, and a read slice that exposed the timestamp would be building half of somebody else's ceremony. Recorded in section 6 as a seam.

---

### 6. What this ADR does NOT rule, named rather than implied

| Left open                                                  | Whose it is                                                                                                                                                          | Why it is not taken here                                                                                                 |
| ---------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| **The read-surface half of `required_factor`**             | the auth surface. `#290` (unlanded) holds section 3's implementation; [P4](../plans/P4-portal-and-site.md) section 4.4 and section 10 put auth itself to the founder | Clause 5 and section 3. `DEP-M4-07` and `INV-M4-14` stay open, and this ADR says so rather than reporting three of three |
| **`Me.impersonation`, the carrier**                        | the same surface, for the same reason, plus the `IMPERSONATION-C1` resolution question section 4 names                                                               | Clause 6 and 7                                                                                                           |
| **`rule_diff_acknowledged_at` on the wire**                | [M03](../plans/M03-billing-checkout.md), whose `AS-M3-05` the acknowledgement serves                                                                                 | Section 5. A write, and a fourth contract row                                                                            |
| **`SC-M4-04`, `SC-M4-10`, `SC-M4-01`, `SC-M4-11`**         | [P4](../plans/P4-portal-and-site.md) section 6: the first two are P5's, the last two are money path and phaseless                                                    | Named in `P4-h`'s own cell as deliberately absent                                                                        |
| **Whether `KycStatus.state`'s vocabulary is four or five** | [M19](../plans/M19-kyc-identity.md), or a founder ruling                                                                                                             | Section 7                                                                                                                |
| **The framework**                                          | still open, still [session 158](../sessions/2026-08-24-session-158.md)'s item 1                                                                                      | Unmoved by this slice: these are view models, as the four before them are                                                |

---

### 7. One finding this slice could not avoid noticing

**[M04:86](../plans/M04-trader-portal.md) says `SC-M4-07` shows "four honest states". The database declares five.** `kyc_status` at [`0001:29`](../../packages/db/migrations/0001_extensions_and_enums.sql) is `('kyc_required', 'pending', 'verified', 'rejected', 'expired')`, and [API_CONTRACT:517](../architecture/API_CONTRACT.md) declares `KycStatus.state` as an open `string` rather than as either vocabulary.

**The screen is built against the FIVE**, because the enum is the value that will actually arrive and a view that handled four of five would render the fifth as nothing. `expired` and `rejected` are plainly different states with different things to do, so the likeliest reading is that "four" predates one of them. **That is a recommendation and not a ruling**: amending an `approved` plan needs its own ADR, this one's subject is the three shapes, and the view is correct under either reading because it handles every member the database can produce and refuses an unknown one loudly rather than rendering a blank.

---

### 8. The approval line, ADDED BY THE DISPATCH LOOP AND NOT BY THE AUTHOR

**THIS ENTRY LANDED WITHOUT AN APPROVAL LINE AND THAT IS RECORDED RATHER THAN QUIETLY REPAIRED.** Every other ADR in this corpus carries one, the dispatch asked for one by name, and [session 226](../sessions/2026-08-26-session-226.md)'s own log says _"approval UNSIGNED"_ while the entry stated no clause to be unsigned about. So at merge there was nothing to run and nothing to sign, and the section below is written by the loop that merged it rather than by the session that ruled it.

**THAT DISTINCTION MATTERS AND IS NOT CEREMONY.** An approval clause is worth something because the author commits to a falsifiable check BEFORE knowing whether it passes; a clause written afterwards by the reader is only a summary of what already holds. This one is therefore stated as **what was verified at merge**, and it claims nothing about what the author would have chosen.

```
# 1. All three shapes reach the contract, which is clauses 1, 4 and 6.
grep -c EconomicCalendarPanelResponse docs/architecture/API_CONTRACT.md   -> 1
grep -c ImpersonationSession          docs/architecture/API_CONTRACT.md   -> 1
grep -c required_factor               docs/architecture/API_CONTRACT.md   -> 2

# 2. TWO of the three are implemented and the THIRD DELIBERATELY IS NOT,
#    which is clause 5 rather than an omission: a field arriving on a
#    refusal cannot render a control disabled before the refusal, so
#    `required_factor` is a contract row here and no portal code.
grep -c EconomicCalendarPanelResponse apps/portal/src/api/types.ts        -> 2
grep -c ImpersonationSession          apps/portal/src/api/types.ts        -> 2
grep -rc 'required_factor|requiredFactor' apps/portal/src                 -> 0

# 3. Clause 2 held: the contract gained THREE sections and no new top-level
#    one, so section 12 is still addressed by its number.
git diff 9244c2c..HEAD -- docs/architecture/API_CONTRACT.md | grep -E '^\+#{2,4} '
  -> ### 3.2 The impersonation session
     ### 6.1 Dashboard panels
     #### GET /economic-calendar

# 4. Clause 8, and it is the one worth checking hardest: ADR-012 is
#    unconditional. The check is that the token is the ONLY spelling of the
#    admin origin anywhere in the entry, never that it appears once.
grep -c ADMIN_ORIGIN docs/decisions/ADR-111.md                            -> 2
#    Both occurrences are the placeholder token: clause 8's own sentence,
#    and this line. NEITHER IS A DOMAIN.
grep -cE '[a-z0-9-]+\.(com|net|io|co|app|dev)' docs/decisions/ADR-111.md  -> 0
#
#    THIS CLAUSE WAS WRONG ON ITS FIRST DRAFT AND THE ERROR IS LEFT VISIBLE
#    because it is the exact failure mode this corpus keeps recording. It
#    first read `-> 1`, which was true of the entry BEFORE this section was
#    appended and false the moment the section naming the token existed. A
#    clause that counts occurrences of a token in the file that contains the
#    clause is self-referential and drifts on its own next edit. The count is
#    kept for the record and the domain grep is what actually holds the line.

# 5. The tree carrying it.
pnpm exec vitest run apps/portal   -> 17 files, 132 passed
pnpm run verify                    -> exit 0
gates                              -> 30 of 30      invariants -> 9 of 9
```

**SIGNED ON DELEGATED AUTHORITY, 2026-08-26, `status: accepted`. NOT MONEY PATH**, so no `E2` read is owed: five read screens, three contract shapes, no migration, no `packages/db` import and no dependency.

**THE ONE THING THIS SIGNATURE DOES NOT COVER** is clause 7's refusal, which is a judgment rather than a measurement: the banner gets a shape and no endpoint, because its carrier must be the response that resolved the session. Nothing in the tree can falsify that today, since `GET /me` is written and `Me.impersonation` is not populated. It is recorded here as the clause a later slice can prove or overturn.
