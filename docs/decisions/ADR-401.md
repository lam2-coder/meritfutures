## ADR-401: the uniqueness condition on `G1` is REFUSED as a gate at 442 of 495 and TAKEN as a condition on the repair at 80; it is NOT the same seam as `ADR-398`'s sub-token defect, which reaches 0 of the named population; the dated census under `git log -S` is 74 repairable against 39 born wrong; and `git log -S` does not name a commit until it names a traversal (2026-09-07, status: proposed)

**TWO AMENDMENTS TO [ADR-388](ADR-388.md) WERE RECORDED AS NEEDING A ROW RATHER THAN A PARAGRAPH, AND THE DISPATCH ASKED WHETHER THEY ARE ONE SEAM OR TWO BEFORE EITHER IS RULED ON.** They are two. **[ADR-399](ADR-399.md) section 6 is about the arity of REFERENCE**, one name with many referents in the cited file; **[ADR-398](ADR-398.md) section 5 is about the arity of ASSERTION**, one token carrying two claims whose reference times differ. Their remedies are disjoint, they co-fire on nothing, and [ADR-382](ADR-382.md)'s pattern is not what this is.

**RULED, AND THE FIRST HALF IS A REFUSAL WITH A MEASUREMENT.** [ADR-399](ADR-399.md) recorded that `G1` should require a token UNIQUE in the cited file, and recorded that it *"sorts nothing differently"*. **It sorts 442 of 495.** Taken as written it would shrink the decidable population from 493 to **53** and convert **42 correct born-wrong verdicts into undecidable**, which loses true information rather than sharpening it. **The observation behind it is real and it binds the REPAIR rather than the GATE**, where it reaches **80** pointers, mints no cell, and leaves [ADR-397](ADR-397.md)'s three-way partition exactly as it stands.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `401`, branch `claude/g1-below-the-token`, session **596**, off the wave integration branch `claude/wave-398s` at `28e2b0ee` rather than off `main`.
- **CONTROL OVER THE CORPUS'S HONESTY. Ships `proposed` with an UNSIGNED approval line. Merging is not signing. `E2` read owed.**
- **THE DELIVERABLE IS THREE RULINGS, ONE RE-RUN CENSUS, ONE PRICED HOLE AND FOUR SEEDS.** No invariant number was minted, no gate was added, widened or weakened, no register grew, no test was skipped, and no file outside this row's fence was written.
- **EVERY FIGURE THE DISPATCH CARRIED WAS RE-DERIVED AND THREE DO NOT HOLD**: the container's shallowness, the 4,301, and *"changes no verdict"*. Section 2 and section 8.

---

### 1. The objective

Take the two amendments to [ADR-388](ADR-388.md) that [ADR-399](ADR-399.md) section 6 and [ADR-398](ADR-398.md) section 5 each recorded as owed, **reconcile them before ruling on either**, and re-run [ADR-388](ADR-388.md)'s census of named pointers in dated records under the instrument [ADR-396](ADR-396.md) section 7 ruled correct. One objective, non-money, control over the corpus's honesty.

---

### 2. The container, measured rather than inherited, and the dispatch is wrong about it in both directions

**THE CONTAINER ARRIVED WITH NO REPOSITORY, FOR THE FIFTH CONSECUTIVE WAVE.** The stated working directory was empty, `git rev-parse` returned *fatal: not a git repository*, and no checkout of this repository existed on the container. The tree was attached to this session's authorized set and cloned by hand.

**AND THE CLONE IS FULL, WHICH WAS RUN AND REPORTED RATHER THAN ASSUMED IN EITHER DIRECTION.**

| | |
|---|---|
| `git rev-parse --is-shallow-repository` | **`false`** |
| `git rev-list --count HEAD` on the default branch | **4,320** |
| What the dispatch stated about this container | *"My container says `true`"* |
| What the dispatch stated the count was | 4,301, which is [ADR-396](ADR-396.md)'s figure from three waves ago |

**BOTH HALVES OF THE ENVIRONMENT SENTENCE ARE FALSE FOR THIS CONTAINER AND THE FIRST HALF IS THE ONE THAT MATTERS.** [ADR-397](ADR-397.md) measured `true` on its own container and [ADR-396](ADR-396.md), [ADR-398](ADR-398.md), [ADR-399](ADR-399.md) and [ADR-400](ADR-400.md) each measured `false` on theirs. **That is four consecutive rows measuring a full clone against one older row's `true`, and the dispatch is still handing the `true` forward.** The standing advice to deepen costs nothing when it is unnecessary; the standing CLAIM that a clone is shallow has now been wrong five times running. **This is [ADR-400](ADR-400.md) section 17 item 5 repeated because nothing about it has changed, and it is the dispatcher's to fix.**

**THE BASE WAS REPRODUCED ON `28e2b0ee` BEFORE THE FIRST EDIT**, on a tree `git status --porcelain` reported EMPTY: suite **336 files / 8,109 passed / 10 skipped / 0 failed**, gates **33 of 33**, invariants **35 of 35**, `typecheck`, `lint` and `format:check` all exit 0.

**`pnpm run verify` was NOT run and [`falsify.mjs`](../../scripts/corpus/falsify.mjs) was NOT run.** Both are forbidden to this row and both mutate the working tree. Every leg was invoked directly.

---

### 3. The reader, and the population, with the two pointers that are not findings named before they are counted

**THE CHECKS' OWN READER WAS RELOCATED OUTSIDE THE REPOSITORY WITH ITS `HERE` CONSTANT PINNED TO THE REAL CHECKS DIRECTORY**, which is [ADR-400](ADR-400.md)'s method rather than [ADR-388](ADR-388.md)'s and is taken here **by fence rather than by choice**: `packages/tooling/**` is row `402`'s this wave and was never opened for writing. `git status --porcelain` was EMPTY at every moment before the first edit.

**THE COPY WAS VALIDATED BEFORE ITS READER WAS BELIEVED.** Run through it, **34 of 35** checks reproduce their live verdict and the one that does not is **`RI-18`**, which resolves a dependency against its own module location in a way the relocation defeats. **That is [ADR-397](ADR-397.md)'s and [ADR-400](ADR-400.md)'s result reproduced, the same check and the same cause, on a third base.**

| Scope, through each check's own input | Citations | Carry a name the binder binds |
|---|---|---|
| `docs/` under a dated or session heading, out of `RI-16` by shape | 7,676 | **292** |
| `docs/` live, `RI-16`'s scope | 1,099 | **73** |
| Source files, `RI-15`'s scope | 393 | **130** |
| **READ BY THE CHECKS' READER** | **9,168** | **495** |

**THE TWO EXTRA DATED POINTERS ARE [ADR-400](ADR-400.md)'s OWN DECLARED SPECIMENS AND THAT WAS DERIVED BY SET DIFFERENCE RATHER THAN ASSUMED.** That entry's section 12 contributes *"two named citations that are wrong on purpose, at its own lines 266 and 270"*, and says a later census should not count them as findings. **Differenced against the same reader run at `c219c0d1`, the population gains exactly those two and loses nothing.**

| | |
|---|---|
| Named dated-record pointers on `c219c0d1` | **290** |
| On `28e2b0ee` | **292** |
| Added | [ADR-400](ADR-400.md) line 266 and line 270, both `mintCertificateCode` |
| Removed | **none** |

**SO THE FINDINGS-ELIGIBLE POPULATION IS 290 DATED AND 493 IN ALL**, and every figure below excludes the two specimens by name. **[ADR-397](ADR-397.md)'s and [ADR-400](ADR-400.md)'s 290 / 73 / 130 and 493 reproduce exactly on a third base**, which makes that census the most confirmed measurement in this corpus at four independent derivations.

---

### 4. The census re-run under `git log -S`, which is what [ADR-396](ADR-396.md) section 7 asked for at this grain and nobody had taken

**[ADR-400](ADR-400.md) SECTION 7 RAN THE CORRECTED INSTRUMENT OVER ALL 493 AND PUBLISHED ONE CORPUS-WIDE ROW.** It never broke out the dated records, and **the dated records are the population [ADR-388](ADR-388.md)'s ruling is about**: its headline split of 76 repairable against 42 born wrong is a dated-record figure, and section 7's totals cannot be read back to it. **The dispatch's premise that the census has never been re-run is therefore half true, and the half that was missing is the half that decides the ruling.**

| Verdict, over the 290 findings-eligible named pointers in dated records | `git blame -w` | `git log -S`, oldest |
|---|---|---|
| True then, true now. Nothing owed | 169 | **168** |
| **Outcome ONE: DRIFTED after being true, REPAIRABLE** | **75** | **74** |
| **Outcome TWO: FALSE ON THE DAY IT WAS WRITTEN** | **43** | **39** |
| False then, true now, by coincidence | 2 | 2 |
| **Undecidable** | **1** | **7** |

**THE HEADLINE MOVES FROM 76 / 42 TO 75 / 43 TO 74 / 39, AND THE THIRD STEP IS THE INSTRUMENT RATHER THAN THE TREE.** [ADR-388](ADR-388.md) measured 76 / 42 on `8fa66104`, [ADR-397](ADR-397.md) reproduced it exactly, [ADR-400](ADR-400.md) measured 75 / 43 on `c219c0d1` and named the one pointer that moved. **All fifteen cells of that entry's `git blame -w` table reproduce here figure for figure**, across all three scopes, which is what licenses reading the second column as an instrument effect rather than as a harness difference.

**EIGHT OF THE 290 FLIP, AND THEY ARE ENUMERATED RATHER THAN COUNTED.**

| Citing site | Token | Subject | `git blame -w` | `git log -S` |
|---|---|---|---|---|
| [`STATE.md`](../STATE.md) line 10100 | `routes/affiliate.ts:444` | `STATEMENTS_UNREACHABLE` | TWO, born wrong | **ONE, repairable** |
| [session 478](../sessions/2026-08-30-session-478.md) line 9 | `scope.ts:840` | `root` | TWO, born wrong | **ONE, repairable** |
| [`STATE.md`](../STATE.md) line 5641 | `wallet-withdrawals.ts:990` | `INSERT` | TWO, born wrong | undecidable |
| [session 362](../sessions/2026-08-28-session-362.md) line 118 | `repo-invariants.mjs:359` | `DEPLOYABLES` | TWO, born wrong | undecidable |
| [`STATE.md`](../STATE.md) line 7540 | `accountDetailLicence:1433` | `accountDetailLicence` | ONE, repairable | undecidable |
| [session 359](../sessions/2026-08-28-session-359.md) lines 9 and 42 | `accountDetailLicence:1433` | `accountDetailLicence` | ONE, repairable | undecidable |
| [ADR-213](ADR-213.md) line 118 | `routes/payouts.ts:332` | `plan` | nothing owed | undecidable |

**THE SECOND ROW IS [ADR-400](ADR-400.md) SECTION 8's OWN POINTER, REPRODUCED INDEPENDENTLY, AND THE FIRST ROW IS A SECOND INSTANCE OF THAT CLASS THAT ENTRY DID NOT HAVE.** It found one pointer whose class inverted under a sibling's cosmetic edit and said plainly that a single instance cannot say how often it happens. **There are two, and both are pointers `blame` condemns as manufactured evidence that the corrected instrument calls cleanly repairable.**

**AND SIX OF THE EIGHT FLIP INTO UNDECIDABLE, WHICH IS THE COST OF THE BETTER INSTRUMENT STATED AS A COST.** [ADR-400](ADR-400.md) wrote that *"a gate cannot ship on an instrument that refuses fourteen times more often than the one it replaces without a rule for what a refusal means"*. **In dated records the ratio is seven to one and there is still no such rule.** Nothing here proposes one.

---

### 5. THE FINDING: `git log -S` does not name a commit until it names a traversal, and the spread is larger than the disagreement it was adopted to fix

**[ADR-396](ADR-396.md) SECTION 7 RULED THAT `G2` IS DERIVED *"at the earliest commit that introduced the cited coordinate into the citing file, by `git log -S` over the coordinate token, taking the oldest result"*.** That sentence reads like a specification. **It is not one.** Run at [ADR-400](ADR-400.md)'s own base over its own 493, four readings of it, each defensible, disagree:

| Reading of the rule | Pointers with no introducing commit | Pointers where it names a DIFFERENT commit than the default reading |
|---|---|---|
| The token as the file writes it, default traversal | 8 | **0**, this is the leg every figure above uses |
| The same, `--full-history` | 8 | **3** |
| The same, `--first-parent` | **0** | **493, every one** |
| The token as the reader NORMALISES it | **63** | **56** |

**THE THIRD ROW IS THE ONE THAT MATTERS AND IT IS NOT A CORNER CASE.** On a history whose merges are pull requests, a first-parent walk attributes every introduction to the MERGE commit rather than to the commit that wrote the line, and it never refuses. **[ADR-388](ADR-388.md) section 3 buckets its own growth figures *"against merge pull requests on this branch's first parent"***, so a reader coming to [ADR-396](ADR-396.md)'s rule from that entry has been told which walk this corpus counts on. **Two rows in this corpus would read the same rule and produce a different commit for all 493 pointers.**

**AND THE FOURTH ROW IS THE MISTAKE THIS ROW MADE FIRST AND CAUGHT BY ARITHMETIC RATHER THAN BY CARE.** A first pass searched the reader's `cited` field, which is synthesised as target plus coordinate and is not the string in the file wherever the pointer sits inside a markdown link. It returned **42 undecidables in the dated records alone**, more than the 14 [ADR-400](ADR-400.md) reports for the entire corpus. **A subset cannot exceed its superset, which is what said the leg was wrong.** The rule as written does not say which string to search either.

**THIS ROW'S `git log -S` FIGURES DISAGREE WITH [ADR-400](ADR-400.md) SECTION 7's PUBLISHED ROW, MEASURED AT THAT ENTRY'S OWN BASE SO THAT BASE MOVEMENT CANNOT EXPLAIN IT.**

| Over 493, `git log -S` | [ADR-400](ADR-400.md) section 7 | This row, at `c219c0d1` |
|---|---|---|
| Nothing owed | 342 | **346** |
| Outcome ONE | 83 | **85** |
| Outcome TWO, born wrong | **46** | **46** |
| False then, true now | 8 | **7** |
| Undecidable | 14 | **9** |
| Different commit from `blame` | 57 of 493 | **66 of 493** |
| Different verdict from `blame` | 23 of 493 | **17 of 493** |

**THIS IS REPORTED AS AN INSTRUMENT DISAGREEMENT AND NOT AS THAT ENTRY BEING WRONG, BECAUSE THE RULE DOES NOT PIN DOWN ENOUGH FOR EITHER OF US TO BE RIGHT.** The evidence that the harness here is sound is that its `git blame -w` leg reproduces that entry's fifteen cells exactly at both bases, and its `git log -S` leg is byte-stable across the two bases while that entry's is not reproducible from its own description. **The honest conclusion is [ADR-400](ADR-400.md)'s own, strengthened: the gate problem is not a tuning problem, and it is now not even an instrument problem. It is that the instrument has no definition.**

**RULED: A ROW DERIVING `G2` BY `git log -S` STATES ITS TRAVERSAL AND ITS SEARCH STRING, OR ITS FIGURE IS NOT REPRODUCIBLE.** That is not an amendment to [ADR-396](ADR-396.md)'s choice of instrument, which stands and which section 4 confirms is the better one. **It is the observation that a rule naming a command is not the same thing as a rule naming a measurement**, and it costs one clause.

---

### 6. The hole [ADR-400](ADR-400.md) section 17 item 2 left unpriced, priced, and half of it retires

**THAT ITEM NAMES TWO KNOWN HOLES IN THE CORRECTED INSTRUMENT AND SAYS NEITHER IS RULED OUT OVER THE 493.** Both are priced here over all 495 pointers, specimens included, because a specimen is as good as a finding for measuring an instrument.

**HOLE ONE: A COORDINATE APPEARING TWICE IN ONE FILE ATTRIBUTES BOTH TO THE FIRST INTRODUCTION.**

| | |
|---|---|
| Distinct (citing file, token) pairs carrying MORE THAN ONE site | **25** |
| Pointers sitting on such a pair | **55 of 495**, 11.1 percent |
| Pairs on which `git blame -w` names more than one commit across the sites | **4** |
| **Pairs on which the two instruments reach a DIFFERENT VERDICT because of it** | **ZERO** |

**THE HOLE IS REAL IN SHAPE AND COSTS NOTHING ON THIS POPULATION, AND THE REASON IS A PROPERTY OF HOW THIS CORPUS WRITES RATHER THAN OF `git`.** On 21 of the 25 pairs `blame` itself names one commit for every site, because the sites were written in one diff: a session log's `<!--index:` summary and the body sentence it copies, or a table cell quoted twice inside one entry. **On the four where `blame` does disagree, all sites already share a verdict, so collapsing them changes no answer.** **ITEM 2's FIRST HALF IS RETIRED, and the retirement is a real result rather than an absence of one.**

**HOLE TWO: A CITATION WRITTEN, DELETED AND REWRITTEN ATTRIBUTES THE SURVIVING LINE TO THE EARLIEST.** This one does not retire.

| | |
|---|---|
| Pointers whose token has MORE THAN ONE `-S` commit over its citing file | **23 of 495** |
| Of those, pointers on which the two instruments disagree on the verdict | **8** |
| Of those 8, in dated records | **2**, including [`STATE.md`](../STATE.md) line 10100 from section 4 |

**SO THE SECOND HOLE IS NOT A THEORETICAL ONE: 8 of the 17 corpus-wide verdict disagreements sit on a token that churned**, and the instrument's rule for which of the several introductions counts is *"the oldest"*, which is exactly the wrong answer where a coordinate was deleted and a different sentence later reused the same number. **It is priced at 23 and handed on in section 11.**

---

### 7. RECONCILED: the two amendments are two seams, and the test is that their remedies do not reach each other's instances

**THE DISPATCH ASKED THIS FIRST AND IT IS ASKED FIRST HERE, BECAUSE [ADR-385](ADR-385.md) MEASURED WHAT IT COSTS TO RULE TWICE ON ONE THING.** Two conditions, each stated as its own entry states it:

- **`U`, from [ADR-399](ADR-399.md) section 6.** The bound subject name occurs MORE THAN ONCE in the cited file at the commit that wrote the citing line. *"A token that occurs five times in the cited file is a token a later reader can search for and cannot resolve."*
- **`M`, from [ADR-398](ADR-398.md) section 5.** The citation sits beside a second, independently falsifiable claim in the same sentence, so that a verdict on one half silently disposes of the other. *"The defect lives at sub-token granularity and the control operates at token granularity."*

| Over the 495 named pointers | |
|---|---|
| `U` fires | **442**, 89.3 percent |
| `M` fires | **1**, and that one is a false positive of this row's own regex on `Promise` beside a coordinate in [ALLOCATION](ALLOCATION.md) |
| Both fire | **1**, the same false positive |
| **Real instances on which both fire** | **ZERO** |

**THEY ARE NOT ONE SEAM AND THE STRUCTURAL ARGUMENT AGREES WITH THE COUNT.** [ADR-388](ADR-388.md) models a citation as a NAME plus a COORDINATE. **`U` is a defect in the name's resolving power**: the map from name to line is not injective, and the failure is about REFERENCE. **`M` is a defect in the unit of classification**: the sentence asserts more than the pair, and the extra assertion has a different reference TIME, present tense for the coordinate and past tense for the cardinal. **The remedies do not reach each other's instances.** Requiring a unique name does nothing for `` `gates.mjs:8579` `` beside `sites: 7`, whose subject is already unique; separating the claims and dating the tensed one does nothing for a pointer whose subject occurs 130 times.

**AND `M`'s POPULATION IS ENUMERABLE RATHER THAN STATISTICAL, WHICH IS WHY IT NEEDS NO RULING HERE.** Counting lines that carry both a coordinate citation and a `` `sites: N` `` register restatement: **8 sites at [ADR-398](ADR-398.md)'s base `c5648f00`, and 6 on this one**, the difference being that entry's own repair of three. **The surviving unrepaired instance is [ADR-337](ADR-337.md) line 202, which [ADR-398](ADR-398.md) named as outside its fence and which is row `403`'s file this wave.** It contributes **0** named pointers to the 495, because its coordinate is written inside a markdown link whose token carries no symbol, which is the shape [ADR-400](ADR-400.md) section 12's matrix row six refuses.

**SO BOTH AMENDMENTS ARE ABOUT A POPULATION NO CHECK CAN SEE, AND THAT IS THE DEEPEST THING THE RECONCILIATION FINDS.** `M`'s live instance is invisible to the binder. **`U`'s motivating instance is too**: the four `` `CRON_INVENTORY:35` `` pointers [ADR-399](ADR-399.md) built the condition on contribute **0** of the 495, because a bare nickname with no path and no link is not a citation to any check in this repository. **[ADR-388](ADR-388.md) section 1 measured that form at 1,403 pointers and called it the largest reason its own figure is a floor.** Two rows have now proposed amendments to a gate, each on an instance the gate's own reader cannot read.

---

### 8. RULED on `U`: refused as a strengthening of `G1`, taken as a condition on the repair, and [ADR-399](ADR-399.md)'s own assessment of it is refuted

**THAT ENTRY RECORDED THE CONDITION AND DECLINED TO TAKE IT, ON THE GROUND THAT IT *"sorts nothing differently"* and *"moves zero verdicts"*.** Both sentences are about this corpus and both were written without measuring it, because [ADR-388](ADR-388.md) was another row's file that wave. **Measured here for the first time:**

| Priced over the 495 named pointers | |
|---|---|
| `G1` as written binds a name | **495** |
| `G1` strengthened to require a token UNIQUE in the cited file | **53** |
| **Moved from decidable to UNDECIDABLE** | **442** |

| What the 442 are classed as today | |
|---|---|
| Nothing owed | 318 |
| **Outcome ONE, REPAIRABLE** | **80** |
| **Outcome TWO, BORN WRONG** | **42** |
| False then, true now | 2 |

**THE 42 IS THE REFUSAL IN ONE NUMBER.** A stricter `G1` does not reclassify a born-wrong pointer as something better; **it stops the corpus being able to say the pointer was born wrong at all**, because `G1` failing means undecidable and undecidable means no reader can tell drift from a false record. **Forty-two verdicts this corpus currently derives correctly would become forty-two shrugs**, and the argument [ADR-388](ADR-388.md) rests its whole ruling on, that repairing without separating the two fabricates a verification more than a third of the time, would lose the population it was measured over. **A gate that converts a right answer into no answer is not a sharper gate.**

**AND THE OBSERVATION BEHIND IT IS STILL RIGHT, WHICH IS WHY IT IS TAKEN SOMEWHERE ELSE.** [ADR-388](ADR-388.md)'s outcome one permits a repair in which *"only the coordinate moves"*. **Where the named subject occurs many times in the cited file, that sentence does not say which coordinate**, and the repairer is doing exactly what [ADR-377](ADR-377.md) section 8 was punished for: guessing about somebody else's sentence. **Quantified at the reader rather than argued:**

| A pointer naming this subject in this file | Occurrences | Coordinates in the file that PASS the checks' own window test |
|---|---|---|
| `firm` in [`scope.ts`](../../packages/db/src/scope.ts) | 130 | **464 of 1,659, 28.0 percent** |
| `derived` in the same file | 81 | 390 of 1,659, 23.5 percent |
| `ledger` in [`checkout.ts`](../../apps/api/src/routes/checkout.ts) | 39 | 153 of 2,810, 5.4 percent |

**MORE THAN ONE WRONG COORDINATE IN FOUR IS ACCEPTED FOR THE MOST REPEATED SUBJECT IN THE MOST CITED FILE IN THIS CORPUS**, so a repairer who lands anywhere in that band gets a green run and a wrong pointer.

**RULED: `G1` AND `G2` ARE UNCHANGED AND NO CELL IS MINTED. THE CONDITION ATTACHES TO OUTCOME ONE'S REMEDY.** Where a repair is permitted and the named subject occurs more than once in the cited file, **the repair NAMES which occurrence it landed on**, in the same sentence, beside the retired coordinate that [ADR-388](ADR-388.md) section 4 already requires. It is a sentence a person writes, not a predicate a runner evaluates, which is the same footing [ADR-400](ADR-400.md) section 12 put `G1` itself on. **It reaches the 80 pointers this corpus currently calls repairable and whose subject is not unique**, and it moves **no** pointer between classes, so [ADR-397](ADR-397.md) section 4's three-cell partition and its *"no fourth cell"* both stand untouched.

**ONE FIGURE IN [ADR-399](ADR-399.md) IS ALSO NAMED WRONG WITHOUT BEING REPRODUCED, PER [ADR-367](ADR-367.md).** Its section 6 table reports the occurrences of the house phrase its four `:35` pointers name as **5, then and now**. **It is 5 at both write commits, `a21bfb8e` and `4b3e8e57`, exactly as recorded, and it was already 6 in the runbook at `c219c0d1`, that row's own base.** The verdict is untouched, because `G2` is decided on the *then* half alone. **The error is the class this corpus keeps finding: a property derived at the record's day and reported as also true of the tree**, which is [ADR-388](ADR-388.md) section 11's own sentence pointing the other way. It is annotated at the site and the figure is left standing.

---

### 9. RULED on the annotation convention's scope, which [ADR-400](ADR-400.md) section 17 item 3 left open with an instance

**[ADR-397](ADR-397.md) SECTION 7 RULED THAT A WRONG FIGURE IN A DATED RECORD IS ANNOTATED AT THE SITE AND NEVER OVERWRITTEN, ON THE GROUND THAT THE CLAUSE CLAIMED THE FIGURE WAS DERIVED THERE.** [ADR-400](ADR-400.md) then annotated a sentence that claims no derivation, recorded that it had done so, and recorded that whether the convention reaches non-numeric claims *"is not this row's to rule and it is now a live question with an instance"*.

**IT BITES THIS ROW TWICE AND IS THEREFORE RULED.** [ADR-399](ADR-399.md)'s *"sorts nothing differently"* is not a figure; it is a claim about a measurement that was never taken. **RULED: THE CONVENTION REACHES ANY WRONG CLAIM IN A DATED RECORD AND NOT ONLY A NUMERIC ONE.**

- **A sentence that claims its own derivation is annotated for [ADR-397](ADR-397.md)'s reason**: overwriting it would record a derivation that never happened.
- **A sentence that states only a consequence is annotated for a different reason**: a reader cannot tell the two apart from the sentence, so a corpus that overwrites the second must first prove the sentence is of the second kind, at every site, forever.
- **The two reasons differ and the practice does not**, so the distinction is worth stating and is **not worth a cell**. [ADR-400](ADR-400.md)'s instinct was right and its reason was one of the two available.

**THAT DISCHARGES ITEM 3 AND IT IS APPLIED IN THIS DIFF FOUR TIMES**, in [ADR-388](ADR-388.md), [ADR-396](ADR-396.md), [ADR-397](ADR-397.md) and [ADR-399](ADR-399.md), each original sentence kept whole and what is retired NAMED rather than reproduced.

---

### 10. Falsification: four seeds, and the pair that carries the ruling is A against C

**ALL FOUR SEEDS LANDED IN ONE LIVE DOCUMENT, [`FOLD-01-phone-identity.md`](../plans/FOLD-01-phone-identity.md), INSIDE `RI-16`'s SCOPE. EVERY ONE WAS RESTORED BYTE-IDENTICAL AND VERIFIED WITH `sha256sum -c` AGAINST A MANIFEST TAKEN BEFORE THE FIRST SEED AND RE-CHECKED AFTER EACH RESTORE. NO RESTORE USED `git checkout --`, AND `git status --porcelain` NAMED ONLY THIS ROW'S OWN FILES AFTER EVERY ONE.**

| # | Seed | Watched | What it establishes |
|---|---|---|---|
| A | `` `firm` at `scope.ts:1135` ``, a WRONG coordinate that happens to sit within the window of one of the name's 130 occurrences | **GREEN, 35 of 35** | The check accepts a wrong pointer whenever the subject is repeated. **This is `U` made visible** |
| B | the same name at `:1`, outside every occurrence window | **RED, 34 of 35**, naming the site and *"9 lines away"* | The check is ARMED on that file and that name, so A is blindness rather than a dead gate |
| C | `` `TREASURY` at `scope.ts:900` ``, a name UNIQUE in the cited file, wrong by the same 9 lines | **RED, 34 of 35** | **The identical wrongness, caught, and the only difference is uniqueness** |
| D | seed B verbatim with two ordinary words between the name and the pointer | **GREEN, 35 of 35**, `name` binds `null` | [ADR-400](ADR-400.md) section 12's matrix rows two and three, reproduced live |

**A AGAINST C IS THE WHOLE RULING IN ONE PAIR.** Two pointers, the same file, the same distance wrong, one caught and one not, and the only thing separating them is whether the subject occurs once. **That is the argument FOR [ADR-399](ADR-399.md)'s observation, taken as far as it goes**, and section 8 is the measurement of what it would cost to make it a gate.

**SEED D IS RECORDED BECAUSE IT COST THIS ROW A FALSE GREEN AND WAS CAUGHT BY DIAGNOSIS RATHER THAN BY CARE.** The first three seeds were written as *"The `firm` classifier is read at ..."* and all three came back green. **`IDENTIFIER_GLUE` admits at most one of `at|in|is|see` between the name and the pointer, so the binder returned `null` and `RI-16` never saw a named citation at all.** A row that had stopped at the first green run would have reported that `RI-16` is blind to seed C as well, which is false.

---

### 11. Citations this diff moved, audited under [ADR-388](ADR-388.md)'s own ruling

**THREE OF THE FOUR ANNOTATED ENTRIES ARE STRICTLY LINE-COUNT NEUTRAL AND THE FOURTH GROWS BY TWO, WHICH WAS CHECKED RATHER THAN PREFERRED.**

| File | Numstat | Line count before and after |
|---|---|---|
| [ADR-388](ADR-388.md) | 3 / 3 | **370 and 370** |
| [ADR-396](ADR-396.md) | 1 / 1 | 265 and 265 |
| [ADR-397](ADR-397.md) | 1 / 1 | 272 and 272 |
| [ADR-399](ADR-399.md) | 3 / 1 | 247 and **249** |

**[ADR-388](ADR-388.md) IS THE HEAVILY CITED ONE AND IT IS NEUTRAL AT EVERY SITE**, three lines each replaced by one longer line. **The corpus carries exactly one line reference into it and it is prose**: [ADR-392](ADR-392.md) line 99 writes *"[ADR-388](ADR-388.md) line 28"*, and line 28 is that entry's own link-form specimen. **Line 28 is line 28 after this diff, read back rather than trusted to the technique**, and [session 595](../sessions/2026-09-07-session-595.md)'s annotation site at line 336 is unmoved for the same reason.

**AND [ADR-399](ADR-399.md)'s TWO ADDED LINES COST NOTHING, DERIVED RATHER THAN ASSUMED.** A grep for every form this corpus writes, `ADR-399.md:NN`, `ADR-399:NN` and the prose *"ADR-399 line NN"*, across every tracked document and source extension, finds **zero**. **No citation anywhere in this repository names that entry by line**, so the insertion at its line 134 moves nothing that anything points at.

**THE THREE REGISTRY ROWS THIS DIFF APPENDS TO [ALLOCATION](ALLOCATION.md) MOVE SEVEN POINTERS, AND THE AUDIT WAS RUN RATHER THAN ASSUMED.** A first draft of this section asserted the insertion sat below every citation into that file. **It does not.** The rows land at line 393 and the deepest pointer into that file names line 638, so seven citations at or below the insertion shift by three.

| Citing site | Cites | Bound name | On the base `28e2b0ee` | Class |
|---|---|---|---|---|
| [ADR-337](ADR-337.md) line 70 | `ALLOCATION.md:638` | **none** | `:638` is prose about `RI-08`; the `RI-37` row is at **702** | already wrong |
| [ADR-386](ADR-386.md) line 265, [ADR-388](ADR-388.md) line 298, [ADR-389](ADR-389.md) line 176, [ADR-398](ADR-398.md) line 186 | `ALLOCATION.md:638` | **none** | the same | already wrong |
| [ADR-386](ADR-386.md) line 266 and [ADR-398](ADR-398.md) line 187 | `ALLOCATION.md:412` | **none** | `:412` is prose about [ADR-034](ADR-034.md)'s two branches | already wrong |

**NOT ONE OF THE SEVEN WENT STALE BECAUSE OF THIS DIFF. ALL SEVEN WERE ALREADY WRONG ON `28e2b0ee`**, checked by reading both coordinates at the base rather than by inheriting a verdict: the `RI-37` register row those sentences are about stood at **702** and now stands at **705**, three lines down, exactly as the three appended rows predict. **This diff moves seven already-wrong pointers three lines further from subjects they lost days ago.**

**AND NONE OF THE SEVEN MAY BE REPAIRED, UNDER EITHER READING OF `G1`.** The binder returns **no name** for all seven, so under a machine `G1` they are UNDECIDABLE and nobody can derive what they should say. Under the person's `G1` that [ADR-400](ADR-400.md) section 12 rules is the right reader, [ADR-337](ADR-337.md) line 70 names `RI-37` and quotes the row, and [ADR-388](ADR-388.md) section 13 already derived it as **class two, `G2` failing by one line, MUST NOT BE REPAIRED**, with [ADR-389](ADR-389.md) and [ADR-398](ADR-398.md) reaching the same verdict independently. **So the citation-repair permission this row was given is UNSPENT, and not because the diff was lucky: it reaches a pointer this diff makes stale, and this diff makes none.**

**THE OTHER THREE INSERTIONS MOVE NOTHING, DERIVED THE SAME WAY.** [`STATE.md`](../STATE.md) gains an appended section at 13781 against a deepest cited line of 12814, [`sessions/README.md`](../sessions/README.md) a generated row at 822 against 781, and [`README.md`](README.md) a generated row at 415 against 378. **`STATE.md`'s only other hunk is a generated count line replaced one for one at line 32, which moves nothing.**

**AND THE FOUR DRIFTED POINTERS INSIDE THIS ROW'S OWN FENCE ARE STILL NOT REPAIRED, WHICH IS [ADR-388](ADR-388.md) SECTION 13's POSITION UNCHANGED.** [`sessions/README.md`](../sessions/README.md)'s `scoped-db.ts:143`, `halts.ts:109`, `EVENTS:263` and `M06:300` name lines holding different text than they held when those rows were written, **not one names its subject, `G1` fails on all four, and this row can derive what they should say no better than the last three could.** The registries this row appends to gain no pointer that fails `G1`.

---

### 12. Verification, every figure derived at the moment of reporting

- **Suite: 336 files / 8,109 passed / 10 skipped / 0 failed**, against a base reproduced on `28e2b0ee` **BEFORE the first edit** of **336 files / 8,109 passed / 10 skipped / 0 failed**. **DELTA ZERO ON EVERY AXIS.**
- **Gates 33 of 33** after `generate`, derived from the runner's own last line. **Invariants 35 of 35**, before and after. `typecheck`, `lint` and `format:check` all exit 0, measured on the base as well.
- **`pnpm run verify` was NOT run and [`falsify.mjs`](../../scripts/corpus/falsify.mjs) was NOT run**, both forbidden to this row and both mutate the working tree.
- **`git diff 28e2b0ee -- apps packages scripts e2e playwright.config.ts CLAUDE.md docs/ops docs/plans docs/architecture docs/reviews docs/testing` is EMPTY.** [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) and `packages/db/src/scope.ts` were opened for READING only. One file was seeded four times and restored byte-identical after each.
- **The relocated copy of the checker lives outside the repository and was never written into the tree**, so `scripts/corpus/**` and `packages/tooling/**`, which are row `402`'s this wave, are byte-identical to their base.
- **No invariant number was minted, no invariant or gate was added, widened or weakened, no leg was written, no register grew, no test was skipped, disabled or quarantined, no `CI-06` letter was spent, no migration number was taken or reserved, no `status` field was moved, and no port was wired.**
- **`402` and `403` are RESERVED in [ALLOCATION](ALLOCATION.md).** Neither reservation names a subject, guesses a branch, claims anything, or carries a link for a number it does not own.

---

### 13. What is still owed, stated precisely enough to be picked up

1. **HOLE TWO OF [ADR-396](ADR-396.md)'s INSTRUMENT IS PRICED AT 23 AND NOT CLOSED**, section 6. A citation written, deleted and rewritten attributes the surviving line to the earliest introduction, **8 of the corpus's 17 instrument disagreements sit on such a token**, and *"take the oldest"* is the wrong rule for exactly that shape. **Whoever re-derives a class on a churned token should take the oldest introduction NOT SEPARATED FROM TODAY BY A DELETION**, which is derivable and which this row did not take because it is a change to [ADR-396](ADR-396.md)'s ruling rather than an annotation of it.
2. **[ADR-398](ADR-398.md)'s SEAM HAS ONE SURVIVING UNREPAIRED INSTANCE AND IT IS [ADR-337](ADR-337.md) LINE 202**, section 7. It carries `[`gates.mjs:8412`](../../scripts/corpus/gates.mjs)` beside `` `sites: 7` ``, which is the shape that entry ruled on and repaired in three siblings. **The remedy is already specified in its section 6**: date the tense, leave the cardinal, delete the coordinate and name the register entry. **That file is row `403`'s this wave.**
3. **THE 80 REPAIRABLE POINTERS WITH A NON-UNIQUE SUBJECT ARE ENUMERATED BUT NOT REPAIRED**, section 8. Every one is outside this fence. **A row repairing any of them owes the sentence section 8 rules**, naming which occurrence it landed on, and the worst of them name a subject with 130, 96 and 81 occurrences in the cited file.
4. **[ADR-388](ADR-388.md) SECTION 16 QUESTIONS 2 AND 3 ARE STILL UNTOUCHED**, as [ADR-400](ADR-400.md) section 17 item 1 left them. Question 2 asks whether the naming convention belongs in a `covers` line or in [`CLAUDE.md`](../../CLAUDE.md), and that entry already named the second as the honest home. **[`CLAUDE.md`](../../CLAUDE.md) is row `402`'s this wave.**
5. **1,403 POINTERS IN `docs/` ARE STILL IN A FORM NO CHECK CAN SEE**, and section 7 is a new argument for reporting rather than widening: **both amendments considered by this row were proposed on instances inside that population.** No widening is proposed, for [ADR-388](ADR-388.md) section 15 item 5's reason and for [ADR-400](ADR-400.md) section 12's.
6. **THE CONTAINER ARRIVES WITH NO REPOSITORY AND THE DISPATCH STILL SAYS THE CLONE IS SHALLOW**, section 2. Five consecutive sessions have paid the clone out of their own budget and four consecutive rows have measured `false`. **No ADR can fix this and this is the fourth consecutive entry to say so.**

---

### 14. Where this entry is most likely wrong

- **`U` IS MEASURED WITH THE CHECKS' BINDER AND SECTION 7 SAYS THE BINDER IS NOT WHO READS `G1`.** [ADR-400](ADR-400.md) ruled `G1` is the person's read and priced the population at roughly 7,100 dated-record pointers against the binder's 290. **The 442 and the 80 are therefore figures about 3.8 percent of the subject**, taken with the instrument that entry says does not define the gate. The direction is knowable and the magnitude is not: a person's `G1` admits quoted phrases and named rows, **which are MORE likely to repeat in a file than a backticked identifier is**, so 89.3 percent is probably a floor on the true non-uniqueness rate rather than a ceiling. The refusal gets stronger under the honest population, not weaker, which is the only reason this row rules on it at all.
- **`M` IS MEASURED WITH A REGEX THIS ROW WROTE AND ITS ONE HIT IS A FALSE POSITIVE.** The claim that the two seams share no real instance rests on a shape test for a backticked `word: N` beside a coordinate, and a second falsifiable claim written in prose rather than in backticks would be missed entirely. **The enumeration in section 7 is the load-bearing half and the 495-wide count is the weaker half.**
- **SECTION 5 CANNOT SAY WHICH `log -S` LEG IS RIGHT, ONLY THAT THE RULE DOES NOT DETERMINE ONE.** This row's leg is stable across two bases and its `blame` companion reproduces fifteen published cells; that is evidence of a sound harness and **not** evidence that [ADR-400](ADR-400.md)'s leg is wrong. **A founder reading section 5 as an accusation is reading it harder than it is written.**
- **THE 28.0 PERCENT IS A PROPERTY OF ONE NAME IN ONE FILE.** It is the worst case this row found and it is presented as one, but the distribution over the 80 was not taken, so **the average repairer's exposure is unknown and is certainly lower**.
- **SEED A IS ONE SEED AND ITS COORDINATE WAS CHOSEN TO PASS.** It proves the check accepts at least one wrong repeated-name pointer, and the 28.0 percent is what generalises it. **Neither shows that any of the 80 was actually repaired wrongly**, and no such instance is claimed.
- **THIS ROW READ SIX ADRs AT LENGTH AND RAN ONE READER OVER TWO TREES.** A figure recorded only in a session log, and any citation form neither the backticked reader nor this row's link regex matches, is outside everything measured here.

---

### 15. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** Inherited as well as owed: [ADR-357](ADR-357.md) and [ADR-360](ADR-360.md) through [ADR-400](ADR-400.md) are `proposed` and unsigned, two rows run beside this one holding `402` and `403`, and this row settles none of them.

  **What a founder read adds and this entry cannot.**

  **THE FIRST THING TO DECIDE IS WHETHER A GATE MAY BE REFUSED FOR BEING TOO STRONG.** Every refusal in this corpus so far has been of a gate that detects nothing. **This one detects a great deal: it fires on 442 of 495 pointers and every one of the 442 is a sentence a repairer could genuinely get wrong.** It is refused because the thing it does with them is turn 42 correct verdicts into shrugs and 80 permitted repairs into forbidden ones, on a population whose whole value is that it is decidable at all. **A founder who holds that a corpus should refuse to decide what it cannot decide confidently would ACCEPT the strengthening and shrink the decidable set to 53**, and that is a coherent position this row argues against rather than forecloses.

  **THE SECOND IS THAT [ADR-396](ADR-396.md)'s INSTRUMENT RULING IS SOUND AND ITS SENTENCE IS NOT A SPECIFICATION.** Two rows following the same six-word rule produce a different commit for all 493 pointers if one of them walks first-parent. **Three entries now rest figures on that rule**, and section 5 is the first time anybody has asked what it actually says. The remedy costs one clause and it is not taken here, because [ADR-396](ADR-396.md) is not this row's file to rewrite; **it is annotated at the site and handed on.**

  **AND THE THIRD IS THAT BOTH AMENDMENTS THIS ROW WAS SENT TO RULE ON WERE PROPOSED ON POINTERS THE CORPUS'S OWN READER CANNOT SEE.** [ADR-399](ADR-399.md)'s four `:35` pointers and [ADR-398](ADR-398.md)'s surviving `sites: 7` site contribute **zero** between them to the 495 every figure in this entry is measured over. **The corpus is now amending a gate on evidence the gate has never been able to read**, which is not an error in either entry and is the strongest argument available for [ADR-400](ADR-400.md) section 12's ruling that `G1` belongs to a person. **A founder who wants these amendments priced over the population they were actually observed in is asking for 8,390 sentences to be read**, and nobody has proposed a way to do that.

- **Approval:** `___________________________` (UNSIGNED. **CONTROL OVER THE CORPUS'S HONESTY, `E2` READ OWED.** **Merging is not signing.**)

| | |
|---|---|
| **Approved by** | |
| **Date** | |
