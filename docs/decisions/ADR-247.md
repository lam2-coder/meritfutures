## ADR-247: tiers 2 and 3 classified entire, the class C residue is TWO of fifteen, and one of the two is established by running the falsifier rather than by arguing it  (2026-08-29, status: proposed)

**[ADR-244](ADR-244.md) removed the sampling objection by reading the whole of tier 1: twenty-one entries, twenty-one classifications, no extrapolation. THIS ENTRY DOES THE SAME FOR TIER 2, THE LEDGER, AND TIER 3, MONEY ENTERING.** Fifteen distinct entries, fifteen classifications, **eleven read in full here and four carried from [ADR-244](ADR-244.md) because they are also tier 1.** Nine are class B with an assertion watched RED on the real tree and GREEN after a byte-identical restore, one is class B on its surviving clause, **TWO are class C**, one is neither, and two land no executable money-path line at all.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `247`, session **437**, dispatched as `claude/class-b-tiers-two-and-three`. The number is spent.

- **THIS ENTRY SIGNS NOTHING AND MARKS NOTHING `accepted`.** It ships `proposed` with an **UNSIGNED** approval line. **The seven entries it moves are not marked `accepted` either** and no `status` field anywhere in this branch changes: what each gains is a recorded class B approval naming an assertion, its transcript and its limits. **The approval is the artifact and not the status field.**

- **NOT THE MONEY PATH BY FILE.** No file under any `src/` is changed and no migration is taken. **Eleven shipped source files and merged migrations were seeded with a defect and restored, each verified byte-identical by `sha256sum` and by an empty `git status --short`**, which is section 3's method and is recorded there per seed. What this branch ADDS is one test file and documents.

- **WHAT IS MEASURED, WHAT IS DERIVED, AND WHAT IS JUDGED.** Every count in section 2 is **MEASURED** live on `origin/main` at `ff36bea`, with the commands published in [the census](../reviews/2026-08-29-class-b-tiers-2-and-3-census.md). Every transcript in section 3 is **MEASURED**: a seed, a run, a restore, a run. Section 5's four probes are **MEASURED**. Section 6's attribution is **MEASURED** by walking git to the commit that added the file. What is **JUDGED** is the class assigned to each of the eleven entries read (section 3), and the reading in section 4 of why [ADR-186](ADR-186.md)'s conjunction resolves toward its choice half where [ADR-187](ADR-187.md)'s resolves toward its property half.

- **THE HEADLINE OF THE ENTRY BEFORE THIS ONE DOES NOT REPEAT AND IT IS NOT SOFTENED.** [ADR-243](ADR-243.md) and [ADR-244](ADR-244.md) both found that *"what was missing was the record and not the control"*. **In tier 2 one entry had no assertion anywhere in this tree** and section 3.9 is the file that closes it.

---

### 1. THE RULING

> **1. TIERS 2 AND 3 ARE CLASSIFIED ENTIRE AND THEIR CLASS C RESIDUE IS TWO ENTRIES OF FIFTEEN.** Nine class B with a transcript, one class B on its surviving clause, **two class C ([ADR-180](ADR-180.md), carried, and [ADR-186](ADR-186.md), established here)**, one NEITHER under [ADR-227](ADR-227.md) section 2's transfer rule, and two landing no executable money-path line. **No extrapolation.** Section 3 and [the census](../reviews/2026-08-29-class-b-tiers-2-and-3-census.md) section 4.

> **2. SEVEN APPROVALS ARE EARNED HERE AND GRANTED BY NOBODY.** [ADR-104](ADR-104.md), [ADR-105](ADR-105.md), [ADR-123](ADR-123.md), [ADR-157](ADR-157.md), [ADR-183](ADR-183.md), [ADR-187](ADR-187.md) and [ADR-193](ADR-193.md). **Nineteen seeds were applied to shipped source and to merged migrations for the class B question and EIGHTEEN went RED**; the nineteenth is section 7's finding. Section 5's four further probes establish the class C. Each entry gains a block naming the assertion, the seed that reddened it, and **what the assertion does not reach.**

> **3. [ADR-186](ADR-186.md) IS CLASS C AND THE DEMONSTRATION IS EXECUTED RATHER THAN ARGUED.** Its central claim is *"the last two codes are both assets"*, which is [ADR-180](ADR-180.md)'s shape one code over. **Ruling `reserve` an `equity`, which is the reading its own section 4 argues against by name, turns exactly ONE case red out of 952, and that case reads a hand-written map back to itself.** Section 5. **This is the falsifier being exhibited and found to be a tautology**, which is [ADR-227](ADR-227.md) section 6 condition 2's named failure.

> **4. THE FINDING, AND IT IS THIS SESSION'S SHARPEST.** `apps/api/src/routes/checkout.ts` is how Merit takes money. It was added in ONE merge, that merge's only entry is [ADR-113](ADR-113.md), and that entry rules a single API_CONTRACT row for `POST /affiliate/creatives`. **662 executable money-path lines are attributed to an entry that never argued them, and the entry says so itself in its own last line.** ADR-227 section 2's transfer rule sends the read to *"the entry that lands the diff"*, and **there is no such entry, because none was written.** Section 6.

> **5. A MERGED MIGRATION'S OWN LITERAL CAN DRIFT FROM THE CONSTANT THAT IS WATCHED.** [`0056`](../../packages/db/migrations/0056_eighth_ledger_code.sql)'s seeded row made identity-scoped leaves **20 of 20 green**; the same change to [`accounts.ts:75`](../../packages/ledger/src/accounts.ts) turns **8 cases red**. The tree is not defective and both say `firm`. **What is true is narrower: the migration's copy is watched by nothing `CI-02` runs.** Section 7.

> **6. NO ASSERTION WENT RED ON UNMODIFIED SHIPPED SOURCE.** The row required such a result be reported prominently as a finding. **There is none to report** and this entry says so plainly rather than hunting for something. Every red in section 3 followed a seed this session applied and reversed.

> **7. NO `RI-` NUMBER IS TAKEN.** `RI-23` landed with [ADR-244](ADR-244.md) and the runner reports **22 of 22**, so `RI-24` is next free and is left free. Every assertion named here lives in a vitest suite `CI-02` already runs, and minting an invariant to hold a claim a suite already holds is a number spent for nothing. **This is [ADR-243](ADR-243.md) finding 2's reasoning applied a second time**, and `RI-23` is the counter-example that shows when the other answer is right.

---

### 2. The population, re-derived rather than carried

Full commands and scope statements are in [the census](../reviews/2026-08-29-class-b-tiers-2-and-3-census.md) sections 2 and 3. Measured on `origin/main` at `ff36bea`:

| | Count |
| --- | ---: |
| entry files | **232** |
| `status: accepted` | **103** |
| `status: proposed` | **129** |
| of the 129, landing at least one money-path file | **53** over **69** files |
| of the 53, **tier 2** by file | **9** |
| of the 53, **tier 3** by file | **7** |
| distinct entries across both, [ADR-187](ADR-187.md) being in both | **15** |

**THE MONEY-PATH QUEUE DID NOT MOVE TODAY, WHICH IS NEW.** The trend was 42, 44, 46, 47, 52, 53 and is now 42, 44, 46, 47, 52, 53, **53**: three entry files were added since [ADR-244](ADR-244.md) and none lands a money-path file. **One flat reading is one reading and is not a plateau**, and it is recorded as a measurement rather than as an argument, because the argument [ADR-227](ADR-227.md) built out of that trend does not need this reading to go either way.

**THE SHALLOW-CLONE TRAP WAS LIVE FOR THE THIRD CONSECUTIVE SESSION.** `git rev-parse --is-shallow-repository` returned `true`, the log reported **207** commits against **3,561**, and `git fetch --unshallow origin` was run before any number above was taken. **Three sessions have now inherited it and nothing in this repository checks a clone's depth before running the derivation that silently depends on it.**

**THE TIER PREDICATE WAS VALIDATED AGAINST A KNOWN ANSWER BEFORE IT WAS TRUSTED.** Run with [ADR-243](ADR-243.md) section 4's tier 1 file list, the same script returns that entry's twenty-one entries **member for member**, which is what makes the census's two rows a transcription of [ADR-227](ADR-227.md) section 8's words rather than a fresh judgement about what counts as the ledger.


### 2.1 Re-derived on the merge with `main` at `823c706`

**`main` MOVED WHILE THIS BRANCH WAS OPEN AND WAS MERGED IN RATHER THAN REBASED.** It carried **one file**, [ALLOCATION](ALLOCATION.md), with wave AU's rows `250` to `253`. **No entry file, no migration and no line of source.** `ALLOCATION` auto-merged with **zero conflict hunks**, this branch's row `247` and main's four new rows being disjoint, and `gates.mjs generate` produced no change on the merged tree.

**SECTION 2's TABLE IS A FACT ABOUT `ff36bea` AND IS NOT REWRITTEN. Re-derived on the merge**: **232** entry files, **103** `accepted`, **129** `proposed`, unchanged in every cell, which is what a merge bringing no entry file should produce and is checked rather than assumed. **The money-path count and both tier memberships are therefore unmoved**, and the fifteen classifications stand on the same fifteen entries.

**Re-derived on the merge, each command run separately**: **33 of 33** gates, **22 of 22** invariants off the runner's own last line, **270 files / 6,590 passed / 6 skipped**, typecheck 0, lint 0, `format:check` clean. **Identical to the pre-merge run, because main brought no executable line.**

---

### 3. The transcripts

**EVERY SEED WAS APPLIED TO SHIPPED SOURCE OR TO A MERGED MIGRATION, RUN, AND REVERSED FROM A BYTE COPY TAKEN BEFORE IT.** Each restore is verified two ways: the file's `sha256sum` before and after are identical, and `git status --short` reports the source tree clean. **No shipped file is edited by this branch.**

**BASELINE, REPRODUCED BEFORE A LINE MOVED**, on `ff36bea` after `pnpm install`: **33 of 33** gates after `generate`, **22 of 22** invariants off the runner's own last line, **269 files / 6,583 passed / 6 skipped**, typecheck 0, lint 0, `format:check` clean.

#### 3.1 ADR-104, and the guard is watched protecting the claim rather than the caller

**THE CENTRAL CLAIM IS A PROPERTY AND THE ENTRY STATES IT PRECISELY ENOUGH TO BE FALSIFIED**, which is its own section 3's heading. `entriesOf` is the only producer of an `EntryDraft` and its whole body pushes `+a` and `-a`, so *"there is no `Transfer` value for which that is false, including one assembled past both brands by a cast"*. The heading's third clause is the same shape: *"a halt is only a halt because this code path honours it"*, and nothing in the database reads `ledger_halts`.

**THE ENTRY'S OWN APPROVAL CLAUSE NAMES TWO OF THESE THREE SEEDS**, and they are run here rather than trusted.

| Seed | Result |
| --- | --- |
| [`posting.ts:236`](../../packages/ledger/src/posting.ts), the credit leg deleted from `entriesOf` | **RED, 30 of 121**, led by *"a Posting assembled past BOTH brands still folds to zero, which is the claim"*, which is the entry's own named case |
| [`accounts.ts:145`](../../packages/ledger/src/accounts.ts), `accountKey` drops `${ref.identityId}`, so two traders share one position | **RED, 12 cases**, including three `PT-03` properties and five halt cases. **This is the seed the entry calls the one worth the most**, because it leaves every arithmetic property green in isolation |
| [`halts.ts:135`](../../packages/ledger/src/halts.ts), `assertNoLiveHalt` short-circuits to a no-op | **RED, 1 of 121**, on *"a live halt against an identity this posting touches REFUSES it, and nothing is written"* |
| All three restored from byte copies | **GREEN, 121 of 121.** `sha256` identical on all three, `git status` clean |

**WHAT IT DOES NOT COVER, AND THE ENTRY'S OWN FOUNDER BLOCK IS THE LIST.** That the halt check lives INSIDE `postTransaction`, which buys a control that cannot be forgotten and pays a full read of `ledger_halts` on every posting forever. That the chart is read whole and handed in, which puts a correctness-versus-staleness choice in front of every caller. That `packages/ledger` declares no dependency on `packages/db`. **And nothing here meets a database**: the entry's section 10 is eleven cases run by hand, once, in the session that wrote the code, and it says itself *"it is not a gate and it will not run again"*.

#### 3.2 ADR-157: a term is recognised by identity, and the lock is a row lock

**THE CENTRAL CLAIM IS A PROPERTY.** Clause 2: the three constructors are the only producers and each mints into a module-private `WeakSet`, so *"a caller cannot hand-roll a term, cannot smuggle a `SQL` through one, and cannot receive one across a process boundary"*. Clause 4: `lockAt` is `rowAt` plus `FOR UPDATE` on the same predicate.

**THIS IS `E2`'s OWN NAMED FAILURE CLASS**, the identity-scoped accessor, which is the tier where a model's assessment is worth least and an assertion is worth most.

| Seed, in [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) | Result |
| --- | --- |
| `scopedAddressPredicate` returns `addressPredicate(key, at)`, the tenancy conjunct dropped from the read half | **RED, 4 of 315**, led by *"THE BOLA PAIR: a scoped lock carries the HANDLE's identity and the CALLER's address"* and *"a lock composes the SAME predicate the matching read composes, on EVERY scoped table"* |
| `.for('update')` deleted from `lockingSelectStatement` | **RED, 4 of 315**, including *"the unscoped lock carries the address ALONE, which is what that door is for"*. **The entry predicted four** |
| `isFilterTerm` reads `'term' in candidate` instead of `TERMS.has(candidate)` | **RED, 1 of 315**, on *"a term is recognised by IDENTITY, so an object that merely looks like one is a VALUE"*. **This is the `jsonb` attack clause 2 was written for** |
| All three restored | **GREEN, 315 of 315.** `sha256` identical, `git status` clean |

**WHAT IT DOES NOT COVER, AND THE ENTRY'S FOUNDER BLOCK IS THE LIST.** That the aggregate is REFUSED, which blocks nothing today and reads like an oversight without section 5. That the lock is a ROW lock rather than the advisory lock two approved invariants name in prose. **And the assertions read RENDERED SQL rather than executing it**, which is [ADR-243](ADR-243.md) section 4.3's stated gap arriving one file over: no database here decides whether the predicate returns the rows it renders.

#### 3.3 ADR-183: three positions, opened by the database, per row

**THE CENTRAL CLAIM IS A PROPERTY AND THE ENTRY DECLARES ITS OWN SPLIT**: *"That the three codes are the right set is DERIVED, from `LEDGER_ACCOUNT_SCOPE`. **That a trigger is the right home is JUDGED**."*

| Seed, in [`0054`](../../packages/db/migrations/0054_identity_ledger_accounts.sql) | Result |
| --- | --- |
| `promotional_credit` in the trigger's VALUES becomes `reserve`, a FIRM-scoped code provisioned onto an identity, which is the tuple the entry executed and found the database ACCEPTS | **RED, 2 of 9**, on *"the trigger seeds every per-identity code and no other, in both directions"* and *"no firm-scoped code is seeded onto an identity"*. **The database accepts that row and this file is the only thing that refuses it**, which is the entry's own section 5 |
| `FOR EACH ROW` becomes `FOR EACH STATEMENT`, which is *"when the identity is created"* losing its subject | **RED, 1 of 9**, on *"the trigger fires per row after an identity is inserted"* |
| Both restored | **GREEN, 9 of 9.** `sha256` identical, `git status` clean |

**A THIRD EDIT WAS ATTEMPTED FIRST AND IS DISCLOSED RATHER THAN DROPPED.** Deleting the `promotional_credit` tuple outright produced a malformed `VALUES` list with `trader_wallet` repeated; it turned the same set-equality case red, and it was discarded and rewritten as the clean seed above **because a transcript is worth what the edit that produced it is worth.**

**WHERE THIS CLASSIFICATION IS FRAGILE, NAMED RATHER THAN LEFT TO BE FOUND.** The entry's founder block opens on the trigger: *"A WRITING TRIGGER IS A PRECEDENT AND THE PRECEDENT IS THE COST"*, and this is the twenty-second trigger in a schema whose other twenty-one only assert. **A reader could call the choice of home the point of the diff**, in which case this is class C. **This entry calls the provisioning the point and the home a residual choice**, on the ground that the entry measured the alternative closed rather than preferring against it: `grep -rni "insert into[[:space:]]*identities" --include='*.ts'` returns zero across `apps/`, `packages/` and `scripts/`, so shape (b) is a ruling written against an absence. **That measurement was re-run here and still returns zero.**

**WHAT IT DOES NOT COVER.** Whether `promotional_credit` should be provisioned for an instrument nobody has granted, which the founder block calls the second judgement and prices as reversible. Whether the right instant is the first sighting or the first purchase, which the entry names as where it is most likely wrong. **And nothing executes the trigger**: every case above reads the migration's text.

#### 3.4 ADR-187: the eighth code, and the seed that stayed green

**THE CENTRAL CLAIM IS A DERIVATION AND THE ENTRY SAYS SO**: *"That shape (i) is the only representable shape is DERIVED ... What is JUDGED is the SPELLING and the decision to SEED."*

| Seed, in [`0056`](../../packages/db/migrations/0056_eighth_ledger_code.sql) | Result |
| --- | --- |
| The eighth arm of `ledger_accounts_kind_matches_code` flips to `kind = 'asset'`, which is shape (iii) returning | **RED, 2 of 20**, on *"every liability OTHER than the minted one is per identity"* and *"every firm code is ruled, and exactly one of them is the obligation"* |
| **The seeded row becomes `('withdrawals_in_flight', 'liability', 'identity')`** | **GREEN, 20 of 20. It does not red.** Section 7 |
| Both restored | **GREEN.** `sha256` identical, `git status` clean |

**THE APPROVAL IS EARNED ON THE FIRST SEED AND THE SECOND IS SECTION 7's SUBJECT.** The kind is what the heading claims and what the derivation establishes: `LT-06` credits the slot and `LT-07` debits it, so the account rises when the trader's claim is extinguished and falls when the cash leaves.

**WHAT IT DOES NOT COVER.** The SPELLING, which the entry names as judged and prices at one string. The decision to SEED a row, which its section 5 argues both ways. **And the migration's own `scope` literal**, which section 7 measures as held by nothing this container runs.

#### 3.5 ADR-105: the port owns the order

**THE CENTRAL CLAIM IS A PROPERTY.** Ruling 1: *"No adapter re-implements the ordering and no adapter can reach the parse without the digest having agreed."* Ruling 3: *"there is no parsed body to be had without it."*

**THE ENTRY'S OWN APPROVAL CLAUSE NAMES ITS FIRST CASE BY TITLE**, and the ORDER seed below is the one that carries the heading.

| Seed, in `packages/psp` | Result |
| --- | --- |
| **[`webhook.ts`](../../packages/psp/src/webhook.ts): step 3, the freshness window, is moved ABOVE step 2, the digest, which is ruling 1's ORDER reversed** | **RED, 2 of 69**, on *"THE ORDER: a stale timestamp with a BAD signature reports the signature"*. **That is the claim itself**: an attacker controls the timestamp bytes, so a window check running first lets an unauthenticated party choose which branch executes |
| `webhook.ts`: the digest is taken over a re-serialised body rather than over `presented.signedBytes` | **RED, 8 cases**, including *"refuses a body re-serialised before verification"*, which is the entry's own named clause, and every fake's round trip |
| [`amount.ts`](../../packages/psp/src/amount.ts): a `payment_method='wallet'` row returns `0n` instead of refusing | **RED, 1 of 69**, on *"payment_method='wallet' is REFUSED rather than returning zero (INV-M3-13)"*. **This is ruling 5's fail-open direction at the processor boundary**: a wallet purchase that reached a processor at all is the invariant broken |
| All three restored | **GREEN, 69 of 69.** `sha256` identical, `git status` clean |

**WHAT IT DOES NOT COVER, AND THE ENTRY'S FOUNDER BLOCK IS THE LIST.** That the nonce need not be a header, which reads API_CONTRACT's bold ordering as a coverage requirement rather than a presentation one. That an approved plan's unimplementable rule is honoured in its REASON rather than amended. That `WebhookVerificationError` carries `raw`, which the entry itself calls *"an argument rather than a proof"*. **And no socket is opened by anything here**: both implementations are fakes and the vendor is unselected, so nothing above says a real provider's scheme fits.

#### 3.6 ADR-123: an empty book refuses

**THE CENTRAL CLAIM IS A PROPERTY AND THE ENTRY'S CONDITION FOR EXISTING WAS THAT THE TREE CARRIED A GREEN ASSERTION OF THE OPPOSITE.** Its own words: *"the condition is met and section 1 finding 4 is the proof, which is a passing test asserting the opposite of what this entry rules."*

| Seed, in [`replay.ts`](../../apps/worker/src/batch/replay.ts) | Result |
| --- | --- |
| The `report.accountsAudited === 0` guard is short-circuited, so an empty book reports CLEAN again | **RED, 1 of 66**, on *"REFUSES a run over a book with no stored state at all (ADR-073 section 5)"* |
| The guard RETURNS the report instead of throwing, which is the shape the entry's own comment calls *"a refusal that returns a report can be read like a clean one"* | **RED, 1 of 66**, the same case. **Both directions of the same clause, and the second is the one a later session would actually write** |
| Both restored | **GREEN, 66 of 66.** `sha256` identical, `git status` clean |

**WHAT IT DOES NOT COVER.** The ORDER of the two guards, which the entry rules deliberately and which **no assertion can hold**: over an empty book `storedRows > 0` is false anyway, so swapping them changes no outcome, and the entry says so itself. **That is a clause of a class B entry that is not itself falsifiable**, recorded here rather than folded into the approval. **And the 27 executable lines this entry's merge lands in `checkout.ts` are not its subject**: the entry states in its own third bullet that the enrichment wiring *"rules nothing and is deliberately absent from this entry"*.

#### 3.7 ADR-113 is NEITHER, and the transfer has nowhere to go

**Section 6.** Its central claim is a row in a frozen contract document. **The money-path lines credited to it are somebody else's slice**, and the entry says so in its own last line.

#### 3.8 ADR-225 and ADR-238 land no executable money-path line

**MEASURED RATHER THAN TRUSTED TO THE FILTER**, both diffs read: restricted to money-path files and stripped of blank and comment lines, each leaves **zero**. [ADR-225](ADR-225.md) says it itself: *"Every change in this pull request is a comment or a reason string."* [ADR-238](ADR-238.md) rules three obstructions on `POST /checkout` and clears none of them. **What that discharges is the BEHAVIOURAL read and nothing else**, which is [ADR-244](ADR-244.md) section 4's distinction and it holds here for the same reason.

#### 3.9 ADR-193, and this is the one whose assertion did not exist

**THE CENTRAL CLAIM IS A PROPERTY IN TWO CLAUSES.** *"The rule refuses the LINK and not the operation"*: a re-application posted as a fresh transaction with its own kind, key and reason is ACCEPTED, and the same movement wearing `reversal_of` is refused. *"It lives in a trigger because the builder is not weak here but INCAPABLE"*: a row-level `CHECK` cannot query another row, which is why [`0009`](../../packages/db/migrations/0009_ledger.sql)'s constraint could only ever carry `reversal_of <> id`.

**NOTHING IN THIS TREE ASSERTED ANY OF IT.** `grep -rl "0059\|LEDGER-C3" --include='*.test.ts' packages/ apps/` returned nothing. The ruling was watched executing against PostgreSQL 16 by the session that wrote it, in that entry's section 5, and **that run is not a control**: it happened once, in the session that also wrote the migration, and nothing re-runs it.

**THE ASSERTION:** [`packages/db/test/reversal-chain.test.ts`](../../packages/db/test/reversal-chain.test.ts), **7 cases**. It reads the trigger's arming clause, the guard's own predicate, the second-row SELECT that a `CHECK` cannot make, `0009`'s surviving constraint, and the absence of any supersession in the file.

| Seed, in [`0059`](../../packages/db/migrations/0059_reversal_chain.sql) | Result |
| --- | --- |
| The `WHEN (NEW.reversal_of IS NOT NULL)` clause is deleted, so the guard is armed on every INSERT rather than on the LINK | **RED, 1 of 7**, on *"the trigger is ARMED by reversal_of, so an un-chained posting never reaches it"*. **That is the "not the operation" half** |
| The guard's `SELECT lt.reversal_of INTO target_link` becomes `SELECT le.amount_cents`, so the rule reads a leg rather than a link | **RED, 1 of 7**, on *"the refusal fires on the TARGET row's own link and on nothing else"* |
| `ALTER TABLE ledger_transactions DROP CONSTRAINT ledger_transactions_no_self_reversal;` is smuggled in before the `COMMIT` | **RED, 1 of 7**, on *"no merged object is dropped, replaced or altered through this migration"*, which is the entry's *"Two statements, two objects, and no `DROP`"* |
| All three restored | **GREEN, 7 of 7.** `sha256` identical, `git status` clean |

**WHERE THIS CLASSIFICATION IS FRAGILE.** The entry's own bullet says *"What is JUDGED is whether a chain is wrong at all"*, and **a reader could call that the point of the diff**, in which case this is class C. **This entry calls the LINK-not-operation reading the point**, on the ground that the entry's section 2 grounds the prohibition on [`0009:104`](../../packages/db/migrations/0009_ledger.sql)'s own promise, which the corpus made before this session existed, and on the ground that the entry's own emphasis is that *"the answer is not the obvious one"*: what is new is the separation of the link from the movement, not the refusal.

**WHAT IT DOES NOT COVER, AND THE LIMIT IS LARGER HERE THAN ANYWHERE ELSE IN THIS ENTRY.** **Every case reads TEXT and nothing executes the trigger.** A rewrite preserving these bytes and changing the behaviour defeats all seven, which is [ADR-243](ADR-243.md)'s structural-assertion caveat at its sharpest. The entry's finding 3 is untouched: `LEDGER-C3` refuses the chain, and **a fresh, honestly-authored, un-chained posting against a withdrawal that settled last week is accepted by everything in this database.** The entry calls that hole *"larger than the one it does close"* and this session agrees and does not rule it.

---

### 4. Two conjunctions, resolved in opposite directions, one line apart in the same constant

**[ADR-186](ADR-186.md) AND [ADR-187](ADR-187.md) BOTH RULE A `kind` LITERAL INTO THE SAME CHECK CONSTRAINT AND THEY GO TO DIFFERENT CLASSES.** Saying why is the only useful form of this ruling, because a reader could otherwise take it as inconsistency.

| | [ADR-186](ADR-186.md), `reserve` and `psp_clearing` | [ADR-187](ADR-187.md), `withdrawals_in_flight` |
| --- | --- | --- |
| **Does anything post against the code?** | **No**, and both of [ADR-177](ADR-177.md)'s grounds for refusing them still hold | **Yes.** `LT-06` credits the slot, `LT-07` debits it |
| **What constrains the kind?** | GLOSSARY prose, an anchor table, and two sentences in `checkout.ts` | The SIGN of two legs under one convention |
| **What reds when it is reversed?** | A hand-written map being read back to itself. Section 5 | The postings' own arithmetic, in a suite that resolves the kind from the legs |
| **Class** | **C** | **B** |

**THE TEST FILE ITSELF DRAWS THIS LINE AND THIS ENTRY DID NOT INVENT IT.** [`chart-of-accounts-kinds.test.ts`](../../packages/ledger/test/chart-of-accounts-kinds.test.ts)'s header on `RULED` says of the eighth code: *"Its kind is a DERIVATION, not a judgement -- `LT-06` credits the slot and `LT-07` debits it"*, and of the other two: *"Neither has a posting, so neither is an ADR-177 derivation; both are read off what the tree says they ARE."* **A session had already written the distinction into the instrument and no entry had used it to classify anything.**

**AND [ADR-186](ADR-186.md) ITSELF STATES THE SAME CONCLUSION IN ITS OWN FOUNDER BLOCK**, which is the strongest evidence available that this class is the entry's own and not this session's: *"Nothing in the tree can falsify a `reserve` ruling by execution, because nothing posts against it and nothing reads it, so this literal is a claim no behaviour depends on until something does."*

**WHAT WOULD MOVE IT OUT OF CLASS C IS A POSTING AND NOT AN ARGUMENT.** The moment anything posts against either code, the sign of the leg constrains the kind and the assertion writes itself. **That is [ADR-227](ADR-227.md) section 9's own mechanism**: a row leaves class C when somebody writes the assertion, and here nobody can until somebody writes the posting.

---

### 5. The four probes that establish the class C

**A CLASS C IS NORMALLY ARGUED AND THIS ONE IS RUN.** The question is not whether SOMETHING reds when the ruling is reversed. It is whether what reds is an independent property or a restatement. Suite: `packages/ledger`, `packages/db` and `packages/rail`, **36 files and 952 cases green** before each probe, each probe restored byte-identically.

| Probe | Result |
| --- | --- |
| `reserve` flips to `liability` in [`0055`](../../packages/db/migrations/0055_last_two_ledger_kinds.sql) **alone** | **RED, 1 of 952**, on *"a superseding constraint carries every arm the one before it carried"*. **A COPIES-AGREE property, silent on which kind is right** |
| `reserve` flips to `liability` in **both** `0055` and [`0056`](../../packages/db/migrations/0056_eighth_ledger_code.sql) | **RED, 4 of 952.** Three are *"exactly one firm code is a liability"* and its relatives, which hold that `reserve` is NOT a liability and say nothing about which of the remaining four it is |
| `psp_clearing` flips to `liability` in both | **RED, 4 of 952**, the same four |
| **`reserve` flips to `equity` in both, which is the reading [ADR-186](ADR-186.md) section 4 argues against BY NAME** | **RED, 1 of 952, and it is `expect(Object.fromEntries(ARMS)).toEqual(RULED)`** |

**THE FOURTH PROBE IS THE WHOLE ARGUMENT.** `RULED` is a hand-written map carrying `reserve: 'asset'`, transcribed from ADR-186's own ruling. **951 of 952 cases are indifferent to whether `reserve` is an asset or an equity**, and the one that is not is the entry's sentence being read back to itself. **That is [ADR-227](ADR-227.md) section 6 condition 2's named failure, exhibited rather than asserted.**

**THIS IS NOT AN ARGUMENT AGAINST THE TEST OR AGAINST THE MAP.** A total map over the vocabulary is the right shape and the file argues for it. **What the probe establishes is what the map's rows are worth, one row at a time**, and the file's own header already separates the derivations from the judgements inside it.

---

### 6. THE FINDING: the money-entering path landed under an entry that rules an API_CONTRACT row

**`apps/api/src/routes/checkout.ts` WAS ADDED ONCE.** `git log --first-parent --diff-filter=A origin/main -- apps/api/src/routes/checkout.ts` returns exactly one commit, `f5a3660`, *"session 220, P3-n, checkout, attribution inside the transaction, and the half of the wall ADR-112 did not move"*. **That merge's only ADR file is [ADR-113](ADR-113.md)**, whose five rulings are about where a row for `POST /affiliate/creatives` goes in [API_CONTRACT](../architecture/API_CONTRACT.md) section 7, what its response carries, and whether it needs elevation. **Not one is about the checkout handler.**

**THE ENTRY SAYS SO ITSELF AND HAS SINCE 2026-08-26**, in its own last line: *"MONEY PATH by position rather than by content: this row moves no money and the entry it ships with does. The `E2` line-by-line read is owed on the session, not discharged by this signature."*

**SO ADR-113 IS NEITHER, AND THE TRANSFER RULE HAS NOWHERE TO SEND THE READ.** [ADR-227](ADR-227.md) section 2 rules that *"an owed read transfers to the entry that lands the diff"*. **Here the entry in the merge and the entry that lands the diff are not the same object, because the second does not exist.** Five later entries touch the file and every one rules something else: an audit refusal ([ADR-123](ADR-123.md)), a ledger code ([ADR-187](ADR-187.md)), six citations ([ADR-225](ADR-225.md)), a write door ([ADR-230](ADR-230.md)), and three obstructions it declines to clear ([ADR-238](ADR-238.md)).

**THIS IS THE EXACT INVERSE OF [ADR-243](ADR-243.md) SECTION 3 AND IT IS THE WORSE DIRECTION.** That error attributes one diff to two entries and inflates a queue by one. **This one attributes 662 executable lines of the money-entering path to an entry that never argued them**, and it makes the census look like it holds a reader for a file nobody has read. A reader who opens the money-path derivation learns that `checkout.ts` sits under `ADR-113`; a reader who opens `ADR-113` learns it is about affiliate creatives.

**WHAT IS NOT CLAIMED, SAID PLAINLY.** Nothing here says the checkout handler is wrong and **nothing here reads it**. `checkout.test.ts` exists and is green. **What is missing is the ENTRY**: the document that states what the handler's central claim is, so that section 3's question has something to ask. **Until one exists, `checkout.ts` cannot be class B, cannot be class C, and cannot be counted honestly in either.** That entry is named here as owed and is not written, because writing it is a money-path slice of its own and this row's fence is a census.

**AND HOW MANY OTHER MONEY-PATH FILES SIT THE SAME WAY IS NOT MEASURED AND NOT GUESSED.** The walk that would find them is one command per file, run over all 69, and this session ran it over tiers 2 and 3 only.

---

### 7. Finding 2: only one of two copies of the eighth code's scope is watched

**THE ROW REQUIRED A SEED THAT REDDENED ON REAL CODE BE REPORTED PROMINENTLY. THERE IS NONE. WHAT THERE IS INSTEAD IS A SEED THAT SHOULD HAVE REDDENED AND DID NOT.**

| Seed | Result |
| --- | --- |
| [`0056`](../../packages/db/migrations/0056_eighth_ledger_code.sql)'s seeded row becomes `('withdrawals_in_flight', 'liability', 'identity')` | **GREEN, 20 of 20** |
| [`accounts.ts:75`](../../packages/ledger/src/accounts.ts)'s `withdrawals_in_flight: 'firm'` becomes `'identity'` | **RED, 8 cases** across two suites |

**THE TREE IS NOT DEFECTIVE AND [ADR-187](ADR-187.md)'s RULING STANDS.** Both copies say `firm` today and the approval rests on the kind seed, which is the clause the heading makes. **What is true is narrower and it is the part worth carrying**: the suite holds the TYPESCRIPT copy of the scope and not the migration's own, so the two can diverge and only one side is watched. The instrument that would catch it is [`probe_ledger_constraints.sql`](../../scripts/db/probe_ledger_constraints.sql)'s `K3` block, which needs a running PostgreSQL that `CI-02` does not have.

**A MERGED MIGRATION IS NEVER EDITED, SO A DIVERGENCE INTRODUCED THERE IS PERMANENT.** The case that would close it is a set comparison between `0056`'s seeded tuples and `LEDGER_ACCOUNT_SCOPE`, on [`identity-provisioning.test.ts`](../../packages/ledger/test/identity-provisioning.test.ts)'s own precedent one migration over. **It is named here and not written**, because it belongs to whoever holds the fence that includes the fourth copy's watcher, and adding it while classifying would be this census widening itself.

---

### 8. What this entry does not do

- **It approves nothing by signature.** Not the seven, not the census, not itself. **No `status` field moves anywhere in this branch.**
- **It reads 11 of the 53 money-path entries in full** and carries [ADR-244](ADR-244.md)'s four. **Twenty-one money-path entries fall in no tier 1, 2 or 3 and are read by nobody**, named in [the census](../reviews/2026-08-29-class-b-tiers-2-and-3-census.md) section 9. **No ratio is carried to them.**
- **It writes ONE assertion**, [`reversal-chain.test.ts`](../../packages/db/test/reversal-chain.test.ts), for the one entry of nine that had none.
- **It takes no migration number, no `RI-` number and no `CI-06` letter.** `RI-24` is next free and is left free.
- **It edits no shipped source.** Nine files were seeded and restored byte-identically; the branch adds one test file and documents.
- **It does not read `checkout.ts`.** Section 6 measures an attribution and reads no handler.
- **It does not touch [`MERIT_BUILD_MASTER_PROMPT.md`](../../MERIT_BUILD_MASTER_PROMPT.md)**, which is frozen, and it does not enact [ADR-227](ADR-227.md) section 10's proposed amendment. **That amendment is still the founder's to accept or refuse, and if refused, all seven approvals below collapse into class C and stand as owed reads.**

---

### 9. Approval

- **Approval line: PENDING, UNSIGNED.** **NOT the money path by file.** No `src/` file is changed and no migration is taken.

  **What a founder read adds and this entry cannot.**

  **THE FIRST THING TO DECIDE IS SECTION 6, AND IT IS THE ONLY ITEM HERE THAT NEEDS A DECISION RATHER THAN A READ.** `POST /checkout` is how Merit takes money, its handler is 662 executable lines, and **no entry in this corpus argues it.** The read it is owed cannot be transferred, because [ADR-227](ADR-227.md) section 2 transfers a read to an entry and there is no entry. **The cheap answer is to write one now**, retroactively, stating the handler's central claim so that it can be classified. **The expensive answer is to say that a retroactive entry written by a session that did not build the thing is a worse artifact than an honest gap**, and that is a defensible position this entry does not take on the founder's behalf.

  **THE SECOND IS WHETHER SECTION 4's LINE IS THE RIGHT ONE.** [ADR-186](ADR-186.md) and [ADR-187](ADR-187.md) rule adjacent literals in one constraint and land in different classes, on the ground that one kind is constrained by a posting's sign and the other by prose. **A founder who holds that both are judgements gets a residue of three rather than two**, and the direction of that correction is upward, which is the direction this session had no incentive to find. **A founder who holds that both are derivations gets one**, and section 5's fourth probe is the argument against that reading: 951 of 952 cases do not care.

  **THE THIRD IS THAT THIS SESSION'S CLASSIFIER READING DISAGREES WITH [ADR-244](ADR-244.md)'s.** That entry scored **47** of 53 as residue with 6 carrying no founder block; this transcription scores **53 of 53** on both halves, over the same fifty-three entries. **Neither number is defended here**, and [ADR-244](ADR-244.md) section 7.1 already ruled that no pattern over prose can ask section 3's question. **Two transcriptions of one pattern disagreeing is that ruling arriving as evidence rather than as an argument**, and it is the fourth time in this thread a pattern over prose has been caught.

  **WHERE THIS ENTRY IS MOST LIKELY WRONG, named rather than left to be found.** **Two of the seven turn on reading a conjunction's property half as its point rather than its choice half**, and both are named in place: [ADR-183](ADR-183.md), where the writing trigger could be called the point of the diff rather than the provisioning, and [ADR-193](ADR-193.md), where *"whether a chain is wrong at all"* could be called the point rather than the link-versus-operation separation. **If a reader takes the other reading on both, the residue in these two tiers is four of fifteen rather than two.** The other five do not turn on either reading.

  **AND THE SECOND-MOST LIKELY IS ABOUT DEPTH RATHER THAN CLASS, AND IT IS WORSE HERE THAN IN TIER 1.** **Five of the nine class B entries rest wholly or partly on assertions that read TEXT**: three migrations ([ADR-183](ADR-183.md), [ADR-187](ADR-187.md), [ADR-193](ADR-193.md)) and two that read rendered SQL ([ADR-157](ADR-157.md)) or a migration beside behaviour. **Nothing in tier 2 executes against a database, because there is none here**, and the four entries whose rulings are triggers and constraints were each watched running exactly once, by the session that wrote them. **Section 3.9 is the sharp case**: `LEDGER-C3` is a trigger, its whole behaviour is unreachable without PostgreSQL, and the assertion written here holds its TEXT. **That is worth more than nothing and much less than a run**, and the repair is the seeded-mutation harness and the integration stage that [ADR-244](ADR-244.md) section 9 and [`migrations.integration.test.ts`](../../packages/db/test/migrations.integration.test.ts) each name and neither builds.
