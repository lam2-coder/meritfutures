## ADR-440: the sentence four documents carry was never STALE, it was FALSE WHEN WRITTEN, and the line-neutral repair preserves the pointer without preserving what it lands on (2026-09-07, status: proposed)

**Row 440 of the 435s wave. Branch `claude/stale-sentence-line-neutral`, off `claude/wave-435s` at `69662904`, session 635.**

**THE REPAIR IS TAKEN AND IT IS LINE-NEUTRAL, MEASURED RATHER THAN ASSURED.** [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) is **4581 lines before and 4581 lines after**, `wc -l` on both sides of the diff, at **16 insertions and 16 deletions** over two blocks of **7** and **9** lines. `RI-15` and `RI-16` both **PASS before and after**, and both were watched RED on this branch to prove they read the two new citations rather than skipping them (section 6).

**AND THE COMMISSION'S OWN WORD IS THE FIRST THING THIS ROW HAD TO CORRECT.** [ADR-437](ADR-437.md) section 12 item 1 calls it *"the stale sentence"*. **It never went stale.** The comparison it denies landed on **2026-08-25** in `600814b7`; every copy of the sentence in this tree was written on **2026-08-30 or later**, and the two in `scoped-db.ts` on **2026-09-03** and **2026-09-07**. Section 4 is the commit-by-commit derivation. **Nothing drifted. Seven live sentences were false on the day their author typed them**, which is `RI-15`'s own phrase *"false the day it was written"* ([`repo-invariants.mjs:3644`](../../packages/tooling/checks/repo-invariants.mjs)) arriving on a claim about the suite instead of on a pointer.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `440`, session **635**, branch `claude/stale-sentence-line-neutral`, off `69662904`.
- **Fence, BY FILE:** [`packages/db/src/scoped-db.ts`](../../packages/db/src/scoped-db.ts), this file, and the registries. Everything else read only. **No migration, no workflow, no `apps/worker` file, no check file.**
- **Money:** **COMMENT-ONLY DIFF IN THE MONEY PATH'S OWN PACKAGE.** `packages/db` is the money path's package and the two repaired blocks govern the type every money read is taken through, so the row is treated as money path. **Not one executable line changed**: the diff is 32 lines of block comment and the compiler output is identical. Section 8 is the accounting. Integer cents, and no float enters a financial path here because no expression here changed.

---

### 1. The container, measured rather than assumed

**THE DISPATCH'S CONTAINER SENTENCE IS NOW TWENTY-TWO ROWS OLD AND IS STILL RIGHT.** The container arrived with **no repository** and cloning it was the first act of this row. `gh` is absent.

| Fact | Value |
| --- | --- |
| `node` | `v22.22.2` |
| `pnpm` | `10.33.0`, `pnpm install --frozen-lockfile` clean in 7.7s |
| Cores | 4 |
| Memory | 15 GiB total |
| Repository on arrival | **absent** |

**THE FIRST `git push` WAS REFUSED `403` AND THE DISPATCH'S OWN REMEDY WORKED FIRST TIME**, *"not in this session's authorized repository set"*, then `add_repo` with `access: "push"`, then the same push. **No pre-check was taken against the remote.** This is the same sequence [ADR-437](ADR-437.md) section 1 reports, confirmed a second time.

**BOTH RUNNER PATHS THE DISPATCH NAMES EXIST AND BOTH RAN.**

---

### 2. The base, reproduced before the first edit, and it IS GREEN

| Leg | On `69662904`, before any edit |
| --- | --- |
| `pnpm vitest run` | **342 files, 8310 passed, 11 skipped, 8321 total**, 82.84s, 0 failed |
| `pnpm run typecheck` | exit 0 |
| `pnpm run lint` | exit 0 |
| `pnpm run format:check` | clean |
| `node scripts/corpus/gates.mjs check` | **33 of 33 gates pass** |
| `node packages/tooling/checks/repo-invariants.mjs` | **35 of 35 invariants hold**, `RI-15` and `RI-16` among them |

**`CI-06f` IS GREEN ON THIS BASE**, which [ADR-437](ADR-437.md) section 11 could not say on its own: rows `435` and `436` are now allocated, so the hole that entry reported is closed by the two rows that filled it rather than by anything here. Section 10 item 2 is what this row's own claim does to it.

**`pnpm run verify` WAS NOT RUN AND `scripts/corpus/falsify.mjs` WAS NOT RUN.** Both are prohibited by the dispatch.

---

### 3. The population, DERIVED, and it is not four

**THE DISPATCH AND [ADR-437](ADR-437.md) SECTION 5 BOTH SAY FOUR PLACES. THE LIVE POPULATION IS SEVEN SOURCE SITES AND THE SENTENCE IS IN SEVENTEEN PLACES ALTOGETHER.** Derived with one grep over the whole tree for the substring `` compares a `schema.ts` column ``, then every hit opened.

| Site | Says it as TRUE | In this fence |
| --- | --- | --- |
| [`scoped-db.ts:3446`](../../packages/db/src/scoped-db.ts), `CatalogRow` limit 1 | yes, until this row | **yes, REPAIRED** |
| [`scoped-db.ts:4573`](../../packages/db/src/scoped-db.ts), `DeclaredRow`'s docblock | yes, until this row | **yes, REPAIRED** |
| [`evaluate.ts:139`](../../apps/worker/src/breaker/evaluate.ts) | yes | no |
| [`sweep.ts:387`](../../apps/worker/src/recon/sweep.ts) | yes | no |
| [`recon-sweep.test.ts:218`](../../apps/worker/test/recon-sweep.test.ts) | yes | no |
| [`recon-sweep.test.ts:773`](../../apps/worker/test/recon-sweep.test.ts) | yes | no |
| [`digests.test.ts:684`](../../apps/worker/test/digests.test.ts) | yes | no |
| [`ADR-299.md:119`](ADR-299.md) section 5.1 item 5 | yes | no, and it is a DATED RECORD |
| [`ADR-303.md:45`](ADR-303.md) | yes | no, DATED |
| [`ADR-426.md:9`](ADR-426.md) | yes | no, DATED |
| [`ADR-432.md:134`](ADR-432.md) | yes | no, DATED |
| [`STATE.md:11282`](../STATE.md) | yes | **prose in a file granted for GENERATOR OUTPUT ONLY** |
| [`sessions/README.md:852`](../sessions/README.md), [`session-627.md:9`](../sessions/2026-09-07-session-627.md) | yes | no, session records |
| [`ALLOCATION.md:429`](ALLOCATION.md), [`sessions/README.md:857`](../sessions/README.md), [`session-632.md:9`](../sessions/2026-09-07-session-632.md) | **no, they report it FALSE** | row `440`'s cell only |

**THE TWO LOAD-BEARING COPIES ARE THE TWO THE DISPATCH NAMES AND THE REASON IS RIGHT.** They are what a session narrowing a port stands in front of: `CatalogRow`'s limits are the file's own statement of what the type does not buy, and `DeclaredRow`'s docblock is the declaration every narrowed accessor resolves to. **Both were repaired. The other five source sites are outside this fence and are NAMED AND LEFT**, per section 10 item 1.

**A GREP FOR THAT SUBSTRING STILL RETURNS SEVENTEEN SITES AFTER THIS ROW, AND TWO OF THEM ARE NOW CORRECTIONS RATHER THAN CLAIMS.** That is `RI-14`'s idiom working as designed and it is stated here so the next row's grep does not read as a failed repair.

---

### 4. What the sentence claims, why it is false, and WHEN it became false

**THE CLAIM, IN ITS FOUR WORDINGS.** *"no check in this tree compares a `schema.ts` column TYPE against the DDL"* (`scoped-db.ts`, `ADR-299`, `ADR-303`, `ADR-426`, `digests.test.ts`), *"nothing in this tree compares..."* (`sweep.ts`, `ADR-432`), *"nothing here compares..."* ([`recon-sweep.test.ts:218`](../../apps/worker/test/recon-sweep.test.ts)), *"nothing compares..."* (`sessions/README.md`). One claim, four spellings, which is why no single literal grep is a control (section 9).

**THE COMPARISON EXISTS AND WAS OPENED AT SOURCE RATHER THAN INHERITED FROM [ADR-437](ADR-437.md).**

| What is compared | Where | Population |
| --- | --- | --- |
| Column TYPE | [`scoped-db.test.ts:2733`](../../packages/db/test/scoped-db.test.ts), `tsType(column)` against `ddlType(def)` | every column of every registered non-view relation, one named case per relation |
| Column NULLABILITY | [`scoped-db.test.ts:2738`](../../packages/db/test/scoped-db.test.ts), `column.notNull` against `declaredNotNull(def)` | the same |
| Non-degeneracy | [`scoped-db.test.ts:2754`](../../packages/db/test/scoped-db.test.ts) | asserts the reader visited every column the transcription declares |
| Column DEFAULT | **nowhere** | [`scoped-db.test.ts:2520`](../../packages/db/test/scoped-db.test.ts)'s `TYPE_ENDS_AT` cuts `DEFAULT` off the DDL text before the comparison, deliberately |

**AND THE DATES ARE THE FINDING.** `git log -S` over each site, run on this branch:

| Site | Commit that wrote it | Date | Comparison already in the tree at that commit |
| --- | --- | --- | --- |
| the comparison itself | `600814b7`, *"ADR-094 section 3's comparison: column TYPE and NULLABILITY are asserted"* | **2026-08-25** | n/a |
| [`ADR-299.md:119`](ADR-299.md) | `e812e5ae` | 2026-08-30 | **yes**, verified by `git show e812e5ae:packages/db/test/scoped-db.test.ts` |
| [`scoped-db.ts:3446`](../../packages/db/src/scoped-db.ts) | `9363ef8a` | 2026-09-03 | **yes**, verified the same way |
| [`scoped-db.ts:4573`](../../packages/db/src/scoped-db.ts) | `2a47dccb` | 2026-09-07 | **yes** |
| `evaluate.ts`, `sweep.ts`, `recon-sweep.test.ts`, `digests.test.ts` | four commits | 2026-09-07 | **yes** |

**SO THE WORD "STALE" IS WRONG IN THE DISPATCH, IN [ADR-437](ADR-437.md) AND IN THIS ROW'S OWN BRANCH NAME, AND THE DIFFERENCE MATTERS FOR SECTION 9.** A stale sentence is one a later change falsified, and a control shaped like *"re-check every claim when the thing it describes moves"* would catch that. **Not one of these seven sentences was ever true.** No change-triggered control reaches them. What would reach them is a control that evaluates the claim ON THE DAY IT IS WRITTEN, which is a different and cheaper thing.

**AND THE SECOND COPY IS MISATTRIBUTED AS WELL AS FALSE.** [`scoped-db.ts:4573`](../../packages/db/src/scoped-db.ts) read *"ADR-112 foreclosure 4 records that no check in this tree compares a `schema.ts` column type against the DDL"*. [`ADR-112.md:92`](ADR-112.md) foreclosure 4 says nothing of the kind: it is about ADDRESSABILITY and EXHAUSTIVENESS, *"nothing yet makes the transcription complete -- the suite catches a key the transcription INVENTS, and does not require it to be exhaustive."* **Four of the five source sites outside this fence carry the same misattribution.** The repaired text says so.

**WHAT SURVIVES, AND IT IS WHY NEITHER BLOCK WAS SIMPLY DELETED.** The ruling both blocks carry, [ADR-299](ADR-299.md) section 5.1 item 5's *"the runtime guards must not be dropped on the strength of the declared type"*, **holds on a narrower and better footing**, which is [ADR-437](ADR-437.md) section 5's conclusion re-derived here rather than inherited: the comparison is against the **folded migration set as text**, a second transcription rather than the database; `ADR-112` foreclosure 4's exhaustiveness gap is untouched; and **DEFAULT is compared nowhere.**

---

### 5. The repair, and the constraint stated as a number

**BOTH BLOCKS WERE REWRITTEN IN PLACE AT THE SAME LINE COUNT**, on the idiom `RI-14` uses and on the worked example sitting four lines below the first one: [`scoped-db.ts:3461`](../../packages/db/src/scoped-db.ts), limit 4, writes `This read "..." ` then `` `RI-14` `` then what is true now. Both repairs copy that shape exactly.

| | `CatalogRow` limit 1 | `DeclaredRow` docblock |
| --- | --- | --- |
| Lines before | 3442-3448, **7** | 4571-4579, **9** |
| Lines after | 3442-3448, **7** | 4571-4579, **9** |
| Longest line before / after | 79 / 93 | 79 / 95 |
| Superseded sentence kept, quoted | **yes** | **yes** |

**THE FILE: `wc -l` READS 4581 ON `git show HEAD~1:packages/db/src/scoped-db.ts` AND 4581 ON THE WORKING FILE.** `git diff --numstat` is **`16  16`**. The budget came from character width and not from cutting the claim: the file's longest line was already **100** and limit 4 already runs to **91**, so reflowing at 93 and 95 bought **129 characters** in the first block and **209** in the second, which was enough to add the quotation, the correction, the citation and the misattribution finding **without dropping a clause**. One clause was compressed: `DeclaredRow`'s closing sentence about [`digests/rows.ts`](../../apps/worker/src/digests/rows.ts) now reads *"spent it, deleting a mapping and keeping refusals"* rather than naming both halves at length. **That is the cost, stated, and it is the cost [`repo-invariants.mjs:3010`](../../packages/tooling/checks/repo-invariants.mjs) records three earlier entries paying.**

**AND THE ESTATE ALREADY RULED THAT LINE-NEUTRALITY IS NOT THE PROPERTY, WHICH THE DISPATCH DOES NOT SAY AND A FROZEN DOCUMENT DOES.** [`repo-invariants.mjs:3008-3016`](../../packages/tooling/checks/repo-invariants.mjs): *"WHAT THIS GRAMMAR COSTS THE PROSE AROUND IT IS RULED BY ADR-386 AND THE RULING IS NOT LINE-COUNT NEUTRALITY. Nine sessions held a diff to equal insertions and deletions so no pointer would drift ... That property is neither necessary nor sufficient. THE ONE THAT IS BOTH IS THAT NO CITED LINE MOVES."* **This row honoured the stronger constraint anyway**, because it is sufficient, because it is what the dispatch commissioned, and because it was satisfiable without loss. **A later row repairing one of the five copies outside this fence should read that ruling first and not assume it must count lines.**

**THE 429 IN [ADR-437](ADR-437.md) SECTION 12 ITEM 1 IS A REAL NUMBER MEASURING A DIFFERENT THING, AND CONFIRMING IT WAS THIS ROW'S INSTRUCTION.** Derived here at the moment of reporting: `scoped-db.ts:NNNN` citations occur **447 times over 155 files**, against that entry's **429 over 153** and [ADR-426](ADR-426.md) section 8's **367 over 140**. **But the number a one-line insertion at limit 1 would MOVE is 134, not 447**, because 134 of the 447 point at a line below 3441; at the `DeclaredRow` docblock the figure is **4**. The trap is real and it is a third the size the headline suggests, which is exactly the sibling-measurement error class the dispatch warned about, arriving inside the warning.

**AND LINE-NEUTRALITY PRESERVES THE POINTER WITHOUT PRESERVING WHAT IT LANDS ON, WHICH IS THE COST NOBODY HAS WRITTEN DOWN.** Nine citations name `scoped-db.ts:3446` or `:4573` today, in [ALLOCATION.md](ALLOCATION.md), [`sessions/README.md`](../sessions/README.md), [`session-632.md`](../sessions/2026-09-07-session-632.md) and [ADR-437](ADR-437.md). All nine still resolve and all nine still land inside the repaired block. **`:4573` still holds the quoted sentence, so a reader following it meets the claim and its correction together. `:3446` no longer does**: it now holds the middle of the correction, so a reader following *"the stale sentence is at `:3446`"* meets the repair rather than the sentence. **Every one of the nine is in a DATED RECORD**, which [`repo-invariants.mjs:3013`](../../packages/tooling/checks/repo-invariants.mjs) rules is NAMED rather than repaired. They are named here.

---

### 6. Proof, not assertion

**`RI-15` AND `RI-16` PASS AND WERE WATCHED READING THE NEW CITATIONS.** A repair that merely left them green would not prove they had looked at it.

| Leg | Before the edit | After the edit |
| --- | --- | --- |
| `RI-15` | **PASS** | **PASS** |
| `RI-16` | **PASS** | **PASS** |
| Whole check | **35 of 35** | **35 of 35** |

**THE RED WAS WATCHED, ON THIS BRANCH, AND REVERTED BYTE-IDENTICALLY.** Both new citations were rewritten from `` `scoped-db.test.ts:2728` `` to `` `scoped-db.test.ts:99999` `` against a file of 3652 lines. `RI-15` went **FAIL with 2 findings**, `34 of 35`. Restored, and the file is byte-identical to the commit: **`wc -l` 4581, both citations back, 35 of 35.** **So `RI-15` reads both of the two citations this row added, and neither is a pointer nothing checks.**

**NO CITED LINE MOVED**, verified positively rather than by the diffstat alone: [`scoped-db.ts:3440`](../../packages/db/src/scoped-db.ts) is still the `FOUR LIMITS` header, [`:3449`](../../packages/db/src/scoped-db.ts) is still limit 2's opener (which [ADR-437](ADR-437.md) section 7 reason 2 cites), [`:3461`](../../packages/db/src/scoped-db.ts) is still limit 4's `This read` (cited by [ALLOCATION](ALLOCATION.md) row `437`), and [`:4581`](../../packages/db/src/scoped-db.ts) is still `export type DeclaredRow` (cited by [ADR-432](ADR-432.md)).

---

### 7. What the repaired text says

**LIMIT 1**, [`scoped-db.ts:3442-3448`](../../packages/db/src/scoped-db.ts), reads: the guards do not come off; *"This read \"no check in this tree compares a `schema.ts` column TYPE against the DDL.\" `RI-14` (ADR-440): FALSE when written."*; then the comparison at [`scoped-db.test.ts:2728`](../../packages/db/test/scoped-db.test.ts) over every column of every registered non-view relation; then the three facts that keep the ruling standing, `ADR-112` foreclosure 4, MIGRATION TEXT rather than the database, and DEFAULT compared nowhere; then the original closing sentence, unchanged: *"A caller that reads money off one of these rows still checks the value it read."*

**THE `DeclaredRow` DOCBLOCK**, [`:4571-4579`](../../packages/db/src/scoped-db.ts), does the same and adds the misattribution: *"foreclosure 4 is about EXHAUSTIVENESS."* Its ruling sentence, `ADR-299` section 5.1 item 5, and its `STILL CHECKS THE VALUE` conclusion are both unchanged.

**NEITHER BLOCK NOW ASSERTS ANYTHING THIS ROW DID NOT OPEN.** The comparison, the population, the `TYPE_ENDS_AT` cut and `ADR-112.md:92` were each read at primary source.

---

### 8. Money

**COMMENT-ONLY, IN THE MONEY PATH'S OWN PACKAGE, AND THE DISPATCH ASKED TO HEAR IT LOUDLY EITHER WAY.** The diff is **32 lines, every one of them inside a `/** */` block**. No declaration, no signature, no expression, no export moved. `pnpm run typecheck` is exit 0 and the emitted types are the same types. **`DeclaredRow` and `CatalogRow` resolve to exactly what they resolved to on the base**, which is the property that would make this a money diff if it were false.

**AND THE SUBJECT IS AS MONEY-ADJACENT AS [ADR-437](ADR-437.md)'s WAS.** Both repaired sentences are the paragraph a session reads before deciding whether to drop a runtime guard on a money read. **A false sentence there is the one that makes a guard look better-supported than it is, in the direction that argues for KEEPING guards** -- so this particular falsehood was failing safe, and the cost is the one [ADR-437](ADR-437.md) section 13 names: it makes the transcription look weaker than it is, so the next row's argument is built on the wrong reason. **Integer cents. Not one `throw` deleted, not one refusal retired, because nothing executable is in this diff.**

---

### 9. Why a comment can go stale here with nothing noticing, and whether three instances justify a control

**THE PLAIN ANSWER: NOTHING IN THIS RUN READS A COMMENT'S CLAIM AGAINST THE THING IT DESCRIBES, AND THAT IS TRUE OF EVERY LEG, NOT OF SOME.**

| Leg that opens these files | What it asks | Why it cannot see this |
| --- | --- | --- |
| `RI-15` | does a `file:line` pointer RESOLVE, and is a name within 2 lines of it | **LOCATION, never truth.** The sentence carried no pointer at all |
| `RI-16` | the same, in live documents | the same, and ADR sites are excluded by shape |
| `RI-14` | does a claim that a NAMED thing does not exist survive, against the tree's exports | the sentence names no export. A CHECK is not an export |
| `RI-35` / `absence-claims.mjs` | is a REGISTERED absence claim still absent, per a probe | the sentence was never registered, so no leg of it runs |
| `CI-06/derivable-counts` | does a prose sentence under `docs/` state a derivable COUNT | this is not a count, and five of the seven sites are not under `docs/` |
| `tsc`, `eslint`, `prettier`, `vitest` | nothing about prose | a block comment is whitespace to all four |

**SO THE HOLE IS NOT THAT A CONTROL DEGRADED. IT IS THAT A CLAIM ABOUT THE SUITE, WRITTEN IN A FILE THE SUITE COMPILES, IS ADDRESSED BY NO RUNNER AT ALL.** That is the class [ADR-434](ADR-434.md) section 6 named and priced at seven register entries, and [ADR-436](ADR-436.md) section 7 named again and priced at one `RI-nn` plus a six-row registry. **This is the third instance in seven rows and it is the worst of the three by every measure taken**: seven live sites rather than one or two, four different wordings, five of the seven still standing after this repair, and **7 of 7 false** against [ADR-436](ADR-436.md) section 5's base rate of 2 of 6.

**AND THIS INSTANCE DIFFERS FROM ITS TWO PREDECESSORS IN THE ONE WAY THAT CHANGES THE ANSWER.** [ADR-434](ADR-434.md) section 6 refused its control because *"IT FORCES REGISTRATION AND NOT TRUTH"*: the probe there was already answering correctly, so a registered claim would have been NAMED and never RED. [ADR-436](ADR-436.md) section 7.1 (b) refused a general reader on `RI-14`'s own design note at [`repo-invariants.mjs:1882`](../../packages/tooling/checks/repo-invariants.mjs), because an import claim is a claim about behaviour. **Neither objection reaches this claim.** *"No check compares a `schema.ts` column type against the DDL"* is precisely `RI-14`'s shape -- a claim that a NAMED thing does not exist -- with a CHECK in the place where `RI-14` requires an export. **It is mechanically decidable, by a probe of about four lines, and that probe flips.**

#### 9.1 The control, priced

**CANDIDATE A: ONE `absence-claims.mjs` ARTIFACT PLUS ONE CLAIM-SITE ROW PER LIVE SENTENCE.** The register already has this exact shape, at [`absence-claims.mjs:815-839`](../../packages/tooling/checks/absence-claims.mjs) for the artifact and [`:1806-1812`](../../packages/tooling/checks/absence-claims.mjs) for a site.

| what it needs | size, derived from the entries that exist |
| --- | --- |
| one artifact, `schema-type-vs-ddl-comparison`, with a two-half probe reading `tsType(` and `ddlType(` out of [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) | **about 20 lines**, on `db-pool-sql-executor`'s shape at [`:815`](../../packages/tooling/checks/absence-claims.mjs) |
| one claim-site row per live sentence, **five today** after this row, or seven had it existed on 2026-08-25 | **about 7 lines each**, on the shape at [`:1806`](../../packages/tooling/checks/absence-claims.mjs) |
| a seeded counterfactual per site, watched failing and restored byte-identically | five seeds, this tree's standing discipline for a new check |
| the pin that keeps the register honest | the problem, and section 9.2 |
| fence | `packages/tooling/checks/**` and its suite, which is nobody's this wave and is a row of its own |

**IT WOULD HAVE GONE RED, AND THAT IS THE SENTENCE NEITHER PRECEDENT COULD WRITE.** The probe answers `present` from 2026-08-25 onward. A claim site registered as `live` against a `present` artifact is a finding on the next run. **Had the register carried [`ADR-299`](ADR-299.md)'s sentence when it was written on 2026-08-30, `9363ef8a`, `2a47dccb` and the four `apps/worker` commits could not have copied it into six more files without a red run.**

**CANDIDATE B: A LITERAL GREP, IN A CASE, FOR THE SENTENCE.** **REFUSED, AND SECTION 4 IS THE MEASUREMENT.** Four wordings over seven sites; a grep on the one stable substring `` compares a `schema.ts` column `` matches **17 sites today, two of which are this row's corrections and four of which report it false**, so the case needs an allowlist, and an allowlist is candidate A with the probe removed and the honesty removed with it. It is also [ADR-436](ADR-436.md) section 6's *"mechanism running backwards"*: a case shaped `toContain(sentence)` pins a false sentence in place forever.

**CANDIDATE C: A GENERAL CHECK THAT READS ANY COMMENT'S CLAIM ABOUT ANY CHECK.** **REFUSED WITHOUT A NUMBER**, on the same authority [ADR-436](ADR-436.md) invoked: [`repo-invariants.mjs:1882`](../../packages/tooling/checks/repo-invariants.mjs).

#### 9.2 The recommendation, and the argument against it

**YES: THREE INSTANCES JUSTIFY A CONTROL, AND IT IS CANDIDATE A, AND IT IS A ROW OF ITS OWN AND NOT THIS ONE.** A control landing in the diff of the defect it would have caught has no counterfactual, which is this tree's standing rule and [ADR-436](ADR-436.md) section 7's own reason for deferring.

**THE ARGUMENT AGAINST, STATED SO THE ROW THAT TAKES IT ARGUES WITH SOMETHING.** The register is hand-maintained and its staleness is checked only by a pin that can ask whether a row EXISTS, not whether its quoted sentence is still the sentence in the file. **That is this defect class arriving inside its own repair**, and it is the objection [ADR-436](ADR-436.md) section 7.2 raises against its registry too. **The difference is that the probe here is not prose**: a row can go stale, and the probe still answers from the tree, so a stale row degrades to a MISSED site rather than to a false green on a site that is covered.

**AND THE QUESTION THAT ROW MUST SETTLE AND THIS ONE CANNOT: HOW MANY SENTENCES OF THIS SHAPE ARE THERE?** *"No check in this tree does X"* is a phrasing this corpus reaches for constantly and nobody has surveyed it. **The scope choice is one artifact for this claim, five rows, an afternoon; or a survey of every negative claim about the suite, which is a different and much larger row.** This row recommends the first, on [ADR-436](ADR-436.md) section 7.3's precedent, and recommends that the survey be commissioned separately rather than folded in.

**NOTHING WAS BUILT. NO `RI-nn` WAS MINTED, NO `CI-06` LETTER SPENT, NO REGISTER TOUCHED, NO CASE ADDED.**

---

### 10. What this row did NOT do

1. **THE FIVE COPIES OUTSIDE THIS FENCE WERE NAMED AND LEFT**, section 3: [`evaluate.ts:139`](../../apps/worker/src/breaker/evaluate.ts), [`sweep.ts:387`](../../apps/worker/src/recon/sweep.ts), [`recon-sweep.test.ts:218`](../../apps/worker/test/recon-sweep.test.ts) and [`:773`](../../apps/worker/test/recon-sweep.test.ts), [`digests.test.ts:684`](../../apps/worker/test/digests.test.ts). All five are under `apps/worker/**`, which this fence holds READ ONLY. **The same constraint is satisfiable there** -- each is a block comment with the same width headroom -- **and this row did not widen its fence to be thorough.** The four ADR copies and [`STATE.md:11282`](../STATE.md) are dated records or prose in a file granted for generator output only, and [`repo-invariants.mjs:3013`](../../packages/tooling/checks/repo-invariants.mjs) rules that a pointer in a dated record is NAMED rather than repaired.
2. **NO ALLOCATION NUMBER WAS INVENTED AND NO RESERVATION WAS WRITTEN FOR `438` OR `439`, WHICH LEAVES `CI-06f` RED AND IS THE ESTABLISHED ANSWER RATHER THAN THIS ROW'S.** `437` is the highest claimed row on this base and this row claims `440`, so `438` and `439` are holes and `CI-06f` asserts gaplessness over allocated PLUS reserved. **Four consecutive rows have already met this and all four reported rather than repaired**: [ALLOCATION](ALLOCATION.md) row `434`'s cell records it for `432` and `433`, and [ADR-437](ADR-437.md) section 10 item 4 for `435` and `436`, on row `423`'s own words that a reservation's subject is *"to be set at dispatch and NOT invented here"*. **This row's dispatch names the two concurrent rows' FENCES and not their subjects**, so a reservation written here would be invented, and the fence grants this row's own cell alone.
3. **NO GATE WAS WEAKENED, NO TEST SKIPPED, DISABLED OR QUARANTINED, NO EXISTING CASE INVERTED OR LOOSENED.** The suite total is unchanged in both directions, section 11.
4. **NO CASE WAS ADDED FOR THIS REPAIR.** A case that asserts a comment's text is candidate B, refused in section 9.1.
5. **ONE FILE OUTSIDE THE FENCE'S PROSE WAS CHANGED, BY THE GENERATOR THE DISPATCH REQUIRES.** `node scripts/corpus/gates.mjs generate` rewrote the span NAMED `adr_count` in [`docs/STATE.md`](../STATE.md), because this row adds an ADR file and that span counts them. **No prose in that file was touched**, which is what makes [`STATE.md:11282`](../STATE.md)'s copy of the sentence item 1's rather than this row's.
6. **`scoped-db.ts`'s OTHER 4,549 LINES WERE NOT TOUCHED**, and no accessor, verb, type or export in this package moved.

---

### 11. Every leg, re-run after the last edit

**COUNTS DERIVED AT THE MOMENT OF REPORTING AND NOT CARRIED FROM AN EARLIER RUN.**

| Leg | Result |
| --- | --- |
| `pnpm vitest run` | **342 files, 8310 passed, 11 skipped, 8321 total.** **Base 8321, mine 8321, delta 0.** Skipped **11 before and 11 after**; files **342 before and 342 after**; **0 failed** |
| `pnpm run typecheck` | exit 0 |
| `pnpm run lint` | exit 0 |
| `pnpm run format:check` | clean |
| `node scripts/corpus/gates.mjs generate` | rewrote one span, NAMED `adr_count`, in [`docs/STATE.md`](../STATE.md), plus one row in each of the two generated registry tables. Nothing else moved |
| `node scripts/corpus/gates.mjs check` | **32 of 33 gates pass.** The one failure is `CI-06f`, *"ADR-438 is neither present nor reserved (a hole)"* and the same for `ADR-439`, section 10 item 2 |
| `node packages/tooling/checks/repo-invariants.mjs` | **35 of 35 invariants hold**, **`RI-15` PASS and `RI-16` PASS**, unmoved from the base |
| `wc -l packages/db/src/scoped-db.ts` | **4581 before, 4581 after** |

**THE SKIPPED COUNT IS 11 AND NOTHING IN THIS DIFF CHANGES WHAT RUNS.** No test file is in it.

**`pnpm run verify` WAS NOT RUN AND `scripts/corpus/falsify.mjs` WAS NOT RUN.**

**THE BARREL WAS NOT TOUCHED, SO NO LEG COUNT IS OWED.** [`packages/db/src/index.ts`](../../packages/db/src/index.ts) is not in this row's diff.

**FILES CHANGED BY THIS ROW:** [`packages/db/src/scoped-db.ts`](../../packages/db/src/scoped-db.ts), this file, [ALLOCATION](ALLOCATION.md) row `440` only, [`docs/decisions/README.md`](README.md), [`docs/sessions/README.md`](../sessions/README.md), [`docs/sessions/2026-09-07-session-635.md`](../sessions/2026-09-07-session-635.md), and one generated span in [`docs/STATE.md`](../STATE.md). **No migration. No workflow. No check file. No `apps/` file.**

---

### 12. What is owed, stated precisely enough to be picked up

1. **FIVE COPIES OF THE SENTENCE ARE STILL LIVE AND ALL FIVE ARE UNDER `apps/worker/**`**, section 10 item 1. Each is a block comment with the same width headroom, so the line-neutral repair is available on all five, and a row holding `apps/worker/**` can take all of them in one commit.
2. **THE CONTROL IS WORTH BUILDING AND IS PRICED AT ONE ARTIFACT, FIVE CLAIM-SITE ROWS AND FIVE SEEDS**, section 9.1. It needs `packages/tooling/checks/**` and its suite. **It must not be bolted onto a repair.**
3. **NOBODY HAS SURVEYED HOW MANY SENTENCES IN THIS TREE CLAIM "NO CHECK DOES X"**, section 9.2, and that survey decides whether the control is one artifact or a family.
4. **[ADR-437](ADR-437.md) SECTION 12 ITEM 1's "429 CITATIONS" IS A COUNT OF THE WHOLE CITATION POPULATION INTO `scoped-db.ts` AND NOT OF WHAT AN EDIT WOULD MOVE.** It is **447 over 155 files** today, and the number an insertion at limit 1 would move is **134**, section 5. **The instruction that follows from it is unchanged**; only its size is.
5. **`ADR-386`'s RULING, AT [`repo-invariants.mjs:3008`](../../packages/tooling/checks/repo-invariants.mjs), IS THAT LINE-COUNT NEUTRALITY IS NEITHER NECESSARY NOR SUFFICIENT.** Two dispatches in a row have commissioned it as the constraint. **The next one should commission "no cited line moves" instead**, which is weaker, is the ruled property, and would have let this repair use more room than it needed.
6. **NOTHING COMPARES A `schema.ts` DEFAULT AGAINST THE DDL**, which is [ADR-437](ADR-437.md) section 12 item 2 and is untouched here. It is now the ONLY leg of the transcription with no comparison, and both repaired blocks say so.
7. **[ADR-432](ADR-432.md) SECTION 10 ITEM 1's CITATION IS STILL OFF BY ONE**, [ADR-437](ADR-437.md) section 10 item 3, and it is still not repaired.
8. **`CI-06f` IS RED AT `438` AND `439`**, section 10 item 2.

---

### 13. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** Inherited as well as owed: [ADR-299](ADR-299.md), [ADR-303](ADR-303.md), [ADR-426](ADR-426.md), [ADR-432](ADR-432.md), [ADR-434](ADR-434.md), [ADR-436](ADR-436.md) and [ADR-437](ADR-437.md) are `proposed` and unsigned, and this row settles none of them.

  **What a founder read adds and this entry cannot.**

  **THE FIRST THING TO DECIDE IS WHETHER THIS PROGRAMME WANTS A CONTROL ON SELF-DESCRIPTION, BECAUSE THREE ROWS HAVE NOW FOUND THE SAME DEFECT AND ALL THREE FOUND IT BY READING.** [ADR-434](ADR-434.md) priced its hole and refused. [ADR-436](ADR-436.md) priced its hole and recommended a row. **This one is the first where the claim is mechanically decidable and where the probe would actually have flipped**, section 9. A founder who does not want that row should say so, because the alternative is that the fourth instance is found by reading too, and the fourth costs whatever it costs.

  **THE SECOND IS THAT THIS WAS NEVER STALENESS AND THE WHOLE ESTATE HAS BEEN CALLING IT THAT.** Section 4. Seven sentences, four wordings, six files, and **not one of them was ever true**. That changes what a control has to be: a change-triggered re-check catches nothing here, and what is needed is something that evaluates a claim the day it is written. **A founder should decide whether the corpus's habit of asserting what the suite does NOT do is worth keeping at all**, given that this is the second time in a fortnight that such an assertion was wrong in the direction of understating the tree.

  **THE THIRD IS THAT THE RULING SURVIVED AND THE ROW DID NOT WEAKEN A GUARD.** Both repaired blocks still say the runtime guards stay, on a narrower and better footing: the comparison is against migration TEXT, exhaustiveness is still open, and DEFAULT is compared nowhere. **No caller was told it may stop checking money.** A founder reading only the diff should see that this is the one direction this repair could have gone wrong in and did not.

  **THE FOURTH IS THAT TWO DISPATCHES IN A ROW HAVE COMMISSIONED A CONSTRAINT THE ESTATE ALREADY RULED IS NOT THE RIGHT ONE**, section 12 item 5. It cost nothing here. **It will cost the row that repairs the five `apps/worker` copies, if that row is told to count lines rather than to keep cited lines still.**

- **Approval:** `___________________________` (UNSIGNED. **COMMENT-ONLY DIFF IN THE MONEY PATH'S OWN PACKAGE. `E2` READ OWED, ON THE FILES NAMED IN SECTION 11.** **Merging is not signing.**)

|                 |     |
| --------------- | --- |
| **Approved by** |     |
| **Date**        |     |
