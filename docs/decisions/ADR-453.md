## ADR-453: the fold paragraph's ARGUMENT is checked and holds of all fifteen, two further sentences beside it are FALSE about this tree, and the `either` paragraph's seven are this file's `.references()` rather than the DDL's (2026-09-08, status: proposed)

**Row 453 of the 452s wave. Branch `claude/schema-fold-paragraph`, off `origin/claude/wave-452s` at `6fb9147b`, session 648.**

**THE COMMISSIONED ITEM IS TAKEN AND THE UNCOMMISSIONED ONE BESIDE IT IS TAKEN TOO.** [ADR-449](ADR-449.md) section 9 item 1 is discharged: `schema.ts:81`'s two numerals are corrected, its ARGUMENT is checked against the four tables it did not name and HOLDS of all of them, and both are bound by cases that recompute them. [ADR-449](ADR-449.md) section 9 item 4 is discharged in the same diff because it was cheap: the `either` paragraph's seven are DERIVED, and the population question that row *"narrowed without evidence either way"* is settled with a witness.

**AND THE PARAGRAPH THIS ROW WAS SENT TO CORRECT CARRIED TWO MORE FALSE SENTENCES THAN THE ONE IT WAS SENT FOR.** `schema.ts:70` called `RENAME` an offender that turns the suite red while [ADR-278](ADR-278.md) folds it and [`0075`](../../packages/db/migrations/0075_calibration_observed_on.sql) is one, on `simulation_runs`, which is registered. `schema.ts:86` called `otp_challenges` the only carrier that also takes an `ALTER COLUMN` while [`0067`](../../packages/db/migrations/0067_rule_state_phase_vocabulary.sql) retypes `rule_states.phase` under [ADR-216](ADR-216.md). **This is the third and fourth time one paragraph has outlived a ruling that widened the fold, and the paragraph narrates the first two in its own text.**

- **Origin:** [ALLOCATION](ALLOCATION.md) row `453`, session **648**, branch `claude/schema-fold-paragraph`, off `6fb9147b`. The row was RESERVED by the dispatcher with its subject written, and this row converts one cell.
- **Fence, BY FILE:** [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts), [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts), this file, [ALLOCATION](ALLOCATION.md) row `453` only, the registries, this session's log, [`docs/INDEX.md`](../INDEX.md) and [`docs/STATE.md`](../STATE.md) for `gates.mjs generate` output only. **`INDEX.md` was granted and not used**, section 10 item 7. **No migration, no `scope.ts`, no [`scoped-db.ts`](../../packages/db/src/scoped-db.ts), no file under `packages/tooling/`, no `apps/` file, no workflow.**
- **Money:** **NON-MONEY DIFF IN A MONEY-PATH FILE.** Nothing here declares, reads or computes a cents value. The tables the corrected sentence names include money tables (`payout_requests`, `wallet_withdrawals`, `liability_snapshots`, `promotional_credit_grants`), and what moves about them is a COMMENT and an assertion over that comment, never a column.

---

### 1. The container and the base, measured rather than assumed

**THE CONTAINER ARRIVED WITH NO REPOSITORY.** `/home/user` was empty and no clone was present. The repository was attached in-session with `access: "push"`, cloned once, and `pnpm install --frozen-lockfile` run before any figure below was taken. The clone is shallow, so `origin/claude/wave-452s` was fetched with an explicit refspec rather than assumed present.

**THE BASE IS `origin/claude/wave-452s` AT `6fb9147b`**, which is `wave-450s` plus one commit reserving rows `452` and `453`. It is not `main` and it is not `wave-450s`.

**THE BASE WAS REPRODUCED GREEN ON EVERY LEG BEFORE THE FIRST EDIT, and green includes `CI-06f`:** 33 of 33 gates, 35 of 35 invariants, and **344 test files / 8,612 passed / 11 skipped / 0 failed**. **`CI-06f` is green at this base and is green at every commit this row makes**, which the dispatch said to report as a finding if it were not. It is not a finding.

**THE DISPATCH'S FRAMING WAS RE-DERIVED AND ONE FIGURE IN IT IS WRONG ABOUT THIS BASE**, corrected in section 7 rather than absorbed. Its account of `ADR-449` section 9 item 1 is otherwise accurate, and its warning that a sibling ADR is not primary source is the warning that produced sections 5 and 6.

### 2. THE CARRIERS, DERIVED

The sentence at [`schema.ts:81`](../../packages/db/src/schema.ts) read *"ELEVEN of the 109 below carry later columns"*. Derived here over `packages/db/migrations`, `--` comments stripped, splitting on `;` and matching `ALTER TABLE <t> ... ADD COLUMN`:

| Figure | Header read | DERIVED | How |
| --- | --- | --- | --- |
| Tables carrying a later `ADD COLUMN` | 11 | **15** | the migration set |
| The denominator, *"the N below"* | 109 | **117** | relations DECLARED in `schema.ts` |
| Of the fifteen, REGISTERED | not stated | **15 of 15** | `TABLE_KEYS` |
| Of the fifteen, DECLARED in this file | not stated | **15 of 15** | `Object.values(schema).filter(is PgTable)` |

**THE FOUR THE OLD SENTENCE DID NOT NAME ARE `affiliate_commissions`, `firm_parameters`, `liability_snapshots` AND `wallet_withdrawals`**, which is what `ADR-449` derived and it reproduces exactly.

**THE DENOMINATOR IS 117 AND NOT THE 116 `ADR-449` NAMED, AND THAT IS A CORRECTION TO A SIBLING ENTRY RATHER THAN A DIFFERENT MEASUREMENT.** 116 is `TABLE_KEYS.length`, the REGISTERED population; 117 is the count of relations declared in this file, which is the population the word *"below"* denotes and the same figure [`schema.ts:8`](../../packages/db/src/schema.ts) already states as *"THE 117 DECLARATIONS IN THIS FILE"*. `ADR-449` is a DATED RECORD and [ADR-386:169](ADR-386.md) NAMES rather than repairs it, so it is named here. **The NUMERATOR is FIFTEEN under either population**, because the one relation that is declared and not registered, `live_account_state`, carries no later column; only the denominator moves.

### 3. THE ARGUMENT, WHICH IS THE HALF `ADR-449` SAID A ROW TAKING THIS OWES

`ADR-449` section 9 item 1: *"A row that takes it owes more than the numerals: the sentence argues that none of the eleven 'could be registered at all before ADR-094', and that argument has to be checked against the four before they join the list."*

**THE CLAIM IS A COUNTERFACTUAL ABOUT A SUPERSEDED RULE, AND IT IS TRUE OF A TABLE EXACTLY WHEN ITS `CREATE TABLE` BODY DISAGREES WITH ITS DECLARATION.** Before [ADR-094](ADR-094.md) a transcription was compared against the table's `CREATE TABLE` body alone. So a table could not have been registered under that rule exactly when the body it would have been compared against is missing something the declaration states.

**DERIVED PER TABLE, AND IT HOLDS OF ALL FIFTEEN INCLUDING THE FOUR.** For every one of the fifteen the `CREATE TABLE` column set is a PROPER SUBSET of the declared column set, and the difference is EXACTLY the set of names the later `ADD COLUMN`s add. No carrier has a column in its CREATE body that the declaration lacks, and no carrier's gap contains a name no `ADD COLUMN` supplies.

| Table | CREATE body | Declared | The gap, which is the later `ADD COLUMN` set |
| --- | --- | --- | --- |
| `admin_actions` | 11 | 13 | `initiative`, `on_behalf_of_identity_id` |
| `affiliate_commissions` | 10 | 11 | `affiliate_id` |
| `contact_channels` | 8 | 12 | `complained_at`, `value_ciphertext`, `value_encrypted_at`, `value_key_id` |
| `firm_parameters` | 6 | 8 | `dual_control_approval_id`, `id` |
| `identity_phones` | 19 | 22 | `phone_ciphertext`, `phone_encrypted_at`, `phone_key_id` |
| `liability_snapshots` | 8 | 9 | `funded_accounts` |
| `notification_kinds` | 10 | 11 | `rate_limit_exempt` |
| `otp_challenges` | 8 | 10 | `channel`, `destination_hash` |
| `payout_requests` | 24 | 29 | `held_at`, `hold_expires_at`, `hold_flag_id`, `hold_reason`, `hold_tos_clause` |
| `phone_change_requests` | 13 | 18 | `new_phone_ciphertext`, `new_phone_encrypted_at`, `new_phone_key_id`, `prior_notified_email_notification_id`, `prior_notified_sms_dispatch_id` |
| `plan_versions` | 12 | 15 | `decided_on_simulation_run_id`, `fee_back_repeats`, `simulation_waiver_reason` |
| `promotional_credit_grants` | 11 | 12 | `source_payout_request_id` |
| `rule_states` | 25 | 29 | `breach_kind`, `breached`, `calendar_revision_id`, `lifetime_settled_cents` |
| `sessions` | 14 | 17 | `auth_factor`, `elevated_at`, `elevated_by_factor` |
| `wallet_withdrawals` | 19 | 24 | `approved_at`, `approved_by`, `cancelled_at`, `dual_control_approval_id`, `dual_control_threshold_cents` |

**SO THE ARGUMENT IS SOUND AND THE FOUR JOIN THE LIST WITHOUT WEAKENING IT.** That is now a RUN and not a sentence: `every carrier disagrees with its own CREATE TABLE body, which is the argument` iterates the derived carrier set and asserts both directions per table.

**AND THERE IS ONE DISTINCTION THE OLD SENTENCE'S NEXT CLAUSE DOES NOT SURVIVE, WHICH IS WHY THE HEADER NOW STATES IT.** The clause reads *"which is why the ruling came before the transcription rather than after it"*, and it is a claim about ORDER. **All eleven originally named carry at least one `ADD COLUMN` from a migration that predates `ADR-094`** (`0029` under `ADR-039`, `0031`/`ADR-041`, `0034`/`ADR-046`, `0035`/`ADR-047`, `0041`/`ADR-066`, `0043`/`ADR-069`, `0044`/`ADR-070`, `0045`/`ADR-071`). **All four of the newly named take every one of theirs from a migration that POSTDATES it**: `0049`/`ADR-128`, `0070`/`ADR-232`, `0072`/`ADR-234`, `0076`/`ADR-284`, `0078`/`ADR-321`. `firm_parameters` is the sharpest case: [`0074`](../../packages/db/migrations/0074_firm_parameters.sql) CREATEs it under `ADR-126`, so before `ADR-094` the table did not exist and the counterfactual is over-determined. **For the four, the ruling came before the DRIFT and not merely before the transcription**, and the header now says so rather than folding them silently into a sentence about eleven.

### 4. THE SECOND FALSE SENTENCE IN THE SAME PARAGRAPH, AND IT NAMES A REGISTERED TABLE AS UNREGISTERABLE

`schema.ts:70` ON THE BASE read *"and `DROP COLUMN` and `RENAME` stay offenders that turn the suite red -- so"*, continuing *"the day one of those lands the check fails rather than silently reading a stale CREATE."* **The paragraph is rewritten in place, so that line number is base-relative and the struck sentence is kept at [`schema.ts:5382`](../../packages/db/src/schema.ts).**

**FALSE ABOUT `RENAME` ON THIS TREE.** [ADR-278](ADR-278.md) widened the fold's vocabulary to include `ALTER TABLE ... RENAME COLUMN`, and [`0075`](../../packages/db/migrations/0075_calibration_observed_on.sql) renames `simulation_runs.calibration_observed_at` to `calibration_observed_on`. `simulation_runs` is REGISTERED. The suite is green. Derived over the migration set: **one `RENAME` statement and zero `DROP COLUMN` statements.**

**THE FOLD'S VOCABULARY IS FOUR MEMBERS AND THE HEADER SAID TWO.** [`scoped-db.test.ts:868`](../../packages/db/test/scoped-db.test.ts), inside `ColumnFold`, says *"statements outside the fold's four-member vocabulary"*: `ADD COLUMN` ([ADR-094](ADR-094.md)), `ALTER COLUMN ... DROP NOT NULL` ([ADR-103](ADR-103.md)), `ALTER COLUMN ... TYPE` ([ADR-216](ADR-216.md)) and `RENAME COLUMN` ([ADR-278](ADR-278.md)). **`DROP COLUMN` is the only offender left**, with zero instances.

**THIRD FALSE SENTENCE, ONE CLAUSE ALONG.** `schema.ts:86` to `:87` ON THE BASE read *"`otp_challenges` IS THE ONLY ONE OF THE ELEVEN THAT ALSO CARRIES AN `ALTER COLUMN`"*, and that number is base-relative for the same reason. Derived: **two of the fifteen do**, and on different members. `otp_challenges` takes `DROP NOT NULL` in `0029`; `rule_states` takes `TYPE` in `0067` under `ADR-216`.

**AND THE PARAGRAPH NARRATES THIS EXACT FAILURE TWICE IN ITS OWN TEXT.** It records that the sentence read *"`ALTER COLUMN` STAYS AN OFFENDER"* until `ADR-106` and would have told a reader `otp_challenges` could not be registered, and that the same refusal survived in two table headers and *"COST A SECOND TABLE TEN WAVES"*. **It then did it again, about `RENAME`, and the entry that most recently rewrote the paragraph's neighbours did not notice.** Its own conclusion, *"A REFUSAL IN A COMMENT OUTLIVES THE RULING THAT SUPERSEDED IT"*, is correct and was not sufficient. What is added here is not a better sentence but `the header refuses the shapes the fold refuses and no others`, which asserts the `DROP COLUMN` instance count is zero and REFUSES the old wording by pattern.

### 5. THE `either` PARAGRAPH'S SEVEN, DERIVED, AND THE POPULATION QUESTION SETTLED WITH A WITNESS

[ADR-449](ADR-449.md) section 9 item 4: [`schema.ts:41`](../../packages/db/src/schema.ts) claims *"seven others carry both an identities edge and an accounts edge and every one of the seven declares its identity column NOT NULL"*, that row corrected the sentence's population from 115 to 116 and **did not derive the seven**, and a row that binds it *"also settles whether the sentence is about registered relations or about the DDL"*.

**BOTH ARE SETTLED AND THE ANSWER IS THE `.references()` OF THIS FILE.**

| Reading | Tables carrying BOTH edges | Minus `events` | Sentence |
| --- | --- | --- | --- |
| `schema.ts`'s `.references()` | **8** | **7** | **TRUE as written** |
| The DDL, any FK to `accounts` however added | **9** | **8** | FALSE as written |

**THE WITNESS IS `purchases` AND IT IS THE WHOLE DIFFERENCE.** [`0006`](../../packages/db/migrations/0006_commerce.sql) declares `parent_account_id uuid NULL` with no inline FK, and [`0007:178`](../../packages/db/migrations/0007_accounts.sql) adds it afterwards: `FOREIGN KEY (parent_account_id) REFERENCES accounts(id) ON DELETE RESTRICT`. The header's own rule at [`schema.ts:101`](../../packages/db/src/schema.ts) admits `.references()` **only** when the `CREATE TABLE` body declares the FK inline, so this file declares no accounts edge for `purchases` and the sentence's population excludes it. `0006`'s own comment names the cycle break: *"purchases.parent_account_id -> fk added in 0007_accounts"*.

**THE SEVEN, DERIVED off drizzle's own foreign-key metadata:** `account_adjustments`, `certificates`, `graduation_benefits`, `journal_entries`, `payout_requests`, `plan_size_unlocks` and `risk_flags`. **Every one of the seven declares its identity column NOT NULL**, so that half of the sentence holds, and it holds under the DDL reading too because `purchases.identity_id` is NOT NULL as well. **The only thing the reading changes is the count.**

**AND `ADR-449`'s NARROWING FROM 115 TO 116 TURNS OUT NOT TO MATTER, WHICH IS WORTH SAYING BECAUSE THAT ROW CALLED IT *"the one most likely to be wrong"*.** All nine both-edge tables in the DDL are declared here and all nine are registered, so the population figure in that sentence (116 registered, 117 declared, or 118 created) does not move the answer. **What moves the answer is the FK reading, and that is now stated in the header rather than left to be re-derived a third time.**

### 6. THE HEADER EDIT IS LINE-NEUTRAL, AND THAT IS FORCED BY A FILE NOBODY MAY EDIT AT ALL

**THE POPULATION WAS DERIVED FROM THE BASE AND NOT FROM THE WORKING TREE**, on the dispatch's instruction, because this entry's own pointers would otherwise be counted: `git grep -hoP 'schema\.ts[#:]L?\K[0-9]+' origin/claude/wave-452s -- docs packages apps scripts`.

| Figure | `ADR-449` on ITS base | **DERIVED on `6fb9147b`** |
| --- | --- | --- |
| Distinct cited lines into `schema.ts` | 70 | **78** |
| Files citing it | 64 | **67** |
| Cited lines INSIDE the header (1 to 114) | 8 | **15** |
| Cited lines BELOW the header | 62 | **63** |
| Highest cited line | 5351 | **5360** |

The eight extra lines and three extra files are `ADR-449`'s own, plus rows `450` and `451`. **The fifteen header lines are `4`, `6`, `8`, `13`, `15`, `20`, `23`, `26`, `33`, `41`, `46`, `49`, `81`, `109` and `112`.** No citer outside `docs`, `packages`, `apps` and `scripts` exists; the same grep over the rest of the tree returns nothing.

**AND THREE OF THE FIFTEEN ARE NOT POINTERS AT ALL.** `schema.ts:20` and `:23` in [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs), [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts) and [ADR-383](ADR-383.md) are SYNTHETIC FIXTURES and prose examples about what `RI-15` can and cannot see, written into temporary trees. **A grep for pointers cannot tell them from real ones**, which is [ADR-450](ADR-450.md)'s finding arriving from the other direction, and it means the header population is over-reported by any measurement of this shape, this one included.

**THE DECIDING FACT IS STRONGER ON THIS BASE THAN `ADR-449` COULD STATE IT.** Of the 67 citing files, **32 are ADRs and 22 are session logs**, both dated records. The rest are `apps/api/src/routes/accounts.ts`, `apps/worker/src/detectors/fills.ts`, `apps/worker/src/provisioning/vocabulary.ts`, [`STATE.md`](../STATE.md), [ALLOCATION](ALLOCATION.md), [`sessions/README.md`](../sessions/README.md), [`39-fixture-status-and-blockers.md`](../testing/golden-scenarios/39-fixture-status-and-blockers.md), [`DELTA_MANIFEST.md`](../../packages/db/DELTA_MANIFEST.md), [`schema.ts`](../../packages/db/src/schema.ts), [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts), the two `packages/tooling/` files, **and [`0067_rule_state_phase_vocabulary.sql`](../../packages/db/migrations/0067_rule_state_phase_vocabulary.sql), which cites `schema.ts:163`.**

**THAT MIGRATION IS THE ARGUMENT.** [ADR-386](ADR-386.md) requires a pointer in a live file to be REPAIRED. A merged migration is not merely outside this fence: **migrations are sacred, once merged never edited, only superseded (constitution E2)**, so a pointer inside one can NEVER be repaired by anybody. An edit that moved `schema.ts:163` would owe a repair that the constitution forbids. Three `apps/` files and two `packages/tooling/` files are outside this fence on top of that, and `packages/tooling/` is row `452`'s this wave. **Line-neutrality here is not a preference and not even only a fence constraint; below line 114 it is permanent.**

**WHAT WAS DONE, AND IT IS CHECKED RATHER THAN CLAIMED.**

| Fact | `schema.ts` | `scoped-db.test.ts` |
| --- | --- | --- |
| `wc -l` base / branch | 5458 / 5494 | 4918 / 5207 |
| `git diff --numstat` | `88 52` | `289 0` |
| Header lines 1 to 114 | **44 replaced by 44** | n/a |
| Prefix vs base | **lines 115 to 5381 byte-identical** | **lines 1 to 4917 byte-identical** |
| Distinct cited lines into the file | 78, highest **5360** | 49, highest real **4662** |
| **Cited lines moved** | **0** | **0** |

The 36 added lines in `schema.ts` are all BELOW 5381, which is below the highest cited line, 5360. **Every one of the fifteen header pointers was opened and read after the edit**, on the dispatch's warning that a pointer into a hot file can be silently wrong: `:4`, `:6`, `:8`, `:13`, `:15`, `:20`, `:23`, `:26`, `:33`, `:46`, `:49`, `:109` and `:112` are untouched lines, `:41` still opens *"It is the only table of that shape in the 116: seven others"*, and `:81` still opens the carriers sentence it was cited for.

### 7. ONE FIGURE IN THE DISPATCH IS WRONG ABOUT THIS BASE

The dispatch states that `ADR-449` derived *"70 cited lines over 64 files, 8 of them in the header (`4`, `6`, `15`, `20`, `23`, `49`, `109`, `112`), with 62 below it"* and instructs the row to derive its own. **The instruction is right and the inherited figures are stale by exactly the amount `ADR-449` itself added**: 78 over 67, 15 in the header, 63 below. **The dispatch's own instruction is what catches it**, and it is recorded here rather than absorbed because the same eight-in-header figure has now been quoted forward twice.

Its account of the fence, the base, the reserved rows and the stop condition reproduced exactly. **`CI-06f` is green at the base and stayed green**, as the dispatch predicted, so no finding is owed there.

### 8. WHAT THE HEADER EDIT COST, STATED RATHER THAN ABSORBED

**`RI-14` GOVERNS AND THE THREE STRUCK SENTENCES ARE KEPT**, at [`schema.ts:5382`](../../packages/db/src/schema.ts), beside the declaration that moved them and beside `ADR-449`'s four, which is this file's own one-row-old precedent for reading `RI-14`'s *"beside"* as beside the DECLARATION. Each is recorded with what it read, why it was TRUE when written, and what superseded it.

**TWO PASSAGES ARE COMPRESSED AND THAT IS NAMED.** The `either` paragraph's ORDINAL narrative at lines 34 to 36 loses one clause of retelling; the fold paragraph's `reserve_coverage_snapshots` passage loses the `grep 'ALTER TABLE reserve_coverage_snapshots'` transcript and its `ADR-199` citation, and keeps the claim, which was re-derived here and holds: `reserve_coverage_snapshots` is ALTERed by nothing and `0049` ALTERs `liability_snapshots` in the same file. **Content moved; it was not dropped**, which is the same accounting `ADR-449` gave for compressing lines 15 to 23.

**AND ONE SUMMARY LINE ABOVE THE PARAGRAPH WAS ALSO NARROW.** [`schema.ts:62`](../../packages/db/src/schema.ts) said the suite *"REPLAYS every later `ADD COLUMN`"*, which is one member of four. It now says it replays every later statement in the vocabulary below. That line is not cited by anything.

### 9. THE CONTROLS ADDED, AND THE RED WATCHED UNDER EACH

**FOUR CASES, APPENDED TO THE BLOCK `ADR-449` OPENED AT [`scoped-db.test.ts:4662`](../../packages/db/test/scoped-db.test.ts) FOR EXACTLY THIS**, which is where `ADR-449` section 9 item 1 said the binding leg belongs. The block now holds NINE cases. **The append is at the foot of the file and moves no cited line**, and the four read the header FLATTENED rather than raw, because the sentences they bind wrap wherever a line-neutral budget puts them and pinning a break would redden a reflow that changed no figure.

| # | Case | Binds |
| --- | --- | --- |
| 6 | `the fold paragraph names the carriers a run RECOMPUTES` | FIFTEEN, 117, and the fifteen NAMES as a set |
| 7 | `every carrier disagrees with its own CREATE TABLE body, which is the argument` | the proper-subset claim, per table, both directions |
| 8 | `the header refuses the shapes the fold refuses and no others` | zero `DROP COLUMN`, the `RENAME` count, the old wording REFUSED by pattern, and the two `ALTER COLUMN` carriers |
| 9 | `the seven others are the ones THIS FILE declares, and the eighth is in the SQL` | seven, spelled in words, every identity column NOT NULL, and `purchases` as the DDL-only eighth |

**CASE 6 BINDS THE NAMES AND NOT ONLY THE COUNT**, because a sentence reading FIFTEEN and listing eleven passes a count-only leg, and that is precisely the shape the old sentence had after the four arrived.

**TEN SEEDS, EVERY ONE OF THE FOUR CASES WATCHED RED, AND EVERY SEED REVERTED AND VERIFIED BYTE-IDENTICAL** by `git hash-object` against the value taken before the seed.

| # | Seed | Case reddened |
| --- | --- | --- |
| 1 | `FIFTEEN` to `ELEVEN` at `schema.ts:81` | 6 |
| 2 | denominator `117` to `116` at `schema.ts:81` | 6 |
| 3 | `wallet_withdrawals` DROPPED from the header's name list | 6 |
| 4 | the PROPER SUBSET argument REWORDED AWAY, figures untouched | 7 |
| 5 | the struck sentence `` `DROP COLUMN` and `RENAME` stay offenders `` RESTORED | 8 |
| 6 | `seven others` to `eight others` at `schema.ts:41` | 9 |
| 7 | **TREE:** `funded_accounts` DELETED from the `liability_snapshots` declaration | 7 |
| 8 | **TREE:** `.references(() => accounts.id)` ADDED to `purchases.parentAccountId` | 9 |
| 9 | the carriers sentence REWORDED to *"Several relations below carry later columns"* | 6 |
| 10 | **TREE:** a relation DECLARED in `schema.ts` and registered nowhere | 6 |

**SEEDS 7, 8 AND 10 ARE THE ONES THAT SAY THE DERIVED SIDE IS LIVE**, which is `ADR-449` seed 10's argument taken three times. Each moves the TREE and leaves the header untouched: a block comparing the header with itself passes all seven header seeds and is worthless. **Seed 8 is the sharpest**, because it is the exact edit that would make the DDL reading and this file's reading agree, and the header's `seven` goes stale the moment it lands.

**SEEDS 4 AND 9 ANSWER `ADR-442` SECTION 9's OBJECTION** that a gate over a GRAMMAR is satisfiable by rewording. Deleting the argument and rewording the count away redden a case each rather than zero.

**TWO SEEDS WERE REFUSED AND BOTH ARE NAMED.** A tree-side seed for case 6's carrier SET and any tree-side seed for case 8 would each mean editing a migration, and **migrations are sacred** (constitution E2). `ADR-449` and `ADR-447` refused the same seed for the same reason. What stands in their place is case 7's per-table leg, which reads the migration set and the declarations together and is red if either side stops matching, and case 8's assertion that the `DROP COLUMN` population is zero rather than merely unmentioned.

**`RI-30` TURNED RED ON THIS ROW'S OWN HELPER AND THE INVARIANT WAS OBEYED RATHER THAN ARGUED WITH.** The first spelling of `flat()` unprefixed the header with `.replace(/^\/\/ ?/, '')`, which is exactly the idiom [ADR-279](ADR-279.md)'s invariant is name-blind to, and it was caught on the first run, reported at `scoped-db.test.ts:4936` as the file then stood and now sitting at [`scoped-db.test.ts:4942`](../../packages/db/test/scoped-db.test.ts) with its reason written above it. **Importing `stripComments` would have been the wrong repair as well as a line-moving one**: that function DELETES a comment's text and this leg exists to READ it, and the import block of that file sits above every line anything cites. **The helper now tests a prefix and slices it**, which is neither the idiom nor a second stripper, and the reason is written beside it so the next reader does not reintroduce the `.replace()` as a tidy-up. **A gate that fires on the diff that adds it is the cheapest possible time to learn this**, and it is recorded because the alternative on offer was an exemption.

**ONE DEFECT IN THIS ROW'S OWN METHOD WAS FOUND BY IT.** The first seed pass reverted with `git checkout -- packages/db/src/schema.ts`, which restores HEAD and not the working tree, and it silently discarded the header edit; seeds 2 and 3 then ran against the BASE header and reported red for the wrong reason. **It was caught because the byte-identity check failed**, which is the only reason the run was not reported as three clean seeds. The seeds were redone against a byte copy. **This is the argument for checking the revert rather than trusting it**, and it is recorded because the failure mode is invisible in a passing transcript.

### 10. What is owed, stated precisely enough to be picked up

1. **THE FOLD'S VOCABULARY IS DOCUMENTED AS TWO MEMBERS IN THE SUITE ITSELF, AND THIS ROW DID NOT REPAIR IT.** `foldTable`'s doc says *"ONE TABLE, REPLAYED. ADR-094, WIDENED ONCE BY ADR-103"* at [`scoped-db.test.ts:1201`](../../packages/db/test/scoped-db.test.ts) and *"THE VOCABULARY IS CLOSED AT TWO MEMBERS AND THE DEFAULT IS STILL FAIL. `DROP COLUMN` and `RENAME` are REFUSED and never skipped ... Both have ZERO instances in this migration set"* at [`:1207`](../../packages/db/test/scoped-db.test.ts). **The last clause is FALSE**: `0075` is a `RENAME` and the function folds it. `ColumnFold`'s doc at [`:868`](../../packages/db/test/scoped-db.test.ts), three hundred lines above, says FOUR members. **The file contradicts itself.** It is in this fence and was left alone deliberately: repairing it is a second argument about how much of a doc comment a row correcting a different file's header should rewrite, and nothing asserts either comment. **A row that takes it should bind it the way case 8 binds the header, not merely reword it.**
2. **`ADR-449` SECTION 9 ITEM 2 IS UNTOUCHED.** The generation comparison is still two comparisons; the narrow one at [`scoped-db.test.ts:4563`](../../packages/db/test/scoped-db.test.ts) survives because the general block iterates `DDL_NAMES`. **The fold becomes takeable the day `live_account_state` is registered or the day those loops take a wider population**, and both are diffs on [`scope.ts`](../../packages/db/src/scope.ts) or on the loop headers. Not in this fence.
3. **`ADR-449` SECTION 9 ITEM 3's EIGHT POINTERS ARE STILL OFF BY 460 AND ROW `450` RULED ON THEM.** Nothing here moved them and nothing here repaired them; `ADR-447` is a DATED RECORD and [ADR-386:169](ADR-386.md) NAMES it.
4. **THE `either` PARAGRAPH'S NAMES ARE STILL NOT IN THE HEADER.** Case 9 derives the seven and asserts their number and their nullability, and [`schema.ts:41`](../../packages/db/src/schema.ts) still names none of them, where the fold paragraph now names all fifteen of its carriers. **A row that wants the names in the prose owes the line budget for seven table names inside a header that may not grow**, which is the same constraint section 6 derives and is the reason this row did not take it.
5. **NO POINTER IN THIS TREE IS CHECKED AGAINST AN ADR**, which is [ADR-450](ADR-450.md)'s derivation at [`repo-invariants.mjs:2305`](../../packages/tooling/checks/repo-invariants.mjs). Section 6 adds a second face of it: **three of the fifteen header pointers this row counted are synthetic fixtures**, so the population any pointer census reports is an upper bound and is stated as one. **A census that distinguished a fixture from a citation would change `ADR-450`'s figures as well as this row's**, and it is a `packages/tooling/` diff.
6. **`ADR-447` SECTION 9 ITEMS 4, 5 AND 6 ARE UNTOUCHED**, as they were at `ADR-449`. The live read path is still not a process, the non-registration assertion is still not independently watched red, and the `<!--gen:-->`-inside-inline-code question at [`gates.mjs:1275`](../../scripts/corpus/gates.mjs) is still open.
7. **`docs/STATE.md` MOVED BY ONE GENERATED SPAN AND `docs/INDEX.md` DID NOT MOVE AT ALL.** `gates.mjs generate` incremented `adr_count` at [`STATE.md:32`](../STATE.md) because this row adds an ADR; `INDEX.md` carries no `adr_count` span, so the fence's grant over it went unused and is reported unused rather than claimed. **No prose in either file was touched**, which is the fence as written.

### 11. Every leg, re-run after the last edit

- **`node scripts/corpus/gates.mjs generate`**, then **`check`: 33 of 33 gates pass**, and `CI-06f` is among them at every commit this row made. The base was 33 of 33 before the first edit and the row opened no hole: row `453` was RESERVED by the dispatcher and this row converts it, and row `452` was left alone.
- **`node packages/tooling/checks/repo-invariants.mjs`: 35 of 35 invariants hold**, `RI-14`, `RI-15` and `RI-16` among them. **`RI-30` was RED once, on this row's own `flat()` helper**, and was repaired by removing the idiom rather than by exempting the file (section 9).
- **`pnpm vitest run`:** base **344 files / 8,612 passed / 11 skipped / 0 failed**; branch **344 / 8,616 / 11 / 0**. **Delta: +4 passed, +0 files, +0 skipped, +0 failed.** Both measured in this session against `6fb9147b`, the base by stashing the working tree and re-running.
- **`pnpm run typecheck && pnpm run lint && pnpm run format:check`: all clean.** `format:check` was RED once, on the appended block, and was fixed by running Prettier over the file rather than by reformatting by hand.
- **`pnpm run verify` and `scripts/corpus/falsify.mjs` were NOT run**, on the dispatch's instruction.

### 12. What this row did NOT do

**NO MIGRATION TOUCHED. NO `scope.ts`. NO [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) AND NO FILE UNDER `packages/tooling/`**, both of which row `452` holds this wave. **NO `apps/` FILE. NO WORKFLOW. NO `RI-nn` MINTED. NO `CI-06` LETTER SPENT. NO GATE WEAKENED, NO TEST SKIPPED, DISABLED OR QUARANTINED, NO ASSERTION INVERTED, NO TEST DELETED.** **NO LANDED ADR EDITED**: `ADR-449`'s denominator and the dispatch's blast radius are NAMED in sections 2 and 7 and repaired nowhere.

**AND NO RESERVATION WAS WRITTEN FOR A ROW THIS SESSION DOES NOT OWN.** Row `453` is converted from `RESERVED` to `TAKEN` and no other cell of [ALLOCATION](ALLOCATION.md) is touched, row `452` included.

### 13. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** **Merging is not signing.** Inherited as well as owed: [ADR-303](ADR-303.md), [ADR-416](ADR-416.md), [ADR-421](ADR-421.md), [ADR-426](ADR-426.md), [ADR-430](ADR-430.md), [ADR-432](ADR-432.md), [ADR-437](ADR-437.md), [ADR-438](ADR-438.md), [ADR-439](ADR-439.md), [ADR-440](ADR-440.md), [ADR-443](ADR-443.md), [ADR-444](ADR-444.md), [ADR-445](ADR-445.md), [ADR-446](ADR-446.md), [ADR-447](ADR-447.md), [ADR-449](ADR-449.md), [ADR-450](ADR-450.md) and [ADR-451](ADR-451.md) are `proposed` and unsigned, and this row settles none of them.

**What a founder read adds on the `E2` read, and this entry cannot.**

**THE FIRST IS THAT THIS ROW WIDENED ITS OWN SCOPE INSIDE ITS FENCE.** It was commissioned for one sentence and corrected three, plus a fourth summary line, plus the `either` paragraph. **Every one is inside the fence and every one is derived**, but the judgement that a false neighbouring sentence in the same paragraph is this row's business rather than the next row's is a judgement. **The cost of having left them is that `schema.ts:70` would keep telling a reader that a registered table cannot be registered**, which is the failure the paragraph itself is a monument to; the cost of taking them is a larger diff over a comment block than the dispatch asked for.

**THE SECOND IS THAT THE HEADER'S PROSE IS NOW MORE MACHINE-BOUND, NOT LESS.** `ADR-449` made five sentences load-bearing and named that as a tax on whoever next edits the header; this row makes four more so, including one that asserts a specific sentence is ABSENT. **A gate that forbids a wording is a new shape in this tree** and it is the only shape that would have caught the `RENAME` sentence, because every figure in that sentence was already correct. **A founder may hold that this is the wrong direction and that the census belongs in an exported constant the header cites**, which `ADR-449` also raised. That is a bigger diff than this fence allows and it is still a real alternative.

**THE THIRD IS SECTION 3's LAST PARAGRAPH.** The four newly named carriers took their columns AFTER `ADR-094`, so the sentence's ordering clause is true of the eleven and merely vacuous for the four. **This row kept them in one list and stated the distinction rather than splitting the sentence in two.** The other reading is that a claim about why a ruling preceded a transcription should not be carrying four tables for which the transcription and the drift both came later.

**THE FOURTH IS SECTION 9's LAST TWO PARAGRAPHS, AND BOTH ARE PROCESS FINDINGS ABOUT THIS ROW'S OWN METHOD.** A seed harness that reverts with `git checkout` restores HEAD, not the working tree, and will report clean red for the wrong reason on every seed after the first. It was caught only by the byte-identity check that `ADR-449` established as convention. **A founder may want that convention written where a dispatch can require it, rather than inherited from the previous row's entry.**

- **Approval:** `___________________________` (UNSIGNED. **E2 READ OWED**, on the files named in section 6. **Merging is not signing.**)

|                 |     |
| --------------- | --- |
| **Approved by** |     |
| **Date**        |     |
