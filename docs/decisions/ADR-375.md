## ADR-375: everything `wiring.test.ts` owed, where the answer to the standing question is YES and `wired` is now the set a RUN installs, the support that expired is repaired on a narrower ground than it named, the `operator` hole four entries share is bound once, and the `RI-35` registration a prior row asked for CANNOT LAND until a sentence outside this fence is repaired (2026-09-06, status: proposed)

**FOUR CONSECUTIVE WAVES FENCED [`apps/api/test/wiring.test.ts`](../../apps/api/test/wiring.test.ts) SO IT HAD A SINGLE OWNER, AND EACH REPORTED WHAT IT FOUND THERE INSTEAD OF REPAIRING IT.** [ADR-366](ADR-366.md), [ADR-369](ADR-369.md), [ADR-371](ADR-371.md) and [ADR-372](ADR-372.md) each left an item behind. **The fence turned findings into a QUEUE rather than losing them, and this row is the queue being drained.**

- **Origin:** [ALLOCATION](ALLOCATION.md) row `375`, branch `claude/everything-wiring-test-owes`, session **570**.
- **THE DELIVERABLE IS FOUR REPAIRS IN ONE FILE, FOUR NEW CASES, ONE REFUSAL WITH ITS REASON MEASURED, AND FOURTEEN FALSIFICATION SEEDS.** No port is wired, no door is opened, no triple moves, no number is minted, no letter is spent.
- **NOT THE MONEY PATH ITSELF. IT IS THE CONTROL OVER THE MONEY PATH'S HONESTY**, which is [ADR-369](ADR-369.md)'s own description of this file. Ships `proposed` with an **UNSIGNED** approval line. **Merging is not signing.**
- **NO FILE UNDER ANY `src/` IS CHANGED.** `git diff origin/main -- apps/api/src` is **EMPTY** and `git diff --stat origin/main -- packages/db/migrations scripts/db` is **EMPTY**, after five seeds edited `src/` and every one was restored and verified with `sha256sum -c`.
- **A DISPATCH PROMPT IS NOT A SOURCE** ([ADR-357](ADR-357.md)). The row named three owed items and said not to take the list as complete or correct. **It is neither**: section 1 is the enumeration derived from the ADRs themselves, one of the three cannot be done at all, and two items the dispatch did not name are repaired here.

---

### 1. The owed set, enumerated at primary source rather than accepted

**EVERY ROW BELOW WAS OPENED AT THE FILE AND LINE IT NAMES BEFORE ANYTHING WAS TOUCHED.**

| # | owed by | what | real |
|---|---|---|---|
| 1 | [ADR-372](ADR-372.md) §10.1 | `wired` is POSITION-BLIND: an install below `await main()` counts | **YES.** Retired in section 3 |
| 2 | [ADR-372](ADR-372.md) §10.2 | `wired` is a `Set`, so a port installed twice counts once | **YES.** Bound in section 3 |
| 3 | [ADR-372](ADR-372.md) §10.3 | the pattern counts a BLOCK-commented call at column zero, which is `RI-25`'s class | **YES.** Retired in section 3 |
| 4 | [ADR-372](ADR-372.md) §10.4 | the pattern counts a call inside a multi-line template literal | **YES.** Retired in section 3 |
| 5 | [ADR-372](ADR-372.md) §10.5 | the scan reads ONE file and nothing said so | **YES, AND ALREADY DISCHARGED AS A CONTROL** by [`start-program.test.ts`](../../apps/api/test/start-program.test.ts). What was owed was the STATEMENT, and it is written now |
| 6 | [ADR-372](ADR-372.md) §10.6 | the refusal of nested shapes is the FORMATTER's work rather than the pattern's | **YES.** Retired in section 3, and it is the one ADR-372 could name and could not exercise |
| 7 | [ADR-371](ADR-371.md) §6.1 | the `setAdminSessionSource` citation of `start.ts:206` and `:245` has expired | **YES.** Repaired in section 5 |
| 8 | [ADR-371](ADR-371.md) §6.3 | two `RI-35` registrations for the `CRON_INVENTORY` replay clause | **REAL, AND IT CANNOT LAND.** Section 7 |
| 9 | [ADR-369](ADR-369.md) "Next" | three entries still do not name `operator` | **YES.** Repaired and bound in section 6 |
| 10 | [ADR-372](ADR-372.md) §5 | four shapes counted as wired that install nothing | **YES, AND IT IS ITEMS 1, 3, 4 AND 6 UNDER ANOTHER HEADING.** Section 3 |

**TWO OF THESE ARE NOT WHAT THEY LOOK LIKE, AND SAYING SO IS PART OF THE ANSWER.** Item 10 was dispatched as a third subject and is the same four defects the first list already carries, counted twice because one ADR states them as a census and the other as a queue. Item 5 was owed as a control and had been discharged by the row that recorded it, which is the outcome the dispatch predicted and asked to have written down rather than treated as an embarrassment.

**AND TWO ITEMS THE DISPATCH DID NOT NAME ARE REPAIRED HERE.** One citation into this file was broken by this row's own diff and is inside the fence (section 8). The `soleCardinal` collision [ADR-367](ADR-367.md) measured is live in item 7's own sentence, which is why the repair NAMES the retired clause instead of quoting it.

---

### 2. THE RULING

> **1. THE ANSWER TO THE STANDING QUESTION IS YES, AND IT IS THE WHOLE OF THIS ROW'S VALUE.** `wired` is derived from the SYNTAX TREE and is now the set of ports a RUN of `start.ts` would install before the process begins serving. **The file's constraint is not lifted**: it still never imports that module, because importing it binds a port. **Parsing is not executing**, which is [ADR-372](ADR-372.md)'s measurement and not a new claim. Section 3.
>
> **2. FOUR OF THE SIX OWED ITEMS ARE RETIRED OUTRIGHT AND NOT MITIGATED.** A parser refuses a block comment, a template literal, a nested call and a call below `main()` **without being taught any of them**, because three of the four are not statements and the fourth is a position. **Widening the pattern was never the remedy and is not what shipped.** Section 3.
>
> **3. THIS TAKES A DECISION [ADR-372](ADR-372.md) SECTION 15 RECORDED AS OPEN, AND THE ENTRY SAYS SO RATHER THAN LETTING IT LOOK MECHANICAL.** Option (b) is what shipped; option (a)'s agreement bar shipped beside it rather than instead of it. **[ADR-367](ADR-367.md) section 5's reason was tested against this and does NOT apply**, and section 4 is that argument.
>
> **4. THE EXPIRED SUPPORT IS REPAIRED AND THE VERDICT DOES NOT MOVE.** The claim was FALSE IN HALF: the per-IP dimension is built and installed, the per-ASN dimension is absent. **The ground survives on a NARROWER support than the one it named**, and a row that struck it on the old pointer would have removed a true reason on a false reading. Section 5.
>
> **5. THE REPAIR MOVES THE SENTENCE FROM `RI-15`'s THREE WEAK LEGS INTO ITS STRONG ONE, AND THAT IS THE GENERAL REMEDY FOR THE HOLE [ADR-371](ADR-371.md) NAMED.** Both new citations carry a backticked NAME. **The remedy for a citation whose subject is prose is to cite a NAME, not to widen the check.** Watched red twice. Section 5.
>
> **6. THE THREE `operator` OMISSIONS ARE REPAIRED AND THE FOUR ENTRIES NOW REST ON ONE DERIVED FACT.** `ApiDb` declares five doors and none yields a `SystemTx`. **The case FAILS ON GOOD NEWS**: the day that door opens, all four clauses expire in the same instant and the bar names all four. Section 6.
>
> **7. THE `RI-35` REGISTRATION [ADR-371](ADR-371.md) ASKED FOR CANNOT LAND, AND THE REASON IS ORDER RATHER THAN DIFFICULTY.** The artifact is PRESENT, so a `live` registration turns `RI-35` red at a sentence this row may not edit; a `retired` one would use the register to hide a live false sentence. **The registration is DOWNSTREAM of a repair that belongs to another row, and both halves sit on the SAME LINE**, so any binding written today breaks the moment that repair lands. **`absence-claims.mjs` was not opened for writing and no `RI-nn` was minted.** Section 7.
>
> **8. FIFTY CITATIONS POINT INTO THIS FILE, THIS ROW'S DIFF MOVED EVERY ONE OF THEM, AND NO GATE IN THIS REPOSITORY CAN TELL.** `RI-15` and `RI-16` are green at 35 of 35 across the whole change. **Not one of the fifty carries a name they can check**, which is [ADR-371](ADR-371.md) section 5.1's blind spot measured at fifty sites instead of at one. Section 8.

---

### 3. `wired` is the set a run installs, and what that retires

**THE DEFECT WAS NEVER A WRONG ANSWER.** The line pattern gives the right answer on every tree anybody has measured. [ADR-372](ADR-372.md) section 4 measured WHY, and the finding is that **one of the three things holding it up is the formatter**: every nested shape is refused because `prettier` indents a nested statement and `format:check` is a gate in a DIFFERENT RUNNER. A derivation whose soundness is on loan from a formatter is sound by accident.

**WHAT SHIPPED.** The module is parsed once. The installs are the top-level statements that are bare calls to a `use`/`set` name, taken in order, stopping at a top-level `throw` and at `await main()`. `wired` is the set of their ports; the LIST is kept beside it, which is the only reason the duplicate case below is assertable here rather than only in a neighbouring file.

| owed item | disposition | why |
|---|---|---|
| **§10.1** an install below `await main()` counts as wired | **RETIRED** | position is a fact the tree carries and text does not. The install is not counted, and the triple MOVES |
| **§10.2** `wired` is a `Set` | **BOUND, not retired** | it is still a `Set`, because every consumer wants set semantics. The list is the input and a case reads it |
| **§10.3** a BLOCK comment at column zero | **RETIRED** | a comment is not a statement |
| **§10.4** a call inside a template literal | **RETIRED** | a string is not a statement |
| **§10.5** the scan reads one file | **STATED** | held by `start-program.test.ts` since [ADR-372](ADR-372.md). Asserting it twice would be the defect this corpus keeps finding |
| **§10.6** the nested refusal is the formatter's | **RETIRED** | indentation is not read. **A fixture carries an UNINDENTED nested call**, which is the shape no formatter would ever write and therefore the shape no run of `format:check` could hold up |

**THE PATTERN IS KEPT AND STILL RUN.** `CALLS` is declared in the one-line shape `start-program.test.ts` extracts it by, the two derivations are asserted to agree on this tree, and a case asserts BOTH that shape and the initialiser of `wired`. **So a pattern that stopped meaning what it means is a red bar rather than a silent second opinion**, and a derivation silently swapped back is a red bar too, which section 9 records was NOT true of the first draft.

**AND THE FIVE SHAPES ARE NAMED RATHER THAN COUNTED**, so one leaving the list arrives in the diff carrying its own name. The fifth is the unindented one and it is the reliance ADR-372 could name and could not exercise.

---

### 4. Can it be DERIVED? The standing question, answered for the fourth time

**THE ROW WAS TOLD TO READ WHAT [ADR-363](ADR-363.md), [ADR-367](ADR-367.md) AND [ADR-369](ADR-369.md) CONCLUDED BEFORE PROPOSING ANYTHING, AND THE READING CHANGES THE ANSWER RATHER THAN DECORATING IT.**

- **[ADR-363](ADR-363.md)** derived `LIVE_DEFAULT` and left `LIVE_DEFAULT_DECLARED` beside it as a PIN under `RI-14`, because what it said was still true and only its authority had changed. **That is the shape this row copied**: `wiredByText` is not deleted, it is demoted from source to pin.
- **[ADR-367](ADR-367.md)** derived two counts and bound them as `size - 1` so they cannot part, and left a third hand-maintained **for a stated reason that is not difficulty**: deriving it would have CHOSEN between the options in an open founder question, so the derivation would have answered a question wearing a mechanical answer's clothes.
- **[ADR-369](ADR-369.md)** split its own question into three halves and derived two, refusing the third for exactly ADR-367's reason: it was a POLICY nothing in this tree had ruled.

**SO THE TEST IS NOT "IS IT DERIVABLE", IT IS "DOES THE DERIVATION SETTLE SOMETHING SOMEBODY ELSE IS HOLDING".** Applied here:

| half | derivable | does ADR-367 section 5's reason apply |
|---|---|---|
| **what a run of `start.ts` installs** | **YES, and it is derived** | **NO.** It is a fact of the tree. No open question turns on it |
| **whether `wired` should BE that** | trivially | **THIS IS THE ONE TO ARGUE, and it is argued below rather than assumed** |

**THE HONEST POSITION IS THAT THIS ROW TOOK A DECISION [ADR-372](ADR-372.md) LEFT OPEN, AND IT IS DEFENSIBLE FOR THREE REASONS THAT ARE STATED SO THEY CAN BE OVERRULED.** First, ADR-372's own section 15 argues for option (b) in its own words, so the derivation implements the reasoning of the entry that asked the question rather than pre-empting it. Second, the question is about THIS FILE'S OWN IMPLEMENTATION and not about a port, a ruling, a status or a number a founder is holding: nothing outside this file changes meaning. Third, **the alternative is worse in a way that is measurable**: leaving `wired` on the pattern leaves the triple, which is the figure this corpus cites when it says what a deployment serves, resting on an input three waves have now recorded as unsound.

**WHAT WOULD MAKE THIS THE WRONG CALL is if a founder wanted option (c), both numbers reported.** That is one line away from what shipped and the entry says so rather than pretending the choice is closed.

**AND THE REMAINING ITEM IS GENUINELY NOT DERIVABLE HERE.** Item 5, that the scan reads one file, cannot be settled inside this derivation at all: it is a property of every OTHER module, and the only honest form of it is the directory walk `start-program.test.ts` already runs. **It is named rather than duplicated.**

---

### 5. The expired support, repaired, and the general remedy for `RI-15`'s prose hole

**[ADR-371](ADR-371.md) SECTION 4 IS RE-VERIFIED AND HOLDS EXACTLY.** `start.ts:206` now reads a sentence about a composition and `:245` one about the certificate list's origin guard. Both pointers land in an argument about the certificate image source, and the clause they supported claimed the limit `INV-M11-05` requires is absent from this tree.

**THE CLAIM IS FALSE IN HALF AND THE GROUND IS NOT.** The per-IP dimension is built and installed: `useCertificateRateLimiter` is the eleventh install and it is one of the eleven this file counts. The per-ASN dimension is still absent and `start.ts` records it owed with its blocker named. **The surviving support is `RATE_LIMITED_ROUTES`, which is the two PUBLIC certificate rows**, so the limit that exists has never reached `/admin/*` and ground two is about a surface it does not cover. **THE VERDICT DOES NOT MOVE.**

**THE RETIRED CLAUSE IS NAMED AND NEVER REPRODUCED**, on the dispatch's binding rule and [ADR-367](ADR-367.md)'s measured collision. `start.ts` keeps the clause beside its correction under `RI-14` in three places and holds a fourth, narrower live one, so a reader matching on the prose gets four answers of which three are history. **A fifth copy in this file would be that collision one register down**, in the file whose `soleCardinal` exists because of it.

**AND THE REPAIR CHANGES WHICH LEG OF `RI-15` HOLDS THE SENTENCE, WHICH IS THE PART THAT GENERALISES.** [ADR-371](ADR-371.md) section 5.1 measured that the old pointer carried a path and a line and NO NAME, because its subject was a quoted sentence, so only the three weak legs applied and all three passed while the pointer was wrong. **Both new citations carry a backticked name.** Section 9 seeds each of them past `CITATION_WINDOW` and both turn `RI-15` red at 34 of 35.

> **THE REMEDY FOR A CITATION WHOSE SUBJECT IS PROSE IS TO CITE A NAME, NOT TO WIDEN THE CHECK.** `RI-15` has nothing to anchor to when a sentence is the subject, and ADR-371 declined to repair that and was right to. **What it did not say, and what this row measured, is that the citing sentence can almost always be rewritten to have a NAMED subject instead**, and the rewrite is strictly better prose as well as a defended pointer.

---

### 6. The `operator` hole, repaired in three entries and bound in one place

**[ADR-369](ADR-369.md) SECTION 4 FOUND `operator` AND `now` UNNAMED BY FOUR ENTRIES AND CALLED IT [ADR-358](ADR-358.md)'s MECHANISM PRODUCING ONE HOLE FOUR TIMES.** It repaired the wallet entry and left the other three, which is the last item in the queue this row was sent to drain.

**EACH OF THE THREE NOW NAMES `operator` AND ACCOUNTS FOR IT.** The member yields the transaction the port's other methods run on; it needs a `SystemTx`; `ApiDb` declares five doors and none yields one, refused by [ADR-171](ADR-171.md) clause 1. **`useAdminPayoutBackend` is the sharpest of the three**: its entry enumerates `AdminPayoutTx`'s four methods and calls the port *"the one of the five that is closest to wireable"* while never naming the member that opens the transaction those four run inside.

**THE SHARED FACT IS DERIVED ONCE AND NOT RESTATED FOUR TIMES, WHICH IS THE DIFFERENCE BETWEEN THIS AND THREE MORE PARAGRAPHS.** A case derives the set of blocked ports whose interface declares `operator`, asserts it is exactly the four by name, asserts every one of the four NAMES the member, and slices `ApiDb` to assert no door yields a `SystemTx`. **IT FAILS ON GOOD NEWS.** The day that door opens, all four clauses stop being true in the same instant and the bar names all four, rather than each entry going stale on its own schedule for somebody to find one at a time.

**`now` STAYS IN THE PINNED GAP, ON THE WALLET ENTRY'S OWN PRECEDENT.** [ADR-369](ADR-369.md) repaired `operator` there and deliberately left `now`, because a clock needs no door and an entry forced to state a blocker for it would be writing a sentence to satisfy a check. **This row does not widen that.**

**THE SLICE IS OF THE INTERFACE AND NOT A GREP OVER THE FILE**, because `db.ts` argues in its own comments that the door does NOT exist and a whole-file grep would be satisfied by that argument. Non-vacuity is asserted in both directions: the slice must carry `FirmTx`, which is a door this deployable does open.

---

### 7. `RI-35`: the registration cannot land, and the reason is ORDER

**[ADR-371](ADR-371.md) SECTION 5.2 RULED THAT `RI-35` SHOULD BE MEASURED RATHER THAN EXTENDED**, on the measurement that lifting the sweep's `test/` exclusion reaches two lines out of thirty-five because seventeen of nineteen artifacts have no needle. **That ruling is re-read and it is not disturbed. Nothing in `packages/tooling/checks/**` was opened for writing and no `RI-nn` was minted.**

**WHAT IT ASKED FOR INSTEAD WAS REGISTRATION, AND THE MEASUREMENT SAYS THE REGISTRATION IT NAMED CANNOT BE WRITTEN TODAY.**

**FIRST, THE FACT, VERIFIED AT PRIMARY SOURCE RATHER THAN INHERITED FROM [ADR-370](ADR-370.md).** `postgresBatchPorts` serves `accountDaysFrom` at [`apps/worker/src/batch/adapter.ts:870`](../../apps/worker/src/batch/adapter.ts) and `storedRuleStates` at `:887`, and `runReplayAudit` calls exactly those two reads. **So the clause is FALSE today**, and ADR-370's finding stands re-confirmed rather than re-discovered.

**SECOND, THE REGISTER HAS NO DISPOSITION THAT FITS.** `live` requires the artifact to be **ABSENT**, and it is present, so leg 2 turns `RI-35` red at the lying sentence, which is leg 2 doing exactly its job. `retired` requires the sentence to be quoted as history under `RI-14`, and it is not: it is stated as current. **Registering it `retired` would be using the register to conceal a live false sentence, which is the inversion of what `RI-35` is for.**

**THIRD, AND THIS IS THE ONE THAT SETTLES IT: THE REGISTRATION IS DOWNSTREAM OF A REPAIR, AND THE REPAIR IS ANOTHER ROW'S.** The sentence lives in `docs/ops/runbooks/CRON_INVENTORY.md`, which this wave fences to a sibling, and [ADR-371](ADR-371.md) section 6 item 2 already records that repair as owed. **A `live` registration cannot precede it and a `retired` one cannot precede it either.**

**FOURTH, AND IT IS WORTH SEPARATING FROM THE OTHERS: BOTH HALVES OF THAT ROW ARE ON ONE LINE.** The caller half, which HOLDS, and the ports half, which has expired, are one markdown table row. `RI-35`'s leg 1 anchors a claim to a VERBATIM substring of a single line. **So any registration bound to that line, including the honest caller half that would go green today, breaks the moment the owed repair rewrites the row.** A register entry that is guaranteed to fail on the next correct edit is worse than no entry.

**AND ONE THING ADR-371 DID NOT MENTION, MEASURED HERE.** Every one of the register's sites is a code file. **A `docs/` site would be the first**, and `RI-35`'s own `covers` says it EXCLUDES `docs/`. That exclusion is written about leg 6's sweep and not about leg 1's anchor, so it is not a prohibition, **but registering the first `docs/` site is a change to what the check is FOR and it is exactly the widening ADR-371 declined.**

> **WHAT IS OWED, RESTATED SO THE NEXT ROW DOES NOT REDISCOVER THIS.** The order is: repair the `CRON_INVENTORY` replay row FIRST, splitting the caller half from the ports half and marking the ports half retired under `RI-14`; THEN register the `BatchPorts` artifact with the retired half bound to it. **A row taking the registration alone will find it cannot, and will spend its budget finding out.**

---

### 8. Fifty citations, every one moved, and nothing could tell

**183 CITATION SITES ACROSS 74 FILES POINT INTO THIS FILE, AT 50 DISTINCT LINE NUMBERS**, derived at reporting time. **This row's diff took the file from 3,175 lines to 3,593, and every one of those fifty line numbers now holds different text.**

**`RI-15` AND `RI-16` ARE GREEN AT 35 OF 35 ACROSS THE WHOLE CHANGE, AND THAT WAS MEASURED BEFORE THE FIRST EDIT RATHER THAN DISCOVERED AFTER IT.** A probe inserted thirty lines near the top of the untouched file and ran the invariants: 35 of 35. **Not one of the fifty carries a backticked name the checks can anchor to.** They carry a path and a line, or a quoted sentence, so only the three weak legs apply: the path resolves, the file reaches the line, the line is not blank or a bare closing brace.

**THIS IS [ADR-371](ADR-371.md) SECTION 5.1's HOLE MEASURED AT FIFTY SITES INSTEAD OF AT ONE**, and it cuts two ways. It is what made this row's edit affordable at all, and it is what makes every one of those fifty pointers worth nothing as a pointer.

**THREE OF THE 183 SITES ARE OUTSIDE `docs/`.** One is this file citing itself and **it is repaired here**, from `:215` to `:357`, which is the line still carrying the apostrophe the sentence is about. **The other two are `packages/tooling/checks/repo-invariants.mjs:2662` and `packages/tooling/test/repo-invariants.test.ts:1657`, both citing `wiring.test.ts:215`, both outside this row's fence and both now wrong.** They are reported rather than repaired.

---

### 9. Falsification

**FOURTEEN SEEDS. TWELVE WATCHED RED ON FIRST RUN, TWO WATCHED GREEN, AND BOTH GREENS CHANGED THE WORK.** Every seed restored byte-identical, verified with `sha256sum -c` against a manifest re-taken before each one.

| # | seed | file | watched |
|---|---|---|---|
| 1 | one install moved below `await main()` | `src` | **RED on 3 cases**, and the port LEAVES `wired`, which the pattern could not do |
| 2 | a blocked port installed inside a BLOCK comment at column zero | `src` | **RED.** `RI-25`'s class on the real file, and the port does NOT become wired |
| 3 | one port installed twice | `src` | **RED**, the duplicate case |
| 4 | an UNINDENTED install nested in a conditional | `src` | **RED**, the formatter reliance on the real file |
| 5 | the `throw` clause removed from the derivation | test | **RED**, so the inert clause is load-bearing in its fixture |
| 6 | the `main()` cutoff removed from the derivation | test | **RED** |
| 7 | **`wired` reverted to the line pattern** | test | **GREEN.** Section 9.1 |
| 8 | the unindented shape dropped from the named list | test | **GREEN as first drafted, RED when aimed properly.** Section 9.2 |
| 9 | `start.ts:315` drifted past `CITATION_WINDOW` | test | **RED**, `RI-15` at 34 of 35 |
| 10 | `certificate-rate-limit.ts:151` drifted past the window | test | **RED**, `RI-15` at 34 of 35 |
| 11 | `routes/admin-payouts.ts:383` drifted past the window | test | **RED**, `RI-15` at 34 of 35 |
| 12 | **`ApiDb` given a door yielding a `SystemTx`** | `src` | **RED ON GOOD NEWS**, naming all four entries |
| 13 | `operator` unnamed again in one repaired entry | test | **RED on 2 cases**, the census and the door |
| 14 | 30 lines inserted near the top of the UNTOUCHED file | test | **GREEN by design**, and it is the citation measurement in section 8 |

#### 9.1 Seed 7 went green and the fix is in the file rather than in this entry

**REVERTING `wired` TO `new Set(matches(startSource, CALLS))` LEFT EVERY CASE IN THE FILE PASSING**, the agreement case included, because that case then compares the text derivation with ITSELF. **On a tree where the two derivations agree, no observation of `wired` can tell which one produced it.** The fixtures defend the reader; nothing defended that `wired` USES it.

**SO THE BIND IS STRUCTURAL AND THE FILE SAYS SO RATHER THAN DRESSING IT UP.** A case reads this file's own source and asserts the initialiser, **anchored at the start of a line**, on the same precedent `start-program.test.ts` uses when it reads `CALLS` out of here. **The anchor is not decoration**: [ADR-371](ADR-371.md) section 7 found its own case 2 satisfied by the statement COMMENTED OUT because it used `toContain` over a whole-file read. Re-run against the pin, seed 7 is RED.

#### 9.2 Seed 8 went green because the seed was wrong, which is worth separating

The first attempt replaced a label in the fixture list AND in the named list, so both moved together and the assertion stayed consistent. **A seed that edits both sides of an equality is not a seed.** Aimed at the named list alone, it is red.

#### 9.3 One restore was done wrong, and it is the same mistake in the third consecutive wave

**Seed 9's first run used `git checkout --` on a file whose citation repair was still UNCOMMITTED, and reverted the repair with the seed.** `sha256sum -c` caught it at once, and seed 10 had already run against the reverted file and reported a false GREEN. The repair was re-applied, verified against the pre-seed hash, committed, and both seeds re-run against a committed tree. **[ADR-367](ADR-367.md) made this mistake, [ADR-369](ADR-369.md) made it again and recorded it, and it happened here anyway.** The instrument that catches it is the manifest re-taken before every seed, and the rule that prevents it is: **seed only against a committed tree.**

---

### 10. Where this entry is most likely wrong

- **THE READER IS A SYNTAX TREE AND NOT A TYPE CHECKER**, which is [ADR-372](ADR-372.md) section 13's caveat inherited whole. It knows a statement is a top-level call to a name beginning `use` or `set`; it does not know the name resolves to a port setter. `tsc` closes that on every commit and this file does not duplicate it.
- **REACHABILITY IS MODELLED BY ONE CLAUSE AND REACHABILITY IS BIGGER THAN THAT.** A top-level `await` of a promise that never settles leaves everything below it unreached and nothing here models it. `start-program.test.ts`' straight-line case forecloses the shapes that would need it, and **this file now depends on that case in a way it did not before**, which is a dependency between two suites that nothing asserts.
- **THE `operator` ACCOUNTING RESTS ON `ApiDb` DECLARING NO `SystemTx` DOOR, DERIVED FROM AN INTERFACE SLICE.** A handle reaching the same authority under another name, or supplied by some mechanism other than a door on `ApiDb`, would be invisible to it. **That is the same shape as [ADR-371](ADR-371.md) section 8's caveat about `RATE_LIMITED_ROUTES`, and it is the second time this class of derivation has been the load-bearing one.**
- **SECTION 4's ARGUMENT THAT ADR-367 SECTION 5's REASON DOES NOT APPLY IS AN ARGUMENT AND NOT A PROOF.** A founder who reads section 15 question 1 as still open will read this row as having answered it. **The entry states that reading rather than burying it**, and reverting to option (a) or moving to option (c) is a small change to one line.

---

### 11. Verification, every figure derived at reporting time

- **Suite: 335 files / 8,064 passed / 10 skipped / 0 failed**, against a base of **335 files / 8,060 passed / 10 skipped / 0 failed** reproduced on `c7987c3c` with a clean tree **BEFORE the first edit**. Delta **+4 cases in ONE EXISTING FILE entirely by ADDITION with zero removals**, files and skips unmoved, attributed **by removal**: `wiring.test.ts` runs **27** alone on this tree and **23** alone on the base.
- **Gates 33 of 33** after `generate`. **Invariants 35 of 35.** No number minted, no `CI-06` letter spent.
- `pnpm run typecheck`, `pnpm run lint` and `pnpm run format:check` all exit 0.
- `git diff origin/main -- apps/api/src` is **EMPTY**, `git diff --stat origin/main -- apps/worker packages` is **EMPTY**, and `git diff --stat origin/main -- packages/db/migrations scripts/db` is **EMPTY**.
- **`pnpm run verify` was NOT run and `scripts/corpus/falsify.mjs` was NOT run**, both forbidden by the row for mutating the working tree.
- **THE ALLOCATION FENCE IS WIDENED BY ONE ROW AND IT IS REPORTED RATHER THAN LEFT QUIET.** The highest row on the merged head is `373` and `375` landing alone opens a hole at `374`. **The gate was RUN with `375` alone to measure it rather than assumed: 32 of 33, `CI-06f` failing.** A reservation cell naming no subject is written for `374`, on [ADR-372](ADR-372.md) section 14's precedent and the three waves that have followed it.
- **THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL**, which sessions 550, 553, 557, 558, 561, 562, 564, 565 and 567 all recorded and which is now the ninth wave running. The tree was attached and cloned by hand and `HEAD` was confirmed to be the `c7987c3c` the row names rather than assumed.
- **GITHUB ACCESS, TESTED RATHER THAN ASSUMED.** No `mcp__github__*` tool is offered, `gh` is NOT on `PATH`, and `curl` against the repository API answers **403**. **`git push` authenticates and every commit here is pushed.** No pull request was opened and none may be.
- **No gate was weakened, no test skipped, disabled or quarantined, no invariant number minted, no `CI-06` letter spent, no migration number taken or reserved, no port wired, no door opened, no `status` field moved, and no number in the wiring triple adjusted.**

---

### 12. What is still owed, stated precisely enough to be picked up

1. **The `CRON_INVENTORY.md` replay row, then the `RI-35` registration, IN THAT ORDER.** Section 7. The ports half is false, the caller half holds, both are on one line, and the registration cannot precede the repair.
2. **`packages/tooling/checks/repo-invariants.mjs:2662` and `packages/tooling/test/repo-invariants.test.ts:1657` both cite `wiring.test.ts:215` and both are wrong now.** The line they mean is `:357`. Outside this row's fence.
3. **`packages/db/DELTA_MANIFEST.md` cites `wiring.test.ts:219`, and `docs/decisions/ALLOCATION.md` cites `:1337`, `:1432` and `:1450`.** All drifted. Section 8 measures that no check will report any of them.
4. **The imprecision at `start.ts:311`**, which [ADR-372](ADR-372.md) section 9 reported and left. It says of the rate limiter's install that *"it is the last line"*; it is the last INSTALL and `await main()` is two lines below it. **The reader it misleads is the next session appending an install.** It is a `src/` edit and this row may not make one. **What HAS changed is that such an append is now caught by `wiring.test.ts` itself**, and not only by `start-program.test.ts`.
5. **`RI-15`'s two named holes**, `IDENTIFIER_GLUE` ([ADR-357](ADR-357.md)) and `DECLARATION_SHAPES` ([ADR-369](ADR-369.md)), plus the prose-subject hole ([ADR-371](ADR-371.md)). **Section 5 offers a remedy for the third that is not a widening**, and offers nothing for the first two.

---

### 13. Open questions, recorded as options because none is this row's to answer

1. **Is option (b) the right disposition of [ADR-372](ADR-372.md) section 15 question 1?** Section 4 argues yes and takes it. **Options: (a) revert `wired` to the pattern and keep only the agreement bar, which is what shipped before; (b) what shipped; (c) have the triple report BOTH numbers, as [ADR-357](ADR-357.md) did for `{blocked, refusing, live}`.** **(c) is the only one that makes the divergence visible in the triple itself rather than only in a bar**, and it costs a number the corpus would then have to cite in two forms.
2. **Should a `docs/` file ever be an `RI-35` claim site?** Section 7. **Options: (a) never, and the runbook's claims are held by prose review, which is what has been happening; (b) yes for LIVE runbooks and never for dated records, which is the distinction `RI-35`'s own `covers` draws for its sweep; (c) yes, and lift the sweep too, which [ADR-371](ADR-371.md) measured buys two lines.**
3. **Do the fifty drifted citations into this file want a remedy at all?** Section 8. **Options: (a) no, because a citation into a test file is a reading aid rather than a control, and the fifty prove nobody has followed one; (b) require a named subject on every citation into `apps/**/test`, which is a rule `RI-15` could enforce and which would make this row's diff cost fifty repairs; (c) stop citing line numbers into this file and cite the `BLOCKED` key instead, which is stable across every edit.** **(c) is the cheapest and it is a convention rather than a check.**

---

### 14. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** Inherited as well as owed: [ADR-357](ADR-357.md) and [ADR-360](ADR-360.md) through [ADR-373](ADR-373.md) are all `proposed` and unsigned, and this row settles none of them.

  **What a founder read adds and this entry cannot.**

  **THE FIRST THING TO DECIDE IS SECTION 4, AND IT IS A QUESTION ABOUT AUTHORITY RATHER THAN ABOUT CODE.** [ADR-372](ADR-372.md) asked whether `wired` should be derived from the syntax tree and recorded three options. This row implemented one of them. **The argument that this is not [ADR-367](ADR-367.md) section 5's defect is that nothing outside this file changes meaning**, and a founder who disagrees should say so, because the same argument would let a later row settle a larger question the same way.

  **THE SECOND IS SECTION 7, AND IT IS A REQUEST TO BE TOLD THE ORDER RATHER THAN THE WORK.** A prior row recorded a registration as owed. It cannot be written until a sentence in another file is repaired, both halves of that sentence are on one line, and that file belongs to somebody else this wave. **The failure mode is a future row spending its whole budget discovering this**, which is why it is section 7 rather than a line in section 12.

  **THE THIRD IS SECTION 8, AND IT IS THE ONE NOBODY ASKED FOR.** Fifty citations point into this file. This row moved every one of them and every gate stayed green. **Either those citations are controls, in which case this repository has none over them, or they are reading aids, in which case they should stop carrying line numbers.** They cannot be both, and they have been treated as both for four waves.

- **Approval:** `___________________________` (UNSIGNED. **CONTROL OVER THE MONEY PATH'S HONESTY, `E2` READ OWED.** **Merging is not signing.**)

| | |
|---|---|
| **Approved by** | |
| **Date** | |
