## ADR-404: the 80 reproduce exactly and 53 of them are repaired with the occurrence named; `ADR-398`'s seam has ZERO surviving instances because a sibling row repaired the last one 19 minutes before the entry that recorded it as owed; and an owed list cannot see inside its own wave (2026-09-07, status: proposed)

**THE DISPATCH CARRIED TWO PREMISES AND ONE OF THEM IS FALSE.** [ADR-401](ADR-401.md) section 8's **80** is real: it reproduces cell for cell on a later base through an independently built copy of the checks' own reader, as does every other census figure that entry published, under both instruments. **[ADR-401](ADR-401.md) section 13 item 2 is not.** Its surviving instance, [ADR-337](ADR-337.md) line 202, was repaired by [ADR-403](ADR-403.md) at commit `d548187a`, and the dispatch's escalation of it to *"named three times now and repaired by nobody"* is wrong in the half that matters.

**RULED, AND THE FIRST HALF IS A REPAIR RATHER THAN A REFUSAL.** Of the 80, **65** have an occurrence that survives byte-identical at a derivable new coordinate. **53 are repaired**, in **51 sentence units carrying 55 pointers across 25 documents**, each naming which occurrence it landed on beside the retired coordinate. **27 are refused in six shapes**, four of which no previous row has had to name.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `404`, branch `claude/the-eighty-repairable-pointers`, session **599**, off the wave integration branch `claude/wave-401s` at `1f621d5c` rather than off `main`.
- **CONTROL OVER THE CORPUS'S OWN POINTERS. Ships `proposed` with an UNSIGNED approval line. Merging is not signing. `E2` read owed.**
- **THE DELIVERABLE IS 55 REPAIRED POINTERS, ONE REFUTATION, SIX REFUSAL SHAPES AND FOUR SEEDS.** No invariant number was minted, no gate was added, widened or weakened, no register grew, no test was skipped, and no file outside this row's fence was written.
- **EVERY FIGURE THE DISPATCH CARRIED WAS RE-DERIVED.** The 80 holds. The 442 of 495 holds on the findings-eligible population. **Line 202 does not.**

---

### 1. The objective

Take the 80 repairable pointers whose subject is not unique in the cited file, which [ADR-401](ADR-401.md) section 8 priced and section 13 item 3 recorded as unrepaired, **derive the list rather than inherit it**, and repair those that survive [ADR-388](ADR-388.md)'s two gates, [ADR-397](ADR-397.md)'s precondition and [ADR-396](ADR-396.md)'s refusal shapes, each carrying the sentence [ADR-401](ADR-401.md) section 8 rules. Second, discharge [ADR-398](ADR-398.md)'s surviving seam instance. One objective, non-money, control over the corpus's own pointers.

---

### 2. The container, measured rather than inherited, and the dispatch stated nothing about it this time

**THE CONTAINER ARRIVED WITH NO REPOSITORY, FOR THE SIXTH CONSECUTIVE WAVE.** The stated working directory was empty, `git rev-parse` returned *fatal: not a git repository*, and the only `.git` directories on the container belonged to `rbenv` and `nvm`. The tree was attached to this session's authorized set and cloned by hand.

| | |
|---|---|
| `git rev-parse --is-shallow-repository` | **`false`** |
| `git rev-list --count` on the base `1f621d5c` | **4,338** |
| Wall clock for the clone, out of this session's own budget | **15 seconds** |
| `.git` on disk | **182 MB** |
| What the dispatch stated about this container | **nothing, deliberately** |

**THIS IS THE FIRST DISPATCH IN SIX WAVES THAT DID NOT ASSERT A SHALLOWNESS, AND THAT IS THE CORRECT FIX RATHER THAN A NEW MEASUREMENT.** [ADR-396](ADR-396.md), [ADR-398](ADR-398.md), [ADR-399](ADR-399.md), [ADR-400](ADR-400.md) and [ADR-401](ADR-401.md) each measured `false` against a dispatch asserting `true`. **The assertion is now gone and the empty container is not**, so five entries asking for the same thing have been half heard. **No ADR can fix the remaining half and this is the fifth consecutive entry to say so.**

**THE BASE WAS REPRODUCED ON `1f621d5c` BEFORE THE FIRST EDIT**, on a tree `git status --porcelain` reported EMPTY: suite **336 files / 8,109 passed / 10 skipped / 0 failed**, gates **33 of 33**, invariants **35 of 35**, `typecheck`, `lint` and `format:check` all exit 0.

**`pnpm run verify` was NOT run and [`falsify.mjs`](../../scripts/corpus/falsify.mjs) was NOT run.** Both are forbidden to this row and both mutate the working tree. Every leg was invoked directly.

---

### 3. The reader, and the census reproducing at a fifth independent derivation

**THE CHECKS' OWN READER WAS RELOCATED OUTSIDE THE REPOSITORY WITH ITS `HERE` CONSTANT PINNED TO THE REAL CHECKS DIRECTORY**, which is [ADR-400](ADR-400.md)'s and [ADR-401](ADR-401.md)'s method and is taken here **by fence rather than by choice**: `packages/tooling/**` is row `406`'s this wave and was never opened for writing.

| Scope, through each check's own input, on `1f621d5c` | Citations | Carry a name the binder binds |
|---|---|---|
| `docs/` under a dated or session heading, out of `RI-16` by shape | 7,692 | **294** |
| `docs/` live, `RI-16`'s scope | 1,100 | **73** |
| Source files, `RI-15`'s scope | 393 | **130** |
| **READ BY THE CHECKS' READER** | **9,185** | **497** |

**RUN OVER [ADR-401](ADR-401.md)'s OWN BASE `28e2b0ee`, THIS READER RETURNS 495, AND THE DIFFERENCE IS EXACTLY TWO POINTERS.** They are [ADR-401](ADR-401.md)'s own falsification seeds A and C, at its lines 236 and 238, which that entry's section 10 declares are wrong on purpose. **Differenced pointer by pointer, the population gains those two and loses nothing**, which is [ADR-400](ADR-400.md)'s method applied to the row that applied it to [ADR-400](ADR-400.md).

| | |
|---|---|
| Named pointers on `28e2b0ee`, this reader | **495**, which is [ADR-401](ADR-401.md)'s figure exactly |
| On `1f621d5c` | **497** |
| Added | [ADR-401](ADR-401.md) line 236 and line 238, `firm` and `TREASURY` |
| Removed | **none** |
| Declared specimens now standing | **4**: [ADR-400](ADR-400.md) lines 266 and 270, [ADR-401](ADR-401.md) lines 236 and 238 |
| **FINDINGS-ELIGIBLE** | **493** |

**SO THE 493 IS UNCHANGED ACROSS THREE MERGES, BY COINCIDENCE RATHER THAN BY STABILITY**: the population grew by two and the declared-specimen list grew by the same two. **That census is now confirmed at five independent derivations and this is the third distinct base**, which makes it the most confirmed measurement in this corpus.

---

### 4. Both instruments reproduce, and [ADR-401](ADR-401.md) section 5's disagreement with [ADR-400](ADR-400.md) resolves in [ADR-401](ADR-401.md)'s favour

**`G2` IS DERIVED HERE BY BOTH LEGS AND THE TRAVERSAL AND SEARCH STRING ARE STATED, WHICH [ADR-401](ADR-401.md) SECTION 5 RULES IS THE CONDITION OF A REPRODUCIBLE FIGURE.** The `log -S` leg searches **the raw backticked token as the citing file writes it**, over the **citing** file, **default traversal**, no `--first-parent` and no `--full-history`, taking the **oldest** result. The `blame` leg is `git blame -w` on the citing line.

| Verdict over the 290 findings-eligible named pointers in dated records | `git blame -w` | `git log -S` |
|---|---|---|
| True then, true now. Nothing owed | **169** | **168** |
| Outcome ONE: DRIFTED after being true, REPAIRABLE | **75** | **74** |
| Outcome TWO: FALSE ON THE DAY IT WAS WRITTEN | **43** | **39** |
| False then, true now, by coincidence | **2** | **2** |
| Undecidable | **1** | **7** |

**ALL TEN CELLS ARE [ADR-401](ADR-401.md)'s, FIGURE FOR FIGURE, ON A LATER BASE THROUGH A HARNESS BUILT WITHOUT SIGHT OF ITS CODE.** Its `blame` column reproduces only when a cited file that did NOT EXIST at the write commit is counted UNDECIDABLE rather than false; counted as false the born-wrong cell reads 44 and the undecidable cell reads 0. **That is a one-pointer methodological seam nobody had stated, and it is stated here rather than left for a sixth row to rediscover.**

**AND [ADR-401](ADR-401.md) SECTION 5's CORPUS-WIDE `log -S` ROW REPRODUCES TOO.** That entry measured **346 / 85 / 46 / 7 / 9** over 493 at [ADR-400](ADR-400.md)'s base and reported it as disagreeing with that entry's published **342 / 83 / 46 / 8 / 14**. **This row lands on 346 / 85 / 46 / 7 / 9 exactly**, at a third base, with a third harness. **That is not proof [ADR-400](ADR-400.md) is wrong**, and section 5 was careful to say the rule does not determine enough for either to be right; **it is a second independent reading agreeing with the second of the two, which is the most a corpus can get from a rule that has no definition.**

---

### 5. The 80, derived rather than inherited, and it is 80

**`U` IS TAKEN AS [ADR-401](ADR-401.md) SECTION 7 STATES IT**: the bound subject name occurs more than once in the cited file **at the commit that wrote the citing line**, matched case-insensitively as a substring, which is how `nearestName` matches.

| Priced on `1f621d5c` | |
|---|---|
| `U` fires over the 497 named pointers | **444** |
| `U` fires over the **493 findings-eligible** | **442** |
| **[ADR-401](ADR-401.md)'s figure, over its 495** | **442** |

**THE TWO EXTRA ARE ITS OWN SEEDS AND BOTH ARE NON-UNIQUE**, `firm` at 130 occurrences and `TREASURY` at 19, so 442 plus 2 is 444 and the findings-eligible reading returns that entry's number unchanged.

| Of the 442, classed under `git blame -w` | |
|---|---|
| Nothing owed | **318** |
| **Outcome ONE, REPAIRABLE** | **80** |
| **Outcome TWO, BORN WRONG** | **42** |
| False then, true now | **2** |

**THE 80 IS 80.** Every cell of [ADR-401](ADR-401.md) section 8's two tables reproduces. **The condition it ruled therefore has the population it was priced over, and this row is the first to spend it.**

**AND 77 OF THE 80 SURVIVE THE RULED INSTRUMENT AS WELL.** Under `git log -S` the same 80 are 77 class one and **3 undecidable**, the three being the `accountDetailLicence:1433` pointers [ADR-401](ADR-401.md) section 4 already enumerated as flipping. **No pointer in the 80 becomes born wrong under the better instrument**, which is the direction that matters: a repair permitted by the weaker leg and forbidden by the stronger would be the class-two hazard, and there is none.

---

### 6. THE FINDING: 65 of the 80 have a surviving occurrence, and the coordinate is derivable rather than a judgement

**[ADR-401](ADR-401.md) SECTION 8's ARGUMENT IS THAT A REPAIRER LANDING ANYWHERE IN THE ACCEPTED BAND GETS A GREEN RUN AND A WRONG POINTER**, and it priced that band at 28.0 percent for the worst name in the most cited file. **The answer is not to pick from the band. It is not to pick at all.**

**THE OCCURRENCE THE RECORD READ IS A LINE, AND A LINE HAS AN IMAGE.** At the commit that introduced the coordinate, exactly one line within the window held the named subject. That line is followed to this base through the diff between the two blobs, and the repair is its image. **No occurrence is chosen; one is tracked.**

| Over the 80, the occurrence the record read | |
|---|---|
| Survives at a new coordinate, **byte-identical** | **65** |
| The line was deleted or replaced | **12** |
| No introducing commit under the ruled instrument | **3** |

**ALL 55 REPAIRED POINTERS WERE VERIFIED BYTE-IDENTICAL AT THEIR NEW COORDINATE AND VERIFIED TO HOLD THE NAME**, by reading the blob at the write commit and the file on this base and comparing the two lines. **55 of 55**, against [ADR-396](ADR-396.md)'s 36 of 38.

**AND THE DERIVATION HAD A BUG THAT AROSE FROM ARITHMETIC AND WAS CAUGHT BY ARITHMETIC, WHICH IS RECORDED BECAUSE IT WOULD HAVE PRODUCED 28 WRONG POINTERS.** The first line map consumed the anchor line of a pure insertion hunk as though it had been replaced, so every coordinate below any insertion walked forward by the size of that insertion. **It reported 37 byte-identical images and 28 whose text had changed.** A line that MOVES without being EDITED cannot change text, so 28 was not a result, it was a contradiction. **Corrected, the same run reports 65 byte-identical and zero changed**, and the 28 were the off-by-one. **A row that had shipped the first number would have written 28 pointers to coordinates nothing in this repository could contradict.**

---

### 7. REFUTED: [ADR-398](ADR-398.md)'s seam has ZERO surviving instances, and the reason is that an owed list cannot see inside its own wave

**[ADR-401](ADR-401.md) SECTION 13 ITEM 2 RECORDS ONE SURVIVING UNREPAIRED INSTANCE AT [ADR-337](ADR-337.md) LINE 202. IT IS REPAIRED.** The cell was read at primary source before anything else in this row was done.

| | |
|---|---|
| What [ADR-337](ADR-337.md):202 carried at `28e2b0ee`, [ADR-401](ADR-401.md)'s base | `` [`gates.mjs:8412`](../../scripts/corpus/gates.mjs) `` beside `` `sites: 7` `` |
| What it carries on `1f621d5c` | the register entry **named**, `` (`:8412`, retired) ``, the cardinal untouched, the tense dated |
| The commit that repaired it | **`d548187a`**, [ADR-403](ADR-403.md), 2026-09-07 **02:54:04** |
| The commit that wrote [ADR-401](ADR-401.md) | `9e88acad`, 2026-09-07 **03:13:22** |
| Is either an ancestor of the other | **NO.** Two branches, one wave |

**SO [ADR-401](ADR-401.md) WAS RIGHT AND IS NOW WRONG, AND NOTHING IT DID CAUSED THAT.** Its base still carried `:8412`; the sibling that repaired it committed nineteen minutes earlier on a branch it could not see; the wave merged both. **This is [ADR-388](ADR-388.md) section 3's own mechanism, the one it measured at 13.5 percent of same-day pointers, arriving at an OWED LIST rather than at a citation.** A row's owed list is a dated record of what was outstanding **on that row's base**, and a wave that merges three rows can discharge an item before the entry naming it lands.

**THEREFORE NO ANNOTATION IS OWED AND NONE IS WRITTEN, WHICH NEEDS AN ARGUMENT BECAUSE THE OPPOSITE LOOKS LIKE THE CONVENTION.** [ADR-397](ADR-397.md) section 7 as scoped by [ADR-401](ADR-401.md) section 9 annotates **a wrong claim in a dated record**. **This claim was true when written**, which is [ADR-388](ADR-388.md)'s outcome one and not its outcome two: the record is stale, not wrong, and the file is not this row's to edit in any case. **Annotating it would record a defect where there was a correct measurement**, which is the same manufacture the ruling exists to prevent, pointing the other way.

**AND THE SEAM ITSELF IS EMPTY, DERIVED RATHER THAN ASSUMED.** Every line under `docs/` carrying a `` `sites: N` `` register restatement beside a `gates.mjs` coordinate was greped: **the four sites are [ADR-334](ADR-334.md):196, [ADR-335](ADR-335.md):160, [ADR-336](ADR-336.md):190 and [ADR-337](ADR-337.md):202, and all four now cite the register entry BY NAME.** [ADR-398](ADR-398.md) repaired the first three and [ADR-403](ADR-403.md) the fourth. **The remaining greps are prose ABOUT the seam**, including [ADR-398](ADR-398.md)'s own ruling sentence and [ADR-401](ADR-401.md)'s own owed-list line, and a sentence reporting a shape is not an instance of it.

**THE DISPATCH ALSO SAID THIS IS [ADR-381](ADR-381.md)'s MECHANISM AGAIN, AND IT IS THE OPPOSITE.** That mechanism is a defect named repeatedly by rows that each correctly decline to fix it, until the record of declining hardens into a prohibition. **Here the third naming was a repair.** [ADR-398](ADR-398.md) named it as outside its fence, [ADR-401](ADR-401.md) named it on a base where it stood, and [ADR-403](ADR-403.md) fixed it and derived its class independently rather than inheriting either.

---

### 8. The repairs, and the sentence [ADR-401](ADR-401.md) section 8 rules, applied 51 times

**51 SENTENCE UNITS CARRYING 55 POINTERS ACROSS 25 DOCUMENTS.** The unit is one citing line and one backticked token, because a comma token is one thing a reader trusts and [ADR-396](ADR-396.md) section 10 refused a repair that split one.

**THE FORM IS [ADR-388](ADR-388.md) SECTION 4's PLUS [ADR-401](ADR-401.md) SECTION 8's, AND NOTHING ELSE.** The coordinate moves; the retired coordinate is kept in backticks beside it **without a path**, which is that section's rule for a document; and the occurrence is NAMED. A repaired cell now reads `` [`productionAffiliateDeps:672`](../../apps/api/src/routes/affiliate.ts) (`:478` retired; the 1st of 3 occurrences in that file) ``.

**THE SUBJECT NAME IS NOT REPEATED IN BACKTICKS INSIDE THE ANNOTATION, AND THAT WAS A DECISION RATHER THAN A STYLE.** `citedIdentifier` binds the nearest backticked token BEFORE a citation, so a name written into an annotation could rebind a later citation on the same line. **Measured rather than argued: the reader's named population is 497 before this diff and 497 after, and the pointer-by-pointer difference is exactly the 55 moved and nothing else.** No binding changed, and not one retired coordinate reads as a citation.

**EIGHT OF THE 55 LAND ON A LINE THE RECORD DID NOT NAME, AND THE REPAIR IS STILL THE HONEST ONE.** In those eight the subject sat one or two lines from the cited coordinate and passed on the window rather than on the line. **The repair lands on the occurrence, which is what section 8 asks to be named**, so the pointer becomes exact where it was previously only within reach. **The record's claim is preserved and sharpened; it is not moved to a different subject.**

**AND ONE UNIT MOVES A POINTER THAT CURRENTLY PASSES, WHICH IS THE SINGLE MOST QUESTIONABLE EDIT IN THIS DIFF AND IS STATED AS SUCH.** `` `scope.ts:482,487` `` in [`STATE.md`](../STATE.md):5710 and [ADR-176](ADR-176.md):32 names two plan-config tables five lines apart. `:482` has drifted; **`:487` still passes, and it passes by coincidence**, because `firm` occurs 130 times in that file and a line within two of 487 happens to hold one. **Its true image is 887.** Repairing `:482` alone would put 405 lines between two coordinates the record reads as neighbours, which is exactly the separation [ADR-396](ADR-396.md) section 10 refused a repair to avoid. **Both halves move, the adjacency is preserved at five lines, and both land on their own images.** **This is the first time in this corpus a repair has moved a pointer that was green**, and a founder who holds that a passing pointer is never touched would refuse these two and accept the false adjacency instead.

---

### 9. The 27 refused, in six shapes, and four of them are new

**EXPECTING REFUSALS AND REPORTING THEM AS RESULTS IS THE INSTRUCTION AND THIS IS THE RESULT.**

| Shape | Pointers | Why the coordinate does not move |
|---|---|---|
| **THE OCCURRENCE LINE WAS DELETED OR REPLACED** | **12** | The line that held the subject at the write commit does not survive the diff to this base. [ADR-397](ADR-397.md)'s precondition on outcome one is not satisfied at the granularity that matters: the NAME is still in the file, at 8, 48 or 94 other places, and **that is worse than absence rather than better**, because a repairer with a live name and no surviving line is exactly the guesser [ADR-401](ADR-401.md) section 8 legislated against. [ADR-396](ADR-396.md)'s second and third shapes |
| **REGISTERED AS A KNOWN FINDING IN A SIBLING'S FILE** | **6** | All six sit in [WAVE-06](../plans/WAVE-06-admin-console-transport.md) and all six are named in `DOC_CITATIONS_OWNED_ELSEWHERE`. **The register SHRINKS ONLY**, so a repair makes the entry match nothing and `RI-16` red, and the entry lives in [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs), **row `406`'s file this wave**. Watched, section 10 |
| **THE TARGET FILE IS A SIBLING'S THIS WAVE** | **4** | Four repairs were derived, applied, verified byte-identical, and then REVERTED. They point into [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs), which row `406` holds. **[ADR-403](ADR-403.md)'s rule: a coordinate repaired to a value its own wave invalidates is the defect re-armed with a fresher date.** The dispatch forbade line numbers into `scripts/corpus/**` and said nothing about `packages/tooling/**`; the hazard is identical and the corpus's own ruling is followed rather than the dispatch's narrower sentence |
| **NO INTRODUCING COMMIT UNDER THE RULED INSTRUMENT** | **3** | The three `accountDetailLicence:1433` pointers. `git log -S` over the token finds no commit, so `G2` is undecidable and `git blame -w` alone is not sufficient to decide it ([ADR-396](ADR-396.md) section 7) |
| **OUTSIDE THIS ROW'S FENCE** | **1** | [`db.test.ts`](../../apps/api/test/db.test.ts):66. Byte-identical image, derivable, and `apps/**` is not this row's to write |
| **A RANGE WHOSE END IS NOT DERIVABLE** | **1** | [ADR-096](ADR-096.md):38 cites `` `scope.ts:40-48` ``. The subject sits at the range START, so the start has an image and **the end has none**: nothing in the record says what line 48 was chosen to include. Repairing the start alone would assert a span the record never asserted, which is section 10 of [ADR-396](ADR-396.md) inside one token rather than across two |

**THE SECOND, THIRD AND SIXTH SHAPES ARE NEW, AND SO IS THE FIRST IN ITS REASONING.** [ADR-396](ADR-396.md)'s four shapes are all properties of the SUBJECT. **Three of these six are properties of the WAVE or the FENCE**, which is a different kind of unrepairability and one that expires: the same pointer is repairable by a row whose wave does not hold the target file.

---

### 10. Falsification: four seeds, and the pair that carries the honesty statement is B against C

**EVERY SEED WAS RESTORED BYTE-IDENTICAL AND VERIFIED WITH `sha256sum -c` AGAINST A MANIFEST TAKEN BEFORE THE FIRST SEED AND RE-CHECKED AFTER EACH RESTORE. NO RESTORE USED `git checkout --`, AND `git status --porcelain` NAMED ONLY THIS ROW'S OWN FILES AFTER EVERY ONE.**

| # | Seed | Watched | What it establishes |
|---|---|---|---|
| **A** | The registered [WAVE-06](../plans/WAVE-06-admin-console-transport.md) pointer `` `assertNamesNoSubject:219` `` repaired to its correct image `:249` at **one** of its two sites | **GREEN, 35 of 35** | **The prediction failed, and the failure is the finding.** A `DOC_CITATIONS_OWNED_ELSEWHERE` key is `(file, cites, name)` and carries **no line**, so the entry stays matched by the site that was not repaired |
| **A2** | The same repair at **both** sites | **RED, 34 of 35**, *"the register claims ... and it is not one on this ref"* | The blocker is real and it is mechanical. The remedy the message names is deleting the entry, and the entry is in row `406`'s file |
| **B** | A repaired pointer in a DATED record moved to a badly wrong coordinate, `` `scope.ts:913,922` `` to `` `scope.ts:1,2` `` | **GREEN, 35 of 35** | **Nothing in this repository checks any of the 55 repairs.** `RECORD_HEADING` masks the record from `RI-16` and `RI-15` reads source comments only |
| **C** | The identical wrongness on a LIVE, UNREGISTERED citation in the same corpus, `` `buildFeed:306` `` to `:3` | **RED, 34 of 35**, naming the site and *"303 lines away"* | The check is ARMED, so B is scope rather than a dead gate |

**A AGAINST A2 IS A MEASUREMENT THAT CHANGED THIS ROW'S OWN ANSWER.** The six [WAVE-06](../plans/WAVE-06-admin-console-transport.md) pointers were refused on a prediction; seed A refuted the prediction; **seed A2 confirms the refusal on a different mechanism than the one it was made on.** A row that had stopped at the first green run would have reported the register as no obstacle and repaired six pointers into a red build on a sibling's branch.

**AND B AGAINST C IS THE HONESTY STATEMENT THIS ENTRY OWES.** **A green run is not evidence that any of these 55 repairs is right.** They rest on the blob at the write commit, the diff to this base, and a byte comparison, all of which are recorded here so a later row can re-derive rather than trust. **The gates that ran green ran green before the diff too, and would have run green over 55 fabrications.**

---

### 11. Citations this diff moved, audited under [ADR-388](ADR-388.md)'s own ruling

**EVERY ONE OF THE 26 FILES IS STRICTLY LINE-COUNT NEUTRAL, CHECKED PER FILE RATHER THAN IN AGGREGATE.** 45 insertions against 45 deletions, every change a line replaced by a longer line. **So no citation anywhere in this corpus goes stale because of this diff, and the citation-repair exception is UNSPENT.**

**THE ONE FILE THIS ROW DID NOT EDIT BY HAND IS [`sessions/README.md`](../sessions/README.md), AND THAT IS [ADR-396](ADR-396.md) SECTION 8's MECHANISM REPRODUCED.** Eight of the repaired session logs carry their `<!--index:` line verbatim into that file's `session_entries` generated span. `CI-06g` went **RED at 32 of 33** in the window between the repairs and the regeneration, `node scripts/corpus/gates.mjs generate` rewrote exactly **eight rows**, and the gate returned to 33 of 33 on its own. **No gate was weakened, no register grown and no exemption written**, and the eight rows are a consequence of in-fence edits rather than an out-of-fence edit.

**THE REGISTRY ROWS THIS DIFF APPENDS ARE AUDITED THE SAME WAY [ADR-401](ADR-401.md) AUDITED ITS OWN, AND THE FIGURE IS DERIVED RATHER THAN INHERITED.** That entry counted **seven** citations at or below its own insertion. **There are ten**, because its own three registry rows added three more at its lines 266, 267 and 268, so the population an appending row disturbs grows by exactly the rows the previous appending row wrote.

| Citing site | Cites | Bound name | On the base `1f621d5c` | Class |
|---|---|---|---|---|
| [ADR-337](ADR-337.md):70, [ADR-386](ADR-386.md):265, [ADR-388](ADR-388.md):298, [ADR-389](ADR-389.md):176, [ADR-398](ADR-398.md):186, [ADR-401](ADR-401.md):266 and :267 | `ALLOCATION.md:638` | **none** | `:638` is the `RI-nn` allocation HEADING, not the `RI-37` row | already wrong |
| [ADR-386](ADR-386.md):266, [ADR-398](ADR-398.md):187, [ADR-401](ADR-401.md):268 | `ALLOCATION.md:412` | **none** | `:412` is an EMPTY LINE | already wrong |

**NOT ONE OF THE TEN WENT STALE BECAUSE OF THIS DIFF. ALL TEN WERE ALREADY WRONG ON `1f621d5c`**, read at both coordinates at the base rather than inherited, and this diff moves them three lines further from subjects they lost days ago. **None may be repaired under either reading of `G1`**: the binder returns no name for all ten, and under the person's `G1` that [ADR-400](ADR-400.md) section 12 rules is the right reader, [ADR-388](ADR-388.md) section 13 already derived `:638` as class two, `G2` failing by one line, MUST NOT BE REPAIRED. **So the citation-repair exception is UNSPENT twice over**: the repair diff moves no line at all, and the registry diff reaches only pointers that were already wrong.

**THE OTHER THREE INSERTIONS MOVE NOTHING, DERIVED THE SAME WAY.** [`STATE.md`](../STATE.md) gains an appended section at 13865 against a deepest cited line of 12814, derived through the same reader, [`sessions/README.md`](../sessions/README.md) a generated row at 825 against 781, and [`README.md`](README.md) a generated row at 418 against 378.

---

### 12. Verification, every figure derived at the moment of reporting

- **Suite: 336 files / 8,109 passed / 10 skipped / 0 failed**, against a base reproduced on `1f621d5c` **BEFORE the first edit** of **336 files / 8,109 passed / 10 skipped / 0 failed**. **DELTA ZERO ON EVERY AXIS.**
- **Gates 33 of 33** after `generate`, derived by counting the runner's own `PASS` lines. **Invariants 35 of 35**, before and after. `typecheck`, `lint` and `format:check` all exit 0, measured on the base as well.
- **`pnpm run verify` was NOT run and [`falsify.mjs`](../../scripts/corpus/falsify.mjs) was NOT run**, both forbidden to this row and both mutate the working tree.
- **`git diff 1f621d5c -- apps packages scripts e2e playwright.config.ts CLAUDE.md docs/ops docs/architecture docs/testing docs/reviews` is EMPTY.** [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) and [`gates.mjs`](../../scripts/corpus/gates.mjs) were opened for READING only; the relocated copy of the checker lives outside the repository and was never written into the tree, so `scripts/corpus/**` and `packages/tooling/**`, which are row `406`'s this wave, are byte-identical to their base.
- **[ADR-388](ADR-388.md), [ADR-400](ADR-400.md) and [ADR-402](ADR-402.md) are row `405`'s and were not written**, and [ADR-396](ADR-396.md) is row `406`'s and was not written. All four were read.
- **No invariant number was minted, no invariant or gate was added, widened or weakened, no leg was written, no register grew, no test was skipped, disabled or quarantined, no `CI-06` letter was spent, no migration number was taken or reserved, no `status` field was moved, and no port was wired.**
- **`405` and `406` are RESERVED in [ALLOCATION](ALLOCATION.md).** Neither reservation names a subject, guesses a branch, claims anything, or carries a link for a number it does not own.

---

### 13. What is still owed, stated precisely enough to be picked up

1. **THE 27 REFUSED ARE ENUMERATED AND NOT REPAIRED**, section 9, and **eleven of them are refused by this WAVE rather than by the pointer**. The 6 registered, the 4 in row `406`'s file and the 1 outside this fence are all repairable by a row whose wave does not hold the target, and the 4 in [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) were derived and verified before being reverted, so that row does not start from nothing.
2. **THE 12 WHOSE OCCURRENCE LINE IS GONE NEED A RULE THIS ROW DID NOT WRITE.** [ADR-397](ADR-397.md)'s precondition asks whether the subject is still in the file and all 12 answer yes; the question that decides them is whether the LINE survived, and the two come apart exactly where the name is not unique. **That is a sharpening of the precondition and it belongs to whoever owns [ADR-388](ADR-388.md).**
3. **A REGISTERED FINDING WITH TWO SITES IS RELEASED ONLY WHEN BOTH ARE REPAIRED**, section 10 seed A. That is a property of the register's key rather than a defect, but nothing states it, and a row repairing one site of two will read a green run as permission.
4. **AN OWED LIST CANNOT SEE INSIDE ITS OWN WAVE**, section 7. Nothing is proposed: a gate would have to read three branches that do not exist yet. **What is cheap is for a row to re-read the site before inheriting an owed item**, which is what this row did and is the only reason the refutation exists.
5. **1,403 POINTERS IN `docs/` ARE STILL IN A FORM NO CHECK CAN SEE**, and the 80 this row spent are 80 of the 3.8 percent the binder reaches. **No widening is proposed**, for [ADR-388](ADR-388.md) section 15 item 5's reason and [ADR-400](ADR-400.md) section 12's.
6. **THE CONTAINER STILL ARRIVES WITH NO REPOSITORY**, section 2. Six consecutive sessions have paid the clone out of their own budget. **The shallowness assertion is gone from the dispatch and the empty container is not**, and this is the fifth consecutive entry to say no ADR can fix it.

---

### 14. Where this entry is most likely wrong

- **THE 55 ARE CHECKED BY NOTHING AND SEED B PROVES IT.** They rest on a line map this row wrote, and that map was **wrong on its first run in a way that would have produced 28 bad pointers**, section 6. It was corrected and every repair was then verified byte-identical against the blob, which is the strongest available evidence and is not a check. **If the map is wrong in a second way, the same verification would confirm the same wrong lines**, because both legs read the map.
- **THE OCCURRENCE INDICES ARE COUNTED WITH THE CHECKS' OWN SUBSTRING MATCH, WHICH IS NOT WHAT A READER MEANS BY AN OCCURRENCE.** `firm` matches inside `confirm`, so *"the 27th of 130 occurrences"* is a statement about the reader rather than about the file. **The coordinate does not depend on it**: the image is tracked from a line, not chosen from a list. **Only the naming sentence does**, and it is the sentence [ADR-401](ADR-401.md) section 8 ruled must be written.
- **MOVING `:487`, WHICH PASSES TODAY, IS THE EDIT MOST LIKELY TO BE REFUSED ON READ**, section 8. The argument is adjacency and it is [ADR-396](ADR-396.md) section 10's argument used to permit rather than to refuse. **A founder may hold that the refusal direction is the only safe one.**
- **THIS ROW REFUSED FOUR REPAIRS ON A HAZARD IT COULD NOT MEASURE.** Row `406`'s branch does not exist on the remote, so whether it moves a line in [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) is unknown. **If it does not, four correct repairs were declined**; [ADR-403](ADR-403.md)'s rule was followed because the cost of being wrong is asymmetric, not because the hazard was observed.
- **THE REFUTATION IN SECTION 7 IS ABOUT ONE SITE.** It shows [ADR-401](ADR-401.md) section 13 item 2 is discharged; it says nothing about the rest of that entry's owed list, and items 1, 4 and 5 were not tested.
- **THIS ROW READ SEVEN ADRs AT LENGTH AND RAN ONE READER OVER THREE TREES.** A figure recorded only in a session log, and any citation form the backticked reader does not match, is outside everything measured here.

---

### 15. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** Inherited as well as owed: [ADR-357](ADR-357.md) and [ADR-360](ADR-360.md) through [ADR-403](ADR-403.md) are `proposed` and unsigned, two rows run beside this one holding `405` and `406`, and this row settles none of them.

  **What a founder read adds and this entry cannot.**

  **THE FIRST THING TO DECIDE IS WHETHER A DATED RECORD'S COORDINATE MAY BE MOVED AT ALL, 55 TIMES, IN ONE DIFF.** [ADR-388](ADR-388.md) section 2 permits it and this row has met every condition that ruling and its two amendments impose. **But no check can see any of these 55**, seed B, so the corpus is being asked to accept a large edit to its own history on the strength of a derivation rather than a gate. **A founder who holds that the unreadable population should be left alone until something can read it would refuse the whole diff**, and the reply is that leaving it alone is what produced 80 pointers nobody could repair without guessing.

  **THE SECOND IS THAT THIS ROW REVERTED FOUR CORRECT REPAIRS FOR A REASON THAT IS NOT ABOUT THEM.** They were derived, applied and verified, and then taken out because a sibling holds the file they point into. **That is [ADR-403](ADR-403.md)'s rule and it is right**, and it also means the corpus's repair rate is now partly a function of how its waves are cut. **Three of the six refusal shapes in section 9 are properties of the dispatch rather than of the pointer**, which is a new thing for this register to be recording.

  **AND THE THIRD IS THAT AN OWED LIST WAS WRONG WITHIN NINETEEN MINUTES OF BEING WRITTEN, BY A SIBLING, AND THE DISPATCH THEN HARDENED IT INTO AN ACCUSATION.** [ADR-401](ADR-401.md) recorded a true measurement about its own base; the dispatch read it forward as *"repaired by nobody"* and named [ADR-381](ADR-381.md)'s mechanism, which is the opposite of what happened. **This is the twelfth consecutive wave in which a row has refuted a premise it was handed**, and the premise this time was manufactured by carrying a dated record forward as though it were a live one. **That is the same error this corpus rules on in citations, committed against an ADR.**

- **Approval:** `___________________________` (UNSIGNED. **CONTROL OVER THE CORPUS'S OWN POINTERS, `E2` READ OWED.** **Merging is not signing.**)

| | |
|---|---|
| **Approved by** | |
| **Date** | |
