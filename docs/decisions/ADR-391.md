## ADR-391: five findings that no fence covered, four repaired and one repair refused, priced with the runner ADR-386 said a row needs and did not have (2026-09-06, status: proposed)

**[ADR-381](ADR-381.md) FOUND THE MECHANISM: A FINDING NOBODY'S FENCE COVERS IS A FINDING THAT DOES NOT GET FIXED. THIS ROW IS THAT DIAGNOSIS TURNED INTO A FENCE, AND EVERY ITEM IN IT HAD BEEN NAMED BY SEVERAL ROWS AND REPAIRED BY NONE.** Four are repaired here. **The fifth is not, because the repair on offer for it is wrong**, and the measurement that shows so is one `import` statement in a file nobody had opened. **AND THE INSTRUMENT [ADR-386](ADR-386.md) SECTION 11 ITEM 1 ASKED FOR IS BUILT**, so the price this corpus charges prose is derivable by the next row rather than reconstructed from a scratch copy that is deleted when the session ends.

- **Origin:** [ALLOCATION](ALLOCATION.md) row `391`, branch `claude/the-findings-nobodys-fence-covered`, session **586**, off the wave integration branch rather than off `main`. ADR number and session number assigned centrally and not derived.
- **MONEY-ADJACENT. ADR-003 STRICT. `E2` READ OWED.** `packages/db` is the accessor every money read goes through, and `apps/worker/src/index.ts` is the barrel over the run ledger and the payout sweeps. Ships `proposed` with an **UNSIGNED** approval line. **Merging is not signing.**
- **THE STANDING BAR IS NOT TOUCHED AND THIS IS THE FIFTH CONSECUTIVE ROW TO LEAVE IT STANDING.** No port was wired, no job scheduled, no sink passed, no default parameter added to `expirySweepIo`, and no cron expression, interval, batch size, concurrency, retry count, backoff or timeout was invented. [ADR-382](ADR-382.md)'s bar stands with its two exits unchanged, after [ADR-376](ADR-376.md), [ADR-378](ADR-378.md), [ADR-385](ADR-385.md) and [ADR-387](ADR-387.md).
- **NO MIGRATION WAS TAKEN OR RESERVED, NO INVARIANT WAS MINTED, AND NO GATE WAS REGISTERED.** `git diff --stat origin/main -- packages/db/migrations scripts/db` is **EMPTY**, derived at reporting time. `RI-` numbers stand at **35**. The new runner is an INSTRUMENT and is deliberately absent from `gates.mjs`, which is another row's file this wave in any case.
- **NOT ONE EXECUTABLE LINE CHANGED.** Every source edit is a comment or a JSON documentation key. The one new file is a runner nothing imports and no pipeline invokes.
- **EVERY FIGURE THE DISPATCH HANDED THIS ROW WAS DECLARED SECOND-HAND AND EVERY ONE WAS RE-DERIVED AT PRIMARY SOURCE.** Two do not reproduce and sections 4 and 6 say which. One handed-down repair is refused outright and section 5 is the measurement.

---

### 1. What was measured before the first edit

**THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL.** `/home/user` was empty: no clone, no `node_modules`, and the dispatch's own instruction not to prefix a command with a `cd` into the tree described a tree that was not there. The repository was attached to this session's authorized set and cloned by hand, and `HEAD` was confirmed to be the `3b664fcf` the row names rather than assumed.

**AND THE ORDINAL THIS FACT IS USUALLY REPORTED WITH IS ITSELF A CARRIED FIGURE, WHICH IS THIS ROW'S SUBJECT ARRIVING IN ITS OWN FIRST SECTION.** Derived at reporting time: **27 session logs under `docs/sessions/` carry the exact phrase**, the earliest being [session 482](../sessions/2026-08-30-session-482.md). **TWO ENTRIES BOTH CALL THEMSELVES THE THIRTEENTH CONSECUTIVE ONE**, [ADR-386](ADR-386.md) and [session 582](../sessions/2026-09-06-session-582.md), and [ADR-385](ADR-385.md) calls itself the twelfth. **The run is not consecutive either**: sessions [579](../sessions/2026-09-06-session-579.md) and [583](../sessions/2026-09-06-session-583.md) record nothing, and there are further gaps at 563 and 572. This log is the **28th** to carry it and claims no ordinal beyond that.

**Base, on a clean tree, before anything was touched: 336 files / 8,107 passed / 10 skipped / 0 failed.** Gates **33 of 33**, invariants **35 of 35**, `typecheck`, `lint` and `format:check` all clean. **THESE ARE NOT [ADR-386](ADR-386.md)'s FIGURES**, which are `336 / 8,099 / 10 / 0` measured on `8fa66104`; rows `386`, `387` and `388` have merged into the wave branch since.

**THE CITATION CENSUS WAS RUN BEFORE THE FIRST EDIT AND IT DECIDED THE SHAPE OF EVERY REPAIR IN THIS DIFF.** It was not run from a scratch copy assembled by hand, which is what [ADR-385](ADR-385.md) and [ADR-386](ADR-386.md) each had to do: it was run from [`scripts/corpus/citation-fan-in.mjs`](../../scripts/corpus/citation-fan-in.mjs), written first and used for the rest of the session. Section 7 is that runner and section 8 is what it decided.

---

### 2. THE RULING

**RULED: THE FOUR REPAIRS THE DISPATCH NAMED ARE REAL, ARE MADE HERE, AND NOT ONE OF THEM NEEDED ANY LINE TO MOVE.** [ADR-370](ADR-370.md)'s line 84 and both section 3.1 cells, `apps/admin/package.json`'s copy of the `@playwright/test` clause, `pg-boss-queue.ts`'s wrong verb, and the two scope words in `lt-07.test.ts` and `rule-state-producibility.test.ts`. Sections 3 and 4.

**RULED: THE REPAIR [ADR-385](ADR-385.md) OFFERED FOR `packages/db` IS WRONG, THE SENTENCE IT POINTED AT AS THE MODEL IS FALSE FOR THE SAME REASON, AND THE SCOPE [ADR-387](ADR-387.md) OFFERED IN ITS PLACE IS NOT TRUE EITHER.** *Add the two words `at runtime`* was called the cheapest item on that list and the clearest. **It is neither: `packages/db/test/date-column-timezone.test.ts` imports `pg` as a VALUE**, which [ADR-387](ADR-387.md) found and had no fence to act on, so the qualified sentence is false exactly where the unqualified one is. **And `packages/db/src` does not rescue it**, because `scoped-db.ts` lives there and imports `pg` too. **Both sites are marked in place, neither is given the offered words, and the axis every published repair has qualified is the wrong one.** Section 5.

**RULED: [ADR-386](ADR-386.md) SECTION 5 REPRODUCES FOUR FOR FOUR AT PRIMARY SOURCE, AND [ADR-385](ADR-385.md)'s TWO ERRORS ARE CORRECTED IN THAT ENTRY RATHER THAN ONLY REPORTED ABOUT IT.** Its `numstat` sentence is wrong about its own diff in two independent ways, and its *three of six* is four clauses across three edits. **The clause it dropped unrecorded is restored at its own site**, which is the half a document-only correction cannot deliver. Section 6.

**RULED: THE FAN-IN RUNNER IS AN INSTRUMENT AND NOT A GATE, AND THE REFUSAL IS BY CONSTRUCTION RATHER THAN BY COST.** There is no threshold at which a fan-in is wrong. A file with 451 citations into it is carrying a PRICE, not a defect, and the only decision the number informs belongs to the row about to pay it. **No `RI-nn` is minted, no `CI-06` letter is spent and `gates.mjs` was never opened.** Section 7.

**AND RULED: THE POSITIONAL REFERENCE *ADR-370 LINE 84* IS INVISIBLE TO EVERY CONTROL THIS CORPUS HAS, INCLUDING THE ONE BUILT HERE.** Six documents carry it, `RI-15` and `RI-16` do not read it because it is not their grammar, and the runner in section 7 inherits that blindness because it borrows their reader. **This entry does not mint a check for it; it states the hole and works around it, and section 10 says why the workaround is not a fix.**

---

### 3. ADR-370, repaired at the seventh asking, and what six confirmations bought

**SIX ROWS FOUND THIS AND NONE COULD REACH IT.** [ADR-371](ADR-371.md) section 6 item 4, [ADR-376](ADR-376.md) section 5 and its section 9 item 1, [ADR-378](ADR-378.md), [ADR-382](ADR-382.md), [ADR-385](ADR-385.md) section 9 item 5 and [ADR-386](ADR-386.md) section 11 item 5 each re-derived the same defect and each recorded, in its own words, that `docs/decisions/ADR-370.md` was in nobody's fence. **Six confirmations bought one repair, and it arrived the wave somebody was handed the file.** That is [ADR-381](ADR-381.md)'s mechanism measured on a single document.

**THE TWO SECTION 3.1 CELLS.** They record `LiveIngestIo` and `WithdrawalApprovalSweepIo` as having *no inhabitant of any kind*. Derived here at the declaration rather than carried: **both have a refusing inhabitant**, `UNWIRED_LIVE_INGEST_IO` in [`live/ports.ts`](../../apps/worker/src/live/ports.ts) and `UNWIRED_WITHDRAWAL_APPROVAL_IO` in [`withdrawals/ports.ts`](../../apps/worker/src/withdrawals/ports.ts), **and that entry's own section 3 counts both among its eleven** while reserving the phrase for `ExpiryEventPort` alone. Neither file is in this fence, so neither is cited by line: this wave's rule is that a pointer into a file a row does not own is a pointer into a moving target, and the constant's name is stable where its line number is not.

**THE VERDICT DOES NOT MOVE AND THAT IS THE WHOLE OF THE EDIT.** A refusing default is not an adapter, so both rows are still blocked on a missing one. **The repair is to the sentence and never to the ruling**, which is [ADR-376](ADR-376.md) section 5's own distinction applied to the document it could not open.

**LINE 84, WHICH CARRIED THE ABSOLUTE AND THE CONSEQUENCE TOGETHER AND CALLED THEM *EXACT*.** `UNWIRED_EXPIRY_SWEEP_IO` composes a refusing `ExpiryEventPort` INLINE in [`sweeps/ports.ts`](../../apps/worker/src/sweeps/ports.ts), so a value of that type exists in this tree and is passable, and [`test/expiry-adapter.test.ts`](../../apps/worker/test/expiry-adapter.test.ts) case 6 passes it, gets the same value back and watches it reject. **The blocker is a call nobody has written rather than one that cannot be written.** What survives whole is the arity, and that half of line 84 was right.

**AND SECTION 3's TWELFTH-PORT CELL IS FALSE IN THE SAME WAY AND THE DISPATCH DID NOT NAME IT.** It is one table above the two cells that were named, it carries the identical error, and repairing the named cells while leaving it would have produced a document that contradicts itself two tables apart. It is corrected on the same terms. **Section 9 of that entry predicted this exact case and called it hypothetical**; the prediction is now recorded as fulfilled, beside the paragraph that made it.

**EVERY ONE OF THESE EDITS IS AN IN-PLACE REPLACEMENT OF THE LINE IT CORRECTS.** The reason is section 10: `ADR-370 line 84` is a positional reference in six documents and none of the controls can see it, so a document that grows above line 84 breaks six pointers silently. The corrections are appended INSIDE the cells and INSIDE line 84, and the only lines this diff adds to that file are below line 199.

---

### 4. The four false-clause sites, each derived at primary source

| site | what it claims | derived here | verdict |
|---|---|---|---|
| [`apps/admin/package.json:17`](../../apps/admin/package.json) | `e2e/pass.ts:17` is the only file in this repository that imports `@playwright/test` | **TWO files import it.** `e2e/pass.ts:17` and `playwright.config.ts:53`, over every `from`, `import(` and `require` of the specifier in the tree | **FALSE**, and repaired |
| [`pg-boss-queue.ts:4`](../../packages/queue/src/pg-boss-queue.ts) | the only file in the workspace that NAMES `pg-boss`, and `test/surface.test.ts` asserts that it does | **37 git-tracked files outside `docs/` name it and 69 inside**, against **one** importer. The case asserts `exactly one module in the workspace imports pg-boss` | **FALSE**, and repaired |
| [`lt-07.test.ts:125`](../../packages/rail/test/lt-07.test.ts) | the two lines in `posting.ts` are the only place *in this repository* a sign is written | **FOUR test files write a literal negative `amountCents`**, and `packages/ledger/src/posting.ts` is alone only under `src/`. Three further test files carry `-t.amountCents` as expectation TEXT, this one included | **FALSE**, and repaired |
| [`rule-state-producibility.test.ts:155` and `:663`](../../apps/api/test/rule-state-producibility.test.ts) | *in the tree* | **SIX files under `scripts/db` carry an uncommented `INSERT INTO rule_states`**, so `:155` is false and true only under `src/`. `:663` is false too: eight tracked files carry `decodeEngineGates(` and the sweep reads the three under a `src/`, because `deployableSources` walks `apps/*/src` and `packages/*/src` and nothing else | **BOTH FALSE**, both repaired |

**THE `pg-boss` FIGURE REPRODUCES AND ITS COMPANION DOES NOT.** [ADR-387](ADR-387.md) row 9 records *37 outside `docs/` and 67 inside*. **The 37 reproduces exactly. The 67 is 69 here**, and the difference is documents minted since, that entry itself among them, which is `CI-06/derivable-counts`'s own rule about a count of identifiers being a fact about a moment. **AND ITS SECTION 10 ITEM 5 CALLS THE 37 *NON-DOCUMENTARY*, WHICH IT IS NOT**: two of the 37 are `MERIT_BUILD_MASTER_PROMPT.md` and `packages/db/DELTA_MANIFEST.md`. **The non-documentary count is 35.** The verdict is untouched by either correction, and the numbers are corrected because they were offered as measurements.

**TWO OF THESE FOUR ARE [ADR-387](ADR-387.md)'s FINDINGS AND THIS ROW IS THE FIRST TO BE HANDED THE FILES.** Its section 5 sites 3 and 11 rule `rule-state-producibility.test.ts:155` and `lt-07.test.ts:125` FALSE, and both verdicts and both figures reproduce here exactly: **six probe files** and **four literal-negative test files**, re-derived from the tree rather than read off that table. **This row's own first reading of both was narrower and wrong**, taking each for a claim that was merely mis-scoped, and the probe count is what corrected it. That is worth recording because the dispatch described both sites in the mis-scoped terms and a row that had trusted the description would have written a weaker correction into the file.

---

### 5. The repair ADR-385 offered for `packages/db` is WRONG, and one import statement is the measurement

**[ADR-385](ADR-385.md) SECTION 9 ITEM 1 CALLS THIS THE CHEAPEST ITEM IN ITS LIST AND THE CLEAREST**, and gives the repair as *the two words `at runtime`*, *already written at `scoped-db.ts:4218`*. **[ADR-387](ADR-387.md) SECTION 10 ITEM 2 ALREADY FOUND THAT THE REPAIR DOES NOT WORK AND COULD NOT REACH THE FILES. THIS ROW CONFIRMS IT INDEPENDENTLY, ACTS ON IT, AND CORRECTS THE SCOPE THAT ENTRY OFFERED IN ITS PLACE.**

**DERIVED AT REPORTING TIME, over every `import` of the specifier `pg` in `apps/**` and `packages/**`. NINE FILES IMPORT `pg`, ALL NINE UNDER `packages/db/**`, AND TWO OF THE NINE IMPORT IT AS A VALUE.**

| file | form |
|---|---|
| `packages/db/src/client.ts` | **VALUE**, `import { Pool, types as pgTypes } from 'pg'` |
| `packages/db/test/date-column-timezone.test.ts` | **VALUE**, `import pg from 'pg'` |
| `packages/db/src/scoped-db.ts` and six suites under `packages/db/test/` | `import type`, erased |

**SO `client.ts:4` IS FALSE AS AN ABSOLUTE, AND `scoped-db.ts:4218`'s NARROWER SENTENCE IS FALSE TOO.** *`client.ts` remains the only file in the workspace that imports it at runtime* is refuted by a suite that imports the package's default export and runs in the same `vitest` invocation as everything else. **Adding those two words to `client.ts` would have produced a second copy of a false sentence, in the file the first copy was quoted from, and it would have looked like a repair.** That is the mechanism [ADR-034](ADR-034.md) named running through a REMEDY rather than through a restatement, and it is the reason a handed-down repair is worth deriving before it is applied.

**AND THE SCOPE [ADR-387](ADR-387.md) OFFERED INSTEAD IS ALSO NOT TRUE AS WRITTEN.** Its section 10 item 2 says *the scope that is true is `packages/db/src`*. **`scoped-db.ts` is under `packages/db/src` and it imports `pg`**, so at that scope the sentence is still false; what is true at it is `client.ts` being the only file there that imports `pg` AS A VALUE. **The qualifier the sentence needs is about the KIND of import and not only about the directory**, and every published repair so far has qualified the wrong axis.

**WHAT IS TRUE, AND IT IS MECHANICALLY ENFORCED RATHER THAN ASSERTED IN PROSE.** `merit/no-raw-db-client` bans the `pg` specifier across `apps/**` and `packages/**` with `packages/db/**` as its single `ignores` entry, and it reads the specifier without asking whether the import is type-only. **The claim the guard actually holds is that NO FILE OUTSIDE `packages/db/**` IMPORTS `pg`**, which the header goes on to state correctly in its very next clause. The absolute overstates it by precisely the exemption it then names, four lines later, in the same paragraph.

**AND THAT IS WHY NEITHER SITE IS REWRITTEN HERE.** The true sentence is longer than the false one and both files are frozen at the line the corpus cites: `client.ts:4` carries **eight** citations and `scoped-db.ts:4218` carries **nine**, all seventeen inside dated records that no commit may repair. **Both false sentences are kept whole and marked in place at zero citation cost, the derivation lives here, and the words `at runtime` are not spent.** A row that later wants the header to state the guard's claim outright should take `packages/db/**` with that as its objective, and it will be a rewrite of a paragraph rather than the two-word edit this list has been advertising for three waves.

---

### 6. ADR-385's own accounting, corrected in that entry and at the site it left wrong

**BOTH ERRORS WERE RE-DERIVED FROM THE DIFF AT `215e44fc` RATHER THAN FROM THE ENTRY THAT REPORTED THEM, AND [ADR-386](ADR-386.md) SECTION 5 REPRODUCES FOUR FOR FOUR.**

**ONE: THE `numstat` SENTENCE IS WRONG IN TWO INDEPENDENT WAYS.** `git show --numstat 215e44fc` reports **seven** source files, not the number that sentence names, and **two of the seven are not neutral**: [`playwright.config.ts`](../../playwright.config.ts) at 8 insertions against 4 and [`apps/portal/test/kyc-source.test.ts`](../../apps/portal/test/kyc-source.test.ts) at 7 against 6. **The diff grew by five lines while stating that it had not.** The five files under `apps/worker` are each neutral and they are the five its own table lists, which is what the sentence was true of; the generalisation is what failed, and neither file it failed on appears in the table it generalised from.

**AND THE CONCLUSION SURVIVES INTACT, WHICH IS WHY THIS IS A CORRECTION AND NOT A RETRACTION.** No citation moved, because nothing was cited below the added lines. **The claim is true and the arithmetic offered as its proof is not**, and [ADR-386](ADR-386.md) has since ruled the property that actually carries it.

**TWO: *THREE OF SIX* IS FOUR CLAUSES ACROSS THREE EDITS.** The `job.ts` wiring detail and the standalone `@param` wording are one repair, four lines apart in the same `expiry-adapter.ts` hunk. *A finding for the session log* is in `recon/sweep.ts`, which that entry's table of six does not list. **And a fourth clause was dropped that the entry did not record at all.**

**THE UNRECORDED DROP IS RESTORED AT ITS OWN SITE AND THAT IS THE HALF NEITHER PREVIOUS ENTRY COULD DELIVER.** [`index.ts:592-597`](../../apps/worker/src/index.ts) retired the false absolute AND dropped *`expirySweepIo` takes the sink as a REQUIRED argument* beside it, which is a fact about the signature and was not the clause being retired. [ADR-386](ADR-386.md) named the loss in a document. **A drop recorded in one document and absent from the file it happened in is a drop the reader of that file cannot see**, so the clause is back where it was taken from, verified at the declaration: `events` is positional with no default and `now` is defaulted.

**AND THE BLOCK IS STILL EXACTLY SIX LINES, BECAUSE SIX IS WHAT THE CITATION GRAPH ALLOWS IT.** `index.ts:592-597` is cited AS A RANGE by [ADR-385](ADR-385.md), [ADR-386](ADR-386.md) twice, [ALLOCATION](ALLOCATION.md), [session 581](../sessions/2026-09-06-session-581.md) and [STATE](../STATE.md), and **19 citations land on lines 592, 593, 594 and 596 individually**. Line 594 is cited twice for the sentence naming `apps/api/src/events.ts`, so that path could not move off it. **This is [ADR-386](ADR-386.md) section 4's finding happening to the row that is writing about it**, and the difference is that the budget was derived before the sentence was written rather than discovered by a green run afterwards.

---

### 7. `scripts/corpus/citation-fan-in.mjs`, which is an instrument and not a gate

**[ADR-386](ADR-386.md) SECTION 6 ENDS BY SAYING WHAT A ROW ACTUALLY NEEDS IS THE FAN-IN NUMBER FOR THE FILE IT IS ABOUT, AND THAT NO RUNNER REPORTS IT. ITS SECTION 11 ITEM 1 OWES IT.** Every fan-in figure in that entry's section 3, and every one in [ADR-385](ADR-385.md) section 1, came from an instrumented copy of the checks package built by hand and deleted before the gates were run. **Both entries did the right thing and neither left anything behind**, so the next row started by rebuilding the same scaffolding. This file is that scaffolding, kept.

**IT READS WITH THE CHECKER'S OWN READER AND NOT WITH A GREP, WHICH IS THE ONLY WAY ITS NUMBERS CAN AGREE WITH THE CHECKS.** `RI-15` and `RI-16` share one grammar: `citationsIn` tokenizes the backticks and `citationTargets` resolves a pointer either against the citing file or by suffix against the walk. **A grep reproduces neither the inheritance window nor the markdown-href resolution**, so a grep-derived fan-in would disagree with the checks in exactly the cases a row cares about.

**THOSE FUNCTIONS ARE NOT EXPORTED AND THIS ROW DID NOT EXPORT THEM.** `packages/tooling/**` is another row's fence this wave and adding an export to a file somebody else is editing is how a merge conflict becomes a false green. **So the runner copies the whole `checks/` directory to a temporary root, appends one export statement to the copy, imports it and deletes it.** The directory is copied whole rather than the one file because `repo-invariants.mjs` and `ui-server-endpoints.mjs` import each other, and rewriting one file's specifiers to point at the originals loads the graph twice and dies in a temporal dead zone. It is copied to the PATH IT ALREADY OCCUPIES because two constants in that module are derived from its own location: `REPO_ROOT` is three directories up and `OWN_PACKAGE` walks up to the nearest manifest. **Both failures were observed rather than anticipated, and both are the module correctly telling the truth about where it is.**

**IT REPORTS LIVE AND DATED EDGES APART, BECAUSE THEY ARE NOT THE SAME PRICE.** [ADR-386](ADR-386.md)'s ruling repairs a live pointer and NAMES a dated one, and a single total hides which a row is about to pay. **A file whose fan-in is 40 dated edges and 2 live ones is cheap to edit and reads expensive when the two are added together.** The document side deliberately counts what `RI-16` skips: an unpoliced pointer is still a pointer that goes wrong.

**TWO CENSUSES ARE REPORTED AND THE DIFFERENCE BETWEEN THEM IS THIS ROW'S OWN WRITING.** The census that PRICED the edits, taken on the base plus nothing, is **10,602 edges onto 582 files**, with `scoped-db.ts` at 451, `routes/payouts.ts` at 387 and `scope.ts` at 376. At reporting time, with this entry, its session log, its `STATE` section and its `ALLOCATION` row on the tree, it is **10,747 edges onto 583 files** and `scoped-db.ts` is 455. [ADR-386](ADR-386.md) recorded 9,614 edges on `8fa66104` with 443, 387 and 374. **`routes/payouts.ts` matches at 387 across all three**, which is an independent implementation of one reader agreeing with a hand-built one on the file that has not moved and differing on the two that have. **Nothing here re-types any of that entry's numbers.**

**AND IT IS NOT A GATE, BY CONSTRUCTION RATHER THAN BY COST.** There is no threshold at which a fan-in is wrong, so there is no assertion to make. `INVENTORY_PROBES`, the `CHECKS` array and `gates.mjs` are all untouched, and the runner is invoked by nothing: a row runs it when it is about to edit a file, which is the only moment the number means anything.

---

### 8. What this diff moved: NOTHING, and the runner is how that was decided rather than how it was checked afterwards

**EVERY SOURCE HUNK IN THIS DIFF IS AN IN-PLACE REPLACEMENT AT THE SAME LINE NUMBERS.** `git diff -U0` renders `@@ -17 +17 @@`, `@@ -155 +155 @@`, `@@ -595,3 +595,3 @@`, `@@ -8,2 +8,2 @@`, `@@ -4219 +4219 @@`, `@@ -5,5 +5,5 @@`, `@@ -125 +125 @@` and `@@ -663 +663 @@`. **The only insertions in the diff are into [ADR-370](ADR-370.md) below line 199 and into [ADR-385](ADR-385.md), which nothing cites by line or by position.**

**AND HERE IS WHAT A SINGLE ADDED LINE AT EACH EDIT POINT WOULD HAVE COST, DERIVED WITH THE RUNNER BEFORE EACH EDIT WAS WRITTEN.**

| file | edit point | pointers that would have moved |
|---|---|---|
| [`apps/worker/src/index.ts`](../../apps/worker/src/index.ts) | `:591` | **55**, 4 live and 51 dated, over 13 lines in 35 files |
| [`packages/queue/src/pg-boss-queue.ts`](../../packages/queue/src/pg-boss-queue.ts) | `:4` | **17**, 3 live and 14 dated |
| [`packages/db/src/client.ts`](../../packages/db/src/client.ts) | `:7` | **15**, 1 live and 14 dated |
| [`apps/api/test/rule-state-producibility.test.ts`](../../apps/api/test/rule-state-producibility.test.ts) | `:154` | **11**, all dated |
| [`packages/rail/test/lt-07.test.ts`](../../packages/rail/test/lt-07.test.ts) | `:124` | **8**, 1 live and 7 dated |
| [`apps/admin/package.json`](../../apps/admin/package.json) | `:16` | **4**, all dated |
| [`packages/db/src/scoped-db.ts`](../../packages/db/src/scoped-db.ts) | `:4218` | **3**, all dated |

**113 POINTERS, 9 LIVE AND 104 DATED. ZERO MOVED.** The 104 are the expensive ones: [ADR-386](ADR-386.md)'s reason 2 rules that a pointer inside a dated record cannot be repaired by a commit, so moving those lines would not have cost a repair, it would have cost 104 pointers nobody may fix.

**AND THE SAME SEVEN POINTS PRICE AT 128 NOW, 10 LIVE AND 118 DATED, BECAUSE OF THIS ENTRY.** The table above is the decision-time census and it is the one that governed the edits; the 15 new pointers are this row's own ADR, session log, `STATE` section and `ALLOCATION` row citing the files it repaired. **A row that documents a file makes the next edit to that file more expensive**, which is [ADR-386](ADR-386.md) section 3's finding arriving as a consequence of writing rather than of editing, and it is stated here because the runner made it visible.

**WHAT THAT PRICE BOUGHT AND WHAT IT COST IS STATED RATHER THAN HIDDEN, BECAUSE HIDING IT IS THE DEFECT [ADR-386](ADR-386.md) SECTION 6 REASON 4 NAMES.** Three of the seven corrections are shorter than the full derivation deserves and say *see ADR-391* instead: `client.ts`, `scoped-db.ts` and `pg-boss-queue.ts`. **One paragraph separator was spent**, the blank comment line at `client.ts:9`, which is now the last line of the correction. **NO CLAUSE WAS DROPPED ANYWHERE IN THIS DIFF.** Every sentence that was there is still there, whole, and the four repairs that fit in place are written out in full. **The line-count discipline here is a consequence of the fan-in and not a rule obeyed**, which is the distinction [ADR-386](ADR-386.md) ruled and this is the first row to apply it with the number in hand.

---

### 9. What is bound mechanically: NOTHING NEW, and the honest report is that this is correct

**NO CASE WAS ADDED, NO INVARIANT MINTED, NO GATE REGISTERED.** The suite delta is **ZERO on every axis** and that is the expected result: this diff changes no executable line, so a case here would be pinning prose, which [ADR-324](ADR-324.md)'s rule says goes stale in silence.

**AND THE THREE FACTS THIS ENTRY MOST DEPENDS ON ARE ALREADY BOUND BY CASES THIS ROW DID NOT WRITE AND DID NOT TOUCH.**

| fact | already bound by | goes RED when |
|---|---|---|
| the refusing `ExpiryEventPort` exists, refuses, and both censuses are blind to it | `test/schedule.test.ts` `9.1` | the sink stops being composed inline, or either census learns to see it |
| the sink is passable, demonstrated by passing it | `test/expiry-adapter.test.ts` `6` first | the value stops being reachable or stops refusing |
| `events` is declared with no default, which is the arity this row restored the sentence about | `test/expiry-adapter.test.ts` `6` second and `1` | the default parameter appears |
| exactly one module imports `pg-boss` | `packages/queue/test/surface.test.ts` | a second importer appears |
| no file outside `packages/db/**` imports `pg` | `merit/no-raw-db-client`, attached in `eslint.config.js` | the glob or the single `ignores` entry changes |

**THE LAST ROW IS THE ONE WORTH READING TWICE.** The true claim about `pg` has a real mechanism behind it and the false one had nothing, which is [ADR-385](ADR-385.md) section 5's separator holding on the site that entry got wrong.

**NO SEED WAS PLANTED, AND SECTION 8's TABLE IS WHY THERE WAS NOTHING TO WATCH FAIL.** A comments-only diff has no failure to observe. The one scratch artifact this session made, the instrumented copy of the checks package, is created and deleted inside a single process by the runner itself, in the system temp directory, and `git status --porcelain` was confirmed empty of it before any gate ran.

---

### 10. The blind spot this row worked around and did not close

**`ADR-370 line 84` IS A POSITIONAL REFERENCE INTO A DOCUMENT AND NOTHING IN THIS CORPUS CAN SEE IT.** Six documents carry it: [STATE](../STATE.md), [ALLOCATION](ALLOCATION.md), [sessions/README](../sessions/README.md), [session 580](../sessions/2026-09-06-session-580.md), [ADR-385](ADR-385.md) twice and [ADR-386](ADR-386.md). **`RI-15` and `RI-16` do not read it because `ADR-370 line 84` is not `ADR-370.md:84`**, and the runner in section 7 inherits that blindness exactly, because its whole virtue is that it borrows their grammar rather than inventing one.

**SO THE FILE THIS ROW WAS DISPATCHED TO REPAIR IS FROZEN ABOVE LINE 84 BY REFERENCES NO CONTROL PROTECTS, AND THE ONLY REASON NOTHING BROKE IS THAT THE ROW WENT LOOKING.** Every correction to that document is written inside the line it corrects, and the four added lines are below 199.

**THIS IS NOT MADE A CHECK AND THE REASON IS THAT THE SHAPE IS A GREP FOR A PHRASING RATHER THAN FOR A PROPERTY**, which is [ADR-385](ADR-385.md) section 11's own argument against its `S3` gate, and it is satisfied by rewording. **A row that wants to close it should probably close it at the other end**, by ruling that a reference into a document is written in the citation grammar so the checks can read it, which is a corpus convention and is the founder's. **It is stated here rather than left for a seventh row to rediscover**, which is the entire lesson of section 3.

---

### 11. Owed outside this fence, stated precisely enough to be picked up

1. **`packages/db/**` needs a paragraph rewritten, not two words.** Section 5. [`client.ts:4`](../../packages/db/src/client.ts) and [`scoped-db.ts:4218`](../../packages/db/src/scoped-db.ts) are both marked and neither is restated, because the true sentence is longer than the false one and seventeen dated citations hold both lines. **The repair [ADR-385](ADR-385.md) section 9 item 1 has been advertising for three waves is wrong and should be struck from that list**, which this entry does in that entry's own section 11 rather than only here.
2. **[ADR-376](ADR-376.md) line 92 still carries the retired sink clause**, which [ADR-385](ADR-385.md) section 9 item 4 raised as a new finding and no row has been given the file. `docs/decisions/ADR-376.md` is outside this fence. **It is the same shape as the item this row was dispatched for and it will need the same in-place treatment**, because `ADR-376 line 92` is a positional reference in four documents, derived here, and section 10 is about exactly that.
3. **[ADR-370](ADR-370.md) section 3's twelfth-port cell was repaired here although the dispatch named only line 84 and section 3.1.** Recorded so the next reader knows the edit was wider than the instruction, and why: leaving it would have made the document contradict itself two tables apart.
4. **[`apps/worker/src/schedule.ts`](../../apps/worker/src/schedule.ts) still reserves *no value at all, not even a refusing one* for `ExpiryEventPort`**, in the `ADR-376` header paragraph that is otherwise correcting the identical collapse. **Cited without a line number on purpose**: that file is not this fence's, and this wave's rule is that a pointer into a file a row does not own is a pointer into a moving target. It is kept beside its own correction there under `RI-14` and it is not this fence's. **Named, not repaired, and this row's diff does not make it worse.**
5. **The `//next` key in [`apps/admin/package.json`](../../apps/admin/package.json) is one JSON string carrying about twenty distinct claims.** Repairing one clause in it required reading the whole line. **No control reads that key at all** and its `INVENTORY_PROBES` sentence says so. It is a register with no reader and it should probably be a document.
6. **Nothing is owed inside `packages/tooling/**`, `scripts/corpus/gates.mjs`, `.github/workflows/corpus.yml`, `docs/ops/runbooks/**`, [ADR-352](ADR-352.md), [ADR-312](ADR-312.md), [ADR-275](ADR-275.md) or the session 466 log.** None was opened for writing. `repo-invariants.mjs` was READ, and is copied at run time by the section 7 runner, which writes nothing.

---

### 12. Where this entry is most likely wrong

**SECTION 7's RUNNER TRUSTS A COPY, AND A COPY IS A SECOND PLACE THE GRAMMAR CAN BE.** It is copied at every invocation from the file on disk, so it cannot drift, but the copy runs with a different `REPO_ROOT` and a different `OWN_PACKAGE` root than the original does. **`OWN_PACKAGE` resolves to `packages/tooling/` in the copy because the layout is preserved, and that was verified by the run failing loudly when it was not.** A future change that makes either constant mean something the copy cannot reproduce would make this runner quietly disagree with the checks, and nothing would report it. **A row that adds a third derived-from-location constant to that module should re-read this paragraph.**

**AND THE RUNNER COUNTS EDGES, NOT DAMAGE.** An edge onto a line whose citation carries no bindable name is one `RI-15` asserts almost nothing about, and one onto a named declaration is load-bearing. **Section 8's 113 treats them alike**, so it is an upper bound on the harm and not a measure of it. [ADR-386](ADR-386.md) section 6 reason 1 measured that distinction at 10.2 percent and this runner does not reproduce it.

**SECTION 5's CENSUS IS OVER `apps/**` AND `packages/**` AND NOT OVER THE WHOLE TREE.** `e2e/`, `scripts/` and the root were read for the `@playwright/test` figure and not for the `pg` one, on the ground that the lint rule's own glob is the two workspace roots. **A `pg` import in `scripts/` would be invisible to both the rule and this count**, and it would falsify section 5's true sentence rather than its false one.

**AND THE FOUR REPAIRS IN SECTION 4 ARE MARKERS THAT NAME THIS ENTRY, NOT SELF-CONTAINED CORRECTIONS.** A reader who opens `pg-boss-queue.ts` learns that the verb is wrong and must come here for the figure. **That is the corpus's idiom and it is also how a correction becomes unreachable when the entry naming it is one of four hundred**, which is [ADR-367](ADR-367.md)'s trade taken again without new evidence that it is the right one.

---

### 13. Verification

Every figure derived at reporting time off each runner's own last line, stated in the [session log](../sessions/2026-09-06-session-586.md).

- **Suite: 336 files / 8,107 passed / 10 skipped / 0 failed**, against a base of **336 / 8,107 / 10 / 0** reproduced on `3b664fcf` **BEFORE the first edit**. **Delta ZERO on every axis**, which is the attribution as well as the result: no case was added, none removed, none renamed, and no file moved.
- Gates **33 of 33** after `generate`. Invariants **35 of 35**. `typecheck`, `lint` and `format:check` clean.
- **`pnpm run verify` and `scripts/corpus/falsify.mjs` were NOT run**, both forbidden by this row because both mutate the working tree.
- **CITATIONS WERE CHECKED BY READING THEM AND NOT BY A GREEN INVARIANT RUN.** Section 8's hunk list is the evidence that none could have moved, and it is derived from the diff rather than from the checks, which by their own headers never read git history.
- **NO SEED WAS PLANTED.** Section 9 says why: the diff changes no executable line.
- **No pull request was opened and nothing was merged, by instruction.**

---

### 14. Approval

**Status `proposed`. The approval line below is UNSIGNED and merging is not signing.** Inherited as well as owed, and DERIVED rather than carried, because the phrase this slot usually holds is short: [ADR-385](ADR-385.md) and [ADR-387](ADR-387.md) both write it as *`ADR-357` and `ADR-360` through* the current number, which reads as though `358` and `359` were settled. **They are not.** Listing every `docs/decisions/ADR-*.md` at reporting time: **274 of the 374 entries carry `status: proposed`, and the run from [ADR-112](ADR-112.md) to [ADR-388](ADR-388.md) is unbroken.** The last entry that is anything else is [ADR-111](ADR-111.md), `accepted`. **This row settles none of them.** **`389` and `390` are RESERVED and not written**, derived by listing `docs/decisions/` at reporting time: two siblings hold them on this wave, neither number is claimed here, and neither reservation names a subject or a branch.

  **What a founder read adds and this entry cannot.**

  **THE FIRST THING TO DECIDE IS WHETHER THE DISPATCH IS NOW THE BINDING CONSTRAINT ON THIS BUILD.** Six rows found the [ADR-370](ADR-370.md) defect and none could repair it. [ADR-385](ADR-385.md) published a repair for `packages/db` that was wrong, and it stood for three waves because the file was never handed to anybody who could test it. **Both are failures of allocation and neither is a failure of diligence**, and every row involved did its job. A founder can change how fences are cut; a row can only report that the cut is where the loss happens.

  **THE SECOND IS WHETHER A WRONG REPAIR IN AN OWED LIST IS WORSE THAN NO REPAIR.** *Add the two words `at runtime`* was cheap, clear, wrong, and quoted forward by two entries. **A row in a hurry would have applied it, passed every gate, and left a false sentence in the file the corpus quotes for the true one.** Owed items are written to be picked up without re-derivation and that is their value; this one shows the cost. Whether owed lists should carry the repair or only the finding is a convention question and it is the founder's.

  **AND THE THIRD IS THAT THIS ROW BUILT A RUNNER NOBODY IS OBLIGED TO USE.** Section 7 is an instrument by construction, so nothing makes the next row run it, and the four sessions that paid this price before it existed each paid it by hand and then threw the tooling away. **An instrument with no caller is a file that rots**, which is `scripts/corpus/tsconfig.json`'s own stated reason for existing. Whether it should be named in `CLAUDE.md`'s start ritual, so a row about to edit a cited file is told to price it, is a decision this entry deliberately does not take.

- **Approval:** `___________________________` (UNSIGNED. **MONEY-ADJACENT, `E2` READ OWED.** **Merging is not signing.**)

| | |
|---|---|
| **Approved by** | |
| **Date** | |
