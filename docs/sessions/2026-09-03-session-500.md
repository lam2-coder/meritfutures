---
status: approved
depends_on: [README.md]
last_updated: 2026-09-03
---

## 2026-09-03 - Session 500: row 309's question was ruled by ADR-289 and applied, so NO RULING WAS MADE AND NO ADR WAS MINTED

<!--index: **NO ADR. NO RULING. `ADR-309` IS NOT MINTED AND THE NUMBER IS STILL FREE.** Row `309` dispatched [ADR-287](../decisions/ADR-287.md) slice 1 and finding `F2`, `eligibility_snapshot_id`, with the stop condition *"an ADR that NAMES ONE of the three shapes and REFUSES THE OTHER TWO IN WRITING"* and the standing instruction to **stop and report rather than choose when the specification and the tree contradict each other**. **THEY CONTRADICT EACH OTHER AND THIS SESSION STOPPED.** The row's premise is that the field *"is declared by the frozen API contract and nothing supplies it"*. **The second half holds and the first half is FALSE on this tree**: [ADR-289](../decisions/ADR-289.md) already owns [ADR-287](../decisions/ADR-287.md) slice 1 and finding `F2` (ALLOCATION row `289`, session **480**, **TAKEN AND LANDED**), it refused all three shapes in writing on the merits, it took the FOURTH, and the ruling is applied end to end. **VERIFIED AT SOURCE AND NOT INHERITED, FIVE PLACES**: the port declares `insertPayoutRequest(row: PayoutRequestInsert): Promise<void>` ([`payouts.ts:628`](../../apps/api/src/routes/payouts.ts)), the route calls `await tx.insertPayoutRequest(row);` with NO destructure ([`:1251`](../../apps/api/src/routes/payouts.ts)), `PayoutResponse` carries no such field, the suite asserts its ABSENCE with `expect(body).not.toHaveProperty('eligibility_snapshot_id')` ([`payouts.test.ts:513`](../../apps/api/test/payouts.test.ts)), and [API_CONTRACT section 6](../architecture/API_CONTRACT.md) at line **500** carries the removal note ending **"Do not restore it."** **SO THE STOP CONDITION IS UNMEETABLE HONESTLY**: three shapes cannot be refused as open when a landed entry already refused them, and the row forbids the fourth, which is the one the tree took. **THE ROW'S `WHY THIS MATTERS NOW` IS ALSO FALSE**: slice 6 is NOT blocked on this ruling, because [ADR-289](../decisions/ADR-289.md) section 4.3 already rewrote its stop condition. **AND THE SAME DEFECT IS DISPATCHED IN PARALLEL**: row `310` carries [ADR-287](../decisions/ADR-287.md) slice 2 and finding `F4`, which is ALLOCATION row `290`, [ADR-290](../decisions/ADR-290.md), **LANDED**. **THE FOUNDER'S FOURTH OPTION IS NOT FORECLOSED AND IS NOT A NEW ADR**: [ADR-289](../decisions/ADR-289.md) ships `proposed` and **UNSIGNED** with the `E2` read **OWED**, so the decision the row reserves to the founder is a SIGNATURE ON [ADR-289](../decisions/ADR-289.md), not a ruling a session can make twice. **NO `src/` LINE, NO TEST, NO MIGRATION WRITTEN OR NUMBER RESERVED, NO `RI-nn` MINTED, NO ADR, NOTHING SIGNED, NO FROZEN DOCUMENT TOUCHED.** Row `310`'s entries were not touched. **LANDMINE, [ADR-289](../decisions/ADR-289.md)'s own, HIT AGAIN**: the container arrived with **no `node_modules`**, so `pnpm install --frozen-lockfile` was run BEFORE any check. **No pre-install count was derived and none is claimed**; [ADR-289](../decisions/ADR-289.md) records `RI-18` erroring `Cannot find module 'typescript'` in that state, and a session reporting a count before installing reports a false one. Counts derived at reporting time, each command run separately: **33 of 33** gates after `generate`, **32 of 32** invariants, `format:check` clean. **`pnpm run verify` was NOT run and `falsify.mjs` was NOT run**, both forbidden by the row; vitest and typecheck were NOT run and are NOT claimed, this being a docs-only diff. **NO GITHUB ACCESS: THE BRANCH IS PUSHED AND NO PULL REQUEST WAS OPENED BY THIS SESSION.** -->

---

### 1. The objective, and why it was not met

Row `309` dispatched [ADR-287](../decisions/ADR-287.md) slice 1 and finding `F2`: rule what `eligibility_snapshot_id` is. Its stop condition is an ADR naming one of three shapes and refusing the other two in writing.

**The row also carries the standing instruction that governs this session**: *"STOP AND REPORT rather than choosing when the specification and the tree contradict each other."* They do, so it stopped. **Nothing was chosen and nothing was written into the decision registry.**

### 2. The contradiction, at primary sources

The row states the subject as *"`eligibility_snapshot_id` is declared by the frozen API contract and nothing supplies it."* **The second half holds. The first half is false on this revision.**

[ADR-289](../decisions/ADR-289.md) is titled for this exact finding and its first line reads *"Row `289` owns [ADR-287](../decisions/ADR-287.md) finding F2 and slice 1 of its section 7 ... THE CONTRACT MOVES."* ALLOCATION row `289` at line **281** records it **TAKEN AND LANDED**, `status: proposed`, approval line **UNSIGNED**, session **480**, branch `claude/an-id-with-no-supplier`.

**The seven lines [ADR-289](../decisions/ADR-289.md) section 4.2 left owed are all taken.** Checked by opening the files rather than by reading the record that says so:

| What [ADR-289](../decisions/ADR-289.md) ruled | Live source on this revision |
|---|---|
| The port member returns nothing | `insertPayoutRequest(row: PayoutRequestInsert): Promise<void>;` [`payouts.ts:628`](../../apps/api/src/routes/payouts.ts) |
| The route stops destructuring it | `await tx.insertPayoutRequest(row);` [`payouts.ts:1251`](../../apps/api/src/routes/payouts.ts) |
| The wire field is gone from `PayoutResponse` | `grep -n eligibility_snapshot apps/api/src/routes/payouts.ts` returns **two** lines, `:615` and `:1101`, and both are prose about the `jsonb` column. **Neither is a field.** |
| The tautological test is inverted rather than deleted | `expect(body).not.toHaveProperty('eligibility_snapshot_id');` [`payouts.test.ts:513`](../../apps/api/test/payouts.test.ts) |
| The frozen contract moved | [API_CONTRACT](../architecture/API_CONTRACT.md) line **500**, the removal note, closing **"Do not restore it."** |

**Repo-wide, the two spellings now return FOUR lines in THREE files and not one of them is a declaration**: two comments explaining the removal ([`payout-backend.ts:773`](../../apps/api/src/payout-backend.ts), [`payouts.ts:615`](../../apps/api/src/routes/payouts.ts)) and the inverted assertion with its comment ([`payouts.test.ts:506`](../../apps/api/test/payouts.test.ts) and [`:513`](../../apps/api/test/payouts.test.ts)).

**Even the drift downstream of the ruling has already been repaired.** [`payout-backend.ts:770-777`](../../apps/api/src/payout-backend.ts) records that a comment there *"read that this member must return `{ eligibilitySnapshotId }`"*, that the claim *"outlived that ruling by two sessions"*, and that it was corrected on [ADR-293](../decisions/ADR-293.md) section 4D.

### 3. Why the stop condition cannot be met honestly

**The three shapes are not open questions on this tree.** [ADR-289](../decisions/ADR-289.md) section 3 refuses each in writing and on the merits: `payout_requests.id` in 3.1, a minted uuid in 3.2, a migration in 3.3, **and because the migration is refused on the merits no number was taken and none was reserved**. An ADR-309 naming one of the three would either restate a landed entry under a new number or contradict it, and a second registry entry ruling a question already ruled is the drift this corpus mints ADRs to prevent.

**The fourth option is the one the tree took, and the row forbids taking it.** That is not a conflict this session may resolve by choosing. **It is also not foreclosed to the founder**, which is the part worth stating plainly: [ADR-289](../decisions/ADR-289.md) is `proposed` and **UNSIGNED** with the `E2` read **OWED**, so what the row reserves to the founder still stands as a decision, and its shape is a signature on [ADR-289](../decisions/ADR-289.md), or a refusal of it, rather than a ruling a session repeats.

### 4. Two further places where the row and the tree disagree

- **`WHY THIS MATTERS NOW` is stale.** The row states slice 6 is *"BLOCKED ON THIS RULING and cannot be dispatched until it lands."* [ADR-289](../decisions/ADR-289.md) section 4.3 already rewrote slice 6's stop condition: *"As ruled, there is no declared return value. Slice 6's `insertPayoutRequest` writes the row and returns nothing."* **Slice 6 is not blocked on a ruling. It is dispatchable.**
- **The parallel dispatch carries the same defect.** Row `310` is dispatched as [ADR-287](../decisions/ADR-287.md) slice 2, `failure_note` and `timeline`, which is finding `F4`. **ALLOCATION row `290` at line 282 records that as [ADR-290](../decisions/ADR-290.md), LANDED.** Reported because the dispatcher needs it. **Row `310`'s entries were not touched by this session and its side of the expected merge conflict was not resolved.**

### 5. What this session did not do

- **No ADR was minted. `ADR-309` is free.** The number pairs with the ALLOCATION row, so a corrected re-dispatch of row `309` can still take it.
- **No `src/` line, no test, no migration written, no migration number reserved, no `RI-nn` minted, nothing signed.**
- **No frozen document was touched.** [API_CONTRACT](../architecture/API_CONTRACT.md) was read and not edited.
- **No shape was chosen and no refusal was written into the registry**, because writing either would be the choosing the row forbids.

### 6. Blockers

**The dispatcher owns this one.** Row `309` needs to be withdrawn as satisfied, or re-dispatched against a subject that is actually open. Nothing in the tree blocks the payout port on `eligibility_snapshot_id`.

### 7. Next

**Slice 6 is unblocked and its stop condition is [ADR-289](../decisions/ADR-289.md) section 4.3's**, with finding `F3`'s landmine still binding: `splitBp` and `clampReason` are written nowhere but the snapshot, and `plan_version_id` is derived from `accounts.plan_version_id` on the same transaction. **Slice 7 is unblocked by [ADR-290](../decisions/ADR-290.md).** **Slice 8 still cannot be scheduled**: `HoldFlag.tosClause` has no value space and `DEP-M7-05` owes the clauses to counsel.

### 8. Files touched

| File | Change |
|---|---|
| [ALLOCATION](../decisions/ALLOCATION.md) | Row `309` only. The outcome appended |
| [STATE](../STATE.md) | This session's section |
| This file | New |
| [`docs/decisions/README.md`](../decisions/README.md), [`docs/sessions/README.md`](README.md) | Regenerated spans, `node scripts/corpus/gates.mjs generate` |

### 9. Verification

Each command run separately, counts read off the runner at the moment of reporting.

| Command | Result |
|---|---|
| `pnpm install --frozen-lockfile` | **Run FIRST, because `node_modules` was absent on arrival.** [ADR-289](../decisions/ADR-289.md)'s landmine. **No count was derived before it and none is claimed** |
| `node scripts/corpus/gates.mjs generate` | ran |
| `node scripts/corpus/gates.mjs check` | **33 of 33** |
| `node packages/tooling/checks/repo-invariants.mjs` | **32 of 32** |
| `pnpm run format:check` | clean |

**`pnpm run verify` was NOT run and `scripts/corpus/falsify.mjs` was NOT run**, both forbidden by the row. **vitest and typecheck were NOT run and no count from either is claimed**: this diff is docs only and cannot move them.
