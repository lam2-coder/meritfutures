## 2026-08-15 - Session 29: the allocation registries made honest, and the four fold ADRs

- **Done.** [ADR-039](../decisions/ADR-039.md), [ADR-040](../decisions/ADR-040.md), [ADR-041](../decisions/ADR-041.md) and [ADR-042](../decisions/ADR-042.md) are written, the eight allocation rows the three approved plans each claimed and none had written exist, and a **third allocation table** for `CI-06` gate letters claims `k`, `l` and `m`. **Money path, recording only, one commit.** No migration, no schema, no module document and no golden scenario was touched. One cross-cutting correction was folded rather than recorded, and it is named below.
  - **THE CORRECTION THE BRIEF COMMISSIONED HAD ALREADY BEEN MADE, AND THE STALENESS HAD MOVED FOUR ROWS DOWN.** The brief named the `036` row as still reading `reserved, unmerged` after the pull request that merged it, citing PR #11. `036` reads `merged to main (PR #13), allocated` and **records its own repair in its own cell**; it was corrected in the fold that landed ADR-037 and ADR-038. **`037` and `038` were the stale rows**, merged by PR #15 (`efcd578`) and still claiming to be reserved on `claude/builder-reviewer-loop-rykvhs`, a branch that no longer exists. **Third and fourth instance.** Found by reading `git log` against the table rather than by reading the table, and **a brief written against this column was wrong about it inside two days**, which is the same evidence one layer out. Both rows corrected; the paragraph beneath the table now records the count and the reason, which is that **the State column is prose, no gate can check it, and [ADR-036](../decisions/ADR-036.md) already said so.**
  - **THE "NOTHING IS RESERVED" SENTENCE IS DELETED RATHER THAN CORRECTED.** The migration table's closing line read "Nothing is reserved today and `0029` is the next free number" while **two approved fold plans each stated their reservation rows were written in the same commit as the other's**. They were not, and that sentence was the only thing in the repository asserting it. [ADR-034](../decisions/ADR-034.md)'s remedy applies to it exactly, either generate the number or delete it and point at the source, so the next free number is now **read from the last row** rather than stated beside it.
  - **THE THIRD TABLE EXISTS BECAUSE THREE FOLDS CLAIMED FROM AN UNREGISTERED NAMESPACE AT ONCE**, and each plan hand-maintained the other two's letters in its own prose (FOLD-01 section 7 says FOLD-02 takes `l`, FOLD-02 section 4.4 says FOLD-01 takes `k`, S-E section 7.1 says both). **ADR-038 collided this week on the registry that already had a table**, which is the counterexample to the argument that a third one is premature. The table also records that **[ADR-036](../decisions/ADR-036.md)'s alternatives list names a rejected `CI-06k` that is not a reservation**: a letter in prose beside a table is exactly the failure the shared `allocated()` parser was hardened against on the other two, where a three-digit numeral in a sentence used to reserve a number silently. **No gate reads this table yet and the ADR says so** rather than implying enforcement, in `CI-06g`'s parameter half's position and stated the same way.
  - **NINE FOUNDER RULINGS THAT EXISTED NOWHERE IN THE CORPUS ARE CARRIED.** ADR-039: **the cost breaker degrades, it does not stop**, because phone verification is mandatory at registration and a tripped breaker therefore means no new customers, so **fail-closed protects money on provisioning and destroys it on registration**; registration continues with verification deferred to the `pre_funded` gate ADR-021 already makes real, **and it trips alarms**, because a breaker that degrades silently is one nobody notices is stuck. ADR-040: **OQ-M5-02 closes at 48 hours** matching the hold, since the investigate-time justification is identical and the post-credit case holds money already recognised as owed; **the unsuppressible alarm list moves from three to four**, because a releaser that can be muted is not a control. ADR-041: **`restricted`, not a new `suspended` value**, since `0002:41` already says who is restricted is restricted across every account they hold and **`G-ELIGIBLE` not naming `identities.status` is the whole finding**; **M15 partial, +3 to 5 days, P8**, on `INV-M15-06`, because a role vanishing at the moment of enforcement publishes the enforcement. ADR-042: **"business day" is the rail's language**, quoted and never computed, so M05's "10 business days" becomes 48 wall-clock hours while the published "2 to 3 business days" settlement claim is **unchanged**; **the 48 hour clocks are wall clock and the obligation is to RELEASE, not to SETTLE**; **OQ-SE-01 accepts all four of F-1 to F-4**; **OQ-SE-02 is the current year plus two with the horizon alarm at six months**; and **OQ-SE-04 is the second transcription, where the blindness is the condition**, because a second reader who can see the first rationalises disagreements rather than surfacing them, and a calendar package is rejected since it encodes someone else's reading of the same publication and is **a second instance of the same error class rather than an independent check**.
- **Next.** The three folds may write against their numbers. **FOLD-01 session 3** (`0029`, DATA_MODEL, DELTA_MANIFEST), **FOLD-02 session 3** (`0030`, `0031`), **S-E session 2** (`0032`). **Each is money path and takes a fresh session, no exceptions**, per [ADR-003](../decisions/ADR-003.md).
- **Blockers.** None. All four FOLD-02 open questions and all four S-E open questions are ruled.
- **Landmines.**
  - **[ADR-028](../decisions/ADR-028.md) BELIEVED ITS SWEEP WAS COMPLETE AND IT WAS SIX SITES SHORT.** [STATE_MACHINES section 2](../architecture/STATE_MACHINES.md), the authoritative payout drawing, still routed `approved --> transferring --> settled` and `frozen --> transferring` on a table whose enum has not held the value since 2026-08-14. ADR-028 named two sites it corrected and **the C-02 ruling named M05 and missed the machine**. **Corrected here, on ADR-028's own remedy**, with `approved --> settled` carrying a ledger fact rather than a guard name because under [ADR-019](../decisions/ADR-019.md) the internal leg is one transaction and **no guard exists to name**, and inventing one in a recording session is the thing the fold plans forbid. **The remaining four sites are named in ADR-040 rather than left for a grep**: `G-NO-IN-FLIGHT` and `G-FREEZE-DURING-FLIGHT` in section 10, [M01](../plans/M01-rules-engine.md)'s `SD-09` and its `hasPayoutInFlight` comment, [EDGE_CASES](../edge-cases/README.md) line 148, and [API_CONTRACT](../architecture/API_CONTRACT.md) line 335's status union. All four are surfaces FOLD-02 session 4 must touch anyway to add `held_pending_review`, and **correcting them in the same edit that widens the predicate is one read rather than two.** The only reason this was found at all is that a founder read the machine rather than the ADR.
  - **TWO `falsify.mjs` SEEDS WERE INVALIDATED BY THE RESERVATIONS AND ONE OF THEM FAILED SILENTLY.** Both `CI-06h` scope cases were pinned to the literal `0029`. Reserving it made `CI-06h/unallocated` write a file the table now claims, so its finding stopped firing and the harness reported `DID NOT FAIL`; and it made `CI-06h/reserved` insert a **second** row for an already-reserved number, so it passed while asserting nothing and **reported success**. **One went silent and one went vacuous, and only the silent one announced itself.** The `SEEDS` entry for `CI-06h` had the same problem: renaming `0028` to `0030` now lands on a legitimate reservation. All three retargeted to `0033`, the maintenance cost of a seed pinned to a literal recorded in the file, and **each watched failing on its own finding again**. A harness that can go vacuous without saying so is the same object as the gates it exists to check.
  - **`payout.freeze_expiring` HAS NO LEAD VALUE ANY MORE AND THE FOLD MUST CHOOSE ONE DELIBERATELY.** It was specified at "2 business days before `freeze_expires_at`" against a 10 business day window. With the window at 48 wall-clock hours, **a two-day lead is degenerate**: the warning fires when the clock starts. ADR-040 records that the event recomputes in hours and that the value is set where the event is written, and **does not convert the old number and move on**, which is how a stale parameter survives a unit change.
  - **THE STATE COLUMN HAS NOW BEEN FOUND STALE FOUR TIMES AND THE CROSS-REF JOB IS STILL NOT WRITTEN.** `035`, `036`, `037`, `038`. Each was found by a human comparing the table to `git log`, and the cross-branch assertion that would close it, a job that can see both refs, remains the standing recommendation from [ADR-034](../decisions/ADR-034.md) and [ADR-036](../decisions/ADR-036.md). **A fourth manual correction is not a control.**
- **Verification run in this session:** eleven of eleven gates pass; `generate` rewrote `adr_count` from 38 to 42 in [INDEX](../INDEX.md) and [STATE](../STATE.md) and reports nothing else drifted; `falsify.mjs` reports all eleven failing dirty on their own findings with five scope cases holding, after the three `CI-06h` retargets.
- **Files touched:** `docs/DECISIONS.md` (the three allocation tables and four ADRs), `docs/architecture/STATE_MACHINES.md` (section 2 only), `scripts/corpus/falsify.mjs` (three `CI-06h` seed retargets), `docs/{INDEX,STATE,SESSION_LOG}.md`. **No `.sql` file, no `packages/` file, no workflow and no module document was touched.**

---
