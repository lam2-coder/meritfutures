---
status: approved
depends_on: [README.md]
last_updated: 2026-08-22
---

## 2026-08-22 - Session 132: P2 planned, and six of its stated contents already existed

- **Objective:** plan P2 against the tree rather than against [P2](../plans/P2-rules-engine.md), which was written 2026-08-16. One plan document and its allocation rows. No code, no migration, no gate, no fixture, nothing signed.
- **Done:** **[P2-rules-engine-build](../plans/P2-rules-engine-build.md)**, `status: draft`. Eight sessions, **five of them concurrent**, each with a fence BY FILE, a money-path flag and a depends-on BY FILE. **Six of the eight things P2's stated contents amount to already exist and no entry recorded that they had.** Claims `ADR-078` to `ADR-081`, migration `0046`, and session numbers **132 to 140**, all in the first commit. **28 of 28 gates, `falsify.mjs` green at 52 scope and 10 loader cases, `format:check` clean, 101 files / 1,410 passed / 45 skipped unmoved**, because this session writes no code.
- **Next:** dispatch wave 1, which is sessions **133, 134, 135, 136 and 137** together. Four items go to the founder rather than to a session: `OQ-F6-01` with a recommendation of 10,000 cents and its arithmetic, the TradingCalendar transcription, `CI-04`'s Neon branch, and the `E2` reads on 134, 135, 138 and 140.
- **Blockers:** none for this session.
- **Landmines:**

  - **THE BRIEF SAID THE SIMULATOR WAS FILE MODE ONLY AND IT IS NOT.** [`stream.ts`](../../packages/rithmic/src/simulator/stream.ts) exists, [`index.ts`](../../packages/rithmic/src/index.ts) exports `streamRun`, `foldStream`, `sampleTicks`, `StreamOptions` and `LiveAccountTick`, and [`stream.test.ts`](../../packages/rithmic/test/stream.test.ts) asserts the streaming path folds back to the file summary exactly, in integer cents. It landed in merge `0b526c8`. **A plan written on the brief would have scheduled `ADR-020`'s tier 2 simulator twice.**

  - **SESSION 129's SECOND MONEY-PATH FINDING IS FALSE AGAINST THE TREE, AND THAT IS THIS SESSION'S MOST USEFUL OUTPUT.** It reads *"`R-15`'s permanent lock makes a rising funded life unstorable after the lock day"* and cites [`0015:208`](../../packages/db/migrations/0015_rule_states.sql)'s `rule_states_high_water_bounds_balance`. **That constraint was superseded on 2026-08-17 by [`0037`](../../packages/db/migrations/0037_supersede_rule_states_high_water_bounds_balance.sql) under [ADR-053](../decisions/ADR-053.md), which is `accepted`**, and the replacement `rule_states_high_water_bounds_balance_unlocked` is scoped so the bound holds only while the floor is unlocked. `0037`'s header works the arithmetic on the exact case. **The defect is not that the finding was wrong; it is that a session cited a merged constraint by file and line without asking whether a later migration had superseded it**, which is the one question `E2`'s never-edit-only-supersede rule makes mandatory. It cost this plan a whole application: `ADR-079` is one ruling with one application, not two.

  - **`ADR-074` SECTION 8 LISTS FIVE RUNNER EDITS AND THERE ARE SIX.** Run `CI-06/identifier-series` today and its note still says *"ADR-074 section 3 states 118 declared and 1,086 members and section 5 states 44 pending"*. Those are the **pre-amendment** figures that section 3.1 retraced and section 8 narrowed. A session that makes the five edits and leaves the note reproduces the error the amendment was signed to end.

  - **CI-03's POLARITY FLIP FINISHED AND `STATE` DOES NOT SAY SO.** The coverage block re-derives per run: **43 direct, 0 inverted, every group A to H at 100 percent, `declaration.holds` true so the direction is ENFORCED**. 43 of the suite's 45 skips are the retired pre-`ADR-048` assertion, which cannot run again. Anyone reading the skip count as a backlog reads it backwards.

  - **THE ENGINE'S RULE SET IS COMPLETE AT 46 OF 50 AND THE FOUR ARE NOT A QUEUE.** [`rules.ts`](../../packages/rules-engine/src/rules.ts) argues each: `R-01` needs a fill instant `DailyMark` does not carry, `R-05` needs session bounds `CalendarDay` does not carry, `R-11` is the caller's live-mark predicate and `R-20` is the platform setpoint. **Neither `R-01` nor `R-05` waits on the calendar transcription**, which is why P2 can finish without it.

  - **`CI-07`'s CONDITION IS NOT MET BY P2, MEASURED RATHER THAN ASSUMED.** No package in this repository declares a `build` script and there is no bundler in the tree. The eight sessions add two engine source files, one migration and four document repairs, and none introduces a build. The brief asked *"IF P2 INTRODUCES ONE"*; it does not.

- **Files touched:** [P2-rules-engine-build](../plans/P2-rules-engine-build.md) (new), [P2-rules-engine](../plans/P2-rules-engine.md) (one pointer line, the document is otherwise left as written), [ALLOCATION](../decisions/ALLOCATION.md), [README.md](README.md), [INDEX](../INDEX.md), [STATE](../STATE.md), and this file.

### What the measurement found, in one table

| P2's stated content | State at `edb2e13` |
| --- | --- |
| The engine | **Two files short.** 46 of 50 rules, all six exported functions, the calendar four |
| STRATEGY 5.1, unit and property | **Done for the seven P2 owns.** `PT-06`'s arrival-order half skipped by derivation on `replay` |
| STRATEGY 5.2, golden replay files | **Done.** 43 of 43 written, all direct, all green |
| STRATEGY 5.3, simulation harness | **Done**, and `CI-09` nightly consumes it |
| The simulator, file AND streaming | **Both done** |
| The nightly batch | **Done** |
| The replay self-audit | **Done**, asserted at 250-day scale by session 129 |
| `RE-D-01`, `RE-D-02`, `RE-D-03` | **Done and falsified**, though `STATE` records them as *"Nothing"* three times |

### The one defect that is real, and why no gate could have seen it

[`0015:193`](../../packages/db/migrations/0015_rule_states.sql)'s `rule_states_consistency_period_started` requires `consistency_period_start_day <= trading_day`. [`progression.ts:339`](../../packages/rules-engine/src/day/progression.ts) sets `consistencyPeriodStartDay` to the trading day **after** the pass day, which is `R-47` and `AS-12` read correctly: [M01:987](../plans/M01-rules-engine.md) says the period is *"trading days strictly after the anchor"* and *"the eval pass day is excluded"*. **Every eval pass in the book would be rejected by Postgres on insert.**

**Nothing in P2's done-condition could catch it.** CI-03 folds the engine and never touches a database; the migration suite installs the schema and never folds the engine. The two halves are each green about their own half. It was found by folding a funded life and looking at the row, which is what session 129 did.

`ADR-079` and migration `0046` are reserved for it, and [`0037`](../../packages/db/migrations/0037_supersede_rule_states_high_water_bounds_balance.sql) is both the model and the precedent, down to `ADR-053`'s refusal to reuse the superseded constraint's name.

### What the plan hands the founder rather than a session

| # | Item | Recommendation |
| --- | --- | --- |
| 1 | **`OQ-F6-01`**, the dual-control threshold in integer cents | **10,000 cents.** [M20](../plans/M20-wallet.md) `WF-M20-02` sets the external withdrawal minimum at $100, so at 10,000c every adjustment large enough to be withdrawn on its own needs the second key. [`0038:191`](../../packages/db/migrations/0038_account_adjustments.sql) makes the threshold a `NOT NULL` column per row, so until a number exists the `CHECK` is **inert** and the dual-control half of [ADR-067](../decisions/ADR-067.md) is specification rather than enforcement. **`OQ-F6-02`'s sub-threshold aggregation gap is not closed by any value here** |
| 2 | **TradingCalendar's data** | Blocked on egress, not on engineering: the session that wrote [`cme-2026-2028.source.json`](../../packages/db/src/seed/calendars/cme-2026-2028.source.json) got `403` on `CONNECT www.cmegroup.com:443`, and the file is `awaiting-transcription`. **It blocks none of the eight sessions** |
| 3 | **`CI-04`'s Neon branch** | No session can grant it. Open since 2026-08-20, and `CI-09`'s replay leg waits on it too |
| 4 | **The `E2` reads** on 134, 135, 138 and 140 | Money path. 135 carries a migration and must not merge before the read |

### `OI-27`, `OI-28` and `OI-29`, dispositioned

**`OI-29` is the one with a P2 surface and the other two have none.** Its remedy needs a canonical digest over the published rules; **nothing in TypeScript computes `rules_digest` today**, and that serializer is `hash.ts`, which session 138 lands. **Its enforcement, a trigger or an application publish path, is P3's** and does not move here. `OI-27` is registry hygiene in [ADR-074](../decisions/ADR-074.md)'s family and `OI-28` widens [ADR-042](../decisions/ADR-042.md)'s closed `UNIT_TOKENS` for an M21 column; neither touches the engine, and both belong to the next session holding [`DELTA_MANIFEST`](../../packages/db/DELTA_MANIFEST.md).

### What was NOT done, named rather than implied

No code, no migration, no gate, no fixture. Nothing was signed. [ADR-072](../decisions/ADR-072.md), [ADR-073](../decisions/ADR-073.md), [ADR-074](../decisions/ADR-074.md), [ADR-076](../decisions/ADR-076.md) and [ADR-077](../decisions/ADR-077.md) were read and none was reopened. [M02](../plans/M02-rithmic-bridge.md) was not touched, and the plan states that none of the eight sessions waits on the vendor call. **The `0015` citation audit section 4.2 makes necessary was not performed**: it is named for the next session holding `DELTA_MANIFEST` rather than absorbed into a planning session's fence.
