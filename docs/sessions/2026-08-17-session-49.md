---
status: approved
depends_on: [README.md]
last_updated: 2026-08-17
---

## 2026-08-17 - Session 49: golden fixtures batch 5, thirty of 284, and four held-back reasons checked against their sources (non-money)

- **Done:** **One fixture, [GS-064](../../packages/rules-engine/fixtures/GS-064-breach-and-payout-eligibility-on-the-same-day.yaml), and four corrections to the held-back table.** [Session 48](2026-08-17-session-48.md) established mechanically that every M1-owned scenario was either a fixture or a named held-back row, and it was right, so this batch had nothing left to transcribe. It **checked the reasons instead**: one was wrong and three were incomplete. GS-064's row said "not inside five sessions", which is arithmetic on `win_days.required_count = 5`; that is [Appendix A.1](../plans/M01-rules-engine.md)'s figure and [Appendix A.2](../plans/M01-rules-engine.md) carries **3**, so the fixture fits in four sessions on [`MERIT-RAPID-50K.json`](../../packages/rules-engine/fixtures/plans/MERIT-RAPID-50K.json), which has been in `fixtures/plans` since batch 2. `pnpm run verify` green: **15 of 15 gates**, 30 fixtures loading with 0 load failures. **`packages/rules-engine/src/` and `test/` are untouched**, per the session fence.
- **Next:** **The fixture-wiring session is unchanged as the bottleneck and this batch adds one item to its list.** It owns the `bigint` conversion in `diffEndState`, the `L-13` citation rule, then [ADR-048](../decisions/ADR-048.md)'s polarity derivation. The new item is **`sessions[].kind`**, which is carried and ignored with no `L-nn` rule refusing it. **Nothing else in the M1 partition is writable**: 30 written, 43 held back, and every one of the 43 now names a blocker that was re-derived this session rather than inherited.
- **Blockers:** **`INV-07` versus the funded reset is a founder ruling and it is unchanged**, the successor to the one ADR-050 closed. The **floor-lock jump disagreement** (M01 section 3.4 against 3.6) is unchanged. **Neither holds a field on GS-064**, which is the first fixture since GS-055 that can say that: it never locks and never passes an eval.
- **Landmines:**

  - **THE WRONG HELD-BACK REASON IS THIS CORPUS'S OWN RECURRING SHAPE ARRIVING A FOURTH TIME.** "Eligibility is six trading days after the win-day gate is satisfied" is a **correct observation about Core EOD** carried one unchecked step to the directory. [Session 47](2026-08-16-session-47.md) named the pattern, session 48's engine log found it in the DO ordering table (`R-02`, `R-06` and `R-10` filed as blocked on a calendar transcription that three of them never needed), session 47's fixture log found it in the R-30 denominator row, and this is the fourth. **The plan record that dissolved it was written by the session that recorded the reason's neighbours**: batch 2 transcribed Merit Rapid precisely because Core EOD could not express the eval-consistency block, and nobody re-read the rest of the table against the record it had just added. **A held-back row records a reason, and a reason nobody re-derives becomes a fact** is the lesson batch 3 already wrote into this repository. It is cheaper to re-derive the table than to trust it.

  - **THE CALENDAR ROW'S REASON WAS TRUE AND WAS NOT THE BLOCKER, WHICH IS WORSE THAN BEING WRONG.** GS-001, GS-003, GS-004 and GS-030 to GS-032 were filed under "`cme-2026.json` declares five full sessions and no fill stream, and inventing either would be transcription from recollection". Every clause of that is true. **`EngineInput` is `{ planConfigVersion, accountState, dayMarks }` and carries no calendar at all**, so `is_half_day` and `halted` reach nothing whatever the calendar declares, and `DailyMark` carries `fillCount` and no instant. **A founder who transcribed the CME publication to unblock those six would have unblocked none of them.** A true reason standing in front of a binding one is a reason that will be acted on and will not help, and it survives review precisely because it is true.

  - **`sessions[].kind` IS THE ONE PLACE THIS FORMAT CAN SILENTLY DROP A STATED CONDITION.** `loadCalendar` reads `sessions[].trading_day`, builds a `Set` of strings for `L-08`, and discards the rest; every session in `cme-2026.json` states `"kind": "full"` and nothing reads it. The equivalent on a `days:` row is `L-10`'s refusal and on an `account:` block is `L-09`'s, and both exist because "the fixture states a condition, the engine never sees it, and the scenario passes while pinning something else" is the worst outcome available on a money path. **It is not on `AWAITING_M01_INPUT` either**, because that list is about fixture fields, so `L-14` cannot notice it rotting. Recorded rather than fixed: every `L-nn` rule is the fixture-wiring session's.

  - **A LONGER SYNTHETIC CALENDAR WAS WORKED OUT IN FULL AND THEN NOT ADDED, AND THE ORDER MATTERS.** It would have been legitimate on [`calendar.ts`](../../packages/rules-engine/src/calendar.ts)'s own `GAPPED_SLICE` precedent, where session 47 ruled that what TR-01 forbids is writing down which days the exchange trades and that a slice supplied to make two answers disagree is not that. It was dropped **because it unblocks nothing**: GS-064 fits in four sessions once the plan is read correctly, GS-070's blocker is not the calendar, and every other row is blocked on a settlement, a clamp, a nested gate, a second account, or an input `EngineInput` does not have. **The failure mode avoided is a convention introduced for a fixture that did not need it**, which the next session inherits and cannot audit. Finding the cheaper repair is what made the expensive one unnecessary, and it was found by reading the plan record rather than by reasoning about the calendar.

  - **`engine_eligible` IS PINNED FOR THE FIRST TIME AND ONLY IN THE DIRECTION M01 STATES WITHOUT AMBIGUITY.** GS-055's sibling declined to pin `true` because M01 lists `R-38` as a funded gate in group F while declaring its input on `ExternalGates`, and `R-40`'s context list does not name it, so **the conjunction's membership is unruled**. That does not reach `false`: `R-25`'s row says "no eligibility" in its own words and section 3.6's breach block writes `engineEligible: false` outright. **A contested conjunction still has one uncontested value.** The same asymmetry is why GS-080's second half ("asserts that `engineEligible` is unaffected") has a **second** blocker beyond the nested-object one, which its row now records.

  - **`engine_eligible: false` ALONE WOULD HAVE BEEN WORTH NOTHING, AND THE SIX CARRIED FIELDS ARE THE FIXTURE.** An engine that never computed eligibility at all also reports `false`. What separates them is that `win_days_count`, `traded_days_count`, `withdrawable_cents`, `high_water_balance_cents` and both consistency accumulators are pinned at **day three's** values while `balance_cents` is pinned at **day four's**, because the breach block writes the trading day, the phase, the breach fields, the floor at open, the closing balance and `engineEligible`, and nothing else. Day four is deliberately a **traded win day closing at a new high**, so an engine that ran DO-6 reports 4 and 4, one that ran DO-9 reports a withdrawable of 40,000, and one that ran DO-7 reports a high-water balance of 5,140,000 and a floor of 4,890,000. **Four eligible-looking wrong engines, each failing on a different field.**

  - **GS-070's REAL BLOCKER IS THAT ITS ASSERTION CANNOT BE ISOLATED, NOT THAT THE FORMAT LACKS A SHAPE.** The row said the sibling has no shape for `assertions`, which is true and is downstream of the harder fact: **`funded_start_not_size` cannot be reached independently of `opening_mismatch`.** DO-3 tests `opening == prior.balance + adjustment` first, `L-11` fixes `adjustment_cents` at 0, and the prior balance after `R-31`'s reset **is** `size_cents`, so any opening that trips AS-14's check trips `INV-18` in the same pass. A fixture could state that the day was refused; it could not state which of the two refused it, and AS-14 is a claim about the second one.

  - **284 IS THE WRONG DENOMINATOR FOR THE QUESTION A READER OF THAT DIRECTORY IS ASKING, AND IT WAS THE ONLY ONE THERE.** The registry defines 284 and this directory can only ever hold **M1's partition of it, which is 73**; the other 211 are other modules' fixtures in other suites, and writing one here would double-count against that module's coverage. "29 of 284" reads as 255 fixtures still owed here. **Both counts are now carried**, because dropping the registry-wide one would understate how far the suite as a whole has to go, and P2's done-condition already quotes the owned set of 73 rather than the registry total.

- **Files touched:** [`GS-064-breach-and-payout-eligibility-on-the-same-day.yaml`](../../packages/rules-engine/fixtures/GS-064-breach-and-payout-eligibility-on-the-same-day.yaml) (new) and its sibling (new), [`fixtures/README.md`](../../packages/rules-engine/fixtures/README.md), [STATE](../STATE.md), [sessions/README](README.md), this file.

### The count, reported rather than implied

**Thirty of 284, and thirty of M1's 73.** One fixture is a small batch and it is the number the format allows, which is worth stating plainly rather than dressing up. The other product of the session is **43 held-back rows whose blockers were re-derived rather than inherited**, four of which changed.

| Row | Was | Is |
|---|---|---|
| GS-064 | Not inside five sessions | **Written.** The reason was Core EOD's win-day count read as the directory's |
| GS-001, GS-003, GS-004, GS-030 to GS-032 | The calendar declares five full sessions | `EngineInput` carries **no calendar**, so a transcribed CME year moves none of them |
| GS-070 | Five sessions, and no `assertions` shape | `funded_start_not_size` is unreachable independently of `opening_mismatch` |
| GS-080 | The nested `engine_gates` object | **And** no fixture may pin `engine_eligible: true` while `R-38`'s membership is unruled |

### What is NOT here

**No engine code and no `EC-nnn`.** P2 section 2 alternates fixture and engine sessions: "a session doing both has derived its expectations from its own output." `packages/rules-engine/src/` and `test/` were not opened, and the concurrent session owns both.

**No ADR.** This session found no contradiction in a frozen document. What it found was four claims in **this repository's own documentation** that the plan documents refute, and those were corrected in place because a directory README is not a frozen document.

**No new calendar**, for the reason recorded above, and the reasoning is kept rather than discarded so that the next session that reaches for one starts from where this one stopped.

**CI-03's polarity is untouched**, for the sixth session running. `engineIsIdentityStub()` still governs, every fixture including GS-064 is still asserted to fail, and ADR-048's stated prerequisite is still unwritten.
## 2026-08-17 - Session 49: the nightly batch and `state_hash`, with the comparison deliberately not wired (non-money)

- **Done:** **The nightly batch exists and it folds through the engine's published interface only.** [`apps/worker/src/batch/`](../../apps/worker/src/batch/) carries three files: [`state-hash.ts`](../../apps/worker/src/batch/state-hash.ts) (SD-08, SHA-256 over ADR-026 C-07's nineteen columns in the declared order), [`ports.ts`](../../apps/worker/src/batch/ports.ts) (the I/O boundary, declared before its client exists), and [`nightly.ts`](../../apps/worker/src/batch/nightly.ts) (read ingest, fold `advanceDay` over the trading day, write `rule_states`). **`packages/rules-engine/src`, `test/` and `fixtures/` are untouched**, per the session fence, and the module resolver enforces it rather than a convention: `@merit/rules-engine`'s `exports` map publishes `.` and nothing else, so no path into the engine's internals exists from `apps/worker` at all. **`pnpm run verify` green: 6 of 6 invariants, 571 tests over 45 files, 15 gates clean and dirty, 16 scope cases.** Seventy-nine of those tests are this session's.
- **Next:** **The replay self-audit's COMPARISON, and it is deliberately not this session's.** `state_hash` is computed and written on every row; nothing yet re-derives a stored row and compares the two. It waits until more rules exist, because an audit run against six of eight rule groups would report agreement about the two it can fold and silence about the rest, which reads exactly like an audit that found nothing (FM-17). The other two next items are the adapter (blocked on a database client that does not exist) and the event sink (blocked on `0017`'s vocabulary mapping).
- **Blockers:** **The batch cannot run against anything.** There is no Drizzle client, no `pg` in any manifest, and [`packages/db`](../../packages/db/src/index.ts)'s `ScopedDb` carries one field. That is why the ports are declared and not implemented, and it is the same restraint `packages/db`'s own header states: "neither the client nor the accessor exists yet, and the scaffold does not invent them." The calendar transcription still blocks a batch run over real days, unchanged and a founder item.
- **Landmines:**

  - **THE HASH'S FRAMING IS A DECISION THE SOURCES DO NOT MAKE, AND IT FIXES EVERY STORED HASH FOREVER.** ADR-026 C-07 and M01 Appendix B.2 give three rendering rules and **no separator**: declared order, `bigint` base-10, `null` as an explicit sentinel, no whitespace. **Plain concatenation is not injective and the collision is one column pair away**: `traded_days_count = 1, win_days_count = 23` and `12, 3` both render `123`, so two genuinely different states carry one hash and INV-04 asserts nothing on exactly the counters R-33 and R-34 gate on. Each field is now length-prefixed `<utf8 byte length>:<utf8 bytes>`, which is injective **with no argument about the value domains**, because a separator character is injective only while no field can contain it and that is a claim a future column can falsify quietly. **It is settled now because `rule_states` has zero rows** (`0035`'s header says so in its own words), and this is the only moment at which choosing costs nothing. **Changing it after the first row lands invalidates every hash in the table and no migration repairs it, only a full audited rewrite under B.4.** Founder read wanted on this line specifically.

  - **`engine_gates` CANNOT BE `JSON.stringify` AND THE REASON IS NOT STYLE.** `JSON.stringify` **throws on a bigint** and four of the twenty-five leaves are `Cents`, so the obvious rendering does not merely drift, it does not run. Underneath that, M01 section 1.4 bans "iteration over an object's keys where the result affects output" and names "canonical serialization in `hash.ts`" as the replacement, so hashing today's insertion order would diverge every row in the book the day someone reorders two fields in `EngineGateResults` with no rule changed. The leaves are an explicit ordered list. **And the hash is over the ENGINE'S value, never over Postgres's `jsonb`**, which stores keys sorted by length then bytewise: a hash recomputed from a round-trip is a **different serializer** and would disagree with every hash the batch wrote. `RuleStateRow` says so on the column.

  - **`account_id` IS COLUMN 1 AND IT IS NOT ON `RuleState`.** `DayInput` carries no account id by design ("the fold is per account by construction and the caller that supplied the marks is the one that knows whose they are"), so the batch supplies it and nothing else can. It is **asserted** as a canonical lowercase UUID rather than normalized: a caller that started sending upper case would otherwise keep working while every hash it produced changed, and that divergence is invisible in a field diff because the field is not on the state.

  - **THE WATERMARK IS READ BEFORE THE SLICE, AND REVERSING TWO LINES IS A SILENT LIE.** If a correction commits between the two reads, the slice carries it and the stamp does not, so the row claims an **older** calendar than the one it read: replay finds it out of scope and B.4 step 4 rewrites it, which is the protocol working. Reversed, the row claims a calendar it never saw and, in [`0035`](../../packages/db/migrations/0035_rule_states_calendar_revision.sql)'s own words, "replay would then believe a stale row was current". **This is the code half of the trigger `0035` refused to write**, and it is asserted on call order rather than left to a comment.

  - **`context_gates` IS `NOT NULL` AND THE DAY FOLD DOES NOT PRODUCE IT.** SD-06 splits the gates precisely so the context half never enters the replayed state, so `advanceDay` returns none, and a `rule_states` row cannot be written without them. The batch asks **`evaluatePayout`** rather than re-deriving R-40 in a worker, which would be a second implementation of a money gate outside the engine. R-06 is satisfied rather than strained: the state passed is the day the batch just closed. **`requestedCents: null` is ADR-009's "pay the maximum I am eligible for"**, which is the right argument for a question that is not a request.

  - **`0015` NAMES FOUR CONTEXT GATES AND THE ENGINE PUBLISHES FIVE.** The column comment says "freeze, recon_blocked, KYC, in-flight"; `ContextGateResults` carries those plus **`accountActive`**, which R-40 states and API_CONTRACT's eligibility shape renders, and R-38's in-flight verdict is reported on its own field because API_CONTRACT has no slot for it. The engine's shape is what gets stored, so the column and the endpoint speak one vocabulary. **`0015` is merged and only an ADR moves it**, so this is flagged rather than reconciled.

  - **M01 SECTION 1.3 PUTS `hash.ts` IN THE ENGINE AND THE LINT RULE WOULD REFUSE IT THERE TODAY.** `merit/engine-purity` reports **every non-relative import** inside `packages/rules-engine/src/**`, so `import { createHash } from 'node:crypto'` is an error, while M01 section 1.4's own banned-constructs table permits "`crypto` **beyond a pure hash**". **The prose allows the import the rule refuses.** Building in `apps/worker` sidesteps it and does not resolve it: whoever writes `hash.ts` must take a scoped lint exception or hand-roll SHA-256, and neither is a decision to make in passing. Carried as `OI-15`. The module is written so that resolving it is a **file move**: it imports published types and `node:crypto` and nothing else.

  - **A TYPECHECK FAILURE LANDED BECAUSE A PIPE ATE ITS EXIT STATUS.** `pnpm run typecheck 2>&1 | tail -8 && git commit ...` continues past a red compile, because a pipeline exits with the status of its **last** command. The branded `BasisPoints` literals in the batch test were pushed broken and fixed one commit later. **This is not a new lesson and that is the point**: `falsify-ci.mjs` already refuses to trust a run it cannot bound. A check whose failure cannot reach the shell is a check that passed.

  - **EVENTS ARE RETURNED AND NOT PERSISTED, AND THE REPORT SAYS SO RATHER THAN THE MEMORY.** `advanceDay` emits M01 section 5's facts; writing them needs `0017`'s tables and EVENTS.md's vocabulary mapping. They are carried on each outcome so the gap is visible in the type.

  - **THE ADAPTER OWES A PER-ACCOUNT ADVISORY LOCK AND NO LOCK API WAS INVENTED.** FM-10: "settlement webhook and nightly batch race on the same account", answered by "`applySettlement` is the only writer of anchors, is idempotent on `payout_request_id`, and the batch takes a per-account advisory lock". The lock belongs with the connection and the connection does not exist, so it is named in `runNightlyBatch`'s contract instead of stubbed.

- **Files touched:** [`apps/worker/src/batch/state-hash.ts`](../../apps/worker/src/batch/state-hash.ts) (new), [`apps/worker/src/batch/ports.ts`](../../apps/worker/src/batch/ports.ts) (new), [`apps/worker/src/batch/nightly.ts`](../../apps/worker/src/batch/nightly.ts) (new), [`apps/worker/test/state-hash.test.ts`](../../apps/worker/test/state-hash.test.ts) (new), [`apps/worker/test/nightly-batch.test.ts`](../../apps/worker/test/nightly-batch.test.ts) (new), [`apps/worker/src/index.ts`](../../apps/worker/src/index.ts), `apps/worker/package.json`, `pnpm-lock.yaml`, [STATE](../STATE.md), this file.

### What `0033` and `0035` bought, and why the computation lands before the comparison

**`INV-04` is "replaying every mark from day one reproduces stored state byte-identically", and this session builds the left-hand side of that sentence.** The right-hand side is the audit, and it is not wired.

Saying that plainly is the point. A green suite over a batch that computes `state_hash` looks exactly like a green suite over a batch whose hashes are being **checked**, and the difference is the whole of `INV-04`. The [PR](../../apps/worker/src/batch/nightly.ts) says it, `nightly.ts`'s header says it, and this entry says it, because the failure mode is not that somebody lies about it: it is that six weeks from now nobody remembers which half exists.

**What makes the computation worth landing early is what the two migrations already installed.** [`0033`](../../packages/db/migrations/0033_trading_calendar_revision_required.sql) made the calendar's prior image **mandatory**, so every correction leaves an unforgeable record of what the calendar said before it moved; it exists because ADR-042 F-2 "landed as a table nobody is required to use", and its own header names the consequence exactly: replay was "unable to tell a calendar correction from an engine regression, on the nightly self-audit, at the moment somebody is paging". [`0035`](../../packages/db/migrations/0035_rule_states_calendar_revision.sql) then **joined that record to a state row**, because `0033` left replay holding the evidence with no way to scope by it.

Together they are what makes a calendar correction **distinguishable** from an engine regression. Before them, a holiday correction fell into Appendix B.3's "anything else: page", and since a calendar correction changes the day sequence for **every account at once**, the first one diverged the whole book: 5,000 pages on one morning, which is how B.5's warning comes true. **The alarm does not fail by being wrong. It fails by being right five thousand times.**

**The half those two migrations could not install is the one this session wrote.** `0035`'s header refuses a trigger asserting the stamp is current, and refusing is the ruling rather than an omission: a correction committing between the fold and the write leaves a row that genuinely read the older calendar, and stamping it with the newer watermark "records a calendar it never saw". No per-row constraint can tell "not yet written" from "pristine calendar" without fabricating, which is `OI-14`. So the honest stamp is the one the fold read, and **only the batch is in a position to write it** because only the batch knows when the fold ran. That is now two lines in `runNightlyBatch`, in the order that makes them true.
## 2026-08-17 - Session 49: the local demo, and the four things the first watched run surfaced (non-money)

- **Done:** **[`scripts/demo/`](../../scripts/demo/README.md) runs the seeded simulator through the engine and prints what happens**, in memory, with no database, no file written and **no dependency added** (VG-12): the entry is a `.mjs` shim registering a twenty-line resolve hook so Node's own type stripping can load the workspace's TypeScript sources. It seeds a population against **Core EOD at 50K** transcribed from [Appendix A.1](../plans/M01-rules-engine.md), folds `advanceDay` over each account, and prints balance, floor, high-water balance, win days, consistency, every gate with its have and its need, and eligibility. **`--seed` reproduces byte for byte**, asserted on the RENDERED STRING in `scripts/demo/test/determinism.test.ts`, which runs in the `unit` project. **Both sides are in the default run and are themselves asserted**: one account reaches eligibility ($1,022.50 at ordinal 1 of 5), one breaches with the platform's liquidation record as evidence, one **breaches a trailed floor the platform setpoint is nowhere near**. `pnpm run verify` green: **502 tests, 15 of 15 gates clean and dirty, 16 scope cases.** **`packages/rules-engine/src/` and `test/` are untouched**, per the session fence.
- **Next:** Unchanged and not this session's: the fixture-wiring session still owns the `bigint` comparison in `diffEndState`, the `L-13` citation rule, and [ADR-048](../decisions/ADR-048.md)'s polarity derivation. **Three of the four findings below want a ruling before they want code.**
- **Blockers:** **`DEP-M2-03`'s setpoint source is now a question with a number attached** (finding 1). `R-32`'s anchor and its authoritative column still block the `DayInput` amendment. The calendar transcription still blocks group A, and the demo works around it with a weekday sequence that says in three places it is not the CME calendar.
- **STATE.md is deliberately not edited.** Nothing about the post-FREEZE position moved: no rule landed, no fixture landed, no migration was written, and the build sequence is where session 48 left it. A STATE edit that recorded a demo would be a hand-maintained line that says nothing, which is the drift [ADR-034](../decisions/ADR-034.md) exists to end.
- **Landmines:**

  - **MERIT'S FLOOR TRAILS AND THE PLATFORM'S SETPOINT DOES NOT, AND THE DEMO PRINTS THE GAP IN CENTS.** `DEMOSWNG250002` locked its floor at 5,046,250c on 2026-11-16 and breached on 2026-11-19 at a low of 5,009,500c. **The setpoint was 4,750,000c, pushed once at provisioning, so the gap was 296,250c and the platform had no reason to act.** The breach therefore carries **no auto-liquidation record**, and that account closed the day **up 74,500c on its size** and was still breached. Every rule involved is working: `R-13` trails the floor, [ADR-014](../decisions/ADR-014.md) makes the lock permanent, the low is the breach comparison input (`0014`). But [DATA_CAPABILITIES](../../research/DATA_CAPABILITIES.md) section 1 names the EOD report's liquidation record as Merit's breach evidence, and **on this shape of breach there is nothing to cite**. How often M2 re-pushes the setpoint is an M02 question; this run makes the cost of "once, at provisioning" visible.

  - **A DISABLED CONSISTENCY RULE REPORTS AS SATISFIED RATHER THAN AS DISABLED.** `consistencyOk` returns `{ ok: true, skipped: false, bestDayShareBp: null }` when `cfg.enabled` is false, and its own header states that `skipped` **is not** `!enabled`. So Core EOD's `phase.passed` payload reads `consistency={satisfied=true skipped=false}` on a plan whose **eval consistency is disabled**. That is exactly the vocabulary `CV-19` fixed and which the same file quotes: a gate never evaluated must be "visibly disabled in the eligibility breakdown ... so no trader or support agent ever sees a gate that reads as satisfied when it was never evaluated". **The funded gate is enabled on all three v1 plans so nothing in the lineup renders wrong today**; the eval half of `R-28` is where it would surface, in an event payload M16 and M04 both read. **Not fixed here: the engine is another session's.**

  - **`EngineEvent` CANNOT BE NARROWED BY A CONSUMER.** `DayOutput.events` is `readonly EngineEvent[]` and `EngineEvent` is `{ type: string; tradingDay }`. The nine concrete events extend it and **no discriminated union is exported**, so a consumer wanting `PhasePassedEvent.resetFloorCents` must cast. The demo walks each event's own fields instead, which needs no cast, but a portal rendering one event type per screen will reach for one.

  - **THE `R-31` RESET IS A SEAM THE SIMULATOR CANNOT SEE, AND A NAIVE FOLD SHOWS NOTHING ELSE.** `advanceEvalProgression` sets `balanceCents: plan.sizeCents` on the pass day; the simulator carries its own balance and has no notion of a phase. So a single `simulate()` call folded straight through **refuses every funded account with `opening_mismatch` on the day after it passes**. The demo restarts the simulator at the day after the pass, which is what `INV-M2-07` says the platform does (`buildPopulation` already opens every account at exactly `size_cents`), rather than patching a balance the engine returned. **The restart changes no draw**: every `rng.ts` key is `(seed, accountRef, tradingDay, ...)` and none is the session's index.

  - **A BREACH DAY IS NEVER A TRADED DAY.** `DEMORISK500002` traded on 2026-11-02, was liquidated, breached, and its state carries `tradedDaysCount: 0`, because `DO-4` returns before `DO-6`. That is `R-25`'s ordering law working as written and it is worth knowing before someone reconciles a traded-day count against a fill count and finds them off by one on precisely the accounts that closed.

  - **THE DEMO PROVES NOTHING ABOUT THE CSV OR THE NORMALIZER, AND ITS README SAYS SO FIRST.** It calls `simulate()` and converts the day in memory, skipping the file. `INV-M2-11` is that simulator output and vendor output go through the same parser, and **that is exactly the seam this demo steps over**. Three other boundaries are stated in the same place: no settlement ever happens so `R-37` reports `skipped` on every row of every run, the context gates are constants so only the engine half of eligibility moves, and `R-04` is unexercised because the calendar has no halted session to offer.

  - **`scripts/demo` JOINED `vitest.config.ts`'s `SOURCES` RATHER THAN BECOMING A FIFTH PROJECT.** The projects map to CI stages and there is no demo stage; what the directory holds is a unit suite by every ordinary meaning of the word, so it belongs in CI-02 beside the others. `RI-03` reads the config as text and checks the four stage names are present, which they still are. **A test matched by no project does not run**, and a determinism claim nothing runs is a claim rather than a control. The root `typecheck` script names `scripts/demo` explicitly for the same reason: `pnpm -r` walks the workspace and this directory is deliberately not in it.

- **Files touched:** [`scripts/demo/README.md`](../../scripts/demo/README.md), `run.mjs`, `ts-resolve.mjs`, `config.ts`, `bridge.ts`, `fold.ts`, `render.ts`, `main.ts`, `test/determinism.test.ts`, `tsconfig.json` (all new), [vitest.config.ts](../../vitest.config.ts), [package.json](../../package.json), [sessions/README](README.md), this file.
## 2026-08-17 - Session 49: `P2-1`, the config contract, and R-17 was never outside this package (money path)

- **Done:** **`resolvePlan` and `validatePlan` exist, so all six of [M01](../plans/M01-rules-engine.md) section 1.3's exported functions now do.** `CV-01` to `CV-19` and `PW-01` to `PW-04` are transcribed from section 2.4 into [`plan/validate.ts`](../../packages/rules-engine/src/plan/validate.ts); [`plan/resolve.ts`](../../packages/rules-engine/src/plan/resolve.ts) materializes one plan at one size and applies no percentage anywhere. **`RE-C-01` to `RE-C-19` land as a suite**, each asserting both sides of its operator and each conditional rule also asserting its precondition FALSE. **`R-17` is DECLARED: forty-five of fifty.** **Eight new seeded mutants**, all watched failing on their own finding. `pnpm run verify` green: **6 of 6 invariants, 566 tests with 29 skipped, 15 of 15 gates clean and dirty, 16 scope cases**, and a full `falsify-ci.mjs` run reports **58 of 61** on a clean tree, the three that ERROR being `gitleaks`, `semgrep` and `syft`/`grype` absent from this container's PATH.
- **Next:** The fixture-wiring session is still the largest unblocking left in P2 and is unchanged: the resolvable-citation `L-nn` rule, the loader's `bigint` comparison, then [ADR-048](../decisions/ADR-048.md)'s per-fixture polarity. **`IMPLEMENTED_RULES` is 45 when that session reads it.** After it, `replay.ts` and `hash.ts` (`SD-08`), which are what would declare `R-11`.
- **Blockers:** **Four of the five remaining rules cannot move without a founder ruling or a document amendment**, and that was established before any code was written this session rather than discovered at the end. `R-32`'s anchor and authoritative column are founder rulings. `R-20`'s setpoint push is M02's (`DEP-M2-03`). `R-01` and `R-05` are stated against columns `CalendarDay` and `DailyMark` do not carry, so the calendar transcription does not unblock them. **`R-11` is the only one an engineering session can still reach**, by writing `replay`.
- **Landmines:**

  - **THE SESSION BRIEF SAID "START AT THE FIRST TITLED-BUT-UNDECLARED RULE AND WORK FORWARD" AND THAT IS `R-01`, WHICH IS NOT ENGINE-COMPUTABLE.** Working forward from it produces no code for four consecutive rules. The two that could move each needed a DIFFERENT unwritten file, and `ADR-003` allows one per session, so the fork went to the founder before anything was written: `P2-1` (declares `R-17` via CV-01) or `replay.ts` (declares `R-11`). **`P2-1` was chosen.** The general point is worth keeping: **an undeclared count is not a work queue.** Six undeclared rules were two units of available work and four statements about other layers.

  - **`R-17` WAS FILED UNDER "DISCHARGED OUTSIDE THE ENGINE ENTIRELY" AND `validatePlan` IS IN THIS PACKAGE.** Its row reads "rejected at publish by CV-01", and M01 section 1.3's layout puts `plan/validate.ts` in `packages/rules-engine`. The line that grouped it with `R-11` and `R-20` was reading "publish" as "somebody else". **THIS IS THE THIRD TIME THE SAME STEP HAS BEEN TAKEN**: [session 48](2026-08-17-session-48.md) found `R-02`, `R-06` and `R-10` filed the same way, and [session 47](2026-08-16-session-47.md) named the pattern first. A correct observation about the neighbours, generalised without checking the rule in hand. It is now written into [`src/rules.ts`](../../packages/rules-engine/src/rules.ts) beside the reason rather than only into a log, because the pattern has outlived two sessions that each named it.

  - **`min_settlement_lag_trading_days` IS REQUIRED BY M01 AND ABSENT FROM DATA_MODEL SECTION 11's SHAPE.** M01 section 2.4 is explicit that it "remains **a published configuration constant rather than a literal in engine code**, for the same reason it always was: a future change to the settlement model re-runs this comparison instead of quietly invalidating it". `PW-02a` and `PW-02b` are unwritable without it. DATA_MODEL section 11's `plan_versions.rules` example carries no such key, and neither does `test/generators/plan-config.ts`. **The field is declared on `PlanRulesJson` on M01's authority and the gap is reported rather than folded**: writing `0` inside `publishDiff` would violate the sentence that made it config, and editing an approved document is an ADR and not a commit. `ADR-019` fixes the v1 value at `0`.

  - **`plan_version_sizes` MATERIALIZES ONE `drawdown_cents` AND ONE `daily_loss_limit_cents`, AND `rules` DECLARES BOTH PER PHASE.** M01 Appendix A lists "Eval drawdown" and "Funded drawdown" as separate rows, `R-12` says the funded reset uses "the **funded** drawdown", and `R-31` spells it `size_cents - funded drawdown_cents`. All three v1 plans set the two equal, so nothing in the corpus exercises a difference and `0004_catalog.sql` committed to one column. **If they ever differ, `resolvePlan` has one number and two phases to hand it to and whichever phase loses gets the other's drawdown silently**, which is the eval floor moving without a rule saying so. The new `MZ-per-phase` finding refuses the publish instead of picking, and it is gated on the eval phase being ENABLED, because `ResolvedPlan.eval` is null on Direct and no eval drawdown is ever resolved there.

  - **THREE FINDINGS CARRY NO `CV-nn` AND THEY BLOCK ANYWAY.** `MZ-lock-flag`, `MZ-per-phase` and `MZ-cap-ordinals` are the three ways `plan_version_sizes` can disagree with the jsonb it was computed from. M01's CV table enumerates none of them, and **`0004_catalog.sql` is the primary source that puts the check at publish**: "The publish path writes both, and CV-publish validation asserts the materialized flag matches the parent's jsonb." They block under `FM-07`, whose recovery line is "publish blocked" and whose blast radius is "accounts permanently ineligible while looking healthy". **They are reported on their own channel rather than filed under a neighbouring CV rule**, because borrowing an id would make a finding nobody ruled look like one somebody did.

  - **`CV-04` HAS NO `phase_eval.enabled` PRECONDITION AND `CV-03` DOES, ONE ROW APART.** Transcribed literally, a Direct plan, whose eval phase is disabled by Appendix A.3, must still publish `phase_eval.min_trading_days >= 1` for a phase it never runs. **The independent oracle reads it identically**, so the asymmetry is on the page rather than in one reading of it. Followed and reported, on the `R-15` and `R-22` precedent, with the consequence asserted in `RE-C-04` so a later session finds it rather than rediscovering it.

  - **DATA_MODEL SECTION 11's EXAMPLE TURNS THE EVAL FLOOR LOCK OFF AND NOTHING READS THAT FIELD.** Its `phase_eval.drawdown.lock.enabled` is `false` while `phase_funded`'s is `true`; `plan_version_sizes.floor_lock_enabled` is ONE column and `0004_catalog.sql` names its source as `phase_funded.drawdown.lock.enabled`. So both resolved phases carry the funded lock. **That the eval floor locks is ruled and not inferred**: [ADR-050](../decisions/ADR-050.md) computes GS-019's eval floor from it, "the lock triggers at 260,000c of profit and the eval target is 300,000c, so **every** v1 eval pass is also a lock day". It also agrees with `fixtures-in-code.ts`, which the fold has used since group B. Reported, not folded.

  - **A KILLED `falsify-ci.mjs` RUN LEAVES A MONEY-PATH MUTANT IN THE WORKING TREE, AND IT HAPPENED TWICE IN THIS SESSION.** `seededEdit` restores in a `finally` and a `finally` does not run when the process is killed. A timed-out run left the `R-33` mutant in [`gates.ts`](../../packages/rules-engine/src/payout/gates.ts): `tradedDaysSkipped` pinned to `false`, so `CV-19`'s disabled gate reports **satisfied** rather than skipped, which is `GS-080`'s exact failure. **It was caught only because the NEXT run's own "the mutant found no ..." error fired against the already-mutated line**, which is the harness noticing by luck and one `git add -A` from a branch. **A signal handler is not the fix and was tried first**: the gate runs its child SYNCHRONOUSLY, so node is blocked inside the spawn for the whole of each case and a queued handler cannot run, and `SIGKILL` is uncatchable regardless. The restore is now a journal written to disk BEFORE each mutation and replayed by the next invocation, verified by `SIGKILL`ing a run mid-case.

  - **AN ORPHANED `falsify-ci.mjs` FROM AN EARLIER KILL WAS STILL SEEDING MUTANTS WHILE I READ `git status`.** Two consecutive reads showed two different files modified, which is what sent the first diagnosis down the wrong path entirely (a phantom defect in `gates.ts`). `pkill -f falsify-ci` before trusting a status read after any interrupted run. This is the second face of [session 48](2026-08-17-session-48.md)'s "do not edit files while `falsify-ci.mjs` is running": the run you interrupted may not have stopped.

- **Files touched:** [`src/types.ts`](../../packages/rules-engine/src/types.ts), [`src/plan/validate.ts`](../../packages/rules-engine/src/plan/validate.ts) (new), [`src/plan/resolve.ts`](../../packages/rules-engine/src/plan/resolve.ts) (new), [`src/index.ts`](../../packages/rules-engine/src/index.ts), [`src/rules.ts`](../../packages/rules-engine/src/rules.ts), `test/plan-validate.test.ts` (new), `test/plan-resolve.test.ts` (new), `test/published-plans-in-code.ts` (new), `test/{rule-coverage,implemented-rules,rules-c-floor,evaluate.golden}.ts`, [`falsify-ci.mjs`](../../scripts/ci/falsify-ci.mjs), `.gitignore`, [STATE](../STATE.md), this file.

### The count, reported rather than implied

**Fifty of fifty titled. Forty-five of fifty declared.** The gap is five and every one of the five names where it is discharged.

| Not declared | Discharged by | Reachable by an engineering session? |
|---|---|---|
| R-01 | Ingest. `DailyMark` carries `fillCount` and no instant | **No.** A transcribed calendar adds rows, not a column |
| R-05 | `trading_calendar.session_open_at`/`session_close_at`, and `0032` | **No.** `CalendarDay` carries neither column |
| R-11 | The caller's `superseded_by is null`, **and replay recomputing forward** | **Yes**, by writing `replay.ts`. It is the only one |
| R-20 | M02's setpoint push (`DEP-M2-03`). The engine owes the number, not the push | **No.** The engine performs no I/O |
| R-32 | **Nothing.** The refusal is implemented; the anchor and the column are unruled | **No.** Two founder rulings |

`R-17` left the table because "rejected at publish by CV-01" was never somebody else's job.

### What `RE-C-oracle` is, and why it is worth more than nineteen more assertions

`test/generators/validate-plan.ts` transcribed the same nineteen rules from the same table in **session 40**, before any engine code existed, and its own header explains why it is deliberately not the generator: "If one module did both, the counterfactual would prove only that the code agrees with itself."

`plan/validate.ts` was written from M01's table and the two were compared **afterwards**. `RE-C-oracle` is that comparison as an executable check: each of the nineteen seeded violations is built in both stored shapes and both validators must name the same id. That is constitution C10's writer/reviewer split arriving as a test rather than as a process claim.

**The two differ in three places and none is a rule**, each documented at its site:

1. **The oracle calls `isInt` on money and this file cannot.** Its money is `number`, so half a cent is constructible and has to be rejected; here money is `Cents`, which is `bigint`.
2. **The oracle reads one merged `MaterializedPlan` and this file reads two stored shapes.** Where the merged view has one field, the tree has a jsonb key and a materialized column that can disagree, so `CV-09`, `CV-11`, `CV-12`, `CV-16` and `CV-17` name which of the two they read. The three `MZ-` findings are what make that choice safe instead of arbitrary.
3. **The oracle stops at the nineteen.** `PW-01` to `PW-04` and the materialization findings are here because this is the publish path.

### Appendix A.4 is now a test rather than a table

A.4 exists so "the first `validatePlan` run is a confirmation rather than a discovery". It is reproduced row by row: all three plans publish clean, and the publish diff matches exactly, including **`PW-03` firing on Core EOD alone** (150,000 > 100,000, while Merit Rapid and Direct tie and a strict `>` does not fire) and **`PW-02b` on Merit Rapid alone** (0 + 1 < 3, dominated) against `PW-02a` on the other two (0 + 5 = 5, co-binding).

Core EOD is also validated at **all four ADR-024 sizes**, with the bp arithmetic asserted to reproduce the transcribed 50K and 150K columns. `at(15_000_000).floor_lock_at_profit_cents` is 760,000c, which is Appendix A.1's own figure.

And `resolvePlan` is asserted against `fixtures-in-code.ts`'s `CORE_50K` and `MERIT_RAPID_50K`, which are `ResolvedPlan` values transcribed from the same appendix by a session that had not written the function. **Two independent readings of Appendix A check each other**, which is the property a golden file cannot supply here.

### What is NOT here

**No golden fixture and no `EC-nnn`.** P2 section 2 alternates fixture and engine sessions.

**No ADR.** Four findings above are reported and none is resolved, on the reason sessions 44, 45, 47 and 48 each gave: a defect in a frozen document is the founder's, and a session that writes an ADR to unblock its own path is grading its own work. Two of the four are candidates: `min_settlement_lag_trading_days`'s absence from DATA_MODEL section 11, and whether `plan_version_sizes` should carry a per-phase drawdown.

**CI-03's polarity is untouched**, for the sixth session running.

**`replay.ts`, `hash.ts` and `SD-08` are still absent**, and `R-11` is what they would declare.
