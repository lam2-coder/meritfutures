---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 247: `OI-18`, and the eligibility question was answered in 2026-08-20 and broken in 2026-08-21 (money path by subject)

<!--index: **THE DISPATCH'S PREMISE WAS FALSE AND THAT IS THE FINDING.** `OQ-F3-01` and `OQ-F3-02` did not sit open since session 86: [ADR-067](../decisions/ADR-067.md) sections 3 and 4 ruled both on 2026-08-20, the entry heads itself `accepted`, and the founder granted it on 2026-08-21. Only the registers had not moved. **The contradiction the dispatch named was closed by [ADR-075](../decisions/ADR-075.md) on 2026-08-21, and closing it created the one that is live**: `INV-M20-06` went to `identities.status = 'active'`, which shut the SECOND of the two exits `ADR-067` section 3 stands on, so **for a `closed` identity the horn `ADR-067:103` declared FALSE is true again** and [`0038:106`](../../packages/db/migrations/0038_account_adjustments.sql)'s stated reason, inside a MERGED money-path migration, is no longer true of the door set. **[ADR-135](../decisions/ADR-135.md) clause 2 posts the credit anyway**, because a refusal to post is not a refusal to owe and refusing hides the liability from `P-M6-01` rather than preventing it; the accepted cost is an obligation Merit records and cannot pay, bounded by `closed` having **no writer** because `G-ENFORCEMENT-CLOSE` is defined nowhere. **Clause 4 says the missing fact is what closure IS and refuses to invent it. Clause 5 hands `INV-M20-06` against `INV-M20-09` to session 236's `M20`.** No migration number spent; `M20`, `0038` and `ADR-067` untouched. **One test, six assertions, two seeded reverts watched failing and both files restored byte-identically.** The approval clause names a five-part predicate rather than a count and was RUN. 32 of 32 gates, 10 of 10 invariants, `pnpm run verify` exit 0, 150 files / 2,662 passed / 1 skipped -->

- **Objective:** rule `OI-18`, `ADR-067`'s eligibility question, which the dispatch describes as having no safe default and having sat open since [session 86](2026-08-20-session-86.md).
- **Done:** **[ADR-135](../decisions/ADR-135.md), `status: proposed`, UNSIGNED, six clauses.** **Clause 1 closes `OQ-F3-01` and `OQ-F3-02` by pointing at [ADR-067](../decisions/ADR-067.md), which ruled both on 2026-08-20 and was granted on 2026-08-21**, so the registers were stale and the questions were not open. **Clause 2 rules the residue that actually is live**: [ADR-075](../decisions/ADR-075.md) shut the wallet exit on a `closed` identity the day after [ADR-067](../decisions/ADR-067.md) rested on it, and the credit posts anyway, with both directions priced. Clause 4 names the missing fact as **what closure IS** and refuses to infer it; clause 5 hands `INV-M20-06` against `INV-M20-09` to **session 236's [M20](../plans/M20-wallet.md)**; clause 6 closes `OQ-F3-02` with no residue. **One test block in [`account-adjustments.test.ts`](../../packages/db/test/account-adjustments.test.ts), six assertions, named loudly in section 4 of the plan above.** **32 of 32 gates, 10 of 10 invariants, `falsify` clean at 80 scope and 10 loader cases, `pnpm run verify` exit 0, 150 files / 2,662 passed / 1 skipped.** **Two seeded reverts watched failing and both source files restored byte-identically.**
- **Next:** **three things this fence does not hold, all named in [ADR-135](../decisions/ADR-135.md) and none started here.** `OQ-135-01` and `OQ-135-02` are the founder's and gate clause 5. **[M20](../plans/M20-wallet.md) carries two invariants that now disagree** and one of them moves, which is **session 236's**. And [ADR-067](../decisions/ADR-067.md) section 9 still owes amendments to the `GS-296` and `GS-298` registry rows, which was outside that session's fence and is outside this one.
- **Blockers:** **[ADR-135](../decisions/ADR-135.md) is UNSIGNED and the `E2` read is owed. MONEY PATH BY SUBJECT, NOT MERGED.** Clause 2 accepts a cost on a trader's ability to receive money already credited to them, which is exactly the class `E2` reserves the line-by-line read for. **Two questions go to the founder rather than to a session**: `OQ-135-01`, what closure IS, and `OQ-135-02`, whether closure forfeits a wallet balance. Neither is an engineering question and clause 4 refuses to infer either.
- **Landmines:** **[`0038:106`](../../packages/db/migrations/0038_account_adjustments.sql) states a reason that is no longer true of the door set** and it is inside a MERGED money-path migration, so it is superseded rather than edited ([E2](../../MERIT_BUILD_MASTER_PROMPT.md)) and **session 240 owns the number**. A reader who finds that header before finding [ADR-135](../decisions/ADR-135.md) will believe a `closed` identity can extract its credit. **[`0038:83`](../../packages/db/migrations/0038_account_adjustments.sql) also still calls [ADR-067](../decisions/ADR-067.md) `proposed` with approval `PENDING`**, stale by one status transition, same reason. **And a code line has answered an open founder question**: [`checkout.ts:894`](../../apps/api/src/routes/checkout.ts) refuses any identity that is not `active`, which decides `OQ-075-01` in a direction [ADR-075](../decisions/ADR-075.md) explicitly declined to rule. All three are **reported and not repaired**, [ADR-135](../decisions/ADR-135.md) section 7.
- **Files touched:** [ADR-135](../decisions/ADR-135.md) (new), [FOLD-03](../plans/FOLD-03-vendor-parity-gap-fill.md) section 11's `OQ-F3-01` and `OQ-F3-02` rows, [ALLOCATION](../decisions/ALLOCATION.md)'s `OI-18` and `135` rows, [`packages/db/test/account-adjustments.test.ts`](../../packages/db/test/account-adjustments.test.ts), [sessions/README](README.md), [decisions/README](../decisions/README.md) and [INDEX](../INDEX.md) and [STATE](../STATE.md) by generated span, this file.

---

### THE PLAN, written before any source file, per the dispatch

Plan mode is deliberately NOT entered: it wedges a remote session on an approval
prompt nobody is watching. **This section is the plan, it is committed and pushed
before the first source file, and it is amended in place if the tree contradicts it.**

#### 1. The dispatch's premise, measured

The dispatch says `OQ-F3-01` and `OQ-F3-02` "have sat open since session 86" and need
a ruling. **Measured against the tree, that is half true and the half that is false is
the more useful half.**

| # | Claim | Measured at `file:line` | Effect on the objective |
|---|---|---|---|
| 1 | `OQ-F3-01` and `OQ-F3-02` are open | **They were RULED.** [ADR-067:104](../decisions/ADR-067.md) rules the eligibility trade and [ADR-067:120](../decisions/ADR-067.md) rules the debit. [ADR-067:1](../decisions/ADR-067.md) is `status: accepted` and [ADR-067:255](../decisions/ADR-067.md) records **"Founder approval: GRANTED 2026-08-21"** | The registers are stale, not the questions. This session does not re-rule what is ruled |
| 2 | The contradiction to expect is `INV-M20-06` reading `= 'restricted'` against a payout gate reading `= 'active'` | **CLOSED, on 2026-08-21, by [ADR-075](../decisions/ADR-075.md).** [M20:62](../plans/M20-wallet.md) now reads **`identities.status = 'active'`** and carries the amendment note in its own cell; [STATE_MACHINES:421](../architecture/STATE_MACHINES.md) moved with it | The predicted contradiction does not exist. **Closing it created a different one**, and that one is this session's whole subject |
| 3 | Session 236 holds `M20`, do not fix | Held when this branch was cut. `M20` is not touched. **236 landed as `5421885` while the pull request was open**, and [P5 section 5.1](../plans/P5-payouts-and-wallet.md) reaches this session's own premise finding independently while naming two residues, **neither of which is this one** | The live contradiction is named and routed. Routing corrected to the file: `M20` is `approved` and unowned |

#### 2. What is actually live, and why it is a ruling rather than a transcription

**[ADR-067:104](../decisions/ADR-067.md)'s third path was "excluded from the payout
computation, land in the wallet", and its whole force is [M20:41](../plans/M20-wallet.md):
the wallet is "the only balance in the system with two exits".** [ADR-067:103](../decisions/ADR-067.md)
calls the fold plan's "excluded and therefore unwithdrawable" horn **false** on exactly
that ground.

**[ADR-075](../decisions/ADR-075.md) landed the next day and shut the second exit for one
identity state.** [ADR-075:27](../decisions/ADR-075.md) amends `INV-M20-06` and
`G-WITHDRAWAL-CLEARED` to `identities.status = 'active'`, and [ADR-075:29](../decisions/ADR-075.md)
states the consequence in its own words: **"no `closed` identity can move value out
through any door."**

**So for a `closed` identity, both exits are shut and the refuted horn is true again.**
[ADR-067:179](../decisions/ADR-067.md) permits an adjustment against `closed` and reasons
from the pre-amendment text; [`0038:106`](../../packages/db/migrations/0038_account_adjustments.sql)
carries the same reason into a **merged money-path migration**: *"`closed` is permitted for
INV-M20-09's reason: a wallet balance is payable on demand FOREVER."* That sentence is now
false about the door set. **A merged migration is never edited, only superseded** ([E2](../../MERIT_BUILD_MASTER_PROMPT.md)),
and `0038` is not superseded here.

**Two invariants in one document now disagree.** [M20:62](../plans/M20-wallet.md) shuts every
extraction door on `closed`; [M20:65](../plans/M20-wallet.md) `INV-M20-09` says a wallet
balance is **payable on demand forever** and that dormancy never forfeits it.
**`M20` was session 236's when this branch was cut and is untouched here.** 236 has since landed and did not take this pair, so [ADR-135](../decisions/ADR-135.md) clause 5 routes it to the file rather than to a session.

#### 3. The ruling this session will write, with both costs priced

`ADR-135`, `status: proposed`, approval line UNSIGNED.

| Clause | Content |
|---|---|
| **1** | `OQ-F3-01` and `OQ-F3-02` **as [FOLD-03:258-259](../plans/FOLD-03-vendor-parity-gap-fill.md) asks them are CLOSED by `ADR-067`**, not by this entry. The registers are amended to say so |
| **2** | **`ADR-067`'s answer to `OQ-F3-01` is false in exactly one state and the state is `closed`**, since `ADR-075`. Stated with both citations and with `0038:106`'s stale reason named |
| **3** | **THE RULING.** The credit is **posted**. `ADR-067` section 7 stands, `0038` is not superseded, and the cost accepted is an obligation Merit can record and cannot pay while the identity is `closed`. **Both directions priced in full** |
| **4** | **The fact nobody has is what closure IS.** `G-ENFORCEMENT-CLOSE` is `closed`'s only inbound edge ([STATE_MACHINES:329](../architecture/STATE_MACHINES.md)) and is **defined nowhere** ([STATE_MACHINES:353](../architecture/STATE_MACHINES.md)); [ADR-041](../decisions/ADR-041.md) says closure for cause is terminal and **per account** while the drawing routes an **identity**. This entry refuses to infer one from what an adjustment needs |
| **5** | **What binds whoever defines `G-ENFORCEMENT-CLOSE`**: state what happens to a wallet balance held at closure. `INV-M20-06` and `INV-M20-09` disagree and one of them moves. **236's file, named not edited** |
| **6** | **`OQ-F3-02` stands entirely.** `ADR-075` does not touch the debit direction and `0038`'s reversal-only machinery reads no identity status |

**Why clause 3 goes the way it does, which is the durable part.** Refusing the row is the
option that looks cautious and is not: **a refusal to post is not a refusal to owe.** The
obligation exists whether or not the row does, so refusing deletes only the record of it,
which is the one thing the whole of `ADR-067` exists to create. The mechanical cost is also
real: refusing needs a superseding migration against a merged money-path file, and this
session **has no migration number** (session 240 owns them all in this wave).

**Why the accepted cost is affordable today and will not be later, as a fact rather than a
hope.** `closed` **has no writer**: `grep` over `apps/`, `packages/` and `scripts/` finds no
statement that writes `identities.status`, and `closed`'s only inbound edge is undefined. The
strand clause 3 accepts is **unreachable in the tree that exists**, and becomes reachable on
the day somebody defines the closure edge. That is why clause 5 binds that session and not
this one.

#### 4. The one code change, named loudly

**A test, no schema, no migration, no production code.**
[`packages/db/test/account-adjustments.test.ts`](../../packages/db/test/account-adjustments.test.ts)
gains a describe block pinning the three extraction doors at `= 'active'`.

**The ruling forces it.** Clause 3 accepts a cost whose size depends on a tree property, and
**an accepted cost that nothing watches is exactly how this defect arrived**: `ADR-067` rested
on `M20:62`, `M20:62` moved the next day, and nothing failed. [CLAUDE.md](../../CLAUDE.md) says
to prefer a new mechanical assertion over a bigger model whenever the error is checkable. This
one is checkable. The file already carries the idiom in `ADR-067 and SD-M6-09 still say what
0038 was built from`, and its own header says every assertion "fails on a real, specific
regression that a future session could otherwise land without anybody noticing."

#### 5. The fence, and what is refused inside it

**Written:** [ADR-135](../decisions/ADR-135.md) (new), the `OQ-F3-01`, `OQ-F3-02` and `OI-18`
register rows, [ALLOCATION](../decisions/ALLOCATION.md)'s `135` row amended in place,
[decisions/README](../decisions/README.md) and [INDEX](../INDEX.md) by `gates.mjs generate`,
[sessions/README](README.md), this file, and the one test.

**Refused inside the fence:** no migration number. No edit to `M20` (236's at the time, and an `approved` document either way), to `0038`
(merged, `E2`), to `ADR-067` (`accepted` and approved), or to the `GS-296` and `GS-298`
rows (`ADR-067` section 9 already owes those to whoever holds that fence).

#### 6. Verification, and the approval clause names an invariant rather than a count

`pnpm run typecheck`; `repo-invariants.mjs` 10 of 10; `gates.mjs generate && check` 32 of 32;
`falsify.mjs` clean; `pnpm run verify` exit 0; `pnpm vitest run` green.

**The approval clause is a predicate over four sites and it is RUN on this branch before it
is claimed**, per the dispatch: five count-naming clauses have drifted and in every one the
ruling held and the enumeration did not.
