---
status: approved
depends_on: [README.md]
last_updated: 2026-09-08
---

## 2026-09-08 - Session 670: the convention item 1 asks for has held 45 times with nothing enforcing it, a control keyed on the entry number is satisfiable by every commit it can judge, and the key had to be the number because git history answers a shallow clone with the graft

<!--index: **THE ROW IS [ADR-475](../decisions/ADR-475.md), `proposed` and UNSIGNED. NON-MONEY DIFF, TWO NEW FILES AND NO EXISTING SOURCE FILE EDITED, `E2 READ OWED`.** [ADR-473](../decisions/ADR-473.md) section 10 items 1 and 3, one convention in two halves, neither applicable retroactively because both live in dated records [ADR-386](../decisions/ADR-386.md):169 forbids repairing. **228 AND 280 RE-DERIVED AT `ADR-473`'s OWN BASE BY AN INSTRUMENT THIS ROW COMMITTED: 233 of 283**, and **the 52 reproduces exactly** under the weaker predicate this row started with. **THE ROW'S QUESTION IS ANSWERED YES FOR ONE HALF AND NO FOR THE OTHER, AND THE RETROACTIVITY IS NOT WHAT DECIDES IT.** A control is worth minting when its forward population is a regularity a future commit could break: **item 3's is 45 entries deep and item 1's is empty.** **ITEM 3 IS GATED**, by [`citation-base-ref.mjs`](../../packages/tooling/checks/citation-base-ref.mjs) and [its suite](../../packages/tooling/test/citation-base-ref.test.ts), 42 cases, after testing each of `ADR-473` section 4's three grounds against THIS control rather than inheriting them: a harvester that cannot ENUMERATE a bare-token list can still DETECT the entry that published one, this check opens no source file so it has no ref to be wrong at, and keyed forward its findings inside dated records are zero. **THE KEY IS THE ADR NUMBER AND GIT HISTORY WAS REFUSED ON A MEASUREMENT**: `CI-02` clones one commit deep, and **in a shallow clone `git log --diff-filter=A` does not fail, it answers with the graft**, giving `ADR-001` and `ADR-100` an add-commit dated 2026-09-05 with no error anywhere. **THE CUTOFF IS DERIVED**, one above `ADR-416`, the highest publishing entry naming no base, and leg D holds it there so it cannot be raised past a finding; **above it the practice was already universal and nothing required it, 45 of 45 over 57 consecutive numbers**. **THE PREDICATE IS ADJACENCY AND NOT SHAPE** ([ADR-467](../decisions/ADR-467.md) section 9), four naming words measured off the corpus, which is what stops `ADR-228`'s `9223372036854775807` counting as a base. **THE GRANDFATHERED HALF IS A PINNED COUNT AND NOT A ROW PER MEMBER**, because `ADR-386`:169 already names the un-repairable categorically and a register would rewrite one sentence 233 times; the both-sides pin is `tree-input-budget.mjs`'s and it is what makes an emptied harvest red rather than green. **ITEM 1's SPLIT GATE IS REFUSED and the condition for revisiting it is a count rather than a judgement: three entries above 474 publishing an owed list**, because a predicate carrying a threshold and a heading vocabulary fitted to a population of one written by its own author is a site and not a shape. **WATCHED FIRING AND NOT OVER-FIRING ON THE REAL TREE**, seeded one word on this entry's own file, 457 entries classified both ways and **exactly one moved**, restored byte-identical by `sha256sum -c`. **THE SUITE FOUND TWO DEFECTS IN THE INSTRUMENT BEFORE IT LANDED**, both invisible to every leg: a bare pointer resolved against `docs/decisions/`, and one `#Lnnn` pointer counted twice. **THIS ENTRY IS THE FIRST TEST OF ITS OWN RULING AND IT PASSES.** `price-register.mjs` is red by design and by precedent -->

### 1. The objective

Rule [ADR-473](../decisions/ADR-473.md) section 10 items 1 and 3, engage `ADR-450` and `ADR-473` rather than inherit them, and answer whether a control can be keyed to fire only on entries newer than a stated ref and whether that is a gate worth having or a convention worth only writing down.

### 2. Done

- **[`packages/tooling/checks/citation-base-ref.mjs`](../../packages/tooling/checks/citation-base-ref.mjs) and [its suite](../../packages/tooling/test/citation-base-ref.test.ts)**, 42 cases, four legs, keyed forward on the entry number. [ADR-475](../decisions/ADR-475.md) sections 4 and 5.
- **228 and 280 re-derived** at `ADR-473`'s own base by this instrument: 233 of 283, and 52 exactly. `ADR-475` section 2.
- **The ruling on the retroactive problem**, argued against `ADR-450` section 6 and `ADR-473` section 4 ground by ground. `ADR-475` sections 3, 4 and 6.
- **Git history refused as a key, on a measurement rather than a preference.** `ADR-475` section 5.1.
- **The control watched firing and not over-firing** on the real tree, seeded and restored. `ADR-475` section 7.
- **Two defects found in the instrument by its own suite before it landed.** `ADR-475` section 8.

### 3. Next

- The founder's read on `ADR-475` section 12's second point: whether 45 of 45 is a convention or one dispatcher's template, because pinning a template is pinning a habit.
- `ADR-473` section 10 item 2, the fast-check seeding decision. **Money path, and a new session.**
- `ADR-475` section 10 item 1, the split gate, once three entries above 474 have published an owed list.

### 4. Blockers

None. `price-register.mjs` is red on this branch by design and by precedent, `ADR-475` section 9.1.

### 5. Landmines

- **`git log --diff-filter=A` LIES IN THIS CONTAINER AND DOES NOT SAY SO.** The clone is shallow, `CI-02`'s is one commit deep, and every add-date older than the graft comes back as the graft's. Never key anything on it without checking two known-old files first.
- **`owed` is a substring of `narrowed` and `followed`.** Any heading vocabulary over that word needs boundaries; without them it is 8.1 percent wide on this corpus.
- **A bare `path.ts:12` in an entry is REPO-RELATIVE and a link target is ENTRY-RELATIVE.** Resolving both the same way produces `docs/decisions/apps/...`, which passes every extension test and is a path no entry meant.
- **An entry publishing sites in files it created cannot classify them at its own base**, because they are `ABSENT` there. The ref beside a list is the ref the list was COMPILED against.

### 6. Files touched

- `packages/tooling/checks/citation-base-ref.mjs` (new)
- `packages/tooling/test/citation-base-ref.test.ts` (new)
- `docs/decisions/ADR-475.md` (new)
- `docs/decisions/README.md`, `docs/INDEX.md`, `docs/decisions/ALLOCATION.md` (row 475 only)
- `docs/sessions/2026-09-08-session-670.md` (new), `docs/sessions/README.md` (generated span)
- `docs/STATE.md` (generator output only, no prose changed)
