## 2026-08-15 - Session 30: DECISIONS.md becomes a directory (ADR-043, stage 1 of 5)

- **Done:** [ADR-043](../decisions/ADR-043.md): the four append-only registries become directory-per-entry. **Stage 1 only**, which is `DECISIONS.md`, chosen first because it carries the anchor problem the whole change turns on. 43 ADR entries to `docs/decisions/ADR-nnn.md`, 5 gate closures to `docs/decisions/gates/`, the 3 allocation tables to `docs/decisions/ALLOCATION.md`. **889 inbound links rewritten and 441 outbound links re-based**, both driven by the map [`split-decisions.mjs`](../../scripts/corpus/split-decisions.mjs) derives, never by a hand-typed table. New gate `CI-06n`. Nine gate readers updated, `falsify.mjs` seeds derived rather than pinned per the founder rider.
- **Next:** Stage 2, `EDGE_CASES.md` (119 block entries per file, the 22-row Appendix B4 battery kept whole). Then `DATA_MODEL.md` (96 tables), `SESSION_LOG.md`, `GOLDEN_SCENARIOS.md` (per section, 30 + 3). Each is its own pull request ending mergeable with all gates green.
- **Blockers:** None. `main` is clean and stage 1 stands alone.
- **Landmines:**
  - **The split rule is not uniform and the reason is load-bearing:** split what is appended to, keep together what is closed, never split a table a gate reads as a table. A later session that splits the gate closures per ruling, or `ALLOCATION.md` per row, breaks `CI-06f` and `CI-06h` and scatters records that only mean anything together.
  - **The brief named six gates to update. There are nine.** `CI-06b` and `CI-06c` change scope, `CI-06h` reads the migration allocation table, `CI-06i` reads `DATA_MODEL`. A gate whose input is now a GLOB cannot rely on `ENOENT` to enforce rule 2, because a glob matching nothing returns an empty array rather than throwing. Every directory-shaped reader added here carries its own emptiness check, and stages 2 to 5 must do the same.
  - **`ADR-D1` and `ADR-D2` exist.** The first entry pattern hardcoded `D1` and a falsify scope case caught it immediately, on the same run it was written.
  - **Three links cite a "Wave 2 gate" record that does not exist** in the registry. They pointed at a 1,537-line file and named nothing, so nothing was ever broken and nothing was ever right. They now point at the registry README, and the missing closure is a real gap rather than a rewrite artifact.
  - **The first CI gate letter reached for was `k`, which FOLD-01 holds.** Reading the allocation table is what caught it, on the session whose entire subject is collision.
- **Files touched (stage 1):** `docs/decisions/` (50 new), `docs/DECISIONS.md` (deleted), `scripts/corpus/{split-decisions,rewrite-links}.mjs` (new), `scripts/corpus/{gates,falsify}.mjs`, `docs/testing/STRATEGY.md`, `docs/{INDEX,STATE,SESSION_LOG}.md`, `.claude/agents/reviewer.md`, `CLAUDE.md`, and 112 documents carrying rewritten links.

### Stage 2: EDGE_CASES.md

- **Done:** `docs/EDGE_CASES.md` to `docs/edge-cases/`. **119 block entries per file, the 22-row Appendix B4 battery kept whole** as one file per ADR-043's ruling that a table row is not a document. 36 inbound links and 51 outbound re-based. `CI-06d`, `CI-06e` and the `ec_count` span read the directory through one shared reader. Content conservation asserted: 536 of 536 non-blank lines survive.
- **Next:** Stage 3, `DATA_MODEL.md` (96 `### <table>` sections, `CI-06i`'s input). Then `SESSION_LOG.md` and `GOLDEN_SCENARIOS.md` (per section, 30 + 3).
- **Blockers:** None.
- **Landmines:**
  - **The rewriter is now ONE script with a config per registry**, not a copy per registry. Stage 2 was the event that would have produced the second copy, which is OQ-P1-04's defect exactly. The `decisions` pass is idempotent and is the regression check that generalizing it did not break stage 1.
  - **The provenance dividers (`## Entries from M05`) are not entries and not containers**, because the entries below them sit at the same heading level. They became grouping headings in the README index with their prose intact. A later session that treats them as either will lose a paragraph or create eight files whose whole content is a label.
  - **`once()` now accepts a RegExp and still checks its anchor.** A seed whose anchor has moved must announce itself rather than replace nothing.
- **Files touched (stage 2):** `docs/edge-cases/` (121 new), `docs/EDGE_CASES.md` (deleted), `scripts/corpus/split-edge-cases.mjs` (new), `scripts/corpus/{rewrite-links,gates,falsify}.mjs`, `docs/{INDEX,SESSION_LOG}.md`, `CLAUDE.md`, and 49 documents carrying rewritten links.

### Two findings from stage 2

**The `SEED IS STALE` mechanism added in stage 1 caught its first real seed on the next stage.** `CI-06e`'s seed still edited `docs/EDGE_CASES.md`. It was watched reporting `SEED IS STALE` with the `ENOENT` named, rather than crashing the run or being scored as an ordinary gate failure, which is what the founder's rider asked for.

**INDEX claimed the registry held "140 entries" against 141.** Not in a generated span, so `CI-06g` could not see it. It is the eighth site of the hand-maintained-count class and is now an `ec_count` generated span, on [ADR-034](../decisions/ADR-034.md)'s own remedy.

### Stages 3, 4 and 5: DATA_MODEL, SESSION_LOG, GOLDEN_SCENARIOS

- **Done:** The conversion is complete. `DATA_MODEL.md` to 96 table records, `SESSION_LOG.md` to 30 session files, `GOLDEN_SCENARIOS.md` to 33 section files. **334 files across five registries**, all five wired into `CI-06n`, and `CI-06d`, `CI-06e`, `CI-06i` plus the `adr_count`, `ec_count` and `gs_count` spans all read directories through one guarded reader each.
- **Next:** Nothing in this change. The build sequence resumes at the P1 planning session.
- **Blockers:** None.
- **Landmines:**
  - **The rewriter's old-location base is PER REGISTRY.** `DATA_MODEL` sat one directory deeper than the others, and re-basing its outbound links against `docs/` finds nothing to fix and leaves every one of them broken. It reported `outbound: 0` on a registry of 96 files, which is the number that gave it away. A sixth registry must set `from:` or repeat this.
  - **Per-section filing does not cost resolution.** Every `GS-nnn` maps to the section file that defines it, first-definition wins, because section 1's numbering map lists identifiers defined later and pointing citations at the map would be worse than pointing them at the README.
  - **`CI-06i`'s regex is copied into the splitter verbatim** rather than approximated. If the two readings ever differ by one heading, the split moves a record the gate then reports as missing.
- **Files touched (stages 3 to 5):** `docs/architecture/data-model/` (97 new), `docs/sessions/` (31 new), `docs/testing/golden-scenarios/` (34 new), the three source files deleted, `scripts/corpus/{split-data-model,split-sessions}.mjs` (new), `scripts/corpus/{rewrite-links,gates,falsify}.mjs`, `docs/INDEX.md`, `CLAUDE.md`.

### Three findings from the last three stages

**SESSION_LOG carried two entries numbered Session 9.** The sequence ran `9, 10, 11, 9, 13`, so the second was the missing Session 12, and it is corrected. Nothing was checking session numbers, and the defect was invisible while the entries shared a file. **The split is what surfaced it**, because two entries cannot share a filename.

**This session's own log was the second duplicate.** Stages 1 and 2 were written as two `Session 30` entries. They are one session, so they are now one entry with a subsection per stage.

**Two INDEX rows carried hand-maintained counts**, "140 entries" against 141 and "257 scenarios" against 257. Neither sat in a generated span, so `CI-06g` could not see either. They are the eighth and ninth sites of that class and are now `ec_count` and `gs_count` generated spans.
