---
status: approved
depends_on: [README.md]
last_updated: 2026-08-20
---

## 2026-08-20 - Session 106: the fold queue closes, and what the next planning session inherits (review desk)
<!--index: the fold queue closes: eleven pull requests merged, 24 of 24 gates, and the merge discipline the wave established by getting it wrong once -->

- **Objective:** merge the remaining eleven sessions of WAVE-03, FOLD-03, FOLD-04 and FOLD-05, leave `main` green, and hand off.
- **Done. Eleven pull requests merged.** `main` reports **24 of 24 gates**, `falsify.mjs` clean and dirty with **35 scope cases** and 10 loader cases, **7 of 7 invariants**, and **74 test files, 1085 passing**. **The fold queue is empty.**
- **Next:** the fixture backlog and `CI-07` to `CI-09`. Neither is corpus work and both are named below.
- **Blockers:** None.

### What the four plans delivered

| | |
|---|---|
| **WAVE-03** | The 106 duplicate registry keys are **19**, and the 19 are the parallel-session convention [ADR-064](../decisions/ADR-064.md) ruled valid rather than damage. Two new gates: `CI-06v` (no orphan table fragment) and `CI-06w` (the allocation registries as multisets, which is `OI-11` closed) |
| **FOLD-03** | The vendor-parity gap-fill. The Tier-1 economic calendar (`0039`) **closes `DEP-M7-06`**, which M07 declared when it was written and no table satisfied, so `D-04` was not implementable. Scheduled digests (`0040`), the duplicate-signal views, communications completeness (`0041`), and `ADR-067` with `0038` |
| **FOLD-04** | Impersonation as a distinct session type that **cannot elevate** (`ADR-068`, `0042`), and admin capability parity (`ADR-069`, `0043`) |
| **FOLD-05** | Plan-config completeness (`ADR-070`), `M21` admitted as the **first new module after FREEZE** (`ADR-071`), fee-back and the ladder unlock (`0044`), display-name decoupling, and `packages/harness` |

### The merge discipline this wave established, because it was learned by getting it wrong

**A union is correct for an APPEND and wrong for a DELETION, and the two are indistinguishable in a conflict hunk.**

| File class | Resolution |
|---|---|
| `sessions/README.md`, `decisions/README.md`, `DELTA_MANIFEST.md`, M06's delta table | **union.** Two rows for two sessions are two entries |
| `CI06U_REGISTER` | **deletion applied to main's copy.** The register shrinks only |
| `gen:` spans | **regenerate.** Each side is right about its own registry and wrong about the other's |

**The register was unioned once and the count went UP from 91 to 93**, resurrecting entries two sessions had already cleared. `CI-06u` failed on the merge that created it.

### Three findings no session could have produced alone

- **`SD-M6-nn` and `M6-N-nn` are shared registries with no allocation table.** Five sessions wrote M06 concurrently and all five would have taken `SD-M6-07`. **`SD` numbers cannot be reserved in a document**: `ADR-026` requires a `DELTA_MANIFEST` row for every `SD-` identifier under `docs/`, so the allocation had to live in the prompts. `M6-N-nn` surfaced the same way one wave later.
- **No gate detects a conflict marker.** `<<<<<<< HEAD` stood in [INDEX](../INDEX.md) and [STATE](../STATE.md) and 24 of 24 passed. Named as `CI-06/conflict-markers`; under [ADR-065](../decisions/ADR-065.md) a new gate takes a **slug** and needs no letter.
- **The `CI-06u` falsification seed decayed twice**, each time because the corpus it was anchored to was repaired. Hardcoded to `inv-m20-06`, then generalised to the register's first claim, then broken by the assumption that every claim carries exactly two rows. **A harness anchored to corpus state decays as the corpus is repaired**, and the wave that repairs fastest breaks it soonest.

### Landmines

- **THE `closed` IDENTITY DIVERGENCE WAS RECORDED BY THREE SESSIONS BEFORE ONE OWNED IT.** `ADR-062` made the payout gate `identities.status = 'active'` while [M20](../plans/M20-wallet.md)'s `INV-M20-06` blocks on `= 'restricted'`, so a `closed` identity could not request a payout but could still spend its wallet. `ADR-062`, `ADR-068` and FOLD-04 each named it and none held M20. **`I3` was given it explicitly and closed it.** The general lesson is that a finding recorded outside every fence belongs to nobody until someone is named.

- **`packages/harness` IS A NEW WORKSPACE PACKAGE AND A STALE `node_modules` READS AS FIVE FAILING SUITES.** `Cannot find package '@merit/rithmic'` is a missing workspace link, not a break. `pnpm install` fixes it. A reviewer who runs the suite without installing will diagnose a red tree that is green.

- **`M21` IS ADMITTED AND UNBUILT, AND ITS HARNESS IS BUILT AND UNWIRED.** `packages/harness` exists with its own tests; nothing calls it. `M21`'s console is a plan document and `apps/admin` is 44 lines.

- **Files touched:** the eleven merges and their conflict resolutions, [`falsify.mjs`](../../scripts/corpus/falsify.mjs) (one seed), [STATE](../STATE.md), [sessions/README](README.md), this file.
