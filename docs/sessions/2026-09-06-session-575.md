---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 575: the coverage of two invariants measured instead of described, a fourth blindfold and a fifth, and a very recent entry whose property survived while its cause did not

<!--index: **THE ROW IS [ADR-380](../decisions/ADR-380.md), `proposed` and UNSIGNED. CONTROL OVER THE CORPUS'S HONESTY, `E2` READ OWED.** [ADR-377](../decisions/ADR-377.md) ruled the citation FORM alone and found two blindfolds on the way, on top of the one [ADR-357](../decisions/ADR-357.md) named; a control with three blindfolds, two found by accident, is a control whose coverage nobody knows. **THE DELIVERABLE IS A NUMBER WHERE THERE WAS A LIST OF LEGS.** Every wrong in-file line number was simulated against each check's own legs, over its real scope, with the checks' own reader imported rather than a regex, and held to an ORACLE first: the verdict function returns **0** findings at the true lines, which is what both checks return. **`RI-15` REPORTS 44.2 PERCENT OF WRONG LINE NUMBERS AND `RI-16` REPORTS 5.2**, and the figure that matters is that **1,189 of `RI-16`'s 1,259** perturbable citations are checked by **NOTHING** about which line they name: any line in the file passes, and what is asserted is that the file exists and is long enough. **BLINDFOLD FOUR IS THE RANGE LEG AND NO LINE SAID SO**: miss (10) records that the VACANCY rule fires only when EVERY candidate is vacant, and the range leg is written the same way, so a pointer past the end of the file it MEANS is answered by any same-named file that is longer. That is the one leg needing NO NAME, the one [ADR-377](../decisions/ADR-377.md) used twice to prove the check armed, and **all 8 citations in `RI-15`'s scope whose line number is guarded by nothing at all are ambiguous paths**; `src/index.ts:52` already points past the end of one of its own seventeen candidates with 35 of 35 green. **BLINDFOLD FIVE IS THAT `RI-16` HAS NEITHER THE VACANCY LEG NOR THE ANCHOR**, and the cost of giving it them was measured once on a smaller corpus: on this tree it is **101 findings plus 15**, **87** of them in `approved` documents where a change is an ADR rather than a commit, against a figure on record of less than half that, **RETIRED and NAMED rather than reproduced under `RI-14`**. The A/B is the sharpest result here: the identical pointer onto a blank line is **RED for `RI-15` and GREEN for `RI-16`**, same reader, same tree, opposite verdicts. **[ADR-377](../decisions/ADR-377.md) SECTION 6's PROPERTY HOLDS AND ITS STATED CAUSE DOES NOT.** The binding is not lost to the scanner's running pairing state; `citedIdentifier` reads locally. It is lost to `NEGATED_CLAIM` being tested against **70 flattened characters before the name** while `flattenReasons` replaces a newline with a SPACE, so a previous comment line not closing with `.` or `;` leaks its own words into the next sentence's negation test. Every unbound insertion point measured carries a negation word in that leaked window and every bound one does not. **AND THE SIZE IS SMALLER THAN A SIX-POINT SAMPLE GAVE**: 62 of 983 insertion points, **6.3 percent**, the three-in-six figure retired and named. **THAT ENTRY'S OWN SECTION 11 PREDICTED THIS DIRECTION IN TERMS**, which is the hedge being right rather than the row being wrong. **THE BOOTSTRAPPING PROBLEM CANNOT BE FIXED AND THE ENTRY SAYS SO PLAINLY**, and not on the arithmetic: [ADR-377](../decisions/ADR-377.md) section 5's figures reproduce exactly at 13 files, 6 citations outside the specimen pair, 107 findings and 0 outside it, so narrowing buys six passing citations. The argument AGAINST that refusal is stated before it is answered, because a guard is for tomorrow and seed 8 shows a real defect going silent in one of the eleven files nobody exempted on purpose. It is answered structurally: the specimens must be false citations, so any exemption for them is written INSIDE the file being checked, where this corpus's shrinks-only rule for registers cannot be applied. **TWO REMEDIES WERE WRITTEN, RUN, AND REFUSED ON THEIR OWN RESULTS.** Nearest-by-directory resolution narrows 12 of 13 ambiguous paths to one file and raises 3 findings, and **all three were opened at primary source: two are the check guessing and the third is miss (10)'s own stated example**. The negation repair is four lines and newly binds two citations, one passing and one reddening [`wiring.test.ts:936`](../../apps/api/test/wiring.test.ts) over `routes/payouts.ts:606` for `subject`, declared at `:609`, **3 lines, the narrowest FALSE distance on record over a citation that lands inside its own declaration's docblock and is arguably TRUE**; the anchor would clear it and the anchor is never asked, because the window returns first. **THE PREDICTION FOR THAT SEED WAS COMPUTED FIRST AND THE COMPUTATION WAS WRONG IN ITS PARTICULARS**, naming the wrong bound name and the wrong reason; the seed corrected this entry's own text. **A COMPUTATION IS A CLAIM.** **A NARROWER READER PROPERTY IS REPORTED**: a path that WRAPS a line is severed by the newline joiner, the reader keeps the basename, and the citation inherits blindfold four; two sites are written that way and one resolves to the WRONG FILE. **WHAT SHIPS IS ASSERTION RATHER THAN WIDENING.** Eight cases hold the four blindfolds, each with a one-variable control that fires beside it and each silent half additionally shown to be READ by a pointer past every candidate, because a case that only asserts silence cannot tell a blindfold from a check that stopped running. **EACH IS A CLEARING CONDITION AND THAT WAS VERIFIED RATHER THAN CLAIMED**: applying the section 7 repair turned the case named for it RED. **THE `covers` CARDINALS WERE THEMSELVES STALE AND THAT IS THE ARGUMENT FOR CASES**: `RI-15`'s read 588 files and 207 citations where this tree gives 753 and 390. **NO LEG ADDED, NO GRAMMAR WIDENED, NO INVARIANT MINTED, `absence-claims.mjs` NEVER OPENED FOR WRITING.** **`RI-35` IS RE-VERIFIED AND UNMOVED**: `CRON_INVENTORY.md` line 21 is still **2,576** characters, `accountDaysFrom` is exact at `adapter.ts:870` and `storedRuleStates` at `:887`, nothing under any `src/` calls `runReplayAudit`, and `docs/ops/runbooks/**` is outside this fence, so the queue is one step long and this row does not shorten it. **TWENTY SEEDS, TWELVE WATCHED RED AND EIGHT WATCHED GREEN, EVERY GREEN A BLINDFOLD**, every one restored byte-identical under `sha256sum -c` with `git status --porcelain` empty after each, and **no restore used `git checkout --`**. **28 CITATION SITES SHIFTED BELOW AN INSERTION POINT AND NONE IS REPAIRED**: 27 in ADRs and session logs, the 28th in `docs/STATE.md` under a `## 2026-08-29 - Session 415` heading, so every one is a dated record and the citation-repair right does not reach one; the shift table is written down and was verified by reading at the far end rather than by arithmetic. **THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL**, the eleventh consecutive session to record it, and the clone landed exactly on the `e18ea41f` the row names. **NO `mcp__github__*` TOOLS, NO `gh` ON PATH, `api.github.com/repos/lam2-coder/meritfutures` ANSWERS 403 while `git push` AUTHENTICATES**; the branch is pushed and **no pull request was opened and nothing was merged, by instruction**. **No gate weakened, no test skipped, disabled or quarantined, no `CI-06` letter spent, no migration taken or reserved, no `status` field moved, no port wired, and `apps/**` edited only by eleven seeds every one of which was restored.** Counts derived at reporting time off each runner's own last line: suite **336 files / 8,082 passed / 10 skipped / 0 failed** against a base reproduced on `e18ea41f` BEFORE the first edit of **336 / 8,074 / 10 / 0**, a delta of **+8 cases in ONE EXISTING FILE entirely by ADDITION with zero removals**, files unmoved at 336 and skips unmoved at 10, attributed BY REMOVAL: `repo-invariants.test.ts` runs **265** alone here and **257** alone on the base. Gates **33 of 33** after `generate`, invariants **35 of 35**, typecheck, lint and `format:check` clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `380`: establish what [`RI-15`](../../packages/tooling/checks/repo-invariants.mjs) and `RI-16` actually cover as a measured statement rather than a description, name every blindfold including any fourth, decide whether the own-package hole can be fixed, and act on what [ADR-377](../decisions/ADR-377.md) section 9 leaves of `RI-35`.

**One objective.** It resolved into one measurement, two new blindfolds, one corrected cause, two measured refusals, eight cases and twenty seeds.

---

### 2. What was measured before the first edit

**THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL.** `/home/user` was empty, with no clone and no `node_modules`. **This is the eleventh consecutive session to record it.** The tree was attached to this session's authorized set and cloned by hand, and `HEAD` was confirmed to be the `e18ea41f` the row names rather than assumed.

**Base, on a clean tree, before anything was touched: 336 files / 8,074 passed / 10 skipped / 0 failed.** Gates **33 of 33** after `generate`, invariants **35 of 35**, typecheck, lint and `format:check` clean. `repo-invariants.test.ts` alone: **257**.

**THESE ARE NOT THE FIGURES THE LAST THREE ENTRIES REPORT**, because all three measured a tree on which the others had not yet merged. A row carrying either forward would have reported a delta against a tree that no longer exists.

---

### 3. The measurement, and why it is a perturbation rather than a description

**A `covers` LINE NAMES LEGS AND A READER OF A CITATION HAS ONE QUESTION: WOULD A WRONG LINE NUMBER BE REPORTED.** So every single-line citation in each check's real scope had its cited line replaced by every other line of the file it resolves to, and the checks' own legs were run on each.

**THE READER IS THE CHECKS' OWN**, imported from an export-added copy placed in the same directory so `REPO_ROOT` resolved and deleted afterwards. **The copy re-enters the `ri11` import cycle and had to be loaded second**, which that file's header predicts in terms.

**AND IT WAS HELD TO AN ORACLE BEFORE IT WAS BELIEVED**: the verdict function returns 0 findings at the true lines for both checks, which is what both checks return on this tree. [ADR-380](../decisions/ADR-380.md) section 1 is the table.

---

### 4. The two new blindfolds

**FOUR IS THE RANGE LEG.** [ADR-380](../decisions/ADR-380.md) section 3. The one leg that needs no name is off for every path that names more than one file, and the eight citations in `RI-15`'s scope guarded by nothing at all are exactly that set.

**FIVE IS `RI-16` HAVING NEITHER OF `RI-15`'s TWO OTHER LEGS**, priced here for the first time on this corpus at 101 plus 15. Section 4 of the entry.

**AND ONE STATED CAUSE DID NOT SURVIVE RE-DERIVATION**, which the dispatch asked for by name when it said to treat even a very recent measurement as a claim. Section 5 of the entry.

---

### 5. What was NOT done, and why

**NO LEG WAS ADDED TO EITHER CHECK.** Both remedies were written and run. Nearest-by-directory resolution raises three findings of which two are the check guessing, verified by opening both files; the negation repair reddens one honest citation in `apps/api/test/wiring.test.ts`, which this fence does not hold. **Refused on their own results and stated rather than taken quietly.**

**THE OWN-PACKAGE EXCLUSION IS NOT NARROWED**, and the entry says plainly that it cannot be. The argument against that refusal is written down before it is answered.

**`RI-35` IS NOT EXTENDED AND `absence-claims.mjs` WAS NOT OPENED FOR WRITING**, on [ADR-371](../decisions/ADR-371.md) section 5.2's ruling, which this row re-read and did not disturb. **What remains is obstacle 4 alone and it is one line split in a file outside this fence.**

**NO CITATION WAS REPAIRED.** All 28 sites this diff shifts are dated records.

---

### 6. Falsification

**TWENTY SEEDS, TWELVE WATCHED RED AND EIGHT WATCHED GREEN, every one restored byte-identical under `sha256sum -c` with `git status --porcelain` empty after each restore.** Eleven edited `apps/**`, two edited the checker itself. **No restore used `git checkout --`.** The table is [ADR-380](../decisions/ADR-380.md) section 11. Three worth keeping here:

- **Seed 4 is the one that produced a finding and it went the wrong way by design.** A past-end-of-file pointer inside a twelve-candidate path is GREEN where the same pointer with its path stated in full is RED. **The only variable is how many files the path names.**
- **Seeds 7b and 7c are one pointer read by two checks.** `apps/admin/src/index.ts:152` is a blank line: RED for `RI-15`, GREEN for `RI-16`.
- **Seed 12 corrected this entry's own text.** The consequence of the section 7 repair was computed first, and the computation named the wrong bound name and the wrong reason. The seed reports `subject` at 3 lines. **A computation is a claim.**

---

### 7. Blockers and landmines

- **LANDMINE: a green `RI-16` is a file-existence test over 94.4 percent of what it reads.** Measured. No later row should read that green as evidence a pointer was followed.
- **LANDMINE: `RI-15` and `RI-16` cannot see a citation whose path names more than one file drift at all.** Eight such citations exist today and one of them already points past the end of one of its own candidates.
- **LANDMINE, NARROWER: a path that WRAPS a line loses its directory prefix to the newline joiner.** Two sites, one resolving to the wrong file. Neither is in this fence.
- **OWED AND OUTSIDE THIS FENCE:** the five items in [ADR-380](../decisions/ADR-380.md) section 15, of which the `CRON_INVENTORY` line split is now named by a third consecutive row.
- **Concurrency: two sibling rows are running now**, one on `apps/api/**` and one on `apps/worker/**`. **Neither tree was opened for writing**; `apps/**` was read and seeded eleven times and every seed was restored. **`380` is this row's and no number is reserved.**

---

### 8. This session's own tooling, measured rather than assumed

```
$ command -v gh                                            -> not found (exit 1)
$ curl -s -o /dev/null -w '%{http_code}' \
    https://api.github.com/repos/lam2-coder/meritfutures   -> 403
```

**No `mcp__github__*` tool exists in this session's surface**, checked by search rather than by inspection of a list. **`git push` authenticates** through the session's git proxy and did so on every commit. **This matches sessions 554, 558, 562, 564, 565, 568, 569, 570, 571, 573 and 574 exactly.** The branch is pushed and **no pull request was opened, by instruction**.

**Every Bash command was run bare from the working directory**, per the row's operational note, and **no permission stall occurred**, which is the twelfth consecutive row to report that.

---

### 9. Verification, derived at reporting time

Every figure in the index block above was derived at reporting time off each runner's own last line. **`pnpm run verify` and `scripts/corpus/falsify.mjs` were NOT run**, both forbidden by the row for mutating the working tree.

---

### 10. Next

The founder's `E2` read of [ADR-380](../decisions/ADR-380.md) and the three decisions its approval block names, of which **the first is whether a green `RI-16` should keep the name it has** given what it now measurably asserts. Then **the negation repair with its price paid**, which needs a fence holding `wiring.test.ts` and `routes/payouts.ts` together. Then **the `CRON_INVENTORY` line split**, named by a third consecutive row and reachable by none of them.
