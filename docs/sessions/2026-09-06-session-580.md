---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 580: the repair ledger and the class census were two different sets, both were short, and the class they name is decided by opening a file rather than by a runner

<!--index: **THE ROW IS [ADR-385](../decisions/ADR-385.md), `proposed` and UNSIGNED. MONEY-ADJACENT, ADR-003 STRICT, `E2` READ OWED.** [ADR-382](../decisions/ADR-382.md) named a defect class, gave it TWO numbers, and said in writing it had not reconciled them. **THEY ARE TWO DIFFERENT SETS: intersection FOUR, union SEVEN.** The *three of five* is a REPAIR ledger and the *six* is a CLASS census, and they were never the same question, so that entry named TWO sites left while **FOUR were live**. **AND BOTH NUMBERS ARE SHORT.** Three further sites carry the same claim about the same subject: [`index.ts:532`](../../apps/worker/src/index.ts), where the precise claim degrades into the false consequence **seven lines apart inside one paragraph**, plus [ADR-370](../decisions/ADR-370.md) line 84 and [ADR-376](../decisions/ADR-376.md) line 92. **The class stands at TEN sites on `ADR-382`'s own counting convention, not six.** **THE TWO SITES IT LEFT FOR A CITATION BUDGET ARE REPAIRED AT A COST OF ZERO CITATIONS**, because its unstated assumption was that a repair ADDS lines: every edit here is line-count neutral, `git diff --numstat` reports equal insertions and deletions on all five source files, `expirySweepIo` is still at `expiry-adapter.ts:193`, and **every cited anchor was OPENED AND READ after the edit** rather than inferred from a green invariant run. **TWO OF `ADR-382`'s CARRIED FIGURES DO NOT REPRODUCE**: 36 citations across 10 distinct lines below [`index.ts:596`](../../apps/worker/src/index.ts) where it recorded 30 across 7, and 10 at [`expiry-adapter.ts:193`](../../apps/worker/src/sweeps/expiry-adapter.ts) where it recorded 8; the census was derived by driving `repo-invariants.mjs`'s OWN `citationsIn` and `citationReader` from a scratch copy, so the resolution is the checker's. **SIX SITES REPAIRED, retired clauses NAMED and never reproduced per [ADR-367](../decisions/ADR-367.md) section 4**, whose seed 3 watched a quoting retirement hand a prose-matching case two answers; the one retired FIGURE is written as a bare number so it cannot parse as a live citation. **ONE CORRECTION IS SHARPER THAN THE CLAUSE IT REPLACED**: `schedule.ts` read *the constructor CANNOT BE CALLED* and [`test/expiry-adapter.test.ts:599`](../../apps/worker/test/expiry-adapter.test.ts) calls it, green on this tree and on the base, so the line now reads NOTHING CALLS IT. **THE CLASS RECURS OUTSIDE `apps/worker` AND THAT IS THE QUESTION `ADR-382` COULD NOT REACH.** Shapes swept and counted at reporting time: 38 code sites and 150 documentary sites for *the only X in this repository*, 136 code sites for *nothing in this tree*, and **12 code sites for the mechanically decidable subfamily**. **FIVE WERE OPENED AT PRIMARY SOURCE AND THREE ARE FALSE AS WRITTEN**: [`playwright.config.ts`](../../playwright.config.ts) said `e2e/pass.ts:17` was the only file importing `@playwright/test` while **its own line 53 imports it**; [`packages/db/src/client.ts:4`](../../packages/db/src/client.ts) claims the only `pg` import while [`scoped-db.ts:75`](../../packages/db/src/scoped-db.ts) imports it **and `scoped-db.ts:4218` carries the true narrower claim in the very file that falsifies the absolute**; [`pg-boss-queue.ts:4`](../../packages/queue/src/pg-boss-queue.ts) says NAMES where its guard asserts IMPORTS, 32 files against one. **THE SEPARATOR IS NOT SUBJECT MATTER**: every absolute that survived a derivation is asserted by a named case, a lint rule or an invariant, and every false one is asserted by nothing. **The class also recurs on a different subject inside `apps/worker`**, [`recon/sweep.ts:150`](../../apps/worker/src/recon/sweep.ts) denying that any module writes `recon_blocked` while `:614` of the same file writes it and the corpus cites `:614` EIGHT times; repaired, and it runs in the OPPOSITE direction, so the mechanism is not direction-specific. **[ADR-380](../decisions/ADR-380.md) SECTION 15 ITEM 4 IS RULED: NEITHER wrapped path resolves to the wrong file under the shipped reader.** `pass.ts` is unique in the tree and `accounts.ts` is rescued by a line-count accident, 824 against 304 and 152; the premise is true only of the NEAREST-BY-DIRECTORY remedy that entry's own section 6 measured and REFUSED, and the restatement lost the qualifier. **THE LARGER DEFECT AT THAT SITE WAS NOT THE WRAP**: the comment attributes a direct quotation to `accounts.ts:824` and that sentence is at **`:846-848`**, 22 lines away, invisible because the citation carries no backticked name so `RI-15` asserts only that the file exists. Both paths written whole, the pointer repointed, both kept on the line `ADR-380` cites. **CHECKABILITY REFUSED WITH A MEASUREMENT AND NO INVARIANT MINTED OR NUMBER RESERVED.** `RI-14` reads EXPORTS and its own header excludes *exists but cannot be CONSTRUCTED*, which is every site here, and it covers THREE files, none of them a site; `RI-35` binds a sentence to an artifact whose ABSENCE a probe reports and every artifact here is PRESENT, the falsehood being in the quantifier. **What replaces a gate is a rule a reviewer can apply and a runner cannot**: an absolute in prose either NAMES the mechanism that asserts it or is written as the narrower claim that is true. **NOTHING IS BOUND MECHANICALLY AND THAT IS REPORTED RATHER THAN DRESSED UP**: this diff changes no executable line, so a test would be pinning prose, which `ADR-324`'s rule says goes stale in silence. **The standing bar is UNTOUCHED**: no port wired, no job scheduled, no sink passed, no default parameter added to `expirySweepIo`, no schedule parameter invented, no gate weakened, no test skipped, disabled or quarantined, no `CI-06` letter spent, no migration taken or reserved, and `packages/tooling/**`, `apps/api/**` and `docs/ops/runbooks/**` were never opened for writing. **NO SEED WAS PLANTED and section 5 says why**: a comments-only diff has nothing to watch fail, so a seed would have been theatre. **`383` and `384` are RESERVED naming no subject**, so `CI-06f` stays gapless with two siblings running. **THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL**, the twelfth consecutive session to record it, and the dispatch's instruction not to `cd` into the tree described a tree that was not there. Suite **336 files / 8,085 passed / 10 skipped / 0 failed** against a base reproduced on `0fc59a22` **BEFORE the first edit** of **336 / 8,085 / 10 / 0**, a delta of **ZERO on every axis**, which is the expected and correct result for a comments-only diff; gates **33 of 33** after `generate`, invariants **35 of 35**, typecheck, lint and `format:check` clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. The branch is pushed and **no pull request was opened and nothing was merged, by instruction**. -->

---

### 1. The objective

Row `385`: reconcile the two counts [ADR-382](../decisions/ADR-382.md) published for one defect class, repair the two sites it left, measure whether the class recurs outside `apps/worker`, settle [ADR-380](../decisions/ADR-380.md) section 15 item 4, and rule on whether the class is checkable.

**One objective.** It resolved into one reconciliation, six prose repairs, one corpus measurement and one refusal.

---

### 2. What was measured before the first edit

**THE CONTAINER ARRIVED WITH NO REPOSITORY.** `/home/user` was empty. The tree was attached to this session's authorized set and cloned by hand, and `HEAD` was confirmed to be `0fc59a22` rather than assumed. Twelfth consecutive session to record this.

**Base: 336 files / 8,085 passed / 10 skipped / 0 failed.** Gates **33 of 33**, invariants **35 of 35**, typecheck, lint and `format:check` clean. Not [ADR-382](../decisions/ADR-382.md)'s figures, which were measured on `e18ea41f` before rows `380`, `381` and `382` merged.

**The citation census was run BEFORE the first edit because it decided the shape of every repair**, and it was derived by driving the checker's own reader rather than by grepping. [ADR-385](../decisions/ADR-385.md) section 1 carries the table.

---

### 3. The reconciliation

The *five* and the *six* share four members. The five's fifth is `test/schedule.test.ts` case `6.1`; the six's fifth and sixth are `schedule.ts:263` and `test/breaker.test.ts:974`. **Union seven, intersection four.** Three further sites carry the same claim and neither number reaches them. [ADR-385](../decisions/ADR-385.md) section 3 carries the membership table.

---

### 4. What was repaired, and the technique that made it free

Six sites, every one line-count neutral, **zero citations moved**. The technique is the row's transferable output: [ADR-382](../decisions/ADR-382.md) priced the repair correctly and priced only the version that ADDS lines. [ADR-385](../decisions/ADR-385.md) section 4 carries the per-site table and section 11 carries the cost of the technique, which is that three repairs dropped a clause to fit a line budget.

---

### 5. Blockers and landmines

- **LANDMINE, the one worth carrying forward.** `RI-14` does not read any of these files. It covers three, all under `apps/api`, and its header excludes claims about constructibility by design. **A row told to repair something "under `RI-14`" is being told to obey a doctrine, not to expect a runner.** A green invariant run says nothing about whether these repairs are right.
- **LANDMINE.** The citation graph imposes a LINE BUDGET on prose. A comment above a heavily cited declaration can only be rewritten, not extended, without paying for pointer repairs across many files. Two rows have now paid different prices for this and neither has named it as a corpus problem.
- **LANDMINE.** `citationReader` resolves by path SUFFIX, then drops candidates the line number cannot reach. **An ambiguous citation can therefore be rescued by the line number alone**, which is what keeps `kyc-source.test.ts:42` pointing at the right file today. It is a margin, not a property.
- **BLOCKER, unchanged.** [ADR-370](../decisions/ADR-370.md) is in no fence anybody has dispatched. Fifth consecutive row to record it, and this row adds `ADR-376` line 92 to what is owed there.
- **NO SEED WAS PLANTED.** This diff changes no executable line, so there was nothing to observe failing. The one scratch artifact, a copy of the checks package used to drive the citation reader, was deleted and `git status --porcelain` confirmed empty before any gate was run. **No restore was done with `git checkout --` because nothing tracked was ever modified for a seed.**

---

### 6. Verification, derived at reporting time

- Suite **336 files / 8,085 passed / 10 skipped / 0 failed**, against a base of **336 / 8,085 / 10 / 0** reproduced on `0fc59a22` before the first edit. **Delta zero on every axis**, attributed by inspection: the diff is comments only.
- Gates **33 of 33** after `node scripts/corpus/gates.mjs generate`. Invariants **35 of 35**. `typecheck`, `lint`, `format:check` clean.
- **`pnpm run verify` and `scripts/corpus/falsify.mjs` NOT run**, both forbidden by the row because both mutate the working tree.
- Every cited anchor in every edited file was opened and read after the edit.

---

### 7. Files touched

`apps/worker/src/sweeps/expiry-adapter.ts`, `apps/worker/src/index.ts`, `apps/worker/src/schedule.ts`, `apps/worker/src/recon/sweep.ts`, `apps/worker/test/breaker.test.ts`, `apps/portal/test/kyc-source.test.ts`, `playwright.config.ts`, `docs/decisions/ADR-385.md` (new), `docs/decisions/ALLOCATION.md`, `docs/decisions/README.md`, `docs/sessions/README.md`, `docs/sessions/2026-09-06-session-580.md` (new), `docs/STATE.md`.

---

### 8. Next

The founder's `E2` read of [ADR-385](../decisions/ADR-385.md) and the three decisions its approval block names, of which **the first is whether a third consecutive refusal to gate a measured defect class is discipline or a pattern**. The concrete alternative on offer is a check over the 12-site decidable subfamily, which section 11 flags as the argument this entry is weakest against. Then the five items in section 9 that no fence dispatched so far can reach, of which item 1 is two words.
