---
status: approved
depends_on: [README.md]
last_updated: 2026-08-29
---

## 2026-08-29 - Session 393: the screen is ready and the source is not, and the wire's gap vocabulary has no figure on this page to speak about yet

<!--index: **THE SCREEN IS READY AND THE SOURCE IS NOT, WHICH IS THE ARRANGEMENT THE DISPATCH PROVIDED FOR AND THE ONE THE MEASUREMENT SUPPORTS.** `readLiability` is still not one of `IMPLEMENTED_ADMIN_READS`' **five**, re-derived at reporting time, so the root route renders its honest unavailable state and its `P5-l` blocker now recites the register rather than a WAVE-06 line count, BOUND by `M6-A-86` to the array it recites. **WHAT LANDED IS THE JOIN NOBODY HAD WRITTEN**: `apps/admin/src/liability-read.ts`, which narrows `GET /admin/liability`'s body structurally against `api/types.ts`, enforces `ADR-203` ruling 2 ON THE READING SIDE in both directions, and projects what survives into `page.ts`'s input. One response builds a whole `LiabilityHomePage` and the bytes carry the figures. **THE CONSOLE'S ABSENT AND THE WIRE'S NULL DO NOT DISAGREE AND THEY ALSO DO NOT YET MEET**: both nullable sites, `payout_velocity` and `per_plan[].cusum`, are `P-M6-04` and `P-M6-06`, which `page.ts` holds in `PENDING` and for which `LiabilityHomeInput` has no member, so `ADR-203`'s mechanism is total and has no figure on THIS page. **THREE THINGS THE PROJECTION REFUSES TO INVENT, EACH A MEASUREMENT AND TWO OF THEM NEEDING A RULING**: the third component has no column, `eligible_next_7d` carries the FIGURE and no AS-OF while `INV-M6-04` requires one, and two of `P-M6-09`'s five trust inputs are on the wire as FIGURES and neither as a STATE. **`reserve.breaker_armed` IS A SECOND ANSWER TO A QUESTION `0049` DELIBERATELY STORES NO COLUMN FOR**, so a body whose flag and ratio disagree is REFUSED rather than half rendered. **Eleven seeded defects, each watched failing and reverted from a byte-identical copy with the hash checked both ways; seed 5 fired one case, was strengthened, and fired two on the retake.** **243 files / 6,047 passed / 6 skipped** against a `main` baseline of **242 / 6,002 / 6** reproduced exactly at `028143c` before a line changed; `apps/admin` from 15 files / 426 cases to **16 / 471**. 33 of 33 gates, **17 of 17** invariants (`DISPATCH_PROTOCOL` section 4 says 16 and is one behind `RI-18`), `falsify` clean read at its output, typecheck 0, lint 0, format clean, `@merit/admin` **six routes**, unmoved. **No ADR number, no migration number, no `SD-nn`, no `apps/api` or `packages/` file touched, `api/types.ts` not opened, `liability.ts`, `figure.ts`, `live-liability.ts` and `page.ts` not opened.** -->

**Objective.** Make the admin liability dashboard read the live source, or render the honest unavailable state and say exactly why.

**It renders the honest unavailable state, and the "exactly why" is a source that is not composed rather than a screen that is not built.** What this session added is the half between the wire and the page that nobody had written, so that "the day a supplier exists, what changes is one function body" is a sentence with a named function behind it instead of a promise.

### The measurement first, because the dispatch asked for it twice

| Claim | Where it was checked | Result |
|---|---|---|
| The liability dashboard's route is `/` | `pnpm --filter @merit/admin build`, then the page file itself | **TRUE.** `/` renders `LiabilityHomeRoute` from [`app/page.tsx`](../../apps/admin/src/app/page.tsx), which renders the unsupplied arm |
| The build prints five routes | the same build | **SIX**, and the sixth is not this diff's: `/search` landed in session 375. `/`, `/accounts/[accountId]`, `/feed`, `/flags`, `/identities/[identityId]`, `/search` |
| `readLiability` is not one of `IMPLEMENTED_ADMIN_READS` | [`admin-source/index.ts`](../../apps/api/src/admin-source/index.ts), read at reporting time | **TRUE, and the count is five**: `listEvents`, `listFlags`, `readAccount`, `readIdentityGraph`, `searchAccounts` |
| Nothing can mint the admin session cookie | a grep for `setAdminSessionSource` outside `test/` | **TRUE.** One declaration, no non-test caller |
| `repo-invariants.mjs` reports 16 of 16 | the runner, on the merge base, before a line changed | **REFUTED. 17 of 17.** `RI-18` landed with `ADR-203` and [`DISPATCH_PROTOCOL`](../DISPATCH_PROTOCOL.md) section 4's table is one behind. Not repaired here: that file is approved and outside this fence |
| Session 364's debt 5, that `LiabilityResponse` carries one of the five figures the page reads | [`api/types.ts`](../../apps/admin/src/api/types.ts) | **ALREADY CLEARED**, by `ADR-188` and session 363. All four of the other figures are declared, and `app/page.tsx` had already recorded the clearing and retired the blocker |

### Done

- **THE JOIN NOBODY HAD WRITTEN.** [`liability-read.ts`](../../apps/admin/src/liability-read.ts) is the one place that turns a `GET /admin/liability` body into a `LiabilityHomeInput`. `page.ts` assembled the page from an input and `api/types.ts` transcribed the body, and nothing connected them: an adapter written today would have had to invent the narrowing at the route, which is a second answer to what the response means in every screen that ever reads it.
- **THE NARROWING IS STRUCTURAL AND IT REBUILDS RATHER THAN CASTS.** [`http/client.ts`](../../apps/admin/src/http/client.ts) returns `unknown` and argues the refusal at the point it declines to narrow: "a type parameter a caller supplies is a cast the compiler cannot check ... Narrowing is the screen's". A predicate returning `body is LiabilityResponse` hands back the original object, so a field the server added travels on unnamed; `narrowLiabilityResponse` assembles a value field by field out of what it read, and a case asserts an undeclared field does not survive.
- **`ADR-203` RULING 2 IS ENFORCED ON THE READING SIDE, IN BOTH DIRECTIONS, AND THE DUPLICATION IS THE POINT.** `assertLiabilityGapsPaired` runs inside `projectLiability` on the producer, and `ADR-203` section 10 finding 3 records that it "is declared and watched and is on no served path". A console whose only defence against a bare `null` is a guard that does not run has no defence. The same four refusals are held here over the bytes this page received: the bare null, the gap over a present figure, the duplicate path, and `awaiting` non-`null` exactly when the cause is `awaiting_dependency`, with `detail` blank refused on a `.trim()`.
- **THE CLOSED VOCABULARY IS EXHAUSTIVE IN BOTH DIRECTIONS HERE, WHICH `as const satisfies` IS NOT.** `ADR-179`'s device, which `ADR-203` section 6 takes, refuses a member the union does not have and CANNOT SEE a member the union gained: an array of two still satisfies a union of three. `WIRE_GAP_CAUSES` is the key set of a `Record` keyed by the union indexed off `LiabilityResponse` itself, so a fourth cause minted in `admin-reads.ts` is a type error in this console rather than a `default` arm nobody wrote. A case also reads `LIABILITY_GAP_CAUSES` out of the producer and asserts the two sets are equal.
- **A BODY THAT DISAGREES WITH ITSELF ABOUT THE BREAKER IS REFUSED RATHER THAN HALF RENDERED.** `reserve.breaker_armed` is on the wire and `ReserveCoverageSnapshot` has no member for it, because `0049_reserve_coverage_snapshots.sql` deliberately stores the ratio and not the arming: "storing it would recreate in one column exactly the drift item 1 removes from another". So this console has two answers and one place to put them. Dropping the flag silently renders a breaker state the server did not send; taking the flag puts the drift back. The row is refused, which is what `assertRatioIsFromReserveAlone` already does one function over for the numerator, and a case fires on the dangerous direction: a breaker shown disarmed beside a ratio under 1.0 on one screen.
- **THE SEAM HAS THREE ARMS AND NEVER THROWS.** `readLiabilityHome` takes one `AdminApiResult` and answers `supplied`, `failed` or `refused`. The third is the one a two-arm union loses: a transport failure and a 200 carrying a body this console refuses are different events with different remedies, and collapsing them would tell an operator the console cannot reach a service it just reached. It takes a result rather than fetching one, so the package's one `fetch(` call site and `M6-A-41`'s refusal of a network call under `src/app/` both stay true.
- **THE ROOT ROUTE'S `P5-l` BLOCKER RECITES THE REGISTER, AND `M6-A-86` BINDS IT.** It cited a `WAVE-06` line count and a wave-relative fence sentence. It now names the five methods `IMPLEMENTED_ADMIN_READS` holds and says `readLiability` is not among them, and a case reads that array out of `apps/api` at run time and fails when the recital and the register stop agreeing. **It also says plainly that the console half is not what is missing**, because sessions 368 and 375 were both dispatched to build screens that already existed and a reader who concludes the screen is unbuilt rebuilds it.

### The three things the projection refuses to invent

**A projection that fills every optional member of `LiabilityHomeInput` renders a fuller page than the response can pay for**, and `AS-M6-04` is the failure a chosen value produces: a confident number with a source citation attached.

| Member | Why it stays unsupplied | Whose |
|---|---|---|
| `snapshot.withdrawalsInFlight` | **NO COLUMN AND THEREFORE NO FIELD.** `ADR-195` section 6 row 1. `theThreeNumbers` renders it ABSENT with that reason and marks the total INCOMPLETE, which is session 364's behaviour and was not redesigned | already ruled |
| `eligibleNextSevenDays` | **THE RESPONSE CARRIES THE FIGURE AND NOT ITS AS-OF.** Finding 2 below | **needs a ruling** |
| `trustSignals` | **TWO OF `P-M6-09`'s FIVE INPUTS ARE ON THE WIRE AS FIGURES AND NEITHER AS A STATE.** Finding 3 below | **needs a ruling** |

`live` is a fourth absence and it is a COMMITMENT rather than a gap: `API_CONTRACT` fixes that the live figure "is never a field on `GET /admin/liability`", because that response is the one an operator opens during an incident.

### Findings not in the dispatch

1. **THE CONSOLE'S ABSENT AND THE WIRE'S NULL DO NOT DISAGREE, AND THEY DO NOT YET MEET.** The dispatch asked whether `figure.ts`'s ABSENT-with-reason and `ADR-203`'s null-plus-reason mean the same thing. They do, and section 5's mapping is exact: every `gaps` entry becomes one `absent()` call with `detail` as the `reason`. **AND THERE IS NO FIGURE ON THIS PAGE WHERE BOTH SPEAK.** The response has exactly two nullable sites, `payout_velocity` and `per_plan[].cusum`; they are `P-M6-04` and `P-M6-06`; `page.ts` holds both in `PENDING` and `LiabilityHomeInput` declares no member for either. So the wire's mechanism is total, correct, and silent on this page, and every absence the liability home renders today comes from a field the wire carries unconditionally or from a field it does not carry at all. **This is a measurement rather than a defect and it is not repaired**: giving the page a `P-M6-04` input is a panel `page.ts` holds in `PENDING` with a supplier named, not a renderer change.

2. **`eligible_next_7d` CARRIES THE FIGURE AND NO AS-OF, AND `INV-M6-04` MAKES THAT UNRENDERABLE RATHER THAN UNTIDY.** `EligibleNextSevenDays.asOfInstant` is required and the only instant on the body that could fill it is the top-level `as_of`, which is `liability_snapshots`' clock. `api/types.ts` argues that exact move down one field: `reserve` carries its own `as_of` "BECAUSE IT IS A DIFFERENT TABLE ON A DIFFERENT CLOCK", and dating one group with another's puts "the book's clock on the rail's figure". `P-M6-03` is `DEP-M6-01`'s forecast and is not the snapshot. **`ADR-203` section 7a already names this field as a third figure needing that entry's shape and section 9 explicitly leaves `GET /admin/eligible-forecast`'s body to `ADR-166` clause 3.** **REPORTED AND NOT REPAIRED**: the repair is a field on a frozen contract, and the alternative, dating a forecast with a snapshot's clock, is the one this console may not take.

3. **TWO OF `P-M6-09`'s FIVE TRUST INPUTS ARE ON THE WIRE AS FIGURES AND NEITHER CARRIES A STATE.** `api/types.ts` maps `integrations.recon.mismatches_open` to `recon_mismatches_open` and `integrations.batch.last_success_at` to `batch_last_success`, and `TrustSignal` requires a `state` that `data-trust.ts` defines as the input "AS ITS SUPPLIER REPORTED IT". Neither is reported. Deriving one needs a boundary no document fixes: the count above which the estate is distrusted, and the age above which a batch is late. `page.ts` refuses exactly this move for staleness in its own words, "picking the boundary would invent a control nobody chose", and `INV-M6-12` refuses it for an alarm. **So the projection supplies NO trust signal and the verdict is red on five missing rather than green on two invented**, which is `assessDataTrust`'s documented behaviour and names an owner for each of the five. **REPORTED AND NOT REPAIRED, and it is one of two rulings**: the response grows a state beside each figure, or a document fixes the two boundaries. Either is `API_CONTRACT` or a panel definition.

4. **`reserve.breaker_armed` IS ON THE WIRE AND NO TABLE STORES ONE.** `0049` argues that the arming is a rendering rather than a stored fact and refuses the column; `INV-M6-12` says no client recomputes an alarm. The response carries both the ratio and the arming, so one of the two rules is going to be broken by any client that renders the panel. This session took the third answer, refusing a disagreement, which keeps both rules unbroken for a coherent body and says nothing about whether the field should be there. **REPORTED**: whether `GET /admin/liability` should carry an arming at all is `API_CONTRACT`'s question and this fence holds no ADR number.

5. **`DISPATCH_PROTOCOL` SECTION 4's INVARIANT COUNT IS ONE BEHIND.** It states 16 of 16 and the runner prints 17 of 17 on the merge base with nothing of this session's applied. `RI-18` landed with `ADR-203`. **REPORTED AND NOT REPAIRED**: that document is `approved` and is not in this fence.

6. **A `WAVE-06` LINE COUNT WAS REMOVED RATHER THAN RE-DERIVED.** The `P5-l` blocker cited "1,764 lines of console code". This session could not reproduce the count's definition from the document, so it took the citation out and replaced the sentence with a measurement a reader can check at any time, which is the register's five names. **Removing an unreproducible number is not the same as refuting it** and this entry says which was done.

### What is left

- **THE ADAPTER.** `AdminReadSource.readLiability` has no implementation, so `setAdminReadSource` stays on `wiring.test.ts`'s BLOCKED list. `apps/api/**` was not this fence and session 392 held `apps/api/src/admin-source/**` while this ran.
- **THE OPERATOR SESSION.** `ADR-171`'s blocker, unmoved and re-derived: no table holds an operator, a role or an operator session, and nothing calls `setAdminSessionSource` outside a test.
- **THE THREE MEMBERS THE PROJECTION LEAVES UNSUPPLIED.** One is ruled and rendering correctly; the other two are findings 2 and 3 and each needs a ruling before a line of console code changes.
- **THE FOUR PANELS `page.ts` HOLDS IN `PENDING` WHOSE FIGURES ARE NOW ON THE WIRE.** `P-M6-04`, `P-M6-05`, `P-M6-06` and `P-M6-08` all have fields on `LiabilityResponse` and no member of `LiabilityHomeInput`. Their `blockedBy` reasons say no module supplies the input, which is still literally true because nothing composes the read, and each becomes false on the day one does. **Not repaired here**: the repair is a page input per panel, which is four panels rather than a sentence.

### Landmines

1. **A ZERO DEFAULT ON THE THIRD COMPONENT FIRED ONE CASE, AND THE ONE IT FIRED WAS TWO FUNCTIONS FROM THE PAGE.** Seed 5 supplied `withdrawalsInFlight: { cents: 0n }` from the projection and turned exactly one case red, in the arithmetic rather than in the bytes an operator reads. The case was strengthened to assert the INCOMPLETE clause in the rendered lines and the seed was retaken: two red. **A seed that fires narrowly is the same warning as one that does not fire.**
2. **`Number.isInteger` IS NOT `Number.isSafeInteger`, AND ONLY ONE OF THEM CATCHES THE FAILURE THAT MATTERS.** Both refuse a fractional cents value. Above `Number.MAX_SAFE_INTEGER` a JSON number has ALREADY lost digits by the time the narrowing sees it, so `BigInt` would faithfully widen a value that is already wrong. Seed 3 weakened the check and one case fired.
3. **A `Record` KEYED BY A UNION AND AN ARRAY WITH `as const satisfies` ARE NOT THE SAME GUARD.** The second cannot see a member the union GAINED. `ADR-203` section 10 finding 1 measures a related blindness at seven paths; this is a different one, in the client, and the fix costs one object literal.
4. **THE PROJECTION TAKES A ROLE AS A STRING AND CHECKS NOTHING.** `requireAdminRole` and `mayReadLiabilityHome` run inside `buildLiabilityHome`, one call later, and a case asserts a non-role reaches the page builder and is refused there. A second copy of the closed role set in this module would be a second answer to what an admin role is.
5. **A NARROWING FAILURE IS A BODY A PRODUCER SENT AND NOT A FAULT IN THIS CONSOLE.** `readLiabilityHome` catches it and returns `refused` with the message. An unhandled throw on the console's ROOT route tells an operator the console is broken when the truth is that one field was wrong, and it does so on the screen they opened during the incident.

### Verification

- `pnpm install` first. **`pnpm vitest run`: 243 files / 6,047 passed / 6 skipped**, against a `main` baseline of **242 / 6,002 / 6** reproduced exactly at `028143c` before a line changed. `apps/admin` goes from 15 files / 426 cases to **16 / 471**: 42 new cases in [`liability-read.test.ts`](../../apps/admin/test/liability-read.test.ts) and 3 in [`render.test.ts`](../../apps/admin/test/render.test.ts).
- `node scripts/corpus/gates.mjs generate` then `check`: **33 of 33**. `node packages/tooling/checks/repo-invariants.mjs`: **17 of 17**, `RI-18` included, re-run after the last edit to every file.
- `pnpm run typecheck` 0, `pnpm run lint` 0, `pnpm run format:check` clean.
- `pnpm --filter @merit/admin build`: **six routes**, unmoved by this diff.
- `node scripts/corpus/falsify.mjs`: read at its OUTPUT rather than its exit code. *"All 33 gates pass clean and fail dirty, 80 scope case(s) hold, and 10 loader case(s) land on the side of the CI-03 boundary they name."* `git status` clean afterwards.
- **`pnpm run verify` was NOT run**, per `DISPATCH_PROTOCOL` section 4.
- The barrel surface goes from **74 names over 8 modules to 86 over 9**, derived by `B.7` from what the modules declare rather than transcribed.

### The eleven seeds, each applied to the committed shape and reverted from a byte-identical copy

`git status --porcelain` empty after every one, and the `sha256` compared both ways.

| # | Seed | Result |
|---|---|---|
| 1 | the bare-`null` refusal disabled | **2 red** |
| 2 | the gap-over-a-present-figure refusal disabled | **1 red**, so both directions are live |
| 3 | `Number.isSafeInteger` weakened to `Number.isInteger` | **1 red** |
| 4 | `assertArmingAgrees` not called | **2 red** |
| 5 | the third component defaulted to `0n` in the projection | **1 red**, then **2 red** on the retake. Landmine 1 |
| 6 | `eligibleNextSevenDays` dated with the snapshot's clock | **1 red** |
| 7 | a trust state derived from a boundary nobody ruled | **1 red** |
| 8 | the blank-`detail` check loses its `.trim()` | **1 red** |
| 9 | `./liability-read.ts` dropped from `ADMIN_BARREL_LEGS` | **4 red** |
| 10 | the rebuild replaced by a cast past the key type | **6 red** |
| 11 | one composed method name dropped from the served blocker prose | **2 red** |

### Blockers

None hit. The fence held: `apps/api/**` and `packages/**` were READ and not touched, [`api/types.ts`](../../apps/admin/src/api/types.ts) was read and not opened for edit so `RI-18` needed no re-run beyond the standard one, and [`liability.ts`](../../apps/admin/src/liability.ts), [`figure.ts`](../../apps/admin/src/figure.ts), [`live-liability.ts`](../../apps/admin/src/live-liability.ts) and [`page.ts`](../../apps/admin/src/page.ts) were read and not edited. Session 392's fence, `apps/api/src/admin-source/**` and `apps/api/test/**`, was not entered and its work was not waited on.

### Amended after the merge with `origin/main` at `0fa118a`

**`main` MOVED WHILE THIS BRANCH WAS OPEN AND THE MERGE IS TAKEN RATHER THAN WAITED ON.** [#520](https://github.com/lam2-coder/meritfutures/pull/520) landed, which is session 392: the session this dispatch said was composing `readLiability` while this one ran. **Two conflicts, both in append-only registries and both resolved keep-both**, ordered by session number: [STATE](../STATE.md) and this directory's [README](README.md), where 392's entry and this one were appended to the same place. `gates.mjs generate` agrees with the resolved README rather than with either side's copy. No code file conflicted, and 392's three files are [`admin-source/liability.ts`](../../apps/api/src/admin-source/liability.ts) and two of its suites, none of which this fence holds.

**THE CLAIM THIS ENTRY IS BUILT ON WAS RE-DERIVED ON THE MERGED HEAD RATHER THAN CARRIED, AND IT HELD.** `IMPLEMENTED_ADMIN_READS` still names `listEvents`, `listFlags`, `readAccount`, `readIdentityGraph` and `searchAccounts`, and **`readLiability` is still not among the five**. Session 392's own STATE section says so in its first sentence: *"`readLiability` IS NOT COMPOSED AND THE DISPATCH'S CENTRAL PREMISE DID NOT SURVIVE THE CHECK."* So the root route's `P5-l` blocker stands unchanged, and `M6-A-85` and `M6-A-86`, which read that array at run time rather than quoting it, are green on the merged head. **Those two cases are the mechanism by which this would have been caught the other way**: had 392 composed the read, both would have gone red on this merge and the blocker would have come out rather than sitting stale.

**392 REPORTED A FINDING THAT NARROWS ONE OF THIS ENTRY'S, AND IT IS RECORDED RATHER THAN FOLDED IN.** Its STATE section reports that [ADR-203](../decisions/ADR-203.md)'s absence shape *"costs `RI-18` its sight inside every field it applies to"*, because a union is not reduced to a field set. That is the same blindness [ADR-203](../decisions/ADR-203.md) section 10 finding 1 quantified at seven paths, measured again from a second direction. **It does not move anything in this diff**: `RI-18` binds three declarations of `LiabilityResponse` and this session edited none of them, and the repair is `packages/tooling`, which is nobody's this wave.

**EVERY GATE RE-RUN ON THE MERGED HEAD**, not inherited from before it: 33 of 33 gates, 17 of 17 invariants, typecheck 0, lint 0, format clean, `falsify` clean read at its output with the tree clean afterwards, `@merit/admin` six routes unmoved, and **244 files / 6,063 passed / 6 skipped**. The move from **243 / 6,047 / 6** is +1 file and +16 cases, all of them 392's.

### Files touched

[`apps/admin/src/liability-read.ts`](../../apps/admin/src/liability-read.ts) (new), [`apps/admin/test/liability-read.test.ts`](../../apps/admin/test/liability-read.test.ts) (new), [`apps/admin/src/index.ts`](../../apps/admin/src/index.ts), [`apps/admin/src/app/page.tsx`](../../apps/admin/src/app/page.tsx), [`apps/admin/test/service.test.ts`](../../apps/admin/test/service.test.ts), [`apps/admin/test/render.test.ts`](../../apps/admin/test/render.test.ts), [STATE](../STATE.md) (append), this file and its [README](README.md) row.
