---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 563: two merged entries rule opposite ways on one rule, and both located the arms in the member list where the arms are in the return type ([ADR-368](../decisions/ADR-368.md), proposed)

<!--index: **THE ROW IS [ADR-368](../decisions/ADR-368.md), `proposed` and UNSIGNED. MONEY-ADJACENT (auth, admin), `E2` READ OWED.** Two merged entries written four hours apart disagree about a general rule. [ADR-360](../decisions/ADR-360.md) ruled that a half-install of `setAdminSessionSource` cannot exist because *"That shape needs at least two members"*; [ADR-364](../decisions/ADR-364.md) ruled the claim does not carry to a four-member port and, in the same clause, ENDORSED the reason. **RULED: BOTH ARE RIGHT ABOUT THEIR OWN PORT'S DISPOSITION AND THE GENERALISATION THEY SHARE IS FALSE, and it is false about the port it was first stated about rather than only about the transfer.** `AdminSessionSource` declares ONE member whose return type is a closed THREE-arm union, and driven through the real `adminHandler` the arms answer **401, 403 and 503**; one installed object, well-typed with no cast, answers 401 to one token and 503 to another, which is ADR-356's shape on the port ruled incapable of presenting it. **ADR-360's OWN SEED TABLE recorded an arm of that single member reddening two cases and the finding never reached its conclusion.** The deciding property is members TIMES the arms of each member's return type; the sufficient direction is left UNMEASURED rather than assumed. **The arity-blind half of ADR-356 carries and ADR-360 applied it correctly.** Both dispositions stand, neither frozen entry is amended, six executed sites carry the refuted reason in test NAMES and comments with every assertion beneath them true, and one item ADR-364 left owed was not collected by [ADR-367](../decisions/ADR-367.md). -->

---

### 1. The objective

Row `368`: read both merged entries at their primary sources, determine mechanically what *"the reason is the interface"* means, and rule which is right or rule that the generalisation is what is false.

**One objective.** It resolved into one ruling, nine executed assertions, four falsification seeds and a census of what rests on the refuted version.

---

### 2. What the disagreement turned out to be

**IT IS NOT TWO RIVAL ANSWERS. IT IS ONE ANSWER ASSERTED TWICE.**

| entry | its clause | what it does with the member-count reason |
|---|---|---|
| [ADR-360](../decisions/ADR-360.md) `:31` | *"That shape needs at least two members."* | states it |
| [ADR-364](../decisions/ADR-364.md) `:32` | *"That ruling was correct about a ONE-member port."* | **endorses it**, then flips the conclusion for its own four-member port |

**ADR-364 DID MAKE ONE REAL CORRECTION AND IT IS NOT THE ONE ITS TITLE ADVERTISES.** `ADR-360.md:31` gave two reasons, member count and the `null` default. ADR-364 retired the second, *"The default is not the install"*, correctly: its own port also defaults to `null`, so that reason would have proved the false thing about it. **It kept the other reason and this row is the first thing to test it.**

---

### 3. What was measured

| what | where | answer |
|---|---|---|
| `AdminSessionSource` members | `admin-reads.ts:235-237`, sliced | **ONE**, `lookup` |
| `InternalOpsSource` members | `internal.ts:807-812`, sliced | **FOUR** |
| `AdminSessionLookup` arms | `admin-reads.ts:207-210`, sliced | **THREE** |
| the `unknown` arm | driven through `adminHandler` | **401** |
| the `not-an-operator` arm | same | **403** |
| the `operator` arm, role admitted | same | **503**, the read source is consulted and is null |
| one source, two tokens | same | **401 and 503**, a live arm beside a refusing one |

**THE SENTENCE ADR-360 NEEDED WAS FOUR LINES ABOVE THE INTERFACE IT SLICED.** `admin-reads.ts:194` reads *"THREE ARMS AND NOT TWO, and the third is what makes API_CONTRACT section 12's `/admin/*` row implementable."*

**AND THE `operator` ARM TYPECHECKS WITH NO CAST BY THE MODULE'S OWN RULING.** `AdminPrincipal.role` is a plain `string` (`:185-189`) deliberately, so that the refusal lives at `resolveAdminRole` rather than in a cast. Seed 2 confirms it: a role outside `ADMIN_ROLES` answers 403 instead of 503.

---

### 4. What landed

| file | what |
|---|---|
| [`apps/api/test/half-install-arity.test.ts`](../../apps/api/test/half-install-arity.test.ts) | **NEW.** Nine cases: the two slicers and their fixtures, the three arms of one member on the wire, the mixed-arm install, and the arity-blind channel loss. **The only file this row wrote outside `docs/`** |
| [`docs/decisions/ADR-368.md`](../decisions/ADR-368.md) | **new.** The ruling, `proposed`, UNSIGNED |
| [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) | row `368` taken |
| `docs/decisions/README.md`, `docs/INDEX.md`, `docs/STATE.md`, `docs/sessions/README.md` | registry rows, the generated ones by `gates.mjs generate` |

**NO `src/` FILE WAS OPENED FOR WRITING IN EITHER WORKSPACE.** `apps/api/test/wiring.test.ts` and `apps/worker/src/schedule.ts` belong to siblings this wave and were read only.

---

### 5. What rests on the refuted version

**THE [ADR-358](../decisions/ADR-358.md) SHAPE, CONFIRMED RATHER THAN ASSUMED: ADR-364 repaired the clause it was dispatched about and INHERITED the rest**, carrying the member-count reason into its own executed suite as a premise.

**SIX EXECUTED SITES, AND NOT ONE IS A FAILING ASSERTION.**

| site | carries |
|---|---|
| `apps/api/test/admin-session-source.test.ts:328` | the test NAME, *"so there is no partial install"* |
| `apps/api/test/admin-session-source.test.ts:329-333` | the comment deriving it |
| `apps/api/test/internal-ops-source.test.ts:352` | the test NAME |
| `apps/api/test/internal-ops-source.test.ts:354-355` | *"THAT IS A FACT ABOUT THAT INTERFACE"*, the sharpest single site |
| `apps/api/test/internal-ops-source.test.ts:7-9` | the same premise in the file header |
| `apps/api/test/internal-ops-source.test.ts:325` | the section heading |

**EVERY ASSERTION BENEATH THEM IS TRUE AND WAS RE-DERIVED GREEN.** What propagated is the REASON, in names and comments, where nothing asserts it and nothing can redden.

**NINE REGISTER AND JOURNAL SITES** restate one of the two titles: `decisions/README.md:374` and `:378`, `ALLOCATION.md:352` and `:356`, `STATE.md:12786`, `:12814`, `:12818`, both session files and both `sessions/README.md` rows. **These are generated spans derived from frozen headings and must not be repaired.**

**AND ONE ITEM ADR-364 LEFT OWED WAS NOT COLLECTED BY [ADR-367](../decisions/ADR-367.md).** `wiring.test.ts:1415-1418` still ends the `setInternalOpsSource` entry with *"this port's OWN docblock refuses it harder"*. ADR-367 collected rows 360 to 363; row 364's item was not in its enumeration. **Confirmed independently, reported, not repaired: the file is a sibling's.**

---

### 6. `RI-14`, and this row's own defect

**NOTHING IS DELETED.** Both entries stay merged and unamended, every site above stays where it is, and this row is additive.

**IT FOLLOWS [ADR-367](../decisions/ADR-367.md)'s REMEDY FOR THE COLLISION [ADR-358](../decisions/ADR-358.md) PREDICTED.** The executed file NAMES the retired figure and spells neither refuted sentence into a string, so a prose-matching case is not handed two answers.

**THIS ROW'S OWN SLICER DEFECT IS KEPT AS A FIXTURE.** The union slicer's first draft took the first `;` after the alias, and the first `;` in `AdminSessionLookup` sits INSIDE the `operator` arm separating `kind` from `principal`, so it sliced one arm of three. **It is the mirror of the defect ADR-360 records in its own member slicer**, a delimiter read at the wrong nesting depth, once running past the terminator and once stopping short. **It was caught by the case failing while every wire case was already green**, which is the count being wrong in the same file whose measurement was right.

---

### 7. Verification

| gate | result |
|---|---|
| `node scripts/corpus/gates.mjs generate` then `check` | **33 of 33** |
| `node packages/tooling/checks/repo-invariants.mjs` | **35 of 35** |
| `pnpm vitest run` | **332 files, 8,028 passed, 10 skipped, 0 failed** |
| `pnpm run typecheck` | clean |
| `pnpm run lint` | clean |
| `pnpm run format:check` | clean |

**BASELINE REPRODUCED BEFORE THE FIRST EDIT** at `8ff3a2a4`: **331 files, 8,019 passed, 10 skipped, 0 failed**. Delta **+1 file, +9 passed**, attributable by removal: deleting the one new file returns the suite to baseline exactly.

**FOUR SEEDS, EVERY ONE WATCHED RED AND RESTORED BYTE-IDENTICAL under `sha256sum -c`**: the mixed-arm source collapsed to one arm (**RED**, 1); the `operator` arm carrying a role outside `ADMIN_ROLES` (**RED**, 3, answering 403 rather than 503); the union slicer taking the first `;` (**RED**, 3, reporting three arms as one); block comments not skipped during the member scan (**RED**, 1, ADR-360's recorded defect reproduced).

**ONE SEED NOT RUN AND NAMED RATHER THAN GLOSSED.** Changing `authorizeAdmin`'s branches would falsify section 4 directly, and `apps/api/src/` is outside this row's fence. No seed was attempted there. The claim rests on the three arms answering three DIFFERENT statuses through the real exported handler, each case falsifying the others' "no arm" hypothesis.

**ONE RESTORE NEEDED DOING BY HAND AND IS RECORDED.** The first seed ran while the new file was still UNTRACKED, so `git checkout --` could not restore it and reported a pathspec error. It was reversed explicitly and verified byte-identical under `sha256sum -c` before anything else ran. **The file was committed before the remaining three seeds.**

---

### 8. Blockers and landmines

**NO BLOCKER.** No port was wired, no triple moved, no migration was written, edited or reserved, no door opened, no closed list widened, no gate weakened, and no test was skipped, disabled or quarantined.

**THE LANDMINE IS THE ONE SECTION 5 NAMES.** A refuted reason living in six green test names is invisible to every control this repository has. **This is the third wave in a row to find a claim propagating where nothing asserts it**, and whether that wants a control is bigger than this row.

**AND ONE MEASUREMENT IS DELIBERATELY INCOMPLETE.** Section 6 rule 1 is a NECESSARY condition. The sufficient direction needs a sweep of all fourteen ports and is [ADR-368](../decisions/ADR-368.md) OQ-1, named as unmeasured rather than assumed.

**PUSHING WORKS AND THE GITHUB API DOES NOT, TESTED IN BOTH DIRECTIONS RATHER THAN ASSUMED.** No `gh` binary and no `mcp__github__*` tool; `api.github.com/repos/lam2-coder/meritfutures` answers **403**; `git push -u origin` authenticated and succeeded. **No pull request was opened and none is this row's to open.**

**THE REPOSITORY WAS NOT PRESENT IN THIS CONTAINER AT SESSION START** and was attached and cloned before any work began, landing at `8ff3a2a4`, which is the base this row was given. `pnpm install --frozen-lockfile` ran clean.

---

### 9. Next

The founder's read of [ADR-368](../decisions/ADR-368.md) and the three decisions its approval block names: whether a refuted reason under a correct ruling is worth a row at all, OQ-1's sweep, and whether the propagation class in section 5 wants a control.

**OWED AND OUTSIDE THIS ROW'S FENCE.** `apps/api/test/wiring.test.ts:1415-1418`, whose repair text [ADR-364](../decisions/ADR-364.md) section 9 item 1 already states and which [ADR-367](../decisions/ADR-367.md) did not collect. The six executed sites in section 5, whose repair is to test NAMES and comments and whose assertions all stand.
