---
status: approved
depends_on: [README.md]
last_updated: 2026-08-20
---

## 2026-08-20 - Session 77: the vendor-parity gap-fill planned, and four of six items were mis-scoped (planning)

- **Objective:** fold six items referred from the PropAccount vendor demo into their named module plans with acceptance criteria and golden scenarios, sized MUST or SHOULD. Fence: `docs/plans/FOLD-03-vendor-parity-gap-fill.md`, `docs/decisions/ALLOCATION.md`, `docs/INDEX.md`, `docs/testing/golden-scenarios/`, this file.
- **Done. [FOLD-03](../plans/FOLD-03-vendor-parity-gap-fill.md), its numbers, and its scenarios.** `ADR-066` and `ADR-067` reserved unwritten, migrations `0038` to `0041` claimed, **GS-285 to GS-299 registered as section 36** with the ownership partition and provenance updated. **22 of 22 gates pass**, `falsify.mjs` clean and dirty with 30 scope cases, `check:invariants` 7 of 7.
- **Next:** `F0`, `ADR-066`, alone at the front. Then `F1`, `F4` and `F5` concurrently.
- **Blockers:** None. **`ADR-067` is money path and is specified rather than written**, per ADR-003.
- **NOT DONE, and it is the difference between the referral and this session:** **no module plan was folded.** M04, M06, M07 and M16 are unedited. A fold into a frozen document without a ruling is the commit the constitution forbids, and `ADR-066` does not exist yet.

### Four of the six items are not what the referral described

**Each checked at `file:line` rather than reasoned about.** The referral was right about every item's intent and wrong about the tree four times, which is the ordinary result of reading a demo against a corpus nobody re-read first.

| Item | What the tree says |
|---|---|
| **The economic calendar** | **Not new scope.** [M07](../plans/M07-risk-abuse.md) `DEP-M7-06` reads *"A maintained Tier-1 economic calendar, as data \| M6 admin, seed \| D-04 fires on the wrong windows"*, and `FM-M7-08` already requires a staleness monitor *"like `contract_specs` and `trading_calendar`"*. **No table satisfies it, so `D-04` is not implementable today** |
| **Scheduled reports** | **There is no reporting layer and there are no saved views.** A grep for `saved view`, `reporting layer` and `scheduled report` across `docs/plans/` and `docs/architecture/` returns nothing; the only `SFTP` is [M02](../plans/M02-rithmic-bridge.md)'s vendor wire format |
| **Communications** | **[M16](../plans/M16-notification-center.md) has FIVE classes, not four.** Section 1.2 is headed *"The five classes"* and `NC-M16-05` is [ADR-039](../decisions/ADR-039.md) amendment 2. **And most of the log exists**: the admin endpoint, `rendered_body`, and `FM-M16-05`'s `sent_at` / `delivered_at` / `read_at` separation |
| **Balance adjustment** | Entirely new, **and it amends [ADR-010](../decisions/ADR-010.md)**. [M06](../plans/M06-admin-ops-console.md) section 3.4 states the sensitive set as a closed list of five and an adjustment is not on it |

**The calendar finding changed the answer rather than the wording.** The referral asked which source to use, embed or API. **The corpus already ruled "maintained as data"**, and an embed cannot carry a revision, cannot be monitored for staleness and cannot be joined to `fills`. So the source question is foreclosed: an ingested feed into a Merit-owned table, with the [M04](../plans/M04-trader-portal.md) panel reading Merit's row. **An embed beside it would be the second source of truth for "when was the news", which is what the referral warned against and what `FM-M7-08` already guards.**

**Item 3 is the one the referral described exactly.** [M07](../plans/M07-risk-abuse.md):84 carries every signal named, and `D-18` supplies phone and carrier through `identity_phones` (`SD-M19-05`). Views over existing signals, no duplicated detection, no migration.

### Landmines

- **THE GATES CAUGHT THREE DEFECTS IN THIS SESSION'S OWN OUTPUT AND EVERY ONE WOULD HAVE SHIPPED.** `CI-06a` rejected the reservation rows for linking `ADR-066`, `ADR-067` and FOLD-03 before any of them existed, **which is the same trap [session 76](2026-08-20-session-76.md) recorded one session earlier and this session walked into anyway.** `CI-06u` found eight duplicate first-cell keys in FOLD-03, all of them the word `Acceptance` repeated down four two-column tables; the rows now carry distinct meaningful keys rather than a widened exemption. `CI-06d` refused fifteen `GS-nnn` citations that were not in the registry, **and then refused them a second time for being in the registry and owned by nobody**, which is the partition assertion doing exactly what it was written for.

- **`0041` IS RESERVED CONTINGENT AND MAY BE RELEASED.** [M16](../plans/M16-notification-center.md) `SD-M16-02` already widens `notifications` and the machine already carries a `delivered` transition, so the bounce and complaint outcomes may need no migration at all. **The folding session confirms the delta before spending the number.** Gaplessness is over allocated plus reserved, so an unspent reservation costs nothing.

- **GS-296 TO GS-299 ARE REGISTERED AHEAD OF THE RULING THAT DEFINES THEM.** They are `ADR-067`'s and `ADR-067` is unwritten. The numbers are spent either way and a hole is worse than a forward reference, but **a reader who finds them before the money session runs will find scenarios describing a feature the corpus has not ruled on.** Section 36's opening paragraph says so.

- **THE EligIBILITY QUESTION IN `ADR-067` IS THE WHOLE RULING AND IT HAS NO SAFE DEFAULT.** Zero denial means an eligible request is paid, so **an adjustment that creates eligibility creates an obligation**; excluding adjustments makes a goodwill credit unwithdrawable, which is a different broken promise. `OQ-F3-01`. **And the debit direction is worse**: nothing in the corpus permits taking money from a trader's balance, and `INV-M6-03` forbids an admin action that denies a payout. `OQ-F3-02` asks whether a debit is permitted at all.

- **THIS FOLD RUNS ON `claude/merit-futures-briefing-7auoor` AND NOT ON `main`, AND THAT IS FORCED.** [WAVE-03](../plans/WAVE-03-duplicate-registry-keys.md)'s `061` to `065` live only on that branch, so a branch without them shows five holes and fails `CI-06f`. **Thirty-nine remote refs were read before `066` was taken** and all maxed at `060` except that one.

- **Files touched:** [FOLD-03](../plans/FOLD-03-vendor-parity-gap-fill.md) (new), [ALLOCATION](../decisions/ALLOCATION.md) (two ADR rows, four migration rows), [INDEX](../INDEX.md) (one row), [golden-scenarios/36](../testing/golden-scenarios/36-gs-285-to-gs-299-the-vendor-parity-gap-fill.md) (new), [golden-scenarios/README](../testing/golden-scenarios/README.md) (one row), [golden-scenarios/33](../testing/golden-scenarios/33-ownership-index-and-coverage-reconciliation.md) (four owner rows, one provenance row, the contiguity sentence), [STATE](../STATE.md) (one section appended), [sessions/README](README.md), this file.

### What was NOT done, named rather than implied

**No module plan was folded.** M04, M06, M07, M16, PROP_TECH_LANDSCAPE and DELIVERY_PLAN are unedited. **No ADR was written**, so `ADR-066` and `ADR-067` are reservation rows and nothing else. **No migration file was written**; `0038` to `0041` are claims. **Platform downloads and competitions are stated exclusions** and were not specified.
