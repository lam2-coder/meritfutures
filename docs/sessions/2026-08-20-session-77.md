## 2026-08-20 - Session 77: ADR-061 proposed, the repair-versus-amendment line for duplicated table keys
<!--index: ADR-061 proposed: when a duplicated table key is a repair that lands by commit and when it is an amendment that needs a ruling -->

- **Done.** [ADR-061](../decisions/ADR-061.md) is written, `status: proposed`, with an unsigned founder-approval line. **No duplicate is repaired**: `CI-06u` reports the same **106** keys across the same **8** files after this branch as before it, checked rather than asserted. It is the WAVE-03 session that runs alone at the front, and every other session in the wave is blocked on the founder's signature here.
  - **The drafted rule was scored, not transcribed, and the score moved it.** The wave plan drafts three triggers plus a safeguard as four conditions on one level. **The safeguard is promoted to being the rule** and the three triggers are demoted to the three ways its test can look satisfied and not be. The reason is mechanical rather than aesthetic: a session applying four independent conditions will apply the three it can check and skip the one it cannot, and the one it cannot check is the only one whose default is safe.
  - **T1, different predicates: refused as a trigger, kept as the money-path worked case.** Every T1 case is a safeguard case, and the safeguard also covers the case T1 misses, the session that cannot tell. **Two expressions of one concept that agree today is the exact defect [session 20](2026-08-15-session-20.md) removed from the runner** when `CI-06b` and `CI-06c` carried two regexes for one document set ten lines apart. Its instance stands: `G-ELIGIBLE` is defined twice in [STATE_MACHINES](../architecture/STATE_MACHINES.md), `identities.status <> 'restricted'` against `identities.status = 'active'`, both citing [ADR-041](../decisions/ADR-041.md), read from eighteen files.
  - **T2, both halves cited elsewhere: unrunnable as drafted, and the correction was forced by a check that refuted my first draft of the section.** A citation names an identifier, never a meaning; `INV-M5-17` is cited from seven files and not one citation reveals which of [M05](../plans/M05-payout-system.md)'s two `INV-M5-17` invariants its author read. The runnable form is "is the thing this row names cited **outside** the file it is damaged in". **Run over all 59 session-index keys in this session, the first form of the question returned 59 of 59 cited elsewhere**, which is the opposite of what the section claimed, because each key matches its own entry file's heading. Excluding the entry a row indexes, which is `CI-06n`'s reconciliation rather than a citation, it is **0 of 59**. The section was rewritten around the checked answer.
  - **T3, the repair changes a rule the corpus states about itself: kept, and it is the trigger that earns its place.** It is the only one that catches a case where both rows agree and the delete is still an amendment, and its two instances hold **77 of the 106**: [ALLOCATION](../decisions/ALLOCATION.md)'s fifteen reservation-plus-merged pairs, which are the [ADR-034](../decisions/ADR-034.md) allocation protocol rather than formatting, and the [sessions README](README.md)'s shared-number convention, which the register's own note already calls "a ruling that is owed, not a duplicate to repair".
  - **The entry reports what its own rule costs.** Applied to today's register it routes **83** keys to a ruling on findings already recorded, leaves **23** to a reading nobody has done, and **clears at most 23 and possibly none**. That is section 7's strongest objection to the entry's own recommendation and it is stated there rather than buried. The defence is prospective: the value is duplicate 107, the next one the keep-both merge produces, not the backlog.
  - **[INDEX:37](../INDEX.md) was verified at that line before being cited**, per the brief. The heading reads "Tracking (living docs, updated every session)" and its rows name INDEX, STATE, `sessions/`, `decisions/` and ALLOCATION. **Four of the eight damaged files sit inside that carve-out and hold 89 of the 106 keys.** The entry holds the row short of what it does not say: it licenses documents that are **appended to**, and a repair is a **delete of a row a different session wrote**.
  - **FROZEN is corpus-wide and never a field, and that is checked.** Zero files under `docs/` carry `status: frozen`; all eight damaged files read `status: approved`; [CLAUDE.md](../../CLAUDE.md) lists `frozen` in the vocabulary and nothing uses it. So "a frozen document" cannot be resolved by opening a document, only through [STATE](../STATE.md)'s corpus-wide declaration, which makes the Tracking row the only standing exception.
  - **22 of 22 gates pass and [`falsify.mjs`](../../scripts/corpus/falsify.mjs) is clean.** `adr_count` moved 60 to 61 in [INDEX](../INDEX.md) and [STATE](../STATE.md), generated rather than typed.
- **Next.** **The founder signature on [ADR-061](../decisions/ADR-061.md), which is the gate on the rest of WAVE-03.** Section 7's second objection is the one to read first: the default is set against a wrong merge and is paid for with a queue of 83, and inverting it for the seven files off the money path is a defensible ruling this entry would not argue hard against. After the signature, the wave should be planned as **rulings on 83 and readings on 23**, not as 106 repairs.
- **Blockers.** None for this session. The wave behind it is blocked on the signature above.
- **Landmines.**
  - **TWO CLAIMS IN THE SESSION BRIEF DID NOT SURVIVE CHECKING.** `docs/plans/WAVE-03-duplicate-registry-keys.md`, named as the first document to read, **does not exist in the tree**, so the rule was scored from the brief's own statement of it against primary sources. That is recorded as section 7's fourth objection inside the entry rather than as a footnote, because **a trigger scored from a quotation of itself is one transcription away from the error class [CLAUDE.md](../../CLAUDE.md) warns about**. Every instance was checked against `gates.mjs`, INDEX and the damaged files; that proves the instances are real and cannot prove the drafted rule is quoted whole.
  - **`061` WAS NOT RESERVED IN [ALLOCATION](../decisions/ALLOCATION.md) AND THE FENCE WAS BUILT ON THAT PREMISE.** The brief said the number was already there in its final form and that the file must not be touched. The ADR table ended at `060`. **The row was written**, and it is the only edit to that file: none of its eighteen registered duplicates is touched. **The fence's other prohibition was kept exactly.**
  - **`CI-06f` CANNOT SEE AN UNRESERVED NUMBER AT THE TOP OF THE SEQUENCE, AND [ALLOCATION](../decisions/ALLOCATION.md) SAYS OTHERWISE ABOUT ITSELF.** Its prose reads "a branch inventing an unreserved number fails". **It fails only below the maximum.** Gaplessness is over allocated plus reserved, so an entry file at the top with no row opens no hole. **Watched both ways**: the entry with no reservation row passes `CI-06f`, and the same file renumbered to `062` fails it on the hole at `061`. **The number a race actually collides on is the next one, which is the number this gate cannot see.**
  - **THE FALSIFIER WAS THE STRICTER READER AND THE RUNNER WAS GREEN.** `falsify.mjs`'s `CI-06f` seed derives the first free number **from the allocation table** and plants an entry five past it, so an entry present on disk and absent from that table sends the seed off-target: `FAILED OFF-TARGET, expected a finding naming ADR-061, got ADR-062 to ADR-064`. **That is what caught the missing row.** A gate and its seed disagreeing about what a registry says is a finding about the registry, and it turned a fence resting on a false premise into a checkable fact rather than a judgment call.
  - **THE REGISTER CANNOT TELL AN ESCALATED KEY FROM AN UNREAD ONE, AND THIS ENTRY CREATES THAT GAP.** `CI06U_REGISTER` has one state per key, and its run note calls all 106 "a repair this gate is waiting for". After this rule, 83 of them are rulings the founder is waiting on. **That is `CI-06l`'s stale-list class, named three times in this corpus already.** The cheap remedy is a comment beside each key naming the ADR that owns it. **It is recommended in section 6 and deliberately not taken**: `gates.mjs` is outside the fence, no `CI-06` letter is claimed, and a mechanism written by the entry that found the need for it has been reviewed by nobody.
  - **[STATE](../STATE.md) WAS NOT UPDATED BEYOND ITS GENERATED SPAN, WHICH DEPARTS FROM THE END RITUAL.** [CLAUDE.md](../../CLAUDE.md) says a session updates STATE; the fence for this session named three files and STATE was not among them. **The omission is recorded here rather than taken silently.** The thing STATE would carry is an open item for the 83 escalated keys, and it should be written by whichever session the founder's signature unblocks, because its content depends on how the signature falls.
  - **THERE IS NO SESSION 76 IN THIS REGISTRY AND THIS LOG IS NUMBERED 77.** The number was assigned by the brief. If 76 is an unmerged sibling, it lands normally; if it is never written, the [sessions README](README.md) carries a hole that nothing checks, because `CI-06n` reconciles rows to files in both directions and asserts nothing about gaplessness.
- **Files touched.** [`docs/decisions/ADR-061.md`](../decisions/ADR-061.md) (new), [`docs/decisions/README.md`](../decisions/README.md) (one row), [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) (the `061` reservation row, and nothing else), [`docs/INDEX.md`](../INDEX.md) and [`docs/STATE.md`](../STATE.md) (the `adr_count` span, generated), [sessions README](README.md) (one row), this file. **No file under `packages/` and no line of `scripts/` was edited. No registered duplicate was repaired.**
