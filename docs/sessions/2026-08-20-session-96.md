---
status: approved
depends_on: [README.md]
last_updated: 2026-08-20
---

## 2026-08-20 - Session 96: FOLD-04 I3, the impersonation touchpoints, and the two allocations the brief asked for turned out to be zero and seven (non-money)

- **Done:** **The four surfaces [ADR-068](../decisions/ADR-068.md) says `I3` owns.** [M04](../plans/M04-trader-portal.md) section 3.9 and `INV-M4-17`: the **persistent banner**, stated mechanically rather than as the word "persistent", with the operator as its audience and the trader structurally unable to receive it. [M05](../plans/M05-payout-system.md) section 3.6 and `INV-M5-23`: the payout refusal placed **before `G-ELIGIBLE`** and never expressed as a gate result. [M19](../plans/M19-kyc-identity.md) section 3.5 and `INV-M19-15`: the KYC refusal, which is the only one on the list whose harm is not an authorization harm. [M20](../plans/M20-wallet.md) section 3.7 and `INV-M20-16`: the wallet-spend refusal, kept **out** of `INV-M20-06` deliberately, plus **`OQ-M20-07`**, which takes the `closed`-identity door three entries have recorded and none owned. [SECURITY](../architecture/SECURITY.md) section 3.1: the seven-route refusal map, `C-20` extended to carry `GS-300`'s alert, and the `D5` finding below. **No rejection logic, no ADR, no migration.** `node scripts/corpus/gates.mjs check`: **24 of 24**. `pnpm vitest run`: **1014 pass, 42 skipped, 67 files**.
- **Next:** **The eight `D5` rows are owed to [API_CONTRACT](../architecture/API_CONTRACT.md) section 12 and cannot be written until its factor vocabulary gains a token**, which is [FOLD-04](../plans/FOLD-04-impersonation-and-admin-parity.md)'s own definition of done and is now the second debt this fold owes that file, after `ADR-069`'s eighteen routes. **`M6-N-06`'s surface is owed to [M03](../plans/M03-billing-checkout.md)**, and it has to cover `psp`, `wallet` and `mixed`. **`OQ-M20-07` needs the founder**, and its landing set is two files that move together.
- **Blockers:** **`OQ-M20-07` is a founder question and this session could not answer it inside its fence**, for the reason in the landmine below rather than for a fence reason. [ADR-068](../decisions/ADR-068.md) and [ADR-062](../decisions/ADR-062.md) are both `status: proposed` with unsigned approval lines, so every surface here is written against rulings the founder has not signed.
- **Landmines:**

  - **REQUIREMENT 4 AND REQUIREMENT 7 ONLY LOOK LIKE A CONTRADICTION, AND THE RESOLUTION IS A CONSTRAINT RATHER THAN A RULE.** [ADR-068](../decisions/ADR-068.md) requires a **persistent banner** and **no trader-facing disclosure**. Read as two rules about one screen they cannot both hold. **They hold because the banner renders inside the impersonation session and there is no other session it could reach**: the trader auth path resolves a bearer token on `sessions.refresh_token_hash` ([`0002`](../../packages/db/migrations/0002_identity.sql):342) and `IMPERSONATION-C1` refuses that row **in both directions**. **Non-disclosure is a property of the session-type boundary and not a portal responsibility**, which matters because a portal responsibility is something a later refactor can drop.

  - **THE `D5` MATRIX IS NOT IN [SECURITY](../architecture/SECURITY.md), AND ITS VOCABULARY CANNOT EXPRESS AN IMPERSONATION REFUSAL AT ALL.** The fence named `SECURITY` for "the `D5` matrix entries". **Before this session, `grep -i d5 docs/architecture/SECURITY.md` returned exactly one hit, `C-18`, and the `d5` in it sits inside an anchor slug pointing at [API_CONTRACT](../architecture/API_CONTRACT.md) section 12.** A case-sensitive `grep D5` returned **zero**. The matrix lives in [API_CONTRACT](../architecture/API_CONTRACT.md) and is not in this fence. That is the smaller half. **The larger half is that the rows cannot be written yet even by whoever holds that file.** `CI-06k` reads the **Required factor** column against a closed vocabulary of six tokens, and **none of them is true here**: `passkey or dual_channel` asserts that an elevation reaches the route, which [`0042`](../../packages/db/migrations/0042_impersonation_sessions.sql) makes impossible by carrying no `elevated_at` and no `elevated_by_factor`, so the cell would be **false in the direction that reads as coverage**; `session` asserts an ordinary read surface; `admin_sso` is the surface the session is initiated **from**. The honest cell is a token that does not exist, proposed as **`never`** and deliberately **not claimed**, because adding to a closed vocabulary changes **what a gate accepts** and that is not a transcription.

  - **THE `M6-N-nn` ALLOCATION THE BRIEF ASKED FOR IS ZERO, AND CHECKING BEFORE ALLOCATING WAS THE ENTIRE INSTRUCTION.** The brief said to start at or above `M6-N-09`. [M06](../plans/M06-admin-ops-console.md) section 8.1a was read: `M6-N-01` to `M6-N-08` already cover **all seven blocked routes and the replay negative**, so the four surfaces in this fold produce **no new negative-authz identifier**. The only unclaimed rows this capability implies are the RBAC rows for the **initiation and exit** routes across three roles, and those are router-enumerated rows in [M06](../plans/M06-admin-ops-console.md), which is not in this fence. **An ordinal claimed outside the registry that records it is exactly the collision section 8.1a exists to prevent**, so the count is reported as zero rather than left unstated.

  - **`OQ-M20-06` WAS ALREADY SPENT, AND THE ENTRY THAT DECLINED TO TAKE IT PREDICTED WHY.** [ADR-062](../decisions/ADR-062.md) numbered the `closed`-identity divergence `OQ-062-01` rather than `OQ-M20-06` because *"taking `06` would allocate into a namespace a concurrently running session holds"*. **That session landed**: `OQ-M20-06` is now [ADR-070](../decisions/ADR-070.md)'s `fee_back.repeats` lock. So the question takes **`OQ-M20-07`**. The reservation reasoning was right and the number it protected is gone, which is the case for reading the block in **every** namespace before allocating and not only in the one the brief names.

  - **THE RECONCILIATION COULD NOT LAND HERE, AND THE REASON IS A CITATION RATHER THAN A FENCE.** The brief permitted reconciling `INV-M20-06` with the payout gate. **`G-WITHDRAWAL-CLEARED`'s primary source IS `INV-M20-06`.** [Session 78](2026-08-20-session-78.md) held [STATE_MACHINES](../architecture/STATE_MACHINES.md) and deliberately left that guard alone, on the ground that moving it **without** M20 would make the two documents disagree, *"which is strictly worse than the divergence"*. **Moving `INV-M20-06` without [STATE_MACHINES](../architecture/STATE_MACHINES.md) produces the identical disagreement from the other side.** The two files move together or neither moves. **And [ADR-062](../decisions/ADR-062.md) is unsigned**, so adopting its predicate would have propagated an unsigned money-path ruling into four further documents on a session's authority. What `OQ-M20-07` adds instead is the thing that was actually missing: **a number in the namespace of the document that contains the site**, the **door inventory nobody had assembled** (five doors read `identities.status`, **one written `= 'active'` and four written the other way**), a proposed predicate, and the observation that **the four are not one kind of door**, because extraction and purchase may deserve different answers.

  - **THE PAYOUT REFUSAL MUST NOT BE MODELLED AS A GATE FAILURE, AND THE COST IS IN THE RECORD RATHER THAN IN THE RESPONSE.** [STATE_MACHINES](../architecture/STATE_MACHINES.md) section 2 rules that a request failing `G-ELIGIBLE` returns the **gate breakdown** and emits **`payout.blocked`**. That path is built for a trader who did not qualify and every artefact on it says so, including an event that reaches the account timeline. **An impersonation session is not a trader who did not qualify.** Routing its refusal there writes a false eligibility story onto a real account and emits an event asserting something untrue about a human who did nothing, and both survive into the evidence pack [M06](../plans/M06-admin-ops-console.md) later exports as fact. **Same `403`, different record, and the record is the half that lasts.** `INV-M5-23`.

  - **`GS-302` CANNOT DISCRIMINATE WHICH CONTROL REFUSED, BECAUSE BOTH DO.** The scenario impersonates a `restricted` identity and asserts that every money surface `restricted` already blocks **stays blocked**. In an impersonation session the impersonation refusal fires too, so **`GS-302` would stay green if the restriction leg of `INV-M20-06` were dropped tomorrow.** That is acceptable as an outcome pin and is not evidence the context gate still fires. The discriminating assertion is `INV-M20-06` exercised from a **trader** session, named in words in [M20](../plans/M20-wallet.md) section 3.7 with **no ordinal claimed**, because [M06](../plans/M06-admin-ops-console.md) is not this session's to write in.

  - **NEITHER `M6-N-02` NOR `M6-N-06` NAMES `mixed`, AND [M03](../plans/M03-billing-checkout.md) HAS ALREADY PAID FOR THAT LESSON ONCE.** `M6-N-02` is written against `payment_method = wallet` and `M6-N-06` against `POST /checkout`, and a `M6-N-06` run with `psp` says nothing about `mixed`. `INV-M3-15` exists at the resolved-identity step because the restriction refusal was refusing on the wallet leg **after the card leg was underway**, and the rule that came out of it is that **every hard limit is asserted against all three payment methods**. The impersonation refusal sits **before** the payment-method branch so one decision refuses all three, and the assertion has to be run against all three anyway.

  - **THE KYC REFUSAL IS THE ONE THAT SURVIVES THE MACHINERY VANISHING, AND IT IS WORTH SAYING WHERE THE ROUTE LIVES.** Its harm is not an authorization harm: the verification row is **attributed to its subject**, and `POST /kyc/session` enters the funnel `INV-M19-11` captures at the time and says *"data not captured at the time cannot be reconstructed"*. **An operator-initiated session the subject never completes is an abandonment written against a real human and into the dataset [M19](../plans/M19-kyc-identity.md) section 10's placement decision is to be settled by.** `OQ-F4-02` is separated from it deliberately, because that question asks about an **admin-attributed** verification and the impersonation case is the opposite shape: **trader-attributed and false**, not admin-attributed and possibly worthless.

  - **THREE INHERITED REFUSALS ARE WRITTEN AS INHERITED IN ALL THREE PLACES, WHICH FEELS LIKE UNDER-DELIVERING AND IS THE INSTRUCTION.** [M05](../plans/M05-payout-system.md), [M19](../plans/M19-kyc-identity.md) and [M20](../plans/M20-wallet.md) each say **"nothing"** in the column where new enforcement would go, and cite `C-27` plus the schema fact that [`0042`](../../packages/db/migrations/0042_impersonation_sessions.sql) has no column an elevation could be written to. The argument is made **once**, in [M05](../plans/M05-payout-system.md) section 3.6 and in [SECURITY](../architecture/SECURITY.md) section 3.1, and repeated in one sentence elsewhere. **A document claiming new enforcement where an old invariant already holds is how the old one gets removed later**, and four copies of one argument is the same defect wearing the opposite coat.

- **Files touched:** [M04](../plans/M04-trader-portal.md), [M05](../plans/M05-payout-system.md), [M19](../plans/M19-kyc-identity.md), [M20](../plans/M20-wallet.md), [SECURITY](../architecture/SECURITY.md), [sessions/README](README.md), this file. **No fence exceptions taken.** [M06](../plans/M06-admin-ops-console.md), [API_CONTRACT](../architecture/API_CONTRACT.md), [ALLOCATION](../decisions/ALLOCATION.md) and [STATE_MACHINES](../architecture/STATE_MACHINES.md) were all read and none was written, and each of the four has a debt named above rather than a quiet edit. **[STATE](../STATE.md) was not updated**, the same departure from the end ritual [session 77](2026-08-20-session-77.md) and [session 78](2026-08-20-session-78.md) each recorded, and for the same reason: the fence named six things and `STATE` was not among them. What it would carry is `OQ-M20-07`'s arrival and the `D5` vocabulary debt, and both are true of a branch that is not merged yet.

### What was written, and what was deliberately not

**Written:** the banner's mechanics and its two permitted divergences from the trader's screen; the payout refusal's placement and its record; the KYC refusal's second reason; the wallet refusal's exclusion from `INV-M20-06`; the seven-route map with the inherited three named as inherited; `GS-300`'s alert given one home; and `OQ-M20-07` with a door inventory and a proposed predicate.

**Not written, each with its reason stated at the site rather than left to be discovered:** any rejection logic (the fence, and [ADR-068](../decisions/ADR-068.md) says the refusals are server-side authorization decisions); any `M6-N-nn` ordinal (the block is already sufficient and [M06](../plans/M06-admin-ops-console.md) is out of fence); any `D5` row in [API_CONTRACT](../architecture/API_CONTRACT.md) (out of fence, and the vocabulary cannot express it yet); any route name for impersonation initiation or exit (**inventing an endpoint inside a security document is how a control gets written against an endpoint nobody built**); and any change to `INV-M20-06`'s predicate (the two-file coupling above).
