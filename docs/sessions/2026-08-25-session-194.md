---
status: approved
depends_on: [README.md]
last_updated: 2026-08-25
---

## 2026-08-25 - Session 194: M19's tables enter the transcription, and the one that belongs to TWO identities is refused rather than given half a rule
<!--index: **[M19](../plans/M19-kyc-identity.md)'s slice under [ADR-092](../decisions/ADR-092.md), computed from `TABLE_KEYS` rather than received. FIVE OF SIX, and the honest report is that all five were AVAILABLE**: `main` was `dfca9bc` at open and `dfca9bc` at commit, re-fetched rather than assumed, so none of the four concurrent siblings had landed and nothing was already there. `kyc_verifications`, `sanctions_screenings`, `kyc_funnel_events`, `identity_phones` and `phone_change_requests` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **21 of 111 becomes 26 of 111**, all five `owned` on a `NOT NULL identity_id` the DDL declares against `identities(id)`. **No ADR, no migration, no new document.** **`dedupe_matches` IS REFUSED AND THE REASON IS THE VOCABULARY RATHER THAN THE TABLE**: it carries `identity_a` AND `identity_b`, both `NOT NULL REFERENCES identities(id)`, under `CHECK (identity_a < identity_b)`, so which side an identity lands on is decided by **uuid ordering** -- and `OwnedRule.column` is one column while [`scopePredicate`](../../packages/db/src/scoped-db.ts) renders one `eq`, so a rule on either column returns **roughly half that identity's match rows, silently**. **It would have passed every assertion in the suite**, which is why the refusal is worth recording: it came from reading `dedupe_matches_canonical_order` and not from a gate. This is [ADR-092](../decisions/ADR-092.md) section 3's named-and-unruled class, **third member** beside `identity_links` and `attributions`, and **a transcription rules nothing**, so it was reported rather than allocated an ADR. **`otp_challenges` is out for the already-ruled reason and is ALSO NOT M19'S**: [`0029`](../../packages/db/migrations/0029_phone_identity_and_auth.sql) writes `ALTER COLUMN email_normalized DROP NOT NULL`, unregistrable under [ADR-094](../decisions/ADR-094.md) exactly as `trading_calendar` is, and `grep -n 'otp'` over M19 returns one line which is about `contact_channels`. **TWO OF THE FIVE CARRY DRIFT AND THE FOLD WAS WATCHED APPLYING IT**: [`0034`](../../packages/db/migrations/0034_reversible_contact_addresses.sql) adds three `ADD COLUMN`s to `identity_phones` and five to `phone_change_requests` (ADR-046), while **0029's change to `kyc_verifications` is `DROP CONSTRAINT` plus `ADD CONSTRAINT`** widening `verification_purpose` for `reverify_phone_change` (SD-M19-07), which moves no column set -- and deleting `phone_ciphertext` from the transcription turns the per-table assertion RED naming the column, so the replay is live on this session's own tables. **`identity_phones` scoped by `phone_hash` was seeded and is caught** by the DDL check, which matters because [`identity_phones_live_number_idx`](../../packages/db/migrations/0029_phone_identity_and_auth.sql) is **deliberately not unique**, so that rule would have returned STRANGERS' rows. **MERIT NEVER PROXIES IDENTITY DOCUMENTS and each `why` says which side of that line its table holds**: `provider_applicant_id` is a reference and `phone_hash` is a digest, both `bytea` declared as a CUSTOM TYPE and never approximated as `text`. **TWO GAPS NAMED AGAINST THIS SESSION**: column TYPE and NULLABILITY are still asserted nowhere on any of the twenty-six, and **`phone_change_requests` reaches an identity two ways with nothing tying them** (`old_phone_id NOT NULL REFERENCES identity_phones(id)`), which is `certificates`' shape and now its **second instance** rather than a one-off. 42 tests to **47**, suite **1,552 to 1,557**, both baselines MEASURED by restoring `origin/main`'s three files rather than inherited; `verify` exit 0 at 8 of 8 invariants and 106 files; 30 of 30 gates; `falsify:ci CI-01` **12 of 12** -->

- **Objective:** M19's tables enter [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts), minus whatever is already registered, under [ADR-092](../decisions/ADR-092.md) and [ADR-094](../decisions/ADR-094.md). **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR.**
- **Done:** **5 of 6.** `kyc_verifications`, `sanctions_screenings`, `kyc_funnel_events`, `identity_phones` and `phone_change_requests` registered; **`dedupe_matches` refused and reported**. The registry goes from 21 of 111 to **26 of 111**. Two seeds watched failing rather than predicted.
- **Next:** **`dedupe_matches` needs a per-TABLE ruling**, and it is the third member of a class [ADR-092](../decisions/ADR-092.md) section 3 already named and declined to take: a row that belongs to two identities has no rule in a four-class vocabulary whose `owned` member names one column. Until it has one, `dedupe_matches` is unregistered, therefore unreachable, therefore safe. **`M19-0` is still not complete**: [session 168](2026-08-24-session-168.md)'s slice is the six tables **and** a write accessor, and `ScopedDb` is still `__brand`, `identityId` and `rows()`. Every M19 slice that writes still waits on that, on an HTTP framework, and slice 4 on a queue.
- **Blockers:** none for this session. **The `E2` line-by-line read is owed these five registrations**, which makes twenty-six money-path tables merged on delegated authority without it.
- **Landmines:**
  - **`dedupe_matches` WOULD HAVE PASSED EVERY ASSERTION IN THE SUITE.** `identity_a` is a `NOT NULL` column the DDL declares `REFERENCES identities(id)`, so `every owned rule names a column the DDL declares REFERENCES identities(id)` is satisfied by it; the column-set comparison, the SQL-name check and the fold are all indifferent. **Nothing mechanical stands between a future session and half a rule on this table.** The thing that refuses it is `dedupe_matches_canonical_order CHECK (identity_a < identity_b)`, read from the migration, plus the arithmetic that follows from it.
  - **`phone_change_requests` REACHES AN IDENTITY TWO WAYS AND NOTHING TIES THEM.** `identity_id` direct, `old_phone_id NOT NULL REFERENCES identity_phones(id)` one hop out. A row whose prior phone belongs to A while its `identity_id` says B is representable and is B's request under the landed rule. **This is `certificates`' gap ([session 188](2026-08-25-session-188.md)) recurring**, so the shape is a class rather than an accident. **Repairing it is a migration and this fence holds none.**
  - **`identity_phones_live_number_idx` IS DELIBERATELY NOT UNIQUE** ([ADR-039](../decisions/ADR-039.md) (b), and `probe_phone_identity.sql`'s `SUCCESS 2` asserts the ABSENCE of a unique index on `phone_hash`). One number may therefore be live on more than one identity, and a rule scoping this table by `phone_hash` would return strangers' rows. Seeded and caught, but the reason it is a real trap rather than a perverse one is that the non-uniqueness is itself a ruling.
  - **`nullable: false` ON THE FIVE NEW `owned` RULES IS ASSERTED NOWHERE**, exactly as on the six that were already there, and neither is any column TYPE. [ADR-094](../decisions/ADR-094.md) section 3 names that axis and states why it is not closed inside a money-path slice. **This session did not close it and nothing written here implies it is closed.**
  - **`scope.ts` AND `scoped-db.test.ts` LANDED IN ONE COMMIT AND THAT WAS DELIBERATE.** `SQL_NAME` is `Readonly<Record<TableKey, string>>` and total, so a commit carrying either file alone is red. The per-file commit rule is served better by a green pair than by two commits neither of which compiles.
  - **`node_modules` WAS ABSENT** and `pnpm install --frozen-lockfile` was run before anything could be verified; the lockfile is unchanged. **`falsify:ci` was never backgrounded and `git add -A` was never used in this session**; `git status --porcelain` was read immediately after the `CI-01` run and reported zero.
- **Files touched:** [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts), [`packages/db/src/scope.ts`](../../packages/db/src/scope.ts), [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts), [STATE](../STATE.md) (one appended `##` section), [sessions/README](README.md) (row 194 struck, entry row generated), this file. **Nothing outside the fence.** [M19](../plans/M19-kyc-identity.md), [M20](../plans/M20-wallet.md)'s neighbours, [`0003_kyc.sql`](../../packages/db/migrations/0003_kyc.sql), [`0029_phone_identity_and_auth.sql`](../../packages/db/migrations/0029_phone_identity_and_auth.sql), [`0034_reversible_contact_addresses.sql`](../../packages/db/migrations/0034_reversible_contact_addresses.sql), [ADR-092](../decisions/ADR-092.md) and [ADR-094](../decisions/ADR-094.md) were **read and none was written**.

### What was NOT done, named rather than implied

**No ADR, no migration, no new document, no `INDEX.md` row, no gate, no fixture, and no edit to [`docs/plans/`](../plans/P4-portal-and-site.md), which this fence makes read-only.** **`dedupe_matches` was NOT registered and no number was allocated for it**: wanting one was the signal to stop and report, and the report is above and in the pull-request body. **No landed rule was re-argued, restated or improved**: the twenty-one rows that were there when this branch opened are byte-identical on it. **The write accessor was not touched**, so registering these five makes them READABLE and nothing else, which is [ADR-092](../decisions/ADR-092.md) section 9's boundary unchanged.
