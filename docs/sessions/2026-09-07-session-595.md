---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 595: `G2` is not enforced, the reason on record could never have applied, every pointer a check could redden is already a registered finding, and `G1` is ruled to be the person's read

<!--index: **THE ROW IS [ADR-400](../decisions/ADR-400.md), `proposed` and UNSIGNED. CONTROL OVER THE CORPUS'S HONESTY, `E2` READ OWED. NOTHING MINTED: no invariant, no gate, no leg, no register, no exemption, and `scripts/corpus/**` never opened for writing.** [ADR-388](../decisions/ADR-388.md) section 16 question 1 had been deferred by three rows. **RULED: `G2` IS NOT ENFORCED, and the stated reason for refusing option (c) is REFUTED.** That entry refused a leg on `RI-15` because it *"would go red on 42 dated records nobody may repair"*; **`RI-15`'s input is 754 SOURCE FILES and NOT ONE `.md`**, derived from the walk, and its own `covers` line says it reads `docs/decisions` *"NOT AT ALL, deliberately"*. **The reachable red set is NINE, and every one of the nine is ALREADY a key in its host check's own exemption register** (9 in `RI-16`'s, 7 in `RI-15`'s, reconciled key by key), so a `G2` leg **detects nothing new** and its only contribution is a class label. **That label is not computable twice the same way**: `git blame -w` and [ADR-396](../decisions/ADR-396.md)'s `git log -S` name a different commit on **57 of 493** and a different verdict on **23**, which is the corpus-wide figure [ADR-396](../decisions/ADR-396.md) section 7 asked for and nobody had taken. **Born wrong is 52 under one instrument and 46 under the other, and 43 of the 52 sit in dated records NO CHECK READS**, at a rate of 14.8 percent against source files' 3.1. **One pointer changed class between [ADR-397](../decisions/ADR-397.md)'s base and this one**, [session 478](2026-08-30-session-478.md) line 9, re-blamed by row 396's own repair to a DIFFERENT pointer on the same line. **And the job that runs `repo-invariants.mjs` checks out at `fetch-depth: 1`, where `git blame` does NOT error but ANSWERS**: run there the leg reports **18** born wrong where there are **9**, and all 9 extras are repairable pointers condemned as manufactured evidence. **RULED: `G1` IS THE PERSON'S READ**, not `citationsIn`'s: the binder binds **290 of 7,665** dated-record pointers and a systematic 1-in-123 sample of **60** of the 7,375 it reads no name for finds **56** that name a subject a person could search for. **[ADR-397](../decisions/ADR-397.md)'s six census figures reproduce EXACTLY** and its two instances re-derive. **[ADR-377](../decisions/ADR-377.md) section 6's binding instability is REPAIRED, measured at 0 verdicts moved over 493, and it is reported AGAINST this row's own conclusion.** Base reproduced on `c219c0d1` before the first edit; suite **336 / 8,109 / 10 / 0** delta zero, gates **33 of 33**, invariants **35 of 35**.-->

---

### 1. The objective

Answer [ADR-388](../decisions/ADR-388.md) section 16 question 1, whether `G2` should ever be enforced by a check rather than merely required, and [ADR-397](../decisions/ADR-397.md) section 12 item 2, whether `G1` is read by a person or by `citationsIn`. **Both answers ship or neither is worth having**, because the second decides the population the first is priced over. One objective, non-money, control over the corpus's honesty.

---

### 2. What was measured before the first edit

**The container arrived with NO REPOSITORY AT ALL.** The stated working directory was empty, `git rev-parse` returned *fatal: not a git repository*, and a search to depth 6 from `/` found no checkout of this repository anywhere; the only `.git` directories on the container are `/opt/rbenv`, `/opt/rbenv/plugins/ruby-build` and `/opt/nvm`. The tree was attached to this session's authorized set and cloned by hand.

**And the clone is FULL, which was RUN rather than assumed in either direction.** `git rev-parse --is-shallow-repository` returned **`false`** immediately after the clone, with **4,310** commits reachable from `origin/main` and **4,319** from the base. No deepening was needed and no `--depth` bound was ever imposed. [ADR-397](../decisions/ADR-397.md) measured `true` on its container and [ADR-396](../decisions/ADR-396.md) measured a full clone on its own; **both are right about themselves and a row that inherits either is guessing.**

**The base was reproduced on `c219c0d1` before the first edit**, on a tree `git status --porcelain` reported empty, with `HEAD` confirmed rather than assumed: gates **33 of 33**, invariants **35 of 35**, suite **336 files / 8,109 passed / 10 skipped / 0 failed**, `typecheck`, `lint` and `format:check` all exit 0. The invariant runner's wall clock, which the pricing in section 4 is measured against, is **5,407 ms**.

**`pnpm run verify` was NOT run and `falsify.mjs` was NOT run.** Both are forbidden to this row and both mutate the working tree. Every leg was invoked directly.

---

### 3. What landed

- **[ADR-400](../decisions/ADR-400.md)**, new, `proposed`, approval line UNSIGNED. Two rulings, a corpus-wide census under two instruments, and six falsification runs.
- **[ADR-388](../decisions/ADR-388.md) line 336 annotated at the site**, one line replaced by one longer line, file 371 lines before and after. The original sentence is kept whole and the refutation is appended, per `RI-14` and [ADR-367](../decisions/ADR-367.md).
- **[ADR-397](../decisions/ADR-397.md) line 248 annotated at the site**, on the same terms, file 273 lines before and after.
- **Three rows in [ALLOCATION](../decisions/ALLOCATION.md)**: `398` and `399` RESERVED naming no subject, no branch name and no link to a file that does not exist, and `400` TAKEN. Session numbers **derived** from the offset of 195 that holds on rows `388`, `397` and `400`.
- **The generated spans and [STATE](../STATE.md)**, regenerated rather than hand-edited.

**Nothing else.** No invariant number minted, no gate added, widened or weakened, no leg written, no register grown, no `CI-06` letter spent, no migration number taken or reserved, no `status` field moved, no test skipped, disabled or quarantined, and no port wired. **`git diff c219c0d1 --name-only` names no file outside `docs/`.**

---

### 4. The findings, each derived rather than inherited

1. **`RI-15` CANNOT GO RED ON A DATED RECORD.** Its input is `citedReasonFiles`, filtered by `/\.(?:ts|tsx|mts|mjs|js)$/`: **754 files, 0 of them `.md`, 0 anywhere under `docs/`.** The cost [ADR-388](../decisions/ADR-388.md) priced option (c) at was never available to it, and `RI-16` excludes a dated record by a shape rule that is `CI-06/derivable-counts`' rather than its own.
2. **THE NINE REACHABLE POINTERS ARE ALREADY FLAGGED.** Of the **203** named pointers in both checks' scopes, **18 occurrences are wrong today**, being **16 distinct keys**, and all 16 are already entries in those checks' own exemption registers. The match is exact on both sides with nothing left over. **A `G2` leg raises its first finding on a pointer the host check already raised.**
3. **THE INSTRUMENT IS NOT SETTLED, CORPUS-WIDE.** [ADR-396](../decisions/ADR-396.md) proved `git blame -w` insufficient and asked for its census to be re-run. Run here over all **493** named pointers: different commit on **57**, different verdict on **23**. Born wrong is **52** under `blame` and **46** under `log -S`; undecidable goes from **1** to **14**.
4. **THE CLASS IS NOT STABLE.** [session 478](2026-08-30-session-478.md) line 9, citing `scope.ts:840` for `root`, is outcome ONE at `ebc21238` (`root` one line away) and outcome TWO at `c219c0d1` (eleven lines away). The cause is `8fe77a65`, **row 396's own repair commit**, one insertion and one deletion in that file, repairing a different pointer on the same line.
5. **CI HAS NO HISTORY AND FAILS QUIETLY.** [`ci.yml`](../../.github/workflows/ci.yml) line 194 runs the invariants in a job whose checkout sets no `fetch-depth`. A depth-1 clone of this base was made and `git blame` **answers** there rather than erroring, attributing every line to the single commit. The leg then reports **18** born wrong against **9**, and outcome ONE becomes unreachable by construction. **This repository already writes `fetch-depth: 0` twice, for `VG-1` and `CI-06f`, each time with a comment saying the gate would otherwise *"pass by being unable to look"*.**
6. **`G1` IS THE PERSON'S READ.** The binder binds two shapes and only two, run as a six-case matrix; a name in FRONT of a markdown link never binds at any distance, because `[` is not in `IDENTIFIER_GLUE`. Against the binder's **290 of 7,665**, a systematic sample of 60 finds **56** naming a searchable subject, and the four that fail are a kind rather than a residue: three cite a thing that happened rather than a symbol.

---

### 5. The premise that was handed to this row and did not survive

**[ADR-377](../decisions/ADR-377.md) SECTION 6's BINDING INSTABILITY IS REPAIRED, AND THIS ROW LEANED ON IT UNTIL IT MEASURED IT.** That entry found the binder's pairing was a running state over every backtick earlier in the file, so the same sentence bound in one position and not three lines down; [ADR-388](../decisions/ADR-388.md) section 7 rests on the same finding. **One backtick prepended to the top of every file moves 0 of 493 named verdicts and 0 of 9,336 unnamed ones on this base.** `citedIdentifier` now reads *"THE TWO BACKTICKS IMMEDIATELY BEFORE THE CITATION, NEVER A PAIRING WALKED FROM THE START OF THE FILE"*.

**It is reported in full because it points against this row's conclusion.** The binder is stable; it is narrow. So the refusal in [ADR-400](../decisions/ADR-400.md) does not argue that name binding is flaky, and the first draft of its section 13 was deleted rather than kept.

**AND THE 42 IS NOT A CONSTANT.** [ADR-388](../decisions/ADR-388.md) measured 42, [ADR-397](../decisions/ADR-397.md) reproduced it and called it measured twice, and it is **43** here under the same instrument and **39** under the corrected one. The dispatch carried *"not one verdict in 420"* as well; the 420 excludes every live document, which is the only document population a check actually reads.

---

### 6. Landmines

- **A CENSUS THAT DOES NOT USE THE CHECK'S OWN INPUT MEASURES A DIFFERENT CORPUS.** `RI-16` reads `citationsIn(documentScope(text).body, false)`, on the **masked** body; `RI-15` reads raw source with inheritance ON. This row's first pass used raw text for both and got 9,524 documents citations instead of 8,764, with a classification to match. **Both drafts were internally consistent and only one was about this repository.** The discarded figures are not reported as a second reading.
- **`git log -S` ON [`STATE.md`](../STATE.md) COSTS 3,262 ms PER POINTER** against 35 ms on a session log. Any future corpus-wide history pass should expect the cost to concentrate almost entirely in that one file.
- **A `G2` LEG PRICED ON A LAPTOP WOULD SHIP INVERTED.** Every developer clone is full; the CI job's is one commit deep. The leg goes green on nine it should raise and red on nine it should not, and nothing in this repository would report the difference.
- **THE ANNOTATION CONVENTION NOW HAS A NON-NUMERIC INSTANCE AND NO RULE.** [ADR-397](../decisions/ADR-397.md) section 7 ruled that a wrong FIGURE in a dated record is annotated at its site because the figure claimed to have been derived. [ADR-388](../decisions/ADR-388.md) section 16's option (c) claims no derivation; it states a consequence, and it is wrong. This row annotated it anyway and says so as an open question rather than as a settled extension.

---

### 7. Files touched

`docs/decisions/ADR-400.md` (new), `docs/decisions/ADR-388.md`, `docs/decisions/ADR-397.md`, `docs/decisions/ALLOCATION.md`, `docs/decisions/README.md`, `docs/sessions/README.md`, `docs/STATE.md`, and this log.

**Read and never opened for writing:** [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs), [`gates.mjs`](../../scripts/corpus/gates.mjs), [`ci.yml`](../../.github/workflows/ci.yml), [`corpus.yml`](../../.github/workflows/corpus.yml), and [ADR-377](../decisions/ADR-377.md), [ADR-388](../decisions/ADR-388.md), [ADR-396](../decisions/ADR-396.md) and [ADR-397](../decisions/ADR-397.md) at length. **This row holds `packages/tooling/**` and did not write to it**; the reader copy used for the census was placed outside the repository with its `HERE` constant pinned, validated at 34 of 35 with `RI-15` and `RI-16` both passing, and deleted after the last derivation.

---

### 8. Next

**Nothing in [ADR-400](../decisions/ADR-400.md) is owed to a builder, because nothing was built.** Its section 17 hands on five items, of which two are live questions rather than work: whether the annotation convention reaches non-numeric claims, and [ADR-388](../decisions/ADR-388.md) section 16 question 3, which section 12 of this row's entry makes heavier by ruling that the 43 is a floor over one twenty-fifth of the population rather than a count.

**The approval block asks the founder three things**, and the third is the one everything else depends on: whether `G1` means what a person can read or what `citationsIn` can bind. **Three consecutive rows have priced against the binder while describing `G1` in words only a person satisfies.**
