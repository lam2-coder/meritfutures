---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 633: the DEFAULT is compared against the DDL for the first time, and three columns disagree

<!--index: **THE ROW IS [ADR-438](../decisions/ADR-438.md), `proposed` and UNSIGNED. NON-MONEY DIFF, MONEY-ADJACENT SUBJECT, `E2` READ OWED. NO `src/` FILE IN ANY PACKAGE CHANGED, NO MIGRATION TOUCHED, NO `RI-nn` MINTED AND NO `CI-06` LETTER SPENT.** [ADR-437](../decisions/ADR-437.md) section 12 item 2 commissioned a comparison of a `schema.ts` DEFAULT against the DDL as *"the cheapest real check left in this area"*; it is added as one more loop in [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) beside the type and nullability comparisons, on the same fold and the same population, and **it went RED on its first run on three columns**. `kyc_verifications.raw_result`, `sanctions_screenings.list_refs` and `identity_phones.release_evidence` are `NOT NULL DEFAULT '{}'` in the migration set and carry no default in the transcription. **All three are the FAIL-CLOSED direction** and the fail-open population is asserted at ZERO over the whole registry. **Neither side was repaired**: a migration is nobody's and `schema.ts` is the dispatch's *"input, not your target"*, so the three are recorded as literals in a register that is derived back from the registry and is RED on a fourth AND on an unannounced repair. **The population is 413 and [ADR-437](../decisions/ADR-437.md)'s 337 is low by exactly the 76 `.defaultRandom()` columns**, `413 - 76 = 337`. **The money half is clean and is now a check rather than one reading on one afternoon**: 12 `*_cents` columns carry a default, that entry's figure confirmed exactly, none disagrees, each is `0` on both sides, and `typeof column.default` is asserted `'bigint'` because `String(0)` and `String(0n)` are both `"0"`. Watched RED six ways in a throwaway `git worktree`, then green on the real tree. Counts derived at reporting time in [ADR-438](../decisions/ADR-438.md) section 10. **`pnpm run verify` was NOT run and `scripts/corpus/falsify.mjs` was NOT run**, both forbidden by the row. **THE CONTAINER ARRIVED WITH NO REPOSITORY** for the twentieth consecutive row, and `git push` was refused `403` until `add_repo` with `access: "push"` was called; the repository was NOT pre-checked. -->

### 1. The objective

Find the existing type-and-nullability comparison [ADR-437](../decisions/ADR-437.md) points at, confirm for myself that DEFAULT is genuinely absent from it, and add the DEFAULT comparison in the same place and the same idiom as one more loop. Derive the population rather than quoting it. Demonstrate the check failing before showing it green. State plainly what a green comparison still leaves unproven, on [ADR-112](../decisions/ADR-112.md) foreclosure 4 and [ADR-299](../decisions/ADR-299.md) section 5.1 item 5.

### 2. Done

- **The container measured and reported.** No repository on arrival, twentieth consecutive row. `git push` refused `403` and succeeded after `add_repo` with `access: "push"`; the repository was **not** pre-checked, which the row forbids.
- **The base reproduced at `69662904`** on a clean tree before the first edit, and **it is green**: gates **33 of 33**, invariants **35 of 35**, suite **342 files / 8,310 passed / 11 skipped / 0 failed**. `CI-06f` is green on this base, which it was not on [ADR-437](../decisions/ADR-437.md)'s.
- **The existing comparison found, read and confirmed.** It is in [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) and nowhere else, it does cover the whole registered population, and DEFAULT is deliberately cut off the DDL text before it runs. ADR-438 section 3 is the re-derivation.
- **The population DERIVED by walking `TABLES` and the fold**, not counted by grepping a spelling. ADR-438 section 4 is the table.
- **The DEFAULT comparison added as one more loop**, 332 lines inserted into the file that already holds its two siblings, sharing `foldTableDefs`, `columnsOf`, `DDL_NAMES` and `VIEW_NAMES`.
- **Three disagreements found and reported at `file:line` on both sides**, ADR-438 section 5, with the direction of each stated and the dangerous direction asserted empty.
- **Watched RED six ways** in a throwaway `git worktree` on a temporary branch, both removed afterwards, then green on the real tree. ADR-438 section 7 is the table of verbatim reds.
- **What a green comparison does not catch stated as six numbered limits**, ADR-438 section 8, with `ADR-112` foreclosure 4's instance re-derived independently: **four DDL tables are transcribed nowhere and at least six `DEFAULT` clauses live on them.**

### 3. Not done, and why

- **`schema.ts` was NOT edited**, though three of its lines are wrong against the DDL by this row's own measurement. The dispatch names it as input rather than target and the fence is by file.
- **No migration was opened for edit and no migration number was reserved.** Migrations are sacred, never edited, only superseded.
- **No `RI-nn` was minted and no `CI-06` letter was spent.** The check lives in the existing file, which is the condition the dispatch set.
- **The stale sentence in its four places was NOT repaired**, and two of them are now more wrong than [ADR-437](../decisions/ADR-437.md) left them. Neither file is in this fence and the repair must be line-neutral.
- **The `GENERATED ALWAYS AS` expression is still compared nowhere.** It is a different check, one of its five columns is `*_cents`, and adding it here would be the parallel instrument the dispatch refused.
- **One file outside the fence's prose WAS changed, by the generator the dispatch requires.** `gates.mjs generate` rewrote the span named `adr_count` in `docs/STATE.md`. No prose in it was touched.

### 4. Blockers

- **None for this row.** The `E2` read is OWED and is not a blocker on the work; the entry ships `proposed` and UNSIGNED and nothing here was signed.
- **The three disagreements are blocked on a ruling and not on a diff.** Whether the transcription is corrected or the migration superseded is a decision for a row that owns `schema.ts`, and [`0029:190`](../../packages/db/migrations/0029_phone_identity_and_auth.sql)'s `CHECK` is a real argument that one of the three should stay as it is.

### 5. Landmines

- **THREE COLUMNS DISAGREE ABOUT A DEFAULT AND THE SUITE NOW SAYS SO ON EVERY RUN.** `kyc_verifications.raw_result`, `sanctions_screenings.list_refs`, `identity_phones.release_evidence`. All fail-closed. `TRANSCRIPTION_OMITS_DEFAULT` is red on a fourth AND red on repairing one of these three without moving the register in the same commit.
- **A POPULATION COUNTED BY GREPPING A SPELLING IS A POPULATION COUNTED WRONG.** [ADR-437](../decisions/ADR-437.md)'s 337 missed all 76 `.defaultRandom()` columns exactly. Third count in four rows to need correcting; all three checks in this file walk the objects instead.
- **`hasDefault` IS TRUE FOR AN IDENTITY COLUMN.** 20 of them. A DEFAULT comparison that reads only the `DEFAULT` keyword calls every one a disagreement and is reporting its own reader.
- **`String(0)` AND `String(0n)` ARE BOTH `"0"`.** A money column whose transcribed default became a JavaScript number passes every text comparison in this file. `typeof` is the only leg that sees it, and it is watched failing.
- **A GENERATED COLUMN AGREES HERE FOR THE WRONG REASON.** `reconciliations.delta_cents` is a `*_cents` column omittable from `$inferInsert` because the database REFUSES a written value, not because it defaults one. Both sides read `false` and agree.
- **THIS DISCHARGES ONE OF [ADR-437](../decisions/ADR-437.md) SECTION 7's FOUR REASONS AND IS NOT CLEARANCE TO NARROW A WRITE.** The other three are untouched: no ruling names the write path, the admission rule still has no mapping to delete, and the return still buys nothing.
- **`live_account_state` STILL HAS THREE `*_cents` COLUMNS AND IS TRANSCRIBED NOWHERE.** Confirmed independently by a second row. Fail-closed today.

### 6. Files touched

`packages/db/test/scoped-db.test.ts`, `docs/decisions/ADR-438.md` (new), `docs/decisions/ALLOCATION.md` (row `438` only), `docs/decisions/README.md`, `docs/sessions/README.md`, `docs/sessions/2026-09-07-session-633.md` (new), `docs/STATE.md` (generated spans only). **No `src/` file in any package. No migration. No workflow.**
