---
status: approved
depends_on: [README.md]
last_updated: 2026-08-30
---

## 2026-08-30 - Session 484: the approval branch is one declaration away and the declaration is in a file this fence does not reach

<!--index: **THE RULING IS [`ADR-293`](../decisions/ADR-293.md), `proposed` and UNSIGNED, `E2` READ OWED AND NOT PERFORMED. A BUILD ROW ON THE MONEY PATH THAT SHIPPED NO CODE**: [`ADR-287`](../decisions/ADR-287.md) SLICE 6, the `hold === null` path of `insertPayoutRequest()`, **BLOCKED ON ONE DECLARED RETURN TYPE IN A FILE THE FENCE DOES NOT REACH.** [`ADR-289`](../decisions/ADR-289.md) ruled the member returns `Promise<void>` and specified SEVEN OWED LINES to a row whose fence reaches them; **NONE OF THE SEVEN HAS RUN**, `payouts.ts:588-590` still declares `Promise<{ readonly eligibilitySnapshotId: string }>`, and `apps/api/src/routes/payouts.ts` is named by NO allocated row's fence, this one included. **THE BLOCKER IS PROVEN BY A COMPILER RATHER THAN ASSERTED**: a full candidate body was written and `tsc -p apps/api --noEmit` returned **ONE ERROR, `TS2322` ON THE RETURN TYPE, AND NOTHING ELSE**, so the scoped `accounts` read, the `payoutRequests` insert, every property spelling and the stamped tenancy column all typecheck; **the candidate was REVERTED and no `src/` line moved.** **THE TWO WAYS TO SHIP CODE INSIDE THE FENCE WERE REFUSED ON THE MERITS**: producing any value for `eligibilitySnapshotId` is refused individually by [`ADR-289`](../decisions/ADR-289.md) sections 3.1 to 3.3 and would be the fence deciding a money-path question, and landing the body as an unreferenced helper would be money-path code with no caller and no census entry, [`ADR-291`](../decisions/ADR-291.md)'s F2 landmine made worse. **THE LOCK, HANDED OVER BY [`ADR-291`](../decisions/ADR-291.md) AS THIS ROW'S OPEN QUESTION, IS DECIDED IN WRITING: THE PAYOUT PATH LOCKS.** `PayoutTx` gains `lockScope(): Promise<void>`, `postgresPayoutBackend` delegates it to `handle.lockScope()`, `decidePayout` calls it as its FIRST statement, and **`transact` STILL TAKES NO LOCK, now as a decision rather than a report.** **THE EVIDENCE IS THE READ-THEN-WRITE THIS BRANCH PERFORMS**, which slice 6 is what creates: `G-NO-IN-FLIGHT` is read through `subject()` (`payouts.ts:1137`), decided in memory (`:1152-1153`, commented *a liability control and not a convenience*) and written against (`:1201`), and the clamp reads a balance and writes an approval against it. **THE IDENTICAL SHAPE ONE DOOR OVER IS LOCKED AND SAYS WHY IN ITS OWN WORDS**: `gateNoInFlight` is *FOUR STATUSES, FILTERED IN MEMORY, AND UNDER `lockScope()`* (`wallet-withdrawals.ts:1415`) and `decideWithdrawal` takes the lock as its first statement above every gate (`:1490`, under *a gate evaluated before the lock is a gate evaluated against a state another transaction can still change*). **THE UNIQUE INDEX IS A BACKSTOP AND NOT THE CONTROL**: `payout_requests_no_in_flight_uq` (`0010_payouts.sql:200-201`) turns a contract-specified 409 into a 500 on the losing transaction, and it is keyed on the ACCOUNT while the schema denormalises `identity_id` precisely because the exposure question is about the HUMAN (`0010:45-49`). **AND ONE CITATION EVERY DOCUMENT REPEATS IS WRONG, RE-DERIVED AT SOURCE**: `databaseWithdrawalBackend` does NOT call `tx.lockScope()` inside `transact`, it EXPOSES the member (`wallet-withdrawals.ts:1121-1123`) and the decision function calls it; `checkout.ts:952,958,2121` is the second, independent instance of the same arrangement, so **NO ADAPTER IN THIS TREE LOCKS INSIDE `transact`** and this ruling does not either. **THE THREE TRAPS WERE RE-DERIVED AND ALL THREE HOLD**: `grep -rn split_bp packages/db/migrations` and `grep -rn clamp_reason` each return ZERO lines, both values are already stored inside `eligibility_snapshot jsonb NOT NULL` (`0010:74`) by `snapshotOf` (`payouts.ts:1070`), whose `clamp` object writes `reason` and `split_bp` at its own lines 1084 and 1087, so **NEITHER A COLUMN WAS ADDED NOR A VALUE DROPPED**; and `payout_requests.plan_version_id` is `NOT NULL` (`0010:68`) while `PayoutRequestInsert` carries FOURTEEN fields and not that one, so it is **DERIVED FROM `accounts.plan_version_id`** (`0007:50`), which `0007:12` and the trigger at `0027:272` make immutable for the life of the account. **A FOURTH LEG WAS CHECKED THAT F3 STATES ONLY IN PASSING**: `identity_id` needs no derivation because a scoped write STAMPS it and naming it in an insert's values is REFUSED (`scoped-db.ts:369-370,483`), so its absence from the insert shape is the door working rather than a field somebody forgot. **THE HOLD BRANCH WAS NOT BUILT AND NOT SCHEDULED**: no clause id, no enum of clause names, no default string, `holdFlag()` left refusing, `DEP-M7-05` still owes the clauses to counsel. **`subject()`, `holdFlag()` AND `listPayouts()` ARE UNTOUCHED AND STILL REFUSE. THE COUNTS DO NOT MOVE: TEN WIRED OF TWENTY-FOUR DECLARED, FOURTEEN BLOCKED** (`wiring.test.ts:1432`), confirmed at source before and after, because `start.ts` was never opened. **NO MIGRATION WAS WRITTEN, NO NUMBER TAKEN OR RESERVED, `packages/db/migrations/**` IS UNTOUCHED. NO `RI-nn` WAS MINTED AND `RI-34`, RESERVED TO THIS ROW BY NAME, IS LEFT FREE**, with three candidates enumerated and each refused; the real defect this session found is a **CORPUS GATE rather than an invariant** (that every `OWED` edit named in an ADR has an allocation row whose fence reaches it) and `scripts/corpus/` is out of fence, so it is **REPORTED AND NOT BUILT**. **NO DOOR WIDENED (`CATALOG_TABLE_KEYS` is still five), NO FENCE WIDENED BY A BYTE, NO GATE WEAKENED, NO STUB, NO CAST, NO SYNTHESISED DEFAULT, NO FABRICATED ID, NO FLOAT (no fixture was written at all), NO SECRET, AND MERIT STAYS PASSWORDLESS.** **FENCE HELD**, the only files outside it being the two GENERATED registry READMEs `CI-06n` requires. **WHAT IS OWED IS A CHECKLIST AT LIVE TEXT** ([`ADR-293`](../decisions/ADR-293.md) section 4) needing **ONE MONEY-PATH ROW WHOSE FENCE NAMES `apps/api/src/routes/payouts.ts`**. **THE DEFECT IS IN THE DISPATCH AND IS NAMED AS SUCH**: [`ADR-289`](../decisions/ADR-289.md) specified the owed edits completely, said which row should take them and said they should run before slice 3; three rows were then allocated and none did. **THE CONTAINER STARTED WITH NO `node_modules`** and `pnpm install --frozen-lockfile` was run before anything was measured, which is row `294`'s reported defect met in the wild. -->

---

### 1. The objective

**[ALLOCATION](../decisions/ALLOCATION.md) row `293`: [ADR-287](../decisions/ADR-287.md) slice 6, `insertPayoutRequest()`, THE APPROVAL BRANCH ONLY, the `hold === null` path.** One objective, money path, [ADR-003](../decisions/ADR-003.md) strict regime.

**IT DID NOT LAND, AND THE REPORT IS THE DELIVERABLE.** Row `293`'s own instruction is *"if the fence is too narrow, STOP AND REPORT which file you need and why. Do not widen it."* The fence is too narrow, the file is named, and the fence was not widened.

### 2. What was read before anything was written

Row `293` in full, then [CLAUDE.md](../../CLAUDE.md), then [ADR-287](../decisions/ADR-287.md) F3 and its slice table, [ADR-289](../decisions/ADR-289.md) end to end because it is the ruling that unblocked this slice, and [ADR-291](../decisions/ADR-291.md)'s row because it is the module being extended. Then, at source and not through any ADR: `PayoutRequestInsert` and `PayoutTx` at their declarations, `snapshotOf` and `decidePayout` in full, `payout_requests` in `0010_payouts.sql` end to end including every index and CHECK, `accounts.plan_version_id` in `0007` with its immutability trigger in `0027`, the scope registry entries for `accounts` and `payoutRequests`, `ScopedTx` in `scoped-db.ts`, and both lock precedents in `wallet-withdrawals.ts` and `checkout.ts`.

### 3. Done

1. **[ADR-293](../decisions/ADR-293.md) written**, `status: proposed`, approval line **UNSIGNED**. It carries the blocker with a compiler transcript, the three re-derivations, the lock ruling, and the owed work as a checklist at live text.
2. **The three traps re-derived at source and all three confirmed**, plus a fourth leg (`identity_id` is stamped by the handle) that [ADR-287](../decisions/ADR-287.md) F3 states only in passing.
3. **The lock decided in writing, either way, as the row demanded.** The payout path locks; it is a port member called by the decision function; `transact` still takes no lock. Slices 4, 5 and 7 inherit a decision rather than a silence.
4. **One repeated citation corrected at source.** `databaseWithdrawalBackend` does not lock inside `transact`.
5. **Row `293` in [ALLOCATION](../decisions/ALLOCATION.md) rewritten** to what actually happened, and this log and [STATE](../STATE.md) updated.

### 4. Next

**ONE MONEY-PATH ROW, WHOSE FENCE MUST NAME `apps/api/src/routes/payouts.ts`**, taking [ADR-293](../decisions/ADR-293.md) section 4 A to D in one commit set: [ADR-289](../decisions/ADR-289.md)'s seven owed lines, this ruling's four lock lines, slice 6's body (which section 2.2 already compiled), and three prose corrections. **It is the same row, not four**, because the body cannot compile until the declaration moves and the lock cannot be added without touching the same file.

### 5. Blockers

**`apps/api/src/routes/payouts.ts` is in no allocated row's fence.** That is the whole blocker and it is a dispatch gap rather than a difficulty. Everything else about slice 6 is derived, compiled and written down.

### 6. Landmines

- **The declared return type is an INVITATION and [ADR-289](../decisions/ADR-289.md) said so.** While `payouts.ts:590` promises a value nothing can supply, every fixture and every implementer between here and the row that lands the body must produce one. This session was offered exactly that trade and refused it; the next one will be too.
- **`payout-backend.ts:233-237` still explains the pre-ruling reason** for the refusal, that the member *"must return `{ eligibilitySnapshotId }`"*. It is inside this row's fence and was **deliberately not corrected**, because a comment describing a ruling in a commit that does not implement it is worse than a stale one. It belongs in the commit that lands the body.
- **The lock ruling is not code.** Until it is taken, `G-NO-IN-FLIGHT` is decided outside any lock and the losing transaction of a race gets a 500 where the contract specifies a 409.
- **`node_modules` was absent on a fresh container** and `repo-invariants.mjs` would have printed a held-invariant count over a crashed check. Row `294` owns that defect; this session simply installed first.

### 7. Files touched

`docs/decisions/ADR-293.md` (new), `docs/decisions/ALLOCATION.md`, `docs/STATE.md`, this file. **Generated as a consequence and required by `CI-06n`**: `docs/decisions/README.md`, `docs/sessions/README.md`. **`apps/`, `packages/` and `scripts/` are untouched**, verified by `git status`.
