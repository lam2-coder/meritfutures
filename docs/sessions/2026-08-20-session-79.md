---
status: approved
depends_on: [README.md]
last_updated: 2026-08-20
---

## 2026-08-20 - Session 79: the `INV-M5` collision, ruled as a merge and a renumber (money path, plan mode)
<!--index: The `INV-M5` collision, ruled as a merge and a renumber -->

- **Done:** **[ADR-063](../decisions/ADR-063.md), `status: proposed`, unsigned.** `SET A` keeps `INV-M5-17`, `18` and `19`; `SET B`'s `17` is **absorbed into `18`**; `SET B`'s `18` and `19` become `21` and `22`. [M05](../plans/M05-payout-system.md)'s table goes from **24 rows and 20 identifiers to 22 rows carrying `01` to `22`, gapless, each defined once**. Three citations re-pointed, [M20](../plans/M20-wallet.md)'s `INV-M20-06` merged, its invisible duplicate edge deleted, and **`CI06U_REGISTER` loses `M05` and `M20` entirely**. **22 of 22 gates, 30 scope cases, 62 files and 939 tests.**
- **Next:** [STATE](../STATE.md)`:286` needs `17` to become `18` and `18` to become `21`; `STATE:351` and `:1766` describe the defect and close with the ruling. **All three are S5's** and are recorded in the PR body rather than reached.
- **Blockers:** None. One file outside the stated fence was edited and is named below.
- **Landmines:**

  - **IT IS FIVE INVARIANTS ACROSS SIX ROWS, NOT SIX, AND THE BRIEF SPECIFIED A PURE RENUMBER.** `SET A`'s `INV-M5-18` and `SET B`'s `INV-M5-17` are **the same invariant**: both say the assertion runs on the query rather than the job, and **both claim to be "the fourth unsuppressible alarm"**, of which `M05` names exactly one ([`:215`](../plans/M05-payout-system.md), [`:285`](../plans/M05-payout-system.md)). **Two rows cannot both be the fourth of three.** So this needed [ADR-061](../decisions/ADR-061.md)'s merge rule and its renumber rule at once. **The arithmetic is what confirms it**: merging that pair and `INV-M5-01`'s leaves `01` to `22` gapless, where a miscount would have left a hole or a spare.

  - **THREE CITATIONS WERE NOT IN THE INVENTORY AND ALL THREE ARE OUTSIDE THE FENCE.** [`FOLD-03:98`](../plans/FOLD-03-vendor-parity-gap-fill.md), `FOLD-03:133` and `GS-288` all cite `SET A`'s `18`, all landed with PR #115 after the inventory was taken. **Both corrections pointed the same way**: keeping `SET A`'s numbering leaves six citations correct with no edit and needs three re-points, against six re-points for `SET B` with three of them unreachable from here. **The count decided it, not a preference.**

  - **THE MERGED `18` KEEPS ONE SET'S ID AND THE OTHER'S STATEMENT, AND FIVE CITATIONS DEPEND ON THE ENFORCEMENT COLUMN BEING A UNION.** They cite it for the **on-the-query-not-the-job** clause, which was `SET A`'s half and survives only because nothing was chosen between the two columns. **Narrowing it later would leave every citation resolving and every one wrong**, which is this ruling's own defect one layer down. The founder asked for the dependency to be explicit; it is stated in the ADR, in a table of what-moved-from-where, and **in `INV-M5-18`'s own cell**, so a session editing the row meets it without opening the ADR.

  - **`M05:212` AND `M05:510` CITED THE SAME INVARIANT UNDER TWO DIFFERENT NUMBERS**, twenty-nine screens apart in one document. That is the sharpest illustration of what an identifier meaning two things costs, and neither reads as wrong on its own.

  - **THE REPAIR BROKE A FALSIFY SEED, AND THE HARNESS IS WHAT SAID SO.** `CI-06u/a-repaired-register-entry-is-itself-a-finding` hardcoded `inv-m20-06`; removing that register entry left it reporting `DID NOT FAIL` on a tree where the gate was working perfectly. **A seed that names a defect the corpus is allowed to repair has an expiry date nobody records**, which is `CI-06m`'s seed's own stated reason for deriving its target. It derives now. **Two further corrections came from the harness rather than from reading**: `SEED IS STALE`, because the register's first claim is a link target no row spells literally and the gate normalises before comparing, so reimplementing that normalisation in the seed would be a second expression of one concept; and `FAILED OFF-TARGET`, because `expect` and `seed` are resolved against **different trees** and both called one function, which is the `CI-06p` seed's recorded lesson in its own words.

  - **ONE FILE OUTSIDE THE FENCE, AND IT IS THE ONE THIS SESSION BROKE.** [`falsify.mjs`](../../scripts/corpus/falsify.mjs) is not in the stated fence; the seed it carries stopped working because of this repair, so repairing it belongs here rather than to whoever meets the red next. [`gates.mjs`](../../scripts/corpus/gates.mjs) is shared with concurrent sessions and its diff is **exactly four deletions**.

  - **THE CLAIM I WAS TOLD TO CHECK GOES A THIRD WAY.** `ADR-060` item 6 says `M05:88` cites `SD-06`; session 75 found it does not. **The cell today cites both, for different propositions**: `SD-09` is the partial unique index, `SD-06` is the separate claim that `R-38` is a context gate rather than a term in `engineEligible`. Session 75's correction is commit `212e4b2` and the `ADR-060` fold added the second citation beside it. **Neither was wrong and neither needs correcting.** No edit.

  - **ONE REPAIR NO GATE COULD EVER HAVE FOUND.** `M20:114` was a byte-identical duplicate of `:112` inside a mermaid block. `CI-06u` skips fenced blocks by design, it is not a table row, and deleting it moved no gate from red to green. It was found by reading.

- **Files touched:** [ADR-063](../decisions/ADR-063.md) (new), [decisions README](../decisions/README.md), [M05](../plans/M05-payout-system.md), [SECURITY](../architecture/SECURITY.md), [M20](../plans/M20-wallet.md), [`gates.mjs`](../../scripts/corpus/gates.mjs) (four deletions), the two `adr_count` spans in [INDEX](../INDEX.md) and [STATE](../STATE.md) via `gates.mjs generate`, this log and [`sessions/README.md`](README.md). **Outside the fence and named:** [`falsify.mjs`](../../scripts/corpus/falsify.mjs). **[STATE_MACHINES](../architecture/STATE_MACHINES.md) was not touched**: S2 holds it.
