---
status: approved
depends_on: [README.md]
last_updated: 2026-08-22
---

## 2026-08-22 - Session 134 (P2-b): `replay` joins the rules engine's public surface (money path)

- **Objective:** write [ADR-078](../decisions/ADR-078.md) and export `replay` from `packages/rules-engine`, retiring the scaffold's `evaluate` stub.
- **Done:** **[ADR-078](../decisions/ADR-078.md), `status: proposed`, approval line unsigned.** [`replay.ts`](../../packages/rules-engine/src/replay.ts) transcribes M01 section 3.7; `replay` and `ReplayAssertionError` are exported and `evaluate` is deleted. **`PT-06`'s arrival-order permutation is LIVE and passing**, three tests where one was a named skip. Eight assertions of `replay`'s own across two new test files.
- **The count:** **`pnpm run verify` exits 0.** 28 of 28 gates, `falsify` reports **all 28 pass clean and fail dirty**, 52 scope cases and 10 loader cases. **101 files / 1417 passed / 44 skipped**, against 1404 and 45 on `main`.
- **Next:** **the second code path still exists.** [`apps/worker/src/batch/replay.ts`](../../apps/worker/src/batch/replay.ts)`:253` folds its own loop, so M01 3.7's *"there is no second code path"* is still false on `main` (sessions 138 and 140). **`GS-071` has since left `writable`**, landed as `covered-elsewhere` by [session 139](2026-08-22-session-139.md); `writable` now holds no row. Corrected rather than left standing.
- **Blockers:** **this branch waits on the founder's E2 read and must not be merged before it.**
- **Landmines:**

  - **THE BRIEF'S SITE COUNT WAS WRONG AND APPLIED AS WRITTEN IT WOULD HAVE REFUSED THE EXPORT.** It said *"two of the three sites say export and one does not, which is the opposite balance"* from `clampPayout`. Section 1.3's layout lists `payout/clamp.ts` at `:65` exactly as it lists `replay.ts` at `:67`, alongside `day/breach.ts`, `day/floor.ts`, `day/counters.ts`, `payout/gates.ts` and `hash.ts`, **none of whose functions are exported**. A file in a layout is not a vote for export. The balance is **identical**, and what rules it is 1.3's own rationale: withholding **serves** it for the clamp, because `evaluatePayout` still reaches the clamp, and **defeats** it for the fold, because nothing reaches a whole account life. The miscount is also on `main` at [P2 build plan](../plans/P2-rules-engine-build.md)`:296`, outside this fence, and is named as owed.

  - **THE APPROVAL LINE'S CANDIDATE CLAUSE WAS FALSE ARITHMETIC.** *"The engine's public surface gains exactly one name"* is never true here: deleting `evaluate` while adding `replay` is net zero, and adding `ReplayAssertionError` makes it **plus two, minus one**, thirteen names to fourteen. The clause now names the **list** rather than a count, because a list cannot drift the way a count can and the list is already machine-checked.

  - **`ReplayAssertionError` IS DELIBERATELY NOT AN `EngineInvariantError`.** [`errors.ts`](../../packages/rules-engine/src/errors.ts) draws one line: the engine **throws** when its own arithmetic broke an invariant and **refuses** through the assertion channel when the input is wrong, *"because the day's data being wrong is not the batch's crash"*. A refused day is the second kind. Replay throws anyway because it **cannot represent a skipped day**: a `RuleState[]` missing one is indistinguishable from a complete history.

  - **`replay` REFUSED THE DEMO'S OWN CORPUS AND WAS RIGHT TO.** The permutation property, written first over 30 sessions of the demo population, stopped at `2026-12-08` on `opening_mismatch` (`INV-18`). [`scripts/demo/fold.ts`](../../scripts/demo/fold.ts) folds in **segments**, restarting the simulator when an account passes and is re-provisioned at `size_cents`; a re-provisioned opening balance is not the prior closing plus an adjustment. **The demo's stream can be two lives and `replay`'s subject is one.** The corpus is now the contiguous prefix, taken by **asking `replay` where it stops** rather than hard-coding a day the simulator could move.

  - **THE BREAK'S COUNTERFACTUAL WAS WRONG THE FIRST TIME.** Appending the marks again does not fold past the terminal day: the sort puts the duplicates back in day order and the break fires before them, which is the break **working**. What earns the break its place is folding the day after the terminal one against the terminal state, which `advanceDay` refuses. Recorded in the test rather than quietly replaced.

  - **`tsc` CAUGHT WHAT `vitest` DID NOT.** `fc.shuffledSubarray` takes a mutable array and the corpus is `readonly`. Eleven tests passed while the typecheck failed, which is the whole reason `verify` runs both.

  - **TWO SENTENCES IN `evaluate.golden.test.ts` HAD ALREADY GONE FALSE** before this session touched it: *"four of those six are now written"* (all six are) and *"`evaluate` ... still what CI-03's polarity probe folds"* (`engineIsIdentityStub` folds `advanceDay`). Both corrected. **The file name now outlives its subject** and is deliberately NOT renamed: a rename is outside this fence and risks a `CI-06a` link. **Recorded for whoever holds `packages/rules-engine/test` next.**

- **Files touched:** [`replay.ts`](../../packages/rules-engine/src/replay.ts), [`replay.test.ts`](../../packages/rules-engine/test/replay.test.ts) and [`replay.property.test.ts`](../../packages/rules-engine/test/replay.property.test.ts) (all new), [`index.ts`](../../packages/rules-engine/src/index.ts), [`evaluate.golden.test.ts`](../../packages/rules-engine/test/evaluate.golden.test.ts), `evaluate.test.ts` and `evaluate.property.test.ts` (deleted), [`replay-determinism.property.test.ts`](../../scripts/demo/test/replay-determinism.property.test.ts), [ADR-078](../decisions/ADR-078.md) (new), [ALLOCATION](../decisions/ALLOCATION.md) and [decisions README](../decisions/README.md), [STATE](../STATE.md), this file and its row.

### The fence extensions, both forced

**[`evaluate.golden.test.ts`](../../packages/rules-engine/test/evaluate.golden.test.ts)** pins the engine's exact export list **and** asserted `typeof engine.evaluate`, so deleting the function without editing it ships a red tree. **[decisions README](../decisions/README.md)** is `CI-06n`: an ADR with no row there is a red gate. Neither was chosen. **A session fenced out of the file that validates its own change cannot end green**, which is the shape that made session 127 unmergeable.
