---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 599: the 80 reproduce exactly and 53 are repaired with the occurrence named, the seam a dispatch called unrepaired was repaired by a sibling nineteen minutes earlier, and a line map that was wrong once

<!--index: **THE ROW IS [ADR-404](../decisions/ADR-404.md), `proposed` and UNSIGNED. CONTROL OVER THE CORPUS'S OWN POINTERS, `E2` READ OWED. NOTHING MINTED.** **THE 80 IS 80**, and every census cell [ADR-401](../decisions/ADR-401.md) published reproduces on a third base through an independently built copy of the checks' own reader, under both instruments, including that entry's corpus-wide `git log -S` row against [ADR-400](../decisions/ADR-400.md)'s. **ITS SECTION 13 ITEM 2 IS REFUTED AND NOTHING IT DID CAUSED THAT**: [ADR-337](../decisions/ADR-337.md):202 was repaired by [ADR-403](../decisions/ADR-403.md) at `02:54:04` and that entry was written at `03:13:22` on a parallel branch whose base still carried `:8412`, so **an owed list is a dated record of one row's base**, the claim was TRUE when written, no annotation is owed, and the seam has **ZERO** surviving instances. **65 of the 80 have an occurrence that survives byte-identical**, and the coordinate is TRACKED through the diff between the two blobs rather than chosen from the 28.0 percent band. **53 REPAIRED in 51 units carrying 55 pointers across 25 documents, all 55 verified byte-identical at the new coordinate**, each naming which occurrence beside the retired coordinate. **27 REFUSED IN SIX SHAPES, THREE OF THEM PROPERTIES OF THE WAVE RATHER THAN OF THE POINTER**, including **4 derived, verified and then REVERTED** because they point into row `406`'s file. **THE LINE MAP WAS WRONG ON ITS FIRST RUN** and would have produced 28 bad pointers; it was caught by arithmetic, not by care. **FOUR SEEDS AND THE FIRST REFUTED THIS ROW'S OWN PREDICTION.** Seed B is GREEN at 35 of 35 on a badly wrong coordinate in a dated record, so **nothing in this repository checks any of the 55**. Every file line-count neutral, 45 against 45, citation-repair exception UNSPENT. Suite **336 / 8,109 / 10 / 0**, delta ZERO; gates **33 of 33**, invariants **35 of 35**. **NO REPOSITORY ON ARRIVAL FOR THE SIXTH CONSECUTIVE WAVE**, `is-shallow` **`false`** at 4,338 commits -->

### 1. The objective

Derive the 80 repairable pointers whose subject is not unique in the cited file rather than inherit them, repair those that survive [ADR-388](../decisions/ADR-388.md)'s two gates, [ADR-397](../decisions/ADR-397.md)'s precondition and [ADR-396](../decisions/ADR-396.md)'s refusal shapes, each carrying the sentence [ADR-401](../decisions/ADR-401.md) section 8 rules, and discharge [ADR-398](../decisions/ADR-398.md)'s surviving seam instance. One objective, non-money.

### 2. Done

- **[ADR-404](../decisions/ADR-404.md) written**, `proposed`, approval line UNSIGNED.
- **The 80 re-derived at 80** through the checks' own reader relocated outside the repository, and **every figure [ADR-401](../decisions/ADR-401.md) published reproduced**: 495 on its own base, 442 on the findings-eligible population, all ten dated census cells under both instruments, and its corpus-wide `346 / 85 / 46 / 7 / 9`.
- **53 of the 80 repaired**, 51 units, 55 pointers, 25 documents, all verified byte-identical at the new coordinate and holding the name.
- **[ADR-401](../decisions/ADR-401.md) section 13 item 2 refuted** at primary source and dated to the minute against [ADR-403](../decisions/ADR-403.md)'s repair commit. **No annotation written**, because the claim was true when written.
- **Four seeds**, one of which refuted this row's own prediction about the `DOC_CITATIONS_OWNED_ELSEWHERE` register.
- **Registries appended**: [ALLOCATION](../decisions/ALLOCATION.md) row `404` TAKEN with `405` and `406` RESERVED naming nothing, [STATE](../STATE.md) section, both generated `README` rows.

### 3. Next

The 27 refused, section 9 of the entry. **Eleven of them are refused by this wave rather than by the pointer** and are repairable by a row that does not share a fence with row `406`; the 4 in [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) were fully derived and verified before being reverted, so that row starts from a list rather than from nothing.

### 4. Blockers

None. `pnpm run verify` and [`falsify.mjs`](../../scripts/corpus/falsify.mjs) were forbidden and were not run.

### 5. Landmines

- **A LINE MAP BUILT FROM `git diff -U0` HUNKS IS OFF BY ONE ON PURE INSERTIONS UNLESS THE ANCHOR LINE IS MAPPED FIRST.** A hunk reading `@@ -100,0 +101,5 @@` inserts AFTER old line 100, so old 100 is still unchanged context. Consuming it as replaced walks every later coordinate forward by the size of the insertion. **This row shipped that map into a first run and it reported 37 byte-identical images and 28 whose text had changed.** A line that moves without being edited cannot change text. **The 28 was a contradiction rather than a result**, and it is the only reason the bug was found; the corrected run reports 65 and zero.
- **A `DOC_CITATIONS_OWNED_ELSEWHERE` ENTRY IS KEYED ON `(file, cites, name)` AND CARRIES NO LINE.** A citation registered as a known finding at TWO sites in one document stays matched when one site is repaired, so `RI-16` stays green and a row reads that as permission. **It goes red only when the last site goes.** Seed A and seed A2.
- **NOTHING IN THIS REPOSITORY CHECKS A POINTER IN A DATED RECORD.** Seed B moved a repaired coordinate to `` `scope.ts:1,2` `` and invariants stayed at 35 of 35. Seed C put the identical wrongness on a live citation and it went red at *"303 lines away"*. **A green run after a repair sweep in dated records is evidence of nothing about the sweep.**
- **AN OWED LIST CANNOT SEE INSIDE ITS OWN WAVE.** [ADR-401](../decisions/ADR-401.md) recorded an item that a sibling had discharged nineteen minutes earlier on a branch it could not see. The entry is correct about its own base and stale on the integration branch. **Re-read the site before inheriting an owed item**, and do not read a dated owed list forward as a live claim; the dispatch did, and turned a true measurement into *"repaired by nobody"*.
- **`gates.mjs generate` IS PART OF REPAIRING A SESSION LOG, NOT AN AFTERTHOUGHT.** Eight of the repaired logs carry their `<!--index:` line verbatim into [`sessions/README.md`](README.md)'s generated span. `CI-06g` was RED at 32 of 33 between the repairs and the regeneration, exactly as [ADR-396](../decisions/ADR-396.md) section 8 records.
- **THE CONTAINER ARRIVES WITH NO REPOSITORY, SIXTH CONSECUTIVE WAVE.** `git rev-parse --is-shallow-repository` returned **`false`**; 4,338 commits, 15 seconds, 182 MB. **This dispatch asserted no shallowness, which is the first time in six waves**, so half of what five entries asked for has landed.

### 6. Files touched

[ADR-404](../decisions/ADR-404.md) (new), [ALLOCATION](../decisions/ALLOCATION.md), [decisions/README](../decisions/README.md), [`sessions/README.md`](README.md), [STATE](../STATE.md), this log, and the 25 documents carrying the repaired pointers: [STATE](../STATE.md), [ADR-176](../decisions/ADR-176.md), [ADR-182](../decisions/ADR-182.md), [ADR-190](../decisions/ADR-190.md), [ADR-192](../decisions/ADR-192.md), [ADR-213](../decisions/ADR-213.md), [ADR-217](../decisions/ADR-217.md), [ADR-251](../decisions/ADR-251.md), [ADR-284](../decisions/ADR-284.md), [ADR-363](../decisions/ADR-363.md) and fifteen session logs. **`scripts/corpus/**` and `packages/tooling/**` were read and never written.**
