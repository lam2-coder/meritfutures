---
status: approved
depends_on: [README.md]
last_updated: 2026-08-28
---

## 2026-08-28 - Session 339: the last two codes are both assets, the hole closes to a refusal, and the sentence that decides it was one line above the one two entries read

<!--index: **[ADR-186](../decisions/ADR-186.md) LANDS, `status: proposed`, approval line UNSIGNED, and [`0055_last_two_ledger_kinds.sql`](../../packages/db/migrations/0055_last_two_ledger_kinds.sql) IS TAKEN AND SPENT.** `psp_clearing` is an `asset`, `reserve` is an `asset`, `ledger_accounts_kind_matches_code` names all seven codes and its `ELSE true` closes to `ELSE false`. **THE CONSTRAINT IN FORCE WAS READ FROM `pg_get_constraintdef` AND NOT FROM A FILE NAME, and the hole was watched ACCEPTING first: TEN acceptances**, `psp_clearing` and `reserve` each admitted as `asset`, `liability`, `revenue`, `expense` AND `equity`, which is wider than any prior entry recorded. **THE RULING TURNS ON [`checkout.ts:1679`](../../apps/api/src/routes/checkout.ts), ONE LINE ABOVE THE SENTENCE [ADR-177](../decisions/ADR-177.md) AND [ADR-181](../decisions/ADR-181.md) EACH QUOTED**: *"There is no processor in this transaction, so there is nothing IN clearing."* **That IS [ADR-181](../decisions/ADR-181.md) section 8's *"funds held at a payment provider for onward payment"*, which that entry called the coherent alternative NOT written in this tree and put to the founder as what would overturn its refusal of [ADR-174](../decisions/ADR-174.md) section 3 shape (iii).** It does not overturn it: **both readings are ASSETS and the role shape (iii) needs is a firm-scoped `liability`**, so the alternative reaches the same answer as the reading already on the table. **`reserve` IS THE WEAKER RULING AND THE ENTRY SAYS SO FIRST**: both of `ADR-177`'s grounds STILL HOLD and were re-derived at source, and what changed is the ground for DISCOUNTING the prose that remains, because [`0052:114-116`](../../packages/db/migrations/0052_chart_of_accounts.sql) distrusted GLOSSARY's *"funds set aside"* as *"asset-shaped prose of exactly the kind that turned out to be wrong for firm_treasury"* and [ADR-180](../decisions/ADR-180.md) ruled that prose RIGHT. **The `equity` reading is NAMED and refused on the anchor**: `treasury_balances.source` is CHECKed to `('provider_api','manual_attestation')` and an appropriation of retained earnings has no rail balance. **RULING 3 IS THE ONE THAT BINDS A FUTURE SESSION AND THE ENTRY SAYS SO**: `ELSE false` refuses an eighth code until its kind is ruled in the migration that mints it, `0009:46`'s rule one column over, and **deleting the ELSE would not be the same closure** because a CASE with no ELSE returns NULL and a CHECK passes on NULL. **WATCHED IN BOTH STATES**: with `code_is_declared` dropped inside a transaction the eighth code is REFUSED under `ELSE false` and LANDS under `0053`'s `ELSE true`. **WHAT IT COSTS THE EIGHTH CODE IS THAT THE EIGHTH CODE GETS MORE LIKELY**: after `0055` no firm code can hold a `liability`, so shape (iii) is UNREPRESENTABLE rather than refused, and shape (i) or (ii) is what is left. **NO ROW IS SEEDED AND THAT IS ARGUED**, because `0052` seeded for postings that resolve and nothing posts against either, and a `reserve` ROW is the first object a reader could sum and mistake for the figure `SD-M5-03` anchors outside this ledger. **FOUR FINDINGS NOT IN THE DISPATCH.** (1) The `checkout.ts:1679` sentence. (2) **[ADR-181:42](../decisions/ADR-181.md)'s stated MECHANISM IS FALSE as of this tree**: the RCR numerator cannot see a ledger balance, executed with **777,000c** in a `reserve` ledger account and `rcr_bp` unmoved at **8000**, and **the only `treasury_balances.account_code` value anywhere in this tree is `'payout_wallet'`**, which is not one of the seven; the CONCLUSION survives on `SD-M5-03`'s other ground. (3) **[`0054:62-64`](../../packages/db/migrations/0054_identity_ledger_accounts.sql)'s named hole is NARROWED AND NOT CLOSED**: identity-scoped `reserve` as `liability` refused, as `asset` still ACCEPTED, because the DDL never ties `code` to `scope`. (4) **`ledger_accounts_kind_matches_code` HAD NEVER BEEN WATCHED FIRING BY ANYTHING THAT RUNS**, only by a text reader that would stay green against a constraint that failed to install; `LEDGER-K1` a/b/c is the repair and all three were watched FAILING under their own seeded defect. **AND IT REPORTS PRESSURE ON [ADR-180](../decisions/ADR-180.md)'S HIGHEST-WEIGHTED GROUND RATHER THAN HIDING IT.** **THIS ENTRY HAD TO EDIT `packages/rail`, the fourth in this sequence to.** `0009`, `0027`, `0038`, `0052`, `0053` and `0054` byte for byte unchanged, no eighth code, no name, vocabulary closed at seven, `M05` section 2.1 untouched. 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean, **218 files / 5,139 passed / 6 skipped both before and after**, baseline reproduced at `5d1d28b9` first, `falsify` clean with the tree clean after. -->

**Done:** [ADR-186](../decisions/ADR-186.md); [`0055_last_two_ledger_kinds.sql`](../../packages/db/migrations/0055_last_two_ledger_kinds.sql), `E2 READ: MONEY PATH`; `LEDGER-K1` a, b and c in [`probe_ledger_constraints.sql`](../../scripts/db/probe_ledger_constraints.sql); the three armed cases in [`chart-of-accounts-kinds.test.ts`](../../packages/ledger/test/chart-of-accounts-kinds.test.ts) replaced rather than emptied, two in [`in-flight-obligation.test.ts`](../../packages/ledger/test/in-flight-obligation.test.ts) strengthened, `0055` registered as the fourteenth site in that file's eighth-code price registry, [`identity-provisioning.test.ts`](../../packages/ledger/test/identity-provisioning.test.ts)'s hole comment corrected, one forced case in [`lt-07.test.ts`](../../packages/rail/test/lt-07.test.ts); [ALLOCATION](../decisions/ALLOCATION.md) rows `186` and `0055` amended in place ([ADR-065](../decisions/ADR-065.md) T3); [INDEX](../INDEX.md)'s probe row; the [STATE](../STATE.md) append; this log and its generated `README` row.

**Next:** **the eighth code is now the cheapest remaining shape and its price is unchanged at thirteen files plus this constraint**, which [`in-flight-obligation.test.ts`](../../packages/ledger/test/in-flight-obligation.test.ts) registers and scans for in both directions. A session that takes it needs a fence covering [GLOSSARY](../GLOSSARY.md)'s class list, [`data-model/ledger_accounts.md`](../architecture/data-model/ledger_accounts.md), [`data-model/README.md`](../architecture/data-model/README.md), [`schema.ts`](../../packages/db/src/schema.ts), [`scope.ts`](../../packages/db/src/scope.ts), [`checkout.ts`](../../apps/api/src/routes/checkout.ts) and [`lt-07.test.ts`](../../packages/rail/test/lt-07.test.ts), which is the seven outside [ADR-181](../decisions/ADR-181.md)'s. **Separately and smaller: the `code`-to-`scope` tie**, which no constraint in this schema makes and which is what leaves finding 3's hole one kind wide. **And [ADR-181](../decisions/ADR-181.md) section 6's stranded obligation is still owed a ninth transaction**, untouched here.

**Blockers:** none for this entry. **The founder question is a READING and not a derivation**: whether `checkout.ts:1679` and `:1682` are statements about a class or two sentences written to explain why a wallet purchase touches no clearing account.

**Landmines:** **`ELSE false` is the only thing in this diff that binds a session that has not been dispatched yet.** It is unreachable today and its whole value is prospective, and a founder who reads it as this session pricing somebody else's decision is reading it fairly. Second: **`reserve` is ruled from a literal no behaviour depends on.** Nothing posts against it and nothing reads it, so no test and no probe in this tree can falsify the ruling by execution; `LEDGER-K1b` watches the constraint ACCEPT `asset`, which proves the constraint works and says nothing about whether `asset` is right. Third: **finding 3's hole is narrower and still open**, and a reader who sees `('reserve','liability','identity')` refused may conclude the identity-scoped case is closed; `('reserve','asset','identity')` still lands. Fourth: **this entry's citations into [ALLOCATION](../decisions/ALLOCATION.md) are by row number and not by line**, deliberately, because rows `186` and `0055` both grew by more than a screen here.

**Files touched:** `docs/decisions/ADR-186.md`, `packages/db/migrations/0055_last_two_ledger_kinds.sql`, `scripts/db/probe_ledger_constraints.sql`, `packages/ledger/test/chart-of-accounts-kinds.test.ts`, `packages/ledger/test/in-flight-obligation.test.ts`, `packages/ledger/test/identity-provisioning.test.ts`, `packages/rail/test/lt-07.test.ts`, `docs/decisions/ALLOCATION.md`, `docs/decisions/README.md` (generated span), `docs/INDEX.md`, `docs/STATE.md`, `docs/sessions/README.md` (generated span), this file, plus the generated `migration_files`, `e2_files` and `adr_count` spans the runner rewrote across five further documents.

---

### 1. The constraint in force, read from the database

**A CHECK cannot be extended in place, so the constraint the database HAS is the last one added, whatever file added it.** `0001` through `0054`, forward-only from empty, PostgreSQL 16, `ON_ERROR_STOP`.

| Figure | Source | Value |
|---|---|---|
| Migrations applied | file count | **54** |
| Base tables | `information_schema.tables` | **114** |
| `ledger_accounts` rows | `count(*)` | **2** |
| Non-internal triggers | `pg_trigger` | **22** |
| Kind constraint | `pg_get_constraintdef` | five arms, `ELSE true` |

**And the hole, watched ACCEPTING rather than asserted open.** Two prior entries tried four of these ten between them.

| Code | `asset` | `liability` | `revenue` | `expense` | `equity` |
|---|---|---|---|---|---|
| `psp_clearing` | ACCEPTED | ACCEPTED | ACCEPTED | ACCEPTED | ACCEPTED |
| `reserve` | ACCEPTED | ACCEPTED | ACCEPTED | ACCEPTED | ACCEPTED |

---

### 2. The same ten after `0055`

| Code | `asset` | `liability` | `revenue` | `expense` | `equity` |
|---|---|---|---|---|---|
| `psp_clearing` | **ACCEPTED** | REFUSED | REFUSED | REFUSED | REFUSED |
| `reserve` | **ACCEPTED** | REFUSED | REFUSED | REFUSED | REFUSED |

Every refusal names `ledger_accounts_kind_matches_code`. **The five arms `0052` and `0053` wrote still refuse** (`firm_treasury` and `fees_revenue` as `liability`), **the ruled kinds are still ACCEPTED with the seeded row deleted inside the transaction first**, and an UPDATE reclassifying the seeded `firm_treasury` row is refused, so the constraint binds both paths.

---

### 3. The `ELSE` word, watched in both states

The row is `('withdrawals_payable','liability','firm')`, which is the spelling [ADR-181](../decisions/ADR-181.md) section 5 used and refused to mint.

| Constraint in force, and what guard one is doing | Result |
|---|---|
| `0055` `ELSE false`, with `code_is_declared` STANDING | REFUSED by `ledger_accounts_code_is_declared` |
| `0055` `ELSE false`, with `code_is_declared` DROPPED in the transaction | **REFUSED by `ledger_accounts_kind_matches_code`** |
| `0053` `ELSE true`, with `code_is_declared` DROPPED the same way | **LANDS** |

**That difference is the whole content of ruling 3.** [ADR-181](../decisions/ADR-181.md) section 4 measured that minting the eighth code must move `0009`'s CHECK and `0027`'s `LEDGER-C2` together. It is now three, and the third fails loudly rather than admitting.

---

### 4. The RCR, and the mechanism [ADR-181](../decisions/ADR-181.md) states that this tree does not have

That entry refuses `reserve` for shape (iii) partly on the ground that giving it this posting *"makes the numerator of the breaker's ratio grow as withdrawals are approved"*.

**The numerator cannot see a ledger balance.** `reserve_coverage_snapshots.reserve_cents` is tied by `RESERVE-C1` and by a foreign key to the `treasury_balances` row the snapshot names; `treasury_balances` is written from a provider API or a dated manual attestation; and of the functions this schema installs, exactly three read `ledger_entries` and they are the zero-sum, opposite-signs and adjustment-posting asserts. **No function reads both paths.**

**Executed.**

| What | Value |
|---|---|
| `reserve` LEDGER balance, posted against `fees_revenue` | **777,000c** |
| `treasury_balances` reading the snapshot anchors to | 2,000,000c |
| `reserve_cents` stored | 2,000,000c |
| `cvar99_cents` | 2,500,000c |
| **`rcr_bp`** | **8000** |

`RESERVE-C1` passed, because the copy equals the rail's number and the ledger is not an input. **The conclusion survives on `SD-M5-03`'s other ground and on the class ruling; the reason is what was wrong**, which is the shape [ADR-180](../decisions/ADR-180.md) found in [`0052`](../../packages/db/migrations/0052_chart_of_accounts.sql).

**And the second measurement in the same place**: the only `treasury_balances.account_code` value anywhere in this tree is `'payout_wallet'` ([`probe_reserve_coverage.sql:64`](../../scripts/db/probe_reserve_coverage.sql)), which is not one of the seven. The anchor has never named the ledger `reserve` account.

---

### 5. `LEDGER-K1`, and the three seeded defects it was watched failing under

| Block | What it watches | Seeded defect | Result |
|---|---|---|---|
| **K1a** | `reserve` as `liability` REFUSED | the database rolled back to `0053`'s constraint | probe exits **3**, *"PROBE FAILED: LEDGER-K1 admitted reserve as a liability"* |
| **K1b** | `reserve` as `asset` ACCEPTED | `reserve`'s arm inverted, every other arm kept | the INSERT raises on the constraint |
| **K1c** | the `ELSE` arm refuses an undeclared code | `ELSE true` restored | *"PROBE FAILED: LEDGER-K1 ELSE arm admitted an undeclared code with an unruled kind"* |

**The a/b split is the probe file's own rule**: a probe that only ever attempts forbidden things passes against a guard that rejects everything. **K1c is the half no text reader can see at all**, which is why [`chart-of-accounts-kinds.test.ts`](../../packages/ledger/test/chart-of-accounts-kinds.test.ts) staying green was never evidence this constraint was wired up.

---

### 6. Verification

Each command run separately, `pnpm install` first, `pnpm run verify` never run, and the `main` baseline reproduced at `5d1d28b9` before a line changed.

| Command | Baseline | After |
|---|---|---|
| `gates.mjs generate` then `check` | 33 of 33 | **33 of 33** |
| `repo-invariants.mjs` | 16 of 16 | **16 of 16** |
| `pnpm run typecheck` | exit 0 | **exit 0** |
| `pnpm run lint` | exit 0 | **exit 0** |
| `pnpm run format:check` | clean | **clean** |
| `pnpm vitest run` | 218 files / 5,139 passed / 6 skipped | **218 / 5,139 / 6** |
| `falsify.mjs` | clean | **clean**, tree clean after |
| Migrations from empty | 54 files, 114 tables, 2 rows, 22 triggers | **55 files, 114 tables, 2 rows, 22 triggers** |
| `probe_ledger_constraints.sql` | pass | **pass, with three new blocks** |
| `probe_reserve_coverage.sql` | pass | **pass** |

**The test delta is zero in COUNT and not in content.** Four cases were rewritten in place and none deleted, because a `for` loop over an emptied list passes while reading nothing, which is the shape of green [`chart-of-accounts-kinds.test.ts`](../../packages/ledger/test/chart-of-accounts-kinds.test.ts) exists to refuse.
