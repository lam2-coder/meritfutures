---
status: approved
depends_on: [README.md]
last_updated: 2026-08-25
---

## 2026-08-25 - Session 189: the operator path boundary was an assumption on `main` this morning, it is a property now, and the hole was wider than the session that found it reported
<!--index: **[ADR-098](../decisions/ADR-098.md) proposed, `status: proposed`, approval line UNSIGNED, and `RI-09` written in the same branch.** **RULED: the operator path's absence from every UI deployable is a PROPERTY of this repository and not an assumption that has held because nobody wrote the file.** **THE FINDING WAS RE-DERIVED ON `origin/main` AT `38e420b` WITH NO FRAMEWORK ANYWHERE IN THE WORKSPACE**, in a throwaway `git worktree`, because [ADR-095](../decisions/ALLOCATION.md) measured it on an unmerged branch that has Next.js installed. **The clause reproduces exactly, so the framework is not the cause, and the hole is WIDER than reported rather than narrower.** **FIVE route files across ALL FOUR non-`api` deployables** -- session 183's own `apps/portal/src/app/api/v1/admin/payouts/route.ts`, plus `/api/v1/internal/jobs` on the **marketing origin**, the same under `apps/admin` and `apps/worker`, and a **public** `/api/v1/checkout` inside `apps/portal` -- leave the estate **BYTE-IDENTICAL to the clean baseline**: lint **0**, invariants **7 of 7**, typecheck **0**, gates **30 of 30**, `surface.test.ts` **10 passed**, full suite **106 files / 1,531 passed / 1 skipped**, the same 1,531. **The gates and the suite had not previously been counted among the green results.** **NO EXISTING MECHANISM IS DEFECTIVE AND NONE IS A NEAR MISS**: `RI-04` reads dependency fields and a route handler declares no dependency, `merit/no-raw-db-client` bans a client specifier and a route imports nothing, `surface.ts` classifies paths **for `apps/api`** and cannot see `apps/portal`. **`RI-04` has been five deployables wide since [session 153](2026-08-24-session-153.md) and still never asks what one SERVES.** **THE CLASS GENERALISES AND THE SHAPE DOES NOT, which is what this session was dispatched to decide**: [ADR-096](../decisions/ADR-096.md)'s remedy is `RI-01`'s shape one directory over, `RI-01`'s shape reads a MANIFEST, and a route handler declares nothing, so pointing it here would have produced a check that cannot see its own subject. **`RI-09` reads `BASE_PATH` and `OPERATOR_PREFIXES` out of [`surface.ts`](../../apps/api/src/surface.ts) and keeps NO copy**, on `RI-06`'s relationship to the ESLint plugin: a third prefix moves the finding **with no edit to the checker**, and removing the list makes it **ERROR rather than pass**. Three path shapes; `ROUTING_ROOTS` deliberately only the two Next.js spells, because a guessed `routes` would refuse [`apps/site/src/routes/`](../../apps/site/src/routes/page.ts), which exists and is render functions. **Ten cases, each watched failing on its own seed, and the second matters most: the same path under `apps/api` must PASS**, as must [`apps/portal/src/api/types.ts`](../../apps/portal/src/api/types.ts), which `ADR-083` section 3 cites as evidence the portal is a CLIENT. **Mutation-checked**: removing the route-group skip, the owner exemption or the operator branch each fails exactly one case. **`RI-08` IS CLAIMED BY [ADR-096](../decisions/ADR-096.md) IN PROSE AND THIS IS `RI-09`**, the gap deliberate and stated in the checker's header; **the reason a collision was possible is that ALLOCATION has four tables and NO `RI-nn` table**, `OI-27`'s class, reported and not repaired. **[ADR-083](../decisions/ADR-083.md) IS SIGNED AND NOT EDITED**, and its **SIGNED clause's second half carries the probe `ADR-095` moves** -- *"no `package.json` under `apps/` declares a `build` script: the count is 0"* -- **verified still 0 on `main`, not 0 on #240**, a fact the founder should meet at the merge. **The direction it fails in is SILENCE and it fails OPEN**, and after this entry the silence is **bounded rather than removed**: section 5 enumerates four things a path check cannot see. `verify` **exit 0** at **8 of 8 invariants** and **106 files / 1,541 passed / 1 skipped**, 30 of 30 gates. **Nothing under `apps/` written**, no `CI-06` letter, no gate register, no `VG` row, no probed artifact, no migration, no `SD-nn` -->

- **Objective:** rule whether the operator path's absence from the public deployment is a PROPERTY of this repository or an ASSUMPTION that has held because nobody wrote the file, and, if it must be a property, write the mechanism. **Reproduce the inherited finding before ruling on it.**
- **Done:** [ADR-098](../decisions/ADR-098.md) (new, `status: proposed`, approval line UNSIGNED), `RI-09` in [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) with ten cases in [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts), [ALLOCATION](../decisions/ALLOCATION.md)'s `098` row amended in place, [decisions/README](../decisions/README.md) row and both `adr_count` spans generated, [STATE](../STATE.md) appended. **`pnpm run verify` exit 0 end to end.**
- **Next:** **the founder's signature**, and with it the two `ADR-083` items in section 9. Then the **source half** of this boundary, which does not exist: `RI-09` is the PATH half exactly as `RI-01` is the manifest half of the engine's purity boundary, and `merit/engine-purity` has no counterpart here. `ADR-095`'s `F4` rule and [ADR-096](../decisions/ADR-096.md)'s `RI-08` both remain owed to `P4-e` and neither is closed by this session.
- **Blockers:** none. Nothing here waits on [#240](https://github.com/lam2-coder/meritfutures/pull/240): the gap predates the framework and was measured without it.
- **Landmines:**
  - **`RI-09` REFUSES A SPELLING AND NOT A SERVED ROUTE, AND A GREEN `RI-09` MUST NOT BE QUOTED AS "no UI deployable serves an operator path".** A catch-all `[...slug]/route.ts`, a `next.config` rewrite, a middleware or a hand-written router table is invisible to it. `ADR-098` ruling 3 exists to stop that quotation and the `covers` line enumerates **four** things it cannot see rather than three.
  - **`ROUTING_ROOTS` IS `['app', 'pages']` AND ADDING A THIRD IS A MEASUREMENT, NOT A TIDY-UP.** Adding SvelteKit's `routes` **today** turns [`apps/site/src/routes/`](../../apps/site/src/routes/page.ts) into a candidate: it holds seven pure render functions and no `api`, `admin` or `internal` subdirectory, so it passes **by luck of its contents** rather than by design. A session adding `routes` must check that directory first.
  - **THE NUMBER IS `RI-09` AND THE GAP AT `RI-08` IS DELIBERATE.** [ADR-096](../decisions/ADR-096.md) claims `RI-08` in prose, in [ALLOCATION](../decisions/ALLOCATION.md)'s `096` row and in [STATE](../STATE.md), assigned to `P4-e`. **Renumbering `RI-09` to close the gap falsifies three landed citations.** The checker's header says so; **the header is the only control**, because there is no `RI-nn` register.
  - **THE FIXTURE NOW HOLDS ITS OWN `surface.ts` AND THAT IS THE POINT.** `cleanTree()` gained `apps/api/src/surface.ts` with its own `BASE_PATH` and `OPERATOR_PREFIXES`. **It is not a second copy of the repository's list to be kept in step**: the case that rewrites it and watches the finding follow is what proves the checker reads the tree. A session that "fixes" the fixture to import the real constants deletes that proof.
  - **`ADR-083`'s SIGNED CLAUSE STOPS HOLDING WHEN [#240](https://github.com/lam2-coder/meritfutures/pull/240) MERGES.** Its second half is *"no `package.json` under `apps/` declares a `build` script: the count is 0"*, verified **0** here and **not 0** there. `ADR-095` section 9 item 3 reports the section 5 half; **this half is reported here for the first time.** Neither entry may be edited by a session.
  - **The five seeds lived in a throwaway `git worktree` off `origin/main` and were never in this working tree.** `git status --porcelain apps` reports zero on this branch. A session reproducing the finding must do the same: `apps/portal`, `apps/site`, `pnpm-lock.yaml` and `pnpm-workspace.yaml` are all held by [#240](https://github.com/lam2-coder/meritfutures/pull/240) awaiting the founder.
- **Files touched:** [docs/decisions/ADR-098.md](../decisions/ADR-098.md) (new), [`packages/tooling/checks/repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs), [`packages/tooling/test/repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts), [ALLOCATION](../decisions/ALLOCATION.md) (its `098` row, amended in place), [decisions/README](../decisions/README.md) (generated row), [INDEX](../INDEX.md) and [STATE](../STATE.md) (generated `adr_count` spans, plus one appended `##` section in STATE), [sessions/README](README.md) (row 189 struck, entry row generated), this file. **`apps/`, `packages/db`, `scripts/`, `.github/` and `docs/testing/` were READ and not written.**
