---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 600: the clause a careful entry ruled on was the wrong one, and question 3's population is 13 rather than 42

<!--index: **THE ROW IS [ADR-405](../decisions/ADR-405.md), `proposed` and UNSIGNED. NON-MONEY, DATED RECORDS AND ONE REGISTRY, `E2` READ OWED.** **[ADR-388](../decisions/ADR-388.md) SECTION 16 QUESTIONS 2 AND 3 ARE BOTH ANSWERED AND ONE IS RETIRED WITH THE MEASUREMENT THAT JUSTIFIES RETIRING IT.** **QUESTION 2: RULED (c), and [ADR-402](../decisions/ADR-402.md)'s ruling is AFFIRMED on its option and CORRECTED on its subject.** That entry read `clause 2` as the honesty rule, which is `clause 1`'s SECOND BRANCH by [ADR-388](../decisions/ADR-388.md) section 18's own phrasing; `clause 2` is the NAMING convention, as [ADR-401](../decisions/ADR-401.md) section 13 item 4 calls it, and the question's own predicate *"binds nothing and no runner reads it"* is false of `G2` and written verbatim of the naming paragraph. **The consequence is not a quibble: the bullet now in front of a founder asks for a name only where the coordinate is dropped, so a pointer with a verified coordinate and NO name satisfies it and can never be repaired by anybody.** The two drafts are CONSOLIDATED into ONE bullet carrying both halves, PROPOSED and not taken; [`CLAUDE.md`](../../CLAUDE.md) is untouched and `git diff` on it is EMPTY. **THE `CLAUDE.md` HOME IS MEASURED FOR THE FIRST TIME**: the em-dash rule is the one convention in that list no runner enforces, and **368 of 387 entries obey it, with the last 23 consecutive entries carrying none**. **QUESTION 3: THE PREMISE IS REFUTED AND THE QUESTION IS RETIRED.** *"Every one of those pointers sits in a table cell whose verdict column says the claim was checked"* was never measured. All **42** born-wrong pointers under [ADR-396](../decisions/ADR-396.md)'s ruled instrument, traversal and search string stated, were read at their sites against a criterion written first: **13 make a claim of a check, 15 are the sentence REPORTING somebody else's pointer wrong, 6 are declared specimens, 4 are quotations, 4 name a seeded tree that was reverted**, and **7** sit in a verdict column. **Option (c), offered as the one place that would have caught all 42, reaches 7**; (b) has the same scope and is refused with it. The `E2` read is unchanged and the remedy for the 13 is question 2's second half, at write time. **TWO FIGURES IN [ADR-388](../decisions/ADR-388.md) SECTION 1 DO NOT REPRODUCE AT ITS OWN BASE**, found while validating the reader: the `101` validation under six readings and the `7,242` denominator under both, annotated at the site and never overwritten. **The 8,623, the 363 and the 290 the census rests on reproduce to the unit.** **NO GATE WEAKENED, NO TEST TOUCHED, NO INVARIANT MINTED, NOT ONE EXECUTABLE LINE CHANGED, ZERO CITATION REPAIRS FORCED**, the three annotated entries edited line-count neutrally. -->

### 1. The objective

[ADR-388](../decisions/ADR-388.md) section 16 questions 2 and 3, owed by [ADR-400](../decisions/ADR-400.md) section 17 item 1 and again by [ADR-401](../decisions/ADR-401.md) section 13 item 4. Both ship. Rule on where the naming convention belongs and, if the answer is [`CLAUDE.md`](../../CLAUDE.md), consolidate [ADR-402](../decisions/ADR-402.md)'s proposal so a founder has one thing to approve.

### 2. Done

- **The container was measured before anything else.** `/home/user` empty, `git rev-parse --is-inside-work-tree` returns `fatal: not a git repository`, and after the clone `git rev-parse --is-shallow-repository` returns **`false`** over **4,329** commits. Nothing was deepened. Sixth consecutive entry to record the empty container and seventh consecutive row to measure a full clone.
- **The base was reproduced on `1f621d5c` before the first edit**: 33 of 33 gates, 35 of 35 invariants, 336 test files with 8,109 passed and 10 skipped, typecheck, lint and format clean. `pnpm run verify` and `falsify.mjs` were not run, on instruction.
- **The reader was built outside every fence.** `packages/tooling/**` is another row's this wave, so the export-added copy went under `node_modules/` at the one depth where `REPO_ROOT` still resolves. That directory is in the checker's own `SKIP_DIRS`, so the copy could not enter its own census. Deleted before the commit.
- **[ADR-405](../decisions/ADR-405.md) written**, both questions ruled, one consolidated bullet proposed.
- **Three entries annotated line-count neutrally**: [ADR-388](../decisions/ADR-388.md) at four sites, [ADR-400](../decisions/ADR-400.md) at one, [ADR-402](../decisions/ADR-402.md) at two.
- **Registry rows for `405` TAKEN and `404` and `406` RESERVED**, the reservations naming no subject, guessing no branch and carrying no link to a document that does not exist.

### 3. Landmines

- **The census counts a correct finding as a fresh defect.** Fifteen of the 42 born-wrong pointers are an entry doing exactly what outcome two requires: leaving a wrong coordinate in place and writing the finding beside it. Repairing one would make the sentence say the pointer had never been wrong. **A row deriving a class must separate the two.**
- **Four born-wrong pointers name a tree that was seeded, watched red and reverted.** `G2` tests them against a tree they were never about and can only ever return false. That is a hole in the instrument and not a defect in the record.
- **`git blame -w` and `git log -S` disagree on four sites here and they are the four [ADR-401](../decisions/ADR-401.md) section 4 enumerates.** Reproduced independently at a different base, which is the second validation of this row's harness.
- **A validation figure can be inherited rather than run.** [ADR-388](../decisions/ADR-388.md)'s reader was validated against a number none of six readings reproduces at its own base, and the number is dated to an entry whose own `covers` line says it was not re-run.

### 4. Blockers

None. The [`CLAUDE.md`](../../CLAUDE.md) bullet needs a founder and cannot be taken by any session; that is a decision and not a blocker.

### 5. Files touched

[`docs/decisions/ADR-405.md`](../decisions/ADR-405.md) (new), [`ADR-388.md`](../decisions/ADR-388.md), [`ADR-400.md`](../decisions/ADR-400.md), [`ADR-402.md`](../decisions/ADR-402.md), [`ALLOCATION.md`](../decisions/ALLOCATION.md), [`decisions/README.md`](../decisions/README.md) and [`sessions/README.md`](README.md) through the generator, [`STATE.md`](../STATE.md), and this log. **Nothing outside `docs/`.**

### 6. Next

The consolidated bullet needs a signature. [ADR-388](../decisions/ADR-388.md) section 15 items 1, 2, 3, 5 and 7 remain open; its section 16 is now closed. The 7,242 denominator is annotated and unresolved, and whoever re-derives the corpus-wide dated population should state which reading they take.
