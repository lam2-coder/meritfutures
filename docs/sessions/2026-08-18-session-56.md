---
status: draft
depends_on: [README.md]
last_updated: 2026-08-18
---

## 2026-08-18 - Session 56: `absorbs_into`, the shape ADR-055 shipped without its validator (money path)

**STUB, written before the work. It is committed first so a sibling branch can see this session exists and what it holds**, which is the discipline [ALLOCATION](../decisions/ALLOCATION.md) applies to numbers and this file applies to itself. The `Done` line is written from what landed, not from what was planned, and if a claim below is not in the final diff it is a defect in this log rather than a plan that slipped.

- **Objective:** **Write the validator [ADR-055](../decisions/ADR-055.md) deliberately shipped without.** The ADR rules the shape and enumerates six rejections; writing the checks inside the document that rejects that practice would have been the corpus disagreeing with itself in one file, so the ruling and its enforcement are separate sessions on purpose.
- **Fence:** `packages/db/src/seed/calendars/**` and `packages/db/test/`, plus this log.
- **NO TRANSCRIPTION.** `holidays`, `early_closes` and `coverage` stay `null`. This session builds the shape two blind readers will fill; it does not fill it.
- **Done:** *(written at the end)*
- **Next:** *(written at the end)*
- **Blockers:** *(written at the end)*
- **Landmines:** *(written at the end)*
