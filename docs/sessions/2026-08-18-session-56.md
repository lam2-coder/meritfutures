---
status: approved
depends_on: [README.md]
last_updated: 2026-08-18
---

## 2026-08-18 - Session 56: ADR-054 implemented, the DO-5 call site removed, and RE-U-025 given its assertion (money path)

- **Done:** **[ADR-054](../decisions/ADR-054.md) is implemented and `CI-03`'s red set is EMPTY for the first time since the loader started folding real rules.** One call was deleted from [`advance.ts`](../../packages/rules-engine/src/day/advance.ts)'s `DO-5` block, so `withdrawableCents` carries through the `...settledState` spread as the other twelve carried fields already do. `RE-U-025` now asserts the **carried-field set** directly. **748 passing, 0 failing, from 747 and 1**, exactly as the review desk predicted. `GS-064` is green and **no other fixture moved**. 17 of 17 gates.
- **Next:** **The residue below needs a decision that is NOT this session's**: after `RE-U-025` the tree pins **nine** written fields on a breach row while [M01](../plans/M01-rules-engine.md) section 3.6 states **eight**.
- **Blockers:** None.
- **Landmines:**

  - **`RE-U-025` ALREADY EXISTED, AND THE EXPECTED TEST COUNT IS WHAT PROVED IT.** The brief said to write it "which does not exist", and `reU('R-25')` renders as `RE-U-025  R-25  ...` ([`rule-coverage.ts`](../../packages/rules-engine/test/rule-coverage.ts)), so the test has existed since group D landed. What did not exist was its **assertion**. The tell was arithmetic: a NEW test would have produced **749** passing, and the brief predicted **748**, which is reachable only by `GS-064` flipping alone. **The count was the specification and it disagreed with the prose**, so the count won. ADR-054 agrees in its own words: "`R-25`'s asserts the carried-field set directly."

  - **A STALE LOCAL INSTALL MADE THE BASELINE LOOK WRONG BY EIGHT TESTS, AND THE FIRST INSTINCT WAS TO DOUBT THE BRIEF.** The tree reported **739** passing against the brief's 747. The cause was not the code: [session 55](2026-08-17-session-55.md) added `fast-check` to the ROOT `package.json` for `scripts/demo`, this container's `node_modules` predated it, and `replay-determinism.property.test.ts` **failed to collect** rather than to assert. Vitest reported it as `2 failed` test FILES against `1 failed` test, which is the only visible difference between a file that failed and a file that never ran. **A test that does not run is not a test that passed**, and the count it silently removes looks exactly like a brief with the wrong number in it. `pnpm install` restored 747 and the brief was right all along.

  - **THE FALSIFY HARNESS OUTLIVED ITS OWN INVOCATION AND WAS STILL SEEDING MUTANTS INTO THE TREE.** `falsify-ci.mjs` was run in a loop of three, each wrapped in `timeout 300`, and the outer Bash call died first. **The node process survived as an orphan and kept mutating**: three consecutive `git status` calls named three different dirty files (`breach.ts`, then `floor.ts`, then `gates.ts`), which is what a live mutation harness looks like from the outside. **This is [session 49](2026-08-17-session-49.md)'s landmine in a new shape.** That session's repair was a journal replayed by the NEXT invocation, and it is intact and did its job; what it does not cover is a run that is never killed at all and simply outlives the shell that started it. **The tree was verified clean before anything was committed**, and no commit was made while the harness was live.

  - **AND THE LOOP WAS WRONG BEFORE IT WAS SLOW.** `falsify-ci.mjs` takes a STAGE argument (`falsify-ci.mjs CI-01`), which `package.json`'s own note says. Running it three times bare and grepping the output for `R-24`, `R-25` and `R-35` ran the entire suite three times to answer a question one filtered run answers. The cost was not the wasted minutes; it was that a long unattended run is exactly the condition the landmine above needs.

  - **THE REMEDY WAS PROVED IN BOTH DIRECTIONS RATHER THAN ASSERTED.** A remedy that cannot fail is worth nothing, so `RE-U-025` was watched failing before it was trusted. **Runtime**: reintroducing the exact deleted call turns it red with `expected 0n to be 20000n`. **Compile time**: deleting one key from the `Record<keyof RuleState, 'written' | 'carried'>` classification produces `TS2741: Property 'lifetimeSettledCents' is missing`. The first catches a recompute added to the terminal branch; the second catches a field added to `RuleState` that nobody classified. **The second is the one that catches the CLASS**, and it is why the record is a total map rather than a list.

  - **THE CHANGE IS VISIBLE IN THE DIFF AND INVISIBLE TO `CI-03`, AND ADR-054 REQUIRES IT SAID OUT LOUD.** `gatesAfterBreach` now reports `minimumAmount.withdrawableCents` at the **carried** value rather than at zero. That field is nested inside `engine_gates`, and [`compare.ts`](../../packages/golden-loader/src/compare.ts) diffs only flat fields with `Object.is`, **so no fixture pins it and nothing in `CI-03` can see it move.** A reader of the diff will. It is stated here because the only place it can be stated is here.

  - **RESIDUE: THE TREE NOW PINS NINE WRITTEN FIELDS AND M01 STATES EIGHT.** `engineGates` is written on a breach row by `gatesAfterBreach`, it is **not** among section 3.6's eight, and it is pre-existing and correct: `RuleState` requires the gate record and section 3.6 predates the field. `RE-U-025` classifies it `written` **with its reason attached**, because an unexplained ninth entry is the next reader's false lead. **Whether 3.6's block should gain it, or whether the divergence stands with a stated reason, is NOT DECIDED HERE**: it is outside what ADR-054 names, and amending a frozen document past what its ruling covers is its own failure mode. Recorded in [STATE](../STATE.md) as an open item.

  - **THE PHASE GUARD WAS LEFT ALONE ON PURPOSE AND THAT IS THE HALF THAT COULD HAVE GONE WRONG QUIETLY.** `withdrawableCents`'s `phase !== 'funded'` guard is M01's `withdrawable()` verbatim. It is the eval phase's, it stays reachable through `R-32` expiry which does fall through to `DO-9`, and removing it would make **40,000** reachable, a value ADR-054 records as produced by no correct reading at all. Both surviving call sites are intact: `initialState` and `DO-9`.

- **Files touched:** [`advance.ts`](../../packages/rules-engine/src/day/advance.ts), [`rules-d-breach.test.ts`](../../packages/rules-engine/test/rules-d-breach.test.ts), [STATE](../STATE.md), [sessions/README](README.md), this file. **Authority throughout: review desk, 2026-08-18.**
## 2026-08-18 - Session 56: `absorbs_into`, the shape ADR-055 shipped without its validator (money path)

- **Done:** **[ADR-055](../decisions/ADR-055.md)'s shape and its validator landed together**, which is why the ruling deliberately shipped without them. [`cme-2026-2028.source.json`](../../packages/db/src/seed/calendars/cme-2026-2028.source.json) carries `absorbs_into` on every holiday with its three states and `coverage.evidence_to`; [`generate.mjs`](../../packages/db/src/seed/calendars/generate.mjs) enforces the ruling's **six rejections** plus the evidence bound and one type check; the generator suite goes from **eighteen seeded violations to twenty-seven**, each watched failing on its own finding. **`pnpm run typecheck`, `lint` and `format:check` are green and 590 unit tests pass.** **No transcription**: `holidays`, `early_closes` and `coverage` are still `null` and the file is still refused.
- **Next:** **The holiday list is still the only bottleneck and this session did not touch it.** It is a different page of the same site and has still not been retrieved as an artifact. The shape two blind readers will fill is now complete and checked, so `OQ-SE-04` is unblocked the moment the retrieval happens. **Two limitations are recorded rather than fixed** and both are below.
- **Blockers:** **None inside the fence.** `pnpm run verify` exits 1 on **one** fixture, `GS-064`, which is [ADR-054](../decisions/ADR-054.md)'s and is repaired by deleting one call in [`advance.ts`](../../packages/rules-engine/src/day/advance.ts). Proved not this session's: `packages/rules-engine` and `packages/golden-loader` are **byte-identical to `origin/main`** on this branch, and every changed file is under `packages/db/src/seed/calendars/`, `packages/db/test/` or `docs/sessions/`. `GS-024` is now green, so ADR-052 landed.
- **Landmines:**

  - **ONE OF THE SIX REJECTIONS IS UNREACHABLE, AND FINDING THAT OUT COST A SEED THAT SILENTLY PASSED.** `absorbed-session-not-claimed` is the reverse pass: a trade date follows a holiday and no holiday names it. **It cannot fire.** Let `D` trade with a holiday at `D - 1`. If that holiday says `null`, `absorbed-null-but-next-day-trades` fires because `D` is the next calendar day and it trades. If it says an object, `absorbed-trading-day-not-next` requires it to name `nextTradingDay(D - 1)`, whose walk starts at `D` and returns `D`, so `D` is claimed. Both branches close before the reverse pass runs. **The seeded violation written for it built, and the harness said so**, which is the one thing that separates this from shipping it green. It is kept because the ruling enumerates it, and its `describe` block carries the proof plus a case per branch asserting the finding that actually catches each. **If that block ever goes red the backstop has become reachable and owes a seed.**

  - **A CHECK THAT CANNOT FAIL IS NOT A CHECK, AND THE TEMPTING FIX WAS TO WRITE A SEED THAT FAILS ON SOMETHING ELSE.** Any of the three obvious seeds for it reject on `absorbed-null-but-next-day-trades` or `absorbed-trading-day-not-next`, and each would have read as coverage in the file whose own header says "a rule that rejects for the wrong reason and a rule that accepts for the wrong reason are the same defect". **Deleting the rejection was also available and was not taken**: it is one of the ruling's six and removing it is a change to the ruling rather than to its enforcement.

  - **A SECOND LIMITATION, OUTSIDE THE SIX AND NOT INVENTED HERE.** A run of consecutive holidays lets **two** entries claim one trade date, and both are legitimate: `nextTradingDay` skips a holiday, so Wednesday and Thursday both correctly name Friday, whose session opened Tuesday evening before the run. **Nothing requires the two to state the same bounds**, and `generate` keys them by trade date so the last one wins silently. There is a passing test for the agreeing case, written because the shape reads like a duplicate claim and the next reader will assume it is a defect.

  - **ADR-055's OWN SNIPPET WRITES `date` AND THE GENERATOR HAS ALWAYS PARSED `day`.** Section 3's illustrative JSON uses `"date": "2026-09-07"`; `readHolidays` has read `h.day` since it was written. **The file follows the merged code, and the discrepancy is named in three places** a transcriber will actually be looking at: the source file's `_holidays_note`, the README's step 2, and a test that asserts the note says so. The ADR rules `absorbs_into`; it does not rule a rename, and silently renaming a key the merged generator reads would be a change nobody asked for.

  - **THE COMMITTED FILE CARRIES FOUR INDEPENDENT REFUSALS AND THE CASE ASSERTING THREE WAS WRONG.** It was written expecting `status`, the null exception lists and `evidence_to`, and the run reported `provenance-field-missing` sitting between the last two. **The count was corrected to what the runner said rather than the runner argued with**, and the case now peels all four off in order, which is what a transcriber will actually experience.

  - **THE MONEY-PATH LINE IS ONE LINE IN `generate`, AND EVERYTHING ELSE IS A GUARD AROUND IT.** A trade date whose session was absorbed opens where the exchange says it opened, not where `open_day_offset: -1` computes. Without it the row is a strict **subset** of the real session and a fill on the Sunday evening of Labor Day weekend falls inside no session in the file, which is `R-01`'s condition. **Three positive-control cases pin it**: the absorbed row opens Wednesday rather than Thursday, every unabsorbed row is unchanged so the exception stays an exception, and the absorbed open does not reach back past the preceding close, which is how this rule breaks.

  - **`absorbs_into` JOINED THE `OQ-SE-04` BLIND DIFF, BECAUSE A TRANSCRIBED VALUE NO SECOND READER IS COMPARED ON HAS NO SECOND READER.** It uses a shared-keys variant rather than `diffMaps`, so a holiday one reader missed reads as **one** disagreement instead of two and the count in the diff stays equal to the number of things to go and check.

  - **ONE PRE-EXISTING CONVENTION VIOLATION IS NAMED AND NOT FIXED.** [`generate.mjs:225`](../../packages/db/src/seed/calendars/generate.mjs) carries an em-dash, which Appendix F bans. It is inside the fence and one character, and it is left alone on the same reasoning as the `date`/`day` discrepancy: a drive-by edit in a file under review is diff noise, and naming it here is what makes it fixable by whoever is next in the file.

- **Files touched:** [`cme-2026-2028.source.json`](../../packages/db/src/seed/calendars/cme-2026-2028.source.json), [`generate.mjs`](../../packages/db/src/seed/calendars/generate.mjs), [`trading-calendar-generator.test.ts`](../../packages/db/test/trading-calendar-generator.test.ts), the [calendars README](../../packages/db/src/seed/calendars/README.md), this log and [`docs/sessions/README.md`](README.md). **Five commits, each pushed immediately**, the first of them this log as a stub before any work.
