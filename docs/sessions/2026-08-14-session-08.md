## 2026-08-14 - Session 8: founder rulings on the four gate findings
- **Done.** Folded all four rulings raised after the batch 1 gate, closing every open finding that session produced.
  1. **Merit Rapid's per-day ceiling of record is $300** (30,000c), corrected in all four flagged locations: ADR-018, [M01 AS-03](../plans/M01-rules-engine.md), STATE, and [SIMULATION_HARNESS](../testing/SIMULATION_HARNESS.md). The $240 figure was **settlement-anchored commentary predating ADR-019**, and the `w=3` calibration was basis anchored and already contained the 3 day cycle, so the correction carries **no economic change**: $889 firm dollars per funded account, 48.1 percent funded-to-payout, 2.09 payouts per payer, and roughly 18 percent margin all stand as recorded. Added the adversary-dossier framing in ADR-018 and M01: the ceiling nominally exceeds the MFF-magnet benchmark, and the defense is the win-day gate, the 8-payout ladder (8 x 90,000c = **720,000c, roughly $7,200 per account lifetime**, INV-17), and detection, **never the per-day rate**.
  2. **Core EOD and Direct compressing to 5 trading days CONFIRMED as intended.** Wallet-instant credit is lineup-wide by design; their economics equal the original sim calibration, which was basis anchored throughout. STATE item cleared.
  3. **Conservatism relocation codified.** Calibration bands are **central estimates**; conservatism lives in `rho = 0.30`, the regime-stress ruin scenarios, and the RCR breaker at 1.0. **`CVaR99 at rho = 0.30` is the reserve floor, never the estimate.** Written into DECISIONS, [GLOSSARY](../GLOSSARY.md#cvar99) (both the CVaR99 and RCR entries), [M05](../plans/M05-payout-system.md) (AS-M5-03, OQ-M5-05, DEP-M5-06), [M06](../plans/M06-admin-ops-console.md) (P-M6-07, DEP-M6-05), and SIMULATION_HARNESS as a reporting requirement on the harness rather than a note about it.
  4. **The publish-time cadence check split into two typed messages.** [M01](../plans/M01-rules-engine.md) section 2 now carries a full typed table (`PW-01` to `PW-04`) with an `info` / `warning` severity column: **`PW-02a` `info`** for co-binding, **`PW-02b` `warning`** for dominated, with distinct text. Appendix A.4's validation walk re-rendered against it, and [M03](../plans/M03-billing-checkout.md)'s publish diff now groups by severity with `warning` first. GS-141 added.
- **Next:** Wave 3 batch 2 (M09 to M19), unchanged. Then the consolidated schema-delta migration.
- **Blockers:** None.
- **Landmines:**
  - **`research/calibration/mc_lifecycle.py` is still not in the repository.** Checked this session: `research/calibration/` does not exist. Every reference across DECISIONS, ADR-015, ADR-018, and SIMULATION_HARNESS already points at that path, which is deliberate (the path is the contract), but until the files land Appendix A's parameters are sourced to the model **by citation rather than by diff**, which is the entire gap committing them closes. Recorded in STATE rather than left to be noticed.
  - **The $240 correction is a bookkeeping fix and must not be read as a de-risking.** Nothing about the plan's economics moved. What moved is that a stale annotation stopped contradicting the parameters, and the corpus now states one number in four places instead of two numbers in four places. If anyone later finds a $240 reference, it is a miss, not a second opinion.
  - **"Central estimate" and "reserve floor" come out of the same harness run and would otherwise carry the same name.** That is precisely how a firm ends up sizing its payout wallet against the middle of a distribution. SIMULATION_HARNESS now requires them emitted as **distinct named outputs**, and the RCR's denominator is specified as the floor in both M06 and the GLOSSARY. If the harness ever emits one number, the RCR silently becomes a coin flip and nothing will complain.
  - **The typed publish messages are a credibility control, not formatting.** After ADR-019 the cadence check fires on all three v1 plans; rendering them uniformly would have put two false positives in front of the founder on every single publish, which is AS-M6-02's mechanism (a control that cries wolf gets overridden) arriving at a publish gate instead of a circuit breaker. The `info` / `warning` split is what keeps the one real warning legible.
  - The `~$7,200 lifetime` figure is worth memorizing alongside the `$300/day` one, because they are the same plan and only one of them is a liability number. A per-day rate that terminates after 24 trading days is a different object from one that does not.
- **Files touched:** docs/{DECISIONS,STATE,INDEX,GLOSSARY,SESSION_LOG}.md; docs/plans/{M01,M03,M05,M06}*.md; docs/testing/{GOLDEN_SCENARIOS,SIMULATION_HARNESS}.md.

---
