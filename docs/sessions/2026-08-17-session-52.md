---
status: approved
depends_on: [README.md]
last_updated: 2026-08-17
---

## 2026-08-17 - Session 52: the short calendar repaired, and the repair was two halves rather than one (non-money)

- **Done:** **CI-03 goes from 6 red of 30 to 2 red of 30.** [`cme-2026.json`](../../packages/rules-engine/fixtures/calendars/cme-2026.json) holds **seven** sessions, `2026-10-30` to `2026-11-09`, and the four funded fixtures that were refusing on their own open day now anchor at the session before their first mark. `GS-020`, `GS-054`, `GS-055` and `GS-061` pass **completely**; `GS-064` goes from thirteen diffs to **one**, and that one is a new finding rather than this repair's residue. **`GS-024` still fails and fails identically**, `floor_cents: expected 5010000, engine produced 5050001n`, byte for byte what it printed before this session touched anything. **No expected value was edited** (TR-01) and **nothing outside [`packages/rules-engine/fixtures/`](../../packages/rules-engine/fixtures/README.md) was touched**, per the session fence. 7 of 7 invariants, 16 of 16 gates, typecheck, lint and format clean; 743 of 777 tests pass with the 2 failures being the 2 fixtures.
- **Next:** **Two fixtures, two founder rulings, and neither is an engineering session.** `GS-024` is the M01 section 3.4 versus section 3.6 floor-lock disagreement [session 51](2026-08-17-session-51.md) already filed. `GS-064` is a new one and is stated below. Beyond them, [`golden-loader/src/calendar.ts`](../../packages/golden-loader/src/calendar.ts)'s header still says the fixture calendar "holds five consecutive sessions"; the substance of that paragraph survives, because seven contiguous sessions still make position equal the dense index, but the **number is now stale and the file is outside this session's fence**.
- **Blockers:** The CME transcription is unchanged and still a founder item, so the fixture calendar is still `status: partial` and still not derived. `INV-07` versus the funded reset is untouched.
- **Landmines:**

  - **THE DIAGNOSIS WAS RIGHT ABOUT THE CAUSE AND HALF RIGHT ABOUT THE REPAIR, AND THE MISSING HALF WAS MEASURABLE IN ONE RUN.** [Session 51](2026-08-17-session-51.md) and the brief for this one both concluded that five of the six failures were "one calendar two sessions short". **Extending the calendar fixed exactly one of the five.** The session after `2026-11-06` is what `GS-020` needed, because `R-31` asks the slice for the next trading day and the slice could not answer. The session before `2026-11-02` is **necessary and not sufficient** for the other four: `opened_on` is a fixture INPUT, so lengthening the calendar under a file does not move that file's anchor off its own first mark. `advanceDay` refuses on `mark.tradingDay <= input.prior.tradingDay` ([`advance.ts`](../../packages/rules-engine/src/day/advance.ts) DO-1), and both sides of that comparison read `2026-11-02` no matter how many sessions surround them. **A diagnosis that names the right cause can still name a repair that does not reach it**, and the only thing that told the two apart was running the extension and counting.

  - **CHANGING A FIXTURE INPUT AND CHANGING A FIXTURE EXPECTATION ARE DIFFERENT ACTS, AND THE DIFFERENCE IS CHECKABLE RATHER THAN RHETORICAL.** The brief said the fixtures are correct and forbade editing their expected values. Moving `opened_on` is neither an expectation nor a free choice: **`opened_on` reaches exactly one rule.** It arrives on `DayInput` under [ADR-051](../decisions/ADR-051.md), feeds `R-32`'s elapsed-trading-day count in [`progression.ts`](../../packages/rules-engine/src/day/progression.ts), and `R-32` runs **only in the eval phase**. A funded fold cannot read it, so a funded fixture cannot compute a different number from a different anchor. The proof offered is the outcome and not that paragraph: **with the anchor moved, `GS-054`, `GS-055` and `GS-061` match every pinned field**, including AS-02's `R-29` cross multiplication turning over at 335,000c and `GS-061`'s pinned NEGATIVE `floor_locked` with 5,000c of margin. An edit that made a fixture agree with the engine by construction could not have reproduced numbers M01 derived independently.

  - **THE FOUR THAT REFUSED WERE VIOLATING A CONTRACT THE LOADER ALREADY STATES IN PROSE, AND EIGHT NEIGHBOURS WERE FOLLOWING IT.** [`run.ts`](../../packages/golden-loader/src/run.ts) says it outright: a funded fixture "is handed an explicit prior anchored at `opened_on` and its first mark must be strictly after it", because `initialState` cannot open an account in `funded` on a plan that has an evaluation phase. **All eight funded fixtures that were already green open the session before their first mark.** The four that failed were the four that did not, and the convention was documented, followed by two thirds of the directory, and enforced by nothing. **A convention with a majority and no gate is a convention that has already been broken and will be again.**

  - **`GS-064` IS A SECOND CAUSE THAT THE FIRST ONE WAS HIDING, AND IT IS A THREE-WAY DISAGREEMENT RATHER THAN A TWO-WAY ONE.** Twelve of its thirteen fields now match, including everything the scenario is named for. The thirteenth is `withdrawable_cents`: the fixture pins **20,000** and the engine produces **0**. The engine states its reading in its own comment, that "R-35 on a closed account is `0n` ... recomputed rather than carried from `prior`". The fixture derives 20,000 from `R-35` on the **pre-breach** balance, `5,120,000 - 5,000,000 - 100,000`, on its own line 44. **Neither number is `R-35` on the closing balance, which would be 40,000** against the pinned `balance_cents: 5,140,000`. So there are three defensible readings of which balance `R-35` measures on the row that closes an account, the corpus picks one, the engine picks another, and **the third is the one a reader would derive from `R-35`'s text and the fixture's own pinned balance**. Not settled here: picking a side by editing either file is exactly the TR-01 failure the stage exists to catch.

  - **A REFUSAL ON MARK ONE MAKES EVERY PINNED FIELD LOOK WRONG AND MAKES THE DIRECTORY LOOK WORSE THAN IT IS.** Thirty-one of the forty diffs this session cleared were not disagreements at all: they were the opening balance and zero, reported field by field, because no state had been written. `GS-055` is the sharpest case. Its whole claim is that five days is the **cheapest** path clearing every gate, and its note says shortening the path by one day should break a **named** gate; a refusal on day one shortened it by five, so the file was proving nothing about the claim it exists to pin while presenting nine confident-looking numeric disagreements. **The count of failing FIELDS is not evidence about the count of failing FACTS**, and one refusal upstream inflates the first without touching the second.

  - **THE CONTROL THE BRIEF DEMANDED WAS RUN FIRST AND IT IS WHY THE ANCHOR WAS FINDABLE.** The eight funded fixtures opening on `2026-11-02` with a first mark on `2026-11-03` all passed before anything was edited. That single fact rules out the plan, the calendar name, the funded start path and `initialState` itself, and leaves the relationship between `opened_on` and mark one as the only variable. **Verifying the control cost one command and was the difference between reading the four fixtures and guessing at them.**

- **Files touched:** [`fixtures/calendars/cme-2026.json`](../../packages/rules-engine/fixtures/calendars/cme-2026.json), [`GS-054`](../../packages/rules-engine/fixtures/GS-054-manufactured-dilution-days-from-a-hedged-pair.yaml), [`GS-055`](../../packages/rules-engine/fixtures/GS-055-minimum-variance-path-to-a-full-cap-extraction.yaml), [`GS-061`](../../packages/rules-engine/fixtures/GS-061-peak-picking-requesting-on-a-local-maximum.yaml), [`GS-064`](../../packages/rules-engine/fixtures/GS-064-breach-and-payout-eligibility-on-the-same-day.yaml), [STATE](../STATE.md), [sessions/README](README.md), this file.

### The count, reported rather than implied

**Four of the six pass, one drops to a single field, and one is unchanged by design.**

| Fixture | Before | After |
|---|---|---|
| `GS-020` | refused `calendar_coverage_miss` on `2026-11-06`, 4 diffs | **passes** |
| `GS-054` | refused `not_forward` on `2026-11-02`, 9 diffs | **passes** |
| `GS-055` | refused `not_forward` on `2026-11-02`, 9 diffs | **passes** |
| `GS-061` | refused `not_forward` on `2026-11-02`, 9 diffs | **passes** |
| `GS-064` | refused `not_forward` on `2026-11-02`, 13 diffs | no refusal, **1 diff**, and it is a new finding |
| `GS-024` | no refusal, 1 diff on `floor_cents` | **unchanged**, same field, same two numbers |

**`GS-024` failing identically is the check on the repair rather than a leftover.** It was the one fixture the brief fenced off, its cause is the floor-lock ruling and not the calendar, and a repair that had quietly moved it would have been a repair that changed something nobody could name.
## 2026-08-17 - Session 52: the GS-024 floor divergence, ruled against the engine on a derivation nobody had checked (money path)

- **Done:** **[ADR-052](../decisions/ADR-052.md) is written and `proposed`**, ruling `R-15`'s lock effect for section 3.6's **assignment** against section 3.4's **`max`**, which is the reading [`floor.ts`](../../packages/rules-engine/src/day/floor.ts) ships. The divergence day was computed by hand under both readings and is **2026-11-03**, `GS-024`'s day two. **Three findings the corpus did not carry**: `CV-11`'s derivation of `INV-21` fails under the `max` and is the argument that decides the ruling; **`GS-024` is the only fixture whose pinned floor differs between the readings** and the existing catalogue of affected files omits it; and **[`0015`](../../packages/db/migrations/0015_rule_states.sql)'s `high_water_balance_cents >= balance_cents` check refuses to store any locked account that makes a new closing high**, which is every successful funded account. **No fixture and no engine file was touched**, per the brief. 16 of 16 corpus gates pass.
- **Next:** **The founder's signature on [ADR-052](../decisions/ADR-052.md), then two sessions in this order.** First `floor.ts`, where the `max` becomes a branch, the sub-step `neverRetreats('R-15', ...)` call is deleted, `CI-02/engine-R-15`'s mutant inverts, and **`GS-024` goes green so CI-03's six failures become five**. Then the `0015` constraint: its own ADR, its own superseding migration, its own money-path session. **`GS-065` is the natural third**, and section 2 of the ADR is already its stream.
- **Blockers:** Nothing blocked this session. **`INV-07` versus the funded reset is unchanged and untouched**, and it is the sibling of the item [ADR-050](../decisions/ADR-050.md) closed. The `R-22` operator disagreement is likewise unchanged: same shape, same authority, still unruled.
- **Landmines:**

  - **THE ARITHMETIC IN ADR-052 SECTION 2 IS `GS-065`'S CONTENT, AND IT SHOULD BE TRANSCRIBED RATHER THAN RE-DERIVED.** `MERIT-RAPID-50K`, funded, lock-day close `5,350,001`, floor `5,100,001`, three payouts of `100,000 + 100,000 + 50,001`, balance `5,100,000`, **one cent below the account's own floor with no losing day in the stream**. `GS-065`, `GS-081` and `GS-083` are `INV-21`'s three fixtures and **all three are unwritten**, which is why nothing in the tree would have caught the `max`. A session that re-derives this may derive it differently; `TR-01` says transcribe from the document, and the document now exists.

  - **THE ENGINE IS WRONG BECAUSE A TEST WAS MADE STRICTER THAN THE RULE IT WAS TESTING, AND THIS FILE PRAISED THE TIGHTENING.** [Session 45](2026-08-16-session-45.md) changed `R-14`'s tripwire to fire after each sub-step and STATE called that "the more useful half of the finding". **`INV-06` is a property of the stored day-over-day floor series** and section 3.6's own tripwire compares against the **prior day's** floor; under the assignment `GS-024`'s series is `4,900,001 -> 5,010,000 -> ...`, monotone non-decreasing. The sub-step check asserts a property no document states, the assignment `R-15` specifies violates it, and **the engine resolved the contradiction by changing the rule to satisfy the test.** The generalisable form: a check tightened beyond its invariant does not find defects, it manufactures them, and the rule is what loses.

  - **"NEITHER IS OBVIOUSLY WRONG" WAS FALSE, AND THE THING THAT DECIDED IT WAS A DERIVATION RATHER THAN A RULE.** Both readings satisfy `R-14`, `R-15`'s trigger, `INV-06` at day granularity and `INV-07`. **The discriminator is `CV-11`, which derives `INV-21` from "post-lock it equals it"** — a clause about the floor's value, stated inside a config-validation row, that nothing was checking. **No rule table, no state diagram and no invariant row distinguishes the readings.** Reading only the sources the brief named would have produced a tie.

  - **A PUBLISH-TIME CONTROL CANNOT BOUND A RUNTIME QUANTITY, AND THAT IS WHY THE `max` IS NOT REPAIRABLE.** [ADR-014](../decisions/ADR-014.md) deliberately moved `INV-21` from a runtime recompute to `CV-11` and `CV-17`. Under the `max` the post-lock floor depends on the lock-day closing balance. **A new validation cannot be written**, because the quantity is the trader's to choose after the plan is published. This is the shape to look for elsewhere: an invariant whose enforcement layer no longer contains its inputs.

  - **THE 40,001 WAS NOT A ROUNDING DECISION, AND THE BRIEF'S HYPOTHESIS THAT IT WAS SENT THE INVESTIGATION SOMEWHERE PRODUCTIVE ANYWAY.** It is exactly the overshoot above the trigger, `5,300,001 - (5,000,000 + 260,000)`, and the odd cent is **`GS-023`'s moved cent**, which `GS-024`'s own header states: day one carries `150,001` where `GS-023` carries `150,000`, and it is still in the balance on day two. **No division exists anywhere in the floor path**, so no remainder could arise there. Chasing the cent is what located the boundary pair and proved the divergence is the overshoot rather than the scenario.

  - **TWO READINGS WOULD HAVE MADE EVERY SENTENCE IN THE CORPUS TRUE AT ONCE, AND A MERGED MIGRATION REFUTES BOTH.** Freezing `hwb` at the trigger (`5,260,000`) or before the lock day's update (`5,150,001`) makes the `max` resolve to `5,010,000` and `CV-12`'s redundancy claim literally correct. **Both store a `hwb` below that day's `balance_cents` and `0015`'s check rejects the row.** The reconciliation that saves the most text is not available, and finding that out took reading a merged migration rather than re-reading M01.

  - **`INV-06` OCCUPIES TWO ROWS OF M01 SECTION 1.5**, different wording, both citing [ADR-050](../decisions/ADR-050.md). Same shape as the `ADR-046` duplicate-heading defect, and **no gate reads that table for duplicates** — the same absence that let `ADR-046` pass fifteen gates. Recorded, not fixed.

  - **A STATED GATE COUNT WAS CHECKED AND IS 16, FOR A REASON THAT WAS NOT THE ONE OFFERED.** The count is 16 because **`ADR-026`'s manifest gate runs alongside the fifteen `CI-06` letters**, not because `CI-06p` pushed the letters to 16: `CI-06p` is one of the fifteen (`a` to `n` plus `p`, with `o` reserved and unwritten). The number is right and the derivation was not, which is `ADR-034`'s subject from the other direction.

- **Files touched:** [ADR-052](../decisions/ADR-052.md), [decisions/ALLOCATION](../decisions/ALLOCATION.md), [decisions/README](../decisions/README.md), [STATE](../STATE.md), [INDEX](../INDEX.md) (generated span), [sessions/README](README.md), this file. **`packages/` is untouched, verified by `git diff --stat`.**

### The hand computation was confirmed executably, and it is the six-to-five prediction made checkable

**`pnpm run verify` is RED and was red before this session**: `6 failed | 739 passed | 32 skipped`, the six being `GS-020`, `GS-024`, `GS-054`, `GS-055`, `GS-061` and `GS-064`, which is [session 51](2026-08-17-session-51.md)'s set unchanged. **This session touched seven files and all seven are under `docs/`** (`git diff --stat` over its own five commits), so it neither caused nor cleared any of them. Recorded because the plan for this session predicted `verify` would stay green and it is not green; the prediction was wrong about the baseline, not about the change.

> **STALE WITHIN THE HOUR, AND LEFT STANDING SO THE CORRECTION READS AGAINST IT.** [PR #72](https://github.com/lam2-coder/meritfutures/pull/72) — **the other session 52**, sharing this file — merged minutes after PR #73 opened, extending the fixture calendar to seven sessions and re-anchoring four funded fixtures. **`CI-03` is now two failures, `GS-024` and `GS-064`**, so the six-to-five prediction below is **two-to-one**. Everything the ADR rules on is unaffected: **`GS-024`'s single diff is byte-for-byte identical across the merge.** Both sessions independently measured six, both were right when they measured, and **the two landed the same afternoon**, which is how a count checked against the tree still arrives stale.

**`GS-024` fails on exactly one diff, and it is the one this ADR rules on:**

```
floor_cents: expected 5010000, engine produced 5050001n
```

**That is the hand computation in section 1 of [ADR-052](../decisions/ADR-052.md), confirmed by the engine itself**, and it is the whole of `GS-024`'s disagreement: every other field and every event matches. **So the prediction is not an estimate: `CI-03` goes from two failures to one.** When `floor.ts` takes the assignment, `GS-024`'s only diff disappears and the count moves. **The one left standing is `GS-064`**, whose surviving `withdrawable_cents` diff is the other session 52's finding and a different ruling.

### What was read, and in what order, because the order is why the finding is not a tie

M01 section 3.4 including the state diagram, section 3.6's `DO-7`, `CV-12`, `R-13`, `R-15`, [ADR-014](../decisions/ADR-014.md), `INV-06`, `INV-07` and `GS-024`'s registry row, as the brief specified. **That set produces a tie**, and the three sources that broke it were not in it: **`CV-11`** (section 2.4), **[`0015_rule_states.sql`](../../packages/db/migrations/0015_rule_states.sql)** and **[STATE](../STATE.md) itself**, which already carried the disagreement as an open item and saved this session from re-discovering it as news. **`R-35` and `R-43` supplied the drain** that turns a floor discrepancy into an `INV-21` violation. The sweep of all seventeen fixtures pinning `floor_cents` is what established `GS-024` is alone, and that is a claim about absence, so it was made by enumeration rather than by grep.
