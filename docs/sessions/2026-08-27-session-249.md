---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 249: seven scenarios over three terms, and four of the seven citations had EXPIRED

<!--index: THE SMALL DISPOSITIONS, and the term worth checking rather than assuming was the one that paid. **All four `outside-loader-boundary` citations blamed `replay` living in `apps/worker`, and `ADR-078` put `replay` on the engine's public surface five days before this session opened.** The term still stands on all four, for four different surviving reasons, every one of them re-cited: stored `rule_states` and ingest events on `GS-034`, `payout_requests.eligibility_snapshot` on `GS-074`, the cross-version diff machinery on `GS-075`, and `scripts/demo` on `GS-072`. **`GS-072` IS IN A THIRD STATE NOBODY HAD A CELL FOR: its skip cleared itself, the assertion is LIVE and PASSES, and the row still cannot move**, because `CI-06/fixture-inventory` reads `describe.skipIf` as disabled whatever its condition evaluates to. The gate is not relaxed and the two files that would repair it are outside this fence, so it is REPORTED. **`ADR-137` proposed: `OI-16` never held `GS-004` and `GS-031`'s expected end state**, its own register says so, and `ADR-072`'s tie-break run in both directions puts both rows on `format-cannot-express`; the fixture calendar's own `what_this_format_cannot_carry` field had named them by id all along. `OI-16` stays OPEN and holds nothing. `GS-079` stands, re-checked at source. 32 of 32 gates, 10 of 10 invariants, `verify` exit 0 -->

- **Objective:** dispose of seven golden scenarios over three terms, `outside-loader-boundary` (4), `open-question` (2) and `no-plan-record-value` (1), each row worked individually, with [ADR-137](../decisions/ADR-137.md) written only if `open-question`'s two members needed an ANSWER rather than a disposition.
- **Done:** **counts verified from the file and not taken from the dispatch: 4, 2, 1.** [ADR-137](../decisions/ADR-137.md), `status: proposed`, UNSIGNED, nine sections, **not money path**. Seven rows of [section 39](../testing/golden-scenarios/39-fixture-status-and-blockers.md) rewritten. **`open-question` reads ZERO and stays in the vocabulary**; `format-cannot-express` reads 18; `outside-loader-boundary` holds 4 with **all four citations replaced**; `no-plan-record-value` holds `GS-079` unchanged in substance. **`pnpm run verify` exit 0**: 10 of 10 invariants, typecheck, lint and format clean, **150 files / 2,656 passed / 1 skipped**, 32 of 32 gates, `falsify` clean.
- **Next:** **three things this fence does not hold, all named rather than done.** `GS-072`'s discharge is EARNED and owed: the id wants naming once outside its `describe.skipIf` block in [`replay-determinism.property.test.ts`](../../scripts/demo/test/replay-determinism.property.test.ts), after which the row moves to `covered-elsewhere` with no ruling. [`gates.mjs`](../../scripts/corpus/gates.mjs)'s `CI-06/fixture-inventory` header and assertion 6 both still assert that `replayExists` *"is false"*, which stopped being true at [ADR-078](../decisions/ADR-078.md); **the RULE is unaffected and only the worked example is stale.** And [STATE](../STATE.md)'s `OI-16` **What it holds** cell names two rows it no longer holds.
- **Blockers:** **[ADR-137](../decisions/ADR-137.md) is UNSIGNED.** It amends an assignment [ADR-072](../decisions/ADR-072.md) made under a granted approval, so clause 1 is the founder's to read: *an item [ADR-059](../decisions/ADR-059.md) recommendation 3 was granted to open does not hold what its register says it holds.* **If that reading is wrong, two table cells revert and nothing else does. NOT MERGED.**
- **Landmines:** **`OI-16` is NOT closed and this session could not close it.** It is a retrieval about a vendor feed, [ADR-059](../decisions/ADR-059.md) section 6's *"'I could not find it' and 'it does not exist' are different claims"* is carried forward unchanged, and a later reader finding `open-question` at zero must not read that as the question being answered. **The `vendor-call` 14 were not touched, not ruled on, and none was found mis-filed.** **One predicted conflict**: sessions 237 and 248 hold other terms' rows in the same file, and the two summary tables are shared ground. The row sets are disjoint; **the summary cells are not.**
- **Files touched:** [ADR-137](../decisions/ADR-137.md), [section 39](../testing/golden-scenarios/39-fixture-status-and-blockers.md), [ALLOCATION](../decisions/ALLOCATION.md), [decisions/README](../decisions/README.md), [INDEX](../INDEX.md), the generated `adr_count` spans in [STATE](../STATE.md), [sessions/README](README.md), this file.

---

### 1. The counts, verified from the file rather than taken from the dispatch

`4 / 2 / 1` as dispatched, and the seven ids are:

| Term | Rows |
|---|---|
| `outside-loader-boundary` | `GS-034`, `GS-072`, `GS-074`, `GS-075` |
| `open-question` | `GS-004`, `GS-031` |
| `no-plan-record-value` | `GS-079` |

**Every summary count in the document was RECOMPUTED from the 316 rows after the edits and not adjusted from the previous figure**, which is the rule that document states about itself. Status: 43 written, 0 writable, 9 covered-elsewhere, 264 blocked. Blockers: 227, 18, 14, 4, 0, 1. Both sum to their totals and the row set holds 316 distinct ids.

### 2. The term worth checking rather than assuming, and it had expired in all four citations

**The dispatch's suspicion was right and the correction is bigger than expected: not one of the four citations was still true, and the term was still right on all four.**

Every one of them read some form of *"replay runs in `apps/worker`, outside the loader import boundary"*. [ADR-078](../decisions/ADR-078.md) put `replay` on the rules engine's public surface on 2026-08-22, at [`index.ts:198`](../../packages/rules-engine/src/index.ts), with the pure fold at [`replay.ts`](../../packages/rules-engine/src/replay.ts) whose own header says `apps/worker/src/batch/replay.ts` *"builds one today, with its own loop and its own break, and that is the drift ADR-078 ends."* **The worker file now CALLS the engine's `replay`.** So the reason four rows carried was a reason about a file that had changed owners.

**A block disposition over these four would have been unanimous and unanimously wrong in its reasons.** Worked individually:

| Row | What survives, and it is different every time |
|---|---|
| `GS-034` | **Stored** states changing (`rule_states`, [`0015`](../../packages/db/migrations/0015_rule_states.sql)), the superseding mark ([`schema.ts:651`](../../packages/db/src/schema.ts)'s `daily_marks.superseded_by`, a NEW ROW pointing the old one at it), and `ingest.correction_received` plus a review flag, which are ingest events and not engine events |
| `GS-074` | The second conjunct only: **the settled payout keeping its original snapshot** is [`schema.ts:2416`](../../packages/db/src/schema.ts)'s `payout_requests.eligibility_snapshot`, a jsonb written once that no engine call returns. **The first conjunct DID become engine-expressible** and no suite asserts it: [`replay.test.ts`](../../packages/rules-engine/test/replay.test.ts) and [`replay.property.test.ts`](../../packages/rules-engine/test/replay.property.test.ts) carry order, the terminal break and the four reconciliations, and no supersession. **A row does not move on half an assertion** |
| `GS-075` | The **cross-version** machinery rather than the fold: `diffStoredAgainstRecomputed` and `inScopeForDetection`'s `row.engineVersion === engineVersion`, in [`apps/worker/src/batch/replay.ts`](../../apps/worker/src/batch/replay.ts). The engine has no equivalent. That the approval and the page do not exist anywhere does not change the term, on [ADR-072](../decisions/ADR-072.md)'s own words: *"a symbol in `apps/worker` ... fails W1 whether or not it is implemented"* |
| `GS-072` | Section 3 |

### 3. `GS-072` IS IN A THIRD STATE, AND IT IS THE MOST USEFUL THING THIS SESSION FOUND

**Its own row predicted the move**: *"the block switches itself on the day the engine exports `replay` ... and on that day this row moves without a ruling."* That day has passed. `replayExists` is `Object.keys(engine).includes('replay')` and `replay` is exported.

**Watched, not read, which is what [section 39](../testing/golden-scenarios/39-fixture-status-and-blockers.md) requires of this status:**

```
PT-06: arrival-order permutation is LIVE
 Test Files  1 passed (1)
      Tests  11 passed (11)
```

Three of those eleven are the block: the anti-vacuity guard, `any permutation of arrival order yields byte-identical stored state`, and the control asserting the arrival order genuinely moved while it was being folded. **Nothing in the file is skipped.**

**And the row still cannot move.** `CI-06/fixture-inventory` assertion 6 refuses a `covered-elsewhere` citation that names the row ONLY from inside a disabled construct *"or the comment run above one"*, `describe.skipIf` is in `TEST_DISABLERS` **whatever its condition evaluates to**, and `GS-072` is named at `:332` and `:341`, both in that comment run. **Moving the row turned the gate red and the gate was not relaxed.** It rules its own limit in its own text: *"IT IS A STATIC PARSE AND NOT A VITEST RUN ... It cannot see an OUTCOME."*

**No alternative citation exists.** [`replay.property.test.ts:52`](../../packages/rules-engine/test/replay.property.test.ts) carries `PT-06 unit  any permutation of arrival order yields byte-identical states` and [`replay.test.ts:64`](../../packages/rules-engine/test/replay.test.ts) carries the total-order case; **neither file names `GS-072`**, so both fail the same assertion for the opposite reason.

**So the reason moved and the row did not**, and that is what the row and the prose above the table now say. It is no longer *"the assertion runs nowhere"*, which was true and has stopped being true. It is that the only reader a gate has cannot tell the difference. **The repair is one naming of the id outside the block, in a file this fence does not hold.**

### 4. `open-question`, which needed an ANSWER and got one

**Two questions were being conflated and only one of them is `OI-16`.**

| | |
|---|---|
| **What `OI-16` asks** | *"Does any market-data or platform feed Merit will hold report a whole-session halt or limit lock, at the granularity `R-04` consumes it (one boolean per trading day per product)?"* **Open. Not answerable here.** A retrieval, whose **Who** cell has always read *"the founder or the vendor call"* |
| **What the two rows need** | That a golden fixture can SAY a session was halted. **It cannot, and three files say so** |

**[ADR-072](../decisions/ADR-072.md)'s tie-break decides it and it was run in both directions.** If `OI-16` were answered tomorrow, neither fixture could be written: [`loader.ts:569`](../../packages/golden-loader/src/loader.ts) reduces a calendar session to `trading_day` and an optional `kind`, and [`calendar.ts:158`](../../packages/golden-loader/src/calendar.ts) writes `halted: false` on every day by constant, refusing to invent a key it has no source for. If the record grew that key tomorrow, both could: `CalendarDay.halted` exists at [`types.ts:222`](../../packages/rules-engine/src/types.ts), `advanceDay` reads it, and `R-04` is implemented and published. **Only W2 survives its counterfactual.**

**AND THE RECORD ITSELF HAD NAMED THESE TWO ROWS ALL ALONG.** [`cme-2026.json`](../../packages/rules-engine/fixtures/calendars/cme-2026.json)'s `what_this_format_cannot_carry` field: *"A record key plus a loader mapping would be needed to change that, which is `halted`'s blocker exactly (GS-004, GS-031)."* **Both candidate blockers were written down, in two files, and the table took the weaker one.** That is a mis-filing rather than a judgement call, and it is why the entry is worth its number.

**`OI-16` stays OPEN and now holds NOTHING**, which is [ADR-137](../decisions/ADR-137.md) section 5 and is a correction owed in [STATE](../STATE.md), outside this fence.

### 5. `no-plan-record-value`, which was a disposition and is unchanged

**`GS-079` stands, re-checked at source rather than carried.** All three plan records still read `"daily_loss_limit": null` at line 31, [M01](../plans/M01-rules-engine.md)'s `R-22` row still reads *"No v1 plan configures a daily loss limit"*, and the M1 gate closure that created the scenario says the same.

**The FORMAT is explicitly not the blocker, which is what makes the term the right one rather than an accident**: [`plan.ts:159`](../../packages/golden-loader/src/plan.ts) already parses `rules.daily_loss_limit` into a soft or hard limit with `limit_cents`, and would accept a fourth record stating one. **Writing one is [`TR-01`](../testing/STRATEGY.md)'s prohibition exactly**: a value no plan document states. The citation gains the second half [session 117](2026-08-20-session-117.md) named and the table had dropped: what clears it is a plan-document decision, then a session holding the `fixtures/plans/` fence.

### 6. Fence, checked before writing and again before the pull request

| Session | Its rows in this file | Outcome |
|---|---|---|
| **237** | `no-fixture-format`, 227 | **Row sets disjoint.** None of the seven is one of the 227 |
| **248** | `format-cannot-express`, 16 | **Row sets disjoint on ARRIVAL and the term's COUNT is shared.** This session moved two rows INTO that term, so the summary cell reads 18 here and 16 on `main`. **Predicted rather than resolved unilaterally**, on session 235's precedent for exactly this shape |
| **245** | the DERIVATION mechanism, no individual row | No collision. Nothing in this diff touches how the table is derived |

**The `vendor-call` 14 were not read for disposition, not ruled on, and not reinterpreted.** They were passed over while locating the seven, and **none was found mis-filed**, which is the only report the dispatch permits on them.

**Table-boundary check, by hand and not by gate.** The 316 data rows occupy a contiguous run from the delimiter to end of file with **no non-`| GS-` line among them**; the two summary tables each keep their terminating blank line; the one new prose block sits above the first table and is a `###` heading with body text, not a row. `CI-06u` and `CI-06v` both pass and **neither of them asks whether a row belongs**, which is why this was checked by reading.

### 7. Verification, each figure produced by a command

| | |
|---|---|
| `pnpm run typecheck` | **exit 0** |
| `node packages/tooling/checks/repo-invariants.mjs` | **10 of 10 invariants hold** |
| `node scripts/corpus/gates.mjs generate && check` | **32 of 32 gates pass** |
| `node scripts/corpus/falsify.mjs` | **clean.** All 32 gates pass clean and fail dirty, 80 scope cases, 10 loader cases |
| `pnpm exec vitest run` | **150 files, 2,656 passed, 1 skipped**, identical to `main`, as a docs-only diff must be |
| `pnpm run verify` | **exit 0** |

**`pnpm run falsify:ci` was NOT run and was not backgrounded.** It mutates the working tree, this diff is documentation, and nothing in it is one of the stages that harness watches.

**One intermediate state is recorded because it is the session's best evidence rather than a mistake to hide.** `GS-072` was moved to `covered-elsewhere`, `CI-06/fixture-inventory` went red naming the reason, and the row was moved back. **That red run is the whole of section 3**, and a session that had not tried the move would have reported the row as blocked without ever learning that its assertion had started passing.

### 8. The approval line, UNSIGNED

> **`OI-16` does not hold what its own register says it holds, and the fixture calendar had named the real blocker of `GS-004` and `GS-031`, by id, in a committed JSON field.**

Both halves are readings of primary sources rather than executions, and they are quoted at the file in [ADR-137](../decisions/ADR-137.md) section 3 so the read is a comparison rather than a search. **NOT MONEY PATH**, so no `E2` line-by-line read is claimed. **If clause 1 is wrong, clause 2 goes back with it and the cost is one revert of two table cells.**
