---
status: approved
depends_on: [README.md]
last_updated: 2026-08-22
---

## 2026-08-22 - Session 137: the pre-ADR-048 golden block retired, and two comments that described a fold the code had left

- **Objective:** retire the half of CI-03's polarity mechanism that can never run again, and correct the comment at `polarity.ts:130` that is false about the code beneath it.
- **Done.** `describe.runIf(!declaration.holds)` and its 43 tests are gone from [`fixtures.golden.test.ts`](../../packages/golden-loader/test/fixtures.golden.test.ts). The comment is corrected in [`polarity.ts`](../../packages/golden-loader/src/polarity.ts).
- **The count:** `pnpm vitest run` **1410 passed / 2 skipped**, from 1410 passed / 45 skipped. **The passing count does not move and 43 skips go.** `28 of 28 gates`, falsify green with **52 scope cases and 10 loader cases** intact, typecheck / lint / `format:check` clean.
- **The CI-03 coverage block is byte-identical**, diffed rather than eyeballed: captured through `$GITHUB_STEP_SUMMARY` before and after, `diff` reports no difference.
- **Next:** the same retirement is owed one file over, in `determinism.test.ts`, which is out of this fence. See below.
- **Blockers:** none.
- **Files touched:** [`fixtures.golden.test.ts`](../../packages/golden-loader/test/fixtures.golden.test.ts), [`polarity.ts`](../../packages/golden-loader/src/polarity.ts), this file, [README](README.md) and one appended section in [STATE](../STATE.md). **`coverage.ts`, `run.ts` and `index.ts` were in the fence and needed no edit**: the report is computed from `declaration.holds` and never from the test block, which is why it could not move.

### What was verified before anything was cut

Every claim in the brief was read off a run rather than taken from the brief. The coverage block reports **43 fixtures deriving direct and 0 deriving inverted**, every group A to H at 100 percent, and **"The derived direction is ENFORCED"**, which is `declaration.holds` being true. The `golden` project alone was **55 passed / 43 skipped**, so the retired block was the whole of the skip.

**One measurement was wrong on arrival and it was mine, not the tree's.** The first full run reported 5 failed files, all of them `Cannot find package '@merit/rithmic'`. That is a stale `node_modules` in this container against a workspace package `main` has since added, and `pnpm install` cleared it. **It is recorded because a session that had not re-run after installing would have opened a pull request claiming main was red.**

### The guard is kept, and the difference matters

`checkDeclarationAgainstFold` is a real mechanism and ADR-048 case 1 is a real failure mode. It still runs before any fixture is consulted, the coverage block still prints the premise in bold when it does not hold, and `describe.runIf(declaration.holds)` still refuses to enforce a derived direction against a fold that reaches none of the cited rules. **What went was the duplicate assertion, not the check.**

**What this removal does not change, stated because a reader will ask.** If the fold regressed to an identity, this stage would go green with the premise printed in bold and no per-fixture assertion running. **That was equally true before**: the retired block asserted "every fixture must not match", which a stub satisfies, so it passed too. Making a regression RED is a change in behaviour rather than a removal of dead code, and the stop condition for this session was the same passing count, so it is left to whoever holds that objective.

### The corrected comment, and the failure mode that did not move with the fold

`polarity.ts:130` said `runFixture` "calls none of them: it calls `evaluate`, the scaffold's placeholder". [`run.ts`](../../packages/golden-loader/src/run.ts) imports `advanceDay` and `initialState`, calls `buildCalendarSlice`, and does not import `evaluate` at all; its own header says so in the past tense. `engineIsIdentityStub` folds `advanceDay` too.

**The FOURTH-FAILURE-MODE reasoning is kept because it is still live rather than historical**, and that is the finding worth the desk's attention:

| | |
|---|---|
| `advanceDay` | folded, and it reaches `applySettlement` itself at DO-2, so group H is on the path |
| **`evaluatePayout`** | **read-time, and not on this fold's path at all** |

**`R-40` and `R-41` are implemented in `payout/evaluate.ts`, are in the declared set, and are cited by fixtures that therefore derive `direct` and are asserted to MATCH against a fold that never calls `evaluatePayout`.** They pass, because nothing those fixtures pin is produced there. **That is ADR-048 case 1 in its partial form, alive on this tree**, and `checkDeclarationAgainstFold` does not catch it: it detects the TOTAL case, a fold returning its input by reference and emitting nothing. The corrected comment now says which case it catches. **Widening it is new mechanism rather than dead-code removal and was not done here.**

### Reported, not touched

- **`determinism.test.ts` holds the mirror image of what was retired here, and it is out of this fence.** One of the two remaining skips in the whole suite is `RE-D-01 > the golden corpus is SKIPPED while the loader folds the identity stub`, sitting behind `test.skipIf(!stubbed)`. The loader does not fold the identity stub any more, and **the placeholder's own comment says it "disappears the day the loader moves"**. The day arrived. The other remaining skip is `PT-06` in `scripts/demo/test/replay-determinism.property.test.ts`.
- **A second stale reference in `polarity.ts`'s header, fixed in passing:** it pointed at `declarationReachesTheFold`, which resolves to nothing on this tree. The function is `checkDeclarationAgainstFold`.
- **`fixtures.golden.test.ts:50` still opens "CI-03 is currently a sharp instance of it" and then describes three conditions in the past tense**, two of which are no longer true. It is in the fence and was deliberately left, to keep this diff to the one correction the brief named. It is a one-line change.
