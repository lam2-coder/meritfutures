---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 634: the hole ADR-435 named in `golden` is closed in the shape that entry specified, and re-deriving its numbers corrects one of them

<!--index: **THE ROW IS [ADR-439](../decisions/ADR-439.md), `proposed` and UNSIGNED. NON-MONEY DIFF AND NON-MONEY SUBJECT, `E2` READ OWED. ONE WORKFLOW FILE AND THE REGISTRIES, AND NOTHING ELSE EXECUTES.** [ADR-435](../decisions/ADR-435.md) section 10 named `golden` as the one stage its skip-count control could not reach, because `.github/workflows/golden.yml` was outside that row's fence. **THE HOLE REPRODUCES AT SOURCE AND ONE OF ITS NUMBERS DOES NOT**: `golden` is **56** cases as stated, over **2** test files and not 7, because the 7 is `numTotalTestSuites` -- the two files' root suites plus five `describe` blocks -- so the slip is a unit error and not a count error. **AND THE HOLE IS LARGER THAN THE ENTRY THAT NAMED IT KNEW**: `golden.yml`'s SECOND invocation carries a REGISTERED skip today, `determinism.test.ts`'s `test.skipIf(!stubbed)` at `:191`, so the unbound surface was never only a set of zeros. **CLOSED IN THE SPECIFIED SHAPE, FOUR STEPS WHERE THERE WERE TWO**: `--reporter=json` BESIDE each line's human-readable reporter and never instead of it, both reports to `RUNNER_TEMP` and never the workspace, one `skipped-tests.mjs` step reading each. **`--reporter=verbose` IS KEPT ON A STRONGER ARGUMENT THAN `CI-02`'s**: replacing it would delete the coverage statement the stage exists to emit in order to buy a check on the number that statement is about. **RED IN BOTH DIRECTIONS ON COPIES OF REPORTS THIS BRANCH PRODUCED, WITH NO EDIT TO THE TREE.** **THE MERGED-STAGE REPAIR WAS REFUSED AT SOURCE**: `ci.yml:247-256` was opened and forbids it in its own words. **`skipped-tests.mjs` NEEDED NO FIX AND GOT NONE**, derived from four sites in it before it was measured. **NO `CI-06` LETTER SPENT, NO `RI-nn` MINTED, NO GATE WEAKENED, NO TEST FILE TOUCHED, NO REGISTER ROW EDITED, NO `skipIf` OR `runIf` SITE TOUCHED, NO MIGRATION.** [ADR-435](../decisions/ADR-435.md)'s SECOND, smaller hole about `RI-03` and a fifth project is UNTOUCHED and said so. **AND ADR NUMBER `438` WAS RESERVED AND NOT CLAIMED**, because `CI-06f` reads it as a hole the moment `439` exists on disk. -->

### 1. The objective

Close [ADR-435](../decisions/ADR-435.md) section 10's named hole: `golden` is checked by nothing, and a skip appearing there would be invisible exactly the way all eleven were before that entry. Confirm the hole at source and **re-derive its numbers rather than trust them**, wire the two `vitest run` invocations in the shape [ADR-435](../decisions/ADR-435.md) wrote twice, refuse the merged-stage repair [`ci.yml:247-256`](../../.github/workflows/ci.yml) forbids, and prove the control firing in both directions before pushing.

### 2. Done

- **The hole confirmed at source, five separate claims.** Both `vitest run` lines carry no reporter that writes a file, `outputFile` appears nowhere in the base file, the job has six steps and none of them is a check, and `golden` reports **0** skipped today. [ADR-439](../decisions/ADR-439.md) section 2.
- **One number corrected.** **56 cases over 2 files**, not 7. The 7 is the suite count. Recorded because a file count is what the register is keyed on.
- **A second finding the dispatch did not predict.** `golden.yml:127`'s selection carries a registered skip, so this row's second step is not a zero-assertion. [ADR-439](../decisions/ADR-439.md) section 3.
- **The register's behaviour on an unseen project derived before it was measured**, at [`skipped-tests.mjs:289-292`](../../packages/tooling/checks/skipped-tests.mjs), [`:310`](../../packages/tooling/checks/skipped-tests.mjs), [`:334`](../../packages/tooling/checks/skipped-tests.mjs) and [`:185-223`](../../packages/tooling/checks/skipped-tests.mjs). It is a PASS. **No fix owed and none made.**
- **Both directions demonstrated verbatim**, on copies of reports this branch's own workflow lines wrote, with the reports' summary counters moved consistently so the finding under test fires rather than the arithmetic refusal.
- **The four edited steps run verbatim with `RUNNER_TEMP` set**, both suites green, both checks `PASS`, and `git status --porcelain` afterwards naming only the workflow file.

### 3. Next

- **A founder read on whether this control should now get a number.** It is two jobs and four steps and appears in neither `gates.mjs list` nor `repo-invariants.mjs list`. [ADR-439](../decisions/ADR-439.md) section 10 sketches the thin static `RI-nn` that would close the convention-only half and declines to mint it.
- **`ADR-438` is RESERVED and its session amends the row in place** ([ADR-065](../decisions/ADR-065.md) T3).

### 4. Blockers

None. Nothing was blocked on a signature, an approval or an `E2` read, all three of which happen after merge on the merged diff.

### 5. Landmines

- **A sibling row landing a higher ADR number first turns every unreserved lower number into a `CI-06f` hole.** [ALLOCATION](../decisions/ALLOCATION.md)'s own note about `053`, `054` and `055` predicted this and this session paid it. `438` was reserved rather than claimed, which asserts nothing about that row's subject.
- **The two report paths are string literals written twice each and nothing binds a check's path to the run's above it.** A typo makes the check exit `2`, which is red rather than green, so the failure mode is safe. The same is true of [ADR-435](../decisions/ADR-435.md)'s two.
- **`golden`'s skip count is expected to move.** [`fixtures.golden.test.ts:248`](../../packages/golden-loader/test/fixtures.golden.test.ts) is a `runIf`, so the day M01 lands this control is red on a change everybody wanted, and the register gains a row in the same commit.
- **The container arrived with no repository and the proxy refused the first push**, for the seventh consecutive row.

### 6. Files touched

- [`.github/workflows/golden.yml`](../../.github/workflows/golden.yml). Four steps where there were two.
- [`docs/decisions/ADR-439.md`](../decisions/ADR-439.md). New.
- [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md). Row `439` claimed, row `438` reserved.
- [`docs/decisions/README.md`](../decisions/README.md) and [`docs/STATE.md`](../STATE.md). **`gates.mjs generate` OUTPUT ONLY.** No prose in either was touched by hand.
- This log and [`docs/sessions/README.md`](README.md).
