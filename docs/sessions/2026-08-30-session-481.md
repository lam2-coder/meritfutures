---
status: approved
depends_on: [README.md]
last_updated: 2026-08-30
---

## 2026-08-30 - Session 481: two fields with no column, one ruled absent by construction and one whose named source is empty

<!--index: **THE RULING IS [`ADR-290`](../decisions/ADR-290.md), `proposed` and UNSIGNED. A RULING ROW: NO `src/` LINE MOVED, NO TEST CHANGED, NO MIGRATION WAS WRITTEN, NO MIGRATION NUMBER WAS TAKEN OR RESERVED, NO `RI-nn` WAS MINTED, AND THE COUNTS DO NOT MOVE AT TEN WIRED OF TWENTY-FOUR DECLARED WITH FOURTEEN BLOCKED**, derived here from `wired` being the set of ports `start.ts` calls (`wiring.test.ts:138`, ten calls, eight `use` and two `set`) and `BLOCKED` carrying fourteen keys (`:147`) with `usePayoutBackend` among them. **THE COUNT THIS ROW EXISTS TO CORRECT WAS RE-DERIVED RATHER THAN RESTATED, AND IT HOLDS: TEN, EIGHT, TWO.** `PayoutListItem` declares ten fields one per line at `routes/payouts.ts:262-271`; `payout_requests` carries **TWENTY-NINE** columns, twenty-four at `0010:42-124` counted mechanically off the file plus five at `0031:47-52`, and `grep -rn 'ALTER TABLE payout_requests' packages/db/migrations` returns two lines BOTH INSIDE `0031`, so that file is the only one that alters the table and by its own header it relaxes nothing. **FIELD 8 IS THE ONE AN INHERITED READING WOULD GET WRONG**: `PayoutHold.tos_clause` (`routes/payouts.ts:229`) reads `payout_requests.hold_tos_clause`, which IS a column, and it is a DIFFERENT FIELD from `HoldFlag.tosClause` (`:487`), which `ADR-287` section 4 correctly calls unsuppliable. The write side cannot produce the value and the read side has a column to put it in; both are true and field 8 resolves. **`failure_note` IS A RULED ABSENCE AND IT IS PROVABLE RATHER THAN CONCEDED.** It serves `null` on every row v1 can return, BY CONSTRUCTION: the field describes `status: 'failed'`, the only two edges into `failed` are `G-HOLD-ENFORCED` and `G-FREEZE-ENFORCED` (`STATE_MACHINES:146,151`), both enforcement acts on a request that must already exist, and nothing on this tree writes a `payout_requests` row at all. **THREE CANDIDATE SUPPLIERS REFUSED WITH REASONS**, the sharpest being `admin_actions.reason`: `API_CONTRACT:444` excludes admin reasoning from every trader projection while `portal/.../view.ts:43` renders this field **VERBATIM** as a *"server-composed trader-safe sentence"*; the hold columns are refused because `payout_requests_hold_is_complete` NULLs all five on the way to `failed`, which `API_CONTRACT:1358` states from the other side. **THE COLUMN IS SPECIFIED IN FULL AND NOT WRITTEN, AND A SECOND GAP IS NAMED THAT A MIGRATION ALONE WOULD NOT CLOSE**: `PayoutEnforceRequest` (`API_CONTRACT:1341-1345`) carries `reason`, `tos_clause` and `evidence_pack_id` and NO trader-directed note, and the `frozen --> failed` edge has **no endpoint in the contract at all**, so a row that writes only the column produces a column nothing can fill. **`timeline`'s SOURCE IS NAMED AS `events`**, on `subject_kind = 'payout_request'` and `subject_id`, every part of the addressing already existing (`0017:46-47`, `:68`, `scope.ts:1550`, and six code catalogue entries carrying that token). **IT SERVES `[]` BECAUSE THE NAMED SOURCE IS EMPTY**, derived mechanically: a grep for `makeEventSink`, `TRANSACTION_EVENT_WRITER` and `UNWIRED_EVENT_SINK` over every `apps/*/src` and `packages/*/src`, excluding the one file that declares all three, returns **ONE** line and it is a string literal inside a refusal message (`worker/.../adapter.ts:200`); `routes/payouts.ts:146` says *"Nothing in `apps/api/src` writes an event"* in its own words and `events.ts:101` says `payout.settled` *"has no producer in this tree yet"*. **THE FOLD OVER `payout_requests` TIMESTAMPS IS REFUSED IN WRITING AND THAT IS THE HALF `ADR-287` HAD NOT REACHED.** The table looks like a history because it carries four state timestamps, and **TWO OF THE FOUR ARE DESTROYED ON EXIT BY CONSTRAINT**: `payout_requests_hold_is_complete` (`0031:62-72`) and `payout_requests_freeze_is_complete` (`0010:141-149`) are **BICONDITIONALS ON STATUS**, so `held_at` and `frozen_at` must be NULL the instant the row leaves those states, and `failed` never had a timestamp at all (`grep -rn failed_at packages/db/migrations` returns ZERO). Only `approved_at` and `settled_at` survive into a terminal row, so the fold renders a settled payout that was held for forty hours **with no hold in it**, which is a partial history that reads as a complete one and hides the hold from exactly the trader who was held. **THREE PRECONDITIONS NAMED AND NONE IS `listPayouts`'s**, two of them catalogue gaps found here: `payout.settled` is **absent from `EVENT_CATALOGUE`** (`events.ts:344` carries ten names, listed mechanically), and the `frozen` state has **no payout-request-addressable event anywhere**, because `payout.blocked` is `subjectKind: 'account'` (`events.ts:368`) and its payload carries no `payout_request_id`, while `payout.transfer_failed`'s carries none either. The third is a ruled `event_name` to `state` mapping, which is a naming decision rather than a transcription because `payout.approved` and `payout.hold_released` both produce `approved`. **AND `GET /accounts/:accountId/timeline` IS NOT THIS FIELD UNDER ANOTHER NAME**: `API_CONTRACT:434-442` projects `events` for the ACCOUNT into `{occurred_at, trading_day, kind, summary, detail}`, sharing no field with `{state, at}`. **`API_CONTRACT` IS FROZEN, IT MOVED, AND THE MOVE IS STATED RATHER THAN PERFORMED**: two prose paragraphs under `GET /payouts`, one per field, and **NO DECLARED TYPE CHANGED** because `null` and `[]` already inhabit both and narrowing would make every future producer an amendment. `API_CONTRACT:1360` was **left exactly as written**, because it was true of the row that wrote it and editing it would erase where the question was first asked. **THREE FINDINGS THE ROW DID NOT ASK FOR.** **F5**: once a held request leaves `held_pending_review` the hold is erased from its own record, `hold` is null by the contract's own rule and `timeline` is `[]` by this ruling, so **no field of `PayoutListItem` can say the hold happened** and a trader held for forty hours and then paid sees a payout indistinguishable from one that was never held. **F6**: `approved_at` is `NOT NULL DEFAULT now()` (`0010:89`) and `0031` relaxed nothing by its own stated design, while the wire types it nullable and `API_CONTRACT:515` requires null while held, **so a straight column read publishes a false approval time on the one state that most needs to render correctly**; one suppression rule is owed to slice 7 and is stated here rather than left to be found. **F7**: `payout.transfer_failed` is the external leg and never sets `payout_requests.status = 'failed'`, so `M04:281` must not be read as wiring a rail-retry message into a closure for cause. **[ADR-287](../decisions/ADR-287.md) SLICE 7 IS UNBLOCKED** and `GET /payouts` can answer with all ten fields sourced, eight off columns and two off this ruling. **NO COLUMN WAS ADDED, NO FIELD WAS ADDED TO `PayoutEnforceRequest`, NO PROJECTION WAS INVENTED, NO TIMESTAMP WAS FOLDED INTO A HISTORY, NO DEFAULT WAS SYNTHESISED, NO STUB, NO CAST, NO FLOAT, NO SECRET, AND NO PORT WAS WIRED.** **FENCE HELD**; the two files outside it are GENERATED, `docs/decisions/README.md` and `docs/sessions/README.md`, rewritten by `gates.mjs generate`, which is `ADR-287` section 9's disposition taken a second time. **A LANDMINE FOR EVERY SESSION IN THIS CONTAINER: `node_modules` WAS ABSENT ON ARRIVAL**, and `repo-invariants.mjs` reported **29 of 30** with `RI-18` erroring `Cannot find module 'typescript'`, which is an unrun check reported as a held invariant count rather than a failure. After `pnpm install --frozen-lockfile` it reports 30 of 30. Counts derived at reporting time, each command run separately: **33 of 33** gates after `generate`, **30 of 30** invariants off the runner's own last line, **297 files / 7,223 passed / 6 skipped**, which is byte for byte session 479's figures on `main` and is what a ruling row that touches no test must report. Typecheck 0, lint 0, `format:check` clean. **`pnpm run verify` was NOT run and `falsify.mjs` was NOT run**, both forbidden by the row. -->

---

### 1. The objective

**[ALLOCATION](../decisions/ALLOCATION.md) row `290`: slice 2 of [ADR-287](../decisions/ADR-287.md) section 7, the `failure_note` and `timeline` ruling, and finding `F4` is its specification.** One objective. Money path, ruling only, [ADR-003](../decisions/ADR-003.md) regime.

### 2. What was read before anything was written

Row `290` in full, then [ADR-287](../decisions/ADR-287.md) end to end with finding `F4` and slice 2 read twice, then [CLAUDE.md](../../CLAUDE.md), [STATE](../STATE.md) and the last two session logs. Then the primary sources, in this order: `PayoutListItem` and `PayoutHold` at their declarations, the whole of `payout_requests` in `0010` and `0031`, `payout_status` in `0001` and `0030`, `events` in `0017`, the event catalogue and the payout route in `apps/api/src`, the scope registry, `GET /payouts` and the two admin payout endpoints in [API_CONTRACT](../architecture/API_CONTRACT.md), the payout machine in [STATE_MACHINES](../architecture/STATE_MACHINES.md) section 2, [EVENTS](../architecture/EVENTS.md) sections 6 and 12, and [M04](../plans/M04-trader-portal.md) section 5 with [M05](../plans/M05-payout-system.md) section 5.

**THE ROW SAID NOT TO INHERIT A COUNT AND THE COUNT WAS NOT INHERITED.** The ten fields were read off the declaration one line at a time and each of the eight was matched to a named column at a named line. Section 1 of [ADR-290](../decisions/ADR-290.md) is that table. **The claim holds and the re-derivation was not wasted**, because it is what found `F6` and what separated `PayoutHold.tos_clause` from `HoldFlag.tosClause`.

### 3. The two rulings

[ADR-290](../decisions/ADR-290.md) sections 2 and 3. **They are different outcomes for different reasons and the entry says so in its own title.** One field is absent and provably harmless in v1; the other has a source that is named, reachable and empty.

### 4. What was refused, in writing

**The fold over `payout_requests`'s own state timestamps**, [ADR-290](../decisions/ADR-290.md) section 3.2. It is four lines of code and it ships something rather than nothing, and it is refused because two of the four timestamps are NULLed by constraint on exit. **A partial history that reads as a complete one is worse than an empty one.** That refusal is the judgement in this entry most worth a founder overruling, and section 8 says so.

**Three candidate suppliers for `failure_note`**, section 2.3, each with the corpus line that refuses it.

### 5. What is owed and was not taken

- A `payout_requests.failure_note` column, **specified in full** in section 2.4 and not written. No migration number claimed or reserved.
- A trader-directed note field on `PayoutEnforceRequest`, and an endpoint for the `frozen --> failed` edge. **Out of this fence**, which reached API_CONTRACT for two fields on one endpoint.
- An `events` producer, two event-catalogue repairs, and a ruled `event_name` to `state` mapping. Section 3.6.

### 6. Next

- **[ADR-287](../decisions/ADR-287.md) slice 7 is unblocked** and can be dispatched: `GET /payouts` with all ten fields sourced. **`F6`'s suppression rule belongs to that session** and is written down so it does not have to be rediscovered.
- Row `289` (slice 1, `eligibility_snapshot_id`) and row `291` (slice 3, the backend module) are the rest of the near-term path and neither was touched here.
- Slice 8 is unaffected and remains unschedulable. The critical path out of 503 still runs through counsel.

### 7. Blockers and landmines

- **LANDMINE, AND IT REACHES EVERY SESSION IN THIS CONTAINER: `node_modules` IS ABSENT ON ARRIVAL.** `repo-invariants.mjs` reports **29 of 30** with `RI-18` erroring `Cannot find module 'typescript'`, and the runner prints that as a held-invariant count rather than as a failure. A session that reports 29 of 30 without reading the non-`PASS` line has reported an unrun check as a broken one. Run `pnpm install --frozen-lockfile` first.
- **LANDMINE:** `payout_requests` is a **current-state table by constraint**, not a history. Two of its four state timestamps are NULLed on exit by biconditionals and `failed` has no timestamp at all. Any code folding those columns into a history is silently partial on exactly the rows that matter.
- **LANDMINE:** `approved_at` is `NOT NULL DEFAULT now()` and the wire types it nullable. A straight column read publishes a false approval time on a held request. `F6`.
- **LANDMINE:** `PayoutHold.tos_clause` and `HoldFlag.tosClause` are different fields with different suppliers. Reading [ADR-287](../decisions/ADR-287.md) section 4's refusal onto the list item's `hold` block would drop a field that resolves.
- **BLOCKER, FOUNDER:** unchanged from session 478. The ToS clause vocabulary, `DEP-M7-05`, Wave 4 legal.
- **NOT A BLOCKER, BUT NAMED:** `F5`. Until the `events` producer lands, Merit holds payouts on a clock the trader can read and keeps no trader-visible record that the hold occurred.

### 8. Files touched

- `docs/decisions/ADR-290.md` (new)
- `docs/architecture/API_CONTRACT.md`, two prose paragraphs under `GET /payouts`, no declared type changed
- `docs/decisions/ALLOCATION.md` (row `290`)
- `docs/STATE.md`
- `docs/sessions/2026-08-30-session-481.md` (new, this file)
- `docs/decisions/README.md` and `docs/sessions/README.md`, **generated spans only**, rewritten by `gates.mjs generate`
