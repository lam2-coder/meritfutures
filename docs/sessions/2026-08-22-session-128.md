---
status: approved
depends_on: [README.md]
last_updated: 2026-08-22
---

## 2026-08-22 - Session 128: WAVE-05 `X3` and `X4`, `GS-141`'s severity half and three sole-error equalities

- **Objective:** [WAVE-05](../plans/WAVE-05-tier2-fixture-shapes.md) `X3` and `X4`, both in [`plan-validate.test.ts`](../../packages/rules-engine/test/plan-validate.test.ts) and in that order: `GS-141`'s uncovered severity half, then `GS-076`, `GS-077` and `GS-078` strengthened from `toContain` to a sole-error equality.
- **Done:** both items, in the fixed order, in one file. `X3` is one case and one `expect` carrying three clauses; `X4` is three sole-error equalities over `SEEDS['CV-10']`, `SEEDS['CV-06']` and `SEEDS['CV-01']`. **Every assertion was watched failing before it was committed, and the two subject-seeded perturbations are the ones worth reading.** **1405 -> 1409 passing**, 28 of 28 gates, `falsify.mjs` clean. No seed changed, no source file touched, `replay.test.ts` untouched.
- **Next:** `X2` (session 127) and then `X8`, which is the gate that reads `covered-elsewhere`. `X8` still carries [session 123](2026-08-21-session-123.md)'s finding against it: a cited site that SKIPS satisfies "the file names the row" and executes nothing.
- **Blockers:** none.
- **Landmines:** **the three seeds each produce exactly one error today and that is a property of Appendix A.1, not of the validator.** Each seed is Core EOD at 50K with one field moved. A future edit to `coreSize()` or `coreRules()` that moves a second field into violation turns these three equalities red, and the correct repair is the seed, not the assertion.
- **Files touched:** [`packages/rules-engine/test/plan-validate.test.ts`](../../packages/rules-engine/test/plan-validate.test.ts), this log, [README.md](README.md).

### `X3`: `GS-141`'s severity half, one `expect` and three clauses

**[ADR-076](../decisions/ADR-076.md) section 2 rules `GS-141` `covered-elsewhere` and records the residual in the same breath.** The exact-set block at the head of `Appendix A.4  the approved lineup publishes` compares **ids** through `diffIds`, so the row's first half is asserted and its last sentence, *"Asserts the two are never rendered identically"*, is exactly what an id comparison cannot see.

**One new case, `PW-02a and PW-02b are never rendered identically, which is GS-141 last sentence`**, in that same describe. One `expect`, over an object carrying three clauses at once:

| Clause | Value |
|---|---|
| `coBindingSeverity` | `PW-02a` off Core EOD 50K is `info` |
| `dominatedSeverity` | `PW-02b` off Merit Rapid 50K is `warning` |
| `textsDiffer` | their `message` strings are not equal |

**Three clauses and not two, because no two of them are the row's claim.** Two findings carrying the same text under different severities are rendered identically to a reader who reads the text; one tone over two texts collapses co-binding into dominated at a glance. The row asks that both differ at once, so both are in one `expect` and a partial failure prints which half moved.

**The two cannot be read off one plan.** [`validate.ts`](../../packages/rules-engine/src/plan/validate.ts) builds `PW-02a` and `PW-02b` in the two arms of a single `if` on `lag + gap` against `required_count`, so no plan emits both. Which plan carries which is Appendix A.4's own row, and the exact-set block immediately above has just pinned it, which is why this case may read them off two plans without asserting that mapping a second time.

**The `X1` comment above the exact-set block was updated and not replaced.** It said the severity and the text *"are unasserted today"* and named `X3` as the missing `expect`. That sentence is false the moment this case lands, so it now points at the case rather than at the gap. No assertion in it moved.

### Watched failing, four times, and the fourth seeds the defect rather than the expectation

Each clause was perturbed, the rejection read, and the file restored from a saved copy.

| Perturbation | Rejected with |
|---|---|
| `coBindingSeverity: 'info'` -> `'warning'` | `- "coBindingSeverity": "warning"` / `+ "coBindingSeverity": "info"` |
| `dominatedSeverity: 'warning'` -> `'info'` | `- "dominatedSeverity": "info"` / `+ "dominatedSeverity": "warning"` |
| `textsDiffer: true` -> `false` | `- "textsDiffer": false` / `+ "textsDiffer": true` |
| **The subject seeded**: `dominated` reads `PW-02a` off Direct 50K, so the two findings compared genuinely ARE one finding | `- "dominatedSeverity": "warning"` / `- "textsDiffer": true` / `+ "dominatedSeverity": "info"` / `+ "textsDiffer": false` |

**The fourth is the one that matters.** A perturbed expectation proves an assertion is live; it does not prove it catches the defect it was written for. Reading `PW-02a` from a second plan makes "rendered identically" true in the subject, and **two of the three clauses flip together** on it. That is the shape of the failure `GS-141` exists to catch.

**1405 -> 1406 passing.**

### `X4`: three sole-error equalities, and the strengthening is not a coverage gap being closed

**[ADR-076](../decisions/ADR-076.md) section 2 states this residual and states its own limits in the same sentence.** The registry rows for `GS-076`, `GS-077` and `GS-078` say *"Publish fails with the failing validation rule named"*, which `toContain` satisfies exactly, so nothing was uncovered. It is *"a strengthening available for the price of three characters per row"*, and it is worth taking because **`GS-083`'s block already argues for it in its own words**: a `toContain` *"would pass on a config that `CV-09` or `CV-10` also rejected, and then the file would be asserting that SOMETHING blocked rather than that `CV-17` did."*

**One new `it.each` in the `RE-C-oracle` describe**, three rows, each naming the registry row and its rule:

| Row | Seed | Asserted |
|---|---|---|
| `GS-076` | `SEEDS['CV-10']` | `idsOf(validatePlan(...))` **equals** `['CV-10']` |
| `GS-077` | `SEEDS['CV-06']` | equals `['CV-06']` |
| `GS-078` | `SEEDS['CV-01']` | equals `['CV-01']` |

**All three seeds produce exactly one error, so there is no finding to report on this half.** Each is Appendix A.1's Core EOD at 50K with one field moved, which is why the equality holds; the check was run rather than assumed, because a seed producing two errors would have been the session's finding and not a reason to keep `toContain`. **No seed was touched.**

**The equality is on the ENGINE and the containment above it stays on the ORACLE.** The existing `%s fires in both validators` case keeps `toContain` on both validators for all nineteen rules. Pinning the oracle's error set exactly would assert that the two independent transcriptions agree on **multiplicity**, which no registry row asks for and which is a different claim from the one that case was written to make.

### Watched failing, and the seeded subject is the whole argument for `X4`

| Perturbation | Rejected with |
|---|---|
| `toEqual([id])` -> `toEqual([])`, all three rows | `expected [ 'CV-10' ] to deeply equal []`, `expected [ 'CV-06' ] to deeply equal []`, `expected [ 'CV-01' ] to deeply equal []` |
| **The subject seeded**: a second violation merged into each seed at the call site, `buffer_cents: c(-1)`, which trips `CV-07` and `CV-11` beside the rule under test | `expected [ 'CV-07', 'CV-10', 'CV-11' ] to deeply equal [ 'CV-10' ]`, `expected [ 'CV-06', 'CV-07', 'CV-11' ] to deeply equal [ 'CV-06' ]`, `expected [ 'CV-01', 'CV-07', 'CV-11' ] to deeply equal [ 'CV-01' ]` |
| **The same seeded subject under `toContain`**, which is the form `X4` replaces | **PASSED, 3 of 3.** `Tests  3 passed \| 60 skipped (63)` |

**The third row is the one to read and it is recorded because it passed.** On a config that three rules reject, `toContain` reports that the rule under test is among them and an equality reports that it is the only one. That difference is exactly what `GS-083`'s block calls *"asserting that SOMETHING blocked"*, and it is now measured rather than argued: the same subject, the same three cases, green under the old form and red under the new one.

### The `X1` comments, kept true rather than left standing

**Three of [session 123](2026-08-21-session-123.md)'s five comment sites in this file made a claim about the ASSERTION FORM**, and one names `toContain` outright. Each now records the strengthening in one clause:

- `CV-01`'s entry gains *"whose error set is pinned to `['CV-01']` exactly (WAVE-05 `X4`)"*.
- `CV-06`'s and `CV-10`'s gain the same clause for their own ids.
- The `GS-141` comment above the exact-set block, which said the severity and the text *"are unasserted today"*, now points at the case `X3` added.

**`RE-C-01`, `RE-C-06` and `RE-C-10` keep their `toContain` and the comments still say so correctly.** Those are the both-sides-of-the-operator cases section 8.4 requires, asserting a rule firing at one value and NOT firing at the neighbouring one; their subjects are boundary configs rather than one-field seeds, and `X4`'s fence is the seeds.
