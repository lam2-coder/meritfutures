---
status: approved
depends_on: [README.md]
last_updated: 2026-09-04
---

## 2026-09-04 - Session 504: the builder with no door gets one, and the ground it was shut on had expired three entries later on the same day

<!--index: **THE RULING IS [ADR-314](../decisions/ADR-314.md), `proposed` and UNSIGNED. BUILD, MONEY PATH, `E2` READ OWED AND FOUR INHERITED** ([ADR-189](../decisions/ADR-189.md), [ADR-267](../decisions/ADR-267.md), [ADR-270](../decisions/ADR-270.md), [ADR-305](../decisions/ADR-305.md)). [ADR-305](../decisions/ADR-305.md) section 7 slice 1. **THE ABSENCE WAS RE-DERIVED BEFORE ANYTHING MOVED AND BOTH DISPATCHED LINE NUMBERS WERE RIGHT**: `walletWithdrawalApprovalPosting` was declared at [`reversal.ts:218`](../../packages/ledger/src/reversal.ts) with no `export` and its only caller was `:265`, inside `reversalPosting(...)`, which is `LT-09`'s leg. **THE BUILDER NOW CARRIES `export` AND [`index.ts`](../../packages/ledger/src/index.ts) HANDS IT OUT**, under the name the corpus already used, so no citation was invalidated by a rename. **`LT-09` STILL DERIVES FROM IT THROUGH `reversalPosting` AND THERE IS STILL EXACTLY ONE CONSTRUCTION OF `LT-06`'s ARITHMETIC IN `packages/ledger/src`**, held by [`lt06-approval-builder.test.ts`](../../packages/ledger/test/lt06-approval-builder.test.ts) in five cases from both sides, entries and text. **THREE DEFECTS WERE SEEDED, RUN AND RESTORED**, `sha256sum -c` clean on both source files and `git status` clean of seeds: a second wallet leg in a new `src` module (the directory walk found it, which a file list would not have), `LT-09` restating the legs inline instead of composing, and the builder's two legs swapped. **THE THIRD FIRED [`lt06-posting-timing.test.ts`](../../apps/api/test/lt06-posting-timing.test.ts) PIN 1** at *"expected 25000n to be -25000n"* with that file's other nine cases green, which is the check this slice was measured by. **PIN 1's ASSERTIONS ARE BYTE-IDENTICAL AND ONLY ITS `:72` COMMENT MOVED.** **ONE CASE STAYED GREEN UNDER DEFECT 2 AND THAT IS THE FINDING WORTH CARRYING**: the entry-for-entry negation passed against a byte-identical fork, because a behavioural case catches two copies only once they have already parted, which is one commit too late on the money path. The textual case catches the fork itself and neither is sufficient alone. **THE DOCBLOCK WAS REWRITTEN AT THE SAME LINE COUNT ON PURPOSE**, seventeen lines in and seventeen out, so `walletWithdrawalApprovalPosting` is still at `:218` and *"this library opens no transaction and cannot"* is still at `:259`: **ten of the eighteen `reversal.ts:nn` citations this corpus carries point below that docblock and all ten still resolve, so the citation-repair right this row granted was NOT SPENT ANYWHERE.** **THE ROW'S OWN TEXT IS WRONG IN ONE FIGURE AND IT IS CORRECTED RATHER THAN COPIED**: the ground expired **three** entries later and on the **SAME DAY** ([ADR-267](../decisions/ADR-267.md) 2026-08-30 wrote the comment, [ADR-270](../decisions/ADR-270.md) 2026-08-30 ruled the writer), not eight; the row's finding stands untouched and only its count moved. **ONE STALE CITATION FOUND, REPORTED AND NOT REPAIRED**: [ADR-267](../decisions/ADR-267.md) finding 3 cites [`reversal.ts:202-234`](../../packages/ledger/src/reversal.ts) for `walletWithdrawalFailurePosting`, that range holds the APPROVAL builder and its docblock, and the function is at `:261-270`. It was already wrong, this session did not invalidate it, and it sits in a dated record where `CI-06`'s own ruling forbids the rewrite. **NOTHING IS WIRED AND `start.ts` WAS NOT OPENED: ten wired of twenty-four declared, fourteen blocked, unmoved.** No migration number taken or reserved, no `RI-nn` minted, no gate weakened, no fence widened, nothing signed, and no assertion in any pre-existing test file was changed, added or deleted. Counts derived at reporting time, each command run separately: **303** test files passed with **7,391** tests passed and **6** skipped, gates **33 of 33** after `generate`, invariants **32 of 32** off the runner's own last line, typecheck, lint and `format:check` clean. **`pnpm run verify` and `scripts/corpus/falsify.mjs` were NOT run**, both forbidden by the row. `node_modules` was absent on arrival and was restored by `pnpm install --frozen-lockfile`. **[docs/INDEX.md](../INDEX.md) NEEDED NO EDIT and that was checked rather than assumed**: it indexes `decisions/` as ONE row and says in that row's own words that entry files are *"indexed by that README under `CI-06n`, not by this row"*. **NO GITHUB ACCESS WAS ASSUMED: the branch is pushed and a pull request was attempted.** -->

---

### 1. The objective

Row `314`, one objective: [ADR-305](../decisions/ADR-305.md) section 7 slice 1. Give `LT-06`'s builder a public door out of `packages/ledger`, without letting `LT-09` and `LT-06` acquire two statements of one arithmetic.

**Row `314`'s third cell governed over the dispatch prompt wherever they differed. They differed in one figure**, and section 4 is that.

---

### 2. What was read, and in what order

`CLAUDE.md`, [STATE](../STATE.md), row `314` in [ALLOCATION](../decisions/ALLOCATION.md) in full, then [`reversal.ts`](../../packages/ledger/src/reversal.ts) whole, [`index.ts`](../../packages/ledger/src/index.ts), [`lt06-posting-timing.test.ts`](../../apps/api/test/lt06-posting-timing.test.ts) whole, [ADR-305](../decisions/ADR-305.md) sections 2 and 7, [ADR-270](../decisions/ADR-270.md) section 2, [ADR-092](../decisions/ADR-092.md) section 5, [ADR-267](../decisions/ADR-267.md), [`ninth-transaction.test.ts`](../../packages/ledger/test/ninth-transaction.test.ts), and session 503.

---

### 3. The absence, re-derived at source before anything moved

| Claim | Verdict |
|---|---|
| `walletWithdrawalApprovalPosting` declared at `reversal.ts:218` with no `export` | **RIGHT.** Read at the line |
| Its only caller at `:265`, inside `walletWithdrawalFailurePosting` | **RIGHT.** A repository-wide grep for the specifier returned the declaration, that call, one comment ([`lt06-posting-timing.test.ts:72`](../../apps/api/test/lt06-posting-timing.test.ts)) and five documents, and no other call |
| The package index did not name it | **RIGHT.** The `./reversal.ts` block carried three constants, `reversalPosting`, `walletWithdrawalFailureKey`, `walletWithdrawalFailurePosting` and two types |

**A re-derivation that agrees is worth reporting as clearly as one that does not**, and both of [ADR-305](../decisions/ADR-305.md)'s `F1` line numbers were right on the day this session read them.

---

### 4. The one figure the row got wrong, and why it is in the entry rather than only here

The row and the dispatch prompt both say the ground the door was shut on *"expired eight entries later"*. **It is three, and it is the same calendar day.** Derived from the entries' own `##` heading dates:

| | Entry | Its own heading date |
|---|---|---|
| Wrote the comment at `:72` | [ADR-267](../decisions/ADR-267.md), whose section 5 names *"`apps/api/test/lt06-posting-timing.test.ts` (new)"* | **2026-08-30** |
| Ruled `LT-06`'s writer, clause 2 | [ADR-270](../decisions/ADR-270.md) | **2026-08-30** |

The older statement of the same ground is `reversal.ts`'s own docblock, written with [ADR-189](../decisions/ADR-189.md) on **2026-08-28**, which is eighty-one entries and two days from the discharge. **Neither reading gives eight.** The substance is untouched: the ground is discharged, and the row's point, that this is a slice and not a reversal of anything, holds exactly. **It is surfaced because a count nobody re-checked is the failure this project has named as its own worst**, and writing "eight" into a source comment because a prompt said so would be repeating it.

---

### 5. What was built

| File | What moved |
|---|---|
| [`packages/ledger/src/reversal.ts`](../../packages/ledger/src/reversal.ts) | `export` on `:218`, docblock above it rewritten at the same line count |
| [`packages/ledger/src/index.ts`](../../packages/ledger/src/index.ts) | One specifier, `:77` |
| [`packages/ledger/test/lt06-approval-builder.test.ts`](../../packages/ledger/test/lt06-approval-builder.test.ts) | New. Five cases |
| [`apps/api/test/lt06-posting-timing.test.ts`](../../apps/api/test/lt06-posting-timing.test.ts) | The `:72` comment only |

**THE COMMENT WAS REWRITTEN RATHER THAN DELETED, WITH BOTH DATES IN IT.** A reader who finds an exported builder under a comment saying it is deliberately private concludes somebody overrode a decision. The comment now names [ADR-267](../decisions/ADR-267.md), [ADR-270](../decisions/ADR-270.md), both dates and the authority that discharged the ground, so what the reader sees is a conditional decision's condition being met.

---

### 6. The falsification pass, and the case that stayed green

Three defects, each seeded into the tree, run, and restored from a `sha256`-verified backup.

| # | Defect | Fired |
|---|---|---|
| 1 | A second `identityAccount('trader_wallet', ...)` leg in a NEW `packages/ledger/src` module | `the wallet leg is stated ONCE`: *"expected [ …(2) ] to have a length of 1 but got 2"* |
| 2 | `walletWithdrawalFailurePosting` stops composing the builder and restates the legs inline | `the composition is one call` **and** `the wallet leg is stated ONCE` |
| 3 | The exported builder's two legs swapped | PIN 1: *"expected 25000n to be -25000n"*, its file's other nine cases green |

**DEFECT 1 IS THE REASON THE CHECK WALKS THE DIRECTORY.** The fork was in a file no list names, and a `.filter` over a hardcoded set would have watched the files rather than the claim, which is the miss [`in-flight-obligation.test.ts`](../../packages/ledger/test/in-flight-obligation.test.ts) records against itself.

**DEFECT 2 IS THE ONE WORTH CARRYING, BECAUSE A CASE STAYED GREEN UNDER IT.** The entry-for-entry negation passed, because the forked legs were byte-identical to the builder's on the day the fork was written. That is exactly the hazard [ADR-092](../decisions/ADR-092.md) section 5 names: **a behavioural case catches two copies only once they have already parted**, which on this path is one commit after a withdrawal was reversed by the wrong amount. The textual case catches the fork itself. Neither is sufficient and the pair is why there are two, and [ADR-314](../decisions/ADR-314.md) section 8 states the textual one's known evasion rather than leaving a founder to find it.

---

### 7. Citations: nothing was spent, and one was left alone deliberately

**THE SAME-LINE-COUNT REWRITE WAS A CITATION DECISION RATHER THAN A STYLE ONE.** Seventeen lines in, seventeen out. This corpus carries **eighteen** `reversal.ts:nn` citations; **ten** of them point below the rewritten docblock (`:218` five times, `:259` five times) and all ten still resolve to the text they name. `RI-15` and `RI-16` were run afterwards and hold. **No citation-repair edit was made anywhere**, which is the cheapest form this row's repair right could take.

**ONE STALE CITATION, REPORTED AND NOT REPAIRED.** [ADR-267](../decisions/ADR-267.md) finding 3 cites [`reversal.ts:202-234`](../../packages/ledger/src/reversal.ts) as `walletWithdrawalFailurePosting`, *"its exact negation"*. That range is the APPROVAL builder's docblock and body; `walletWithdrawalFailurePosting` is at `:261-270`. **It was already wrong before this session and this session did not invalidate it**, so it falls outside a right scoped to citations these edits break, and it sits inside a dated record where `CI-06`'s own ruling is that a rewrite *"would REWRITE THE RECORD TO SAY SOMETHING IT DID NOT SAY"*. It is owed to whichever row next opens that entry.

---

### 8. What this session did not do

**Nothing is wired.** No port gained an adapter, no manifest gained a dependency, [`start.ts`](../../apps/api/src/start.ts) was not opened, and the counts stand at **ten wired of twenty-four declared, fourteen blocked**. No migration was written and no number was taken or reserved. No `RI-nn` was minted. No gate was weakened and no fence was widened. **No assertion in any pre-existing test file was changed, added or deleted.** [ADR-305](../decisions/ADR-305.md) slice 2 (`lt01` into `packages/ledger`) and slice 3 (the ledger handle on `ExpiryTx`) are untouched, and [`index.ts`](../../packages/ledger/src/index.ts) took no conflict, which is what the row said would mean something had gone wrong.

---

### 9. Gates

Each run separately, counts read off each runner's own last line.

| Check | Result |
|---|---|
| `vitest run` | **303** files passed, **7,391** tests passed, **6** skipped |
| `gates.mjs generate` then `check` | **33 of 33 gates pass.** `generate` rewrote one span, `adr_count` 296 -> 297 |
| `repo-invariants.mjs` | **32 of 32 invariants hold** |
| `pnpm run typecheck` | clean |
| `pnpm run lint` | clean |
| `pnpm run format:check` | *"All matched files use Prettier code style!"* |
| `pnpm run verify`, `falsify.mjs` | **NOT RUN.** Forbidden by the row |

---

### 10. Next

**[ADR-305](../decisions/ADR-305.md) slice 2 is now the one to dispatch into [`index.ts`](../../packages/ledger/src/index.ts)**, and it should be told that this branch already appended one specifier to the `./reversal.ts` block, so its own append is a second line in a different block and not a conflict.

**Slices 3, 4 and 5 are unblocked by nothing this session did and were unblocked before it**, per [ADR-305](../decisions/ADR-305.md) section 7. **Slice 7, the driver, is the first that can now compose `LT-06` at all**, and it still needs slices 2, 3 and 5 first.

**Owed and outside every fence this session held**: [ADR-267](../decisions/ADR-267.md) finding 3's citation, above.

---

### Blockers

None. The branch is pushed and mergeable.

### Landmines

**A GREEN BEHAVIOURAL TEST DOES NOT PROVE ONE STATEMENT OF A FACT.** Section 6 defect 2 is the demonstration, in this tree, on the money path: a duplicated builder passes every assertion about its output until the copies drift. Anything guarding a one-statement property needs a reader of the text as well.

**THE `the wallet leg is stated ONCE` CASE HAS A KNOWN EVASION AND A KNOWN FALSE POSITIVE.** It counts lines containing `identityAccount('trader_wallet'` across `packages/ledger/src`. A builder taking the account as a parameter passes it; a second legitimate posting that debits `trader_wallet` (`LT-08` would be one, and it lives in `apps/api` today) trips it and has to be admitted deliberately. **The admission cost is the point rather than a defect**, and a session that finds this case red should read [ADR-314](../decisions/ADR-314.md) section 8 before deleting it.
