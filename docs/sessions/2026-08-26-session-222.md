---
status: approved
depends_on: [README.md]
last_updated: 2026-08-26
---

## 2026-08-26 - Session 222: the provisioning saga, and an admission nobody can construct without the evidence
<!--index: **[ADR-107](../decisions/ADR-107.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH and the `E2` read is owed.** [P3 wave 3](../plans/P3-wave3-modules.md)'s `P3-l` is written: seven files under [`apps/worker/src/provisioning/`](../../apps/worker/src/provisioning/index.ts) and **62 tests**, with **no route, no `packages/db` file, no migration, no dependency and no shared file of any kind**. **THE RULING IN ONE LINE IS THAT THE FAIL-CLOSED EXIT IS A VALUE WITH ONE PRODUCER RATHER THAN A CHECK THAT REFUSES**: `SetpointConfirmation` is branded on a module-local `unique symbol`, `setpointConfirmation` is its only producer, and `TradingAdmission`'s admitted arm carries one as a REQUIRED field, so **an admission cannot be CONSTRUCTED without the evidence** and `admitToTrading` is a fold seeded at a refusal whose loop has no `else` -- deleting the loop entirely leaves every account out of trading. **`entitleAfterSetpoint` TAKES THE CONFIRMATION AS A REQUIRED ARGUMENT, which makes `INV-M2-15`'s ordering the ARGUMENT LIST rather than a step list somebody follows.** **`confirmed_inferred` IS NEVER EVIDENCE, FOR ANY OPERATION**, which is the form of [M02](../plans/M02-rithmic-bridge.md) section 3.6.2's finding that needs no migration, and the anchor is the account's CURRENT floor, which is `INV-M2-08` composed with `INV-M2-13` rather than a staleness rule somebody has to write. **ADR-006's TRANSACTIONAL-ENQUEUE CRITERION HAS A CALLER FOR THE FIRST TIME** and is asserted in both directions against a model that refuses to make a statement durable unless the connection that ran it committed. **THE CENTRAL FINDING IS THAT [ADR-102](../decisions/ADR-102.md)'s SYSTEM DOOR CANNOT NAME A ROW**, executed rather than argued: `SystemTx.update` renders `update "provisioning_queue" set "status" = $1 returning ...` with **NO `WHERE` CLAUSE**, and `updateStatement`, `deleteStatement` and `selectStatement` each take `where` in a REQUIRED position while [`scoped-db.ts:685`](../../packages/db/src/scoped-db.ts)'s `systemTx` passes `undefined` for all three with no parameter to override it, so a status advance through the accessor writes **EVERY ROW IN THE TABLE** -- worse than the `semi-join` class that entry inherits and names as open, because a superset has no bound. **REPORTED, NOT ROUTED AROUND**: `sqlExecutor` would run it and its one-member vocabulary is `'job-enqueue'`, which is the deliberate door, so `ProvisioningAdvancePort` and `ProvisioningReadPort` are declared and BLOCKED on their own declarations and **`0048` stays free**. **COMPENSATION IS STATED AS THREE ANSWERS RATHER THAN AS A MECHANISM**: five of the seven operations in `0007`'s CHECK list have NO INVERSE, for three different reasons; nothing handed to the vendor is ever rolled back, so compensation is forward-only; and **when compensation itself FAILS the account is in the state it was already in**, not because anything noticed but because admission needs positive evidence and a failed compensation produces none. **THE SUITE FOUND A DEFECT IN ITS OWN SUBJECT ON ITS FIRST RUN**: the brand's key was a `declare const ... unique symbol`, which type-checks, satisfies every use and **ERASES**, so under `node --experimental-strip-types` the only producer of an admission threw `ReferenceError` on five cases while `tsc --noEmit` reported nothing -- `RI-10`'s class in a construct `RI-10` does not look at. **TEN MUTATIONS SEEDED AND ONE SURVIVED, REPORTED AS A FINDING WITH ITS CASE**: dropping `frame(key)` from the canonical serialization left **all sixty tests green**, because the pair the suite tested is disambiguated by the value framing alone, and the pair that needs key framing carries the SAME VALUES UNDER DIFFERENT FIELD NAMES -- a payload naming `risk_floor_cents` and one naming `account_ref` with the same number are one intent under `provisioning_queue_intent_uq`, so the second is silently not enqueued and an account is left at a floor nobody pushed. **FOUR COMPILE-TIME REFUSALS WATCHED FAILING BY THEIR ERROR** and every seed removed with `git status --porcelain` empty. **THREE DISAGREEMENTS BETWEEN THE DISPATCH AND THE PLAN ARE REPORTED**, and the registration clause was **ALREADY DISCHARGED** by [session 205](2026-08-25-session-205.md). **`packages/rithmic`'s `PlatformAdapter` IS NOT M02 SECTION 2.1's** -- `provision()` and `entitle()` are NULLARY there -- and the package is outside this fence, so the port carries M02's shape. Suite **121 files / 1,950 passed / 1 skipped** to **122 files / 2,012 passed / 1 skipped**, `typecheck` exit 0, `eslint apps/worker` exit 0, invariants **9 of 9**, gates **30 of 30**, `verify` exit 0, `apps/worker` **loads under `node --experimental-strip-types` with 44 exports**, and the registry is **100 of 111** where the dispatch said 99 -->

- **Objective:** the `provisioning_queue` driver, the `ProvisioningOp` pipeline, `provision` and `entitle` against the simulator, compensation, and `INV-M2-13`'s fail-closed exit. [P3 wave 3](../plans/P3-wave3-modules.md) section 9's `P3-l` row and section 15's prompt. **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **One ADR: [ADR-107](../decisions/ADR-107.md)**, reserved before dispatch.
- **Done:** [ADR-107](../decisions/ADR-107.md) at `status: proposed`, approval line UNSIGNED and written as **five COMMANDS with their expected output** rather than as a count, all five executed before the entry was committed. Seven new files under [`apps/worker/src/provisioning/`](../../apps/worker/src/provisioning/index.ts): [`vocabulary.ts`](../../apps/worker/src/provisioning/vocabulary.ts), [`payload.ts`](../../apps/worker/src/provisioning/payload.ts), [`machine.ts`](../../apps/worker/src/provisioning/machine.ts), [`admission.ts`](../../apps/worker/src/provisioning/admission.ts), [`compensation.ts`](../../apps/worker/src/provisioning/compensation.ts), [`ports.ts`](../../apps/worker/src/provisioning/ports.ts) and [`saga.ts`](../../apps/worker/src/provisioning/saga.ts), plus [`test/provisioning.test.ts`](../../apps/worker/test/provisioning.test.ts) at **62 tests**. Suite **121 files / 1,950 passed / 1 skipped** to **122 files / 2,012 passed / 1 skipped**.
- **Next:** **[ADR-102](../decisions/ADR-102.md)'s system door needs a predicate and that is the largest thing this session hands on.** It blocks the status advance and the per-account read for this saga and for anything else that has to name a row through `systemDb`; it is a superseding entry rather than a commit, and the shape it should take is a ruling about what the system authority MEANS. **The manifest line [`apps/worker/src/index.ts`](../../apps/worker/src/index.ts) has asked for since [session 147](2026-08-24-session-147.md) is still owed**: `@merit/db`, `@merit/queue` and `@merit/rithmic` are all undeclared and `node-linker=isolated` makes an undeclared import unresolvable, so the wiring session adds three importer entries and one `pnpm-lock.yaml` block. **[`packages/rithmic`](../../packages/rithmic/src/index.ts)'s `PlatformAdapter` still carries the nullary scaffold signatures** and reconciling it with [M02](../plans/M02-rithmic-bridge.md) section 2.1 is somebody's. **Whether the fail-closed assertion discharges `GS-138` is a ruling nobody has taken** and [section 39](../testing/golden-scenarios/39-fixture-status-and-blockers.md) is untouched.
- **Blockers:** none that stopped this session. Two ports are declared and BLOCKED on the finding above, and both say so on their own declarations rather than in a comment.
- **Landmines:**
  - **`declare const X: unique symbol` TYPE-CHECKS, SATISFIES EVERY USE, AND ERASES.** `tsc --noEmit` exits 0 and, under `node --experimental-strip-types`, a computed key `[X]` is a reference to a binding that does not exist. The only producer of an admission in this module threw `ReferenceError` on its first run and **nothing in the type system reported anything**, because a `declare` is a promise that something else supplies the value. This is `RI-10`'s class -- type-checks and cannot run -- **in a construct `RI-10` does not look at**, and nothing in the corpus warns about it. A brand written this way in a money path is a control that is absent at runtime and present in the review.
  - **[ADR-102](../decisions/ADR-102.md)'s `SystemTx` REACHES EVERY ROW OF EVERY TABLE AND CANNOT NAME ONE.** `update`, `delete` and `rows` all render with no `WHERE` clause. The three statement builders each declare `where` in a **required** argument position, deliberately, and `systemTx` hands them `undefined`. **The danger is that it looks like an ordinary accessor call**: `tx.update('provisioningQueue', { status: 'delivered' })` reads as one row and writes all of them, with every type in the workspace green. On `provisioning_queue` a constraint that exists for a different reason (`provisioning_queue_delivered_has_timestamp`) happens to catch it; on the `SELECT` nothing does.
  - **A CANONICAL SERIALIZATION THAT FRAMES ONLY VALUES IS NOT INJECTIVE OVER A MAP.** `hash.ts`'s framing is injective for a FIXED COLUMN LIST, where position identifies the field. A payload is a map, so the key has to be framed too, and **the obvious test for it does not catch its absence**: `{ab:'c'}` against `{a:'bc'}` is disambiguated by the value framing alone. The pair that catches it has the same values under different names. This survived a seeded mutation at 60 of 60.
  - **`provisioningFileName` IS NOT IDEMPOTENT OVER TIME AND [M02](../plans/M02-rithmic-bridge.md) SECTION 3.3 READS AS THOUGH IT IS.** That section says "The same intents always produce the same filename" and specifies a name carrying `yyyymmdd_hhmmss`. The corpus resolves its own tension one line away and the resolution is a COLUMN: `0007:280`'s `file_name` is "the idempotent name, assigned at batch build", and the retry edge is "same `payload_hash`, **same file name**". **A retry that recomputes re-uploads under a new name**, which is the double-application `INV-M2-02`'s outbound mirror exists to prevent.
  - **FIVE OF THE SEVEN PROVISIONING OPERATIONS HAVE NO INVERSE, SO THE SAGA'S SAFETY IS NOT IN ITS COMPENSATION.** `create_user` has none because a platform login is per human and shared across accounts; `set_risk` and `set_permissions` have none because they are setpoints rather than acts and "undoing" one means pushing a value nobody currently believes in, which `INV-M2-08` forbids; `disable_account` and `disable_entitlement` **are** the compensations. A reader who takes "saga" to mean "compensating transactions" will look for a rollback that is not there and cannot be.
  - **THE COMPENSATION ORDER IS NOT THE REVERSE OF THE FORWARD ORDER.** [M02](../plans/M02-rithmic-bridge.md) section 3.6 is `disable_entitlement` then `disable_account`, "fail-closed on the way out". Reversing the forward order gives `disable_account` first, which **leaves a live entitlement behind on exactly the failure the ordering exists for**, and it is the natural thing to write.
  - **`'nightly-batch'` IS DOING MORE WORK THAN ITS OWN COMMENT CLAIMS.** [M02](../plans/M02-rithmic-bridge.md) section 3.1 makes the provisioning drain `ST-M2-9`, a stage of the nightly batch, so the word is honest; section 3.6.1 records that **the provisioning CSV push runs every 15 minutes**. A fifteen-minute cron reading a word that says "nightly" is [ADR-102](../decisions/ADR-102.md) section 7's own argument against the member it refused, arriving on the two it kept.
  - **`packages/rithmic`'s `PlatformAdapter` IS A PLACEHOLDER AND READS LIKE A CONTRACT.** `provision(): Promise<PlatformAccountId>` and `entitle(): Promise<void>` are both nullary; [M02](../plans/M02-rithmic-bridge.md) section 2.1 declares both with arguments. A later session that implements the package's signature will find it cannot express a batch at all.
- **Files touched:** [`apps/worker/src/provisioning/vocabulary.ts`](../../apps/worker/src/provisioning/vocabulary.ts), [`payload.ts`](../../apps/worker/src/provisioning/payload.ts), [`machine.ts`](../../apps/worker/src/provisioning/machine.ts), [`admission.ts`](../../apps/worker/src/provisioning/admission.ts), [`compensation.ts`](../../apps/worker/src/provisioning/compensation.ts), [`ports.ts`](../../apps/worker/src/provisioning/ports.ts), [`saga.ts`](../../apps/worker/src/provisioning/saga.ts), [`index.ts`](../../apps/worker/src/provisioning/index.ts) and [`test/provisioning.test.ts`](../../apps/worker/test/provisioning.test.ts) (all new), [`apps/worker/src/index.ts`](../../apps/worker/src/index.ts), [ADR-107](../decisions/ADR-107.md) (new), [ALLOCATION](../decisions/ALLOCATION.md) (its row, amended in place when the file landed), [decisions/README](../decisions/README.md) and [INDEX](../INDEX.md) (generated), [STATE](../STATE.md) (one appended `##` section plus generated count spans), [sessions/README](README.md) (row 222 struck, entry row generated), this file. **Nothing outside the fence.** [M02](../plans/M02-rithmic-bridge.md), [ADR-102](../decisions/ADR-102.md), [ADR-104](../decisions/ADR-104.md), [ADR-006](../decisions/ADR-006.md), [`0001_extensions_and_enums.sql`](../../packages/db/migrations/0001_extensions_and_enums.sql), [`0007_accounts.sql`](../../packages/db/migrations/0007_accounts.sql), [`scoped-db.ts`](../../packages/db/src/scoped-db.ts), [`scope.ts`](../../packages/db/src/scope.ts), [`schema.ts`](../../packages/db/src/schema.ts), [`job-queue.ts`](../../packages/queue/src/job-queue.ts), [`packages/rithmic`](../../packages/rithmic/src/index.ts), [`hash.ts`](../../packages/rules-engine/src/hash.ts) and [`batch/ports.ts`](../../apps/worker/src/batch/ports.ts) were **read and none was written**.

### What was NOT done, named rather than implied

**No route, no network call, no vendor SDK, no dependency, no migration, no migration number, no `CI-06` letter, no gate register row, no `VG` row, no `RI-nn`, no `SD-nn`, no `OI-nn`, no fixture and no edge-case entry.** No file under [`packages/`](../../packages/db/src/schema.ts) is touched at all, and no app but `worker`, so this ran beside session 215 with no shared file. **[`pnpm-workspace.yaml`](../../pnpm-workspace.yaml) and [`pnpm-lock.yaml`](../../pnpm-lock.yaml) are both untouched**, which is the first slice in this wave for which the second is also true: `apps/worker` is an existing importer and this session added no dependency to it.

**NO UPDATE STATEMENT EXISTS IN THIS MODULE AND THAT IS THE FINDING RATHER THAN AN OMISSION.** The transition is DECIDED by [`machine.ts`](../../apps/worker/src/provisioning/machine.ts) and has to be APPLIED by something that can name a row. Nothing in this repository can.

**NO CSV IS RENDERED.** The batch's file NAME is computed, because `0007` needs it in `file_name` and M02 section 3.3 specifies it. The file's CONTENT is the adapter's, and `V-M2-07` (whether the vendor treats a repeated filename as replace or as error) plus the whole of `assumptions.ts` are unconfirmed.

**NO `platform_entitlements` ROW IS WRITTEN.** `entitleAfterSetpoint` calls the port and the port writes nothing. The hygiene ledger `FM-M2-11` exists for is a different slice's.

### The registration clause was already discharged, verified on the tree rather than trusted

[P3 wave 3](../plans/P3-wave3-modules.md) section 15's prompt says so and instructs the session to check rather than believe it: `grep -c "pgTable('provisioning_queue'" packages/db/src/schema.ts` returns **1**, and [`scope.ts:903`](../../packages/db/src/scope.ts) carries the rule, `derived` through `account_id`. [Session 205](2026-08-25-session-205.md) landed it while the plan's branch was open. **So this slice's "one registration" is already done and the honest form is to say so**, which is [ADR-092](../decisions/ADR-092.md)'s own stated cost -- "a session cannot know its own slice size before it runs" -- arriving in the good direction.

### The three disagreements between the dispatch and the plan, reported

**1. The branch name.** The dispatch named `claude/s222-p3l-provisioning-saga` and [P3 wave 3](../plans/P3-wave3-modules.md) section 15 names `claude/p3l-provisioning-saga`. **The plan wins on substance by its own instruction and a branch name is not substance**: the designated-branch instruction is the harness's and binds where the work lands. This is [session 213](2026-08-25-session-213.md)'s and [session 217](2026-08-25-session-217.md)'s disagreement recurring identically, which makes it **a standing property of the dispatch template rather than a one-off**, now on its third occurrence.

**2. The ADR, and this one went the dispatch's way.** Section 15's prompt says *"Your session-log number is 222. **No ADR expected.**"* The dispatch reserved `ADR-107` for this branch on `main` before dispatching it and instructed the session to write the entry under it. **The entry is written**, for two reasons that are the plan's own rather than the dispatch's: [ADR-102](../decisions/ADR-102.md) section 8 item 2 names this slice by number and says *"both must say which they are, and that is the point"* about the write authority, and item 5 names its compensation path -- **two rulings the plan itself demands be stated, and a ruling is an ADR and not a commit**. The reservation row also names this branch, so leaving it unwritten leaves a dangling reservation on `main`.

**3. The fence, and section 5.3 of the plan already corrected section 15.** Section 15's fence holds `packages/db/test/scoped-db.test.ts` (its row ONLY); section 9's row and **section 5.3** both record that session 205's merge removed every `packages/db` file from this slice. **No `packages/db` file is touched.** Separately, section 14's dispatch-order block reads `P3-l the provisioning saga MONEY (needs P3-f, P3-h)` while section 9's row says *"No `packages/db` file and no route, so NOT `P3-h`"*; **the row is the reasoned position and the ASCII block is stale.** This session ran with `P3-h` (session 215) still open and shared no file with it.

### One dispatch baseline was stale, re-derived rather than trusted

**The dispatch said the registry is `99 of 111`, at its ceiling. It is `100 of 111`.** Re-derived on `30e6f64` by reading `TABLE_KEYS` and `SCOPE_RULES`: 1 `root`, 40 `owned`, 19 `derived`, 40 `firm`. [Session 216](2026-08-25-session-216.md) merged `ledger_halts` and [`scoped-db.test.ts:322`](../../packages/db/test/scoped-db.test.ts) says so in its own comment. **A count is a fact about a tree at a commit**, which is [ADR-102](../decisions/ADR-102.md) section 9's identical correction of its own dispatch. Every other baseline held exactly: gates **30 of 30**, invariants **9 of 9**, `pnpm exec vitest run` **121 files / 1,950 passed / 1 skipped**, `verify` exit 0.

### The seeds, and the one that survived

**Ten runtime mutations, four compile-time refusals, every one watched and every one removed.**

| Seeded | Result |
|---|---|
| nothing (this branch) | **62 passed** |
| `confirmed_inferred` accepted as confirmation | **2 failed / 58 passed** |
| the current-floor equality dropped (`INV-M2-08`) | **1 failed / 59 passed** |
| `setpointConfirmation` short-circuited to always confirm | **8 failed / 52 passed** |
| the `set_risk` inferred ban removed from the machine | **1 failed / 59 passed**, the case looping all six statuses |
| **the enqueue reaching its OWN connection** (`ADR-006`) | **3 failed / 57 passed** |
| the type tag removed, so `'100'` and `100n` collide | **1 failed / 59 passed** |
| compensation skipped on the provision failure | **3 failed / 57 passed** |
| `buildBatch` recomputing the file name on a retry | **1 failed / 59 passed** |
| **`frame(key)` dropped from the canonical serialization** | **SURVIVED at 60 of 60.** Two cases added; it now fails 2 |
| an admitted arm returned with no evidence | `TS2322: Property 'evidence' is missing in type '{ admitted: true; }'` |
| a `SetpointConfirmation` forged by an object literal | `TS2741: Property '[SETPOINT_CONFIRMATION]' is missing` |
| `entitleAfterSetpoint` called without the evidence | `TS2554: Expected 3 arguments, but got 2` |
| an eighth operation added with no compensation row | `TS2741: Property 'delete_user' is missing in type` |

**One seed was designed wrong and is reported as such**: rewriting the fold's guard from `if (evidence !== null) admission = ...` to `if (evidence === null) continue; admission = ...` left 60 of 60 green, and it should have -- the two are the same program. **A refactor is not a mutation**, and the property it was meant to probe is the one the compile-time seeds cover: an admitted arm cannot be returned without a value only one function produces.
