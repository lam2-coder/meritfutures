---
status: approved
depends_on: [README.md]
last_updated: 2026-08-28
---

## 2026-08-28 - Session 323: `P7-l`, and the alarm's independence from the job turns out to be expressible as a parameter list

<!--index: **[P7](../plans/P7-risk-and-abuse.md) section 8's `P7-l`. [`0040`](../../packages/db/migrations/0040_report_schedules.sql) landed `report_schedules` and `report_deliveries` with ZERO ROWS on merge, deliberately, "so nothing below can be read as evidence that any digest has ever been delivered"; both named digests have producers now and so does the dead-man switch.** **`CRON_INVENTORY:35`'s RULE IS A PROPERTY OF A PARAMETER LIST AND NOT A COMMENT**: `DigestAlarmIo` carries a transaction and a clock and NOTHING a producer could hand it, [`alarm.ts`](../../apps/worker/src/digests/alarm.ts) imports nothing from `produce.ts` and the suite reads its source as text, and the handle the alarm receives HAS NO `insert`. A job that crashed after writing "success" and a job that never ran are the same fact to the person who did not get the digest, so they are the same input: neither is an input at all. **`GS-288` IS ASSERTED IN THREE SHAPES**, and the named one is `5.1`: the transport succeeds, the producer builds a valid `delivered` row, the insert is swallowed, the run reports **`delivered: 1`**, the table holds **nothing**, and the alarm fires. **THE ANCHOR IS UNSTATED AND NO NUMBER WAS INVENTED FOR IT**: the cadence is a schema fact confirmed against PostgreSQL 16 for all four digests, but WHICH weekday at WHICH hour in WHICH zone is stated nowhere, so `DIGEST_WINDOW_ANCHOR` ships EVERY term `unstated` and the alarm measures elapsed time from each schedule's OWN history, which is the same fact up to one period late; **the alternative was to invent a weekday, and an alarm firing against a window nobody agreed to is `GS-287` with the sign flipped**. **`CADENCE_PERIOD_MS.monthly` IS `null`** and a monthly schedule is reported as `cadence_unanchored` rather than passed over, because a 30-day constant would have been a number this slice invented. **THE `P7-l` ROW CALLS THESE "the two MUST digests" AND [M06](../plans/M06-admin-ops-console.md) SECTION 3.6 AND [ADR-066](../decisions/ADR-066.md) SECTION 3 BOTH DISAGREE**: `weekly_flag_queue` is sized **SHOULD**, the second MUST is `daily_liability`, and its content waits on `P5-l`'s `readLiability`. Recorded beside `PRODUCED_DIGESTS`, not repaired. **A DIGEST WITH NO PRODUCER GETS NO ROW**, which is `0040`'s own design: there is no `skipped` outcome and "it writes nothing and the missing row is itself the finding". **`GS-290` IS ENFORCED TWICE**, a downgrade and a refusal, and **all eleven `report_deliveries` `CHECK`s were watched firing in PostgreSQL 16**, including the documented surprise that a fifth digest is refused by `cadence`'s `NOT NULL` rather than by `digest`'s own `CHECK`. **`INV-M6-10` IS SWEPT OVER THE RENDERED ARTIFACT** and the flag-queue link points at the QUEUE and never at a subject, a reading stated rather than assumed. **THE BARREL'S LEGS ARE DATA NOW AND A SEED PROVED MODULE GRANULARITY IS NOT ENOUGH**: deleting ONE of a module's two export blocks left the module a specifier, left the leg check green and left `typecheck` at ZERO ERRORS, so `9.4` asserts the barrel re-exports every NAME of this slice's four modules. **Writing the lists found [`batch/statistics.ts`](../../apps/worker/src/batch/statistics.ts), which this barrel has never re-exported**; recorded, not repaired. **TWENTY-ONE DEFECTS SEEDED AND ALL TWENTY-ONE CAUGHT, TWO AS MISSES FIRST**, and **THE SUITE FOUND A REAL DEFECT IN THIS SESSION'S OWN CODE**: `report_deliveries_failure_states_its_reason` was written with one negation too many, refusing every well-formed `delivered` row and accepting every `failed` one. **TWO FENCE NOTES**: `breaker.test.ts` carries a hand-written leg count any barrel slice invalidates, corrected 17 to 21 and reported; and **session 320's reported defect in [`db.test.ts`](../../apps/worker/test/db.test.ts) bit again**, its scan reading a wrapped message ending in *"from"* as an import, reworded inside this fence, **the regex still wrong and still nobody's**. 33 of 33 gates, 14 of 14 invariants, typecheck 0, lint 0, format clean, **209 files / 4,837 passed / 6 skipped** against a `main` baseline of 208 / 4,764 / 6 reproduced exactly first, `falsify` clean. **NO ADR NUMBER, NO MIGRATION NUMBER, no `packages/db` change, no contract change, no `SqlExecutorReason` or `SystemReason` member, no `pg` import, no cast past a key type, and `apps/api` untouched so the `{declared, wired, blocked}` triple does not move.** -->

- **Objective:** [P7](../plans/P7-risk-and-abuse.md) section 8's `P7-l`. `weekly_loss_ratio_cusum`
  and `weekly_flag_queue` acquire producers against
  [`0040`](../../packages/db/migrations/0040_report_schedules.sql), and the delivery-failure alarm
  asserts the QUERY and never the job's own report
  ([CRON_INVENTORY](../ops/runbooks/CRON_INVENTORY.md), `GS-288`).

- **Done:** **Five files, 73 new cases.**
  [`apps/worker/src/digests/ports.ts`](../../apps/worker/src/digests/ports.ts) (the vocabularies
  transcribed from `0040`, the read union of two and the write union of one, `ADR-157`'s one term,
  the cadence periods, the anchor with every term `unstated`, both bodies, the transport and the two
  unwired doors), [`rows.ts`](../../apps/worker/src/digests/rows.ts) (the column readers, separate so
  the alarm need not import the producer), [`alarm.ts`](../../apps/worker/src/digests/alarm.ts) (the
  dead-man switch, four finding kinds on three kinds of evidence),
  [`produce.ts`](../../apps/worker/src/digests/produce.ts) (the CSV artifact, the SHA-256, the row
  builder mirroring all eleven `CHECK`s, and the run), and
  [`test/digests.test.ts`](../../apps/worker/test/digests.test.ts) (**73 cases in ten sections**).
  Plus the barrel's digests block and its three leg lists.
  **Every claim above was measured on this branch with each command run separately**, and the `main`
  baseline was reproduced before a line changed. `0040`'s constraints were additionally watched
  firing against **PostgreSQL 16** with the full migration set applied.

- **Next:** **The adapter and the scheduler, and neither is a door.** `DigestIo` is satisfied
  structurally by [`src/db.ts`](../../apps/worker/src/db.ts)'s `LIVE_DB.batch` with no `SystemReason`
  member added; what is missing is a transport, a `tradingDayOf` supplier and a scheduler, and the
  scheduler is `ADR-086`'s absent job store rather than this slice's gap. **`daily_liability` is the
  MUST with no producer** and it needs `AdminReadSource.readLiability`, which is `P5-l`'s: that one
  slice closes the C8 ritual's remaining input. **The flag-queue content port needs a supplier**,
  which is `P7-i`'s queue in `apps/api` and cannot be imported from here. **`DEP-M6-05` unblocks the
  CUSUM**, and until it lands the digest carries the blocker's name rather than a chart.

- **Blockers:** **`DIGEST_WINDOW_ANCHOR`'s three terms are the founder's or a ruling's.** No approved
  document names the weekday, the hour, the zone or the day of month a digest window closes on, and
  `CRON_INVENTORY`'s Expected-by cell for this row reads *"the schedule's own window"*, which names a
  thing rather than defining one. **The alarm works without them** and says what that costs; a
  wall-clock alarm does not exist until they are stated. **`monthly_revenue_cohort` cannot be
  evaluated for a missed window at all** and is reported as `cadence_unanchored`.

- **Landmines:**
  1. **[`apps/worker/test/breaker.test.ts`](../../apps/worker/test/breaker.test.ts) carries a
     hand-written barrel-leg count that EVERY barrel-editing slice invalidates.** Corrected 17 to 21
     here. `WORKER_BARREL_LEGS` in the barrel is the same list as data and is the better subject for
     that assertion; the enumeration was kept because two independent lists that must agree is a
     stronger check than one.
  2. **Session 320's reported defect in [`db.test.ts`](../../apps/worker/test/db.test.ts) is still
     live and it bit this session.** Its bare-specifier scan reads a wrapped message ending in the
     word *"from"* as an import declaration. One comment string here ended a segment that way and the
     full suite went red on a string, not on a dependency. Reworded inside this fence; **the regex is
     unrepaired and belongs to nobody**.
  3. **A leg check at MODULE granularity does not catch the historical failure.** Deleting one of a
     module's two export blocks leaves the module a specifier and `typecheck` at zero errors. Seeded
     and watched passing before `9.4` existed.
  4. **[`batch/statistics.ts`](../../apps/worker/src/batch/statistics.ts) has never been re-exported
     by this barrel.** Its suite imports it by path. Recorded in
     `WORKER_MODULES_NOT_RE_EXPORTED` with its reason rather than added, because adding a leg is a
     decision for the slice that owns that file.
  5. **The `P7-l` row's "the two MUST digests" is false against `M06` section 3.6 and `ADR-066`
     section 3.** `weekly_flag_queue` is a SHOULD. Nothing about the build changes; the sizing claim
     does, and a later reader taking the row at its word would think the MUSTs are covered.

- **Files touched:** [`apps/worker/src/digests/ports.ts`](../../apps/worker/src/digests/ports.ts),
  [`rows.ts`](../../apps/worker/src/digests/rows.ts),
  [`alarm.ts`](../../apps/worker/src/digests/alarm.ts),
  [`produce.ts`](../../apps/worker/src/digests/produce.ts),
  [`apps/worker/test/digests.test.ts`](../../apps/worker/test/digests.test.ts),
  [`apps/worker/src/index.ts`](../../apps/worker/src/index.ts),
  [`apps/worker/test/breaker.test.ts`](../../apps/worker/test/breaker.test.ts) (one leg count, landmine 1),
  [STATE](../STATE.md) (appended) and this log.
