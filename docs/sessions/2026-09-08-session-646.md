---
status: approved
depends_on: [README.md]
last_updated: 2026-09-08
---

## 2026-09-08 - Session 646: four cases asserted a write PostgreSQL refuses, the defect is twenty tables wide and not the two the sibling reported, and the widening it blocked is now green and watched by nothing

<!--index: **THE ROW IS [ADR-451](../decisions/ADR-451.md), `proposed` and UNSIGNED. NOT A MONEY DIFF, `E2` READ OWED.** [ADR-448](../decisions/ADR-448.md) section 8 item 1 taken and DISCHARGED. `setFor` at [`keyed-accessor.test.ts:196`](../../packages/db/test/keyed-accessor.test.ts) and `someOtherColumn` at [`write-accessor.test.ts:180`](../../packages/db/test/write-accessor.test.ts) each skipped the TENANCY columns and returned the first column left, which on every table carrying an identity `id` IS `id`, so **four cases drove an UPDATE naming a `GENERATED ALWAYS AS IDENTITY` column, which PostgreSQL answers `428C9` to**, and were green because no database is involved. Both helpers now also skip the columns the DATABASE writes, read off `column.generated` and `column.generatedIdentity`. **ALL FOUR CASES ARE CORRECTED AND NONE IS SKIPPED, DISABLED, QUARANTINED OR DELETED**, and every assertion each carried still runs, because none of them ever asserted anything about WHICH column was named. **THE DEFECT IS TWENTY TABLES WIDE AND `ADR-448` REPORTS TWO**: derived here, both helpers pick a database-written column on **20** tables, **10** in `SCOPED_KEYS` and **10** in `FIRM_KEYS`, and the two that entry names are the FIRST member of each list, which is what a per-key `expect` label reports when it fails. A failure label is a sample and it was written down as a census. **`isGenerated` RE-DERIVED RATHER THAN INHERITED AND THE SIBLING WAS RIGHT**: on the pinned `drizzle-orm`, `'isGenerated' in column` is false on **all 1367** columns of the registry; `generated` is **5** on **4** tables and `generatedIdentity` is **20**, every one named `id` and every one `always`, **25** database-written columns on **22** distinct tables. **NOT ONE CITED LINE MOVED**: **3** distinct lines cited into `keyed-accessor.test.ts` and **6** into `write-accessor.test.ts`, over **11** citing documents of which **one is LIVE SOURCE OUTSIDE THIS FENCE**, [`generated-column-writes.mjs:505`](../../packages/tooling/checks/generated-column-writes.mjs), so a moved pointer could not have been repaired; all **9** captured before the first edit and diffed byte-identical after the last, the in-function edits REPLACING lines one for one and both helpers APPENDED past the last cited line on [ADR-426](../decisions/ADR-426.md)'s remedy. **THE PRICE'S OWN WORDING IS REPORTED WRONG**: it names "the set each already skips" and only `setFor` has a set, `someOtherColumn` holding one `string` compared with `!==`. **THE CONTROL WATCHED IN BOTH DIRECTIONS**: the identity widening seeded at [`scoped-db.ts:4690`](../../packages/db/src/scoped-db.ts) turns exactly `ADR-448`'s four cases red on the UNCORRECTED helpers and leaves the WHOLE SUITE GREEN on the corrected ones, the file reverted and `git hash-object` verified byte-identical `57675a14530fe7960dbafb977c1964384600aade` both times and never with `git checkout --`. **SO `ADR-448` SECTION 8 ITEM 2 IS NOW TAKEABLE, AND THIS ROW FOUND WHAT STILL STANDS BEFORE IT**: [`generated-column-writes.mjs`](../../packages/tooling/checks/generated-column-writes.mjs) prints a WORD-FOR-WORD IDENTICAL `PASS` on the tree with the refusal and on the tree without it, saying in its own output that it reaches 20 identity columns and "does not assert on" them, **so the widening could land, do nothing, and redden nothing**; the clause and the leg change want ONE commit rather than two. **`RI-15` FIRED ON THIS ROW'S OWN RECORD OF A POINTER FINDING**, stitching a backticked `:442` shorthand onto an earlier `repo-invariants.mjs` and landing on a blank line; the invariant was right, the draft was wrong, and that is THREE CONSECUTIVE ENTRIES in which the pointer invariants caught the record of a pointer finding. **NO `scoped-db.ts`, NO `schema.ts`, NO `scoped-db.test.ts`, NO FILE UNDER `packages/tooling/` which row `450` holds this wave, NO MIGRATION, NO `apps/` FILE, NO GATE WEAKENED, NO TEST SKIPPED, NO LANDED ADR EDITED, NO `RI-nn` MINTED, NO `CI-06` LETTER SPENT.** **`CI-06f` IS RED AT `450` AND IS REPORTED RATHER THAN REPAIRED**, on the precedent rows `434`, `437`, `440`, `444` and `449` set: this row was told to claim `451` and no other cell, and [ALLOCATION](../decisions/ALLOCATION.md)'s own `053`/`054`/`055` paragraph already rules that a number is reserved by whoever hands it out. Counts derived at reporting time: base `origin/claude/wave-448s` at `5b7fe6c` reproduced BEFORE the first edit and GREEN, suite **344 files / 8,612 passed / 11 skipped / 0 failed**, invariants **35 of 35**, gates **33 of 33**, typecheck, lint and `format:check` clean; branch **344 / 8,612 / 11 / 0**, invariants **35 of 35**, gates **32 of 33** on the `CI-06f` hole at `450` and nothing else, typecheck, lint and `format:check` clean, **delta +0 files, +0 passed, +0 skipped, +0 failed**, which is what a CORRECTION looks like rather than a null result. `ADR-448`'s **8,605** and **8,607** are a different base two merges back and are not what this row subtracts from. **THE CONTAINER ARRIVED WITH NO REPOSITORY** and with no `node_modules`. -->

### 1. The objective

Discharge [ADR-448](../decisions/ADR-448.md) section 8 item 1: four cases across [`keyed-accessor.test.ts`](../../packages/db/test/keyed-accessor.test.ts) and [`write-accessor.test.ts`](../../packages/db/test/write-accessor.test.ts) drive an UPDATE naming an `always` identity column, which is a write PostgreSQL refuses. That entry states it is owed on its own terms whether or not the identity widening in its item 2 is ever taken, because a case asserting a statement the database would reject asserts nothing about the code under test.

### 2. Done

- **The container measured and reported.** No repository on arrival, no `node_modules`; `add_repo` with `access: "push"`, one clone, install from the lockfile.
- **The base reproduced at `5b7fe6c` on `origin/claude/wave-448s`, not `main`, before the first edit, and it is green** on all six legs.
- **The Drizzle properties re-derived rather than inherited**, on a throwaway probe over all 116 registry tables. `isGenerated` is not merely `undefined`; the key is absent on all 1367 columns, which is the stronger statement.
- **The population derived rather than quoted**, by replicating both helpers against the real registry. It is **20** tables and [ADR-448](../decisions/ADR-448.md) reports **2**, and the mechanism of that error is named: a per-key `expect` label is a sample.
- **The blast radius derived before the first edit**, both files, and the one LIVE citer outside this fence identified. That is what forced zero movement rather than line-count neutrality.
- **Both helpers corrected**, one clause in one and three lines replaced by three in the other, with the reason the shapes differ measured against Prettier's `printWidth` rather than chosen.
- **The correction shown not to remove coverage**: every one of the 116 tables still yields a column for both helpers, so neither `throw` becomes reachable.
- **The control watched firing in BOTH directions**, seeded and reverted byte-identically, and the widening [ADR-448](../decisions/ADR-448.md) blocked measured green on the corrected tree.
- **A finding that entry could not have had**: the checker beside the guard cannot distinguish the widened tree from the unwidened one.
- **`RI-15` caught this row's own comment and the pointer was fixed rather than the invariant argued with.**

### 3. Next

- **The identity widening is takeable and wants ONE commit, not two**: the clause at [`scoped-db.ts:4690`](../../packages/db/src/scoped-db.ts) and the `identityReached` leg change together, because the checker is currently blind to the difference ([ADR-451](../decisions/ADR-451.md) section 11 item 1).
- **[ADR-448](../decisions/ADR-448.md) section 6's two-table sentence is named and not repaired**, [ADR-386](../decisions/ADR-386.md):169 ([ADR-451](../decisions/ADR-451.md) section 11 item 2).
- **The INSERT builders are still unguarded** and `live_account_state.intraday_movement_cents` is still reachable by no builder ([ADR-448](../decisions/ADR-448.md) section 8 items 3 and 4, unmoved).

### 4. Blockers

- **`CI-06f` is red at `450`, and no row on this branch may fix it.** The number was handed out without being reserved, which [ALLOCATION](../decisions/ALLOCATION.md) already rules is the dispatcher's step and not the receiving row's. Sixth occurrence.
- **No founder read is available and none was waited for.** Eleven ADRs this row inherits are `proposed` and unsigned and it settles none of them.

### 5. Landmines

- **A HELPER THAT PICKS "THE FIRST COLUMN LEFT" IS A SILENT ORACLE.** Both of these did, both were wrong on 20 tables, and the suite was green throughout because no database is involved. **Any test helper that derives its input by elimination is asserting something about the database it has not checked.**
- **A FAILING ASSERTION'S LABEL IS A SAMPLE AND READS LIKE A CENSUS.** [ADR-448](../decisions/ADR-448.md) reported 2 where the answer is 20, from an honest reading of vitest output. **Derive the set; do not copy the first failure.**
- **A CHECKER CAN PASS IDENTICALLY ON TWO TREES THAT DIFFER IN THE THING IT NAMES.** [`generated-column-writes.mjs`](../../packages/tooling/checks/generated-column-writes.mjs) does, today, and says so in its own output. **Read what a control says it does NOT assert on.**
- **THE PRINTER IS PART OF THE LINE-NEUTRALITY CONSTRAINT.** An element added to a broken array literal is a LINE under Prettier, and a conjunct that overruns `printWidth` is a LINE. **Measure the character count before deciding a clause is free.**

### 6. Files touched

- [`packages/db/test/keyed-accessor.test.ts`](../../packages/db/test/keyed-accessor.test.ts) - `setFor`'s three loop lines replaced by three, its docstring replaced in place, `databaseWrites` and its `RI-14` record appended past the last cited line.
- [`packages/db/test/write-accessor.test.ts`](../../packages/db/test/write-accessor.test.ts) - `someOtherColumn`'s guard, docstring and `throw` message each replaced one line for one, the same helper appended past the last cited line.
- [`docs/decisions/ADR-451.md`](../decisions/ADR-451.md) - new.
- [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) - row `451` only.
- [`docs/decisions/README.md`](../decisions/README.md), [`docs/sessions/README.md`](README.md), [`docs/INDEX.md`](../INDEX.md), [`docs/STATE.md`](../STATE.md) - generated spans only, `generate` output, no prose changed.
- This file.
