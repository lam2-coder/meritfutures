---
status: approved
depends_on: [README.md]
last_updated: 2026-09-05
---

## 2026-09-05 - Session 526: six of the twenty records nobody wrote, and the two merged figures the measurement moved

<!--index: **THE ROW IS [ADR-335](../decisions/ADR-335.md), `proposed` and UNSIGNED.** [ADR-334](../decisions/ADR-334.md) shipped `RI-37` with a SHRINK-ONLY register of twenty merged migrations that carry no landing record, and refused to write those sections in the diff that added the check. **THIS ROW TAKES SIX AND THEY ARE A CLUSTER RATHER THAN A SLICE**: `0052` to `0057`, the chart of accounts and the ledger code set, one `CHECK` superseded four times under its own name plus two triggers and the eighth ledger code. All six are `MONEY PATH` by their own headers and all six appeared in [`DELTA_MANIFEST`](../../packages/db/DELTA_MANIFEST.md) **zero times in any form**. They now hold **sections 40 to 45** and their six register rows come out **in the same commit**, which is leg 3's own stated mechanism; **the register goes from twenty to fourteen**. **NOT MONEY BY FILE AND IT IS THE RECORD OF SIX MONEY-SCHEMA MIGRATIONS**, so **`E2` READ OWED** with six inherited ([ADR-177](../decisions/ADR-177.md), [ADR-180](../decisions/ADR-180.md), [ADR-183](../decisions/ADR-183.md), [ADR-186](../decisions/ADR-186.md), [ADR-187](../decisions/ADR-187.md), [ADR-189](../decisions/ADR-189.md), all `proposed` and UNSIGNED). **NO SQL AND NO MIGRATION EDITED**: `git diff --stat 27115f60..HEAD -- packages/db/migrations scripts/db` is EMPTY. **EVERY FIGURE MEASURED, NONE RECONSTRUCTED**: PostgreSQL 16.13, SEVEN forward-only installs from empty under `ON_ERROR_STOP=1` stopping at `0051` through `0057`, one full install and FIVE full-set-minus-one installs, counts from `pg_tables`, `pg_indexes`, `pg_constraint`, `pg_trigger` and `pg_proc`. **THE TABLE, INDEX, FOREIGN-KEY AND UNIQUE COUNTS DO NOT MOVE ONCE ACROSS ALL SEVEN STOPS**: 114, 397, 167 and 12. What moves is CHECKs 481 to 482 at `0052`, triggers 21 to 22 at `0054` and 22 to 23 at `0057`, functions 107 to 109 on the same two, and `ledger_accounts` rows 0 to 3. **Four of the six supersede a constraint under its own name, so the delta is a PREDICATE no count can see.** **`0055` RE-APPLIES CLEAN AND IS THE ONLY ONE THAT DOES**, exit 0 with a byte-identical definition; the live partial-replay hazard is recorded and not repaired. **`0053`'s AND `0056`'s REFUSALS COME FROM THEIR SEEDED ROWS RATHER THAN THEIR DDL.** **THE PROBE SET WAS DERIVED BY EXECUTION**, three ways, and found two readers beyond the dispatch's two: **`0053` HAS NO PROBE AT ALL**, both readers exit 0 with the file removed and with its row deleted, which is [ADR-180](../decisions/ADR-180.md)'s own verification row 8 still true; **`0052`'s four arms are covered by nothing** because no database this repository can build holds the predicate it installed; **`0052` cannot be removed from the set at all**, `0053:203` dropping its constraint with no `IF EXISTS`; and **`0057`'s own probe cannot run at `0057`**, dying at `column "approved_at" ... does not exist`, a column `0070:153` adds thirteen migrations later. **TWO FIGURES IN MERGED RECORDS DISAGREE AND BOTH CAUSES WERE EXECUTED**: [ADR-183](../decisions/ADR-183.md) and `0054`'s header say two trigger functions match the word `UPDATE` and both are in a string, and three match `UPDATE`, two `INSERT`, one `DELETE`, with exactly one surviving a comment-and-string strip as a `SELECT ... FOR UPDATE` row lock, so the ruling's claim holds and the count does not; and [ADR-334](../decisions/ADR-334.md) records sixteen of its twenty as money path where the header form measures **fifteen**, `0073_operator_directory.sql:4` opening `NOT THE MONEY PATH BY FILE` while a loose search returns eighteen, **and ADR-334's own register row for `0073` carries no marker, so that diff's two halves disagreed and the machine-readable half was right**. **NEITHER RECORD IS AMENDED.** **SECTIONS 40 TO 45 BECAUSE THE SEQUENCE IS LANDING ORDER**, with migration order INSIDE the six ruled rather than defaulted; a lettered `31a` refused. **NO CHECK, NO GATE, NO LETTER, NO SLUG, NO MIGRATION NUMBER, NOTHING WEAKENED.** One fence-edge edit declared: the two size sentences above the register rows said `TWENTY ... SIXTEEN of them money path` and are corrected to fourteen and nine. Counts at reporting time: suite **312 files / 7,618 passed / 10 skipped / 0 failed** against a `27115f60` baseline reproduced BEFORE the first edit of **312 / 7,604 / 10 / 0**, a delta of **+14 cases in one existing file with SKIPS UNCHANGED**; invariants **35 of 35** and gates **33 of 33**, both unchanged; `RI-37`'s own sizes **75 on disk, 61 recorded across 29 section-1 rows and 34 landing headings, 14 on the register**; lint, `format:check` and typecheck clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `335`: write six of the twenty landing records [ADR-334](../decisions/ADR-334.md) registered and could not write, `0052` to `0057`, **measured rather than reconstructed**, and shrink the backlog register by exactly six in the same commit.

**One objective.** The work is the measurement. The prose is what the measurement produced.

---

### 2. The base and the baseline, both derived before the first edit

`27115f60`, PR #688's head, which carries `main` plus [ADR-334](../decisions/ADR-334.md) and `RI-37`, unmerged but present. **The base being ahead of `main` is deliberate and was re-derived rather than taken from the dispatch**: `git ls-tree origin/main --name-only docs/decisions/` ends at `ADR-332.md`, so `333` and `334` sit beneath this branch and `335` opens no `CI-06f` hole.

**There are 35 invariants and 33 gates on this ref**, both read off their own runners' last lines, with `RI-37` as the last PASS.

**Baseline:** `pnpm vitest run` **312 files, 7,604 passed, 10 skipped, 0 failed**, exit 0; invariants **35 of 35**; gates **33 of 33**; `git status --porcelain` empty. **Skips reported separately from passes throughout.**

**THE DISPATCH'S CHECKABLE CLAIMS WERE EACH RE-DERIVED AND ALL HELD.** The six files exist, each carries exactly one `E2 READ: MONEY PATH` header line, each appears in the manifest **zero** times, section 1's table is closed at `0029` so a `## <n>.` section is the only shape available, and 39 is the maximum section number.

**THE PRE-EDIT GREP FOUND FIVE THINGS PINNING THIS SUBJECT AND THE FIFTH IS THE ONE THAT PAID.** `grep -rn "DELTA_MANIFEST"` over `apps`, `packages`, `scripts` and `.github` returned **53 lines in 18 files**, four of which pin this subject: `gates.mjs:166`, `:982` (the `manifest_changes` span, which counts `SD-nn` and `U-nn` first cells and which eight new delta rows would have moved had they been keyed that way), `:8412` (`CI-06/retired-constraints` at `sites: 7`) and `RI-31`. **None moved**, and the retired-constraint claim was checked by re-implementing the gate's own rule: 21 `DROP CONSTRAINT` statements over the directory, 7 retired names, **none of them in this cluster**, because every constraint the six drop is re-added under its own name in the same file. **The fifth grep was for line-number citations of the manifest**, and it found that `DELTA_MANIFEST.md:2043`, cited by [ADR-260](../decisions/ADR-260.md) and [session 451](2026-08-30-session-451.md), had **already drifted to 2019 before this row existed**; this diff moves it six further and reports it.

---

### 3. What landed

1. **[`DELTA_MANIFEST`](../../packages/db/DELTA_MANIFEST.md) sections 40 to 45**, one per migration, each with the forward-only install, the catalogue counts and their deltas, the re-application result, the constraint or index read out of `pg_constraint` and `pg_index`, the probe coverage executed in both directions, the delta table keyed by ADR, and the part a reader should carry forward.
2. **Six rows in section 16's section-number table**, written in the same commit as the sections they claim.
3. **Six rows deleted from `LANDING_RECORD_BACKLOG`** in [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs), in that same commit, plus the two sentences above them that state the register's size.
4. **Fourteen cases added to [`migration-landing-record.test.ts`](../../packages/db/test/migration-landing-record.test.ts)**: six reconstructions, six counterfactuals, the all-six case, and a cluster case on the heading shape; the size case moved from twenty to fourteen.
5. **[ADR-335](../decisions/ADR-335.md), row `335`, this log and the STATE entry.**

---

### 4. The three things worth carrying forward

**FIRST, THE PROBE-COVERAGE TABLE IS THE THING NO ADR CONTAINED.** It was derived by applying the migration set five times with one file missing, and it says that `0053` is watched by nothing, that `0052`'s four arms are unreachable because three later files rewrite the constraint, and that `0057`'s own probe can no longer be run at `0057`. **Not one of those is in the six ADRs and not one would survive a reading of the migration text.**

**SECOND, `0055` RE-APPLIES CLEAN AND IT IS A LIVE HAZARD RATHER THAN A CURIOSITY.** A `DROP CONSTRAINT` plus an `ADD CONSTRAINT` and nothing else is idempotent by rerun; rerun against today's schema it installs seven arms over `0056`'s eight and makes a seeded row unwritable, and nothing refuses it. Recorded in section 43 and named as somebody else's number.

**THIRD, A MEASUREMENT DISAGREEING WITH A MERGED RECORD IS A FINDING AND NOT A NUMBER TO OVERWRITE.** Two disagreements were found, one in [ADR-183](../decisions/ADR-183.md) and one in [ADR-334](../decisions/ADR-334.md) itself, and both causes were executed rather than argued. **The second is the more interesting**: ADR-334's prose and ADR-334's own register row disagreed with each other inside one diff, and the machine-readable half was the one that was right.

---

### 5. What is next

- **Fourteen backlog sections remain**, nine of them money path, dispatched knowing the size. `0059` is the seventh of the seven that appear in the manifest zero times and is the natural next cluster with `0063`.
- **`0053` needs a probe block**, which is a `scripts/db/**` row this one could not take.
- **A leg that reads a landing section's CONTENT** rather than only its existence, which [ADR-334](../decisions/ADR-334.md)'s approval block names as the cheap version.
- **Sections `24`, `26` to `30` and `32` to `34` still have no row in section 16's table**, and nothing compares that table to the headings.
