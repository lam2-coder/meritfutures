---
status: approved
depends_on: [README.md]
last_updated: 2026-09-08
---

## 2026-09-08 - Session 664: the prose line-number pointer is a population of ninety-eight, sixteen of the forty-nine readable ones were dead, and every repair deletes the number

<!--index: **THE ROW IS [ADR-469](../decisions/ADR-469.md), `proposed` and UNSIGNED. NON-MONEY DIFF, NINE FILES AND COMMENTS ONLY, `E2` READ OWED.** [ADR-467](../decisions/ADR-467.md) section 12 item 1's live-source instance, repaired, plus the sweep that entry did not run. **THE DISPATCH'S MOTIVATING FACT DOES NOT TRANSFER AND THIS ROW MEASURED THAT FIRST.** `at its own line 270` entered [`refusal-naming.js`](../../packages/eslint-plugin-merit/rules/refusal-naming.js) in `4d1d2ba`, and reading line 270 of `write-guard-set.mjs` at that same commit returns the `/^refuse[A-Z]/` filter: **the pointer was TRUE the day it was written** and aged into falsity in `fe20954` and `6678885`, two other rows' commits. So this is an AGED claim in LIVE SOURCE, **not a sixth instance of the founder's question**, and the repair stands on `ADR-386`:169 plus `ADR-467` section 5 rather than on "false when written". **THE POPULATION IS DERIVED AGAINST WHAT THE THREE DERIVATIONS ACTUALLY SEE**, read rather than assumed: the word form `line NNN` is invisible always because every reader parses a colon token; a bare `` `:NNN` `` is invisible unless a `path.ext:NNN` sits within `RI-15`'s `CITATION_INHERIT_LINES` = 6 above it; and all of `packages/tooling/` is invisible by `RI-15`'s `OWN_PACKAGE` exclusion. **LIVE SOURCE HELD 109 CANDIDATES, 11 REACHABLE BY INHERITANCE AND 98 INVISIBLE over 29 files**, 33 `line NNN`, 58 `` `:NNN` ``, 7 `at :NNN`; **49 inside `packages/tooling/` and 49 outside it**. **THE 49 OUTSIDE WERE RESOLVED ONE BY ONE AGAINST THE FILE EACH SENTENCE NAMES AND 16 WERE DEAD**, a defect rate near a third in a class no check has ever read. **ALL 16 REPAIRED BY DELETING THE NUMBER**: `API_CONTRACT line 23` in [`client.ts`](../../apps/portal/src/http/client.ts) x4 and its test, where line 23 is Identity scoping and all four claims quote the **Idempotency** paragraph at **:25**; [`operator-role-vocabulary.test.ts`](../../packages/db/test/operator-role-vocabulary.test.ts), which explained why it finds the `Roles:` line by prefix and not by number **using a number**, and the number was 27 lines out; `replay.ts` `:167`/`:173` in [`index.ts`](../../packages/rules-engine/src/index.ts) and `evaluate.golden.test.ts`, where the walks are at `:316` and `:322`; `gates.ts` line 185 for a literal on `:186`, twice; **`floor-monotonicity.property.test.ts` line 299, WHICH IS BLANK**, for a `fold` declared at `:184`; and `account-reads.ts:912` for an import at `:131`. **NINETEEN LINES OVER NINE FILES AND NOT ONE LINE INDEX MOVES**, every edit a same-length in-place block, base and head line counts identical per file; **`stripComments` reports the executable source BYTE-IDENTICAL on 9 of 9**. **THE SIXTEENTH REPAIR WAS OUTSIDE THIS ROW'S OWN MEASURED POPULATION**: a bare `:173` opening a wrapped comment line, which the sweep's own forms cannot see. **The derivation written to find prose pointers missed one of the pointers it was written to find**, which is `ADR-467` section 7's finding arriving inside a different instrument. **THE SERIES RE-DERIVED: 0, 2, 7, 8, 15, AND NOW 16.** At one instant, `git grep -oP 'write-guard-set\.mjs[#:]L?\K[0-9]+'` reads **7 on `origin/main`** and **16 on `7f7ba904`**, this row's base. **ZERO of the 16 is live source**, row 467's repair having removed the last one, and **15 of the 16 exist because somebody wrote down that the pointer was wrong**. **DATED RECORDS NAMED AND NOT REPAIRED**: **4,091 sites over 568 dated records** and 932 over 26 living documents, roughly forty times the live-source population, which is the scale `ADR-467` section 12 item 3's stacked question actually reaches. **NO PRICE IS PROPOSED AND `PRICE_REGISTER.md` IS NOT TOUCHED**, so `price-register.mjs` is **GREEN**. The ground is measured rather than asserted: a checker must bind a number to one of several files a sentence names, and **the self-pointer is not the decidable subclass, because in this row's own subject `"its own"` meant the file named EARLIER IN THE SENTENCE and not the containing file.** At head live source holds **one** `its own` + number site, a MENTION in `price-register.mjs`, and **one** `this file's own` site, which does bind to its container. **OWED TO ROW 468: thirteen sites in `packages/tooling/test/repo-invariants.test.ts`** at `:529` x2, `:1883`, `:1974` x2, `:2059`, `:2126`, `:2271`, `:2277`, `:2286`, `:2308`, `:2683`, `:2785`, plus `absence-claims.test.ts:151`, unadjudicated because that file's pointers are suite seeds. **NO MONEY-PATH FILE EDITED**: [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) carries two population sites, both TRUE, read and left. **NO GATE ADDED OR WEAKENED, NO TEST SKIPPED, DISABLED, QUARANTINED OR DELETED, NO CONTROL BUILT, NOTHING FROM THE SWEEP COMMITTED, NO `RI-nn` MINTED, NO `CI-06` LETTER SPENT, NO MIGRATION, NO DATED RECORD EDITED.** `pnpm run verify` and [`falsify.mjs`](../../scripts/corpus/falsify.mjs) **NOT run**. Counts at reporting time: suite **347 files, 8713 PASSED, 11 skipped, 8724 TOTAL, 0 failed**, run once after the last edit with the zero delta asserted from the `stripComments` identity rather than a second run; invariants **35 of 35**; gates **33 of 33**; `write-guard-set.mjs` **PASS** with the convention literal still found in `refusal-naming.js`; `price-register.mjs` **PASS**, 72 prices over 13 entries; typecheck, lint and `format:check` clean. -->

### 1. The objective

Take [ADR-467](../decisions/ADR-467.md) section 12 item 1, the one live-source prose pointer that entry named and could not fence, and **sweep rather than spot-fix**: derive the population of line numbers written into prose with no path beside them, which no citation derivation in this corpus can enumerate, repair what is dead in live source by DELETING the number, and name what sits in dated records without touching it.

### 2. Done

- **Measured the base before the first edit.** Container arrived with no `node_modules`. After `pnpm install --frozen-lockfile`: gates **33 of 33**, invariants **35 of 35**, `write-guard-set.mjs` and `price-register.mjs` **PASS**.
- **Did the archaeology the dispatch asserted rather than repeating it.** `at its own line 270` was **TRUE in the commit that wrote it**, `4d1d2ba`, read at the blob. It aged into falsity in two other rows' commits. That narrows `ADR-467` section 3's finding to the file it was made about and it is stated as a correction.
- **Read what each derivation actually sees** (`RI-15`'s `CITATION_INHERIT_LINES` and `OWN_PACKAGE`, `RI-16`'s exclusions, the `git grep` adjacency form) and derived the invisible population against that: **98 sites over 29 files in live source at base**, 49 of them inside `packages/tooling/`.
- **Resolved the 49 outside it one by one. 16 were dead.** All 16 repaired by deleting the number and naming what the sentence is about.
- **Held the line-move constraint absolutely.** Nineteen lines over nine files, every edit a same-length in-place block, **no line index moves anywhere**, and `stripComments` reports the executable source byte-identical on 9 of 9.
- **Re-derived the citation series at this base**: **7 on `origin/main`, 16 on `7f7ba904`**, and none of the 16 is live source.
- **Named 4,091 dated-record sites and repaired none**, per `ADR-386`:169.
- **Priced nothing**, on a measurement rather than a preference: `"its own"` is anaphoric, and this row's own subject is the counterexample.

### 3. Next

- The founder's question in `ADR-467` section 12 item 3 now has a measured scale behind it: **4,091 sites** in records nobody may edit.
- Row 468's file holds thirteen unadjudicated sites, named in [ADR-469](../decisions/ADR-469.md) section 10 item 1.
- `ADR-469` is `proposed` and UNSIGNED with **`E2 READ OWED`**.

### 4. Blockers

None. Every check re-run after the last edit is green.

### 5. Landmines

- **`"its own"` does not mean the containing file.** In the very comment this row repaired it meant the file named earlier in the sentence. Any future rule that binds a self-pointer to its container is wrong on the corpus's own worked example.
- **A sweep's instrument is a member of its own population.** This row's derivation missed a bare `:173` opening a wrapped comment line, in the file next to one it caught, and the miss was only found by reading. Report what the instrument measured **and** what you repaired outside it, separately.
- **`adapter.ts:81` is a prose pointer pinned by a test.** [`schedule.test.ts:1013`](../../apps/worker/test/schedule.test.ts) asserts the sentence verbatim, so editing that comment is a test change. It is TRUE today and was left alone.
- **`packages/tooling/`'s 49 sites are worked examples by `RI-15`'s own ruling.** Repairing one would delete the example.

### 6. Files touched

- [`packages/eslint-plugin-merit/rules/refusal-naming.js`](../../packages/eslint-plugin-merit/rules/refusal-naming.js) - the subject, 5 lines for 5
- [`apps/portal/src/http/client.ts`](../../apps/portal/src/http/client.ts), [`apps/portal/test/http-client.test.ts`](../../apps/portal/test/http-client.test.ts) - the `API_CONTRACT line 23` family, 5 sites
- [`packages/db/test/operator-role-vocabulary.test.ts`](../../packages/db/test/operator-role-vocabulary.test.ts)
- [`packages/rules-engine/src/index.ts`](../../packages/rules-engine/src/index.ts), [`packages/rules-engine/test/evaluate.golden.test.ts`](../../packages/rules-engine/test/evaluate.golden.test.ts), [`packages/rules-engine/test/engine-eligible-conjunction.property.test.ts`](../../packages/rules-engine/test/engine-eligible-conjunction.property.test.ts), [`packages/rules-engine/test/property-harness.ts`](../../packages/rules-engine/test/property-harness.ts)
- [`apps/api/test/db.test.ts`](../../apps/api/test/db.test.ts)
- [`docs/decisions/ADR-469.md`](../decisions/ADR-469.md) (new), this log (new), and the registries
