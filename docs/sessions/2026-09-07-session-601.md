---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 601: the census was re-run three rows ago and reproduces cell for cell, hole one is 43 and not 55, hole two is closed for four pointers and no dated one, and a pointer into a dependency is undecidable rather than born wrong

<!--index: **THE ROW IS [ADR-406](../decisions/ADR-406.md), `proposed` and UNSIGNED. NON-MONEY, TOOLING AND CONTROL OVER THE CORPUS'S OWN POINTERS, `E2` READ OWED.** **THE DISPATCH'S FIRST PREMISE IS REFUTED: [ADR-388](../decisions/ADR-388.md)'s DATED CENSUS WAS RE-RUN BY [ADR-401](../decisions/ADR-401.md) IN SESSION 596, AND [ADR-403](../decisions/ADR-403.md) SECTION 13 ITEM 3 DECLARED IT STILL OWED TWO SESSIONS LATER WITHOUT NAMING THAT ENTRY.** It reproduces here on a base two merges later, **74 repairable against 39 born wrong** over 290 findings-eligible dated pointers, with **all ten of that entry's dated cells and its corpus-wide `-S` row** reproduced at its own base. **HOLE ONE IS 43 POINTERS OVER 19 COORDINATES, NOT 55 OVER 25**: the key on record is *(citing file, token)*, and a comma list is one site writing several coordinates rather than one coordinate written twice; 6 such pairs carrying 12 pointers are the whole difference. It retires with **ZERO** verdict splits. **HOLE TWO IS CLOSED.** `G2` is derived at the SURVIVING introduction, the newest `-S` commit whose first parent held the token zero times; it moves **4 of 497** pointers, all from *false then, true now* to *nothing owed*, cuts the `git blame -w` disagreement from **17 to 13**, and moves **NO DATED POINTER**, so the ruling's own headline is untouched by its own correction. **A THIRD HOLE IS OPENED AND RULED**: [ADR-274](../decisions/ADR-274.md) cites a line of the `pg` package's own source, a path never tracked on any ref here, and a census that reads *no candidate file* as *false when written* manufactures a class-two verdict out of its own scope. **That one pointer is the entire disagreement between this row's first harness and [ADR-401](../decisions/ADR-401.md), in every column at both bases.** **`CI-06e`'s clause is NOT written**: the price re-derives at **81 pointers, 17 live and 64 dated**, at that gate's own definition, and this diff does not move that region. **ONE INSTRUMENT KEPT AND NO GATE**, [`g2-census.mjs`](../../scripts/corpus/g2-census.mjs), because four rows have built this scaffolding and thrown it away, and because [ADR-401](../decisions/ADR-401.md) ruled a `-S` figure must state its traversal and its search string, which a prose sentence cannot. -->

### 1. The objective

Price the two holes [ADR-396](../decisions/ADR-396.md) section 13 item 1 left in the corrected `G2` instrument, close what is worth closing, and re-run [ADR-388](../decisions/ADR-388.md)'s dated census under an instrument that handles them. One objective, non-money, control over the corpus's honesty.

### 2. Done

- **The container measured and reported.** No repository on arrival, sixth wave running. On this row's own clone `git rev-parse --is-shallow-repository` returned **`false`**, 4,329 commits, no `.git/shallow`, so nothing was deepened.
- **The base reproduced on `1f621d5c`** before the first edit, on an empty `git status --porcelain`: gates **33 of 33** derived off the runner's own line, invariants **35 of 35**, suite **336 / 8,109 / 10 / 0**, typecheck, lint and format clean.
- **[ADR-401](../decisions/ADR-401.md)'s population table reproduced at its own base**, all four rows, and its dated census reproduced cell for cell in both instrument columns.
- **Hole one re-priced at 43 over 19 and retired**, on a key that does not count comma lists as repeated coordinates.
- **Hole two priced at 23 and CLOSED**, with the four moved pointers enumerated and the closure demonstrated on a built case in a scratch repository.
- **A third hole found, ruled and reproduced**: a cited path never tracked here is undecidable, not born wrong.
- **`CI-06e` left alone with its price re-derived** through [`citation-fan-in.mjs`](../../scripts/corpus/citation-fan-in.mjs), because this diff does not move [`gates.mjs`](../../scripts/corpus/gates.mjs).
- **[`g2-census.mjs`](../../scripts/corpus/g2-census.mjs) written** as an instrument, in no `GATES` array, minting nothing.
- **[ADR-396](../decisions/ADR-396.md) section 13 item 1 annotated**, line-count neutral, one insertion against one deletion.
- **[ADR-406](../decisions/ADR-406.md) written**, `proposed`, approval line unsigned.

### 3. Landmines

- **AN OWED-ITEMS LIST ROTS FROM THE BOTTOM AND NOTHING CHECKS IT.** Four consecutive entries carry *the census has never been re-run*; the third of them ran it. The dispatch inherited the fourth. **An owed item is prose about the future, so no runner can redden on it**, and the only thing that caught it here was the instruction to read [ADR-401](../decisions/ADR-401.md) before starting.
- **A CENSUS CAN READ ITS OWN FOOTPRINT.** The first draft of [`g2-census.mjs`](../../scripts/corpus/g2-census.mjs) quoted two coordinates in its header comment as worked examples. Both are citations by the checks' own grammar: they entered this row's own population, moved the source scope from 130 to 131, and one was a coordinate [ADR-401](../decisions/ADR-401.md) declares wrong on purpose. **A source file that writes about citations becomes an input to the thing measuring them.**
- **THE TOKEN A `-S` SEARCH NEEDS IS NOT THE READER'S `cited` FIELD AND IS NOT RECOVERABLE BY REGULAR EXPRESSION.** A naive backtick sweep mispairs every line written as a name and a coordinate together inside a double-backtick span, and there are twelve of those; it also needs a target tiebreak, because two citations to different files at the same line number on one line happens twice here. **12 of 497 tokens came back wrong before the reader's own pairing was used**, and arithmetic caught it rather than care: a refinement cannot refuse more often than the thing it refines.
- **THE CLOSURE READS THE FIRST PARENT AND ITS ONE-SENTENCE FORM DOES NOT SAY SO.** That is [ADR-401](../decisions/ADR-401.md) section 5's own finding reappearing inside the remedy for a different hole. The instrument states it; the clause does not.

### 4. Blockers

None. **The `E2` read is owed and is not a blocker to merging**, and merging is not signing.

### 5. Files touched

[`docs/decisions/ADR-406.md`](../decisions/ADR-406.md) (new), [`scripts/corpus/g2-census.mjs`](../../scripts/corpus/g2-census.mjs) (new), [`docs/decisions/ADR-396.md`](../decisions/ADR-396.md), [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md), [`docs/decisions/README.md`](../decisions/README.md) and [`docs/sessions/README.md`](README.md) (both by `generate`), [`docs/STATE.md`](../STATE.md), and this log.

### 6. Next

- **THERE IS STILL NO RULE FOR WHAT AN UNDECIDABLE VERDICT MEANS.** The corrected instrument refuses **9** times corpus-wide where `git blame -w` refuses **1**, and **7 to 1** in dated records. **No gate can ship on it until a refusal means something**, and this row's closure deliberately does not change that ratio.
- **`CI-06e` still states no limitation**, at a re-derived price of 81 pointers and 64 dated ones. The condition [ADR-402](../decisions/ADR-402.md) set for writing it has not arrived.
- **[ADR-403](../decisions/ADR-403.md) section 13 item 3 is false and sits in a dated record.** Whether a discharged owed-item is annotated at its site is the same open question two entries already carry about a false non-numeric claim, and this is the first instance where the false claim is about work already finished.
- **The container arrives with no repository, six waves running.** It is the dispatcher's and no ADR can fix it.
