---
status: approved
depends_on: [README.md]
last_updated: 2026-09-08
---

## 2026-09-08 - Session 657: the priced leg is red on three legitimate functions, one of them is the feared shape itself, so the predicate lands whole and its verdict lands declarable

<!--index: **THE ROW IS [ADR-462](../decisions/ADR-462.md), `proposed` and UNSIGNED. NON-MONEY DIFF, MONEY-ADJACENT SUBJECT, `E2 READ OWED`.** [ADR-459](../decisions/ADR-459.md) section 9 items 1 and 2 taken, both unblocked by #742's merge at `bbb9e22d`, an ancestor of this base. **ITEM 1's PREDICATE IS KEPT ENTIRE AND ITS VERDICT IS NOT.** It reaches **6** functions from the pinned builders over **11 edges**, and **3 do not match the convention and all 3 are legitimate**: `columnByName` at [`:102`](../../packages/db/src/scoped-db.ts) throws on registry drift, `bothOf` at [`:1122`](../../packages/db/src/scoped-db.ts) makes drizzle's `and` total, `scopePredicate` at [`:130`](../../packages/db/src/scoped-db.ts) throws on a `pair` key with no scoped reading. **THE THIRD IS NOT A NEAR MISS, IT IS THE PRICED SHAPE EXACTLY**: it returns a value, carries a non-verb name, and its own comment at [`:235`](../../packages/db/src/scoped-db.ts) calls its throw *"the runtime half of a type refusal"*, so a refusal ALREADY lives under a non-verb name and no name-shaped, return-shaped or position-shaped predicate can separate it from the arrival item 1 wants caught. **This reaches [ADR-459](../decisions/ADR-459.md) section 8's conclusion from the opposite direction** -- that entry found the guard shape outside the accessor, this one finds three non-guards wearing it INSIDE the builder callgraph. **What lands is item 1's three terms with leg D's instrument on them**: all six still evaluated, a non-conformer DECLARED with a `SETTLED` or `OPEN` class rather than convicted, and the strict alternative written into the header with its price. **ITEM 2 LANDS AS PRICED AND ITS CITATION IS WRONG BY SIX.** [ADR-459](../decisions/ADR-459.md):186 places the shared literal at `write-guard-set.mjs:270`; re-derived, `:270` is a comment about `uniqueKeys`, the literal is at **`:264`**, leg C's second `refuse[A-Z]` form at **`:278`**, prose at **`:61`**. Both dispatcher figures reproduce, the entry's does not. **[ADR-459](../decisions/ADR-459.md) NOT EDITED and [`refusal-naming.js:109`](../../packages/eslint-plugin-merit/rules/refusal-naming.js) carries the same six and is another row's**; the repair that outlives both is the named `CONVENTION` constant, read by leg A and leg F alike, so the line number stops mattering. **LEG F KEYS ON PRESENCE AND NOTHING ELSE** -- not the line, the leg count, the length or a hash -- because row 463 is editing that file concurrently, **and it STRIPS COMMENTS FIRST**, which is `RI-25`'s lesson. **THREE CONTROLS WATCHED ON THIS ROW'S OWN LINES**, both seed targets absent from the base file: leg E red on 2 findings with one word changed at head `:367`, leg F red at head `:179` against a copy whose convention was COMMENTED OUT while still holding the literal as raw text twice, and **leg F GREEN when the literal merely MOVES from `:114` to `:154` with a second leg appended**, which is the half that protects row 463. Every restore `sha256sum -c` byte-identical. **ONE OF THIS ROW'S OWN SUITE CASES WAS WRONG AND IS RECORDED RATHER THAN TIDIED**: it asserted a word the finding did not carry and seeded half of what it claimed; the message took leg D's vocabulary and the seed was widened, after the behaviour was READ from a live run. **A BASELINE FIGURE IS CORRECTED**: the container arrived WITH a repository and WITHOUT `node_modules`, breaking a run of seven, and the pre-install invariants run reported **34 passed / 1 ERRORED (`RI-18`) of 35**; after install the same tree reports **35 of 35**, re-measured on the STASHED base rather than assumed. **`scoped-db.ts` and `refusal-naming.js` both READ AND NOT EDITED, blob hashes identical at base and head.** Counts derived at reporting time: gates **33 of 33**, invariants **35 of 35**, suite **346 files / 8,670 passed / 11 skipped / 0 failed** against a base of **346 / 8,659 / 11 / 0**, a delta of **+11 cases in 0 new files**, which is this branch entire; typecheck, lint and `format:check` clean. **NO `RI-nn`, NO `CI-06` LETTER, NO MIGRATION, NO GATE WEAKENED, NO TEST SKIPPED OR DELETED.** `pnpm run verify` and `falsify.mjs` NOT run.-->

### 1. The objective

Take [ADR-459](../decisions/ADR-459.md) section 9 items 1 and 2, both written BLOCKED ON PR #742 and both dispatchable because #742 has merged. Item 1 prices a fifth leg on [`write-guard-set.mjs`](../../packages/tooling/checks/write-guard-set.mjs) keyed on **being called by a write builder** rather than on the callee's name or return shape. Item 2 prices one assertion tying the convention literal in the checker to the copy in [`refusal-naming.js`](../../packages/eslint-plugin-merit/rules/refusal-naming.js). The dispatch set the subject, required the `:270` citation to be re-derived rather than trusted, required the item-2 assertion to key on **presence** so a concurrent row moving that line could not turn this one red, and required the new legs to be **watched red on this row's own change**.

### 2. What the container arrived with

**A REPOSITORY, AND NO `node_modules`.** This breaks the run of seven consecutive rows that found no checkout at all. The `SessionStart` hook's `git pull --ff-only` ran and reported `Already up to date`, `origin` resolved, and `50dc4cb8` was checked out onto the branch first time with no refspec repair. `pnpm install --frozen-lockfile` was needed before anything could be measured, **and one baseline figure depended on it**: the pre-install invariants run reported 34 passed, 0 failed, **1 ERRORED** (`RI-18`), and the post-install run on the same tree reports 35 of 35. That was re-measured against the stashed base rather than attributed, so no part of it is charged to this row's diff.

### 3. What was derived, before the first edit

Base `50dc4cb8`, with dependencies present: gates **33 of 33** with a clean tree after `generate`, invariants **35 of 35**, suite **346 files / 8,659 passed / 11 skipped / 0 failed**, `write-guard-set.mjs` **PASS** at 9 guards, 6 builders, 6 matrix rows, 10 declared cells and **4 `OPEN`**, typecheck and lint and `format:check` clean. In `scoped-db.ts`: **81** top-level function declarations, **9** matching the convention, **6** constructing a write, and **0** functions declared as a `const` arrow. Item 1's predicate over those: **11 builder-to-thrower edges** across **6** distinct callees, **3** conforming and **3** not.

### 4. The verdict, which keeps the predicate and moves only its disposition

**Built**: leg E, item 1's three terms unaltered, with a `DECLARED_THROWER` table that lets a non-conformer be answered in writing with a `SETTLED` or `OPEN` class, which is the instrument leg D already applies to an empty matrix cell. **Built**: leg F, presence of `CONVENTION.source` in the rule file, over comment-stripped text, keyed on nothing else. **Refused**: the conviction wording, because it exits 1 on a clean tree over three functions nobody should rename, and because narrowing the predicate until those three fell outside it is [ADR-458](../decisions/ADR-458.md)'s error class. **Not decided**: whether a refusal reachable from a builder must be reachable by some instrument other than a reviewer, which is `scopePredicate`'s `OPEN` cell and is priced in [ADR-462](../decisions/ADR-462.md) section 9 rather than answered.

### 5. Verification

Gates **33 of 33**, invariants **35 of 35**, `write-guard-set.mjs` **PASS exit 0**, suite **346 files / 8,670 passed / 11 skipped / 0 failed**, typecheck and lint and `format:check` clean. Every leg-A-to-D figure in the check's PASS line is unchanged from the base, including [ADR-448](../decisions/ADR-448.md) section 8 item 3's four `OPEN` generated-column cells. Three controls watched on lines this row added, each restored `sha256sum -c` byte-identical. `pnpm run verify` and `scripts/corpus/falsify.mjs` **not run**, both forbidden by the row and both mutating the working tree.

### 6. Next

[ADR-462](../decisions/ADR-462.md) section 9 carries five items. The one worth reading first is item 1: two entries have now hit the same wall from opposite directions, and neither had the standing to rule on it. Item 3 re-derives [ADR-458](../decisions/ADR-458.md) section 11 item 4 and finds it still true and now more urgent, `write-guard-set` matching nothing under `.github/workflows/`, and leaves it with its owner.
