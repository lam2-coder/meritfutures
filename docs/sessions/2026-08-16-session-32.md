## 2026-08-16 - Session 32: FOLD-01 session 4, the invariants and the adversarial scenarios
<!--index: FOLD-01 session 4, the invariants and the adversarial scenarios -->

- **Done:** [FOLD-01](../plans/FOLD-01-phone-identity.md) section 6.1's four non-money documents, folded against [ADR-039](../decisions/ADR-039.md) and against [`0029`](../../packages/db/migrations/0029_phone_identity_and_auth.sql), which is merged and is the authority on what exists.
  - **[SECURITY](../architecture/SECURITY.md):** C-01 widened to three factors with §2.6's stuffing-immunity claim **preserved verbatim**; **C-27** the authentication and authority boundary; **C-28** pre-identity velocity plus the cost breaker; §2.6 gains **SIM swap** and **SMS OTP read without possession of the handset** as two separate rows; **§2.7 rescoped** to founder and admin credentials with its prior wording quoted rather than replaced; **new §4.8**, the phone-change ceremony as six enforced legs; §3 maps C-27 and C-28 to the endpoints they govern; §6 gains the breaker's three alarm events; **limitation 6** states the registration fail-open as a limitation.
  - **[M19](../plans/M19-kyc-identity.md):** `SD-M19-05/06/07`; **`INV-M19-13`** (the phone hard link binds the graph and never enforces alone) and **`INV-M19-14`** (the recycling guard); section 3.2's fifth re-verification trigger with its four inherited properties; **`AS-M19-09`**, the recycled number, including the adversarial half; **`DEP-M19-09`**, portability history as a disqualifying vendor criterion.
  - **[M07](../plans/M07-risk-abuse.md):** section 3.1 grows a **third resolution tier** and section 7.9 the matching confidence row; **`D-18`**, the registration lookup carrying the fleet signature as a named four-leg composite scored by D-16; **section 3.5**, the recycling wiring by column; **`OQ-M7-05`** raised.
  - **[M16](../plans/M16-notification-center.md):** **`INV-M16-11` recorded as CONFIRMED, not amended, in those words**, with its claim untouched; **`INV-M16-12`**, the pre-identity split; **`NC-M16-05`**, the fifth class; `SD-M16-04/05/06/07`; section 3.2's phone leg; **`AS-M16-07`**, the pumping attempt; the breaker's metrics and alerts.
  - All 12 gates green before each commit, `generate` reports every span already matching.
- **Next:** **FOLD-01 session 5** (M04, M03, M10, privacy policy, counsel packet; non-money, may compound). Then session 6, the registries and the gates: EDGE_CASES, GOLDEN_SCENARIOS, STRATEGY, `CI-06k`, API_CONTRACT, EVENTS, STATE_MACHINES, INDEX. **Two founder items are open from this session and are named under blockers.**
- **Blockers:** None for session 5. Two rulings are wanted and neither blocks:
  - **`SD-M7-nn` does not exist and this session did not create one.** See the first landmine. The founder may overrule and require a numbered delta, in which case it needs a `DELTA_MANIFEST` row and a migration to be about.
  - **`OQ-M7-05`**, whether "hard link" means auto-enforce or flag-and-no-state-change for **biometric dedupe**. ADR-039 settled it for the phone only.
- **Landmines:**
  - **The brief named `SD-M7-nn` and there is no such delta, deliberately.** [FOLD-01 section 6.1](../plans/FOLD-01-phone-identity.md) promised M07 "the M7 delta wiring portability history to the recycling decision". **Section 4 of the same plan enumerates nine changes and none is M7's, and `0029` contains none.** Every input and output already exists: `ported`, `last_ported_at`, `released_at`, `release_evidence` and both live-number indexes from `SD-M19-05`; `identity_restriction_episodes.opened_at` from `0031`; and `suppressed` / `suppressed_by` from **`SD-M7-04`**, which has been in `0002` since the reconciliation. `identity_links.link_kind` is `text NOT NULL` with **no CHECK**, so a phone edge kind is a vocabulary value and not a migration. **Writing `SD-M7-nn` into a plan would have failed the `ADR-026` gate on the spot** (every `SD-nn` cited in `docs/` needs exactly one manifest row), and inventing a `deferred` row for a change with nothing in it is worse than the pre-claiming the corpus already refused: it is a delta a later session must open, read, and discover is empty. **Recorded in M07 section 3.5 as a finding, not left as a silent omission.** **And the gate proved the point while this log was being written**: naming the identifier here, in prose explaining why it was not allocated, failed `ADR-026` on the spot. It is spelled `SD-M7-nn` throughout for that reason, which is also why the regex takes two digits.
  - **No `EC-nnn` or `GS-nnn` identifier was claimed anywhere in this diff, and that is a hard constraint rather than a preference.** `CI-06d` fails on **any** `GS-nnn` or `EC-nnn` cited in `docs/` that does not resolve to a registry definition, so the conventional "EC-nnn, GS-nnn" tail on `AS-M19-09` and `AS-M16-07` would have broken the build. Both scenarios name session 6 in prose instead. **Session 6 must go back and add the citations**, or two adversarial scenarios ship with no fixture reference.
  - **`INV-M16-11`'s claim text is byte-identical and must stay that way.** ADR-039 records it as **confirmed**, so only its Enforcement cell gained provenance. A later editor "harmonising" it with `INV-M16-12` converts a confirmation into an amendment, which is the one thing the ruling says it is not.
  - **`footprint_present` and `ported` are three-valued and `NULL` is not `false`.** D-18's composite tests `footprint_present IS FALSE`. **A detector written against `IS NOT TRUE` scores every vendor timeout as a fleet member**, turning a supplier outage into a flood of flags against real customers. The near-miss fixture named in M07 section 8 is exactly that case, and it is named because it is the one a reader would omit.
  - **Two stale hand-maintained counts were found in the documents this session touched, and only one was replaced with a number.** M19's section 2 preamble cited `dedupe_matched_identity_id`, a column [ADR-029](../decisions/ADR-029.md) rules is **never created**; corrected. M07's detector-test count read "22, two per detector" against a section 3.2 that has held more than eleven detectors since the batch 1 gate; **replaced by the rule that produces it** per [ADR-034](../decisions/ADR-034.md), because there is no CI span for a test count. **No ordinal is claimed for either**, on session 31's finding that the tally of hand-maintained counts is itself double-booked.
  - **`SECURITY` §8's items are cited by number from §9.1.** The registration fail-open was appended as **limitation 6** rather than inserted, because inserting at 5 would have silently repointed "recorded above as limitation 5" at the wrong entry.
  - **The degraded window needs a repeating page, not a page on entry.** A trip that pages and a recovery that pages still leave the interval between them silent, and the interval is where unverified registrations accumulate. Recorded in M16 §9.2 and in SECURITY §6.
- **Files touched:** `docs/architecture/SECURITY.md`, `docs/plans/{M19-kyc-identity,M07-risk-abuse,M16-notification-center}.md`, `docs/{INDEX,STATE}.md`, `docs/sessions/{README,2026-08-16-session-32}.md`. **No migration, no registry entry, no `DELTA_MANIFEST` row**, which was the stop condition.

### The one place the ruling and the plan disagreed, and the ruling won

[FOLD-01](../plans/FOLD-01-phone-identity.md) predates the founder's degradation ruling, which is recorded in [ADR-039](../decisions/ADR-039.md) and carried into this session's brief: **the cost breaker degrades, it does not stop.** The plan's section 6.1 row for SECURITY says only "C-28 pre-identity OTP velocity and the cost breaker", which a reader arriving cold would implement fail-closed, because every other breaker in this estate is fail-closed and `plan_breaker_state` has a `paused` state to copy. **Fail-closed protects money on provisioning and destroys it on registration**: phone verification is mandatory, so a stopped breaker means no new customers, tripped at the price of the traffic that trips it, which is the attacker's business model in amendment 2 anyway.

`0029` already encodes it (`otp_send_budget.state` has no stopping value), and this session put the reason in three places a reader arrives from: C-28's row, `INV-M16-12`, and `AS-M16-07` counter 4. **The second-order attack is priced beside it rather than left for whoever notices**: an attacker who trips the breaker on purpose buys registrations and not funded accounts, because `pre_funded` fires before Merit's capital is at risk, the window is alarmed while it runs, and `deferred_registrations` makes the cohort countable rather than anonymous.
## 2026-08-16 - Session 32: S-E session 3, the calendar source shape and the session generator (non-money)
<!--index: S-E session 3, the calendar source shape and the session generator -->

- **Done:** [P1 S-E](../plans/P1-SE-trading-calendar.md) session S-E3, **partially, and the missing half is the transcription itself.** [`packages/db/src/seed/calendars/`](../../packages/db/src/seed/calendars/README.md) now holds the source-file contract ([`cme-2026-2028.source.json`](../../packages/db/src/seed/calendars/cme-2026-2028.source.json)), the generator ([`generate.mjs`](../../packages/db/src/seed/calendars/generate.mjs)), the OQ-SE-04 diff, and a README carrying the retrieval and blind-transcription procedures. **Eighteen seeded violations, each asserted on its own finding rather than on the fact that something threw**, plus the positive control first and a `TZ=Asia/Kolkata` byte-identity check run out of process. `pnpm run verify` green: 10 typechecks, 191 tests, all 12 gates pass clean and fail dirty.
- **Next:** **Complete the transcription from an environment that can reach `cmegroup.com`**, following [the README's four steps](../../packages/db/src/seed/calendars/README.md). Then S-E4 (the loader and its six proofs) and S-E5 (`CI-06m`, the lint rule, the SQL shape check, the fixture derivation). **`0032` still needs the founder's E2 read** and nothing merges before it.
- **Blockers:** **The CME publication could not be retrieved.** See below. It is the whole of what is missing and it is not recoverable inside this environment.
- **Landmines:**
  - **`holidays: null` is not `holidays: []` and a future session will want to "finish" the file by writing `[]`.** The empty list asserts the exchange closes on no day of the year: it loads clean, it passes every structural check, and it makes every counter advance through Christmas. `null` says nobody has read the publication and the generator refuses it on `exception-list-not-transcribed`. This is [ADR-042](../decisions/ADR-042.md) F-1's lesson, that a holiday is a **positive fact rather than an absence**, applied one layer earlier to the file.
  - **The holding test in [`trading-calendar-generator.test.ts`](../../packages/db/test/trading-calendar-generator.test.ts) is written to fail when the transcription lands.** It asserts the committed source file refuses to generate. That failure is the reminder to replace it with the real assertions (declared counts, coverage bounds, the generated file being reproducible), and it is deliberate rather than a test somebody forgot.
  - **The DST days are stated in three places on purpose and two of them are not redundant.** `findDstTransitions` discovers them from IANA, `checkDstTransitions` derives the same days from the published second-Sunday-in-March / first-Sunday-in-November rule, and the test lists them literally. A `pnpm install` that moves tzdata under a pinned Node would otherwise move two hundred and fifty session boundaries by an hour with every test still passing, because every side of every comparison moves together. **Permanent-DST legislation is the live version of this and it is not hypothetical.**
  - **`generate.mjs` is `.mjs` inside a TypeScript package and `packages/db/tsconfig.json` gained `allowJs` for it.** It is JS because CI-06m and the loader both invoke it as `node generate.mjs` and nothing in this repository builds TypeScript. `checkJs` is off and there is deliberately no hand-written `.d.mts`: a declaration file is a second statement of one API, maintained by hand, which is the drift class this whole session exists to remove one table over.
- **Files touched:** `packages/db/src/seed/calendars/` (new: `README.md`, `cme-2026-2028.source.json`, `generate.mjs`), `packages/db/test/trading-calendar-generator.test.ts` (new), `packages/db/tsconfig.json`, `docs/{STATE.md,sessions/}`.

### The retrieval that did not happen, and why nothing was written in its place

**The environment's egress policy answers `403` to `CONNECT www.cmegroup.com:443`**, and to `iana.org` and `nyse.com` besides; only GitHub and the package registries are reachable. The proxy's own instruction for that class is to report the blocked host rather than route around it.

**So the holiday and early-close lists are `null` and nothing was written into them.** The reason that is the right outcome rather than a shortfall is worth stating where the next session will look:

> A holiday list written from recollection is exactly the failure `TR-01` names, "every value transcribed from the named authority and never from an implementation", **with the added defect that the implementation in question is a memory of the publication rather than the publication**. It would look identical to a correct transcription, it would pass every structural check in `generate.mjs`, and the first mechanism that could falsify it is `SD-M2-04`'s divergence alarm, **retrospectively, after trading**.
>
> And it defeats OQ-SE-04 in the same stroke. The ruling rejects a calendar package because "it encodes someone else's reading of the same publication, a second instance of the same error class rather than an independent check". **Two recollections of one publication are worse**: they are perfectly correlated by construction, so the diff comes back empty and proves nothing, which is the ruling's own stated failure mode in its own words.

Everything that does not depend on those values is finished. **What is owed is the transcription, the retrieved artifact and its SHA-256, the generated sessions file, and the second reader's file.** No part of the generator, the contract or the checks waits on them.

### One finding needing a founder ruling, and it is a transcription rule rather than a schema change

**CME publishes the word "holiday" for days that still carry a shortened Globex session.** The ruled model has no state for that:

| State | What `0032` enforces |
|---|---|
| **A holiday** | **No session at all.** `session_open_at` and `session_close_at` are `NULL` under `trading_calendar_holiday_has_no_session` ([ADR-042](../decisions/ADR-042.md) F-1) |
| **An early close** | A **full trading day for every counter** (B4 #3, [EC-005](../edge-cases/EC-005.md)) with `session_close_at` brought forward |

**A day published as a holiday on which a fill can still occur has no session to contain it, so R-01's containment lookup finds none.** That is the same orphaned-fill failure **F-3** exists to prevent, arriving through the other door: F-3 guards a close time that is too early, and this is a session interval that is absent entirely.

**Recommendation, offered without deciding it:** a day on which any of `ES`, `MES`, `NQ`, `MNQ`, `CL` or `GC` trades at all is an **early close**, however the publication labels it, and `is_holiday` is reserved for days on which none of them trades. The cost is that `is_holiday` will disagree with the word "holiday" on the exchange's page, which is a documentation problem rather than a containment one, and the disagreement belongs in `notes`.

**It has to be settled before the transcription rather than after**, because both readers must be given the same rule or the second transcription is answering a different question than the first, and OQ-SE-04's empty diff would then be empty for the wrong reason.

### A registry defect found while reading, not fixed here

**[`DELTA_MANIFEST`](../../packages/db/DELTA_MANIFEST.md) carries two rows numbered `OI-06`**: the payout-destination cooling window (FOLD-01, line 241) and the missing `trading_calendar` revisions trigger (S-E2, line 245). Two sessions on the same day claimed the same identifier from a namespace with no allocation table, **which is precisely the ADR-034 race the three allocation tables exist to end, on the one registry that never got one**. It is left as found rather than renumbered: which row becomes `OI-09` is a call about two open findings that are not this session's, and a silent renumber breaks every citation of whichever one moves.
## 2026-08-16 - Session 32: the AI and LLM policy (ADR-044), written by the review desk
<!--index: The AI and LLM policy (ADR-044), written by the review desk -->

- **Done:** [ADR-044](../decisions/ADR-044.md), Merit's first ruling on whether it may use a language model in its own product. Four permitted classes, three prohibitions, an amendment to constitution [E3](../../MERIT_BUILD_MASTER_PROMPT.md), and `CI-06o` claimed. **`044` and the gate letter `o` were claimed in [ALLOCATION](../decisions/ALLOCATION.md) before the ADR was written**, per [ADR-034](../decisions/ADR-034.md). No migration, no schema, no engine behavior and no running gate changed. All 12 gates pass clean and fail dirty.
- **Next:** The **M10 and M15 threat-model pass** is the only named precondition ADR-044 creates, and it blocks nothing until someone proposes the support assistant. **`CI-06o` is unwritten**, and until it runs, prohibition 1 is prose. Everything else in the queue is unchanged: FOLD-01 s4, then FOLD-02 s4, then `OI-06`.
- **Blockers:** None.
- **Landmines:**
  - **The brief cited E3 as authority for something E3 does not permit, and it read as a citation.** E3 scopes the support bot to **published docs** *until a threat-model pass says otherwise*; the brief added the trader's live gate state, which is internal per-identity data. That is the "otherwise", and E3 had already priced it. **A citation that widens its source is the hardest defect class to see, because the source is right there and it looks checked.** The ADR amends E3 explicitly rather than borrowing its authority.
  - **"Labeled advisory" does not answer [M13](../plans/M13-trader-analytics-journal.md), and a reader will think it does.** M13's governing sentence is that a second computation of anything the engine computes means two rulebooks, and its failure mode is **a trader who believes something different from what the engine will enforce**. A label addresses a misreading. M13's failure is a misbelief. The **narration boundary** (a model may narrate engine-computed numbers, never derive one) is the answer, and its value is that it is **checkable** where a label is not.
  - **The brief prohibited its own permission.** "No unreviewed AI-generated user-facing copy" and "per-trader runtime insights" cannot both hold, because runtime narration can never be pre-reviewed. Scoping the prohibition to **published, static** surfaces is what makes the pair consistent, and the ADR records that it was a contradiction rather than smoothing it.
  - **ADR-020's pattern does not transfer whole and the difference is the fallback.** [ADR-020](../decisions/ADR-020.md)'s indicative tier is nondeterministic in *freshness* only and always degrades to a correct stale value. A model layer has **no stale-but-correct fallback**, so "graceful degradation" here can only mean the deterministic surface is complete with the model layer **deleted**. Stated in those words because the phrase otherwise gets built as a spinner.
  - **Permitting is not specifying, and under FREEZE the difference is four uncosted features.** `grep` for `llm`, `genai`, `machine learning`, `generative` and `ml` across `docs/` returns **zero hits**, so every one of these behaviors is currently out of scope by omission. An ADR that listed four permitted features without saying it adds no scope would have committed five frozen module plans to build them.
  - **`OI-09`: `CI-06n` matches any markdown link, not a table row.** Its `run()` accepts a **prose mention** as a registry row. Its title says "has a README row" and its `covers` line honestly says "is linked from"; the check matches the weaker one. **Found because `ADR-043` had no row in [decisions/README](../decisions/README.md) and the gate was green**: one ADR of 43 missing from the registry index, and it was the ADR that wrote `CI-06n` to pay for its own `CI-06c` exemption. The row is added; **the gate is not fixed**, because tightening a parser without a `falsify.mjs` seed watched failing is how the 109 phantom anchors happened. **Third instance of the loose-parser class**, after `CI-06g`'s `[a-z_]+`.
  - **`OI-10`: keep-both merges duplicated four passages in STATE and no gate could see it.** The schema-delta paragraph twice identically, the `CI-06, corpus integrity` row three times, action item 3 three times with three different tails (`0032` next, `0032` done, `0032` not started), and a **stale** P1 row asserting `0029` to `0031` were "reserved and unwritten" after all three merged. **`CI-06g` cannot catch this**: duplicated spans carrying the *same* value match their query, so the gate passes over a document holding a false sentence. The dedup step in the merge recipe is a human obligation with nothing behind it. **The `Eleven checks` count was also wrong against the twelve `gates.mjs` reports**, corrected in the same pass.
  - **The gate that would catch `OI-10` was deliberately NOT claimed.** "No restated span-carrying sentence appears twice in one document" needs a letter, and claiming one for a gate nobody has scoped precisely is the prose-reserves-a-number failure [ALLOCATION](../decisions/ALLOCATION.md) already records against itself. It stays a recommendation until someone scopes it.
  - **The ADR State column was stale in five rows at once**, `039` to `043`, after PR #17 and PR #18 merged them. **Fifth through ninth instance.** [ADR-036](../decisions/ADR-036.md) already records that the column is prose no gate can check, so this needs no ruling and will recur.
- **Files touched:** `docs/decisions/ADR-044.md` (new), `docs/decisions/README.md` (ADR-044 row, **and ADR-043's missing row**), `docs/decisions/ALLOCATION.md` (`044` claimed, `o` claimed, five stale State rows corrected, `n` corrected to written-and-running), `docs/STATE.md` (the ADR-044 record, `OI-09`, `OI-10`, and the four keep-both duplications removed), `docs/INDEX.md` (`adr_count` span), `docs/sessions/`.

### What the desk did not do, stated rather than implied

**`CI-06o` is not written.** ADR-044 section 8 specifies it precisely enough to build and says in the ADR itself that until it runs and has been watched failing on a seeded violation, the money-path model ban is prose. It is cheapest now, while the payout, ledger and auth packages do not exist and the gate is close to vacuous, which is [ADR-042](../decisions/ADR-042.md)'s own argument for its SQL shape check.

**`CI-06n`'s parser is not tightened**, for the reason in the landmines: a parser change needs its own seed, watched failing, and that is a tooling session rather than a line in this diff.

**Neither is a deferral dressed as a decision.** Both are one scoped session each, and both are named here so the next reader does not assume the ADR arrived enforced.
