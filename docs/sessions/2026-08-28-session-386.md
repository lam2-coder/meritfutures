---
status: approved
depends_on: [README.md]
last_updated: 2026-08-28
---

## 2026-08-28 - Session 386: a liability figure that is not there is a null whose reason rides on the body, and the corpus had already built the shape twice

<!--index: **TWO BLOCKERS ON ONE WALL, RE-DERIVED FROM THE SUITE RATHER THAN FROM THE DISPATCH.** [`admin-source-liability.test.ts`](../../apps/api/test/admin-source-liability.test.ts)'s `B2` case names its own clearing condition -- *"the wire gains a shape for 'there is no window', which is a change to a response `RI-18` binds in three copies"* -- and `B3`'s names *"either half: the calibration landing, or the shape gaining an absent form"*. **Both are this branch, and `RI-18` proves the atomicity rather than the argument**: moving [`API_CONTRACT:896`](../architecture/API_CONTRACT.md) alone is **RED on twelve findings**, moving all three is **17 of 17**. **THE DISPATCH'S `B3` PREMISE IS ONE WEEK STALE AND THE CORRECTION CHANGED THE WORK.** It reads `B3` as having no absence form; [`ADR-202.md:119`](../decisions/ADR-202.md) ruling 3 had already ruled `per_plan[].cusum` to `{...} \| null` in all three declarations, and [`ADR-202`](../decisions/ADR-202.md) section 9 hands THIS entry its subject by name -- *"Whether `LiabilityResponse` should carry a `gaps` array ... is its own entry"*. **So `B3` is a TRANSCRIPTION and `B2` is the new ruling, and the two absences are not the same absence**: `B3`'s has ONE reason for every plan on every day and is a fact about the ROADMAP; `B2`'s has TWO, they differ per request, and they are facts about the ESTATE. **THE HALF THAT NEEDED NO RULING IS PROVED BY A LIVE GUARD.** `assertContractScalars` admits a `*_cents` or `*_bp` value only as a `Number.isSafeInteger`, so `{ payout_velocity: { last_7d_cents: null } }` **throws**, and the obvious minimal answer to `B2` -- null the two numbers that need a window, keep `alarm` -- is not a shape this contract can serve at all, whatever an entry rules. `ADR-202` reached the same conclusion for `cusum` by argument; this branch met it as a sweep. **THE BARE NULL IS BEATEN ON `B2`'s OWN EVIDENCE RATHER THAN ON PRINCIPLE.** It clears the bar `B2` failed, because it beats `0 / false`. It then fails a second bar the same case sets: [`payout-velocity.ts:385`](../../apps/api/src/admin-source/payout-velocity.ts)'s `PayoutVelocityVerdict` answers `evaluated`, `exhausted` and `uncovered`, the last two demand different acts by whoever reads the panel, and a bare null makes them one -- `ADR-042` F-4's UNKNOWN re-collapsing one layer up with a pager attached. **SO: `null` AT THE SITE, REASON ON THE BODY, PAIRED IN BOTH DIRECTIONS.** `gaps` is required and `[]` on a complete response; `assertLiabilityGapsPaired` refuses a `null` no entry names AND an entry naming a figure that is present, the second being the worse failure because it tells an operator mid-incident that a number they are reading is missing. **`cause` IS CLOSED AT THREE AND `detail` IS REQUIRED FREE TEXT**, on [ADR-179](../decisions/ADR-179.md)'s shape -- a union declared in one place, published as data, watched by a source-reading case -- and on [ADR-165](../decisions/ADR-165.md)'s grain rule that a member names a KIND and never a figure. The closure ground is `ADR-179`'s mirrored: that column was the RECORD of a promise, this field is an INPUT to a person's decision, and a free-text cause makes the branch a substring match. `detail` stays open because a vocabulary cannot hold *"four trading days short"* and because `figure.ts`'s bar -- *"'unavailable' written by the schema is the same silence, spelled"* -- is a sentence about text. **THE DIVERGENCE FROM THE CONSOLE IS ARGUED IN TERMS.** [`figure.ts:127`](../../apps/admin/src/figure.ts) tags at the SITE because a `Reading` has no body to hang a gap on; `origin`, `label` and `definition` DO NOT CROSS, because they are the page's `P-M6-nn` roster and putting them on the wire makes [ADR-188](../decisions/ADR-188.md)'s *"no field is named for a panel"* false; the console's `reason` is free text because it is RENDERED and the wire's `cause` is closed because it is BRANCHED ON. **Every `gaps` entry maps to exactly one `absent()` call**, which is the test a boundary shape has to pass. **NINE SEEDS, EIGHT RED, AND THE ONE THAT DID NOT FIRE IS THE BILL.** `readonly last_7d_cents` and `readonly statistic` renamed in [`api/types.ts`](../../apps/admin/src/api/types.ts) **alone** return **17 of 17 invariants, 1841 passing tests and `tsc` at ZERO errors**, because `RI-18` does not walk into a union arm: **50 paths per copy became 48**, seven leaving and five arriving, where `ADR-202` predicted three. **Two further greens are measurements**: an OMITTED `cusum` key in all three copies leaves `RI-18` green, because an optional key is still a field; `awaiting` renamed `blocked_on` in all three leaves it green, because the field sets still agree, so the guard on that name is the runtime sweep and never a declaration check. **THE FENCE WAS WIDENED INTO FIVE FILES AND NOT ONE IS A DECISION**: `PayoutVelocityPanel` becomes `NonNullable<...>` (the alias always meant the object; the alternative was `?.` through twenty assertions), `readSnapshot`'s `Omit` gains `gaps` (a gap is not a `liability_snapshots` column), two fixtures gain `gaps: []`, and two `WAVE-06` citations move because this branch inserted 116 lines above them. **BOTH CLEARING CONDITIONS SPENT IN COMMITS OF THEIR OWN, BOTH CASES INVERTED RATHER THAN DELETED**, session 381's earlier `B2` inversion left standing. **NEITHER BLOCKER IS CLEARED**: a shape that can carry an answer is not an answer, `IMPLEMENTED_ADMIN_READS` stays at **five**, and `readLiability` is still blocked on `B4` and `B5`. **THE LEAF CENSUS MOVED IN THREE NUMBERS AND NOT THE FOURTH**: 40 declared became **39**, 13 blocked became **8** plus 4 standing over an empty `gaps` array, and **27 PRODUCED IS UNCHANGED** -- which is what it means for a shape ruling to be a shape ruling. **FIVE FINDINGS REPORTED AND NOT REPAIRED**, each with its fence, the first being `RI-18`'s union blindness and the third being that `assertLiabilityGapsPaired` is declared, watched, and on NO SERVED PATH. **NO MIGRATION NUMBER, no `SD-nn`, no `GS-nnn`, no `SystemReason` or `SqlExecutorReason` member, no `pg` import, no cast past a key type, no ledger account named, `ADR-188` not reopened, and every merged migration byte for byte unchanged.** 33 of 33 gates, 17 of 17 invariants, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` read as OUTPUT with the tree clean after it, and **240 files / 5,933 passed / 6 skipped** against a base of **240 / 5,928 / 6 reproduced before a line changed**. -->

**Objective.** Rule the shape a liability figure takes on the wire when it is not there.

**Outcome.** [ADR-203](../decisions/ADR-203.md), and the shape is built rather than only ruled. `null` at the site, the reason on the body in a closed cause vocabulary of three, and the two paired in both directions by a control that is watched failing in each.

### Done

- **[`docs/decisions/ADR-203.md`](../decisions/ADR-203.md)** (new), `status: proposed`, approval line **UNSIGNED**, **MONEY PATH, `E2` READ OWED**.
- **[`API_CONTRACT`](../architecture/API_CONTRACT.md) section 8 AMENDED**, `LiabilityResponse` only. `payout_velocity` and `per_plan[].cusum` gain `| null`; `gaps` is added.
- **The two code declarations `RI-18` binds**, [`admin-reads.ts:610`](../../apps/api/src/routes/admin-reads.ts) and [`api/types.ts:147`](../../apps/admin/src/api/types.ts), moved in the same diff, plus `LiabilityGap`, `LiabilityGapCause`, `LIABILITY_GAP_CAUSES` and `assertLiabilityGapsPaired`.
- **`B2` and `B3`'s pinned cases INVERTED**, in a commit of its own naming which condition fired, and the leaf census re-derived.
- **`ALLOCATION` row `203`** amended in place, `INDEX`'s `API_CONTRACT` row given the amendment note, `decisions/README.md` and `STATE`'s generated spans regenerated by the runner.

### 1. The convergence, and the premise that did not survive

Both cases name a change to one response and one names `RI-18`'s three copies. `B3`'s FORM was already ruled by [`ADR-202.md:119`](../decisions/ADR-202.md); that entry's section 9 names a `gaps` array on `LiabilityResponse` as a separate entry's subject, which is this one. **The two rulings do not disagree anywhere**, and `ADR-202`'s three refusals are adopted rather than revisited: the omitted key, the per-member nullable, and the per-ROW reason. What it did not refuse is the per-BODY reason, because it had already pointed at a body that carries one.

### 2. What is proved rather than judged

`assertContractScalars` refuses a nullable `*_cents`, so the absence cannot sit on a member. That eliminates a family of options before any preference is expressed, and it is a guard rather than an argument.

### 3. The cost, measured in both directions

`copiesOf` at the moment of writing: **50 paths per copy at the branch point, 48 on this branch.** Seed 9 renamed a member of each nullable object in the console's copy alone and **nothing fired** -- 17 of 17, 1841 passed, `tsc` at zero. That hole opens the day this merges and the repair is in `packages/tooling`, which is nobody's this wave.

### Next

- The `RI-18` union-arm change, which the entry recommends commissioning **before** this merges.
- `B4` and `B5`, which are what `readLiability` is still blocked on.
- Unifying `ProjectionGap` and `LiabilityGap`, giving the former the `cause`.

### Blockers

- **`B4` and `B5` stand.** Neither is advanced here.
- **`DEP-M6-05` is M06 Wave 4** and is not advanced, narrowed or priced.

### Landmines

- **`RI-18` IS NOW BLIND TO SEVEN PATHS AND THE BLINDNESS IS TOTAL.** A member of `payout_velocity` or of `per_plan[].cusum` renamed in one copy passes every control in this repository, the typechecker included. This is the single most important sentence in this log.
- **`assertLiabilityGapsPaired` READS THE ABSENT PATHS OFF THE VALUE WITH TWO HAND-WRITTEN `if`s.** The day a third field goes nullable, a bare null on it is unguarded. The entry names this as where it is most likely wrong.
- **A shape that can carry an answer is not an answer.** `B2` and `B3` are RULED and neither is CLEARED. A later session reading "the wire shape landed" must not read that as a lifted blocker.
- **The absence may never move to a `*_cents` member**, in this response or any other, without first moving the scalar sweep.

### Files touched

`docs/decisions/ADR-203.md` (new), `docs/architecture/API_CONTRACT.md`, `docs/decisions/ALLOCATION.md`, `docs/decisions/README.md`, `docs/INDEX.md`, `docs/STATE.md`, `docs/plans/WAVE-06-admin-console-transport.md`, `docs/sessions/2026-08-28-session-386.md` (new), `apps/api/src/routes/admin-reads.ts`, `apps/api/src/admin-source/liability.ts`, `apps/api/src/admin-source/payout-velocity.ts`, `apps/admin/src/api/types.ts`, `apps/api/test/admin-reads.test.ts`, `apps/api/test/admin-breaker.test.ts`, `apps/api/test/admin-source-liability.test.ts`, `apps/api/test/admin-source-liability-book.test.ts`, `apps/api/test/payout-velocity.test.ts`.
