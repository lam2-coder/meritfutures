---
status: approved
depends_on: [README.md]
last_updated: 2026-09-08
---

## 2026-09-08 - Session 669: the planted extension set is read out of the checkers own suffix literals, the ninth extension is watched taking the read comparison away and giving it back, and two walkers were already blind for a reason no extension set can fix

<!--index: **THE ROW IS [ADR-474](../decisions/ADR-474.md), `proposed` and UNSIGNED. NON-MONEY DIFF, TWO CODE FILES, BOTH OF THEM ROW 470's OWN AND NEITHER ON `main` YET, `E2 READ OWED`.** [ADR-472](../decisions/ADR-472.md) section 10 item 4 taken: *"THE PLANTED TREE KNOWS EIGHT EXTENSIONS AND THEY ARE WRITTEN DOWN."* **THE FAILURE WAS REPRODUCED ON THE REAL TREE BEFORE ANYTHING WAS FIXED AND IT REPORTED `PASS` AT EXIT 0**: one character at [`generated-column-writes.mjs:345`](../../packages/tooling/checks/generated-column-writes.mjs), `.sql` to `.sqlx`, took `#derivedSet` from `read=[1,3]` to `read=[0,0]` and the check's `grew` count from 6 to 5, with nothing red and no line of the verdict changed. **The set is now read out of the checkers' own `endsWith` literals and unioned with the written floor**, which STAYS written because the derivation reaches neither `.yml` nor `.txt`: **12 extensions, 8 floor and 9 derived, 3 literals refused as unplantable AND NAMED, 2 arguments not literals.** **The round size was a written 8 against a written 8 and that coincidence would have eaten the fix.** **The same seed watched being caught**: 13 extensions, `read=[1,3] of [13,39]`, `grew` back to 6. **NO VERDICT MOVED.** **TWO ROSTERED WALKERS WERE ALREADY BLIND AT THE BASE AND NO EXTENSION SET CAN FIX THEM**, so the residue is MEASURED by `enumeratesUnread` rather than parsed for. **Leg C named four of this row's own cases and was right about all four.** Fifteen new cases. -->

### 1. The objective

Take [ADR-472](../decisions/ADR-472.md) section 10 item 4, quoted: *"THE PLANTED TREE KNOWS EIGHT EXTENSIONS AND THEY ARE WRITTEN DOWN. `PLANTED_EXTENSIONS` is the single place where the measurement carries knowledge about what a checker's inputs look like. A checker filtering on a ninth would enumerate a directory whose every name it rejects, which shows as an enumeration with no reads and is still a correct verdict, but the read comparison silently stops being available for it."* **Pay the price that entry set, which is deriving the extension set from the checkers' own string literals, and because the failure mode is SILENT, seed a ninth extension and watch the read comparison go away while the check still says `PASS` BEFORE fixing anything.**

### 2. Done

- **The failure was reproduced on the real tree before anything was fixed, and it reported `PASS` at exit 0.** One character at [`generated-column-writes.mjs:345`](../../packages/tooling/checks/generated-column-writes.mjs), `.sql` to `.sqlx`. `#derivedSet` went from `read=[1,3]` to `read=[0,0]`, `#run` from `[3,9]` to `[2,6]`, and the whole check's `grew` count from 6 to 5. **Nothing red, and the headline verdict byte-identical.**
- **The extension set is read out of the checkers' own `endsWith` literals** through `callsIn`, structure off the blanked side and text off the kept one, and unioned with the written floor. **12 extensions: 8 floor, 9 derived, 3 literals refused as unplantable and named, 2 arguments not literals.**
- **The floor stays written and the measurement is why**: the derivation reaches neither `.yml` nor `.txt`, because nothing in that directory spells either in a suffix test, so a replacement rather than a union would have planted fewer kinds of name than the list it replaced.
- **The round size was a written 8 against a written 8, and that coincidence would have eaten the fix.** `plant` cycles the suffixes, so a ninth extension in a round of eight is planted in the LARGE round only. `roundsFor` sizes each round as a multiple of the set; at the floor's eight it is still 8 and 24.
- **The same seed watched being caught**: 13 extensions with `.sqlx` in the derived set, `#derivedSet` back to `read=[1,3] of [13,39]`, `grew` back to 6. **Both seeds restored byte-identical, `cmp` clean, `git status --porcelain` empty after each.**
- **NO VERDICT MOVED.** 8 of 9 measured, 1 inconclusive, 6 grew, before and after.
- **Two rostered walkers were already enumerating and reading nothing at the base and no extension set can fix them.** [`price-register.mjs:160`](../../packages/tooling/checks/price-register.mjs) filters on `/^ADR-\d+\.md$/`, a name PREFIX. Inventing a name to satisfy a pattern is the second corpus [ADR-472](../decisions/ADR-472.md) section 3.2 refused, so `enumeratesUnread` MEASURES the residue and the report names both walkers. **Watched firing with a second seed: 2 to 3.**
- **What the parse does when it cannot decide is written down and asserted**, against [ADR-470](../decisions/ADR-470.md) section 7's four silent defects: it adds and never subtracts, it plants a candidate it cannot rule out, it REFUSES AND COUNTS BY NAME a literal no filename can carry, it counts a non-literal argument as `unfolded`, and it THROWS when it decides nothing at all.
- **Fifteen new suite cases**, 46 to 61 in the file, with the ninth-extension control held permanently in both directions.
- **Leg C named four of this row's own new cases on the first run and was right about all four.** The budget came off the four.
- **Two stale citations found and NAMED, not repaired.** [`ADR-472.md:187`](../decisions/ADR-472.md) cites `tree-input-budget.test.ts:702` and the title it quotes was at line 715 at this row's base. Off by thirteen, wrong on the day it was written, and [ADR-472](../decisions/ADR-472.md) section 10 item 7 named the other stale pointer in the same file and missed this one.
- **`CI-06u` named the first draft of this entry's own citation table** for four rows under one first cell, and the table was rekeyed rather than the gate relaxed.
- **[ADR-474](../decisions/ADR-474.md) written**, `status: proposed`, approval line UNSIGNED, four prices set.

### 3. Next

- The integrator registers [ADR-474](../decisions/ADR-474.md) section 10 items 1 to 4 in [`PRICE_REGISTER.md`](../decisions/PRICE_REGISTER.md) as `OPEN` / `UNASSESSED`. **This row did not touch that file and `price-register.mjs` goes red naming this entry's own four anchors, which is the check working.**
- **The `E2` read on [`tree-input-budget.mjs`](../../packages/tooling/checks/tree-input-budget.mjs) and [`tree-input-budget.test.ts`](../../packages/tooling/test/tree-input-budget.test.ts).** Both are row 470's files and neither is on `main` yet.
- **Section 10 item 3 is the one that matters most**: the two blind walkers are named and not answered, and answering them needs a decision about whether a checker may DECLARE the name shape its planted tree should carry.
- [ADR-470](../decisions/ADR-470.md) section 10 items 1, 3 and 4 and [ADR-472](../decisions/ADR-472.md) section 10 items 1, 2 and 3 are untouched and still unowned.

### 4. Blockers

- **None that stopped anything.** `price-register.mjs` is red by construction, section 9 of the entry, and exactly two cases go red for the same reason: [`price-register.test.ts`](../../packages/tooling/test/price-register.test.ts)'s *every leg holds on the tree this check ships with* and *the corpus prices things, so the check is not passing on an empty set*. **No case was touched to quiet them**, and the integrator registering section 10's four anchors turns all three green.

### 5. Landmines

- **THE ROUND SIZE AND THE EXTENSION COUNT WERE THE SAME NUMBER AND NOTHING SAID SO.** `8` was written in two unrelated-looking places and the measurement depended on their agreement. The relation is explicit now, and the general shape, a coupling between two written constants that no check asserts, is not enumerated anywhere in that module.
- **A CHECKER THAT SWITCHES FROM `endsWith('.sql')` TO `/\.sql$/` SILENTLY LEAVES THE DERIVED SET.** Only one call shape is parsed. The floor is what catches it and the floor does not carry every extension. Section 10 item 1 prices the second shape.
- **THE DERIVED HALF OF THE SET HAS NO CAP.** `PLANT_CAP` bounds directories and nothing bounds the suffix set, which is a union over a directory that grows. Section 10 item 4.
- **THE CONTAINER ARRIVED WITH NO `node_modules`, FOR THE SECOND ROW RUNNING.** A session that runs the suite before installing reads a green baseline over an unrunnable tree.
- **`repairable-sites.mjs` EXITS 2 WITH NO ARGUMENT AND THAT IS ITS USAGE.** A sweep that reads exit codes will call it a red.

### 6. Files touched

- [`packages/tooling/checks/tree-input-budget.mjs`](../../packages/tooling/checks/tree-input-budget.mjs), the derivation, the round sizing, `enumeratesUnread`, and the report lines.
- [`packages/tooling/test/tree-input-budget.test.ts`](../../packages/tooling/test/tree-input-budget.test.ts), fifteen cases.
- [`docs/decisions/ADR-474.md`](../decisions/ADR-474.md), new.
- [`docs/decisions/README.md`](../decisions/README.md) and [`docs/STATE.md`](../STATE.md), generator output only.
- [`docs/INDEX.md`](../INDEX.md), the checker's row.
- [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md), row `474`'s cells only.
- [`docs/sessions/README.md`](README.md) and this file.

**TWO SEEDS WERE WRITTEN OUTSIDE THE FENCE AND BOTH WERE RESTORED BYTE-IDENTICAL**, both at [`generated-column-writes.mjs:345`](../../packages/tooling/checks/generated-column-writes.mjs), each verified with `cmp` against a copy taken before the edit and each followed by an empty `git status --porcelain`.

### 7. The counts

| Leg | Base `7fcb2ee1` | Head |
| --- | --- | --- |
| `pnpm vitest run` | 349 files, **8,780 passed, 11 skipped, 8,791 total, 0 failed** | on the code alone **8,795 passed, 11 skipped, 8,806 total, 0 failed**; with this entry landed **8,793 passed, 2 failed, 11 skipped**, and the two are `price-register.test.ts`'s live-corpus cases |
| `gates.mjs check` | 33 of 33 | 33 of 33 |
| `repo-invariants.mjs` | 35 of 35 | 35 of 35 |
| `tree-input-budget.mjs` | PASS, 41 cases, 9 walkers, 8 measured / 1 inconclusive / 6 grew | PASS, 43 cases, 9 walkers, 8 measured / 1 inconclusive / 6 grew, **12 extensions, 2 blind walkers** |
| `price-register.mjs` | PASS, 89 prices over 18 entries | **RED, four findings, every one an anchor inside this entry** |
| `typecheck`, `lint`, `format:check` | clean | clean |
| the check's own cost | 926ms, 916ms, 877ms | 1,070ms, 1,129ms, 1,111ms |

**`pnpm run verify` and [`falsify.mjs`](../../scripts/corpus/falsify.mjs) were NOT run.** Both mutate the working tree and both are forbidden to this row. **`RE-P-01` did not fire on either full-suite run.**
