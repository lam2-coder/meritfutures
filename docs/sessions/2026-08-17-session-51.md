---
status: approved
depends_on: [README.md]
last_updated: 2026-08-17
---

## 2026-08-17 - Session 51: `R-32` computed on ADR-051, and the fencepost pinned (money path)

- **Done:** **`R-32` is implemented and declared. `IMPLEMENTED_RULES` 45 -> 46.** The refusal that stood in [`progression.ts`](../../packages/rules-engine/src/day/progression.ts) since session 47 is deleted and the rule computes. `DayInput` gains `openedOn: TradingDay`, required, which is the M01 section 2.1 amendment `ADR-051` authorises. **The fencepost was this session's to settle and `RE-U-032` is the settlement**, not a sentence. `pnpm run verify` green: **7 of 7 invariants, 737 tests with 32 skipped, 16 of 16 gates clean and dirty, 18 scope cases, 10 loader cases.**
- **Next:** `R-11` is the only remaining rule an engineering session can reach, by writing `replay`. `R-01`, `R-05` and `R-20` are each discharged by a layer that is not this engine.
- **Blockers:** **None of the four undeclared rules is blocked on a founder ruling any more**, which is new. R-32 was the last one that was.
- **Landmines:**
  - **THE FENCEPOST READING TAKEN, STATED PLAINLY: the opening day is elapsed day 1**, so `max_days` is **the number of trading days the account may trade**. With `max_days = 3` the third day folds and the fourth expires. `tradingDaysBetween` is exclusive of its anchor and answers 0 on the opening day, so without the `+ 1` a limit of N grants N+1 days. **What the pin asserts:** on `CME_WINDOW` anchored at 2026-11-02, `max_days = 3` leaves 2026-11-04 (elapsed 3) folding and expires 2026-11-05 (elapsed 4), with the `account.expired` payload compared field by field. Both sides were **watched failing** when the `+ 1` was dropped.
  - **`GAPPED_SLICE` IS THE INDEPENDENT ASSERTION AND IT IS THE MORE VALUABLE ONE.** Swapping `sequence` subtraction for a date difference leaves **the boundary pair green** -- `CME_WINDOW`'s five days are consecutive, so the two arithmetics agree -- and fails only the gapped fold, where subtraction answers 3 and date arithmetic answers 5. Measured, not assumed: the mutant was applied and the pair passed. R-02 and AS-06 stop being stated rules and become a test that fails if anyone reaches for a `Date`.
  - **`account.expired` CAME FROM THE CATALOGUE AND M01 SECTION 5.2 DOES NOT LIST IT.** [EVENTS.md](../architecture/EVENTS.md) carries it with `{account_id, trading_day, expiry_rule}`; section 5.2's eleven names do not include it. That is a **gap in section 5.2** rather than a licence to coin a name, because section 5.2 declares "all exist in the approved EVENTS.md catalogue except the two marked NEW". The union went from eight members to nine and `engine-events.test.ts` states the arithmetic rather than just the new total.
  - **AN UNANSWERABLE ANCHOR REFUSES, REUSING `calendar_coverage_miss`.** P2 section 1 rules the case by name: "replay will ask for the sequence of an anchor older than the slice". `eval_expiry_unimplemented` is **retired rather than renamed**, and the retirement is commented where the member sat, because a kind kept "just in case" is a branch every consumer writes and nothing ever takes.
  - **EXPIRED IS NOT BREACHED.** The expiry writes `phase: 'closed'` with `breached: false` and `breachKind: null`, and the day still emits `day.closed`. A consumer reading `breachKind` to explain the closure would otherwise be handed a drawdown type that never happened.
- **Files touched:** `packages/rules-engine/src/{types,rules,index}.ts`, `src/day/{progression,advance}.ts`, `test/{rules-e-progression,implemented-rules,engine-events,fixtures-in-code,rule-coverage}.ts` and eight further test files for the required field, `fixtures/README.md`, `scripts/ci/falsify-ci.mjs`, `apps/worker/src/batch/{ports,nightly}.ts`, `apps/worker/test/nightly-batch.test.ts`, `scripts/demo/fold.ts`.

### `accounts.expires_on` is ruled out as an input, and three mechanisms already make that structural

`ADR-051` rules that **`phase_eval.max_days` binds and `accounts.expires_on` is a derived materialisation that is never an input to `R-32`**. Nothing written this session makes that mechanically true: it is true because the column was not read. **A ruling whose enforcement is "we did not do the wrong thing" reads as unenforced to the next person**, so the enforcement is worth naming, because this one is genuinely enforced and by four separate things.

**The engine cannot reach a database at all.** [`RI-01`](../../packages/tooling/checks/repo-invariants.mjs) is "packages/rules-engine declares no workspace dependencies, in any dependency field", so `@merit/db` is unreachable by construction rather than by convention. [`RI-07`](../../packages/tooling/checks/repo-invariants.mjs) is "packages/rules-engine's transitive module graph reaches no Node builtin", which removes `node:fs` and `node:net` and with them every path to a socket or a file. **`types: []`** in [the package's tsconfig](../../packages/rules-engine/tsconfig.json) withholds the ambient Node typings, so a call to one would not compile even if the module resolved. And **`merit/engine-purity`**, attached at `error` in [`eslint.config.js`](../../eslint.config.js), is the lint-level statement of the same rule.

So a future session cannot quietly make `expires_on` authoritative by reading it: there is no query to write, no client to import, and three of the four mechanisms fail the build before the fourth is consulted. **What remains genuinely conventional is only that the value is not passed IN on `DayInput`** -- and that is visible in one interface, which is where ADR-051 put the amendment.

### A defect I introduced and the pin caught, which is the honest order to report it in

`falsify-ci.mjs` applies a seeded mutant, runs its check, and restores the file. **I ran it in the background and killed it mid-run**, so two seeds were left applied on disk, and a `git add -A` swept both into commit `b77f0ef`: R-32's fencepost with the `+ 1` dropped, and `plan/validate.ts`'s `MZ-per-phase` check short-circuited to `if (false && ...)`.

**Both entered in that one commit and nowhere else**, checked commit by commit against `origin/main` rather than assumed. The repair is `fdf14a3`.

**What caught it was `RE-U-032` itself**, failing on the fencepost by name -- the same failure the seed is watched producing, this time against its own author. A vacuous pin would have shipped an engine that granted every account one day more than its plan allows. **The lesson is the `git add -A`, not the kill**: a blanket add after running a tool that edits the tree in place cannot tell the session's work from the tool's leftovers, and `git status` was reporting `validate.ts` as modified the whole time.

**`falsify-ci.mjs` prints the moral itself, one line above its own summary: "A harness that leaves a seed behind has planted the next bug."** It does not leave one when it is allowed to finish. The completed run reports **61 of 65 seeds watched failing on the violation aimed at them**, with `CI-02/engine-R-32` among them (`found: RE-U-032 + R-32`); the three `CI-05` scanner seeds error because gitleaks, semgrep and syft are absent from this container's PATH, unchanged from prior sessions.

### No golden fixture, and the corpus is what decided it

`ADR-051` asked for the fencepost to be settled by an executable pin at the boundary and named a golden fixture. **The pin landed as `RE-U-032` instead, on three checks made before the decision rather than after it.**

**M01's own coverage column names `RE-U-032` and nothing else.** R-13's names `GS-009, GS-011` and R-25's names `GS-063, GS-064`, so the column does carry golden scenarios where the corpus intends them; R-32's carries a unit test alone, which makes it the *designated* pin rather than a substitute. **[GOLDEN_SCENARIOS](../testing/golden-scenarios/README.md) runs to GS-284 and none of them is eval expiry**, so minting a 285th would add a row to a frozen document, which is an ADR and not a commit. And **the plan record would have to invent `max_days`**: all three Appendix A columns carry `null`, which is GS-079's blocker exactly and is the rule TR-01 states.

**`CI-06d` then caught this very paragraph citing the number it argues against.** The first draft wrote the would-be identifier out in full, and the gate reported it as a citation to a scenario GOLDEN_SCENARIOS does not have -- correctly, because **prose naming an identifier is a citation whatever the surrounding sentence claims about it**. It reads "a 285th" now. A session arguing that a number must not be minted had minted it in the act of arguing, which is the smallest possible version of the drift the registry gates exist for.

**And it caught the fix's own description a second time**, because the sentence explaining the catch quoted the identifier twice more. That is the gate being exactly as literal as it should be: there is no "mentioned, not cited" exemption, and a registry that honoured one would be a registry anybody could talk their way past.

Recorded in [the fixtures README](../../packages/rules-engine/fixtures/README.md) beside GS-079 rather than left as an absence. **If the session moving the loader off `evaluate` lands first and this becomes writable, the fixture is a follow-up and the boundary is pinned either way.**
