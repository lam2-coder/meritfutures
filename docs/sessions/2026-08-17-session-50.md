---
status: approved
depends_on: [README.md]
last_updated: 2026-08-17
---

## 2026-08-17 - Session 50: the State column is deleted, and the letter registry gets `CI-06p` (non-money)

- **Done:** **[ALLOCATION](../decisions/ALLOCATION.md)'s State column is deleted from all three tables**, on [ADR-034](../decisions/ADR-034.md)'s own remedy, and **[`CI-06p`](../testing/STRATEGY.md) is written, seeded and watched failing on each of its three assertions.** The derivable half of the deleted cell is derived at check time by `node scripts/corpus/gates.mjs allocation`, a subcommand that lands with the gate. Letter `p` is claimed in the table it protects. **16 of 16 gates pass, and the full falsification harness passes**: every gate clean and dirty, 18 scope cases including the two new ones.
- **Next:** **`CI-06f`'s missing counterpart on the ADR table**, which is `CI-06p`'s second assertion one registry over: every `ADR-nnn` on disk is claimed by a row, exactly as `CI-06h` already requires of every migration on disk. It is **blocked on [`OI-11`](../STATE.md)** and named as blocked rather than left as an absence: the ADR table claims `039` to `046` twice each and the assertion fails on arrival. `OI-11` needs a ruling on which of each pair survives, and it is now the blocker for two gates rather than one.
- **Blockers:** **`OI-11`, the duplicate rows**, unchanged and untouched here. **The cross-branch job**, still unwritten: `CI-06p` inherits `CI-06f` and `CI-06h`'s two-ref gap verbatim and declares it on arrival.
- **Landmines:**

  - **A DECLARED GAP IS A DESCRIPTION OF A DEFECT, NOT A FIX FOR ONE, AND THIS IS THE MEASUREMENT.** [ADR-036](../decisions/ADR-036.md) ruled the State column prose no gate can check, and STRATEGY carried a paragraph declaring it as a fifth coverage gap. **Both were written before eight of the eleven drift instances.** Every word in them was true, the column was correctly described as unenforceable in two frozen documents, and it went on being wrong roughly every other day. The corpus's existing discipline is that a gate declares what it does not cover; **this session's evidence is that declaring is where the work starts, not where it ends**, because a reader who knows a column is unchecked still reads it and a session that repairs it by hand still leaves it repairable by hand.
  - **THE ELEVEN REPAIRS EACH LOOKED LIKE THE FIX AND EACH WAS THE DEFECT RECURRING.** The rows are the record: `035` was corrected four commits after PR #9 and the row says so, `036` was corrected in the fold that landed ADR-037 and ADR-038, and the session brief that commissioned that correction **was itself wrong about which row was stale inside two days**. Every one of the eleven was a careful person applying care to a class that care does not fix. ADR-034 says this in its own words about counts ("no amount of care fixes this class, because care is what was already being applied") and the column beside its own table was the site that had not heard it.
  - **THE REGISTRY THE SHARED PARSER CANNOT READ IS THE ONE THAT ENDS UP WITH NO GATE, WHICH INVERTS THE INTUITION.** `CI-06p`'s specification was written in the letter table's own prose a week ago, in the file the gate protects, and two harder gates were built beside it while it sat. The reason is mechanical rather than about attention: `allocated()` matches a three-digit or four-digit first cell and **a letter does not parse**, so ADR-036's careful insistence on one parser for both numeric tables became the reason the third table had none. **A registry shaped differently from its siblings is the one most likely to be unguarded**, precisely because reuse is cheapest everywhere else.
  - **GAPLESSNESS CANNOT FORCE A ROW, AND A GATE THAT ONLY ASSERTED IT WOULD BE DECORATION HERE.** A new gate in the runner fills its own hole, so the letters stay gapless forever while nobody claims anything and the table stops being what a sibling branch reads. The assertion that earns `CI-06p` is `CI-06h`'s second one, **every implemented letter is claimed by a row**, and the pair generalises: presence in the artifact and a claim in the registry each imply the other, or the registry is decoration. **`CI-06f` has never had that direction**, which nobody had noticed because the ADR table has been complete by hand.
  - **THE LETTER PARSER'S SECTION BOUND IS NOT THE NUMERIC ONE, AND COPYING IT WOULD HAVE FAILED SILENTLY.** `allocated()` bounds a table on the next `\n## `. The letter table is the **last** `##` section in ALLOCATION.md, so that bound runs to end of file and every table row in the prose below it would claim a letter. It is written to bound on any heading, in both the gate and the harness helper, and the reason is in a comment beside each: the two numeric tables have a `##` sibling beneath them and never had this problem, which is exactly what makes the copy tempting and wrong.
  - **THE SEED CLAIMS TWO LETTERS PAST THE FREE ONE, BECAUSE `expect` IS RESOLVED AFTER THE SEED RUNS.** `CI-06l`'s harness bug is the precedent: a seed that edits the row the expectation is computed from makes the harness ask about a different target than the one the gate names. Claiming `free + 2` opens the hole at `free` and leaves `free` where it was, so the expectation is stable by construction rather than by luck.
  - **THE QUIET DIRECTION NEEDED NO SEEDED CASE, AND SAYING WHY IS THE POINT.** `CI-06h/reserved` exists because a reserved hole passing is indistinguishable from a gate switched off. `CI-06p` needs no such case: **`o` is claimed by [ADR-044](../decisions/ADR-044.md) and implemented by nobody**, so every clean run of the harness already asserts that a sibling branch's reservation is a hole that passes. That is a real control planted in the corpus rather than in a temporary directory, and duplicating it as a scope case would have tested the same tree twice.
  - **WHAT A READER LOSES IS STATED IN THE FILE RATHER THAN GLOSSED.** The tables no longer say which claims are live. They never held that as a fact; they held a fact somebody entered, and the eleven instances measure how long it survived. `git ls-remote` answers it across refs, which is the manual step the `044` collision was written to teach, and the `allocation` subcommand answers the half one ref can honestly answer, labelling `absent` as **absent from this ref and nothing more**.

- **Files touched:** [ALLOCATION](../decisions/ALLOCATION.md), [`gates.mjs`](../../scripts/corpus/gates.mjs), [`falsify.mjs`](../../scripts/corpus/falsify.mjs), [STRATEGY](../testing/STRATEGY.md), [STATE](../STATE.md), [sessions/README](README.md), this file. The `gate_count` spans in [INDEX](../INDEX.md), STATE, STRATEGY and [session 38](2026-08-16-session-38.md) were regenerated 15 to 16 by `gates.mjs generate`, which is the span mechanism doing its job across a gate addition.

### What was NOT done, named rather than implied

**The duplicate rows are untouched.** `OI-11` owns them and it is a ruling rather than an edit. With the volatile column gone, each pair now differs only in its claimant, a branch row beside the row recording that branch's merge, which is the form a dedup session will collapse.

**No ADR was written for the deletion**, and that is a judgment worth recording rather than an omission. The remedy applied is [ADR-034](../decisions/ADR-034.md)'s, stated in ADR-034's own words and already applied once inside this file to the next-free-number sentence; the gate letter was allocated in the table before the gate was written, per ADR-036. **A new ADR would restate a ruling that exists**, which is the shape [ADR-037](../decisions/ADR-037.md) exists to refuse. If the founder reads the deletion as amending ADR-036's "it cannot verify the State column" consequence rather than executing ADR-034's remedy, that is an ADR and this paragraph is where the disagreement is visible.

**`CI-06o` is still prose.** [ADR-044](../decisions/ADR-044.md)'s money-path model ban has a letter and no gate, and this session claimed `p` around it rather than taking it. The hole it leaves is legitimate under `CI-06p`'s own assertion and is what the clean-tree run proves.
## 2026-08-17 - Session 50: the fixture wiring, and the premise ADR-048 rests on (non-money)

- **Done:** **All three items, in the stated order, each watched failing on its own seeded violation.** `L-13` makes `source:` a resolvable citation ([ADR-048](../decisions/ADR-048.md)'s stated prerequisite); `diffEndState` compares `bigint` cents against JSON numbers; ADR-048's polarity derivation is per fixture, reported per rule group. `pnpm run verify` green: **670 tests, 15 of 15 gates clean and dirty, 16 scope cases, 10 new loader cases.** **`packages/rules-engine/src/` is untouched**, per the session fence, and no fixture was authored or regenerated.
- **Next:** **The fold, and it is now the only thing between this stage and its own assertion.** `runFixture` calls `evaluate`, which is not one of [M01 section 1.3](../plans/M01-rules-engine.md)'s six functions. Rewiring it to `resolvePlan` -> `initialState` -> `advanceDay` makes `declaration.holds` true, at which point 30 per-fixture assertions switch on with nothing edited. It needs `PLAN_CONFIG_SOURCE` widened past `plan_version_id` and `EngineInput` retired from the loader, and **it wants a plan-mode session** because it changes how every fixture is loaded. Two founder rulings still hold fields on some fixtures and would surface the moment it lands: **`INV-07` versus the funded reset**, and the **floor-lock jump disagreement**.
- **Blockers:** **Nothing blocked this session.** The one decision above the code was taken by the founder mid-session and is recorded below.
- **Landmines:**

  - **`IMPLEMENTED_RULES` IS TRUE OF THE PACKAGE AND FALSE OF THE FOLD, AND THAT IS A HOLE ADR-048 DOES NOT NAME.** The ADR enumerates four ways the derivation can be wrong and closes each, three of them "before any fixture is consulted". **Its failure mode 3 is "a rule id is added to the declared set without the rule being implemented", answered by cross-checking against the passing `RE-U-nn` set.** That cross-check is satisfied here: all 45 declared rules are implemented and every one has a passing unit test. **They are implemented in `advanceDay`, `applySettlement` and `evaluatePayout`, and CI-03 folds `evaluate`**, the scaffold placeholder that returns its input by reference and emits nothing. So a rule can be implemented, unit-tested, declared, cross-checked, and **unreachable from the stage that derives polarity from the declaration**. ADR-048 speaks of "the engine" as one thing and M01 section 1.3 exports six functions; the loader calls none of them. `checkDeclarationAgainstFold` is the check that names it, and it is the retired probe's own warning promoted from a comment, **because the case that header named stopped being hypothetical**.

  - **ALL 30 FIXTURES FLIP, AND THE FLIP IS NOT THE FINDING.** Every fixture on the tree cites only rules the engine declares, so all 30 derive `direct` and none derives `inverted`. That is not a fixture problem and none was edited: the batches were written to cite resolvable rules and [session 48](2026-08-17-session-48.md) said in its own words that "all four derive to `direct`". **What the derivation surfaced is the premise underneath it**, and the honest count is that 30 of 30 would fail today for a reason that has nothing to do with any of their subjects.

  - **THE VACUITY CLASS SURVIVES `L-13` BY ONE PREFIX, AND THE ADR'S OWN PREREQUISITE DOES NOT CLOSE IT.** ADR-048 case 4 is a fixture citing nothing, closed "by a prerequisite rather than by care": [P2 section 2](../plans/P2-rules-engine.md)'s resolvable citation. **But that prerequisite accepts "at least one `R-nn`, `CV-nn` or `INV-nn`", three prefixes, and polarity is derived from rules.** A fixture citing only `INV-06` satisfies `L-13`, cites no rule, and "every rule this fixture cites is implemented" is vacuously true of it exactly as before. Closed structurally in `derivePolarity`: an empty cited-rule set derives `inverted`, never `direct`. **This is the sixth costume of the same defect** the ADR already lists in five, and it arrived inside the repair for the fifth.

  - **THE `bigint` DEFECT WAS INVISIBLE AND WOULD HAVE STAYED INVISIBLE UNTIL THE MOMENT IT MATTERED.** `INV-02` makes every money field the engine returns a `bigint`, JSON has no literal for one, and `Object.is(4770000n, 4770000)` is false. Under the inverted polarity every fixture must FAIL and every fixture did, **so a comparison that could never agree with anything looked exactly like one that worked**. It would have surfaced as thirty unexplained red fixtures on the day the first group flipped. **No seeded fixture can catch it**, which is why its three seeds mutate source rather than data: that is the discriminating experiment, and a data seed would have been decoration.

  - **THE REPORT PRINTED TWO IDENTICAL NUMBERS AND CALLED THEM A DIFF.** `String(4770000n)` and `String(4770000)` are the same characters, so the finding read "expected 4770000, engine produced 4770000". **A reader seeing that goes looking for a bug in the differ**, which is the most expensive possible way to spend the first hour of an investigation. `show` now renders a `bigint` with its `n`. Whatever a comparison decides about two types, it may not describe them in a way a reader cannot tell apart.

  - **A COERCION FIX IS ONE CHARACTER FROM BEING WORSE THAN THE DEFECT, SO BOTH DIRECTIONS ARE SEEDED.** The old comparison could never assert a cent. A comparison that agrees with everything asserts **every** cent is right, and money moves in the second direction. `compare/over-agreement` mutates `bigintAgrees` to `return true` and watches the one-cent case go quiet. The third seed removes `Number.isSafeInteger`, at which point a fractional expectation reaches `BigInt()` and **throws**: an exception is not a diff, it names no field, and CI-03 would report a crash where a fixture defect belongs.

  - **THE RULE GROUPS ARE READ FROM M01 BECAUSE THE ENGINE STATES THEM IN COMMENTS.** ADR-048 requires coverage to report polarity per rule group and rejects deriving polarity from the group, "because the group is not written on the fixture and would become a second hand-maintained mapping, which is ADR-034's class". **That argument applies to the report too.** `RuleId` in `rules.ts` carries the eight boundaries as comments, which no machine reads; restating the ranges in the loader would have been the second expression. M01 states them as headings above the rule tables, so the document that defines a rule also says which group it is in. **Empty groups are printed**: G and H have no fixture citing them, and a table that listed only the groups with work done would shrink to the work done.

  - **`falsify.mjs` COULD NOT RUN A TYPESCRIPT LOADER AND NOW CAN, AND THE FIX WAS IN THE CHECKER RATHER THAN THE HARNESS.** `copyTree` omits `node_modules`, so nothing needing vitest or a workspace resolution runs in a tree copy. `check.mjs` therefore imports `./src/loader.ts`, `./src/compare.ts` and `./src/polarity.ts` **directly and never the barrel**, which re-exports the two modules that import the engine as a value. **That is what lets a case seed the RULE and not only the DATA**, which is the whole reason the `bigint` and polarity seeds exist at all. `polarity.ts` takes the declared rule set as a parameter for the same reason, which is the argument `compare.ts` already makes for itself.

  - **THE DEFERRED ASSERTION IS ONE COMPUTED CONDITION AND NOT A `pending` FLAG, AND THE DIFFERENCE IS WHERE IT LIVES.** TR-03 forbids the per-fixture escape hatch this package names in its own source. What landed instead: **one condition, derived, covering the whole directory, printed in bold in the stage's own output on every run, and unreachable from any fixture.** The two blocks are mutually exclusive and nothing is edited to move between them, so the derived assertion starts and the standing one stops on the day the fold changes. **A test asserts the block still says it**, because a finding that exists only in a variable is the quiet direction.

  - **THE CORRUPTION PROBE HAD TO BE RE-AIMED AT THE ASSERTION THAT RUNS.** It measures whether a corrupted expectation still passes, and it had one global polarity to measure against. With the derived direction reported rather than enforced, measuring against the DERIVED direction would have reported 0 blind fixtures while all 30 were blind, **describing a stage other than the one that just ran**, which is the exact defect `coverage.ts` exists to prevent.

- **Files touched:** [`packages/golden-loader/src/loader.ts`](../../packages/golden-loader/src/loader.ts), [`compare.ts`](../../packages/golden-loader/src/compare.ts), [`coverage.ts`](../../packages/golden-loader/src/coverage.ts), [`run.ts`](../../packages/golden-loader/src/run.ts), [`index.ts`](../../packages/golden-loader/src/index.ts), [`polarity.ts`](../../packages/golden-loader/src/polarity.ts) (new), [`check.mjs`](../../packages/golden-loader/check.mjs) (new), [`test/loader.test.ts`](../../packages/golden-loader/test/loader.test.ts), [`test/compare.test.ts`](../../packages/golden-loader/test/compare.test.ts), [`test/fixtures.golden.test.ts`](../../packages/golden-loader/test/fixtures.golden.test.ts), [`scripts/corpus/falsify.mjs`](../../scripts/corpus/falsify.mjs), [sessions/README](README.md), this file.

### The founder ruling taken mid-session

**The derivation is REPORTED and not ENFORCED while the premise it rests on does not hold.**

The measurement was taken before the question was asked: derivation wired, 30 of 30 `direct`, 31 failures counting the declaration cross-check. Three options were put up.

| Option | Outcome |
|---|---|
| Enforce now | CI-03 red on 31 assertions. [STRATEGY section 1](../testing/STRATEGY.md): a permanently red required stage is worse than a smaller one nobody clicks through |
| **Report, do not enforce** | **RULED.** The stage asserts what it can honour and says in bold what it is not asserting and why |
| Rewire the fold this session | Larger than the three items, changes how every fixture loads, and two open founder rulings could make it red for real corpus reasons |

**What makes the ruled option honest rather than convenient is that nothing was weakened to reach it.** The derivation is implemented as ADR-048 states it, the numbers it produces are printed in full including the thirty that would fail, and the switch is a computed condition rather than a flag. **What it defers is enforcement against a fold that cannot honour it**, which is the same thing TR-02 has been saying about this stage since it was written.

### What is NOT here

**No engine code.** `packages/rules-engine/src/` was not opened. The concurrent session owns it, and [P2 section 2](../plans/P2-rules-engine.md) alternates fixture and engine sessions because "a session doing both has derived its expectations from its own output".

**No fixture authored and none regenerated.** The fence allowed regeneration and nothing needed it: all 30 already cite resolvably, and the `L-13` seeds run against copies.

**No ADR.** The finding above deserves one and it is **not this session's to write**: it amends an accepted ADR on the money path, and the repair it points at (the fold) is a design session. Recorded here and in `polarity.ts`'s own header so the next session starts from the measurement rather than from the argument.

**STRATEGY's CI-03 row is not amended**, and ADR-048's consequences require it: the row should say what a green CI-03 means during P2. It is a corpus document outside this session's fence, and amending it before the enforcement question is settled would fix a sentence about a stage that is about to change again.
## 2026-08-17 - Session 50: a disabled consistency gate reports skipped, and the EngineEvent union (money path)

- **Done:** **Two review-desk rulings, both landed, and the first one was repaired by being read out of the sources rather than out of the file.** [`consistencyOk`](../../packages/rules-engine/src/day/consistency.ts) returned `{ ok: true, skipped: false }` when `cfg.enabled` is false, so a gate the plan never configured rendered as **SATISFIED** in every consumer that reads the flag. The repair is **one line**, `skipped: true`, plus the header comment that caused the wrong repair to be proposed first. [`EngineEvent`](../../packages/rules-engine/src/types.ts) is now the **discriminated union** of the eight concrete events rather than the base `{ type: string; tradingDay }`; the base is renamed `EngineEventBase` and `EngineEventType` is derived from the union. New suite [`engine-events.test.ts`](../../packages/rules-engine/test/engine-events.test.ts), six cases. `pnpm run verify` green apart from `falsify:ci`'s absent scanners: **6 of 6 invariants, 663 tests with 30 skipped, 15 of 15 gates clean and dirty, 16 scope cases**. **ZERO fixtures regenerated**, and that is a property of the format rather than a piece of luck: see the third landmine.
- **Next:** **The `apps/worker` leaf-table exhaustiveness hole** recorded below is the follow-up this session declined to take, because it is outside the fence and is a real finding independently of anything here. After it, the fixture-wiring session is still the bottleneck for `GS-080` and `GS-060`, both of which turn on the `skipped` flag this session just made correct and neither of which can be written until `diffEndState` grows a path syntax.
- **Blockers:** None taken. Two things were **reported rather than changed**, both because changing them would have been this session widening a frozen rule on its own authority: `R-37`'s zero-gap case and `scripts/demo/render.ts`'s now-stale comment.
- **Landmines:**

  - **THE WRONG RULING WAS REASONED OUT OF A CODE COMMENT THAT CONTRADICTED FIVE FROZEN CITATIONS, AND BOTH ENDS OF THE REVIEW BELIEVED IT.** [`consistency.ts`](../../packages/rules-engine/src/day/consistency.ts)'s own header read **"`skipped` IS NOT `!enabled`"** and reserved the flag for `R-30`'s denominator rule. That sentence was a previous session's invention. The corpus says the opposite four separate times in nearly the same words: `CV-19` ([M01:286](../plans/M01-rules-engine.md)) "using the **same `skipped` shape** as the consistency denominator rule", [EC-050](../edge-cases/EC-050.md) "the **identical shape** the consistency denominator rule already uses", [ADR-015](../decisions/ADR-015.md) "the **same shape** the consistency denominator rule already uses", [GLOSSARY](../GLOSSARY.md) "reports `pass: true, skipped: true` and renders as disabled", and `GS-080` "the **same shape** the consistency denominator rule uses". The first ruling issued from the review desk therefore called for a **third state** (`evaluated` / `skipped` / `disabled`) and forbade the one-line fix, on the strength of the comment. **The comment is what had to be deleted, not the vocabulary.** This is [EC-157](../edge-cases/EC-157.md)'s shape exactly: **one artifact disagreeing with four, and the disagreeing one winning because it was the one nearest to hand.** There it was an executable `CHECK` constraint against four written identities, and the lesson recorded was that "an executable statement of the wrong identity is still the wrong identity ... the only one of the five artifacts that was never read against the other four". Here it was a code comment against five frozen citations, and it was never read against them either. **A claim nobody re-derived became a fact**, on a money path, for the second time in two days. The comment now carries all five citations, so the next reader is corrected by the file rather than misled by it.

  - **THE ONE SOURCE THAT AGREED WITH THE CODE IS THE REASON THE CODE LOOKED RIGHT, AND IT IS THE WEAKER KIND OF SOURCE.** [M01 section 3.6](../plans/M01-rules-engine.md)'s reference algorithm writes `if (!cfg.enabled) return { ok: true, skipped: false };` verbatim, and `consistency.ts` was a faithful transcription of it. So a reviewer checking the implementation against the primary source found agreement and stopped. **A five-line sketch inside a section about arithmetic lost to a validation rule stated in section 2.4 and repeated in four documents outside M01**, and naming that precedence is the transferable part: the specific and repeated statement governs the incidental one. **No frozen document moved and no ADR was needed**, which is the test that the repair was the right one. A third state would have required amending `CV-19`, `EC-050`, `ADR-015`, the GLOSSARY, `GS-080`'s stated shape and `API_CONTRACT`'s published gate object, to fix a defect whose cause was one comment.

  - **NO FIXTURE COULD EVER HAVE CAUGHT THIS, AND THE FIXTURE THAT WOULD IS HELD BACK ON A DIFFERENT BLOCKER.** `engine_gates` is a **nested object** and [`diffEndState`](../../packages/golden-loader/src/compare.ts) compares flat fields with `Object.is`, so no expectation reaches the `skipped` flag; `GS-021`'s own sibling says so in its `note`. That is why zero `*.expected.json` moved and it is why the claim is worth stating rather than celebrating: **the golden suite was silent because it is structurally incapable of speaking here**, not because the change was safe. `GS-080` is precisely "a gate configured to zero renders as disabled, not as satisfied" and is blocked on that same nested-object limit. **The defect and the test that would have found it were blocked by the same missing loader feature**, which is the argument for the path syntax stated in cost rather than in principle.

  - **`maxDayShareBp` IS NOW LOAD BEARING AND IT IS THE TYPE THAT GUARANTEES IT, NOT A CONVENTION.** Folding the disabled case into `skipped` is only safe because support can still tell **"passed because there was nothing to test"** from **"never configured"**, and that distinction survives with no new field: `ConsistencyRules` is a discriminated union whose `{ enabled: false }` arm **has no `maxDayShareBp` field at all**, so the disabled branch has no limit available to report and `null` is the only value it can carry; `R-30`'s branch is reached only under `enabled: true`, where `CV-06` pins `0 < max_day_share_bp <= 10000` and the value can never be a falsy `0` that reads like an absence. It was **verified against the type before being relied on** and is now asserted in [`rules-e-progression.test.ts`](../../packages/rules-engine/test/rules-e-progression.test.ts), because a property that is the whole reason a third state was not added must not stay a readable coincidence.

  - **`R-37`'s ZERO-GAP CASE IS THE SAME ARITHMETIC SHAPE AND WAS REPORTED RATHER THAN CHANGED.** The case the ruling asked about, **no cadence anchor**, is already correct: [`gates.ts`](../../packages/rules-engine/src/payout/gates.ts) reports `pass: true, skipped: true` and `rules-f-gates.test.ts` already pins it with `CV-19`'s reasoning. What is left is `cadence_gap_trading_days == 0`, where `counted >= 0` is unconditionally true and the gate reports `pass: true, skipped: false`, reading as satisfied. `EC-050`'s **general** rule covers it in its own words: "a gate whose configured threshold makes it unconditionally true reports `pass: true, skipped: true`", and it is the identical shape `CV-19` rules for `R-33`'s zero. **But no frozen document says zero disables `R-37`, and `PW-04` cuts the other way**: it fires a `warning` on `cadence_gap_trading_days == 0` with `required_win_days <= 1` ("approaching uncapped daily extraction"), which treats zero as a live and dangerous configuration rather than an absent gate. **It is unreachable on all three v1 plans** (Core EOD 5, Direct 5, Merit Rapid 1), so nothing is at risk today. Changing it would be this session extending `EC-050` to a gate no document names, one session after being corrected for reasoning from an invention. It needs a ruling, not a commit.

  - **`apps/worker`'s GATE-LEAF TABLE HAS NO EXHAUSTIVENESS TIE, AND ITS COUNT ASSERTION DOES NOT SUPPLY ONE.** [`state-hash.ts`](../../apps/worker/src/batch/state-hash.ts)'s `ENGINE_GATE_LEAVES` is a hand-written list of twenty-five dotted paths, typed `path: string`, with **no `keyof`, no `Record<>` over the interface and no `satisfies`**. `state-hash.test.ts` asserts `toHaveLength(25)`, which pins the list against **itself** and not against `EngineGateResults`. So a field added to any gate interface compiles, is **silently never hashed**, and every test still passes at 25. That is `SD-08`'s divergence surface losing a leaf without a single failure anywhere. **This session's change did not add a gate field**, precisely because the repair turned out to be one line, so the hole is untouched and unexercised rather than newly dangerous. It is recorded here as a follow-up because it is a real finding **independent of anything in this diff** and because the next session that does widen a gate record will hit it without warning. The explicit ordered array is correct and required (M01 section 1.4 bans key iteration); what is missing is the compile-time proof that the array is complete. The shape that supplies it is in [`engine-events.test.ts`](../../packages/rules-engine/test/engine-events.test.ts), where `Record<EngineEventType, true>` makes a missing member a compile error in both directions.

  - **A COMMENT IN `scripts/demo/render.ts` NOW DESCRIBES A WORLD THAT NO LONGER EXISTS, AND IT WAS LEFT THAT WAY ON PURPOSE.** It reads "there is no exported discriminated union, and a per-type renderer would need a cast per event type", which was **true when written and was the best available evidence for ruling 4** (a consumer documenting the cast it was forced into). It is false as of this session. `scripts/demo` is outside the fence, so it is reported rather than edited, and it is reported **loudly** because a stale comment that a later reader trusts is exactly the failure the first landmine on this page describes. The file's behavior is unaffected: it walks `Object.entries` and needs no narrowing.

- **Files touched:** [`consistency.ts`](../../packages/rules-engine/src/day/consistency.ts), [`types.ts`](../../packages/rules-engine/src/types.ts), [`index.ts`](../../packages/rules-engine/src/index.ts), [`rules-e-progression.test.ts`](../../packages/rules-engine/test/rules-e-progression.test.ts), [`engine-events.test.ts`](../../packages/rules-engine/test/engine-events.test.ts) (new), [STATE](../STATE.md), [sessions/README](README.md), this file.
## 2026-08-17 - Session 50: `P2-7`, the three merge blockers that were never written, and PT-06's three halves (test infra)

- **Done:** **All thirteen file touches of [P2-7](../plans/P2-rules-engine.md) section 7.** [M01 section 1.4](../plans/M01-rules-engine.md) names `RE-D-01`, `RE-D-02` and `RE-D-03` and calls all three **merge blockers**; verified before building, **none of the three was in the tree** and the strings appeared only in prose. All three now exist and all three have been **watched failing on a seeded violation aimed at them**. The third generator [P2 section 5](../plans/P2-rules-engine.md) names is built, and `PT-06`'s harness is built with **two of its three halves measured to be vacuous rather than asserted to be live**. `pnpm run verify` exits 0: **714 passed, 32 skipped across 51 files, 7 of 7 invariants, 15 of 15 corpus gates**, `CI-01` 10 of 10 and `CI-02` 50 of 50 falsifiable.
- **Next:** **The fixture-wiring session is still the bottleneck and this session sharpens why.** Two of the four new gates run against a corpus that **proves nothing today**, because `runFixture` still folds `evaluate`, the scaffold identity stub. That is not a defect in the gates and it is not narrated anywhere a reader has to remember it: it is **derived on every run** from `engineIsIdentityStub()` and printed. The day the loader moves to `advanceDay`, three skipped assertions switch themselves on with no edit. `P2-8` gains one item: exporting `replay` un-skips `PT-06`'s permutation block, which currently **throws by design** if reached.
- **Blockers:** **None new.** The calendar transcription remains the founder item blocking groups A, F and H, unchanged. `INV-07` versus the funded reset and the floor-lock jump disagreement are unchanged and neither was touched.
- **Landmines:**

  - **`LC_ALL` CANNOT BE RANDOMIZED IN-PROCESS, AND THE OBVIOUS READING OF STRATEGY 3.1 PRODUCES A SUITE THAT PASSES FOREVER WHILE PROVING NOTHING.** Section 3.1 gives `PT-06` "`TZ` and `LC_ALL` randomized per case, which is how a `toLocaleDateString` gets caught". `TZ` works: V8 re-reads `process.env.TZ` on every `Date` operation, and one instant renders at +00:00, +14:00 and +05:30 under three successive assignments. **`LC_ALL` does not.** Node resolves the ICU default locale **once at startup**, so `Intl.DateTimeFormat().resolvedOptions().locale` stays `en-US` however it is assigned afterwards and a bare `toLocaleDateString()` keeps formatting `1/2/2026` under a Turkish `LC_ALL`. The same values **on the command line** do change it. So the locale half is carried **across processes** by `RE-D-02` and `engine-digest.mjs`, and `localeIsProcessScoped()` **measures** the fact rather than restating it, so a future runtime that fixes this flips the suites on by itself. Written into [STRATEGY section 8.2](../testing/STRATEGY.md) because the next reader of section 3.1 will otherwise implement the sentence literally and ship a vacuous gate.

  - **THE HOLE `RE-D-03` CLOSES IS A PATH, NOT A LINE, WHICH IS WHY FOUR MECHANISMS MISSED IT.** `RI-01` reads the manifest and says so in its own `covers`. `merit/engine-purity` reads one file at a time and returns early on **every** relative specifier (`if (source.startsWith('.')) return`). `eslint.config.js` attaches it to `packages/rules-engine/src/**/*.ts` **and nothing else**. `tsconfig`'s `types: []` removes ambient declarations, not an explicit `import ... from 'node:fs'`. So `packages/rules-engine/impure/x.ts` importing `node:crypto`, reached from `src/index.ts` as `../impure/x.js`, is **invisible to all four at once**. Seeded on this tree: `RI-07` reports both findings with the import trail while `RI-01` and `eslint packages/rules-engine/src` **both stay green**, and the falsification case demands the string `ADDITIVITY: RI-01 green, engine-purity green` in its own needles, so a future change that made the old mechanisms catch it would be reported OFF-TARGET rather than silently accepted.

  - **A MUTANT WHOSE EFFECT NEVER LEAVES ITS FUNCTION PROVES NOTHING, AND THE GATE WAS RIGHT TO SAY SO.** `RE-D-02`'s first seed assigned `toLocaleDateString()` to a variable it then discarded. The digest was byte-identical under every locale and the harness reported "did not fail". The reflex is to assume the gate is broken; **the gate was correct and the seed was worthless**. It is now a `toLocaleString()` thousands separator, which reaches every money figure in the output. The general shape: **a seeded violation must reach the surface the gate compares**, and "I planted something impure" is not the same claim as "I planted something the gate can see".

  - **THE ENGINE'S `types: []` REACHES ITS TEST DIRECTORY, AND THAT IS LOAD BEARING RATHER THAN INCONVENIENT.** `PT-06`'s harness needs `process`; `packages/rules-engine/tsconfig.json` sets `types: []` with `include` covering `test/**/*.ts`, and `tsc` refused the file five times. **Widening that array would have been weakening a gate to pass it**, which the working agreements forbid in as many words and which that file's own comment calls "the strongest of the three mechanisms guarding this boundary". The harness lands in `packages/golden-loader/test/` for exactly the reason that package's tsconfig already gives for the loader itself. **The type error was the design telling us where the file goes**, and the cheap fix was the wrong one.

  - **`INV-17` CANNOT BE VIOLATED ALONE, AND THE COUNTERFACTUAL DECLARES THAT RATHER THAN RELAXING TO ACCOMMODATE IT.** The lifetime bound is `max_payouts * max cap`; while `R-42` clamps every approval and `R-49` bounds the count, it holds **arithmetically** as their conjunction over a life. The settlement counterfactual asserts a **single** finding per inverted rule, so this one is entered in a `JOINT` table as a **compile-checked list of one**, with a test asserting the list has exactly one entry. A counterfactual quietly permitted two findings would be a hole in the one assertion that file's honesty rests on, and it would hide the day some future rule genuinely started breaking two things at once.

  - **THREE COLLATERAL-FINDING DEFECTS IN THE SETTLEMENT GENERATOR WERE FOUND BY THE SINGLE-FINDING ASSERTION, AND EACH WAS FIXED BY MOVING THE INVERSION RATHER THAN LOOSENING THE ASSERTION.** Shifting only the first ordinal to 2 also broke consecutiveness (the whole stream shifts now). Approving one cent over the cap also crossed `INV-17` (it now runs one rung below the ladder with the other approvals at the minimum). `R-49` and `INV-17` needed the amounts pinned in **opposite directions** to separate breaking the count bound from breaking the lifetime bound. This is `day-sequence.property.test.ts`'s stated experience arriving again on a different structure.

  - **A SETTLEMENT DAY CARRIES ITS WITHDRAWAL AND NOTHING ELSE, WHICH IS A MODELLING CHOICE AND NOT AN ACCIDENT.** `adjustmentCents` is a day's **net** non-trading movement, so the first version of the generator let a drawn deposit offset the withdrawal and emitted a mark whose net was `-9,999` against an approval of `10,000`. Netting is real. But a generator that models it **cannot state `R-10` as a checkable inequality**, and a rule the oracle cannot state is a rule nothing enforces. Recorded because the choice is invisible in the output and a later session will otherwise "fix" it.

  - **A MARKED DIRECTORY MAKES `falsify-ci.mjs` THROW BEFORE ANY CASE RUNS.** `sweep()` removes stale `__falsify__` paths with a non-recursive `rmSync`, so a seeded **directory** produces `EISDIR` at startup and takes every case down with it, not just its own. Avoided rather than fixed: the seed is a file at the package root, which is outside `src/**` already. **The fix belongs to whoever next owns that harness**, and it is a one-word change (`recursive: true`).

  - **PT-06's OWN PROBE CONTAINED THE DEFECT PT-06 HUNTS.** Written at 03:00 UTC, `toDateString()` did not move under `Pacific/Kiritimati` (+14), because 03:00 plus fourteen hours is still the same day. At 20:00 it does. **A test written at the first hour would have reported "no timezone sensitivity here" about a codebase riddled with it**, which is precisely the failure mode of a trading day derived from a timestamp: correct for most of the day in most zones, wrong for the hours that cross the boundary.

- **Files touched:** [`validate-settlement-sequence.ts`](../../packages/rules-engine/test/generators/validate-settlement-sequence.ts) (new), [`settlement-sequence.ts`](../../packages/rules-engine/test/generators/settlement-sequence.ts) (new), [`settlement-sequence.property.test.ts`](../../packages/rules-engine/test/generators/settlement-sequence.property.test.ts) (new), [`golden-loader/test/harness/environment.ts`](../../packages/golden-loader/test/harness/environment.ts) (new), [`scripts/demo/test/replay-determinism.property.test.ts`](../../scripts/demo/test/replay-determinism.property.test.ts) (new), [`scripts/ci/engine-digest.mjs`](../../scripts/ci/engine-digest.mjs) (new), [`golden-loader/test/determinism.test.ts`](../../packages/golden-loader/test/determinism.test.ts) (new), [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) (RI-07), [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts) (six cases), [`falsify-ci.mjs`](../../scripts/ci/falsify-ci.mjs) (four cases), [STRATEGY](../testing/STRATEGY.md) (section 8, appended), two manifests and the lockfile, [STATE](../STATE.md), [sessions/README](README.md), this file.

### The count, reported rather than implied

**Thirteen file touches, all thirteen landed.** The plan's first draft said "nine files" above a list of twelve numbered items covering thirteen touches; the founder caught it before any code was written, which is worth recording in the session whose whole subject is gates that stop a stated count drifting from the thing it counts.

**Four gates, four seeded violations, each watched failing on its own finding and watched passing out of scope.** Two of the four carry the out-of-scope assertion **inside the harness's own needle mechanism** rather than in a comment, so it is checked on every run.

### What is honestly still vacuous

Stated here as well as derived in the code, because a session log is read once by someone deciding what to do next.

| Assertion | Today | What changes it |
|---|---|---|
| `RE-D-01` over the demo corpus | **REAL.** 45 of 50 rules folded under the trap, and the same bytes out | |
| `RE-D-02` across three `(TZ, LC_ALL)` pairs | **REAL.** Spawned, and the spawn is watched moving the locale | |
| `RI-07` / `RE-D-03` | **REAL.** Fails on the escape while the other four mechanisms stay green | |
| `PT-06` timezone invariance | **REAL** over `runDemo` | |
| `RE-D-01`/`RE-D-02` over the **golden** corpus | **VACUOUS.** The loader folds `evaluate` | the fixture-wiring session |
| `PT-06` locale invariance **in-process** | **VACUOUS**, and inert by measurement | a Node that re-resolves the locale |
| `PT-06` arrival-order permutation | **UNEXPRESSIBLE.** No `replay` export | `P2-8` |
