---
status: approved
depends_on: [README.md]
last_updated: 2026-08-20
---

## 2026-08-20 - Session 107: WAVE-04, the fixture backlog and the gate inventory (planning, non-money)

- **Objective:** plan the first wave that is not corpus work: `OI-25`, the fixture backlog, and `OI-26`, the gate rows that do not exist.
- **Done:** **[WAVE-04](../plans/WAVE-04-fixture-backlog-and-gate-inventory.md), eight sessions, five concurrent**, with the partition, the fences, the reserved numbers and a pasteable prompt each. `ADR-072`, `ADR-073` and `ADR-074` reserved in [ALLOCATION](../decisions/ALLOCATION.md) **in their own commit before any session runs**; no migration number and no `CI-06` letter. INDEX row added, one `##` section appended to [STATE](../STATE.md). **24 of 24 gates, 74 test files and 1085 passing, on this branch.**
- **Next:** `W1` through `W5` may start together. `W6` and `W7` wait on rank 1; `W8` waits on all of them and is the only session that writes [`gates.mjs`](../../scripts/corpus/gates.mjs).
- **Blockers:** None. Nothing in the wave waits on the Rithmic vendor call.

### The finding that changed the plan

**276 is the subtraction and it is not a queue.** 40 fixtures against <!--gen:gs_count-->316<!--/gen--> scenarios, both verified by running. **Sixteen are writable today**; 14 need one ruling each, 3 are held, and **243 have no module code and no fixture format**. The second half of that is the one worth carrying: the fixture format is `packages/rules-engine/fixtures/GS-NNN-*.yaml`, a day stream folded through the engine, M1-shaped in every field, so **a module whose code landed tomorrow would still have nowhere to write its fixtures**. Every fixture on disk is M1-owned.

**And "the engine has not implemented the rule" is not a blocker at all.** [ADR-048](../decisions/ADR-048.md) derives polarity from the cited rules against `IMPLEMENTED_RULES`, so a fixture citing an undeclared rule loads, derives inverted, and flips on its own when the rule lands. `falsify.mjs` holds the scope case. The M1 backlog is bounded by the format and by open questions, never by engine progress.

### Landmines

- **`OI-19` AND `OI-20` WERE ALREADY TAKEN AND THE BRIEF HANDED BOTH OUT AGAIN.** [STATE](../STATE.md) carries `OI-19` (the conflict-marker gate) and `OI-20` (the three-digit session number); the highest allocated is **`OI-24`**, in [session 95](2026-08-20-session-95.md), and it lives in a session log rather than in any registry. **The two new items are `OI-25` and `OI-26`.** Two sessions opening `OI-19` concurrently is the duplicate-key class WAVE-03 spent nine sessions on, and **the collision arrived inside the prompt asking for the gate that catches collisions**. `OI` has no allocation table; that is `ADR-074`'s second question.

- **`<<<<<<< HEAD` NEVER STOOD IN A COMMIT, AND THE GATE IS STILL WORTH WRITING.** `git log --all -G'^<<<<<<< ' -- '*.md' '*.ts' '*.mjs' '*.json'` returns nothing across every branch head and every `refs/pull/*/head`. It stood in a **working tree** during a merge. The record also disagrees with itself, 22 of 22 in [session 105](2026-08-20-session-105.md) and in STATE against 24 of 24 in session 106. **The consequence for `W8` is concrete: `CI-06/conflict-markers` must SEED its falsification marker rather than anchor on a historical one**, because there is no historical one, and `OI-21` is the record of what happens to a seed anchored on corpus state.

- **`CI-06/identifier-series` WAS BRIEFED AS CHEAP AND IS NOT.** Roughly forty `<PREFIX>-nn` series under `docs/`, `OI` alone at 220 occurrences with no registry, and **"definition site" is undefined**. It gets `ADR-074` and a survey before it gets a runner. **`SD-nn` is the case that cannot be solved by a document at all**: [ADR-026](../decisions/ADR-026.md) requires a `DELTA_MANIFEST` row for every `SD-` identifier appearing under `docs/`, so a reservation creates an obligation it cannot discharge.

- **`docs/sessions/2026-08-20-session-106.md` IS NOT ON `main`.** It is on `origin/claude/merit-futures-briefing-7auoor`, unmerged, so `main`'s session registry ends at 105 and **every reference to session 106 in WAVE-04 is deliberately unlinked**: `CI-06a` fails on a link to an absent document. 106 is spent, which is why this session is 107.

- **A STALE `node_modules` READS AS FIVE FAILING SUITES AND IT IS NOT A BREAK.** `pnpm install` turns 69 of 74 into 74 of 74 in under a second. Session 106 recorded it; it reproduced here exactly. **Every WAVE-04 prompt opens with `pnpm install` for this reason.**

- **TWO OF THIS SESSION'S OWN COMMITS FAILED GATES BEFORE THEY LANDED, AND BOTH WERE THE FAILURE CLASS THE PLAN IS ABOUT.** Writing a `gs_count` span **opener** inside prose *describing* a misplaced span opened a span that was never closed, which is `CI-06t` exactly; and three prose references to session 106 were links to a file that is not on this ref, which is `CI-06a`. **Neither was caught by reading the draft.** Both were caught by running the gates, which is the argument the plan makes in section 5 about preferring a gate to more care.

- **Files touched:** [WAVE-04](../plans/WAVE-04-fixture-backlog-and-gate-inventory.md), [INDEX](../INDEX.md), [ALLOCATION](../decisions/ALLOCATION.md), [STATE](../STATE.md), [sessions/README](README.md), this file.
