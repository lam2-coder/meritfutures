---
status: approved
depends_on: [README.md]
last_updated: 2026-08-28
---

## 2026-08-28 - Session 364: ADR-195's third component lands as arithmetic, and the reason that named a closed item is repaired in the file that carries it

<!--index: **I AM AT 1 OF 2 ON `P5-l` AND THE OTHER HALF IS NOT MINE TO TAKE.** [ADR-195](../decisions/ADR-195.md) section 9's assertion is TRANSCRIBED rather than approximated: `theThreeNumbers` gains a third component, the sum at [`liability.ts:303`](../../apps/admin/src/liability.ts) gains a third addend, and `M6-A-70` is [ADR-195](../decisions/ADR-195.md) section 4's table over `LT-06`, `LT-07` and `LT-09` rather than a paraphrase of it, including the two-term panel FAILING the approval case. **THE FIELD IS OPTIONAL AND THE ABSENCE IS THE HONEST SHAPE**: no column of `liability_snapshots` holds the obligation, re-derived at `0009` and `0049`, so an unsupplied term renders ABSENT with that reason and the total says it is INCOMPLETE. **A SECOND FINDING WAS FOUND BY READING RATHER THAN BY THE DISPATCH AND IT IS IN THIS FENCE**: `floatCoverage`'s absence said `GET /admin/wallet/reconciliation` "is registered by no route module in this tree" and that has gone FALSE, so the reason is repaired and BOUND to the route module by a test that reads it. **`P5-l`'s DATA-SOURCE HALF CANNOT LAND HERE AND THE MEASUREMENT SAYS WHY**: `LiabilityResponse` carries `open_liability_cents` and none of the other four figures `LiabilityHomeInput` reads, and `readLiability` is not one of `IMPLEMENTED_ADMIN_READS`' four. **NINE REGISTERED DEBTS COLLECTED WITH A DISPOSITION EACH.** Seven seeded defects, each watched failing and reverted from a byte-identical copy with the hash checked both ways. **227 files / 5,451 passed / 6 skipped** against a `main` baseline of **227 / 5,429 / 6** reproduced exactly at `087fa570` before a line changed; 33 of 33 gates, 16 of 16 invariants, `falsify` clean read at its output, typecheck 0, lint 0, format clean, `@merit/admin` **five routes**, unmoved. **No ADR number, no migration number, no `SD-nn`, no new export, no barrel leg, `figure.ts` not opened, `live-liability.ts` not opened, `apps/admin/src/app/**` not opened, `apps/api/**` and `packages/**` not touched at all.** -->

**Objective.** Land `P5-l`, the slice that owns [`page.ts`](../../apps/admin/src/page.ts), [`liability.ts`](../../apps/admin/src/liability.ts) and [`live-liability.ts`](../../apps/admin/src/live-liability.ts).

**I am at 1 of 2.** The rendering half of what remained is landed. The data-source half is measured and cannot be taken inside this fence, and section "What is left" says why in numbers rather than in a promise.

### The registered debts, collected first and each with a disposition

Nine items are registered against `P5-l` or against these three filenames across six waves. **The list is not the spec**; `P5-l`'s row is. Every disposition below was re-derived at source.

| # | Registered by | What | Disposition |
|---|---|---|---|
| 1 | [ADR-195](../decisions/ADR-195.md) section 6 row 4 and section 9, session **361** | `theThreeNumbers` gains a third component, the sum gains a third addend, and a case proving the sum is unchanged when `amount_cents` moves from the wallet term to the obligation term | **MINE, AND LANDED.** Section "Done" |
| 2 | Session **344** | `assertNamesNoSubject`'s uuid pattern is `\b`-anchored, so a subject id glued to a word character is not a match | **ALREADY TRUE.** Session 348 repaired it and `M6-A-55` asserts the widened refusal over a generated corpus. **The function is not touched by this diff** |
| 3 | Session **358** | Four [WAVE-06](../plans/WAVE-06-admin-console-transport.md) citations into [`page.ts`](../../apps/admin/src/page.ts), every one exactly thirty lines short | **SOMEBODY ELSE'S**, `WAVE-06`'s, and this dispatch says so. **Three are UNMOVED by this diff and one MOVED**: see "Citations this diff moved" |
| 4 | Session **296** | Eight of `liability.ts`'s exports were on no barrel leg | **ALREADY TRUE.** `W6-b` landed all 23 missing names. **This diff adds no exported name**, so no leg moves and [`index.ts`](../../apps/admin/src/index.ts) is not opened |
| 5 | Sessions **296** and **340**, and [`app/page.tsx`](../../apps/admin/src/app/page.tsx)'s `BLOCKED_ON` | `LiabilityResponse` carries `open_liability_cents` and none of the other four figures `LiabilityHomeInput` reads | **STILL SOMEBODY ELSE'S.** [`api/types.ts`](../../apps/admin/src/api/types.ts) is session 363's this wave and [API_CONTRACT](../architecture/API_CONTRACT.md) is frozen. **This is what blocks `P5-l`'s data-source half** |
| 6 | Sessions **340**, **345**, **353** | `AdminReadSource.readLiability` has no adapter, so no deployment composes the port | **SOMEBODY ELSE'S**, `apps/api/**`. Re-derived at reporting time: `IMPLEMENTED_ADMIN_READS` holds `listEvents`, `listFlags`, `readAccount` and `readIdentityGraph`, and `readLiability` is not among them |
| 7 | Session **296** | `GET /admin/wallet/reconciliation` is registered by no route module, so `P-M6-07`'s second ratio renders absent | **MINE, AND THE CLAIM HAS GONE FALSE.** Repaired. Section "Done" item 2 |
| 8 | [ADR-195](../decisions/ADR-195.md) premise 12 | [`wallet-withdrawals.ts:78`](../../apps/api/src/routes/wallet-withdrawals.ts) describes `LT-06`'s credit leg as `firm_treasury` and cites the document that refutes it | **STILL SOMEBODY ELSE'S AND STILL STANDING**, re-read at source. `apps/api` is not this fence |
| 9 | [ADR-195](../decisions/ADR-195.md) section 7 | [`M06:218`](../plans/M06-admin-ops-console.md)'s `#### INV-M6-10` heading is a SECOND definition site, and it rather than a gap is what holds `INV-M6` in `PENDING_SERIES` | **SOMEBODY ELSE'S.** `gates.mjs` and [M06](../plans/M06-admin-ops-console.md) are outside this fence |

### Done

- **[ADR-195](../decisions/ADR-195.md)'s ASSERTION IS TRANSCRIBED AND NOT APPROXIMATED.** `LiabilitySnapshot` gains `withdrawalsInFlight`, `ThreeNumbers.openLiabilityComponents` gains a third member, and the sum at [`liability.ts:303`](../../apps/admin/src/liability.ts) gains its third addend. `P-M6-01`'s panel prints FOUR readings where it printed three.
- **`M6-A-70` IS SECTION 4's TABLE RATHER THAN A PARAPHRASE OF IT.** `LT-06` moves `amount_cents` from the wallet term to the obligation term and the total does NOT move; `LT-07` falls the total by the amount; `LT-09` returns it to the wallet and leaves the total where it was. **And the two-term panel is asserted FAILING the approval case in the same suite**, because a conservation test that never shows the shape it rules out has proved that this arithmetic is self-consistent rather than that it is right.
- **THE TERM IS A MAGNITUDE AND THE LEDGER NET IS REFUSED.** `requireNonNegative` over the obligation, and a case that fires on it. [ADR-195](../decisions/ADR-195.md) section 4: a term written as the ledger net SUBTRACTS the obligation from a figure whose whole purpose is that it not fall wrongly.
- **THE FIELD IS OPTIONAL AND THE ABSENCE CARRIES THE MEASUREMENT.** Re-derived at [`0009`](../../packages/db/migrations/0009_ledger.sql) and [`0049`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql): that table holds `as_of`, `open_liability_cents`, `bounded_near_term_cents`, `remaining_ladder_exposure_cents`, `wallet_balances_cents`, `absorbed_corrections_cents`, `funded_accounts`, `id` and `computed_at`, and **not one of them is this obligation**. Unsupplied renders ABSENT with that reason and the total's own definition says it is **INCOMPLETE**. A zero would claim the obligation was measured and found empty.
- **THE RECORD CARRIES ITS OWN `source`, WHICH IS THE ONE PLACE THIS FILE DEPARTS FROM ITS OWN RULE.** Every other figure here takes its source from the function because the function reads exactly one table; this term is the figure that breaks that premise. [ADR-195](../decisions/ADR-195.md) clause 3 names TWO producers and chooses neither, and `INV-M6-04` requires the reader be told which one they are reading.
- **THE DELIBERATE SHARED READ IS UNTOUCHED.** `P-M6-07`'s `floatCents` is still `wallet_balances_cents` and the obligation does not enter it ([ADR-195](../decisions/ADR-195.md) clause 4), asserted in `M6-A-70`'s last case.
- **A REASON THAT NAMED A CLOSED ITEM IS REPAIRED, AND IT WAS FOUND BY READING RATHER THAN BY THE DISPATCH.** `floatCoverage`'s absence said `GET /admin/wallet/reconciliation` *"is registered by no route module in this tree"*. **That has gone false**: [`admin-wallet.ts:332`](../../apps/api/src/routes/admin-wallet.ts) declares `WALLET_RECONCILIATION_PATH`, [`admin-wallet.ts:1820`](../../apps/api/src/routes/admin-wallet.ts) serves it, and [`registry.ts`](../../apps/api/src/registry.ts) discovers every route module under `routes/`. **What blocks it is a BACKEND**: `useAdminWalletBackend` is on [`wiring.test.ts`](../../apps/api/test/wiring.test.ts)'s BLOCKED list and `reconcile` in particular is refused there for needing a join and an aggregate. The demand-projection half of the reason still holds, re-derived: no migration names a near-term withdrawal-demand projection. **The panel still renders absent**, and the reason is now BOUND to the route module by a case that reads it, which is [`surface.test.ts`](../../apps/admin/test/surface.test.ts)'s own device one suite over.

### Next

- **THE COLUMN, THE DELTA, THE FIELD AND THE ADAPTER, WHICH ARE ONE SLICE.** [ADR-195](../decisions/ADR-195.md) section 6 is that list and this session holds only row 4 of it. The renderer now has a shape to receive them: supplying `withdrawalsInFlight` is the whole of what the console side needs.
- **THE `NOT NULL` WINDOW IS STILL OPEN, RE-DERIVED RATHER THAN INHERITED.** The only `INSERT INTO liability_snapshots` in the tree is [`probe_reserve_coverage.sql:206`](../../scripts/db/probe_reserve_coverage.sql), and the only `INSERT INTO reserve_coverage_snapshots` is in the same file. [`0049`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql)'s precedent for `ADD COLUMN ... NOT NULL` with no default holds for exactly as long as that stays true.
- **`P5-l`'s DATA-SOURCE HALF**, which needs debts 5 and 6 above.
- **[`live-liability.ts`](../../apps/admin/src/live-liability.ts) AND `INV-M6-15`**, the finding below.

### Findings not in the dispatch

1. **THE LIVE FIGURE'S THREE TERMS DO NOT INCLUDE A SETTLEMENT, SO UNDER `INV-M6-15` IT DOES NOT FALL WHEN A SAME-DAY WITHDRAWAL'S CASH LEAVES.** [`live-liability.ts`](../../apps/admin/src/live-liability.ts) sums the last closed open liability, same-day `account_adjustments` at par (`SD-M6-09`) and the intraday movement implied by the feed. **`LT-07` is none of the three.** [ADR-195](../decisions/ADR-195.md) clause 2 makes `LT-07` the only external-leg posting that moves this panel, so a same-day settlement moves the authoritative figure at the next close and never moves the live one. **REPORTED AND NOT REPAIRED**: `SD-M6-09` fixes that term list, [ADR-195](../decisions/ADR-195.md) did not rule the live figure, and adding a fourth term to a tier-2 figure on a session's own judgement is the shape of change that should be ruled. The file is in this fence and is deliberately not opened.
2. **`GET /admin/wallet/reconciliation` IS SERVED BY A SECOND MODULE AND THE ADMIN-READS REGISTER SAYS SO IN ITS OWN COMMENT.** [`admin-reads.test.ts:502`](../../apps/api/test/admin-reads.test.ts) lists it as `unserved` by `ADMIN_READ_ENDPOINTS` while the comment above that assertion states that *"a second module serving a section 8 row is not a register entry: it is a route that is served"*. Both are true and reading only the list is how finding 7 above stayed stale for a wave. Not this fence.

### Citations this diff moved

**Derived after the last edit to each file.**

| Cited as | Cited by | On `main` | Now |
|---|---|---|---|
| `renderLiabilityHome:630` | [WAVE-06](../plans/WAVE-06-admin-console-transport.md) | `:660`, 30 short | **`:668`, now 38 short. MOVED BY THIS DIFF** |
| `assertNamesNoSubject:219` | [WAVE-06](../plans/WAVE-06-admin-console-transport.md) | `:249`, 30 short | `:249`, **unmoved** |
| `assertFloatIsNotReserve:263` | [WAVE-06](../plans/WAVE-06-admin-console-transport.md) | `:293`, 30 short | `:293`, **unmoved** |
| `buildLiabilityHome:391` | [WAVE-06](../plans/WAVE-06-admin-console-transport.md) | `:421`, 30 short | `:421`, **unmoved** |
| `liability.ts:156` | [ADR-195](../decisions/ADR-195.md) section 4 | `:156` | **`:255`** |
| `liability.ts:184` | [ADR-195](../decisions/ADR-195.md) section 6 row 4 | `:184` | **`:303`** |
| `liability.ts:534` | [ADR-195](../decisions/ADR-195.md) section 3, session 361 landmine 1 | `:534` | **`:709`** |
| `liability.ts:554` | [ADR-195](../decisions/ADR-195.md) clause 4 | `:554` | **`:729`** |

**`RI-16` holds at 16 of 16 across every one of these**, because each pointer's backticks carry a path and a line rather than an identifier the checker binds. **The four `WAVE-06` rows are session 358's registration and are not repaired here**, per this dispatch. The four `liability.ts` rows are [ADR-195](../decisions/ADR-195.md)'s and that entry is merged.

### What is left, and the measurement rather than the promise

**`P5-l`'s row asks for two things and one of them cannot be taken inside this fence.** *"Float and reserve render as visibly separate figures and the RCR is computed from reserve alone"* landed in session **296**. *"`apps/admin`'s arithmetic reads a real row"* is blocked in two places, both measured:

- **`LiabilityResponse` carries `open_liability_cents` and none of the other four figures the page reads**, so an adapter written today could fill one of `P-M6-01`'s components and neither `P-M6-02`, `AS-M6-04` nor `P-M6-10`. [`api/types.ts`](../../apps/admin/src/api/types.ts) is session 363's this wave and [API_CONTRACT](../architecture/API_CONTRACT.md) is frozen.
- **`readLiability` has no adapter.** `IMPLEMENTED_ADMIN_READS` holds four names and it is not one of them, so `setAdminReadSource` stays on the BLOCKED list. `apps/api/**` is not this fence.

**THE DEPENDENCY IS NAMED RATHER THAN ASSUMED, WHICH IS WHAT THIS DISPATCH ASKED FOR.** Session 363 was deciding whether the `LiabilityResponse` field lands now or waits for the column. **It had not landed when this branch was cut and `origin/main` was unmoved at `087fa570` when this was written.** What this session needs from that decision is one field; what it built in the meantime needs nothing from it, because the renderer's third term is optional by construction and its absence is a stated measurement rather than a hole.

### Blockers

None hit. The fence held: [`figure.ts`](../../apps/admin/src/figure.ts) was not opened (`P5-l`'s row does not name it, and no figure here needed an origin outside `P-M6-01`), [`index.ts`](../../apps/admin/src/index.ts) was not opened (no leg moved), [`live-liability.ts`](../../apps/admin/src/live-liability.ts) was not opened, `apps/admin/src/app/**` was read and not rewritten, and `apps/api/**`, `packages/**` and [API_CONTRACT](../architecture/API_CONTRACT.md) were not touched at all.

### Landmines

1. **`formatCents` RENDERS UNITS AND NOT CENTS, AND A PAGE-BYTES ASSERTION WRITTEN IN CENTS PASSES VACUOUSLY OR FAILS LOUDLY.** 750,000c prints as `7500.00`. A first draft of `M6-A-72` asserted `750000.00` and went red; the failure was the assertion rather than the code, and the same slip written as a `not.toContain` would have been a green test asserting nothing.
2. **AN UNSUPPLIED THIRD TERM DEFAULTED TO ZERO IS A CONFIDENT WRONG ANSWER WITH A SOURCE CITATION ATTACHED.** The seed that made the absent arm unreachable turned three cases red across two files. The panel is INCOMPLETE today rather than wrong, and it stays that way only while nothing posts `LT-06`.
3. **`theThreeNumbers` IS CALLED IN `describe` BODIES, SO A THROW INSIDE IT IS A COLLECTION ERROR AND NOT A NAMED FAILURE.** One seed reported `no tests` instead of a failing case. A seed that produces a collection error has been watched failing, but it has not been watched failing where a reader would look; that seed was retaken in a shape that names the cases.
4. **THE TOTAL STAYS `authoritative` WHILE A COMPONENT IS ABSENT, AND THAT IS LOAD-BEARING.** [`live-liability.ts`](../../apps/admin/src/live-liability.ts) calls `authoritative()` on it before the trust check. Marking the incomplete total indicative would suppress the live figure entirely, which is a far larger change than [ADR-195](../decisions/ADR-195.md) ruled. `M6-A-72`'s last case asserts the live arm still reads it.
5. **A REASON IS A CLAIM AND CLAIMS GO STALE SILENTLY.** Finding 7 above sat for a wave because nothing bound the sentence to the thing it described. The repair is not the new wording; it is the case that reads [`admin-wallet.ts`](../../apps/api/src/routes/admin-wallet.ts) and fails when the two stop agreeing.

### Verification

- `pnpm install` first. **`pnpm vitest run`: 227 files / 5,451 passed / 6 skipped**, against the `main` baseline of **227 / 5,429 / 6** reproduced exactly at `087fa570` before a line changed. `apps/admin` goes from 14 files / 366 cases to **14 / 387**, +21 cases and no new file.
- `node scripts/corpus/gates.mjs check`: **33 of 33**. `node packages/tooling/checks/repo-invariants.mjs`: **16 of 16**, `RI-16` included and re-run after the last edit to each file.
- `pnpm run typecheck` 0, `pnpm run lint` 0, `pnpm run format:check` clean.
- `pnpm --filter @merit/admin build`: **five routes**, unmoved. `/`, `/accounts/[accountId]`, `/feed`, `/flags`, `/identities/[identityId]`.
- `node scripts/corpus/falsify.mjs`: read at its OUTPUT rather than its exit code. *"All 33 gates pass clean and fail dirty, 80 scope case(s) hold, and 10 loader case(s) land on the side of the CI-03 boundary they name."*
- **`pnpm run verify` was NOT run**, per this dispatch.
- **SEVEN SEEDED DEFECTS, EACH WATCHED FAILING AND EACH REVERTED FROM A BYTE-IDENTICAL COPY WITH THE HASH CHECKED BOTH WAYS**: the third addend dropped from the sum (5 red), the term summed as the ledger net (5 red), the unsupplied term rendered as a zero figure (3 red, after the retake in landmine 3), the magnitude refusal removed (1 red), the third component dropped from the page panel (2 red), the `INCOMPLETE` clause deleted (2 red), and the stale reconciliation reason put back (1 red).

### Amended after the merge with `origin/main` at `1b68683`

**`main` MOVED WHILE THIS BRANCH WAS OPEN AND THE MERGE IS TAKEN RATHER THAN WAITED ON.** [#481](https://github.com/lam2-coder/meritfutures/pull/481), [#483](https://github.com/lam2-coder/meritfutures/pull/483) and [#484](https://github.com/lam2-coder/meritfutures/pull/484) landed. **Two conflicts, both in append-only registries and both resolved keep-both**: [STATE](../STATE.md) and this directory's [README](README.md), where session 365's entry and this one were appended to the same place. No code file conflicted.

**THE TWO CLAIMS THAT BLOCK `P5-l`'s REMAINING HALF WERE RE-DERIVED ON THE MERGED HEAD RATHER THAN CARRIED**: `LiabilityResponse` still carries neither `wallet_balances_cents` nor the in-flight obligation, and `IMPLEMENTED_ADMIN_READS` still holds `listEvents`, `listFlags`, `readAccount` and `readIdentityGraph` with `readLiability` absent. **`origin/main` was unmoved at `087fa570` when the body above was written and that sentence is dated rather than wrong.**

**[#481](https://github.com/lam2-coder/meritfutures/pull/481) REPOINTED FIVE CITATIONS AND NONE OF THEM WAS ONE OF SESSION 358's FOUR.** Re-read at source after the merge: [WAVE-06](../plans/WAVE-06-admin-console-transport.md) still cites `renderLiabilityHome:630`, `assertNamesNoSubject:219`, `assertFloatIsNotReserve:263` and `buildLiabilityHome:391`, so the table above stands unchanged and those four are still that registration's rather than this session's.

**EVERY GATE RE-RUN ON THE MERGED HEAD**, not inherited from before it: 33 of 33 gates, 16 of 16 invariants including `RI-16`, typecheck 0, lint 0, format clean, `falsify` clean read at its output, **227 files / 5,451 passed / 6 skipped** (unmoved, because everything `main` brought is documents), and `@merit/admin` **five routes**.

### Files touched

[`apps/admin/src/liability.ts`](../../apps/admin/src/liability.ts), [`apps/admin/src/page.ts`](../../apps/admin/src/page.ts), [`apps/admin/test/liability.test.ts`](../../apps/admin/test/liability.test.ts), [`apps/admin/test/page.test.ts`](../../apps/admin/test/page.test.ts), [STATE](../STATE.md) (append), this file and its [README](README.md) row.
