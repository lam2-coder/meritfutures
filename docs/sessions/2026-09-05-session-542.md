---
status: approved
depends_on: [README.md]
last_updated: 2026-09-05
---

## 2026-09-05 - Session 542: the record backlog reaches zero, and four of the eleven files it held turn out to be watched by nothing

<!--index: **THE ROW IS [ADR-351](../decisions/ADR-351.md), `proposed` and UNSIGNED.** [ADR-334](../decisions/ADR-334.md) opened `RI-37`'s shrink-only register at twenty merged migrations with no landing record; [ADR-335](../decisions/ADR-335.md) took six and [ADR-336](../decisions/ADR-336.md) three. **THIS ROW TOOK THE REMAINING ELEVEN, ALL OF THEM, AND `LANDING_RECORD_BACKLOG` IS NOW EMPTY**, so `RI-37` leg 1 is unconditional and a migration merged without a section is a finding on the commit that merges it. **THE LIVE TOTAL WAS DERIVED AND NOT INHERITED**: eleven, read off the register before the first edit, with six carrying an `E2 READ: MONEY PATH` header measured by `grep -c` over each file. `0059` appears in the manifest **zero times in any form**, re-derived and confirmed, the only one of the eleven for which that is true. **NOTHING IS RECONSTRUCTED.** One forward-only ladder with full object counts after every migration on disk, eleven databases stopping at each landing point, eleven more each missing one file, and a clone of the full set per perturbation, all against **PostgreSQL 16.13**. **THE HEADLINE FINDING IS COVERAGE AND IT IS LARGER THAN THE ONE [ADR-335](../decisions/ADR-335.md) REPORTED: FOUR OF THE ELEVEN ARE WATCHED BY NOTHING** under `scripts/db/`, established by removal and again by perturbation, and **two of the four open with `E2 READ: MONEY PATH`**: `0044`, whose idempotency index its own header calls the only thing between a retried settlement and a doubled credit, and `0074`, the base account cap. All four guards were then **watched being live** by hand, seven executed statements, and `0041` has no guard at all to watch. **`0037` MOVES NOT ONE OBJECT COUNT** because its supersession is a rename, and it is the only file of the eleven covered in both directions. **TWO REGISTER CONTRADICTIONS, IN OPPOSITE DIRECTIONS.** The manifest's `OI` table was superseded **as an allocator** in 2026-08-23 and its `State` column was never superseded at all, so nine cells read `Open` against closed items; each closure was verified at the artifact that closed it and never at [ALLOCATION](../decisions/ALLOCATION.md), and each row keeps its original words with the disposition beside it (`RI-14`). `OI-06` was the same defect from the far side, closed by `0051` and by manifest section 28 while the allocator still read `Open`. **A MEASURED CITATION ERROR REPAIRED**: `probe_wallet_debit_provenance.sql:53` said `0054` provisions seven scoped accounts and it provisions three, seven being the size of the whole chart of accounts. **`0054` IS NOT EDITED**, constitution `E2`. **`CI-06/retired-constraints` WENT RED AND THE REGISTER WAS RAISED RATHER THAN THE RECORD TRIMMED**, 7 to 10, the first raise any entry in it has taken, with the argument in the entry: the alternative was a landing record for a supersession forbidden to say what it superseded. **THE RAISE MOVED FIVE LINE CITATIONS IN `data-model-columns.mjs` BY ELEVEN AND `RI-15` CAUGHT ALL FIVE THE SAME RUN.** **TWELVE RESERVATION ROWS, `339` TO `350`, EACH CLAIMING NOTHING**, and the branch is measured for exactly one of them: `ADR-339` is on `claude/the-letter-that-heads-four-sections`, and `ADR-340` to `ADR-350` are on **no ref out of the 556** under `refs/remotes/origin/`, so those eleven rows name no branch rather than inventing one. **THE SESSION NUMBER IS DERIVED**: `530` was taken by the `339` branch, and row-to-session is `+191` at nine consecutive measured points. **ZERO LINES under `packages/db/migrations`; `scripts/db` is one file.** **EMPTYING THE REGISTER BROKE FOUR CASES IN `RI-37`'s OWN SUITE** and it is now EXPORTED as a seam, because two of the four assert leg 3 off a live entry and the alternative was deleting them; the size assertion inverted rather than disappearing and two discriminators were added beside it. **`origin/main` MERGED IN TWICE, AT `13169f90` AND `ca24913a`** (ADR-339, ADR-340, ADR-344): nine conflicts across the two, `ALLOCATION` resolved PER NUMBER with the REAL row winning, so main's `339`, `340` and `344` replace my placeholders and SIX remain; `341` to `343` had a placeholder on BOTH sides in different words and main's are taken. `data-model-columns.mjs` had BOTH SIDES wrong about the merged `gates.mjs` so all five citations were re-derived by grep, and the STATE seam was audited at 17 doubled blanks on each side, byte-identical. **THE BACKLOG FIGURES ARE DERIVED AT REPORTING TIME**: 75 migrations on disk, register size **0**, `RI-37` **0** findings, **11 of 11** cleared of which **6** money path, and `git diff --name-only origin/main -- packages/db/migrations` returns ZERO FILES. Counts at reporting time ON THE MERGED HEAD: suite **315 files / 7,702 passed / 10 skipped / 0 failed** against `origin/main` measured in its own worktree at **315 / 7,700 / 10 / 0**, a delta of **+2 cases in one existing file with SKIPS UNCHANGED and no file added**; invariants **35 of 35** and gates **33 of 33**; lint, `format:check` and typecheck clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `351`: clear what is left of `RI-37`'s landing-record backlog by measuring each migration rather than transcribing its ADR, and repair the register contradictions the survey found between [`DELTA_MANIFEST`](../../packages/db/DELTA_MANIFEST.md) section 16 and [ALLOCATION](../decisions/ALLOCATION.md).

**One objective.** This is documentation and register work, so a compounding session is permitted; but every item is a claim about a money-path migration, so nothing here is transcribed.

---

### 2. What was re-derived before the first edit

| Claim | Command | Held |
|---|---|---|
| the register holds eleven | the register itself, on the branch head `c0d15f0` | **YES.** `RI-37` passing on that tree is the proof: leg 1 reports an unrecorded migration and leg 3 reports a stale row |
| six of the eleven are money path | `grep -c 'E2 READ: MONEY PATH'` returning 1 on each of six files | **YES.** `0037`, `0044`, `0050`, `0059`, `0063`, `0074` |
| `0059` is in the manifest zero times in any form | `grep -c '0059' packages/db/DELTA_MANIFEST.md` | **YES.** Zero |
| `probe_wallet_debit_provenance.sql:53` says seven and `0054` provisions three | the line, then `0054:127-131` and `0054:163-168` | **YES.** Both the trigger's `INSERT` and the backfill carry three codes |
| `OI-06 (payout destinations)` is open in ALLOCATION and closed by `0051:8` | both files, plus manifest section 28's heading | **YES** |
| `main` ends at [ADR-338](../decisions/ADR-338.md) and `339` to `350` sit on siblings | `git ls-tree origin/main`, then `git cat-file -e` over all **556** refs under `refs/remotes/origin/` | **PARTLY.** `339` is on one branch; **`340` to `350` are on no ref this session can see** |
| PostgreSQL 16 available | `psql --version` -> **16.13** after `pg_ctlcluster 16 main start` | **YES** |

**Baseline on `c0d15f0` before the first edit:** invariants **35 of 35**; gates **33 of 33**; typecheck, lint and `format:check` clean; suite **312 files / 7,666 passed / 10 skipped / 0 failed**; `git status --porcelain` empty.

---

### 3. What landed

- **Eleven landing records, [`DELTA_MANIFEST`](../../packages/db/DELTA_MANIFEST.md) sections 49 to 59**, one per migration, in three commits: `0037`, `0059`, `0063`; then `0039`, `0040`, `0041`, `0043`; then `0044`, `0050`, `0073`, `0074`. Each carries object counts read from the catalogue at its own landing point, the file's re-application behaviour, its probe coverage established by counterfactual, and `assert_no_floats.sql`'s result. **Eleven register rows deleted in the same commits as the sections that close them**, which is leg 3's own rule.
- **`LANDING_RECORD_BACKLOG` is an empty `Map`**, kept rather than deleted so leg 3 survives, with the comment saying what the zero does and does not mean.
- **Nine `State` cells in the manifest's `OI` table** gain their live disposition beside their original words, and the superseding banner is widened to name the `State` column.
- **`OI-06 (payout destinations)`** repaired in [ALLOCATION](../decisions/ALLOCATION.md), same shape, other direction.
- **[`probe_wallet_debit_provenance.sql`](../../scripts/db/probe_wallet_debit_provenance.sql)'s seven-versus-three sentence**, corrected with the original kept beside it.
- **`RETIRED_REGISTER`'s manifest entry raised 7 to 10**, with the argument and the ADR number in the entry.
- **[ADR-351](../decisions/ADR-351.md)**, `proposed` and UNSIGNED, row `351`, and twelve reservation rows.
- **`LANDING_RECORD_BACKLOG` is exported as a test seam**, because emptying it broke four cases in `RI-37`'s own suite and two of them assert leg 3 off a live register entry. The alternative was deleting them. The size assertion inverted rather than disappearing, and two new cases were added beside it: one putting an entry back and watching leg 3 report it, one reaching the multi-site path a single-section fixture cannot.

- **`origin/main` merged in TWICE, at `13169f90` and then at `ca24913a`**, carrying [ADR-339](../decisions/ADR-339.md), [ADR-340](../decisions/ADR-340.md) and [ADR-344](../decisions/ADR-344.md). Nine conflicts across the two, enumerated with `git diff --name-only --diff-filter=U` before anything was staged, never `git add -u`. [ALLOCATION](../decisions/ALLOCATION.md) resolved **per number, real row wins**: main's rows for `339`, `340` and `344` replace three of my placeholders, `345` to `350` keep mine, one row per number. **`341` to `343` had a placeholder on BOTH sides in deliberately different words**, which is the convention working; main's are taken and neither claims anything. `docs/STATE.md`'s seam was audited rather than eyeballed: doubled blank lines number **17 on my tree and 17 on main and the two lists are byte-identical**, so neither merge introduced one. **`data-model-columns.mjs` was the one worth reading**: both sides had repointed the same five citations into `gates.mjs` and **both were wrong about the merged file**, so all five were re-derived by grepping it. The second merge brings [ADR-344](../decisions/ADR-344.md)'s changes to [`apps/worker/src/index.ts`](../../apps/worker/src/index.ts); **that barrel is byte-identical to main's here, 77 export lines each with the export sets diffing clean**, so no leg moved.

**Counts derived at reporting time off each runner's own last line, ON THE HEAD MERGED WITH `ca24913a`:** invariants **35 of 35**, gates **33 of 33**; typecheck, lint and `format:check` clean; suite **315 files / 7,702 passed / 10 skipped / 0 failed** against `origin/main` measured in its own worktree at **315 / 7,700 / 10 / 0**, a delta of **+2 cases in one existing file with SKIPS UNCHANGED and no file added**.

**The backlog figures are derived here rather than quoted:** **75** migrations on disk, `LANDING_RECORD_BACKLOG.size` **0**, `RI-37` returning **0** findings, **11 of 11** cleared by this row of which **6** carry an `E2 READ: MONEY PATH` header. **`git diff --name-only origin/main -- packages/db/migrations` returns ZERO FILES**, so no merged migration is edited (constitution `E2`); `scripts/db` shows exactly one file, [`probe_wallet_debit_provenance.sql`](../../scripts/db/probe_wallet_debit_provenance.sql), +20 -1.

---

### 4. What was not done, and why

- **No probe was written for any of the four uncovered migrations.** They are owed and named in [ADR-351](../decisions/ADR-351.md) section 9. This row's fence is `scripts/db`-zero except for one comment repair, and writing four probe blocks would have made a records row into a schema-verification row wearing the wrong title.
- **No migration is edited and none is written.** `git diff --stat origin/main -- packages/db/migrations` is **zero lines**, verified at reporting time.
- **None of the nine `OI` items is re-opened or re-ruled.** `OI-04`'s row says two `SECURITY DEFINER` functions and the catalogue holds three; that is written down and left for a reader.
- **No branch is named for `ADR-340` to `ADR-350`.** A reservation row inventing one would hand the dispatcher a resolution instruction pointing at the wrong place.

---

### 5. Blockers and landmines

- **FOUR MERGED MIGRATIONS ARE WATCHED BY NOTHING AND TWO ARE MONEY PATH.** `0044`'s fee-back idempotency index and `0074`'s closed vocabulary are the ones that matter; `0063`'s OTP write-once trigger is the third and `0041` the fourth. The only exercise any of them has ever had against a running database is the seven statements in [ADR-351](../decisions/ADR-351.md) section 3, taken by hand and gone the moment that section is read rather than run.
- **`0044`'s CONTROL IS JOINTLY HELD BY THE SCHEMA AND ONE WRITER.** A settlement retry writing a fee-back grant with `source_payout_request_id` left `NULL` falls outside the partial index entirely and doubles the credit with no refusal. The DDL cannot make that column mandatory. **This is the single most valuable probe the session found and did not write.**
- **`RETIRED_REGISTER` HAS NOW ADMITTED A RAISE.** The rule allowing it was written in the same commit that used it. A register that admits a raise whenever the raiser argues well has a different property from the one it advertises, and the founder should decide whether that is the right property.
- **`data-model-columns.mjs` HOLDS FIVE POINTERS INTO `gates.mjs`'s LINE NUMBERS.** Any edit to a comment nine thousand lines above them breaks all five. `RI-15` catches it every time, which is what makes the cost affordable rather than an argument that the pointers are safe.
- **`RI-37`'s SUITE NOW WRITES INTO THE CHECK'S OWN REGISTER.** `withRegisterEntry` seeds and restores in a `finally`, and the restore is what keeps a failing expectation from leaving the register dirty for the next case in the file. It is the only writer, and a second one would make the export a hazard rather than a seam.
- **SIX RESERVATION ROWS REMAIN AND NONE NAMES A BRANCH.** Twelve were written; `339`, `340` and `344` were superseded by their owners' real rows, and `341` to `343` were replaced by main's own placeholders for the same numbers. The remaining six, `345` to `350`, collide in [ALLOCATION](../decisions/ALLOCATION.md) by construction as their owners land, and each owner overwrites its own cell.
- **THE SESSION NUMBER `542` HOLDS AND THE RULE THAT PRODUCED IT DOES NOT.** Row `344` took session `532`, not `535`, so sessions are claimed in merge order rather than by row number and the `+191` mapping is falsified as a rule. Re-derived across every ref after the merge, the claimed set is `530`, `531`, `532`, `533`, `536`, `539` and this row's `542`, so **`542` collides with nobody**. The obvious instruction, take the next number above main's maximum, would have returned `532` and collided with row `344`.

---

### 6. Files touched

[`packages/db/DELTA_MANIFEST.md`](../../packages/db/DELTA_MANIFEST.md), [`packages/tooling/checks/repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs), [`scripts/corpus/gates.mjs`](../../scripts/corpus/gates.mjs), [`scripts/corpus/data-model-columns.mjs`](../../scripts/corpus/data-model-columns.mjs), [`scripts/db/probe_wallet_debit_provenance.sql`](../../scripts/db/probe_wallet_debit_provenance.sql), [`packages/db/test/migration-landing-record.test.ts`](../../packages/db/test/migration-landing-record.test.ts), [`docs/decisions/ADR-351.md`](../decisions/ADR-351.md) (new), [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md), [`docs/decisions/README.md`](../decisions/README.md), [`docs/STATE.md`](../STATE.md), this log and [`docs/sessions/README.md`](README.md).

---

### 7. Next

The founder's `E2` read of [ADR-351](../decisions/ADR-351.md) and the three questions its approval block names, the second of which is the `RETIRED_REGISTER` raise. Then, in priority order, the four probe blocks section 9 names: `0044`'s idempotency index first because the stake is a doubled credit on the settlement path, then `0063`'s OTP write-once trigger, then `0074`'s vocabulary, then `0041`. None of the four needs a migration and all four are `scripts/db` rows.
