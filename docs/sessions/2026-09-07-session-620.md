---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 620: the retirement is refused a third time, because a derived population inherited a hand-typed consequence column and five rows never ran the gate that saw it

<!--index: **THE ROW IS [ADR-425](../decisions/ADR-425.md), `proposed` and UNSIGNED. NON-MONEY DIFF, MONEY-ADJACENT SUBJECT. `E2` READ OWED on section 6's table and on the ruling section 10 says only a founder can make.** **THE RETIREMENT OF [`apps/api/src/events.ts`](../../apps/api/src/events.ts) IS REFUSED AND THE FILE IS 88 LINES AT THE END OF THIS ROW EXACTLY AS AT THE START.** **THE POPULATION WAS DERIVED ON THIS BASE AND NOT INHERITED**, and [ADR-422](../decisions/ADR-422.md)'s entire measurement REPRODUCES: `import` **1**/**1**, `path-literal` **9**/**3**, `enumeration` **20**/**18**, `config-glob` **4**/**3**, `prose` **72**/**72**, **21** delete-breaking files, **172** undecided sites, **171** unfoldable directory reads, **0** path aliases, over **2,491** files by `git ls-files`. **THE DELETION WAS WATCHED RED IN A THROWAWAY `git worktree`**, reproducing invariants **34 of 35**, **4 files failed / 337 passed**, **9 tests failed / 8,237 passed**, ADR-419's five cases and ADR-422's fifth red file. **AND THE THIRD LEG DECIDED IT**: `gates.mjs check` goes to **32 of 33** with **`CI-06a` at 72 findings**, a leg [ADR-414](../decisions/ADR-414.md), [ADR-418](../decisions/ADR-418.md), [ADR-419](../decisions/ADR-419.md) and ADR-422 all omitted, derived by grepping all four and finding **zero** mentions in each. **THE SEVENTH KIND IS A RELATIVE MARKDOWN LINK**, **72** of them over **30** documents, breaking on DELETE and on MOVE, every one classified `prose` by an instrument whose register says `prose` breaks on neither and that DATED sites are not a cost while **56** of the **72** are exactly those. **THE 30 ARE DISJOINT FROM THE 21, SO THE TRUE POPULATION IS 51 FILES**, and **62** links over **28** files are outside this fence under [ADR-386](../decisions/ADR-386.md) or in another row's. **THE IN-FENCE HALF AND A TOMBSTONE WERE BOTH PRICED AND REFUSED.** **33 of 33** gates, **35 of 35** invariants, **341 / 8,246 / 10 / 0**, unchanged from base. Sixteenth consecutive container with no repository. -->

**Branch `claude/retire-the-shim-for-real`, off `claude/wave-422s` at `bfd6c400`. Row [`425`](../decisions/ALLOCATION.md). The entry is [ADR-425](../decisions/ADR-425.md).**

### 1. The objective

Retire `apps/api/src/events.ts` on a population DERIVED by `packages/tooling/checks/dependants.mjs` rather than on a definition widened by the last injury, re-deriving the population on this base instead of inheriting ADR-422's, asking the instrument for more hops, and stopping rather than widening if any of it fell outside the fence.

### 2. Done

- **The population was re-derived on `bfd6c400` and ADR-422's every figure reproduced**, counted from the run's own site list rather than read off its summary.
- **The sixth kind was asked for and bounded.** The closure runs **4**, **6**, **15**, **15** at one to four hops, so it SATURATES at three, which ADR-422 did not report.
- **The deletion was watched RED in a throwaway `git worktree`**, never in the working tree, and the working tree was confirmed 88 lines and clean throughout.
- **`gates.mjs check` was added to the probe and it decided the row.** `32 of 33`, `CI-06a` with **72** findings, and `gates.mjs generate` inside the probe repaired none of them.
- **The seventh kind was found, measured and written up**: a relative markdown link, which the instrument classifies as `prose` and its register calls harmless.
- **[ADR-425](../decisions/ADR-425.md)** and the registries.

### 3. Refused, and not attempted

- **THE RETIREMENT.** The derived population is not fully inside the fence: **62** links over **28** files sit in dated records, in `STATE.md` which this row holds through `generate` only, and in a runbook another row holds.
- **THE IN-FENCE HALF**, moving `RI-35`'s register `site`, on [ADR-418](../decisions/ADR-418.md) section 5's recorded reason that half empties a live assertion while leaving the module undeletable. Section 6 of the entry makes that second clause larger, not smaller.
- **A TOMBSTONE FILE** with the export removed and the file kept, priced and refused as an arrangement no entry in this corpus authorises.
- **WEAKENING `CI-06a`.** The obvious way to make the deletion pass is a dated-record exemption on the only control that saw this. Not proposed.
- **`pnpm run verify` and `scripts/corpus/falsify.mjs`**, both forbidden by the row.
- **No pull request, no signature, no `E2` read waited for, no migration number taken or reserved.**

### 4. The probe, and what it cost

`git worktree add /home/user/probe HEAD`, the deletion and every leg run there, `git worktree remove --force` after. The real tree was `git status --porcelain` empty and `apps/api/src/events.ts` 88 lines before, during and after. Legs run under the deletion: `repo-invariants.mjs`, `pnpm vitest run`, `pnpm run typecheck` (**exit 2**, both errors in `apps/worker/test/replay-adapter.test.ts`), `pnpm run lint` (**exit 0**), `gates.mjs generate` and `gates.mjs check`. **The last two are the ones no previous row ran.**

### 5. Blockers

**None to wait on, and one to report.** The row ends `proposed`, UNSIGNED, with an `E2` read owed. **The retirement itself is blocked on a ruling no row may take**: whether a dated record's markdown link may be repaired when its TARGET is deleted. [ADR-386](../decisions/ADR-386.md) rules the `file:line` pointer case and its reason does not obviously carry, `8fe77a65` acted on one side of it inside a fence granted for it, and no entry states the rule.

### 6. Landmines

- **A DERIVED POPULATION CAN STILL CARRY A HAND-TYPED CONSEQUENCE.** `dependants.mjs` derives WHICH files reach a target and TYPES BY HAND whether each kind breaks on delete or move. The second half has exactly the property [ADR-422](../decisions/ADR-422.md) diagnosed in definitions, and this row is the injury.
- **A PROBE AS WIDE AS THE LAST INJURY IS THE SAME DEFECT AS A DEFINITION AS WIDE AS THE LAST INJURY.** Five rows priced one deletion with two legs each.
- **`gates.mjs` IS AT `scripts/corpus/gates.mjs`.** The dispatch named `packages/tooling/checks/gates.mjs` and the first invocation exited `MODULE_NOT_FOUND`. The location was derived from the root manifest.
- **`CI-06a` TRUNCATES ITS FINDING LIST AT 40.** Its count said 72 and it printed 40. The full population was taken from the tree and agreed with the count exactly.

### 7. Files touched

`docs/decisions/ADR-425.md` (new), `docs/decisions/ALLOCATION.md` (rows `425`, `426`, `427`; row `423` left as the reservation it is), `docs/sessions/2026-09-07-session-620.md` (new), and `docs/decisions/README.md`, `docs/sessions/README.md`, `docs/STATE.md`, `docs/INDEX.md` through `gates.mjs generate` only.

**NO FILE OUTSIDE THE REGISTRIES AND THIS ENTRY IS EDITED.** Read, run and cited by name, never written: `apps/api/src/events.ts`, `apps/api/test/event-placement.test.ts`, `apps/worker/test/replay-adapter.test.ts`, `packages/tooling/checks/absence-claims.mjs`, `packages/tooling/test/absence-claims.test.ts`, `packages/tooling/checks/dependants.mjs`, and `packages/tooling/test/repo-invariants.test.ts`, which ADR-422 says needs no edit and which this row did not give one.
