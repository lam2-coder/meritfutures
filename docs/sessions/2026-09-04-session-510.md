---
status: approved
depends_on: [README.md]
last_updated: 2026-09-04
---

## 2026-09-04 - Session 510: a close in no session, and the two arms get different answers

<!--index: **THE RULING IS [ADR-319](../decisions/ADR-319.md), `proposed` and UNSIGNED. MONEY-ADJACENT: it decides a customer-visible `date` on a terminal account row. The ONLY `src/` change is the port's DOCBLOCK, rewritten in EXACTLY the eleven lines it replaced so that no citation into that file moves; `tradingDay(): string` at [`admin-writes.ts:346`](../../apps/api/src/routes/admin-writes.ts) is byte-identical.** **BOTH NON-SESSION ARMS ARE RULED AND THEY ARE RULED DIFFERENTLY, WHICH IS HOW [ADR-042](../decisions/ADR-042.md) `F-4` SURVIVES THIS CONSUMER.** `not_a_session` takes the trading day of the **LATEST SESSION WHOSE CLOSE IS AT OR BEFORE** the instant, read off that row by comparing instants; `outside_coverage` takes **NO DAY and the close is REFUSED**. **THE DIRECTION IS THE CORPUS'S OWN AND NOT THIS SESSION'S**: [`M01:470`](../plans/M01-rules-engine.md)'s `R-06` is *"Every evaluation is against the last closed day and nothing more recent"*, a terminal date must not be in the future, the backward answer cannot be revised by a later calendar load while the forward one can, and a forward day would sit beyond every `rule_states` row the account will ever have ([`0015_rule_states.sql:225`](../../packages/db/migrations/0015_rule_states.sql)). **A THIRD CLAUSE THE ROW DID NOT ASK FOR AND THE MEASUREMENT FORCED**: a Saturday close writes Friday's day and so does an in-session Friday close, so **the BASIS is recorded in the audit `after` block** ([`admin-writes.ts:1303`](../../apps/api/src/routes/admin-writes.ts)) over a closed two-member vocabulary. Without it `F-4`'s substitution returns at the consumer while `tradingDayAt` keeps refusing it one package away, where no engine test can see it. **THE REFUSAL'S COST IS STATED AND THE THREE THINGS THAT BOUND IT ARE MEASURED, AND NONE OF THEM MAKES IT ZERO**: freeze still works and still stops money leaving ([`admin-writes.ts:1217`](../../apps/api/src/routes/admin-writes.ts), `now()` and never `tradingDay()`) but **requires an open risk flag** ([`admin-writes.ts:1193`](../../apps/api/src/routes/admin-writes.ts)); an estate on that arm has already stopped folding ([`adapter.ts:585`](../../apps/worker/src/batch/adapter.ts)); and the remedy is loading the calendar, which `F-4` already alarms on. **THE ROW'S OWN WORKER PREMISE WAS CHECKED AND IS FALSE.** `grep -rn "tradingCalendarLoads\|trading_calendar_loads" apps/worker/src/` returns **11** lines over **2** files, not zero; the adapter folds coverage at [`adapter.ts:431`](../../apps/worker/src/batch/adapter.ts) and refuses at [`:585`](../../apps/worker/src/batch/adapter.ts); the repair is named in the file itself, [`adapter.ts:386`](../../apps/worker/src/batch/adapter.ts), *"`ADR-273` finding 1, and `ADR-277` is the repair"*. **SO THE NIGHTLY FOLD IS NOT OUT OF COMPLIANCE THE DAY THIS IS SIGNED**, which is the opposite of what the row expected, and the two paths reached the same two answers independently. **THE FOUR-ARM CONSTRAINT IS DISPOSED OF AND THIS SERVES ONE ARM.** `closed_admin` is the only terminal status any `src/` line writes and [`admin-writes.ts:1330`](../../apps/api/src/routes/admin-writes.ts) the only write of a day; `breached` and `graduated` are **not instant lookups at all** and take the fold's own `rule_states.trading_day` ([`0015_rule_states.sql:44`](../../packages/db/migrations/0015_rule_states.sql)); **`closed_chargeback` owes a SEPARATE ruling that nobody holds**, because its actor is a PSP webhook with no operator to refuse to, so section 5's refusal does not transfer even though section 4's direction does. **THE PORT'S SIGNATURE IS RULED AND NOT CHANGED**: it must become `TradingDayAnchor`'s two-arm shape ([`adapter.ts:464`](../../apps/worker/src/batch/adapter.ts)), and changing it here breaks the unwired default at [`:443`](../../apps/api/src/routes/admin-writes.ts) and the caller at [`:1301`](../../apps/api/src/routes/admin-writes.ts), both outside the `:317`-`:347` this row was given, and needs an [API_CONTRACT](../architecture/API_CONTRACT.md) row this fence does not reach. **THREE FINDINGS REPORTED, NONE REPAIRED, ALL IN `apps/worker/**`**: [ADR-251](../decisions/ADR-251.md) seed **C** is **LIVE** at [`fills.ts:449`](../../apps/worker/src/detectors/fills.ts), `at.toISOString().slice(0, 10)`, stamping canary `fills.trading_day` at an instant [`fills.ts:433`](../../apps/worker/src/detectors/fills.ts) chose deliberately to be in no session, which is exactly where that entry predicted the paper-over would be writable; **four one-arm ports ask one three-arm question and `RI-27`'s census is BLIND to all four**, because its needle is the column and `grep -c` for `sessionCloseAt`, `session_close_at`, `closeAtMs` or `closeMs` over the four files returns **0, 0, 0, 0**; and `toCalendarSlice`'s coverage is still self-declared at [`adapter.ts:371`](../../apps/worker/src/batch/adapter.ts). **`useAdminWriteBackend` IS STILL BLOCKED ON `principal(request)` BEHIND THE C-08 PURCHASE and is now also behind the build slice section 9 specifies. Nothing is wired, no `BLOCKED` entry moved, and the wired count does not move.** **NO MIGRATION NUMBER, NO `RI-nn` AND NO `SD-nn` WERE TAKEN**, and `apps/worker/**` and `packages/rules-engine/**` were read closely and neither was written. Counts derived at reporting time, each command run separately: gates **33 of 33** after `generate`, invariants **32 of 32** off the runner's own last line, lint **0**, `format:check` clean, typecheck clean, **303 files / 7,398 passed / 6 skipped** against an `origin/main` baseline at `d963032` reproduced by stashing, of **303 / 7,398 / 6**: **a delta of zero, which is what a comment-only `.ts` diff must produce and is the reason it was measured rather than assumed.** `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. `node_modules` was absent on arrival and was restored by `pnpm install --frozen-lockfile`. -->

---

### 1. The objective

Row `319` dispatched a **RULING** and nothing else: say which trading day an operator close takes when the instant is in no exchange session, for **both** non-session arms, with the cost of a refusal stated if the answer is a refusal.

**The row's own third cell in [ALLOCATION](../decisions/ALLOCATION.md) governed over the dispatch prompt wherever they differed.** They differed in one place and it mattered: the prompt carries a finding about `apps/worker` that the row does not, and section 3 of [ADR-319](../decisions/ADR-319.md) measures it false.

---

### 2. What was read, and in what order

`CLAUDE.md`, then [STATE](../STATE.md), then row `319` in full, then the tail of [docs/sessions](README.md).

**Then the five primary sources the row named, every one opened:** the port's docblock and the whole close handler in `apps/api/src/routes/admin-writes.ts`; `tradingDayAt`, `TradingDayAt`, `tradingDayAtSequence` and `buildSessionCalendar` in `packages/rules-engine/src/calendar.ts`; `accounts_terminal_has_close_date` and the columns it governs in [`0007_accounts.sql`](../../packages/db/migrations/0007_accounts.sql); [ADR-042](../decisions/ADR-042.md) finding `F-4`; and [ADR-251](../decisions/ADR-251.md) with its three seeded paper-overs.

**Then past what the row named**, because two of its premises are citations and the row says to check rather than trust: `apps/worker/src/batch/adapter.ts` in full, `apps/worker/src/detectors/fills.ts`, `apps/worker/src/breaker/ports.ts`, `apps/worker/src/digests/ports.ts`, `apps/api/src/admin-source/liability.ts`, [`0015_rule_states.sql`](../../packages/db/migrations/0015_rule_states.sql), [M01](../plans/M01-rules-engine.md)'s rule table, `RI-27` and its register in `packages/tooling/checks/repo-invariants.mjs`, and [ADR-312](../decisions/ADR-312.md) sections 5 and 9.

**Not one `file:line` in the ADR was copied from the prompt, from [ADR-312](../decisions/ADR-312.md) or from a session log.** Every one was opened and the line read before it was written down.

---

### 3. Done

**[ADR-319](../decisions/ADR-319.md), fourteen sections, `proposed`, approval line UNSIGNED.** The ruling is summarised in the index block above and in the entry's own `Decision` line. What is worth carrying out of it into the next session is below.

**THE RULING IS TWO DIFFERENT ANSWERS AND THAT IS THE DELIVERABLE.** A single sentence covering both arms would have collapsed `outside_coverage` into `not_a_session` at the consumer, which is [ADR-251](../decisions/ADR-251.md)'s seed **B** ([`ADR-251.md:128`](../decisions/ADR-251.md)) arriving one package up from where that entry watched it go red.

**THE THREE MEASURED PAPER-OVERS ARE DISPOSED OF BY NAME RATHER THAN REDISCOVERED**, as the row required. **A** is the nearest-loaded-day costume: refused because "nearest" is unsigned distance and this ruling is directional, on `R-06`'s direction, and because the second query the ruling makes is a **different question** asked after discriminating on the arm rather than a fallback hidden inside `tradingDayAt`. **B** is the collapse: refused by giving the two arms observably different outcomes, and section 6 exists because the outcomes alone were not enough. **C** is the UTC calendar date: refused, and then **found live in `apps/worker`**, which is finding 1.

**THE PORT'S DOCBLOCK IS THE ONLY `src/` CHANGE AND IT WAS SIZED TO THE LINE.** The replacement paragraph occupies **exactly the eleven lines it replaced**, so `:346`, `:443`, `:451`, `:1160`, `:1193`, `:1217`, `:1281`, `:1301`, `:1303`, `:1318`, `:1328` and `:1330` all still hold what every citation in the corpus says they hold. **That was deliberate**: growing a comment by one line would have moved twelve pointers in this entry alone and an unknown number elsewhere, and `RI-15` and `RI-16` would have found them one at a time.

**`RI-27` WAS FOUND AND IS THE MECHANICAL CONTROL THIS SUBJECT ALREADY HAS.** Its own words are that an unregistered close-instant site *"is a candidate FOURTH statement of `R-06`'s selection and needs an ADR"* ([`repo-invariants.mjs:6512`](../../packages/tooling/checks/repo-invariants.mjs)). The build slice's new export lands in `packages/rules-engine/src/calendar.ts`, which is **already registered** with disposition `required-input` ([`repo-invariants.mjs:6475`](../../packages/tooling/checks/repo-invariants.mjs)); the disposition still holds and the row's `what` sentence goes stale. **The register does not grow**, because `admin-writes.ts` names no column.

---

### 4. Next

**THE BUILD SLICE IS SPECIFIED AND DISPATCHABLE FROM [ADR-319](../decisions/ADR-319.md) SECTION 9 ALONE.** It needs a fence over `apps/api/src/routes/admin-writes.ts`, `packages/rules-engine/src/calendar.ts`, [API_CONTRACT](../architecture/API_CONTRACT.md), `apps/api/test/**` and `RI-27`'s register. **It does not unblock the port**, which stays behind the C-08 purchase.

**A `closed_chargeback` RULING IS OWED AND IS ALLOCATED TO NOBODY.** It is the hardest of the four arms and the one an entry ruling all four would have got wrong by inattention.

**THE THREE FINDINGS WANT A ROW WHOSE FENCE REACHES `apps/worker/**`.** Finding 1 is the sharpest: seed **C** is live, and the instant it stamps was chosen to be in no session, so it is this row's own subject computed from a clock.

---

### 5. Blockers

**None for this row.** The stop condition is met.

**One standing, and it is not this row's:** `useAdminWriteBackend` needs `principal(request)`, which reduces to `setAdminSessionSource` and is behind the **C-08 SSO purchase**, which is founder-owed and untouched here.

---

### 6. Landmines

**A ROW'S OWN FINDING CAN BE STALE AND THIS ONE WAS.** The prompt states, as a measurement, that `apps/worker` reads `trading_calendar_loads` at zero occurrences. It reads it at **11**, and `ADR-277` repaired the gap between [ADR-251](../decisions/ADR-251.md) and now. **A session that had ruled from the premise would have written a paragraph telling somebody to go fix a fold that is already fixed.**

**A CITATION IN A MERGED ENTRY HAD DRIFTED.** [ADR-251](../decisions/ADR-251.md) section 2 cites `anchorCalendar` at `liability.ts:1605`; it is at [`liability.ts:1624`](../../apps/api/src/admin-source/liability.ts) now. **Repointed in [ADR-319](../decisions/ADR-319.md) section 3 rather than carried, and no claim of that entry changes.** Nothing in `liability.ts` was written by this session.

**A COMMENT REWRITE IS A LINE-NUMBER EVENT.** Eleven lines in, eleven lines out, checked by grep after the edit and before the commit. **The cheap version of this mistake costs somebody else a red `RI-16`.**

**`RI-27`'s NEEDLE IS THE COLUMN, WHICH MEANS THE CONSUMERS ARE INVISIBLE TO IT.** Four one-arm ports ask this exact question and the census sees none of them, because a port typed `string` names no column. **That is not a defect in the check and it is worth knowing before somebody concludes a green `RI-27` means the estate has one answer.**

---

### 7. Files touched

- `docs/decisions/ADR-319.md` (new)
- `docs/decisions/ALLOCATION.md` (row `319`)
- `docs/decisions/README.md`, `docs/sessions/README.md` (generated by `gates.mjs generate`)
- `docs/STATE.md` (the `adr_count` span, and the entry below)
- `docs/sessions/2026-09-04-session-510.md` (this file)
- `apps/api/src/routes/admin-writes.ts` (**the `tradingDay` docblock only**, eleven lines for eleven; the declaration is byte-identical)

**Nothing else.** No migration, no test, no workflow, no manifest, no `apps/worker/**`, no `packages/rules-engine/**`, no `docs/INDEX.md`.
