---
status: approved
depends_on: [README.md]
last_updated: 2026-08-30
---

## 2026-08-30 - Session 495: the lock is a port member, and the race that was a 500 is the 409 the contract names

<!--index: **THE RULING IS [`ADR-301`](../decisions/ADR-301.md), `proposed` and UNSIGNED, `E2` READ OWED AND NOT PERFORMED. MONEY PATH, AND THIS ONE HAS A `src/` DIFF: FOUR LINES.** [ADR-293](../decisions/ADR-293.md) section 3.5 IS BUILT, all four clauses. `PayoutTx` declares `lockScope(): Promise<void>` on `WithdrawalTx`'s and `CheckoutTx`'s two independent precedents, `postgresPayoutBackend` delegates it to `handle.lockScope()` in one line, and **`decidePayout` calls it as its FIRST STATEMENT**, above the identity gate. **NOTHING WAS RE-DECIDED**: [ADR-293](../decisions/ADR-293.md) ruled it and [ADR-295](../decisions/ADR-295.md) section 5A wrote the checklist. **THE STOP CONDITION IS A RACE AND BOTH TRANSCRIPTS ARE IN THE ENTRY**: two concurrent requests through the real route, over a fixture that models the partial unique index and the row lock and nothing else. **RED before the four lines, `expected [ 200, 500 ] to not include 500`**, the loser reaching the insert, taking a unique violation, and `unwiredOrThrow` rethrowing it because a unique violation is not a `PayoutBackendUnwired`; **GREEN after, one 200 and one 409, ONE committed row and ONE insert attempted**, the last being the assertion that the BACKSTOP did not do the lock's work. **`transact` STILL TAKES NO LOCK** and the case asserting it is now a REFUSAL with [ADR-293](../decisions/ADR-293.md) section 3.4's reason rather than a placeholder for an open question. **TWO OF THE FOUR DISPATCHED ITEMS HAD ALREADY BEEN TAKEN BY [ADR-295](../decisions/ADR-295.md) AND THAT IS REPORTED RATHER THAN QUIETLY RE-TAKEN**: the `transact` docblock no longer stated the lock as slice 6's OPEN QUESTION, and item **D2** was already repaired inside this fence; what was still false is the paragraph recording the ruled member as NOT BUILT, which THIS ROW'S OWN DIFF falsified. **THE ONE DOCUMENT STILL ASSERTING THE D2 SENTENCE AS TRUE IS [ADR-291](../decisions/ADR-291.md) CLAUSE 4, AND IT IS OUT OF FENCE.** **FOUR SEEDS WATCHED RED AND RESTORED**, one of them leaving the race case GREEN, which is stated rather than hidden because it is why the placement has its own cases. **`db-recorder.ts` GAINS A `lockScope` VERB**, [ADR-295](../decisions/ADR-295.md) section 5A item 4, recording the ACT and no key because the accessor takes no argument. **`RI-35`, RESERVED TO THIS ROW BY NAME, IS LEFT FREE**: the real defect is a CORPUS GATE and not an invariant, it belongs beside `CI-06f`, and `scripts/corpus/` is out of fence. Reported for the fourth time, not built. **NO `start.ts`, NO `packages/db` EDIT, NO MIGRATION WRITTEN OR NUMBER RESERVED, NO DOOR WIDENED, NO GATE WEAKENED, NO FENCE WIDENED, NO STUB, NO CAST, NO SYNTHESISED DEFAULT, NO FLOAT, NOTHING SIGNED. THE WIRED COUNTS DO NOT MOVE: TEN WIRED OF TWENTY-FOUR DECLARED, FOURTEEN BLOCKED.** Counts derived at reporting time, each command run separately and every exit code read without a pipe: **33 of 33** gates after `generate`, **32 of 32** invariants off the runner's own last line, **298 files / 7,264 passed / 6 skipped** against a baseline of **298 / 7,258 / 6** taken on this tree before the first edit. `typecheck` 0, `lint` 0, `format:check` clean. **`pnpm run verify` was NOT run**, forbidden by the row, and `falsify.mjs` was never invoked by any path. **THE BRANCH IS PUSHED AND NO PULL REQUEST WAS OPENED BY THIS SESSION.** -->

---

### 1. The objective

**[ALLOCATION](../decisions/ALLOCATION.md) row `301`: build [ADR-293](../decisions/ADR-293.md) section 4 item B, the four lines that make its lock ruling true.** One objective, money path, [ADR-003](../decisions/ADR-003.md) strict. [ADR-295](../decisions/ADR-295.md) built slice 6's approval branch and explicitly did NOT build the lock, because two of the four clauses are lines of [`routes/payouts.ts`](../../apps/api/src/routes/payouts.ts) its fence granted for seven named lines only.

### 2. What was read before anything was written

Row `301` in full, then `CLAUDE.md`, [STATE](../STATE.md) and the tail of [`docs/sessions/`](README.md); then [ADR-293](../decisions/ADR-293.md) sections 3 and 4 entire, [ADR-295](../decisions/ADR-295.md) sections 2.3, 3 and 5, and [ADR-291](../decisions/ADR-291.md)'s clause 4. **Then every claim was re-derived at source rather than carried**: `grep -n lockScope apps/api/src/routes/payouts.ts` returned nothing and exit code `1` on `origin/main` before a line was written; `WithdrawalTx.lockScope` and its docblock; `CheckoutTx.lockScope` and *"IT TAKES NO ARGUMENT, AND THAT IS THE WHOLE OF ITS SAFETY"*; `databaseWithdrawalBackend`'s exposure of the member and `decideWithdrawal`'s call of it as its first statement; `ScopedTx.lockScope`'s declaration AND its implementation, which is a locking select over `identities` that hands back the row it locked; `decidePayout` end to end; `unwiredOrThrow`; and the `usePayoutBackend` blocked reason.

### 3. Done

- **The four lines.** `PayoutTx.lockScope(): Promise<void>` as the port's FIRST member; the one-line delegation in `postgresPayoutBackend`; `await tx.lockScope();` as `decidePayout`'s first statement; and the `transact` docblock corrected where it recorded the member as owed.
- **Three cases in [`payouts.test.ts`](../../apps/api/test/payouts.test.ts)**, the race and the two placement assertions, **watched RED before the source moved and GREEN after**.
- **Three cases in [`payout-backend.test.ts`](../../apps/api/test/payout-backend.test.ts)** on the delegation, each seeded RED and restored, and the `transact` no-lock case retargeted from a gap into a refusal.
- **A `lockScope` verb on [`db-recorder.ts`](../../apps/api/test/db-recorder.ts)**, which answers a ROW because the accessor does.
- **[ADR-301](../decisions/ADR-301.md)**, nine sections, `proposed`, UNSIGNED.
- **[ALLOCATION](../decisions/ALLOCATION.md) row `301`**, rewritten from a reservation into the landed record, **[STATE](../STATE.md)** and this log.

### 4. Next

**[ADR-287](../decisions/ADR-287.md) slices 4 and 5**, then slice 7, then the founder-gated slice 9, whose remaining precondition is now the `E2` read alone: the second one [ADR-295](../decisions/ADR-295.md) gave it is discharged. [ADR-287](../decisions/ADR-287.md)'s four stale F2 citations and the corpus gate are still owed and still out of every allocated fence. `RI-32`, `RI-35` and `RI-38` are free.

### 5. Blockers

**None, and nothing was reported blocked.** The fence named [`routes/payouts.ts`](../../apps/api/src/routes/payouts.ts) IN FULL this time, which is what the two previous rows on this file lacked, and every line this row owed was inside it. [ADR-301](../decisions/ADR-301.md) ships `proposed` with an UNSIGNED approval line and merging is not signing.

### 6. Landmines

- **THE DISPATCH FOR THIS ROW DESCRIBED A TREE ONE COMMIT OLDER THAN THE ONE IT RAN ON.** Two of its four named items were already taken. **A row brief is a specification and not an observation**, and the first act of a session that takes one should be to check each named edit at source. Both were checked and both were found already done.
- **THE RACE CASE DOES NOT PROVE THE PLACEMENT.** Moving `await tx.lockScope()` below the identity gate leaves it GREEN, because a lock taken after the identity gate still serialises the read-then-write. **The placement is a separate property and rests on two separate cases**, one on the trace and one on the source text. A session that deletes those two keeps a green suite and loses the ruling.
- **THE FIXTURE'S BARRIER IS "BOTH INSIDE `transact`" AND IT HAD TO BE.** A barrier on the READ is unreachable under the lock and would HANG rather than go green, which is a fixture deciding the answer. The next concurrency case in this file inherits that constraint and it is not obvious from the outside.
- **THE RECORDER'S `lockScope` ANSWERS A ROW, AND THE DEFAULT IS STILL `undefined`.** A suite that wants the discard property asserted has to set `locks` explicitly; against the default the case passes over nothing.
- **[ADR-291](../decisions/ADR-291.md) CLAUSE 4 STILL SAYS `databaseWithdrawalBackend` CALLS `tx.lockScope()` FIRST.** Four documents now record that sentence as retired and one still asserts it. **A grep for the sentence returns both kinds and they read the same at a glance.**
- **EVERY PAYOUT REQUEST NOW HOLDS A ROW LOCK ON THE CALLER'S IDENTITY FOR THE LIFE OF ITS TRANSACTION, INCLUDING REQUESTS ABOUT TO BE REFUSED.** That is the cost [ADR-293](../decisions/ADR-293.md) section 3.5 measured and accepted. Nothing reaches it today because `start.ts` does not call `usePayoutBackend`; slice 9 is the day it starts being paid.

### 7. Fence

**Held.** [`apps/api/src/routes/payouts.ts`](../../apps/api/src/routes/payouts.ts), [`apps/api/src/payout-backend.ts`](../../apps/api/src/payout-backend.ts), the tests of each plus [`db-recorder.ts`](../../apps/api/test/db-recorder.ts) and [`wiring.test.ts`](../../apps/api/test/wiring.test.ts)'s blocked reason, [`docs/decisions/ADR-301.md`](../decisions/ADR-301.md), [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md), [`docs/STATE.md`](../STATE.md), this log, and the two GENERATED registry spans in [`docs/decisions/README.md`](../decisions/README.md) and [`docs/sessions/README.md`](README.md). **`packages/**` IS UNTOUCHED**, measured rather than intended: `git diff origin/main --stat -- packages` is EMPTY. **[`docs/INDEX.md`](../INDEX.md) IS NOT TOUCHED**, on [ADR-295](../decisions/ADR-295.md) section 8's reading: an ADR is a registry entry, needs no INDEX row, and no document's status or title changes here.
