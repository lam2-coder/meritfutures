## 2026-08-16 - Session 31: FOLD-01 session 3, migration `0029` (money path)

- **Done:** [`0029_phone_identity_and_auth.sql`](../../packages/db/migrations/0029_phone_identity_and_auth.sql), [ADR-039](../decisions/ADR-039.md)'s nine changes, with an `E2 READ: MONEY PATH` header naming five things and why. Three new tables ([`identity_phones`](../architecture/data-model/identity_phones.md), [`phone_change_requests`](../architecture/data-model/phone_change_requests.md), [`otp_send_budget`](../architecture/data-model/otp_send_budget.md)) and amended columns on six existing ones. **Delta numbers allocated here rather than in the plan**, per [FOLD-01](../plans/FOLD-01-phone-identity.md) section 4: `SD-M19-05/06/07`, `SD-M16-04/05/06/07`, `SD-M4-04`, `U-07`. Nine design records written or amended, [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md) section 4a and section 14 added, the `0029` allocation row corrected in the same commit that created the file. **The full 29-file set applies forward-only from empty against PostgreSQL 16, re-apply rejected, 99 tables / 340 indexes / 381 checks / 6 triggers, and 48 constraint assertions executed against the installed schema, leading with the success case.** All 12 gates green, `falsify.mjs` green.
- **Next:** **The founder's E2 read on `0029`.** Then FOLD-01 session 4 (SECURITY, M19, M07, M16: the invariants and the adversarial scenarios). `OI-07` (the committed probe) is one commit and unowned.
- **Blockers:** None. Nothing merges before the E2 read.
- **Landmines:**
  - **The unique index on `identity_phones.phone_hash` is deliberately absent and a reader will want to add it.** ADR-039 splits the hard link: identity to phone is a constraint, phone to identity is a flag. Making `phone_hash` unique refuses the innocent owner of a recycled number at the door, before the portability check that exists to rescue them can run. **A probe asserts that the second verification is PERMITTED**, so the ruling now fails loudly if someone "completes the pair".
  - **`otp_send_budget.state` has no `paused`, and `plan_breaker_state`, whose pattern it copies, does.** One word restores fail-closed and turns the SMS-bill control into a denial of service on registration. The founder ruled the opposite and the reason is in the column comment.
  - **`notifications.class` is NOT widened to `pre_identity_auth` and that is structural.** `notifications.identity_id` is `NOT NULL`, so a pre-identity message cannot be a `notifications` row. A later session "completing the pair" adds a value no row can legally carry.
  - **`OI-08`: the NO-FLOATS `DO` block is positional.** It lives in `0027` and cannot see `0028` or `0029`. A future migration adding a `numeric` money column sails past the guard the corpus believes protects it. Checked by hand this session; the fix belongs in the install job, not in a money-path migration.
  - **The tally of hand-maintained counts is itself double-booked.** Two findings claim "eighth" and two claim "ninth". No ordinal is claimed for this session's, and the reason is written into the manifest: a running total with no allocation table is the ADR-034 race one registry over.
- **Files touched:** `packages/db/migrations/0029_phone_identity_and_auth.sql` (new), `packages/db/DELTA_MANIFEST.md`, `docs/architecture/data-model/` (3 new records, 6 amended, README, `.map.json`), `docs/decisions/ALLOCATION.md`, `docs/{STATE,INDEX}.md` (spans plus two prose figures this diff falsified), `docs/sessions/`.

### What the plan got wrong, and it is small

[FOLD-01 section 6.2](../plans/FOLD-01-phone-identity.md) says the fold amends "**five** existing tables". It is **six**, and all six are rows in the plan's own section 4 table: `otp_challenges`, `sessions`, `contact_channels`, `identity_signals`, `notification_kinds`, `kyc_verifications`. Recorded rather than quietly corrected, because the plan is what the fold is scored against and sessions 4 to 6 read it.

### The one thing the scope fence cost, named rather than dropped

[FOLD-01](../plans/FOLD-01-phone-identity.md)'s definition of done item 5 names `scripts/db/probe_phone_identity.sql`. The 48 assertions exist and were watched passing, but they ran **ad hoc** and are not wired into [`corpus.yml`](../../.github/workflows/corpus.yml), because the session's stop condition was the migration, its data-model files and its manifest rows. **That is `OI-07`, open**, and it is the object section 13 of the manifest names by name: a probe that ships beside a fix and never runs again is the same thing as the golden test that was missing. One commit, and it needs an owner.
## 2026-08-16 - Session 31: FOLD-02 session 3, migrations 0030 and 0031

- **Done:** The payout enforcement window and the identity restriction episode land as DDL. [`0030_payout_hold_enum.sql`](../../packages/db/migrations/0030_payout_hold_enum.sql) is one statement with **no `BEGIN`/`COMMIT`**; [`0031_payout_hold_and_identity_restriction.sql`](../../packages/db/migrations/0031_payout_hold_and_identity_restriction.sql) carries six changes: the five hold columns and `payout_requests_hold_is_complete`; **both** `SD-09` predicates dropped and re-created under their own names with `held_pending_review` inside them; `payout_requests_hold_expiry_idx`; `wallet_withdrawals_live_freeze_blocks_settlement` and the re-created open index; `identity_restriction_episodes` with its partial unique and two completeness `CHECK`s; and the replacement `COMMENT ON TABLE payout_requests` carrying the amended zero-denial sentence. **All 30 files apply forward-only from empty against PostgreSQL 16, zero errors: 97 tables, 331 indexes, 351 check constraints, 6 triggers.** [`probe_payout_hold.sql`](../../scripts/db/probe_payout_hold.sql) is **11 / 11**, six success cases first. Records folded into [DATA_MODEL](../architecture/data-model/README.md) (`payout_requests`, `wallet_withdrawals`, the new `identity_restriction_episodes` record and its README row) and [DELTA_MANIFEST section 14](../../packages/db/DELTA_MANIFEST.md). All 12 gates pass clean and fail dirty.
- **Next:** FOLD-02 session 4. **Nothing in this branch touches `0029` or `0032`**, which are FOLD-01's and S-E's. The founder's E2 read on `0030` and `0031` is the remaining work on this branch and it is not engineering.
- **Blockers:** None.
- **Landmines:**
  - **Two files because one is impossible, and it is proven by execution.** A combined form was written and run: `ERROR: unsafe use of new value "held_pending_review" of enum type payout_status`, `HINT: New enum values must be committed before they can be used`, exit 3. The split form then applied cleanly to the same database. A later session that "tidies" the two into one produces a migration that **cannot run**, and `0030`'s header says so at length for exactly that reader.
  - **`0030` deliberately has no transaction block**, unlike every other file in the set. The install job applies one file per `psql -f` invocation, so it runs in autocommit and the value is durable before `0031` reads it. Adding `BEGIN`/`COMMIT` to make it look like its neighbours breaks it.
  - **The counterfactual's first harness reported the wrong verdict.** It was written `if psql ... | tee out.txt; then`, which tests **`tee`**'s exit status and never `psql`'s, so the failing migration scored as a pass. The migration was correct throughout; only the thing measuring it was broken. Capture `rc=$?` from `psql` directly.
  - **Both `SD-09` predicates moved together, in one file, adjacent.** `0010` wrote them that way on purpose and this is the future change it was written for. Widening one and not the other is the C-02 defect committed a second time by the migration citing it.
  - **The halt on `wallet_withdrawals` is a `CHECK` and not a status**, because it is orthogonal to the rail state. Collapsing it into `wallet_withdrawal_status` is `SD-M5-06`'s named mistake. The open index is re-created under the same name and the same predicate so a halted row **stays visible** to the sweep.
  - **`restricted` was not renamed to `suspended`** ([ADR-041](../decisions/ADR-041.md)). The state already exists, is already reversible, and is already on the trader's `GET /me`. What was missing was the binding surface and the episode record.
  - **The probe needed five fixture corrections before it ran**, every one a constraint this set already enforces: `users.email`, a severity-5 `risk_flags` row needing `sla_due_at`, `plan_versions` needing `public_slug` / `created_by` / a publish transition, `purchases` needing its four money columns and a PSP reference, and a funded `accounts` row needing `funded_on`. Read the DDL rather than guessing at it.
  - **Neither file is idempotent and neither pretends to be.** Re-applying `0031` fails at `column "held_at" ... already exists` and `0030` at `enum label ... already exists`, both exit 3. That is the correct behaviour: a file that tolerates a second application cannot tell a fresh database from a corrupted one.
- **Files touched:** `packages/db/migrations/{0030_payout_hold_enum,0031_payout_hold_and_identity_restriction}.sql` (new), `scripts/db/probe_payout_hold.sql` (new), `docs/architecture/data-model/identity_restriction_episodes.md` (new), `docs/architecture/data-model/{README,payout_requests,wallet_withdrawals}.md`, `packages/db/DELTA_MANIFEST.md`, `docs/{INDEX,STATE}.md`, `docs/sessions/{README,2026-08-16-session-31}.md`.
## 2026-08-16 - Session 31: `0032` carries ADR-042's F-1 to F-4 (S-E session 2, money path)

- **Done:** [`0032_trading_calendar_holidays_coverage_revisions.sql`](../../packages/db/migrations/0032_trading_calendar_holidays_coverage_revisions.sql) with its `E2 READ: MONEY PATH` header, carrying all four of [ADR-042](../decisions/ADR-042.md)'s findings, **which are one migration or none**. **F-1**: session columns nullable, `trading_calendar_session_ordered` dropped and rewritten under its own name, `trading_calendar_holiday_has_no_session` added, so a holiday is a **positive fact rather than an absence**. **F-2**: `trading_calendar_revisions`, append-only, prior image derived as `to_jsonb(OLD)` rather than a listed column set. **F-3**: the latest-close semantics as a `COMMENT ON COLUMN` plus a `CHECK` that a half day records something in `notes`; **the symbol dimension is rejected**, as ruled. **F-4**: `trading_calendar_loads`, append-only, so a day outside coverage is **unknown** rather than an unbroken holiday. Records: [DELTA_MANIFEST section 14](../../packages/db/DELTA_MANIFEST.md) and `OI-06`, three [DATA_MODEL](../architecture/data-model/README.md) entries, and this log. **Verified against a real PostgreSQL 16**: the set applies forward-only from empty with `ON_ERROR_STOP=1`, zero errors, **98 tables, 332 indexes, 359 check constraints, 6 triggers**, and **36 perturbations pass, each checked by message rather than by exception class, each group led by its positive control**.
- **Next:** **S-E3**, the source file: the CME holiday list and early-close list with per-group CT times, its provenance block, the retrieved artifact and its SHA-256, and the generated sessions committed. Money path, fresh session. Then S-E4 (the loader and its six proofs, plus `scripts/db/probe_trading_calendar.sql`) and S-E5 (`CI-06m`, the import ban, the SQL shape check, the fixture derivation).
- **Blockers:** **`OI-06` needs a founder ruling before S-E4 writes the loader**, because it decides whether the loader is the only thing that can move a calendar row. Nothing else is blocked; S-E3 does not depend on it.
- **Landmines:**
  - **Dropping `NOT NULL` from a column named inside an existing `CHECK` does not make the `CHECK` null-safe. It makes it VACUOUS on exactly the rows that now carry `NULL`**, because a `CHECK` that evaluates to `NULL` **passes**. This is [ADR-035](../decisions/ADR-035.md)'s `array_length` defect arriving a second time through a different door. It was **executed rather than reasoned about**: a scratch schema carrying the weak reading, ruled `CHECK` and all, **committed a holiday with a fabricated close instant and no open**, in a containment table, on a day the exchange is shut. Any future session widening a column inside a constraint must re-derive the constraint, not just the column.
  - **F-1's ruled `CHECK (is_holiday = (session_open_at IS NULL))` names ONE of the two session columns.** On its own it leaves a holiday free to carry a `session_close_at`. The pairing inside the rewritten ordering constraint is what closes that, and a later session that "simplifies" the ordering constraint back to `close > open` reopens it silently.
  - **`0026`'s closing `ALTER DEFAULT PRIVILEGES` grants the application role full DML on every table a future migration creates.** So **append-only is false by default** for any new table until the migration that creates it revokes. `0032` is the first migration to hit this. The rule is now written into [DATA_MODEL section 1](../architecture/data-model/README.md) rather than left to be rediscovered, and the append-only set is **twenty** tables across two migrations.
  - **`CI-06g`'s span parser read the name as `[a-z_]+`, with no digits**, so the `e2_files` span was **not a span at all**: invisible to the checker and to the generator, green while the number was wrong. INDEX said 18 files carry an `E2 READ` header against 19 on disk, **in the same sentence calling it a generated span**. Found only because `0032` became the nineteenth. Fixed to `[a-z0-9_]+` and **watched failing on a seeded violation and passing when restored**. A span that cannot be parsed is worse than a hand-maintained number, because it reads as checked. **Any new span name with a digit was silently dead until today**, and `e2_files` was the only one.
  - **Prose that quotes the literal span syntax becomes a span.** Writing the `e2_files` marker out in full inside a STATE paragraph made `CI-06g` parse the rest of the section as span content and fail. Fenced blocks are masked; inline backticks are not. Describe a span by name, never by writing its delimiters.
  - **STATE said "zero `interval` in any case" across the migration set and a case-insensitive grep now returns five**, all the English word in `0032`'s comments. **Zero `interval` arithmetic is the property; zero occurrences of the letters is not.** ADR-042's SQL shape check must **parse the construct**, or it will fire on a comment and still miss `date + '5 days'::interval`. The sentence also said "merged migrations" beside a span that counts the directory, and `0032` is not merged. Both corrected.
  - **The 36 perturbations are not a committed probe.** `scripts/db/probe_trading_calendar.sql` is S-E4's deliverable beside the loader it exists to test. A run that is recorded and never wired is the shape RIDER 2 in [`corpus.yml`](../../.github/workflows/corpus.yml) was added to end.
  - **Two perturbations were written expecting the wrong constraint and the schema corrected the expectation.** Checking by message is what made that visible; an exception-class check would have scored both green and proven nothing about which constraint is load-bearing.
  - **`dependent_row_count` is one column more than ADR-042 lists**, and it is flagged in the migration header for the founder to reject if the ruling is read as five fields exactly. Without it `incident_ref` is nullable with nothing saying when, and "an incident, not a data edit" stays a convention.
  - **That `prior_row.trading_day` equals the row's `trading_day` is NOT a `CHECK`.** The comparison needs a jsonb rendering of a date, which is `STABLE` rather than `IMMUTABLE`, and PostgreSQL rejects a non-immutable expression in a `CHECK`. It belongs to the loader and its probe.
  - **`0029` to `0031` were not touched**, and the applied sequence is `0001` to `0028` then `0032`, which is the order `corpus.yml`'s glob already produces. The hole is reserved in [ALLOCATION](../decisions/ALLOCATION.md), so `CI-06h` passes on it.
- **Files touched:** `packages/db/migrations/0032_trading_calendar_holidays_coverage_revisions.sql` (new), `packages/db/DELTA_MANIFEST.md`, `docs/architecture/data-model/{trading_calendar_revisions,trading_calendar_loads}.md` (new), `docs/architecture/data-model/{README,trading_calendar}.md`, `scripts/corpus/gates.mjs`, `docs/{STATE,INDEX}.md`, `docs/sessions/{README,2026-08-16-session-31}.md`.
