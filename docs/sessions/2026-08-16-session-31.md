## 2026-08-16 - Session 31: FOLD-02 session 3, migrations 0030 and 0031

- **Done:** [ADR-040](../decisions/ADR-040.md)'s payout enforcement window and [ADR-041](../decisions/ADR-041.md)'s identity restriction, in the database. **[`0030_payout_hold_enum.sql`](../../packages/db/migrations/0030_payout_hold_enum.sql)**: `payout_status` gains `held_pending_review`, one statement, no `BEGIN`/`COMMIT`. **[`0031_payout_hold_and_identity_restriction.sql`](../../packages/db/migrations/0031_payout_hold_and_identity_restriction.sql)**: the hold trio and its completeness CHECK, **both `SD-09` predicates** dropped and re-created under their existing names, the hold-expiry index, `wallet_withdrawals_frozen_cannot_settle`, `identity_restriction_episodes`, and the replacement `COMMENT ON TABLE payout_requests` carrying the amended zero-denial sentence. Both carry an `E2 READ: MONEY PATH` header. Records: [DATA_MODEL](../architecture/data-model/README.md) (new [`identity_restriction_episodes`](../architecture/data-model/identity_restriction_episodes.md) record, [`payout_requests`](../architecture/data-model/payout_requests.md) and [`wallet_withdrawals`](../architecture/data-model/wallet_withdrawals.md) amended, four invariant rows, section 17 corrected) and [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md) (`U-07`, `U-08`, `U-09`; `SD-09` marked amended; `OI-06` opened; section 14). Plus [`probe_payout_hold.sql`](../../scripts/db/probe_payout_hold.sql), 18 assertions, and the counterfactual, both wired into the `migrations` job.
- **Next:** FOLD-02 session 4, money path, fresh session: STATE_MACHINES, [M05](../plans/M05-payout-system.md), [M20](../plans/M20-wallet.md), [M07](../plans/M07-risk-abuse.md), [SECURITY](../architecture/SECURITY.md) section 4. **`G-ELIGIBLE` gaining `identities.status` is that session's**, and until it lands the restriction is representable and unenforced, which is the same shape `0011`'s halt was in until this one.
- **Blockers:** None. `0029` and `0032` are untouched and still reserved.
- **Landmines:**
  - **The two-file split's real reason is atomicity, not impossibility.** The combined form fails only when it is wrapped in `BEGIN`/`COMMIT`; with the transaction block removed it succeeds under the install job's autocommit. FOLD-02 finding 6 says "cannot be one file" and that is imprecise. **`0031` is the file that needs the transaction**, because it drops a uniqueness guarantee before re-creating it. A future session that reads the plan and not the probe will draw the wrong general rule.
  - **`payout_requests_hold_is_complete` deliberately does NOT have the freeze trio's shape.** A released hold keeps its trio so the hold is provable from the row; a released freeze must clear its trio, because that constraint is merged. The asymmetry inside one file is real, is flagged in `0031` section 4, and is a founder call at the E2 read.
  - **`wallet_withdrawals_open_idx` was NOT re-created**, which departs from ADR-040's literal wording. Its predicate already matches halted rows, because the halt is orthogonal to the rail status by that same ADR's ruling. Recorded in DELTA_MANIFEST section 14. If the read wants it anyway it is a superseding migration, not an edit.
  - **`evidence_packs` is account-scoped and a restriction is per human** (`OI-06`). `identity_restriction_episodes.evidence_pack_id` is nullable for that reason. Widening `evidence_packs` belongs to M06.
  - **The 48 hour SLA binding the restriction rather than the payout is NOT enforced by DDL** and the column says so. It spans two tables. It is a golden scenario and an alarm, and a reader who assumes `sla_due_at` enforces it will not add the assertion.
- **Files touched:** `packages/db/migrations/{0030_payout_hold_enum,0031_payout_hold_and_identity_restriction}.sql` (new), `scripts/db/probe_payout_hold.sql` (new), `docs/architecture/data-model/identity_restriction_episodes.md` (new), `docs/architecture/data-model/{README,payout_requests,wallet_withdrawals}.md`, `packages/db/DELTA_MANIFEST.md`, `scripts/corpus/gates.mjs`, `.github/workflows/corpus.yml`, `docs/{INDEX,STATE}.md`.

### Verification, executed rather than described

All <!--gen:migration_files-->30<!--/gen--> migrations apply forward-only from empty against PostgreSQL 16 under `ON_ERROR_STOP=1`: **97 tables, 331 indexes, 353 check constraints, 6 triggers**, read from the catalogue rather than from a grep. `probe_payout_hold.sql` is **18 / 18**, leading with four success cases before it attempts a single rejection. All 12 corpus gates pass and `falsify.mjs` still watches each of them fail dirty.

### Three findings

**The counterfactual needed its own counterfactual, and that is what made it mean anything.** The combined `ALTER TYPE` plus predicate was watched failing, as FOLD-02 section 9 item 5 requires. Then the same statements were run **without** the transaction block and they succeeded. Watching only the failure would have licensed the general claim "an enum value and its predicates cannot share a file", which is false and which the next session would have inherited. Both directions are now a CI step that fails the build if the combined form ever succeeds.

**`CI-06g` could not see the `e2_files` spans and reported success.** `spansIn()` matched span names against `[a-z_]+`, and `e2_files` is the one key in `SPAN_QUERIES` containing a digit. Four spans in STATE and INDEX matched nothing: never compared, never regenerated, always green. They read **18**, correct when written, stale from the moment `0028` merged, and the number they carry is **the scope of the founder's E2 read**. Found because this session wrote the same key at `20` into two other documents and `check` passed with the corpus saying both. **The tenth site of the hand-maintained-count class, and the first one hiding inside the gate that exists to end it.**

**DELTA_MANIFEST's own migration table stopped at `0027` and its heading said "27 files" while 28 were on disk.** `0028` merged with [ADR-035](../decisions/ADR-035.md) and was never rowed, and the same omission sat in DATA_MODEL section 17. Both are corrected, and both counts are spans now rather than prose.
