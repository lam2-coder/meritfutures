---
status: approved
depends_on: [README.md]
last_updated: 2026-08-30
---

## 2026-08-30 - Session 479: a red security gate on `main`, and an unrewritable commit is PINNED while the tree it left behind is REPAIRED

<!--index: **THE RULING IS [`ADR-288`](../decisions/ADR-288.md), `proposed` and UNSIGNED. `CI-05`'s `VG-1` WAS RED ON `main` AT `4222b8e4` IN BOTH HALVES AND IS GREEN IN BOTH HALVES NOW**, each command run separately: `gitleaks git . --redact --no-banner --exit-code 1` reports `no leaks found` over **2,443** commits and exits 0, and `gitleaks dir .` the same. **NO SECRET WAS EXPOSED AND NONE EVER WAS.** **NO MIGRATION, NO `src/` LINE, NO `RI-nn` MINTED, `RI-32` STILL FREE, AND `.github/workflows/ci.yml` BYTE FOR BYTE UNCHANGED.** **THE DIAGNOSIS WAS RE-DERIVED WITH THE REPOSITORY'S OWN PINNED SCANNER**, `8.30.1`, tarball digest checked against [`ci.yml:118`](../../.github/workflows/ci.yml) before it was unpacked: exactly **two** findings, both `generic-api-key`, both `apps/api/test/payouts.test.ts` at **1151** and **1170**, both in commit `00cb1bbb`, entropies **3.640224** and **3.681881**, both test fixture idempotency keys. **TWO OF THE ROW'S OWN PREMISES ARE FALSE AND THE FIRST IS CONSEQUENTIAL: THIS REPOSITORY HAS RECORDED A CONFIRMED FALSE POSITIVE BEFORE.** [`.gitleaksignore`](../../.gitleaksignore) has existed since session 254, carries **six** entries over four sites, and its header runs to `:69` defending exactly this decision and instructing the session that wants a fifth entry to read it first. **THE SECOND IS BENIGN**: the predicted local `dir` count of twenty with eighteen untracked build artifacts does not reproduce on an unbuilt tree, where `dir` reports **2, both tracked, zero untracked**, partitioned against `git ls-files`. **AND A LANDMINE THE ROW COULD NOT HAVE KNOWN: THE SESSION CONTAINER'S CLONE IS SHALLOW.** The first history scan read **311** commits and not 2,437; `git fetch --unshallow` was run before any claim about history, the scan then read **2,443**, and it found the same two findings and nothing else, which was luck rather than method. **THE RULING IS A SPLIT DECIDED ON OPPOSITE GROUNDS, BECAUSE ONLY ONE HALF CAN STILL BE CHANGED.** The working tree is **REPAIRED**, both fixtures renamed forward to the `idem-<word>` convention **seven** siblings in the same file already use, so **no `dir` entry exists at all**; the history is **PINNED** by two full `commit:file:rule:startline` fingerprints, because `git merge-base --is-ancestor 00cb1bbb origin/main` exits **0** and the only removal is a rewrite of the default branch. **THE FOUR PRIOR REFUSALS ARE ANSWERED RATHER THAN IGNORED AND THE FOURTH ANSWERS ITSELF**: all four sat on unmerged branches where a rebase plus `--force-with-lease` was available and founder-authorised, and [`STATE.md:7225`](../STATE.md) states this entry's distinguishing rule in the corpus's own words, that session 254's fingerprints *"are defended only because their commits are in `main`"*. **ANCESTRY ALONE WOULD LET ANYTHING THROUGH, SO IT IS PAIRED WITH A SHAPE TEST**: the earlier values were a real provider's webhook-secret prefix plus sixteen hex characters and a real base64 image header bound to `const secret`, credentials **by shape**, and these are lowercase hyphenated English labels. **NO GATE WAS WEAKENED AND IT WAS WATCHED RATHER THAN ARGUED. THREE SEEDS**: a synthetic credential at the exact pinned line in the exact pinned file is caught by `dir` (exit 1); **the same file at BOTH pinned line numbers in a DIFFERENT commit is caught by `git` (exit 1)**, which is the narrowness proof; and the file was restored from a byte copy with `sha256sum -c` reporting `OK`. **THE THIRD SEED IS A FINDING RATHER THAN A CONFIRMATION**: `generic-api-key` carries a **stopword allowlist**, so a high-entropy value containing `fake` or `example` is SILENT, and the row's direction to synthesise an *obviously fake* seed defeats its direction to *watch it go red*. The first seed here did exactly that and reported green. **THE DURABLE HALF IS THE PROCESS DEFECT AND IT IS THE DISPATCHER'S**: all six checks the merge ritual reads are LOCAL and [`package.json:21`](../../package.json) deliberately excludes the `CI-05` scanners from `verify`, which [`STATE.md:3769`](../STATE.md) already wrote down in plain words; had the ritual opened #632's CI once, `00cb1bbb` would have been unmerged and `.gitleaksignore` would have gained nothing. **`ci.yml` AND [`SECURITY.md`](../architecture/SECURITY.md) ARE BOTH UNCHANGED WITH REASONS GIVEN.** **THE INVARIANT THAT SHOULD EXIST IS REGISTERED RATHER THAN SMUGGLED**: nothing checks that a `.gitleaksignore` entry is still live, and the runner is outside this fence. **FENCE HELD**; the two files outside it are GENERATED, `docs/decisions/README.md` and `docs/sessions/README.md`, rewritten by `gates.mjs generate`. Counts derived at reporting time, each command run separately: **33 of 33** gates after `generate`, **30 of 30** invariants off the runner's own last line, **297 files / 7,223 passed / 6 skipped** against a baseline taken on this same tree before a line changed of **297 / 7,223 / 6**, a delta of **ZERO**. Typecheck 0, lint 0, `format:check` clean. **`pnpm run verify` was NOT run and `falsify.mjs` was NOT run**, both forbidden by the row. -->

---

### 1. The objective

**[ALLOCATION](../decisions/ALLOCATION.md) row `288`: `CI-05`'s `VG-1` is red on `main` and nothing else matters until it is green.** One objective. A security gate, not a money path, though the one file it touches is a money-path test file.

### 2. What was read before anything was written

Row `288` in full, then [CLAUDE.md](../../CLAUDE.md), then the `VG-1` half of [`ci.yml`](../../.github/workflows/ci.yml) and [`scripts/ci/install-pinned-tool.sh`](../../scripts/ci/install-pinned-tool.sh), then every `gitleaks` mention in the corpus outside `docs/sessions/`. **That last read is what found the row's false premise and it is the reason this entry exists in the shape it does.**

### 3. The measurement

[ADR-288](../decisions/ADR-288.md) section 1 carries the claim-by-claim table. **Every figure in the row was re-derived and three of them moved.**

**The scanner was the repository's own.** Fetched from the release [`ci.yml:117`](../../.github/workflows/ci.yml) names, digest checked against [`:118`](../../.github/workflows/ci.yml) before it was unpacked, match exact.

**The clone was shallow and that was caught before it mattered.** `git rev-parse --is-shallow-repository` returns `true` and the first scan read 311 commits. After `git fetch --unshallow` it reads 2,443 and reports the same two findings.

### 4. The ruling

[ADR-288](../decisions/ADR-288.md) sections 3 and 4. **A split: the tree is repaired because it can still be changed, and the history is pinned because it cannot.**

The candidates the row offered were priced and refused with reasons: a new `.gitleaks.toml` would be a second suppression mechanism beside a first and carries a live footgun, since a root config replaces the default rule set unless it opts back in; `--baseline-path` requires a workflow flag and suppresses by comparison against a snapshot of all current findings rather than by pinned identity.

### 5. The four refusals

[ADR-288](../decisions/ADR-288.md) section 4.1. **This repository has refused a gitleaks suppression four times and a session reaching for that file owes those refusals an answer.** All four were on unmerged branches, and the fourth states the distinguishing rule itself.

### 6. Watched going red

[ADR-288](../decisions/ADR-288.md) section 5. Three seeds, no real credential anywhere, byte-identical restore verified with `sha256sum -c`.

**The stopword finding is the part worth carrying out of this session.** Two correct instructions, jointly producing a session that seeds a credential, sees green, and concludes the gate works when it never fired.

### 7. Next

- Rows `285` and `286` remain the near-term path and neither was touched here.
- **The merge-ritual fix is not in this repository's code.** It is one line: read the pull request's own CI conclusion before merging, and treat a red required stage as blocking regardless of what the local checks say.
- The `.gitleaksignore` liveness invariant is registered in [ADR-288](../decisions/ADR-288.md) section 6 and needs a fence that reaches `packages/tooling/checks/repo-invariants.mjs`.

### 8. Blockers and landmines

- **LANDMINE, AND IT REACHES EVERY SESSION RATHER THAN THIS ONE:** the session container's clone is **shallow**. Any claim about history from a `gitleaks git .`, a `git log`, a `git rev-list` or an `-S` search is a claim about a twelfth of the commits unless `git fetch --unshallow` has been run first.
- **LANDMINE:** `generic-api-key` carries a stopword allowlist. A seeded secret labelled `fake` or `example` is silently not a finding, so a gate can be "watched going red" and never fire.
- **LANDMINE:** a document that quotes a flagged literal reintroduces the finding in the record of the finding. This corpus has watched that happen twice, at [`STATE.md:4219`](../STATE.md) and [`:7222`](../STATE.md). Nothing in this session's four documents reproduces either value.
- **LANDMINE CARRIED:** a third entry in [`.gitleaksignore`](../../.gitleaksignore) is evidence the merge-ritual fix did not land, and that file's new section says to read it that way.
- **NOT A BLOCKER, BUT NAMED:** the ancestry test that carries section 4 is a test about git rather than about security, and the shape test paired with it is enforced by prose alone.

### 9. Files touched

- `.gitleaksignore` (two `git` fingerprints, and the argument for them)
- `apps/api/test/payouts.test.ts` (two string literals and a comment; no assertion moved)
- `docs/decisions/ADR-288.md` (new)
- `docs/decisions/ALLOCATION.md` (row `288`)
- `docs/STATE.md`
- `docs/sessions/2026-08-30-session-479.md` (new, this file)
- `docs/decisions/README.md` and `docs/sessions/README.md`, **generated spans only**, rewritten by `gates.mjs generate`

**No migration, no `src/` line, and `.github/workflows/ci.yml` and `docs/architecture/SECURITY.md` are both unchanged with reasons given in [ADR-288](../decisions/ADR-288.md) section 6.**
