---
status: approved
depends_on: [README.md]
last_updated: 2026-08-25
---

## 2026-08-25 - Session 200: M17's four tables enter the transcription, and the trap this module makes plausible is caught in one direction and silent in the other
<!--index: **[M17](../plans/M17-offers-engine.md)'s slice under [ADR-092](../decisions/ADR-092.md), computed from `TABLE_KEYS` rather than received. FOUR OF FOUR, and the honest report is that all four were AVAILABLE**: `main` was `3c9c2d3` at open and `3c9c2d3` immediately before the log, re-fetched rather than assumed, so no sibling of sessions 195 to 203 had merged. `offer_experiments`, `price_floors`, `offers` and `promotional_credit_grants` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **53 of 111 becomes 57 of 111**, at **1 root, 22 owned, 7 derived and 27 firm**, every figure read back out of the registry rather than added up. **No ADR, no migration, no new document.** **`offers` IS `ledger_accounts`' SHAPE AND NOT `coupons`', AND THE SUITE SAYS SO RATHER THAN TASTE**: `coupons` is firm because it has no identity column, `offers` has one, and `offers_identity_scope_matches` makes `identity_id` BICONDITIONAL with `scope = 'identity'`, so `owned` with `nullable: true` returns exactly the rows the DDL says are that person's -- and declaring it `firm` was seeded and fails by name with *"offers reaches identities directly"*. **What that read returns is narrower than the table's name suggests and it is said out loud**: the offers ADDRESSED TO a person, never the offers AVAILABLE to them, and **a `segment` offer names no segment because the table has no segment column at all**, so it is unreachable under every class rather than under this one. **THE ADR-092 TRAP IS CAUGHT IN ONE DIRECTION AND SILENT IN THE OTHER, BOTH WATCHED**: `offers` declared `derived` through the FIRM `offer_experiments` on `experiment_id` fails the bounded chain assertion by name, while **`promotional_credit_grants` declared `derived` through the OWNED `offers` on `source_offer_id` passes ALL 78 TESTS** -- the chain terminates, nothing throws, and the rule would return no grant whose `source_offer_id` is NULL, which is every fee-back and every loyalty grant. **The bounded assertion bounds a FIRM parent and does not bound a NULLABLE-column OWNED one**, a strictly larger silent class than the one it was written for. **ONE OF THE FOUR CARRIES DRIFT**: [`0044`](../../packages/db/migrations/0044_fee_back_and_ladder_unlock.sql)'s `source_payout_request_id`, one `ADD COLUMN`, folded by [ADR-094](../decisions/ADR-094.md), and deleting it from the transcription turns the per-table comparison RED naming the column. **[Session 166](2026-08-24-session-166.md)'s `price_floors` finding is RE-STATED AND NOT ACTED ON**: PK `(product_ref, effective_from)` against `dual_control_approvals.subject_id uuid NOT NULL`, so a control four documents assert cannot name its subject, and a repair is a migration this fence holds none of. **All four standing refusals survive checked by name.** 74 tests to **78**, suite **1,584 to 1,588**, both baselines MEASURED by restoring `origin/main`'s three files; `verify` exit 0 at 8 of 8 invariants and 106 files; 30 of 30 gates; `falsify:ci CI-01` **12 of 12** -->

- **Objective:** M17's tables enter [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts), minus whatever is already registered, under [ADR-092](../decisions/ADR-092.md) and [ADR-094](../decisions/ADR-094.md). **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR.**
- **Done:** **4 of 4, and all four were available.** `offer_experiments` and `price_floors` registered `firm`, `offers` `owned` on a NULLABLE `identity_id`, `promotional_credit_grants` `owned` on a `NOT NULL` one. The registry goes from **53 of 111 to 57 of 111**, at 1 root, 22 owned, 7 derived and 27 firm, **30 of the 57 reachable through the scoped accessor**. Five seeds run, four watched failing and **one watched PASSING**, which is the finding.
- **Next:** **The bounded chain assertion needs the second half of its own class.** It refuses a derivation whose parent is `firm` and admits one whose parent is `owned` on a NULLABLE column, and the second returns a strict subset silently. That is a change to [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) that would turn red on whatever it finds across 57 registered tables, so it is its own session on [session 165](2026-08-24-session-165.md)'s reasoning about a gate whose cost is unmeasured. **[M17](../plans/M17-offers-engine.md)'s `S2` is complete and `S1`, `S3` to `S6` are not this session's**: `S3` waits on founder item `F2`, `S4` and `S5` on `S3`, and `S6` is not dispatchable at all. Nothing in M17 has a caller yet, which is the shape [session 166](2026-08-24-session-166.md) measured and this session did not change.
- **Blockers:** none for this session. **The `E2` line-by-line read is owed these four registrations**, which makes fifty-seven money-path tables merged on delegated authority without it.
- **Landmines:**

  - **THE SEED THAT PASSED IS WORTH MORE THAN THE FOUR THAT FAILED.** `promotionalCreditGrants` declared `derived` via `offers` on `source_offer_id`, `foreignColumn: 'id'`, `traversal: 'hop'`, leaves the suite GREEN AT 78. Every assertion is satisfied: the column exists, the target is `offers`' primary key so the hop check passes, the foreign key is declared inline in the `CREATE TABLE` body, and the chain terminates at an `owned` rule so the firm-parent assertion never fires. **The rule is nonetheless wrong in the direction that returns rows**: `source_offer_id` is NULLABLE, so every fee-back grant ([`0044`](../../packages/db/migrations/0044_fee_back_and_ladder_unlock.sql)) and every loyalty-issued grant reaches nobody, and a grant issued from a `public` offer reaches nobody either because `offers`' own rule is `owned` on a NULLABLE column. **[Session 188](2026-08-25-session-188.md)'s assertion was written against `plan_version_sizes`, whose parent is FIRM, and this is the same mistake one class over with nothing looking at it.**

  - **`offers` IS THE TABLE THIS SESSION'S BRIEF PREDICTED WOULD BE MISCLASSIFIED, AND THE PREDICTION POINTED THE WRONG WAY.** *"An offer is a FIRM object and a redemption is an OWNED one"* is `coupons` and `coupon_redemptions` exactly and it is **not** `offers`: `coupons` carries no identity column, `offers` carries `identity_id uuid NULL REFERENCES identities(id)` under a biconditional CHECK. **`firm` here is not a judgement call, it is red**: `no firm table carries a column referencing identities` fails naming the table. The pattern that fits is `ledger_accounts`, one file over, where `scope` is CHECKed against `identity_id` in the same shape.

  - **A SCOPED READ OF `offers` IS NARROWER THAN ITS NAME AND A CALLER WILL GET THIS WRONG.** It returns the offers **addressed to** an identity, never the offers **available to** them. A `public` row is the firm's; a `segment` row names no segment, because [`0024`](../../packages/db/migrations/0024_offers.sql) gives the table no segment column at all, so nothing connects it to a member of its segment under any class in the vocabulary. **The `why` says so and there is no gate that would.**

  - **[SESSION 166](2026-08-24-session-166.md)'s `price_floors` FINDING STANDS EXACTLY WHERE IT STOOD.** PK `(product_ref, effective_from)` at [`0024:94`](../../packages/db/migrations/0024_offers.sql), no `uuid`, against `dual_control_approvals.subject_id uuid NOT NULL` at [`0016:227`](../../packages/db/migrations/0016_treasury_controls.sql). [M17:150](../plans/M17-offers-engine.md), [EC-119](../edge-cases/EC-119.md), [`price_floors.md:2`](../architecture/data-model/price_floors.md) and `0024`'s own comment all assert a control the table cannot express. **Registering it changed nothing about that in either direction**, and a repair is a migration this fence holds none of. `product_ref` is also bare `text` on both sides of the floor clamp, with no FK, no domain and no shared CHECK, so the clamp is a text join.

  - **`offers.offer_type` IS BARE `text` WITH NO CHECK, IN THE FILE WHOSE HEADER ARGUES THAT A CHECK IS THE SCHEMA ENFORCING THE RULE.** `varies` got the argument and `offer_type` did not, so the six `OF-M17-nn` values are unrepresented in DDL and the transcription can only restate that. `offers.criteria_version` is likewise a bare `integer` that cannot address `loyalty_criteria`, whose primary key is `(benefit_code, version)`.

  - **COLUMN TYPE AND NULLABILITY ARE ASSERTED NOWHERE, ON ALL 57.** The two new `nullable` flags and every column type above are compared on their NAME alone. [ADR-094](../decisions/ADR-094.md) section 3 names the axis and states why closing it is its own session. **This session did not close it and nothing written here implies it is closed.**

  - **`scope.ts` AND `scoped-db.test.ts` LANDED IN ONE COMMIT, DELIBERATELY.** `SQL_NAME` is `Readonly<Record<TableKey, string>>` and total, so a commit carrying either alone is red. `schema.ts` went in its own commit before them, which is green on its own because a declared-but-unregistered `pgTable` compiles. **The registry size was recomputed from `TABLE_KEYS.length` at all five sites and never incremented**, and `DDL_NAMES` needed no edit at all because [session 182](2026-08-24-session-182.md) made it derived.

  - **KILLING `falsify:ci` MID-RUN PLANTS THE SEED THE HARNESS EXISTS TO CLEAN UP.** A first invocation was stopped at a ten-minute timeout and left `packages/rules-engine/src/plan/validate.ts` modified; the next `CI-01` run then reported **11 of 12** and left `packages/rules-engine/src/day/progression.ts` modified, printing its own *"THE HARNESS CHANGED THE TREE"* diff. Both files were restored by `git checkout HEAD --`, and the clean re-run is **12 of 12 at exit 0** with `git status --porcelain` empty immediately after. **`git add -A` was never used in this session** and the two restorations are the reason the branch carries no engine file.

- **Files touched:** [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts), [`packages/db/src/scope.ts`](../../packages/db/src/scope.ts), [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts), [STATE](../STATE.md) (one appended `##` section), [sessions/README](README.md) (row 200 struck, entry row generated), this file. **Nothing outside the fence.** [M17](../plans/M17-offers-engine.md), [`0024_offers.sql`](../../packages/db/migrations/0024_offers.sql), [`0044_fee_back_and_ladder_unlock.sql`](../../packages/db/migrations/0044_fee_back_and_ladder_unlock.sql), [`0016_treasury_controls.sql`](../../packages/db/migrations/0016_treasury_controls.sql), [ADR-092](../decisions/ADR-092.md), [ADR-094](../decisions/ADR-094.md) and [session 166](2026-08-24-session-166.md) were **read and none was written**.

### What was NOT done, named rather than implied

**No ADR, and wanting one was taken as the stop signal.** The bounded chain assertion's silent half is a ruling about what a mechanical control must cover, and a transcription rules nothing, so it is reported here and in [STATE](../STATE.md) rather than allocated a number. **No migration**, so `price_floors` still cannot be the subject of a dual-control row, `offer_type` still has no CHECK, `product_ref` is still bare text on both sides of the clamp, and `promotional_credit_grants` still has no `spendable_from`. **No new document and no `INDEX.md` row.** **`docs/plans/**` was read-only** and [M17](../plans/M17-offers-engine.md) is unchanged. **The fold was not widened**: it refused nothing in this slice, and had it refused something the table would have stayed unregistered.
