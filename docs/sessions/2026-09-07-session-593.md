---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 593: the claim that was never wrong, the coordinate the certifying wave broke in its own diff, and the prose gate refused because 30 of 33 covers strings mix limitations with assertions

<!--index: **THE ROW IS [ADR-398](../decisions/ADR-398.md), `proposed` and UNSIGNED. CONTROL OVER THE CORPUS'S OWN RECORDS, `E2` READ OWED.** **THE DISPATCH'S FIRST PREMISE IS REFUTED AT PRIMARY SOURCE: THE `sites: 7` CLAIM WAS NEVER WRONG.** The register was read out of [`gates.mjs`](../../scripts/corpus/gates.mjs) at **eight refs** and reads **7** at each of the four commits that ADDED an entry carrying the claim and **10** from `c2024b48`, the child of the commit adding [ADR-351](../decisions/ADR-351.md), whose section 7 rules the raise. **Four true dated measurements, falsified afterwards by a row none of them could see.** **THE POPULATION IS SIX SENTENCES OVER FOUR FILES AND NOT THREE FILES WITH ONE CLAIM**, and the fourth, [ADR-337](../decisions/ADR-337.md) line 202, is outside the fence and still carries the ORIGINAL `:8412`. **THE FINDING IS THAT THE VALUE AND THE COORDINATE FALSIFY AT DIFFERENT COMMITS**, and [ADR-395](../decisions/ADR-395.md) section 6 certified this coordinate sound and its own diff broke it in the same commit: it wrote *"the drift is in the value rather than the line"* on `ebc21238` where `:8579` was exact, then moved every line from 2,525 on by **88**, and 8579 plus 88 is **8667**. **Its 13-token sweep could not have reached it**, because the three were already classified LEFT **on the value** and the classification applies to the whole token, so the defect lives below the granularity of [ADR-388](../decisions/ADR-388.md)'s control. **RULED: THE DISPATCH ASKED ABOUT THE WRONG HALF OF THE TOKEN.** The cardinal is neither repaired nor deleted, because rewriting 7 to 10 manufactures a measurement and [ADR-383](../decisions/ADR-383.md)'s delete-and-point remedy does not transfer from a LIVE `covers` self-description to a table whose own third column reads *"Moved?"*; **a cardinal inside a dated record is not a restatement of a register, so the TENSE is what is repairable** and it is dated at all five in-fence sites with every figure untouched. **The COORDINATE is the half deleted and replaced by a name**, wrong at three of eight refs and already repaired by two prior rows. **THE `CI-06l` ROW IS REPAIRED FROM THE RUNNER RATHER THAN PASTED** and the derived clause agrees with the published one. **THE GATE OVER THAT SILENCE IS REFUSED WITH A MEASUREMENT**: the only buildable predicate raises **4 findings over 3 of 33 gates**, the repair takes it to **3**, and **all three survivors are FALSE**; **30 of 33 `covers` strings carry a limitation clause** over only **22 distinct backticked tokens**, so the gate would be red on three correct rows and silent on every real defect. **THE DIFF MOVES NOT ONE LINE IN THE FOUR DOCUMENTS IT REPAIRS** (numstat 1/1, 2/2, 2/2, 1/1, the `STRATEGY` edit width-neutral too) and **ZERO CITATION REPAIRS ARE FORCED**; the three registry rows move five sites and all five are already-wrong pointers two rulings say must stay wrong. **NO GATE WEAKENED, ADDED OR WIDENED, NO TEST SKIPPED, DISABLED OR QUARANTINED, NO INVARIANT MINTED, NO `CI-06` LETTER SPENT, NO MIGRATION TAKEN OR RESERVED.** `pnpm run verify` and [`falsify.mjs`](../../scripts/corpus/falsify.mjs) **NOT run**, both forbidden. Suite **336 / 8,109 / 10 / 0** against a base of **336 / 8,109 / 10 / 0** reproduced on `c219c0d1` BEFORE the first edit, **delta ZERO on every axis**; gates **33 of 33** and **33** again off `GATES.length`, invariants **35 of 35**, typecheck, lint and `format:check` clean. **THE CONTAINER ARRIVED WITH NO REPOSITORY FOR THE FOURTH CONSECUTIVE WAVE AND THE CLONE THIS ROW MADE IS FULL**: `is-shallow` **false**, **4,310** commits. -->

---

### 1. The objective

**One objective.** Two findings from [ADR-395](../decisions/ADR-395.md)'s owed list: the `sites: 7` claim in three named ADRs, and [STRATEGY](../testing/STRATEGY.md) section 4.4's `CI-06l` row. It resolved into one refuted premise, one enumeration the dispatch got wrong, one mechanism finding about the corpus's own citation control, five dated repairs, one row repair and one gate refused with a measurement.

---

### 2. Done

- **Measured the container before trusting the dispatch about it.** Empty working directory, no repository anywhere to depth 6 from `/`, cloned it, and `git rev-parse --is-shallow-repository` returned **`false`** at **4,310** commits. Reported rather than repaired: it is the dispatcher's, four waves running.
- **Reproduced the suite base on `c219c0d1` BEFORE the first edit**: 336 files, 8,109 passed, 10 skipped, 0 failed.
- **Refuted the dispatch's first premise at primary source**, by reading the register out of `gates.mjs` at eight refs rather than out of any record of it.
- **Enumerated the population rather than inheriting it**: six claim-bearing sentences over four files, plus fourteen sites that only report the finding and are correct as written.
- **Found and derived the sub-token classification hole**, section 5 of the entry.
- **Ruled the cardinal unrepairable and undeletable, and the coordinate deletable**, and applied both.
- **Repaired the `CI-06l` row from the runner**, width-neutral and line-count-neutral.
- **Refused the prose gate with a measurement** and priced the slice that would make it buildable.

---

### 3. Next

The five items in [ADR-398](../decisions/ADR-398.md) section 13, of which the first is a one-line repair to [ADR-337](../decisions/ADR-337.md) and the third is a ruling that needs a row rather than a hand.

---

### 4. Blockers

None. `gates.mjs` and everything under `scripts/corpus/` were read-only to this row and no edit to them was needed.

---

### 5. Landmines

- **`:8667` is a live coordinate into a file that moves nearly every wave**, which is why this row cited the register entry **by name** instead. Anybody tempted to re-point it at a number should read section 5 first.
- **The `sites: 7` figures must stay at 7.** Three rows have now declined to rewrite them and a fourth attempt will look like a repair.
- **The predicate in section 9 is one predicate.** The refusal rests on the 30-of-33 mixing measurement, not on the predicate's precision.

---

### 6. Files touched

| File | Change |
|---|---|
| [`docs/decisions/ADR-398.md`](../decisions/ADR-398.md) | new |
| [`docs/testing/STRATEGY.md`](../testing/STRATEGY.md) | the `CI-06l` row, one line replaced by one line of the same width |
| [`docs/decisions/ADR-334.md`](../decisions/ADR-334.md) | one cell, coordinate named and tense dated |
| [`docs/decisions/ADR-335.md`](../decisions/ADR-335.md) | two cells, same |
| [`docs/decisions/ADR-336.md`](../decisions/ADR-336.md) | two cells, same |
| [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) | row `398` TAKEN, rows `399` and `400` RESERVED and unlinked |
| [`docs/decisions/README.md`](../decisions/README.md) | generated |
| [`docs/sessions/README.md`](README.md) | generated |
| [`docs/STATE.md`](../STATE.md) | one session section appended |
| this file | new |
