---
status: approved
depends_on: [README.md]
last_updated: 2026-09-08
---

## 2026-09-08 - Session 649: the corpus states the wrong SQLSTATE for a stored generated column, measured again on a database this row started, and repaired line-neutrally in every live file the fence holds

<!--index: **THE ROW IS [ADR-454](../decisions/ADR-454.md), `proposed` and UNSIGNED. MONEY PATH, `E2` READ OWED.** [ADR-452](../decisions/ADR-452.md) section 8 taken: the corpus states PostgreSQL refuses a write naming a `GENERATED ALWAYS AS (...) STORED` column with `42601`, and `42601` is `syntax_error`. **NOT TAKEN ON TRUST FROM [ADR-452](../decisions/ADR-452.md)**: a PostgreSQL 16.13 cluster was initialised in this container, given a table modelled on `reconciliations` carrying an `ALWAYS` identity column and a `STORED` generated column, and asked thirteen questions with `VERBOSITY verbose` and again through `GET STACKED DIAGNOSTICS`. **`428C9` for both halves, both statement shapes**, and row 452's numbers reproduce exactly. **ONE DISTINCTION ROW 452 DID NOT TEST IS THE ONE THE CORRECTED PROSE NEEDS**: `OVERRIDING SYSTEM VALUE` rescues the identity half and does NOT rescue the stored half, so with the SQLSTATE no longer a discriminator the override is the difference that survives. Both repaired comment blocks were telling the two features apart BY their code, so a one-token substitution would have left the contrast silently gone; both were reflowed inside their existing line counts and widths. **THE POPULATION IS 48 MENTIONS ACROSS 14 FILES**, derived from the base at the moment the number was written, against [ADR-452](../decisions/ADR-452.md)'s **26 across 11**, which re-derives correctly on that entry's own base: recording a false claim adds mentions of it. **11 REPAIRED, 0 UNREPAIRABLE LINE-NEUTRALLY, 2 OWED OUT OF FENCE, 35 IN DATED RECORDS.** [ADR-448](../decisions/ADR-448.md)'s *"`428C9`, not `42601`"* is right about the identity half and wrong as the contrast its position makes it read as, and that reading reached the suite verbatim. **RED BEFORE GREEN ON THE SUITE ASSERTIONS**: `:159` and `:380` watched failing against corrected message text at 2 failed and 23 passed, then 25 of 25 green, no case deleted and none skipped. **NOT ONE CITED LINE MOVES**: 173 cited lines in `scoped-db.ts`, three of them edited IN PLACE, `7/7` and `6/6` numstat on the two source files and one line added below every cited line in the suite. **THE CORPUS RULE QUESTION IS NAMED AND NOT ANSWERED**: whether a dated record's factual error may be repaired at all. Suite **344 files, 8619 passed, 11 skipped, 0 failed**, unchanged from the base because the row added an assertion and not a case; **33 of 33** gates, **35 of 35** invariants. -->

### 1. The objective

Take [ADR-452](../decisions/ADR-452.md) section 8: the corpus states in many places that PostgreSQL refuses a write naming a `GENERATED ALWAYS AS (...) STORED` column with `42601`. Measure it against a database this row starts itself rather than against that sentence, and repair the live source only, line-neutrally, inside a fence of three files.

### 2. Done

- **The container measured and reported. THERE WAS NO CHECKOUT AT ALL**, no working tree and no `origin` to fetch from. The repository was located through the session's own repository listing, cloned, authorised for push with `add_repo`, and `24e9f715` resolved in it. A dispatch that opens with `git fetch origin` assumed a thing that was not true here.
- **The base reproduced at `24e9f715` on `origin/claude/wave-452s`, not `main`**, before the first edit, and measured green on every leg: **33 of 33** gates, **35 of 35** invariants, **344 files, 8619 passed, 11 skipped, 0 failed**, typecheck, lint and format clean.
- **A PostgreSQL 16.13 cluster initialised and started in this container**, `initdb` on a unix socket, and a table built modelling [`schema.ts`](../../packages/db/src/schema.ts)'s `reconciliations`: an `ALWAYS` identity `id` and a `STORED` generated `delta_cents` over two integer-cent balances, plus the `BY DEFAULT` identity column the estate does not have.
- **Thirteen questions asked twice**, once through `psql` with `\set VERBOSITY verbose` and once through `GET STACKED DIAGNOSTICS ... RETURNED_SQLSTATE` inside a `plpgsql` handler, so the code read is the server's and not a client's formatting. **`428C9` for the stored half and the identity half, on `INSERT` and on `UPDATE`.** `42601` is what `UPDATE recon SET SET` returns.
- **Row 452's section 8 reproduced exactly and independently**, on a different cluster in a different container, without its table being read first.
- **`Q10` is the question row 452 did not ask.** `OVERRIDING SYSTEM VALUE` on the stored half is refused with the same `428C9` and the same message. The override is an identity feature and does nothing for a generated column, which makes it the discriminator the SQLSTATE was wrongly being used as.
- **[`scoped-db.ts`](../../packages/db/src/scoped-db.ts) repaired at three mentions**, `:4589`, `:4598` and `:4711`, the last a live money-path error message shipped to callers. **`7/7` numstat, 4718 lines both sides.**
- **[`generated-column-writes.mjs`](../../packages/tooling/checks/generated-column-writes.mjs) repaired at four**, `:20` and the three findings the check emits at `:597`, `:827` and `:971`. **`6/6` numstat, 1055 lines both sides.**
- **[`generated-column-writes.test.ts`](../../packages/tooling/test/generated-column-writes.test.ts) repaired at four**, watched RED first. `:159` and `:380` asserted the wrong code and are why it survived; `:339` carried [ADR-448](../decisions/ADR-448.md)'s contrast into the suite as a comment with a passing assertion beneath it.
- **The population derived from the base and every mention classified**, into repaired, not repairable line-neutrally, owed out of fence, and dated record. **11 plus 0 plus 2 plus 35 is 48, and 3 plus 0 plus 2 plus 9 is 14.**

### 3. Next

- **The two owed lines**, [`keyed-accessor.test.ts:764`](../../packages/db/test/keyed-accessor.test.ts) and [`write-accessor.test.ts:999`](../../packages/db/test/write-accessor.test.ts). Both state `42601` for a stored generated column and both are false. Two bare substitutions, both line-neutral.
- **The founder ruling on dated records**, [ADR-454](../decisions/ADR-454.md) section 9. It gates 35 mentions across 9 files and two other entries are already waiting on it.
- **The integration suite, asked for a third time.** Six database-backed cases already exist at [`admin-writes.test.ts:702`](../../apps/api/test/admin-writes.test.ts) and are skipped on an absent `DATABASE_URL`. Two consecutive sessions have now started a PostgreSQL by hand in under a minute.
- **The INSERT builders are still unguarded**, [ADR-448](../decisions/ADR-448.md) section 8 item 3. `Q3` and `Q4` measure that an INSERT naming either kind is refused at the database, and `refuseGeneratedColumn` runs on the UPDATE path only.

### 4. Blockers

- **The dated-record question blocks 35 of the 48 mentions** and no row may answer it. [ADR-386](../decisions/ADR-386.md):169 forbids repairing a dated record's pointers; whether it forbids repairing its FACTS is unruled.
- **Nothing else blocked.** Every repair inside the fence was achievable line-neutrally, so the dispatch's "record the site and leave it" clause was never reached.

### 5. Landmines

- **`42601` AND `428C9` ARE BOTH FIVE CHARACTERS, WHICH MAKES THE SUBSTITUTION LOOK SAFER THAN THE REPAIR IS.** Two of the three files argued the two features apart BY their SQLSTATE. Swapping one token there leaves a block that states the same code twice with the contrast silently deleted, which is the reading that produced the error. **The argument had to change, not the token.**
- **THE SQLSTATE IS NO LONGER A DISCRIMINATOR AND ONE ASSERTION WAS WRITTEN ASSUMING IT WAS.** [`generated-column-writes.test.ts:347`](../../packages/tooling/test/generated-column-writes.test.ts) existed so the two halves could not be conflated. With both answering `428C9` it cannot do that job. It is kept because it now pins something else worth pinning, that the WRONG code never comes back, and the stored half was given the same guard at `:381` because having none is how it drifted.
- **A SUITE THAT ASSERTS THE STRING ITS OWN SOURCE WRITES PROVES NOTHING ABOUT A DATABASE.** Two assertions pinned `42601` and went green for four rows. The green run was the evidence offered, and the six cases that would have touched a real database are skipped.
- **RECORDING A FALSE CLAIM ADDS MENTIONS OF IT.** The population grew from 26 across 11 to 48 across 14 between two adjacent commits, and the whole difference is [ADR-452](../decisions/ADR-452.md), its session log and the registry cells that summarise them. A row quoting a forward population figure will be wrong by the size of the entry that reported it.
- **[ADR-452](../decisions/ADR-452.md) DISAGREES WITH ITSELF.** Its section 8 says 26 mentions across 11 files and its approval section says 19 places. Re-derived on that entry's own base `6fb9147`, section 8 is right. It is a dated record and is not edited here.
- **PR #738 LANDED WHILE THIS ROW WAS OPEN.** `origin/claude/wave-452s` moved from `24e9f715` to `6ca5617c` merging it, and `git diff` between them is empty. The ownership block that excluded the two owed files may already have cleared. **A fence is not re-derived mid-row from a branch moving underneath it**, so this row did not act on it.

### 6. Files touched

- [`packages/db/src/scoped-db.ts`](../../packages/db/src/scoped-db.ts): `:4589`, `:4598` and `:4711`, and the two-feature block reflowed eight lines in and eight out at its own 80 columns. **No line added, none removed.**
- [`packages/tooling/checks/generated-column-writes.mjs`](../../packages/tooling/checks/generated-column-writes.mjs): `:20`, `:597`, `:827` and `:971`, and the two-feature block reflowed three lines in and three out. **`:20`, `:505` and `:597` are the cited lines and none moved.**
- [`packages/tooling/test/generated-column-writes.test.ts`](../../packages/tooling/test/generated-column-writes.test.ts): `:159` and `:380` corrected, the comment at `:336` rewritten five lines in and five out, one assertion added at `:381`. **`:347` and `:380` are the cited lines and both are unmoved; the added line sits below both.** No case added, removed or skipped.
- [`docs/decisions/ADR-454.md`](../decisions/ADR-454.md), [ALLOCATION](../decisions/ALLOCATION.md) row `454` result cell only, the decisions registry, [INDEX](../INDEX.md), this log and its registry row, and [STATE](../STATE.md) for generated output only.
