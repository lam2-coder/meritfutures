## 2026-08-15 - Session 28: S-E planned, TradingCalendar as data and the wall-clock unit boundary
<!--index: S-E planned, TradingCalendar as data and the wall-clock unit boundary -->

- **Done.** [P1-SE-trading-calendar](../plans/P1-SE-trading-calendar.md) is written and approved. **Plan only, per the brief's stop condition.** No code, no seed data, no rows, no allocation rows, no ADR. Money path, plan mode, [ADR-003](../decisions/ADR-003.md) strict regime, planned across five sessions.
  - **THE SOURCE IS THE EXCHANGE'S OWN PUBLICATION AND IT IS VERIFIED IN THREE LAYERS RATHER THAN TRUSTED.** Transcribed by hand with a provenance block, the retrieved artifact committed beside it and its SHA-256, which is `TR-01`'s discipline applied to calendar data. Structural checks offline; **DST verified against IANA through the tzdata Node already ships**, with the file stating both the CT wall time and the UTC instant and the loader **verifying rather than computing**; and `AS-M2-06`'s existing divergence alarm on `fills.trading_day_vendor` as **the only mechanism that can falsify a row from outside the calendar**. No calendar library is introduced: VG-12 makes a dependency a human admission, and a library is not more authoritative than the exchange.
  - **THE FIXTURE CALENDAR BECOMES DERIVED, WHICH IS WHAT IT ALREADY COMMITTED TO IN ITS OWN NOTE.** [`cme-2026.json`](../../packages/rules-engine/fixtures/calendars/cme-2026.json) says "two hand-maintained calendars is the drift class this corpus has found thirteen times". `CI-06m` regenerates it and asserts `git diff --quiet`, which is the pattern `corpus.yml` already uses for spans. The letter is `m` because FOLD-01 claims `k` and FOLD-02 claims `l`.
  - **CORRECTION AFTER TRADING IS PARTITIONED, AND THE PARTITION IS ASSERTED RATHER THAN JUDGED.** E2 makes a **migration** sacred and does not reach a data row, but **the reason E2 exists reaches it exactly**. No dependent row in `fills`, `daily_marks` or `rule_states` is an ordinary data change the loader makes after asserting the count is zero. A dependent row is **an incident**: prior image, replay through the same `advanceDay` fold, and B4 #5 governs, so a settled payout whose eligibility moves is **never clawed back**. This is already `FM-M2-04`'s stated recovery.
  - **FIVE FINDINGS, EACH NEEDING A RULING.** **`is_holiday` is unwritable as designed**: `session_open_at` and `session_close_at` are `NOT NULL` under `CHECK (close > open)`, so a holiday row must carry a **fabricated** session while the CHECK immediately beside it says a holiday has no session to contain fills in. **A corrected row leaves no prior image**, so `INV-04`'s replay cannot distinguish a calendar correction from an engine regression. **Coverage has no storage**, so an exhausted calendar is **indistinguishable from an unbroken holiday** and every counter silently stops with nothing raising. **One `session_close_at` cannot serve six symbols** across CME, NYMEX and COMEX on an early-close day; recommended resolution is the latest close, because R-01 is containment and the latest close is the one that cannot orphan a fill.
  - **THE FOUNDER'S WALL-CLOCK RULING IS CARRIED AND THE AUDIT BEHIND IT IS MEASURED RATHER THAN ASSERTED.** Across all 28 merged migrations: **zero `::date`, zero `CAST(`, zero `interval` in any case, no `date` column with any default, and every `now()` a `DEFAULT now()` on a `timestamptz`.** No existing trading-day counter is computed against a wall clock. **The risk runs the other way**: `0029` to `0031` introduce the first `interval '48 hours'` and the first `now()` comparisons on the money path, and the moment that is idiomatic the next session needing "five trading days from now" has a working pattern that is wrong on roughly 104 days a year. The three mechanisms land **before** those migrations, while they are still green and therefore cheapest.
- **Next.** **Session 1 of the plan's five: ADR-042, F-5's six reservation rows and this fold's two allocation rows.** Money path, fresh session, landing alone so a sibling can read the claims. **F-5 is a hard prerequisite**: `0032` cannot be claimed over a hole at `0029` to `0031`.
- **Blockers.** None for the fold. Four founder questions are open and none blocks session 1, though `OQ-SE-01` decides how large `0032` is.
- **Landmines.**
  - **FOLD-02 SAYS ITS ALLOCATION ROWS WERE WRITTEN AND THEY ARE NOT IN [DECISIONS](../decisions/README.md).** Section 2 states "Both reservation rows for FOLD-01 are written in the same commit as this fold's". On this branch, which carries **both** fold plans, the migration table still reads "Nothing is reserved today and `0029` is the next free number" and the ADR table still ends at 036. **A sibling reading this branch still sees `0029` and ADR-037 as free.** The gates cannot catch it: no ADR heading and no migration file exists for those numbers, so gaplessness holds vacuously and eleven of eleven pass. **This is a hand-maintained claim about the registry that exists to end hand-maintained claims**, which is where this corpus keeps finding them.
  - **A `date` COLUMN IN THIS SCHEMA IS NOT RELIABLY A TRADING DAY, SO THE OBVIOUS CONVENTION IS ALREADY FALSE.** There are **45** of them. `affiliate_commissions.payable_after` and `chargeback_window_ends_on` are both wall clock and DATA_MODEL says so well, naming them "Merit's own clock" and "the card networks' rather than ours". `published_statistics` carries `as_of_trading_day`, whose unit is in the name, **beside `window_start_day`, whose DATA_MODEL Why cell is empty** and whose unit lives only in [M12](../plans/M12-statistic-definitions.md)'s "trailing 90 trading days", **in one table**. Any rule of the form "a `date` is a trading day" would have to be written against a tree that already violates it, so the plan requires the unit to be **declared** instead, and gates the declaration.
  - **"BUSINESS DAY" IS A THIRD UNIT WITH NO DEFINITION AND NO DATA.** It is used in M04, M05, M07, M09, M11, M12, M16 and three runbooks, including the published settlement claim the brand is built on, and it is **not in [GLOSSARY](../GLOSSARY.md)**. `freeze_expires_at` is a `timestamptz` on two money tables and [M05](../plans/M05-payout-system.md) describes it in business days, so **the column can only express wall clock while the prose promises a unit the system cannot compute**. Whoever writes that sweep reaches for the only calendar in the database, which is the trading calendar, which is a different set of days: the exchange trades on days banks are shut and shuts on days banks trade. `OQ-F2-02` is already circling this without naming the unit; `OQ-SE-03` names it.
- **Verification run in this session:** eleven of eleven gates pass, and `falsify.mjs` reports all eleven failing dirty with five scope cases holding.
- **Files touched:** `docs/plans/P1-SE-trading-calendar.md` (new), `docs/INDEX.md` (one row), `docs/{STATE,SESSION_LOG}.md`. **No `.sql` file, no `packages/` file, no `scripts/` file and no workflow was touched, and no number was claimed in any allocation table.**
