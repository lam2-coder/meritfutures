---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 588: the pointers into `gates.mjs` counted corpus-wide rather than inherited, ADR-330's five repaired, and two that pass both of ADR-388's gates and still cannot be repaired

<!--index: **THE ROW IS [ADR-393](../decisions/ADR-393.md), `proposed` and UNSIGNED. NON-MONEY, TOOLING AND CONTROL OVER THE CORPUS'S OWN POINTERS, `E2` READ OWED.** **THE DISPATCHED POPULATION IS REFUTED AND IT IS LARGER**: 115 citations resolve to `gates.mjs` over 61 files and **84 over 44 files are wrong**, against the 36 handed down, because [ADR-390](../decisions/ADR-390.md) measured only the pointers above its own first insertion and says so. **[ADR-330](../decisions/ADR-330.md)'s FIVE ARE CLASS ONE AND ARE REPAIRED**, G2 derived at `54c6179a` where all five were exact, all five now uniformly 186 lines out. **[ADR-388](../decisions/ADR-388.md)'s THREE CLASSES ARE INCOMPLETE**: two pointers pass G1 AND G2 and are still unrepairable because the subjects they name were DELETED, so there is no coordinate to move to. **THREE ARE CLASS TWO AND STAY WRONG**, two of them inside `ADR-330` itself, pointing at a source comment the commit that wrote them never touched. **24 coordinate tokens repaired across 8 files, every one re-read at primary source after the edit; 6 further repairs forced by this row's own three registry rows and all six listed.** **THE CLONE WAS SHALLOW AND THE REPOSITORY WAS NOT PRESENT AT ALL**: the shallow check was run first, returned `true` at 1 commit, and the base branch had to be fetched before any history question could be asked. No invariant minted, no gate touched, no register grown, no migration taken, `scripts/corpus/**` and `packages/tooling/checks/**` read and never written. The branch is pushed and **no pull request was opened and nothing was merged, by instruction**. -->

---

### 1. The objective

Take the pointers into [`gates.mjs`](../../scripts/corpus/gates.mjs) that [ADR-390](../decisions/ADR-390.md) section 11 derived and could not reach, put each through [ADR-388](../decisions/ADR-388.md)'s two gates rather than inheriting the verdict, repair the ones that are repairable, and refuse the ones that are not.

**One objective. Non-money. The row is [ADR-393](../decisions/ADR-393.md).**

---

### 2. What was measured before the first edit

**The environment did not match the dispatch and that was established first.** `git rev-parse --is-shallow-repository` returned **`true`** at **1** reachable commit, with `main` the only ref present, and the working directory held no checkout of this repository at all. The clone was made by this session, the base branch fetched, and the check re-run: **`false`**, **4,300** commits reachable, `--unshallow` refused as redundant. **Only then was anything derived.**

Base on `c5648f00`, reproduced off each runner's own last line before the first edit: gates **33 of 33**, invariants **35 of 35**, suite **336 files / 8,107 passed / 10 skipped / 0 failed**, typecheck, lint and `format:check` clean.

---

### 3. Done

- **The population derived through the checks' own citation grammar** rather than a grep: **115** citations resolving to `gates.mjs` over **61** files, **52** distinct coordinates, **84** wrong over **44** files, **31** still exact, **11** carrying no path inside the citation token, a form the first pass of this row's own reader missed.
- **[ADR-330](../decisions/ADR-330.md)'s five classified and repaired.** G1 holds on all five; G2 derived at `54c6179a`, where every one of the five coordinates carried its subject and the file ended at the guard's closing line. All five uniformly **186** lines out, which reproduces `ADR-390`'s 129 plus the 57 its own diff added.
- **Two pointers refused for a reason no entry has recorded.** [ADR-098](../decisions/ADR-098.md)'s and [ADR-119](../decisions/ADR-119.md)'s pass G1 and G2 and cannot be repaired: the `build`-key probe and the register entry they name were **deleted from the file**, so the ruling's remedy has no target.
- **Three refused as class two**, two inside `ADR-330` and one in the `ALLOCATION.md` row for `185`. `54c6179a` never touched the file the first two cite, at that commit or its parent, so no reading makes them true on their day.
- **24 coordinate tokens repaired** across `ADR-330`, `ADR-182`, `ADR-185`, `ADR-334`, `ADR-335`, `ADR-336` and `ALLOCATION.md`, each retired coordinate kept in backticks beside the new one and never re-cited with its path.
- **Six repairs forced by this row's own three allocation rows**, in `ADR-312` (four), `ADR-275` and one session log, each classified before it was touched and all six listed in the entry.

---

### 4. Next

The **63** wrong pointers into `gates.mjs` outside this fence, over 35 files: **nine ADRs no entry has named** ([ADR-064](../decisions/ADR-064.md), [ADR-188](../decisions/ADR-188.md), [ADR-272](../decisions/ADR-272.md), [ADR-276](../decisions/ADR-276.md), [ADR-278](../decisions/ADR-278.md), [ADR-279](../decisions/ADR-279.md), [ADR-287](../decisions/ADR-287.md), [ADR-290](../decisions/ADR-290.md), [ADR-297](../decisions/ADR-297.md)), 18 session logs, both registry READMEs, `STATE.md`, two plan documents and `DELTA_MANIFEST.md`. **Section 4 of [ADR-388](../decisions/ADR-388.md) has to be run on each individually and this row ran it on 21.**

---

### 5. Blockers

**None for this row.** The five subjects `ADR-330` names are cited at the same retired coordinates in `STATE.md`, `sessions/README.md` and [session 521](2026-09-05-session-521.md), all class one on the same derivation, and all three files are **append-only to this row**, so the classification exists and the repair does not. **That is a fence limit and not a finding.**

---

### 6. Landmines

- **A shallow clone answers a history question wrongly rather than refusing, and this container had no clone at all.** Anything asking what a file held at a past commit must run the shallow check first and say what it returned. **`--unshallow` was not needed here; a bounded `--depth=1000` fetch of the base branch brought the whole history down.**
- **Adding a row to `ALLOCATION.md` moves every pointer into the tail of that file.** Three rows moved thirteen. Two of the thirteen were exact and had to be repaired outside the fence; the rest were already wrong or were coordinates cited as the subject of a finding, where a repair would falsify the finding.
- **One coordinate can be class one in one document and class two in another.** The `CI-06/derivable-counts` header coordinate is exact-when-written in [ADR-185](../decisions/ADR-185.md) and was 150 lines stale when the `ALLOCATION.md` row for `185` copied it. **G2 is per pointer, never per subject.**
- **A repaired pointer can lead a reader to a line that contradicts the record's own dated figure**, and that is correct behaviour. The `CI-06/retired-constraints` register entry now reads a larger `sites` value than the three entries citing it state. The coordinate is repaired; the figure is not.

---

### 7. Files touched

| File | What |
|---|---|
| [`docs/decisions/ADR-393.md`](../decisions/ADR-393.md) | new |
| [`docs/sessions/2026-09-06-session-588.md`](2026-09-06-session-588.md) | new, this file |
| [`docs/decisions/ADR-330.md`](../decisions/ADR-330.md) | five coordinates repaired across three lines, two class-two pointers LEFT, line count unchanged |
| [`docs/decisions/ADR-182.md`](../decisions/ADR-182.md), [`docs/decisions/ADR-185.md`](../decisions/ADR-185.md) | one and two coordinates repaired, line counts unchanged |
| [`docs/decisions/ADR-334.md`](../decisions/ADR-334.md), [`docs/decisions/ADR-335.md`](../decisions/ADR-335.md), [`docs/decisions/ADR-336.md`](../decisions/ADR-336.md) | four, two and two coordinates repaired, line counts unchanged |
| [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) | four coordinates repaired in place; three rows added, one TAKEN and two RESERVED naming no subject, no branch and no ADR link |
| [`docs/decisions/ADR-312.md`](../decisions/ADR-312.md), [`docs/decisions/ADR-275.md`](../decisions/ADR-275.md), [`docs/sessions/2026-08-30-session-466.md`](2026-08-30-session-466.md) | six pointers this row's own registry rows moved, repaired under the citation-repair exception and listed |
| [`docs/decisions/README.md`](../decisions/README.md), [`docs/sessions/README.md`](README.md) | one generated row each, written by `gates.mjs generate` |
| [`docs/STATE.md`](../STATE.md) | one session section appended at the end of the file |
| [`docs/decisions/ADR-098.md`](../decisions/ADR-098.md), [`docs/decisions/ADR-119.md`](../decisions/ADR-119.md) | **READ AND NOT WRITTEN.** Both pointers pass G1 and G2 and have no repair target |
| `scripts/corpus/**`, `packages/tooling/checks/**` | **READ ONLY.** The file this row repairs pointers into is byte-identical to its base |

---

### 8. Verification

Counts derived at reporting time off each runner's own last line. **`pnpm run verify` and `scripts/corpus/falsify.mjs` were never run.**

| Runner | Base on `c5648f00` | After this diff |
|---|---|---|
| `node scripts/corpus/gates.mjs check` | 33 of 33 | **33 of 33** |
| `node packages/tooling/checks/repo-invariants.mjs` | 35 of 35 | **35 of 35** |
| `pnpm vitest run` | 336 / 8,107 / 10 / 0 | **336 / 8,107 / 10 / 0**, delta ZERO |
| `pnpm run typecheck` | clean | **clean** |
| `pnpm run lint` | clean | **clean** |
| `pnpm run format:check` | clean | **clean** |
| `node scripts/corpus/gates.mjs generate` | | `adr_count` 375 to 376, in place, no line moved |

**`CI-06a` WENT RED ONCE AT 32 OF 33** on a markdown link this row wrote by accident into a registry cell. **The corpus was repaired and the gate was not touched.**
