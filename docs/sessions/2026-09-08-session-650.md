---
status: approved
depends_on: [README.md]
last_updated: 2026-09-08
---

## 2026-09-08 - Session 650: the generated expressions are executed against a live database rather than compared as text, and all six compute what the corpus says they mean

<!--index: **THE ROW IS [ADR-455](../decisions/ADR-455.md), `proposed` and UNSIGNED. MONEY PATH, `E2` READ OWED**, inheriting [ADR-443](../decisions/ADR-443.md)'s, [ADR-452](../decisions/ADR-452.md)'s and [ADR-454](../decisions/ADR-454.md)'s. [ADR-443](../decisions/ADR-443.md) section 8 items 1 and 2 taken: that entry compared every `GENERATED ALWAYS AS` clause as TEXT and stated its own limit, *"both sides are STRINGS and neither is evaluated"*. **A PostgreSQL 16.13 cluster was initialised in this container, all 75 migrations applied forward-only from empty under `ON_ERROR_STOP` (exit 0), and every generated column given rows that make its arithmetic observable.** **THE POPULATION IS SIX AND IT WAS DERIVED THREE WAYS THAT AGREE**: the migration text, [`schema.ts`](../../packages/db/src/schema.ts), and `pg_attribute.attgenerated` on the installed database, all STORED, of which **2** are `*_cents`. **ALL SIX AGREE WITH WHAT THE CORPUS SAYS THEY MEAN AND THAT IS THE RESULT.** `intraday_movement_cents` is `equity - opening` signed over five regions including through-zero; `delta_cents` is `ours - theirs`; `rcr_bp` holds `0049`'s stated bound to the integer, `214748x` giving `2147480000` bp and `214749x` raising `22003`, **which [ADR-443](../decisions/ADR-443.md) section 8 item 2 names as unchecked**; `pre_identity_auth` is neither silenceable nor exempt, `0029`'s SMS-pumping argument; a fifth digest raises `23502` naming `cadence`, exactly what `0040` records having executed. **THREE FACTS NO DOCUMENT COMPARISON CAN SEE ARE ASSERTED**: recomputation on UPDATE on three tables, agreement between the generated delta and the CHECK that restates it, and that the money subtraction raises `22003` rather than wrapping. **`428C9` CONFIRMS [ADR-454](../decisions/ADR-454.md) INDEPENDENTLY** on two columns that row did not use. **SIX FINDINGS AND NO COLUMN DISAGREED**: [ADR-443](../decisions/ADR-443.md)'s *"nobody has executed one"* was false when written because [`probe_reserve_coverage.sql`](../../scripts/db/probe_reserve_coverage.sql) already executed `rcr_bp`; its sixth column is no longer unreachable because `live_account_state` is now transcribed; this row's own money-column count was wrong by one and is corrected; a single-line grep under-counts this population by a third; `DELTA_MANIFEST` has no section 27; and `rcr_bp`'s truncation direction is unfalsifiable through its own constraints. **14 SEEDED PERTURBATIONS, ONE PER ASSERTION, EACH WATCHED FIRING**, plus the counterfactual against `0001`..`0049` dying at the census with exit 3, plus the workflow step removed and watched turning `CI-06h` and `CI-06s` red at **31 of 33**. **ONE FENCE DEPARTURE DECLARED**: a 37-line needle insertion moved five `gates.mjs` citations in [`data-model-columns.mjs`](../../scripts/corpus/data-model-columns.mjs) by exactly 37, all five exact at the base, all five repointed at the content they name. **NO MIGRATION, NO `src/` FILE, NO TEST FILE, NO EXISTING PROBE EDITED, NO GATE WEAKENED, NOTHING SIGNED, NO PR OPENED.** Suite **344 files, 8619 passed, 11 skipped, 8630 total, 0 failed**, against a base of **344 / 8619 / 11 / 0** measured on `c3830d12` in this same tree, delta ZERO on every axis; **33 of 33** gates, **35 of 35** invariants, typecheck, lint and format clean. -->

### 1. The objective

Take [ADR-443](../decisions/ADR-443.md) section 8 items 1 and 2. That entry compared every `GENERATED ALWAYS AS` expression in [`schema.ts`](../../packages/db/src/schema.ts) against the migrations, all five agreed, and it stated plainly that both sides were strings and neither was evaluated. Row 452 proved a row can run a real PostgreSQL in its own container, so the limit is reachable. **Execute the expressions against a live database and compare COMPUTED VALUES, money first, and wire the result as a probe on `CI-06h`.**

### 2. Done

- **The container measured and reported. THERE WAS NO REPOSITORY AT ALL**, for the sixth consecutive wave. `/home/user` was empty and `git rev-parse` reported *"not a git repository"*. The repository was located through the session's own repository listing, attached with push access, cloned, and `c3830d12` resolved in it off `claude/wave-454s`.
- **The base reproduced at `c3830d12` before the first edit**: **33 of 33** gates and **35 of 35** invariants.
- **A PostgreSQL 16.13 cluster initialised and started in this container.** The server refuses to run as `root`, so an unprivileged user was created and `initdb` run under it on a unix socket at port 5433. This is [ADR-452](../decisions/ADR-452.md)'s recipe.
- **All 75 migrations applied forward-only from empty under `ON_ERROR_STOP`, in filename order, exit 0.** Numbers `0001`..`0082` with 7 reserved gaps. Object counts derived from the database: tables **118**, indexes **414**, CHECK constraints **518**.
- **THE POPULATION DERIVED THREE WAYS AND NOT QUOTED.** The migration text gives 6, [`schema.ts`](../../packages/db/src/schema.ts) gives 6, and `pg_attribute` on the installed database gives **6, all STORED, zero VIRTUAL**. `pgboss` contributes none: [`0079:329`](../../packages/db/migrations/0079_pgboss_job_store.sql) copies a table with `INCLUDING GENERATED` that declares none, which was checked rather than assumed.
- **MONEY FIRST, AS THE ROW REQUIRED.** `live_account_state.intraday_movement_cents` executed over a gain, a loss, a flat day, a day through zero into negative equity, and both operands negative; `reconciliations.delta_cents` over agreement, holding more, holding less, and both sides negative. **Every value read back out of PostgreSQL after PostgreSQL computed it.**
- **`0049`'s OVERFLOW BOUND CHECKED, WHICH [ADR-443](../decisions/ADR-443.md) SECTION 8 ITEM 2 NAMES AS UNCHECKED.** `214748x` gives `2147480000` bp and `214749x` raises `22003 integer out of range`. The bound is exact to the integer.
- **THREE ASSERTIONS NO DOCUMENT COMPARISON CAN MAKE.** Recomputation on UPDATE on three tables; the generated delta agreeing with `reconciliations_status_matches_delta`, which is a relation between two objects rather than a property of one; and `bigint` overflow raising rather than wrapping, so a maximal loss cannot render as a gain.
- **[`probe_generated_column_values.sql`](../../scripts/db/probe_generated_column_values.sql) written**, 9 successes before the first rejection on [`probe_reserve_coverage.sql`](../../scripts/db/probe_reserve_coverage.sql)'s precedent, 5 rejections after. Its SUCCESS 1 derives the population from the catalog and pins all six by name.
- **Wired into `CI-06h` and pinned in [`gates.mjs`](../../scripts/corpus/gates.mjs)'s needle list**, in the same commit that wires it. Removing the step was watched turning `CI-06h` and `CI-06s` red at **31 of 33**, and the workflow restored byte-identical.
- **14 SEEDED PERTURBATIONS, ONE PER ASSERTION, EACH WATCHED FIRING**, the committed file restored byte-identical after each. Plus the counterfactual: applied against `0001`..`0049` the probe dies in SUCCESS 1 at the census, reading five, exit 3.
- **[DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md) section 60 written**, the first section there that records no landing, with the install and execution transcripts and a section-number registry row.
- **ONE FENCE DEPARTURE TAKEN AND DECLARED.** The 37-line needle insertion moved five `gates.mjs` citations in [`data-model-columns.mjs`](../../scripts/corpus/data-model-columns.mjs) by exactly 37. All five were verified exact at `c3830d12` against `git show`, and all five repointed at the content they name and read back off the file. The runner flagged three; two more drifted silently and were repaired on [ADR-303](../decisions/ADR-303.md)'s precedent.

### 3. Next

- **[`scoped-db.test.ts:3592`](../../packages/db/test/scoped-db.test.ts) carries a comment that is now false**, saying `live_account_state` is *"transcribed NOWHERE"* while [`schema.ts`](../../packages/db/src/schema.ts) transcribes it and a newer block one file over compares it. **Out of this fence** and owed to whichever of PR #739 and wave 454s lands last.
- **Nothing asserts that no statement in this tree WRITES one of the six.** [ADR-443](../decisions/ADR-443.md) section 11 item 3 unchanged. [`generated-column-writes.mjs`](../../packages/tooling/checks/generated-column-writes.mjs) exists and was out of fence, so whether it closes this is unanswered here.
- **The four money-path `E2` reads remain owed**, this row's and its three inherited.

### 4. Blockers

None. Every deliverable landed and every leg is green.

### 5. Landmines

- **A SINGLE-LINE GREP FOR `GENERATED ALWAYS AS (` FINDS 4 OF 6.** Two of the six put a newline between `AS` and `(`. This row's first derivation made exactly that mistake. **The under-count is silent and in the reassuring direction**, and any future census should read `pg_attribute` or use a multiline pattern.
- **COUNTING MONEY COLUMNS BY EYE IS THE WRONG INSTRUMENT.** Three files were written saying *"three of the six are integer cents"*; `attname LIKE '%\_cents'` says **two**, because `rcr_bp` is basis points. Corrected in a commit that says so.
- **ANY INSERTION IN `gates.mjs`'s NEEDLE LIST MOVES FIVE CITATIONS IN `data-model-columns.mjs`.** They sit around line 9400 and later, far below the list. A row adding a needle should expect `RI` to go red and should budget the repair.
- **`DELTA_MANIFEST` HAS NO SECTION 27**, running 26 then 28. A row picking "the next section number" by reading the maximum is fine; a row assuming the numbering is dense is not. This row's probe cited a section 27 in its first draft.
- **`prettier` DISAGREES WITH ITSELF ACROSS VERSIONS.** `npx prettier` fetches a newer release that reports `gates.mjs` unformatted; the repo's pinned binary at `./node_modules/.bin/prettier` reports it clean, and the pinned one is what `format:check` runs. **Do not reformat a file on the strength of an `npx` reading.**
- **RESERVE-C1 makes `reserve_coverage_snapshots` fixtures interdependent.** A coverage row's `reserve_cents` must equal the `treasury_balances` attestation it names, so each coverage figure needs its own anchor carrying that number. This cost one wrong attempt.

### 6. Files touched

[`scripts/db/probe_generated_column_values.sql`](../../scripts/db/probe_generated_column_values.sql) (new), [`.github/workflows/corpus.yml`](../../.github/workflows/corpus.yml), [`scripts/corpus/gates.mjs`](../../scripts/corpus/gates.mjs), [`scripts/corpus/data-model-columns.mjs`](../../scripts/corpus/data-model-columns.mjs), [`packages/db/DELTA_MANIFEST.md`](../../packages/db/DELTA_MANIFEST.md), [ADR-455](../decisions/ADR-455.md), [ALLOCATION](../decisions/ALLOCATION.md), [`docs/decisions/README.md`](../decisions/README.md), [`docs/sessions/README.md`](README.md), this file, and generated spans in [`docs/STATE.md`](../STATE.md). **[`docs/INDEX.md`](../INDEX.md) needed no change and was not touched**; `CI-06c` passes without one.

**No migration. No `src/` file in any package. No test file. No existing probe edited.**
