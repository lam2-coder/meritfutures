---
status: approved
depends_on: [README.md]
last_updated: 2026-08-25
---

## 2026-08-25 - Session 215: the four tables the registry cannot hold, under ONE two-identity ruling
<!--index: **[ADR-106](../decisions/ADR-106.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH and the `E2` read owed. RULED in six clauses whose one-line version is that A ROW WHOSE SUBJECT IS A PAIR OF IDENTITIES BELONGS TO BOTH AND IS SCOPED TO NEITHER.** `pair` is a fifth `ScopeClass` member and [`scope.ts`](../../packages/db/src/scope.ts)'s own claim that *"the question has exactly these four answers on this schema"* was **FALSE about migrations that predate it**: `0002_identity.sql` created `identity_links` with two `NOT NULL REFERENCES identities(id)` columns on the day the schema was written, and **exactly 4 tables in 111 declare more than one**, measured by reading every `CREATE TABLE` body. `identity_links`, `dedupe_matches` and `attributions` enter as `pair` and `otp_challenges` as `firm`; registry **100 to 104 of 111** at 1 root, 40 owned, 19 derived, 3 pair, 41 firm. **THE CLASS DOES NOT MAKE THEM SCOPED-READABLE AND THAT IS THE RULING RATHER THAN A LIMITATION.** The predicate exists and is one line -- `columnA = $1 OR columnB = $1` returns precisely the rows that are one person's -- and **what it returns is the problem**: every row carries the OTHER party's identity uuid out of a `NOT NULL` column. **THE CORPUS HAD ALREADY RULED THIS AT ARITY THREE AND IT IS APPLIED RATHER THAN INVENTED**: `correlation_groups` is `firm` and its own `why` says *"the reason it MUST NOT be traversed is stronger than the reason it cannot: returning the row to each member would tell every member which OTHER accounts the detector grouped them with"* -- and at arity two the disclosure is **WORSE**, because a member of a group learns a set and a party to a pair learns precisely who. **A SCOPED READ IS A FILTER AND NOT A PROJECTION** is the sentence the clause rests on. **THE COUNTER-ARGUMENT IS ANSWERED RATHER THAN IGNORED**: `risk_flags` and `identity_signals` are registered and carry detector material, and `risk_flags`' `why` that *"REGISTERING A TABLE MAKES IT READABLE AND NOTHING ELSE"* is right and undisturbed -- every one of those names exactly ONE person, and what a pair row adds is a SECOND SUBJECT arriving as the tenancy column itself. **A `pair` KEY IS THE FIRST `TableKey` THAT IS A MEMBER OF NEITHER NARROW TYPE**: excluded from `ScopedTableKey` for the disclosure and from `FirmTableKey` because [ADR-102](../decisions/ADR-102.md) clause 5 gives `firmDb()` no reason on the ground that no identity is at risk, and here two are -- so the two key sets **no longer partition `TableKey`** and `systemDb(reason)` is the only door, which is what [M06](../plans/M06-admin-ops-console.md)'s console and [M07](../plans/M07-risk-abuse.md)'s detectors, **the only readers the corpus names**, actually need. **`INV-M7-09` IS THE SENTENCE THAT LOOKS LIKE A COUNTER-EXAMPLE AND IS NOT**: a trader may CONTEST a link and the contested link is visible **to the admin who acts on it**, and M07 says the suppression is written *"through the `SECURITY DEFINER` function ... never by the application role"*. **`GET /affiliate/stats` RETURNS COUNTS AND CENTS AND NO REFERRAL ROW**, so nothing in the contract wants the disjunction either. **`otp_challenges` IS THE FIRST `firm` ROW WHOSE REASON IS TIMING RATHER THAN OWNERSHIP** -- every other firm table holds a row no identity will EVER own and this one holds a row whose identity does not exist YET -- and it is **the first registered table to exercise [ADR-103](../decisions/ADR-103.md)'s second fold member**, which closes [session 214](2026-08-25-session-214.md)'s named landmine that the member ran on no registered table. **EIGHT SEEDS WATCHED and the first is the one worth carrying**: `identity_links` declared `owned` on `identity_a` leaves `typecheck` at **exit 0 with zero `error TS`** and turns **EXACTLY TWO assertions red, both of them this entry's**, so every mechanical check that existed before ADR-106 accepts the wrong answer -- watched happening rather than predicted. A `derived` rule through a pair table compiles and turns **nine** red with the chain one naming the class; both narrow doors are `TS2345` on a pair key while `systemDb('operator-console').rows('attributions')` in the same file **compiles**. **THREE THINGS REPORTED RATHER THAN TAKEN AND THE FIRST CONTRADICTS THE PLAN: `affiliate_commissions` is NOT unblocked**, its reason having moved one class along -- the rule now COMPILES where it could not be written at all, and throws. **TWO FILES OUTSIDE THE FENCE, forced and refusal-only**: a fifth class member makes two exhaustive switches non-exhaustive and a predicate is built in exactly one file, and the fence named session 213 as `scoped-db.ts`'s holder **and session 213 merged**; no open remote ref holds either file, checked across 231. **SESSION 220 INHERITS [SESSION 216](2026-08-25-session-216.md)'s OPEN QUESTION WITH A SECOND TABLE ATTACHED.** **A CITATION IN THIS SESSION'S OWN BRIEF DOES NOT RESOLVE**: the prompt says *"ADR-092 section 9 named three times"* and section 9 is `Consequences` naming the four ORPHANS -- the class is section 3, once. **`schema.ts`'s HEADER SENTENCE HANDED OVER BY SESSION 214 IS REPAIRED** and its counts move 99 to 104, twelve to seven and TEN drifted tables to ELEVEN, the last replayed rather than incremented. `scoped-db.test.ts` **231 to 246**, `write-accessor.test.ts` **32 to 33**, suite **1,950 to 1,966 passed / 1 skipped**, ADR-103's column comparisons **1,183 to 1,240**, `typecheck` exit 0, lint and format clean, invariants **9 of 9**, gates **30 of 30**, `verify` exit 0. **Sessions 218 and 219 CAN PROCEED.** **NOT MERGED** -->

- **Objective:** take ONCE the per-table ruling [ADR-092](../decisions/ADR-092.md) named and did not take, and register the four tables [P3 wave 3](../plans/P3-wave3-modules.md) needs and the registry cannot hold: `identity_links`, `attributions` and `dedupe_matches` under a SINGLE two-identity ruling, plus `otp_challenges` under [ADR-103](../decisions/ADR-103.md)'s widened fold. **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **One ADR: [ADR-106](../decisions/ADR-106.md), reserved before dispatch.**
- **Done:** [ADR-106](../decisions/ADR-106.md) landed at `status: proposed` with the approval line UNSIGNED, and the class with it. [`scope.ts`](../../packages/db/src/scope.ts) gains `PairRule`, `PairTableKey` and a narrowed `ScopedTableKey`; [`schema.ts`](../../packages/db/src/schema.ts) gains four transcriptions and a repaired header; [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) gains six pair assertions plus one that closes session 214's fold landmine, **231 to 246**. Registry **100 to 104 of 111**, and **none of the four is reachable through `scopedDb`**. Suite **121 files / 1,950** to **121 files / 1,966 passed / 1 skipped**.
- **Next:** **The `E2` line-by-line read is owed on [ADR-106](../decisions/ADR-106.md), on [`scope.ts`](../../packages/db/src/scope.ts), on [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) and on both test files**, and the approval clause is six commands so the read can execute it rather than trust it. **Sessions 218 (`P3-j`) and 219 (`P3-k`) can proceed**: `otp_challenges` is `firm`, so a request handler with no identity reaches it through `firmDb()` / `firmTx`, and neither session needs a pair table. **`affiliate_commissions` needs a ruling nobody has**: what a row DERIVED from a two-party row belongs to. **`identity_merges` is registrable and belongs to whoever needs it.** **Session 220 needs the ruling session 216 asked for and now for two tables.**
- **Blockers:** none for this session. **Two forced fence calls**, section 10 of the entry.
- **Landmines:**
  - **THE DISJUNCTION IS ONE `case` AWAY AND THAT IS THE HONEST DESCRIPTION OF HOW REVERSIBLE THIS RULING IS.** `scopePredicate`'s `pair` branch throws; replacing the throw with `or(eq(a, id), eq(b, id))` and deleting `PairTableKey` from `ScopedTableKey` makes all three tables trader-readable in about ten lines, with the suite still green **except** for the two door assertions. **The control here is the CLASS and not the difficulty**, and a session that wants it should have to write an entry rather than a diff.
  - **CLAUSE 2 IS A JUDGEMENT AND CLAUSE 1 IS NOT, AND ONLY CLAUSE 2 IS WORTH ARGUING ABOUT.** That an `owned` rule on one of two columns is wrong is arithmetic. That the right remedy is to refuse the read rather than to build the disjunction rests on three checkable things and one thin one: `correlation_groups`' `why` (quoted, in the tree), the absence of any trader-facing reader in `docs/` (an absence is a fact about a corpus at a commit), the contract's affiliate surface being aggregates (quoted) -- and **`INV-M7-09`'s dispute path, where the corpus is thinnest, because M07 describes the dispute as a thing that HAPPENS without ever saying through what.**
  - **`ScopedTableKey ∪ FirmTableKey ≠ TableKey` FROM HERE, AND CODE WRITTEN BEFORE TODAY MAY ASSUME OTHERWISE.** [`write-accessor.test.ts`](../../packages/db/test/write-accessor.test.ts) asserted that partition explicitly and its failure is what surfaced this; **anything that assumed it silently will not announce itself.** The suite asserts the three-way split now and `PAIR_KEYS.length > 0` beside it, so a future collapse back to two doors is red.
  - **A `pair` TABLE IS UNREACHABLE TO EVERY `apps/api` REQUEST HANDLER.** A handler is neither `'nightly-batch'` nor `'operator-console'`, and unlike a `firm` table there is no `firmDb()` to fall back to. This is [ADR-102](../decisions/ADR-102.md) section 7's standing hole arriving on a class with no third door, and **`attributions` is written inside checkout**, so it is session 220's problem in a concrete shape.
  - **`refuseTenancyColumn` NOW LOOPS AND THE SINGULAR READER RETURNS `undefined` FOR TWO DIFFERENT REASONS.** `tenancyColumn` is `undefined` for `firm` (no columns) and for `pair` (two), and only `tenancyColumns` distinguishes them. **The one caller that STAMPS a column takes `OwnedTableKey`**, so it can never be handed a table it would have to guess about -- but a future caller that reads the singular and assumes non-`firm` implies a string will be wrong on three tables. The suite asserts the three-way shape of that reader by class.
  - **THE `pair` CLASS'S `why` FIELDS CARRY THE DISCLOSURE ARGUMENT AND NOTHING MECHANICAL CHECKS IT.** Both columns being `NOT NULL`, distinct, and declared against `identities(id)` are all asserted; **that returning the row would disclose the counterparty is prose**, and [ADR-042](../decisions/ADR-042.md) already ruled prose is not a control. What stands in its place is the TYPE: the tables are not in `ScopedTableKey`, so the disclosure cannot happen through the accessor whatever the comment says.
  - **`falsify:ci` WAS NOT RUN IN THIS SESSION AND NOTHING WAS `git add -A`ed AFTER ANY SEED.** Every seeded file was restored from a pre-seed copy taken before the first seed, the one seeded source file was deleted, and `git status --porcelain` was read empty after each restore and before each commit.
- **Files touched:** [`packages/db/src/scope.ts`](../../packages/db/src/scope.ts) (the class, the two key types, four rules, the rewritten refusal docblock), [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts) (four transcriptions, the header repair session 214 handed over), [`packages/db/src/scoped-db.ts`](../../packages/db/src/scoped-db.ts) (**outside the fence, forced**: a `case 'pair'` that throws and `tenancyColumns`), [`packages/db/src/index.ts`](../../packages/db/src/index.ts) (**outside the fence, forced**: one export), [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts), [`packages/db/test/write-accessor.test.ts`](../../packages/db/test/write-accessor.test.ts) (**outside the fence, forced**: its own partition assertion), [ADR-106](../decisions/ADR-106.md) (new), [ALLOCATION](../decisions/ALLOCATION.md) (its reserved row amended in place), [decisions/README](../decisions/README.md) and [INDEX](../INDEX.md) (generated), [STATE](../STATE.md) (one appended `##` section plus generated count spans), [sessions/README](README.md) (row 215 struck, entry row generated), this file. [ADR-092](../decisions/ADR-092.md), [ADR-094](../decisions/ADR-094.md), [ADR-101](../decisions/ADR-101.md), [ADR-102](../decisions/ADR-102.md), [ADR-103](../decisions/ADR-103.md), [M04](../plans/M04-trader-portal.md), [M06](../plans/M06-admin-ops-console.md), [M07](../plans/M07-risk-abuse.md), [M08](../plans/M08-affiliate-system.md), [M19](../plans/M19-kyc-identity.md), [API_CONTRACT](../architecture/API_CONTRACT.md) and all 47 migrations were **read and none was written**.

### What was NOT done, named rather than implied

**No migration, no migration number claimed, no `CI-06` letter, no gate register row, no `VG` row, no `RI-nn`, no `SD-nn`, no `OI-nn`, no fixture, no edge-case entry, no route, nothing under [`apps/`](../../apps/api/src/server.ts), no dependency, no `build` script, and no edit to [`docs/plans/`](../plans/P3-wave3-modules.md), which this fence makes read only.**

**`affiliate_commissions` IS NOT REGISTERED AND THE PLAN SAYS IT FOLLOWS MECHANICALLY.** [P3 wave 3](../plans/P3-wave3-modules.md) section 15 says it *"follows `attributions` mechanically and needs no ruling of its own"*. **That is false under this ruling and the derivation is in the entry**: its only path to an identity is `attribution_id`, and a chain terminates at `owned` or at `root`. Before this entry the rule could not be written at all; after it, it compiles and throws. **The stop condition said FOUR registrations and four landed.**

**`identity_merges` IS NOT REGISTERED.** It is the fourth table of this shape and the class now covers it, so its refusal in [`scope.ts`](../../packages/db/src/scope.ts) is **discharged and replaced by a sentence saying so**. [ADR-092](../decisions/ADR-092.md)'s rule is that the first session that NEEDS a table registers it; [M18](../plans/M18-graduation-track.md) names it and this session does not need it.

**`trading_calendar` IS NOT REGISTERED.** [Session 214](2026-08-25-session-214.md) made it registrable alongside `otp_challenges` and this fence does not name it. It is [M01](../plans/M01-rules-engine.md)'s.

**THE DISJUNCTION IS NOT BUILT.** `scopePredicate`'s `pair` branch throws. That is clause 2 and it is the entry's central judgement rather than an omission.

**[ADR-101](../decisions/ADR-101.md) SECTION 8's BLIND `firm` ASSERTION IS STILL NOT REPAIRED.** It reads the `CREATE TABLE` rather than the fold and misses `admin_actions.on_behalf_of_identity_id`. **It matters slightly more after this entry** -- the `firm` class now takes a fourth table on a timing argument -- and it is still a ruling about what `firm` PROMISES rather than about any one table, and still not this entry's subject.

**[`index.ts`](../../packages/db/src/index.ts)'s HEADER SAYS *"SEVEN TABLES OF 111 ARE REACHABLE"* AND IS NOT REPAIRED.** It was true at [ADR-084](../decisions/ADR-084.md) and has been stale for fourteen sessions. This session touches that file for one export line and does not rewrite a paragraph it does not own, on [session 206](2026-08-25-session-206.md)'s precedent.
