---
status: approved
depends_on: [README.md]
last_updated: 2026-09-05
---

## 2026-09-05 - Session 547: the breaker gets four of its five ports, and the day it stamps has no synchronous source

<!--index: **THE ROW IS [ADR-352](../decisions/ADR-352.md), `proposed` and UNSIGNED.** **ALL SIX DISPATCHED CLAIMS REPRODUCED**, stated because two rows in this wave were sent claims that did not. `BreakerIo` has FIVE members; [`apps/worker/src/breaker/adapter.ts`](../../apps/worker/src/breaker/adapter.ts) serves FOUR over [ADR-165](../decisions/ADR-165.md)'s one door. **THE FIFTH IS THE EVENT SINK AND IT IS UNREACHABLE RATHER THAN UNWRITTEN**: `RI-04` plus `node-linker=isolated` put the only composed writer one deployable over, and `EVENT_CATALOGUE`'s **ten** names carry no `breaker.` name for [ADR-159](../decisions/ADR-159.md) clause 1 to admit. **AND THE COST IS THE WHOLE RUN RATHER THAN ONE PLAN**, which is worse than [ADR-349](../decisions/ADR-349.md)'s detector case: `evaluate.ts` holds ONE transaction ([ADR-006](../decisions/ADR-006.md)) and catches nothing, and the FIRST evaluation emits for every active plan because `from_state` is null there, so a deployment holding the composed value writes NO `plan_breaker_state` row on its first night. **THE WRITING FORCED A RULING THE DISPATCH DID NOT ANTICIPATE**: `tradingDayOf` is SYNCHRONOUS and this deployable's only anchored trading day is `anchorLastClosedDay`, which is not, so the day is anchored ONCE before the value exists and `postgresBreakerIo` is the only `async` io factory in the barrel. Deriving the day from the instant was REFUSED under [ADR-146](../decisions/ADR-146.md) clause 4; widening the port was NOT TAKEN and is recorded as an option because `DigestIo` carries the identical member and row `354` meets the same wall. The same instant becomes `now()` and `tradingDayOf` REFUSES any other rather than ignoring its argument. **THE JOB IS NOT SCHEDULED AND THE REGISTRY ROW NOW NAMES THREE BLOCKERS OF WHICH ONE IS AN ADAPTER**: the sink; `OQ-M6-02`'s unstated minimum sample, which declines BEFORE a transaction opens or the clock is read; and **A THIRD THAT IS NEW AND IS RULED NOWHERE**, that `0016`'s `PRIMARY KEY (plan_id, evaluated_on)` plus a plain insert with no upsert plus `CRON_INVENTORY`'s DAILY cadence collide on every non-session day. **`ports.ts`'s EXISTING CONCURRENT-RUN RULING DOES NOT REACH IT**: that sentence holds because both runs *"computed the same day from the same rows"*, and across a weekend the second computes the same day from DIFFERENT rows, since the window's lower bound is an instant while the key is a day. **THE EVENT-NAME DIVERGENCE IS NAMED AND NOT RULED, AND IT IS FOUR-WAY RATHER THAN THREE**: `M06:265` carries `breaker.state_changed` marked NEW with the code's exact eight fields, so `ports.ts` transcribed an approved module plan rather than inventing a name, and what never happened is the fold into `EVENTS.md`, which carries `circuit_breaker.tripped`/`.reset` instead. **THE TWO ARE NOT INTERCHANGEABLE**: the registry payload has NO `sample_size`, which `AS-M6-02` makes required, and a tripped/reset pair cannot express `insufficient_data`, which `GS-113` says must never be alarmed. Pinned as assertions in case 6.2 so it cannot close quietly on one side. **NO THRESHOLD, CAP OR INTERVAL INVENTED**: the one number used, the daily 09:00 CT cadence, is read from `CRON_INVENTORY`. Four open questions recorded with their options. **SIX SEEDED DEFECTS EACH WATCHED RED AND EACH RESTORED GREEN**: a `tradingDayOf` derived from the instant (4 failed), one ignoring its argument (1), a sink that returns (2), an unenforced table union (1), a swallowed calendar refusal (2), and a dropped barrel leg (1, in `breaker.test.ts` case 8.1). **THE FALSIFICATION RUNS BOTH WAYS**: case 5.1 loses the whole run when a plan changes state, and **case 5.3 COMMITS when none does**, which is the case that tells "refuses when an alert is owed" apart from "never works". **RI-13 CAUGHT THIS ROW'S OWN ADR MID-SESSION** for lacking its founder block, and it was written rather than the gate loosened. **ONE STALE SENTENCE FOUND AND LEFT**: `batch/adapter.ts`'s `EVENT_SINK_BLOCKER` says `apps/worker` declares two workspace dependencies and it declares FOUR; the conclusion is untouched because none is an `apps/*` package, and `breaker/adapter.ts` states the count correctly rather than copying it. **THE SESSION ARRIVED WITH NO REPOSITORY AT ALL**: `/home/user` was empty and the tree was attached and cloned by hand before anything could be measured. **SESSION NUMBER DERIVED OVER EVERY PUSHED REF**, not over `main`: maximum in use **546**, so **547** is the next number ABOVE the maximum and the `537`, `538`, `540` and `541` gaps are deliberately left, since an unpushed sibling may be standing in one. `ADR-352.md` exists on no pushed ref and rows `353` to `355` are left reserved exactly as the base wrote them. **NO SQL, NO MIGRATION, NO GATE, NO INVARIANT, NOTHING WEAKENED, NO TEST SKIPPED, NO FENCE WIDENED.** Counts derived at reporting time off each runner's own last line: suite **323 files / 7,856 passed / 10 skipped / 0 failed** against a `c5356e60` baseline reproduced BEFORE the first edit of **322 / 7,839 / 10 / 0**, **A DELTA OF +1 FILE AND +17 CASES MEASURED BY REMOVAL RATHER THAN INFERRED**, with skips unchanged. Gates **33 of 33** after `generate`, invariants **35 of 35**, typecheck, lint and `format:check` all 0, `git diff --stat origin/main -- packages/db/migrations scripts/db` **EMPTY**. Barrel legs counted after the edit at **34**, derived by running case 8.1's own regex over `src/index.ts` rather than predicted. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `352`: re-derive the dispatched claims about `apps/worker/src/breaker/evaluate.ts`, write its adapter, and take the rulings the writing forces. **One objective.**

The adapter is a small file. The work was the re-derivation, the trading-day finding, and the three rulings.

---

### 2. What was re-derived before the first edit

**The tree had to be created before anything could be measured.** `/home/user` was empty, `git status` reported "not a git repository", and the session had no source attached. `lam2-coder/meritfutures` was attached and cloned by hand, then `pnpm install --frozen-lockfile` was run because the clone arrives with no `node_modules`. **A check that cannot run is not a check that passed.**

| The dispatch's claim | Held |
|---|---|
| `BreakerIo` has five members: `transact`, `terms`, `events`, `now`, `tradingDayOf` | **YES**, at `ports.ts:438` |
| `UNWIRED_BREAKER_IO` is its only inhabitant | **YES.** The only other sites are a port parameter and a `test/` fixture |
| `evaluateBreaker` has no caller: three occurrences under any `src/` | **YES.** Declaration, barrel, registry row |
| `BreakerEventPort` is one method taking the transaction on purpose | **YES**, citing `ADR-006` in terms |
| The code emits `breaker.state_changed`; the catalogue's ten do not carry it; `EVENTS.md` carries `circuit_breaker.*` | **YES, ALL THREE**, and a fourth party was found |
| `ADR-348` registered `event-sink-caller`; `RI-04` plus `node-linker=isolated` is the fence | **YES** |

**SIX OF SIX.** Baseline reproduced before the first edit: **322 files / 7,839 passed / 10 skipped / 0 failed**, gates **33 of 33**, invariants **35 of 35**.

---

### 3. What was ruled

1. **Four of five members served**, `events` refused with its blocker measured, and the refusal costs the whole run rather than one plan. [ADR-352](../decisions/ADR-352.md) sections 2 and 3.
2. **The clock is one anchored instant**, because `tradingDayOf` is synchronous and its only honest source is not. Section 4. **This was not in the dispatch and is the finding the writing produced.**
3. **The job is NOT scheduled**, on three blockers of which the third is new. Section 5.
4. **The event-name divergence is named and cited and not ruled.** Section 6.

---

### 4. Files touched

| File | What |
|---|---|
| `apps/worker/src/breaker/adapter.ts` | **NEW.** The adapter |
| `apps/worker/test/breaker-adapter.test.ts` | **NEW.** Seventeen cases, six seeded defects each watched red |
| `apps/worker/src/index.ts` | One barrel leg, and its `WORKER_BARREL_LEGS` line |
| `apps/worker/test/breaker.test.ts` | Case 8.1's deliberate literal, 33 to 34, derived after the edit |
| `apps/worker/src/schedule.ts` | The breaker row names three blockers, old sentence kept beside its correction per `RI-14` |
| `docs/decisions/ADR-352.md` | **NEW** |
| `docs/decisions/ALLOCATION.md` | Row `352`, replacing the reservation |
| `docs/decisions/README.md`, `docs/sessions/README.md`, `docs/STATE.md` | The registry rows |

**No migration, no SQL, no gate, no invariant, no fence.**

---

### 5. Next, blockers, landmines

**NEXT.** Nothing in this row is unfinished. The job runs when a founder answers `OQ-M6-02` and rules what `evaluated_on` means, in that order; the sink is `P5-b`'s and is a different fence.

**BLOCKERS.** All four are the founder's and are ADR-352 section 8: the minimum sample, the meaning of `evaluated_on`, the event name, and the CUSUM's two parameters.

**LANDMINES.**

- **The primary key collides on non-session days.** ADR-352 section 5. Anyone who schedules this job without ruling `evaluated_on` first will lose a whole night's run every weekend, and `ports.ts`'s existing sentence about concurrent runs reads like a ruling on it and is not.
- **`postgresBreakerIo` is `async` and no other io factory in this barrel is.** A caller that forgets to await it gets a promise where a `BreakerIo` is expected.
- **The third argument is a seam for this suite only** and case 4.1 asserts no `src/` module passes it. A no-op sink there would make every evaluation commit with its alert dropped.
- **`batch/adapter.ts`'s dependency enumeration is stale.** It says two and there are four. The conclusion holds; the sentence does not.
