---
status: approved
depends_on: [README.md]
last_updated: 2026-08-20
---

## 2026-08-20 - Session 108: WAVE-04 W1, `ADR-072` and the fixture status registry (non-money)
<!--index: WAVE-04 `W1`: `ADR-072`, fixture writability as three checkable conditions, and `39-fixture-status-and-blockers.md`, a status and a stated blocker for all 316 registry rows -->

- **Objective:** rule what makes a golden fixture WRITABLE, as three conditions that can be checked rather than judged, and give every one of the <!--gen:gs_count-->316<!--/gen--> registry rows a status and, where it has no fixture, a stated blocker from a closed vocabulary.
- **Done:** **[ADR-072](../decisions/ADR-072.md)** (`status: proposed`, approval line unsigned) and **[section 39](../testing/golden-scenarios/39-fixture-status-and-blockers.md)**, one table, 316 rows, in numeric order, with a status on every row and a blocker plus a supporting citation on all 274 that have no fixture. Its row added to the golden-scenarios README, which `CI-06n` reads; no INDEX row, because INDEX carries one row for the directory. One `##` section appended to [STATE](../STATE.md) for `OI-25`, editing no existing line. **24 of 24 gates, 74 test files and 1085 passing.**
- **Next:** `ADR-072` needs a signature before `CI-06/fixture-inventory` (`W8`) has a subject to read. **`W2`, `W3` and `W4` should read section 39 before they start**, and the wave's fixture count should be re-cut against it rather than against the plan.
- **Blockers:** None for this session. The wave has one and it is below.

### The finding, and it is about the plan rather than the corpus

**Three of [WAVE-04 section 2](../plans/WAVE-04-fixture-backlog-and-gate-inventory.md)'s five counts do not survive the table.** 40 written agrees and 243 with no code agrees. **16 writable derives as 2** (`GS-059` and `GS-080`), 14 needing a ruling as **31 M1 rows blocked**, and 3 held as **2**, on `OI-16`.

**Every one of the fourteen that moved is the same mistake, W1 against W2.** The plan reaches its sixteen by finding the code: `R-43` and `R-44` live in `payout/clamp.ts`, `R-46` to `R-50` in `payout/settle.ts`, `advanceDay` is the fold. **All of that is true and all of it is a W1 finding on rows that fail W2.** The golden stage folds `advanceDay` and diffs a `RuleState`, and `approved_cents`, `clamp_reason`, `trader_cents` and `firm_cents` live on `PayoutEvaluation`, which no fixture in this format ever sees. **The plan states the governing sentence itself, in bold, one paragraph above the partition**: *"the M1 backlog is bounded by W2 and W3, not by W1."* Then it partitions on W1. That is the argument for stating the three conditions as a form rather than as a paragraph.

**The operational consequence: `W2`, `W3` and `W4` are queued to write sixteen fixtures and fourteen fail W2 today.** `W2` has one writable row of six, `W3` has none of six, `W4` has one of four.

### Landmines

- **THE BEST HAND-MAINTAINED RECORD IN THE TREE IS STALE, AND IT IS THE ONE THIS SESSION'S SUBJECT IS ABOUT.** [`fixtures/README`](../../packages/rules-engine/fixtures/README.md)'s held-back table covers every M1-owned scenario and is re-derived against primary sources. It is also **stale in at least four rows**: `GS-053`, `GS-060`, `GS-067` and `GS-068` sit on held-back rows with their fixtures on disk, and the `GS-059` row still names `L-11` as its binding clause after `L-11` was lifted. **Nothing in section 39 was taken from it.** Every blocker was re-checked against current code: `RuleState`'s fields in `types.ts`, `SETTLEMENT_KEYS` and `EXPECTATION_KEYS` and the `L-10` day-ordering refusal in `loader.ts`, `compare.ts`'s nested diff, the 83 sessions in `cme-2026.json`, and `"daily_loss_limit": null` in all three plan records. **Two rows changed status because of that re-check and both moved toward writable.**

- **`M02` IS THE ONE MODULE PLAN OF TWENTY-ONE THAT IS NOT `approved`.** `for f in docs/plans/M*.md; do grep -m1 '^status:' $f; done` is the whole check, and it is what separates M2's 14 rows (`vendor-call`, a W3 hold) from the 229 (`no-fixture-format`, a W2 one). It is cheaper than any reading of what those scenarios say.

- **`no-module-code` WAS OFFERED AND IS DELIBERATELY NOT IN THE VOCABULARY.** For the 229 it is true and it is not the binding half. A term naming the half that clears first turns the table back into the queue `ADR-072` exists to stop.

- **`gates.mjs check` FAILS BETWEEN TWO MUTUALLY-REFERENCING FILES AND THAT IS NOT A DEFECT.** `ADR-072` links section 39 and section 39 links `ADR-072`, so the first of the two commits carries one dangling forward link for exactly one commit. The final tree is green. The alternative is one commit for two files, which the wave's own rule 3 rules against.

- **A SECOND 316-ROW TABLE OF `GS-nnn` FIRST CELLS DOES NOT BREAK `CI-06d` OR `CI-06u`, AND IT WAS WORTH CHECKING FIRST.** `CI-06d` reads `goldenBody()`, which concatenates every `\d{2}-*.md` in the directory, so section 39 is in its input; it collects **distinct identifiers into a Set**, so 316 more definition-shaped rows dedupe rather than duplicate. `CI-06u` is per table and the first cells are unique within it.
