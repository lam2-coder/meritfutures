---
status: approved
depends_on: [README.md]
last_updated: 2026-08-24
---

## 2026-08-24 - Session 187: the endpoint-table reconciliation gate priced at 96 rows and 24 rulings, and the six sessions that disagreed were disagreeing about the unit
<!--index: **[P4](../plans/P4-portal-and-site.md) section 10 item 2 MEASURED, no gate written, no row repaired.** All 22 `docs/plans/M*.md` reconciled against [API_CONTRACT](../architecture/API_CONTRACT.md) **by executing a script at `a0c7916`**, quoted in full with its output in [the review document](../reviews/2026-08-24-endpoint-table-reconciliation.md). **73 endpoints named by a plan and absent from the contract, 23 carried by the contract and named by NO plan, 34 agreed out of a union of 130: the gate is RED ON 74% OF THE SURFACE the day it is written**, and the aggregate is 94 to 98 across four defensible readings, 96 at both the narrowest and the widest. **THE SIX PRIOR SESSIONS DID NOT DISAGREE ABOUT THE TREE, THEY DISAGREED ABOUT THE UNIT, AND THE CORPUS DEFINES NONE**: 124 parsed rows carry **134 endpoint references** and 7 of 20 tables have a row naming more than one, so [159](2026-08-24-session-159.md)'s *"four"* needs a human to read `` `and /enforce` `` as an endpoint, [165](2026-08-24-session-165.md)'s *"six"* and [166](2026-08-24-session-166.md)'s *"five"* are ROW counts against endpoint counts of 7 and 7, and [164](2026-08-24-session-164.md)'s *"six"* is a third unit against 10 references and 8 absences. **24 disagreements in five kinds, and TWO OF THE FOUR AXES THE DISPATCH NAMED ARE EMPTY ON THIS TREE**: `Auth:` occurs **0** times across all 22 plans and 2 plan rows in the whole corpus name a status code, while **11** are parameter spellings (`:id` in the older plans against a typed name in the contract), **0** are method conflicts, **2** are **API_CONTRACT DISAGREEING WITH ITSELF** on `POST /accounts/:id/payout` and `/reset` between its own sections 5, 6 and 11, **2** are M05 and M20 marking the same two wallet endpoints `NEW` and not-`NEW`, and **9** are endpoints named by 2+ plans that NO plan claims to own because the role column is 103 tokens against 31 cells of free prose; **zero endpoints have two claimed owners**. **13 of the 23 contract orphans are the auth surface, which corroborates [ADR-093](../decisions/ADR-093.md) from a direction it did not use.** **71 of the 76 plan-side absences are self-declared `**NEW**` and the marker is DEFINED FOR EVENTS AND FOR NO ENDPOINT TABLE ANYWHERE**; the 5 not-`NEW` rows are the sharper object, two of them cited by M14 as owned by M17 and M18, **neither of whom names them**. **5 rows could not be parsed and every one is a finding**, plus [M07](../plans/M07-risk-abuse.md) with no table and [M12-statistic-definitions](../plans/M12-statistic-definitions.md) with no section 4, so **20 of 22 plans measured by table and the other two named rather than dropped**. **The dispatch's own INDEX clause is false for a `docs/reviews/` file** ([`gates.mjs:167`](../../scripts/corpus/gates.mjs)) and the fence was narrowed rather than widened. 30 of 30 gates, `verify` exit 0 at 7 of 7 invariants and 106 files / 1,526 passed / 1 skipped, none of it moved by this session -->

- **Objective:** measure the cost of a gate reconciling each module plan's endpoint table against [API_CONTRACT](../architecture/API_CONTRACT.md), in both directions and including the disagreements between plans, so a later session can price the gate [session 165](2026-08-24-session-165.md) declined to write. **MEASURE, DO NOT BUILD.**
- **Done:** one new document, [docs/reviews/2026-08-24-endpoint-table-reconciliation.md](../reviews/2026-08-24-endpoint-table-reconciliation.md), carrying the four answers, the extraction script in full, and its full output. STATE appended. **No gate written, no row repaired, no ADR, no migration, no plan touched.**
- **Next:** the gate's design session, which now has a price. Its first two decisions are named in the review document's section 8: **this cannot be one gate** (13 of the 23 orphans are the auth surface ADR-093 has already assigned to P3, so failing a module plan for not naming `POST /auth/logout` reports the wrong module's absence), and **71 of the 76 plan-side absences are one process failure with 71 instances rather than 71 defects**.
- **Blockers:** none. Nothing in this session waits on anything.
- **Landmines:**
  - **Every figure here is anchored to `a0c7916`** and `git diff --stat origin/main -- docs/plans docs/architecture` was empty on this branch at commit time. **The counts move the moment any plan or the contract does**, which is the defect [P4 section 11 rule 3](../plans/P4-portal-and-site.md) names: an absence is a fact about a file at a commit.
  - **The aggregate is 96 at both the narrowest and the widest reading and that is arithmetic, not a stability property.** It is 94 under one intermediate reading and 98 under the other. A later session quoting "96" without the reading it belongs to will be quoting a number that has three siblings.
  - **A count of endpoints and a count of rows differ by 10 across the corpus.** Any gate reporting a figure must declare its unit in its own `covers` line or it produces a seventh number agreeing with none of the six that came before.
  - **Seven items are named in the review document's section 9 and DELIBERATELY LEFT**, among them the contract's two self-disagreeing rate-limit rows at [`API_CONTRACT:651-652`](../architecture/API_CONTRACT.md) and the four cells whose second code span is a bare suffix or a bare method. **A later session repairing them changes the 96.**
  - **The extraction script is NOT committed**, `scripts/` being outside this session's fence. It lives in the review document's appendix A and a reproducing session must save it out of tree first. **If it is ever promoted to `scripts/corpus/`, the three parser decisions in section 1 become the gate's rulings and must be stated there rather than inherited.**
- **Files touched:** [docs/reviews/2026-08-24-endpoint-table-reconciliation.md](../reviews/2026-08-24-endpoint-table-reconciliation.md) (new), [STATE](../STATE.md) (one appended `##` section), [sessions/README](README.md) (row 187 struck, entry row generated), this file. **`docs/INDEX.md` deliberately NOT touched**, and the reason is in STATE and in the pull-request body.
