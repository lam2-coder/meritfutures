---
status: approved
depends_on: [README.md]
last_updated: 2026-08-21
---

## 2026-08-21 - Session 123: WAVE-05 `X1`, six assertion sites name their row, and one of them names a row it does not assert

- **Objective:** [WAVE-05](../plans/WAVE-05-tier2-fixture-shapes.md) `X1`. Every assertion site [ADR-076](../decisions/ADR-076.md) cites names, in its own file, the `GS-nnn` it discharges, so `X8`'s `covered-elsewhere` reading of `CI-06/fixture-inventory` has a tree it can pass.
- **Done:** **six comments, in three files, and not one assertion changed.** `GS-076`, `GS-077`, `GS-078` and `GS-141` in [`plan-validate.test.ts`](../../packages/rules-engine/test/plan-validate.test.ts), `GS-073` in [`determinism.test.ts`](../../packages/golden-loader/test/determinism.test.ts), `GS-072` in [`replay-determinism.property.test.ts`](../../scripts/demo/test/replay-determinism.property.test.ts). **Six is one per `covered-elsewhere` row that did not already name itself**, which is a different six from the brief's, and the difference is the first finding below.
- **Next:** `X2` (section 39 gains the status and the seven rows move; the line-number map below is for it), `X3` and `X4` (the same file, assertions this session refused to touch), `X8` (the gate, now that the tree can pass it). **`X5` is not blocked by this session**, which [WAVE-05](../plans/WAVE-05-tier2-fixture-shapes.md) section 2 says it is.
- **Blockers:** none. [ADR-076](../decisions/ADR-076.md) remains `proposed` and unsigned, and the second finding below is written for the founder to read before signing it.

### The six, one per row, with the site each names

| Row | File | The comment | What it says the site does |
|---|---|---|---|
| `GS-078` | [`plan-validate.test.ts`](../../packages/rules-engine/test/plan-validate.test.ts) | `:74`, at `CV-01` | intraday trailing drawdown refused at publish, `RE-C-01` and `SEEDS['CV-01']` |
| `GS-077` | the same file | `:82`, at `CV-06` | **both** of the row's halves, 0 bp and above 10000 bp, against 1 and 10000 passing |
| `GS-076` | the same file | `:89`, at `CV-10` | a cap below the minimum payout, `toContain` in `RE-C-10` and both validators in `SEEDS` |
| `GS-141` | the same file | `:154`, above the three-plan block | the exact-set half **and the severity half it does not reach**, named as `X3` |
| `GS-073` | [`determinism.test.ts`](../../packages/golden-loader/test/determinism.test.ts) | `:232`, above `RE-D-02` | the fold under a spawned `TZ=Asia/Kolkata` and `ja_JP.UTF-8`, the row's own pair |
| `GS-072` | [`replay-determinism.property.test.ts`](../../scripts/demo/test/replay-determinism.property.test.ts) | `:217`, above the permutation block | **that the block is skipped and asserts nothing today** |

**The two sites that already named their row were left alone**, which is [ADR-076](../decisions/ADR-076.md) section 1's own measurement: [`rules-f-context.test.ts:140`](../../packages/rules-engine/test/rules-f-context.test.ts) for `GS-052`, and `plan-validate.test.ts` for `GS-083`.

### Finding one: the sixth plan-validate site does not exist, because `GS-083` already names itself three times

**The brief listed five comment sites in [`plan-validate.test.ts`](../../packages/rules-engine/test/plan-validate.test.ts) and one of the five was `GS-083` at `:874`.** Confirmed against the file before writing, which is what the brief asked for: **`GS-083` appears three times in that file**, at the section header, at the `describe` title, and inside the block's own citation of M01. [ADR-076](../decisions/ADR-076.md) section 1's table says the same thing in its first row, *"yes, three times"*. **So the file needed four comments and not five**, and a fifth would have been a second claim about a row already claimed.

**The six that were owed are derivable and they are not a count anybody has to keep.** Seven rows move to `covered-elsewhere` ([ADR-076](../decisions/ADR-076.md) section 6): `GS-072`, `GS-073`, `GS-076`, `GS-077`, `GS-078`, `GS-083`, `GS-141`. One of the seven names itself. **Six do not, and those six are exactly this session's six.** `GS-052` is the eighth citation and the second that already names itself, and its row is half covered rather than `covered-elsewhere`.

### Finding two, and it is the session's: `GS-072`'s cited site executes nothing, and `X8` as specified cannot tell

**[ADR-076](../decisions/ADR-076.md) section 6 counts `GS-072` among the seven rows that move to `covered-elsewhere` today.** Section 3 rules that it *"needs no ruling at all"* because [`replay-determinism.property.test.ts:217`](../../scripts/demo/test/replay-determinism.property.test.ts) *"switches itself on the day the engine exports `replay`"*, which is true and is a statement about the future.

**What runs today is nothing, and this session watched it rather than reading it.** `pnpm exec vitest run scripts/demo/test/replay-determinism.property.test.ts --reporter=verbose`:

```
↓ PT-06: arrival-order permutation > any permutation of arrival order yields byte-identical stored state
  PT-06: arrival-order permutation is UNEXPRESSIBLE (the engine exports no replay; P2-8)
```

`replayExists` is `Object.keys(engine).includes('replay')` and is false; the `describe.skipIf` skips; the single test body **throws** rather than asserting. **Section 1's governing rule makes discharge depend on the assertion being EXECUTED somewhere a gate can read**, and `GS-072`'s is not executed anywhere in this tree.

**The consequence is about `X8` rather than about this file.** The assertion [ADR-076](../decisions/ADR-076.md) attaches to `covered-elsewhere` is that *"the cited path exists and contains the row's own id"*. After this session that is true of `GS-072`, **so the gate will pass on a row whose assertion is skipped**, and it will keep passing on the day somebody deletes the throw. A grep cannot separate a running assertion from a named skip, and `GS-072` is the row where the two come apart. **Not repaired here:** the fix is either a status `GS-072` does not have yet or an `X8` that reads the skip, and both are rulings. `X1` adds comments.

**The comment at `:217` therefore claims what is true and no more**: that the row belongs at this block when the block switches on, and that it is not asserted now. Dropping it was the one option the brief refused, and it was the right refusal: a silent absence would have left `X2` to write a citation nobody had checked.

### Finding three: `GS-141`'s site covers one half, which the ruling already says and the file now says too

[ADR-076](../decisions/ADR-076.md) section 2 rules `GS-141` `covered-elsewhere` and records the residual in the same breath. Confirmed at the block: `diffIds` compares **ids**, so the exact-set half is asserted and the *"never rendered identically"* half — `info` against `warning`, and the distinct text — is not. **The comment states both halves and names `X3` as the session that closes the second.** It is not a new finding; it is the residual made visible at the site, which is the whole point of `X1`.

### `X5` is not blocked by this session, and [WAVE-05](../plans/WAVE-05-tier2-fixture-shapes.md) section 2 says it is

The `X5` row reads *"Depends on `X1`, same file"* against [`replay.test.ts`](../../apps/worker/test/replay.test.ts). **`X1` did not touch `apps/worker/test/` and owed nothing there.** `GS-071` is ruled `writable`, not `covered-elsewhere` ([ADR-076](../decisions/ADR-076.md) section 3), so `X8` never reads it and no comment is owed until the assertion exists; the session that writes the 250-day replay names the row in the same commit. **`X5` can start now.** Recorded rather than repaired: WAVE-05 is outside this fence.

### The line-number map, for `X2`

Four comments shifted [`plan-validate.test.ts`](../../packages/rules-engine/test/plan-validate.test.ts) below line 64. **Every [ADR-076](../decisions/ADR-076.md) citation into that file moves**, and `X2` writes section 39's citations from this column rather than from the ADR:

| [ADR-076](../decisions/ADR-076.md) cites | Now | What is there |
|---|---|---|
| `:65` (`CV-01`) | **`:77`** | the rule; `GS-078` is named at `:74` |
| `:70` (`CV-06`) | **`:85`** | the rule; `GS-077` is named at `:82` |
| `:74` (`CV-10`) | **`:92`** | the rule; `GS-076` is named at `:89` |
| `:135` to `:141` (the three-plan block) | **`:166`** | the block; `GS-141` is named at `:154` |
| `:702`, `:717`, `:726`, `:739` (`SEEDS`) | **`:733`, `:748`, `:757`, `:770`** | the seeded violations |
| `:770` (the `it.each` over `SEEDS`) | **`:801`** | the runner |
| `:874` (`GS-083`) | **`:905`** | the `describe`, which names the row itself |

**The other two files did not move where it matters.** In [`determinism.test.ts`](../../packages/golden-loader/test/determinism.test.ts) the comment begins at **`:232`**, the line the ADR cites, and the `describe` moves to `:245`. In [`replay-determinism.property.test.ts`](../../scripts/demo/test/replay-determinism.property.test.ts) the comment begins at **`:217`**, the line the ADR cites, and the `describe` moves to `:231`. Both citations still land on a line that names the row, which is the property the citation is for.

### Verification

`pnpm exec vitest run`: **101 files, 1404 passed, 45 skipped**, byte-identical to the count taken on this branch before the first comment. **A session that only adds comments must not move that number and it did not.** `node scripts/corpus/gates.mjs check`: **27 of 27**. `node scripts/corpus/falsify.mjs`: *"All 27 gates pass clean and fail dirty, 45 scope case(s) hold, and 10 loader case(s) land on the side of the `CI-03` boundary they name"*, which is `origin/main`'s own line, run there for comparison rather than assumed. `pnpm run format:check`, `pnpm run lint` and `pnpm -r run typecheck`: clean, run after every file.

**`CI-06n` was watched failing first and it was right.** With the log stub committed and no row in the entries list, the gate reported *"entry file with no row in `docs/sessions/README.md`"* and `falsify.mjs`'s clean phase turned it into its one problem. It was repaired by writing the row, which is the end ritual, not by touching the gate.

- **Landmines:**
  - **`GS-072` is now `covered-elsewhere`-shaped and is not covered.** After this session the row's citation resolves and its file names it, so `X8` will pass it. The assertion behind it is skipped. **The gate and the truth agree on six of the seven rows and disagree on this one**, and the disagreement is invisible to any grep.
  - **`covered-elsewhere` inherits the defect it was built to end, one level down.** A `written` row is checked against a directory listing. A `covered-elsewhere` row is checked against a comment, and **a comment is a claim a refactor can leave behind**: renaming a `describe` breaks nothing the gate reads, and moving the assertion out from under the comment breaks nothing at all.
  - **Every line number [ADR-076](../decisions/ADR-076.md) states into `plan-validate.test.ts` is now stale.** The map above is this session's, and it goes stale on the next edit to that file, which is `X3`. `X3` and `X4` land in that file before `X2` writes section 39 if the wave runs in its stated order.
- **Files touched:** [`plan-validate.test.ts`](../../packages/rules-engine/test/plan-validate.test.ts), [`determinism.test.ts`](../../packages/golden-loader/test/determinism.test.ts), [`replay-determinism.property.test.ts`](../../scripts/demo/test/replay-determinism.property.test.ts), [STATE](../STATE.md), [sessions/README](README.md), this file. **No fixture, no loader, no gate, no source file, and no assertion.**
