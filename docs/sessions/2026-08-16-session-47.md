## 2026-08-16 - Session 47: groups F, G and H, forty-one of fifty (money path)

- **Done:** **Every rule M01 states that the engine can compute without the calendar data or a new ADR is now computed.** Five commits on one branch: **R-35** and the `withdrawableCents` field; **group G** ([`payout/clamp.ts`](../../packages/rules-engine/src/payout/clamp.ts), R-42 to R-45); **group F's engine half** ([`payout/gates.ts`](../../packages/rules-engine/src/payout/gates.ts), R-33, R-34, R-36, R-37, R-39, R-41, wired at DO-9, giving `RuleState` its `engineGates` and `engineEligible` and `day.closed` its `gate_results`); **group H** ([`payout/settle.ts`](../../packages/rules-engine/src/payout/settle.ts), R-19, R-46 to R-50, wired at DO-2); and **the context half** ([`payout/evaluate.ts`](../../packages/rules-engine/src/payout/evaluate.ts), R-38 and R-40). **Nineteen new rules, twenty-two to forty-one of fifty**, each with an `RE-U-nn` asserting its operator at the boundary on both sides and **each with its own seeded mutant in [`falsify-ci.mjs`](../../scripts/ci/falsify-ci.mjs), watched failing on it**. `calendar.ts` gains `tradingDaysBetween` and `tradingDayAtSequence` for R-37. **Four of M01's six exported functions now exist.** `pnpm run verify` green: **460 tests, 15 corpus gates clean and dirty, 43 falsify cases with 40 watched failing on their own finding** (the three that error are scanners absent from this container's PATH, unchanged from prior sessions).
- **Next:** **The fixture-wiring session is now the single largest unblocking left in P2** and nothing this session did moved it: the resolvable-citation `L-nn` rule, the loader's `bigint` comparison, then [ADR-048](../decisions/ADR-048.md)'s per-fixture polarity, in that order. After it: `P2-1` (`resolvePlan`, `validatePlan`, the `RE-C-nn` suite), then `hash.ts` and `replay`. Groups A, F's fixtures and H's fixtures still wait on the calendar.
- **Blockers:** **The calendar transcription** (founder, [P2](../plans/P2-rules-engine.md) section 6) blocks group A's four rules and every golden fixture that folds a real day sequence. **`INV-06`'s scope** (founder) still blocks `RE-P-01`, unchanged from [session 45](2026-08-16-session-45.md). **R-32** needs a `rule_states` column and therefore an ADR.
- **Landmines:**
  - **M01's SKETCH CHECKS `INV-18` AGAINST THE POST-SETTLEMENT BALANCE, AND FOLLOWING IT MEANS NO ACCOUNT EVER HAS A STATE ROW ON THE DAY IT WAS PAID.** Section 3.6 runs DO-2 (which reduces `balanceCents` by the approved amount) and then compares `mark.opening` against `s.balanceCents + mark.adjustmentCents` where `s` is post-settlement. **`SD-01` puts the settled withdrawal in `adjustment_cents`**, applied at the open of the effective day (`R-10`, `AS-10`), so the withdrawal is counted **twice**: on a 150,000c Core EOD payout the sketch expects an opening 150,000c below the real one, raises `opening_mismatch`, and DO-3's rule is that a failure **writes no state for the day**. **`INV-18`'s own row reads `mark.opening_balance_cents == PRIOR.balance_cents + mark.adjustment_cents`**, and following the row makes the two readings identical rather than contradictory, because `prior.balance + adjustment` **is** the post-settlement balance. The engine follows the row. **This is the third disagreement of the R-15 and R-22 shape and it is the most expensive**, because the other two need a config no v1 plan carries and this one fires on every payout.
  - **`clampPayout` IS `export`ED IN SECTION 3.6 AND IS NOT ONE OF SECTION 1.3's SIX.** Section 1.3 is unambiguous ("the public surface is six functions. Nothing else is exported, because every additional export is a way for a caller to reimplement a rule slightly differently"), so the clamp is module-scoped and reachable only through `evaluatePayout`. That is what makes section 4's "the identical function with the identical inputs" **structural** rather than a convention `FM-16` relies on being followed. `evaluate.golden.test.ts` asserts the exact export list, so widening it is a red stage rather than a diff nobody reads.
  - **SECTION 5.2 STILL LISTS `account.live_invitation_issued` ON `R-49`'s ROW AND `R-49` RULES IT OUT BY NAME.** [ADR-024](../decisions/ADR-024.md) makes graduation eligibility a review-pool flag and invitation "a discretionary operator action taken from that pool, **outside the engine**". The engine emits only `account.graduated` and `RE-U-049` asserts the **absence** of the other, because an event with no producer is one a consumer will eventually wait for.
  - **`R-35` HAS NO OPERATOR AND API_CONTRACT PUBLISHES A `buffer` GATE ANYWAY, SO ONE WAS ADOPTED.** `R-35` is a formula and it is the only group F row whose operator column is empty. Adopted: `pass` is `withdrawable > 0`, `R-35`'s own `max` read as a question. **The choice cannot move a payout** and that is why it was safe to adopt without a ruling: `R-39` requires `min(withdrawable, cap) >= 10,000c`, so any state clearing `R-39` clears this gate on either reading of the boundary. It is **dominated in exactly `PW-01`'s sense** and it stays in the conjunction regardless, because `INV-15` is "AND(**every** engine gate)" and a gate shown to the trader but left out of the conjunction is the shortcut path `INV-15` forbids. **Reported because choosing an operator M01 does not state is not a session's call to make silently.**
  - **`R-38` IS FILED UNDER "GROUP F: FUNDED GATES" AND ITS INPUT IS CONTEXT, AND ALL THREE DOCUMENTS DISAGREE ABOUT THE FILING WHILE AGREEING ABOUT THE MECHANISM.** `hasPayoutInFlight` is in `ExternalGates`, whose own comment reads "context, never replayed (`INV-23`)", so it cannot be a term in `engineGates` without putting an unreplayable fact into the replayed state, which is the nightly divergence storm `SD-06` exists to prevent. **`AS-01` calls it "part of eligibility". API_CONTRACT's `gates` object, which M01 section 2.2 names as the shape of `FullGateResults`, carries no in-flight entry at all** and surfaces the condition as `POST`'s `conflict` error plus the `SD-09` partial unique index. Adopted: it binds through `contextEligible` and is reported on `PayoutEvaluation.noPayoutInFlight`. **The ten-key `gates` object is asserted exactly**, so a later session cannot widen a shape other modules render.
  - **M01's `hasPayoutInFlight` COMMENT HAS BEEN WRONG SINCE [ADR-028](../decisions/ADR-028.md) AND IS STILL WRONG.** It reads `approved | transferring | frozen`; `transferring` was retired and `held_pending_review` was added by [ADR-040](../decisions/ADR-040.md). [STATE](../STATE.md) already names M01 as the one document `FOLD-02`'s sweep has not reached and `ADR-040` names two M01 sites by name. **The engine reads a resolved boolean, so the drift cannot reach the arithmetic**, which is exactly why it will keep not being noticed.
  - **THE CALENDAR HEADER CLAIMED THE GAP COUNT WAS BLOCKED ON THE CALENDAR DATA, AND THAT WAS ONE CLAIM TOO WIDE.** `nextTradingDayAfter` had already been moved across the same line on the narrower reading. **What `TR-01` forbids is writing down which days the exchange trades from recollection**, and subtracting two `sequence` values the caller's own slice supplied does not do that: `sequence` is "a **dense** index into the calendar" (section 2.1), so the difference is the true count of exchange trading days in the interval whether or not the slice holds every day between them. **The fixtures are still blocked; the function is not**, and the header now says which.
  - **`GateInputState` IS A MECHANISM AND NOT A CONVENIENCE.** It is `RuleState` minus `engineGates` and `engineEligible`, and every function that COMPUTES those two fields takes it. So an evaluator that carried a prior row's eligibility forward on some branch is a **compile error** rather than a review note, which is `INV-15`'s "no shortcut path" made structural in the same idiom as `PlanConfigVersionIsClosed` and `CalendarSliceIsData`. It is also what lets `initialState` build in one pass, with no placeholder gate set for a later edit to leave behind.
  - **THE BREACH PATH STATES ITS GATES RATHER THAN EVALUATING THEM, AND THE REASON IS `R-37`.** The cadence gate can **refuse** a day when the slice does not cover the anchor, and a breach day that refused would be a breach the fold declined to record on a caller's window choice. `gatesAfterBreach` returns every `pass: false` including the two that can otherwise report `skipped`, because a closed account is not "skipped past" its gates. **A breach is the one outcome that must be written whatever else is unknown.**
  - **ALL THREE MID-DAY REFUSAL PATHS NOW CARRY `prior` RATHER THAN THE HALF-FOLDED STATE.** `types.ts` already promised "the `state` on the output is the state the fold arrived with"; DO-8's two refusals and DO-9's were passing a state whose counters had advanced and whose floor had trailed against a day the fold then declined to write. Nothing is written on a refusal either way, so this changes no stored value; what changes is that a caller logging `output.state` on a refusal is no longer shown a row that never existed.
  - **`settlement_unimplemented` IS DELETED FROM `AssertionKind`, NOT LEFT IN PLACE**, for the reason `eval_progression_unimplemented` was: a refusal kind nothing can emit is a lie about what the engine refuses. The test that asserted it now asserts the applied settlement, and it got stronger for it: it pins that the withdrawal reaches the balance **exactly once**, which is the failure on both sides (omitted is a second payout waiting; double-counted breaches the account that earned it).
  - **`R-42`'s MUTANT IS INVISIBLE ON EVERY PUBLISHED PLAN, WHICH IS WHY IT WAS WORTH SEEDING.** All three v1 plans carry a single cap rung, so "the **last** entry whose `from_ordinal <= ordinal`" and "the **first**" cannot be told apart by any published config. `withCapSchedule` builds a `CV-09`-shaped ladder the lineup does not use, on the same justification `withStaticDrawdown` and `withDailyLossLimit` already carry, and the second rung's value is stated as arbitrary rather than dressed up as a parameter.
- **Files touched:** `packages/rules-engine/src/payout/{clamp,gates,settle,evaluate}.ts` (new), `src/{types,index,calendar,rules,errors}.ts`, `src/day/{advance,progression}.ts`, `packages/rules-engine/test/rules-{f-gates,f-context,g-payout,h-settlement}.test.ts` (new), `test/{fixtures-in-code,rule-coverage,implemented-rules,advance-day,evaluate.golden}.ts`, `scripts/ci/falsify-ci.mjs` (fifteen new engine mutants), `docs/STATE.md`, `docs/sessions/`.

### The count, reported rather than implied

**Forty-one of fifty, and the nine that are left are blocked rather than unwritten.** `implemented-rules.test.ts` now asserts the nine **by name** and asserts that the declared set plus the undeclared list sums to **50**, so a session that implements one without declaring it, or declares one without implementing it, cannot land.

| Not implemented | Waiting on |
|---|---|
| R-01, R-02, R-05, R-06 | **The calendar transcription.** Group A, founder item |
| R-10, R-11, R-17, R-20 | Nothing in the engine. Ingest, publish validation, and the platform setpoint |
| R-32 | **An ADR.** Elapsed trading days is not derivable from `RuleState`, so the field is a schema delta |

**Twenty-eight of the forty-one are watched failing on a mutant of themselves.** Every rule this session declared arrived with one; the thirteen without are from sessions 44 and 45 and are the ones whose operators are already pinned by a boundary pair.

### What was NOT done, named rather than implied

**CI-03's polarity is untouched**, for the fourth session running, and the brief said so explicitly. `engineIsIdentityStub()` still governs and every golden fixture is still asserted to fail. **[ADR-048](../decisions/ADR-048.md)'s stated prerequisite is still unwritten**, and it is now the thing most of P2 is queued behind.

**`RE-P-01` and the floor-monotonicity property were not written**, because `INV-06`'s scope is unruled and another session is ruling it.

**No golden fixture was written and no `EC-nnn` was claimed.** P2 section 2 alternates fixture and engine sessions: "a session doing both has derived its expectations from its own output."

**No ADR was written for any of the three M01 disagreements or the two adopted rulings.** A defect in a frozen document is the founder's, and a session that writes an ADR to resolve a contradiction it found in its own path is a session grading its own work. Sessions 44 and 45 handled R-22 and R-15 the same way and this follows them.

**`hash.ts`, `SD-08` and `replay` are absent**, so `RuleState` still carries no `stateHash`. `OI-14` ([ADR-047](../decisions/ADR-047.md)) is still P2's to discharge and is untouched.
