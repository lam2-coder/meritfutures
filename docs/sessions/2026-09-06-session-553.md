---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 553: the entry said ONE, the tree says TWO, and the survivor is a write with no write door

<!--index: **THE ROW IS [ADR-358](../decisions/ADR-358.md), `proposed` and UNSIGNED. MONEY PATH, `E2` READ OWED.** **RULED: `useAffiliateDeps` has TWO obstructions and not ONE.** The entry has named **THREE**, then **TWO**, then a **DIFFERENT ONE**, then **ONE**, and every rewrite repaired the clause it was dispatched about and **INHERITED the rest**, so the method list was never re-derived. **ALL FIVE DISPATCHED CLAIMS REPRODUCE**; the finding is in what none of them said. **`stats` IS ONE AND THE ENTRY HAS IT RIGHT**: `conversions_30d` counts over `attributions`, `pair`, excluded from `ScopedTableKey` BY TYPE and thrown on by `scopePredicate`, so it waits on a NAMED COUNTING DOOR. **`submitCreative` IS THE OTHER AND NO VERSION OF THE ENTRY HAS EVER NAMED IT.** It WRITES `affiliate_creatives`, which is `derived`, and it was served a refusal that **called it a READ and reasoned about `affiliates`**. Every write verb was checked rather than assumed: `ScopedTx.insert` takes `OwnedTableKey`, `insertUnder` takes `ParentedTableKey` which is `Extract<DerivedTableKey, 'sessions'>` and **A CLOSED LIST OF ONE**, `insertAsParty` takes `PartyWritableTableKey` and the table is not `pair`, `FirmTx.insert` takes `FirmTableKey`, and **`ApiDb` exposes `scoped`, `firm`, `resolution`, `establishment` and `publicLookup` AND NO SYSTEM HANDLE**, so `SystemTx.insert` is unreachable from a request path and `SystemReason` never had a member one could name. **Nothing in this tree writes the table.** **THIS ROW'S OWN FIRST FINDING WAS REFUTED BY ITS OWN FALSIFICATION, AND THE REFUTATION IS KEPT BECAUSE IT CUTS AGAINST THE ROW**: `requiredDisclosure` looked blocked on the same shape, and **`ApiDb.firm()` reaches `tos_versions` because it is `firm`**, so the count is TWO and not THREE. Its verdict was right and **its reason named the wrong table**, which is repaired rather than left, because an adapter written from that sentence would read the disclosure inside the caller's scoped transaction and **would not compile**. **NO GATE COULD HAVE CAUGHT EITHER, AND THE REASON IS STRUCTURAL.** `RI-14` is a claim of **ABSENCE** against a **PRESENT** thing ([ADR-214](../decisions/ADR-214.md)); these claimed a door **PRESENT** while the tree has none, the same defect inverted. **AND `RI-14` AND A PROSE-MATCHING CASE ARE IN DIRECT CONFLICT, WHICH IS SHARPER THAN THE STALENESS [ADR-324](../decisions/ADR-324.md) NAMED**: a repaired message must QUOTE the false sentence, so the matched words survive **inside the quotation of their own retirement** and the case can never go red. **Measured rather than reasoned: after both repairs, the old case *"four methods say a door exists and an adapter does not"* was still GREEN.** The message's own voice is now separated from what it quotes, using the corpus's own double-quote convention, and the expectation is **DERIVED from the key sets in `scoped-db.ts`** so a widening of `ParentedTableKey` or `CATALOG_TABLE_KEYS` is red the day it happens. **THE DEFAULT REFUSES LOUDLY ENOUGH, AND THIS WAS ASSERTED RATHER THAN ASSUMED**: `unwired()` REJECTS on every method with a distinctly named class, `unavailableWhenUnwired` catches only that class, logs at `error` and serves **503**, and **there is no configuration under which this port quotes a number**. The hazard here was never the value; it was the reason. **SERVABLE PARTLY, AND THE PARTIAL SERVE IS NOT TAKEN**: `GET /affiliate/statements` is reachable on doors alone once a cursor rule exists, `POST /affiliate/links` the moment a base URL is configured, and the other two endpoints need a `packages/db` diff first. Building a serve on a blocker inventory that was wrong until today is the error this row exists to correct; it is recorded as owed. **THE PORT IS NOT WIRED**: `start.ts` untouched, `productionAffiliateDeps` unchanged, all six methods still refuse, `{declared, wired, blocked}` unmoved, and **`Object.keys(BLOCKED).length` counted at reporting time is 14**. **NO COMMISSION RATE, ATTRIBUTION WINDOW, PAYOUT THRESHOLD, CURSOR OR BASE URL INVENTED**; three open questions recorded as options. **Five falsifications run both ways, and ONE OF THIS ROW'S OWN NEW CASES WAS FOUND TAUTOLOGICAL BY RUNNING THEM** and rewritten. **Four citations repaired** under the granted right, three shifted by this branch and one measured stale by eleven lines in `routes/payouts.ts`, which names the very declaration this finding rests on. **NO SQL, NO MIGRATION TAKEN OR RESERVED, no gate weakened, no test skipped or disabled, no invariant minted, no `CI-06` letter spent, no closed list widened, `packages/**` untouched.** **THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL** and the tree was attached and cloned by hand. **NO `mcp__github__*` TOOLS, NO `gh`, `api.github.com` ANSWERS 403 while `git push` AUTHENTICATES**: the branch is pushed and mergeable and **no pull request was opened; it is owed**. Counts derived at reporting time: **325 files / 7,908 passed / 10 skipped / 0 failed** against a base reproduced BEFORE the first edit of **325 / 7,903 / 10 / 0**, a delta of **+5 cases measured by removal** with files and skips unchanged; gates **33 of 33**, invariants **35 of 35**, typecheck, lint and `format:check` all clean, SQL diff **EMPTY**. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `358`: measure what is actually left of `useAffiliateDeps`' blocker, and rule on whether the port can be served.

**One objective. The dispatch said the row is as much about the entry's reliability as about the port, and that is where it resolved.**

---

### 2. What was re-derived before the first edit

**The baseline reproduces the dispatch's figure exactly**, measured before anything was touched: **325 files / 7,903 passed / 10 skipped / 0 failed**.

**All five dispatched claims reproduce.**

| claim | verdict |
|---|---|
| `affiliate.ts:602` declares `interface AffiliateDeps` | **REPRODUCES** |
| `:613` is `productionAffiliateDeps` and `:615` assigns it as the live default, so the port has a REFUSING default rather than no default | **REPRODUCES** |
| `useAffiliateDeps` is declared at `:618` | **REPRODUCES** |
| the `BLOCKED` entry says ONE obstruction, crediting ADR-321 and ADR-324 | **REPRODUCES.** Both ADRs read in full; both say what the entry says they say, and both are `proposed` |
| it is one of the **14** in `{declared: 25, wired: 11, blocked: 14}` | **REPRODUCES.** Counted, not subtracted: `Object.keys(BLOCKED)` enumerated to 14 with `useAffiliateDeps` eleventh |

**The finding is not in any of them.** It is in the partition the entry asserts over the six methods and that no dispatch mentioned.

---

### 3. The ruling

[ADR-358](../decisions/ADR-358.md) sections 2 to 5 hold it. **TWO obstructions: `stats`' counting door, which the entry has, and `submitCreative`'s write door, which it has never had.** Both are a `packages/db` diff followed by an adapter, not an adapter.

**`AffiliateStats` has NINE fields and the entry accounts for four.** The other five are not obstructions and are named in the ADR so the next reader need not re-derive them: `code`, `commission_bp`, `status` and `chargeback_rate_bp` are columns of `affiliates`, and `clicks_30d` counts over `affiliate_clicks`, which is `derived`.

---

### 4. Where this row was wrong, kept because it cuts against the row

The first derivation had **THREE** obstructions, with `requiredDisclosure` blocked because `tos_versions` is `firm`, `firm` keys are excluded from `ScopedTableKey`, and `CATALOG_TABLE_KEYS` does not carry it.

**`ApiDb.firm()` refutes it.** `FirmTx` is generic over `FirmTableKey`, nine call sites in this deployable already open it, and the catalogue list bounds what a **scoped** transaction may read rather than what the deployable may read.

**The count is TWO because the falsification was run, not because the first reading was careful.** ADR-358 section 4 records it, and the founder block names the judgement it rests on: `FirmTx` reaches every firm row in the estate, and no call site has argued that trade in writing.

---

### 5. What landed

| file | what |
|---|---|
| [`apps/api/src/routes/affiliate.ts`](../../apps/api/src/routes/affiliate.ts) | `CREATIVES_HAVE_NO_WRITE_DOOR` and `DISCLOSURE_READABLE` replace `AFFILIATES_READABLE` on two methods, each quoting the retired sentence beside its correction per `RI-14` |
| [`apps/api/test/affiliate-stats-obstructions.test.ts`](../../apps/api/test/affiliate-stats-obstructions.test.ts) | the door is DERIVED per method from the key sets; the prose-matching case is replaced and quoted |
| [`apps/api/test/wiring.test.ts`](../../apps/api/test/wiring.test.ts) | the `BLOCKED` entry says TWO, keeps every retired count, and states what the port can serve today |
| [`apps/api/src/routes/payouts.ts`](../../apps/api/src/routes/payouts.ts), [`apps/portal/src/http/client.ts`](../../apps/portal/src/http/client.ts) | citation repairs |

---

### 6. Blockers, landmines and what is owed

- **The pull request is owed.** `git push` authenticates and the GitHub API does not: no `mcp__github__*` tools exist, `gh` is absent, and `api.github.com` answers **403** *"GitHub access is not enabled for this session"*. The branch is `claude/three-obstructions-then-one`, pushed and mergeable.
- **`wiring.test.ts` is edited by rows 356, 357 and 359 concurrently**, and 357 is ruling on whether `{25, 11, 14}` is honest at all. This row's edit is confined to the `useAffiliateDeps` value and two citations, and **the 14 was COUNTED rather than subtracted**. Expect a merge.
- **A partial serve is owed and not taken**, with its two blocking questions recorded as `OQ-358-01` and `OQ-358-02`.
- **`RI-14` versus prose-matching is repaired at one file and is available everywhere in this corpus.** ADR-358's founder block puts the question of whether it is an invariant rather than one file's problem, and declines to mint `RI-38` on one site.
