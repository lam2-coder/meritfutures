## 2026-08-20 - Session 78: all ten of section 10's duplicated guards read, four ruled in ADR-062 and six merged under ADR-061
<!--index: All ten of STATE_MACHINES section 10's duplicated guards read: four are contradictions ruled in ADR-062, six are agreements merged under ADR-061, and CI06U_REGISTER loses the file. The payout gate reads `identities.status = 'active'`, and OQ-062-01 opens on the wallet door M20 still leaves open to a closed identity -->

- **Done.** [STATE_MACHINES](../architecture/STATE_MACHINES.md) section 10 carries each of its ten duplicated guards **once**, and `CI06U_REGISTER` has lost the file entirely in the same commit as the repair. `CI-06u`'s run note moves from **106 keys across 8 files to 96 across 7**. [ADR-062](../decisions/ADR-062.md) is written, `status: proposed`, with an unsigned founder-approval line. **All ten pairs were read against primary sources: FOUR are contradictions and SIX are agreements.**
  - **The classification, one line per guard.** Contradictions: `G-ELIGIBLE` (:403/:406), `G-FREEZE-CLEARED / G-FREEZE-ENFORCED` (:416/:423), `G-HOLD-REQUIRED` (:417/:424), `G-ENFORCEMENT-RESTRICT` (:420/:432). Agreements: `G-CLAMP` (:404/:407, byte-identical), `G-NO-IN-FLIGHT` (:405/:408), `G-FREEZE-DURING-FLIGHT` (:415/:422), `G-HOLD-RELEASED` (:418/:425), `G-HOLD-ENFORCED` (:419/:426), `G-RESTRICTION-LIFTED` (:421/:433).
  - **`G-ENFORCEMENT-RESTRICT` IS A FOURTH CONTRADICTION AND IT CAME OUT OF THE SEVEN NOBODY HAD READ.** :420 requires the flag lifecycle's `investigating` to `enforced` **path**; :432 authorizes a restriction from a flag standing **at** `investigating`. The witness is a flag at `investigating` with no enforcement decided. Section 7 of the same document binds "no automatic transition into `enforced`" and requires an **exported evidence pack id** on that transition, and it makes entering `investigating` what sets `payouts_frozen`. So the corpus already has a measure for the investigating stage and it is the **freeze**; the restriction is the enforcement. :432 would make it available on suspicion with no pack. :420 also names an actor, which [`0031`](../../packages/db/migrations/0031_payout_hold_and_identity_restriction.sql)'s `opened_by NOT NULL` requires and :432 omits. **This is the case for reading all ten rather than the three that were known.**
  - **`G-ELIGIBLE` is ruled `identities.status = 'active'`, and THE BRIEF'S OWN EVIDENCE POINT FOR THE OTHER HALF DID NOT SURVIVE CHECKING.** The brief states that [ADR-041](../decisions/ADR-041.md)'s binding table "expresses the wallet and withdrawal gates as `restricted -> blocked`, which is the `<>` shape". The table was read at [ADR-041:20](../decisions/ADR-041.md): it is headed "What `restricted` binds", it has seven rows, every behavior cell reads `blocked`, and **it says nothing whatever about `closed`**. Both predicates block `restricted`, so both satisfy it and **it cannot discriminate**. What decides it is that the only inbound edge to `closed`, `G-ENFORCEMENT-CLOSE`, is **defined nowhere** (section 9 says so in terms), so the witness cannot be shown unreachable; and that `<>` **fails open** on every value the enum gains later while `=` fails closed. On a payout path that settles it.
  - **`G-RESTRICTION-LIFTED` was decided by the constraint, exactly as the brief asked.** :421 names `restored_at`, `restored_by` and `restore_evidence`; :433 names two of the three. `identity_restriction_restore_is_complete` at [`0031:264`](../../packages/db/migrations/0031_payout_hold_and_identity_restriction.sql) is **all-or-none** over all three, so the row that would separate the halves is **unwritable** and they are coextensive over every row the database can hold. **AGREEMENT, and the complete enumeration was kept.**
  - **No clause was grafted from a discarded half into a surviving one.** [ADR-061](../decisions/ADR-061.md) authorizes deleting a row that says what another row says. It does not authorize composing a third row, and a merged cell neither session wrote is a new assertion wearing a repair's clothes. Each of the six keeps one half **verbatim**, and the commit message says which and why.
  - **The four ruled rows cite [ADR-062](../decisions/ADR-062.md) in the table itself**, so a reader of the frozen document finds the ruling rather than a predicate that changed with no trace. The six merged rows carry no citation: they are repairs, and ADR-061 puts the record in the commit message.
  - **22 of 22 gates pass, `CI-06u` among them, and `pnpm vitest run` is green at 939 passed and 42 skipped.** `adr_count` moved 61 to 62 in [INDEX](../INDEX.md) and [STATE](../STATE.md), generated rather than typed.
- **Next.** **The founder signature on [ADR-062](../decisions/ADR-062.md), and an answer to `OQ-062-01`.** Section 7's first objection is the one to read first: this entry makes the payout gate the only outbound door written `= 'active'`, and a founder may reasonably prefer one shape across all of them. The remedy this entry recommends is to move the other doors rather than to move this one back.
- **Blockers.** [ADR-061](../decisions/ADR-061.md) is `status: proposed` and unsigned, and six rows were deleted under it. If it is refused or inverted, those six deletions were unauthorized. They are reversible by design: section 5 of ADR-062 records each with the half kept and the reason.
- **Landmines.**
  - **`OQ-062-01`, AND IT IS THE ONE TO READ FIRST. A `closed` IDENTITY CANNOT REQUEST A PAYOUT AND CAN STILL SPEND ITS WALLET BALANCE.** [M20](../plans/M20-wallet.md)'s `INV-M20-06` at [M20:62](../plans/M20-wallet.md) enumerates the wallet and external-withdrawal gate set with `identities.status = 'restricted'`, the `<>` shape. After this ruling the payout gate is strictly narrower. **The divergence exists in M20 today and this ruling exposes it rather than creating it**, and it runs against `INV-M20-06`'s own first clause, "every context gate that blocks the external leg blocks wallet spend too", which [M20:286](../plans/M20-wallet.md) calls the module's most important single line. **It is raised as a numbered open question rather than a recorded fold because it would otherwise belong to nobody**: this session's fence stops at STATE_MACHINES, and session S3 holds M20 to delete the `INV-M20-06` prose duplicate without ruling on its predicate. Numbered `OQ-062-01` and not `OQ-M20-06`, deliberately: M20's questions run to `05` and taking `06` would allocate into a namespace a concurrently running session holds, which is the collision shape [ALLOCATION](../decisions/ALLOCATION.md) records twice.
  - **`G-WITHDRAWAL-CLEARED` IS INSIDE THIS FILE, READS "the identity not `restricted`", AND WAS DELIBERATELY NOT TOUCHED.** Its predicate's primary source is `INV-M20-06`, so changing it here **without** M20 would make the two documents disagree, which is strictly worse than the divergence `OQ-062-01` records. It is also not one of the ten duplicated guards.
  - **[STATE_MACHINES](../architecture/STATE_MACHINES.md) SECTION 9 CARRIES ITS BINDING ENUMERATION TWICE AND NO GATE CAN SEE IT.** Two "What `restricted` binds" tables, at `:339` and `:362` on the pre-repair ref, with different columns and different wording: one says "**Revocation** is always available", the other "**suspension** is always available", for the same act. **`CI-06u` is per table**, so two tables sharing keys is explicitly not a finding there, and it is in no register. **Not repaired**: it is not this session's objective and it is not a registered key, so repairing it would be an unruled edit to a frozen money-path document.
  - **[`gates.mjs`](../../scripts/corpus/gates.mjs)'S `CI-06u` PREAMBLE IS NOW STALE AND WAS LEFT STALE.** It says the guard table "is where ten of the 105 are". After this commit it is where none of them are. The comment is outside this session's register entry, and the 105-against-106 corrections are owned by sessions S5 and S8 per [STATE](../STATE.md). **Named here so the next reader knows it is known.**
  - **[ALLOCATION](../decisions/ALLOCATION.md)'S `062` ROW STILL READS "Reserved, unwritten".** Stale from this merge. ALLOCATION is held by S6, no gate requires the edit, and its eighteen registered duplicates are exactly the kind of file two sessions must not write at once. **Recorded rather than reached**, which is the opposite call from [session 77](2026-08-20-session-77.md)'s on the `061` row, and the difference is that there the row was **missing** and the falsifier failed without it.
  - **[STATE](../STATE.md) WAS NOT UPDATED BEYOND ITS GENERATED SPAN**, the same departure from the end ritual [session 77](2026-08-20-session-77.md) recorded and for the same reason: the fence named five things and STATE was not among them. What STATE would carry is the register moving to 96 across 7 and the arrival of `OQ-062-01`, and both are true of a branch that is not merged yet.
  - **THE RECORD'S LINE CITATIONS FOR `G-ELIGIBLE` ARE WRONG AND ARE STILL WRONG ON MAIN.** [`gates.mjs`](../../scripts/corpus/gates.mjs) and [session 75](2026-08-19-session-75.md) say "405 and 408"; those are `G-NO-IN-FLIGHT` rows. `G-ELIGIBLE` was :403 and :406. The wave plan and [STATE:1779](../STATE.md) already correct it; the two sites that carry the error were not in this fence.
- **Files touched.** [`docs/decisions/ADR-062.md`](../decisions/ADR-062.md) (new), [`docs/architecture/STATE_MACHINES.md`](../architecture/STATE_MACHINES.md), [`scripts/corpus/gates.mjs`](../../scripts/corpus/gates.mjs) (`CI06U_REGISTER` only), [`docs/decisions/README.md`](../decisions/README.md), [`docs/INDEX.md`](../INDEX.md) and [`docs/STATE.md`](../STATE.md) (`adr_count` span only), this log and its [README](README.md) row.
