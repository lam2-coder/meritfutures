---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 589: the retired clause repaired at the site nobody had been given, two pointers repaired to names because both targets move again this wave, one class-two pointer confirmed and left, and an owed figure that reproduces at neither five nor seven as four

<!--index: **THE ROW IS [ADR-394](../decisions/ADR-394.md), `proposed` and UNSIGNED. MONEY-ADJACENT, ADR-003 STRICT, `E2` READ OWED.** Four items, three carrying verdicts already derived elsewhere, all four re-derived here and all four verdicts surviving. **THE RETIRED SINK CLAUSE IS REPAIRED AT [ADR-376](../decisions/ADR-376.md) LINE 92**, which [ADR-385](../decisions/ADR-385.md) section 9 item 4 raised as a new finding and which no row had ever been given the file for, with the false absolute and its false consequence kept whole beside their correction under `RI-14` and NAMED rather than reproduced per [ADR-367](../decisions/ADR-367.md). **THE FALSIFICATION IS RE-DERIVED AT THE TREE AND IS SHARPER THAN THE PUBLISHED ONE**: [ADR-382](../decisions/ADR-382.md) proved a refusing `ExpiryEventPort` is composable, and `test/expiry-adapter.test.ts` actually PASSES `UNWIRED_EXPIRY_SWEEP_IO`'s member to `expirySweepIo` and asserts it back, so a claim that nothing in the tree can be passed is refuted by a green test in the tree that passes something. **RULED: A REPAIR MINTS A NEW POINTER, SO A REPAIR IS BOUND BY [ADR-388](../decisions/ADR-388.md)'s RULE FOR NEW POINTERS.** Both class-one pointers are therefore repaired to the NAME of their subject and not to a fresh coordinate, because both targets move again inside this wave and the session-342 target moves inside this row's own diff, sitting below the very allocation table this row appends three rows to. **A POINTER REPAIRED TO A COORDINATE ITS OWN DIFF INVALIDATES IS THE DEFECT RE-ARMED WITH A FRESHER DATE.** `P7`'s pointer and session 342's are CLASS ONE, each derived by `git blame` plus a read of the cited file at the blamed commit rather than inherited. **[ADR-337](../decisions/ADR-337.md) LINE 70 IS CLASS TWO, RE-DERIVED A THIRD TIME AND LEFT WRONG**: at the commit that wrote it the coordinate held the `RI-36` row while `RI-37` sat ONE LINE BELOW, so it was off by one on the day it was written and repairing it would manufacture evidence of a check that never happened. **THE FINDING IS NOW WRITTEN BESIDE THAT POINTER**, which [ADR-388](../decisions/ADR-388.md) requires and no row had done, so a fourth row does not spend itself confirming it again. **THE GENERATOR COUPLING IS VERIFIED AT THE GENERATOR AND THEN WATCHED, NOT ASSUMED**: `gates.mjs`'s `session_entries` copies a session file's own `<!--index:` text verbatim into the sessions README, [`split-sessions.mjs`](../../scripts/corpus/split-sessions.mjs) is NOT the mechanism a reader would have guessed and cannot run at all because `docs/SESSION_LOG.md` does not exist, and one repair moved exactly one line of that README. **THE DISPATCH'S INHERITED FIGURE DOES NOT SURVIVE**: [ADR-391](../decisions/ADR-391.md) calls `ADR-376 line 92` a positional reference in FOUR documents and says it derived that, and it is FIVE at the parent of its own commit, SEVEN at that commit and seven on `c5648f00`. That is why every edit here is line-count neutral and why NOTHING outside this fence went stale. **THE CLONE WAS SHALLOW ON ARRIVAL AND WAS DEEPENED BEFORE ANY HISTORY WAS READ**, since three of four items are decided by `git blame`, which a truncated graph answers wrongly rather than refusing. **THE `schedule.ts` CLAUSE IS OWED AND NAMED WITHOUT A COORDINATE, WITH ITS EXACT REPLACEMENT WRITTEN OUT**, and a SECOND clause in the same header carries the same defect and is recorded with it. **THE STANDING BAR IS UNTOUCHED AND THIS IS THE SIXTH CONSECUTIVE ROW TO LEAVE IT STANDING.** No port wired, no job scheduled, no sink passed, no default parameter added to `expirySweepIo`. **NOT ONE EXECUTABLE LINE CHANGED, NOTHING OUTSIDE `docs/` WAS WRITTEN, AND NO CITATION MOVED.** No invariant minted, no `CI-06` letter spent, no migration taken or reserved, no gate weakened, no test skipped, disabled or quarantined. Suite **336 / 8,107 / 10 / 0** against a base of **336 / 8,107 / 10 / 0** reproduced on `c5648f00` before the first edit, **delta ZERO on every axis**; gates **33 of 33**, invariants **35 of 35**, typecheck, lint and `format:check` clean -->

---

### 1. The objective

Row `394`: repair the retired sink clause at [ADR-376](../decisions/ADR-376.md), which had been raised as a finding and never fenced to anybody, and settle three pointers whose classes the dispatch supplied and required to be derived again.

**One objective.** It resolved into three repairs, one deliberate refusal to repair, one finding written beside the pointer it belongs to, one owed item with its replacement written out, and one refuted premise.

---

### 2. What was measured before the first edit

**The container arrived with no repository at all.** `/home/user` was empty, with no clone and no `node_modules`, and `git rev-parse` failed in the directory the dispatch named as the repository root. The tree was attached to this session's authorized set and cloned by hand. [ADR-382](../decisions/ADR-382.md) recorded this as the eleventh consecutive session and it has not been fixed since.

**The clone was shallow and it was deepened before any history was read.** `git rev-parse --is-shallow-repository` returned `true` on arrival. After a bounded refetch over the base branch and `main` it returns `false`, `.git/shallow` is absent and the base branch has a full ancestry. **Every class verdict in this row was derived only after that was true**, which matters because a shallow clone does not refuse a `git blame`, it answers from a truncated graph.

**`HEAD` was confirmed to be `c5648f00` rather than assumed.**

**Base, on a clean tree:** 336 files / 8,107 passed / 10 skipped / 0 failed. Gates **33 of 33**, invariants **35 of 35**, `typecheck`, `lint` and `format:check` all clean.

---

### 3. What landed

1. **[ADR-376](../decisions/ADR-376.md) line 92**, the retired sink clause, repaired in place under `RI-14` with the retired half named rather than reproduced.
2. **[P7](../plans/P7-risk-and-abuse.md)**, class one, repaired to the **Scheduled digest delivery** row name rather than to a coordinate in a runbook another row is restructuring this wave.
3. **[Session 342](2026-08-28-session-342.md)**, class one, repaired to the named paragraph rather than to a coordinate this row's own allocation rows would move, and the sessions README followed it through the generator.
4. **[ADR-337](../decisions/ADR-337.md) line 70**, class two, left wrong with the finding written beside it.
5. **[ADR-394](../decisions/ADR-394.md)**, the ruling, `proposed` and UNSIGNED.

---

### 4. Landmines

- **An owed list is now a source of errors.** [ADR-391](../decisions/ADR-391.md)'s "four documents" is wrong, after [ADR-385](../decisions/ADR-385.md)'s published repair was wrong for three waves. Both were written to be picked up without re-derivation, which is exactly the property that makes a wrong one expensive.
- **`apps/worker/src/schedule.ts` carries TWO clauses of the same defect, not one.** A row given that file and only the first will leave the second.
- **The `G2` reads are single-commit reads.** They are taken at the commit `git blame` names as the last writer of the citing line, which is the ruling's test and cannot distinguish an unrelated later rewrite.

---

### 5. Next

The founder's `E2` read of [ADR-394](../decisions/ADR-394.md) and the three decisions its approval block names, of which the first is whether a repaired pointer should ever be a coordinate again given that two of two were knowably unstable at the moment of writing.

Counts derived at reporting time off each runner's own last line: suite **336 files / 8,107 passed / 10 skipped / 0 failed**, against a base reproduced on `c5648f00` **before the first edit** of **336 / 8,107 / 10 / 0**, delta **ZERO on every axis**; gates **33 of 33**, invariants **35 of 35**, `typecheck`, `lint` and `format:check` clean.
