## 2026-08-16 - Session 34: the phantom payout status, and a parallel FOLD-02 session 4 that lost the race

- **Done:** [M05](../plans/M05-payout-system.md) said `status: settled_to_wallet` in **two** places. That value does not exist and never has: [ADR-028](../decisions/ADR-028.md) considered it and **rejected it by name**, and [`0001`](../../packages/db/migrations/0001_extensions_and_enums.sql) records the rejection in its own comment block. Both sites corrected to `settled`, with the provenance written into the file. All 12 gates green, `falsify.mjs` green.
- **Next:** FOLD-02 session 5 (M02's provisional platform leg, M06, M03, M04, M16, the M08 confirmation), unchanged by this session.
- **Blockers:** None.
- **Landmines:**
  - **ADR-028 believed it had corrected this file, and quotes the phantom sentence as evidence for its own ruling.** It names M05's freeze target as one of two sites it fixed and read past `settled_to_wallet` twice on the way there. **ADR-040's list of four remaining sites does not carry it either**, because that list was assembled by grepping for `transferring` and this defect is a different word. **A sweep scoped to the symptom finds only the symptom.**
  - **This session originally did the whole of FOLD-02 session 4 and a parallel session merged first** (PR #27). Rather than open a competing pull request, the branch was checked finding by finding against `main`, and **all but one item was already there**: `G-NO-IN-FLIGHT` and `G-FREEZE-DURING-FLIGHT` are corrected on main, `held_pending_review` is in the machine, M07's enforcement outcomes and M20's halt properties are folded. Only the phantom status survived, so only the phantom status is in this diff. **The duplicated work is the cost of two sessions holding one plan section, and the branch `claude/fold02-s4-machines-and-surfaces` is abandoned rather than merged.**
  - **The merged version deliberately did NOT draw the `wallet_withdrawals` machine**, calling it "a fold of its own and not FOLD-02's", and it found a defect this session did not: ADR-028 named `G-SETTLEMENT-CONFIRMED` and `G-TRANSFER-EXHAUSTED` as guards, and **section 10 has never defined either**; the guards that exist are `G-WEBHOOK-SETTLED` and `G-RETRY-BUDGET-EXHAUSTED`. **The seven-state external-leg machine is still undrawn** and is the open item that survives both sessions.
  - **Session numbers are racing and this entry is 34 because 33 was taken three times.** Four entries are numbered 31 and three each at 32 and 33, written by parallel sessions each taking the next number it could see. `CI-06n` cannot see it because every one of those rows resolves to a file that exists. This is [ADR-034](../decisions/ADR-034.md)'s failure one registry over, in the registry that records the sessions doing the folding.
- **Files touched:** `docs/plans/M05-payout-system.md`, `docs/sessions/`.
