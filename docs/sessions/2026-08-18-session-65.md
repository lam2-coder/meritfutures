---
status: approved
depends_on: [README.md]
last_updated: 2026-08-18
---

## 2026-08-18 - Session 65: the replay self-audit's comparison is wired (money path, plan mode)
<!--index: the replay self-audit's comparison is wired: hash first, field diff on mismatch, and an empty scope refuses -->

- **Done:** **INV-04's right-hand side exists.** [`replay.ts`](../../apps/worker/src/batch/replay.ts) re-derives a stored row and compares it: `state_hash` first by `Buffer.equals`, then a field-by-field diff **only on mismatch** (B.2), naming the SQL column or `engine_gates.<dotted.path>`. Scope is B.4 step 1 read twice, for `engine_version` and for `calendar_revision_id` ([ADR-047](../decisions/ADR-047.md)), as an explicit partition with the mode on the report so B.4 step 2's dry run needs no second comparator. Findings go through a new `raiseDivergence` port, grouped per account-day and expanded by the adapter to satisfy `EVENTS.md:190`'s singular `field`. **No schema change: `0038` is untouched and unclaimed.** Suite **789 -> 850 passed**, 38 skipped unchanged, 17 of 17 gates, typecheck/lint/format clean.
- **Next:** the four unwired obligations named in the module header, each out of this fence: B.1's payout halt, the event's destination (`0017`'s tables), a scheduler, and `replay.audit_completed`, which [`CRON_INVENTORY.md`](../ops/runbooks/CRON_INVENTORY.md) requires and the `EVENTS.md` catalogue does not define.
- **Blockers:** none for this objective.
- **Landmines:**
  - **`OI-14` IS SATISFIED AND ITS ROW IS NOT CLOSED, AND THE DIFFERENCE MATTERS.** The refusal on an empty in-scope set is built and tested. `OI-14` is a row in [`packages/db/DELTA_MANIFEST.md`](../../packages/db/DELTA_MANIFEST.md), outside this session's fence, so its closure is a documentation edit somebody else makes. **A session that reports closing a registry row it cannot edit leaves the next reader believing a registry moved when it did not.**
  - **AN AUDIT THAT FOLDS FROM `AccountDay.prior` AUDITS THE VALUE IT IS AUDITING.** `foldAccountDay` dropped `out.state` and `RuleStateRow` cannot rebuild a `RuleState`, so the obvious implementation reads the stored prior, folds day 41 off the poisoned day 40, and reports one divergence where there are 210. Watched failing: `expected 1 to be 3`.
  - **A POISONED STORED ROW DOES NOT PROPAGATE AND CANNOT PROVE THIS.** Later stored rows stay faithful, so both implementations report one divergence. The fixture must poison the **state** later days fold from.
  - **AND THE POISON CANNOT BE A BALANCE.** `balanceCents + 1` makes the next day's DO-3 fail `INV-18` and the fold **refuses** to write a row, so no downstream-wrong storage exists to audit. Only a field no assertion cross-checks, such as a counter, produces it. The engine refusing is the engine working.
  - **`state-hash.test.ts` PASSING UNTOUCHED IS THE PROOF THE WIDENING MOVED NO BYTE**, including its independently transcribed serialization. `Pick` is erased; no runtime proof was needed or written for that half.
- **Files touched:** [`replay.ts`](../../apps/worker/src/batch/replay.ts), [`ports.ts`](../../apps/worker/src/batch/ports.ts), [`nightly.ts`](../../apps/worker/src/batch/nightly.ts), [`state-hash.ts`](../../apps/worker/src/batch/state-hash.ts), [`index.ts`](../../apps/worker/src/index.ts), `test/replay.test.ts`, `test/fixtures.ts`, `test/nightly-batch.test.ts`, this log, [STATE](../STATE.md).
