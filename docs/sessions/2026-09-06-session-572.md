---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 572: the fifty pointers worth nothing are eight thousand two hundred and fifty, the two remedies offered were measured and both are dead, and the pointer that died inside the checker itself was true the day it was written

<!--index: **THE ROW IS [ADR-377](../decisions/ADR-377.md), `proposed` and UNSIGNED. CONTROL OVER THE CORPUS'S HONESTY, `E2` READ OWED.** [ADR-375](../decisions/ADR-375.md) section 8 measured fifty dead pointers into one file and asked whether a path-plus-line citation can be made checkable and whether it is worth it. **RULED: OUTCOME (c). THE DEAD POINTERS ARE REPAIRED WHERE SOMEBODY OWNS THEM AND THE FORM IS LEFT ALONE.** The population is **8,723 citation sites, 473 named and 8,250 path-and-line only, a checkable fraction of 5.4%**, derived by importing the checkers' own reader; **`wiring.test.ts` is EIGHTH and not worst**, at 168 against `scoped-db.ts`'s 436. **Outcome (b) is measurably dead**: of 1,558 bare citations, exactly ONE carries a quote that lands near the line it cites. **Widening `IDENTIFIER_GLUE` reaches none of them and turns 33 of 49 newly-bound citations RED.** **Two further blindfolds are named**: `RI-15` cannot read its own package, and the NAMED form binds or does not depending on where in a file it sits. **The exemplary slice is two citations, both owed and both in this fence**, retired rather than repointed because the subject is gone and because [ADR-375](../decisions/ADR-375.md)'s instruction to repoint them was itself wrong. **`RI-35`'s ordering obstacle has cleared itself** and what remains is a line split outside this fence. Five seeds, two RED and three GREEN on purpose, all restored byte-identical. Suite **336 / 8,070 / 10 / 0** against a base of **336 / 8,070 / 10 / 0**, a delta of **ZERO**. -->

---

### 1. The objective

Row `377`: answer [ADR-375](../decisions/ADR-375.md) section 8's question. **Can a citation that carries only a path and a line be made checkable, and is it worth it?**

**One objective.** It resolved into one ruling, one corpus-wide census, three refusals each measured before it was made, two repairs, and one correction to the entry that asked the question.

---

### 2. What was measured before the first edit

**THE CONTAINER ARRIVED WITH NO REPOSITORY.** `/home/user` was empty. The tree was attached and cloned inline, landing on `6698f9c0`, which is exactly the base this row was cut from, and `pnpm install --frozen-lockfile` ran clean.

**Base, on a clean tree, before anything was touched: 336 files / 8,070 passed / 10 skipped / 0 failed.** Gates **33 of 33** after `generate`, invariants **35 of 35**, `typecheck`, `lint` and `format:check` clean, and `git status` clean after `generate`.

---

### 3. The census, derived rather than counted

**BY IMPORTING THE CHECKERS' OWN READER**, from an export-added copy of `repo-invariants.mjs` placed in its own directory so `REPO_ROOT` resolved, deleted afterwards. **A hand-rolled regex would have measured a different grammar than the one the checks enforce**, which is the subject.

```
                                    citations   named    bare   named%
RI-15  source files                       389     129     260    33.2
RI-16  live docs, in scope               1369      71    1298     5.2
       docs under a dated heading        6965     273    6692     3.9
       CORPUS-WIDE                       8723     473    8250     5.4
```

**AND THE FILE FOUR WAVES FENCED IS EIGHTH.** `scoped-db.ts` carries 436 sites from 136 files at 170 distinct lines; `wiring.test.ts` carries 168 from 67 at 48.

---

### 4. What was refused, each measured first

1. **A checkable FORM adopted going forward** is 8,250 sites, and it cannot be scoped to new citations because `RI-15` never reads git history and says so as its own miss (7).
2. **A content check** was measured and is dead: 335 bare citations carry a quoted phrase, **ONE** quote lands near the pointer, 58 appear nowhere in the file they sit beside.
3. **Widening `IDENTIFIER_GLUE`** binds none of the pointers in question and turns **33 of 49** newly-bound citations RED on the nearest common word.
4. **Narrowing `RI-15`'s own-package exclusion** would newly guard **six** citations, all of which pass, and the exclusion is structurally correct because those files reproduce false citations as specimens.

---

### 5. What landed

- **`docs/decisions/ADR-377.md`**, the ruling.
- **Two citations retired**, in `packages/tooling/checks/repo-invariants.mjs` and `packages/tooling/test/repo-invariants.test.ts`. Both cited `wiring.test.ts` at a line that was **TRUE the day it was written**, at commit `70571d0d`, the commit that introduced `RI-15`, where the file was 424 lines. It is 3,593 now and the clause the pointer named has been rewritten away. **They carry no line number now.**
- The `377` row in [ALLOCATION](../decisions/ALLOCATION.md), the [decisions README](../decisions/README.md) row, this entry and [STATE](../STATE.md).

**NO INVARIANT WAS MINTED OR WIDENED.** `RI-15`'s grammar was not opened for writing and `RI-35` was not extended, on [ADR-371](../decisions/ADR-371.md) section 5.2's ruling re-read here and not disturbed.

---

### 6. The correction this row owes the entry that dispatched it

**[ADR-375](../decisions/ADR-375.md) SECTION 12 ITEM 2 TOLD THE NEXT ROW WHICH LINE THOSE TWO CITATIONS MEAN, AND IT IS THE WRONG LINE.** That line carries a comment about `API_CONTRACT`; the subject is at `:920` and `:1108`. **The repair tracked where the old line's TEXT had moved rather than where the SUBJECT had moved.** Following the instruction would have written a second wrong pointer into this fence.

**Two of that entry's three figures reproduce exactly** and the site count is one higher than any commit of its own branch held. **The retired figure is NAMED rather than re-quoted, under `RI-14`.**

---

### 7. Falsification

**FIVE SEEDS, TWO RED AND THREE GREEN ON PURPOSE, every one restored byte-identical and verified with `sha256sum -c` against a four-file manifest re-checked after each restore.** Thirty lines into `wiring.test.ts` stayed GREEN at 35 of 35, reproducing [ADR-375](../decisions/ADR-375.md)'s probe on this tree rather than inheriting it. A past-end-of-file citation went RED at 34 of 35; **the same citation inside `packages/tooling/` stayed GREEN**, which is the own-package finding as an A/B needing no name to bind. Putting the retired pointer back into this row's own repair reddened nothing.

**THE SEED THAT CHANGED THE WORK WENT THE WRONG WAY FIRST.** A drifted named citation written the obvious way went GREEN, and chasing it produced the finding that binding depends on position in the file. **A row that had accepted that green would have reported `RI-15`'s strong leg dead.**

---

### 8. Blockers and landmines

- **`apps/api/test/wiring.test.ts:3252` cites a line that does not carry what it names**, the same error as section 6 and made by the same row. Outside this fence, reported and not repaired.
- **`RI-15` now has four named holes and none is repaired**: the prose subject, `IDENTIFIER_GLUE`, `DECLARATION_SHAPES`, and position-dependent binding, which is new here.
- **Four of the forty-eight distinct lines cited into `wiring.test.ts` are now blank or a bare closing brace.** `RI-15`'s vacancy leg would name every one if those citers were in scope; they are documents under dated headings.

---

### 9. Verification, derived at reporting time

Suite **336 files / 8,070 passed / 10 skipped / 0 failed**, against a base reproduced on `6698f9c0` before the first edit of **336 / 8,070 / 10 / 0**, a delta of **ZERO on every axis**, attributed by removal: `repo-invariants.test.ts` runs **257** alone on this tree and **257** alone on the base. **This row's code diff is comment text in two files and changes no behaviour.** Gates **33 of 33** after `generate`. Invariants **35 of 35**. `typecheck`, `lint` and `format:check` clean. **`pnpm run verify` and `scripts/corpus/falsify.mjs` were NOT run**, both forbidden by the row. **`git diff origin/main -- apps packages/db packages/rules-engine packages/ledger scripts` is EMPTY.** No row is reserved: `377` follows `376`, the highest on the merged head.

---

### 10. This session's own tooling, measured rather than assumed

**GITHUB API ACCESS: NO.** `gh` is not on `PATH`, no `mcp__github__*` tool is offered, and `curl` against the repository API returns **403**. **`git push` authenticated normally**, which is the pattern prior rows recorded. The parent opens the pull request.

**THE BARE-COMMAND NOTE HELD.** Every Bash call ran from the working directory with no compound `cd` prefix and there were zero permission stalls.

**ONE THING COST TIME AND IS WORTH RECORDING.** The export-added copy of `repo-invariants.mjs` failed to import with `Cannot access 'ri11' before initialization`, **which that file's own header predicts in its own words** about the `ui-server-endpoints.mjs` cycle: importing that module first throws exactly this. Importing the real module before the copy resolved it.

---

### 11. Next

The founder's `E2` read of [ADR-377](../decisions/ADR-377.md) and the three decisions its approval block names, of which **the first is a decision to live with a known hole**: 8,250 citations that no check can hold honest, and the alternative is not a check but a smaller corpus of citations. Then section 8's correction, which is about a standard no check enforces. Then **`RI-35`'s remaining obstacle, which is now a line split rather than a repair**, so the next row does not re-derive section 9.
