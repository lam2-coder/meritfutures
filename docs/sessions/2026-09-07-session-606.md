---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 606: the fifth blocker stands, both its terms are outside this fence, and the file that states it was the last one pricing a refactor that had already landed

<!--index: **THE ROW IS [ADR-411](../decisions/ADR-411.md), `proposed` and UNSIGNED. MONEY PATH, THE ADMIN LIABILITY READ, `E2` READ OWED. ONE `src/` FILE CHANGED AND IT IS A COMMENT.** **THE FIFTH BLOCKER IS `B5`, `eligible_next_7d`'s PER-ACCOUNT HALF, DERIVED AT [`liability.ts`](../../apps/api/src/admin-source/liability.ts) RATHER THAN INHERITED FROM [ADR-407](../decisions/ADR-407.md).** **IT CANNOT BE CLEARED FROM THIS FENCE AND THE TWO TERMS FAIL DIFFERENTLY**: term 2 widens `EligibleNext7d` across the three copies `RI-18` binds, none of them in fence and all three read; term 1 is a supplier of `EligibleFoldIo.resolvePinnedPlan` whose cheapest home is the unexported `planLeg` in `payout-backend.ts`, one file outside. **SO `readLiability` IS NOT COMPOSED, `IMPLEMENTED_ADMIN_READS` IS UNCHANGED AND [`wiring.test.ts`](../../apps/api/test/wiring.test.ts) NEEDED NO EDIT**, which was verified rather than assumed. **THE DEFECT IS TERM 1 ITSELF**: `liability.ts` said the blob decoded in one place, in `apps/worker`, out of this deployable's reach, and priced the term as a `packages/rules-engine` move that had not landed. **ALL FOUR CLAUSES ARE FALSE BY MEASUREMENT** and the one-place clause is false in the other direction too, the census being three. **THREE OF ITS OWN NEIGHBOURS ALREADY RECORD THE CORRECTION AND THE CANONICAL SIDE DID NOT MOVE.** **`RI-19` HELD OVER IT BECAUSE IT BINDS THE MODULE TO THE CASE AND NEITHER TO THE TREE**, and both copies drifted together; the property is now held locally as POSITIVE reads at primary sources plus a census over `apps/api/src` for the residue. **THERE IS NO SIXTH BLOCKER**, derived three ways from the file and from no entry, and the third read, every `Bn` label mentioned and never opened, is the one that would have caught `B5`, which arrived as unnumbered prose and sat for four sessions. **ONE THING BLOCKS THE METHOD THAT IS NOT A BLOCKER ON A FIGURE**: `LiabilityGapCause` has no member for a producer a deployment did not install, unchanged. **TWO TEMPTATIONS REFUSED AND BOTH NAMED**, the second new: widening `LiabilityTx` and writing the `PlanVersionSizeRow` mapping a second time on the money path would have spent a term inside fence with nothing in the tree to stop it. **CONTAINER EMPTY AGAIN, ELEVENTH IN A ROW; FULL CLONE, 15.0s, 4348 COMMITS, 185M.** **EVERY NEW CONTROL WATCHED RED UNDER FIVE SEEDS BEFORE IT WAS BELIEVED.** **NO GATE ADDED, WIDENED OR WEAKENED, NO TEST SKIPPED, NO INVARIANT MINTED, NO MIGRATION, NO `CI-06` LETTER.** -->

### 1. The objective, and what it turned into

The row was dispatched at the fifth blocker on `AdminReadSource.readLiability`, with two branches: clear it and compose the method, or state exactly what it needs and then check whether a sixth exists.

**THE SECOND BRANCH IS WHAT HAPPENED, AND THE FIFTH BLOCKER IS REAL.** [ADR-411](../decisions/ADR-411.md) section 4 derives it at [`liability.ts`](../../apps/api/src/admin-source/liability.ts): `B5` is `eligible_next_7d`'s per-account half, a second blocker on the group `B1` held, and the dispatch's summary of [ADR-407](../decisions/ADR-407.md) reproduces at the file rather than being taken from it.

### 2. Why it could not be cleared

Section 5 of the entry, term by term, with the reads that decide each.

| Term | What it is | Whose |
|---|---|---|
| 1 | A supplier of `EligibleFoldIo.resolvePinnedPlan`: the decode composed with the account's `plan_version_sizes` read | **ANOTHER ROW'S.** A slice, not a ruling, and not a purchase |
| 2 | A wire that can say the figure is a forecast: `EligibleNext7d` carries no measured term | **A FOUNDER'S TO DIRECT, ANOTHER ROW'S TO EXECUTE.** Not a purchase |

**THE GROUP GOES WHOLE OR NOT AT ALL**, so either term alone leaves it unproducible.

**TERM 1 IS MUCH SMALLER THAN THREE SESSIONS OF RECORDS SAID.** `decodePlanRules` is re-exported from the engine's index, this deployable has declared `@merit/rules-engine` since session 252, and `planLeg` in `payout-backend.ts` already decodes the blob and resolves the plan. What is unsupplied is a composition, and `LiabilityTx` declares `rows` and `rowsWhere` and no `catalogRowAt`, so this module cannot address the size row. **THE CHEAP VERSION IS ONE EXPORT AND ONE CALL IN A FILE THIS FENCE MAY NOT WRITE.**

### 3. The defect, which is in the blocker's own first term

`liability.ts` was the last statement in this fence still pricing term 1 as a `packages/rules-engine` move. [ADR-283](../decisions/ADR-283.md) made that move. [`eligible-next-7d.ts`](../../apps/api/src/admin-source/eligible-next-7d.ts) records the correction twice, [`index.ts`](../../apps/api/src/admin-source/index.ts) records it, and `wiring.test.ts` records it at length. **THE CANONICAL SIDE IS THE ONE THAT DID NOT MOVE**, and `RI-19` reads the module as the term set.

**THIS IS [ADR-407](../decisions/ADR-407.md) SECTION 7's DEFECT ONE FILE OVER, ON THE STANDING BLOCKER RATHER THAN THE SPENT ONES.** The repair paraphrases and does not quote, on `RI-14`'s rule.

### 4. What `RI-19` could not see

It binds the module's statement to the case's restatement and neither to the tree. Both were written from each other and both were wrong about the same fact, so the check compared two copies of a retired premise and passed on every run. **REPORTED AND NOT REPAIRED**: widening it is a different check and `packages/tooling` is outside this fence. The property is held locally instead, in the direction that matters.

### 5. Whether a sixth exists

**NO. FIVE, DERIVED THREE WAYS FROM THE FILE AND FROM NO ENTRY**, this dispatch's summary included: the heading's count word, the numbered item openers, and every distinct `Bn` label mentioned anywhere whether opened or not. All three agree.

**THE THIRD IS THE ONE THAT WOULD HAVE CAUGHT `B5`** and it is now a control that reports an orphaned label with the line it was first mentioned on. The file's one denial of a further blocker was checked at its own subject and is sound.

**ONE THING BLOCKS THE METHOD AND IS NOT ONE OF THE FIVE**: the gap cause for an unwired producer, [ADR-407](../decisions/ADR-407.md) section 11 item 1, unchanged.

### 6. The container and the base

`/home/user` held `.` and `..`. No repository, the eleventh consecutive row to record that. A FULL clone, 15.0 seconds, `is-shallow-repository` `false`, 4348 commits, `.git` at 185M, base `08b9acf2` on `claude/wave-407s`.

Baseline on a clean tree, every leg run directly, `verify` and `falsify.mjs` not run: 33 of 33 gates, 35 of 35 invariants, 337 test files and 8141 tests passing with 10 skipped, typecheck done, lint silent, format clean. After the diff: the same everywhere but the suite, which is 8144 passing and 10 skipped, grown by the three cases of section 5 and nothing else.

### 7. What was refused

Two things, both in [ADR-411](../decisions/ADR-411.md) section 10. Composing with a `null` and an `awaiting_dependency` gap, which contradicts the ruling that fixes this field's causes in three copies. **AND, NEW THIS ROW, TAKING TERM 1 BADLY**: widening `LiabilityTx` and writing the `PlanVersionSizeRow` mapping a second time in this deployable would have spent a term entirely inside fence with nothing in the tree to stop it, on the blob that fixes every cents threshold a payout is decided against.
