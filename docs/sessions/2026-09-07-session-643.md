---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 643: the guard landed 3,300 lines from where it was priced because that was the only place it could go, and the widening beside it costs two files nobody owns

<!--index: **THE ROW IS [ADR-448](../decisions/ADR-448.md), `proposed` and UNSIGNED. MONEY PATH, `E2` READ OWED.** [ADR-445](../decisions/ADR-445.md) section 8 item 1's hole is CLOSED: `updateStatementOn` reached `.set(values)` at [`scoped-db.ts:1221`](../../packages/db/src/scoped-db.ts) with the caller's keys unfiltered, and `refuseGeneratedColumn` now stands in that `.set(...)`. **THREE OF THE ROW'S OWN INSTRUCTIONS DID NOT SURVIVE MEASUREMENT AND ALL THREE ARE REPORTED.** ONE, **the priced placement is not satisfiable**: `scoped-db.ts` carries **168 distinct cited line numbers** and **135 sit below the `:473` the price names**, and the live pointers among them are under `apps/api/**` and `apps/worker/**`, which this fence does not hold, so [ADR-386](../decisions/ADR-386.md)'s NO CITED LINE MOVES and the fence are jointly unsatisfiable there. The guard is appended below `:4581`, the highest cited line, on the idiom the file's own tail already records, and the CALL **replaces the text of `:1221`** rather than inserting a line above it, which is why it returns the values it cleared instead of returning `void`. **The diff is exactly two hunks and ZERO cited lines move.** TWO, **`isGenerated` does not exist on a Drizzle column**, and the price names it: probed on the real registry it is `undefined` on every column including the generated one, the properties are `generated` and `generatedIdentity`, and a guard written to that wording would have refused nothing and passed a suite that never saw it fire. That is the SECOND consecutive entry to price work against a Drizzle property this tree does not have, after `$inferInsert`. THREE, **the identity widening is NOT taken and the reason is a measurement**: seeded and run, it turns **four cases red across two files**, [`keyed-accessor.test.ts:442`](../../packages/db/test/keyed-accessor.test.ts) and [`write-accessor.test.ts:311`](../../packages/db/test/write-accessor.test.ts), [`:479`](../../packages/db/test/write-accessor.test.ts), [`:887`](../../packages/db/test/write-accessor.test.ts), because `someOtherColumn` and `setFor` each skip the tenancy columns and take the first column left, which on `ledger_entries` and `liability_snapshots` is `id`. **Those four cases assert the accessor ACCEPTS an UPDATE PostgreSQL answers `428C9` to**, which is a finding worth more than the widening, and neither file is in this fence. **THE POPULATION IS DERIVED THREE WAYS THAT AGREE**: 75 migrations, **20** `GENERATED ALWAYS AS IDENTITY` in the DDL across 14 files, 20 `.generatedAlwaysAsIdentity` in `schema.ts`, 20 on the registry through Drizzle, every one `id` and every one `always`; and 6 stored generated columns of which **5 are reachable**, because `live_account_state` is not a registry table. The 20 is derived and not inherited. **THE CONTROL WAS WATCHED FIRING TWICE.** Guard removed: the checker went to **6 findings naming all five reachable columns including `reconciliations.delta_cents`**, and four cases went red. Widening seeded: four cases red across two files. **Both reverted and `git hash-object` verified byte-identical, never `git checkout --`.** The demonstration [ADR-445](../decisions/ADR-445.md) shipped to hold the hole open is DELETED with its `updateTargets` helper in the same commit, per `RI-14`, and **leg D replaces it**, asserting the refusal over the real registry and the real builder and cross-folding the identity count against leg A's DDL fold. **`RI-15` CAUGHT THIS ROW'S OWN FIRST DRAFT**: it cited a line inside the installed `drizzle-orm` package, which is in no file of this tree, the invariant was right and the draft was wrong, and no pointer into `node_modules` survives anywhere in the diff. Suite **344 files / 8,607 passed / 11 skipped / 0 failed** against a base of **344 / 8,605 / 11 / 0** measured in this same session on `ea353e8`, delta **+2 passed and +2 total**, attributed by removal: two demonstration cases deleted, four leg D cases added. Invariants **35 of 35**, gates green after `generate`, typecheck, lint and `format:check` clean. **`pnpm run verify` and `scripts/corpus/falsify.mjs` NOT run**, both forbidden. **No migration, no `schema.ts`, no `scoped-db.test.ts`, no file under `apps/`, no test under `packages/db/test/`, no gate weakened and no test skipped, disabled or quarantined.** **Nothing here answers [ADR-437](../decisions/ADR-437.md) section 13**: this row adds a REFUSAL and not a writer. -->

### 1. The objective

Close [ADR-445](../decisions/ADR-445.md) section 8 item 1: refuse a values key naming a stored generated column in the accessor's UPDATE builder, delete the demonstration that held the hole open, and take section 8 item 2's identity widening with it.

**THE DATE IS THE SESSION'S AND NOT THE WALL CLOCK'S.** The session opened at `2026-09-07T23:43Z` and crossed midnight UTC during the base measurement.

### 2. Done

- **The guard.** `refuseGeneratedColumn` in [`scoped-db.ts`](../../packages/db/src/scoped-db.ts), refusing both spellings of a stored generated column, naming the column, the rule and `42601`. Called from `.set(...)` at `:1221`.
- **The placement, derived rather than taken from the price.** 168 distinct cited lines, 135 below `:473`, live pointers outside the fence. Appended below `:4581`; call replaces the text of `:1221`. **Two hunks, zero cited lines moved.**
- **Leg D** in [`generated-column-writes.mjs`](../../packages/tooling/checks/generated-column-writes.mjs), asserting the refusal on the real registry and cross-folding the identity count against leg A.
- **The demonstration deleted**, with its `updateTargets` helper, per `RI-14`, and the checker header's now-false paragraphs narrowed beside the record of what they said.
- **[`eslint.config.js:42`](../../eslint.config.js)**, one glob added in place so line 41 and 42 both stay put. [ADR-445](../decisions/ADR-445.md) section 8 item 5.
- **[ADR-448](../decisions/ADR-448.md)**, `proposed`, UNSIGNED, with section 8 carrying seven owed items each cited at `file:line`.

### 3. Next

Section 8 item 1 of [ADR-448](../decisions/ADR-448.md) is the one to take first and it is owed whether or not the widening ever lands: one clause each in `setFor` at [`keyed-accessor.test.ts:196`](../../packages/db/test/keyed-accessor.test.ts) and `someOtherColumn` at [`write-accessor.test.ts:180`](../../packages/db/test/write-accessor.test.ts). The identity widening cannot land green before it.

### 4. Blockers

**The identity widening, blocked by the fence rather than by the code.** One clause in the guard closes it; four cases in two unowned files go red when it does. The row resolved fence against red suite in favour of the fence and recorded the alternative.

### 5. Landmines

- **`isGenerated` IS NOT A DRIZZLE PROPERTY.** Anyone repairing this area from [ADR-445](../decisions/ADR-445.md)'s wording will write a guard that refuses nothing. The properties are `generated` and `generatedIdentity`.
- **THE GUARD SITS 3,300 LINES FROM THE TWO IT BELONGS BESIDE AND RETURNS A VALUE INSTEAD OF `void`.** Both are consequences of the citation graph, both are stated in the file, and neither is taste.
- **FOUR CASES IN THIS SUITE ASSERT A WRITE POSTGRESQL WOULD REFUSE WITH `428C9`.** They are green because no database is involved.
- **THE GUARD READS `values` AND MUST NEVER READ AN ADDRESS.** [`sweep.ts:615`](../../apps/worker/src/recon/sweep.ts) addresses `reconciliations` by its identity `id` and depends on that distinction.

### 6. Files touched

[`packages/db/src/scoped-db.ts`](../../packages/db/src/scoped-db.ts), [`packages/tooling/checks/generated-column-writes.mjs`](../../packages/tooling/checks/generated-column-writes.mjs), [`packages/tooling/test/generated-column-writes.test.ts`](../../packages/tooling/test/generated-column-writes.test.ts), [`eslint.config.js`](../../eslint.config.js), [`docs/decisions/ADR-448.md`](../decisions/ADR-448.md), [ALLOCATION](../decisions/ALLOCATION.md) row `448`, [decisions/README](../decisions/README.md), this log, [sessions/README](README.md), [INDEX](../INDEX.md) and [STATE](../STATE.md) for the `adr_count` span only.
