---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 584: the runbook restructure built, measured and refused, because its fifth column is read out of the table row by eleven assertions no row that owns the page may write

<!--index: **THE ROW IS [ADR-389](../decisions/ADR-389.md), `proposed` and UNSIGNED. CONTROL OVER AN OPERATIONS DOCUMENT A HUMAN READS UNDER PRESSURE, `E2` READ OWED.** **[ADR-388](../decisions/ADR-388.md) SECTION 15 ITEM 1 PRICED OPTION (d) AT "FIFTEEN POINTERS MOVE" AND SAID THE ROW TAKING IT NEEDS `docs/ops/runbooks/**` PLUS THE CITATION-REPAIR RIGHT. THAT SPECIFICATION IS ONE FENCE SHORT BY A FACTOR OF THREE.** The restructure was **BUILT** rather than estimated: columns one to four as the table, column five as a `## Severity if absent, by job` section of 21 `### <job>` notes keyed by the job name normalised exactly as `gates.mjs`'s own `normJob` normalises it. **IT WORKS AND THE FIGURES ARE GOOD**: the table falls from **31,301 characters to 2,700**, from nine rows over 2,000 characters to **zero**, from a longest row of **4,653 to 427**, and from **324 rendered lines at 100 columns to 34** with the tallest row going **47 to 5**; **columns one to four are BYTE-IDENTICAL across the move**, 2,427 trimmed characters at both ends, so nothing was rewritten or summarised. **Gates 33 of 33 and invariants 35 of 35 throughout, `RI-35` INCLUDED.** **AND THEN THE SUITE WENT RED AT ELEVEN ASSERTIONS IN SEVEN FILES ACROSS THREE DEPLOYABLES**, 8,096 passed against a base of 8,107. **TEN OF THE ELEVEN ASSERT THAT A NAMED PHRASE OF THE SEVERITY PROSE OCCURS IN THE TABLE ROW LINE**, which is the one thing option (d) exists to stop being true: `NOT YET WIRED OR SCHEDULED`, `DISCHARGED SINCE`, `GS-122`, `FM-M7-08`, `payout_requests.hold_expires_at`, `It asserts the query, not the job`. **FOUR OF THEM READ `schedule.test.ts`'s OWN COPY OF `cronRows`**, which returns the whole row line, so no note format puts the prose back without putting it back in the table. **SIXTEEN TEST FILES READ THAT RUNBOOK: FIFTEEN ARE ROW `391`'s, ONE IS ROW `390`'s, AND NONE IS THIS ROW'S.** Editing them is forbidden, shipping a red suite is worse than shipping nothing, and weakening a test is refused outright, **so the page is REVERTED BYTE-IDENTICAL (`git diff 3b664fcf -- docs/ops/runbooks` empty) and the corrected four-fence specification is handed on with everything measured**. **THE ELEVENTH FAILURE IS THE ONE THE DISPATCH TOLD THIS ROW TO WATCH AND IT IS NOT WHAT IT LOOKED LIKE**: `RI-35`'s registered anchor SURVIVES the restructure untouched, occurring once, on one line, in the `### Replay self-audit` note, because [ADR-384](../decisions/ADR-384.md) anchored it to a SENTENCE and not a coordinate; **the register needed no change and its TEST does**, asserting the anchor sits on the row it is about. **SEED E IS THE HAZARD NOBODY SHOULD REDISCOVER**: written first as a `###` subsection INSIDE `## Scheduled work`, the notes sit inside the span `cronRows` parses, a pipe-leading note line is read as a scheduled JOB, and since that set only grows **a phantom job made of note prose SATISFIES a coverage row whose real row is gone**: a job renamed out of `CI-06l`'s reach is RED at three findings and **the same rename plus one pipe-leading note line is GREEN at 33 of 33**. Promoting the notes to a peer `##` terminates the scan and the pair is RED at 32 of 33 in both halves. **SEED E EXISTS BECAUSE SEED D's PREDICTION WAS WRONG**, recorded rather than corrected. **THE CLASS TEST WAS RUN ON ALL FIFTEEN POINTERS BEFORE ONE WAS TOUCHED AND IT REFUSED FOUR: THE `:35` SITES WERE BORN ONE LINE SHORT ON 2026-08-28** at `a21bfb8e` and `4b3e8e57`, when the digest row stood at 36; only [P7](../plans/P7-risk-and-abuse.md) line 407, written a day earlier at `acec6666`, was ever true. **THEY WERE NEVER IN [ADR-388](../decisions/ADR-388.md)'s 42**, whose census ran through a reader that entry had itself measured as blind to thirteen of these fifteen, **so the corpus's figure for born-wrong pointers is a floor with four more underneath it**. **[ADR-388](../decisions/ADR-388.md) IS RIGHT ABOUT `ADR-352:30` AND [ADR-384](../decisions/ADR-384.md) IS REFUTED**, derived independently at four commits: `key: 'event-sink-caller'` sits at **822 at `3303e58c`** (the one and only commit that has ever touched that file), then 852, 875 and **902 on `3b664fcf`**, so **the dispatch's own `:875` was exact on the previous row's base and is 27 lines short of this one**, the same error a third time in one wave. **The repair is anchored to `3b664fcf` in the sentence itself** because `packages/tooling/**` is row `390`'s. **A METHOD FINDING: THIS CONTAINER ARRIVED WITH NO CLONE AT ALL, AND ON THE SHALLOW ONE TAKEN TO RECOVER, `git blame -w` NAMED A MERGE COMMIT AND RETURNED G2 FALSE FOR [session 342](2026-08-28-session-342.md); ON FULL HISTORY IT NAMES `d597b383` AND THE SAME TEST IS EXACT.** A shallow clone does not refuse a G2 question, it answers it wrongly, in the direction of falsifying a true pointer. **[ADR-388](../decisions/ADR-388.md)'s "SEVEN" IS EIGHT AND ITS "THREE IN ADR-312" IS FOUR**: line 193 carries the token twice. All six commit hashes it named reproduce exactly, [ADR-337](../decisions/ADR-337.md) line 70's CLASS TWO verdict included, which is re-derived and left alone. **SEVEN REPAIRS LANDED**: [ADR-352](../decisions/ADR-352.md) line 30 and six on [`ALLOCATION.md`](../decisions/ALLOCATION.md). **NO INVARIANT MINTED, NO GATE ADDED, WIDENED OR WEAKENED, NO TEST SKIPPED, DISABLED OR QUARANTINED, NO REGISTER GROWN, NO FILE OWNED BY ROW `390` OR `391` WRITTEN.** Counts derived at reporting time: **33 of 33** gates after `generate`, **35 of 35** invariants, **336 files / 8,107 passed / 10 skipped / 0 failed** against a base reproduced on `3b664fcf` before the first edit of **336 / 8,107 / 10 / 0**, delta zero on every axis, typecheck 0, lint 0, `format:check` clean. **`pnpm run verify` was NOT run and `falsify.mjs` was NOT run.** -->

---

### 1. The objective

Row `389`: take [ADR-388](../decisions/ADR-388.md) section 10's option (d) on [`CRON_INVENTORY.md`](../ops/runbooks/CRON_INVENTORY.md)'s scheduled table, or refute it; repair [ADR-352](../decisions/ADR-352.md) line 30 and derive whether [ADR-384](../decisions/ADR-384.md) or [ADR-388](../decisions/ADR-388.md) is right about it; and repair the class-one pointers into [`ALLOCATION.md`](../decisions/ALLOCATION.md) that section 13 derived and could not reach.

**One objective.** It resolved into a refusal with the restructure built and measured behind it, seven repairs, five seeds, one hazard found by a seed whose predecessor's prediction was wrong, and seven premises that do not reproduce.

---

### 2. Done

- **[ADR-389](../decisions/ADR-389.md)**, the entry, `proposed` and UNSIGNED. **Option (d) is ruled right about the page and refused as not takeable by this fence**, with the eleven failing assertions enumerated and the corrected four-fence specification written out.
- **The restructure built, run through every gate, invariant and the full suite, seeded five times, and reverted byte-identical.** `git diff 3b664fcf -- docs/ops/runbooks` is empty.
- **Seven pointer repairs.** [ADR-352](../decisions/ADR-352.md) line 30 (`:822` to `:902`, anchored to `3b664fcf`), [ADR-312](../decisions/ADR-312.md) four times, [ADR-275](../decisions/ADR-275.md), [session 466](2026-08-30-session-466.md).
- **Row `389` written into [ALLOCATION](../decisions/ALLOCATION.md), with `390` and `391` RESERVED naming no subject, no branch and no entry.**
- This log, the [STATE](../STATE.md) section, and the generated registry spans.

---

### 3. Next

**Option (d) needs one four-fence dispatch and everything it needs is now derived.** `docs/ops/runbooks/**` plus `apps/worker/**`, `apps/api/**`, `packages/db/**` and `packages/tooling/test/**`. The transform is mechanical and was run; the eleven assertions in [ADR-389](../decisions/ADR-389.md) section 2 are the work. Three things it must carry: the notes are a PEER `##` section and never a `###` inside `## Scheduled work` (seed E), the `RI-35` anchor sentence stays on one line (seed C), and the four class-two `:35` pointers must not be repaired.

Then: [P7](../plans/P7-risk-and-abuse.md) line 407 and [session 342](2026-08-28-session-342.md) line 9, both class one and both outside this fence; and whether the corpus's 42 is republished as a floor now that four more sit under it.

---

### 4. Blockers

**Option (d) is blocked on a fence rather than on a measurement, and the block is recorded rather than worked around.** Every figure a dispatcher needs is in [ADR-389](../decisions/ADR-389.md).

---

### 5. Landmines

- **A SHALLOW CLONE ANSWERS A G2 QUESTION WRONGLY RATHER THAN REFUSING IT.** `git blame -w` falls back to the nearest merge commit when the real parent is outside the fetch depth, and the verdict then falsifies a true pointer. Any row deriving [ADR-388](../decisions/ADR-388.md)'s honesty gate must check `git rev-parse --is-shallow-repository` first. **This session began with no clone at all**, and the first shallow recovery produced exactly this error before it was caught.
- **THE SEVERITY NOTES, IF (d) IS EVER TAKEN, MUST BE A PEER `##` SECTION.** As a `###` under `## Scheduled work` they sit inside the span `CI-06l` parses, and a pipe-leading line there becomes a phantom scheduled job that can mask a real coverage defect. Seed E watched it green then red.
- **NOTHING WOULD BIND A SEVERITY NOTE TO ITS JOB.** Seed A was green on the restructured tree: an emptied note is invisible to every check here.
- **`:472` AND `:644` HOLD AT MERGE ONLY IF ROWS `390` AND `391` OVERWRITE THEIR RESERVED CELLS IN PLACE**, which is what this branch's integration commits do. Every one of the six sites names or quotes its subject, so a sibling insertion degrades the coordinate and not the recoverability.
- **`CRON_INVENTORY.md` IS READ BY SIXTEEN TEST FILES IN TWO OTHER FENCES.** Any future edit to its scheduled table, not only a restructure, should run the suite before it is believed. Gates and invariants are all green across a change that breaks eleven assertions.

---

### 6. Files touched

`docs/decisions/ADR-389.md`, `docs/decisions/ADR-352.md`, `docs/decisions/ADR-312.md`, `docs/decisions/ADR-275.md`, `docs/sessions/2026-08-30-session-466.md`, `docs/decisions/ALLOCATION.md`, `docs/STATE.md`, this log, and the generated registry spans in `docs/decisions/README.md` and `docs/sessions/README.md`. **`docs/ops/runbooks/CRON_INVENTORY.md` was restructured and reverted byte-identical; it carries no change.**
