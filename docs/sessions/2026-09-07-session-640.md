---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 640: the property established over the statements themselves, TRUE of every INSERT and FALSE of every UPDATE, and a registry red this row was forbidden to clear

<!--index: **THE ROW IS [ADR-445](../decisions/ADR-445.md), `proposed` and UNSIGNED. NON-MONEY DIFF, MONEY-ADJACENT SUBJECT, `E2` READ OWED.** **THE ARTIFACT IS BUILT AND IT IS NOT A GREP**: [`generated-column-writes.mjs`](../../packages/tooling/checks/generated-column-writes.mjs) folds the generated-column set out of `packages/db/migrations/` on every run, then calls the accessor through a driverless Drizzle handle and reads the column list off the statement it sends. **116 of 116 registry keys build an INSERT and none names a member of the set, structurally**, because Drizzle takes an insert's column list from the TABLE. **5 built UPDATEs DO name one**, because `.set()` maps over the CALLER's keys and `WriteValues` is `Readonly<Record<string, unknown>>`; four of the five are on a table `merit_app` may update. **THE COMMISSION'S PREMISE DID NOT SURVIVE READING**: the dispatch rests the insert half on `$inferInsert` and the accessor does not use `$inferInsert` anywhere on the write path. **THE SET IS SIX AND `schema.ts` DECLARES FIVE**: `live_account_state.intraday_movement_cents` is generated in the DDL and its table is not in `schema.ts`. **`CI-06f` IS RED**: `441` to `444` are neither present nor reserved, and this row was told to claim `445` and no other cell. -->

### 1. The objective

`ADR-443` section 11 item 3 as the dispatch states it: **nothing in this tree asserts that no statement writes a `GENERATED ALWAYS AS` column.** Establish the property, or establish that it cannot be established this way. Derive the generated-column set from the DDL and hardcode nothing.

### 2. Done

- **The container measured and reported.** No repository on arrival; `add_repo` with `access: "push"` then `git clone --depth 1`. `node v22.22.2`, `pnpm 10.33.0`, 4 cores, 15 GiB.
- **The base reproduced at `89b02405`** on a clean tree before the first edit and **it is green**: suite **342 files / 8,429 passed / 11 skipped / 0 failed**, gates **33 of 33**, invariants **35 of 35**.
- **The population of write statements DERIVED rather than assumed.** 75 `.insert(`/`.update(` sites outside tests, of which 7 are in [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) and every other is a method call on an accessor handle. **Exactly one hand-written SQL statement exists in shipped source**, [`LIVE_CACHE_UPSERT_SQL`](../../apps/worker/src/live/ports.ts), and it targets the one generated column `schema.ts` does not carry.
- **The artifact BUILT, and it builds the statements rather than reading the source that makes them.** Three legs, each exact over a derived population: the DDL fold with an audit against its own input, the accessor's INSERTs through a driverless Drizzle handle, and the hand-written literals. [ADR-435](../decisions/ADR-435.md)'s move one layer down.
- **Every leg watched failing.** [`generated-column-writes.test.ts`](../../packages/tooling/test/generated-column-writes.test.ts), 20 cases. Legs A and C on a synthetic tree; **leg B falsified on the REAL accessor and the REAL registry** by handing it a set that names a column the DDL does not generate.
- **The UPDATE hole demonstrated on a real built statement** and held open by a test that goes red when the repair lands, rather than by a sentence.
- **The commission's premise corrected.** `$inferInsert` appears nowhere on the write path; `WriteValues` is `Readonly<Record<string, unknown>>` at [`scoped-db.ts:507`](../../packages/db/src/scoped-db.ts), and `updateAt` types the ADDRESS against the table's columns and the VALUES against nothing.

### 3. Not done, and why

- **The UPDATE half is NOT asserted by any leg.** A leg asserting it would be RED ON LANDING, and the only two ways to make it green are narrowing the population until the red disappears, which is the check [ADR-429](../decisions/ADR-429.md) section 10 forbids, or repairing `updateStatementOn`, which is in `packages/db/src/` and outside this fence. Priced in ADR-445 section 8 item 1.
- **Nothing under `packages/db` was touched.** Not `schema.ts`, not `scope.ts`, not `scoped-db.ts`, not one test.
- **`.github/workflows/ci.yml` was NOT edited.** 28 distinct cited line numbers, 20 of them at line 237 or further down, and the constraint on a widely cited file is that no cited line moves. The wiring is the suite, which is `CI-02`. ADR-445 section 8 item 4.
- **Identity columns are counted and not asserted on.** Drizzle emits one into an INSERT column list when a caller names it, so a leg over them would be red on landing over a defect the commission did not name. ADR-445 section 8 item 2.
- **`insertUnderStatement` and `pairInsertStatement` are not separately exercised.** Coverage is complete at the TABLE level and not at the BUILDER level, and the difference is an argument from reading. ADR-445 section 8 item 3.
- **`pnpm run verify` and `scripts/corpus/falsify.mjs` were NOT run.** Both mutate the working tree.

### 4. The registry red, reported rather than worked around

**`CI-06f` FAILS ON THIS BRANCH WITH FOUR FINDINGS**: `ADR-441`, `ADR-442`, `ADR-443` and `ADR-444` are each *"neither present nor reserved (a hole)"*. [`gates.mjs:863`](../../scripts/corpus/gates.mjs) asserts gaplessness from 1 to `max(present, allocated)`, and this row was told to claim `445` **and no other cell**, not to write reservations for rows it does not own, and not to invent numbers.

**32 of 33 gates pass and the gate is not weakened.** [ALLOCATION](../decisions/ALLOCATION.md) already carries the record of the last time this happened, to `053`, `054` and `055`: numbers assigned to parallel sessions and reserved nowhere, so gaplessness *"was broken at every intermediate state and the first of the three to reach a commit paid for all of it"*. **The remedy is a reservation and it belongs to whoever dispatches the wave.**

### 5. Every leg, after the last edit

| Leg | Result | Delta against the base |
| --- | --- | --- |
| `pnpm vitest run` | **343 files, 8,449 passed, 11 skipped, 8,460 total, 0 failed** | **+1 file, +20 passed, +0 skipped, +0 failed** |
| `node packages/tooling/checks/repo-invariants.mjs` | **35 of 35** | unchanged |
| `node scripts/corpus/gates.mjs generate` | one span rewritten, `adr_count` 423 to 424 in [`STATE.md`](../STATE.md) | a derived count, not an edit |
| `node scripts/corpus/gates.mjs check` | **32 of 33.** `CI-06f` red, section 4 | **the four holes this row was forbidden to fill** |
| `pnpm run typecheck` | exit 0 | unchanged |
| `pnpm run lint` | exit 0 | unchanged |
| `pnpm run format:check` | clean | unchanged |
| `node packages/tooling/checks/generated-column-writes.mjs` | **PASS.** 6 stored generated columns on 5 tables from 75 migrations, beside 20 identity columns; 116 built INSERTs name none; 12 hand-written statements over 405 shipped files mention none | new |

### 6. Handoff

- **[ADR-445](../decisions/ADR-445.md) section 8 is seven owed items, each with a `file:line` and a price.** Item 1 is the one that matters: a guard beside `refuseTenancyColumn` at [`scoped-db.ts:473`](../../packages/db/src/scoped-db.ts), refusing a values key whose column is generated. `delta_cents` is a money column on a table `merit_app` may update and nothing stands between a caller naming it and a `42601`.
- **Row `447` is the integration point and the check is ready for it.** When `live_account_state` reaches `schema.ts`, a transcription that omits `.generatedAlwaysAs(...)` makes leg B red on the built INSERT. The set is derived, so nothing has to be edited for that to work.
- **`ADR-443` is not in this tree.** Every claim this row makes about the commission is a claim about the dispatch text, which is quoted, and about the tree, which is derived.
