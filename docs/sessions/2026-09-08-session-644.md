---
status: approved
depends_on: [README.md]
last_updated: 2026-09-08
---

## 2026-09-08 - Session 644: the header census is asserted rather than corrected a fourth time, the header edit is line-neutral because a non-neutral one is not satisfiable inside the fence, and the narrow generation comparison is kept because the general one cannot reach its table

<!--index: **THE ROW IS [ADR-449](../decisions/ADR-449.md), `proposed` and UNSIGNED. NON-MONEY DIFF IN A MONEY-PATH FILE, `E2` READ OWED.** **TWO COMMISSIONED ITEMS TAKEN AND THE THIRD REFUSED AT SOURCE.** [ADR-447](../decisions/ADR-447.md) section 9 items 1 and 2: the `schema.ts` header's totality clause was false and its denominator stale a third time, and the durable repair is **five cases that READ the header text and recompute every figure it states**, at [`scoped-db.test.ts:4662`](../../packages/db/test/scoped-db.test.ts). **THE HEADER TEXT IS CORRECTED BECAUSE THE ASSERTION FORCES IT**: asserting a stale figure would be writing a case that asserts the wrong answer. **THE CENSUS DERIVED**: **117** relations declared in `schema.ts` against **116** keys, **115** registered tables of **118** `CREATE TABLE`, plus one view; **48** `firm`, **3** `pair`, **65** served by `scopedDb`; the three tables no accessor reaches are `identity_merges`, `identity_signal_weights` and `live_account_state`, NAMED rather than counted. **THE HEADER EDIT IS LINE-NEUTRAL AND THAT IS FORCED RATHER THAN CHOSEN**: 70 distinct lines of `schema.ts` are cited over 64 files, 8 inside the header, and **three of the eight live citers are under `apps/`, which this fence does not hold**, so a moved line would owe a repair this row may not make. **20 header lines replaced by 20; `schema.ts` lines 115 to 5358 byte-identical; `scoped-db.test.ts` lines 1 to 4638 differ at exactly two lines, both formerly blank. 0 of 70 and 0 of 44 cited lines moved.** **THE DELETION IS REFUSED AND THE FILE CONTAINED THE PROOF**: the general comparison iterates `DDL_NAMES`, built from `TABLE_KEYS`, and `ADR-443`'s own case at [`scoped-db.test.ts:3825`](../../packages/db/test/scoped-db.test.ts) asserts `live_account_state` is NOT in `ALL_REGISTERED`, so the general one cannot reach the narrow one's table and deleting it would leave a **MONEY** column's generation expression compared by nothing. `ADR-421` section 9's admission rule refuses it rather than commanding it. **The dispatch's line numbers are also wrong about this base**: `:4103` is an accessor-refusal case and `:3217` is the DEFAULT loop. **[ADR-444](../decisions/ADR-444.md) section 14 item 1 CLOSED**: the reciprocal back-pointer at [`:2725`](../../packages/db/test/scoped-db.test.ts) and [`:3214`](../../packages/db/test/scoped-db.test.ts), on a DIFFERENT greppable form so it does not pollute the 10 citing sites `ADR-444` section 5.3's grep counts. **TEN SEEDS, ALL FIVE CASES WATCHED RED**, each reverted and verified byte-identical by `git hash-object`; seed 9 rewords the sentence away and reddens two, seed 10 moves the TREE and not the header. **ONE SEED REFUSED**: the denominator would need a migration edited and migrations are sacred. **TWO FIGURES DERIVED STALE AND NAMED RATHER THAN REPAIRED**: `schema.ts:81` reads "ELEVEN of the 109" where the derived answer is **FIFTEEN of 116**, and **`ADR-447`'s eight pointers into `scoped-db.test.ts` are all off by exactly 460** because of the wave merge rather than any row's edit, seven of the eight landing on live code where `RI-15` cannot see them. **NO MIGRATION, NO `scope.ts`, NO `scoped-db.ts`, NO `packages/tooling/`, NO `apps/` FILE, NO GATE WEAKENED, NO TEST SKIPPED OR DELETED, NO LANDED ADR EDITED.** Counts derived at reporting time: invariants **35 of 35**, suite base **344 files / 8,605 passed / 11 skipped / 0 failed** and branch **344 / 8,610 / 11 / 0**, **delta +5 passed and the refused deletion contributes 0**, the narrow block still carrying its 7 cases. Typecheck, lint and `format:check` clean. **THE CONTAINER ARRIVED WITH NO REPOSITORY** and with no `node_modules`. -->

### 1. The objective

Discharge [ADR-447](../decisions/ADR-447.md) section 9 items 1, 2 and 3 and [ADR-444](../decisions/ADR-444.md) section 14 item 1: make the `schema.ts` header's census self-checking rather than hand-maintained, fold the narrow generation comparison into the general one if the general one covers it, and give the canonical statement at [`scoped-db.ts:3466`](../../packages/db/src/scoped-db.ts) a reciprocal back-pointer in the suite.

### 2. Done

- **The container measured and reported.** No repository on arrival; `add_repo` with `access: "push"`, then one clone. `node_modules` absent and installed from the lockfile.
- **The base reproduced at `ea353e84`** on `origin/claude/wave-444s`, not `main`, before the first edit, **and it is green**: invariants **35 of 35**, suite **344 files / 8,605 passed / 11 skipped / 0 failed**, typecheck, lint and `format:check` clean. Neither `pnpm run verify` nor `falsify.mjs` was run.
- **The census derived twice**, once by a throwaway probe under `vitest` and once by the shipped cases, and **all four figure families the header names were stale**, not the two the dispatch named.
- **Five cases written that READ the header as text and recompute what it states**, including the one numeral the header spells in WORDS, which had gone stale in that spelling too and is now rendered rather than typed.
- **The header corrected line for line.** 20 lines replaced by 20; the boundary held at the first `import`, line 115.
- **The line-neutrality decided on a derived fact rather than on taste.** Three of the eight live citers of `schema.ts` are under `apps/`, outside this fence, so a non-neutral edit is not satisfiable here at all.
- **The deletion REFUSED at source**, with the proof taken from `ADR-443`'s own case in the general block rather than from any document.
- **The reciprocal back-pointer written at both sites**, each on a line that was blank, so the two `describe`s stay at 2726 and 3215 where `ADR-444` names them.
- **Ten seeds, every case watched RED, every seed reverted byte-identically** by `git hash-object` on both sides.
- **Two stale figures named rather than repaired**, with their derived values, because both are claims this fence did not investigate.

### 3. Next

- The fold paragraph's figures at [`schema.ts:81`](../../packages/db/src/schema.ts): **FIFTEEN of 116** and not eleven of 109, and the `ADR-094` argument re-checked against the four tables the sentence does not name ([ADR-449](../decisions/ADR-449.md) section 9 item 1).
- The `either` paragraph's *"seven others"* at [`schema.ts:41`](../../packages/db/src/schema.ts), still unbound, and the population question this row narrowed without evidence (section 9 item 4).
- A row that can register `live_account_state` or widen the registry loops, at which point the narrow comparison becomes deletable and not before (section 9 item 2).

### 4. Blockers

- **None for this row.** [ADR-449](../decisions/ADR-449.md) is `proposed` and UNSIGNED and the `E2` read is owed; no founder read was available during the session and none was required to finish.
- **`CI-06f` is red at `448`**, which is row `448`'s cell on this wave. Reported rather than repaired; no reservation was written for a row this session does not own.

### 5. Landmines

- **A HEADER COMMENT IS NOW LOAD-BEARING.** Five cases fail if anyone rewords nine specific sentences in the `schema.ts` header. That is deliberate and it is a tax; the alternative shape, a census in an exported constant that the header cites, is named in [ADR-449](../decisions/ADR-449.md) section 13 and is a bigger diff than this fence allowed.
- **A POINTER LANDING ON A DIFFERENT REAL LINE IS INVISIBLE TO `RI-15`.** `ADR-447`'s eight pointers into `scoped-db.test.ts` are off by exactly 460 on this base and the gate is green. **Check position, not just the gate**, and prefer citing a case TITLE over a line number when appending to a file a wave is also appending to.
- **A DISPATCH'S LINE NUMBERS AGE FASTER THAN ITS REASONING.** Two of the three sites this row was given were something else entirely by the time it read them, and the third premise, that the general comparison covers the narrow one, was false at source.

### 6. Files touched

- [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts) - header census corrected line-neutrally, `RI-14` record appended below the highest cited line.
- [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) - five cases appended at the foot, two back-pointers on lines that were blank.
- [`docs/decisions/ADR-449.md`](../decisions/ADR-449.md) - new.
- [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) - row `449` only.
- [`docs/decisions/README.md`](../decisions/README.md), [`docs/sessions/README.md`](README.md), [`docs/INDEX.md`](../INDEX.md), [`docs/STATE.md`](../STATE.md) - generated spans and registers, `generate` output only in `STATE.md`.
- This file.
