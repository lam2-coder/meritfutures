---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 296: the liability dashboard's rendering half, and float beside reserve rather than inside it

<!--index: **`P-M6-07` LANDS ON THE PAGE AND THE FLOAT LANDS BESIDE IT. No ADR, no migration, no `SD-nn`, no `packages/db` change, no contract change, no route, no gate, and `0049`, `admin-reads.ts`, `admin-payouts.ts` and `liability_snapshots` all untouched.** **THE PANEL'S STATED BLOCKER HAD GONE FALSE**: the `PENDING` row read *"the columns do not exist"*, [`0049`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql) landed them under [ADR-128](../decisions/ADR-128.md) and closed `OI-01`, and what is actually missing is a PRODUCER, which that file's own header states as a deliberate omission. **FLOAT ENTERS THE DENOMINATOR AS EXPOSURE AND NEVER THE NUMERATOR AS RESERVE**, and [`liability.ts`](../../apps/admin/src/liability.ts) holds no field, argument or local carrying `reserve + float` except the one inside the assertion that refuses it. **`rcr_bp` IS READ AND NOT RECOMPUTED**, on `0049`'s founder-read item 1; the recomputation survives only as a coherence check, in `bigint` reproducing Postgres' `(reserve_cents * 10000) / NULLIF(cvar99_cents, 0)` exactly, and it catches BOTH shapes of the defect. **THE FIXTURE FLIPS THE BREAKER RATHER THAN MOVING A NUMBER**: `GS-229`'s book is 8,000bp ARMED from reserve alone and 14,000bp NOT ARMED with the float folded in, which is `AS-M20-08` in two lines. **ASSERTED IN THE NEGATIVE OVER THE RENDERED BYTES**, and the SECOND SEED PROVED WHY THAT IS NOT THE SAME CHECK AS THE UNIT ONE: the ratio line computed from float plus reserve was **caught only in the bytes** and every unit case passed, because the leak was in a rendered line and not in any `Figure`. **Three seeds watched failing, each reverted from a byte-identical copy with the hash checked both ways.** **THE TWO SCAN SCOPES DIFFER DELIBERATELY**: forbidden CENTS in the `P-M6-07` panels only, because money can coincide; forbidden RATIO over the whole page, because a bp token collides with nothing. **Both skipped at zero float, and the skip is stated.** **THE API HALF WAS MEASURED BEFORE A ROUTE WAS WRITTEN AND NO ROUTE WAS WRITTEN**: `GET /admin/liability` serves `P-M6-07` IN FULL and **cannot serve `P-M6-01` at all**, because `LiabilityResponse` carries `open_liability_cents` and no `wallet_balances_cents`, which hands a client one component under a name that reads as the panel and is what `INV-M6-11` forbids; `bounded_near_term_cents` and `remaining_ladder_exposure_cents` are absent too. **`admin-liability.ts` DECLINED ON THAT MEASUREMENT**: the path is taken, `compose` refuses a duplicate `METHOD /path`, and a new path is a new `API_CONTRACT` row, which is an ADR `P5-c` holds and this session was allocated no number for. **The remedy is three fields on a frozen response shape and not a route.** **THREE MORE FINDINGS, EACH OUTSIDE THE FENCE**: none of the eight new exports reaches [`index.ts`](../../apps/admin/src/index.ts)'s explicit barrel, `GET /admin/wallet/reconciliation` is registered by no route module so `P-M6-07`'s SECOND ratio renders absent, and **"float" in `admin-reads.ts` means the NUMBER TYPE in all three of its occurrences**. **`ADMIN_ORIGIN` appears nowhere in this diff and neither does any host literal**, both greps pasted empty. 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, 183 files / 3,559 passed / 6 skipped, `falsify` clean. **`apps/admin` goes 116 to 152 cases, +36 and no new file**, which is this session's attributed contribution. -->

- **Objective:** [P5](../plans/P5-payouts-and-wallet.md) section 8's `P5-l`, RENDERING HALF. The liability dashboard gains `P-M6-07`'s reserve coverage panel and the wallet float panel, with **float and reserve rendered as visibly separate figures and the RCR computed from reserve alone** ([P5 section 5.3](../plans/P5-payouts-and-wallet.md), `INV-M20-08`, `AS-M20-08`, `DEP-M20-06`). The API half landed in [session 256](2026-08-27-session-256.md) under [ADR-144](../decisions/ADR-144.md) and this session checks what it serves rather than assuming it.
- **Done:** [`apps/admin/src/liability.ts`](../../apps/admin/src/liability.ts) gains `ReserveCoverageSnapshot`, `reserveCoverage`, `TreasurySource`, `TREASURY_SOURCES`, `requireTreasurySource`, `RCR_BREAKER_BP`, `formatRatioBp` and the `ReserveCoverage` shape; [`apps/admin/src/page.ts`](../../apps/admin/src/page.ts) renders `P-M6-07` as TWO panels, corrects the stale `PENDING` row, and gains `assertFloatIsNotReserve`; [`test/liability.test.ts`](../../apps/admin/test/liability.test.ts) and [`test/page.test.ts`](../../apps/admin/test/page.test.ts) gain four describe blocks each. [STATE](../STATE.md) appended, generated spans re-run. **Verified in pieces**, `pnpm install` first, `falsify.mjs` last and alone: 33 of 33 gates, 12 of 12 invariants, `typecheck` exit 0, `lint` exit 0, `format:check` clean, 183 test files / 3,559 passed / 6 skipped. The dispatch baseline (33, 12 of 12, 183 / 3,521 / 6) reproduced exactly before anything was edited.
- **Next:** **The panel has no producer and that is the next thing worth building.** `reserve_coverage_snapshots` has no writer at all, so on a real deployment `reserveCoverage` is called with nothing and the `PENDING` row prints. `DEP-M6-02` (a live rail balance, `SD-M5-03`) and `DEP-M6-05` (`CVaR99 at rho = 0.30`) are its two inputs, and `RESERVE-C1` means the writer has to name a `treasury_balances` row and copy its balance exactly. **`P-M6-01` cannot be served over the wire until `LiabilityResponse` carries `wallet_balances_cents`**, which is `P5-c`'s file and an ADR. **`P-M6-07`'s second ratio waits on `GET /admin/wallet/reconciliation`**, which is session 298's `admin-wallet.ts`.
- **Blockers:** none for this slice, and **one for the surface it renders**. `apps/admin` has no HTTP client and no wiring to `GET /admin/liability`; `buildLiabilityHome` takes every input as a parameter and reads nothing ambiently, deliberately, so *"the arithmetic reads a real row"* is met in the sense that the row's SHAPE is now the migration's rather than a fixture's, and NOT in the sense that a process fetches one. **The port that would fetch it, `AdminReadSource`, is wired by nothing in this tree** and [ADR-144](../decisions/ADR-144.md) section 5 states why: what is missing is a SHAPE and not an authority, and the adapter is owed to a slice that holds `packages/db`. That is unchanged by this session and outside this fence.
- **Landmines:** **`INV-M5-15` IS THE SENTENCE A READER MEETS FIRST AND IT DOES NOT SAY WHICH SIDE OF THE RATIO.** *"Wallet balances are included in Open Liability and in the reserve coverage ratio"* is true and reads as the numerator; `P-M6-07` and `INV-M20-08` settle it the other way. Anyone touching this panel reads [P5 section 5.3](../plans/P5-payouts-and-wallet.md) before the source. **THE FLOAT IS ALREADY INSIDE `cvar99_cents`** ([ADR-019](../decisions/ADR-019.md), `GS-130`), so a renderer that "corrects" the denominator by adding the float counts it twice, and one that adds it to the numerator cancels it out; the panel prints that sentence, and it is the half of section 5.3 easiest to lose. **`formatCents` WOULD PRINT 12,500 BASIS POINTS AS `125.00`**, which is why the ratio arrives as a note rather than as a `Figure` and why `figure.ts` was not widened for it. **`figure.ts`'s `ORIGIN_ID` CLOSES THE ROSTER AT M06's OWN PANEL LIST**, so the float has no origin of its own and carries `P-M6-07`: M20 supplies it TO that panel (`DEP-M20-06`), and minting an origin here would widen a roster this fence does not hold. **THE BYTE SCAN'S CENTS SEARCH IS SCOPED TO THE `P-M6-07` PANELS ON PURPOSE** -- widening it to the whole page would make an unrelated figure that happened to equal `reserve + float` throw the page, which is a control that gets deleted the first time it fires wrongly. **`apps/admin/src/page.ts` IS SHARED WITH SESSION 294** (`P5-n`, the event feed) and the file's hash was checked unchanged after the `origin/main` merge at `a7796b3`; whoever merges second re-reads it whole.
- **Files touched:** [`apps/admin/src/liability.ts`](../../apps/admin/src/liability.ts), [`apps/admin/src/page.ts`](../../apps/admin/src/page.ts), [`apps/admin/test/liability.test.ts`](../../apps/admin/test/liability.test.ts), [`apps/admin/test/page.test.ts`](../../apps/admin/test/page.test.ts), [STATE](../STATE.md), [sessions/README](README.md), the generated spans, this file. **`apps/admin/src/live-liability.ts` was inside the fence, read, and NOT changed**: section 3.5's live figure is built from `P-M6-01`'s authoritative base and has no reserve, no float and no ratio in it, so there was nothing in it for section 5.3 to rule on. **No `apps/api` file was touched**, and `apps/api/src/routes/admin-liability.ts` was declined rather than written.

---

### The plan, written here rather than in interactive plan mode

**Six steps, in order, each committed and pushed as it lands.**

1. **Reproduce the baseline before editing anything.** Done before this file was written: 33 of 33 gates, 12 of 12 invariants, `typecheck` exit 0, `lint` exit 0, `format:check` clean, 183 test files / 3,521 passed / 6 skipped.
2. **Check what the landed route serves, and say so.** `GET /admin/liability` is registered in [`admin-reads.ts`](../../apps/api/src/routes/admin-reads.ts) and its body is [API_CONTRACT](../architecture/API_CONTRACT.md) section 8's `LiabilityResponse`, which is `approved` and therefore an ADR to change. The finding goes in the pull-request body. **`apps/api/src/routes/admin-liability.ts` is created only if the landed route does not serve the panel AND a new path can be taken without editing a frozen contract**, and this session expects to decline it.
3. **[`apps/admin/src/liability.ts`](../../apps/admin/src/liability.ts) gains the reserve-coverage row and the float figure.** The row maps [`0049_reserve_coverage_snapshots.sql`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql)'s columns by meaning, on `LiabilitySnapshot`'s own precedent. **`rcr_bp` is READ rather than recomputed**, because `0049` generates it in the database and a figure the database computes cannot disagree with the numbers stored beside it; the recomputation exists only as an **assertion** that the row is coherent, and that assertion is what refuses a numerator with float folded into it.
4. **[`apps/admin/src/page.ts`](../../apps/admin/src/page.ts) renders two panels where one `PENDING` row stood.** `P-M6-07` reserve coverage, and the wallet float panel beside it. The stale `PENDING` entry (*"the columns do not exist"*) is corrected: they exist, and what is missing is a producer.
5. **Assert section 5.3 in the negative, over the rendered bytes.** A page-level scan on the `P-M6-07` lines, on `assertNamesNoSubject`'s precedent, plus unit assertions in `liability.ts`. **Seed both named defects, watch them fail, revert from a byte-identical copy, paste both.**
6. **Verify in pieces, `format:check` before the final commit, `falsify.mjs` last and alone.**

**What this session may not do.** No ADR number and no migration number is allocated to it. It does not repair [`liability_snapshots`](../../packages/db/migrations/0009_ledger.sql) ([session 240](2026-08-27-session-240.md)'s `OI-01` work owns it), does not touch `admin-reads.ts`, `admin-payouts.ts`, `packages/db` or `0049`, adds no `SqlExecutorReason` or `SystemReason` member, imports no `pg`, and casts past no key type. [`apps/admin/src/page.ts`](../../apps/admin/src/page.ts) is shared with session 294 (`P5-n`, the event feed) and is re-read whole after any merge.

---

### 1. What the landed route serves, measured rather than assumed

**The dispatch said to check first and say what was found, and the answer is BOTH halves of a yes and a no.**

```
$ grep -n "path: '/admin" apps/api/src/routes/admin-reads.ts
1160:    path: '/admin/liability',
1170:    path: '/admin/eligible-forecast',
1194:    path: '/admin/accounts',
1235:    path: '/admin/accounts/:accountId',
1247:    path: '/admin/identities/:identityId/graph',
1259:    path: '/admin/flags',
1310:    path: '/admin/evidence/:accountId',
```

| What the panel needs | On `LiabilityResponse` | Consequence |
|---|---|---|
| `reserve_cents`, `cvar99_cents`, `rcr_bp`, `breaker_armed` | **all four**, under `reserve` | **`P-M6-07` is served in full.** Nothing was owed here and nothing was built |
| `wallet_balances_cents` | **absent** | **`P-M6-01` cannot be rendered by a client of this endpoint.** It carries `open_liability_cents`, which is `0009`'s COLUMN and one component of the panel, under a name that reads as the panel. `INV-M6-11` forbids exactly that, and [`liability.ts`](../../apps/admin/src/liability.ts)'s header was written against this hazard before the endpoint existed |
| `bounded_near_term_cents` | **absent** | `P-M6-02` has no wire representation |
| `remaining_ladder_exposure_cents` | **absent** | `AS-M6-04` has no wire representation |
| the wallet float | **absent** | M20 puts it on `GET /admin/wallet/reconciliation`, which is registered by no route module |

**And the word means the other thing on that file:**

```
$ grep -n "float" apps/api/src/routes/admin-reads.ts
79:// "`*_cents` are JSON integers. `*_bp` are JSON integers. No floats, no
357:  'books, so a float here is a float on the books rather than a rounding in a client',
593:  * then `assertContractScalars` is what keeps a float out of the money inside it.
```

**Three occurrences and all three are the NUMBER TYPE.** Not one is the cash balance. That is worth recording on the one file that will eventually have to serve both senses, and it is why every sentence in this session's code that could be read either way says which it means.

**So `apps/api/src/routes/admin-liability.ts` was DECLINED.** `/admin/liability` is taken, `compose` refuses a duplicate `METHOD /path` across the whole module set, and a NEW path is a new [API_CONTRACT](../architecture/API_CONTRACT.md) row. That document is `approved`, [P5 section 8](../plans/P5-payouts-and-wallet.md) makes an edit to it an ADR rather than a commit, `P5-c` holds those rows, and **this session was allocated no ADR number and may not take one**. A second route serving the three missing fields under a different path would be the same widening with a longer URL. **The remedy is three fields on a frozen response shape, and it is reported here rather than reached for.**

### 2. The three seeds, watched failing and reverted from byte-identical copies

**Copies taken before the first edit, hashes checked both ways.**

```
$ sha256sum apps/admin/src/liability.ts apps/admin/src/page.ts
0e5ebbb4423cce6f396e8b6b4b4f95bef5499726120b1dac474b074f23bcbf38  apps/admin/src/liability.ts
602cc3b423ebb5693bb655cdeb0953ab9c263b432d9d0e4d7664ce95a1013c73  apps/admin/src/page.ts
```

**Seed 1: the float folded into the reserve figure** (`cents: coverage.reserveCents + floatCents`).

```
FAIL  apps/admin/test/page.test.ts
PageError: a reserve coverage line prints 35000.00, which is the reserve (20000.00) with the
wallet float (15000.00) folded into it: "Reserve, the RCR numerator: 35000.00 [...]". Wallet
balances are NEVER counted toward reserve (INV-M20-08), the float is segregated in reporting as
well as in fact, and this is the line AS-M20-08 is about

FAIL  apps/admin/test/liability.test.ts > M6-A-31 > reserve and float are two figures and neither is the other
AssertionError: expected 3500000n to be 2000000n
FAIL  apps/admin/test/liability.test.ts > M6-A-31 > no member of the panel holds reserve plus float
AssertionError: expected 3500000n not to be 3500000n

Test Files  2 failed | 5 passed (7)
     Tests  2 failed | 106 passed (108)
```

**Seed 2: the ratio line computed from float plus reserve**, and **this is the one that justifies the whole byte-level assertion.**

```
FAIL  apps/admin/test/page.test.ts
PageError: a rendered line prints 14000 bp, which is the coverage ratio computed from float PLUS
reserve rather than from reserve alone (8000 bp): "Reserve coverage ratio: 14000 bp (0.8000x)
[...] breaker: ARMED at 10000 bp [...]". The RCR is computed from RESERVE ALONE (P-M6-07,
INV-M20-08, GS-229). A ratio that flatters itself with the same money on both sides is the
breaker at 1.0 becoming fictional, on the one screen an operator opens during an incident

Test Files  1 failed | 6 passed (7)
     Tests  108 passed (108)
```

**Read the second line of that count.** `Tests 108 passed (108)`: **every unit case passed.** The coherence check in `liability.ts` never fired, because the row it was handed was coherent; `ratioBp` was still 8,000 and every `Figure` still held the right cents. The defect existed **only in the rendered string**, which is precisely session 261's finding and precisely why `P6` section 8 restates it. An arithmetic guard on the input cannot see a renderer that does the sum on its way to a line.

**It also shows the second half of the same failure**, which is the one an operator would actually be misled by: the line printed `14000 bp` and `breaker: ARMED` **in the same sentence**, because the arming came from the true ratio and the number came from the false one. A page that says ARMED beside a figure of 1.4x is a page whose reader concludes the breaker is broken.

**Seed 3: the float figure dropped from its panel** (`[coverage.floatCoverage]` alone).

```
FAIL  apps/admin/test/page.test.ts
PageError: the reserve coverage panels do not print the reserve and the wallet float as two
separately labelled figures. DEP-M20-06 requires M6 to render float and reserve as VISIBLY
SEPARATE figures, and a panel that prints one of them is the panel AS-M20-08 describes with a
step already taken

Test Files  1 failed | 6 passed (7)
     Tests  108 passed (108)
```

**Reverted, and the tree proved identical:**

```
$ sha256sum apps/admin/src/page.ts apps/admin/src/liability.ts
602cc3b423ebb5693bb655cdeb0953ab9c263b432d9d0e4d7664ce95a1013c73  apps/admin/src/page.ts
0e5ebbb4423cce6f396e8b6b4b4f95bef5499726120b1dac474b074f23bcbf38  apps/admin/src/liability.ts
$ git status --short
$ pnpm vitest run apps/admin
Test Files  7 passed (7)
     Tests  152 passed (152)
```

### 3. `ADMIN_ORIGIN`, grepped and pasted empty

**[ADR-012](../decisions/ADR-012.md): the real admin domain never enters the corpus, the repository, or any public artifact.** This session works on the admin surface, so the diff is swept rather than remembered, and the result is pasted **even though it is empty**, because an unrun grep and an empty one look the same in a session log.

```
$ git diff origin/main...HEAD -- . | grep '^+' | grep -nEi 'https?://|\.com|\.io|\.net|\.org|\.dev|\.app|[a-z0-9-]+\.[a-z]{2,}/'
$ echo $?
1

$ git diff origin/main...HEAD -- . | grep -n 'ADMIN_ORIGIN'
$ echo $?
1

$ git diff origin/main...HEAD -- . | grep '^+' | grep -nE "'https?"
$ echo $?
1
```

**`ADMIN_ORIGIN` does not appear in this diff at all**, and no host literal was added. The one origin-shaped string in the files this session touched is `https://ops.example.invalid` in `page.test.ts`'s pre-existing fixture, which this session did not write and did not change.

### 4. What the count is, honestly

**`apps/admin` goes from 7 test files / 116 cases to 7 files / 152 cases: +36 cases and NO NEW FILE**, because both suites were appended to rather than joined by a third. That is this session's attributed contribution and it does not move.

**The whole-tree figure is mostly not this session's.** The dispatch baseline reproduced exactly before anything was edited (183 files / 3,521 passed / 6 skipped); `origin/main` was merged in once, at `a7796b3`, bringing session 291's own corrections, and the head figure is 183 / 3,559 / 6. **The +38 is +36 here and +2 from that merge**, and [`apps/admin/src/page.ts`](../../apps/admin/src/page.ts)'s hash was checked unchanged across it, which is the collision session 294 was warned about not yet arriving.

**One thing this session did NOT do that its row could be read as promising.** [P5](../plans/P5-payouts-and-wallet.md) section 8 says `apps/admin`'s arithmetic *"reads a real row"*. It now reads the **real row's SHAPE** -- `reserve_coverage_snapshots`' columns as `0049` created them, mapped by meaning, with the generated ratio read rather than recomputed -- and no process in this tree fetches one, because `apps/admin` has no HTTP client and `AdminReadSource` is wired by nothing. Saying that plainly is worth more than a claim the next session would have to discover was half true.
