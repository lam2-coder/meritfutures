---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 629: a live comment on the money path's own barrel stated two false things about a check, one of them never true, and no leg of that check could ever have said so

<!--index: **THE ROW IS [ADR-434](../decisions/ADR-434.md), `proposed` and UNSIGNED. NON-MONEY DIFF IN THE MONEY PATH'S OWN PACKAGE, `E2` READ OWED. NO GATE ADDED, WIDENED OR WEAKENED, NO LEG ADDED, NO NEEDLE REGISTERED, NO CENSUS EDITED, NO ARTIFACT ADDED OR REMOVED, NO MARKER ADDED TO THE ABSENCE VOCABULARY, NO `RI-nn` MINTED OR RESERVED, NO `CI-06` LETTER SPENT, NO MIGRATION, NO WORKFLOW, NO TEST FILE ADDED OR CHANGED, NO TEST SKIPPED, DISABLED OR QUARANTINED, NOT ONE BYTE UNDER `packages/tooling/**`, `apps/**`, `packages/db/**` OR `scripts/**`.** **THE ONE SOURCE FILE THIS ROW EDITS IS THE MONEY PATH'S OWN BARREL AND THE WHOLE DIFF ON IT IS COMMENT TEXT**: 55 insertions, 27 deletions, every added and every removed line beginning `//`, no export moved, no binding added or dropped, no monetary value read or written. **BOTH STALE SENTENCES ARE CONFIRMED AT SOURCE AND NEITHER WAS TAKEN ON [ADR-431](../decisions/ADR-431.md)'s REPORT.** [`packages/ledger/src/index.ts`](../../packages/ledger/src/index.ts) lines 148 to 174 said **THE PROBE CANNOT TELL A PUBLICATION FROM AN INSTALL** and **WHAT HOLDS THE GREEN IS THIS LINE BREAK**; seven spellings of the same publication were driven through the PRE-REPAIR check, taken from `git show c6e500dd^`, and the shipped one side by side over a throwaway `git worktree`. **ALL SEVEN READ `absent` ON THE SHIPPED PROBE**, so the first sentence is false; **the ONE-LINE two-name form with the writer FIRST fires on the pre-repair check with no reflow anywhere**, so the second was never true, and **three seeded installs read `present` on both sides**, so the probe's power over a real install is untouched. The false sentences stood **53 commits** and **16 commits** respectively behind this row's base. **THE REPAIR KEEPS WHAT IT CORRECTS UNDER `RI-14`**: both sentences are quoted verbatim as an indented block under `IT READ`, followed by *THE FIRST OF THOSE IS FALSE SINCE ADR-431. THE SECOND WAS NEVER TRUE*, and the request the comment used to make of the next reader is explicitly WITHDRAWN. **IT IS A LIVE FILE AND NOT A DATED RECORD**, settled against [ADR-386:169](../decisions/ADR-386.md), which puts the two on opposite sides of this exact question and is in any case about POINTERS rather than about claims. **NO CITED LINE MOVED**: the only two `path:NNN` pointers into this file, at `:72` and `:77`, are above the first line touched, and the three dated pointers below it are NAMED and not repaired. **"NO GATE REPORTS THIS" IS TRUE AND [ADR-431](../decisions/ADR-431.md)'s REASON FOR IT IS WRONG.** The file is not *outside every leg by construction*: **two legs open it on every run.** Leg 6 sweeps it and the barrel ALREADY CARRIES TWO ABSENCE-VOCABULARY LINES, one of them inside the stale sentence itself; what holds the silence is that the register's **7 needles**, from three artifacts, none reach the window, and `event-sink-caller` registers `needles: []` by a reasoned decision. **SEEDED AND WATCHED FIRING**: a single `// runReplayAudit` line above the marker turns leg 6 RED at `packages/ledger/src/index.ts:155`, the stale sentence's own line. Leg 7 reads the file by name through the census and **its number moved because of this row's own diff**, 3 lines to 4 and a total of 28 to 29, and it said nothing, because what it derives is a COUNT. Legs 1 to 5 need a registered claim SITE and the register holds **33 claims over 17 sites**, none of them this file. `RI-14` is scoped to **three files** under `apps/api` and its own miss (1) says it *"reads EXPORTS and never behaviour"*. **THE HOLE IS PRICED AND NOT CLOSED**: the cheapest control that reaches this paragraph is a needle on the two names PLUS `SWEEP_WINDOW` 2 to 3, costing **7 new register entries** on the base tree, of which 3 are bought by the window alone against needles that already ship. **AND THE MEASUREMENT IS THE ARGUMENT AGAINST BOLTING IT ON**: leg 6 forces REGISTRATION AND NOT TRUTH, so the control would have named this sentence and never reddened at it; its reach is a **3-line** distance inside a comment that is about 20 lines on the repaired paragraph, so the same control now misses it; and the file it lands in is row 433's this wave. **THREE FINDINGS AGAINST LIVE AND DATED SENTENCES, NONE REPAIRED HERE**: [ADR-431:84](../decisions/ADR-431.md) and the LIVE `sweptBy` string at [`absence-claims.mjs:1204`](../../packages/tooling/checks/absence-claims.mjs) both say a list ending in the writer *"stayed green however it was wrapped"*, which is false under `trailingComma: "all"` and is contradicted by ADR-431's own section 4.2; and that same `sweptBy`'s argument against a needle predicts a body of noise where the derived cost at the shipped window is **2 registrations**, because it counts the lines the NAMES reach and leg 6 additionally requires an absence marker. Counts derived at reporting time: **32 of 33** gates after `generate`, **35 of 35** invariants with **`RI-35` PASS at 0 findings**, suite **base 8272, mine 8272, delta 0** over **341 files / 11 skipped / 0 failed**, typecheck exit 0, lint exit 0, `format:check` clean. **THE ONE RED GATE IS `CI-06f` AT 432 AND 433 AND IS REPORTED RATHER THAN REPAIRED**, on [ADR-428](../decisions/ADR-428.md)'s and [ADR-431](../decisions/ADR-431.md)'s shared precedent, since closing it means inventing RESERVED rows for two concurrent sessions, which [`ALLOCATION.md`](../decisions/ALLOCATION.md) row `423` forbids in its own words. **No gate was weakened, silenced or narrowed. THE REPAIR REQUIRED NO REGISTER EDIT AT ALL**, which the dispatch asked to hear either way. **`pnpm run verify` was NOT run and `scripts/corpus/falsify.mjs` was NOT run**, both forbidden by the row. **THE CONTAINER ARRIVED WITH NO REPOSITORY** and `git push` was refused **403** by the proxy until `add_repo` was called with `access: "push"`. -->

### 1. The objective

Repair a live sentence on the money path's own barrel that states something false about `RI-35`'s `event-sink-caller` probe, **after confirming the staleness at source rather than taking [ADR-431](../decisions/ADR-431.md)'s account of it**; keep the corrected sentences beside their correction under `RI-14`; **derive rather than repeat** the claim that no gate reports it; and **price the hole without closing it**, because a control bolted onto a repair gets neither a counterfactual nor a subject.

### 2. Done

- **The container measured and reported.** No repository on arrival, cloned with no depth flag, `pnpm install --frozen-lockfile` 8s, exit 0. **`git push` refused `403`** until `add_repo` was called with `access: "push"`.
- **The base reproduced at `9eb52656`** on a clean tree before the first edit: `generate` reported *every span already matches its query*, gates **33 of 33**, invariants **35 of 35**, suite **341 files / 8272 passed / 11 skipped / 0 failed**, `format:check` clean.
- **The staleness confirmed at source, both halves, against the pre-repair check.** Seven publication spellings and three installs through both artifact registers over a throwaway `git worktree`, the working tree never seeded, the worktree clean after every case.
- **The comment repaired with the correction kept beside what it corrects.** Comment-only diff on `packages/ledger/src/index.ts`, verified line by line.
- **`ADR-386`'s live-versus-dated distinction checked at source before being relied on**, and the three dated pointers into the rewritten paragraph NAMED rather than repaired.
- **The "no gate reaches this" claim derived leg by leg and one leg of ADR-431's reasoning refuted**, with a leg 6 seed watched RED at the stale sentence's own line and leg 7's census number watched moving under this row's diff without a word.
- **The hole priced at four configurations of a candidate control**, with the reason the cheapest one is still not worth bolting on here.
- **The registries landed and `generate` run.** One file outside the named fence was written, by `generate` and not by hand: [`docs/STATE.md`](../STATE.md)'s `adr_count` span moved **414** to **415**, one integer, and `CI-06g` is red without it.
- **ADR-434 written**, `proposed`, UNSIGNED, `E2` READ OWED.

### 3. Not done, and why

- **The hole is not closed.** [ADR-434](../decisions/ADR-434.md) section 6. The measured control forces registration and not truth, its reach is a line distance inside a comment, and the file it lands in is row 433's this wave. It deserves its own row with its own counterfactual.
- **No register, check file or test file was edited**, and **the repair did not require one**: `RI-35` reports 0 findings and 35 of 35 invariants hold with no register edit of any kind.
- **The two overstatements in [ADR-434](../decisions/ADR-434.md) section 8 are not repaired.** One is in a dated record and is NAMED under [ADR-386:169](../decisions/ADR-386.md); the other is in `absence-claims.mjs`, which row 433 holds this wave.
- **`CI-06f` is red at 432 and 433 and is reported rather than repaired**, on the precedent two rows have now taken. No gate weakened, silenced or narrowed.
- **`pnpm run verify` and `scripts/corpus/falsify.mjs` were NOT run**, both forbidden by the row.

### 4. Owed

- **A founder read on [ADR-434](../decisions/ADR-434.md) section 10**, three decisions, of which the first is whether prose in the money path's own package is inside the `E2` read: today it is outside, and a sentence telling the next reader how to edit the writer's publication was wrong for 53 commits with every gate green.
- **The priced control**, if a founder wants it: 7 register entries, a global window widening, and a row of its own.
