## 2026-08-16 - Session 42: `OQ-P2-02`'s migration, the calendar watermark on `rule_states` (MONEY PATH)

- **Done:** **[`0035_rule_states_calendar_revision.sql`](../../packages/db/migrations/0035_rule_states_calendar_revision.sql)**, [ADR-047](../decisions/ADR-047.md)'s migration and the other half of `0033`. `0033` made the prior image mandatory on the calendar side; nothing joined that record to a state row, so **replay held the evidence and could not scope by it**. `rule_states.calendar_revision_id` references `trading_calendar_revisions(id)` `ON DELETE RESTRICT`, and it is **excluded from `state_hash`**, which is the ruling. [`probe_rule_states_calendar_revision.sql`](../../scripts/db/probe_rule_states_calendar_revision.sql) is **10 / 10, six successes before the first rejection**, wired into [`corpus.yml`](../../.github/workflows/corpus.yml) and **pinned in `CI-06h` in the same commit**. **Nine counterfactuals watched failing on their own findings.** The set applies forward-only from empty against PostgreSQL 16, re-apply rejected, **all seven probes and the NO-FLOATS assertion pass on the `0035` schema**, 15 of 15 gates. Records in [DELTA_MANIFEST section 19](../../packages/db/DELTA_MANIFEST.md), [ADR-047](../decisions/ADR-047.md), [M01 Appendix B.3 and B.4](../plans/M01-rules-engine.md), the [`rule_states` design record](../architecture/data-model/rule_states.md), [ALLOCATION](../decisions/ALLOCATION.md), [INDEX](../INDEX.md) and [STATE](../STATE.md).
- **Next:** **The founder's E2 read on `0035`**, which is the only thing between this branch and a merge. Then P2's engine work, which is what actually populates the column and what `OI-14` belongs to.
- **Blockers:** None. The founder's read is a gate, not a blocker.
- **Landmines:**
  - **`0034` WAS CLAIMED TWICE IN ONE TABLE, IN ADJACENT ROWS, AND TWELVE GATES PASSED.** ADR-047 claimed `0034`; `ADR-046` claimed it in the row below, wrote the file and merged in PR #41. **This was not the two-ref gap** that the `044` collision exposed: it was one file, one ref, adjacent rows. `allocated()` in [`gates.mjs`](../../scripts/corpus/gates.mjs) accumulates claims into a **`Set`**, so two rows claiming `0034` produce one member, gaplessness holds, and **the table that exists to make a duplicate claim visible cannot see one.** This migration is `0035`. **The recovery was free only because the second file was still unwritten**: ADR-034's renumber-the-cheaper-branch remedy does not exist here, because a merged migration is sacred (E2) and cannot be renamed at all. Raised as `OI-11` and **deliberately not fixed**: the ADR table claims `039` to `046` twice each and the migration table claims `0033` twice, so the assertion fails on arrival and needs a record cleanup and a ruling on which of each pair survives.
  - **`0035` BROKE A PROBE WRITTEN BEFORE IT, AND ONLY RUNNING ALL OF THEM FOUND IT.** `probe_calendar_revision_required.sql`'s `REJECTION 8` truncated `trading_calendar, trading_calendar_revisions` **by name**, a hand-maintained list of every table in the dependency graph as of `0033`. `rule_states.calendar_revision_id` added a third, so the statement failed on the foreign key **before CALENDAR-C2 could fire**. The guard was intact; the probe had gone blind to it. **Two things already in the file stopped it being a silent pass**: the finding is checked **by message** rather than by exception class, and [section 18](../../packages/db/DELTA_MANIFEST.md)'s rule to run every probe rather than only the new one was followed. **The repair removes the list rather than extending it**: `TRUNCATE ... CASCADE` makes PostgreSQL derive the referencing set, so the next migration to reference either table cannot break it the same way, and `CASCADE` is the stronger test besides. `OI-08`'s lesson in a new costume.
  - **ON A NULLABLE COLUMN THE DANGEROUS EDIT IS A TIGHTENING, AND NO REJECTION CAN SEE ONE.** Section 13's lesson is normally that a guard refusing everything passes an inventory of refusals. This migration is the mirror image: **`NOT NULL` on `calendar_revision_id` installs cleanly, satisfies all four rejections, and refuses every state row the engine writes until somebody has corrected the calendar at least once.** `SUCCESS 1` is what fails the day somebody adds it. This is ADR-039's `SUCCESS 2` in a different costume, and it is why the probe leads with six successes rather than four.
  - **THE PROBE'S OWN HEADER OVERCLAIMED AND A SEEDED RUN CORRECTED IT.** It said the `= max(id)` guard would be caught by `SUCCESS 4` alone. **The naive form fails at `SUCCESS 1`**, because it refuses `NULL` as well; only the refined form that permits `NULL` reaches `SUCCESS 4`. **Both were seeded and the header now names both.** That is the third time in three days that executing a counterfactual corrected a claim this corpus had already written down, and it cost one sentence to fix because it was found before the merge.
  - **THE COLUMN NAME INVITES THE WRONG READING, AND A PROBE ASSERTS THE NON-CORRESPONDENCE.** `calendar_revision_id` is a **watermark over the whole calendar**, not the revision that corrected this row's `trading_day`. A rule state is folded over the day sequence from day one, so **a per-day pointer would scope replay to the corrected day and miss every downstream counter**, which is the entire failure ADR-047 exists to prevent. `SUCCESS 5` walks from a state for `2026-06-03` to the prior image of `2026-06-01` and **requires them to differ**, so a future "fix" constraining the two to agree fails there and nowhere else. Seeded and watched failing.
  - **THE RULING ITSELF IS OUT OF REACH OF EVERY CONSTRAINT, AND THE CONTRACT IS WHAT IS ASSERTED INSTEAD.** The exclusion from `state_hash` is computed by an engine that does not exist yet. The only machine-readable record of the hash input set is the column comment `0015` deliberately put it in ([ADR-026](../decisions/ADR-026.md) C-07), so `SUCCESS 6` asserts **both halves**: that the comment names the column, and that it names it as **excluded**. A comment adding it to the **hashed** list would satisfy a presence check while inverting the ruling; that inversion was seeded and is caught.
  - **NO TRIGGER ASSERTS THE STAMP IS CURRENT, AND REFUSING TO WRITE ONE IS A DECISION.** The tempting guard would force a row that genuinely read the older calendar to **claim one it never saw**, because a correction can commit between the fold and the write. **A control that can fabricate is not a control**, which is `0033`'s own header applied back to it.
  - **`CI-06h`'s `covers` LINE SAID "ALL FIVE DATABASE PROBES" WHILE THE LIST HELD SIX.** A hand-maintained count inside the gate file that exists to end hand-maintained counts. It points at the required-needle list now instead of counting it.
- **Observation, not fixed here:** three exposures are **named and left open** rather than solved, each with a reason. `OI-12`: a calendar **`INSERT`** moves no watermark, so a day backfilled inside an existing coverage window changes the day sequence retroactively with nothing recording it. ADR-045 owns `trading_calendar`'s guards and ADR-047 does not rule it, so this session did not add a money-path trigger on its own authority (`0032` header item 3's restraint, a second time). `OI-13`: **B.4 step 4's audited rewrite has no grant**, pre-existing and identical for `engine_version`. `OI-14`: **the replay job must refuse a run whose in-scope set is empty while rows exist**, because an audit that has stopped looking reports exactly like one that found nothing, and no per-row constraint can tell "not yet written" from "pristine calendar" without fabricating.
- **Files touched:** `packages/db/migrations/0035_rule_states_calendar_revision.sql`, `scripts/db/probe_rule_states_calendar_revision.sql`, `scripts/db/probe_calendar_revision_required.sql`, `.github/workflows/corpus.yml`, `scripts/corpus/gates.mjs`, `packages/db/DELTA_MANIFEST.md`, `docs/decisions/{ALLOCATION,ADR-047}.md`, `docs/plans/M01-rules-engine.md`, `docs/architecture/data-model/rule_states.md`, `docs/{INDEX,STATE}.md`, `docs/sessions/{README,2026-08-16-session-42}.md`.
