---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 569: the two properties nobody guards, where one of the two was guarded before this row opened the tree and the construction closure is thirteen rather than twelve

<!--index: **THE ROW IS [ADR-374](../decisions/ADR-374.md), `proposed` and UNSIGNED. MONEY PATH, ADR-003 STRICT, `E2` READ OWED.** [ADR-372](../decisions/ADR-372.md) section 6 ruled that [`start.ts`](../../apps/api/src/start.ts) cannot produce the half-install [ADR-356](../decisions/ADR-356.md) priced at **200/500** and ruled WORSE, for **TWO INDEPENDENT REASONS**, and this row was dispatched on the premise that **NOTHING GUARDS EITHER**. **THAT PREMISE IS HALF FALSE AND REFUTING IT IS THIS ROW'S MOST VALUABLE OUTPUT, EXACTLY AS THE DISPATCH ASKED.** **THE PLACEMENT HALF WAS ALREADY GUARDED, BY THE FILE ADR-372 ITSELF LANDED**: `apps/api/test/start-program.test.ts` asserts no install sits after `main()` and that `main()` is the LAST top-level statement, both derived from the syntax tree, and **ADR-372 section 10 item 1 records that item *"DISCHARGED as a control"* in its own words**. A row acting on the dispatched belief would have built a second control over a defended property and left the undefended one in prose. **WHAT IS GUARDED BY NOTHING IS REASON ONE IN ITS ENTIRETY** (*"no adapter factory holds a construction-reachable `throw`"*, which was a session reading eleven function bodies once, re-read by nothing) **AND THE PREMISE UNDERNEATH REASON TWO** (*"`app.listen` is reached only inside `main`"*, which existed in this repository as ADR-372 prose and as a COMMENT at `start-program.test.ts:251`; **a comment is not a control**, and a `listen` at module scope in any module `start.ts` imports binds the port DURING the import block, before every install line runs, **which is ADR-356's half-install reached without moving one install**). **BOTH ARE BUILT.** **THREE FIGURES RE-DERIVED, TWO HOLD EXACTLY AND ONE DOES NOT**: **26** top-level statements (**14** `ImportDeclaration`, **12** `ExpressionStatement`) holds; **11** adapter factories holds; **the construction closure is THIRTEEN functions and ADR-372 section 6 enumerates twelve**. The two it misses are **`blocked`**, called eleven times inside the object literal `databaseAuthBackend` RETURNS (a literal in a `return` is evaluated when the factory is called), and **the second reach of `databaseVerifySource`** from inside `databaseCertificateImageSource`, whose returned literal is `{ record: databaseVerifySource(db, env).record }`. **THE RULING SURVIVES INTACT AND THE ENUMERATION IS CORRECTED**: the closure holds **ZERO** construction-time `throw` statements, which is what reason one claims; `RI-14` is discharged by **NAMING** the superseded figure rather than reproducing the sentence that carried it. **CONSTRUCTION TIME IS DEFINED MECHANICALLY AND THE DEFINITION IS DEFENDED**: a node is at construction time of `f` when its NEAREST enclosing function-like ancestor is `f` itself, **parameter default initializers INCLUDED**, because `start.ts` writes `databaseAuthBackend(LIVE_DB)` and `postmarkOtpSender` is reachable ONLY as that call's omitted fourth argument. **A throw in the body is an install that can fail; a throw in a closure the factory returns is the per-request fail-closed refusal every adapter here is built out of, and the two look identical to a grep.** **THE DEFINITION'S TWO KNOWN HOLES ARE CLOSED BY ASSERTION RATHER THAN BY ANALYSIS**, an IIFE and a callback invoked by a construction-time callee: the opaque-callee set is PINNED at exactly `Map` and the function-argument set at EMPTY, **both equalities and neither a filter**. **FOUR SEPARATE ANTI-VACUITY DEVICES, because three of the four cases assert a list is EMPTY and a reader that read nothing would report the same**: the eleven roots are **NAMED rather than counted**, every named root must have RESOLVED to a body the case actually read, the walk must reach **PAST** the roots (13 from 11), and **`postmarkOtpSender` must be in the walked set**. **THE FOURTH CLOSED A REAL HOLE IN THIS ROW'S OWN FIRST DRAFT**: dropping the parameter-default clause left devices 1 to 3 green while an entire construction-time path went unread. **A SECOND FIRST-DRAFT DEFECT WAS THE `listen` GUARD PINNING `index.ts:168`**, a line number the row forbids asserting; it derives `index.ts::main` instead, a file and an enclosing function name, because a control that reddens on any edit above line 168 in a file this row does not own is a control that cries wolf and gets deleted. **ADR-372 SECTION 5's FOUR SHAPES ARE ALL CAUGHT BY THESE GUARDS TOO, ON A DIFFERENT MECHANISM**: a BLOCK comment, a call after `main()`, dead code after a top-level `throw` and a call inside a template literal each **remove a name from the pinned root list**, and the pin is names rather than a cardinal because a count would go from eleven to ten and say nothing about which. **ELEVEN FALSIFICATION SEEDS, TEN WATCHED RED AND ONE WATCHED GREEN ON PURPOSE**, seven editing `src/`, every one restored byte-identical and verified with `sha256sum -c`. **SEED 11 IS THE DIRECTION A GUARD LIKE THIS FAILS IN QUIETLY**: a `throw` inside a RETURNED CLOSURE of a real factory, on the real file rather than only in a fixture, watched GREEN, because a control that flagged every `throw` under a factory would flag all eleven adapters on day one, be understood as noise and be widened until it meant nothing. **SEEDS 8, 9 AND 10 SEED THIS ROW'S OWN GUARD RATHER THAN THE TREE**, which is the instruction not to add a second accidental control taken literally: `RI-25` exists here because a control passed while the thing it guarded was merely commented out, and ADR-372 calls its own text derivation *"correct here and sound only by accident"*. **`start.ts` IS NOT EDITED**, no finding forced it, and **ADR-372 section 9's imprecision at `start.ts:311` is left where it is**, named a landmine for a THIRD consecutive entry: the sentence points an appending session at the wrong place and there are now TWO controls that redden on the append it invites, so a prose repair to a money-path file by a row whose objective is guards is scope this row does not have and does not need. **OWED, AND IT IS A COVERAGE GAP IN A CONTROL RATHER THAN A DEFECT IN THE TREE**: `start-program.test.ts`' one-file-scan case is titled *"the only module under `apps/api/src`"* and scans `src` and `src/routes` with a NON-RECURSIVE listing, so **`apps/api/src/admin-source/`'s ten `.ts` files are never read**; the CLAIM is still true, verified here over all three directories, and only the COVERAGE is short. **NO PORT WIRED, NO DOOR OPENED, NO TRIPLE MOVED, no gate weakened, no test skipped, disabled or quarantined, no invariant minted, no `CI-06` letter spent, no migration number taken or reserved, no `status` field moved, and `git diff origin/main -- apps/api/src` EMPTY.** **NO ROW IS RESERVED**: `374` follows `373`, the highest on the merged head, so `CI-06f` is gapless with this row alone. **THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL**, the ninth consecutive session to record it, and the clone landed exactly on the `c7987c3c` the row names. **NO `mcp__github__*` TOOLS, NO `gh` ON PATH, `api.github.com/repos/lam2-coder/meritfutures` ANSWERS 403 while `git ls-remote` AND `git push` BOTH AUTHENTICATE**; the branch is pushed and **no pull request was opened, by instruction**. Counts derived at reporting time off each runner's own last line: suite **336 files / 8,064 passed / 10 skipped / 0 failed** against a base reproduced on `c7987c3c` **BEFORE the first edit** of **335 files / 8,060 passed / 10 skipped / 0 failed**, a delta of **+1 file and +4 cases entirely by ADDITION with zero removals** and skips unmoved at 10, `start-construction.test.ts` running **4** alone, attributed BY REMOVAL; gates **33 of 33**; invariants **35 of 35**; typecheck, lint and `format:check` clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `374`: build guards for the two properties [ADR-372](../decisions/ADR-372.md) section 6 ruled on, so that neither is a property which holds by inspection and is checked by nothing.

**One objective.** It resolved into one refuted premise, one corrected enumeration, two guards, and one coverage gap reported in a control this row does not own.

---

### 2. What was measured before the first edit

**THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL.** `/home/user` was empty and the tree had to be attached to the session's authorized set and cloned by hand, the condition sessions 550, 553, 557, 558, 561, 562, 564, 565 and 567 all recorded. **The clone landed exactly on `c7987c3c`**, which is the base the row names, confirmed rather than assumed.

**Base, on a clean tree, before anything was touched: 335 files / 8,060 passed / 10 skipped / 0 failed.**

**The dispatched figures, re-derived:**

| Dispatched | Derived | Holds |
|---|---|---|
| **twenty-six** top-level statements | **26**: 14 imports, 12 expression statements | **YES, exactly** |
| **eleven** adapter factories | **11**, one per install, all before `main()` | **YES, exactly** |
| construction reaches the eleven plus `postmarkOtpSender` | **THIRTEEN** distinct functions | **NO. One hop deep; the closure is not** |

---

### 3. THE REFUTATION, WHICH IS THE ROW'S MOST VALUABLE OUTPUT

**THE DISPATCH SAID NOTHING GUARDS EITHER PROPERTY. ONE OF THE TWO WAS GUARDED BEFORE THIS ROW OPENED THE TREE.**

`apps/api/test/start-program.test.ts`, landed by ADR-372 in the same wave, asserts that no install sits after `main()` and that `main()` is the LAST top-level statement. **ADR-372 section 10 item 1 says so itself**, recording that item *"DISCHARGED as a control"*.

**WHAT SURVIVES THE REFUTATION IS THE HALF THAT MATTERS AND IT IS TWO THINGS, NOT ONE.** Reason one is unguarded in its entirety. The PREMISE under reason two, that `app.listen` is reached only inside `main`, is unguarded and is the half that decides whether "the last statement" means anything at all.

---

### 4. What landed

| file | what |
|---|---|
| [`apps/api/test/start-construction.test.ts`](../../apps/api/test/start-construction.test.ts) | **new. +4 cases.** The construction-throw walk, the two escape-hatch pins, the definition exercised in both directions over fixtures, and the `listen` guard |
| [`docs/decisions/ADR-374.md`](../decisions/ADR-374.md) | **new.** The ruling, `proposed`, UNSIGNED |
| [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) | row `374` taken. **No row reserved**: `374` follows `373` and `CI-06f` is gapless with this row alone |
| `docs/decisions/README.md`, `docs/sessions/README.md` | registry rows |
| [`docs/STATE.md`](../STATE.md) | this entry |

**NO FILE UNDER ANY `src/` WAS CHANGED.** `git diff origin/main -- apps/api/src` is **EMPTY** after seven seeds edited it and every one was restored and verified with `sha256sum -c`.

**`docs/INDEX.md` WAS NOT CHANGED**, on the disposition [session 557](2026-09-06-session-557.md) recorded and sessions 561, 565 and 567 followed: entry files are fragments indexed by their own README.

---

### 5. Falsification

**Eleven seeds. Ten watched RED, one watched GREEN on purpose, every one restored byte-identical.** Three worth keeping here:

- **SEED 2 IS THE PARAMETER-DEFAULT PATH AND THE TRANSITIVE WALK IN ONE.** A `throw` in `postmarkOtpSender`, which `start.ts` reaches only by omitting `databaseAuthBackend`'s fourth argument. Red.
- **SEED 8 IS THIS ROW SEEDING ITS OWN GUARD, AND IT FOUND A REAL HOLE IN THE FIRST DRAFT.** Dropping the parameter-default clause from the reader left three of the four anti-vacuity devices green while an entire construction-time path went unread. The fourth device exists because of this seed.
- **SEED 11 IS THE DIRECTION THAT MATTERS AND IT IS WATCHED GREEN.** A `throw` inside a returned closure of a real factory. **A guard that flagged it would flag all eleven adapters on day one and be widened until it meant nothing.** Seeds 1 and 2 show it is not green because it is blind.

---

### 6. Blockers and landmines

- **The founder's `E2` read of [ADR-374](../decisions/ADR-374.md) is owed.** Its approval block names three decisions, the second of which is what to do the day a factory legitimately needs a shape the definition cannot analyse: **widening the pinned list is the wrong answer and nothing but that paragraph says so.**
- **LANDMINE, NAMED FOR A THIRD CONSECUTIVE ENTRY: `start.ts:311` CALLS THE LAST INSTALL THE LAST LINE.** ADR-372 section 9 reported it and declined the repair; this row declines it again, with a stronger reason, and it is a one-line `src/` prose row for whoever wants it. **A finding nobody's fence covers is a finding that does not get fixed.**
- **LANDMINE: THE OPAQUE PIN AND THE ROOT PIN ARE BOTH MAINTENANCE TAXES THAT CAN BE PAID BADLY.** Each reddens a money-path bar and each has a tempting repair that skips the reading the pin exists to force.
- **OWED TO `start-program.test.ts`, WHICH THIS ROW DOES NOT OWN:** its one-file-scan case scans two directories non-recursively while its title claims all of `apps/api/src`. `admin-source/`'s ten files are never read. **The claim is TRUE, verified here over all three directories; only the coverage is short.**
- **Concurrency: sibling rows are running now.** This row reserved no `ALLOCATION` number. A registry conflict at merge is expected and is the designed workflow.

---

### 7. Verification, derived at reporting time

| runner | result |
|---|---|
| `pnpm vitest run` | **336 files / 8,064 passed / 10 skipped / 0 failed** |
| baseline on `c7987c3c`, before the first edit | **335 files / 8,060 passed / 10 skipped / 0 failed** |
| delta | **+1 file and +4 cases. Skips unchanged.** Attributed by removal: `start-construction.test.ts` runs **4** alone and does not exist on the base |
| `scripts/corpus/gates.mjs generate` then `check` | **33 of 33** |
| `packages/tooling/checks/repo-invariants.mjs` | **35 of 35** |
| `pnpm run typecheck` | clean |
| `pnpm run lint` | clean |
| `pnpm run format:check` | clean |
| `git diff origin/main -- apps/api/src` | **EMPTY** |
| `git diff --stat origin/main -- packages/db/migrations scripts/db` | **EMPTY** |

**`pnpm run verify` was NOT run and `scripts/corpus/falsify.mjs` was NOT run**, both forbidden by the row.

---

### 8. This session's own tooling, measured rather than assumed

```
$ command -v gh                                            -> not found
$ curl -s -o /dev/null -w '%{http_code}' \
    https://api.github.com/repos/lam2-coder/meritfutures   -> 403
$ git ls-remote https://github.com/lam2-coder/meritfutures -> OK, HEAD c7987c3c
```

**No `mcp__github__*` tool exists in this session's surface.** `git push` authenticates through the session's git proxy and did so on every commit. **This matches sessions 554, 558, 562, 564, 565 and 567 exactly**, with one addition: `git ls-remote` also authenticates, which is how the base commit was confirmed before the clone.

**Every Bash command was run bare from the working directory**, per the row's operational note. **No permission stall occurred**, which is the tenth consecutive row to report that. The one departure from the note is recorded rather than glossed: the working directory had to be set to the clone once, because the container arrived with no repository and `/home/user` is not the tree.

**THE BRANCH IS `claude/two-properties-nobody-guards`, PUSHED. NO PULL REQUEST WAS OPENED, BY INSTRUCTION.**

---

### 9. Next

The founder's `E2` read of [ADR-374](../decisions/ADR-374.md) and the three decisions its approval block names. Then, in order of cheapness: the `start-program.test.ts` coverage gap in section 6, which is a recursive listing and nothing else; `start.ts:311`, which is one line of prose and has now outlived three entries that each correctly declined it; and ADR-372 section 15 question 1, which is whether `wired` should stop being a line pattern at all.
