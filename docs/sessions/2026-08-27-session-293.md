---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 293: the destination registry, and the recommendation's missing word is the whole control

<!--index: placeholder -->

- **Objective:** close `OI-06 (payout destinations)` by landing `payout_destinations` in migration `0051`, taking or refusing [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md)'s own recommendation with an argument, and registering the table in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts). **MONEY PATH, `E2` READ OWED on every line of `0051`.**
- **Done:** placeholder
- **Next:** placeholder
- **Blockers:** placeholder
- **Landmines:** placeholder
- **Files touched:** placeholder

---

### 0. THE PLAN, written into the first commit because no human is watching to approve one

**[ADR-003](../decisions/ADR-003.md) strict makes plan mode mandatory for a money-path slice and this session is harness-launched**, so the plan is committed here before a line of DDL is written, and every departure from it below is reported as a departure rather than silently absorbed.

**The baseline was reproduced BEFORE anything changed**, per the dispatch: 33 of 33 gates, 12 of 12 invariants, `typecheck` exit 0, `lint` exit 0, `format:check` clean, **183 files / 3,521 passed / 6 skipped.**

**The disposition, stated up front: the recommendation is TAKEN on its key and its two columns, and AMENDED on the one thing it does not say.** `(identity_id, destination_ref)`, `first_seen_at`, `cooling_until`, read by both payout legs and by the affiliate rail. The amendment is `cooling_until NOT NULL`, and the argument for it is that a nullable cooling column is a control that fails OPEN on exactly the row an attacker just wrote.

**The order of work, one file per commit:**

1. This stub.
2. `packages/db/migrations/0051_payout_destinations.sql`, with an `E2 READ: MONEY PATH` header naming what the founder must read and why, on `0050`'s shape.
3. The PostgreSQL 16 probe: `0001..0051` applied forward-only from empty under `ON_ERROR_STOP`, the table count read from `pg_tables`, the grants read from `information_schema.table_privileges` rather than assumed, and the revoke shown load-bearing by running the file WITHOUT it.
4. `docs/architecture/data-model/payout_destinations.md`, which `CI-06i` requires in both directions.
5. `packages/db/src/schema.ts` and `packages/db/src/scope.ts`, appended rather than interleaved ([ADR-092](../decisions/ADR-092.md) section 2).
6. `docs/decisions/ADR-169.md`, stating what it FORECLOSES and not only what it chooses (P5 rule 8).
7. [ALLOCATION](../decisions/ALLOCATION.md)'s `169` row and its `0051` row, both amended IN PLACE ([ADR-065](../decisions/ADR-065.md) T3, `CI-06f`).
8. [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md): the delta row and the `OI-06 (payout destinations)` row.
9. [STATE](../STATE.md), one appended `##` section at the END.
10. This log, filled in, and `gates.mjs generate`.

**What this session will NOT do, decided before it starts:** no `SqlExecutorReason` member, no `SystemReason` member, no `pg` import, no cast past a key type, no edit to [`scoped-db.ts`](../../packages/db/src/scoped-db.ts), no route, no contract row, and no edit to any merged migration.

**One fence cell is already known to be missing and it is reported here rather than discovered later.** [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts):412 asserts `createdTables` is 113 and a new `CREATE TABLE` moves it to 114; the same two counts above it move when a table is registered. The fence does not name that file. **The precedent is two sessions old and runs the same way both times**: `92bacff` moved it 111 to 112 for `0049` and `d18c87f` moved it 112 to 113 for `0050` under the commit subject *"the fence did not hold this file"*. This session follows it, keeps the edit to the counts and their reasons, and reports it in the pull-request body.
