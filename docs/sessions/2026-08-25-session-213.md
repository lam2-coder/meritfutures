---
status: approved
depends_on: [README.md]
last_updated: 2026-08-25
---

## 2026-08-25 - Session 213: the accessor learns to write, to run a transaction, and to say who is asking when nobody is
<!--index: **[ADR-102](../decisions/ADR-102.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH and the `E2` read owed. RULED in three clauses: `scopedDb(id)` and `systemDb(reason)` are UNCHANGED and stay READ ONLY, a write is reached only through `transaction(handle, fn)`, and an INSERT has no `WHERE` clause so the identity is STAMPED from the handle over `OwnedTableKey` alone; `SqlExecutor` is the shape [`job-queue.ts`](../../packages/queue/src/job-queue.ts)'s `JobTransaction` declares and `transaction()` is its ONLY producer; `firmDb()` serves `FirmTableKey` and takes NO reason, and `SystemReason` stays at two members.** **`P3-f` IS THE PRECONDITION UNDER P3's WHOLE WAVE 3 AND TEN SLICES WERE BEHIND IT.** **NOTHING IN THIS WORKSPACE COULD ENQUEUE A JOB AND IT WAS RE-DERIVED AT SOURCE BEFORE IT WAS FIXED**: `enqueue` takes the caller's open transaction FIRST with no overload that omits it and its own header says `packages/db` *"can satisfy it without exporting its client"*, and `packages/db` did not, so [ADR-006](../decisions/ADR-006.md)'s transactional-enqueue criterion has never been gradeable. **THERE IS NO NON-TRANSACTIONAL WRITE, AND THAT IS `job-queue.ts`'s OWN RULING APPLIED TO THE ACCESSOR RATHER THAN A NEW ONE**: *"THE AWKWARD ONE TO WRITE IS THE NON-TRANSACTIONAL ONE... There is no `enqueueNow`, no optional transaction and no ambient default."* **THE READ INTERFACES DID NOT MOVE AND THAT IS THE MOST USEFUL THING THIS SESSION FOUND.** `CI-01/scope-system-db-not-assignable` seeds a value carrying exactly `__brand`, `reason`, `identityId` and `rows()` and demands the needle `__brand`, on [`falsify-ci.mjs:689-694`](../../scripts/ci/falsify-ci.mjs)'s own stated reason that a missing property *"passes a naive needle while proving the WRONG THING"*. **A required method added to `ScopedDb` makes `tsc` report FOUR MISSING PROPERTIES and the case stops proving the brand.** [`falsify-ci.mjs`](../../scripts/ci/falsify-ci.mjs) is **outside this fence**, so the constraint was met BY DESIGN rather than by widening, and `node scripts/ci/falsify-ci.mjs CI-01` reports **12 of 12**, unchanged, found on `ScopedTableKey` and on `__brand`. **A FOURTH FAILURE CLASS IS THE WRITE PATH'S OWN AND IS THE ONLY ONE THIS ENTRY INVENTS A CONTROL FOR: RE-PARENTING.** A `SET` that moves the tenancy column hands the row to somebody else AFTER the predicate has already agreed to it, and it is least visible on `derived`, where the column being moved is a foreign key and looks like data: `ledger_entries.ledger_account_id` re-pointed at another identity's account leaves this scope and enters theirs with every check in the package green. `tenancyColumn(key)` returns `rule.localColumn` for `derived` and the guard refuses both the SQL and the Drizzle spelling. **OF [ADR-101](../decisions/ADR-101.md)'s THREE READ-SIDE CLASSES, THE WRITE PATH CANNOT SUFFER ONE, INHERITS ONE HARMLESSLY, AND INHERITS ONE IN THE DIRECTION THAT DESTROYS**: a non-total reverse-edge `semi-join` returns a subset on a read and **deletes a subset and leaves the rest** on a write, with no error anywhere, and `ledger_transactions` is the registry's only `semi-join` rule. **Not closed here, and the remedy edits a row this fence excludes.** **THE THIRD DOOR EXISTS BECAUSE ONE DOOR WAS SERVING TWO DIFFERENT ACTS**: `systemDb(reason).rows(key)` is generic over `TableKey`, so it serves *everybody's rows read without their identity* AND *nobody's rows*, and only the first has an identity at risk. [ADR-096](../decisions/ADR-096.md) is the precedent and **it went the other way**, and its remedy is unavailable because under that same entry `apps/api` is the process the site reads THROUGH. A third `SystemReason` is refused on ADR-096's own argument **and on a finding it did not have: the reason names the CALLER and the key type names the REACH, and they are not connected**, so `systemDb('request-handler')` would have read `accounts` and `payout_requests` firm-wide. **WHAT THE THIRD DOOR COSTS IS STATED AGAINST THE ENTRY**: the `firm` classification now decides read AND write authorization for **35 tables with no word written at the call site**, [ADR-101](../decisions/ADR-101.md) section 8's blind `firm` assertion is the only thing checking it, and [ADR-096](../decisions/ADR-096.md) section 9's measured hole gets **cheaper** to fall into because `firmDb()` is shorter than `systemDb('operator-console')`. **`RI-08` is still unwritten and is worth more after this entry than before it.** **FOUR COMPILE-TIME REFUSALS WERE WATCHED FAILING AND NONE IS PERMANENTLY WATCHED** -- `TS2345` on `FirmTableKey`, `TS2345` on `OwnedTableKey` twice, and `TS2322` on `__brand` with `identityId` present so the BRAND is what refuses -- **and `falsify-ci.mjs` is where they belong and is outside this fence**, so the follow-on is four case objects taking `CI-01` from 12 to 16. **THREE MUTATIONS WATCHED AT 5, 3 AND 6 OF 32 FAILING**, the third being `tenancyColumn` reading nothing, which is [ADR-101](../decisions/ADR-101.md)'s own lesson applied before it cost anything; **one weakness in that result is recorded rather than rounded up**, because one refusal test still passes there on a message its pattern also matches. **NOTHING EXECUTED A WRITE AND THE SUITE SAYS SO IN ITS OWN HEADER**: `ci.yml`'s `integration` job runs on bare `ubuntu-latest` with no services block, so every assertion reads the SQL the accessor BUILDS, over a driverless Drizzle handle that RECORDS what it is asked to run, which is what `scopedTx(...).update(...)` actually sends rather than a reimplementation of it. **NO TABLE'S ROWS ARE TOUCHED**: [`scope.ts`](../../packages/db/src/scope.ts), [`schema.ts`](../../packages/db/src/schema.ts) and [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) are not edited at all, which is the whole reason this ran concurrently with five sessions, and the registry stands at **92 of 111** unmoved. **A DISPATCH BASELINE WAS TEN STALE AND IS REPORTED RATHER THAN INHERITED**: the full suite is **1,675 passed** at this branch's base and not 1,665. `typecheck` exit 0 with zero `error TS`, `eslint packages/db` exit 0, invariants **8 of 8**, gates **30 of 30**, `verify` exit 0, suite **111 files / 1,675** to **112 files / 1,707 passed / 1 skipped** -->

- **Objective:** rule and implement the write accessor, the transaction primitive and the reader with no identity. **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **One ADR: [ADR-102](../decisions/ADR-102.md).**
- **Done:** [ADR-102](../decisions/ADR-102.md) landed at `status: proposed` with the approval line UNSIGNED, and the accessor with it: [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) gains `firmDb()`, `transaction()`, three transaction handles, `tenancyColumn()` and the statement builders; [`index.ts`](../../packages/db/src/index.ts) exports them; [`write-accessor.test.ts`](../../packages/db/test/write-accessor.test.ts) is a NEW file at **32 tests**. **`scopedDb` and `systemDb` are byte-identical to what [ADR-084](../decisions/ADR-084.md) signed**, which is a ruling rather than a convenience and is why `CI-01` still reports 12 of 12. Suite **111 files / 1,675** to **112 files / 1,707 passed / 1 skipped**.
- **Next:** **The `E2` line-by-line read is owed on [ADR-102](../decisions/ADR-102.md) and on [`scoped-db.ts`](../../packages/db/src/scoped-db.ts)**, and the approval clause is written as four commands so the read can execute it rather than trust it. **Four compile-time refusals need permanent watching in [`falsify-ci.mjs`](../../scripts/ci/falsify-ci.mjs), which is outside this fence**: four case objects on [ADR-084](../decisions/ADR-084.md)'s own pattern, `CI-01` 12 to 16, needing an `ALLOCATION` row and nothing else. **`INSERT ... SELECT ... WHERE EXISTS` for `derived` tables is named, priced and not built**, and the first caller that needs it should build it with its own semantics in hand. **Ten slices can start**, and sessions 214 and 215 will merge into this with no shared file.
- **Blockers:** none for this session.
- **Landmines:**
  - **ADDING A REQUIRED METHOD TO `ScopedDb` BREAKS [ADR-084](../decisions/ADR-084.md)'s SECOND WATCHED REFUSAL AND THE BREAK IS QUIET.** `CI-01/scope-system-db-not-assignable` seeds a value carrying exactly `__brand`, `reason`, `identityId` and `rows()` and needles on `__brand`, precisely so the brand and not a missing property is what refuses. Add `insert`, `update`, `delete` or `transaction` to that interface and `tsc` reports four missing properties instead. **The needle catches it today; a needle on the exit code would not have, and that is the shape the next session should look for.** This is the single constraint that decided the design, and the honest ordering is that the fence made it visible, the constraint was checked, and `job-queue.ts` had already ruled the same question for the same reason.
  - **[ADR-101](../decisions/ADR-101.md)'s THIRD FAILURE CLASS REACHES THE WRITE PATH AND IS WORSE THERE.** A `semi-join` on a reverse edge whose relation is not total returns a SUBSET on a read; through `delete` it **deletes a subset and leaves the rest**, committed, with no error anywhere. **`ledger_transactions` is the registry's only `semi-join` rule and rests on that unstated property today.** This session inherits it and does not close it: the remedy ADR-101 prices is an attestation on reverse-edge rules, which edits `ledgerTransactions`' row in [`scope.ts`](../../packages/db/src/scope.ts), outside this fence.
  - **RE-PARENTING IS A FAILURE CLASS THE READ SIDE DOES NOT HAVE, AND ON `derived` IT DOES NOT LOOK LIKE ONE.** The predicate matched the row as it stood; a `SET` that moves the tenancy column hands the row away afterwards. On `owned` the column is called `identity_id` and a reviewer sees it. On `derived` it is a foreign key -- `ledger_entries.ledger_account_id`, `provisioning_queue.account_id` -- and re-pointing it reads like ordinary data maintenance. **`tenancyColumn` returns `rule.localColumn` for that class and the guard refuses it, and this is the one control in the entry that nothing upstream asked for.**
  - **`firmDb()` MAKES THE `firm` CLASSIFICATION LOAD BEARING FOR AUTHORIZATION AND ITS ONLY CHECK IS KNOWN BLIND.** 35 tables are now readable and writable with no word written at the call site, and [ADR-101](../decisions/ADR-101.md) section 8 records that `no firm table carries a column referencing identities` reads the `CREATE TABLE` and misses `admin_actions.on_behalf_of_identity_id`, `ALTER`-added in [`0043`](../../packages/db/migrations/0043_admin_attributed_actions.sql). **`admin_actions` is correctly `firm`. What is missing is any guarantee that the next one will be.**
  - **THIS ENTRY LOWERS THE WALL INSIDE [ADR-096](../decisions/ADR-096.md) SECTION 9's MEASURED HOLE.** That entry proved that adding `"@merit/db": "workspace:*"` to `apps/site/package.json` and seeding a firm-wide read leaves lint, typecheck, invariants and gates all green, and named `RI-08` as the remedy. `RI-08` is still unwritten. **A marketing page could previously reach a `firm` table only by writing `systemDb('operator-console')`, which reads oddly; it can now write `firmDb()`, which reads like nothing.**
  - **THE TWO-WORD SYSTEM VOCABULARY NOW AUTHORIZES WRITING EVERY ROW IN THE ESTATE.** `'nightly-batch'` needs it. **`'operator-console'` is the one that costs**: it was accepted for a liability dashboard and now reaches every row with a `delete`, and nothing in this package distinguishes the two. Narrowing it later means splitting the vocabulary and touching both call sites.
  - **THE POOL IS REACHED THROUGH DRIZZLE'S `$client`, WHICH [`client.ts`](../../packages/db/src/client.ts)'s DECLARED RETURN TYPE ERASES.** `client.ts` is outside this fence, so `poolFromClient()` proves the property at runtime and the suite watches it. **A Drizzle upgrade that moves that property breaks every write in the estate at once**, and what catches it is a unit test rather than a type.
  - **NO WRITE IN THIS ESTATE HAS EVER BEEN EXECUTED AGAINST A DATABASE.** `ci.yml`'s `integration` job runs on bare `ubuntu-latest` with no services block. The suite asserts what the accessor SENDS and nothing asserts what Postgres does with it, and that is a gap rather than a decision.
  - **ONE MUTATION RESULT IS WEAKER THAN THE COUNT SUGGESTS.** Under the `tenancyColumn`-reads-nothing seed, `an INSERT naming the tenancy column is refused, in the registry spelling` still PASSES, because the accessor then throws a different error whose message the assertion's pattern also matches. **The mutation is caught six times and that test is not one of them.**
  - **`falsify:ci` WAS RUN IN THE FOREGROUND, NEVER BACKGROUNDED, AND `git add -A` WAS NEVER USED IN THIS SESSION.** Every seeded file was removed and `git status --porcelain` read before each commit; [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) was diffed byte-for-byte against a pre-seed copy after each of the three mutations.
- **Files touched:** [`packages/db/src/scoped-db.ts`](../../packages/db/src/scoped-db.ts) (append, plus its import block), [`packages/db/src/index.ts`](../../packages/db/src/index.ts) (one export block), [`packages/db/test/write-accessor.test.ts`](../../packages/db/test/write-accessor.test.ts) (new), [ADR-102](../decisions/ADR-102.md) (new), [ALLOCATION](../decisions/ALLOCATION.md) (its row, reserved in the first commit and amended in place when the file landed), [decisions/README](../decisions/README.md) and [INDEX](../INDEX.md) (generated), [STATE](../STATE.md) (one appended `##` section plus a generated count span), [sessions/README](README.md) (row 213 struck, entry row generated), this file. **Nothing outside the fence.** [ADR-008](../decisions/ADR-008.md), [ADR-006](../decisions/ADR-006.md), [ADR-084](../decisions/ADR-084.md), [ADR-092](../decisions/ADR-092.md), [ADR-096](../decisions/ADR-096.md), [ADR-100](../decisions/ADR-100.md), [ADR-101](../decisions/ADR-101.md), [`scope.ts`](../../packages/db/src/scope.ts), [`client.ts`](../../packages/db/src/client.ts), [`job-queue.ts`](../../packages/queue/src/job-queue.ts), [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) and [`falsify-ci.mjs`](../../scripts/ci/falsify-ci.mjs) were **read and none was written**.

### What was NOT done, named rather than implied

**No migration, no migration number claimed, no `CI-06` letter, no gate register row, no `VG` row, no `RI-nn`, no `SD-nn`, no `OI-nn`, no fixture, no edge-case entry, no route, nothing under [`apps/`](../../apps/api/src/server.ts), and no edit to [`docs/plans/`](../plans/P3-wave3-modules.md), which this fence makes read only.**

**NO DEPENDENCY WAS ADDED, SO NO `VG-12` ADMISSION IS CLAIMED.** `packages/db/package.json` and `pnpm-lock.yaml` are untouched: `drizzle-orm/pg-proxy` is a subpath of a package already admitted, and `pg` is imported for its TYPES ONLY, which erases, so [`client.ts`](../../packages/db/src/client.ts) remains the only file in the workspace that imports it at runtime. **No `build` script was added anywhere**, which `CI-07` and the gate inventory require to stay absent.

**[`packages/db/src/scope.ts`](../../packages/db/src/scope.ts), [`schema.ts`](../../packages/db/src/schema.ts) AND [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) ARE NOT EDITED AT ALL.** `git status --porcelain` names none of them on this branch. **No table is registered, unregistered or re-argued**, which is [ADR-092](../decisions/ADR-092.md)'s ownership rule kept and the whole reason this session could run beside sessions 206, 207, 208, 214 and 215.

**[`scripts/ci/falsify-ci.mjs`](../../scripts/ci/falsify-ci.mjs) IS NOT WIDENED ONTO, AND THAT IS THE DECISION THIS SESSION IS LEAST SURE OF.** [ADR-084](../decisions/ADR-084.md) section 10 widened onto exactly this file, for exactly this reason, and stated it: *"The two compile-time refusals cannot be watched anywhere else. A session fenced out of the file that validates its change cannot end green."* **This session ends green without it because the four new refusals were watched failing here, with the compiler output quoted in [ADR-102](../decisions/ADR-102.md) section 9, and because the existing two are unaffected.** But a refusal watched once in a session log is not a refusal a gate holds. **The follow-on is small, has a one-file fence, and is named in the entry's section 12 rather than left to be noticed.**

**THE `INSERT ... SELECT ... WHERE EXISTS` FORM FOR `derived` TABLES IS NOT BUILT.** It is correct, it is roughly forty lines, and it is refused on [`job-queue.ts`](../../packages/queue/src/job-queue.ts)'s own rule that *"a primitive admitted before a caller exists is a primitive nobody can remove"*. **Neither of the two slices that will insert into a `derived` table is blocked by the absence** -- a double-entry posting touches two identities' accounts by construction and is a firm-class act, and the provisioning saga runs on a job with no session -- **but both now have to say which they are, and that is the point rather than a workaround.**

**THE BLIND `firm` ASSERTION IS NOT REPAIRED AND [ADR-101](../decisions/ADR-101.md)'s THIRD CLASS IS NOT CLOSED.** Both are named with their one-line remedies in [ADR-102](../decisions/ADR-102.md) sections 3 and 7, both edit files this fence excludes, and both are worth more now than they were this morning.

### The disagreement between the dispatch and the plan, reported

**The dispatch named the branch `claude/s213-p3f-write-accessor` and [P3 wave 3](../plans/P3-wave3-modules.md) section 15 names it `claude/p3f-write-accessor`.** The plan wins on substance by its own instruction and by the dispatch's, but a branch name is not substance: the designated-branch instruction is the harness's and is binding on where the work lands, so the work is on the dispatch's name. **Reported rather than resolved silently**, which is what the dispatch asked for.

**The dispatch's baseline for the full suite was `1,665 passed`; measured on this branch's base it is `1,675 passed / 1 skipped` over 111 files.** Ten stale, and every figure in this log is the measured one.

### The merge, and what it changed

**[PR #277](https://github.com/lam2-coder/meritfutures/pull/277) merged at `c0205ec` while this branch was open**, moving the base from `8914c66`. It is [session 209](2026-08-25-session-209.md)'s reported defect repaired -- `node --experimental-strip-types` does not map `./x.js` onto `x.ts`, so no `start` script in this estate had ever run -- and it renames **every relative specifier in the workspace from `.js` to `.ts`** across 178 files.

**IT TOUCHED FOUR OF THE FIVE `packages/db` FILES THIS SESSION READS OR WRITES.** **The conflict was ONE HUNK, in [`scoped-db.ts`](../../packages/db/src/scoped-db.ts)'s import block, and it was a keep-both**: this session had added `schema.ts` and `FirmTableKey` to the same statements the sweep re-spelled. **Nothing else conflicted**, because this session appends to the end of one source file, adds one new test file, and touches no table's rows -- which is the reason the diff was kept to the mechanism in the first place.

**EVERYTHING WAS RE-RUN ON THE MERGE RESULT RATHER THAN INHERITED**, and the falsification with it: `typecheck` exit 0 with zero `error TS`, `vitest run packages/db` **9 files / 278 passed**, full suite **112 files / 1,707 passed / 1 skipped**, invariants **8 of 8**, gates **30 of 30**, `verify` exit 0, and **`node scripts/ci/falsify-ci.mjs CI-01` 12 of 12** still found on `ScopedTableKey` and on `__brand`.

**ONE CLAIM WAS RE-MEASURED BECAUSE IT WAS A COUNT THIS SESSION HAD NOT TAKEN ITSELF.** [ADR-102](../decisions/ADR-102.md) section 7 says `admin_actions` is the only one of the **35** firm tables reaching `identities` directly; [ADR-101](../decisions/ADR-101.md) measured that over **31**. **Folded over all 47 migrations on the merge result, it is still the only one.** The registry is unmoved at **92 of 111**.

### What the founder's `E2` read is being asked for

**Three of the eight foreclosures in [ADR-102](../decisions/ADR-102.md) section 8 are judgements rather than derivations, and they are the read's subject.**

**That every write opens a transaction.** Paid on every single-statement insert in the estate, forever, in exchange for making [ADR-006](../decisions/ADR-006.md)'s central consequence structural instead of a convention. **Undoing it means adding a method to `ScopedDb`, which is the change that breaks `CI-01`'s brand seed**, so this is close to a one-way door.

**That the two-word system vocabulary now authorizes writing.** `'operator-console'` was accepted for a liability dashboard and now reaches every row in the estate with a `delete`.

**That `firmDb()` takes no reason.** It rests on the claim that a `firm` row endangers nobody, which is true exactly as far as the `firm` classification is true, and the only mechanical check on that classification is known blind.
