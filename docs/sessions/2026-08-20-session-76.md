---
status: approved
depends_on: [README.md]
last_updated: 2026-08-20
---

## 2026-08-20 - Session 76: WAVE-03 planned, and five claims in the record that the tree refutes (planning)
<!--index: WAVE-03 planned: the 106 duplicate registry keys sequenced into nine sessions, five concurrent, with every number reserved before any session runs. Five claims in the record refuted against the tree, three findings no gate can see, and the letter registry's wall -->

- **Objective:** sequence the duplicate registry keys `CI-06u` pinned and did not repair. Deliverable is a plan under `docs/plans/` with the fences, the reserved numbers and a pasteable prompt per session. Fence: `docs/plans/WAVE-03-duplicate-registry-keys.md`, `docs/INDEX.md`, `docs/decisions/ALLOCATION.md`, this file.
- **Done. The plan, its INDEX row, and every number it spends reserved in its own commit before any session runs.** Nine sessions, five concurrent. **22 of 22 gates pass**, `falsify.mjs` reports all 22 clean and dirty with 30 scope cases and 10 loader cases, `check:invariants` 7 of 7. **Not one of the duplicates was repaired**, which was the instruction.
- **Next:** session `S1`, `ADR-061`, alone at the front. Then `S2` to `S6` concurrently.
- **Blockers:** None. `S7` and `S8` are blocked on `S6` merging and the plan says so at their prompts.

### The count is 106 and the corpus says 105 in six places

`node scripts/corpus/gates.mjs check` prints **`106 known duplicate key(s) registered across 8 file(s)`** and has printed it on every run since `CI-06u` landed. The register's `docs/sessions/README.md` list holds **59** keys, not the 58 the survey table reports. **The gate and the prose beside it have disagreed since the day they were written together.**

### Four more claims checked against the tree rather than against the record

| Claim | What the tree says |
|---|---|
| `G-ELIGIBLE` at lines 405 and 408 | [STATE_MACHINES](../architecture/STATE_MACHINES.md) **:403 and :406**. Lines 405 and 408 are `G-NO-IN-FLIGHT`. Verified unmoved since the `CI-06u` commit with `git show 5b30dff` |
| A `suspended` identity separates the two `G-ELIGIBLE` predicates | **No such value.** [`0001`](../../packages/db/migrations/0001_extensions_and_enums.sql):27 is `('active','restricted','closed')` and [ADR-041](../decisions/ADR-041.md) refused to add one. **The witness is `closed`**, which passes `<> 'restricted'` and fails `= 'active'` |
| ALLOCATION's `r` rows are byte-identical but for one link, so the dedupe reached one | Both stand on `main` at :151 and :153 and differ by a whole parenthetical clause. The dedupe reached neither |
| `OI-10` deduplicated 2026-08-16 | `CI-06, corpus integrity` heads **four** rows of [STATE](../STATE.md) today. Two read `22`, two read `Eleven` |

**The contradiction is real in every case; only its stated witness was wrong.** A `closed` identity reaching a payout gate is a sharper question than a value that does not exist, and `ADR-062` now has to answer it.

### Three findings no gate can see, and the trap that follows

**Four stray blank lines shatter [ALLOCATION](../decisions/ALLOCATION.md)'s letter table.** `markdownTables()` builds a table from a maximal run of pipe lines carrying at least one delimiter row, so the table is five runs and only the first has a delimiter. **Seven rows, `q` through `u`, are read by no table gate.** That, and not the dedupe, is why `r` is absent from the register.

**Inside that blind spot, the letter `u` is claimed by two different gates**, at :157 and :159. `allocatedLetters()` returns a `Set` and `CI-06p` asserts uniqueness over [STRATEGY](../testing/STRATEGY.md) rather than over this file, so a live collision in the allocation registry is invisible to the registry's own gate. This is `OI-11`.

**And six rows of STRATEGY's gate inventory sit outside any table**, at :238 to :243, behind a re-inserted header, a re-inserted delimiter and a blank line at :235 to :237. `CI-06p` reads them, because `strategyGateLetters()` is a line regex. `CI-06u` does not. **`CI-06u`'s own row is one of the six.**

**The trap:** removing those blank lines merges the fragments and makes `r` and `u` duplicate keys. The register shrinks only, so a session cannot add them. **Whoever removes the blank lines must repair `r` and `u` in the same commit.** Session `S6` holds both halves for that reason and its prompt says so twice.

### WAVE-01's R1 never ran

[WAVE-01](../plans/WAVE-01-post-freeze-parallel-sessions.md) section 4 plans **`R1`, the duplicated registry rows, session 70, LAST AND ALONE**. There is no `docs/sessions/2026-08-18-session-70.md` and no index row for it. **The plan exists, the number was spent, the work never happened**, and the duplicate set has grown by five ADR numbers since. `S5` and `S6` absorb it rather than supersede it.

### The line, and it was already half drawn

[INDEX](../INDEX.md):37, `Tracking (living docs, updated every session)`, already names [INDEX](../INDEX.md), [STATE](../STATE.md), [sessions/](README.md), [decisions/](../decisions/README.md) and [ALLOCATION](../decisions/ALLOCATION.md) as documents that move by commit. **Four of the eight damaged files are on that list and nobody has cited it.** No file in this corpus carries `status: frozen`; all eight read `status: approved`, so FROZEN is the corpus-wide state and that row is the existing carve-out. `ADR-061` draws the other half, over specification documents, with three triggers and a safeguard that escalates rather than merges when a session cannot state that two halves agree.

**Five ADRs across 106 keys.** Ruling all of them teaches the next session that an ADR is a formality; ruling none lets a session pick half a money-path invariant, which is `G-ELIGIBLE` today.

### Landmines

- **THE LETTER REGISTRY IS ONE FREE LETTER FROM ITS WALL, AND THIS SESSION FOUND IT BY HITTING IT.** A third letter was reserved, `falsify.mjs` reported **`SEED IS STALE ... seed anchor exhausted`**, and the number was given back. [`nextFreeLetter()`](../../scripts/corpus/falsify.mjs) scans **`a` to `x` only**, deliberately, because every `CI-06p` seed needs two letters of headroom above the one it names. **The usable registry is 24 letters, not 26**, `CI-06w` spends the twenty-third, and `CI-06a` through `CI-06u` were claimed in about a week. `ADR-065` now owns the question and the remedy is not a bigger alphabet.

- **THE GATE CAUGHT THIS PLAN'S OWN DUPLICATE KEY BEFORE THE PLAN WAS COMMITTED.** The wave table's first column was headed `Wave` and its values repeat by construction. `CI-06u` reported four findings against `docs/plans/WAVE-03-duplicate-registry-keys.md` in the same run that reports 106 elsewhere. **The column is now headed `Rank`, which reuses [WAVE-01](../plans/WAVE-01-post-freeze-parallel-sessions.md)'s already-argued `DIMENSION_HEADERS` exemption for exactly this shape** rather than widening the list, which is what that exemption's own comment asks for.

- **THE RESERVATION ROWS FAILED `CI-06a` ON THEIR FIRST WRITE AND THAT IS THE CONTROL WORKING.** They linked the plan document before it existed. **A reservation row names a bare number and does not link it**, which the corpus already knew and which this session re-learned by breaking it. The rows are unlinked now and `S6` links them once, at the end, in one pass.

- **`S6` IS CONCURRENT ONLY BECAUSE THIS PLAN PRE-RESERVED.** `WAVE-01`'s `R1` had to run last and alone because sibling sessions were appending claims to ALLOCATION while it edited them. No WAVE-03 session touches that file except `S6`. **If any session is later given a number it must claim itself, `S6` goes back to being serial and the wave loses a round.**

- **Files touched:** [WAVE-03](../plans/WAVE-03-duplicate-registry-keys.md) (new), [INDEX](../INDEX.md) (one row), [ALLOCATION](../decisions/ALLOCATION.md) (five ADR rows, two letter rows, `last_updated`), [STATE](../STATE.md) (one section appended at the end, no existing line edited), [sessions/README](README.md), this file.

### What was NOT done, named rather than implied

**Not one of the 106 duplicates was repaired**, which was the instruction. No gate was written and `scripts/corpus/` was not opened for edit. **No migration number was taken and `0038` stays free.** Nothing under `packages/` was modified.

**Nothing was added to `CI06U_REGISTER`.** The three findings above are not register candidates: `r` and `u` would make the gate fail on arrival, because the register asserts an entry names a duplicate the gate can currently find, and it cannot find rows it never parsed. They close through `S6`'s repair and `CI-06v`.
