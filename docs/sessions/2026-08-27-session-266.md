---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 266: P7 planned, and the phase turns out to have no head of its own

<!--index: **`P7` IS PLANNED AGAINST THE TREE AND ITS THREE DONE-GATES ARE EACH A REFUSAL**: a canary gate that fires when a detector finds NOTHING, a redaction gate that passes when a document OMITS something, and a breaker gate that passes when the breaker DECLINES to fire. **TWO OF P7'S SIX STATED CONTENTS ALREADY EXIST AS ROUTES THAT ANSWER NOTHING.** `GET /admin/flags`, `POST /admin/flags/:flagId/status` and `GET /admin/evidence/:accountId` are registered on the operator surface and `AdminReadSource` is a port nothing wires, on [`admin-reads.ts:621`](../../apps/api/src/routes/admin-reads.ts)' own words *"WHAT IS MISSING IS NOT AN AUTHORITY, IT IS A SHAPE"*. All five `SD-M7-nn` deltas are landed and every M07 table is registered in both `schema.ts` and `scope.ts`; **no file under `apps/worker/src` names a detector and `detector_definitions` has no seed**, so a run today could not record the parameters it ran under, which is `INV-M7-04`. **P7 HAS NO HEAD OF ITS OWN AND THAT IS THE FINDING**: the read shape is [`scoped-db.ts`](../../packages/db/src/scoped-db.ts), which is [`P5-a`](../plans/P5-payouts-and-wallet.md)'s file, and a detector is a JOIN and an AGGREGATE where `P5-a` is scoped to a range term, a null term and a row lock, so **the argument is written where `P5-a`'s author will read it rather than into a second slice on the one door**. **THE ONE PRECONDITION P7 DOES OWN IS `RI-08`**: `DB_ADMITTED` holds one name, `@merit/api`, and [CRON_INVENTORY:27](../ops/runbooks/CRON_INVENTORY.md) rows detector runs as a scheduled job, so the deployable that runs every detector cannot declare the accessor at all. **THREE DISPATCH CLAIMS DID NOT SURVIVE AND ALL THREE ARE REPORTED.** *"ADR-145, four writes"* is **seven**, and that entry's own first sentence reads *"in all seven admin writes"*. **The reserved condition is REFUTED**: [M06:300](../plans/M06-admin-ops-console.md), the batch 1 gate closure and [EC-071](../edge-cases/EC-071.md) each give all four audiences a redaction profile and collapse `internal`, `counsel` and `regulator` into one, so `GS-112` is writable as stated; `OQ-F4-03` is still open and asks a **different** question about the same column, whether an impersonation record appears in a regulator pack, which is counsel's. And [`0049:48`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql)'s disposition of `per_plan` **checked four of five fields**, leaving the CUSUM with no storage on a table whose primary key is `(plan_id, evaluated_on)` with `metric` outside it and no column a running `S_t` fits. **[ADR-155](../decisions/ADR-155.md) FIRED ON THE OTHER HALF OF THE SAME `P7` ROW**: `ADR-022`'s v1 tier says a hard link **auto-enforces**, `ADR-029` says it *"bans an account without human review"*, and [STATE_MACHINES:301](../architecture/STATE_MACHINES.md) says *"Binding: no automatic transition into `enforced`"* while being `approved`, dated six days later, and naming twelve dependencies of which `ADR-022` is not one. **RULED: the v1 tier names a LINK CLASS and "auto-enforce" is the EDGE written at the hard-link ceiling without review**, never a transition on the `risk_flags` machine. **`OQ-M7-05` is NOT answered and is the founder's**, and the counter-argument is recorded at full strength because the ruling has a real cost. **TWO FINDINGS REPORTED RATHER THAN REPAIRED**: `INV-M7-02`'s *"has no grant"* names a permission that does not exist, `0026` creating three roles of which one is usable and holding full DML on `risk_flags` with no revoke; and `GS-235`'s subject line *"v1 tier: a hard link auto-enforces"* would settle `OQ-M7-05` **by being written**. **`DELIVERY_PLAN` IS NOT EDITED AND THE FENCE'S OWN CONDITION IS WHY**: `grep` for `plans/P` over that document returns zero, so no phase row points at its plan and `P4` and `P5` both landed without one. Thirteen slices, four waves, fences BY FILE, collisions BY FILE against sessions 265 and 267. 33 of 33 gates, 11 of 11 invariants, `falsify.mjs` clean, `verify` exit 0, 169 files / 3,091 passed / 6 skipped and unmoved, because this session writes no code -->

- **Objective:** write [`docs/plans/P7-risk-and-abuse.md`](../plans/P7-risk-and-abuse.md), the phase plan for the detectors, the flags queue and the evidence packs. Not money by file.
- **Done:** the plan, twelve sections, thirteen slices over four waves with fences and collisions by file. [ADR-155](../decisions/ADR-155.md), whose CONDITIONAL fired on a different finding than the one reserved. [ALLOCATION](../decisions/ALLOCATION.md)'s `155` row amended in place, [INDEX](../INDEX.md)'s plan row, and the ADR registry regenerated.
- **Next:** **three slices are buildable today and ten are not.** `P7-a` (`apps/worker` admitted to `DB_ADMITTED`), `P7-b` (the contract's `audience` parameter) and `P7-d` (the `detector_definitions` seed) need nothing this tree lacks. Everything else waits on [`P5-a`](../plans/P5-payouts-and-wallet.md) for a read shape and on `ADR-125` for a fixture format. **The one thing this session could not do and should be done next is put section 3.1's argument into `P5-a`'s prompt before it runs**, because a `P5-a` that lands a range term and no aggregate blocks every wave-2 slice and makes the reach-around one line.
- **Blockers:** `OQ-M7-05` (the founder's, and `ADR-155` clause 3 refuses it deliberately); `OQ-M6-02`'s breaker minimum (the founder's, and it is a two-term conjunction against a one-scalar column); the CUSUM's home, which is a migration number this session may not take; the fixture format for all twelve of P7's golden scenarios.
- **Landmines:** **`apps/worker/src/index.ts` is a hand-maintained barrel and seven P7 slices edit it**, which is the collision [ADR-100](../decisions/ADR-100.md) removed one deployable over and which a keep-both merge does not catch, because a merged re-export list type-checks. **`CI-06r` reads "NOT GRANTED" as containing "GRANTED"** and failed on this entry's first approval line; the repair is [ADR-144](../decisions/ADR-144.md)'s wording, *"NOT GIVEN ... this line stays UNSIGNED"*, and it is a gate reading a substring rather than a verdict. **`decisions/README.md`'s ADR table is a generated span** (`adr_registry`) and a hand-written row there is reverted by the next `generate`; the row was written by hand, measured as inside the span, reverted and regenerated. **`admin-reads.ts` and `admin-writes.ts` both declare their data source as a PORT and neither is wired**, so a reader measuring "the flags queue exists" from the route table measures a surface that answers nothing.
- **Files touched:** [`docs/plans/P7-risk-and-abuse.md`](../plans/P7-risk-and-abuse.md), [ADR-155](../decisions/ADR-155.md), [ALLOCATION](../decisions/ALLOCATION.md), [decisions/README](../decisions/README.md) (generated span), [INDEX](../INDEX.md), [STATE](../STATE.md) (generated spans only), [sessions/README](README.md), this file.

---

### The count, honestly: one document, one ruling, and three of thirteen slices buildable

The dispatch asked for the count rather than a claim.

| | |
|---|---|
| Plan sections written | twelve, on [P5](../plans/P5-payouts-and-wallet.md)'s shape |
| Slices with a fence by file | thirteen, over four waves |
| Slices buildable against **this** tree | **three**: `P7-a`, `P7-b`, `P7-d` |
| Slices blocked on [`P5-a`](../plans/P5-payouts-and-wallet.md)'s read shape | **nine** |
| Slices blocked on a migration number | **one**, `P7-c` |
| Golden scenarios P7 needs | twelve, and **twelve** are `blocked / no-fixture-format`, including all three done-gates |
| ADR numbers this plan spends | **one**, its own. Five slices need one and the plan claims none of them |
| Migration numbers this plan spends | **zero**. `0050` is free at this commit and `P7-c` needs it |

---

### The three dispatch claims that did not survive, with what each one would have cost

| Cited | Verdict, and the cost of believing it |
|---|---|
| *"`ADR-144`, seven reads; `ADR-145`, four writes"* | **The reads are right and the writes are SEVEN.** `admin-writes.ts` declares `/freeze`, `/unfreeze`, `/close`, `/note`, `/flags/:flagId/status`, `/plans/:planId/versions` and `/plans/versions/:versionId/publish`, and the operator report registers all seven. [ADR-145](../decisions/ADR-145.md)'s opening sentence and its clause 1 both say *"seven"*. **Cost of believing four**: the flags-queue slice's fence would have omitted `POST /admin/flags/:flagId/status`, which is the write half of the queue P7 is being asked to build |
| *"`ADR-068`'s impersonation ruling faced the same question ... an evidence pack is where that open question becomes a table, and it is the likeliest place `ADR-155` fires"* | **REFUTED as stated, and the underlying question is real and different.** [M06:300](../plans/M06-admin-ops-console.md) reads *"`internal`, `counsel`, and `regulator`: full detail including detector internals"*; the [batch 1 gate closure](../decisions/gates/wave-3-batch-1-gate-closure-2026-08-14.md) reads *"The `regulator` audience follows the internal profile"*; [EC-071](../edge-cases/EC-071.md) transcribes it a third time. **Four audiences, two profiles, `GS-112` writable as stated.** `OQ-F4-03` asks whether an **impersonation record** appears in a regulator pack, which is a different question about the same column and is counsel's. **Cost of believing it**: a session would have spent `155` inventing a redaction profile that three approved documents already define, which is `ADR-041`'s refused fourth value arriving from the other direction |
| *"The API serves 54 registered routes"* | **CORRECT, and it is a union rather than a sum.** `public` registers 36, `operator` 19, and `GET /health` is on both, so 54 distinct. Taken from `CompositionReport.registered` as the dispatch instructed and not from a `grep` |

**Two claims the dispatch made that are exactly right and worth restating**, because a plan reader will lean on both: `admin_actions.reason` is `NOT NULL` under *"NO UNEXPLAINED ADMIN ACTION, EVER"* ([`0017:82`](../../packages/db/migrations/0017_events_and_audit.sql)), **with the qualification `ADR-145` finding 1 already measured, that `NOT NULL` admits `''` and `'   '`**; and `ADMIN_ORIGIN` is a placeholder whose real domain never enters the corpus ([ADR-012](../decisions/ADR-012.md)), and no slice below carries a hostname.

---

### `DELIVERY_PLAN` is not edited, and the fence's own condition is the reason

The fence granted [DELIVERY_PLAN](../DELIVERY_PLAN.md) **only if its `P7` row must point at the new plan**. It must not:

```
$ grep -c 'plans/P[0-9]' docs/DELIVERY_PLAN.md
0
```

**No phase row in that document points at its phase plan**, and [P4](../plans/P4-portal-and-site.md) and [P5](../plans/P5-payouts-and-wallet.md) both landed without one. Adding a pointer for `P7` alone would make the section 4 table inconsistent in the direction that looks like a repair, so the conditional does not fire and the file is untouched.

---

### Verification

| Check | Result |
|---|---|
| `node scripts/corpus/gates.mjs generate && check` | **33 of 33** |
| `node packages/tooling/checks/repo-invariants.mjs` | **11 of 11** |
| `node scripts/corpus/falsify.mjs` | exit **0**, clean |
| `pnpm run verify` | exit **0** |
| `pnpm vitest run` | **169 files, 3,091 passed, 6 skipped**, unmoved from `main`, because this session writes no code |
