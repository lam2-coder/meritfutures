---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 624: the two reds are repaired at the fixture and not at the constraint, the repair is proved to still be under the foreign key it satisfies, and the reason a test could stay red in silence is that the only job with a database runs no vitest

<!--index: **THE ROW IS [ADR-429](../decisions/ADR-429.md), `proposed` and UNSIGNED. NON-MONEY DIFF, MONEY-ADJACENT SUBJECT (the two repaired cases are the admin audit trail), `E2` READ OWED. ONE FILE CHANGED AND IT IS THE ONE TEST FILE THE FENCE NAMES. NO MIGRATION, NO WORKFLOW, NO `src/` FILE, NO GATE ADDED OR WEAKENED, NO `RI-nn` MINTED, NO `CI-06` LETTER SPENT, AND NO TEST SKIPPED, DISABLED, QUARANTINED, RENAMED OR RETARGETED.** **BOTH REDS WERE REPRODUCED BEFORE ANYTHING CHANGED**, on PostgreSQL **16.13** with **75 of 75** migrations applied forward-only from empty under `ON_ERROR_STOP=1` through [`corpus.yml:220-226`](../../.github/workflows/corpus.yml)'s loop verbatim in **3.48s**, `CI-06h`'s own count assertions re-run and agreeing at **118 tables / 32 triggers / 414 indexes / 518 checks**: `Tests 2 failed \| 36 passed \| 1 skipped (39)`, which reproduces [ADR-428](../decisions/ADR-428.md) section 6 term for term. **A FIX THAT COULD NOT BE SHOWN FAILING FIRST WOULD NOT HAVE BEEN REPORTED AS A FIX.** **[ADR-428](../decisions/ADR-428.md)'s CAUSE CLAIM WAS RE-DERIVED AT EVERY LOAD-BEARING JOINT AND HOLDS.** The foreign key is at [`0073:274-277`](../../packages/db/migrations/0073_operator_directory.sql); RED 2 names it in its own output; **RED 1 does not, and was proved rather than inferred** by three measurements: the route maps `23503` to exactly that `400` at [`admin-writes.ts:858-862`](../../apps/api/src/routes/admin-writes.ts), the statement the route issues is refused by that key when run directly through `psql`, and the identical statement returns `INSERT 0 1` once the directory row exists. `NOT NULL` firing before a referential trigger re-measured and holding. [`ADR-237:167`](../decisions/ADR-237.md) and [`:183`](../decisions/ADR-237.md) read at source, and `grep -l "INSERT INTO admin_actions" scripts/db/*.sql` returns **exactly the two files ADR-237 repaired and no third**. **ONE OF ADR-428'S FIGURES NAMES TWO DIFFERENT INTERVALS AND THIS ROW SEPARATES THEM.** The RECORD went stale over **28** migrations, 47 to 75; the RED itself stood for **8 migrations and 9 days**, because `0073` first appears at `7e1a05e9` on **2026-08-29** with **67** migration files in the tree. **And the block was ALREADY 20 migrations past its last recorded run on the day it went red**, `git show 7e1a05e9:apps/api/test/admin-writes.test.ts` carrying the same 47-migration line. **The correction is one of degree and it makes the finding worse: the red had 8 days in which nothing could have caught it and would have had 800 for the same reason.** **THE REPAIR IS TWO ROWS IN `operators` INSIDE `seed()`** at [`admin-writes.test.ts:754-755`](../../apps/api/test/admin-writes.test.ts), which is [ADR-237](../decisions/ADR-237.md)'s own ruled remedy (*"The repair is the fixture and never the constraint"*) applied to the caller it missed. **Two actors and no third, derived from the block**: `sso:owner@merit` from [`:841`](../../apps/api/test/admin-writes.test.ts) and `sso:probe@merit` from [`:974`](../../apps/api/test/admin-writes.test.ts). **Idempotent because `operators.actor` is UNIQUE at [`0073:78`](../../packages/db/migrations/0073_operator_directory.sql), `seed()` runs per case and the transaction COMMITS**, proved by a second run against the same already-populated database coming back **33 of 33**. `idp_issuer` and `idp_subject` left NULL, which is [`0073:116-121`](../../packages/db/migrations/0073_operator_directory.sql)'s operator-who-cannot-sign-in and ADR-237's own choice for the two `.sql` probes, **so the three callers now agree with each other**. **AND THE ROWS GENUINELY LAND**: `select count(*) from admin_actions` returns **2** after the run, so the success half passes because a row that could not exist now exists. **THE REPAIR IS PROVED TO SATISFY THE CONSTRAINT RATHER THAN HIDE IT, AND THAT IS THE HALF A FIXTURE REPAIR USUALLY LEAVES OUT.** Seeding an operator turns both reds green; so would DELETING the foreign key. **One case is added** at [`:988`](../../apps/api/test/admin-writes.test.ts) using `sso:ops@merit`, which `ACCOUNT_ACTION_ROLES` at [`admin-writes.ts:159`](../../apps/api/src/routes/admin-writes.ts) admits and `seed()` deliberately does not write, so it clears the role gate, clears `NOT NULL`, clears 0043's biconditional, reaches the key and the key answers. **It asserts its own precondition** at [`:998-1003`](../../apps/api/test/admin-writes.test.ts), so a later row that seeds every role gets a red rather than a silent inversion. **THE STRUCTURAL GAP IS FOUR GREPS AND IT IS THE MORE VALUABLE HALF.** `grep -rn "DATABASE_URL" .github/workflows/` returns **nothing, exit 1**; there are **four** workflow files; `grep -rn "services:"` returns **exactly one match**, [`corpus.yml:163`](../../.github/workflows/corpus.yml), which is `CI-06h`'s; `grep -rn "vitest run"` returns **four invocations and none of them is in that job**. **So the set of jobs with a database and the set of jobs that run TypeScript assertions are DISJOINT, and nothing in the tree says so.** The property that actually held when `0073` landed was not *schema changes are checked against their callers* but *schema changes are checked against their `.sql` callers*. **Two further layers named**: the block did not fail, it SKIPPED, and a skip is green, while the count that would carry the warning is bound to nothing (`grep -rn "10 skipped"` over `scripts/`, `packages/tooling/`, `.github/` and `docs/STATE.md` returns nothing); and the hand-run record at [`admin-writes.test.ts:21-22`](../../apps/api/test/admin-writes.test.ts) is **a measurement with no expiry date**, true when written and reading exactly as confident 28 migrations later. **NO INVARIANT IS MINTED FOR ANY OF THE THREE AND THE RESTRAINT IS DELIBERATE**, on ADR-428 section 12 item 3's ground that a static approximation would pass while the property it names is false. **NO CI WIRED**, as dispatched; what this row would wire is stated and stopped at. **ADR-428 SECTION 12 ITEM 1'S SEQUENCING WORRY IS ANSWERED AND THE ANSWER IS THAT IT DOES NOT ARISE**: the seed and the move to `*.integration.test.ts` are not the same edit, a move carries the repaired `seed()` with it, so taking the seed first costs the moving row nothing. **FOUR WHOLE-SUITE RUNS, TWO ON THE UNTOUCHED BASE**, the repair stashed and restored with `git status --porcelain` read at each boundary: base without `DATABASE_URL` **341 files / 8,264 passed / 10 skipped / 0 failed**; base with it **8,270 / 2 / 2 FAILED**; repaired without it **8,264 / 11 / 0**; repaired with it **341 files / 8,273 passed / 2 skipped / 0 failed**. **The total moves by exactly one and it is the new case**, 8,274 to 8,275 in both columns. **Without a database the new case is a skip and the passing count does not move at all, which is the honest statement of this repair's cost to CI today: it adds one more thing CI does not run.** **ADR-428's 9-to-8-plus-1 exchange reproduces exactly**, [`pool-executor.test.ts:79`](../../packages/db/test/pool-executor.test.ts) switching OFF when a database appears. **GATES ARE 33 OF 33 ON THIS BASE WHERE ADR-428 MEASURED 32 OF 33, AND THAT IS NOT A REPAIR**: its `CI-06f` red over holes at 425, 426 and 427 predicted *"it closes for free either way"*, and they landed on `claude/wave-425s` and it closed for free. Counts derived at reporting time: gates **33 of 33** and invariants **35 of 35**, with `CI-06/table-row-width` red on the first run over two unescaped `\|` characters inside a quoted vitest total and **repaired in the rows rather than in the gate**; `typecheck`, `lint` and `format:check` all exit 0. **`pnpm run verify` was NOT run and `scripts/corpus/falsify.mjs` was NOT run**, both forbidden by the row, and `verify` chains `falsify` so running the first would have breached the second through it. **THE CONTAINER ARRIVED WITH NO REPOSITORY, SEVENTEEN CONSECUTIVE ROWS, AND THE GIT PROXY REFUSED THE FIRST TWO PUSHES.** Both are the dispatcher's and this row only measures them. -->

### 1. The objective

Apply [ADR-428](../decisions/ADR-428.md) section 9's priced repair, prove it, and leave the tests honest. **Reproduce the two reds first and record the verbatim failing output before changing anything**; re-derive ADR-428's cause claim at primary source rather than taking it; apply the smallest repair that makes the two cases pass honestly, and check that it is not a fixture hiding the constraint; re-run and record the verbatim passing output plus whole-suite totals with and without `DATABASE_URL`; and write the entry, whose more valuable half is why a test could stay red in silence at all. **Wire no CI, weaken no gate, skip no test.**

### 2. Done

- **The container measured and reported.** No repository on arrival; `/home/user` held `.` and `..`. Cloned by this row, `pnpm install --frozen-lockfile` exit 0. **The git proxy refused the first two pushes** with *not in this session's authorized repository set*; the documented remedy was applied and the same ref then pushed unchanged.
- **The base measured on the UNTOUCHED tree at `d915dc14`**, not inferred from the repaired one: **341 files / 8,264 passed / 10 skipped / 0 failed** with no `DATABASE_URL`, **8,270 / 2 / 2 FAILED** with one, gates **33 of 33**, invariants **35 of 35**.
- **PostgreSQL 16.13 stood up and 75 of 75 migrations applied forward-only from empty** on `CI-06h`'s own loop verbatim in **3.48s**, zero errors, and `CI-06h`'s object-count assertions re-run against it and agreeing at **118 tables / 32 triggers**. **Three separate databases**, one per measurement, each built from empty, because the success cases COMMIT.
- **Both reds reproduced before the first edit**, verbatim transcript in [ADR-429](../decisions/ADR-429.md) section 4.
- **The cause re-derived rather than taken**, including the half ADR-428 did not have to prove: RED 1 carries no constraint name in its own output and was closed by three measurements. [ADR-429](../decisions/ADR-429.md) section 5.
- **The repair applied**, two `operators` rows inside `seed()`, idempotent and measured so; [ADR-237](../decisions/ADR-237.md)'s ruled remedy applied to the caller it missed.
- **The repair proved to be under the constraint it satisfies**, by one added case that goes red if the foreign key ever goes away.
- **Four whole-suite runs recorded**, two on the base and two on the repair, so the numbers are comparable.
- **The structural gap derived in four greps** and stated as the row's larger finding. [ADR-429](../decisions/ADR-429.md) section 10.
- **The interval separated into three**, correcting a figure ADR-428 uses for two different things. [ADR-429](../decisions/ADR-429.md) section 9.

### 3. Not done, and why

- **NO CI WIRED.** ADR-428 section 10 derives the smallest safe wiring and notes that a naive service block on `CI-02` would turn a currently-passing case OFF. That decision is the founder's with both entries in front of them, and the dispatch reserved it.
- **NO INVARIANT MINTED FOR THE SILENT-CALLER CLASS.** The honest control is a job that runs those callers against the schema. A static check approximating it would pass while the property it names is false, which is the failure this corpus has paid for most often.
- **NO SIBLING ENTRY EDITED, AND NO STALE CITATION REPAIRED.** [ADR-428](../decisions/ADR-428.md) section 13 items 3 and 4 are still open. The hand-run record at [`admin-writes.test.ts:21-22`](../../apps/api/test/admin-writes.test.ts) was left exactly as found, because updating it would restart the same clock; the question of whether such a record should exist at all is in the approval block.
- **NO FILE OWNED BY A CONCURRENT ROW TOUCHED.** `apps/worker/**`, `packages/db/src/**`, `packages/db/test/**` and `packages/tooling/checks/absence-claims.mjs` were read and not changed.
- **NO PULL REQUEST OPENED.** The wave is integrated by the dispatcher.
