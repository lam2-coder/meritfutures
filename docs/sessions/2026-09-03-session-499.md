---
status: approved
depends_on: [README.md]
last_updated: 2026-09-03
---

## 2026-09-03 - Session 499: subject() answers whole, because state reads the row the worker wrote

<!--index: **THE RULING IS [`ADR-308`](../decisions/ADR-308.md), `proposed` and UNSIGNED, `E2` READ OWED AND NOT PERFORMED. MONEY PATH, AND IT HAS A `src/` DIFF.** [ADR-287](../decisions/ADR-287.md) SLICE 5 IS BUILT AND IT IS THE LAST OF `subject()`: [ADR-306](../decisions/ADR-306.md) left exactly two things refusing by name in one throw, `subject.state` and `subject.plan.size`, and both are built, so `PayoutTx.subject(accountId)` RETURNS A `PayoutSubject` against a seeded database and FIVE of the EIGHT members now answer. **NOTHING IS SYNTHESISED AND THE RULE MOVED OUT OF A UNION INTO THE RETURN TYPES**: `SubjectLeg<T>` had no producer of its `ok: false` arm once both legs were built and no case could ever red the refusal block below it, so the type is RETIRED and `stateLeg` answers a `RuleState` or throws while `planLeg` answers a `ResolvedPlan` or throws. **THE ROW'S STOP CONDITION NAMED THAT TYPE AND THIS ROW DELETED IT**, which is the one place this session read its instruction against its substance; the property is preserved, the deletion is asserted in the suite, and [ADR-308](../decisions/ADR-308.md) section 9 raises it as the FIRST thing a founder read adds rather than certifying it. **`state` READS AND DOES NOT FOLD**, on `INV-M5-02` and [ADR-239](../decisions/ADR-239.md), because a request-path fold would answer every case here correctly and is exactly the divergence [ADR-026](../decisions/ADR-026.md) `C-07`'s `state_hash` exists to detect on the one path no replay audit reads; the day is `ScopedTx.lastClosedTradingDay()` on [ADR-268](../decisions/ADR-268.md), READ BEFORE THE ROWS IT SELECTS because `R-06` is the selection, and a case seeds two days and watches the member read the one the DOOR gave. **THE READ ORDER IS NOW LOAD BEARING RATHER THAN TIDY**: [ADR-285](../decisions/ADR-285.md) ruling 4 requires the ownership answer first because an empty scoped read IS `RuleStateAbsent`, so a state-first implementation would hand a prober 503 where section 1 requires 404, and until this row that hazard was hypothetical. **THE SIZE DECODE RUNS ON [ADR-303](../decisions/ADR-303.md)'s TYPED DOOR, AS ITS FIRST PRODUCTION CALLER**, which is what [ADR-286](../decisions/ADR-286.md) section 5 named as the remedy and why row `303` ran ahead of this one; the mapping onto the engine's `PlanVersionSizeRow` is written at the call, the one `jsonb` goes to the engine's `decodeCapScheduleCents`, and EVERY MONEY COLUMN IS STILL CHECKED AT RUN TIME on that door's own limit 1. **THE ABSENT-SIZE-ROW REFUSAL [ADR-306](../decisions/ADR-306.md) NAMED AS UNWRITTEN IS WRITTEN AND THE GROUND IS THE DDL**: there is NO composite foreign key from `accounts` to the grid, so an unresolvable pair is a `PayoutRowError` and a 500 and never a neighbouring rung. **FIVE SEEDED DEFECTS WATCHED RED AND RESTORED**: state-before-ownership reds 6, a cast cap schedule reds 2, stripping the money checks reds 1, the latest-row day reds 5, the blanket refusal reds 30. **ONE ASSERTION BEYOND THE TWO THE ROW NAMED MOVED, IN A FILE THE ROW PUT INSIDE THE FENCE**: the `resolvePlan(` census required NO `apps/api` producer and its own comment predicted this row exactly; it is INVERTED at three, not deleted. **[`wiring.test.ts`](../../apps/api/test/wiring.test.ts) IS UNTOUCHED AND ITS `usePayoutBackend` REASON IS NOW STALE IN ONE CLAUSE** (the size row is discharged and the refusing members are four, not five); no assertion reds, and repairing it belongs to SLICE 9, whose fence must name that file and `rule-state-producibility.test.ts` up front. **NO MIGRATION WRITTEN OR NUMBER RESERVED, NO ROUTE BEHAVIOUR, NO ENGINE FILE, NO `packages/db` FILE, NO NEW PACKAGE, NO KEY ADMITTED TO `CATALOG_TABLE_KEYS`, NO READ VERB ADDED, NO DOOR WIDENED, NO GATE WEAKENED, NO CASE DELETED OR SKIPPED OR RELAXED, NO CAST ONTO AN UNVALIDATED ROW, NO SYNTHESISED DEFAULT, NO FLOAT, NO `RI-nn` MINTED, NOTHING SIGNED. THE WIRED COUNTS DO NOT MOVE: TEN WIRED OF TWENTY-FOUR DECLARED, FOURTEEN BLOCKED.** Counts derived at reporting time, each command run separately: **33 of 33** gates after `generate`, **32 of 32** invariants off the runner's own last line, **302 files / 7,372 passed / 6 skipped** against a baseline of **302 / 7,358 / 6**, a delta of **exactly fourteen cases and no new file**, all fourteen in `payout-backend.test.ts`. `typecheck` clean, `lint` clean after one `no-loss-of-precision` repair, `format:check` clean after one prettier reflow. **`pnpm run verify` was NOT run**, forbidden by the row, and `falsify.mjs` was never invoked by any path. **NO GITHUB ACCESS: THE BRANCH IS PUSHED AND NO PULL REQUEST WAS OPENED BY THIS SESSION.** -->

---

### 1. The objective

[ADR-287](../decisions/ADR-287.md) slice 5, which COMPLETES `subject()`: the `state` leg and the size decode inside `plan`, against a seeded fixture, with nothing synthesised and with `start.ts` still not calling `usePayoutBackend`.

### 2. What was done

| File | Change |
|---|---|
| [`apps/api/src/payout-backend.ts`](../../apps/api/src/payout-backend.ts) | `stateLeg` added; `planLeg` reads the size row and returns a `ResolvedPlan`; `SubjectLeg<T>` and `STATE_LEG` **retired**; four value-level helpers added (`centsOf`, `centsOrNullOf`, `boolOf`, `textOf`). New imports: `ruleStateOn`, `resolvePlan`, `decodeCapScheduleCents`, `PlanVersionSizeRow` |
| [`apps/api/test/payout-backend.test.ts`](../../apps/api/test/payout-backend.test.ts) | The fixture gains `ruleStates`, `planVersionSizes` and `lastClosedTradingDay()`; two `describe` blocks rewritten and one added, **+14 cases** (48 to 62) |
| [`apps/api/test/rule-state-producibility.test.ts`](../../apps/api/test/rule-state-producibility.test.ts) | The two needles retired and replaced by three property assertions; the `resolvePlan(` census inverted at three. Section 5 |
| [`docs/decisions/ADR-308.md`](../decisions/ADR-308.md) | New. Nine sections, `proposed`, UNSIGNED |
| [ALLOCATION](../decisions/ALLOCATION.md), [STATE](../STATE.md), the two registry READMEs, this file | The records |

### 3. The two legs

`state` reads `ScopedTx.lastClosedTradingDay()` and then `rowsWhere('ruleStates', { accountId })`, and hands both to `ruleStateOn`. **The day is read first because `R-06` is the selection**, and the predicate omits the day on `eligible-next-7d.ts:817`'s precedent one door over, where the selection belongs to the function that owns `R-06`. Both reads are on the payout transaction: `ruleStates` is `derived` via `accounts` and the calendar door runs on the same `source`.

`plan` reads the size row at `{ planVersionId, sizeCents }`, which is `plan_version_sizes_version_size_uq`'s own column pair, maps it onto the engine's `PlanVersionSizeRow` and calls `resolvePlan(rules, size)`. **The mapping is an accepted `FM-16` that [ADR-303](../decisions/ADR-303.md) limit 2 declares in advance**, and the one `jsonb` is the engine's `decodeCapScheduleCents` rather than a fourth statement of it.

### 4. Falsification, watched rather than asserted

| Seed | Red cases |
|---|---|
| `stateLeg` moved ahead of the `accounts` read | 6 |
| the cap schedule cast instead of decoded | 2 |
| every money check stripped off the typed catalogue row | 1 |
| the day taken from the latest `rule_states` row | 5 |
| the member collapsed back to the blanket refusal | 30 |

Each was restored byte for byte and the pair of suites is green at **93** cases. **The third row is one case and is reported as one**: with the typed door in place `tsc` holds the shape and only the runtime check holds the value.

### 5. The assertion beyond the two the row named

The row put [`rule-state-producibility.test.ts`](../../apps/api/test/rule-state-producibility.test.ts) inside the fence up front, for the two needles. **A THIRD assertion in that file tracks the same remainder and this diff necessarily reds it**: the `resolvePlan(` census required the producer list to hold at two, and its own comment reads *"a third entry appearing under `apps/api/src` is this port's `plan` half landing, and it turns this case red so the entry above cannot stay as it is."*

**It was written to break here, so it is inverted rather than deleted**, held at exactly three with an added assertion that the one `apps/api` producer is the adapter and not a route. This is reported rather than buried: the row named two needles and the tree carries three assertions of the remainder, which is a measurement [ADR-308](../decisions/ADR-308.md) section 9 hands to the founder alongside [ADR-306](../decisions/ADR-306.md) section 8's open question about that file.

### 6. Landmines

- **[`wiring.test.ts`](../../apps/api/test/wiring.test.ts)'s `usePayoutBackend` reason is STALE in one clause and no assertion reds.** It reads that what stands is the size row's decoding and the FIVE refusing members; the first is discharged and the second is four. **Slice 9's fence must name that file and `rule-state-producibility.test.ts` up front.**
- **The stop condition named `SubjectLeg<T>` and this row deleted it.** Section 2 of this log and section 9 of the entry. Restoring it is a visible diff, because the deletion is asserted.
- **`decidePayout` can now run end to end and nobody has run it.** `holdFlag` still refuses and is slice 8, whose supplier is COUNSEL, so the route is closer to live than the wired count suggests and is still not installable.
- **The `db-recorder.ts` consolidation debt grew by one verb**: this row's fixture now also answers `lastClosedTradingDay()`.
- **[ADR-303](../decisions/ADR-303.md)'s one owed line** (re-exporting `CatalogRow` from `packages/db/src/index.ts`) was **not needed** and is still unspent.

### 7. Blockers

None for this row. [ADR-287](../decisions/ADR-287.md) slice 8 remains unschedulable: `HoldFlag.tosClause` has no value space in this repository and `DEP-M7-05` owes the clauses to counsel.

### 8. Verification, each command run separately

| Command | Result |
|---|---|
| `node scripts/corpus/gates.mjs generate` | every span already matches its query |
| `node scripts/corpus/gates.mjs check` | **33 of 33** gates pass |
| `node packages/tooling/checks/repo-invariants.mjs` | **32 of 32** invariants hold |
| `pnpm install` | clean |
| `pnpm vitest run` | **302 files / 7,372 passed / 6 skipped**, against a baseline of 302 / 7,358 / 6 |
| `pnpm run typecheck` | clean |
| `pnpm run lint` | clean, after one `no-loss-of-precision` repair in the new cap-schedule case |
| `pnpm run format:check` | clean, after one prettier reflow |

**The skip count did not move.** `pnpm run verify` was NOT run, forbidden by the row, and `scripts/corpus/falsify.mjs` was never invoked by any path.

### 9. Files touched

**[INDEX](../INDEX.md) IS NOT AMONG THEM AND THAT IS THE CONVENTION RATHER THAN AN OMISSION.** That file indexes registries, and its own `decisions/` and `sessions/` rows say entry files are *"fragments rather than corpus documents and are indexed by that README under `CI-06n`, not by this row"*. No corpus document was added here, and `gates.mjs generate` reports every span already matching its query.

`apps/api/src/payout-backend.ts`, `apps/api/test/payout-backend.test.ts`, `apps/api/test/rule-state-producibility.test.ts`, `docs/decisions/ADR-308.md`, `docs/decisions/ALLOCATION.md`, `docs/decisions/README.md`, `docs/STATE.md`, `docs/sessions/README.md`, `docs/sessions/2026-09-03-session-499.md`.

**NO GITHUB ACCESS. The branch is pushed and no pull request was opened by this session.**
