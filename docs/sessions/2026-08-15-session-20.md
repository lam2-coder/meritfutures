## 2026-08-15 - Session 20: OQ-P1-04 ruled, the P1 scaffold plan approved
<!--index: OQ-P1-04 ruled, the P1 scaffold plan approved -->

- **Done.** The one question blocking S-B is ruled and folded, and [P1-monorepo-scaffold](../plans/P1-monorepo-scaffold.md) is `approved`. The branch was rebased onto `main` first: PR #9 merged after it was cut.
  - **OQ-P1-04 ruled option A, with a structural amendment that is part of the ruling rather than a note.** CI-06b's document set was `^(docs|research|packages)/` and CI-06c's was `^(docs|research)/` plus `DELTA_MANIFEST` by name, ten lines apart in the same runner. **The instruction was explicitly not to narrow CI-06b's regex to match CI-06c's**: two expressions of one concept that agree today is how the defect was born. One `isCorpusDocument` predicate now lives in [`gates.mjs`](../../scripts/corpus/gates.mjs) and **both gates call it**.
  - **C disqualified on evidence, and the evidence is one file wider than the ruling stated.** `docs/legal/README.md` and `docs/ops/runbooks/README.md` were named; **`research/calibration/README.md` is a third**, also `status: approved` with an INDEX row. A filename exemption would have silently stopped checking three corpus documents. **B rejected** for making a gate green by making its status field meaningless.
  - **[`falsify.mjs`](../../scripts/corpus/falsify.mjs) gains a SCOPE phase.** `SEEDS` proves a gate **can** fail; it cannot prove the gate reads the right set of files, which is the whole of what OQ-P1-04 was. Two cases, one per direction: `CI-06b/out` asserts a file under `packages/` with no frontmatter is **not** a finding, `CI-06b/in` asserts a file under `docs/` with no frontmatter **is**. Exit is non-zero unless both land on the side of the boundary they name.
  - **Both scope cases were watched fail before they were trusted.** Against the pre-ruling regex `CI-06b/out` reports `READ A FILE IT MUST NOT` and names `packages/rules-engine/README.md`; against a predicate narrowed until it no longer reads `docs/`, `CI-06b/in` reports `DID NOT FAIL` and the run exits 1. **Eleven gates pass clean and fail dirty, and thirteen assertions now hold.**
  - **`STRATEGY` was not edited and no ADR was opened.** Its CI-06b row reads "every document" and the ruling settles which document class that always meant. Changing a frozen document requires an ADR; not changing one requires nothing.
- **Next.** **S-A**: the migration number allocation table as ADR-036, a second table beside the ADR allocation in [DECISIONS](../decisions/README.md). Then **S-B**, the scaffold, carrying all three riders. Neither was begun in this session and the instruction was explicit that they must not be.
- **Blockers.** None for P1 planning. The founder's E2 read on the 18 money-path migration files is unchanged and still gates the schema branch.
- **Landmines.**
  - **THE RULED PREDICATE GIVES UP ONE CHECK AND IT IS WORTH A ONE-LINE RULING.** `isCorpusDocument` excludes `docs/INDEX.md`, because CI-06c skips it: INDEX cannot carry a row pointing at itself. **CI-06b checked INDEX's frontmatter before this session and now nothing does.** INDEX is `status: approved` today and a hand-edit to `status: nearly` would pass every gate in the runner. The fix that keeps the coverage is a second expression inside CI-06b, which is precisely what the amendment exists to forbid, **so the loss is recorded rather than quietly traded back.** A third gate reading the predicate, or a one-line exception the founder rules explicitly, are the two honest ways out.
  - **The by-name allowlist inside the predicate is a hand-maintained list**, which is [ADR-034](../decisions/ADR-034.md)'s own drift class, sitting inside the fix for a drift. One entry, `packages/db/DELTA_MANIFEST.md`. **The expiry is written as a comment in the runner rather than only in a plan: if it reaches three it needs a rule instead of a list.** Nothing enforces that threshold.
  - **`SCOPE_CASES` has no completeness check and `SEEDS` does.** A new gate with no seeded violation is a hard error in `falsify.mjs`; a new gate with no scope case is silent. That is deliberate, because a scope case per gate would be eleven cases nobody chose, but it means **the scope discipline is opt-in and will be remembered only where a boundary has already been argued about.** The next boundary argument has to add its own case.
  - **The push needed `--force-with-lease`.** The branch was cut before PR #9 and the rebase rewrote its one commit. The remote held exactly the pre-rebase commit and nothing else, checked before the push rather than after.
  - **A plan cannot be approved and still describe its own blocker.** Section 4's definition of done and section 6's S-B dependency row both named OQ-P1-04 as open, and flipping only the frontmatter would have left an `approved` document contradicting itself in two places. Both were re-tensed with the ruling.
- **Files touched:** `scripts/corpus/gates.mjs` (the predicate, CI-06b and CI-06c call sites, CI-06b's `covers`); `scripts/corpus/falsify.mjs` (`SCOPE_CASES`, the scope phase, `copyTree` extracted); `docs/plans/P1-monorepo-scaffold.md` (status, sections 4, 6 and 7); `docs/{STATE,INDEX,SESSION_LOG}.md`. **No `.sql` file was edited and no frozen document was changed.**

---
