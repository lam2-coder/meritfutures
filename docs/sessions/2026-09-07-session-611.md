---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 611: B5 term 1 is spent, the door adds no reach, and the first dispatch premise in this chain to survive the row it was given to

<!--index: **THE ROW IS [ADR-416](../decisions/ADR-416.md), `proposed` and UNSIGNED. MONEY PATH, THE ADMIN LIABILITY READ, `E2` READ OWED. `B5` TERM 1 IS SPENT.** [ADR-413](../decisions/ADR-413.md) section 12 item 1 said a row holding `packages/db` AND `apps/api/src/admin-source/**` executes the term; this row held both and executed it. **THE DISPATCH ASKED FOR A THIRD CORRECTION AND THERE IS NOT ONE**: every claim was re-derived at source and [ADR-413](../decisions/ADR-413.md)'s framing is correct in every checkable part, which is the first time in this chain a dispatch premise survived the row it was given to. **AND [ADR-411](../decisions/ADR-411.md) TURNS OUT TO HAVE BEEN RIGHT ABOUT THE PRICE AND WRONG ONLY ABOUT THE ORDER**: with the door in place the term really is one export and one call, and what was missing was never a refactor and never a decoder but a HANDLE for the call to be made on. **THE DOOR ADDS NO REACH AND THAT IS MEASURED RATHER THAN ARGUED**, which is the measurement [ADR-413](../decisions/ADR-413.md) did not make: `systemTx.rowAt` and a catalogue read at the same address run `refuseUnaddressed`, `unscopedAddressPredicate`, `selectStatement` and `oneOrNone`, the same four functions, so the SELECT is identical; what the new verb adds is `refuseUncatalogued`, which NARROWS the vocabulary from every registered table to five, and a declared return type. **SO IT IS [ADR-303](../decisions/ADR-303.md) LIMIT 4 TAKEN FOR ONE VERB ON ONE HANDLE**, and a case drives both verbs over every `CATALOG_TABLE_KEYS` member and asserts the SQL and the binds are identical. **`catalogRows` AND `catalogRowsWhere` ARE REFUSED** on `ScopedTx`'s own rule that a primitive admitted before a caller exists is a primitive nobody can remove, held by a case asserting the system handle carries exactly one catalogue verb. **THE EXPORT IS [ADR-413](../decisions/ADR-413.md) SECTION 7's OWN NAMED WINNER**, the split at `planLeg`'s first seam, taken once the handle objection moved: `resolvePinnedPlan(handle: CatalogReadTx, planVersionId, sizeCents)` with `planLeg` as its `accounts`-row caller, still unexported. **NO SECOND `PlanVersionSizeRow` MAPPING WAS WRITTEN**, which the dispatch refused and a derived census still holds at ONE file, because the second caller reaches the FIRST caller's statement instead of writing its own; the seventy-six existing `payout-backend` cases pass unmoved across the split, which is the evidence it is a move and not a rewrite. **TEN SEEDS WATCHED RED AND ALL TEN REVERTED**, including a money seed that maps `drawdown_cents` from `size.bufferCents`, invisible to `tsc` because both are `bigint`, RED at `expected 50000n to be 250000n`. **ONE SEED FAILED TO FALSIFY ITS CASE AND IS REPORTED**: swapping `unscopedAddressPredicate` for `unscopedFilterPredicate` left the equivalence case GREEN because the two render the same SQL at these addresses, so a second seed was written; a case believed on a seed that could not have failed is a case nobody has tested. **THREE STRUCTURAL DECISIONS WERE FORCED BY CHECKS THAT READ `scoped-db.ts` AS TEXT, AND ALL THREE WERE MEASURED RATHER THAN GUESSED.** `extends TxCommon, CatalogReadTx` states the signature once and was written first; it unanchored THREE cases in `apps/worker/test/`, one of them a non-vacuity guard, so the member is declared inline and the signature is stated three times with a COMPILE-TIME WITNESS holding it. A probe inserted twenty filler lines and ran the invariants: exactly ONE citation broke out of 366 into that file, `RI-15` did not fire at all, so every insertion is an append after the last cited line. And QUOTING the interface's opening line in a docblock broke THREE MORE checks in `packages/ledger`, `packages/enrichment` and `apps/api`, which read the body by counting braces, so the quotation is gone and the lesson is in the file. **ONE CONTROL WAS NARROWED AND IT IS ARGUED RATHER THAN SLIPPED IN**: `admin-source-flags.test.ts` forbade ANY `@merit/db` import under the directory while its own stated reason was capability, and `apps/api/test/db.test.ts` rules in terms that a type-only import is not pinned "because a type buys no capability"; the property with teeth, that no module takes a VALUE off the accessor, is unchanged, and a second case pins every type-only accessor import as a LIST. **TERM 2 STANDS AND `readLiability` IS STILL NOT COMPOSED**: `IMPLEMENTED_ADMIN_READS` is unchanged, the wiring triple does not move, the group goes whole or not at all (`EC-074`), and the two files term 2 would move in are FORBIDDEN to this row. **`docs/architecture/API_CONTRACT.md`, `apps/api/src/routes/admin-reads.ts` AND `apps/admin/` ARE EMPTY IN `git diff`. `packages/db/migrations/` IS UNTOUCHED, NO MIGRATION NUMBER RESERVED OR TAKEN, NO GATE ADDED OR WEAKENED, NO TEST SKIPPED, DISABLED OR QUARANTINED, NO FIGURE INVENTED, NO DEFINITION WIDENED.** **THE CONTAINER ARRIVED WITH NO REPOSITORY, THIRTEEN CONSECUTIVE ROWS**: `/home/user` held `.` and `..`, cloned in **18s**, `.git` **191M**, `pnpm install --frozen-lockfile` **11.2s**; thirteen rows is no longer an anecdote and the fix belongs in the image or the sentence belongs out of the template. Counts derived at reporting time: **33 of 33** gates after `generate`, **35 of 35** invariants, and a base reproduced on `de5f90b5` before the first edit. **`pnpm run verify` was NOT run and `falsify.mjs` was NOT run**, both forbidden by the row. -->

### 1. The objective

Take [ADR-413](../decisions/ADR-413.md) section 12 item 1, the `packages/db` door that entry measured `B5` term 1 to need and could not take because it held neither package. Re-derive the framing at source first, because two consecutive rows had already corrected their dispatch on this exact item and the dispatch said a third correction would be a fine outcome. Then build it if it is buildable, with the tests that watch it and a watched RED.

### 2. Done

- **The container measured and reported.** No repository on arrival, thirteenth consecutive row. Cloned in **18s**, `.git` **191M**, worktree **59M**, `pnpm install --frozen-lockfile` **`Done in 11.2s`**.
- **The base reproduced at `de5f90b5`** on a clean tree, branched from `origin/claude/wave-413s` and not from `main`: `generate` reported *every span already matches its query* and left `git status` empty, gates **33 of 33**, invariants **35 of 35**, suite **337 files / 8,163 passed / 10 skipped / 0 failed**, typecheck 0, lint 0, `format:check` clean.
- **[ADR-413](../decisions/ADR-413.md)'s framing re-derived and CONFIRMED**, by grep over `packages/db/src` rather than from the entry: `catalogRow` occurs ten times in that directory and every one is in `scoped-db.ts`.
- **The measurement that entry did not make**: the door is a NARROWING of a read `SystemTx` already performs, not a widening of the operator console, and the SQL equality is held as a run over every catalogue key.
- **The door built**: `CatalogReadTx` with one member, `catalogRowAt` on `SystemTx`, both exported from `packages/db`'s index.
- **The export taken at [ADR-413](../decisions/ADR-413.md) section 7's own seam**, and the supplier written as a delegation in `apps/api/src/admin-source/pinned-plan.ts`.
- **Twelve cases added across two files, ten seeds watched RED and reverted**, four of them compile-time.
- **Three prose claims repaired because this row made them false**, in `liability.ts`, `eligible-next-7d.ts` and `admin-source/index.ts`, paraphrased rather than quoted under `RI-14`.
- **Two `file:line` citations repointed** because this row moved their targets, both in files it owns.

### 3. Refused

- **`B5` TERM 2.** [ADR-411](../decisions/ADR-411.md) item 1 and [ADR-413](../decisions/ADR-413.md) item 2 both rule it needs a founder, atomic across the three copies `RI-18` binds, and the dispatch forbids the three paths it lives in. Untouched, `git diff` empty on all three.
- **A SECOND `PlanVersionSizeRow` MAPPING IN `apps/api`.** The whole shape of the delivery exists to avoid it: the supplier is a delegation and states no row, and the census still returns one file.
- **COMPOSING `readLiability`.** One term of two is spent, the group goes whole or not at all, and a method that cannot fill the paths `B5` holds would answer a 500 where `AdminSourceNotComposed('readLiability')` already answers a named refusal.
- **`catalogRows` AND `catalogRowsWhere` ON `SystemTx`.** No caller, and `packages/db`'s own rule about primitives admitted before callers.
- **THE REST OF [ADR-303](../decisions/ADR-303.md) LIMIT 4.** `rows`, `rowsWhere`, `rowAt` and `lockAt` still return `unknown` on every handle. That surface has real callers and is a separate row, exactly as that entry said.

### 4. Blockers

- **`B5` TERM 2 IS NOW THE WHOLE OF WHAT STANDS BETWEEN THIS ESTATE AND THE FIGURE**, and should be priced as the only term rather than as one of two.
- **THE GAP CAUSE FOR AN UNWIRED PRODUCER IS STILL UNRULED**, [ADR-407](../decisions/ADR-407.md) section 11 item 1, and blocks `readLiability` independently of `B5`.
- **THE `E2` READ IS OWED** on `payout-backend.ts`'s split and on `scoped-db.ts`'s new member. Nobody in this loop can sign it.

### 5. Landmines

- **`packages/db/src/scoped-db.ts` IS READ AS TEXT BY AT LEAST SIX CHECKS IN FOUR PACKAGES, AND PROSE IN IT IS EXECUTABLE.** Three read the `SystemTx` body by COUNTING BRACES from its declaration, so an unmatched `{` in a comment breaks them; three more anchor on the interface's opening line as a literal string, so widening an `extends` clause unanchors them; and `RI-15` and `RI-16` read 366 `file:line` citations into the file, so an insertion above a cited line breaks it at a window of two. **All three were hit in this row and all three were repaired by choosing a different formulation rather than by editing the checks.** A row editing this file should probe with filler lines and run `repo-invariants.mjs` BEFORE writing anything.
- **A SEED THAT LEAVES ITS CASE GREEN IS NOT EVIDENCE THE CASE IS WRONG, AND IT IS NOT EVIDENCE THE CASE IS RIGHT EITHER.** `unscopedFilterPredicate` and `unscopedAddressPredicate` render the same SQL at a unique-key address, so the obvious seed for the equivalence case could not have failed.
- **`drawdownCents` AND `bufferCents` ARE BOTH `bigint`**, so the type system cannot see a swap between them anywhere on this path, and every cents threshold a payout is decided against comes off that row.
- **`pinned-plan.ts` IS A MODULE IN `admin-source/` THAT IS NOT A READ.** It implements no `AdminReadSource` method and does not belong in `IMPLEMENTED_ADMIN_READS`. Named in `index.ts` so the next reader counting files against that array does not find one more file than names.

### 6. Files touched

`packages/db/src/scoped-db.ts`, `packages/db/src/index.ts`, `packages/db/test/catalog-read.test.ts`, `apps/api/src/payout-backend.ts`, `apps/api/src/admin-source/pinned-plan.ts` (new), `apps/api/src/admin-source/liability.ts`, `apps/api/src/admin-source/eligible-next-7d.ts`, `apps/api/src/admin-source/index.ts`, `apps/api/test/admin-source-pinned-plan.test.ts` (new), `apps/api/test/admin-source-liability.test.ts`, `apps/api/test/admin-source-flags.test.ts`, `apps/api/test/wiring.test.ts`, [ADR-416](../decisions/ADR-416.md), this record and the registries.
