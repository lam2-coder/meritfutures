---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 574: the two rows' leavings, where the coverage gap one of them named is repaired, and the four-shape claim the other made about its own guards is measured on the real file and found false for one

<!--index: **THE ROW IS [ADR-379](../decisions/ADR-379.md), `proposed` and UNSIGNED. MONEY PATH, ADR-003 STRICT, `E2` READ OWED.** [ADR-374](../decisions/ADR-374.md) and [ADR-375](../decisions/ADR-375.md) landed together against `apps/api` and each recorded what it left; this row picked up both lists and verified every item at primary source before acting on it. **THE MOST VALUABLE OUTPUT IS A REFUTATION, AS IN EACH OF THE LAST THREE WAVES, AND IT IS OF A CLAIM RATHER THAN OF A DISPATCH.** [ADR-374](../decisions/ADR-374.md) section 5 states that all four of [ADR-372](../decisions/ADR-372.md) section 5's shapes are caught by the guards that row built. **THREE ARE.** Each shape was seeded into the REAL [`start.ts`](../../apps/api/src/start.ts) at the last install and the three files in the slice run SEPARATELY so each answer is its own: a block comment, an install below `await main()` and a call inside a template literal turn all three RED, and **dead code after a top-level `throw` left `start-construction.test.ts` ENTIRELY GREEN** while a run of the module aborts at the throw and constructs nothing past it. That row's table says the walk also stops and the name vanishes; **`factoriesInstalledBeforeMain` broke at `main()` AND NOWHERE ELSE**, so all eleven factories stayed in the pinned list. **THE SHAPE WAS NEVER LOOSE AND THE DISTINCTION IS THE WHOLE FINDING**: the same seed reddens the other two, since `start-program.test.ts` forbids a top-level `throw` outright and `wiring.test.ts` truncates `wired` at one. **WHAT WAS DEFECTIVE IS A CONTROL'S ACCOUNT OF ITS OWN MECHANISM, AND THE ERROR RAN IN THE OVER-REPORTING DIRECTION, WHICH IS HOW A GUARD GOES QUIET RATHER THAN RED.** **RUNNING THE THREE FILES SEPARATELY IS WHAT MADE IT VISIBLE**: one suite run reports RED, the row records that the shape is caught, and the false claim survives. The reader now breaks at a top-level `throw` where both siblings already broke, its docstring is true about its own answer again, and **a fixture case holds the clause because a clause defended by no case is a claim**. **[ADR-374](../decisions/ADR-374.md) SECTION 7 ITEM 1 IS REPAIRED AND IT WAS A COVERAGE GAP RATHER THAN A DEFECT IN THE TREE**: the one-file-scan case is titled for all of `apps/api/src` and read `src` and `src/routes` by name, **58** `.ts` files recursively against **48** read and **10** missed, all ten under `admin-source/`; the CLAIM holds over all 58 at **ZERO** module-scope setter calls outside `start.ts`, so only the COVERAGE was short. The walk is recursive now, in the shape `start-construction.test.ts` already used, **the unread directory is NAMED rather than counted and no cardinal is pinned** because one would move on every new module and be deleted as noise. **ITEM 2 IS VACUOUS AND IS RECORDED AS DISCHARGED-BY-ABSENCE RATHER THAN AS DONE**: no statement of the construction closure exists in either file this row owns, so there is nothing to correct from twelve to thirteen, which is worth recording because a later row finding nothing would conclude somebody had beaten it to it. **WHAT `wired` MOVING TO THE SYNTAX TREE RETIRES IS A REASON AND NO PIN, AND THE DISPATCH'S GUESS THAT PINNED COUNTS MAY NOW BE DERIVABLE DOES NOT SURVIVE**: every count in this slice was already derived. What was false was `start-program.test.ts`' opening sentence, one case title naming a text-matched `wired` set that no longer exists, and one census framed as a QUEUE that [ADR-375](../decisions/ADR-375.md) has emptied by construction. **THE ONE PIN THAT NAMED THE OLD DERIVATION AS ITS GROUND IS STILL LOAD-BEARING ON ANOTHER**, and saying so is the answer rather than a hedge: a top-level `throw` truncates the install order and the tree derivation reports the truncation as a SMALLER `wired`, a cardinal moving with no name attached, where the pin makes it arrive carrying its own. **Under `RI-14` the retired sentence is KEPT where it stands and the retired phrase is NAMED rather than reproduced.** **REPORTED AND NOT RESOLVED: THREE FACTS IN THIS SLICE ARE NOW ASSERTED IN TWO FILES**, which [ADR-375](../decisions/ADR-375.md) was awake to and declined for a fourth fact; retiring a money-path case to remove a redundancy is a larger call than the row that noticed it holds, and it ships as an open question with no value chosen. **`start.ts` IS NOT EDITED AND SECTION 9's IMPRECISION AT `start.ts:311` IS LEFT FOR THE THIRD TIME, RE-CHECKED AT PRIMARY SOURCE RATHER THAN INHERITED**: the comment is at `:311`, the install at `:315`, `await main()` at `:317`, and the append the sentence invites was SEEDED, turning **three of three** files red where that row named two. **It is named a landmine for a FOURTH consecutive entry and the approval block asks for that to be ended rather than renewed.** **`wiring.test.ts` WAS NEVER OPENED FOR WRITING and that was measured rather than preferred**: [ADR-375](../decisions/ADR-375.md) section 8 reports 183 citation sites across 74 files at 50 distinct lines into it, all moved by that row with `RI-15` and `RI-16` green throughout, so nothing here can see such a citation drift and the cheapest correct decision was to put nothing there. **THE ONE LINE THIS DIFF DID MOVE IS CITED SIX TIMES**: `start-program.test.ts:188` is `:251`, repaired in six files including two belonging to another row, **line numbers only**, and **DERIVED BY READING AT BOTH ENDS RATHER THAN BY ARITHMETIC OR BY TRUSTING A GATE**. **SIX SEEDS, SEVEN OBSERVATIONS OF RED AND ONE OF GREEN, THE GREEN BEING THE FINDING**, four editing `src/`, every file restored byte-identical under `sha256sum -c` against a four-file manifest re-verified after each restore, and **no restore was done with `git checkout --`**, which is the mistake three consecutive waves recorded. **Seed 5 is falsification in both directions in one step**: the same edit reddens the repaired case here and leaves it **GREEN at 6 of 6** on `origin/main`. **NO `src/` FILE CHANGED, no port wired, no door opened, no triple moved, no gate weakened, no test skipped, disabled or quarantined, no invariant minted, no `CI-06` letter spent, no migration number taken or reserved, no `status` field moved.** **`377` AND `378` ARE RESERVED** so `CI-06f` stays gapless with two siblings running now, naming no subject and claiming nothing. **THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL**, the tenth consecutive session to record it, and the clone landed exactly on the `6698f9c0` the row names. **NO `mcp__github__*` TOOLS, NO `gh` ON PATH, `api.github.com/repos/lam2-coder/meritfutures` ANSWERS 403 while `git push` AUTHENTICATES**; the branch is pushed and **no pull request was opened, by instruction**. Counts derived at reporting time off each runner's own last line: suite **336 files / 8,071 passed / 10 skipped / 0 failed** against a base reproduced on `6698f9c0` **BEFORE the first edit** of **336 files / 8,070 passed / 10 skipped / 0 failed**, a delta of **+1 case in ONE EXISTING FILE entirely by ADDITION with zero removals**, files unmoved at 336 and skips unmoved at 10, attributed BY REMOVAL: `start-construction.test.ts` runs **5** alone here and **4** alone on the base, `start-program.test.ts` runs **6** alone on BOTH. Gates **33 of 33** after `generate`, invariants **35 of 35**, typecheck, lint and `format:check` clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `379`: pick up what [ADR-374](../decisions/ADR-374.md) and [ADR-375](../decisions/ADR-375.md) each recorded leaving against `apps/api`, verify every item at primary source rather than accept the dispatch's account of it, and act on what the entries actually say.

**One objective.** It resolved into nine dispositioned items, two repairs, one refutation, one re-checked non-repair, six citations moved and repaired, and six seeds.

---

### 2. What was measured before the first edit

**THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL.** `/home/user` was empty, with no clone and no `node_modules`. **This is the tenth consecutive session to record it.** The tree was attached to this session's authorized set and cloned by hand, and `HEAD` was confirmed to be the `6698f9c0` the row names rather than assumed.

**Base, on a clean tree, before anything was touched: 336 files / 8,070 passed / 10 skipped / 0 failed.** Gates **33 of 33**, invariants **35 of 35**, typecheck, lint and `format:check` clean. **These are not the figures either predecessor entry reports**, because both measured a tree on which the other had not yet merged, and a row that carried either forward would have reported a delta against a tree that no longer exists.

**AND THE DISPATCH'S SEVEN CLAIMS WERE RE-DERIVED BEFORE ANY OF THEM WAS USED.** [ADR-379](../decisions/ADR-379.md) section 1 is that table. Six hold exactly. One does not, and it is section 4.

---

### 3. The refutation

**[ADR-374](../decisions/ADR-374.md) SECTION 5 SAYS ALL FOUR OF [ADR-372](../decisions/ADR-372.md) SECTION 5's SHAPES ARE CAUGHT BY THE GUARDS THAT ROW BUILT. THREE ARE.**

Each shape was seeded into the **real** [`apps/api/src/start.ts`](../../apps/api/src/start.ts) at the last install, and the three files in the slice were run **separately** so that each one's answer is its own.

| shape | `start-program` | `start-construction` | `wiring` |
|---|---|---|---|
| a BLOCK comment at column zero | RED | RED | RED |
| the install below `await main()` | RED | RED | RED |
| **dead code after a top-level `throw`** | RED | **GREEN** | RED |
| the call inside a template literal | RED | RED | RED |

**`factoriesInstalledBeforeMain` BROKE AT `main()` AND NOWHERE ELSE**, so a throw above the last install left all eleven factories in the pinned list, all four cases green, and nothing in that file saying a run of the module it reads would abort partway.

**THE SHAPE WAS NEVER LOOSE AND THE DISTINCTION IS THE WHOLE FINDING.** The same seed reddens the other two files. **What was defective is a control's account of its own mechanism**, and the error ran in the OVER-reporting direction, which is how a guard goes quiet rather than red. **RUNNING THE THREE FILES SEPARATELY IS WHAT MADE IT VISIBLE**: one suite run reports RED, the row records that the shape is caught, and the false claim survives.

---

### 4. What shipped

| commit | what |
|---|---|
| the recursive scan | [ADR-374](../decisions/ADR-374.md) section 7 item 1. **58** `.ts` files under `src` recursively, **48** read, **10** missed, all under `admin-source/`. The claim held over all 58; only the coverage was short |
| the reachability clause | section 3 above. One clause, inert on this tree, held by a fixture whose answer changes when it goes |
| the six citations | `start-program.test.ts:188` is `:251`, derived by reading at both ends |
| [ADR-379](../decisions/ADR-379.md), `ALLOCATION`, the registries | the ruling, `proposed`, UNSIGNED |

**NO `src/` FILE HAD ITS BEHAVIOUR CHANGED**, and `git diff origin/main -- apps/api/src` is EMPTY after four seeds edited it.

**`apps/api/test/wiring.test.ts` WAS NEVER OPENED FOR WRITING**, and that was measured rather than preferred: [ADR-375](../decisions/ADR-375.md) section 8 reports 183 citation sites across 74 files pointing into it at 50 distinct lines, every one moved by that row with `RI-15` and `RI-16` green throughout. **Nothing in this repository can see a citation into that file drift**, so the cheapest correct decision was to put nothing there.

---

### 5. What was NOT done, and why

**THE IMPRECISION AT `start.ts:311`, LEFT FOR THE THIRD TIME AND RE-CHECKED RATHER THAN INHERITED.** The comment sits at `:311`, the install it describes at `:315`, `await main()` at `:317`. **The reason two rows declined is stronger again, measured**: [ADR-374](../decisions/ADR-374.md) section 6 names two controls that redden on the append the sentence invites, and this row seeded that append on the real file and watched **three of three** files go red. A `src/` prose repair for a sentence covered three ways, taken by a row whose objective is somebody else's leavings, is scope this row does not have.

**THE DUPLICATION IS REPORTED AND NOT RESOLVED.** Three facts in this slice are now asserted in two files. **Retiring a money-path case to remove a redundancy is a larger call than the row that noticed it holds**, and every argument for keeping both is also the argument somebody makes for never deleting a passing test. It is [ADR-379](../decisions/ADR-379.md) section 13 question 3, recorded as options with no value chosen.

**FOUR OWED ITEMS ARE OUTSIDE THIS FENCE AND ARE VERIFIED STILL WRONG RATHER THAN REPAIRED.** The `CRON_INVENTORY` replay row and the `RI-35` registration after it; the two `packages/tooling` sites citing `wiring.test.ts:215`; the five drifted citations in `DELTA_MANIFEST.md` and `ALLOCATION.md`; and `RI-15`'s three holes. **This row's diff moves none of them**, and `already broken` is a reason rather than a licence.

---

### 6. Falsification

**SIX SEEDS, SEVEN OBSERVATIONS OF RED AND ONE OF GREEN, every file restored byte-identical under `sha256sum -c` against a four-file manifest re-verified after each restore.** Four edited `src/`. **No restore was done with `git checkout --`**, which is the mistake three consecutive waves recorded; every seed was taken against a committed tree and restored from a copy made before it.

- **Seed 3 is the only green in the set and it is what the entry is for.** Section 3.
- **Seed 5 is falsification in both directions in one step.** A module-scope setter call in `src/admin-source/flags.ts` reddens the repaired case here and leaves the same case **GREEN at 6 of 6** against `origin/main`. **A repair that only reddens on the new tree has shown that it fires; one that also goes green on the old has shown what it fires ON.**
- **Seed 6 seeds this row's own repair rather than the tree**, and reddens at the fixture case alone, which is the evidence that the fixture holds the clause rather than that the clause holds itself.

---

### 7. Blockers and landmines

- **LANDMINE, NAMED FOR A FOURTH CONSECUTIVE ENTRY: `start.ts:311`.** A one-line `src/` prose row. Four entries have named it and none has repaired it, each on a good reason. **The fourth naming is where that stops being diligence**, and [ADR-379](../decisions/ADR-379.md) section 14 asks for it to be ended rather than renewed.
- **LANDMINE: the reachability clause added here is INERT and may be unreachable.** `start-program.test.ts` forbids a top-level `throw` outright, so the clause can only matter on a tree where that case is already red. **The argument for it is that a reader whose docstring is false about its own answer is a reader nobody can reason about**, not that it will ever fire.
- **LANDMINE: three facts, two files.** Section 5.
- **OWED AND OUTSIDE THIS FENCE:** the four items in section 5, each verified at primary source here and left where it stands.
- **Concurrency: two sibling rows are running now**, one on `apps/worker/**` and one on `packages/tooling/checks/**`. Neither file tree was opened. `377` and `378` are RESERVED so `CI-06f` stays gapless; **the cells name no subject and claim nothing**, and a row taking one replaces the cell whole. **THIS IS THE SIXTH CONSECUTIVE WAVE DISPATCHED WITHOUT A RESERVATION BLOCK.**

---

### 8. This session's own tooling, measured rather than assumed

```
$ command -v gh                                            -> not found (exit 1)
$ curl -s -o /dev/null -w '%{http_code}' \
    https://api.github.com/repos/lam2-coder/meritfutures   -> 403
```

**No `mcp__github__*` tool exists in this session's surface**, checked by search rather than by inspection of a list. **`git push` authenticates** through the session's git proxy and did so on every commit. **This matches sessions 554, 558, 562, 564, 565, 568, 569, 570 and 571 exactly.** The branch is pushed and **no pull request was opened, by instruction**.

**Every Bash command was run bare from the working directory**, per the row's operational note, and **no permission stall occurred**, which is the eleventh consecutive row to report that.

---

### 9. Verification, derived at reporting time

Every figure in the index block above was derived at reporting time off each runner's own last line. **`pnpm run verify` and `scripts/corpus/falsify.mjs` were NOT run**, both forbidden by the row for mutating the working tree.
