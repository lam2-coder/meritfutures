---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 261: the portal renders purchases and certificates, and the classification the dispatch cited does not exist

<!--index: THE PORTAL'S FIRST TWO RENDERED SCREENS FOR `SC-M4-06` AND `SC-M4-08`, shipped unwired and now **WIRED AND BUILDING** on the merge with session 250, in one route group `app/(purchases)/` whose URLs `next build` names in its own route table: `/purchases` and `/certificates`. **The dispatch's premise is wrong and the correction is this session's first finding: `GS-302` is `restricted` in `identity_status`, which is an IDENTITY STATUS and not a data classification.** `0001:27` is `CREATE TYPE identity_status AS ENUM ('active', 'restricted', 'closed')` and `ADR-041` refused a fourth value of THAT enum. No data-classification vocabulary exists anywhere in this corpus, so a rendered certificate tests nothing about one. **What a public certificate may carry IS settled, in two places that agree**: `INV-M11-01` enumerates it and `AS-M4-03` rule 3 states it as absences. **So `ADR-149`'s condition is FALSE and the number is NOT TAKEN.** The control that ships instead is an ALLOWLIST at the boundary: `publishedCertificate()` builds field by field from a closed list and `certificate_id` does not cross it, because `SD-M11-01` separated the primary key from the public `code` so the token could be rotated after an incident. **Eighteen assertions, and the ones that matter are absences over the RENDERED BYTES.** The refusal path was leaking the `certificate_id` through an error message and the test caught it. **Four unowned reads named**: `GET /accounts/:accountId/certificate` is approved and claimed by no session in this wave, `content_documents` has no contract row at all, `GET /certificates` is M11's and unwritten, and the purchase cursor has no field. **`CI-07` REOPENED ON THIS COMMIT AND WAS LEFT RED RATHER THAN RE-RULED; SESSION 250 THEN CLOSED IT BY WRITING THE STAGE**, and section 8.1 records that this log forecast that wrongly TWICE, in opposite directions, which is this entry's own headline error class pointed at its author. **11 of 11 invariants** including 250's new `RI-11`, **33 of 33 gates**, `falsify` clean, **`pnpm --filter @merit/portal build` exit 0**, 2,677 tests green -->

- **Objective:** render `SC-M4-06` and `SC-M4-08` as real pages against the view modules that already exist, and establish from the corpus rather than from intuition what a certificate may show, to whom, and what it must not.
- **Done:** `apps/portal/src/app/(purchases)/`, seven files, serving `/purchases` and `/certificates`. **`ADR-149` NOT TAKEN**, and its `ALLOCATION` row is amended in place with the reason. 11 of 11 invariants, typecheck, lint and format clean, 152 files / 2,674 passed / 1 skipped, `falsify` clean, and **31 of 32 gates with `CI-07` red on purpose (section 9)**. Both pages rendered with real-shaped data and the output pasted into the pull request.
- **Next:** **the segment is WIRED and the two `page.ts` files needed no change**, which is what shipping against a declared port bought. Session 250's merge also brings `jsx: "preserve"`, `jsxImportSource` and `src/**/*.tsx` into [`apps/portal/tsconfig.json`](../../apps/portal/tsconfig.json), so **converting the two screen modules from `createElement` to `.tsx` is now mechanical and is deliberately NOT done here**: it is a readability change to files nobody is blocked on, and it belongs to whoever next has reason to open them.
- **Blockers:** **`CI-07` is CLOSED** (section 8.1). What remains is **four reads this segment needs and cannot have**, all named in `apps/portal/src/app/(purchases)/ports.ts` and section 4 below. Two are session 253's and in flight; two have no owner at all.
- **Landmines:** **the `.ts` page extension is deliberate and reversing it without the compiler option makes these files invisible to `pnpm run typecheck` rather than checked by it.** The rule diff's PAIRING is this screen's inference over the server's list order and not a fact the server states, and the unpairable panel is what keeps that inference honest. **`apps/portal/src/index.ts` does not export this segment** and `surface.test.ts` was not extended, deliberately: a page is not a library export and that file is outside this fence.
- **Merged `origin/main` four times under this branch**, at `fafeee7`, `3f32575`, `5a65e3dc` and `cbeb8d96`, as ten sessions landed underneath. **Two conflicts, both additive and neither a logic choice**: [STATE](../STATE.md)'s two appended sections were kept in landing order, and [README](README.md)'s conflict fell inside the **generated** `session_entries` span **both times**, which [ADR-088](../decisions/ADR-088.md) and [P4 section 9](../plans/P4-portal-and-site.md) say merges by re-running `gates.mjs generate` rather than by hand. [ALLOCATION](../decisions/ALLOCATION.md) auto-merged.
- **Files touched:** `apps/portal/src/app/(purchases)/ports.ts`, `apps/portal/src/app/(purchases)/fixtures.ts`, `apps/portal/src/app/(purchases)/model.ts`, `apps/portal/src/app/(purchases)/purchases-screen.ts`, `apps/portal/src/app/(purchases)/certificates-screen.ts`, `apps/portal/src/app/(purchases)/purchases/page.ts`, `apps/portal/src/app/(purchases)/certificates/page.ts`, [`apps/portal/test/page-purchases.test.ts`](../../apps/portal/test/page-purchases.test.ts), [`apps/portal/test/page-certificates.test.ts`](../../apps/portal/test/page-certificates.test.ts), [ALLOCATION](../decisions/ALLOCATION.md), [STATE](../STATE.md), [sessions/README](README.md), the generated span, this file.

---

### 1. THE DISPATCH'S PREMISE IS WRONG, AND THE CITATION IT RESTS ON RESOLVES TO SOMETHING ELSE

The dispatch, and the `149` row in [ALLOCATION](../decisions/ALLOCATION.md) and the `261` row in [README](README.md) that carry the same sentence:

> A certificate is a PUBLIC artifact and `GS-302` is `restricted` (cited to `0001:27`; `ADR-041` refused a fourth value, so the vocabulary is closed at three). **A rendered certificate is the first real test of that classification in this repository.**

**Every citation in it resolves. The conclusion drawn from them does not.**

| Claim | Checked against | Verdict |
|---|---|---|
| `0001:27` declares a closed vocabulary and [ADR-041](../decisions/ADR-041.md) refused a fourth value | [`0001_extensions_and_enums.sql`](../../packages/db/migrations/0001_extensions_and_enums.sql):27 is `CREATE TYPE identity_status AS ENUM ('active', 'restricted', 'closed');` | **TRUE** |
| `GS-302` is `restricted` | [`37-gs-300-to-gs-304`](../testing/golden-scenarios/37-gs-300-to-gs-304-impersonation-and-admin-parity.md):19, *"Impersonation of a **`restricted`** identity"* | **TRUE** |
| A certificate is a public artifact | `certificates` is in `0020_public_surface.sql`, and [M11](../plans/M11-certificates-social-proof.md) section 4 makes `GET /verify/:code` public and unauthenticated | **TRUE** |
| ...therefore a rendered certificate tests **that classification** | **`identity_status` IS AN IDENTITY'S STATUS AND NOT A CLASSIFICATION OF DATA.** `GS-302` rules who may VIEW a restricted trader's screens during impersonation. It says nothing about what a certificate may carry | **FALSE** |

**There is no data-classification vocabulary in this corpus.** `grep -i classification` over [SECURITY](../architecture/SECURITY.md) and the rest of `architecture/` returns nothing of the kind; the only occurrences of `restricted` beside data are `identity_status` and the `on delete restrict` on [`impersonation_sessions`](../architecture/data-model/impersonation_sessions.md). So the sentence in the two registries names a test that cannot be performed, because the thing to be tested does not exist.

**This is the corpus's own recurring error class in a new costume**, and [session 95](2026-08-20-session-95.md) already named it: *"a claim about a table checked against the file that created it rather than against the table as it now stands."* Here the citation was checked and the INFERENCE from it was not. The number resolved, the line said what it was said to say, and the word `restricted` was carried from an enum of identity states into a sentence about data sensitivity by nothing more than the word being the same.

---

### 2. WHAT A PUBLIC CERTIFICATE MAY CARRY IS SETTLED, IN TWO PLACES THAT AGREE

So the real question was asked at the real source instead. It is answered twice and the two answers are the same answer.

| Source | Text |
|---|---|
| [M11](../plans/M11-certificates-social-proof.md) `INV-M11-01` | *"A certificate's claims are **minimal**: the account's plan, size, trading day, the kind-specific value, and nothing else \| Claim schema per kind, validated at issuance. **No identity, no email, no display name, no cumulative total, no lifetime figure.**"* |
| [M04](../plans/M04-trader-portal.md) `AS-M4-03` rule 3 | *"Claims are minimal by construction: no identity, no email, no cumulative totals. A certificate is a fact about an account on a day, and the smaller the claim, the less there is to forge usefully."* |

[M11](../plans/M11-certificates-social-proof.md)'s governing sentence is the same rule written as a warning aimed at exactly this session: *"a certificate is a small, signed, revocable claim about one account on one day, and **every temptation in this module is a temptation to make it bigger than that**."*

**`ADR-149`'s condition was "if the corpus does not settle what a public certificate may carry". It does. The number is NOT TAKEN**, its row is amended in place under [ADR-065](../decisions/ADR-065.md) T3, and the founder can reverse that on the strength of section 1 alone if the registry sentence is thought worth an entry rather than an amendment.

#### 2.1 The control that ships instead, and why it is an allowlist

`apps/portal/src/app/(purchases)/model.ts`'s `publishedCertificate()` **constructs** its result field by field from `PUBLISHED_CERTIFICATE_FIELDS`. It does not spread, it does not copy and delete, and it does not filter.

**The difference is what happens on the day somebody adds a field to `CertificateView`.** A spread carries the new field onto a public artifact by default and a reviewer has to notice. This refuses to carry it until somebody writes its name in the list. A redaction pass is a denylist wearing an allowlist's clothes, and a denylist is wrong about every field invented after it was written.

#### 2.2 THREE THINGS THAT DO NOT CROSS, EACH FOR A REASON READ OFF A ROW

| What | Why not | Where |
|---|---|---|
| **`certificate_id`** | `SD-M11-01` adds `code` and states the reason in its own row: it *"is distinct from `id` so the public token can be rotated after an incident without rewriting the primary key"*. A screen that prints the id publishes the half the split keeps private, on the one screen whose purpose is copying something out. It is not even a React key here: `verify_url` is unique and already public, so the id never enters the render tree | `model.ts`, asserted in `page-certificates.test.ts` |
| **Any account or identity identifier** | `SD-M4-01` puts `account_id` and `identity_id` on the row; `CertificateResponse` puts neither on the wire. **The screen is therefore not grouped by account**, which is the obvious layout: grouping would put an account id on the page and would make the absence assertion unstateable, because "none inside the card element" depends on which element somebody screenshots | `certificates-screen.ts` |
| **Any total** | Two rules, one absence. `INV-M4-01` bans client-side money arithmetic and `AS-M11-07` bans a certificate becoming an aggregate publisher, because *"a total assembled from cards is a statistic with no method page"* | Both screens |

#### 2.3 The finding the test found, which is the reason the test reads bytes

**The first draft of the refusal path passed `UnverifiableCertificateError.message` straight to the screen.** That message names the `certificate_id`. So the page refused to render the card, said why, and **published the identifier the allowlist exists to withhold, through an error string** rather than through a field.

An assertion written over the model's fields would have passed. The assertion is written over the rendered markup, and it failed. The reason is stated in the test file: a value reaches a page through an `alt`, a `title`, a `data-` prop or an error message without ever appearing in a visible field, so an absence claim has to be made about the bytes.

---

### 3. WHAT THE SCREENS SAY THAT THEY CANNOT PROVE, SAID ON THE SCREEN

**`CertificateResponse` carries neither `revoked_at` nor `revoked_reason`**, which [`view/certificates.ts`](../../apps/portal/src/view/certificates.ts) had already recorded. `AS-M4-03` rule 2 makes revocation real and the verify page the only surface that reports it. So a card on this screen is a card that **was issued** and is not a card that **is currently valid**, and leaving that absence silent would let the screen imply the stronger claim. The page says so, in the same render, above the cards.

The same shape twice more, both named in `apps/portal/src/app/(purchases)/ports.ts` rather than papered over: `INV-M11-09`'s **deferred** state has no representation in the approved response, so a deferred certificate and an absent one are one `null` here; and the **share affordance is the `verify_url` and never the `image_url`**, because the image is signed and time limited, so a copy of it is a snapshot that expires and cannot show a later revocation. [M11](../plans/M11-certificates-social-proof.md) section 4's public `GET /certificates/:code/image.png` is the image that re-renders from the live row, and it is served off `code`, which the response does not carry.

---

### 4. FOUR READS THIS SEGMENT NEEDS, TWO OF WHICH NOBODY OWNS

The dispatch asked for what is needed from session 253 rather than an invented client. There are four, and naming them found two that are owed by nobody.

| Read | Owner | State |
|---|---|---|
| `GET /purchases` | Session **253** | In flight. The interface is declared and **takes no cursor argument**: the contract calls it a cursor list and names no cursor field, so a parameter invented here would be a guess this screen then renders paging controls against |
| `GET /plans/:planId/versions/:version` | Session **253** | In flight. `SD-M4-02`'s diff costs two reads of it per reset and never a subtraction |
| `GET /accounts/:accountId/certificate?kind=` | **NOBODY** | Approved in [API_CONTRACT](../architecture/API_CONTRACT.md) section 6. Sessions 251 to 258 divide the unwritten contract endpoints and **none of them claims this row**: 251 takes the four `/accounts` rows and this is not among them |
| The simulated-environment disclosure text | **NOBODY, AND NO CONTRACT ROW EXISTS** | `INV-M4-09` makes it a compliance obligation on this screen. [`view/disclosure.ts`](../../apps/portal/src/view/disclosure.ts) already records that no contract row serves `content_documents` to the portal, so the provenance is asserted by the caller rather than proven |

`GET /certificates`, [M11](../plans/M11-certificates-social-proof.md) section 4's trader-facing list including deferred entries, is in no `API_CONTRACT` section at all. **So this screen assembles its list by asking per account and per kind**, which is the shape the approved contract can actually answer.

---

### 5. THE SEGMENT, AND THREE CHOICES THAT ARE NOT PREFERENCES

**One route group, two URLs.** `app/(purchases)/` is this session's one directory under `app/`; `purchases/page.ts` serves `/purchases` and `certificates/page.ts` serves `/certificates`, so neither screen nests under the other. It is a shape this repository's own tooling already reads: `RI-09` in [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) strips `(group)` and `@slot` segments before deciding what a path spells, so the invariant sees `/purchases` exactly as a browser will. Neither file is a route handler and neither carries `'use server'`, so nothing here serves `/api/v1` or an operator path ([ADR-083](../decisions/ADR-083.md) section 3, [ADR-095](../decisions/ADR-095.md) ruling 3).

**`.ts` and not `.tsx`, and it is not a stylistic choice.** [`tsconfig.base.json`](../../tsconfig.base.json) sets no `jsx` option and `apps/portal/tsconfig.json` includes `src/**/*.ts` only, both of which arrive with session 250's `app/layout.tsx`. **A `.tsx` page written here would have been INVISIBLE to `pnpm run typecheck` rather than checked by it**, which is a green gate that read nothing, and this session's own verification list opens with that gate. `ts` is a default `pageExtensions` entry in `next@16.3.2` (`node_modules/next/dist/server/config-shared.js`:107, read out of the installed package), so these are pages Next.js routes and files the existing gate checks. Converting after 250 lands the compiler option is mechanical.

**Mobile first without a stylesheet this session does not own.** The global stylesheet arrives with the root layout, which is 250's. So the layout is carried by the elements: stacked `article` and `dl` blocks reflow at 375px with no CSS at all, and a wide `table` would not. A page that looked right only once somebody else's CSS landed would be a page whose mobile claim nobody could check today.

---

### 6. `SC-M4-06`: THE OMISSION THAT WOULD READ AS A POSITIVE CLAIM

M04 section 3.1's one thing the purchases screen must get right is *"the rule diff when versions differ (`SD-M4-02`)"*.

**`GET /purchases` states each purchase's `plan.version` and states no pairing.** So the pairing is this screen's: a `reset` row is paired with the nearest **earlier** purchase on the same `plan_id`, where "earlier" is a position in the server's own list order and never a comparison of `created_at`, which would be this file quietly deciding the list is sorted by time.

**And a reset whose partner is not on the page renders as UNPAIRABLE rather than as unchanged.** A cursor list has a second page, so the earlier purchase is legitimately absent, and a panel that rendered nothing there would say *"your terms did not change"* about a comparison it never made. That is [`view/purchases.ts`](../../apps/portal/src/view/purchases.ts)'s own rule about its own walk, pointed one level up: *"An omission that reads as a positive claim is the worst failure available on this screen, because the screen exists to be the record of what the trader agreed to when they repurchased."*

**The purchases screen carries no absence assertion of the certificate kind, and that is the point.** A purchase is the trader's own record on the trader's own authenticated screen (`INV-M4-10`), so its identifiers are theirs and are not a leak. Applying the certificate rule here would be cargo cult; applying the purchase rule there would be the failure the certificate rule exists to prevent.

---

### 7. Two things measured on the way past, reported and not taken

1. **[`view/certificates.ts`](../../apps/portal/src/view/certificates.ts) and [`view/disclosure.ts`](../../apps/portal/src/view/disclosure.ts) cannot be loaded by `node --experimental-strip-types`.** Both declare an error class with a TypeScript **parameter property** (`constructor(readonly certificate_id: string)`), and the loader reports `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX: TypeScript parameter property is not supported in strip-only mode`. [ADR-083](../decisions/ADR-083.md) rules that loader is how a deployable runs. It does not bite `apps/portal` today, because [ADR-095](../decisions/ADR-095.md) moved its `start` script to `next start` and Next.js compiles with SWC, and it is recorded because the next reader to try running a portal module directly will meet it. **Neither file was changed**: rendering did not force it.
2. **`apps/api/package.json`'s claim that `tsc` resolves `@merit/db` through the workspace is already recorded as wrong** by `RI-09`'s own header, in a session whose fence did not hold that file. It still stands. Out of this fence too.

---

### 8. `CI-07` REOPENS ON THIS COMMIT, AND IT IS LEFT RED ON PURPOSE

`CI-06/gate-inventory` reports one finding and it is this:

> `STRATEGY.md`:190 `CI-07`: the artifact "a page, layout or route file under `apps/*/src/app/`" HAS ARRIVED (`apps/portal/src/app/(purchases)/certificates/page.ts`, `apps/portal/src/app/(purchases)/purchases/page.ts` is an App Router file), so the row reopened on the commit that added it and the stage is not written.

**That is the gate working, not the gate breaking.** [ADR-073](../decisions/ADR-073.md) disposition (b) is written so a waiting row goes red the day its artifact arrives, and [ADR-095](../decisions/ADR-095.md) section 6 states the consequence in its own words: *"the row moves in this commit or the gate lies in the direction that looks like success."* [`apps/portal/package.json`](../../apps/portal/package.json)'s own `//next` note predicted this exact reopen when the `build` script landed.

**And the stage still cannot be written, for the reason the row itself already gives about its predecessor.** `next build` in `apps/portal` exits **1** on *"Couldn't find any `pages` or `app` directory"* until session **250** lands `next.config` and `app/layout.tsx`. A `CI-07` written on this commit could only fail forever, which [STRATEGY](../testing/STRATEGY.md) section 4's first paragraph forbids, and skipping it instead is forbidden by [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) rule 2. That is the same two-sided argument the row has carried twice already, against an artifact that has now moved one final step down the chain.

**Three reasons it is not this session's to close, and any one of them is enough.**

| # | Reason |
|---|---|
| **1** | **An implemented leg names a job in [`ci.yml`](../../.github/workflows/ci.yml)**, and the row says so in its own last sentence: [P4 section 8](../plans/P4-portal-and-site.md) gives that file to **`P4-i`** and to no portal-page slice. `ci.yml` and [STRATEGY](../testing/STRATEGY.md) are both outside this fence |
| **2** | **The stage would be red on arrival.** Nothing this session can write makes `next build` exit 0, because the missing artifact is a root layout in somebody else's fence |
| **3** | **Session 250 triggers the identical reopen and is the session after which a build can succeed.** `app/layout.tsx` is itself *"a `page`, `layout` or `route` file under `apps/*/src/app/`"*, so this row is red on 250's commit whether or not this branch exists, and 250 is the first commit on which the row can move to IMPLEMENTED rather than to a second re-ruling |

**And the decisive reason is a collision rather than a fence line.** The one move available to this session is a RE-RULING, not an implementation: the artifact has fully arrived, so the only honest edit names the NEXT link, which is [ADR-080](../decisions/ADR-080.md)'s rule that *"a chained condition names its NEXT LINK and never its whole chain"*. **That next link is session 250's `app/layout.tsx`**, and re-ruling the row onto a file another session is writing right now means 250's commit re-reopens the row and moves it again. [P4 section 9](../plans/P4-portal-and-site.md) rows [STRATEGY](../testing/STRATEGY.md) as **serial across slices for exactly this reason**: *"Not textually overlapping and still serial, because `CI-06/gate-inventory` reads the whole cell and a keep-both merge produces a plausible cell with one leg lost."* Two concurrent sessions editing the `CI-07` cell is the failure that row predicts, named in advance.

**`falsify.mjs` reports 2 problems and both trace to this one red gate**, which is worth stating because a bare count reads as two defects. The clean-tree leg fails because `CI-06/gate-inventory` no longer passes clean, and its own seed then reports `FAILED OFF-TARGET` because a gate that is already red cannot be watched going red. `falsify.mjs` carries a seed named *"an activation condition's artifact ARRIVING: an App Router file under `apps/*/src/app/` reopens `CI-07`"*, so the harness was built anticipating this day. On `origin/main` at `c947e39` the same run is clean. **Both problems close the moment the row moves, and neither is a second finding.**

**Weakening the gate to pass it, or widening the fence to finish, were both available and both refused, and the row closed without either.**

#### 8.1 RESOLVED. `CI-07` IS CLOSED, AND BOTH OF THIS SECTION'S FORECASTS WERE WRONG

**The row is green.** Session 250 merged as [#349](https://github.com/lam2-coder/meritfutures/pull/349) and its commit `9ba14bee`, *"close CI-07: write the stage the first rendered document reopened"*, adds the `build-and-bundle` job to [`ci.yml`](../../.github/workflows/ci.yml). `gates.mjs check` is **33 of 33** on the merge and `falsify.mjs` is clean.

**Two forecasts were made here and both were wrong, in opposite directions.**

| Forecast | What happened |
|---|---|
| *"250 first, or 250's commit carries `CI-07`'s disposition"* | Half right. 250 did carry it, but not in the commit this section meant |
| The correction to that: *"landing 250 does NOT turn this row green; the repair is `P4-i`'s"* | **Wrong.** 250 wrote the stage itself, in a commit that did not exist when `ADR-138` section 7 was read |

**The second error is this session's own headline finding pointed at its author.** `ADR-138` section 7 was read correctly and quoted accurately: at that moment it tabulated three ways out and took *"leave it a finding and say so."* The inference drawn from it, that the stage therefore belonged to `P4-i`, treated a **snapshot of a live branch** as a settled ruling. A branch under active work is not a decided one, and the entry's own text said the repair was *"one commit for whoever holds that file"* rather than that 250 would not become that holder. **A citation checked and an inference carried one step too far is exactly what section 1 of this log is about.** It is recorded here rather than quietly overwritten.

**What was right is the part that cost something to hold.** The gate was not weakened, the fence was not widened, `STRATEGY.md` was not edited from this branch, and no re-ruling was taken. `ADR-138` section 7's refusal of the re-ruling option, *"a re-ruling today would be relaxing a register to keep it green"*, is what the closed row now vindicates: the row went green because somebody wrote the missing stage, which is the only honest way a waiting row is ever supposed to close.

**Weakening the gate to pass it, or widening the fence to finish, were both available and both refused, and the row closed without either.**

#### 8.1 A CORRECTION, MADE AFTER READING SESSION 250's OWN ENTRY

This section first said the choice was *"250 first, or 250's commit carries `CI-07`'s disposition"*. **The second half is false and session 250 had already settled it.** `ADR-138` section 7, read on `claude/portal-app-shell` rather than taken from a summary, hits the identical reopen on `app/layout.tsx` and `app/page.tsx`, tabulates the same three ways out, and **takes the same one this session did: leave it a finding and say so.** `pnpm run verify` exits 1 on that branch too, on this row alone.

**So landing 250 does NOT turn this row green.** The repair is a build job in [`ci.yml`](../../.github/workflows/ci.yml), which `ADR-138` calls *"one commit for whoever holds that file"* and [P4 section 8](../plans/P4-portal-and-site.md) gives to **`P4-i`**. Until `P4-i` runs, every portal-page branch carries this row red, which is `ADR-073` disposition (b) behaving exactly as designed across six sessions at once rather than six independent defects.

**And one of this section's own arguments expires when 250 lands, in the direction that makes the refusal stronger.** The argument above is that the stage *cannot* be written because `next build` exits 1. **`ADR-138` records that `next build` exits 0 on its branch**, so once the layout is on `main` the stage becomes genuinely writable and the "could only fail forever" reasoning no longer applies. `ADR-138` then refuses the re-ruling for the better reason, and it is the one that governs from here: *"`ADR-095`'s own reason has expired ... A re-ruling today would be relaxing a register to keep it green, which [P4 section 11](../plans/P4-portal-and-site.md) rule 13 makes the same move as widening a fence."* **The conclusion is unchanged and the reason for it is not, which is why the correction is recorded rather than quietly absorbed.**

---

### 9. Verification

| Gate | Result |
|---|---|
| `pnpm run typecheck` | **exit 0.** The two `page.ts` files and all five modules are inside `apps/portal/tsconfig.json`'s `include` and are genuinely checked, which is the whole reason for section 5's second choice |
| `pnpm --filter @merit/portal build` | **exit 0 on the merge with session 250.** `next build` compiles, type-checks and prerenders, and the route table names both of this segment's URLs: `/certificates` and `/purchases`, beside 250's `/` and `/_not-found`. **The route group produces the URLs this session claimed it would**, proved by the bundler rather than argued from `RI-09`'s parser |
| Render with real data | Both pages rendered to static markup from the corpus's own Core EOD rules object, and the output is in the pull request |
| `pnpm run check:invariants` | **11 of 11.** `RI-11` arrived with session 250 (*"no package declares a Server Action, and no framework routing config spells the API surface"*) and **this segment passes it mechanically**, which is the check that turns section 5's no-route-handler claim from an assertion into a control. `RI-09` reads the new paths and passes: it strips the route group and sees `/purchases` and `/certificates`, neither of which is `api` or an `OPERATOR_PREFIXES` entry |
| `gates.mjs generate && check` | **33 of 33.** `CI-07` closed by session 250's stage (section 8.1) and `ADR-131`'s new gate arrived in the same merge. `CI-06a` needed the two page paths written as inline code rather than as links, because a markdown link destination cannot carry a bare `(` |
| `falsify.mjs` | **clean.** *"All 33 gates pass clean and fail dirty."* The two problems it reported before were both consequences of the one red row, as recorded, and both closed with it |
| `pnpm vitest run` | **Re-measured on the merge with `origin/main` at `cbeb8d96`: 153 files, 2,683 passed, 1 skipped.** Before the first merge it was 152 / 2,674 / 1; the six sessions that landed underneath are the difference and none of it is this session's to claim |
| `pnpm run lint` and `format:check` | clean on the merge |
| `pnpm run verify` | every leg green |
