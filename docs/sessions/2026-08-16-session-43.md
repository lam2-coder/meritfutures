## 2026-08-16 - Session 43: EC-157's Repair A, and the duplicate the gate could not see (money path)
<!--index: EC-157's Repair A (`0036`), and the duplicate `ADR-046` heading no gate could see -->

- **Done:** Two things, both settled. **[EC-157](../edge-cases/EC-157.md) is ruled Repair A** and executed as [`0036_supersede_daily_marks_balance_arithmetic.sql`](../../packages/db/migrations/0036_supersede_daily_marks_balance_arithmetic.sql): `daily_marks_balance_arithmetic` is dropped and `daily_marks_inv19_closing_identity` (`closing = opening + realized_pnl`) replaces it. `0014` is untouched. [`probe_daily_marks_identities.sql`](../../scripts/db/probe_daily_marks_identities.sql) is **6 / 6, three success cases first**, and the counterfactual was executed. **[ADR-049](../decisions/ADR-049.md)** carries the `CalendarSlice` ruling that was a second `## ADR-046` heading, moved on a citation count. **All 36 migrations apply forward-only from empty: 102 tables, 404 check constraints.** 15 of 15 gates pass clean and fail dirty, 16 scope cases, 283 tests.
- **Next:** The founder's E2 read on `0036`. `INV-18` is now asserted **only** in M02 and the engine, so whoever builds `DO-3` owns it entirely.
- **Blockers:** None.
- **Landmines:**
  - **THE EXECUTABLE ARTIFACT WAS THE ONE THAT WAS WRONG, and that is the transferable finding.** Four artifacts put the adjustment at the open (`INV-18`, `R-10`, `EC-034`, and **`0014`'s own column comment**) against one that put it inside the day. The reading a session under pressure re-derives is that the constraint must be authoritative *because* it is executable. **An executable statement of the wrong identity is still the wrong identity**, and it was the only one of the five that had never been read against the other four.
  - **The schema no longer asserts the adjustment at all, and that cost is the ruling's price rather than an oversight.** `INV-18` reads `prior.balance_cents`, which lives in `rule_states`; a `CHECK` cannot see across rows. It now lives only in `INV-M2-06` (M02, before the engine sees the mark) and `R-07` at `DO-3`. **If either of those is skipped, nothing catches a bad opening balance.**
  - **The old constraint name is DROPPED, not redefined.** Reusing `daily_marks_balance_arithmetic` for the new statement would leave every reference pointing at a constraint whose meaning had silently changed, which is `C-02` in a new costume. A reader who greps the old name now finds nothing, which is the correct answer.
  - **The mislabel is the mechanism, not a footnote.** `0014`'s comment and the design record both called the constraint **"INV-18"** and it is neither identity. A reader checking whether the schema enforced `INV-18` found a constraint that said it did. `0014`'s `--` comment can never be edited (E2), so `0036` replaces the **column comment**, which is metadata, exactly as `0031` replaced `0010:225`.
  - **The day-sequence test is FLIPPED rather than deleted**, and the disappearance of a branch is itself the record: it used to split on whether the adjustment was zero, because zero was the only value at which the constraint and the two invariants agreed. It now has one expectation. It is kept so that a later migration putting the adjustment back into the closing identity fails there.
  - **`CI-06f`'s duplicate assertion already existed and the PARSER made it unreachable.** `adrEntries()` used a **non-global** `exec`, returning at most one entry per file, so two `## ADR-046` headings produced one entry and `seen` never collided. **Fifteen gates passed over the duplicate.** The repair is not a new assertion; it is making the parser capable of reaching the one that was there.
  - **The seed was written and watched NOT firing before the parser changed**, in that order and on the founder's instruction: `DID NOT FAIL  CI-06f/duplicate-heading`, then `in scope, failed`. Tightening a parser without one is how 109 phantom anchors happened.
  - **On this registry `ADR-034`'s tiebreak and constitution `E2` can point in opposite directions.** Here they agreed. When they do not, **E2 wins**, because a merged migration cannot be renumbered retroactively and an ADR can. Recorded in ALLOCATION rather than left for the next collision to rediscover.
  - **`0036` was claimed in ALLOCATION in its own pushed commit before the file existed**, and the free number was checked against **all nine remote refs** rather than the table alone. The table cannot see a sibling branch, which is what `ADR-044` and `0034` were both lost to.
- **Files touched:** `packages/db/migrations/0036_supersede_daily_marks_balance_arithmetic.sql` (new), `scripts/db/probe_daily_marks_identities.sql` (new), `docs/decisions/ADR-049.md` (new), `docs/decisions/{ADR-046,README,ALLOCATION}.md`, `docs/architecture/data-model/daily_marks.md`, `packages/db/DELTA_MANIFEST.md`, `scripts/corpus/{gates,falsify}.mjs`, `packages/rules-engine/` (README and four generator files), `docs/plans/P2-rules-engine.md`, `docs/{STATE,INDEX}.md`, `docs/sessions/`.

### The counterfactual, verbatim

The probe against `0001`-`0035`, which is the set before this migration:

```
ERROR:  new row for relation "daily_marks" violates check constraint "daily_marks_balance_arithmetic"
```

`psql` exits **3** on SUCCESS 1, the mark for a settled payout. Under `0036` the same row commits. That is EC-157's claim executed rather than argued, and it is why the probe **leads with the success case**: the whole finding is that a legitimate row was refused, and a probe that only ever attempted forbidden things passed against this constraint for twenty-two migrations.

### What was NOT done, named rather than implied

**`EC-157`'s own entry is not rewritten to `status: ruled`.** The entry is the finding and it states both repairs and their costs; editing it to record the outcome would delete the analysis the ruling was made from. The ruling lives in `0036`'s header, DELTA_MANIFEST section 20 and this log. **If the founder wants the registry entry to carry the disposition, that is a one-line change and it is deliberately not taken here.**

**No `SD-nn` was claimed.** `0036` corrects a constraint; it folds no delta, so [ADR-026](../decisions/ADR-026.md)'s completeness gate has nothing to read and the manifest says so in section 20.
