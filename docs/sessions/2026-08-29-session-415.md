---
status: approved
depends_on: [README.md]
last_updated: 2026-08-29
---

## 2026-08-29 - Session 415: `zod at every boundary` names a mechanism this workspace has never had, the citations are five in the record and six in the tree, and the one thing zod uniquely supplies is not validation

<!--index: **THE RULING IS [ADR-225](../decisions/ADR-225.md), `proposed` and UNSIGNED, and no dependency was admitted.** `zod` is a dependency of **no `package.json`** across all **20** manifests, is in the lockfile only as a transitive of the Stryker toolchain, and is **resolvable by no workspace package**, which was MEASURED rather than argued: a `createRequire` rooted at four package paths and the repository root each threw `MODULE_NOT_FOUND`. **THE ROW SAID FIVE CITATIONS AND THE TREE HOLDS SIX**, because the audit's whole-repository sweep was case-SENSITIVE and two of the sites write *"Zod"*; the sixth is `packages/rules-engine/test/generators/plan-config.ts`. **ONE OF THE SIX WAS A DEFECT.** `apps/portal/src/api/types.ts` ASSERTED the validator exists as its reason for refusing a second copy of the rule schema; the refusal stands and the reason is replaced with `validatePlan`, which exists. **THE SHARPEST FINDING IS NOT ABOUT ZOD.** `0004_catalog.sql:63` says `plan_versions.rules` is *"validated by zod at the write boundary"* and BOTH halves fail: the draft INSERT at `admin-writes.ts:1421` checks only that `rules` is a JSON object, and `validatePlan` runs one route later at the PUBLISH transition. `0017:49`'s matching claim is TRUE in substance and wrong only in mechanism. **AND THE FOUNDER'S QUESTION IS NOT THE ONE THE ROW POSED**: every validation claim in the corpus is already discharged by 17 hand-written total validators, and the only zod claim with NO substitute in this tree is the OpenAPI generator, which `API_CONTRACT:37`, `ADR-083` ruling 3 and `ADR-144:125` all name and which no file in this tree implements. **No migration edited or written, no `package.json` touched, `pnpm-lock.yaml` untouched, `MERIT_BUILD_MASTER_PROMPT.md` untouched.** Every change is a comment or a reason string. -->

**Objective.** Rule what Merit's boundary validation actually is, and make the
citations of a zod validator either true or gone.

**Outcome.** **[ADR-225](../decisions/ADR-225.md) lands, `proposed` and
UNSIGNED. The number is spent because the entry rules.** Five rulings, one
comment repaired, five quotations given a dated clause, no dependency admitted
and no migration touched.

### 1. What the row claimed, and what the tree says

| Row `225` and the [D2 audit](../reviews/2026-08-29-d2-controls-audit.md) | Derived before a line changed |
|---|---|
| `zod` in **no `package.json`** across 20 manifests | **HOLDS EXACTLY.** 20 manifests found, zero matches |
| Lockfile only, as a Stryker transitive | **HOLDS.** `zod@4.4.3:` at `pnpm-lock.yaml:2605`, one dependant, `mutation-server-protocol@0.4.1` at `:3981` |
| Resolvable by no workspace package | **HOLDS, AND IT IS MEASURED.** `createRequire` from `apps/api/src`, `apps/portal/src`, `packages/psp/src`, `packages/rules-engine/src` and the root each threw `MODULE_NOT_FOUND` after `pnpm install --frozen-lockfile` |
| No source file imports it | **HOLDS.** Zero matches over `apps/**` and `packages/**` |
| **THE CITATIONS ARE FIVE** | **REFUTED. SIX.** `grep -rin "zod" apps packages --include=*.ts --include=*.tsx -l` matches six files. The audit's sweep was case-sensitive and two sites write *"Zod"* |
| *(not in the row): the lockfile cost* | **`zod@4.4.3` HAS ZERO DEPENDENCIES.** `zod@4.4.3: {}` at `pnpm-lock.yaml:4477`, no install scripts. **The cheapest `VG-12` admission this repository has weighed**, and it argues AGAINST the entry's own recommendation |
| *(not in the row): a corpus document that already says it* | **`P4:57` ALREADY RECORDS THE ABSENCE** in an approved corpus document: *"no `zod`"* |

### 2. What was ruled

**Five rulings, in [ADR-225](../decisions/ADR-225.md) section 5.** The line
that does the work is ruling 1: **a shipped comment may QUOTE a corpus document
naming a control this tree does not have, provided it names its own discharge in
the same comment; it may not ASSERT the control exists.** That sorts the six.
Five quote and name their discharge. One asserted.

**The `VG-12` admission was refused on the gate and not on the merits**
(ruling 5). [`P1-SE-trading-calendar:38`](../plans/P1-SE-trading-calendar.md):
*"VG-12 makes a new dependency a human admission a session cannot grant
itself"*. Both precedents record a grant the founder DELEGATED; row `225`
delegates nothing. **So the entry recommends and does not decide.**

### 3. The six, and why only one was a defect

| Site | Verdict |
|---|---|
| `packages/psp/src/amount.ts` | **THE MODEL.** Declines the schema and rebuilds the guarantee. Paragraph unchanged, pointer added |
| `apps/api/src/routes/checkout.ts` | Quotation naming its own discharge. One clause |
| `apps/api/src/events.ts` | Quotation naming its own discharge. One clause |
| `packages/rules-engine/src/plan/validate.ts` | Quotation naming its own discharge. One clause |
| `packages/rules-engine/test/generators/plan-config.ts` | **THE SIXTH, named in no prior document.** One clause |
| `apps/portal/src/api/types.ts` | **THE DEFECT.** Repaired in place, refusal kept |

### 4. What the session found that the row did not name

1. **The citations are six, not five**, and the reason is a case-sensitive
   search meeting a house style that shouts.
2. **`0004_catalog.sql:63` is false in SUBSTANCE and not only in mechanism.**
   `plan_versions.rules` is INSERTed on a check that it is a JSON object
   (`admin-writes.ts:1421`); `validatePlan` runs at PUBLISH (`:1549`).
   **Registered, not repaired: `admin-writes.ts` is not in this fence.**
3. **`validatePlan` is not a schema.** It reads named keys and refuses nothing
   for being unknown, so the rules object can carry a key no validator names.
   That is what makes the portal's repaired reason stronger than the one it
   replaced.
4. **The only zod claim with no hand-written substitute is the OpenAPI
   generator**, and `grep -rin "openapi\|swagger" apps packages --include=*.ts`
   matches three files, none of which generates anything.
5. **`type Validated<T>` is declared NINE times across nine route files and the
   nine are byte-identical once whitespace is normalised.** That copy count is
   the strongest argument on the side the entry does not recommend.
6. **THE COMMENTS COST LINE NUMBERS AND IT IS DECLARED.** Six files grew:
   `checkout.ts` **+8**, `events.ts` **+10**, `amount.ts` **+9**,
   `validate.ts` **+5**, `types.ts` **+17**, `plan-config.ts` **+7**.
   **`RI-15` found two and five were repointed**, the two it named plus three
   in the same reason blocks. **It reads only code as the CITING side**
   (`CITED_REASON_EXTENSIONS`, `repo-invariants.mjs:2288`), so markdown
   `file:line` citations are outside every check here and are NOT repointed:
   they span more than twenty ADRs and an ADR is a dated record.
   **Registered, not repaired.**
7. **A finding registered against `STRATEGY:192`, WITHDRAWN by the dispatcher
   at the merge.** This log claimed *"a session cannot grant itself"* is cited
   as `STRATEGY:192` by ADR-116, ADR-127, `pnpm-workspace.yaml`, STATE and
   three session logs, and that the line carries no such sentence. **The line
   carries it**: `docs/testing/STRATEGY.md:192` is the `CI-09` row and its
   Stryker paragraph reads *"the `VG-12` admission, which a session cannot
   grant itself"*, so every one of those citations resolves. The evidence was
   misread twice: `grep -n "grant" docs/testing/STRATEGY.md` returns FOUR
   lines and not three, and 192 is among them.
   `P1-SE-trading-calendar:38` states the rule in its own voice and
   `STRATEGY:192` quotes it. **Two sources for one sentence is not a broken
   citation, and this is the entry's own defect class produced while writing
   the entry.**

### 5. Counts, derived at reporting time

**33 of 33** gates. **19 of 19** invariants, read off the runner's own last
line. Typecheck clean, lint 0, `format:check` clean. Vitest as reported in the
verification section of the pull request. **The invariant runner reports 18 of
19 on a tree with no `node_modules`**, because `RI-18` shells out to
`typescript`; `pnpm install --frozen-lockfile` first and it is 19.

**Landmines.** `RI-18` errors rather than fails without an install, and the
last line still reads as a count. Read the non-`PASS` lines, not the total.

**Files touched.** `docs/decisions/ADR-225.md` (new), `ALLOCATION.md`,
`decisions/README.md`, `STATE.md`, this log, `sessions/README.md`,
`apps/api/test/checkout.test.ts` and `apps/api/test/wiring.test.ts` (five line
numbers repointed, nothing else), and six comments: `apps/portal/src/api/types.ts`, `apps/api/src/events.ts`,
`apps/api/src/routes/checkout.ts`,
`packages/rules-engine/src/plan/validate.ts`, `packages/psp/src/amount.ts`,
`packages/rules-engine/test/generators/plan-config.ts`.

**Next.** The founder's answer to ruling 5. Then, on B, the two migration
corrections ride the next migration that has business touching those tables;
on A, nothing is owed and the conversion is sequenced as its own slice.

**Blockers.** None. The entry is UNSIGNED by design.
