## 2026-08-14 - Session 16: the builder/reviewer loop, formalized as a citation check
<!--index: the builder/reviewer loop, formalized as a citation check -->

- **Done.** Non-money infrastructure. Two deliverables and a proposal the founder rules on.
  - **[`.claude/agents/reviewer.md`](../../.claude/agents/reviewer.md) written.** Its system prompt loads [GUIDE_BRIEFING](../GUIDE_BRIEFING.md) and [DECISIONS](../decisions/README.md), and its task on any diff or plan is: **for every factual claim, locate the primary source and quote `file:line`. Any claim that cannot be cited IS the finding.** It does not assess whether reasoning is sound; it assesses whether the facts are real. Carries the primary-source hierarchy (the migration beats the architecture document, the ADR beats a plan citing it, the registry beats a prose tally), a six-step procedure, four verdict statuses (**CITED, UNCITED, CONTRADICTED, UNRESOLVABLE**), and the limits: never edits, never ranks severity, never recommends, never substitutes for the E2 read.
  - **Verdicts land at `docs/reviews/<item>-verdict.md`**, one per item, overwritten rather than accumulated. The directory is created by the first verdict.
  - **[ADR-033](../decisions/ADR-033.md) records it**, with the five errors of 2026-08-14 as the rationale and the citation for what caught each.
  - **Renumbered 2026-08-14, after this entry was written: this ADR was `ADR-031` and is now `ADR-033`.** `claude/schema-delta-reconciliation` claimed 031 and 032 for its `published_statistics` rulings from the same `main`, and neither branch could see the other. **Founder ruling: that branch keeps its numbers** because they are cited inside migration comments, `DELTA_MANIFEST` and `DATA_MODEL`, and a migration is sacred once merged. The references above are rewritten rather than annotated, because an ADR number is an identifier and a stale one now points at a different ruling. **The allocation table and `CI-06f` exist so this cannot recur.**
  - **[INDEX](../INDEX.md) carries the agent** in the Root table, per CI-06c.
- **Next.** The founder's ruling on the proposed `Stop` hook (below). Independently: the **E2 line-by-line read** on the sixteen money-path migration files on `claude/schema-delta-reconciliation`, which is the natural first item to point the reviewer at.
- **Blockers.** None. **One founder decision pending**, and it is a decision rather than a blocker.
- **Landmines.**
  - **The proposed `Stop` hook is a reversal of a recorded decision and is therefore NOT implemented.** Constitution C10 mandates a blocking `Stop` completion gate ([MERIT_BUILD_MASTER_PROMPT.md:371](../../MERIT_BUILD_MASTER_PROMPT.md)); [CLAUDE.md:22](../../CLAUDE.md) records that the committed hooks **exit zero rather than blocking, on purpose**, so a network outage cannot wedge a session. **Both are correct about different failure modes.** Any implementation needs a ruled answer to "what happens when the check itself cannot run", which is a founder decision. Writing the hook without that ruling would have been the exact failure ADR-033 exists to prevent.
  - **The session was briefed on "three unauthorized float exemptions" and the record carries two.** `published_statistics.numerator` and `.denominator` shipped outside the authorization; the third non-integer column, `value_numeric`, **is authorized and was left authorized**. Three non-integer columns exist, two were unauthorized. Recorded in ADR-033 rather than smoothed, because an uncited number inside an ADR about uncited numbers is the finding.
  - **Two merge conflicts with `claude/schema-delta-reconciliation` are expected and both are trivial.** Both branches append to the end of [DECISIONS.md](../decisions/README.md) and to the end of this file. **Resolve by keeping both blocks in order.** ADR numbering was chosen to make this safe: 026 to 030 are that branch's, this one is **031**.
  - **[STATE.md](../STATE.md) was deliberately not touched.** It is being rewritten wholesale by the reconciliation branch, this session changes none of the nine surviving items, and the one pending decision is carried by ADR-033 and by this entry. **A conflict in STATE buys nothing here**, and the end ritual's purpose is met by the handoff record rather than by the edit.
  - **The reviewer is available and mandatory nowhere.** Until the hook question is ruled, invoking it is a session's judgment. **A PASS verdict is an input to the founder's E2 read and never a clearance**, which the agent definition states in those words, because the failure mode of any review artifact is that a later reader treats it as one.
- **Files touched:** `.claude/agents/reviewer.md` (new); `docs/DECISIONS.md`; `docs/INDEX.md`; `docs/SESSION_LOG.md`.

---
