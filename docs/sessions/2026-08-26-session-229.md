---
status: approved
depends_on: [README.md]
last_updated: 2026-08-26
---

## 2026-08-26 - Session 229: `RI-08`, the invariant three entries named and none wrote
<!--index: **[ADR-117](../decisions/ADR-117.md) lands, `status: proposed`, approval line UNSIGNED, NOT MONEY PATH by file and the hole it closes is a money-path hole.** [ADR-096](../decisions/ADR-096.md) section 9 named `RI-08`, measured the hole and assigned it to `P4-e`; `P4-e` landed without it, [ADR-095](../decisions/ADR-095.md)'s `F4` and section 9 item 5b cite the same gap, and **three entries had cited it with none closing it.** **THE SCOPE IS THE ONLY THING RULED AND IT IS A WIDENING OF A SIGNED ENTRY.** ADR-096 scopes the check to `apps/site`, widens itself one sentence later to `packages/queue` and calls the wider version *"the better version of it"*; ADR-095 item 5b names the same under-scoping from the framework side. **FIVE manifests now state the accessor's absence as a design property where ADR-096 counted two, and ZERO packages in this workspace declare `@merit/db` in any dependency field**, so the check guards EVERY workspace package against an admission list that is **EMPTY**, which is `onlyBuiltDependencies: []`'s shape and [ADR-084](../decisions/ADR-084.md) section 5's. **`apps/api` is NOT pre-admitted** though [ADR-109](../decisions/ADR-109.md) says it will need the accessor first: a list that admits against an unstated need is a list joining itself. **THE MANIFEST GATE WAS MEASURED IN BOTH DIRECTIONS RATHER THAN ASSUMED**, and it contradicts a line in the tree: an undeclared `@merit/db` import reports **`TS2307` from `apps/site` AND from `apps/api`**, with no `paths` mapping anywhere, so the manifest field is not merely where the dependency is recorded but the only place the capability can be acquired -- which is why `RI-08` has no source half, and which makes [`apps/api/package.json:15`](../../apps/api/package.json)'s *"`tsc` resolves it happily through the workspace"* **wrong on this tree** (out of fence, reported). **THREE READINGS OF "RESOLVES TO THE ACCESSOR", EACH SEEDED ON THE REAL TREE AND WATCHED FAILING**: the key, a `workspace:`/`npm:` alias parsed out rather than substring-matched, and a `link:` path that writes the accessor's name NOWHERE. **BOTH NEAR-MISSES WATCHED STAYING SILENT**: `@merit/dbtools`, the substring trap, and an ordinary workspace dependency. **FALSIFIED BEFORE IT WAS TRUSTED**, which is `RI-10`'s landmine paid off: with `acquires` neutered the six detection cases go red and the two silence cases stay green. **TWO GUARDS THROW RATHER THAN PASS** and they are about the admission list, because that is the direction this check gets weakened in: a stale name, and a list covering every package. **`ALLOCATION` GAINS A FIFTH TABLE, FOR `RI-nn`, and it is an ALLOCATOR and says so**: `gates.mjs`'s `PENDING_SERIES` names the tooling README as `RI`'s register, that README is out of fence and its table stops at `RI-05`, so the pending entry stands. **`CI-06/identifier-series` was probed in BOTH shapes before the table was written, ten rows and five, and both reported 30 of 30.** **FOUR FINDINGS REPORTED RATHER THAN TAKEN.** 10 of 10 invariants, 30 of 30 gates, 134 files / 2,256 passed / 1 skipped, `pnpm run verify` exit 0. **NOT MERGED.** -->

- **Objective:** write `RI-08`, the invariant [ADR-096](../decisions/ADR-096.md) section 9 named and nobody wrote, export it, and watch it fail on a seeded violation in both directions.

- **Done:** [`packages/tooling/checks/repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) gains `RI-08` and exports it in `CHECKS`; [`packages/tooling/test/repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts) gains eight seeded cases and two throw guards; [ADR-117](../decisions/ADR-117.md) rules the scope at `status: proposed`, approval line UNSIGNED; [ALLOCATION](../decisions/ALLOCATION.md) gains the `RI-nn` table and its `117` row is amended in place.

### The hole, re-measured rather than inherited

[ADR-096](../decisions/ADR-096.md) section 9's own words: *"A marketing page can acquire a write-capable connection to the trader database in a one-line manifest diff, and every check in this repository stays green while it happens."* Three entries cited it. `grep -n "RI-08" packages/tooling/checks/repo-invariants.mjs` on `main` returned **no output** and the runner reported **9 of 9**.

**Session 210 is not the failure and the entry says so.** `P4-e` wrote [`apps/site/test/manifest.test.ts`](../../apps/site/test/manifest.test.ts), scoped to the one package its fence held, enumerated three things a green run must not be read as covering, and handed the general case on in writing: *"The hole is narrower after this file and it is still open."* **That is what made the gap findable.**

### Why the scope widened, which is the only thing ADR-117 rules

`grep -rn '@merit/db' --include=package.json .` outside `node_modules`, counted here:

| Manifest | Its own `//` key | Entry |
|---|---|---|
| [`packages/queue`](../../packages/queue/package.json) | *"IT DECLARES NO WORKSPACE DEPENDENCY, AND `@merit/db`'s ABSENCE IS THE DESIGN"* | [ADR-086](../decisions/ADR-086.md) |
| [`packages/ledger`](../../packages/ledger/package.json) | the same sentence, *"on `packages/queue`'s own precedent"* | [ADR-104](../decisions/ADR-104.md) |
| [`packages/psp`](../../packages/psp/package.json) | *"It declares no `@merit/db` either"* | [ADR-105](../decisions/ADR-105.md) |
| [`apps/api`](../../apps/api/package.json) | *"THERE IS STILL NO `@merit/db` HERE and its absence is session 219's finding"* | [ADR-109](../decisions/ADR-109.md) |
| [`apps/site`](../../apps/site/package.json) | ADR-096 ruling 2 makes the absence a DECLARED property | [ADR-096](../decisions/ADR-096.md) |

**ADR-096 counted two and called it *"a second instance rather than a new class"*. It is a class, and it grew by three in the eight days while nothing checked any of them.** A check naming two of those five would have been silent about the other three on the day it was written, which is `RI-04`'s own recorded drift when `apps/api` was absent from `DEPLOYABLES` and *"`RI-04` reported PASS while asserting nothing whatsoever about `apps/api`"*.

### The seeds, on the REAL tree, `apps/site/package.json` and nothing else changed

```
"@merit/db": "workspace:*"
  FAIL RI-08 ... dependencies.@merit/db is `workspace:*`, which names it directly

"db": "workspace:@merit/db@*"
  FAIL RI-08 ... dependencies.db is `workspace:@merit/db@*`, which aliases it as `db`

"db": "link:../../packages/db"
  FAIL RI-08 ... dependencies.db is `link:../../packages/db`, which links its directory as `db`

"@merit/dbtools": "workspace:*"        PASS   1 of 1 invariants hold
"@merit/rules-engine": "workspace:*"   PASS   1 of 1 invariants hold
```

**Every seed was removed and `git status --porcelain` reported only the check file after.** The third form is the one that matters most: **neither the key nor the specifier writes `@merit/db` anywhere**, so a name test of any kind is blind to it and only resolution finds it. That is why `acquires` is three named readings rather than `spec.includes(name)`, which reports `@merit/dbtools` and misses the link.

**`RI-10`'S LANDMINE PAID OFF.** It shipped once with `relative` used but not imported, a latent `ReferenceError` that could only fire on a real finding. The three seeds above printed full finding messages, so every line of `RI-08`'s finding path executed. **Then the check was falsified**: with `acquires` neutered to return `null`, the six detection cases go red and the two silence cases stay green.

### One thing measured that contradicts a line in the tree

[`.npmrc`](../../.npmrc)'s `node-linker=isolated` makes an undeclared import unresolvable at RUN time. **It does not resolve at BUILD time either**, seeded once into `apps/site/src` and once into `apps/api/src`, no manifest line, no `paths` mapping anywhere in the tree:

```
src/__seed__db.ts(1,26): error TS2307: Cannot find module '@merit/db' or its
  corresponding type declarations.
```

**Both packages.** So the manifest field is the only place the capability can be acquired, which is why `RI-08` has no source half. [`apps/api/package.json:15`](../../apps/api/package.json) says `tsc` *"resolves it happily through the workspace"*; its conclusion survives and its stated reason does not. Out of fence, unedited, reported.

### What is reported rather than resolved

| # | Item | Whose |
|---|---|---|
| 1 | `apps/api/package.json:15`'s `tsc` sentence, measured false above | a slice holding that manifest |
| 2 | **A raw driver DECLARED in a manifest and not yet imported is refused by nothing.** `merit/no-raw-db-client` reads source, so `"pg": "catalog:"` is green until the first import. **This is `RI-08`'s shape pointed at `CLIENT_FAMILIES` and deliberately not smuggled in here** | an entry of its own |
| 3 | **[`packages/tooling/README.md`](../../packages/tooling/README.md)'s invariant table stops at `RI-05` and calls `RI-04` *"four deployables"* when `DEPLOYABLES` holds five.** `gates.mjs`'s `PENDING_SERIES` names exactly this, holding `RI` for *"a table in the tooling README; members have no row"*. Out of fence, unedited | a slice holding that README |
| 4 | [`scripts/ci/falsify-ci.mjs`](../../scripts/ci/falsify-ci.mjs) watches `RI-01` fail on a seeded `@merit/db` dependency and has **no `RI-08` case** | a slice holding `scripts/ci/` |

### What was NOT done, named rather than implied

**No application code, no package manifest, NO MIGRATION.** `git status --porcelain packages/db apps` reports zero on this branch. **`packages/tooling/README.md` was not edited** even though it is in `packages/tooling`: the fence names two files there and this is not one of them, and finding 3 is the honest form of that. **`gates.mjs` was not edited**: the `RI-nn` table was probed against `CI-06/identifier-series` in both shapes precisely so it would not need to be.

### Verification

| Command | Result |
|---|---|
| `node packages/tooling/checks/repo-invariants.mjs` | **10 of 10 invariants hold** |
| `node scripts/corpus/gates.mjs check` | **30 of 30 gates pass** |
| `pnpm exec vitest run` | **134 files, 2,256 passed, 1 skipped** |
| `pnpm run verify` | **exit 0** |
| `git status --porcelain` | clean |

### Plan

`RI-08` is written, exported and watched failing in both directions. **The pull request is open and NOT merged.** The four findings above are each somebody else's fence and none was taken.
