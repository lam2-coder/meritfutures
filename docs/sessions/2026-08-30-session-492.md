---
status: approved
depends_on: [README.md]
last_updated: 2026-08-30
---

## 2026-08-30 - Session 492: one predicate, three statements, one of them wrong on the money in both directions

<!--index: **THE RULING IS [`ADR-302`](../decisions/ADR-302.md), `proposed` and UNSIGNED. MONEY PATH, BUILD, `E2` READ OWED, AND THE READ IS OF THE DIFF RATHER THAN OF A DECISION BECAUSE ONE OF THREE LEGS CHANGES BEHAVIOUR.** The dispatched divergence was re-derived at source before anything was repaired and **IT HOLDS, AND IT IS LARGER THAN THE ROW SAID BY ONE FIELD**: `readCapSchedule` ([`routes/catalog.ts:1219`](../../apps/api/src/routes/catalog.ts) on `origin/main`) tested `Number.isInteger`, TRUE of `2 ** 53 + 1`, then converted with `BigInt`, so a `cap_cents` past `Number.MAX_SAFE_INTEGER` was **ADMITTED AND HANDED BACK ROUNDED**; it **REFUSED the base-10 string** [ADR-283](../decisions/ADR-283.md) ruling 5 blessed; and it held **`from_ordinal`** to `Number.isInteger` where `toCapScheduleCents` and `decodeCapSteps` both held it to `Number.isSafeInteger`. **ALL THREE OF THE ROW'S LINE CITATIONS WERE EXACT ON `origin/main`**, which is reported because the row warned they might not be. **THE COLLAPSE LANDED AS ONE COMMIT AND THAT IS HOW THE NAMED TRAP WAS DISARMED**: `decodeCapScheduleCents` ([`packages/rules-engine/src/plan/cap-schedule-codec.ts:218`](../../packages/rules-engine/src/plan/cap-schedule-codec.ts)) was added, exported, and all three readers deleted with their call sites repointed in `7cb2f70`, **so no commit on this branch holds the FOUR statements [ADR-286](../decisions/ADR-286.md) and [ADR-269](../decisions/ADR-269.md) refused.** The tests landed second, on a tree already correct. **THE COLLAPSE IS ASYMMETRIC AS RULED**: `apps/worker` and `apps/site` are behaviour-preserving on what they admit and refuse and change only the CLASS of the refusal, which `decodeEngineGates` has done unwrapped through both those adapters since [ADR-250](../decisions/ADR-250.md); **`apps/api` GAINS the refusal of an unsafe `cap_cents` and of an unsafe `from_ordinal` and LOSES the refusal of the base-10 string.** **THE HARM IS TRACED TO THE TRADER RATHER THAN ASSERTED, AND THE HONEST VERSION IS NARROWER ON ONE HALF AND WIDER ON THE OTHER**: the endpoint publishes only the ordinal-1 rung through `capAtFirstOrdinal` and `centsToJson`, which refuses a `bigint` past the same ceiling, so the ROUNDED value was caught one step later and answered 500 for a different reason, and a rounded rung at ordinal two or above was admitted and silently never rendered; **the STRING half is what was costing something, and it cost the WHOLE PUBLIC CATALOGUE**, a 500 on every plan for a row `apps/worker` and `apps/site` read fine. **BOTH HALVES WERE REACHABLE**: [`0004:168`](../../packages/db/migrations/0004_catalog.sql) is `jsonb NOT NULL` with no CHECK on its contents and [`admin-writes.ts:1767`](../../apps/api/src/routes/admin-writes.ts) passes the steps through unchecked. **THE STOP CONDITION IS MET**: link 7's cap-schedule census reads **ONE**, and the divergence case [ADR-286](../decisions/ADR-286.md) wrote as RED-on-repair went red and is rewritten to assert the repaired property in both directions. **[ADR-286](../decisions/ADR-286.md) SECTION 7 ITEM 1 IS CLOSED.** **A SECOND FINDING, IN THE GATE THAT WAS SUPPOSED TO CATCH THIS**: two of that census's three per-file needles quoted a `from_ordinal:` line that exists VERBATIM a second time in the same file, in the BASIS-POINT reader, so each would have stayed green on a file whose cents reader had been deleted; found by running it rather than by reading it, repaired to `cap_cents` lines, and the two basis-point readers asserted still present so a later session does not read this collapse as reaching them. **NO MIGRATION, NO `RI-nn` MINTED, `RI-38` LEFT FREE, `packages/db/**` UNTOUCHED, `CATALOG_TABLE_KEYS` NOT WIDENED, THE SIZE ROW NOT TAKEN, `API_CONTRACT` NOT EDITED, NOTHING WIRED AND NO WIRING COUNT MOVED.** Counts derived at reporting time, each command run separately: **33 of 33** gates after `generate`, **31 of 31** invariants, **299 files / 7,290 passed / 6 skipped** against session 487's 298 / 7,249 / 6, a delta of **+1 file and +41 cases, all of them this row's codec suite**; typecheck 0, lint 0, `format:check` clean. **`pnpm run verify` was NOT run**, forbidden by the row. **`node_modules` was absent on arrival and was restored by `pnpm install --frozen-lockfile`; its absence was never committed.** **NO GITHUB ACCESS: NO PULL REQUEST WAS OPENED AND NOTHING WAS MERGED.** The branch is pushed and left mergeable; `origin/main` moved during the session and was FETCHED AND MERGED IN, never rebased, and every gate was re-run on the merged tree. -->

---

### 1. The objective

**[ALLOCATION](../decisions/ALLOCATION.md) row `302`: collapse the three readers of `plan_version_sizes.payout_cap_schedule_cents` onto one codec in `packages/rules-engine`, repairing the divergence in `apps/api`'s copy.** One objective, money path, build. `ADR-299` section 7's row, dispatched ahead of the door row.

### 2. What was read before anything was written

Row `302` in full, then `CLAUDE.md`, [STATE](../STATE.md) and the tail of the session logs. Then **`ADR-299` section 7 in full, read from `origin/claude/a-decoder-with-no-home` with `git show`**, because that entry has not landed on `main` and the row said it might not have.

Then, at source rather than through any entry: **all three readers and every primitive each is built from** (`jsonCents`, `jsonInteger`, `jsonField`, `jsonList`, `jsonRecord`; `bigintCents`, `integer`, `field`, `list`, `record`; `asRow` and the inline checks), `SizeCapScheduleStep` and both local step types, `capAtFirstOrdinal` and `centsToJson`, the size-row write path in `admin-writes.ts`, `0004_catalog.sql`'s declaration of the column, `rules-codec.ts` in full as the nearest precedent, the engine's `index.ts` export block and its `ADR-078` rationale, and link 7 of `rule-state-producibility.test.ts` together with the `usePayoutBackend` reason it asserts against.

### 3. Done

- **`packages/rules-engine/src/plan/cap-schedule-codec.ts`**, new. `decodeCapScheduleCents` plus `CapScheduleCodecError`, exported from the package door with the `ADR-078` argument written out.
- **All three readers routed through it and deleted**, in the same commit: `toCapScheduleCents`, `decodeCapSteps`, `readCapSchedule`.
- **`packages/rules-engine/test/plan-cap-schedule-codec.test.ts`**, new, 41 cases, spined on a round trip against `published-plans-in-code.ts`'s independent transcription of Appendix A. Section 4 executes the divergence rather than describing it.
- **Link 7's census rewritten to ONE** and its divergence case rewritten to the repaired property, which is the row's stop condition.
- **[ADR-302](../decisions/ADR-302.md)**, twelve sections, `proposed`, **UNSIGNED**.
- **[ALLOCATION](../decisions/ALLOCATION.md)**, [STATE](../STATE.md), [INDEX](../INDEX.md), the two registry READMEs and this log.

### 4. Next

**Row `302` is complete and the branch is left mergeable.** **Row `303` is unblocked**, which is `ADR-299` ruling 1's door row over `packages/db/src/scoped-db.ts`. **The rules-blob census is untouched at three** and its collapse is still [ADR-283](../decisions/ADR-283.md) section 7's row. **The `PlanVersionSizeRow` rename is still owed and still not dispatched**, and this row held `routes/catalog.ts` and correctly declined it: `ADR-299` section 8 rules a rename with no other subject costs a review and moves nothing, and folding it into a money-path diff would spend an `E2` read on it.

### 5. Blockers

**One, and it is environmental rather than about the work: THIS SESSION HAS NO GITHUB ACCESS, SO NO PULL REQUEST WAS OPENED.** `gh` is not installed and the API answers `403 GitHub access is not enabled for this session`. **The branch `claude/one-of-three-rounds` is pushed and left mergeable**, `origin/main` was fetched and merged in rather than rebased when it moved mid-session, and every gate was re-run on the merged tree. Session 487 met the same wall. **Nothing is blocked on the ruling itself.** [ADR-302](../decisions/ADR-302.md) ships `proposed` with an UNSIGNED approval line and merging is not signing. Section 12 names the three things a founder read decides: the error-class change, which is the only unrequested observable change in the diff; whether section 5's honest narrowing of the rounding half reads as a walk-back of the dispatch, which it is not; and **whether the census-needle defect in section 7 is load-bearing in the other censuses of that file**, which this row did not have the fence to ask.

### 6. Landmines

- **TWO OF LINK 7's THREE CAP-SCHEDULE NEEDLES WERE NOT UNIQUE TO THE READER THEY NAMED, AND THE PATTERN MAY NOT BE LOCAL.** `from_ordinal: jsonInteger(jsonField(source, 'from_ordinal', where), ...)` and `from_ordinal: integer(field(source, 'from_ordinal', at), ...)` each appear **twice** in their own file, once for the cents schedule on the size row and once for the basis-point schedule in `plan_versions.rules`. A `toContain` on either was satisfied by the wrong function. **The `'cap_cents'` quoted-key sweep beside them was sound and is what actually held the count.** Repaired here; **not audited across the file's other censuses.**
- **THE BASIS-POINT CAP SCHEDULE IS STILL STATED THREE TIMES AND LOOKS EXACTLY LIKE THE ONE THAT WAS JUST COLLAPSED.** `toCapScheduleBp` (`apps/worker`), `decodeCapScheduleBp` (`apps/site`) and `decodeCapSchedule` (`rules-codec.ts`) read `cap_bp` out of `plan_versions.rules`. **It is [ADR-283](../decisions/ADR-283.md) section 7's row and not this one**, and link 7 now asserts the two deployable-side ones are still present so this collapse is not misread as having reached them.
- **`plan_version_sizes.payout_cap_schedule_cents` HAS NO CHECK ON ITS CONTENTS AND THE ADMIN WRITE PATH CHECKS ONLY THAT IT IS AN ARRAY.** [`0004:168`](../../packages/db/migrations/0004_catalog.sql) and [`admin-writes.ts:1767`](../../apps/api/src/routes/admin-writes.ts). The read side is now one predicate; **the write side still admits any step objects at all**, including a `cap_cents` this codec will refuse on the way back out. That is a row nobody has, and it is where the next sighting of this value will come from.
- **THE ERROR CLASS CROSSING TWO DEPLOYABLES IS THE ONE UNREQUESTED OBSERVABLE CHANGE IN THIS DIFF.** `apps/worker` and `apps/site` now throw `CapScheduleCodecError` where they threw `BatchRowError` and `SiteAdapterError`. Nothing in either branches on the class and `decodeEngineGates` set the precedent, but a future handler wanting to tell "the row is malformed" from "the response is malformed" gets one class for both.
- **"THE ROUNDED DOUBLE IS SERVED" IS ONE STEP STRONGER THAN WHAT WAS TRUE, AND THE ENTRY SAYS SO.** `centsToJson` refuses a `bigint` past the same ceiling, so the ordinal-1 rung 500'd rather than publishing a rounded cap, and a rounded rung at ordinal two or above was never rendered at all. **The reader really did admit and round it**; the guard that caught it is one function later and exists for a different reason. Anyone restating the finding should restate that qualification with it.

### 7. Fence

**Held.** `packages/rules-engine/src/plan/cap-schedule-codec.ts` (new), `packages/rules-engine/src/index.ts`, `packages/rules-engine/test/plan-cap-schedule-codec.test.ts` (new), `packages/rules-engine/test/evaluate.golden.test.ts`, `apps/worker/src/batch/adapter.ts`, `apps/site/src/catalog/adapter.ts`, `apps/api/src/routes/catalog.ts`, `apps/api/test/rule-state-producibility.test.ts`, `apps/api/test/wiring.test.ts`, `ADR-302`, [ALLOCATION](../decisions/ALLOCATION.md), [STATE](../STATE.md), [INDEX](../INDEX.md), this log and the two registry READMEs, **which are GENERATED by `node scripts/corpus/gates.mjs generate` and not hand-written**.

**`wiring.test.ts` IS THE ONE ENTRY IN THAT LIST WHOSE ADMISSION IS A READING RATHER THAN A NAME**, and [ADR-302](../decisions/ADR-302.md) section 11 states it: it is not a test of `catalog.ts`, it holds the `usePayoutBackend` reason that link 7 asserts against, and **this diff made two of its sentences false**. Correcting a statement this diff falsified is not widening a fence; leaving it would have left the tree asserting its own repaired defect is live. No other subject in that file was touched.

**`packages/db/**` WAS NOT READ AND NOT EDITED except for `0004_catalog.sql`'s declaration of the column, read once to establish that the shape is unconstrained; `git diff origin/main --stat -- 'packages/db'` is EMPTY.**
