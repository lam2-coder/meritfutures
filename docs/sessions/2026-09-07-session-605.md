---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 605: the producer moves to packages/ledger, the move was refused under the fence as dispatched and taken under one granted for it, and the path it vacated turned out to be what three suites, three pointers and one register used to find it

<!--index: **THE ROW IS [ADR-410](../decisions/ADR-410.md), `proposed` and UNSIGNED. NON-MONEY. THE PRODUCER MOVED, NO MANIFEST EDITED, NO SINK WIRED.** **[ADR-408](../decisions/ADR-408.md) SECTION 6's TABLE REPRODUCES EXACTLY**, re-derived from both manifests: `@merit/db`, `@merit/ledger` and `@merit/rules-engine` are the whole intersection, so the move needed no new package, no manifest edge and no `VG-12` admission. **THE HOME IS RULED `packages/ledger`** on the writer's shape, which is [`tx.ts`](../../packages/ledger/src/tx.ts)'s construction reached independently one deployable over. **`@merit/db` WOULD HAVE MADE THE WIRING IT EXISTS TO ENABLE ILLEGAL** under `apps/worker/test/digests.test.ts` case 10.2; **`@merit/rules-engine` LOSES ON ITS ZERO-I/O CONTRACT AND NO GATE WOULD HAVE CAUGHT IT**, `merit/engine-purity` having been RUN over the producer placed there and reported nothing, the probe proved live by a seeded `new Date()`. **THE MOVE WAS REFUSED UNDER THE FENCE AS DISPATCHED AND THE REFUSAL WAS RUN RATHER THAN REASONED**: **7** files and **12** cases red, **5** files and **7** cases outside every fence that wave handed out, because three suites under `apps/worker/test` recover the catalogue by searching the OLD PATH's TEXT and no module left behind can satisfy them. **THE DISPATCHER READ THAT AND WIDENED THE FENCE RATHER THAN ARGUING WITH IT**, naming five files; the move is taken, the two pointers repaired by REMOVING THE COORDINATE ([ADR-404](../decisions/ADR-404.md)). **ONE REFUSAL SURVIVES THE GRANT**: a second declaration of `buildEvent` would keep an `RI-15` anchor green in the file the move exists to empty, refused before the widening and after. **A SEVENTH BINDING APPEARED THAT NO FENCE COULD ANTICIPATE**: `RI-35`'s install probe reads a barrel's re-export LIST as a value position, so it **cannot tell a publication from an install**; answered in the barrel with the reason recorded there, the probe untouched, the correct `sweptBy` registration named as owed. **`apps/worker` IS NOT WIRED**, deliberately. **SEVEN LEGS WATCHED RED**, every seed restored byte-identical by `sha256sum`; section 4's good-news leg UPDATED to pin the carrier set to a singleton rather than deleted. **SIXTEEN STALE PROSE SENTENCES ACROSS TEN `apps/worker` FILES ARE REGISTERED BY A CASE RATHER THAN REPAIRED.** **NO GATE ADDED, WIDENED OR WEAKENED, NO TEST SKIPPED, NO INVARIANT MINTED, NO MIGRATION, NO `CI-06` LETTER, NO MANIFEST EDITED.** -->

### 1. The objective

Take the relocation [ADR-408](../decisions/ADR-408.md) section 6 priced and did not take: re-derive which `packages/*` addresses both deployables already reach, choose the home for the event producer on the merits, move it there and point the old path at it, so the deployable holding the `SystemTx` can reach the sink. One objective, non-money.

### 2. Done

- **The container measured and reported.** No repository on arrival, eleventh row running. `/home/user` held `.` and `..`, and a `find` for any `.git` under it returned nothing. Cloned by this row: **17.824s real**, **NOT shallow**, **4,348** commits, **185M** of `.git`. The first `git push` was refused 403 by the proxy as an unauthorized repository, and the repository was attached and the push retried once.
- **The base reproduced at `08b9acf2`** on an empty `git status --porcelain`: gates **33 of 33**, invariants **35 of 35**, suite **337 / 8,141 / 10 / 0**, typecheck, lint and `format:check` clean. `generate` reported *every span already matches its query*.
- **Section 6's table re-derived from both manifests** and it reproduces exactly.
- **The home ruled `packages/ledger`, with both losers refused at a source rather than on taste**, including running `merit/engine-purity` over the producer placed in `packages/rules-engine` and proving that probe live before believing its silence.
- **The move PERFORMED and the whole suite run against it**, which is the measurement no prior record of this blocker had.
- **The refusal reported**, the fence extension granted in answer, and the move then taken.
- **The five granted files repaired**, each to the narrowest change that clears its binding.
- **The producer's own header repaired where the move made it false**, every refuted clause kept beside its correction under `RI-14`.
- **The refusal message updated** so the address half of the blocker is recorded as spent and the install half is named, with both suites' assertions following the message rather than being deleted.
- **Section 4's good-news leg updated and section 6 rewritten** from a refutation into a record of the move, every case still failing on good news.
- **Seven legs watched RED**, every seed restored and confirmed byte-identical by `sha256sum`.

### 3. Refused

- **A SECOND DECLARATION OF `buildEvent` AT THE CITED COORDINATE.** It is the only shape that keeps `RI-15`'s anchor green without touching the citing file, and it would make an invariant report a pointer as verified exactly where the thing it names had moved. Refused before the fence widened and refused after it did. The pointer was repaired by removing the coordinate.
- **WIDENING `RI-35`'s PROBE, ADDING A SECOND EXCLUSION, OR WEAKENING `stripComments`.** All three were forbidden and none was taken. The false positive is answered in the barrel and the correct repair is named as owed.
- **WIRING `apps/worker`.** Forbidden by the row and a separate slice regardless.
- **ANY FILE NOT IN THE ORIGINAL FENCE OR THE ENUMERATED EXTENSION.** Sixteen stale prose sentences across ten `apps/worker` files are registered by a case rather than repaired.

### 4. The measurement that produced the refusal

| population | observed on the probe tree |
|---|---|
| test files failing | **7** of 337 |
| cases failing | **12** of 8,151 |
| inside the fence as dispatched | **2** files, **5** cases |
| outside every fence that wave handed out | **5** files, **7** cases |
| invariants | **32 of 35**: `RI-15` 3 findings, `RI-16` 2, `RI-35` 1 |
| the clearing condition | **six one-line edits across five files**, none of them anybody's under the original fence |

**THE DESTINATION WAS NEVER THE VARIABLE.** Every one of the six keys on the SOURCE path, so all three candidate homes produce the identical six and one probe answered all three.

### 5. The RED that was watched

| seed | file seeded | result |
|---|---|---|
| the two full-path pointers stripped | `apps/worker/src/schedule.ts` | RED |
| the searched declaration renamed to `EVENTX_CATALOGUE` | `apps/worker/test/schedule.test.ts` | RED |
| the exclusion constant repointed | `packages/tooling/checks/absence-claims.mjs` | RED |
| a second `packages/*` carrier minted | `packages/rules-engine/src/index.ts` | RED |
| a coordinate pointer put back into the old path | `apps/worker/src/schedule.ts` | RED |
| the exclusion constant put back | `packages/tooling/checks/absence-claims.mjs` | RED, two cases |
| the compatibility module widened to publish the writer | `apps/api/src/events.ts` | RED, three cases |

**ONE SEED PROVED NOTHING AND IS RECORDED RATHER THAN REPLACED QUIETLY.** Renaming a declaration to `EVENT_CATALOGUE_X` left `export const EVENT_CATALOGUE` intact as a substring and the case stayed green. It was corrected before the RED was believed.

**AND THREE RESTORES IN THE SECOND BATCH WENT TO `HEAD` RATHER THAN TO THE WORKING STATE**, silently undoing three of this row's own repairs. The next full run caught it, all three were reapplied and every leg re-derived after. `git checkout --` is the obvious restore instrument and it is the wrong one on a tree carrying uncommitted repairs in the same files.

### 6. Found and not repaired

| the finding | whose it is |
|---|---|
| sixteen prose sentences across ten `apps/worker` files still say the producer lives at the old path. None breaks and none is in the granted fence | whichever row next holds `apps/worker/**`. Bound by a case, so a new one lands red and so does a repair |
| `apps/worker/src/detectors/adapter.ts` builds a refusal message naming the old path and `apps/worker/test/detector-census.test.ts` asserts it, so the two must move together | the same row, in one change |
| `RI-35`'s register `site` is the old path, so the claim sentence stays at a compatibility module | `packages/tooling`'s, with section 7's `sweptBy` registration |
| dated pointers in four ADRs, `STATE.md` and one review carry coordinates into the old path | nobody's. [ADR-386](../decisions/ADR-386.md) names rather than repairs a pointer in a dated record, and `RI-16` reads none of them |
| ADR-408 section 9's vocabulary gap, three of nine emitted names buildable, is unmoved by the address decision | split four ways and named there |

### 7. Counts at reporting time

Every figure below was derived at the moment it was written, on this branch, each command run separately. `pnpm run verify` and `scripts/corpus/falsify.mjs` were **NOT run**, both forbidden by the row.

| Leg | Baseline at `08b9acf2` | After this row |
|---|---|---|
| Gates | **33 of 33** | **33 of 33**, unchanged because this row adds none |
| Invariants | **35 of 35** | **35 of 35**, unchanged because this row mints none |
| Suite | **337 files / 8,141 passed / 10 skipped / 0 failed** | **337 / 8,147 / 10 / 0**, a delta of **+6 cases**, all six in one section of one existing file |
| Typecheck, lint, `format:check` | clean | clean |

**NOT THE MONEY PATH.** The diff is one moved module, one barrel block, one compatibility module, three suites' path constants, two citation pointers, one register constant, two registry pointers, one test file's two sections, and the records, with **no manifest edited, no migration, no `.sql`, nothing under `scripts/db`, no schema delta and no financial value read or written.**
