---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 609: the sixteen reproduce, a second instrument finds eight more, and finishing the row turns a case red in a file the row may not write

<!--index: **THE ROW IS [ADR-414](../decisions/ADR-414.md), `proposed` and UNSIGNED. NON-MONEY. ELEVEN FILES UNDER `apps/worker` AND THE REGISTRIES, NOTHING ELSE.** **[ADR-410](../decisions/ADR-410.md) SECTION 9's FIGURE REPRODUCES EXACTLY**: sixteen prose sentences across ten files, seven under `src` and three under `test`, re-derived with the path matcher rather than inherited, out of nineteen references across eleven files once the live import and the coupled pair are separated out. **AND A SECOND INSTRUMENT FINDS EIGHT MORE THAT NO PATH MATCHER CAN SEE**: a sentence reading *"the producer one deployable over"* or *"no sink is reachable from this deployable at all"* states where the producer lives, names no path, and is the same claim in the same tense; eight such sites survive in [`schedule.ts`](../../apps/worker/src/schedule.ts), [`event-sink.test.ts`](../../apps/worker/test/event-sink.test.ts) and [`breaker.test.ts`](../../apps/worker/test/breaker.test.ts), one of them a TEST NAME. **ALL TWENTY-FOUR ARE REPAIRED, PLUS THE PAIR.** **THE ADDRESS WAS THE SMALLER HALF OF THE DEFECT**: every one of the sixteen sat inside a claim that this deployable could not REACH the producer, and each premise was checked at its own source: `RI-04` and `node-linker=isolated` are TRUE AND NO LONGER BITING because the producer is in a package `apps/worker` declares, *"declares `@merit/db` and `@merit/rules-engine` and nothing else"* was FALSE BEFORE THIS WAVE against a manifest carrying **four**, `db.test.ts` pins **five** permitted bare specifiers including `@merit/ledger`, and *"nothing under `packages/**` carries one"* is FALSE. **SO NO PATH WAS REPOINTED UNDER A FALSE REASON**: sentences name `TRANSACTION_EVENT_WRITER` and `@merit/ledger`, which are a declaration and a manifest line, and history clauses name the DEPLOYABLE and never re-plant the address, so the old path leaves `apps/worker` rather than surviving inside quotation marks. **ONE PARAGRAPH HAD ALREADY WRITTEN THE REPAIR THAT LANDED**, [`sweeps/expiry-adapter.ts`](../../apps/worker/src/sweeps/expiry-adapter.ts) closing its refusal by naming *"a package both arrows already reach"*, and it is kept and marked under `RI-14` rather than deleted. **THE PAIR'S RED WAS WATCHED FIRST**, the producer side repaired alone and `detector-census.test.ts` failing 1 of 17 with the whole message in the output, both files then restored byte-identical by `sha256sum -c`; the assertion moved from a PATH to the SUBJECT and went from five legs to **six**, which is a strengthening. **`RI-35`'s PROBE WAS READ BEFORE A SINGLE STRING WAS WRITTEN**, so no added occurrence puts the writer's name before `.`, `,` or `)`. **REPAIRING PROSE MOVED SIX CITED LINES AND TWO WERE IN `apps/api/test/wiring.test.ts`**, so [ADR-386](../decisions/ADR-386.md) section 6's ruling was applied literally rather than the practice it refused: fan-in derived per file and split into live and dated, `batch/adapter.ts` and `index.ts` rewritten to **net zero** because pointers this row cannot repair sit below the edits, `schedule.ts` left free because every edit is BELOW its deepest cited line, the four in-fence pointers repaired by REMOVING the coordinate ([ADR-404](../decisions/ADR-404.md)), and **every externally cited line then read at its number on both refs and confirmed byte-identical** rather than inferred from `numstat`, which is that entry's own second refusal. **THE ROW SHIPS WITH THE SUITE RED ON ONE CASE AND THAT IS THE REFUSAL**: `apps/api/test/event-placement.test.ts` pins the three stale suites as a non-empty list and ADR-410 wrote it to fail *"the day somebody holding that fence repairs one"*. **THE REPAIR IS ONE ASSERTION, IT IS OUTSIDE THIS FENCE, AND THAT IT IS SMALL IS THE REASON IT WAS NOT TAKEN**, ADR-410 section 5 having been in the same position and answered by a WIDENED FENCE rather than by a crossing. The rest of that case was verified still passing READ-ONLY with its own `OLD_PATH` constant, nothing written into `apps/api` and no seed planted there. **`replay-adapter.test.ts` STILL IMPORTS THE OLD PATH DELIBERATELY**: it is the compatibility module's only consumer and that module's header says so about itself, so repointing it would falsify a sentence in a deployable this row may not repair, which is this row's own defect one wave later. **REPORTED AND NOT REPAIRED**: `RI-35`'s `sweptBy` reads *"exactly seven lines in the shipped scope"* and the figure derived at reporting time is **28**, **11** of them under `apps/worker`, handed to row `415` derived. **CONTAINER EMPTY AGAIN, TWELFTH IN A ROW; SHALLOW CLONE, 3.817s, 19M, against 17.8s and 185M full one wave earlier.** **NO GATE ADDED, WIDENED OR WEAKENED, NO TEST SKIPPED, DISABLED OR QUARANTINED, NO INVARIANT MINTED, NO MIGRATION, NO `CI-06` LETTER, NO MANIFEST EDITED, NO SINK WIRED, NO DATED RECORD REPAIRED.** Counts derived at reporting time, each command run separately. -->

### 1. The objective

Repair the sixteen prose sentences under `apps/worker` that [ADR-410](../decisions/ADR-410.md) section 9 registered rather than fixed, deriving the set at source rather than inheriting the figure; repair the refusal message and the assertion that reads it in one change; and touch no dated record, no `packages/tooling`, no `apps/api` and no `packages/ledger`. One objective, non-money.

### 2. Done

- **The container measured and reported verbatim.** No repository on arrival, twelfth row on the record. `ls -la /home/user` returned `.` and `..` and nothing else; `git status` returned *fatal: not a git repository (or any of the parent directories): .git*; a `find` for any `.git` returned three, all toolchain. This row cloned the tree itself in **3.817 s** to a `--depth 1` clone, **19M** of `.git`, then fetched the base at `--depth 50`; `pnpm install --frozen-lockfile` took **6.9 s** with no lockfile change.
- **The base reproduced on `0f89c6ac`** before the first edit, on an empty `git status --porcelain`: gates **33 of 33**, invariants **35 of 35**, suite **337 files / 8,152 passed / 10 skipped / 0 failed**, typecheck, lint and `format:check` clean. `generate` reported *every span already matches its query*.
- **The set derived twice, with two instruments, and both counts reported.** The path matcher reproduces ADR-410's sixteen exactly, including its seven-and-three split. The semantic reading finds eight more that carry the identical claim without a path.
- **The coupled pair repaired in one change, with the RED constructed first** and both files restored byte-identical before the real edit.
- **Every reachability premise checked at its own source**, in the manifests and in `db.test.ts`'s own pin, rather than argued from the prose that carried them.
- **`RI-35`'s `event-sink-caller` probe read before writing any string**, so the repairs cannot be read as an install.
- **[ADR-386](../decisions/ADR-386.md) section 6 applied to the six pointers the repairs moved**, with per-file fan-in derived, two files brought to net zero, four in-fence coordinates removed, and every externally cited line verified byte-identical at its number on both refs.
- **The `sweptBy` figure re-derived and handed to row `415`**, and rows `413` and `415` reserved in [ALLOCATION](../decisions/ALLOCATION.md) while `414` was claimed.

### 3. Refused, with the measurement

- **`apps/api/test/event-placement.test.ts` is RED and is left RED.** Completing the objective empties a list that case asserts is non-empty. The case is designed to fail on exactly this, says so in its own comment, and lives in a file the dispatch puts outside this fence. The repair owed is one assertion plus its comment block, and the remainder of the case was verified still passing without writing into `apps/api`.
- **`replay-adapter.test.ts`'s import of the old path is left standing**, because repointing it would strand `apps/api/src/events.ts`, whose header states it is a compatibility name for exactly one consumer.
- **No dated record was repaired**, and six that this row moved or falsified are named in [ADR-414](../decisions/ADR-414.md) section 7 instead.

### 4. Counts at reporting time

Each command run separately, on the tree as committed: gates **33 of 33**, invariants **35 of 35**, suite **337 files / 8,151 passed / 10 skipped / 1 failed**, the one failure being the tripwire in section 3; typecheck, lint and `format:check` clean. **`pnpm run verify` was NOT run and `falsify.mjs` was NOT run**, both forbidden by this row and both mutating the working tree.
