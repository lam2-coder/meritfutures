---
status: approved
depends_on: [README.md]
last_updated: 2026-08-24
---

## 2026-08-24 - Session 146 (P3-c): `CI-04` acquires a selected project, and `VG-3` and `VG-6` expire with it

- **Objective:** write [ADR-085](../decisions/ADR-085.md), select the `integration` Vitest project from a CI job, and rule `VG-3` and `VG-6` under [ADR-080](../decisions/ADR-080.md) (d).
- **Done:** **[ADR-085](../decisions/ADR-085.md), `status: proposed`, approval line unsigned.** `ci.yml` gains an `integration` job; `CI-04` takes `CI-09`'s shape, one implemented leg plus a dated condition. `VG-3` and `VG-6`'s chains **expire on the same commit** and take `Waiting` on a dated artifact. New unlettered gate [`CI-06/vg-inventory`](../../scripts/corpus/gates.mjs), plus two `falsify-ci` cases and seven `falsify` cases. **The `x` rule is written into [ALLOCATION](../decisions/ALLOCATION.md).**
- **The count:** **`pnpm run verify` exits 0.** 29 of 29 gates, 7 of 7 invariants, **104 files / 1512 passed / 1 skipped**, `falsify` reports **all 29 pass clean and fail dirty** over **62 scope cases**. `falsify:ci` reports **68 of 71**, and the three are `CI-05` cases erroring on `gitleaks`, `semgrep` and `syft`/`grype`, none of which is on `PATH` in this container; they run in CI. **My two `CI-04` cases both failed as required, naming the test.**
- **Next:** the Neon branch is still `CI-04`'s second leg and still dated. `VG-3` and `VG-6` reopen the day `fastify` enters the lockfile, which is the gate's own seeded assertion.
- **Blockers:** **this branch waits on the founder's read and must not be merged before it.**
- **Landmines:**

  - **THE BRIEF'S CITATION FOR "`x` IS NOT FREE" DID NOT RESOLVE, AND THE RULE IS REAL.** It cited `ALLOCATION.md:186`; that span rules `CI-06k` folklore-rejected by [ADR-036](../decisions/ADR-036.md), a different letter, and **`x` appears nowhere in [ALLOCATION](../decisions/ALLOCATION.md) as a constraint.** The rule lived in exactly one place, [`falsify.mjs`](../../scripts/corpus/falsify.mjs)'s `nextFreeLetter()`, whose loop scans `a` to `x`. **It is now written into ALLOCATION**, because a constraint enforced only by a comment inside a harness is one the next reader has to already know about. [ADR-082](../decisions/ADR-082.md)`:124`'s *"`x` stays unclaimed"* records that ADR-082 took no letter; it does not release one, and it was read as a release once.

  - **THE NEW GATE'S STEP ASSERTION WAS VACUOUS ON ITS FIRST DRAFT.** It matched `**`x`**` while section 4.2 writes `steps `a` and `b``, `step `a`` and `rule [`n`](path)`. **It matched nothing**, so a Wired row reported as checked while ADR-080 section 3's strictly-stronger half asserted zero. Found by a seed reporting `SEED IS STALE`, not by reading. A Wired row naming neither a step nor a rule is now itself a finding.

  - **A SEED REMOVED THE WRONG WAITER.** `register-shrinks-when-an-artifact-is-re-ruled` re-worded one row's artifact, but `fastify` has **two** waiters (`VG-3` and `VG-6`), so the register entry stayed live and the case fired on a different finding. The seed now picks a row whose artifact nothing else waits on. **A scope case that fails off-target is a case proving the wrong thing.**

  - **`CI-06/gate-inventory` REJECTED THE FIRST `CI-04` LEG**, which named the workflow and not the job. [ADR-073](../decisions/ADR-073.md) `(a)` wants the job, because a stage can be named in a file that runs a different one.

  - **`CI-06v` SURFACED A PRE-EXISTING ORPHAN.** `CI-06/gate-inventory`'s own section 4.4 row already sat outside the table behind a blank line. As a single line it was under the gate's minimum run of two; adding a sibling beside it made the fragment detectable. **Both rows moved into the table**, repairing a row no table gate was reading.

  - **[ADR-080](../decisions/ADR-080.md)'s SIGNED APPROVAL CLAUSE IS NOW TIME-ANCHORED.** Its enumeration, *"`VG-2`, `VG-3`, `VG-6` and `VG-10` chain on `CI-07` and `CI-04`, and neither stage is implemented"*, is false against this tree from this commit. **The rule survives; the enumeration was a measurement.** Recorded rather than edited: ADR-080 is outside this fence, on session 149's ADR-078 precedent.

  - **OUT OF FENCE, RECORDED AND NOT REPAIRED.** [`pnpm-workspace.yaml:40-42`](../../pnpm-workspace.yaml) says postgres.js *"costs 0 transitive packages against `pg`'s 6"* **and cites ADR-084 in the same sentence**, which measured `pg` at **14 packages, 13 transitive** ([ADR-084](../decisions/ADR-084.md)`:35`). Session 145 contradicted its own finding in the file that carries it.

- **Files touched:** [`ci.yml`](../../.github/workflows/ci.yml), [`vitest.config.ts`](../../vitest.config.ts), [`falsify-ci.mjs`](../../scripts/ci/falsify-ci.mjs), [`gates.mjs`](../../scripts/corpus/gates.mjs), [`falsify.mjs`](../../scripts/corpus/falsify.mjs), [STRATEGY](../testing/STRATEGY.md) 4.1/4.2/4.4, [ADR-085](../decisions/ADR-085.md) (new), [ALLOCATION](../decisions/ALLOCATION.md), [decisions README](../decisions/README.md), [INDEX](../INDEX.md), [STATE](../STATE.md), this file and its row, plus the six `gate_count` span files.

### The forced edits, named rather than discovered

**`gate_count` 28 to 29** is regenerated by `gates.mjs generate` into six files: [STRATEGY](../testing/STRATEGY.md), [INDEX](../INDEX.md), [STATE](../STATE.md), [WAVE-03](../plans/WAVE-03-duplicate-registry-keys.md), [session 38](2026-08-16-session-38.md) and [session 81](2026-08-20-session-81.md). Mechanical and span-only; precedent `99dba04`, the commit that added `CI-06/gate-inventory`. **[decisions README](../decisions/README.md)** gains one row, which is `CI-06n`.
