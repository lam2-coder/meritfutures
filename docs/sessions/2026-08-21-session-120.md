---
status: approved
depends_on: [README.md]
last_updated: 2026-08-21
---

## 2026-08-21 - Session 120: `0045` lands, and the exception it makes cheap is the one it exists to record (money path)

- **Objective:** write [`0045_simulation_runs.sql`](../../packages/db/migrations/0045_simulation_runs.sql), which is `SD-M21-01` and `SD-M21-02` ([M21](../plans/M21-plan-designer.md) section 2.1), plus its probe, its manifest rows and its workflow step. Not `SD-M21-03`.
- **Done:** **[`0045`](../../packages/db/migrations/0045_simulation_runs.sql) is written and the contingency is met.** `simulation_runs` with 19 columns, **12 named CHECK constraints** and two partial indexes; `plan_versions` gains `decided_on_simulation_run_id`, `simulation_waiver_reason` and **two** named constraints. [`probe_simulation_decision_record.sql`](../../scripts/db/probe_simulation_decision_record.sql) asserts **16 things**: six successes, eight rejections each checked by SQLSTATE **and constraint name**, two `0028` interactions. [DELTA_MANIFEST section 23](../../packages/db/DELTA_MANIFEST.md), `SD-M21-01` and `SD-M21-02` moved to `landed` in place, `OI-27`, `OI-28` and `OI-29` opened, the [ALLOCATION](../decisions/ALLOCATION.md) row amended in place, a new [`simulation_runs`](../architecture/data-model/simulation_runs.md) design record and the [`plan_versions`](../architecture/data-model/plan_versions.md) one extended.
- **The count, and it moved under the branch after the merge, which is stated rather than overwritten.** **At the time this session ran: 24 of 24 gates**, `falsify` reporting all 24 pass clean and fail dirty. **After `origin/main` was merged in** (sessions 114, 115 and 122, which add three gates): **27 of 27 gates**, `falsify` reporting **all 27 pass clean and fail dirty**, 45 scope cases and 10 loader cases. **The re-verification is the one that counts**, because the branch that gets read is the merged one. Both figures are here because a session log that silently adopts a later number stops being a record of what the session actually saw. Also **101 files / 1404 passed / 45 skipped**, format, lint and typecheck clean. **All 45 migrations applied forward-only into an empty PostgreSQL 16.13**, figures queried from the catalogue: **111 tables** against 110 at `0044`. **11 of 11 probes green.**
- **Next:** `SD-M21-03` (`competitor_plan_models`) still claims no migration number and `OQ-M21-06` carries it. `OI-29`, the soundness of the publish link, needs a trigger or an application publish path and is a ruling nobody has made.
- **Blockers:** **this branch waits on the founder's E2 read and must not be merged before it.**
- **Landmines:**

  - **THE PLAN'S COUNTERFACTUAL WAS WRONG AND RUNNING IT IS WHAT SHOWED THAT.** The plan predicted the probe would fail against a `0044` database on the publish-with-neither assertion. **It fails earlier, at `SUCCESS 1`, with `relation "simulation_runs" does not exist`**, which proves the TABLE is load-bearing and says **nothing about the `CHECK`**, and the `CHECK` is the control the whole ruling rests on. A probe that dies on its first statement is not a counterfactual for a constraint. The one that means something was run instead and is recorded: **the same publish insert is ACCEPTED at `0044` (`rows=1`) and refused at `0045`.** A predicted counterfactual that was never executed asserts less than none.

  - **`sample_size >= 0` LOOKS LIKE A WEAKER CHECK AND IS THE CORRECT ONE.** The instinct is `> 0`, because a simulation over zero samples is not a simulation. [`provenance.ts`](../../packages/harness/src/provenance.ts) throws only on `runSampleSize < 0`, so **a provenance record with `runSampleSize: 0` is legal in the harness**, and a `> 0` CHECK would make a legal provenance **unstorable** at the write boundary of a run that had already executed. This is the schema agreeing with its own primary source instead of with an instinct, and `SUCCESS 2` watches a zero-sample row write rather than leaving the argument in a comment.

  - **THE CLOSED UNIT VOCABULARY HAS NO WORD FOR THIS COLUMN, AND PICKING THE NEAREST ONE QUIETLY IS A KNOWN FAILURE.** `CI-06m` forces every `date` column to declare one of exactly three tokens. **`wall clock` is refuted by its own definition**, *"Merit's own clock, answered only by `now()`"*: `calibration_observed_at` is never `now()`, and writing `now()` into it is precisely the defect the column exists to prevent. `trading day` is refuted harder. **`rail clock` is declared because its operative half, "quoted and never computed by Merit", is exactly true**, and `affiliate_commissions.chargeback_window_ends_on` already carries it for the same reason. **The noun is still wrong: a calibration vendor is not a rail.** `OI-28`, candidate ADR owed, argued in the design record rather than picked, because picking the nearest-looking token is how three rows passed `CI-06m` by accident before the marker was tightened.

  - **`num_nonnulls` COUNTS THE EMPTY STRING AS PRESENT, WHICH IS WHY THE BLANK FLOOR IS A SECOND CONSTRAINT.** A waiver of `''` satisfies `num_nonnulls(...) = 1` while recording nothing at all: the publish passes the control and the reader learns no reason. **Separate and named**, so `REJECTION 3` can assert the refusal comes from the floor and not from the exactly-one check. A compound constraint passes for one reason and fails for two.

  - **THE FK PROVES A ROW WAS NAMED AND PROVES NOTHING ABOUT WHAT IT SAYS.** `OI-29`. No `CHECK` can read another table, so a publish decided on a **`failed`** run, or on a run over a draft since edited so `rules_digest` no longer matches, or on a run belonging to a different plan, all satisfy `plan_versions_publish_decision_recorded` today. Stated in the migration header and in the design record and not only in the manifest, because the next reader of that FK is standing at the column.

  - **ONE IDENTIFIER SERIES, TWO DEFINITION SITES, AND THIS SESSION WALKED INTO IT.** The `OI` table in [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md) stops at `OI-24`, so this session allocated `OI-25`. **`OI-25` and `OI-26` were already taken**, in [WAVE-04 section 6](../plans/WAVE-04-fixture-backlog-and-gate-inventory.md) and [STATE](../STATE.md), and neither site names the other. That is [ADR-074](../decisions/ADR-074.md)'s exact subject, **caught by a founder read rather than by the `CI-06/identifier-series` gate being written for it in a sibling session as this landed.** `OI-27` records the near-miss rather than just the renumber, because a live instance is worth more to that session than a clean renumber is to this one.

  - **`## 21.` IS CLAIMED TWICE IN DELTA_MANIFEST AND WAS DELIBERATELY NOT REPAIRED.** `0038`'s section and `0042`'s both take it, and the section-number table carries **both** rows, so **the table that exists to end section-number drift is carrying the drift**. `22` is the true maximum, which is why section `23` is correctly numbered. It belongs to [FOLD-04](../plans/FOLD-04-impersonation-and-admin-parity.md) `I2`, is outside this fence, and a money-path session does not renumber another session's sections. Recorded in `OI-27`.

  - **THREE PROBES BROKE AND THAT IS THE CONSTRAINT WORKING.** Exactly the three that publish a `plan_versions` fixture; the other three touching the table leave it at `draft` and were untouched, which is itself a check that the constraint did not widen into forbidding ordinary authoring (`SUCCESS 3` asserts that directly). Each now carries a waiver naming itself as a fixture.

- **Files touched:** [`0045_simulation_runs.sql`](../../packages/db/migrations/0045_simulation_runs.sql) and [`probe_simulation_decision_record.sql`](../../scripts/db/probe_simulation_decision_record.sql) (both new), [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md), `.github/workflows/corpus.yml`, the three repaired probes, [ALLOCATION](../decisions/ALLOCATION.md), [`simulation_runs`](../architecture/data-model/simulation_runs.md) (new), [`plan_versions`](../architecture/data-model/plan_versions.md), [data-model README](../architecture/data-model/README.md), [sessions README](README.md), this file, and the generated spans in [INDEX](../INDEX.md) and [STATE](../STATE.md).

### The fence extension the founder ruled on rather than discovered

`scripts/corpus/gates.mjs` is held by the sibling session writing `CI-06/identifier-series`. **`CI-06s` asserts both directions**: the `psql` step in `corpus.yml` **and** the needle inside `ci06h`'s `required` array, read from that block alone. There is no green tree with only one of them, and a branch ending with `CI-06s` red is not mergeable, so both edits are in one commit. The two regions of the file are disjoint: the sibling adds gate objects and `GATES` entries, this session adds **one string** to `ci06h`'s array. The founder merges the sibling first and resolves.

### What `0045` bought, against what it was reserved for

[ALLOCATION](../decisions/ALLOCATION.md) reserved it CONTINGENT on the `M21` plan naming a persisted run. **The contingency was met and the number was spent in full**, which is the opposite of `0041`'s outcome three sessions earlier, where neither half of the reservation survived being read against the schema. Both halves survived here, **and the second is the one that binds**: `simulation_runs` is a record, and a record nothing is required to point at is a table that fills up while the question it was built to answer stays unanswerable. The `CHECK` on `plan_versions` is what makes the pointing mandatory.
