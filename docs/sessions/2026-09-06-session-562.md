---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 562: the collection row, where the dispatch's own list of what was owed was wrong about one item and missing two

<!--index: **THE ROW IS [ADR-367](../decisions/ADR-367.md), `proposed` and UNSIGNED. Control over the money path's honesty rather than the money path itself.** Wave 360s fenced `apps/api/test/wiring.test.ts` to one owner, so three sibling rows found defects in it they were required to REPORT rather than repair. This row is the collection. **RULED: ALL FOUR OWED ITEMS ARE REAL, AND THE DISPATCH'S LIST OF THREE WAS ONE ITEM COUNTED TWICE, ONE RIGHT, ONE NAMING A FIGURE THAT IS NOT IN THE FILE IT NAMED, AND IT OMITTED TWO.** The SSO purchase blocks **EIGHT** entries where the prose read SIX and FIVE OTHER, and `postgresPayoutBackend` implements **SIX** members of eight and refuses **TWO** where the clause read four and four and named `subject` and `listPayouts` among the rejecters, both of which answer today. **THE TWO REPAIRABLE COUNTS ARE NOW DERIVED FROM THE TREE AND CANNOT GO STALE AGAIN**, and the third is left hand-maintained on purpose because deriving it would answer a founder's open question. **[ADR-358](../decisions/ADR-358.md)'s `RI-14` collision is MEASURED rather than assumed**, and the one port [ADR-362](../decisions/ADR-362.md) and [ADR-363](../decisions/ADR-363.md) read opposite ways is PINNED rather than resolved. -->

---

### 1. The objective

Row `367`: collect and repair what [ADR-360](../decisions/ADR-360.md), [ADR-361](../decisions/ADR-361.md), [ADR-362](../decisions/ADR-362.md) and [ADR-363](../decisions/ADR-363.md) left owed inside `wiring.test.ts`, and ask whether each repaired count can be DERIVED instead.

**One objective.** It resolved into an enumeration, four repairs, four executed derivations, ten falsification seeds and one deliberate fence widening.

---

### 2. The enumeration, which is the first half of the deliverable

The dispatch named three owed items and said at least one might be wrong. **It was wrong about one, listed one twice, and omitted two.**

| The dispatch said | Measured on the merged head | Verdict |
|---|---|---|
| one `BLOCKED` entry's prose is stale, repair is one entry's wording | The same finding as its next item, and it is **TWO entries' wording**: `wiring.test.ts:253` and `:357`, both named in [ADR-360](../decisions/ADR-360.md) section 7's own table | **REAL, listed twice** |
| an entry undercounts the SSO purchase by two, SIX where the tree derives EIGHT | Exact | **REAL** |
| `ADR-356` derived eight at `admin-read-constructibility.test.ts:371` | The pointer is exact. **The row that built the case is [ADR-313](../decisions/ADR-313.md)**, agreeing with [ADR-312](../decisions/ADR-312.md)'s hand derivation | **POINTER RIGHT, ATTRIBUTION WRONG** |
| a clause reads FIVE of eight | **The clause in this file reads FOUR of eight.** *"FIVE MEMBERS OF EIGHT"* is `payout-backend.ts:4`, a `src/` file this row's fence forbids | **REAL IN SUBSTANCE, WRONG FIGURE AND WRONG FILE** |
| not listed, first omission | [ADR-362](../decisions/ADR-362.md) section 8 item 4: the `ledger` pointer, `checkout.ts:1030`, belongs at `:1056` | **REAL, omitted** |
| not listed, second omission | [ADR-362](../decisions/ADR-362.md) section 8 item 1: `LIVE_DEFAULT` gains a member under its own docblock's reading | **REAL, omitted** |

**AND THE OMITTED POINTER ITEM HAS ITSELF GONE STALE SINCE IT WAS WRITTEN.** [ADR-362](../decisions/ADR-362.md) says `:1030` holds `insertWalletDebit`; on the merged head that is at `:1026`. The repair target `:1056` is exact and the description of the defect is not.

---

### 3. What landed

| file | what |
|---|---|
| [`apps/api/test/wiring.test.ts`](../../apps/api/test/wiring.test.ts) | three prose repairs, one pointer repair, and four executed cases. **The only file this row wrote outside `docs/`** |
| [`docs/decisions/ADR-367.md`](../decisions/ADR-367.md) | **new.** The ruling, `proposed`, UNSIGNED |
| [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) | row `367` taken, and `364` to `366` reserved. Section 6 |
| `docs/decisions/README.md`, `docs/INDEX.md`, `docs/STATE.md`, `docs/sessions/README.md` | registry rows, the generated ones by `gates.mjs generate` |

---

### 4. The derivations, and the one that was refused

| Derived | From | Reddens when |
|---|---|---|
| both SSO figures | the live `BLOCKED` map, by the predicate the other file runs over a parse of this one | the set grows, or the prose changes |
| the payout member total | `PayoutTx` and `PayoutBackend`, sliced at the braces by this file's own `scanTop` | either interface grows a member |
| the payout refusal partition | the names `payout-backend.ts` spells into its own refusals | a member stops refusing, or a refusal is spelled without a literal name |
| which members may be NAMED as rejecting | the same spellings | the historic defect is written back in |

**THE BRANCH DISTINCTION IS THE DERIVATION AND THE FIRST DRAFT DID NOT HAVE IT.** That draft forbade naming ANY answering member among the rejecters and **failed on its own first run**, on `insertPayoutRequest`, whose hold arm genuinely does refuse and which the sentence names on purpose. The failure is kept in the case's comment: the first version would have forced a true sentence to be deleted.

**THE THIRD COUNT IS DELIBERATELY NOT DERIVED.** `UNPROVEN_RULING` and the triple stay hand-maintained, not because the count is hard but because settling it means either moving a port into `LIVE_DEFAULT` or minting a third verdict, and both are [ADR-362](../decisions/ADR-362.md) section 9 question 1, which is the founder's.

---

### 5. Falsification

**TEN SEEDS, EVERY ONE WATCHED RED AND EVERY ONE RESTORED BYTE-IDENTICAL under `sha256sum -c`.** Four touched `apps/api/src` and `git diff origin/main -- apps/api/src` is EMPTY at reporting time.

| # | seed | watched |
|---|---|---|
| 1 | a fifteenth `BLOCKED` reason names `AdminSessionSource` | **RED**, naming the nine-member set |
| 2 | the live SSO figure is changed and the tree is not | **RED** |
| 3 | the retirement QUOTES its false sentence instead of naming the figure | **RED**, two matches. **[ADR-358](../decisions/ADR-358.md)'s collision, measured** |
| 4 | `holdFlag` stops refusing, as slice 8 landing would do | **RED, 2 cases** |
| 5 | a refusal spelled without a literal member name | **RED**, the capture guard, before any count moved |
| 6 | `subject` written back among the rejecters (**the historic defect**) | **RED**, naming `subject` |
| 7 | `PayoutBackend` grows a ninth member | **RED**, the member total |
| 8 | `PRODUCTION_CHECKOUT_ADAPTERS.enrichment` stops being `null` | **RED, 3 cases** |
| 9 | `CheckoutAdapters.enrichment` stops declaring `null` | **RED**, the contract half |
| 10 | the port is promoted into `LIVE_DEFAULT` by writing it a ruling (**reverse direction**) | **RED**, and it fails for a better reason than expected |

**SEED 10 IS THE ONE WORTH READING TWICE.** Writing a ruling is the cheapest way a later row would promote that port, and it does not work: `LIVE_DEFAULT` only admits ports the walk calls `unproven`, so [ADR-363](../decisions/ADR-363.md)'s own both-directions assertion rejects the ruling as unnecessary. **Reaching the set at all requires changing the GRAMMAR, and seed 8 shows that reddens the pin.**

**AND ONE RESTORE WAS DONE WRONG AND IS RECORDED RATHER THAN GLOSSED.** The first restore used `git checkout --` on a file whose derivations were still uncommitted, and it reverted them along with the seed. They were re-applied from the scratchpad copy and `sha256sum -c` confirmed the result byte-identical. **Every seed after that was run against a committed tree.**

---

### 6. The fence widening, reported rather than quiet

**Row `367`'s fence names only its own row in `ALLOCATION.md`, and this row wrote four.** `CI-06f` is gapless over allocated plus reserved, `364` to `366` belong to three siblings running now, and `367` landing alone opens three holes and turns that gate red at 32 of 33 with nothing wrong. **The reservation cells name no subject and claim nothing**, on [session 558](2026-09-06-session-558.md) section 7's precedent and for its reason. The alternative was pushing a branch with a red gate.

---

### 7. Blockers and landmines

- **The founder's read of [ADR-367](../decisions/ADR-367.md) is owed**, and its approval block names three things: section 9 question 1, now two rows old and with a fourth option; whether section 8's three `src/` repairs are worth a row; and whether enumerating rather than trusting a dispatch becomes the default for collection rows.
- **STILL OWED, AND OUTSIDE THIS ROW'S FENCE.** `payout-backend.ts:4` reads *"FIVE MEMBERS OF EIGHT"* while `:18` of the same file says six answer and two reject. `payout-backend.ts:64` reads *"TEN of TWENTY-FOUR declared with FOURTEEN blocked"* where the tree asserts `{declared: 25, wired: 11, blocked: 14}`. `checkout.ts:1198`'s docblock says *"checkout answers 503"*, true of the card arm and false of the wallet arm.
- **LANDMINE: the payout derivation rests on a HABIT.** It works because that adapter spells the member name into every refusal. Nothing requires that to continue; a refusal spelled otherwise turns the file red with no defect present, and that is the intended direction of the error.
- **LANDMINE: [`WAVE-06:273`](../plans/WAVE-06-admin-console-transport.md) cites `wiring.test.ts:1749` for the wiring triple and was ALREADY wrong before this diff**, which moves the triple further. `RI-16` is silent because *"pins the triple at"* binds no name: a fourth live instance of the `IDENTIFIER_GLUE` hole. Out of this row's grant, which covers `apps/**`.
- **LANDMINE: `csrf.test.ts` flakes on the base**, per [session 554](2026-09-06-session-554.md). **It did not present in either full run here**, both clean.
- **Concurrency: three siblings are running now** and this row reserved their `ALLOCATION` numbers. A registry conflict at merge is expected and is the designed workflow.

---

### 8. Verification, derived at reporting time

| runner | result |
|---|---|
| `pnpm vitest run` | **328 files / 7,970 passed / 10 skipped / 0 failed** |
| baseline on `ee7ce5a4`, before the first edit | **328 files / 7,966 passed / 10 skipped / 0 failed** |
| delta | **+4 cases in ONE EXISTING FILE. No file added, skips unchanged.** Attributed **by removal**: `wiring.test.ts` runs **18** alone on this tree and **14** alone on the base |
| `scripts/corpus/gates.mjs generate` then `check` | **33 of 33** |
| `packages/tooling/checks/repo-invariants.mjs` | **35 of 35**, unchanged, no number minted |
| `pnpm run typecheck` | clean |
| `pnpm run lint` | clean |
| `pnpm run format:check` | clean |
| `git diff --stat origin/main -- packages/db/migrations scripts/db` | **EMPTY** |
| `git diff origin/main -- apps/api/src` | **EMPTY** |

**`RI-30` WENT RED ON THIS ROW'S OWN FIRST DRAFT**, at 34 of 35, because the payout derivation shipped its own two-pass comment stripper. It imports the canonical one now. **That is the invariant catching precisely the defect it was minted for, inside the diff that introduced it.**

**`pnpm run verify` was NOT run and `scripts/corpus/falsify.mjs` was NOT run**, both forbidden by the row.

**GITHUB ACCESS, TESTED IN BOTH DIRECTIONS RATHER THAN ASSUMED.** No `gh` binary on `PATH` and no `mcp__github__*` tool in this session's surface; `api.github.com/repos/lam2-coder/meritfutures` answers **403**; `git push -u origin` authenticates and succeeds. **This matches sessions 554 to 558.** **THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL** and the tree was attached and cloned by hand before anything could be measured, which is session 553's report reproduced.

---

### 9. Next

The founder's read of [ADR-367](../decisions/ADR-367.md). Then, in order of cheapness: **section 9 question 1**, which two rows have now asked and which this row has measured as an inference defect rather than a matter of taste; the `src/` repairs in section 7, now recorded as owed by two waves; and `RI-15`'s `IDENTIFIER_GLUE`, which has a fourth live instance and is still its own row.
