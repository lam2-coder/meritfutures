---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 232: `ADR-120`, the auth wiring, and the two constructions that make a session rather than use one (MONEY PATH, auth, `E2`)
<!--index: THE PLAN, WRITTEN BEFORE ANY SOURCE FILE. `apps/api` is admitted to `DB_ADMITTED` as its first entry ever, and `ADR-120`'s subject is why that is correct here when sessions 218 and 220 were right to refuse it. `ADR-112`'s keyed accessor unblocks the three writes `routes/auth.ts`'s header names, and the measurement this session opens with is that it unblocks EVERYTHING A SESSION CAN DO AND NOTHING THAT MAKES ONE: `scopedTx.insert` takes `OwnedTableKey` and `sessions` is `derived`, and a pre-identity read of `users` by email has no door at any authority -->

- **Objective:** implement `AuthBackend` and `IdempotencyStore` against the real accessor, admit `apps/api` to the trader database, and write the ruling that admission is.

---

### THE PLAN (written first, per the dispatch, and committed before any source file)

#### 0. What was read before anything was planned

[ADR-112](../decisions/ADR-112.md) in full, [ADR-117](../decisions/ADR-117.md) in full,
[ADR-109](../decisions/ADR-109.md) in full, [ADR-113](../decisions/ADR-113.md) in full,
[`apps/api/src/routes/auth.ts`](../../apps/api/src/routes/auth.ts) (1,114 lines),
[`apps/api/src/idempotency.ts`](../../apps/api/src/idempotency.ts) (388 lines), and the
`RI-08` section of [`packages/tooling/checks/repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs)
(lines 767 to 1032). Then the primary sources those name, at the migration and at the
registry rather than at the prose about them.

#### 1. Six findings, each re-derived at source before a line was planned

| # | Claim | Verified at |
|---|---|---|
| 1 | **`sessions` is `derived` via `users`, so a keyed READ and a keyed WRITE both work through `scopedTx` and an INSERT does not** | [`scope.ts:435`](../../packages/db/src/scope.ts) for the rule; [`scoped-db.ts:1051`](../../packages/db/src/scoped-db.ts) declares `insert<K extends OwnedTableKey>` and [`:293`](../../packages/db/src/scoped-db.ts) makes `OwnedTableKey` the `'owned'` classes only. **A session row cannot be written at any authority a request handler holds** |
| 2 | **`sessions.refresh_token_hash` is UNIQUE in the DDL AND in the transcription**, so it is a legal address | [`0002_identity.sql:342`](../../packages/db/migrations/0002_identity.sql) `bytea NOT NULL UNIQUE`; [`schema.ts`](../../packages/db/src/schema.ts) `refreshTokenHash: bytea('refresh_token_hash').notNull().unique()`. This is [ADR-112](../decisions/ADR-112.md) section 4's fold landing on the safe side for this table |
| 3 | **`otp_challenges` is `firm` and `otp_send_budget` is `firm`**, so both are reachable through `firmTx` with no identity | [`scope.ts:1074`](../../packages/db/src/scope.ts) and [`scope.ts:636`](../../packages/db/src/scope.ts) |
| 4 | **`idempotency_keys.key` is the PRIMARY KEY and `identity_id` is NULLABLE**, so the IDENTITY arm of [ADR-109](../decisions/ADR-109.md) clause 4 is addressable through `scopedTx` and the UNOWNED arm is addressable through nothing | [`0017_events_and_audit.sql:103`](../../packages/db/migrations/0017_events_and_audit.sql); [`scope.ts:1032`](../../packages/db/src/scope.ts). A `RowFilter` is equality only ([ADR-112](../decisions/ADR-112.md) clause 1), so `identity_id IS NULL` is not expressible, and `firmDb()` refuses an `owned` key |
| 5 | **A pre-identity READ has no door.** `users.email` is UNIQUE and `users` is `owned`, so resolving an email to an identity requires the identity | [`scope.ts:428`](../../packages/db/src/scope.ts); `SystemReason` is two members at [`scoped-db.ts:176`](../../packages/db/src/scoped-db.ts) and a request handler is neither |
| 6 | **`CI-06/vg-inventory` goes RED the instant `scopedDb` is named under `apps/api/src`**, measured rather than predicted | Seeded one file containing the identifier: `31 of 32`, two findings, both `docs/testing/STRATEGY.md`, `VG-3` at `:209` and `VG-6` at `:212`, each reading *"the artifact HAS ARRIVED. This is the assertion that fails on good news: re-rule the row rather than the gate"*. Seed removed, `git status --porcelain` clean, `32 of 32` restored |

**FINDING 1 AND FINDING 5 ARE ONE SENTENCE AND IT IS THIS SESSION'S HEADLINE.**
[ADR-112](../decisions/ADR-112.md) unblocked **everything a session can DO and nothing that
makes one**. Revoke one session, consume one challenge, stamp `elevated_at`: all three land.
Mint a session, resolve an email to a person: neither has a door, and neither is a predicate
problem, which is the shape [ADR-109](../decisions/ADR-109.md) clause 1 found one level up
about the authority.

#### 2. What will be built

**`apps/api/src/db.ts` (new).** The one place `@merit/db` is imported, the one place the
`IdentityId` cast is written, and the seam an adapter is testable through: two doors
(`scopedTx(identityId, fn)` and `firmTx(fn)`), each a one-line delegation to
`transaction(scopedDb(...))` and `transaction(firmDb(...))`.

**`apps/api/src/auth-backend.ts` (new).** `databaseAuthBackend(db)`, implementing the seven
`AuthBackend` methods the accessor can express and refusing the other nine with a NAMED
blocker per method rather than one undifferentiated error:

| Wired | Door |
|---|---|
| `sessionByToken` | `scopedTx.rowAt('sessions', { refreshTokenHash })` |
| `logout` | `scopedTx.updateAt('sessions', { id }, { revokedAt })` |
| `revokeSession` | the same, on a caller-named id |
| `listSessions` | `scopedTx.rows('sessions')` |
| `elevate` (`dual_channel` arm) | `firmTx.updateAt('otpChallenges', { id }, ...)` then `scopedTx.updateAt('sessions', { id }, ...)` |
| `readPhoneChange` | `scopedTx.rows('phoneChangeRequests')` |
| `cancelPhoneChange` | `scopedTx.updateAt('phoneChangeRequests', { id }, ...)` |

| Refused, with the blocker named at the method | Blocker |
|---|---|
| `verifyOtp`, `passkeyLoginVerify` | finding 1 AND finding 5: no pre-identity read, and no INSERT onto a `derived` table |
| `passkeyRegisterOptions`, `passkeyRegisterVerify`, `passkeyLoginOptions`, `elevate`'s `passkey` arm | no WebAuthn verifier is admitted in this workspace and admitting one is a `VG-12` decision of its own |
| `requestOtp` | nothing in this tree DELIVERS a code. A handler that writes a challenge and answers `sent: true` having sent nothing is worse than a 503 |
| `verifyPhone` | `PhoneVerifyResponse.line_type` is a provider lookup ([ADR-039](../decisions/ADR-039.md) (a)) and no adapter exists |
| `openPhoneChange` | [ADR-039](../decisions/ADR-039.md) (c) requires the PRIOR number be notified, and a change request written without it is a control that exists in prose |
| `readMe` | `Me.max_accounts` is a plan parameter, and no plan parameter is stated in application code |

**`apps/api/src/idempotency-store.ts` (new).** `databaseIdempotencyStore(db)`, discharging
[ADR-109](../decisions/ADR-109.md) clause 3 for the `IdentityScope` arm through
`scopedTx.rowAt` / `.insert` / `.updateAt`, and refusing the `UnownedScope` arm with finding
4 written at the site. **The two arms are two code paths because they are two types**, which
is clause 4's whole point arriving in an implementation rather than in an interface.

**THE SESSION TOKEN CARRIES ITS OWN IDENTITY, AND THAT IS A RULING RATHER THAN AN ENCODING.**
`sessionByToken` must find a row before any identity is known, which finding 5 says no door
serves. The token is minted as `<identityId>.<secret>` and the stored hash is over the WHOLE
string, so the identity in the cookie is a HINT the database is never asked to trust: a
rewritten prefix changes the hash and finds nothing, and a colliding hash is still refused by
the scope predicate, which is [ADR-112](../decisions/ADR-112.md)'s composition doing exactly
what it was built for. **The identity is not READ off the row; it is PROVED by the row
coming back.** It discloses nothing: `VerifyResponse.identity_id` and `Me.identity_id` both
already hand the client that value.

#### 3. The admission, which is what `ADR-120` is for

`DB_ADMITTED` gains `@merit/api` and nothing else, `apps/api/package.json` gains
`"@merit/db": "workspace:*"`, and `ADR-120` says what makes that correct HERE when
[session 218](2026-08-26-session-218.md) and [session 220](2026-08-26-session-220.md) were
right to refuse it: **it is this session's subject rather than its by-product.** `RI-08`'s
own throw on a list that covers every package is not touched, no name but `@merit/api`
joins, and the check's `guarded.length === 0` refusal stays the gate defending its own
weakening.

#### 4. Two FORCED WIDENINGS, named here before they are taken

Both are compelled by the stop condition's `32 of 32`, both have an explicit in-tree
precedent, and neither is reached for.

| Widening | Forced by | Precedent |
|---|---|---|
| [`docs/testing/STRATEGY.md`](../testing/STRATEGY.md), the `VG-3` and `VG-6` closure cells | finding 6. The cells themselves say *"the accessor's name appearing inside `apps/api` is the first commit on which the subject exists"* and *"the direction a re-ruling should move"* | [ADR-100](../decisions/ADR-100.md) moved these same two cells when `fastify present in the lockfile` arrived |
| [`scripts/corpus/gates.mjs`](../../scripts/corpus/gates.mjs), `VG_PROBES` | the same gate's OTHER assertion: *"BOTH REGISTERS SHRINK IN THE STALE DIRECTION"*. Leaving the arrived probe registered while no row waits on it is a second finding | the register's own comment: *"THIS ENTRY REPLACED `fastify present in the lockfile`, WHICH ARRIVED ... the fastify probe is removed rather than left beside a row that no longer waits on it"* |

**The new artifact is `a Postgres service in ci.yml's integration job`**, measured absent at
[`.github/workflows/ci.yml:498`](../../.github/workflows/ci.yml). It is strictly closer to
both subjects than the accessor's name was, and it is the artifact
[ADR-102](../decisions/ADR-102.md) section 16 and [ADR-112](../decisions/ADR-112.md) section 9
have each named as owed: *"`CI-04` exists, its project exists, and what it has never had is a
database."*

**SESSION 233 IS REWRITING SPECIFIERS UNDER `scripts/` AND `gates.mjs` IS UNDER `scripts/`.**
That is a possible collision and it is REPORTED here rather than resolved quietly, per the
dispatch. This session touches `VG_PROBES` only. On `repo-invariants.mjs` the split is the
dispatched one: `DB_ADMITTED` here, `RI-10`'s scope there.

#### 5. What will NOT be touched

**No migration. `0048` stays free.** Nothing in `packages/db`: findings 1, 4 and 5 are
reported as findings and the constructions they name are the founder's, on
[ADR-109](../decisions/ADR-109.md) clause 2's own precedent. `RI-08` is not weakened and
`DB_ADMITTED` is not widened past one name. No new runtime dependency, so `VG-12` is asked to
admit nothing and the only lines `pnpm-lock.yaml` gains are this importer's own.

#### 6. The approval line's shape, decided before the code

Unsigned, one clause, EXECUTABLE, **scoped to the files this session rules and naming no
package-wide count**, on [ADR-109](../decisions/ADR-109.md)'s own record that a package-wide
count has drifted six times and the ruling held every time. The strongest shape available:
a seeded handler reaching another identity's session **through the wired backend** writing
ZERO rows, executed against a real PostgreSQL 16 in this container as
[ADR-112](../decisions/ADR-112.md) section 9 was, and reported as EVIDENCE rather than
claimed as a gate, because [`ci.yml`](../../.github/workflows/ci.yml)'s integration job has
no database and a test that skips is worse than a test that does not exist.

**And the compile refusal the dispatch requires:** [session 218](2026-08-26-session-218.md)'s
approval clause is that an SMS-established factor for `C-27` elevation FAILS TO COMPILE.
`ElevationFactor` is re-seeded with `'sms_otp'` in this session and `TS2322` watched, because
a wiring change that widened the union would pass every test in this repository.

---

### Progress

**The plan is committed and pushed before any source file, per the dispatch.** What follows
is appended as the work lands.
