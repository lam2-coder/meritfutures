---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 590: the gate leg ADR-392 wrote out and could not land, derived from the file rather than pasted, and the duplicate-note case its written form reports clean

<!--index: **THE ROW IS [ADR-395](../decisions/ADR-395.md), `proposed` and UNSIGNED. MONEY-ADJACENT, ADR-003 STRICT, CONTROL OVER THE CORPUS'S OWN GATE RUNNER, `E2` READ OWED.** [ADR-392](../decisions/ADR-392.md) section 11 item 1 wrote this predicate out and could not land it because [`gates.mjs`](../../scripts/corpus/gates.mjs) was read-only to every row that wave; this row lands it as a **fifth assertion inside `CI-06l`**, not a new gate, so `GATES.length` and the `gate_count` span are unchanged and gates stay **33 of 33** DERIVED rather than assumed. **EVERY PART OF THE PREDICATE WAS RE-DERIVED FROM THE FILE AS IT STANDS RATHER THAN PASTED**: both section anchors occur EXACTLY ONCE so `indexOf` is unambiguous, the 21 table cells and 21 headings normalise through `normJob` to the same 21 strings with both set differences EMPTY, and `/\n## /` cannot match `\n### ` so the notes sit inside a section that still terminates at the next peer heading at 29,634 characters. **THE WRITTEN PREDICATE IS RIGHT ABOUT ITS TWO DIRECTIONS AND BLIND IN A THIRD, AND THAT IS THE FINDING.** Run VERBATIM against a page carrying **22 note headings collapsing to 21 keys**, one job holding `S3` and `S1` at once, [ADR-392](../decisions/ADR-392.md)'s form reports **0 findings** because `Map.set` keeps the last write; the leg that shipped keeps the notes as a LIST and reports the duplicate. Its other two directions are sound and that is stated as plainly as the defect: verbatim it reports 1 finding on seed A and 0 on the pristine page, exactly right. **FIVE SEEDS, EVERY RESTORE VERIFIED BYTE-IDENTICAL UNDER `sha256sum -c` AND NONE TAKEN WITH `git checkout --`.** A: an emptied note body goes **RED at 32 of 33**, closing [ADR-389](../decisions/ADR-389.md)'s seed A which was GREEN at 33 of 33 and 35 of 35 for two consecutive rows. B: a table row with no note RED. C: a **one-character** misspelling of a note heading fires **BOTH directions as two different sentences**, which is the seed that would have caught a leg wired to the wrong key. D: the duplicate, RED here and CLEAN under the written form. E: the severity heading removed, which is the notes folding back under the table, now **ERRORs at exit 3** where [ADR-392](../decisions/ADR-392.md)'s seed E2 measured a silent 33 of 33. **CITATION REPAIR, SEPARATED BY MEASUREMENT RATHER THAN ASSUMED**: the diff moves every line of `gates.mjs` from 2,525 on by **88**, there are **47 citation sites** at or after it, and **13 coordinate tokens over 6 subjects in 3 files were exact at `ebc21238` and are repaired**, the rest already wrong before this diff and LEFT. `RI-15` caught **2 of the 13** at **34 of 35**, the identical result [ADR-390](../decisions/ADR-390.md) records one wave earlier in the same file; the other eleven were found by measurement rather than by the control. **REFUTED BY READING THE REGISTER**: [ADR-334](../decisions/ADR-334.md), [ADR-335](../decisions/ADR-335.md) and [ADR-336](../decisions/ADR-336.md) claim `sites: 7` where the entry reads **`sites: 10`**, so that pointer was already wrong and is not this diff's. **NOTHING IS OWED ON [`CRON_INVENTORY.md`](../ops/runbooks/CRON_INVENTORY.md)**: it was read-only to all three rows and the leg needed NO edit to it, the document already satisfying the predicate at 21 against 21. **OWED**: [STRATEGY](../testing/STRATEGY.md) section 4.4's `CI-06l` row now under-describes the gate and the exact clause is written out; no gate goes red on it because `CI-06p` checks letters and never prose. **NO GATE WEAKENED, NO TEST SKIPPED, DISABLED OR QUARANTINED, NO INVARIANT MINTED, NO `CI-06` LETTER SPENT, NO MIGRATION TAKEN OR RESERVED.** `pnpm run verify` and [`falsify.mjs`](../../scripts/corpus/falsify.mjs) **NOT run**, both forbidden. Suite **336 / 8,109 / 10 / 0** against a base of **336 / 8,109 / 10 / 0** reproduced on `ebc21238` BEFORE the first edit, **delta ZERO on every axis and zero attributed by removal because nothing was removed**; gates **33 of 33**, invariants **35 of 35**, typecheck, lint and `format:check` clean. **THE CLONE IS SHALLOW AND THE CONTAINER ARRIVED WITH NO CLONE AT ALL FOR THE THIRD CONSECUTIVE WAVE**, which makes it a property of the dispatch rather than an incident. -->

---

### 1. The objective

Row `395`: land the gate leg [ADR-392](../decisions/ADR-392.md) section 11 item 1 wrote out and could not commit, which is the other half of [ADR-389](../decisions/ADR-389.md) section 12 item 2, binding the severity notes to the scheduled table in both directions.

**One objective.** It resolved into one gate assertion, one defect found in the specification it was given, five seeds, and thirteen citation coordinates repaired across three files.

---

### 2. Done

- **Assertion 5 of `CI-06l`**, with `CRON_SEVERITY` and `severityNotes(body)` beside the constants and helpers it already has. `gates.mjs` is **89 insertions against one deletion**.
- **The header comment and the `covers` line moved from four assertions to five.** A runner whose `covers` under-states what it checks is the same defect as one that over-states it.
- **Thirteen citation coordinates repaired** in [`data-model-columns.mjs`](../../scripts/corpus/data-model-columns.mjs), [ADR-330](../decisions/ADR-330.md) and [ADR-182](../decisions/ADR-182.md), each new value re-derived by grepping for the declaration.
- **The `data-model-columns.mjs` paragraph that exists to record these moves now records the third**, rather than continuing to say "twice".
- [ADR-395](../decisions/ADR-395.md), `proposed`, approval line UNSIGNED.

---

### 3. Next

- **[STRATEGY](../testing/STRATEGY.md) section 4.4's `CI-06l` row.** Outside this fence. The clause is written out in [ADR-395](../decisions/ADR-395.md) section 8 item 1.
- **The `sites: 7` claim** in [ADR-334](../decisions/ADR-334.md), [ADR-335](../decisions/ADR-335.md) and [ADR-336](../decisions/ADR-336.md), measured wrong against a register reading `sites: 10`.
- **The `RB-nn` links in the severity notes.** Three of 21 carry one and nothing checks the target exists, which [ADR-392](../decisions/ADR-392.md) section 11 item 5 raised and this leg does not address.

---

### 4. Blockers

None. The row landed what it was dispatched to land.

---

### 5. Landmines

- **`normJob` strips parentheticals, so two jobs differing only inside brackets normalise to one key** and the new duplicate direction would read them as a duplicate. No such pair exists on this tree; one added later would make a correct page report as defective.
- **The severity notes must stay a peer `##` section.** Folding them back under `## Scheduled work` now makes `CI-06l` ERROR rather than pass silently, which is louder, but it is still a shape that stops the gate rather than one it survives.
- **Thirteen line-number coordinates were rewritten into three files this wave while row `396` is repairing pointers across 35 files.** Two sets of repairs to the same coordinates can collide at a merge, and [ADR-330](../decisions/ADR-330.md)'s own record says both sides of such a conflict can be wrong about the merged file. The resolution is to re-derive by grepping for each declaration.
- **The container arrived with no clone for the third consecutive wave** and the clone is shallow. Deriving anything from history without deepening first will answer wrongly rather than refuse.

---

### 6. Files touched

- [`scripts/corpus/gates.mjs`](../../scripts/corpus/gates.mjs)
- [`scripts/corpus/data-model-columns.mjs`](../../scripts/corpus/data-model-columns.mjs) (citation repair only)
- [`docs/decisions/ADR-330.md`](../decisions/ADR-330.md) (citation repair only)
- [`docs/decisions/ADR-182.md`](../decisions/ADR-182.md) (citation repair only)
- [`docs/decisions/ADR-395.md`](../decisions/ADR-395.md) (new)
- [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md), [`docs/decisions/README.md`](../decisions/README.md), [`docs/sessions/README.md`](README.md), [`docs/STATE.md`](../STATE.md) (registries)
- `docs/sessions/2026-09-06-session-590.md` (this file)
