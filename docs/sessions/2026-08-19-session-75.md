---
status: draft
depends_on: [README.md]
last_updated: 2026-08-19
---

## 2026-08-19 - Session 75: a gate for duplicate first-cell keys in docs/ markdown tables (non-money)

**STUB, written as this session's first action so the log exists before the gate does.** Filled at the end.

The merge script resolves conflicts keep-both and dedupes only byte-identical lines over 60 characters. What survives: two `ADR-050` rows with different titles, fourteen duplicate `CI-06` rows in STRATEGY, duplicated STATE passages (`OI-10`). Every one is two sessions appending to one markdown table.

**A merge script runs on one machine; a gate runs on every PR.** `CI-06p` caught the STRATEGY case only because it happened to assert uniqueness over one table. This generalises it.

**Survey before assertion.** If duplicate keys are legitimate somewhere, the finding is WHICH, and the gate states its scope rather than relaxing to fit.

- **Done:** in progress.
- **Next:** in progress.
- **Blockers:** in progress.
- **Landmines:** in progress.
- **Files touched:** in progress.
