---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 597: the `covers` structuring slice is refused at 64 pointers nobody may repair against zero true findings, the 30 of 33 reproduces and is a floor, and the file it lives in is the eighteenth most cited rather than the first

<!--index: **THE ROW IS [ADR-402](../decisions/ADR-402.md), `proposed` and UNSIGNED. CONTROL OVER THE CORPUS'S OWN GATE RUNNER, `E2` READ OWED.** [ADR-398](../decisions/ADR-398.md) section 9 priced a structuring slice over `covers` at 30 of 33 gate definitions and could not take it because [`gates.mjs`](../../scripts/corpus/gates.mjs) was read-only to its whole wave. **This row held that file and REFUSED THE SLICE ON A MEASUREMENT RATHER THAN A FENCE.** Restructuring the field so a runner can separate an assertion from a limitation begins at the first `covers:` in the array and moves **81 pointers**: **17 live and repairable by the row that moves them**, and **64 in DATED RECORDS**, where [ADR-386](../decisions/ADR-386.md) section 6 rules a pointer is NAMED and never repaired, because a commit cannot amend a record. **THERE IS NO CHEAP CORNER OF THE FILE**: the deepest cited line is **9,601**, the LAST LINE of a 9,601-line file, so the free tail is EMPTY and even the last `covers` alone moves 16. **THE YIELD IS ZERO AND IT IS WORSE THAN THE RECORD SAYS.** [ADR-398](../decisions/ADR-398.md)'s predicate, rebuilt against the live array, raises **9 findings over 6 of 33 gates** where that entry reported 3 and 3, and **all nine are FALSE**: each is a parsing mechanism or an anchoring example rather than a claim, and each gate resolves to **exactly one** STRATEGY section 4.4 row, so the row matching is not the variable. **Sixty-four unrepairable pointers for zero true findings is not a close trade.** **THE HANDED PREMISE HOLDS, WHICH IS ITSELF THE FINDING AFTER TEN CONSECUTIVE WAVES OF REFUTATIONS**: **30 of 33** reproduces exactly, on a file byte-identical to that entry's own base. **AND IT IS A FLOOR RATHER THAN A COUNT.** The grammar producing it reads explicit negation, and `CI-06i` states a blind spot as *"Table names only; columns are checked against a live catalogue, which needs a database"*, which no negation pattern matches; a wider grammar gives **31**, and only **two** gates state no limitation at all. **SO THE 30 IS NOT A DEBT, IT IS COVERAGE**: ninety-one percent of these gates document their own blind spot in the one field a person reads when deciding whether a gate does what its name says, and the slice would spend 64 pointers nobody may fix converting prose a person reads well into structure a runner reads badly. **REFUTED: `gates.mjs` AND `repo-invariants.mjs` ARE NOT THE TWO MOST CITED FILES IN THIS REPOSITORY.** Derived through [`citation-fan-in.mjs`](../../scripts/corpus/citation-fan-in.mjs) over 583 files they are the **eighteenth** at 111 edges and the **twenty-fourth** at 80, against `scoped-db.ts` at **455**. **The refutation makes the slice CHEAPER than the dispatch framed it and is reported first for that reason**; the ruling does not rest on it, because 64 unrepairable pointers is 64 at every rank. **[ADR-388](../decisions/ADR-388.md) SECTION 16 QUESTION 2 IS RULED (c), THE `CLAUDE.md` CONVENTIONS LIST, AND OPTION (b) IS REFUTED BY THIS ROW'S OWN CENSUS** rather than declined on taste: `covers` already carries two moods with no grammar separating them, and a rule a check merely recommends would be a third mood in a field that cannot support two. **THE TEXT IS PROPOSED AND NOT TAKEN**, because changing a frozen document is an ADR and not a commit; `CLAUDE.md` is untouched and `git diff 28e2b0ee -- CLAUDE.md` is EMPTY. **[ADR-396](../decisions/ADR-396.md)'s TWO CENSUS HOLES ARE REPORTED AS OWED AND NOT PRICED**, because this slice reads no git history and classifies no pointer. **ONE INSTRUMENT KEPT AND IT IS NOT A GATE**, [`covers-census.mjs`](../../scripts/corpus/covers-census.mjs), on the precedent [`citation-fan-in.mjs`](../../scripts/corpus/citation-fan-in.mjs) sets in the same directory and for its stated reason: two rows built this census by hand and got two different answers from it. It is in no `GATES` array, mints no letter, asserts nothing, carries Rule 2 on both inputs, and **writes no cardinal into itself**, which is [ADR-383](../decisions/ADR-383.md) section 8 and [ADR-386](../decisions/ADR-386.md) section 7 applied to a file this row wrote rather than quoted at files it did not. **[ADR-398](../decisions/ADR-398.md)'s "22 distinct backticked tokens" IS 22 OCCURRENCES OVER 21 DISTINCT TOKENS**, the duplicate being `Artifact: **...**` carried by two gates; annotated and NOT overwritten. **THIS ROW'S OWN FIRST PRICING WAS A GREP AND IT WAS WRONG**: 77 pointers over 65 files where the checks' own grammar gives 81 over 44, which the instrument's header predicts in terms. **NOT ONE `covers` STRING WAS EDITED and `gates.mjs` is byte-identical to its base.** **ZERO CITATION REPAIRS ARE FORCED**: the three registry rows move 7 dated pointers at 2 distinct lines, `:412` and `:638`, both already ruled LEFT by entries that reached them first. **NO GATE ADDED, WIDENED OR WEAKENED, NO INVARIANT MINTED, NO `RI-` RESERVED, NO `CI-06` LETTER SPENT, NO REGISTER GROWN, NO TEST SKIPPED, DISABLED OR QUARANTINED, NO MIGRATION TAKEN OR RESERVED.** `pnpm run verify` and [`falsify.mjs`](../../scripts/corpus/falsify.mjs) **NOT run**, both forbidden to this row. Suite **336 / 8,109 / 10 / 0** against a base of **336 / 8,109 / 10 / 0** reproduced on `28e2b0ee` BEFORE the first edit, **delta ZERO on every axis**; gates **33 of 33** off the runner's own line and **33** again off `GATES.length` as a second reader, invariants **35 of 35**, typecheck, lint and `format:check` clean. **THE CONTAINER ARRIVED WITH NO REPOSITORY FOR THE FIFTH CONSECUTIVE WAVE, THE REPOSITORY WAS NOT NAMED IN THE DISPATCH, AND THE CLONE THIS ROW MADE IS FULL**: `is-shallow` **`false`** at **4,320** commits, where the dispatch's own container returned `true`.-->

---

### 1. The objective

Rule on the `covers` structuring slice [ADR-398](../decisions/ADR-398.md) section 9 priced at 30 of 33 gate definitions and could not take, inside a file that was read-only to that entire wave and is held by this row; and rule on [ADR-388](../decisions/ADR-388.md) section 16 question 2, which [ADR-400](../decisions/ADR-400.md) recorded untouched. One objective, non-money, control over the corpus's own gate runner.

---

### 2. What was measured before the first edit

**The container arrived with NO REPOSITORY AT ALL**, for the fifth consecutive wave. The stated working directory was empty, `git rev-parse --is-inside-work-tree` returned *fatal: not a git repository*, and the repository was not named in the dispatch either; it was resolved from this session's authorized set and cloned by hand.

**The clone is FULL and this was RUN rather than assumed in either direction.** `git rev-parse --is-shallow-repository` returned **`false`**, with **4,320** commits. **The dispatch stated its own container returned `true`**, and instructed this row to measure rather than inherit. That instruction was right and the answer is the opposite of the one it carried.

**The base was reproduced on `28e2b0ee` before the first edit**, on an empty `git status --porcelain`: gates **33 of 33**, invariants **35 of 35**, suite **336 files / 8,109 passed / 10 skipped / 0 failed**, `typecheck`, `lint` and `format:check` all clean. **The gate count was DERIVED twice**, from the runner's own summary line and again by importing `GATES` and reading its length, and never assumed at 33.

**`pnpm run verify` was NOT run and `falsify.mjs` was NOT run.** Both are forbidden to this row.

---

### 3. What shipped

[ADR-402](../decisions/ADR-402.md), and one new file: [`covers-census.mjs`](../../scripts/corpus/covers-census.mjs), an instrument and not a gate. **No `covers` string was edited.** `git diff 28e2b0ee -- scripts/corpus/gates.mjs` is EMPTY, so the file the slice would have restructured is byte-identical to its base and not one of its 111 inbound pointers moved.

---

### 4. Next

Rows `401` and `403` hold their own subjects this wave and this row guessed neither. **What this row leaves owed** is written out in [ADR-402](../decisions/ADR-402.md) section 11: [ADR-396](../decisions/ADR-396.md)'s two census holes, unpriced because this slice touches no part of that instrument; `CI-06e`'s missing limitation clause, which costs the whole slice's 81 pointers to add one sentence and should ride along with the next diff that must move that region anyway; and the proposed [`CLAUDE.md`](../../CLAUDE.md) line, which needs a founder because it is a change to a frozen document.

---

### 5. Landmines

- **A grep is not the citation grammar, and this row proved it on itself.** The first pricing of the slice was a `grep -rE` and gave 77 pointers over 65 files; [`citation-fan-in.mjs`](../../scripts/corpus/citation-fan-in.mjs) gives **81 over 44**, and the disagreement runs in both directions at once. Any row pricing an edit should use the instrument.
- **`covers` figures are grammar-dependent and a single number hides it.** 30 under explicit negation, 31 when `only` is read as a limitation. A row handed one of these is holding one end of an interval.
- **The free tail of `gates.mjs` is EMPTY.** The deepest cited line is 9,601 in a 9,601-line file, four of the deepest pointers being `data-model-columns.mjs` reading this runner's tail, so *"growth below the deepest cited line is free"* buys nothing at all here.

---

### 6. Files touched

- `docs/decisions/ADR-402.md` (new), `scripts/corpus/covers-census.mjs` (new)
- `docs/decisions/ALLOCATION.md`, `docs/decisions/README.md`, `docs/sessions/README.md` (generated span), `docs/STATE.md`, this log
- **`scripts/corpus/gates.mjs`, `packages/tooling/**` and [`CLAUDE.md`](../../CLAUDE.md) were READ and never opened for writing.**
