---
status: approved
depends_on: [README.md]
last_updated: 2026-08-30
---

## 2026-08-30 - Session 449: the comment named a blocker that had discharged, and the half nobody re-derived is that `prior` was never the expensive read

<!--index: **THE RULING IS [`ADR-258`](../decisions/ADR-258.md), `proposed` and UNSIGNED, MONEY PATH, `E2` READ OWED, AND TWO `E2` DEBTS ARE INHERITED** because [ADR-250](../decisions/ADR-250.md) and [ADR-248](../decisions/ADR-248.md) are both UNSIGNED and this row calls the first and rests on the second. **NO MIGRATION NUMBER AND NO `RI-` NUMBER WERE TAKEN.** Row `258` said to re-derive one comment before building anything. **IT IS FALSE IN EVERY CLAUSE.** [`adapter.ts:29`](../../apps/worker/src/batch/adapter.ts) read *"`loadAccountDay` REFUSES. six fields, and `prior` needs the codec"*; the cheap half is that [ADR-250](../decisions/ADR-250.md) landed the codec, and **THE HALF THAT MATTERS IS THAT `prior` NEVER NEEDED ANYTHING ELSE**. `AccountDay.prior` is a `RuleState` and NOT a `RuleStateRow`, so it carries no `contextGates`, no `stateHash`, no `calendarRevisionId` and no `accountId` -- **the four fields [ADR-250](../decisions/ADR-250.md) section 7 lists as what makes `storedRuleStates` expensive.** That paragraph is TRUE of its own subject and reads as true of this one to anybody who does not open both interfaces, which is the defect class this estate keeps finding wearing its most convincing costume. **FIVE FIELDS OF SIX NOW RESOLVE OFF ROWS**: `plan` (`resolvePlan` over the PINNED `plan_versions.rules` and its `plan_version_sizes` row, matched on `size_cents` IN PROCESS because `0004:220`'s key is a standalone `CREATE UNIQUE INDEX` `packages/db` refuses as an ADDRESS), `prior` (the latest `rule_states` row STRICTLY before the day, `INV-14`, through `decodeEngineGates`), `mark` (the unsuperseded `daily_marks` row, and TWO live rows is REFUSED rather than resolved), `settlements` (`settled` `payout_requests` whose `effective_trading_day` is the day, `SD-03`) and `openedOn` (`R-32`'s anchor, read and never derived). **AN ACCOUNT WITH NO LIVE MARK IS ANSWERED `null`**, which `ports.ts` declares and which the shipped port threw on. **THE SIXTH REFUSES AND NOTHING IS SYNTHESISED**: `external` is [ADR-248](../decisions/ADR-248.md)'s ruling 3, and the five are resolved BEFORE the throw so the message carries a WITNESS that they did. **A FINDING NOBODY WENT LOOKING FOR AND IT IS THE MOST CONSEQUENTIAL THING HERE**: [`apps/site/src/catalog/adapter.ts:552`](../../apps/site/src/catalog/adapter.ts) ALREADY decodes `PlanRulesJson` and `PlanVersionSizeRow` key for key, so this diff is the SECOND statement of one stored shape and that is `FM-16` by name; the two read different renderings today (that one the WIRE, this one the COLUMN, which is [ADR-250](../decisions/ADR-250.md) section 5's own line) and the merged one's home is `packages/rules-engine`, **which this row's fence forbids**, so it is registered and not taken. **AND ONE ROW CANNOT BE FOLDED BY THIS BUILD, SAID OUT LOUD**: `min_settlement_lag_trading_days` is required by `M01` section 2.4 and ABSENT from DATA_MODEL section 11's example, and it is READ rather than defaulted to `0`, because a default is the plan parameter in application code DATA_MODEL section 12 bans. **TWO CASES WATCHED RED ON UNMODIFIED SHIPPED SOURCE** driving the deployment's own `postgresBatchPorts`, and **TEN SEEDS FIRED ON THE LANDED MODULE**, every restore verified with `sha256sum -c`. **THE PORT IS NOT WIRED AND THE WIRED COUNT DOES NOT MOVE**: `usePayoutBackend`'s second clause narrows and its blocker MOVES into clause six, which already carried `ADR-248`. Counts derived at reporting time: **33 of 33** gates, **22 of 22** invariants, **279 files / 6,724 passed / 6 skipped** against a baseline of **278 / 6,698 / 6** measured on this tree before a byte changed, typecheck clean, lint 0, `format:check` clean. **`falsify.mjs` NOT run**: forbidden by this row and it mutates the working tree. **A LANDMINE THAT IS NOT THERE, AND THE CLAIM WAS INHERITED RATHER THAN DERIVED**: sessions 439 and 445 recorded that `docs/STATE.md` carries the `gen:adr_count` paragraph FOUR and then EIGHT times, at `RI-12`'s ceiling, so a ninth copy would turn it RED. **RE-DERIVED ON THIS TREE: THAT PARAGRAPH APPEARS ONCE.** The other six occurrences of the token are PROSE ABOUT the duplication, including session 445's own. `STATE.md`'s worst repeated line of 80 characters or more appears **5** times and it is a different line (`**The position:** ... gates.mjs check ...`); the file actually AT the ceiling is `docs/reviews/2026-08-20-admin-parity-audit.md` at **8**. **A landmine repeated across three session logs without one of them opening the file is the same defect this row was sent to find.** -->

**Objective.** Re-derive `adapter.ts:29`, then build every field of an `AccountDay` that is
buildable and name what is not.

**Outcome.** The comment is false in every clause, and the interesting half is not the one the row
anticipated. Five fields resolve. One refuses, and it is the one [ADR-248](../decisions/ADR-248.md)
already ruled.

### 1. What the row claimed, and what the tree says

| Row `258` and the dispatch | Derived before a line changed |
|---|---|
| `adapter.ts:29` carries that sentence | **HOLDS**, at exactly that line |
| [ADR-250](../decisions/ADR-250.md) landed the codec at `614e7c83` | **HOLDS** |
| `nightly.ts:299` calls the port | **HOLDS** |
| `adapter.ts:445` refuses by name | **HOLDS**, `:445` the method and `:446` the throw |
| `ports.ts:48` declares the six fields | **HOLDS** |
| *"a comment naming a blocker that has since discharged"* | **HOLDS, and worse than the row expected.** Section 2 |

**ONE LIVE CITATION IN THIS TREE HAD ALREADY GONE STALE AND IT IS NOT THE ROW'S.**
[`wiring.test.ts`](../../apps/api/test/wiring.test.ts) points at `adapter.ts:473` for the installed
encoder, which this diff moves to `:535`. It is **repaired there**, because that is a live reason.
The same pointer in [ADR-245](../decisions/ADR-245.md), [ADR-250](../decisions/ADR-250.md),
[STATE](../STATE.md) and session 440's log sits under a **dated heading**, which `RI-16` excludes on
`CI-06`'s own ruling that rewriting a dated record is the worse defect. **Add 62 when reading those
four.**

### 2. The finding

**`prior` IS A `RuleState`. `storedRuleStates` RETURNS `RuleStateRow`s. THEY ARE DIFFERENT TYPES AND
THE DIFFERENCE IS EXACTLY THE EXPENSIVE PART.**

| Field | `RuleStateRow` | `RuleState` |
|---|---|---|
| `contextGates` | **YES** | no (`INV-23`) |
| `stateHash` | **YES** | no (`SD-08` is computed FROM a state) |
| `calendarRevisionId` | **YES** | no ([ADR-047](../decisions/ADR-047.md)'s stamp on the ROW) |
| `accountId` | **YES** | no (the fold is per account by construction) |

[ADR-250](../decisions/ADR-250.md) section 7 names three of those four as why
`storedRuleStates` still refuses, and it is **right about its own subject**. A successor reading it
concludes the same of `prior`, because the paragraph verifies.

### 3. What was built

| Artefact | What it holds |
|---|---|
| [`adapter.ts`](../../apps/worker/src/batch/adapter.ts) | The header table row, both blockers rewritten, `loadAccountDay` implemented, and one new section: the plan decoder, the size-row reader, the `RuleState` rebuilder, the live-mark and settlement reads, and the `witness` |
| [`account-day.test.ts`](../../apps/worker/test/account-day.test.ts) | 26 cases. `plan` compared WHOLE against `fixtures.PLAN`, which is M01 Appendix A.1; `prior` compared WHOLE against the state `foldAccountDay` produced, through `JSON.stringify` and back |
| [`fixtures.ts`](../../apps/worker/test/fixtures.ts) | The stored-row half of Core EOD at 50K, extracted for this file's own stated reason so the suite and the entry-point harness share one copy |
| [`entrypoint-harness.ts`](../../apps/worker/test/entrypoint-harness.ts) | `refusing-port` now carries a WHOLE account day, because a two-key stub mark no longer reaches a port refusal |
| [`wiring.test.ts`](../../apps/api/test/wiring.test.ts) | `usePayoutBackend`'s second clause narrowed, `adapter.ts:473` repointed |

### 4. The transcript

**RED ON UNMODIFIED SHIPPED SOURCE, AND IT IS A BEHAVIOUR.** `origin/main`'s `adapter.ts` was
restored and the deployment's own `postgresBatchPorts` was driven through the real port:

```
FAIL  RED A: an account with no live mark is answered null over the real port
BatchPortUnwired: BatchPorts.loadAccountDay cannot be served by this deployment: an `AccountDay`
carries six fields ... and this adapter resolves none of them
  at Object.loadAccountDay apps/worker/src/batch/adapter.ts:446:29

FAIL  RED B: the refusal cites ADR-248 and not the codec
AssertionError: expected '...' to contain 'ADR-248'
```

**TEN SEEDS, EACH RED, EACH RESTORED WITH `sha256sum -c` PASSING**: the prior admitting the day's own
row (1 case); settlements keyed on `settled_trading_day` (1); `source_hash` rendered base64 (2);
`jsonCents` admitting an unsafe integer (1); the size grid not matched on the account's size (17);
superseded marks folded (2); `min_settlement_lag_trading_days` defaulted to `0` (1); the breach pair
unchecked (1); the port refusing without reading anything (2); and `DRAWDOWN_TYPES` narrowed so
`CV-01` becomes unreachable (1).

| Check | Baseline | Final |
|---|---|---|
| `gates.mjs generate` then `check` | 33 of 33 | **33 of 33** |
| `repo-invariants.mjs`, last line | 22 of 22 | **22 of 22** |
| `npx vitest run` | 278 / 6,698 / 6 skipped | **279 / 6,724 / 6 skipped** |
| `pnpm run typecheck` | clean | clean |
| `pnpm run lint` | clean | clean |
| `pnpm run format:check` | clean | clean |

`pnpm run verify` and `falsify.mjs` were **not run**: the row forbids the first by name and the
second mutates the working tree.

### 5. Where this session is most likely wrong

**NO DATABASE WAS REACHABLE AND THE WHOLE OF SECTION 3 IS READ THROUGH A RECORDER.** Four driver
behaviours are assumed rather than proved: `date` as a string, `bytea` as a `Uint8Array`, `bigint`
columns as `bigint`, and `jsonb` already parsed. They are the same four `adapter.ts`'s shipped ports
already assume and they are `packages/db`'s to prove.

**THE PRIOR IS FOUND BY SCANNING THE ACCOUNT'S WHOLE `rule_states` HISTORY**, because the accessor
publishes no ordered or limited read. `rule_states_account_day_desc_idx` exists for exactly this
query and nothing uses it. Same trade `accountsWithStoredState` already declares and calls wrong.

**AND THE `null` ARM MAKES ONE PATH QUIETER THAN IT WAS.** An account whose mark is superseded
between the partition read and this call is now `absent` and the batch exits zero. That is
`ports.ts`'s declared contract, and it is the one direction in which this diff removes a throw.

### 6. What is still owed

- **The `E2` read on [`ADR-258`](../decisions/ADR-258.md)**, MONEY PATH, and the three it inherits.
- **`R-38`'s grain.** Row `254` and session 446. It is the last field, and
  [ADR-248](../decisions/ADR-248.md) section 12 already says it is the cheapest thing on the
  critical path to decide.
- **The merged plan codec** in `packages/rules-engine`, section 4 of the ADR. Two decoders exist
  now; the cost of three is not linear.
- **One line in DATA_MODEL section 11 or in `M01` section 2.4.** Until one moves, a
  `plan_versions` row written to the approved example cannot be folded.
- **An ordered read on `packages/db`** for the prior, before the fold runs nightly.

### 7. Landmines for the next session

- **A TRUE PARAGRAPH ABOUT A NEIGHBOURING PORT IS THE HARDEST STALE REASON TO SEE.**
  [ADR-250](../decisions/ADR-250.md) section 7 is correct about `storedRuleStates` and was read by
  this row's author as covering `prior`. Nothing mechanical in this tree compares a reason's SUBJECT
  against the type it is about.
- **`apps/api/test/rule-state-producibility.test.ts` STILL SAYS "it serves four of ten" AND
  "the six refusals"** in two `describe` titles. Its assertions all pass, because they sweep for
  `new BatchPortUnwired('<port>'` and all five names still appear. **That file is outside this
  row's fence**, so the stale titles are reported rather than repaired.
- **THE `RI-12` LANDMINE THREE SESSION LOGS HAVE NOW CARRIED IS NOT THERE, AND NOBODY OPENED THE
  FILE.** Session 439 recorded FOUR copies of `STATE.md`'s `gen:adr_count` paragraph, session 445
  recorded EIGHT and called it "no headroom left: a ninth copy turns `RI-12` RED". **Re-derived on
  this tree: that paragraph appears ONCE.** The other six occurrences of the token are prose ABOUT
  the duplication, session 445's own included, and one of them (session 421's section, `STATE.md`
  line 9394) records the collapse to one copy. `STATE.md`'s worst repeated line of 80 characters or
  more appears **5** times and is a different line entirely. **The document actually at `RI-12`'s
  ceiling is `docs/reviews/2026-08-20-admin-parity-audit.md`, at 8**, and no session log names it.
  This is the same defect class as the one this row was sent to find, in the handoff journal rather
  than in a source comment.
- **`pnpm vitest` IS NOT A SCRIPT IN THIS WORKSPACE.** `npx vitest run` or `pnpm test`.
- **DEPENDENCIES ARE NOT INSTALLED IN A FRESH CONTAINER.** `pnpm install --frozen-lockfile` first.
