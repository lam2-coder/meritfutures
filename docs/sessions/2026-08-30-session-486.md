---
status: approved
depends_on: [README.md]
last_updated: 2026-08-30
---

## 2026-08-30 - Session 486: the approval branch landed, and the lock its own ADR ruled is still one fence away

<!--index: **[ADR-287](../decisions/ADR-287.md) SLICE 6 LANDED. THE RULING IS [`ADR-295`](../decisions/ADR-295.md), NINE SECTIONS, `proposed` AND UNSIGNED. MONEY PATH, `E2` READ OWED.** `postgresPayoutBackend.insertPayoutRequest` writes an `approved` `payout_requests` row through the scoped door and `PayoutTx.insertPayoutRequest` returns `Promise<void>`. **THE HARD HALF WAS [ADR-293](../decisions/ADR-293.md)'s AND IS IMPLEMENTED RATHER THAN REDONE**: that entry proved by compiler that the declaration was the only error and ruled the lock, and neither is re-decided here. **[ADR-289](../decisions/ADR-289.md)'s SEVEN OWED LINES ARE ALL SEVEN TAKEN**, matched on text and not on number, and the seventh is INVERTED rather than deleted: the tautology that checked the route forwarded what a fixture invented becomes `expect(body).not.toHaveProperty('eligibility_snapshot_id')`, so the field cannot be restored without a red test. **THE THREE TRAPS WERE RE-DERIVED AT SOURCE ON THIS TREE BEFORE A CHARACTER WAS WRITTEN AND ALL THREE HOLD**: `split_bp` and `clamp_reason` return ZERO lines across the migrations and both values are already inside `eligibility_snapshot`, so the body names NEITHER and drops NEITHER; `plan_version_id` is `NOT NULL`, is absent from the insert shape, and is derived from `accounts` ON THE SAME TRANSACTION, sound because `0007:12` states the column never moves and `0027:272` is the trigger that raises if it does; and `identity_id` is stamped by the handle, so naming it is REFUSED. The insert names ELEVEN of the shape's FOURTEEN fields plus the derived one, and an absent `accounts` row is a `PayoutRowError` and a 500 rather than a default. **THE HOLD BRANCH REFUSES BY NAME**, `PayoutBackendUnwired('insertPayoutRequest.hold')`, on a TWO-SIDED predicate matching `payout_requests_hold_is_complete`: no clause id was invented, no enum of clause names written and no default string chosen, and `DEP-M7-05` still owes the clauses to COUNSEL. **THE LOCK [ADR-293](../decisions/ADR-293.md) RULED IS INHERITED AND IS NOT BUILT, WHICH IS THIS ROW'S ONE RESIDUE AND IS REPORTED LOUDLY RATHER THAN QUIETLY.** Clauses 2 and 4 of that ruling are the `PayoutTx.lockScope()` declaration and the `decidePayout` call, both lines of `routes/payouts.ts`, and row `295` granted that file *"for the declaration and ADR-289's seven lines ONLY"*. Clause 3 does not compile without clause 2, so there was no partial arm to take. **THE FENCE WAS NOT WIDENED**, the residue is written into `usePayoutBackend`'s own blocked reason where the installing session cannot avoid it, and it is a SLICE 9 PRECONDITION rather than a live exposure because `start.ts` still does not call `usePayoutBackend`. **THE DISPATCH DEFECT [ADR-293](../decisions/ADR-293.md) SECTION 2.4 NAMED IS RECORDED A SECOND TIME AND IS STILL THE DISPATCHER'S**: that entry said one row should take A to D and its fence must name `routes/payouts.ts`; row `295` named the file and granted A and C. **`RI-34` WAS MINTED AND SPENT**, reserved to row `293` and returned by [ADR-293](../decisions/ADR-293.md) section 5 with this exact candidate named as worth taking on the day a body landed. Four legs (no `splitBp` or `clampReason` in either spelling, no tenancy column, `planVersionId` present, and a SPREAD is its own finding), five sentinels that THROW, and ONE STATED VACUOUS ARM. **FIVE SEEDS WENT RED ON THE REAL TREE AND EVERY RESTORE WAS VERIFIED**; the two SCHEMA sentinels were driven against a FIXTURE ROOT so `packages/db/migrations/**`, out of fence and sacred under `E2`, was never opened for writing. **`packages/tooling/test/repo-invariants.test.ts` CAUGHT THE FIRST VERSION**: its synthetic tree has no payout schema and the schema sentinels threw on it, and the repair moved the PRECONDITION to `CREATE TABLE payout_requests` rather than loosening a sentinel, which made sentinel two stronger. **THE CENSUS MOVED IN THREE TEST FILES RATHER THAN GOING STALE**, and `rule-state-producibility.test.ts` now asserts the hold-arm refusal is PRESENT, so a session that "completes" this member by inventing a clause id has to delete that line. **SEVEN CITATIONS WERE REPOINTED BY `RI-15` AND `RI-16`, ALL OF THEM STALENESS THIS DIFF CREATED.** **NO `start.ts`, NO MIGRATION WRITTEN OR NUMBER RESERVED, NO DOOR WIDENED, `CATALOG_TABLE_KEYS` STILL FIVE, NO SYNTHESISED DEFAULT, NO CAST, NO FLOAT IN SOURCE OR FIXTURE, MERIT STILL PASSWORDLESS. THE COUNTS DID NOT MOVE: `{ declared: 24, wired: 10, blocked: 14 }`**, confirmed at source before and after, and `grep -c 'usePayoutBackend(' apps/api/src/start.ts` returned `0` both times. Counts derived at reporting time, each command run separately: **33 of 33** gates after `generate`, **32 of 32** invariants off the runner's own last line, **298 files / 7,258 passed / 6 skipped** against a baseline of **298 / 7,249 / 6** measured on this tree before the first edit, a delta of **exactly nine cases and no new file**: nine added in `payout-backend.test.ts` less one lost as an `it.each` list shrank from three members to two, plus one from `repo-invariants.test.ts`'s per-check `test.each` picking up `RI-34`. Typecheck 0, lint 0, `format:check` clean, `falsify.mjs` clean and FOREGROUND with `git status` verified byte-identical after it. **`pnpm run verify` was NOT run**, forbidden by the row. **NO GITHUB ACCESS: NO PR WAS OPENED AND NOTHING WAS MERGED**, per the dispatch. -->

---

### 1. The objective

**[ALLOCATION](../decisions/ALLOCATION.md) row `295`: [ADR-287](../decisions/ADR-287.md) slice 6, `insertPayoutRequest()`, the approval branch and the `hold === null` path, with the declaration changed to `Promise<void>` per [ADR-289](../decisions/ADR-289.md).** One objective, money path, [ADR-003](../decisions/ADR-003.md) strict.

### 2. What was read before anything was written

Row `295` in full, then [ADR-293](../decisions/ADR-293.md), [ADR-289](../decisions/ADR-289.md), [ADR-291](../decisions/ADR-291.md)'s subject through `payout-backend.ts` itself, and [ADR-287](../decisions/ADR-287.md) finding F3 with its slice table. Then, at source rather than out of any of them: `0010_payouts.sql`, `0007_accounts.sql`, `0027_triggers_invariants.sql`, `0030_payout_hold_enum.sql`, `0031_payout_hold_and_identity_restriction.sql`, `packages/db/src/scope.ts`'s two registrations, `ScopedTx`'s declaration in `scoped-db.ts`, `payoutRequests` in `schema.ts`, `databaseWithdrawalBackend`'s shape in `wallet-withdrawals.ts`, `db-recorder.ts`'s verb list, and every test that names `insertPayoutRequest`.

### 3. Done

- **[`apps/api/src/routes/payouts.ts`](../../apps/api/src/routes/payouts.ts).** [ADR-289](../decisions/ADR-289.md)'s five lines in that file: the response field deleted, the port docblock rewritten with the ruling's reason, `insertPayoutRequest(row: PayoutRequestInsert): Promise<void>;`, the destructure reduced to a call, and the wire field deleted. **Nothing else in that file.**
- **[`apps/api/src/payout-backend.ts`](../../apps/api/src/payout-backend.ts).** The approval-branch body, the hold-branch refusal, a `text()` narrowing beside `member()`, and [ADR-293](../decisions/ADR-293.md) section 4D's two prose corrections: the retired return-value comment and the false `lockScope` sentence, both corrected where they were made.
- **[`apps/api/test/payout-backend.test.ts`](../../apps/api/test/payout-backend.test.ts).** Nine cases over the new member, driven against `db-recorder.ts`, with every money value a `bigint` literal. The refusing-member list drops to two.
- **[`apps/api/test/payouts.test.ts`](../../apps/api/test/payouts.test.ts).** [ADR-289](../decisions/ADR-289.md)'s two remaining lines, the tautology inverted rather than deleted.
- **[`apps/api/test/rule-state-producibility.test.ts`](../../apps/api/test/rule-state-producibility.test.ts) and [`apps/api/test/wiring.test.ts`](../../apps/api/test/wiring.test.ts).** The census and `usePayoutBackend`'s clause FIVE, both moved rather than left stale, and the owed lock line written into the blocked reason.
- **[`packages/tooling/checks/repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs).** `RI-34` only.
- **[ADR-295](../decisions/ADR-295.md)**, nine sections with every transcript, `proposed`, UNSIGNED. Register row, next-free note, [STATE](../STATE.md), this log.

### 4. Blockers

**None met. One residue is REPORTED and not taken**, and it is section 3 of the entry: [ADR-293](../decisions/ADR-293.md)'s lock ruling needs two lines of `routes/payouts.ts` that this row's fence does not reach.

### 5. Landmines for the next session

- **THE PAYOUT PATH IS RULED TO LOCK AND DOES NOT.** `PayoutTx` declares no `lockScope`, `decidePayout` calls none, and the read-then-write this slice created is backstopped only by `payout_requests_no_in_flight_uq`, which turns a contract-specified 409 into a 500 and is keyed per account where the exposure question is per identity. **It costs nothing today because `start.ts` does not call `usePayoutBackend`.** It is a slice 9 precondition and it is written into that entry's blocked reason.
- **`db-recorder.ts` HAS NO `lockScope` VERB.** The moment clause 3 lands, `postgresPayoutBackend` calls `handle.lockScope()` and every case in `payout-backend.test.ts` throws a `TypeError`. It is one verb and it was deliberately not added ahead of the member.
- **THE HOLD ARM'S REFUSAL NAME IS LOAD BEARING.** `rule-state-producibility.test.ts` asserts `new PayoutBackendUnwired('insertPayoutRequest.hold')` is present and the plain `('insertPayoutRequest')` is absent.
- **`RI-34` HAS ONE ARM THAT PASSES OVER NOTHING**, keyed on `CREATE TABLE payout_requests` being in the migrations. It exists for the synthetic fixture and it is safe only while migrations stay append-only.

### 6. Files touched

`docs/decisions/ADR-295.md` (new), `docs/decisions/ALLOCATION.md`, `docs/decisions/README.md` and `docs/sessions/README.md` (both generated), `docs/STATE.md`, this log, `apps/api/src/routes/payouts.ts`, `apps/api/src/payout-backend.ts`, `apps/api/test/payout-backend.test.ts`, `apps/api/test/payouts.test.ts`, `apps/api/test/rule-state-producibility.test.ts`, `apps/api/test/wiring.test.ts`, `packages/tooling/checks/repo-invariants.mjs`.

### 7. Next

**Row `295` is complete and the branch is left mergeable.** The next row on this port is the one that takes [ADR-295](../decisions/ADR-295.md) section 5A: the lock, four items, money path, and **its fence must name [`apps/api/src/routes/payouts.ts`](../../apps/api/src/routes/payouts.ts) without a clause limiting it to named lines.** [ADR-287](../decisions/ADR-287.md)'s stale F2 citations and the corpus gate refused three times now are section 5B and 5C. `RI-32` and `RI-35` are both free.
