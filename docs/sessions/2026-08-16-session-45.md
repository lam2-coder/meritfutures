## 2026-08-16 - Session 45: golden fixtures batch 1, three to eighteen of 284 (non-money)

- **Done:** **Fifteen fixture pairs**, each a YAML input and an expected end-state JSON sibling in the format [GOLDEN_SCENARIOS section 2](../testing/golden-scenarios/02-fixture-format.md) rules: **GS-002, GS-005, GS-006, GS-007, GS-010, GS-012, GS-013, GS-014, GS-015, GS-016, GS-017, GS-018, GS-019, GS-025, GS-063**. Every value is transcribed from [M01](../plans/M01-rules-engine.md)'s rule taxonomy, its Appendix A.1 column and the scenario's own registry row, and every header names which document ruled which number (`TR-01`). **`pnpm run verify` green; CI-03 reports 18 fixtures loaded, 0 load failures, coverage 18 of 284.**
- **Next:** The **fixture-wiring session** is still the one everything waits on, unchanged by this: the resolvable-citation `L-nn` rule, the loader's `bigint` comparison, then [ADR-048](../decisions/ADR-048.md)'s per-fixture polarity, in that order. Batch 2 of the fixtures needs one of two unblockings, and they are different sessions: a **second plan record** transcribed from the Merit Rapid column unblocks GS-020 to GS-024, and the **calendar transcription** (`S-E`, founder) unblocks GS-001, GS-003, GS-004 and GS-030 to GS-032.
- **Blockers:** None for this batch. The two findings below are founder items and neither blocks a fixture that is written.
- **Landmines:**
  - **M01 DISAGREES WITH ITSELF ON THE FLOOR LOCK, ON EVERY DAY THAT OVERSHOOTS THE TRIGGER.** Section 3.6's `DO-7` trails to `hwb - drawdown` and then **assigns** `floor = floor_lock_floor_at_cents`; [section 3.4](../plans/M01-rules-engine.md)'s binding formulation takes `max(hwb - drawdown, locked value)` and calls the `max` "redundant given the update order". **The `max` is redundant only where `CV-12` makes the two equal, which is the day the trigger is met exactly.** On a Core EOD 50K day closing at `size + 299,999c` the assignment gives **5,010,000c** and the formula gives **5,049,999c**, and the assignment is the one that moves the floor DOWN relative to the value it computed four lines earlier. **`GS-018`'s expectation pins no floor because of this** and says so in its own note; `GS-015` and `GS-016` are unaffected because their lock day meets the trigger to the cent, which is exactly what `GS-015` exists to pin.
  - **THE FUNDED RESET LOWERS THE FLOOR, AND `INV-06` SAYS THAT NEVER HAPPENS.** `R-31` sets `floor = size_cents - funded drawdown_cents` on the pass day. On Core EOD the eval target is **300,000c** and the lock trigger is **260,000c** ([Appendix A.1](../plans/M01-rules-engine.md)), so **every eval pass is also a lock day**: the floor is at least 5,010,000c when `DO-7` finishes and `R-31` writes 4,750,000c at `DO-8`. `INV-06` reads "the floor never decreases. **No exception, no phase qualifier**, no settlement carve-out". `R-12` sanctions it in its own words ("at account open, **and again at the funded reset**") and the `GS-019` registry row states 4,750,000 outright, so **the fixtures follow `R-12` and the row**. The tension is `INV-06`'s phrasing against `R-12`'s second clause. **`RE-P-01` as literally stated fails on `GS-019`**, which is the form the founder will meet it in if it is not ruled first.
  - **NEITHER FINDING WAS EDITED INTO A FROZEN DOCUMENT AND NO `EC-nnn` WAS CLAIMED**, for [session 44](2026-08-16-session-44.md)'s reasons unchanged: a defect in a frozen document is an ADR rather than a commit, and an edge case arrives with a number from the allocation table and a regenerated count, from the session that rules it. `TR-04` wants an `EDGE_CASES` entry for each, and the FREEZE gate wants an ADR to add one; **that ordering is the founder's to resolve**, and both findings are recorded here, in the expectations that met them, and in the pull request.
  - **`floor_locked` IS PINNED BY NOBODY ON A PASS DAY, ON PURPOSE.** `R-31` lists what the reset zeroes and the lock flag is not on the list, so it carries through beside a floor **below** the value it locked to. No document says whether it survives. `GS-017` and `GS-019` state every other field `R-31` names and leave that one out rather than choose a reading.
  - **A ZERO CAN BE RIGHT FOR THE WRONG REASON, AND `GS-025` IS WHERE IT SHOWS.** `R-35` returns 0 for any account that is not funded, so a fold that begins in `eval` reports the pinned withdrawable of 0 without the buffer arithmetic ever running. `account.phase` reaches no engine input today (`AWAITING_M01_INPUT`, `L-09`), so **the fixture pins `phase: funded` beside the zero**: when polarity flips, an engine folding this stream in `eval` fails on the phase field rather than passing quietly on the zero.
  - **FOURTEEN OF THE FIRST TWENTY-NINE SCENARIOS ARE STILL UNWRITABLE, AND IT IS THE FORMAT RATHER THAN THE ENGINE.** The fixture calendar declares five **full** sessions and no fill stream, so every scenario turning on a session's kind (GS-001, GS-003, GS-004, GS-030 to GS-032) would need a measurement invented. Core EOD **disables eval consistency**, so GS-020 to GS-024 need a second plan record. Payout arithmetic (GS-026 to GS-029, GS-042) is not the day fold and the sibling has no shape for a clamp result. All four groups are named in [`fixtures/README.md`](../../packages/rules-engine/fixtures/README.md) rather than left as an absence.
  - **THE BOUNDARY PAIRS ARE ONE ASSERTION IN TWO FILES AND THE DIFF IS THE PROOF.** GS-006 against GS-007, GS-013 against GS-014, GS-017 against GS-018, and GS-019 against GS-063 each differ in **exactly one input**. A reader checking that a fixture pins an operator rather than an outcome can diff the pair; a future session that "tidies" a second measurement between two of them destroys the assertion without touching either expectation.
- **Files touched:** `packages/rules-engine/fixtures/` (30 new files, `README.md` updated), `docs/{STATE}.md`, `docs/sessions/`.

### The count, reported rather than implied

**I am at 18 of 284.** Three existed; fifteen landed here. The number is not written into a test: `stageCoverage()` re-derives it on every CI-03 run and the block prints it, which is why this line and the stage cannot disagree for long.

**The fifteen are not the easiest fifteen, they are the writable fifteen.** Everything in GS-001 to GS-029 that the current fixture format can state without inventing an input is now written, plus GS-063 because it is GS-019's day with one number changed. What is left of that range is blocked on a calendar, on a second plan record, or on a payout evaluation that is not a day fold, and each is named above and in the fixture README.

### Why this was safe to write before the rules exist

**[ADR-048](../decisions/ADR-048.md), read forward rather than waited on.** Polarity is derived per fixture from the rules the fixture cites, so a fixture for a rule the engine has not implemented is **inert rather than a false red**. Every file here declares its rule set in `source:` as a list of `R-nn` that exist in M01 section 3.5, which is also the prerequisite ADR-048 names: the citations are resolvable on the day the `L-nn` rule that enforces resolvability lands, rather than needing a sweep afterwards.

**CI-03's polarity was NOT touched**, and nothing in `packages/rules-engine/src` or `test/` was opened: another session owns both, and a fixture derived from an implementation proves only that the code agrees with itself.
