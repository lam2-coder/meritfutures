## 2026-08-16 - Session 45: DO-8, the eval progression, group E (money path)

- **Done:** **`advanceDay` folds an eval-phase day end to end.** [`packages/rules-engine/src/day/progression.ts`](../../packages/rules-engine/src/day/progression.ts) is [M01 section 3.1](../plans/M01-rules-engine.md)'s DO-8 and [`day/consistency.ts`](../../packages/rules-engine/src/day/consistency.ts) is R-29 and R-30 in the one place M01 asks for them ("called by both phases, so the two variants can never drift apart"). **Six new rules: R-26, R-27, R-28, R-29, R-30, R-31, taking the count to twenty-two of fifty**, each with an `RE-U-nn` asserting its operator at the boundary on both sides. **`nextTradingDayAfter` lands in `calendar.ts`** for R-31's period start. **A reachable `INV-06` defect in the landed floor is fixed** (see landmines). **`pnpm run verify` green: 322 tests, 15 gates clean and dirty, 13 CI-02 seeded violations, 16 scope cases.**
- **Next:** Group F (`DO-9`'s gates, R-33 to R-41, which also gives `RuleState` its `withdrawableCents`, `engineEligible` and `engineGates`) or group G (`clampPayout`, R-42 to R-45, `PT-05`, and the `INV-11` arithmetic P2 section 5 assigns to P2 under its own name). **The fixture-wiring session is a third and it is still the one ADR-048 waits on**: the `L-nn` resolvable-citation rule, the loader's bigint comparison, then the per-fixture polarity derivation, in that order.
- **Blockers:** **`INV-06`'s scope is a founder ruling and `RE-P-01` cannot be written without it** (first landmine). Groups A, F and H still wait on the calendar transcription, which is a founder item (P2 section 6).
- **Landmines:**
  - **THE FLOOR LOCK LOWERED THE FLOOR ON THE ACCOUNT'S BEST DAY, ON EVERY V1 EVAL PASS, AND THE SUITE WAS GREEN.** R-15 assigned `floor_lock_floor_at_cents` where [M01 section 3.4](../plans/M01-rules-engine.md)'s binding expression takes a `max`. A day that **jumps past** the trigger has already had its floor trailed above the locked value: closing 5,300,000c trails to 5,050,000c and the lock was pinning it back to 5,010,000c, **40,000c down, which is `INV-06`**. It is not exotic: the lock triggers at 260,000c of profit and the Core EOD eval target is 300,000c, so **every account that passes crosses it**. M01 calls the `max` "redundant given the update order" on the reasoning that CV-12 puts the trailing floor exactly at the lock value, and **that holds only when the balance lands on the trigger rather than jumping past it**, which R-15's own `>=` allows.
  - **`RE-U-015` PASSED THROUGHOUT, AND WHY IT DID IS THE TRANSFERABLE PART.** It folded a day landing **on** the trigger and a day one cent below it. Those are the two points where CV-12 makes the assignment and the `max` agree, so the test was pinned to the only pair of inputs that cannot distinguish the correct rule from the wrong one. **A boundary pair is not coverage of an operator when both sides of the boundary sit inside the same degenerate case.** The overshoot case is now folded and `CI-02/engine-R-15` seeds the old assignment back.
  - **R-14's TRIPWIRE DID NOT FIRE, AND IT WAS THE CONTROL THAT SHOULD HAVE.** It compared the final floor against the floor at the **start of the day**, and 5,010,000 is still above the 4,750,000 the day opened at. **A monotonicity check that only samples the endpoints cannot see a retreat inside the interval.** It now runs after each sub-step (`R-13` then `R-15`), which is what makes the R-15 mutant fail twice over: the expectation goes red and the tripwire throws before it gets there. This is the constitution's own "prefer a new CI gate over a bigger model whenever the error is checkable", arriving as a strengthened assertion rather than a note.
  - **`INV-06` VERSUS R-31: THE FUNDED RESET LOWERS THE FLOOR AND THAT IS THE CORPUS'S OWN ARITHMETIC.** On a Core EOD pass day the eval floor is 5,050,000c and the funded floor is 4,750,000c. **R-12 ("and again at the funded reset with the funded drawdown"), R-31 and GS-019 all state that number**, so the engine follows them; the balance drops to `size_cents` in the same step, so no loss room narrows and ADR-014's concern (a floor falling under a balance that did not) is not this case. **What is unsettled is `INV-06`'s scope**: "no exception, no phase qualifier" reads per account, `D-M2-1`'s "by resetting the account **or provisioning a new one**" reads per phase, and **`RE-P-01`'s generator cannot be written until one of them is ruled**. Asserted in `RE-U-031` rather than only commented, so the next session reads it instead of re-deriving it.
  - **R-31 CLEARS `floorLocked` AND R-31's SENTENCE DOES NOT SAY SO.** It lists balance, hwb, floor and the counters and stops. The floor machine answers the rest: [section 3.4](../plans/M01-rules-engine.md) starts the machine at `[*] --> trailing`, and R-12 names the funded reset as one of its two entry points. **A funded account arriving locked would have `hwb` frozen at `size_cents` and R-13 guarded off for the rest of its life**, so its floor could never trail and the lock could never re-engage at the funded trigger. That is not a reading of R-31, it is the funded floor deleted. And it is live rather than hypothetical, because every v1 eval pass locks at DO-7 before DO-8 resets.
  - **R-32 REFUSES AND IS NOT DECLARED, WHICH IS THE COUNT BEING HONEST.** "Elapsed trading days `>` the limit expires the account", and **elapsed trading days is not derivable from `RuleState`**: section 2.2's record carries no account-open day, and `tradedDaysCount` counts days with fills, which R-08 makes a different quantity. **The field is a column on `rule_states`, so it is a schema delta and an ADR**, and `SD-01` to `SD-10` do not contain it. `max_days` is null on all three plans; a plan that set it now refuses the day rather than trading an account past its own expiry with a green state row. `implemented-rules.test.ts` asserts the **absence**, so a later session cannot declare it without implementing it.
  - **`rule.floor_locked` FIRES ON A DAY THE ACCOUNT THEN LEAVES.** DO-7 locks and DO-8 resets, in that order, because "no step may be reordered". The event is true of the eval account and is noise on a timeline a trader reads as funded. **The engine is faithful to the ordering law and does not suppress it**; whether M10 does is not the engine's call, and inventing a suppression here would be an engine rule nothing in the corpus states.
  - **DIVISION EXISTS IN THE ENGINE NOW, AND WHERE IT SITS IS THE WHOLE ARGUMENT.** M01 section 3.5 writes "no division exists anywhere in the engine" and section 3.6's own reference algorithm calls `ceilDiv` four lines later. The split implemented is: **the verdict is cross multiplication and never division** (R-29, `INV-03`), and `profitNeededToDiluteCents` and `bestDayShareBp` are **reported, never compared**, both computed only after R-30 established a positive denominator. `FM-15` is "structurally impossible" rather than handled, and `RE-U-030` asserts that.
  - **`MERIT_RAPID_50K` IS IN THE FIXTURES FOR ONE REASON.** Core EOD and Direct both carry `Eval consistency: disabled`, so a suite holding only CORE-50K could exercise R-28, R-29 and R-30 nowhere except through a config it invented, and an invented config is a number with no source. Every value is transcribed from [Appendix A.2](../plans/M01-rules-engine.md)'s 50K column with the citation beside it.
  - **`eval_progression_unimplemented` IS DELETED FROM `AssertionKind`, NOT LEFT IN PLACE.** Nothing can emit it, and a refusal kind nothing can emit is a lie about what the engine refuses. Two tests that asserted it now assert the fold instead, and **`RE-U-025` got stronger for it**: the surviving half of the breach pair now genuinely passes the eval, so R-25's claim ("no `phase.passed`, no eligibility, no graduation") is proved by a discarded pass rather than by a refusal standing in for one.
- **Files touched:** `packages/rules-engine/src/day/{progression,consistency}.ts` (new), `src/day/{advance,floor}.ts`, `src/{types,index,calendar,rules}.ts`, `packages/rules-engine/test/rules-e-progression.test.ts` (new), `test/{fixtures-in-code,rule-coverage,implemented-rules,advance-day,rules-c-floor,rules-d-breach,evaluate.golden}.ts`, `scripts/ci/falsify-ci.mjs` (seven engine mutants), `docs/STATE.md`, `docs/sessions/`.

### The count, reported rather than implied

**Twenty-two of fifty.** The twenty-eight that are not implemented are not silent: `src/rules.ts` names the group each belongs to and why it is absent, `advance.ts`'s header maps every `DO-n` to implemented or refusing, and `implemented-rules.test.ts` asserts the number **22** so a session that adds a rule without noticing cannot land.

Group E is **six of seven**. R-32 is the seventh and it refuses, for the reason above. Everything from R-33 is groups F, G and H; R-02 still needs the real calendar; R-10, R-11, R-17, R-19 and R-20 are discharged outside the day fold.

### What was NOT done, named rather than implied

**CI-03's polarity is untouched.** `engineIsIdentityStub()` still governs and every golden fixture is still asserted to fail. ADR-048's derivation lands when the first fixture genuinely passes, and its stated prerequisite, the `L-nn` resolvable-citation rule, is still unwritten.

**No ADR was written for the R-15 disagreement, or for `INV-06`'s scope.** Both are defects or gaps in a frozen document, the founder rules those, and a session that writes an ADR to resolve a contradiction it found in its own path is a session grading its own work. Session 44 handled M01's R-22 disagreement the same way and this session follows it.

**No golden fixture was written.** P2 section 2 alternates fixture sessions and engine sessions and says why: "a session doing both has derived its expectations from its own output." Group E's `GS-017` to `GS-023` belong to a fixture session.

**No `EC-nnn` was claimed** for the floor defect. An edge case arrives with a number from the allocation table and a regenerated count, and both belong to the session that rules it rather than to the one that found it.
