---
status: draft
depends_on: [README.md]
last_updated: 2026-08-26
---

## 2026-08-26 - Session 227: the keyed accessor
<!--index: **IN FLIGHT. The plan gate is committed and the slice is being built.** [ADR-112] is reserved and unwritten. The objective is a keyed read and a keyed write in `packages/db`, with the six unaddressed write methods removed -->

- **Objective:** `packages/db` gains a read and a write that can NAME ONE ROW, and the methods that silently cannot are removed. [ADR-109](../decisions/ADR-109.md) clause 2 states the need and says in its own words that it does not design it; the design authority is DELEGATED to this session and the `E2` line-by-line read on what lands is still owed. **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **One ADR: [ADR-112], reserved before dispatch.**

---

### Plan

**This section is the plan gate.** [ADR-003](../decisions/ADR-003.md) asks for plan mode on a money path; this is a remote autonomous session and the harness's plan mode blocks on an approval prompt nobody is watching, so the discipline is discharged in writing instead. **It is committed and pushed before any source file is touched.** If the plan moves while the slice is built, this section is amended in place and the move is named.

#### 1. What was measured before anything was planned, at `6a5ac5b`

Every claim the dispatch made was re-derived rather than trusted, which the dispatch asked for by name.

| # | Claim | Verified |
|---|---|---|
| 1 | **`scopedTx.update` and `.delete` narrow by tenancy and by nothing else** | [`scoped-db.ts:671,679`](../../packages/db/src/scoped-db.ts). Both pass `scopePredicate(key, identityId)` and take no second argument. An `UPDATE` writes every row one identity owns |
| 2 | **`systemTx.rows` / `.update` / `.delete` pass `undefined` into a REQUIRED `where` position** | [`scoped-db.ts:695,701,704`](../../packages/db/src/scoped-db.ts), against [`:453,463,473`](../../packages/db/src/scoped-db.ts) where `where: SQL \| undefined` is declared. An `UPDATE` writes **EVERY ROW IN THE TABLE** |
| 3 | **`firmTx` is identical** | [`scoped-db.ts:714,720,723`](../../packages/db/src/scoped-db.ts) |
| 4 | **`sqlExecutor`'s vocabulary is one member and it THROWS on anything else** | [`scoped-db.ts:567`](../../packages/db/src/scoped-db.ts) declares `SqlExecutorReason = 'job-enqueue'`; [`:741`](../../packages/db/src/scoped-db.ts) checks it at run time |
| 5 | **THE DEFECTIVE METHODS HAVE NO REAL CALLERS.** The dispatch's central measurement, re-taken | `grep -rn "\.update(\|\.delete("` over `apps/` and `packages/` returns, outside `packages/db` itself, **only `createHash(...).update(...)` and four comments**. `packages/db/test/write-accessor.test.ts` is the only file in the tree that invokes any of the six |
| 6 | **The only real `.rows(...)` invocations on a transaction handle are two ledger reads** | [`chart.ts:96`](../../packages/ledger/src/chart.ts) and [`halts.ts:93`](../../packages/ledger/src/halts.ts), both through `LedgerTx`, which is `packages/ledger`'s own restatement and declares `rows` and `insert` only |
| 7 | **`packages/ledger/test/accessor-bind.test.ts` READS `scoped-db.ts` and is OUTSIDE this fence** | It asserts `SystemTx` still declares `rows<K extends TableKey>` and `insert<K extends TableKey>` verbatim, that `ScopedDb` / `SystemDb` / `FirmDb` carry no write verb, that `SystemReason` is exactly two members, and that `FirmTx.insert` takes `FirmTableKey`. **Every one of those survives this plan**, which is what keeps the fence at the size the dispatch drew it |
| 8 | **The registry is 104 of 111**, where the dispatch's predecessors said 100 | Read from `TABLE_KEYS` and `SCOPE_RULES` at `6a5ac5b`: 1 `root`, 40 `owned`, 19 `derived`, 3 `pair`, 41 `firm`. A count is a fact about a tree at a commit |

**Baselines, measured on this branch's base rather than inherited:** full suite **128 files / 2,170 passed / 1 skipped**; `packages/db` **9 files / 407 passed**; `write-accessor.test.ts` **33 passed**; `scoped-db.test.ts` **246 passed**; invariants **9 of 9**; gates **30 of 30**; `node scripts/ci/falsify-ci.mjs CI-01` **12 of 12**.

**So the dispatch's measurement holds and a broken method can be REMOVED rather than deprecated.** The one correction is arithmetic rather than substance: the dispatch's defect list names eight methods and calls them six. The six are the WRITES. `systemTx.rows` and `firmTx.rows` render a bare table scan, and that is not a defect: it is exactly what `systemDb(reason).rows` and `firmDb().rows` already grant at the same authority, and it is the pair with the two real callers.

#### 2. The evidence that decides rulings 1 and 2, gathered before either was written

Two folds were run over the tree. Both are reproduced as assertions in the suite this session lands, so neither is a number in a document nobody can re-take.

**Fold A, over the Drizzle schema.** `getTableConfig` reports, per table, the inline `.primaryKey()` and `.unique()` columns and the table-level `primaryKey(...)` and unique constraints. **103 of the 104 registered tables carry at least one derivable unique key. 13 carry a composite one. `treasuryBalances` carries none.**

**Fold B, over all 47 migrations, in both directions.**

| Direction | Count | What it means |
|---|---|---|
| **A key the SCHEMA declares that the MIGRATIONS do not** | **0** | The dangerous direction is EMPTY. Nothing the accessor would accept as an address is unique only in `schema.ts`'s imagination |
| **A key the MIGRATIONS declare that the SCHEMA does not** | **34**, over 34 tables | The fail-closed direction. The accessor refuses an address the database would in fact have made unique, which is a REFUSED write and not a wrong one |

**`treasury_balances` is the extreme case of the second row and it is named rather than repaired.** [`0009_ledger.sql:150`](../../packages/db/migrations/0009_ledger.sql) declares `PRIMARY KEY (account_code, as_of)` and [`schema.ts:581`](../../packages/db/src/schema.ts) declares no key at all, so it is the one registered table with no addressable key. **`schema.ts` is outside this fence** and it is `firm` with no caller, so the repair is reported with its diff rather than taken.

#### 3. The four rulings, each decided on the evidence above

**RULING 1. A PREDICATE IS A TYPED EQUALITY CONJUNCTION OVER DECLARED COLUMNS, NEVER A `SQL` FRAGMENT.**

`RowFilter<K>` is derived from `(typeof TABLES)[K]['_']['columns']`, so the caller may name only the columns that table actually has and a typo is `TS2353` rather than a run-time surprise. **Prototyped and type-checked before this plan was written.**

- **The `SQL` shape is REFUSED and what it would have cost is stated.** It hands every call site the ability to write any predicate, which is the surface [ADR-008](../decisions/ADR-008.md) scoped this wrapper to bound and [ADR-102](../decisions/ADR-102.md) section 5 named again about `executeSql`. A caller holding a legitimate handle could then write a predicate that reaches another identity's row, and the diff a reviewer reads would look like ordinary accessor use rather than like raw SQL.
- **The `byId(id)` shape is REFUSED on evidence rather than on taste.** `idempotency_keys`'s primary key is `key` ([`0017:103`](../../packages/db/migrations/0017_events_and_audit.sql)), `treasury_balances`'s is `(account_code, as_of)`, and **13 registered tables carry a composite primary key**. A `byId` accessor does not serve the table [ADR-109](../decisions/ADR-109.md) exists for, which is the first one it meets.

**RULING 2. AN ADDRESS MUST CONTAIN A DECLARED UNIQUE KEY. A FILTER NEED NOT, AND A FILTER MAY ONLY READ.**

- **A WRITE takes a `RowAddress<K>`:** a `RowFilter` whose named column set CONTAINS at least one primary key or unique constraint the schema declares. Checked at run time against Drizzle's own table config, so it is derived from the schema rather than restated. **An addressed write therefore names at most one row by construction rather than by the caller's care.**
- **A READ may take a plain `RowFilter<K>`**, and the argument is checkable rather than aesthetic: **a filtered read is a NARROWING of a read that already exists at the same authority**, so it grants no reach. `scopedTx.rows` already returns every row this identity owns and `systemTx.rows` already returns every row in the table; a conjunct can only remove rows from either. A filtered WRITE at non-unique columns is different in kind, because the unaddressed write is being removed and there is no wider method for it to narrow.
- **A `null` or `undefined` value in an address is REFUSED.** A unique key over a nullable column does not bound the row count when the predicate is `IS NULL`, and `col = NULL` matches nothing, so neither reading of a null is a row this accessor should write.
- **The address may not name the tenancy column on the scoped path.** [ADR-102](../decisions/ADR-102.md) clause 4 already rules the tenancy column is never the caller's to name in an insert's values or an update's `SET`; an address is the third place a caller could name it, and the handle supplies it there too. On `systemTx` and `firmTx` there is no handle-supplied scope to contradict, so an address there may name any column.

**RULING 3. THE TENANCY PREDICATE AND THE ADDRESS COMPOSE, AND THE WRONG SHAPE DOES NOT COMPILE.**

- `updateStatement` and `deleteStatement` stop accepting `SQL | undefined`. Their `where` becomes a BRANDED intersection with two disjoint members, `ScopedWritePredicate` and `UnscopedWritePredicate`, each with exactly one producer. **So `updateStatement(source, key, values, undefined)` does not compile, and neither does handing the scoped builder an unscoped predicate.** The brand is a type-only intersection and not a `unique symbol`, because [session 222](2026-08-26-session-222.md) measured that `declare const X: unique symbol` type-checks, satisfies every use and **erases** under `node --experimental-strip-types`.
- `scopedWritePredicate(key, identityId, address)` is `and(scopePredicate(key, identityId), addressPredicate(key, address))` and it is the only thing `scopedTx`'s writes call.
- **Asserted in both directions**: every scoped keyed write renders the tenancy predicate the matching read renders AND the address conjunct, over every `ScopedTableKey`; and a scoped keyed update naming a row belonging to another identity is watched returning zero rows rather than one.

**RULING 4. THE SIX UNADDRESSED WRITE METHODS ARE REMOVED.**

`ScopedTx.update`, `ScopedTx.delete`, `SystemTx.update`, `SystemTx.delete`, `FirmTx.update` and `FirmTx.delete` cease to exist. Nothing in the tree calls them (item 5 above), so this breaks no caller, and [ADR-042](../decisions/ADR-042.md)'s finding that a comment is not a control is why they are not left behind one. `rows` and `insert` stay on all three handles unchanged.

**A whole-table write at any authority is FORECLOSED**, named rather than left to be discovered, on [`job-queue.ts`](../../packages/queue/src/job-queue.ts)'s own rule that [ADR-102](../decisions/ADR-102.md) borrows twice: a primitive admitted before a caller exists is a primitive nobody can remove.

**RULING 5. `sqlExecutor`'s VOCABULARY DOES NOT MOVE.** The three needs [ADR-109](../decisions/ADR-109.md) clause 2 names -- a keyed read, a keyed status advance and a keyed stamp -- are served by this construction without raw SQL, so widening the list buys nothing and would spend the admission [ADR-102](../decisions/ADR-102.md) section 5 kept narrow. **What is still not expressible is named rather than implied**: `ON CONFLICT`, `ORDER BY`, `LIMIT` and `FOR UPDATE` have no shape here and each is a later diff with its own argument.

#### 4. Files, in the order they land, one commit and one push each

1. **This log's Plan section.** The gate.
2. **[`packages/db/src/scoped-db.ts`](../../packages/db/src/scoped-db.ts).** The address types, the two predicate producers, the branded write predicates, four new methods per handle (`rowsWhere`, `rowAt`, `updateAt`, `deleteAt`) and the six removals.
3. **`packages/db/test/keyed-accessor.test.ts`** (new). Both folds re-taken as assertions, the composition asserted in both directions, the refusals watched.
4. **[`packages/db/test/write-accessor.test.ts`](../../packages/db/test/write-accessor.test.ts).** Its four assertions that invoke a removed method, moved onto the addressed form.
5. **[ADR-112]**, and then its rows in [ALLOCATION](../decisions/ALLOCATION.md), [decisions/README](../decisions/README.md), [INDEX](../INDEX.md), [STATE](../STATE.md) and [sessions/README](README.md), plus this log's body.

**NOT touched, and each for a stated reason.** No migration and `0048` stays free. [`schema.ts`](../../packages/db/src/schema.ts), [`scope.ts`](../../packages/db/src/scope.ts) and [`client.ts`](../../packages/db/src/client.ts): outside the fence, and section 2's 34-key gap is reported rather than repaired. [`packages/ledger/src/chart.ts`](../../packages/ledger/src/chart.ts) and [`halts.ts`](../../packages/ledger/src/halts.ts): `rows` does not move, so the fence's conditional never opens. `AuthBackend` and `IdempotencyStore`: the next session's, deliberately.

**[`packages/db/src/index.ts`](../../packages/db/src/index.ts) is outside the fence and is NOT edited, which has a consequence worth stating rather than discovering.** The construction is usable from another package without it, because a caller writes `tx.updateAt('idempotencyKeys', { key }, values)` and never names the type; a caller that wants to DECLARE a variable of the address type needs one line added to that export list, and the first such caller adds it.
