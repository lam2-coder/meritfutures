## 2026-08-15 - Session 23: S-C, CI-01, CI-02 and CI-05 with VG-4 and VG-12

- **Done.** `.github/workflows/ci.yml` exists and all three stages are **green in Actions on the pull request**, not only on a laptop. Three jobs, none of them `needs:` another, each ending with its own seeded-violation harness. **VG-1, VG-4 and VG-12 are wired and each has been watched failing.** S-D and S-E were not begun, per the instruction.
  - **VG-4 was written rather than deferred, and the blocker it was deferred behind was the softer half of the rule.** The scaffold recorded that it "needs the `scopedDb(identity)` accessor to exist before it can name what it permits", in four files. What the rule **bans** is fixed today; what it **points at** is a package that exists. `merit/no-raw-db-client` reports the Drizzle family, seven drivers and six second ORMs, plus the four routes around a plain import (re-export, star re-export, dynamic `import()`, `require()`), plus deep imports into `@merit/db` including the relative form no `exports` map can stop.
  - **It is attached to `apps/**` AND `packages/**`, which is wider than [STRATEGY section 4.2](../testing/STRATEGY.md)'s wording.** STRATEGY phrases VG-4 over "app paths"; [ADR-008](../decisions/ADR-008.md) and `packages/db`'s own header phrase the invariant over the complement, and **the wider set is the correct one**: a raw connection opened in `packages/rithmic` is exactly as unscoped as one in `apps/portal`. The one exception is an `ignores` line in `eslint.config.js` rather than an allowlist inside the rule, because a list inside the rule is a list a rule change can widen without a reviewer reading the word "ignores".
  - **`scripts/ci/falsify-ci.mjs`, thirteen seeded violations, run as the LAST STEP OF EACH JOB rather than as a fourth job.** A stage cannot be green while the proof that it can fail lives somewhere a person deleted. It reports `FAILED OFF-TARGET` rather than a pass when a gate exits non-zero without producing the planted finding, which is [STATE](../STATE.md)'s two-checks-not-one applied to the VG gates.
  - **VG-12's fourth part is recorded as a policy rather than performed by a job.** `--frozen-lockfile`, `pnpm audit`, a syft SBOM kept as an artifact and a grype scan are mechanical. **Human approval is not**: a job can see the dependency surface changed and cannot see that a human agreed, and a green check that reads as an approval nobody gave is worse than no check. `.github/CODEOWNERS` plus the branch-protection setting is where it lives, and CI-05 prints the changed files as a `::notice::` without blocking.
  - **The scanners are pinned by version AND by digest**, which is VG-12 applied to the tools that enforce VG-12. Each sha256 was read from the run that downloaded it rather than from a release page.
- **Next.** **S-D** (the golden fixture loader and CI-03) and **S-E** (TradingCalendar as data: money path, fresh session, plan mode) are the remaining P1 sessions and are independent of each other. **CI-04, CI-07, CI-08 and CI-09 are not P1's scope** and the nine unwired VG gates sit inside them.
- **Blockers.** **One founder action: turn on "Require review from Code Owners" for `main`.** Without it `.github/CODEOWNERS` requests a reviewer and nothing enforces one, and VG-12's approval half is documentation. The E2 read on the 18 money-path migration files is unchanged; **no `.sql` file was touched in this session**.
- **Landmines.**
  - **THE FIRST SEEDED VIOLATION THAT FAILED WAS THE HARNESS BEING RIGHT ABOUT ITSELF.** `CI-05/gitleaks` reported `DID NOT FAIL` on a syntactically perfect AWS access key id, and **gitleaks was correct to ignore it**: the seed read `AKIA` + `QYLPT7EXAMPLE` + `000`, and gitleaks drops any candidate whose secret contains a stopword, `EXAMPLE` being the archetype. **A seed has to be a thing the gate is supposed to catch, not a thing that looks like one to the person writing it**, and this is the same shape as CI-06e's seed landing on the convention paragraph above `EC-001`. It cost one run and would have cost a false sense of VG-1 forever if the harness only checked exit codes.
  - **A BUG IN THE ERROR-HANDLING PATH OF THE SCRIPT WHOSE ENTIRE JOB IS ERROR HANDLING.** `install-pinned-tool.sh` opened with `${3:?asset name template, with {v} for the version}`; `${var:?word}` ends at the first unmatched `}`, so the brace **inside the message** terminated the expansion and the rest of the message became part of the asset name. CI requested `gitleaks_{v}_linux_x64.tar.gz for the version}`. **It works on every path that never fails**, which is why it survived being written and read.
  - **TWO SCANNER PINS WERE 22 MINORS STALE AND NOTHING 404d.** `syft` 1.29.0 against 1.51.0 and `grype` 0.95.0 against 0.117.0, both downloading and installing cleanly. The installer only speaks up on a 404, so the stale pins were invisible until **grype could not migrate the schema its own database is published in**. A scanner behind its own database does not report a smaller set of findings, **it reports none**, and `grype` exited 1 with `database does not exist` rather than passing, which is the only reason this was a red build instead of a silent hole. **The drift step exists because of this and is deliberately non-blocking**: a stale pin is a decision to make, not a build to break.
  - **`Keep the SBOM` under `always()` went red for a file that could not exist**, turning one failed install into two red steps and pointing the reader at the wrong one. The guard is `hashFiles('sbom.cdx.json') != ''`, which still keeps the artifact when grype goes red, since that is the run whose SBOM is most worth reading.
  - **CI-02 IS GREEN ABOUT LESS THAN ITS ROW MEANS, and the pull request says so rather than letting the check mark imply otherwise.** [STRATEGY section 4.1](../testing/STRATEGY.md) gives CI-02 the `PT-nn` suites and every module's `Mxx-*-nn` suites. **Neither exists**; they arrive with P2. The stage runs 79 tests over 11 files, of which the meaningful ones are S-B's invariant suite and this session's two rule suites. It grows with no workflow change, and **Vitest exits non-zero when a selected project matches no test files**, so an emptied project fails rather than reporting green for having looked at nothing. `--passWithNoTests` must never be added to that line.
  - **The registry half of semgrep can go red on a day nobody touched this tree.** `p/typescript` and `p/secrets` are upstream and their rules change. Accepted rather than hidden, and it is **why the falsification seed targets an owned rule** in `.semgrep/merit.yml`, whose text this repository controls. A seed calibrated to somebody else's rule text reports "cannot be made to fail" the day they rewrite it.
  - **Every push runs the workflow twice**, once for `push` and once for `pull_request`, because the trigger block matches `corpus.yml`'s and the concurrency group keys on `github.ref`, which differs between the two. Inherited rather than introduced, and worth a ruling before the pipeline has ten stages instead of three.
  - **`typescript-eslint` was added to `packages/eslint-plugin-merit` as a test dependency**, at the existing `catalog:` version. Under espree, `import type { X } from 'y'` is a **parse error**, so two cases about a merge-blocking rule could not be written and its TypeScript behavior would have been a guess. **This is itself a VG-12 dependency-surface change**, and CI-05's notice named it on the pull request, which is the control working on its first opportunity.
  - **A type-only import of a client is a VG-4 finding, and that is a decision rather than an accident.** It opens no connection so the BOLA argument does not reach it; what it does is put the import line in a file outside `packages/db`, ready for the next commit that needs a value. Flagged for the founder to strike if the trade is wrong.
- **Files touched:** `.github/workflows/ci.yml`, `.github/CODEOWNERS`, `.semgrep/merit.yml`, `scripts/ci/{falsify-ci.mjs,install-pinned-tool.sh}`, `packages/eslint-plugin-merit/{rules/no-raw-db-client.js,test/no-raw-db-client.test.ts}` (all new); `eslint.config.js`, `package.json`, `pnpm-lock.yaml`, `packages/eslint-plugin-merit/{index.js,package.json,README.md}`, `packages/tooling/{eslint.base.js,README.md}`, `packages/db/{README.md,src/index.ts}`, `apps/portal/src/index.ts`, `docs/{STATE,SESSION_LOG}.md`. **`corpus.yml`, `scripts/corpus/*` and `packages/db/migrations/*` were not touched.**

---
