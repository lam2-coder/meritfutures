## 2026-08-15 - Session 27: FOLD-02 planned, the payout enforcement window and identity-level suspension

- **Done.** [FOLD-02](../plans/FOLD-02-enforcement-window-and-suspension.md) is written and approved. **Plan only, per the brief's stop condition.** No ADR was written, no migration was created, and no module document was folded. Two rulings, two ADRs, one superseded-never-edited schema change, planned across roughly twenty-six files and eight sessions.
  - **NUMBERS CLAIMED BEFORE ANYTHING IS WRITTEN, AND FOLD-01'S RESERVED IN THE SAME ROWS.** ADR-040 (Ruling A), ADR-041 (Ruling B), `0030` and `0031`. **ADR-039 and `0029` are reserved for [FOLD-01](../plans/FOLD-01-phone-identity.md)**, which is committed on this branch and had claimed neither yet: `CI-06f` and `CI-06h` assert gaplessness over allocated **plus reserved**, so claiming 040 over a hole at 039 is a finding the gate reports. The stale `036` row ("reserved, unmerged"; it merged in PR #11) is corrected in the same commit. **The plan first claimed 038 and 039 and both were already taken; see the landmine below.**
  - **AMENDMENT 4 ANSWERED: THE HOLD IS NOT THE FREEZE UNDER A SECOND NAME, AND THE DISCRIMINATOR IS WHETHER THE LEDGER HAS MOVED.** A held request has posted nothing (no LT-01, no wallet credit, nothing owed), so release means **approve and pay** and enforcement reverses nothing; a frozen request has posted LT-01 and the money is already the trader's. Two consequences ruled rather than inherited: the hold **stores its full evaluated decision** so release re-evaluates nothing and every existing `NOT NULL` and CHECK on `payout_requests` survives untouched, and **a hold reaching expiry pays even if the account breached during it**, because the alternative is that Merit's own hold cost the trader money.
  - **THE SAME QUESTION APPLIED TO RULING B AND THE ANSWER WENT THE OTHER WAY.** `identity_status` already carries a reversible `restricted` value, wired through `0001`'s enum, `0002`'s column and CHECK, [STATE_MACHINES section 9](../architecture/STATE_MACHINES.md)'s machine, `identity.restricted` and `enforcement.applied`'s `restrict` action, [M20](../plans/M20-wallet.md) INV-M20-06's spend and withdrawal gates, and the trader's own `GET /me`. **`suspended` is `restricted` under a second name**, so the plan enumerates and asserts its binding surface instead of adding a second value. **This is the plan's one departure from the ruling's literal wording and it is flagged rather than absorbed.**
- **Next.** **Session 2 of the plan's eight: ADR-040, ADR-041, all five allocation rows and the `036` correction.** Money path, fresh session. It lands alone so a sibling branch can read the claims before writing against them. Sessions 2, 3 and 4 are money path and take a fresh session each.
- **Blockers.** None for the fold. Four founder questions are open and none of them blocks session 2.
- **Landmines.**
  - **AN ADR-NUMBER COLLISION WITH AN OPEN SIBLING PULL REQUEST, AND NO GATE COULD SEE IT.** This plan first claimed **038 and 039**. `claude/builder-reviewer-loop-rykvhs`, open as **PR #15**, had already reserved **037 and 038** in its own copy of the allocation table. Nothing on this branch could see that: this branch is one commit ahead of a `main` whose table ends at 036, and **eleven of eleven gates passed against that table**. **This is `CI-06f`'s and `CI-06h`'s own declared coverage gap met in the wild within two days of being declared**, and [ADR-036](../decisions/ADR-036.md) names it exactly: the table "cannot stop a branch that never reads `main`", and the cross-branch assertion "needs a job that can see both refs". **The remedy that worked was reading the open pull request list before writing a number, which is a human step in a process whose entire argument is that human steps drift.** The numbers moved here rather than at merge, on [ADR-034](../decisions/ADR-034.md)'s own tiebreak: PR #15's are cited across a landed fold and this fold's existed only inside its own plan. **Every remote branch was then checked, not only the one with an open PR**: `claude/merit-futures-briefing-7auoor`, `claude/p1-scaffold-plan`, `p10` and `s-d` end at 035 or 036, `dev` and `premain` predate the table, and **`0029` is still the next free migration number everywhere**, so only the ADR half moved. **The cross-ref job is the standing recommendation and it is still not written.**
  - **THE ZERO-DENIAL SENTENCE IS NOT IN [CLAUDE.md](../../CLAUDE.md), AND THE CONSTRAINT IS RESTATED IN TEN PLACES.** The brief attributed the words "no `denied` status **and no review state**" to CLAUDE.md; CLAUDE.md carries no zero-denial paragraph at all. The words are [GUIDE_BRIEFING](../GUIDE_BRIEFING.md)'s, and the constraint also lives in `0001:73`, `0010:77`, `0010:225`, [STATE_MACHINES](../architecture/STATE_MACHINES.md) line 89, [M05](../plans/M05-payout-system.md) INV-M5-01, [M06](../plans/M06-admin-ops-console.md) section 1.2, [M07](../plans/M07-risk-abuse.md) line 13, [TOS_CLAUSES](../legal/TOS_CLAUSES.md) line 118 and this log at line 46. **Amending one leaves the corpus contradicting itself in nine places, and two of the ten are inside MERGED migrations that E2 forbids editing.** `0010:225` is a `COMMENT ON TABLE`, which is replaceable metadata, so `0031` re-states it; the two `--` comments stand forever and the ADR says so rather than implying the sweep was complete.
  - **THE EXTERNAL LEG'S HALT ALREADY EXISTS AS COLUMNS WITH NO STATE TO SIT IN, AND IS THEREFORE UNENFORCED.** `wallet_withdrawals` carries `frozen_at`, `freeze_flag_id`, `freeze_expires_at` and a freeze-expiry index (`0011`), and `wallet_withdrawal_status` has **no** frozen or halted value (`0001`). A halted withdrawal still matches `wallet_withdrawals_open_idx` and nothing refuses settlement. **Folded as enforcement rather than as a state**, deliberately and asymmetrically with the payout hold: on `payout_requests` the hold **replaces** approval and is mutually exclusive with every other status, while on `wallet_withdrawals` the halt is **orthogonal** to the rail state, and collapsing an orthogonal hold into the rail's status column is SD-M5-06's named mistake. **The asymmetry is stated in the ADR so it does not read as an oversight.**
  - **`restricted` BLOCKS THE WALLET AND NOTHING ELSE RULING B NAMES.** `G-ELIGIBLE` names `payouts_frozen` on account and identity and does **not** name `identities.status`; [M03](../plans/M03-billing-checkout.md)'s checkout has no restriction check; nothing revokes platform trading on a restriction. **The state exists and its enforcement surface does not**, which is the actual content of Ruling B, and it is why the fold enumerates seven surfaces in one table rather than assuming the word covers them.
  - **`ALTER TYPE ... ADD VALUE` CANNOT BE USED IN THE TRANSACTION THAT ADDS IT, AND EVERY INDEX PREDICATE IN THE CHANGE IS SUCH A USE.** Every migration wraps itself in `BEGIN`/`COMMIT` and the install job runs each file through `psql -f` in autocommit, so the enum value and the widened predicates **cannot be one file**. Hence `0030` (the value alone, deliberately outside a transaction, with the reason in its header) and `0031`. **The definition of done requires executing the counterfactual**, that a combined file fails, rather than citing the manual: `0028`'s lesson is that a probe attempting only forbidden things passes against a guard that rejects everything.
  - **AMENDMENT 3 IS THE `C-02` DEFECT VERBATIM AND EVERY OTHER OBJECT ON THE TABLE WAS DISPOSITIONED RATHER THAN ASSUMED.** Both SD-09 predicates gain `held_pending_review`. The ordinal unique stays `WHERE status <> 'failed'` and **that is correct**: a held request holds its rung, enforcement sends it to `failed`, and the rung is released (EC-037). `payout_requests_freeze_is_complete` and `..._reflection_needs_settlement` were each read against the file and need no change, which is a claim checked rather than a hope.
  - **THE AUTO-RELEASE IS NOW THE LOAD-BEARING CONTROL, SO IT IS STRUCTURAL IN THREE PLACES AND MUTABLE IN NONE.** It joins the existing hourly freeze-expiry sweep and its **S1** dead-man switch ([CRON_INVENTORY](../ops/runbooks/CRON_INVENTORY.md)); its alarm fires on **the query, not the job** ([M02](../plans/M02-rithmic-bridge.md) FM-M2-11's idiom); and a hold or freeze past expiry becomes the **fourth unsuppressible alarm**, amending [M06](../plans/M06-admin-ops-console.md) OQ-M6-01. Plus `CI-06l`, one new gate asserting from the tree that every expiry column names a release job. **The letter is `l` because FOLD-01 section 7 claims `k`**, and gate identifiers have no allocation table, so it is claimed when its STRATEGY row is written.
  - **RESTORATION IS CONTINGENT ON `V-M2-15` AND SUSPENSION IS NOT, WHICH IS AN ASYMMETRY RATHER THAN A CAVEAT.** Re-enabling an entitlement against an unconfirmed `set_risk` is an unenforced funded account, which INV-M2-13 forbids and `0007`'s `provisioning_queue_set_risk_never_inferred` already makes unwritable. With neither an acknowledgement artifact nor a readable risk setting, **a suspended trader would be revocable and not restorable.** [M02](../plans/M02-rithmic-bridge.md) is still at `review` by [ADR-005](../decisions/ADR-005.md) and this fold does not move it; the leg is marked PROVISIONAL and a row joins the vendor agenda.
  - **NO DELTA, EDGE-CASE OR GOLDEN-SCENARIO IDENTIFIER IS INVENTED IN THIS PLAN**, per FOLD-01's own finding: only ADR and migration numbers have an allocation table, so a plan naming an `SD-nn` before its manifest row exists has pre-claimed in a registry with no claim mechanism. Each change names its owning module and table instead.
- **Verification run in this session:** eleven of eleven gates pass, `generate` reports every span already matching so nothing drifted, and `falsify.mjs` reports all eleven failing dirty with five scope cases holding.
- **Files touched:** `docs/plans/FOLD-02-enforcement-window-and-suspension.md` (new), `docs/INDEX.md` (one row), `docs/{STATE,SESSION_LOG}.md`. **No `.sql` file, no `packages/` file, no `scripts/` file and no workflow was touched, and no module document was folded.**
