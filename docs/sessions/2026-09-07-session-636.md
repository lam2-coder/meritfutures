---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 636: nine copies and not five, repaired under "no cited line moves" rather than under line-count neutrality

<!--index: **THE ROW IS [ADR-441](../decisions/ADR-441.md), `proposed` and UNSIGNED. COMMENT-ONLY DIFF IN A MONEY-ADJACENT PACKAGE, `E2` READ OWED.** **[ADR-440](../decisions/ADR-440.md) SECTION 12 ITEM 1 SAYS FIVE COPIES UNDER `apps/worker/**`. THERE ARE NINE**, and the undercount is mechanical: that entry's grep needle wraps across a line break at [`rows.ts:28`](../../apps/worker/src/digests/rows.ts) and [`breaker.test.ts:374`](../../apps/worker/test/breaker.test.ts), and a FIFTH WORDING it never enumerated stands at [`recon-sweep.test.ts:830`](../../apps/worker/test/recon-sweep.test.ts) and [`digests.test.ts:1458`](../../apps/worker/test/digests.test.ts). **ALL NINE REPAIRED on `RI-14`'s idiom. NOT ONE CITED LINE MOVED**: 101 citations reach the six files over 44 distinct lines, 39 of those 44 hold bytes identical before and after, the other 5 are the claim sites themselves and all 5 are still at their own line numbers. **`RI-14`, `RI-15` and `RI-16` PASS before and after**, and `RI-15` was watched RED on all ten citations this row added, in two passes, then restored byte-identically. **THE CONSTRAINT WAS "NO CITED LINE MOVES" AND NOT LINE-NEUTRALITY**, per [`repo-invariants.mjs:3008`](../../packages/tooling/checks/repo-invariants.mjs) read at source: five sites are held to their exact span, three grow because nothing is cited below them, and [`rows.ts`](../../apps/worker/src/digests/rows.ts) carries NO CITATION AT ALL. **NO CONTROL WAS BUILT; it is row `442`'s and its price is nine claim-site rows, not five.** **NO GATE WEAKENED, NO TEST SKIPPED OR DISABLED, NO CASE ADDED OR INVERTED, NO `RI-nn` MINTED, NO MIGRATION, NO WORKFLOW, NO CHECK FILE, NO `packages/` FILE.** Suite **base 8440, mine 8440, delta 0** over **342 files / 11 skipped / 0 failed**; typecheck, lint and `format:check` clean.-->

### 1. The objective

Repair the copies of *"no check in this tree compares a `schema.ts` column TYPE against the DDL"* that [ADR-440](../decisions/ADR-440.md) section 12 item 1 leaves live under `apps/worker/**`, keeping the superseded sentence beside its correction per `RI-14`. **Hold the repair to NO CITED LINE MOVES**, which [`repo-invariants.mjs:3008`](../../packages/tooling/checks/repo-invariants.mjs) rules is the property, rather than to the line-count neutrality two previous dispatches commissioned. Re-derive the population, the claim and the citation figures rather than trust the sibling entry. Do not build the control.

### 2. Done

- **The container measured and reported.** No repository on arrival, twenty-third consecutive row. `git push` refused `403` and succeeded after `add_repo` with `access: "push"`; the remote was **not** pre-checked.
- **The base reproduced at `9847bcd0`** on a clean tree before the first edit and **it is green**: gates **33 of 33**, invariants **35 of 35**, suite **342 files / 8,429 passed / 11 skipped / 0 failed**, typecheck, lint and `format:check` clean.
- **The ruling read at source before anything was edited.** [`repo-invariants.mjs:3008-3016`](../../packages/tooling/checks/repo-invariants.mjs): line-count neutrality is *"neither necessary nor sufficient"* and the property that is both is *"THAT NO CITED LINE MOVES"*. The dispatch's paraphrase is exact.
- **The population derived and it is NINE, not five.** Three greps over `apps/worker`, every hit opened. Two sites are the same sentence broken one word earlier than [ADR-440](../decisions/ADR-440.md)'s needle; two more are a fifth wording that entry never enumerated. [ADR-441](../decisions/ADR-441.md) section 3 is the table.
- **The claim falsified at primary source**, at [`scoped-db.test.ts:2728`](../../packages/db/test/scoped-db.test.ts), [`:2734`](../../packages/db/test/scoped-db.test.ts), [`:2739`](../../packages/db/test/scoped-db.test.ts) and [`:2754`](../../packages/db/test/scoped-db.test.ts); the misattribution confirmed at [`ADR-112.md:92`](../decisions/ADR-112.md); and the one leg that genuinely IS missing confirmed at [`scoped-db.test.ts:2520`](../../packages/db/test/scoped-db.test.ts), whose `TYPE_ENDS_AT` carries `DEFAULT` in its lookahead.
- **The dates derived per site.** `git log -S` plus `git show <commit>:packages/db/test/scoped-db.test.ts` for each of the four originating commits: **all four were 2026-09-07 and the comparison was already in the tree at every one of them**, thirteen days old. Nothing drifted.
- **The citation figures derived per file AND per site**, mirroring [`repo-invariants.mjs:3180`](../../packages/tooling/checks/repo-invariants.mjs)'s `citationTargets` so an `href` beats a bare basename. The spread of what a one-line insertion would move is **0 to 25**.
- **All nine repaired.** Five held to their exact block span; three grown because nothing is cited below them; and [`recon-sweep.test.ts:773`](../../apps/worker/test/recon-sweep.test.ts) keeps the cited sentence VERBATIM with its correction opening on the same line.
- **`RI-15` watched RED on every citation this row added and restored byte-identically**, in two passes, 4 findings then 6. [ADR-441](../decisions/ADR-441.md) section 6.

### 3. Not done, and why

- **NO CONTROL WAS BUILT.** [ADR-440](../decisions/ADR-440.md) section 9.2 rules it must not be bolted onto a repair, and the dispatch names row `442` as its owner. This row's only contribution is the corrected price: **nine claim-site rows and nine seeds, against a claim with FIVE spellings**.
- **The four ADR copies and [`STATE.md:11282`](../STATE.md) were NOT repaired.** Dated records, which [`repo-invariants.mjs:3013`](../../packages/tooling/checks/repo-invariants.mjs) rules are NAMED rather than repaired; and `STATE.md` is granted for generator output only.
- **The two [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) copies were NOT touched.** Already repaired by row `440`, and `packages/db/**` is outside this fence.
- **Three TRUE claims in this fence were left standing**, at [`breaker.test.ts:1358`](../../apps/worker/test/breaker.test.ts), [`:1415`](../../apps/worker/test/breaker.test.ts) and [`:1422`](../../apps/worker/test/breaker.test.ts). Two are about DEFAULT and one quotes foreclosure 4 correctly. A row that repaired every grep hit would have falsified all three.
- **No reservation was written and no number invented.** `440` is the highest claimed row on this base, this row claims `441`, and there is no hole.
- **One file outside the fence's prose WAS changed, by the generator the dispatch requires.** `gates.mjs generate` rewrote the span named `adr_count` in [`docs/STATE.md`](../STATE.md). No prose in that file was touched.

### 4. Blockers

- **None for this row.** The `E2` read is OWED and is not a blocker on the work; the entry ships `proposed` and UNSIGNED and nothing here was signed.
- **The control is blocked on a fence and on a survey**, not on a design. It needs `packages/tooling/checks/**`, and the survey [ADR-440](../decisions/ADR-440.md) section 12 item 3 asks for decides whether it is one artifact or a family.

### 5. Landmines

- **A SUBSTRING GREP OVER SINGLE LINES CANNOT SEE A SENTENCE THAT WRAPS.** Two of the nine were missed exactly that way, in the entry that refused a grep-based control for being too weak. **Any survey commissioned here must flatten line breaks before it counts.**
- **THE CLAIM HAS FIVE SPELLINGS AND NOT FOUR.** [ADR-440](../decisions/ADR-440.md) section 4 lists four. The fifth, *"`schema.ts` is a transcription and nothing compares it to the DDL"*, is BROADER than the others and was live in two files.
- **THE CONTROL'S PRICE IS NINE ROWS AND NINE SEEDS, NOT FIVE.** Row `442` should not spend the number in [ADR-440](../decisions/ADR-440.md) section 9.1.
- **`rows.ts` HAS NO CITATION IN THIS TREE AT ALL** and [`evaluate.ts:139`](../../apps/worker/src/breaker/evaluate.ts) has 25 below it. **The spread is why "no cited line moves" is derived per site and never assumed.**
- **TEN NEW CITATIONS NOW POINT INTO [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) FROM THIS FENCE.** A row that reflows that file above line 2520 moves all ten.
- **A GREP FOR THE SENTENCE STILL RETURNS ITS HITS AFTER THIS ROW**, now as quotations inside corrections. That is `RI-14`'s idiom working, not a failed repair.
- **`apps/worker` IS MONEY-ADJACENT AND THREE OF THE NINE SITES GUARD A MONEY READ.** Comment-only, not one `throw` retired, not one cast removed, typecheck exit 0.

### 6. Files touched

[`apps/worker/src/breaker/evaluate.ts`](../../apps/worker/src/breaker/evaluate.ts), [`apps/worker/src/recon/sweep.ts`](../../apps/worker/src/recon/sweep.ts), [`apps/worker/src/digests/rows.ts`](../../apps/worker/src/digests/rows.ts), [`apps/worker/test/breaker.test.ts`](../../apps/worker/test/breaker.test.ts), [`apps/worker/test/recon-sweep.test.ts`](../../apps/worker/test/recon-sweep.test.ts), [`apps/worker/test/digests.test.ts`](../../apps/worker/test/digests.test.ts), [`docs/decisions/ADR-441.md`](../decisions/ADR-441.md) (new), [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) (row `441` only), [`docs/decisions/README.md`](../decisions/README.md), [`docs/sessions/README.md`](README.md), this file, and one generated span in [`docs/STATE.md`](../STATE.md). **No migration, no workflow, no check file, no `packages/` file, no `apps/api` file.**
