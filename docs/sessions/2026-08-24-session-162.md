---
status: approved
depends_on: [README.md]
last_updated: 2026-08-24
---

## 2026-08-24 - Session 162: M08 measured, and "one rail" is not one rail in the schema either
<!--index: **[M08](../plans/M08-affiliate-system.md) measured against the tree. All five `SD-M8-nn` deltas LANDED and no superseding migration touches any M8 table; application code is ZERO, and the one file in the tree carrying the word is [`surface.test.ts:69`](../../apps/api/test/surface.test.ts), where `/affiliate/stats` is a string in a path-classification list. **THE HEADLINE IS ONE LEVEL BELOW THE ONE M08 SECTION 3.4 ALREADY FOUND.** That section ruled that "one rail" was doing the work of a shared GATE and none of it in the mechanism. Measured here: it is not doing the work of a shared RAIL either. [`payout_transfers.payout_request_id`](../../packages/db/migrations/0010_payouts.sql) is `NOT NULL REFERENCES payout_requests(id)`, and `payout_requests` is `NOT NULL` to both `accounts` and `plan_versions`, so an affiliate settlement cannot be a row in EITHER table, while `affiliate_statements.paid_transfer_ref` is bare `text` with no FK, which is the second spelling [ADR-017](../decisions/ADR-017.md) exists to refuse. It propagates: [M07:116](../plans/M07-risk-abuse.md) rows `D-09`'s source as `payout_transfers`, so `INV-M8-10` reads a table affiliate money cannot enter, and `DEP-M8-02` and `DEP-M8-05` are unmet in the SCHEMA and not merely in code. **`affiliate_statements.status` has no `held` value** while the trader side got exactly that shape in `0030` and `0031`, so `INV-M8-12` has no state to hold in. **No affiliate table is in `schema.ts`'s seven of 111**, so `SCOPE_RULES` totality makes every M8 read a COMPILE ERROR today, and `attributions` carries TWO identity columns against a four-class vocabulary whose `OwnedRule` names one. **`OQ-075-01` blocks NO slice**: `restricted` is ruled and buildable, `closed` is one predicate. Nine slices, no ADR number and no migration number claimed. **29 of 29 gates, 7 of 7 invariants, 104 files / 1,512 passed / 1 skipped, all unaffected by this session, which writes no code** -->

- **Objective:** measure [M08](../plans/M08-affiliate-system.md) against the tree the way [session 143](2026-08-23-session-143.md) measured P3. An INVENTORY. No code, no migration, no gate, no fixture, no ADR, no ruling, and no document but this one.

- **Done:** M08 read section by section and every claim in it checked against its primary source by running a command. **What exists:** all five schema deltas, both migrations, five data-model records, three of four API endpoints, six events, and nothing else. **What does not exist:** everything that is not those. The nine slices, their fences by file, their money-path flags and their collisions are below. **29 of 29 gates, 7 of 7 invariants, `pnpm run verify` clean at 104 files / 1,512 passed / 1 skipped, `falsify` 62 scope and 10 loader cases**, measured here rather than inherited and moved by nothing this session did.

- **Next:** three things go to the founder before any slice is dispatched, and the first two are rulings a build session must not take for itself. **(1) The rail.** `payout_transfers` widened, or a sibling table on the same provider machinery. Widening a merged money-path table's meaning is the restraint [`0032`](../../packages/db/migrations/0032_trading_calendar_holidays_coverage_revisions.sql) exercised and `OQ-M8-05` cites in its own words. **(2) The event names**, where the plan and the frozen catalogue disagree in both directions on five of nine. **(3) `OI-06`**, open since [FOLD-01](../plans/FOLD-01-phone-identity.md) and the only thing standing between `INV-M8-11` and a session. Then `M08-1` and `M08-3`, which nothing blocks.

- **Blockers:** none for this session. For M08 itself: `M08-9` is blocked on `OI-06`, which is **OPEN and deliberately undecided**; `M08-4` and `M08-8` are blocked on the rail ruling; every fixture M08 owns is `blocked / no-fixture-format`. **`OQ-075-01` blocks nothing**, and section 6 below says why rather than assuming it.

- **Landmines:**

  - **"ONE RAIL" IS NOT ONE RAIL IN THE SCHEMA, AND THIS IS M08 SECTION 3.4's OWN FINDING ONE LEVEL DOWN.** That section ruled that riding a shared rail *"was doing the work of a shared gate in the reading and none of it in the mechanism"*, and it stopped at the gate. Measured at the rail: [`0010:235`](../../packages/db/migrations/0010_payouts.sql) declares `payout_transfers.payout_request_id uuid NOT NULL REFERENCES payout_requests(id)`, and `0010:43` and `0010:68` declare `payout_requests.account_id` and `payout_requests.plan_version_id` both `NOT NULL`. **An affiliate has no account and no plan version**, so an affiliate settlement is not writable as a `payout_request` and therefore not writable as a `payout_transfer`. What the affiliate side has instead is `affiliate_statements.paid_transfer_ref text NULL` in [`0012`](../../packages/db/migrations/0012_disputes_and_affiliate_settlement.sql), a free-text reference pointing at nothing, **which is a second spelling of the destination and is precisely what [ADR-017](../decisions/ADR-017.md) was accepted to prevent.** Checked for a superseding migration first, as instructed: `grep -rn "payout_transfers" packages/db/migrations/` returns `0010` and one `REVOKE` line in [`0026:159`](../../packages/db/migrations/0026_roles_and_grants.sql), and nothing else. **The absence survives the wider grep.**

  - **AND IT PROPAGATES INTO THE DETECTOR, WHICH IS THE HALF `AS-M8-05` SAID WAS SAFE.** [M07:116](../plans/M07-risk-abuse.md) rows `D-09`'s source table as `payout_transfers` in the row's own Source column. So `INV-M8-10`, *"affiliate payouts and trader payouts share one destination-concentration check"*, reads a table affiliate money structurally cannot enter. **`DEP-M8-02` and `DEP-M8-05` are therefore unmet in the SCHEMA and not merely unbuilt in code**, which is a different and worse claim than either dependency row states. `AS-M8-05`'s own sentence, *"a second one is not an efficiency, it is a blind spot with a schedule"*, describes the tree as merged.

  - **`INV-M8-12` HAS NO STATE TO HOLD IN, AND THE TRADER SIDE GOT THE EXACT SHAPE IT NEEDS.** `affiliate_statements.status` is `CHECK (status IN ('draft','issued','paid','void'))` in `0012` and in [its data-model record](../architecture/data-model/affiliate_statements.md). **There is no `held`.** The invariant's load-bearing half is *"HELD rather than voided"*, and the only non-terminal value the table offers is `void`, which is the one outcome section 3.4 spends four paragraphs refusing. Meanwhile [`0030:57`](../../packages/db/migrations/0030_payout_hold_enum.sql) adds `held_pending_review` to `payout_status` and [`0031:47`](../../packages/db/migrations/0031_payout_hold_and_identity_restriction.sql) adds `held_at`, `hold_flag_id` and `hold_expires_at` to `payout_requests` with a constraint binding all three together. **The pattern exists, is merged, and was never applied to the door M08 owns.** No superseding migration touches it: `grep -rln "attributions\|payment_disputes" packages/db/migrations/` returns `0005` and `0012`, and no `ALTER TABLE affiliate*` exists anywhere.

  - **NO AFFILIATE TABLE IS REACHABLE FROM CODE, AND THE REASON IS A COMPILE ERROR RATHER THAN A GAP.** [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts) says in its own first line *"SEVEN TABLES OF 111, AND THAT IS REPORTED RATHER THAN ROUNDED UP"*, and none of the seven is an M8 table. [`scope.ts`](../../packages/db/src/scope.ts) declares `SCOPE_RULES` total over `keyof TABLES` by a `satisfies` clause, so **a table absent from `schema.ts` is unreachable through either accessor rather than reachable and unscoped.** Every slice that touches the database orders after `M08-1`. **The judgment inside that slice is real and is why it is money path**: `attributions` carries `buyer_identity_id` AND `affiliate_identity_id`, both `NOT NULL REFERENCES identities(id)` (`0012`, `SD-M8-05`), and the four-class vocabulary `'root' | 'owned' | 'derived' | 'firm'` offers `OwnedRule` with **one** `column`. A row owned by two identities has no shape in it, and picking one silently decides whose data an affiliate can read.

  - **THE PLAN AND THE FROZEN EVENT CATALOGUE DISAGREE ON FIVE OF NINE NAMES, IN BOTH DIRECTIONS.** M08 section 5 opens *"Per [EVENTS](../architecture/EVENTS.md) section 9, plus three NEW"*, and section 9 is [EVENTS:281](../architecture/EVENTS.md) to `:290`. **It carries `attribution.created`, not `attribution.recorded`. It carries `commission.accrued` and `commission.paid`, not `affiliate.commission_accrued` and `affiliate.commission_paid`. It carries NO payable event at all. It carries no click event at all** (`grep -n -i "click" docs/architecture/EVENTS.md` returns nothing, so `affiliate.click_recorded` is not a rename, it is an absence). **And it carries `affiliate.chargeback_rate_updated`, which M08 does not list.** The three M08 marks NEW are genuinely absent. **The split is not confined to M08**: [M07:254](../plans/M07-risk-abuse.md) consumes `attribution.recorded` for `D-10` and the catalogue produces `attribution.created`, so two plans and one catalogue hold three positions. Both documents are frozen, so this is an ADR and not a commit, and it must be taken by a session holding EVENTS.

  - **THE ONE THING M08 SAYS IS MISSING IS THE ONE THING THAT ARRIVED.** Section 5 states *"The restore has no event and this module is the second to say so"*, and `FM-M8-11` exists because of it. **[EVENTS:52](../architecture/EVENTS.md) now carries `identity.restriction_lifted`**, with `restriction_episode_id`, `restored_by`, `restore_evidence` and `restored_at`, and [EVENTS:68](../architecture/EVENTS.md) names M08 by name as one of the three consumers whose release half did not work. **`FM-M8-11`'s stated blocker is discharged and the failure mode is not**: the event exists, and nothing consumes it, and a held statement still raises nothing on its own. This is the row M08 wrote *"rather than being assumed"*, and the assumption it refused is the half that came true.

  - **`POST /affiliate/creatives` EXISTS IN M08 AND IN NO CONTRACT.** M08 section 4 marks it **NEW** and owns it. `grep -n "creatives" docs/architecture/API_CONTRACT.md` returns **nothing**; section 7 defines `GET /affiliate/stats`, `GET /affiliate/statements` and `POST /affiliate/links` and stops. **The approver route is worse and is already on the record**: [the admin parity audit:175](../reviews/2026-08-20-admin-parity-audit.md) rows it `GAP` with *"Missing: both halves"*, and [ADR-069:207](../decisions/ADR-069.md) says *"Row 21 closes the submit half only... Owed to M08"*. So `INV-M8-08`'s compliance control has **a table with a reviewer column and no route that can ever write it.**

  - **`INV-M8-11` HAS NO COLUMN TO COOL, WHICH IS A HARDER ABSENCE THAN `OI-06` STATES.** `OI-06` in [DELTA_MANIFEST:335](../../packages/db/DELTA_MANIFEST.md) is **OPEN, deliberately not decided**, and reports that `destination_ref` is the destination *of a transfer* and *"no table records that a destination changed or when"*. Measured on the affiliate side, it is one step worse: `grep -n -i "destination" 0005 0012` returns **zero lines**. `affiliates` carries no destination at all, so there is nothing whose change could be recorded. `OI-06`'s own recommendation already names *"the affiliate rail under C-24"* as a reader of the registry it declines to decide, so `M08-9` is not merely unbuilt, it is downstream of an open item and of the rail ruling both.

  - **NO `identities.status` GATE IS IMPLEMENTED ANYWHERE, AND [ADR-075](../decisions/ADR-075.md)'s CLAIM REPRODUCES WIDER THAN IT WAS MADE.** The ADR states *"`grep identities.status packages/rules-engine/src` returns zero hits"*. Run over all of `packages/` and `apps/` for `*.ts`, the hits are [`schema.ts:49`](../../packages/db/src/schema.ts) declaring the enum and [`account-adjustments.test.ts:260`](../../packages/db/test/account-adjustments.test.ts) asserting the string is **not** present. **No gate reads the column on any of the five doors**, so `INV-M3-15`, `INV-M8-12`, `G-ELIGIBLE`, `G-WITHDRAWAL-CLEARED` and `INV-M20-06` are five invariants over one unimplemented predicate. That is what still makes all of this cheap, and it is the same sentence ADR-075 wrote about its own moment.

  - **THE OBSERVABILITY SECTION HAS NO READ GRANT AND THE DEFAULT IS DELIBERATE.** All eight metrics in M08 section 9 aggregate `affiliate_clicks`, `attributions`, `affiliate_commissions` or `affiliates`. [`0026:127`](../../packages/db/migrations/0026_roles_and_grants.sql) grants `merit_analytics` SELECT on **nine enumerated tables** and none is an M8 table, and the file states the reason: *"the risk here is a table added later becoming readable by default, and the default should be that it is not."* So the grant is a deliberate migration line, not an oversight to be swept, and it belongs to whichever slice first needs a metric rather than to a tidy-up.

  - **EVERY FIXTURE M08 OWNS IS BLOCKED FOR ONE REASON, AND THE OWED ONE WAS NEVER MINTED.** [Section 39](../testing/golden-scenarios/39-fixture-status-and-blockers.md) rows `GS-045` at `:110`, `GS-123` to `GS-127` at `:188` to `:192` and `GS-140` at `:205`, **all seven `blocked / no-fixture-format`**, each citing that the only fixture format in the tree is M1's day stream folded through `advanceDay`. Separately, M08 section 8.1 owes one more scenario in words and claims no `GS-nnn` for it on `CI-06d`'s rule; **no number has been claimed since**, so the restriction scenario, which section 8.1 itself calls the half that matters because *"a gate asserted only on the refusal passes just as well when it refuses everybody"*, is still unnamed. The seven `M8-x-nn` suite prefixes in section 8 exist in M08 and in [session 36](2026-08-16-session-36.md) and nowhere else in the tree.

- **The slices, with fences by file.** No ADR number and no migration number is claimed by any of them: [ALLOCATION](../decisions/ALLOCATION.md) is not in this session's fence and thirteen siblings are measuring concurrently, which is the collision shape ALLOCATION already records three times.

  | ID | Objective | Fence, by file | Money path | Depends on |
  |---|---|---|---|---|
  | `M08-1` | The six M8 tables enter the Drizzle transcription and the scope registry, and `attributions`' two identity columns get a ruled `ScopeClass` rather than a picked one | [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts), [`packages/db/src/scope.ts`](../../packages/db/src/scope.ts), [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) | **MONEY PATH.** `balance_cents` and `amount_cents` are signed money, and a wrong scope rule on `attributions` reads another human's purchase | nothing. **First** |
  | `M08-2` | An ADR ruling the rail: `payout_transfers` widened, or a sibling table on the same provider machinery | `docs/decisions/ADR-0nn.md` (new), [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md), [`docs/decisions/README.md`](../decisions/README.md) generated span | **MONEY PATH.** It decides the shape of an outbound payment path | nothing. **First, in parallel with `M08-1`** |
  | `M08-3` | `affiliate_statements` gains `held`, `held_at` and its restriction-episode link, on `0030` and `0031`'s shape, with a probe | `packages/db/migrations/00nn_affiliate_statement_hold.sql` (new), [`packages/db/DELTA_MANIFEST.md`](../../packages/db/DELTA_MANIFEST.md), [`docs/architecture/data-model/affiliate_statements.md`](../architecture/data-model/affiliate_statements.md), `scripts/db/probe_affiliate_statement_hold.sql` (new), [`.github/workflows/corpus.yml`](../../.github/workflows/corpus.yml) | **MONEY PATH, `E2` READ.** It is the state in which earned money is withheld from a human | a migration number from ALLOCATION. Not `M08-1` |
  | `M08-4` | The settlement path itself, whatever `M08-2` ruled, plus the `INV-M8-12` gate at issue **and** at settlement | the files `M08-2` names, plus `apps/worker/src/batch/` | **MONEY PATH, `E2` READ** | `M08-1`, `M08-2`, `M08-3` |
  | `M08-5` | Attribution resolution as a pure fold: code override, then last touch within 30 days, then none, with the literal self-deal refused | `packages/affiliate/src/attribution.ts` and its test (new package) | **MONEY PATH.** It decides who is paid. `INV-M8-01`, `INV-M8-02`, `INV-M8-03` | `pnpm-workspace.yaml` admits the package. Not `M08-1`: it is pure |
  | `M08-6` | Commission arithmetic on `amount_paid_cents` in integer cents, and the payable predicate reading **both** clocks | `packages/affiliate/src/commission.ts` and its test | **MONEY PATH.** `INV-M8-04`, `INV-M8-05` | `M08-5`'s package |
  | `M08-7` | Clawback as a compensating negative row and the carried balance, asserted against `affiliates_negative_balance_has_clock` | `packages/affiliate/src/clawback.ts` and its test | **MONEY PATH.** `INV-M8-06`, `SD-M8-04` | `M08-6`, for the sign convention |
  | `M08-8` | An ADR reconciling M08 section 5's nine event names against EVENTS section 9's six, in both directions, and M07's third position | `docs/decisions/ADR-0nn.md` (new), [`docs/architecture/EVENTS.md`](../architecture/EVENTS.md), [`docs/plans/M08-affiliate-system.md`](../plans/M08-affiliate-system.md), [`docs/plans/M07-risk-abuse.md`](../plans/M07-risk-abuse.md), ALLOCATION, `decisions/README.md` | **Not money path.** It moves no rule and no money. It amends two frozen documents, so ADR and not commit | nothing |
  | `M08-9` | The affiliate destination and its 48 hour cooling window | blocked. See below | **MONEY PATH** when it exists | `OI-06`, which is OPEN, **and** `M08-2` |

- **The collisions between these slices, per FILE.**

  | File | Slices | Resolution |
  |---|---|---|
  | `packages/db/DELTA_MANIFEST.md` | `M08-3`, and `M08-9` if it ever runs | Serial. A manifest row cannot be appended to per branch. `M08-3` first |
  | `docs/decisions/ALLOCATION.md` | `M08-2`, `M08-3`, `M08-8` | **Serial, and this is the sharp one.** Three slices claim into one table, and [session 143](2026-08-23-session-143.md) recorded that an unreserved number at the TOP of the sequence opens no hole `CI-06w` can see. All three claims are written in **one** commit before any of the three runs |
  | `docs/decisions/README.md` generated span | `M08-2`, `M08-8` | Regenerated, not hand-edited. Two ADRs landing in one round is a re-run of `gates.mjs generate`, not a merge |
  | `packages/affiliate/src/index.ts` and `package.json` | `M08-5`, `M08-6`, `M08-7` | **Serial on the manifest and the barrel, concurrent on the three implementation files.** `M08-5` creates the package; `M08-6` and `M08-7` add one export each |
  | `pnpm-workspace.yaml` | `M08-5` only, and every sibling module doing the same thing | **The P3 wave-1 lesson exactly.** [Session 143](2026-08-23-session-143.md) found wave 1 serial on `pnpm-lock.yaml` because *"four concurrent branches would each merge cleanly alone and none of them together"*. A new workspace package is that same file. If any of the thirteen concurrent measurements also proposes a package, those slices are serial with `M08-5` whether or not they know it |
  | `docs/plans/M08-affiliate-system.md` | `M08-8` only | No other slice edits the plan. Amending it is `M08-8`'s ADR, never a build session's commit |
  | `.github/workflows/corpus.yml` | `M08-3` only, within M08 | Every probe-adding session in the tree contends here. One step, appended |

  **No two slices in this set write the same source file.** The contention is entirely in the four shared registries, and all four are append-only tables where the resolution is ordering rather than merging.

- **What I could not determine.**

  1. **What the rail is supposed to be.** [ADR-017](../decisions/ADR-017.md)'s title says *"one rail and one transfer table"*, which reads as `payout_transfers` widened. The schema as merged supports neither that nor a sibling. **This is not a session's call**: it widens the meaning of a merged money-path table, which is the restraint `OQ-M8-05` names in its own words as *"not a thing a non-money session grants itself"*. `M08-2` is an ADR for exactly that reason.
  2. **Whether the event-name split is a plan defect or a catalogue defect.** Both documents are frozen and both are `approved`. The tie-break is not derivable from either.
  3. **`OQ-M8-05` is still open and still unowned.** Does a held affiliate statement start the restriction episode's 48 hour SLA? `identity_restriction_episodes.sla_due_at` exists in `0031` and its comment says *"where a payout is pending"*. **No column changes on either reading**, as M08 section 10 already established; what changes is which rows set the clock. It is not a blocker for `M08-3`, because the column being added is `held_at` and not the SLA.

- **Does `OQ-075-01` block any slice? NO, and the reasoning is worth stating rather than assuming.**

  `OQ-075-01` asks two questions and only the second touches M08: **must an affiliate statement settle to a `closed` identity?** [ADR-075](../decisions/ADR-075.md) section 3 refuses to answer it, and refuses on a substantive ground rather than for lack of time: [M20](../plans/M20-wallet.md)'s `INV-M20-09` calls a forfeiture clause on money already earned **the single most brand-destroying term available to this firm**, and an affiliate statement is money already earned, so moving `INV-M8-12` to `= 'active'` would withhold it permanently *"through a consistency sweep rather than through a decision"*.

  **It blocks nothing, for three reasons that were each checked.**

  - **The `restricted` half is already ruled and is the whole of `INV-M8-12`.** [ADR-041](../decisions/ADR-041.md), [STATE_MACHINES:345](../architecture/STATE_MACHINES.md) and M08 section 3.4 all rule `restricted` blocks settlement, held rather than voided, at issue and again at settlement. `M08-3` and `M08-4` build that. `OQ-075-01` adds at most **one disjunct to one predicate** later.
  - **`closed` and `restricted` are different states with different arguments.** ADR-075's own witness paragraph is the proof: `closed` fails `= 'active'` and passes `<> 'restricted'`, which is exactly why the two halves separate cleanly.
  - **Seven of the nine slices never touch the predicate at all.** Attribution, commission arithmetic, clawback, the schema transcription, the events ADR and the rail ADR are all upstream of, or orthogonal to, which identity statuses may be settled to.

  **What `OQ-075-01` does do is make `M08-4` the slice that must not guess.** A build session writing `= 'active'` there because the three extraction doors read that way would decide a forfeiture the ADR explicitly declined to decide. `M08-4`'s prompt should say so and say STOP, on the pattern [session 143](2026-08-23-session-143.md) used for `CI-07`.

- **Files touched:** [`docs/sessions/README.md`](README.md) (the CLAIM row for `162`, **written and struck in one commit**, plus the generated `session_entries` span re-run), this file.

### What was NOT done, named rather than implied

**No code, no migration, no gate, no fixture, no probe, no ADR, no document but this log.** [`docs/INDEX.md`](../INDEX.md) is untouched and correctly so: `CI-06c` reads INDEX completeness in both directions over **documents**, and a session log is not one, which was checked against the gate rather than assumed from the fence. [`docs/STATE.md`](../STATE.md) is untouched. [ALLOCATION](../decisions/ALLOCATION.md) is untouched and **no number of any kind is claimed** by this session or by any of its nine slices, because thirteen siblings are measuring concurrently and a claim written from a fence that does not hold the file is the collision it is meant to prevent. **No ruling was taken** on the rail, on the event names, on `OQ-M8-05`, on `OQ-075-01` or on `OI-06`; all five are put with their evidence and their arithmetic. **[M08](../plans/M08-affiliate-system.md) itself was read and not edited**, including section 5's *"the restore has no event"* sentence, which is now false against [EVENTS:52](../architecture/EVENTS.md) and is `M08-8`'s to repair rather than this session's.

**One claim in the dispatching brief did not survive its primary source, and it is recorded rather than worked around.** The brief states *"Session 162 is already CLAIMED for you"*. It was not: [the CLAIM table](README.md) ends at `156`, and `157` through `162` carry no row. There was no row to strike. **On the table's own rule** that *"a number is free when no row claims it, and a row here is the only claim there is"*, and on [session 143](2026-08-23-session-143.md)'s precedent of writing an allocation row on session 142's behalf, the row for `162` is **written here and struck in the same commit**, which is the state the table defines for a number whose session has landed. `CI-06u` reads it as one key with the `~~` and the `**` stripped, which [the table's own note](README.md) records as having been seeded rather than assumed. **A number dispatched with no claim is the 110-111-112 collision of 2026-08-21 with the clock still running**, and five more numbers below this one are in that state right now.
