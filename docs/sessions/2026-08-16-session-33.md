## 2026-08-16 - Session 33: `0033` closes the calendar prior-image gap, and DELTA_MANIFEST gets the fourth allocation table (money path)

- **Done:** [ADR-045](../decisions/ADR-045.md) (**claimed as `044` and moved when an open sibling pull request turned out to hold that number**, see the landmines), [`0033_trading_calendar_revision_required.sql`](../../packages/db/migrations/0033_trading_calendar_revision_required.sql), [`probe_calendar_revision_required.sql`](../../scripts/db/probe_calendar_revision_required.sql) wired and pinned, and [DELTA_MANIFEST sections 16 and 17](../../packages/db/DELTA_MANIFEST.md).
  - **The number was claimed first, in its own commit, and pushed before the file existed.** That is what the founder asked for and it is stricter than what [ALLOCATION](../decisions/ALLOCATION.md) said: the sentence under the migration table read "adds its row here **in the same commit that creates the file**", which leaves open exactly the window the table exists to close, and which was looser than [ADR-034](../decisions/ADR-034.md)'s own wording one table above it, **on the registry where a collision is unrecoverable**. Corrected in the same commit as the claim.
  - **`0033` asserts and does not write**, which is [`0027`](../../packages/db/migrations/0027_triggers_invariants.sql)'s idiom rather than a preference: not one guard in `0027` repairs anything. **`CALENDAR-C1`**, a deferred constraint trigger, refuses an `UPDATE` that commits with no `trading_calendar_revisions` row carrying that row's `to_jsonb(OLD)`, and refuses one whose `dependent_row_count` is not the number the database itself counts. **`CALENDAR-C2`** refuses `DELETE` and `TRUNCATE`. It edits nothing: `0004`, `0027` and `0032` are untouched on disk.
  - **All 33 files apply forward-only from empty against PostgreSQL 16.13, zero errors: 102 tables, 351 indexes, 397 check constraints, 9 triggers.** Three of the four figures do not move.
  - **The probe is 12 / 12 with four success cases first**, and four counterfactuals were watched failing on their own findings.
  - **The manifest's own registry exists at last**: `OI-nn` and section numbers, with both collisions recorded as the reason. **Nothing renumbered.**
- **Next:** **The founder's E2 read** on `0033` (and on the twenty-two other money-path files, which is still the only thing between this work and a merge). Then S-E's transcription, still blocked on egress to `cmegroup.com`, then S-E4 (the loader and its six proofs) and S-E5.
- **Blockers:** None from this session. Two things are owed by somebody else and neither is new: the CME retrieval, and the E2 read.
- **Landmines:**
  - **The counted half of `CALENDAR-C1` is one assertion more than the ruling names, and it is separately rejectable.** `dependent_row_count` was self-reported, and `trading_calendar_revisions_incident_named_when_dependent` reads that number and nothing else, so **an incident became an ordinary data change by typing a zero**. The trigger recounts. If the founder reads the ruling narrowly, delete the second `IF` and the image half stands unharmed; `REJECTION 4` in the probe is the assertion that owns it, and it was watched failing with that `IF` removed.
  - **The image is compared as `jsonb`, so the loader S-E4 has not written yet must build it in the database.** `to_jsonb(t)` in SQL, not an object assembled in TypeScript: an application-built image renders timestamps differently and is missing whatever a later migration adds. That is `0032`'s derived-not-listed rule enforced rather than restated, and **it will look like a mysterious refusal to whoever writes the loader without reading this**.
  - **A no-op `UPDATE` is an `UPDATE`.** There is no exempt column, deliberately, so an unconditional upsert over a full year of calendar rows would demand a prior image for every row it rewrote to the same values. The loader needs `ON CONFLICT DO UPDATE ... WHERE t IS DISTINCT FROM excluded`. `REJECTION 6` exists to make this a fact rather than a note.
  - **The probe forces the deferred check with `SET CONSTRAINTS ... IMMEDIATE`, and without that line its four success cases assert nothing.** The file ends in `ROLLBACK` like every probe here, so a check deferred to commit is a check that never runs, and the file would print four green successes having verified none of them. **That is the vacuous-pass shape a third time**, after the `CHECK` that evaluated to `NULL` ([ADR-035](../decisions/ADR-035.md)) and the `DO` block that read a prefix of the schema (`OI-08`). Anyone editing this probe who removes those lines gets a green file that proves less than nothing.
  - **`TRUNCATE trading_calendar` alone fails on the revisions foreign key, and probing only that form tests PostgreSQL rather than this migration.** The probe truncates **both** tables in one statement, which is the form the foreign key has no objection to, and that is the only form in which `CALENDAR-C2` is the thing standing in the way.
  - **`rule_states` has no `trading_day` index and `0033` does not add one.** The count query scans it. `rule_states` is written once per account per day, so an index serving a query that runs only on a calendar correction would be paid for on every mark forever. It is a trade, written down here so the next person to see the plan does not read it as an oversight.
  - **THE ADR NUMBER COLLIDED WITH AN OPEN SIBLING PULL REQUEST, and this session caused it.** `044` was claimed first, in its own commit, exactly as the founder asked, and the check that was skipped is the one no table can do: **reading the remote branches.** `main` ends at `043`, this branch is not behind `main`, twelve gates passed, and PR #25 has held `ADR-044` on `claude/merit-futures-briefing-7auoor` the whole time. **The ADR is `ADR-045` now**, moved before the first pull request was opened, and [ALLOCATION](../decisions/ALLOCATION.md) records both the collision and why this branch moved when [ADR-034](../decisions/ADR-034.md)'s tiebreak points the other way. **The claim-first discipline is not the thing that failed; it is what limited the damage to a `sed` across twelve files instead of a founder resolving a conflict in a frozen registry.**
  - **`CI-06f`'s cross-branch gap has now been met in the wild twice in three days**, after [FOLD-02](../plans/FOLD-02-enforcement-window-and-suspension.md)'s plan hit it on `038`. Both times a session found it by hand and both times the job that would find it mechanically was still unwritten. **It is not carried as a new `OI-nn`**, because it is not this file's namespace and it is already stated as a declared gap in `CI-06f`, `CI-06h` and [ADR-036](../decisions/ADR-036.md). It is named here because two instances in three days is the evidence that decides whether it gets written.
  - **Session numbers are colliding in [the sessions index](README.md) exactly the way `OI-nn` was**: four rows say "Session 31" and two say "Session 32". This log claims 33 and the collision is left as found, because it is the same defect one registry over and the same argument applies to renumbering it.
- **Files touched:** `docs/decisions/{ALLOCATION.md,ADR-045.md,README.md}`, `packages/db/migrations/0033_trading_calendar_revision_required.sql`, `scripts/db/probe_calendar_revision_required.sql`, `scripts/corpus/gates.mjs`, `.github/workflows/corpus.yml`, `packages/db/DELTA_MANIFEST.md`, `docs/architecture/data-model/{README.md,trading_calendar.md,trading_calendar_revisions.md}`, `docs/{STATE.md,INDEX.md}`, `docs/sessions/{README.md,2026-08-16-session-33.md}`.

### The founder's restraint in `0032` is what made this session cheap

`0032` could have added this trigger. It found the gap, wrote the finding into its own header **and** into `OI-06` **and** into the design record, and stopped, because [ADR-042](../decisions/ADR-042.md) was silent and a money-path trigger is not a thing a session grants itself. **The cost of that restraint was one open item for one day.** The cost of the alternative would have been a guard the founder had never ruled on, sitting inside a file that is sacred once merged, discovered at the E2 read with nothing to compare it against.

**And the gap it left was bigger than the row it wrote.** `OI-06` named the `UPDATE` case and called the `DELETE` question "the same question"; it is not quite the same, it is the bypass, and `TRUNCATE` past a row trigger is a third door neither had named. A session that had added the trigger quietly would very likely have added only the first of the three.

### What this session found that it was not looking for

**[ADR-043](../decisions/ADR-043.md) had no row in the ADR registry table.** It was linked from a sentence in the README's preamble, and `CI-06n` accepted that, because the gate matches **any markdown link anywhere in a registry README** while its title says "every registry entry has a README **row**". Its `covers` line is honest and says "is linked from"; the title overstates it. So a merged ADR fell out of the registry it belongs to and twelve gates stayed green.

**The row is added. The gate is not narrowed here**, and that is a deliberate stop rather than a shrug: narrowing it means deciding what counts as a row across every registry directory [ADR-043](../decisions/ADR-043.md) created, and then watching it fail on a seeded violation, which is a session's work and not a footnote to somebody else's. Carried as **`OI-09`**, the first number allocated from the table this session built, on the day it was built.
## 2026-08-16 - Session 33: FOLD-01 session 5, the portal, the vendors and the legal surface

- **Done:** [FOLD-01](../plans/FOLD-01-phone-identity.md) section 6.1's five remaining non-money documents, folded against [ADR-039](../decisions/ADR-039.md) and against [`0029`](../../packages/db/migrations/0029_phone_identity_and_auth.sql), which is merged and is the authority on what exists.
  - **[M04](../plans/M04-trader-portal.md):** `SD-M4-04` recorded with `elevated_by_factor`'s check list stated as the enforcement; **`INV-M4-14`** (a sensitive action the session is not elevated for renders disabled with its reason and its route, enforced by required props) and **`INV-M4-15`** (the portal never computes elevation, enforced by a negative-authz test); **section 3.7, the authority boundary shown rather than hit**; `SC-M4-11`; `AS-M4-05` gains the SIM-swap shape, two counters and its residual; `FM-M4-10`; three metrics, an alert, `OQ-M4-06`, `DEP-M4-07` and `DEP-M4-08`.
  - **[M03](../plans/M03-billing-checkout.md):** section **7.9.1**, the registration lookup as ADR-023's vendor at a second call site with the failure posture inherited verbatim and portability history as a **disqualifying** selection criterion; section **7.9.2**, the two Cost Stack lines and why the *shape* of the cost changed rather than its size; two metrics.
  - **[M10](../plans/M10-integrations.md):** **`IN-M10-06`** the SMS sender; **`INV-M10-12`**; three tables and one column added to the replication exclusions; `FM-M10-10` and `FM-M10-11`; **section 7.9** carrying both findings; the chaos suite gains registration with its assertion explicitly blocked; two metrics, an alert, `OQ-M10-05`, `OQ-M10-06`, `DEP-M10-07` and `DEP-M10-08`.
  - **[COUNSEL_PACKET](../legal/COUNSEL_PACKET.md):** item 3 becomes three mappings and gains **3d**, telecom metadata lawful basis, with four sub-questions and six facts the answer turns on.
  - **[PRIVACY_POLICY](../legal/PRIVACY_POLICY.md):** the **telephony collection row**, which **cites 3d rather than asserting a basis**, the **communication delivery providers** sharing row, and open item 5.
  - All 12 gates green before each commit, `generate` reports every span already matching.
- **Next:** **FOLD-01 session 6**, the registries and the gates: `EDGE_CASES`, `GOLDEN_SCENARIOS`, `STRATEGY`, `CI-06k`, `API_CONTRACT`, `EVENTS`, `STATE_MACHINES`, `INDEX`. **It now owes five scenario citations across two sessions and a set of endpoints the corpus has been citing without defining.**
- **Blockers:** None for session 6. **Two rulings are wanted and one of them is a proven gap rather than a preference.** Both are named under landmines because both are the kind of thing a later session would otherwise assume was decided.
  - **`OQ-M10-06`.** Nothing in the schema holds an address the dispatcher can send to. Recommendation offered, not decided.
  - **`OQ-M10-05`.** Whether registration degrades on an SMS **vendor** outage the way C-28 degrades on a **cost** trip. Recommendation offered, not decided.
- **Landmines:**
  - **There is no plaintext telephone number in any of the twenty-nine migrations, and two documents assert that something holds one.** `identity_phones.phone_hash`, `phone_change_requests.new_phone_hash`, `otp_challenges.destination_hash` and `contact_channels.value_hash` are all one-way, and `identity_phones.phone_preview` is "enough to recognise, not enough to reconstruct". Both [`0019`](../../packages/db/migrations/0019_notifications_and_community.sql) and the [`contact_channels`](../architecture/data-model/contact_channels.md) design record say the value is hashed because **"the sending path holds the address"**. **The sending path is [M10](../plans/M10-integrations.md), it holds nothing by design, and `AS-M10-06` part 3 forbids a vendor holding it.** So `INV-M16-03`'s prior-contact notification, [SECURITY §4.8](../architecture/SECURITY.md) leg 2, and **every security-class SMS Merit itself initiates** have no destination. Only challenge-response flows work, because there the trader types the number into the request. **The email side has the same gap on prior addresses and not on current ones**: `users.email` is plaintext but is one column on one row, so a change overwrites it. `SD-M16-06` made the countermeasure **representable** and not **sendable**, and the difference is the whole finding.
  - **The sharpest form of it, and the reason it would survive a review unnoticed.** `phone_change_requests_applied_is_complete` makes `prior_notified_at` a precondition of the write, which reads as enforcement. **A database can only assert that a timestamp exists.** A handler with no address to send to and a NOT NULL-shaped obligation will fill it, the constraint will pass, and the anti-takeover control will read as enforced in every document and in every test that inspects the row while never having sent anything. **Storage-enforceable is not send-enforceable**, and this corpus has no gate that can tell them apart.
  - **`IN-M10-06` is on the critical path of registration on day one, and `INV-M10-01`'s claim was left exactly as written.** Its four-item list names purchase, provisioning, rule evaluation and payout, and does not name registration. That is an omission of a flow that did not exist when the list was written, not an exemption. **Widening the list would have been asserting a protection that does not exist**, so the omission is named in section 7.9.3 instead. A later reader who takes `INV-M10-01` to mean "no vendor is ever load bearing" is wrong from 2026-08-16 onward.
  - **`otp_send_budget.scope_key` is polymorphic by `scope_kind` and one of its four meanings is a raw IP.** The first draft of M10's replication note said its scope keys were hashes and country codes; that is true for `phone`, `country` and `global` and **false for `ip`**, where the comment in `0029` says "the address". The column is excluded at column level for that reason. **A column-level exclusion whose correctness depends on a sibling column's value cannot be evaluated at replication time**, which is why the whole column goes rather than the `ip` rows, and the shape appears the moment a table stores a key whose type is a value.
  - **No `EC-nnn` or `GS-nnn` identifier was claimed anywhere in this diff, for session 4's reason exactly.** `CI-06d` fails on any such identifier cited in `docs/` that does not resolve to a registry definition. The three scenarios M04 owes are named **in words** in its section 8.2 so session 6 has the list, and the count of unclaimed citations is now **five across two sessions**. A third session doing this without session 6 running would start to look like a convention rather than a constraint.
  - **No `OI-nn` was claimed for `OQ-M10-06` and that is a consequence of an unfixed defect.** [`DELTA_MANIFEST`](../../packages/db/DELTA_MANIFEST.md) already carries **two rows numbered `OI-06`**, found by S-E3. Claiming a third number into a namespace with a live collision makes the collision harder to resolve, not easier.
  - **Two hand-maintained counts in M04's test plan were replaced by their rules, and one was already wrong.** The visual suite read "9, one per screen" against a section 3.1 that has listed ten screens since [ADR-019](../decisions/ADR-019.md). The stated count and the stated rule disagreed and nothing caught it, which is [ADR-034](../decisions/ADR-034.md)'s class. M10's chaos-suite "4" went the same way. **No ordinal claimed for either**, on session 31's finding that the tally of hand-maintained counts is itself double-booked.
  - **`SC-M4-11` is the one identifier this session allocated, and it is reversible on purpose.** It records a surface `AS-M4-05` counter 2 and `GS-104` already committed to and that section 3.1 never listed, which is why `SD-M4-03` was written in section 2 to serve a screen the screen table did not have. **The reason it was allocated where session 4 refused to allocate `SD-M7-nn` is not inconsistency**: `SD-nn` has an allocation table and a gate that would have failed on the spot, `SC-M4-nn` has neither, and the surface here already exists in approved text. Nothing outside M04 section 3.7 cites it.
  - **The "Next 3 actions" list in [STATE](../STATE.md) still has three near-identical copies of item 3 and this session did not add a fourth.** Session 32 recorded the duplication and left it as found. Appending a fifth session's version of the same line is the failure the note describes, so the current position is stated in this session's own STATE section instead. **All three copies name a FOLD-01 next-session that is now two sessions stale**, and whoever rules on the `OI-06` collision should fix them in the same pass, since it is the same cause.
- **Files touched:** `docs/plans/{M04-trader-portal,M03-billing-checkout,M10-integrations}.md`, `docs/legal/{PRIVACY_POLICY,COUNSEL_PACKET}.md`, `docs/{INDEX,STATE}.md`, `docs/sessions/{README,2026-08-16-session-33}.md`. **No migration, no registry entry, no `DELTA_MANIFEST` row**, which was the stop condition.

### The one thing the fold's own brief could not settle, and why it was raised rather than inherited

FOLD-01 section 6.1 gives M10 "a sixth integration, the SMS sender" in six words, and the six words are correct. **What they do not carry is that this integration is unlike the five it joins**, and the difference is not a matter of degree.

Every existing integration is post-commit and asynchronous, and `INV-M10-01` is the invariant that makes that true rather than customary. The SMS sender is synchronous, it runs before anything has committed, and phone verification is mandatory, so **its outage is a closed front door rather than a delayed message**. `AS-M10-06` is a whole adversarial scenario about a vendor becoming load bearing by drift over six months with no moment anybody could have said no; here the moment is available, it is now, and the answer is that the vendor is load bearing by design.

**The temptation was to settle it by inheritance**, because [ADR-039](../decisions/ADR-039.md)'s degradation ruling fits the shape so exactly. Registration is where nothing has moved and nothing is owed; a refusal protects only a bill or an uptime figure; the mechanism to reuse is built, alarmed and countable. Applying it would have taken one sentence.

> **It was not applied, and the reason is the asymmetry the ruling itself is built on.** Degrading on a **cost** trip is a decision about Merit's own SMS bill, which Merit controls. Degrading on a **vendor** outage hands the same lever to anyone who can take a third-party SMS provider offline, and an SMS provider is a substantially easier target than Merit. **The founder ruled on the first bargain and has not been shown the second**, and "the same reasoning obviously applies" is precisely the move that produced this corpus's three worst recorded errors.

Both answers are testable and the fifth chaos flow is written to whichever lands. **What is not testable is the absence of an answer**, which is the only reason it is a blocker-shaped item in a session with no blockers.
