## 2026-08-16 - Session 33: FOLD-02 session 4, the machines and the invariants

- **Done:** [FOLD-02](../plans/FOLD-02-enforcement-window-and-suspension.md) session 4, money path. [STATE_MACHINES](../architecture/STATE_MACHINES.md) section 2 gains `held_pending_review` as a pre-approval state reachable from nothing else, with its two exits, the ledger discriminator against `frozen`, and the zero-denial amendment written out in both halves. **Section 3 becomes two sub-machines and `wallet_withdrawals` is drawn for the first time.** Section 10 gains `G-HOLD-REQUIRED`, `G-HOLD-RELEASED`, `G-HOLD-ENFORCED`, the four external-leg guards, and **definitions for `G-ENFORCEMENT-RESTRICT` and `G-RESTRICTION-LIFTED`, which section 9 has named since this document was written and section 10 defined nowhere.** `G-ELIGIBLE` gains `identities.status = 'active'`. Section 11 gains five cross-machine rows. [M05](../plans/M05-payout-system.md) gains section 3.3 (the hold path) and INV-M5-17 to INV-M5-20; [M20](../plans/M20-wallet.md) gains INV-M20-12 to INV-M20-14 and section 3.3a; [M07](../plans/M07-risk-abuse.md) gains section 3.3a tabulating the four enforcement outcomes; [SECURITY](../architecture/SECURITY.md) section 4 gains items 8 to 12. All 12 gates green, `falsify.mjs` green.
- **Next:** FOLD-02 session 5 (M02's provisional platform leg, M06, M03, M04, M16, the M08 confirmation). **M04, M03, M10 and the legal documents were not touched**, per the session fence.
- **Blockers:** None.
- **Landmines:**
  - **`frozen --> transferring` was already fixed before this session started.** The brief named it as this session's correction; it had landed with [ADR-040](../decisions/ADR-040.md)'s own commit. What was **not** fixed, and is now, is the pair of guards in section 10 that still read `transferring`: `G-NO-IN-FLIGHT` and `G-FREEZE-DURING-FLIGHT`. The drawing was corrected and the guards behind it were not, which is the more dangerous half, because the guard is what the engine reads.
  - **Section 2 pointed at "section 3" for `wallet_withdrawals`' machine and section 3 was `payout_transfers`**, a different table with a different enum. `wallet_withdrawals` had **no drawing anywhere in the authoritative document**. That is how ADR-040's halt came to land on a machine no drawing contained, and it is why the `0011` unenforced-halt defect survived a founder-grade review.
  - **M05 said `status: settled_to_wallet` in two places, and [ADR-028](../decisions/ADR-028.md) explicitly REJECTED that value.** `0001`'s comment block records the rejection. ADR-028 names M05 as one of two files it corrected and read past the phantom twice on the way to the freeze target. **Fifth site of that sweep, and not on ADR-040's list of four remaining sites either**, because nobody had looked in the file the ADR believed it had finished.
  - **Renumbering M05's freeze path to 3.4 retargeted three inbound pointers**, in [M16](../plans/M16-notification-center.md), [M12](../plans/M12-transparency-platform.md) and FOLD-02 section 4.6. All three are corrected in the same commit. A renumber that retargets a pointer silently is the same defect class as a predicate that stops matching, and **nothing in CI catches it**: `CI-06a` validates that a link resolves, not that it resolves to what it used to mean.
  - **The hold narrows AS-M7-01's margin on purpose.** Detection cadence against extraction speed was already thin; a hard 48 hour release makes it thinner. The trade is written into M07 rather than left for the first investigator to discover at 3am.
  - **Four session-log entries are numbered 31 and two are numbered 32.** Parallel sessions on separate branches each took the next number they could see. This entry is **33** because 32 was taken twice already. Session numbers have no allocation table, which is [ADR-034](../decisions/ADR-034.md)'s race one registry over, and `CI-06n` cannot see it because every one of those rows resolves.
- **Files touched:** `docs/architecture/{STATE_MACHINES,SECURITY}.md`, `docs/plans/{M05-payout-system,M20-wallet,M07-risk-abuse,M16-notification-center,M12-transparency-platform,FOLD-02-enforcement-window-and-suspension}.md`, `docs/{STATE}.md`, `docs/sessions/`.

### What this session did not do, named rather than dropped

**`CI-06l` is not written.** FOLD-02 section 4.4 names it (every expiry column either names a release job in CRON_INVENTORY or sits on a written exemption list) and section 8 assigns it to **session 7**, with CRON_INVENTORY and the registries. This session states the sweep and the dead-man switch in prose in four documents; **nothing yet checks from the tree that the sweep exists.** Until session 7 lands it, the auto-release is load-bearing by assertion.

**No `EC-nnn` or `GS-nnn` was claimed.** Section 7's minimum of eight golden scenarios belongs to session 7, and a plan that pre-names a registry identifier has claimed in a registry with no claim mechanism. The five cross-machine rows added to STATE_MACHINES section 11 are the scenarios' content, written where a reader of the machine will find them, and they become `GS-nnn` rows when that session writes them.

**Event names for the three halt transitions are proposed, not folded.** [EVENTS](../architecture/EVENTS.md) is session 6's file. The drawing needs a column; the registry is what makes a name real, and the table says so where it could otherwise be mistaken for a claim.
