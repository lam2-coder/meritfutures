## 2026-08-15 - Session 25: the S-D review rulings folded
<!--index: the S-D review rulings folded -->

- **Done.** Four founder rulings from the S-D read, all non-money. Two changed frozen documents and carry ADRs ([ADR-037](../decisions/ADR-037.md), [ADR-038](../decisions/ADR-038.md)); one is an EDGE_CASES entry ([EC-141](../edge-cases/EC-141.md)); one is code with a seeded case. **`gates` and `falsify` are green: eleven gates pass clean and fail dirty, five scope cases hold.**
  - **THE THIRTEEN RESTATED VALUES ARE DELETED, NOT THE WRONG ONE CORRECTED.** [GOLDEN_SCENARIOS section 3](../testing/golden-scenarios/README.md) restated thirteen `CORE-50K` parameters from [M01 Appendix A.1](../plans/M01-rules-engine.md#a1-core-eod-core_eod) **in the sentence that named Appendix A as their only authority**, with the ladder stale at 8 against 5. Fixing the 8 leaves twelve copies and the mechanism intact. **The section now points at the appendix and states no parameter**, and the one surviving copy is `fixtures/plans/CORE-50K.json`, which is a config record the loader reads rather than a restatement.
  - **GS-066 AND GS-067 CARRIED THE SAME STALE 8 IN THEIR PINS**, two sections below the shorthand and outside the ruling's literal scope. Both now name `max_payouts`. The boundary pair is what those fixtures pin and the literal was never load bearing to it; leaving them would have left the exact defect the ruling is about visible on the same page.
  - **CI-06g'S RULE EXTENDS FROM COUNTS TO PARAMETERS AND ITS RUNNER DOES NOT, WHICH IS STATED IN ITS OWN `covers` LINE.** A count misleads a reader; **a plan parameter is an input to a running system**. Closing the enforcement needs a query that can tell a shorthand from a scenario's own arithmetic, since GS-026's "withdrawable 214,250, cap 150,000" is a computed boundary a fixture exists to pin. That query is not ruled and a gate that fails on correct prose is a gate that gets switched off.
  - **TWO SILENT MIS-PARSES IN THE YAML SUBSET, both found by probing rather than by reading.** `yaml` is **not** admitted; VG-12 stands.
  - **THE FIRST ONE IS THE SERIOUS ONE: A SEQUENCE ITEM DROPPED ITS TAIL.** `parseNode` returns how far it got and **both sequence-item call sites discarded that number**, so `- \n  - a\n  keep_me: 1` parsed to `[["a"]]` with `keep_me` read from disk, parsed and thrown away, on every stream, with no error on any of them. **A fixture input the engine never sees is the worst outcome a golden file has**: the scenario passes while pinning something the author did not write. It is the same failure the loader's `AWAITING_M01_INPUT` list prevents from the other direction, and **a field the parser never surfaces cannot reach that refusal at all.** `readWholeItem` now refuses to read part of an item.
  - **THE SECOND IS THE RECORDED DATE HAZARD, GENERALIZED.** `True`, `yes`, `NULL`, `0x1F`, `007`, `+5`, `1_000`, `.inf` and `1:30` were all strings here and are booleans, null, integers, a float and a sexagesimal under a real library. The bare `YYYY-MM-DD` trading day is now the **single named admission** in that class, and the same-commit quoting obligation is recorded **at the line of code that admits it**, where somebody reaching for the dependency will read it, rather than only in a file header.
  - **CI-03 NOW PRINTS WHAT IT PROVES, AND THE CLAIMS ARE MEASURED RATHER THAN WRITTEN.** `src/coverage.ts` derives the polarity from the same probe the suite uses and **proves the corrupted-expectation claim by corrupting every loaded fixture and re-running the stage's own assertion over it**. The block goes to the job log and `$GITHUB_STEP_SUMMARY`. **`--reporter=verbose` is part of the command and is not cosmetic**: Vitest's default reporter swallows test stdout on a passing run, so without it the block is computed and discarded.
- **Next.** **S-E**, TradingCalendar as data: money path, fresh session, **plan mode**. The founder's E2 read of the S-D money-path files is unaffected by this session, which touched no `.sql` and no engine source.
- **Blockers.** None.
- **Landmines.**
  - **THE FALSIFICATION HARNESS CARRIED A HAND-MAINTAINED COUNT, WHICH IS THE SIXTH SITE OF ADR-034'S CLASS AND THE FIRST INSIDE THE TOOLING.** `falsify.mjs`'s CI-06g seed spelled the `ec_count` opener out and pinned `140` beside it, so **adding EC-141 broke the harness built to catch hand-maintained counts**, and it broke in the direction that reads worst: the corrupted value became the true one. It now reads the span and adds one, which is a violation by construction whatever the query returns. The anchor check survives, because a missing span still throws.
  - **THE STAGE ASSERTS THAT IT IS CURRENTLY BLIND, which is a strange assertion until the alternative is stated.** The blindness is real either way and the choice is between a suite that knows it and one that does not. `test.runIf(stubbed)` carries it, so when M01 lands the assertion stops running and the emitted block prints the caught direction instead. **Nothing is edited to make that happen**, which is the property the polarity already had.
  - **EC-141 OWNS NO GOLDEN SCENARIO AND USES CI-06e'S DOCUMENTED `none owned` SENTINEL.** It is a property of the fixture **format** rather than of an engine behavior, so a scenario asserting it would be a golden file about golden files and would put an identifier in the registry that pins no rule. The gate **reports** the exception rather than swallowing it, which is the whole point of the sentinel.
  - **THE ADR ALLOCATION TABLE'S `036` ROW WAS STALE AGAIN, four commits after the merge that falsified it**, exactly as `035`'s was. Second instance, same column, and [ADR-036](../decisions/ADR-036.md) already records that the State column is prose no runner can check. Corrected in place.
  - **NOTHING IN THIS SESSION MOVED A PARAMETER.** The ladder is 5 on Core EOD and Merit Rapid and 4 on Direct, it was always that, and `fixtures/plans/CORE-50K.json` is byte-identical apart from its `note`. The ruling deleted prose, not a value.
- **Files touched:** `docs/testing/GOLDEN_SCENARIOS.md` (section 3's shorthand, GS-066 and GS-067's pins, the header record, frontmatter); `docs/testing/STRATEGY.md` (CI-03 and CI-06g rows, the coverage-gap paragraph); `docs/DECISIONS.md` (allocation table, ADR-037, ADR-038); `docs/EDGE_CASES.md` (EC-141, the count span, frontmatter); `packages/golden-loader/src/{yaml,coverage,index}.ts`; `packages/golden-loader/test/{yaml,fixtures.golden}.test.ts`; `packages/golden-loader/README.md`; `packages/rules-engine/fixtures/plans/CORE-50K.json` (the `note` only); `.github/workflows/golden.yml`; `scripts/corpus/falsify.mjs`; `docs/{STATE,SESSION_LOG,INDEX}.md`. **No `.sql` file was edited, no engine source was touched, and no plan parameter changed.**

---

---
