---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 639: a claim about the suite is ruled written ONCE and cited, and the citation is policed by an invariant that was already green

<!--index: **THE ROW IS [ADR-444](../decisions/ADR-444.md), `proposed` and UNSIGNED. COMMENT-ONLY DIFF, MONEY-PATH NEIGHBOURHOOD, `E2` READ OWED ON NINE FILES.** **THE CONVENTION IS RULED AND APPLIED TO SIXTEEN SITES, NOT THIRTEEN.** The enumeration was RE-DERIVED by running the census over this base and it confirms `ADR-442`: **thirteen live source sites, nine `apps/worker`, three `apps/api`, one [`scoped-db.ts:4193`](../../packages/db/src/scoped-db.ts)**, every one wrapped across the substring a line-oriented grep searches for. **THREE MORE CARRY THE SIBLING DEFAULT CLAIM AND ALL THREE ARE IN FENCE**, so sixteen were repaired and none named and left. **THE INSTRUMENT CANNOT SEE TWO OF THEM**: `nowhere` is in none of the census's three negation vocabularies, so [`scoped-db.ts:3447`](../../packages/db/src/scoped-db.ts) and [`:4576`](../../packages/db/src/scoped-db.ts) were found by READING [ADR-440](../decisions/ADR-440.md)'s own repair text. **THE RULING**: the canonical statement lives in LIVE SOURCE and never in a dated record, at limit 1 of `CatalogRow` [`scoped-db.ts:3466`](../../packages/db/src/scoped-db.ts); a citing site keeps the RULING that holds its own `throw`, NAMES the canonical site, and RESTATES nothing. **THE ARGUMENT IS THAT THE RUNNER FOR THE CITING HALF IS ALREADY BUILT AND GREEN**: `RI-15` covers every `file:line` citation in every source file DERIVED FROM THE WALK, so the convention moves sixteen undecidable sentences into a decidable check's scope rather than trading a control for a habit. **AND THE GATE COLLECTED ON THIS ROW'S OWN DIFF**: the first draft cited [`scoped-db.ts:3442`](../../packages/db/src/scoped-db.ts) and `RI-15` refused all fifteen copies (34 of 35); repointed at the declaration, 35 of 35. A restatement would have drawn no finding from any check in this tree. **THE DISPATCH'S OWN PREMISE CORRECTED**: `ADR-443` has NOT landed, so on this base the `GENERATED ALWAYS AS` expression is compared NOWHERE, and writing the dispatch's version into the canonical statement would have put one false sentence exactly where sixteen sites now point. **NO CITED LINE MOVES, DERIVED PER FILE**: 152 distinct cited lines into `scoped-db.ts` alone, citations below an edit in seven of nine files, every block rewritten AT ITS OWN LINE COUNT by a tool that REFUSES a changed count; `numstat` `n  n` on all nine, `wc -l` identical both sides. Suite **base 8,429, mine 8,429, delta 0** over 342 files / 11 skipped / 0 failed; invariants 35 of 35 both sides; **`CI-06f` red at `441`, `442`, `443` because this row added `444`**, reported and NOT repaired. -->

### 1. The objective

Rule the convention `ADR-442` section 15 item 4 puts and nobody has answered, that **a claim about the suite is written ONCE and cited thereafter**, argue it against a runner that checks every copy and price both, then apply whatever was ruled to the thirteen live source sites `ADR-442` section 15 item 1 enumerates. Re-derive the enumeration rather than trust the list. Comments only.

### 2. Done

- **The container measured and reported.** No repository on arrival; `add_repo` with `access: "push"` then one shallow clone. The remote was not pre-checked.
- **The base reproduced at `89b02405` on a clean tree before the first edit, and it is green**: gates **33 of 33**, invariants **35 of 35**, suite **342 files / 8,429 passed / 11 skipped / 0 failed**, typecheck, lint and `format:check` all clean. `gates.mjs generate` left the tree clean.
- **The enumeration re-derived by RUNNING the census, not by reading the list.** `ADR-441`, `ADR-442`, `ADR-443` and `packages/tooling/checks/absence-grammar-census.mjs` are on `claude/wave-441s` and are not on this base; the census was run from a copy with its one import repointed at this tree's own unmodified `dependants.mjs`, and **nothing under `packages/tooling/checks/` was edited**.
- **Thirteen live source sites, and `ADR-442`'s split of nine, three and one is confirmed.** Every one wraps across the substring, which is `ADR-441` section 11 item 3 holding on every member of the population.
- **Three more sites carry the sibling DEFAULT claim, and all three are inside this fence**, so they were repaired rather than named and left. Two of them are [ADR-440](../decisions/ADR-440.md)'s own repair text.
- **The census cannot see two of those three.** `nowhere` is in none of its three negation vocabularies. They were found by reading, not by running.
- **The claim falsified at primary source**, at [`scoped-db.test.ts:2728`](../../packages/db/test/scoped-db.test.ts) for TYPE and NULLABILITY per column, [`:2679`](../../packages/db/test/scoped-db.test.ts) for views, [`:3217`](../../packages/db/test/scoped-db.test.ts) for DEFAULT, with non-degeneracy guards at [`:2754`](../../packages/db/test/scoped-db.test.ts) and [`:3311`](../../packages/db/test/scoped-db.test.ts).
- **The dispatch's own premise corrected.** It states `ADR-443` added the `GENERATED ALWAYS AS` comparison; that entry has not landed and on this base the generation expression is compared nowhere.
- **The convention ruled, argued against a runner over every copy, and both priced.** [ADR-444](../decisions/ADR-444.md) sections 5 to 8, including the case against the ruling.
- **All sixteen sites repaired**, each keeping its struck sentence beside its correction on `RI-14`'s idiom, each citing the canonical site instead of restating it.
- **No cited line moves, derived per file rather than inherited**, and enforced by a tool that refuses a block whose line count changed.
- **The pointer watched RED two ways and restored byte-identically**, and `RI-15` corrected this row's first draft of the citation.

### 3. Next

- The reciprocal back-pointer in `scoped-db.test.ts`, which is the one property this ruling does not buy and is another row's file.
- `nowhere` added to the census's `UNIVERSAL` vocabulary, with the figure re-derived rather than adjusted.
- Whether the ruling generalises past a claim about the SUITE to a claim about the DOMAIN, which this row did not extend and a founder should settle.

### 4. Blockers

None for this row. `CI-06f` is red at `441`, `442` and `443` because this row added `444` and those rows are on a wave branch that has not landed; **no reservation was written for a row this session does not own.**

### 5. Landmines

- **`packages/db/src/scoped-db.ts` carries 152 distinct cited lines and its last cited line is its last line**, so any insertion anywhere in it moves a pointer. Every edit there must be line-count neutral per block, and the two prior rows in this chain reached the same conclusion independently.
- **A comment run's first line is not where its claim sits.** The census reports the run's start; the sentence is often ten or more lines later. A row repairing one of these needs both numbers.
- **The canonical statement now says the generation expression is compared nowhere.** The moment `ADR-443` lands that is false and must be corrected in the same commit. It is one line in one place, which is the ruling paying for itself, and it is the highest-risk line in the diff.

### 6. Files touched

- `docs/decisions/ADR-444.md` (new), `docs/decisions/ALLOCATION.md` row `444`, `docs/decisions/README.md` (generated), `docs/STATE.md` (one generated span), this log and `docs/sessions/README.md` (generated).
- `apps/worker/src/breaker/evaluate.ts`, `apps/worker/src/digests/rows.ts`, `apps/worker/src/recon/sweep.ts`, `apps/worker/test/breaker.test.ts`, `apps/worker/test/digests.test.ts`, `apps/worker/test/recon-sweep.test.ts`, `apps/api/src/payout-backend.ts`, `apps/api/test/payout-backend.test.ts`, `packages/db/src/scoped-db.ts`. **Comments only, every one line-count neutral.**
