---
status: approved
depends_on: [README.md]
last_updated: 2026-09-08
---

## 2026-09-08 - Session 652: the unregistered generated table is transcribed now, both halves of the arithmetic survive, and one leg of the block is vacuous

<!--index: **THE ROW IS [ADR-457](../decisions/ADR-457.md), `proposed` and UNSIGNED. MONEY PATH, `E2` READ OWED.** [ADR-455](../decisions/ADR-455.md) section 12 item 1 taken and DISCHARGED. [`scoped-db.test.ts:3592`](../../packages/db/test/scoped-db.test.ts) read *"`live_account_state` is CREATED by `0050` and transcribed NOWHERE"* and row 447 transcribed it: `grep -c live_account_state packages/db/src/schema.ts` returns **7**, RE-DERIVED here, and [`schema.ts:5447`](../../packages/db/src/schema.ts) declares the table. **BUT `UNREGISTERED_GENERATED_TABLE` IS THE RIGHT NAME AND KEEPS IT**: the leg reads `ALL_REGISTERED`, which is REGISTRATION, and the table is unregistered deliberately on `0050`'s `REVOKE ALL ... FROM merit_app`. The docblock conflated `transcribed` with `registered`; the constant never did. **BOTH HALVES OF THE ARITHMETIC SURVIVE**, derived from the tree: the migration set's generated money population is **TWO** (`reconciliations.delta_cents`, `live_account_state.intraday_movement_cents`) and this block's is **ONE**. **[ADR-455](../decisions/ADR-455.md)'s three-to-two correction moved its OWN figure onto the number this docblock already carried**, so neither half is a casualty of it. **THE LEG IS NOT VACUOUS AND IS NOT SUPERSEDED, WHICH REFUSES THE SECOND CLAUSE OF THE COMMISSIONING ITEM**: of its five assertions, two are reached by nothing else in the tree, two are duplicated by [`:4626`](../../packages/db/test/scoped-db.test.ts) and [`:4606`](../../packages/db/test/scoped-db.test.ts), and **ONE IS VACUOUS**, comparing the string literal `'intraday_movement_cents'` with itself, named as vacuous at its site rather than reworded. **[ADR-443](../decisions/ADR-443.md) section 11 item 1's PREDICTION IS DISCONFIRMED BY EVENT**: row 447 transcribed the table and `:3825` stayed green, because transcription was never the predicate. **Line-neutral: 5207 lines both sides, `--numstat` 11/11, 52 of 54 cited lines byte-identical**, the 53rd being `:3592` itself and the 54th the out-of-range probe. **Left uncorrected with reasons:** the test title at `:3825` is now false but is quoted VERBATIM by [ADR-449](../decisions/ADR-449.md):109, a dated record. **One correction beyond the docblock**: `:4472` claimed this table has the only generated `*_cents` column in the estate and the estate has two. -->

### 1. The objective

Take [ADR-455](../decisions/ADR-455.md) section 12 item 1. The docblock at [`scoped-db.test.ts:3592`](../../packages/db/test/scoped-db.test.ts) rests on a premise row 447 falsified, and the constants beneath it are said to be named for a condition that no longer holds. **Two things to settle and they are separate: whether the block's arithmetic is still right, and whether the block still does useful work.** Re-derive every figure from the tree rather than from [ADR-455](../decisions/ADR-455.md) or the reservation, and repair line-neutrally on a file carrying **54 distinct cited lines**, `:3592` among them.

### 2. What the container arrived with

**NO REPOSITORY.** No checkout, no working tree, no `origin`, no `node_modules`. This is the fourth consecutive row to find it and the third to record it, after [ADR-449](../decisions/ADR-449.md) and [ADR-456](../decisions/ADR-456.md). **The dispatch anticipated a shallow-clone refspec problem and said it was not worth reporting; that is not the fault that occurred.** The repository was located through the session's own repository listing, attached for push, cloned, and `0d5b6a61` resolved in it. `git fetch origin claude/wave-456s` then succeeded on the first attempt. The distinction is recorded because a row that reads the dispatch's reassurance and stays quiet reports a fault it did not have.

### 3. What was derived

| Derivation | Result |
| --- | --- |
| `grep -c live_account_state packages/db/src/schema.ts` | **7**, and [`:5447`](../../packages/db/src/schema.ts) is a real `pgTable` declaration |
| `live_account_state` in `ALL_REGISTERED` | **NO**, and deliberately, on `0050`'s `REVOKE` |
| `GENERATED ALWAYS AS` in the migrations, multiline | **6** |
| The same with a single-line grep | **4**, reproducing [ADR-455](../decisions/ADR-455.md)'s undercount-by-a-third warning |
| Migration-set generated `*_cents` columns | **TWO** |
| Block-reachable generated `*_cents` columns | **ONE** |
| Distinct cited lines in the file | **54**, including `3563`, `3592`, `3601` and the `99999` probe |

### 4. The verdict on the leg, which is not uniform

**THE DISPATCH ASKED WHETHER THE LEG IS TRUE FOR A DIFFERENT REASON OR VACUOUS. IT IS BOTH, ON DIFFERENT ASSERTIONS.** The population arithmetic and the expression literal are unique live work. The registry-absence assertion is true for a different reason and is duplicated by a newer block that states the reason better. **The `_cents` assertion is vacuous and always was**: it reads a constant declared 258 lines above it, so no edit to the migrations, the schema or the registry can falsify it. [ADR-457](../decisions/ADR-457.md) section 4 rules on each of the five separately, because one verdict for the block would be false whichever verdict it was.

### 5. Verification

**Base `0d5b6a61` and head both green and identical on every figure**, which is the expected result for a comment-only diff: gates **33 of 33**, invariants **35 of 35**, vitest **344 files, 8619 passed, 11 skipped, 0 failed**, typecheck, lint and `format:check` clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` were not run; both mutate the working tree.

### 6. Next

**Nothing is owed to this row.** [ADR-457](../decisions/ADR-457.md) section 7 records five sites left uncorrected with reasons, three of them in dated records that [ADR-386](../decisions/ADR-386.md) section 6 says are named and not amended. **The one a later row could take is the test title at [`:3825`](../../packages/db/test/scoped-db.test.ts)**, which is false and is quoted verbatim by [ADR-449](../decisions/ADR-449.md):109; taking it means deciding whether a live source line may be corrected when a merged entry reproduces it in full, which is a ruling this row did not have and did not invent.
