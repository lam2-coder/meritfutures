---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 263: the referrals screen renders, and the pending creative's disclosure is REQUIRED rather than attached

<!--index: **SC-M4-09 IS A DOCUMENT A BROWSER RENDERS.** `apps/portal/src/app/referrals/` is the portal's first App Router segment and `view/referrals.ts` finally renders. **THE COMPLIANCE CLAUSE IS ASSERTED ON THE EXACT CASE IT NAMES**: a `pending` creative whose row carries no `disclosure_version_id` renders the disclosure under "Disclosure required at approval", with the absence stated BEFORE the text, and `CreativeSubmissionView` has no field an attachment could be written into. **`next build` EXITS 0 AND `/referrals` IS A LIVE ROUTE**, which is not what `ADR-095` section 6's dated probe predicted: the premise *"next build exits 1 on Couldn't find any pages or app directory"* is discharged and the segment builds without session 250's layout. **`CI-06/gate-inventory` GOES RED AT 31 OF 32 BY DESIGN**: CI-07's artifact ARRIVED and the stage is `P4-i`'s file, so the gate is left failing rather than weakened. **FOUR FINDINGS REPORTED RATHER THAN RULED**: `tos_versions`' closed CHECK has no member for the NFA promotional disclosure, `view/disclosure.ts` cannot be loaded by `node --experimental-strip-types`, no contract row reads a creative back, and the segment is `.ts` rather than `.tsx` because `ADR-095` F7's tsconfig edit belongs to session 250. **`ADR-151`'s CONDITIONAL DID NOT FIRE and `151` is unspent.** 10 of 10 invariants, typecheck, lint and format clean, 151 files / 2,670 passed -->

- **Objective:** render `apps/portal/src/view/referrals.ts` as a real page under `apps/portal/src/app/`, and render the creative submission's `required_disclosure` as what `ADR-113` clause 2 says it is.
- **Done:** `apps/portal/src/app/referrals/` holds `page.ts`, `screen.ts` and `data.ts`; `view/referrals.ts` gains the creative-submission half; `apps/portal/test/app-referrals.test.ts` asserts the clause against RENDERED HTML at 12 assertions. **`pnpm --filter @merit/portal build` EXIT 0**, `Route (app) f /referrals`, and `next start` serves it. **Typecheck, lint, `format:check` clean; 151 files / 2,670 passed / 1 skipped; 10 of 10 repo invariants.** **`gates.mjs check` is 31 of 32 and is left that way**, section 4.
- **Next:** three things this fence does not hold. **`CI-07`'s stage**, which is `.github/workflows/ci.yml` and [STRATEGY](../testing/STRATEGY.md) section 4.1, both `P4-i`'s. **`app/layout.tsx`, `next.config` and `next-env.d.ts`**, session 250's, without which this screen has neither of the shell's two obligations. **The portal's API client**, without which `loadReferralScreenData` refuses.
- **Blockers:** **NOT MERGED, and it must not merge before session 250's layout.** A deployed `/referrals` without that layout carries neither `ADR-068` requirement 4's impersonation band nor `INV-M4-09`'s footer, both of which `shell/app-shell.ts` makes properties of the SHELL. The refusing data port is what makes that unreachable today rather than merely unlikely.
- **Landmines:** **`gates.mjs check` exits 1 on this branch and `falsify.mjs` reports two problems, all three the same CI-07 arrival** (section 4). **`view/referrals.ts` now carries a WIRE SHAPE**, `CreateCreativeResponse`, which belongs in `api/types.ts`; whoever moves it owns adding it to `api-types.test.ts`'s `TRANSCRIBED` list. **`toCreativeSubmission` is not re-exported from `src/index.ts`**, which `surface.test.ts` calls "a surface that exists and cannot be found"; that file is contended by six concurrent portal sessions and was not taken.
- **Files touched:** [`apps/portal/src/app/referrals/page.ts`](../../apps/portal/src/app/referrals/page.ts), [`apps/portal/src/app/referrals/screen.ts`](../../apps/portal/src/app/referrals/screen.ts), [`apps/portal/src/app/referrals/data.ts`](../../apps/portal/src/app/referrals/data.ts), [`apps/portal/src/view/referrals.ts`](../../apps/portal/src/view/referrals.ts), [`apps/portal/test/app-referrals.test.ts`](../../apps/portal/test/app-referrals.test.ts), [ALLOCATION](../decisions/ALLOCATION.md), [README](README.md), the generated span, this file.

---

### 1. THE CLAUSE, and the exact case it is asserted on

**Re-derived at source before anything was written, because a dispatch prompt is not a source.**

| # | Claim | Verified |
|---|---|---|
| 1 | **An APPROVED creative must carry a disclosure and a SUBMITTED one need not** | [`0005:140`](../../packages/db/migrations/0005_affiliate_program.sql): `CONSTRAINT affiliate_creatives_approved_has_disclosure CHECK (status <> 'approved' OR disclosure_version_id IS NOT NULL)` |
| 2 | **`disclosure_version_id` is nullable and references `tos_versions`** | [`0005:126`](../../packages/db/migrations/0005_affiliate_program.sql): `disclosure_version_id uuid NULL REFERENCES tos_versions(id) ON DELETE RESTRICT` |
| 3 | **`status` DEFAULTs to `pending`** | [`0005:117`](../../packages/db/migrations/0005_affiliate_program.sql) |
| 4 | **M08's sentence reads as a pin and is about a different moment** | [`M08:160`](../plans/M08-affiliate-system.md): *"Returns the current required disclosure text so the affiliate can attach it before submitting rather than after being rejected"* |
| 5 | **The contract field is `required_disclosure` and is SEPARATE from the creative** | [`API_CONTRACT:610`](../architecture/API_CONTRACT.md), and [ADR-113](../decisions/ADR-113.md) clause 2's *"so nobody reads it as a pin"* |
| 6 | **The response carries no `disclosure_version_id` AT ALL** | [`API_CONTRACT:601`](../architecture/API_CONTRACT.md) declares five fields on `creative` and the disclosure is not among them, and [`:604`](../architecture/API_CONTRACT.md) is the separate field |

**Every citation in the dispatch prompt checked out. None was wrong.**

**SO THE NULL IN THE DATABASE IS AN ABSENT FIELD ON THE WIRE, and that is what makes the clause assertable.** `toCreativeSubmission` has no branch that could produce an attachment because there is nothing in `CreateCreativeResponse` to read one from. The rendered proof is section 2.

---

### 2. What renders, with real data

`pnpm vitest run` over the real view model, `react-dom/server`, segment CSS elided in this paste only:

```html
<section class="mf-referrals__card" aria-labelledby="mf-referrals-creative">
<h2 id="mf-referrals-creative">Creative submitted for review</h2>
<dl class="mf-referrals__facts">
<div class="mf-referrals__fact"><dt>Reference</dt><dd>https://example.com/merit-eval-review</dd></div>
<div class="mf-referrals__fact"><dt>Kind</dt><dd>landing</dd></div>
<div class="mf-referrals__fact"><dt>Status</dt><dd>pending</dd></div>
<div class="mf-referrals__fact"><dt>Submitted</dt><dd>2026-08-27T14:02:11Z</dd></div>
</dl>
<section class="mf-referrals__required" aria-labelledby="mf-referrals-required" data-disclosure-state="required-at-approval">
<h3 id="mf-referrals-required">Disclosure required at approval</h3>
<p class="mf-referrals__lede">This submission does not carry a disclosure. Merit records the disclosure on a creative when the creative is approved, so a creative awaiting review carries none.</p>
<p>The review will require the disclosure below.</p>
<blockquote class="mf-referrals__quote">This communication is a paid promotion of Merit Futures. Trading involves substantial risk of loss and is not suitable for every investor.</blockquote>
<p class="mf-referrals__meta">Version 4, tos_versions 3d9b6c14-77ae-4b2f-9c08-51ad4e7f2c60. This is the version in force now. A review applies the version in force when the review happens, which may be a later one.</p>
</section>
</section>
```

**THE ASSERTION THAT MAKES THIS A CHECK RATHER THAN A SCREENSHOT** is that the disclosure text occurs **exactly once in the whole document**, and that occurrence is inside the region carrying `data-disclosure-state="required-at-approval"`. One occurrence is what turns a claim about a region into a claim about the screen: there is nowhere else it could be, the creative's own field list included. Beside it: the absence is stated before the text; the words `attached`, `Disclosure attached` and `Approved with` appear nowhere; `<dt>Disclosure</dt>` appears nowhere; and `CreativeSubmissionView`'s key list is asserted exactly, so a later author cannot add the field without the suite saying so.

**AND THE CHECK IS WATCHED FAILING**, on `RI-06`'s argument that a control seen only passing is a control nobody has seen work. The seeded render is the diff a real regression would be, which is a heading and a placement rather than a type change: the text moved into the creative's field list and the lede replaced with *"Disclosure attached."*. Under it the text occurs twice, `<dt>Disclosure</dt>` is present and the word `attached` is on the screen.

---

### 3. FOUR FINDINGS, REPORTED RATHER THAN RULED

| # | Finding | Why it is not taken here |
|---|---|---|
| **1** | **`tos_versions.document` HAS NO MEMBER FOR THE NFA PROMOTIONAL DISCLOSURE.** Its CHECK is closed at `'tos', 'privacy', 'risk_disclosure', 'affiliate_tos'` ([`0004:233`](../../packages/db/migrations/0004_catalog.sql)), and `affiliate_creatives.disclosure_version_id` can point only there. Meanwhile [`view/disclosure.ts`](../../apps/portal/src/view/disclosure.ts) brands SC-M4-09's disclosure `'content_documents'` and argues it at length. **So the corpus has two homes for disclosure text and the closed one admits no member named for what NFA I-26-12 requires a promoter to carry** | Resolving it is an ADR against `0005`, `0020`, [M08](../plans/M08-affiliate-system.md) and [M09](../plans/M09-marketing-site.md), and it changes nothing about how this screen must render: the two texts are DIFFERENT ARTIFACTS from DIFFERENT TABLES under every reading, and the screen renders them as two, never as one. A rendering session that ruled a schema question would be widening its fence to answer a question its own output does not depend on |
| **2** | **`apps/portal`'s SOURCE CANNOT BE LOADED BY `node --experimental-strip-types`.** [`view/disclosure.ts:80`](../../apps/portal/src/view/disclosure.ts) declares `constructor(readonly source: DisclosureSource)`, a TypeScript parameter property, and that mode refuses one: `SyntaxError [ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX]`. [ADR-083](../decisions/ADR-083.md) rules that every deployable runs that way, and this app's `start` script is `next start`, so nothing is broken TODAY | The fence says `view/disclosure.ts` may move only if rendering forces it and warns that session 262 reads the same file for the KYC screen. Rendering did not force it: this session simply did not add a second instance, and `MissingRequiredDisclosureError` assigns in the body with the measurement recorded above it |
| **3** | **NO CONTRACT ROW READS A CREATIVE BACK.** `POST /affiliate/creatives` is the only row and there is no `GET`, so the only moment `required_disclosure` exists is the submission itself. **M08 section 4's stated product goal, seeing the requirement *"before submitting rather than after being rejected"*, is unreachable from a read surface.** [ADR-113](../decisions/ADR-113.md) section 5 recorded the OPERATOR half as owed; the trader-side READ is owed too and nobody had said so | Adding a contract row is an ADR against an `approved` document, which is what `ADR-113` itself was. The screen states the absence (*"No creative has been submitted for review."*) and shows no disclosure text at all in that state, because inventing a required disclosure at the point of render is precisely what `view/disclosure.ts` exists to make impossible |
| **4** | **THE SEGMENT IS `.ts` AND NOT `.tsx`.** `apps/portal/tsconfig.json` sets neither `jsx` nor a `dom` lib and includes `src/**/*.ts`, so a `.tsx` file here is one `tsc --noEmit` never reads, and a `.ts` file importing one fails to compile. [ADR-095](../decisions/ADR-095.md) F7 puts both options with the first page, which is session 250's | Editing that file would collide with the session that owns it, and P4 section 11 rule 13 makes widening a fence and weakening a gate the same move. **The cost is readability and the purchase is every check**: each page file is inside `tsc`, inside `eslint`'s `apps/**/*.ts` block, inside `inv-m4-01.test.ts`'s money scan and inside `surface.test.ts`'s no-transport walk, none of which reads a `.tsx`. Converting is mechanical and is named in the file for the author who lands the options |

---

### 4. `CI-06/gate-inventory` IS RED AND IS LEFT RED

```
FAIL   CI-06/gate-inventory  Every pipeline stage row is implemented in a workflow or waits on a dated artifact  (1)
       docs/testing/STRATEGY.md:190 CI-07: the artifact "a page, layout or route file under apps/*/src/app/"
       HAS ARRIVED (apps/portal/src/app/referrals/page.ts is an App Router file), so the row reopened on the
       commit that added it and the stage is not written. This is the half of ADR-073's ruling that fails on
       good news, and it is the whole reason the condition is a condition rather than the word "deferred".
       Write the stage, or take a ruling that moves the row
```

**This is the gate working exactly as [ADR-073](../decisions/ADR-073.md) designed it and exactly as [ADR-095](../decisions/ADR-095.md) section 6 predicted, on the commit that delivers the artifact.** [Session 225](2026-08-26-session-225.md) met the same wall from the site's side, seeded a page, watched the gate go red, and removed the seed rather than widening its fence.

**Neither remedy is inside this fence.** The stage names a workflow file under `.github/workflows/` and a top-level job key in it, which is `P4-i`'s cell, and the row itself is [STRATEGY](../testing/STRATEGY.md) section 4.1's. **So the gate is left failing and this entry says so in the place a reader looks**, rather than the row being moved, the probe narrowed or the file deleted.

**MEASURED IN BOTH DIRECTIONS SO THE ATTRIBUTION IS NOT A GUESS.** With `apps/portal/src/app` moved aside on this exact tree, `gates.mjs check` reports every gate passing; restored, it reports one failure and it is this one. Nothing else in this diff moves a gate.

**`falsify.mjs` REPORTS TWO PROBLEMS AND BOTH ARE THIS ONE.** Its clean-tree leg requires every gate to pass, which this one now does not; and its seeded case for `CI-06/gate-inventory`, whose seed is literally *"an activation condition's artifact ARRIVING: an App Router file under apps/\*/src/app/ reopens CI-07"*, now reports `FAILED OFF-TARGET` because the gate was already red before the seed was planted. **The same runner on `origin/main` at this session's base reports every gate passing clean and failing dirty.** A falsifier that cannot tell its own seed from the tree is a falsifier reporting honestly that the tree already contains its seed.

---

### 5. `next build` EXITS 0, WHICH IS NOT WHAT THE PROBE PREDICTED

[ADR-095](../decisions/ADR-095.md) section 6 executed the probe rather than arguing it and recorded the result:

> ```
> Error: > Couldn't find any `pages` or `app` directory. Please create one under the project root
> ```

**That premise is discharged by this commit.** `pnpm --filter @merit/portal build` exits 0 with this segment and no root layout, no `next.config` and no `tsconfig` change:

```
✓ Compiled successfully in 3.9s
  Running TypeScript ...
  Finished TypeScript in 1263ms ...
✓ Generating static pages using 3 workers (2/2) in 90ms

Route (app)
┌ ○ /_not-found
└ ƒ /referrals
```

**`f /referrals` IS `dynamic = 'force-dynamic'` DOING ITS JOB AND IT IS LOAD BEARING TWICE.** This route reads one identity's commission figures, so there is no build-time answer for it; and opting out of prerendering is what keeps `next build` from executing the refusing data port and failing the build on the unwired state, which would have handed session 250 a red build it did not cause.

**THE ROUTE WAS SERVED AND WATCHED REFUSING**, because a build that compiles is not a screen that behaves. `next start`, then `GET /referrals`:

```
HTTP 500
⨯ Error [ReferralDataUnwiredError]: the referrals screen has no data source. apps/portal declares wire
  types and no transport ... It needs GET /affiliate/stats and, where one exists, the POST
  /affiliate/creatives response ... and the NFA I-26-12 disclosure text, which no contract row serves to
  the portal at all. Rendering seeded figures instead would state commission amounts Merit never
  computed, on the one screen M08 AS-M8-04 is about.
```

**THE REFUSAL IS THE HONEST HALF OF SHIPPING UNWIRED and it is also the control that makes section 6 safe.** The tempting alternative was seeded figures so the route renders today, and a referral screen showing invented commission amounts is a screen that looks finished and states money Merit never computed, on exactly the surface `AS-M8-04` is about.

**`next build` WRITES `apps/portal/next-env.d.ts` AND `.next/`, AND NEITHER IS COMMITTED.** `.next` is already ignored; `next-env.d.ts` is not, and it belongs with `next.config` in session 250's scaffold along with the `.gitignore` line it needs. It was removed and the tree is clean. **The build also phoned home**, which is [ADR-095](../decisions/ADR-095.md) F5 exactly as foreclosed: `NEXT_TELEMETRY_DISABLED=1` or a `next.config` line, both owed by the session that writes the config.

---

### 6. What this session did NOT do

- **NO ADR. `ADR-151`'s conditional did not fire and `151` is unspent**, on [session 225](2026-08-26-session-225.md)'s discipline that a session which mints no ADR records that rather than leaving a reserved number looking spent. Its row is amended in place per [ADR-065](../decisions/ADR-065.md) T3, quoting the reservation it replaces. **Rendering forced no ruling**: `ADR-113` had already ruled the only question this screen could have reopened, and section 3's four findings are each a question whose answer this screen does not depend on. The [ALLOCATION](../decisions/ALLOCATION.md) row records that rather than leaving a reserved number looking spent.
- **NO MIGRATION AND NO MIGRATION NUMBER.** `0048` stays free.
- **`apps/portal/package.json` IS UNTOUCHED**, session 250's, and so are `tsconfig.json`, `src/index.ts`, `src/api/types.ts` and `view/disclosure.ts`.
- **NO ROUTE HANDLER AND NO SERVER ACTION.** The segment holds one `page` file, exports no `'use server'` function and contains no `fetch(`, `NextRequest`, `NextResponse` or the string `/api/v1`, asserted over all three files rather than promised in a comment. [ADR-095](../decisions/ADR-095.md) ruling 3 and [ADR-083](../decisions/ADR-083.md) section 3.
- **NO FLOAT.** Zero float literals in expression position across the three segment files and `view/referrals.ts`, comments and the CSS template literal stripped; zero occurrences of `toFixed`, `parseFloat`, `Number(`, `Math.round` or a division by 100 anywhere in the diff. Every decimal that reaches the screen is a STRING produced by [`format/money.ts`](../../apps/portal/src/format/money.ts) before this segment sees it, and the suite asserts the rendered set is exactly the formatter's five.
- **NO NEW DEPENDENCY.** React and Next are [ADR-095](../decisions/ADR-095.md)'s admission; `react-dom/server` is the same package.
- **NO `CI-06` LETTER, NO GATE REGISTER ROW, NO `VG` ROW, NO `RI-nn`, NO `OI-nn`, NO FIXTURE, NO EDGE CASE.**

---

### 7. `origin/main` MOVED UNDER THIS BRANCH AND THE CONFLICT WAS IN THE GENERATED SPAN

`main` went from `c947e39` to `3f32575` while this branch was open, carrying sessions 236, 241, 242, 244, 246, 248 and 249. **One conflict, and it was in [`sessions/README`](README.md)'s ENTRY table**, which [ADR-088](../decisions/ADR-088.md) made a generated span: seven rows arrived where this branch had appended one.

**IT WAS RESOLVED BY RE-RUNNING THE GENERATOR AND NOT BY HAND**, which is what a generated span is for. `gates.mjs generate` rewrote the span from the entry files themselves and reports `every span already matches its query` on the next run, so the bytes are the generator's rather than a merge resolution that happens to look right. **The CLAIM table above it took no conflict**, which is [ADR-088](../decisions/ADR-088.md)'s own reason for refusing to generate it working in the other direction: a claim row is edited by the session that owns it and by nobody else.

`ALLOCATION` auto-merged. **Measured rather than assumed:** the only difference this branch carries against `main` in either file is row `151` and the session 263 entry.

**THE MERGE DOES NOT CLEAR SECTION 4 AND THAT WAS CHECKED RATHER THAN HOPED.** [STRATEGY](../testing/STRATEGY.md) moved on `main` for session 244's and 246's reasons; `CI-07`'s row at `:190` is unchanged and still carries no implementation, so `CI-06/gate-inventory` fails on the merged head for the same reason and at the same count. **Session 250 has not landed.**

**Re-run on the merged tree:** 10 of 10 invariants, typecheck, lint and `format:check` exit 0, **151 files / 2,670 passed / 1 skipped**, generator idempotent, gates 31 of 32 with the same single failure.
