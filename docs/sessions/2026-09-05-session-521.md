---
status: approved
depends_on: [README.md]
last_updated: 2026-09-05
---

## 2026-09-05 - Session 521: a sweep that stops at `scripts/`, and the seventh occurrence sitting in the directory it does not read

<!--index: **THE BUILD IS [ADR-330](../decisions/ADR-330.md), `proposed` and UNSIGNED, and it is an AMENDMENT TO `RI-35` in [`absence-claims.mjs`](../../packages/tooling/checks/absence-claims.mjs) rather than a new invariant: 34 of 34 before and 34 of 34 after, no number spent.** **NOT THE MONEY PATH AND NOT A MIGRATION**: `git diff --stat 7fa27c93..HEAD -- packages/db/migrations scripts/db` is EMPTY, no SQL, no `SD-nn`, no `CI-06x` gate. **`E2` READ OWED ANYWAY**, because the diff changes what a control asserts about eight live absence claims, one of which is the refusal `GET /affiliate/stats` serves; four reads are inherited. **THE DEFECT IS A BLIND SPOT [ADR-328](../decisions/ADR-328.md) NAMED IN ITS OWN APPROVAL BLOCK AND A ROW FELL INTO ONE LATER.** Leg 6 swept `apps/*/src` and `packages/*/src`; occurrence 4 lived under `scripts/db/` and was registered BY HAND for exactly that reason; [ADR-329](../decisions/ADR-329.md) finding 1 then found the **SEVENTH** occurrence at [`data-model-columns.mjs:56`](../../scripts/corpus/data-model-columns.mjs), a clause saying `gates.mjs` CANNOT BE IMPORTED that is **false in BOTH halves** -- `gates.mjs:9118` exports `GATES` and `:9324-9327` guards its own invocation -- falsified by `55824c62` on 2026-08-30 and standing for six sessions. **EVERY PART OF THE SCOPE WAS MEASURED BEFORE IT WAS CHOSEN**: the whole of `scripts/`, `.sql` INCLUDED, minus any directory named `test`, which is **+50 files and +33,992 lines** over a 338-file, 150,916-line shipped scope for **388 files and 184,908 lines** swept. `.sql` is IN because occurrence 4 was that shape and a scope reaching the directory but not the file shape reproduces the blind spot one level down; **the lexer objection is answered by measurement rather than waved away**, because this sweep parses NOTHING and `RI-30`'s single stripper is still not imported for the reason the check's own header already gives. A whole directory rather than a written list of subdirectories, on [ADR-329](../decisions/ADR-329.md) section 7's rule about registers somebody has to keep. **THE PROBES DID NOT MOVE WITH THE SWEEP**: `sweptSources()` is a second function beside an untouched `shippedSources()`, because widening that walk in place would have retired three live claims by editing a file walk. **THREE CLAIMS SURFACED, UNDER THE ROW'S OWN STOP-AND-REPORT THRESHOLD**: the seventh occurrence, **repaired with a derivation and now `retired`** under `RI-14`, and two quoting somebody else's thrown error text -- pg-boss's and PostgreSQL's -- registered **`unbindable`** rather than narrowed away. **NOTHING TRUE WAS DELETED**: both stand byte for byte. **THE FALSIFIABILITY TEST IS TWO COMMITS**: `aef3bfc7` is **RED at `RI-35`, 33 of 34**, `2d8e20a3` is the repair at **34 of 34**, and leg 6 is separately watched reporting the site by DISCOVERY. **FOUND AND NOT REPAIRED, AND IT IS THE WORK THIS ROW CREATED**: the duplication argument the false clause propped up is now a deferral with **no blocker** and its remedy clause is unclaimed, while [ADR-329](../decisions/ADR-329.md) added a third reader of the same migration set -- named as owed, parsers untouched. **Nothing was wired and nothing was scheduled**: the counts do not move at ten wired of twenty-four declared, fourteen blocked. Counts derived at reporting time: suite **309 files / 7,564 passed / 6 skipped / 0 failed**, against a `7fa27c93` baseline reproduced BEFORE the first edit of **309 files / 7,551 passed / 6 skipped / 0 failed**; the delta is **+13 cases and +0 files**, all thirteen in this check's existing suite. Invariants **34 of 34**, unchanged; gates **33 of 33** after `generate`, unchanged; lint, `format:check` and typecheck clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `330`: widen `RI-35`'s leg 6 sweep to `scripts/`, dispose of everything the widening surfaces, and repair the seventh occurrence with a derivation.

**One objective.** The build is one scope change. **The work is the measurement**: what the widened scope costs, what it surfaces, and whether each thing it surfaces is a claim or a quotation.

---

### 2. The base and the baseline, both derived before the first edit

`7fa27c93`, PR #683's head, which carries `main` plus [ADR-329](../decisions/ADR-329.md) with `RI-36`. **The base being ahead of `main` is deliberate and was not "fixed"**: `main` carries through [ADR-328](../decisions/ADR-328.md) with `RI-35`, so `330` opens no `CI-06f` hole.

**There are 34 invariants and 33 gates on this ref**, both read off their own runners' last lines before anything was edited rather than taken from the dispatch.

**THE DISPATCH'S THREE QUOTED SOURCE LINES WERE EACH RE-READ AT THEIR FILES AND ALL THREE HELD.** `data-model-columns.mjs:56` carries the clause verbatim; `gates.mjs:9118` reads `export const GATES = [`; `gates.mjs:9324-9327` carries the `invokedDirectly` guard under a comment reading *"Importable by the suite that reads this report, runnable by CI-06."* **A fourth fact was derived rather than given**: `git log -S invokedDirectly` dates the guard to `55824c62`, 2026-08-30, which is [ADR-294](../decisions/ADR-294.md)'s row.

**Baseline, off each runner's own last line, before a line was changed:** `pnpm vitest run` **309 files, 7,551 passed, 6 skipped, 0 failed**; invariants **34 of 34**; gates **33 of 33**; `git status --porcelain` empty.

---

### 3. What landed

1. **The widened sweep in [`absence-claims.mjs`](../../packages/tooling/checks/absence-claims.mjs)**: `scriptSources()` and `sweptSources()`, the second scope beside an untouched `shippedSources()`. One new absence marker, `/cannot be imported/i`, which is the seventh occurrence's own words and reaches exactly two lines. One new artifact, `gates-importable`, whose probe reads BOTH halves the false sentence claimed and whose needles name the ARTIFACT rather than the file, measured.
2. **Three register entries**: the seventh occurrence, and two `unbindable` with reasons.
3. **The seventh site repaired** in [`data-model-columns.mjs`](../../scripts/corpus/data-model-columns.mjs), deriving from the file it names at line numbers, with the retired sentence quoted beside its correction under `RI-14`.
4. **Thirteen cases** in [`absence-claims.test.ts`](../../packages/tooling/test/absence-claims.test.ts): the reconstruction, its counterfactual, the probe's both-halves rule, leg 6 by discovery, the same bytes one directory over watched silent, `.sql` in, `test/` out, the new sentinel, and `queue-door`'s probe watched not moving.
5. **Two fixture repairs** in [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts), both FIDELITY rather than exemption and both found by a red suite rather than anticipated.

**THE COMMITS ARE THE FALSIFIABILITY RECORD.** `aef3bfc7` carries the widened sweep and the register row and is **RED at `RI-35` by construction, 33 of 34**; `2d8e20a3` is the repair at **34 of 34**; `287b2804` is the suite. The branch history carries the red rather than only quoting it.

---

### 4. What was measured and NOT taken

- **`scripts/corpus` plus `scripts/db` instead of the whole directory.** Saves 14 files and 5,828 lines, surfaces nothing: `scripts/ci` and `scripts/demo` produce zero findings. Refused because a written list of subdirectories is a register somebody has to keep.
- **`/\bgates\.mjs\b/` as the artifact's needle.** Reaches **84 lines** over the widened scope, 32 of them in `falsify.mjs`, and produces a fourth finding at `falsify.mjs:3217` that asserts nothing about this tree. Refused on [ADR-328](../decisions/ADR-328.md)'s own rule that a needle which mostly names other things registers noise.
- **Tightening the `not installed` marker to miss a backticked quotation.** Refused because the tightening available would blind the vocabulary to occurrence 2's own sentence, whose only structural difference is the punctuation after the word. The line is `unbindable` instead, where a reader can argue with it.
- **Restructuring the parsers.** Named as owed and left alone. See below.

---

### 5. Landmines

1. **THE DUPLICATION ARGUMENT IS NOW A DEFERRAL WITH NO BLOCKER AND NOBODY HOLDS THE ROW.** `data-model-columns.mjs`'s header justified a second SQL parser by the import being impossible. The import is possible; the remedy clause it wrote for itself, *"export the one in gates.mjs and delete the copy below"*, is available and unclaimed. **And the consequence got bigger one row ago**: [ADR-329](../decisions/ADR-329.md) added `foldColumnNullability` at `repo-invariants.mjs:8467`, a third reader of the same migration set. Collapsing any of them changes what `CI-06i`, `data-model-columns.mjs` and `RI-36` each assert. **This should be an allocated row rather than a finding at the bottom of an ADR**, which is what [ADR-326](../decisions/ADR-326.md) section 8 finding 1 was before it became [ADR-327](../decisions/ADR-327.md).
2. **`RI-35` STILL SWEEPS ONLY REGISTERED NEEDLES.** An absence claim about an artifact nobody registered is as invisible today as yesterday. This row closed the SCOPE half of ADR-328's approval-block worry and left the DISCOVERY half exactly where it was.
3. **THE SWEEP NOW READS 184,908 LINES WITH NO LEXER, AND TWO OF THE THREE THINGS IT SURFACED WERE QUOTATIONS.** That ratio is the warning. As the scope grows the fraction of findings that are quoted text grows with it, and each lands in the register as an entry that decides nothing. The failure mode is not a false green, it is a register nobody reads.
4. **`data-model-columns.mjs` is still unregistered as a gate**, which is [ADR-329](../decisions/ADR-329.md) finding 2. Unchanged.
5. **Unchanged from the rows before this one**: [`schedule.ts:220`](../../apps/worker/src/schedule.ts)'s reachability clause; [ADR-305](../decisions/ADR-305.md) slice 8's stale citation; `0080` and `0081` carrying no `DELTA_MANIFEST`; `identity_restriction_episodes.sla_due_at` invisible to `CI-06l`; `purchases.md`'s *"three and not one"* sentence; `wallet_entries.md` carrying no `Constraints:` line.

---

### 6. Next

The founder's `E2` read on [ADR-330](../decisions/ADR-330.md) and on the four inherited, all `proposed` and all UNSIGNED. Then the parser row in landmine 1, allocated rather than left as a finding.

---

### 7. Files touched

[`packages/tooling/checks/absence-claims.mjs`](../../packages/tooling/checks/absence-claims.mjs), [`packages/tooling/test/absence-claims.test.ts`](../../packages/tooling/test/absence-claims.test.ts), [`packages/tooling/test/repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts), [`scripts/corpus/data-model-columns.mjs`](../../scripts/corpus/data-model-columns.mjs), [ADR-330](../decisions/ADR-330.md), [ALLOCATION](../decisions/ALLOCATION.md) (row `330` and the `RI-35` row), this log, [STATE](../STATE.md) and the two generated registry READMEs.

**[`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) was NOT edited**, so no citation into it could go stale. **`packages/db/migrations` and `scripts/db` show a 0-line diff.**
