---
status: approved
depends_on: [README.md]
last_updated: 2026-08-23
---

## 2026-08-23 - Session 141: 102 `0015` citations checked, three stale, and `OI` gets the one allocation site `ADR-074` ruled it

- **Objective:** audit every citation of a `0015` constraint against the migration directory for a superseding definition, and rule `OI-27` to one allocation site.
- **Done.** Both. **102 citation sites checked, 99 resolved and 3 stale**, all three in [`apps/worker/test/replay.test.ts`](../../apps/worker/test/replay.test.ts) and none of them repairable from this fence. **The method generalises and it finds one more**, in [`docs/architecture/data-model/README.md:376`](../architecture/data-model/README.md). **`OI-27` is closed**: [ALLOCATION](../decisions/ALLOCATION.md) gained the fourth table [ADR-074](../decisions/ADR-074.md) section 7 ruled, and [`DELTA_MANIFEST` section 16](../../packages/db/DELTA_MANIFEST.md) points at it rather than duplicating it.
- **The count:** `node scripts/corpus/gates.mjs check` **28 of 28**, `node scripts/corpus/falsify.mjs` **green over 55 scope cases and 10 loader cases**, `pnpm vitest run` **101 files / 1,417 passed / 1 skipped**, `pnpm run format:check` clean. **The suite is unmoved by this session and the numbers are NOT [session 135](2026-08-22-session-135.md)'s 1,410 and 45**: [session 137](2026-08-22-session-137.md) retired the pre-`ADR-048` block, which was 43 of those 45 skips, and 133, 134, 136 and 139 landed between. Quoted from this run rather than carried forward, which is the same failure this session spent its day auditing. **No gate, no migration, no ADR.**
- **Next:** four repairs, none of them this fence's, listed under **Reported, not repaired** below. The largest is not the count.
- **Blockers:** none.
- **Landmines:**

  - **THE GREP SESSION 135 RAN WAS THE RIGHT ONE AND IT IS NOT THE ONE THAT WAS MISSING.** Its landmine reads *"the mandated grep was run before any `0015` constraint was cited: `rule_states_consistency_period_started` is defined once, at `0015:193`, and nothing supersedes it"*. **That was true when it was written and its own migration then made it false.** The mandated grep is for a SUPERSEDING migration BEFORE citing. **The grep nobody has ever run is for EXISTING citations AFTER superseding**, and it is the whole of the residue this audit found: `0037` left two behind on 2026-08-17 and `0046` left one behind yesterday. A supersession is two sweeps and the corpus has only ever mandated the first.

  - **ONE OF THE THREE IS INVERTED RATHER THAN MERELY STALE, AND THAT IS THE ONE WORTH READING.** [`replay.test.ts:768`](../../apps/worker/test/replay.test.ts) says `rule_states_high_water_bounds_balance` *"rejects every later row whose balance exceeds the lock-day close"*. `0037`'s replacement is `floor_locked OR high_water_balance_cents >= balance_cents`, so **a locked row is exactly the row the live constraint EXEMPTS**. The comment is not out of date about a name; it states the opposite of what the database does, and it is the load-bearing justification for why that test's 250-day stream is capped at 219,000c of profit. **This is [session 129](2026-08-22-session-129.md)'s second money-path finding, still standing in the file that produced it** after [session 132](2026-08-22-session-132.md) refuted it in the plan, in [STATE](../STATE.md), in [INDEX](../INDEX.md) and in [`ADR-079`](../decisions/ADR-079.md). Five documents were corrected and the code comment was not, which is the direction nobody sweeps.

  - **`docs/architecture/data-model/` IS IN THIS FENCE AND THE GENERALISED FINDING WAS DELIBERATELY NOT REPAIRED.** The brief says *"if you find the same shape against another migration, report it; do not widen the repair"*, so it is reported at full detail and left. **It is a two-line correction that needs no ruling and whose replacement text already exists** one file away, at [`daily_marks.md:26`](../architecture/data-model/daily_marks.md). Naming it here rather than taking it, because the grant is a sentence from the founder and a widened audit is not.

  - **`ADR-074` SECTION 7 PREDICTS A GATE FAILURE THAT DOES NOT HAPPEN, AND IT WAS RUN RATHER THAN BELIEVED.** It states *"the day that table lands this entry stops naming a defect and the gate fails until `OI` is promoted in the same commit"*, and `scripts/corpus/` is outside this fence, so on the ruling's own reading the table was unlandable here. **`CI-06/identifier-series` passes.** Its pending assertion fires only when EVERY member of a pending series has exactly one definition site, and **seven of the twenty-nine had none at all** because STATE defines `OI-17` to `OI-23` with a bold lead, which ADR-074 itself rules is not a definition site. **28 of 28 before the table and 28 of 28 after it.** Had the prediction been taken at face value the session would have reported the item as blocked.

  - **`OI-19` AND `OI-20` ARE EACH ALLOCATED TWICE AND NOTHING IN THE CORPUS HAD EVER COUNTED IT.** `OI-06`'s double claim is on record in three places. These two are not: sessions 105 and 106 each took both numbers on 2026-08-20, [WAVE-04 section 6](../plans/WAVE-04-fixture-backlog-and-gate-inventory.md) renumbered session 106's pair to `OI-25` and `OI-26` *"in this plan and everywhere after it"*, and **STATE is append-only, so session 106's two paragraphs still carry the old numbers and always will.** Found by building the allocation table, which is the argument for the table.

  - **THE THREE STALE CITATIONS ARE `OI-30`, WHICH IS THE FIRST NUMBER TAKEN FROM THE NEW TABLE**, and it has exactly one definition site: its ALLOCATION row. The finding is written here and in STATE **without leading a heading or a first cell with the identifier**, deliberately, because that is what would mint a second site and repeat `OI-17` to `OI-23`'s defect in the commit that names it.

- **Files touched:** [ALLOCATION](../decisions/ALLOCATION.md) (the fourth table, and the one line that says how many tables the file has), [`DELTA_MANIFEST`](../../packages/db/DELTA_MANIFEST.md) (section 8's lead, section 16's `OI-nn` pointer, the `OI-27` row), [STATE](../STATE.md) (one appended section), [README](README.md), this file. **No migration, no ADR, no gate, no fixture, and `docs/architecture/data-model/` was read and not written.**

### The audit, and both numbers

**A citation is one `file:line` outside `0015` itself naming one of the eight constraints `0015` defines, or pointing at one by `0015:<line>`.** The set is a reproducible grep and the unit is mechanical rather than judged. **102 sites over 28 files.** Collapsing multi-line quotations -- a `sql` block and the sentence introducing it are one claim -- gives **95 claims and the same three stale**.

**Two of `0015`'s eight constraints are retired.** `rule_states_high_water_bounds_balance` by [`0037`](../../packages/db/migrations/0037_supersede_rule_states_high_water_bounds_balance.sql) on 2026-08-17, `rule_states_consistency_period_started` by [`0046`](../../packages/db/migrations/0046_supersede_rule_states_consistency_period_started.sql) on 2026-08-22. **The other six are live**, so every citation of them resolves by construction and 8 of the 102 are that.

| Disposition | Sites |
|---|---|
| **Dated record** -- a session log, an ADR, an ALLOCATION row, an append-only STATE section, a plan's dispatch brief | 67 |
| **Supersession machinery** -- `0037`, `0046` and their two probes, where the retired name appears in order to retire it or assert its absence | 20 |
| **Live constraint** | 8 |
| **The string rather than the constraint** -- `CI-06s`'s near-miss, where `corpus.yml` names the retired constraint in a comment three lines above the probe step and a loose parser reads that as coverage | 4 |
| **STALE** | **3** |

**The dated-record class is 67 of 102 and it is the class most likely to be argued with, so the rule is stated rather than assumed.** A session log, a signed ADR and an append-only STATE section are records of a position on a date. [Session 129](2026-08-22-session-129.md)'s finding is stale as a claim about today's tree and **correct as a record of what that session found**, which is why session 132 refuted it in a new entry instead of editing the old one, and why [ADR-052](../decisions/ADR-052.md) still quotes `0015:208` verbatim four rulings later. **Rewriting them destroys the only thing they are for.** The test applied throughout is whether a reader who wants to know what the database does today would land on the sentence: a `## Session 106` heading dates its own contents and a code comment does not.

**And the class that is NOT a dated record is the one that found everything.** [`floor.ts:138`](../../packages/rules-engine/src/day/floor.ts) cites the same constraint at the same line as the three stale sites and **resolves**, because it is tensed: *"`0015_rule_states.sql:208`'s `rule_states_high_water_bounds_balance` **rejected** such a row **when ADR-052 was written**"*. One code comment carries its own date and three do not, in two files, on the same constraint. **That is the whole difference and it is free.**

### The method generalises, and the seed is derivable

**Retired is mechanical.** A constraint name a migration `DROP`s and no migration `ADD`s back. Over the whole directory: **16 `DROP CONSTRAINT` statements, 9 of which re-add the same name inside the same migration, leaving 7 retired names** across `0029`, `0036`, `0037` and `0046`. All seven were swept, not only `0015`'s two.

| Retired | By | Outside dated records |
|---|---|---|
| `contact_channels_kind_check`, `identity_signals_kind_check`, `notification_kinds_class_check`, `kyc_verifications_verification_purpose_check` | `0029`, each to a `*_allowed` name | **All resolve.** Every citation states the drop and the replacement in one sentence, which is the shape the other three should have had. One of the four is cited nowhere at all |
| `daily_marks_balance_arithmetic` | `0036`, to `daily_marks_inv19_closing_identity` | **ONE STALE**, below |
| `rule_states_high_water_bounds_balance` | `0037` | 2 stale |
| `rule_states_consistency_period_started` | `0046` | 1 stale |

**The nine same-name re-adds are a second and weaker shape and this audit does not close it.** `trading_calendar_session_ordered` and eight others were dropped and re-added under their own names with changed predicates, so the NAME resolves for every citation while the PREDICATE moved underneath it. That is not name-greppable and it is not what was asked for; it is named here so the next audit does not think it was covered.

### The one the generalisation found

**[`docs/architecture/data-model/README.md:376`](../architecture/data-model/README.md) is a row in the live invariant-to-enforcement table and BOTH of its halves were falsified on 2026-08-16.**

```
| **`closing = opening + realized_pnl + adjustment`** (INV-18) | `daily_marks_balance_arithmetic`, checkable only because `SD-01` exists |
```

**Its sibling document already says so.** [`daily_marks.md:32`](../architecture/data-model/daily_marks.md) records that the label was wrong, that `closing = opening + realized_pnl + adjustment` *"appears nowhere in M01"*, that `INV-18` is the OPENING identity and `INV-19` the closing one, and that the constraint *"added the adjustment a second time, inside the day"* and refused the mark for **every settled payout**. [EC-157](../edge-cases/EC-157.md) Repair A ruled it and `0036` merged it. **`daily_marks.md`'s Constraints line was repaired and the enforcement table one directory up was not**, so the corpus states the repaired identity in the table of one and the falsified identity in the table of all.

**This is the audit's shape exactly and on a different migration**, which is what the brief asked to be told rather than fixed.

### The rule a gate could read

> **A retired constraint name may appear only where the appearance retires it or records it.**

**The seed is fully derivable from `packages/db/migrations/*.sql`**: `{names in DROP CONSTRAINT} \ {names in ADD CONSTRAINT}`, which is seven today and grows by itself with every supersession. **The permitted sites are four**: the migration that defined it, the migration that dropped it, a `scripts/db/probe_*.sql` asserting its absence, and a dated record (`docs/sessions/`, `docs/decisions/ADR-*.md`). Every other appearance is a finding.

**It is `CI-06/retired-constraints` and it takes a SLUG, not a letter**, per [ADR-065](../decisions/ADR-065.md) and `OI-17`: 23 of the 24 usable `CI-06` letters are spent and `falsify.mjs` needs two letters of headroom for its `CI-06p` seeds. **It ships with a dated register of the four findings above, which can only shrink**, on `CI-06u`'s shape, or it ships blocking once they are repaired. **Its scope is written and never computed**, per [ADR-074](../decisions/ADR-074.md) section 2, because an exemption set derived from what currently passes passes forever.

**Two things it would not do**, so nobody counts them as covered: the nine same-name re-adds above, and whether a citation of a LIVE constraint describes it correctly. It asserts existence, never content.

**Not written. `scripts/corpus/` is outside this fence.**

### `OI-27`: the ruling existed and the table did not

**[ADR-074](../decisions/ADR-074.md) section 7 ruled this on 2026-08-21**: *"`OI`'s allocation moves to ALLOCATION as a fourth table, superseding `DELTA_MANIFEST` section 16"*, with the move assigned to *"a session that holds both in its fence"*. **This is that session and nothing new was ruled.** Section 16's `OI-nn` half now carries a pointer and allocates nothing; **its rows stay**, because they are the only copy of several findings and because ALLOCATION carries the number while the manifest carries the finding, which is section 7's own distinction. **The section-number table is untouched**: section numbers really are the manifest's own namespace and always were, which is the half of section 16's original argument that survives.

**`OI-06`'s double claim was section 7's to dispose of and it is disposed by the manifest's own standing ruling**, not by a new one: cite with the subject attached. All six multiply-claimed rows lead with a distinct first cell, so they are six keys to `CI-06u` and every citation can say which item it means. **Nothing is renumbered**, and on `OI-19` and `OI-20` a renumber has already been attempted once, by WAVE-04, and STATE could not follow it.

### Reported, not repaired

- **The three `replay.test.ts` citations**, `OI-30`. `apps/worker` is outside this fence. `:768` is the inverted one and is the one to read first.
- **[`docs/architecture/data-model/README.md:376`](../architecture/data-model/README.md)**. **Inside this fence and left on the brief's instruction.** Two lines, no ruling, replacement wording already written at `daily_marks.md:26`.
- **`CI-06w` extended to a fourth table**, which is what would have caught [session 120](2026-08-21-session-120.md): WAVE-04's `OI-25` claim and session 120's would have been two rows over one key in one file. A fourth entry in a list the runner already walks.
- **`OI` is still in `PENDING_SERIES`** and promoting it means reconciling thirty members to one definition site each. **Measured: six have exactly one and twenty-four have two or more.** That is a session, and the table this one landed is the allocator it will promote `OI` onto.
- **[`P2-rules-engine-build`](../plans/P2-rules-engine-build.md) records no session as landed**, so its `P2-c` brief still reads as pending work against a constraint `0046` retired yesterday. Five of its eight `0015` citations are that brief. It is a dispatch document and out of this fence, and a session dispatched from it for `P2-d` or later would read section `P2-c` as owed.
