---
status: approved
depends_on: [README.md]
last_updated: 2026-08-18
---

## 2026-08-18 - Session 72: ADR-059 frames the three group A input questions, and two of them were already answered in a place the corpus does not read (planning)
<!--index: ADR-059 proposed: the three group A input questions, two already answered in test code and one with no source anywhere -->

- **Done:** **[ADR-059](../decisions/ADR-059.md) is written, `proposed`, with an unsigned founder-approval line.** It states the group A gap as three separate questions, derives each one's blast radius by `git grep` rather than by estimate, and checks for each whether the VALUE it needs exists. **Two of the three are already decided and the decision is in the wrong place**: [`rule-coverage.ts:120`](../../packages/rules-engine/test/rule-coverage.ts)'s `DISCHARGED_ELSEWHERE` holds `R-01` and `R-05` with stated reasons and [`implemented-rules.test.ts:57`](../../packages/rules-engine/test/implemented-rules.test.ts) asserts the count, while [M01](../plans/M01-rules-engine.md) lines 421 and 425 still list both in the same table as the forty-six the engine computes, with the `Config` and `Arithmetic` columns filled in. **The third has no source anywhere in the tree.** `R-06` is excluded, named, and left alone: `GS-035` needs a clock and `INV-01` forbids one. The ALLOCATION row was committed first as a bare reservation and is linked now that the entry exists, on `ADR-055`'s precedent. **`node scripts/corpus/gates.mjs check`: 18 of 18.** `pnpm vitest run`: **908 pass, 40 skipped, 60 files, no failures**, stated although no code changed, because the claim being made is that nothing changed.
- **Next:** **The signature queue is now three deep and this entry is third.** [ADR-056](../decisions/ADR-056.md) and [ADR-058](../decisions/ADR-058.md) are both `proposed` with `PENDING` lines from earlier this week, ADR-060 is being drafted beside this one by session 73, and [the unsigned-ADR audit](../decisions/gates/unsigned-adr-audit-2026-08-18.md) puts six corpus-phase entries in front of all of them. **ADR-059 should be read last of the three live ones**: 056 and 058 each block a specific piece of work, and 059 blocks a coverage-reporting decision that has already waited four batches and will keep.
- **Blockers:** **Question two of the ADR cannot be answered from this tree at all.** Whether any feed Merit will hold reports a whole-session halt or limit lock is a fact about a vendor, and `packages/rithmic/` holds a simulator rather than a feed. `GS-004` and `GS-031` stay blocked on a retrieval, which is deliberately a different state from the three rows the ADR recommends excluding.
- **Landmines:**

  - **THE BRIEF'S ACCOUNT OF THE `halted` CONSTANT WAS TRUE AND INCOMPLETE, AND THE MISSING HALF CHANGES WHAT KIND OF PROBLEM IT IS.** [`golden-loader/src/calendar.ts:158`](../../packages/golden-loader/src/calendar.ts) writes `halted: false` as a constant, as briefed. **[`db/src/seed/calendars/generate.mjs`](../../packages/db/src/seed/calendars/generate.mjs) writes the same constant at lines 1002 and 1035, in the PRODUCTION seed path.** So it is not that no golden fixture can set the flag; **no calendar Merit is able to build can set it**, and [`0004_catalog.sql:331`](../../packages/db/migrations/0004_catalog.sql) has carried the column with a `DEFAULT false` since the catalog migration. A loader defect and a missing input look identical from the fixture directory and have entirely different remedies.

  - **`R-05` NEEDS NO MIGRATION AND THE WHOLE CORPUS READS AS IF IT DOES.** [`0004_catalog.sql:318`](../../packages/db/migrations/0004_catalog.sql) already declares `session_open_at timestamptz NOT NULL` and line 319 `session_close_at`, and `0032` later made them nullable. **The columns exist; the projection to `CalendarDay` drops them on purpose.** What does not exist is the VALUES: [`cme-2026-2028.source.json`](../../packages/db/src/seed/calendars/cme-2026-2028.source.json) carries `"status": "awaiting-transcription"` with `holidays: null` and `early_closes: null`, and the engine's fixture calendar states in its own note that **78 of its 83 sessions are synthetic**. A design proposed against those columns would have been correct and unfillable.

  - **A TEST ALREADY EXISTS WHOSE ONLY JOB IS TO FAIL IF QUESTION THREE IS ANSWERED YES, AND NOTHING ELSE IN THE TREE WOULD SEE THAT CHANGE ARRIVE.** [`rules-a-calendar.test.ts:271`](../../packages/rules-engine/test/rules-a-calendar.test.ts) builds `Record<keyof CalendarDay, true>` and asserts `toHaveLength(4)` at line 280, with a comment at 269 saying why. **`RI-07` would not catch a session-bound field**, because a string comparison reaches no Node builtin; **`merit/engine-purity` would not**, because no import is involved; **`types: []` would not**, because no ambient typing is used. Four purity mechanisms and the one that sees it is a hand-written key count.

  - **THE FACT HAS NOW BEEN DERIVED FOUR TIMES INDEPENDENTLY AND AGREES EVERY TIME, WHICH IS THE ARGUMENT FOR THE ADR RATHER THAN AGAINST IT.** Batch 9's table in [`fixtures/README.md`](../../packages/rules-engine/fixtures/README.md), [session 58](2026-08-18-session-58.md)'s, [session 59](2026-08-18-session-59.md)'s and this one's sections 1 to 3 are four separate readings of `types.ts`, `calendar.ts` and the two seed files. **Four agreeing derivations of one stable fact is a fact that is not written down where the next reader looks**, and each derivation cost a session's reading.

  - **`docs/STATE.md` WAS EDITED ON AN EXISTING LINE AND IT WAS NOT AN AUTHORED EDIT.** The fence for this session was append-only on that file. `scripts/corpus/gates.mjs generate` rewrote the `adr_count` generated span from 58 to 59 in both [INDEX](../INDEX.md) and [STATE](../STATE.md), which `CI-06g` requires and which is the mechanism [ADR-034](../decisions/ADR-034.md) exists to enforce. **Leaving it alone would have failed the gate.** It is recorded here rather than left for a reviewer to find in the diff.

- **Files touched:** [`docs/decisions/ADR-059.md`](../decisions/ADR-059.md) (new), [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) (the 059 row, twice: reserved then linked), [`docs/decisions/README.md`](../decisions/README.md) (one row), [`docs/INDEX.md`](../INDEX.md) and [`docs/STATE.md`](../STATE.md) (the `adr_count` span, generated), [`docs/STATE.md`](../STATE.md) (one appended section), [sessions/README](README.md), this file. **No file under `packages/` was edited.**
