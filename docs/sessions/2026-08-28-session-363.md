---
status: approved
depends_on: [README.md]
last_updated: 2026-08-28
---

## 2026-08-28 - Session 363: the three copies of the liability response agree, and the adapter behind it is measured as unwritable

<!--index: **`ADR-188`'s CODE HALF LANDS AND `RI-18` REPORTS ZERO ON THE MERGE OF THIS BRANCH AND [#479](https://github.com/lam2-coder/meritfutures/pull/479), WHICH IS THE WHOLE POINT OF THE SLICE.** All three declarations of `LiabilityResponse` now carry **50** field paths and are **identical path for path**, derived at reporting time by importing session 362's own check rather than by counting: [API_CONTRACT:896](../architecture/API_CONTRACT.md), [`api/types.ts:144`](../../apps/admin/src/api/types.ts) and [`admin-reads.ts:529`](../../apps/api/src/routes/admin-reads.ts), where each code copy carried 42 before. **The eight are `ADR-188`'s and none was a transcription slip**: clause 1's four `liability_snapshots` columns and clause 4's four `reserve` members. `open_liability_cents` keeps its name, no total is sent, and `absorbed_corrections_cents` is carried **negative in both fixtures** so a clamp at zero fails rather than passes. **`withdrawals_in_flight_cents` DID NOT LAND AND THE REASON IS A COLUMN CENSUS RATHER THAN A JUDGEMENT**: `ADR-195` section 6 row 1 owes the column and takes no migration number, its row 3 says the field follows *"the moment the column exists"*, and `ADR-188` clause 5 refuses it before then. Re-derived: **58 migrations declare 735 distinct column names and that is not one of them.** **THE ADAPTER IS NOT WRITTEN AND THAT IS THE SESSION'S LARGEST FINDING, MEASURED IN FOUR PARTS.** `LiabilityResponse` projects **six groups and exactly one has a producible source**: `liability_snapshots` is a `TableKey`, so `ADR-188` clause 1's seven top-level fields are a keyed read plus ordinary code. **`reserve` is a COMPILE ERROR and not a design gap**: [`0049:154`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql) creates `reserve_coverage_snapshots` and **no file of `packages/db/src/` registers it**, so a `Tx` naming it is `TS2322` against `TABLE_KEYS`, seeded and watched, which is **session 349's refusal on a different table and took `ADR-191` to clear**. **`per_plan[].cusum`, `integrations.batch` and `eligible_next_7d` need columns no migration declares at all**, so that code would compile and have nothing to read. So the remaining work is a migration and a registration, both in `packages/db`, and **a module that could not be composed would have been an unimported file**: `IMPLEMENTED_ADMIN_READS` stays at **four** and `setAdminReadSource` stays BLOCKED. **[`admin-source-liability.test.ts`](../../apps/api/test/admin-source-liability.test.ts) IS THE SUITE OF THAT ABSENCE, TWELVE CASES, EACH WITH A STATED CLEARING CONDITION**, so the day a blocker lifts a case goes red and names it instead of the next session re-deriving all four. **Its census asserts its own non-vacuity FIRST, through an `ALTER`-added column**, because a parse matching nothing would make every absence pass for the wrong reason. **Five seeds, five reds**: the census with `ADD COLUMN` removed, the column seeded into a migration, the table registered, the method composed, the refusing arm replaced by a resolved null. **THE THREE DEFENCES WERE SEEDED SEPARATELY AND FAIL AT THREE SITES**: `TS2353` (excess property on the `Pick`), `TS2741` (missing property on the `Pick`), `TS2741` (missing property on `AdminReadSource`), plus the synchronous `AdminSourceNotComposed('readLiability')` already held at [`admin-source-evidence.test.ts:1162`](../../apps/api/test/admin-source-evidence.test.ts). **TWO FINDINGS THE DISPATCH DID NOT CARRY, AND BOTH ARE CITATION DEBT ON `admin-reads.ts`'s OWN LANDMINE.** The code half moved `setAdminReadSource` by 44 lines and `RI-16` went red on [WAVE-06:257](../plans/WAVE-06-admin-console-transport.md); it is repointed in a commit of its own. **AND [#479](https://github.com/lam2-coder/meritfutures/pull/479) LANDS `RI-16` RED ON ITS OWN BRANCH, WHICH ITS LOG DOES NOT RECORD**: its edits moved `DEPLOYABLES` from `359` to `385` in `repo-invariants.mjs` and [WAVE-06:174](../plans/WAVE-06-admin-console-transport.md) still cites `359`. **It is repairable on neither branch alone** and is one integer at merge time. 33 of 33 gates, **16 of 16 invariants on this branch**, **16 of 17 on the merge with `RI-18` GREEN and `RI-16` the one red**, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` clean read as output, **228 files / 5,441 passed / 6 skipped** against a `main` baseline of **227 / 5,429 / 6** reproduced first at `087fa570`. **No ADR number, no migration number, no `SystemReason` member, no `SqlExecutorReason` member, no `pg` import, no cast past a key type, no ledger account named, `API_CONTRACT.md` byte for byte unchanged, `packages/**` untouched, and no comment in `admin-reads.ts` names the adapter directory.** -->

**Objective.** Write `ADR-188`'s code half so the three copies of `LiabilityResponse` agree, then write `readLiability`'s adapter.

### Done

- **[`admin-reads.ts:529`](../../apps/api/src/routes/admin-reads.ts)** `LiabilityResponse` and **`projectLiability`** at [`:1202`](../../apps/api/src/routes/admin-reads.ts), the eight fields.
- **[`api/types.ts:144`](../../apps/admin/src/api/types.ts)**, the same eight, plus the narrowing `ADR-188` item 2 registered.
- **[`admin-reads.test.ts`](../../apps/api/test/admin-reads.test.ts)** and **[`admin-breaker.test.ts`](../../apps/api/test/admin-breaker.test.ts)**, the two typed fixtures.
- **[WAVE-06:257](../plans/WAVE-06-admin-console-transport.md)**, one integer, in its own commit.
- **[`admin-source-liability.test.ts`](../../apps/api/test/admin-source-liability.test.ts)** (new), twelve cases.
- **[`admin-source/index.ts`](../../apps/api/src/admin-source/index.ts)**, one header paragraph pointing at that suite. **No composition line moved.**

### 1. The code half, and what the three copies now agree on

The contract carried **50** field paths and each code copy **42**. The two code copies agreed with each
other exactly, so this was one document ahead of two files rather than three files drifting, and the eight
missing paths were `ADR-188`'s ruling in full:

| Clause | Paths |
| --- | --- |
| 1, the top-level fields are one `liability_snapshots` row column for column | `wallet_balances_cents`, `bounded_near_term_cents`, `remaining_ladder_exposure_cents`, `absorbed_corrections_cents` |
| 4, `reserve` carries its own `as_of` and its anchor | `reserve.as_of`, `reserve.treasury_account_code`, `reserve.treasury_as_of`, `reserve.treasury_source` |

All three copies now read **50** and are identical path for path, derived by importing `RI-18`'s own
`copiesOf` rather than by counting fields.

**Two things are carried in the fixtures rather than in prose.** `absorbed_corrections_cents` is the only
signed figure on this response and both fixtures carry it **negative**, so a renderer or a sweep that
clamped it at zero is a red test rather than a silent one. And `reserve.as_of` is deliberately **not** the
top-level `as_of` in either fixture, because a fixture that repeated the instant could not tell a
projection carrying both clocks from one that dated the rail with the book.

### 2. `withdrawals_in_flight_cents` did not land, and the reason is measured

`ADR-195` ruled the in-flight obligation a **term in Open Liability** and its section 6 lists four things
owed, of which the column is the first and the field is the third. Row 3 says the field follows from
`ADR-188` clause 1's projection *"the moment the column exists"*, and is refused before then by that
entry's clause 5, **no field is added for a figure no column produces**.

The column does not exist, re-derived rather than accepted: a census over all **58** migrations, reading
`ALTER TABLE ... ADD COLUMN` as well as table bodies, finds **735** distinct column names and none of them
is this one. Adding the field would have put a member on a served shape that the projection rule cannot
fill, which is how a response starts lying. **The clearing condition is pinned** in
`admin-source-liability.test.ts`, so the migration that adds the column turns a case red and names the
field, the delta row and the renderer that go with it.

### 3. The adapter is unwritable, and the four blockers clear separately

`admin-source/index.ts` already carried the sentence, written by session 356: the *"no join and no
aggregate to reach for"* reason is measured false for three methods and **"for `readLiability` it still
stands in full"**. That is still true and the reason is narrower and harder than an aggregate.

| Group | Paths | Source |
| --- | --- | --- |
| the seven top-level fields | 7 | **`liabilitySnapshots` IS a `TableKey`.** A keyed read plus ordinary code, like the three adapters this directory holds |
| `reserve` | 8 | **`reserve_coverage_snapshots` is registered nowhere.** [`0049:154`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql) creates it and no file of `packages/db/src/` names it. `TS2322`, seeded and watched |
| `per_plan[].cusum` | 3 | **No column named for a CUSUM statistic in any migration.** `plan_breaker_state` carries the other half, so this group is partly producible, which is worse than wholly blocked |
| `integrations.batch` | 2 | **No `last_success_at` and no `last_duration_ms` anywhere** |
| `eligible_next_7d` | 6 | **A per-trading-day forecast nothing stores**, and `SD-M6-01`'s two identity-max columns landed in no migration, which is `ADR-188` finding 9 re-derived |

The `reserve` blocker is **session 349's refusal on a different table**: that session was dispatched to
write `listEvents`, seeded a `Tx` naming `'events'`, watched `tsc` refuse it against `TABLE_KEYS`, wrote
the refusal down, and `ADR-191` is what cleared it. The same seed here is refused the same way.

**So a module was not written, and the reason is not a fence.** A `liability.ts` producing what it can
would satisfy no method of `AdminReadSource`, so it could not enter
`composeImplementedAdminReads`, so nothing would import it. The remaining work is a migration and a
registration, both in `packages/db`, and both need an entry nobody holds.

### 4. What the suite of an absence is, and why it beats a paragraph

A sentence in a header is true on the day it is written and silent every day after.
`reserve_coverage_snapshots` will be registered by somebody for some other read, and nothing in this tree
would then tell the session holding this method that its blocker had gone.

Twelve cases, every one a live read with a stated clearing condition. **The census asserts its own
non-vacuity first**, reading back five columns the producible groups depend on, one of which
(`funded_accounts`) exists only as an `ALTER`. Watched failing in five directions, tree clean after each:

| Seed | Case that went red |
| --- | --- |
| the census pattern loses `ADD COLUMN` | the non-vacuity guard |
| `withdrawals_in_flight_cents` seeded into `0049` | `ADR-195`'s clearing condition |
| `reserveCoverageSnapshots` appended to `TABLE_KEYS` | blocker 1's clearing condition |
| `'readLiability'` added to `IMPLEMENTED_ADMIN_READS` | the method is absent from the composed set |
| the refusing arm replaced by a resolved `null` | the synchronous refusal that names the method |

### 5. The three defences, seeded separately, failing at three sites

| Seed | Error |
| --- | --- |
| the key in the object `composeImplementedAdminReads` returns, the name NOT in `IMPLEMENTED_ADMIN_READS` | `TS2353`, excess property on `PartialAdminReadSource` |
| the name in `IMPLEMENTED_ADMIN_READS`, no key | `TS2741`, missing property on `PartialAdminReadSource` |
| the `readLiability` arm deleted from `composeAdminReadSource` | `TS2741`, missing property on `AdminReadSource` |

The runtime one is unchanged and is asserted from the method's own side as well as from
[`admin-source-evidence.test.ts:1162`](../../apps/api/test/admin-source-evidence.test.ts): the throw is
**synchronous**, so it is a deployment that was not finished rather than a read that failed.

### 6. Two findings the dispatch did not carry, and both are the same landmine

**THE CODE HALF MOVED A CITED LINE AND `RI-16` CAUGHT IT.** The eight fields and their headers added 44
lines above `setAdminReadSource`, so [WAVE-06:257](../plans/WAVE-06-admin-console-transport.md)'s
`admin-reads.ts:804` named a line holding `export interface AdminReadSource {`. One integer, in a commit
of its own so the code half's diff is exactly what `ADR-188` ruled.

**AND THE NEIGHBOURING CITATION MOVED TWICE, WHICH IS THE SAME LANDMINE ARRIVING FROM THE OTHER SIDE.**
`AdminReadSource:695` was unmoved when the code half landed and was re-run rather than assumed. It then
went stale on `main` without this branch touching it: [#481](https://github.com/lam2-coder/meritfutures/pull/481)
repointed it `695` to `760`, and merging `main` here shifted both by this branch's own 44. **Both numbers
in that sentence are re-derived on the merged tree and neither is arithmetic on an old one**:
`AdminReadSource` is at `804` and `setAdminReadSource` at `848`.

**AND [#479](https://github.com/lam2-coder/meritfutures/pull/479) HAS THE SAME DEBT AND ITS LOG DOES NOT
RECORD IT.** Its edits to `repo-invariants.mjs` moved `DEPLOYABLES` from `359` to `385`, and
[WAVE-06:174](../plans/WAVE-06-admin-console-transport.md) still cites `359`, so that branch lands `RI-16`
red **on its own base as well as on this one**. Its log reports *"16 of 17 invariants"* with `RI-18` as
the only failure, which means `RI-16` was not re-measured after those edits: that session's own landmine,
*derive a line citation last*, arriving on the session that wrote it. **It is repairable on neither
branch alone**, because `DEPLOYABLES` is at `359` here and at `385` there, so the repointing is a
merge-time edit and it is one integer.

### Next

- **The two pull requests land in ONE merge**, and `WAVE-06:174` is repointed `359` to `385` in that
  merge. Neither alone is green.
- **A slice holding `packages/db` takes `readLiability` forward**, and it is two things rather than one:
  registering `reserve_coverage_snapshots` (an `ADR-191`-shaped entry) and a migration adding the columns
  `per_plan[].cusum`, `integrations.batch` and `eligible_next_7d` have none of.
- **`ADR-195`'s four owed things stay owed** and the column is the first of them.
- **`ADR-188` item 4 is unrepaired and in this fence**: `api/types.ts` glosses `INV-M6-12` as *"no client
  recomputes an alarm"* where [M06:56](../plans/M06-admin-ops-console.md) constrains an alarm's INPUT.
  It is not made newly wrong by this diff, so it is reported rather than swept.

### Blockers

None reached. No ADR number and no migration number were allocated to this session and neither was needed.
The adapter's blocker is a finding rather than a blocker on this session: it is `packages/db`'s and was
measured rather than worked around.

### Landmines

- **Neither pull request is green alone.** #479 alone turns `CI-01` and `CI-02` red on `main`; this branch
  alone leaves `RI-18` red once #479 lands. They are one merge.
- **`RI-16` is red on that merge until `WAVE-06:174` is repointed.** Section 6.
- **`admin-reads.ts` is heavily cited and citations there are derived LAST.** This session shifted them by
  44 and one document had to follow.
- **`admin-source-liability.test.ts` is a suite of absences and every case is written to expire.** A case
  going red is the news it exists to deliver, not a regression, and its message says which blocker lifted.
- **The column census is a parse and its non-vacuity guard is the first case.** A type name added to
  `packages/db`'s DDL vocabulary that the pattern does not list would silently shrink the census; the
  guard catches the total collapse and not a single missing type.

### Files touched

- `apps/api/src/routes/admin-reads.ts`
- `apps/admin/src/api/types.ts`
- `apps/api/test/admin-reads.test.ts`, `apps/api/test/admin-breaker.test.ts`
- `apps/api/test/admin-source-liability.test.ts` (new)
- `apps/api/src/admin-source/index.ts`
- `docs/plans/WAVE-06-admin-console-transport.md`
- `docs/STATE.md`, `docs/sessions/2026-08-28-session-363.md` (new), `docs/sessions/README.md`
