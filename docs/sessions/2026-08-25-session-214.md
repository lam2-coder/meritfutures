---
status: approved
depends_on: [README.md]
last_updated: 2026-08-25
---

## 2026-08-25 - Session 214: column type and nullability are compared, and `ALTER COLUMN` stops being a proxy refusal
<!--index: **[ADR-103](../decisions/ADR-103.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH and the `E2` read owed. RULED in three clauses: a transcription's column TYPE and NULLABILITY are COMPARED against the migration set as of the last migration; [ADR-094](../decisions/ADR-094.md) section 3's `ALTER COLUMN` clause is SUPERSEDED and only that clause, so the fold's vocabulary gains `ALTER COLUMN ... DROP NOT NULL` as its second member with `DROP COLUMN` and `RENAME` still refused; and the `ALTER COLUMN` sub-vocabulary is itself CLOSED at that one shape with a default of FAIL.** **THE ORDER IS THE RULING RATHER THAN AN IMPLEMENTATION DETAIL**: ADR-094 refused `ALTER COLUMN` because *"deleting the refusal before writing the comparison would be trading a real control for nothing"*, so the comparison landed FIRST, in its own commit, watched failing on four seeded transcriptions, and the fold widened after it. **THE COMPARISON FINDS ZERO DISAGREEMENTS ACROSS 99 TABLES AND 1,183 COLUMNS, WHICH IS THE OPPOSITE OF THE RESULT THIS SLICE WAS PRICED FOR**: [P3 wave 3](../plans/P3-wave3-modules.md) section 12 row 1 calls its cost *"unmeasured by construction"*, ADR-094 predicted it *"would turn the suite red on whatever it finds across seven already-merged money-path files"*, and the dispatch instructed the session to stop and report if the count stopped being small. **Nine sessions transcribed 99 tables with nothing checking this axis at all and got every column right**, so no row of [`schema.ts`](../../packages/db/src/schema.ts) is edited and the file is untouched. **A GREEN RESULT FROM A NEW COMPARISON IS THE ONE MOST LIKELY TO BE VACUOUS, AND THREE ASSERTIONS STAND WHERE THAT SILENCE IS**: both sides of every per-table assertion are READERS and two readers that degrade together AGREE, which is [ADR-084](../decisions/ADR-084.md) section 7's failure and ADR-094's seed B, so **fourteen SENTINEL columns are written out as LITERALS** and checked against both readers, the type reader is asserted non-degenerate over all 1,183 columns, and its output vocabulary is asserted to span sixteen spellings. **SEED B WAS WRITTEN FOR THIS COMPARISON AND WATCHED BEING THE ONLY RED**: with the per-table assertion made to compare the DDL WITH ITSELF and `contact_channels.value_hash` still seeded as `text`, **all 99 per-table comparisons stayed GREEN and the sentinel assertion was the only failure**; add a reader collapsed onto `'text'` and the vocabulary assertion fires by name. **THE AXIS IS NOT COSMETIC AND [ADR-046](../decisions/ADR-046.md) IS WHY**: `contact_channels.value_hash` is a DIGEST and `value_ciphertext` is envelope-encrypted under a key not present in this database, so a digest transcribed as `text` is a digest the application layer reads as a string; money is integer cents, so `purchases.list_price_cents` transcribed `integer` silently caps a balance at 2,147,483,647 cents; and `identities.status` read as `text` leaves [ADR-041](../decisions/ADR-041.md)'s refusal to add a fourth member with nothing to hold. **FOUR SEEDED TRANSCRIPTIONS WATCHED FAILING, EACH NAMING ITS COLUMN**, and `git status --porcelain` read empty after every restore. **THE FOLD'S SECOND MEMBER HAS NO EXERCISE IN THE REGISTRY AND THE ENTRY SAYS SO RATHER THAN GLOSSING IT**: `otp_challenges` and `trading_calendar` are the only two tables in 47 migrations carrying an `ALTER COLUMN`, neither is registered, so `foldTable(...).relaxed` is EMPTY on all 99 and a vocabulary member no assertion runs is one nobody has checked. **The suite therefore folds those two BY NAME, registers neither, and watches the relaxation happen** -- NOT NULL at the `CREATE`, nullable after the fold, type unchanged -- and a second assertion re-derives every `ALTER COLUMN` in the set and holds the carriers at exactly those two and the count at exactly three, **which covers unregistered tables the refusal assertion cannot see**. **FIVE STATEMENT SHAPES AND TWO MUTATIONS WATCHED**, each seeded as a migration file and removed: `SET DATA TYPE` REFUSED with the statement quoted; **`DROP NOT NULL` on a registered table turning the NEW COMPARISON red by column name, which is the ordering executed rather than argued**; and three throws, on a `PRIMARY KEY` column, on an already-nullable one, and on a column not on the table. **`otp_challenges` AND `trading_calendar` BECOME REGISTRABLE AND NEITHER IS REGISTERED HERE**, so **SESSION 215 CAN PROCEED**. **THE HONEST COVERAGE IS TWO AXES AND THE ENTRY CARRIES A TABLE OF WHAT IT CANNOT SEE**: `DEFAULT` (335 transcribed calls, priced as the next comparison and **the one with a caller already written**, since [ADR-102](../decisions/ADR-102.md)'s `insert` builds statements from it), `CHECK` (the transcription states none), `UNIQUE` and indexes, inline `REFERENCES` beyond the columns a scope rule names, and column order. **ONE FENCE CALL IS REPORTED RATHER THAN TAKEN**: [`schema.ts`](../../packages/db/src/schema.ts)'s header now says `ALTER COLUMN` stays an offender that turns the suite red, which is FALSE about this tree; `P3-g`'s fence admits that file for ONLY the rows the comparison finds wrong, the comparison found none, **never widen a fence to finish**, and session 215 holds the file next and moves the same header's drifted-table count. **The approval clause is FIVE COMMANDS and names no count**, on [ADR-100](../decisions/ADR-100.md)'s and [ADR-102](../decisions/ADR-102.md)'s precedent after eight signed clauses drifted on a figure. **Registry unmoved at 99 of 111**; [`scope.ts`](../../packages/db/src/scope.ts) and [`schema.ts`](../../packages/db/src/schema.ts) are not edited at all. `typecheck` exit 0 with zero `error TS`, `eslint packages/db` exit 0, invariants **9 of 9**, gates **30 of 30**, `verify` exit 0, `scoped-db.test.ts` **125 to 229**, suite **112 files / 1,718** to **112 files / 1,822 passed / 1 skipped** -->

- **Objective:** write the comparison [ADR-094](../decisions/ADR-094.md) section 3 specified and did not number, so that `ALTER COLUMN` stops being a proxy refusal. **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **One ADR: [ADR-103](../decisions/ADR-103.md).**
- **Done:** [ADR-103](../decisions/ADR-103.md) landed at `status: proposed` with the approval line UNSIGNED, and the comparison with it. [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) gains 99 per-table TYPE-and-NULLABILITY assertions, three non-vacuity assertions, and two on the widened fold, **125 to 229**. The fold's vocabulary is two members: `foldTable` records an `ALTER COLUMN ... DROP NOT NULL` and `foldTableDefs` applies it, **off ONE reader** so the two folds cannot disagree. **The comparison finds ZERO disagreements across 99 tables and 1,183 columns**, so [`schema.ts`](../../packages/db/src/schema.ts) is untouched. Suite **112 files / 1,718** to **112 files / 1,822 passed / 1 skipped**.
- **Next:** **The `E2` line-by-line read is owed on [ADR-103](../decisions/ADR-103.md) and on [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts)**, and the approval clause is five commands so the read can execute it rather than trust it. **Session 215 can proceed**: `otp_challenges` folds clean under the widened vocabulary and registers with its full effective column set compared on both axes. **The DEFAULT comparison is the cheapest next one and it now has a caller**: 335 `.default*()` calls are transcribed, nothing compares them, and [ADR-102](../decisions/ADR-102.md)'s `insert` builds statements from them. **[`schema.ts`](../../packages/db/src/schema.ts)'s header sentence about `ALTER COLUMN` is false and belongs to session 215.**
- **Blockers:** none for this session.
- **Landmines:**
  - **THE COMPARISON FOUND NOTHING AND THAT IS THE RESULT MOST EASILY MISREAD.** Zero disagreements does not mean the axis was never at risk; it means nine sessions got it right with nothing checking. **Four seeded transcriptions that every other mechanical check in this package accepted are the evidence** -- `bytea` written as `text`, `bigint` written as `integer`, and `.notNull()` on a `NULL` column all passed `typecheck`, `eslint`, the name comparison and every scope assertion before this entry. The next transcribing session has no such record.
  - **THE TYPE ALIAS TABLE IS THE ONE EDIT THAT CAN MAKE THIS COMPARISON AGREE WITH A WRONG TRANSCRIPTION.** It maps two spellings onto one type and it has exactly two entries, both PostgreSQL's own alias for `timestamptz`. **An entry that is not an alias makes two DIFFERENT types compare EQUAL**, silently, on every column of that type. `ADR-103` foreclosure 2 makes adding one an ADR; nothing mechanical stops it.
  - **THE FOLD'S SECOND MEMBER RUNS ON NO REGISTERED TABLE.** `foldTable(...).relaxed` is empty on all 99, so the only thing exercising `ALTER COLUMN` is the pair of assertions that fold `otp_challenges` and `trading_calendar` by name. **Those two table names are hand-written in the suite.** They are held honest by the closure assertion that re-derives every `ALTER COLUMN` statement in the migration set, which is why that assertion exists; delete it and the fold's new member could go unexercised with the suite green.
  - **`SET DATA TYPE` IS THE SHAPE THIS ENTRY IS LEAST READY FOR AND IT IS THE ONE MOST LIKELY TO ARRIVE NEXT.** It moves the axis this entry compares, it will arrive with rows already in the table, and it is refused today on the correct ground that it has zero instances. **The refusal is loud** -- a red suite naming the statement -- **but the ruling it needs is not written**, and a session meeting it under deadline will be tempted to add one regex.
  - **[`schema.ts`](../../packages/db/src/schema.ts)'s HEADER IS NOW FALSE IN A MONEY-PATH FILE.** *"`ADD COLUMN` is folded, and `DROP COLUMN`, `ALTER COLUMN` and `RENAME` stay offenders that turn the suite red."* The fence forbade the repair and [ADR-042](../decisions/ADR-042.md) already ruled prose is not a control, **but a reader who trusts that sentence will conclude `otp_challenges` cannot be registered.** Session 215 is the session that both needs it to be false and holds the file.
  - **NULLABILITY IS NOW READ TWICE FROM ONE HELPER AND `PRIMARY KEY` IS WHY.** `declaredNotNull` reads the key as well as the words, because `wallet_dormancy.identity_id` is `uuid PRIMARY KEY REFERENCES identities(id)` with no `NOT NULL` text at all. **That is correct for a column-level `PRIMARY KEY` and it is why `withoutNotNull` THROWS on one**: a `DROP NOT NULL` against a primary key is a statement PostgreSQL refuses, and absorbing it would have produced a column still reading NOT NULL after a statement whose whole purpose was to relax it. **Composite `PRIMARY KEY (a, b)` declares nothing on the column**, and all fifteen composite keys in this tree spell `NOT NULL` on every member explicitly, checked at source rather than assumed.
  - **`falsify:ci` WAS NOT RUN IN THIS SESSION AND NOTHING WAS `git add -A`ed AFTER ANY SEED.** Every seeded file was restored from a pre-seed copy or deleted, and `git status --porcelain` was read before each commit. The five migration-file seeds were written to `packages/db/migrations/9999_seed_alter_column.sql` and removed; the directory is back at 47 files.
- **Files touched:** [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) (the comparison, the fold's second member, the two ALTER assertions), [ADR-103](../decisions/ADR-103.md) (new), [ALLOCATION](../decisions/ALLOCATION.md) (its row, reserved in the first commit and amended in place when the file landed), [decisions/README](../decisions/README.md) and [INDEX](../INDEX.md) (generated), [STATE](../STATE.md) (one appended `##` section plus generated count spans), [sessions/README](README.md) (row 214 struck, entry row generated), this file. **Nothing outside the fence.** [ADR-094](../decisions/ADR-094.md), [ADR-092](../decisions/ADR-092.md), [ADR-101](../decisions/ADR-101.md), [ADR-102](../decisions/ADR-102.md), [ADR-046](../decisions/ADR-046.md), [`schema.ts`](../../packages/db/src/schema.ts), [`scope.ts`](../../packages/db/src/scope.ts) and all 47 migrations were **read and none was written**.

### What was NOT done, named rather than implied

**No migration, no migration number claimed, no `CI-06` letter, no gate register row, no `VG` row, no `RI-nn`, no `SD-nn`, no `OI-nn`, no fixture, no edge-case entry, no route, nothing under [`apps/`](../../apps/api/src/server.ts), no dependency, no `build` script, and no edit to [`docs/plans/`](../plans/P3-wave3-modules.md), which this fence makes read only.**

**[`packages/db/src/schema.ts`](../../packages/db/src/schema.ts) AND [`packages/db/src/scope.ts`](../../packages/db/src/scope.ts) ARE NOT EDITED AT ALL.** `git status --porcelain packages/db/src` reports zero on this branch. The fence admitted `schema.ts` for **only the rows the comparison finds wrong**, and it found none. **No table is registered, unregistered or re-argued** and the registry stands at **99 of 111** unmoved, which is [ADR-092](../decisions/ADR-092.md)'s ownership rule kept and the reason this ran beside sessions 213 and 215.

**`otp_challenges` AND `trading_calendar` ARE NOT REGISTERED.** This session makes them REGISTRABLE and stops there. Registering either would be taking a table [P3 wave 3](../plans/P3-wave3-modules.md) section 9 assigns to session 215, and the scope class for `otp_challenges` is a ruling this entry does not make.

**THE `DEFAULT` COMPARISON IS NOT WRITTEN.** 335 `.default*()` calls are transcribed and nothing compares them. It is named and priced in [ADR-103](../decisions/ADR-103.md) section 4 rather than built, because a slice that already carries a ruling and 104 assertions on the money path should not also carry a second axis nobody measured.

**[ADR-101](../decisions/ADR-101.md) SECTION 8's BLIND `firm` ASSERTION IS NOT REPAIRED.** It reads the `CREATE TABLE` rather than the fold and misses `admin_actions.on_behalf_of_identity_id`. It is a live finding, the repair is a ruling about what the `firm` class promises, and it is neither this entry's subject nor inside its fence.
