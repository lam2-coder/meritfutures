## 2026-08-15 - Session 26: FOLD-01 planned, the passwordless-auth and phone-as-identity ruling
<!--index: FOLD-01 planned, the passwordless-auth and phone-as-identity ruling -->

- **Done.** [FOLD-01](../plans/FOLD-01-phone-identity.md) is written and approved. **Plan only, per the brief's stop condition.** No ADR was written, no migration was created, and no module document was folded. The `FOLD-nn` series is new: a founder ruling that touches nine documents and the first post-merge migration is too large to fold from a prompt and too small to be a module.
  - **THE TWO DECISIONS THE BRIEF REQUIRED WERE TAKEN, AND THE FIRST RESOLVED ON EVIDENCE RATHER THAN PREFERENCE.** The registration lookup is **[ADR-023](../decisions/ADR-023.md)'s existing vendor**, not a second one, because ADR-023 already purchases "email and **phone** digital-footprint... device, IP, VPN and datacenter detection" (`DECISIONS.md:483`, restated at `M03:311` and [M07](../plans/M07-risk-abuse.md) D-15). **Three of the ruling's four (a) signals are inside the purchased scope and only portability history is separable**, so portability history becomes a **disqualifying selection criterion** in ADR-023's procurement rather than an assumption: amendment 3's recycling guard has no input without it. Carrier metadata joins **counsel packet item 3 as a new 3d**, and the privacy policy row cites 3d rather than asserting a basis.
  - **THE HARD LINK WAS AMBIGUOUS BETWEEN TWO APPROVED DOCUMENTS AND THE FOUNDER RULED IT AT PLAN APPROVAL.** (b) says "auto-enforced like KYC face-dedupe"; `INV-M19-04` says a dedupe hit **changes no state** and [ADR-022](../decisions/ADR-022.md) says hard links **auto-enforce**. Ruled: **one live verified phone per identity is a real unique index; a second identity verifying a live number completes, binds the edge at the hard ceiling, and opens a severity-5 flag against both.** The reason is amendment 3, because refusing at the door puts the innocent owner of a recycled number into a support ticket before the portability check can reach them.
- **Next.** **Session 2 of the plan's six: the ADR plus both allocation rows.** Money path, fresh session. It lands alone so a sibling branch can read the claim, and `0029` is claimed in the migration allocation table before the file exists. Sessions 2 and 3 are money path and take a fresh session each.
- **Blockers.** None for the fold. `OI-06` needs a founder ruling but does not block it.
- **Landmines.**
  - **`OI-06`. THE 48 HOUR PAYOUT-DESTINATION COOLING WINDOW HAS NO STORAGE, AND IT WAS FOUND BY TRYING TO MODEL THE RULING'S (c) ON THE CONTROL (c) SAYS TO COPY.** There is no `payout_destinations` table anywhere in the merged 96. `destination_ref` is a column on `payout_transfers` (`0010:243`) and `wallet_withdrawals` (`0011:132`), the destination **of a transfer**, and nothing records that a destination changed or when. **C-11, C-24, [SECURITY section 4](../architecture/SECURITY.md) item 1, [M20](../plans/M20-wallet.md) `WF-M20-02` and [M04](../plans/M04-trader-portal.md)'s destination-cooling scenario all cite a control whose input does not exist.** Ruled **surface only**: `0029` builds the phone hold on its own storage and does not touch it, because folding an unasked-for change into the diff the founder reads line by line is how E2 gets slower and worse at once.
  - **[SECURITY section 2.7](../architecture/SECURITY.md) SAYS "no SMS-based second factor anywhere in the stack" AND THE RULING ADDS SMS OTP.** A direct textual contradiction in an approved document. The resolution is that section 2.7 sits under "The founder (the human asset)" and is **rescoped to founder and admin credentials**, with admin auth keeping hardware-key SSO and no SMS path. **It is resolved in the ADR explicitly rather than glossed**, because a contradiction folded silently is one a later reader resolves the other way.
  - **`contact_channels.kind check in ('email','push')`, so the `INV-M16-03` prior-contact countermeasure CANNOT NOTIFY A PRIOR NUMBER TODAY.** (c)'s notification to the prior number is unbuildable until `sms` joins that check. The table's whole design note is that supersession exists so a prior contact survives as a row, and the one contact kind the ruling makes an identity signal is the one kind it cannot hold.
  - **THE PLAN PRE-CLAIMED TEN DELTA IDENTIFIERS, AN EDGE CASE AND A GOLDEN SCENARIO, AND [ADR-026](../decisions/ADR-026.md)'S GATE REFUSED ALL TWELVE. THE GATE WAS RIGHT.** **Only ADR numbers and migration numbers have an allocation table** ([ADR-034](../decisions/ADR-034.md), [ADR-036](../decisions/ADR-036.md)); a delta identifier is claimed by its `DELTA_MANIFEST` row existing. Writing one in a plan before the row exists is pre-claiming in a registry with no claim mechanism, which is the drift those two ADRs were built to stop, **reproduced inside the plan for folding them**. The plan now names each change by its owning module and table and takes the number when the row is written. **This is the shape of the corpus's recorded error class and it was caught by a robot in under a minute rather than at merge.**
  - **THE FOLD IS TWENTY-ONE FILES, NOT NINE, AND THE COUNT IS STATED BEFORE THE FOLD RATHER THAN DISCOVERED INSIDE IT.** Nine carry the ruling. Twelve are forced by named gates: `CI-06i` needs a `### <table>` section per new table in both directions, ADR-026 needs a manifest row per delta, `CI-06d` and `CI-06e` need the registry entries, and **API_CONTRACT, EVENTS and STATE_MACHINES are forced by the change itself with no gate that would catch their absence**, which is why the plan names them.
  - **ONE NEW CI GATE IS PROPOSED RATHER THAN THREE, AND IT COVERS THE TWO AMENDMENTS THAT ARE CHECKABLE FROM THE TREE.** `CI-06k` asserts that every endpoint declares a required factor, that every sensitive action C-27 names declares a non-single factor, and that **no notification class outside post-identity security and money is rate-limit exempt**. It must be watched failing on its seeded finding before it is trusted, per `falsify.mjs`.
- **Verification run in this session:** eleven of eleven gates pass, `generate` is a no-op so no span drifted, and `falsify.mjs` reports all eleven failing dirty with five scope cases holding.
- **Files touched:** `docs/plans/FOLD-01-phone-identity.md` (new), `docs/INDEX.md` (one row), `docs/{STATE,SESSION_LOG}.md`. **No `.sql` file, no `packages/` file, no `scripts/` file and no workflow was touched, and no module document was folded.**

---
