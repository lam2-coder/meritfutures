---
status: approved
depends_on: [README.md]
last_updated: 2026-08-29
---

## 2026-08-29 - Session 397: `eligible_next_7d` gains `| null` across all three copies, and `ADR-208` rules the sibling body `ADR-203` refused

<!--index: **`B5`'s TERM 3 IS PAID AND TERM 1 NOW HOLDS THE BLOCKER ALONE.** `eligible_next_7d` gained `\| null` across `RI-18`'s three declarations in ONE commit, on [ADR-203](../decisions/ADR-203.md) ruling 8's shape and [ADR-204](../decisions/ADR-204.md) ruling 9's requirement. **[ADR-208](../decisions/ADR-208.md) WAS TAKEN AND THE PART `ADR-203` DOES NOT COVER IS THE SIBLING BODY**, which that entry's own section 7a poses and declines -- *"what does `GET /admin/eligible-forecast` SERVE when there is no horizon?"*, *"the corpus leans one way and this entry does not take it"* -- and lists `EligibleForecastResponse` under REFUSED. The nullable declaration is a compile error inside that second body, so the transcription could not land without answering it. **RULED: a body carrying the `null` and its reason, never `adminNotFound`**, because `API_CONTRACT` section 8's own paragraph over these three paths records the two siblings as *"registered by nothing"*, answering 404 today, and that *"the two failures look identical from outside the tree"*; the gap is `LiabilityGap` and NOT `ProjectionGap`, whose `awaiting` is a required `string` where this field's two causes require it null; it is FORWARDED from the liability body rather than rebuilt; and no body type is written into the frozen contract, on [ADR-185](../decisions/ADR-185.md)'s form. **AN APPROVED ENTRY'S MEASUREMENT DID NOT SURVIVE AND THE CONCLUSION IT SUPPORTED IS REVERSED**: `ADR-203` section 7a states *"`RI-18` STAYS GREEN, AND THAT IS THE WHOLE DIFFERENCE"* under the partial move and therefore that *"the atomicity argument ... does not reach this field"*, and seeded on this branch the `apps/api` copy ALONE is **RED on six findings** -- session 392's seed A reproduced -- because the contract declares the object INLINE and `admin-reads.ts` declares it as a NAMED INTERFACE the reader expands. **THE ATOMIC MOVE ALSO COSTS TWO MORE TYPE ERRORS THAN THAT TABLE NAMES**, both in `apps/admin`, in a file no entry had reached because no entry had made the move `RI-18` forces. **A CONTROL WAS FOUND ON NO SERVED PATH AND IS NOW ON TWO**: `assertLiabilityGapsPaired` runs inside `projectLiability` and the forecast handler never called it, so `ADR-203` ruling 2 held on `/admin/liability` alone; the handler now runs the PRODUCER'S OWN function rather than a second copy, and four new cases watch it. **`RI-18` LOSES SIX PATHS AND BUYS BACK NONE**, 48 to 42 per copy derived by importing `copiesOf` at the moment of writing, and the book's census moves 39 declared / 5 blocked to **35 / 1 with 34 PRODUCED, UNCHANGED**: **A FALLING BLOCKED-LEAF COUNT MEANT THE CHECK STOPPED LOOKING RATHER THAN A FIGURE ARRIVING**, and the produced count is the only thing that says which. **`ADR-203` RULING 7's STATED PRICE IS ALSO STALE, IN THE SAFE DIRECTION**: it says a member of a nullable object can be renamed in the console's copy with every control green, and today `tsc` catches it at `apps/admin/src/liability-read.ts`, because that narrowing decoder landed after the entry. **FIVE FILES TAKEN OUTSIDE THE DISPATCHED FENCE, EACH REPORTED WITH ITS REASON**, two forced by a red typecheck and a red suite and one on the finding that **the fence was derived from `RI-18`'s SUBJECT LIST and `RI-18` READS DECLARATIONS AND NOT VALIDATORS**, so any fence derived from it under-names by exactly the validators. **SEVEN SEEDS, EVERY ONE WATCHED**, including the three that fire on the added pairing line and the one that fires `RI-19`. `readLiability` STAYS UNCOMPOSED for a seventh session on session 374's judgement and `EC-074`'s whole-or-nothing, and `IMPLEMENTED_ADMIN_READS` is still five. No migration number, no `CI-06` letter, no `RI-nn`, no `SD-nn`, and `packages/**` and `apps/worker/**` were not entered. Counts derived at reporting time: **33 of 33** gates, **18 of 18** invariants, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` read as OUTPUT with the tree clean after it, and **244 files / 6,079 passed / 6 skipped** against an `origin/main` baseline at `5a19161` of **244 / 6,075 / 6 reproduced before a line changed**. The delta is **+4 cases and no new file**. -->

Branch `claude/eligible-next-7d-nullable`, cut from `origin/main` at `5a19161`. **MONEY PATH, ADR-003 strict.** `B5`'s **term 3**, and the last term of that blocker that was this session's.

### Done

**The three `RI-18` declarations of `LiabilityResponse` moved in ONE commit.** `docs/architecture/API_CONTRACT.md`, `apps/admin/src/api/types.ts` and `apps/api/src/routes/admin-reads.ts` each carry `eligible_next_7d ... | null`, on [ADR-203](../decisions/ADR-203.md) ruling 8's shape and [ADR-204](../decisions/ADR-204.md) ruling 9's requirement.

**[ADR-208](../decisions/ADR-208.md) WAS TAKEN, AND THE ROW ASKED FOR THE PART `ADR-203` DOES NOT COVER: THE SIBLING BODY.** That entry's own section 7a poses the question and declines it -- *"what does `GET /admin/eligible-forecast` SERVE when there is no horizon?"*, *"the corpus leans one way and this entry does not take it"* -- and lists `EligibleForecastResponse` under **REFUSED rather than taken**. The `| null` is a compile error inside that second body, so the transcription could not land without answering it. Ruled: a body carrying the `null` and its reason, never `adminNotFound`; `LiabilityGap` and not `ProjectionGap`; the gap forwarded from the liability body rather than rebuilt; and no body type written into `API_CONTRACT`.

**A control was found on no served path and is now on two.** `assertLiabilityGapsPaired` runs inside `projectLiability`, and `/admin/eligible-forecast` does not call `projectLiability`: it reads the same source and builds its own body. So `ADR-203` ruling 2's one control ran on `/admin/liability` and on no other path in this tree that reads a liability. The handler now runs the **producer's own function** rather than a second copy of the predicate.

**`B5`'s clearing condition still reads three terms**, with term 3's assertion inverted from an absence into a positive one, exactly as session 394 inverted term 2's. `RI-19` binds the module's enumeration to the case's restatement in both directions and was watched firing.

### Counts, all derived at the moment of writing

| | |
|---|---|
| `node scripts/corpus/gates.mjs check` | **33 of 33** |
| `node packages/tooling/checks/repo-invariants.mjs` | **18 of 18** |
| `pnpm vitest run` | **244 files, 6,079 passed, 6 skipped**, against a baseline of **6,075 passed** reproduced on `origin/main` before a line changed |
| `pnpm -r typecheck`, `pnpm lint`, `pnpm format:check` | exit 0, 0 problems, clean |
| `node scripts/corpus/falsify.mjs` | all 33 gates pass clean and fail dirty, 80 scope cases, 10 loader cases; tree restored, `git status --porcelain` empty |
| `RI-18` paths per copy | **48 to 42.** Six leave, none arrive |
| book census | **39 declared / 5 blocked to 35 / 1**, with **34 PRODUCED, UNCHANGED** |
| `IMPLEMENTED_ADMIN_READS` | **five**, and it does not name `readLiability` |

### Findings that were not in the dispatch

1. **`ADR-203` SECTION 7a's `RI-18` MEASUREMENT IS FALSE ON THIS TREE, AND THE CONCLUSION IT SUPPORTED IS REVERSED.** That entry states *"`RI-18` STAYS GREEN, AND THAT IS THE WHOLE DIFFERENCE"* under the partial move, and therefore that *"the atomicity argument that forced `payout_velocity` and `cusum` into one diff does not reach this field"*. Seeded on this branch, the `apps/api` copy alone is **RED on six findings**. The mechanism is the reverse of that sentence: `API_CONTRACT` declares the object **inline** and `admin-reads.ts` declares it as a **named interface the reader expands**, so nulling one copy drops that copy's five member paths and leaves the other two carrying them. Session 392's seed A recorded the same result and no entry had reconciled the two.

2. **THE ATOMIC MOVE COSTS TWO MORE TYPE ERRORS THAN `ADR-203`'s TABLE NAMES**, both in `apps/admin`, because that measurement was taken on the partial move `RI-18` refuses.

3. **A FALLING BLOCKED-LEAF COUNT CAN MEAN THE CHECK STOPPED LOOKING.** `BLOCKED_LEAVES` went five to one and **not one figure gained a source**: `RI-18` does not walk into a union arm. The produced count is the only thing that disambiguates it, which is why the case now asserts declared, blocked and produced together.

4. **`ADR-203` RULING 7's STATED PRICE IS STALE, IN THE SAFE DIRECTION.** It says a member of a nullable object can be renamed in the console's copy with every control green. Seeded today, `tsc` catches it at `apps/admin/src/liability-read.ts`, because that narrowing decoder landed after the entry and reads every member against `NonNullable<LiabilityResponse[...]>`. A type checker is not an invariant, but the union blindness is no longer unwatched.

5. **THE FENCE WAS DERIVED FROM `RI-18`'s SUBJECT LIST, AND `RI-18` READS DECLARATIONS AND NOT VALIDATORS.** Any fence derived from it under-names by exactly the validators. `apps/admin/src/api/types.ts` was inside the fence and its only producer, `apps/admin/src/liability-read.ts`, was not.

### Files taken outside the dispatched fence, each with its reason

| File | Why |
|---|---|
| `apps/admin/test/liability-read.test.ts` | `pnpm -r typecheck` is RED without it. Two reads become `?.`; neither case can pass on a `null`, so no assertion is weakened |
| `apps/api/test/admin-source-liability-book.test.ts` | the suite is RED without it. `BLOCKED_LEAVES` and the census, both re-derived by running the reader |
| `apps/admin/src/liability-read.ts` | finding 5. A declaration whose validator stays behind is a deployable that rejects the shape it declares |
| `apps/api/test/admin-reads.test.ts` | the forecast body gained a field, and the new pairing line needs a case that watches it fire |
| `docs/plans/WAVE-06-admin-console-transport.md` | one line citation repointed, caught by `RI-16`, under the citation-repair right granted up front |

### Seeds, and every one was watched

| Seed | Result |
|---|---|
| `\| null` on the `apps/api` copy alone | `RI-18` **RED, six findings** |
| `\| null` on all three copies | `RI-18` **PASS** |
| `total_cents` renamed inside the nulled group, console copy only | `RI-18` blind, **`tsc` catches it twice** |
| `last_7d_cents` renamed, console copy only | `RI-18` blind, **`tsc` catches it once**, refuting `ADR-203` ruling 7's price |
| the `eligible_next_7d` line removed from `assertLiabilityGapsPaired` | **three cases fail** |
| `assertLiabilityGapsPaired` removed from the forecast handler | **one case fails** |
| term 3 struck from the case's restatement | **`RI-19` fires**, naming the term and its distinctive identifiers |

### Refused

**`readLiability` IS NOT COMPOSED**, for a seventh session, on [session 374](2026-08-28-session-374.md)'s judgement and [EC-074](../edge-cases/EC-074.md)'s whole-or-nothing. Terms 1 and 2 of `B5` were not touched. `packages/**` and `apps/worker/**` were not entered at all. No migration number, no `CI-06` letter, no `RI-nn`, no `SD-nn`. A body type for `EligibleForecastResponse` in `API_CONTRACT` was refused on `ADR-185`'s form; unifying `ProjectionGap` and `LiabilityGap` was refused on `ADR-203` finding 2.

### Blockers

**`B5` STANDS, HELD BY TERM 1 ALONE**: a `writeRuleState` implementation, which is session 395's in `apps/worker/**` and `packages/**` and was in flight as PR #525 while this ran.

### Landmines

- **`ADR-203` sections 7a and 10 now carry two claims this tree contradicts** (findings 1 and 4). Both are registered in `ADR-208` section 7 rather than repaired: changing an approved entry's measurement is an ADR about that entry.
- **Three of `LiabilityResponse`'s groups are outside `RI-18`'s reach**, and the console's decoder is what backstops them. A future refactor that types that decoder's arms loosely would take the backstop away silently.
- **`ADR-208` ruling 4 assumes the forecast's absence has no cause the liability body cannot name.** If the slice that clears term 1 finds one, the forecast answers with a `null` and an empty `gaps`, which the pairing control refuses as a 500. Loud, but a failure; the entry's approval block hands the check to the next session.
- **`hash.ts` still says "four of the twenty-five leaf fields below are `Cents`" where seven are**, registered by `ADR-206` against `packages/**` and re-derived here as still seven.

### Files touched

`docs/decisions/ADR-208.md` (new), `docs/decisions/ALLOCATION.md`, `docs/decisions/README.md` (generated span), `docs/STATE.md`, `docs/architecture/API_CONTRACT.md`, `docs/plans/WAVE-06-admin-console-transport.md`, `docs/sessions/2026-08-29-session-397.md` (new), `apps/api/src/routes/admin-reads.ts`, `apps/api/src/admin-source/liability.ts`, `apps/api/test/admin-reads.test.ts`, `apps/api/test/admin-source-liability.test.ts`, `apps/api/test/admin-source-liability-book.test.ts`, `apps/admin/src/api/types.ts`, `apps/admin/src/liability-read.ts`, `apps/admin/test/liability-read.test.ts`.

### Next

Term 1. When `writeRuleState` has an implementation, `B5` clears whole and `readLiability` can be composed; nothing else on this response is blocked.
