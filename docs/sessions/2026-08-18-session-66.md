---
status: approved
depends_on: [README.md]
last_updated: 2026-08-18
---

## 2026-08-18 - Session 66: golden fixtures batch 10, the settlement block, and a retraction's third copy (non-money)
<!--index: golden fixtures batch 10: `L-11` lifted, the settlement block, GS-067 and GS-068, and six rejections with a reason each -->

- **Done:** **`L-11` is lifted and the fixture format states a `SettlementFact`.** The rule refused every non-empty `settlements:` list and its own comment named the remedy; the block is now there and the five field names are transcribed from the type rather than chosen. **Two fixtures written, GS-068 and GS-067**, taking the directory from **36 to 38 of 284** and the M1 partition to **38 written, 35 held back, 73 total**. **Six of the eight rows behind `L-11` did not come out and each one's reason was re-derived**, not inherited. **`L-15` is new**, refusing the inert settlement. **A defect in [`run.ts`](../../packages/golden-loader/src/run.ts) was found by the change that made it reachable.** The retraction left standing at `fixtures.golden.test.ts:202` is closed. `pnpm vitest run`: **793 pass, 40 skipped, 54 files, no failures**; **17 of 17 gates**; the stage prints **38 of 284, 38 derive direct, 0 inverted, derived direction ENFORCED**.
- **Next:** **The next batch is blocked on `compare.ts`, not on the engine and not on the format.** Four of the six remaining settlement rows (GS-053, GS-082, and GS-060/GS-080 beside them) need `engine_gates`, which is a nested object that `compare.ts` diffs flat with `Object.is`. That is one change in [`packages/golden-loader/src/compare.ts`](../../packages/golden-loader/src/compare.ts) and it unblocks more rows than anything else now left.
- **Blockers:** **GS-066 needs a representation for a FAILED transfer and `SettlementFact` has none, because a failed transfer produces no fact at all.** GS-241 states two figures (`675,000c`, `450,000c`) that are R-44's split, which lives on `PayoutEvaluation` and never reaches `RuleState`. Neither is fixable by this format.
- **Landmines:**

  - **`run.ts` PASSED EVERY SETTLEMENT TO EVERY DAY, AND IT WAS UNOBSERVABLE UNTIL THE FORMAT COULD STATE ONE.** `DayInput.settlements` is documented on the engine's own type as "those whose `effectiveTradingDay` equals `mark.tradingDay`", and `advanceDay` takes that at its word: **DO-2 sorts by ordinal and applies every fact it is handed, with no day comparison anywhere in the loop.** `run.ts` handed it the whole array on every mark. `L-11` had forced `settlements: []` on all 36 fixtures, so the missing filter could not fire, and the first fixture to state a settlement would have settled its payout once per trading day. **The defect and its visibility arrived in the same commit**, which is the argument for lifting a refusal rather than working around it: a rule that makes a code path unreachable also makes its bugs unfalsifiable.

  - **A RETRACTION LEFT ONE COPY STANDING FOR TWO BATCHES AND NO GATE IN THIS REPOSITORY CAN SEE THAT SHAPE.** `fixtures.golden.test.ts:202` opened `FOUNDER RULING, 2026-08-17` twenty-eight lines below the sentence retracting it. **`CI-06q` passes over it and is not at fault**: 2026-08-17 carries three declared rulings, so the **date** resolves while the **claim** is false, and the gate's own covers line says it checks that an authority exists and never that it says what is attributed to it. [Session 55](2026-08-17-session-55.md) had already executed the proof. Closed here because the loader is inside this session's fence; the general defect is not closed.

  - **`L-15` EXISTS BECAUSE THE INERT SETTLEMENT IS THE ONE FAILURE THIS DIRECTORY CANNOT SEE.** A fact whose `effective_trading_day` is not a day in the fixture's own stream is never applied, so the fixture **passes** while pinning an end state no settlement reached, reading all the while like a scenario about one. Every other loader rule refuses something malformed; this one refuses something that would have been **green**.

  - **BOTH FIXTURES PASSED ON THE FIRST RUN AND THAT IS ONLY EVIDENCE AFTER THE SEEDED VERSION HAS BEEN WATCHED FAILING.** Every value in both was derived from M01's rule taxonomy and Appendix A before the engine was run. GS-068's `consistency_period_start_day` was then seeded at `2026-11-26` and its `consistency_best_day_cents` at 50,000, and each came back naming its own field; GS-067's `account.graduated` was deleted and then moved to position 17, and the second probe printed three per-index diffs. **A first-run pass against an engine nobody consulted is the good outcome and is indistinguishable from a vacuous fixture until it is falsified.**

  - **GS-067 PINS AN EVENT SEQUENCE BECAUSE THE END STATE CANNOT HOLD BOTH SIDES OF `>=`.** An engine writing `>` graduates at no settlement and ends `funded`; an engine graduating a rung early ends `graduated` **exactly as the correct one does**. Only the event's POSITION separates the second from correct behaviour, so an end-state-only fixture would have caught one of the two failures and looked complete.

  - **`trading_day` IS UNPINNABLE ON A GRADUATING DAY AND THAT IS A FINDING, NOT AN OMISSION.** `applySettlement` builds its result by spreading the prior state and never sets `tradingDay`, and R-49 returns at DO-2 because "no trading day follows". So the state leaving the graduating day names `2026-10-05` while its own `account.graduated` event carries `2026-10-06`: **the state and its event disagree about which day it is.** M01 R-49 states the phase change and the closure and says nothing about the row's date. GS-067 pins neither value and records the disagreement, per the rule that a fixture pinning a contested value ratifies a ruling nobody made.

- **Files touched:** [`fixtures/GS-068`](../../packages/rules-engine/fixtures/GS-068-consistency-period-boundary-at-the-settlement-day.yaml), [`fixtures/GS-067`](../../packages/rules-engine/fixtures/GS-067-graduation-fires-exactly-on-the-ladder-count.yaml) and both siblings, [`fixtures/README`](../../packages/rules-engine/fixtures/README.md), [`golden-loader/src/loader.ts`](../../packages/golden-loader/src/loader.ts), [`golden-loader/src/run.ts`](../../packages/golden-loader/src/run.ts), [`golden-loader/test/loader.test.ts`](../../packages/golden-loader/test/loader.test.ts), [`golden-loader/test/fixtures.golden.test.ts`](../../packages/golden-loader/test/fixtures.golden.test.ts), [STATE](../STATE.md), [sessions/README](README.md), this file.

### The count, reported rather than implied

**Two written, six rejected with a reason each, and the six are the deliverable as much as the two are.** The objective warned against batch 9's error, where four group A rules were named as newly writable and not one of them was. This batch derived its candidates from what the engine declares (46 of 50) and what the format can state, found that **every writable row sat behind a loader rule rather than behind the engine**, and lifted the rule instead of writing around it. The rejected six break into three kinds: **four need `engine_gates` diffed as a nested object**, **one needs a failed transfer that produces no fact**, and **one states figures that live on `PayoutEvaluation`**. Only the first kind is one change away.
