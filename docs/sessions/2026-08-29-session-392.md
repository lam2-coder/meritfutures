---
status: approved
depends_on: [README.md]
last_updated: 2026-08-29
---

## 2026-08-29 - Session 392: `B5` is not cleared, its clearing condition had two statements with two of three terms each, and all three terms are outside this fence

<!--index: **`readLiability` IS NOT COMPOSED, `B5` IS NOT CLEARED, AND THE DISPATCH'S CENTRAL PREMISE DID NOT SURVIVE THE CHECK.** The dispatch says "`B5` IS NOW A WIRING JOB AND NOT A RESEARCH JOB, WHICH IS WHY IT IS DISPATCHABLE TONIGHT". **THE WIRING IT NAMES LIES ENTIRELY OUTSIDE THE FENCE IT WAS DISPATCHED WITH**, and establishing that precisely is this session's first deliverable. **THE CONDITION WAS STATED TWICE, IN TWO PLACES, WITH TWO OF ITS THREE TERMS EACH, AND NO CONTROL COMPARED THEM.** [`liability.ts`](../../apps/api/src/admin-source/liability.ts)'s `B5` block said *"a `rule_states` writer lands AND a primary source declares the stored `engine_gates` shape, or a ruling defines the forecast over columns that exist"*; [`admin-source-liability.test.ts`](../../apps/api/test/admin-source-liability.test.ts) said *"a `writeRuleState` implementation lands, AND `eligible_next_7d` gains its `\| null`"*. **NEITHER NAMED THE OTHER'S SECOND TERM.** That is `FM-16`'s shape -- a second statement of one predicate, free to drift -- arriving on the clearing condition that exists to prevent exactly that, and it is why four sessions in a row have read a two-term condition. The condition is now written ONCE, in the module, with all three terms, and the case asserts all three. **THE RULING ARM IS STRUCK**: `ADR-204` is that ruling, it landed, and three sessions have now recorded that it moves nothing. **TERM 1: a `writeRuleState` IMPLEMENTATION**, which is `apps/worker/**` and `packages/**`, both outside the fence and the second explicitly *"nobody's this wave"*. **MEASURED LIVE RATHER THAN GREPPED: `rule_states` HOLDS ZERO ROWS** over all 60 migrations and 115 live tables. **TERM 2: a primary source declaring the stored `engine_gates` ENCODING**, which is an ADR, and the dispatch withholds an ADR number in terms. **AND TERM 2 IS REACHED THROUGH `projectPayout`'s OWN INPUT TYPE, WHICH NO ENTRY HAD SAID**: [`project.ts:211`](../../packages/rules-engine/src/payout/project.ts) declares `readonly state: RuleState;` and [`types.ts:1006`](../../packages/rules-engine/src/types.ts) declares `readonly engineGates: EngineGateResults;`, so the producer session 389 built cannot be called from an adapter that has no declared encoding for that bag, **and a landed writer does not by itself supply one**. **TERM 3: `eligible_next_7d: EligibleNext7d \| null`**, and `RI-18` makes it ATOMIC ACROSS THREE FILES: `docs/architecture/API_CONTRACT.md` (FROZEN, so an ADR), `apps/api/src/routes/admin-reads.ts` (outside `admin-source/**`) and `apps/admin/src/api/types.ts` (session 393's, which the dispatch forbids reaching into). **SEEDED AND WATCHED: the partial move is RED with SIX findings** and names all three declarations. **SO NOT ONE OF THE THREE TERMS IS SPENDABLE IN THIS FENCE, AND COMPOSING AGAINST THEM WOULD HAVE TURNED AN HONEST 500 INTO A WRONG ANSWER** -- session 374's judgement, upheld by a fifth session. **WHAT WAS BUILT IS THE ONE THING THIS FENCE OWED: `B5` LEG 2, WHICH `ADR-204` SECTION 8 REFUTED AS DDL AND RETURNED HERE AS CODE.** `readCalendarSlice` walks the covered interval holding the last closed day, filters holidays, orders ascending and takes POSITION as `sequence`, then hands the days to the engine's own `buildCalendarSlice`. **THE INTERVAL IS THE WHOLE COVERED ONE AND NOT THE HORIZON**, because `projectPayout` requires a calendar covering the cadence anchor, which is a PAST day; a slice built to the horizon would refuse every account whose anchor predates it, which reads as an empty projection rather than as a window chosen too narrowly. **EXECUTED AGAINST A LIVE POSTGRESQL over 60 migrations, 115 tables and 34 seeded `trading_calendar` rows** declaring coverage `2026-11-16..2026-12-24` across TWO loads that overlap by one day: anchored at `2026-11-24`, 28 sessions, `sequence` equals position for all 28, **`2026-11-26` is a holiday and consumes NO index**, and **`2026-11-20` to `2026-11-30` is 5 TRADING days and 10 CALENDAR days**, which is `AS-06`'s cost as a number. **THE ENGINE'S OWN READERS WERE RUN OVER THIS ADAPTER'S SLICE**, which is what makes it more than this file agreeing with itself: `lookupCalendarDay` answers `not_a_session` for the holiday inside coverage and `outside_coverage` for `2026-12-28`, **a REAL row past the edge**, keeping `ADR-042` F-4's two answers apart on live data; `nextTradingDayAfter` walks `2026-11-25` to `2026-11-27` through the holiday and MISSES past `2026-12-24`; and the slice and `readTradingHorizon` agree day for day because both walk one anchor by identity. **THE SLICE HAS NO `exhausted` ARM AND THE MISSING ARM IS THE FINDING**: a horizon asks for seven days and can be short of them, a slice asks for the covered interval and takes what it holds, so reporting a one-session estate as short would be reporting the calendar's size as a defect -- and a case shows the SAME estate exhausting the horizon, so the two answers are shown to differ on one input rather than described as differing. **A SECOND FINDING THAT WAS IN NO DISPATCH AND THAT NOBODY HAD MEASURED: `ADR-203`'s ABSENCE SHAPE COSTS `RI-18` ITS SIGHT INSIDE EVERY FIELD IT APPLIES TO.** Three seeds and a control: `\| null` on one copy alone is **RED** (five paths vanish at once, because that check's own words are *"A UNION is deliberately NOT reduced"*); on all three copies it is **GREEN**; and with all three nulled, `by_day` RENAMED on ONE copy is **GREEN AND DID NOT FIRE**. The control is the same drop at the same depth under a parent that is not nulled (`reserve.rcr_bp`) and it is **RED**, so the non-firing is the shape and not the seed. **AND IT IS NOT HYPOTHETICAL: `per_plan[].cusum` HAS CARRIED `\| null` SINCE `ADR-203`, SO `RI-18` IS BLIND INSIDE IT ON `main` TODAY** -- a member can be dropped from one of the three copies and nothing fires, which was seeded on the clean tree and confirmed. **Reported and not repaired: `packages/tooling` is not this fence's**, and the shape is a deliberate ruling in that file rather than a bug. **A SEED CAUGHT A DEFECT IN THIS SESSION'S OWN NEW CASE AND THAT IS THE MOST VALUABLE RESULT HERE.** A first draft asked whether each file CONTAINED the string `eligible_next_7d`; renaming the member to `eligible_next_7d_GONE` left it **GREEN**, because the old name is a prefix of the new one. The predicate is now a member name anchored on its colon, the non-firing is written into the case's own comment, and the re-seed is **RED**. **TWELVE SEED APPLICATIONS: NINE RED, THREE GREEN, AND ALL THREE GREENS ARE MEASUREMENTS RATHER THAN PASSES.** Six on this diff's own code all red -- date arithmetic for position, the coverage upper bound dropped, the holiday filter dropped, the re-throw removed so a `CalendarSliceError` escapes an admin read, the slice built to the horizon only, and `uncovered` collapsed into an empty slice. **ONE PROCEDURAL DEFECT IS REPORTED AGAINST THIS SESSION ITSELF**: a seed was restored with `git checkout` on a file carrying uncommitted work and wiped it, which is precisely what `DISPATCH_PROTOCOL` section 4 forbids in its own words (*"restore from a copy taken before the seed rather than with `git checkout`"*); the work was rewritten, the byte-copy discipline was used for every seed after it, and the tree is clean. **`DISPATCH_PROTOCOL:49` STILL SAYS 16 OF 16 AND THE TREE REPORTS SEVENTEEN**, for the fourth session running. **NO ADR NUMBER, NO MIGRATION NUMBER, NO `SD-nn`, NO `GS-nnn`**, no `SystemReason` or `SqlExecutorReason` member, no `pg` import, no cast past a `TableKey`, no ledger account named, no gate weakened, no fence widened, no merged migration touched, and `API_CONTRACT.md`, `routes/admin-reads.ts`, `apps/admin/**`, `packages/**` and every migration byte for byte unchanged. `IMPLEMENTED_ADMIN_READS` is at **FIVE** and does not name `readLiability`; `eligible_next_7d`'s **5** leaves are blocked against **39** declared, so **34** produced, unchanged. Counts derived at reporting time: **33 of 33** gates, **17 of 17** invariants, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` read as OUTPUT with all 33 gates clean-and-dirty plus 80 scope and 10 loader cases and the tree clean after it, **60** migrations and **115** live tables, and **243 files / 6,018 passed / 6 skipped** against an `origin/main` baseline at `028143c` of **242 / 6,002 / 6 reproduced before a line changed**. The delta is **+1 file and +16 cases**, exactly the 14 written in the new file plus the 2 written in the pinned one. -->

**Objective.** Clear `B5`, the last standing liability blocker, and compose `readLiability`
if and only if every leaf is then producible.

**Outcome.** `B5` is not cleared and `readLiability` is not composed. Its clearing condition
had two statements carrying two of three terms each, and **all three terms lie outside this
session's fence**. Leg 2, the one piece that was this fence's, is built and executed.

### Done

- **[`liability.ts`](../../apps/api/src/admin-source/liability.ts)**: `readCalendarSlice`, the
  `CalendarSlice` loader [`ADR-204`](../decisions/ADR-204.md) section 8 returned to this fence;
  the `B5` block's refuted leg-2 prose repaired; the clearing condition written once with all
  three terms.
- **[`admin-source-liability-calendar.test.ts`](../../apps/api/test/admin-source-liability-calendar.test.ts)**
  (new): 14 cases, the engine's own readers run over the slice this adapter built.
- **[`admin-source-liability.test.ts`](../../apps/api/test/admin-source-liability.test.ts)**: the
  restated condition with its third assertion, and 2 cases pricing term 3.

### 1. The premise that did not survive

The dispatch says `B5` is "now a wiring job and not a research job, which is why it is
dispatchable tonight". **The wiring its clearing condition names is not reachable from the
fence it was dispatched with**, and the fence is `apps/api/src/admin-source/**`,
`apps/api/test/**`, `STATE` and this file.

| Term | Where it lives | Why this session cannot spend it |
|---|---|---|
| 1. a `writeRuleState` implementation | `apps/worker/**`, `packages/**` | Outside the fence, and the dispatch says `packages/**` is nobody's this wave |
| 2. a declared `engine_gates` encoding | an ADR | The dispatch withholds an ADR number in terms |
| 3. `eligible_next_7d ... \| null` | `API_CONTRACT.md` + `routes/admin-reads.ts` + `apps/admin/src/api/types.ts` | `RI-18` binds all three atomically. One is FROZEN, one is outside `admin-source/**`, one is session 393's |

**Saying so is the deliverable rather than a caveat**, and it is why the composition is
refused for a fifth time on session 374's own reasoning.

### 2. The condition was two statements, and that is the sharper finding

Two files each stated it, each with two of three terms, and no control compared them. The
module named the encoding and not the `| null`; the case named the `| null` and not the
encoding. **A clearing condition is a control, and this one had the defect it exists to
catch.** It is now written once, with three terms, and asserted.

**And term 2 is reached through `projectPayout`'s own input type**, which no entry had
recorded: `PayoutProjectionInput.state` is a `RuleState` and `RuleState.engineGates` is an
`EngineGateResults`. So a landed writer clears term 1 and leaves term 2 standing.

### 3. Leg 2, built and executed

`ADR-204` section 8 refuted leg 2 as DDL and named what it actually requires. Session 389's
next-slice line named the same thing. **An entry's argument and a slice a suite runs are
different objects**, which is `B2`'s lesson a second time.

```
LIVE   60 migrations, 115 tables, 34 trading_calendar rows, 2 loads overlapping by one day
ANCHOR 2026-11-24   COVERAGE 2026-11-16..2026-12-24   28 sessions
DENSE  sequence === position for all 28
R-37   2026-11-20 -> 2026-11-30 is 5 TRADING days and 10 CALENDAR days
       2026-11-26 is a holiday, is absent, and consumes NO index

  the engine's own readers, over this adapter's slice
  lookupCalendarDay 2026-11-26  ->  not_a_session      (inside coverage)
  lookupCalendarDay 2026-12-28  ->  outside_coverage   (a REAL row past the edge)
  nextTradingDayAfter 2026-11-25 -> 2026-11-27         (through the holiday)
  nextTradingDayAfter 2026-12-24 -> MISS               (coverage is the authority)
  horizon and slice agree day for day                  (one anchor, by identity)
```

### 4. What `ADR-203`'s absence shape costs, which nobody had measured

| # | Seed | Result |
|---|---|---|
| A | `\| null` on the `apps/api` copy alone | **RED**, six findings, all three declarations named |
| B | the same on all three copies | **GREEN** |
| C | with all three nulled, `by_day` renamed on ONE copy | **GREEN. DID NOT FIRE** |
| control | the same drop at the same depth, parent NOT nulled | **RED** |

A union is not reduced to a field set, so a nulled field contributes one path and none of its
members. **`per_plan[].cusum` has carried `\| null` since `ADR-203`, so `RI-18` is blind inside
it on `main` today**, which was seeded on the clean tree and confirmed. Reported and not
repaired: `packages/tooling` is not this fence's, and the shape is a ruling rather than a bug.

### 5. Found and not in the dispatch

1. **The clearing condition was two statements with two of three terms each.** Section 2.
2. **`RI-18` goes blind inside any field taking the absence shape**, and one field is already
   in that state. Section 4.
3. **Term 2 is reached through `projectPayout`'s own input type**, so terms 1 and 2 are not
   one term with two halves.
4. **A seed caught a defect in this session's own new case**: a substring predicate that a
   renamed member satisfies. Fixed, re-seeded, red, and written into the case's comment.
5. **`DISPATCH_PROTOCOL:49` says 16 of 16 and the tree reports 17**, for the fourth session.

### Next

- **`B5` needs all three terms and no single fence holds them.** The cheapest next slice is
  term 2, an ADR declaring the stored `engine_gates` encoding, because it is the one that
  makes a `rule_states` READER writable and it costs no code.
- **Term 3 wants one ADR moving `API_CONTRACT` and both code copies in one commit**, and the
  sibling `EligibleForecastResponse` body is still an open ruling (`ADR-203`).
- **`M01:785` still wants a pointer to `projectPayout`**, which session 389 reported and which
  is an ADR rather than a commit.

### Blockers

**None new, and `B5` is unchanged in substance.** What changed is that its shape is now
written once and completely, so the next session reads three terms rather than two.

### Landmines

- **A CLEARING CONDITION STATED IN TWO PLACES IS TWO CONDITIONS.** Nothing compared them and
  each was individually true. Write a condition once and point at it.
- **`git checkout` IS NOT A SEED RESTORE.** Used on a file carrying uncommitted work it wipes
  the work, which happened here once. `DISPATCH_PROTOCOL` section 4 says byte copy and means it.
- **A `toContain` ON A FIELD NAME IS SATISFIED BY EVERY SUPERSTRING OF IT.** `eligible_next_7d`
  is a prefix of `eligible_next_7d_GONE` and the case stayed green. Anchor on the colon.
- **A BUILT INPUT READS LIKE A LIFTED BLOCKER.** `projectPayout` takes five inputs and this
  session produced one. Both the module and the suite say so before anything else.
- **AN ABSENCE SHAPE CAN COST A CONTROL ITS SIGHT.** `\| null` buys a wire that can decline and
  spends every field path underneath it.

### Files touched

- `apps/api/src/admin-source/liability.ts`
- `apps/api/test/admin-source-liability-calendar.test.ts` (new),
  `apps/api/test/admin-source-liability.test.ts`
- `docs/STATE.md`, `docs/sessions/2026-08-29-session-392.md` (new), `docs/sessions/README.md`
