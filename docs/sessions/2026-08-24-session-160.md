---
status: approved
depends_on: [README.md]
last_updated: 2026-08-24
---

## 2026-08-24 - Session 160: M06 measured, and the module's schema arrived without its process
<!--index: **M06 measured against the tree, and the shape is the INVERSE of [session 143](2026-08-23-session-143.md)'s P3 finding: M06's SCHEMA IS COMPLETE and M06's RUNTIME DOES NOT EXIST.** All eleven `SD-M6-nn` deltas have a merged migration and [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md) rows every one `landed`; **zero of the forty operator route-and-verb pairs the corpus specifies exist**, because no process in this tree listens on a socket: `fastify` is in no manifest and [`apps/api/src/index.ts`](../../apps/api/src/index.ts) prints *"no routes yet"*. [`apps/admin`](../../apps/admin) is 2,673 lines of PURE FUNCTIONS over injected inputs, 26 `M6-A-nn` tests, and it opens no connection and mutates nothing. **The precondition under nearly every slice is not the router**: `packages/db/src/schema.ts` carries **7 tables of 111** and **exactly one of M06's fourteen**, so `scopedDb`/`systemDb` cannot reach thirteen of them **by compile error, deliberately**. `SD-M6-01` is **2 columns of 4** against a manifest row reading `landed` ([session 112](2026-08-21-session-112.md)'s unrepaired finding). **All 30 M6-owned golden scenarios are `blocked / no-fixture-format`, one shared cause**, and five carry unit assertions the registry does not row `covered-elsewhere`. `M6-A` is the only one of the seven suites with a single test in it. **This session ran ahead of its own CLAIM row**: `160` was unclaimed at `ef4cccc`, nothing was fabricated, and `#199` reserved the whole fourteen-session wave mid-measurement, so **fourteen sessions ran against a table that did not know they existed** - a claim that is correct and LATE, which `CI-06w` cannot see because the multiset is right the moment it lands. Nine slices, four MONEY PATH, three BLOCKED before they can start. No code, no migration, no ruling -->

- **Objective:** measure [M06](../plans/M06-admin-ops-console.md) against the tree the way [session 143](2026-08-23-session-143.md) measured P3. An INVENTORY. No document, no `INDEX.md` row, no `STATE.md` append, no ADR, no code.

- **Done:** the inventory below. **29 of 29 gates, `pnpm run verify` green at 104 files / 1,512 passed / 1 skipped, `falsify.mjs` over 62 scope and 10 loader cases**, all measured in this session and none of it moved by it, because this session writes one log.

---

### 1. What M06 specifies, section by section

| Section | What it commits Merit to |
|---|---|
| **1.1 What it is** | `apps/admin` on a separate apex behind `ADMIN_ORIGIN`, IP allowlisted, hardware-key SSO, unlinked. **Five surfaces**: liability home, account drill-down, **identity drill-down**, flags queue, event feed. Plus one export, the evidence pack |
| **1.2 What it is not** | Five boundaries. No rule arithmetic (M1), no detection (M7), no denial (amended by [ADR-040](../decisions/ADR-040.md) to freeze-plus-expiring-hold), no money movement (M5), **no edit of a live account's `plan_version_id`, and its absence is the control** |
| **1.3 Invariants** | **Fourteen**, `INV-M6-01` to `INV-M6-14`. `01` audit middleware and an append-only table; `02` no shared cookie/CORS/CSP; `03` no admin denial of a payout; `04` every number names its as-of and source; `05` audited export with a declared audience; `06` mandatory suppression expiry; `07` `insufficient_data` is never a breach; `08` server-side dual control across two credentials in 24h; `09` `readonly` cannot mutate and `ops` cannot configure; `10` no bulk identity render; `11` wallet balances inside every liability figure; `12` an indicative figure decides nothing; `13` **no console affordance extends any clock**; `14` restriction and restore both carry a complete record |
| **2 Deltas** | **Eleven rows**, `SD-M6-01` to `SD-M6-07` and `09` to `11`, naming **thirteen tables**. `08` is unclaimed by position |
| **3.1 Liability home** | **Ten panels**, `P-M6-01` to `P-M6-10`, each with its number DEFINED. **`P-M6-09` data trust is last in the list and first on the page** and gates every number above it |
| **3.2 / 3.2a Drill-downs** | Account: *why did this account get this outcome*, `gate_results` per day **from the stored row and never a recomputation**. Identity: *who is this human and what do they hold*, and it is the surface a restriction and a restore are opened from |
| **3.3 / 3.3a Enforcement** | `open -> investigating -> {dismissed, enforced}`, **no automatic transition into `enforced`**. Three outcomes from `enforced`: closure (one account, terminal), freeze (one payment, expiring), **restriction (one human, reversible only by a documented restore)**. Two v1 entry points, one v1.x |
| **3.4 Dual control** | `pending -> approved` on the **same payload hash** within 24h, over cap, split, cadence gap, treasury and rail credentials. The honest framing is carried **into the UI**: compromise resistance, not insider resistance |
| **3.5 Live Open Liability** | [ADR-020](../decisions/ADR-020.md) tier 2. Last close plus indicative movement, **visually distinct, beside the as-of figure**, read by no breaker, written into no snapshot, and **suppressed rather than shown when `P-M6-09` is red** |
| **3.6 Digest delivery** | [ADR-066](../decisions/ADR-066.md). **Four named digests, two MUST**, cadence GENERATED from digest, vocabulary closed by CHECK. **The alarm fires on `report_deliveries`, never on the job.** No `skipped` outcome exists |
| **4 Endpoints** | M6 owns [API_CONTRACT](../architecture/API_CONTRACT.md) sections 8 and 9 in full and **adds no new endpoint shape**; four obligations stated |
| **5 Events** | Consumes the catalogue for the feed; emits eight, **three of them NEW**. **The restore has no event and section 5 names that as a gap it does not fill** |
| **6 Failure modes** | **Ten**, `FM-M6-01` to `FM-M6-10` |
| **7 Adversarials** | **Six**, five novel. Detector disclosure, small-sample breaker, the muted alarm, the confidently wrong number, the owned admin session, and the action under duress |
| **7.9 / 7.10** | The identity-graph explorer (v1.x) and **six standing duplicate-signal views** (SHOULD), sorted on `P-M6-01`'s figure descending, **no schema delta claimed and none needed** |
| **8 Test plan** | **Eight suites**: `M6-A` 11, `M6-B` 9, `M6-N` one per mutating route per role, `M6-R` one per named refusal plus two happy paths, `M6-U` 1 generative, `M6-D` 6, `M6-E` 5, and 6 owned `GS-nnn`. **`M6-N-01` to `M6-N-08` are `ADR-068`'s and the router enumeration starts at `M6-N-09`** |
| **9 Observability** | **Eleven metrics** and a nine-item alert list, two of which page on conditions the schema cannot refuse |
| **10 Open questions** | **Five.** `01` and `03` RULED. **`02` breaker minimum, `04` daily digest and `05` unsuppressible-set storage are OPEN** |
| **11 Parity** | [ADR-069](../decisions/ADR-069.md). **Thirty-four trader actions enumerated, eighteen close across 17 paths and 18 route-and-verb pairs**, ten permanently EXCLUDED, and **`OQ-F4-01` and `OQ-F4-02` block six rows** |

---

### 2. What EXISTS, every row run rather than recalled

**Every claim in this section is a command executed on `ef4cccc` in this session.**

| Thing | Measured by | What came back |
|---|---|---|
| **`apps/admin`** | `wc -l apps/admin/{src,test}/*.ts` | 8 source files, 7 test files, **2,673 lines**. Four commits, all titled *"M6 read"* |
| **Its surface** | `grep '^export' apps/admin/src/*.ts` | `figure`/`absent`/`authoritative`/`render`, `theThreeNumbers`, `inAdversarialOrder`, `assessDataTrust`, `liveOpenLiability`, `buildLiabilityHome`, `renderLiabilityHome`, `resolveAdminOrigin`, `requireAdminRole`, `mayReadLiabilityHome`, `SERVICE = 'admin'` |
| **What it does NOT do**, in its own words | [`apps/admin/src/index.ts:29`](../../apps/admin/src/index.ts) | *"NOTHING IN THIS PACKAGE MUTATES. That is a property of the tree rather than a convention: there is no route, no client and no write path in it"* |
| **Its tests** | `grep -o 'M6-[A-Z]-[0-9]*' apps/admin/test` | **`M6-A-01` to `M6-A-26`, gapless. Nothing else.** No `M6-B`, `M6-N`, `M6-R`, `M6-U`, `M6-D` or `M6-E` test exists anywhere in the tree |
| **The eleven deltas** | `grep -l 'CREATE TABLE <t>' packages/db/migrations/*.sql`, one table at a time | **All thirteen tables exist in a merged migration.** `liability_snapshots` `0009`; `plan_breaker_state`, `alarm_suppressions`, `dual_control_approvals` `0016`; `evidence_packs` `0008`; `admin_actions` `0017`; `account_adjustments` `0038`; `economic_calendar` + `_loads` `0039`; `report_schedules` + `_deliveries` `0040`; `impersonation_sessions` + `_page_views` `0042`; `admin_actions` altered by `0043` |
| **`SD-M6-04`** | `grep redaction_profile packages/db/migrations` | [`0008:257`](../../packages/db/migrations/0008_risk.sql), all three columns, **plus a CHECK the delta did not ask for**: `audience <> 'trader' OR includes_detector_detail = false` |
| **`SD-M6-11`** | [`0043:107`](../../packages/db/migrations/0043_admin_attributed_actions.sql) | `initiative` with its three-member CHECK, `on_behalf_of_identity_id`, and the **biconditional** `admin_actions_on_behalf_matches_initiative`. **Exactly as specified** |
| **`INV-M6-01`'s second clause** | [`0026:88`](../../packages/db/migrations/0026_roles_and_grants.sql) | `REVOKE UPDATE, DELETE ON ... admin_actions ... FROM merit_app, PUBLIC`. **Append-only, and from PUBLIC as well as the app role** |
| **`SD-M6-10`'s constraint half** | [`0042`](../../packages/db/migrations/0042_impersonation_sessions.sql) | Two tables, three CHECK guards, two cross-table triggers, narrow REVOKEs. `IMPERSONATION-C1` in both directions |
| **`ADR-068` requirement 4** | [`apps/portal/src/shell/impersonation-banner.ts`](../../apps/portal/src/shell/impersonation-banner.ts) | **132 lines and it exists.** `placement` is the literal `'shell-band'` and neither `dismissible` nor `onDismiss` is a prop |
| **`economic_calendar_current`** | `grep 'CREATE.*VIEW' 0039` | [`0039:276`](../../packages/db/migrations/0039_economic_calendar.sql). The view exists |
| **`SD-M6-06`/`07`/`09` assertions** | `packages/db/test/{economic-calendar,report-schedules,account-adjustments}.test.ts` | They exist and **they are text assertions over the migration and the plan**, which each file says of itself: *"There is no delivery job, no renderer and no transport: apps/ is a scaffold. NOTHING HERE SENDS ANYTHING"* |
| **7.10's six views** | `packages/db/test/duplicate-signal-views.test.ts` | Exists, same kind. Its own comment: the ranking and the soft link *"get their executable half when the console is"* built |
| **The route-set selection** | [`apps/api/src/surface.ts:69`](../../apps/api/src/surface.ts) | `OPERATOR_PREFIXES = ['/admin', '/internal']`, `SERVICE_BY_SURFACE = { public: 'api', operator: 'api-admin' }`, `resolveSurface` throwing on unset. **[ADR-089](../decisions/ADR-089.md)'s machinery is real and is pinned by `apps/api/test/surface.test.ts`** |
| **A database connection** | [`packages/db/src/client.ts`](../../packages/db/src/client.ts) | **It exists now.** `pg` + drizzle, lazy pool, `DATABASE_URL` read at first use. Session 143's *"the client does not exist"* is stale; `145` landed it |
| **The accessor already knows about M6** | [`scoped-db.ts:143`](../../packages/db/src/scoped-db.ts) | `type SystemReason = 'nightly-batch' \| 'operator-console'`. **The reason the console will read under is already a member** |

---

### 3. What does NOT exist, exactly

**1. NO PROCESS IN THIS TREE LISTENS ON A SOCKET, so all forty operator route-and-verb pairs are absent.** `grep '"fastify"'` over every `package.json` returns **nothing**; `apps/api/src` is two files; [`apps/api/src/index.ts:84`](../../apps/api/src/index.ts) says *"Still not a server: the framework is ruled and not installed, and no route exists to register"* and `main()` logs `no routes yet`. The forty, counted at their sources: **16 in [API_CONTRACT section 8](../architecture/API_CONTRACT.md)** (14 headings, one of which carries three paths), **4 operator routes in section 9** (`/health` is public and served by both deployments), **18 parity route-and-verb pairs** from section 11.2, and **2 impersonation routes** from `SD-M6-10`. **Zero.**

**2. THIRTEEN OF M06's FOURTEEN TABLES ARE UNREACHABLE THROUGH EITHER ACCESSOR, BY CONSTRUCTION.** [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts) opens *"SEVEN TABLES OF 111, AND THAT IS REPORTED RATHER THAN ROUNDED UP"* and the seven are `identities`, `accounts`, `ledger_accounts`, `ledger_transactions`, `ledger_entries`, `treasury_balances`, `liability_snapshots`. **Exactly one of them is M06's.** `SCOPE_RULES` is total over the keys of that file, so a table absent from it is *"a COMPILE ERROR at the call site rather than an unscoped read at runtime"*. **This is not a defect and repairing it is not optional either**: it is the control working, and it is the first line of nearly every slice in section 4.

**3. NOTHING WRITES `admin_actions`.** `grep -rn 'admin_actions\|adminActions' --include=*.ts apps packages` outside `test/` returns **no hit**. `INV-M6-01`'s *"Middleware, not per-endpoint discipline"* has nothing to attach to, and `M6-U-01`, the generative test that enumerates the router, has no router to enumerate.

**4. SIX OF THE TEN PANELS.** [`page.ts:141`](../../apps/admin/src/page.ts) carries a `PENDING` array naming five with the supplier that blocks each: `P-M6-04` (M5's settled series), `P-M6-05` (`plan_breaker_state` landed in `0016` and **nothing writes it**), `P-M6-06` (`DEP-M6-05`, the harness's `mu_0` and `sigma`), `P-M6-07` (`OI-01`, and the columns do not exist), `P-M6-08` (M3's routing state). **They are listed on the page rather than omitted from it**, which is the module's own argument applied to its own incompleteness. The sixth is `P-M6-03`, which renders **absent** rather than pending, for the reason below.

**5. `SD-M6-01` IS TWO COLUMNS OF FOUR AND THE MANIFEST SAYS `landed`.** `grep -rn 'eligible_next_7d\|identity_max\|open_liability_bounded'` over the whole repository returns **no SQL hit at all**: the only hits are `apps/admin` naming the columns in a refusal message, `M05` naming the metric, `M06` specifying it, and [session 112](2026-08-21-session-112.md) reporting it. `absorbed_corrections_cents` is in [`0009:188`](../../packages/db/migrations/0009_ledger.sql); the bounded figure is there under the **different name** `bounded_near_term_cents`; **`eligible_next_7d_identity_max_cents` and `eligible_next_7d_identity_max_id` are in no migration.** [DELTA_MANIFEST:133](../../packages/db/DELTA_MANIFEST.md) still reads `landed`. **Session 112 named this and it has not moved.**

**6. ALL 30 M6-OWNED GOLDEN SCENARIOS ARE `blocked / no-fixture-format`, AND IT IS ONE CAUSE.** [Section 33.1](../testing/golden-scenarios/33-ownership-index-and-coverage-reconciliation.md) rows M6 at **30**: `GS-112` to `117`, `130`, `251`, `279`, `282`, `284`, `288` to `292`, `296` to `304`, `312` to `316`. Read one at a time out of [section 39](../testing/golden-scenarios/39-fixture-status-and-blockers.md), **thirty of thirty** carry the identical blocker: *"the only fixture format in the tree is M1's day stream folded through `advanceDay`"*. **A dashboard has no day stream**, so this is not a backlog M6 works off, it is a format M6 needs invented.

**7. THE MECHANISMS, NONE OF WHICH EXIST.** `grep -rln 'evidence_pack\|dual_control\|alarm_suppression\|plan_breaker\|redaction' --include=*.ts apps packages` returns **two files**: `apps/admin/src/page.ts` (naming `plan_breaker_state` in a blocked-panel reason) and `packages/db/test/account-adjustments.test.ts`. **No breaker evaluator, no suppression handler, no dual-control resolver, no pack builder, no redaction profile, no delivery job, no renderer, no transport.**

**8. THE UNSUPPRESSIBLE SET IS STILL NOTHING.** [`0016`](../../packages/db/migrations/0016_treasury_controls.sql)'s `alarm_suppressions` carries `alarm_key text NOT NULL` with **no CHECK and no reference list**, read in the file; `grep -rn 'unsuppressible' --include=*.ts --include=*.mjs` returns **nothing**. `AS-M6-03`'s honest limit and `OQ-M6-05` are both exactly as written, unmoved.

**9. `ADR-069`'s EIGHTEEN ARE OWED TO API_CONTRACT AND STILL ARE.** `grep -c 'admin/identities' API_CONTRACT` returns **1**, and that one is `GET /admin/identities/:identityId/graph`, which predates the fold. **Finding 3 of section 11.5 is unrepaired**, and `grep -rn 'impersonat' API_CONTRACT` returns **zero**, so `SD-M6-10`'s two routes are absent from the contract as well.

**10. `OI-15` VERIFIED OPEN AT SOURCE.** `grep -n 'initiative\|on_behalf' docs/architecture/data-model/admin_actions.md` returns **nothing**. The design record does not carry `SD-M6-11`'s two columns, its CHECK or its index, and `CI-06i` reconciles TABLES rather than columns, so no gate can see it.

**11. THE ECONOMIC CALENDAR HAS A TABLE, A LOADS TABLE AND A VIEW, AND NO ROW.** `packages/db/src/seed/` holds `calendars/` and its contents are **CME trading-hours sources only**; `grep -rln economic_calendar packages/db/src` returns **nothing**. `DEP-M7-06` assigned the dataset to *"M6 admin, **seed**"* and the seed half is the half that is missing, so `D-04` news-window clustering is still unimplementable **for want of data rather than for want of schema**, which is a different sentence from the one M06 section 2 wrote.

---

### 4. The slices

**Nine. Objective, fence BY FILE, money-path flag, dependency BY FILE.** Four are MONEY PATH under [CLAUDE.md](../../CLAUDE.md)'s regime table (auth, ledger, payout, rules engine). **Three cannot start**, and each says which open question or missing file stops it.

| # | Objective | Fence | Money | Depends on |
|---|---|---|---|---|
| **A** | **The router, and the 404 that is the router's rather than a check somebody maintains.** Install Fastify, register the operator route set behind `resolveSurface`, and assert the public deployment answers 404 for `/admin/*` and `/internal/*`. **No M6 handler.** The route table is what B, C and G enumerate | [`apps/api/src/index.ts`](../../apps/api/src/index.ts), `apps/api/src/server.ts` (new), `apps/api/src/routes.ts` (new), [`apps/api/package.json`](../../apps/api/package.json), [`apps/api/test/surface.test.ts`](../../apps/api/test/surface.test.ts), `apps/api/test/server.test.ts` (new), `pnpm-workspace.yaml`, `pnpm-lock.yaml` | no | nothing |
| **B** | **`INV-M6-01` as middleware and `M6-U-01` as a generative test over the route table.** Every mutating route writes `actor`, `reason` non-empty, `before`, `after`, and `SD-M6-11`'s `initiative` biconditional. **A new endpoint with no audit row fails automatically** | `apps/api/src/admin/audit.ts` (new), `apps/api/test/admin-audit.test.ts` (new), [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts), [`packages/db/src/scope.ts`](../../packages/db/src/scope.ts), [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) | **YES** | **A** (`apps/api/src/routes.ts`) |
| **C** | **`INV-M6-09` server side, and `M6-N-09` onward enumerated from the router.** Three roles against every mutating route. **`M6-N-01` to `M6-N-08` are `ADR-068`'s and are NOT this slice's to number**, and `M6-N-04` lands as a labelled TRIPWIRE because its route does not exist | `apps/api/src/admin/rbac.ts` (new), `apps/api/test/admin-rbac.test.ts` (new), [`apps/admin/src/roles.ts`](../../apps/admin/src/roles.ts), [`apps/admin/test/access.test.ts`](../../apps/admin/test/access.test.ts) | **YES** | **A**, **B** |
| **D** | **`SD-M6-01`'s two missing columns and `OI-01`'s homeless RCR, CVaR and reserve fields**, in one superseding migration, and the manifest row moves off `landed`. **Unblocks `P-M6-03` and `P-M6-07` together**, which is why they are one slice: both are `liability_snapshots` and a migration is never edited | `packages/db/migrations/00NN_liability_snapshot_identity_max_and_reserve.sql` (new), [`packages/db/DELTA_MANIFEST.md`](../../packages/db/DELTA_MANIFEST.md), [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts), [`apps/admin/src/page.ts`](../../apps/admin/src/page.ts), [`apps/admin/src/liability.ts`](../../apps/admin/src/liability.ts), [`apps/admin/test/page.test.ts`](../../apps/admin/test/page.test.ts), [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) | **YES, `E2` READ** | nothing |
| **E** | **The breaker evaluator, `P-M6-05` and the `M6-B-nn` suite.** Writes `plan_breaker_state` daily with `sample_size` beside `min_sample`, `insufficient_data` as a first-class outcome, and `breaker.state_changed` **carrying the sample size**. `GS-113` | `apps/worker/src/batch/breaker.ts` (new), `apps/worker/test/breaker.test.ts` (new), [`apps/admin/src/page.ts`](../../apps/admin/src/page.ts), [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts), [`packages/db/src/scope.ts`](../../packages/db/src/scope.ts) | no | **session 147**'s queue (`ADR-086`), CLAIMED and unwritten; `OQ-M6-02` for the minimum |
| **F** | **Dual control, `SD-M6-05` and the `M6-D-nn` six.** `pending -> approved` resolved server side against the **payload hash**, two distinct credentials inside 24 hours, both actors on the `admin_actions` row, and the compromise-resistance sentence rendered into the surface | `apps/api/src/admin/dual-control.ts` (new), `apps/api/test/dual-control.test.ts` (new), [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts), [`packages/db/src/scope.ts`](../../packages/db/src/scope.ts) | **YES** | **A**, **B**, **C** |
| **G** | **The two read drill-downs, 3.2 and 3.2a, under `INV-M6-10`.** Account: every `gate_results` row for the disputed day **read from storage and never recomputed**. Identity: the human, what they hold, and every restriction episode. **Reachable only by naming a subject, and every view logged as an access** | `apps/admin/src/drilldown/{account,identity}.ts` (new), `apps/admin/test/drilldown.test.ts` (new), `apps/api/src/admin/accounts.ts` (new), [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts), [`packages/db/src/scope.ts`](../../packages/db/src/scope.ts) | no | **A**, **C** |
| **H** | **BLOCKED. Restriction and restore, 3.3a, and the `M6-R-nn` refusals.** `INV-M6-13` and `INV-M6-14`: the episode row with its cited flag, ToS clause, actor and pack, `GS-117`'s typed reason before the confirm enables, and **no route that writes any of the three clocks** | would hold `apps/api/src/admin/enforcement.ts`, `packages/db/src/schema.ts`, `packages/db/src/scope.ts` | **YES** | **BLOCKED: the restore has no event.** M06 section 5 names it; `identity.restricted` has no counterpart in [EVENTS](../architecture/EVENTS.md) and [`G-RESTRICTION-LIFTED`](../architecture/STATE_MACHINES.md) is a transition with no event against universal rule 1. **EVENTS is [FOLD-02](../plans/FOLD-02-enforcement-window-and-suspension.md) session 6's file.** Also **A**, **B**, **C** |
| **I** | **BLOCKED. The alarm-suppression surface and the unsuppressible set.** Create and release with the expiry `0016` already makes NOT NULL, `alarm.suppressed` / `.suppression_expired`, and the four keys enforced rather than asserted. `GS-114` | would hold a new migration or a new reference table, plus `apps/api/src/admin/alarms.ts` | **YES** (its fourth member is a payout hold or freeze past expiry) | **BLOCKED on `OQ-M6-05`**, which is a founder call on storage, and on **`DEP-M6-08`**: M5's expiry query and M7's severity-4 band, neither of which exists |

**A tenth slice is deliberately not written.** The evidence pack and its redaction profiles (`SD-M6-04`, `M6-E-nn`, `GS-112`, `GS-116`) **cannot be sliced yet**: `DEP-M6-03` requires M7 to supply *"a detector-parameter registry so redaction knows what to strip"*, and `detector_definitions` exists as a table with no registry behind it. **Writing the slice would mean specifying M7's deliverable inside M6's fence**, so it is named here instead.

**And section 3.6's delivery job is not a slice for a reason worth stating.** `SD-M6-07` is fully landed, `CRON_INVENTORY` already carries the alarm row, and `packages/db/test/report-schedules.test.ts` already pins the schema half. **What is missing is a job runner, a renderer and a transport, and the first of those is session 147's**, so the slice is a body waiting on a queue rather than a design waiting on a ruling. It joins the list the day `ADR-086` lands.

---

### 5. The collisions between these slices, per FILE

**Read per file, not per slice, because a merge conflicts on a file.**

| File | Slices that hold it | Consequence |
|---|---|---|
| **[`packages/db/src/schema.ts`](../../packages/db/src/schema.ts)** | **B, D, E, F, G**, and H and I when they unblock | **THE ORDERING FILE, and it is the one nobody would predict.** Seven of the nine slices must add a `pgTable` to it. This is [session 143](2026-08-23-session-143.md)'s `pnpm-lock.yaml` finding one layer down: **each branch merges cleanly alone and no two merge together**, because they append to one object literal. **Any wave running more than one of B, D, E, F or G concurrently is serial on this file** |
| **[`packages/db/src/scope.ts`](../../packages/db/src/scope.ts)** | **B, E, F, G**, and H and I | Same shape, same object, one file over. `SCOPE_RULES` is **total over `TableKey`**, so the two files move together **or the workspace does not compile**. Treat `schema.ts` and `scope.ts` as ONE unit of contention rather than two |
| **[`apps/admin/src/page.ts`](../../apps/admin/src/page.ts)** | **D, E** | Both edit the `PENDING` array: D removes `P-M6-07` from it, E removes `P-M6-05`. **A keep-both merge leaves a panel both rendered and listed as pending**, which is the one failure this array exists to prevent |
| **[`apps/api/src/routes.ts`](../../apps/api/src/index.ts)** (new in A) | **A** writes it, **B, C, F, G** read it | Not a write collision. It is a **read dependency four ways**, so A is a barrier rather than a peer: B's generative test and C's enumeration are both *"over the route table"*, and a route table that moves under them moves their counts |
| **[`apps/admin/src/roles.ts`](../../apps/admin/src/roles.ts)** | **C** only | No collision, recorded because it looks like one: it holds the closed role set that C extends and G reads, and G reads it through the package export rather than editing it |
| **[`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md)** | **D**, and I if `OQ-M6-05` picks a migration | Two migration numbers off one table. `CI-06w` reads it as a multiset, so **an expected collision costs a resolution and a discovered one costs a cycle** |
| **[`packages/db/DELTA_MANIFEST.md`](../../packages/db/DELTA_MANIFEST.md)** | **D**, and I | Same, one file over, and D additionally **repairs** row 133 rather than appending to it |

**No two slices hold the same migration file, and that is by construction rather than by luck**: D and I are the only slices that write SQL and they write different tables. **Migrations are sacred; the contention here is entirely in the two TypeScript files that transcribe them.**

---

### 6. What could not be determined

1. **Whether `bounded_near_term_cents` and `open_liability_bounded_cents` are the same number under two names, or two numbers.** `0009`'s comment describes `bounded_near_term_cents` in `SD-M6-01`'s exact words for `open_liability_bounded_cents` and in `P-M6-02`'s exact words, which is strong evidence for one number. **It is not proof**, and the difference decides whether slice D adds one column or two. **A session holding `0009`'s successor takes it; this session does not rule.**

2. **Whether `M6-A-01` to `M6-A-26` discharge section 8.1's `M6-A-nn` row.** Section 8.1 sizes the liability-aggregation suite at **11** and 26 exist, but section 8.3 already converted counts that restated a list into the list's own rule for the RBAC suite **and did not do so for this row**. So `26` against `11` is either over-delivery or a suite that absorbed `M6-A-16` to `M6-A-26`'s access, page and origin cases from suites the table names separately. **Not determinable from the plan as written.**

3. **Whether five of M6's thirty golden scenarios should be `covered-elsewhere` rather than `blocked`.** `GS-115`, `288`, `289`, `290`, `291` and `292` each carry a live assertion citing them by identifier in `apps/admin/test/liability.test.ts`, `packages/db/test/report-schedules.test.ts` or `packages/db/test/duplicate-signal-views.test.ts`, and section 39 gained `covered-elsewhere` in [session 127](2026-08-22-session-127.md) for exactly this case. **Eight rows carry it today and none of them is M6's.** Whether these six qualify turns on how much of each scenario the assertion actually discharges, and each of those files says in its own header that it discharges the artifact half and not the behaviour. **A judgment, and section 39 is not this session's file.**

4. **Whether the eighteen parity routes are eighteen slices or one.** Section 11.1 makes every one of them carry the same six properties, which reads like one middleware and eighteen thin handlers. **But rows 16 and 17 are a payout origination that re-evaluates every gate and a dual-controlled wallet withdrawal**, and those are money path in a way that row 27's leaderboard opt-out is not. **Sizing them needs `OQ-F4-01` closed**, because four of the eighteen are blocked behind it and a fence drawn across a blocked row is a fence that changes when the question is answered.

5. **What `RI-04` will say about a fifth and sixth app directory.** [Session 153](2026-08-24-session-153.md) widened `DEPLOYABLES` to five for `apps/api`. Slice A adds no directory, so nothing moves. **But `RI-04`'s list is five DEPLOYABLES against [ADR-089](../decisions/ADR-089.md)'s six SERVICES**, and the entry says those two numbers are correct together. **Verified as consistent today and flagged as the pair a later reader reconciles by making one of them wrong.**

---

- **Next:** **slice A, and nothing else can be sensibly ordered before it.** Four of the nine depend on the route table it creates, and it is the only slice with no dependency of its own. **Then D**, which is independent of A entirely and is `E2` read, so it can run concurrently with A and must not run concurrently with E. **Four items go to the founder rather than to a session**: `OQ-M6-02`'s breaker minimum (blocks E), `OQ-M6-05`'s storage question (blocks I), `OQ-F4-01` and `OQ-F4-02` (block six parity rows and the sizing in section 6 item 4), and **whether the fixture-format question in section 3 item 6 is M6's to answer or the golden-scenario owner's**, because thirty blocked rows behind one cause is a format decision rather than thirty pieces of work.

- **Blockers:** none for this session. For M06: three slices blocked as tabulated, and **one blocker is not on any slice's list because it is upstream of all of them** - `packages/db/src/schema.ts` carries one of M06's fourteen tables and that is the first line of seven slices.

- **Landmines:**

  - **THE MODULE'S SHAPE IS THE INVERSE OF P3's AND READING IT AS A PERCENTAGE IS THE ERROR.** [Session 143](2026-08-23-session-143.md) found **none** of P3's six contents existing. Here **eleven of eleven deltas are merged, thirteen tables exist, `0026` makes the audit table append-only, `0042` enforces the impersonation boundary in both directions, and the two documents governing the deployment topology are signed.** A reader tallying that against forty absent routes gets a module that looks well advanced. **It is not: what exists is the part that a migration can carry, and what is missing is every part that needs a process.** `apps/admin` is 2,673 lines of pure functions over injected inputs; `packages/db/test/report-schedules.test.ts` says of itself *"NOTHING HERE SENDS ANYTHING"*. **The schema arrived first and the process never did**, and the honest summary is that M06 is further from working than a delta count suggests and closer to buildable than a route count suggests.

  - **`packages/db/src/schema.ts` IS THIS MODULE's `pnpm-lock.yaml` AND NOBODY WOULD PREDICT IT.** Seven of nine slices append a `pgTable` to one object literal in one file, and [`scope.ts`](../../packages/db/src/scope.ts)'s `SCOPE_RULES` must move in the same commit **or the workspace does not compile**, because totality over `TableKey` is what makes an unscoped read a compile error. **Four concurrent branches would each merge cleanly alone and none of them together**, which is [session 143](2026-08-23-session-143.md)'s wave-1 finding recurring one layer down on a file nobody thinks of as shared. **A wave dispatching B, D, E, F and G concurrently is dispatching five sessions onto one object literal.**

  - **`SD-M6-01` HAS NOW BEEN FOUND TWICE AND REPAIRED ZERO TIMES.** [Session 112](2026-08-21-session-112.md) reported the two absent columns against a `landed` manifest row on 2026-08-21 and [`page.ts:314`](../../apps/admin/src/page.ts) renders the panel **absent with the missing column names in the reason, never as zero**, which is the right handling and is also **why nothing forces the repair**: the page is correct about being unable to answer, so the module ships around the gap indefinitely. `CI-06i` reconciles tables and not columns, so no gate sees it. **`OI-01` is the same defect on the same table**, open since 2026-08-14, and its own text says the reserve and CVaR fields *"need a home before M06 is built"*. **Two records, one table, three years of gate coverage that cannot reach either**, and slice D exists to close both in one migration because a second migration against `liability_snapshots` is a second file that can never be edited.

  - **ALL THIRTY GOLDEN SCENARIOS BLOCKED IS ONE SENTENCE, NOT THIRTY ROWS OF WORK.** Every one of `GS-112` to `GS-316` in M6's ownership block carries the identical blocker: the only fixture format in the tree is M1's day stream folded through `advanceDay`. **A liability dashboard, a redaction profile, a dual-control window and an impersonation replay have no day stream between them**, so this is not thirty pieces of fixture work waiting in a queue, it is **one format decision nobody has taken**, and until it is taken every M6 slice ships with unit tests and no golden coverage. **Stating it as thirty blocked rows invites thirty small answers to a question that has one.**

  - **`M6-A` IS THE ONLY ONE OF SEVEN SUITES WITH A SINGLE TEST IN IT, AND THE SUITE NAMES LOOK LIKE COVERAGE.** `M6-A-01` to `M6-A-26` exist and are gapless. **`M6-B`, `M6-N`, `M6-R`, `M6-U`, `M6-D` and `M6-E` return no grep hit anywhere in the tree.** `M6-N-01` to `M6-N-08` are worse than absent: they are **CLAIMED by `ADR-068` and unwritten**, so an inventory reading section 8.1a finds eight identifiers with owners and concludes they exist. **`M6-N-04` is the sharpest case**, because the ADR itself says the test it names asserts against a route that does not exist and must be labelled a tripwire in its own body. **A claimed identifier reads exactly like a delivered one in every registry that lists it.**

  - **THE RESTORE HAS NO EVENT AND THAT IS WHAT BLOCKS THE ENFORCEMENT SLICE, NOT THE ENFORCEMENT.** `identity.restricted` is in the approved catalogue; **`identity.payouts_frozen` has `identity.payouts_unfrozen` beside it and `identity.restricted` has nothing.** So `G-RESTRICTION-LIFTED` is a transition with no event against [STATE_MACHINES](../architecture/STATE_MACHINES.md)' universal rule 1, and **`DEP-M6-07`'s re-enable half has nothing to consume**: a restore completes on Merit and never completes on the trading platform, which is `FM-M2-16`. **The schema half is fully landed** in [`0031`](../../packages/db/migrations/0031_payout_hold_and_identity_restriction.sql), episode row, open-uniqueness and the all-or-none restore constraint, so **the slice looks unblocked from the migration and is blocked from [EVENTS](../architecture/EVENTS.md)**, which is [FOLD-02](../plans/FOLD-02-enforcement-window-and-suspension.md) session 6's file and no session's fence today.

  - **THIS SESSION RAN AHEAD OF ITS OWN CLAIM ROW, AND THE RACE CLOSED WHILE IT WAS MEASURING.** The brief says session 160 *"is already CLAIMED for you"* and instructs a strike. **It was not, at the commit this session started from.** On `ef4cccc`, `grep -n 160 docs/sessions/README.md` returned **no hit in the file** and the CLAIM table carried `147`, `148` and `156` live and nothing else unstruck. **A struck row asserts that a number was claimed and then written**, so nothing was fabricated and the absence was recorded instead. **Then [`#199`](https://github.com/lam2-coder/meritfutures/pull/199) merged mid-session** and reserved `157` to `170` for the whole measurement wave, `160` among them, at which point the instruction became executable and **the row is struck here as the brief always intended.** **The finding survives the repair and is worth more than the row was**: fourteen sessions were dispatched before the commit claiming their numbers was merged, so **for the length of that window every one of the fourteen was running against a table that did not know it existed**. That is [ADR-055](../decisions/ADR-055.md)'s hole and [ADR-082](../decisions/ADR-082.md)'s missing allocation row arriving from a third direction: not a number taken twice and not a number never claimed, but **a claim that is correct and late**, which is invisible to `CI-06w` because the multiset is right the moment it lands. **The cheap fix is ordering rather than a gate, and the corpus had already written it down**: commit `2ee84a6`, one merge before this session's base, is titled *"Claim session 156 before dispatching it, not after"*. **This wave is the instance where that lesson was stated and the fourteen went out first anyway**, which is a rule living in a commit subject rather than anywhere a dispatcher reads.

- **Files touched:** this file (new), and [`docs/sessions/README.md`](README.md), **whose `session_entries` generated span is REGENERATED and not hand-edited**. The regeneration is a forced fence extension over the brief's *"the one CLAIM row you strike"* and it is stated rather than taken quietly: [`gates.mjs:1067`](../../scripts/corpus/gates.mjs) builds that table from every session file's `<!--index:` line and **a section with no row is a hard error in the generator**, so a new log with no regeneration fails `CI-06t`. **One CLAIM row struck, `160`'s own, which arrived from [`#199`](https://github.com/lam2-coder/meritfutures/pull/199) mid-session and is merged into this branch rather than written by it.** Nothing else: no `INDEX.md` row, no `STATE.md` append, no ADR, no manifest row, no code, no migration, no gate, no fixture.

### What was NOT done, named rather than implied

**No slice was implemented and nothing was ruled.** The five items in section 6 are **open and are not recommendations dressed as findings**; `OQ-M6-02`, `OQ-M6-05`, `OQ-F4-01` and `OQ-F4-02` are the founder's and none of them was closed here. **`SD-M6-01` was measured and not repaired**, because the repair is a migration and a migration is slice D's; **[DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md) row 133 still reads `landed`** and moving it without the migration that makes it true would trade one wrong row for another. **`OI-15` was verified open at its source and not closed**, since `docs/architecture/data-model/admin_actions.md` is outside this fence. **No `SD-M6-nn` number was claimed**, and `08` stays unclaimed by position exactly as M06 section 2 leaves it. **[ADR-089](../decisions/ADR-089.md) and [ADR-068](../decisions/ADR-068.md) were read in full and folded rather than restated**, and what they left undone is in sections 3 and 4 rather than summarised back. **The real admin domain appears nowhere**: `ADMIN_ORIGIN` is written as the placeholder [ADR-012](../decisions/ADR-012.md) requires, in this file and in every file measured.
