---
status: approved
depends_on: [README.md]
last_updated: 2026-08-24
---

## 2026-08-24 - Session 184: six written-out edits transcribed, six applied clean, and the largest one states the precondition it could have asserted away
<!--index: **[ADR-092](../decisions/ADR-092.md)'s four named [P4](../plans/P4-portal-and-site.md) amendments and [ADR-093](../decisions/ADR-093.md)'s two [DELIVERY_PLAN](../DELIVERY_PLAN.md) cell edits are APPLIED. ALL SIX APPLIED CLEANLY, none repaired by judgement**, every anchor matching exactly once against the file as it stands. **DELIVERY_PLAN is FROZEN and both its edits landed on line 127 and nowhere else**: P3's Contents cell gains the authentication surface and its Definition of done gains [API_CONTRACT section 12](../architecture/API_CONTRACT.md)'s `C-27` rows in BOTH directions, **including the third clause ADR-093 predicts a hurried transcription will drop** -- `GET /sessions` and `GET /phone/change` returning **200** from a single-factor session, without which a done-condition passes on an implementation that refuses everything. **THE LARGEST P4 AMENDMENT CARRIES ITS PRECONDITION RATHER THAN ASSUMING IT DISCHARGED**: `P4-a` and `P4-b` are concurrent only once [ADR-092](../decisions/ADR-092.md) section 5's `DDL_NAMES` line lands, and it has not -- `DDL_NAMES` is still `ReadonlyArray<readonly [TableKey, string]>` at [`scoped-db.test.ts:394`](../../packages/db/test/scoped-db.test.ts), read on this branch, with session 182 landing the fix CONCURRENTLY -- so the amendment reads *strictly serial until it lands, concurrent after* at all four P4 sites that asserted the old serialization. The other three: section 9's row takes ADR-092 section 2's answer (**the owner is the TABLE, the queue is the TYPE CHECKER**), section 6's estate-wide row is superseded in its second clause (the other 91 belong to the **modules**, registered per table, first writer wins), and section 10 item 1 is answered with its dispatch consequence, **ten of ten measured transcription slices concurrent** with one intersecting pair rebasing four rows out. **One judgement is recorded rather than made silently**: DELIVERY_PLAN's `last_updated` frontmatter still reads `2026-08-20` and was deliberately left, on ADR-093 section 4's *"Nothing else in DELIVERY_PLAN moves"* -->

- **Objective:** transcribe the six edits [ADR-092](../decisions/ADR-092.md) and [ADR-093](../decisions/ADR-093.md) wrote out and were each fenced out of making. Compose nothing, rule nothing, hold no ADR number.
- **Done:** **six of six applied cleanly.** Four amendments to [P4](../plans/P4-portal-and-site.md), two cell edits to the frozen [DELIVERY_PLAN](../DELIVERY_PLAN.md), one [STATE](../STATE.md) append, this log. `pnpm run gates` at 30 of 30 after **each** edit rather than once at the end; `pnpm run verify` exit 0, 106 test files, 1,526 passed, 1 skipped.
- **Next:** merge 182 and this in either order, **but read the interaction**: 182 lands the `DDL_NAMES` precondition this amendment names as absent, and once it merges P4's four conditional clauses become unconditional in substance without needing a further edit, because each is written as a condition rather than as a date.
- **Blockers:** none. Nothing in this session waited on anything.
- **Landmines:**
  - **[DELIVERY_PLAN](../DELIVERY_PLAN.md)'s `last_updated` still reads `2026-08-20` after a frozen-document amendment landed on 2026-08-24.** Deliberately left, on [ADR-093](../decisions/ADR-093.md) section 4's *"Nothing else in DELIVERY_PLAN moves"*, and **`CI-06a` cannot see it** because the gate checks that the field is present and matches `YYYY-MM-DD`, never that it is fresh. **A reader checking whether ADR-093 landed by looking at the frontmatter will get the wrong answer.** One character to repair, and it belongs to whoever next holds the file with an authority to move it.
  - **Four P4 sites asserted `P4-a`/`P4-b` serialization and [ADR-092](../decisions/ADR-092.md) section 8 item 2 names none of them.** The entry states the amendment; it does not enumerate where the old claim lives. Amending only the one the entry gestures at would have left three sites contradicting it. **All four are listed in this entry's table below** so the next reader can check the set rather than rediscover it.
  - **[P4](../plans/P4-portal-and-site.md) section 12's gating sentence still reads *"section 10's items 1, 3 and 6 are answered"* as a precondition on dispatch.** Items 1 and 3 are now both answered, by [ADR-092](../decisions/ADR-092.md) and [ADR-093](../decisions/ADR-093.md), and **that sentence was NOT edited**, because ADR-092 names four amendments and a fifth is not this session's to make. It is not wrong, only satisfied.
- **Files touched:** [P4](../plans/P4-portal-and-site.md), [DELIVERY_PLAN](../DELIVERY_PLAN.md), [STATE](../STATE.md), [sessions/README](README.md), this file.

### The six edits, individually, and what each anchor matched

**Reported per edit rather than as "all six applied", because the dispatch asked for the individual answer and a silent repair is the failure mode it was guarding against.**

| # | Entry and clause | Where it landed | Applied |
|---|---|---|---|
| **1** | [ADR-093](../decisions/ADR-093.md) section 4 **edit 1**, the Contents cell | [DELIVERY_PLAN:127](../DELIVERY_PLAN.md), between *"`M19` KYC with the composite trigger set"* and *"[ADR-023](../decisions/ADR-023.md) enrichment in observe mode"* | **CLEAN.** Anchor matched **once**, the fenced block transcribed character for character including its trailing comma |
| **2** | [ADR-093](../decisions/ADR-093.md) section 4 **edit 2**, the Definition of done cell | [DELIVERY_PLAN:127](../DELIVERY_PLAN.md), appended after *"verification firing at each configured trigger"* | **CLEAN.** Anchor matched **once**. **All three clauses carried, the third included** |
| **3** | [ADR-092](../decisions/ADR-092.md) section 8 **item 1**, section 9's row resolves | [P4](../plans/P4-portal-and-site.md) section 9, the `schema.ts`/`scope.ts` row's *"Across phases"* clause | **CLEAN.** Anchor matched **once** |
| **4** | [ADR-092](../decisions/ADR-092.md) section 8 **item 2**, the serialization | [P4](../plans/P4-portal-and-site.md) section 8's wave-1 prose (**the amendment itself**), `P4-b`'s depends-on cell, section 9's row, section 12's diagram | **CLEAN, at four sites.** See below |
| **5** | [ADR-092](../decisions/ADR-092.md) section 8 **item 3**, the estate-wide row's second clause | [P4](../plans/P4-portal-and-site.md) section 6, the *"estate-wide transcription"* row | **CLEAN.** Anchor matched **once** |
| **6** | [ADR-092](../decisions/ADR-092.md) section 8 **item 4**, the dispatch consequence | [P4](../plans/P4-portal-and-site.md) section 10 item 1, appended | **CLEAN.** Anchor matched **once** |

**Every anchor was asserted to match exactly once before the replacement ran**, so a silently-multiplied or silently-missed edit is not among the ways this could have gone wrong.

### The one edit that is four edits, and why leaving three sites alone would have been worse

[ADR-092](../decisions/ADR-092.md) section 8 item 2 states the amendment and does not say where the claim it amends lives. **It lives in four places, found by grep rather than by reading:**

| Site | Said | Now says |
|---|---|---|
| **section 8, the wave-1 prose** | *"Wave 1 is SERIAL ... on `schema.ts` for `P4-a` and `P4-b`"* | unchanged, **with ADR-092's amendment APPENDED beneath it** rather than rewritten over it |
| **section 8, `P4-b`'s depends-on cell** | *"Strictly serial with it"* | strictly serial **until** the `DDL_NAMES` line lands, concurrent after |
| **section 9, the collision row** | *"Within P4: `P4-a` then `P4-b`, strictly serial"* | the same condition, pointing at section 8's amendment |
| **section 12, the dispatch diagram** | *"two pairs, each pair serial"* | *"two pairs"*, with the condition on its own line under the `P4-a -> P4-b` arrow |

**The original prose was appended to rather than rewritten**, which is the choice worth recording: rewriting *"P3 wave 1's finding on two files instead of one: four concurrent branches"* to fit a three-way serialization would have been composing, and the amendment marker resolves the contradiction more honestly than a smoothed paragraph.

### The precondition was checked against the tree rather than against the dispatch

The dispatch said session 182 is landing [ADR-092](../decisions/ADR-092.md) section 5's `DDL_NAMES` line concurrently. **That was verified rather than believed**, twice:

- `git fetch origin main` immediately before writing the amendment: `origin/main` at `a0c7916`, [session 181](2026-08-24-session-181.md)'s merge, with no 182 branch merged.
- `grep -n "DDL_NAMES" packages/db/test/scoped-db.test.ts` on this branch: **`:394` still declares `ReadonlyArray<readonly [TableKey, string]>`**, which is exactly the non-total form ADR-092 section 5 measures the silent failure through.

**So the amendment's condition is a fact about the tree at this commit and not a hedge.** It is written as a condition rather than as a date, so 182 merging discharges it without a further edit to P4.
