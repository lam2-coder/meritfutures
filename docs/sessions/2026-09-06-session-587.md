---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 587: option (d) taken with the four fences it needed, the eleven repaired where they live, and a citation-repair right that turns out to earn nothing

<!--index: **THE ROW IS [ADR-392](../decisions/ADR-392.md), `proposed` and UNSIGNED. CONTROL OVER AN OPERATIONS DOCUMENT A HUMAN READS UNDER PRESSURE. MONEY-ADJACENT, ADR-003 STRICT, `E2` READ OWED.** [ADR-389](../decisions/ADR-389.md) built this restructure, watched the suite go RED at eleven assertions in seven files, and reverted the page byte-identical because none of the eleven was in its fence. **This row is the four-fence row it specified, and its specification is exact.** Column five becomes a peer `## Severity if absent, by job` section of 21 `### <job>` notes keyed by `normJob`: body rows **31,301 characters to 2,700**, **324 rendered lines to 34**, tallest row **47 wrapped lines to 5**, rows over 2,000 characters **9 to 0**, **columns one to four BYTE-IDENTICAL** and all 21 note bodies carried verbatim, both asserted rather than eyeballed. **THE ELEVEN REPRODUCE ONE FOR ONE** (336 / 8,096 / 10 / **11 FAILED**, seven files) and all eleven are repaired across all four fences. **FOUR OF THEM ASSERTED COLUMN-FOUR PHRASES AND STAY ON THE ROW**, so `scheduledRows` returns `{job, line, note}` as three fields rather than one concatenation that would have made every one of them pass for the wrong reason. **THE ELEVENTH IS STRENGTHENED**: the anchor's containing `### ` heading replaces containment, and seed F watches `RI-35` stay GREEN at 35 of 35 while that case alone goes RED. **THE LEG IS HALF LANDED**, `4.4` and `4.5` binding notes to rows in both directions and refusing the subsection form, **with the `gates.mjs` half OWED and its predicate written out**, that file being nobody's this wave. **THE CITATION-REPAIR RIGHT EARNS ZERO REPAIRS AND THAT IS THE FINDING**: fifteen pointers re-derived from 21 raw tokens, the three `:27` EXACT at `c5648f00` with line 27 byte-identical, and **all eight class-one pointers ALREADY four to six lines short at this base**, so none is this diff's to repair under [ADR-390](../decisions/ADR-390.md) section 9's rule. The four class-two `:35` are LEFT and **their subject's move from line 37 to 125 is disclosed rather than counted as a fix**. **Six seeds, five RED where predicted**; [ADR-389](../decisions/ADR-389.md)'s seed E confirmed independently at **33 of 33 under the `###` form against 32 of 33 under the peer `##`**, which is the hazard that would have let a stray pipe disarm `CI-06l` over the table. **NO INVARIANT MINTED, NO `RI-` RESERVED, NO `CI-06` LETTER SPENT, NO GATE ADDED, WIDENED OR WEAKENED, NO TEST SKIPPED, DISABLED OR QUARANTINED, NO MIGRATION TAKEN OR RESERVED.** **The standing bar is UNTOUCHED for the sixth consecutive row** and **no file under any `src/` directory was written at all**. **The container arrived with NO CLONE for the second consecutive wave, and `git rev-parse --is-shallow-repository` reports `true`**, so section 6 is built to need no history: it compares two blobs and uses no `git blame`, `git log` or commit range anywhere. Suite **336 / 8,109 / 10 / 0** against a base of **336 / 8,107 / 10 / 0** reproduced on `c5648f00` before the first edit, **plus two by ADDITION** (`4.4` and `4.5`) and zero on every other axis; gates **33 of 33**, invariants **35 of 35**, typecheck, lint and `format:check` clean. -->

---

### 1. The objective

Take [ADR-388](../decisions/ADR-388.md) section 10's **option (d)** on [`CRON_INVENTORY.md`](../ops/runbooks/CRON_INVENTORY.md), which [ADR-389](../decisions/ADR-389.md) ruled right about the page and refused as not takeable by a row that owns only the runbook. Carry the three constraints that entry derived by seeding, write the leg its section 12 item 2 owed, and re-derive every figure the dispatch handed over.

### 2. Done

- **The restructure.** Columns one to four stay a table; column five becomes a peer `## Severity if absent, by job` section of **21** `### <job>` notes, keyed by the job name normalised exactly as [`gates.mjs`](../../scripts/corpus/gates.mjs)'s `normJob` normalises it. Emitted by script, not transcribed. **Columns one to four are byte-identical across the move and all 21 note bodies are byte-identical to the cells they came from**, both checked by parsing both blobs rather than by reading.
- **The eleven assertions repaired**, in `apps/api/test`, `apps/worker/test`, `packages/db/test` and `packages/tooling/test`.
- **Cases `4.4` and `4.5`** added to [`schedule.test.ts`](../../apps/worker/test/schedule.test.ts): notes bound to rows in both directions with no empty bodies, and the scheduled section required to terminate before the notes.
- **[ADR-392](../decisions/ADR-392.md)**, `proposed`, approval line **UNSIGNED**.

### 3. The two things worth carrying forward

**THE SPECIFICATION WAS RIGHT AND THE DISPATCH'S OWN ESTIMATE WAS NOT.** Every structural figure in [ADR-389](../decisions/ADR-389.md) reproduced at the character on this base, its enumeration of the eleven matched one for one, and its seed E reproduced in both halves. **The one refuted premise is the dispatch's claim that the citation-repair right was "real work": it is zero work**, because every pointer this diff moves was already four to six lines short before this diff and the three that were exact are the three the move cannot touch.

**AND THE SPLIT HAD TO BE MADE CAREFULLY OR IT WOULD HAVE BEEN WORTHLESS.** Four of the eleven asserted phrases that live in **column four**, not column five. Concatenating the row and its note would have turned all eleven green in one line and destroyed the only thing the restructure buys: an assertion meaning *"the dead-man alert names this column"* can no longer be satisfied by the phrase appearing in a paragraph of severity prose.

### 4. Next

- **The `gates.mjs` leg**, written out in full in [ADR-392](../decisions/ADR-392.md) section 11. It needs no new `CI-06` letter and its register is empty on this tree at 21 jobs against 21 notes.
- **[P7](../plans/P7-risk-and-abuse.md) line 407**, `:35` should read `:37`, still unrepaired and row `394`'s file this wave.
- **[session 342](2026-08-28-session-342.md) line 9 and [ADR-337](../decisions/ADR-337.md) line 70**, [ADR-389](../decisions/ADR-389.md) items 4 and 5, untouched.

### 5. Blockers

None. The wave's fencing was sufficient for this row for the first time in three waves.

### 6. Landmines

- **THE NOTES MUST STAY A PEER `##`.** Demoting them to a `###` inside `## Scheduled work` lets `cronRows` run through them, and any note line opening with a pipe becomes a phantom scheduled job that can satisfy a `CI-06l` coverage row whose real job has been renamed away. Measured both ways: **33 of 33 with the defect hidden, 32 of 33 with it visible.** Case `4.5` refuses the shape.
- **THE `RI-35` ANCHOR IS ONE LINE AND NOTHING MECHANICAL PROTECTS IT.** `format:check`'s glob carries no `.md` term and no `docs/` term, so Prettier has never read this file. A hand reflow of the `### Replay self-audit` note body takes `RI-35` to 34 of 35.
- **THE EIGHT ALREADY-WRONG POINTERS INTO THIS RUNBOOK ARE NOW NINETY LINES WORSE AND STILL NOT REPAIRABLE UNDER THE RULE.** Whether the corpus wants a rule that lets somebody fix a drifted pointer is a founder question and this is the third wave to reach it.
- **`4.5` RE-IMPLEMENTS `cronRows`'s BOUND RATHER THAN IMPORTING IT**, because `gates.mjs` is not importable from a test here. If that bound ever changes, `4.5` keeps checking the old one and passes.

### 7. Files touched

- `docs/ops/runbooks/CRON_INVENTORY.md`
- `apps/api/test/internal-ops-constructibility.test.ts`
- `apps/worker/test/detector-runner.test.ts`, `apps/worker/test/expiry.test.ts`, `apps/worker/test/schedule.test.ts`
- `packages/db/test/economic-calendar.test.ts`, `packages/db/test/report-schedules.test.ts`
- `packages/tooling/test/absence-claims.test.ts`
- `docs/decisions/ADR-392.md` (new), `docs/decisions/ALLOCATION.md`, `docs/decisions/README.md`
- `docs/sessions/2026-09-06-session-587.md` (new), `docs/sessions/README.md`, `docs/STATE.md`

**No file under any `src/` directory, no migration, no `scripts/`, no `.github/`, and nothing under `packages/tooling/checks/`.**
