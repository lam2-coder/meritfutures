## 2026-08-17 - Session 48: the last nine rules, and three of them were never blocked (money path)

- **Done:** **All fifty rules now carry an `RE-U-nn` test, and forty-four of fifty are declared.** Nine rules landed: **R-02, R-06 and R-10 as DECLARATIONS**, and **R-01, R-05, R-11, R-17, R-20 and R-32 as tests asserting an absence or a refusal**. `RULE_ASSERTIONS` and `IMPLEMENTED_RULES` are no longer the same set, and `DISCHARGED_ELSEWHERE` is what separates them: M01 section 8.4's coverage rule is over **fifty**, [ADR-048](../decisions/ADR-048.md)'s declared set is over **what the engine computes**, and treating them as one set is what let this suite report a complete `RE-U` series while it was nine short of one. **Eight new seeded mutants**, four in CI-02 and four in CI-01. `pnpm run verify` green: **6 of 6 invariants, 474 tests with 25 skipped, 15 of 15 gates clean and dirty, 16 scope cases**, and `falsify-ci.mjs` reports **37 of 37 watched failing** on a clean tree.
- **Next:** The fixture-wiring session is unchanged as the largest unblocking left in P2: the resolvable-citation `L-nn` rule, the loader's `bigint` comparison, then ADR-048's per-fixture polarity. **`IMPLEMENTED_RULES` is now materially more useful to that session than it was**, because it is 44 rather than 41 and because the six it excludes each name where they are discharged instead.
- **Blockers:** **`INV-06`'s scope** (founder) still blocks `RE-P-01`, unchanged from [session 45](2026-08-16-session-45.md). **R-32's anchor and authoritative column** (founder) still block its operator; the refusal is asserted in the meantime. **The calendar transcription** (founder) still blocks every golden fixture that folds a real day sequence, and **no longer blocks any rule**.
- **Landmines:**
  - **THREE OF THE NINE WERE NOT BLOCKED ON ANYTHING, AND M01 SECTION 3.1's ORDERING TABLE SAYS SO IN ONE LINE EACH.** `DO-1`'s row is cited `R-02, R-06` and `DO-3`'s is cited `R-07, R-10`. **`advance.ts` has carried all three checks since group B landed, citing them by name in its own comments.** R-02 and R-06 were on the undeclared list under "blocked on the calendar transcription", which is true of the rest of group A and true of their **golden files**; R-10 was there under "discharged outside the engine", which is true of R-11 beside it and not of R-10. **Nobody read the DO table against the list.** This is [session 47](2026-08-16-session-47.md)'s own generalisation arriving a third time: a correct observation ("group A needs the calendar") followed by one unchecked inferential step ("so all of group A is blocked").
  - **R-01 AND R-05 WOULD NOT HAVE MOVED WHEN THE CALENDAR LANDED EITHER, AND THAT IS THE MORE EXPENSIVE HALF OF THE SAME ERROR.** Both are stated against `trading_calendar.session_open_at` and `session_close_at`. **`CalendarDay` carries neither column**; it is `{tradingDay, isHalfDay, halted, sequence}`. A transcribed CME year adds **rows**, not columns. R-01 is a containment lookup over a fill's execution timestamp and **`DailyMark` carries `fillCount` and no fill and no instant**. So a session arriving after the transcription to "finish group A" would have found two rules it could not write and no statement anywhere of why. They are now filed against ingest and `0032` instead, with the reason in `DISCHARGED_ELSEWHERE` where a reader hits it before starting.
  - **THE ABSENCE TESTS WERE WRITTEN AS `Object.keys` CHECKS FIRST AND THAT VERSION WAS WORTH NOTHING.** `Object.keys` reads what the **fixture** constructed, so a `supersededBy` added to `DailyMark` and left unset by `mark()` passes it silently, which is precisely the change R-11 exists to notice. All three are now **compile-time**: a `Record<keyof DailyMark, true>` for R-01, a `Record<keyof CalendarDay, true>` for R-05, and an `Extract<keyof DailyMark, 'supersededBy' | 'superseded_by'>[]` for R-11, which resolves from `never` to a real union the moment the field exists. That is `PlanConfigVersionIsClosed`'s and `CalendarSliceIsData`'s idiom one directory over, **and it is what made a seeded mutant possible for all three**: without it there was no single edit that could turn them red.
  - **R-17's MUTANT IS IN CI-01 AND NOT CI-02, AND THE PLACEMENT IS THE FINDING.** Every other rule is asserted by a comparison a unit test runs. R-17 is "config-supported and **unimplemented**", so what discharges it is that `intraday_trailing` **cannot be written down**: `DrawdownType` has two members and `RE-U-017` pins that with `@ts-expect-error`. **Vitest cannot evaluate a type error at all** -- it runs transpiled code and the directive is gone by then -- so widening the union is watched failing against `tsc`. The same is true of R-01, R-05 and R-11. **Four of this session's eight mutants are in the typecheck stage**, which is a shape CI-02's list did not previously have.
  - **`GAPPED_SLICE` IS A SYNTHETIC SLICE AND ITS NAME IS CHOSEN SO IT CANNOT BE READ AS A CME CLAIM.** `CME_WINDOW`'s five days are consecutive, so **the difference of the sequences and the difference of the dates are both 4** and an engine written either way passes: the window cannot tell R-02's "`sequence` subtraction, never date arithmetic" apart from the thing it forbids. The new slice holds three of five covered days, so subtraction answers **2** where date arithmetic answers 4. **This is not TR-01's forbidden move**, on `calendar.ts`'s own established reading: what TR-01 forbids is writing down which days the exchange trades, and what is asserted here is a property of the engine's arithmetic over whatever slice a caller supplied. The days are omitted to make two answers disagree and for no other reason, and the file says so.
  - **`RE-U-032` IS HONESTLY SHORT OF SECTION 8.4's SENTENCE AND SAYS SO IN THREE PLACES.** Section 8.4 wants "the operator at the boundary on both sides"; R-32's operator is `>` against elapsed trading days and **nothing here exercises it**. What the test asserts is the **refusal** at its own boundary: `max_days` null folds, `max_days` set refuses, **at two values rather than one**, because a reader who saw only the 30-day case could believe the rule was implemented and the account simply had not expired. The gap is recorded in the test, in `DISCHARGED_ELSEWHERE` and in `src/rules.ts` rather than closed by a green count.
  - **A STALE CLAIM SESSION 47 WITHDREW WAS STILL LIVE IN TWO FILES.** "R-32's field is a column on `rule_states`, so it is a schema delta" was withdrawn in session 47 and corrected in `progression.ts` and STATE.md; **`src/rules.ts` and `implemented-rules.test.ts` still carried it**. Both now carry the corrected reading (one field on `DayInput`, an M01 section 2.1 amendment, and two genuinely unruled things). **A withdrawal that reaches three of five sites is a withdrawal that will be re-inherited**, which is the same mechanism as the held-back fixture rows in session 47's other log.
  - **`falsify-ci.mjs` COUNTS ITS OWN TREE-CHANGE CHECK AS A GATE, AND A DIRTY TREE MAKES IT REPORT ONE SHORT.** Two runs this session reported "34 of 35" and "35 of 36" with **every mutant listed as `failed as required`**. The missing one was the harness noticing the tree differed before and after, because the working tree was edited **while it ran**. It is not a false positive: the check is right, the run was contaminated, and the verdict is worthless from a dirty tree. **Do not run vitest or edit files while `falsify-ci.mjs` is running**, because it seeds mutants into `src/` in place.
- **Files touched:** `packages/rules-engine/src/rules.ts`, `packages/rules-engine/test/rules-{a-calendar,b-marks,c-floor,e-progression}.test.ts`, `test/{rule-coverage,implemented-rules,fixtures-in-code}.ts`, `scripts/ci/falsify-ci.mjs`, `docs/STATE.md`, `docs/sessions/`.

### The count, reported rather than implied

**Fifty of fifty titled. Forty-four of fifty declared.** The gap is six and every one of the six names where it is discharged.

| Not declared | Discharged by | Watched failing on |
|---|---|---|
| R-01 | Ingest. `DailyMark` carries `fillCount` and no instant | `CI-01/engine-R-11` |
| R-05 | `trading_calendar.session_open_at`/`session_close_at`, and `0032` | `CI-01/engine-R-05` |
| R-11 | The caller's `superseded_by is null`, and replay forward | `CI-01/engine-R-11` |
| R-17 | `DrawdownType`'s closed union today; CV-01 at publish when `validatePlan` exists | `CI-01/engine-R-17` |
| R-20 | M02's setpoint push (`DEP-M2-03`). The engine owes the number, not the push | `CI-02/engine-R-20` |
| R-32 | **Nothing.** The refusal is implemented; the anchor and the authoritative column are unruled | `CI-02/engine-R-32` |

**Every rule in the table has a mutant**, which was not true when the session started and is the reason the compile-time rewrite was worth doing.

### What is NOT here

**No golden fixture, and no `EC-nnn`.** P2 section 2 alternates fixture and engine sessions: "a session doing both has derived its expectations from its own output."

**No ADR**, for the reason sessions 44, 45 and 47 each gave: a defect in a frozen document is the founder's, and a session that writes an ADR to resolve a contradiction on its own path is a session grading its own work. **This session found no new contradiction in M01**; what it found was three claims in *this repository's own comments* that M01 refutes, and those were corrected in place because a comment is not a frozen document.

**CI-03's polarity is untouched**, for the fifth session running. `engineIsIdentityStub()` still governs and every golden fixture is still asserted to fail. ADR-048's stated prerequisite is still unwritten.

**`RE-P-01`, `hash.ts`, `SD-08` and `replay` are absent**, and `RE-P-01` was explicitly this session's not to write.
