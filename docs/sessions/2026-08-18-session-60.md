---
status: approved
depends_on: [README.md]
last_updated: 2026-08-18
---

## 2026-08-18 - Session 60: the wave plan, and the numbers reserved before the sessions that spend them (non-money, planning only)

**Written as a stub first, so the session was on the record before it had a result, and completed in
place.** The fence is `docs/plans/WAVE-01-post-freeze-parallel-sessions.md`,
`docs/decisions/ALLOCATION.md`, `docs/INDEX.md`, `docs/STATE.md` and this file. **No gate, no test,
no migration and no engine file was touched.**

- **Objective:** turn the 2026-08-18 handoff into a runnable wave: verify its claims against the
  tree, enumerate what is actually unblocked, **reserve every registry number the wave spends before
  a session starts**, and write the prompts. Planning only.

- **Done: [WAVE-01](../plans/WAVE-01-post-freeze-parallel-sessions.md) is `approved`, ten sessions
  with six of them concurrent, each prompt complete and pasteable.** Three `CI-06` letters reserved
  (`r`, `s`, `t`) and session-log numbers 61 to 70 allocated. **17 of 17 gates pass.**

  | | |
  |---|---|
  | **The fences are what make six concurrent true** | The gates lane (`G1` to `G4`) is **serial**, because all four write `gates.mjs` and `falsify.mjs` and the PR #7 / PR #8 reconciliation is what two branches independently writing that one file already cost. The three `packages/rules-engine/test` sessions are fenced to **files that do not exist yet** and may edit no existing test and no existing generator. `R1` runs **last and alone** because its objective is editing the lines every other session appends to |
  | **The numbers were reserved before the document that spends them** | `r` and `s` in their own commit, ahead of the plan. **The rows were verified to PARSE as claims rather than as prose**, `allocatedLetters()` returning `a` to `s`, because a row that reads like a reservation and reserves nothing is this registry's own recorded failure |
  | **Session-log numbers are allocated in the plan** | That registry has no allocation table. [README](README.md) carries **two rows numbered 58 and two numbered 59** today, which is ADR-034's race one registry over for the second time. `CI-06n` cannot see it because every one of those rows resolves |

### Three of the handoff's claims did not survive the tree, and one changes what to build

| Claim | The tree |
|---|---|
| "136 edge cases" | **157.** 136 is a **file** count; `EC-012-to-033` carries twenty-two entries as rows in one file. `CI-06g`'s span is right. **The gate was suspected and the report was wrong** |
| "The remaining group A rules, four still blocked" | **Right in count, wrong in cause.** No calendar transcription clears any of them: R-01 needs an instant `DailyMark` does not carry, R-04 a halted flag written as a constant, R-05 session bounds `CalendarDay` does not have, R-06 a clock `INV-01` forbids. **Reaching the first three is a change to the engine's input types, which is an ADR and not a fixture session** |
| Next action 6, `ENGINE_GATE_LEAVES` untied | **Largely discharged.** [`state-hash.test.ts:452`](../../apps/worker/test/state-hash.test.ts) walks a representative value, asserts both directions, and exercises the walker on a **widened** value. `W1` is told to judge the residual optional-field case and record it, **not to build it** |

### The defect the plan's own append found, which is the session's most useful output

**Appending to [STATE](../STATE.md) made `CI-06g` fail, and the cause was two days old.** Line 1404
described a `falsify.mjs` seed by **writing a span's opener out in full and never closing it**. An
opener with no closer anywhere after it does not match, so the gate skipped it **in silence**. It was
invisible for exactly as long as it was the last such token in the file, and the first section
appended below it supplied the closer it had been waiting for: 10,062 characters became its value.
**Every one of the wave's ten sessions would have hit it**, because every one of them appends there.

**Then the reservation row for the gate against it made the identical mistake**, spelling the opener
out while reserving `CI-06t` against spelling the opener out. `CI-06g` caught that one within the
minute. **Fifth instance of the span-parser class**, and the G4 prompt now instructs the gate's own
`covers` line and STRATEGY row to name the tokens rather than spell them.

- **Next:** run rank 1. `G1` (61), `E1` (62), `E2` (63), `E3` (64), `W1` (65) and `F1` (66) start
  concurrently from `origin/main`; `G2` (67), `G3` (68) and `G4` (69) follow the gates lane in order;
  `R1` (70) runs last and alone. **The prompts are section 4 of the plan and need no editing.**

- **Blockers:** none for the wave. **Six of the nine items the corpus waits on are founder or
  third-party actions**: [ADR-056](../decisions/ADR-056.md)'s signature (which blocks `RE-P-02` and
  the crossing fixture), [ADR-058](../decisions/ADR-058.md)'s, the
  [unsigned-ADR audit](../decisions/gates/unsigned-adr-audit-2026-08-18.md)'s recommended split,
  `OI-01`, `OI-06`, and the CME retrieval cadence.

- **Landmines:**
  - **An unclosed generated span is invisible until it is not the last one in the file.** `CI-06g`
    reports nothing; the next append is what detonates it, and the session that appends is never the
    session that caused it. `CI-06t` is reserved for the gate. Until it lands, **a document that
    describes a span must name it, never spell it**.
  - **Group A is not a calendar problem and reads exactly like one.** Batch 9's objective named four
    group A rules as newly writable and not one of the four was; the handoff repeated it four days
    later; this plan is written to stop the third occurrence.
  - **The session-log registry has now raced twice** and still has no allocation table. WAVE-01 is
    one for this wave only, which is a patch and not a fix.
  - **`STATE.md` is the wave's one shared file.** Every prompt carries "append one section at the
    END, edit no existing line, do not touch Next 3 actions" for that reason. `W1` is the session
    best placed to correct next action 6 and is explicitly told not to.

- **Files touched:** [WAVE-01](../plans/WAVE-01-post-freeze-parallel-sessions.md) (new),
  [ALLOCATION](../decisions/ALLOCATION.md) (three letter rows), [INDEX](../INDEX.md) (one row),
  [STATE](../STATE.md) (one appended section, plus **one existing line repaired**: 1404's unclosed
  span, which is the only existing line this session edited and the reason is above),
  [sessions/README](README.md) (one row), this file.
