## 2026-08-16 - Session 38: FOLD-02 sessions 6 and 7, the registries and `CI-06l`

- **Done:** [FOLD-02 §8](../plans/FOLD-02-enforcement-window-and-suspension.md)'s registries session, **plus session 6, which had not run.** The corpus now carries **<!--gen:gate_count-->21<!--/gen--> gates**. Non-money throughout: no migration, no `DELTA_MANIFEST` row, no ADR, no allocation row.
- **Done:** [FOLD-02 §8](../plans/FOLD-02-enforcement-window-and-suspension.md)'s registries session, **plus session 6, which had not run.** The corpus now carries **<!--gen:gate_count-->21<!--/gen--> gates**. Non-money throughout: no migration, no `DELTA_MANIFEST` row, no ADR, no allocation row.
  - **The pre-flight check the brief asked for came back non-clean and it mattered.** No sibling was *running* session 7, but [session P](https://claude.ai/code) was **blocked one confirmation away from it**, on `main`, holding no branch. Raised before writing anything; the founder ruled this session takes it. **`CI-06k` confirmed on `main`** (PR #34, `9a77b2a`), so the branch is fresh off a tree that carries it.
  - **Session 6 had not run, and the check is the reason this session knows.** The brief said to check rather than assume. The check was a grep: **no ToS clause, no [GUIDE_BRIEFING](../GUIDE_BRIEFING.md) line, no [API_CONTRACT](../architecture/API_CONTRACT.md) type and no [EVENTS](../architecture/EVENTS.md) row carried `held_pending_review`.** What had merged as "session 6" was **FOLD-01's**, one fold over, and the two folds' session numbers had been running in parallel for three days. **Assuming would have shipped session 7 on top of a hole.**
  - **[`CI-06l`](../testing/STRATEGY.md), every expiry has a sweep.** Every `*_expires_at` column the migrations declare is dispositioned **exactly once** in [CRON_INVENTORY](../ops/runbooks/CRON_INVENTORY.md): in the coverage table against a release job **that is itself a row of that document's scheduled table**, or on the written exemption list **with a reason**. Reads the DDL and one document, no database.
  - **[CRON_INVENTORY](../ops/runbooks/CRON_INVENTORY.md):** the freeze-expiry sweep row now names **three clocks and is still one row**; the **nightly hold and freeze expiry assertion** is its own row, S1, unsuppressible; and the coverage table plus the exemption list are the gate's input.
  - **[GOLDEN_SCENARIOS](../testing/golden-scenarios/README.md):** new **[section 35, GS-273 to GS-284](../testing/golden-scenarios/35-gs-273-to-gs-284-the-enforcement-window-and-identity-restriction.md)**, range derived from the registry maximum. The brief's eight, plus GS-276 (the sweep reporting success), GS-278 (the widened no-in-flight predicate), and the **M03 and M08 scenarios owed in words**. **`GS-114`'s row now names four unsuppressible alarms.**
  - **[EDGE_CASES](../edge-cases/README.md):** `EC-150` to `EC-156`, and **[EC-040](../edge-cases/EC-040.md) amended**. **Four of the seven are one shape** and it is the fold's whole finding: an artifact that reads as a control and is not one. **`EC-156` is the one to read**, because there the entire vocabulary of a halt existed as columns on `wallet_withdrawals` and **nothing refused settlement**.
  - **[TOS_CLAUSES](../legal/TOS_CLAUSES.md):** clause 5 now enumerates **three** enforcement outcomes rather than two, clause 13 carries the 48 hours, and **new section 8** drafts both against the exact risk in clause 8's idiom. **Section 6's "and no review state" is amended**, which is one of [ADR-040](../decisions/ADR-040.md)'s ten sites. **[GUIDE_BRIEFING](../GUIDE_BRIEFING.md) item 4** is another, and it was the only site stating zero denial in full.
  - **[EVENTS](../architecture/EVENTS.md):** eight rows. `payout.held`, `payout.hold_released`, `payout.hold_enforced`, `payout.expiry_overdue`, the two wallet halt events, and **`identity.restriction_lifted`**, which unblocks the three consumers [session 36](2026-08-16-session-36.md) found with a working hold half and no release half.
  - **[API_CONTRACT](../architecture/API_CONTRACT.md):** the status union, `approved_at` becoming nullable, the `hold` block with `resolves_by`, the **`identity_restricted` code defined**, and `GET /me`'s `restriction` block.
  - **All 14 gates pass clean and fail dirty, 11 scope cases hold, and `generate` ran before the commit.**
- **Next:** **FOLD-02 is complete through session 7.** Session 8 (DELIVERY_PLAN and M15's scope move) **already landed** as [session 34](2026-08-16-session-34.md), out of order, so the fold's remaining work is the founder's **E2 read on `0030` and `0031`**. **Two sessions are owed and neither is this one's**: the M01 sweep and the keep-both dedup, both money path, both named below. **Do not start S-E session 5 from a tree that predates this one**: `gates.mjs` and `falsify.mjs` were both edited again.
- **Blockers:** None. `OQ-M2-06`, `OQ-M6-05` and `OQ-M8-05` remain open and none of them blocked anything here.
- **Landmines:**
  - **`CI-06l`'s FIRST RUN FALSIFIED THE RUNNER, NOT THE CORPUS, AND THE COLUMN IT WAS WRONG ABOUT WAS `hold_expires_at`.** The gate reported the hold's own coverage row as a **stale entry naming a column no migration declares**, which is a real finding shape pointed at a true row. **`columnCatalogue()` read one column out of a multi-column `ALTER TABLE`**: the expression required `ADD COLUMN` to follow the table name immediately, so [`0031`](../../packages/db/migrations/0031_payout_hold_and_identity_restriction.sql)'s five hold columns contributed `held_at` and nothing else and **four were invisible to the runner**.
    - **It is `CI-06j`'s catalogue, not `CI-06l`'s**, so this had been live since `0031` landed. **It survives because it fails in the safe direction**: a column missing from the catalogue makes `CI-06j` report a **live trigger reference as a phantom**, which is a false finding somebody investigates rather than a false pass nobody sees. **No trigger has named one of the four yet.** The first guard written over the hold fires it, and that is a money-path session meeting a gate that is wrong about the schema.
    - **Widening a catalogue can only remove `CI-06j` findings, never add one**, so the fix is one-directional and was taken. **Fifth gate found with a parser narrower than the property it claims**, after `CI-06a`'s 109 phantom anchors, `CI-06e`'s 119 phantom refless entries, `CI-06k`'s whole-matrix false report and `CI-06b`'s blindness to a duplicate frontmatter key. **No ordinal is claimed beyond that**, and it is deliberately **not** folded into [ADR-034](../decisions/ADR-034.md)'s tally, which counts hand-maintained numbers and is a different class; the first draft of the STRATEGY subsection ran the two together and said "nineteenth", which was two counters added to each other.
  - **THE FIRST DRAFT OF THIS SESSION'S OWN GATE DOCUMENTATION CARRIED THREE WRONG SECTION CITATIONS, AND NO GATE COULD SEE ANY OF THEM.** `CI-06a` resolves **links and anchors**; a citation written as prose (`M06 section 6.2`, `M16 section 7`) resolves to nothing and is checked by nobody. All three were caught by reading the target files, and all three were **plausible**: the M06 unsuppressible-limit paragraph is in `AS-M6-03` and not a numbered subsection, and both alert lists are section 9 rather than 7 or 8. **A fourth was caught the same way**: M06's three owed scenarios are in **§8.3**, not §8.2, and §8.2 is where the `GS-114` divergence note lives, so the two are one subsection apart and the wrong one reads correctly. **This is the gap `CI-06a` declares and it is wider than it sounds**, because the corpus's convention is to cite by section number in prose, and prose section numbers move whenever a section is inserted.
  - **`CI-06l` FOUND A SCHEDULED JOB THAT DOES NOT EXIST, AND IT IS NOT CLOSED.** `plan_breaker_state.override_expires_at` and `otp_send_budget.override_expires_at` are both exempted on the same ground: the row is keyed by evaluation date and **a later evaluation does not carry an expired override forward**, so the override lapses by being recomputed. **The recomputation is a scheduled job with no row in the cron inventory.** Neither exemption is wrong and both rest on a job that CRON_INVENTORY's own opening rule says does not exist until it has a row: *"a job in this table without a dead-man switch is a job that does not exist."* **Written into the document as a named gap rather than papered over**, because adding a scheduled job to the estate is a change to the estate and this session's fence is a gate.
  - **`identity_restriction_episodes.sla_due_at` IS A CLOCK THE GATE CANNOT SEE, AND WIDENING THE PATTERN WOULD MAKE THE GATE WORSE.** It is swept and alarmed like the other three; it is simply not named `*_expires_at`. Matching any timestamp would catch every `starts_at`, `verified_at` and `created_at` in the schema, and **a gate that fails on correct DDL is a gate that gets switched off**, which is the trade `CI-06g` already records for the bare-numeral sweep. **Declared in the `covers` line and in CRON_INVENTORY rather than fixed badly**, and it is the one place where a reader could infer from a green `CI-06l` that every clock is covered.
  - **A SEED THAT DELETES A ROW CANNOT NAME THE ROW IT DELETED, AND THE HARNESS SCORED THAT AS THE GATE'S FAULT.** `falsify.mjs` resolves `expect` against the **seeded** tree, after the seed runs. The first `CI-06l` seed deleted the first coverage row, so `firstCoveredColumn` then returned the **next** column and the case reported `FAILED OFF-TARGET` while the gate was naming exactly the column that had been broken. **Two readers now exist on purpose**: the gate's column reference is anchored and strict, the harness's is loose and finds the same row before and after the edit, and **they must not be the same function**. The same off-by-one hit the `CI-06l/exempt` control, which reported `CONTROL DID NOT FIRE`. **Both were the harness describing its own target wrongly**, which is the third distinct way this file has been watched being wrong about a gate that was right.
  - **THE `payout.expiry_overdue` EVENT BREAKS [EVENTS](../architecture/EVENTS.md)' OWN DELIVERY RULE AND THE EXCEPTION IS WRITTEN DOWN.** That document says events are written in the same transaction as the state change that caused them, so an event exists **if and only if** the fact does. This event records **that a fact did not happen**, and there is no transaction for an absence. Stated at the point of the exception rather than left for a reader to notice the contradiction, because the alternative was to silently weaken the rule for one row.
  - **`wallet.withdrawal_sent`, `wallet.withdrawal_settled` AND `wallet.withdrawal_failed` ARE NAMED BY [STATE_MACHINES §3.2](../architecture/STATE_MACHINES.md)'s TRANSITION TABLE AND ARE STILL NOT IN THE CATALOGUE.** This session added the first `wallet.*` rows EVENTS has ever carried, which is how the absence became visible, and **it deliberately did not invent the other three**: they belong to a seven-state machine [ADR-040](../decisions/ADR-040.md) explicitly declined to draw, and shaping three events for it here is how a catalogue acquires rows nobody ruled on. **[M20](../plans/M20-wallet.md)'s to close.**
  - **THE M01 SWEEP IS STILL NOT ANYBODY'S.** ADR-040 named four unswept `transferring` sites; sessions 4 and 7 have now closed all four **except M01's two**, and M01 is not in FOLD-02 section 7's file list at all. `SD-09`'s predicate has moved twice without M01 saying so, and `hasPayoutInFlight`'s comment still reads `approved | transferring | frozen`. **The one document that must not disagree about which requests are outstanding is the engine's.** Money path, needs a session.
  - **THE GATE LETTER WAS VERIFIED AFTER THE GATE WAS WRITTEN, NOT BEFORE, AND THE ORDER IS THE WHOLE POINT OF THE TABLE.** `l` was free and claimed by [FOLD-02](../plans/FOLD-02-enforcement-window-and-suspension.md) section 4.4 in [ALLOCATION](../decisions/ALLOCATION.md), and it is claimed by this branch on **every** remote ref, so nothing collided. **That is luck rather than process.** The table exists so a letter is claimed before the artifact is written, on [ADR-034](../decisions/ADR-034.md)'s rule, and this session read the gate letter out of the fold plan's prose and started writing. **The plan's prose and the table agreed, which is exactly the condition under which skipping the check is invisible**: `ADR-038` collided this week because two branches each trusted their own copy.
  - **The State column had drifted on two rows and one of them was stale on `main`.** `k` and `l` both read "reserved, unwritten"; `CI-06k` merged in PR #34 and `CI-06l` is written here. **Corrected in this commit**, and it is the fourth recorded instance of that column drifting after `ADR-035`'s row, `ADR-036`'s row and the `035` correction. **Neither allocation gate parses the State column**, which STRATEGY section 4.4 already names as its fifth declared gap: the Numbers column is load-bearing and checked, and the State column is prose for a reader and decays like all prose. **This is the argument for the cheap gate that table's own closing paragraph proposes**, uniqueness and gaplessness over the letters, and it would still not have caught this, because a stale State cell is not a numbering defect.
  - **Session 38 was claimed by reading the remote branches, and 35 was left alone on purpose.** 34, 36 and 37 are taken and **35 is free but reserved in spirit** by [session 36](2026-08-16-session-36.md)'s note, which moved to 36 so two earlier claimants could settle on 34 and 35. `git ls-tree` against all nine remote branches shows none claims 35, 37 or 38. **Session numbers still have no allocation table**, which is [ADR-034](../decisions/ADR-034.md)'s race one registry over and is now the fourth session in a row to spend a step on it.
- **Files touched:** [CRON_INVENTORY](../ops/runbooks/CRON_INVENTORY.md), [STRATEGY](../testing/STRATEGY.md), [GOLDEN_SCENARIOS README](../testing/golden-scenarios/README.md) plus [section 35](../testing/golden-scenarios/35-gs-273-to-gs-284-the-enforcement-window-and-identity-restriction.md) and [section 12](../testing/golden-scenarios/12-gs-112-to-gs-117-admin-and-ops-console-m6.md), [EDGE_CASES README](../edge-cases/README.md) plus EC-150 to EC-156 and [EC-040](../edge-cases/EC-040.md), [TOS_CLAUSES](../legal/TOS_CLAUSES.md), [GUIDE_BRIEFING](../GUIDE_BRIEFING.md), [EVENTS](../architecture/EVENTS.md), [API_CONTRACT](../architecture/API_CONTRACT.md), [INDEX](../INDEX.md), [STATE](../STATE.md), [`gates.mjs`](../../scripts/corpus/gates.mjs), [`falsify.mjs`](../../scripts/corpus/falsify.mjs).
