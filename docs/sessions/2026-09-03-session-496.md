---
status: approved
depends_on: [README.md]
last_updated: 2026-09-03
---

## 2026-09-03 - Session 496: the door has no caller, so it gets a type, and every refusal it had it still has

<!--index: **THE RULING IS [`ADR-303`](../decisions/ADR-303.md), `proposed` and UNSIGNED, `E2` READ OWED AND NOT PERFORMED. MONEY PATH, AND IT HAS A `src/` DIFF.** [ADR-299](../decisions/ADR-299.md) ruling 1 IS BUILT: `catalogRows`, `catalogRowsWhere` and `catalogRowAt` return `CatalogRow<K>`, which is `(typeof TABLES)[K]['$inferSelect']`, instead of `unknown`. **THE STOP CONDITION WAS TWO-SIDED AND THE SECOND SIDE WAS MEASURED RATHER THAN CLAIMED**: the refusal inventory of [`catalog-read.test.ts`](../../packages/db/test/catalog-read.test.ts) was derived from the file BEFORE the first edit, re-derived after, and compared as sets. **BEFORE MINUS AFTER IS EMPTY.** Twenty cases existed and twenty pass unchanged, three were added, **no case was deleted, skipped, relaxed, renamed or retargeted**, and no count fell. **THE GUARD ORDER IS UNTOUCHED**: `refuseUncatalogued` is still the first statement of all three verbs. **THE TYPE HALF IS CHECKED BY `tsc` AND NOT BY VITEST**, because `packages/db/tsconfig.json` includes `test/**/*.ts`; four type aliases pin [ADR-299](../decisions/ADR-299.md) section 5.1's LIMITS (the `jsonb` stays `unknown`, the four scoped read verbs stay `unknown`, a nullable column keeps its null, the row is neither `unknown` nor `any`) and **THREE WERE WATCHED RED**. **THE RUNTIME GUARDS DID NOT COME OFF AND `CatalogRow`'s DOCBLOCK SAYS SO**, which is section 5.1 item 5 carried into the file it governs rather than left in an ADR. **NINE LINE-NUMBERED CITATIONS REPAIRED UNDER THE GRANTED EXTENSION, EACH DERIVED FROM THE CONTENT IT NAMES**: the diff inserts in TWO places, so a citation below the first moved **46** lines and one below the second moved **51**, and `ALLOCATION.md:203` is the one a uniform offset would have broken. **`RI-16`'s OWN NOMINATION FOR IT WAS WRONG TOO**, pointing at a comment mentioning `BEGIN` rather than at `await conn.query('BEGIN')`. **TWO OF THE NINE WERE NOT FLAGGED AND WERE REPAIRED ANYWAY, IN FILES THE RUNNER HAD ALREADY OPENED; THE ENTRY DECLARES IT AS THE ONE PLACE THIS ROW WENT PAST THE LETTER OF ITS GRANT.** **FOURTEEN MORE DRIFTED CITATIONS IN ELEVEN ADRs AND SESSION LOGS ARE LEFT STANDING**, because `RI-16` excludes a dated or session heading by design and rewriting a record would make it say something it did not say. **FIVE LIVE SENTENCES NOW SAY `catalogRowAt` RETURNS `Promise<unknown>` AND ALL FIVE ARE FALSE**, in `accounts.ts`, `payouts.ts`, `wiring.test.ts` and `rule-state-producibility.test.ts`, ALL OUT OF FENCE. **`RI-32`, RESERVED TO THIS ROW IF NEEDED, IS LEFT FREE.** **NO MIGRATION, NO ROUTE BEHAVIOUR, NO ENGINE FILE, NO NEW PACKAGE, NO KEY ADMITTED TO `CATALOG_TABLE_KEYS`, NO WRITE VERB, NO DOOR WIDENED, NO GATE WEAKENED, NO FENCE WIDENED, NO STUB, NO SYNTHESISED DEFAULT, NO FLOAT, NOTHING SIGNED. THE WIRED COUNTS DO NOT MOVE: TEN WIRED OF TWENTY-FOUR DECLARED, FOURTEEN BLOCKED.** Counts derived at reporting time, each command run separately: **33 of 33** gates after `generate`, **32 of 32** invariants off the runner's own last line, **302 files / 7,340 passed / 6 skipped** against a baseline of **302 / 7,337 / 6** measured on this tree with all five files reverted to `6f0efab`. `typecheck` clean, `lint` clean, `format:check` clean. **`pnpm run verify` was NOT run**, forbidden by the row, and `falsify.mjs` was never invoked by any path. **THE BRANCH IS PUSHED AND NO PULL REQUEST WAS OPENED BY THIS SESSION**, which has no GitHub access. -->

---

### 1. The objective

**[ALLOCATION](../decisions/ALLOCATION.md) row `303`: build [ADR-299](../decisions/ADR-299.md) ruling 1, so `catalogRowAt` returns the declared row type instead of `unknown`.** One objective, money path, [ADR-003](../decisions/ADR-003.md) strict.

### 2. What was read before anything was written

`CLAUDE.md`, [STATE](../STATE.md), the tail of [`docs/sessions/`](README.md), then row `303` in full, then [ADR-299](../decisions/ADR-299.md) entire with **section 5.1 read before a line was written**, as the row required. Then every claim was re-derived at source: `catalogRowAt`'s return at `6f0efab`; `grep -c unknown docs/decisions/ADR-233.md` returning `0`; the door's callers; `schema.ts`'s `size_cents` mode and the cap schedule's missing `.$type<>()`; `packages/db/tsconfig.json`'s `include`; and the census of line-numbered citations into `scoped-db.ts`.

### 3. Done

- **`CatalogRow<K>` declared** at [`scoped-db.ts:3455`](../../packages/db/src/scoped-db.ts), derived from `TABLES` and never listed, with a docblock carrying [ADR-299](../decisions/ADR-299.md) section 5.1's four limits into the file they govern.
- **The three verbs return it.** `Promise<CatalogRow<K>[]>` twice and `Promise<CatalogRow<K> | undefined>` once, in the interface and in `scopedTx`.
- **A fifth direction in the suite**: four type aliases, one compiled-not-called function, one tuple binding them, and three runtime cases. **No existing case was touched.**
- **The refusal inventory derived, re-derived and compared**, before and after, by one script.
- **Nine citations repaired**, each from the content it names at its new position.
- **[ADR-303](../decisions/ADR-303.md)**, twelve sections, `proposed`, UNSIGNED. **[ALLOCATION](../decisions/ALLOCATION.md) row `303`** rewritten from a reservation into the landed record, **[STATE](../STATE.md)** and this log.

### 4. Next

**[ADR-287](../decisions/ADR-287.md) slice 5**, which row `303` was dispatched to unblock and which follows this row rather than `302`. It is the first caller this door has ever had.

**And the five false sentences in section 6 below are the first thing a row over `apps/api/src/routes/**` owes.** Two of them are the recorded reason a port is still blocked, and the reason has now changed.

### 5. Blockers

**None reached.** Nothing outside the fence and its granted extension was required for a green tree, so nothing was reported as blocked.

### 6. Landmines

- **THE FENCE FORBADE REPAIRING FIVE SENTENCES THIS DIFF MADE FALSE.** [`accounts.ts:114`](../../apps/api/src/routes/accounts.ts), [`accounts.ts:969`](../../apps/api/src/routes/accounts.ts), [`payouts.ts:424`](../../apps/api/src/routes/payouts.ts), [`wiring.test.ts:794`](../../apps/api/test/wiring.test.ts) and [`rule-state-producibility.test.ts:914`](../../apps/api/test/rule-state-producibility.test.ts) all state that `ScopedTx.catalogRowAt` returns `Promise<unknown>`. **`wiring.test.ts:794` is the sharpest**: this row repaired its line NUMBER and could not repair its CLAIM, so it now points accurately at a line that contradicts it.
- **A UNIFORM OFFSET WOULD HAVE BEEN RIGHT EIGHT TIMES AND WRONG ONCE.** The diff inserts 46 lines above `ScopedTx` and 5 more inside `scopedTx`, so the two regions shift by different amounts. The row warned about exactly this and it was not theoretical.
- **AND `RI-16`'s OWN NOMINATED LINE WAS WRONG FOR THAT NINTH ONE.** For `ALLOCATION.md:203` it nominated `scoped-db.ts:4103`, a comment reading *"`BEGIN` / `COMMIT` / `ROLLBACK` ARE ISSUED ON THE CONNECTION RATHER THAN"*. The statement the row names is `await conn.query('BEGIN');` at **4158**. **The runner finds the nearest occurrence of a TOKEN and a row names a STATEMENT.** A session that trusts the runner's nomination instead of reading the file will write a citation that passes the check and misleads the reader, which is the defect `RI-16` exists to prevent.
- **[`rule-state-producibility.test.ts:918`](../../apps/api/test/rule-state-producibility.test.ts) ASSERTS THIS DOOR'S GENERIC HEAD AS A STRING**, three directories away. It still passes because this row changed the return line and not the head. **A row that renames a generic parameter on `catalogRowAt` goes red there** and the reason will not be obvious from the failure.
- **`CatalogRow` IS NOT RE-EXPORTED FROM [`packages/db/src/index.ts`](../../packages/db/src/index.ts)**, which is out of fence. Nothing is blocked by it today, because inference does not need the name. A caller that wants to NAME the type in a signature needs one line added there.
- **AN UNREFERENCED TYPE ALIAS IS AN ESLINT ERROR IN THIS REPOSITORY.** Type-level cases in a suite must be bound to a value or they fail `lint` even though they pass `tsc`. The tuple `ADR_303_TYPE_CASES` is that binding and the reason is written beside it.
- **THE ROW SAID `273` CITATIONS AND IT IS `272`**, over `105` distinct lines, measured at `6f0efab`. A prose figure in a dispatch rather than an assertion.

### 7. Files touched

| File | Change |
| --- | --- |
| [`packages/db/src/scoped-db.ts`](../../packages/db/src/scoped-db.ts) | `CatalogRow<K>` and its four limits; three signatures and three implementations; two comment lines |
| [`packages/db/test/catalog-read.test.ts`](../../packages/db/test/catalog-read.test.ts) | The fifth direction, additive only |
| [`apps/api/src/routes/payouts.ts`](../../apps/api/src/routes/payouts.ts), [`apps/api/test/wiring.test.ts`](../../apps/api/test/wiring.test.ts) | **Two line numbers each and nothing else** |
| [`ADR-303`](../decisions/ADR-303.md) | New |
| [`ALLOCATION`](../decisions/ALLOCATION.md) | Row `303` rewritten, plus four line numbers in rows that are not `303` |
| [`STATE`](../STATE.md), this log, [`decisions/README`](../decisions/README.md), [`sessions/README`](README.md) | The record and the generated spans |
