---
status: draft
depends_on: [README.md]
last_updated: 2026-08-18
---

## 2026-08-18 - Session 57: `GS-083` written where session 56 routed it, in the `RE-C-nn` suite (money path)

**Written as a stub first so the session is on the record before it has a result.** The fence is
[`packages/rules-engine/test/`](../../packages/rules-engine/test/) and the golden-scenarios registry
row for `GS-083` if it needs a pointer to the new home.

- **Objective:** `GS-083` is `CV-17`: trailing drawdown, floor lock **DISABLED**, cap at or above the
  drawdown, and publishing must **FAIL with `CV-17` NAMED**. [Session 56](2026-08-18-session-56.md)
  established that it cannot be a golden fixture: a publish validation returns a `ValidationResult`
  against `(rules, sizes[])`, `EXPECTATION_KEYS` is `{end_state, events, pins, note}`, the loader
  folds a day stream and diffs a `RuleState`, and `validatePlan` is never called in the fold. It
  routed the row to the `RE-C-nn` suite. Write it there, alongside `GS-076` to `GS-078`.

- **Done:** _(pending)_

- **Next:** _(pending)_

- **Blockers:** _(pending)_

- **Landmines:** _(pending)_

- **Files touched:** _(pending)_
