---
status: approved
depends_on: [README.md]
last_updated: 2026-08-18
---

## 2026-08-18 - Session 57: ADR-056's central claim EXECUTED, and it does not reproduce (money path, test only)
<!--index: ADR-056 executed: the carried-lock counterfactual folded forward -->

**Written as a stub before any work, so the session was on the record before it had a result, and
completed in place.** The fence was [`packages/rules-engine/test/`](../../packages/rules-engine/test/)
and this file.

- **Objective:** [ADR-056](../decisions/ADR-056.md) is `proposed` and asks not to be signed until the
  claim it turns on is **executed rather than argued**: "the second-funded-day breach in the table
  above is a DERIVATION FROM THE `DO` ORDERING, NOT AN EXECUTION ... The signature should follow a
  run, not this paragraph." Run it.

- **Done: THE BREACH DOES NOT REPRODUCE.** It reproduces on no v1 plan, on no funded day, and not for
  the reason the ADR gives. Two files, both green, both watched failing:
  [`adr-056-carried-lock.test.ts`](../../packages/rules-engine/test/adr-056-carried-lock.test.ts) (6
  tests, the walk) and
  [`adr-056-carried-lock.property.test.ts`](../../packages/rules-engine/test/adr-056-carried-lock.property.test.ts)
  (10 tests, the sweep). **783 passing from 767, no other test moved**, 17 of 17 gates, 7 of 7
  invariants, 19 scope cases. **No engine file was edited: `packages/rules-engine/src` is
  byte-identical to `origin/main` on this branch.**

### The result, in the order a reader needs it

**EVERY PREMISE ADR-056 ARGUES FROM IS TRUE, AND ITS MECHANISM IS NOT.**

| The ADR's claim | Executed |
|---|---|
| Every v1 eval pass is also a lock day, so every funded account arrives locked | **TRUE.** The pass day emits `rule.floor_locked`, `phase.passed`, `day.closed`, in that order, on both plans that have an eval phase |
| `floor_lock_floor_at_cents` is `size_cents + 10,000c` and `R-31` writes exactly `size_cents` | **TRUE** on all three v1 plans, resolved from the published rows |
| So a funded account whose floor was re-assigned to the locked value stands 10,000c under water | **TRUE**, and it is the ADR's whole point |
| **`DO-7` "assigns `floor = floor_lock_floor_at_cents` on every day the account is locked"** | **FALSE.** This is the sentence the ruling's table rests on |
| Therefore every funded account breaches on its second funded day, on all three v1 plans | **DOES NOT REPRODUCE** |

**`advanceFloor` ASSIGNS THE LOCKED FLOOR ONCE, ON THE DAY THE LOCK ENGAGES, AND CARRIES IT
AFTERWARDS.** `R-15`'s block is guarded by `!floorLocked` ([`floor.ts`](../../packages/rules-engine/src/day/floor.ts)),
so on an already-locked day the assignment is not reached and `floorCents` is `input.priorFloorCents`
untouched. **The file's own header states the expression form** (`floor(d) = floorLocked ?
floor_lock_floor_at_cents : max(...)`) **and the implementation is stateful**, and the two agree on
every day of every ordinary account, because nothing between the lock day and today ever moves the
floor. **They part company at exactly one place: a state whose floor was moved by something other
than the lock while `floorLocked` stayed true.** That state exists nowhere in the corpus except the
counterfactual this session built.

**AND THE SIGN IS REVERSED, WHICH IS THE MEASUREMENT WORTH READING TWICE.** On the walked stream, with
a funded day two whose low is 4,800,000c:

| | Carried lock (the literal `INV-07`) | Shipped (what the engine does) |
|---|---|---|
| Funded day 1 floor | 4,750,000c, frozen | 4,850,000c, trailed (`R-13`) |
| Funded day 2, low 4,800,000c | **survives**, 4,800,000 >= 4,750,000 | **BREACHES**, 4,800,000 < 4,850,000, shortfall 50,000c |

**The carried lock is STRICTLY MORE PERMISSIVE than the engine it was supposed to be more dangerous
than**, because its floor is frozen at the lowest value the funded phase ever has. The property sweep
puts it beyond one stream: over **3,442 folded funded days across 2,000 generated streams**, with
**1,790 shipped breaches and 1,485 carried breaches**, the carried lock breached on **zero** days the
shipped fold survived. The reason is structural rather than statistical: both accounts leave `R-31`
with the same floor, the carried one is frozen there by the two `!floorLocked` guards, and the
shipped one is non-decreasing from there by `R-13` and `R-14`, so `R-18`'s comparator is at or below
the shipped one on every day.

**WHAT ACTUALLY HAPPENS UNDER THE CARRIED LOCK IS ADR-056's OTHER ARGUMENT, AND THAT ONE REPRODUCES
EXACTLY.** The funded floor freezes at `size_cents` minus the funded drawdown and never moves again:
it never trails a cent of the profit, and the lock can never re-engage, because `R-15`'s trigger is
guarded by the same flag. **The sweep watched the shipped account re-lock 232 times and the carried
account not once.** The ADR's words for it are still the right ones: "That is not a constrained
funded floor. It is the funded floor deleted." **The ruling's conclusion survives its own table.**

### Three narrowings the run produced, each smaller than the one above it

**"ALL THREE V1 PLANS" IS TWO.** DIRECT-50K carries `phase_eval.enabled: false` (Appendix A.3) and a
null `profit_target_cents`, so `resolvePlan` gives it no eval rules, `initialState` opens it
**funded**, and **`R-31` never runs on it at all**. The consequence for Direct is not that the breach
fails to reproduce; it is that the transition `INV-07` is being amended against does not exist there.
A Direct state claiming the eval phase is refused (`eval_phase_without_eval_rules`), which is the
executable form of it and is pinned.

**EVEN GRANTING THE MECHANISM, "EVERY FUNDED ACCOUNT ON ITS SECOND FUNDED DAY" IS CONDITIONAL.**
Measured under the mutant, on CORE-50K: a funded day one that closes **flat** or **+9,999c** breaches
on day two; **+10,001c does not**, because the balance has cleared the locked floor and the day's low
can stay above it. The ADR's table assumes the balance is still `size_cents` on day two, which is
true of the account that made no money on its first funded day and of no other. **This narrowing is
moot while the mechanism is false**, and it is recorded because a re-derivation of the ADR on other
grounds would inherit it.

**NEITHER THE FOLD NOR THE SCHEMA ANNOUNCES THE CARRIED LOCK.** `R-13`'s guard freezes `hwb` at
`size_cents`, so every profitable funded day leaves the high-water balance **below the account's own
balance**, on 2,000 of the swept days. [`0037`](../../packages/db/migrations/0037_supersede_rule_states_high_water_bounds_balance.sql)
replaced `0015`'s unconditional bound with `floor_locked OR high_water_balance_cents >=
balance_cents`, so **a locked row is EXEMPT and the database stores it without complaint**. The
literal reading produces no breach, no assertion, no refusal and no rejected row. It fails silently
and permanently, in the trader's favour and against the house.

### Next

**The signature is the founder's and this run does not give it.** What the run changes is what the
signature would be ratifying:

- **`ADR-056`'s "Why ADR-052 makes this sharper than it was" section and its three-row table are
  FALSE AGAINST THE FOLD and should be amended or withdrawn before the ADR is signed.** The sentence
  that fails is "under ADR-052 `DO-7` **assigns** `floor = floor_lock_floor_at_cents` on every day the
  account is locked". The table's first row is right, its second row is wrong at the last column, and
  its third row does not happen.
- **The ruling itself is still supported**, on the argument the ADR makes immediately before that
  section and which this run reproduces: `R-13`'s guard freezes `hwb`, the floor can never trail, the
  funded lock can never re-engage, and the funded floor is deleted. **It is a different and gentler
  defect than the one the ADR leads with**, and the "Alternatives considered" entry for carrying the
  lock should say so: it is not that the literal reading breaches every funded account, it is that it
  hands every funded account a floor frozen at its opening value for life.
- **[`0037`'s item 4](../../packages/db/migrations/0037_supersede_rule_states_high_water_bounds_balance.sql)
  is the other thing waiting on this signature**: no cross-row `hwb` freeze trigger was written
  because "`INV-07` versus that reset is an OPEN FOUNDER ITEM. A trigger would have to take a side on
  it." The ruling closes that.
- **The fixture ADR-056 names stays unwritten**, and is unblocked by acceptance rather than by this
  run.

- **Blockers:** none. Nothing in the fence was blocked and nothing outside it was touched.

### Landmines

- **THE ADR's MECHANISM CLAIM IS A CORRECT READING OF THE FILE'S HEADER AND A WRONG READING OF ITS
  CODE, WHICH IS WHY IT SURVIVED A SESSION AND A REVIEW.** [`floor.ts`](../../packages/rules-engine/src/day/floor.ts)
  opens with `floor(d) = floorLocked ? floor_lock_floor_at_cents : max(...)`, which is a **stateless
  expression over the day**, and the implementation is a **stateful carry**. Anyone deriving the
  counterfactual from the header gets ADR-056's answer; anyone running it gets this one. **The two
  forms are observationally identical on every account the corpus admits**, so no fixture, no property
  and no gate can tell them apart, and the disagreement is only visible in the one state the corpus
  has not decided is reachable. **This is the shape of defect the constitution's own caution is
  about**: not a capability failure, a failure to check a claim against the primary source, where the
  primary source here is the executable and not the comment above it.

- **THE COUNTERFACTUAL WAS CONSTRUCTED BY FLIPPING ONE FIELD ON THE FOLD's OWN OUTPUT, AND ANY OTHER
  CONSTRUCTION WOULD HAVE MEASURED A DIFFERENT ENGINE.** `R-31`'s effects list names phase, `balance`,
  `hwb`, `floor`, the counters and the consistency period start, and does not name `floorLocked`, so
  the literal reading is that the flag is carried and **every other field is exactly what the shipped
  fold computed**. `DO-7` set it true on the pass day and `DO-8` set it back to false in the same
  fold, so **the flip restores a value the engine itself produced and then overwrote**. Editing
  `progression.ts` to produce the state, which the brief forbade, would have folded a different engine
  and the result would not have been about the shipped one.

- **THE REMEDY WAS PROVED IN THE ONLY DIRECTION THAT MATTERS HERE, AND A GREEN TEST ASSERTING AN
  ABSENCE IS WORTH NOTHING UNTIL IT IS.** A test whose result is "the predicted thing did not happen"
  is indistinguishable from a test that asserts nothing, so **ADR-056's predicted mechanism was
  temporarily reintroduced into `advanceFloor`** (`if (floorLocked && lock.enabled) floorCents =
  lock.floorAtCents`) and both files were watched failing: the walk with `expected 5010000n to be
  4750000n` on funded day one, and the sweep shrinking to **the ADR's own scenario, a breach on the
  SECOND funded day at `floorOpen` 5,010,000c against a shipped fold surviving at 4,750,000c**. The
  mutant was reverted, `git diff --stat -- packages/rules-engine/src` is empty, and no commit was made
  while it was live.

- **THE SWEEP's SUPPORT IS MEASURED RATHER THAN ASSUMED, BECAUSE "BREACHES ON NO DAY THE SHIPPED FOLD
  SURVIVES" IS TRUE FOR FREE ON A SWEEP WHERE NOTHING EVER BREACHES.** Six reachability tests, each
  asserting a case is visited: funded days folded at all, the shipped account breaching, the carried
  account breaching, the two floors diverging, the shipped funded lock re-engaging, and the carried
  `hwb` sitting below its own balance. **The counts at the time of writing are 3,442 / 1,790 / 1,485 /
  1,127 / 232 / 2,000**, and each is asserted `> 0` rather than pinned, because pinning a generator's
  yield is how a property test becomes a change-detector.

- **NEITHER FILE PINS A RULING AND BOTH SAY SO IN THEIR HEADERS.** `ADR-056` is `proposed`; nothing
  asserted here says clearing the lock is correct. Every expectation is about a **counterfactual
  input** and constrains no ruling, which is what makes it a test rather than the fixture the ADR
  names. `GS-044` and `GS-242` declined to pin `floor_locked` across the pass for exactly this reason
  and that reasoning is still correct.

- **THE PROPERTY FILE CLAIMS NO `RE-P-nn` NUMBER, DELIBERATELY.** The `RE-P` series is M01 section 8's
  list of properties the engine **must have**; this is a measurement of a state the corpus has not
  decided is reachable. Giving it a number would put a counterfactual in the register of commitments.

- **A LONG CALENDAR SLICE HAD TO BE BUILT AND IT NAMES NO EXCHANGE.** `CME_WINDOW` holds five sessions,
  which leaves three funded days after a pass, and a sweep needs more. `LONG_WINDOW` follows
  `GAPPED_SLICE`'s precedent verbatim: `TR-01` forbids writing an exchange's calendar from
  recollection, so what the slice asserts is a property of the engine's arithmetic over whatever slice
  the caller supplied. **The funded fold reads only the ordering**: `R-37` needs a cadence anchor and
  there are no settlements, `R-32` needs `max_days` and no v1 plan configures one.

- **`STATE.md` WAS NOT AMENDED AND THAT IS A DELIBERATE OMISSION AGAINST THE END RITUAL.** The fence
  was `packages/rules-engine/test/**` plus this log, `STATE` is an `approved` corpus document, and the
  open founder item it carries for `INV-07` **does not close on this run**: the run informs the
  signature and the signature closes the item. **The pointer a future session needs is this file**,
  and the sessions README row is what makes it findable.

- **Files touched:** [`adr-056-carried-lock.test.ts`](../../packages/rules-engine/test/adr-056-carried-lock.test.ts)
  (new), [`adr-056-carried-lock.property.test.ts`](../../packages/rules-engine/test/adr-056-carried-lock.property.test.ts)
  (new), this file, [docs/sessions/README.md](README.md). **Three commits, each pushed immediately**,
  the first of them this log as a stub before any work.
## 2026-08-18 - Session 57: `GS-083` written where session 56 routed it, in the `RE-C-nn` suite (money path)
<!--index: `GS-083` written in the `RE-C-nn` suite: publishing fails and `CV-17` is the code named -->

**Written as a stub first so the session was on the record before it had a result, and completed in
place.** The fence is [`packages/rules-engine/test/`](../../packages/rules-engine/test/) and the
golden-scenarios registry row for `GS-083`; the ritual files are this log,
[sessions/README](README.md) and [STATE](../STATE.md).

- **Objective:** `GS-083` is `CV-17`: trailing drawdown, floor lock **DISABLED**, cap at or above the
  drawdown, and publishing must **FAIL with `CV-17` NAMED**.
  [Session 56](2026-08-18-session-56.md) established that it cannot be a golden fixture and routed it
  to the `RE-C-nn` suite. Write it there.

- **Done:** **`GS-083` is written, green, and its second half is executed rather than recited.** Four
  cases in
  [`plan-validate.test.ts`](../../packages/rules-engine/test/plan-validate.test.ts), plus a pointer
  from
  [`GS-083`'s registry row](../testing/golden-scenarios/07-gs-079-to-gs-083-scenarios-created-by-the-m1-gate-rulings-m1.md)
  to the file. **771 passing, 0 failing** across 52 test files, **17 of 17 gates**, `check:invariants`
  7 of 7, `falsify` clean on all 17 gates plus 19 scope cases and 10 loader cases, `typecheck`, `lint`
  and `format:check` green, and `CI-02/engine-CV-17` still watched failing on its own finding.

  **THE PUBLISH HALF IS AN EQUALITY, NOT A `toContain`, AND THAT IS THE WHOLE OF WHAT IT ADDS.** The
  brief's requirement was that "a test asserting only that publishing failed would pass if some other
  rule rejected it". `idsOf(result)` is asserted **equal to `['CV-17']`** with an empty
  `materialization` set beside it, so the config is Appendix A.1 to the cent apart from the lock flag
  and the cap, and `CV-17` is the only thing standing between it and an account. The finding's `path`
  and `sizeCents` are pinned too, because a publish diff that says `CV-17` without either is a founder
  reading nineteen rules to find one number. The oracle transcription is required to name the same
  single id.

  **THE COUNTERFACTUAL RUNS ON THE SAME `(rules, size)` PAIR `validatePlan` REJECTED.** `resolvePlan`
  applies no `CV` rule -- `CV-01` is the only one it can refuse -- so the rejected config is
  resolvable and the day fold runs on it. One transcription, both halves, and the breach below is the
  engine's own `R-13`, `R-48` and `R-21` rather than a subtraction typed into a comment.

  ```
  2026-11-03   5,000,000 + 400,000  = 5,400,000   a new closing high, R-13 trails on the CLOSE
               5,400,000 -  250,000 = 5,150,000   the floor, `drawdown` below the same high
               5,400,000 - 5,000,000 - 100,000 = 300,000   withdrawable, R-35, exactly the cap
  2026-11-04     300,000 settled, adjustment at the OPEN (R-10), no fills, no realized P&L
               5,400,000 -  300,000 = 5,100,000   the balance
               5,150,000 -5,100,000 =    50,000   the shortfall, and `cap - drawdown` exactly
  ```

  **`R-48` is what makes it a breach**: the floor at the open is the one the closing high produced, no
  recompute rescues it, and the account is closed on a day it did not trade. The event list is
  `['payout.win_days_reset', 'breach.detected']` and no `day.closed` follows, which is the settlement
  applying at `DO-2` and `R-24` ending the account at `DO-4`, in that order.

- **Next:** **`L-11` is still the biggest single blocker in the fixture directory**: six rows
  (`GS-052`, `GS-053`, `GS-066`, `GS-067`, `GS-068`, `GS-082`) are behind a `settlements:` block. Two
  loose ends from this session are named under the landmines and neither is urgent: the fixture
  README's held-back row still routes `GS-083` to "the `RE-C-nn` suite" without naming the file, and
  `R-15`'s M01 row still carries the claim `ADR-052` refuted.

- **Blockers:** none.

- **Landmines:**

  **THE PUBLISH HALF IS NOT NEW COVERAGE AND CLAIMING IT WOULD BE THE EASY VERSION OF THIS SESSION.**
  `RE-C-17` has asserted `CV-17` at the boundary on both sides since group C landed, and it asserts
  **both halves of the precondition false** as well. Gating `CV-17`'s condition off turns `RE-C-17`,
  `RE-C-oracle` and **two** of `GS-083`'s four cases red together, so the rule was already caught. What
  `GS-083` adds to that half is the **soleness** and the counterfactual; the arithmetic is the part
  that did not exist. **Measured rather than assumed**: the mutant was run and the failing set read.

  **THE COUNTERFACTUAL'S CAUSE IS COVERED AND ITS CONSEQUENCE WAS NOT.** Reintroducing a post-payout
  floor recompute in [`applySettlement`](../../packages/rules-engine/src/payout/settle.ts) --
  `floorCents = balance - approved - drawdown`, the `reset_to_balance_minus_dd` `ADR-014` retired --
  turns `RE-U-019`, `RE-U-048` and both of `GS-083`'s counterfactual cases red. `RE-U-019` and
  `RE-U-048` assert **the floor field did not move**. `GS-083` asserts **what follows from its not
  moving**, which is that the account breaches, and `INV-21` is a statement about the consequence.
  Nothing in the tree had ever run the unsafe config; `GS-065` and `GS-081` both pin `breached: false`
  on the safe one.

  **THE MINIMAL ARITHMETIC WOULD HAVE COLLIDED WITH `ADR-052` SECTION 2, AND THE DRIFT SESSION 56
  WARNED ABOUT WAS REACHABLE WITHOUT COPYING ANYTHING.** The obvious counterexample is `cap = drawdown
  + 1` on `CORE-50K`, which needs a closing high of **5,350,001** to make one full cap withdrawable.
  That is the exact figure [ADR-052](../decisions/ADR-052.md) section 2 lands on, by a different route
  on a different plan, because both are `size + buffer + cap` and the two plans share a buffer. A
  reader comparing the two files would have read one as a transcription of the other, which is what
  [session 56](2026-08-18-session-56.md) said must not happen: ADR-052's counterexample is **`CV-11`'s
  half** (lock ENABLED, a LOCKED floor), and `GS-083` is `CV-17`'s (lock DISABLED, a TRAILING floor).
  **A cap of 300,000c was chosen instead**, 600bp against A.1's 500bp drawdown, and it is better on its
  own terms: the shortfall is then `cap - drawdown` exactly, which states `CV-17`'s inequality as
  money, and no figure in the block appears in ADR-052. **The near-collision is the finding.** The
  guard against filing one half of `INV-21` under the other is not "do not copy"; it is deriving the
  numbers from `CV-17`'s own row and then checking what they landed on.

  **`cap_bp` HAD TO MOVE WITH `cap_cents` AND NOTHING WOULD HAVE CAUGHT IT IF IT HAD NOT.**
  `MZ-cap-ordinals` compares **ordinals**, not amounts, so a rules block saying 300bp beside a size row
  saying 300,000c publishes with no finding at all. Leaving A.1's `cap_bp: 300` behind would have made
  the file claim "this config is Appendix A.1 apart from two changes" while carrying a third that its
  own `toEqual(['CV-17'])` could not see. Set to 600bp, because 600bp of 5,000,000c **is** 300,000c.
  The same applies to the lock: disabled on the jsonb and on the size row together, or `MZ-lock-flag`
  fires and the block measures a materialization finding instead of `CV-17`.

  **M01's REASON THAT NO V1 PLAN REACHES `CV-17` IS TRUE AND IS NOT THE WHOLE REASON.** The row says
  "all three enable the lock, and `CV-11` covers that case", and the precondition is
  `size.floor_lock_enabled`, so that is checkable and it holds. **Checked further: all three also
  clear the inequality independently** -- Core 150,000 < 250,000, Rapid 100,000 < 250,000, Direct
  150,000 < 200,000 on A.3's 400bp drawdown. So **turning the lock off on a v1 plan publishes clean**,
  and it publishes because `CV-17` evaluated and passed rather than because it was skipped. Both are
  pinned, because "the rule is unreachable" and "the rule is reachable and satisfied" are different
  claims and only the second is true of the unlocked lineup.

  **THE ONLY GATE BETWEEN THIS CONFIG AND AN ACCOUNT IS THE PUBLISH GATE, AND THE TEST DEMONSTRATES
  IT BY WORKING.** `resolvePlan` resolved a config `validatePlan` had just refused, and the fold folded
  it to a breach without complaint. [`errors.ts`](../../packages/rules-engine/src/errors.ts)'s second
  class exists for "a config `validatePlan` must have rejected at publish, arriving anyway" and covers
  `CV-01` and `capForOrdinal`; **nothing anywhere re-checks `cap` against `drawdown` at runtime**, and
  by `ADR-014` nothing should. That is the arrangement `R-48` calls "the stronger one because it fails
  at publish time instead of at settlement time", and this file is what makes the cost of the
  arrangement visible.

  **THE REGISTRY ROW EDIT IS ADDITIVE AND THE DOCUMENT IS `approved` RATHER THAN `frozen`.** The
  constitution's rule is that changing a frozen document takes an ADR. The existing cell text is
  byte-identical and one sentence is appended naming the home and the structural reason; no assertion
  in the row moves. The brief fenced the edit in by name. It is recorded here because "additive" is a
  judgement the next reader should be able to check rather than take, and because the row previously
  said "the fixture also carries the arithmetic" while pointing at a directory that can never hold it.

  **TWO LOOSE ENDS ARE NAMED AND NOT FIXED, BOTH OUTSIDE THE FENCE.** The fixture README's held-back
  row at [`fixtures/README.md`](../../packages/rules-engine/fixtures/README.md) still says "`RE-C-nn`
  is the suite that owns them" and does not name the file, so the registry row and the README now sit
  at different resolutions. And `R-15`'s M01 row still reads "`CV-12` forces the trigger to sit exactly
  where the trailing floor already is, so **the floor never jumps**", which `ADR-052` refuted in
  general; session 56 recorded it and it is an M01 edit wanting an ADR rather than a commit.

  **THE HARNESS WAS RUN BY CASE NAME AND NOT IN A LOOP.** [Session 56](2026-08-18-session-56.md)'s
  landmine was a `falsify-ci.mjs` run that outlived the shell that started it and kept mutating the
  tree. `node scripts/ci/falsify-ci.mjs list` names every case, `CI-02/engine-CV-17` is one
  invocation, and the tree was verified clean with `git status` after every mutation in this session.
  All three manual mutants were reverted with `git checkout --` and the working tree was empty before
  each commit.

- **Files touched:**
  [`plan-validate.test.ts`](../../packages/rules-engine/test/plan-validate.test.ts),
  [`07-gs-079-to-gs-083-...md`](../testing/golden-scenarios/07-gs-079-to-gs-083-scenarios-created-by-the-m1-gate-rulings-m1.md),
  [STATE](../STATE.md), [sessions/README](README.md), this file. **Four commits, each pushed
  immediately**, the first of them this log as a stub before any work.

### The three mutants, and which side of the fence each landed on

| Seeded | What went red | What stayed green |
|---|---|---|
| `CV-17`'s condition gated off in [`validate.ts`](../../packages/rules-engine/src/plan/validate.ts) | `RE-C-17`, `RE-C-oracle`, `GS-083`'s publish half and its equality case | **`GS-083`'s counterfactual**, which reads the engine and not the validator |
| A post-payout floor recompute reintroduced in [`settle.ts`](../../packages/rules-engine/src/payout/settle.ts) | `RE-U-019`, `RE-U-048`, **`GS-083`'s counterfactual** (`expected false to be true` on `breached`) and its equality case (`expected 4850000n to be 5100000n`) | `GS-083`'s publish half |
| `R-22` relaxed from `>` to `>=` in [`breach.ts`](../../packages/rules-engine/src/day/breach.ts), out of scope | `RE-U-022`, alone, 1 of 291 | **all four `GS-083` cases** |

**The first two rows are the scope separation stated as a measurement.** A file whose halves both went
red on both mutants would be one assertion written twice.
## 2026-08-18 - Session 57: two accepted rulings that left a row behind (ADR only, docs fence)
<!--index: ADR-057: one refuted sentence in four documents, and the ninth breach-row field -->

**Written as a stub before any work, so the session was on the record before it had a result, and
completed in place.** The fence is `docs/`.

- **Objective:** Rule, in one ADR, the two residues that accepted rulings left behind and that
  neither applying session was in scope to fix. **First**, `R-15`'s row in
  [M01](../plans/M01-rules-engine.md) section 3.5 still carries the claim
  [ADR-052](../decisions/ADR-052.md) refuted, while `CV-12`'s own row was corrected. **Second**,
  M01 section 3.6's breach block enumerates **eight** written fields and the engine writes **nine**,
  the ninth being `engineGates` via `gatesAfterBreach`, which `RE-U-025` now pins;
  [ADR-054](../decisions/ADR-054.md)'s applying session recorded this as residue and deliberately
  did not decide it.

- **Done:** **[ADR-057](../decisions/ADR-057.md) is written and applied, `status: accepted`, and it
  amends six rows across three documents.** `057` was claimed in
  [ALLOCATION](../decisions/ALLOCATION.md) before the ADR was written and after all twenty-eight
  remote refs were read, by file and by claim; every one maxed at `056`. **The first repair turned
  out to be four sites rather than one. The second is ruled against the divergence: section 3.6's
  block gains `engineGates` and is nine.** **No engine file, no fixture and no test assertion
  changed**, which is the strongest available evidence that neither ruling moves behaviour. 17 of 17
  gates pass, and the whole diff is 19 inserted lines across `M01`, `GLOSSARY` and `ADR-014`.

- **Next:** **`RE-P-15` is unblocked and is one session in `packages/rules-engine/test`.** It is
  already registered in M01 section 8.2, it is unwritten, and it is now writable against a section
  3.6 that it does not contradict. **Before this ruling, writing it faithfully to the document
  produced a test that fails on `GS-064`'s shape.** Two comment-only staleness repairs follow it and
  are named below.

- **Blockers:** None. **The signature is a read rather than a gate**, and the ADR says which half
  needs it.

- **Landmines:**

  - **THE BRIEF'S FIXTURE PREMISE WAS FALSE AND `GS-024` IS THE FILE.** The objective as received
    states that "no fixture in the directory catches it" and asks that this be verified before
    writing. It does not hold. **`GS-024` pins `floor_cents: 5010000` on a lock day that overshoots
    the trigger by 40,001c**, which is the one value the refuted claim cannot produce, and it went
    red for exactly as long as the defect existed. [ADR-052](../decisions/ADR-052.md)'s own
    consequences and [STATE](../STATE.md) line 916 both already call it "the only fixture in the
    corpus whose pinned floor differs between the two readings". **The instruction to name a fixture
    and not write it therefore had nothing to name for that half**, and the correct statement is
    narrower: `GS-081`, `GS-015`, `GS-016` and `GS-056` all close their lock day at exactly the
    trigger and cannot see it; `GS-017`, `GS-019` and `GS-044` overshoot and end after `R-31` has
    rewritten the floor; `GS-018` overshoots and pins no floor at all.

  - **AND THE HONEST LIMIT OF THAT: NO FIXTURE CAN CATCH PROSE.** `GS-024` catches an engine that
    implements the refuted claim. The engine has been correct since session 55 and the document has
    been wrong since before ADR-052, **which is the exact interval in which a reader repairing the
    code toward the document reintroduces the defect and it looks like diligence.** That is
    [ADR-056](../decisions/ADR-056.md)'s landmine in the opposite direction and it is why the
    documentary repair is worth a session on its own.

  - **`ADR-052`'s AMENDMENT TABLE HAS FOUR ROWS AND ITS APPLYING SESSION APPLIED THREE.** The fourth
    is **`ADR-014`'s decision block**, which still carried the `max` and the sentence ADR-052 names
    in its own table. **That was not in the objective and is the larger half of the finding**: the
    brief named one unapplied site and there were four, one of them explicitly enumerated by the
    ruling that refuted it.

  - **`R-15` WAS LEFT ON PURPOSE, AND THE REASON WAS SOUND AT THE TIME.** Session 55's commit says
    "DELIBERATELY NOT TOUCHED: R-15's row and CV-11 carry the same 'never jumps' phrasing", citing
    ADR-052's own "`R-15` ... unchanged". **That declaration is true of the rule and false of the
    sentence in the same cell.** R-15's Effect column is an effect; what follows it is a rationale,
    and the "unchanged" swept it up. **An amendment table enumerates locations and a claim
    propagates by restatement**, which is the whole lesson and is cheaper to fix as a habit than as
    a gate: a ruling that refutes a sentence should grep for the sentence. That grep found four of
    the five copies.

  - **`CV-11` IS A FALSE LEAD AND IT IS IN THE COMMIT HISTORY, WHICH IS THE WORST PLACE FOR ONE.**
    The same commit message groups `CV-11` with `R-15` as carrying the same phrasing. **It does
    not.** `CV-11` reads "post-lock it **equals** it", which is **TRUE** under the assignment and is
    the premise ADR-052 section 2 turns on: the `max` makes it false, that is what makes `INV-21`
    unenforceable, and that is what decided the ruling. **`CV-11` is the row ADR-052 upheld.** It
    was read at its line rather than trusted from the commit message, and it is named in the ADR's
    not-touched list so the next reader does not repair a correct row.

  - **THE SECOND REPAIR IS DECIDED BY A PROPERTY M01 ALREADY STATES, NOT BY THE TYPE.** The tempting
    argument for the ninth field is that `RuleState` requires it, and the tempting counter is that a
    specification should not carry TypeScript furniture. **Both are beside the point.** `RE-P-15`
    reads "`engineEligible` equals the conjunction of `engineGates`, for every generated state", and
    section 3.6 spreads `{ ...s }`, so an unlisted field **carries**. On any breach after an
    eligible day the carried conjunction is `true` beside `engineEligible: false`. **`GS-064` is
    that day and is already in the directory**, and its own note says every eligibility input "is
    still standing at the value that made the account eligible at the previous close". A field a
    stated property quantifies over is part of the specification by construction.

  - **THE OBVIOUS FORM OF THE AMENDMENT IS WRONG AND IT IS ONE WORD AWAY.** Writing
    `evaluateEngineGates` into the breach block would be `R-24` broken: `R-37` can **refuse** a day
    whose calendar slice does not cover the cadence anchor, and a breach the fold declined to record
    on a caller's window choice is a breach that did not happen. The gates on that row are
    **stated**. `gatesAfterBreach` is a separate function for exactly this reason and the amendment
    names it.

  - **A DECISION WITH NO AUTHORITY WAS SITTING IN `gates.ts` AND THIS RULING IS THE FIRST TO CITE
    IT.** Its comment rules that every `pass` is `false` including the two that can be `skipped`,
    because "a `pass: true, skipped: true` on a breach row would put a satisfied-looking gate on the
    worst row in an account's life". **That is trader-visible and it cited nothing**, which is the
    class [session 56](2026-08-18-session-56.md) named as sitting one level below `CI-06q`'s reach.
    The gate's own stated blind spot has now produced two instances in two days.

  - **FIVE CODE SITES ARE STALE AND ALL FIVE ARE OUTSIDE THE FENCE, SO THEY ARE NAMED AND NOT
    TOUCHED.** [`plan/validate.ts`](../../packages/rules-engine/src/plan/validate.ts) and
    [`test/generators/validate-plan.ts`](../../packages/rules-engine/test/generators/validate-plan.ts)
    both quote `CV-12`'s **superseded** "Rejected because" text verbatim. **Those two are
    constitution C10's writer and reviewer, transcribed independently in different sessions, and
    they are stale identically**, which is what a shared stale source looks like from the outside
    and is worth more than either copy alone. Also
    [`plan-validate.test.ts`](../../packages/rules-engine/test/plan-validate.test.ts) line 379,
    [`rules-c-floor.test.ts`](../../packages/rules-engine/test/rules-c-floor.test.ts) lines 163 to
    166, and `GS-015`'s yaml header, which quotes `R-15`'s sentence as a `TR-01` source citation and
    **goes stale the moment this ADR lands**. `RE-U-025`'s comment joins them: it says the ninth
    field is "RESIDUE recorded in STATE.md", and section 2 decides it. **All six are comment-only,
    no assertion moves, and that is the check on the claim that this ruling changes no behaviour.**

  - **`GS-015`'s TITLE READS AS THE GENERAL CLAIM AND ITS BODY DOES NOT.** "Floor lock engages
    continuously with no jump" is the refuted sentence in five words; the registry row's body, "at
    profit 260,000 the trailing floor reaches exactly `size + 10,000`", is true of that stream
    because it lands on the trigger to the cent. **Renaming it is a sweep** across the fixture
    filename, its `.expected.json`, the registry row and STATE, and a fixture id's title is its
    identity. Named rather than done.

  - **`CI-06n` CAUGHT THIS SESSION'S FIRST COMMIT, EXACTLY AS SESSION 56 PREDICTED IT WOULD.** The
    stub was committed before anything else, per the brief, with no row in the sessions README.
    Session 56 recorded the same finding and recorded it as a demonstration that the gate is live;
    this session reproduced it knowingly and repaired it in the next commit. **A prediction that
    comes true on the next session is a gate description that is accurate**, which is worth more
    than the two minutes it cost.

- **Files touched:** [ADR-057](../decisions/ADR-057.md) (new),
  [M01](../plans/M01-rules-engine.md), [GLOSSARY](../GLOSSARY.md),
  [ADR-014](../decisions/ADR-014.md), [ALLOCATION](../decisions/ALLOCATION.md),
  [decisions/README](../decisions/README.md), [STATE](../STATE.md),
  [sessions/README](README.md), this file. [INDEX](../INDEX.md) and STATE carry regenerated
  `adr_count` spans, 56 to 57. **Five commits, each pushed immediately**, the first of them this log
  as a stub before any work.

### What was NOT done, named rather than implied

**No fixture was written and none needed naming for the first repair**, because `GS-024` already
pins it and is green.

**`RE-P-15` was not written.** It is the second repair's check, it is already a registered row in
M01 section 8.2 so no identifier is claimed, and it is a property test in
`packages/rules-engine/test`, outside this session's fence.

**No code was touched**, including the six stale comments the sweep found. Every one is named above
with its file and line.

**`CV-11`, `R-14`, `INV-06`, `INV-07`, `CV-12`'s operator, `R-15`'s Effect column and trigger, and
every parameter are unchanged.** The amendment is six rows and nineteen inserted lines.
