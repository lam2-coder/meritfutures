---
status: approved
depends_on: [README.md]
last_updated: 2026-08-18
---

## 2026-08-18 - Session 57: ADR-056's central claim EXECUTED rather than argued (money path, test only)

**Written as a stub before any work, so the session is on the record before it has a result.**

- **Objective:** [ADR-056](../decisions/ADR-056.md) is `proposed` and asks not to be signed until the
  claim it turns on is **executed**. The claim: under the literal reading of `INV-07`, where
  `floorLocked` is CARRIED through the `R-31` funded reset, **every funded account breaches on its
  second funded day on all three v1 plans**. Run it.
- **Fence:** [`packages/rules-engine/test/`](../../packages/rules-engine/test/) and this file. **No
  engine file, no fixture, no `M01` amendment, no signature.** `progression.ts` writes
  `floorLocked: false` and ADR-056 RATIFIES that; the carried-lock state is constructed directly
  rather than by editing `DO-8`.
- **Why a test and not a fixture:** a fixture would pin the outcome of a ruling that is still
  `proposed`, which is exactly what `GS-044` and `GS-242` correctly declined to do.
- **Result:** PENDING (this is a stub).
