---
status: approved
depends_on: [README.md]
last_updated: 2026-08-28
---

## 2026-08-28 - Session 352: the thirteen keep their 503, and what was wrong was the order rather than the status

<!--index: **[ADR-192](../decisions/ADR-192.md) LANDS, `status: proposed`, approval line UNSIGNED**, [ALLOCATION](../decisions/ALLOCATION.md) row `192` amended in place ([ADR-065](../decisions/ADR-065.md) T3). **NO MIGRATION NUMBER, AND `server.ts` AND [API_CONTRACT](../architecture/API_CONTRACT.md) ARE BOTH IN THE FENCE AND BOTH BYTE FOR BYTE UNCHANGED, WHICH IS A RULING RATHER THAN AN OMISSION.** [ADR-190](../decisions/ADR-190.md)'s measurement reproduced EXACTLY at `d86a029a` over a real `compose()` and Fastify's `inject`, every registered operator route one at a time: 28 registered, 45 withheld, 23 `/admin/*`, **ten answering 401 then 500 and thirteen answering 503 in both states**. **RULED: [ADR-190](../decisions/ADR-190.md)'s GROUND IS TWO GROUNDS AND ONLY ONE OF THEM REACHES THE THIRTEEN, AND THAT IS A COUNT RATHER THAN A READING.** Its clause 2 refused a 503 for `AdminReadError` because that class carries two facts across **15** throw sites of which only **2** are about wiring; the four write modules carry four DEDICATED classes with **17 throw sites, 17 of 17 inside their `UNWIRED_*` default constant and NOT ONE refusing data a wired backend returned**, derived at reporting time against constants spanning `admin-writes.ts:329-341`, `admin-wallet.ts:653-661`, `admin-payouts.ts:415-421` and `admin-certificates.ts:386-395`. So *"a 503 answers try again shortly to a float in a cents field"* has no instance here, **the status does not move and no code is minted**, and [ADR-190](../decisions/ADR-190.md) section 8 had already named this reversal and its condition. **A CLASS SPLIT IS PRICED AND REFUSED**: the split already exists at the MODULE boundary and a fifth class would be a second thing four handlers must choose between correctly, to separate facts that are not mixed. **RULED: THE 503 MOVES BEHIND THE 401, AND WHAT REACHES THE THIRTEEN IS CLAUSE 3, WHICH IS ABOUT ORDER RATHER THAN STATUS.** `principal`'s refusal by an unwired backend answers **401**, because a deployment that cannot authenticate anyone has authenticated nobody, which is true of every caller and needs no guess at credential shape; every other port member still answers 503, behind authentication, and each of the four suites asserts it there. **MEASURED AFTER: ALL 23 `/admin/*` ROUTES ANSWER 401 TO AN ANONYMOUS CALLER AND NO REGISTERED OPERATOR ROUTE ANSWERS 503 BEFORE AUTHENTICATING.** **THE DISCLOSURE IS NOT WHAT THREE DOCUMENTS SAID IT WAS AND THE REAL ONE IS WORSE**: *"confirm their own existence"* cannot be the harm, since the ten answer 401 anonymously and confirm existence just as well and an unregistered path answers 404; what the 503 added is **DEPLOYMENT STATE**, an unauthenticated live readout of which operator ports this deployment has not finished wiring, shrinking as slices land, which is exactly the fact [ADR-190](../decisions/ADR-190.md) clause 3 refused to put on the wire for the read side. **AND THE COMMENT THAT ENTRY QUOTED ONCE IS WRITTEN FOUR TIMES**, at `admin-writes.ts:844`, `admin-wallet.ts:1340`, `admin-payouts.ts:1030` and `admin-certificates.ts:700`: the argument the disclosure violated was stated four times in the four files that violated it. **TWO OF THE THREE FIGURES MOVED.** The 7 of 16 holds (`STATUS_CODE` 6 keys, `TITLE` 7, section 2 sixteen codes); **the hand-written 503 titles are 21 and not 18**, 11 declarative and 10 positional. **THE VOCABULARY IS REGISTERED RATHER THAN REPAIRED AND THE REFUSAL IS ARGUED FROM [ADR-190](../decisions/ADR-190.md)'s OWN EXPERIMENT**: `admin-writes.ts:384` states the hand-built title as the DESIGN, and adding the `STATUS_CODE` entry is precisely what let a class-level status reach the wire there for twelve cases that should not have had it; **nothing throws a 503 today, so the entry would change no response and would arm the next class that sets one.** **NO OPERATOR ENDPOINT DECLARES `service_unavailable` IN THE CONTRACT AT ALL**, the only per-endpoint declaration anywhere being `API_CONTRACT:341`'s public checkout, so the fence's conditional is unexercised and the gap is registered. **AND [ADR-190](../decisions/ADR-190.md) WROTE A CONTROL THAT PINS THE DEFECT IT REGISTERED FOR REPAIR**: `admin-reads.test.ts`'s `unavailable.length > 0` refused to pin the count 13 for a stated reason and pinned the set's non-emptiness for none, so it could not survive its own item 3 being discharged; it becomes `toEqual([])`, a bound made an equality, and it is the ONE file outside the dispatch's list this session touched, argued in section 7 rather than buried. `apps/api/src/routes/admin-reads.ts` is byte for byte unchanged. **FIVE SEEDS, FIVE CAUGHT**, tree clean after each. 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean, **224 files / 5,294 passed / 6 skipped** against a `main` baseline of **224 / 5,288 / 6** reproduced exactly first, `falsify` clean. **No migration number, no ADR number beyond 192, no `SystemReason` or `SqlExecutorReason` member, no `pg` import, no cast past a key type, no ledger account named.**-->

**Objective.** `ADR-192`: rule what the thirteen operator write routes answer, on the side of [ADR-190](../decisions/ADR-190.md)'s ground that entry registered rather than ruled.

**Done:** [ADR-192](../decisions/ADR-192.md), the `principal` catch in [`admin-writes.ts`](../../apps/api/src/routes/admin-writes.ts), [`admin-wallet.ts`](../../apps/api/src/routes/admin-wallet.ts), [`admin-payouts.ts`](../../apps/api/src/routes/admin-payouts.ts) and [`admin-certificates.ts`](../../apps/api/src/routes/admin-certificates.ts), their four suites, section 4 of [`admin-writes.test.ts`](../../apps/api/test/admin-writes.test.ts) as the surface-wide control, one assertion in [`admin-reads.test.ts`](../../apps/api/test/admin-reads.test.ts), one citation in [`payouts.ts`](../../apps/api/src/routes/payouts.ts), [ALLOCATION](../decisions/ALLOCATION.md) row `192` amended in place, the [STATE](../STATE.md) append, this log and the generated `README` rows.

**Next:** [ADR-192](../decisions/ADR-192.md) section 10's five items. The one to take first is item 3, because it is the check that makes this entry's own ruling load bearing and it is a sweep rather than an investigation: every `new *Unwired` must sit inside an `UNWIRED_*` constant, and the day one does not, clause 1's ground is gone. Item 1 is the larger latent defect and is a check rather than the table entry it looks like.

**Blockers:** none. One fence ambiguity was resolved rather than waited on, and it is landmine 1.

**Landmines:**

1. **`apps/api/test/admin-reads.test.ts` WAS TOUCHED AND THE DISPATCH DID NOT LIST IT EITHER WAY.** The fence named `apps/api/src/routes/admin-reads.ts` as byte for byte unchanged, which it is, and named *"their suites"* meaning the four write modules'. [ADR-190](../decisions/ADR-190.md)'s partition case asserted `unavailable.length > 0` over the anonymous probe, so discharging that entry's own item 3 turns it red. **The change is `toEqual([])`, a bound made an equality**, and it is argued in [ADR-192](../decisions/ADR-192.md) section 7 with the alternatives. A founder who reads it as a widened fence is reading it the way the constitution's sentence invites.

2. **A CONTROL THAT PINS A MEASUREMENT CAN PIN THE DEFECT THE MEASUREMENT FOUND, AND [ADR-190](../decisions/ADR-190.md) DID IT WHILE EXPLICITLY GUARDING AGAINST THE OTHER HALF.** It refused to pin the count 13, with a stated reason, so a wiring slice could not turn it red. It then pinned the 503 set's non-emptiness, with no reason stated, which is the very thing its section 7 asked the next session to remove. **The general shape: an assertion about a state your own entry calls wrong is an assertion that the wrong state persists.**

3. **`server.ts` IS STILL A TRAP AND IT IS NOW A DELIBERATELY UNSPRUNG ONE.** A thrown error carrying `statusCode = 503` is rewritten to `internal_error` at [`:150`](../../apps/api/src/server.ts) with the true status logged at [`:154`](../../apps/api/src/server.ts). This session refused the two-line table fix, on the ground that the entry is inert until it is wrong and that [ADR-190](../decisions/ADR-190.md) measurement two is exactly that entry going wrong. **A session that reads item 1 as "add the two keys" will reproduce twelve failures across two files** and should read section 6 first.

4. **THE 401 IS NOW LOAD BEARING IN A WAY NO TYPE HOLDS.** Four `catch` blocks decide that an unwired `principal` is 401 and every other port member is 503. Nothing but the suites keeps them agreeing, and the estate's shape is four near-identical handlers that have already drifted once (`admin-certificates.ts` catches `VerifyPresentationError` beside its own class; the other three do not). **The surface sweep in `admin-writes.test.ts` section 4 is the only control that reads all four.**

5. **`apps/admin` STILL PRERENDERS ITS THREE ROUTES IN NO CI JOB** (session 340's fourth landmine, session 344's fifth, session 350's fifth, unmoved).

**Files touched:** `docs/decisions/ADR-192.md`, `docs/decisions/ALLOCATION.md`, `docs/STATE.md`, `apps/api/src/routes/admin-writes.ts`, `admin-wallet.ts`, `admin-payouts.ts`, `admin-certificates.ts`, `payouts.ts`, `apps/api/test/admin-writes.test.ts`, `admin-wallet.test.ts`, `admin-payouts.test.ts`, `admin-certificates.test.ts`, `admin-reads.test.ts`, `docs/decisions/README.md` (generated span), `docs/sessions/README.md` (generated span), this file.

---

### 1. The baseline, reproduced before a line changed

Each command run separately, with `pnpm install` first. `pnpm run verify` was never run.

| Command | `main` at `d86a029a` | This branch |
|---|---|---|
| `node scripts/corpus/gates.mjs check` | 33 of 33 | **33 of 33** |
| `node packages/tooling/checks/repo-invariants.mjs` | 16 of 16 | **16 of 16** |
| `pnpm vitest run` | 224 files / 5,288 passed / 6 skipped | **224 / 5,294 / 6** |
| `pnpm run typecheck` | exit 0 | **exit 0** |
| `pnpm run lint` | exit 0 | **exit 0** |
| `pnpm run format:check` | clean | **clean** |
| `node scripts/corpus/falsify.mjs` | clean | **clean**, tree clean after |

The delta is **+6 cases and no new file**, every one appended to a suite that already existed.

---

### 2. The measurement, both sides

Every entry of `compose()`'s own `registered` list injected one at a time, with no cookie and with one, over `buildServer({ surface: 'operator' })` and the modules on disk.

| Family | Routes | Anonymous, before | Anonymous, after | With a cookie, after |
|---|---|---|---|---|
| the shared `adminHandler` of [`admin-reads.ts`](../../apps/api/src/routes/admin-reads.ts) (7), [`admin-breaker.ts`](../../apps/api/src/routes/admin-breaker.ts) (2), [`admin-feed.ts`](../../apps/api/src/routes/admin-feed.ts) (1) | **10** | 401 `unauthenticated` | **401**, unmoved | **500** `internal_error`, unmoved |
| the four write backends | **13** | 503 `service_unavailable` | **401** `unauthenticated` | **401** `unauthenticated` |
| not `/admin/`: `health`, `internal` | 5 | 200, 400, 500 | the same | the same |

**28 registered and 45 withheld on both sides**, which is [ADR-190](../decisions/ADR-190.md)'s figure and session 349's, reproduced rather than carried.

---

### 3. The count that decided it

| | class | throw sites | *"no backend is installed"* | refusing data a wired source returned |
|---|---|---|---|---|
| the ten | `AdminReadError` | **15** | **2** | **13** |
| the thirteen | `AdminWriteUnwired`, `AdminWalletUnwired`, `AdminPayoutUnwired`, `AdminCertificateUnwired` | **17** | **17** | **0** |

Every one of the 17 is a member of its module's `UNWIRED_*` default object, verified by line range rather than by reading, and there is no other way to construct one. **The data refusals in these modules are `Refusal`s carrying their own contract codes, caught one branch earlier.**

---

### 4. Five seeds, five caught

Each applied to the committed shape, watched failing, and reverted with the tree clean after every one.

| # | Seed | Result |
|---|---|---|
| A | the disclosure reintroduced: `admin-writes.ts`'s `principal` catch rethrows | **4 failed across 2 files**, [ADR-190](../decisions/ADR-190.md)'s own strengthened case among them |
| B | the module stops sending 503 at all: the `AdminWriteUnwired` catch answers 401 | **1 failed**, the leg asserting an authenticated operator still gets 503 |
| C | the disclosure reintroduced in `admin-payouts.ts` only, probed from `admin-writes.test.ts` | **1 failed**, the surface sweep, in a different module's suite |
| D | the same disclosure by a different number: `admin-wallet.ts` answers 500 before authenticating | **1 failed**, the sweep's `-> 401` leg |
| E | the repointed citation drifted back | **`RI-15` fails, 15 of 16** |

**B is the counterfactual and it is the one worth keeping.** Every other case in this session passes on a module that simply deleted its 503, which would be the ruling misread as *"stop sending 503"* rather than *"send it to someone who has authenticated"*.

**D is the seed [ADR-190](../decisions/ADR-190.md)'s framing would have missed.** A 500 before authenticating discloses the same deployment fact as a 503, which is why the sweep asserts the answer IS 401 rather than asserting it is not 503.
