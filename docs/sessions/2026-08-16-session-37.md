## 2026-08-16 - Session 37: FOLD-01 session 6, the registries and CI-06k

- **Done:** [FOLD-01 §6.2](../plans/FOLD-01-phone-identity.md)'s registries session. **The corpus now carries thirteen gates**, and the five fixture citations two earlier sessions owed are live. **Non-money throughout: no migration, no `DELTA_MANIFEST` row, no ADR.**
  - **[GOLDEN_SCENARIOS](../testing/golden-scenarios/README.md):** new **[section 34, GS-258 to GS-272](../testing/golden-scenarios/34-gs-258-to-gs-272-phone-identity-and-the-authority-boundary.md)**. **The range was derived from the registry rather than quoted**, per the brief. One scenario per ruling point (a) to (e), plus amendments 2 and 3, plus the authority boundary, which is §6.2's minimum of eight; it runs to fifteen because several points are only pinned by a **pair**. **GS-263 pins the residual** (a reassignment with no portability record at all), **GS-265 pins a control the schema cannot enforce**, and **GS-267 pins the quiet direction** of the boundary, that a single-factor session still renders every read surface.
  - **[EDGE_CASES](../edge-cases/README.md):** **EC-142 to EC-149**, each naming a scenario that resolves. **[EC-146](../edge-cases/EC-146.md) is the one to read**: it converts [session 33](2026-08-16-session-33.md)'s landmine into a registry entry.
  - **[API_CONTRACT](../architecture/API_CONTRACT.md):** `POST /auth/otp` gains `channel` with **no default**; `POST /auth/elevate`, **§3.1**'s phone verification and change endpoints, and `GET /sessions` plus revoke, **which AS-M4-05 has been owed since it was approved**; `GET /me` gains the phone and session block; §11 gains six rows; and **§12's negative-authz matrix gains the required-factor column** with a published closed vocabulary and a `C-27:` tag on each sensitive action.
  - **[EVENTS](../architecture/EVENTS.md):** `phone.verified`, `phone.change_requested`, `phone.reassignment_detected`, `sms.budget_breaker_tripped`, with four payload decisions argued rather than shaped. **No payload carries a number, a hash of one, or a preview of one.**
  - **[STATE_MACHINES](../architecture/STATE_MACHINES.md):** **§12**, the phone-change machine `SD-M19-06` stores, its four guards, and what the hold does to the machines below it.
  - **[STRATEGY](../testing/STRATEGY.md):** the `CI-06k` row, its declared non-coverage, and the subsection on why a three-assertion gate needs more than one seed.
  - **All 13 gates pass clean and fail dirty, 8 scope cases hold, `generate` reports every span already matching, and it ran before the commit rather than after.**
- **`CI-06k`, the declared-authority gate.** Three assertions, from the tree, no database. Every row of §12's matrix carries a required-factor cell; every sensitive action C-27 names appears there and declares a **non-single** factor; and `notification_kinds.rate_limit_exempt` is generated over the post-identity `security` and `money` classes only, **read from the generated expression rather than from seed rows**, because the generated column is what makes a seed row unable to lie.
  - **It was watched failing on the seeded finding for all three assertions, not one.** `SEEDS` holds one violation per gate, so a three-assertion gate is otherwise watched failing on a third of itself. The seed covers the missing declaration; **`CI-06k/exempt-class`** covers the exemption; **`CI-06k/single-factor-read`** covers the partition **in both directions**, with a control that downgrades a C-27 row to `session` and must fire.
  - **Every seed derives its target at seed time**, per the founder's rider. The matrix rows, the C-27 action names and the notification class list are all things the corpus edits, and a seed naming one by literal goes stale **silently**.
  - **The control preserves the `C-27:` tag and downgrades only the factor.** Replacing the whole cell untags the row, the missing-action assertion fires instead, and the case fails off-target while looking like it worked. That was found by writing it the wrong way first.
- **The gate was wrong before the corpus was, and it was caught by reading the failure.** `CI-06k`'s first run reported **every sensitive row as carrying no declaration**. Two parser defects: it read the vocabulary legend above the matrix as endpoint rows, and its token pattern required each factor to be its own code span, so the single span `passkey or dual_channel` matched nothing. **The corpus was right both times.** This is `CI-06a`'s 109 phantom anchors and `CI-06e`'s 119 phantom refless entries in a third place, and it is the argument for `falsify.mjs` restated: a gate is not trustworthy because it fails, it is trustworthy once you have checked what it fails on.
- **Next:** **FOLD-01 is complete through session 6.** The remaining FOLD-01 work is the founder's **E2 read on `0029`**, which is unchanged by this session. **Do not start FOLD-02 session 7 or S-E session 5 from this branch**: both write `gates.mjs` and `falsify.mjs`, which this session changed.
- **Blockers:** None. **Two open questions from earlier sessions are still open and neither blocks anything here**: `OQ-M10-05` (whether registration degrades on an SMS **vendor** outage the way C-28 degrades on a **cost** trip) and `OQ-M10-06` (nothing in the schema holds an address the dispatcher can send to). **`OQ-M10-06` is now cited by [EC-146](../edge-cases/EC-146.md) rather than living only in a session log.**
- **Landmines:**
  - **THREE MORE HAND-MAINTAINED COUNTS WERE FOUND WRONG, AND TWO OF THEM WERE INSIDE THE DOCUMENTATION OF THE GATE THAT EXISTS TO END THEM.** [STRATEGY §4.4](../testing/STRATEGY.md) read "**All ten of the gates above ... eleven checks in one runner**" against eleven gate rows plus `ADR-026`, wrong since `CI-06n` landed; [INDEX](../INDEX.md) said `check` runs "**CI-06a to CI-06j ... eleven in all**", with `CI-06n` already in the runner; and STRATEGY's **`TR-04` row**, which is the rule that every discovered gap becomes a registry entry, stated the registry as "**140 entries**". All three are now spans. **`gate_count` is a new query and it reads `GATES.length`**, written beside `GATES`, which is the one place that cannot drift from the runner. **Sixteenth, seventeenth and eighteenth instances**, and the count is given because the instances are what justify [ADR-034](../decisions/ADR-034.md).
  - **THE GOLDEN-SCENARIO REGISTRY CARRIED THREE BARE `257`s AND A BARE `301`, AND THEY WOULD HAVE GONE WRONG IN THIS COMMIT.** Three sentences in [GOLDEN_SCENARIOS](../testing/golden-scenarios/README.md) and one cell in [INDEX](../INDEX.md) stated the count outside any span, beside spans that state the same number. Adding section 34 falsifies all four. **They sat inside `CI-06g`'s own declared gap**, that it compares the spans that exist and does not sweep for bare numerals, which is the same gap that produced the fourteenth and fifteenth instances two days ago. The three `257`s are spans now; **the `301` row total is deleted rather than generated**, because no query in `gates.mjs` derives it and a number nothing can check is the thing the pointed form exists for. **The sweep is still unwritten, and this is the third time it has been the finding.**
  - **`STATE_MACHINES` had a DUPLICATE `depends_on` KEY in its frontmatter and every gate was green.** Two `depends_on` lines, one carrying nine targets and one carrying four. **The frontmatter parser keeps the last key it sees**, so the longer list, which is the accurate one, was invisible to `CI-06b`'s resolution check: five dependencies were neither resolved nor reported. Merged into one line here. **`CI-06b` cannot see a duplicate key at all** — it builds a map and a second assignment overwrites the first silently — so this is a gate whose parser is looser than the property it claims, which is `OI-09`'s class a second time. **Not fixed in the runner**, because tightening it needs a seeded violation it has been watched failing on, and that is a tooling change with its own falsification.
  - **The four new guards are in §12 rather than in §10, against that document's own convention, and it is deliberate.** §10 currently defines **seven guards twice with non-identical bodies**, the keep-both merge artifact [session 36](2026-08-16-session-36.md) recorded. Adding rows to a table mid-repair collides with the dedup and makes it harder to read. **The deviation is written into §12 itself** so a reader meets it as a decision rather than as an inconsistency, and the guards move when §10 is repaired.
  - **`GS-265` is the first golden scenario in this registry written to fail against a passing constraint.** `phone_change_requests_applied_is_complete` makes `prior_notified_at` a precondition of `applied`, which **reads as enforcement**, and a database can only assert that a timestamp exists. The scenario asserts the **`integration_dispatches` row**, which is the only form of the claim the `CHECK` cannot make. **Storage-enforceable is not send-enforceable, and this corpus still has no gate that can tell them apart.**
- **Files touched:** [API_CONTRACT](../architecture/API_CONTRACT.md), [EVENTS](../architecture/EVENTS.md), [STATE_MACHINES](../architecture/STATE_MACHINES.md), [STRATEGY](../testing/STRATEGY.md), [GOLDEN_SCENARIOS README](../testing/golden-scenarios/README.md) plus [section 34](../testing/golden-scenarios/34-gs-258-to-gs-272-phone-identity-and-the-authority-boundary.md), [EDGE_CASES README](../edge-cases/README.md) plus EC-142 to EC-149, [M19](../plans/M19-kyc-identity.md), [M16](../plans/M16-notification-center.md), [M04](../plans/M04-trader-portal.md), [INDEX](../INDEX.md), [STATE](../STATE.md), [`gates.mjs`](../../scripts/corpus/gates.mjs), [`falsify.mjs`](../../scripts/corpus/falsify.mjs).
- **Verification, run as pieces rather than as `pnpm run verify`:** `check:invariants` 5/5, `typecheck` clean across all workspaces, `lint` clean, `format:check` clean on the project's own glob, `vitest` 191 passed / 3 skipped, `gates.mjs check` **13/13**, `falsify.mjs` all gates clean and dirty with 8 scope cases. **One pre-existing failure, confirmed by stashing:** `prettier --check` flags `scripts/corpus/gates.mjs` and `scripts/corpus/falsify.mjs` on the **untouched** tree as well, and `scripts/` is not in `format:check`'s glob, so the repo's own gate does not read those files. Not fixed here: reformatting two files this session rewrote would bury the diff a founder has to read.
