---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 294: the event producer, and the one door it needs is not open

<!--index: placeholder -->

- **Objective:** build [P5](../plans/P5-payouts-and-wallet.md) section 8's `P5-n`, the event feed and the writer underneath it. [`apps/api/src/events.ts`](../../apps/api/src/events.ts) becomes the producer every P5 route and job emits through, satisfying the sink [`apps/worker/src/sweeps/ports.ts`](../../apps/worker/src/sweeps/ports.ts) already declared, and [`apps/admin/src/feed.ts`](../../apps/admin/src/feed.ts) becomes [M06](../plans/M06-admin-ops-console.md)'s fifth surface. **No ADR number and no migration number is allocated to this session and it may take neither.**
- **Done:** placeholder
- **Next:** placeholder
- **Blockers:** placeholder
- **Landmines:** placeholder
- **Files touched:** placeholder

---

### 1. THE PLAN, committed before a line of the producer exists

**The baseline was reproduced before anything was written**, each command run separately after
`pnpm install`: **33 of 33 gates, 12 of 12 invariants, `typecheck` exit 0, `lint` exit 0,
`format:check` clean, 183 test files / 3,521 passed / 6 skipped**. That is the dispatch's figure
exactly.

**No CLAIM row exists for this session** in [sessions/README](README.md), whose reservation table
ends at `230`, so rule 1's strike had nothing to strike and none was invented.

**THE FIRST MEASUREMENT REFUTES THE DISPATCH AND IT CHANGES WHAT THIS SLICE CAN BE.** The dispatch
says *"`events` is registered in `schema.ts` and `scope.ts` as of session 274 this evening."*
It is not, and [session 274](2026-08-27-session-274.md) says so in its own log, in terms:
*"`P5-n` IS NOT UNBLOCKED"*. What is planned below is what remains buildable with that door shut,
and what is not buildable is reported rather than reached around.
