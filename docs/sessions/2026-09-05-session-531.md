---
status: approved
depends_on: [README.md]
last_updated: 2026-09-05
---

## 2026-09-05 - Session 531: the parser only the tests installed, and the two sentences that were unfalsifiable until it was wired

<!--index: **THE ROW IS [ADR-340](../decisions/ADR-340.md), `proposed` and UNSIGNED.** All three of API_CONTRACT section 10's webhook rows answered `500 internal_error` on a live process, with **nine** `RawBodyUnavailableError` lines across three requests, because `installRawWebhookBodyParser` had **no caller under any `src/`**: the suite installed it and `compose` did not. **SO NO WEBHOOK RECEIVER'S OWN ANSWER WAS REACHABLE OVER HTTP AT ALL**, which made PSP settlement, Rise transfer confirmation and the KYC callback unverifiable end to end. **ALL THREE OF THE DISPATCH'S MEASURED CLAIMS REPRODUCE**, re-derived on `c0d15f0c` before the first edit. **`RouteDefinition` GAINS `rawBody`, THE THREE RECEIVERS DECLARE IT, AND `compose` REGISTERS THEM INSIDE ONE CHILD CONTEXT** carrying the buffer parser. **THE SCOPE RULING IS MEASURED RATHER THAN ARGUED**: [ADR-109](../decisions/ADR-109.md) clause 6 refused a root registration in a sentence that had never been run, so it was built and run, and it fails **136 tests across 7 files** in `apps/api`, every one a route handed a `Buffer` where its handler reads fields off an object, `POST /checkout` among them. **`CompositionReport.raw` NAMES THE SET** so a fourth raw route is a red case rather than a discovery. **A DEFECT THIS ROW CREATED AND THEN MET**: fastify 5.12.1 reports a non-default `application/json` parser as already present, so a child under a root that carries one throws `FST_ERR_CTP_ALREADY_PRESENT` at `ready()`, which would have broken the three suites that install on the root; `installRawBodyParser` removes before it adds and a case watches it. **TWO SENTENCES THAT COULD NOT BE CHECKED WHILE EVERYTHING ANSWERED 500 ARE WRONG THE MOMENT THEY CAN BE**: `webhooks-psp.ts` predicted **503** for itself twice and the measured answer is **404**, because `receivePspWebhook` resolves the `:provider` before it reads its dependencies; it also named `productionAdapters`, which is not a symbol in that file. The Rise and KYC predictions were re-measured and **both hold**. **THE DISPATCH'S OWN CITATION IS WRONG AND SO ARE TWO SHIPPED FILES**: the raw-body seam is [ADR-109](../decisions/ADR-109.md) **clause 6**, not ruling 4. **FOUR PARAGRAPHS AMENDED IN PLACE UNDER `RI-14`, CORRECTION BESIDE ORIGINAL, NOTHING DELETED.** **NINE LINE CITATIONS REPOINTED** under `RI-15`/`RI-16`; dated session logs and dated `STATE` entries deliberately not repointed, and one stale-pointer register cell reported rather than half-repaired. **THREE FALSIFICATION TRANSCRIPTS**, one per mechanism, each seeded and watched red. **A ROW `339` RESERVATION WAS WRITTEN, CLAIMED NOTHING, AND WAS REPLACED BY ITS OWNER'S REAL ROW WHEN [ADR-339](../decisions/ADR-339.md) LANDED ON `main` MID-SESSION**, which is [ADR-065](../decisions/ADR-065.md) T3 working; no reservation row survives in this diff, and `531` is measured rather than assumed. **NO SQL, NO MIGRATION, NO GATE, NO INVARIANT, NO LETTER, NO SLUG, NOTHING WEAKENED, NO TEST SKIPPED, NO FENCE WIDENED.** Counts at reporting time: suite **313 files / 7,674 passed / 10 skipped / 0 failed** against a `c0d15f0c` baseline reproduced before the first edit of **312 / 7,666 / 10 / 0**, a delta of **+8 cases in +1 new file with skips unchanged**; invariants **35 of 35** and gates **33 of 33**, both unchanged; lint, `format:check` and typecheck clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `340`: make `installRawWebhookBodyParser` register on the real composition path so that each webhook receiver's designed response is what a live process actually returns, and rule whether that registration is global or scoped to the webhook routes.

**One objective.** The fix is one property and one `register` call. The work is the scope ruling and proving it.

---

### 2. What was re-derived before the first edit

The dispatch named three measured claims and asked for every one to be re-derived rather than trusted. **All three hold.**

| Claim | Command | Held |
|---|---|---|
| All three webhook rows answer `500 internal_error` on a live process | `MERIT_API_SURFACE=public PORT=8899 node --experimental-strip-types apps/api/src/start.ts`, then `curl -X POST` at each row | **YES.** `500`, body `{"code":"internal_error"}`, three times |
| The log carries `RawBodyUnavailableError` for all three | `grep -c` over the process log | **YES**, **9** lines |
| `installRawWebhookBodyParser` is registered only by the test suite | `grep -rn` over `--include=*.ts` | **YES.** One definition, four imports, **no call under any `src/`** |

**Baseline on `c0d15f0c`, before the first edit:** suite **312 files / 7,666 passed / 10 skipped / 0 failed**; invariants **35 of 35**; gates **33 of 33**; lint, `format:check` and typecheck clean; clean tree. **`git diff --stat origin/main -- packages/db/migrations scripts/db` is EMPTY** and stayed empty.

**One thing the dispatch got wrong, and it is a citation.** It calls the seam *"ADR-109 ruling 4"*, as do `webhooks-psp.ts` and `webhooks-rise.ts`. Clause 4 is the unowned replay being a second type. The raw-body ruling is **clause 6**. Both source sites corrected in place, wrong number kept beside the correction.

---

### 3. What landed

1. **[`apps/api/src/registry.ts`](../../apps/api/src/registry.ts)**: `RouteDefinition.rawBody`, `BODY_METHODS`, the `defineRoutes` refusal for a raw body on a verb that carries none, `CompositionReport.raw`, `compose`'s partition and its single `app.register`, and `installRawBodyParser`.
2. **One property each** on the three receivers' route declarations.
3. **[`apps/api/test/raw-body-scope.test.ts`](../../apps/api/test/raw-body-scope.test.ts)**, eight cases in its own file.
4. **Four paragraphs amended in place** with their corrections beside them, per `RI-14`.
5. **Nine line citations repointed** across two plans, four ADRs and the golden-scenario registry.

---

### 4. The scope decision, which the dispatch asked to be ruled

**ROUTE-SCOPED, AND THE REFUSAL OF THE ALTERNATIVE IS A NUMBER RATHER THAN A SENTENCE.** [ADR-109](../decisions/ADR-109.md) clause 6 had already refused a root registration, in prose written before there were forty-seven routes to take JSON parsing away from. The counterfactual was built and run:

| Composition | `apps/api` |
|---|---|
| The child context, as shipped | **76 files, 2,511 passed, 5 skipped, 0 failed** |
| The parser on the root | **7 files failed, 136 tests failed** |

`checkout.test.ts` alone loses 67 cases. **The scope is visible in the diff in two places**: `compose` registers raw routes on `scope` and everything else on `app`, and `CompositionReport.raw` states the set a case then pins.

---

### 5. What was found that the row did not ask for

**`webhooks-psp.ts` PREDICTED ITS OWN LIVE ANSWER TWICE AND WAS WRONG BOTH TIMES.** Its header and its `productionDeps` docblock each say the route answers **503**. Measured on a live process with the parser registered, it answers **404**, because `receivePspWebhook` step 1 resolves the `:provider` before step 2 consults `store` and `applier`, and `adapters: () => null` makes every provider name unknown. The header also named `productionAdapters`, a symbol that is not in the file.

**NEITHER IS A DEFECT IN THE RECEIVER.** 404 is right for an unknown provider. What was wrong is a sentence about it, written when no process could be asked. **The Rise and KYC predictions were measured the same way and both hold**, 503 and 404 respectively.

---

### 6. Falsification

| Seed | Case that went red | Failure |
|---|---|---|
| `rawBody: true` deleted from all three receivers | the designed-answer case and the report case | `expected 500 to be 503`; `expected [] to strictly equal [ Array(3) ]` |
| Parser installed on the ROOT | the sibling-parses case | `expected { bytes: true, ... } to match object { bytes: false, ... }`, plus 136 failures across `apps/api` |
| `removeContentTypeParser` deleted | the root-install-still-boots case | `FastifyError: Content type parser 'application/json' already present.` |

---

### 7. Blockers and landmines

- **THE RAW CONTEXT'S ROUTES ARE REGISTERED AT `ready()`.** Nothing observable changes, because `listen` and `inject` both await boot and `compose` still refuses every collision synchronously. But `app.hasRoute` for a raw path is false in the window between `compose` returning and the app booting. No caller in this tree inspects the router there, checked by grep, which is a weaker instrument than a case.
- **THE ALIAS.** `installRawBodyParser` answers to two names, because three suites held by concurrent rows import the old one. Named in the approval block as a thing the founder may want undone.
- **`repo-invariants.mjs`'s stale-pointer register names `routes/webhooks-psp.ts:66`, now line 78**, and its recorded distance moved with the head. Reported rather than half-repaired: that table is a derivation over the whole tree.
- **The `rawBodyOf` message still names the PSP handler for all three receivers.** A wording debt `webhooks-kyc.ts` and `webhooks-rise.ts` both already record, still owed.

---

### 8. Files touched

`apps/api/src/registry.ts`, `apps/api/src/index.ts`, `apps/api/src/routes/webhooks-psp.ts`, `apps/api/src/routes/webhooks-kyc.ts`, `apps/api/src/routes/webhooks-rise.ts`, `apps/api/test/raw-body-scope.test.ts` (new), `docs/decisions/ADR-340.md` (new), `docs/decisions/ALLOCATION.md`, `docs/decisions/README.md`, `docs/sessions/README.md`, `docs/STATE.md`, and the nine citation repairs in `docs/plans/P6-live-tier.md`, `docs/plans/WAVE-06-admin-console-transport.md`, `docs/decisions/ADR-161.md`, `docs/decisions/ADR-166.md`, `docs/decisions/ADR-182.md`, `docs/decisions/ADR-221.md` and `docs/testing/golden-scenarios/39-fixture-status-and-blockers.md`.

---

### 9. Next

The founder's `E2` read of [ADR-340](../decisions/ADR-340.md) and the three questions its approval block names. Then, in order: any one of the three webhook receivers' dependencies, all of which are now the only thing between a signed payload and a 200; and a re-read of `webhooks-psp.ts`'s remaining 503 prose against a process, which nobody could do until this row.

**Session number, and it stopped being an assumption before this log was pushed.** `528` carried [ADR-337](../decisions/ADR-337.md) and `529` carried [ADR-338](../decisions/ADR-338.md). **THIS PARAGRAPH READ:** *"`339` is held by an unmerged sibling branch this session cannot read, so 531 assumes that branch carries 530. The dispatcher resolves it at merge if that is wrong."* **[ADR-339](../decisions/ADR-339.md) then landed on `main` at `9906c43b` while this branch was open and it carries session **530**, so `531` is measured. `main` was merged in, the four registry files conflicted exactly as the row predicted, and **the row `339` reservation this session wrote was replaced in place by its owner's real row**, which is [ADR-065](../decisions/ADR-065.md) T3 working rather than a resolution anybody had to invent. **RE-RUN ON THE MERGED HEAD**: suite **314 files / 7,682 passed / 10 skipped / 0 failed**, which is this row's 313 / 7,674 / 10 / 0 plus the one file and eight cases [ADR-339](../decisions/ADR-339.md) brought with it; gates **33 of 33**, invariants **35 of 35**, lint, `format:check` and typecheck clean. This row's own delta is unchanged at **+8 cases in +1 new file with skips unchanged**.
