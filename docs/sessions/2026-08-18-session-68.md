---
status: approved
depends_on: [README.md]
last_updated: 2026-08-18
---

## 2026-08-18 - Session 68: CI-06s, every probe is run and pinned, and OI-07's fourth occurrence repaired

- **Done:** **`CI-06s` exists and it found a live violation on arrival.** Every `scripts/db/probe_*.sql` on disk must appear as a psql **step** in [`corpus.yml`](../../.github/workflows/corpus.yml) **and** as a needle in `CI-06h`'s required list, and a needle naming a probe no file provides is a finding. **The fourth occurrence of `OI-07` was repaired in its own commit**: `probe_rule_states_high_water_bound.sql` was on disk, was wired, and `high_water_bound` appeared nowhere in [`gates.mjs`](../../scripts/corpus/gates.mjs). **19 of 19 gates**, falsify green with all three directions watched failing, vitest 908 passed / 40 skipped.
- **Next:** nothing owed by this objective. `CI-06o` and `CI-06t` are sessions 67 and 69.
- **Blockers:** none.
- **Landmines:**
  - **THE DESK WAS RIGHT AND I CHECKED ANYWAY, WHICH IS THE POINT.** Nine probes on disk, nine wired, eight pinned. Exactly one violation and no second, verified by running the greps rather than by trusting the brief.
  - **THE FOURTH OCCURRENCE WAS MINE.** The probe was written and wired in the session that landed ADR-053, and the needle was not written in that session. Wiring a probe and pinning it are two edits in two files, and the second is the one forgotten, because the first is the one that makes CI green.
  - **REPAIR FIRST, THEN SEED, OR THE TWO PROVE EACH OTHER.** The unpinned direction is the one this session repaired, so its scope case invents its own probe rather than reusing the repaired one.
  - **THE GATE READS ITS OWN FILE AND HAD TO BE BOUNDED.** `CI-06h`'s needle list is a local `const` inside its `run()`, so the pinned side is read as text from `ci06h`'s block alone. Unbounded, this gate's own prose names probe filenames and would report every probe pinned on the day it was written.
  - **THE BLOCK BOUND CANNOT NAME THE NEXT GATE.** The declarations are not alphabetical: `ci06i` is declared at line 986 and `ci06h` at 1078. The first bound searched forward for `const ci06i` and found nothing, and the gate's own sentinel caught it: *"CI-06s read no probe filename out of CI-06h's block."* It searches for the next `const ci06X = {` instead.
  - **ADDING A GATE MOVES `gate_count` AND THAT REACHES OUTSIDE ANY GATE SESSION'S FENCE.** `generate` rewrote eight spans across four files, two of which are outside this fence. Unavoidable, stated in the PR body, and sessions 67 and 69 will collide on the same spans.
- **Files touched:** [`gates.mjs`](../../scripts/corpus/gates.mjs), [`falsify.mjs`](../../scripts/corpus/falsify.mjs), [STRATEGY](../testing/STRATEGY.md), [STATE](../STATE.md), [INDEX](../INDEX.md), [session 38](2026-08-16-session-38.md) (span only), this log.
