---
status: approved
depends_on: [README.md]
last_updated: 2026-09-05
---

## 2026-09-05 - Session 546: the console stays shut because its lock is a purchase, and the crash a cookie could trigger in front of it is closed

<!--index: **THE ROW IS [ADR-343](../decisions/ADR-343.md), `proposed` and UNSIGNED.** The dispatch asked for a console that answers and **the honest deliverable is a console that refuses correctly**. Its headline reproduces by injection over a real `compose()`: **23** `/admin/*` routes registered on the operator surface, **13 answering 401 to an anonymous caller and to a cookie alike, and TEN answering 401 then 500**, which is [ADR-192](../decisions/ADR-192.md) finding 1's partition at a tree eleven wiring slices later. **THE 500 WAS AN UNAUTHENTICATED CRASH PATH RATHER THAN AN OPERATOR BEING TOLD THE TRUTH**: the cheapest way for anybody at all to make the admin surface throw and log an error was to send a `Cookie:` header, holding no credential of any kind. **BOTH JUDGEMENTS THE ROW RESERVED ARE REFUSALS AND BOTH ARE ARGUED RATHER THAN ASSUMED.** No `OperatorAssertionVerifier` is written: C-08 is phishing-resistant hardware-key SSO, a verifier would have to establish signature, issuer, audience, replay **AND that the authentication behind the assertion was actually phishing-resistant**, and that fifth item is the entire content of the control and a fact about a provider Merit has not bought, so an implementation checking the first four **would pass every test anybody would write for it and satisfy none of it**. And `readLiability` and `exportEvidence` are **LEFT OWED, in those words**: composing anything needs an `AdminSourceBackend` over `systemDb('operator-console')`, `ApiDb` declares five doors and none is it, [ADR-171](../decisions/ADR-171.md) clause 1 refuses to add it and section 9 conditions it on the very session source refused one clause earlier. **OPENING THE DOOR ANYWAY WAS CONSIDERED AND REFUSED WITH THE ARGUMENT**: a live system handle onto every account in a deployable where nobody can authenticate, bought for zero observable behaviour, with a moved wiring triple reporting it as progress. **WHAT LANDS IS THE DEFECT UNDERNEATH.** An unwired session source resolves to `unknown` rather than throwing, so all **23** routes now answer 401 to a cookie and to nothing alike ([ADR-192](../decisions/ADR-192.md) clause 2's ground: a deployment that cannot authenticate anyone has authenticated nobody); an unwired read source answers **503 behind the authorization and never in front of it**, built as a handler code exactly as four sibling modules build it, with `server.ts` **byte for byte unchanged**. **THIS CLOSES [ADR-190](../decisions/ADR-190.md) ITEM 2 WHERE [ADR-192](../decisions/ADR-192.md) SECTION 3 LEFT IT OPEN, "for `admin-reads.ts` alone, where the mixing is"**, and no fifth class is minted: `AdminReadError` goes from **22 throw sites of which 2 were about wiring to 20 of which 0 are**. **NO PORT IS INSTALLED, SO THE WIRING TRIPLE DOES NOT MOVE** and `wiring.test.ts:1554` is untouched at `{ declared: 24, wired: 10, blocked: 14 }`, which is the mechanical confirmation of both refusals rather than a claim about them. **THREE DISPATCH FIGURES DID NOT SURVIVE**: the 500 is TEN routes and not 23, two of three cited line numbers are wrong (`currentSessionSource` was at **233** and not `:1087`), and the port has **six of seven** methods with producers and not five, `exportEvidence` being composed already and there being no `readEvidence` at all. **FOUR SENTENCES AMENDED IN PLACE UNDER `RI-14`**, including `admin-feed.ts`'s claim that a 503 *"would need a new canonical code"*, which [ADR-192](../decisions/ADR-192.md) finding 9 had already refuted and four modules were already contradicting. **[ADR-184](../decisions/ADR-184.md) SECTION 7.2 IS NOT AMENDED BECAUSE IT IS NOT WRONG**: it ruled on the READ port and the 500 that mattered was the SESSION port's, which its own 7.3 reported as a coupling *"for the wiring slice"*. **THE CONTROL IS A SECOND LEG ON [ADR-192](../decisions/ADR-192.md)'s SURFACE SWEEP**, and the gap it fills is the whole story: that sweep injected **no credential of any kind**, which is exactly half the surface. **FOUR RESERVATION ROWS WRITTEN AND ALL FOUR CLAIM NOTHING**; `339` names its branch because a commit adding its file exists, and `340` to `342` say they cannot name one because after fetching every head the file is absent from every ref. **NO SQL, NO MIGRATION, NO GATE, NO INVARIANT, NO LETTER, NO SLUG, NO TEST SKIPPED OR DISABLED, NOTHING WEAKENED, NO FENCE WIDENED AND NO AUTHORIZATION REQUIREMENT RELAXED** (`authorizeAdmin` and `resolveAdminRole` are byte for byte unchanged). **A DISPATCHER CORRECTION FIRED AT 19:17:26Z MID-SESSION AND LARGELY CONFIRMS THIS ENTRY**, agreeing the port is gated on a purchase and forbidding an invented auth scheme; **its operative instruction to write the verifier is REFUSED because it contradicts its own prohibition** (a JWKS verifier needs three security parameters the corpus does not carry: the `alg` allowlist, a clock-skew tolerance, and whether phishing-resistance must be evidenced and by which claim) and because **`refusingAssertionVerifier` and `operatorFromAssertion` have ZERO callers**, so it would replace an uncalled refusal with uncalled cryptography inside an auth boundary. **THE CORRECTION FOUND A REAL ERROR IN THIS ENTRY'S FIRST DRAFT, AMENDED IN PLACE**: the `systemDb('operator-console')` door is owed by the READ source ALONE, because `operators` and `operatorSessions` are class `firm` and `ApiDb.firm()` reaches both today, so a database-backed `AdminSessionSource` is **BUILDABLE and DECLINED rather than blocked** -- [ADR-237](../decisions/ADR-237.md) section 8's objection is spent now that clause 1 answers 401 either way, and what carries the refusal instead is that wiring it buys no observable change while costing a real transaction on every unauthenticated probe of the admin surface. **A DISPATCHER DECISION FIRED AT 20:19:59Z AND RULED: LEAVE `AdminSessionSource` DECLINED**, supplying a reason this session did not have -- **an install nothing can exercise is an install nothing falsifies and it arms itself silently**, so a port wired against an empty table first really runs the day somebody lands the identity provider, opening a console that can freeze accounts and correct ledgers as a SIDE EFFECT of an unrelated slice -- and a **third argument that makes the cost worse than neutral**: the transaction would sit behind every `/admin/*` request including those answering 401 to a caller holding nothing, so **an anonymous prober gets to schedule database work**, while `start.ts:206` and `:245` say the rate limit `INV-M11-05` requires "EXISTS NOWHERE IN THIS TREE". **THE DECISION CITED THAT AT `start.ts:174-176` AND IT IS AT `:206` AND `:245`.** The `setAdminSessionSource` blocker entry is rewritten to say what was established, including that [ADR-237](../decisions/ADR-237.md) section 8's 401-misreport objection is SPENT. **ON THE VERIFIER SLICE THE ANSWER IS THAT IT IS ALREADY LANDED**: `refusingAssertionVerifier` already answers `unconfigured` naming the missing variables and `unavailable` when all three are set, both arms tested, so there is nothing to write; what is owed is a verifier that checks an assertion, which needs the format, which is the purchase, **and a CALLER, since nothing turns `unconfigured` into a 503 because nothing calls it at all**. **TWO DECISION FIGURES DID NOT HOLD**: `ADR-339` is on `main` and **`ADR-340` is not**, so `340` keeps a reservation beside `341` and `342` while this session's `339` reservation is dropped for `main`'s real row; and the citation above. **The row `342` comparison is RELAYED AND NOT VERIFIED**, `ADR-342.md` being on no fetchable ref. **`origin/main` moved to `9906c43b` mid-session and is MERGED rather than rebased over**, colliding on the session number: `main`'s `ADR-339` session is 530, so this one is **531**. Counts at reporting time: suite **312 files / 7,668 passed / 10 skipped / 0 failed** against a `c0d15f0c` baseline reproduced BEFORE the first edit of **312 / 7,666 / 10 / 0**, a delta of **+2 cases in existing files with SKIPS UNCHANGED and no file added** **AND THAT PAIR WAS MEASURED BEFORE THE MERGE. AFTER MERGING `origin/main` AT `9906c43b` THE SUITE IS 312 files -> 313 files / 7,676 passed / 10 skipped / 0 failed**, derived at reporting time; the extra file and the further cases are `main`'s session 530 work arriving, not this branch's. **The `+2` is stated against the base it was actually measured on (`c0d15f0c`) and the post-merge base was NOT separately measured**, so no delta is claimed against `9906c43b`., five further cases amended in place; invariants **35 of 35** and gates **33 of 33**, both unchanged. **The baseline invariant figure is the post-install one and the pre-install run is recorded rather than smoothed**: `RI-18` ERRORED at `Cannot find module 'typescript'` and the runner refused to call that 34 of 35. **Four seeded violations watched red and restored green**, the pair that matters pulling opposite ways. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `343`: wire the operator session source and the admin read source so the console answers, composing what exists rather than rewriting it, and argue two reserved judgements rather than assume them.

**One objective, and it resolved into a refusal plus a defect.** The wiring is refused on both halves with the argument written out. What lands is the unauthenticated crash path the dispatch found underneath it.

---

### 2. What was re-derived before the first edit

**The finding reproduces and its scope does not.** Derived by composing every route module on disk and injecting each registered route twice through Fastify's own `inject`, once bare and once carrying `merit_admin_session=x`:

|                                                      | `origin/main` at `c0d15f0c` |
| ---------------------------------------------------- | --------------------------- |
| `/admin/*` routes registered on the operator surface | **23**                      |
| registered / withheld                                | **28 / 46**                 |
| answering 401 anonymous and 401 with a cookie        | **13**                      |
| answering 401 anonymous and **500** with a cookie    | **10**                      |

**The ten are `admin-reads.ts`'s seven, `admin-breaker.ts`'s two and `admin-feed.ts`'s one**, every one of them routed through `admin-reads.ts`'s `adminHandler`. The thirteen go through their own builders and [ADR-192](../decisions/ADR-192.md) clause 2 moved them to 401 on 2026-08-28.

**A grep would have found 16 of the 23, and one was run first.** `admin-wallet.ts`, `admin-payouts.ts` and `admin-certificates.ts` build their paths through their own route builders and match no `path: '/admin` needle, which is the reason the census was taken by injection.

| Baseline command                                   | Result                                           |
| -------------------------------------------------- | ------------------------------------------------ |
| `node scripts/corpus/gates.mjs check`              | 33 of 33                                         |
| `node packages/tooling/checks/repo-invariants.mjs` | 35 of 35                                         |
| `pnpm vitest run`                                  | 312 files / 7,666 passed / 10 skipped / 0 failed |
| `pnpm run typecheck` / `lint` / `format:check`     | exit 0 / exit 0 / clean                          |

**THE FIRST INVARIANT READING WAS NOT A MEASUREMENT AND IS RECORDED RATHER THAN SMOOTHED.** Run before `pnpm install`, `RI-18` ERRORED at `Cannot find module 'typescript'` and the runner refused to call that 34 of 35, on its own rule that _"a check that crashed is not a check that held"_. That refusal is the control working. The baseline above is the post-install run.

---

### 3. The two refusals

**THE VERIFIER.** `SECURITY` C-08 is _"RBAC on admin, admin on a separate origin, IP allowlist, hardware-key SSO"_ and its threat row S answers a phished admin credential with phishing-resistance and nothing else. A verifier satisfying this repository must establish five things: signature against the configured JWKS, issuer, audience, unexpired and unreplayed `assertionId`, **and that the authentication behind the assertion was actually phishing-resistant**. Items 1 to 4 are a library and a cache. **Item 5 is the whole control**, it is a fact about a provider Merit has not bought, and an implementation that skipped it **would pass every test anybody would write for it**. That is the dispatch's own sentence about a security hole with a green test suite, and it is why none is written.

**And item 4 has a second blocker that is not the provider.** A verified assertion becomes a session by writing an `operator_sessions` row and **nothing in this repository can write one**: Merit is passwordless by [ADR-039](../decisions/ADR-039.md) and [ADR-237](../decisions/ADR-237.md) section 8 already ruled that a session source over an empty table answers every real operator 401.

**THE COMPOSITION.** `readLiability` and `exportEvidence` are **left owed**. Composing anything onto `AdminReadSource` needs an `AdminSourceBackend`, whose one method yields an `AdminSourceTx`, for which `systemDb('operator-console')` is the only supplier. `ApiDb` declares five doors and none is it; [ADR-171](../decisions/ADR-171.md) clause 1 refuses to add it and section 9 conditions it on the session source refused above. **A partial liability figure would also be the worse failure the row named**: `EligibleNext7d` declares `total_cents`, `account_count` and `by_day[]` and not one measured term, so no field could carry the fact that a number was incomplete.

---

### 4. What landed

|                                                                           | Before                   | After                         |
| ------------------------------------------------------------------------- | ------------------------ | ----------------------------- |
| unwired session source, caller sends a cookie                             | **500 `internal_error`** | **401 `unauthenticated`**     |
| unwired session source, caller sends nothing                              | 401 `unauthenticated`    | 401 `unauthenticated`         |
| unwired read source, **admitted operator**                                | 500 `internal_error`     | **503 `service_unavailable`** |
| `AdminReadError` throw sites in the module, and how many are about wiring | 22, of which **2**       | 20, of which **0**            |
| `wiring.test.ts:1554`                                                     | `{ 24, 10, 14 }`         | `{ 24, 10, 14 }`, untouched   |

**The 503 is reached only after `authorizeAdmin` returned `allowed`**, so no deployment state reaches a caller who has proved nothing. `server.ts` is byte for byte unchanged, which is [ADR-192](../decisions/ADR-192.md) clause 4 kept rather than reopened.

---

### 5. Verification and falsification

| Command                                                            | Baseline              | This branch               |
| ------------------------------------------------------------------ | --------------------- | ------------------------- |
| `gates.mjs generate && check`                                      | 33 of 33              | **33 of 33**              |
| `repo-invariants.mjs`                                              | 35 of 35              | **35 of 35**              |
| `pnpm vitest run`                                                  | 312 / 7,666 / 10      | **312 / 7,668 / 10**      |
| typecheck, lint, `format:check`                                    | exit 0, exit 0, clean | **exit 0, exit 0, clean** |
| `git diff --stat origin/main -- packages/db/migrations scripts/db` | n/a                   | **EMPTY**                 |

**Four seeds, four caught, tree clean after every one.**

| #   | Seed                                                                                     | Result                      |
| --- | ---------------------------------------------------------------------------------------- | --------------------------- |
| A   | the throw is reintroduced, which is `origin/main`'s exact behaviour                      | **8 failed across 2 files** |
| B   | the read source is checked BEFORE `authorizeAdmin`, so a 503 reaches an anonymous caller | **7 failed across 2 files** |
| C   | `authorizeAdmin` admits a `not-an-operator` session as `owner`                           | **2 failed**                |
| D   | `resolveAdminRole` defaults an unrecognised role to `readonly`                           | **2 failed**                |

**A and B pull in opposite directions**, which is the pair worth reading: A proves the defect cannot return, B proves the fix cannot be over-applied into the disclosure [ADR-192](../decisions/ADR-192.md) closed. **C and D are the row's own falsification request**, and they fire because the guard is untouched.

---

### 6. Next, blockers, landmines

**Next.** Nothing in this area is a code task. The operator console needs the identity-provider purchase, then a verifier, then a directory write, then a login route, then the door.

**Blockers.** All of the above reduce to one purchase. **Nine wiring entries now reduce to it**, which is the schedule exposure [STATE](../STATE.md)'s surviving-items table already names in six of nine rows.

**Landmines.**

1. **`RI-18` errors rather than fails when `node_modules` is absent**, and the runner correctly refuses to report a lower count. A session that reads the pre-install line as a baseline will report 34 of 35 and be wrong about which check is unknown.
2. **The `/admin/*` census cannot be taken by grep.** Three modules build their paths through their own builders. Injection over `compose()` is the only honest reader, and 16 is the number a needle returns.
3. **The surface sweep now has two legs and a third is missing**: nothing injects a WELL-FORMED cookie against a wired session source across the whole surface, because no deployment can wire one. The day the verifier lands, that leg is owed.
4. **`admin-writes.ts` and `admin-reads.ts` each declare their own `adminHandler` and `toAdminRoutes`.** They are different functions with the same names and different refusal shapes. A reader who greps one name and reasons about the surface will be reasoning about half of it.

---

### 6b. The session number was 531 and is 546

**THE NUMBER WAS TAKEN WHILE THIS BRANCH SAT UNMERGED.** This row wrote its
log as session `531` and never opened a pull request, and in the interval
row `340` landed on `main` under the same number. `531` therefore belongs to
the merged row and this one moves to `546`, derived at the integration merge
across every pushed ref: `521` to `536`, `539` and `542` to `545` are in use,
so `546` is above the maximum rather than in the `537`, `538`, `540` or `541`
gaps, which an unpushed sibling may still be standing in. The renumber is
followed through this heading, `ADR-343`'s `- **Origin:**` line, the
`ALLOCATION` cell, the sessions README row and the `STATE` heading.

**THIS IS THE SECOND CONSEQUENCE OF THE SAME DISPATCHER DEFECT SECTION 7
RECORDS**, and it is worth stating beside the first: a branch that is told
it cannot open a pull request does not just finish late, it finishes holding
a number it cannot defend. Row `342` lost `533` the same way in the same
merge.


### 7. A dispatcher correction, and the one clause of it that does not hold

A scheduled correction fired at **2026-09-05T19:17:26Z**, after dispatch and before this log landed. **It is right about the thing that matters** and section 3 had reached the same place independently: the port is gated on a purchase, `SECURITY:26` calls it _"a purchase Merit has not made"_, and no authentication scheme may be invented.

**ITS OPERATIVE INSTRUCTION IS REFUSED, BECAUSE IT CONTRADICTS ITS OWN PROHIBITION.** _"Write the verifier against the declared seam"_ asks for the one artifact that cannot be written without inventing three security parameters this corpus does not carry: the `alg` allowlist, a clock-skew tolerance, and **whether phishing-resistance must be evidenced on the assertion and by which claim**. `CLAUDE.md` rules that a silent constitution means propose and proceed **on approval**, and this entry is UNSIGNED.

**AND ITS PREMISE, _"the code stops being the blocker"_, IS FALSE IN BOTH DIRECTIONS.** Derived at the moment of reporting: `refusingAssertionVerifier` and `operatorFromAssertion` have **zero callers** outside their own suite, and the thing that would call a real verifier is a login route minting an `operator_sessions` row, which needs a verified assertion, which needs the provider. **A verifier written today replaces an uncalled refusal with uncalled cryptography, and no test here could tell a correct one from a plausible one.**

**WHAT THE CORRECTION DID PRODUCE IS A REAL ERROR IN THIS SESSION'S FIRST DRAFT**, found by being sent back to the primary sources. The entry listed `systemDb('operator-console')` as owed by the session source. **It is not.** `operators` and `operatorSessions` are class `firm` ([`scope.ts:1596`](../../packages/db/src/scope.ts), [`:1600`](../../packages/db/src/scope.ts)), so `ApiDb.firm()` reaches both today and `resolveOperatorSession` is already the verdict. **The door is owed by the READ source alone**, whose `AdminSourceTx` spans `owned` keys the firm door refuses by construction.

**SO A DATABASE-BACKED `AdminSessionSource` IS BUILDABLE AND IS DECLINED RATHER THAN BLOCKED**, on a narrower ground than the first draft gave. [ADR-237](../decisions/ADR-237.md) section 8 refused it because a 401 would misreport an unfinished deployment; **clause 1 has made that 401 the answer either way, so the objection is spent** and a new one has to carry the refusal. It does: wiring it buys **no observable change** and costs a real transaction against two tables **on every unauthenticated probe of the admin surface**, a database round trip an anonymous caller gets to schedule. It would also move the triple to `wired: 11` and report as progress a port whose table cannot be filled.
