---
status: approved
depends_on: [README.md]
last_updated: 2026-08-29
---

## 2026-08-29 - Session 405: the cause of the eligibility rejection is gone and the rejection is right anyway, so the reason is repaired rather than cleared

<!--index: **THE MEASUREMENT SAYS DO NOT CLEAR IT, AND THAT IS THE DELIVERABLE.** `databaseAccountReads` is wired in production at [`start.ts:76`](../../apps/api/src/start.ts) and its `readEligibility` rejects on `ELIGIBILITY_BLOCKER` ([`account-reads.ts:851`](../../apps/api/src/routes/account-reads.ts)); the schema delta that reason names IS spent, and the refusal survives on three grounds none of the reason mentioned. **THE TRAP THE DISPATCH NAMED IN ADVANCE IS THE ANSWER: `rule_states` HOLDS ZERO ROWS, MEASURED LIVE** on PostgreSQL 16.13 rebuilt from empty over all **62** merged migrations and **115** tables, `select count(*) from rule_states` = **0**, read from the catalogue rather than assumed. **AND NOTHING WOULD PUT A ROW THERE.** The single insert site in the whole tree is [`state-writer.ts:714`](../../apps/worker/src/batch/state-writer.ts), swept over every `apps/*/src` and `packages/*/src` with comments stripped rather than grepped; its only caller is [`nightly.ts:316`](../../apps/worker/src/batch/nightly.ts); **no adapter implements `BatchPorts` over Postgres**; and [`index.ts:154`](../../apps/worker/src/index.ts) EXPORTS the batch without scheduling it, in a barrel with **zero** top-level statements. **A SECOND GROUND THE DISPATCH DID NOT CARRY AND IT IS INDEPENDENT OF ROWS: EVEN GIVEN AN ADAPTER THE WRITER WOULD REFUSE.** [`RuleStateWriterIo.encodeEngineGates`](../../apps/worker/src/batch/state-writer.ts) at [`:337`](../../apps/worker/src/batch/state-writer.ts) has **no implementation under any `src/`** and [`UNWIRED_RULE_STATE_WRITER_IO:366`](../../apps/worker/src/batch/state-writer.ts) throws by name, because the stored encoding of `engine_gates` is `B5` term 2 and is a corpus amendment rather than a line of code. **A THIRD, AND IT IS THE SYMMETRIC HALF NOBODY HAD STATED: THE SAME ABSENCE BINDS THE READER.** [`types.ts:1006`](../../packages/rules-engine/src/types.ts) types `RuleState.engineGates` as `EngineGateResults` and [`schema.ts:824`](../../packages/db/src/schema.ts) types the column `jsonb`, so an adapter handed a row would have to INVENT a decoding, which is the same act on the read side that the writer refuses on the write side. **SO THE REJECTION STAYS AND THE REASON IS REWRITTEN**: serving a confident verdict computed off an empty table is [`ADR-190`](../decisions/ADR-190.md)'s failure exactly, and a 503 is the honest answer. **`ADR-215` IS NOT TAKEN AND THE NUMBER GOES BACK TO THE POOL UNSPENT.** No ruling was missing: `INV-M5-02` already binds the two endpoints, [`ADR-207`](../decisions/ADR-207.md) already ruled the storage, and every sentence that landed is transcription of a measurement. Row `210` and `RI-20` came back unspent the same night and this is the third. **THE REPAIR ARRIVES WITH A COMPARATOR RATHER THAN A PROMISE, WHICH IS THE WHOLE POINT OF THE SLICE.** The old reason was false on `main` for two sessions and **no gate could see it**: `RI-14` reads EXPORTS and a migration column is not an export, `RI-15` and `RI-16` do not reach a claim inside a string literal. **Five cases in [`account-reads.test.ts:847`](../../apps/api/test/account-reads.test.ts) derive each clause at its own source** -- the three columns out of the migration corpus, the single insert site out of a comment-stripped sweep of every deployable, the two files that name `runNightlyBatch` at all, the `RuleStateWriterIo` declaration census, and the engine type against the column type -- so the next drift is red rather than quiet. **SEVEN SEEDS, SIX RED AND ONE GREEN CONTROL**, applied to the committed shape and each restored from a byte copy with SHA-256 checked both ways, `git status --porcelain` empty after every one. **THE SWEEP CAUGHT ITSELF ON ITS FIRST RUN AND THAT IS THE MOST USEFUL RESULT NOBODY ARRANGED**: the blocker NAMES `runNightlyBatch` inside a string literal, comment stripping does not reach a string, and the case counted the reason as a caller. The exclusion is in the file with that finding written beside it, which is `RI-15`'s blindness met from the inside. **THE HEADER IS REWRITTEN IN EXACTLY THE TWELVE LINES IT OCCUPIED, AND THAT IS A CONTROL RATHER THAN A FLOURISH.** A sixteen-line draft moved `ELIGIBILITY_BLOCKER` from `851` to `855` and turned **`RI-15` and `RI-16` RED on three citations at once** -- two inside [`wiring.test.ts`](../../apps/api/test/wiring.test.ts), which is **session 404's live file** -- so the tree was rewritten to disturb neither, measured at 18 of 18 before and after. **The invariant runner was the thing that caught it and a reader would not have been.** **A FINDING IN NO DISPATCH: [`schema.ts:835`](../../packages/db/src/schema.ts) SAYS THE ENGINE'S TYPE `was not persistable in this schema, which is why readEligibility rejects in production today`, AND HALF OF THAT IS NOW WRONG.** It still rejects; that is no longer why. `packages/db` is session 406's fence; **reported, not repaired.** **AND `usePayoutBackend` IS NOT WIRED AND NOTHING HERE CLAIMS IT**: the wiring triple is unmoved at `{ declared: 23, wired: 6, blocked: 17 }` and [`ADR-213`](../decisions/ADR-213.md) section 8's four grounds stand, its item 2 having named this repair as owed. Counts derived at reporting time: **33 of 33** gates, **18 of 18** invariants read off the runner's own last line, typecheck exit 0, lint 0, `format:check` clean, `falsify.mjs` read as OUTPUT with all 33 gates clean-and-dirty plus 80 scope and 10 loader cases and the tree clean after it, **62** migrations and **115** live tables, `rule_states` **29** columns and **0 ROWS**, and **246 files / 6,134 passed / 6 skipped** against an `origin/main` baseline at `1259b00` of **246 / 6,129 / 6** reproduced before a line changed. The delta is **+5 cases and 0 files**. -->

**Objective.** Measure whether `readEligibility`'s `ELIGIBILITY_BLOCKER`
rejection still has a cause, and clear it only if it does not.

**Outcome.** **It has a cause. Three, and the reason named none of them.** The
rejection stands, the reason is repaired against the tree, and **`ADR-215` is
returned to the pool unspent.**

### Done

- **[`account-reads.ts`](../../apps/api/src/routes/account-reads.ts)**:
  `ELIGIBILITY_BLOCKER` at [`:851`](../../apps/api/src/routes/account-reads.ts)
  rewritten to the three live causes, and the header's `/eligibility` paragraph
  rewritten **in exactly the twelve lines it occupied**. Nothing else in the
  file moved: no port wired, no method changed, no status changed.
- **[`account-reads.test.ts`](../../apps/api/test/account-reads.test.ts)**: five
  cases at [`:847`](../../apps/api/test/account-reads.test.ts) deriving each
  clause at its own source, and the existing blocker-pattern case repointed off
  `lifetimeSettledCents`, which the repaired reason uses to say the opposite.
- `ALLOCATION` row `215` amended in place, `STATE.md` appended, this file.

### 1. The measurement, which is the deliverable

Every premise the dispatch carried was re-derived. **All of them held, and the
answer they add up to is the opposite of the one the slice was priced for.**

| Question | Measured |
|---|---|
| Do the three columns exist? | **Yes.** `0065` declares all three, read out of the migration corpus |
| Does the writer set them? | **Yes.** Session 400, `RULE_STATE_WRITE_COLUMNS` at 26 |
| **How many rows does `rule_states` hold?** | **ZERO**, live, over 62 migrations and 115 tables |
| What inserts a row? | **One site in the tree**, [`state-writer.ts:714`](../../apps/worker/src/batch/state-writer.ts) |
| What calls it? | [`nightly.ts:316`](../../apps/worker/src/batch/nightly.ts), and nothing calls THAT |
| Does anything schedule the batch? | **No.** Two files in all of `src/` name `runNightlyBatch`: the module that declares it and the barrel that exports it. `apps/worker/src/index.ts` has **zero** top-level statements |
| Is there a `BatchPorts` adapter over Postgres? | **No.** The only implementation is `scripts/demo/world.ts`, in memory, whose `writeRuleState` refuses |
| **Could the writer write, given one?** | **No.** `encodeEngineGates` has no implementation under any `src/`; `UNWIRED_RULE_STATE_WRITER_IO` throws by name |
| **Could this reader decode a row, given one?** | **No.** `RuleState.engineGates` is a structure and the column is opaque `jsonb`. `B5` term 2 again, from the other side |

**The last two are not in the dispatch and neither depends on the row count.**
Clause 2 was reachable from session 400's own blocker list; clause 3 is the
symmetric half and nothing in the corpus had stated it.

**`ResolvedPlan` IS NOT A BLOCKER HERE AND IT WAS CHECKED RATHER THAN ASSUMED.**
[ADR-213](../decisions/ADR-213.md) section 8 item 1 blocks `usePayoutBackend` on
it: `plan_versions` and `plan_version_sizes` are scope class `firm` and no
`ScopedTx` reaches either. `ApiDb` declares a `firm()` door beside `scoped()`
and [`routes/catalog.ts`](../../apps/api/src/routes/catalog.ts) already reads the
catalogue through it, so for THIS port the plan is a second door rather than a
refusal. **It is not listed as a cause because it is not one.**

### 2. Why the rejection is not cleared, in one line

The forbidden fix was available: delete three clauses that are false and the
port looks unblocked. **It would answer every trader `eligible: false` off an
empty table**, with a `max_payout_cents` of `0` computed from nothing, and
nothing in the tree would refuse it. That is [ADR-190](../decisions/ADR-190.md)'s
finding stated for a money read: **a wrong answer is worse than the honest
refusal it replaces.**

### 3. `ADR-215` is not taken

Reserved **CONDITIONAL** for a ruling `ADR-207` and `ADR-204` do not already
make. There is none. `INV-M5-02` binds the two endpoints and is unchanged;
`ADR-207` ruled the storage and section 5's open question is untouched here;
`B5` term 2 is a corpus amendment somebody else holds. **Everything that landed
is transcription of a measurement, and a measurement needs no number.**

### 4. Watched failing: seven seeds, six red and one green

Applied to the committed shape, each restored from a byte copy with SHA-256
checked both ways and never with `git checkout`; `git status --porcelain` empty
after every one.

| | Seed | Result |
|---|---|---|
| 1 | the stale existence claim returns to the blocker | **RED**, the schema-delta case |
| 2 | the blocker stops naming the single insert site | **RED**, clause 1's insert case |
| 3 | a second `RuleStateWriterIo` value ships under `src/` | **RED**, clause 2 |
| 4 | the comment stripper is blinded | **RED**, clause 1's caller case, on comments in `replay.ts` and `statistics.ts` |
| 5 | `engine_gates` stops being `jsonb` in the transcription | **RED**, clause 3 |
| 6 | the blocker is renumbered `(1)(2)(3)` to `(a)(b)(c)` | **GREEN, and that is correct.** The cases pin the CLAIMS and not the presentation, which is the blindness measured rather than described |
| 7 | the blocker drops its clearing condition | **RED**, clause 3's second assertion |

**Seed 4 is the one worth keeping.** The sweep is over code and not over prose
because these modules quote what they refuse, and seed 4 is that decision
watched being load-bearing rather than asserted.

### 5. Found and not in the dispatch

- **THE SWEEP CAUGHT ITSELF, ON ITS FIRST RUN.** `ELIGIBILITY_BLOCKER` names
  `runNightlyBatch` inside a **string literal**, comment stripping does not
  reach a string, and clause 1's caller case counted this module as a caller of
  the batch. The exclusion is in the file with the finding written beside it:
  it is `RI-15` and `RI-16`'s exact blindness, met from the inside by the file
  that blindness was found on.
- **THE TWELVE-LINE HEADER IS A CONTROL.** The first draft ran to sixteen lines,
  moved `ELIGIBILITY_BLOCKER` from `851` to `855`, and turned `RI-15` and
  `RI-16` **red on three citations**: two in [`wiring.test.ts`](../../apps/api/test/wiring.test.ts)
  (session **404**'s live file, which the fence forbids editing) and one in
  [`ALLOCATION`](../decisions/ALLOCATION.md). **Rewriting to the same line count
  disturbed neither**, so nothing was reported stale and nothing was edited
  across a fence. Baseline and final both **18 of 18**, derived from the
  runner's own last line.
- **[`schema.ts:835`](../../packages/db/src/schema.ts) IS HALF STALE.** It reads
  *"the engine's own type was not persistable in this schema, which is why
  `readEligibility` rejects in production today"*. It still rejects; **that is
  no longer why.** `packages/db/**` is session 406's fence. **Reported, not
  repaired.**
- **`docs/STATE.md:4646` carries the same spent sentence** as the session-395
  era record of the finding. It is an append-only historical entry rather than a
  live claim, so it is named here and left alone.

### Next

- **`B5` term 2, the declared stored encoding of `engine_gates`.** It is now the
  single thing blocking BOTH directions: the writer cannot fill column
  `engine_gates` and a reader cannot decode it. It is a corpus amendment.
- **An adapter implementing `BatchPorts` over Postgres, and something that
  schedules the batch.** Without both, `rule_states` stays empty and this
  endpoint stays 503 whatever else lands.
- **The state-hash ruling**, session 400 section 5. Still free, still empty.

### Blockers

- **`readEligibility` still rejects, and after this session it rejects for
  reasons that are true.** That is the intended end state.
- **`usePayoutBackend` is not wired**, `{ declared: 23, wired: 6, blocked: 17 }`
  unmoved, and [ADR-213](../decisions/ADR-213.md) section 8's four grounds all
  stand.

### Landmines

- **The blocker is a claim inside a string literal and no runner can read it.**
  What keeps it true is `account-reads.test.ts`'s five cases and nothing else.
  Anyone editing that string must edit them together.
- **Clause 1's caller case excludes this module by name.** A second module that
  legitimately named `runNightlyBatch` would need adding to that list, and a
  reader who adds it without asking why has removed the case's whole subject.
- **The header's line count is load-bearing** for two citations in another
  package's file. Adding a line to it moves `ELIGIBILITY_BLOCKER` and turns
  `RI-15` and `RI-16` red until every citation is repointed.

### Files touched

`apps/api/src/routes/account-reads.ts`, `apps/api/test/account-reads.test.ts`,
`docs/decisions/ALLOCATION.md` (row `215`), `docs/STATE.md`,
`docs/sessions/README.md` (generated span), `docs/INDEX.md` (generated span),
and this file. **No migration, no `ADR`, no `SD-nn`, no `RI-nn`, no
`SystemReason` or `SqlExecutorReason` member, no `pg` import, no cast past a key
type, no ledger account named, no gate weakened, no fence widened,
`packages/**` and `apps/worker/**` byte for byte unchanged, and
`apps/api/test/wiring.test.ts` untouched.**
