---
status: approved
depends_on: [README.md]
last_updated: 2026-08-20
---

## 2026-08-20 - Session 80: M12's sign-off table and INDEX's plan rows each key once, repaired per row, and ADR-061 would have escalated three of the five keys (non-money)

- **Done:** **Two registries repaired per row rather than per block, and five keys leave `CI06U_REGISTER`.** [M12](../plans/M12-statistic-definitions.md)'s founder sign-off table had `S-14` and `S-15` each heading two rows, one **ruled** and one **unsigned** in each pair; the ruled rows keep their identifiers, because a row with a signature against it cannot move, and the unsigned pair took `S-17` and `S-18`. [INDEX](../INDEX.md) rowed M03, M04 and M05 twice, and **the richer row was the FIRST for M03 and M04 and the SECOND for M05**, so a block delete in either direction would have lost content. `CI06U_REGISTER` loses `docs/INDEX.md` and `docs/plans/M12-statistic-definitions.md` entirely, going **106 keys across 8 files to 101 across 6**. `node scripts/corpus/gates.mjs check`: **22 of 22**, `CI-06a` and `CI-06n` among them. `node scripts/corpus/falsify.mjs`: **22 gates pass clean and fail dirty, 30 scope cases hold**. `pnpm vitest run`: **939 pass, 42 skipped, 62 files**.
- **Next:** **The register's remaining 101 keys are the same six files WAVE-03 sequenced**, and the two largest, [ALLOCATION](../decisions/ALLOCATION.md)'s 18 and the [sessions README](README.md)'s 59, are [ADR-061](../decisions/ADR-061.md) section 4's T3 instances and are not repairs at all until a rule about session numbering is settled.
- **Blockers:** **[ADR-061](../decisions/ADR-061.md) is `status: proposed` and unsigned.** This session's authority to repair came from its brief, not from that entry, and the two disagree on three of the five keys. See the landmine below; it is the finding of this session and it is not a blocker on the work, which is done.
- **Landmines:**

  - **ADR-061 AS DRAFTED WOULD HAVE ESCALATED THREE OF THE FIVE KEYS THIS SESSION REPAIRED, AND THE BRIEF SAID NEITHER REPAIR NEEDED A RULING.** Its section 5 routes the register by name. **`m05-payout-system.md` is listed under "Escalates on a recorded T1"**, in its own words *"whose two INDEX rows give M05 two different purposes"*. **`M12 2`**, which is `s-14` and `s-15`, sits under *"Not yet read by anybody ... Each defaults to escalation until a session states agreement"*, and so do INDEX's `m03` and `m04`. **Under that entry, signed, four of these five keys are a founder's signature and only the agreement test could release them.** The work stands as briefed and the disagreement is recorded rather than resolved by a session.

  - **THE M12 REPAIR IS OUTSIDE ADR-061'S MACHINERY ENTIRELY, AND THIS IS THE PART WORTH CARRYING FORWARD.** That entry's whole framework decides **which row survives a DELETE**: its agreement test asks whether a session can state that both rows say the same thing, because if it cannot, deleting one loses content. **The M12 repair deletes nothing.** Both rows survive with their content intact and one pair takes free identifiers. **A renumber has no content to lose, so there is no question for the agreement test to answer**, and a rule written only about deletes reads as if it covers renumbers because both are "repairing a duplicate key". Two rows stating entirely different things is the case where a renumber is *obviously* right and a delete is *obviously* a ruling, and ADR-061 routes them identically.

  - **M05's SECOND "PURPOSE" IS A CORRUPTION RATHER THAN A PURPOSE, WHICH IS A READING AND IS RECORDED AS ONE.** [ADR-061](../decisions/ADR-061.md) calls the pair T1 on the strength of the two strings. The deleted row read *"bounded freeze, reset"*; the kept row reads *"the 48 hour enforcement window (pre-approval hold and bounded freeze), reserve"*. **INDEX itself assigns resets to M03**, in the M03 row two lines above, and [M05](../plans/M05-payout-system.md) states the reserve 15 times and the 48 hour window 13. So `reset` is `reserve` degraded by one merge and the hold was dropped with it. **That dissolves the T1 and it dissolves it by reading, which is exactly the standing ADR-061 refuses to give a session.**

  - **THE GATE HEADER'S OWN COUNT DISAGREES WITH THE REGISTER IT DESCRIBES, AND IT DID BEFORE THIS SESSION.** [`gates.mjs`](../../scripts/corpus/gates.mjs)'s `CI-06u` header says *"105 of those survive the exemptions, in 8 files"*; the runner printed **106 across 8** on a clean `main`, and [session 76](2026-08-20-session-76.md)'s README row, [WAVE-03](../plans/WAVE-03-duplicate-registry-keys.md) and [ADR-061](../decisions/ADR-061.md) all use 106. **A hand-maintained count beside a generated one, which is [ADR-034](../decisions/ADR-034.md)'s class**, inside the gate written to catch registries disagreeing with themselves. **Not repaired here**: the fence is this session's own register entries, and the header is prose about a survey rather than a claim about the current tree.

  - **THE SIGN-OFF TABLE NOW READS 14, 15, 17, 18, 16 AND THE ORDER WAS LEFT ALONE DELIBERATELY.** Moving `S-16` would edit a row this session was not asked to touch, and the existing grouping is semantic rather than numeric: the two **ruled** rows, then the two **unsigned** rows, then the row **approved at FREEZE** which carries `(APPROVED at FREEZE)` in its own key cell and is followed by the prose about it. **A founder who wants numeric order should say so**; it is one row moved and no content changes.

  - **`sixteen-row` IN [M12-transparency-platform](../plans/M12-transparency-platform.md) IS NOW WRONG UNDER BOTH READINGS.** Line 460 describes the sign-off table as *"a sixteen-row founder sign-off"*. **The table has held 18 rows all along**, so the phrase was already false as a row count and was true only as a count of distinct identifiers. The repair makes the identifiers 18 as well. **Outside this fence and flagged rather than fixed**, and it is [session 36](2026-08-16-session-36.md)'s "five stale fourteens against a sixteen-row table" recurring in the document that describes the table rather than in the table.

- **Files touched:** [`docs/plans/M12-statistic-definitions.md`](../plans/M12-statistic-definitions.md) (two keys), [`docs/INDEX.md`](../INDEX.md) (three rows deleted), [`scripts/corpus/gates.mjs`](../../scripts/corpus/gates.mjs) (two `CI06U_REGISTER` entries removed), [sessions/README](README.md), this file. **No other file under `docs/` or `packages/` was edited, and `INDEX.md` was not regenerated.**
