---
status: approved
depends_on: [README.md]
last_updated: 2026-09-08
---

## 2026-09-08 - Session 655: the line the guard needed was already blank, so the expression-shaped variant the price named is not built, and the 172 is the whole citation set rather than the part below the insertion

<!--index: **THE ROW IS [ADR-460](../decisions/ADR-460.md), `proposed` and UNSIGNED. MONEY PATH, `E2` READ OWED.** [ADR-458](../decisions/ADR-458.md) section 11 item 1 taken and **CLOSED, BUT NOT WITH THE SHAPE IT PRICED**. That item priced an expression-shaped variant of `refuseTermInValues` substituted into `.values(...)` at `:2282` *"because the void spelling cannot be placed there without moving 172 cited lines"*. **BOTH HALVES ARE FALSE ON THIS BASE AND BOTH WERE RE-DERIVED RATHER THAN TAKEN.** The citation set reproduces exactly, **173 distinct numbers, 172 in range against 4718 lines, one synthetic `99999`**, but 172 is the size of the WHOLE in-range set: **80** cited lines sit below `:2194` and **79** at or below `:2282`, so the obstacle was overstated more than twofold. **AND THE OBSTACLE IS NOT THERE AT ALL**: `insertUnderStatement`'s body holds **six blank lines**, `:2211`, `:2220`, `:2243`, `:2252`, `:2266` and `:2281`, **none of them cited**, so the ordinary void call places with **zero** lines added. [`scoped-db.ts:2211`](../../packages/db/src/scoped-db.ts) was blank and now reads `refuseTermInValues(key, values);`. **THE PLACEMENT IS THE FILE'S OWN IDIOM AND NOT A CONTORTION**: the result is character-for-character the shape [`scopedInsertStatement:524-525`](../../packages/db/src/scoped-db.ts) already carries, and it matches `pairInsertStatement:2410`'s ordering. **IT ALSO REFUSES EARLIER THAN THE PRICED SHAPE COULD**, above every line that touches `values` and above the parent-proof `SELECT`, where `:2282` sits below it. **WHY IT WAS MISSED IS NAMED RATHER THAN SCORED**: the constraint genuinely binds at `:1221`, inside `updateStatementOn`'s fourteen-line body with two cited lines in it and no blank to spend, and it was carried one builder along to a hundred-line body without re-derivation. **LINE-NEUTRAL AND THE PROOF IS THE WHOLE DIFF**: 4718 lines both sides, one changed line, **all 172 in-range cited lines byte-identical, 0 moved and 0 changed**. **THE CONTROL WAS WATCHED FIRING ON THIS ROW'S OWN CHANGE**: guard added with the pin stale turned `write-guard-set.mjs` RED on **leg C and leg D together**, and leg D's finding is [ADR-458](../decisions/ADR-458.md) section 7's sixth case demonstrated on the real tree rather than seeded. Pin updated in the same commit, declared cells **11 to 10** and `OPEN` cells **5 to 4**; **the guard roster stays at 9 and the write-guard count at 3**, which is the evidence the void spelling was right: a new `refuse[A-Z]` name would have made it 4 and owed leg D five fresh reasons all saying *"the other spelling of the same rule"*. **NO NEW FUNCTION, NO NEW NAME, `refuseTermInValues` KEEPS ITS `void` RETURN AND ITS FOUR CALL SITES.** One suite case was invalidated by this row and **rewritten rather than skipped or deleted**, seeding a `SETTLED` cell instead; one added, pinning the closure from the side leg C does not reach, watched red with the guard removed and restored byte-identical. **NO LIVE DEFECT AND NONE CLAIMED**, re-derived at source: one producer, [`auth-backend.ts:1092`](../../apps/api/src/auth-backend.ts) on `sessions`, passing a string, a `Buffer`, a `Date` and an `AuthFactor`, none of them a term. **[ADR-448](../decisions/ADR-448.md) SECTION 8 ITEM 3 NOT REOPENED**, all four generated-column `OPEN` cells stand. **NO `eslint.config.js`, NO `packages/eslint-plugin-merit/`, both PR #743's, and nothing here needed either.**-->

### 1. The objective

Take [ADR-458](../decisions/ADR-458.md) section 11 item 1: `insertUnderStatement` takes a caller-supplied values object and refuses no term in it, alone among the builders that take one. The dispatch set the subject at hand-out time, named the placement as *"the difficulty, and it is the whole row"*, required the 172 cited lines to be re-derived rather than trusted, required the pin in [`write-guard-set.mjs`](../../packages/tooling/checks/write-guard-set.mjs) to move in the same commit as the guard, and required the check to be **watched red** with the guard added and the pin stale before it was made green.

### 2. What the container arrived with

**NO REPOSITORY.** No checkout, no working tree, no `origin`, no `node_modules`. The **seventh consecutive row** to find it, after [ADR-449](../decisions/ADR-449.md), [ADR-456](../decisions/ADR-456.md), [ADR-457](../decisions/ADR-457.md), [ADR-458](../decisions/ADR-458.md) and [ADR-459](../decisions/ADR-459.md). It was located through the session's own repository listing, attached for push and cloned. **The shallow-clone refspec problem the dispatch anticipated did NOT occur here**, which is where this row rejoins [ADR-457](../decisions/ADR-457.md) and [ADR-458](../decisions/ADR-458.md) and parts from [ADR-459](../decisions/ADR-459.md), which hit both faults in sequence: `git fetch --depth=200 origin claude/wave-462s` resolved `e2924079` first time with no refspec edit. Recorded because the two faults have different fixes.

### 3. What was derived, before the first edit

Base `e2924079`: suite **346 files / 8658 passed / 11 skipped / 8669 total / 0 failed**, invariants **35 of 35**, gates **33 of 33**, `write-guard-set.mjs` **PASS** at 11 declared cells and 5 `OPEN`. The citation set over `docs packages apps scripts`: **173** distinct numbers, **172** in range against a 4718-line file, one synthetic `99999`. Below `:2194`: **80**. At or below `:2282`: **79**. Blank lines inside `insertUnderStatement` (`:2181` to `:2283`): **six**, and **zero** of them cited.

### 4. The verdict, which is the hole closed by a cheaper shape than the one priced

**Built** ([ADR-460](../decisions/ADR-460.md) sections 2 and 3): the void call at `:2211`, no line added, no function declared, no name minted. **Refused**: the expression-shaped variant, because the constraint that forced `refuseGeneratedColumn`'s shape at `:1221` does not hold in this builder, and building it anyway would have put a fourth column in the guard matrix for a rule the tree already had. **Chosen among six blanks for a reason**: `:2211` is the earliest at which the guard can run, above every line that reads `values` and above the parent-proof `SELECT`, so the refusal happens before a read inside the caller's transaction. That is a behavioural improvement over the priced `:2282` rather than a match for it.

### 5. Verification

The check was run RED before it was run green, on the real tree and on this row's own edit: guard placed and pin stale gives **two findings, leg C naming the changed builder and leg D naming the `OPEN` cell as a stale declaration**, the latter being [ADR-458](../decisions/ADR-458.md) section 7's sixth case arriving as an event. Pin updated and stale reason deleted in the same commit gives **PASS** at 10 declared cells and 4 `OPEN`. The new suite case was watched red by removing the guard from the tree (**7 of 15** cases red) and the file restored byte-identical against `git hash-object`. After the last edit: gates **33 of 33**, invariants **35 of 35**, `write-guard-set.mjs` **PASS**, suite **346 files / 8659 passed / 11 skipped / 8670 total / 0 failed**, a delta of **+1 passed and +1 total** and nothing else, which is the one added case entire. Typecheck exit 0, lint exit 0, `format:check` clean with no reflow. `gates.mjs generate` left the tree carrying only this row's files. **`pnpm run verify` and `scripts/corpus/falsify.mjs` were NOT run**; both mutate the working tree and both are forbidden to this row.

### 6. Next

[ADR-460](../decisions/ADR-460.md) section 10. Item 1, the four generated-column `OPEN` cells, is [ADR-448](../decisions/ADR-448.md) section 8 item 3 and is founder-owed rather than unowned. Item 2 is [ADR-458](../decisions/ADR-458.md) section 11 item 4's workflow step, still waiting on `corpus.yml`. Item 3 is the `172` standing in two dated records that this row corrects and does not edit, on [ADR-386](../decisions/ADR-386.md) section 6. **The finding worth reading first is item 4**: this row found a priced repair whose forcing constraint had been carried from a builder where it binds to one where it does not, and **nobody has swept the tree for other prices written the same way.** That is a survey rather than a fix, and it is unowned.
