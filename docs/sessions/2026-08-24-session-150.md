---
status: approved
depends_on: [README.md]
last_updated: 2026-08-24
---

## 2026-08-24 - Session 150: two branches merged, three predicted conflicts resolved, and ADR-084 signed on the clause its own session could not sign

- **Objective:** land [session 149](2026-08-23-session-149.md) and [session 145](2026-08-23-session-145.md), which were opened against the same commit and touched the same three registries, then resolve the `proposed` ADR standing on the result.
- **Done:** **`#190` merged, `#191` brought current and merged**, the three conflicts resolved by identity rather than by side, and **[ADR-084](../decisions/ADR-084.md) signed** on delegated authority after all three parts of its clause were executed. 28 of 28 gates, 7 of 7 invariants, `falsify:ci` **12 of 12 at `CI-01`**.
- **Next:** session **146** (`P3-c`) is unblocked. `147` and `148` are money path and `148` carries migration `0047`.
- **Blockers:** none.

### The conflicts were predicted before either branch was written, and that is the only reason they were cheap

Both pull requests were based on `338a4f4` and both touched [`decisions/README`](../decisions/README.md), [`STATE`](../STATE.md) and [`sessions/README`](README.md). **The collision was named in the dispatch, with its resolution written down in advance**, so resolving it was mechanical:

| File | Shape | Resolution |
|---|---|---|
| [`STATE`](../STATE.md) | two `##` sections appended at EOF | **both**, 149 first because it merged first |
| [`sessions/README`](README.md) | two entry rows | **both** |
| [`decisions/README`](../decisions/README.md) | 149 repaired the status column on seven rows; 145 added the `084` row | **149's text**, with `084` re-inserted after `083` |

**Only the third is a real overlap and it is the one a keep-both would have got wrong**, by restoring seven `proposed` rows that session 149 had just repaired. It was resolved by taking one side and re-adding the other side's single new row, which is the resolution session 126 recorded after a keep-both duplicated a one-line span.

> **`adr_count` was the figure to watch and it did not move by accident.** Session 149 minted no ADR, so its span stayed at 83; session 145 mints `084`. The merged result is **84**, and `gates.mjs generate` agrees with the registry rather than with either branch's copy of the number.

### ADR-084 was signed on execution, and its own session was right not to sign it

Every part of the clause was run on the merge branch rather than inherited from `#191`:

- `pnpm exec vitest run packages/db` green at **8 files / 154 tests**, printing `7 declared tables, 7 scope rules, 0 reachable without one`.
- `pnpm exec eslint apps packages` exit **0**, **zero** `merit/no-raw-db-client` findings.
- `node scripts/ci/falsify-ci.mjs CI-01` **12 of 12**, up from ten. `CI-01/scope-firm-table-refused` found on `ScopedTableKey`; `CI-01/scope-system-db-not-assignable` found on `__brand`.
- `pnpm run verify` green end to end, `gates.mjs check` 28 of 28.

**THE PART THAT DECIDED THE SIGNATURE IS SECTION 7, AND IT IS A FINDING THE SESSION MADE AGAINST ITSELF.** Its first suite passed **all 22 assertions** against a seed scoping `accounts` by `user_id` instead of `identity_id`, because the render test took its expected column **from the rule** — code asserted against itself. A user is a login and an identity is the person, so that seed silently returns a different row set for every trader with more than one login: **the exact BOLA failure [ADR-008](../decisions/ADR-008.md) scoped the wrapper to bound.** Four checks now take their expectation from the migrations, and all four were re-run green here.

> **A session that finds its own validating suite non-discriminating, records it in the entry, and rebuilds it has done the thing the line-by-line read exists to catch.** That is what a delegated signature can rest on. It is not the same as the read.

### What the signature does not buy, stated because it is now five files deep

`ADR-084` is **money path** and the founder's `E2` read has not happened. Three design choices are unreviewed and no assertion reaches them: **`pg` over postgres.js at a measured 14 packages, 13 transitive**, under a `VG-12` regime whose point is that a person admits a dependency; the permanent foreclosure of any HTTP or serverless deployment of any surface; and a `firm` class unreachable from `scopedDb` forever.

- **Landmines:**
  - **FIVE money-path files are now merged on delegated authority without the `E2` read**: `134` and `135` ([P2](../plans/P2-rules-engine-build.md) section 9), [`0046`](../../packages/db/migrations/0046_supersede_rule_states_consistency_period_started.sql) and [`hash.ts`](../../packages/rules-engine/src/hash.ts) ([session 149](2026-08-23-session-149.md)), and now `packages/db/src/**`. **The count is rising and each signature says so in its own approval line.** It is one debt in five places rather than five separate notes.
  - **Two sessions opening against one commit and touching three shared registries is now the normal case**, not the exception: `146`, `147` and `148` inherit the same three forced fence edits. The conflict is cheap only while it is predicted.
- **Files touched:** [ADR-084](../decisions/ADR-084.md), [decisions/README](../decisions/README.md), [STATE](../STATE.md), [sessions/README](README.md), this file. **No code file changed in this session**; the merge carried session 145's code unmodified.
