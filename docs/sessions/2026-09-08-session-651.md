---
status: approved
depends_on: [README.md]
last_updated: 2026-09-08
---

## 2026-09-08 - Session 651: two accessor docblocks argued a distinction the database does not make, measured on a PostgreSQL this row started, and both rewritten line-neutrally

<!--index: **THE ROW IS [ADR-456](../decisions/ADR-456.md), `proposed` and UNSIGNED. MONEY PATH, `E2` READ OWED.** [ADR-454](../decisions/ADR-454.md) section 11 item 1 taken and DISCHARGED. Both `databaseWrites` docblocks argued that `generated` and `generatedIdentity` are told apart by their SQLSTATE, `42601` against `428C9`. **Measured on a PostgreSQL 16.13 this row started: `428C9` on ALL FOUR HALVES**, stored-generated and identity, INSERT and UPDATE, all four raised by `rewriteTargetListIU`; `42601` is `syntax_error` and only a parse failure returns it. Rows 452 and 454 reproduce exactly on a third cluster. **The distinction is real and is NOT the SQLSTATE**: it is the `DETAIL` line and `OVERRIDING SYSTEM VALUE`, which lifts the identity refusal and leaves the generated one standing, and that is what both blocks now carry. **All three registry figures re-derived on the pinned `drizzle-orm@0.45.2` and all three HOLD**: 1367 columns over 116 keys, 5 stored-generated over 4 tables, 20 identity columns every one `id` and every one `always`. **Line-neutral, 8/8 numstat per file, 775 and 1010 lines both sides, 11 of 13 pointers byte-identical** and the two that moved are exactly the two `ADR-454` nominated as owed. **Left uncorrected with reasons:** both files now MISCOUNT THEIR OWN CITATIONS, and `ADR-454` mispriced this row as two bare substitutions. -->

### 1. The objective

Take [ADR-454](../decisions/ADR-454.md) section 11 item 1, the two lines row 454 was fenced out of and PR #738 has since unfenced. [`keyed-accessor.test.ts:764`](../../packages/db/test/keyed-accessor.test.ts) and [`write-accessor.test.ts:999`](../../packages/db/test/write-accessor.test.ts) each sit in a docblock whose central argument is that `generated` and `generatedIdentity` are distinguished by the SQLSTATE PostgreSQL answers them with. **Measure that against a database this row starts itself rather than against [ADR-452](../decisions/ADR-452.md) or [ADR-454](../decisions/ADR-454.md), re-derive the registry figures both blocks state rather than inheriting them, and repair line-neutrally.**

### 2. Done

- **The container measured and reported. THERE WAS NO CHECKOUT AT ALL**, no working tree and no `origin` to fetch from, which is the third consecutive row to find this. The repository was located through the session's own repository listing, cloned, authorised for push, and `f3c315ea` resolved in it. **The base is `origin/claude/wave-454s` at `f3c315ea`, not `main`.**
- **A PostgreSQL 16.13 started in this container** and a table built carrying both column kinds: an `ALWAYS` identity `id` of the shape all 20 identity columns have, and a `STORED` generated column of the shape `reconciliations.delta_cents` has.
- **Ten questions asked with `\set VERBOSITY verbose`**, so the code read is the server's. **`428C9` on all four halves and on both mixed cases; `42601` only from a deliberate parse failure asked as a control.** All four refusals are raised by the same function, `rewriteTargetListIU`, at four adjacent sites in `rewriteHandler.c`, which is the mechanical reason they cannot carry different codes.
- **Rows 452 and 454 reproduced exactly and independently**, on a third cluster in a third container, without either row's table being read first. **There is no disagreement to report**, and the dispatch's clause for one was not reached.
- **The discriminator confirmed and it is what the corrected prose needed.** `OVERRIDING SYSTEM VALUE` on the identity half is ACCEPTED; on the stored-generated half it is refused with the same `428C9` and the same message. A `GENERATED BY DEFAULT AS IDENTITY` column is refused by neither statement, which is why the registry's *"every one `always`"* is load-bearing rather than decorative.
- **All three registry figures re-derived on `drizzle-orm@0.45.2` and all three HOLD.** 1367 columns over 116 `TABLE_KEYS`; 5 stored-generated over 4 tables; 20 identity columns, the not-`id` set empty and one distinct type seen; `'isGenerated' in column` false on 0 of 1367. **The five generated columns and the twenty identity tables are NAMED in [ADR-456](../decisions/ADR-456.md) section 3**, so the counts are checkable rather than quotable.
- **[`keyed-accessor.test.ts`](../../packages/db/test/keyed-accessor.test.ts) rewritten at `:762`-`:771`.** **`8/8` numstat, 775 lines both sides, no executable line changed.**
- **[`write-accessor.test.ts`](../../packages/db/test/write-accessor.test.ts) rewritten at `:998`-`:1006`**, in its own words rather than a copy of the sibling's. **`8/8` numstat, 1010 lines both sides, no executable line changed.**
- **The citation sets derived FROM THE BASE REF and every one of the thirteen pointers opened on both sides.** Five into `keyed-accessor.test.ts`, eight into `write-accessor.test.ts`. **Eleven land on byte-identical lines.**
- **`CI-06u` refused the first draft of `ADR-456`'s generated-column table** for keying two rows on `notificationKinds`. **The gate was right**; the table is re-keyed on the qualified column.

### 3. Next

- **The founder ruling on dated records, asked a third time.** [ADR-456](../decisions/ADR-456.md) section 7 item 2. [ADR-454](../decisions/ADR-454.md) section 9 referred it over 35 mentions; this row adds a mispricing inside `ADR-454` itself. **No row may answer it.**
- **Both accessor files now MISCOUNT THEIR OWN CITATIONS.** [`keyed-accessor.test.ts:736`](../../packages/db/test/keyed-accessor.test.ts) says three cited lines and there are five; [`write-accessor.test.ts:972`](../../packages/db/test/write-accessor.test.ts) says six and there are eight. **The remedy is the sentence shape, not a fourth correction of a number**: replace the enumerated count with a pointer to the `git grep` that derives it, which is [ADR-034](../decisions/ADR-034.md)'s second branch.
- **The INSERT builders are still unguarded**, [ADR-448](../decisions/ADR-448.md) section 8 item 3, unmoved and now measured a second time. `Q1` and `Q3` refuse an INSERT naming either kind at the database and `refuseGeneratedColumn` runs on the UPDATE path only.
- **The integration suite, asked for a fourth time.** Six database-backed cases exist and are skipped on an absent `DATABASE_URL`. **Three consecutive sessions have now started a PostgreSQL by hand in under a minute** to answer a question CI could answer on every commit.

### 4. Blockers

- **The dated-record question blocks the two findings in [ADR-456](../decisions/ADR-456.md) section 7 that live in merged records** and no row may answer it.
- **Nothing else blocked.** Every repair inside this row's subject was achievable line-neutrally, so the dispatch's *"record the site and leave it"* clause was never reached for the subject. It is reached twice, for findings deliberately left rather than for repairs that could not be made.

### 5. Landmines

- **A TOKEN SWAP WOULD HAVE PRODUCED A SENTENCE THAT CONTRADICTS ITSELF, AND `42601` AND `428C9` BEING THE SAME LENGTH IS WHY THAT LOOKS SAFE.** Both blocks were built as *"this one, which PostgreSQL answers X to; that one, which it answers Y to"*. Substituting inside that structure yields *"which it answers `428C9` to; ... which it answers `428C9` to"*, whose grammar still claims a contrast its content denies. **[ADR-454](../decisions/ADR-454.md) priced this row as two bare substitutions for exactly that reason.**
- **THE BUDGET IS NOT THE FILE, IT IS THE GAP AROUND A CITED LINE.** Both rewritten lines are themselves cited, so the block above each is frozen in length and the block below each is frozen in length, INDEPENDENTLY. Prose can move inside those two windows and nowhere across them. **A rewrite that is line-neutral over the file can still be impossible.**
- **A GREEN `RI-15` DOES NOT MEAN A CITER'S SENTENCE IS STILL TRUE.** Both files are `.ts` and both invariants read them, and both were green on every row while the false argument sat there. **They check that a pointer LANDS, not that a claim is TRUE**, which is [ADR-451](../decisions/ADR-451.md) section 9's point from the other direction. The per-pointer table in [ADR-456](../decisions/ADR-456.md) section 6 is the second check and it was done by opening every one.
- **A CLAIM ABOUT DATABASE BEHAVIOUR WRITTEN INTO A COMMENT SHOULD NAME THE ROW THAT MEASURED IT.** The `42601` claim was refuted by [ADR-452](../decisions/ADR-452.md) and these two blocks kept asserting it through rows 452, 453, 454 and 455, because nothing in the comment said what it rested on. **Both replacement blocks now name `ADR-456`.** That is the shape applied once, not proposed as a rule.
- **MEASURING THE BASE AFTER THE EDIT COSTS THE ABILITY TO SAY A RED LEG WAS PRE-EXISTING.** This row corrected and pushed before it asked what `f3c315ea` did, then checked the base out detached and measured it. **Every leg was green on both sides, so it cost nothing here and would have cost a row's whole finding had one been red.**

### 6. Files touched

- [`packages/db/test/keyed-accessor.test.ts`](../../packages/db/test/keyed-accessor.test.ts): `:762`-`:771` rewritten, eight lines in and eight out at the file's own 82-column comment wrap. **775 lines both sides. `:196`, `:442`, `:663` and `:737` are cited and all four are byte-identical; `:764` is cited and is the line [ADR-454](../decisions/ADR-454.md) nominated as owed.**
- [`packages/db/test/write-accessor.test.ts`](../../packages/db/test/write-accessor.test.ts): `:998`-`:1006` rewritten, eight lines in and eight out. **1010 lines both sides. `:10`, `:111`, `:180`, `:184`, `:311`, `:479` and `:887` are cited and all seven are byte-identical; `:999` is cited and is the second line `ADR-454` nominated as owed.** `:111` is the pointer held by [`generated-column-writes.mjs:505`](../../packages/tooling/checks/generated-column-writes.mjs), a live file outside this fence, and it did not move.
- [`docs/decisions/ADR-456.md`](../decisions/ADR-456.md), [ALLOCATION](../decisions/ALLOCATION.md) row `456` result cell only, the decisions registry, this log and its registry row, and [STATE](../STATE.md) for generated output only. **[INDEX](../INDEX.md) needs no row**: it points at the decisions directory rather than at each entry, so a new ADR does not change it.
- **NOT TOUCHED, AND NOTHING IN THEM WAS WANTED:** `scoped-db.ts`, `schema.ts`, `scoped-db.test.ts`, the `generated-column-writes` pair, `scripts/`, `corpus.yml` and `DELTA_MANIFEST.md`. The dispatch's *"write down what you would have changed"* clause produces an empty list.

### 7. Verification

**`pnpm run verify` WAS NOT RUN AND `scripts/corpus/falsify.mjs` WAS NOT RUN.** Both mutate the working tree. Every count below was derived at the moment it was written.

| Leg | Base `f3c315ea` | Head |
| --- | --- | --- |
| `gates.mjs generate` then `check` | **33 of 33**, generator wrote nothing | **33 of 33**, generator wrote the ADR count and the two registry rows and no prose |
| `repo-invariants.mjs` | **35 of 35** | **35 of 35** |
| `pnpm vitest run` | **344 files, 8619 passed, 11 skipped, 0 failed** | **344 files, 8619 passed, 11 skipped, 0 failed** |
| `pnpm run typecheck` | clean | clean |
| `pnpm run lint` | clean | clean |
| `pnpm run format:check` | clean | clean |

**THE SUITE COUNTS ARE IDENTICAL ON BOTH SIDES AND THAT IS THE EXPECTED RESULT**, stated rather than assumed: this row changed comment text only, so a moved count would have meant something other than this diff caused it.
