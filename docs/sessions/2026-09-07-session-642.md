---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 642: the live cache is transcribed, compared by name because the registry loops stop at the registry, and left unregistered because merit_app holds nothing on it

<!--index: **THE ROW IS [ADR-447](../decisions/ADR-447.md), `proposed` and UNSIGNED. MONEY PATH, `E2` READ OWED.** `live_account_state` was declared in no TypeScript file; it is now in [`schema.ts`](../../packages/db/src/schema.ts) with **three** `*_cents` columns, one of them `GENERATED ALWAYS AS (equity_cents - opening_equity_cents) STORED`. **WHICH MIGRATIONS BUILD IT WAS DERIVED**: `0050` is the only file naming it, so the `CREATE` body IS the column set, and a case re-derives that on every run. **THE DISPATCH'S PREMISE IS FALSE IN TWO PLACES**: `ADR-441` through `ADR-446` do not exist on `origin/main`, `grep -c GENERATED_ALWAYS_AS` over the test file returns **0**, so **three** comparisons existed at this base and not four; and the table carries **three** `*_cents` columns in total, of which the third IS the generated one. **`ADR-443`'s PREDICTION IS UNVERIFIABLE FROM THIS BASE AND FALSE ABOUT IT**: the document is absent, the constant does not exist, and base line `3825` is inside `` `SystemReason` is still exactly two members ``. Corrected in this row's own entry; no landed ADR edited. **NOT REGISTERED IN `scope.ts`, AND THE REASON IS A GRANT**: `0050` REVOKEs ALL from `merit_app` and every accessor door opens on that role, so a key would be a permission claim `packages/db` cannot honour. **FIVE COMPARISONS WRITTEN BY NAME** and six of seven cases watched RED under a seeded violation, each reverted byte-identically by `git hash-object`. **BOTH EDITED FILES ARE PURE APPENDS**, `107 0` and `194 0`, prefixes byte-identical through 5320 and 3984, so **0 of 62 and 0 of 20 cited lines moved**. **NO WRITE PATH ADDED, NO MIGRATION TOUCHED, NO GATE WEAKENED, NO TEST SKIPPED.** Counts derived at reporting time: invariants **35 of 35**, gates **32 of 33** with `CI-06f` the only failure and its six holes exactly `ADR-441` to `ADR-446`, reported rather than repaired because this row owns one cell, suite base **342 files / 8,429 passed / 11 skipped / 0 failed** and branch **342 / 8,436 / 11 / 0**, delta **+7**, **all seven from the named block and none from a registry loop**. -->

### 1. The objective

Transcribe `live_account_state` into [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts) from the DDL as it stands after every migration, deriving which migrations build it; bring it under the mechanical comparisons; verify `ADR-443` section 11 item 1's prediction at source and correct it in this row's own entry if it is wrong.

### 2. Done

- **The container measured and reported.** No repository on arrival, twenty-third consecutive row; `add_repo` with `access: "push"` first, then one clone. `node_modules` absent as well and installed from the lockfile.
- **The base reproduced at `89b02405`** on a clean tree before the first edit and **it is green**: gates **33 of 33**, invariants **35 of 35**, suite **342 files / 8,429 passed / 11 skipped / 0 failed**, typecheck, lint and `format:check` clean. Neither `pnpm run verify` nor `falsify.mjs` was run.
- **The migration set derived rather than assumed.** One case-insensitive grep over `packages/db/migrations`: `0050_live_cache_and_role.sql` is the only file naming the table, so the fold replays nothing onto it and the `CREATE` body is the column set as of the last migration.
- **Ten columns transcribed and compared mechanically**, not read: type, nullability, default and the generation clause, every one against `0050`.
- **The dispatch's arithmetic corrected.** Three `*_cents` columns in total, the third of which is the generated one; and three comparisons existed at this base, not four.
- **`ADR-443`'s prediction checked at source and found unverifiable from this base**, on three derived facts. Corrected in [ADR-447](../decisions/ADR-447.md) section 6 rather than by editing a file that is not in the tree.
- **The registration REFUSED, with the reason at primary source.** `0050`'s revoke, its header items 3 and 4, and the single-pool argument in `client.ts`. Recorded as an assertion rather than as a comment.
- **Six of seven cases watched RED** under a seeded violation and each seed reverted byte-identically, `git hash-object` on both sides. The seventh is named as owed, with why it could not be seeded cheaply.
- **Blast radius derived and reported as zero.** Both files pure appends; prefixes byte-identical; no cited line moved in either.

- **A MID-ROW FENCE CORRECTION ARRIVED AND WAS ALREADY SATISFIED.** It added `docs/STATE.md` for `generate` output only; the `generate` output was already committed. **Its own figure is wrong about this base**: it says `STATE.md:32` carries `426`, and `git show origin/main:docs/STATE.md` gives **423** with **424** on this branch. **Exactly one span moved anywhere in the diff**, `adr_count`; `sql_tables` 118, `sql_triggers` 32, `migration_files` 75 and `manifest_changes` 118 are unchanged and [INDEX](../INDEX.md) is byte-identical, which is the confirmation the correction asked for.

- **THE POSTURE CHANGE NAMED PLAINLY** ([ADR-447](../decisions/ADR-447.md) section 8.1), on a second dispatcher correction. **Exactly one surface moved and it is the schema barrel**, not the typed accessor: `scopedDb`, `firmDb` and `systemDb` all take `TableKey` and this relation has no key, so naming it at a door is a compile error exactly as before. **No write becomes possible**, on three independent refusals: `client()` is deliberately unexported and the package declares one entry point, no accessor admits the key, and `merit_app` holds no privilege at all. **The generated column is NOT a key of `$inferInsert` and IS a key of `$inferSelect`**, both compiled as type assertions and **both watched failing**; the probe was deleted rather than landed, because row 445 owns that control. `ADR-437` section 13 stays unanswered and this row does not answer it.

### 3. Next

- The general `GENERATED ALWAYS AS` comparison over the whole registry, at which point the narrow one this row wrote is DELETED rather than kept ([ADR-447](../decisions/ADR-447.md) section 9 item 3).
- The `schema.ts` header's totality clause and its stale denominator ([ADR-447](../decisions/ADR-447.md) section 9 items 1 and 2), either line-neutrally or with the 62-citation move taken deliberately.
- A process that connects as `merit_live`, without which this transcription is a type nothing reads (section 9 item 4).

### 4. Blockers

- **None for this row.** [ADR-447](../decisions/ADR-447.md) is `proposed` and UNSIGNED and the `E2` read is owed; no founder read was available during the session and none was required to finish.

### 5. Landmines

- **A DISPATCH CAN CARRY AN UNMERGED SIBLING'S REASONING AS SETTLED FACT.** This row was sent to verify a section number in a document that does not exist on `origin/main`. Check the base before trusting a sibling's citation; this is the second premise in three rows to fail that way.
- **`git checkout -- <file>` ON AN UNCOMMITTED FILE DESTROYS THE WORK.** It happened once here, on the transcription, mid red-watch. The transcription was rewritten and committed BEFORE any further seeding, and every later revert was `git checkout HEAD -- <file>` against a committed baseline with a `git hash-object` check on both sides.
- **REGISTERING A KEY IN `TABLES` WITHOUT ITS `SQL_NAME` ENTRY BREAKS THE SUITE AT COLLECTION**, not at the case you expect, because `SQL_NAME` is total by COMPILE error and a runtime key with no entry breaks every loop that indexes it.

### 6. Files touched

- [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts), append only, `107 0`.
- [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts), append only, `194 0`.
- [`docs/decisions/ADR-447.md`](../decisions/ADR-447.md), new.
- The registries: [ALLOCATION](../decisions/ALLOCATION.md) row `447` (hand-written), and the generated spans in [decisions/README](../decisions/README.md), [sessions/README](README.md) and [STATE](../STATE.md), rewritten by `gates.mjs generate` rather than by hand.
- **[INDEX](../INDEX.md) NEEDED NO EDIT AND THAT WAS CHECKED RATHER THAN ASSUMED.** Its `decisions/` row at line 48 and its `sessions/` row at line 46 both say entry files are indexed by their own README under `CI-06n` and not by that row, and `gates.mjs generate` rewrote no span in it. An INDEX row for this ADR would be a hand-maintained duplicate of a generated table.
