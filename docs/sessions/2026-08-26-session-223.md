---
status: approved
depends_on: [README.md]
last_updated: 2026-08-26
---

## 2026-08-26 - Session 223: ADR-023 enrichment, observe mode

- **Objective:** the vendor-agnostic checkout enrichment adapter as `packages/enrichment`, its `integration_contracts` row, signals recorded and scored, and **nothing blocked**. [ADR-023](../decisions/ADR-023.md) rules enrichment bought rather than built and observe mode non-blocking, so the ruling is settled and this slice is its transcription. **MONEY PATH by position**: it runs inside checkout's transaction. [ADR-003](../decisions/ADR-003.md) strict. **ADR `115` is reserved CONDITIONALLY.**

---

### Plan

**This section is the plan gate and it was written, committed and pushed before any source file was touched.** [ADR-003](../decisions/ADR-003.md) asks for plan mode on a money path; this is a remote autonomous session and the harness's plan mode blocks on an approval prompt nobody is watching, so the discipline is discharged in writing instead. If the plan moves while the slice is built, this section is amended in place and the move is named.

#### 1. What was read before anything was written

[CLAUDE.md](../../CLAUDE.md), [STATE](../STATE.md), [P3-wave3-modules section 9's `P3-p` row](../plans/P3-wave3-modules.md) and its section 15 body, [ADR-023](../decisions/ADR-023.md) in full, [ADR-112](../decisions/ADR-112.md) (the accessor changed today and this slice writes through it), [M03 section 7.9](../plans/M03-billing-checkout.md) and 7.9.1, [M07 section 3.2](../plans/M07-risk-abuse.md)'s `D-15`, `D-12` and `D-18` rows and section 3.3's severity scale, [`0002_identity.sql`](../../packages/db/migrations/0002_identity.sql)'s `identity_signals`, [`0008_risk.sql`](../../packages/db/migrations/0008_risk.sql)'s `risk_flags`, [`0018_integrations.sql`](../../packages/db/migrations/0018_integrations.sql)'s `integration_contracts` and `integration_dispatches`, [`0029`](../../packages/db/migrations/0029_phone_identity_and_auth.sql)'s kind vocabulary, [`scope.ts`](../../packages/db/src/scope.ts), [`schema.ts`](../../packages/db/src/schema.ts), [`scoped-db.ts`](../../packages/db/src/scoped-db.ts), and both precedents this package is built on: [`packages/psp`](../../packages/psp/src/port.ts) for a vendor-agnostic port with no network, and [`packages/ledger`](../../packages/ledger/src/tx.ts) for a library that writes through a caller's transaction and declares no `@merit/db`.

#### 2. The ordering constraint, handled rather than worked around

**Session 220 is creating [`apps/api/src/routes/checkout.ts`](../../apps/api/src/routes/) concurrently with this session and it does not exist on `main`.** `ls apps/api/src/routes` at session start returns `auth.ts`, `health.ts`, `me.ts`, `public-methods.ts` and `webhooks-psp.ts`, and no `checkout.ts`. So the whole of `packages/enrichment` is built first and the call site is attempted last, after a fresh `git fetch origin main`. **If 220's file has landed by then, this session adds its call and nothing else. If it has not, the file is NOT created and NOT stubbed**, the package ships complete and unwired, the call site is named as owed in the pull request body, and session 220's branch is named as the one that will carry it. A checkout file invented by the wrong session is a collision that costs both branches.

#### 3. The ruling, and what it forecloses

ADR-023's rollout is graduated and step 1 is observe mode: *"Signals recorded, scored, and reported; nothing is blocked. The purpose is to learn the distribution on Merit's own traffic."* Step 3, the soft decline plus review queue, is not this session's. **So there is no code path in this slice where an enrichment outcome changes what checkout returns, and that is asserted rather than promised.** Three structural controls carry it, in preference to a comment:

1. **`observeEnrichment` returns `Promise<void>`.** There is no value for a call site to branch on. This is [`packages/psp`](../../packages/psp/src/port.ts)'s second rule ("nothing in the interface returns a decision") tightened one notch, because the psp port at least reports what the provider said and this one reports nothing at all to its caller.
2. **It never throws.** Every failure direction is caught inside it. A throw inside checkout's transaction is a rollback, so a throwing enrichment call is enforcement by exception, which is the "silent decline" ADR-023 forbids even in the mode that is allowed to decline.
3. **The package contains no decision vocabulary at all**, and a test greps its own source to prove it: no `severity`, no `decline`, no `refuse`, no `block`, no threshold constant and no mapping from a score to a `risk_flags` band. A scorer that silently gains authority is the failure this ruling forecloses, so the absence is a mechanical assertion.

#### 4. Where the signals go, and the one place the corpus made this a decision

`identity_signals.kind` carries `footprint_enrichment` as `U-04`, added for exactly this vendor ([`0002`](../../packages/db/migrations/0002_identity.sql) and [`0029`](../../packages/db/migrations/0029_phone_identity_and_auth.sql)), so the recorded half has a home the corpus already built. **The scored half does not, and inventing one is a migration this fence forbids.** The available shape is `risk_flags`, whose `source` column is reserved `'vendor:<name>'` in its own DDL comment "so a vendor detector plugs in without a migration", and **this session does not write one.** Three reasons, and the first is the corpus's own precedent:

- **`D-12` already ruled this shape for a detector that observes without accusing:** *"Output is a watched-cluster set, not a flag: it seeds D-13 and D-14 rather than accusing anyone."* Observe mode accuses nobody by construction.
- **`risk_flags` is money-adjacent and a severity is not a score.** [ADR-040](../decisions/ADR-040.md)'s `G-HOLD-REQUIRED` holds a payout on an unresolved flag of severity 4 or above, and [M03 section 7.9](../plans/M03-billing-checkout.md)'s open question on whether the same band should stop a purchase is still open. Writing a severity from an untuned score would let this slice acquire authority over money going out while claiming to have none over money coming in.
- **A flag per checkout is a queue nobody works**, and `risk_flags.status` defaults to `open`.

**So the score is computed, carried in the reported outcome, and has no persisted column until a migration gives it one.** That is stated in the pull request body and in [ADR-115](../decisions/ADR-115.md) rather than papered over by writing a band into `value_preview`, whose DDL says it is a display fragment of the value it previews.

#### 5. What the accessor permits, which decides the write shape

[ADR-112](../decisions/ADR-112.md) landed in [#301](https://github.com/lam2-coder/meritfutures/pull/301) today: `rowAt`, `updateAt` and `deleteAt` name ONE row, `scopedDb` ANDs its tenancy predicate onto the address, and `update` and `delete` no longer exist on any handle. Two facts read out of it decide this slice's shape:

- **`identity_signals` has exactly one addressable key and it is `id`.** `uniqueKeys` reads Drizzle's table config for inline and table-level primary keys and unique constraints, and **it does not read a `uniqueIndex`**. `identity_signals_identity_kind_value_uq` is a `CREATE UNIQUE INDEX` in [`0002`](../../packages/db/migrations/0002_identity.sql) and [`schema.ts`](../../packages/db/src/schema.ts) declares no key for it, so `updateAt('identitySignals', { kind, valueHash }, ...)` is refused by `refuseUnaddressed`. **A repeat observation is therefore a `rowsWhere` then an `updateAt` addressed by `id`.**
- **A blind insert would roll back the purchase, and on the most ordinary path there is.** A returning trader buying a second account presents the same enrichment values, the unique index raises `23505`, the error poisons checkout's transaction and Postgres turns its `COMMIT` into a `ROLLBACK`. **That is enrichment blocking a checkout through the database rather than through a decision**, and it is precisely the outcome this ruling forecloses. Reading before writing is what avoids it.

#### 6. Files, in the order they land

| # | File | What it is |
|---|---|---|
| 1 | `packages/enrichment/package.json`, `tsconfig.json` | The manifest. **No runtime dependency, no workspace dependency**, on [`packages/ledger`](../../packages/ledger/package.json)'s precedent: the writer is injected, so this package cannot open a pool even by mistake, and `merit/no-raw-db-client` is satisfiable with no `ignores` line |
| 2 | `src/tx.ts` | `EnrichmentTx`, the subset of [ADR-102](../decisions/ADR-102.md)'s `ScopedTx` this path uses, restated. Two statements of one shape, bound by `test/accessor-bind.test.ts` reading `packages/db/src/scoped-db.ts`, which is `packages/ledger`'s instrument pointed at the same file |
| 3 | `src/port.ts` | `EnrichmentAdapter`. One method, `assess`. **Nothing in the interface returns a decision.** Three-valued facets, because `null` means the vendor did not find out and `false` means it looked and there is none, which is [M07](../plans/M07-risk-abuse.md)'s named mass-false-positive trap |
| 4 | `src/contract.ts` | The `integration_contracts` row as declared data, its installer, and the allowlist. **A facet absent from the allowlist never leaves**, because `INV-M10-02` makes the contract an allowlist and not a denylist. **No enabled row means no call at all** |
| 5 | `src/score.ts` | The scorer. Integer basis points on `identity_links.confidence_bp`'s 0 to 10000 scale, no floats, branded so it cannot be passed where a severity is meant. An unknown facet is excluded from the denominator rather than scored as a zero |
| 6 | `src/observe.ts` | Observe mode. `Promise<void>`, never throws, one timeout, and the three failure directions each leaving the transaction untouched |
| 7 | `src/fakes/` | Three fakes: one that answers, one that hangs past the timeout, one that errors. `packages/psp`'s precedent, and no vendor is named anywhere |
| 8 | `src/index.ts` | The exports, and the list of what is deliberately absent |
| 9 | `test/*` | The three failure directions, the allowlist in both directions, the read-then-write shape, the accessor bind, and the mechanical no-authority grep |
| 10 | [ADR-115](../decisions/ADR-115.md) | The scoring shape and the write shape, which ADR-023 does not settle |
| 11 | The call site, **or the honest debt** | Section 2 |

#### 7. The timeout, and what happens on each side of it

`ENRICHMENT_TIMEOUT_MS` is **800**, integer milliseconds, and it is a constant this package exports rather than a number a call site passes, so an operator reading one file learns the whole latency budget checkout carries for this. When it fires: the pending vendor promise is **abandoned and its eventual rejection swallowed**, so a late failure cannot surface as an unhandled rejection after the request is gone; nothing from the vendor is recorded, because there is nothing to record; the outcome `timed_out` is reported to the injected sink, which is what [RB-11](../ops/runbooks/RB-11-verification-provider-outage.md) step 5 means by watching the enrichment vendor separately; and **`observeEnrichment` resolves normally, so checkout commits.**

#### 8. The approval line, unsigned, one checkable clause

> **An enrichment call that times out, errors or returns a maximal risk score leaves the checkout COMMITTED and reports the outcome, watched in all three directions.**

**Cost if wrong:** observe mode silently becomes enforcement by outage, which is the silent decline ADR-023's step 3 forbids even when enforcement IS enabled.

#### 9. What this session will NOT do, named rather than discovered

**No migration and `0048` stays free.** No `packages/db` edit, so `schema.ts` gains no unique-key declaration even though section 5 shows one missing. No `risk_flags` write and no severity anywhere. No vendor named. No threshold, because thresholds are tuned on beta data and this slice has none. No route registration, no `API_CONTRACT` row, no `SD-nn`, no `CI-06` letter, no gate register row, no fixture, no edge case. **`pnpm-workspace.yaml` is untouched and no dependency is admitted**; `pnpm-lock.yaml` gains this package's own `importers` block and nothing else, which is [`packages/psp`](../../packages/psp/package.json)'s measured precedent at +12 lines and is a mechanical consequence of a workspace package existing rather than a fence widening.

#### 10. Stop condition

The adapter, the `integration_contracts` row, signals recorded and scored, nothing blocked and that asserted in all three directions, the call site wired or honestly owed, **30 of 30 gates, 9 of 9 invariants, `pnpm run verify` exit 0, full vitest green**, the pull request open and NOT merged. The `E2` read is the founder's.
