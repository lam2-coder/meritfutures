---
status: approved
depends_on: [README.md]
last_updated: 2026-09-08
---

## 2026-09-08 - Session 645: the eight pointers are invisible to both citation checks and not to one of them, 89.5% of this tree's pointers bind no name, and the control is refused because it cannot see seven of its own motivating case

<!--index: **THE ROW IS [ADR-450](../decisions/ADR-450.md), `proposed` and UNSIGNED. NO MONEY DIFF, `E2` READ OWED.** **THE POPULATION IS DERIVED, NO CONTROL IS BUILT, AND THE REFUSAL IS THE FINDING.** [ADR-449](../decisions/ADR-449.md) section 9 item 3 is RIGHT about the eight: all off by exactly 460 and every `+460` lands on its subject. **ONE DETAIL CORRECTED**: six land on live code and not seven, because `:4136` lands on a comment; and the eight are TEN SITES. **THE DISPATCH'S PREMISE IS FALSE IN TWO PLACES AND BOTH MAKE THE HOLE BIGGER.** `RI-15` has four legs and not one, and it does not read `ADR-447` at all: its own miss (4) at [`repo-invariants.mjs:3292`](../../packages/tooling/checks/repo-invariants.mjs) says it reads `docs/decisions` *"NOT AT ALL, deliberately"*. **AND `RI-16` DOES NOT SEE THEM EITHER**, which nobody had considered: `documentScope` at [`repo-invariants.mjs:3723`](../../packages/tooling/checks/repo-invariants.mjs) flags EVERY line of a document whose FIRST heading matches `RECORD_HEADING`, and every ADR heading carries its date. **433 of the 435 files under `docs/decisions/` are wholly out of scope**, the exceptions being `ALLOCATION.md` and `README.md`. **So nothing sees any of the eight: eight of eight, not seven, and 433 ADRs behind them.** **THE CENSUS WAS RUN WITH THE TREE'S OWN READER AND CALIBRATED AGAINST THE CHECKS**: restricted to the legs that run it reports 21 sites over exactly **19** distinct keys, matching the **9** and **10** register entries key for key. **10,242 POINTERS**: 419 source, 1,199 live document, **8,624 (84.2%) inside dated records no check reads**. **737 decidable defects and 716 of them invisible to every live check.** **THE NUMBER THE DISPATCH ASKED FOR DOES NOT EXIST**: **9,166 (89.5%) bind NO NAME**, so the question has no machine answer for them, and the eight are nine of those plus one. **Where checking is possible, dated records drift about nine times as often as live source**: 132 of 299, against 7 of 143 in source. **That is not a sample of the 9,166 and is not multiplied by them.** **A CONTROL IS REFUSED ON THREE INDEPENDENT GROUNDS**: it catches **1 of 8** by vacancy and **0 of 8** by name, which is `ADR-429` section 10's ninth instance; its remedy is forbidden by [ADR-386:169](../decisions/ADR-386.md) where 84.2% of the population sits; and **this fence structurally cannot register a check**, because `CHECKS` lives in the file this row was told to read and not edit. **THE HONEST ANSWER IS A CONVENTION AND IT IS DEMONSTRATED**: with a bindable name, **8 of 8 fail at `ADR-447`'s line and 8 of 8 pass at the true line**, so the machinery is starved of input rather than missing. **It cannot be retrofitted and must not be**: 8,624 of the 9,166 rewrites are inside dated records. **NO CHECK BUILT, NO FILE UNDER `packages/` OR `apps/` TOUCHED, `repo-invariants.mjs` READ AND NOT EDITED, NO LANDED ADR EDITED, NO GATE WEAKENED, NO TEST SKIPPED, NO PROBE COMMITTED.** **THE CONTAINER ARRIVED WITH NO REPOSITORY** and with no `node_modules`. -->

### 1. The objective

Discharge [ADR-449](../decisions/ADR-449.md) section 9 item 3: derive how many pointers in this tree land on a line that is not what the citing text is about, across code and documents, then decide whether a control is possible and build it ONLY if it is honest.

### 2. Done

- **The container measured and reported.** No repository on arrival and none anywhere on the filesystem; `add_repo` at `access: "push"`, then one clone. `node_modules` absent and installed from the lockfile.
- **The base reproduced at `5b7fe6c2`** on `origin/claude/wave-448s`, not `main`, before the first edit, **and it is green**: invariants **35 of 35**, gates **33 of 33**, suite **344 files / 8,612 passed / 11 skipped / 0 failed**. Neither `pnpm run verify` nor `falsify.mjs` was run.
- **The eight reproduced at source**, each opened at the line `ADR-447` cites and at `+460`, and `ADR-449` is right about all eight.
- **The dispatch's framing re-derived and found false in two places**, both in the direction that makes the hole bigger: `RI-15` never reads an ADR, and `RI-16` does not read one either.
- **The census run with the tree's own reader**, a byte copy of `repo-invariants.mjs` given one `export` line and deleted before the first commit, **calibrated by reproducing both checks' finding sets key for key**.
- **A control REFUSED, with three independent reasons and the cost of the honest alternative derived.**
- **The convention change argued and priced by measurement**, 8 of 8 against 1 of 8, including the one grammar limit that constrains how the rule must be worded.

### 3. Next

- **The joint gap named in `RI-15`'s and `RI-16`'s own headers** ([ADR-450](../decisions/ADR-450.md) section 9 item 2). Each header is correct alone and neither says the other exists; together they leave 433 of 435 files under `docs/decisions/` read by neither.
- **The live-scope vacancy and anchor slice**, priced at 92 and 14 findings (section 9 item 3). It is dischargeable and **it answers none of this row's finding**.
- **A ruling on whether a convention with no gate is worth writing down** (section 9 item 4), since any enforcement shape at all is an edit to a file no row on this wave holds.

### 4. Blockers

- **None for this row.** [ADR-450](../decisions/ADR-450.md) is `proposed` and UNSIGNED and the `E2` read is owed; no founder read was available during the session and none was required to finish.

### 5. Landmines

- **A GREEN CITATION GATE SAYS NOTHING ABOUT AN ADR.** `RI-15` and `RI-16` are both green on a tree carrying 716 pointers that fail one of the tree's own mechanical legs. **Do not read either gate as covering a pointer you wrote in an ADR. Neither one has ever looked at one.**
- **TWO CORRECT EXCLUSIONS COMPOSED INTO A HOLE NOBODY DECIDED ON.** `RI-15` skips ADRs because they are dated records; `RI-16` skips dated records because repairing one rewrites the record. Each reason is good. **The composition was never written down anywhere**, and it is 84.2% of this corpus's pointers.
- **A PROBE IS NOT A CONTROL.** The measurement harness reproduced both checks exactly and was still deleted before the first commit, because shipping it would have put a file named like a check into `packages/tooling/checks/` that nothing invokes.
- **CITE A NAME, NOT ONLY A NUMBER.** A pointer with a backticked fragment beside it is checkable by machinery that already runs. A bare pointer carrying only a number is checkable by nothing, in any scope, forever. **Writing that sentence with a worked example in it introduced an unresolvable pointer into this very file** and the census caught it, which is the shortest demonstration of the finding this row could have asked for: the example was removed rather than kept, because a document may not ship a pointer nobody can follow.

### 6. Files touched

- [`docs/decisions/ADR-450.md`](../decisions/ADR-450.md) - new.
- [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) - row `450` only.
- [`docs/decisions/README.md`](../decisions/README.md), [`docs/sessions/README.md`](README.md), [`docs/INDEX.md`](../INDEX.md), [`docs/STATE.md`](../STATE.md) - generated spans and registers, `generate` output only in `STATE.md`.
- This file.
