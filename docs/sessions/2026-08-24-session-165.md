---
status: approved
depends_on: [README.md]
last_updated: 2026-08-24
---

## 2026-08-24 - Session 165: M16 measured against the tree, and the schema is finished while nothing above it has begun
<!--index: **M16 measured against the tree, and the answer is the OPPOSITE shape from [session 143](2026-08-23-session-143.md)'s P3: ALL EIGHT schema deltas are landed and NOTHING ABOVE THE SCHEMA EXISTS.** `SD-M16-01` to `SD-M16-03` in [`0019`](../../packages/db/migrations/0019_notifications_and_community.sql), `04` to `07` in [`0029`](../../packages/db/migrations/0029_phone_identity_and_auth.sql), `08` in [`0041`](../../packages/db/migrations/0041_contact_channel_complaints.sql), every one **landed** in [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md). Zero lines of TypeScript name a notification, zero of the six endpoints exist, zero of the eleven fixtures are written and **all eleven are `blocked / no-fixture-format`**. **A NINTH schema touch exists that M16 section 2 does not row**: [`0028:166`](../../packages/db/migrations/0028_supersede_plan_version_immutability.sql) supersedes `notification_kinds_has_channels`. **NONE of M16's six endpoints appears anywhere in [API_CONTRACT](../architecture/API_CONTRACT.md), in any form**, and no gate can see that. **`notifications` and `notification_preferences` carry ZERO later `ALTER TABLE`, `notification_kinds` and `contact_channels` carry four `ADD COLUMN` between them**, which splits the [ADR-084](../decisions/ADR-084.md) transcription into two slices rather than one. **M16:270 cites "the nine" [EVENTS section 11](../architecture/EVENTS.md) triggers and section 11 rows TWELVE**; `identity.restriction_lifted` is named by NO module plan at all. Eight slices, three needing `packages/queue` which **does not exist**: session 147's branch carries a placeholder log and no code. `POST /me/contact-channels` is refused to an impersonation session **by inheritance and by nothing else** ([ADR-068](../decisions/ADR-068.md) row 5, "this entry adds nothing"). 29 of 29, 7 of 7, 104 files / 1,512 passed / 1 skipped, all measured here. No code, no migration, no gate, no ADR, nothing signed -->

- **Objective:** measure [M16](../plans/M16-notification-center.md) against the tree the way [session 143](2026-08-23-session-143.md) measured P3. An INVENTORY. No document, no `INDEX` row, no `STATE` append, no ADR, no ruling.

- **Done:** the six things the brief asked for, below. **29 of 29 gates, 7 of 7 repo invariants, 104 test files / 1,512 passed / 1 skipped, `pnpm run verify` exit 0**, executed on this tree rather than inherited from a prior log. Every one is unaffected by this session, which writes no code.

- **The count, honestly.** M16 is 520 lines over eleven sections. All eleven are read and measured. **Two claims inside M16 are false against the tree today** (section 2's "eight deltas" and section 5's "the nine triggers") and both are reported here rather than repaired, because M16 is `approved` and moving it takes an ADR.

---

### 1. What M16 specifies, section by section

| Section | What it commits Merit to |
|---|---|
| **1.1 to 1.2** | The five classes `NC-M16-01` to `NC-M16-05`. **The class decides what a preference may do**, and the line sits between account-state (channel choice only) and marketing (fully mutable). `NC-M16-05` pre-identity auth is [ADR-039](../decisions/ADR-039.md) amendment 2's, is never rate-limit exempt, is never coalesced, and is **a policy row and never a `notifications` row** |
| **1.3** | Five boundaries. M10 transports, M17 decides an offer exists, M15 is not a channel, M04 renders, M07 owns what a trader may know |
| **1.4** | Thirteen invariants `INV-M16-01` to `INV-M16-13`. `03` (every verified channel **and every prior one**), `04` (a preference change confirms to the OLD contact first), `11` (security and money exempt from rate limiting), `12` (the exemption is post-identity, and the split is the invariant), `13` (a complaint suppresses marketing and nothing else) |
| **2** | Eight schema deltas `SD-M16-01` to `SD-M16-08` |
| **3.1** | The lifecycle machine: `created` to `suppressed` / `coalesced` / `queued` to `dispatched` to `delivered` / `failed` / `bounced`; `bounced` **terminal**, `failed` retries on the same idempotency key |
| **3.2** | The contact-change ceremony, twice: the email leg, and the phone leg with **both prior legs recorded by one `prior_notified_at`**, the new number's OTP as `NC-M16-05`, and the confirmation **never SMS alone** (`C-27`) |
| **3.3** | Coalescing, keyed on **identity and never account** |
| **3.4** | Delivery outcomes. `bounced` is per message and already legal in `0019`; a spam complaint is **per destination** and is `contact_channels.complained_at`. Three refusals of `spam_complaint` as a `delivery_status`, and one of `notification_preferences` as its home |
| **4** | Six endpoints M16 **owns**: `GET /notifications`, `POST /notifications/:id/read`, `GET` and `PATCH /me/notification-preferences`, `POST` and `DELETE /me/contact-channels`, `GET /admin/notifications/:identityId` |
| **5** | Five events emitted, and a consumed set given as "the nine [EVENTS section 11](../architecture/EVENTS.md) triggers, plus the payout family, `wallet.*`, `kyc.*`, `flag.status_changed`, `enforcement.applied`, `certificate.issued`, `loyalty.benefit_earned`, `tos.version_published`, and `day.closed`" |
| **6** | Ten failure modes `FM-M16-01` to `FM-M16-10` |
| **7 and 7.9** | Seven adversarial scenarios, all NOVEL, plus the verification-notification tone rules bound by [M19 section 7.9](../plans/M19-kyc-identity.md) |
| **8** | Eight unit suites (`M16-C`, `-S`, `-B`, `-T`, `-P`, `-N`, `-R`, `-A`) totalling **56 named tests**, plus **11 owned golden fixtures**, plus section 8.3's coverage rule: every kind has a class test **and** a negative suppression test |
| **9** | Thirteen metrics, thirteen alerts, one M06 dashboard panel |
| **10** | Four open questions `OQ-M16-01` to `OQ-M16-04` |
| **Dependencies** | Six, `DEP-M16-01` to `DEP-M16-06`, on M10, M7 (twice), M5, M4 and M3/M9 |

---

### 2. What EXISTS, measured by running commands

**The schema is COMPLETE. All eight deltas are landed and [DELTA_MANIFEST:177-209](../../packages/db/DELTA_MANIFEST.md) says so on every row.**

| Delta | Where, at line | What is actually there |
|---|---|---|
| `SD-M16-01` | [`0019:33`](../../packages/db/migrations/0019_notifications_and_community.sql) | `notification_kinds`, with `mutable` **generated** at `:56`, `notification_kinds_immutable_never_coalesced` at `:68`, `notification_kinds_class_idx` at `:77` |
| `SD-M16-02` | [`0019:86`](../../packages/db/migrations/0019_notifications_and_community.sql) | `notifications`, `class` and `template_version` denormalized AT SEND TIME, `rendered_body`, `coalesce_key`, `dispatch_ref` FK to `integration_dispatches`, `delivery_status` at `:124` already carrying **`bounced`**, `notifications_delivered_has_timestamp` at `:131`, four indexes |
| `SD-M16-03` | [`0019:181`](../../packages/db/migrations/0019_notifications_and_community.sql) | `contact_channels`, supersession columns, `contact_channels_live_uq` at `:202` and `contact_channels_recently_superseded_idx` at `:207`, which is `INV-M16-03`'s own read |
| `SD-M16-04` | [`0029:388`](../../packages/db/migrations/0029_phone_identity_and_auth.sql) | `otp_send_budget`, four scopes, `state` with **no stopping value**, `otp_send_budget_degraded_is_alarmed`, `deferred_registrations`, the dated override |
| `SD-M16-05` | [`0029:519`](../../packages/db/migrations/0029_phone_identity_and_auth.sql) | `otp_challenges.channel` (no default, deliberately), `destination_hash` at `:522`, `email_normalized` relaxed at `:527`, `otp_challenges_exactly_one_destination` at `:532` |
| `SD-M16-06` | [`0029:619`](../../packages/db/migrations/0029_phone_identity_and_auth.sql) | `contact_channels_kind_allowed`, widened to `sms` |
| `SD-M16-07` | [`0029:684`](../../packages/db/migrations/0029_phone_identity_and_auth.sql) | `notification_kinds_class_allowed` gains `pre_identity_auth`; `rate_limit_exempt` **generated** at `:695`; the coalescing constraint re-added at `:706`; `notification_kinds_rate_limit_exempt_idx` at `:713` |
| `SD-M16-08` | [`0041:86`](../../packages/db/migrations/0041_contact_channel_complaints.sql) | `contact_channels.complained_at` and its partial index at `:107` |

**A NINTH TOUCH EXISTS AND M16 SECTION 2 DOES NOT ROW IT.** [`0028:166`](../../packages/db/migrations/0028_supersede_plan_version_immutability.sql) drops and re-adds `notification_kinds_has_channels`, moving it from `array_length(default_channels, 1) >= 1` to `cardinality(default_channels) >= 1`. The constraint `SD-M16-01` specifies **is not the constraint in the database**. It is a strengthening, not a defect, and it is a delta M16's own table cannot see.

**A TENTH AND ELEVENTH EXIST ON `contact_channels` FROM OTHER MODULES.** [`0034`](../../packages/db/migrations/0034_reversible_contact_addresses.sql) adds `value_ciphertext` (`:225`), `value_key_id` (`:235`) and `value_encrypted_at` (`:238`) under [ADR-046](../decisions/ADR-046.md), and creates the `merit_dispatcher` role at `:192` with **no `DELETE`** on the founder's amendment. That file is what gives `INV-M16-03` an address it can actually send to; both `0019` and the design record said "the sending path holds the address" and the sending path held nothing.

**Two CI gates already enforce M16 policy, and both are green.**

- [`gates.mjs:2069`](../../scripts/corpus/gates.mjs), `CI-06k` assertion 3, reads the **generated expression** for `rate_limit_exempt` out of the migrations and fails on any class outside `POST_IDENTITY_EXEMPT = {security, money}` (`:2073`). It reads the expression rather than seed rows on purpose: the generated column is what makes a seed row unable to lie.
- [`falsify.mjs:2200`](../../scripts/corpus/falsify.mjs), `CI-06k/exempt-class`, watches it fail on a seeded class, deriving the class from the migration at seed time rather than naming one.

**Three database probes exercise M16 tables**, run against `postgres:16` by [`corpus.yml`](../../.github/workflows/corpus.yml): [`probe_phone_identity.sql`](../../scripts/db/probe_phone_identity.sql) (`:347` seeds `notification_kinds`, `:698` is its `SD-M16-07` block, `:764` asserts `rate_limit_exempt` is not writable, `:775` asserts the coalescing constraint), [`probe_reversible_contact_addresses.sql:208`](../../scripts/db/probe_reversible_contact_addresses.sql), and [`probe_plan_version_immutability.sql:184`](../../scripts/db/probe_plan_version_immutability.sql) for `notification_kinds_has_channels`.

**The supporting rows other modules owe exist.** `phone_change_requests.prior_notified_at` at [`0029:297`](../../packages/db/migrations/0029_phone_identity_and_auth.sql), with `0034:399` and `:407` adding the two dispatch references that say which legs it covers. `detector_definitions.is_sensitive` at [`0008:41`](../../packages/db/migrations/0008_risk.sql) is `SD-M7-03`, which `DEP-M16-02` names as the template allowlist's source. `integration_dispatches` exists in `0018` and is `notifications.dispatch_ref`'s target. [`0042:183`](../../packages/db/migrations/0042_impersonation_sessions.sql)'s `IMPERSONATION-C1` makes an impersonation token unresolvable as a trader session, in both directions.

**One CRON_INVENTORY row is M16's and it is a written exemption**: [`CRON_INVENTORY:74`](../ops/runbooks/CRON_INVENTORY.md) dispositions `otp_send_budget.override_expires_at` as lapsing by recomputation, on `plan_breaker_state`'s precedent one row up, so `CI-06l` is satisfied without a sweep.

**The fixture registry rows all eleven, and rows them consistently.** [Section 33.1:24](../testing/golden-scenarios/33-ownership-index-and-coverage-reconciliation.md) gives M16 `GS-192` to `GS-197`, `GS-270` to `GS-271`, `GS-293` to `GS-295`, count **11**, which agrees with M16 section 8.1's own cell. [Section 39](../testing/golden-scenarios/39-fixture-status-and-blockers.md) rows every one of the eleven `blocked | no-fixture-format`, at `:257-262`, `:335-336` and `:358-360`, all with the same reason: the only fixture format in the tree is M1's day stream folded through `advanceDay`.

---

### 3. What does NOT exist, exactly

**No application code. Not one line.** `grep -rn -i "notification\|contact_channel\|contactChannel" --include="*.ts" apps packages` returns **one hit**, [`duplicate-signal-views.test.ts:142`](../../packages/db/test/duplicate-signal-views.test.ts), and it is a comment about a regex incidentally matching a channel-kind check. The wider grep for `M16` outside `docs/` and `migrations/` returns only comments in two test files, one workflow and the two corpus scripts already named above. **The absence survives the widest grep I could write.**

| What M16 specifies | What is there |
|---|---|
| Six endpoints (section 4) | **Nothing.** [`apps/api/src/index.ts:36`](../../apps/api/src/index.ts) states it: *"NO ROUTE IMPLEMENTS A BEHAVIOUR AND THAT IS NOT AN OVERSIGHT."* The app is [`surface.ts`](../../apps/api/src/surface.ts)'s startup partition and nothing else. Fastify is ruled by [ADR-083](../decisions/ADR-083.md) and **not installed**: `apps/api/package.json` declares zero dependencies |
| Five events emitted (section 5) | **Nothing.** No file in `apps` or `packages` names `notification.created`, `notification.suppressed`, `notification.delivery_failed`, `contact.channel_changed` or `notification.preferences_changed` |
| The three tables in the ORM (`SD-M16-01` to `03`) | **Nothing.** [`packages/db/src/schema.ts:4`](../../packages/db/src/schema.ts) declares *"SEVEN TABLES OF 111"* and none of the seven is M16's. [`scope.ts:39`](../../packages/db/src/scope.ts) `TABLES` is the same seven, and `SCOPE_RULES` is total over it **as a compile error** |
| A registered kind (`INV-M16-01`) | **Nothing outside the probes.** The only `INSERT INTO notification_kinds` in the tree is [`probe_phone_identity.sql:347`](../../scripts/db/probe_phone_identity.sql) and its two siblings, which seed and roll back. `packages/db/src/seed/` holds **calendars only** |
| A template catalogue, and the lint over it (`INV-M16-06`, `M16-T-nn`) | **Nothing.** `apps/site/src/content/documents.ts` and `apps/portal/src/copy/copy-block.ts` are the only copy surfaces and neither is a template catalogue. The three merit ESLint rules are `engine-purity`, `no-calendar-in-expiry-path` and `no-raw-db-client` |
| 56 unit tests across eight prefixes (section 8.1) | **Zero.** `grep -rn "M16-C-\|M16-S-\|M16-B-\|M16-T-\|M16-P-\|M16-N-\|M16-R-\|M16-A-"` over `*.ts` and `*.mjs` returns nothing |
| Eleven golden fixtures | **Zero written**, and all eleven `blocked / no-fixture-format` (section 2) |
| Thirteen metrics and thirteen alerts (section 9) | **Nothing.** No metric name, no alert rule, no M06 panel |
| The queue the lifecycle's `queued` state names | **`packages/queue` does not exist.** `packages/` holds `db`, `eslint-plugin-merit`, `golden-loader`, `harness`, `rithmic`, `rules-engine`, `tooling`. Session 147's branch `origin/claude/s147-adr086-queue` carries a **placeholder session log and no `packages/queue` file at all** |

**AND THE ONE THAT WOULD BE MISSED: none of M16's six endpoints appears in [API_CONTRACT](../architecture/API_CONTRACT.md), in any form.** Grepping the whole file for `notif`, `inbox` and `contact.chan` returns three hits and **not one of them is an endpoint**: `prior_notified_at` inside a `PhoneChangeRequest` type at `:206`, the `POST /auth/otp` rate-limit row at `:644`, and the section 12 negative-authz row at `:692`. That row reads *"Changing an email or phone contact from a non-elevated session"* and its note says `POST /phone/change` **and the email equivalent share one row** -- the equivalent is never named. So API_CONTRACT carries the **factor** for M16's most sensitive route and carries no **path** for any of the six. **No gate can see this**: `gates.mjs list` shows nothing that reconciles a module plan's endpoint table against the contract, and `CI-06k`'s assertion 2 is satisfied by the un-pathed row at `:692`.

---

### 4. The slices

Eight. Every fence is BY FILE. **Every one of the three `packages/queue` dependencies is marked, and it does not exist yet.**

| # | Objective | Fence, by file | Money path | Depends on |
|---|---|---|---|---|
| **S1** | `notifications` and `notification_preferences` join the ORM transcription. The seven become nine | `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts` | no | 145 (landed). **NOT the queue** |
| **S2** | `notification_kinds` and `contact_channels` join it, **and the [ADR-084](../decisions/ADR-084.md) drift assertion learns a column set that is a `CREATE` plus later `ADD COLUMN`s.** Needs an ADR | `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, `docs/decisions/ADR-nnn.md` (new) | **yes.** `contact_channels` is the anti-takeover control's row and `C-27`'s subject | **S1** on all three files. **NOT the queue** |
| **S3** | The kind registry: every kind M16 names, seeded with class, `template_code` and `default_channels`; `M16-R-nn` and `M16-C-nn`, including section 8.3's negative half | `packages/db/src/seed/notification-kinds/**` (new), `packages/db/test/notification-kinds.test.ts` (new) | **yes.** The class column is `INV-M16-02`'s entire enforcement | **S1**, **S2**. **BLOCKED on `OQ-M16-03`**: `default_channels` for every kind is undecidable while "does push ship" is open. **NOT the queue** |
| **S4** | The read surface: `GET /notifications`, `POST /notifications/:id/read`, `GET /me/notification-preferences` | `apps/api/src/routes/notifications.ts` (new), `apps/api/src/index.ts`, `apps/api/package.json`, `apps/api/test/notifications.test.ts` (new) | no | **S1**; 144 (landed); Fastify in the catalog, which is 146/147's `pnpm-lock.yaml`. **BLOCKED**: the three paths are not in API_CONTRACT. **NOT the queue** |
| **S5** | The send path: class resolution, channel resolution, render from a versioned template into `rendered_body`, **transactional enqueue** | `packages/notifications/**` (new), `packages/notifications/package.json`, `pnpm-workspace.yaml` (catalog only), `pnpm-lock.yaml` | **yes.** `INV-M16-02` and `INV-M16-11` are enforced here or nowhere | **S3**; **`packages/queue` (session 147) -- REQUIRED.** [ADR-006](../decisions/ADR-006.md)'s whole reason is that *"enqueue participates in the same transaction as the state change that caused it"*, and a send path that enqueues outside the transaction reintroduces exactly the bug ADR-006 was accepted to remove |
| **S6** | Coalescing: the window, keyed per identity, and the batch-replay idempotency `GS-194` pins | `packages/notifications/coalesce.ts` (new), `docs/ops/runbooks/CRON_INVENTORY.md`, a migration **if the window needs a clock** | no | **S5**; **`packages/queue` -- REQUIRED**, and additionally a **scheduled** primitive, which [P3-d's fence](../plans/P3-ledger-billing-identity.md) explicitly excludes: *"CRON_INVENTORY IS NOT IN YOUR FENCE and the scheduled jobs are not yours"* |
| **S7** | The contact-change ceremony: `POST` and `DELETE /me/contact-channels`, `PATCH /me/notification-preferences`, the `C-27` elevation, the prior-contact window | `apps/api/src/routes/contact-channels.ts` (new), `apps/api/src/routes/notification-preferences.ts` (new), `apps/api/src/index.ts`, `apps/api/test/**`, `docs/architecture/API_CONTRACT.md` | **yes. `C-27`, auth** | **S2**, **S4**, **S5**; **`packages/queue` -- REQUIRED** for `INV-M16-04`'s confirmation leg. **BLOCKED on `OQ-M16-02`**: the window has no duration, no column and no config table |
| **S8** | Delivery outcomes and the proof-of-notice query: `bounced` terminal, `complained_at` consulted for marketing only, `GET /admin/notifications/:identityId`, and the two alarms that fire **on the delivery record** | `packages/notifications/delivery.ts` (new), `apps/api/src/routes/admin-notifications.ts` (new), `apps/api/src/index.ts`, `docs/architecture/API_CONTRACT.md`, `docs/ops/runbooks/CRON_INVENTORY.md` | **yes.** `FM-M16-09` is an access incident, not a statistic | **S5**; **`packages/queue`** for the sweep; **M10** for the provider webhook, which has no code either |

**Three of the eight need `packages/queue` and it does not exist.** S5, S6 and S8 are unstartable until session 147 lands, and **S6 needs more than 147 is building**: 147's own fence says the scheduled jobs are not its. **S1 to S4 and S7's API halves need nothing from the queue at all**, so the module is not blocked as a whole -- it is blocked from the send path downward.

**No slice needs a new migration**, with one conditional: S6 needs one only if the coalescing window turns out to need a clock column. Section 6 below says why that is undetermined.

---

### 5. The collisions between these slices, PER FILE

| File | Slices | Resolution |
|---|---|---|
| `packages/db/src/schema.ts` | **S1, S2** | **SERIAL.** One file, one `pgTable` block per table; two branches both appending compile alone and neither with the other |
| `packages/db/src/scope.ts` | **S1, S2** | **SERIAL, and harder than schema.ts.** `SCOPE_RULES` is a mapped type over `TableKey`, so a table added in one branch and a rule added in the other **do not compile together**. Same ordering as above |
| `packages/db/test/scoped-db.test.ts` | **S1, S2** | **SERIAL.** `DDL_NAMES` is one list and S2 additionally changes the drift assertion's rule |
| `apps/api/src/index.ts` | **S4, S7, S8** | **SERIAL, or one owner.** Every route registration lands here. Three branches each adding a registration line is three conflicts in one hunk |
| `docs/architecture/API_CONTRACT.md` | **S4, S7, S8** | **ONE SLICE ADDS ALL SIX ROWS, and it is a ruling rather than an edit.** The document is `approved` and the corpus is frozen: six endpoint sections are a change to a frozen document, so an ADR carries them. Splitting them three ways is three ADRs against one document |
| `packages/notifications/**` | **S5, S6, S8** | **SERIAL on the package manifest and `index.ts`; parallel below them.** `coalesce.ts` and `delivery.ts` do not touch each other |
| `pnpm-workspace.yaml` and `pnpm-lock.yaml` | **S4, S5** | **SERIAL, and 147 HOLDS THEM RIGHT NOW.** [Session 143](2026-08-23-session-143.md) already measured this: *"a lockfile cannot be appended to per row"*, and P3-d's fence carries `pnpm-workspace.yaml` (catalog only) and `pnpm-lock.yaml`. Neither S4 nor S5 may start while 147 is open |
| `docs/ops/runbooks/CRON_INVENTORY.md` | **S6, S8** | **SERIAL.** Both add a scheduled row and the coverage table is one table |
| `packages/db/migrations/` | **S6 only, conditionally** | No collision. Seven of the eight slices write no migration |

---

### 6. What I could not determine

1. **Whether the coalescing window has a clock at all, and therefore whether S6 needs a migration.** Section 3.3's machine says `open --> rendered: window closes` and names no duration, no column and no trigger. `notifications` carries `coalesce_key` and no `*_expires_at`, so **`CI-06l` cannot see this window** -- its own `covers` line says it reads no clock whose column is not named `*_expires_at`. Whether the window closes on a timer, on the nightly batch's boundary, or on a count is not decided anywhere in the corpus I could find.

2. **Whether `packages/queue` will expose a scheduled primitive.** S6 and S8 need one. `origin/claude/s147-adr086-queue` carries no code, its log is a placeholder, and its brief says *"What lands is the interface and its adapter, not a job."* **This is unknowable until 147 lands** and is the single largest uncertainty in the slice graph.

3. **Which document owns adding the six endpoint rows, and under whose ADR.** API_CONTRACT is frozen. M16 section 4 is frozen. The rows are missing from the first and present in the second, and nothing in the corpus says which moves.

4. **Whether `OQ-M16-02`'s window is a constant, a plan-config row, or a new config table.** No config table exists in any of the 46 migrations. `otp_send_budget` and `plan_breaker_state` are the closest shapes and both are per-evaluation-day rows rather than settings.

5. **Whether `identity.restriction_lifted` is M16's to send.** [EVENTS section 11](../architecture/EVENTS.md) rows it with the guard *"always send"*, and grepping all of `docs/` shows it named in **EVENTS.md, STATE.md and one 2026-08-16 session log and in NO module plan whatsoever**. A trigger with an always-send guard and no owning module is either M16's twelfth consumed trigger or nobody's.

6. **Whether M16's eight suites are Vitest projects or files.** `vitest.config.ts` declares `unit`, `property`, `integration` and `golden`; section 8.1's "runs: every commit / blocks: merge" cells fit `unit`, but nothing states it, and `integration` still needs `CI-04`'s database.

---

- **Next:** three items go to the founder rather than to a session. **`OQ-M16-02`** and **`OQ-M16-03`** each block a named slice above and both are still open ([the identifier survey](../reviews/2026-08-21-identifier-series-survey.md):346 rows `OQ-M16` as **4 raised, 4 with no disposition table anywhere**). **The API_CONTRACT gap** is the third, and it is the one to take first because four slices wait behind it. Then S1, which nothing blocks and which needs no ruling, no queue and no migration.

- **Blockers:** none for this session. For M16: `packages/queue` (session 147, in flight), `OQ-M16-02`, `OQ-M16-03`, the six absent API_CONTRACT rows, and the fixture format all eleven golden scenarios are blocked on.

- **Landmines:**

  - **THE SEVEN-TABLE DRIFT ASSERTION SPLITS M16'S FOUR TABLES CLEANLY IN TWO, AND THE SPLIT IS WHY S1 AND S2 ARE SEPARATE SLICES.** [`scoped-db.test.ts:422`](../../packages/db/test/scoped-db.test.ts) asserts *"no later migration changes a column on any of the seven"*, re-derived every run over `ADD`/`DROP`/`ALTER COLUMN` and `RENAME` with `ADD CONSTRAINT` exempt. Measured per table: **`notifications` and `notification_preferences` carry ZERO later `ALTER TABLE` of any kind**, so they join the transcription with the assertion untouched. **`notification_kinds` carries `ADD COLUMN rate_limit_exempt` at [`0029:695`](../../packages/db/migrations/0029_phone_identity_and_auth.sql), and `contact_channels` carries three at [`0034:225,235,238`](../../packages/db/migrations/0034_reversible_contact_addresses.sql) plus one at [`0041:86`](../../packages/db/migrations/0041_contact_channel_complaints.sql)** -- four `ADD COLUMN` between them. **A session adding all four tables at once turns the suite red in the assertion whose entire purpose is to fail loudly**, and the fix is not to loosen it: [ADR-084:115](../decisions/ADR-084.md) already says *"An absence is the one kind of claim a wider grep refutes"* about this exact assertion, and the wider grep here refutes it for two of the four. The ruling S2 needs is how a transcription reads a table whose column set is a `CREATE` plus later `ADD COLUMN`s, and it is a ruling because the alternative -- reading the whole migration set per table -- is a second parser.

  - **`POST /me/contact-channels` IS REFUSED TO AN IMPERSONATION SESSION BY INHERITANCE AND BY NOTHING ELSE, AND [ADR-068](../decisions/ADR-068.md) SAYS SO IN ITS OWN TABLE.** Row 5, *"contact change, either kind | `C-27` | **nothing**"*. Four routes are refused explicitly and three are inherited, and this is one of the three. **The mechanism is that an impersonation session can never elevate**, because requirement 2 says it never inherits or intercepts the trader's OTP or passkey, and [SECURITY:45](../architecture/SECURITY.md)'s `C-27` requires a passkey assertion or a dual-channel confirmation for a contact change of either kind. **ADR-068 states the corollary against itself**: if a later change ever gives an impersonation session a way to elevate, *"three refusals disappear silently and no test in this entry's list would fail"*. So **S7 is where `M6-N-05` becomes assertable for the first time** -- [M06:475](../plans/M06-admin-ops-console.md) rows it as `C-27, inherited` -- and a slice that implements the route without the elevation check has not left a failing test behind, it has left no test at all.

  - **M16 SECTION 5 CITES "THE NINE" EVENTS TRIGGERS AND [SECTION 11](../architecture/EVENTS.md) ROWS TWELVE.** Counted: twelve `| \`event\` |` rows under `## 11` at `:306`. The nine M16 was written against are the twelve minus `payout.held`, `payout.hold_released` and `identity.restriction_lifted`, all three of which arrived with the enforcement-window work after M16's `last_updated: 2026-08-20`. **Two of the three are covered by M16's own "plus the payout family"; the third is not, and no other module plan names it either.** This is [session 155](2026-08-24-session-155.md)'s lesson recurring one document over: a clause that names a count against a document that can grow. It is reported and not repaired, because M16 is `approved`.

  - **M16 SECTION 2 SAYS "EIGHT DELTAS" AND THE DATABASE CARRIES A NINTH CHANGE TO AN M16 CONSTRAINT.** [`0028:166`](../../packages/db/migrations/0028_supersede_plan_version_immutability.sql) supersedes `notification_kinds_has_channels`. **The delta count is right and the delta table is incomplete**, which is a different defect from a wrong number: `DELTA_MANIFEST` rows eight `SD-M16-nn` and all eight are landed, so nothing in the delta ledger is wrong. What is wrong is that a reader of M16 section 2 believes the constraint reads `array_length(...) >= 1` and it reads `cardinality(...) >= 1`. **A session writing `M16-R-nn` against the plan's text rather than the DDL writes an assertion the database does not hold.**

  - **NO GATE RECONCILES A MODULE PLAN'S ENDPOINT TABLE AGAINST API_CONTRACT, AND M16 IS SIX ROWS OF EVIDENCE FOR ONE.** `CI-06d` reconciles `GS-nnn` and `EC-nnn`, `CI-06i` reconciles DATA_MODEL against the migrations in both directions, `CI-06c` reconciles INDEX. **There is no `CI-06` for endpoints**, and `CI-06k`'s own `covers` line names the hole from the other side: *"it does not know which endpoints are sensitive beyond `C-27`'s three named actions, so an endpoint nobody classified is invisible to assertion 2."* Six endpoints a module `Owns` are invisible to every gate in the runner. **This is a gate proposal and this session does not write it**, because writing it turns the corpus red on however many other modules share the defect, and measuring that is its own session.

  - **THE FIVE CLASSES ARE FOUR IN `notifications` AND FIVE IN `notification_kinds`, BY DESIGN, AND "COMPLETING THE PAIR" IS THE TRAP.** [`0019:102`](../../packages/db/migrations/0019_notifications_and_community.sql) checks `notifications.class IN ('security','money','account_state','marketing')` and `0029:684` widens **only** `notification_kinds`. [`0029:681`](../../packages/db/migrations/0029_phone_identity_and_auth.sql) states the reason in the file: `notifications.identity_id` is `NOT NULL`, so a pre-identity message cannot be a `notifications` row at all, and *"a later session 'completing the pair' by widening `notifications.class` would be adding a value that no row can ever legally carry."* **S3 is the slice most likely to reach for it**, because a registry seeder holding five classes sees a four-class check one table over and reads it as an omission.

  - **THE ELEVEN FIXTURES ARE BLOCKED ON A FORMAT AND NOT ON M16.** All eleven rows of [section 39](../testing/golden-scenarios/39-fixture-status-and-blockers.md) read `blocked | no-fixture-format` with one shared reason. **So none of the eight slices above can discharge a `GS-nnn`**, and a slice that claims one in its done-clause is claiming something the fixture registry says cannot be written. The eight unit suites are M16's only available evidence until the format question is answered elsewhere.

- **Files touched:** [`docs/sessions/2026-08-24-session-165.md`](2026-08-24-session-165.md) (new, this file), [`docs/sessions/README.md`](README.md) (the `165` CLAIM row, plus its generated ENTRY row). **Nothing else.** No `INDEX.md` row, no `STATE.md` append, no ADR, no code, no migration, no gate, no fixture.

### What was NOT done, named rather than implied

**No implementation and no ruling.** M16 is **read and not edited**, and the two false claims inside it (section 2's delta table and section 5's "the nine") are reported here rather than repaired, because the document is `approved` and moving it takes an ADR that this session's fence does not hold. **No ADR was written or proposed**, so no `ALLOCATION` number is claimed. **The endpoint gate is described and not written.** **`OQ-M16-01` to `OQ-M16-04` are untouched**; two of them block named slices and both are put to the founder rather than decided here. **No session number is claimed for any of the eight slices**, because three of them wait on a package that does not exist yet and dispatching a fence against a path nobody has created is how a claim goes stale.

**One thing was found and could not be done inside the fence.** The brief states that session 165 was already CLAIMED. **It is not**: the CLAIM table on `main` carries `147`, `148`, `156` and a struck `152`, and no row for `165`. There is nothing to strike, so the row is **written here already struck**, in numeric position, on [session 143](2026-08-23-session-143.md)'s precedent of writing a missing registry row on the dispatching session's behalf and [ADR-065](../decisions/ADR-065.md) T3's amend-in-place rule. **A deleted row is indistinguishable from a number nobody ever claimed**, which [this README](README.md) says is the state the table exists to end, and an unwritten one is worse: it is a number the next session reads as free.
