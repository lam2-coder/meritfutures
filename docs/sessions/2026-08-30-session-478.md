---
status: approved
depends_on: [README.md]
last_updated: 2026-08-30
---

## 2026-08-30 - Session 478: the whole purchase, enumerated, and the critical path out of 503 runs through a lawyer

<!--index: **THE RULING IS [`ADR-287`](../decisions/ADR-287.md), `proposed` and UNSIGNED. A PLANNING ROW: the deliverable is a PLAN and not a backend. NO `src/` LINE MOVED, NO TEST CHANGED, NO MIGRATION WAS WRITTEN, NO `RI-nn` WAS MINTED, `0077` IS STILL NEXT, AND THE COUNTS DO NOT MOVE AT TEN WIRED OF TWENTY-FOUR DECLARED WITH FOURTEEN BLOCKED.** **`PayoutTx` DECLARES FOUR METHODS AND NOT MORE** (`routes/payouts.ts:548`, `:551`, `:554`, `:564`), and the carrier declares three members beside them (`:572,575,586`); all seven are classified at their primary sources because a `PayoutTx` nobody can open is not an implementation. **THE DISPATCHER'S TWO FACTS HOLD** and `grep -rn insertPayoutRequest` over every `apps/*/src` and `packages/*/src` returns 2 lines, neither an implementation; the only satisfying value in the repository is a test fixture. **ONE METHOD IS BUILDABLE TODAY**: `identityStatus` needs the `scoped` door (`apps/api/src/db.ts:237`), `identities` as `root` (`scope.ts:840`) and `ScopedTableKey` excluding only `firm` and `pair` (`scope.ts:1627`), and its signature carries `ADR-140`'s ordering, which an adapter caching the row inside `subject()` would satisfy and break. **ONE IS BUILDABLE EXCEPT IN TWO LEGS ALREADY DISPATCHED TO ROWS 285 AND 286, AND BOTH WERE RE-DERIVED RATHER THAN INHERITED**: `unwiredOrThrow` still rethrows at `routes/payouts.ts:1387`, so a wired backend meeting an unfolded day answers 500 where money leaves the firm; and no size-row decoder exists in any of the four `src/` trees, `PlanVersionSizeRow` being an exported TYPE only. **AND THE TWO THIS SESSION FOUND ARE VALUES WITH NO SUPPLIER RATHER THAN DOORS WITH NO IMPLEMENTATION, WHICH IS A KIND OF ABSENCE EIGHTEEN REVISIONS OF `usePayoutBackend`'s ENTRY HAVE NEVER NAMED.** **F2**: `insertPayoutRequest` must return an `eligibilitySnapshotId` and **NO SUCH ID EXISTS ANYWHERE IN THIS REPOSITORY**; `0010_payouts.sql:70-73` argues in its own comment AGAINST a snapshot table, the column is `jsonb`, and `eligibility_snapshot_id` occurs exactly ONCE in all of `docs/`, at `API_CONTRACT.md:490`. The three shapes an implementer would reach for are enumerated and none is taken, because two of them move a frozen contract or the port and the third contradicts `0010`'s own reasoning. **F1**: `HoldFlag` needs a `tosClause` and a `reason`, and **`risk_flags` CARRIES SEVENTEEN COLUMNS AND NEITHER IS AMONG THEM** (`0008_risk.sql:114-170`), while `payout_requests_hold_is_complete` (`0031:63-72`) makes both NOT NULL on a hold, **SO THE HOLD BRANCH OF THE MONEY PATH IS UNWRITABLE ON THIS TREE**. The `reason` half is sharper than a missing column: `G-HOLD-REQUIRED` fires on `open` OR `investigating` (`STATE_MACHINES.md:414`) and `M07:15` rules that entering `investigating` is the human act that produces a written reason, so a hold raised off an `open` flag has none by construction, and the three plausible substitutes are each refused with their source. **F4, AN INHERITED CLAIM CHECKED AND HALF FALSE**: the entry closes by saying `listPayouts` and `idempotency` are both constructible today; `idempotency` is (`idempotency-store.ts:144`) and `listPayouts` is NOT, because `failure_note` has no column and `API_CONTRACT.md:1356` states its storage unresolved in the corpus's own words, and `timeline` has no ruled source. **F3, TWO LANDMINES REGISTERED BECAUSE THE FAILURE MODE IS SILENT**: `PayoutRequestInsert.splitBp` and `.clampReason` have NO column in any migration and both values already sit inside the snapshot (`routes/payouts.ts:1060,1063`), so the correct action is to write neither; and `payout_requests.plan_version_id` is NOT NULL (`0010_payouts.sql:68`) while the insert shape does not carry it, derivable from `accounts.plan_version_id`. **FOUNDER-OWED, NAMED SEPARATELY AND NOT PLANNED AROUND: `HoldFlag.tosClause` STOPS AT COUNSEL.** `TOS_CLAUSES.md:9` is *"DRAFT FOR COUNSEL. Not publishable text"* with *"no drafted prose anywhere in this document, deliberately"*, so there is no clause identifier vocabulary at all, and `DEP-M7-05` (`M07:471`) owes the open clauses to Wave 4 legal. **No placeholder clause id was invented, no identity provider stubbed, no admin session invented, no hostname, bucket, key or secret written, and Merit stays passwordless.** **THE OTHER FOUR CANDIDATES WERE CHECKED RATHER THAN ASSUMED AND NONE IS ON THIS PORT**: `useAuthBackend` is not among the fourteen BLOCKED entries so the SSO purchase does not gate this, Rithmic reads nothing on this path, and Postmark gates OTP DELIVERY and therefore a DEPLOYMENT rather than a line of `PayoutTx`. **THE SIZE IS NINE SESSIONS AND IT IS NOT ROUNDED DOWN**: three rulings, five builds and the installation, each with a stop condition, and the wired count is pinned as a stop condition because `wired` is the set of ports `start.ts` calls (`wiring.test.ts:138`), so a module may land without moving it. **SEVEN ARE SCHEDULABLE, SLICE 8 IS NOT, AND SLICE 9 DEPENDS ON IT**, so no sequence of sessions alone takes this route off 503 and the critical path runs through a lawyer; the shortening move is named as a founder CHOICE rather than assumed, and refused here because removing the hold reopens the zero-denial argument `ADR-040` closed. **FENCE HELD.** The two files outside it are GENERATED: `docs/decisions/README.md` and `docs/sessions/README.md` carry generated spans (`adr_registry` and `session_entries`) rewritten by `gates.mjs generate` (`gates.mjs:1068,1094`), which `CI-06n` requires and which no hand edit produced. **NO NEW `docs/plans/` DOCUMENT WAS WRITTEN** though the fence permitted one, because `CI-06c` would require a hand-written `docs/INDEX.md` row and `INDEX.md` is not in the fence. **NO PORT WIRED, NO DOOR WIDENED, NO GATE WEAKENED, NO STUB, NO CAST, NO SYNTHESISED DEFAULT, NO FLOAT, NO SECRET.** Counts derived at reporting time, each command run separately: **33 of 33** gates after `generate`, **30 of 30** invariants off the runner's own last line, **297 files / 7,215 passed / 6 skipped**, measured against a baseline taken by stashing this branch's whole diff on this same tree and re-running: **297 / 7,215 / 6**, a delta of **ZERO**, which is what a planning row that changes no test must report. Typecheck 0, lint 0, `format:check` clean. **`pnpm run verify` was NOT run and `falsify.mjs` was NOT run**, both forbidden by the row. -->

---

### 1. The objective

**[ALLOCATION](../decisions/ALLOCATION.md) row `287`: the third of the three obstructions `usePayoutBackend`'s entry names, which that entry calls "the whole purchase".** The row's deliverable is an honest enumeration of `PayoutTx`'s declared surface plus a slicing, and it forbids a backend. One objective, money path, [ADR-003](../decisions/ADR-003.md) regime.

### 2. What was read before anything was written

Row `287`, then `usePayoutBackend`'s entry in full (`apps/api/test/wiring.test.ts:361-811`, comment block and reason string alike), then `routes/payouts.ts`'s port declarations and every docblock on `PayoutSubject`, `HoldFlag` and `PayoutRequestInsert`, then the migrations, the scope registry, the engine barrel and the two legal documents the hold reaches.

**The dispatcher verified two things and both were re-derived.** `PayoutTx` is a type at `routes/payouts.ts:572` and `:1097`, and nothing implements it. A recursive grep for `insertPayoutRequest` over `apps/*/src` and `packages/*/src` returns two lines, the declaration and the one call site.

### 3. The enumeration

Section 1 of [ADR-287](../decisions/ADR-287.md) carries the table and sections 2 to 5 carry the per-method reasoning with a citation on every claim. **Four methods, plus three carrier members named because `transact` is the only thing that produces a `PayoutTx`.**

### 4. What was found rather than inherited

Four findings, `F1` to `F4`, in [ADR-287](../decisions/ADR-287.md) sections 4 and 5. Two are values with no supplier, one is a landmine pair, and one corrects a sentence `usePayoutBackend`'s entry states today.

### 5. The boundary

[ADR-287](../decisions/ADR-287.md) section 6. **`HoldFlag.tosClause` stops at counsel and nothing was stubbed to get past it.**

### 6. The slicing

[ADR-287](../decisions/ADR-287.md) section 7. **Nine sessions. Seven schedulable, one not schedulable at all, and the installation depends on the one that is not.**

### 7. Next

- Rows `285` (session 476) and `286` (session 477) are the near-term path and neither was touched here.
- **Slices 1 and 2 of section 7 are rulings and can be dispatched immediately**; neither needs rows `285` or `286`.
- **`F1`, `F2` and `F4` are owed to `usePayoutBackend`'s entry** by a row whose fence reaches `apps/api/test/**`. `F4` corrects a claim that entry makes today.

### 8. Blockers and landmines

- **BLOCKER, FOUNDER:** the ToS clause vocabulary. `DEP-M7-05`, Wave 4 legal. Slice 8 cannot be scheduled without it and slice 9 depends on slice 8.
- **BLOCKER, RULING:** `eligibility_snapshot_id` has no supplier and choosing one moves either the port shape or a frozen contract.
- **LANDMINE:** `PayoutRequestInsert.splitBp` and `.clampReason` have no column and must be written nowhere but the snapshot.
- **LANDMINE:** `payout_requests.plan_version_id` is NOT NULL and the insert shape does not carry it.
- **LANDMINE CARRIED:** `docs/STATE.md` still carries the `gen:adr_count` paragraph at `RI-12`'s ceiling of eight. This append adds no ninth copy.

### 9. Files touched

- `docs/decisions/ADR-287.md` (new)
- `docs/decisions/ALLOCATION.md` (row `287`)
- `docs/STATE.md`
- `docs/sessions/2026-08-30-session-478.md` (new, this file)
- `docs/decisions/README.md` and `docs/sessions/README.md`, **generated spans only**, rewritten by `gates.mjs generate`

**No file under any `src/`, no test, and no migration.**
