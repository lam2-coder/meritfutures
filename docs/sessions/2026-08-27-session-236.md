---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 236: `P5` planned against the tree

<!--index: **[P5-payouts-and-wallet](../plans/P5-payouts-and-wallet.md), `status: draft`: fourteen slices, four waves, fence BY FILE, money column, depends-on BY FILE.** **The answer is the INVERSE of [P4](../plans/P4-portal-and-site.md)'s**: four of P5's five stated contents are WRITES and the fifth reads a table with no producer. **The precondition is four sentences in one file** and [ADR-112](../decisions/ADR-112.md) wrote every one of them on purpose: `RowFilter` admits *"no `OR`, no `IN`, no range and no `IS NULL`"* and `SqlExecutorReason` has ONE member, so the hourly sweep cannot express `freeze_expires_at <= now()`, `readLiveHalts` cannot say `released_at IS NULL`, and **nothing in this tree can make one of two racing transactions lose**, which is `GS-230` and `DELIVERY_PLAN`'s own *"wallet concurrency green"*. **THE BRIEF'S CENTRAL PREMISE DID NOT SURVIVE**: `INV-M20-06`'s contradiction was CLOSED by [ADR-075](../decisions/ADR-075.md) six days earlier, in the very session the brief cites as having reported it, and what remains is a stale restatement inside [M20](../plans/M20-wallet.md)'s own open-question section. **The contradiction P5 DID meet is [ADR-124](../decisions/ADR-124.md)'s**: the automatic global ledger halt has no mechanism, `0016` rules it never will be a row, and `RB-07` tells an operator to confirm a stop nothing performs. Two P5 contents are in no phase cell, both money path. **32 of 32 gates, `verify` exit 0. No code, no migration, nothing signed** -->

- **Objective:** write [P5](../plans/P5-payouts-and-wallet.md) against the tree rather than against [M05](../plans/M05-payout-system.md) and [M20](../plans/M20-wallet.md), with a slice table a dispatch loop can read the way [P3 section 9](../plans/P3-ledger-billing-identity.md) and [P4 section 8](../plans/P4-portal-and-site.md) can be read.
- **Done:** **[P5-payouts-and-wallet](../plans/P5-payouts-and-wallet.md)**, twelve sections, **fourteen slices over four waves** with a fence BY FILE, a money column and a depends-on BY FILE on every row, plus a per-file collision table against the thirteen sessions running beside this one. **[ADR-124](../decisions/ADR-124.md), `status: proposed`, UNSIGNED, four ruled clauses and one explicit refusal.** [ALLOCATION](../decisions/ALLOCATION.md) row 124 amended in place per [ADR-065](../decisions/ADR-065.md) T3, [INDEX](../INDEX.md) row added, [STATE](../STATE.md) appended. **32 of 32 gates, `pnpm run verify` exit 0, 150 test files / 2,656 passed / 1 skipped**, measured after `pnpm install` because `verify` cannot typecheck without `node_modules`.
- **Next:** the allocation commit section 7 requires, which is **six ADR numbers from `138` and two migration numbers that are not `0048`**, and then `P5-a`, `P5-c` and `P5-d`, all three of which can start immediately and share no file.
- **Blockers:** **`P5-a` is the head of the phase and every other slice is behind it.** Section 10's items 1 and 2 gate the dispatch: [ADR-124](../decisions/ADR-124.md)'s storage and clock block `P5-k`, and P5's whole done-condition sits behind `ADR-125` (session 237), which is running now.
- **Landmines:** **the reach-around is one line and it is a deadline rather than a preference.** Every money route in waves 2 and 3 needs a predicate the accessor does not have, and `SqlExecutorReason` is one member away from being the door every future predicate goes through. Section 11 rule 10 exists for that and it is P5's own rule. **[ADR-124](../decisions/ADR-124.md) refuses to design the storage and the clock question INVERTS [ADR-040](../decisions/ADR-040.md)'s** every-hold-expires reasoning: a global halt that releases itself resumes paying out of a book nobody explained.
- **Files touched:** [P5-payouts-and-wallet](../plans/P5-payouts-and-wallet.md), [ADR-124](../decisions/ADR-124.md), [ALLOCATION](../decisions/ALLOCATION.md), [INDEX](../INDEX.md), [STATE](../STATE.md), [sessions/README](README.md), the generated spans, this file.

---

### 1. The tree, measured, and the two figures that decide the phase

| Measurement | Result |
|---|---|
| Gates, `pnpm run verify` | **32 of 32**, **exit 0**, 150 files / 2,656 passed / 1 skipped |
| Migrations | 47. **`0048` is [session 240](README.md)'s** |
| Tables created against tables transcribed | **111 against 91.** P4 measured 7 of 111 |
| Route modules under [`apps/api/src/routes/`](../../apps/api/src/routes/health.ts) | **8** |
| **What a filter may say** | equality, ANDed. **No `OR`, no `IN`, no range, no `IS NULL`** ([`scoped-db.ts:504`](../../packages/db/src/scoped-db.ts)) |
| **What raw SQL may be run for** | **ONE reason**, `'job-enqueue'` ([`scoped-db.ts:1007`](../../packages/db/src/scoped-db.ts)) |
| Wallet endpoints in [API_CONTRACT](../architecture/API_CONTRACT.md) | **ZERO**, over four mentions of the word |
| `wallet.*` rows in [EVENTS](../architecture/EVENTS.md) | **2**, against eighteen P5 emits |
| P5's golden scenarios | `GS-106` to `GS-111`, `GS-222` to `GS-231`: **16 rows, 16 blocked** |

**The two that decide it are the last four.** The accessor decides whether P5 has a first slice; the contract and the catalogue decide whether it has a second and a third that can run beside it.

---

### 2. THE BRIEF'S CENTRAL PREMISE, checked at source

The dispatch names one contradiction and reserves this session's ADR against it:

> *"`INV-M20-06` blocks on `identities.status = 'restricted'` while the payout gate after `ADR-062` reads `= 'active'` ... This was reported by session 95 and named as an open question, never fixed."*

**[M20:62](../plans/M20-wallet.md) reads `identities.status = 'active'` and carries its own correction note.** [ADR-075](../decisions/ADR-075.md) is `status: accepted`, moved `INV-M20-06` and `G-WITHDRAWAL-CLEARED` in one change, and its decision line closes `OQ-062-01` and its `OQ-M20-07` restatement. **Its founder approval was granted in session 95**, which is the session the brief cites as having reported the problem rather than as having closed it.

**Two things are genuinely left and neither is the one named.** [M20](../plans/M20-wallet.md)'s own `OQ-M20-07` body is **stale**, still asking the closed question in the present tense with two of its five door-inventory cells carrying the pre-`ADR-075` shape; and `OQ-075-01` is open on the two COMMERCE doors, which [ADR-075](../decisions/ADR-075.md) section 3 refused to move on a stated argument rather than by omission.

**The check cost one grep and it changes the session's whole output**, because an ADR written to rule a closed question is an entry that re-opens it.

---

### 3. The contradiction that WAS met

Four documents, each correct about its own half, and no session had assembled them:

| Document | What it says |
|---|---|
| [EVENTS:263](../architecture/EVENTS.md) | `ledger.invariant_violated` is *"the one event whose consumer is allowed to change system behavior automatically"*, and the payload carries a `global` scope |
| [`0016:55`](../../packages/db/migrations/0016_treasury_controls.sql) | `ledger_halts.identity_id` is `uuid NOT NULL` because *"a halt with no subject is the global halt and the global halt is not a row, it is an incident"* |
| [`halts.ts:109`](../../packages/ledger/src/halts.ts) | *"A FIRM-ONLY POSTING IS NEVER REFUSED BY THIS"*, and `LT-02` and `LT-07` are firm-only postings |
| [RB-07:31](../ops/runbooks/RB-07-ledger-imbalance.md) | *"Every payout is stopped. **Confirm it**, including the internal wallet-credit leg"* |

**So an approved architecture document's automatic behaviour has no mechanism, a merged migration rules that it never will be a row, and a runbook instructs an operator to verify an act nothing performs.** [ADR-124](../decisions/ADR-124.md) rules the shape in four clauses and **refuses the storage, the clock and the authority**, which are a migration and are not a planning session's to take.

---

### 4. What made the slice table possible, and it was not this session

**Three rulings landed in the eight days before this session and each removed a serialization P5 would otherwise have inherited.**

| Ruling | What it removes from P5 |
|---|---|
| [ADR-092](../decisions/ADR-092.md) | The transcription is owned by the TABLE and queued by the TYPE CHECKER, so P5's three table sets are concurrent with each other and with any other session's disjoint set. **P4's largest collision is not P5's** |
| [ADR-100](../decisions/ADR-100.md) | The route module list **is the directory listing**, so five P5 route slices add five disjoint files and merge in any order, and `compose` refuses the one collision they actually make |
| [ADR-112](../decisions/ADR-112.md) | The accessor can name ONE ROW, and it also gained **`rowsWhere`**, which the dispatch summary does not name. That makes [`chart.ts`](../../packages/ledger/src/chart.ts)'s header stale in the direction that under-states the tree: it reasons from *"there is no read that carries a caller's predicate"* and calls the remedy *"named and not built"*. It is built |

**What none of them removed is the lock**, and that is the whole of section 3 of the plan.
