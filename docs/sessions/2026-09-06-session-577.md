---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 577: the expiry constructor refused a third time and the refusal made a STANDING BAR, where the ground four registers gave for it is false and the guard holding it was one integer

<!--index: **THE ROW IS [ADR-382](../decisions/ADR-382.md), `proposed` and UNSIGNED. MONEY-ADJACENT, ADR-003 STRICT, `E2` READ OWED.** [ADR-378](../decisions/ADR-378.md) refused the expiry constructor and named its hazard, and the dispatch asked for that to be settled rather than declined a fourth time. **IT IS SETTLED: THE HAZARD IS NOT ANSWERABLE FROM THIS DEPLOYABLE AND THE REFUSAL IS RECORDED AS A STANDING BAR WITH TWO NAMED EXITS**, `P5-n`'s producer or the nightly hold and freeze expiry assertion, which is **S1, unsuppressible, and carries no `BUILT` marker** on a page where ten rows do. **AND THE GROUND FOUR REGISTERS GIVE FOR THE BAR IS FALSE, WHICH IS THIS ROW'S MOST VALUABLE OUTPUT.** All four say nothing in this tree can be passed for the sink and three draw the consequence that the blocker is a call which does not compile. **`UNWIRED_EXPIRY_SWEEP_IO` COMPOSES A REFUSING `ExpiryEventPort` INLINE** at [`sweeps/ports.ts:366`](../../apps/worker/src/sweeps/ports.ts), inside the io's own unwired default, so a value of that type exists, is exported with the value holding it, and **is passable**. **SEED A GAVE `events` THAT DEFAULT AND `typecheck` STAYED GREEN ACROSS EVERY WORKSPACE.** **SO THE REPAIR IS ONE EDIT AND NOT TWO**, since nobody needs to write the value [ADR-378](../decisions/ADR-378.md)'s seed 6 writes, **and NEITHER PORT CENSUS SEES IT**: seed A2 writes the default inline naming nothing, leaves `test/schedule.test.ts` **GREEN at 19 of 19** including `6.1` and `7.2`, and reddens **exactly one assertion in the repository**, the arity pin at `test/expiry-adapter.test.ts:294`. **A MONEY-PATH BAR WAS RESTING ON ONE INTEGER.** Seed A3 takes the sibling-constructor path instead and is caught by that file's caller census, so the bar is now held by two assertions plus a census and its stated reason is true for the first time in four entries. **THE VERDICT DOES NOT MOVE AND ITS GROUND NARROWS**: the refusal stands on what the sweep would DO, measured sharper here in two places. **Each leg of [`sweeps/expiry.ts`](../../apps/worker/src/sweeps/expiry.ts) CATCHES its own error and reports the row `failed`**, so a sweep over a rejecting sink completes, returns a report and throws nothing, which is `ADR-239`'s defect with a clock in front of it plus the reason the report never carries the failure out. **And the two doors down that DID take this edit differ in what the job withholds**: [`breaker/adapter.ts:333`](../../apps/worker/src/breaker/adapter.ts) and [`detectors/adapter.ts:334`](../../apps/worker/src/detectors/adapter.ts) default their sink to a refusal, but a detector run that records nothing withholds nothing from a trader, while an expiry sweep that releases nothing converts a bounded hold into an unbounded one, which is [ADR-040](../decisions/ADR-040.md)'s denial nobody authorized. **[ADR-370](../decisions/ADR-370.md) SECTION 9's SECOND PREDICTION HAS COME TRUE AND TWO ROWS PASSED OVER IT.** That paragraph offers a second inline composition as a hypothetical; it is this very port, and **case `6.1` names the caveat for `RuleStateWriterIo` three assertions before committing the same error for `ExpiryEventPort`** in one case, in the file written because a count in prose is asserted by nothing. Bound in case `9.1`. **THE DISPATCH'S STALE-CLAIM-PATTERN PREMISE IS REFUTED IN THE USEFUL DIRECTION**: the report-lag class does NOT recur in `apps/worker`, and the live sites are asserted where they could break, `recon/adapter.ts`'s at [`test/recon-adapter.test.ts:417`](../../apps/worker/test/recon-adapter.test.ts). **What IS a pattern, at SIX sites, is a neighbouring class**, one precise claim degraded by restatement into a false absolute and then into a false consequence, which is [ADR-034](../decisions/ADR-034.md)'s mechanism on a claim rather than on a count. **THE CLAUSE IS REPAIRED AT THREE OF FIVE SITES UNDER `RI-14`, NAMED RATHER THAN REPRODUCED, AND THE TWO LEFT ARE NAMED WITH THEIR PRICE**: 30 citations sit below [`index.ts:596`](../../apps/worker/src/index.ts) and 8 point at [`expiry-adapter.ts:193`](../../apps/worker/src/sweeps/expiry-adapter.ts), so this row paid for meaning in two unrepaired sites where [ADR-378](../decisions/ADR-378.md) paid for neutrality in a dropped clause. **THREE SEEDS, EVERY ONE OBSERVED AND EVERY FILE RESTORED BYTE-IDENTICAL under `sha256sum -c`** against a seven-file manifest re-verified after each restore, and **no restore was done with `git checkout --`**. **FIVE CITATIONS THIS DIFF MOVED ARE REPAIRED, line numbers only, derived by reading at both ends.** **[ADR-370](../decisions/ADR-370.md) SECTION 3.1's TWO CELLS ARE CONFIRMED UNCORRECTED FOR THE FOURTH CONSECUTIVE ROW**, and its section 9 text is owed a second correction from here. **THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL**, the eleventh consecutive session to record it. **NO `mcp__github__*` TOOLS, NO `gh` ON PATH, `api.github.com/repos/lam2-coder/meritfutures` ANSWERS 403 while `git push` AUTHENTICATES**; the branch is pushed and **no pull request was opened and nothing was merged, by instruction**. **No job scheduled, no port wired, no disposition moved, no schedule parameter invented, no gate weakened, no test skipped, disabled or quarantined, no invariant minted, no `CI-06` letter spent, no migration taken or reserved, and `apps/api/**` and `packages/tooling/checks/**` never opened for writing.** **`380` and `381` are RESERVED** so `CI-06f` stays gapless with two siblings running now, naming no subject. Suite **336 files / 8,077 passed / 10 skipped / 0 failed** against a base reproduced on `e18ea41f` **BEFORE the first edit** of **336 / 8,074 / 10 / 0**, a delta of **+3 cases in TWO EXISTING FILES entirely by ADDITION with zero removals**, files unmoved at 336 and skips unmoved at 10, attributed BY REMOVAL; gates **33 of 33** after `generate`, invariants **35 of 35**, typecheck, lint and `format:check` clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `382`: settle whether [ADR-378](../decisions/ADR-378.md)'s named hazard on the expiry constructor can be answered or must become a standing bar, act on the real shape of [ADR-370](../decisions/ADR-370.md) section 9's hole, sweep `apps/worker` for the stale-claim-inside-a-repair class, and take everything section 8 records owed that this fence reaches.

**One objective.** It resolved into one ruling, one refutation of the ruling's own stated ground, three cases, one retired clause repaired at three sites, three seeds and five citations moved and repaired.

---

### 2. What was measured before the first edit

**THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL.** `/home/user` was empty, with no clone and no `node_modules`. This is the **eleventh consecutive session** to record it. The tree was attached to this session's authorized set and cloned by hand, and `HEAD` was confirmed to be the `e18ea41f` the row names rather than assumed.

**Base, on a clean tree, before anything was touched: 336 files / 8,074 passed / 10 skipped / 0 failed.** Gates **33 of 33** after `generate`, invariants **35 of 35**, typecheck, lint and `format:check` clean. **These are not the figures either predecessor entry reports**, because both measured `6698f9c0` and rows `377` through `379` have merged since.

**AND THE CITATION SWEEP DECIDED WHICH FILES COULD BE REPAIRED AT ALL, WHICH IS WHY IT RAN FIRST:**

```
apps/worker/test/schedule.test.ts        ->   0 sites                          edit freely
apps/worker/test/expiry-adapter.test.ts  ->   5 sites, 2 distinct lines         repair them
apps/worker/src/schedule.ts              ->  44 sites, deepest cited line :541  add below :541
docs/ops/runbooks/CRON_INVENTORY.md      ->   1 site at :94                     row 34 is one line
apps/worker/src/sweeps/expiry-adapter.ts ->   8 sites, all at :193              NOT EDITED
apps/worker/src/index.ts                 ->  30 sites BELOW :596, 7 lines       NOT EDITED
```

---

### 3. The items, dispositioned

| # | Item | At primary source | Action |
|---|---|---|---|
| 1 | the hazard [ADR-378](../decisions/ADR-378.md) section 3 names | **REAL, and NOT answerable from this deployable** | **STANDING BAR, two named exits.** [ADR-382](../decisions/ADR-382.md) section 3 |
| 2 | the ground four registers give for the bar | **FALSE.** The sink is passable and the default compiles | **REPAIRED at 3 of 5 sites.** Section 4 |
| 3 | which guard holds the bar | **one `Function.length` assertion**, plus a caller census on the other path | **BOUND AND SAID.** Section 4 |
| 4 | [ADR-370](../decisions/ADR-370.md) section 9's hole | asymmetry confirmed, and its **second** prediction is now a fact | **THE INLINE CASE IS BOUND.** Section 5 |
| 5 | stale-claim-inside-a-repair as a pattern | **NOT a pattern in `apps/worker`** | **SWEPT AND REFUTED.** Section 5 |
| 6 | [ADR-378](../decisions/ADR-378.md) section 8's four owed items | **ALL STILL OUTSTANDING**, re-verified | **OUTSIDE THIS FENCE** |

---

### 4. The refutation, measured in both directions

**THREE SEEDS. EVERY ONE OBSERVED, EVERY FILE RESTORED BYTE-IDENTICAL under `sha256sum -c` against a seven-file manifest re-verified after each restore.**

| seed | edit | typecheck | `schedule.test.ts` | `expiry-adapter.test.ts` |
|---|---|---|---|---|
| **A** | `events` defaults to `UNWIRED_EXPIRY_SWEEP_IO.events` | **GREEN, every workspace** | **GREEN 19 of 19** | RED 2 of 18: arity pin and the name check |
| **A2** | the same default written inline, naming nothing | **GREEN** | **GREEN 19 of 19** | **RED 1 of 18: the arity pin ALONE** |
| **A3** | a sibling constructor instead, arity untouched | **GREEN** | **GREEN 19 of 19** | RED 1 of 18: the caller census |

**SEED A2 IS THE FINDING.** The cheapest repair available to a fourth row is one line, it compiles, both port censuses stay green on it, and the whole of what turns red in this repository is `expirySweepIo.length` at [`test/expiry-adapter.test.ts:294`](../../apps/worker/test/expiry-adapter.test.ts).

**AND SEED B FALSIFIES THE NEW CASE IN THE OTHER DIRECTION**: hoisting the sink out of its inline position and into a named `const` reddens `9.1` alone, which is the case reporting that the value is inline and invisible.

---

### 5. The sweep, and what it actually found

**THE DISPATCH ASKED WHETHER "A REPAIR THAT FIXES A LINE AND LEAVES ITS OWN SUMMARY DESCRIBING THE OLD STATE" HAS MORE INSTANCES IN `apps/worker`. IT WAS SWEPT AND THE ANSWER IS NO.** Every report-lag phrasing in `apps/worker/src` and `apps/worker/test` was enumerated and the live ones checked at the file they name. `index.ts:424` on `state-hash.ts`, `index.ts:1459` on `packages/db/src/scope.ts`, and `index.ts:1582` with `recon/adapter.ts:110` on the caller census all still hold, **and the last of those is asserted at `test/recon-adapter.test.ts:417`, in the one file that could have broken it.**

**WHAT IS A PATTERN, AT SIX SITES, IS A NEIGHBOURING CLASS**, and it is the one this row is about: a precise claim degraded by restatement into a false absolute, then into a false consequence. The six are `sweeps/expiry-adapter.ts` twice, `schedule.ts:688`, `schedule.ts:263` inside the retired [ADR-370](../decisions/ADR-370.md) paragraph, `index.ts:596` and `test/breaker.test.ts:974`.

---

### 6. Blockers and landmines

- **LANDMINE, AND IT IS INSIDE THIS FENCE: two sites carry the false clause and are NOT repaired.** [`index.ts:596`](../../apps/worker/src/index.ts) has 30 citations below it across 7 distinct lines; [`sweeps/expiry-adapter.ts`](../../apps/worker/src/sweeps/expiry-adapter.ts)'s `expirySweepIo` docblock has 8 pointing at `:193`, the declaration itself, with every line of the correction above it. **The second is the site a row about to make the edit actually reads**, which makes it the more valuable and the more expensive.
- **OWED AND OUTSIDE THIS FENCE, CONFIRMED A FOURTH TIME: [`ADR-370.md`](../decisions/ADR-370.md) section 3.1's two cells.** [ADR-371](../decisions/ADR-371.md), [ADR-376](../decisions/ADR-376.md) and [ADR-378](../decisions/ADR-378.md) each named them. **Four consecutive rows have now found an item none of them could reach.**
- **OWED AND OUTSIDE THIS FENCE, SECOND TIME: [`ADR-370.md`](../decisions/ADR-370.md) section 9's own text**, which now owes a second correction: its caution about a second inline composition is a fact rather than a hypothetical.
- **LANDMINE: the three `docs/` citations into `schedule.ts` are still stale and still unreachable.** `ADR-353:24` -> `:309`, `ADR-355:61` -> `:353`, `ADR-361:124` -> `:541`, all re-verified. **Every line this diff adds to that file lands BELOW `:541`, so none of them moved and the citation-repair right does not reach them.** Fourth entry to record it.
- **LANDMINE: two assertions of one fact were created ON PURPOSE**, which [ADR-373](../decisions/ADR-373.md)'s rule normally forbids. `test/expiry-adapter.test.ts` section 6's second case and the arity pin fail on the same edit. **The bar was found resting on one integer**, and the trade is stated in [ADR-382](../decisions/ADR-382.md) section 7 rather than hidden.
- **`apps/worker/src/index.ts:450` was re-checked and needs nothing.** No barrel leg was added or removed, so **no barrel-leg count was re-derived**.
- **`CI-06l` WAS CHECKED BECAUSE `CRON_INVENTORY` IS IN THIS FENCE AND THE PAGE WAS OPENED FOR WRITING.** Row 34 was edited in place, the file is unchanged at **106 lines**, no `*_expires_at` disposition, coverage row or exemption moved, and the gate **PASSES**.
- **Concurrency: two sibling rows are running now**, one in `apps/api/**` and one in `packages/tooling/checks/**`. **Neither tree was opened for writing.** `380` and `381` are RESERVED naming no subject; a sibling taking either replaces that cell whole.

---

### 7. Verification, derived at reporting time

| runner | result |
|---|---|
| `pnpm vitest run` | **336 files / 8,077 passed / 10 skipped / 0 failed** |
| baseline on `e18ea41f`, before the first edit | **336 files / 8,074 passed / 10 skipped / 0 failed** |
| delta | **+3 cases in TWO EXISTING FILES, entirely by ADDITION with zero removals**, files unmoved at 336 and skips unmoved at 10. Attributed **by removal**: `schedule.test.ts` runs **20** here and **19** on the base, `expiry-adapter.test.ts` runs **20** here and **18** on the base |
| `node scripts/corpus/gates.mjs generate` then `check` | **33 of 33** |
| `node packages/tooling/checks/repo-invariants.mjs` | **35 of 35**, no number minted |
| `pnpm run typecheck` | clean |
| `pnpm run lint` | clean |
| `pnpm run format:check` | clean |
| `git diff --stat origin/main -- packages/db/migrations scripts/db` | **EMPTY** |
| `git diff origin/main -- apps/api packages/tooling` | **EMPTY** |

**`pnpm run verify` was NOT run and `scripts/corpus/falsify.mjs` was NOT run**, both forbidden by the row and both mutate the working tree.

---

### 8. This session's own tooling, measured rather than assumed

```
$ command -v gh                                            -> not found (exit 1)
$ curl -s -o /dev/null -w '%{http_code}' \
    https://api.github.com/repos/lam2-coder/meritfutures   -> 403
```

**No `mcp__github__*` tool exists in this session's surface.** `git push` authenticates through the session's git proxy and did so on every commit. **This matches sessions 554, 558, 562, 564, 565, 568, 571 and 573 exactly.**

**THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL**, the eleventh consecutive session to record it. **Every Bash command was run bare from the working directory**, per the row's operational note, and **no permission stall occurred**.

**THE BRANCH IS `claude/the-constructor-refused-twice`, PUSHED. NO PULL REQUEST WAS OPENED AND NOTHING WAS MERGED, BY INSTRUCTION.**

---

### 9. Next

The founder's `E2` read of [ADR-382](../decisions/ADR-382.md) and the three decisions its approval block names, of which **the first is whether the standing bar's second exit is accepted**: a built nightly estate assertion lifts the bar in this entry's reading, and that is the difference between preventing a silent denial and detecting it within a day. Then **the two sites left wrong for a citation budget**, where two consecutive rows have now paid a different price for the same constraint. Then **the fence, asked for the third wave running**, with [ADR-370](../decisions/ADR-370.md)'s section 3.1 cells now confirmed by four rows and its section 9 owed two corrections.
