---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 630: a skip is green, the number that would say so was bound to nothing, and an exact control for it exists because the run already produces the number

<!--index: **THE ROW IS [ADR-435](../decisions/ADR-435.md), `proposed` and UNSIGNED. NON-MONEY DIFF AND NON-MONEY SUBJECT, `E2` READ OWED. AN EXACT CONTROL WAS POSSIBLE AND IT IS BUILT**: `packages/tooling/checks/skipped-tests.mjs` states the per-file not-run counts in ONE place and compares them against a Vitest JSON report, both directions, refusing rather than under-asserting when the report's own summary and its per-case statuses disagree. **IT RUNS NO SUITE**: `CI-02` and `CI-04` already run one and the report is one extra `--reporter=json` on a command line they already carry. **THE STATIC APPROXIMATION IS REFUSED IN THREE TERMS AND THE THIRD IS FATAL ALONE**: which of the two complementary blocks at `pool-executor.test.ts:79` and `:97` skips depends on `DATABASE_URL` and not on the tree, so a tree-only counter would have to MODEL the environment, which is the intent-encoding [ADR-429](../decisions/ADR-429.md) section 10 forbids. **RED IN BOTH DIRECTIONS ON REAL REPORTS WITH NO EDIT TO THE TREE AT ALL.** **[ADR-429](../decisions/ADR-429.md)'s FIFTH GREP DOES NOT REPRODUCE AND THE CORRECTION MAKES THE FINDING WORSE**: 76 hand-typed lines in `docs/STATE.md` state the number and all 76 read 10 while the suite has reported 11 since [ADR-429](../decisions/ADR-429.md) itself. **NO `CI-06` LETTER SPENT, NO `RI-nn` MINTED, NO GATE WEAKENED, NO `skipIf` SITE TOUCHED, NO MIGRATION.** `golden` is left unbound and the hole is stated. -->

### 1. The objective

Answer whether an EXACT control is possible for the third layer of [ADR-429](../decisions/ADR-429.md) section 10 -- a skip is green, and the number that distinguishes a skip from a pass is bound to nothing -- build it only if it is exact, and ship a refusal with the measurement if it is not. **The dispatch expected the refusal roughly half the time.** It was not needed.

### 2. Done

- **The container measured and reported.** No repository on arrival, cloned out of this row's budget, `pnpm install --frozen-lockfile` 8s. **`git push` refused `403`** until `add_repo` was called with `access: "push"`, as the last five rows recorded.
- **The base reproduced at `f2398953` before the first edit**: gates **33 of 33**, invariants **35 of 35**, suite **341 files / 8,290 passed / 11 skipped / 0 failed**.
- **[ADR-429](../decisions/ADR-429.md) section 10's five probes re-derived at source rather than taken.** Four reproduce exactly. **The fifth does not**, and the correction makes the finding worse: `grep -rn "10 skipped"` returns nothing over `scripts/`, `packages/tooling/` and `.github/` as reported, and **76 lines** over `docs/STATE.md`, every one a hand-typed figure in a session's prose, **all 76 reading 10 against a suite that has reported 11 since [ADR-429](../decisions/ADR-429.md) itself**.
- **The eleven skips derived BY NAME rather than by subtraction**, and the projects proved to partition the suite exactly (8,243 + 2 + 56 = 8,301), which is what makes a per-job wiring sound.
- **The exactness question answered and the answer written down as a derivation.** It is possible, and the reason is one sentence: the run already counts the skips. The static version is refused in three terms, the third fatal alone.
- **[`packages/tooling/checks/skipped-tests.mjs`](../../packages/tooling/checks/skipped-tests.mjs) written**, a per-file register and a comparison against a Vitest JSON report, with three refusals that turn a silent pass into a red.
- **Red demonstrated in BOTH directions on REAL reports, editing nothing.** `DATABASE_URL` takes the count down and reproduces [ADR-429](../decisions/ADR-429.md) section 8's 9-to-8-plus-1 exchange without being told to look for it; a `-t` filter takes it up on a run Vitest reports as `success: true` with zero failures.
- **[`packages/tooling/test/skipped-tests.test.ts`](../../packages/tooling/test/skipped-tests.test.ts) written**, 17 cases, none of them skipped, holding both directions permanently.
- **[`.github/workflows/ci.yml`](../../.github/workflows/ci.yml) wired**, four lines across `CI-02` and `CI-04`, changing no existing step's pass or fail.
- **The cost priced off the record rather than guessed**: 376 rows in `docs/STATE.md` state a suite figure, the skip count changes three times across them and a fourth time unrecorded, so the control fires about once in ninety rows.
- **The registries landed and `generate` run.** One file outside the hand-edited set was written by `generate` and not by hand: [`docs/STATE.md`](../STATE.md)'s `adr_count` span, one integer.
- **ADR-435 written**, `proposed`, UNSIGNED, `E2` READ OWED.

### 3. Not done, and why

- **`golden` IS NOT BOUND AND THE HOLE IS STATED RATHER THAN IMPLIED CLOSED.** `.github/workflows/golden.yml` is outside this fence. Its 56 cases over 7 files report 0 skipped today, derived rather than assumed. The repair is two lines and belongs to a row that holds that file.
- **THE TEMPTING WAY TO CLOSE IT IS NAMED AND REFUSED.** Making `CI-02` run `golden` too would buy one report at the price of the stage separation [`ci.yml:247-256`](../../.github/workflows/ci.yml) forbids in its own words.
- **NOT ONE `skipIf` SITE WAS ADDED, REMOVED OR ALTERED.** Eleven cases do not run in CI and this row leaves all eleven not running. Binding the count is not closing the hole [ADR-428](../decisions/ADR-428.md) section 10 prices.
- **NO `CI-06` LETTER SPENT AND NO `RI-nn` MINTED**, and the refusal is structural: every gate and every invariant here reads the tree, and this number exists only in a run.
- **`no-warning-comments` REFUSED THE FRAMEWORK'S OWN WORD FOR ONE OF THE TWO NOT-RUN STATUSES AND THE RULE WAS NOT WEAKENED.** The register's field is named `unwritten` instead.
- **`pnpm run verify` and `scripts/corpus/falsify.mjs` were NOT run**, both forbidden by the row.

### 4. Owed

- **A founder read on [ADR-435](../decisions/ADR-435.md).** The commitment the corpus now carries is that two CI stages have a step that can fail for a reason no `CI-06` letter and no `RI-nn` covers.
- **`golden.yml`'s two lines**, whenever a row holds that file.
- **The wiring [ADR-428](../decisions/ADR-428.md) section 10 and [ADR-429](../decisions/ADR-429.md) section 11 both price.** This row makes the day it lands loud rather than silent, and does not bring it closer.

### 5. Landmines

- **A SKIP IS GREEN AND A FILTERED RUN IS GREENER.** Measured here: `pnpm exec vitest run --project unit <file> -t <name>` reports `success: true`, `numFailedTests: 0`, 1 passed and 6 skipped. A committed `.only` produces the same shape. Nothing in this repository went red on it before this row.
- **THE TWO BLOCKS IN `pool-executor.test.ts` ARE COMPLEMENTS.** Skips do not fall to zero when a database appears; the count moves 4 to 1 because [`:79`](../../packages/db/test/pool-executor.test.ts) switches ON. A row reporting otherwise is reporting the wrong thing.
- **`--reporter=json` ALONE REPLACES THE REPORTER** and takes the human-readable run out of the log. `--reporter=default` is named beside it in both workflow steps for that reason, verified rather than assumed.
- **THE WORD `todo` IS AN ESLint ERROR IN ANY COMMENT IN THIS REPOSITORY**, `no-warning-comments` at [`eslint.base.js:53-56`](../../packages/tooling/eslint.base.js), matched anywhere and on a word boundary. `numTodoTests` passes; `` `.todo` `` does not.

### 6. Files touched

- `packages/tooling/checks/skipped-tests.mjs` (new)
- `packages/tooling/test/skipped-tests.test.ts` (new)
- `.github/workflows/ci.yml`
- `docs/decisions/ADR-435.md` (new)
- `docs/decisions/ALLOCATION.md` (row 435 cell only)
- `docs/decisions/README.md`, `docs/sessions/README.md` (generated spans)
- `docs/sessions/2026-09-07-session-630.md` (new)
- `docs/STATE.md` (`gates.mjs generate` output only, no prose)
