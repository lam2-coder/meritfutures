---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 586: five findings nobody's fence covered, four repaired, one published repair refused, and the fan-in runner that priced every edit before it was written

<!--index: **THE ROW IS [ADR-391](../decisions/ADR-391.md), `proposed` and UNSIGNED. MONEY-ADJACENT, ADR-003 STRICT, `E2` READ OWED.** [ADR-381](../decisions/ADR-381.md) found that a finding nobody's fence covers is a finding that does not get fixed; this row was fenced to break that on five items and it broke it on four. **[ADR-370](../decisions/ADR-370.md) IS REPAIRED AT THE SEVENTH ASKING**, its line 84, both section 3.1 cells and the section 3 twelfth-port cell the dispatch did not name, after [ADR-371](../decisions/ADR-371.md), [ADR-376](../decisions/ADR-376.md), [ADR-378](../decisions/ADR-378.md), [ADR-382](../decisions/ADR-382.md), [ADR-385](../decisions/ADR-385.md) and [ADR-386](../decisions/ADR-386.md) each re-derived the defect and each recorded the file was in nobody's fence. Six confirmations bought one repair. **THE PUBLISHED REPAIR FOR `packages/db` IS REFUSED AND THE REPLACEMENT SCOPE IS REFUSED TOO.** [ADR-385](../decisions/ADR-385.md) section 9 item 1 has advertised *add the two words `at runtime`* for three waves as its cheapest and clearest item; [`date-column-timezone.test.ts:55`](../../packages/db/test/date-column-timezone.test.ts) imports `pg` as a VALUE, so the qualified sentence is false where the unqualified one is, which [ADR-387](../decisions/ADR-387.md) found and had no fence to act on. **AND `packages/db/src`, the scope that entry offered instead, is not true either**, because [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) imports `pg` there. Derived: **NINE files import `pg`, all nine under `packages/db/**`, TWO of them as a value**; what `merit/no-raw-db-client` actually holds, with `packages/db/**` as its single `ignores` entry, is that nothing OUTSIDE that package imports it at all, which the header states correctly four lines below the absolute that overstates it. **Both sites are marked in place and neither is given the offered words.** **THREE MORE FALSE CLAUSES REPAIRED**: [`apps/admin/package.json`](../../apps/admin/package.json), where TWO files import `@playwright/test` and the fifth consecutive statement of that clause outlived the copy [ADR-385](../decisions/ADR-385.md) fixed one file over; [`pg-boss-queue.ts`](../../packages/queue/src/pg-boss-queue.ts), where **37 git-tracked files outside `docs/` and 69 inside NAME `pg-boss` against ONE importer** and the guard asserts IMPORTS; and the two scope words in [`lt-07.test.ts`](../../packages/rail/test/lt-07.test.ts) and [`rule-state-producibility.test.ts`](../../apps/api/test/rule-state-producibility.test.ts). **BOTH OF THOSE LAST TWO ARE FALSE AND NOT MERELY MIS-SCOPED, WHICH IS [ADR-387](../decisions/ADR-387.md)'s VERDICT AND NOT THE DISPATCH'S DESCRIPTION**: six files under `scripts/db` carry an uncommented `INSERT INTO rule_states` and four test files write a literal negative `amountCents`, both figures reproducing exactly. This row's own first reading of both was the weaker one and the derivation corrected it. **TWO OF [ADR-387](../decisions/ADR-387.md)'s FIGURES DO NOT REPRODUCE**: its *67* documents naming `pg-boss` is **69** here, and its *37 non-documentary* is **35**, two of the 37 being `MERIT_BUILD_MASTER_PROMPT.md` and `packages/db/DELTA_MANIFEST.md`. **[ADR-385](../decisions/ADR-385.md) IS CORRECTED IN ITSELF AND AT THE SITE IT LEFT WRONG.** Re-derived from `git show --numstat 215e44fc`: its section 4 sentence names five source files where the diff edits **SEVEN**, and **two are not neutral**, [`playwright.config.ts`](../../playwright.config.ts) at 8 against 4 and [`apps/portal/test/kyc-source.test.ts`](../../apps/portal/test/kyc-source.test.ts) at 7 against 6, so it grew by five lines while stating it had not. Its section 11 *three of six* is **four clauses across three edits**, two of the three named sitting in ONE repair and the third outside the tabled six. **The unrecorded fourth is RESTORED at its own site**, `index.ts:592-597`, in the same six lines, because a drop recorded in a document and absent from the file it happened in is a drop that file's reader cannot see. **[ADR-386](../decisions/ADR-386.md) SECTION 5 REPRODUCES FOUR FOR FOUR.** **THE INSTRUMENT ITS SECTION 11 ITEM 1 OWES IS BUILT**: [`scripts/corpus/citation-fan-in.mjs`](../../scripts/corpus/citation-fan-in.mjs), which copies the `checks/` directory to a temporary root at the path it already occupies, appends one export and reads with `citationsIn`, `citationReader` and `citationTargets` themselves rather than a grep, so its numbers are the checks'. **10,602 edges onto 582 files** at the census that priced this diff and **10,747 onto 583** at reporting time with this row's own documents on the tree, against that entry's 9,614 on `8fa66104`; `routes/payouts.ts` is **387** across all three. **IT IS AN INSTRUMENT AND NOT A GATE, BY CONSTRUCTION**: there is no threshold at which a fan-in is wrong, nothing imports it, `gates.mjs` was never opened, and no `RI-` number was minted or reserved. **IT PRICED EVERY EDIT IN THIS DIFF BEFORE THE EDIT WAS WRITTEN.** A single added line at the seven edit points would have moved **113 pointers, 9 live and 104 dated**, and the same points price at **128** once this row's own documents cite them; **ZERO moved**, every source hunk being an in-place replacement at the same line numbers, which `git diff -U0` shows as `@@ -17 +17 @@`, `@@ -155 +155 @@`, `@@ -595,3 +595,3 @@`, `@@ -8,2 +8,2 @@`, `@@ -4219 +4219 @@`, `@@ -5,5 +5,5 @@`, `@@ -125 +125 @@` and `@@ -663 +663 @@`. **WHAT THE PRICE COST IS STATED AND NOT HIDDEN**: three corrections say *see ADR-391* rather than carrying the derivation, and one paragraph separator was spent at `client.ts:9`. **NO CLAUSE WAS DROPPED ANYWHERE IN THIS DIFF.** **A BLIND SPOT IS NAMED AND NOT CLOSED**: `ADR-370 line 84` is a positional reference in **six** documents, `RI-15` and `RI-16` cannot see it because it is not their grammar, and the new runner inherits that blindness exactly because it borrows their reader; every correction to that document is therefore written inside the line it corrects. **THE UNSIGNED RANGE IS DERIVED RATHER THAN CARRIED**: the phrase two entries repeat reads as though `358` and `359` were settled, and **274 of 374 entries are `proposed` with the run from [ADR-112](../decisions/ADR-112.md) to [ADR-388](../decisions/ADR-388.md) unbroken**, the last non-`proposed` being [ADR-111](../decisions/ADR-111.md). **NOT ONE EXECUTABLE LINE CHANGED.** **The standing bar is UNTOUCHED and this is the fifth consecutive row to leave it standing**: no port wired, no job scheduled, no sink passed, no default parameter added to `expirySweepIo`, no schedule parameter invented, no gate weakened, no test skipped, disabled or quarantined, no `CI-06` letter spent, no migration taken or reserved, and `packages/tooling/**`, `gates.mjs`, `corpus.yml` and `docs/ops/runbooks/**` were never opened for writing. **NO SEED WAS PLANTED**, a comments-only diff having nothing to watch fail. **`389` and `390` are RESERVED naming no subject, no branch and no link**, so `CI-06f` stays gapless with two siblings running. **THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL**, and the ordinal usually attached to that is itself a carried figure: **27 logs carry the exact phrase**, two of them both call themselves the thirteenth, and the run has gaps at 563, 572, 579 and 583, so this is the 28th log to record it and claims no ordinal beyond that. Suite **336 files / 8,107 passed / 10 skipped / 0 failed** against a base of **336 / 8,107 / 10 / 0** reproduced on `3b664fcf` **BEFORE the first edit**, **delta ZERO on every axis**, which is the expected result for a comments-only diff and is the attribution; gates **33 of 33** after `generate`, invariants **35 of 35**, typecheck, lint and `format:check` clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. The branch is pushed and **no pull request was opened and nothing was merged, by instruction**. -->

---

### 1. The objective

Row `391`: repair the findings several rows had each named and none had been given the fence to reach, build the citation fan-in runner [ADR-386](../decisions/ADR-386.md) section 11 item 1 owes, and derive every handed-down figure at primary source rather than carrying it.

**One objective.** It resolved into four repairs, one refusal, two corrections to [ADR-385](../decisions/ADR-385.md), one restored clause and one new instrument.

---

### 2. What was measured before the first edit

**The container arrived with no repository at all.** `/home/user` was empty, the repository was attached to this session's authorized set and cloned by hand, and `HEAD` was confirmed to be `3b664fcf` rather than assumed.

**Base, on a clean tree:** 336 files / 8,107 passed / 10 skipped / 0 failed. Gates **33 of 33**, invariants **35 of 35**, `typecheck`, `lint` and `format:check` clean.

**And the fan-in census was run before the first edit**, from the runner written first, so every edit in this diff was priced rather than checked afterwards.

---

### 3. Done

1. **[ADR-370](../decisions/ADR-370.md) repaired at the seventh asking**, four cells and one paragraph, every one written inside the line it corrects so nothing above line 84 moved.
2. **The `packages/db` repair refused**, with the measurement, and both sites marked in place at zero citation cost.
3. **Three false clauses repaired** in `apps/admin/package.json`, `pg-boss-queue.ts`, `lt-07.test.ts` and `rule-state-producibility.test.ts`, all in place.
4. **[ADR-385](../decisions/ADR-385.md) corrected in itself**, section 4's `numstat` sentence and section 11's *three of six*, both kept beside their corrections.
5. **The clause it dropped unrecorded restored** at `index.ts:592-597`, in the same six lines the citation graph allows that block.
6. **[`scripts/corpus/citation-fan-in.mjs`](../../scripts/corpus/citation-fan-in.mjs) written**, an instrument and not a gate.

---

### 4. Next

The founder's `E2` read of [ADR-391](../decisions/ADR-391.md) and the three decisions its approval block names, of which the first is whether the DISPATCH is now the binding constraint on this build: six rows found one defect and none could repair it, and a wrong repair stood published for three waves because the file was never handed to anybody who could test it.

---

### 5. Blockers

None taken. The standing bar on `expirySweepIo` is untouched and this is the fifth consecutive row to leave it standing.

---

### 6. Landmines

- **`ADR-370 line 84` is a positional reference in six documents and no control can see it.** The new runner cannot either, because it borrows the checks' grammar on purpose. Any row editing that document above line 84 breaks six pointers silently.
- **`index.ts:592-597` is a six-line block with 19 citations inside it and a range citation onto the span.** It cannot grow. Line 594 must keep `apps/api/src/events.ts` on it, because two entries cite that line for that path.
- **The runner's copy depends on two constants that `repo-invariants.mjs` derives from its own location.** A third such constant would make the copy quietly disagree with the checks.
- **[ADR-376](../decisions/ADR-376.md) line 92 is the same shape as the item this row was dispatched for**, still in nobody's fence, and `ADR-376 line 92` is a positional reference in four documents.

---

### 7. Files touched

| File | What |
|---|---|
| [`scripts/corpus/citation-fan-in.mjs`](../../scripts/corpus/citation-fan-in.mjs) | new. The fan-in instrument, registered in no gate table |
| [`apps/worker/src/index.ts`](../../apps/worker/src/index.ts) | the dropped clause restored, 3 lines for 3, inside a block that cannot grow |
| [`apps/admin/package.json`](../../apps/admin/package.json), [`packages/db/src/client.ts`](../../packages/db/src/client.ts), [`packages/db/src/scoped-db.ts`](../../packages/db/src/scoped-db.ts), [`packages/queue/src/pg-boss-queue.ts`](../../packages/queue/src/pg-boss-queue.ts), [`packages/rail/test/lt-07.test.ts`](../../packages/rail/test/lt-07.test.ts), [`apps/api/test/rule-state-producibility.test.ts`](../../apps/api/test/rule-state-producibility.test.ts) | false or mis-scoped clauses marked in place, every hunk an equal-length replacement |
| [`docs/decisions/ADR-370.md`](../decisions/ADR-370.md) | four cells corrected in place, one paragraph appended below line 199 |
| [`docs/decisions/ADR-385.md`](../decisions/ADR-385.md) | sections 4 and 11 corrected, both kept beside their corrections |
| [`docs/decisions/ADR-391.md`](../decisions/ADR-391.md) | new |
| [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) | row `391` TAKEN, rows `389` and `390` RESERVED naming no subject, no branch and no link |
| [`docs/decisions/README.md`](../decisions/README.md), [`docs/sessions/README.md`](README.md) | generated spans, written by `gates.mjs generate` |
| [`docs/STATE.md`](../STATE.md) | one appended session section |
| this file | new |

**NOT TOUCHED, AND EACH WAS READ:** [`packages/tooling/checks/repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) (read, and copied at run time by the new runner, which writes nothing), [`scripts/corpus/gates.mjs`](../../scripts/corpus/gates.mjs), `.github/workflows/corpus.yml`, `docs/ops/runbooks/**`, [`apps/worker/src/schedule.ts`](../../apps/worker/src/schedule.ts), [`apps/worker/src/sweeps/expiry-adapter.ts`](../../apps/worker/src/sweeps/expiry-adapter.ts), [`apps/worker/test/expiry-adapter.test.ts`](../../apps/worker/test/expiry-adapter.test.ts), [`apps/worker/test/schedule.test.ts`](../../apps/worker/test/schedule.test.ts), [ADR-352](../decisions/ADR-352.md), [ADR-312](../decisions/ADR-312.md), [ADR-275](../decisions/ADR-275.md) and the session 466 log.

---

### 8. Verification

Every figure derived at reporting time off each runner's own last line.

- **Suite: 336 files / 8,107 passed / 10 skipped / 0 failed**, against a base of **336 / 8,107 / 10 / 0** reproduced on `3b664fcf` before the first edit. **Delta ZERO on every axis.**
- Gates **33 of 33** after `generate`. Invariants **35 of 35**. `typecheck`, `lint` and `format:check` clean.
- **`pnpm run verify` and `scripts/corpus/falsify.mjs` NOT run**, both forbidden by this row.
- **Citations checked by reading them.** The hunk list in [ADR-391](../decisions/ADR-391.md) section 8 is derived from the diff and not from a green invariant run.
- **No seed planted**, and no seed to restore: the diff changes no executable line, and the runner's only scratch artifact is created and deleted inside its own process outside the tree.
- **No pull request opened and nothing merged, by instruction.**
