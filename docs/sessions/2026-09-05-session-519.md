---
status: approved
depends_on: [README.md]
last_updated: 2026-09-05
---

## 2026-09-05 - Session 519: a sentence that outlived the tree. Four occurrences, three hand repairs, and one register that binds a claim to the artifact it names

<!--index: **THE BUILD IS [ADR-328](../decisions/ADR-328.md), `proposed` and UNSIGNED, and `RI-35` in [`absence-claims.mjs`](../../packages/tooling/checks/absence-claims.mjs). NOT THE MONEY PATH AND NOT A MIGRATION**: `git diff --stat` against `packages/db` and `scripts/db` is EMPTY. **`E2` READ OWED ANYWAY**, on [ADR-324](../decisions/ADR-324.md)'s own unresolved question, because one of the fifteen registered claims is the refusal `GET /affiliate/stats` serves; five reads are inherited. **THE DEFECT IS A SHIPPED SENTENCE SAYING SOMETHING DOES NOT EXIST, FALSIFIED BY A LATER ROW, WITH NOTHING GOING RED.** Four occurrences are on record and each was repaired BY HAND: [ADR-324](../decisions/ADR-324.md) on `affiliate.ts` after `0078`, [ADR-326](../decisions/ADR-326.md) on the worker barrel after `0079`, [ADR-327](../decisions/ADR-327.md) on `pg-boss-queue.ts` after the SAME `0079` one row later, and on `probe_pgboss_job_store.sql` after `0082`. **THREE ROWS EACH BUILT ONE DERIVATION FOR ONE SITE AND THIS ROW BUILDS THE ONE THAT SCALES.** `RI-35` reads two WRITTEN registers: **ten artifacts** each with a probe answering `present` or `absent`, and **fifteen claims** across nine sites each binding one VERBATIM line to one artifact, eight `live` and seven `retired`. **SIX LEGS**, of which leg 2 FAILS ON GOOD NEWS -- a `live` claim whose artifact landed is a finding at the sentence that is now lying -- leg 3 catches the defect running backwards, and **leg 6 is the one that scales**: any line in `apps/*/src` or `packages/*/src` naming a registered artifact within TWO lines of a word from the written absence vocabulary must be a registered claim. **A TREE-WIDE GREP WAS MEASURED AND REJECTED**: the four occurrences' own phrasings reach **255 lines** in that scope and most are TRUE, so a gate satisfied by deleting honest prose is a gate somebody deletes. **THE WINDOW OF TWO IS A MEASUREMENT**: at one the sweep misses `schedule.ts`'s provisioning row, at three it reaches a `pg-boss-queue.ts` sentence about a REFUSED ALTERNATIVE that asserts no absence. **THE FOUR OCCURRENCES WERE RECONSTRUCTED FROM `git show` AS THEY STOOD THE DAY THEY WENT STALE AND EACH WAS WATCHED GOING RED**, each with its counterfactual tree watched GREEN beside it; **occurrence 3's case registers only occurrence 2 and reports BOTH**, which is ADR-327's row happening on the day ADR-326 landed. **OCCURRENCE 4 SPLITS AND ONLY ONE HALF IS CATCHABLE**: the absence half is reported, and the *"means granting CREATE"* half is registered UNBINDABLE because measurement rather than an artifact falsified it. **THE FIFTH SITE WAS FOUND WHILE SIZING THE ROW AND IS STALE**: `packages/queue/src/index.ts` said `@merit/db` CANNOT SUPPLY EITHER a transaction or a SQL executor, and it supplies BOTH -- `transaction` at `index.ts:79`, `SqlExecutor` at `:86` -- with the constructor half MEASURED UNUSABLE by [ADR-327](../decisions/ADR-327.md) section 9 finding 1. Repaired with a derivation and registered, one entry of each disposition. **A SIXTH OCCURRENCE WAS FOUND AND NOT REPAIRED** at `schedule.ts:220`. **NOTHING WAS WIRED AND NOTHING WAS SCHEDULED**: the counts do not move at ten wired of twenty-four declared, fourteen blocked. Counts derived at reporting time: suite **308 files / 7,521 passed / 6 skipped / 0 failed**, against an `f54494e9` baseline reproduced BEFORE the first edit of **307 files / 7,489 passed / 6 skipped / 0 failed**; the delta is **+1 file and +32 cases**, thirty-one of them this row's own file and the thirty-second the `test.each(CHECKS)` row that grows with the array. Invariants **33 of 33** where the baseline was 32 of 32; gates **33 of 33** after `generate`; lint, `format:check` and typecheck clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `328`: end a defect with four recorded occurrences by building the control that scales, rather than repairing a fifth sentence by hand.

**One objective.** The build is one check and two registers. **The work is the falsifiability**: a gate that cannot catch the four occurrences that motivated it is decoration.

---

### 2. The base and the baseline, both derived before the first edit

`f54494e9`, PR #681's head, which carries `main` plus [ADR-327](../decisions/ADR-327.md) and `0082`. **The base being ahead of `main` is deliberate and was not "fixed".** `RI-34` was re-derived as the highest live invariant off the runner's own last line, so `RI-35` is free rather than assumed free. Eight dispatch premises were re-derived at their own files and **all eight held**, including the four occurrences' verbatim texts, each read out of `git show` on the commit that repaired it.

**Baseline, off each runner's own last line, before a line was changed:** `pnpm vitest run` **307 files, 7,489 passed, 6 skipped, 0 failed**; `repo-invariants.mjs` **32 of 32**; `git status --porcelain` empty.

---

### 3. What landed

1. **[`packages/tooling/checks/absence-claims.mjs`](../../packages/tooling/checks/absence-claims.mjs)**, new: the two registers and `RI-35`'s six legs. Its own file on `RI-11`'s and `RI-18`'s precedent, with the dependency running ONE WAY so the `CHECKS` literal can import it in any order.
2. **The `RI-35` row in [ALLOCATION](../decisions/ALLOCATION.md)'s invariant register, in the same commit as the check.** `RI-29` reported the missing row before it was written, naming the live title, which is that check doing its job for the fifth wave running.
3. **[`packages/tooling/test/absence-claims.test.ts`](../../packages/tooling/test/absence-claims.test.ts)**, new: 31 cases. Four reconstructions with four counterfactuals, one seed per leg, five sentinels watched throwing, and the live register asserted against this repository.
4. **The fixture estate in [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts)**, so the synthetic tree satisfies `RI-35` too. The claim SITES are derived from the register; what makes each artifact present or absent is a second implementation, on that file's own rule for `RI-22`'s mint.
5. **The fifth site, [`packages/queue/src/index.ts`](../../packages/queue/src/index.ts)**, repaired with a derivation and registered.
6. **One citation repair**, `WAVE-06-admin-console-transport.md:174`, which `RI-16` reported when the new import moved `DEPLOYABLES` by eight lines.

---

### 4. The falsifiability test, which is the whole row

**RECONSTRUCTED FROM `git show`, NOT PARAPHRASED.**

| # | Site | Falsified by | Result |
|---|---|---|---|
| 1 | `apps/api/src/routes/affiliate.ts` | `0078` | **RED**, one finding, leg 2. GREEN on the tree without `0078` |
| 2 | `apps/worker/src/index.ts` | `0079` | **RED**, one finding, leg 2. GREEN without `0079` |
| 3 | `packages/queue/src/pg-boss-queue.ts` | the same `0079` | **RED TWICE**, and the second is the point |
| 4 | `scripts/db/probe_pgboss_job_store.sql` | `0082` | **RED**, one finding, leg 2. GREEN without `0082` |

**OCCURRENCE 3's CASE REGISTERS ONLY OCCURRENCE 2**, which is the register [ADR-326](../decisions/ADR-326.md) would have left. `RI-35` reports leg 2 on the registered sentence AND leg 6 on `pg-boss-queue.ts:2`, unregistered. **That is the row ADR-327 had to spend, reported by a check on the day ADR-326 landed.**

**A CASE BESIDE IT ASSERTS WHY A LINE-SCOPED SWEEP WOULD NOT HAVE SEEN IT**, because that is the design decision most likely to be reverted by somebody tidying: the real line carrying `THAT MIGRATION DOES NOT EXIST YET` contains none of the four needles, and the sweep reports it anyway because the needle is on the line above.

**OCCURRENCE 4 IS THE HONEST LIMIT.** Its absence half is caught. Its *"means granting CREATE"* half is a claim about what `pgboss.create_queue` DOES, falsified by RUNNING pg-boss rather than by an artifact landing, and no probe over this tree reports it. It is registered UNBINDABLE with that reason and the case asserts it stays quiet. **And its site is a `.sql` file under `scripts/db/`, which the sweep never reads**, so that occurrence is caught by the register and not by leg 6.

---

### 5. Landmines, for whoever is next

1. **[`apps/worker/src/schedule.ts:220`](../../apps/worker/src/schedule.ts) IS A SIXTH OCCURRENCE AND IT IS NOT REPAIRED.** The provisioning row's `why` still says *"the header above measures the store as unreachable by the role this deployable connects as"*, and `0082` granted `merit_app` `USAGE` on `pgboss` the day before. The clause `RI-35` registers at that site is a different one and is TRUE; the stale clause is about REACHABILITY rather than about an artifact existing, so no needle in this register reaches it. **A row of its own.**
2. **Two registered claims are not pinned by any leg if their register entry is deleted**: `pg-boss-queue.ts`'s retired quote, whose line carries no needle inside the window, and the `.sql` site, which the sweep does not read. `UNPROBEABLE_ARTIFACTS` in `gates.mjs` has the same property.
3. **`apps/worker/package.json`'s absence claim is deliberately NOT registered.** The synthetic fixture materialises every registered claim site from the register, and a `.json` site would leave a manifest `RI-01` through `RI-08` cannot parse. That claim is already bound by `test/schedule.test.ts` case 5.1.
4. **The sweep does not read `scripts/`**, which is where occurrence 4 lived. Widening it to `scripts/**/*.sql` is the single change that would most improve this check and it was not made.
5. **[ADR-305](../decisions/ADR-305.md) slice 8's citation `worker/src/index.ts:1326-1331` is still stale**, as [ADR-326](../decisions/ADR-326.md) and [ADR-327](../decisions/ADR-327.md) each recorded. **`0080` and `0081` still carry no `DELTA_MANIFEST` section.** **`identity_restriction_episodes.sla_due_at` is still invisible to `CI-06l`.**

---

### 6. Next

The founder's `E2` read of [ADR-328](../decisions/ADR-328.md) and the three questions its approval block names, the first being whether a register somebody has to keep is a control or a decoration, and the second whether the sweep's scope should have included `scripts/`. Then the queue's **ONE DOOR** in `apps/worker/src`, which is the artifact three registered `live` claims are waiting on and which owes [ADR-327](../decisions/ADR-327.md) section 9 finding 1: **the day it lands, `RI-35` turns those three sentences red together**, which is the property this row exists to install.
