## 2026-08-16 - Session 41: the plaintext floor, and a probe that was wired and not pinned (MONEY PATH)

- **Done:** **`0034`'s plaintext-refusal guard**, on the founder's ruling of 2026-08-16 approving the redesign as written and naming the one thing it was missing. `CHECK (octet_length(<col>) >= 29)` on `contact_channels.value_ciphertext`, `identity_phones.phone_ciphertext` and `phone_change_requests.new_phone_ciphertext`, guarded to apply only when the column is not null: twelve bytes of nonce, sixteen of GCM tag, at least one of ciphertext, against an E.164 of at most sixteen bytes. **This is an edit to `0034` and not a superseding migration, because [PR #41](https://github.com/lam2-coder/meritfutures/pull/41) has not merged.** The probe grows from **27 assertions to 37, 37 / 37**, and from eight seeded schemas to **eighteen**, each watched failing on its own finding. `pnpm run verify` exits 0: 6/6 invariants, typecheck, lint, format, 210 tests, **15 of 15 gates**, falsify clean and dirty. The set applies forward-only from empty against PostgreSQL 16.13: 102 tables, 354 indexes, **401 to 404 checks**, 143 foreign keys, 10 triggers. Records folded into [ADR-046](../decisions/ADR-046.md), [DELTA_MANIFEST section 18](../../packages/db/DELTA_MANIFEST.md), the three design records, [STATE](../STATE.md) and [INDEX](../INDEX.md).
- **Next:** **The founder's E2 read on `0034`**, which is what this branch is now waiting on and the only thing between it and a merge. Then the two things `0034` names and does not build: the forward-only backfill, and a key manager as a launch dependency. The gate named under landmines (`probe_*.sql` wired implies pinned) needs a `CI-06` letter and a session of its own.
- **Blockers:** None. The founder's read is a gate, not a blocker.
- **Landmines:**
  - **`OI-07` HAPPENED A THIRD TIME AND WAS CAUGHT BEFORE THE MERGE THIS TIME.** [`probe_reversible_contact_addresses.sql`](../../scripts/db/probe_reversible_contact_addresses.sql) was wired into `corpus.yml` in the commit that created it and **never added to `CI-06h`'s required-needle list**, which is the identical omission `probe_payout_hold.sql` made and which left that file one `git rm` from being `OI-07` again for a day. It is pinned now, and the pin was watched failing dirty: deleting the workflow step makes `CI-06h` report the probe is no longer run. **Wiring a probe and pinning it are two edits in two files, and only the second makes the first permanent.** Three occurrences is a pattern, and the shape of the fix is a gate reading `corpus.yml` and asserting that every `scripts/db/probe_*.sql` on disk is both run and pinned. **No gate is claimed here**, because a gate arrives with a `CI-06` letter and a seeded violation of its own.
  - **THE PROBE'S OWN STATED DEFENCE HAD A HOLE IN IT, ONE TABLE OVER FROM WHERE IT WAS LOOKING.** The file leads with success cases precisely because an inventory of refusals passes perfectly against a guard that rejects everything. **On `identity_phones` there was no success case at all**: `R3` and `R3a` are both rejections and nothing ever sealed an identity phone successfully, so a seeded `CHECK` reading `phone_ciphertext IS NULL` passed **all thirty-two assertions and reported nothing**. `S2c` is the missing success. **A seeded run found it and a reading did not**, which is the second time in two days that executing a counterfactual corrected a claim this corpus had already written down.
  - **AN ASSERTION THAT DEPENDS ON A COLUMN BEING UNSET IS ONE AN EARLIER FIXTURE CAN SILENTLY DISARM.** `R3` violated the completeness `CHECK` by setting two of three columns and **relying on `phone_encrypted_at` never having been written**. Adding `S2c` above it seals the row first, so the omitted column would have inherited `S2c`'s timestamp and `R3` would have reported the write accepted. It clears the column explicitly now.
  - **TWO CONSTRAINTS ON ONE COLUMN MEAN THE PROBE SCORES WHICHEVER POSTGRESQL REACHED FIRST.** `R1`, `R2` and `R3` name the **completeness** `CHECK` and were written with a one-byte `'\xaa'` ciphertext, which violates the new **floor** as well. PostgreSQL reports one of two violated constraints in an order it does not document. That is not hypothetical: [DELTA_MANIFEST section 18](../../packages/db/DELTA_MANIFEST.md) recorded the same defect in `probe_phone_identity.sql` one day earlier. Every fixture now seals through `pg_temp.sealed()` so each assertion violates exactly the constraint it names, and **the floor is a separate constraint rather than a clause inside `*_ciphertext_is_complete` for the same reason**.
  - **AN OFF-BY-ONE IN THE STRICT DIRECTION IS INVISIBLE TO AN INVENTORY OF REFUSALS.** `>= 30` or `> 29` refuses the smallest legal envelope and **every rejection in the probe still passes**; it would have been found in production by a security notice that could not be written. `S2b`, `S2c` and `S5b` are exactly 29 bytes and `R2a`, `R3b` and `R4b` are exactly 28, so each of the three floors is pinned from both sides. The pair on the other two tables exists because a seeded floor of **17**, above every E.164 and below the envelope, passed all 33 assertions and failed nothing.
  - **THE BOUNDARY FIXTURES ARE CHECKED BEFORE ANYTHING RESTS ON THEM (`H0`).** Six assertions read `pg_temp.sealed_minimum()` and `pg_temp.sealed_empty()` for their byte counts, and a `sealed_minimum()` of 30 bytes would report the floor correct **at 29 and at 30**, passing either way and proving neither. Seeded, `H0` fails first.
  - **THE FLOOR'S REACH IS PARTIAL ON ONE TABLE AND THAT IS STATED RATHER THAN ROUNDED UP.** `contact_channels.kind` is `('email','push','sms')` since `0029`, so that column also holds email addresses and push tokens and **a plaintext email of 29 bytes or more clears the floor**. The refusal is total for telephone numbers, which is the class `INV-M10-12` names and the class `OQ-M10-06` was about, and total on the other two tables, which hold a number and nothing else.
- **Observation, not fixed here:** [INDEX](../INDEX.md) lists four files under `scripts/db/` and there are six probes plus two assertions on disk. `probe_phone_identity.sql` and `probe_calendar_revision_required.sql` have no row. This session added the row for the file it wrote and left the other two alone rather than describing probes it did not verify.
- **Files touched:** `packages/db/migrations/0034_reversible_contact_addresses.sql`, `scripts/db/probe_reversible_contact_addresses.sql`, `scripts/corpus/gates.mjs`, `packages/db/DELTA_MANIFEST.md`, `docs/decisions/ADR-046.md`, `docs/architecture/data-model/{contact_channels,identity_phones,phone_change_requests}.md`, `docs/{INDEX,STATE}.md`, `docs/sessions/{README,2026-08-16-session-41}.md`.
