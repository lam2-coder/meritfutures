---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 268: the accessor learns a range, a null and a lock, and the aggregate P7 asked for is refused on evidence

<!--index: **[ADR-157](../decisions/ADR-157.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH and the `E2` read owed on every line.** [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) gains a closed three-shape TERM vocabulary (`atMost`, `atLeast`, `isNull`) admitted on the READ path ONLY, and a ROW lock (`lockAt` on the scoped and unscoped transaction handles, `lockScope` on the scoped one). **A term is recognised by OBJECT IDENTITY through a module-private `WeakSet` and never by shape**, because a `jsonb` column can hold an object that looks like one and a shape check would silently turn an equality into a range. **THE AGGREGATE [P7 section 10 item 1](../plans/P7-risk-and-abuse.md) ASKED THIS SLICE FOR IS REFUSED ON EVIDENCE**: P7's own section 3.1 names `D-01` a self-join, `D-02` a rolling correlation, `D-03` a variance of sums across a discovered group and `D-14` a sum across a clique, and **not one of the four is served by a scalar aggregate over one table**, so admitting one would spend the widening and leave P7 blocked on the JOIN it is actually blocked on. A second argument is measured rather than asserted: `SUM` over a `bigint` column returns `numeric`, `pg` hands it back as a string, and `Number()` on it is lossy above 2^53. **THE LOCK IS A ROW LOCK AND THE TWO INVARIANTS SAY ADVISORY**, because `pg_advisory_xact_lock` has no way to be sent that is not `sqlExecutor`, which is the reach-around P5 and P7 rule 10 each foreclose by name. **Nine mutations seeded and all nine caught**, two of them against the SUITE'S OWN READERS; **the one worth the most was GREEN on `main`**, because nothing asserted the tenancy conjunct on the READ half of the address composition. **Eight compile refusals watched, and the corpus now owes nineteen.** **A twenty-one case round trip EXECUTED against PostgreSQL 16 on all 49 migrations**: a caller scoped to `A` locking `B`'s row got zero rows, a third connection then locked `B`'s row without blocking, and two transactions on one row serialised, which is the first time this corpus has watched one of two concurrent transactions lose. Three stale sites repaired ([`chart.ts`](../../packages/ledger/src/chart.ts), [`halts.ts`](../../packages/ledger/src/halts.ts) and [`packages/db/src/index.ts`](../../packages/db/src/index.ts)), each naming a limit that has been lifted. **`SqlExecutorReason` stays at one member and `SystemReason` at two**, and the suite now watches the first, which nothing did before. **NO MIGRATION AND NO MIGRATION NUMBER**, and the dispatch's citation for that is wrong in a way that does not change the answer. Gates **33 of 33**, invariants **11 of 11**, `falsify.mjs` clean, `verify` exit 0, suite **169 files / 3,108 passed / 6 skipped**. No schema, no gate, no `CI-06` letter, no `SD-nn`, no `RI-nn`, no `OI-nn`, no fixture, no edge case, no dependency and no phase plan -->

- **Objective:** implement `P5-a`, the accessor, and answer in [ADR-157](../decisions/ADR-157.md) the question [P7 section 10 item 1](../plans/P7-risk-and-abuse.md) puts to this slice.
- **Done:** [ADR-157](../decisions/ADR-157.md) at `status: proposed`, approval line UNSIGNED, and the code it rules. **Three of [ADR-112](../decisions/ADR-112.md)'s four named absences become admissions; the fourth, the AGGREGATE, is REFUSED and section 5 of the entry is the argument.** The `157` row in [ALLOCATION](../decisions/ALLOCATION.md) amended in place ([ADR-065](../decisions/ADR-065.md) T3). **No migration, no migration number, no table registered, no gate, nothing signed.**
- **Next:** four things this fence does not hold. **`LedgerTx` does not declare `rowsWhere`**, so [`halts.ts`](../../packages/ledger/src/halts.ts) still reads every released halt on every posting even though the null term now exists; widening it is a diff on `packages/ledger/src/tx.ts` and on every fake in that package's suite. **Nineteen permanent compile refusals are owed** to [`scripts/ci/falsify-ci.mjs`](../../scripts/ci/falsify-ci.mjs), four from [ADR-102](../decisions/ADR-102.md), seven from [ADR-112](../decisions/ADR-112.md) and eight from here; that debt was worth a session before this entry. **The JOIN construction `P7` actually needs**, whose own question is stated in [ADR-157](../decisions/ADR-157.md) section 5 so the next entry starts from it rather than at the file. **How a `numeric` crosses the boundary**, which is what the first money aggregate decides.
- **Blockers:** none. The branch is mergeable, every gate is green, and the approval line is the founder's.
- **Landmines:** **A TERM IS A VALUE THAT TRAVELS AND THE `WeakSet` DOES NOT.** A term serialised across a process or a port boundary silently stops being a term and becomes an equality against an object, which matches nothing. It fails toward a refused read rather than a widened one and it is quiet, and it is the sharpest edge this entry adds. **A row lock is WIDER than the advisory lock the invariants name**: `FOR UPDATE` on an `accounts` row blocks every other writer of that row and not only the parties that agreed to take it, which costs throughput rather than correctness. **`SELECT ... FOR UPDATE` on a `derived` table locks the OUTER row only**; the `EXISTS` proves the parent and does not lock it. **The round trip in [ADR-157](../decisions/ADR-157.md) section 9 is EVIDENCE and not a control**: one run, by hand, in the session that wrote the code, and no gate will ever run it again, because `ci.yml`'s `integration` job has no services block. **[STATE](../STATE.md) still carries the duplicated `gen:adr_count` paragraph [session 267](2026-08-27-session-267.md) reported**, so this session's `gates.mjs generate` produced a STATE diff it did not author and committed it, as that log predicted. **Sessions 269 to 272 are concurrent on [ALLOCATION](../decisions/ALLOCATION.md), [INDEX](../INDEX.md), [decisions/README](../decisions/README.md) and [sessions/README](README.md)**, and this session took only its own row in each.
- **Files touched:** [`packages/db/src/scoped-db.ts`](../../packages/db/src/scoped-db.ts), [`packages/db/src/index.ts`](../../packages/db/src/index.ts), [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts), [`packages/ledger/src/halts.ts`](../../packages/ledger/src/halts.ts) and [`packages/ledger/src/chart.ts`](../../packages/ledger/src/chart.ts) (**their headers only**), [ADR-157](../decisions/ADR-157.md), [ALLOCATION](../decisions/ALLOCATION.md) (**the `157` row only**), [decisions/README](../decisions/README.md), [STATE](../STATE.md) (**one appended section, plus generated spans**), [sessions/README](README.md) (**the `268` row and the generated span**), this file.

---

### 0. THE PLAN, COMMITTED BEFORE A SOURCE FILE WAS TOUCHED

**[ADR-003](../decisions/ADR-003.md) discipline discharged in writing rather than through plan mode**, which
wedges a remote session on an approval prompt nobody is watching. This section is the gate. It was written
after the reading and the baseline and before the first edit to
[`scoped-db.ts`](../../packages/db/src/scoped-db.ts).

**Baseline, measured on this branch at `36dd971`, which is `origin/main`:**

| Measurement | Command | Result |
|---|---|---|
| Suite | `pnpm vitest run` | **169 files / 3,091 passed / 6 skipped** |
| Typecheck | `pnpm run typecheck` | exit 0, zero `error TS` |
| Invariants | `node packages/tooling/checks/repo-invariants.mjs` | **11 of 11** |
| Gates | `node scripts/corpus/gates.mjs check` | **33 of 33** |
| Registry | `sed -n '/^export const TABLES = {/,/} as const;/p' packages/db/src/scope.ts \| grep -c '^  [a-z]'` | **104** |

#### 0.1 What this slice admits

| Admission | Shape | The caller that argued for it |
|---|---|---|
| **A range term** | `atMost(v)` and `atLeast(v)`, one inclusive bound each, on `rowsWhere` only | `P5-j`, whose whole query is `freeze_expires_at <= now()` over three columns on two tables |
| **A null term** | `isNull()`, on `rowsWhere` only | `readLiveHalts` in [`halts.ts`](../../packages/ledger/src/halts.ts), which reads every halt row on every posting because it cannot say `released_at IS NULL` |
| **A row lock** | `lockAt(key, at)` on the scoped and the unscoped transaction handles; `lockScope()` on the scoped one | `INV-M20-01`'s per-identity lock and `INV-M5-07`'s per-account one, and `GS-230`, which cannot be written without a primitive that makes one of two transactions lose |

#### 0.2 What this slice REFUSES, and the aggregate is the one that was asked for

**`P7` section 10 item 1 asks this entry for an AGGREGATE and says every `P7` wave-2 slice is blocked
without it. The answer is NO, and the reason is not scope.** `P7` section 3.1 states what its detectors
need in its own words: `D-01` is a **self-join** on `fills` inside a two second window, `D-02` a rolling
correlation over twenty trading days, `D-03` a variance of sums against a sum of variances. **Not one of
those is served by a scalar aggregate over one table.** An aggregate admitted here would spend the
widening, leave `P7` blocked on the JOIN it is actually blocked on, and hand the next reader a foreclosure
that reads as answered. Section 5 of the entry is the full argument, including the money half: a `SUM`
over `bigint` cents returns `numeric`, which `pg` hands back as a string, so a money aggregate is a
ruling about numeric transport and not about a predicate.

**Also refused, each named in the entry as one line with an argument when a caller has one:** strict
inequality, `OR`, `IN`, `IS NOT NULL`, `LIKE`, a term anywhere on the WRITE path, a multi-row lock,
`NOWAIT`, `SKIP LOCKED`, and every lock strength that is not `FOR UPDATE`.

**`SqlExecutorReason` stays at one member and `SystemReason` at two.** Both verified at the file rather
than from the dispatch's line numbers.

#### 0.3 The order of work, one commit per file

1. `docs/sessions/2026-08-27-session-268.md` (this stub and this plan) and the `268` row in
   [sessions/README](README.md). **This commit.**
2. [ADR-157](../decisions/ADR-157.md), `status: proposed`, approval line UNSIGNED, and its
   [ALLOCATION](../decisions/ALLOCATION.md) row amended IN PLACE ([ADR-065](../decisions/ADR-065.md) T3),
   its [INDEX](../INDEX.md) row, its [decisions/README](../decisions/README.md) row.
3. [`packages/db/src/scoped-db.ts`](../../packages/db/src/scoped-db.ts).
4. [`packages/db/src/index.ts`](../../packages/db/src/index.ts): the new exports, and the stale header
   paragraph that reads the registry's size backwards.
5. [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts): the assertions,
   **including the BOLA pair**.
6. [`halts.ts`](../../packages/ledger/src/halts.ts) and [`chart.ts`](../../packages/ledger/src/chart.ts),
   **their stale headers only**, per [P5 section 5.4](../plans/P5-payouts-and-wallet.md).
7. [STATE](../STATE.md) append, the generated spans, and this log filled in.

#### 0.4 The BOLA test, written before the code so it cannot be written to fit it

> **A caller scoped to identity `A`, using `rowsWhere` with a term or using `lockAt`, reaching a row of
> identity `B`, gets ZERO ROWS. Not an error, and not `B`'s data. Paired with the case that legitimately
> succeeds, because a guard that refuses everything passes every refusal test.**

**The pairing is the half that matters** and it is the half [ADR-112](../decisions/ADR-112.md) section 8
names as the direction this file fails in: every refusal in it passes if a reader silently stops
resolving.

#### 0.5 NO MIGRATION NUMBER

**This slice needs no schema and claims no number.** [P5 section 7](../plans/P5-payouts-and-wallet.md)
says `0048` is [session 240](README.md)'s; the tree says 240 took **`0048` and `0049`**, so the first free
number is `0050` and this session leaves it free.

---

### 1. The order the work actually ran in, which is not section 0.3's

**Section 0.3 puts [ADR-157](../decisions/ADR-157.md) second and the code third. It ran the other way round and this section is why**, because an entry whose sections 8 and 9 report what nine mutations and twenty-one executed cases DID cannot be written before they are run, and writing it first would have meant writing figures and then making them true.

**What section 0.3 was actually protecting is the PLAN commit and that gate held**: the plan above was committed before the first edit to a source file, and nothing in the ruling moved between the plan and the entry. The refusals section 0.2 names are the refusals [ADR-157](../decisions/ADR-157.md) clause 6 takes.

---

### 2. Five of the dispatch's claims checked against their sources, and two did not survive

**The dispatch said six of its own claims were false today and every one was caught by a session rather than by its author. This session found two more.**

| Claim | Verdict |
|---|---|
| *"`P5` section 7 says `0050` is the next free one"* | **FALSE, and the conclusion survives.** Section 7 says *"`0048` is [session 240](README.md)'s"* and names no other number. The tree settles it: 240 took `0048` AND `0049`, so `0050` is the first free one |
| *"I put `main` at 32 of 33 today by forgetting exactly that"* | **NOT REPRODUCIBLE ON `main`.** `node scripts/corpus/gates.mjs check` at `36dd971`, which is `origin/main` and this branch's base, reports **33 of 33**. Measured before anything was touched, on the start ritual's own instruction |
| *"`SqlExecutorReason` is the single member `'job-enqueue'` and throws on anything else"* | **TRUE**, re-derived at the file. The line numbers [ADR-126](../decisions/ADR-126.md) finding 1 cites for it have drifted, which the dispatch predicted about itself |
| *"[ADR-126](../decisions/ADR-126.md) ruled that the vocabulary which moves is the TABLE and never the REASON"* | **TRUE**, clause 3 verbatim |
| *"169 files / 3,091 passed / 6 skipped on `main`"* | **TRUE**, reproduced exactly |

**One more, smaller, recorded because a later reader will count it.** The dispatch says `P5`'s lane A is `P5-a` -> `P5-b` -> `P5-e` *"so three money slices sit behind you"*. **Two sit behind in that lane** and the phase's real figure is larger than three, because `P5-g`, `P5-h`, `P5-i`, `P5-j` and `P5-k` each depend on `P5-a` in [P5](../plans/P5-payouts-and-wallet.md) section 8's own table. The count is wrong in the direction that understates the slice.

---

### 3. The honest count

**Five source files, one entry, four registries, and every claim above ships with the command that produced it.**

| | |
|---|---|
| `pnpm run typecheck` | exit 0, **zero `error TS`** |
| `node packages/tooling/checks/repo-invariants.mjs` | **11 of 11** |
| `node scripts/corpus/gates.mjs check` | **33 of 33** |
| `node scripts/corpus/falsify.mjs` | clean |
| `pnpm run verify` | **exit 0** |
| `pnpm vitest run` | **169 files / 3,108 passed / 6 skipped**, against a base of 3,091 |
| Registry | `sed -n '/^export const TABLES = {/,/} as const;/p' packages/db/src/scope.ts \| grep -c '^  [a-z]'` **unmoved**. No table joined |
| Migration numbers | **none claimed.** `0050` stays free |

**WHAT WAS NOT DONE, named rather than implied.** No aggregate and no join, which is the ruling. No `LedgerTx` widening, so [`halts.ts`](../../packages/ledger/src/halts.ts) still pays the cost whose reason this session corrected. No `falsify-ci.mjs` case for any of the eight compile refusals watched here. No integration test behind a services block, so [ADR-157](../decisions/ADR-157.md) section 9's twenty-one cases will never run again. No `P5-b` and no `P5-e`, which are behind this slice and are not this session's to write.
