## 2026-08-14 - Session 14: the schema-delta reconciliation, migrations 0002 to 0026
<!--index: the schema-delta reconciliation, migrations 0002 to 0026 -->
- **Done.** Wrote the remaining **24 migration files**, completing the 27-file set. All **93** schema changes are now folded at create: 88 numbered plus the 5 unnumbered ones, 90 in the v1 core sequence and 3 in the marked reserved sequence. `packages/db/DELTA_MANIFEST.md` written with every delta's disposition, and [DATA_MODEL](../architecture/data-model/README.md) amended under [ADR-026](../decisions/ADR-026.md).
  - **Verified installable.** All 27 files apply in order against PostgreSQL 16 with `ON_ERROR_STOP`: **96 tables, 326 indexes, 342 check constraints, 5 triggers.** No file was edited to make that pass. This is a syntax and dependency check and **proves nothing about whether a delta was folded correctly**, which is exactly what the E2 read is for.
  - **The file sequence was reconstructed rather than inherited.** Only four anchors existed in the three committed files (0001 of 27, 0009 ledger, "the SD-09 index in 0010", "wallet_withdrawals in 0011", "no UPDATE grant, see 0026", 0027 last). The rest was derived from the dependency graph and is recorded in DELTA_MANIFEST section 1 so nobody has to derive it twice.
  - **Rules that were prose are now DDL.** AS-M2-03's binding (`set_risk` may never reach `confirmed_inferred`), SD-M7-01's clean-night guard (a detector run cannot claim `ok` while it found fewer synthetics than it seeded), SD-M6-04's disclosure rule (a trader-audience evidence pack may never carry detector detail), SD-M6-05's second-person rule (`approved_by <> requested_by`), INV-18 (`closing = opening + realized_pnl + adjustment`, checkable only because SD-01 exists), and SD-M17-04's `varies` check, which makes an experiment against a rule or a gate **unwritable**.
  - **Three shape decisions the deltas did not specify**, each written down where it lands: SD-10's conditional not-null is a materialized `floor_lock_enabled` on `plan_version_sizes` rather than a trigger, because a CHECK cannot read the parent's jsonb; SD-M2-01's `payload_hash` is written by the enqueue path rather than generated, because a generated column needs an immutable cast of `jsonb` and the duplicate-intent guard must not rest on a Postgres version question; `wallet_entries` uses direction-plus-magnitude rather than the ledger's signed convention, because reusing one sign convention for two different questions is the shape of error [ADR-027](../decisions/ADR-027.md) was reversed over.
- **Next.** The founder's **E2 line-by-line read** on the sixteen money-path files. Nothing merges without it. Then the DATA_MODEL table-by-table rewrite (below), then the CI manifest gate, then the first module against this schema.
- **Blockers.** None.
- **Landmines.**
  - **A sixth unnumbered change exists and [ADR-026](../decisions/ADR-026.md)'s table of five does not carry it.** `provisioning_status` gains `confirmed_inferred`: the approved DATA_MODEL declares five values, [M02 section 3.2](../plans/M02-rithmic-bridge.md) adds a sixth and makes it a distinct state, and AS-M2-03 makes it binding that `set_risk` may never reach it. **It is folded** (the value in `0001`, the binding as a CHECK in `0007`), so nothing is missing from the schema. Two things are wrong on the record: **`0001`'s inline marker cites `SD-M2-06`, which is the `reconciliations` delta** and lands in `0014`; and **the total in scope is arguably 94 rather than 93.** Migrations are sacred so `0001` is not edited, and this manifest does not move ADR-026's count on its own authority. **The founder rules it: a `U-06` entry, or a finding that a state-machine value inside an approved plan is not a "schema change" for this purpose.** Recorded in DELTA_MANIFEST section 7.
  - **[ADR-030](../decisions/ADR-030.md)'s stale list is wrong in two of its four entries, and following it would have been a money-path error.** It names `win_days.required_count: 5` and `phase_eval.min_trading_days: 1` as stale in DATA_MODEL section 11. **Both are Core EOD's frozen values** per [M01 Appendix A.1](../plans/M01-rules-engine.md), sourced to constitution 0.4. **`w = 3` is Merit Rapid's** ([ADR-018](../decisions/ADR-018.md)) and the section 11 example was never a Merit Rapid example: its `cadence_gap_trading_days: 5`, `cap_bp: 300` and `split_bp: 9000` are Core EOD's throughout. **Changing them to match the list would have put Merit Rapid's cadence on Core EOD's contract.** The two key names were C-06's real content and they are folded. Recorded in the amended section 11 rather than applied silently.
  - **DATA_MODEL is only partly at post-migration truth, and the banner says so rather than implying otherwise.** Sections 3, 8, 11 and the new 17 are amended. **The table-by-table rewrite of sections 4 through 10 is NOT done.** Until it is, those tables must be read together with DELTA_MANIFEST, exactly as section 11 previously said of M01's ten deltas. A document that claims to be the post-migration truth while carrying pre-migration tables is worse than one that admits the gap.
  - **Three genuine reference cycles need a forward foreign key** and are not an ALTER chain: `purchases.parent_account_id` closed in `0007`, `accounts.terminal_settlement_id` closed in `0010`, `purchases.wallet_ledger_transaction_id` closed in `0011`. Each carries a comment at both ends. The ordering that forces them is real: `accounts` must precede `0010_payouts`, and `accounts` needs `purchases`, which needs `coupons`, which needs `affiliates`. **That chain is also why the affiliate module is split across `0005` and `0012`.**
  - **`0026`'s append-only revokes are a list, and a list drifts.** Eighteen tables are named there against DATA_MODEL section 1's Mutability set. **The CI check must assert the revoke list against the document rather than trusting either**, which is noted in the file and is not yet built.
  - **Two legitimate single-column updates exist on append-only tables** (`daily_marks.superseded_by`, `identity_links.suppressed`) and the grants forbid them. They are `SECURITY DEFINER` functions arriving with the module that owns each transition, each with its negative-authz test. **Those functions do not exist yet**, so a naive first implementation of either transition will fail at the grant, which is the correct failure and will look like a bug.
  - **`liability_snapshots` exists in two shapes in the corpus** and the migration follows `0009`, which was written last session: `SD-M6-01`'s column set, keyed on `as_of timestamptz`. DATA_MODEL section 8 still shows the older `snapshot_on date` shape with `funded_accounts`, `reserve_cents`, `cvar99_cents`, `rcr_bp` and `per_plan`. **The migration is the truth and section 8 is one of the tables not yet rewritten.** The RCR fields have no home in the folded shape and need one before M06 is built.
- **Files touched:** `packages/db/migrations/` (24 new files: 0002 to 0008, 0010 to 0026); `packages/db/DELTA_MANIFEST.md`; `docs/architecture/DATA_MODEL.md`; `docs/{INDEX,STATE,SESSION_LOG}.md`.

---
