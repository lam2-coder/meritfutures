---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 607: the seed never reaches the table because nothing in this repository reaches a database, and the schema was never what stopped it

<!--index: **THE ROW IS [ADR-412](../decisions/ADR-412.md), `proposed` and UNSIGNED. NON-MONEY. ONE TEST FILE AND THE REGISTRIES, NO `src/` FILE AND NO `.sql` FILE.** **THE ROW WAS DISPATCHED TO MAKE A SEED LAND AND DELIBERATELY DID NOT WRITE A LOADER**, and every clause of the refusal carries its measurement. **THE SEED HAS NO LOADER AND NEITHER DOES THE OTHER ONE**: every import in every module under `packages/db/src/seed` is a `node:` builtin, both generators say so about themselves in their own headers, and until this row nothing checked either sentence. **THE CONVENTION IS WRITTEN DOWN AND IT POINTS INSIDE THIS ROW'S FENCE**: `seed/calendars/README.md` states that a loader lives at `packages/db/src/seed/` on [ADR-008](../decisions/ADR-008.md)'s reason, it is slice `S-E4`, and `P7`'s slice table allocates no detector loader at all, so this is a slice that was never written rather than one that slipped. **REFERENCE DATA DOES LAND IN MIGRATIONS HERE AND THE SPLIT IS BY WHAT THE VALUES ARE**: four `ledger_accounts` codes across `0052`, `0053`, `0054` and `0056` are closed literals a CHECK refuses, and an open, versioned, effective-dated registry the founder moves without a deploy is the other kind, which is `SD-M7-03`'s whole reason. **THE SCHEMA IS NOT WHAT STOPS IT AND THAT IS THE EXPENSIVE WRONG TURN FORECLOSED**: `0008` carries the DDL, `0026` grants `merit_app` INSERT on all tables and its append-only REVOKE does not name this table, `scope.ts` registers `detectorDefinitions` as `firm`, and `firmDb` plus `transaction` publish a firm write door, so **no migration and no grant is needed and none was taken**. **WHAT ACTUALLY STOPS IT IS THAT THERE IS NOWHERE TO LOAD INTO**: not one of the four workflows deploys anything, and the only database any migration has ever touched is the `postgres:16` service container `CI-06h` destroys with the job, so `detector_definitions` is not empty in a deployment, it is empty because there is no deployment. **A LOADER WOULD HAVE NO CALLER AND NO ENVIRONMENT**, which is [ADR-409](../decisions/ADR-409.md)'s own sentence one row earlier, that an install nothing can exercise is an install nothing falsifies. **BOTH OF ADR-409'S FIGURES REPRODUCE**: eleven of the eighteen seeded rows state no number, the same eleven, derived from the committed artifact; and the three module lists hold 3, 4 and 7 detectors, which is fourteen, with the census green at 17 of 17, so **thirteen of fourteen decline against a fully loaded registry and `D-02` is the one that runs**. **SO THE ENTRY SAYS WHICH: LOADING IS NECESSARY AND IT IS NOT SUFFICIENT**, and a founder told the loader unblocks the detectors would have been told something false. **BUILT: two cases in `seed-detectors.test.ts` that pin the division mechanically**, one walking BOTH seeds for a non-`node:` import specifier and one slicing `0026` to the append-only statement with a positive control, both watched RED on the real tree, the migration restore confirmed byte-identical with `sha256sum -c`. **REPORTED AND NOT REPAIRED**: `RI-35`'s register carries NEITHER seed, so both READMEs go false in silence on the day a loader lands, and that register is outside this fence; the calendar seed is in the identical condition and is older; and the dispatch's own framing of the blocker is one step short, which is the class of defect [ADR-409](../decisions/ADR-409.md) recorded about its own dispatch. **NO LOADER, NO MIGRATION, NO MIGRATION NUMBER, NO GATE ADDED OR WEAKENED, NO INVARIANT MINTED, NO TEST SKIPPED, `packages/db/src/index.ts` UNTOUCHED, NO EVENT PRODUCER UNDER `packages/*`.** Counts derived at reporting time, each command run separately. -->

### 1. The objective

Derive why `detector_definitions` is empty when the seed exists on disk, establish this repository's own convention for landing a seed rather than inventing one, re-derive [ADR-409](../decisions/ADR-409.md)'s two figures, and build whatever is buildable inside `packages/db/src/seed/**` and the seed's test. One objective, non-money.

### 2. Done

- **The container measured and reported verbatim.** No repository on arrival, eleventh row on the record. `ls -la /home/user` returned two directory entries and nothing else; `git status` returned *fatal: not a git repository (or any of the parent directories): .git*. This row cloned the tree itself in **17.8 s** to a **full** clone, `git rev-parse --is-shallow-repository` **`false`** at **4348** commits and **185M** of `.git`, and `pnpm install --frozen-lockfile` took **20.3 s** with no lockfile change.
- **The base reproduced on `08b9acf2`** before the first edit, on an empty `git status --porcelain`: gates **33 of 33**, invariants **35 of 35**, suite **337 files / 8,141 passed / 10 skipped**, typecheck, lint and `format:check` clean. `generate` reported *every span already matches its query*.
- **The seed module, its source, its rows and its README read in full**, and the no-loader division derived from the import specifiers rather than accepted from the prose.
- **The convention established at primary source**, from the OTHER seed's README, its `S-E4` slice row in `P1-SE`, and [ADR-008](../decisions/ADR-008.md)'s reason.
- **Both migration-borne reference-data precedents read**, `0052` through `0056`, and the split between the two conventions stated rather than assumed.
- **The schema half derived and foreclosed**: DDL, grant, scope registration and write door, each checked at its own source.
- **The absence of any deployment derived from the workflows**, which is the finding that made the refusal decisive rather than procedural.
- **Both of [ADR-409](../decisions/ADR-409.md)'s figures re-derived**, one from the committed artifact and one by executing the census.
- **Two cases written and both watched RED on the real tree**, with the migration restore confirmed byte-identical.

### 3. Refused

- **NO LOADER WAS WRITTEN**, although this row's fence contains the exact directory the convention names for one. It would have no caller and no environment, `P7` allocates no slice for it, and `S-E4` already specifies the shape with six proofs and a probe file.
- **NO MIGRATION AND NO MIGRATION NUMBER**, and none was needed: the table has carried its DDL since `0008` and its grant since `0026`.
- **NO ROW HAND-WRITTEN INTO A MIGRATION**, which would also have thrown away the one property the seed exists for, that every value carries the `M07` line it was read from.
- **`packages/db/src/index.ts` NOT WRITTEN**, and nothing here needed it: a module inside `packages/db` reaches `scoped-db.ts` by relative path.
- **NO EVENT PRODUCER CREATED UNDER `packages/*`.**
- **`apps/worker/**`, `packages/db/migrations/**`, `apps/api/src/admin-source/**` and `apps/api/test/wiring.test.ts` READ, CITED BY NAME, AND NOT WRITTEN.**
- **THE THIRTEEN DECLINES WERE NOT RE-PARTITIONED.** [ADR-409](../decisions/ADR-409.md) section 5 did that work and it stands; this row re-derived the figures and not the partition.

### 4. The RED that was watched

| Seed | What was done | What was measured |
|---|---|---|
| **1** | A two-line `load.mjs` written beside the generators importing `firmDb` and `transaction` from `../scoped-db.ts` | **RED**, offender set `["../scoped-db.ts"]`, so the finding names the specifier |
| **2** | `detector_definitions` inserted into the append-only `REVOKE` list in `0026` | **RED** on the append-only assertion with the positive control still green |

Seed 1 was removed and left `git status --porcelain packages/db/src/seed/` empty. Seed 2 mutated a merged migration and `sha256sum -c` reported **OK** on the restore.

### 5. Found and not repaired

- **`RI-35`'s register carries neither seed.** Both seed READMEs ship an absence claim about a loader; `packages/tooling/checks/absence-claims.mjs` registers neither, so both sentences go false in silence on the day one lands. Outside this fence.
- **The calendar seed is in the identical condition and is older**, named unwritten since session 31. It is one gap with two instances.
- **`P7`'s slice table has no row for a detector loader.** `P7-d` delivered a seed and was never asked for one. Outside this fence.
- **The dispatch's framing is one step short**, in the same class [ADR-409](../decisions/ADR-409.md) section 10 recorded about its own.

### 6. Counts at reporting time

Each command run separately, on the branch, after `gates.mjs generate`. **`pnpm run verify` was NOT run and `falsify.mjs` was NOT run**, both forbidden by this row because both mutate the working tree.

| Leg | Baseline at `08b9acf2` | On this branch |
|---|---|---|
| `gates.mjs check` | 33 of 33 | **33 of 33** |
| `repo-invariants.mjs` | 35 of 35 | **35 of 35** |
| `pnpm vitest run` | 337 files / 8,141 passed / 10 skipped | **337 / 8,143 / 10** |
| `pnpm run typecheck` | clean | **clean** |
| `pnpm run lint` | clean | **clean** |
| `pnpm run format:check` | clean | **clean** |

**The delta is +2 cases and it is exactly the two this row wrote.** No case skipped, disabled or quarantined; no gate added, widened or weakened; no existing assertion relaxed.
