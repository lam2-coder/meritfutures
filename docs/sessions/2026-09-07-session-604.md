---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 604: a BI event was destroying every detector_runs row and is repaired, four of the runner's five ports are served, and the blocker that says every detector declines is false of the one that runs

<!--index: **THE ROW IS [ADR-409](../decisions/ADR-409.md), `proposed` and UNSIGNED. NON-MONEY. TWO `src/` FILES CHANGED, BOTH IN FENCE.** **THE REPAIR IS THE ROW.** `runner.ts` emitted `detector.run_completed` FIRST and UNCONDITIONALLY inside the write transaction, so a refusing sink rolled back the `detector_runs` row of EVERY run, `ok` runs included, against `INV-M7-07`. **NO RULE IN `M07`, IN [`EVENTS`](../architecture/EVENTS.md) OR IN THIS DEPLOYABLE BINDS THAT NAME TO THE RUN ROW**; the one that is bound is `detector.run_degraded` and [`ports.ts`](../../apps/worker/src/detectors/ports.ts) says so of that name alone, citing `ADR-006`. **[ADR-349](../decisions/ADR-349.md)'s ARGUMENT WAS SOUND AND WAS APPLIED TO A POPULATION IT IS NOT ABOUT**: a row committed without its page is `AS-M7-05`'s dashboard with an extra step only where there IS a page, and a non-degraded run has none. **SO THE PAGE NOW GOES FIRST AND FATALLY AND THE BI POINT SECOND AND RECOVERABLY**, its failure RETURNED onto `DetectorRunOutcome.error` rather than swallowed, because a swallowed emit is the no-op sink [`adapter.ts`](../../apps/worker/src/detectors/adapter.ts) calls the worst value in that file arriving by another door. **A DEGRADED RUN STILL TAKES ITS ROW DOWN WITH ITS PAGE AND THAT IS ASSERTED TWICE AS A CONTROL.** **`DetectorRunnerIo` HAS FIVE MEMBERS, FOUR HAVE A PRODUCER, THE FIFTH IS `events` AND HAS NONE**, and all three blockers in [`schedule.ts`](../../apps/worker/src/schedule.ts) were checked at their own sources and all three hold. **THE THIRD BLOCKER'S SECOND CLAUSE OVERREACHES**: eleven of eighteen seeded rows state no number, exactly, but *"a loaded registry still gets DetectorDeclined"* is true of thirteen of the fourteen detectors that exist and false of `D-02`. **THE JOB IS STILL UNSCHEDULED AND THE REASON IS ONE BLOCKER SHORTER**: with an empty registry a scheduled run now writes eighteen `failed` rows a night and detects nothing, and the loader and the sink no longer have to clear together. **NEITHER SEED IN THIS WORKSPACE HAS A LOADER**, calendars included, so that gap is shared and unowned. **EVERY PRECONDITION [P7](../plans/P7-risk-and-abuse.md) STATES FOR `P7-e` HAS BEEN MET SINCE 2026-08-27.** **THE CENSUS IS BUILT** and its alarm was watched RED before it was believed, finding on the way that answering `OQ-M7-02` for `D-14` did NOT make it run: it needed a severity band too. **A CORRECT PROSE EDIT IN THIS ROW'S OWN FENCE TURNED `RI-15` RED** by moving a symbol a pointer in an unowned file cites by line, and was redone LINE-COUNT NEUTRAL. **THE DISPATCH ASSERTED `P7-e` WAS UNDONE ELEVEN DAYS AFTER IT MERGED AND CORRECTED ITSELF MID-SESSION**, which is [ADR-406](../decisions/ADR-406.md) section 4's failure a second time in two waves and is recorded rather than absorbed. **NO GATE ADDED, WIDENED OR WEAKENED, NO TEST SKIPPED, NO INVARIANT MINTED, NO MIGRATION, NO `CI-06` LETTER.** -->

### 1. The objective

Derive at primary source what `UNWIRED_DETECTOR_RUNNER_IO` actually waits on, clear whatever has cleared, and either land the runner and its canaries or state precisely what stands in the way. One objective, non-money.

### 2. Done

- **The container measured and reported.** No repository on arrival, seventh row running. `git rev-parse` returned *fatal: cannot change to '/home/user/meritfutures'*. This row cloned the tree itself, so its own `git rev-parse --is-shallow-repository` is **`true`** at **430** commits, and nothing needed deepening.
- **The base reproduced on `43ae94a0`** before the first edit, on an empty `git status --porcelain`: gates **33 of 33**, invariants **35 of 35**, suite **336 / 8,109 / 10 / 0**, typecheck, lint and `format:check` clean. `generate` reported *every span already matches its query*.
- **`apps/worker/src/detectors/**` read in full** and `DetectorRunnerIo` derived member by member against whatever serves it.
- **All three recorded blockers re-derived at their own sources.** All three hold. Two are outside `apps/worker`.
- **Every detector module run through the real `runDetectors` against its own real seed row**, which is how `D-02` was found to be the one that runs.
- **THE EMIT ORDERING DEFECT DERIVED, RULED AND REPAIRED**, after searching `M07` and `EVENTS` for a transactional requirement on `detector.run_completed` and finding none.
- **The degraded control asserted in two files**, because the repair must not buy `INV-M7-07` with `AS-M7-05`.
- **The cross-module census written** and its alarm falsified in two stages rather than asserted.
- **`adapter.ts`'s header corrected line-count neutrally** after the first attempt turned `RI-15` red.
- **The seed restored byte for byte** after the falsification, and the suite re-run green.

### 3. Refused

- **The runner was NOT wired.** No sink is reachable and nothing loads the registry. An install nothing can exercise is an install nothing falsifies.
- **`detector_run_id` was NOT added to the payload**, although `runner.ts` is in fence: `event-sink.test.ts` pins its absence and is outside the fence, and clearing that leg clears nothing while no sink is reachable at all.
- **No no-op sink was passed anywhere**, and no `src/` file passes a second argument to `postgresDetectorRunnerIo`. That assertion was neither relaxed nor touched.
- **Nothing was scheduled**, and `schedule.ts`, `index.ts` and `packages/db/src/seed/**` were read, cited by name, and not written, **including where this row's own repair left two of them carrying a false sentence**.
- **The declines were NOT partitioned past the two classes legible in the runner's own message**, because a partition that over-fits their wording breaks on a rewording rather than on a change of fact.

### 4. The repair, and the control that guards it

| Event | Order | Failure |
|---|---|---|
| `detector.run_degraded` | First, only when degraded | **THROWS, unchanged.** The row goes down with the page, which is the criterion `ports.ts` states citing `ADR-006` |
| `detector.run_completed` | Second, every run | **RETURNED, not thrown.** The row commits and the refusal lands on the outcome the caller already reads |

| Under the composed default | Before | After |
|---|---|---|
| A non-degraded run, which is all fourteen today | unrecorded | **RECORDED, carrying the refusal by name** |
| A degraded run | unrecorded | **unrecorded, deliberately unchanged** |

### 5. The RED that was watched

| Mutation to the registry row a detector reads | Observed |
|---|---|
| `D-14`'s `max_abs_summed_position` stated at `100` | **STILL GREEN.** It declined again: *"its detector_definitions row states no severity and offers no case to take one from"* |
| That knob **and** `severity` stated at `3` | **RED.** `AssertionError: expected [ 'D-02', 'D-14' ] to deeply equal [ 'D-02' ]` |

**The first row is the more useful one and it was not the expected result.** A plan that prices `OQ-M7-02` as the thing between the estate and working detection is pricing one of at least two questions.

### 6. Found and not repaired

| Finding | Owner |
|---|---|
| No seed in this workspace has a loader, calendars and detectors alike | **Unassigned.** The largest remaining blocker |
| `detector.run_degraded` is in no version of the event catalogue | Whoever holds the catalogue |
| `DetectorRunnerUnwired`'s message says *no adapter is installed* and one is | A row dispatched to it. In fence, not taken |
| This row's repair leaves a false sentence in `schedule.ts` and in `index.ts`, both unowned this wave | Whichever row next holds those files |
| `OQ-M7-02` is priced as one question and `D-14` needed two | **The founder** |
| `P7`'s precondition list for `P7-e` is complete and satisfied while the gate is shut | Whoever maintains `P7` |
| Four seeded rows have no detector module: `D-06`, `D-12`, `D-15`, `D-17`. `D-12`'s absence is ruled, the other three are unbuilt | `P7`'s remaining detector slices |
| The dispatch asserted `P7-e` was undone eleven days after it merged, and corrected itself mid-session | **The dispatcher**, at its own request |

### 7. Counts at reporting time

Every figure below was derived at the moment it was written, on this branch, each command run separately. `pnpm run verify` and `scripts/corpus/falsify.mjs` were **NOT run**, both forbidden by the row.

| Leg | Baseline at `43ae94a0` | After this row |
|---|---|---|
| Gates | **33 of 33** | **33 of 33**, unchanged because this row adds none |
| Invariants | **35 of 35** | **35 of 35**, unchanged because this row mints none |
| Suite | **336 files / 8,109 passed / 10 skipped / 0 failed** | **337 / 8,127 / 10 / 0**, a delta of **+18 cases**, seventeen in one new file and one from splitting an existing case into a behaviour and its control |
| Typecheck, lint, `format:check` | clean | clean |

**NOT THE MONEY PATH.** No migration, no SQL, no probe, and no financial value read or written.
