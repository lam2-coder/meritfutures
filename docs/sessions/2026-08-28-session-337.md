---
status: approved
depends_on: [README.md]
last_updated: 2026-08-28
---

## 2026-08-28 - Session 337: the trigger count is ruled, the table stands, and the ruled repair deletes the number rather than correcting it

<!--index: **[ADR-185](../decisions/ADR-185.md) LANDS, `status: proposed`, approval line UNSIGNED, AND IT REPAIRS NO DOCUMENT, which is a measurement rather than a shortage of time.** **THE COUNT WAS DERIVED AT THE SOURCE and the dispatch deliberately stated no number: [EVENTS section 11](../architecture/EVENTS.md) CARRIES TWELVE ROWS**, read between its heading and section 12, and the three [M10](../plans/M10-integrations.md) omits are `payout.held`, `payout.hold_released` and `identity.restriction_lifted`. **RULING 1 IS [ADR-180](../decisions/ADR-180.md)'S TEST APPLIED RATHER THAN ITS OUTCOME**: what separates two unanimities is what each is MADE OF, and here it is twelve authored rows, each with its own message and guard and each bound to its `EVENTS` line by a running test, against **one figure typed three times and re-read never**. **RULING 2 IS THE ONE THAT COSTS SOMETHING: THE REPAIR IS NOT `nine` BECOMING `twelve`.** [ADR-034:6](../decisions/ADR-034.md)'s remedy has two branches and [ALLOCATION:197](../decisions/ALLOCATION.md) records that **there is no third**; a corrected number is true for as long as nobody adds a row, **which is exactly how long `nine` was true: three days**. Generating it is refused on a cost the runner PRINTS every run: `CI-06/derivable-counts` already binds the noun **`triggers`** to a query returning **21**, the SQL triggers. **THE DEFECT IS DRIFT AND NOT A TYPO AND THE HISTORY IS THE EVIDENCE**: nine rows from `26546ed0` until **`b6bec035` added three at 2026-08-16 16:05:22**, touching neither plan, and `M10` was edited at **17:51:09 the same afternoon** about something else. **One edit to one table falsified three sentences in two other documents and nothing noticed for twelve days**, which [`gates.mjs:8417`](../../scripts/corpus/gates.mjs) says in its own header it cannot see because the recogniser is value-anchored and fires only *"while it is still RIGHT"*. **FIVE LIVE SITES AND ONLY TWO IN THE FENCE**: [M16:270](../plans/M16-notification-center.md) says nine too, and [`loops.ts`](../../apps/worker/src/integrations/loops.ts) cites both `M10` sentences and quotes one. **NOTHING IS REPAIRED AND BOTH SHAPES WERE EXECUTED**: repairing `M10:31` plainly turns [`integrations-loops.test.ts`](../../apps/worker/test/integrations-loops.test.ts)'s `test 1.3` RED, 1 failed of 34, with its designed message; repairing it under THIS CORPUS'S OWN quote-the-original convention ([M05:145](../plans/M05-payout-system.md)) leaves it **34 passed while its own failure message is false**. **That is green and wrong, and it would disable a control by satisfying its letter inside the entry whose subject is a control satisfied by its letter**, so ruling 3 forbids the quotation and the repair is registered as FOUR FILES AND ONE COMMIT with `1.3` to be RETIRED rather than inverted. **THE DISPATCH'S PREMISE OVERSTATES THE HARM IN ONE PLACE AND THE ENTRY SAYS SO: the figure never fed the generator**, because [session 328](2026-08-28-session-328.md) built `IN-M10-03` from `EVENTS` and shipped twelve. **FOUND AND NOT IN THE DISPATCH: `EVENTS` HOLDS TWO DISAGREEING DEFINITIONS OF THE M10 TRIGGER SET AND NEITHER IS NINE** -- **23 events carry `MAIL`, section 11 carries 12, they overlap in 11**, and `payout.transfer_failed` is a section 11 row with NO `MAIL` tag while being the row two plans lean on for *"always send"*; and **[M10:312](../plans/M10-integrations.md) states a second stale derivable count IN THIS FENCE**, *"twenty-nine migrations"* against **53**, in a sentence opening *"checked against the migrations rather than recalled"*. **NO ADR SIGNED, NO MIGRATION, NO CODE, NO GATE, AND `EVENTS` NOT EDITED.** 33 of 33 gates, 16 of 16 invariants, **214 files / 5,064 passed / 6 skipped**, baseline reproduced at `b3c9cf0d` first, test delta ZERO. -->

**Done:** [ADR-185](../decisions/ADR-185.md), the [ALLOCATION](../decisions/ALLOCATION.md) row `185` amended in place ([ADR-065](../decisions/ADR-065.md) T3), [INDEX](../INDEX.md)'s `M10` row, the [STATE](../STATE.md) append, this log and its generated `README` row.

**Next:** the registered repair is **four files and one commit** and belongs to the next session holding `apps/worker`: `M10:31`, `M10:165`, [`loops.ts`](../../apps/worker/src/integrations/loops.ts)'s section 2 header and `ALLOWLIST_UNDECLARED.quote`, and the retirement of `test 1.3`. `M10:312`'s stale migration count rides with it, because three sentences in one plan under one defect class should move in one commit. **[M16:270](../plans/M16-notification-center.md) is a separate amendment** and needs its own number. **The `MAIL`-versus-section-11 divergence needs an `EVENTS` ruling** and is the larger of the two findings.

**Blockers:** none for this entry. **The repair is blocked on a fence and not on a question**: the ruling is settled and the commit that applies it is specified file by file.

**Landmines:** **the quote-the-original amendment convention silently defeats any test that asserts a document's wording by substring**, and that is now demonstrated rather than suspected. `test 1.3` is one instance; the convention is corpus-wide and so is the class. **A session that repairs `M10:31` and leaves `1.3` standing ships a green suite asserting the opposite of what the tree says.** Second: `ADR-183` and `ADR-184` are reserved and unwritten at the time of writing, so this branch carries `185` with two holes below it; gaplessness is asserted over allocated plus reserved and passes, but a reader of `decisions/README.md` sees `182` then `185`. Third: **this entry's own citations into `ALLOCATION.md` are line-anchored at `:197`**, and its row is at `:177`, so a later session that grows row `185` past one line moves them.

**Files touched:** `docs/decisions/ADR-185.md`, `docs/decisions/ALLOCATION.md`, `docs/decisions/README.md` (generated span), `docs/INDEX.md`, `docs/STATE.md`, `docs/sessions/README.md` (generated span), this file.

---

### 1. The count, derived at the source and not inherited

**The dispatch stated no number on purpose and this session derived one.** Read between [`EVENTS:396`](../architecture/EVENTS.md) and section 12 at [`EVENTS:415`](../architecture/EVENTS.md), taking every line that opens a backticked event name:

| # | Trigger | # | Trigger |
|---|---|---|---|
| 1 | `account.provisioned` | 7 | `payout.transfer_failed` |
| 2 | `phase.passed` | 8 | `payout.held` |
| 3 | `phase.pass_deferred_consistency` | 9 | `payout.hold_released` |
| 4 | `breach.detected` | 10 | `identity.restriction_lifted` |
| 5 | `payout.approved` | 11 | `account.graduated` |
| 6 | `payout.settled` | 12 | `kyc.rejected` |

**Twelve.** `M10:31` and `M10:165` say nine.

---

### 2. Why it is drift rather than a typo, which is the finding

**The sentence was TRUE when it was written**, and a reader who assumes carelessness repairs the cheap side.

| Commit | Date | Rows in section 11 |
|---|---|---|
| `26546ed0` | 2026-08-13 | **9** |
| `9db4fa96` | 2026-08-16 15:12 | **9** |
| **`b6bec035`** | **2026-08-16 16:05:22** | **12** |
| `main` today | 2026-08-27 onward | **12** |

`b6bec035` added the three rows and touched neither plan. `af6fbf56` edited `M10` at **17:51:09 that afternoon**, eight insertions and four deletions, about `OQ-M10-06`, and did not re-read the count. `M16:270`'s sentence dates to `9a0c737d` on 2026-08-14, when section 11 also carried nine.

---

### 3. Both shapes executed, and the one that stays in the fence is refused

| Shape | `apps/worker/test/integrations-loops.test.ts` | Honest? |
|---|---|---|
| **A.** repair `M10:31`, no quotation | **1 failed, 33 passed**, only `1.3`, with its designed message | **YES**, and it leaves the branch red |
| **B.** repair `M10:31`, keep the old wording quoted per [M05:145](../plans/M05-payout-system.md) | **34 passed** | **NO** |

**Shape B is the finding.** The convention guarantees the superseded wording survives in the file, so a substring assertion over that file cannot tell a live claim from a quoted dead one. **`1.3` would pass while asserting, in its own name, that `M10` still says nine.**

---

### 4. Verification

Each command run separately, `pnpm run verify` never run, baseline reproduced at `b3c9cf0d` before a line changed.

| Command | Baseline | After |
|---|---|---|
| `gates.mjs generate && check` | 33 of 33 | 33 of 33 |
| `repo-invariants.mjs` | 16 of 16 | 16 of 16 |
| `pnpm run lint` | 0 | 0 |
| `pnpm run format:check` | clean | clean |
| `pnpm vitest run` | 214 files / 5,064 passed / 6 skipped | 214 files / 5,064 passed / 6 skipped |
| `falsify.mjs` | clean | clean, tree clean after |

**The test delta is zero because no test was touched**, which is the point of the entry rather than an omission from it.
