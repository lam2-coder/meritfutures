---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 566: the blocker whose support expired while the blocker stood, and a check measured rather than extended

<!--index: **THE ROW IS [ADR-371](../decisions/ADR-371.md), `proposed` and UNSIGNED. MONEY-ADJACENT (the expiry sits in the entry SEVEN ports reduce to, four of them through `principal(request)`), `E2` READ OWED.** [ADR-370](../decisions/ADR-370.md) found one blocker discharged seven rows earlier by a row that was not about it; this row asked whether it was the only one. **SCOPE DERIVED AND STATED RATHER THAN TAKEN FROM THE DISPATCH**: 68 of the 69 ADRs numbered 300-379 mention a blocker, `BLOCKED` carries **14 entries, 86,352 characters and 208 absence-shaped sentences**, `CRON_INVENTORY` carries **21 job rows**, `docs/edge-cases/` **137 entries**, so two filters cut it and both are argued in the open: **only a claim about THIS TREE can expire silently** (a payment rail or a vendor call cannot go stale without the founder knowing), and **prefer the registers that DISPATCH**, of which ADR-370 read one (`schedule.ts`) and left two. **AND WITHIN `BLOCKED` THE INTUITION IS BACKWARDS**: [ADR-358](../decisions/ADR-358.md)'s mechanism repairs the clause a row was dispatched about, so the 31,339-character entry that self-corrects in six places has BEEN re-read and the 366-character one has not, which is why the four smallest were read in full alongside the root. **RULED: FIFTEEN OF SIXTEEN HOLD AT PRIMARY SOURCE AND EVERY ONE IS RECORDED AS CHECKED so the next row need not re-check it** (no `src/` caller of `runReplayAudit` or `runProvisioningSaga`, both only barrel re-exports; zero `INSERT INTO statistic_definitions` anywhere; `detector.run_degraded` absent from `EVENTS.md` AND `apps/api/src/events.ts`; no writer of `report_schedules` in any `src/`, migration or seed; `UNWIRED_LIVE_INGEST_IO` still the only `LiveIngestIo`; `OperatorAssertionVerifier` still with NO implementation; `refusingAssertionVerifier` and `operatorFromAssertion` still with zero callers outside their suite; `packages/kyc` one fake, `packages/psp` two, `packages/enrichment` the same, no vendor adapter in any; `productionKycDeps` still both-null; all five `useCertificateRevokeBackend` citations exact). **RULED: ONE HAS EXPIRED AND WHAT EXPIRED IS THE SUPPORT AND NOT THE BLOCKER**, which is a different failure from ADR-370's and is separated on purpose. **`wiring.test.ts:363` cites `start.ts:206` and `:245` for the claim that `INV-M11-05`'s limit is absent, AND IT WAS EXACT WHEN WRITTEN**: both parents of `6decbaeb` (2026-09-05 20:31:49), the merge that authored the line, carry that sentence at exactly those two lines, measured with `git show`. **`149e1595` (2026-09-05 19:50:21), NOT AN ANCESTOR OF IT, then added 83 lines to `start.ts` and rewrote the paragraphs**, so the pointers now land in an argument about the certificate image source: ADR-358's mechanism arriving through a MERGE between two branches that could not see each other rather than through a rewrite. **THE CLAIM IS FALSE IN HALF** (the per-IP dimension is built at `src/certificate-rate-limit.ts` and installed at `start.ts:315`; the per-ASN half is still owed and `start.ts:181-186` records it owed) **AND THE GROUND IT SUPPORTS SURVIVES**, because `RATE_LIMITED_ROUTES` is `['verify', 'certificate_image']` and reaches no admin route and `start.ts` installs no second limiter, so an anonymous prober is still unbounded on `/admin/*`. **A ROW THAT READ THE CITATION, FOUND THE LIMIT BUILT AND STRUCK THE GROUND WOULD HAVE REMOVED A TRUE REASON ON A FALSE READING**, which is what an expired SUPPORT does and an expired BLOCKER does not. **THREE OTHER CITATIONS LAND TWO OR THREE LINES INTO A DOCBLOCK AND ARE REPORTED AS TOLERATED DRIFT RATHER THAN INFLATED INTO FINDINGS**, which is what keeps the one finding worth reading. **`RI-15` CANNOT CATCH THIS CLASS AND THE REASON IS STRUCTURAL**: its name-anchoring leg fires only on a BACKTICKED NAME and this citation's subject is a QUOTED SENTENCE, so only its three weak legs applied and the path resolves, the file reaches 206 of 317, and the line is a comment. **`RI-35` WAS MEASURED AND DELIBERATELY NOT EXTENDED**, derived by importing the check: **19 artifacts, 30 claims, and only TWO artifacts carrying any needle at all**, with **NO registered artifact naming `BatchPorts`, `postgresBatchPorts` or `runReplayAudit`**, so leg 6 swept `schedule.ts` and had nothing to fire near; and lifting its `test/` exclusion, **whose stated premise is false for exactly one file and it is the one that matters**, would fire on **2 of that file's 35 absence-marker lines**. **SO THE HONEST REMEDY IS REGISTRATION AND NOT MACHINERY, AND `absence-claims.mjs` WAS NEVER OPENED FOR WRITING**; two registrations are named as owed instead. **[ADR-370](../decisions/ADR-370.md) SECTION 3.1 IS WRONG BY ITS OWN VOCABULARY IN TWO CELLS**, recording `LiveIngestIo` and `WithdrawalApprovalSweepIo` as having no inhabitant of any kind while its own section 3 counts both refusing defaults among its eleven and reserves *no value at all* for `ExpiryEventPort`; **named rather than repaired**, and `CRON_INVENTORY`'s wording for the same fact is the precise one. **THE RETIRED CLAUSE IS NAMED AND NEVER REPRODUCED** (ADR-367's collision reproduces: `grep -c` returns FOUR hits in `start.ts`, three retired quotations and one live and narrower). **NO `src/` FILE WAS CHANGED, no migration taken or reserved, `apps/api/test/wiring.test.ts` and `apps/api/src/start.ts` never opened for writing, no route docblock touched, no gate weakened, no test skipped, disabled or quarantined, no `RI-nn` minted, no `CI-06` letter spent, no `status` field moved.** **ONE NEW TEST FILE, TWO CASES, TWO SEEDS WATCHED RED AND BOTH RESTORED BYTE-IDENTICAL under `sha256sum -c`**, one in the GOOD NEWS direction and one in the HARM direction; **and seed 2 found a real weakness in the case written to catch it**, `toContain` being satisfied by the install COMMENTED OUT, now anchored to the start of a line. **THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL** and the tree was attached and cloned by hand, the condition sessions 550, 553, 557, 558, 561, 562 and 565 recorded. **NO `mcp__github__*` TOOLS, NO `gh` ON PATH, `api.github.com/repos/lam2-coder/meritfutures` ANSWERS 403 while `git` READ AND `git push` AUTHENTICATE**; the branch is pushed and **no pull request was opened, by instruction**. Counts derived at reporting time. -->

---

### 1. The objective

Row `371`: [ADR-370](../decisions/ADR-370.md) found a recorded blocker that had been discharged seven rows earlier by a row that was not about it. **Ask whether that is the only one.**

**One objective.** It resolved into one expiry of a different kind, fifteen blockers recorded as checked, two structural answers about why the existing checks missed it, and a decision not to build anything.

---

### 2. What was derived before the first edit

**The baseline was measured on `133315cb` with a clean tree, before anything was touched.**

| | |
|---|---|
| Suite | **332 files / 8,036 passed / 10 skipped / 0 failed** |
| Gates | **33 of 33** |
| Invariants | **35 of 35** |

**The scope was then derived rather than taken from the dispatch**, which said so of its own figure. Section 1 of the entry carries the five counts.

---

### 3. THE RULING

[ADR-371](../decisions/ADR-371.md) section 2 holds it in five clauses. The three that carry the row:

**RULED: FIFTEEN OF THE SIXTEEN BLOCKERS READ AT PRIMARY SOURCE STILL HOLD**, each cited in section 3 of the entry **so that the next row need not re-check them**, which was the dispatch's own instruction and is half the value of the row.

**RULED: ONE HAS EXPIRED, AND WHAT EXPIRED IS THE SUPPORT AND NOT THE BLOCKER.**

**RULED: `RI-35` COULD HAVE CAUGHT ADR-370's CASE AND WAS NOT EXTENDED, BECAUSE THE MEASUREMENT SAYS REGISTRATION IS THE REMEDY.**

---

### 4. What landed

| file | what |
|---|---|
| [`docs/decisions/ADR-371.md`](../decisions/ADR-371.md) | **new.** The ruling, `proposed`, UNSIGNED |
| [`apps/api/test/blocker-support-expiry.test.ts`](../../apps/api/test/blocker-support-expiry.test.ts) | **new. +2 cases.** What ground two actually rests on, bound in both directions |
| [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) | row `371` taken, written single-spaced |
| `docs/decisions/README.md`, `docs/STATE.md`, `docs/sessions/README.md` | the registry row and the generated spans |

**No file under any `src/` was changed.** Two falsification seeds edited `src/` and both were restored from a byte copy with `sha256sum -c` verifying it, which is recorded here rather than left to be inferred from a clean `git status`.

**`docs/INDEX.md` was NOT changed**, on the disposition [session 557](2026-09-06-session-557.md) recorded and sessions 561 and 565 followed.

---

### 5. Falsification

**Two seeds, both watched RED and both restored byte-identical.**

- **Seed 1 runs in the GOOD NEWS direction.** An admin member added to `RATE_LIMITED_ROUTES` reddens case 1, which is the day ground two stops being a reason to decline the port and the entry needs re-reading.
- **Seed 2 runs in the HARM direction** and **found a real weakness in the case written to catch it.** With the limiter install commented out in `start.ts`, case 2's `toContain` was still satisfied by the commented line, so the seed passed the case and reddened only its neighbour. Anchored to the start of a line it reddens both, and the reason is a comment in the file rather than only here.

---

### 6. Blockers and landmines

- **The founder's `E2` read of [ADR-371](../decisions/ADR-371.md) is owed.** Its approval block names three decisions, the second of which is a request NOT to build something.
- **OWED AND OUTSIDE THIS FENCE: [`apps/api/test/wiring.test.ts:363`](../../apps/api/test/wiring.test.ts).** The two pointers must move and the sentence must be split into its built half and its owed half. **THE VERDICT MUST NOT MOVE.** Entry section 6 item 1 states it precisely enough to be picked up, including which site to cite and why the retired clause must be NAMED rather than quoted.
- **OWED AND OUTSIDE THIS FENCE: [`docs/ops/runbooks/CRON_INVENTORY.md`](../ops/runbooks/CRON_INVENTORY.md)'s Replay self-audit row**, which [ADR-370](../decisions/ADR-370.md) section 7 already stated and which is **still uncorrected on this tree**. Re-confirmed here, not re-discovered.
- **OWED: two registrations for `RI-35`**, named in entry section 6 item 3. The row that takes them owns `packages/tooling/checks/absence-claims.mjs` and must fence `packages/tooling/test/` with it.
- **LANDMINE: `RI-15` has a third hole and it is structural.** A citation whose subject is a QUOTED SENTENCE carries no backticked name, so the anchoring leg never fires and only the three weak legs apply. **No register entry closes this**, which is why it is recorded rather than repaired, on ADR-369's precedent one row over.
- **LANDMINE: seventeen of `RI-35`'s nineteen artifacts carry no needle.** Leg 6 is the leg that finds UNREGISTERED claims and it is driven by two facts about the queue's door. **Widening its walk while the needles are empty is motion**, and this row measured that before declining to do it.
- **LANDMINE: 42,877 characters of `BLOCKED` were read for citations only.** `usePayoutBackend` and `useWithdrawalBackend` were swept for `file:line` pointers and their interiors were not read sentence by sentence. **The argument for skipping them is in tension with ADR-358 and the entry says so in section 8.**
