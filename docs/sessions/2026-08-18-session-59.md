---
status: approved
depends_on: [README.md]
last_updated: 2026-08-18
---

## 2026-08-18 - Session 59: ADR-056's central mechanism does not reproduce, and the ruling is rewritten around the argument that does (non-money, ADR only)

**Written as a stub first, so the session was on the record before it had a result, and completed in
place.** The fence is `docs/` and this file.

- **Objective:** [PR #90](https://github.com/lam2-coder/meritfutures/pull/90) executed
  [ADR-056](../decisions/ADR-056.md)'s central claim and it **does not reproduce**. Verify the refuting
  fact against `floor.ts` directly, then **rewrite the ADR rather than withdraw it**: its CONCLUSION
  reproduced and the surviving argument becomes the primary one. Fold in the three narrowings #90
  recorded. `ADR-056` stays `proposed` and unsigned.

- **Done: [ADR-056](../decisions/ADR-056.md) is rewritten in place, `status: proposed`, founder line
  still unsigned.** The **ruling wording, the consequences and the fixture section are unchanged**; the
  argument underneath them is not. **17 of 17 gates pass.** No engine file, no test and no fixture was
  touched, and the diff is four files, all under `docs/`.

  | What moved | To what |
  |---|---|
  | **The section "Why ADR-052 makes this sharper than it was" and its three-row breach table** | **DELETED.** Replaced by a section that quotes the refuted sentence verbatim, states that it is false, cites the run, and reports what was measured instead |
  | **The interlock argument, previously one paragraph of Context** | **PROMOTED to the ruling's primary argument**, with the 232-to-zero re-lock measurement attached to it |
  | **"a live path on every v1 plan"** | **"TWO of the three"**, with Direct's absence stated as an absence |
  | **The lead paragraph's exposure sentence**, which read "the correction would breach every funded account" | The correction freezes every funded floor at its opening value for life, **without a breach, an assertion or a refused row to announce it** |
  | **The `Alternatives considered` entry for carrying the lock**, which asserted the second-funded-day breach | Rewritten to the measured defect, and it now says in its own words that the carried lock is **more permissive**, not less |
  | **The founder-approval paragraph**, which asked for a run before the signature | The condition is recorded **DISCHARGED**: the run happened and refuted the claim, which is the condition working rather than failing |
  | **A new consequence row** | [`0037`'s item 4](../../packages/db/migrations/0037_supersede_rule_states_high_water_bounds_balance.sql) is unblocked by acceptance and is the **detection** half of the ruling |
  | **[STATE](../STATE.md)'s founder-item row**, which restated the refuted breach as fact | Repaired to the refutation and the surviving argument. **The item stays OPEN**: it closes on the signature, not on the run |

### The refuting fact, verified twice and independently of #90

**Read first.** [`floor.ts`](../../packages/rules-engine/src/day/floor.ts) line 116 guards the `R-15`
block with `!floorLocked`, and line 99 opens `advanceFloor` with `floorCents = input.priorFloorCents`.
On an already-locked day neither `R-13` nor `R-15` is entered and the prior floor is returned untouched.

**Then executed**, because reading a guard is how the original claim was made. `advanceFloor` was
called once with `priorFloorLocked: true`, `priorFloorCents: 4,750,000c` and a CORE-50K funded
drawdown, on a green day closing 5,100,000c:

| Returned | Value | What it refutes or confirms |
|---|---|---|
| `floorCents` | **4,750,000c** | **ADR-056 predicted 5,010,000c.** The locked floor is not re-assigned |
| `highWaterBalanceCents` | **5,000,000c**, against a close of 5,100,000c | The `hwb` freeze, and the row `0037` exempts |
| `lockEngagedAtProfitCents` | **null** | No re-lock event on a locked day, which is the 232-to-zero measurement in one call |

The script lived in the session scratchpad and not in the repository, so the tree carries no new file
from it. The two committed test files from #90 were also re-run and are green, 16 of 16.

- **Next:** **the signature, and nothing in this session brings it closer than the run already did.**
  On acceptance the amendment sites are unchanged from the ones the ADR has always named:
  [M01](../plans/M01-rules-engine.md) section 1.5's `INV-07` row, section 3.5's `R-31` and `R-15` rows,
  section 8's `RE-P-02` row, and the one fixture that crosses the pass and pins `floor_locked: false`.
  **Acceptance also unblocks [`0037`'s item 4](../../packages/db/migrations/0037_supersede_rule_states_high_water_bounds_balance.sql)**,
  the cross-row `hwb` freeze trigger that was deliberately not written while `INV-07` versus the reset
  was open, and that trigger is the only thing that would make the carried-lock state **detectable**
  rather than merely wrong.

- **Blockers: approval is PENDING by design**, and this session did not touch it. The ADR amends a
  frozen invariant, a frozen rule's effects list and a frozen rule's permanence clause, all on the
  money path ([E2](../../MERIT_BUILD_MASTER_PROMPT.md)).

- **Landmines:**

  - **THE REFUTED SENTENCE IS STILL IN THE ADR, ON PURPOSE, AND A LATER READER WILL BE TEMPTED TO
    TIDY IT AWAY.** It is quoted as a blockquote where its section used to be, under a heading that
    says it is false. **A ruling that quietly drops a wrong argument teaches its next reader nothing**,
    and this particular wrong argument is instructive twice over: it was a **correct reading of
    `floor.ts`'s header and a wrong reading of its code**, and the header is still there saying
    `floor(d) = floorLocked ? floor_lock_floor_at_cents : max(...)`. Anyone who re-derives the
    counterfactual from the comment will get ADR-056's original answer again. The quote is what stops
    that being a rediscovery.

  - **THE LEAD PARAGRAPH CARRIED THE REFUTED CLAIM AND SITS ABOVE THE SECTION THAT WAS DELETED, WHICH
    IS WHERE THIS REWRITE WAS MOST LIKELY TO MISS IT.** Line 5 read "the correction would breach every
    funded account". Deleting the named section and its table would have left that sentence standing
    as the ADR's **first** statement of the defect, in bold, three lines from the title. **The
    instruction named the section and the table; it did not name this sentence**, and a rewrite scoped
    literally to what was named would have shipped the false claim in the most-read position in the
    file.

  - **[STATE](../STATE.md) RESTATED THE REFUTED BREACH AS FACT AND NOTHING POINTED AT IT.** The
    founder-item row said "so the account breaches on its second funded day" with no hedge.
    [Session 57](2026-08-18-session-57.md) deliberately did not amend STATE, and its reasoning was
    right about the thing it was reasoning about: **the founder ITEM closes on the signature, not on a
    run.** The side effect was a false sentence living in an `approved` document for a day. **The two
    are different acts**: closing the item is the founder's, repairing a restatement the run falsified
    is not a ruling and needs no signature. The row now records the refutation and says in its own
    words that it is **still open**.

  - **[ALLOCATION](../decisions/ALLOCATION.md)'s `056` row ALSO INVOKES ADR-052 AND IS DELIBERATELY
    LEFT ALONE.** It reads that ADR-052 "sharpened it: the lock is now an assignment rather than a
    term, so what happens to it at the reset is a harder question than it was", which is **not false**
    and asserts no breach. Editing a registry three branches are appending to, for no change in truth
    value, is a merge conflict for nothing, which is [session 58](2026-08-18-session-58.md)'s own
    reasoning applied to the same file. **Named here so it is fixable by whoever is next in it.**

  - **THE COUNTS IN THE ADR ARE ONE RUN'S AND ARE NOT REPRODUCIBLE TO THE DIGIT.** 3,442 folded funded
    days, 1,790 shipped breaches, 1,485 carried, 232 re-locks, 2,000 streams. The sweep asserts each
    `> 0` rather than pinning it, deliberately, so a re-run yields different numbers and **that is not
    drift**. The ADR says so in the sentence that carries them. What is structural, and what the
    argument actually rests on, is the ordering: both accounts leave `R-31` with the same floor, the
    carried one is frozen by two `!floorLocked` guards, the shipped one is non-decreasing, so the
    carried floor is at or below the shipped floor on every day.

  - **DIRECT-50K's NARROWING IS AN ABSENCE AND READS LIKE A REPRIEVE.** "The defect does not reach
    Direct" is true and is the wrong sentence. `phase_eval.enabled` is `false` with a null
    `profit_target_cents` (Appendix A.3), so Direct **opens funded and `R-31` never runs on it**: the
    transition `INV-07` is being amended against does not exist there. A reader who files Direct under
    "unaffected plans" will look for the same exemption on a future plan that does have an eval phase
    and not find it. **Merit Rapid was checked rather than assumed**, in Appendix A.2 and not only in
    the test transcription: eval profit target 300,000c and floor lock at profit 260,000c at 50K, both
    identical to Core, so its every eval pass is a lock day too.

  - **`0037` IS NOT WRONG AND THE ADR HAD TO SAY SO WHILE USING IT AS THE EVIDENCE.** Its
    `floor_locked OR high_water_balance_cents >= balance_cents` **correctly** stops refusing the rows a
    legitimately locked account produces, which is `GS-016`'s expected end state and the whole reason
    `0037` exists. Its side effect is that a carried-lock row is **indistinguishable from a legitimate
    one**. The finding is not "a constraint is too weak", it is **a right constraint hiding a wrong
    state**, and writing it the other way would send the next session to strengthen a migration that
    should not move.

  - **THE SILENT-FAILURE NARROWING IS THE ONE THAT CHANGES THE RISK AND IT ARRIVED THIRD IN A LIST OF
    THREE.** #90 recorded it alongside two narrowings that shrink claims. This one does not shrink
    anything: it says that under the literal reading there is **no breach, no assertion, no refusal and
    no rejected row**, so the state would be stored, published and settled against for the life of every
    funded account, in the trader's favour, with the first evidence a payout nobody could account for.
    **A defect that trips a constraint the day it ships is bounded by its own noise. This one has
    none**, and that is why it got a consequence row of its own rather than a clause in the table.

  - **THE ADR'S TITLE, RULING, CONSEQUENCES AND FIXTURE SECTION SURVIVED THE REWRITE UNCHANGED, AND
    THAT IS THE EVIDENCE FOR NOT WITHDRAWING IT.** A ruling whose conclusion needs a new wording after
    its central argument fails is a different ruling and should have been a different number. This one
    needed a different **argument** for the same sentence. **The number, the amendment sites and the
    unsigned line are all the same as they were**, which is what a founder comparing the two versions
    needs to be able to see at a glance.

- **Files touched:** [ADR-056](../decisions/ADR-056.md), [STATE](../STATE.md),
  [sessions/README](README.md) and this file. **Four commits, each pushed immediately**, the first of
  them this log as a stub before any work. **No `adr_count` span moved**, because no ADR was added or
  removed and the status did not change.

### What was NOT done, named rather than implied

**The ADR was not signed and no amendment site was touched.** All five remain conditional on
acceptance, exactly as they were before this session, and amending them on a rewrite would take the
decision on a session's authority while the line is still blank.

**No test, no fixture and no engine file was edited.** The verification script ran from the session
scratchpad against the tree as committed. `git diff --stat -- packages/` is empty for this branch.

**The fixture the ADR names is still unwritten**, and the rewrite does not change what unblocks it: the
signature does, not a run and not a better argument.
## 2026-08-18 - Session 59: the two group A rows the transcribed half day unblocked, and the four rules it did not (non-money, fixtures only)

**Written as a stub first, so the session was on the record before it had a result, and completed in
place.** The fence is `packages/rules-engine/fixtures/**` and this file, plus the one row in
[`docs/sessions/README.md`](README.md) that `CI-06n` requires for this entry.

- **Objective:** [PR #92](https://github.com/lam2-coder/meritfutures/pull/92) took
  [`cme-2026.json`](../../packages/rules-engine/fixtures/calendars/cme-2026.json) from seven bare
  weekdays to eighty-three sessions carrying four transcribed holidays, two early closes and both
  absorption shapes. **Re-derive** which of registry groups A, F and H that unblocks against the
  registry rows rather than inheriting PR #92's claim, then write the group A fixtures that are now
  writable.

- **Done:** **Two fixtures, [GS-003](../../packages/rules-engine/fixtures/GS-003-half-day-counts-as-a-full-trading-day.yaml)
  and [GS-032](../../packages/rules-engine/fixtures/GS-032-half-day-the-trader-sat-out.yaml), both
  grading `direct` and green.** The directory goes 34 to 36 and group A goes 1 citing fixture to 3.
  17 of 17 gates pass, `falsify` holds all 17 clean-and-dirty, 789 tests pass. **The re-derivation is
  the larger half of the session and it disagrees with the objective**, so it is stated first.

### The objective named four rules and not one of them is writable

**R-01, R-02, R-05 and R-06 were named as "the group A fixtures that are now writable". The two rows
that actually came unblocked are `R-03`'s.** A group named by RULE and a group named by SCENARIO do
not have the same members, and [M01 section 3.5](../plans/M01-rules-engine.md)'s coverage column is
the mapping between them. Every cell below was checked against its own primary source, not against
[session 58](2026-08-18-session-58.md)'s report:

| Rule | Coverage column | Writable | The primary source |
|---|---|---|---|
| **R-01** fill containment | GS-001, GS-030 | **NO** | `DailyMark` is `{tradingDay, opening, closing, high, low, realizedPnl, adjustment, fillCount, sourceHash}` ([`types.ts`](../../packages/rules-engine/src/types.ts)) and carries **no instant**. [`rules.ts`](../../packages/rules-engine/src/rules.ts) files R-01 among the four undeclared rules: "transcribing the CME year adds rows, not columns" |
| **R-02** counter advance | GS-002 | **ALREADY WRITTEN**, batch 3. It was the only group A fixture in the directory |
| **R-03** half day | GS-003, GS-032 | **YES. THIS SESSION.** `kind: "half"` maps to `CalendarDay.isHalfDay` in [`calendar.ts`](../../packages/golden-loader/src/calendar.ts), `R-03` is in `IMPLEMENTED_RULES`, and `2026-11-27` is transcribed |
| **R-04** halted | GS-004, GS-031 | **NO** | `calendarRowsFromRecord` writes `halted: false` as a constant. No calendar moves it |
| **R-05** session bounds | GS-030 | **NO** | `CalendarDay` is `{tradingDay, isHalfDay, halted, sequence}`. Session bounds are two columns it does not carry, and R-05 is undeclared |
| **R-06** last closed day | GS-035 | **NO**, and it is the one row whose blocker is not the engine. **R-06 IS declared.** GS-035 is "payout at 23:59:59 versus batch at 00:05": it needs a **clock**, `INV-01` forbids the engine from reading one, and it has been a group B row all along |

**So the objective's four rules and the two writable rows do not intersect at all.** The rules named
are the group A rules a calendar cannot reach; the rows that moved are the ones a calendar was the
only thing holding. **The re-derivation agrees with session 58's table row for row**, which is worth
saying plainly: the instruction to re-derive rather than inherit found no error in what was inherited,
and the check was still the right one to run because the objective built on top of it was wrong.

**Groups F and H gained nothing, re-checked and unchanged.** Every group H row is behind `L-11`'s
settlement block, which needs `payout_request_id`, `ordinal`, `approved_cents`, `basis_trading_day`
and `effective_trading_day` and has no calendar clause at all. Group F's one calendar row, `GS-059`,
lost its calendar clause when the file spanned three holiday clusters and keeps `cadenceAnchorDay`,
which only a settlement sets. `GS-060` and `GS-080` are `compare.ts` diffing flat fields against a
nested `engine_gates`.

### The absorbed session is the part of the objective that cannot be written, and it is not close

The objective called it "the one worth pinning": trade date `2026-09-08` opens Sunday `2026-09-06` at
17:00 CT where a naive `session_rule` computes Monday 17:00, so a fill in that gap lands in no
session. **That is real, it is [ADR-055](../decisions/ADR-055.md)'s `absorbs_into`, and it is
`R-01`'s** — the first row of the table above. The format has no instant to place, the record states
no open to place it against, and `advanceDay` reads neither. The calendar file says so in a key of
its own (`what_this_format_cannot_carry`); this session re-checked it against `DailyMark` rather than
believing it, and `DailyMark` has nine fields and none is a timestamp.

**What a fixture CAN see of a holiday is the absence**, which is why both files sit on the
Thanksgiving week: `2026-11-26` is not a session, and `2026-11-25` and `2026-11-27` are adjacent in
`sequence` two calendar days apart. That is R-02's distinction, and **neither file pins it**, because
`sequence` subtraction reaches no field this format carries — the same sentence GS-002's sibling
already had to write.

### The two fixtures

| Fixture | What it asserts that no earlier fixture does |
|---|---|
| **GS-003** | **An early close advances every counter exactly as the full session before it does.** Both days realize 30,000c on 3 fills, so the 2 and 2 says the half day contributed 1 and 1; an engine scoring it as a fraction reads 1 and 1. `high_water_balance_cents` 5,060,000 and `floor_cents` 4,810,000 are a second measurement that the half day reached DO-7 and not only DO-6 |
| **GS-032** | **The same claim in the other direction**, Appendix B4 item 3. The half day carries no fills: `trading_day` reaching `2026-11-27` says it was evaluated, the counts holding at 1 and 1 say R-08's strict `>` and R-09's `>=` answered as on any full session with no fills, and R-14 carries the floor across unchanged |

- **Neither file rests on a synthetic day, and that took choosing.** 78 of the calendar's 83 sessions
  are invented. `2026-11-25` (full, 16:00 CT), `2026-11-27` (early close, 13:45 CT) and the absence of
  `2026-11-26` are transcribed from the Thanksgiving artifact; `2026-11-30` is not. **A three-session
  walk through `11-30` was the more vivid fixture and its count of 3 would have depended on a day no
  artifact states**, so it was not written. `opened_on` is `2026-11-25`, the account's own first
  tradeable day ([ADR-051](../decisions/ADR-051.md)), rather than the synthetic session before it.

- **Next:** the group A rows that remain are `GS-001`, `GS-004`, `GS-030`, `GS-031` and `GS-049`, and
  **none of them is unblocked by anything inside this fence**. `GS-004` and `GS-031` need a `halted`
  key on the calendar record and a loader mapping with it, which is the cheapest of the five and is
  one session in `packages/golden-loader` plus the record format. `GS-001` and `GS-030` need an input
  the engine's types do not carry and are not fixture work at all. **The other cheap thing this
  session did not do is `2026-12-24`**, the second transcribed `half` day, which no fixture names.

- **Blockers:** none inside the fence.

- **Landmines:**

  - **THE OBJECTIVE'S RULE LIST WAS WRONG IN A WAY THAT LOOKS RIGHT, AND THE SHAPE IS THE CORPUS'S
    OWN RECURRING ONE.** "Group A" is M01's `Group A: time and calendar`, which is R-01 to R-06. The
    HELD-BACK rows filed under group A are `GS-001`, `GS-003`, `GS-004`, `GS-030`, `GS-031`, `GS-032`
    and `GS-049`. **Reasoning from the rule ids to the writable scenarios skips the coverage column,
    and the coverage column is where the two sets stop matching**: R-02's scenario was written months
    of batches ago, R-06's scenario is a clock scenario in group B, and R-01's and R-05's scenarios
    are blocked on the engine's input types rather than on the calendar. This is the same step
    [session 47](2026-08-16-session-47.md) named and session 48 found again: a correct observation
    about one register carried one unchecked step into another.

  - **`GS-032`'S REGISTRY ROW SAYS "IDENTICAL TO GS-003 WITH THE REAL CALENDAR" AND THIS DIRECTORY
    HOLDS ONE CALENDAR.** Taken literally that is GS-003 written twice under a second id, which is
    the mirror of `GS-049`'s objection rather than an answer to it. The two files differ by the half
    day's fill count instead. **It is a reading of the row and it is recorded as one inside GS-032's
    own sibling**, so a founder who disagrees can see what was decided rather than infer it from two
    files that look like an accident. **If the reading is rejected, the fix is GS-032 alone**;
    GS-003 stands on its registry row's own words either way.

  - **CITING `R-01` ON EITHER FILE WOULD HAVE ASSERTED IT TO FAIL, AND THE NEAR MISS IS WORTH THE
    LINE.** [ADR-048](../decisions/ADR-048.md) derives polarity from the citation: a fixture citing
    a rule the engine does not declare derives `inverted` and the stage then requires it to FAIL.
    GS-003's registry row is about Thanksgiving Friday, `R-01` is group A's first rule and is about
    the same day's fills, and it is undeclared. Both files cite `R-03`, `R-02`, `R-08` and `R-09`
    and nothing undeclared. **The mechanism works and would have caught it**; what it would not have
    done is explain the red.

  - **THE DIRECTORY'S OWN COUNT HAD DRIFTED BEFORE THIS SESSION AND THE STAGE HAD BEEN PRINTING THE
    TRUTH THE WHOLE TIME.** [`fixtures/README.md`](../../packages/rules-engine/fixtures/README.md)
    carried **33** in its count table and **34** in its reconciliation table twelve paragraphs below,
    and the directory held 34: batch 8 landed `GS-081` and updated one of the two. The lede sentence
    that carries the stale number is the sentence reading "the count is the stage's own, re-derived
    on every run **rather than written here**". Found by running the stage, not by reading the file.
    Both generations are kept stacked, with the new figures beside them.

  - **`CI-06n` FAILED ON THIS SESSION'S OWN LOG AND THAT IS THE GATE WORKING.** A stub written before
    any work has no row in [`docs/sessions/README.md`](README.md) until somebody writes one, and the
    gate refuses an entry file with no row. The row was added and the gate went green. Recorded
    because the stub-first pattern will hit it every time and the fix is one line.

- **Files touched:**
  [`GS-003-half-day-counts-as-a-full-trading-day.yaml`](../../packages/rules-engine/fixtures/GS-003-half-day-counts-as-a-full-trading-day.yaml)
  and its `.expected.json`,
  [`GS-032-half-day-the-trader-sat-out.yaml`](../../packages/rules-engine/fixtures/GS-032-half-day-the-trader-sat-out.yaml)
  and its `.expected.json`,
  [`fixtures/README.md`](../../packages/rules-engine/fixtures/README.md),
  [`docs/sessions/README.md`](README.md) and this file. **Five commits, each pushed immediately**, the
  first of them this log as a stub before any work.
