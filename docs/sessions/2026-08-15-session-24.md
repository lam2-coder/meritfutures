## 2026-08-15 - Session 24: S-D, the golden fixture loader and CI-03
<!--index: S-D, the golden fixture loader and CI-03 -->

- **Done.** The loader, the fixture format, the fixture directory, three hand-derived fixtures and the CI-03 stage. **`.github/workflows/golden.yml` rather than `ci.yml`**, which is S-C's file to create and did not exist when this session ran. **S-E was not begun**, per the instruction.
  - **THE LOADER IS A PACKAGE BECAUSE THE PURITY BOUNDARY LEAVES IT NO CHOICE, and the constraint turned out to be the feature.** [P1 section 2.2](../plans/P1-monorepo-scaffold.md) requires that it "reads a directory and imports the engine's public entry point only", and `packages/rules-engine`'s `tsconfig` sets `types: []` so `node:fs` **does not exist inside that package**. A loader living in `packages/rules-engine/test/` could only read a directory by weakening the strongest of the three mechanisms guarding the engine. As `packages/golden-loader` it reaches the engine through the `exports` map, which publishes `.` and nothing else, so **`../rules-engine/src/floor.js` is not resolvable at all**: the obligation is discharged by the module resolver rather than by a reviewer.
  - **TR-02 SAYS THE FIXTURE FAILS BEFORE THE FUNCTION EXISTS, AND `evaluate` IS THE IDENTITY STUB. So the stage ASSERTS the failure instead of suffering it**, and the polarity is **read off the engine by a probe** rather than written into a fixture. While the probe holds, a fixture that **matches** is the finding, because a fixture satisfied by an engine that computes nothing pins nothing. **A per-fixture `pending: true` was the alternative and it is the escape hatch TR-03 forbids.** When M01 lands the probe stops holding and the same three fixtures become live assertions with no fixture edited and no flag removed.
  - **Twelve loader rules, each watched failing on its own seeded violation, plus a positive control.** The seedbed is a **copy of the real fixture directory** rather than a miniature, so a change to the format is a change to what the cases are seeded against. Each case asserts the **rule id** that came back, not that something threw, which is `falsify.mjs`'s criterion at this scale.
  - **The stage was watched failing on two seeds beyond the suite.** An expectation the stub already satisfies gives `expected 0 to be greater than 0` on that fixture's name; a tab inserted into a fixture gives `every fixture loads` with `L-12 ... line 42: tab character`. Both name the seeded finding.
  - **No new dependency.** `yaml` would be a VG-12 admission a session cannot grant itself, so `src/yaml.ts` reads a strictly specified subset and **throws on everything outside it** with a line number. Fifteen out-of-subset constructs are asserted to throw. It refuses decimal numbers by name, which is the no-floats rule enforced at the one place a money figure is written by hand.
- **Next.** **S-C** (CI-01, CI-02, CI-05 with VG-12) and **S-E** (TradingCalendar as data: **money path, fresh session, plan mode**) are the remaining P1 sessions and neither is blocked by this one. **The rest of the fixtures arrive with P2**, when there is an engine to derive an expected end state against.
- **Blockers.** None. Two items need a founder ruling and neither blocks S-C or S-E: the ladder discrepancy and the `yaml` dependency, both below.
- **Landmines.**
  - **THE FOURTEENTH AND FIFTEENTH HAND-MAINTAINED COUNTS FOUND WRONG, and the instruction to derive rather than quote is the only reason they were.** [STRATEGY](../testing/STRATEGY.md) said **255** in two places, section 3.2 and CI-03's row. **The registry defines 257**: derived as distinct `GS-\d{3}` identifiers, **contiguous from GS-001 to GS-257 with no holes**, which is `gs_count`'s own query. 255 is what the registry stood at before the Wave 4 consolidation renumbered the verification-UX pair to GS-256 and GS-257, and STRATEGY was not updated with it. **Both are generated spans now.** They survived because `CI-06g`'s stated coverage gap is exactly this shape: it compares the spans that exist and does **not** sweep for bare numerals adjacent to a registry noun.
  - **THE PLAN SHORTHAND DISAGREES WITH THE APPENDIX IT NAMES AS ITS OWN AUTHORITY, AND THIS ONE IS A PARAMETER RATHER THAN A COUNT.** [GOLDEN_SCENARIOS section 3](../testing/golden-scenarios/README.md)'s `CORE-50K` shorthand says **ladder 8**; [M01 Appendix A.1](../plans/M01-rules-engine.md) says **5** per [ADR-024](../decisions/ADR-024.md), in the same sentence that calls Appendix A "the only place these numbers are defined". Twelve of the thirteen restated values agree; this one does not. **`fixtures/plans/CORE-50K.json` follows the named authority and the discrepancy is reported, not resolved**: a count folds into a span, a plan parameter needs a founder ruling. No fixture reads the field yet, which is the only reason it is not urgent.
  - **THE CORPUS'S FIXTURE FORMAT AND THE SCAFFOLD'S ENGINE TYPES DO NOT AGREE, AND FOUR FIELDS REACH NO ENGINE INPUT.** `account.phase`, `account.opened_on`, `days[].adjustment_cents` and `settlements` are all in the printed example and none is on `EngineInput`. **The choice was between dropping them silently and naming them, and a dropped input on a money path is the worst outcome available**: the fixture states a condition, the engine never sees it, the scenario passes while pinning something else. The loader refuses any field it can neither map nor find on one visible list, `adjustment_cents` may only be `0`, `settlements` may only be `[]`, and `L-14` asserts every entry on the list is still used so it cannot rot into a permanent excuse.
  - **`traded_day` RUNS THE OTHER WAY: the type declares it and the format does not supply it.** [R-08](../plans/M01-rules-engine.md) derives it from `fill_count > 0`, so **a loader computing it would be a loader that has implemented a rule the fixtures exist to check**. The fixture states it. That is a departure from the block GOLDEN_SCENARIOS section 2 prints, and it is written in the fixture's own header rather than left for a reader to notice.
  - **The format is ambiguous about where the expectation lives and the ambiguity is in two approved documents.** Both rule it as "YAML plus an expected end-state **JSON sibling**", and section 2's example then shows `expect:` inline. **The reading taken is that the sibling IS the `expect` block serialized as JSON**, which keeps both sentences true; `L-05` refuses a YAML carrying an `expect` key, because the one shape neither reading permits is a fixture with two of them. **If the founder reads it the other way, `L-05` inverts and the three fixtures move their siblings inline.**
  - **`registryIds()` IS A SECOND EXPRESSION OF `gs_count`'S QUERY, in the runner [OQ-P1-04](../plans/P1-monorepo-scaffold.md) was ruled about.** Unifying them needs `scripts/corpus/gates.mjs` to export a membership helper and this session was scoped out of that directory. **Recorded in the package README with the tiebreak: if the two ever disagree, the loader is the one that is wrong.** The loader's own test asserts contiguity rather than a count, so it does not become a sixteenth place stating a number.
  - **THE CALENDAR IS FIVE SESSIONS AND ITS NAME SAYS `cme-2026`.** TradingCalendar as data is S-E and there is not one calendar row in the repository. The file declares a `coverage` interval and `L-08` enforces it, so a fixture naming a day outside the window is **refused** rather than run against a calendar that does not know about it, which is what stops a five-day file from being mistaken for the year. **When S-E lands this file is derived from the seeded rows, not maintained beside them.**
  - **State hashes are not compared, and it is a decision rather than an omission.** [STRATEGY section 3.2](../testing/STRATEGY.md) has the loader diff field by field "before comparing state hashes". A hash of the **engine's** output can only be obtained by running the engine, which is the direction TR-01 forbids; a hash of the **fixture's** stated end state needs the full state shape, which is M01's. The field-by-field diff is what runs and the hash is an open item rather than a line of dead code.
  - **The reverse inventory direction is off, deliberately.** A registry row with no fixture is CI-06's half of loader rule 2 and would today fail on every scenario the registry defines. It arrives with P2. **The golden stage says so in a test rather than leaving a green check to be read as full coverage.**
- **Files touched:** `packages/golden-loader/` (new: `package.json`, `tsconfig.json`, `README.md`, `src/{index,loader,compare,run,yaml}.ts`, `test/{loader,compare,yaml}.test.ts`, `test/fixtures.golden.test.ts`); `packages/rules-engine/fixtures/` (new: `README.md`, three `GS-nnn-*.yaml` with their `.expected.json` siblings, `plans/CORE-50K.json`, `calendars/cme-2026.json`); `.github/workflows/golden.yml` (new); `pnpm-lock.yaml`; `docs/testing/STRATEGY.md` (two count spans and CI-03's row); `docs/{STATE,SESSION_LOG}.md`. **No `.sql` file was edited, nothing under `scripts/corpus/` was touched, `.github/workflows/corpus.yml` is unchanged, and no file S-C creates was written.**
