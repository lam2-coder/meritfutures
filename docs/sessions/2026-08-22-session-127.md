---
status: approved
depends_on: [README.md]
last_updated: 2026-08-22
---

## 2026-08-22 - Session 127: WAVE-05 `X2`, section 39 gains `covered-elsewhere` and SIX rows move, not seven
<!--index: WAVE-05 `X2`: section 39 gains the `covered-elsewhere` status and **SIX** rows move to it, not the seven `ADR-076` section 6 lists. `GS-072`'s cited block was re-run rather than inherited and is still a skip that throws, so it stays `blocked` with a self-clearing citation. `GS-071` to `writable`, and `GS-049`, `GS-059` and `GS-080` to `written`, which empties `CI06FIXTURE_REGISTER` of all three entries it holds. **`GS-049` carries a disposition and not just a status.** The head counts are recomputed by reading the tree, and the status vocabulary the runner holds is now one term behind the document, which is `X8`'s and is red until it lands -->

- **Objective:** [WAVE-05](../plans/WAVE-05-tier2-fixture-shapes.md) `X2`. [Section 39](../testing/golden-scenarios/39-fixture-status-and-blockers.md) gains the `covered-elsewhere` status defined on [ADR-076 section 1](../decisions/ADR-076.md)'s EXECUTED, the rows that ruling moves carry a re-derived citation, `GS-071` moves to `writable`, three stale rows move to `written`, and every derived count at the head is recomputed from the tree.
- **Done:** **one document, eleven rows, and one of them is the one that did not move.** `GS-073`, `GS-076`, `GS-077`, `GS-078`, `GS-083` and `GS-141` are `covered-elsewhere`; **`GS-072` is not**, and [ADR-076 section 6](../decisions/ADR-076.md) says seven. `GS-071` is `writable` in [`apps/worker/test`](../../apps/worker/test/replay.test.ts). `GS-049`, `GS-059` and `GS-080` are `written`, which is `CI06FIXTURE_REGISTER`'s three entries repaired. `GS-049` carries a written disposition and not only a status.
- **Next:** **`X8` and it now has two owed repairs rather than one** (below, and in the pull request). `X3` and `X4` are session 128 and they move every line number this document states into [`plan-validate.test.ts`](../../packages/rules-engine/test/plan-validate.test.ts).
- **Blockers:** none for this session. **`CI-06/fixture-inventory` is RED on this branch and cannot be made green inside this fence**, which is the finding below rather than a blocker on the work.

### `GS-072` was re-run rather than inherited, and the brief's reading holds

The command was run on this branch, not quoted:

```
pnpm exec vitest run scripts/demo/test/replay-determinism.property.test.ts --reporter=verbose
  PT-06: arrival-order permutation is UNEXPRESSIBLE (the engine exports no replay; P2-8)
↓ PT-06: arrival-order permutation > any permutation of arrival order yields byte-identical stored state
  Tests  8 passed | 1 skipped (9)
```

`replayExists` is `Object.keys(engine).includes('replay')` at [`replay-determinism.property.test.ts:76`](../../scripts/demo/test/replay-determinism.property.test.ts), the `describe.skipIf` is at [`:231`](../../scripts/demo/test/replay-determinism.property.test.ts), and the single body throws. **The row stays `blocked | outside-loader-boundary`**, with its citation rewritten to state the skip, the condition, and that **the block switches itself on the day the engine exports `replay`**, which [WAVE-05 section 3](../plans/WAVE-05-tier2-fixture-shapes.md) already calls self-clearing. Six rows moved. **Nothing was moved because a document said seven.**

### The other five citations were watched executing, which is what the status now means

`covered-elsewhere` is defined in the document as **two conditions**, no golden fixture AND an assertion that RUNS, with a list of three things that look like coverage and are not: a path that resolves, a file containing the `GS-nnn`, and a `describe` that names the row while being skipped. **The third is `GS-072` and it is named there as the case that produced the list.** Every other cited site was executed and seen to PASS rather than opened:

| Row | The assertion that EXECUTES | Watched |
|---|---|---|
| `GS-073` | `RE-D-02` at [`determinism.test.ts:245`](../../packages/golden-loader/test/determinism.test.ts) | 5 cases pass, `TZ=Asia/Kolkata` with `ja_JP.UTF-8` among them |
| `GS-076` | `RE-C-10` at [`plan-validate.test.ts:373`](../../packages/rules-engine/test/plan-validate.test.ts), plus `SEEDS['CV-10']` at `:757` under the oracle `it.each` at `:801` | pass |
| `GS-077` | `RE-C-06` at [`:282`](../../packages/rules-engine/test/plan-validate.test.ts), both bounds both sides, plus `SEEDS['CV-06']` at `:748` | pass |
| `GS-078` | `RE-C-01` at [`:208`](../../packages/rules-engine/test/plan-validate.test.ts), funded AND eval phase, plus `SEEDS['CV-01']` at `:733` | pass |
| `GS-083` | its own `describe` at [`:905`](../../packages/rules-engine/test/plan-validate.test.ts) | 4 cases pass, the first an `idsOf` EQUALITY |
| `GS-141` | the three-plan `it.each` at [`:166`](../../packages/rules-engine/test/plan-validate.test.ts) | 3 cases pass, and the severity half is unasserted, which the row says |

**The line numbers were re-derived from the tree and not copied from [session 123](2026-08-21-session-123.md)'s map**, and the exercise was worth doing for a reason the map could not carry: session 123's map gives the RULE lines in `CV_ASSERTIONS` (`:77`, `:85`, `:92`), and what a `covered-elsewhere` citation needs is the line of the test that RUNS (`:208`, `:282`, `:373`). Those are different lines and the map was right about the ones it stated. Each row also names the test id, because **`X3` and `X4` edit that file next and every number above goes stale**; the id does not.

### `GS-049`: one fixture discharges the row, and the residual is somewhere else

**Decided, in the document, and not deferred.** The fixture carries **all three probe shapes** the registry row names in one 21-day stream and pins two of them whole, including the one it exists for, `profit_needed_to_dilute_cents: 2213334` as a ceiling. What is missing is the third probe's LENGTH, twelve flat days against a hundred, and **that is not a residual because a sibling pins one `end_state` and the end state after twelve flat days and after a hundred is the same state, field for field.** A longer grind buys a longer `events` list.

**One claim in the fixture's own sibling was checked and did not hold.** It names *"`RE-P-01`, `RE-P-03` and `RE-P-04`"* as where the trajectory is asserted over a length that means something. `RE-P-01` executes at [`floor-monotonicity.property.test.ts:259`](../../packages/rules-engine/test/floor-monotonicity.property.test.ts); `RE-P-03`'s content executes as `PT-02` at [`win-days-monotonicity.property.test.ts:153`](../../packages/rules-engine/test/win-days-monotonicity.property.test.ts); **`RE-P-04` executes nowhere.** No property suite in the tree asserts that `tradedDaysCount` is non-decreasing, which is [M01](../plans/M01-rules-engine.md)'s `INV-09`. **The row still moves to `written`**, because the status is a fact about the directory ([ADR-072](../decisions/ADR-072.md)) and because the fixture pins `traded_days_count: 9` across the grind; the missing suite is a residual on `RE-P-04` and is named in the document so it is not found a fourth time.

### `writable` now names a place, and that is a widening `ADR-076` performs without saying so

`W1` is loader reachability in [ADR-072](../decisions/ADR-072.md)'s own words: *"a symbol in `apps/worker` or `packages/rithmic` fails W1 whether or not it is implemented"*. **`GS-071` fails `W1` read that way and always will.** [ADR-076 section 3](../decisions/ADR-076.md) rules it `writable` anyway, which is only coherent under section 1's governing rule that a row is discharged by an assertion that RUNS wherever it runs. So `writable` has stopped meaning *"a fixture can be written in `packages/rules-engine/fixtures`"* and now means *"the assertion can be written, in the suite named"*. **The document says this out loud and the one `writable` row states its suite.** It is a consequence of the ruling rather than a new one, and it is recorded because a reader of the column would otherwise infer the old meaning.

### The counts were recomputed by reading the tree, and the directory was listed

[`packages/rules-engine/fixtures`](../../packages/rules-engine/fixtures/README.md) holds **43 `GS-nnn-*.yaml` and 43 `.expected.json` over 43 distinct ids**, and the set of `written` rows is now that same set exactly. All ten head figures were derived by parsing the edited rows, never by adjusting the previous number:

| | Before | After |
|---|---|---|
| written | 40 | **43** |
| writable | 2 | **1** |
| covered-elsewhere | — | **6** |
| blocked | 274 | **266** |
| `format-cannot-express` | 22 | **16** |
| `outside-loader-boundary` | 6 | **4** |
| M1 partition | 40 + 2 + 31 = 73 | **43 + 1 + 6 + 23 = 73** |

### Verification, and the count reported honestly

`pnpm exec vitest run`: **101 files, 1405 passed, 45 skipped**, unmoved by this session, which is what a document-only session must produce. `pnpm run format:check` and `pnpm run lint`: clean. `node scripts/corpus/gates.mjs generate`: *"every span already matches its query"*, nothing to commit.

**`node scripts/corpus/gates.mjs check`: 27 of 28. `CI-06/fixture-inventory` FAILS with nine findings and every one of them is a repair inside [`scripts/corpus/`](../../scripts/corpus/gates.mjs), which this session's fence forbids and which is `X8`'s.** Three are the register entries this session's repair makes stale; six are the six rows, because `FIXTURE_STATUSES` is `['written', 'writable', 'blocked']` and is deliberately written in the runner rather than derived ([ADR-074](../decisions/ADR-074.md) section 2). `node scripts/corpus/falsify.mjs`: **2 problems**, the clean-phase failure above and one more, `CI-06/fixture-inventory/register-repaired` reporting **SEED IS STALE**: its seed looks for a fixture on disk whose row is not `written` and there is no longer one, which is the seed going stale **because the defect was repaired**. Both are `X8`'s and both are named in the pull request.

- **Landmines:**
  - **A red gate is landing on `main` deliberately and it must not be read as a corpus defect.** `X2` before `X8` is [WAVE-05 section 2](../plans/WAVE-05-tier2-fixture-shapes.md)'s stated order and the plan does not name its cost: the document gains a status the runner does not know, so `CI-06/fixture-inventory` is red for exactly as long as it takes `X8` to land. **The plan argues the mirror of this for `X1` before `X8`** and stops one item short of noticing that `X2` has the same shape pointing the other way.
  - **Every line number this document states into [`plan-validate.test.ts`](../../packages/rules-engine/test/plan-validate.test.ts) is stale after session 128**, which is `X3` and `X4` in that same file. The citations name the test id beside the line for this reason, and a re-derivation is owed at the end of that session and not at the start of a later one.
  - **`covered-elsewhere` is checked by a comment and a comment is a claim a refactor can leave behind.** Session 123 recorded this and it survives unchanged: moving an assertion out from under a naming comment breaks nothing any grep reads. The document's own answer is that the citation is verified by EXECUTING it, which is a discipline and not yet a gate; **`X8` is where it becomes one, and `GS-072` is the row that proves a grep alone cannot.**
  - **`RE-P-04` is declared in [M01](../plans/M01-rules-engine.md) and implemented nowhere**, so `INV-09` has no property suite. Found while checking `GS-049`'s disposition, unrelated to the fence, repaired by nobody yet.
- **Files touched:** [section 39](../testing/golden-scenarios/39-fixture-status-and-blockers.md), [STATE](../STATE.md), [sessions/README](README.md), this file. **No fixture, no loader, no test, no gate and no source file.**
