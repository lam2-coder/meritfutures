---
status: approved
depends_on: [README.md]
last_updated: 2026-08-30
---

## 2026-08-30 - Session 450: the probe was already run, the seed that said otherwise cannot be installed, and the copy nobody watches is the row rather than the migration

<!--index: **THE RULING IS [`ADR-259`](../decisions/ADR-259.md), `proposed` and UNSIGNED, MONEY PATH BY WHAT IT GUARDS, `E2` READ OWED. NO MIGRATION NUMBER IS TAKEN AND NO SHIPPED SOURCE FILE OR `status` FIELD MOVES.** Row `259` sent one session to close a gap [ADR-247](../decisions/ADR-247.md) section 7 named, and instructed it to establish FIRST, by reading, whether `CI-02` can be given a PostgreSQL service. **IT DOES NOT NEED ONE AND BOTH HALVES OF THE PREMISE ARE FALSE.** **THE PROBE IS RUN**: [`corpus.yml`](../../.github/workflows/corpus.yml)'s `migrations` job is `CI-06h`, it declares a `postgres:16` service, applies every migration forward-only from empty under `ON_ERROR_STOP` and then runs [`probe_ledger_constraints.sql`](../../scripts/db/probe_ledger_constraints.sql), so `LEDGER-K3` has read the migration's own copy on every push since [ADR-187](../decisions/ADR-187.md) merged; the claim is true of `CI-02` and false of CI. **AND [ADR-247](../decisions/ADR-247.md)'s SEED CANNOT BE INSTALLED**: `('withdrawals_in_flight', 'liability', 'identity')` in [`0056`](../../packages/db/migrations/0056_eighth_ledger_code.sql) violates `ledger_accounts_scope_identity` because that INSERT names no `identity_id`, the set **fails to apply at `CI-06h`'s first step**, and no probe is reached, so the green suite measured that vitest has no database rather than that a copy is unwatched. **THE ANSWER TO THE ROW'S OTHER QUESTION IS THAT NO PROBE IS IN THE SAME POSITION**: all **18** `probe_*.sql` are wired and pinned, which is what `CI-06s` holds. **THE GAP THAT IS REAL IS SEVEN CODES WIDE AND IN A DIFFERENT PLACE.** `ledger_accounts_kind_matches_code` binds a row's KIND to its CODE with `ELSE false`; **NOTHING binds its SCOPE**, and one INSERT per code on a real database found a wrong-scope row **ACCEPTED FOR ALL EIGHT**. `LEDGER-K3` defends one of them as a `'firm'` literal and the provisioning block defends three more as a SQL array, so the tree holds **FOUR** copies of the fact rather than the two [ADR-247](../decisions/ADR-247.md) was worried about, and none of the four is a comparison. **BUILT**: [`assert_ledger_scope_agrees.mjs`](../../scripts/db/assert_ledger_scope_agrees.mjs), which holds **NO** copy -- it imports `LEDGER_ACCOUNT_SCOPE` from [`accounts.ts`](../../packages/ledger/src/accounts.ts) (zero imports, Node 22 type stripping, so the exported OBJECT rather than a parse) and reads `pg_constraint` and `ledger_accounts` -- three assertions each in both directions, wired at `CI-06h` with `--falsify` first, pinned. **RED BEFORE GREEN ON FIVE MIGRATION-LEVEL SEEDS**, each applied to a COPY of the migration set in its own database: two of the five (`a firm-scoped trader_wallet row`, `an identity-scoped reserve row`) the existing probe passes and this reddens, and **the entry reports the three it does not add rather than counting them**. **SIX FALSIFICATION SEEDS**, every one inside a rolled-back transaction with a no-leak assertion after. **THE TYPESCRIPT SIDE IS A CENSUS RATHER THAN ONE CODE**: all eight flipped one at a time, `typecheck` **exit 1 on every one** and vitest RED on every one (7 to 13 cases over 2 to 4 files), each restored `sha256sum` identical; [ADR-187](../decisions/ADR-187.md) section 4's *"tsc sees nothing at all"* is about ADDING a code and does not transfer to changing a scope, because `FirmAccountCode` and `IdentityAccountCode` are conditional types over this exact map. **THE PROMINENT FINDING IS `OI-07`'s FIFTH OCCURRENCE, LIVE ON `main`, IN THE HALF `CI-06s` CANNOT SEE**: that gate scans `probe_*.sql` only, and [`assert_append_only_grants.mjs`](../../scripts/db/assert_append_only_grants.mjs) and [`assert_date_unit_shape.mjs`](../../scripts/db/assert_date_unit_shape.mjs) were on disk, wired, and **pinned by NOTHING**, so deleting either step was a silent change with every gate green; the first is `OI-03`'s entire implementation. **`RI-24` IS TAKEN**, went RED on both on its first run, and its ALLOCATION row is written in the same commit as the check, from the check. **`CI-06s` IS DELIBERATELY NOT WIDENED.** **OWED AND NAMED**: `RI-24` has no seeded-violation case, because [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts) is outside row `259`'s fence and a fence is not widened to finish; and the scope-per-code CHECK that would make section 3 unrepresentable is a migration this row reserves no number for. Counts derived at reporting time: **33 of 33** gates, **23 of 23** invariants against a baseline of 22, **278 files / 6,699 passed / 6 skipped** against a baseline of **278 / 6,698 / 6** measured on this tree before a byte changed, a delta of **+1 case and ZERO files**, which is the `test.each(CHECKS)` clean-tree case `RI-24` adds by joining the array and nothing else; **this session added no test file, because `RI-24`'s seeded case is owed and outside the fence**, typecheck 0, lint 0, `format:check` clean. **`falsify.mjs` NOT run**: forbidden by this row and it mutates the working tree. -->

**Objective.** Establish whether `CI-02` can be given a PostgreSQL service and what it costs, then
either make the migration's own copy of the eighth code's scope watched by something that reads a
real database, seed watched RED before GREEN, or refuse in writing and leave the gap documented.

**Outcome.** **Neither.** The instrument the row said was not run has been running on every push
since it was written, and the seed that reported the gap cannot be installed at all. What is left
after both are removed is a real gap in a different place, and it is closed.

---

### 1. What the row claimed, and what the tree says

| Row `259` and [ADR-247](../decisions/ADR-247.md) section 7 | Derived before a line changed |
|---|---|
| *"The instrument already exists and is not run"* | **FALSE.** `corpus.yml` job `migrations` runs it against a `postgres:16` service |
| *"it needs a running PostgreSQL that `CI-02` does not have"* | **TRUE of `CI-02`, and `CI-06h` has one** |
| *"whether any OTHER probe in `scripts/db/` is in the same position"* | **NO PROBE IS.** 18 of 18 wired and pinned. **Two of the four `assert_*` files were, in a different way.** Section 3 |
| `0056` seeded to `identity` leaves the suite GREEN, 20 of 20 | **TRUE AND IT MEASURES NOTHING.** That set does not apply |
| `accounts.ts:75` seeded goes RED, 8 cases across two suites | **HOLDS in direction**, 10 across three on this tree, and section 2 extends it to all eight |
| *"only one side is watched"* | **NOT ON THIS CODE.** Both sides of `withdrawals_in_flight` are watched. The unwatched thing is the ROW, on the other seven |

---

### 2. The measurements

**2.1 The database was real.** PostgreSQL 16 installed in the container, `0001` through `0074`
applied forward-only from empty under `ON_ERROR_STOP`, **118 base tables**, which is
[STATE](../STATE.md)'s `sql_tables` span. Every seeded set was a separate database built from a
separate copy of the migration directory, so no migration file was touched.

**2.2 Nothing binds scope to code.** One INSERT per code, inside a rolled-back transaction:

```
ACCEPTED WRONG SCOPE: firm_treasury=IDENTITY  psp_clearing=IDENTITY  fees_revenue=IDENTITY
  reserve=IDENTITY  trader_withdrawable=FIRM  trader_wallet=FIRM  promotional_credit=FIRM
  withdrawals_in_flight=IDENTITY
REFUSED: (none)
```

**2.3 Five migration-level seeds, against the existing probe and against the new check.**

| Seed | `probe_ledger_constraints.sql` | new check |
|---|---|---|
| `0054`'s trigger drops `promotional_credit` | RED | RED |
| `0054`'s trigger opens `withdrawals_in_flight` per identity | RED | RED |
| the declared vocabulary narrows to seven | RED | RED |
| a firm-scoped `trader_wallet` row is seeded | **GREEN** | **RED** |
| an identity-scoped `reserve` row is seeded | **GREEN** | **RED** |
| [ADR-247](../decisions/ADR-247.md)'s own seed | **the set does not apply** | n/a |

**2.4 [ADR-187](../decisions/ADR-187.md)'s REGISTRY FIRED ON THIS SESSION'S NEW FILE.** The first draft explained
which trigger substitution the kind constraint admits **by naming the firm codes and their kinds in a comment**, and
[`in-flight-obligation.test.ts`](../../packages/ledger/test/in-flight-obligation.test.ts) went RED on it: *"the registry names
every file that enumerates the firm subset, and no other"*. **The claim is DERIVED now** -- the file reads the arms of
`kind_matches_code` off `pg_get_constraintdef`, intersects them with the codes the library declares firm, and the falsify path
requires exactly one -- so a ninth firm liability turns an assertion RED instead of leaving a stale comment, and the file is
not an enumeration site, so the mint price stays at six outside sites.

**2.5 The TypeScript census, all eight codes, each restored `sha256sum` identical.** `typecheck`
exit 1 on every one; vitest 7 to 13 cases over 2 to 4 files on every one.

---

### 3. The finding the row required be prominent

**`OI-07`'s FIFTH OCCURRENCE, LIVE ON `main`.** `CI-06s` exists to hold that every probe is both run
and pinned, in both directions, and its on-disk scan is `/^probe_[a-z0-9_]+\.sql$/`. `scripts/db/`
also holds four ASSERTIONS, and two of them were wired and pinned by nothing:

```
FAIL   RI-24  Every scripts/db assertion is run by corpus.yml and pinned by CI-06h  (2)
       scripts/db/assert_append_only_grants.mjs is not pinned by CI-06h's required-needle list ...
       scripts/db/assert_date_unit_shape.mjs is not pinned by CI-06h's required-needle list ...
```

`assert_append_only_grants.mjs` is `OI-03`'s entire implementation and the only reader of the
database's grants in this repository. **Deleting its step was a silent change with all 33 gates and
all 22 invariants green.** Both needles are added in the same commit and labelled in `gates.mjs` as
a finding rather than as routine wiring.

---

### 4. What was built

| Artefact | What it holds |
|---|---|
| [`assert_ledger_scope_agrees.mjs`](../../scripts/db/assert_ledger_scope_agrees.mjs) | The set comparison [ADR-247](../decisions/ADR-247.md) named and did not write. Vocabulary, every row's scope, and `0054`'s provisioning, each in both directions, plus `--falsify` |
| [`corpus.yml`](../../.github/workflows/corpus.yml) | Two steps in the `migrations` job, `--falsify` first |
| [`gates.mjs`](../../scripts/corpus/gates.mjs) | Three `CI-06h` needles. **Two are section 3's finding** |
| [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs) | `RI-24`, with its ALLOCATION row in this commit, from the check |

**It holds no copy of the scope map, which is why it is a `.mjs` and why it parses nothing.** A
`.sql` file would need a third literal copy, watched against neither of the first two, which is the
trap the row named. `accounts.ts` has zero imports and Node 22 strips types on import, so the file
reads the exported object.

---

### 5. What is still owed

- **The `E2` read on [`ADR-259`](../decisions/ADR-259.md).** MONEY PATH by what it guards, UNSIGNED.
- **`RI-24`'s seeded-violation case.** [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts)
  is where every other check is watched failing and `packages/tooling/test/**` is outside this row's
  fence. **The next row that holds it should take this first.**
- **The scope-per-code CHECK.** It would make section 2.2 unrepresentable rather than merely
  observed, in the shape [ADR-186](../decisions/ADR-186.md) gave `kind`. It is a migration, this row
  reserves no number, and it supersedes a merged money-path constraint. **It is the larger of the
  two repairs and this session took the smaller one.**
- **Whether [ADR-247](../decisions/ADR-247.md) section 7 is amended or left.** It is `proposed` and
  nothing rests on it; this session did not touch another entry's finding.

---

### 6. Landmines for the next session

- **A DISPATCH'S PREMISE IS A CLAIM AND NOT A FACT.** This row's central instruction rested on
  *"the instrument exists and is not run"*, and one `grep -n 'probe_' .github/workflows/*.yml`
  refuted it before any work began. **Two consecutive rows have now carried a false citation**:
  session 445's row named a migration filename that does not exist.
- **A SEED THAT DOES NOT INSTALL IS NOT A SEED.** [ADR-247](../decisions/ADR-247.md) read a green
  suite against an unrepresentable migration as evidence of a blind spot. **Apply the seed before
  believing what its greenness means**, which needs a database and this container can run one:
  `initdb`, `pg_ctl -o '-p 5433 -k /tmp'`, and PostgreSQL 16 is already installed.
- **`RI-23`'s ALLOCATION ROW IS AN ORPHAN NO TABLE GATE READS.** Two cells against that table's three columns, after a
  blank line, admitted by `CI-06v` only because its minimum orphan length is 2 and the fragment is 1. Adding a row beneath it
  turned `CI-06v` RED, which is how it was found. **`RI-24` went into the table instead**, so the numbers read 21, 22, 24, 23.
  Whoever holds a fence over that table should fold `RI-23` back in as three cells.
- **`CI-06s` READS `probe_*.sql` ONLY, AND `RI-24` NOW READS `assert_*`.** A third naming family
  under `scripts/db/` would be unread by both.
- **`docs/STATE.md` STILL CARRIES THE `gen:adr_count` PARAGRAPH AT `RI-12`'s CEILING.** Session 445
  recorded eight against a ceiling of eight. This session's `STATE` permission is `append` and it
  added no copy, so the position is unchanged and the next keep-both merge on that line turns
  `RI-12` RED.
- **`pnpm vitest` IS NOT A SCRIPT IN THIS WORKSPACE.** `npx vitest run` or `pnpm test`.
- **DEPENDENCIES ARE NOT INSTALLED IN A FRESH CONTAINER.** `pnpm install` first.

---

### 7. Files touched

`scripts/db/assert_ledger_scope_agrees.mjs` (new), `.github/workflows/corpus.yml`,
`scripts/corpus/gates.mjs`, `packages/tooling/checks/repo-invariants.mjs`,
`docs/decisions/ADR-259.md` (new), `docs/decisions/ALLOCATION.md`, `docs/decisions/README.md`,
`docs/INDEX.md`, `docs/STATE.md` (append), `docs/sessions/` (this file and its README row).

**No file under any `src/`, no migration, and no `status` field.**
