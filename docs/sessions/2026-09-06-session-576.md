---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 576: the imprecision three rows declined, where the first decline was an interim that recommended the repair, the two after it inherited the verdict without that qualification, and the ground all three cite for declining is the ground that makes the repair safe

<!--index: **THE ROW IS [ADR-381](../decisions/ADR-381.md), `proposed` and UNSIGNED. MONEY PATH, ADR-003 STRICT, `E2` READ OWED.** Three consecutive rows found one wrong sentence in [`start.ts`](../../apps/api/src/start.ts) and each left it, and this row was sent to settle whether that is a ruling nobody wrote down or a defect everybody assumed somebody else would take. **IT IS NEITHER, AND THAT IS THE FINDING: ONE GENUINE FENCE REFUSAL FOLLOWED BY TWO INHERITANCES OF ITS VERDICT WITHOUT ITS QUALIFICATION.** [ADR-372](../decisions/ADR-372.md) section 9 declined under a fence permitting a `src/` edit only if a finding forced it, **and its own section 15 question 3 recorded that decline as an INTERIM**, listed three dispositions of which two are repairs, and named the cheapest. [ADR-374](../decisions/ADR-374.md) section 6 and [ADR-379](../decisions/ADR-379.md) section 7 declined on SCOPE, the same sentence with two nouns swapped, and **neither names that question, that recommendation or the word interim**, measured. By the third entry the interim is being defended as *"a deliberate non-repair"* whose reversal would be worse than leaving it alone. **THIS IS [ADR-358](../decisions/ADR-358.md)'s MECHANISM ONE REGISTER UP**: every row re-derived the clause it was dispatched about, whether the imprecision is still real, and all three got it right; none re-derived whether the decline still followed. **AND THE CLAUSE EACH ADDED TO SHOW ITS REASON HAD GROWN STRONGER RUNS BACKWARDS**: the count of controls reddening on the invited append rose one, two, three, and that measures how thoroughly the CONSEQUENCE is guarded, which is an argument that the repair is SAFE rather than an argument that it should not happen. **NO ENTRY IN THE SEQUENCE EVER STATES A REASON THE SENTENCE SHOULD REMAIN FALSE**, and [ADR-374](../decisions/ADR-374.md)'s own approval block diagnosed it one entry before the third decline: *"a finding nobody's fence covers is a finding that does not get fixed, and it is one line."* **RULED: REPAIRED.** The comment said of the rate limiter install that it is the last LINE; it is the last INSTALL, with `await main()` two below it. The corrected sentence names the install, names the real last line and states the append boundary the old one pointed past. **THE CITATION SWEEP DECIDED THE SHAPE OF THE REPAIR RATHER THAN BEING REPORTED AFTER IT**: **27** sites read `start.ts:311` and **6** read `:315`, so the repair is **four comment lines replaced by four**, the file unchanged at **317** lines, and **this diff moves no line in any file**, so no citation anywhere needed repairing and none was touched. **THE PREMISE ALL THREE DECLINES SHARE IS MEASURED FOR THE FIRST TIME AND IT IS ZERO**: all three call it a prose repair to a money-path file and treat that as carrying risk, and **the emitted program is byte-identical at 1,726 bytes** through TypeScript's own emitter, **with the instrument falsified in both directions**, including a reordering reported as different at an identical byte count. **NO CASE IS ADDED AND THAT IS A DECISION WITH A RULING BEHIND IT**: the load-bearing half of the corrected sentence is pinned three ways, seeded and measured here at `start-program` **1 of 6** red, `start-construction` **2 of 5** red and `wiring` **3 of 27** red, and the other half is a layout fact [ADR-372](../decisions/ADR-372.md) section 8 expressly ruled must not be pinned. **THE RETIRED SENTENCE IS NAMED AND NOT REPRODUCED IN `start.ts`**, on [ADR-375](../decisions/ADR-375.md) section 5's measurement that this file already keeps three retired clauses and that a fifth copy would be [ADR-367](../decisions/ADR-367.md)'s collision one register down. **A DISPATCH PREMISE IS REFUTED, AS IN EACH OF THE LAST FIVE WAVES, AND IT IS REFUTED BY THE SECTION THE DISPATCH SENT THIS ROW TO READ**: [ADR-379](../decisions/ADR-379.md) section 5 does not say pinned counts may now be derivable, it records that guess as tested one wave earlier and **not surviving**, so **`wired` unlocked NOTHING** and the same guess has now been dispatched twice and refuted twice. **[ADR-379](../decisions/ADR-379.md) section 12 ITEM 4 IS TAKEN AND DISCHARGED; ITEM 5 IS INSIDE THIS FENCE AND DELIBERATELY LEFT**, because that entry recorded it as the founder's with three options and no value chosen. **FOUR PROBES, SIX OBSERVATIONS, every file restored byte-identical under `sha256sum -c` against a four-file manifest**, and **no restore was done with `git checkout --`**. **ONE PROBE FAILED AS WRITTEN AND IS RECORDED RATHER THAN REPLACED SILENTLY**: whitespace inside `await main( );` was normalised by the emitter, so it measured the formatter and not the program. **No port wired, no door opened, no handler behaviour changed, no triple moved, no gate weakened, no test skipped, disabled or quarantined, no invariant minted, no `CI-06` letter spent, no migration taken or reserved, no `status` field moved, and `apps/worker/**` and `packages/tooling/checks/**` never opened at all.** **`380` IS RESERVED** so `CI-06f` stays gapless with two siblings running, naming no subject. **THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL**, the eleventh consecutive session to record it. **NO `mcp__github__*` TOOLS, NO `gh` ON PATH, `api.github.com/repos/lam2-coder/meritfutures` ANSWERS 403 while `git push` AUTHENTICATES**; the branch is pushed and **no pull request was opened and nothing was merged, by instruction**. Suite **336 files / 8,074 passed / 10 skipped / 0 failed** against a base reproduced on `e18ea41f` BEFORE the first edit of **336 / 8,074 / 10 / 0**, a **delta of ZERO** with no test file opened for writing at all; gates **33 of 33** after `generate`, invariants **35 of 35**, typecheck, lint and `format:check` clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `381`: read all three declines of the imprecision at [`start.ts`](../../apps/api/src/start.ts)`:311`, state precisely what each one's reason actually was, and then RULE, either repairing it with all three declines answered or ruling it permanently declined with the reason written down once.

**One objective.** It resolved into one `src/` repair, one finding about the sequence itself, one dispatch premise refuted, one deliberate non-repair inside the fence, and four probes.

---

### 2. What was measured before the first edit

**THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL.** `/home/user` was empty, with no clone and no `node_modules`. **This is the eleventh consecutive session to record it.** The tree was attached to this session's authorized set and cloned by hand, and `HEAD` was confirmed to be the `e18ea41f` the row names rather than assumed.

**Base, on a clean tree, before anything was touched: 336 files / 8,074 passed / 10 skipped / 0 failed.** Gates **33 of 33** after `generate`, invariants **35 of 35**, typecheck, lint and `format:check` clean.

**THESE ARE NOT [ADR-379](../decisions/ADR-379.md)'s FIGURES AND THAT IS EXPECTED.** That row measured `6698f9c0` and reported a base of 8,070; that wave has since merged. **A row carrying either figure forward would report a delta against a tree that no longer exists.**

**AND THE CITATION SWEEP DECIDED THE SHAPE OF THE REPAIR RATHER THAN BEING REPORTED AFTER IT:**

```
citation sites reading `start.ts:311`  ->  27
citation sites reading `start.ts:315`  ->   6
```

**SO THE REPAIR IS FOUR COMMENT LINES REPLACED BY FOUR**, verified after: `start.ts` unchanged at **317** lines, `:315` and `:317` where they were.

---

### 3. What each decline actually said

| entry | the ground it states | shape |
|---|---|---|
| [ADR-372](../decisions/ADR-372.md) section 9 | *"THE FENCE PERMITTED A CHANGE ONLY IF A FINDING FORCED IT. NONE DID."* | **A FENCE RULE**, binding on that row. **One** control named |
| [ADR-374](../decisions/ADR-374.md) section 6 | *"a row whose objective is guards, for a sentence already covered by two failing bars, is scope this row does not have"* | **SCOPE**. **Two** controls named |
| [ADR-379](../decisions/ADR-379.md) section 7 | *"a row whose objective is somebody else's leavings"*, plus *"a deliberate non-repair reversed without understanding is worse than leaving it alone"* | **SCOPE**, the same sentence with two nouns swapped. **Three** controls named |

**EACH ROW WAS RIGHT ABOUT ITSELF AND NONE OF THE THREE IS A RULING ABOUT THE SENTENCE**, and the repository has been reading them as one for four entries.

---

### 4. The drift, and the qualification that went missing

**[ADR-372](../decisions/ADR-372.md) SECTION 15 QUESTION 3 IS WHERE THE ANSWER LIVES AND IT APPEARS IN NO LATER ENTRY.** It recorded three dispositions, two of them repairs, and ended *"(b) is the cheapest and this row took (a) as its interim"*.

**MEASURED**: [ADR-374](../decisions/ADR-374.md) never refers to that section at all; [ADR-379](../decisions/ADR-379.md) refers to [ADR-372](../decisions/ADR-372.md) section 15 exactly once and it is to QUESTION 1, about `wired`; the word `interim` occurs in neither entry.

**AN INTERIM RECHARACTERISED AS A DELIBERATE RULING IS EXACTLY HOW A TEMPORARY ANSWER BECOMES PERMANENT WITHOUT ANYBODY RULING IT.** This is [ADR-358](../decisions/ADR-358.md)'s mechanism with an ADR verdict as the inherited clause instead of a blocker count.

---

### 5. What was NOT done, and why

**NO CASE WAS ADDED.** The load-bearing half of the corrected sentence is pinned three ways, measured here. The other half, *"it is the last install"*, is a layout fact [ADR-372](../decisions/ADR-372.md) section 8 expressly ruled must not be pinned, because *"an assertion that pinned line order would be defending a property no deployment can distinguish"*. The third shape available, asserting the absence of the retired phrase, is the weakest in the house per [ADR-378](../decisions/ADR-378.md). **The residual risk is stated rather than hidden: nothing mechanical stops a later row restoring the false wording.**

**[ADR-379](../decisions/ADR-379.md) SECTION 12 ITEM 5 IS INSIDE THIS FENCE AND WAS DELIBERATELY LEFT.** Three facts asserted in two files, both `apps/api/test`. That entry recorded it as the founder's with three options, no value chosen and no recommendation, and its approval block calls it the one it most wants told rather than guessed. **A row answering it would be answering a question its own predecessor declined to answer, in a session whose one objective is elsewhere.**

**FOUR OWED ITEMS ARE OUTSIDE THIS FENCE** and this diff moves none of them: the `CRON_INVENTORY` replay row then the `RI-35` registration; the two `packages/tooling` sites citing `wiring.test.ts:215`; the four drifted citations in `DELTA_MANIFEST.md` and `ALLOCATION.md`; and `RI-15`'s holes.

---

### 6. Falsification

**FOUR PROBES, SIX OBSERVATIONS, every file restored byte-identical under `sha256sum -c` against a four-file manifest re-verified after each restore.** No restore was done with `git checkout --`.

- **Probe 1 is the one that changed the work.** The install moved below `await main()` on the real file: **RED in three of three**, `start-program` 1 of 6, `start-construction` 2 of 5, `wiring` 3 of 27. Had it come back red in fewer, section 5's decision to add no case would have been wrong.
- **Probes 3 and 4 falsify the emitter instrument.** The install commented out DIFFERS at 1,726 against 1,553 bytes; the install moved below `main()` DIFFERS **at an identical byte count**, which is what proves the check reads the program rather than its size.
- **One probe failed as written and is recorded rather than replaced silently.** Whitespace inside `await main( );` returned IDENTICAL, correctly, because the emitter normalises it. It was measuring the formatter.

---

### 7. Blockers and landmines

- **LANDMINE, GENERAL AND UNGUARDED: a deliberate non-repair recorded as an INTERIM, with a recommended disposition, read by later rows as a settled verdict.** `RI-14` reads non-existence claims about named exports and cannot see this; no gate distinguishes an interim from a ruling. **The sequence it produced ran four entries with every gate green throughout.** [ADR-381](../decisions/ADR-381.md) section 16 question 1 records the options.
- **LANDMINE: the corrected sentence is held by an unsigned ADR and nothing else.** Section 5. That is the normal condition of prose in this corpus and it is worth deciding whether it should be.
- **LANDMINE: two phrases were dropped to hold the line budget**, *"for concurrent slices"* and *"this file's"* before "header". Both recoverable from the header the sentence names. **A reader who thinks a `src/` register should never lose a phrase to a line budget is disagreeing with a real trade**, and the alternative was moving six citations to buy back nine words.
- **REPORTED AND NOT RESOLVED: [ADR-379](../decisions/ADR-379.md) section 12 item 5**, inside this fence and left. Section 5.
- **Concurrency: two sibling rows are running now**, one on `apps/worker/**` and one on `packages/tooling/checks/**`. **Neither tree was opened, for reading or for writing.** **`380` is RESERVED** so `CI-06f` stays gapless; the cell names no subject and claims nothing, and a row taking it replaces the cell whole.

---

### 8. This session's own tooling, measured rather than assumed

```
$ command -v gh                                            -> not found (exit 1)
$ curl -s -o /dev/null -w '%{http_code}' \
    https://api.github.com/repos/lam2-coder/meritfutures   -> 403
```

**No `mcp__github__*` tool exists in this session's surface**, checked by search rather than by inspection of a list. **`git push` authenticates** through the session's git proxy and did so on every commit. **This matches sessions 554, 558, 562, 564, 565, 568, 569, 570, 571, 573 and 574 exactly.** The branch is pushed and **no pull request was opened and nothing was merged, by instruction**.

**THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL**, the eleventh consecutive session to record it. **Every Bash command was run bare from the working directory** once the clone existed, per the row's operational note, and **no permission stall occurred**, which is the twelfth consecutive row to report that.

---

### 9. Verification, derived at reporting time

| runner | result |
|---|---|
| `pnpm vitest run` | **336 files / 8,074 passed / 10 skipped / 0 failed** |
| baseline on `e18ea41f`, before the first edit | **336 files / 8,074 passed / 10 skipped / 0 failed** |
| delta | **ZERO.** No test file was opened for writing, so there is nothing to attribute by removal. The three files in the slice run **6**, **5** and **27** alone, all passing |
| `node scripts/corpus/gates.mjs generate` then `check` | **33 of 33** |
| `node packages/tooling/checks/repo-invariants.mjs` | **35 of 35**, no number minted |
| `pnpm run typecheck` / `lint` / `format:check` | clean |
| `apps/api/src/start.ts`, before and after | **317** lines and **317** lines |
| emitted program, before and after | **byte-identical at 1,726 bytes** |
| `git diff origin/main -- apps/worker packages/tooling docs/ops` | **EMPTY** |

**`pnpm run verify` was NOT run and `scripts/corpus/falsify.mjs` was NOT run**, both forbidden by the row and both mutate the working tree.

---

### 10. Next

The founder's `E2` read of [ADR-381](../decisions/ADR-381.md) and the three decisions its approval block names, of which **the third is the only one that generalises**: an interim read as a ruling is invisible to every gate this repository has. Then **[ADR-379](../decisions/ADR-379.md) section 12 item 5**, which wants a ruling and not a row, and which is now the oldest thing inside `apps/api` that no row will take on its own authority.
