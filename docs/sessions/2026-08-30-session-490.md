---
status: approved
depends_on: [README.md]
last_updated: 2026-08-30
---

## 2026-08-30 - Session 490: five citations in the plan, and the sixth pointer in the same sentence resolves to nothing

<!--index: **THE RULING IS [`ADR-298`](../decisions/ADR-298.md), `proposed` and UNSIGNED. NON-MONEY, CITATION REPAIR ONLY, AND THE FILE REPAIRED IS THE ONE THE REMAINING PAYOUT SLICES ARE DISPATCHED FROM.** All five citations row `298` dispatched were **re-derived at source** and **all five are stale**: the backwards range [`payouts.ts:588-566`](../../apps/api/src/routes/payouts.ts) becomes **`588-590`**, `:1177` becomes **`:1201`**, `:1226` becomes **`:1250`**, `:248` becomes **`:249`**, and [`wiring.test.ts:1337`](../../apps/api/test/wiring.test.ts) becomes **`:1432`**. **FIVE CITATIONS ACROSS SIX SITES**, because the wired-count pointer appears twice in [ADR-287](../decisions/ADR-287.md) and repairing one instance is the defect this row exists to end. **[ADR-289](../decisions/ADR-289.md) SECTION 1.3's NUMBERS WERE NOT COPIED AND THE PROHIBITION EARNED ITSELF: TWO OF ITS SIX LIVE VALUES ARE ALREADY FALSE**, its `wiring.test.ts:1402-1409` now addressing a factory walk, and its *"`API_CONTRACT.md` line 490 HOLDS"* falsified by **ADR-289's own ruling** in the same commit set. **THE SIXTH POINTER IN F2's SENTENCE CANNOT BE RESOLVED TO A PRIMARY SOURCE AND IS LEFT**: [ADR-289](../decisions/ADR-289.md) deleted the field from [API_CONTRACT](../architecture/API_CONTRACT.md), [`:500`](../architecture/API_CONTRACT.md) now reads *"Do not restore it"*, and repointing a *"required by"* to the record of a deletion is a substance edit wearing a citation's clothes. **THE CAUSE IS DERIVED FROM THE COMMITS RATHER THAN INHERITED**: at `00cb1bb^` **every** pointer checked resolved, `00cb1bb` added 125 lines above them, and `96be966`, titled *"repoint nine citations"*, changed five lines and moved **five** pointers, one of them by its start only. **SO [ADR-289](../decisions/ADR-289.md)'s *"repointed section 1's table in full"* IS NOT WHAT THE COMMIT DID**: the table has seven rows and four were repointed, which changes the size of what is owed from three sites to twenty-five. **AND `C2`'s STALE POINTER NOW RESOLVES TO A REAL, WRONG DECLARATION**, `:575` being `subject()` today, which is worse than resolving to a comment. **TWENTY-FIVE FURTHER STALE SITES ARE ENUMERATED WITH LIVE VALUES AND NONE IS REPAIRED**, because an arbitrary subset is exactly the failure `96be966` produced on this file; the remainder is left COMPLETE rather than arbitrary and needs no fence beyond the one row `298` carried. **THREE SUBSTANTIVE CLAIMS THE TREE CONTRADICTS ARE NAMED AND NOT FIXED**, each a money-path question this non-money row has no standing to decide: F2's *"required by the contract"*, section 1's *"returns 2 lines"* (six today, four of them [ADR-291](../decisions/ADR-291.md)'s rejecting stub), and section 7 still reading as though slice 1 were open. **NO FINDING, VERDICT, RULING OR SLICE MOVED**: the [ADR-287](../decisions/ADR-287.md) diff is **three lines** and every changed byte is inside a `file:line` pointer. **`RI-16` DOES NOT READ THAT FILE BY DESIGN**, its exclusion 1 skipping dated headings, which is correct and is why the drift survived a repair pass and three build rows. **NO `RI-nn` MINTED AND `RI-39` IS LEFT FREE**, three candidates refused, one of them RED ON LANDING over the twenty-five. **THE WIRED COUNT WAS CONFIRMED AT THE ASSERTION RATHER THAN QUOTED: TEN WIRED OF TWENTY-FOUR DECLARED, FOURTEEN BLOCKED.** Counts derived at reporting time, each command run separately: **33 of 33** gates after `generate`, **31 of 31** invariants off the runner's own last line, **298 files / 7,249 passed / 6 skipped** with a delta of **ZERO**, lint 0, `format:check` clean. **`pnpm run verify` was NOT run**, forbidden by the row. -->

---

### 1. The objective

**[ALLOCATION](../decisions/ALLOCATION.md) row `298`: repair the five stale citations session 480 found inside [ADR-287](../decisions/ADR-287.md) finding F2, re-deriving every live value at source, under an ADR because `ADR-287` is approved and changing it requires one.**

### 2. What was read before anything was written

The container arrived with **no repository and no `node_modules`**. Both were restored before anything was measured (section 6). Then `CLAUDE.md`, [STATE](../STATE.md), the tail of [docs/sessions/](README.md), row `298` in full, then [ADR-289](../decisions/ADR-289.md) sections 1.3, 4.2 and 5.2, [ADR-287](../decisions/ADR-287.md) in full, `RI-15` and `RI-16`'s headers in [`repo-invariants.mjs`](../../packages/tooling/checks/repo-invariants.mjs), and [ADR-297](../decisions/ADR-297.md) as the format precedent.

### 3. Done

- **[ADR-298](../decisions/ADR-298.md)**, ten sections, `proposed`, **UNSIGNED**. Records what each of the five citations said, what is true, and how each live value was derived, quoting the primary source at `file:line` for every repair.
- **[ADR-287](../decisions/ADR-287.md)**: five citations repointed across six sites. **Three lines changed, every changed byte inside a pointer.**
- **[ALLOCATION](../decisions/ALLOCATION.md)**: row `298` marked taken and landed.
- **[STATE](../STATE.md)**: this session's entry.

### 4. Next

- **ONE ROW FOR THE TWENTY-FIVE.** [ADR-298](../decisions/ADR-298.md) section 5 lists every remaining stale site in [ADR-287](../decisions/ADR-287.md) with the live value derived at source. **It needs no rights beyond row `298`'s own fence**, `docs/decisions/ADR-287.md` for citations only, and the work is mechanical.
- **A MONEY-PATH ROW FOR SECTION 6 S3**, which is the only finding that compounds: [ADR-287](../decisions/ADR-287.md) section 7 is the dispatch surface for the remaining slices and still reads as though slice 1 were open, three ADRs after it was discharged.
- **[ADR-289](../decisions/ADR-289.md)'s SEVEN OWED LINES ARE STILL UNRUN**, unchanged since [ADR-293](../decisions/ADR-293.md) reported it. [`payouts.ts:590`](../../apps/api/src/routes/payouts.ts) still declares the return type ADR-289 ruled cannot exist, which is why citation 1 is repaired to a live line rather than deleted.

### 5. Blockers

**None.** [ADR-298](../decisions/ADR-298.md) ships `proposed` with an UNSIGNED approval line and merging is not signing. Section 10 names the three things a founder read decides: whether stopping at five was right, whether the unresolvable sixth pointer should be left standing or repointed, and what section 7 of [ADR-287](../decisions/ADR-287.md) is, a dated record or a live dispatch table.

### 6. Landmines

- **`ADR-289` SECTION 1.3 IS A TABLE OF LIVE VALUES AND TWO OF ITS SIX ARE NOW FALSE.** It is the most inviting thing in this corpus to copy, it is presented as derived, and it was. **A live value published beside a ruling can be falsified by that same ruling**: `API_CONTRACT.md:490` stopped holding in ADR-289's own commit set.
- **A STALE CITATION THAT RESOLVES TO A PLAUSIBLE DECLARATION IS WORSE THAN ONE THAT RESOLVES TO A COMMENT.** [ADR-287](../decisions/ADR-287.md) row `C2` cites `listPayouts` at [`payouts.ts:575`](../../apps/api/src/routes/payouts.ts), and `:575` is `subject()` today: right file, right interface family, wrong member, and nothing flags it.
- **NEITHER `RI-15` NOR `RI-16` READS ANY POINTER IN ANY ADR.** `RI-15` says `docs/decisions` *"NOT AT ALL"* and `RI-16` excludes dated headings on purpose. **Every ADR in this corpus is unguarded on citations, and the exclusion is correct**, so the remedy is an ADR per repair rather than a widened check.
- **A COMMIT MESSAGE'S COUNT IS NOT A MEASUREMENT.** `96be966` says *"repoint nine citations"* and moved five. The claim was read as settled by the next session and by [ADR-289](../decisions/ADR-289.md), which recorded the pass as repairing *"section 1's table in full"*.
- **`node_modules` WAS ABSENT ON ARRIVAL AGAIN**, restored by `pnpm install --frozen-lockfile` and never committed. That is row `294`'s reported defect met for at least the third session running.
- **THIS CONTAINER STARTED WITH NO CLONE AT ALL.** `/home/user` was empty and not a git repository. The repository was attached and cloned inside the session before the start ritual could run, so the `SessionStart` hook's `git pull --ff-only` never ran and the pull was done by hand.

### 7. Fence

**Held, and nothing outside it moved.** [`docs/decisions/ADR-298.md`](../decisions/ADR-298.md), [`docs/decisions/ADR-287.md`](../decisions/ADR-287.md) **for citations only**, [`ALLOCATION`](../decisions/ALLOCATION.md), [`STATE`](../STATE.md), [`INDEX`](../INDEX.md), this log, and the two registry READMEs, **which are GENERATED by `node scripts/corpus/gates.mjs generate` and not hand-written**. **[INDEX](../INDEX.md) NEEDED NO EDIT AND RECEIVED NONE**: it indexes the [decisions registry](../decisions/README.md) rather than the entries inside it, and the ADR count it would carry is a generated span that lives in [STATE](../STATE.md), where `generate` moved it from 281 to 282. **`apps/**`, `packages/**`, `scripts/**` and `e2e/**` were read and not edited**, measured rather than intended: `git diff origin/main --stat -- apps packages scripts e2e` is EMPTY.
