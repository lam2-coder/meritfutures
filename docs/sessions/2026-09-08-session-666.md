---
status: approved
depends_on: [README.md]
last_updated: 2026-09-08
---

## 2026-09-08 - Session 666: seven of the thirteen were string literals, twelve of the thirteen line numbers had already moved, and the three repairs delete nine numbers and add none

<!--index: **THE ROW IS [ADR-471](../decisions/ADR-471.md), `proposed` and UNSIGNED. NON-MONEY DIFF, ONE FILE AND FOUR COMMENT LINES, `E2 READ OWED`.** [ADR-469](../decisions/ADR-469.md) section 10 item 1's thirteen prose pointers in [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts), taken now that row 468's fence is gone. **ALL THIRTEEN SITES SURVIVE AND TWELVE OF THE THIRTEEN LINE NUMBERS DO NOT**, re-derived by finding each site's TEXT from `git show 7f7ba904:<path>` rather than trusting either list: `:529` moved to `:530` and the other ten moved by EIGHTEEN, in row 468's scan-budget commit, two days between the finding and its repair. **AND SEVEN OF THE THIRTEEN ARE NOT COMMENTS AT ALL**: five are the bodies of files a case writes into a temporary tree and two are the finding text an `expect` asserts, so **under this dispatch's own rule that no assertion, input or verdict may move, seven were never repairable and a line-indexed sweep could not tell.** **THE AXIS THAT DECIDES IT IS MECHANICAL AND NEITHER PREDECESSOR CONSIDERED IT.** `ADR-467` section 9 rules USE against MENTION undecidable and ADJACENT against NON-ADJACENT decidable; **COMMENT against LITERAL is a third, it is decidable, and the reader already ships**: run [`stripComments`](../../packages/tooling/checks/strip-comments.mjs) and ask whether the match survives on its own line index. **THE FILE HOLDS 132 SITES AND 104 OF THEM ARE NOT COMMENTS**, which is `RI-15`'s own-package exclusion vindicated on 104 sites and over-general on 28. **THE 28 WERE ADJUDICATED ONE BY ONE AND ONLY THREE BLOCKS WERE DEAD.** The other twenty-five describe THIS FILE'S OWN FIXTURE (the seed is the next statement and cannot go stale), quote a SHAPE (a markdown link whose label is a bare `` `:1` `` and whose target is `path.ts`, no file this tree has), quote a claim that STILL RESOLVES (`DECISIONS.md:483`, `ports.ts:98`, `0010_payouts.sql:200-201`, `EVENTS:396`, all four checked at primary source here), or are ALREADY RETIRED and say so. **`:1682` IS THE ONE THAT MATTERS: THIS FILE REACHED `ADR-467` SECTION 5'S RULING ON ITS OWN, UNDER `ADR-377`, BEFORE `ADR-467` EXISTED** -- *"the figure is named rather than reproduced ... the line number was the only part of the sentence that could go stale"* -- and the three repairs are that convention applied to the three paragraphs it had not reached. **THE SWEEP FOUND A FOURTEENTH BLOCK THE THIRTEEN DID NOT NAME**, `scope.ts:644,649` and `:675,684` at `:2019` and `:2021`, invisible to `ADR-469`'s forms because a comma-list token has a comma where its `` `:NNN` `` form wants a backtick: **the instrument missed it for the exact reader defect the test at that site exists to prove.** **THREE REPAIRS, NINE NUMERIC POSITIONS DELETED AND NONE ADDED**, which is mechanical: every number on a removed line is `1307 911 644 649 675 684 644 649 739 16` and the only number on an added line is the `16` of `RI-16`. Each names the thing instead: a bare `},`, a `path:N,M` comma-list token, and a declaration the citation missed (`setAdminReadSource` is at `admin-reads.ts:1140`, not `:739`). **ALL THREE ARE REF-QUALIFIED USES, `ADR-467` SECTION 9'S HARDEST CLASS**, true about `origin/main` when written; repaired on this file's own `ADR-377` precedent rather than on an argument imported from outside it. **COMMENT-ONLY, PROVED FOUR WAYS**: 4,648 lines at base and head so NOT ONE LINE INDEX MOVES; the diff body with comment and blank lines stripped is EMPTY; `stripComments` over the base blob and the head file is **BYTE-IDENTICAL at 129,395 bytes**; and the NOT-COMMENT population is **104 at base and 104 at head**. **THE CITATION SET WAS DERIVED AT THE MOMENT OF EDITING**: eleven site-number pairs over seven documents naming eight distinct lines, and all eight read byte for byte what they read at base, four of them BELOW the edits. **ROW 468's SCAN BUDGETS SURVIVE AT UNCHANGED INDICES**, six `CORPUS_SCAN_MS` sites, and no edited comment is adjacent to one: the nearest is 736 lines above the first edit and 860 below the last. **THE BASE SUITE WAS NOT GREEN, BEFORE A LINE OF THIS ROW'S CHANGED.** `RE-P-01` at [`floor-monotonicity.property.test.ts:277`](../../packages/rules-engine/test/floor-monotonicity.property.test.ts) failed on the base run, passed eight isolated re-runs and passed at head: **one failure in ten, and it is not a flake, it is structural.** Measured here: **56 `fc.assert` call sites over 14 property files, not one pinning a seed, and no `fc.configureGlobal` anywhere**, so a counterexample against a money-path property is not reproducible and the failing run's was lost. **NOTHING WAS TOUCHED**; it is outside this fence and section 9 item 2 prices it. **DATED RECORDS: `ADR-469`'s 4,091 figure is QUOTED AND NOT RE-DERIVED**, because this fence is one source file. What was found in one is that `ADR-469` section 10 item 1's twelve stale numbers will send its next reader to the wrong lines; **not repaired, and NOT a sixth instance of the founder's stacked question**, because they aged rather than being false when written. **TWO PRICES SET, so [`PRICE_REGISTER.md`](../decisions/PRICE_REGISTER.md) IS UNTOUCHED AND THIS BRANCH LANDS ONE CHECK RED, EXPECTEDLY**: the eligibility pass, priced as a compile-time instrument and NOT as a gate because its own motivating case sits in an unrepairable dated record and the gate would ship permanently red; and the fast-check seeding decision. **NO GATE ADDED OR WEAKENED, NO TEST SKIPPED, DISABLED, QUARANTINED OR DELETED, NO CONTROL BUILT, NOTHING FROM THE SWEEP COMMITTED, NO `RI-nn` MINTED, NO `CI-06` LETTER SPENT, NO MIGRATION, NO DATED RECORD EDITED, NOTHING UNDER `packages/tooling/checks/` TOUCHED (row 470's fence), NO OTHER TEST FILE AND NO MONEY-PATH FILE EDITED.** `pnpm run verify` and [`falsify.mjs`](../../scripts/corpus/falsify.mjs) **NOT run**. Counts at reporting time, from THREE suite runs: **8712 PASSED, 1 failed, 11 skipped, 8724 TOTAL** at base; **347 files ALL PASSED, 8713 PASSED, 11 skipped, 8724 TOTAL, 0 failed** at the code commit, which is the honest statement of what the CODE change does; and **8711 PASSED, 2 failed, 11 skipped, 8724 TOTAL** with this entry in the tree, **both failures being `price-register.test.ts` running the checker against the real `docs/decisions` and therefore against this entry's own prices**, which `ADR-468` section 9 recorded identically; invariants **35 of 35**; gates **33 of 33**; `write-guard-set.mjs` and `price-register.mjs` **PASS with byte-identical sentences at base and at the code commit**; typecheck, lint and `format:check` clean. -->

### 1. The objective

Take [ADR-469](../decisions/ADR-469.md) section 10 item 1's thirteen prose line-number pointers in [`repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts), now that the fence row 468 held over that directory is gone; **re-derive them at this base rather than trusting the list**, sweep the whole file rather than only the thirteen, and repair by DELETING the number on [ADR-467](../decisions/ADR-467.md) section 5's ruling, keeping every change a comment and proving it.

### 2. Done

- **The thirteen re-derived at this base.** All thirteen sites survive; twelve of the thirteen line numbers had moved, one by `+1` and ten by `+18`, in row 468's scan-budget commit. [ADR-471](../decisions/ADR-471.md) section 2 is the table.
- **Seven of the thirteen found to be string literals**, five seeded file bodies and two expected findings, and therefore ineligible under the dispatch's own constraint. The discriminator is `stripComments`, mechanical and free.
- **The whole file swept**: 132 sites, 28 in comments and 104 not. All 28 adjudicated one by one against the file each sentence names; **three blocks dead**, one of which the thirteen did not name.
- **Three comment blocks repaired, four lines for four**, nine numeric positions deleted and none added. Commit `2493647`.
- **Comment-only proved four ways** and the citation set into the file derived at the moment of editing: eight cited lines byte-identical, six scan-budget sites at unchanged indices.
- **[ADR-471](../decisions/ADR-471.md) written**, `status: proposed`, approval line UNSIGNED, two prices set.

### 3. Next

- The integrator registers `ADR-471` section 9 items 1 and 2 in [`PRICE_REGISTER.md`](../decisions/PRICE_REGISTER.md) as `OPEN` / `UNASSESSED`. **This row did not touch that file and `price-register.mjs` goes red naming this entry's own anchors, which is expected** ([ADR-464](../decisions/ADR-464.md) section 1).
- **The `E2` read on this file and on the `RE-P-01` finding.**
- `absence-claims.test.ts:151`, `ADR-469`'s fourteenth site, is still unowned. **Run the comment test on it first**; that file is the same kind of file as this one.

### 4. Blockers

None encountered. Nothing in the dispatch was refused and nothing was left unfinished inside the fence.

### 5. Landmines

- **THE BASE SUITE WAS RED BEFORE THIS ROW CHANGED A LINE**, at `RE-P-01`, and it was green at head. **A session that runs the suite only once, after its edits, will conclude the tree was green throughout.** The preceding two entries both did exactly that and both recorded `0 failed`.
- **A `file:line` list published in a dated record is a stale artefact within days**, and eleven of this dispatch's twelve stale numbers came from one other row's ordinary diff. Re-derive by finding the TEXT, never by reading the index.
- **Seven of thirteen sites in a repair list were untouchable and nothing said so.** Before compiling any pointer list as a backlog, strip comments and drop what survives.
- **`fc.assert` in this tree pins no seed at 56 sites.** A property failure here is not reproducible and its counterexample is not kept.

### 6. Files touched

| file | what |
| --- | --- |
| [`packages/tooling/test/repo-invariants.test.ts`](../../packages/tooling/test/repo-invariants.test.ts) | four comment lines for four, nine numbers deleted and none added; executable source byte-identical |
| [`docs/decisions/ADR-471.md`](../decisions/ADR-471.md) | new, `proposed`, UNSIGNED |
| `docs/sessions/2026-09-08-session-666.md` | new, this file |
| [`docs/decisions/README.md`](../decisions/README.md), [`docs/sessions/README.md`](README.md) | generated registry spans, by `gates.mjs generate` |
