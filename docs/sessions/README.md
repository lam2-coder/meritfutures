---
status: approved
depends_on: []
last_updated: 2026-08-28
---

# SESSION LOG

Append-only handoff journal (C3 ritual), one file per session since
[ADR-043](../decisions/ADR-043.md). Newest entry last. Format per entry: done / next /
blockers / landmines / files touched. A session that dies mid-task must be recoverable
from its own file alone.

**A session number is an allocation, not an identifier** ([ADR-064](../decisions/ADR-064.md)).
Parallel sessions on one day share a number and a log file, each appending its own `##`
section, so one file legitimately carries several rows here. The row identifies a session;
the first cell only points at the file that holds it. Rows are ordered by session number,
and within a number by the order the sections appear in the file.

**Numbers CLAIMED and not yet written.** The 110, 111 and 112 collision of 2026-08-21 was
not a numbering mistake, it was the absence of a claim: three app sessions and three
[WAVE-04](../plans/WAVE-04-fixture-backlog-and-gate-inventory.md) sessions each read the
highest row here and each took the next number, correctly and simultaneously. **A number
is free when no row claims it, and a row here is the only claim there is.** So a session
dispatched against a number claims it before it runs. **`119` is this session and is written**, which is why it is in the entries below and not here.

| Number      | Claimed for                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | State                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ~~**114**~~ | ~~WAVE-04 `W7`, `CI-09` the nightly workflow~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**115**~~ | ~~WAVE-04 `W8`, three of the four `CI-06` slug gates~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**120**~~ | ~~`0045_simulation_runs.sql`, `SD-M21-01` and `SD-M21-02` ([M21](../plans/M21-plan-designer.md) section 2.1)~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**121**~~ | ~~WAVE-04 `W9`, `CI-06/gate-inventory`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**122**~~ | ~~The four Tier 2 fixture-shape rulings, [WAVE-04 section 2.3](../plans/WAVE-04-fixture-backlog-and-gate-inventory.md)~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**123**~~ | ~~[WAVE-05](../plans/WAVE-05-tier2-fixture-shapes.md) `X1`, the six comments naming the `GS-nnn` each assertion site discharges~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**124**~~ | ~~WAVE-05 `X6`, `GS-030`'s surviving half in [`trading-calendar-generator.test.ts`](../../packages/db/test/trading-calendar-generator.test.ts)~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**125**~~ | ~~[ADR-074](../decisions/ADR-074.md)'s four disagreements, reported by `CI-06/identifier-series` in session 115 and unresolved inside a `proposed` ruling~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**126**~~ | ~~Three signatures and one refusal on the four `proposed` ADRs~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**127**~~ | ~~[WAVE-05](../plans/WAVE-05-tier2-fixture-shapes.md) `X2`, section 39 gains `covered-elsewhere` and the stale rows move~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**128**~~ | ~~WAVE-05 `X3` and `X4`, `GS-141`'s severity half and the sole-error equalities, one file~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**129**~~ | ~~WAVE-05 `X5`, `GS-071` at scale in [`replay.test.ts`](../../apps/worker/test/replay.test.ts)~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**130**~~ | ~~WAVE-05 `X7`, the ADR repairing `GS-030`'s and `EC-012`'s falsified clause~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**131**~~ | ~~WAVE-05 `X8`, `CI-06/fixture-inventory` learns `covered-elsewhere`, **on `X2`'s branch** because the status and the vocabulary that validates it are one change~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**133**~~ | ~~`P2-a`, [ADR-074](../decisions/ADR-074.md) section 8's runner edits, after which `CI-06/identifier-series` reports 117 and 1,083 ([P2 build plan](../plans/P2-rules-engine-build.md))~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**134**~~ | ~~`P2-b`, `replay` joins the rules engine's public surface ([ADR-078](../decisions/ADR-078.md)). **MONEY PATH** ([P2 build plan](../plans/P2-rules-engine-build.md))~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**135**~~ | ~~`P2-c`, the eval-pass `CHECK` on `rule_states` (`ADR-079`, migration `0046`). **MONEY PATH, `E2` READ** ([P2 build plan](../plans/P2-rules-engine-build.md))~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**136**~~ | ~~`P2-d`, extending [ADR-073](../decisions/ADR-073.md) disposition (b) to the twelve `VG` rows (`ADR-080`) ([P2 build plan](../plans/P2-rules-engine-build.md))~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**137**~~ | ~~`P2-f`, retiring the pre-[ADR-048](../decisions/ADR-048.md) golden block and one comment that is false about the code beneath it ([P2 build plan](../plans/P2-rules-engine-build.md))~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**138**~~ | ~~`P2-g`, `hash.ts` into `packages/rules-engine` (`ADR-081`), **after 134**. **MONEY PATH** ([P2 build plan](../plans/P2-rules-engine-build.md))~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**139**~~ | ~~`P2-e`, [section 39](../testing/golden-scenarios/39-fixture-status-and-blockers.md)'s stale line numbers, `GS-071`, and [ADR-077](../decisions/ADR-077.md)'s third recitation, **after 133** ([P2 build plan](../plans/P2-rules-engine-build.md))~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**140**~~ | ~~`P2-h`, the worker's replay audit folds the engine's `replay`, **after 134 and 138**. **MONEY PATH** ([P2 build plan](../plans/P2-rules-engine-build.md))~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**141**~~ | ~~The `0015` citation audit and `OI-27`, both named by [session 132](2026-08-22-session-132.md) as owed and not P2's~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**142**~~ | ~~`OI-28`, widening [ADR-042](../decisions/ADR-042.md)'s closed `UNIT_TOKENS` for a calibration vendor's observation date (`ADR-082`)~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**143**~~ | ~~P3 planned against the tree: ledger, billing and identity ([DELIVERY_PLAN](../DELIVERY_PLAN.md) section 4)~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**144**~~ | ~~`P3-a`, the `API` container is ruled and gets a deployable (`ADR-083`). [OVERVIEW](../architecture/OVERVIEW.md) section 2 draws it and section 3 does not row it ([P3 build plan](../plans/P3-ledger-billing-identity.md))~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**145**~~ | ~~`P3-b`, the Drizzle client and `scopedDb`'s methods (`ADR-084`), which [ADR-008](../decisions/ADR-008.md) made part of its own acceptance. **MONEY PATH** ([P3 build plan](../plans/P3-ledger-billing-identity.md))~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**146**~~ | ~~`P3-c`, `CI-04` acquires an artifact and a selected project, and `VG-3` and `VG-6` expire with it (`ADR-085`), **after 142 and 145** ([P3 build plan](../plans/P3-ledger-billing-identity.md))~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**147**~~ | ~~`P3-d`, the job interface and pg-boss ([ADR-006](../decisions/ADR-006.md)) behind it (`ADR-086`), **after 146**. **MONEY PATH** ([P3 build plan](../plans/P3-ledger-billing-identity.md))~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**148**~~ | ~~`P3-e`, `OI-29`'s enforcement: the publish decision becomes SOUND and not only present (`ADR-087`, migration `0047`), **after 141, 142 and 146**. **MONEY PATH, `E2` READ** ([P3 build plan](../plans/P3-ledger-billing-identity.md))~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**156**~~ | ~~`VG-8`'s `PreToolUse` half, which [ADR-080](../decisions/ADR-080.md) ruled a **FINDING** rather than a waiting row: three documents name the hook as its implementation and [`.claude/settings.json`](../../.claude/settings.json) carries `SessionStart` and `Stop` only. Implements a signed ruling, so it mints no ADR~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**157**~~ | ~~**Measurement wave**: [M03](../plans/M03-billing-checkout.md) (billing and checkout) measured against the tree, the way [session 143](2026-08-23-session-143.md) measured P3. **Deliverable is its own session log and nothing else** -- no new document, no INDEX row, no STATE append, so the wave collides on nothing~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**158**~~ | ~~**Measurement wave**: [M04](../plans/M04-trader-portal.md) (the trader portal) measured against the tree, the way [session 143](2026-08-23-session-143.md) measured P3. **Deliverable is its own session log and nothing else** -- no new document, no INDEX row, no STATE append, so the wave collides on nothing~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**159**~~ | ~~**Measurement wave**: [M05](../plans/M05-payout-system.md) (the payout system) measured against the tree, the way [session 143](2026-08-23-session-143.md) measured P3. **Deliverable is its own session log and nothing else** -- no new document, no INDEX row, no STATE append, so the wave collides on nothing~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**160**~~ | ~~**Measurement wave**: [M06](../plans/M06-admin-ops-console.md) (the admin and ops console) measured against the tree, the way [session 143](2026-08-23-session-143.md) measured P3. **Deliverable is its own session log and nothing else** -- no new document, no INDEX row, no STATE append, so the wave collides on nothing~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**161**~~ | ~~**Measurement wave**: [M07](../plans/M07-risk-abuse.md) (risk and abuse) measured against the tree, the way [session 143](2026-08-23-session-143.md) measured P3. **Deliverable is its own session log and nothing else** -- no new document, no INDEX row, no STATE append, so the wave collides on nothing~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**162**~~ | ~~**Measurement wave**: [M08](../plans/M08-affiliate-system.md) (the affiliate system) measured against the tree, the way [session 143](2026-08-23-session-143.md) measured P3. **Deliverable is its own session log and nothing else** -- no new document, no INDEX row, no STATE append, so the wave collides on nothing~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**163**~~ | ~~**Measurement wave**: [M10](../plans/M10-integrations.md) (integrations) measured against the tree, the way [session 143](2026-08-23-session-143.md) measured P3. **Deliverable is its own session log and nothing else** -- no new document, no INDEX row, no STATE append, so the wave collides on nothing~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **WRITTEN.** Struck, and its entry is below. **This row merged at 15:57 UTC and this session's opening pull had found `ef4cccc` (15:49 UTC)**, so it measured a tree with no `163` row and recorded that as a finding; the row is real, it arrived mid-measurement, and this is the strike it asked for. **The clause "collides on nothing" is the one part refuted by running it**: the log makes the `session_entries` span stale, which is `CI-06g` and `CI-06n` at 27 of 29, and every session in the wave writes this file through the generator                                                                                                                                                                                                                                                                                                                      |
| ~~**164**~~ | ~~**Measurement wave**: [M13](../plans/M13-trader-analytics-journal.md) (trader analytics and journal) measured against the tree, the way [session 143](2026-08-23-session-143.md) measured P3. **Deliverable is its own session log and nothing else** -- no new document, no INDEX row, no STATE append, so the wave collides on nothing~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**165**~~ | ~~**Measurement wave**: [M16](../plans/M16-notification-center.md) (the notification centre) measured against the tree, the way [session 143](2026-08-23-session-143.md) measured P3. **Deliverable is its own session log and nothing else** -- no new document, no INDEX row, no STATE append, so the wave collides on nothing~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **WRITTEN.** Struck, and its entry is below. **The reservation landed on `main` AFTER this branch opened**, so the measurement below reports the row absent at its base commit `ef4cccc`; it is struck here rather than written here                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ~~**166**~~ | ~~**Measurement wave**: [M17](../plans/M17-offers-engine.md) (the offers engine) measured against the tree, the way [session 143](2026-08-23-session-143.md) measured P3. **Deliverable is its own session log and nothing else** -- no new document, no INDEX row, no STATE append, so the wave collides on nothing~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**167**~~ | ~~[M18 the graduation track](../plans/M18-graduation-track.md) measured against the tree: an INVENTORY, no document and no ruling~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**168**~~ | ~~Measurement wave: [M19](../plans/M19-kyc-identity.md) (KYC and identity) measured against the tree~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**169**~~ | ~~**Measurement wave**: [M20](../plans/M20-wallet.md) (the wallet) measured against the tree, the way [session 143](2026-08-23-session-143.md) measured P3. **Deliverable is its own session log and nothing else** -- no new document, no INDEX row, no STATE append, so the wave collides on nothing~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**170**~~ | ~~**Measurement wave**: [M21](../plans/M21-plan-designer.md) (the plan designer) measured against the tree, the way [session 143](2026-08-23-session-143.md) measured P3. **Deliverable is its own session log and nothing else** -- no new document, no INDEX row, no STATE append, so the wave collides on nothing~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**152**~~ | ~~The ADR registry table and the session ENTRY table become GENERATED spans (`ADR-088`); the CLAIM table is deliberately NOT generated~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**172**~~ | ~~**P4 planned against the tree**, the way [session 143](2026-08-23-session-143.md) planned P3 -- but consuming the FOURTEEN measurement logs sessions 157 to 170 wrote rather than measuring from scratch. **Eight of them independently found the same shape** and that agreement is the plan's starting evidence. Fence: `docs/plans/P4-*.md` (new), INDEX (its row), STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**173**~~ | ~~`CI-06/retired-constraints`, [session 141](2026-08-23-session-141.md)'s finding: seven constraints have been retired by superseding migrations and three stale citations have been merged since, because the mandated grep runs BEFORE citing and never AFTER superseding. Unlettered, seeded from the migration directory. Fence: `scripts/corpus/gates.mjs`, `scripts/corpus/falsify.mjs`, STRATEGY section 4.4's row only, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**174**~~ | ~~**M08's affiliate rail defect**, found by [session 162](2026-08-24-session-162.md): `payout_transfers.payout_request_id` is `NOT NULL REFERENCES payout_requests(id)` and `payout_requests` is `NOT NULL` to both `accounts` and `plan_versions`, so **an affiliate settlement cannot be a row in either table**, while `affiliate_statements.paid_transfer_ref` is bare `text` with no FK. **RULING ONLY, no migration.** MONEY PATH. Fence: one new ADR, ALLOCATION (its row), INDEX (its row), STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**175**~~ | ~~**M13's grants contradiction**, found by [session 164](2026-08-24-session-164.md): _"the role that implements its one money-path invariant revokes the table that invariant reads."_ Re-derive it at the source before ruling. **RULING ONLY, no migration.** MONEY PATH. Fence: one new ADR, ALLOCATION (its row), INDEX (its row), STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**176**~~ | ~~The six `portal-api` code sites [ADR-089](../decisions/ADR-089.md) retires, named and deliberately left by [session 154](2026-08-24-session-154.md). Non-money, mechanical, but every site must be read rather than sed-replaced. Fence: `apps/portal/**`, `apps/api/**`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**177**~~ | ~~Two stale one-liners that three sessions have now named and none held in fence: [`pnpm-workspace.yaml`](../../pnpm-workspace.yaml)'s _"postgres.js costs 0 transitive packages against `pg`'s 6"_ against [ADR-084](../decisions/ADR-084.md)'s measured **14, 13 transitive**; and [STATE](../STATE.md)'s stale `VG-11` assignment, named by [ADR-073](../decisions/ADR-073.md) and again by [ADR-080](../decisions/ADR-080.md). Fence: those two files, `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**179**~~ | ~~**[P4](../plans/P4-portal-and-site.md) section 10 item 1: who owns [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts) across phases.** Ten of fourteen measurements proposed the same slice on one pair of files, correctly, from inside fourteen separate fences. P4 calls this **the single decision that most changes how many of the remaining phases can run concurrently** and refuses to take it. **RULING ONLY** (`ADR-092`), no transcription, no `packages/db` change. Fence: one new ADR, ALLOCATION (its row), INDEX (its row), STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**180**~~ | ~~**[P4](../plans/P4-portal-and-site.md) section 10 item 3: whether auth gets a phase, and which.** [API_CONTRACT](../architecture/API_CONTRACT.md) specifies it, the schema carries it, no code implements it, and **no phase's contents name it**. Three readings are available and P4 takes none. Amending a done-condition needs an ADR because [DELIVERY_PLAN](../DELIVERY_PLAN.md) is frozen. **RULING ONLY** (`ADR-093`). Fence: one new ADR, ALLOCATION (its row), INDEX (its row), STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**183**~~ | ~~**[P4](../plans/P4-portal-and-site.md) `P4-c`: the UI framework is RULED, and `CI-07`'s cell plus `VG-2`'s chain move in the same commit.** [ADR-083](../decisions/ADR-083.md) rules the API's runtime and nothing about a UI; M04 and M09 both name Next.js and the workspace holds none. **`VG-10` must NOT be expired with `VG-2`.** `ADR-095`. Fence: P4 section 8's `P4-c` row~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**182**~~ | ~~**[P4](../plans/P4-portal-and-site.md) `P4-a`: the drift ruling, then `plan_versions`, `rule_states` and `sessions` enter the transcription.** How an [ADR-084](../decisions/ADR-084.md) transcription reads a table whose column set is a `CREATE TABLE` plus later `ADD COLUMN`s. **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict, plan mode. Carries [ADR-092](../decisions/ADR-092.md) section 5's precondition: `DDL_NAMES` derived from `SQL_NAME` or declared total. `ADR-094`. Fence: P4 section 8's `P4-a` row~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**184**~~ | ~~**Apply [ADR-092](../decisions/ADR-092.md)'s four named amendments to [P4](../plans/P4-portal-and-site.md), and [ADR-093](../decisions/ADR-093.md)'s two cell edits to [DELIVERY_PLAN](../DELIVERY_PLAN.md).** Both rulings write their edits out verbatim and neither made them, each being outside its own fence. **Transcribe, do not compose**, and rule nothing. No ADR. Fence: `docs/plans/P4-portal-and-site.md`, `docs/DELIVERY_PLAN.md`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**185**~~ | ~~**[P4](../plans/P4-portal-and-site.md) section 10 item 6: what `apps/site`'s adapter reads through.** `SitePorts` at [`ports.ts:115`](../../apps/site/src/catalog/ports.ts) is four public reads with no identity, and `SystemReason` at [`scoped-db.ts:143`](../../packages/db/src/scoped-db.ts) is a closed two-member vocabulary neither of whose members is a marketing page. **RULING ONLY** (`ADR-096`), no adapter written. **It blocks `P4-e` alone and `P4-e` cannot start without it.**~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**186**~~ | ~~**[P4](../plans/P4-portal-and-site.md) section 10 item 4: whether `GS-143` and `GS-144`'s blocker term moves.** Both are P4's done-condition, both are rowed `no-fixture-format` with 264 others, and **neither is a fixture in any format**: their subject is a build that does not exist. Whether [ADR-072](../decisions/ADR-072.md)'s closed six-term vocabulary has a term, gains one, or these two rows leave section 39. **RULING ONLY** (`ADR-097`). **`P4-j` must not invent one and this is why.**~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**187**~~ | ~~[P4](../plans/P4-portal-and-site.md) section 10 item 2: MEASURE the cost of a gate reconciling each module plan's endpoint table against [API_CONTRACT](../architecture/API_CONTRACT.md)~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**188**~~ | ~~**[P4](../plans/P4-portal-and-site.md) `P4-b`: the other ten P4 read tables enter the transcription** -- `plan_version_sizes`, `content_documents`, `page_revalidations`, `statistic_definitions`, `published_statistics`, `proof_links`, `review_requests`, `certificates`, `daily_marks`, `purchases`. **All ten measured clean of later column changes**, so no ruling is needed and [ADR-094](../decisions/ADR-094.md)'s replay is the pattern. **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR.** Fence: P4 section 8's `P4-b` row~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**189**~~ | ~~**The hole `session 183` (unlinked until #240 merges)'s approval clause found, and it is true on `main` TODAY**: an operator-shaped API route inside a UI deployable is refused by nothing. [ADR-083](../decisions/ADR-083.md)'s construction rests on the operator path not existing in the public deployment and **no check enforces that**. **RULING plus, if the ruling calls for one, the mechanism** (`ADR-098`). Independent of whether `ADR-095` merges: the gap predates the framework~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**190**~~ | ~~**M03's tables enter the transcription**, under [ADR-092](../decisions/ADR-092.md) section 8 item 4: the owner is the TABLE, first writer wins, and the slice is **M03's tables MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster. Measured by [session 157](2026-08-24-session-157.md); the plan is [M03](../plans/M03-billing-checkout.md). **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing** ([session 188](2026-08-25-session-188.md)'s precedent). Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**191**~~ | ~~**M06's tables enter the transcription**, under [ADR-092](../decisions/ADR-092.md) section 8 item 4: the owner is the TABLE, first writer wins, and the slice is **M06's tables MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster. Measured by [session 160](2026-08-24-session-160.md); the plan is [M06](../plans/M06-admin-ops-console.md). **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing** ([session 188](2026-08-25-session-188.md)'s precedent). Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**192**~~ | ~~**M07's tables enter the transcription**, under [ADR-092](../decisions/ADR-092.md) section 8 item 4: the owner is the TABLE, first writer wins, and the slice is **M07's tables MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster. Measured by [session 161](2026-08-24-session-161.md); the plan is [M07](../plans/M07-risk-abuse.md). **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing** ([session 188](2026-08-25-session-188.md)'s precedent). Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**193**~~ | ~~**M16's tables enter the transcription**, under [ADR-092](../decisions/ADR-092.md) section 8 item 4: the owner is the TABLE, first writer wins, and the slice is **M16's tables MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster. Measured by [session 165](2026-08-24-session-165.md); the plan is [M16](../plans/M16-notification-center.md). **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing** ([session 188](2026-08-25-session-188.md)'s precedent). Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **WRITTEN.** Struck, and its entry row is generated below. **5 of 6**: `otp_challenges` stays unregistered, refused by [ADR-094](../decisions/ADR-094.md)'s fold                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ~~**194**~~ | ~~**M19's tables enter the transcription**, under [ADR-092](../decisions/ADR-092.md) section 8 item 4: the owner is the TABLE, first writer wins, and the slice is **M19's tables MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster. Measured by [session 168](2026-08-24-session-168.md); the plan is [M19](../plans/M19-kyc-identity.md). **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing** ([session 188](2026-08-25-session-188.md)'s precedent). Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**195**~~ | ~~**M04's tables enter the transcription**, under [ADR-092](../decisions/ADR-092.md): the owner is the TABLE, first writer wins, and the slice is **M04's tables MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster. Measured by [session 158](2026-08-24-session-158.md); the plan is [M04](../plans/M04-trader-portal.md). **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing.** Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**196**~~ | ~~**M05's tables enter the transcription**, under [ADR-092](../decisions/ADR-092.md): the owner is the TABLE, first writer wins, and the slice is **M05's tables MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster. **[ADR-092](../decisions/ADR-092.md) section 8 item 4 names this slice and `M20-a` as the ONE intersecting pair: both claim the same four wallet tables.** Whichever runs second reports them as already registered; neither coordinates. Measured by [session 159](2026-08-24-session-159.md); the plan is [M05](../plans/M05-payout-system.md). **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing.** Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**197**~~ | ~~**M08's tables enter the transcription**, under [ADR-092](../decisions/ADR-092.md): the owner is the TABLE, first writer wins, and the slice is **M08's tables MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster. Measured by [session 162](2026-08-24-session-162.md); the plan is [M08](../plans/M08-affiliate-system.md). **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing.** Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**198**~~ | ~~**M10's tables enter the transcription**, under [ADR-092](../decisions/ADR-092.md): the owner is the TABLE, first writer wins, and the slice is **M10's tables MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster. Measured by [session 163](2026-08-24-session-163.md); the plan is [M10](../plans/M10-integrations.md). **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing.** Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**199**~~ | ~~**M13's tables enter the transcription**, under [ADR-092](../decisions/ADR-092.md): the owner is the TABLE, first writer wins, and the slice is **M13's tables MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster. Measured by [session 164](2026-08-24-session-164.md); the plan is [M13](../plans/M13-trader-analytics-journal.md). **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing.** Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**200**~~ | ~~**M17's tables enter the transcription**, under [ADR-092](../decisions/ADR-092.md): the owner is the TABLE, first writer wins, and the slice is **M17's tables MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster. Measured by [session 166](2026-08-24-session-166.md); the plan is [M17](../plans/M17-offers-engine.md). **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing.** Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**201**~~ | ~~**M18's tables enter the transcription**, under [ADR-092](../decisions/ADR-092.md): the owner is the TABLE, first writer wins, and the slice is **M18's tables MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster. Measured by [session 167](2026-08-24-session-167.md); the plan is [M18](../plans/M18-graduation-track.md). **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing.** Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **WRITTEN.** Struck, and its entry row is generated below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ~~**202**~~ | ~~**M20's tables enter the transcription**, under [ADR-092](../decisions/ADR-092.md): the owner is the TABLE, first writer wins, and the slice is **M20's tables MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster. **The other half of the intersecting pair with `M05`.** See session 196. Measured by [session 169](2026-08-24-session-169.md); the plan is [M20](../plans/M20-wallet.md). **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing.** Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**203**~~ | ~~**M21's tables enter the transcription**, under [ADR-092](../decisions/ADR-092.md): the owner is the TABLE, first writer wins, and the slice is **M21's tables MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster. Measured by [session 170](2026-08-24-session-170.md); the plan is [M21](../plans/M21-plan-designer.md). **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing.** Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**205**~~ | ~~**`M02`'s tables enter the transcription** (`M02-rithmic-bridge`), the wave's largest slice at seven candidates across three migrations. **`M02` is the ONE plan in the corpus that is not `approved`**: [ADR-005](../decisions/ADR-005.md) holds it at `review` pending the Rithmic vendor call. The session is dispatched anyway and **the reasoning is recorded rather than assumed**: [ADR-094](../decisions/ADR-094.md) rules that a transcription reads a table **as of the LAST MIGRATION**, so the SOURCE is the merged and frozen DDL and never the plan, and [ADR-092](../decisions/ADR-092.md) already moved ownership from the module to the TABLE. The plan is the slicing device only. **The session verifies that premise before it transcribes and STOPS if the plan's `review` status touches anything it is about to write.** The slice is that module's tables **MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster, under [ADR-092](../decisions/ADR-092.md): the owner is the TABLE and the first writer wins. **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing.** Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~ | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**206**~~ | ~~**`M14`'s and `M15`'s tables enter the transcription** (`M14-loyalty-retention`, `M15-discord-integration`), five candidates across two migrations. Two modules in one session because neither slice is worth its own merge and the two table sets are disjoint. The slice is that module's tables **MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster, under [ADR-092](../decisions/ADR-092.md): the owner is the TABLE and the first writer wins. **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing.** Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**207**~~ | ~~**`M09`'s, `M11`'s and `M16`'s tables enter the transcription** (`M09-marketing-site`, `M11-certificates-social-proof`, `M16-notification-center`), four candidates. Grouped because two of them are the `tos_versions` / `tos_acceptances` pair in the same migration, and reading one without the other is how a version table and its acceptances get different classes. The slice is that module's tables **MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster, under [ADR-092](../decisions/ADR-092.md): the owner is the TABLE and the first writer wins. **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing.** Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**208**~~ | ~~**`M18`'s, `M06`'s, `M10`'s and `M01`'s remaining tables enter the transcription** (`M18-graduation-track`, `M06-admin-ops-console`, `M10-integrations`, `M01-rules-engine`), four singles. **`M01`'s slice is ONE table and not two**: `events` is refused and its refusal is already recorded in `packages/db/src/scope.ts`. **Do not register it.** The slice is that module's tables **MINUS whatever is already registered**, computed from `TABLE_KEYS` on the tree the session opens rather than from a roster, under [ADR-092](../decisions/ADR-092.md): the owner is the TABLE and the first writer wins. **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR: a transcription rules nothing.** Fence: `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/test/scoped-db.test.ts`, STATE (append), `sessions/`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**209**~~ | ~~**`P4-d`: Fastify enters the catalog and `apps/api` gets a ROUTE REGISTRY.** [P4](../plans/P4-portal-and-site.md) section 11's own row is the specification and this session reads it rather than a restatement. **THE CRITICAL PATH**: `P4-f`, `P4-h` and every M19 route slice sit behind it, and the row says the registry's SHAPE decides whether M19's seven serialize. Unblocked by [ADR-095](../decisions/ADR-095.md) and [ADR-099](../decisions/ADR-099.md) landing. **NOT money path by content, HIGH consequence by shape.** Fence is the plan's row verbatim~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**210**~~ | ~~**`P4-e`: the site's four read ports get an adapter**, so the config-rendered plans and rules pages resolve against `systemDb` and render real rows. [P4](../plans/P4-portal-and-site.md) section 11's row is the specification. **This is the first thing in this repository a person can LOOK at.** Its dependencies `P4-a` and `P4-b` are discharged: the registry is at 80 of 111. **NOT money path**: it reads a published config and holds no session. **[ADR-096](../decisions/ADR-096.md)'s hole is its neighbour and `RI-08` is still unwritten** -- the session reports whether its work closes, widens or leaves it~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**211**~~ | ~~**PLANNING SESSION: `P3` wave 3, the six modules.** [P3](../plans/P3-ledger-billing-identity.md) section 6 deliberately did not dispatch it and stated why: _"every module session's fence is a path inside a deployable `ADR-083` has not yet named"_. **THAT BLOCKER HAS EXPIRED** -- `ADR-083` landed, `apps/api` exists, and `P4-d` is writing the route registry. **P3 IS THE LARGEST GAP IN THE PROJECT: all six of its stated contents are ZERO application code.** The plan's own ordering table is the input. **Highest reasoning: depth here converts directly into migrations never written.** Output is a plan document plus reserved numbers, NOT code~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**212**~~ | ~~**The `wallet_entries` scope-rule ruling, and it is a NEW ADR.** Session 202 SEEDED a `derived` rule on `wallet_entries` through `ledger_transactions` and **`pnpm run typecheck` exited 0 with the suite green at 78 of 78** -- a wrong scope rule that every mechanical check in the package accepts. It compiles and terminates because `ledgerTransactions` is registered `derived`; it answers _whose ledger accounts appear on this transaction_ rather than _whose wallet holds this entry_, and the two agree only while no transaction touches two identities' accounts, **which double-entry makes the normal case**. **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. Reported by [#262](https://github.com/lam2-coder/meritfutures/pull/262) and repaired by nobody, because an integration rules nothing~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**213**~~ | ~~**`P3-f`: the accessor learns to WRITE, to run a TRANSACTION, and to say who is asking when nobody is.** [P3-wave3-modules](../plans/P3-wave3-modules.md) section 9's row is the specification and section 15 is the prompt. **THE BARRIER: every other slice in P3 wave 3 is downstream of it, and NOTHING blocks it.** `ScopedDb` is `__brand`, `identityId` and `rows()`; [ADR-092](../decisions/ADR-092.md) section 9 says _"this entry rules NOTHING about the write accessor"_; and `packages/queue`'s `enqueue` takes a `JobTransaction` nothing in the workspace can produce. Sessions [157](2026-08-24-session-157.md) and [168](2026-08-24-session-168.md) proposed this same slice from inside two fences. **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict, `E2`. **Its test file is NEW and not `scoped-db.test.ts`, which is the whole reason it runs concurrently with five other sessions**~~                                                                                                                                                                                                                                                                                                                                                                                                                                | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**214**~~ | ~~**`P3-g`: column TYPE and NULLABILITY are compared, and `ALTER COLUMN` stops being a proxy refusal.** [ADR-094](../decisions/ADR-094.md) section 3 wrote this session's specification and priced it and nobody numbered it: _"measuring that is its own session"_. **It is on P3's critical path**, because `otp_challenges` is refused by that fold and `POST /auth/otp` is the only endpoint in the contract that runs before an identity exists. **The cost is unmeasured by construction** and ADR-094's "seven" is now **80**. **MONEY PATH**, `E2`. Depends on sessions 205 to 208 via `schema.ts` and `scoped-db.test.ts`~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**215**~~ | ~~**`P3-h`: the four tables the registry cannot hold.** `identity_links`, `attributions` and `dedupe_matches` under **ONE** two-identity ruling rather than three per-table ones, plus `otp_challenges` under 214's widened fold; `affiliate_commissions` follows `attributions` mechanically. [ADR-092](../decisions/ADR-092.md) section 9 names the class three times and takes none, and `attributions` is the table [M08](../plans/M08-affiliate-system.md) section 3.1 resolves **inside the checkout transaction**. **MONEY PATH**, `E2`. Depends on **214** and on 205 to 208, all three files shared~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **216**     | **`P3-i`: the ledger posting path, and `PT-03`.** Double-entry posting as `packages/ledger`, because M03's `INV-M3-10`, M05's `DEP-M3-06`, M08's commission clock and the nightly batch all post through it and `RI-04` forbids an app depending on an app. `ledger_halts` enters the registry with it, closing one of [ADR-092](../decisions/ADR-092.md) section 9's four orphans. **MONEY PATH**, `E2`. Depends on **213** and **215**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **CLAIMED**, dispatched by session 211                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ~~**217**~~ | ~~**`P3-m`: the `PspAdapter` port and two fakes. No routes, no network, no vendor.** [Session 157](2026-08-24-session-157.md)'s `M3-h` verbatim, and the provider set is already closed at `"psp_a"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | "psp_b"`. **API_CONTRACT section 10's HMAC-before-parsing verifier belongs to this port** rather than to a route handler. **MONEY PATH** by subject. **Depends on nothing if it takes no dependency**, and joins the `pnpm-lock.yaml` serialization if it does~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **WRITTEN.** Struck, and its entry is below |
| ~~**218**~~ | ~~**`P3-j`: the authentication surface**, the seventh P3 content [DELIVERY_PLAN](../DELIVERY_PLAN.md)'s cell gained when [ADR-093](../decisions/ADR-093.md)'s edits landed and P3 section 6's ordering table does not carry. Seven contract headings, `GET /me`, `C-27` elevation, and **[API_CONTRACT section 12](../architecture/API_CONTRACT.md)'s rows written BEFORE the endpoints ship**, which is P3's fifth done-clause. **AUTH, MONEY PATH**, `E2`. Depends on **213**, **215** and **209**~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**219**~~ | ~~**`P3-k`: the idempotency layer and `POST /webhooks/psp/:provider`.** Verify before parsing, persist raw, dedupe on `(psp, provider_event_id)`, `200` for duplicates, defer out-of-order. **It does NOT depend on auth and that is deliberate**: a webhook carries an HMAC and no session, so it has no row in section 12's matrix. `idempotency_keys` is a first-writer-wins registration. **MONEY PATH**, `E2`. Depends on **213**, **215**, **217**, **209**~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | ~~**CLAIMED**, dispatched by session 211~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ~~**220**~~ | ~~**`P3-n`: checkout and attribution, ONE slice**, because [M08](../plans/M08-affiliate-system.md) section 3.1 resolves attribution inside the checkout transaction and two sessions writing inside one transaction are one session. `POST /checkout`, `POST /accounts/:accountId/reset`, the server-authoritative price and the recomputed coupon. **`POST /affiliate/creatives` is in M08 and in no contract, so its row is an ADR.** **MONEY PATH**, `E2`. Depends on **216**, **218**, **219**, **217**, **209**~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | ~~LANDED, dispatched by session 211~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **221**     | **`P3-o`: M19 KYC, the composite trigger set.** The vendor-agnostic provider port, `POST /kyc/session`, `GET /kyc/status`, `POST /webhooks/kyc/:provider`, and `{second_distinct_account_purchase, pre_funded}` firing on the earliest. **Merit never proxies documents** (`INV-M19-07`). **Dedupe, sanctions and reverification are NOT in it**: P3's content is the verification path and its triggers. **MONEY PATH**, `E2`. Depends on **218**, **219**, **220**, **209**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **CLAIMED**, dispatched by session 211                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ~~**222**~~ | ~~**`P3-l`: the provisioning saga against the simulator.** The `provisioning_queue` driver, the `ProvisioningOp` pipeline, `provision` and `entitle` against [`packages/rithmic`](../../packages/rithmic/src/index.ts), compensation, and `INV-M2-13`'s fail-closed exit. **It is the first caller [ADR-006](../decisions/ADR-006.md)'s transactional-enqueue criterion has ever been graded against.** **Writes no route, so it needs nothing from 209.** **MONEY PATH**, `E2`. Depends on **213** and **215**~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**223**~~ | ~~**`P3-p`: [ADR-023](../decisions/ADR-023.md) enrichment, observe mode.** The vendor-agnostic adapter, its `integration_contracts` row, signals recorded and scored and **nothing blocked**, which is the ruling rather than a choice. Last and smallest, and non-blocking, so nothing waits on it. **MONEY PATH** by position: it runs inside checkout's transaction. Depends on **213** and **220**~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | ~~LANDED, dispatched by session 211~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ~~**224**~~ | ~~**`P4-f`: M12's method pages, the contract row and then the route.** `GET /public/methods/:statCode` enters [API_CONTRACT](../architecture/API_CONTRACT.md) and `apps/api` serves it. **API_CONTRACT is `approved`, so the contract half is an ADR and not a commit**, and that ADR is **`110`, reserved before dispatch**. Unblocked by PR #240's merge on 2026-08-25, which discharged the `VG-12` admission every P4 slice behind `P4-c` was waiting on. **`P4-d` is landed** ([ADR-100](../decisions/ADR-100.md)), so this slice's route file is a new file under `apps/api/src/routes/` and **[P4](../plans/P4-portal-and-site.md) section 8's fence cell naming `apps/api/src/routes/index.ts` is STALE**: ADR-100 made the module list a DIRECTORY LISTING and that file does not exist. Not money. Depends on **`P4-d`** and **`P4-b`**, both landed~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **225**     | **`P4-j`: the parameter lint, `GS-143` and `GS-144`.** [P1 section 3](../plans/P1-monorepo-scaffold.md) tier 3: MDX carrying a bare parameter value fails the build, and a published statistic without its trailing window fails it including on the OG image path. **[Section 5.3](../plans/P4-portal-and-site.md) is its ruling and it must not invent one.** **NO ADR IS EXPECTED**: the fence's _"the blocker term moves"_ ADR is discharged by [ADR-097](../decisions/ADR-097.md), which writes both corrected rows out verbatim for transcription. `111` is reserved conditionally against the case that section 5.3 turns out not to reach. **ITS SECTION 8 FENCE IS TWO CELLS TOO NARROW AND THE DEFECT WAS EXECUTED RATHER THAN PREDICTED** ([session 186](2026-08-24-session-186.md)): a two-row fence over `39-fixture-status-and-blockers.md` cannot execute a two-row move, because `CI-06/fixture-inventory`'s fifth assertion derives the summary tables from the rows. The fence is widened to that file's summary cells by this reservation, and to nothing else. Not money. Depends on **`P4-c`** and **`P4-e`**, both landed                                                                                                                                                                                                         | **CLAIMED**, dispatched by this session                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ~~**226**~~ | ~~**`P4-h`: the portal's remaining READ screens, and the three shapes with no contract row.** `SC-M4-05` rules, `SC-M4-06` purchases and the rule diff, `SC-M4-07` KYC status, `SC-M4-08` certificates, `SC-M4-09` referrals, plus [session 158](2026-08-24-session-158.md)'s `required_factor`, `EconomicCalendarPanelResponse` and `ImpersonationSession`, **none of which has a contract row**, which is why an ADR is required rather than optional. **`SC-M4-04`, `SC-M4-10`, `SC-M4-01` and `SC-M4-11` are deliberately NOT in it** and [P4](../plans/P4-portal-and-site.md) section 6 says whose they are. Unblocked by [#291](https://github.com/lam2-coder/meritfutures/pull/291), session 224's `P4-f`, which was the last of its three dependencies to land. **ADR `111` is reserved for it.** Not money. Depends on **`P4-c`**, **176** and **`P4-f`**, all landed~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **WRITTEN.** Struck, and its entry is below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ~~**227**~~ | ~~**THE KEYED ACCESSOR: `packages/db` gains a read and a write that can name ONE ROW.** [ADR-109](../decisions/ADR-109.md) clause 2 states the need and deliberately does not design it, and [session 222](2026-08-26-session-222.md), [session 218](2026-08-26-session-218.md) and [session 219](2026-08-26-session-219.md) each hit the same wall from a different caller. **The design authority is DELEGATED to this session by the founder**, and the `E2` read on what it lands is still owed. **THE DEFECTIVE METHODS HAVE NO REAL CALLERS**, measured rather than assumed: every mention of `systemTx.update`, `firmTx.update` and `scopedTx.update` in the tree is a COMMENT routing around it, and the only real invocations of any of the six are two `.rows(...)` reads in [`packages/ledger`](../../packages/ledger/src/chart.ts). So the fix fits inside `packages/db` and does not break a caller. **MONEY PATH**, `E2`. **ADR `112` is reserved for it.** Depends on **[#293](https://github.com/lam2-coder/meritfutures/pull/293)** and **[#290](https://github.com/lam2-coder/meritfutures/pull/290)**, both merged~~                                                                                                                                                                                                                 | ~~LANDED, dispatched by this session~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **228**     | **`P4-i`: the slop-score pass, `SS-01` to `SS-08`, and `CI-08`'s cell moves with it.** Playwright enters the catalog, which is a **`VG-12` dependency admission** and is normally the founder's alone by that gate's own wording. **The founder has DELEGATED it explicitly for this slice.** Unblocked by [#296](https://github.com/lam2-coder/meritfutures/pull/296), session 226's `P4-h`, which was its last dependency. `ADR-116` is reserved. Not money. Depends on **`P4-e`** and **`P4-h`**, both landed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **LANDED 2026-08-26**, [session 228](2026-08-26-session-228.md). [ADR-116](../decisions/ADR-116.md) records the DELEGATED `VG-12` grant; `@playwright/test@1.56.1` admitted at `+4` closure, three real and one per-platform; `CI-08` implemented in the same commit as its artifact, at two of three projects and over fixtures because `apps/*/src/app/` does not exist. **The repaired probe fired for the right reason and then retired with its condition, and `falsify.mjs`'s two seeded cases retired with it, both measured dead before the edit**                                                                                                                                                                                                                                                                                                                 |
| **229**     | **`RI-08`, the invariant [ADR-096](../decisions/ADR-096.md) named and nobody wrote.** A marketing page can acquire a write-capable connection to the trader database in a one-line manifest diff and every check stays green; ADR-096 section 9 names the remedy and assigns it to `P4-e`, which landed without it. **Three sessions have now cited this hole and none has closed it.** `ADR-117` is reserved conditionally: the invariant number is already allocated by ADR-096, so a new entry is expected only if the shape needs a ruling. Not money. Depends on nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **CLAIMED**, dispatched by this session                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ~~**230**~~ | ~~**The two gates this corpus has named as OWED and not written.** First, **a markdown table that does not end where its author meant it to**: [#299](https://github.com/lam2-coder/meritfutures/pull/299) absorbed nine prose lines into the session reservation table and all 30 gates stayed green, because `CI-06u` looks for duplicate first cells and `CI-06v` for orphan fragments and neither looks at whether a row belongs. Second, **two independent increments of the same prose number merging green and wrong**, which the queue calls _"the gate that is owed"_ and which `ADR-042` already ruled prose cannot control. Both are `CI-06` slugs under [ADR-065](../decisions/ADR-065.md) and need no gate letter. [ADR-118](../decisions/ADR-118.md) reserved conditionally, and the condition was met. Not money. Depends on nothing~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | ~~LANDED, dispatched by this session~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **231**     | **The demo-world seed script, so `CI-09`'s replay leg stops being unprobeable.** [STRATEGY](../testing/STRATEGY.md) section 4.1 registers the artifact and [session 121](2026-08-21-session-121.md) rows it _"Registered. Names neither a path nor a manifest key"_, which is one of the four conditions [ADR-073](../decisions/ADR-073.md) section 2 (b) already names OPEN and which `CI-06/gate-inventory` reports unprobeable every run. **Ruling the path is the deliverable and building the script is the second half**; this is the only blocker in the graph that is not another session. `ADR-119` is reserved REQUIRED. Not money by subject, but it unblocks `P4-g`, which is. Depends on nothing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **LANDED 2026-08-26.** [ADR-119](../decisions/ADR-119.md), `status: proposed`, unsigned. The artifact is [`scripts/demo/world.ts`](../../scripts/demo/world.ts) and the world is a **VALUE over `BatchPorts`**, not a Postgres seed. **`CI-06/gate-inventory` is RED with two predicted findings**, which is disposition (b) working; the probe and the `nightly.yml` job are owed and the probe collides with session **230**. See [session 231](2026-08-26-session-231.md)                                                                                                                                                                                                                                                                                                                                                                                               |
| ~~**232**~~ | ~~**THE AUTH WIRING: `AuthBackend` and `IdempotencyStore` get an implementation, and `apps/api` is ADMITTED to the trader database.** Every auth route on `main` answers `503` because [session 218](2026-08-26-session-218.md) declared `AuthBackend` blocked on its own declaration, and [ADR-113](../decisions/ADR-113.md) records session 220 declining `IdempotencyStore` for the same reason: both need `@merit/db` in a manifest that declares none. **[ADR-112](../decisions/ADR-112.md) removed the predicate obstacle and [ADR-117](../decisions/ADR-117.md) turned the dependency into a NAMED ADMISSION**, so the slice is now well defined rather than blocked: admit `apps/api` to `DB_ADMITTED`, declare the accessor, implement both ports. **MONEY PATH, AUTH**, `E2`. `ADR-120` reserved REQUIRED: the admission is an authority change on the deployable serving the whole public surface~~                                                                                                                                                                                                                                                                                                                                                                                                                                          | ~~LANDED 2026-08-27, [ADR-120](../decisions/ADR-120.md), dispatched by this session~~                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **233**     | **THE `scripts/` SPECIFIER SWEEP, and `RI-10` widened to cover it.** 16 specifiers across 7 files in `scripts/demo/` name `.js` files that are `.ts`, so `scripts/demo/main.ts` dies with `ERR_MODULE_NOT_FOUND` under the node loader on `main` today, and the `.mjs` wrappers are what make any of it runnable. **`RI-10` misses it because `RI-10` was deliberately scoped to `src/`**, and the scoping decision is the finding rather than the specifiers. Confirmed pre-existing against `origin/main` before any branch was blamed. Not money. `ADR-121` reserved CONDITIONALLY: widening an invariant's scope is a ruling, transcribing 16 specifiers is not                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **LANDED 2026-08-27.** [ADR-121](../decisions/ADR-121.md), `status: proposed`, unsigned. **32 specifiers corrected across 7 files**, each checked against disk first; the reserved count of 16 was wrong and is superseded (19 across 5 non-test files, 32 across 7 with the two Vitest suites). `RI-10` widened behind a `runtimeLoaded` predicate: `src/` under apps/ or packages/, everything under `scripts/`, minus `test/`. **The `nightly.yml` line was told to move and did NOT**: `seed-world.ts` exits 0 writing zero bytes, so the move would have swapped a self-audit for a module-graph load and gone green doing it. 32 of 32 gates, 10 of 10 invariants, `verify` exit 0, 147 files / 2,553 passed. See [session 233](2026-08-27-session-233.md)                                                                                                           |
| **234**     | **`P4-g`: the M12 machine, the nightly statistics run.** Reads authoritative closed-session tables, computes each registered definition, writes one immutable `published_statistics` row per window and never overwrites. **`input_digest` is the reproduction claim and it needs a producer.** Unblocked at last by [#312](https://github.com/lam2-coder/meritfutures/pull/312): `CI-09`'s replay self-audit leg was its second dependency and had been _"waiting"_ since 2026-08-20. **MONEY PATH**: `INV-M12-01` binds it to closed-session authoritative data and `ST-03`/`ST-04` publish money on a public surface. `ADR-122` reserved REQUIRED. Depends on **`P4-b`** and **session 164's `M13-e`** via `ports.ts`, both landed                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | **LANDED 2026-08-27.** [ADR-122](../decisions/ADR-122.md), `status: proposed`, UNSIGNED, seven clauses. **`input_digest` is taken over the computation's OWN ARGUMENT rather than over a description of the query, and it EXCLUDES the answer**, so two runs whose digests match and whose values differ is an arithmetic regression. What it cannot cover is named: there is no `engine_version` column and folding a worker version into the digest bytes was REFUSED. **Immutability is left to the database**, which already refuses in three places. Two structural findings reported and not repaired: `statistic_definitions_live_uq` gives a statistic one live row carrying one `window_spec`, and `INV-M12-06`'s k floor has no number. **MONEY PATH, `E2` READ PENDING, NOT MERGED.** See [session 234](2026-08-27-session-234.md)                              |
| **235**     | **THE TWO THINGS ADR-119 AND ADR-115 LEFT OWED, which are one session because both are refusals moving to where they belong.** First, `ADR-119` clause 7: the `accountsAudited === 0` refusal lives in [`seed-world.ts`](../../scripts/demo/seed-world.ts) and is owed a home in `replay.ts`, because **a refusal in the caller is weaker than a refusal in the audit: the next caller inherits nothing.** Second, `packages/enrichment`'s call site in `checkout.ts`, which [session 223](2026-08-26-session-223.md) correctly shipped OWED rather than inventing a file session 220 was concurrently creating. **MONEY PATH by position**: the call site runs inside checkout's transaction. `ADR-123` reserved CONDITIONALLY                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **LANDED 2026-08-27.** [ADR-123](../decisions/ADR-123.md), `status: proposed`, unsigned. **Both halves landed and both were watched failing.** `runReplayAudit` refuses on `accountsAudited === 0` itself, watched RETURNING `accountsAudited: 0, diverged: 0` green on `421e52e` and THROWING `ReplayAuditRefusal` here, with `nightly.yml`'s `replay-self-audit` command byte-identical either side. The enrichment call site is wired and **both** of `ADR-115`'s two places are asserted, one by reading `checkout.ts` and one behaviourally: a hanging vendor, a throwing vendor and a throwing reporter each commit the purchase. **Three fence findings reported rather than resolved quietly**, among them that the refusal was in `world.ts` and not `seed-world.ts`. 32 of 32, 10 of 10, `verify` exit 0. **MONEY PATH BY POSITION, `E2` READ OWED, NOT MERGED** |
| **236**     | **PLANNING SESSION: `P5` planned against the tree**, the way [P3](../plans/P3-ledger-billing-identity.md) and [P4](../plans/P4-portal-and-site.md) were. [DELIVERY_PLAN](../DELIVERY_PLAN.md) gives `P5` **M20 wallet and the two-leg payout, the external rail in sandbox, the freeze path with its expiry sweep, the admin liability dashboard including wallet balances, and the event feed**, and **no `P5-*.md` plan exists**, so no `P5` slice can be dispatched without inventing scope. This session writes the plan and its slice table. Not money by file; every slice it names is                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | **LANDED 2026-08-27.** [P5-payouts-and-wallet](../plans/P5-payouts-and-wallet.md), `status: draft`, **fourteen slices over four waves**, fence BY FILE, money column, depends-on BY FILE, plus a per-file collision table against the thirteen sessions running beside it. **THE ANSWER IS THE INVERSE OF [P4](../plans/P4-portal-and-site.md)'S**: four of P5's five stated contents are WRITES and the fifth reads a table with no producer, so the phase P4 could build against a read-only accessor is the phase P5 cannot. **The precondition is four sentences in ONE file**, each written on purpose by [ADR-112](../decisions/ADR-112.md): `RowFilter` admits *"no `OR`, no `IN`, no range and no `IS NULL`"* and `SqlExecutorReason` has ONE member, so the hourly sweep cannot express `freeze_expires_at <= now()`, `readLiveHalts` cannot say `released_at IS NULL` and already pays for it on every posting, and **nothing in this tree can make one of two racing transactions lose**, which is `INV-M20-01`, `GS-230` and `DELIVERY_PLAN`'s own *"wallet concurrency green"*. **The seam is that one file and nothing else**, because [ADR-092](../decisions/ADR-092.md) already settled the transcription and [ADR-100](../decisions/ADR-100.md) already made the route registry a directory listing, so P5 is one serial head and then four concurrent route slices, three concurrent worker slices and a dashboard slice. **THE DISPATCH'S CENTRAL PREMISE DID NOT SURVIVE THE MEASUREMENT IT ASKED FOR**: `INV-M20-06`'s contradiction with the payout gate was CLOSED by [ADR-075](../decisions/ADR-075.md) on 2026-08-21, approved **in session 95**, which is the session the brief cites as having reported it; what remains is a **stale restatement inside [M20](../plans/M20-wallet.md)'s own `OQ-M20-07` body**, two of whose five door-inventory cells still carry the pre-`ADR-075` shape. **The contradiction P5 DID meet is [ADR-124](../decisions/ADR-124.md)'s**: [EVENTS:263](../architecture/EVENTS.md) makes the global ledger halt automatic, [`0016:55`](../../packages/db/migrations/0016_treasury_controls.sql) rules it is never a row, [`halts.ts:109`](../../packages/ledger/src/halts.ts) never refuses a firm-only posting, and [RB-07:31](../ops/runbooks/RB-07-ledger-imbalance.md) tells an operator to *"Confirm it"*. Four documents, four correct halves, no mechanism. The entry rules four clauses and **refuses to design the storage**. **Two P5 contents are in no phase cell and both are money path**: [M18](../plans/M18-graduation-track.md)'s terminal settlement, for which no document says what row it writes, and **`OI-06`'s destination registry**, where the gate is drawn, the enum value is in `0001`, the index scanning for it is in `0011` and the input has never existed. **All sixteen of P5's golden scenarios are `blocked / no-fixture-format`, so three of the four done-clauses sit behind `ADR-125` and the fourth names no scenario at all.** **MERGED `c947e39` AND RE-MEASURED ON THE RESULT**: [#330](https://github.com/lam2-coder/meritfutures/pull/330) reserved sessions 250 to 264 while this branch sat in review, **three of them intersect `P5`** (252 holds `POST /accounts/:id/payout`, which IS `P5-f`; 256 holds `GET /admin/liability`; 258 holds `POST /webhooks/rise`), the first free ADR number moved from `138` to `153`, **and `ADR-140`'s REQUIRED reservation restates the premise this session had just refuted**, so a money-path route session is dispatched against a contradiction [ADR-075](../decisions/ADR-075.md) closed. Neither row is edited; [P5](../plans/P5-payouts-and-wallet.md) section 5.5 names what session 252 will actually meet. **32 of 32 gates, `pnpm run verify` exit 0, 150 files / 2,656 passed / 1 skipped. No code, no migration, no gate, nothing signed**, and `ALLOCATION` holds six ADR numbers and two migration numbers this plan names and cannot spend |
| **237**     | **THE FIXTURE-FORMAT RULING, which is the largest blocked population in the corpus.** **227 of the 264 blocked golden scenarios sit behind ONE term**, `no-fixture-format`, whose reason text is corpus-wide: _"the only fixture format in the tree is M1's day stream folded through `advanceDay`."_ [P4 section 5.3](../plans/P4-portal-and-site.md) established it is a **W2** term, a statement about what a format can EXPRESS. **The premise has moved**: an API surface, a portal, a ledger, a KYC port and a checkout now exist, so a format for an HTTP surface is possible where it was not. `ADR-125` reserved REQUIRED. Not money                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | **CLAIMED**, dispatched by this session                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **238**     | **THE AUTH AUTHORITY RULING: minting a session, and resolving a person from the address they typed.** [ADR-120](../decisions/ADR-120.md)'s finding, in its own words: _"ADR-112 unblocked everything a session can DO and nothing that makes one."_ Read, revoke, list and elevate are ADDRESSES through the scoped door and all land. **Mint and resolve have no door, and both are AUTHORITY rather than PREDICATE** -- the shape [ADR-109](../decisions/ADR-109.md) clause 1 met one level up and answered in the other direction, on evidence that has since moved. **MONEY PATH, AUTH**, `E2`. `ADR-126` reserved REQUIRED                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **CLAIMED**, dispatched by this session                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **239**     | **`CI-09`'s STRYKER LEG**, waiting since 2026-08-20 on _"the VG-12 admission, whose human half a session cannot grant itself"_. **[#308](https://github.com/lam2-coder/meritfutures/pull/308) set the precedent four hours ago**: Playwright was admitted by delegation and [ADR-116](../decisions/ADR-116.md) RECORDED the delegation rather than proceeding as though the gate did not apply. The founder delegates this one on the same terms. `ADR-127` reserved REQUIRED. Not money                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | **CLAIMED**, dispatched by this session                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **240**     | **THE MIGRATION CLUSTER: `OI-01`, `OI-03`, `OI-04`, `OI-12` and `OI-13`, in ONE session because they share one migration number.** `liability_snapshots` in two shapes; `0026`'s append-only revoke list that drifts because it is a list; two legitimate single-column updates on append-only tables with no `SECURITY DEFINER` function; a calendar `INSERT` that moves no watermark; and `B.4` step 4's audited rewrite with no grant. **Five sessions would claim five migration numbers for what is one forward-only migration**, and a migration is never edited once merged. **MONEY PATH**, `E2`. `ADR-128` reserved REQUIRED, and the migration number is claimed in the same commit. **LANDED as TWO numbers, `0048` and `0049`**: `OI-01` is a founder choice between two named designs and does not belong in the same file as three `SECURITY DEFINER` paths, and `OI-03` needed no DDL at all | **LANDED 2026-08-27**, [ADR-128](../decisions/ADR-128.md), `0048` and `0049` |
| **241**     | **`OI-14` CLOSED and `OI-30` repaired, both in `apps/worker`.** `OI-14` reads _"the replay job must refuse an empty in-scope set"_ and [#315](https://github.com/lam2-coder/meritfutures/pull/315) DID THAT: `runReplayAudit(EMPTY_BOOK, CONFIG)` now rejects with `ReplayAuditRefusal`. **The register has not caught up, which is the blind spot [ADR-117](../decisions/ADR-117.md)'s own register calls a finding in itself.** `OI-30`: three citations in `replay.test.ts` name `0015` constraints that `0037` and `0046` retired, and `apps/worker` was outside the auditing session's fence. `ADR-129` reserved CONDITIONALLY                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **LANDED 2026-08-27.** **BOTH ROWS WERE STALE RATHER THAN OPEN.** `OI-14` is satisfied and closed on an EXECUTED refusal, and it was NOT [#315](https://github.com/lam2-coder/meritfutures/pull/315) that satisfied it: #315 added the `accountsAudited === 0` guard for the EMPTY BOOK and [ADR-123](../decisions/ADR-123.md) section 4 says in its own words that it does not close the row. `OI-14`'s guard is `storedRows > 0 && inScope === 0`, it predates #315, and **both guards were falsified independently**: neuter either alone and exactly one case goes red. **`OI-30` was repaired on 2026-08-24 by [session 151](2026-08-24-session-151.md), three days before this dispatch called it open**, and all three citations were re-derived from `0015`, `0037`, `0046` and the two live predicates rather than taken on that session's word. **None is a case where the test depends on the retired property, so `ADR-129`'s condition did not fire and the number RETURNS TO THE POOL.** **ONE NEW FINDING, EXECUTED**: an account named by `accountsWithStoredState()` with no rows and no days returns `{accountsAudited: 1, storedRows: 0, inScope: 0, diverged: 0}`, which neither guard sees and which is `FM-17` whole. It is `ADR-123` section 7's rejected alternative, whose stated reason holds for one half of that input class and not the other, both halves executed here. **Pinned in the suite as a HOLE and deliberately not fixed**: reversing an argued rejection on `INV-04`'s right-hand side is a ruling. **`apps/worker/src` UNEDITED. No migration, no ADR, no gate.** 32 of 32, 10 of 10, `verify` exit 0
| ~~**242**~~ | ~~**`OI-24`: M06's hand-maintained delta count, whose remedy the register already names as a `<!--gen:-->` span.** [ADR-034](../decisions/ADR-034.md) ruled that no document states a derivable count and `CI-06/derivable-counts` landed in [#309](https://github.com/lam2-coder/meritfutures/pull/309), so the remedy is now ENFORCEABLE rather than merely correct. **Four concurrent sessions were each about to make M06 section 2's _"Six deltas"_ sentence stale**, which is the drift this closes. `ADR-130` reserved CONDITIONALLY. Not money~~ | **WRITTEN.** Struck, and its entry is below. **The condition FIRED**: the gate was silent on the site at every value the sentence can hold, and `ADR-130` rules a `file` predicate so a per-document population is expressible |
| **243**     | [**Session 243**](2026-08-27-session-243.md): `OI-17`, and the free letter turns out NOT TO BE SPENDABLE. `nextFreeLetter()` scans `a` to `x` and every `CI-06p` seed claims two past what it returns, so the one free letter is the falsifier's headroom rather than an allocation. Watched: claiming `x` reports `seed anchor exhausted`. The series is **CLOSED at `w`**, `x`, `y` and `z` are retired unused, no existing letter is renamed (145 of the 283 citing files are dated records [ADR-118](../decisions/ADR-118.md) rules may not be rewritten), and `CI-06/closed-letter-series` fails on a row claiming a twenty fourth. [ADR-131](../decisions/ADR-131.md), `status: proposed`. Not money | **DONE**, pull request open, not merged |
| **244**     | **`OI-09`: `CI-06n` accepts a link in prose where its own title says a ROW.** A gate whose title and behaviour disagree is one that will be cited for a property it does not have. `ADR-132` reserved CONDITIONALLY. Not money                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **LANDED 2026-08-27.** [ADR-132](../decisions/ADR-132.md), `status: proposed`, unsigned, six clauses. **THE TITLE WAS THE CORRECT HALF**, on [ADR-043](../decisions/ADR-043.md)'s own words: the ruling that asked for `CI-06n` says *"every entry file has a **row** in its registry README and every README **row** resolves to a file"*, in an entry whose other clause turns on *"a row is not a document"*. So the parser is narrowed rather than the title, and **a row is a line beginning with a pipe, outside a code fence**. **MEASURED IN BOTH FORMS BEFORE IT WAS RULED: 648 entry files over the five registries, 648 satisfied under the loose reading and 648 under the tightened one, so NO DOCUMENT GOES RED.** **Watched failing on the real 2026-08-16 defect reproduced**, which is [ADR-043](../decisions/ADR-043.md) indexed only by its README preamble sentence: PASS at `cce89de` and FAIL here on the identical tree, with a second seed for the fence direction and the unseeded tree green in both. **A NARROWING AND NOT A WEAKENING**: the gate reports more, never less, and nothing that failed before passes now. **THE IDENTIFIER IS DELIBERATELY NOT RENAMED**, because session 243 holds `OI-17` and the `CI-06<letter>` registry in this same file; the slug question is rowed for it. **ONE LANDMINE AND IT IS NAMED: the new direction has no [`falsify.mjs`](../../scripts/corpus/falsify.mjs) seed**, that file being outside this session's fence, so a revert would be silent until one lands. 32 of 32 gates, 10 of 10 invariants, `verify` exit 0, 150 files / 2,656 passed / 1 skipped, `falsify` clean at 80 scope and 10 loader cases. **NOT MERGED** |
| **245**     | **`OI-25`: the fixture backlog is DERIVED rather than SUBTRACTED, and [ADR-076](../decisions/ADR-076.md) did not close it.** [WAVE-04](../plans/WAVE-04-fixture-backlog-and-gate-inventory.md) section 6 is the origin. This matters more now than when it was raised, because session 237 is about to move a 227-row population and a backlog that cannot be derived cannot be checked afterwards. `ADR-133` reserved CONDITIONALLY. Not money                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **LANDED 2026-08-27.** [ADR-133](../decisions/ADR-133.md), `status: proposed`, UNSIGNED, four clauses. **The counts of [section 39](../testing/golden-scenarios/39-fixture-status-and-blockers.md) are generated spans** over a new [`fixture-backlog.mjs`](../../scripts/corpus/fixture-backlog.mjs) reading the rows below, so the backlog is READ and can no longer be TYPED, and `OI-25` is closed in the register and at its origin. **THE FIRST RUN OF THE GENERATOR REPAIRED A COUNT THAT HAD ALREADY DRIFTED BY FORTY**, in the document whose subject is derived counts, past 32 green gates. **The finding is bigger than the span**: `CI-06/derivable-counts` cannot see this number for three independent reasons and only the third is a hole, a SUMMARY table row excluded by a shape rule argued over a DATA row, measured with a probe rather than asserted. **Five perturbations, two of them negative controls that must REFUSE**, and both did. One landed harness seed broke and announced itself. `verify` exit 0 |
| **246**     | **`OI-15`: the `admin_actions` design record does not carry `SD-M6-11`'s columns.** `CI-06i` requires a `### <table>` design record for every `CREATE TABLE` in both directions, and a record that omits columns the schema carries is a record that reads as complete and is not. Documentation only, no migration. `ADR-134` reserved CONDITIONALLY. Not money                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | **LANDED.** The record is at post-migration truth, read from [`0017`](../../packages/db/migrations/0017_events_and_audit.sql) and [`0043`](../../packages/db/migrations/0043_admin_attributed_actions.sql) rather than from the record or from `schema.ts`. **[ADR-134](../decisions/ADR-134.md) proposed and UNSIGNED, because the conditional FIRED**: the record repair is transcription and rules nothing, and what needed a ruling is what a design record OWES its table plus why the check asserting it is not in `verify` today. **[`data-model-columns.mjs`](../../scripts/corpus/data-model-columns.mjs) is the reconciliation `CI-06i` never did**, and it found the drift is NOT confined to this record: `contact_channels.complained_at`, `plan_versions.fee_back_repeats` and `promotional_credit_grants.source_payout_request_id` are missing from their records too, **all three outside this fence**, so the check landed at full strength and **deliberately unregistered** rather than narrowed until green. **A struck-through row is a TOMBSTONE and not a claim**, which is what keeps `kyc_verifications`' `ADR-029` row from reading as the drift it is the opposite of. **Seven seeds watched failing.** **The schema was right in all four cases and no migration is owed.** 32 of 32, 10 of 10, `verify` exit 0, 150 files / 2,656 passed / 1 skipped. **NOT MONEY PATH, NOT MERGED.** See [session 246](2026-08-27-session-246.md) |
| **247**     | **`OI-18`: `ADR-067`'s eligibility question has NO SAFE DEFAULT**, and `OQ-F3-01` and `OQ-F3-02` are the two open questions under it. **A question with no safe default is one that cannot be closed by picking the cautious option**, which is why it has sat open since session 86 and why it needs a ruling rather than a transcription. `ADR-135` reserved REQUIRED. **MONEY PATH by subject**: eligibility gates a payout                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **LANDED 2026-08-27.** [ADR-135](../decisions/ADR-135.md), `status: proposed`, UNSIGNED, six clauses. **THE PREMISE WAS FALSE AND THAT IS THE FINDING**: `OQ-F3-01` and `OQ-F3-02` were ruled by [ADR-067](../decisions/ADR-067.md) sections 3 and 4 on 2026-08-20, `accepted`, founder approval GRANTED 2026-08-21, and only the registers had not moved. **The dispatch's named contradiction was closed on 2026-08-21 by [ADR-075](../decisions/ADR-075.md), and closing it created the one that is live**: `INV-M20-06` went to `identities.status = 'active'`, which shut the SECOND of the two exits `ADR-067` section 3 stands on, so for a **`closed`** identity the horn `ADR-067` declared false is true again and [`0038:106`](../../packages/db/migrations/0038_account_adjustments.sql)'s stated reason is no longer true of the door set. **Clause 2 posts the credit anyway and prices both directions**; clause 4 says the missing fact is what closure IS and refuses to invent it; clause 5 hands `INV-M20-06` against `INV-M20-09` to **236's `M20`**. **No migration number spent. `M20`, `0038` and `ADR-067` untouched.** One test, named loudly. Approval clause is a five-part predicate rather than a count and was RUN on the branch |
| **248**     | **THE SECOND FIXTURE TERM: `format-cannot-express`, 16 scenarios.** Distinct from session 237's `no-fixture-format` and deliberately a separate session: [ADR-072](../decisions/ADR-072.md)'s `W1`/`W2` tie-break separates them, and conflating the two is how [P4 section 5.3](../plans/P4-portal-and-site.md) found `GS-143` and `GS-144` mis-filed. `ADR-136` reserved CONDITIONALLY. Not money                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | **CLAIMED**, dispatched by this session                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| **249**     | **THE SMALL DISPOSITIONS: `outside-loader-boundary` (4), `open-question` (2) and `no-plan-record-value` (1), seven scenarios over three terms.** Each is small, none shares a term with 237 or 248, and the seven have never been examined together. **The `vendor-call` 14 are NOT in this session and are not dispatchable at all**: they wait on the Rithmic vendor call that holds [M02](../plans/M02-rithmic-bridge.md) at `review` under [ADR-005](../decisions/ADR-005.md). `ADR-137` reserved CONDITIONALLY. Not money                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | **LANDED 2026-08-27.** [ADR-137](../decisions/ADR-137.md), `status: proposed`, UNSIGNED, not money path. **The condition fired: both `open-question` members needed an ANSWER.** `OI-16` asks about a vendor feed, its own register says it does not hold `R-04`, and [ADR-072](../decisions/ADR-072.md)'s tie-break run in both directions puts `GS-004` and `GS-031` on `format-cannot-express`; [`cme-2026.json`](../../packages/rules-engine/fixtures/calendars/cme-2026.json)'s own `what_this_format_cannot_carry` field had named the two ids all along. **`OI-16` stays OPEN and holds nothing.** **ALL FOUR `outside-loader-boundary` CITATIONS HAD EXPIRED at [ADR-078](../decisions/ADR-078.md)**, and the term still stands on all four, re-cited to a different surviving reason each. **`GS-072` is in a third state**: its skip cleared itself, the assertion is LIVE and PASSES, and the row cannot move because `CI-06/fixture-inventory` reads `describe.skipIf` as disabled whatever its condition evaluates to. **Reported, not resolved; the gate is not relaxed.** `GS-079` stands, re-checked at source. 32 of 32 gates, 10 of 10 invariants, `verify` exit 0, 150 files / 2,656 passed. **NOT MERGED.** See [session 249](2026-08-27-session-249.md) **Session 248 landed mid-branch and the PREDICTED COLLISION FIRED**: resolved per row rather than keep-one-side, and every summary count RECOMPUTED from the 316 rows, so `format-cannot-express` reads **11**, which is neither side's number. |
| **250** | **THE PORTAL'S FIRST RENDERED DOCUMENT, AND THE SINGLE HIGHEST-VALUE ITEM ON THE BOARD.** `apps/portal` carries **18 view modules and 3,484 lines that render nothing**, because there is no `app/` directory: its own manifest says _"THERE IS STILL NO PAGE, NO LAYOUT, NO ROUTE AND NO `next.config`"_ and `next build` exits 1. [ADR-095](../decisions/ADR-095.md) admitted Next.js and left the first page to `P4-e` and `P4-h`; **both merged and neither wrote one.** This session writes `next.config`, `app/layout.tsx` and the first page, and lands the refusal ADR-095's own clause records as missing. **Every other portal session is blocked on this one.** `ADR-138` reserved REQUIRED. Not money | **CLAIMED**, dispatched by this session |
| **251** | **`GET /accounts`, `GET /accounts/:account`, `POST /accounts/:account`, `POST /accounts/:id/reset`.** Four of the **33 contract endpoints that do not exist**: `API_CONTRACT` names 53 and `apps/api` implements 20. **MONEY PATH**: an account carries a balance and a reset moves one. `ADR-139` reserved CONDITIONALLY | **WRITTEN.** Two of the four exist to be written: `POST /accounts/:account` is in NO section of `API_CONTRACT` and the reset is already served by `checkout.ts`. [ADR-139](../decisions/ADR-139.md) proposed |
| **252** | **`GET /payouts` and `POST /accounts/:id/payout`.** **MONEY PATH, and it meets the `INV-M20-06` contradiction head on**: after [ADR-062](../decisions/ADR-062.md) the payout gate reads `identities.status = 'active'` while `M20` blocks on `= 'restricted'`, so a `closed` identity cannot request a payout but can still spend its wallet. Session **247** is ruling on the eligibility half concurrently. `ADR-140` reserved REQUIRED | **LANDED 2026-08-27.** [ADR-140](../decisions/ADR-140.md), `status: proposed`, unsigned. **THE CONTRADICTION IN THIS ROW WAS ALREADY CLOSED WHEN IT WAS WRITTEN**: [ADR-075](../decisions/ADR-075.md) moved `INV-M20-06` to `= 'active'` on 2026-08-21 and [M20:62](../plans/M20-wallet.md) reads that today. What ADR-140 rules instead is the residue: `G-ELIGIBLE`'s identity-status term is a **named refusal before the account is read**, `identity_restricted` 422, never a gate result, because `ExternalGates` cannot evaluate it and `EligibilityResponse.gates` has no cell for it. `LT-01` posts through `packages/ledger`. Eight seeds watched failing. See [session 252](2026-08-27-session-252.md) |
| **253** | **`GET /plans`, `GET /plans/:plan`, `GET /purchases`.** The catalogue is registered `firm` and has no identity column, so a trader-facing read crosses the scope classes [ADR-106](../decisions/ADR-106.md) rules. `ADR-141` reserved CONDITIONALLY. Not money | **CLAIMED**, dispatched by this session |
| ~~**254**~~ | **The four `/affiliate/*` rows**: `POST /affiliate/links`, `POST /affiliate/creatives`, `GET /affiliate/stats`, `GET /affiliate/statements`. [ADR-113](../decisions/ADR-113.md) already wrote the creatives CONTRACT row and named the operator half OWED. `packages/affiliate` exists at 453 lines. `ADR-142` reserved CONDITIONALLY | **LANDED**, [session 254](2026-08-27-session-254.md). `ADR-142`'s conditional DID NOT FIRE and the number stays free |
| **255** | **The four `/internal/*` rows**: `GET /internal/health/deep`, `/internal/jobs`, `/internal/recon/status`, `POST /internal/batch/run`. **An internal surface any deployable can call is the shape [ADR-083](../decisions/ADR-083.md) section 3 refuses**, and `ADR-096`'s hole is that nothing enforces it. `ADR-143` reserved CONDITIONALLY | **DONE**, [2026-08-27 - Session 255](2026-08-27-session-255.md). **The conditional did NOT fire and `143` is released**: the refusal exists in three places, was measured on the running process on both surfaces, and was falsified by removal. `ADR-096`'s hole is the MANIFEST half, closed by `RI-08`; the SURFACE half was closed by `RI-09` and `compose` |
| **256** | **Seven `/admin/*` READS**: accounts, accounts/:account, identities/:identity, flags, liability, eligible-forecast, evidence/:account. **NO ROUTE IN THIS REPOSITORY HAS EVER BEEN WRITTEN AGAINST THE ADMIN ORIGIN.** It is a separate origin under [ADR-012](../decisions/ADR-012.md), roles closed at `owner` / `ops` / `readonly`. **`ADMIN_ORIGIN` IS A PLACEHOLDER AND THE REAL DOMAIN NEVER ENTERS THE REPOSITORY.** `ADR-144` reserved REQUIRED | **LANDED**, [ADR-144](../decisions/ADR-144.md) `proposed` and UNSIGNED. **The ruling ADDS NOTHING and that is the argument for it**: `OPERATOR_PREFIXES` already held `/admin` and `start.ts` is untouched. **404 on `public` and 401 on `operator` from the same file**, run. **The guard refuses in three directions**; `SystemReason` is NOT widened, the finding being that the accessor lacks a query SHAPE rather than an authority. **The domain sweep over the whole diff returns ZERO.** Three brief citations named as unresolving |
| **257** | **Four `/admin/*` WRITES**: accounts/:account, flags/:flag, plans/:plan, plans/versions/:version. **MONEY PATH: an operator changing a plan version moves money.** `admin_actions.reason` is `NOT NULL` under _"NO UNEXPLAINED ADMIN ACTION, EVER"_, so a write recording no reason is refused by the database rather than by the route. `ADR-145` reserved REQUIRED | **CLAIMED**, dispatched by this session |
| **258** | **`GET /economic-calendar` and `POST /webhooks/rise`, and `GET /public/methods/:statCode` found COMPLETE rather than partial.** **THE CONDITIONAL FIRED: [ADR-146](../decisions/ADR-146.md), `status: proposed`, unsigned, seven clauses.** The calendar read is the first endpoint whose response carries an `*_at` and a `*_day` on ONE object, and [API_CONTRACT](../architecture/API_CONTRACT.md) section 1's suffix rule had been carried entirely by whoever names a field. The two agree on most rows and disagree on evening ones, so a derived day reads as correct all afternoon and is wrong at 23:30. The suffix rule becomes a REFUSAL at the point of projection; the route reads no clock, because the staleness threshold's other side is a TRADING day; `stale: false` beside `covered_through_day: null` is refused (`DEP-M4-09`, the confident empty panel). **The calendar clause is asserted on `2026-03-03T23:30:00Z`**, UTC date `2026-03-03`, trading day `2026-03-04`, with the divergence asserted BEFORE the rendering. **`POST /webhooks/rise` forced a NEW PORT and it is said loudly**: `PspId` is CHECK-closed on `purchases.psp`, so `@merit/psp`'s verifier cannot serve the payout rail, and this is the THIRD implementation of one ordering rule; [ADR-114](../decisions/ADR-114.md) section 4's condition for lifting it has RECEDED. **The webhook clause is watched in both directions on one payload.** **Three dispatch citations corrected**, among them that there is no route registry to append to: the module list is the directory listing. 32 of 32, 10 of 10, `verify` exit 0. **NOT MONEY BY CONTENT, NOT MERGED** | **[WRITTEN](2026-08-27-session-258.md)** |
| **259** | **PORTAL PAGE: accounts and marks.** Renders `view/accounts.ts` and `view/marks.ts`, which exist and render nothing today. **Blocked on session 250's layout**; ships the segment honestly unwired if 250 has not landed. `ADR-147` reserved CONDITIONALLY. Not money | **CLAIMED**, dispatched by this session |
| ~~**260**~~ | ~~**PORTAL PAGE: payouts and eligibility.** `view/eligibility.ts` is the one screen where [ADR-067](../decisions/ADR-067.md)'s open question becomes visible to a trader, and sessions **247** and **252** are both live on it. **Blocked on 250.** `ADR-148` reserved CONDITIONALLY~~ | ~~LANDED 2026-08-27, [ADR-148](../decisions/ADR-148.md) PROPOSED, dispatched by this session~~ |
| **261** | **PORTAL PAGE: purchases and certificates.** `GS-302` is `restricted` and a certificate is a public artifact, so a rendered one is the first test of that classification. **Blocked on 250.** `ADR-149` reserved CONDITIONALLY | **LANDED 2026-08-27, UNWIRED.** `app/(purchases)/` serves `/purchases` and `/certificates`; 250 had not landed so `next build` still exits 1 for the missing layout, and the segment shipped honestly unwired. **`ADR-149` NOT TAKEN, and this row's own reason is why**: `GS-302` is `restricted` in **`identity_status`**, an identity's status and not a classification of data, and no data-classification vocabulary exists in this corpus. **What a public certificate may carry is settled by `INV-M11-01` and `AS-M4-03` rule 3**, so the condition is false. The control shipped is an allowlist at the render boundary that `certificate_id` does not cross, on `SD-M11-01`'s key-versus-`code` split. **The refusal path was publishing that id through an error message and the byte-level absence assertion caught it.** **`CI-07` REOPENS ON THIS COMMIT AND IS LEFT RED**: its artifact is a page file under `apps/*/src/app/` and this is the arrival, the stage cannot be written until 250 makes `next build` exit 0, and `ci.yml` is `P4-i`'s. **31 of 32 gates, 10 of 10 invariants.** See [session 261](2026-08-27-session-261.md) |
| **262** | **PORTAL PAGE: KYC status and disclosure.** **Merit never proxies a document** ([ADR-114](../decisions/ADR-114.md)) and a KYC screen is where a UI would most naturally try to. **Blocked on 250.** `ADR-150` reserved CONDITIONALLY | **LANDED 2026-08-27. NO ADR: the `150` condition did not fire and the number stays free.** `apps/portal/src/app/kyc/` renders `SC-M4-07` and `pnpm --filter @merit/portal build` exits **0** with `/kyc` in the route table as **dynamic**, which is the first time any `apps/portal` build has produced a route. **ADR-114 clause 6 is enforced in both of its places and the clause is asserted by trying**: a payload carrying a passport, a nested selfie URL, `INV-M19-09`'s provider reason code and a birth date takes the render path to a refusal, and the emitted bytes are searched for the VALUES with each needle proven findable first. **M04 section 7.9's string catalogue now exists and its lint runs**, which `view/kyc.ts` reported as owed and unbuildable. **Written with `createElement` in `.ts` rather than JSX**, measured: `.tsx` needs a `tsconfig` line six concurrent first-page slices share and none holds. **`CI-07` reopened on the artifact this page IS**, reported with a written-out patch rather than weakened; **session 250 then landed that patch and every other item this session reported as owed**, so the segment is now **WIRED to the real root layout** (the band, `<main>` and the `INV-M4-09` footer stop being this segment's, which would otherwise have printed the compliance disclosure TWICE) and **`pnpm run verify` exits 0**: 11 of 11 invariants, 33 of 33 gates, falsify clean, 2,772 passed / 6 skipped. **One repository-wide red remains and it is not this branch's**, confirmed red on `main` itself |
| **263** | **PORTAL PAGE: referrals.** The required disclosure binds to APPROVAL and is nullable at `pending`, so a screen rendering it as pinned is wrong in the compliance direction. **Blocked on 250.** `ADR-151` reserved CONDITIONALLY | **LANDED 2026-08-27.** `apps/portal/src/app/referrals/` is the portal's first App Router segment and `view/referrals.ts` renders. **The clause is asserted on the case it names**: a `pending` creative whose row carries no `disclosure_version_id` renders under "Disclosure required at approval", the absence stated BEFORE the text, the text occurring EXACTLY ONCE in the document and that occurrence inside the region, and `CreativeSubmissionView` carrying no field an attachment could be written into. **Watched failing on the seeded render.** **`next build` EXITS 0 and `/referrals` is a live route**, discharging [ADR-095](../decisions/ADR-095.md) section 6's dated premise that it exits 1 for want of an `app` directory; served under `next start` and watched REFUSING, because the data port refuses rather than seeding commission figures Merit never computed. **`CI-06/gate-inventory` went RED at 31 of 32 on CI-07's arrival and was left red rather than weakened; session 250 then wrote the stage and it is CLOSED**, `verify` exit 0 at 33 of 33 and 11 of 11 invariants on the merged head. **Four findings reported rather than ruled**, the first being that `tos_versions`' closed CHECK has no member for the NFA promotional disclosure while `view/disclosure.ts` brands the same obligation `content_documents`. **`ADR-151`'s conditional did NOT fire and `151` is unspent.** **`origin/main` moved to `3f32575` under this branch and the one conflict was inside the ENTRY span**, resolved by re-running `gates.mjs generate` rather than by hand; the merged head re-measures at 10 of 10 invariants, 151 files / 2,670 passed and the same 31 of 32. NOT MERGED, and it must not merge before session 250's layout |
| ~~**264**~~ | ~~**PORTAL PAGE: economic calendar, timeline, rules and as-of.** A rendered as-of stamp is a claim about when the firm last computed something. **Blocked on 250.** `ADR-152` reserved CONDITIONALLY~~ | **WRITTEN.** Struck, and its entry is below. All four rendered, none routed: a `page.tsx` reopens `CI-07` and the repair is outside the fence. `ADR-152`'s condition FIRED, on the as-of half |
| ~~**265**~~ | ~~**`P6` PLANNED AGAINST THE TREE. THE TRADEABLE PHASE.** [DELIVERY_PLAN](../DELIVERY_PLAN.md)'s `P6` row is [ADR-020](../decisions/ADR-020.md)'s tier 2: streaming ingest through the adapter, the live cache, WebSocket delivery, live portal surfaces, live Open Liability, degradation and labeling. Its named gates are **`GS-132` byte-identical with the cache POISONED** and **`GS-133` relabeling in the SAME RENDER as the fallback**. Plan against what the tree now serves (54 registered routes, 12 portal routes) rather than against the plans. `ADR-154` reserved CONDITIONALLY. Not money by file; the live tier is what a trader trades on~~ | **WRITTEN.** Struck, and its entry is below |
| ~~**266**~~ | ~~**`P7` PLANNED: RISK AND ABUSE.** Tier-1 detectors including `D-12` to `D-14`, the flags queue, **two-tier evidence packs**, CUSUM and circuit breakers, [ADR-022](../decisions/ADR-022.md)'s **v1 tier ONLY**, plus Metabase, Chatwoot and Loops wiring. Named gates: detector canaries found on every run, **evidence-pack redaction BY AUDIENCE green**, and the breaker reporting `insufficient_data` rather than firing on a small sample. `ADR-155` reserved CONDITIONALLY. Not money by file~~ | **WRITTEN.** Struck, and its entry is below. **`ADR-155`'s CONDITION FIRED AND NOT ON THE HALF THIS ROW NAMED**: all four evidence-pack audiences already have a redaction profile ([M06:300](../plans/M06-admin-ops-console.md), the batch 1 gate closure, [EC-071](../edge-cases/EC-071.md)) and three of them are one profile, so `GS-112` is writable as stated. What fired is `ADR-022`'s v1 tier, which says a hard link **auto-enforces** against [STATE_MACHINES:301](../architecture/STATE_MACHINES.md)'s *"no automatic transition into `enforced`"*. **Thirteen slices, three of them buildable today.** `P7` has no head of its own: the read shape is [`P5-a`](../plans/P5-payouts-and-wallet.md)'s file and a detector is a join and an aggregate. *"ADR-145, four writes"* is **seven** |
| **267** | **`OI-28` AND `OI-29`, the last two dispatchable open items.** `OI-28`: `simulation_runs.calibration_observed_at` names no unit token [ADR-042](../decisions/ADR-042.md) declares. `OI-29`: `plan_versions_publish_decision_recorded` **makes the link exist and nothing makes it SOUND**, and its digest half belongs to [ADR-081](../decisions/ADR-081.md). **`OI-16` is NOT in this session and is not dispatchable**: it is *"BLOCKED ON A RETRIEVAL... the founder or the vendor call"* with no engineering content. `ADR-156` reserved REQUIRED | **CLAIMED**, dispatched by this session |
| ~~**268**~~ | ~~**`P5-a`: THE ACCESSOR, AND IT IS THE KEYSTONE OF TWO PHASES.** [P5](../plans/P5-payouts-and-wallet.md) wave 1 lane A starts here and **[P7](../plans/P7-risk-and-abuse.md)'s ENTIRE WAVE 2 waits on it**: P7 section 10 item 1 says *"if `P5-a` lands without an aggregate, every wave-2 slice is blocked and the reach-around is one line"*, and that the argument belongs in this slice's entry. **MONEY PATH**, `E2`. `ADR-157` reserved REQUIRED~~ | **WRITTEN.** Struck, and its entry is below. [ADR-157](../decisions/ADR-157.md) proposed, approval line UNSIGNED, `E2` owed. **THE WIDENING LANDED AND THE AGGREGATE DID NOT**: a closed three-shape term vocabulary on the READ path and a ROW lock, and [P7](../plans/P7-risk-and-abuse.md) section 10 item 1's request REFUSED ON EVIDENCE, because P7's own section 3.1 names a JOIN as the blocker and no scalar aggregate serves one of the four detectors it lists. `SqlExecutorReason` unmoved and now watched. **No migration number** |
| ~~**269**~~ | ~~**`P5-c`: the contract half of P5 wave 1, lane B.** Concurrent with `P5-d` and with lane A. `ADR-158` reserved CONDITIONALLY~~ | **IN FLIGHT**, [session 269](2026-08-27-session-269.md) |
| ~~**270**~~ | ~~**`P5-d`: the catalogue half of P5 wave 1, lane B.** Concurrent with `P5-c` and with lane A. `ADR-159` reserved CONDITIONALLY~~ | **WRITTEN.** Struck, and its entry is below. **`ADR-159`'s CONDITION AS RESERVED CANNOT FIRE, BECAUSE THE ROW AND THE ENTRY IT CITES NAME TWO DIFFERENT CATALOGUES**: [ADR-141](../decisions/ADR-141.md) rules a trader-scoped read of the PLAN catalogue in [`catalog.ts`](../../apps/api/src/routes/catalog.ts) and `P5-d`'s catalogue is the EVENT catalogue, whose fence holds no code, no route and no read. **The number is spent on the half [P5 section 8](../plans/P5-payouts-and-wallet.md) named**, which marks the ADR REQUIRED where the allocation row marked it CONDITIONAL. **RULED: a name becomes a catalogue ROW only where every field is a column a migration declares or a field of a mirror a plan names**, which is the question [EVENTS](../architecture/EVENTS.md) section 6 deferred in writing. Three of the six lifecycle names had a name and no shape anywhere in the corpus; **`wallet.credited`'s payload as [M05](../plans/M05-payout-system.md) wrote it cannot be produced for two of the three storable credits**. Five payloads amended at the fold, every one named in the entry's own table. **`OQ-M5-07` answered at twelve wall-clock hours.** Findings reported and not repaired, among them the two `cancelled` edges that leave universal rule 1 unmet, **that nothing checks an event name in a drawing against the registry**, and that [P5 section 9](../plans/P5-payouts-and-wallet.md) rows [EVENTS](../architecture/EVENTS.md) for one slice while [P6](../plans/P6-live-tier.md) gives the same file to session 272. **No surface registers 54 routes**: 36 public, 19 operator, 54 the union |
| ~~**271**~~ | ~~**`P6-a`: the adapter learns `streamLive`, and the seam moves from the ARTIFACT to the TYPE.** [P6](../plans/P6-live-tier.md)'s dispatch order marks it *"ADR-154, landed. NOTHING ELSE"* -- the only wave-1 slice with no dependency at all. `ADR-160` reserved CONDITIONALLY. Not money~~ | **LANDED 2026-08-27.** `streamLive` is on [`PlatformAdapter`](../../packages/rithmic/src/index.ts) carrying [M02](../plans/M02-rithmic-bridge.md) section 3.5's own signature, `simulatorLiveFeed` implements it and nothing else, and the tier-2 conformance suite runs over each implementation rather than asserting anything of the interface. **`ADR-160` NOT TAKEN: the condition did not fire and the number returns to the pool.** **The defect was in the START and not in the seam** -- a microtask pump delivers the first tick before the consumer holds the `Subscription`, so `close()` from inside the handler is a dead-zone reference; delivery is one turn of the event loop for that reason. **A FOURTH enumeration site exists and clause 5 names three**, [`packages/rithmic/README.md`](../../packages/rithmic/README.md), outside the fence and reported with its patch. 33 of 33 gates, 11 of 11 invariants, `verify` exit 0. **`P6-b` is SERIAL behind this on `OVERVIEW`.** NOT MERGED |
| ~~**272**~~ | ~~**`P6-d`: the contract and the catalogue for the live tier.** Wave 1, concurrent with `P6-a`. **`P6-b` is SERIAL behind `P6-a` on `OVERVIEW` and is NOT dispatched here**; `P6-c` is blocked on [P6](../plans/P6-live-tier.md) section 10 item 1, which is the founder's. `ADR-161` reserved REQUIRED, in the plan's own words~~ | **WRITTEN.** [ADR-161](../decisions/ADR-161.md) `status: proposed`, UNSIGNED. `API_CONTRACT` sections 6.1 and 8 and `EVENTS` section 5.4, all three as documents; no code, no migration, no fence widened. See [session 272](2026-08-27-session-272.md) |
| ~~**273**~~ | ~~**THE PORTAL'S HTTP CLIENT, AND THE FIRST `fetch` IN `apps/portal`.** [P6 section 6](../plans/P6-live-tier.md) rows it as *"the portal's API client for HTTP"*, owned by [P4](../plans/P4-portal-and-site.md)'s remaining work rather than by any `P6` slice, and **`P6-h` depends on it** for the base URL and the session policy it must not invent a second time. `ADR-162` reserved REQUIRED~~ | **WRITTEN.** [ADR-162](../decisions/ADR-162.md) `status: proposed`, UNSIGNED. The client is [`apps/portal/src/http/client.ts`](../../apps/portal/src/http/client.ts), `surface.test.ts` is narrowed PER NEEDLE rather than deleted, and `app/payouts/` is the one segment wired. See [session 273](2026-08-27-session-273.md) |
| ~~**274**~~ | ~~**`P5-b`: the three P5 tables nothing has registered.** `wallet_spend_limits`, `events` and `payment_disputes`, all three landed DDL, into [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts) with a scope rule each. Wave 1 lane A, behind `P5-a`. **NO ADR AND NO MIGRATION NUMBER ALLOCATED, and the session may take neither.** MONEY~~ | **WRITTEN.** Struck, and its entry is below. **THE COUNT IS ONE OF THREE AND THE ROW ABOVE IS WHY IT READS AS THREE**: `wallet_spend_limits` was already registered by [session 202](2026-08-25-session-202.md) and [ADR-092](../decisions/ADR-092.md) section 2 clause 1 forbids re-arguing it; `payment_disputes` is registered here, `derived` through `purchases`; **`events` IS REFUSED** for the reason already standing in `scope.ts` since [session 195](2026-08-25-session-195.md), one identity column beside one ACCOUNT column with neither required and no CHECK tying them, **which needs a sixth scope class and therefore an ADR this session was forbidden to take.** No number was taken |
| ~~**278**~~ | ~~**`P6-c`: the live cache, its medium, its role and the grant that makes `INV-M2-14` structural.** Wave 1, concurrent with `P6-a`, `P6-b` and `P6-d`. **[P6 section 10 item 1](../plans/P6-live-tier.md) held this slice for a founder answer and [ALLOCATION](../decisions/ALLOCATION.md)'s `ADR-164` row DELEGATES it to the session**, which must argue one of three readings rather than assume one. `ADR-164` reserved REQUIRED; migration `0050` reserved and returned to the pool if the winning reading needs no table. **MONEY PATH, `E2` READ OWED**~~ | **WRITTEN.** [ADR-164](../decisions/ADR-164.md) `status: proposed`, UNSIGNED. **Reading 1 ARGUED AND TAKEN**: `live_account_state` plus a fifth role `merit_live` in [`0050`](../../packages/db/migrations/0050_live_cache_and_role.sql), with `merit_app` revoked from the cache entirely. See [session 278](2026-08-27-session-278.md) |

**A row moves out of this table by being written, not by being edited.** When the session
lands, its entry joins the list below and its row here is **struck THROUGH, never deleted**.
**A STRUCK ROW AND ITS LIVE TWIN ARE ONE KEY TO `CI-06u`, AND THAT WAS CHECKED RATHER
THAN ASSUMED.** A keep-both merge of this table produces `~~**123**~~` beside `**123**`,
which happened on `123`, `124` and `125` in the 2026-08-21 merge round. **The obvious
worry is that the gate reads first cells literally and would see two different keys.**
It does not: seeding the live twin makes `CI-06u` fail, naming the key as `123` with the
`~~` and the `**` stripped. So the strikethrough rule costs this table nothing, and the
duplicate is caught whether or not a merge resolves it by reading first.

A deleted row is indistinguishable from a number nobody ever claimed, which is the state
this table exists to end; [ADR-065](../decisions/ADR-065.md) T3 amends a landed reservation
in place for the same reason. **Session 119 deleted `114` and `115` and session 122 struck
its own row through**, on one sentence that did not say which, so the sentence now does and
the two deleted rows are restored above.

## Entries

<!--gen:session_entries-->
| | |
|---|---|
| [2026-08-13 - Session 1](2026-08-13-session-01.md) | skeleton (section 0.5) |
| [2026-08-13 - Session 2](2026-08-13-session-02.md) | Wave 1 research (all 7 docs) |
| [2026-08-13 - Session 3](2026-08-13-session-03.md) | Wave 1 gate closure + Wave 2 architecture (all 8 docs) |
| [2026-08-13 - Session 4](2026-08-13-session-04.md) | Wave 2 gate closure + Wave 3 M01 (rules engine plan) |
| [2026-08-13 - Session 5](2026-08-13-session-05.md) | M1 gate closure + Wave 3 batch 1 (M02 through M08) |
| [2026-08-14 - Session 6](2026-08-14-session-06.md) | Wave 1 amendment, Axcera brochure (primary source) |
| [2026-08-14 - Session 7](2026-08-14-session-07.md) | repo workflow to a single trunk, then the Wave 3 batch 1 gate |
| [2026-08-14 - Session 8](2026-08-14-session-08.md) | founder rulings on the four gate findings |
| [2026-08-14 - Session 9](2026-08-14-session-09.md) | Wave 3 batch 2 (M09 through M20). Wave 3 complete |
| [2026-08-14 - Session 10](2026-08-14-session-10.md) | consolidated founder addendum, batch 2 gate closure, calibration source |
| [2026-08-14 - Session 11](2026-08-14-session-11.md) | ADR-024, ladder and live invitation separated |
| [2026-08-14 - Session 12](2026-08-14-session-12.md) | pre-wave folds plus Wave 4 (testing, ops, design, legal) |
| [2026-08-14 - Session 13](2026-08-14-session-13.md) | FREEZE gate closed, corpus FROZEN |
| [2026-08-14 - Session 14](2026-08-14-session-14.md) | the schema-delta reconciliation, migrations 0002 to 0026 |
| [2026-08-14 - Session 15](2026-08-14-session-15.md) | two rulings on the transparency surface (ADR-031, ADR-032) |
| [2026-08-14 - Session 16](2026-08-14-session-16.md) | the builder/reviewer loop, formalized as a citation check |
| [2026-08-14 - Session 17](2026-08-14-session-17.md) | the ADR collision resolved, and two gates that make it structural |
| [2026-08-15 - Session 18](2026-08-15-session-18.md) | DATA_MODEL brought to post-migration truth, and CI-06i |
| [2026-08-15 - Session 19](2026-08-15-session-19.md) | PR #7 and PR #8 reconciled into one branch, ADR-035 accepted, P1 measured |
| [2026-08-15 - Session 20](2026-08-15-session-20.md) | OQ-P1-04 ruled, the P1 scaffold plan approved |
| [2026-08-15 - Session 21](2026-08-15-session-21.md) | S-A, ADR-036 and the migration allocation table |
| [2026-08-15 - Session 22](2026-08-15-session-22.md) | S-B, the monorepo scaffold |
| [2026-08-15 - Session 23](2026-08-15-session-23.md) | S-C, CI-01, CI-02 and CI-05 with VG-4 and VG-12 |
| [2026-08-15 - Session 24](2026-08-15-session-24.md) | S-D, the golden fixture loader and CI-03 |
| [2026-08-15 - Session 25](2026-08-15-session-25.md) | the S-D review rulings folded |
| [2026-08-15 - Session 26](2026-08-15-session-26.md) | FOLD-01 planned, the passwordless-auth and phone-as-identity ruling |
| [2026-08-15 - Session 27](2026-08-15-session-27.md) | FOLD-02 planned, the payout enforcement window and identity-level suspension |
| [2026-08-15 - Session 28](2026-08-15-session-28.md) | S-E planned, TradingCalendar as data and the wall-clock unit boundary |
| [2026-08-15 - Session 29](2026-08-15-session-29.md) | the allocation registries made honest, and the four fold ADRs |
| [2026-08-15 - Session 30](2026-08-15-session-30.md) | DECISIONS.md becomes a directory (ADR-043, stage 1 of 5) |
| [2026-08-16 - Session 31](2026-08-16-session-31.md) | FOLD-01 session 3, migration `0029` and the phone-identity schema |
| [2026-08-16 - Session 31](2026-08-16-session-31.md) | FOLD-02 session 3, migrations `0030` and `0031` |
| [2026-08-16 - Session 31](2026-08-16-session-31.md) | `0032` carries ADR-042's F-1 to F-4 (S-E session 2, money path) |
| [2026-08-16 - Session 31](2026-08-16-session-31.md) | `OI-07` and `OI-08` close: the phone probe committed, NO-FLOATS made whole-schema |
| [2026-08-16 - Session 32](2026-08-16-session-32.md) | FOLD-01 session 4, the invariants and the adversarial scenarios |
| [2026-08-16 - Session 32](2026-08-16-session-32.md) | S-E session 3, the calendar source shape and the session generator |
| [2026-08-16 - Session 32](2026-08-16-session-32.md) | The AI and LLM policy (ADR-044), written by the review desk |
| [2026-08-16 - Session 33](2026-08-16-session-33.md) | `0033` closes the calendar prior-image gap, and DELTA_MANIFEST gets the fourth allocation table |
| [2026-08-16 - Session 33](2026-08-16-session-33.md) | FOLD-01 session 5, the portal, the vendors and the legal surface |
| [2026-08-16 - Session 33](2026-08-16-session-33.md) | FOLD-02 session 4, the machines and the invariants (money path) |
| [2026-08-16 - Session 33](2026-08-16-session-33.md) | FOLD-02 session 4, the machines and the invariants |
| [2026-08-16 - Session 34](2026-08-16-session-34.md) | FOLD-02 session 8, DELIVERY_PLAN and M15's scope move |
| [2026-08-16 - Session 36](2026-08-16-session-36.md) | FOLD-02 session 5, the surfaces (M02, M06, M03, M08) |
| [2026-08-16 - Session 37](2026-08-16-session-37.md) | FOLD-01 session 6, the registries and `CI-06k` |
| [2026-08-16 - Session 38](2026-08-16-session-38.md) | FOLD-02 sessions 6 and 7, the registries and `CI-06l` |
| [2026-08-16 - Session 39](2026-08-16-session-39.md) | S-E session 5: `CI-06m`, the import ban, the SQL shape check |
| [2026-08-16 - Session 40](2026-08-16-session-40.md) | the fast-check plan generator, CV-01 to CV-19 made executable |
| [2026-08-16 - Session 40](2026-08-16-session-40.md) | the three P2 ADRs, and the ownership partition made checkable |
| [2026-08-16 - Session 40](2026-08-16-session-40.md) | `OQ-M10-06`: the reversible address, and the notice that became evidence |
| [2026-08-16 - Session 41](2026-08-16-session-41.md) | `0034`'s plaintext floor, and a probe that was wired and not pinned |
| [2026-08-16 - Session 42](2026-08-16-session-42.md) | `OQ-P2-02`: the calendar watermark on `rule_states`, and `0034` claimed twice |
| [2026-08-16 - Session 42](2026-08-16-session-42.md) | the arbitrary day-sequence generator, and two arithmetics for one row |
| [2026-08-16 - Session 43](2026-08-16-session-43.md) | EC-157's Repair A (`0036`), and the duplicate `ADR-046` heading no gate could see |
| [2026-08-16 - Session 44](2026-08-16-session-44.md) | `advanceDay`: DO-1 to DO-7, sixteen of fifty rules, and the thirty-four that say so |
| [2026-08-16 - Session 45](2026-08-16-session-45.md) | golden fixtures batch 1: three to eighteen of 284, and two disagreements inside M01 |
| [2026-08-16 - Session 45](2026-08-16-session-45.md) | DO-8, the eval progression: twenty-two of fifty, and the floor defect group E found |
| [2026-08-16 - Session 45](2026-08-16-session-45.md) | the synthetic Rithmic simulator, file mode, and the vendor-call diff made executable |
| [2026-08-16 - Session 46](2026-08-16-session-46.md) | the simulator's streaming mode, ADR-020's tier 2 |
| [2026-08-16 - Session 47](2026-08-16-session-47.md) | `INV-06`'s scope tested and refuted, and two findings whose premises did not survive |
| [2026-08-16 - Session 47](2026-08-16-session-47.md) | golden fixtures batch 3: twenty-one to twenty-five of 284, and the format's ceiling |
| [2026-08-16 - Session 47](2026-08-16-session-47.md) | rules engine groups F, G and H: 41 of M01's 50 rules |
| [2026-08-17 - Session 48](2026-08-17-session-48.md) | `INV-06` ruled (ADR-050), and `RE-P-01` pins the `R-31` exception |
| [2026-08-17 - Session 48](2026-08-17-session-48.md) | golden fixtures batch 4: twenty-five to twenty-nine of 284, and the ruling that unblocked them |
| [2026-08-17 - Session 48](2026-08-17-session-48.md) | the last nine rules: 50 of 50 titled, 44 of 50 declared, and three that were never blocked |
| [2026-08-17 - Session 49](2026-08-17-session-49.md) | golden fixtures batch 5: thirty of 284, and four held-back reasons checked against their sources |
| [2026-08-17 - Session 49](2026-08-17-session-49.md) | the nightly batch and `state_hash`, with the replay comparison deliberately not wired |
| [2026-08-17 - Session 49](2026-08-17-session-49.md) | the local demo: the simulator through the engine, and four things the first watched run surfaced |
| [2026-08-17 - Session 49](2026-08-17-session-49.md) | `P2-1`: the config contract, CV-01 to CV-19, and R-17 declared (45 of 50) |
| [2026-08-17 - Session 50](2026-08-17-session-50.md) | the State column deleted on ADR-034's remedy, and `CI-06p` gives the letter registry its gate |
| [2026-08-17 - Session 50](2026-08-17-session-50.md) | the fixture wiring: `L-13`, the `bigint` comparison, and the premise ADR-048 rests on |
| [2026-08-17 - Session 50](2026-08-17-session-50.md) | a disabled consistency gate reports `skipped`, and the `EngineEvent` union |
| [2026-08-17 - Session 50](2026-08-17-session-50.md) | `P2-7`: the three merge blockers that were never written, and PT-06's three halves |
| [2026-08-17 - Session 51](2026-08-17-session-51.md) | golden fixtures batch 6: none written, and the polarity question answered by measurement |
| [2026-08-17 - Session 51](2026-08-17-session-51.md) | CI-03 folds `advanceDay`: 24 of 30 fixtures pass, and the six that do not have three causes |
| [2026-08-17 - Session 51](2026-08-17-session-51.md) | `R-32` computed on ADR-051, and the fencepost pinned by the boundary pair |
| [2026-08-17 - Session 52](2026-08-17-session-52.md) | the short calendar repaired, and the repair was two halves rather than one |
| [2026-08-17 - Session 52](2026-08-17-session-52.md) | The GS-024 floor divergence, ruled against the engine on a derivation nobody had checked |
| [2026-08-17 - Session 53](2026-08-17-session-53.md) | `CI-06q`, cited authority exists, and the limitation that it would not have caught its own cause |
| [2026-08-17 - Session 54](2026-08-17-session-54.md) | `GS-064`'s `withdrawable_cents`, ruled on the ordering law rather than on the arithmetic |
| [2026-08-17 - Session 54](2026-08-17-session-54.md) | `ADR-055`, the open end of a trade date's session, and a finding narrower than it was recorded |
| [2026-08-17 - Session 55](2026-08-17-session-55.md) | ADR-052 applied: the locked floor becomes an assignment |
| [2026-08-17 - Session 55](2026-08-17-session-55.md) | golden fixtures batch 7: the forty-three re-derived, and three unblocked by one field |
| [2026-08-18 - Session 56](2026-08-18-session-56.md) | `ADR-054` implemented: the `DO-5` call site removed, and `RE-U-025` asserts the carried-field set |
| [2026-08-18 - Session 56](2026-08-18-session-56.md) | `INV-07` and the funded reset: ADR-056 proposed |
| [2026-08-18 - Session 56](2026-08-18-session-56.md) | `GS-081` written and `GS-083` routed out: `INV-21`'s missing fixtures, and a counterexample filed under the wrong half |
| [2026-08-18 - Session 56](2026-08-18-session-56.md) | `absorbs_into`, the shape ADR-055 shipped without its validator |
| [2026-08-18 - Session 57](2026-08-18-session-57.md) | ADR-056 executed: the carried-lock counterfactual folded forward |
| [2026-08-18 - Session 57](2026-08-18-session-57.md) | `GS-083` written in the `RE-C-nn` suite: publishing fails and `CV-17` is the code named |
| [2026-08-18 - Session 57](2026-08-18-session-57.md) | ADR-057: one refuted sentence in four documents, and the ninth breach-row field |
| [2026-08-18 - Session 58](2026-08-18-session-58.md) | the CME source publishes forward-only, and ADR-042's coverage reason is falsified |
| [2026-08-18 - Session 58](2026-08-18-session-58.md) | the fixture calendar gains the four transcribed holiday weeks: a holiday, an early close and an absorbed session |
| [2026-08-18 - Session 59](2026-08-18-session-59.md) | ADR-056 rewritten: the refuted mechanism deleted, the surviving argument promoted, three narrowings folded in |
| [2026-08-18 - Session 59](2026-08-18-session-59.md) | GS-003 and GS-032 written: the two group A rows the transcribed half day unblocked, and the four rules it did not |
| [2026-08-18 - Session 59](2026-08-18-session-59.md) | the six unsigned ADRs audited against the tree, and three of them were signed at the M1 gate |
| [2026-08-18 - Session 60](2026-08-18-session-60.md) | the wave plan, and the numbers reserved before the sessions that spend them |
| [2026-08-18 - Session 61](2026-08-18-session-61.md) | `CI-06r`, and the half of its own brief the tree refuted |
| [2026-08-18 - Session 62](2026-08-18-session-62.md) | `PT-04` and `PT-07` asserted, and each found a ruling by failing on it |
| [2026-08-18 - Session 63](2026-08-18-session-63.md) | PT-02 and PT-08 asserted, and PT-02's row names one exception where the engine has two |
| [2026-08-18 - Session 64](2026-08-18-session-64.md) | `PT-05` asserted, and a property that could not see its own mutant |
| [2026-08-18 - Session 65](2026-08-18-session-65.md) | the replay self-audit's comparison is wired: hash first, field diff on mismatch, and an empty scope refuses |
| [2026-08-18 - Session 66](2026-08-18-session-66.md) | golden fixtures batch 10: `L-11` lifted, the settlement block, GS-067 and GS-068, and six rejections with a reason each |
| [2026-08-18 - Session 67](2026-08-18-session-67.md) | `CI-06o`, the money-path model ban: ADR-044's prohibition 1 stops being prose, and both assertions watched failing |
| [2026-08-18 - Session 68](2026-08-18-session-68.md) | CI-06s: every probe is run and pinned, and OI-07's fourth occurrence repaired |
| [2026-08-18 - Session 69](2026-08-18-session-69.md) | `CI-06t`, and the four live sites the tree scan found before anything was seeded |
| [2026-08-18 - Session 71](2026-08-18-session-71.md) | `compare.ts` diffs `engine_gates`, and the handoff's count was wrong by one |
| [2026-08-18 - Session 72](2026-08-18-session-72.md) | ADR-059 proposed: the three group A input questions, two already answered in test code and one with no source anywhere |
| [2026-08-18 - Session 73](2026-08-18-session-73.md) | `ADR-060` proposed: what `engine_eligible` contains, and the missing enumeration that was the real blocker |
| [2026-08-19 - Session 74](2026-08-19-session-74.md) | the four adapter copies collapsed to one module, and the proof that it changed nothing |
| [2026-08-19 - Session 75](2026-08-19-session-75.md) | ADR-059 signed as a split: the layer column on M01's fifty rules, the halt open question named, and the seed comment |
| [2026-08-19 - Session 75](2026-08-19-session-75.md) | `CI-06u`: no markdown table in `docs/` has two rows with the same first-cell key, and the 105 the survey found |
| [2026-08-19 - Session 75](2026-08-19-session-75.md) | ADR-060 folded, INV-15's closed six-member enumeration, and RE-P-15 written |
| [2026-08-20 - Session 76](2026-08-20-session-76.md) | WAVE-03 planned: the 106 duplicate registry keys sequenced into nine sessions, five concurrent, with every number reserved before any session runs. Five claims in the record refuted against the tree, three findings no gate can see, and the letter registry's wall |
| [2026-08-20 - Session 77](2026-08-20-session-77.md) | ADR-061 proposed: when a duplicated table key is a repair that lands by commit and when it is an amendment that needs a ruling |
| [2026-08-20 - Session 78](2026-08-20-session-78.md) | All ten of STATE_MACHINES section 10's duplicated guards read: four are contradictions ruled in ADR-062, six are agreements merged under ADR-061, and CI06U_REGISTER loses the file. The payout gate reads `identities.status = 'active'`, and OQ-062-01 opens on the wallet door M20 still leaves open to a closed identity |
| [2026-08-20 - Session 79](2026-08-20-session-79.md) | The `INV-M5` collision, ruled as a merge and a renumber |
| [2026-08-20 - Session 80](2026-08-20-session-80.md) | M12's S-14/S-15 pairs renumbered and INDEX's M03/M04/M05 rows deduped, each chosen per row; ADR-061 would have escalated three of the five keys and a renumber has nothing for its agreement test to answer |
| [2026-08-20 - Session 81](2026-08-20-session-81.md) | STATE's P1-item table repaired: nine duplicated keys, the span-carrying row kept in each, and OI-10's "Deduplicated 2026-08-16" was false |
| [2026-08-20 - Session 82](2026-08-20-session-82.md) | `ADR-065`: one key one row, the blank-line parser trap, the `u` collision ruled, and the letter registry is out of alphabet |
| [2026-08-20 - Session 83](2026-08-20-session-83.md) | CI-06v: no orphan table fragment, and the gate inventory was its own first finding |
| [2026-08-20 - Session 84](2026-08-20-session-84.md) | `CI-06w`: the allocation registries read as multisets, `OI-11` closed, and a count that was doing two jobs |
| [2026-08-20 - Session 85](2026-08-20-session-85.md) | ADR-064: a session number is an allocation and not an identifier, the index merged to one table of 121 rows over 83 keys, and one entry recovered that every gate was green over |
| [2026-08-20 - Session 86](2026-08-20-session-86.md) | FOLD-03 planned: the vendor-parity gap-fill, with ADR-066 and ADR-067 reserved, migrations 0038 to 0041 claimed and GS-285 to GS-299 registered. Four of the six referred items were mis-scoped against the tree, and the economic calendar turned out to be an unsatisfied commitment rather than new scope |
| [2026-08-20 - Session 87](2026-08-20-session-87.md) | ADR-066: the vendor-parity gap-fill, five surfaces admitted and the calendar found to be an outstanding commitment |
| [2026-08-20 - Session 88](2026-08-20-session-88.md) | the admin parity audit: 34 trader-side mutating actions enumerated at file:line, 2 with an owner-role admin equivalent, 18 gaps and 2 narrow-exception candidates. The admin surface has no endpoint that performs a trader action on a trader's behalf, contract signature turned out to be a field rather than a route, and ten rows needed a class the fold did not have |
| [2026-08-20 - Session 89](2026-08-20-session-89.md) | FOLD-03 F1: the Tier-1 economic calendar as Merit-owned data (`0039`), closing `DEP-M7-06`, with M04's panel, `FM-M7-08`'s staleness alarm and GS-285 to GS-287. The migrations test turned out to assert on-disk contiguity that ADR-036 rules out, so the ruled workflow could not go green |
| [2026-08-20 - Session 90](2026-08-20-session-90.md) | FOLD-03 F2: scheduled digest delivery (`0040`, `SD-M6-07`), four named digests rather than the report builder ADR-066 refused, and the alarm reading the delivery record rather than the job's own report. Two of the twenty-four executed assertions were refused by a constraint other than the one they were aimed at, and the test's own no-secrets tripwire fired on a comment saying 'Never a credential' |
| [2026-08-20 - Session 91](2026-08-20-session-91.md) | FOLD-03 F3: the six standing duplicate-signal views in M06 section 7.10, sorted by aggregate open liability because sorting by signal count teaches an operator to chase coffee shops, with one sentence in M07 and no migration. The sixth view turned out not to be a signal at all, and it has to read both payout legs because ADR-028 split them |
| [2026-08-20 - Session 92](2026-08-20-session-92.md) | FOLD-03 F4: the `bounced` and `spam_complaint` outcomes specified, `0041` spent on neither thing it was reserved for, and two migration-numbering controls found to disagree |
| [2026-08-20 - Session 93](2026-08-20-session-93.md) | the vendor-parity assessment: 13 structural advantages each cited to where it is specified, the two exclusions written down as exclusions, and six MUST components against six SHOULD. FOLD-03's "four of six are SHOULD" sentence is not carried, because ADR-066 had already retired it, and the calendar the check found missing was on our own Wave 1 MUST list the whole time |
| [2026-08-20 - Session 94](2026-08-20-session-94.md) | `ADR-067` and `0038`: an adjustment posts to the wallet and never to withdrawable, a debit may only reverse a credit the table itself posted, and a `restricted` identity is permitted rather than refused because `INV-M20-06` already blocks extraction. The eligibility trade FOLD-03 handed the session was a false pair, because the corpus has two exits and one of its horns describes a door that does not exist. Verified against a real PostgreSQL 16: 40 migrations, 18 refusals and 3 commits watched, and one of `ADJ-C1`'s six branches turned out to be unreachable |
| [2026-08-20 - Session 95](2026-08-20-session-95.md) | FOLD-04 `I2`: `ADR-068` and `0042`, impersonation as a distinct session type that cannot elevate. The finding made the ruling smaller: `C-27` already refuses three of the seven blocked routes, so four refusals are new and three are inherited. `IMPERSONATION-C1` is enforced in both directions, and a single-sided guard was watched passing an inventory of refusals with the ordering hole open |
| [2026-08-20 - Session 95](2026-08-20-session-95.md) | the T3 debt was a disabled gate, not untidy prose |
| [2026-08-20 - Session 95](2026-08-20-session-95.md) | fourteen signatures, and one of the nine declines was a live hole rather than a missing signature |
| [2026-08-20 - Session 96](2026-08-20-session-96.md) | FOLD-04 `I3`: the impersonation touchpoints. The banner's audience is the operator and `IMPERSONATION-C1` is what makes non-disclosure structural rather than a portal rule. Both allocations the brief asked for were checked and answered by the check: the `M6-N-nn` count is **zero**, because the claimed block already covers all seven routes, and `OQ-M20-06` had been spent since the entry that declined to take it, so the `closed`-identity door is `OQ-M20-07`. The `D5` matrix is not in SECURITY and its factor vocabulary cannot express an impersonation refusal at all |
| [2026-08-20 - Session 97](2026-08-20-session-97.md) | `ADR-069`, the admin parity closure: eighteen gaps close at `owner`, ten NOT-PARITY rows become the declared exclusion list `GS-304` needs to be writable at all, and `0043` is spent because the reservation's contingency was worded against attribution when the missing half was initiative |
| [2026-08-20 - Session 98](2026-08-20-session-98.md) | the plan-config completeness audit: 47 parameters, 3 first-class, 11 materialized, 29 versioned-unconstrained, 4 absent. The referral's taxonomy was missing the state that holds 11 of them, and the exemplary table has one column with no constraint at all |
| [2026-08-20 - Session 99](2026-08-20-session-99.md) | `ADR-070`: plan config is versioned, the `rules` blob stays and its publication is constrained, and contract limits are Merit-owned |
| [2026-08-20 - Session 100](2026-08-20-session-100.md) | `ADR-071`: `M21` admitted as the first new module after FREEZE, with the Monte Carlo harness named as a dependency that does not exist |
| [2026-08-20 - Session 101](2026-08-20-session-101.md) | `FOLD-05` P4: the `M21` plan, the first module document written after FREEZE. Five of the six simulation outputs turned out to already be calibration bands, the parameter form is 43 fields rather than 47, and the harness dependency is a port whose spec has been approved since Wave 4 |
| [2026-08-20 - Session 102](2026-08-20-session-102.md) | FOLD-05 gaps 1 and 2: the fee-back credit reuses `promotional_credit_grants`, the ladder unlock reads the hard-merged identity, and `repeats` ships locked to false |
| [2026-08-20 - Session 103](2026-08-20-session-103.md) | FOLD-05 P6: the marketed size label folded as a versioned disclosure field, with the empty string made unwritable so the absent case has one rendering and not two. No gate can check a label against a number, which is why the control is immutability |
| [2026-08-20 - Session 104](2026-08-20-session-104.md) | FOLD-05 P7: `packages/harness`, the Monte Carlo harness `DEP-M21-01` names. A trial loop and an aggregator over the day model, folded through the real engine, with every result carrying its calibration identity and its own sample size. `AS-08` turned out to be a direct requirement on the port that lives in M01 rather than in the harness document, `RE-S-nn` names two different lists across two approved documents, and one of M21's three unidentified outputs looks like `RE-S-04` under another name |
| [2026-08-20 - Session 105](2026-08-20-session-105.md) | the WAVE-03 merge: eight pull requests landed, the register down from 106 keys across 8 files to 59 across 1, and two findings the merge produced that no branch could see |
| [2026-08-20 - Session 106](2026-08-20-session-106.md) | the fold queue closes: eleven pull requests merged, 24 of 24 gates, and the merge discipline the wave established by getting it wrong once |
| [2026-08-20 - Session 107](2026-08-20-session-107.md) | WAVE-04 planning: eight sessions for the fixture backlog (`OI-25`) and the gate inventory (`OI-26`). **The backlog is 276 rows and sixteen fixtures of available work**, because 243 scenarios have no module code AND no fixture format. Six claims in the brief did not survive the tree, including both `OI` numbers, which were already taken |
| [2026-08-20 - Session 108](2026-08-20-session-108.md) | WAVE-04 `W1`: `ADR-072`, fixture writability as three checkable conditions, and `39-fixture-status-and-blockers.md`, a status and a stated blocker for all 316 registry rows |
| [2026-08-20 - Session 109](2026-08-20-session-109.md) | WAVE-04 `W2`: payout arithmetic and the gate object. **One fixture of six**, and the other five are not writable: the fold diffs a `RuleState` and every value `GS-026` to `GS-029` pins lives on `PayoutEvaluation`, which no fixture in this format ever sees. `GS-080` is the first fixture in the directory to pin `engine_eligible: true` |
| [2026-08-20 - Session 113](2026-08-20-session-113.md) | WAVE-04 W6: `ADR-074`, what a `<PREFIX>-nn` definition site is. **215 series, not the forty briefed**, and `OI` turned out to HAVE an allocation table -- in `DELTA_MANIFEST`, on a namespace argument that now excludes ten of its own members. A definition site is a row or a heading inside a **declared** register, the gate's scope is **written rather than computed** because a computed scope can never fail, and 44 broken series are named as pending rather than exempted |
| [2026-08-20 - Session 116](2026-08-20-session-116.md) | WAVE-04 `W3`: **zero of six fixtures.** `L-11` was lifted and was never the binding clause on any of `GS-035`, `GS-057`, `GS-058`, `GS-066`, `GS-082` or `GS-241`; each is held by a clause that was always beside it, re-derived here against `types.ts`, `rules.ts`, `loader.ts` and M01. `GS-241`'s four figures were computed by hand and all four are correct, and the only half a fixture could reach is already pinned by `GS-067` |
| [2026-08-20 - Session 117](2026-08-20-session-117.md) | WAVE-04 `W4`: the day fold and its boundaries. **Two golden fixtures of the four**, `GS-059` and `GS-049`, every expected end state derived by hand from M01 under `TR-01` and each watched failing on a perturbed pin. `GS-047` is blocked by `L-10`, the loader rule that states its own assertion, and `GS-079` by three plan columns that read `none`; the wave plan had both in Tier 1. `GS-059` finds `EC-049`'s domination claim failing by one trading day |
| [2026-08-20 - Session 118](2026-08-20-session-118.md) | WAVE-04 `W5`: `ADR-073`, a gate-inventory row closes when it is implemented, when it carries a dated activation condition naming one artifact, or when a register outside Actions discharges it. **Four rows are open and not three**, because `CI-04` has no Actions job either, which STATE already said. `CI-09` ships **one leg of four**: the replay self-audit has a subject and no input, and would report a clean audit over zero accounts every night. `VG-11` is struck from `CI-07`, whose contents cell was a transcription of INFRA's gate graph |
| [2026-08-21 - Session 110](2026-08-21-session-110.md) | the M09 site shell: `apps/site` goes from 34 lines to sixteen source files, every public surface a pure function from config to a page model. Eleven invariants landed as shapes rather than as rules. **`3` is a win-day count**, so ADR-018 is read as a ruling that the cadence is `win_days.required_count` and not that it is three; the disclosure text and the payout copy are opposite cases and the corpus says so; and `SD-M9-04`'s column does not exist, so every GS-309 and GS-310 test here renders a view type rather than a row |
| [2026-08-21 - Session 111](2026-08-21-session-111.md) | M04 read surfaces: `apps/portal` from 33 lines to a rendering layer, with four invariants expressed as types (a required `as_of_trading_day`, formatted money strings, a three-valued gate state, and a `CopyBlock` no literal can satisfy). `GS-285` and `GS-100` are asserted in the portal suite and no fixture file was written, because neither is an engine scenario. **Session 111 was already allocated to WAVE-04 `W4`** and was taken here on the founder's call, so `W4` must renumber |
| [2026-08-21 - Session 112](2026-08-21-session-112.md) | M06's read surface: the liability home page, AS-M6-04's three numbers each carrying its own definition, and P-M6-09 gating all of them. Two corpus findings the build produced: **`SD-M6-01`'s identity-max columns are in no migration while the manifest records the delta as landed**, and **AS-M6-04's third number has no panel id anywhere in M06** while `0009` stores it. `P-M6-11` was not minted. Four panels are blocked on named suppliers and `P-M6-07` is blocked on `OI-01`, which said the columns needed a home "before M06 is built" |
| [2026-08-21 - Session 114](2026-08-21-session-114.md) | WAVE-04 `W7`: `CI-09` on a schedule, with the **one leg of four** `ADR-073` section 5 ruled `BUILT`, which gives `packages/harness` the consumer session 106's landmine says it has never had. **No calibration band is supplied and the report says why on every run**: section 5's eleven describe the real lineup under a calibrated population and the real calendar, and the tree has none of the three, so the choice was a band that fails on arrival or none. The seeded violation that matters is a one-session window, where `RE-S-06`'s bound HOLDS over zero funded accounts and reports pass, which is `ADR-073`'s own replay-leg defect reachable in the leg that was built |
| [2026-08-21 - Session 115](2026-08-21-session-115.md) | WAVE-04 `W8`: three `CI-06` slug gates, `gate_count` 24 to 27. **Each gate corrected the record that commissioned it.** `<<<<<<< HEAD` never stood in a commit, so the conflict-markers case SEEDS its violation; `CI-06/fixture-inventory` found 43 fixtures on disk against 40 `written` rows and registers the three by name rather than repairing a file outside the fence; and `ADR-074`'s stated 118 series and 1,086 members do not reproduce from its own four classes, which is the check section 3 asked for. The `CI-06l` trap was walked into once, by the scope case written to avoid it |
| [2026-08-21 - Session 119](2026-08-21-session-119.md) | the WAVE-04 merge: six pull requests, `#154` to `#159`, and three logs renumbered because six concurrent sessions each read the highest row and each took the next number correctly. **The collision was a missing claim, not a numbering mistake**, so a claim table now sits above these entries. `W8` ships three gates of four and `CI-06/gate-inventory` moves to `W9` whole rather than being softened to pass before `CI-09` exists. `adr_count` was wrong on BOTH sides of one conflict and the merged truth was neither |
| [2026-08-21 - Session 120](2026-08-21-session-120.md) | `0045` lands: `simulation_runs` and the publish-decision record. **Exactly one of two, never neither**: a published version resolves to its simulation run or carries a written waiver. The `>= 0` sample-size bound agrees with `provenanceFor` rather than with an instinct, and the hex-to-bytea seam is watched rather than claimed |
| [2026-08-21 - Session 121](2026-08-21-session-121.md) | WAVE-04 `W9`: `CI-06/gate-inventory`, `gate_count` 27 to 28. **A disposition applies to a LEG and never to a row**, which `ADR-073` did not rule: session 114 built one of `CI-09`'s four legs, so a gate reading the row's first word reads one leg as four, and the two readings of "how many are open" give 3 and 6 with both printed. It asserts **both** readings of the activation condition, split per artifact: two of six are probed against the tree and must resolve to ABSENT, and `CI-04`'s Neon branch is not a tree fact so that row's **condition** is asserted and its **artifact** is not. **Four of the six conditions name neither a path nor a manifest key**, which the ruling requires; they are registered, not repaired. `CI-09`'s own cell named no workflow and no job, and the runner's first run was red on a normalizer that ate a glob |
| [2026-08-21 - Session 122](2026-08-21-session-122.md) | the four Tier 2 fixture-shape rulings: `ADR-076`, and none of the fourteen needs a second fixture format |
| [2026-08-21 - Session 123](2026-08-21-session-123.md) | WAVE-05 `X1`: six assertion sites name the registry row they discharge, and no assertion moved. **The six are one per `covered-elsewhere` row that did not already name itself**, which is four in `plan-validate.test.ts` and not five, because `GS-083` names itself three times there. **`GS-072`'s cited site executes nothing**: the permutation block is `skipIf(!replayExists)`, the engine exports no `replay`, and the body throws, so a row `ADR-076` counts as covered has no executed assertion and `X8`'s grep cannot tell. `X5` is not blocked by this session, which the wave plan says it is |
| [2026-08-21 - Session 124](2026-08-21-session-124.md) | WAVE-05 `X6`: `GS-030`'s surviving half, **one test**, and it is a count and not a duration. One trading day and one row across each of the six transitions, with the close instants either side pinning that the clock DID move, without which the count would pass on any ordinary fortnight. **One perturbation passed and is recorded because it did**: a one-day shift off a Sunday changes no weekday. The `X7` evidence is widened from two transitions and sixteen sessions to **six and 60**, every one 23 hours, and the falsified sentence is separated into three claims whose truth values differ |
| [2026-08-21 - Session 125](2026-08-21-session-125.md) | the four `ADR-074` disagreements, resolved in place while the ruling is still unsigned |
| [2026-08-21 - Session 126](2026-08-21-session-126.md) | three signatures and one refusal: `ADR-072`, `ADR-073` and `ADR-074` are `accepted` and **`ADR-076` is WITHHELD on the check its own approval line asked for** -- one of section 6's seven cited sites is a skipped block that throws rather than asserts. `ADR-072`'s prediction was tested by three sessions and was wrong once, in the safe direction. A paragraph claiming `CI-06u` cannot see a struck row and its live twin as one key was refuted by seeding one |
| [2026-08-22 - Session 127](2026-08-22-session-127.md) | WAVE-05 `X2`: section 39 gains the `covered-elsewhere` status and **SIX** rows move to it, not the seven `ADR-076` section 6 lists. `GS-072`'s cited block was re-run rather than inherited and is still a skip that throws, so it stays `blocked` with a self-clearing citation. `GS-071` to `writable`, and `GS-049`, `GS-059` and `GS-080` to `written`, which empties `CI06FIXTURE_REGISTER` of all three entries it holds. **`GS-049` carries a disposition and not just a status.** The head counts are recomputed by reading the tree, and the status vocabulary the runner holds is now one term behind the document, which is `X8`'s and is red until it lands |
| [2026-08-22 - Session 128](2026-08-22-session-128.md) | WAVE-05 `X3` and `X4`, one file and two items. `GS-141`'s last sentence is **three clauses in one `expect`**, because two findings sharing a text under different severities are still rendered identically to a reader who reads the text. `GS-076`, `GS-077` and `GS-078` move to a sole-error equality and **all three seeds produce exactly one error**, so there is no finding on that half. **The strengthening is measured rather than argued**: a second violation seeded into each seed fails the equality and PASSES the `toContain` it replaces, on the same subject |
| [2026-08-22 - Session 129](2026-08-22-session-129.md) | WAVE-05 `X5`: `GS-071` at scale, **one test**, a 250-day funded life asserted hash first and then field by field, with the reason neither is redundant written where the next reader will look before deleting one of them. **The scale is asserted before the claim**, because the same four expectations pass at four days. Watched failing in both directions: an input perturbation rejects on the hash and propagates from the perturbed day forward, and a re-hashed field moved by one names `win_days_count` rather than reporting a generic mismatch. **Two money-path findings that only a funded life produces**: every eval pass row violates `rule_states_consistency_period_started`, and R-15's permanent lock makes any funded life that closes above its lock-day high unstorable under `rule_states_high_water_bounds_balance`. The replay is 10 ms at 250 days, which is a number `CI-09` may want |
| [2026-08-22 - Session 130](2026-08-22-session-130.md) | WAVE-05 `X7`: [ADR-077](../decisions/ADR-077.md), and **the hour is in the WEEKEND GAP**. 782 sessions over three whole years and every one is 23 hours, which widens session 124's 60 and `ADR-076`'s 16. **48 and 50 hours against an ordinary 49** is where the transition actually lands, and both numbers are kept OUT of the rows because nothing executes them. **The two documents state the clause differently and are ruled separately**; the constitution's own antecedent is false too and is recorded rather than ruled. Scope answered on a bounded grep: **nothing in the tree subtracts a session open from a session close**. One session IS 43.25 hours and it is `ADR-055`'s absorbed one |
| [2026-08-22 - Session 131](2026-08-22-session-131.md) | WAVE-05 `X8`: `CI-06/fixture-inventory` learns `covered-elsewhere`, and the assertion behind it requires the cited assertion to be **EXECUTED** rather than merely present. **The plan's specification is refused in writing**: "the citation resolves and its file names the row's id" is satisfied by `GS-072`, whose cited block is a `describe.skipIf` over a false condition whose body throws, which is what withheld [ADR-076](../decisions/ADR-076.md). **A static parse and not a `vitest` run**, refused on falsifiability rather than cost: [`falsify.mjs`](../../scripts/corpus/falsify.mjs) copies the tree without `node_modules`, so a gate needing a workspace resolution could never be watched failing. Watched both ways on one row: seeded `covered-elsewhere` it is refused, and with the `.skipIf` removed and nothing else changed it passes. `CI06FIXTURE_REGISTER` is emptied of the three entries session 127 repaired. **Session 115's row was found eaten by its own prose**: it spells a conflict marker mid-line, a merge resolved on it, and session 130's row landed inside the wound |
| [2026-08-22 - Session 132](2026-08-22-session-132.md) | **P2 planned against the tree rather than against [P2](../plans/P2-rules-engine.md).** [P2-rules-engine-build](../plans/P2-rules-engine-build.md), `status: draft`: eight sessions, **five concurrent**, fence BY FILE. **Six of P2's eight stated contents already existed and no entry said so**: the simulator's STREAMING mode landed in `0b526c8`, `RE-D-01` to `RE-D-03` are built and falsified, and CI-03's polarity flip finished at 43 direct and 0 inverted with the direction ENFORCED. **[Session 129](2026-08-22-session-129.md)'s second money-path finding is FALSE against the tree**: the constraint it cited at `0015:208` was superseded by [`0037`](../../packages/db/migrations/0037_supersede_rule_states_high_water_bounds_balance.sql) under [ADR-053](../decisions/ADR-053.md) five days earlier, so `ADR-079` is one application rather than two. The one real defect stands: `rule_states_consistency_period_started` refuses every eval-pass row the engine is specified to produce, and no gate could see it because CI-03 never touches a database. Claims `ADR-078` to `ADR-081`, migration `0046`, session numbers 132 to 140. **No code, no migration, no gate, nothing signed** |
| [2026-08-22 - Session 133](2026-08-22-session-133.md) | `P2-a`: **`CI-06/identifier-series` reads the scope [ADR-074](../decisions/ADR-074.md) was SIGNED with**, and the two series a signed ruling and a live gate disagreed by are gone. **117 series over 1,083 members, zero findings**, from 115 and 1,070. `OQ-F6` joins the declared scope on a register class that now admits the ADR entry raising it, and its rows were unreadable for a second reason nobody had named: **they are indented two spaces inside a list item**, and a grep says those three are the ONLY indented identifier-leading rows in the repository. `P-M6` joins it with `P-M6-11` in a **third table**, the withheld register, **watched failing in BOTH directions** plus a control that deletes the entry to prove the exemption is doing the work. **The gate's own note was quoting the pre-amendment 118, 1,086 and 44**, and whether the transcription still reproduces the ruled figures is now COMPUTED rather than written into a comment that can go stale, which is exactly how the note went wrong. **The withheld table is deliberately NOT guarded against being empty**: an empty one is the strict end of the rule, and section 5.1 says what ends its one entry. Section 8's open sixth item is decided and measured: the pending predicate stays anchored, because no pending entry changes state under either widening. Nothing in ADR-074 moved |
| [2026-08-22 - Session 134](2026-08-22-session-134.md) | `P2-b`: `replay` joins the engine's public surface under [ADR-078](../decisions/ADR-078.md), and `evaluate` retires. **The brief's site count did not survive reading M01 1.3**: a file in the layout is not a vote for export, so the balance was identical to `clampPayout` and the ruling rests on 1.3's rationale instead |
| [2026-08-22 - Session 135](2026-08-22-session-135.md) | `P2-c`: ADR-079 and `0046`. `0015` bounded the consistency period against the row's own `trading_day` where `R-47` bounds it against the ANCHOR, so every eval-pass row was refused by Postgres. **The row the entry exists for is EXEMPT from its replacement**, so the probe asserts in three directions and watches the new constraint refuse on both sides of `R-47`'s "strictly" rather than only watching the old row insert. Both database states executed, not predicted |
| [2026-08-22 - Session 136](2026-08-22-session-136.md) | `P2-d`: [ADR-080](../decisions/ADR-080.md), **what closes a `VG` row**, taking [ADR-073](../decisions/ADR-073.md) section 8's second ruling after it sat named and unowned since 2026-08-20. **A chained condition names its NEXT LINK and never its whole chain**, and it expires the moment the row it names is implemented. That four-word clause is the entry: without it `VG-5` reads *"waiting on `CI-06`"* forever, against a stage that has run since P1. **Two of the nine "assigned to stages that do not exist" are findings and neither had been counted as one**: `VG-5` has its stage and its subject and no implementation, and `VG-8`'s `PreToolUse` half is named in three documents and absent from `.claude/settings.json`. **`VG-11` takes an artifact rather than a chain**, its stage running and v1 having no upload path by ruling; **`VG-10`'s two legs are CONJUNCTIVE**, which inverts section 4.1's per-leg rule and is what the gate-writing session will inherit wrongly. **The referral's approval clause is corrected**: three rows carry two legs, so "exactly one disposition" fails on arrival. **P1's definition of done is shown to contain a feature Merit has ruled out**, and the choice between two readings is the founder's. No gate written |
| [2026-08-22 - Session 137](2026-08-22-session-137.md) | CI-03's pre-ADR-048 block retired: 43 of the suite's 45 skips were an assertion that could not run again, and two comments described a fold the code had left |
| [2026-08-22 - Session 139](2026-08-22-session-139.md) | Three repairs owed by landed sessions, and **`writable` empties**. `GS-071` moves to `covered-elsewhere` on [ADR-076 section 1](../decisions/ADR-076.md)'s governing rule: no fixture on disk, and session 129's 250-day assertion **EXECUTED and not merely resolvable**, watched at 62 passed and 0 skipped because a passing grep is what `GS-072` also has. **Twelve of the thirteen `plan-validate.test.ts` citations were stale** after session 128 and every one was re-derived by grep, never by arithmetic on the old number; two cells were stale in substance as well, since `X3` landed the residual `GS-141` still claimed and `X4`'s exact-set strengthening was in no cell. [ADR-077](../decisions/ADR-077.md)'s struck clause is re-quoted in the fixture README, in `GS-030`'s wording rather than `EC-012`'s, which is the mis-attribution that entry found. **[`gates.mjs`](../../scripts/corpus/gates.mjs) was in the fence and needed no edit** because the counts derive; **[`falsify.mjs`](../../scripts/corpus/falsify.mjs) was NOT and had to move**, since an empty `writable` starved a seed that hunted for one, and it now builds its anchor the way `register-names-no-defect` does |
| [2026-08-23 - Session 138](2026-08-23-session-138.md) | `P2-g`: [ADR-081](../decisions/ADR-081.md), canonical state hashing moves into the engine and the SHA-256 is **hand-rolled**, which the [2026-08-17 review desk](../reviews/2026-08-17-review-desk.md) section 3 ruled and this session applies rather than re-opens. `node:crypto` is refused there by three mechanisms at once and exempting `RE-D-03` would be weakening a gate to pass it |
| [2026-08-23 - Session 140](2026-08-23-session-140.md) | **The second code path [M01 section 3.7](../plans/M01-rules-engine.md) forbids is closed**, which [ADR-078](../decisions/ADR-078.md) recorded as owed by name and by path. `auditAccount` folds the engine's `replay`, and **four restatements go with the loop**: the arrival order, the settlement grouping, the assertion handling and the terminal break -- the fourth **absent rather than restated**, since `advanceDay` refuses every day after `closed` or `graduated` and the folded-day sets were already identical. **A refusal is reconciled BY FOLDING TWICE and the answer is on the page**: 3.7 forbids a second EXPRESSION of the fold, not a second CALL to it, and the second call cannot refuse because `replay` throws on the FIRST refusal from inside its loop, so every earlier day has already folded. **The alternative is named as refused**: widening `replay` to return the prefix is out of fence AND reopens a signed contract. B.2's asymmetry becomes **structural** -- the recomputed side is a `StateHashSubject` with no hash field, so the stored row cannot be re-hashed even by mistake -- and `context_gates` and the recomputed `calendar_revision_id` stop being computed because nothing ever compared them. **The refusal path had no test**: one was written against the loop-and-break FIRST, watched passing, watched rejecting two perturbations, and only then re-run against the engine fold. `GS-071`'s 250 days are compared against `foldAccountDay`'s own rows, **250 digests then 43 fields a day**, watched rejecting a dropped settlement. **Three stale `0015` citations sit in this fence and are deliberately left**, because the register for them is the unwritten `CI-06/retired-constraints` and not a diff the founder reads line by line |
| [2026-08-23 - Session 141](2026-08-23-session-141.md) | **102 `0015` citation sites checked, 99 resolved and 3 stale**, and both numbers are the result. All three stale are in [`replay.test.ts`](../../apps/worker/test/replay.test.ts) and **one is INVERTED rather than out of date**: `0037`'s replacement exempts the locked row the comment says it refuses, which is [session 129](2026-08-22-session-129.md)'s second finding still standing in the file that produced it after five documents were corrected. **The grep the corpus mandates is the wrong one**: session 135 correctly checked for a SUPERSEDING migration before citing, and nobody has ever checked for EXISTING citations after superseding. **The method generalises over all seven retired constraints** -- 16 `DROP CONSTRAINT` less 9 same-name re-adds -- and finds one more, a live invariant row in [`data-model/README.md`](../architecture/data-model/README.md) whose label and constraint were both falsified by `0036`; **in fence and deliberately not repaired**. Rule stated and gate named, unwritten: `CI-06/retired-constraints`, a slug, seeded from the migration directory. **`OI-27` closed**: [ALLOCATION](../decisions/ALLOCATION.md) gains the fourth table [ADR-074](../decisions/ADR-074.md) section 7 ruled and `DELTA_MANIFEST` section 16 points at it, **`OI-19` and `OI-20` are found double-claimed and were never counted**, and section 7's predicted gate failure is refuted by running it |
| [2026-08-23 - Session 142](2026-08-23-session-142.md) | `OI-28` is ruled by [ADR-082](../decisions/ADR-082.md): **the closed unit vocabulary widens its DEFINITION and never its token list.** `rail clock` becomes any third party's clock Merit quotes and never derives, `rail` naming the class after its commonest member, so [session 120](2026-08-21-session-120.md)'s declaration was right on the day it landed. **A fourth token is refused structurally**: three tokens partition on *who answers this date, and may Merit compute it*, which has exactly three answers, while a fourth partitions on *who the counterparty is*, which has none, and no mechanism [ADR-042](../decisions/ADR-042.md) wired treats a payment rail differently from a data vendor. **`why` had been dead data for as long as the vocabulary existed**, rendered nowhere, so a session at a failing column had to open `gates.mjs` to see what it was choosing between, which is what session 120 did and what `OI-28` came out of; the definitions print with the finding now. **`CI-06m` gains a note line and reports 54 `date` columns at 34, 18 and 2 before the widening and the identical figures after.** `CI-06m/widened-definition-is-not-a-fourth-token` watches `vendor clock` refused. 28 of 28, 56 scope cases, nothing signed |
| [2026-08-23 - Session 143](2026-08-23-session-143.md) | **P3 planned against the tree, and the answer is the OPPOSITE shape from P2's: none of P3's six stated contents exists as application code.** [P3-ledger-billing-identity](../plans/P3-ledger-billing-identity.md), `status: draft`. The reason is one shared precondition rather than six gaps: **the workspace declares zero runtime dependencies**, `ScopedDb` is an interface with one field whose own file says the client does not exist, and [OVERVIEW](../architecture/OVERVIEW.md) draws an `API` container its container table does not row. **That precondition is in NO phase's contents.** `CI-04` is sharper than the record says: a database in CI exists in [`corpus.yml`](../../.github/workflows/corpus.yml)'s `postgres:16` service, its NAMED artifact does not, and its SUBJECT, the `integration` Vitest project, has never been selected by any workflow. **P3 does not wait on the vendor call and meets it at exactly one point**, `GS-138`, which is P3's own third done-clause and is `blocked / vendor-call`. Claims `ADR-083` to `ADR-087`, migration `0047`, sessions 144 to 148, and **repairs `082`**, dispatched to session 142 with no ALLOCATION row. **Wave 1 is SERIAL on `pnpm-lock.yaml` and wave 3 is deliberately not dispatched.** No code, no migration, no gate, nothing signed |
| [2026-08-23 - Session 144](2026-08-23-session-144.md) | `P3-a`: [ADR-083](../decisions/ADR-083.md) rules the `API` container and [`apps/api`](../../apps/api) exists. **The API is its own deployable, ONE CODEBASE DEPLOYED TWICE, and Fastify 5 runs it with no build step ever.** **The brief pointed at the origin argument and the origin argument does not decide this**: [P1 section 2.1](../plans/P1-monorepo-scaffold.md)'s route-group sentence is about the admin console's SEPARATE origin, and [INFRA](../architecture/INFRA.md) puts the portal and the API on the SAME one. The sentence that decides it is [API_CONTRACT section 1](../architecture/API_CONTRACT.md)'s *"no privileged back door"*: a surface that CONTAINS the API has one by construction, and `RI-04` closes it, section 2 drawing three arrows into a container no app may hold. **The mechanism is two status codes one row apart**: 403 is what a permission check returns and **404 is what an absent route returns**, so the surface is chosen at STARTUP and the public deployment registers no operator path. **A partition by the contract's section numbers puts the liveness probe on the admin origin**, section 9 holding `GET /health` marked Public beside `GET /internal/health/deep`; the partition is over path prefixes. **Four framework properties measured against `fastify@5.12.1` on Node `v22.22.2`**, one of which makes every decorator-first framework unavailable rather than merely rejected. **The brief's candidate approval clause is false and stays false after the fix** -- two library rows have no box in a C4 context diagram and correctly never will -- so it is asserted in the one direction that was actually false. **`RI-04` does not see the fifth deployable and INFRA's Railway table names four services where this needs six**, both reported and outside the fence. 28 of 28, 7 of 7, verify green, nothing signed |
| [2026-08-23 - Session 145](2026-08-23-session-145.md) | `P3-b`: **`ScopedDb` stops being an interface with one field and becomes a real accessor**, over a scope registry that is DECLARED and never derived. `ADR-084` supersedes one consequence sentence of signed [ADR-008](../decisions/ADR-008.md) rather than recording it as a finding. |
| [2026-08-23 - Session 149](2026-08-23-session-149.md) | **Seven signatures, and every one of the seven was ALREADY MERGED**: live code on `main` was enforcing seven unsigned rulings, which is the [ADR-073](../decisions/ADR-073.md) defect recurred sevenfold. [ADR-077](../decisions/ADR-077.md) to [ADR-083](../decisions/ADR-083.md) signed on delegated authority, **two of them only after the entry was amended**. **[ADR-078](../decisions/ADR-078.md)'s clause was FALSE against the tree** -- *"exactly the fourteen names"* against a list [ADR-081](../decisions/ADR-081.md) had already moved to twenty -- and signing it unchanged would have contradicted the standard [session 126](2026-08-21-session-126.md) set when it WITHHELD [ADR-076](../decisions/ADR-076.md); the clause is time-anchored and the fourteen survive as a subset of the twenty. The entry's own warning that *"a clause that names the list cannot drift the way a count can"* was aimed one level too low: **the word that drifted was `exactly`**. **[ADR-077](../decisions/ADR-077.md)'s clause SPLIT** -- the absence held, the enumeration offered as evidence did not: its own mandated grep returns **sixteen files against section 5.3's eight**, all eight additions were read, **none subtracts an open from a close**, and the table is amended in place. **A sixth FILE exists; a sixth COMPUTATION OF THE KIND THE CLAUSE NAMES does not.** [ADR-081](../decisions/ADR-081.md)'s hand-rolled SHA-256 was re-tested against `node:crypto` over **281 inputs, 281 matched**, including lone surrogates and every block boundary; **the first draft of that sentence said 322 from memory and the run said 254**, so the corpus was widened and the number that executed is the number written. **A registry column had been wrong for twenty rows and no gate can see it**: `CI-06r` compares a file with itself and says so in its own `covers` line, so nothing checks an ADR heading against the row transcribing it in [`decisions/README`](../decisions/README.md); **27 rows repaired**, twenty of them stale before this session opened, plus four session claim rows WRITTEN and still reading CLAIMED. 28 of 28, 7 of 7. **Two E2 reads stay owed**, `0046` and `hash.ts`, and a signature does not discharge either |
| [2026-08-24 - Session 146](2026-08-24-session-146.md) | `P3-c`: `CI-04` gets `CI-09`'s shape, one implemented leg plus a dated condition, and `VG-3` and `VG-6` take **Waiting** rather than `FINDING`. **The brief's `x` citation did not resolve**: the rule that `x` is not free lives in `falsify.mjs` alone, and this session writes it into [ALLOCATION](../decisions/ALLOCATION.md) |
| [2026-08-24 - Session 147](2026-08-24-session-147.md) | `P3-d`: **[ADR-006](../decisions/ADR-006.md) made the job interface's narrowness a review criterion on 2026-08-13 and nothing had ever been graded against it.** `ADR-086` writes the surface: **five methods, the caller's open transaction as `enqueue`'s FIRST argument**, four of five expressible on a Redis-backed queue and the fifth admitted as the foreclosure. **THE QUEUE SHARES [`packages/db`](../../packages/db/src/client.ts)'s POOL BY CONSTRUCTION AND CANNOT RUN ON IT YET**, measured both ways, and **no second pool was opened**, which is the brief's stop condition met by reporting. Repairs [`pnpm-workspace.yaml`](../../pnpm-workspace.yaml)'s *"`pg`'s 6"*, which contradicted the [ADR-084](../decisions/ADR-084.md) it cited in the same sentence |
| [2026-08-24 - Session 148](2026-08-24-session-148.md) | **[ADR-087](../decisions/ADR-087.md) proposed and [`0047`](../../packages/db/migrations/0047_publish_decision_is_sound.sql) landed**: `OI-29`'s enforcement, ruled to a TRIGGER because the application publish path does not exist in `apps/` at all. **Two of DELTA_MANIFEST's three unsound publish states close and the third does not**, because `simulation_runs.rules_digest` HAS NO PRODUCER: [ADR-081](../decisions/ADR-081.md)'s claim to have landed *"the digest half `OI-29` needs"* does not survive reading `hash.ts`, which hashes `rule_states`. `pgcrypto` IS installed, so the refusal to compare digests is a ruling and not a limitation, and `jsonb::text` is not canonical over numbers. **The reservation cited `0004` for a constraint that lives at `0045:236`** and the mandatory grep is the only reason anyone knows. `OI-29` amended to PARTLY CLOSED, `OI-29b` opened, DELTA_MANIFEST section 25, both data-model pages, 29 of 29 gates. **MONEY PATH, E2 READ PENDING, NOT MERGED** |
| [2026-08-24 - Session 150](2026-08-24-session-150.md) | **Two branches opened against one commit, merged, and the three-way collision they were dispatched to expect cost nothing because it was predicted.** `#190` ([session 149](2026-08-23-session-149.md)) and `#191` ([session 145](2026-08-23-session-145.md)) both based on `338a4f4`, both touching [`decisions/README`](../decisions/README.md), [`STATE`](../STATE.md) and [`sessions/README`](README.md). **Two of the three are additive and were kept whole; the third is the one a keep-both gets WRONG**, because it would have restored the seven `proposed` rows session 149 had just repaired, so it took one side and re-inserted the other side's single new row. **`adr_count` is the figure that proves the merge**: 149 minted no ADR and 145 mints `084`, so the result is **84** and `gates.mjs generate` agrees with the registry rather than with either branch's copy. **[ADR-084](../decisions/ADR-084.md) SIGNED** on delegated authority, every part of its clause executed on the merge branch rather than inherited: `7 declared tables, 7 scope rules, 0 reachable without one`, eslint exit 0 with zero `merit/no-raw-db-client`, and **`falsify:ci` 12 of 12 at `CI-01`** with the two new refusals found on `ScopedTableKey` and on `__brand` rather than on a bare non-zero exit. **What decided the signature is that session 145 found its own suite non-discriminating and said so**: 22 assertions passed against a seed scoping `accounts` by `user_id`, because the render test took its expected column FROM THE RULE, which is code asserted against itself and is the exact BOLA failure ADR-008 scoped the wrapper to bound; four checks now read the DDL instead. 28 of 28, 7 of 7. **FIVE money-path files are now merged on delegated authority without the `E2` read** and every signature says so in its own approval line |
| [2026-08-24 - Session 151](2026-08-24-session-151.md) | The three stale `0015` citations in [`replay.test.ts`](../../apps/worker/test/replay.test.ts) are repaired, **re-derived from `0015`, `0037` and `0046` before a word was edited** rather than taken from the two sessions that found and left them, and [session 141](2026-08-23-session-141.md)'s reading holds on all three. **THE INVERTED ONE WAS A JUSTIFICATION THAT CHANGED OWNER AND NOT A NAME THAT MOVED**: `:826` is the load-bearing reason the 250-day life is capped at 219,000c, it gave that reason as *"a life whose rows no database would accept"*, and `0037`'s `floor_locked OR high_water_balance_cents >= balance_cents` EXEMPTS exactly the locked row it says is refused, with `0037`'s own stated cost being that post-lock the schema asserts nothing about the pair. The cap is still asserted; what holds it is now the file's own two assertions. **The second site got SAFER, which is the opposite direction from the one "stale" suggests**: under `0015` it sat exactly on the boundary at `period start == trading_day`, and under `0046` the comparison is against the ANCHOR, which [`settle.ts:148`](../../packages/rules-engine/src/payout/settle.ts) sets to the basis day, so a consecutive-session fixture clears it STRICTLY by one session. **No assertion is wrong and each was traced rather than assumed**, so this is a comment repair and not a finding: `floorLocked` empty, `hwb >= balance` on all 250, the 219,000c peak under R-15's 260,000c, and the settlement fact's two days all still hold. **The repair form is the TENSE and it was already in the tree** at [`floor.ts:139`](../../packages/rules-engine/src/day/floor.ts), so all three survive the next supersession instead of becoming its residue. **164 tests before and 164 after**, 28 of 28, verify green. `CI-06/retired-constraints` stays unwritten and now inherits three REPAIRED instances, so it must be watched failing on a seeded row; `data-model/README`'s `0036` instance still stands; and **the sweep the corpus has never mandated -- existing citations AFTER superseding -- is untouched** |
| [2026-08-24 - Session 152](2026-08-24-session-152.md) | [ADR-088](../decisions/ADR-088.md) proposed: the ADR table and the session ENTRY table become GENERATED spans, sourced from the entry files themselves, and **the CLAIM table is refused because a claim precedes its file by design**. The brief's 25-of-25 reproduces only under `--first-parent`, which is one merge per session and is the unit contention is measured in; over raw commits it is 9 and a later reader running `git log -25` would think the premise collapsed. **The motivating drift is already repaired**, so the argument is prevention rather than a repair, and the entry says so. **A second drift WAS live and no gate could see it**: [session 95](2026-08-20-session-95.md) holds three `##` sections against one row, and [ADR-064](../decisions/ADR-064.md)'s signed `(log file, section heading)` identity makes the two orphans appear by construction. **The rows are not derivable from the headings**, checked at source, so 185 index markers were backfilled VERBATIM from the rows that already existed and the byte guarantee was proved two independent ways. **Ten ADR headings moved, one line each**, five enriched and five capitalisation-normalised, `status:` untouched; the plan said eleven and `ADR-080` was the row that differed rather than the heading. A **third occurrence of the misfiled-registry-row defect** was found by the generator and repaired, and `CI-06n` caught the over-reach that found it. **No `CI-06x` gate and the letter stays unclaimed**, on the brief's own condition |
| [2026-08-24 - Session 153](2026-08-24-session-153.md) | **`RI-04` was PASSING while asserting nothing whatsoever about the deployable it was cited to protect.** Its `DEPLOYABLES` literal held four names, [ADR-083](../decisions/ADR-083.md) ruled [`apps/api`](../../apps/api) a fifth, and **ADR-083 section 3 leans on this check BY NAME** as the mechanical reason the API cannot live inside a UI surface -- so three green runs between [144](2026-08-23-session-144.md) and here were evidence about four apps read as evidence about five. **`apps/api` needed no edit**: both loops were checked against it BEFORE the literal was widened and it satisfies both, so the invariant was narrower than the tree rather than the tree being wrong. **The title is now COMPUTED from the list** rather than written beside it as the word *"four"*, which is the defect [`RI-05`](../../packages/tooling/checks/repo-invariants.mjs)'s own `covers` line calls *"a hand-maintained count in a different costume"*. **Three seeds watched failing, and the second is the one worth having**: seed A proves `api` is iterated as a subject, seed B proves it entered the `byName` map so the four older apps are now refused a dependency ON it, which is the direction ADR-083's three drawn arrows actually run. **THE FIXTURE HELD A SECOND COPY OF THE LIST, WHICH IS WHY A SUITE THAT EXISTS TO CATCH DRIFT DID NOT CATCH THIS ONE**: `cleanTree()` built its tree from its own four-name literal, agreeing with `DEPLOYABLES` by coincidence and going stale in step with it. `DEPLOYABLES` is exported and the fixture loops over it -- **one list read twice, not two lists maintained together** -- which is a declared fence widening onto the test file, since the checker's own header names it by path. **The standing gap is recorded rather than closed**: RI-04 never reads `apps/`, so a sixth directory with no entry is still invisible, and deriving the list from disk was refused as a different check with a different failure mode. **[INFRA section 2](../architecture/INFRA.md)'s Railway list is reported and NOT taken, and not because of its size**: it is one cell, but INFRA is `approved` corpus and this session carries no ADR, and `portal-api` sitting beside a service named `api` is a naming question rather than a transcription. 7 of 7, 28 of 28, `verify` green at 104 files and 1,512 passed |
| [2026-08-24 - Session 154](2026-08-24-session-154.md) | **[ADR-089](../decisions/ADR-089.md) proposed: six Railway services, derived rather than counted**, and **`portal-api` ruled a STALE FUSED PROCESS NAME rather than a shared-origin label**, so it retires. A prose phrase became a service identity through three transcriptions and no ruling. [INFRA](../architecture/INFRA.md) amended in four places: section 2's list, a new section 2.1, section 3's third hard rule, and one sentence in section 13.2. Six `portal-api` code sites in [`apps/portal`](../../apps/portal) and [`apps/api`](../../apps/api) are named and left. 28 of 28 gates, 7 of 7 invariants. **Renumbered from 152 and ADR-088 at merge**: this branch and [session 152](2026-08-24-session-152.md) were both dispatched to take the lowest free number without either reserving it, which is `OI-27`'s shape and the dispatcher's error rather than either session's |
| [2026-08-24 - Session 155](2026-08-24-session-155.md) | **Three ADRs signed on delegated authority after every conjunct was EXECUTED on the merged tree rather than inherited from a branch**: [ADR-085](../decisions/ADR-085.md) (`CI-04` gets a selected project), [ADR-088](../decisions/ADR-088.md) (generated registries) and [ADR-089](../decisions/ADR-089.md) (six Railway services). **ADR-088's clause named 85 rows and the tree has 88**, so it was time-anchored before signing and the standing assertion became the PROPERTY -- one row per file, one row per section, no loss either way, measured at 88 to 88 and 190 to 190. **That is the THIRD approval clause in two days to name a count that then drifted**, and it happened inside the entry whose own subject is registries drifting. **A defect this session's own dispatcher shipped to `main` was found by the session it mis-renumbered**: the ADR-088 to ADR-089 renumber rewrote the entry and its log but not the FIVE [INFRA](../architecture/INFRA.md) citations pointing at it, so for one commit INFRA cited the generated-registries ruling for the six-services ruling. A renumber's scope is every document that CITES the entry. 29 of 29 gates, 7 of 7 invariants, `falsify:ci` `CI-04` 2 of 2 |
| [2026-08-24 - Session 156](2026-08-24-session-156.md) | **`VG-8`'s `PreToolUse` half is built and watched refusing**, taking the FINDING [ADR-080](../decisions/ADR-080.md) section 6 recorded. Three predicates and exactly the three the corpus enumerates, each a rule over BLAST RADIUS rather than over a command name: `rm -rf` passes inside the worktree and is refused outside it, a database URI passes at `localhost` and is refused at a routable host, `--force-with-lease` passes and `--force` does not. **The dispatch's "three documents" is five documents and nine citations**, and the two ADR-080 did not name include `VIBE_FAILURE_POSTMORTEMS`, which is `VG-8`'s own source. **The three DO disagree and the disagreement decided the scope**: [INFRA](../architecture/INFRA.md) section 10 and constitution C10 carry a FOURTH predicate, the `payout/` and `ledger/` write block, that neither `VG-8` row carries, and **"the confirm flag" it turns on is named twice and defined nowhere**, so it was reported rather than invented. **The CLAIM table's 156 row was reported absent and that report is RETRACTED**: it was true of the commit this branch was cut from and false of `main` ten minutes later, so the row is struck rather than the dispatch corrected. The row is `Discharged outside Actions` and not `Wired` because no Actions job runs a hook that fires on a tool call, which is the next repair. **The hook refused this session's own first commit message**, which quoted a routable host, and the message was reworded rather than the rule softened |
| [2026-08-24 - Session 157](2026-08-24-session-157.md) | **M03 measured against the tree. Its SIX schema deltas ALL LANDED in [`0006_commerce.sql`](../../packages/db/migrations/0006_commerce.sql) and its application code is EMPTY: zero `PspAdapter`, zero routes, zero `coupon`, zero `refund`, zero `mid_health` in any `.ts` in the repository.** So M03 is not [session 143](2026-08-23-session-143.md)'s "schema only" uniformly, and **three specified enforcements are absent rather than unbuilt**. `INV-M3-01` says the purchase's `plan_version_id` is write-once BY TRIGGER; **the tree holds ten triggers and the pin one is on `accounts`, not on `purchases`**. `INV-M3-16`'s `marketed_size_label` **exists at 20 TypeScript sites in [`apps/site`](../../apps/site) and in ZERO SQL**, and [DELTA_MANIFEST:156](../../packages/db/DELTA_MANIFEST.md) rows `SD-M9-04` against migration `0044`, which [ALLOCATION:136](../decisions/ALLOCATION.md) records as **SPENT UNDER A DIFFERENT NAME**. **Both of M03's events marked NEW are registered nowhere and one has two spellings**: [EVENTS section 4](../architecture/EVENTS.md) carries nine M3 rows and neither `mid.health_changed` nor `purchase.first_trade_recorded`, while [M10:165](../plans/M10-integrations.md) writes `psp.mid_health_changed`. **`ScopedDb` is READ-ONLY** (`rows(key)` and nothing else) and `INV-M3-07`, `INV-M3-12` and `INV-M3-13` each require a transaction, so all three are inexpressible. **Seven tables of 111 are in [`schema.ts`](../../packages/db/src/schema.ts) and none is M03's**, which is the cross-module collision rather than an M03 gap. **`CI-06o` CANNOT DISCOVER CHECKOUT AS A MONEY PATH**: `MONEY_PATH_WORDS` is `rules-engine, payout, ledger, auth`. **All 18 golden scenarios M3 touches are `blocked / no-fixture-format`, one cause.** Ten slices, six collision files. **The CLAIM row this session reported ABSENT was reserved on `main` MID-SESSION**, by `#199` at 15:57 UTC, sixteen minutes before this branch committed the claim that it did not exist: the absence was real at `ef4cccc` and the session never re-fetched, which is **a stale measurement of a moving file** and is corrected here rather than carried. Nothing built, nothing ruled, nothing signed |
| [2026-08-24 - Session 158](2026-08-24-session-158.md) | **[M04](../plans/M04-trader-portal.md) measured against the tree. The transcription is CLEAN: all 113 wire fields across the five transcribed types match [API_CONTRACT](../architecture/API_CONTRACT.md) sections 3/5/6/7 exactly, zero missing and zero invented, re-derived here rather than inherited from `api-types.test.ts`. THE DRIFT IS IN THE PLAN'S PROSE, NOT ITS CODE.** M04 section 4 says the session-list endpoint *"has never appeared in the contract"*; `GET /sessions` and `POST /sessions/:id/revoke` landed at [API_CONTRACT:215](../architecture/API_CONTRACT.md) on 2026-08-16 and [STATE](../STATE.md):229 already says so, four days before M04's own `last_updated`. **`required_factor` has TWO hits in the whole repository and both are inside M04**, so `INV-M4-14`'s required prop and `DEP-M4-07` have no wire field anywhere and `Problem` has no such member either. **Section 8.1's golden count says 6 and [ownership index 33.1](../testing/golden-scenarios/33-ownership-index-and-coverage-reconciliation.md) rows M4 fifteen**, which is the drift section 8.1 spends a paragraph refusing on two other rows. **3 of 11 screens exist, 14 of 17 invariants are cited in code, all 4 `SD-M4-nn` landed, and all 15 golden scenarios are `blocked / no-fixture-format`.** Zero `M4-R/N/E/V/F` tests exist outside `docs/`. **THE 158 CLAIM ROW DID NOT EXIST WHEN THIS SESSION OPENED AND LANDED MID-FLIGHT**, as `#199` reserving 157 to 170, so the branch carried a stand-in row and the merge kept the dispatcher's and struck it in place. **`CI-06u` caught it on the merge result and nothing else could have**, which is session 143's predicted-collision lesson arriving from the other direction: the repair, not the defect, is what collided. Thirteen slices, and `apps/portal/src/api/types.ts` is on EIGHT of them. 29 of 29 gates, `verify` green at 104 files / 1,512 passed / 1 skipped. No code, no ruling, no new document |
| [2026-08-24 - Session 159](2026-08-24-session-159.md) | **[M05](../plans/M05-payout-system.md) measured against the tree, and the answer is the OPPOSITE shape from [P3](../plans/P3-ledger-billing-identity.md)'s: M05's SCHEMA IS COMPLETE and its RUNTIME IS EMPTY.** All nine deltas `SD-M5-01` to `SD-M5-09` are merged across [`0009`](../../packages/db/migrations/0009_ledger.sql), [`0010`](../../packages/db/migrations/0010_payouts.sql), [`0011`](../../packages/db/migrations/0011_wallet.sql), [`0030`](../../packages/db/migrations/0030_payout_hold_enum.sql) and [`0031`](../../packages/db/migrations/0031_payout_hold_and_identity_restriction.sql), the deferred zero-sum trigger is in [`0027`](../../packages/db/migrations/0027_triggers_invariants.sql), and [`probe_payout_hold.sql`](../../scripts/db/probe_payout_hold.sql) drives six successes and five rejections in CI. **Against that: ZERO of M05's nine endpoints is routed, and SEVEN of its tables are absent from BOTH [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts)**, whose registries hold seven tables of which only the ledger triple and `treasury_balances` are M05's. **`scopedDb` cannot name `payout_requests`.** **`INV-M5-20` IS STATED, UNASSERTED, AND `0031` SAYS OTHERWISE IN ITS OWN COMMENT** (*"the probe drives it"*); the probe writes `sla_due_at` and never compares it to `hold_expires_at`. **`INV-M5-04`'s SECOND HALF IS UNASSERTED**: per-transaction zero-sum is a constraint trigger, the whole-table assertion exists nowhere. **`ledger_transactions.kind` IS UNCONSTRAINED `text`** and `LT-01` to `LT-08` are a vocabulary in prose. **FIVE of M05's events are absent from [EVENTS](../architecture/EVENTS.md) and [STATE_MACHINES:153](../architecture/STATE_MACHINES.md) asserts one of them already landed.** **`cooling` is a status with no clock, which `CI-06l` cannot see because `CI-06l` reads `*_expires_at`.** **62 specified tests, zero written; all six owned golden scenarios `blocked`.** Eleven slices, four collision files, and **the CLAIM row for `159` was ABSENT when this session measured and ARRIVED WHILE IT WROTE**: `a3d1dd4` reserves `157` to `170` *"before dispatch"* and landed after dispatch, so this session wrote a second row on its own behalf and **`CI-06u` caught the live twin on the first CI run**, which is the gate doing exactly its job. 29 of 29, `pnpm run verify` green, both unaffected by this session, which writes no code |
| [2026-08-24 - Session 160](2026-08-24-session-160.md) | **M06 measured against the tree, and the shape is the INVERSE of [session 143](2026-08-23-session-143.md)'s P3 finding: M06's SCHEMA IS COMPLETE and M06's RUNTIME DOES NOT EXIST.** All eleven `SD-M6-nn` deltas have a merged migration and [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md) rows every one `landed`; **zero of the forty operator route-and-verb pairs the corpus specifies exist**, because no process in this tree listens on a socket: `fastify` is in no manifest and [`apps/api/src/index.ts`](../../apps/api/src/index.ts) prints *"no routes yet"*. [`apps/admin`](../../apps/admin) is 2,673 lines of PURE FUNCTIONS over injected inputs, 26 `M6-A-nn` tests, and it opens no connection and mutates nothing. **The precondition under nearly every slice is not the router**: `packages/db/src/schema.ts` carries **7 tables of 111** and **exactly one of M06's fourteen**, so `scopedDb`/`systemDb` cannot reach thirteen of them **by compile error, deliberately**. `SD-M6-01` is **2 columns of 4** against a manifest row reading `landed` ([session 112](2026-08-21-session-112.md)'s unrepaired finding). **All 30 M6-owned golden scenarios are `blocked / no-fixture-format`, one shared cause**, and five carry unit assertions the registry does not row `covered-elsewhere`. `M6-A` is the only one of the seven suites with a single test in it. **This session ran ahead of its own CLAIM row**: `160` was unclaimed at `ef4cccc`, nothing was fabricated, and `#199` reserved the whole fourteen-session wave mid-measurement, so **fourteen sessions ran against a table that did not know they existed** - a claim that is correct and LATE, which `CI-06w` cannot see because the multiset is right the moment it lands. Nine slices, four MONEY PATH, three BLOCKED before they can start. No code, no migration, no ruling |
| [2026-08-24 - Session 161](2026-08-24-session-161.md) | **M07 measured against the tree: EVERY ONE OF ITS FIVE SCHEMA DELTAS IS LANDED AND NOT ONE LINE OF ITS CODE EXISTS.** All of `SD-M7-01` to `SD-M7-05` read **landed** in [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md), and `detector_runs_synthetics_match_status` puts `INV-M7-07` in DDL. Against 247 source files, case-insensitively, **no detector, no resolver, no canary, and no `M7-D`, `M7-R`, `M7-P`, `M7-S` or `M7-G` suite**; the only file in the tree that reads M07 is [`duplicate-signal-views.test.ts`](../../packages/db/test/duplicate-signal-views.test.ts), which executes no detector and **asserts two of M07's sentences verbatim**. **`INV-M7-02` is CONTRADICTED rather than unbuilt**: it names a permission as its enforcement, [`0026`](../../packages/db/migrations/0026_roles_and_grants.sql) creates three roles and no detector role, and `risk_flags` is absent from its `REVOKE UPDATE` list. **`CI-09`'s detector-canary leg CANNOT REOPEN ITSELF**: its artifact is in `gates.mjs`'s unprobeable register, so unlike `CI-07`'s build script the gate does not fail on good news, and all of M07 could land at 29 of 29 green. **The smallest activation is ONE detector, `D-01`, on inputs [`fills-report.ts`](../../packages/rithmic/src/simulator/fills-report.ts) already emits, needing no database**, plus two registry edits that must land in one commit. Eight slices, six collision files, **the CI leg is not `INV-M7-07`'s control and must not be read as it**. **The CLAIM row this session was told to strike DID NOT EXIST at `ef4cccc` and arrived mid-session when `#199` merged**, so the brief was written against a state its own reservation commit had not yet created; `main` merged in, row struck, and no gate could have caught it because an absent row opens no hole. No code, no migration, no gate, no ADR, nothing signed |
| [2026-08-24 - Session 162](2026-08-24-session-162.md) | **[M08](../plans/M08-affiliate-system.md) measured against the tree. All five `SD-M8-nn` deltas LANDED and no superseding migration touches any M8 table; application code is ZERO, and the one file in the tree carrying the word is [`surface.test.ts:69`](../../apps/api/test/surface.test.ts), where `/affiliate/stats` is a string in a path-classification list. **THE HEADLINE IS ONE LEVEL BELOW THE ONE M08 SECTION 3.4 ALREADY FOUND.** That section ruled that "one rail" was doing the work of a shared GATE and none of it in the mechanism. Measured here: it is not doing the work of a shared RAIL either. [`payout_transfers.payout_request_id`](../../packages/db/migrations/0010_payouts.sql) is `NOT NULL REFERENCES payout_requests(id)`, and `payout_requests` is `NOT NULL` to both `accounts` and `plan_versions`, so an affiliate settlement cannot be a row in EITHER table, while `affiliate_statements.paid_transfer_ref` is bare `text` with no FK, which is the second spelling [ADR-017](../decisions/ADR-017.md) exists to refuse. It propagates: [M07:116](../plans/M07-risk-abuse.md) rows `D-09`'s source as `payout_transfers`, so `INV-M8-10` reads a table affiliate money cannot enter, and `DEP-M8-02` and `DEP-M8-05` are unmet in the SCHEMA and not merely in code. **`affiliate_statements.status` has no `held` value** while the trader side got exactly that shape in `0030` and `0031`, so `INV-M8-12` has no state to hold in. **No affiliate table is in `schema.ts`'s seven of 111**, so `SCOPE_RULES` totality makes every M8 read a COMPILE ERROR today, and `attributions` carries TWO identity columns against a four-class vocabulary whose `OwnedRule` names one. **`OQ-075-01` blocks NO slice**: `restricted` is ruled and buildable, `closed` is one predicate. Nine slices, no ADR number and no migration number claimed. **29 of 29 gates, 7 of 7 invariants, 104 files / 1,512 passed / 1 skipped, all unaffected by this session, which writes no code.** The brief said the CLAIM row for `162` already existed and at `ef4cccc` it did not; **it landed on `main` mid-session** and `CI-06u` caught the keep-both, which is the fourth instance of that shape and the first one a gate stopped |
| [2026-08-24 - Session 163](2026-08-24-session-163.md) | **M10 measured against the tree, and the answer is the INVERSE of [session 143](2026-08-23-session-143.md)'s P3 shape: M10's STORAGE is complete and its BEHAVIOUR is empty.** All four deltas landed, `SD-M10-01` to `03` in [`0018`](../../packages/db/migrations/0018_integrations.sql) at `:39`, `:86`, `:140` and `SD-M10-04` in [`0002`](../../packages/db/migrations/0002_identity.sql)`:58`; `merit_dispatcher` exists with the founder's no-`DELETE` amendment ([`0034`](../../packages/db/migrations/0034_reversible_contact_addresses.sql)`:198`), probed in CI. **Against that: zero lines of application code.** All six vendor names return **0 hits** across every `.ts`, `.mjs`, `.json` and `.yml` in the tree; the four `NEW` endpoints have no router to live in because [`apps/api`](../../apps/api/package.json) declares **no dependencies** and [ADR-083](../decisions/ADR-083.md)'s Fastify is not installed; `pg-boss` is a **comment** at [`client.ts`](../../packages/db/src/client.ts)`:12` and session **147** is CLAIMED and unwritten. **`undici` `MockAgent` does not exist**: only `undici-types`, transitively via `@types/node`. **Three claims inside M10 are refuted by the tree.** It says *"the nine [EVENTS section 11](../architecture/EVENTS.md) triggers"* twice and that section rows **TWELVE**; its `OQ-M10-06` declines an `OI-nn` because *"the `OI` series is the one registry with no allocation table"* and [ALLOCATION](../decisions/ALLOCATION.md) has carried that table since session **141**, running to `OI-30`; and **six of M10's ten event names exist in exactly one file, M10 itself.** [INFRA](../architecture/INFRA.md)`:121` gives Metabase `merit_readonly`, a role **no migration creates**. **Seven slices, and the per-FILE collision is `pnpm-workspace.yaml` plus `pnpm-lock.yaml` across three of them and session 147**, which is [session 143](2026-08-23-session-143.md)'s wave-1 finding recurring. **Nothing dispatched, nothing claimed, no ADR, no code.** **The brief's own fence cannot end green and that is measured**: this file makes the `session_entries` span stale, `CI-06g` and `CI-06n` fail at **27 of 29**, and the fence is widened by the **one generated row** the runner writes, on [session 143](2026-08-23-session-143.md)'s precedent. **The `163` `CLAIM` row did not exist at this session's base and merged mid-measurement** (`199`, 15:57 UTC, against a 15:49 pull); it is struck in place, and the wave's premise that fourteen log-only sessions *"collide on nothing at all"* is refuted by that same 27 of 29. 29 of 29 after, `pnpm run verify` green at 104 files / 1,512 passed / 1 skipped |
| [2026-08-24 - Session 164](2026-08-24-session-164.md) | **M13 measured against the tree. THE SCHEMA IS FULLY LANDED AND THE APPLICATION LAYER IS EMPTY**, which is the opposite shape from most modules: [`0022_analytics_journal.sql`](../../packages/db/migrations/0022_analytics_journal.sql) carries all three deltas, [`0028:179`](../../packages/db/migrations/0028_supersede_plan_version_immutability.sql) already repaired both `array_length` traps, and a grep over **219 TypeScript files returns zero M13 code**. **THE HEADLINE IS A FORK AND BOTH BRANCHES BREAK**: [`0026:157`](../../packages/db/migrations/0026_roles_and_grants.sql) revokes `rule_states` from `merit_analytics` on a reason that **appears in no corpus document**, while `AN-M13-04` IS `rule_states` and `INV-M13-01` names only plan config. As `merit_analytics`, `GS-172`'s subject has no readable source; as `merit_app`, `INV-M13-01` is a convention again, which is the one thing it says it is not. **`GS-174` IS UNIMPLEMENTABLE**: it wants four services to fail by grant and **only three roles exist**, one of which holds full DML on `journal_entries`. **[INFRA](../architecture/INFRA.md):118-122 AND `0026` NAME DIFFERENT ROLES** -- `merit_analytics` is in no architecture document and `merit_readonly` is in no migration. **`SD-M13-01` says `uuid[]` and `fills.id` is `bigint`**, so the frozen plan is the outlier against both the migration and DATA_MODEL. **M12 AND M13 DEFINE NO STATISTIC TWICE**, checked at source: `ST-01` to `ST-07` are firm-level and M13's are per-account, and the boundary is drawn from **both** sides. **29 of 29, 7 of 7, 104 files / 1,512 passed / 1 skipped, all unaffected by this session.** No code, no migration, no ADR, no fixture |
| [2026-08-24 - Session 165](2026-08-24-session-165.md) | **M16 measured against the tree, and the answer is the OPPOSITE shape from [session 143](2026-08-23-session-143.md)'s P3: ALL EIGHT schema deltas are landed and NOTHING ABOVE THE SCHEMA EXISTS.** `SD-M16-01` to `SD-M16-03` in [`0019`](../../packages/db/migrations/0019_notifications_and_community.sql), `04` to `07` in [`0029`](../../packages/db/migrations/0029_phone_identity_and_auth.sql), `08` in [`0041`](../../packages/db/migrations/0041_contact_channel_complaints.sql), every one **landed** in [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md). Zero lines of TypeScript name a notification, zero of the six endpoints exist, zero of the eleven fixtures are written and **all eleven are `blocked / no-fixture-format`**. **A NINTH schema touch exists that M16 section 2 does not row**: [`0028:166`](../../packages/db/migrations/0028_supersede_plan_version_immutability.sql) supersedes `notification_kinds_has_channels`. **NONE of M16's six endpoints appears anywhere in [API_CONTRACT](../architecture/API_CONTRACT.md), in any form**, and no gate can see that. **`notifications` and `notification_preferences` carry ZERO later `ALTER TABLE`, `notification_kinds` and `contact_channels` carry four `ADD COLUMN` between them**, which splits the [ADR-084](../decisions/ADR-084.md) transcription into two slices rather than one. **M16:270 cites "the nine" [EVENTS section 11](../architecture/EVENTS.md) triggers and section 11 rows TWELVE**; `identity.restriction_lifted` is named by NO module plan at all. Eight slices, three needing `packages/queue` which **does not exist**: session 147's branch carries a placeholder log and no code. `POST /me/contact-channels` is refused to an impersonation session **by inheritance and by nothing else** ([ADR-068](../decisions/ADR-068.md) row 5, "this entry adds nothing"). 29 of 29, 7 of 7, 104 files / 1,512 passed / 1 skipped, all measured here. No code, no migration, no gate, no ADR, nothing signed |
| [2026-08-24 - Session 166](2026-08-24-session-166.md) | **M17 measured against the tree, and the answer is the THIRD shape: all four schema deltas LANDED and not one line of application code exists.** [`0024_offers.sql`](../../packages/db/migrations/0024_offers.sql) creates `offer_experiments:42`, `price_floors:82`, `offers:105` and `promotional_credit_grants:193`, [DELTA_MANIFEST:180-183](../../packages/db/DELTA_MANIFEST.md) rows all four **landed**, and `grep -rln 'offer' --include=*.ts` over `apps/` and `packages/` returns **four files, every hit the English word**. **Five NEW endpoints and six NEW event families are absent from [API_CONTRACT](../architecture/API_CONTRACT.md) and [EVENTS](../architecture/EVENTS.md), and the wider grep REFRAMES that absence rather than refuting it**: `loyalty` and `graduation` return **0** hits there too, so the catalogues never absorbed any module plan's NEW rows and no gate reconciles them. **`stackable` IS NOT A COLUMN ANYWHERE**, only prose at [master prompt:156](../../MERIT_BUILD_MASTER_PROMPT.md) and four documents, so `INV-M17-05`'s "necessary" half does not exist and the floor is the only stacking control there is. **`price_floors` CANNOT BE DUAL CONTROLLED AS WRITTEN**: its PK is `(product_ref, effective_from)` and `dual_control_approvals.subject_id` is `uuid NOT NULL` ([`0016:227`](../../packages/db/migrations/0016_treasury_controls.sql)), so the control [`price_floors.md:2`](../architecture/data-model/price_floors.md) asserts has no way to name its subject. **`M17`'S ISSUANCE CAP IS CITED BY TWO DOCUMENTS AND DOES NOT EXIST**: [M20:329](../plans/M20-wallet.md) and [`0044:80`](../../packages/db/migrations/0044_fee_back_and_ladder_unlock.sql) both rest on it and M17's twelve invariants contain no such cap. **An offer is NOT plan configuration under [ADR-070](../decisions/ADR-070.md), and ADR-070 does not say so**: it contains **0** occurrences of `offer` and **0** of `M17`; the boundary is drawn by [M21:46](../plans/M21-plan-designer.md) and `INV-M17-01`, and the converse (a plan-version field that ISSUES an M17 instrument) is already live in `0044` and unruled. Six slices, four founder items. 29 of 29, verify green at 104 files / 1,512 passed / 1 skipped, none of it moved by this session |
| [2026-08-24 - Session 167](2026-08-24-session-167.md) | **[M18](../plans/M18-graduation-track.md) measured against the tree, and the answer splits by layer rather than by section: the SCHEMA IS COMPLETE and the READERS DO NOT EXIST.** All four deltas `SD-M18-01` to `SD-M18-04` are `landed` in [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md), the engine owns `graduated` as a terminal phase with `R-49` unit-tested and `GS-067` pinned, and the words `vault`, `benefit` and `review pool` return **zero hits** across `apps/`, `packages/` and `scripts/`. **Two structural collisions were found and neither is in any corpus document.** `accounts.terminal_settlement_id` is an FK to `payout_requests(id)`, every row of which carries `payout_ordinal integer NOT NULL CHECK (> 0)` unique per account, against `INV-M18-05` and `EC-124`'s *"it is not an ordinal"*: **the terminal settlement is representable only as the ninth rung `EC-124` forbids by name**. And **the amount it must pay is destroyed by the call that graduates the account**: MEASURED, not read, a fifth settlement taking 50,000c of a 150,000c surplus returns `phase: graduated`, a raw surplus of **100,000c**, and `withdrawableCents: 0n`, because [`settle.ts:214`](../../packages/rules-engine/src/payout/settle.ts) sets the phase before [`gates.ts:79`](../../packages/rules-engine/src/payout/gates.ts) reads it, and [`rules-h-settlement.test.ts:279`](../../packages/rules-engine/test/rules-h-settlement.test.ts) pins the zero. **`GS-240` is blocked under the wrong vocabulary term**: two of its three clauses are EXECUTED today by `GS-067`, whose event list is compared by length and position. Six slices proposed, one refused, **collisions given per FILE**, and `docs/plans/M18-graduation-track.md` is the file five of six want. **No code, no migration, no ADR, no ruling. 29 of 29 gates, `pnpm run verify` green at 104 files / 1,512 passed / 1 skipped, neither affected by this session** |
| [2026-08-24 - Session 168](2026-08-24-session-168.md) | **M19 measured against the tree. All SEVEN schema deltas are LANDED and there are ZERO lines of M19 application code**, which is the opposite shape from [session 143](2026-08-23-session-143.md)'s P3 measurement and from every other module measured so far: the money-path migrations arrived first and nothing can reach them. `grep` for `INV-M19|SD-M19|AS-M19|FM-M19|PL-M19|DEP-M19` across every `.ts`, `.mjs` and `.sql` in the tree returns hits in **exactly three files, all SQL**. **The blocking precondition is not "no client" any more, it is "no WRITE": [`scoped-db.ts:139`](../../packages/db/src/scoped-db.ts) exposes `rows()` and nothing else, and M19 is a module whose every operation is a write.** None of M19's six tables is in `SCOPE_RULES`; `fastify` and `pg-boss` are in no manifest and in no `catalog:` entry. **The `INV-M20-06` contradiction [ADR-068](../decisions/ADR-068.md) recorded NO LONGER STANDS**: all three extraction doors read `= 'active'` at their primary sources, closed by [ADR-075](../decisions/ADR-075.md), signed. **A documentary one replaced it one file over and no gate can see it**: [M20:529](../plans/M20-wallet.md)'s `OQ-M20-07` is unstruck and its door inventory at `M20:535` still says `INV-M20-06` blocks on `= 'restricted'`, contradicting **line 62 of its own file**. Four more divergences measured: `kyc.pending` is in no catalogue, `kyc.dedupe_hit`'s payload still names a singular `matched_identity_id` after [ADR-029](../decisions/ADR-029.md), `pre_eval` is unwritable and four approved documents still key behaviour on it, and `INV-M19-02`'s "new CV rule" has no number. **Fourteen slices, twelve of them money path.** No code, no migration, no gate, nothing signed |
| [2026-08-24 - Session 169](2026-08-24-session-169.md) | **M20 measured against the tree. The answer is a THIRD shape, neither P2's nor P3's: all five schema deltas LANDED and not one line of application code exists.** `SD-M20-01` to `SD-M20-04` are `0011_wallet.sql`'s four tables and `SD-M20-05` is `0044`, all **landed** in [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md); `grep -ril wallet` over `packages`, `apps` and `scripts` returns **25 files and not one of them reads, writes or gates a wallet position**. **Open item 1 is CLOSED IN THE TREE AND STALE IN THE DOCUMENT**: [ADR-075](../decisions/ADR-075.md) is `accepted`, `INV-M20-06` at [M20:62](../plans/M20-wallet.md) reads `= 'active'` and `G-WITHDRAWAL-CLEARED` at [STATE_MACHINES:421](../architecture/STATE_MACHINES.md) matches, while **`OQ-M20-07` at [M20:531](../plans/M20-wallet.md) still states the hole as live**, still rows `= 'restricted'`, still calls `ADR-062` unsigned, and still proposes a remedy `ADR-075` section 3 REFUSED. **Open item 2 has a sharper answer than the question**: `wallet_entries` carries **four plain indexes and no uniqueness of any kind**, `ledger_transaction_id` among them, so **two wallet credits may point at one ledger transaction** and nothing structurally refuses `GS-052`'s second and third credit. **The only place SQL executes against PostgreSQL in this tree is [`corpus.yml`](../../.github/workflows/corpus.yml)'s twelve `psql` probes**; `CI-04`'s `integration` job has **no database service**, and **no probe touches a wallet table**. Also measured: **`DEP-M20-04` has no input** (nothing records that a purchase was paid with promotional credit; `payment_method` is CHECKed to `('psp','wallet','mixed')`), and **[EVENTS](../architecture/EVENTS.md) catalogues 2 of the 17 `wallet.*` names the corpus uses**. Six slices proposed, four MONEY PATH, colliding on **one file**. **29 of 29 gates, `pnpm run verify` exit 0, both unaffected by this session.** No code, no migration, no ADR, no fixture |
| [2026-08-24 - Session 170](2026-08-24-session-170.md) | **[M21](../plans/M21-plan-designer.md) measured against the tree, an INVENTORY and not an implementation.** **The plan's two headline absences are both WRONG NOW and both were wrong within a day of it being written.** `DEP-M21-01` says the Monte Carlo harness does not exist: [`packages/harness`](../../packages/harness/README.md) is **4,603 lines over 11 source files and 8 test files, 7 files and 71 tests green**, committed 2026-08-20, the same date the plan carries. `DEP-M21-06` says the four absent parameters wait on `0044`: [`0044`](../../packages/db/migrations/0044_fee_back_and_ladder_unlock.sql) is merged and landed **two of the four**, so requirement (a)'s "every field" is **45 of 47 and not 43**. **The one thing nobody recorded as missing is the thing that blocks everything: NO FUNCTION IN THIS TREE COMPUTES `rules_digest` OR `sizes_digest`.** `sha256` exists in [`hash.ts`](../../packages/rules-engine/src/hash.ts) and [`index.ts`](../../packages/rules-engine/src/index.ts) **does not export it**, so `0045`'s two `NOT NULL bytea` columns cannot be filled and **not one `simulation_runs` row is writable today**. **[ALLOCATION](../decisions/ALLOCATION.md) attributes `plan_versions_publish_decision_recorded` to `0004` in BOTH the `087` and the `0047` rows and it is `0045`'s**, at `0045:236`, with `0004` carrying no such constraint; session 148 is dispatched against that citation. **[API_CONTRACT](../architecture/API_CONTRACT.md)'s `PublishRequest` is `{ reason, second_approver? }` and cannot carry either SD-M21-02 field, so a publish through the contract as written VIOLATES the merged `CHECK`**: the contract is falsified by a migration that is already on `main`. Seven slices, fenced BY FILE, **two money path**; **`M21-a` blocks session 148's `0047`** on canonicalization rather than on any shared file, and three slices collide with it on `ALLOCATION.md`, `DELTA_MANIFEST.md` and `corpus.yml`. **Session 170's CLAIM ROW LANDED MID-SESSION**, eleven minutes after this one opened, in a commit whose subject says `before dispatch`; main was merged in and the row is struck. 29 of 29 gates, `verify` green at 104 files and 1,512 passed, both unaffected by this session. No code, no migration, no ADR, no ruling |
| [2026-08-24 - Session 171](2026-08-24-session-171.md) | **The fourteen-module measurement wave landed, and eight of the fourteen independently found the SAME SHAPE**: the database was built ahead of the application across the whole estate. M18 *"schema is finished while nothing reads it"*, M05 *"finished while nothing can read it"*, M19 *"complete and unreachable"*, M17 *"landed ahead of everything that reads it"*, M20, M03, M07 and M06 the same. **No single session could have established that.** The wave held its fence exactly: all fourteen touched TWO files, their own log and their own struck claim row, ZERO outside `docs/sessions/`. Merging them individually is O(n squared), so twelve went through one integration branch resolving each collision once and regenerating the ENTRY span once at the end. **[ADR-086](../decisions/ADR-086.md) and [ADR-087](../decisions/ADR-087.md) SIGNED**, both money path, both on clauses RE-EXECUTED here rather than inherited: deleting `db: tx` from the queue's `send` reproduces the clause's own message verbatim, and `0047`'s probe was run on two fresh PostgreSQL 16 clusters, 7 rejections and 5 successes at `0047` and death on `REJECTION 1` at `0046`. **The signature on `0047` rests on the direction it REFUSED**: the digest half stays open because the serializer preserves trailing zeros, so `5.0` against `5` would refuse a LEGITIMATE publish. **SEVEN money-path files are now merged on delegated authority without the `E2` read** |
| [2026-08-24 - Session 172](2026-08-24-session-172.md) | **P4 planned against the tree from the FOURTEEN measurements sessions 157 to 170 wrote, rather than measured from scratch.** [P4-portal-and-site](../plans/P4-portal-and-site.md), `status: draft`. **The shape eight of them found independently reproduces, and the aggregate figure is one no single session could produce**: **98 of 100 `SD-Mnn-nn` rows are `landed`** against **7 of 111 tables in [`schema.ts`](../../packages/db/src/schema.ts)**, an accessor whose whole interface is `__brand`, `identityId` and `rows()`, and **zero routes**. **The phasing consequence is the part no module plan can state**: **ten of the fourteen proposed the SAME first slice** on one pair of files whose registry is total by a `satisfies` clause, so the transcription is a phase-level artifact. **P4's three preconditions are each an artifact `CI-06` PROBES** (a `build` script, `@playwright/test`, `fastify`), so each turns a green gate red on the commit that delivers it, and **`VG-10` is the one conjunctive row that must NOT be expired with `VG-2`**. **Three of P4's four done-clauses name an artifact that does not exist**, and `GS-143` and `GS-144` are blocked under a W2 term their own *"the build fails"* assertions refute, which makes them the two of 266 a fixture-format ruling cannot clear. **AUTH IS IN NO PHASE'S CONTENTS AT ALL** and P4's first stated content contains two money-path screens needing it. **ONE DISAGREEMENT AMONG THE FOURTEEN, NAMED NOT AVERAGED**: [session 160](2026-08-24-session-160.md) blocks a slice on a restore event that [EVENTS:52](../architecture/EVENTS.md) carries and whose own text names M06 as the consumer it was written for; sessions 162 and 165 read it correctly. **A measurement taken HERE was wrong on its first run** and the wider re-run moved a slice. Ten slices, fences BY FILE, collisions BY FILE against 173 and 176. **Claims nothing**: ALLOCATION is outside the fence. 29 of 29, verify green at 106 files / 1,526 passed. No code, no migration, no ADR, nothing signed |
| [2026-08-24 - Session 173](2026-08-24-session-173.md) | **`CI-06/retired-constraints` is built, and the seven were re-derived rather than inherited**: 16 `DROP CONSTRAINT` statements less 9 same-name re-adds is still SEVEN, unmoved by `0047`, which drops nothing. **The gate is seeded from `packages/db/migrations/` and spells no constraint name anywhere**, which forced the register to be keyed by **file and count** rather than by name: a register naming its needles would make [`gates.mjs`](../../scripts/corpus/gates.mjs) a citation site of the thing it polices and move its own number on every edit. **152 sites in the tree, 109 exempted across five WRITTEN permitted classes, 43 registered over 21 files**, and the register **pins the count in both directions**, which is the assertion the gate exists for: three stale citations reached one file before anybody counted, and an entry saying only *"this file is known"* would take the fourth in silence. **Two parser findings, both of which fail in the direction that passes**: [`0032`](../../packages/db/migrations/0032_trading_calendar_holidays_coverage_revisions.sql) carries the words `ADD CONSTRAINT below` in a COMMENT, which un-retires a name and shrinks the needle list in silence unless comments are stripped; and BOTH edges of the anchored match are load-bearing in this tree rather than hypothetical, because `0037` names its replacement by SUFFIXING the retired name and every supersession migration's own FILENAME embeds the name it retires. **Watched failing five ways and correctly silent in two, each PASS case with its own control**, including the vacuity throw the brief required. **[`data-model/README.md:376`](../architecture/data-model/README.md) is still wrong and is now COUNTED** |
| [2026-08-24 - Session 174](2026-08-24-session-174.md) | **[ADR-090](../decisions/ADR-090.md) rules M08's affiliate rail. A RULING ONLY: no migration, no `packages/db` change, no engine change, no gate, and no migration number claimed.** All seven clauses of [session 162](2026-08-24-session-162.md)'s finding were re-derived at their source and all seven reproduce. **[ADR-017](../decisions/ADR-017.md)'s TITLE says *"one rail and one transfer table"* and its DECISION LINE says *"one rail, one destination table, one detector"*, and the two are different claims**; session 162 quoted the title and could not determine which governs. **Ruled: the decision line. One rail binds on the DESTINATION and the DETECTOR and not on the transfer table.** **The already-ruled half is named by [M08](../plans/M08-affiliate-system.md) section 3.4's own evidence column**, whose three-row premise table PERFORMED rows one and three against the tree and ACCEPTED row two from four documents and not one migration line; rows one and three are the authorization and `INV-M8-12` is their remedy, row two is the mechanism and it is false. **The transfer table stopped being one table at [`0011`](../../packages/db/migrations/0011_wallet.sql) and it was RULED rather than allowed**: `wallet_withdrawals` carries its own `destination_ref text NOT NULL` and no FK to either payout table, so **`payout_transfers.payout_request_id` is NOT relaxed** and the affiliate leg is `SD-M5-06`'s precedent taken a second time. **ONE CLAUSE OF SESSION 162 IS CORRECTED**: `paid_transfer_ref` is not *"the second spelling of the destination"*, it is not a destination at all, and it matters because [`0010:290`](../../packages/db/migrations/0010_payouts.sql) declares a transfer identifier UNIQUE per row, so a `D-09` grouped on one returns **NO CONCENTRATION ON EVERY INPUT**, which is `AS-M8-05` succeeding with a detector watching. **`INV-M20-09` is answered directly**: no predicate moves, `OQ-075-01` is untouched, the only friction is a delay with a clock the founder already signed, and the tree as merged withholds 100 percent of earned affiliate commission because no settlement path exists. **Three frozen documents are amended by the ruling and NOT edited**, all outside the fence. **A pre-existing defect is REPORTED and not repaired: the `gen:adr_count` line is DUPLICATED VERBATIM in both STATE and INDEX**, from merge `4cc6006`, and **`CI-06g` cannot see it** because it rewrites every span it finds and both copies pass. 29 of 29 gates, 7 of 7 invariants, 106 files / 1,526 passed / 1 skipped, `falsify` clean at 62 scope and 10 loader cases, all unaffected by this session, which writes no code |
| [2026-08-24 - Session 175](2026-08-24-session-175.md) | **[ADR-091](../decisions/ADR-091.md) rules [session 164](2026-08-24-session-164.md)'s M13 grants contradiction. A RULING ONLY: no migration, `0048` NOT claimed, `packages/db` untouched.** The finding was re-derived on a **PostgreSQL 16.13 cluster carrying all 47 migrations**, not read off the file: `merit_analytics` reads **10 of 111 tables** and `rule_states` is not one, so `INV-M13-01`'s named source is unreadable by the role that implements it. **SESSION 164's MECHANISM IS REFUTED WHILE ITS CONCLUSION STANDS**: a second database built with [`0026:157`](../../packages/db/migrations/0026_roles_and_grants.sql) **DELETED** reports the same `false`, so the revoke is **declaratory** and the cause is the omission from the enumerated `GRANT` at `:127-137` -- **the remedy is an ADDITION, and a later `GRANT` was proven to take effect.** **THE HALF NOBODY HAD FOUND**: `rule_states.engine_gates` carries **EIGHT PLAN THRESHOLDS** ([`hash.ts:421-476`](../../packages/rules-engine/src/hash.ts)), so `INV-M13-01`'s *"no access to plan config at all"* and `AS-M13-01`'s *"the thresholds are unreachable"* were **never jointly satisfiable with `AN-M13-04`**. **The revoke's own reason is self-defeating**: `AS-M13-01`'s attack IS what happens when `rule_states` is hard to reach, so denying it deletes the counter and keeps the demand. The alternative *"the invariant is wrong"* is priced and loses on its own terms: `floor_cents` and `withdrawable_cents` live **only** in `rule_states`, so it also costs `AN-M13-01`'s floor. **NOTHING IN THE REPOSITORY CHECKS ANY GRANT**: fifteen probes in [`scripts/db/`](../../scripts/db) and not one reads a privilege catalogue; `VG-8`'s grants leg is the **append-only set** and a different subject. **THE PREDICTED COLLISION WITH SESSION 174 HAPPENED AND ONLY THE MERGE COULD RESOLVE IT**: `#223` took `090` first, so **this entry is [ADR-091](../decisions/ADR-091.md)**. The early renumber was attempted and reverted at **27 of 29**, because `CI-06f` reads gaplessness over allocated **plus reserved** and a vacated number reserves nothing. **A RENUMBER'S BLAST RADIUS IS VISIBLE TO NO GATE HERE, AND THIS LOG CLAIMED OTHERWISE FOR ONE COMMIT**: once `090` was occupied, nineteen stale citations **resolved to the wrong entry** and `CI-06a` passed at **30 of 30 with every one wrong**. It catches a link to a MISSING file, never a WRONG one, so [ADR-089](../decisions/ADR-089.md)'s five stale `INFRA` citations, which pointed at an `ADR-088` that exists, were never a gate left unrun either. **29 of 29, 7 of 7, 106 files / 1,526 passed / 1 skipped, `verify` exit 0.** |
| [2026-08-24 - Session 176](2026-08-24-session-176.md) | **[ADR-089](../decisions/ADR-089.md)'s six `portal-api` code sites retired, and the count REPRODUCES: six under `apps/`, `packages/`, `scripts/` and `.github/`, none outside `apps/`**, so the third conjunct of that entry's checkable clause holds when re-measured rather than inherited. **The definition becomes `portal`**, which is INFRA section 2.1's name and the name every sibling deployable already follows. **TWO OF THE SIX KEEP THE WORD ON PURPOSE**: both are comments, and a comment that records why a fused name was retired is the record ADR-089 exists to leave rather than a site to erase. **THE FINDING IS A SEVENTH SITE THAT CARRIES THE STALENESS WITHOUT CARRYING THE STRING**: [`apps/api/src/surface.ts:92`](../../apps/api/src/surface.ts) read *"INFRA section 2's table names four services and none of them is this one"*, which ADR-089 falsified by rowing both of them in section 2.1 -- in fence, repaired, and a `grep` for `portal-api` would never have reached it. **Every edit is LINE-COUNT NEUTRAL**, so all six `file:line` citations in ADR-089 section 5 still resolve to the site they name, and [`surface.ts:99`](../../apps/api/src/surface.ts) and `:168`, the two lines that entry's SIGNED checkable clause quotes, are still those lines. **Test count identical before and after, 106 files at 1,526 passed and 1 skipped**: a rename that changed a count would have changed behaviour. Three more stale-under-ADR-089 sites reported and left, all outside this fence |
| [2026-08-24 - Session 177](2026-08-24-session-177.md) | **Two one-line repairs were dispatched and only ONE was still needed.** [`pnpm-workspace.yaml`](../../pnpm-workspace.yaml)'s *"0 transitive packages against `pg`'s 6"* was repaired by [session 147](2026-08-24-session-147.md) under [ADR-086](../decisions/ADR-086.md) section 5 on 2026-08-24, and the dispatch that sent this session at it was written from [session 155](2026-08-24-session-155.md)'s open-items list, which was true when written and stale by the time it was read. **The closure was re-measured from `pnpm-lock.yaml` rather than copied from either number**: 14 packages, 13 transitive, 6 direct, reproducing [ADR-084](../decisions/ADR-084.md) section 2 exactly and confirming the `6` was `pg`'s DIRECT count. [STATE](../STATE.md)'s `VG-11` assignment was real and is repaired: [ADR-073](../decisions/ADR-073.md) section 7 struck `VG-11` from `CI-07` on 2026-08-20 and the P1 table carried the losing assignment for four days, named by ADR-073:142, named again by [ADR-080:193](../decisions/ADR-080.md), and repaired by neither because both fences were append-only on that file. **The fence discipline is what cost the four days and it is still the right trade.** One finding recorded and not repaired: **ADR-073:157's arithmetic contradicts ADR-073:142** inside one signed entry, on which side of the running-stage line `VG-11` falls. `CI-06/vg-inventory` does not read the repaired row: it reads STRATEGY section 4.2, which was already correct. 29 of 29 gates before and after |
| [2026-08-24 - Session 178](2026-08-24-session-178.md) | **Six sessions landed and merged**: P4 planned, `CI-06/retired-constraints` built, two money-path defects RULED without migrations, the `portal-api` sites retired and two stale one-liners repaired. **P4's aggregate is the number the fourteen-module wave existed to produce: 98 of 100 schema deltas landed against 7 of 111 tables reachable from the application.** `gate_count` 29 to **30**. **[ADR-090](../decisions/ADR-090.md) SIGNED** on both conjuncts executed: `destination_ref` has exactly two declaring sites, both `text NOT NULL`, and no affiliate destination column exists. **[ADR-091](../decisions/ADR-091.md) is DELIBERATELY LEFT UNSIGNED**, and that is the finding: its clause is executable SQL that is **false on purpose today**, an acceptance test for a migration nobody has written, so there is nothing to execute and a signature would be claiming a check that did not happen. **Sessions 174 and 175 both claimed ADR-090 and wrote entirely different rulings under it** -- the fifth number collision of the day -- resolved by 174 keeping it and 175 renumbering to 091 across every citing file, not just the two carrying it |
| [2026-08-24 - Session 179](2026-08-24-session-179.md) | **[ADR-092](../decisions/ADR-092.md) rules [P4](../plans/P4-portal-and-site.md) section 10 item 1: who owns [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts) across phases. A RULING ONLY: no transcription, no table added, and `git status --porcelain packages/db` reports ZERO.** **THE FINDING IS THAT ALL THREE READINGS P4 NAMES PERMIT EXACTLY ONE CONCURRENT TRANSCRIBING SESSION**, because all three treat the pair as a resource to be held; P4 asks which reading is best and the readings do not differ on the axis it names. **Ruled: reading 3, ONCE PER MODULE, with both of P4's undefined terms defined because each was doing the serialising -- the OWNER is the TABLE and not the module (first writer wins, the registration is never re-argued), and the QUEUE is the TYPE CHECKER and not a document.** As ruled the ceiling is **N, bounded by table-set disjointness rather than by the file**. **THE OWNER CANNOT BE THE MODULE AND IT IS MEASURED, by an aggregate no measurement session could compute because each was blind to thirteen others: over 47 migrations and 22 `M*.md` plans, 54 of 111 tables are named by MORE THAN ONE module plan, `accounts` by 21 of 22 and `identities` by 16, and FOUR are named by none.** **The collision already happened inside the wave**: [session 159](2026-08-24-session-159.md)'s `M5-1` and [session 169](2026-08-24-session-169.md)'s `M20-a` claim the **same four wallet tables**, 159 wrote it down and 169 declared the files *"M20-a's alone"*. **FOUR CONCURRENT-MERGE HAZARDS WERE SEEDED AND WATCHED IN A THROWAWAY WORKTREE rather than predicted**: `TS2451`, `TS1117`, `TS2345` naming `ScopedTableKey`, and a loud textual conflict in all three files -- **so P4 section 9's `pnpm-lock.yaml` analogy is false in the half that matters**, because a lockfile keep-both merge is a plausible wrong file nothing checks and every keep-both merge here is a compile error. **ONE HAZARD IS SILENT AND IT IS THE ENTIRE COST**: [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) states one table-to-SQL-name map **twice**, `SQL_NAME` at `:256` total by a mapped type and `DDL_NAMES` at `:394` a bare array that is not, so deleting one pair leaves `tsc` at 0 and the suite **GREEN AT 25 TESTS AGAINST A BASELINE OF 26** -- the drift assertion disappears, the count goes DOWN, and nothing reports it. **[ADR-084](../decisions/ADR-084.md)'s mapped-type property is preserved and STRENGTHENED**, and the entry states why a total `satisfies` clause is a merge-time VERIFIER rather than what serialises the pair. **Four amendments to P4 are named and deliberately NOT made**, P4 being outside the fence, among them that `P4-a` and `P4-b` may no longer need to be strictly serial. 30 of 30 gates, `verify` exit 0 |
| [2026-08-24 - Session 180](2026-08-24-session-180.md) | **[ADR-093](../decisions/ADR-093.md), `status: proposed`, unsigned. [P4](../plans/P4-portal-and-site.md) section 10 item 3 ruled: of the three readings P4 names, reading 3. AUTH IS P3'S.** All four clauses of P4 section 4.4 re-derived at source and **all four hold**: seven auth headings from [API_CONTRACT:80](../architecture/API_CONTRACT.md), `SD-M4-04` landed at [`0029:565`](../../packages/db/migrations/0029_phone_identity_and_auth.sql), the auth grep returning **five files and not one an implementation**, and no phase's contents naming it. **THE GROUND IS NOT THE ONE P4 OFFERED.** Not that P3 carries M19, which is the weakest of the three, but that **P3's OWN stated contents contain endpoints the contract marks `Auth: session`**: `POST /checkout` at [API_CONTRACT:285](../architecture/API_CONTRACT.md), `POST /kyc/session` at `:488`, and `/affiliate/*` 401 at `:677`. **Section 12 is headed *"required in CI"* and requires every row to exist BEFORE the endpoint ships**, `VG-5` is its subject and `CI-06k` reads it, **so P3 cannot ship what it is already committed to.** Readings 1 and 2 therefore both place auth **after** the phase that needs it, and reading 2 additionally serializes **three P4 contents that need no session at all** (`GET /plans` `Auth: none`, the M12 batch through `systemDb('nightly-batch')`, `GET /public/methods/:statCode` marked **NEW, public**). **[STATE](../STATE.md)'s recorded *"KYC is not auth, so P3 does not carry it"* has a true premise and a conclusion that does not follow**: the premise is about M19 and the conclusion about P3, and [`0002_identity.sql:11`](../../packages/db/migrations/0002_identity.sql) already calls `passkeys`, `otp_challenges` and `sessions` *"THE AUTHENTICATION SURFACE ... the whole of it"* inside the migration named **identity**, which is P3's own phase title. **`SC-M4-01` and `SC-M4-11` STAY IN P4**: the screens were never homeless, only the issuer they post to, and P4 section 4.4's framing hides that. **P4's dispatchability is unchanged, zero dispatched slices touched.** **A FABRICATED QUOTATION WITH THREE CITING SITES AND NO SOURCE**: `grep -c "rewrite"` over [session 136](2026-08-22-session-136.md) returns **0**, and the sentence P3 section 3, P4 section 4.4 and this session's own dispatch attribute to it is not in it; the precedent is real and correctly applied, only the quotation marks are wrong, and `CI-06a` cannot see it because the link resolves. **30 of 30 gates**, `verify` exit 0 at 7 of 7 invariants and 106 files / 1,526 passed / 1 skipped, none of it moved by this session. No code, no route, no migration, no gate, no migration number, and DELIVERY_PLAN not edited |
| [2026-08-24 - Session 181](2026-08-24-session-181.md) | **[ADR-092](../decisions/ADR-092.md) and [ADR-093](../decisions/ADR-093.md) both SIGNED**, and with them [P4](../plans/P4-portal-and-site.md) section 12's bar lifts: section 10 items 1 and 3 are answered. **BOTH CLAUSES WERE EXECUTED BEFORE SIGNING AND ONE OF THEM DID NOT HOLD AS WRITTEN.** ADR-092's reproduced exactly in the direction it says it fails in: deleting one pair from `DDL_NAMES` leaves `pnpm run typecheck` at **exit 0 with zero `error TS`** and the suite **GREEN at 25 tests against a baseline of 26**, silently, while the loud hazard beside it gives **`TS1117`** -- so a registered and scoped table can lose its drift assertion and the test count goes DOWN with nothing reporting it. **ADR-093's half-executed**: `sed -n '285p;488p'` returns two `Auth: session` lines as claimed, but the cell it calls on names `M19` by identifier and `M3` by its TITLE WORDS ONLY, and **the string `M03` occurs nowhere in [DELIVERY_PLAN](../DELIVERY_PLAN.md)**. The ruling is untouched and the enumeration was amended before the signature, which is [ADR-077](../decisions/ADR-077.md) section 5.3's case exactly. **THE CONSEQUENCE FOR THE WAVE IS THE HEADLINE: ADR-092's finding was that all three readings P4 named permit exactly ONE concurrent transcribing session, and its ruling replaces that ceiling with N bounded by table-set disjointness**, so wave 1 is dispatched as **SIX file-disjoint sessions** rather than P4's two serial pairs, and the two section 10 items that were left blocking `P4-e` and `P4-j` are dispatched as rulings of their own rather than waited on |
| [2026-08-24 - Session 182](2026-08-24-session-182.md) | **[ADR-094](../decisions/ADR-094.md), `status: proposed`, approval line UNSIGNED, plus [ADR-092](../decisions/ADR-092.md) section 5's precondition INSTALLED AND WATCHED FAILING.** **RULED: a transcription reads a table AS OF THE LAST MIGRATION and never as of its `CREATE TABLE`, and the drift assertion stops reading one statement and starts REPLAYING the migration set.** **The fold's vocabulary is CLOSED AT ONE MEMBER and its default is FAIL**: `ADD COLUMN` is folded, and `DROP COLUMN`, `ALTER COLUMN` and `RENAME` stay offenders that turn the suite red, so [`schema.ts`](../../packages/db/src/schema.ts)'s refusal is NARROWED rather than deleted and no table is ever registered by being excused from the assertion. **MEASURED FIRST AND RULED SECOND, and the classification is what decides it**: over 47 migrations, **12 of 111 tables carry a later column change -- `ADD COLUMN` on 11, `ALTER COLUMN` on 2, `DROP COLUMN` on ZERO and `RENAME` on ZERO** -- so ten of the twelve are `ADD COLUMN` and nothing else and P4's three are all in that ten. **`ALTER COLUMN` is refused although it CANNOT change a column-name set**, and the entry says so rather than letting a later reader mistake it for a parsing limit: the axis it moves is type and nullability, which this suite has never compared on any registered table, so exempting it would register a table verified on every axis except the one that changed. **FORECLOSED: a per-table exemption list, a comment in place of a check, folding a destructive change while zero instances exist to rule against, and widening the fold by adding a regex.** **THE REGISTRATION IS FOUR TABLES AND NOT THREE, AND THE TYPE CHECKER IS WHAT SAYS SO**: `sessions.user_id` references `users(id)` and not `identities(id)`, so `sessions` reaches an identity only through `users` and `DerivedRule.via` is typed `TableKey`. **BOTH OF ADR-092's DIRECTIONS RE-EXECUTED ON THIS TREE RATHER THAN INHERITED** -- before, deleting one pair leaves typecheck at **exit 0 with zero `error TS`** and the suite **GREEN at 25 against 26**; after, the same deletion is **`TS2741` at `scoped-db.test.ts(64,7)`, typecheck exit 2** -- and **the precondition then fired FOR REAL, unseeded**, refusing the four-table registration until the drift assertions covered it. **ADR-094's own clause executed both directions**: fold vacuous AND the same seven later columns deleted from the transcription leaves **every per-table comparison GREEN and only `the fold is not vacuous` RED**, which is the silent direction watched happening. 26 tests before, **31 after**; `verify` exit 0 at 7 of 7 invariants and 106 files / 1,531 passed / 1 skipped; 30 of 30 gates; `falsify:ci CI-01` **12 of 12** with the firm refusal re-watched over a THIRD firm table |
| [2026-08-24 - Session 183](2026-08-24-session-183.md) | **[ADR-095](../decisions/ADR-095.md), `status: proposed`, unsigned. [P4](../plans/P4-portal-and-site.md) `P4-c`: Next.js `16.3.2` App Router on React `19.2.8`, in `apps/portal` and `apps/site` and in no other package**, five catalog entries as ONE `VG-12` admission whose human leg is the merge rather than the catalog line. **P4 section 4.3 held on re-derivation**: `grep -rlniE 'next\.js|react' docs/decisions/*.md` returns [ADR-083](../decisions/ADR-083.md), whose only occurrence is a **rejected alternative for the API**, against two `approved` plans naming a framework at [M04:25](../plans/M04-trader-portal.md) and [M09:23](../plans/M09-marketing-site.md). **THE ENTRY'S SUBJECT IS WHAT IT FORECLOSES**, per rule 8: seven, and three land on controls this corpus leans on -- `INV-M9-04`'s revalidation is only sound at one Railway replica and no document says how many there are; a server component may import `packages/db` directly and `merit/no-raw-db-client` bans a RAW client rather than `scopedDb`; React is now every Merit UI for years. **`CI-07`'s CELL MOVED IN THE SAME COMMIT AS THE `build` SCRIPT** and takes a new dated condition rather than an implementation, because an implemented leg names a job in [`ci.yml`](../../.github/workflows/ci.yml) and that file is `P4-i`'s fence; the new artifact is **a `page`, `layout` or `route` file under `apps/*/src/app/`**, a PATH where four of six conditions still give neither, absent for a reason that was EXECUTED: `next build` exits **1** on *"Couldn't find any `pages` or `app` directory"*. **`VG-2` KEEPS ITS CHAIN** because `CI-07` acquires no implemented leg and [ADR-080](../decisions/ADR-080.md) rules this exact day in its own words, and **`VG-10` IS NOT TOUCHED**, twice over: same unexpired chain, and its legs are the table's only CONJUNCTIVE pair with no bucket in the tree. **THE COMMIT EXPOSED A DEFECT IN A DIFFERENT ROW'S PROBE**: `next@16.3.2` declares `@playwright/test` as an OPTIONAL PEER, pnpm writes peer blocks into the lockfile verbatim, and `CI-08`'s probe read a declaration as an install, failing the gate at **29 of 30** on an artifact that is genuinely absent. The probe was anchored on the NAME and never on the CONTEXT; it reads an entry KEY now, `CI-08`'s row is untouched, and **the pre-existing near-miss case was found VACUOUS** because both its seeds wrote a shape pnpm's lockfile has nowhere. **[ADR-083](../decisions/ADR-083.md)'s no-build constraint rested on the probe this entry moved and ADR-083 SAYS SO** (*"unavailable while that gate stands as written"*); the ruling survives on independent grounds and the mechanism does not. Closure **179 -> 230, +51**, 36 per-platform binaries, fifteen packages the real admission, `pnpm audit` clean. **30 of 30 gates, `verify` exit 0, 8 of 8 invariants, 106 files / 1,611 passed / 1 skipped, falsify clean-and-dirty over all 30 with 69 scope cases** |
| [2026-08-24 - Session 184](2026-08-24-session-184.md) | **[ADR-092](../decisions/ADR-092.md)'s four named [P4](../plans/P4-portal-and-site.md) amendments and [ADR-093](../decisions/ADR-093.md)'s two [DELIVERY_PLAN](../DELIVERY_PLAN.md) cell edits are APPLIED. ALL SIX APPLIED CLEANLY, none repaired by judgement**, every anchor matching exactly once against the file as it stands. **DELIVERY_PLAN is FROZEN and both its edits landed on line 127 and nowhere else**: P3's Contents cell gains the authentication surface and its Definition of done gains [API_CONTRACT section 12](../architecture/API_CONTRACT.md)'s `C-27` rows in BOTH directions, **including the third clause ADR-093 predicts a hurried transcription will drop** -- `GET /sessions` and `GET /phone/change` returning **200** from a single-factor session, without which a done-condition passes on an implementation that refuses everything. **THE LARGEST P4 AMENDMENT CARRIES ITS PRECONDITION RATHER THAN ASSUMING IT DISCHARGED**: `P4-a` and `P4-b` are concurrent only once [ADR-092](../decisions/ADR-092.md) section 5's `DDL_NAMES` line lands, and it has not -- `DDL_NAMES` is still `ReadonlyArray<readonly [TableKey, string]>` at [`scoped-db.test.ts:394`](../../packages/db/test/scoped-db.test.ts), read on this branch, with session 182 landing the fix CONCURRENTLY -- so the amendment reads *strictly serial until it lands, concurrent after* at all four P4 sites that asserted the old serialization. The other three: section 9's row takes ADR-092 section 2's answer (**the owner is the TABLE, the queue is the TYPE CHECKER**), section 6's estate-wide row is superseded in its second clause (the other 91 belong to the **modules**, registered per table, first writer wins), and section 10 item 1 is answered with its dispatch consequence, **ten of ten measured transcription slices concurrent** with one intersecting pair rebasing four rows out. **One judgement is recorded rather than made silently**: DELIVERY_PLAN's `last_updated` frontmatter still reads `2026-08-20` and was deliberately left, on ADR-093 section 4's *"Nothing else in DELIVERY_PLAN moves"* |
| [2026-08-24 - Session 185](2026-08-24-session-185.md) | **[ADR-096](../decisions/ADR-096.md), `status: proposed`, unsigned. [P4](../plans/P4-portal-and-site.md) section 10 item 6 ruled: of the three readings P4 named, **reading 3, and what does not exist yet is NOT in `packages/db`.** `apps/site` reads over HTTP against the public API, declares no dependency reaching `@merit/db`, and `SystemReason` stays at two members. **THE FINDING P4 DID NOT HAVE IS THAT READINGS 1 AND 2 REACH NO TABLE THE SITE NEEDS**: `systemDb(...).rows()` is generic over `TableKey` ([`scoped-db.ts:155`](../../packages/db/src/scoped-db.ts)), `TableKey` is exactly the seven keys of `TABLES` ([`scope.ts:40-48`](../../packages/db/src/scope.ts)), and **none of the eight tables [M09:66](../plans/M09-marketing-site.md) names has a rule in `scope.ts`** though all eight exist in the migration set. **So a third `SystemReason` member is a NO-OP, watched being one**: `tsc` emits the vocabulary error a third member would delete and, **on the same call twenty-three columns later**, a second `TS2345` printing the seven `TableKey` members back verbatim; reading 2 is `TS2554` before a brand is consulted and `TS2345` on `ScopedTableKey` once the identity is cast away. **[ADR-084](../decisions/ADR-084.md)'s brand disjointness is PRESERVED, spent nowhere and DELIBERATELY NOT RELIED UPON**, in its own section: the site's refusal is module resolution one level above the type system, and **a control that never has to fire is recorded as a cost rather than claimed as a strengthening.** **THE HOLE IS MEASURED AND IT IS GREEN IN FOUR PLACES**: `"@merit/db": "workspace:*"` in [`apps/site/package.json`](../../apps/site/package.json) plus a **firm-wide unscoped read of `accounts` on the marketing origin** leaves `tsc` at **0**, `eslint apps packages` at **0**, `check:invariants` at **7 of 7** and gates at **30 of 30**; `merit/no-raw-db-client` does not fire and is not supposed to. **The direction it fails in is SILENCE and it fails OPEN**, remedy named and not written (`RI-08`, on `RI-01`'s shape). **THERE IS NO ROLE A SITE READER COULD HOLD THAT IS NOT `merit_app`**: [`0026`](../../packages/db/migrations/0026_roles_and_grants.sql) creates three, `merit_analytics` reads ten tables and none of the eight under its own *"THE RULEBOOK IS NOT READABLE BY ANALYTICS"*, so the remaining role is write-capable and `INV-M9-10`'s *"nothing to steal"* stops being true by construction. **THREE OF THE FIVE ENDPOINTS M9 CONSUMES ARE IN NO CONTRACT** (`grep -rn` over `docs/architecture/` returns **zero**) **and a FOURTH READ IS IN NO DOCUMENT AT ALL**: `readRestrictedCountries()` has one site in the tree, its own declaration. **30 of 30 gates**, `verify` exit 0 at 7 of 7 invariants and 106 files / 1,526 passed / 1 skipped, none of it moved by this session. No adapter, no `packages/db` change, no route |
| [2026-08-24 - Session 186](2026-08-24-session-186.md) | **[ADR-097](../decisions/ADR-097.md) rules [P4](../plans/P4-portal-and-site.md) section 10 item 4 and it is OUTCOME 1 of the three: the vocabulary ALREADY HAS the term, it is `outside-loader-boundary`, and `GS-143` and `GS-144` are MIS-FILED. [ADR-072](../decisions/ADR-072.md) is NOT amended and the closed six stay closed at six.** **`no-fixture-format` is wrong on both rows by ADR-072's OWN tie-break, run in both directions rather than read**: clearing W2 with a fixture format for M9 leaves both rows exactly where they are, and clearing W1 discharges both to `covered-elsewhere` under [ADR-076](../decisions/ADR-076.md) section 1 **without a fixture ever being written**, so the term names the half that never binds. **THE WORD "exists" IN THE TERM'S ROW-CONDITION IS A GUARD AND NOT A CONDITION, AND [ADR-048](../decisions/ADR-048.md) IS WHY**: it guards against the `no-module-code` ADR-072 refused BY NAME for *"the 229 rows it would have covered"*, and those rows are refused it because a fixture citing an undeclared rule loads, derives inverted and flips on its own -- **while a row whose expectation is a tool's refusal cites no rule, loads nothing and has no polarity**, so ADR-048 never reached these two and the artifact's absence is not the half that clears first but the only half there is. **A seventh term is refused on [ADR-041](../decisions/ADR-041.md)'s argument rather than on "closed is closed"**, the extent enumerated once as `B1` and `B2`, and the property that separates the terms without reading either definition is the **exit door: a `no-fixture-format` row exits to `written`; an `outside-loader-boundary` row exits to `covered-elsewhere`, never to `written`**, demonstrated already by `GS-071`. **OUTCOME 3 IS RED AND WAS EXECUTED**: both rows deleted, `CI-06/fixture-inventory` fails naming both ids plus two drifted counts, reverted. **THE TWO ROWS ARE NOT ALIKE AND NO UPSTREAM DOCUMENT SAYS SO**: `GS-144` has two tests naming it that PASS today at **21 passed** against a `GS-143` that has none, which satisfies both of section 39's literal `covered-elsewhere` conditions while the row reads `blocked` -- and it still does not move, because its second leg is the OG image path and `grep -rniE "og.image|opengraph"` returns **four sites, all comments**. **That is a FOURTH member of section 39's list of things that look like coverage, and the only one that executes and passes**, asserting a narrower mechanism than the row. **P4 section 5.3's two figures both move**: *"264 others"* is the BLOCKED set and not the cohort, which is **229**, so the true figure is **227**, a 37-row drift larger than four of the six blocker counts; and *"these two"* is a **FLOOR OF FIVE**, `GS-147`, `GS-175` and `GS-195` carrying the same shape, measured over 171 of 229 with the needle stated and named rather than moved. **`P4-j` mints no ADR for the term and its section 39 fence is TWO CELLS TOO NARROW to execute its own edit**, established by running the gate rather than predicting it. **Section 39 is NOT edited**, `git status --porcelain docs/testing` reporting zero; 30 of 30 gates, `verify` exit 0 at 106 files / 1,526 passed. Nothing signed |
| [2026-08-24 - Session 187](2026-08-24-session-187.md) | **[P4](../plans/P4-portal-and-site.md) section 10 item 2 MEASURED, no gate written, no row repaired.** All 22 `docs/plans/M*.md` reconciled against [API_CONTRACT](../architecture/API_CONTRACT.md) **by executing a script at `a0c7916`**, quoted in full with its output in [the review document](../reviews/2026-08-24-endpoint-table-reconciliation.md). **73 endpoints named by a plan and absent from the contract, 23 carried by the contract and named by NO plan, 34 agreed out of a union of 130: the gate is RED ON 74% OF THE SURFACE the day it is written**, and the aggregate is 94 to 98 across four defensible readings, 96 at both the narrowest and the widest. **THE SIX PRIOR SESSIONS DID NOT DISAGREE ABOUT THE TREE, THEY DISAGREED ABOUT THE UNIT, AND THE CORPUS DEFINES NONE**: 124 parsed rows carry **134 endpoint references** and 7 of 20 tables have a row naming more than one, so [159](2026-08-24-session-159.md)'s *"four"* needs a human to read `` `and /enforce` `` as an endpoint, [165](2026-08-24-session-165.md)'s *"six"* and [166](2026-08-24-session-166.md)'s *"five"* are ROW counts against endpoint counts of 7 and 7, and [164](2026-08-24-session-164.md)'s *"six"* is a third unit against 10 references and 8 absences. **24 disagreements in five kinds, and TWO OF THE FOUR AXES THE DISPATCH NAMED ARE EMPTY ON THIS TREE**: `Auth:` occurs **0** times across all 22 plans and 2 plan rows in the whole corpus name a status code, while **11** are parameter spellings (`:id` in the older plans against a typed name in the contract), **0** are method conflicts, **2** are **API_CONTRACT DISAGREEING WITH ITSELF** on `POST /accounts/:id/payout` and `/reset` between its own sections 5, 6 and 11, **2** are M05 and M20 marking the same two wallet endpoints `NEW` and not-`NEW`, and **9** are endpoints named by 2+ plans that NO plan claims to own because the role column is 103 tokens against 31 cells of free prose; **zero endpoints have two claimed owners**. **13 of the 23 contract orphans are the auth surface, which corroborates [ADR-093](../decisions/ADR-093.md) from a direction it did not use.** **71 of the 76 plan-side absences are self-declared `**NEW**` and the marker is DEFINED FOR EVENTS AND FOR NO ENDPOINT TABLE ANYWHERE**; the 5 not-`NEW` rows are the sharper object, two of them cited by M14 as owned by M17 and M18, **neither of whom names them**. **5 rows could not be parsed and every one is a finding**, plus [M07](../plans/M07-risk-abuse.md) with no table and [M12-statistic-definitions](../plans/M12-statistic-definitions.md) with no section 4, so **20 of 22 plans measured by table and the other two named rather than dropped**. **The dispatch's own INDEX clause is false for a `docs/reviews/` file** ([`gates.mjs:167`](../../scripts/corpus/gates.mjs)) and the fence was narrowed rather than widened. 30 of 30 gates, `verify` exit 0 at 7 of 7 invariants and 106 files / 1,526 passed / 1 skipped, none of it moved by this session |
| [2026-08-25 - Session 188](2026-08-25-session-188.md) | **[P4](../plans/P4-portal-and-site.md) section 8 slice `P4-b`. TEN OF TEN, and the honest report is that the number is ten rather than the eleven [session 182](2026-08-24-session-182.md)'s shape predicts**: the registry's totality DID reach past the slice, and what it reached for -- `accounts`, under `daily_marks`' `DerivedRule.via` -- was already registered, which is [ADR-092](../decisions/ADR-092.md)'s *"a session cannot know its own slice size before it runs"* arriving in the direction that makes a slice SMALLER. **No ADR, no migration, no new document.** `plan_version_sizes`, `purchases`, `daily_marks`, `content_documents`, `page_revalidations`, `certificates`, `statistic_definitions`, `published_statistics`, `proof_links` and `review_requests` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **11 of 111 becomes 21 of 111**, 9 of the 21 firm and 12 reachable through the scoped accessor. **[P4 section 8](../plans/P4-portal-and-site.md)'s measurement was RE-DERIVED rather than inherited and it HOLDS**: over all 47 migrations with the drift assertion's own multiline `ALTER TABLE ... ;` algorithm, twelve tables carry a later column change -- **`ADD COLUMN` on 11, `ALTER COLUMN` on 2, `DROP COLUMN` on ZERO, `RENAME` on ZERO** -- reproducing [ADR-094](../decisions/ADR-094.md) section 1 table for table, and **none of the ten is among the twelve**, so the fold applies NOTHING to any of them and each comparison is byte-identical to a `CREATE TABLE` reading. **THE FINDING IS A GAP THE TENTH TABLE MADE PLAUSIBLE RATHER THAN A DRIFT**: `DerivedRule.via` is typed `TableKey`, which includes every FIRM key by construction, so a derived rule whose chain reaches a firm table compiles at every call site, is a member of `ScopedTableKey`, and **THROWS the first time anybody reads it** -- watched, with `plan_version_sizes` declared `derived` through `plan_versions` leaving the whole suite **GREEN at 41** while `scopePredicate` raised `planVersions belongs to no identity` when executed directly. `plan_version_sizes.plan_version_id` is a `NOT NULL` foreign key that reads exactly like the `hop` `daily_marks` and `rule_states` make to `accounts` and is not one, so **the reason is now a bounded check in the suite rather than only a sentence in `scope.ts`** ([ADR-042](../decisions/ADR-042.md)), and a seeded `daily_marks <-> rule_states` cycle is reported by name in 6ms instead of exhausting the stack. **[Session 145](2026-08-23-session-145.md)'s SURVIVING SEED WAS RE-RUN ON A NEW TABLE AND IS CAUGHT**: `purchases` scoped by `user_id` instead of `identity_id` fails naming the column and quoting its DDL. **THE TYPE AND NULLABILITY AXIS IS STILL ASSERTED NOWHERE and this session did not close it**: three new `owned` rules carry `nullable: false` that nothing checks, on [ADR-094](../decisions/ADR-094.md) section 3's reasoning that the comparison is its own session. **`certificates` CARRIES TWO PATHS TO AN IDENTITY AND NO CONSTRAINT TIES THEM** -- `identity_id` direct and `account_id` one hop out -- named rather than repaired, a repair being a migration. 31 tests to **42**, suite total 1,531 to **1,542**; `verify` exit 0 at 7 of 7 invariants and 106 files / 1,542 passed / 1 skipped; 30 of 30 gates; `falsify:ci CI-01` **12 of 12** with the firm refusal re-watched over nine firm tables |
| [2026-08-25 - Session 189](2026-08-25-session-189.md) | **[ADR-098](../decisions/ADR-098.md) proposed, `status: proposed`, approval line UNSIGNED, and `RI-09` written in the same branch.** **RULED: the operator path's absence from every UI deployable is a PROPERTY of this repository and not an assumption that has held because nobody wrote the file.** **THE FINDING WAS RE-DERIVED ON `origin/main` AT `38e420b` WITH NO FRAMEWORK ANYWHERE IN THE WORKSPACE**, in a throwaway `git worktree`, because [ADR-095](../decisions/ALLOCATION.md) measured it on an unmerged branch that has Next.js installed. **The clause reproduces exactly, so the framework is not the cause, and the hole is WIDER than reported rather than narrower.** **FIVE route files across ALL FOUR non-`api` deployables** -- session 183's own `apps/portal/src/app/api/v1/admin/payouts/route.ts`, plus `/api/v1/internal/jobs` on the **marketing origin**, the same under `apps/admin` and `apps/worker`, and a **public** `/api/v1/checkout` inside `apps/portal` -- leave the estate **BYTE-IDENTICAL to the clean baseline**: lint **0**, invariants **7 of 7**, typecheck **0**, gates **30 of 30**, `surface.test.ts` **10 passed**, full suite **106 files / 1,531 passed / 1 skipped**, the same 1,531. **The gates and the suite had not previously been counted among the green results.** **NO EXISTING MECHANISM IS DEFECTIVE AND NONE IS A NEAR MISS**: `RI-04` reads dependency fields and a route handler declares no dependency, `merit/no-raw-db-client` bans a client specifier and a route imports nothing, `surface.ts` classifies paths **for `apps/api`** and cannot see `apps/portal`. **`RI-04` has been five deployables wide since [session 153](2026-08-24-session-153.md) and still never asks what one SERVES.** **THE CLASS GENERALISES AND THE SHAPE DOES NOT, which is what this session was dispatched to decide**: [ADR-096](../decisions/ADR-096.md)'s remedy is `RI-01`'s shape one directory over, `RI-01`'s shape reads a MANIFEST, and a route handler declares nothing, so pointing it here would have produced a check that cannot see its own subject. **`RI-09` reads `BASE_PATH` and `OPERATOR_PREFIXES` out of [`surface.ts`](../../apps/api/src/surface.ts) and keeps NO copy**, on `RI-06`'s relationship to the ESLint plugin: a third prefix moves the finding **with no edit to the checker**, and removing the list makes it **ERROR rather than pass**. Three path shapes; `ROUTING_ROOTS` deliberately only the two Next.js spells, because a guessed `routes` would refuse [`apps/site/src/routes/`](../../apps/site/src/routes/page.ts), which exists and is render functions. **Ten cases, each watched failing on its own seed, and the second matters most: the same path under `apps/api` must PASS**, as must [`apps/portal/src/api/types.ts`](../../apps/portal/src/api/types.ts), which `ADR-083` section 3 cites as evidence the portal is a CLIENT. **Mutation-checked**: removing the route-group skip, the owner exemption or the operator branch each fails exactly one case. **`RI-08` IS CLAIMED BY [ADR-096](../decisions/ADR-096.md) IN PROSE AND THIS IS `RI-09`**, the gap deliberate and stated in the checker's header; **the reason a collision was possible is that ALLOCATION has four tables and NO `RI-nn` table**, `OI-27`'s class, reported and not repaired. **[ADR-083](../decisions/ADR-083.md) IS SIGNED AND NOT EDITED**, and its **SIGNED clause's second half carries the probe `ADR-095` moves** -- *"no `package.json` under `apps/` declares a `build` script: the count is 0"* -- **verified still 0 on `main`, not 0 on #240**, a fact the founder should meet at the merge. **The direction it fails in is SILENCE and it fails OPEN**, and after this entry the silence is **bounded rather than removed**: section 5 enumerates four things a path check cannot see. `verify` **exit 0** at **8 of 8 invariants** and **106 files / 1,541 passed / 1 skipped**, 30 of 30 gates. **Nothing under `apps/` written**, no `CI-06` letter, no gate register, no `VG` row, no probed artifact, no migration, no `SD-nn` |
| [2026-08-25 - Session 190](2026-08-25-session-190.md) | **[M03](../plans/M03-billing-checkout.md)'s table set, transcribed under [ADR-092](../decisions/ADR-092.md) and [ADR-094](../decisions/ADR-094.md). FOUR OF FIVE, and the honest report is that the fifth is `purchases`, which [session 188](2026-08-25-session-188.md) registered first and which is taken exactly as it stands rather than re-argued.** `coupons`, `coupon_redemptions`, `psp_webhook_events` and `mid_health` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **21 of 111 becomes 25 of 111**, 12 of the 25 firm and 13 reachable through the scoped accessor. **No ADR, no migration, no new document.** **THE SLICE WAS COMPUTED FROM `TABLE_KEYS` AND CAME OUT SMALLER THAN M03's OWN COUNT**, which is [ADR-092](../decisions/ADR-092.md)'s *"a session cannot know its own slice size before it runs"* arriving in [session 188](2026-08-25-session-188.md)'s direction rather than [session 182](2026-08-24-session-182.md)'s: the registry's totality reached past the slice for nothing, because `coupon_redemptions` is `owned` on a direct column and every other FK the four carry points at a table already registered or at `affiliates`, which nobody has registered. **ALL FOUR ARE CLEAN OF LATER COLUMN CHANGES, RE-DERIVED RATHER THAN INHERITED**: over all 47 migrations with comments stripped and a multiline `ALTER TABLE ... ;` match, **NO `ALTER TABLE` STATEMENT ANYWHERE IN THE TREE NAMES ANY OF THE FOUR**, so [ADR-094](../decisions/ADR-094.md)'s fold applies nothing and each comparison is byte-identical to a `CREATE TABLE` reading; neither of the two tables ADR-094 leaves unregistrable (`otp_challenges`, `trading_calendar`) is M03's. **THE THREE `firm` RULES EACH TURN ON A NULLABLE FOREIGN KEY THAT READS LIKE A HOP AND IS NOT ONE, AND `psp_webhook_events.purchase_id` IS A TIMING TRAP RATHER THAN A TARGET TRAP**: the column is written by the PROCESSING path, so a derived rule would make a row's tenancy a function of whether a job has run yet -- the same event belonging to nobody while `out_of_order_deferred` and to somebody once `applied` -- and a `rejected_signature` row, stored precisely because its signature did NOT verify, belongs to nobody permanently. **[SESSION 188](2026-08-25-session-188.md)'s FIRM-VIA TRAP WAS RE-RUN ON A TABLE IT NEVER SAW AND ITS ASSERTION CATCHES IT**: `coupon_redemptions.coupon_id` is a `NOT NULL` FK to the firm table `coupons` and reads exactly like `daily_marks`' hop to `accounts`; seeded as `derived`, **`pnpm run typecheck` exits 0 with ZERO `error TS`** and the suite fails on one assertion by name. [Session 145](2026-08-23-session-145.md)'s seed re-run on `coupon_redemptions` scoped by `coupon_id` fails naming the column and quoting its DDL; `mid_health` with `card_settled_count` dropped fails at 11 columns against 12. **`tos_acceptances` IS DELIBERATELY NOT REGISTERED** -- it is [`0004`](../../packages/db/migrations/0004_catalog.sql)'s table and `INV-M3-09`'s gap is a migration -- and **`SD-M3-03`'s delta text names seven columns where [`0006`](../../packages/db/migrations/0006_commerce.sql) writes nine**, the transcription reading the MIGRATION, which is [session 157](2026-08-24-session-157.md)'s finding left where it was found. **COLUMN TYPE AND NULLABILITY REMAIN ASSERTED NOWHERE, ON ALL TWENTY-FIVE**, which is why `citext` and `jsonb` are transcribed rather than approximated as `text`. 42 tests to **46**, suite 1,552 to **1,556**; `verify` exit 0 at 8 of 8 invariants and 106 files / 1,556 passed / 1 skipped; 30 of 30 gates; `falsify:ci CI-01` **12 of 12** with the firm refusal re-watched over twelve firm tables |
| [2026-08-25 - Session 191](2026-08-25-session-191.md) | **[M06](../plans/M06-admin-ops-console.md) under [ADR-092](../decisions/ADR-092.md). THIRTEEN OF THIRTEEN, and the slice size was computed from `TABLE_KEYS` rather than received**: M06's fourteen tables minus `liability_snapshots`, which was already registered, so [session 160](2026-08-24-session-160.md)'s *"thirteen of M06's fourteen are unreachable through either accessor"* is exactly the number and is now zero. **No ADR, no migration, no new document.** `admin_actions`, `evidence_packs`, `identity_restriction_episodes`, `plan_breaker_state`, `alarm_suppressions`, `dual_control_approvals`, `account_adjustments`, `economic_calendar_loads`, `economic_calendar`, `report_schedules`, `report_deliveries`, `impersonation_sessions` and `impersonation_page_views` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **21 of 111 becomes 34 of 111**, eight firm, three owned and two derived, leaving 1 root / 9 owned / 7 derived / 17 firm and **17 of the 34 reachable through the scoped accessor**. **THE TENANCY QUESTION TURNED ON TWO TABLES AND BOTH ARE ARGUED FROM THE ROW RATHER THAN FROM THE READER.** `admin_actions` is **FIRM**, and the tempting rule is `owned` on `on_behalf_of_identity_id`: [`0043`](../../packages/db/migrations/0043_admin_attributed_actions.sql) sets that column **exactly when `initiative = 'trader_request'`, by a biconditional CHECK**, so a scoped read would return the actions Merit took FOR a person and **silently omit every enforcement action it took AGAINST them** -- a strict subset of the rows about that human presented as all of them, which is `purchases`' refusal on a different pair of columns. `impersonation_sessions` is **OWNED by `subject_identity_id` and not by `admin_user_id`**, which [`scope.ts`](../../packages/db/src/scope.ts)'s own header already names as a trap that reaches THE ADMIN'S identity rather than the subject's. **[ADR-094](../decisions/ADR-094.md)'s twelve were re-derived rather than inherited and the figures reproduce** -- `ADD COLUMN` on 11, `ALTER COLUMN` on 2, `DROP COLUMN` and `RENAME` on ZERO, twelve distinct tables of 111 -- and **`admin_actions` is one of the twelve and is the FIRST REGISTERED TABLE WHOSE DRIFT IS M06's**: two `ADD COLUMN`s the fold replays and one `ADD CONSTRAINT` it reads and discards. Neither `otp_challenges` nor `trading_calendar` is M06's, so the two `ALTER COLUMN` refusals cost this slice nothing. **THE FINDING IS THAT THE SUITE FOLDS ONE COLUMN READER AND NOT THE OTHER.** `ddlColumns` replays the migration history under ADR-094; **`ddlColumnDefs` still reads the `CREATE TABLE` body alone**, and the three rule-vs-DDL assertions read through it. So *"no firm table carries a column referencing identities, so the class is not hiding one"* **cannot see a later `ADD COLUMN ... REFERENCES identities(id)`** -- measured over all 47 migrations, **exactly one table of 111 carries one and it is `admin_actions`**, the table this session registered `firm`. The check that exists to contest that rule is blind to the only column that could, **the rule rests on the argument above and not on the suite, and nothing here implies otherwise.** Watched in the other direction too: seeding the refused `owned` rule fails with *"admin_actions.on_behalf_of_identity_id is not a column"*, which is a true refusal for a false reason. **Reported and not repaired, because folding `ddlColumnDefs` changes what three assertions read on all 34 registered tables and its failure direction is unmeasured -- wanting an entry for it is the stop signal rather than the licence.** Four seeds watched failing in the direction each names: a folded `ADD COLUMN` dropped from the transcription fails naming `initiative`; `report_deliveries` declared `derived` through the firm `report_schedules` is caught by [session 188](2026-08-25-session-188.md)'s bounded chain assertion; `impersonation_sessions` scoped by `admin_user_id` fails **quoting the wrong DDL back**. **The firm refusal is asserted by NAME for three of seventeen firm tables** and every one of the seventeen was measured throwing here, non-vacuously at 17; making the assertion total over `FirmTableKey` is one line and is **deliberately not taken**, because four sessions are transcribing into this file concurrently and it is a line that is not this slice's. `scoped-db.test.ts` **42 to 55**, one new per-table drift assertion per registered table; suite **1,552 to 1,565**; `verify` exit 0 at 8 of 8 invariants and 106 files / 1,565 passed / 1 skipped; 30 of 30 gates; `falsify:ci CI-01` **12 of 12**. **Column TYPE and NULLABILITY are still asserted NOWHERE** -- three new `owned` rules carry a `nullable` flag nothing checks and five new columns are declared `bytea` -- and this session did not close that gap |
| [2026-08-25 - Session 192](2026-08-25-session-192.md) | **[M07](../plans/M07-risk-abuse.md)'s tables under [ADR-092](../decisions/ADR-092.md) section 8 item 4. FIVE OF SIX, AND THE SIXTH IS A REFUSAL RATHER THAN A SHORTFALL.** `identity_signals`, `detector_definitions`, `detector_runs`, `risk_flags` and `correlation_groups` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **21 of 111 becomes 26 of 111**, twelve of the twenty-six `firm` and fourteen reachable through the scoped accessor. **No ADR, no migration, no new document.** **`identity_links` IS DELIBERATELY UNREGISTERED**: it carries `identity_a` and `identity_b`, both `uuid NOT NULL REFERENCES identities(id)`, under `identity_links_canonical_order` CHECK `identity_a < identity_b`, so an edge is stored ONCE and **which column a person's id lands in is decided by UUID comparison and by nothing else** -- naming either returns that person a strict, arbitrary subset of their own edges with no error anywhere, which is [ADR-008](../decisions/ADR-008.md)'s BOLA failure in its quietest direction. [ADR-092](../decisions/ADR-092.md) section 9 names it a per-table ruling and takes neither column, a transcription rules nothing, and the call site is refused with **`TS2345` naming `ScopedTableKey`**, watched. **`correlation_groups` IS `firm` AS A REFUSAL AND NOT AS A DEFAULT**: `member_account_ids uuid[]` is untraversable by a vocabulary whose `DerivedRule` names one column against one column, but the reason it MUST NOT be traversed is stronger -- the table starts at three members and [M07](../plans/M07-risk-abuse.md) section 3.4 filters same-identity clustering AT THE DETECTOR, so returning a row to each member would disclose which OTHER accounts the detector grouped them with. **Seeded `owned` on `member_account_ids` and caught**, the suite quoting `member_account_ids uuid[] NOT NULL` against the requirement that an owned column be declared `REFERENCES identities(id)`. **[ADR-094](../decisions/ADR-094.md)'s fold applies NOTHING to any of the five, measured over all 47 migrations**: no `ADD`, `DROP` or `ALTER COLUMN` and no `RENAME` touches any of the six, and the two later `ALTER TABLE`s that do touch this set -- [`0028`](../../packages/db/migrations/0028_supersede_plan_version_immutability.sql) on `correlation_groups` and [`0029`](../../packages/db/migrations/0029_phone_identity_and_auth.sql) on `identity_signals` -- are both constraint work the fold reads and discards. **A GATE FOUND A DEFECT IN THIS SESSION'S OWN PROSE**: the first draft of the `identity_signals` comment named the constraint `0029` superseded and **`CI-06/retired-constraints` failed on it by file and line**; the comment now names the live one alone, because adding `schema.ts` to that gate's register would be widening a register to fit a comment. **`risk_flags` IS WHERE "REGISTERED" AND "TRADER-READABLE" HAD TO BE SAID APART**: `INV-M7-10` is enforced on `evidence_packs`, a different table that is not registered here, and `account_id` is not the scope because it is NULLABLE and `entity_cap`, `payment_velocity` and `name_mismatch` are identity-level findings. **`INV-M7-02` IS STILL CONTRADICTED BY THE TREE and repairing it is a migration this fence has none of.** **THE TYPE AND NULLABILITY AXIS IS STILL ASSERTED NOWHERE**, now over twenty-six tables, and nothing here implies otherwise. 42 tests to **47**, suite total 1,552 to **1,557**, the baseline measured on this tree rather than inherited; `verify` exit 0 at 8 of 8 invariants and 106 files; 30 of 30 gates; `falsify:ci CI-01` **12 of 12** |
| [2026-08-25 - Session 193](2026-08-25-session-193.md) | **M16's table set minus whatever was already registered, under [ADR-092](../decisions/ADR-092.md)'s first-writer-wins ownership. FIVE OF SIX, and the sixth is a REFUSAL that was executed rather than believed.** `notification_kinds`, `notifications`, `notification_preferences`, `contact_channels` and `otp_send_budget` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **21 of 111 becomes 26 of 111**, eleven of the twenty-six firm and fifteen reachable through the scoped accessor. **No ADR, no migration, no new document.** **NOTHING WAS ALREADY REGISTERED AND NOTHING WAS PULLED IN**: `TABLE_KEYS` held the same twenty-one at the session's base and at the re-fetch before the claim, and all five rules are `owned` or `firm`, so no `DerivedRule.via` reached past the slice -- which is [session 182](2026-08-24-session-182.md)'s four-of-three and [session 188](2026-08-25-session-188.md)'s ten-of-ten arriving as neither, the totality having nothing to reach for. **THE SIXTH IS `otp_challenges` AND [ADR-094](../decisions/ADR-094.md) REFUSES IT**: [`0029`](../../packages/db/migrations/0029_phone_identity_and_auth.sql) carries `ALTER COLUMN email_normalized DROP NOT NULL`, the fold's vocabulary is closed at one member with a default of FAIL, and the refusal was **executed**: declared, registered, ruled and named, `tsc --noEmit` exited **0** and `no later migration changes a column in a shape ADR-094 does not fold` turned RED quoting the statement verbatim. Reverted, tree clean. **Widening the fold to fit it would be weakening a gate to pass it.** **TWO OF THE FIVE CARRY LATER COLUMNS**: `notification_kinds` gains `rate_limit_exempt` in `0029` (`SD-M16-07`), `contact_channels` gains three sealed columns in [`0034`](../../packages/db/migrations/0034_reversible_contact_addresses.sql) ([ADR-046](../decisions/ADR-046.md)) and `complained_at` in [`0041`](../../packages/db/migrations/0041_contact_channel_complaints.sql), both unregistrable before ADR-094 and neither excused by it. **[SESSION 188](2026-08-25-session-188.md)'S TRAP WAS PRESENT IN THIS SLICE AND WAS WATCHED BEING CAUGHT**: `notification_kinds` is a firm catalogue and `notifications.kind` and `notification_preferences.kind` are both `NOT NULL REFERENCES notification_kinds(kind)`, reading exactly like the `hop` `daily_marks` makes to `accounts`; seeded, `tsc` exits 0 and the bounded chain assertion fails by name. **Two GENERATED columns are transcribed as generated and two composite primary keys are transcribed at all**, on the file's own `bytea`-is-not-`text` discipline: `mutable` and `rate_limit_exempt` are the module's policy in DDL and a plain boolean here would be a false statement about the one axis nothing checks. **THE TYPE AND NULLABILITY AXIS IS STILL ASSERTED NOWHERE and this session did not close it.** **`contact_channels.superseded_by` ties a supersession to no identity**, which is `certificates`' unrepaired finding in a second place. 42 tests to **47**, suite 1,552 to **1,557** measured on this tree rather than quoted; `verify` exit 0 at 8 of 8 invariants and 106 files; 30 of 30 gates; `falsify:ci CI-01` **12 of 12** with the firm refusal re-watched over eleven firm tables |
| [2026-08-25 - Session 194](2026-08-25-session-194.md) | **[M19](../plans/M19-kyc-identity.md)'s slice under [ADR-092](../decisions/ADR-092.md), computed from `TABLE_KEYS` rather than received. FIVE OF SIX, and the honest report is that all five were AVAILABLE**: `main` was `dfca9bc` at open and `dfca9bc` at commit, re-fetched rather than assumed, so none of the four concurrent siblings had landed and nothing was already there. `kyc_verifications`, `sanctions_screenings`, `kyc_funnel_events`, `identity_phones` and `phone_change_requests` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **21 of 111 becomes 26 of 111**, all five `owned` on a `NOT NULL identity_id` the DDL declares against `identities(id)`. **No ADR, no migration, no new document.** **`dedupe_matches` IS REFUSED AND THE REASON IS THE VOCABULARY RATHER THAN THE TABLE**: it carries `identity_a` AND `identity_b`, both `NOT NULL REFERENCES identities(id)`, under `CHECK (identity_a < identity_b)`, so which side an identity lands on is decided by **uuid ordering** -- and `OwnedRule.column` is one column while [`scopePredicate`](../../packages/db/src/scoped-db.ts) renders one `eq`, so a rule on either column returns **roughly half that identity's match rows, silently**. **It would have passed every assertion in the suite**, which is why the refusal is worth recording: it came from reading `dedupe_matches_canonical_order` and not from a gate. This is [ADR-092](../decisions/ADR-092.md) section 3's named-and-unruled class, **third member** beside `identity_links` and `attributions`, and **a transcription rules nothing**, so it was reported rather than allocated an ADR. **`otp_challenges` is out for the already-ruled reason and is ALSO NOT M19'S**: [`0029`](../../packages/db/migrations/0029_phone_identity_and_auth.sql) writes `ALTER COLUMN email_normalized DROP NOT NULL`, unregistrable under [ADR-094](../decisions/ADR-094.md) exactly as `trading_calendar` is, and `grep -n 'otp'` over M19 returns one line which is about `contact_channels`. **TWO OF THE FIVE CARRY DRIFT AND THE FOLD WAS WATCHED APPLYING IT**: [`0034`](../../packages/db/migrations/0034_reversible_contact_addresses.sql) adds three `ADD COLUMN`s to `identity_phones` and five to `phone_change_requests` (ADR-046), while **0029's change to `kyc_verifications` is `DROP CONSTRAINT` plus `ADD CONSTRAINT`** widening `verification_purpose` for `reverify_phone_change` (SD-M19-07), which moves no column set -- and deleting `phone_ciphertext` from the transcription turns the per-table assertion RED naming the column, so the replay is live on this session's own tables. **`identity_phones` scoped by `phone_hash` was seeded and is caught** by the DDL check, which matters because [`identity_phones_live_number_idx`](../../packages/db/migrations/0029_phone_identity_and_auth.sql) is **deliberately not unique**, so that rule would have returned STRANGERS' rows. **MERIT NEVER PROXIES IDENTITY DOCUMENTS and each `why` says which side of that line its table holds**: `provider_applicant_id` is a reference and `phone_hash` is a digest, both `bytea` declared as a CUSTOM TYPE and never approximated as `text`. **TWO GAPS NAMED AGAINST THIS SESSION**: column TYPE and NULLABILITY are still asserted nowhere on any of the twenty-six, and **`phone_change_requests` reaches an identity two ways with nothing tying them** (`old_phone_id NOT NULL REFERENCES identity_phones(id)`), which is `certificates`' shape and now its **second instance** rather than a one-off. 42 tests to **47**, suite **1,552 to 1,557**, both baselines MEASURED by restoring `origin/main`'s three files rather than inherited; `verify` exit 0 at 8 of 8 invariants and 106 files; 30 of 30 gates; `falsify:ci CI-01` **12 of 12** |
| [2026-08-25 - Session 195](2026-08-25-session-195.md) | **[M04](../plans/M04-trader-portal.md)'s tables under [ADR-092](../decisions/ADR-092.md), computed from `TABLE_KEYS` rather than received. TWO OF FOUR REGISTRABLE, AND EIGHTEEN OF M04'S TWENTY-TWO WERE ALREADY THERE**, which is the shape a portal slice was expected to have and is reported rather than argued up. `plans` (firm) and `passkeys` (derived through `users`, one hop) are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **56 of 111 becomes 58 of 111 on the merge result**, at 1 root, 21 owned, 10 derived and 26 firm counted off `SCOPE_RULES`; the slice was computed against 53 and [session 197](2026-08-25-session-197.md) landed three tables while this branch was open. **No ADR, no migration, no new document, and [M04](../plans/M04-trader-portal.md) was read and not edited.** **THE METHOD IS STATED BECAUSE THE ANSWER DEPENDS ON IT: M04 owns no endpoint**, so its table set is what its section 4 endpoints READ rather than what it writes, and a whole-word grep of the plan alone would have returned six false positives -- `events`, `offers`, `plans`, `fills`, `passkeys` and `payout_requests` -- of which **four are prose, a verb, a URL path segment and a metric name.** **`events` IS THE FINDING AND IT IS REFUSED, NOT DECLARED IN `schema.ts` AT ALL** on [session 192](2026-08-25-session-192.md)'s model: [`0017`](../../packages/db/migrations/0017_events_and_audit.sql) gives it `identity_id uuid NULL` AND `account_id uuid NULL`, **neither required and no CHECK tying them**, so an `owned` rule on the first drops every account-level row and a hop through the second drops every identity-level one -- while [EVENTS](../architecture/EVENTS.md) section 2 rows the portal's timeline `TL` as **per-account** and M04 section 5 consumes `phone.verified` and `phone.change_requested` on the same screen, so **both halves are genuinely read and neither column covers them**. Its `jsonb` payload is the second reason and the one no scope rule reaches: `kyc.dedupe_hit` carries `matched_identity_id`, so a row whose own tenancy column is right still names a DIFFERENT identity, which `INV-M4-06` forbids the portal to receive. **A FOURTH SHAPE of [ADR-092](../decisions/ADR-092.md) section 3's named-and-unruled class** -- one identity column beside one ACCOUNT column, where `identity_links` and `dedupe_matches` have two identity columns -- and **wanting an ADR for it was taken as the stop signal.** **`otp_challenges` is the second refusal and is already ruled**: `0029`'s `ALTER COLUMN ... DROP NOT NULL` against [ADR-094](../decisions/ADR-094.md)'s fold, closed at one member with a default of FAIL, executed by [session 193](2026-08-25-session-193.md) and not re-executed here. **REGISTERING `plans` FALSIFIED A COMMENT ON A TABLE THIS SESSION DOES NOT OWN AND THE REPAIR IS IN THE SAME DIFF**: `plan_breaker_state`'s `plan_id` said it carried no `.references()` *"because `plans` is not one of this file's tables"*, and the second half of that condition changed while the first never did, so `plan_versions.plan_id` and `plan_breaker_state.plan_id` now claim the reference the file's own rule permits -- **ADR-092's "a session cannot know its own slice size before it runs" arriving as PROSE rather than as a table.** **FOUR CONTROLS WATCHED FIRING AND THE TRAP FIRST BECAUSE IT COMPILES**: `passkeys.via` set to the firm `plans` leaves `typecheck` at **exit 0, zero `error TS`**, and turns TWO assertions red naming `plans` as firm; `transports` deleted fails the per-table drift comparison; `plans` declared `owned` on `code` is refused by the DDL check quoting `code text NOT NULL UNIQUE` back; **`TS2739` naming both new keys** was watched before `SQL_NAME` was completed. **`main` DID NOT MOVE WHILE THE SLICE WAS COMPUTED AND THEN IT DID**, `3c9c2d3` with `TABLE_KEYS` at 53 at open and at the claim, re-fetched rather than assumed, so nothing was pulled in and nothing was already taken; **then session 197 merged to `92ea3d9` and [ADR-092](../decisions/ADR-092.md) section 4's fourth row arrived in five files**, resolved structurally and not by editing around a marker, with the two table sets disjoint so every table survived. **COLUMN TYPE AND NULLABILITY ARE STILL ASSERTED NOWHERE, now across 55 tables**, which is why `credential_id` and `public_key` are the `bytea` custom type and `transports` is a `text[]` array rather than approximated. **77 tests to 79** and suite **1,587 to 1,589** on the merge result, and **74 to 76** against `3c9c2d3` before it, all four baselines MEASURED by restoring `origin/main`'s files in this tree rather than quoted; `verify` exit 0 at 8 of 8 invariants and 106 files; 30 of 30 gates; `falsify:ci CI-01` **12 of 12** |
| [2026-08-25 - Session 196](2026-08-25-session-196.md) | **[M05](../plans/M05-payout-system.md)'s tables under [ADR-092](../decisions/ADR-092.md) section 8 item 4. SIX OF SIX, AND NOTHING WAS REFUSED.** `payout_requests`, `payout_transfers`, `wallet_entries`, `wallet_withdrawals`, `wallet_spend_limits` and `wallet_dormancy` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **53 of 111 becomes 59 of 111**, at 1 `root`, 25 `owned`, 8 `derived` and 25 `firm`, so **34 of the 59 are reachable through the scoped accessor**. **No ADR, no migration, no new document.** **THE ONE KNOWN COLLISION RESOLVED BY FIRST-WRITER-WINS WITH NO SECOND WRITER PRESENT**: [ADR-092](../decisions/ADR-092.md) section 8 item 4 names this slice and session 202's `M20-a` as the single intersecting pair over four wallet tables, and all four were unregistered at `3c9c2d3` when this session opened, when it committed and when it wrote this log, so it took all four and 202 will find them present and its slice that much smaller. **Nothing was coordinated and nothing was waited for**, which is the ruling working rather than a problem scheduled around. **THE SLICE WAS SEVEN AND IS SIX BECAUSE A SIBLING ALREADY TOOK ONE**: [session 159](2026-08-24-session-159.md)'s slice 3 named seven tables absent from both files and `identity_restriction_episodes` is now [session 191](2026-08-25-session-191.md)'s, registered `owned` on the same column this session would have chosen, and a registration is never re-argued. **`payout_requests` IS ONE OF [ADR-094](../decisions/ADR-094.md)'s TWELVE and is the only one of the six that is**: [`0031`](../../packages/db/migrations/0031_payout_hold_and_identity_restriction.sql) adds `held_at`, `hold_flag_id`, `hold_expires_at`, `hold_tos_clause` and `hold_reason` in ONE `ADD COLUMN` statement, the fold replays all five, and **deleting `held_at` from the transcription was watched turning the per-table comparison RED at 26 columns against 27**. **`wallet_dormancy` IS NOT ONE OF THE TWELVE ALTHOUGH [`0028`](../../packages/db/migrations/0028_supersede_plan_version_immutability.sql) ALTERS IT**: `DROP CONSTRAINT` plus `ADD CONSTRAINT` is constraint work the fold reads and discards, so the column set is unchanged and the fold was not widened to fit anything. **`payout_requests` IS `owned` BY `identity_id` AND NOT BY `account_id`, WHICH IS THE SHARPEST INSTANCE IN THE ESTATE OF WHAT [ADR-008](../decisions/ADR-008.md) BOUNDED**: both reach the same identity, the direct column is the one the database declares against `identities(id)`, and [`0010`](../../packages/db/migrations/0010_payouts.sql)'s own comment says the column exists because the aggregate-exposure question cannot be a join if it is asked inside the race it protects against. **Seeded `account_id` and caught**, the suite quoting `REFERENCES accounts(id)` back. **`wallet_entries`' TRAP IS `ledger_transaction_id`**: NOT NULL, pointing at a REGISTERED table, reading exactly like the legitimate hop `ledger_entries` makes -- and wrong, because `ledger_transactions` reaches an identity only by a semi-join through the firm leg of a double-entry posting. **Seeded a `derived` rule through the firm `plan_versions` and caught at THREE assertions.** **`wallet_dormancy` IS `owned` AND NOT `root` ALTHOUGH `identity_id` IS ITS WHOLE PRIMARY KEY**: `root` means the row IS the identity, and `identities` is the only table this vocabulary's root may name. **`hold_flag_id` CARRIES NO `.references()` AND `freeze_flag_id` DOES**, which is [`schema.ts`](../../packages/db/src/schema.ts)'s own header rule rather than an omission: the fold reads an `ADD COLUMN` for its column NAME alone, so a constraint claimed for an ALTER-added column would be a claim nothing in this package checks. **THE TYPE AND NULLABILITY AXIS IS STILL ASSERTED NOWHERE**, now over fifty-nine tables and over eight new `bigint` money columns, and nothing here implies otherwise. **The four deliberate absences survive untouched**: `identity_links`, `dedupe_matches`, `otp_challenges` and `trading_calendar` appear in no declaration and no registration site. 74 tests to **80**, suite total 1,584 to **1,590**, the baseline measured on this tree by checking out `3c9c2d3 -- packages/db` and running rather than inherited; `verify` exit 0 at 8 of 8 invariants and 106 files; 30 of 30 gates; `falsify:ci CI-01` **12 of 12** |
| [2026-08-25 - Session 197](2026-08-25-session-197.md) | **[M08](../plans/M08-affiliate-system.md)'s tables under [ADR-092](../decisions/ADR-092.md) and [ADR-094](../decisions/ADR-094.md). THREE OF FIVE, AND BOTH ABSENCES ARE REFUSALS RATHER THAN A SHORTFALL.** `affiliates`, `affiliate_creatives` and `affiliate_clicks` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **53 of 111 becomes 56 of 111**, one new `owned` and two new `derived`, no new `firm`. **No ADR, no migration, no new document.** **`attributions` IS DELIBERATELY UNREGISTERED AND UNDECLARED**, which is [session 192](2026-08-25-session-192.md)'s handling of `identity_links` applied to the sibling [ADR-092](../decisions/ADR-092.md) section 9 names beside it: `buyer_identity_id` and `affiliate_identity_id` are both `uuid NOT NULL REFERENCES identities(id) ON DELETE RESTRICT` and the DDL's own comment says both are stored rather than joined BECAUSE they are two different people, with `attributions_literal_self_deal_is_void` existing to refuse the row where they are one -- so naming the affiliate hands a buyer's purchase attribution to their referrer, which returns rows, raises nothing, and is [ADR-008](../decisions/ADR-008.md)'s BOLA failure. **`affiliate_commissions` FOLLOWS IT OUT AS A CONSEQUENCE RATHER THAN A JUDGMENT**: its only path to an identity is `attribution_id`, `paid_in_statement_id` reaches `affiliate_statements` which is one of [ADR-092](../decisions/ADR-092.md) section 9's four, `DerivedRule.via` is `TableKey`, and an unregistered table has no key to name -- **totality forcing a table OUT of a slice**, which is the direction [ADR-094](../decisions/ADR-094.md) section 6 did not name. **`affiliate_statements` and `payment_disputes` ARE NOT IN THIS SLICE**: [M08](../plans/M08-affiliate-system.md) names neither table by name, whole-word count zero, which is [ADR-092](../decisions/ADR-092.md) section 9's own measurement reproduced. **[ADR-094](../decisions/ADR-094.md)'s FOLD APPLIES NOTHING**: zero `ALTER TABLE` lines naming any affiliate or attribution table across all 47 migrations, so nothing replays and nothing is refused. **[ADR-090](../decisions/ADR-090.md)'s SIBLING SETTLEMENT TABLE DOES NOT EXIST IN ANY MIGRATION**, so no `why` here argues about a rail. **FOUR SEEDS WATCHED FAILING**: `affiliate_clicks` owned on `ip` refused **quoting `ip inet NULL` back**; `click_fingerprint` deleted failing the per-table comparison; **`affiliates` declared `firm` caught TWICE**, by the bounded chain assertion naming `affiliateCreatives -> affiliates` and by the firm-reaches-identities check, which is the trap this session was dispatched to expect; and both unregistered call sites refused with **`TS2345` naming the fifty-six-member union**. **REPORTED AND NOT ACTED ON:** `coupons.affiliate_id` and `purchases.affiliate_id` declare inline FKs to `affiliates(id)` and carry no `.references()`, which is now eligible and is two lines on two other sessions' tables. **TYPE AND NULLABILITY ARE STILL ASSERTED NOWHERE**, now over fifty-six tables, which is why `citext`, `bytea` and `inet` are transcribed rather than approximated. 74 tests to **77**, suite 1,584 to **1,587**, both measured on this tree; `verify` exit 0 at 8 of 8 invariants and 106 files; 30 of 30 gates; `falsify:ci CI-01` **12 of 12** |
| [2026-08-25 - Session 198](2026-08-25-session-198.md) | **[M10](../plans/M10-integrations.md)'s tables under [ADR-092](../decisions/ADR-092.md) section 8 item 4. THREE OF THREE, AND THE HONEST FORM OF THAT NUMBER IS THAT M10 HAS FOUR SCHEMA DELTAS AND ONLY THREE ARE TABLES**: `SD-M10-04` is `identities.support_contact_ref`, a column on a table [session 145](2026-08-23-session-145.md) registered first, so it needed no registration and got none. `integration_contracts`, `integration_dispatches` and `support_context_views` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **53 of 111 becomes 56 of 111**, at 1 root, 22 owned, 7 derived and 26 firm, so **30 of the 56 are reachable through the scoped accessor**. **No ADR, no migration, no new document.** **M10 IS INTEGRATIONS, SO EVERY ROW HERE IS A THIRD PARTY'S RECEIPT AND THE TENANCY QUESTION PARTS FROM *WHOSE ACTION CAUSED IT* ON ALL THREE.** `integration_dispatches` is **`owned` by `identity_id` with `nullable: true`**, which is `ledger_accounts`' shape and which [`0018`](../../packages/db/migrations/0018_integrations.sql) states in the direction that decides the rule -- the dispatches that are about nobody **must not be findable by an identity search that returns them anyway** -- and the row is about the person disclosed, which is what `GET /admin/identities/:identityId/disclosures` reads. **`psp_webhook_events` WAS READ BEFORE ANYTHING SIMILAR WAS CLASSIFIED AND IS WHERE THIS TABLE PARTS FROM THE NEAREST REGISTERED ROW**: [session 190](2026-08-25-session-190.md) made it `firm` because its nullable `purchase_id` is bound by the handler DURING processing, so tenancy would differ before and after a re-drive; `identity_id` here is written with the row and none of `queued`, `sent`, `failed` or `dropped_by_guard` moves it. `support_context_views` is **`owned` by `identity_id`, NOT NULL**, the row being about **the trader who was read** and not the agent who read, which is `impersonation_sessions`' question one surface out; **there is no second identity column to choose between and that is structural**, `agent_ref text NOT NULL` being an actor string that declares no foreign key where `impersonation_sessions.admin_user_id` does reference `users`. `integration_contracts` is **`firm`**: one contract governs every identity's dispatches to that vendor and `field_allowlist` holds field NAMES rather than values. **NO TABLE HAD TO BE REFUSED, AND THAT IS A PROPERTY OF THE THREE RATHER THAN A CLEAN RUN**; the four deliberate absences were checked by name and are untouched. **[ADR-094](../decisions/ADR-094.md)'S FOLD APPLIES NOTHING TO ANY OF THE THREE, MEASURED OVER ALL 47 MIGRATIONS**: the one later `ALTER TABLE` is [`0028`](../../packages/db/migrations/0028_supersede_plan_version_immutability.sql) dropping and re-adding `integration_contracts_enabled_has_fields` **under its own name**, constraint work the fold reads and discards. **THE TRAP WAS SEEDED RATHER THAN AVOIDED AND IT COMPILES**: `integration_dispatches` declared `derived` through the firm `integration_contracts` passes `typecheck` with zero `error TS` and is caught at **3 failed / 74 passed** by [session 188](2026-08-25-session-188.md)'s bounded chain assertion; two further seeds landed at 1 failed / 76 passed each. **FOUR GAPS NAMED AND NONE CLOSED**, of which the new one is that **`OwnedRule.nullable` is DOCUMENTATION AND NOT A CONTROL** -- `scopePredicate` never reads it and nothing compares it to the DDL -- pre-existing since `ledger_accounts` and left, because making it a control is a ruling and a transcription rules nothing. 74 tests to **77**, suite 1,584 to **1,587** at 106 files and 1 skipped; `verify` exit 0 at 8 of 8 invariants; 30 of 30 gates; `falsify:ci CI-01` **12 of 12** |
| [2026-08-25 - Session 199](2026-08-25-session-199.md) | **[M13](../plans/M13-trader-analytics-journal.md)'s tables under [ADR-092](../decisions/ADR-092.md). FIVE OF FIVE, AND THE SLICE IS NOT [SESSION 164](2026-08-24-session-164.md)'s FIVE.** `contract_specs`, `fills`, `round_trips`, `journal_entries` and `analytics_snapshots` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **53 of 111 becomes 58 of 111 at this session's base and 56 of 111 becomes 61 of 111 merged**, recomputed from `TABLE_KEYS.length` in all four places that state it, twice, and never incremented. **M13 names ELEVEN of the 111 tables and SIX were already registered** -- `accounts`, `daily_marks`, `rule_states`, `plan_versions`, `plan_version_sizes` and `certificates` -- **so `daily_marks` came out of session 164's `M13-c` list and `contract_specs` came in**, which is `DEP-M13-03` and M13 section 7's *"tick values come from `contract_specs`, never from a multiplier in analytics code"*. **`journal_entries` IS THE TWO-COLUMN SHAPE AND IT IS NOT THE TWO-COLUMN REFUSAL**: `identity_id uuid NOT NULL REFERENCES identities(id)` is the author and `account_id` is **NULLABLE**, because a `day`-scoped entry need name no account, so scoping through the account silently drops every entry that names none -- one column is total and the other is not, which is what `identity_links` does not have and is why that table stays unregistered and this one does not. **`contract_specs` IS `firm` AND THE CALL SITE WAS WATCHED REFUSING IT**: one `tsc` run over a probe naming all five returns **exactly one error, `TS2345` naming `ScopedTableKey`**, and the four scoped keys compile. **[ADR-094](../decisions/ADR-094.md)'s FOLD APPLIES NOTHING TO ANY OF THE FIVE**, measured over all 47 migrations: no `ADD COLUMN`, `DROP COLUMN`, `ALTER COLUMN` or `RENAME` touches them, and the only later `ALTER TABLE`s in the set -- [`0028`](../../packages/db/migrations/0028_supersede_plan_version_immutability.sql):179-187 restating `round_trips`' two array constraints on `cardinality` where [`0022`](../../packages/db/migrations/0022_analytics_journal.sql):64-66 wrote `array_length` -- are `DROP CONSTRAINT` and `ADD CONSTRAINT` work the fold reads and discards. **THE FOLD WAS NOT WIDENED**, and neither `otp_challenges` nor `trading_calendar` is M13's, so the two `ALTER COLUMN` refusals cost this slice nothing. **`SD-M13-01` SAYS `uuid[]` AND THE TRANSCRIPTION SAYS `bigint[]`, BECAUSE THE DDL IS THE SOURCE**: `fills.id` is `bigint GENERATED ALWAYS AS IDENTITY`, so the frozen plan's cell could never have referenced a fill; **the plan was read and not edited**, since `M13-b` moves a frozen document by ADR and a transcription rules nothing. **THE `rule_states` GRANT FORK IS UNTOUCHED AND MUST NOT READ AS RESOLVED**: `rule_states` was already registered, its rule is taken as it stands, and `ADR-091` is unsigned and is the founder's -- a scope rule is who may READ A ROW through the accessor and a `GRANT` is which ROLE may reach the table, and registering neither grants nor revokes anything. **THREE SEEDS WATCHED FAILING IN THE DIRECTION EACH NAMES**, each re-run on the MERGED tree at 1 or 3 failed / 81 or 79 passed: `journal_entries` scoped by `account_id` fails **quoting the wrong DDL back**, `round_trips` declared `derived` through the firm `contract_specs` trips [session 188](2026-08-25-session-188.md)'s bounded chain assertion **plus the hop-is-a-PK and the declared-FK assertions, three at once**, and `trading_day_vendor` deleted from the `fills` transcription fails the per-table comparison **naming the column**. **THE TYPE AND NULLABILITY AXIS IS STILL ASSERTED NOWHERE**, now over sixty-one tables and over one more `bytea` column declared as a custom type rather than approximated as `text`, and nothing here implies otherwise. **THE PREDICTED CONFLICT ARRIVED AND COST ONE RESOLUTION**: [session 197](2026-08-25-session-197.md) merged first, `main` moved from `3c9c2d3` to `92ea3d9`, and [ADR-092](../decisions/ADR-092.md) section 4's fourth row happened as written -- **a textual conflict in all five files**, every one keep-both because the table sets are disjoint, each resolved by rebuilding the file from `main`'s copy plus whole declarations. 77 tests to **82**, suite total 1,587 to **1,592 passed / 1 skipped** over 106 files, both ends measured on the merged tree; `verify` exit 0 at 8 of 8 invariants; 30 of 30 gates; `falsify:ci CI-01` **12 of 12**. **No ADR, no migration, no new document, `docs/plans/**` read only.** **`E2` read still owed.** |
| [2026-08-25 - Session 200](2026-08-25-session-200.md) | **[M17](../plans/M17-offers-engine.md)'s slice under [ADR-092](../decisions/ADR-092.md), computed from `TABLE_KEYS` rather than received. FOUR OF FOUR, and the honest report is that all four were AVAILABLE**: `main` was `3c9c2d3` at open and `3c9c2d3` immediately before the log, re-fetched rather than assumed, so no sibling of sessions 195 to 203 had merged. `offer_experiments`, `price_floors`, `offers` and `promotional_credit_grants` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **53 of 111 becomes 57 of 111**, at **1 root, 22 owned, 7 derived and 27 firm**, every figure read back out of the registry rather than added up. **No ADR, no migration, no new document.** **`offers` IS `ledger_accounts`' SHAPE AND NOT `coupons`', AND THE SUITE SAYS SO RATHER THAN TASTE**: `coupons` is firm because it has no identity column, `offers` has one, and `offers_identity_scope_matches` makes `identity_id` BICONDITIONAL with `scope = 'identity'`, so `owned` with `nullable: true` returns exactly the rows the DDL says are that person's -- and declaring it `firm` was seeded and fails by name with *"offers reaches identities directly"*. **What that read returns is narrower than the table's name suggests and it is said out loud**: the offers ADDRESSED TO a person, never the offers AVAILABLE to them, and **a `segment` offer names no segment because the table has no segment column at all**, so it is unreachable under every class rather than under this one. **THE ADR-092 TRAP IS CAUGHT IN ONE DIRECTION AND SILENT IN THE OTHER, BOTH WATCHED**: `offers` declared `derived` through the FIRM `offer_experiments` on `experiment_id` fails the bounded chain assertion by name, while **`promotional_credit_grants` declared `derived` through the OWNED `offers` on `source_offer_id` passes ALL 78 TESTS** -- the chain terminates, nothing throws, and the rule would return no grant whose `source_offer_id` is NULL, which is every fee-back and every loyalty grant. **The bounded assertion bounds a FIRM parent and does not bound a NULLABLE-column OWNED one**, a strictly larger silent class than the one it was written for. **ONE OF THE FOUR CARRIES DRIFT**: [`0044`](../../packages/db/migrations/0044_fee_back_and_ladder_unlock.sql)'s `source_payout_request_id`, one `ADD COLUMN`, folded by [ADR-094](../decisions/ADR-094.md), and deleting it from the transcription turns the per-table comparison RED naming the column. **[Session 166](2026-08-24-session-166.md)'s `price_floors` finding is RE-STATED AND NOT ACTED ON**: PK `(product_ref, effective_from)` against `dual_control_approvals.subject_id uuid NOT NULL`, so a control four documents assert cannot name its subject, and a repair is a migration this fence holds none of. **All four standing refusals survive checked by name.** 74 tests to **78**, suite **1,584 to 1,588**, both baselines MEASURED by restoring `origin/main`'s three files; `verify` exit 0 at 8 of 8 invariants and 106 files; 30 of 30 gates; `falsify:ci CI-01` **12 of 12** |
| [2026-08-25 - Session 201](2026-08-25-session-201.md) | **[M18](../plans/M18-graduation-track.md)'s table set minus whatever was already registered, under [ADR-092](../decisions/ADR-092.md)'s first-writer-wins ownership. THREE OF FOUR, and the fourth is not a shortfall: `accounts` carries `SD-M18-01` and was registered before this session opened.** `graduation_benefits`, `graduation_invitations` and `plan_size_unlocks` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **53 of 111 becomes 56 of 111**, twenty-five of the fifty-six `firm` and thirty-one reachable through the scoped accessor. **No ADR, no migration, no new document.** **NOTHING MERGED UNDER THIS SESSION AND THE RE-FETCH SAYS SO**: `git fetch origin main` returns `3c9c2d3` with `TABLES` at fifty-three at open and at commit alike, so the slice computed is the slice finished. **THE FIRM-HOP TRAP THE DISPATCH NAMED WAS PRESENT IN THIS SLICE AND WAS WATCHED BEING CAUGHT.** `plan_size_unlocks.plan_version_id` is `uuid NOT NULL REFERENCES plan_versions(id)` and reads exactly like the `hop` `daily_marks` makes to `accounts`; **A LADDER'S RUNGS ARE THE SAME FOR EVERYONE AND A TRADER'S POSITION ON IT IS THEIR OWN**, and `plan_versions` is FIRM, so a derivation through it constructs no predicate and throws the first time anybody reads the table. Seeded `derived` via `planVersions`: **`tsc --noEmit` exits 0** and the bounded chain assertion turns RED naming the chain, `planSizeUnlocks -> planVersions ... planVersions is firm`. **ALL THREE ARE `owned` AND NOTHING WAS PULLED IN BY TOTALITY**: no `DerivedRule.via` was written, so [session 182](2026-08-24-session-182.md)'s four-of-three had nothing to reach for here. **[ADR-094](../decisions/ADR-094.md)'S FOLD APPLIES NOTHING TO ANY OF THE THREE, MEASURED OVER ALL 47 MIGRATIONS with the suite's own multiline algorithm**: not one `ADD COLUMN`, `DROP COLUMN`, `ALTER COLUMN` or `RENAME` touches any of them, and none of the four deliberately-unregistered tables was approached. **`certificates`' FINDING IS TAKEN AS IT STANDS AND IS NOW IN ITS THIRD AND FOURTH PLACE**: `graduation_benefits` carries `identity_id` AND `account_id`, both `NOT NULL`, and `plan_size_unlocks` carries `identity_id` AND `earned_account_id`, both `NOT NULL`, **and no constraint in either DDL ties the row's identity to the identity that owns its account**. The direct column is the rule in both, on [session 188](2026-08-25-session-188.md)'s landed reading, and a transcription does not re-argue it. **`graduation_invitations` IS RESERVED AND EMPTY AT LAUNCH AND THAT IS A FACT ABOUT ROWS RATHER THAN ABOUT A RULE**: registering it ships no program and confers no read, because [ADR-092](../decisions/ADR-092.md) section 9 makes registration reachability and nothing else. **SESSION 167'S TWO STRUCTURAL COLLISIONS WERE RE-READ AND NEITHER IS MOVED BY THIS SESSION**, both being migration questions inside `M18-B`'s fence and not a transcription's. **THE TYPE AND NULLABILITY AXIS IS STILL ASSERTED NOWHERE, now across fifty-six tables, and nothing written here implies otherwise.** 74 tests to **77**, suite **1,584 to 1,587**, both baselines MEASURED by restoring `origin/main`'s three files rather than inherited; `verify` exit 0 at 8 of 8 invariants and 106 files; 30 of 30 gates; `falsify:ci CI-01` **12 of 12**. **THE PREDICTED CONFLICT THEN ARRIVED**: [session 197](2026-08-25-session-197.md) merged first, `main` moved `3c9c2d3` to `92ea3d9`, and **both branches had independently computed 56 for DISJOINT table sets**, so all five files conflicted textually and every one was resolved structurally rather than keep-both. **The merged result is 59 of 111** at 1 `root`, 24 `owned`, 9 `derived`, 25 `firm`, recomputed from `TABLE_KEYS.length` at all five sites and never incremented; **`scoped-db.test.ts` is 77 on `92ea3d9` and 80 merged**, 77 plus three, and `typecheck` exits 0 with zero `error TS` on the merge result |
| [2026-08-25 - Session 202](2026-08-25-session-202.md) | **[M20](../plans/M20-wallet.md)'s table set under [ADR-092](../decisions/ADR-092.md) and [ADR-094](../decisions/ADR-094.md). FOUR OF FOUR, and the four are the same four [session 196](README.md) was dispatched to claim.** `wallet_entries`, `wallet_withdrawals`, `wallet_spend_limits` and `wallet_dormancy` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts), each `owned` on an `identity_id` the DDL declares against `identities(id)`; **53 of 111 becomes 57 of 111**, at 1 root / 24 owned / 7 derived / 25 firm, so **32 of the 57 are reachable through the scoped accessor**. **No ADR, no migration, no new document.** **[ADR-092](../decisions/ADR-092.md) SECTION 8 ITEM 4'S ONE INTERSECTING PAIR IS THIS ONE AND THE RULING'S FIRST REAL TEST RESOLVED BY NOBODY DOING ANYTHING**: [session 159](2026-08-24-session-159.md)'s `M5-1` and [session 169](2026-08-24-session-169.md)'s `M20-a` claim the same four, 159 wrote the collision down and 169 called the two files *"M20-a's alone"*, and `git fetch origin main` at open and again immediately before the claim both return **`3c9c2d3`**, so all four were unregistered when looked at and the first writer wrote them. **No roster was read because there is no roster**, and the four `why` fields state the TABLE's tenancy rather than M20's use, which is what makes session 196's smaller slice costless rather than a loss. **THE FINDING IS A TRAP THAT IS CAUGHT BY NOTHING, AND IT IS NOT THE ONE THE DISPATCH PREDICTED.** `wallet_entries.ledger_transaction_id uuid NOT NULL REFERENCES ledger_transactions(id)` reads exactly like `ledger_entries`' hop, and because `ledger_transactions` is registered `derived` rather than `firm` the chain **TERMINATES**: seeded as `derived` through it, **`pnpm run typecheck` exits 0 with zero `error TS` and the suite passes at 78 of 78**. The declared FK exists, [session 188](2026-08-25-session-188.md)'s bounded chain assertion is satisfied, and every mechanical check agrees with a rule answering a DIFFERENT QUESTION -- whose ledger accounts appear on this transaction, rather than whose wallet holds this entry -- which agree only while no transaction touches two identities' accounts, a thing nothing enforces and double entry makes ordinary. **This is [session 194](2026-08-25-session-194.md)'s `dedupe_matches` class in a second shape, reported and NOT repaired, because an assertion that `derived` is unavailable where an `owned` column exists is a ruling and a transcription rules nothing.** **The second trap is NULLABILITY rather than target and is equally uncaught**: `freeze_flag_id uuid NULL REFERENCES risk_flags(id)` reaches the CORRECT identity through a registered `owned` table and would still return a person only the withdrawals HELD AGAINST THEM, because the column is NULL on every withdrawal nobody froze. **[ADR-094](../decisions/ADR-094.md)'S FOLD APPLIES NOTHING TO ANY OF THE FOUR, RE-DERIVED RATHER THAN INHERITED**: no `ADD`, `DROP` or `ALTER COLUMN` and no `RENAME` names any of the four over all 47 migrations, and the two later `ALTER TABLE`s that do touch the set -- [`0028`](../../packages/db/migrations/0028_supersede_plan_version_immutability.sql) superseding `wallet_dormancy_review_was_noticed` and [`0031`](../../packages/db/migrations/0031_payout_hold_and_identity_restriction.sql) adding `wallet_withdrawals_live_freeze_blocks_settlement` -- are both constraint work the fold reads and discards; neither of ADR-094's two unregistrable tables is M20's, so the fold was not widened. **`wallet_dormancy` IS THE FIRST REGISTERED TABLE WHOSE `nullable: false` IS READ OFF `PRIMARY KEY` RATHER THAN OFF THE WORDS `NOT NULL`**, and its `why` says so, which matters precisely because nullability is the axis nothing checks. **Two seeds watched failing in the direction each names**, both at 1 failed / 77 passed: `wallet_withdrawals` scoped by `destination_ref` fails **quoting the DDL back**, and `earliest_credit_at` deleted from the transcription fails at 18 columns against 19; the loud registration hazard was watched too, the four registered without their `SQL_NAME` entries being **`TS2739` naming all four**. **`fills`, `offers`, `payout_requests` and `promotional_credit_grants` are named by M20 and deliberately NOT registered** -- all four are other modules' tables M20 reads and none is in session 169's `M20-a` -- and `identity_links`, `dedupe_matches`, `otp_challenges` and `trading_calendar` were not declared, not registered and not touched. **COLUMN TYPE AND NULLABILITY REMAIN ASSERTED NOWHERE, NOW OVER 57 TABLES**, four new `nullable` flags among them, `notified_at` transcribed as a `timestamptz` ARRAY and `wallet_withdrawal_status` as a `pgEnum` in `0001`'s member order because a Postgres enum is ORDERED. 74 tests to **78**, suite **1,584 to 1,588**, both baselines measured on this tree; `verify` exit 0 at 8 of 8 invariants and 106 files; 30 of 30 gates; `falsify:ci CI-01` **12 of 12** with `git status --porcelain` read immediately after it at zero |
| [2026-08-25 - Session 203](2026-08-25-session-203.md) | **[M21](../plans/M21-plan-designer.md)'s tables under [ADR-092](../decisions/ADR-092.md). TWO OF EIGHT, AND SIX OF THE EIGHT WERE ALREADY REGISTERED, which is the outcome the ruling predicts for a module whose reads are the catalogue.** `plans` and `simulation_runs` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **53 of 111 becomes 55 of 111**, 27 of the 55 `firm` and 28 reachable through the scoped accessor, every figure recomputed from `TABLE_KEYS.length` rather than incremented. **No ADR, no migration, no new document.** **BOTH ARE `firm` AS A REFUSAL AND NOT AS A DEFAULT**: `plans` has no identity column at all and no correct one, being the catalogue's root where `plan_versions` is its child -- a version names its plan and an account names its version, so ownership flows FROM the catalogue -- and `simulation_runs` is a projection over a PARAMETER SET whose population is synthetic, so no person's rows are read to produce it. **`simulation_runs.requested_by` IS A WEAKER TRAP THAN `treasury_balances.recorded_by`, NOT A STRONGER ONE**: it is bare `text NOT NULL` on `dual_control_approvals.requested_by`'s precedent with no foreign key at all, so it names no `users` row to derive through, and had it been one, scoping by it would return the firm's plan economics to whichever operator pressed the button. **THE MODULE'S CANONICAL TRAP WAS SEEDED AND WATCHED**: `simulation_runs.plan_version_id` is a foreign key to `plan_versions` that reads exactly like `daily_marks`' hop to `accounts` and is not one, because `plan_versions` is firm; declared `derived` it compiles everywhere, and the bounded chain assertion failed **by name** -- *"simulationRuns derives through simulationRuns -> planVersions, and planVersions is firm"*. Two more seeds watched: `plans` declared `owned` on `id` is refused by the DDL check quoting `id uuid PRIMARY KEY DEFAULT gen_random_uuid()` against the requirement that an owned column be declared `REFERENCES identities(id)`, and `plans.sort_order` deleted from the transcription fails the per-table drift assertion by column name. **[ADR-094](../decisions/ADR-094.md)'s FOLD APPLIES NOTHING TO EITHER TABLE, measured over all 47 migrations**: no `ALTER TABLE` statement in the tree names `plans` or `simulation_runs` in any shape, so both are among the 99 undrifted and their comparisons are byte-identical to a pre-ADR-094 one. **`competitor_plan_models` (`SD-M21-03`) IS NOT ONE OF THE 111 AND CANNOT BE TRANSCRIBED**: M21 section 2.1 declines to claim a migration number for it and `OQ-M21-06` still carries it, so there is no `CREATE TABLE` to read and a declaration here would be a transcription of nothing. **`plan_size_unlocks` WAS CONSIDERED AND DELIBERATELY LEFT TO SESSION 201**: [`0044`](../../packages/db/migrations/0044_fee_back_and_ladder_unlock.sql) attributes it to `SD-M18-04`, [M18](../plans/M18-graduation-track.md) names it four times and M21 names it **zero**, and it is `owned` on `identity_id uuid NOT NULL REFERENCES identities(id)` rather than firm -- an argument that belongs to the plan that owns the table's behaviour, which is [ADR-092](../decisions/ADR-092.md)'s own reason for making the owner the table. **ONE NON-EDIT IS REPORTED RATHER THAN MADE**: `plan_versions.plan_id` is `uuid NOT NULL REFERENCES plans(id)` inline in `0004`, so registering `plans` satisfies the schema header's "only when" condition for it, and the line is session 182's, `.references()` is compared by nothing in this package, and touching a table that is not mine is the tail-splice hazard tranche 1 paid for. **THE TYPE AND NULLABILITY AXIS IS STILL ASSERTED NOWHERE**, now over 55 tables, and nothing here implies otherwise. `scoped-db.test.ts` 74 tests to **76**, suite total 1,584 passed / 1 skipped to **1,586 passed / 1 skipped**, both baselines measured on this tree rather than inherited; `typecheck` exit 0; `verify` exit 0 at 8 of 8 invariants, 106 files, 30 of 30 gates, 68 scope cases; `falsify:ci CI-01` **12 of 12**. **THE PREDICTED CONFLICT THEN ARRIVED**: [session 197](2026-08-25-session-197.md) merged first, `main` moved to `92ea3d9`, and all five files conflicted textually, every one keep-both and every one resolved STRUCTURALLY because git aligned two of them on the `createdAt` / `});` tail these tables share. **The merge result is 58 of 111**, 27 firm and 31 reachable, counted from the merged tree; suite **1,587 to 1,589**, `scoped-db.test.ts` **77 to 79**, +2, one per registered table. Section 9 |
| [2026-08-25 - Session 204](2026-08-25-session-204.md) | **FIVE OF FIVE, MERGED IN THE DISPATCHED ORDER, AND EVERY COMPLETION FIGURE WAS READ BACK OFF THE MERGED TREE RATHER THAN ADDED UP FROM THE BRANCHES.** [Session 198](2026-08-25-session-198.md)'s M10 three, [session 203](2026-08-25-session-203.md)'s M21 two, [session 199](2026-08-25-session-199.md)'s M13 five, [session 201](2026-08-25-session-201.md)'s M18 three and [session 200](2026-08-25-session-200.md)'s M17 four. **56 of 111 becomes 73 of 111**, at **1 root, 29 owned, 12 derived and 31 firm**, so **42 of the 73 are reachable through the scoped accessor**. **No ADR, no migration, no new document, and no transcription: an integration session rules nothing and this one wanted no number.** **`pnpm run typecheck` WAS RUN AFTER EACH OF THE FIVE MERGES AND NOT ONLY AT THE END**, exit 0 with zero `error TS` every time, which is what makes the count below a bisect that never had to run. **EVERY RESOLUTION WAS STRUCTURAL AND NONE WAS A HUNK-LEVEL KEEP-BOTH**: each of the three code files was rebuilt from the CURRENT TREE's copy and the incoming branch's WHOLE declarations re-appended, after asserting the branch byte-identical to its base outside the header count, because every `pgTable` ends with the same `createdAt` / `updatedAt` / `});` tail and git aligns the hunk on lines two unrelated tables merely happen to share. **THE RESOLVER REFUSED ONE LIFT RATHER THAN TRUNCATING IT**: `analytics_snapshots` uses the three-argument `pgTable` form for its composite primary key and closes on `);` rather than `});`, and the splitter threw instead of ending the declaration at the wrong line. **THE REGISTRY SIZE IS A LITERAL IN SIX PLACES, NOT FIVE**, and the sixth is the drifted-table sentence: `promotional_credit_grants` carries a later `ADD COLUMN`, so schema.ts's header moves from EIGHT to NINE, and that was DERIVED by replaying every `ALTER TABLE` across all 47 migrations rather than taken from the branch. **THE SEVEN DELIBERATE REFUSALS ALL SURVIVE, CHECKED BY NAME AFTER EVERY MERGE**: `identity_links`, `attributions`, `affiliate_commissions`, `dedupe_matches`, `otp_challenges`, `trading_calendar` and `competitor_plan_models`, each at zero occurrences in `TABLES` and in `SCOPE_RULES`, with `identity_links` still carrying no `pgTable` declaration at all. **THE `plans` CROSS-BATCH COLLISION THEN BECAME REAL AND WAS SETTLED BY DROPPING, NOT MERGING**: tranche A landed on `main` at `9cf3feb` while this branch waited, taking the registry to 64, so a SIXTH merge followed and **`plans` is the ONLY table the two tranches share**. `main` carried M04's copy first, so **M04's stays and session 203's was dropped** from `schema.ts`, `scope.ts` and `SQL_NAME`, leaving `plans` exactly once in each of the five sites; the failure mode if it had been merged is `TS1117` and it was never reached. **The agreement was verified at both sources before the drop rather than assumed**: both declare it `firm`, both give no scope column and say there is no correct one, and both give the same four reasons in different words. **On that merge the rebuild direction was deliberately INVERTED**, the code files rebuilt from `main`'s copy so M04's `plans` survives, with this branch's sixteen non-`plans` declarations appended to it. **The drifted-table sentence is where two independent increments would have merged green and wrong**: tranche A made it NINE for `payout_requests` and tranche B made it NINE for `promotional_credit_grants`, and the derived answer is **TEN carrying both**. Final registry **80 of 111** at 1 root, 34 owned, 14 derived and 31 firm, 49 reachable; `scoped-db.test.ts` **101**; `typecheck` exit 0; 30 of 30 gates; `verify` exit 0 |
| [2026-08-25 - Session 205](2026-08-25-session-205.md) | **[M02](../plans/M02-rithmic-bridge.md)'s tables under [ADR-092](../decisions/ADR-092.md) and [ADR-094](../decisions/ADR-094.md). SEVEN OF SEVEN, AND NOTHING HAD BEEN CLAIMED BY A SIBLING**: `account_status_history`, `platform_account_refs`, `provisioning_queue`, `platform_entitlements`, `ingest_files`, `raw_ingest_rows` and `reconciliations`, spanning **three migrations** (`0007`, `0013`, `0014`), which is unusual for one module and is the whole of M02's schema footprint outside the six tables already registered. **80 of 111 becomes 87 of 111**, 33 of the 87 `firm` and 54 reachable through the scoped accessor, every figure recomputed from `TABLE_KEYS.length` rather than incremented. **No ADR, no migration, no new document.** **THE PREMISE WAS VERIFIED BEFORE A LINE WAS TRANSCRIBED AND IT HOLDS.** M02 is the one plan in the corpus that is not `approved`, held at `review` by [ADR-005](../decisions/ADR-005.md) pending the Rithmic vendor call; all six of its schema deltas `SD-M2-01` to `SD-M2-06` are **already folded into merged migrations**, so there is no table in the slice whose `CREATE TABLE` is unmerged, and **not one of the sixteen `V-M2-nn` rows changes a tenancy column or a join path** -- they move wire format, parser shape, confirmation semantics, billing UNITS and ref recycling, and the closest any comes is `V-M2-09`, which is why `platform_entitlements.platform_user_ref` exists as `text NULL` with no foreign key beside an `account_id uuid NOT NULL`. Reported plainly: the premise holds, and section 2 is what was checked. **FIVE `derived` THROUGH `accounts` IN ONE HOP AND TWO `firm`.** `ingest_files` is firm because a vendor file carries rows for every account that traded the session, its ONLY foreign key points at ITSELF, and the tenancy runs the other way through `fills`, `raw_ingest_rows` and `reconciliations`. **`raw_ingest_rows` IS FIRM AS A CONSEQUENCE AND IT CARRIES THE SLICE'S REAL FINDING.** Its only reference is to the firm `ingest_files`, so the forward derivation is [ADR-094](../decisions/ADR-094.md)'s named trap -- but **the REVERSE edge is worse and it was SEEDED AND WATCHED PASSING**: `fills.raw_row_id bigint NOT NULL REFERENCES raw_ingest_rows(id)` is a declared FK in the direction the derived-rule assertion accepts, so `derived via fills, localColumn id, foreignColumn raw_row_id, semi-join` **compiles, resolves, terminates at an owned table, and leaves the suite GREEN AT 108 OF 108**, rendering `exists (select 1 from "fills" where ("fills"."raw_row_id" = "raw_ingest_rows"."id" and exists (select 1 from "accounts" ...)))`. It is still wrong: only SOME raw rows become fills, and an EOD balance row, an unparsed row and **every row of a quarantined file** become none, so the reading silently drops exactly the rows a dispute is argued from. **That is session 202's `wallet_entries` finding recurring on a different table, and the green suite is not the check.** Two more seeds watched failing loudly: `reconciliations` declared `derived via ingestFiles` fails the bounded chain assertion **by name**, and `provisioning_queue.payload_hash` deleted from the transcription fails the per-table drift assertion with the column named. **[ADR-094](../decisions/ADR-094.md)'S FOLD APPLIES NOTHING TO ANY OF THE SEVEN, re-derived over all 47 migrations with the suite's own multiline match**: no `ALTER TABLE` names any of them in any shape, so all seven are among the 99 undrifted and **the drifted-table sentence stays at TEN, DERIVED rather than left alone** -- ten registered tables carry `ADD COLUMN` and zero carry a refused verb. **`payload_hash` AND `sha256` ARE `bytea` CUSTOM TYPES AND NOT `text`**, on the file's own rule. **ONE NON-EDIT IS REPORTED RATHER THAN MADE**, session 203's form: registering `ingest_files` and `raw_ingest_rows` satisfies the schema header's "only when" condition for `daily_marks.ingest_file_id` (`:606`) and `fills.ingest_file_id` / `fills.raw_row_id` (`:2915`, `:2917`), whose comments now read *"not one of this file's tables"* wrongly; `.references()` is compared by nothing in this package and touching a table that is not mine is the tail-splice hazard tranche 1 paid for. **`events` WAS NOT REGISTERED AND WAS NEVER A CANDIDATE**: M02 names it once, its refusal is already at [`scope.ts`](../../packages/db/src/scope.ts), and this session neither reopened nor weakened it. **THE TYPE AND NULLABILITY AXIS IS STILL ASSERTED NOWHERE**, now over 87 tables, and nothing here implies otherwise. `scoped-db.test.ts` **101 to 108**, full suite **1,611 to 1,618 passed / 1 skipped**, both baselines measured on this tree by checking `origin/main`'s three files back in and re-running rather than inherited, and each moved by exactly seven, one per registered table; `typecheck` exit 0 with zero `error TS`; `gates` 30 of 30; `verify` exit 0; `falsify:ci CI-01` **12 of 12**. `git fetch origin main` at both ends returns `4fa5b33` with `TABLES` at eighty, so no sibling merged into the pair while this branch was open. **MONEY PATH, `E2` READ OWED.** Section 9 |
| [2026-08-25 - Session 206](2026-08-25-session-206.md) | **[M14](../plans/M14-loyalty-retention.md)'s and [M15](../plans/M15-discord-integration.md)'s tables under [ADR-092](../decisions/ADR-092.md). THREE OF THREE FOR M14 AND TWO OF TWO FOR M15, reported separately because a combined figure hides which module was covered.** `loyalty_criteria`, `loyalty_states`, `loyalty_benefit_grants`, `discord_links` and `discord_announcements` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **80 of 111 becomes 85 of 111**, at 1 root, 37 owned, 14 derived and 33 firm, so **52 of the 85 are reachable through the scoped accessor**, every figure recomputed from `TABLE_KEYS.length` rather than incremented. **No ADR, no migration, no new document, and no plan edited.** **BOTH SLICES CAME IN AT FULL SIZE AND THAT IS THE UNUSUAL OUTCOME**, not the expected one: [session 203](2026-08-25-session-203.md) registered two of eight and [session 202](2026-08-25-session-202.md) registered zero of four, and the reason these five survived is that both modules sit at the edge of the estate -- M14's tables are named by M14 alone and M15's by M15 alone, so no earlier wave had a reason to reach them. **THE SLICE WAS COMPUTED AND NOT RECEIVED**: whole-word match of all 111 migration table names against the two plans returns twelve hits for M14 and nine for M15, and all but five were already registered or refused. **`events` IS THE HIT BOTH PLANS SHARE AND IT WAS REFUSED IN BOTH**, its refusal already standing at [`scope.ts:157`](../../packages/db/src/scope.ts); registering it would have made this seven rather than five and would have been the wrong answer twice. **THREE OWNED AND TWO FIRM, AND THE FIRM PAIR IS A REFUSAL RATHER THAN A DEFAULT**: `loyalty_criteria` holds the PUBLISHED DEFINITION of a benefit rather than an instance of one, so ownership flows FROM `loyalty_benefit_grants` which cites it and never toward it, and `discord_announcements` is Merit speaking to a public ROOM, whose trader-facing twin for the same fact is the `notifications` row that does carry an identity. **THE MODULE'S CANONICAL TRAP IS `loyalty_benefit_grants`' COMPOSITE FOREIGN KEY AND IT WAS SEEDED AND WATCHED**: `loyalty_benefit_grants_criteria_fk` points at `loyalty_criteria (benefit_code, version)`, `loyalty_criteria` is firm, and `DerivedRule.via` is `TableKey` and includes every firm key -- declared `derived` it compiled and **three assertions fired, the chain one by name**: *"loyaltyBenefitGrants derives through loyaltyBenefitGrants -> loyaltyCriteria, and loyaltyCriteria is firm"*. It is also a TWO-COLUMN edge against a `DerivedRule` that names one, so the rule could not have been written truthfully even had the parent carried an identity. Three more seeds watched: `loyalty_criteria` declared `owned` on `benefit_code` is refused by the DDL check quoting `benefit_code text NOT NULL` against the requirement that an owned column be declared `REFERENCES identities(id)`; `discord_links` declared `firm` fails *"no firm table carries a column referencing identities"* by name; and `loyalty_states.tenure_days` and `discord_links.role_opt_ins` deleted from the transcription fail their per-table drift assertions with the column named. **[ADR-094](../decisions/ADR-094.md)'s FOLD APPLIES NOTHING TO ANY OF THE FIVE, DERIVED BY REPLAYING EVERY `ALTER TABLE` ACROSS ALL 47 MIGRATIONS rather than believed from the brief**: no statement in the tree names any of them in any shape, so all five are among the 99 undrifted, **the drifted-table sentence stays TEN and only its denominator moves**, from eighty to eighty-five. **THE REGISTRY SIZE WAS RECOMPUTED IN ALL SIX PLACES AND A SEVENTH STALE NUMBER WAS FOUND WHILE DOING IT**: the comment above the totality test read *"`events` IS ANOTHER OF THE 47"*, a remainder from a wave when 64 were registered, sitting two lines below a correct 31 -- so the same comment stated two different remainders and nothing checked either, which is [ADR-042](../decisions/ADR-042.md)'s prose-is-not-a-control in its cheapest form. **ONE NON-EDIT IS REPORTED RATHER THAN MADE, ON [session 203](2026-08-25-session-203.md)'s PRECEDENT**: `offers.loyalty_grant_id` is `uuid NULL REFERENCES loyalty_benefit_grants(id)` inline in [`0024`](../../packages/db/migrations/0024_offers.sql), so registering the target satisfies the schema header's "only when" condition for it, and the comment beside it now reads falsely. **TWO OLDER INSTANCES OF THE SAME STALENESS ALREADY STAND IN THE FILE** -- `identity_restriction_episodes.flag_id` on `risk_flags` and `account_adjustments.promotional_credit_grant_id` on `promotional_credit_grants`, both saying *"not one of this file's tables"* about tables that now are -- so this is a THIRD instance of a standing condition rather than a new one, `.references()` is compared by nothing in this package, and touching a table that is not mine is the tail-splice hazard wave 2 paid for. **THE TYPE AND NULLABILITY AXIS IS STILL ASSERTED NOWHERE**, now over 85 tables, and nothing here implies otherwise: `link_nonce_hash` and `inputs_digest` are declared as the custom `bytea` and `role_opt_ins` and `breaks_on` as `text[]`, and no assertion in the package would notice had they been written as `text`. **THE SEVEN REFUSALS AND THE FOUR UNREACHABLES ALL SURVIVE, CHECKED BY NAME**: `identity_links`, `attributions`, `affiliate_commissions`, `dedupe_matches`, `otp_challenges`, `trading_calendar`, `events`, `payment_disputes`, `affiliate_statements`, `ledger_halts` and `identity_signal_weights`, each at zero occurrences in `TABLES`, in `SCOPE_RULES` and as a `pgTable`. `scoped-db.test.ts` **101 to 106**, suite total **1,611 to 1,616 passed / 1 skipped**, both baselines measured on this tree before a line was edited, **+5 each, one per registered table**; `typecheck` exit 0 with zero `error TS`; `gates` 30 of 30; `verify` exit 0 at 8 of 8 invariants and 68 scope cases. **`git fetch origin main` returned `4fa5b33` at both ends of the transcription**, so no sibling merged into the pair while the five tables were being written and the slice computed is the slice finished. **`main` THEN MOVED TO `fe05a2e` AND WAS MERGED IN, AND THE MOVE IS NOT A TRANSCRIPTION**: it is [session 183](2026-08-24-session-183.md)'s `P4-c` installing Next.js plus [ADR-095](../decisions/ADR-095.md), [ADR-099](../decisions/ADR-099.md) and [ADR-091](../decisions/ADR-091.md) being signed, so **none of sessions 205, 207 or 208 has landed and no table collided**. The merge is CLEAN: only [`STATE`](../STATE.md) and [`sessions/README`](README.md) changed on both sides and both auto-merged, the three code files are untouched by `main`, and `gates.mjs generate` reproduces the merged `sessions/README` byte for byte rather than disagreeing with it. **Re-verified on the merged tree rather than assumed**: `typecheck` exit 0, `scoped-db.test.ts` **106**, `TABLE_KEYS` **85**, suite **1,616 passed / 1 skipped** unchanged because `main`'s move added no tests, and **scope cases 68 to 69, which is `main`'s and not this branch's**. **THE PREDICTED CONFLICT THEN ARRIVED AND IT WAS THE TABLE ONE**: [session 205](2026-08-25-session-205.md) merged at `7f8215f` taking `main` to **87 of 111**, and ALL FIVE files conflicted textually. **No table collided** -- M02's seven and this session's five are disjoint, so nothing rebased out -- and **every resolution was STRUCTURAL**: each of the three code files was rebuilt from `main`'s copy and this branch's WHOLE declarations re-appended, after proving this branch's diff against the merge base is a 217-line APPEND plus three header lines in `schema.ts`, one docblock line plus 41 added lines in `scope.ts`, and five `SQL_NAME` pairs plus the count literals in the suite -- nothing reordered and no line of another table touched. **THE TWO DOCUMENT CONFLICTS ARE THE TWO SHAPES [session 204](2026-08-25-session-204.md) SEPARATED AND THEY WERE RESOLVED DIFFERENTLY ON PURPOSE**: the CLAIM table is KEYED, so the two sides held rows 205 and 206 each in two states and keep-both would have answered *has 205 been written* with yes and no at once -- the struck copy of each was kept and the stale live twin dropped -- while the generated entry table and both STATE sections are APPENDS and were kept whole in numeric order. **`gates.mjs generate` REWRITES NOTHING on the resolved tree**, which is the only thing that separates a plausible keep-both from one that agrees with its generator. **Merged registry 92 of 111** at 1 root, 37 owned, 19 derived and 35 firm, **57 reachable**, remainder **19**, every literal recomputed from `TABLE_KEYS.length`; **the drifted-table sentence stays TEN, DERIVED by replaying all 47 migrations against the merged 92 rather than carried over** -- twelve tables carry a column change and `otp_challenges` and `trading_calendar` are still the two that are unregistered. `scoped-db.test.ts` **108 to 113** and the suite **1,618 to 1,623 passed / 1 skipped**, **+5 each against `main`'s post-205 baseline**, one per table this branch registers; `typecheck` exit 0 zero `error TS`; `verify` exit 0 at 8 of 8 invariants, 30 of 30 gates and 69 scope cases. **CI-02 THEN WENT RED ON A TEST THIS DIFF DOES NOT TOUCH, AND THE RE-RUN IS WHAT SETTLED IT RATHER THAN THE WORD 'FLAKE'**: `settlement-sequence.property.test.ts`'s *R-44: the ceiling is exercised in both directions* failed with *no settlement divided the split exactly*, and `git diff 7f8215f..HEAD` outside `packages/db/` and `docs/` is **ZERO FILES**, so `packages/rules-engine`, `vitest.config.ts` and the lockfile are byte-identical to the `main` whose own CI was green. **THE ASSERTION IS AN UNSEEDED COVERAGE MEASUREMENT**: it is `fc.sample(settlementSequenceArbitrary(), 400)` with no seed and no `fc.configureGlobal` anywhere in the repo, so it draws differently every run and then asserts a drawn case exists. **The margin was MEASURED rather than assumed** -- 300 trials of that exact computation on this tree gave `exact.length === 0` **zero times**, min 2, p05 7, median 14, max 29 -- so the red is a thin left tail nothing bounds. One re-run, the only one the discipline allows, came back **green on all 14 checks**. **THE FIX IS OUTSIDE THIS FENCE AND IS REPORTED WITH ITS PATCH RATHER THAN TAKEN**: seed the sample, so a measurement of the generator's SUPPORT stops depending on the draw. **`main` THEN MOVED TWICE MORE AND THE SECOND ONE CHANGED A CLAIM IN THIS LOG.** `e7d5e1d` (sessions 210 and 211) auto-merged clean and was deliberately NOT taken -- a green mergeable branch is not work -- and `4d29b08` carries [session 212](2026-08-25-session-212.md)'s [ADR-101](../decisions/ADR-101.md), which adds **five assertions to the suite, one of which now covers this session's three `owned` rules**: *every owned rule states the nullability the DDL declares*. **It was merged and RUN rather than read**, and all five pass; `loyalty_states`, `loyalty_benefit_grants` and `discord_links` each declare `nullable: false` against an `identity_id uuid NOT NULL`, so the new gate agrees with rules written before it existed. **THE TYPE-AND-NULLABILITY GAP THIS LOG REPORTS IS THEREFORE NARROWED AND NOT CLOSED, AND SECTION 13 STATES THE DIFFERENCE**: nullability is now checked for exactly ONE column per `owned` rule, the identity column, and only as the rule's `nullable` flag; column TYPE is still asserted nowhere, and `schema.ts`'s own `.notNull()` transcription is still unchecked on every other column, `link_nonce_hash`, `inputs_digest`, `role_opt_ins` and `breaks_on` among them. `scoped-db.test.ts` **113 to 118**, suite **1,650 to 1,675 passed / 1 skipped**, `TABLE_KEYS` **92**, `typecheck` exit 0, `verify` exit 0 at 30 of 30 gates and 70 scope cases -- **+5 on the suite is still this branch's five tables and the other +20 is session 212's**. Sections 1, 8, 10, 11, 12 and 13 |
| [2026-08-25 - Session 207](2026-08-25-session-207.md) | **[M09](../plans/M09-marketing-site.md)'s, [M11](../plans/M11-certificates-social-proof.md)'s and [M16](../plans/M16-notification-center.md)'s tables under [ADR-092](../decisions/ADR-092.md). FOUR OF FOUR, and the honest form of that number is that nothing was already taken and nothing was refused.** `geo_restrictions`, `tos_versions`, `tos_acceptances` and `certificate_verifications` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **80 of 111 becomes 84 of 111**, at 1 root, 35 owned, 14 derived and 34 firm, so **50 of the 84 are reachable through the scoped accessor**, every figure recomputed from `TABLE_KEYS.length` rather than incremented. **No ADR, no migration, no new document, and `docs/plans` was read only.** **THE PAIR IS THE POINT AND ITS TWO CLASSES DISAGREE**: `tos_versions` and `tos_acceptances` are declared in the same migration, one records what the FIRM published and the other what a PERSON did, and a shared class fails in one direction or the other -- firm for both hands nobody their own signature, owned for both hides a public legal document behind an identity it does not have. `tos_versions` has no identity column and no correct one because every identity is shown the same version and the link runs the other way, which is `plan_versions`' reason on the legal catalogue; `tos_acceptances` carries `identity_id uuid NOT NULL REFERENCES identities(id) ON DELETE RESTRICT` and the DDL has no way to write an unowned acceptance. **THE PAIR ALSO CARRIES THE FILE'S NAMED TRAP IN ITS MOST PLAUSIBLE DRESS**: `tos_version_id` is a `NOT NULL` inline foreign key to a FIRM table, so a `derived` rule through it compiles at every call site and throws on first read; it was SEEDED and the bounded assertion failed **by name**. **`certificate_verifications` IS `firm` BECAUSE THE HOP TO `certificates` DOES NOT EXIST** -- the table declares no foreign key at all and its column is `code_hash bytea`, a digest of the ATTEMPTED code, which addresses no row and resolves to no certificate on every `unknown` result -- **and the class would still be wrong if the join existed**, because the signal the table carries is the RATE of `unknown` across all verifiers (`AS-M11-04`, `FM-M11-04`) and a per-holder slice of an enumeration signal publishes the verifiers instead of the certificate. `geo_restrictions` is `firm` on `price_floors`' shape: the primary key is `country_code char(2)`, the table has no `uuid` of its own, nothing in the tree references it, and a restriction is a statement about a JURISDICTION that is identical for every person in it, which is the property `DEP-M9-04` depends on. **[ADR-094](../decisions/ADR-094.md)'s FOLD APPLIES NOTHING TO ANY OF THE FOUR, DERIVED RATHER THAN BELIEVED**: all 84 `ALTER TABLE` statements over the 47 migrations were extracted with the suite's own multiline match and none names any of the four in any shape, so **the drifted-table sentence stays TEN and only its denominator moves**, and it cannot be raised by a concurrent sibling either, because of the 31 tables unregistered on open exactly two carry a later column change and both are standing refusals. **`events` AND `otp_challenges` APPEARED IN THE DERIVATION AND WERE LEFT ALONE**, their refusals already written in `scope.ts` and in ADR-094 section 3, and none of the four tables ADR-092 section 9 rules unreachable appeared at all, which is the check that the slice was not mis-derived. **THREE SEEDS WATCHED FAILING BY NAME AND EACH RESTORED**: the firm-via trap, a column deleted from the `certificate_verifications` transcription failing the per-table drift assertion, and `geoRestrictions` declared `owned` on `country_code` refused by the DDL check quoting `country_code char(2) PRIMARY KEY`. **COLUMN TYPE IS ASSERTED NOWHERE AND NULLABILITY ONLY ON AN OWNED RULE'S SCOPE COLUMN**: [ADR-101](../decisions/ADR-101.md) landed while this branch waited and closed that one column, which `tos_acceptances.identity_id` satisfies, and nothing else on the axis is checked. `git fetch origin main` returns `4fa5b33` at BOTH ends of the session with `TABLES` at eighty, so no sibling merged into the pair while this branch was open. `scoped-db.test.ts` **101 to 105** and the full suite **1,611 to 1,615 passed / 1 skipped**, both +4, one per registered table; `typecheck` exit 0 with zero `error TS`; `gates` 30 of 30; `verify` exit 0. **THE PREDICTED COLLISION THEN ARRIVED AND COST NOTHING, BECAUSE THE TWO TABLE SETS ARE DISJOINT**: [session 205](2026-08-25-session-205.md)'s `M02` merged first and took `main` to **87**, all five files conflicted textually, **none of its seven intersects these four**, so nothing was dropped and the merged registry is 87 + 4 = **91 of 111** at 1 root, 35 owned, 19 derived and 36 firm, **55 reachable**, counted off the merged tree. **Every code-file resolution was STRUCTURAL** -- each rebuilt from `main`'s whole copy with these four whole declarations re-appended, because git aligns the hunk on the `createdAt` / `updatedAt` / `});` tail every `pgTable` shares -- while the two documents are the other kind, an appended section and a generated row, kept both in numeric order. **THE DRIFTED-TABLE SENTENCE WAS RE-DERIVED OVER THE MERGED 91 AND NOT TAKEN FROM EITHER SIDE**: still **TEN**, the same ten, with zero registered tables carrying `DROP COLUMN`, `ALTER COLUMN` or `RENAME`, so only the denominator moved to *ninety-one*. **The seven refusals all survive, checked by name after the merge**, each at zero occurrences in `TABLES` and `SCOPE_RULES`, and `identity_links` still carries no `pgTable` at all. **A SECOND MERGE FOLLOWED AND CONFLICTED ONLY IN THE DOCUMENTS**: [session 209](2026-08-25-session-209.md)'s `P4-d` took `main` to `4fd4a8a` and registered no table, so the registry pair did not conflict at all and both conflicts were the appended-section and generated-row kind, kept both. Merged `scoped-db.test.ts` **108 to 112**, +4 against session 205's baseline; full suite **1,665 to 1,669 passed / 1 skipped**, also +4, against a `main` baseline that moved three times while this branch waited (1,618, 1,645 with session 209, 1,665 with 210 and 211), because the suite total is a fact about the whole tree and the +4 is the fact about this branch. **SEVEN MERGES OF `main` IN ALL, AND ONLY TWO TOUCHED THE REGISTRY**: sessions 209, 210, 211, the 211 follow-up and 212 registered no table between them and conflicted only in the documents, while 205 and 206 each collided on all five files and each was disjoint from these four, so nothing was ever dropped. **These four then landed on `main` through the wave-3 integration** ([#278](https://github.com/lam2-coder/meritfutures/pull/278)) rather than through this session's own pull request, byte-identical in both files, and the registry reached its ceiling of **99 of 111**. Sections 1 to 7 |
| [2026-08-25 - Session 208](2026-08-25-session-208.md) | **THREE OF FOUR, AND THE FOURTH IS THE FINDING.** `idempotency_keys` ([M10](../plans/M10-integrations.md)), `trading_calendar_loads` ([M06](../plans/M06-admin-ops-console.md)) and `trading_calendar_revisions` ([M01](../plans/M01-rules-engine.md)) are in [`schema.ts`](../../packages/db/src/schema.ts), [`scope.ts`](../../packages/db/src/scope.ts) and `SQL_NAME`; **`identity_merges` ([M18](../plans/M18-graduation-track.md)) is refused and, on [session 192](2026-08-25-session-192.md)'s stronger model, NOT DECLARED IN `schema.ts` AT ALL**, because a declared-but-unregistered `pgTable` is one no drift assertion compares. **80 of 111 becomes 83 of 111 against this session's base and 92 becomes 95 ON THE MERGE RESULT**, sessions 205's seven and 206's five having landed while this branch was open, at **1 root, 38 owned, 19 derived and 37 firm** read back off the merged tree, so **58 of the 95 are reachable through the scoped accessor**. **No ADR, no migration, no new document, and `docs/plans/**` was read and not edited.** **THE SLICE WAS COMPUTED AND NOT RECEIVED**, from `TABLE_KEYS` on the tree opened at `4fa5b33`: eleven candidates across the four plans, of which seven are standing refusals and two are genuinely a sibling session's (`account_status_history` to 205, `loyalty_benefit_grants` to 206) and were LEFT rather than raced. **`M01`'s slice is ONE table because the corpus's heaviest reader of `events` -- twenty mentions, more than any other plan -- cannot have it**, and that refusal was not re-argued. **`identity_merges` IS THE FOURTH MEMBER OF `identity_links`' AND `dedupe_matches`' CLASS**: `surviving_identity_id` AND `merged_identity_id`, both `uuid NOT NULL REFERENCES identities(id)`, **one index on each**, and `identity_merges_distinct` GUARANTEEING they name different people -- so naming the survivor drops the row from the merged identity `INV-M7-06` keeps alive precisely to argue a grandfathered cap from, and naming the merged one drops it from the survivor the cap now binds; both return rows, neither raises, and `firm` is refused by the suite's own no-firm-table-references-identities assertion. **THE ARGUMENT FOR TAKING THE SURVIVOR IS RECORDED RATHER THAN LEFT TO BE RE-DERIVED, BECAUSE IT IS THE STRONGEST ANY OF THE FOUR REFUSALS HAS FACED**: a merge repoints ownership into the surviving row and `users.md` says one identity holds several users only through a merge, so the merged side arguably never holds a scoped session -- **but that is an inference about application behaviour from prose and the DDL says the opposite twice**, and the `distinct` CHECK guarantees the row hands the reader another identity's uuid beside `evidence jsonb` written by a detector, which `INV-M4-06` forbids. **Wanting a number for it was taken as the stop signal.** **A NEIGHBOUR'S FOLD REFUSAL DOES NOT TRAVEL, AND IT WAS DERIVED RATHER THAN BELIEVED**: both new `0032` tables sit in the same migration as `trading_calendar`'s two `ALTER COLUMN ... DROP NOT NULL`, and replayed across all 47 migrations with the suite's own multiline match **neither carries an `ALTER TABLE` of ANY shape**, each created exactly once. **Seeded and watched**: registering `trading_calendar` turns `no later migration changes a column in a shape ADR-094 does not fold` RED **with both statements quoted by filename**, while its two siblings stay green in the same run. **THE TRAP FIRST BECAUSE IT COMPILES**: `tradingCalendarRevisions` declared `derived` through the FIRM `tradingCalendarLoads` leaves `typecheck` at **exit 0, zero `error TS`** and turns THREE assertions red naming the firm table; `dependent_row_count` deleted fails the per-table comparison; `trading_calendar_loads` declared `owned` on `actor` is refused by the DDL check quoting `actor text NOT NULL` back; **`TS2739` naming all three new keys** was watched before `SQL_NAME` was completed. **`idempotency_keys` IS `ledger_accounts`' SHAPE AND NOT `offers`'**: its `identity_id` is nullable with **no CHECK making it biconditional**, so NULL means no identity was recorded and never that the firm owns the row, and `idempotency_keys_identity_idx` is declared `WHERE NOT NULL`, the database already treating the two populations apart. **THE SIX SIZE LITERALS WERE RECOMPUTED FROM `TABLE_KEYS.length` AND THE SEVENTH WAS DERIVED**: the drifted-table sentence stays **TEN**, replayed over every `ALTER TABLE` rather than incremented, because [session 204](2026-08-25-session-204.md) is where two branches raised it to NINE for different tables. **+3 IN BOTH FIGURES AGAINST THREE SUCCESSIVE `main`s**: `scoped-db.test.ts` **101 to 104** and the suite **1,611 to 1,614** against `4fa5b33` and again against `fe05a2e`, then **108 to 111** and **1,618 to 1,621** against `7f8215f` carrying session 205's seven, then **108 to 111** and **1,645 to 1,648** against `4fd4a8a` adding session 209's route registry, then **108 to 111** and **1,665 to 1,668** against `e7d5e1d` adding sessions 210 and 211, then **113 to 116** and **1,670 to 1,673** against `4d29b08` adding session 212's ADR-101, then **118 to 121** and **1,675 to 1,678** against `8914c66` carrying session 206's five and again against `c0205ec`'s repo-wide import fix -- **the whole-suite base moved by 64 across eight merges and the delta never moved at all** -- one per registered table and no fourth every time, **every baseline MEASURED by checking out `origin/main`'s three files into this tree and running rather than quoted or inferred**; `typecheck` exit 0, `verify` exit 0, 30 of 30 gates, `lint` and `format:check` clean. **ALL SEVEN STANDING REFUSALS SURVIVE, CHECKED BY NAME**, and `identity_merges` joins them as an eighth. **THE NULLABILITY HALF OF THIS SESSION'S NAMED GAP WAS NARROWED BY [SESSION 212](2026-08-25-session-212.md) WHILE THE BRANCH WAS OPEN AND THE CLAIM IS CORRECTED RATHER THAN LEFT STANDING**: [ADR-101](../decisions/ADR-101.md) added `every owned rule states the nullability the DDL declares`, so `idempotency_keys.identity_id`'s `nullable: true` is now MECHANICALLY VERIFIED against the folded DDL rather than merely transcribed. **Column TYPE is still asserted nowhere across all 95 tables and nullability only for the ONE scope column per owned rule**, which is why `request_hash` and both `source_digest` columns being `bytea` rather than `text` remains unchecked. **THE ONE STALE FIGURE THIS SESSION DECLINED TO REPAIR WAS REPAIRED BY THE SIBLING THAT MERGED FIRST**: `scoped-db.test.ts`'s `` `events` IS ANOTHER OF THE 47 `` was left alone here on the ground that a hunk in a file three concurrent sessions are appending to costs more than a stale word, [session 205](2026-08-25-session-205.md) judged the opposite from its own side, and the merge result carries its repair at 21 in all three places |
| [2026-08-25 - Session 209](2026-08-25-session-209.md) | **THE DELIVERABLE IS THE SHAPE AND THE SHAPE IS THAT THE MODULE LIST IS NEVER WRITTEN DOWN.** [ADR-100](../decisions/ADR-100.md): a route module is one file under `apps/api/src/routes/` that default-exports `defineRoutes({ name, routes })`, `discoverRouteModules` reads the directory and imports every `.ts` file in sorted order, and **no file outside that directory imports anything inside it**. A slice that adds a route **adds one file and edits nothing another slice edits**, so the **fifteen** slices [P4](../plans/P4-portal-and-site.md) section 9 rows behind this file -- `P4-f`, M03's two, M05's two, M16's three and [M19's seven](2026-08-24-session-168.md) -- touch fifteen disjoint paths and merge in any order. **`apps/api/src/routes/index.ts`, which section 9 names as the BARRIER, is NOT CREATED and is NOT NEEDED**: that is the barrier removed rather than scheduled. **The three properties an array gives free are bought back rather than assumed**, each watched: `defineRoutes` types the shape at the DEFINITION site so `tsc` reports a malformed module in the module's own file; discovery THROWS on any `.ts` file that is not a valid module and on a name that is not its filename stem; and `compose` refuses a duplicate `METHOD /path` across the module set **before** the surface filter -- the collision fifteen concurrent branches make likely and **the one an array would have merged**. **A module declares a PATH and never a SURFACE**, so [ADR-083](../decisions/ADR-083.md) section 4's 404 stays the router's. **WATCHED OVER A REAL SOCKET IN TWO PROCESSES COMPOSING THE SAME MODULE SET**: `GET /api/v1/internal/jobs` answered **404 `application/problem+json`** on `api` and **200** on `api-admin`, the public process logging `"registered":["GET /health"],"withheld":["GET /internal/jobs"]`. **The `VG-12` admission is 46 packages**, closure **233 to 279** and 0 removed, every one resolving to a registry entry carrying a `sha512` integrity hash, `pnpm audit --audit-level=low` exit 0, and **zero declaring `preinstall`, `install` or `postinstall`**. **`VG-3` and `VG-6` are re-ruled off the lockfile onto `scopedDb` named under `apps/api/src`**, which is API_CONTRACT section 1's own mechanism for what both rows are about, and **`CI-06/vg-inventory` was watched failing on the good news at 29 of 30 BEFORE either cell moved**. **THE FALSIFY SEED BROKE AND THAT IS THE DESIGN WORKING**: it seeded a `fastify` key while both rows waited on one, so when a real key landed the anchor stopped existing and the case **THREW** rather than passing, taking `pnpm run falsify` to 1 problem; a harness that matched loosely would have seeded a second key and passed. **TWO FINDINGS REPORTED AND NOT REPAIRED.** First, **no `start` script in this estate has ever run**: `node --experimental-strip-types` does not map `./x.js` onto `x.ts`, and `apps/api`, `apps/admin` and `apps/worker` all die with `ERR_MODULE_NOT_FOUND` on their own first relative import; the repair needs `allowImportingTsExtensions` in a `tsconfig.json` outside this fence and **was executed in a scratch copy to prove it sufficient**, which is where the real-socket run above came from. Second, **[ADR-085](../decisions/ADR-085.md)'s signed clause is the EIGHTH drifted approval clause** -- it says `29 of 29` where the tree is 30 of 30 and says a real `fastify` key makes the gate fail where nothing now waits on one -- and a signed entry moves by a superseding ADR, so `CI-06/vg-inventory/fastify-near-miss` is **RETAINED VERBATIM** rather than deleted or repointed and `scoped-db-near-miss` is added beside it. **THE PLAN DISAGREES WITH ITSELF IN TWO PLACES AND BOTH ARE REPORTED**: `P4-d`'s Contents cell rows `ALLOCATION` and `INDEX` and no ADR file, while section 11 rule 8 names `P4-d` by name as owing one; and it rows `gates.mjs` and not `falsify.mjs`, while section 9, which the plan says is the table to read, rows `falsify.mjs` for `P4-d`. `apps/api` 10 tests to **37**; suite **1,611 to 1,638 passed / 1 skipped**; `typecheck` exit 0; **8 of 8** invariants; **30 of 30** gates; `verify` exit 0; `falsify` 30 gates, **70** scope cases, 10 loader cases; `falsify:ci CI-01` **12 of 12** |
| [2026-08-25 - Session 210](2026-08-25-session-210.md) | **`P4-e` COMPLETE, AND THE FIRST THING IN THIS REPOSITORY A PERSON CAN LOOK AT RENDERS FROM A ROW THAT ARRIVED OVER A SOCKET.** [`apps/site/src/catalog/adapter.ts`](../../apps/site/src/catalog/adapter.ts) implements all four ports as HTTP calls, and **`$310,000.00`, `$799.00` and *"About 4 trading days between payouts"* were rendered from `size_cents: 31000000`, `price_cents: 79900` and `win_days.required_count: 4` served by a real listener**, each asserted equal to the same field fetched back from that listener in the same test run (M9 section 8.3). **THE ROW AND [ADR-096](../decisions/ADR-096.md) DISAGREE ON THE WORDS `systemDb` AND THE ENTRY GOVERNS, WHICH IS AN ORDERING RATHER THAN A CONFLICT**: [P4](../plans/P4-portal-and-site.md) section 10 item 6 named the question OPEN, offered three readings and took none, ADR-096 is that answer, it is `accepted` and signed, and its section 2 shows readings 1 and 2 do not reach a single table the site needs. **NOTHING SPLIT THE DIFFERENCE: zero `packages/db` imports, no pool, no credential, and no manifest line.** **ADR-096 SECTION 9's HOLE IS NARROWED AND NOT CLOSED, MEASURED BY RE-EXECUTING THE ENTRY'S OWN SEED IN A THROWAWAY WORKTREE**: `"@merit/db": "workspace:*"` plus a `systemDb('nightly-batch').rows('accounts')` call still leaves `tsc` at exit 0, `eslint apps packages` at exit 0, `check:invariants` at **8 of 8** and `gates.mjs check` at **30 of 30** -- all four reproduce -- and **the new `apps/site/test/manifest.test.ts` is the fifth place and goes RED**, naming both the manifest line and the source file. **`RI-08` IS STILL UNWRITTEN and the reason is the fence**: it lives in `packages/tooling`, which `P4-e`'s row does not grant, so the repo-wide form covering `packages/queue` is still owed and `check:invariants` still reports eight invariants with `RI-08` absent. **THE PLAN'S ROW IS IN SECTION 8 AND BOTH THE DISPATCH AND [`sessions/README`](README.md) SAY SECTION 11**, which is the rules list; the row was read where it lives. **SIX CONTRACT GAPS ARE REPORTED AND NONE IS REPAIRED**, every one of them refused loudly at the call site instead: `public_slug`, `public_visible` and `superseded_by` are in NO API_CONTRACT response shape though SD-M9-01 stores them and INV-M9-11 cannot be built without the first; `marketed_size_label` is DEP-M9-07's and is owed; **`PlanSize` is referenced twice in API_CONTRACT and declared nowhere in it**; the ARCHIVE half of `CatalogReadPort` has no collection address anywhere, so the adapter reaches the shelf only and the suite asserts that as a property rather than letting a partial archive look whole; and `listAll` and `readRestrictedCountries` throw `UnservedEndpointError` rather than inventing a URL. **THE ONE WIRE DETAIL THIS SESSION CHOSE IS NAMED AND ISOLATED**: no document says how a locale or a version travels to `GET /public/content/:kind/:slug`, so both are query parameters declared as two exported constants. **`BASE_PATH` IS NOT RE-TRANSCRIBED**: `RI-04` forbids a deployable depending on a deployable, so the base URL is configuration and this package spells contract paths only. **The suite is discriminating and was watched being so**: seeding `price_cents` from `reset_price_cents` turns the parity assertion red at `$499.00` against `$799.00`, and deriving `public_slug` from the code and the version turns two tests red because the served slug is deliberately not derivable. `pnpm run verify` exit 0, `typecheck` **zero `error TS`**, **8 of 8** invariants, 30 of 30 gates, suite **1,631 passed / 1 skipped** over 108 files, up 20 from 1,611 over 106. **`apps/site/package.json` was granted and NOT spent: no dependency was added** |
| [2026-08-25 - Session 211](2026-08-25-session-211.md) | **[P3 wave 3](../plans/P3-wave3-modules.md) dispatched at last, `status: draft`, eleven slices over four waves, sessions `213` to `223`.** [P3](../plans/P3-ledger-billing-identity.md) section 6 refused it because its fences were paths inside a deployable `ADR-083` had not named; `ADR-083` is signed, [`apps/api`](../../apps/api/src/index.ts) exists, `P4-d` is dispatched, and the fences are real. **THE BRIEF ASKED FOR THE ROUTE AXIS AND ROUTES ARE NOT THE AXIS.** Every one of P3's contents is a WRITE and [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) gives both accessors `rows()` and nothing else; **[ADR-092](../decisions/ADR-092.md) section 9 says so about itself**, and **sessions [157](2026-08-24-session-157.md) and [168](2026-08-24-session-168.md) proposed the same write slice from inside two different fences without either being able to see the other**, which is ADR-092's own shape one interface over. [`packages/queue`](../../packages/queue/src/job-queue.ts)'s `enqueue` takes a transaction **nothing in the workspace can produce**, so the saga is blocked on the accessor rather than on a queue. **A THIRD ABSENCE NEITHER MEASUREMENT NAMED**: `SystemReason` is closed at two, and `psp_webhook_events`, `coupons` and `integration_contracts` are all `firm`, so three contents have no legitimate reader and [ADR-096](../decisions/ADR-096.md)'s remedy is unavailable because `apps/api` is the process the site reads THROUGH. **`otp_challenges` is refused by SIGNED [ADR-094](../decisions/ADR-094.md) and the remedy is a session that entry SPECIFIES and nobody numbered**; its "seven" is now **80**. **AUTH IS THIRD**: not last because `POST /checkout` is `Auth: session` and section 12 requires its rows before the endpoint ships, not first because of those two preconditions. **[ADR-093](../decisions/ADR-093.md)'s cell edits are APPLIED**, so P3's own six-content table is stale by one row. **The concurrency answer is CONDITIONAL and both branches are dispatchable**: the axis is the ENDPOINT for routes and the PACKAGE for libraries, and ADR-092's third part transfers only if `P4-d` declares the registry total over a closed endpoint union. **The brief's "209 is writing it right now" is false against the tree**: zero branches, zero pull requests, which changes the allocation and not the fences. **Claims sessions 213 to 223 and NO ADR number**, because 209 holds an unclaimed row. **Eight of eight fenced, and the phase's first line of application code is three sessions away rather than one.** **A FIGURE THIS SESSION MEASURED MOVED WHILE THE BRANCH WAS OPEN**: [session 205](2026-08-25-session-205.md) merged at `7f8215f`, the registry went **80 to 87 of 111**, `provisioning_queue` was registered by somebody else, and **`P3-l`'s fence lost all three of its `packages/db` files** while `P3-g`'s cost grew. Both re-derived on the merged tree rather than carried. **THEN [SESSION 209](2026-08-25-session-209.md) MERGED AT `4fd4a8a` AND ANSWERED THE CONDITIONAL THIS PLAN WROTE, BY A MECHANISM IT DID NOT PROPOSE**: section 6.2 asked for a registry total over a closed endpoint union, and [ADR-100](../decisions/ADR-100.md) **removed the shared file instead**, making the module list a DIRECTORY LISTING. **Four route slices are four branches unconditionally**, four fences lost their registry edit, and the `routes/index.ts` collision row is **struck**. **Session 209 also took ADR `100`**, which is this plan's refusal to claim it arriving as an outcome. 30 of 30, verify exit 0 at **111** files / **1,665** passed. No code, no migration, no ADR, nothing signed |
| [2026-08-25 - Session 212](2026-08-25-session-212.md) | **[ADR-101](../decisions/ADR-101.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH and the `E2` read owed. RULED: `derived` is unavailable where the row itself declares a column `REFERENCES identities(id)`, and `derived` may not traverse a NULLABLE edge. `DerivedRule` gains NO `nullable` field and `OwnedRule.nullable` is CHECKED against the DDL instead.** **THE RULING IS FIVE ASSERTIONS IN [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) AND NOT A PARAGRAPH**, on [ADR-042](../decisions/ADR-042.md) and on [CLAUDE.md](../../CLAUDE.md)'s *"prefer a new CI gate over a bigger model whenever the error is checkable"*. **[`packages/db/src/scope.ts`](../../packages/db/src/scope.ts) IS NOT EDITED and no table's rows are touched** -- `git status --porcelain packages/db/src` reports zero -- which is deliberate while sessions 205 through 208 transcribe into that file concurrently. **BOTH OF [SESSION 202](2026-08-25-session-202.md)'s FINDINGS WERE REPRODUCED BEFORE EITHER WAS RULED ON, AND A THIRD WAS FOUND THAT NOBODY HAD**: `wallet_entries` `derived` through `ledger_transactions`, `wallet_withdrawals` `derived` through `risk_flags` on the NULL `freeze_flag_id`, and -- the one that decides the shape of the remedy -- **`sessions` `derived` through `identity_signals` on `device_fingerprint_id`, which is [`scope.ts`](../../packages/db/src/scope.ts)'s OWN NAMED TRAP from its first twenty lines written as a rule.** All three seeded on `main` at `fe05a2e`, all three accepted by the whole estate: `pnpm run typecheck` at exit 0 with **zero `error TS`** and `scoped-db.test.ts` at **101 of 101** on each. **[Session 188](2026-08-25-session-188.md)'s bounded-chain assertion, the derived-FK assertion, the `traversal` assertion and `scopePredicate` all AGREE with all three and none of them is defective**: the chain terminates, the FK is declared, the hop is single-valued, and none of them was ever asking whether the identity it terminates at is the right one. **A HEADER PARAGRAPH IS NOT A CONTROL, and that is measured rather than argued.** **THE CANDIDATE WAS MEASURED AGAINST ALL EIGHTY RULES BEFORE IT WAS ADOPTED AND IT TURNS NOTHING RED**: clause 1 refuses **0 of 14** `derived` rules, clause 2 refuses **0 of 14**, clause 3 passes **34 of 34** `owned` flags, so there is no exemption list because there is nothing to exempt. **CLAUSE 2 IS ALREADY THE REGISTRY'S PRACTICE, ARGUED EIGHT TIMES IN PROSE AND ENFORCED NOWHERE**: `risk_flags`, `coupons`, `coupon_redemptions`, `psp_webhook_events`, `account_adjustments`, `journal_entries`, `promotional_credit_grants` and `wallet_withdrawals` each refuse a derivation on exactly this ground, naming the column and the rows it would drop, and the ninth author wrote the rule anyway. **`OwnedRule.nullable` WAS STATED BY 34 RULES, READ BY NOTHING AND CHECKED BY NOTHING** -- [`scoped-db.ts:84-89`](../../packages/db/src/scoped-db.ts) declines it in its own comment and `grep -n nullable` over the suite returned ZERO over 101 tests -- **which is why a second such field on `DerivedRule` is refused and the existing one is made checkable instead**, four lines, passing on all 34 including `wallet_dormancy` whose `NOT NULL` is carried by `PRIMARY KEY`. **A COMPILE-TIME REFUSAL IS IMPOSSIBLE FOR CLAUSE 1 AND THE REASON IS STRUCTURAL**: the predicate is a fact about the DDL, `tsc` cannot read a migration, and the only encoding is [ADR-092](../decisions/ADR-092.md) section 5's measured two-statements-of-one-fact hazard plus [session 145](2026-08-23-session-145.md)'s rule-asserted-against-itself. **A LIVE FINDING IS RECORDED AND DELIBERATELY NOT REPAIRED**: `no firm table carries a column referencing identities` states a claim that is FALSE about this tree and is green because its reader stops at `CREATE TABLE`, and `admin_actions.on_behalf_of_identity_id` is `ALTER`-added in [`0043`](../../packages/db/migrations/0043_admin_attributed_actions.sql), is the ONLY one of the 31 firm tables that reaches `identities`, and repairing it is a ruling about what the `firm` class PROMISES. **Each seed watched being refused by the clause that NAMES it, and clause 2 fires ALONE on `sessions`**, which is what makes it independently load bearing rather than a refinement of clause 1; two mutations checked, the `PRIMARY KEY` leg failing on `wallet_dormancy` BY NAME and a `CREATE`-only fold failing on `sessions`. **The approval clause is a COMMAND rather than a COUNT**, on the seven that have drifted, and it carries the direction it fails in: a reader that silently stops matching looks exactly like a refusal with nothing to refuse, so the third command must fail **34 times**. `scoped-db.test.ts` **101 to 106**, suite **1,611 to 1,616 passed / 1 skipped**, `typecheck` exit 0, `eslint packages/db` exit 0, 30 of 30 gates, `verify` exit 0 |
| [2026-08-25 - Session 213](2026-08-25-session-213.md) | **[ADR-102](../decisions/ADR-102.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH and the `E2` read owed. RULED in three clauses: `scopedDb(id)` and `systemDb(reason)` are UNCHANGED and stay READ ONLY, a write is reached only through `transaction(handle, fn)`, and an INSERT has no `WHERE` clause so the identity is STAMPED from the handle over `OwnedTableKey` alone; `SqlExecutor` is the shape [`job-queue.ts`](../../packages/queue/src/job-queue.ts)'s `JobTransaction` declares and `transaction()` is its ONLY producer; `firmDb()` serves `FirmTableKey` and takes NO reason, and `SystemReason` stays at two members.** **`P3-f` IS THE PRECONDITION UNDER P3's WHOLE WAVE 3 AND TEN SLICES WERE BEHIND IT.** **NOTHING IN THIS WORKSPACE COULD ENQUEUE A JOB AND IT WAS RE-DERIVED AT SOURCE BEFORE IT WAS FIXED**: `enqueue` takes the caller's open transaction FIRST with no overload that omits it and its own header says `packages/db` *"can satisfy it without exporting its client"*, and `packages/db` did not, so [ADR-006](../decisions/ADR-006.md)'s transactional-enqueue criterion has never been gradeable. **THERE IS NO NON-TRANSACTIONAL WRITE, AND THAT IS `job-queue.ts`'s OWN RULING APPLIED TO THE ACCESSOR RATHER THAN A NEW ONE**: *"THE AWKWARD ONE TO WRITE IS THE NON-TRANSACTIONAL ONE... There is no `enqueueNow`, no optional transaction and no ambient default."* **THE READ INTERFACES DID NOT MOVE AND THAT IS THE MOST USEFUL THING THIS SESSION FOUND.** `CI-01/scope-system-db-not-assignable` seeds a value carrying exactly `__brand`, `reason`, `identityId` and `rows()` and demands the needle `__brand`, on [`falsify-ci.mjs:689-694`](../../scripts/ci/falsify-ci.mjs)'s own stated reason that a missing property *"passes a naive needle while proving the WRONG THING"*. **A required method added to `ScopedDb` makes `tsc` report FOUR MISSING PROPERTIES and the case stops proving the brand.** [`falsify-ci.mjs`](../../scripts/ci/falsify-ci.mjs) is **outside this fence**, so the constraint was met BY DESIGN rather than by widening, and `node scripts/ci/falsify-ci.mjs CI-01` reports **12 of 12**, unchanged, found on `ScopedTableKey` and on `__brand`. **A FOURTH FAILURE CLASS IS THE WRITE PATH'S OWN AND IS THE ONLY ONE THIS ENTRY INVENTS A CONTROL FOR: RE-PARENTING.** A `SET` that moves the tenancy column hands the row to somebody else AFTER the predicate has already agreed to it, and it is least visible on `derived`, where the column being moved is a foreign key and looks like data: `ledger_entries.ledger_account_id` re-pointed at another identity's account leaves this scope and enters theirs with every check in the package green. `tenancyColumn(key)` returns `rule.localColumn` for `derived` and the guard refuses both the SQL and the Drizzle spelling. **OF [ADR-101](../decisions/ADR-101.md)'s THREE READ-SIDE CLASSES, THE WRITE PATH CANNOT SUFFER ONE, INHERITS ONE HARMLESSLY, AND INHERITS ONE IN THE DIRECTION THAT DESTROYS**: a non-total reverse-edge `semi-join` returns a subset on a read and **deletes a subset and leaves the rest** on a write, with no error anywhere, and `ledger_transactions` is the registry's only `semi-join` rule. **Not closed here, and the remedy edits a row this fence excludes.** **THE THIRD DOOR EXISTS BECAUSE ONE DOOR WAS SERVING TWO DIFFERENT ACTS**: `systemDb(reason).rows(key)` is generic over `TableKey`, so it serves *everybody's rows read without their identity* AND *nobody's rows*, and only the first has an identity at risk. [ADR-096](../decisions/ADR-096.md) is the precedent and **it went the other way**, and its remedy is unavailable because under that same entry `apps/api` is the process the site reads THROUGH. A third `SystemReason` is refused on ADR-096's own argument **and on a finding it did not have: the reason names the CALLER and the key type names the REACH, and they are not connected**, so `systemDb('request-handler')` would have read `accounts` and `payout_requests` firm-wide. **WHAT THE THIRD DOOR COSTS IS STATED AGAINST THE ENTRY**: the `firm` classification now decides read AND write authorization for **35 tables with no word written at the call site**, [ADR-101](../decisions/ADR-101.md) section 8's blind `firm` assertion is the only thing checking it, and [ADR-096](../decisions/ADR-096.md) section 9's measured hole gets **cheaper** to fall into because `firmDb()` is shorter than `systemDb('operator-console')`. **`RI-08` is still unwritten and is worth more after this entry than before it.** **FOUR COMPILE-TIME REFUSALS WERE WATCHED FAILING AND NONE IS PERMANENTLY WATCHED** -- `TS2345` on `FirmTableKey`, `TS2345` on `OwnedTableKey` twice, and `TS2322` on `__brand` with `identityId` present so the BRAND is what refuses -- **and `falsify-ci.mjs` is where they belong and is outside this fence**, so the follow-on is four case objects taking `CI-01` from 12 to 16. **THREE MUTATIONS WATCHED AT 5, 3 AND 6 OF 32 FAILING**, the third being `tenancyColumn` reading nothing, which is [ADR-101](../decisions/ADR-101.md)'s own lesson applied before it cost anything; **one weakness in that result is recorded rather than rounded up**, because one refusal test still passes there on a message its pattern also matches. **NOTHING EXECUTED A WRITE AND THE SUITE SAYS SO IN ITS OWN HEADER**: `ci.yml`'s `integration` job runs on bare `ubuntu-latest` with no services block, so every assertion reads the SQL the accessor BUILDS, over a driverless Drizzle handle that RECORDS what it is asked to run, which is what `scopedTx(...).update(...)` actually sends rather than a reimplementation of it. **NO TABLE'S ROWS ARE TOUCHED**: [`scope.ts`](../../packages/db/src/scope.ts), [`schema.ts`](../../packages/db/src/schema.ts) and [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) are not edited at all, which is the whole reason this ran concurrently with five sessions, and the registry stands at **92 of 111** unmoved. **A DISPATCH BASELINE WAS TEN STALE AND IS REPORTED RATHER THAN INHERITED**: the full suite is **1,675 passed** at this branch's base and not 1,665. `typecheck` exit 0 with zero `error TS`, `eslint packages/db` exit 0, invariants **8 of 8**, gates **30 of 30**, `verify` exit 0, suite **111 files / 1,675** to **112 files / 1,707 passed / 1 skipped** |
| [2026-08-25 - Session 214](2026-08-25-session-214.md) | **[ADR-103](../decisions/ADR-103.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH and the `E2` read owed. RULED in three clauses: a transcription's column TYPE and NULLABILITY are COMPARED against the migration set as of the last migration; [ADR-094](../decisions/ADR-094.md) section 3's `ALTER COLUMN` clause is SUPERSEDED and only that clause, so the fold's vocabulary gains `ALTER COLUMN ... DROP NOT NULL` as its second member with `DROP COLUMN` and `RENAME` still refused; and the `ALTER COLUMN` sub-vocabulary is itself CLOSED at that one shape with a default of FAIL.** **THE ORDER IS THE RULING RATHER THAN AN IMPLEMENTATION DETAIL**: ADR-094 refused `ALTER COLUMN` because *"deleting the refusal before writing the comparison would be trading a real control for nothing"*, so the comparison landed FIRST, in its own commit, watched failing on four seeded transcriptions, and the fold widened after it. **THE COMPARISON FINDS ZERO DISAGREEMENTS ACROSS 99 TABLES AND 1,183 COLUMNS, WHICH IS THE OPPOSITE OF THE RESULT THIS SLICE WAS PRICED FOR**: [P3 wave 3](../plans/P3-wave3-modules.md) section 12 row 1 calls its cost *"unmeasured by construction"*, ADR-094 predicted it *"would turn the suite red on whatever it finds across seven already-merged money-path files"*, and the dispatch instructed the session to stop and report if the count stopped being small. **Nine sessions transcribed 99 tables with nothing checking this axis at all and got every column right**, so no row of [`schema.ts`](../../packages/db/src/schema.ts) is edited and the file is untouched. **A GREEN RESULT FROM A NEW COMPARISON IS THE ONE MOST LIKELY TO BE VACUOUS, AND THREE ASSERTIONS STAND WHERE THAT SILENCE IS**: both sides of every per-table assertion are READERS and two readers that degrade together AGREE, which is [ADR-084](../decisions/ADR-084.md) section 7's failure and ADR-094's seed B, so **fourteen SENTINEL columns are written out as LITERALS** and checked against both readers, the type reader is asserted non-degenerate over all 1,183 columns, and its output vocabulary is asserted to span sixteen spellings. **SEED B WAS WRITTEN FOR THIS COMPARISON AND WATCHED BEING THE ONLY RED**: with the per-table assertion made to compare the DDL WITH ITSELF and `contact_channels.value_hash` still seeded as `text`, **all 99 per-table comparisons stayed GREEN and the sentinel assertion was the only failure**; add a reader collapsed onto `'text'` and the vocabulary assertion fires by name. **THE AXIS IS NOT COSMETIC AND [ADR-046](../decisions/ADR-046.md) IS WHY**: `contact_channels.value_hash` is a DIGEST and `value_ciphertext` is envelope-encrypted under a key not present in this database, so a digest transcribed as `text` is a digest the application layer reads as a string; money is integer cents, so `purchases.list_price_cents` transcribed `integer` silently caps a balance at 2,147,483,647 cents; and `identities.status` read as `text` leaves [ADR-041](../decisions/ADR-041.md)'s refusal to add a fourth member with nothing to hold. **FOUR SEEDED TRANSCRIPTIONS WATCHED FAILING, EACH NAMING ITS COLUMN**, and `git status --porcelain` read empty after every restore. **THE FOLD'S SECOND MEMBER HAS NO EXERCISE IN THE REGISTRY AND THE ENTRY SAYS SO RATHER THAN GLOSSING IT**: `otp_challenges` and `trading_calendar` are the only two tables in 47 migrations carrying an `ALTER COLUMN`, neither is registered, so `foldTable(...).relaxed` is EMPTY on all 99 and a vocabulary member no assertion runs is one nobody has checked. **The suite therefore folds those two BY NAME, registers neither, and watches the relaxation happen** -- NOT NULL at the `CREATE`, nullable after the fold, type unchanged -- and a second assertion re-derives every `ALTER COLUMN` in the set and holds the carriers at exactly those two and the count at exactly three, **which covers unregistered tables the refusal assertion cannot see**. **FIVE STATEMENT SHAPES AND TWO MUTATIONS WATCHED**, each seeded as a migration file and removed: `SET DATA TYPE` REFUSED with the statement quoted; **`DROP NOT NULL` on a registered table turning the NEW COMPARISON red by column name, which is the ordering executed rather than argued**; and three throws, on a `PRIMARY KEY` column, on an already-nullable one, and on a column not on the table. **`otp_challenges` AND `trading_calendar` BECOME REGISTRABLE AND NEITHER IS REGISTERED HERE**, so **SESSION 215 CAN PROCEED**. **THE HONEST COVERAGE IS TWO AXES AND THE ENTRY CARRIES A TABLE OF WHAT IT CANNOT SEE**: `DEFAULT` (335 transcribed calls, priced as the next comparison and **the one with a caller already written**, since [ADR-102](../decisions/ADR-102.md)'s `insert` builds statements from it), `CHECK` (the transcription states none), `UNIQUE` and indexes, inline `REFERENCES` beyond the columns a scope rule names, and column order. **ONE FENCE CALL IS REPORTED RATHER THAN TAKEN**: [`schema.ts`](../../packages/db/src/schema.ts)'s header now says `ALTER COLUMN` stays an offender that turns the suite red, which is FALSE about this tree; `P3-g`'s fence admits that file for ONLY the rows the comparison finds wrong, the comparison found none, **never widen a fence to finish**, and session 215 holds the file next and moves the same header's drifted-table count. **The approval clause is FIVE COMMANDS and names no count**, on [ADR-100](../decisions/ADR-100.md)'s and [ADR-102](../decisions/ADR-102.md)'s precedent after eight signed clauses drifted on a figure. **Registry unmoved at 99 of 111**; [`scope.ts`](../../packages/db/src/scope.ts) and [`schema.ts`](../../packages/db/src/schema.ts) are not edited at all. `typecheck` exit 0 with zero `error TS`, `eslint packages/db` exit 0, invariants **9 of 9**, gates **30 of 30**, `verify` exit 0, `scoped-db.test.ts` **125 to 229**, suite **112 files / 1,718** to **112 files / 1,822 passed / 1 skipped** |
| [2026-08-25 - Session 215](2026-08-25-session-215.md) | **[ADR-106](../decisions/ADR-106.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH and the `E2` read owed. RULED in six clauses whose one-line version is that A ROW WHOSE SUBJECT IS A PAIR OF IDENTITIES BELONGS TO BOTH AND IS SCOPED TO NEITHER.** `pair` is a fifth `ScopeClass` member and [`scope.ts`](../../packages/db/src/scope.ts)'s own claim that *"the question has exactly these four answers on this schema"* was **FALSE about migrations that predate it**: `0002_identity.sql` created `identity_links` with two `NOT NULL REFERENCES identities(id)` columns on the day the schema was written, and **exactly 4 tables in 111 declare more than one**, measured by reading every `CREATE TABLE` body. `identity_links`, `dedupe_matches` and `attributions` enter as `pair` and `otp_challenges` as `firm`; registry **100 to 104 of 111** at 1 root, 40 owned, 19 derived, 3 pair, 41 firm. **THE CLASS DOES NOT MAKE THEM SCOPED-READABLE AND THAT IS THE RULING RATHER THAN A LIMITATION.** The predicate exists and is one line -- `columnA = $1 OR columnB = $1` returns precisely the rows that are one person's -- and **what it returns is the problem**: every row carries the OTHER party's identity uuid out of a `NOT NULL` column. **THE CORPUS HAD ALREADY RULED THIS AT ARITY THREE AND IT IS APPLIED RATHER THAN INVENTED**: `correlation_groups` is `firm` and its own `why` says *"the reason it MUST NOT be traversed is stronger than the reason it cannot: returning the row to each member would tell every member which OTHER accounts the detector grouped them with"* -- and at arity two the disclosure is **WORSE**, because a member of a group learns a set and a party to a pair learns precisely who. **A SCOPED READ IS A FILTER AND NOT A PROJECTION** is the sentence the clause rests on. **THE COUNTER-ARGUMENT IS ANSWERED RATHER THAN IGNORED**: `risk_flags` and `identity_signals` are registered and carry detector material, and `risk_flags`' `why` that *"REGISTERING A TABLE MAKES IT READABLE AND NOTHING ELSE"* is right and undisturbed -- every one of those names exactly ONE person, and what a pair row adds is a SECOND SUBJECT arriving as the tenancy column itself. **A `pair` KEY IS THE FIRST `TableKey` THAT IS A MEMBER OF NEITHER NARROW TYPE**: excluded from `ScopedTableKey` for the disclosure and from `FirmTableKey` because [ADR-102](../decisions/ADR-102.md) clause 5 gives `firmDb()` no reason on the ground that no identity is at risk, and here two are -- so the two key sets **no longer partition `TableKey`** and `systemDb(reason)` is the only door, which is what [M06](../plans/M06-admin-ops-console.md)'s console and [M07](../plans/M07-risk-abuse.md)'s detectors, **the only readers the corpus names**, actually need. **`INV-M7-09` IS THE SENTENCE THAT LOOKS LIKE A COUNTER-EXAMPLE AND IS NOT**: a trader may CONTEST a link and the contested link is visible **to the admin who acts on it**, and M07 says the suppression is written *"through the `SECURITY DEFINER` function ... never by the application role"*. **`GET /affiliate/stats` RETURNS COUNTS AND CENTS AND NO REFERRAL ROW**, so nothing in the contract wants the disjunction either. **`otp_challenges` IS THE FIRST `firm` ROW WHOSE REASON IS TIMING RATHER THAN OWNERSHIP** -- every other firm table holds a row no identity will EVER own and this one holds a row whose identity does not exist YET -- and it is **the first registered table to exercise [ADR-103](../decisions/ADR-103.md)'s second fold member**, which closes [session 214](2026-08-25-session-214.md)'s named landmine that the member ran on no registered table. **EIGHT SEEDS WATCHED and the first is the one worth carrying**: `identity_links` declared `owned` on `identity_a` leaves `typecheck` at **exit 0 with zero `error TS`** and turns **EXACTLY TWO assertions red, both of them this entry's**, so every mechanical check that existed before ADR-106 accepts the wrong answer -- watched happening rather than predicted. A `derived` rule through a pair table compiles and turns **nine** red with the chain one naming the class; both narrow doors are `TS2345` on a pair key while `systemDb('operator-console').rows('attributions')` in the same file **compiles**. **THREE THINGS REPORTED RATHER THAN TAKEN AND THE FIRST CONTRADICTS THE PLAN: `affiliate_commissions` is NOT unblocked**, its reason having moved one class along -- the rule now COMPILES where it could not be written at all, and throws. **TWO FILES OUTSIDE THE FENCE, forced and refusal-only**: a fifth class member makes two exhaustive switches non-exhaustive and a predicate is built in exactly one file, and the fence named session 213 as `scoped-db.ts`'s holder **and session 213 merged**; no open remote ref holds either file, checked across 231. **SESSION 220 INHERITS [SESSION 216](2026-08-25-session-216.md)'s OPEN QUESTION WITH A SECOND TABLE ATTACHED.** **A CITATION IN THIS SESSION'S OWN BRIEF DOES NOT RESOLVE**: the prompt says *"ADR-092 section 9 named three times"* and section 9 is `Consequences` naming the four ORPHANS -- the class is section 3, once. **`schema.ts`'s HEADER SENTENCE HANDED OVER BY SESSION 214 IS REPAIRED** and its counts move 99 to 104, twelve to seven and TEN drifted tables to ELEVEN, the last replayed rather than incremented. `scoped-db.test.ts` **231 to 246**, `write-accessor.test.ts` **32 to 33**, suite **1,950 to 1,966 passed / 1 skipped**, ADR-103's column comparisons **1,183 to 1,240**, `typecheck` exit 0, lint and format clean, invariants **9 of 9**, gates **30 of 30**, `verify` exit 0. **Sessions 218 and 219 CAN PROCEED.** **NOT MERGED** |
| [2026-08-25 - Session 216](2026-08-25-session-216.md) | **[ADR-104](../decisions/ADR-104.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH and it is the money path's CENTRE: every other money content in P3 posts through it.** [`packages/ledger`](../../packages/ledger/src/index.ts) exists and `ledger_halts` enters the registry, taking it to **100 of 111**. **THE IMBALANCE IS UNREPRESENTABLE AND NOT MERELY REFUSED**: an entry is never constructed, a `Transfer` is, and `entriesOf` emits `+a` and `-a` for every transfer and nothing else in the package emits an entry, so the legs of any posting sum to zero ARITHMETICALLY -- **including a `Posting` assembled past both brands by a cast**, because the cast goes through the same fold, which the suite proves over the worst forgery it can write. **THE GUARD IS KEPT AND WHAT IT PROTECTS AGAINST IS STATED HONESTLY RATHER THAN OVERSOLD**: `assertBalanced` is unreachable through `entriesOf` by construction, so it is not protecting against a caller but against a LATER SESSION changing `entriesOf`, and it is watched refusing a credit-with-no-debit driven directly. **IT IS A PACKAGE ON `RI-04` AND FOUR NAMED CALLERS ACROSS TWO DEPLOYABLES**, declares NO DEPENDENCY, and takes the caller's OPEN transaction first with no overload that omits it, so it cannot open a pool even by mistake ([ADR-006](../decisions/ADR-006.md) made structural). **THE PATH DOES NOT RE-IMPLEMENT WHAT THE DATABASE ENFORCES** and the entry partitions all fourteen invariants line by line: seven the schema's, seven this path's and **not one of those seven checkable by the database**. **A LIVE `ledger_halts` ROW REFUSES THE POSTING, AND THE FINDING IS THAT NOTHING HAS EVER ENFORCED THAT TABLE**: `grep -n TRIGGER` over [`0016`](../../packages/db/migrations/0016_treasury_controls.sql) returns ZERO while its own index calls itself *"the read every payout and withdrawal path makes before it moves money"*. **A FINDING NOBODY EXPECTED, EXECUTED RATHER THAN ARGUED**: the zero-sum trigger is `AFTER INSERT ON ledger_entries`, so **a `ledger_transactions` row with NO ENTRIES commits clean**, which is [ADR-101](../decisions/ADR-101.md) section 7's open class with a PRODUCER; the remedy is a migration and the plan forbids one, so **`0048` stays free and it is reported**. **TWO THINGS THE ACCESSOR DOES NOT OFFER WERE REPORTED RATHER THAN ROUTED AROUND**, and the first BLOCKS session 220: only `SystemTx` can write a two-party posting and **a checkout handler is neither of its two reasons**. **THE ROUND TRIP WAS EXECUTED**, and rows 3 and 8 are a pair: a credit with no debit written BYPASSED was **REFUSED BY THE DATABASE AT COMMIT**, and the same posting bypassed with a live halt **COMMITTED**. **SIX MUTATIONS WATCHED, two of them weak and recorded as such**; the `accountKey` seed that merges two traders' positions is caught 16 times. **TWO FILES OUTSIDE THE FENCE, both forced and measured both ways**: `pnpm-lock.yaml`, which also refutes the plan's claim that a library slice collides with nothing, and one line of `gates.mjs` because `CI-06o` **failed on the good news**. Registry 99 to **100 of 111**, suite 112 files / 1,718 to **117 / 1,776 passed / 1 skipped**, 30 of 30 gates, 9 of 9 invariants, `verify` exit 0. **NOT MERGED. The `E2` read is owed on every line.** |
| [2026-08-25 - Session 217](2026-08-25-session-217.md) | **[ADR-105](../decisions/ADR-105.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH by subject, and it opens no network, writes no row and adds no dependency.** `packages/psp` exists: five methods, two fakes, **69 tests over 4 files**. **RULED IN EIGHT CLAUSES whose one-line version is THE PORT OWNS THE ORDER AND THE ADAPTER OWNS THE MECHANICS**: `verifyHmacWebhook` runs digest, then the freshness window in BOTH directions, then parse, once, for every provider, and an `HmacWebhookScheme` supplies which headers carry what, how the MAC is encoded, which bytes are signed and which body fields name the event. **THE PARSE IS INSIDE THE VERIFIER AND THERE IS NO OTHER PARSE**, so [`API_CONTRACT:629`](../architecture/API_CONTRACT.md)'s *"HMAC signature verified before parsing"* is a MODULE BOUNDARY rather than a convention a handler could get wrong, and `port.test.ts` fails by name if a `parseWebhook` export ever appears. **THE SECOND FAKE MOVED THE PORT THREE TIMES AND THAT IS THE SESSION'S CENTRAL RESULT**: the nonce stopped being a header, because one real shape has NO NONCE CHANNEL and a port demanding one would have forced that shape to INVENT a nonce, so what the port requires is that the nonce be COVERED BY THE SIGNATURE and not that it be a header; `RefundResult` grew a `pending`, because one provider settles in the call and the other on a later webhook; and the signed byte run stopped being derivable by the port, because one joins timestamp to body with `.` and the other joins timestamp, nonce and body with newlines. **WHAT DID NOT MOVE IS THE HALF WORTH SIGNING**: throws-not-returns, refuses-before-parsing and returns-nothing-the-digest-did-not-cover all survived two providers with no mechanical detail in common. **THE MONEY FINDING IS ARITHMETIC AND NOT PHILOSOPHY: THE AMOUNT A CARD IS CHARGED IS NOT `amount_paid_cents`.** [`0006_commerce.sql:189`](../../packages/db/migrations/0006_commerce.sql)'s `purchases_wallet_leg_matches_method` permits `0 < wallet_debit_cents < amount_paid_cents` on `mixed`, [ADR-019](../decisions/ADR-019.md) admitted `mixed` because *"a trader with $60 in the wallet buying a $99 evaluation is the common case, not an edge one"*, and **a port taking a plain `amountCents` would have charged the card 9900 instead of 3900 with every type in the workspace green**, because the mistake happens on the way OUT of the database where no constraint can see it. `CardAmountCents` is a branded `bigint` with ONE producer, which takes a purchase row and **REFUSES a wallet-funded one rather than returning zero**, on `INV-M3-13`. **THE THROW CANNOT BE BARE AND THE SCHEMA IS WHY**: `psp_webhook_events.signature_verified` is `boolean NOT NULL` under *"a payload whose signature did not verify is still stored"*, so `WebhookVerificationError` carries the `psp`, a refusal from a CLOSED set and the RAW BYTES, because a catch block cannot write that row otherwise. **THREE SIGNATURES DEVIATE FROM [M03](../plans/M03-billing-checkout.md) SECTION 2.1's SKETCH AND ALL THREE ARE REPORTED**, the load-bearing one being that the sketch's literal reading of *"no adapter method takes a price"* CANNOT BE IMPLEMENTED: an adapter that read the purchase row needs a database dependency inside the package whose job is to isolate a vendor, so the rule's REASON is honoured by the brand instead. **FOUR COMPILE-TIME REFUSALS WATCHED FAILING** and the seed removed with `git status --porcelain` empty. **NINE MUTATIONS SEEDED, THREE SURVIVED THE FIRST PASS, ALL THREE REPORTED AS FINDINGS AND CLOSED WITH A CASE EACH**: the MAC LENGTH GUARD dropped left every case green because none presented a well-formed MAC of the wrong size, and `timingSafeEqual` THROWS on a length mismatch, so the route would have returned **500 where the contract requires 401 and a security event**. **A CONSTRAINT NAME IN THIS SESSION'S OWN COMMENTS WAS WRITTEN FROM MEMORY AND DID NOT RESOLVE**, repaired in place. **NOTHING IS ADMITTED**: `node:crypto` is a builtin, `pnpm-workspace.yaml` UNTOUCHED, `pnpm-lock.yaml` **+12 / -0** and all of it this package's own `importers` block. **`gates.mjs` IS NOT EDITED AND THE CONSEQUENCE IS MEASURED**: `packages/psp` is money path by subject and `MONEY_PATH_WORDS` does not carry `psp`, so it is scanned by nothing for a model-SDK import. Suite **112 files / 1,718** to **116 files / 1,787 passed / 1 skipped**, `typecheck` exit 0, invariants **9 of 9**, gates **30 of 30**, `verify` exit 0, registry unmoved at **99 of 111** |
| [2026-08-26 - Session 218](2026-08-26-session-218.md) | **`P3-j`, the authentication surface. FIFTEEN ENDPOINTS, TWO FILES, 43 TESTS, NO DEPENDENCY AND NO ADR.** [API_CONTRACT](../architecture/API_CONTRACT.md) section 3's seven headings, section 3.1's six endpoints and `GET /me`; sixteen routes register on the public surface with [`health`](../../apps/api/src/routes/health.ts) and **none is withheld**. **THE PLAN WAS COMMITTED AND PUSHED BEFORE THE FIRST SOURCE FILE EXISTED**, which is how a remote autonomous session discharges [ADR-003](../decisions/ADR-003.md)'s plan-mode requirement without entering a harness mode that blocks on an approval prompt nobody is watching. **`C-27` IS A TYPE AND THE SEED IS `@ts-expect-error`**: `ElevationFactor` is `sessions.elevated_by_factor`'s CHECK list at `0029:581` written as a union, so an SMS-offered elevation is not a value of `ElevateRequest`, and **a seeded handler comparing the parsed factor against `sms_otp` is `TS2367`** while **widening the union turns all three directives into `TS2578` unused-directive errors** -- watched both ways, so the refusal is checked by `pnpm run typecheck` rather than by a test somebody could delete. **THE REQUIRED FACTOR IS DECLARED ONCE PER ENDPOINT AND IT IS LOAD BEARING**: the route list and the declaration table are DERIVED FROM THE SAME ARRAY, `endpointHandler` applies the factor before any handler body runs, and **no handler in either file contains an authorization check of its own**. **Section 12's six rows are asserted in BOTH directions**, the two quiet ones included: `GET /sessions` and `GET /phone/change` return **200** from a single-factor session, and the read that showed the destination is watched succeeding **from the same session** whose write is refused. **TEN MUTATIONS SEEDED ONE AT A TIME AND EVERY ONE CAUGHT** (4, 2, 2, 3, 5, 5, 3, 1, 1, 1 failures), every seed removed with `git status --porcelain` empty. **NO NUMBER IN EITHER FILE IS A RATE LIMIT**: both the SMS and the EMAIL branch go through `requestOtp`, and the suite drives the velocity by counting to a `send_limit` read off a seeded `otp_send_budget` row. **THE THREE WRITES THIS SURFACE NEEDS CANNOT BE EXPRESSED THROUGH [`packages/db`](../../packages/db/src/scoped-db.ts) AT ALL**, which is [session 222](2026-08-26-session-222.md)'s finding met from the API side and widened: `scopedTx.update` renders the tenancy predicate and **nothing else**, so revoking one session revokes **every session that identity holds**; `systemTx.update` and `firmTx.update` render no `WHERE` clause; and **the remedy the dispatch offered is not available** because `sqlExecutor`'s reason vocabulary is closed to `'job-enqueue'` and THROWS on any other word. So the port is declared, **BLOCKED on its own declaration**, and the default fails closed at **503** on every route. **THREE OF THE DISPATCH'S SIX CITED LINE NUMBERS DO NOT RESOLVE** (695, 720 and 723, not 694, 718 and 721) and the defect is confirmed by shape rather than by line. **A DEBT AND A DISAGREEMENT ARE RECORDED RATHER THAN RULED**: `POST /phone/change/:id/cancel` has **no required factor anywhere in the corpus**, and `POST /auth/elevate`'s declared cell reads `session` where section 12's reads `passkey or dual_channel`, because that cell names the factor the ELEVATION admits and not the authority needed to CALL it. **`GET /me` IS AN ALLOWLIST AND THE SUITE PROVES IT** by handing the backend a row carrying `internal_risk_score` and `detector_name` and asserting neither reaches the response. **`pnpm-workspace.yaml`, `pnpm-lock.yaml` AND `apps/api/package.json` ARE ALL UNTOUCHED**: a WebAuthn verifier admitted here would have no credential store to verify against, and the lockfile is rowed SERIAL across `P3-j`, `P3-m` and `P4-i`. **NO ADR**, so [ADR-108](../decisions/ALLOCATION.md)'s reserved row stands exactly as dispatched. `apps/api` **4 files / 37 tests to 5 / 80**, suite **122 files / 2,028 passed / 1 skipped to 123 / 2,071 / 1**, `typecheck` exit 0, `eslint apps/api` exit 0, gates **30 of 30**, `verify` exit 0. **NOT MERGED. The `E2` read is owed and it is AUTH.** |
| [2026-08-26 - Session 219](2026-08-26-session-219.md) | **[ADR-109](../decisions/ADR-109.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH and the `E2` read is owed.** [P3 wave 3](../plans/P3-wave3-modules.md)'s `P3-k` is written: [`idempotency.ts`](../../apps/api/src/idempotency.ts), [`routes/webhooks-psp.ts`](../../apps/api/src/routes/webhooks-psp.ts) and **51 tests over two files**, with no migration, no `packages/db` file, no catalog entry and no `routes/index.ts`. **THE DISPATCH PREDICTED ONE HOLE AND ASKED FOR IT TO BE VERIFIED RATHER THAN BELIEVED, AND VERIFYING IT FOUND A WIDER ONE.** [Session 215](2026-08-25-session-215.md) registered `idempotency_keys` as `owned` on a NULLABLE `identity_id`, and **registering it is what created the problem**: `owned` excludes it from `firmDb()`, the nullable column is precisely how the unowned replay is kept out of `scopedDb(identityId)`, and `SystemReason` has two members of which a request handler is neither. **THE RULING IN ONE LINE IS THAT THE MISSING CONSTRUCTION IS A PREDICATE AND NOT AN AUTHORITY**: `systemDb` gains no third member, because [`scoped-db.ts:701`](../../packages/db/src/scoped-db.ts) passes `undefined` to `updateStatement`'s REQUIRED `where` position and an UPDATE through it writes **EVERY ROW IN THE TABLE** whatever reason it was handed, and `ScopedTx` is blocked identically, narrowing by tenancy and by nothing else. **FOUR OF THE FIVE AVAILABLE DOORS FAIL ON THE SAME COLUMN OF THE SAME TABLE**, which is why the ruling is about that column. **THE REMEDY THE DISPATCH ITSELF PRESCRIBED IS UNAVAILABLE INSIDE THE FENCE**: `sqlExecutor` takes `SqlExecutorReason`, whose type is the single member `'job-enqueue'`, and [`scoped-db.ts:742`](../../packages/db/src/scoped-db.ts) CHECKS IT AT RUN TIME AND THROWS, so reaching for it is the `packages/db` edit the same dispatch says to report rather than take. **[SESSION 222](2026-08-26-session-222.md) HIT THIS WALL ONE DAY EARLIER FROM `apps/worker` AND REPORTED IT**, so this is the second caller and the ruling is written rather than reported twice. **A SECOND RULING ARRIVES FROM THE FRAMEWORK AND IT IS MEASURED RATHER THAN REASONED**: fastify 5.12.1's default `application/json` parser hands a handler a parsed `Object` with `request.raw.readable` already `false`, so [API_CONTRACT](../architecture/API_CONTRACT.md) section 10's *"HMAC signature verified BEFORE parsing"* is lost before any code of Merit's runs; `{ parseAs: 'buffer' }` recovers the exact bytes and **APPLIES EVEN WHEN REGISTERED AFTER THE ROUTE**, which is what makes the seam small, and `RouteDefinition` carries none. **A HANDLER MAY NEVER RECOVER THE BYTES BY RE-ENCODING `request.body`** -- two JSON texts that parse equal serialise differently, so the MAC fails and every legitimate webhook answers 401 with no line of code looking wrong -- and the suite watches exactly that round trip being refused. **THERE IS NO UPDATE STATEMENT IN THE RECEIVER AND THAT IS THE DEDUPE WORKING**: apply, then INSERT the row carrying the result, then ROLL BACK if `psp_webhook_events_provider_event_uq` refused the claim, so one event delivered twice is **exactly one business effect and two 200s**, and the two responses are byte-identical because one that said "duplicate" would answer which event ids Merit has seen. **THE APPROVAL LINE IS WATCHED IN BOTH DIRECTIONS AND THE SEED IS REAL**: the same case run against a store whose uniqueness check is REMOVED is watched applying the effect TWICE. **A LANDMINE IS NAMED AND NOT REPAIRED**: `idempotency_keys.key` is the PRIMARY KEY and carries no identity, so one caller's token can block another's; it is a DENIAL and not a disclosure, and `beginIdempotent` reports it as its own outcome rather than guessing. **SEVEN MUTATIONS SEEDED AND ALL SEVEN CAUGHT**, every seed removed with `git status --porcelain` clean of them. **THREE DISAGREEMENTS WITH THE DISPATCH ARE REPORTED**, one of which is that its own `firmTx` line numbers are two lines off while the defect they name is exactly as described. Suite **122 files / 2,028 passed / 1 skipped** to **124 files / 2,079 passed / 1 skipped**, `typecheck` exit 0, `eslint apps/api` exit 0, invariants **9 of 9**, gates **30 of 30**, `verify` exit 0, and **`0048` stays free with no file under `packages/db` touched** |
| [2026-08-26 - Session 220](2026-08-26-session-220.md) | **[ADR-113](../decisions/ADR-113.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH and the `E2` read is owed on every line.** `P3-n`: [`POST /checkout`](../architecture/API_CONTRACT.md) and `POST /accounts/:accountId/reset` exist, the price comes from `plan_version_sizes`, the coupon discount is recomputed, and **attribution resolves INSIDE the checkout transaction** ([M08](../plans/M08-affiliate-system.md) section 3.1), which is why this was one slice. **API_CONTRACT SECTION 12's CHECKOUT ROW IS ASSERTED IN BOTH DIRECTIONS AND THE TWO SEEDS ARE NOT THE SAME SIZE**: the tampered-price seed turns **1** assertion red and the seed that makes the price lookup return zero turns **13**, which is why a suite watching only the refusal cannot tell a working lookup from one that refuses everything. **ELEVEN MUTATIONS SEEDED AND ALL ELEVEN CAUGHT**, every one reverted against a byte-identical copy with `md5sum -c` clean before the next; **six turn exactly one assertion red and are recorded as thin rather than rounded up**. **THE APPROVAL LINE IS DRIVEN AND NOT ASSUMED**: the attribution write is the last statement in the transaction and is NOT guarded, and the fixture `transact` runs against a STAGING copy and merges only on return, so the seed that makes it write through is itself one of the eleven. **A REFUSAL DECIDED AFTER A WRITE IS THROWN OUT OF THE TRANSACTION** rather than returned, so the coupon-claim conflict rolls the purchase back; returning it instead turns **12** red. **THE HEADLINE THE DISPATCH EXPECTED IS HALF RIGHT AND THE ENTRY SAYS WHICH HALF**: [ADR-112](../decisions/ADR-112.md) moved the ADDRESS and its own section 10 says it did not move the AUTHORITY, so `attributions` being class `pair` still leaves **no door in `packages/db` a checkout handler may use** -- [session 216](2026-08-25-session-216.md) reported it, [session 215](2026-08-25-session-215.md) attached this table to it, and `CheckoutTx.insertAttribution` is where it lands. **`IdempotencyStore` IS RULED THE NEXT SESSION'S ON THE FENCE RATHER THAN ON PREFERENCE** and [ADR-109](../decisions/ADR-109.md) clause 3 stands unchanged: an implementation needs `@merit/db` in `apps/api`, which is an authority change on the deployable serving the whole public surface. **A THIRD PARTY IS CALLED INSIDE AN OPEN TRANSACTION AND BOTH ALTERNATIVES ARE PRICED** rather than the hazard being routed around: `psp_reference` is `NOT NULL` and is the webhook idempotency anchor. **ONE FILE OUTSIDE THE FENCE, FORCED AND NAMED IN THE PLAN COMMIT BEFORE IT WAS TOUCHED**: `apps/api/package.json` gains `@merit/affiliate`, measured by `ls apps/api/node_modules/@merit` returning `psp` and nothing else under `node-linker=isolated`. **`pnpm-workspace.yaml` IS UNTOUCHED AND SESSION 162's `M08-5` PREDICTED OTHERWISE**: its `packages/*` glob already covers a new package, so that forecast serial collision does not exist. `packages/affiliate` **24** assertions, `apps/api/test/checkout.test.ts` **38**, `apps/api` **145 to 183**, suite **134 files / 2,244 passed to 136 / 2,306 passed / 1 skipped**, `adr_count` **111 to 112**, 30 of 30 gates, 9 of 9 invariants, `pnpm run verify` exit 0. **NO MIGRATION: `0048` STAYS FREE. NOT MERGED.** |
| [2026-08-26 - Session 221](2026-08-26-session-221.md) | **[ADR-114](../decisions/ADR-114.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH and the `E2` read is owed.** [P3 wave 3](../plans/P3-wave3-modules.md)'s `P3-o` is written: [`packages/kyc`](../../packages/kyc/src/index.ts) with the vendor-agnostic provider port, ADR-021's trigger set, the document screen and one fake, plus [`routes/kyc.ts`](../../apps/api/src/routes/kyc.ts) and [`routes/webhooks-kyc.ts`](../../apps/api/src/routes/webhooks-kyc.ts) and **133 assertions over five new files**, with no migration, no `packages/db` file and no `routes/index.ts`. **THE PLACEMENT WAS NOT RE-DECIDED AND THE TWO THINGS IT LEFT OPEN WERE.** ADR-021 ruled the composite set and the FREEZE gate ruled `{second_distinct_account_purchase, pre_funded}` earliest-fires; what neither could see is that **"earliest" is a statement about TIME while a gate evaluates a SNAPSHOT**. The tie inside one evaluation is broken by FUNNEL ORDER, which the corpus already writes three times and all three agree, and **the tie-break is sound only because every condition is MONOTONE** -- a purchase count never falls, an evaluation pass is latched, and the distinct-account count is a HIGH-WATER count rather than a live one, because a fleet operator who closes an account has still been a second-account holder. **That last line is the one most likely to be quietly reversed by whoever writes the query**, and four properties over 500 generated cases each are what would catch it. **THE CONTRACT'S IDEMPOTENCY ANCHOR FOR THIS ENDPOINT NAMES NOTHING AND THREE FACTS SAY SO**: there is no `kyc_webhook_events` table in any of the 47 migrations, the pair `(provider, provider_applicant_id)` is not a unique key and MUST NEVER BECOME ONE because `SD-M19-01` makes a re-verification a new row against the same applicant, and no authority admits a caller with no identity. So [ADR-112](../decisions/ADR-112.md)'s address refusal is **correct here on a table nobody had checked**, the store is a port, `productionDeps` resolves nothing, and `0048` stays free. **ONE ORDERING RULE IS NOW IMPLEMENTED TWICE AND A TYPE IS WHY**: `packages/psp`'s scheme is branded to `PspId`, closed by a CHECK on `purchases.psp`, while `kyc_verifications.provider` carries no CHECK at all because naming a vendor is a disclosure event. The two are bound by a suite that READS `packages/psp/src/webhook.ts` and compares the refusal vocabulary member for member and the ordering step for step, inside the verify function's own body rather than over the file. **MERIT NEVER PROXIES A DOCUMENT AND THE INVARIANT IS ENFORCED TWICE BECAUSE A TYPE CANNOT SEE INSIDE A JSONB BLOB**: the port has no field a document could be assigned to, and the receiver screens a verified payload before any store, REFUSES rather than redacts, and records key paths and never values. The screen's word list is deliberately short and `face_match_score` PASSES, because a screen that refused a score would refuse the one payload M19 exists to record. **FOURTEEN SEEDED MUTATIONS AND ALL FOURTEEN CAUGHT**, the widest being latest-instead-of-earliest at 7 red. **Two defects in this session's own first draft are recorded rather than smoothed over**: the fail-closed backend reached the framework as a 500, and the production webhook route answers 404 rather than the 503 its header claimed. Suite to **139 files / 2,377 passed / 1 skipped**, invariants **9 of 9**, gates **30 of 30**, `verify` exit 0, and **two files outside the fence** (`apps/api/package.json` and `pnpm-lock.yaml`, eighteen importer lines) which **collide with sessions 220 and 223** and are reported rather than resolved |
| [2026-08-26 - Session 222](2026-08-26-session-222.md) | **[ADR-107](../decisions/ADR-107.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH and the `E2` read is owed.** [P3 wave 3](../plans/P3-wave3-modules.md)'s `P3-l` is written: seven files under [`apps/worker/src/provisioning/`](../../apps/worker/src/provisioning/index.ts) and **62 tests**, with **no route, no `packages/db` file, no migration, no dependency and no shared file of any kind**. **THE RULING IN ONE LINE IS THAT THE FAIL-CLOSED EXIT IS A VALUE WITH ONE PRODUCER RATHER THAN A CHECK THAT REFUSES**: `SetpointConfirmation` is branded on a module-local `unique symbol`, `setpointConfirmation` is its only producer, and `TradingAdmission`'s admitted arm carries one as a REQUIRED field, so **an admission cannot be CONSTRUCTED without the evidence** and `admitToTrading` is a fold seeded at a refusal whose loop has no `else` -- deleting the loop entirely leaves every account out of trading. **`entitleAfterSetpoint` TAKES THE CONFIRMATION AS A REQUIRED ARGUMENT, which makes `INV-M2-15`'s ordering the ARGUMENT LIST rather than a step list somebody follows.** **`confirmed_inferred` IS NEVER EVIDENCE, FOR ANY OPERATION**, which is the form of [M02](../plans/M02-rithmic-bridge.md) section 3.6.2's finding that needs no migration, and the anchor is the account's CURRENT floor, which is `INV-M2-08` composed with `INV-M2-13` rather than a staleness rule somebody has to write. **ADR-006's TRANSACTIONAL-ENQUEUE CRITERION HAS A CALLER FOR THE FIRST TIME** and is asserted in both directions against a model that refuses to make a statement durable unless the connection that ran it committed. **THE CENTRAL FINDING IS THAT [ADR-102](../decisions/ADR-102.md)'s SYSTEM DOOR CANNOT NAME A ROW**, executed rather than argued: `SystemTx.update` renders `update "provisioning_queue" set "status" = $1 returning ...` with **NO `WHERE` CLAUSE**, and `updateStatement`, `deleteStatement` and `selectStatement` each take `where` in a REQUIRED position while [`scoped-db.ts:685`](../../packages/db/src/scoped-db.ts)'s `systemTx` passes `undefined` for all three with no parameter to override it, so a status advance through the accessor writes **EVERY ROW IN THE TABLE** -- worse than the `semi-join` class that entry inherits and names as open, because a superset has no bound. **REPORTED, NOT ROUTED AROUND**: `sqlExecutor` would run it and its one-member vocabulary is `'job-enqueue'`, which is the deliberate door, so `ProvisioningAdvancePort` and `ProvisioningReadPort` are declared and BLOCKED on their own declarations and **`0048` stays free**. **COMPENSATION IS STATED AS THREE ANSWERS RATHER THAN AS A MECHANISM**: five of the seven operations in `0007`'s CHECK list have NO INVERSE, for three different reasons; nothing handed to the vendor is ever rolled back, so compensation is forward-only; and **when compensation itself FAILS the account is in the state it was already in**, not because anything noticed but because admission needs positive evidence and a failed compensation produces none. **THE SUITE FOUND A DEFECT IN ITS OWN SUBJECT ON ITS FIRST RUN**: the brand's key was a `declare const ... unique symbol`, which type-checks, satisfies every use and **ERASES**, so under `node --experimental-strip-types` the only producer of an admission threw `ReferenceError` on five cases while `tsc --noEmit` reported nothing -- `RI-10`'s class in a construct `RI-10` does not look at. **TEN MUTATIONS SEEDED AND ONE SURVIVED, REPORTED AS A FINDING WITH ITS CASE**: dropping `frame(key)` from the canonical serialization left **all sixty tests green**, because the pair the suite tested is disambiguated by the value framing alone, and the pair that needs key framing carries the SAME VALUES UNDER DIFFERENT FIELD NAMES -- a payload naming `risk_floor_cents` and one naming `account_ref` with the same number are one intent under `provisioning_queue_intent_uq`, so the second is silently not enqueued and an account is left at a floor nobody pushed. **FOUR COMPILE-TIME REFUSALS WATCHED FAILING BY THEIR ERROR** and every seed removed with `git status --porcelain` empty. **THREE DISAGREEMENTS BETWEEN THE DISPATCH AND THE PLAN ARE REPORTED**, and the registration clause was **ALREADY DISCHARGED** by [session 205](2026-08-25-session-205.md). **`packages/rithmic`'s `PlatformAdapter` IS NOT M02 SECTION 2.1's** -- `provision()` and `entitle()` are NULLARY there -- and the package is outside this fence, so the port carries M02's shape. Suite **121 files / 1,950 passed / 1 skipped** to **122 files / 2,012 passed / 1 skipped**, `typecheck` exit 0, `eslint apps/worker` exit 0, invariants **9 of 9**, gates **30 of 30**, `verify` exit 0, `apps/worker` **loads under `node --experimental-strip-types` with 44 exports**, and the registry is **100 of 111** where the dispatch said 99 |
| [2026-08-26 - Session 223](2026-08-26-session-223.md) | **[ADR-115](../decisions/ADR-115.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH BY POSITION and the `E2` read is owed.** [ADR-023](../decisions/ADR-023.md) rules enrichment bought-not-built and observe-mode non-blocking, and the number was reserved CONDITIONALLY on whether "the vendor port or the scoring shape needs one". **THE SCORING SHAPE NEEDED ONE**, and the finding that forced it is that **there is no column anywhere in the 47 migrations for an observe-mode enrichment score**: the only available surface is `risk_flags`, whose `source` column is reserved `'vendor:<name>'` for exactly this, and writing one would hand an untuned score authority over [ADR-040](../decisions/ADR-040.md)'s `G-HOLD-REQUIRED` while `OQ-M3-03` is still open on whether the same band may stop a purchase. **`D-12` already ruled the shape for a detector that observes without accusing**, so no flag is written, the score is REPORTED and not STORED, and the migration that would store it is specified in the entry rather than written. **THE SECOND FINDING WOULD HAVE SHIPPED AND IT REFUSES A PURCHASE**: `identity_signals` carries its natural key as a `CREATE UNIQUE INDEX`, [ADR-112](../decisions/ADR-112.md)'s `uniqueKeys()` reads four sources and an index is none of them, so a blind insert raises `23505` on the second purchase by any returning trader, and inside checkout's transaction Postgres turns that `COMMIT` into a `ROLLBACK`. The write is a `rowsWhere` then an `updateAt` addressed by `id`, and **the residual race two concurrent checkouts leave open is named with the two constructions that would close it**, both `packages/db` diffs outside the fence. **NON-BLOCKING IS A TYPE AND NOT A PROMISE**: `observeEnrichment` returns `Promise<void>` and never throws, so a call site has nothing to branch on and a vendor failure cannot become enforcement by exception. **NO ENABLED CONTRACT ROW MEANS NO CALL AT ALL**, the row is installed `enabled: false` with `approved_by` a required parameter, and the `integration_dispatches` row is written whenever the subject left Merit INCLUDING on a timeout. `packages/enrichment` is **7 source files and 5 suites, 83 assertions**, declares **no runtime and no workspace dependency** on `packages/ledger`'s precedent, reaches `node:crypto` and nothing else, and `pnpm-lock.yaml` gains **+12 lines, 0 deletions**, which is `packages/psp`'s measured figure exactly. **SEVEN SEEDS, ALL SEVEN CAUGHT AND REMOVED.** Suite **134 files / 2,244 passed** to **139 files / 2,327 passed / 1 skipped**, invariants **9 of 9**, gates **30 of 30**, `verify` exit 0, and **`0048` stays free**. **THE CALL SITE IS OWED AND NOT INVENTED**: `apps/api/src/routes/checkout.ts` is session 220's and had not landed on `origin/main` when this branch closed |
| [2026-08-26 - Session 224](2026-08-26-session-224.md) | **[ADR-110](../decisions/ADR-110.md) lands, `status: proposed`, approval line UNSIGNED, not money.** [P4](../plans/P4-portal-and-site.md) section 8's `P4-f` is written in both halves: `GET /public/methods/:statCode` enters [API_CONTRACT](../architecture/API_CONTRACT.md) section 4, which is an ADR and not a commit because that document is `approved`, and [`apps/api/src/routes/public-methods.ts`](../../apps/api/src/routes/public-methods.ts) serves it under **13 tests**. **THE RULING IN ONE LINE IS THAT THE ADDRESS OF A DEFINITION IS `(stat_code, version)` AND THE PATH CARRIES THE FIRST HALF**: [`0021:124`](../../packages/db/migrations/0021_transparency.sql)'s `statistic_definitions_code_version_uq` makes the pair unique and the published aggregate already spells it (`method_path: '/methods/ST-01/2'`), so the response is **every version, ascending, with the `adr_ref` for every change** ([M12:218](../plans/M12-transparency-platform.md), [M12:144](../plans/M12-transparency-platform.md)) and **no version parameter is invented**, a selector on this path being a second address for a row that already has one. **`live_version` IS [`0021:127`](../../packages/db/migrations/0021_transparency.sql)'s PARTIAL UNIQUE INDEX PROJECTED AND IS NOT "THE DEFINITION IN FORCE TODAY"**, because `INV-M12-07` puts `effective_from` in the future at write time, and **no clock is read anywhere in the handler**, which is what keeps one cached response true for every caller who shares it. **BOTH SURROGATE KEYS ARE WITHHELD** under section 1's allowlist and **a supersession naming no version in the set is REFUSED rather than rendered `null`**, which would publish a superseded definition as the live one. **`404` MEANS ONE THING HERE**, contrasted with the deliberately ambiguous 404 of a trader surface, because this table carries no identity column and there is no correct one. **THE DATA SOURCE IS A PORT AND NOTHING IN THIS TREE WIRES IT**: `apps/api` declares `fastify` and holds no database handle, an unwired source is a **500 and never a 404 or a 503**, and the door the wiring slice must take is `firmDb()` ([`scoped-db.ts:535`](../../packages/db/src/scoped-db.ts)) rather than the per-identity accessor, **whose appearance under `apps/api/src` flips `VG-3` and `VG-6` and fails `CI-06/vg-inventory` at 29 of 30 on good news**. **`INV-M9-04` IS NAMED AND DISPOSITIONED RATHER THAN LEFT AMBIGUOUS**: its trigger is a plan-version publish, so it does not reach this endpoint, and **the gap beside it is recorded** because [M12:236](../plans/M12-transparency-platform.md) gives `stats.definition_changed` the consumers FEED and EVID with no revalidation among them, so a statically rendered method page is invalidated by nothing. **[P4](../plans/P4-portal-and-site.md) section 8's FENCE CELL NAMING `apps/api/src/routes/index.ts` IS STALE and no such file was created**, [ADR-100](../decisions/ADR-100.md) having made the module list a directory listing, so this slice's whole code contribution is one file no other slice edits. **THE SUITE WATCHES THE WITHHELD DIRECTION**: `classifyPath` answers `public`, so the operator deployment registers nothing at this path and answers 404 by having nothing there. **SIX MUTATIONS SEEDED AND EVERY ONE FAILED AT LEAST ONE CASE**, the clock-based `live_version` the most expensive at 4 of 13, and it is the implementation a reader would write from the phrase "the current definition". `apps/api` **37 to 50 passed** over 5 files, suite **123 files / 2,041 passed / 1 skipped**, `typecheck` exit 0, `lint` and `format:check` clean, invariants **9 of 9**, gates **30 of 30**, `falsify` clean-and-dirty over all 30 with 70 scope and 10 loader cases, `verify` exit 0. **No migration, no `packages/db` file, no dependency, no `pnpm-lock.yaml` change, no gate letter and no `VG` row** |
| [2026-08-26 - Session 225](2026-08-26-session-225.md) | **[`P4-j`](../plans/P4-portal-and-site.md) lands: VG-M9-2 exists, `GS-143` and `GS-144` move, and NO ADR IS MINTED, so `111` returns to the pool unused.** **THE BUILD-OR-NO-BUILD CHOICE WAS DECIDED BY EXECUTING IT RATHER THAN BY WEIGHING IT**: seeding `apps/site/src/app/page.tsx`, the minimum page that would make `next build` do anything, turns `CI-06/gate-inventory` red at **29 of 30** because it is `CI-07`'s dated ARRIVAL condition, and `CI-07`'s stage is `P4-i`'s cell in [STRATEGY](../testing/STRATEGY.md) section 4.1 under [ADR-095](../decisions/ADR-095.md) section 6. Creating the page would have been a fence widening to finish, so **the lint is a check over the authored source and over the page models built from it**, which is where *"the build fails"* is expressible in a tree whose content is `content_documents.body_mdx` over HTTP ([ADR-096](../decisions/ADR-096.md)). Seed removed, tree clean. **THE OG SURFACE IS THE DESIGN AND NOT AN EXTRA RULE**: any `og_*` frontmatter key is a linted surface, DISCOVERED rather than listed, so a future social field is covered by being new. **`routes/og.ts` EXISTS, AGAINST THE FOUR COMMENTS AND NO IMPLEMENTATION [SESSION 186](2026-08-24-session-186.md) MEASURED**, and the card cannot carry a bare figure structurally: `OgCardInput.statistic` is typed `RenderedStatistic` and never a string, so no call site can pass `rendered.value`, and the only producer of the card's figure is `statisticText`, which has no `justTheValue`. Title, description AND ALT TEXT each take VG-M9-2. **THE TWO HALVES OF THE CONTROL CONTRADICTED EACH OTHER AND A TEST FOUND IT RATHER THAN A READING**: `statisticText` emits `14.70% (2026-04-14 to 2026-08-20, as of 2026-08-20, n=2803)` and the authoring lint refused it as a bare percentage, so **the lint would have rejected the only sanctioned output of the only sanctioned accessor**. `GS-144` is *"rendered WITHOUT its trailing window"*, so a figure carrying its window is the PASSING case; the carve-out is **pinned to `statisticText` by an assertion over real output across all four units [ADR-031](../decisions/ADR-031.md) enumerates plus INV-M12-05's suppressed shape**, and asserted not to swallow a neighbouring bare figure. **ONE CARVE-OUT BESIDE IT, EXACT AND CITED**: [ADR-042](../decisions/ADR-042.md)'s settlement window is a banking-rail unit Merit quotes and `plan_versions` has no field a `<PlanValue>` could name for it; it is an exact substring rather than a pattern, and a DIFFERENT day count in that sentence fails. **`ADR-097`'s VERBATIM BLOCK DID NOT SURVIVE TRANSCRIPTION ON TWO CLAUSES AND BOTH ARE RECORDED RATHER THAN QUIETLY REPAIRED**: its *"`apps/site/package.json` declares no `build` script"* is FALSE at this ref, because [ADR-095](../decisions/ADR-095.md) landed that script at `a890a3b`, which is **not an ancestor of ADR-097's signing commit `092004e`** -- the clause was true on its own branch and stale once both merged; and the `GS-144` cell's `grep -rniE "og.image\|opengraph"` carries a **literal pipe**, which is text inside ADR-097's code fence and a CELL SEPARATOR inside a table row, so `CI-06/fixture-inventory` refused the row at **five cells against four**. **THE TWO ROWS MOVED TWICE, IN TWO COMMITS, EACH TRUE ABOUT THE TREE AT ITS OWN MOMENT**: first to `blocked / outside-loader-boundary` with `no-fixture-format` 229 to 227 and `outside-loader-boundary` 4 to 6, which is ADR-097's ruling APPLIED rather than left a dead letter; then to `covered-elsewhere` once the lint and the OG leg existed, which is **ADR-097's OWN STATED EXIT** (*"Exits to `covered-elsewhere` when `P4-j` lands the lint"*, and *"stays `blocked` until the OG leg runs"*) and not a second ruling. **SO THE SUMMARY CELLS THAT FINALLY MOVE ARE NOT THE PAIR ADR-097 NAMED**: `blocked` 266 to 264, `covered-elsewhere` 7 to 9, `outside-loader-boundary` back to 4, `no-fixture-format` holding at 227. **`CI-06/fixture-inventory` VERIFIED THE CITATIONS RATHER THAN TAKING THEIR WORD**, reading **9 covered-elsewhere rows FOR EXECUTION with 0 naming their row only from inside a disabled block.** **ADR-097's REQUESTED FOURTH ENTRY ON SECTION 39's *"things that look like coverage"* LIST IS NOT OWED**: that finding was that `assertWindowAttached` asserted a NARROWER mechanism than the row because the OG surface had none, and the surface now exists. **THE CI STEP WATCHES ITSELF FAIL BEFORE IT REPORTS A PASS**, which is [DELIVERY_PLAN](../DELIVERY_PLAN.md) section 4's own done-condition (*"GS-143 and GS-144 failing the build on a seeded violation"*): 16 specimens, **9 seeded that must be refused BY THE RULE EACH NAMES and 7 clean that must build**, three of them on the OG path with a clean body, and a refusal for the WRONG rule fails the step. **It was watched red**: one figure needle removed, the runner exits 1 naming both day-count specimens, restored and re-run clean. **ONE STEP IN `ci.yml` AND THE WORKFLOW IS NOT RESTRUCTURED**, `P4-i` holding cells in that file. **The authored corpus under `apps/site/content/` is EMPTY and the step PRINTS THAT COUNT** rather than implying coverage it lacks; the seeded leg is what keeps it from going vacuous. **NO ADR, so `111` is unused and its ALLOCATION row is left exactly as it stands**, `P4` section 5.3 having reached. **`apps/site/src/index.ts`'s *"deliberately not here"* note is CORRECTED rather than deleted**: it predicted the lint would land *"with the framework that compiles the MDX"*, and nothing compiles anything here. Suite **122 files / 2,028 passed / 1 skipped** to **124 files / 2,062 passed / 1 skipped**, `apps/site` **15 files / 177 passed**, gates **30 of 30**, `verify` exit 0, `eslint` and `format:check` clean, `gates.mjs generate` reporting every span already matching. No migration, no `packages/db` file, no route under `apps/api`, no dependency, no `CI-06` letter, no gate register row and no `VG` row |
| [2026-08-26 - Session 226](2026-08-26-session-226.md) | **[ADR-111](../decisions/ADR-111.md) lands, `status: proposed`, UNSIGNED, not money, and it rules 2 of 3 by contract row and 3 of 3 by disposition.** [P4](../plans/P4-portal-and-site.md) section 8's `P4-h` in both halves: five read screens and the three shapes [session 158](2026-08-24-session-158.md) found with no contract row at all. **`EconomicCalendarPanelResponse` ENTERS [API_CONTRACT](../architecture/API_CONTRACT.md) AS A NEW SECTION 6.1**, `GET /economic-calendar`, `Auth: session`, its shape the portal type UNCHANGED IN A SINGLE FIELD because that type was built column by column off [`0039`](../../packages/db/migrations/0039_economic_calendar.sql); a subsection rather than a new section because **section 12 is addressed BY NUMBER by `CI-06k`, `DEP-M4-07` and [ADR-039](../decisions/ADR-039.md) amendment 4** and inserting ahead of it breaks all three silently. **`required_factor` ENTERS SECTION 2 AS `Problem`'s OPTIONAL EXTENSION MEMBER** over section 12's six-token vocabulary, on a `403` and nowhere else. **SESSION 218's OPEN PULL REQUEST ALREADY SENDS THAT FIELD AND DOES NOT DECLARE IT**: it edits six files and none is API_CONTRACT, so the field is SHIPPED AND UNDECLARED against an `approved` document, and this row makes it legal rather than ruling against a branch in flight. **THE DISCREPANCY IS RULED RATHER THAN PICKED SILENTLY: the `403` member discharges section 12 and discharges NEITHER `DEP-M4-07` NOR `INV-M4-14`**, because a field arriving on a refusal cannot render a control disabled BEFORE the refusal, which is [M04](../plans/M04-trader-portal.md) section 3.7's entire subject. **`ImpersonationSession` IS DECLARED AS SECTION 3.2 WITH NO ENDPOINT**: its carrier is `Me.impersonation` on `GET /me`, because a dedicated read is a second session resolution and **reintroduces `GS-301`**, and `GET /me` is the auth surface's, which [P4](../plans/P4-portal-and-site.md) section 6 puts in no phase at all. `ADMIN_ORIGIN` stays a placeholder ([ADR-012](../decisions/ADR-012.md)) and the shape has no field an origin could go in. **FIVE SCREENS, SEVEN OF ELEVEN NOW BUILT.** The rule diff **RENDERS BOTH SIDES AND NO DELTA**, because every delta is a money value computed client side at the one place the subtraction looks harmless, and the walk is **STRUCTURAL over opaque JSON** because a typed walk reports "nothing changed" about a contract that gained a key. `SC-M4-07` is built against `kyc_status`'s **FIVE** members at [`0001:29`](../../packages/db/migrations/0001_extensions_and_enums.sql) where [M04:86](../plans/M04-trader-portal.md) says four, recorded as a finding and not ruled. Section 7.9's vocabulary rule is enforced as a **REFUSAL ON THE WIRE STRING**, the only place a check can read the string that renders. `surface.test.ts`'s `purchase` entry is **NARROWED AND NOT DELETED** to `checkout`, `reset` and `acknowledg`, which is session 158's own test of a good amendment and is strictly stronger on the write path. `api-types.test.ts` admits the two formerly-exempt types now that they have rows and its floor rises **40 to 150**. Suite **125 / 2,075** to **130 files / 2,125 passed / 1 skipped**, 30 of 30 gates, `verify` exit 0. **No migration, no `packages/db` file, no `apps/api` file, no dependency, no gate letter and no `VG` row** |
| [2026-08-26 - Session 227](2026-08-26-session-227.md) | **[ADR-112](../decisions/ADR-112.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH and the `E2` read is owed.** [ADR-109](../decisions/ADR-109.md) clause 2 said the accessor needs a keyed read and a keyed write and refused to design it; **the founder DELEGATED that design authority to this session and delegated the read to nobody**, and the entry says so in its own approval line. **THE DISPATCH'S CENTRAL MEASUREMENT WAS RE-TAKEN AND HOLDS, WHICH IS WHY A BROKEN METHOD COULD BE DELETED RATHER THAN DEPRECATED**: outside `packages/db` itself every mention of `systemTx.update`, `firmTx.update` and `scopedTx.update` in the tree is a COMMENT routing around it, so `ScopedTx.update`, `ScopedTx.delete`, `SystemTx.update`, `SystemTx.delete`, `FirmTx.update` and `FirmTx.delete` **no longer exist** and [ADR-042](../decisions/ADR-042.md)'s finding that a comment is not a control is why they are not left behind one. **ITS ARITHMETIC IS CORRECTED AND ITS SUBSTANCE IS NOT**: the defect list names eight methods and calls them six, and the six are the WRITES -- `systemTx.rows` and `firmTx.rows` render the same bare table scan `systemDb` and `firmDb` have granted since [ADR-084](../decisions/ADR-084.md), and they are the pair with the two real callers. **FOUR RULINGS**: a predicate is a TYPED EQUALITY over declared columns and never a `SQL` fragment, because a `SQL` parameter hands every call site the surface [ADR-008](../decisions/ADR-008.md) scoped the accessor to bound while reading like ordinary accessor use, and `byId(id)` is refused ON EVIDENCE because `idempotency_keys`' primary key is `key` and **13 registered tables declare a composite one**; a WRITE takes an address CONTAINING a unique key the schema declares while a READ may take a plain filter, **because a filtered read is a NARROWING of a read that already exists at the same authority and grants no reach**; the columns the HANDLE PINS are both the columns the caller may not name and the columns that COUNT toward that key; and the tenancy narrowing and the address COMPOSE, with the drop-tenancy shape refused by `tsc` as `TS2345` rather than by a test somebody has to keep. **TWO DESIGN ERRORS WERE FOUND BY ONE COVERAGE ASSERTION RATHER THAN BY READING AND BOTH ARE RECORDED**: requiring the CALLER's half to contain a key left the four tables keyed `(identity_id, ...)` unaddressable, and refusing a scoped caller naming a `derived` rule's local column left `analytics_snapshots` unaddressable because an `EXISTS` over `accounts` pins neither of `(account_id, as_of_trading_day)`. **THE ADDRESSABILITY CLAIM IS FOLDED AGAINST ALL 47 MIGRATIONS IN THE DIRECTION THAT CAN HURT AND THAT SET IS EMPTY**: zero column sets `schema.ts` declares unique are absent from the DDL, while **34 the DDL declares are absent from the schema**, which is fail-closed and is asserted as a PROPERTY rather than as a count. **`treasury_balances` IS THE ONE REGISTERED TABLE OF 104 WITH NO ADDRESSED WRITE AT ALL**, because `0009` gives it `PRIMARY KEY (account_code, as_of)` and the transcription declares none; `schema.ts` is outside the fence so the suite pins that set to exactly one table. **A PARTIAL unique index is not read as a key** and `provisioning_queue_intent_uq` is the live counter-example. **EIGHT RUNTIME MUTATIONS SEEDED AND ALL EIGHT CAUGHT**, the widest being `handlePinnedColumns` reading nothing at **9 red**, which is [ADR-102](../decisions/ADR-102.md)'s own warning executed; **SEVEN COMPILE REFUSALS WATCHED FAILING BY THEIR ERROR**, the last of them the one the ruling turns on. **THE ROUND TRIP EXECUTED** on PostgreSQL 16 with all 47 migrations applied, ten cases: **row 3 is the approval line, a keyed update through `scopedDb(A)` naming B's row writing ZERO rows and leaving it unchanged**, and row 7 shows one address through two handles reaching two different rows. **`AuthBackend` is NOT wired and `IdempotencyStore` is NOT implemented**, deliberately: this builds the construction they were blocked on, not the callers. **`index.ts` is outside the fence and is not edited**, which costs nothing today because the methods are reachable without naming their types. Suite **128 files / 2,170 passed / 1 skipped** to **134 files / 2,244 passed / 1 skipped** on the merge result, `packages/db` **407 to 431**, `typecheck` exit 0, invariants **9 of 9**, gates **30 of 30**, `verify` exit 0, `CI-01` **12 of 12**, registry unmoved at **104 of 111**, and **`0048` stays free** |
| [2026-08-26 - Session 228](2026-08-26-session-228.md) | **[ADR-116](../decisions/ADR-116.md) lands, `status: proposed`, approval line UNSIGNED, NOT money path.** Playwright entering the catalog is a **`VG-12` admission** which [STRATEGY:192](../testing/STRATEGY.md) says in its own words *"a session cannot grant itself"*; **the founder DELEGATED it for this slice and section 1 of the entry records the delegation rather than proceeding as though the gate did not apply**, which is the one sentence separating this from the failure mode `VG-12` exists to catch. **THE VERSION IS CHOSEN ON A MEASUREMENT AND `latest` IS REFUSED**: `playwright-core`'s `browsers.json` pins a chromium REVISION rather than a range, `1.56.1` is the newest version pinning `1194`, and `1194` is the build every Merit container carries at `/opt/pw-browsers`, so the gate RUNS here; `1.62.1` would have pinned a revision no container holds and produced a check that cannot run, which `repo-invariants.mjs` calls not a check that passed. **THE CLOSURE ARITHMETIC IS RUN IN BOTH FORMS BECAUSE SESSION 183 PROVED THEY DIVERGE**: `279 -> 283`, **`+4`**, of which ONE is a per-platform binary (`fsevents`, `os: [darwin]`, optional) and **THREE are the real admission**, against Next.js's `+51` with 36 per-platform and fifteen real. `pnpm audit --audit-level=low` reports no known vulnerabilities, all four entries carry a `sha512` integrity hash, no build script is ignored, and **the BROWSER is not in the lockfile and is not in that count**, which is where a supply-chain reading of this admission would otherwise stop one step early. **`CI-08` MOVES TO IMPLEMENTED IN THE SAME COMMIT AS ITS ARTIFACT**, P4 rule 10, naming [`ci.yml`](../../.github/workflows/ci.yml) job `e2e`. **THIS SESSION IS THE FIRST TO MAKE THE REPAIRED NEEDLE FIRE FOR REAL AND IT FIRED FOR THE RIGHT REASON**: the finding named `'@playwright/test@1.56.1':` at two spaces and NOT `next@16.3.2`'s optional peer declaration at six, which is still in the file, so [ADR-095](../decisions/ADR-095.md) section 8's repair is proved by the arrival it was repaired for. **AND THEN IT RETIRES, BY THE REGISTER'S OWN RULE**: with no condition naming that artifact, `playwrightInLockfile` is an entry naming nothing, which the stale-direction loop calls a finding; **`falsify.mjs`'s PAIR DOES NOT SURVIVE AND THAT WAS MEASURED RATHER THAN ASSUMED**, both reported `CONTROL DID NOT FIRE` before the edit, one because its seed asserts the absence it was written against and one because no row can produce `HAS ARRIVED` any more. **THE COST OF THAT RETIREMENT IS NAMED**: the only EXECUTABLE statement in this repository that a peer declaration is not an install becomes prose, and [ADR-042](../decisions/ADR-042.md) ruled prose is not a control. **THE PASS RENDERS FIXTURES AND NOT PAGES, MEASURED RATHER THAN CHOSEN**: `ls apps/*/src/app` returns `No such file or directory`, `CI-07` is still open on exactly that path and this gate probes it ABSENT, so there is no page to render; the honest half is that the COMPLIANT direction is self-referential today and **the SEEDED direction is what makes it a control**, and that half is about the checks rather than the fixtures. **FALSIFICATION IS IN BAND RATHER THAN IN `falsify-ci.mjs`**: each surface ships a compliant fixture AND one built to break every check, and the stage fails unless the first is silent and the second fires ALL EIGHT, so both directions run in one job on one commit. **PROVED BY BLINDING**: each of the eight check functions replaced by `return [];` in turn, eight runs, each reporting `8 failed, 8 passed` and naming exactly the blinded check; and in the other direction a 4px `#3b82f6` left border added to the compliant fixture turned four tests red naming `SS-02` AND `SS-04` at one selector. **TWO OF THREE PROJECTS**, `apps/admin` named rather than rounded up: it is in no P4 slice and ADR-095 declined even its framework. **THE DISPATCH FENCE NAMED NEITHER `scripts/corpus` FILE AND THE STOP CONDITION IS UNREACHABLE WITHOUT BOTH**; [P4](../plans/P4-portal-and-site.md) section 8's `P4-i` row names both and section 9 makes shrinking the register this slice's obligation, so the edits are taken at the PLAN's scope and reported. **`CI-07` and `VG-2` ARE UNTOUCHED AND THAT WAS CHECKED**: the `e2e` job builds nothing, so `CI-07` acquires no implemented leg and `VG-2` keeps its chain, with `CI-06/vg-inventory` unmoved at 12 rows. Gates **30 of 30**, invariants **9 of 9**, `verify` exit 0, suite **134 files / 2,244 passed / 1 skipped** unmoved, Playwright **16 passed** |
| [2026-08-26 - Session 229](2026-08-26-session-229.md) | **[ADR-117](../decisions/ADR-117.md) lands, `status: proposed`, approval line UNSIGNED, NOT MONEY PATH by file and the hole it closes is a money-path hole.** [ADR-096](../decisions/ADR-096.md) section 9 named `RI-08`, measured the hole and assigned it to `P4-e`; `P4-e` landed without it, [ADR-095](../decisions/ADR-095.md)'s `F4` and section 9 item 5b cite the same gap, and **three entries had cited it with none closing it.** **THE SCOPE IS THE ONLY THING RULED AND IT IS A WIDENING OF A SIGNED ENTRY.** ADR-096 scopes the check to `apps/site`, widens itself one sentence later to `packages/queue` and calls the wider version *"the better version of it"*; ADR-095 item 5b names the same under-scoping from the framework side. **FIVE manifests now state the accessor's absence as a design property where ADR-096 counted two, and ZERO packages in this workspace declare `@merit/db` in any dependency field**, so the check guards EVERY workspace package against an admission list that is **EMPTY**, which is `onlyBuiltDependencies: []`'s shape and [ADR-084](../decisions/ADR-084.md) section 5's. **`apps/api` is NOT pre-admitted** though [ADR-109](../decisions/ADR-109.md) says it will need the accessor first: a list that admits against an unstated need is a list joining itself. **THE MANIFEST GATE WAS MEASURED IN BOTH DIRECTIONS RATHER THAN ASSUMED**, and it contradicts a line in the tree: an undeclared `@merit/db` import reports **`TS2307` from `apps/site` AND from `apps/api`**, with no `paths` mapping anywhere, so the manifest field is not merely where the dependency is recorded but the only place the capability can be acquired -- which is why `RI-08` has no source half, and which makes [`apps/api/package.json:15`](../../apps/api/package.json)'s *"`tsc` resolves it happily through the workspace"* **wrong on this tree** (out of fence, reported). **THREE READINGS OF "RESOLVES TO THE ACCESSOR", EACH SEEDED ON THE REAL TREE AND WATCHED FAILING**: the key, a `workspace:`/`npm:` alias parsed out rather than substring-matched, and a `link:` path that writes the accessor's name NOWHERE. **BOTH NEAR-MISSES WATCHED STAYING SILENT**: `@merit/dbtools`, the substring trap, and an ordinary workspace dependency. **FALSIFIED BEFORE IT WAS TRUSTED**, which is `RI-10`'s landmine paid off: with `acquires` neutered the six detection cases go red and the two silence cases stay green. **TWO GUARDS THROW RATHER THAN PASS** and they are about the admission list, because that is the direction this check gets weakened in: a stale name, and a list covering every package. **`ALLOCATION` GAINS A FIFTH TABLE, FOR `RI-nn`, and it is an ALLOCATOR and says so**: `gates.mjs`'s `PENDING_SERIES` names the tooling README as `RI`'s register, that README is out of fence and its table stops at `RI-05`, so the pending entry stands. **`CI-06/identifier-series` was probed in BOTH shapes before the table was written, ten rows and five, and both reported 30 of 30.** **FOUR FINDINGS REPORTED RATHER THAN TAKEN.** 10 of 10 invariants, 30 of 30 gates, 134 files / 2,256 passed / 1 skipped, `pnpm run verify` exit 0. **NOT MERGED.** |
| [2026-08-26 - Session 230](2026-08-26-session-230.md) | **Two gates, `gate_count` 30 to 32, and [ADR-118](../decisions/ADR-118.md) at `status: proposed`, unsigned.** `CI-06/table-row-width` catches [#299](https://github.com/lam2-coder/meritfutures/pull/299)'s corruption, which all 30 gates passed over: a row whose cell count disagrees with its table's delimiter row. **The first-cell-shape rule #299 proposed was measured and rejected** -- at an 80% homogeneity threshold it reaches 593 of 1,593 tables and MISSES #299, at 50% it catches #299 and costs 155 registered rows; cell count reaches 1,604 of 1,604 with NO shape exemption and 17 registered rows. `CI-06/derivable-counts` runs the sweep `CI-06g` has declared owed in its own covers line since it was written. **ADR-118 was reserved conditionally and the condition was met**: enforcing ADR-034 forced a ruling on WHICH documents its count rule reaches, and a section headed by a date or a session number is a RECORD it does not reach. 32 of 32 gates, 80 scope cases, `verify` exit 0 |
| [2026-08-26 - Session 231](2026-08-26-session-231.md) | The artifact `CI-09`'s replay self-audit leg has waited on since 2026-08-20 is ruled: [`scripts/demo/world.ts`](../../scripts/demo/world.ts), a path, and the register shrinks from four entries to three. **The world is a value and not a Postgres seed**, on two measurements either of which is enough: nothing in this tree reads a database into `BatchPorts`, and `rule_states` has no column for `lifetimeSettledCents`, `breached` or `breachKind`, so `loadAccountDay` could not serve a `prior` back even once an adapter exists. **`CI-06/gate-inventory` goes RED with two findings, predicted before the row was edited**, which is ADR-073 (b)'s second sentence working. `runNightlyBatch` cannot drive a world more than one day deep, so `foldAccountDay` is the caller |
| [2026-08-27 - Session 232](2026-08-27-session-232.md) | **[ADR-120](../decisions/ADR-120.md), `status: proposed`, UNSIGNED. MONEY PATH, auth, `E2` owed.** `DB_ADMITTED` has a member for the first time and it is `@merit/api`: clause 1 rules that the test is not whether the need is real but whether the need is the SUBJECT, so sessions 218 and 220 were right and neither is overturned. **The price ADR-117 section 4 named was paid and watched** -- manifest line in, list empty, `check:invariants` exits 1 at 9 of 10. **THE HEADLINE IS THE MEASUREMENT AND NOT THE ADMISSION**: ADR-112 unblocked EVERYTHING A SESSION CAN DO AND NOTHING THAT MAKES ONE, because `ScopedTx.insert` takes `OwnedTableKey` and `sessions` is `derived`, and a pre-identity read of `users` by email has no door at any authority -- and ADR-109 clause 1's ground for closing `SystemReason` at two words has MOVED, since it refused a third on the argument that it would buy a door with no predicate and ADR-112 supplied the predicate. **Four of sixteen wired, twelve refused with their own named blocker**, `IdempotencyStore`'s identity arm complete and its unowned arm refused with its unrenderable predicate written out. **The session token carries its own identity and the claim is a hint the database is never asked to trust**, refused twice and independently; there is a parser and no minter, because nothing can insert the row one would be minted for. **Four findings that each stop a method and none of which is the accessor's fault**: no preview column on `phone_change_requests`, no source anywhere for `Me.max_accounts`, no specified digest for `otp_challenges.code_hash` and nowhere to keep a key for one, and nothing in this tree that delivers a code. **THREE forced widenings where the plan predicted two**, the third being a falsify seed that quietly stopped firing once its artifact arrived. Eight seeded mutations plus four more the suite could not carry, and a round trip through Fastify's own router against a real PostgreSQL where A naming B's session id answers 404 with both rows still live afterwards. **A deployment still cannot mint a session** |
| [2026-08-27 - Session 233](2026-08-27-session-233.md) | `scripts/demo/` named `.js` files that are `.ts` at 32 sites across 7 files, so `node --experimental-strip-types scripts/demo/main.ts` died with ERR_MODULE_NOT_FOUND while every gate in the repository stayed green. **`RI-10` missed all of it because `RI-10` was scoped to `src/`**, which was a considered decision the tree grew past. [ADR-121](../decisions/ADR-121.md) widens the scope and fixes what "shipped source" means: the directories `node --experimental-strip-types` loads, minus `test/`. **The `nightly.yml` line that carried a standing instruction to move did NOT move**: measured after the sweep, `seed-world.ts` exits 0 writing zero bytes, so the move would have swapped a self-audit for a module-graph load and gone green doing it |
| [2026-08-27 - Session 234](2026-08-27-session-234.md) | The nightly statistics run: it reads authoritative closed-session facts, computes each registered definition, and writes ONE immutable `published_statistics` row per window per measure per grain cell. **`input_digest` is taken over the computation's own argument rather than over a description of the query**, which is what makes "same digest" mean "same inputs" instead of "the same words about the inputs". It EXCLUDES the value, so two runs whose digests match and whose values differ is an arithmetic regression rather than an invisible one. **The database already refuses the overwrite**: `0026` revokes `UPDATE` and `DELETE` on this table from `merit_app` AND `PUBLIC`, `published_statistics_window_uq` refuses the second row, and `ADR-112` removed `update` from every handle, so the write port carries an insert and no other verb |
| [2026-08-27 - Session 235](2026-08-27-session-235.md) | TWO OWED TRANSCRIPTIONS IN ONE BRANCH, because both are things put in the wrong place ON PURPOSE by sessions that said so. **`ADR-073` section 5's refusal on `accountsAudited === 0` moves out of the demo runner and into `runReplayAudit`**, where `OI-14`'s guard was structurally unable to see the zero case: it reads stored state, so over an empty book it returned `accountsAudited: 0, diverged: 0` and exited green over nothing. **Watched failing where it exits green on `421e52e`** and the `replay-self-audit` nightly command is byte-identical before and after. **`ADR-123` proposed**, because the move INVERTS a green assertion and therefore changes what the audit PROMISES: an empty production book now refuses, and clause 3 accepts that cost in full. Second, **`packages/enrichment` is wired into `checkout.ts`** and `ADR-115`'s TWO places observe mode could quietly become enforcement are asserted at the call site, one by reading the source and one behaviourally. **A vendor that never answers, a vendor that throws and a reporter that throws each commit the purchase** with a response byte-identical to the unwired baseline. **Three seeds watched failing and all three removed.** THREE FENCE FINDINGS REPORTED RATHER THAN RESOLVED QUIETLY. 32 of 32 gates, 10 of 10 invariants, `verify` exit 0 |
| [2026-08-27 - Session 236](2026-08-27-session-236.md) | **[P5-payouts-and-wallet](../plans/P5-payouts-and-wallet.md), `status: draft`: fourteen slices, four waves, fence BY FILE, money column, depends-on BY FILE.** **The answer is the INVERSE of [P4](../plans/P4-portal-and-site.md)'s**: four of P5's five stated contents are WRITES and the fifth reads a table with no producer. **The precondition is four sentences in one file** and [ADR-112](../decisions/ADR-112.md) wrote every one of them on purpose: `RowFilter` admits *"no `OR`, no `IN`, no range and no `IS NULL`"* and `SqlExecutorReason` has ONE member, so the hourly sweep cannot express `freeze_expires_at <= now()`, `readLiveHalts` cannot say `released_at IS NULL`, and **nothing in this tree can make one of two racing transactions lose**, which is `GS-230` and `DELIVERY_PLAN`'s own *"wallet concurrency green"*. **THE BRIEF'S CENTRAL PREMISE DID NOT SURVIVE**: `INV-M20-06`'s contradiction was CLOSED by [ADR-075](../decisions/ADR-075.md) six days earlier, in the very session the brief cites as having reported it, and what remains is a stale restatement inside [M20](../plans/M20-wallet.md)'s own open-question section. **The contradiction P5 DID meet is [ADR-124](../decisions/ADR-124.md)'s**: the automatic global ledger halt has no mechanism, `0016` rules it never will be a row, and `RB-07` tells an operator to confirm a stop nothing performs. Two P5 contents are in no phase cell, both money path. **32 of 32 gates, `verify` exit 0. No code, no migration, nothing signed** |
| [2026-08-27 - Session 237](2026-08-27-session-237.md) | **227 blocked rows carried ONE sentence written 227 times and the sentence was false**: *"the only fixture format in the tree is M1's day stream folded through `advanceDay`"*, while the tree holds FOUR and section 39's own `GS-030` row cites a second one with its own loader. **[ADR-125](../decisions/ADR-125.md) rules the stated reason FALSE on all 227 and moves SEVEN**, each executed and watched passing rather than read: `GS-100`, `GS-115`, `GS-148`, `GS-285`, `GS-309`, `GS-310`, `GS-313`. **Every one became dischargeable when its MODULE'S CODE landed and brought its own suite**, which refutes by measurement the prediction `ADR-072` refused `no-module-code` on. **The term is NOT retired and the reason is a FENCE**: `FIXTURE_BLOCKERS` lives in `gates.mjs`, session 245's file, and `CI-06/fixture-inventory` assertion 5 checks the summary table against it in both directions. **FOURTEEN rows a test file NAMES do not move**, each on the naming suite's own written statement of what it does not assert, which is nearly twice the group that does. `GS-302` and `GS-303` ARE executed, by an install-job probe this document's `covered-elsewhere` definition excludes, and that residual is reported rather than taken |
| [2026-08-27 - Session 238](2026-08-27-session-238.md) | `ADR-120` found that `ADR-112` unblocked everything a session can DO and nothing that MAKES one, and left the two constructions -- minting a session and resolving a person from the address they typed -- as rulings for the founder. This session rules them. **`ADR-109` clause 1's FINDING is DISCHARGED rather than superseded** (its caller held a door and lacked a predicate; `ADR-112` supplied it) **and its DISPOSITION is UPHELD ON A NEW ARGUMENT**, because the old one died when `ADR-112` landed. **Both constructions are expressible at `systemTx` TODAY and neither is expressible at any authority a request handler can name**, so the gap is a VOCABULARY gap, which is the offer `ADR-109` declined arriving with its refusing argument dead. **The ruling takes a TABLE vocabulary rather than a REASON vocabulary**: a reason grants every one of the 104 registered tables at six verbs, a table vocabulary grants exactly what it names. **The measurement that made that a ruling rather than a preference is `ledger_entries`**, which is `derived`/`hop`, so a construction generic over the class would have put a money-creating write behind a trader's own handle |
| [2026-08-27 - Session 239](2026-08-27-session-239.md) | `CI-09`'s third leg of four, which had waited since 2026-08-20 on an artifact that was never a file: "the `VG-12` admission, which a session cannot grant itself". **The founder DELEGATED the grant and [ADR-127](../decisions/ADR-127.md) RECORDS the delegation** rather than proceeding as though the gate did not apply, which is [ADR-116](../decisions/ADR-116.md)'s shape one leg over and the only form the clause survives. **THE ENTRY'S CENTRAL RULING IS A BLOCK THAT IS NOT WRITTEN**: [ADR-073](../decisions/ADR-073.md) section 6 measured seven days ago that a Stryker score threshold spelled in `.mjs` fires `RI-02` at `CI-01` and the same values in JSON do not, and said neither outcome is a ruling anybody made; both the JSON spelling and an edit to `RI-02`'s needle are REFUSED, and writing no such option at all is the literal form of STRATEGY's *"reported as a trend rather than a threshold"*. **The default that ruling now rests on is asserted out of the installed tool's own shipped schema and throws before the run starts.** **THIS ROW COULD NEVER HAVE SPENT A DAY AS A FINDING** the way the replay leg one sentence up did, because an unprobeable artifact arrives silently: the register shrinking is the only observable event there ever was, and it fired at 29 of 32 before it was repaired |
| [2026-08-27 - Session 240](2026-08-27-session-240.md) | THE FIVE OPEN ITEMS OF THE MIGRATION CLUSTER, CLOSED IN TWO MIGRATIONS RATHER THAN ONE, and the split is the finding. Four of them (`OI-03`, `OI-04`, `OI-12`, `OI-13`) are one idea: **a write that changes what the database already said goes through a path that records who and why, or it does not commit**. `OI-01` is a different one: a table the approved design named and the fold did not build. `0048` carries the audited-write regime, `0049` carries `reserve_coverage_snapshots`, and separating them is what lets the founder reject the unruled half without rejecting four guards. **`OI-03` needs no DDL at all**: its own words ask for a check that reads the revoke list AGAINST THE DOCUMENT, and the document currently carries three contradictory copies of the append-only list saying twenty-two, twenty-two and twenty-three while the database says twenty-five |
| [2026-08-27 - Session 241](2026-08-27-session-241.md) | **BOTH ROWS WERE STALE RATHER THAN OPEN, AND THE SECOND ONE NOBODY HAD NOTICED.** `OI-14` is satisfied and `OI-30` was repaired on 2026-08-24 by [session 151](2026-08-24-session-151.md), three days before the dispatch called it open, so the blind spot [ADR-117](../decisions/ADR-117.md) names in its own register has now produced TWO instances in one wave. **The dispatch's premise about `OI-14` is refuted at source**: [#315](https://github.com/lam2-coder/meritfutures/pull/315) did not move `OI-14`'s refusal, it added a second one for the EMPTY BOOK, and [ADR-123](../decisions/ADR-123.md) section 4 says so itself. **Both guards were falsified independently** and each case pinned to its own message. **AND THERE IS A THIRD SCALE NEITHER GUARD SEES, EXECUTED**: an account named by `accountsWithStoredState()` with no rows and no days returns `{accountsAudited: 1, storedRows: 0, inScope: 0, diverged: 0}`, which is `FM-17` whole. It is `ADR-123` section 7's rejected alternative, whose stated reason is true of one half of that input class and false of the other. **Pinned as a hole, not fixed**: closing it reverses an argued rejection on a money-path-by-position file. All three `0015` citations re-derived from `0015`, `0037`, `0046` and the two live predicates rather than taken on session 151's word, and none is a case where the test depends on the retired property. **`ADR-129` NOT USED, condition did not fire, number returns to the pool** |
| [2026-08-27 - Session 242](2026-08-27-session-242.md) | M06 section 2 stated the size of its own delta table in prose. It read *"Seven deltas"*; the generator derives TEN from the table's rows, so the sentence and the table disagreed by three. The count is a `m06_delta_count` span now. **`CI-06/derivable-counts` did not flag it, and did not flag it either when the span was removed and the count retyped at the value the table actually holds**: the vocabulary binds one noun to one CORPUS-WIDE query, `deltas` is bound to `manifest_changes`, and the remedy the gate names for an unpoliced population is shadowed forever by its own first-match-wins rule. `ADR-130` rules a `file` predicate so a PER-DOCUMENT population is expressible. The gate's own rationale lists `Six deltas` -- this exact sentence -- as a specimen of the noise a rejected recogniser would produce |
| [2026-08-27 - Session 243](2026-08-27-session-243.md) | The `CI-06<letter>` registry is retired while it still has a free letter, and the ruling turns on what that letter IS. `nextFreeLetter()` scans `a` to `x` and every `CI-06p` seed claims two past what it returns, so the one free letter is the falsifier's headroom rather than an allocation: a gate taking it does not take a name, it removes the ability to watch `CI-06p` fail. Watched, on a seeded tree: claiming `x` reports `SEED IS STALE ... seed anchor exhausted`. So the series is CLOSED at `w`, `x`, `y` and `z` are retired unused, and `CI-06/closed-letter-series` fails on a row that claims a twenty fourth. No existing letter is renamed: 145 of the 283 citing files are dated session records ADR-118 rules may not be rewritten. |
| [2026-08-27 - Session 244](2026-08-27-session-244.md) | A GATE WHOSE TITLE AND BEHAVIOUR DISAGREED FOR A YEAR, AND THE TITLE WAS THE CORRECT HALF. `CI-06n` said *"Every registry entry has a README ROW"* and its parser accepted any markdown link anywhere in a registry README, so **[ADR-043](../decisions/ADR-043.md) sat outside the ADR registry table on 2026-08-16 with twelve gates green**, indexed by a sentence in that README's preamble. **[ADR-043](../decisions/ADR-043.md) is the ruling that ASKED for the gate and it says "row" twice**, so the original intent decided it and the parser is narrowed rather than the title. **A row is a line beginning with a pipe, outside a code fence.** **MEASURED IN BOTH FORMS BEFORE IT WAS RULED: 648 entry files over five registries, 648 satisfied under the loose reading, 648 under the tightened one, so NO DOCUMENT GOES RED.** **[ADR-132](../decisions/ADR-132.md)'s conditional reservation FIRED** because the narrowing changes what a registry README may look like from here. **Watched failing on the exact 2026-08-16 input**, PASS at `cce89de` and FAIL here on the identical tree, plus a second seed for the fence direction. **A NARROWING, NOT A WEAKENING**, and the direction is what decides it. **ONE THING OWED AND IT IS THE LARGEST: the new direction has no `falsify.mjs` seed**, because that file is outside this session's fence. 32 of 32 gates, 10 of 10 invariants, `verify` exit 0, 150 files / 2,656 passed / 1 skipped, `falsify` clean |
| [2026-08-27 - Session 245](2026-08-27-session-245.md) | THE BACKLOG BECOMES A QUERY. Every count in [section 39](../testing/golden-scenarios/39-fixture-status-and-blockers.md)'s two summary tables, and the row total in its opening sentence, is now a `gen` span over a new [`scripts/corpus/fixture-backlog.mjs`](../../scripts/corpus/fixture-backlog.mjs) reading the rows below, so the number is READ and can no longer be TYPED. **THE FIRST RUN OF THE GENERATOR FOUND A COUNT THAT HAD ALREADY DRIFTED BY FORTY**: the opening sentence read 276 against 316 rows, in the document whose own next paragraph says every count above was recomputed by reading the tree, and it was silent to `CI-06g`, to `CI-06/derivable-counts` and to 32 green gates. **THE FINDING IS BIGGER THAN THE SPAN.** `CI-06/derivable-counts` cannot see this number for three independent reasons and only the third is a hole: a SUMMARY table row is excluded by a shape rule whose stated justification is about a DATA row. **Measured with a probe rather than argued**: identical text, identical value, identical noun, once as prose and once as a summary row, gives one finding and one silence. **Five perturbations, two of them negative controls that must REFUSE**, and both did. One landed harness seed broke and ANNOUNCED ITSELF. `ADR-133` proposed, `OI-25` closed. 32 of 32 gates, 10 of 10 invariants, `verify` exit 0 |
| [2026-08-27 - Session 246](2026-08-27-session-246.md) | THE RECORD WAS THE SMALL HALF. [`admin_actions`](../architecture/data-model/admin_actions.md) now carries `SD-M6-11`'s two columns, its biconditional `CHECK` and its fourth index, read from [`0017`](../../packages/db/migrations/0017_events_and_audit.sql) and [`0043`](../../packages/db/migrations/0043_admin_attributed_actions.sql) rather than from the record or from `schema.ts`. **The class underneath it is that `CI-06i` verifies EXISTENCE and not CONTENT**: it reconciles the table set in both directions and never compares columns, so a design record can contradict its own DDL indefinitely with every gate green. **[`data-model-columns.mjs`](../../scripts/corpus/data-model-columns.mjs) is that comparison** and it found the drift was NOT confined to this record: three siblings are missing a column each, `contact_channels.complained_at`, `plan_versions.fee_back_repeats` and `promotional_credit_grants.source_payout_request_id`, every one of them outside this fence. **The check therefore lands UNREGISTERED rather than narrowed until it was green**, with the registration condition written into [ADR-134](../decisions/ADR-134.md) clause 3 instead of left to judgement. **Seven seeds watched failing, three of them the guards that stop it passing on inputs it cannot reach.** **The schema was right in all four cases and no migration is owed.** 32 of 32 gates, 10 of 10 invariants, `verify` exit 0, 150 files / 2,656 passed / 1 skipped |
| [2026-08-27 - Session 247](2026-08-27-session-247.md) | **THE DISPATCH'S PREMISE WAS FALSE AND THAT IS THE FINDING.** `OQ-F3-01` and `OQ-F3-02` did not sit open since session 86: [ADR-067](../decisions/ADR-067.md) sections 3 and 4 ruled both on 2026-08-20, the entry heads itself `accepted`, and the founder granted it on 2026-08-21. Only the registers had not moved. **The contradiction the dispatch named was closed by [ADR-075](../decisions/ADR-075.md) on 2026-08-21, and closing it created the one that is live**: `INV-M20-06` went to `identities.status = 'active'`, which shut the SECOND of the two exits `ADR-067` section 3 stands on, so **for a `closed` identity the horn `ADR-067:103` declared FALSE is true again** and [`0038:106`](../../packages/db/migrations/0038_account_adjustments.sql)'s stated reason, inside a MERGED money-path migration, is no longer true of the door set. **[ADR-135](../decisions/ADR-135.md) clause 2 posts the credit anyway**, because a refusal to post is not a refusal to owe and refusing hides the liability from `P-M6-01` rather than preventing it; the accepted cost is an obligation Merit records and cannot pay, bounded by `closed` having **no writer** because `G-ENFORCEMENT-CLOSE` is defined nowhere. **Clause 4 says the missing fact is what closure IS and refuses to invent it. Clause 5 hands `INV-M20-06` against `INV-M20-09` to session 236's `M20`.** No migration number spent; `M20`, `0038` and `ADR-067` untouched. **One test, six assertions, two seeded reverts watched failing and both files restored byte-identically.** The approval clause names a five-part predicate rather than a count and was RUN. 32 of 32 gates, 10 of 10 invariants, `pnpm run verify` exit 0, 150 files / 2,662 passed / 1 skipped |
| [2026-08-27 - Session 248](2026-08-27-session-248.md) | SIXTEEN ROWS, SIXTEEN READINGS, AND `ADR-136` IS NOT WRITTEN BECAUSE THE TIE-BREAK RESOLVED ALL OF THEM. **The mis-filing check ran first and cleared every row at once**: all sixteen are M1-owned per [section 33.1](../testing/golden-scenarios/33-ownership-index-and-coverage-reconciliation.md) and M1 has 43 fixtures on disk, so `no-fixture-format` is definitionally wrong for each and none is session 237's. **SEVEN MOVE TO `covered-elsewhere`** under [ADR-076](../decisions/ADR-076.md) section 1's governing rule, each case WATCHED PASSING rather than read: `GS-026` to `GS-029` and `GS-042` and `GS-066` in `rules-g-payout.test.ts`, `GS-047` at `PT-07 / RE-P-13`. **NINE STAY**, and two of the nine are ruled by name in signed entries this session did not reopen. **`GS-030`'S CITATION IS REPAIRED**, which [ADR-077](../decisions/ADR-077.md) section 8 assigned to this file and nobody had taken: it quoted a clause `ADR-077` struck, and both halves of it were wrong in opposite directions. **`GS-001` WAS THE ONE THAT LOOKED LIKE A `W1` ROW AND IS NOT**, because the ingest code its assertion would run against does not exist. **`GS-241` IS THE FINDING**: its gross half is ALREADY EXECUTED and the row still cannot move, because the suite names no row and the comment is outside this fence. 32 of 32 gates, 10 of 10 invariants, `verify` exit 0 |
| [2026-08-27 - Session 249](2026-08-27-session-249.md) | THE SMALL DISPOSITIONS, and the term worth checking rather than assuming was the one that paid. **All four `outside-loader-boundary` citations blamed `replay` living in `apps/worker`, and `ADR-078` put `replay` on the engine's public surface five days before this session opened.** The term still stands on all four, for four different surviving reasons, every one of them re-cited: stored `rule_states` and ingest events on `GS-034`, `payout_requests.eligibility_snapshot` on `GS-074`, the cross-version diff machinery on `GS-075`, and `scripts/demo` on `GS-072`. **`GS-072` IS IN A THIRD STATE NOBODY HAD A CELL FOR: its skip cleared itself, the assertion is LIVE and PASSES, and the row still cannot move**, because `CI-06/fixture-inventory` reads `describe.skipIf` as disabled whatever its condition evaluates to. The gate is not relaxed and the two files that would repair it are outside this fence, so it is REPORTED. **`ADR-137` proposed: `OI-16` never held `GS-004` and `GS-031`'s expected end state**, its own register says so, and `ADR-072`'s tie-break run in both directions puts both rows on `format-cannot-express`; the fixture calendar's own `what_this_format_cannot_carry` field had named them by id all along. `OI-16` stays OPEN and holds nothing. `GS-079` stands, re-checked at source. 32 of 32 gates, 10 of 10 invariants, `verify` exit 0 |
| [2026-08-27 - Session 250](2026-08-27-session-250.md) | THE FIRST `app/` DIRECTORY IN THIS REPOSITORY. `apps/portal` carried 18 view modules and 3,484 lines with no app to run in; **`ADR-095` ruling 6 left the first page to `P4-e` and `P4-h` and both merged without one**, so `next build` has exited 1 on _"Couldn't find any `pages` or `app` directory"_ since the framework was admitted. **`next.config.mjs`, `app/layout.tsx` and `app/page.tsx` land and `pnpm --filter @merit/portal build` exits 0.** The layout wires `toShellView` for real, so `ADR-068`'s impersonation band and `INV-M4-09`'s disclosure footer are properties of the root layout rather than habits; the page renders `toPortalErrorKind` over the statuses the contract names and **invents no account**, because the app has no API client and a portal showing a number it did not receive is the one thing `M04` exists to prevent. **THE RESERVED PREMISE FOR `ADR-138` WAS STALE AND THE SESSION REFUSED IT**: `ADR-095`'s clause about nothing refusing a route handler was amended in place on 2026-08-25 and `RI-09` closes that half, re-executed here at 9 of 10. **`ADR-138` rules the half nothing has ever refused: a SERVER ACTION, which has no path to check, and a `next.config` REWRITE.** Seeded together they left every gate byte-identical while `routes-manifest.json` carried `/api/v1/admin/payouts` compiled to a live matcher. **`RI-11` refuses both, falsified in six directions.** `CI-07` reopens exactly as predicted and is left a FINDING rather than re-ruled. 11 of 11 invariants, 31 of 32 gates |
| [2026-08-27 - Session 251](2026-08-27-session-251.md) | THE DISPATCH NAMED FOUR ENDPOINTS AND THE CORPUS HAS TWO OF THEM FREE. `POST /accounts/:account` is in **no section of `API_CONTRACT`**, and `POST /accounts/:id/reset` is **already served by `checkout.ts`**, where a second declaration is a **STARTUP FAILURE** rather than a merge conflict. **`GET /accounts` is served END TO END** and its four money fields come off ONE `rule_states` row. **`GET /accounts/:accountId` REFUSES its `progress` block and the CONTRACT'S OWN NULLABILITY forces it.** **The BOLA case is proven against a real PostgreSQL 16**, not a fixture. Five seeds watched failing and all five removed. `ADR-139` proposed and unsigned |
| [2026-08-27 - Session 252](2026-08-27-session-252.md) | THE ROUTE THAT PAYS A TRADER, and the contradiction the dispatch named is NOT LIVE: [ADR-075](../decisions/ADR-075.md) moved `INV-M20-06` to `identities.status = 'active'` on 2026-08-21 and [M20:62](../plans/M20-wallet.md) reads that today, so the brief's premise and [ALLOCATION](../decisions/ALLOCATION.md)'s own `140` row are both stale by six days. **What IS live is the residue**: `G-ELIGIBLE` names `identities.status = 'active'` and NOTHING CAN EVALUATE IT -- `ExternalGates` has five fields and none is the identity status, and `EligibilityResponse.gates` has ten cells and none is either. `ADR-140` rules the identity-status term is a NAMED REFUSAL placed before the evaluation and never a gate result, `identity_restricted` 422, covering `closed` as well as `restricted` |
| [2026-08-27 - Session 253](2026-08-27-session-253.md) | THREE OF API_CONTRACT's ENDPOINTS THAT DID NOT EXIST, and the third crosses the scope classes the first two sit inside: `plans`, `plan_versions` and `plan_version_sizes` are all `firm`, `purchases` is `owned`, and `GET /purchases` is the first request handler in `apps/api` that composes one response out of both doors. **[ADR-141](../decisions/ADR-141.md) proposed**, and the conditional fired for a reason the reservation did not anticipate: **the blunt leak is a COMPILE ERROR.** Swapping the scoped door for the firm one is `TS2345` naming `FirmTableKey`, twice, measured -- ADR-106 clause 3's partition holding at a call site outside `packages/db` -- so what the entry rules is the part no type holds: the scoped read runs FIRST and the firm read takes its result as an ARGUMENT. **A first draft of the route's header claimed nothing in the type system refused it; that claim was measured, found wrong, and replaced rather than left standing.** THE HUNT THE DISPATCH POINTED AT CAME BACK NEGATIVE AND THE NEGATIVE IS THE FINDING: `plan_versions` has three `ALTER`-added columns that ADR-102 section 7's blind spot cannot see, and none reaches `identities`, so the `firm` classification is right; what IS there is `simulation_waiver_reason`, operator free text about Merit's own publish decision, on a table two of these routes serve publicly -- withheld by section 1's ALLOWLIST and by no scope class, because a scoped read is a filter and not a projection. **TWO CONTRACT FIELDS ARE NOT COLUMNS**: `payout_cap_cents` flattens the jsonb schedule DATA_MODEL section 12 reserved as an array, so the endpoint cannot express M14's progressive cap; `min_payout_cents` lives on the parent version's rules and is per-VERSION, not per-size. **The cursor is applied AFTER the read** because `RowFilter` is equality-only with no `ORDER BY` and no `LIMIT`, and neither `SystemReason` nor `SqlExecutorReason` was widened to avoid it. **VERIFIED AGAINST A REAL POSTGRESQL 16** with all migrations applied and a two-identity fixture: A's purchase read returns zero of B's rows in both directions, B replaying A's cursor reaches only B's own row, and the waiver text does not reach the wire. Three seeds watched failing and removed. **THREE OF THE DISPATCH'S OWN CITATIONS DID NOT REPRODUCE** and are corrected in ADR-141 section 9, including its FENCE: this registry is a directory listing, so seven concurrent route slices collide on nothing |
| [2026-08-27 - Session 254](2026-08-27-session-254.md) | **THE FOUR `/affiliate/*` ROWS EXIST**: `POST /affiliate/links`, `POST /affiliate/creatives`, `GET /affiliate/stats` and `GET /affiliate/statements`, all four declared through [`auth.ts`](../../apps/api/src/routes/auth.ts)'s `EndpointSpec` machinery at `session` and none a `C-27:` row. **[ADR-113](../decisions/ADR-113.md) CLAUSE 2 IS TRANSCRIBED AND ASSERTED IN BOTH DIRECTIONS, WHICH IS THE HALF A ONE-DIRECTION TEST MISSES**: `required_disclosure` carries the version the review WILL require, and `creative` carries **no key matching `disclos`** at all, because `affiliate_creatives_approved_has_disclosure` binds the column to APPROVAL and it is null at `pending`. **`kind`'s five members are PARSED OUT OF [`0005`](../../packages/db/migrations/0005_affiliate_program.sql) BY THE SUITE** rather than compared against a copy of themselves, and a sixth is `validation_failed` before the port. **THE DISPATCH'S CENTRAL CITATION IS WRONG IN THE MECHANISM AND RIGHT IN THE CONCLUSION, AND A SECOND UNREGISTERED TABLE IT DID NOT NAME IS WORSE**: `attributions` is NOT unregistered, it is registered `pair` at [`scope.ts:1067`](../../packages/db/src/scope.ts) and a `derived` rule through it COMPILES and then throws, which that file has said since [ADR-106](../decisions/ADR-106.md); and the table `GET /affiliate/statements` actually reads is **`affiliate_statements`, which is not in [`schema.ts`](../../packages/db/src/schema.ts) at all**, so it is not a `TableKey` and no rule of any class can name it. **NO TABLE IS REGISTERED TO MAKE A ROUTE POSSIBLE and neither reason vocabulary is widened.** Two more findings: **`POST /affiliate/links` has no table** and `affiliate_clicks` is not it, because its rows are CLICKS and `clicks_30d` counts them; and **`campaign` reaches no column anywhere in the schema**. **THE OPERATOR HALF IS NOT BUILT** ([ADR-113](../decisions/ADR-113.md) clause 5) and `ADR-142`'s conditional therefore **DID NOT FIRE**: no ADR is written and the number stays free. Routes hit over a real socket on ports 3112 to 3114, 200, 400, 403 and 503 all pasted in section 7. `apps/api/test/affiliate.test.ts` **36** assertions, suite **150 files / 2,656 passed to 151 / 2,692 passed / 1 skipped**, 32 of 32 gates, 10 of 10 invariants, `falsify` clean, `pnpm run verify` exit 0. **NO MIGRATION NUMBER. NOT MERGED.** |
| [2026-08-27 - Session 255](2026-08-27-session-255.md) | **THE CONDITIONAL DID NOT FIRE AND `143` IS RELEASED.** The reservation was on *"whether the refusal exists"* and it exists in three places: `surface.ts` classifies `/internal` operator by PREFIX, `compose` calls `surfaceServes` on EVERY declared route and registers only what the surface serves, and `RI-09` refuses any other deployable spelling the path. **MEASURED ON BOTH SURFACES RATHER THAN READ**: `api` reports `23 route(s) registered, 4 withheld` and answers **404 `application/problem+json`** on all four rows; `api-admin` reports `5 registered, 22 withheld` and serves them. **FALSIFIED by deleting `compose`'s `surfaceServes` guard**, whereupon `api` reports `27 registered, 0 withheld` and `POST /internal/batch/run` runs its VALIDATOR on the public origin (**400**, not 404); guard restored, tree clean. **THE MECHANISM HAD NEVER HAD A SUBJECT**: `withheld` was empty on both surfaces until this branch, which is exactly the evidence `index.ts`'s startup line says it cannot offer *"until an operator module exists"*. **NO IN-PROCESS FACTOR CHECK, DELIBERATELY**: `authorize` refuses `admin_sso` unconditionally, so routing these through `endpointHandler` would answer 403 to every operator, and a second operator-flavoured `authorize` is the exact mistake `ADR-083` section 4 refuses. `INTERNAL_REQUIRED_FACTORS` is section 12's declaration and is NOT the enforcement point. **THREE CORPUS DISAGREEMENTS REPORTED RATHER THAN RESOLVED**, all `M02` against the frozen contract, one of them a FOURTH `/internal/*` endpoint in no contract at all. **ONE FENCE WIDENING, FORCED AND DECLARED**: two suites borrowed `GET /internal/jobs` as a synthetic and `compose`'s duplicate refusal fired the day the real row landed. 10 of 10 invariants, 32 of 32 gates, `verify` exit 0, `falsify` clean |
| [2026-08-27 - Session 256](2026-08-27-session-256.md) | **THE FIRST ROUTE MODULE IN THIS TREE WRITTEN AGAINST THE ADMIN ORIGIN**, so [ADR-144](../decisions/ADR-144.md) rules how the surface is SELECTED and every admin route after it follows. **The ruling adds nothing, and that is the argument for it**: `OPERATOR_PREFIXES` already held `/admin`, `discoverRouteModules` already composes every file in the directory, and **`start.ts` IS UNTOUCHED**. A second `buildServer`, a `MERIT_API_SURFACE` branch inside a handler and a `routes/admin/` subdirectory are each refused with a reason. **PROVEN BY EXECUTION IN TWO STATUS CODES**: the same file answers **404 on `MERIT_API_SURFACE=public`** (7 withheld, the router has nothing there) and **401 on `operator`** (8 registered). **A READ ROUTE STILL CHECKS A ROLE** and the guard is run against five principals: 401 / 403 / 403 / 200 / 200 / 200. **THE `SystemReason` FINDING RUNS THE OTHER WAY FROM THE ONE EXPECTED**: `'operator-console'` fits and is NOT widened, and what the keyed accessor lacks is a query SHAPE rather than an authority, so the source is a port and `SqlExecutorReason` is untouched. **Money is swept by NAME** and two calibrations were caught by execution rather than by reading. **[ADR-012](../decisions/ADR-012.md) is now MECHANICAL**: the diff sweep returns ZERO. **Three citations in the dispatching brief do not resolve** and all three are named at their sources. The gate and invariant totals are the TREE's and moved under this branch: 33 of 33 gates and 11 of 11 invariants on the current head, 32 and 10 when the session started, and THIS SESSION ADDS NEITHER. `verify` exit 0 |
| [2026-08-27 - Session 257](2026-08-27-session-257.md) | THE FOUR `/admin/*` WRITE PREFIXES, and the ruling that shapes all of them is that `admin_actions.reason` is `NOT NULL` under *"NO UNEXPLAINED ADMIN ACTION, EVER"* (`0017:82`). **The audit row is INSERTED FIRST, in the same transaction, and the mutation is downstream of it**, so an operator write with no reason is refused by POSTGRES rather than by a route guard, and the route neither defaults a reason nor writes a placeholder to get past the constraint. **PROVED AGAINST A REAL POSTGRES 16** with the whole migration set applied, in both directions: the omitted reason raises `23502` on `admin_actions.reason` and moves nothing, and the same call carrying a reason commits and moves the row. **FOUR FINDINGS NAMED AND NOT ROUTED AROUND**: `NOT NULL` admits the empty string and the column wants a `CHECK`, which is session 240's migration to write; `events` has no `SCOPE_RULES` entry so the event half of every admin action is unreachable from this deployable; three of the seven contract bodies omit `reason` while section 8's preamble requires it; and `CreateVersionRequest.sizes` carries three of the eight NOT NULL money columns `plan_version_sizes` declares. **`ADR-145` proposed, UNSIGNED.** Roles enforced per `INV-M6-09`, `readonly` refused all seven and writing nothing, the whole module withheld from the public surface. **TWO GREEN-AND-WRONG DEFECTS WERE FOUND BY RUNNING THINGS RATHER THAN TYPE-CHECKING THEM**: every database refusal answered `500` because drizzle wraps the SQLSTATE in a `cause` nobody was reading, and `Refusal`'s constructor parameter property made the module unimportable under `node --experimental-strip-types`, which would have stopped the WHOLE API surface from starting with `tsc` clean and 27 assertions green. Both fixed, the second watched failing on a seed |
| [2026-08-27 - Session 258](2026-08-27-session-258.md) | THREE ROWS OF API_CONTRACT, AND THE ONE THAT FORCED A RULING WAS THE CALENDAR. **`GET /economic-calendar`** is the first endpoint in this corpus whose response carries an `*_at` and a `*_day` ON THE SAME OBJECT, and section 1's suffix rule (`*_at` is a UTC instant, `*_day` is an exchange trading day, never a UTC date) has been carried entirely by whoever names a field. The two agree on most rows and disagree on evening ones, so a derived day would be right all afternoon and wrong at 23:30. **`ADR-146` proposed, seven clauses**, and the suffix rule becomes a refusal at the point of projection. The calendar clause is asserted on `2026-03-03T23:30:00Z`, whose UTC date is `2026-03-03` and whose trading day is `2026-03-04`, with the divergence asserted BEFORE the rendering. **`POST /webhooks/rise` forced a NEW PORT and it is said loudly**: `PspId` is closed by a CHECK on `purchases.psp`, so `@merit/psp`'s verifier cannot serve the payout rail, and this is the THIRD implementation of one ordering rule. `ADR-114` section 4's condition for lifting it has RECEDED rather than arrived. **`GET /public/methods/:statCode` was found COMPLETE, not partial**, and the finding is reported rather than a file being edited to look busy. **Three dispatch citations corrected. 32 of 32 gates, 10 of 10 invariants, `verify` exit 0** |
| [2026-08-27 - Session 259](2026-08-27-session-259.md) | THE ACCOUNTS SEGMENT IS A REAL PAGE. `/accounts` and `/accounts/:account` under [`apps/portal/src/app/accounts`](../../apps/portal/src/app/accounts), rendering [`view/accounts.ts`](../../apps/portal/src/view/accounts.ts) and [`view/marks.ts`](../../apps/portal/src/view/marks.ts) with no rewrite of either. **`pnpm --filter @merit/portal build` EXITS 0** and prints both routes; served, `/accounts` answers 500 with this segment's own `AccountsSourceNotWiredError` and `/nope` answers 404, so the route set is real and the only missing piece is the transport. **NO ROOT LAYOUT, NO `next.config`, NO `tsconfig` CHANGE and NO `.tsx`**: `next@16.3.2` synthesises a root layout and its default `pageExtensions` carries `ts`, both read out of the installed package, so the segment costs session 250's shared files nothing. **THE EQUITY CHART IS HAND-BUILT SVG** because `view/marks.ts`'s three refusals (no gap filling, no smoothing, no downsampling) are refusals only a renderer can keep and every one is one prop away in every library. **21 new assertions on the MARKUP**, one of which caught a real omission: the detail screen dropped phase, status and blocks, so a breached account's own screen never said "breached". **[ADR-147](../decisions/ADR-147.md) PROPOSED, the reserved condition fired**: `M04` section 3.3's meter names five figures and three reach no client, two of them being `R-29`'s own operands. **`CI-06/gate-inventory` IS RED AT 31 OF 32 AND `falsify` REPORTS 2 PROBLEMS, BOTH THE SAME EVENT AND BOTH PREDICTED**: `ADR-095` section 6 wrote this commit down in advance and `falsify.mjs` seeded a case for it. **NOT fixable in this fence and NOT weakened.** 10 of 10 invariants, 151 files / 2,677 passed / 1 skipped |
| [2026-08-27 - Session 260](2026-08-27-session-260.md) | **`apps/portal` RENDERS A DOCUMENT.** `src/app/payouts/` is `SC-M4-04`: the verdict, the request control's exact clamped amount, the cap that bounded it, the always-visible consistency meter, the ten gates in the contract's order and the payout history with its holds. **`pnpm --filter @merit/portal build` EXITS 0 and prerenders `/payouts` STATIC, without session 250**, because Next 16 supplies the missing root layout; the segment is unwired for DATA and not for the framework, and the two are separated rather than fused. **THE SEGMENT IS `.ts` AND NOT `.tsx`, AND THAT IS A CONTROL DECISION**: four checks in this repository are scoped to files ending `.ts` and none sees a `.tsx`, so a JSX payout screen would drop all four on the one screen where money is rendered by choosing an extension. **[ADR-148](../decisions/ADR-148.md) PROPOSED**: `G-ELIGIBLE` conjoins TWELVE terms and the eligibility response declares TEN gates, so `identities.status = 'active'` and `G-NO-IN-FLIGHT` reach the trader only on submit. The entry rules the DISPLAY half and refuses the CONTRACT half. **BOTH OF THE DISPATCH'S CITATIONS DID NOT SURVIVE READING** and both are reported. **`CI-07` REOPENED ON GOOD NEWS exactly as its own cell predicts, AND THEN CLOSED**: its stage landed on `main` at `ca4f9fec` and this branch reports **33 of 33 gates and 11 of 11 invariants**, with both routes prerendering static beside session 250's. The one remaining red is `CI-05`, on other branches' findings. 168 portal assertions, `next build` exit 0, typecheck, lint, format and 10 of 10 invariants clean |
| [2026-08-27 - Session 261](2026-08-27-session-261.md) | THE PORTAL'S FIRST TWO RENDERED SCREENS FOR `SC-M4-06` AND `SC-M4-08`, shipped unwired and now **WIRED AND BUILDING** on the merge with session 250, in one route group `app/(purchases)/` whose URLs `next build` names in its own route table: `/purchases` and `/certificates`. **The dispatch's premise is wrong and the correction is this session's first finding: `GS-302` is `restricted` in `identity_status`, which is an IDENTITY STATUS and not a data classification.** `0001:27` is `CREATE TYPE identity_status AS ENUM ('active', 'restricted', 'closed')` and `ADR-041` refused a fourth value of THAT enum. No data-classification vocabulary exists anywhere in this corpus, so a rendered certificate tests nothing about one. **What a public certificate may carry IS settled, in two places that agree**: `INV-M11-01` enumerates it and `AS-M4-03` rule 3 states it as absences. **So `ADR-149`'s condition is FALSE and the number is NOT TAKEN.** The control that ships instead is an ALLOWLIST at the boundary: `publishedCertificate()` builds field by field from a closed list and `certificate_id` does not cross it, because `SD-M11-01` separated the primary key from the public `code` so the token could be rotated after an incident. **Eighteen assertions, and the ones that matter are absences over the RENDERED BYTES.** The refusal path was leaking the `certificate_id` through an error message and the test caught it. **Four unowned reads named**: `GET /accounts/:accountId/certificate` is approved and claimed by no session in this wave, `content_documents` has no contract row at all, `GET /certificates` is M11's and unwritten, and the purchase cursor has no field. **`CI-07` REOPENED ON THIS COMMIT AND WAS LEFT RED RATHER THAN RE-RULED; SESSION 250 THEN CLOSED IT BY WRITING THE STAGE**, and section 8.1 records that this log forecast that wrongly TWICE, in opposite directions, which is this entry's own headline error class pointed at its author. **11 of 11 invariants** including 250's new `RI-11`, **33 of 33 gates**, `falsify` clean, **`pnpm --filter @merit/portal build` exit 0**, 2,677 tests green |
| [2026-08-27 - Session 262](2026-08-27-session-262.md) | THE FIRST `apps/portal` BUILD THAT PRODUCES A ROUTE. `apps/portal/src/app/kyc/` renders `SC-M4-07` from `view/kyc.ts` and `view/disclosure.ts` and rewrites neither, and `pnpm --filter @merit/portal build` exits **0** with `/kyc` listed **dynamic**. **[ADR-114](../decisions/ADR-114.md) clause 6 is enforced in BOTH of its places and the clause is asserted by TRYING**: a payload carrying a passport, a nested selfie URL, `INV-M19-09`'s provider reason code and a birth date is handed to the render path, and the emitted bytes are searched for the VALUES with every needle proven findable first. It refuses, names KEY PATHS, and quotes no value. **M04 section 7.9's string catalogue now EXISTS and its lint runs**, which [`view/kyc.ts`](../../apps/portal/src/view/kyc.ts) reported as owed and unbuildable because a view model authors no sentences. **THREE DISPATCH CLAIMS CHECKED AND ONE IS WRONG**: the build does not need session 250. **`CI-07` reopened on the artifact this page IS**, reported with a written-out patch rather than weakened, and **session 250 then landed that patch and every other item this session reported as owed**, so the segment is now WIRED to the real root layout and `pnpm run verify` exits **0**: 11 of 11 invariants, 33 of 33 gates, falsify clean. `ADR-150`'s condition did NOT fire and the number stays free |
| [2026-08-27 - Session 263](2026-08-27-session-263.md) | **SC-M4-09 IS A DOCUMENT A BROWSER RENDERS.** `apps/portal/src/app/referrals/` is the portal's first App Router segment and `view/referrals.ts` finally renders. **THE COMPLIANCE CLAUSE IS ASSERTED ON THE EXACT CASE IT NAMES**: a `pending` creative whose row carries no `disclosure_version_id` renders the disclosure under "Disclosure required at approval", with the absence stated BEFORE the text, and `CreativeSubmissionView` has no field an attachment could be written into. **`next build` EXITS 0 AND `/referrals` IS A LIVE ROUTE**, which is not what `ADR-095` section 6's dated probe predicted: the premise *"next build exits 1 on Couldn't find any pages or app directory"* is discharged and the segment builds without session 250's layout. **`CI-06/gate-inventory` WENT RED AT 31 OF 32 BY DESIGN** on CI-07's arrival and was left failing rather than weakened; **session 250 then wrote the stage and it is CLOSED, `verify` exit 0 at 33 of 33** (section 9). **FOUR FINDINGS REPORTED RATHER THAN RULED**: `tos_versions`' closed CHECK has no member for the NFA promotional disclosure, `view/disclosure.ts` cannot be loaded by `node --experimental-strip-types`, no contract row reads a creative back, and the segment is `.ts` rather than `.tsx` because `ADR-095` F7's tsconfig edit belongs to session 250. **`ADR-151`'s CONDITIONAL DID NOT FIRE and `151` is unspent.** **`CI-05` IS RED FOR THE WHOLE REPOSITORY AND NOT FOR THIS BRANCH**: `gitleaks` findings on OTHER sessions' unmerged branches, reproduced against the pinned binary and separated from this branch by four scans, and **the count rose from three to four while this branch was open**, the fourth on a second branch, which makes it a pattern rather than one session's slip. 10 of 10 invariants, typecheck, lint and format clean, 151 files / 2,670 passed |
| [2026-08-27 - Session 264](2026-08-27-session-264.md) | **THE `app/calendar` SEGMENT RENDERS ALL FOUR VIEW MODULES WITH REAL DATA, AND IT SHIPPED UNWIRED AND THEN WIRED, BECAUSE THE BLOCKER CLEARED WHILE THE PULL REQUEST WAS OPEN.** **SESSION 250 LANDED ON `main` AFTER THIS BRANCH WAS GREEN**, taking `next.config`, the root layout, the root page AND the `apps/portal/tsconfig.json` edit this session had to make first, and writing `CI-07`'s stage, so the arrival condition that forced `unwired` is discharged and `pnpm --filter @merit/portal build` now emits `/calendar`, `/calendar/[accountId]/timeline` and `/calendar/[accountId]/rules` at exit 0. **THE ORIGINAL REFUSAL WAS STILL CORRECT AND IS KEPT BELOW RATHER THAN REWRITTEN**, because it was true of the tree it was measured on: Seeding `apps/portal/src/app/calendar/page.tsx` turns `CI-06/gate-inventory` red at **31 of 32** naming `CI-07`'s dated ARRIVAL condition, whose repair is a job in [`ci.yml`](../../.github/workflows/ci.yml) and a row in [STRATEGY](../testing/STRATEGY.md) section 4.1, both outside this fence, and which is [session 225](2026-08-26-session-225.md)'s measurement one app over reproduced exactly. Seed removed, **32 of 32**. **A SECOND MEASUREMENT SAYS THE ACTIVATION ARTIFACT AND THE BUILDABILITY CONDITION HAVE COME APART**: `pnpm --filter @merit/portal build` exits **0** with the segment's directory present and NO router file in it, and exits **1** with the identical *"Couldn't find any `pages` or `app` directory"* STRATEGY quotes when the directory is moved away, so `CI-07`'s stage is writable for `apps/portal` while its condition still reads absent. **[ADR-152](../decisions/ADR-152.md)'s CONDITIONAL FIRED ON THE AS-OF HALF AND NOT ON THE TRADING-DAY HALF**: `INV-M4-02` puts the day on the screen and does not put the STALENESS on it, a required `as_of_trading_day` four sessions behind rendering identically to one computed an hour ago, and **[API_CONTRACT](../architecture/API_CONTRACT.md)'s own comment asserting the field IS the last closed day is what makes the confident render feel safe** while being true at composition and silent at read. Freshness is ruled a SERVER claim the portal may only ORDER, as the engine's own lexicographic day comparison; the prop is required, `unstated` is an arm, and a figure dated after the closed-through day is a REFUSAL. **No account-state contract publishes the firm's closed-through day**, four `grep` sites in [`api/types.ts`](../../apps/portal/src/api/types.ts) confirming it, so every account-state screen renders `unstated` today and the gap is NAMED rather than closed by inventing a field. **BOTH CLAUSES WERE WATCHED FAILING ON A SEEDED VIOLATION.** The calendar clause is asserted on `2026-03-12T22:30:00Z`, which is 17:30 CT and therefore trading day **2026-03-13** while its UTC date and its New York date both read **2026-03-12**; grouping by `local_day` instead fails **2 of 26**. The as-of clause renders `2026-03-09` against a firm closed through `2026-03-13`; returning `current` on a behind day fails **1 of 26**. **THE APP'S OWN SURFACE TEST FIRED AND ITS FINDING WAS ACCEPTED RATHER THAN AMENDED**: a first `load.ts` took an injected `fetch` capability, [`surface.test.ts`](../../apps/portal/test/surface.test.ts) went red naming the path, and the transport came back out because that file's stated purpose is that the first `fetch` be a deliberate decision and the framework's entry points are session 250's. **`apps/portal/tsconfig.json` WAS EDITED AND IT IS ARGUABLY 250's**, said in the file rather than left to the blame: `jsx`, the `DOM` lib and the `.tsx` include patterns are what any rendered file needs and this session landed the first one. **THREE DISPATCH CITATIONS ARE WRONG OR LOOSE AND ALL THREE ARE REPORTED**: `ADR-122` rules `published_statistics.input_digest` on M12's PUBLIC surface and is an analogy here rather than an application; `calendar.ts` holds queries over a caller-supplied slice and **exports no instant-to-trading-day function at all**, so no page could have called one; and *"18 view modules and 3,484 lines"* is exact for `apps/portal/src` entire and there are **12** files under `src/view`. Suite **150 files / 2,677 passed** to **151 files / 2,682 passed / 1 skipped**, `repo-invariants` **10 of 10**, gates **32 of 32**, `falsify.mjs` clean with all 32 watched failing dirty, `verify` exit 0. No migration, no `packages/db` file, no route under `apps/api`, no dependency, no `CI-06` letter and no `VG` row |
| [2026-08-27 - Session 265](2026-08-27-session-265.md) | **TIER 2's ARITHMETIC IS BUILT AND ITS PLUMBING IS IN NO ARCHITECTURE DOCUMENT.** [P6-live-tier](../plans/P6-live-tier.md), `status: draft`, eleven slices over four waves with fences BY FILE, collisions BY FILE, and the closed vocabularies each slice may NOT take named on its row. **The answer is a THIRD shape rather than a repeat of either neighbour**: P4's contents were mostly built and unsaid, P5's were absent behind one file, and **P6's split by KIND**, with every ARITHMETIC or TYPE piece built (`live-liability.ts` computes M06 section 3.5's figure with three tiered terms and `authoritative()` refusing it; `stream.ts` emits `LiveAccountTick` and folds byte-identically into the EOD CSV; `view/as-of.ts` declares `Tier` and `Tiered`) and every CONTAINER, TRANSPORT or STORE piece absent. **`grep -rli websocket docs/architecture/` RETURNS NOTHING**: ADR-020's third stated content is in no architecture document at all. **The live cache is named ONLY as the object of a grant, in six places, and never with a medium**, while `0026` gives the one application role every future table by DEFAULT, so the separation gets further away when a table is added. **`ADR-154`'s CONDITIONAL FIRED**: ADR-020 rules the real feed plugs in *"exactly as the batch pipeline already does"* and that mechanism is a FILE, `streamLive` hands its consumer objects, and each remaining reading is refused by a landed document, so the seam moves from the ARTIFACT to the TYPE. **TWO OF FOUR DISPATCH CLAIMS DID NOT SURVIVE**: no surface registers 54 routes (36 public, 19 operator, 54 the union, `GET /health` on both), and **`GS-132` as written asserts what `RI-01` and `RI-07` already guarantee**, so the assertion that can fail is over the batch's INPUT ASSEMBLY. **P6's two gates are NOT waiting on a fixture ruling**, the inverse of P5's position, because ADR-125 clause 2 already ruled the surviving blocker is the absent code. **`DELIVERY_PLAN` NOT TOUCHED**, measured: it links zero phase plans. `verify` exit 0, 33 of 33 gates, 11 of 11 invariants, 169 files / 3,091 passed / 6 skipped, falsify clean |
| [2026-08-27 - Session 266](2026-08-27-session-266.md) | **`P7` IS PLANNED AGAINST THE TREE AND ITS THREE DONE-GATES ARE EACH A REFUSAL**: a canary gate that fires when a detector finds NOTHING, a redaction gate that passes when a document OMITS something, and a breaker gate that passes when the breaker DECLINES to fire. **TWO OF P7'S SIX STATED CONTENTS ALREADY EXIST AS ROUTES THAT ANSWER NOTHING.** `GET /admin/flags`, `POST /admin/flags/:flagId/status` and `GET /admin/evidence/:accountId` are registered on the operator surface and `AdminReadSource` is a port nothing wires, on [`admin-reads.ts:621`](../../apps/api/src/routes/admin-reads.ts)' own words *"WHAT IS MISSING IS NOT AN AUTHORITY, IT IS A SHAPE"*. All five `SD-M7-nn` deltas are landed and every M07 table is registered in both `schema.ts` and `scope.ts`; **no file under `apps/worker/src` names a detector and `detector_definitions` has no seed**, so a run today could not record the parameters it ran under, which is `INV-M7-04`. **P7 HAS NO HEAD OF ITS OWN AND THAT IS THE FINDING**: the read shape is [`scoped-db.ts`](../../packages/db/src/scoped-db.ts), which is [`P5-a`](../plans/P5-payouts-and-wallet.md)'s file, and a detector is a JOIN and an AGGREGATE where `P5-a` is scoped to a range term, a null term and a row lock, so **the argument is written where `P5-a`'s author will read it rather than into a second slice on the one door**. **THE ONE PRECONDITION P7 DOES OWN IS `RI-08`**: `DB_ADMITTED` holds one name, `@merit/api`, and [CRON_INVENTORY:27](../ops/runbooks/CRON_INVENTORY.md) rows detector runs as a scheduled job, so the deployable that runs every detector cannot declare the accessor at all. **THREE DISPATCH CLAIMS DID NOT SURVIVE AND ALL THREE ARE REPORTED.** *"ADR-145, four writes"* is **seven**, and that entry's own first sentence reads *"in all seven admin writes"*. **The reserved condition is REFUTED**: [M06:300](../plans/M06-admin-ops-console.md), the batch 1 gate closure and [EC-071](../edge-cases/EC-071.md) each give all four audiences a redaction profile and collapse `internal`, `counsel` and `regulator` into one, so `GS-112` is writable as stated; `OQ-F4-03` is still open and asks a **different** question about the same column, whether an impersonation record appears in a regulator pack, which is counsel's. And [`0049:48`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql)'s disposition of `per_plan` **checked four of five fields**, leaving the CUSUM with no storage on a table whose primary key is `(plan_id, evaluated_on)` with `metric` outside it and no column a running `S_t` fits. **[ADR-155](../decisions/ADR-155.md) FIRED ON THE OTHER HALF OF THE SAME `P7` ROW**: `ADR-022`'s v1 tier says a hard link **auto-enforces**, `ADR-029` says it *"bans an account without human review"*, and [STATE_MACHINES:301](../architecture/STATE_MACHINES.md) says *"Binding: no automatic transition into `enforced`"* while being `approved`, dated six days later, and naming twelve dependencies of which `ADR-022` is not one. **RULED: the v1 tier names a LINK CLASS and "auto-enforce" is the EDGE written at the hard-link ceiling without review**, never a transition on the `risk_flags` machine. **`OQ-M7-05` is NOT answered and is the founder's**, and the counter-argument is recorded at full strength because the ruling has a real cost. **TWO FINDINGS REPORTED RATHER THAN REPAIRED**: `INV-M7-02`'s *"has no grant"* names a permission that does not exist, `0026` creating three roles of which one is usable and holding full DML on `risk_flags` with no revoke; and `GS-235`'s subject line *"v1 tier: a hard link auto-enforces"* would settle `OQ-M7-05` **by being written**. **`DELIVERY_PLAN` IS NOT EDITED AND THE FENCE'S OWN CONDITION IS WHY**: `grep` for `plans/P` over that document returns zero, so no phase row points at its plan and `P4` and `P5` both landed without one. Thirteen slices, four waves, fences BY FILE, collisions BY FILE against sessions 265 and 267. 33 of 33 gates, 11 of 11 invariants, `falsify.mjs` clean, `verify` exit 0, 169 files / 3,091 passed / 6 skipped and unmoved, because this session writes no code |
| [2026-08-27 - Session 267](2026-08-27-session-267.md) | **BOTH DISPATCHED ROWS WERE STALE AND THE REPAIR FOR ONE OF THEM WAS THE REGISTER.** `OI-28` was CLOSED by [ADR-082](../decisions/ADR-082.md) on 2026-08-23 and the row calling it open names, in its own second cell, the session that closed it; ADR-082's consequences and the `simulation_runs` design record had each PREDICTED this stale row and named the session type that would fix it, and the next dispatch read the row anyway. **NO TOKEN IS WIDENED AND `ADR-042` IS NOT AMENDED**: the vocabulary widened its DEFINITION and never its token list, `UNIT_TOKENS` still holds three members, and `calibration_observed_at` was correctly declared as `rail clock` on the day it landed. **`OI-29` DOES NOT CLOSE AND THIS SESSION REPORTS THAT RATHER THAN A CLOSURE.** It was already PARTLY CLOSED by [`0047`](../../packages/db/migrations/0047_publish_decision_is_sound.sql) under [ADR-087](../decisions/ADR-087.md), and the dispatch's *"its digest half is `ADR-081`'s"* was refuted at [`hash.ts`](../../packages/rules-engine/src/hash.ts) by session 148 and recorded at four sites, none of them the allocator. **THE RULING IS WHAT `0047`'s TWO CHECKS ARE WORTH AFTER THE PUBLISH ROW IS WRITTEN, AND IT IS NOTHING.** Measured on the full migration set under `SET LOCAL ROLE merit_app`, with the existing probe run green first so the control under test was the installed one: a publish written with both checks satisfied survives its run being moved to `failed`, survives it going back to `queued` with `completed_at` nulled, which removes the completion time `0047`'s own header reasons from, and survives it being re-anchored to another plan's version. **THE ASYMMETRY IS THE MECHANISM**: [`0028`](../../packages/db/migrations/0028_supersede_plan_version_immutability.sql) refuses every write to the citing row and `0045` leaves the cited row free by design, so the only unpinned half of the link is the half both checks read; the `DELETE` is refused by the foreign key, so the citation is permanent and only the content of the thing cited is free. **THE NAIVE GUARD IS REFUTED BY THE CORPUS'S OWN PROBE**, whose `SUCCESS 1` performs the legitimate `NULL` to citing-row anchor move one statement after the publish it has just proved sound, so the constraint is narrow and NO MIGRATION NUMBER IS CLAIMED. **A PINNED GREP TRANSCRIPT HAS GONE STALE WITHOUT ITS CONCLUSION MOVING**: three documents promise no output for `rules_digest` and the tree returns [`schema.ts`](../../packages/db/src/schema.ts)'s transcription of the column, which is not a producer, so the cheapest wrong inference available to the next reader is that the producer arrived. **REPORTED AND NOT REPAIRED**: `OI-29b` is allocated in the site [ADR-074](../decisions/ADR-074.md) superseded and has never appeared in the allocator; the `simulation_runs` design record does not say its two load-bearing columns are read once; and [STATE](../STATE.md) carries **1,169 copies of one `gen:adr_count` paragraph**, which `CI-06g` rewrote all of and passed. Gates **33 of 33**, invariants **11 of 11**, `falsify.mjs` clean, `verify` exit 0, suite unchanged at **169 files / 3,091 passed / 6 skipped**. No migration, no `packages/db` code file, no engine change, no gate, no `CI-06` letter, no `SD-nn`, no `RI-nn`, no dependency and no phase plan |
| [2026-08-27 - Session 268](2026-08-27-session-268.md) | **[ADR-157](../decisions/ADR-157.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH and the `E2` read owed on every line.** [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) gains a closed three-shape TERM vocabulary (`atMost`, `atLeast`, `isNull`) admitted on the READ path ONLY, and a ROW lock (`lockAt` on the scoped and unscoped transaction handles, `lockScope` on the scoped one). **A term is recognised by OBJECT IDENTITY through a module-private `WeakSet` and never by shape**, because a `jsonb` column can hold an object that looks like one and a shape check would silently turn an equality into a range. **THE AGGREGATE [P7 section 10 item 1](../plans/P7-risk-and-abuse.md) ASKED THIS SLICE FOR IS REFUSED ON EVIDENCE**: P7's own section 3.1 names `D-01` a self-join, `D-02` a rolling correlation, `D-03` a variance of sums across a discovered group and `D-14` a sum across a clique, and **not one of the four is served by a scalar aggregate over one table**, so admitting one would spend the widening and leave P7 blocked on the JOIN it is actually blocked on. A second argument is measured rather than asserted: `SUM` over a `bigint` column returns `numeric`, `pg` hands it back as a string, and `Number()` on it is lossy above 2^53. **THE LOCK IS A ROW LOCK AND THE TWO INVARIANTS SAY ADVISORY**, because `pg_advisory_xact_lock` has no way to be sent that is not `sqlExecutor`, which is the reach-around P5 and P7 rule 10 each foreclose by name. **Nine mutations seeded and all nine caught**, two of them against the SUITE'S OWN READERS; **the one worth the most was GREEN on `main`**, because nothing asserted the tenancy conjunct on the READ half of the address composition. **Eight compile refusals watched, and the corpus now owes nineteen.** **A twenty-one case round trip EXECUTED against PostgreSQL 16 on all 49 migrations**: a caller scoped to `A` locking `B`'s row got zero rows, a third connection then locked `B`'s row without blocking, and two transactions on one row serialised, which is the first time this corpus has watched one of two concurrent transactions lose. Three stale sites repaired ([`chart.ts`](../../packages/ledger/src/chart.ts), [`halts.ts`](../../packages/ledger/src/halts.ts) and [`packages/db/src/index.ts`](../../packages/db/src/index.ts)), each naming a limit that has been lifted. **`SqlExecutorReason` stays at one member and `SystemReason` at two**, and the suite now watches the first, which nothing did before. **NO MIGRATION AND NO MIGRATION NUMBER**, and the dispatch's citation for that is wrong in a way that does not change the answer. Gates **33 of 33**, invariants **11 of 11**, `falsify.mjs` clean, `verify` exit 0, suite **169 files / 3,108 passed / 6 skipped**. No schema, no gate, no `CI-06` letter, no `SD-nn`, no `RI-nn`, no `OI-nn`, no fixture, no edge case, no dependency and no phase plan |
| [2026-08-27 - Session 269](2026-08-27-session-269.md) | **[ADR-158](../decisions/ADR-158.md) proposed, `status: proposed`, approval line UNSIGNED, and [API_CONTRACT](../architecture/API_CONTRACT.md) acquires the wallet and payout-hold rows [P5](../plans/P5-payouts-and-wallet.md) section 3.3 measured as absent.** **THE `158` CONDITIONAL FIRED SEVEN TIMES AND THE LARGEST DISAGREEMENT IS NOT ON A WALLET ROW**: `payout_requests_hold_is_complete` is a BICONDITIONAL, so a release or an enforcement NULLs all five hold columns and the row **stops being able to say it was ever held**, against [M05](../plans/M05-payout-system.md)'s *"posts the stored decision unchanged"*, which reads as though the record survives. Both operator responses therefore carry the hold read BEFORE the write, and the contract names [`admin_actions`](../architecture/data-model/admin_actions.md)`.before` as the durable record. **`wallet_entries.provenance` is `NOT NULL` on every row and its three members are the CREDIT list by the DDL's own heading**, so every wallet debit in this schema is stored carrying a class that does not describe it; the wire type is a union discriminated on `direction` and a debit carries none, its meaning being `cause` and `reference_id`. **`wallet_entries.id` is a `bigint` identity, the first non-uuid identifier this contract has ever carried**, and reaches the wire as a decimal STRING on [ADR-122](../decisions/ADR-122.md)'s `Number.MAX_SAFE_INTEGER` argument. **P-3's release time is not computable by anything landed**: no column carries a purchase's chargeback-window end, `OQ-M20-02` is open and `DEP-M20-03` is unbuilt, while `affiliate_commissions.chargeback_window_ends_on` is `date NOT NULL` for the identical rule one rail over, so `available_at` states absence rather than a number this repository invented ([ADR-139](../decisions/ADR-139.md) clause 3). **P-1 is a flag on the WITHDRAWAL and never a hold on the balance**, which the dispatch plan's own row blurs. **`composition` is nullable at creation** because `wallet_withdrawals_approved_has_provenance` permits `'{}'` at `requested` and `cooling`. **Section 1's idempotency list gains the external leg**, which `wallet_withdrawals.idempotency_key NOT NULL` under a unique index had already required. **Dual control on the wallet correction is DECLARED and the widening is stated**: `C-10` and [ADR-010](../decisions/ADR-010.md) close the set at four naming one endpoint, and the fail-closed direction was taken with the register repair named as OWED. **NO TOP-LEVEL SECTION WAS CREATED**: the trader rows are `### 6.2` on section 6.1's own recorded reason, because sections 12 and 13 are cited by number ([ADR-111](../decisions/ADR-111.md) clause 2). **The route set was asked rather than grepped**: `discoverRouteModules()` plus `buildServer` reports 36 on `public` and 19 on `operator`, `GET /health` on both, and none of the rows added here is registered. **Seven findings reported and not repaired**, among them that `payout_requests.approved_at` is `NOT NULL DEFAULT now()` while section 6 types it `\| null` *"while held"*, and that `G-NO-IN-FLIGHT` is a UNIQUE index on the leg that moves no cash and a plain scan index on the leg that does. **THIS BRANCH TURNS ONE TEST RED AND IT IS LEFT RED RATHER THAN WEAKENED**: [`admin-reads.test.ts`](../../apps/api/test/admin-reads.test.ts) parses `### GET /admin` headings out of the contract and pins the declared-but-unserved set as a literal, so a third admin read fails it; executed both ways with `API_CONTRACT` as the only variable, and **the file is in no `P5` fence**, which generalises to every session adding an admin GET row and is a fence defect rather than a choice. The patch is written out in the entry, on [session 262](2026-08-27-session-262.md)'s precedent. `typecheck` exit 0, gates **33 of 33**, invariants **11 of 11**, `falsify.mjs` clean, suite **169 files / 3,090 passed / 6 skipped / 1 failed**. **NO CODE, NO EVENT NAME** (`P5-d` holds the catalogue), **NO MIGRATION NUMBER**: `0050` is next free and this entry does not take it. **MONEY PATH BY CONTENT, `E2` OWED ON THE ROUTES, NOT MERGED** |
| [2026-08-27 - Session 270](2026-08-27-session-270.md) | **THE RESERVED CONDITION CANNOT FIRE, BECAUSE THE ROW AND THE ENTRY IT CITES NAME TWO DIFFERENT CATALOGUES.** [ALLOCATION](../decisions/ALLOCATION.md) reserved `159` against `P5-d` meeting the half [ADR-141](../decisions/ADR-141.md) left, and that entry rules a trader-scoped read of the PLAN catalogue in [`catalog.ts`](../../apps/api/src/routes/catalog.ts) against [ADR-106](../decisions/ADR-106.md)'s scope classes. **`P5-d`'s catalogue is the EVENT catalogue** and its fence contains no code, no route, no read and no scope class, so the BOLA leg of the dispatch is unreachable rather than passed. **[P5 section 8](../plans/P5-payouts-and-wallet.md) marks the same ADR REQUIRED while the allocation row marks it CONDITIONAL**, and the plan is the one that describes the work. **[ADR-159](../decisions/ADR-159.md) RULES WHERE A CATALOGUE ROW'S PAYLOAD MAY COME FROM**: a column a migration declares, or a field of a mirror a plan names, and nothing else, which is exactly the question [EVENTS](../architecture/EVENTS.md) section 6 deferred in writing when it refused to invent three shapes for a machine it had not drawn. **THREE OF THE SIX LIFECYCLE NAMES HAD A NAME AND NO SHAPE ANYWHERE IN THE CORPUS** and their payloads are derived field by field under that rule. **`wallet.credited`'s PAYLOAD AS [M05](../plans/M05-payout-system.md) WROTE IT CANNOT BE PRODUCED FOR TWO OF THE THREE STORABLE CREDITS**: [`0011`](../../packages/db/migrations/0011_wallet.sql) declares `wallet_entries.provenance` `NOT NULL` over a closed list of three, only one of which has a payout request or a basis trading day, and the table carries no `account_id` at all. Five payloads are amended at the fold and **every one is named in the entry's own table**, because a registry may amend and may not amend silently. **`OQ-M5-07` IS ANSWERED**: `payout.freeze_expiring`'s lead is set at twelve wall-clock hours because that question says the value is set where the event is written, and `lead_hours` is in the payload because the corpus calls it a launch parameter rather than a constant. **The name disagreement [P5 section 5.2](../plans/P5-payouts-and-wallet.md) predicted needed no ruling of its own**, the drawing's own text having already made the registry the authority; `wallet.withdrawal_halt_cleared` becomes `wallet.withdrawal_halt_released` in one cell and the guard beside it keeps its spelling. **FINDINGS REPORTED AND NOT REPAIRED.** [STATE_MACHINES section 3.2](../architecture/STATE_MACHINES.md)'s diagram draws two edges into `cancelled` under `G-TRADER-CANCELS`, `cancelled` is a live enum value in [`0001`](../../packages/db/migrations/0001_extensions_and_enums.sql), and the transition TABLE rows neither, so universal rule 1 is unmet and there is no event to fold. **NOTHING IN THE TREE CHECKS THAT AN EVENT NAME IN A DRAWING RESOLVES TO A ROW IN THE REGISTRY**, which is why the disagreement survived two `approved` documents and every gate and was caught by a plan reading both. `treasury.coverage_changed` carries `rcr_bp` and no float figure, so a consumer can neither recompute the ratio nor check which side the float landed on. **[P5 section 9](../plans/P5-payouts-and-wallet.md) ROWS [EVENTS](../architecture/EVENTS.md) FOR ONE SLICE AND [P6](../plans/P6-live-tier.md) GIVES THE SAME FILE TO `P6-d`, SESSION 272, CONCURRENT WITH THIS ONE.** And [P5 section 7](../plans/P5-payouts-and-wallet.md)'s *"`0048` stays free"* stands against two landed migrations. **THE DISPATCH'S ROUTE FIGURE IS THE SENTENCE [SESSION 265](2026-08-27-session-265.md) ALREADY REFUTED**: asked of the app rather than of a grep, `CompositionReport.registered` reports 36 on `public` and 19 on `operator`, whose union is 54 because `GET /health` is on both, and no surface registers 54. **NO MIGRATION NUMBER, no `CI-06` letter, no `GS-nnn`, no `OI-nn`, no widening of `SystemReason` or `SqlExecutorReason`, and no code of any kind.** `pnpm run verify` exit 0, 33 of 33 gates, 11 of 11 invariants, `falsify.mjs` clean, 169 files / 3,091 passed / 6 skipped and unmoved. |
| [2026-08-27 - Session 271](2026-08-27-session-271.md) | **[ADR-154](../decisions/ADR-154.md) IS IMPLEMENTED AND `ADR-160` IS NOT TAKEN.** `streamLive` joins [`PlatformAdapter`](../../packages/rithmic/src/index.ts) carrying [M02](../plans/M02-rithmic-bridge.md) section 3.5's own signature, `simulatorLiveFeed` implements it, and clause 3's *"one method and no other"* is enforced twice: `Pick<PlatformAdapter, 'streamLive'>` in `src`, and the returned object's KEY SET at runtime, **because a `Pick` bounds what a consumer may call rather than what the object carries** and an implementation that grew an `ingestEOD` beside `streamLive` would satisfy the annotation and every type-check in this repository. **`GS-084`'s property is not claimed for tier 2 and [`stream-conformance.test.ts`](../../packages/rithmic/test/stream-conformance.test.ts) is what replaces it**: the stated invariants run over each implementation, checked at RUNTIME over delivered ticks rather than declared at the interface, on the ground that a vendor implementation parses ticks off a wire where every field arrives `unknown` and one cast restores each guarantee by assertion. **THE RULING SETTLED THE SEAM AND NOT THE START, AND THE START IS WHERE THE DEFECT WAS.** A microtask pump queued inside `streamLive` runs BEFORE the caller's `await` continuation, so the first tick is delivered while the consumer is still waiting for the `Subscription` that would let it close and `close()` from inside the handler is a reference to an unassigned variable; delivery is one `setImmediate` per tick for that reason and for no other, and the close test is the assertion that catches it. **Three seeded defects watched failing**, including the cheap implementation ADR-154's suite exists to refuse: a feed that delivers its whole day inside `streamLive` passes every invariant and fails both close tests. **A FOURTH ENUMERATION SITE EXISTS AND CLAUSE 5 NAMES THREE**: [`packages/rithmic/README.md`](../../packages/rithmic/README.md) carries the five operations AND *"the interface is declared and nothing implements it"* AND *"`PlatformAdapter` is still unimplemented"*, all three false on this commit, all three outside `P6-a`'s fence, reported with the patch written out rather than reached for. **`M02` was read and not edited.** No migration, no `SD-nn`, no `SystemReason`, no `SqlExecutorReason`, no dependency, no gate, no `CI-06` letter. Gates **33 of 33**, invariants **11 of 11**, `falsify` clean, `verify` exit 0, suite **170 files / 3,098 passed / 6 skipped** against the dispatch's `main` baseline of 169 / 3,091 / 6, which reproduced exactly |
| [2026-08-27 - Session 272](2026-08-27-session-272.md) | **[ADR-161](../decisions/ADR-161.md), `status: proposed`, UNSIGNED. [API_CONTRACT](../architecture/API_CONTRACT.md) section 6.1 gains the live dashboard channel, section 8 gains the live Open Liability, and [EVENTS](../architecture/EVENTS.md) gains section 5.4's `feed.*` family. No code, no migration, no `SD-nn`.** **The entry was REQUIRED and the requirement held for a reason bigger than the freeze**: tier 2 has no shape the contract already carries, because section 1's conventions each presume a client that asked and [`registry.ts`](../../apps/api/src/registry.ts) closes the verb list at five while `compose` keys on `METHOD /path`. **So both live surfaces enter as PAYLOADS with no `METHOD /path` heading**, and the address stays `P6-b`'s while the framing stays `P6-g`'s. **THE CLAUSE THAT BINDS ANOTHER SLICE WITHOUT TAKING ITS RULING IS THAT THE SECTION IS THE SURFACE**: [`surface.ts`](../../apps/api/src/surface.ts) withholds by PREFIX rather than by an endpoint list, so an operator live path outside `['/admin', '/internal']` is withheld from the public deployment by nothing at all, which is [P6](../plans/P6-live-tier.md) section 10 item 2's failing direction closed at the contract. **THE APPROVAL CLAUSE'S FIRST FORM WAS FALSE AND RUNNING IT IS THE ONLY REASON ANYBODY KNOWS**: *"begins with one of `OPERATOR_PREFIXES`"* fails on `GET /health`, section 9's own public row, which `surface.ts` registers on BOTH surfaces deliberately. It now imports `classifyPath`, `surfaceServes` and `LIVENESS_PATH` from that file, holds over every section 8 and 9 endpoint, and has been watched failing on a seeded `/live/liability`. **A DRAFT ALSO ADDED `### GET /admin/liability/live` AND THE FULL SUITE REFUSED IT**: [`admin-reads.test.ts`](../../apps/api/test/admin-reads.test.ts) parses every `### GET /admin` row out of the contract and asserts the unserved set exactly, **no slice's fence in `P6` holds that file**, and the repair was not to widen the fence but to notice that fixing an operator PATH is the same transport ruling the entry refuses to take for the trader. **The degradation is ONE OBJECT and the two arms share no field name**, which is `INV-M4-11` and `INV-M4-12` at compile time rather than in review, and **the first frame on any subscription is authoritative** because a fallback needs something to fall back to. **`feed.*` is produced by the expectation sweep and by neither the feed nor the ingest**, so `EVENTS` section 1's delivery rule holds unchanged and `INV-M2-14` stays true as written. **The contract-versus-tree comparison was run in BOTH directions against `CompositionReport.registered`**: zero registered routes undefined, zero section-12 rows uncovered, and six endpoints defined and registered by nothing that were there before this branch. `pnpm run verify` exit 0, 33 of 33 gates, 11 of 11 invariants, 169 files / 3,091 passed / 6 skipped, `falsify` clean |
| [2026-08-27 - Session 273](2026-08-27-session-273.md) | **[ADR-162](../decisions/ADR-162.md), `status: proposed`, UNSIGNED. [`apps/portal/src/http/client.ts`](../../apps/portal/src/http/client.ts) is the first and only file in `apps/portal` that performs a network call, and `app/payouts/` is the one segment wired to it.** No migration, no `SD-nn`, no `apps/api` change, no `packages/db` change, and the other five segments still render from fixtures. **[`surface.test.ts`](../../apps/portal/test/surface.test.ts) IS NARROWED PER NEEDLE RATHER THAN DELETED OR ALLOWLISTED BY FILE**: `fetch(` moves from *no file* to `src/http/client.ts` while `XMLHttpRequest`, `WebSocket` and `EventSource` still hold at **zero files**, so a client reaching for `EventSource` fails the same test a second segment reaching for `fetch` does; watched failing on a real seeded second transport file. **THE MEASUREMENT THAT DECIDED THE SESSION IS THAT `apps/api` SETS NO CORS HEADER ANYWHERE**, so the browser cannot be the client whatever `SameSite=Lax` permits, and the request is made by the portal's server forwarding exactly `merit_session` and never the whole `Cookie` header. **The cache is `no-store` and its decisive reason is not staleness**: every response is identity-scoped, so a key that omits the session is `FM-M4-03` rather than a stale number. **`get` returns `unknown` and a generic `get<T>` is refused** as a cast the compiler cannot check, so narrowing lands in the segment beside the transcription. **THE DISPATCH'S ONE FALSE PREMISE WAS MEASURED RATHER THAN INHERITED**: `GET /payouts` is registered and **`GET /accounts/:accountId/eligibility` is NOT**, read off `CompositionReport.registered` rather than a grep, so `load` asks nothing of the endpoint that does not exist and the screen's `missing` list shrank from a constant to a measurement. **`./view.ts`, `./sections.ts`, `./page.ts` and `./wire.ts` are untouched**, which is the old header's *"NOTHING ELSE IN THIS SEGMENT CHANGES"* kept literally. **Reported rather than repaired: `PayoutCenterLoad` has no error arm**, so a 500 renders as a missing endpoint, and the repair needs two files this fence does not hold. **`next/headers.js` carries its extension for a measured reason** and this is the first import of `next` in the repository. 33 of 33 gates, 11 of 11 invariants, typecheck 0, lint 0, 172 files / 3,136 passed / 6 skipped, `falsify` clean |
| [2026-08-27 - Session 274](2026-08-27-session-274.md) | **ONE OF THREE, AND THE OTHER TWO ARE EACH A DIFFERENT KIND OF NO.** `payment_disputes` is registered `derived` through `purchases` in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts); **`wallet_spend_limits` WAS ALREADY REGISTERED** by [session 202](2026-08-25-session-202.md) and [ADR-092](../decisions/ADR-092.md) section 2 clause 1 forbids re-arguing it; **`events` IS REFUSED.** **104 of 111 becomes 105 of 111**, at 1 root, 40 owned, 20 derived, 41 firm and 3 pair, so **61 are reachable through the scoped accessor**. **No ADR, no migration, no fence widened.** **THE DISPATCH'S FIRST SENTENCE IS FALSE ABOUT THE TREE** -- *"none of them is in `schema.ts`"* -- and a session that had trusted it would have rewritten a landed money-path rule to say the same thing in different words. **`events` HAD ALL FIVE CLASSES TRIED AGAINST IT RATHER THAN THE TWO THE STANDING NOTE NAMES**: `owned` on `identity_id` compiles and drops every account-level row, `derived` through `account_id` is refused by [ADR-101](../decisions/ADR-101.md) clauses 1 AND 2, `pair` needs a second IDENTITY column and this row's second is an ACCOUNT, `firm` is refused because the row declares a column against `identities(id)`, and `root` is `identities`' alone. **It needs a SIXTH CLASS and this session was forbidden an ADR**, so it is left undeclared entirely, on [session 192](2026-08-25-session-192.md)'s model. **THE PAYLOAD REASON IS DEMOTED RATHER THAN REPEATED**, because `idempotency_keys` -- registered out of the SAME migration -- already rules that *"a scope rule states which ROWS reach an identity and nothing about what is inside one"*. **SEEDING THE `ledger_transaction_id` TRAP CORRECTED A CLAIM THIS SESSION HAD ALREADY WRITTEN**: three comments said it *"COMPILES"*, inherited from session 202's record of the same shape, and it does not -- the new terminus assertion narrows `SCOPE_RULES[rule.via]` statically, so it is **`TS2367` and `TS2339` at typecheck** before ADR-101 clause 2 refuses it at runtime. **Four seeds watched failing by name**, plus **`TS2741` naming `paymentDisputes`** before `SQL_NAME` was completed. **One assertion was WRONG when first written and the failure is recorded rather than quietly fixed**: *"not narrowed by `set_by`"* read the whole statement and failed on the SELECT list, and the claim is about what NARROWS a read, so it is now made against the `WHERE` clause alone. **THE +7 IS ACCOUNTED FOR AND NOT REPORTED AS A TOTAL**: five written here and **two GENERATED by the registration**, 263 to 270, the baseline measured by checking `origin/main`'s three files into this tree. 33 of 33 gates, 11 of 11 invariants, typecheck 0, lint 0, **170 files / 3,122 passed / 6 skipped**, `falsify` clean. **`P5-n` IS NOT UNBLOCKED** and that is the honest half of the count |
| [2026-08-27 - Session 275](2026-08-27-session-275.md) | **[API_CONTRACT](../architecture/API_CONTRACT.md) section 6.2 is SERVED**: [`wallet.ts`](../../apps/api/src/routes/wallet.ts) and [`wallet.test.ts`](../../apps/api/test/wallet.test.ts) are the whole diff besides `STATE` and this log. No ADR, no migration, no `SqlExecutorReason` member, no `pg`, [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) untouched. **THE FINDING IS THAT `held_cents` IS 0 ON EVERY RESPONSE THIS TREE CAN PRODUCE AND IT IS THE FIELD THE SCREEN EXISTS FOR**: [ADR-158](../decisions/ADR-158.md) clause 6 makes `available_at` null because no landed column carries a purchase's chargeback-window end, and **the same absence decides MEMBERSHIP of the hold and not only its release**, so `cents` and `since` are equally uncomputable and a `WalletHold` cannot be built truthfully today. `holdsToday` is one function returning one empty array so the day `DEP-M20-03` lands there is one place to change. **The biconditional does NOT reach this route and the paragraph says why it must not**: a payout under hold has not settled, `LT-01` has not posted, and there is no `wallet_entries` row for it at all, so summing it would make `balance = withdrawable + held` false. **THE BALANCE IS `max(id)` AND NOT `max(occurred_at)`**, because `balance_after_cents` is computed at APPEND time and a backdated row is the one world where the two disagree. **[ADR-157](../decisions/ADR-157.md)'s cost is paid in full**: both endpoints read the whole statement, and `atMost` is this cursor's term BY NAME but unreachable, because [`db.ts`](../../apps/api/src/db.ts) is the one door onto `@merit/db` and re-exports nothing -- a two-line change in a file this fence does not hold, reported rather than reached. **THE DISPATCH'S REGISTRY INSTRUCTION IS REFUTED BY THE APPLICATION**: [ADR-100](../decisions/ADR-100.md)'s registry is the DIRECTORY LISTING, there is no row and no list, and two routes were registered by adding one file and editing nothing. Measured on `CompositionReport`: public 36 to 38, operator withheld 35 to 37, union 54 to 56. **NINE DEFECTS SEEDED AND THE NINTH ESCAPED THE FIRST SUITE** -- a lexical tie-break in `isAfter`, reached only when a page boundary falls INSIDE one `occurred_at`, which silently drops an entry and ends the walk one movement short; two cases added and both watched failing on the reseed. `typecheck` and `lint` caught two more `vitest` could not. **33 of 33 gates, 11 of 11 invariants, typecheck 0, lint 0, 171 files / 3,153 passed / 6 skipped, `falsify` clean**, against a baseline of 170 / 3,115 / 6 that reproduced exactly |
| [2026-08-27 - Session 277](2026-08-27-session-277.md) | **[ADR-163](../decisions/ADR-163.md), `status: proposed`, UNSIGNED. [OVERVIEW](../architecture/OVERVIEW.md) section 2 gains three dotted edges, section 3 gains amended cells, and [INFRA](../architecture/INFRA.md) section 2.1 gains amended cells and still rows six services.** No code, no migration, no `SD-nn`, no gate. **THE PLAN RECOMMENDED AN ANSWER AND PERMITTED A REFUSAL, AND THE RECOMMENDATION IS TAKEN WITH AN ARGUMENT**: the live tier's address is [`apps/api`](../../apps/api) on the two deployments that already exist, because the operator 404 is a property of the ROUTE SET and the route set is chosen at STARTUP by the one file that resolves `MERIT_API_SURFACE`. **THE ADDRESS IS AN ORIGIN AND NOT ONLY A PROCESS**: [INFRA section 3](../architecture/INFRA.md) hard rule 3 scopes the C-08 allowlist and the admin origin's Cloudflare rules to the ORIGIN rather than to a service, so the operator's live delivery inherits them by running where `api-admin` already runs. **THE SIXTH DEPLOYABLE IS THE DANGEROUS CANDIDATE BECAUSE IT IS REFUSED BY NOTHING, MEASURED RATHER THAN ASSERTED**: `apps/live/package.json` plus `apps/live/src/api/v1/admin/liability.ts` takes `repo-invariants.mjs` to **11 of 11 and exit 0**, since `RI-04` and `RI-09` both iterate a five-name list and neither reads `apps/`. **TAKING THE RECOMMENDATION MEANS NEITHER TABLE GAINS A ROW**, which the entry states rather than leaving as an absence, and **no live-cache node is drawn** because two of [P6 section 10 item 1](../plans/P6-live-tier.md)'s three readings draw none. **The mechanism is `P6-g`'s and the constraint imposed on it is one clause**: the contract path sits under `BASE_PATH` and is classified by `classifyPath` at startup, in the process that resolved the surface. Verified in pieces: 33 of 33 gates, 11 of 11 invariants, typecheck 0, lint 0, 170 files / 3,115 passed / 6 skipped, `falsify.mjs` exit 0 |
| [2026-08-27 - Session 278](2026-08-27-session-278.md) | **[ADR-164](../decisions/ADR-164.md), `status: proposed`, UNSIGNED. Migration [`0050`](../../packages/db/migrations/0050_live_cache_and_role.sql) SPENT: `live_account_state` and a fifth role `merit_live`, `SD-M2-07`.** **[P6 section 10 item 1](../plans/P6-live-tier.md) held this slice for a founder answer, the answer was DELEGATED, and reading 1 of three is ARGUED AND TAKEN.** **THE SENTENCE THE SLICE STARTS FROM WAS EXECUTED RATHER THAN BELIEVED**: on the `0001`..`0049` tree a bare `CREATE TABLE zz_hazard_demo (id int)` returns `t` on all four verbs for `merit_app`, with nothing granting it, so [`0026`](../../packages/db/migrations/0026_roles_and_grants.sql)'s `ALTER DEFAULT PRIVILEGES` really does move the separation further away when a table is added. **THE DEPARTURE FROM SIX PRECEDENTS IS ONE VERB**: `0032`, `0039` and `0049` each revoke `UPDATE, DELETE` because each implements append-only, and `FM-M12-08` is about READING, so `0050` revokes `ALL` from `merit_app` AND `PUBLIC`. **A PROCESS-LOCAL STORE IS REFUSED ON `GS-132`**, the phase's own done-condition, which requires the cache populated from OUTSIDE the process that writes it; **A SECOND STATEFUL SERVICE IS REFUSED ON A GROUND [OVERVIEW](../architecture/OVERVIEW.md) SECTION 3 DOES NOT SUPPLY**, because neither limb of the pg-boss sentence transfers to a discardable cache with one writer, and what actually refuses Redis is that it makes the separation a CONVENTION again. **THE COST IS NAMED AND NOT SPENT**: [`client.ts`](../../packages/db/src/client.ts) opens one pool from one `DATABASE_URL` and [INFRA section 5](../architecture/INFRA.md) gives `merit_app` to *"API and worker at runtime"*, so no grant can separate the API's read from the stats worker's, and clause 4 rules the live path a principal the database can name while refusing to say WHICH process -- `P6-b`'s address and `P6-g`'s mechanism. **`F1` forecloses the cheap escape**, granting `merit_app` `SELECT`, which makes `FM-M12-08` false with nothing failing. **THREE COLUMNS REFUSED AND NO MONOTONICITY TRIGGER**, so triggers move 20 to 20. **THE STORE IS NOT APPEND-ONLY**, which resolves [P6 section 3.5](../plans/P6-live-tier.md)'s range-term question to NO. **THE FENCE HAS TWO CELLS IT SHOULD NOT HAVE AND ONE IT LACKS, BOTH EXECUTED**: the table is NOT registered on [ADR-092](../decisions/ADR-092.md)'s own rule quoted in [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts), and that same unfenced file's `expect(createdTables).toBe(112)` counts `^CREATE TABLE` over the whole migration set and was watched failing at 113. **Applied from empty against PostgreSQL 16.13 under `ON_ERROR_STOP`, 50 of 50**; tables 112 to 113, indexes 395 to 396, `CHECK`s 477 to 479, constraints 773 to 777, triggers 20 to 20, read from the catalogue rather than from a grep; **grant probe in BOTH directions and five rejections watched firing**. 33 of 33 gates, 11 of 11 invariants, typecheck 0, lint 0, 170 files / 3,115 passed / 6 skipped, `falsify` clean in 5m20s. **NOT PUSHED AND NO PULL REQUEST: the repository is not in this session's authorized set** |
| [2026-08-27 - Session 280](2026-08-27-session-280.md) | **[ADR-166](../decisions/ADR-166.md), `status: proposed`, UNSIGNED. `GET /admin/evidence/:accountId` requires `?audience=` beside `?reason=` with NO DEFAULT, `EvidenceExportRequest` carries the field, `EvidencePackResponse` echoes it, and section 8's heading over three paths becomes three rows.** **[`0008_risk.sql`](../../packages/db/migrations/0008_risk.sql) makes `audience`, `redaction_profile` and `includes_detector_detail` all `NOT NULL` and the endpoint had input for none of them**, so the generator would have supplied an audience the caller never named, which [M06 section 4](../plans/M06-admin-ops-console.md) forbids in words. **THE NARROWEST DEFAULT IS REFUSED TOO**: `trader` is what the merged `evidence_packs_trader_gets_no_detector_detail` treats as *"the one combination that must be unrepresentable"* rather than as a floor, and the column exists so the audience is *"a DECLARED, AUDITED PROPERTY of the export rather than a judgment made in the moment"*. **The four names are a TRANSCRIPTION of a merged `CHECK` and the suite reads the migration rather than a copy**, so a fifth name cannot drift in. **The echo carries the assertion no type check can make**: a pack built for `internal` against a request for `trader` is well formed and carries a real audience, and it is refused rather than relabelled, because overwriting it is the same disclosure with a truthful label removed. **All three new assertions were WATCHED FAILING on seeded defects**, a silent `trader` default, a relabel, and a fifth audience name. **THE SECOND HALF WENT THE OPPOSITE WAY FROM [ADR-153](../decisions/ADR-153.md) AND THAT IS THE PART WORTH READING.** Measured from `CompositionReport` on both surfaces rather than from a grep: `GET /admin/eligible-forecast` is registered on `operator` and withheld from `public`, and **`GET /admin/loss-ratios` and `GET /admin/cusum` are in NEITHER list on EITHER surface**, a match for either path across both reports returning ZERO. **So they 404 on the admin origin too**, which is not the 404 [`registry.ts`](../../apps/api/src/registry.ts) calls *"this list being non-empty and nothing else"*. **They are NOT deleted**: unlike `GET /auth/otp` they are defined by a heading, pinned by a test as *"a register of contract rows nobody has built"*, and named inside `P7-k`'s own fence. **A backlog and a phantom look identical from outside the tree and the repair for one is the opposite of the repair for the other.** **Their BODIES are deliberately unspecified**, because `P7-k` writes `sample_size` and `insufficient_data` that no current type carries and the CUSUM has no storage until `ADR-167`. **`OQ-F4-03` IS NOT RULED and clause 5 names which half is already closed**: detector internals in a regulator pack is closed by three approved documents, an impersonation record in one is open and COUNSEL's per [ADR-068](../decisions/ADR-068.md) section 3. **`redaction_profile` is not echoed and not ruled**, its vocabulary being `P7-j`'s under `INV-M7-10`. **TWO DISPATCH CLAIMS DID NOT SURVIVE CONTACT AND BOTH ARE REPORTED**: the repository was NOT PRESENT in the container and was cloned before anything could be read, and [ADR-161](../decisions/ADR-161.md)'s pinned unserved list is no longer the two it recorded, [ADR-158](../decisions/ADR-158.md) having added `/admin/wallet/reconciliation` yesterday. **Baseline reproduced BEFORE any change: 33 gates, 11 invariants, typecheck 0, lint 0, 170 files / 3,115 passed / 6 skipped.** After: 33 of 33 gates, 11 of 11 invariants, typecheck 0, lint 0, **170 files / 3,119 passed / 6 skipped**, `falsify.mjs` exit 0. Six foreclosures. No migration, no `SD-nn`, no fence widened, and no domain-shaped string in the diff |
| [2026-08-27 - Session 281](2026-08-27-session-281.md) | **[ADR-167](../decisions/ADR-167.md) proposed, `status: proposed`, approval line UNSIGNED, MONEY PATH BY CONTENT, and `0051` RETURNS TO THE POOL UNSPENT.** [P7 section 10 item 3](../plans/P7-risk-and-abuse.md) held `P7-c` for a founder answer, the answer was delegated, and what was delegated was the authority to CHOOSE rather than permission to assume: **reading 3 of three, `S_t` folded at read time from a series [`0007`](../../packages/db/migrations/0007_accounts.sql) already carries, and no column, table or row holds it.** **Reading 1 is refused on a MONEY-PATH DEFECT rather than on the primary-key change**: with `metric` in the key, `state`'s `'paused'` value governs the added rows and [API_CONTRACT:798](../architecture/API_CONTRACT.md)'s `per_plan.sales_paused` derives from it, so an ADVISORY statistic whose own panel gloss is *"inspect before the funded wave"* gets a column spelling a REVENUE PAUSE one `UPDATE` away, on the table `INV-M5-12` is written about; and `min_sample integer NOT NULL CHECK (min_sample > 0)` forbids even the honest zero, so such a row must invent a minimum sample it does not have. **Reading 2 is refused on [`0049`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql)'s OWN objection with `0049`'s own remedy structurally unavailable**: its founder-read item 1 is *"a ratio stored beside its own numerator invites recomputation drift ... a ratio the database computes cannot disagree with its own inputs"*, answered there by a `GENERATED` column -- **but a generated column is a row-local expression and `S_t` is a recurrence over the row before it**, so the drift returns unpoliceable one migration later on the same rail; and its parameter columns have no source, `mu_0` and `sigma` being `DEP-M6-05`'s and `DEP-M6-05` being **Wave 4**. **THE PLAN'S OWN STATED REASON FOR THE WINNING READING IS CORRECTED RATHER THAN REPEATED**: *"nothing about storing it makes more reproducible"* is too weak, because storing `S_t` makes the value RETRIEVABLE and not CHECKABLE, and **a stored `S_t` that disagrees with a replay yields two answers and no rule for which is right.** **P7 RULE 17's SECOND CLAUSE DOES NOT SURVIVE THE ARITHMETIC AND GETTING IT RIGHT IS WHAT REMOVES THE FLOAT**: every operand of `S_t = max(0, S_(t-1) + (x_t - mu_0 - 0.5*sigma))` is a RATE, so the statistic **IS** commensurable with a basis point and is **NOT** commensurable with `ratio_bp`, which is a LOSS ratio in the same unit -- **a column carrying the right unit and the wrong quantity is exactly what "column for column" mistook for a fit.** `integer` suffices with the bound a multiplication rather than an assumption (`10000` bp per day, `int4` unreached for **588 years**), and **the slack is configured as `k_bp` so `sigma` is never halved on the evaluation path**, because a per-step truncation with a constant sign accumulates in a running sum. **[ADR-157](../decisions/ADR-157.md) finding 8's numeric-transport ruling is NOT reached and the line that would reach it is named**: the moment `x_t` is computed with a SQL `COUNT` or `SUM` rather than by counting rows. **THE FALSE DISPOSITION IS IN TWO PLACES AND THE DISPATCH NAMED ONE**: [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md) section 26's `OI-01` register row says **"column for column"**, enumerates nothing where `0049` at least enumerates the five columns it checked, and is in the register rather than in a migration header, so it is the copy a reader reaches first; corrected in **section 27** and never in `0049`, on section 22's precedent. **THE UNDERLYING GAP IS NOT CLOSED AND THE ENTRY SAYS SO**: the CUSUM had neither storage nor inputs and now has a ruling about the first and still nothing about the second, [`apps/admin/src/page.ts:156`](../../apps/admin/src/page.ts) already listing `P-M6-06` pending on `DEP-M6-05` with `FM-M6-07`'s *"either constant alarms or none, which is the same as no chart"*; **what needs a versioned home when the harness lands is the PARAMETER SET and not the statistic**, and [`detector_definitions`](../../packages/db/migrations/0008_risk.sql) is named as the shape that already exists and deliberately NOT taken, because its `is_sensitive` column is `P7-j`'s strip list. **`OQ-M6-02`'s NUMBER IS UNTOUCHED and only the schema half is ruled**: `sample_size` is the DENOMINATOR's count, sourced from [`0016:116`](../../packages/db/migrations/0016_treasury_controls.sql)'s own *"a two-transaction denominator"*. **NO MIGRATION, NO TABLE, no `schema.ts` registration, no `scope.ts` change, no design record, no `SqlExecutorReason` member, no `SystemReason` member, no `pg` import and no cast**; `schema.ts` receives **one comment and no code**, because it is where a later session would add the column the entry forecloses, and `scope.ts` is not touched even by a comment on its own tenancy-only rule. **NO `E2` READ IS OWED, because nothing was written that a founder must read line by line.** **THE REPOSITORY WAS NOT IN THIS CONTAINER AND WAS ATTACHED AND CLONED BEFORE ANY WORK BEGAN**, `HEAD` landing exactly on the dispatch SHA `552c918`. **Sessions 274 and 278 were not merged into and the union is mechanical**: `schema.ts` gains a comment block above one existing table and no declaration, `scope.ts` is untouched, and `DELTA_MANIFEST` gains a new trailing section. Gates **33 of 33**, invariants **11 of 11**, `typecheck` exit 0, `lint` exit 0, suite **170 files / 3,115 passed / 6 skipped**, `falsify.mjs` exit 0 at 33 gates, 80 scope and 10 loader cases -- the dispatch baseline reproduced BEFORE anything changed and unmoved by all of it |
| [2026-08-27 - Session 282](2026-08-27-session-282.md) | **[`P7-d`](../plans/P7-risk-and-abuse.md) IS WRITTEN AND ELEVEN OF ITS EIGHTEEN ROWS CARRY NO STATED NUMBER AT ALL**, which is `OQ-M7-02`'s own state rather than a gap in the transcription. Every detector [M07 section 3.2](../plans/M07-risk-abuse.md) names has a `detector_definitions` row with `version`, `parameters`, `effective_from` and `is_sensitive`, on [`seed/calendars`](../../packages/db/src/seed/calendars)' shape: a hand-written source, a generator that refuses rather than guesses, and a committed generated artifact. **EVERY VALUE CARRIES THE LINE OF `M07` IT WAS READ FROM AND THE GENERATOR OPENS THAT FILE AT THAT LINE**, requiring the quote verbatim, so a paraphrase fails, a drifted line number fails, and a number nobody can point at fails because there is no line to point it at; **197 citations resolve** and the authority is digested besides, because a cite that still resolves against a different `M07` has silently changed meaning. **A PARAMETER HAS THREE STATES AND THEY ARE NOT INTERCHANGEABLE**: `unstated` sends a later session to find the number `M07` owes, `not_applicable` says there was never one, and only `stated` may carry a value; `D-10` has no numeric threshold because its statistic is a condition and `D-12` writes no `risk_flags` row at all, so `open` there would be false rather than cautious. **TWO SENTENCES ARE READ, NAMED AND DELIBERATELY NOT ENCODED**, and they are the Behavior column `P7` warns is this phase's temptation: `D-16`'s *"Hard links auto-enforce"* and section 7.9's *"Auto-enforce."* over `D-08` and `D-09`. A seed that merely omitted them is indistinguishable from one whose author never read them, so a `refusals` block names both with its authority, the generator requires it, and the refusal is mechanical: any parameter VALUE spelling an enforcement outcome is rejected and `D-16`'s `auto_enforce` is `not_applicable` because `OQ-M7-05` answers it both ways and **writing either value settles an open question by being written**. **`OQ-M7-02`'s ROUTING HALF IS CONTESTED BY FOUR OTHER PLACES IN `M07` AND NEITHER READING IS WRITTEN**: read literally it sends severity 4 and 5 to the digest, which is the band `G-HOLD-REQUIRED` reads to HOLD A PAYOUT, while it is severity 1 that `M07` twice sends there. **`is_sensitive` IS UNIFORMLY `true` AND THAT IS A FINDING FOR [`P7-j`](../plans/P7-risk-and-abuse.md)**: while the column is uniform, a `trader` pack that COMPUTES its strip list from it and one that strips every detector UNCONDITIONALLY produce byte-identical output, so `GS-112` passes either way and `INV-M7-10`'s mechanism is untested; the distinguishing row cannot come from this seed because there is no `M07` line to cite for it, and an assertion goes red the day the column acquires its first `false`. **The test's expected set is PARSED out of the authority rather than written**, order included, and **three seeded violations failed on a MORE SPECIFIC finding than the one they named**, which is what the suite exists to catch. **No migration, no ADR, no `SD-nn`, no `SqlExecutorReason`, no `SystemReason`, no `pg`, no cast past a key type, no `schema.ts` and no `scope.ts`.** Gates **33 of 33**, invariants **11 of 11**, typecheck 0, lint 0, `format:check` 0, `falsify` exit 0, suite **171 files / 3,213 passed / 6 skipped** against the dispatch baseline of 170 / 3,115 / 6, reproduced exactly first |
| [2026-08-27 - Session 283](2026-08-27-session-283.md) | **`apps/site` had `"build": "next build"` and no `app/` directory, so `next build` exited 1 on "Couldn't find any `pages` or `app` directory". It now builds TEN ROUTES plus the framework's own not-found.** The App Router shell lands: `next.config.mjs`, the root layout, and one page per surface, each CALLING the page models `src/routes/` has held since P4-j rather than restating them. **`.github/workflows/ci.yml` carried a comment I was dispatched to repair and it was FALSE: it claimed `apps/portal` was "the only package in this workspace carrying a `build` script that compiles a rendered document"**, which ADR-095 ruling 5 contradicts in the entry that created both scripts. The comment is corrected and `CI-07` now builds both UI deployables. **ADR-095 RULING 4's CARVE-OUT WAS NOT WRITTEN, AND THAT IS THE SESSION'S REAL FINDING**: the revalidation hook's contract path is `POST /internal/revalidate`, `/internal` is an `OPERATOR_PREFIXES` entry in [`surface.ts`](../../apps/api/src/surface.ts), and `RI-09` refuses any file putting an operator prefix directly below a routing root in a non-`api` deployable. Writing the carve-out exactly as ruled turns an invariant red, so it is reported rather than written and it needs a ruling nobody has allocated. **Two more shapes did not fit a page cleanly and are reported rather than restructured**: `/plans/:slug/rules` has no page model because `rulesPage` requires a size, and `/faq` and `/blog` have no index model because `legalIndex` is `legal`'s by construction. |
| [2026-08-27 - Session 284](2026-08-27-session-284.md) | **[`apps/api/src/routes/account-reads.ts`](../../apps/api/src/routes/account-reads.ts) (new) registers API_CONTRACT section 6's `GET /accounts/:accountId/marks`, `/timeline`, `/eligibility` and `/certificate`, all four of which were unregistered on both surfaces.** No ADR, no migration, no `packages/db` change, no `API_CONTRACT` change, no `apps/portal` change. **PROVEN FROM `CompositionReport.registered` RATHER THAN FROM A GREP**: the public surface goes from 36 registered to 40 and the operator surface holds at 19. **ONE OF THE FOUR IS SERVED END TO END AND THREE REFUSE BY NAME, and the count is reported rather than rounded.** `/marks` reads real `daily_marks` and `rule_states` rows through the accessor; `/timeline`, `/eligibility` and `/certificate` raise `AccountReadsBackendUnwired` carrying the blocker and answer 503, which is `databaseAuthBackend`'s shape and `accounts.ts`'s `readProgress`'s shape. **THE THREE BLOCKERS ARE MEASUREMENTS**: `events` is in neither `TABLES` (104 members) nor `SCOPE_RULES`, so no scope class reaches it; `evaluatePayout`'s `RuleState` requires `lifetimeSettledCents`, `breached` and `breachKind` and `0015_rule_states.sql` declares none of the three, with `lifetime_settled_cents` absent from every migration in the tree; and `certificates` carries no image location column at all, so `image_url` has not even a stored value to sign. **A NEW MODULE RATHER THAN `accounts.ts`, ARGUED IN THE FILE**: ADR-100's registry is the directory listing, so a new file collides with nothing, and the ports and tables are disjoint. **TENANCY IS THE ACCESSOR'S AND THE ACCOUNT IS RESOLVED FIRST ON EVERY READ**, which is what makes a stranger's id a 404 rather than a 200 with an empty page. **TWO CONTRACT UNDERDETERMINATIONS ARE RULED IN THE FILE AND REPORTED IN THE PULL REQUEST**: what `corrected` means on a day carrying two marks, and the list envelope for `/marks`. **`@merit/rules-engine` IS DECLARED BY `apps/api` AND `accounts.ts`'s HEADER SAYS IT IS NOT**, which is a stale sentence found and reported, not repaired. 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, 173 files / 3,183 passed / 6 skipped, `falsify` clean |
| [2026-08-27 - Session 285](2026-08-27-session-285.md) | **[ADR-162](../decisions/ADR-162.md) executed for `app/accounts/`. No ADR taken, no migration, no `SD-nn`, no `apps/api` change, no `packages/db` change, and [`http/client.ts`](../../apps/portal/src/http/client.ts) untouched.** **THE DISPATCH'S ENDPOINT ROW WAS WRONG IN BOTH DIRECTIONS AND THE COMPOSITION REPORT IS WHY WE KNOW.** It called `accounts` *"all endpoints registered"* and named two reads; built from `discoverRouteModules()` and `buildServer({ surface: 'public', modules })`, this segment needs FOUR: `GET /accounts`, `GET /accounts/:accountId` and `GET /plans/:planId/versions/:version` are registered and **`GET /accounts/:accountId/marks` IS NOT**. So SC-M4-02 is wired end to end and SC-M4-03 performs both reads that exist and names the marks endpoint as the one it waits on. **THE LOAD TYPES HAVE THREE ARMS AND THE THIRD IS THE ONE ADR-162 SECTION 5 ITEM 1 REPORTS MISSING ON THE PAYOUT CENTRE**: `ready`, `unavailable` (nothing failed) and `error` (a registered endpoint refused), so a 500 and an unregistered route no longer render the same screen, and a 404 on another trader's account reaches the trader as *"not found"* through `toPortalErrorKind` (INV-M4-07). It is declined at authorship rather than repaired: this segment had no load type at all. **The guards check every field the views read**, `progress` on every phase and not only the one in the response at hand, and every `_cents` and `_bp` field as an INTEGER. **`isPinnedPlanSource` deliberately does NOT claim `PlanVersionResponse`**: three of eight fields checked, three claimed, which is foreclosure 5 in the other direction. **`accountsSource()` and `AccountsSourceNotWiredError` are REPLACED rather than left standing**, and the test that asserted the throw is narrowed into a stronger claim: a trader with no accounts, an unwired endpoint and a refusal render as three distinct screens. 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, 173 files / 3,153 passed / 6 skipped, portal build clean, `falsify` clean |
| [2026-08-27 - Session 286](2026-08-27-session-286.md) | **[ADR-162](../decisions/ADR-162.md) EXECUTED FOR THE `kyc` SEGMENT. No new ADR, no migration, no `SD-nn`, no `apps/api` change, no `packages/db` change, and [`apps/portal/src/http/client.ts`](../../apps/portal/src/http/client.ts) is untouched.** `GET /kyc/status` is read through that client and **`POST /kyc/session` IS REFUSED**, so this session wired the read and declined the write. **BOTH ENDPOINTS WERE MEASURED REGISTERED and the refusal is the fence rather than an absence**: `discoverRouteModules()` then `buildServer({ surface: 'public', modules })`, reading `CompositionReport.registered`, lists 36 public routes including both. **THE WRITE IS REFUSED TWICE OVER AND NEITHER FENCE WAS WIDENED.** [`apps/api/src/routes/kyc.ts`](../../apps/api/src/routes/kyc.ts) says `openVerification` *"WRITES TWO ROWS IN ONE TRANSACTION"* and *"it is a new row every time and there is no update on this port"*, which puts it inside [`surface.test.ts`](../../apps/portal/test/surface.test.ts)'s *"nothing that changes a trader account exists in this app"*; and independently **`ApiClient` declares `get` and no second method**, so a POST from here needs an edit to ADR-162's file or a second transport the suite fails on. **NO NEEDLE IN `surface.test.ts` WAS NARROWED, because nothing here needed it narrowed** and session 158's rule is that deleting an entry *"has removed the control while appearing to satisfy it"*. **THE GUARD WAS ALREADY IN THE SEGMENT AND NOW READS THE WIRE**: `screenKycStatus` takes `unknown`, screens the WHOLE payload for a document-shaped key before it reads one field, and refuses a non-string on any of `KYC_STATUS_FIELDS`, which is ADR-162 clause 5 satisfied by a control this segment wrote months before a transport existed. **THE ERROR KIND SHRANK FROM A CONSTANT TO A MEASUREMENT**: [`page.ts`](../../apps/portal/src/app/kyc/page.ts) rendered `toPortalErrorKind(503)` for every failure and now renders the kind the API actually returned, so a 401 says *"Please sign in again"* rather than *"We could not load this just now"*, and everything that is not a measured refusal is still `server_error`. **FAIL CLOSED IS ASSERTED ON THE RENDERED BYTES OVER FIFTEEN FAILURE MODES, ALL OF THEM CLAIMING `verified`**, following [`accounts.ts:824`](../../apps/api/src/routes/accounts.ts)'s *"fails closed, because the alternative is reporting somebody verified"*; **watched failing on a real seeded defect** that returned a fabricated verified status on a refused read, reporting `401 renders no verified badge`. **`ApiConfigError` AND NOTHING ELSE IS CONVERTED, ASSERTED IN BOTH DIRECTIONS.** **The client is built INSIDE `status()` and never at module scope**, because a client bound at module load serves the first trader's cookie to every trader afterwards, which is `FM-M4-03` reached by a `const`. **Reported rather than repaired: [`app/payouts/page.ts`](../../apps/portal/src/app/payouts/page.ts) declares no `dynamic` and `/payouts` PRERENDERS STATIC**, which ADR-162 clause 4 argues against for the whole application. 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, 173 files / 3,150 passed / 6 skipped, `/kyc` still `ƒ` on a cache-cleared build |
| [2026-08-27 - Session 287](2026-08-27-session-287.md) | **[ADR-168](../decisions/ADR-168.md), `status: proposed`, UNSIGNED. [ADR-153](../decisions/ADR-153.md)'s comparison run in the other direction, and the three it finds do not have one answer: TWO MISSING ROWS AND ONE REFUSAL.** [API_CONTRACT](../architecture/API_CONTRACT.md) gains section 6.3 with `GET /certificates` and `GET /certificates/:code/image.png` and one section 11 rate-limit row; `GET /affiliate/creatives` does NOT land. **The dividing line is the SOURCE and not the segment**: [M11 section 4](../plans/M11-certificates-social-proof.md) is `approved` and marks both certificate rows NEW and Owns, so under FREEZE they are commitments the contract never grew, while [M08 section 4](../plans/M08-affiliate-system.md) enumerates the affiliate surface, says in its own prose that it *"adds one"*, and the one is the `POST`. **THE GAP IS NOT THREE AND NOT TWO: `M11` MARKS SIX NEW AND THE CONTRACT DEFINED NONE**, so four stay owed, among them `GET /verify/:code`, which section 6's own `verify_url` field points at -- `ADR-153`'s disease found a second time inside the section this session edited, and deliberately not repaired because a public oracle over Merit's payout book is its own ruling (`AS-M11-04`). **TWO DISPATCH CLAIMS DID NOT SURVIVE**: the list endpoint was offered as marked NEW *"with no citation at all"* and the sentence that marks it NEW **is** the citation, and the image path carries `.png` in `M11` and in the portal while the dispatch and the `168` reservation both drop it, which matters because that row is section 1's **first non-JSON success response**. **NOT ONE OF THE THREE IS A CALL SITE**: all three segments name the endpoints as ABSENCES and route around them, so exactly one comment became false and was replaced and the other two survive verbatim. **The rows were written from the CHECK and two contradict `M11`'s own prose**: [`0020`](../../packages/db/migrations/0020_public_surface.sql) has no status column so `state` is derived and section 3.1's `withheld` cannot be held by the table ([ADR-040](../decisions/ADR-040.md)'s `PayoutListItem` lesson one section up), and `certificates.code` is `NOT NULL` although section 3.2 generates it only in the issued branch. **The public image row keeps `INV-M11-05`'s entropy, rate limit and anomaly signal and CANNOT keep its clock**, because `FM-M11-05`'s own `(code, row_version)` cache separates two VALID codes before an invalid one is asked about. **The comparison was run from the API rather than from a grep**: 54 registered over both surfaces, 60 contract endpoints expanded, 14 rows registered by nothing and 0 registered routes outside the contract's sections, moving to 16 and stated. 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, 172 files / 3,137 passed / 6 skipped, `falsify` clean. **No route registered, no code, no migration, no `SD-nn`, no fence widened.** |
| [2026-08-27 - Session 288](2026-08-27-session-288.md) | **[`apps/api/src/routes/admin-payouts.ts`](../../apps/api/src/routes/admin-payouts.ts) (new) registers [API_CONTRACT](../architecture/API_CONTRACT.md) section 8's `POST /admin/payouts/:id/release` and `POST /admin/payouts/:id/enforce`. MONEY PATH, `E2` READ OWED, DO NOT MERGE UNREAD.** No ADR number, no migration number, no `packages/db` change, no contract change, and **the route registry needed no edit at all** because `registry.ts`'s module list IS the directory listing. **Measured from `CompositionReport` and not from a grep, before and after**: neither path was in `registered` NOR `withheld` on either surface beforehand; afterwards `operator` registers both and `public` withholds both. **`payout_requests_hold_is_complete` ([`0031:62`](../../packages/db/migrations/0031_payout_hold_and_identity_restriction.sql)) is a BICONDITIONAL and these two endpoints are the only things that will ever cross it**, so both writes blank ALL FIVE hold columns and the hold is read off the LOCKED row before the write erases it. **THE CONSTRAINT IS READ OUT OF THE MIGRATION BY THE SUITE AND COMPARED WITH THE `SET` CLAUSE COLUMN FOR COLUMN**, because CI-04 has no database to make that refusal. **What is lost is named rather than worked around**: the contract's hold block carries four members, so `hold_reason` reaches nobody, and after either call the row cannot say it was ever held; `admin_actions.before` is the entire durable record and this module writes all five columns into it. **The release POSTS `LT-01`**, built by [`payouts.ts`](../../apps/api/src/routes/payouts.ts)'s own `lt01()` and never retranscribed, under the request's OWN stored idempotency key so the two doors cannot mint two postings for one approval; the enforcement posts nothing. **ADR-157's ROW LOCK is the first statement of every transaction and no advisory lock is taken.** **`INV-M5-23`'s shape is structural**: the 401 and the 403 are decided before a transaction opens, and the suite asserts the recorder is EMPTY on both. **Reported rather than repaired: `approved_at` is left false on a released hold DELIBERATELY** (overwriting is the lossy direction and `admin_actions.created_at` already records the release), and no column holds `failure_note`. 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, 173 files / 3,178 passed / 6 skipped, `falsify` clean |
| [2026-08-27 - Session 289](2026-08-27-session-289.md) | **[ADR-162](../decisions/ADR-162.md) executed for `app/calendar/`. No ADR taken, no migration, no `SD-nn`, no `apps/api` change, no `packages/db` change, and [`http/client.ts`](../../apps/portal/src/http/client.ts) untouched.** All three screens are wired end to end and the decision session 285 left open was TAKEN: [`api/types.ts`](../../apps/portal/src/api/types.ts) gains API_CONTRACT section 1's `CursorPage<T>`, the timeline reads ONE page at the contract's maximum, and `TimelineScreen` gains a REQUIRED `paging` prop so a truncated read cannot render as a whole timeline. **THE SEGMENT READS FOUR ENDPOINTS AND THE DISPATCH NAMED THREE.** `as_of_trading_day` (INV-M4-02) and the plan pin (M04 section 4) live only on `GET /accounts/:accountId`, so both account screens read it FIRST and compose the second URL from its body. **AND REGISTERED IS NOT SERVED, WHICH IS THIS SESSION'S OTHER MEASUREMENT.** All four are in `CompositionReport.registered`; THREE OF THE FOUR RAISE BEFORE THEY ANSWER in every deployment, including one that has run `start.ts`: `start.ts` never calls `setEconomicCalendarSource` (500), `readProgress` raises `AccountsBackendUnwired` (503), and `readTimeline` raises `AccountReadsBackendUnwired` because *"`events` IS NOT A REGISTERED TABLE"* in `packages/db/src/scope.ts` (503). So every screen renders its `error` arm today and that is the arm WORKING: `unavailable` would tell a trader the timeline is not written yet on a deployment where it is registered, serving and broken. **The load types have three arms from authorship**, `ApiConfigError` and nothing else is converted, and the `missing` list is a measurement. **The guards check every field the views read**: all seven occurrence fields, `next_cursor` and not only the rows, `status` against the contract's two members because `superseded` is derived from it, `rules` as JSON rather than by key, and a `_cents` or `_bp` detail as an INTEGER. **`isCalendarAccountSource` claims three fields and checks three**, which is 285's `isPinnedPlanSource` rule. **`assertOk` and `ApiReadError` are REMOVED**: a second status refusal beside `toPortalErrorKind` is how a second vocabulary gets written. **`/calendar` moved from `○ (Static)` to `ƒ`**, which ADR-162 clause 4 requires of an authenticated read. 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, 177 files / 3,275 passed / 6 skipped, portal build 12 routes, `falsify` clean |
| [2026-08-27 - Session 290](2026-08-27-session-290.md) | **[ADR-162](../decisions/ADR-162.md) executed for `app/referrals/`, and [ADR-168](../decisions/ADR-168.md) clause 3 obeyed rather than papered over. No ADR taken, no migration, no `SD-nn`, no `apps/api` change, no `packages/db` change, no `API_CONTRACT` change, no export added to [`src/index.ts`](../../apps/portal/src/index.ts), and [`http/client.ts`](../../apps/portal/src/http/client.ts) untouched.** **`GET /affiliate/stats` IS REGISTERED AND IS NOW READ**, measured with `discoverRouteModules()` then `buildServer()` over BOTH surfaces reading `CompositionReport.registered`; the guard checks all nine fields of `AffiliateStats` and refuses a non-integer on every `_cents` and `_bp` field and on both counts. **THE SEGMENT HAS THREE ARMS AND THE THIRD IS THE ONE [ADR-162](../decisions/ADR-162.md) SECTION 5 ITEM 1 REPORTS MISSING**, declined at authorship rather than repaired because this segment had no load type at all. **A REFUSED READ IS NOT A MISSING ONE AND THE TYPE ENFORCES IT**: `PendingRead` and `RefusedRead` carry different discriminants, so a `RefusedRead` pushed into `unavailable`'s `missing` is **`error TS2741`**, watched failing on a seeded diff; the screen renders "Waiting on" and "Not served" as two lists under two different words. **THE `missing` LIST IS A MEASUREMENT**: on the branch where the stats read succeeded, the stats endpoint is not in it. **THE DISPATCH'S SECOND JOB WAS REFUTED AT SOURCE AND [`screen.ts:307`](../../apps/portal/src/app/referrals/screen.ts) IS UNCHANGED.** It said the comment names `GET /affiliate/creatives` as something the screen is waiting for; the comment states an ABSENCE, [ADR-168](../decisions/ADR-168.md) clause 3 CONFIRMS that absence and makes it permanent, and that entry already re-read this comment against all three clauses and left it verbatim on the ground that *"a comment edited to look responsive to a ruling that did not touch it is drift"*. **WHAT DID CHANGE IS THE RENDERED COPY**, which is the rendering decision the dispatch assigned: *"No creative has been submitted for review"* is a claim about whether a submission exists that this application can never observe, and once the segment is wired it is false for every affiliate who has submitted one. **NO ENVELOPE WAS NEEDED AND THAT WAS MEASURED**: section 7 declares `AffiliateStats` as nine scalars and `projectStats` returns exactly those nine, so this session did not race session 289 over `api/types.ts`. `surface.test.ts`'s trader-account list was NOT narrowed, because nothing here needed it narrowed. 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, 177 files / 3,268 passed / 6 skipped, portal build 12 routes, `falsify` clean |
| [2026-08-27 - Session 291](2026-08-27-session-291.md) | **[`apps/worker/src/sweeps/expiry.ts`](../../apps/worker/src/sweeps/expiry.ts) and [`ports.ts`](../../apps/worker/src/sweeps/ports.ts) (both new) build [P5](../plans/P5-payouts-and-wallet.md) section 8's `P5-j`, the hourly expiry sweep: ONE job over the THREE clocks [CRON_INVENTORY](../ops/runbooks/CRON_INVENTORY.md) gives it, on that page's own rule that a second sweep is a second thing to stall. MONEY PATH, `E2` READ OWED, DO NOT MERGE UNREAD.** No ADR number, no migration number, no `packages/db` change, no contract change, and [`admin-payouts.ts`](../../apps/api/src/routes/admin-payouts.ts) was read line by line and NOT touched. **THE THREE CLOCKS ARE THREE DIFFERENT ACTS**: the hold releases to `approved`, blanks all five hold columns and **posts `LT-01`** (`INV-M5-17`); the withdrawal halt blanks three columns, leaves the rail's `status` untouched (`INV-M20-13`) and **posts nothing** (`INV-M20-14`). **THE THIRD CLOCK'S RELEASE IS NOT WRITABLE BY ANY CODE IN THIS TREE AND THAT IS MEASURED**: `frozen` releases to `settled`, `payout_requests_settled_has_days` requires `effective_trading_day` and so a calendar, `merit/no-calendar-in-expiry-path` bans that import in this exact path under `ADR-042` and names THIS SWEEP as the failure it exists for, `INV-M5-07`'s `applySettlement` EXISTS but takes a `CalendarSlice` so calling it needs that same banned import (**this session first claimed it did not exist, which was false**), and nothing writes `frozen` at all -- so that leg is swept, WARNED ON via `payout.freeze_expiring` at its twelve-hour lead, and returned as an `unreleasable` finding rather than guessed. **`CI-06l` is not weakened**: all three columns still name this one job and the release-job table is untouched. **The `LT-01` key is the request's OWN stored key under `payouts.ts`'s prefix**, so three doors mint one posting, and the suite BINDS the retyped constant by reading both route files as text because `apps/worker` cannot import them. **ADR-157's range term, its `IS NULL` term and its ROW lock used as ruled**, one instant per run, the precondition re-read under the lock, and no advisory lock reachable from the port. **ONE TRANSACTION PER ROW because `FM-M5-13` is this job stalling.** **Two gaps reported rather than reached around**: `SystemReason` has no word for an hourly sweep and `P5-a` owns that file, and nothing writes an event yet so the sink is a port. **Eight mutations seeded and all eight caught.** 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, 177 files / 3,310 passed / 6 skipped, `falsify` clean |
| [2026-08-27 - Session 292](2026-08-27-session-292.md) | **[ADR-165](../decisions/ADR-165.md), `status: proposed`, UNSIGNED. `DB_ADMITTED` holds two names and the second is `@merit/worker`.** **THE PRICE [ADR-096](../decisions/ADR-096.md) SECTION 4 DESIGNED THE QUIET MANIFEST LINE TO COST WAS PAID AND WATCHED**: with the manifest line added and the list still at one name, `repo-invariants.mjs` exits 1 at **11 of 12** with `FAIL RI-08` naming `apps/worker/package.json`, and with the name admitted it is 12 of 12 with the computed title reading **2**. **THE REASON EXISTED BEFORE THE DOOR DID AND IT WAS WRITTEN FOR THIS CALLER**: `systemDb`'s own docstring justifies `'nightly-batch'` by citing this deployable's [`batch/ports.ts`](../../apps/worker/src/batch/ports.ts), so since [ADR-084](../decisions/ADR-084.md) the accessor has carried a reason whose only legitimate holder could not name the accessor. **`SystemReason` GAINED NO MEMBER.** **ONE DOOR, AND IT IS A MEASUREMENT RATHER THAN A TASTE**: `batch(fn)` over `systemDb('nightly-batch')` with no `scoped`, no `firm` and **no reason parameter**, so `'operator-console'` is unreachable by construction; the four tables [P7 section 3.2](../plans/P7-risk-and-abuse.md) names span three scope classes and **`identity_links` is `pair`**, which [`scope.ts`](../../packages/db/src/scope.ts) calls *"REFUSED BY BOTH NARROW DOORS ... the only door left"*, while `detector_runs` is `firm`, so no two doors cover the set and one does. **FIVE DECLINES NOW STAND BEHIND THIS LIST**, sessions 218 and 220 in `apps/api` and sessions 147 and 222 plus `batch/ports.ts` here, every one naming the same remedy and none taking it. **IT UNBLOCKS NO WAVE-2 SLICE AND SAYS SO**: [ADR-157](../decisions/ADR-157.md) refused the aggregate and [ADR-125](../decisions/ADR-125.md)'s fixture format still blocks all three done-gates, so of P7's three preconditions this closes the third only. **`RI-08`'s LIST IS AMENDED AND NO INVARIANT IS MINTED; `RI-13` STAYS FREE**, the reservation's own *"`RI-12` stays free"* having gone stale the afternoon `RI-12` landed. **FIVE FINDINGS REPORTED AND NOT REPAIRED**, the load-bearing one being that **`RI-08`'s own `covers` line says the admission list *"is EMPTY"* and has been wrong since [ADR-120](../decisions/ADR-120.md)** -- the second hand-maintained claim about this list, the first uncaught for the whole life of the one-name list. **The acquisition-point assertion was written as a SUBSTRING test first and the tree refuted it**, failing on the file whose header names the accessor in order to say it does not import it. **Four seeds watched failing**, each reverted from a byte-identical copy. 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, 183 files / 3,521 passed / 6 skipped, `falsify` clean. **`origin/main` was merged in FIVE times while this branch was open** and the suite figure is mostly not this session's and moves with every merge: against the first merged base with this session's one new file removed the tree reported 176 files / 3,260 passed, so **this session contributes one file and six cases** and the rest arrived on `main`. **No migration, no `SD-nn`, no `packages/db` change, no route, no gate, no `SystemReason` member, and `apps/worker/src/index.ts` untouched.** |
| [2026-08-27 - Session 293](2026-08-27-session-293.md) | **[ADR-169](../decisions/ADR-169.md) proposed, `status: proposed`, approval line UNSIGNED, MONEY PATH, `E2` READ OWED ON EVERY LINE OF [`0051`](../../packages/db/migrations/0051_payout_destinations.sql), NOT MERGED.** `OI-06 (payout destinations)`, open and deliberately undecided since 2026-08-15, is CLOSED. **`C-11` HAS BEEN A SHIPPED CONTROL WITH NO INPUT SINCE THE SCHEMA WAS WRITTEN**: [SECURITY section 4](../architecture/SECURITY.md) item 1 starts a 48 hour window, `C-24` extends it to affiliates, [STATE_MACHINES](../architecture/STATE_MACHINES.md) draws `requested --> cooling` on `G-DESTINATION-COOLING`, `0001` carries the `cooling` enum value and `0011` carries a partial index that scans for it -- and `destination_ref` on `payout_transfers` (`0010:243`) and `wallet_withdrawals` (`0011:132`) is the destination OF A TRANSFER, the only two occurrences of that column name in all fifty merged migrations. **THE RECOMMENDATION IS TAKEN ON ITS KEY AND ITS TWO COLUMNS AND AMENDED ON THE ONE WORD IT DOES NOT CONTAIN, WHICH IS `NOT NULL`**: read literally as `cooling_until timestamptz NULL`, an `INSERT` that omits the column writes a destination that is USABLE THE INSTANT IT EXISTS, because the gate reads `cooling_until > now()` and a NULL compares to nothing -- **a fail-OPEN on the row written at the exact moment `C-11` exists to slow down**, which is [SECURITY](../architecture/SECURITY.md) item 9's own *"columns that record a control are not the control"* applied BEFORE rather than after, and which settles the first-destination question fail-closed as a side effect, since on an identity that has never withdrawn there is nothing to change from. **THE KEY SURVIVED CONTACT FOR A MEASURED REASON RATHER THAN BY DEFERENCE, AND THAT IS THE SESSION'S FINDING**: one row per pair keeps the gate a keyed lookup, and the append-only shape that *"records that a destination changed or when"* actually suggests puts the read on `max(cooling_until)` -- **the scalar aggregate [ADR-157](../decisions/ADR-157.md) REFUSED two slices earlier in this same wave** -- so an event log would have left `P5-h`'s gate unreachable through `scopedDb` and blocked the slice this table exists to unblock. **`UPDATE` SURVIVES AND `PAYOUT-DEST-C1` BOUNDS IT** on `0033`'s own ground that a revoke cannot tell a lengthening from a shortening: `cooling_until` is forward-only with equality PERMITTED so a re-registration inside a longer window is a no-op rather than a read-then-decide race, and `identity_id`, `destination_ref` and `first_seen_at` are immutable. **THE GRANTS ARE A FOURTH SHAPE**: `0032`/`0039`/`0049` revoke `UPDATE, DELETE` for append-only and `0050` revokes `ALL` for reading, and this one revokes **`DELETE` ALONE**, because the row is the memory of the window and a `DELETE` then re-`INSERT` is a fresh window with no trace. **PROVED IN BOTH DIRECTIONS against PostgreSQL 16.13 and not asserted**: with the `REVOKE` line removed `merit_app` holds all four verbs and removes a running cooling window (`DELETE 1`); as written, `permission denied`. **SEVEN REJECTIONS SEEDED AND ALL SEVEN WATCHED FIRING**, with the two legitimate writes accepted. **`cooling_until` IS STORED RATHER THAN COMPUTED AND THAT IS THE ONE DERIVABLE-LOOKING VALUE THIS TABLE ACCEPTS**: a cooling window is a PROMISE MADE AT A MOMENT, so a duration moving from 48 to 72 hours would retroactively re-cool every destination ever registered, and `0049`'s `GENERATED` remedy is structurally unavailable for [session 281](2026-08-27-session-281.md)'s reason -- a generated column is row-local and the duration is config's ([ADR-037](../decisions/ADR-037.md)). **`0051` WAS DRAWN BACK FROM THE POOL AND THE SEQUENCE GAINED NO HOLE**, the first draw the pool has ever taken; it is empty again and `0052` is next free. **THE ROW AS RAISED IS NARROWER THAN THE GAP AND THAT HALF IS REPORTED OPEN**: `OI-06` names *"the affiliate rail under C-24"* as a reader and [session 162](2026-08-24-session-162.md) measured that `affiliates` carries NO DESTINATION COLUMN AT ALL, so `INV-M8-11` now has a registry to write into and still nothing that says what an affiliate's current destination is. **TWO STALE COUNTS IN [`schema.ts`](../../packages/db/src/schema.ts)'s HEADER ARE CORRECTED RATHER THAN INCREMENTED AND NEITHER WAS THIS SESSION'S**: it read *"105 TABLES OF 111"* and *"THE ONE HUNDRED AND FOUR"* while the migration set had been at 112 since `0049` and 113 since `0050`. **NO `SD-nn`, no `SqlExecutorReason` member, no `SystemReason` member, no `pg` import, no cast past a key type, no route, no `API_CONTRACT` edit, no edit to any merged migration and no edit to [`scoped-db.ts`](../../packages/db/src/scoped-db.ts).** Install 51 of 51 forward-only from empty under `ON_ERROR_STOP` with the re-apply failing as it must; tables 113 to **114**, indexes 396 to **397**, `CHECK`s 479 to **481**, all constraints 777 to **781**, triggers 20 to **21**, every figure read from `pg_tables`, `pg_indexes`, `pg_constraint` and `pg_trigger`. Registry 105 declared tables to **106**, 61 served by `scopedDb` to **62**, 41 `firm` and 3 `pair` unmoved. Gates **33 of 33**, invariants **12 of 12**, `typecheck` exit 0, `lint` exit 0, `format:check` clean, suite **183 files / 3,523 passed / 6 skipped** against a reproduced baseline of 3,521, `falsify.mjs` exit 0 at 33 gates, 80 scope and 10 loader cases, leaving the tree clean |
| [2026-08-27 - Session 294](2026-08-27-session-294.md) | **[`apps/api/src/events.ts`](../../apps/api/src/events.ts) and [`apps/admin/src/feed.ts`](../../apps/admin/src/feed.ts) (both new) build [P5](../plans/P5-payouts-and-wallet.md) section 8's `P5-n`, the event feed and the writer underneath it, with 136 new tests and SIX OF SEVEN HALVES LANDED.** **THE DISPATCH'S FIRST FACTUAL CLAIM IS FALSE AND IT IS THE ONE THE SLICE TURNS ON**: *"`events` is registered in `schema.ts` and `scope.ts` as of session 274"*. It is not. [Session 274](2026-08-27-session-274.md) registered `payment_disputes`, found `wallet_spend_limits` already registered, and REFUSED `events` in writing -- *"`P5-n` IS NOT UNBLOCKED"* -- because all five scope classes are either refused by a mechanical assertion or silently lossy and **what the table needs is a SIXTH CLASS and an ADR**. No ADR number was allocated here and `packages/db` is outside the fence, **so `EventWriter.insert` is a port, the live adapter is NOT written, NOT faked and NOT reached around**, which is [P5](../plans/P5-payouts-and-wallet.md) section 11 rule 10. **`payout.freeze_expiring` IS THAT BLOCKER IN ONE ROW AND IT IS IN THE TABLE**: its catalogue payload carries NEITHER tenancy column, so a rule naming either drops it, and it is a counterexample any sixth class must answer. **The sink SATISFIES `apps/worker/src/sweeps/ports.ts`' `ExpiryEventPort` rather than standing beside it**, asserted by an assignment that does not compile if either parameter was narrowed; the declared shape was found CORRECT and thin in two named places (`occurredAt`, `correlationId`) and neither is a defect. **`EVENT_CATALOGUE`'s eight names are BOUND to [EVENTS](../architecture/EVENTS.md) by reading it as text** -- every name a table row, every field the producer reads a field of that row, every producer string the catalogue's own cell -- which is `ADR-159` F-2's shape one level down and **claims no `CI-06` letter**. **`ACTOR_KINDS` is read out of `0017`'s CHECK and every `subject_kind` is asserted to be the singular of a table `schema.ts` registers.** **[M06](../plans/M06-admin-ops-console.md)'s fifth surface is ruled by `INV-M6-10`**: an unfiltered page of `events` IS the bulk identity screen that invariant says does not exist, so the feed has two modes and **the withholding is a rule on the SHAPE OF THE KEY**, which covers `matched_identity_id` and `merged_identity_id` by construction -- the thing [`scope.ts`](../../packages/db/src/scope.ts) records as inexpressible by any scope rule, expressed at the one layer that can. **The aggregate is refused on evidence**: `shown` counts rows read, there is no `total`, and `complete` keeps it honest. **Twelve mutations seeded and twelve caught, and a thirteenth attempt that silently did not apply is reported rather than counted.** 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, **185 files / 3,657 passed / 6 skipped** against a `main` baseline of 183 / 3,521 / 6 that reproduced exactly first. **`page.ts`, `index.ts` and every file outside the fence untouched** |
| [2026-08-27 - Session 295](2026-08-27-session-295.md) | **[`apps/api/src/routes/checkout.ts`](../../apps/api/src/routes/checkout.ts) gains the `payment_method = 'wallet'` arm and posts `LT-08` in the purchase transaction, which is [P5](../plans/P5-payouts-and-wallet.md) section 8's `P5-i` and the ONLY P5 slice that edits an existing route module. MONEY PATH, `E2` READ OWED, DO NOT MERGE UNREAD.** No `packages/db` change, no `SqlExecutorReason` member, no `SystemReason` member, no `pg` import, no advisory lock, no ADR number, no migration number, and no [API_CONTRACT](../architecture/API_CONTRACT.md) edit. **THE STALE PREMISE WAS CHECKED FIRST AND IT IS STALE**: [M20:62](../plans/M20-wallet.md) reads `= 'active'` and [ADR-075](../decisions/ADR-075.md) is `accepted`, so no `INV-M20-06` contradiction was treated as live. **THE ORDERING RULE WAS ALREADY RIGHT AND NOTHING WAS MOVED**: `gateIdentity` is the one authorization decision, it is already the first thing inside both handlers' transactions, and the branch was added strictly below it; `INV-M20-06` is a DIFFERENT refusal against the same subject at a different moment and sits inside the arm, **including its `identities.status` member even though `gateIdentity` has already refused that case**, because a set that drops a member another control covers loses it the day that control moves. **THE ARM FOLLOWS [M20](../plans/M20-wallet.md) SECTION 3.2 LITERALLY, INCLUDING THAT THE POSITION CHECK PRECEDES THE VELOCITY DELAY.** `INV-M20-07` DELAYS with a `Retry-After` COMPUTED by walking the window's debits oldest first, and **`daily_cents: 0` is the one case with no such instant, where the header is omitted rather than fabricated**. **FIVE THINGS REPORTED RATHER THAN REACHED AROUND**: `purchases.psp` and `psp_reference` are both `NOT NULL` and `SD-M3-06` relaxed neither, so **a wallet purchase row is unwritable without naming a processor that was never called**; `wallet_entries.provenance` has no member describing a debit ([ADR-158](../decisions/ADR-158.md) finding 3); **`payment_method` is not on API_CONTRACT's `CheckoutRequest` and `CheckoutResponse` requires two fields a wallet purchase does not have**, so the code is a STRICT SUPERSET no contract-conformant request can reach; the impersonation refusal has no session type to refuse on; and **`mixed` needs a ruling and the number was NOT taken**, refused with a stated reason AFTER `gateIdentity` rather than at the wire. **A DEFECT IN THE FILE IS REPORTED WITH ITS PATCH AND NOT TAKEN**: `gateIdentity` answers `forbidden` 403 where the contract defines `identity_restricted` 422 *"on EVERY payment method"* and [`payouts.ts`](../../apps/api/src/routes/payouts.ts) already answers it. **EIGHT MUTATIONS SEEDED AND ALL EIGHT CAUGHT**, each reverted from a byte-identical copy; the load-bearing one is that **with the refusal moved inside the wallet arm the `wallet` cases stay GREEN** and eight others turn red, which is [M03](../plans/M03-billing-checkout.md) section 3.5.1's table reproduced. **Three of the eight turn exactly one assertion red and that is recorded as thin.** 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, 185 files / 3,693 passed / 6 skipped, `falsify` clean |
| [2026-08-27 - Session 296](2026-08-27-session-296.md) | **`P-M6-07` LANDS ON THE PAGE AND THE FLOAT LANDS BESIDE IT. No ADR, no migration, no `SD-nn`, no `packages/db` change, no contract change, no route, no gate, and `0049`, `admin-reads.ts`, `admin-payouts.ts` and `liability_snapshots` all untouched.** **THE PANEL'S STATED BLOCKER HAD GONE FALSE**: the `PENDING` row read *"the columns do not exist"*, [`0049`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql) landed them under [ADR-128](../decisions/ADR-128.md) and closed `OI-01`, and what is actually missing is a PRODUCER, which that file's own header states as a deliberate omission. **FLOAT ENTERS THE DENOMINATOR AS EXPOSURE AND NEVER THE NUMERATOR AS RESERVE**, and [`liability.ts`](../../apps/admin/src/liability.ts) holds no field, argument or local carrying `reserve + float` except the one inside the assertion that refuses it. **`rcr_bp` IS READ AND NOT RECOMPUTED**, on `0049`'s founder-read item 1; the recomputation survives only as a coherence check, in `bigint` reproducing Postgres' `(reserve_cents * 10000) / NULLIF(cvar99_cents, 0)` exactly, and it catches BOTH shapes of the defect. **THE FIXTURE FLIPS THE BREAKER RATHER THAN MOVING A NUMBER**: `GS-229`'s book is 8,000bp ARMED from reserve alone and 14,000bp NOT ARMED with the float folded in, which is `AS-M20-08` in two lines. **ASSERTED IN THE NEGATIVE OVER THE RENDERED BYTES**, and the SECOND SEED PROVED WHY THAT IS NOT THE SAME CHECK AS THE UNIT ONE: the ratio line computed from float plus reserve was **caught only in the bytes** and every unit case passed, because the leak was in a rendered line and not in any `Figure`. **Three seeds watched failing, each reverted from a byte-identical copy with the hash checked both ways.** **THE TWO SCAN SCOPES DIFFER DELIBERATELY**: forbidden CENTS in the `P-M6-07` panels only, because money can coincide; forbidden RATIO over the whole page, because a bp token collides with nothing. **Both skipped at zero float, and the skip is stated.** **THE API HALF WAS MEASURED BEFORE A ROUTE WAS WRITTEN AND NO ROUTE WAS WRITTEN**: `GET /admin/liability` serves `P-M6-07` IN FULL and **cannot serve `P-M6-01` at all**, because `LiabilityResponse` carries `open_liability_cents` and no `wallet_balances_cents`, which hands a client one component under a name that reads as the panel and is what `INV-M6-11` forbids; `bounded_near_term_cents` and `remaining_ladder_exposure_cents` are absent too. **`admin-liability.ts` DECLINED ON THAT MEASUREMENT**: the path is taken, `compose` refuses a duplicate `METHOD /path`, and a new path is a new `API_CONTRACT` row, which is an ADR `P5-c` holds and this session was allocated no number for. **The remedy is three fields on a frozen response shape and not a route.** **THREE MORE FINDINGS, EACH OUTSIDE THE FENCE**: none of the eight new exports reaches [`index.ts`](../../apps/admin/src/index.ts)'s explicit barrel, `GET /admin/wallet/reconciliation` is registered by no route module so `P-M6-07`'s SECOND ratio renders absent, and **"float" in `admin-reads.ts` means the NUMBER TYPE in all three of its occurrences**. **`ADMIN_ORIGIN` appears nowhere in this diff and neither does any host literal**, both greps pasted empty. 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, 183 files / 3,559 passed / 6 skipped, `falsify` clean. **`apps/admin` goes 116 to 152 cases, +36 and no new file**, which is this session's attributed contribution. |
| [2026-08-27 - Session 297](2026-08-27-session-297.md) | **[`apps/api/src/routes/certificates.ts`](../../apps/api/src/routes/certificates.ts) and [`apps/api/test/certificates.test.ts`](../../apps/api/test/certificates.test.ts) (both new) transcribe [API_CONTRACT](../architecture/API_CONTRACT.md) section 6.3's two rows, `GET /certificates` and `GET /certificates/:code/image.png`.** [ADR-168](../decisions/ADR-168.md) admitted them and its foreclosure 8 refused to build them; this is that slice. **No ADR number, no migration number, no `SD-nn`, no `packages/db` change, no `API_CONTRACT` change, no other route module touched, and the route registry needed no edit.** **THE REGISTRY CLAIM WAS VERIFIED RATHER THAN ASSUMED**: `discoverRouteModules()` then `buildServer()` on both surfaces, and adding one file moved `CompositionReport.registered` on the public surface by exactly the two rows while the operator surface moved them into `withheld`, which is where the operator 404 comes from. **THE `.png` IS SECTION 1'S FIRST NON-JSON SUCCESS RESPONSE AND THE HEADER STATES WHAT SURVIVES AND WHAT CANNOT**: the error model, the base path, the surface partition and the rate-limit row survive; **the response-shape allowlist cannot, because there is no projection inside a PNG**, so on that row the disclosure boundary lives in a renderer that is not in this repository and the suite asserts the metadata and the passed-through body and says it cannot assert the pixels. **THE DISCLOSURE BOUNDARY IS ASSERTED IN THE NEGATIVE OVER POISONED SEEDS**: every row carries an identity uuid, an email, a display name, a cumulative total, a lifetime figure and `revoked_reason`'s internal free text, half as columns and half inside the `jsonb` claim, and the case reads the SERIALIZED body. **`narrowClaims` IS AN ALLOWLIST OVER A `jsonb` COLUMN**, four keys by name, and it is the one place where getting it wrong is silent: `certificates.claims` is `jsonb NOT NULL` and the database constrains nothing about its keys. **`revoked_reason` HAS NO FIELD ON THE ROW TYPE**, which is stronger than an allowlist that declines to mention it. **`state` IS DERIVED AND THE UNION IS THREE MEMBERS**; `withheld` is not typed. **A DEFERRED ROW'S TOKEN IS WITHHELD STRUCTURALLY**: there is no path through `projectCertificate` on which a deferred row reaches the link signer. **A DEFERRED CODE ANSWERS 404 ON THE WIRE AND IS RECORDED AS `deferred` IN THE TABLE**, which is what `certificate_verifications.result`'s fourth member buys. **NO TIMING DEFENCE AND NO CLAIM OF ONE**, per `INV-M11-05`'s verify-only clause. **THE IMAGE ROW CANNOT REACH THE DATABASE FROM THIS DEPLOYABLE AT ALL AND THE REASON IS STRUCTURAL**: it is unauthenticated so `db.scoped` has no identity, and `certificates` is `owned` so `db.firm` refuses it at compile time; the door that would serve it takes a `SystemReason` `apps/api` deliberately does not open. **Both ports are unwired and `start.ts` is outside the fence**, so both rows fail closed at 503 today. **Four mutations seeded and all four caught.** 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, 184 files / 3,574 passed / 6 skipped, `falsify` clean. **The portal's `(purchases)` segment can be wired for the list and NOT for the image.** |
| [2026-08-27 - Session 298](2026-08-27-session-298.md) | **[`apps/api/src/routes/admin-wallet.ts`](../../apps/api/src/routes/admin-wallet.ts) (new) registers all three of [API_CONTRACT](../architecture/API_CONTRACT.md) section 8's wallet rows. MONEY PATH BY CONTENT, `E2` READ OWED, DO NOT MERGE UNREAD.** No ADR number, no migration number, no `packages/db` change, no contract change, and the route registry needed no edit. **Measured from `CompositionReport` and not from a grep, before and after**: none of the three paths was in `registered` NOR `withheld` on either surface beforehand; afterwards `operator` registers all three (21 to 24) and `public` withholds all three (20 to 23), with `public` registered unchanged at 42. **THE HEADLINE IS THAT `0038_account_adjustments.sql` IS THE BUILT DOOR FOR AN ADMIN WALLET CORRECTION AND [ADR-158](../decisions/ADR-158.md) NEVER READ IT.** That entry wrote the `correct` row from `0011` alone across eighteen findings, and `0038`'s `assert_adjustment_wallet_entry_matches` requires `wallet_entries.reference_id = account_adjustments.id` while the contract says `corrects_entry_id` *"Becomes `reference_id`"* -- **and `reference_id` is `uuid NOT NULL` while `wallet_entries.id` is `bigint`, so NO COLUMN ANYWHERE HOLDS WHICH ENTRY A CORRECTION CORRECTS.** Three more disagreements behind it: a correcting DEBIT is unwritable unless it exactly reverses a prior adjustment credit (`account_adjustments_debit_is_a_reversal` plus `assert_adjustment_reversal_is_sound`), dual control is a `dual_control_approvals` row plus a `dual_control_threshold_cents` column rather than the contract's `second_approver` string, and `reason_code` is `NOT NULL` over a closed three-member vocabulary with no wire field. **`GET /admin/wallet/reconciliation` IS [ADR-157](../decisions/ADR-157.md) CLAUSE 6 ARRIVING WHERE THAT ENTRY PREDICTED IT WOULD**: `ledger_position_cents` is a JOIN, `float.total_cents` and `identities_checked` are AGGREGATES over every identity, and both are refused. **Reported and NOT widened**: no `SqlExecutorReason` member, no `SystemReason` member, no `pg` import, no cast past a key type, no ledger account named. **`POST /admin/wallet/:identityId/spend-limit` IS COMPLETE**, an APPEND at `(identity_id, effective_from)` with `set_by` from the session and never from the body, the precondition re-read under the lock and `admin_actions.before` carrying the limit the append supersedes. **Every transaction is `lockAt('identities')` first**, which is `INV-M20-01`'s per-identity lock at the authority this module holds and NOT the advisory lock the invariant's prose names. **`INV-M5-23`'s shape is structural and the suite asserts the recorder is EMPTY on both refusals** rather than reading a status code. **`insufficient_funds` IS NOT IN SECTION 2's CLOSED CODE TABLE and 422 is an assumption**, held as one constant so a ruling moves it in one place. **Eleven mutations seeded and all eleven caught**, one of them rewritten after it went green because the first version opened a transaction that wrote nothing. 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, 184 files / 3,593 passed / 6 skipped, `falsify` clean. **The diff grep for anything domain-shaped is EMPTY and is pasted in the pull request.** |
| [2026-08-27 - Session 299](2026-08-27-session-299.md) | **[`apps/worker/src/live/ingest.ts`](../../apps/worker/src/live/ingest.ts) and [`ports.ts`](../../apps/worker/src/live/ports.ts) (both new) build [P6](../plans/P6-live-tier.md) section 8's `P6-f`, the streaming ingest: it consumes `P6-a`'s `streamLive`, writes `P6-c`'s `live_account_state` and NOTHING ELSE, and asserts that mechanically.** No ADR number, no migration number, no `SD-nn`, no `packages/db`, `packages/rithmic` or `apps/api` change, and no contract change. **THE PREDICATE [`0050`](../../packages/db/migrations/0050_live_cache_and_role.sql) RECORDED AS OWED TO THIS SLICE IS WRONG IN BOTH DIRECTIONS AND THE SAME MIGRATION SAYS WHY.** Its sketch, `excluded.sequence > live_account_state.sequence`, refuses today's first tick because `sequence` is 1-based PER ACCOUNT PER DAY, so a row left over at 400 is never overwritten by today's 1 and the surface freezes at yesterday's number, which is [ADR-020](../decisions/ADR-020.md) rule 3's named failure; and it ADMITS yesterday's straggler, which rewinds the row by a whole session. `0050`'s own `trading_day` comment supplies the correction -- *"a sequence without its day is not an ordinal at all"* -- so **the ordinal is the pair `(trading_day, sequence)`, lexicographic**, and the shipped `WHERE` carries `0050`'s quoted fragment AND the trading-day half. **`INV-M2-14` IS ASSERTED AND NOT COMMENTED**: the four table names are DERIVED from `0050`'s own closing `REVOKE`, each is asserted PRESENT in the module headers and ABSENT from the same files with every comment stripped, in both the snake-case and the Drizzle camelCase spelling, and the import graph is walked and shown CLOSED at two nodes that import nothing bare. **EIGHT MUTATIONS SEEDED AND ALL EIGHT CAUGHT.** **THE EXPECTATION ROW IS A GRANT ARGUMENT RATHER THAN A MISSING ADAPTER AND IT IS REPORTED, NOT REACHED FOR**: `feed.*` belongs to the expectation sweep ([ADR-161](../decisions/ADR-161.md) clause 7), the sweep writes `events` so it runs as `merit_app`, `merit_app` holds NOTHING on the live cache, so **the sweep cannot read the cache to see the absence it alarms on**, and granting it `SELECT` is `0050`'s `F1` and is refused. The row's shape, its grain and the reason `merit_app` reading it is a different question from `F1` are written where the migration session reads them. **The upsert is a string because ADR-112's handle has no upsert and `packages/db` is outside this fence**, and every column of it is bound to `0050`'s `CREATE TABLE`: never the GENERATED movement, never `indicative` or `created_at`, none of the three refused columns, and `as_of_instant` set on the UPDATE arm because a column default does not fire there. **The queue is one pending tick per account, which is a bound derived from the account count rather than a depth cap nobody measured**, and coalescing is observationally equivalent on the cache, so gap detection runs at ENQUEUE. [CRON_INVENTORY](../ops/runbooks/CRON_INVENTORY.md) takes the feed-health row and no other line. 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, 184 files / 3,581 passed / 6 skipped, `falsify` clean |
| [2026-08-27 - Session 300](2026-08-27-session-300.md) | **[`apps/worker/src/detectors/runner.ts`](../../apps/worker/src/detectors/runner.ts), [`ports.ts`](../../apps/worker/src/detectors/ports.ts) and [`canary.ts`](../../apps/worker/src/detectors/canary.ts) (all three new) build [P7](../plans/P7-risk-and-abuse.md) section 8's `P7-e`, `P7`'s FIRST DONE-GATE**: `detector_runs` gains a producer, every run seeds synthetic subjects, asserts it found them, and a run that finds fewer than expected is `degraded` and emits `detector.run_degraded` (`GS-122`, `INV-M7-07`, `SD-M7-01`). No ADR number, no migration number, no `packages/db` change, no `apps/api` change, no contract change, no `SystemReason` member, no `SqlExecutorReason` member, and [`src/db.ts`](../../apps/worker/src/db.ts) read in full and NOT touched. **THE `is_synthetic` COLUMN `AS-M7-05` NAMES DOES NOT EXIST IN ANY MIGRATION** -- [M02](../plans/M02-rithmic-bridge.md) `OQ-M2-01` PROPOSES it and is open -- so the reading taken is the strictly stronger one: **a canary is never written at all**, minted in memory per run and discarded, which makes *"excluded from every aggregate"* true of the queries nobody has written yet rather than of the ones somebody audited. **THE HALF THAT READING DOES NOT BUY IS PINNED RATHER THAN HIDDEN**: a run whose read returns nothing still finds every canary and reports `ok` at `rows_scanned: 0`, which is `AS-M7-05`'s own opening sentence still reachable, and closing it needs the marker column, a migration and a ruling. **BOTH IMPLEMENTATION NOTES ARE ASSERTIONS**: note 1 is stated over the run's WRITES because a run's aggregates are its writes, and note 2 is a nonce guard a detector cannot satisfy by being careful. **THE CHEAP IMPLEMENTATION THE DDL CANNOT SEE IS REFUSED IN CODE**: `detector_runs_synthetics_match_status` is satisfied at `0 >= 0`, so a detector seeding NO canaries reports `ok` forever and the runner refuses one. **NO PATH TO `enforced` AND IT IS THE TYPE THAT STOPS IT** ([ADR-155](../decisions/ADR-155.md), `INV-M7-02`): `DetectorTx` has no addressed write and `DetectorFinding` has no `status` field, so `enforced` is a word with nowhere to go. **`INV-M7-04` IS WHY THE RUN READS THE REGISTRY BEFORE IT READS A FILL**, and `detector_definitions_current_idx` is **NOT UNIQUE**, so two current rows are refused rather than resolved by a coin toss. **[ADR-157](../decisions/ADR-157.md) SECTION 5's REFUSAL IS DESIGNED FOR**: windows through `rowsWhere`, the join in the runner, and its named cost reported per stream. **Ten seeded cheap implementations watched failing**, each reverted from a byte-identical copy checked by digest. **Four findings reported and not repaired**, the load-bearing one being that `detector.run_degraded` has **no row in [EVENTS](../architecture/EVENTS.md)** and the suite asserts the absence so the day it lands the payload is re-derived from the catalogue. 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, 184 files / 3,595 passed / 6 skipped, `falsify` clean. **This session contributes exactly one test file and 72 cases**, measured by removing it (183 files / 3,523). |
| [2026-08-27 - Session 301](2026-08-27-session-301.md) | **[ADR-170](../decisions/ADR-170.md), `status: proposed`, UNSIGNED. [ADR-153](../decisions/ADR-153.md)'s defect found a THIRD time and closed on its own subject.** `API_CONTRACT` has been shipping `verify_url: string; // public verification page` while `grep -n '/verify/'` over that document **exits 1 with no output**; the dispatch's claim was reproduced at source before anything was ruled. **TWO ROWS LAND AND TWO ARE NAMED AS OWED**: `GET /verify/:code` in section 6.3 and `POST /admin/certificates/:id/revoke` in section 8; `PATCH /me/leaderboard` and `GET /public/leaderboard` REFUSED because **`OQ-M11-03` IS the public row's field set and is open**, and `INV-M11-10`'s reversibility needs historical entries that `grep -rl 'leaderboard' packages/db/migrations/` shows no table holds. **THE LOAD-BEARING CLAUSE IS THAT A DEFERRED CODE ANSWERS `unknown`**, so the response union is THREE members where `certificate_verifications.result`'s CHECK is FOUR: `INV-M11-09` defers on an open severity 4+ flag so a public `deferred` would publish that an account is under risk review, and the log keeps the fourth value because a deferred hit is **a leaked token or a 128-bit guess that hit** while an unknown hit is a typo, and those are different incidents. **An allowlist decision and NOT [ADR-040](../decisions/ADR-040.md)'s defect**, which is the inverse. **`INV-M11-05`'s CONSTANT-TIME CLAUSE NAMES THIS ENDPOINT AND EQUALISING WORK CANNOT HONOUR IT**: the database's buffer cache reproduces `FM-M11-05`'s tension below any application ruling, so the mechanism is a **measured time floor** and **a floor set from a p50 is worse than no floor**. **The row has NO `validation_failed` at all**, a shape check being a faster path and an alphabet oracle at once; the SIZE channel is named and deliberately not chased. **`FM-M11-05`'s CACHE IS SCOPED OUT BY ITS OWN TEXT** (it caches *rendered bytes*) and `FM-M11-02`'s recovery path required `no-store` before the clock did. **THE REVOKE ROW WAS WRITTEN FROM `admin_actions_on_behalf_matches_initiative` AND THAT FOUND THE BEST FINDING**: `initiative` is DERIVED from `revocation_class` and is **not a request field**, because an endpoint taking it would let an operator record an enforcement as something the trader asked for; and `:id` is the uuid because `subject_id` is `uuid NOT NULL` and `code` is rotatable. **A DEFERRED CERTIFICATE CANNOT BE REVOKED**, the deferred-plus-revoked encoding of `withheld` failing on `AS-M11-05`'s *"the claim stands"*, so **no endpoint and no column reaches a deferred certificate with an enforcement today** and `revoked --> issued` is drawn and served by nothing. **SECTION 12 DELIBERATELY UNTOUCHED**, the four certificate endpoints' matrix rows named as ONE owed slice, the verify assertion being that **the response is identical with and without a session**. **[ADR-168](../decisions/ADR-168.md)'s OWN CLAUSE NOW PRINTS `3` AND IS RECORDED RATHER THAN REPAIRED**: another session's entry is not edited to make this one's diff quiet. **NO ROUTE REGISTERED**; from the composed server, 62 registered / 64 contract headings / **10 rows registered by nothing, up from 8**. 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, 183 files / 3,521 passed / 6 skipped, and 183 / 3,523 / 6 after `origin/main` was merged in, `falsify` clean. **No migration, no `SD-nn`, no `packages/db` change, no gate, no section renumbered, `M11`, `0020`, `0025` and `apps/api` untouched.** |
| [2026-08-27 - Session 302](2026-08-27-session-302.md) | **`4 of 20` becomes `6 of 20`, and the deliverable is the CHECK rather than the two lines.** The dispatch's finding reproduced exactly before anything changed: `apps/api` declares **20** backend setters across `routes/*.ts` and `start.ts` called **4**, so sixteen route families answered 503 in every deployment while 188 files / 3,925 tests stayed green. **THE COUNT OF ADAPTERS IS THE ANSWER TO "IS THIS MECHANICAL"**: of the sixteen unwired ports, **exactly ONE had a `databaseX` factory already written** (`databaseWalletBackend`), one had a factory that **cannot be called** (`databaseCertificateBackend` needs a signer with no supplier), and **fourteen had none**. Wiring is a line; the other fourteen are slices. **TWO PORTS WIRED**: `useWalletBackend` over the existing adapter, and `setMethodDefinitionSource` over `databaseMethodDefinitions`, built here against the FIRM door with every column narrowed rather than cast. **FOURTEEN NAMED WITH WHAT EACH WAITS ON**, five of them on `systemDb('operator-console')`, which is **a ruling and not an adapter**: `SystemReason` is `'nightly-batch' \| 'operator-console'`, [ADR-165](../decisions/ADR-165.md) ruled it gains no member, and `src/db.ts` was not touched. **THE MONEY PORTS ARE NOT "READY TO WIRE" AND THAT IS THE SESSION'S SECOND FINDING**: `PayoutTx.ledger` is a `LedgerTx` and is **NOT NULLABLE**, so no live deployment can supply a `PayoutBackend` at all, and `CheckoutTx.insertAttribution` writes a `pair` table no `packages/db` authority admits a handler writing. **A THIRD FINDING THE DISPATCH'S GREP COULD NOT SEE: three of the sixteen already hold their PRODUCTION value at module scope** (`affiliate.ts:478`, `kyc.ts:284`, `checkout.ts:1005`), so calling their setter would install what is already installed, raise the count and serve nothing. **`apps/api/test/wiring.test.ts` reads `start.ts` AS TEXT** and fails on an unaccounted port, on a blocked entry that is also wired, on an orphaned entry, on an unchecked reason, and on **an adapter written and never installed** - the half that actually happened. **Watched failing three ways**, including the no-op-call move. 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, **189 files / 3,941 passed / 6 skipped**, `falsify` clean. **No ADR number, no migration number, no `packages/db` change, no handler logic changed, `src/db.ts` untouched.** |
| [2026-08-27 - Session 303](2026-08-27-session-303.md) | **[`apps/api/src/routes/wallet-withdrawals.ts`](../../apps/api/src/routes/wallet-withdrawals.ts) serves [API_CONTRACT](../architecture/API_CONTRACT.md) section 6.2's `POST /wallet/withdrawals`, the LAST UNREGISTERED ROW OF THE CONTRACT'S TRADER SURFACE. [P5](../plans/P5-payouts-and-wallet.md) section 8's `P5-h`. MONEY PATH, `E2` READ OWED, DO NOT MERGE UNREAD.** **THE FINDING IS A DISAGREEMENT BETWEEN TWO APPROVED DOCUMENTS AND IT NEEDS A RULING**: [M05](../plans/M05-payout-system.md) section 1.1 ends *"post LT-06, debiting the wallet position -> status: approved"* and [P5](../plans/P5-payouts-and-wallet.md)'s own `P5-h` row ends *"then `LT-06`"*, while [API_CONTRACT](../architecture/API_CONTRACT.md) section 6.2 types the response `status: "requested" \| "cooling"` annotated *"The creation's reachable states"* and [M05](../plans/M05-payout-system.md) section 2.1 names `LT-06`'s kind **`wallet_withdrawal_approval`**. **THE CONTRACT IS TAKEN AND NO LEDGER TRANSACTION IS POSTED ANYWHERE**, on asymmetry of harm: a row at `requested` is a row a ruling can advance, and an `LT-06` under a `text NOT NULL UNIQUE` key with no named reversal is not. **`WithdrawalTx` CARRIES NO LEDGER HANDLE AT ALL** where `payouts.ts`'s `PayoutTx` does, so posting here would take a port widening a reviewer reads. **A SECOND THING IS OWED WHICHEVER WAY THAT FALLS**: nothing in this tree drives `requested --> approved` or `cooling --> approved`, and `P5-j` sweeps three FREEZE clocks and not `G-COOLING-ELAPSED`. **THE STALE `INV-M20-06` PREMISE WAS CHECKED AT SOURCE FIRST AND IT IS STALE** ([ADR-075](../decisions/ADR-075.md), `accepted`, approval GRANTED 2026-08-21), so nothing was re-ruled and the gate is `=== 'active'` on that entry's fail-closed argument. **`lockScope()` AND NOT `lockAt`, AND THE CASE IS THE REASON**: the row a concurrent second request would create does not exist, and [ADR-158](../decisions/ADR-158.md) finding 8 records `wallet_withdrawals_open_idx` as **not unique**, so without the lock both requests read an empty in-flight set and both insert. **`C-27` ALREADY REFUSES A NON-ELEVATED SESSION AND NO SECOND REFUSAL IS ADDED**, asserted by the case that drives one and finds no lock taken and no row read. **AN ABSENT `payout_destinations` ROW IS REGISTER-AND-COOL AND NEVER NOT-COOLING**, which is [ADR-169](../decisions/ADR-169.md)'s obligation on this slice by name. **FOUR THINGS REPORTED RATHER THAN INVENTED**: the name-match score has no input in this schema, P-1's `provenance_review` has none either, `insufficient_funds` is named on three endpoint rows and absent from section 2's closed code table, and the two `wallet.withdrawal_*` events are `P5-n`'s producer. **A WEAKNESS IN A LANDED CHECK IS RECORDED**: `'[]'::jsonb <> '{}'::jsonb` is TRUE, so an empty array satisfies `wallet_withdrawals_approved_has_provenance`. **ONE FENCE EXCEPTION, ARGUED**: `registry.test.ts` stripped `.ts` BEFORE sorting while `discoverRouteModules` sorts filenames, and `wallet-withdrawals` beside `wallet` is the first pair where those differ. **FIFTEEN DEFECTS SEEDED AND ALL FIFTEEN CAUGHT.** 33 of 33 gates, 12 of 12 invariants, typecheck 0, lint 0, format clean, 189 files / 3,991 passed / 6 skipped, `falsify` clean |
| [2026-08-27 - Session 304](2026-08-27-session-304.md) | **`RI-13` LANDS AND IS GREEN, and no approval line moves.** Fifty-four entries in [docs/decisions](../decisions/README.md) declare their own approval line withheld; twenty carried the marked block `**What a founder read adds and this entry cannot**` and named the judgements, and **thirty-four carried nothing of the kind**. The dispatch estimated thirty-one unsigned and twelve failing; **the real numbers are fifty-four and thirty-four**, and the difference is the finding: `PENDING, UNSIGNED` is one of several spellings, and the newest entries write `Approval line: UNSIGNED` with no `PENDING` at all. **The check keys on a MARKED BLOCK and not on a phrase list**, because a phrase list is a second copy of a convention that drifts; the block is the form thirty-two entries already used before the check existed. **A disposition is read only at the FRONT of a line**, after its markdown furniture and an optional approval label, and a withholding token is read anywhere in the approval statement: the asymmetry is in `covers` and it exists because a disposition misread out of prose about somebody else's grant drops an entry out of scope silently. **Two misreads were watched and seeded**: [ADR-111](../decisions/ADR-111.md) is SIGNED and was reported unsigned, because its approval section runs a shell block whose comments start with `#` and the section looked like it ended there; [ADR-119](../decisions/ADR-119.md) and [ADR-137](../decisions/ADR-137.md) are unsigned and fell out of scope on a `REFUSED` in a table cell and a citation of another entry's `GRANTED`. **Thirty-four founder questions are written, each derived from its own entry's sections and citing where the judgement lives**, and [ADR-153](../decisions/ADR-153.md)'s says almost nothing here is a judgement and why, which the check accepts on the same terms. **Nothing is signed, no ruling moves, no `status:` field moves, and no line of any entry moves except the block added to an entry that lacked one.** 33 of 33 gates, 13 of 13 invariants, typecheck 0, lint 0, format clean. |
| [2026-08-27 - Session 305](2026-08-27-session-305.md) | **91 of the 220 rows [ADR-125](../decisions/ADR-125.md) left on `no-fixture-format` were read row by row against the tree. ONE MOVED AND FIVE COULD NOT.** `GS-229` is `covered-elsewhere`, discharged over its own book by four `describe`s in [`liability.test.ts`](../../apps/admin/test/liability.test.ts) and three in [`page.test.ts`](../../apps/admin/test/page.test.ts), watched passing. **`GS-038`, `GS-155`, `GS-223`, `GS-274` and `GS-314` have assertions that EXECUTE AND PASS TODAY AND STAY BLOCKED**, because `CI-06/fixture-inventory` assertion 6 discharges a row only when the cited file **carries the row's id as a string**, and none of these five does; `GS-038`'s suite is titled with the row's own B4 number, `B4 #9`, and that is not the string the reader wants. **The gate is right and says so in its own text**, that it is a STATIC PARSE which cannot see an outcome; the repair per row is ONE COMMENT in a test file, outside a fence drawn at `docs/testing/golden-scenarios/**`. **`GS-229` moved for a reason that has nothing to do with its merits**: its suite happens to carry the string. **THE SECOND FINDING IS THAT THE QUEUE IS FOUR DIFFERENT THINGS**: rows one comment from discharge; rows where the registry and the tree describe DIFFERENT behaviour, where writing the fixture first picks a winner by accident (`GS-097` answers 400 where the row pins `conflict`; `GS-099`'s receiver DEFERS under `FM-M3-03` where the row pins rejected-and-alarmed; `GS-220` pins publish validation FAILING and the implementation adds the trigger unconfigurably; `GS-136` would have to read the cache `ADR-020` forecloses; `GS-267` wants a disabled control the portal fence admits none of); rows whose assertion is a LATENCY PERCENTILE (`GS-051`), a CONCURRENCY RACE (`GS-230`, `GS-036`, `GS-040`) or a NAMED DATABASE CONSTRAINT (`GS-278`, `GS-283`) rather than a fixture; and rows whose module genuinely has no code. **ONE PREVIOUS CITATION WAS FALSE AND IS REPAIRED**: `GS-112` read *"no test file in this tree names it"* and [`seed-detectors.test.ts:585`](../../packages/db/test/seed-detectors.test.ts) names it. **ONE CITATION THIS SESSION WROTE WAS ALSO FALSE AND IS REPAIRED IN THE SAME BRANCH**: `GS-117` said unfreeze had no route and `POST /admin/accounts/:accountId/unfreeze` is one of the seven [`admin-writes.test.ts:301`](../../apps/api/test/admin-writes.test.ts) pins by equality. **`GS-311` WAS TOUCHED IN ERROR AND IS RESTORED BYTE FOR BYTE**; it carries `vendor-call` and [ADR-125](../decisions/ADR-125.md) moves no row into another session's term. **NO ADR NUMBER TAKEN, `gates.mjs` UNTOUCHED, NO SEVENTH TERM, NO TEST AND NO FIXTURE WRITTEN.** 33 of 33 gates, 12 of 12 invariants, 188 files / 3,925 passed / 6 skipped unchanged, `falsify` clean. |
| [2026-08-28 - Session 306](2026-08-28-session-306.md) | **[ALLOCATION](../decisions/ALLOCATION.md) row `171` allocated an ADR under the heading "ONE RULING UNBLOCKS FIVE PORTS". [ADR-171](../decisions/ADR-171.md) read the five at their own declarations and REFUSED THE DOOR ON THE MEASUREMENT: it moves none of them.** **THE OPERATOR DIRECTORY IS NOT IN THIS DATABASE**: `admin_actions.actor` is `text NOT NULL` with no foreign key ([`0017:77`](../../packages/db/migrations/0017_events_and_audit.sql)) and [`admin-writes.ts:133`](../../apps/api/src/routes/admin-writes.ts) states it, so `setAdminSessionSource` is the SSO port itself and **no door onto the trader database could serve it**; `useAdminWriteBackend`, `useAdminPayoutBackend` and `useAdminWalletBackend` each need `principal(request)`, whose only resolver is that same session source, so **three of the five reduce to the first before any door is reached**; and `setAdminReadSource` says in its own header that what it is missing *"is not an authority, it is a shape"*. **THE ARGUMENT THAT WOULD HOLD EVEN AT FIVE CALLERS**: `ApiDb` is a PARAMETER, so a third method arrives in `databaseAuthBackend(db: ApiDb)`, where `SystemTx.rowAt('users', { email })` is exactly [ADR-120](../decisions/ADR-120.md)'s `B1`, the pre-identity read that file refuses in the constant answering twelve methods. **THE MECHANICAL FORECLOSURE LANDED AND FAILED ON ITS FIRST RUN**: [`apps/api/test/db.test.ts`](../../apps/api/test/db.test.ts) discharges [ADR-165](../decisions/ADR-165.md) section 10 finding 5, and `src/db.ts`'s claim that the accessor grep returns exactly that file is FALSE -- [`routes/account-reads.ts`](../../apps/api/src/routes/account-reads.ts) imports `atMost`, benignly, so the case pins a map and asserts separately that no file but the door file takes a handle. **A RECORDED BLOCKER WAS FALSE**: `wiring.test.ts` said `apps/api` does not declare `@merit/rules-engine` and it has since session 252. **Wired count unchanged at 6 of 21, `start.ts` untouched, triple unchanged at `{ 21, 6, 15 }`.** **NO `SystemReason` member, no `SqlExecutorReason` member, no `pg` import, no cast past a key type, `packages/db` untouched, no handler logic changed, no migration, no gate.** 33 of 33 gates, 13 of 13 invariants, typecheck 0, lint 0, format clean, 192 files / 4,091 passed / 6 skipped. |
| [2026-08-28 - Session 307](2026-08-28-session-307.md) | **[ADR-172](../decisions/ADR-172.md) LANDS. THE TWO RESERVED FINDINGS WERE TWO QUESTIONS AND ONLY ONE WAS A RULING.** **Finding 1 reproduced exactly and took the number**: `PayoutTx.ledger` ([`routes/payouts.ts:395`](../../apps/api/src/routes/payouts.ts)) is a non-nullable `LedgerTx`, whose `insert` names `ledgerTransactions` and `ledgerEntries`, both scope class `derived` (`scope.ts:512,521`); `ScopedTx.insert` takes `OwnedTableKey` (`scoped-db.ts:1739`) and `ParentedTableKey` is `Extract<DerivedTableKey, 'sessions'>` (`:1540`), **a closed list of ONE**, so only `SystemTx` satisfies it and [ADR-165](../decisions/ADR-165.md) refused to widen `SystemReason`. **No live deployment can construct a `PayoutBackend`.** **FINDING 2 IS REFUTED AT SOURCE AND IS OWED NO NUMBER**: [`apps/api/src/idempotency-store.ts:144`](../../apps/api/src/idempotency-store.ts) has implemented `IdempotencyStore` since [ADR-112](../decisions/ADR-112.md), and **`tx.updateAt` IS the addressed UPDATE the premise said was unwritable**, composing tenancy AND address in one predicate (`scoped-db.ts:1940-1951`) and held by ELEVEN executed tests. **The dispatch's own fence asked that this be checked before concluding it impossible, and the check is what refuted it.** **[`wiring.test.ts`](../../apps/api/test/wiring.test.ts) CONTRADICTED ITSELF IN ONE FILE TWENTY LINES APART**, `:150` saying the store exists and `:228` saying no file provides one. **THE RULING IS THAT THE `LT-01` POSTING LEAVES THE REQUEST PATH**: the only handle satisfying `LedgerTx` is `insert<K extends TableKey>` over the whole estate, so a door in `apps/api` would be `systemDb` renamed and `PayoutTx.ledger` is the defect instead. **Three precedents already post this way** -- the worker at `systemDb('nightly-batch')` (`sweeps/ports.ts:219`), the operator console, and the cash door which declined to post on its own request path in writing. **`INV-M5-06` SURVIVES BY CONSTRUCTION**: all three doors build `` `${PAYOUT_ENDPOINT} ${idempotencyKey}` `` from the request's STORED key, so the key belongs to the APPROVAL and not to the DOOR. **`useWithdrawalBackend` STAYS BLOCKED FOR A TRUER REASON**: nothing drives `requested --> approved` and both `requested` and `cooling` are in `OPEN_WITHDRAWAL_STATUSES`, so wiring trades an honest 503 for a **permanent per-trader lockout**; both refuse and only the 503 is reversible. **NO MIGRATION NEEDED AND NONE CLAIMED** -- `0010` already carries `status`, `idempotency_key`, `identity_id` and the three clamp legs. **No port wired, triple unmoved at `{21, 6, 15}`, `scoped-db.ts` untouched, no `SystemReason` member, no `pg` import.** The store construction is PERFORMED in a new suite rather than asserted to typecheck, and the false-premise guard was **watched failing on a seeded mutation and restored byte for byte**. 33 of 33 gates, 13 of 13 invariants, typecheck 0, lint 0, format clean, 192 files / 4,092 passed / 6 skipped, `falsify` clean. |
| [2026-08-28 - Session 308](2026-08-28-session-308.md) | **[ADR-173](../decisions/ADR-173.md), `status: proposed`, approval line UNSIGNED. MONEY PATH BY CONTENT. THE RULING IS THAT THE CONTRACT IS WRONG AND NO MIGRATION LANDS, SO `0052` IS NOT TAKEN.** [ADR-158](../decisions/ADR-158.md) wrote `POST /admin/wallet/:identityId/correct` from [`0011`](../../packages/db/migrations/0011_wallet.sql) alone across eighteen findings and [`0038_account_adjustments.sql`](../../packages/db/migrations/0038_account_adjustments.sql) is the built door for exactly that endpoint. **THE PREMISE WAS RE-DERIVED AT SOURCE AND THEN EXECUTED**: PostgreSQL 16 in the container, `0001` through `0051` forward-only under `ON_ERROR_STOP` with no failure, one fixture and five writes. **The correction written exactly as the contract instructs fails on `invalid input syntax for type uuid`**, so `corrects_entry_id` *"Becomes `reference_id`"* is not merely wrong but **INEXPRESSIBLE**: it never reaches a constraint, a trigger or an invariant. The same correction through `0038`'s door commits; the correcting DEBIT the contract's body admits fails on `account_adjustments_debit_is_a_reversal` with the constraint name in the error. **THE RECORD EXISTS AND IS NOT WHERE THE CONTRACT SAYS, WHICH IS THE THIRD AND BETTER OUTCOME THE DISPATCH NAMED**: `admin_actions.before.corrected_entry` carries the whole corrected entry as it stood before the correction plus an `evidence_refs` entry of kind `wallet_entry`, [`admin-wallet.ts:1504`](../../apps/api/src/routes/admin-wallet.ts) already writes it, and the live database returned the corrected entry's id back out of it. **`0026` revokes `UPDATE, DELETE` on `admin_actions` from `merit_app` and `PUBLIC` exactly as it does on `wallet_entries`**, confirmed from `information_schema.role_table_grants`, so the audit row is as durable as the entry it describes and holds strictly more than a column would. **That is [ADR-158](../decisions/ADR-158.md)'s OWN CLAUSE 7 INSTRUMENT, one clause away from the finding [ADR-158](../decisions/ADR-158.md) missed.** **A FIFTH WRITE THE DISPATCH DID NOT PREDICT MOVED THE RULING FURTHER**: of the three `reason_code` members `0038` admits, only `reconciliation_error` is about an entry, and a `goodwill` credit lands carrying `provenance = 'correction'` because `0011`'s closed list offers it nothing else, so **`corrects_entry_id` is OPTIONAL rather than merely re-described**, which is the widening direction. **NO FOREIGN KEY ANYWHERE IN THE SCHEMA REFERENCES `wallet_entries`** and the absent column is a decision `0038` recorded in writing rather than an oversight. **THE COST IS NAMED**: the audit row is written by the handler and not by a trigger, so a founder who reads `E2` as requiring enforcement should take `0052` for an [`0048`](../../packages/db/migrations/0048_audited_writes_on_append_only_tables.sql)-shaped function taking an `admin_actions.id` as a precondition, and NOT for a column. **THREE OF SESSION 298's FOUR DISAGREEMENTS STAND AND THE ROUTE IS NOT TOUCHED**; `dual_control_threshold_cents` is `NOT NULL` with no source in this tree and cannot be ruled from any document in it. **One divergence is recorded with its one-word patch**: the handler now requires a field the repaired contract makes optional, which is the fail-closed direction of the two. **[`0038`](../../packages/db/migrations/0038_account_adjustments.sql) AND [`0011`](../../packages/db/migrations/0011_wallet.sql) ARE BYTE FOR BYTE UNCHANGED** (`E2`). 33 of 33 gates, 13 of 13 invariants, typecheck 0, lint 0, format clean, 191 files / 4,088 passed / 6 skipped unchanged, `falsify` clean. **ONE FENCE EXCEPTION, ARGUED AND SEEDED**: two assertions in [`apps/api/test/admin-wallet.test.ts`](../../apps/api/test/admin-wallet.test.ts) went red on the contract edit, one pinning the very sentence this entry deletes (INVERTED rather than dropped, because a deleted assertion is how the sentence comes back) and one firing on this entry's own explanatory paragraph, which is **session 298's recorded landmine arriving**; `CORRECTION_BLOCK` is narrowed to the fenced `ts` body, which is what its own doc comment already said was meant. **Three defects seeded into the contract and all three watched failing**, and `API_CONTRACT` is byte identical afterwards. **[`admin-wallet.ts`](../../apps/api/src/routes/admin-wallet.ts) ITSELF IS UNCHANGED** and the refusal is exactly where session 298 put it. **The diff grep for anything domain-shaped is EMPTY and is pasted in the pull request.** **No handler code, no migration, no `packages/db` change, no gate, no section renumbered.** |
| [2026-08-28 - Session 309](2026-08-28-session-309.md) | **[`apps/worker/src/detectors/fills.ts`](../../apps/worker/src/detectors/fills.ts) and [`test/detectors-fills.test.ts`](../../apps/worker/test/detectors-fills.test.ts) (both new) build [P7](../plans/P7-risk-and-abuse.md) section 8's `P7-f`: `D-01` fill clustering, `D-04` news-window clustering and `D-05` martingale sequence, against `P7-e`'s ports and changing none of them.** No ADR number, no migration number, no `packages/db` change, no `apps/api` change, no `SystemReason` member, no `SqlExecutorReason` member, no `pg` import, `runner.ts`, `canary.ts`, `detectors/ports.ts` and [`src/db.ts`](../../apps/worker/src/db.ts) read in full and NOT touched, and the worker barrel APPENDED and re-read whole. **THE BEST FINDING IS IN THE FILE THIS SLICE BUILDS ON: `canary.ts`'s `martingaleSequence` PUTS `realizedPnlCents` AND `sequenceOrdinal` ON A `fills` ROW AND `fills` HAS NEITHER COLUMN** -- `realized_pnl_cents` is `daily_marks`' ([`schema.ts:652`](../../packages/db/src/schema.ts)) and `sequenceOrdinal` is no table's -- so a `D-05` written against that shape would **find its canary every night and never once fire on a real martingale**, which is `AS-M7-05`'s failure wearing a green dashboard. It is REPORTED and worked around inside the fence: `D-05` builds its own battery over `fills.quantity` and `daily_marks.realized_pnl_cents`, and the suite asserts every canary row THIS file builds uses only column names `schema.ts` gives the table it claims to be. **THE NEAR-MISSES ARE THE DELIVERABLE AND EACH IS ONE UNIT FROM ITS POSITIVE**: 3 of 4 shared fills fires and 2 of 4 does not at a 5000bp floor, fills 2s apart pair and 3s apart do not, 3 releases fire and 2 do not, 100 -> 200 contracts fires and 100 -> 199 does not, three sequences fire and two do not, and the firing size series after WINNING days raises nothing. **Every threshold comparison is an integer cross-multiplication rather than a division**, which is what makes "just outside" one contract rather than a rounding. **`D-01` FILTERS SAME-IDENTITY PAIRS AT THE DETECTOR** (`M07` section 3.4) and the suite proves it is not a dismissal either: the same fixture with one field changed writes **no row of any kind**. **`D-01` RUNS ON INGEST** through `fillClustering({at})`, whose narrowed stream is one `atLeast` term beside the day's equality -- `ADR-157` section 5's grant used by name -- **and it still reads the day, because the share's denominator IS the day**, so the ingest mode buys `AS-M7-01`'s detection latency and not read cost. **EVERY THRESHOLD COMES FROM `detector_definitions` AND NONE FROM THE CODE** (`INV-M7-04`), so **all three DECLINE against the shipped seed** and the run is recorded `failed`: eleven of the seventeen numbers they need are `unstated` because `OQ-M7-02` is the founder's. **SEVERITY IS CAPPED RATHER THAN INVENTED**: nothing in the corpus states an SLA duration for severity 4 or 5, `OQ-M7-03` is open, and no seed row carries `sla_hours`, so a finding that would score 4 or 5 is written at 3 with `severity_capped_from` and the reason ON THE FLAG, because an invented clock holds a real trader's payout for 48 hours under [ADR-040](../decisions/ADR-040.md). **THE EVIDENCE CARRIES THE CONDUCT AND DISCLOSES NO THRESHOLD** (`M07` section 3.4, and the seed's own `is_sensitive_reason`: "a ring told the window is 2 seconds spaces its entries by 3"), asserted over every flag all three can raise. **Twelve seeded cheap implementations watched failing**, each reverted from a byte-identical copy checked by digest. **Six findings reported and not repaired**, including that `P7`'s `P7-f` row cites "section 3.4" of a document with no section 3.4 (the ruling is `M07`'s), that `lookback_days` is a parameter no document names and two of these three need, and that the on-ingest CALL SITE is in `src/live/` and outside this fence. 33 of 33 gates, 13 of 13 invariants, typecheck 0, lint 0, format clean, 192 files / 4,137 passed / 6 skipped, `falsify` clean. **This session contributes exactly one test file and 49 cases**, measured against the 191 / 4,088 baseline reproduced before anything changed. |
| [2026-08-28 - Session 310](2026-08-28-session-310.md) | **[`apps/worker/src/detectors/graph.ts`](../../apps/worker/src/detectors/graph.ts) and [`test/detectors-graph.test.ts`](../../apps/worker/test/detectors-graph.test.ts) build [P7](../plans/P7-risk-and-abuse.md) section 8's `P7-g`. ONE OF THE FIVE DETECTORS PRODUCES AN ANSWER TONIGHT AND FOUR DO NOT, and every reason is a measurement.** `D-02` RUNS, capped at severity 3; `D-03`, `D-13` and `D-14` DECLINE under `DetectorDeclined` on parameters [`P7-d`](2026-08-27-session-300.md)'s seed records as `unstated`; **`D-12` IS NOT A `Detector` AT ALL**, because its output is *"a watched-cluster set, NOT a flag"* and `runner.ts` computes `found < expected ? 'degraded' : 'ok'`, so a detector returning neither a finding nor a group is **degraded every night forever** -- and `correlation_groups` cannot hold one either, its `statistic` and `threshold` being `numeric NOT NULL`. **THE MEASUREMENT [ADR-157](../decisions/ADR-157.md) SECTION 5 ASKED FOR WAS TAKEN AND IT REFUTES THE EXPECTATION**: all five ARE readable through the accessor, the join is ordinary code in the runner, and **no `scoped-db.ts` entry is asked for**. What it costs is **22 rows read per row that mattered** at 60 marked accounts over 20 days, executed; plus two costs no clause of that entry names -- **a filter carries ONE narrowing per column so a `BETWEEN` is half a read**, and **an empty filter does not compile so "the whole table" is spelled as a narrowing that is true**. **`GS-118`'s NEGATIVE ASSERTION IS WRITTEN THE STRONG WAY**: the six-account ring on the 5 trading day path raises no flag AND the same run finds its canary, so *"did not fire"* is distinguishable from *"is broken"*; seeding *"`D-02` ignores its window"* turns exactly that case red. **`D-13`'s CONJUNCTION IS PROVED BY THREE NEAR-MISSES EACH SATISFYING TWO CONDITIONS**, and `D-14` works on POSITIONS whose **table does not exist in `scope.ts` at all**. **FOUR FINDINGS NEEDING A RULING**: `SD-M7-02`'s clock has no stated duration, so **no detector can write a severity 4 or 5 flag today**; a canary cannot be sized to the registry parameters it must satisfy, because `Detector.canaries` takes no request; `AS-M7-05` note 1 **forbids a detector reporting any count over its own input**, caught by this suite reading 62 against a population of 60; and the `flag_type` vocabulary has no group member. **`correlation_groups` IS registered, so no schema change and no stop.** 12 of 12 seeded defects caught, four of them defended in two places and needing both sites removed. 33 of 33 gates, 13 of 13 invariants, typecheck 0, lint 0, format clean, 192 files / 4,164 passed / 6 skipped, `falsify` exit 0 with the tree clean. **No ADR number, no migration number, no `packages/db` change, no `apps/api` change, `runner.ts`, `ports.ts`, `canary.ts` and `src/db.ts` read in full and NOT touched.** |
| [2026-08-28 - Session 311](2026-08-28-session-311.md) | **[`apps/worker/src/detectors/identity.ts`](../../apps/worker/src/detectors/identity.ts) (new) builds [P7](../plans/P7-risk-and-abuse.md) section 8's `P7-h`**: `D-07`, `D-08`, `D-09`, `D-10` and `D-11`, plus `D-16`'s v1 half and `D-18`'s detector, against the runner and ports [`P7-e`](2026-08-27-session-300.md) landed and touching neither. No ADR number, no migration number, no `packages/db` change, no `apps/api` change, no `SystemReason` member, no `SqlExecutorReason` member, no `pg` import, and `runner.ts`, `canary.ts`, `ports.ts` and `src/db.ts` read and NOT touched. **SEVEN OF SEVEN DECLINE UNDER `P7-d`'s SEED AND THAT IS THE RESULT RATHER THAN THE SHORTFALL**: `INV-M7-04` makes the registry the only source of a threshold and eleven of the eighteen rows carry no number, so `DetectorDeclined` fires, the run is recorded `failed`, and `detector_runs_unhealthy_idx` reads it the same morning; what each detector waits for is a LIST in `registryBlockers` rather than a shrug. **SEVERITY IS THE BLOCKER FIVE OF THE SEVEN SHARE AND IT IS A MONEY DECISION** ([M07](../plans/M07-risk-abuse.md) section 3.3: 3 to 4 changes who gets held, *"a data change with a recorded effective date, never a deploy"*). **THE CLOCK THAT BAND NEEDS IS NOT SEEDED EITHER AND THAT IS THE FIRST FINDING**: `risk_flags_high_severity_has_sla` requires `sla_due_at` at 4 and 5, `OQ-M7-03` PROPOSES the duration and is OPEN, so `D-09` and `D-16` are blocked on an `sla_hours` no row carries, NAMED rather than defaulted. **[ADR-155](../decisions/ADR-155.md) BINDS HARDEST AND IT IS THE TYPE THAT HOLDS IT**: `DetectorFinding` has no `status`, `DetectorScanInput` hands no transaction, `identity_links` is absent from `DETECTOR_WRITE_TABLES`, so the hard link is READ and never WRITTEN, and the seeded violation **does not compile** (`TS2353`). **`D-18` TESTS `footprint_present IS FALSE` AND NEVER `IS NOT TRUE`, ASSERTED AT BOTH THE PLACES IT RUNS**, because the battery is merged AFTER the read and never crosses `rowsWhere`; seeded as `!== true` the vendor timeout scores as a fleet member and three cases go red. **THE BATTERY FOUND A REAL DEFECT MID-SUITE AND `P7-g` INHERITS ITS SHAPE**: `D-11`'s cross-row join put a synthetic subject and a real one in ONE finding, `DetectorCanaryLeak` refused it, and the repair is that the finding names the CANDIDATE and not the pair, which `GS-112` wanted anyway. **NO EVIDENCE OBJECT CARRIES A THRESHOLD**, asserted mechanically against the seed's own parameter keys, because `INV-M7-04`'s chain already reconstructs the parameter and a copy makes `INV-M7-10`'s stripping load-bearing twice. **TWO STRUCTURAL BLOCKERS REPORTED RATHER THAN REACHED FOR AND NEITHER IS AN ACCESSOR ARGUMENT**: `D-09` has no input, which is `DEP-M7-04` exactly, and the remedy is ONE MEMBER in `ports.ts`; and two of `D-18`'s four legs are `D-15`'s and `D-15` does not exist. **Three smaller findings measured**: `canary.ts`'s `sharedDestination` mints an `identityId` onto a table that has no such column, `CANARY_SHAPES` holds no identity-side shape, and a filter is one value per column so a two-sided window on one column is not expressible. **Twelve seeded defects, all twelve caught.** 33 of 33 gates, 13 of 13 invariants, typecheck 0, lint 0, format clean, 192 files / 4,190 passed / 6 skipped. **This session contributes exactly one test file and 102 cases**, measured against the reproduced baseline of 191 / 4,088. |
| [2026-08-28 - Session 312](2026-08-28-session-312.md) | **FOUR OF THE FIVE THIRD-STATE ROWS ARE `covered-elsewhere` AND THE FIFTH IS NOT.** `GS-155`, `GS-223`, `GS-274` and `GS-314` each gained ONE COMMENT naming the row in the suite that already asserted it, and each was re-read against the registry row and the tree before the claim was made. **`GS-038` DID NOT SURVIVE THE READING AND STAYS `blocked`.** Its duplicate half is asserted whole; its out-of-order half is asserted as far as the DEFERRAL and no further, because *"re-evaluated"* is in [`webhooks-psp.test.ts:490`](../../apps/api/test/webhooks-psp.test.ts)'s own block title and executes nowhere: [`webhooks-psp.ts:373`](../../apps/api/src/routes/webhooks-psp.ts) says `defer_attempts` counts RE-DRIVES, that the receiver never increments it and that *"the re-driver is the batch's"*, and no batch in this tree reads `psp_webhook_events_deferred_idx`, so an out-of-order delivery stops at `out_of_order_deferred` and never reaches the *"correct final state"* the row pins. **Its comment says in capitals that finding `GS-038` in that file is NOT a discharge**, so the string does not become the bait section 39's third trap describes. **THE CONVENTION IS WRITTEN DOWN**, in a new [section 39](../testing/golden-scenarios/39-fixture-status-and-blockers.md) subsection: a suite that proves a `GS-nnn` must carry that id as a LITERAL STRING in a construct that is not skipped, because assertion 6 is a static parse and nothing else can tell it what a suite is about. **The subsection says in its own text that section 39 is the wrong home**, and this entry carries the wording and the two candidate homes a module-suite author would actually meet. **NO ASSERTION WAS EDITED, no test changed, `gates.mjs` untouched, no seventh blocker term, no ADR and no migration number taken, and nothing under `packages/db`, no route and no handler.** Every cited file executed and watched: `51 / 51`, `94 / 94`, `41 / 41`, `23 / 28 with 5 named skips`, `32 / 32`. **33 of 33 gates, 13 of 13 invariants, typecheck 0, lint 0, format clean, 191 files / 4,088 passed / 6 skipped unchanged, `falsify` clean.** The headline is **FOUR**. |
| [2026-08-28 - Session 313](2026-08-28-session-313.md) | **`apps/portal/src/app/(purchases)/source.ts` and `apps/portal/src/app/(purchases)/states.ts` (both new) wire the LAST of the six portal segments to [ADR-162](../decisions/ADR-162.md)'s client, and every segment under `apps/portal/src/app/` now reaches `/api/v1` through the one file permitted to call `fetch(`.** **SC-M4-06 IS WIRED END TO END**: `GET /purchases` at section 1's maximum limit and one `GET /plans/:planId/versions/:version` per side of each rule diff, guarded field by field, `ready` reached from a real request. **SC-M4-08 IS WIRED AS FAR AS IT GOES AND THE STOP IS A MEASUREMENT**: the screen reads by account id and kind, `fixtures.ts` already ruled that asking `GET /accounts` here "would be this page reaching into that one", and `GET /certificates` -- the read M11 wrote so this screen would not need account ids -- is REGISTERED and carries a list item [`api/types.ts`](../../apps/portal/src/api/types.ts) does not transcribe. **ALL FIVE ENDPOINTS THIS SEGMENT NAMES ARE REGISTERED, BUILT FROM `CompositionReport.registered` ON BOTH SURFACES RATHER THAN FROM THE DISPATCH**: 45 public, 24 operator. **AND REGISTERED IS NOT SERVED**: both certificate rows ship an unwired backend and answer 503 today, which is the `error` arm and NOT `unavailable`, and the 503 is in the test's refusal list for that reason. **THE `.png` ROW IS DEMONSTRATED RATHER THAN DESCRIBED**: a 200 carrying image bytes comes back `server_error` with status 200 through ADR-162's client, so this segment does not read it, and it holds no `code` to ask with either. **A THIRD `why` JOINS SESSION 290's TWO AND IT IS MEASURED**: `no_transcription` names an endpoint that is registered and answers and whose shape this application has not transcribed, which is exactly the fact `app/accounts/source.ts` refused `/marks` on and had no word for. **THE DISCLOSURE BOUNDARY IS ASSERTED IN THE NEGATIVE OVER THE RENDERED BYTES OF ALL THREE ARMS**, not just `ready`, because a state component is a second way onto SC-M4-08. **FIVE THINGS REPORTED AND NOT REACHED FOR**: `/payouts` is still statically prerendered with a wired `load()` baked into the artifact; the purchase history can carry a truncated page and has no prop to say so; one 404 carries both "no certificate of that kind" and "not your account"; `GET /certificates` needs a transcription and two rendering files; and a third `states.ts` copy catalogue now exists. **NO ADR NUMBER, NO MIGRATION NUMBER, `http/client.ts` AND `api/types.ts` UNTOUCHED, `model.ts` AND BOTH SCREENS UNTOUCHED.** 33 of 33 gates, 13 of 13 invariants, typecheck 0, lint 0, format clean, 192 files / 4,113 passed / 6 skipped, portal build 12 routes with `/purchases` and `/certificates` moved from static to dynamic, `falsify` clean with the tree clean after it. |
| [2026-08-28 - Session 314](2026-08-28-session-314.md) | **[`packages/rail`](../../packages/rail/src/index.ts) is [P5](../plans/P5-payouts-and-wallet.md) section 8's `P5-m` minus the half session 258 (`ADR-146`) claimed: the payout rail's PORT and its sandbox adapter, on [`packages/psp`](../../packages/psp/src/port.ts)'s SHAPE and not its TYPE. MONEY PATH, `E2` READ OWED, DO NOT MERGE UNREAD.** **`payout_transfers.provider` CARRIES NO CHECK AT ALL**, where `purchases.psp` closes `PspId` at two, so this port's type is derived from a DEFAULT and is deliberately NARROWER than its column; `test/schema-bind.test.ts` asserts the ABSENCE, with the `status` CHECK one row down as the control that the absence is not the file's style. **THE APPROVAL KEY INVERTS `PurchaseIntent`'s RULE AND THAT IS `INV-M5-06`**: a card attempt is a new key and a transfer attempt is the SAME key, so `ApprovalIdempotencyKey` is branded, minted only from a stored row, and carries nothing prefixed onto it. **THE CONTRACT ASKS FOR TWO REPLAY CONTROLS IN ONE SENTENCE AND THEY ARE NOT THE SAME THING**: the nonce refuses a CAPTURE (401) and the event id recognises the rail's OWN RETRY (200, S-2). Both directions are seeded, and two folded ledgers are seeded beside them so that each fold turns exactly one direction red. **THREE FINDINGS MAKE `LT-07` UNWRITABLE AND NONE IS RESOLVED**: its credit leg names *"the payout wallet position"*, which is not one of `0009`'s seven declared codes and cannot be `trader_wallet` because `ADR-124` records `LT-07` as FIRM-ONLY; its ledger key has two recorded conventions pointing opposite ways, and bare is the string `LT-06` already claims under a `text NOT NULL UNIQUE` column; and NO FILE IN THIS TREE SAYS WHETHER `firm_treasury` IS AN ASSET OR A LIABILITY. All three are asserted by `test/lt-07.test.ts` against their primary sources rather than left as comments. **TWO GRANTED FILES WERE DELIBERATELY NOT SPENT AND EACH REFUSAL IS ARGUED**: `webhooks-rail.ts` has no contract row and its only honest path duplicates session 258's `POST /webhooks/rise`, which `compose` refuses at startup; and `OVERVIEW.md` section 3 rows FOUR of this workspace's THIRTEEN packages, `packages/psp` included among the nine absent, so one row for `rail` alone would make an incomplete table read as complete in an `approved` document. **NO ADR NUMBER, NO MIGRATION NUMBER, NO `packages/db` CHANGE, NO ROUTE, NO CONTRACT CHANGE, NO LEDGER POSTING AND NO `IdempotencyStore`**; `wiring.test.ts`'s `{declared: 21, wired: 6, blocked: 15}` triple does not move because no route module is added. **VG-12 IS ASKED TO ADMIT NOTHING** and P5 requires that be stated rather than assumed: no runtime dependency, no workspace dependency, three `catalog:` devDependencies already installed for `@merit/psp`, `node:crypto` a builtin, and the only line `pnpm-lock.yaml` gains is this package's own 12-line `importers` block. **Seven defects seeded and all seven caught**, including the credit leg written `+amountCents`, which is session 288's own first draft. 33 of 33 gates, 13 of 13 invariants, typecheck 0, lint 0, format clean, **198 files / 4,184 passed / 6 skipped** against a `main` baseline of 191 / 4,088 / 6 reproduced exactly first, `falsify` clean. |
| [2026-08-28 - Session 315](2026-08-28-session-315.md) | **[ADR-174](../decisions/ADR-174.md) takes findings (A) and (C) and [ADR-175](../decisions/ADR-175.md) takes (B); the split is argued on the ground that they live in TWO TABLES and only one is blocked.** **THE RESERVATION ANTICIPATED A HOLE IN `LT-07`'s CREDIT SLOT AND THE MEASUREMENT FOUND THE SLOT BESIDE IT WRONG TOO**: `LT-06` credits `firm_treasury` `amount_cents` and `LT-07` debits it, and [`posting.ts:235-236`](../../packages/ledger/src/posting.ts) writes `+amountCents` on a debit and `-amountCents` on a credit, so **THE EXTERNAL LEG MOVES `firm_treasury` BY ZERO**, on the one account [M05:141](../plans/M05-payout-system.md) names as where a cash movement books, in the leg whose own row calls it *"The external leg's cash movement"*. That holds for EVERY `kind`, so it survives finding (C) rather than waiting on it, and a code minted for the credit slot would complete a posting wrong in the other slot. **NO CODE IS MINTED AND `0052` RETURNS TO THE POOL UNSPENT.** **`trader_wallet` IS REFUSED ON ARITHMETIC RATHER THAN ON SCOPE** (`LT-06` already debits it at approval, so an `LT-07` crediting it returns the wallet to where it started while the money leaves the firm), which is worth having because **[ADR-124](../decisions/ADR-124.md) clause 3's ground is the row under repair restated**: its conclusion survives and its stated ground cannot settle the repair. **(C) IS RECORDED AS AN ABSENCE AND IT IS LARGER THAN THE DISPATCH SAID**: against PostgreSQL 16.13 with `0001` through `0051` applied forward-only under `ON_ERROR_STOP`, **`ledger_accounts` HOLDS ZERO ROWS** and the database accepted `firm_treasury` as `'asset'`, as `'liability'` and as `'revenue'` in turn, so **NOTHING SEEDS THE CHART OF ACCOUNTS AT ALL** and four of the seven codes have no `kind` written anywhere. **(A) CANNOT BE CLOSED WHILE (C) IS OPEN** and the three available repair shapes each need the absence filled first, which is the opposite order from the one the reservation anticipated. **(B) IS CLOSED: the two conventions are ONE RULE UNDER TWO SPELLINGS** and the rule is that a key names the EVENT it posts and never the DOOR that reached it; `PAYOUT_ENDPOINT` is a module constant three call sites share, so `LT-01`'s prefix names the payout-approval event and is merely SPELLED as a route. **`LT-07`'s key is `` `${kind} ${wallet_withdrawals.idempotency_key}` ``**, and every leg of it was EXECUTED: the bare key claimed by `LT-06` and refused to `LT-07`, the same refusal across a different reference so per-reference scoping is unavailable, **the endpoint prefix watched FAILING** with two doors admitting two postings for one settlement, and the kind-prefixed key accepted while its own replay was refused. **`LT-01`'s three doors are NOT re-spelled**, because `ledger_entries` is append-only and re-spelling a landed key costs a compensating posting. **NO MIGRATION, NO DOCUMENT AMENDED, NO CODE CHANGE ON ANY MONEY PATH**, no `SystemReason` member and no `SqlExecutorReason` member. **`packages/rail`'s three findings gain a `ruled` field and `lt-07.test.ts` gains twelve cases; EVERY ORIGINAL CASE STILL PASSES UNEDITED**, because no primary source they read is amended. **Eight defects seeded and seven caught first time; the eighth was a MISS and the reason is recorded rather than counted as a pass** (a `toContain` over a seven-code list still matches once an eighth is appended at its end; the assertion now parses the list, and `packages/ledger`'s three-way bind caught the same seed independently). 33 of 33 gates, 13 of 13 invariants, typecheck 0, lint 0, format clean, **204 files / 4,455 passed / 6 skipped** against a `main` baseline of 204 / 4,443 / 6 reproduced exactly first, `falsify` clean. |
| [2026-08-28 - Session 316](2026-08-28-session-316.md) | **[ADR-176](../decisions/ADR-176.md) LANDS, `proposed`. [ADR-172](../decisions/ADR-172.md) clause 2 APPLIED where it was reported.** `PayoutTx.ledger` is DELETED and `decidePayout`'s posting arm is REMOVED, on premises re-derived at source rather than inherited: `LedgerWriteKey` is two `derived` tables (`tx.ts:64`, `scope.ts:513,522`), `ScopedTx.insert` takes `OwnedTableKey` (`scoped-db.ts:1739`), `ParentedTableKey` is a closed list of ONE (`:1540`), so the only satisfying handle is `insert<K extends TableKey>` over the whole estate (`:1802`). **THE VERIFICATION FOUND ONE THING THE RULING NEEDED AND `ADR-172` DID NOT SEE**: `PayoutRequestInsert` had NO `idempotencyKey` member while `payout_requests.idempotency_key` is `text NOT NULL` (`0010:82`), and `admin-payouts.ts:784,809` already READS that column on release. While the posting stood in the handler the key reached it out of local memory, so removing the posting alone would have committed an approval **no door could ever post**, silently. The field is added and stored UNPREFIXED, on `payout_requests_account_idempotency_uq (account_id, idempotency_key)`. **`INV-M5-06` SHOWN AT THREE FILES, NOT CITED**: both remaining doors are `releaseLedgerKey` character for character and both call sites pass `held.idempotencyKey` off the locked row (`admin-payouts.ts:1185`, `expiry.ts:667`). **`usePayoutBackend` DOES NOT BECOME CONSTRUCTIBLE and its `BLOCKED` entry is REWRITTEN rather than shrunk**: `PayoutSubject.state` is a `RuleState` requiring `lifetimeSettledCents`, `breached` and `breachKind`, and `0015_rule_states.sql` declares NONE, `lifetime_settled` appears in NO migration, and no migration names any `BreachKind` member -- **the same blocker the WIRED `databaseAccountReads` already carries at `account-reads.ts:851`**, bound to this port by `INV-M5-02`. Independently, `ResolvedPlan` needs `plan_versions` and `plan_version_sizes`, both scope class `firm`, which `ScopedTableKey` excludes. **A partial backend is REFUSED**: `listPayouts` and `idempotency` are constructible and installing them beside a rejecting `transact` is a live-looking route in front of the arm that approves payouts. **FOUR LINE CITATIONS IN `wiring.test.ts` WERE FALSE**, two of them EIGHTEEN LINES OFF inside the entry `ADR-172` wrote one session earlier to replace a false reason. **Four stale "three doors" claims corrected at their own files.** The six `LT-01` composition assertions were RETARGETED onto `postTransaction(recorder, chart, lt01(...))` rather than deleted, and four new source-reading cases pin a removal a behavioural suite cannot hold. **Triple unmoved at `{21, 6, 15}`, no migration, no `SystemReason` member, `packages/db` untouched.** 33 of 33 gates, 13 of 13 invariants, typecheck 0, lint 0, format clean, 204 files / 4,448 passed / 6 skipped, `falsify` clean. |
| [2026-08-28 - Session 317](2026-08-28-session-317.md) | **[ADR-173](../decisions/ADR-173.md) IS APPLIED TO [`apps/api/src/routes/admin-wallet.ts`](../../apps/api/src/routes/admin-wallet.ts) AND THE APPEND IS STILL REFUSED, WHICH IS WHAT THE ENTRY ITSELF RULED.** MONEY PATH, `E2` READ OWED. `corrects_entry_id` is now OPTIONAL, the `conflict` check is CONDITIONED on the field being present rather than deleted, `admin_actions.before.corrected_entry` and the `wallet_entry` evidence ref are OMITTED rather than NULLED when no entry is named, and the port states the binding a wiring slice must not get wrong: **`wallet_entries.reference_id` IS `account_adjustments.id` and nothing else.** **THE DISPATCH SAID THE BLOCKER WAS GONE AND ADR-173 CLAUSE 6 SAYS THE APPEND STAYS STOPPED; THE ENTRY IS RIGHT AND THIS SESSION FOLLOWED THE ENTRY.** The ruling removed one of session 298's four disagreements and three stand, so what could be finished was the SPECIFICATION of the append and not the append. **EVERY CLAIM WAS RE-DERIVED AT A RUNNING PostgreSQL 16**, `0001` through `0051` forward-only under `ON_ERROR_STOP`, one fixture and seven writes, because that is the method ADR-173 used and its conclusion moved because of what the database returned. The correction written as the OLD contract instructed fails on `invalid input syntax for type uuid: "1"`; the same correction with the adjustment's id COMMITS as `wallet_entries.id = 2`, `provenance = 'correction'`, `balance_after_cents = 60000`; **a WELL-FORMED uuid that is not the adjustment's raises `ADJ-C3` by name**, which is the write ADR-173 did not need and which proves "and nothing else" rather than merely "not a bigint"; a `goodwill` credit against a NULL `account_id` COMMITS as `correction`, which is why the field is optional; the correcting DEBIT fails on `account_adjustments_debit_is_a_reversal`; and an amount at the threshold with no approval fails on `account_adjustments_dual_control_above_threshold`. **`0026`'s revoke was verified AT THE CATALOG and not off the DDL**: `information_schema.role_table_grants` gives `merit_app` exactly `INSERT, SELECT` on `admin_actions`, `wallet_entries` and `account_adjustments`, and PUBLIC nothing. **THE THRESHOLD HAS NO SOURCE ROW TO READ, WHICH IS SHARPER THAN "NO SOURCE IN THE TREE" AND IS REPORTED**: a catalog sweep of all 115 tables finds `dual_control_threshold_cents` on `account_adjustments` alone, so the column is a per-row SNAPSHOT of a policy value nothing in the schema holds, and the first row's value cannot be read from anywhere. **NO MIGRATION, NO ADR NUMBER, NO `packages/db` CHANGE, NO INVENTED THRESHOLD, NO `reason_code` FIELD, NO `SystemReason` MEMBER, NO `SqlExecutorReason` MEMBER, NO `pg` IMPORT, AND `wiring.test.ts` UNTOUCHED** and its triple unmoved, because no route module and no port method is added. **Eight defects seeded and all eight caught**, three of them in the migrations themselves so that the schema assertions are not vacuous. 33 of 33 gates, 13 of 13 invariants, typecheck 0, lint 0, format clean, **204 files / 4,452 passed / 6 skipped** against a `main` baseline of **204 / 4,443 / 6** reproduced exactly first, `falsify` clean. **Four findings reported and not repaired**, including that nothing in the database refuses a correction composed without its audit row, which is the cost ADR-173 section 6 item 1 priced and this session leaves priced. |
| [2026-08-28 - Session 318](2026-08-28-session-318.md) | **[`apps/api/src/admin-source/flags.ts`](../../apps/api/src/admin-source/flags.ts), [`graph.ts`](../../apps/api/src/admin-source/graph.ts) and [`index.ts`](../../apps/api/src/admin-source/index.ts) (all new) build [P7](../plans/P7-risk-and-abuse.md) section 8's `P7-i`: `AdminReadSource.listFlags` and `readIdentityGraph` get an adapter.** **THE ORDERING IS `AS-M7-03`'s AND `GS-120` IS ASSERTED DIRECTLY**: fifty D-01 clusters at severity 3 lose to one identity with three families at severity 2, so the noisy identity wins on both of the wrong keys and an ordering by count or by severity fails the case rather than passing a weaker one. **INDEPENDENCE IS THE INPUT AND NOT THE DETECTOR, WHICH IS THE HALF THAT IS ARITHMETIC**: D-01, D-04 and D-05 all read `fills` and write `copy_cluster`, `news_window` and `martingale`, so one family per detector OR one family per `flag_type` scores one poisoned stream at depth 3 and hands the queue to the adversary; both are seeded and both turn red. **THE FINDING IS THAT TWO APPROVED DOCUMENTS STATE DIFFERENT SORTS FOR ONE QUEUE.** API_CONTRACT section 8 says *"Sorted by severity then age"* and [`admin-reads.ts`](../../apps/api/src/routes/admin-reads.ts)'s `assertFlagOrder` enforces it flat across the page; `AS-M7-03` clause 3 says independent detector families on the identity. A corroboration page inverts severity BY CONSTRUCTION, so that assertion refuses this adapter. **It is REPORTED and not resolved**: no ADR number is allocated, `admin-reads.ts` is `P7-b`'s file, and the suite reads both sentences out of their own documents and asserts the inversion as a fact so the wiring slice meets a red test rather than a 500. **`admin-reads.ts`'s STATED REASON FOR THE BLOCK IS NARROWED AND NOT RETIRED**: *"there is no join and no aggregate to reach for"* is measured FALSE for these two methods (both are keyed reads plus ordinary code, neither reaches `sqlExecutor`) and stands in full for `readLiability`. **THE COMPOSITION FILE IS BUILT SO A DROPPED LEG IS A COMPILE ERROR**, which is the answer to the keep-both merge that emptied `apps/worker/src/index.ts` and typechecked: `IMPLEMENTED_ADMIN_READS` is data and the return type is a `Pick` over it, so neither half can be lost quietly and neither can be added alone. **THE GRAPH REFUSES RATHER THAN TRUNCATES** at its node cap, because a partial `aggregate` is wrong and looks right and `AS-M7-04` is an operator restricting a family on that screen; and a suppressed edge is SHOWN and NOT TRAVERSED, which is `SD-M7-04`'s dispute rendering beside `schema.ts`'s *"stops contributing to enforcement"*. **ADR-157's REFUSED AGGREGATE IS NOT REACHED FOR**: the latest `rule_states` row per account is an argmax done in memory and the read amplification is measured rather than hidden. **NO ADR NUMBER, NO MIGRATION NUMBER, NO `packages/db` CHANGE, NO `@merit/db` IMPORT ANYWHERE UNDER `admin-source/`, `src/db.ts` UNTOUCHED, `admin-reads.ts` AND `admin-writes.ts` READ AND NOT EDITED, `wiring.test.ts` NOT TOUCHED and its `{declared: 21, wired: 6, blocked: 15}` triple unmoved.** **Eighteen defects seeded and all eighteen caught, two of them MISSES first with the reason recorded rather than counted as a pass.** 33 of 33 gates, 13 of 13 invariants, typecheck 0, lint 0, format clean, **205 files / 4,505 passed / 6 skipped** against a baseline of 204 / 4,443 / 6 reproduced exactly first, `falsify` clean with the tree clean after it. |
| [2026-08-28 - Session 319](2026-08-28-session-319.md) | **[`apps/api/src/admin-source/evidence.ts`](../../apps/api/src/admin-source/evidence.ts) (new) is [P7](../plans/P7-risk-and-abuse.md) section 8's `P7-j`, THE SECOND DONE-GATE**: `AdminReadSource.exportEvidence` writes an `evidence_packs` row with all three `SD-M6-04` columns, and the `trader` profile's strip list is COMPUTED from `detector_definitions.is_sensitive` (`INV-M7-10`) rather than hand-listed. **`INV-M7-10`'s MECHANISM IS NOW TESTED, WHICH [SESSION 282](2026-08-27-session-282.md) SAID THE SEED COULD NOT DO**: while `is_sensitive` is uniformly `true`, a computed strip list and an unconditional one produce byte-identical output, so the distinguishing fixture is a REGISTRY and not a seed row, and the union is asserted separately because `severity` and `window_trading_days` belong to several detectors at once. The seed's uniformity is pinned, so the first `false` turns this suite red. **THE SUITE FOUND A REAL DEFECT IN THIS SESSION'S OWN CONTROL**: the no-other-identity check used an ANCHORED uuid match and was blind to an identifier CONTAINED in a string, and `tos_clause` is free text `GS-112` REQUIRES a trader pack to carry, so *"coordinated with account `<uuid>`"* leaked through a field the pack must not drop; repaired, pinned, and the end-to-end refusal watched firing on that clause. **FOUR CORPUS FINDINGS, EACH READ AT SOURCE**: there is NO `gate_results` table and `SD-06` split it into `rule_states.engine_gates` and `context_gates`, so the pack carries two columns and the suite asserts them by name; `risk_flags` carries NO ToS clause column, the clause living on the enforcement that cites the flag with `DEP-M7-05` still owing two of three texts, so the pack carries `null` and NEVER INVENTS ONE; `redaction_profile`'s vocabulary is still UNRULED, the structure transcribed and the two strings LABELLED AS CHOSEN on `P7-d`'s precedent, **a ruling REPORTED AS NEEDED and NO ADR NUMBER TAKEN**; and this slice TAKES NO HANDLE OFF THE ACCESSOR because [ADR-171](../decisions/ADR-171.md) refused the operator door and [`db.test.ts`](../../apps/api/test/db.test.ts) pins the map, so rows arrive through ports. **THREE CONTROLS THAT FAIL DIFFERENTLY**: an allowlist projection, a sweep computed from the registry, and a refusal that THROWS rather than repairing, with nothing stored and no row written when it fires. **A BARE `2` IS NOT A THRESHOLD BY ITS VALUE, IT IS A THRESHOLD BY THE NAME IT ARRIVES UNDER**, which is why the list is names, asserted in both directions. **Seventy-eight negative cases, one per seeded parameter name**, plus the other identity and the thresholds individually. **`evidence.pack_exported` is NOT emitted and is reported as owed**; the audit is the row, which `0008_risk.sql` calls *"ITSELF AN AUDITED ACT"*. **THE `P7-i` COLLISION ON `admin-source/index.ts` HAS NOT HAPPENED YET AND IS STILL COMING**: no branch pushed and nothing on `main`, so the composition was CREATED here, written for the merge, with every unimplemented method THROWING WITH ITS OWN NAME so a keep-both merge that drops a leg is loud instead of compiling. **Nine seeded defects, all nine caught.** **AND WRITING THIS LOG FOUND A LATENT DEFECT IN [`scripts/corpus/gates.mjs`](../../scripts/corpus/gates.mjs)**: its span writer interpolates generated text into a `String.replace` REPLACEMENT argument, whose substitution vocabulary is not literal text, so an index line carrying a dollar sign before a backtick wrote the whole preceding file back into the span and took `README.md` from 549 lines to 743. **`generate` exited 0 and `CI-06g` caught it on the next run.** Reported and NOT repaired: the runner is outside this fence and every gate depends on it. No migration, no ADR, no `SystemReason` member, no `SqlExecutorReason` member, no `pg` import, no cast past a key type, and `admin-reads.ts`, `wiring.test.ts`, `schema.ts` and `scope.ts` read and NOT touched. 33 of 33 gates, 14 of 14 invariants, typecheck 0, lint 0, format clean, 205 files / 4,604 passed / 6 skipped, against a reproduced baseline of 33 / 13 / 0 / 0 / clean / 204 / 4,443 / 6. |
| [2026-08-28 - Session 320](2026-08-28-session-320.md) | **[P7](../plans/P7-risk-and-abuse.md) section 8's `P7-k`, the THIRD DONE-GATE. `plan_breaker_state` landed in [`0016`](../../packages/db/migrations/0016_treasury_controls.sql) and [`apps/admin/src/page.ts`](../../apps/admin/src/page.ts) has said *"nothing writes it"* ever since; it has a writer now.** `GS-113` is asserted with its NEAR-MISS beside it. **`INV-M5-12` IS THE WRITE UNION AND NOT A COMMENT**: `BREAKER_WRITE_TABLES` has EXACTLY ONE MEMBER, so no payout, wallet, halt or restriction table is reachable through the port, while the READ side carries `payoutRequests` because the loss ratio's numerator IS settled payouts. **THE NAMED SEED WAS RUN IN BOTH DIRECTIONS AND THE SECOND IS THE ONE WORTH READING**: written straight, a payout insert is a COMPILE ERROR (`TS2345`); written with the cast that gets past `tsc`, **the typechecker reports ZERO errors** and ten cases go red, which is why the invariant is enforced at four levels rather than one. **`insufficient_data` IS THE STATE BELOW THE MINIMUM AND SALES ARE NOT PAUSED THERE, AND THE LADDER'S ORDER IS THE CONTROL**: the floor is tested BEFORE the ratio and the ratio is not consulted below it, because a ladder that computed an opinion and discarded it is one edit from reporting it. `AS-M6-02`'s own scenario is the fixture: one purchase, one 150,000c payout, **151,515bp against a 6,000bp threshold**, and `salesPaused` FALSE. **`sample_size` IS WRITTEN BESIDE `min_sample` ON EVERY ROW AND EVERY STATE** and `breaker.state_changed` carries both as REQUIRED fields, transcribed from [M06:265](../plans/M06-admin-ops-console.md) parsed out of the document; **that event has NO row in [EVENTS](../architecture/EVENTS.md)** and the absence is PINNED. **`ADR-167` SECTION 5 IS APPLIED AND THE TERM IT COULD NOT PLACE IS APPLIED WHERE THE ENTRY PUT IT**: `sample_size` is the DENOMINATOR's count, and `OQ-M6-02`'s second term reaches `decideState` and NO COLUMN AT ALL, because a row holding the payout count *"would satisfy every CHECK in `0016` and describe the wrong population"*. **NO NUMBER IN THIS SLICE IS THIS SESSION'S**: both `OQ-M6-02` terms ship `unstated`, so **the evaluator DECLINES**, a floor of zero being refused by `min_sample > 0` and a floor of one being `AS-M6-02` produced deliberately. **THE CUSUM IS FOLDED IN INTEGER BASIS POINTS AND RENDERS ABSENT** (`ADR-167` clauses 4 and 5), with the ONE remaining division declared rather than hidden: the pass rate truncates downward, at most 1bp per day, and the bias accumulates toward NOT alarming. **THE API HALF DECLARES NO PORT AND THAT IS A RULING**: `CompositionReport` moves operator `registered` 24 to 26 and public `withheld` 23 to 25, and [`wiring.test.ts`](../../apps/api/test/wiring.test.ts)'s `{declared: 21, wired: 6, blocked: 15}` triple DOES NOT MOVE, because both endpoints are section 8's *"focused projections of the same underlying data"* and compose with `admin-reads.ts`'s handler. **A POPULATED CUSUM ON THE SOURCE RENDERS AS `null`**, because `DEP-M6-05` is Wave 4 and a second surface drawing the chart `apps/admin` refuses to draw is the same failure with a different renderer. **THREE ABSENCES ARE REPORTED AND NOT REPAIRED**: `per_plan[].sample_size` and the three-state distinction behind `sales_paused` both need a field on `LiabilityResponse.per_plan`, which is `P7-b`'s file; and [`admin-reads.test.ts`](../../apps/api/test/admin-reads.test.ts)'s unserved register is **STILL GREEN while two of its three rows are now SERVED**, because it derives `served` from `ADMIN_READ_ENDPOINTS` rather than from the composition report. **THE BARREL BIT EXACTLY AS THE DISPATCH WARNED**: deleting BOTH `detectors/graph.ts` legs leaves `typecheck` at **ZERO ERRORS**, and one assertion catches it, the barrel's **17** legs enumerated. **A LATENT DEFECT IN [`db.test.ts`](../../apps/worker/test/db.test.ts) WAS FOUND BY WALKING INTO IT**: its bare-specifier scan reads a WRAPPED message ending in the word *"from"* as an import, the exact hazard `admin-reads.test.ts` anchors against and this one does not. **HALF of [CRON_INVENTORY](../ops/runbooks/CRON_INVENTORY.md)'s numbered finding is closed and the row says which half.** **Fifteen defects seeded and fifteen caught.** 33 of 33 gates, 13 of 13 invariants, typecheck 0, lint 0, format clean, **206 files / 4,515 passed / 6 skipped** against a `main` baseline of 204 / 4,443 / 6 reproduced exactly first, `falsify` clean. **NO ADR NUMBER, NO MIGRATION NUMBER, no `packages/db` change, no contract change, no `SqlExecutorReason` or `SystemReason` member, no `pg` import and no cast past a key type.** |
| [2026-08-28 - Session 321](2026-08-28-session-321.md) | **[ADR-177](../decisions/ADR-177.md) lands, `status: proposed`, approval line UNSIGNED, and `0052` IS TAKEN** as [`0052_chart_of_accounts.sql`](../../packages/db/migrations/0052_chart_of_accounts.sql), `E2 READ: MONEY PATH`. **[ADR-174](../decisions/ADR-174.md)'s finding (C) is NOT one absence: it is three different things wearing one label and only two of them are silence.** **FOUR KINDS ARE RULED, each DERIVED from a posting the corpus already states** read through [`posting.ts:235-236`](../../packages/ledger/src/posting.ts)'s sign: `fees_revenue` is `revenue` (`LT-01` credits it as recognized revenue while [ADR-067](../decisions/ADR-067.md) and [`0038`](../../packages/db/migrations/0038_account_adjustments.sql) DEBIT it and both say that reduces revenue, and [`checkout.ts:1671`](../../apps/api/src/routes/checkout.ts) states the conclusion outright in shipped source), `trader_wallet` and `trader_withdrawable` are `liability`, and **`promotional_credit` is `liability` DERIVED FROM AN ENFORCED CONSTRAINT rather than from prose**, through [`0038:420-425`](../../packages/db/migrations/0038_account_adjustments.sql)'s sign table on a destination that file's own CHECK admits and its header calls a posting that *"books a liability"*. **THREE ARE REFUSED AND THE CHECK ENDS IN `ELSE true`, which is where the open question is stored.** **THE OBLIGATION TO READ `LT-06`'s ROW IS DISCHARGED AND THE DEFECT IS BIGGER THAN [ADR-174](../decisions/ADR-174.md) MEASURED**: `LT-02` reads backwards too, in the INTERNAL leg that entry's fence excluded. `LT-02` debits `firm_treasury` at settlement, `LT-06` credits it at approval and `LT-07` debits it at settlement, and **all three read correctly as a `liability` and backwards as an `asset` while all five prose sites call it cash**, so the arithmetic is unanimous on one side and the prose on the other and **no literal in a merged migration could be right about both halves**. **ONE ROW IS SEEDED AND ONE IS ALL A MIGRATION CAN SEED**, because only `fees_revenue` is both firm-scoped and settled; `ledger_accounts` goes from **0 rows to 1**, the first it has ever held. **SEVENTEEN PERTURBATIONS EXECUTED against PostgreSQL 16 with `0001` through `0052` applied forward-only from empty, 114 base tables, and every one behaved as designed**, including **the hole watched rather than asserted**. **A DEFECT NOT IN THE DISPATCH BREAKS A CI GATE BY GOING QUIET**: the ledger probe seeded its own `fees_revenue` under `ON CONFLICT DO NOTHING`, so once the seed exists that clause SKIPS the row and the `LEDGER-C1` block raises `LEDGER-C2` on a dangling account instead of probing C1 at all; repaired to READ the chart and falsified both ways. **[ADR-174](../decisions/ADR-174.md) SECTION 7's THREE ARMED ASSERTIONS ALL FIRED**, which is what they were armed for, and are updated to assert the answer rather than deleted. **THE APPEND-ONLY GRANTS GATE REFUSED A WIDENING AND THE REFUSAL IS RECORDED RATHER THAN ROUTED AROUND.** **Twenty-two defects seeded and all twenty-two caught, and TWO WERE MISSES FIRST with the reason recorded rather than counted as a pass**, both the same reason: `0052`'s prose-heavy header quotes its own statements, so two different files read the argument as SQL, one counting two INSERTs and one counting none. 33 of 33 gates, 14 of 14 invariants, typecheck 0, lint 0, format clean, **209 files / 4,773 passed / 6 skipped** against a `main` baseline of 208 / 4,764 / 6 reproduced exactly first, `falsify` clean. |
| [2026-08-28 - Session 322](2026-08-28-session-322.md) | **[ADR-170](../decisions/ADR-170.md) foreclosure 9 says *"no route is registered, no handler is written ... The rows are the specification a later slice transcribes, and the `E2` read is owed on the handlers"*. This session is that later slice.** [`apps/api/src/routes/verify.ts`](../../apps/api/src/routes/verify.ts) is `GET /verify/:code` and [`admin-certificates.ts`](../../apps/api/src/routes/admin-certificates.ts) is `POST /admin/certificates/:id/revoke`, both new files, both registering themselves off the directory listing with **no `registry.ts` edit**, verified by composing rather than assumed. **THE RESPONSE UNION IS THREE WHERE THE LOG'S CHECK IS FOUR AND THE COMPILER PROVES THE CONTAINMENT RUNS ONE WAY**: `VERIFY_RESULTS` is declared `satisfies readonly VerificationResult[]`, so a response value `certificate_verifications` cannot record does not compile, while `deferred` stays in the table because a deferred lookup is a leaked token and an unknown one is a typo. **THE CONSTANT-TIME MECHANISM IS A MEASURED FLOOR AND IT IS APPLIED TO EVERY RESPONSE THE HANDLER PRODUCES, THE 503 INCLUDED**, with the `certificate_verifications` write inside its budget and an overrun LOGGED rather than failed, because a p99 ceiling is exceeded one request in a hundred by construction. **`floor_ms` IS BOUNDED BELOW AND NOWHERE ABOVE, and that is reported rather than filled**: no approved document gives an upper bound and one invented here would be this route choosing a latency budget the corpus has not chosen. **THE COPY TABLE IS VALIDATED IN FULL BEFORE THE LOOKUP AND THAT IS A TIMING CONTROL RATHER THAN TIDINESS**: read lazily on the branch that needs it, a deployment missing only the `account_enforced` sentence answers `unknown` in milliseconds and `valid` in an error, which is a hit-versus-miss oracle assembled out of the configuration error rather than out of the clock. `INV-M11-03`'s wording is the ONE sentence fixed in code; the other five and `INV-M11-04`'s disclosure are supplied, because `OQ-M11-02` is open and copy invented here would answer a legal question in a string literal. **`initiative` IS DERIVED FROM `revocation_class` AND A REQUEST THAT SUPPLIES IT IS REFUSED RATHER THAN IGNORED**, because an operator who sent it believes they set it; `INITIATIVE_BY_CLASS` is typed total over the CHECK's four members and the biconditional is satisfied by construction on every one. **TWO FINDINGS CAME OUT OF SEEDS THAT FAILED, BOTH PRE-EXISTING AND BOTH TREE WIDE**: a `:code` over 100 characters answers **414 in Fastify's own shape**, `application/json`, with an `FST_ERR_MAX_PARAM_LENGTH` code section 2 does not define and **the request path echoed back**, which is a length oracle ahead of the floor and escapes `server.ts`'s closed status table entirely; and a `:code` of `..` is normalized by the router to a traversal and answers the router's 404. **Both reproduce on `GET /certificates/:code/image.png` and `GET /accounts/:accountId/purchases`**, so neither is this slice's and neither is repaired here. **THE DISPATCH'S ROUTE COUNT DID NOT REPRODUCE**: it said 69 registered and the composition report says **70** distinct `METHOD /path` (45 public plus 26 operator, `GET /health` on both), moving to **72**. The central claim held: neither row was registered. **BOTH PORTS ARE BLOCKED AND NO ADAPTER IS WRITTEN, AND EACH ENTRY NAMES WHICH ARM HAS A DOOR**: `VerifySource.record` is writable through `db.firm` today because `certificate_verifications` is scope class `firm`, while `lookup` reaches neither door; the revoke's own obstruction is a CIRCULARITY, since `db.scoped` needs an identity that is a column of the row the door would be opened to read. `{declared, wired, blocked}` moves **21/6/15 to 23/6/17**. **`certificates.test.ts`'s assertion that nothing registers `/verify/` was RIGHT WHEN WRITTEN and is restated over the module rather than deleted**, because its purpose was that no verify row arrive without a ruling and it did that job. **Fourteen effective defects seeded and fourteen caught, and four seeds that DID NOT BITE are reported rather than counted**: two were unreachable BECAUSE of the design (a `certificates.id` leak cannot be written while `VerifyRow` has no such field; an `initiative` read from the body is dead behind the validation refusal), one was a no-op edit, and one did not apply. **The timing seed is a live case**: a source costing 60ms on a hit and nothing on a miss, with both answers held to the floor. 33 of 33 gates, 14 of 14 invariants, typecheck 0, lint 0, format clean, **210 files / 4,885 passed / 6 skipped** against a `main` baseline of 33 / 14 / 0 / 0 / clean / 208 / 4,764 / 6 reproduced exactly first, `falsify` clean. **NO ADR NUMBER, NO MIGRATION NUMBER, no `packages/db` change, no contract change, no `SystemReason` or `SqlExecutorReason` member, no cast past a key type, and `packages/ledger`, `packages/db/migrations`, `admin-reads.ts`, `admin-source/` and `apps/worker/src/index.ts` untouched. E2 READ OWED ON THE HANDLERS.** |
| [2026-08-28 - Session 323](2026-08-28-session-323.md) | **[P7](../plans/P7-risk-and-abuse.md) section 8's `P7-l`. [`0040`](../../packages/db/migrations/0040_report_schedules.sql) landed `report_schedules` and `report_deliveries` with ZERO ROWS on merge, deliberately, "so nothing below can be read as evidence that any digest has ever been delivered"; both named digests have producers now and so does the dead-man switch.** **`CRON_INVENTORY:35`'s RULE IS A PROPERTY OF A PARAMETER LIST AND NOT A COMMENT**: `DigestAlarmIo` carries a transaction and a clock and NOTHING a producer could hand it, [`alarm.ts`](../../apps/worker/src/digests/alarm.ts) imports nothing from `produce.ts` and the suite reads its source as text, and the handle the alarm receives HAS NO `insert`. A job that crashed after writing "success" and a job that never ran are the same fact to the person who did not get the digest, so they are the same input: neither is an input at all. **`GS-288` IS ASSERTED IN THREE SHAPES**, and the named one is `5.1`: the transport succeeds, the producer builds a valid `delivered` row, the insert is swallowed, the run reports **`delivered: 1`**, the table holds **nothing**, and the alarm fires. **THE ANCHOR IS UNSTATED AND NO NUMBER WAS INVENTED FOR IT**: the cadence is a schema fact confirmed against PostgreSQL 16 for all four digests, but WHICH weekday at WHICH hour in WHICH zone is stated nowhere, so `DIGEST_WINDOW_ANCHOR` ships EVERY term `unstated` and the alarm measures elapsed time from each schedule's OWN history, which is the same fact up to one period late; **the alternative was to invent a weekday, and an alarm firing against a window nobody agreed to is `GS-287` with the sign flipped**. **`CADENCE_PERIOD_MS.monthly` IS `null`** and a monthly schedule is reported as `cadence_unanchored` rather than passed over, because a 30-day constant would have been a number this slice invented. **THE `P7-l` ROW CALLS THESE "the two MUST digests" AND [M06](../plans/M06-admin-ops-console.md) SECTION 3.6 AND [ADR-066](../decisions/ADR-066.md) SECTION 3 BOTH DISAGREE**: `weekly_flag_queue` is sized **SHOULD**, the second MUST is `daily_liability`, and its content waits on `P5-l`'s `readLiability`. Recorded beside `PRODUCED_DIGESTS`, not repaired. **A DIGEST WITH NO PRODUCER GETS NO ROW**, which is `0040`'s own design: there is no `skipped` outcome and "it writes nothing and the missing row is itself the finding". **`GS-290` IS ENFORCED TWICE**, a downgrade and a refusal, and **all eleven `report_deliveries` `CHECK`s were watched firing in PostgreSQL 16**, including the documented surprise that a fifth digest is refused by `cadence`'s `NOT NULL` rather than by `digest`'s own `CHECK`. **`INV-M6-10` IS SWEPT OVER THE RENDERED ARTIFACT** and the flag-queue link points at the QUEUE and never at a subject, a reading stated rather than assumed. **THE BARREL'S LEGS ARE DATA NOW AND A SEED PROVED MODULE GRANULARITY IS NOT ENOUGH**: deleting ONE of a module's two export blocks left the module a specifier, left the leg check green and left `typecheck` at ZERO ERRORS, so `9.4` asserts the barrel re-exports every NAME of this slice's four modules. **Writing the lists found [`batch/statistics.ts`](../../apps/worker/src/batch/statistics.ts), which this barrel has never re-exported**; recorded, not repaired. **TWENTY-ONE DEFECTS SEEDED AND ALL TWENTY-ONE CAUGHT, TWO AS MISSES FIRST**, and **THE SUITE FOUND A REAL DEFECT IN THIS SESSION'S OWN CODE**: `report_deliveries_failure_states_its_reason` was written with one negation too many, refusing every well-formed `delivered` row and accepting every `failed` one. **TWO FENCE NOTES**: `breaker.test.ts` carries a hand-written leg count any barrel slice invalidates, corrected 17 to 21 and reported; and **session 320's reported defect in [`db.test.ts`](../../apps/worker/test/db.test.ts) bit again**, its scan reading a wrapped message ending in *"from"* as an import, reworded inside this fence, **the regex still wrong and still nobody's**. 33 of 33 gates, 14 of 14 invariants, typecheck 0, lint 0, format clean, **209 files / 4,837 passed / 6 skipped** against a `main` baseline of 208 / 4,764 / 6 reproduced exactly first, `falsify` clean. **NO ADR NUMBER, NO MIGRATION NUMBER, no `packages/db` change, no contract change, no `SqlExecutorReason` or `SystemReason` member, no `pg` import, no cast past a key type, and `apps/api` untouched so the `{declared, wired, blocked}` triple does not move.** |
| [2026-08-28 - Session 324](2026-08-28-session-324.md) | **`RI-15` LANDS, and it is the LOCATION half of the error class `RI-14` reads the EXISTENCE half of.** A `file:line` inside a written-down reason must resolve, the file must reach the line, and where the pointer is preceded by a backticked name with nothing but glue between them that name must appear within **two lines** of the line cited. **THE WINDOW IS TUNED AGAINST THE CORPUS AND STATED**: the widest TRUE citation measured is one line off and the narrowest FALSE one on record is three, so two is the largest window that catches every false citation on record and the smallest that admits every true one. **WATCHED FAILING ON THE REAL HISTORICAL DEFECT, ON THE REAL TREE**: all four false citations of 2026-08-28 restored into [`wiring.test.ts`](../../apps/api/test/wiring.test.ts), **three fire at four sites**, the fourth is the STATED MISS because `wallet-withdrawals.ts:1506` sits behind the words *"the identity arm this route presents"* and names nothing a runner can look up, and the tree was restored byte for byte with `git status` clean. **THE FIRST DRAFT WAS ARMED FOR 215 LINES AND DISARMED FOR THE REST AND REPORTED PASS**: it paired backticks from the start of the file and `wiring.test.ts:215` uses a backtick as an APOSTROPHE, which inverts every pairing below it; all three copies of the seeded defect went quiet. It now reads the two backticks immediately before the pointer. **THE CHECK FOUND THREE FALSE CITATIONS ON `main` ON ITS FIRST RUN, WHICH MAKES THIS THE SIXTH APPEARANCE OF THE CLASS.** [`routes/payouts.ts:84,87`](../../apps/api/src/routes/payouts.ts) were **FALSE THE DAY THEY WERE WRITTEN**, checked by hand at `e04202a`, and are corrected here. [`wiring.test.ts:168`](../../apps/api/test/wiring.test.ts) cites `routes/admin-wallet.ts:538` for `principal(request)`, declared at `:601`: **TRUE at `563ac3d` and drifted 63 lines by `224fe5b`, a FIFTH false citation in that file**, and it is **NAMED AND NOT ENFORCED** in `CITATIONS_OWNED_ELSEWHERE` because session 322 owns the file, the entry being exact and inert the moment the pointer is corrected. **`docs/decisions` IS DELIBERATELY NOT AN INPUT**: an ADR is a dated record of the tree as it was, its citations go stale by design, and both findings over `ADR-170`..`ADR-176` are this check misreading a document written for a reader. **Twelve seeded cases, both quiet rules asserted in BOTH directions, the window asserted at both boundaries.** Second: **`apps/admin` IS THE FOURTH NO-SERVER APP AND THE REGISTER COUNTS THREE.** Re-derived: **2,802 lines over nine `src/*.ts` files, 200 passing tests in eight suites, NO `build` script**, and the transport grep returns NOTHING. `index.ts:126` says *"there is no server here to receive"* and `main()` prints *"no server yet"*: **the FILE is honest and the REGISTER is not**, and `ADR-171` refused the operator door on the admin identity provider while a second blocker on the same surface went unwritten. No server built. 33 of 33 gates, **15 of 15 invariants**, typecheck 0, lint 0, format clean, 208 files / 4,777 passed / 6 skipped against a `main` baseline of 33 / **14** / 0 / 0 / clean / 208 / 4,764 / 6 reproduced exactly first, `falsify` clean. **NO ADR NUMBER AND NO MIGRATION NUMBER**, `packages/ledger`, `packages/db`, `apps/api/src/routes/admin-reads.ts`, `apps/api/src/admin-source/` and `apps/worker/src/index.ts` untouched, and `wiring.test.ts` READ and never edited. |
| [2026-08-28 - Session 325](2026-08-28-session-325.md) | **[ADR-178](../decisions/ADR-178.md) LANDS, `status: proposed`, approval line UNSIGNED. THE RULING IS THAT CORROBORATION TAKES THE FIRST KEY AND THE CONTRACT'S SENTENCE IS SCOPED RATHER THAN DELETED.** [API_CONTRACT](../architecture/API_CONTRACT.md) section 8 said *"Sorted by severity then age"* and `assertFlagOrder` enforced it FLAT across the page; `AS-M7-03` clause 3 sorts by INDEPENDENT detector families on the identity; a corroborated page inverts severity by construction, so the route refused the adapter `P7-i` built. **`AS-M7-03` clause 3 STANDS UNAMENDED and API_CONTRACT section 8 is AMENDED IN PLACE** ([ADR-065](../decisions/ADR-065.md) T3), its severity-then-age kept as the order WITHIN one corroboration band. **THE GROUND IS THAT A CONTROL OUTRANKS A CONVENTION**: `AS-M7-03` names an attack, an adversary who needs no access to Merit, a failure mode and `GS-120`; section 8's sentence states an order and no ground at all. **AND THE CONTRACT'S STRONGEST GROUND IS MEASURED FALSE IN THIS TREE**: `grep` over `apps/admin`, `apps/portal` and `apps/site` finds NO client of `/admin/flags`, no file outside `apps/api` names `FlagListItem`, and `setAdminReadSource` is still `BLOCKED`, so the route answers 503 before this ruling and after it. **THE PRICE IS PAID ON THE WIRE AND IS THE HALF THAT MAKES THIS DIFFERENT FROM RULING FOR ONE DOCUMENT AND DELETING THE OTHER'S ENFORCEMENT**: `FlagListItem` gains `corroboration_depth`, because `assertFlagOrder` cannot check a key it cannot see and an operator shown a severity 3 above a severity 5 has nothing on the row that says why. **THE ASSERTION GOT STRONGER RATHER THAN WEAKER**, two keys to three, and it now enforces the control it used to refuse. **`P7-i`'s SUITE ASSERTED THE INVERSION AS A FACT AND THAT ASSERTION IS INVERTED DELIBERATELY, NOT DELETED**, keeping `GS-120`'s fixture and the observation that the page still inverts severity, which is why the ruling was needed; its two SOURCE-TEXT matches are replaced by behavioural ones over the now-exported function. **A PREMISE THE RECORD CARRIED CAME BACK FALSE**: [STATE](../STATE.md) and [session 318](2026-08-28-session-318.md) both record the reconciliation as *"the order WITHIN one identity"*, and `compareRanked` compares `corroboration.depth` and never `identityId`, so two identities at the same depth INTERLEAVE. **The band is a DEPTH**, both records are corrected beside their originals rather than overwritten, and the amended contract sentence says which. **TEN SEEDS, ALL TEN CAUGHT, ONE A MISS FIRST**: `projectFlag` shipping a constant depth was caught by NOTHING, because `assertFlagOrder` runs on the port's rows BEFORE the projection, so a wrong value there reaches the operator with the adapter right and the assertion passing; one new case over the SERVED BODY closes it and it was re-seeded and caught. **A FENCE CAUGHT ME AND WAS NOT WIDENED**: `P7-i` asserts `admin-reads.ts` names its implementer nowhere, the retargeted docstring cited the adapter by path, and the prose was reworded. **NO MIGRATION, NO COLUMN, NO INDEX, NO `SystemReason` MEMBER, NO `SqlExecutorReason` MEMBER, NO `pg` IMPORT, NO CAST PAST A KEY TYPE, `packages/db` UNTOUCHED, `0008` BYTE FOR BYTE UNCHANGED, ADR-157's REFUSED AGGREGATE NOT REACHED FOR, `wiring.test.ts` NOT TOUCHED and its `{declared: 21, wired: 6, blocked: 15}` triple unmoved.** 33 of 33 gates, 14 of 14 invariants, typecheck 0, lint 0, format clean, **208 files / 4,765 passed / 6 skipped** against a baseline of 208 / 4,764 / 6 reproduced exactly first, `falsify` clean with the tree clean after it. |
| [2026-08-28 - Session 326](2026-08-28-session-326.md) | **[ADR-180](../decisions/ADR-180.md) lands, `status: proposed`, approval line UNSIGNED, and `0053` IS TAKEN** as [`0053_firm_treasury_kind.sql`](../../packages/db/migrations/0053_firm_treasury_kind.sql), `E2 READ: MONEY PATH`. **THE RULING IS THAT `firm_treasury` IS AN `asset`: THE PROSE IS RIGHT AND THE ARITHMETIC IS WRONG.** Both halves were re-derived at their own sources rather than inherited from the dispatch or from [ADR-177](../decisions/ADR-177.md), and both are CONFIRMED unanimous; **the prose side is one site larger than that entry counted**, because [`payouts.ts:773`](../../apps/api/src/routes/payouts.ts) states it in shipped source. **WHAT SEPARATES THEM IS NOT THEIR UNANIMITY BUT WHAT EACH UNANIMITY IS MADE OF.** The load-bearing ground is that **under `liability` the chart of accounts holds NO CASH ACCOUNT AT ALL and nothing in this tree has ever recorded that**, while the absence the corpus DID record is the other one, [ADR-174](../decisions/ADR-174.md) clause 4's missing in-flight obligation. **[`treasury_balances`](../../packages/db/migrations/0009_ledger.sql) IS SCHEMA RATHER THAN PROSE AND ONLY PARSES AS CASH**, its balance being the RAIL's reported balance under `source CHECK IN ('provider_api','manual_attestation')`; stated at its real strength, `account_code` carries no key into the chart, so it is an argument from the table's SUBJECT. **THE ARITHMETIC'S UNANIMITY IS ONE HAND AND NOT THREE WITNESSES**: three cells of ONE table, at the site [ADR-027:73](../decisions/ADR-027.md) records three direction-or-class errors landing on in a single day with a fourth inside the ADR describing them. **FOUR OF THE PROSE SITES ARE DECISIONS RATHER THAN DESCRIPTIONS** and no session has ever declined an action on the ground that the account is an obligation. **The name is recorded and relied on for nothing.** **[ADR-174](../decisions/ADR-174.md) FINDING 6's ZERO IS NAMED AS EVIDENCE FOR NEITHER SIDE**, because it holds for every kind, which that entry said in terms. **ALL THREE ROWS ARE AMENDED AND `LT-02` IS ONE OF THEM**: [M05](../plans/M05-payout-system.md) section 2.1 moves ONLY the leg naming `firm_treasury`, each row keeping its original text quoted inside it on [ADR-027](../decisions/ADR-027.md)'s own precedent in that table; `LT-02` and `LT-07` CREDIT it at settlement and **`LT-06` does not name it at all**, and all three are marked UNPOSTABLE, which is stronger than what they carried before. **[ADR-174](../decisions/ADR-174.md) REFUSED THIS AMENDMENT AND ITS GROUND IS RECORDED AS NO LONGER HOLDING** rather than stepped around: the row could not be half-corrected while neither leg was determined, and now one is. **The `LT-07` repair is untouched and is now the only open question on that row.** **`LT-01` IS OUT OF FENCE, WAS CHECKED ANYWAY AND READS CORRECTLY**, as does `LT-08`. **EXECUTED against PostgreSQL 16 with `0001` through `0053` applied forward-only from empty under `ON_ERROR_STOP`, 114 base tables**: fourteen perturbations, every one as designed, including **`asset` ACCEPTED with the seed deleted inside the transaction first, so acceptance is watched rather than assumed**, and **the hole still watched** with `psp_clearing` and `reserve` both still ACCEPTING any kind. **A DEFECT WAS FOUND THAT WAS NOT IN THE DISPATCH AND IT IS IN A FILE `E2` MAKES UNAMENDABLE**: [`0052:115-116`](../../packages/db/migrations/0052_chart_of_accounts.sql) says asset-shaped prose *"turned out to be **wrong** for firm_treasury"* where [ADR-177](../decisions/ADR-177.md) section 4 says *"turned out to be **contradicted**"*, so the migration names a loser the entry it transcribes refused to name, and names the half this ruling finds was RIGHT. **[ADR-177](../decisions/ADR-177.md)'s THREE ARMED ASSERTIONS ALL FIRED** and are updated to assert the answer rather than deleted, and [`chart-of-accounts-kinds.test.ts`](../../packages/ledger/test/chart-of-accounts-kinds.test.ts) now reads the constraint IN FORCE rather than `0052`'s by name, gaining three cases that watch the SUPERSESSION itself. **THIRTY-ONE DEFECTS SEEDED, ALL THIRTY-ONE CAUGHT, AND TWO WERE MISSES FIRST, ONE OF THEM A WATCHER THAT STAYED GREEN THROUGH THIS SESSION'S OWN RULING.** **THE FOUNDER QUESTION IS REAL AND IS NAMED PLAINLY**, and the entry names where it is most likely wrong. 33 of 33 gates, 15 of 15 invariants, typecheck 0, lint 0, format clean, **212 files / 4,984 passed / 6 skipped** against a `main` baseline of 212 / 4,981 / 6 reproduced exactly first, `falsify` clean with the tree clean after it. |
| [2026-08-28 - Session 327](2026-08-28-session-327.md) | **[ADR-179](../decisions/ADR-179.md) LANDS, `status: proposed`, approval line UNSIGNED. `evidence_packs.redaction_profile` IS A CLOSED SET OF TWO**, `full-detail` and `trader-facts-only`, in the shape [`scoped-db.ts:197`](../../packages/db/src/scoped-db.ts)'s `SystemReason` and [`:1695`](../../packages/db/src/scoped-db.ts)'s `SqlExecutorReason` already have: a union DECLARED in one place, a source-reading case that says how many members it has, and a widening that is therefore a diff on a test which says why the set is closed. **THE GROUND IS THAT THIS COLUMN IS THE RECORD OF A PROMISE AND NOT AN INPUT TO ONE**, [M06:301](../plans/M06-admin-ops-console.md) making the row the answer to a later argument about what was disclosed, so a free-text label lets two exports that performed the same redaction be labelled differently and lets one row name a redaction **no code in this tree performs**. **THE OPENNESS WAS NOT WHERE THE ALLOCATION ROW SAID IT WAS AND THAT IS THE FINDING.** The row described *"a redaction profile somebody invents at a call site"*; no call site could, because `EvidenceRedactionProfile` was DERIVED from the audience map and was therefore exactly the two values everywhere it was used. **It was open at exactly one line, and that line is the line a vocabulary change is made on**: `regulator: 'regulator-detail'` in the map type-checked **exit 0**, a derived vocabulary being whatever its map says, and is now `TS2322` twice. **THE MAPPING AND THE VOCABULARY ARE NOW SEPARATE THINGS**, which matters because `OQ-F4-03` may yet give `regulator` its own profile: under the old shape that legitimate MAPPING ruling grew the VOCABULARY as a side effect, turning red only two assertions that read as *"update me"* to the session making it. **NO PARALLEL ARRAY OF THE NAMES IS DECLARED**, on [ADR-092](../decisions/ADR-092.md)'s `DDL_NAMES` landmine, and totality is asserted between the declaration and its one consumer in both directions instead. **EACH MEMBER'S PROMISE IS WRITTEN PER MEMBER AND IS THE UNION OF THREE APPROVED STATEMENTS RATHER THAN THE SHORTEST OF THEM**: [M06:485](../plans/M06-admin-ops-console.md)'s `GS-112` names five positives and three negatives, [M06:299](../plans/M06-admin-ops-console.md) and [EC-071](../edge-cases/EC-071.md) name six and four, adding **the computation trace** and **"no comparison against a population"**. `trader-facts-only` strips four things by four mechanisms and the four negatives map onto them one for one, the population comparison being carried by the ALLOWLIST rather than the sweep; `full-detail` strips nothing and **bounds the DETAIL and never the SCOPE**, which its name does not say. **TWO SEEDS WERE MISSES AGAINST THE 155 CASES THAT EXISTED**: `'detector'` and `'detector_version'` joining `TRADER_FLAG_FIELDS` ship the name of the machinery that found a flag into a trader pack, which [ADR-022](../decisions/ADR-022.md) puts at internal tier always, and every one of the 155 passed, because the positives and negatives were each asserted ONE FIELD AT A TIME and nothing said the carried set was EXACTLY the promise. **A promise is a set.** **Ten seeds, all ten caught after**, including the anchored uuid match `P7-j` repaired, watched deliberately because this ruling is written around it. **NO MIGRATION AND NO MIGRATION NUMBER, AND THE CLOSURE'S LIMIT IS STATED**: it holds at the layer that COMPOSES the value and not at the layer that STORES it, [`0008_risk.sql:257`](../../packages/db/migrations/0008_risk.sql) carrying no `CHECK` and [`schema.ts:1836`](../../packages/db/src/schema.ts) typing the column `text`, with the DDL for both constraints written out in the entry and `ADD CONSTRAINT` safe today only because the writer is a port nothing implements. **THREE THINGS REPORTED AND NOT BUILT**: the computation trace, which `grep` shows is no column because [M01:772](../plans/M01-rules-engine.md) makes a trace a REPLAY, so the pack carries its inputs; the free-text channel, closed for the uuid negative and not for the others because prose has no shape a runner matches; and `evidence.pack_exported`. `INV-M7-10` NOT reopened, `admin-source/index.ts` NOT touched, `wiring.test.ts` NOT touched and its `{declared, wired, blocked}` triple unmoved, no `SystemReason` member, no `SqlExecutorReason` member, no `pg` import, no cast past a key type, `packages/db` untouched and `0008` byte for byte unchanged. 33 of 33 gates, 15 of 15 invariants, typecheck 0, lint 0, format clean, **212 files / 4,991 passed / 6 skipped** against a baseline of 33 / 15 / 0 / 0 / clean / 212 / 4,981 / 6 reproduced exactly first, `falsify` clean with the tree clean after. Every one of the ten new tests is a NEW case: none deleted, none weakened. |
| [2026-08-28 - Session 328](2026-08-28-session-328.md) | **[P7](../plans/P7-risk-and-abuse.md) section 8's `P7-m`, THE LAST UNBUILT SLICE IN THE PHASE.** `IN-M10-03`'s outbound lifecycle events through `integration_contracts`' declared field allowlist. **`IN-M10-01` Chatwoot and `IN-M10-02` Metabase were NOT built, neither was needed, and neither is a file in this tree**, exactly as the row says. **THE NAMED DEPENDENCY DID NOT LAND AND THE SLICE WAS BUILDABLE ANYWAY**: the row gives `P7-m` `P5-b` for `events` *"because a lifecycle dispatch reads the event it is dispatching"*, and **`P5-b` RAN AND REFUSED THAT TABLE** for want of a sixth scope class and an ADR number it was forbidden to take, so there is no registered `events` to read and reaching around the accessor is the one line rule 10 forecloses. **That BOUNDS the slice rather than blocking it, because the allowlist is not a read**: the module takes the event as a VALUE and **imports nothing at all**. **TWELVE TRIGGERS AND NOT NINE, AND THE COUNT IS THE SUITE'S SUBJECT**: [M10](../plans/M10-integrations.md)`:31` and `:165` both say nine, `EVENTS` section 11 carries twelve, and `M10:372` generates the negative allowlist tests FROM THE CONTRACT ROWS, so a nine-row table would have generated a correct-looking suite that never met `payout.held`, `payout.hold_released` or `identity.restriction_lifted`, **all three marked "always send"**. The suite reads section 11 AS TEXT and the cardinal appears nowhere as a literal. **NO FIELD LIST IS THIS SESSION'S, SO ALL TWELVE SHIP `undeclared`, EMPTY AND DISABLED**: `M10:31` says *"the minimum payload each needs"* and the corpus never says what that is, and **`0018` already anticipated the state** because `integration_contracts_enabled_has_fields` makes a disabled empty row legal and an enabled empty one impossible. **No row carries `approved_by`, because no person approved one.** **THE SECOND LAYER SURVIVES THE FIRST BEING FILLED IN**: `FORBIDDEN_FIELDS` is sourced at `EVENTS:407` (*"never the evidence and never the detector"*), outranks any allowlist, and **REFUSES rather than filters**, because a silent drop leaves a contradictory row looking approved; **`INV-M10-04` is deliberately NOT in it** because it names CLASSES and a column list inferred from five words would be this session deciding which columns are biometrics. **`guard_expression` CANNOT EXPRESS THE ONE GUARD THIS MODULE EXISTS TO ENFORCE**: `0018` declares it *"evaluated over the allowlisted fields only"* and `breach.detected`'s guard reads restriction status and open flag severity, **neither of which is a field of that event, whose payload carries no `identity_id` at all.** Reported, asserted in the suite, and NOT repaired. **SEND TIME IS A PROPERTY OF THE PARAMETER LIST**: `LoopsLiveState` carries nothing a producer could set to say the guard already passed, `dispatchToLoops` requires it and is the only entry point, and **the guard runs BEFORE the projection** so a suppressed message has no body at all. **`OQ-M10-02` UNTOUCHED AND NO ADR NUMBER TAKEN**, because the vendor choice did not have to be made to proceed; **no URL, no hostname, no credential and no send.** **The attempt budget ships `unstated`** and the two stated numbers are cited at the lines that state them and cross-asserted against the guards, so two transcriptions of one document cannot drift. **THIRTEEN DEFECTS SEEDED AND ALL THIRTEEN CAUGHT, EVERY ONE WITH `typecheck` AT ZERO ERRORS**, including **the 2026-08-28 barrel merge reproduced deliberately**: deleting both export blocks and the leg datum leaves the typechecker silent and turns three assertions red, and dropping only the `export type` block, the cheaper accident, is caught by the name-by-name sweep alone. **ONE FILE OUTSIDE THE ROW'S LIST WAS TOUCHED AND IS REPORTED**: [`breaker.test.ts`](../../apps/worker/test/breaker.test.ts)'s hand-written leg count, 21 to 22, which **`P7-l` wrote in that same enumeration would happen to "every barrel slice after this one"**; the in-fence alternative was a barrel that does not carry the slice. **NO ADR, NO MIGRATION, no `packages/db` change, no contract change, no `SqlExecutorReason` or `SystemReason` member, no `pg` import, no cast past a key type**, and `apps/api` untouched so `wiring.test.ts`'s `{declared, wired, blocked}` triple does not move. **All thirteen of `P7` section 8's rows are now built; Wave 4 is still NOT DISPATCHED.** 33 of 33 gates, 15 of 15 invariants, typecheck 0, lint 0, format clean, **213 files / 5,015 passed / 6 skipped** against a `main` baseline of 212 / 4,981 / 6 reproduced exactly first, `falsify` clean. |
| [2026-08-28 - Session 329](2026-08-28-session-329.md) | **[WAVE-06](../plans/WAVE-06-admin-console-transport.md) LANDS, `status: draft`. A PLANNING SESSION: NOTHING WAS BUILT, NO SOURCE FILE WAS TOUCHED, AND NO NUMBER WAS TAKEN.** **NOT `P8`, AND THE REASON IS A COLLISION RATHER THAN A PREFERENCE**: [DELIVERY_PLAN:132](../DELIVERY_PLAN.md) rows `P8 Hardening` with stated contents, [P6](../plans/P6-live-tier.md) cites it by that name three times and [M15](../plans/M15-discord-integration.md) rows two surfaces at it, and the `P<n>-` filename series transcribes that phase table one file per row; the work is the missing transport under **P5's and P7's** already-stated contents, so it takes the WAVE series' next free number, and **`WAVE-02` is spent rather than free** ([ALLOCATION](../decisions/ALLOCATION.md) row `060` names a WAVE-02 session). **THE CENTRAL QUESTION IS ANSWERED BY AN APPROVED DOCUMENT AND THE WORK WAS TO CITE IT**: [INFRA:43](../architecture/INFRA.md) and [:44](../architecture/INFRA.md) put **two** services on `ADMIN_ORIGIN`, the console (`admin`, `apps/admin`) and the operator API (`api-admin`, `apps/api` at `MERIT_API_SURFACE=operator`), and [:53](../architecture/INFRA.md) routes `/api/v1/*` to the second and *"everything else"* to the first, with `RI-04`, `RI-09` and `RI-11` each already refusing the fused shape. **So `apps/admin` serves HTTP and never serves `/api/v1`, and `OPERATOR_PREFIXES` is not the alternative to this plan but the other half of it.** **What IS undecided is the framework**, which [ADR-095](../decisions/ADR-095.md) ruling 2 declined for this app **by name**. **`apps/admin` MEASURED: 2,802 lines across nine modules, 200 passing tests in eight suites, no HTTP primitive anywhere, no `build`, no `dev`, no `src/app/`, no Playwright project** -- and it is the ONLY UI deployable left in that state, because `apps/portal/src/app/` now holds 48 files and `apps/site/src/app/` 14. **THE 2,802 LINES ARE NOT A SHELL**: every module is a pure fold that takes values and returns values, and what is missing is every input and every output. **THREE FINDINGS.** (1) **The barrel omits 23 exported names and one whole module**: `feed.ts`'s fourteen, `liability.ts`'s eight and `page.ts`'s `assertFloatIsNotReserve`, and since the `exports` map publishes `.` alone the event feed is unreachable from outside the package AND invisible to `service.test.ts`'s no-mutation assertion, which is the only control the package has over its own exports. (2) **The two `INV-M6-10` assertions read a `readonly string[]` a rendered page will not use**, which is `projectFlag`'s 2026-08-28 miss with a renderer in place of a projection. (3) **`API_CONTRACT` sections 8 and 9 carry NO event-feed endpoint** for a surface `M06` section 1.1 names and `feed.ts` implements in 507 lines. **TWO DISPATCH CLAIMS DID NOT SURVIVE**: the operator surface registers **27** routes and not 24, produced over a real `compose()` rather than grepped, and **`RI-15` does not read this file at all** -- its inputs are six named source files and its own `covers` line says it reads `docs/decisions` *"NOT AT ALL"* -- so every citation here is checked by nothing mechanical and every one was written from the file. **THE TWO BLOCKERS ARE PLACED BEHIND RATHER THAN ROUTED THROUGH**: [ADR-171](../decisions/ADR-171.md)'s admin identity provider blocks every write in `M06` and every read's actual rows and is an infrastructure purchase, and `P6-g`'s `VG-12` catalog admission is a HUMAN APPROVAL blocking the live figure; wave 5 is not dispatched and each absence names its blocker. **Ten slices buildable without either.** **FOUR OPEN ITEMS REPORTED RATHER THAN ANSWERED**, including whether a new importer of an already-admitted catalog entry needs a fresh `VG-12` approval (recommended NO, and it is the founder's), and that **`AdminReadSource.searchAccounts` and `readAccount` are owned by no plan at all**. **NO ADR NUMBER, NO MIGRATION NUMBER, NO `SD-M6-nn`, NO `GS-nnn`, NO `M6-N-nn`. No source file touched.** 33 of 33 gates, 15 of 15 invariants, 212 files / 4,981 passed / 6 skipped, all three reproduced on `origin/main` before a line changed and unmoved after. |
| [2026-08-28 - Session 330](2026-08-28-session-330.md) | **[ADR-181](../decisions/ADR-181.md) lands, `status: proposed`, approval line UNSIGNED, and `0054` IS NOT TAKEN.** **THE RULING IS [ADR-174](../decisions/ADR-174.md) SECTION 3 SHAPE (i): the external leg's in-flight obligation is a FIRM-SCOPED `liability` and none of the seven declared codes can hold one.** The class and the scope are DERIVED rather than chosen, from the two slots [ADR-180](../decisions/ADR-180.md) already ruled plus the one sign convention read out of [`posting.ts:235-236`](../../packages/ledger/src/posting.ts): `LT-06` DEBITS `trader_wallet`, so its open slot is a CREDIT; `LT-07` CREDITS `firm_treasury`, so its open slot is a DEBIT; one account rises when the trader's claim is extinguished and falls when the cash leaves, and that is an obligation. **Every code is refused with its own step**, the two ruled kinds read out of the constraint the migration set INSTALLS rather than out of `0052` or `0053` by name, and **the two silences are refused on what the tree says they ARE rather than on their silence**: [`checkout.ts:1682`](../../apps/api/src/routes/checkout.ts) calls crediting `psp_clearing` booking a receivable, which is an ASSET, and `SD-M5-03` anchors `reserve` outside this ledger on purpose, so giving it this posting makes the `RCR` numerator grow as withdrawals are approved. **Shape (ii) is refused because deleting `LT-06` inverts [M20:156](../plans/M20-wallet.md)'s ruled halt behaviour** (*"the halt is holding a transfer, not a claim"*) in a module this session does not own, and shape (iii) because a code ruled a liability BY the posting that needed one is [`0009:46`](../../packages/db/migrations/0009_ledger.sql)'s failure with the direction reversed. **THE SECOND FINDING IS WHY THE MIGRATION NUMBER STAYS IN THE POOL AND IT IS A MEASUREMENT**: [ADR-174](../decisions/ADR-174.md) finding 3 priced the eighth code at *"TWO merged migrations and a third statement in TypeScript"* and it is **thirteen files, seven of them outside this session's fence**, one being [GLOSSARY](../GLOSSARY.md)'s class list, which [`0027:104`](../../packages/db/migrations/0027_triggers_invariants.sql) records as the control that actually caught `firm_payable`; a migration widening the vocabulary while GLOSSARY still says seven has disabled that control. **Minting it today also unblocks nothing**, because `LT-06`'s debit leg is per identity and nothing in this tree creates an identity ledger account. **NO NAME IS MINTED EITHER** and two spellings are refused in advance, `firm_payable` because [`probe_ledger_constraints.sql:84`](../../scripts/db/probe_ledger_constraints.sql) uses it as the live negative fixture. **EXECUTED against PostgreSQL 16 with `0001` through `0053` applied forward-only from empty under `ON_ERROR_STOP`, 114 base tables and `ledger_accounts` at exactly two rows**: both guards fire INDEPENDENTLY, with guard two watched after guard one was dropped inside a transaction, which [ADR-174](../decisions/ADR-174.md) finding 3 asserted from DDL; the `ELSE true` hole is open for exactly two codes; and **THE WHOLE SHAPE WAS BUILT END TO END AND POSTS CORRECTLY**, global sum 0, the obligation at `-25000` cents between the two moments and 0 after both, so the refusal is about a transcription set and a control rather than about whether the arithmetic works. **[M05](../plans/M05-payout-system.md) section 2.1's `LT-06` and `LT-07` rows are AMENDED ADDITIVELY and LINE-COUNT NEUTRAL**, and **[`packages/rail`](../../packages/rail/test/lt-07.test.ts) is untouched and stays green**, because this entry mints no code, adds no constraint arm, seeds no row and rewrites neither row's opening text. **FOUND AND NOT IN THE DISPATCH**: `wallet_withdrawals` has a terminal `failed` state with **no ledger posting anywhere in the corpus**, so a rail-exhausted withdrawal strands the obligation, which is the one genuine argument for shape (ii) and is stated as such. **FOURTEEN DEFECTS SEEDED, TWELVE CAUGHT BY THE NEW WATCHER AND TWO BY `accounts.test.ts` BESIDE IT**, and the division is deliberate rather than a gap. 33 of 33 gates, 15 of 15 invariants, typecheck 0, lint 0, format clean, **214 files / 5,044 passed / 6 skipped** against a `main` baseline of 213 / 5,028 / 6 reproduced exactly first, `falsify` clean with the tree clean after it. |
| [2026-08-28 - Session 331](2026-08-28-session-331.md) | **[ADR-182](../decisions/ADR-182.md) LANDS, `status: proposed`, approval line UNSIGNED. IT IS THE RULING [ADR-095](../decisions/ADR-095.md) RULING 2 DECLINED BY NAME.** **RULING 1: `apps/admin` SERVES HTTP AND NEVER SERVES `/api/v1`, AND THE CENTRAL QUESTION WAS TREATED AS LIVE RATHER THAN DEFAULTED.** `apps/api` at `MERIT_API_SURFACE=operator` is `api-admin`, on the SAME `ADMIN_ORIGIN` under `/api/v1` ([`INFRA:44`](../architecture/INFRA.md)), and its **27 registered routes**, re-derived over a real `compose()` in this session rather than inherited, are the console's DATA SOURCE and not its alternative. **The losing shapes are priced rather than dismissed**: folding the console into `apps/api` makes [`INFRA:43`](../architecture/INFRA.md) FALSE, takes [`ADR-083:49`](../decisions/ADR-083.md)'s back door deliberately in the deployable SECURITY treats as total loss when owned, and, because **`RI-04` forbids `apps/api` declaring `@merit/admin`**, moves 2,802 lines out of a directory three in-flight plans hold; a hand-rolled `node:http` console is **the one shape in which BOTH mechanical halves of `ADR-095` ruling 3 go silent**, because `RI-09` reads PATHS and `RI-11` reads a routing CONFIG and a hand-written router table has neither, which `RI-09`'s own `covers` says at [`repo-invariants.mjs:1334-1335`](../../packages/tooling/checks/repo-invariants.mjs). **RULING 2: NEXT.JS APP ROUTER ON THE CATALOG VERSIONS ALREADY ADMITTED**, a THIRD IMPORTER and no sixth catalog entry. **THE `VG-12` CLOSURE DELTA IS ZERO, DIFFED RATHER THAN ARGUED: 427 package keys before and 427 after, byte-identical sorted lists**, with `pnpm-lock.yaml` gaining 15 lines all inside the `apps/admin` importer block and `pnpm-workspace.yaml` taking no diff at all. The manifest gained `build` and `dev` and its `start` became `next start`, which is `ADR-095` ruling 5's honest-direction argument on the third manifest; both scripts were RUN and both exit 1, on *"Couldn't find any `pages` or `app` directory"* and *"Could not find a production build"*. **THE `tsconfig` DID NOT MOVE, WHICH REVERSES THE SLICE AS DISPATCHED**: `ADR-095` `F7` places the `jsx` keys and the `dom` lib with the first page, both prior instances did exactly that, and the lockfile-serialization argument that justifies landing the React types early does not reach a file one slice touches, so `apps/admin/tsconfig.json` is now `W6-d`'s fence. **`ADR-012` IS DISCHARGED IN FOUR CLAUSES AND TWO OF THEM HAVE NO CHECK**: no `NEXT_PUBLIC_` identifier ever, a RELATIVE API base URL, `ADMIN_ORIGIN` read at request time through [`origin.ts:104`](../../apps/admin/src/origin.ts) with no default, and no `next.config`; the first two are assigned to `W6-c`, which already holds the file. **`ADR-171` IS UNMOVED AND NOT ROUTED AROUND**, re-measured here rather than cited: `admin_actions.actor` is `text NOT NULL` with no foreign key ([`0017_events_and_audit.sql:77`](../../packages/db/migrations/0017_events_and_audit.sql)) and the migrations hold **zero** operator, role or operator-session tables, so the write half stays blocked on an SSO purchase that is the founder's. **THREE FENCES MOVED IN [WAVE-06](../plans/WAVE-06-admin-console-transport.md)** and one prediction was wrong in the cheap direction: `W6-a` needed no `INDEX` row, because `CI-06c` reads the `decisions/` directory row and the per-entry registry is a generated span. **TWO STALE REASONS ARE REPORTED RATHER THAN REACHED PAST**: [`pnpm-workspace.yaml:144`](../../pnpm-workspace.yaml) still says the framework is in NO other package, which this ruling falsifies in the one file the slice may not touch, and [`apps/admin/src/index.ts:126`](../../apps/admin/src/index.ts) still says *"there is no server here"* in a file **session 332 holds this wave**. **NO MIGRATION NUMBER, NO `SD-M6-nn`, NO `GS-nnn`, NO `M6-N-nn`, NO `SystemReason` MEMBER, NO `pg` IMPORT.** Baseline reproduced on `main` exactly and unmoved after: 33 of 33 gates, 15 of 15 invariants, `typecheck` 0, `lint` 0, `format:check` clean, 213 files / 5,028 passed / 6 skipped. `falsify.mjs` ran LAST and ALONE: 33 gates clean and dirty, 80 scope cases, 10 loader cases, **no seed uncaught**. |
| [2026-08-28 - Session 332](2026-08-28-session-332.md) | **[WAVE-06](../plans/WAVE-06-admin-console-transport.md) `W6-b`. THE COUNT WAS RE-DERIVED AND IT IS 23, MATCHING THE PLAN EXACTLY**, per module and per name: `feed.ts` 14 of 14 missing because the module was not a leg at all, `liability.ts` 8 of 13, `page.ts` 1 of 11, and the other five modules 36 of 36 present. The surface is **74 names over 8 modules** and the barrel carried **51**. **THE ASSERTION IS THE SLICE AND THE EXPORTS ARE THE SMALLER HALF.** [`apps/api/src/admin-source/index.ts`](../../apps/api/src/admin-source/index.ts) keeps two defences and argues both; **the `Pick`-over-data half DOES NOT TRANSFER** and [`apps/worker/src/index.ts`](../../apps/worker/src/index.ts) already records why -- a re-export list has no runtime value for a type to be taken over, and minting one would put a second copy of the surface in the file whose job is to have one. **WHAT TRANSFERS IS THE PRINCIPLE, WHICH IS THAT THE TWO HALVES MUST FAIL AT DIFFERENT TIMES.** Section A imports all 74 names FROM THE BARREL by name and binds each to its own module's declaration, so a deletion is a `tsc` error naming the member; section B reads each module's SOURCE and asserts the barrel re-exports every name it declares, which is the only half that can see a name that was **NEVER ADDED**, and never-added is what all 23 were. **NEITHER SUBSUMES THE OTHER**, and `B.4` is a third property neither reaches: the barrel's binding must BE the module's binding, so a name re-exported from the wrong place fails while present and type-checking. **SEVEN SEEDS, ALL SEVEN CAUGHT, AND FOUR LEFT `pnpm run typecheck` GREEN**, which is the demonstration the slice exists for: `assertFloatIsNotReserve` removed from the barrel and from section A is exactly the state `main` was in, and `tsc` reports zero errors over it while `B.1` and `B.5` go red. **`ADMIN_BARREL_LEGS` IS DATA AND `B.2` SWEEPS `src/` RECURSIVELY THOUGH IT IS FLAT TODAY**, because `W6-c` adds `src/api/` and `src/http/` and a top-level sweep would stop covering this package on the next slice while staying green. **THE BARREL'S OWN HEADER WAS FALSE AND IS CORRECTED**: it listed the event feed among the *"M06 surfaces this package does not contain"* while `feed.ts` sat one directory away exporting fourteen names, which is `RI-14`'s error class in a file `RI-14` does not read. **The no-mutation fence was NOT moved**: none of the fifteen runtime values gained begins with any of the 21 verbs and the eight type-only names never reach `Object.keys`. **NO ADR NUMBER, NO MIGRATION NUMBER, and `package.json` and `tsconfig.json` were NOT touched** because session 331 holds them. 33 of 33 gates, 15 of 15 invariants, typecheck 0, lint 0, format clean, **213 files / 5,035 passed / 6 skipped** against a `main` baseline of 33 / 15 / 0 / 0 / clean / 213 / 5,028 / 6 reproduced exactly first, `falsify` clean. |
| [2026-08-28 - Session 333](2026-08-28-session-333.md) | **`RI-16` LANDS and it is the half `RI-15` declared it does not read.** `RI-15`'s own `covers` line records that it reads `docs/decisions` *"NOT AT ALL"* and its input is six named source files; **both were verified at the source before anything was built** and both hold. That left 879 markdown documents carrying 3,463 `file:line` pointers with nothing mechanical following one of them, which is the half where a wrong pointer is read by the founder at an `E2` review. **THE SCOPE IS THE WHOLE OF `docs/` WITH THREE EXCLUSIONS, ALL BY SHAPE AND ALL MEASURED**, and the first is not this check's idea: a citation under a level-1 or level-2 heading naming a DATE or a SESSION is out of scope because `CI-06/derivable-counts` already argued and ruled that axis over this corpus, and **it reaches INSIDE `docs/STATE.md`, a live document that accumulates dated sections and that no directory rule could split**. 1,737 of 2,340 path-bearing citations. The second is a fence or a generated span, masked with `CI-06`'s own two expressions. **The third DIVERGES FROM `RI-15` AND THE MEASUREMENT IS THE ARGUMENT**: a bare `` `:12` `` with no path of its own is skipped rather than inherited, because inheritance adds fifteen findings inside this scope and **ALL FIFTEEN ARE THE CHECK GUESSING A PATH** -- `M05:214` is a plan and a section line, `0011:49` is a migration, `open_ct 17:00` is a clock. **The half of that hole that CAN be closed is closed**: a citation inside a markdown link takes the path the LINK states, which is 130 more citations, a 27 percent gain, resolved to one file rather than to every file whose name ends the same way. **IN SCOPE: 603 citations across 879 documents, five carrying a bindable name. FOUR FINDINGS, ZERO REPAIRED, and the reason is a rule rather than a shortage of time**: all four sit in documents whose frontmatter reads `status: approved` in a frozen corpus, where a change is an ADR rather than a commit, and this session held no ADR number. Each is registered in `DOC_CITATIONS_OWNED_ELSEWHERE` with the repair it waits for named, and **the register SHRINKS ONLY** per `CI-06u`. **POINTING THE SHARED READER AT 19 MB OF MARKDOWN FOUND THREE DEFECTS IN IT, ALL THREE INVISIBLE ON SIX SOURCE FILES**: the citation regex was quadratic on a backtick-sparse document, the line joiner had every leading group optional and two greedy runs over one character class, and the path class dropped a Next.js route group. **`docs/decisions/ALLOCATION.md` did not finish in five minutes and now reads in 0.38 seconds**; `RI-15`'s unchanged suite is the equivalence oracle for all three repairs. **Twelve seeded cases, every one in both directions, plus four seeds run against the REAL tree with the finding text recorded and the tree restored byte for byte, checksums verified. NO SEED WENT UNCAUGHT.** All four design decisions are watched failing under mutation. **NO ADR NUMBER, NO MIGRATION NUMBER. No document repaired and no frozen document edited.** 33 of 33 gates, **16 of 16** invariants, 213 files / 5,041 passed / 6 skipped, baseline reproduced on `origin/main` first. |
| [2026-08-28 - Session 334](2026-08-28-session-334.md) | **[ADR-183](../decisions/ADR-183.md) LANDS, `status: proposed`, approval line UNSIGNED, and `0054` IS TAKEN AND SPENT.** [ADR-181](../decisions/ADR-181.md) section 4's third ground for returning that number was that minting the eighth code *"today unblocks nothing"*, because nothing in this tree creates a ledger account for an identity. **THIS ENTRY MAKES THAT GROUND FALSE.** **RULING 1: EAGER, IN THE DATABASE**, an `AFTER INSERT ON identities` trigger plus a backfill. **(b) IS REFUSED ON A MEASUREMENT**: zero TypeScript files anywhere insert into `identities`, so the path does not exist and a ruling placed on it would leave the defect live the day it was written. **(c) IS REFUSED TWICE AND THE SECOND REFUSAL IS NOT IN THE DISPATCH**: [`chart.ts:46`](../../packages/ledger/src/chart.ts) refuses it in shipped source, AND [`tx.ts:64`](../../packages/ledger/src/tx.ts) makes `ledgerAccounts` read-only on the posting handle, so `postTransaction` CANNOT open an account without widening the union that file calls the point of the type. **THE COST OF (a) HOLDS, MEASURED FROM `pg_proc` AND NOT A GREP: 21 non-internal triggers over 19 functions and NOT ONE BODY WRITES**, the two `UPDATE` matches being prose inside error strings. **A SECOND COST IS FOUND AND WAS NOT IN THE DISPATCH: the trigger is a FOURTH copy of the per-identity vocabulary in a file that can never be edited**, taken only against a test that holds it against `LEDGER_ACCOUNT_SCOPE` in both directions. **RULING 2: ALL THREE CODES**, derived from the identity partition rather than from module ship order, because provisioning two leaves a second provisioning mechanism to be designed later for one code in a module that is not the ledger's. **THE DISPATCH'S PREMISE IS TRUE IN SUBSTANCE AND ITS LITERAL FORM IS NOT**: the grep returns THREE, all three prose anchors in test files. **AND THE FINDING IS SHARPER THAN THE DISPATCH STATES: `LT-01` AND `LT-08` ARE SHIPPED AND WIRED TO LIVE ENDPOINTS AND THEY THROW**, so the absence is a live runtime failure in two request handlers and not only a design gap. **EXECUTED: PostgreSQL 16, `0001` through `0053` from empty, 114 base tables and `ledger_accounts` at two rows reproduced first; `LT-01` at 100,000c and `LT-08` at 25,000c POSTED, zero-sum 0 on each and a global sum of 0.** Ten claims perturbed, including the two that matter: a wrong `kind` REFUSED, a wrong `code` **ACCEPTED** and therefore watched by a test rather than asserted. **TWO FURTHER FINDINGS: `0054` REPRODUCED [ADR-177](../decisions/ADR-177.md)'s DEFECT IN THE CI PROBE**, whose pinned per-identity uuids were skipped by their own `ON CONFLICT` so the `LEDGER-C1` block raised `LEDGER-C2` on a dangling account, found by RUNNING the probe and repaired the way ADR-177 repaired `fees_revenue`; and an identity carrying accounts can never be DELETED, which is aligned with closure being a status. **Eleven seeds, ten caught and the eleventh a control that correctly stayed green.** `0009`, `0027`, `0038`, `0052` and `0053` byte for byte unchanged, no eighth code, `M05` section 2.1 not amended. 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean, **215 files / 5,075 passed / 6 skipped** against a `main` baseline of 33 / 16 / 0 / 0 / clean / **214 / 5,064 / 6** reproduced exactly first. |
| [2026-08-28 - Session 335](2026-08-28-session-335.md) | **`W6-c` LANDS: the wire types, the ONE `fetch`, and the two assertions [ADR-182](../decisions/ADR-182.md) section 8 item 2 assigned to this slice by name.** That entry's own words were *"CLAUSES 1 AND 2 OF SECTION 5 ARE RULES WITH NO CHECK"*, and both are checks now. **THE CLIENT TAKES NO ORIGIN, NOT AS A DEFAULT AND NOT AS A PARAMETER**, which is where it DIVERGES from [`apps/portal/src/http/client.ts`](../../apps/portal/src/http/client.ts) rather than copying it: the portal resolves `MERIT_API_ORIGIN` and refuses when unset because [ADR-083](../decisions/ADR-083.md) ruling 1 puts the trader API on its own origin, and [`INFRA:53`](../architecture/INFRA.md) puts `admin` and `api-admin` on ONE origin separated by path, so there is no origin here for an operator to configure and none for a build to inline. **THE ORIGIN SWEEP HAD A DEFECT THAT MADE IT PASS VACUOUSLY AND SEEDING IT ON THE REAL TREE IS WHAT FOUND IT**: `apps/portal/test/surface.test.ts`'s comment stripper is `\/\/[^\n]*`, which is correct for its own needles and deletes the `//` of `https://host` before the origin sweep reads the line. A `(?<!:)` lookbehind leaves a scheme intact and still strips every real line comment. **FIXTURES MAY NAME A HOST AND ONLY AN IANA-RESERVED ONE**, which is strictly stronger than exempting the two test files that need one and is the half of WAVE-06 rule 3 that reads *"not in a test fixture"*; `src/` is asserted at ZERO absolute origins, reserved names included. **SEVEN SEEDS, ALL SEVEN CAUGHT, five of them run against the REAL TREE with the finding text recorded and the tree restored.** **THE TWO NEW MODULES ARE `ADMIN_MODULES_NOT_RE_EXPORTED` ENTRIES RATHER THAN BARREL LEGS, AND THE FENCE IS NOT THE ONLY REASON**: `B.1` requires a leg to declare **more than three** exports and `api/types.ts` declares **ONE**, so legs was not available for it without editing an assertion in [`test/service.test.ts`](../../apps/admin/test/service.test.ts), which is a file `W6-c` does not hold and which also hardcodes `B.7`'s `74` and `8`. **THE LOST COVERAGE IS PAID FOR IN FENCE**: `surface.test.ts` asserts the hatch holds exactly these two entries and that NEITHER MODULE'S NAMES REACH THE PACKAGE SURFACE, which is `B.1`'s inverse and turns "deliberately absent" into a checked property. **THE COLLISION TABLE'S "with the liability and feed shapes" COULD NOT BE FOLLOWED AND THE REASON IS THE PLAN'S OWN MEASUREMENT**: section 4.2 found the feed has NO CONTRACT ROW and `W6-e` is the slice that writes it, so a feed shape here would be inventing the contract this file exists to copy. **SECTION 9 CONTRIBUTES NO TYPE, MEASURED**: four of its five rows declare no response body at all and the fifth is `GET /health`, which is Public and which the client refuses to reach. **`@types/node@22`'s `RequestInit` DECLARES NO `cache`** because undici implements no HTTP cache, and `apps/admin/tsconfig.json` is `W6-d`'s file, so the init type is widened here rather than the `dom` lib being reached for. **TWO STALE SENTENCES IN [`index.ts`](../../apps/admin/src/index.ts) WERE FALSIFIED BY THIS SLICE AND BOTH ARE REPAIRED**: *"there is no route, no client and no write path in it"* gained a client, and the escape hatch's *"IT IS EMPTY TODAY"* stopped being empty. **NO ADR NUMBER, NO MIGRATION NUMBER, AND `test/service.test.ts`, `tsconfig.json`, `src/app/**`, `page.ts`, `API_CONTRACT` AND `packages/**` WERE NOT TOUCHED.** 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean, **215 files / 5,084 passed / 6 skipped** against a `main` baseline of 33 / 16 / 0 / 0 / clean / **214 / 5,064 / 6** reproduced exactly first, `falsify` clean and the tree clean after. |
| [2026-08-28 - Session 336](2026-08-28-session-336.md) | **[ADR-184](../decisions/ADR-184.md) LANDS, `status: proposed`, approval line UNSIGNED.** [WAVE-06](../plans/WAVE-06-admin-console-transport.md) `W6-e`, the second of two slices in that wave marked **ADR REQUIRED**. **ALL THREE PREMISES RE-DERIVED AT SOURCE AND ALL THREE HOLD**: `apps/api/src/routes/admin-feed.ts` did not exist, [`feed.ts`](../../apps/admin/src/feed.ts) is 507 lines with 421 of tests, and `API_CONTRACT` section 8's **22** endpoints plus section 9's **four** `/internal` rows carry no feed row, **enumerated rather than grepped**. **RULING 1: A SEVENTH METHOD ON `AdminReadSource`, `listEvents`, AND NOT A PORT OF ITS OWN**, and the discriminator is a measurement rather than a preference: the 23 declared ports enumerate to **one admin READ port and four admin WRITE backends**, so the tree's split is reads-versus-writes and not one-module-one-port, and the four siblings that looked like precedent are all writes. **THE STRONGER LOSING ARGUMENT ALSO FAILS, ON A PRECEDENT ONE SLICE OLD**: `setAdminReadSource`'s `BLOCKED` reason is a SHAPE reason that is false for the feed, and it is **already false for `listFlags` and `readIdentityGraph`**, which `admin-source/index.ts` resolved by keeping them on the port and REPORTING the narrowing. **NEITHER DEFENCE COLLAPSES AND THEY MOVE DIFFERENTLY, WHICH IS THEM WORKING**: the `Partial` plus `AdminSourceNotComposed` half gains one arm because it is about what a DEPLOYMENT composed; the `IMPLEMENTED_ADMIN_READS` plus `Pick` half is UNTOUCHED because it is about what that DIRECTORY implements, which is no feed module. **RULING 1'S CODE DOES NOT LAND, AND THAT IS THE SESSION'S LARGEST FINDING RATHER THAN ITS SHORTFALL.** Three files must move together and **two are named by no fence in this wave**: `AdminReadSource` is declared at [`admin-reads.ts:694`](../../apps/api/src/routes/admin-reads.ts) while [ALLOCATION](../decisions/ALLOCATION.md) row `184` and the dispatch fence `admin-source/index.ts`, **which holds the COMPOSITION and not the DECLARATION**, and the plan's own `W6-e` row named the right file; and [`admin-source-flags.test.ts`](../../apps/api/test/admin-source-flags.test.ts) **hardcodes the six method names twice** and is named nowhere at all. **BOTH SHAPES WERE SEEDED AND BOTH WENT RED**, which is the argument executed rather than asserted: a port declared in the new module fails `wiring.test.ts`'s unaccounted check AND its triple, and a seventh method fails two cases in `admin-source-flags.test.ts`. **SPELLING THE SETTER SO `DECLARES` MISSES IT WAS CONSIDERED AND REFUSED BY NAME**: that is weakening a gate to pass it, and a port and the reason it is not wired belong in one commit, which is the regression `wiring.test.ts` exists for. **RULING 2: `INV-M6-10`'s TWO MODES ARE IN THE REQUEST**, a required `?scope=` with **no default**, and **a subject sent under a scope that does not name it is refused rather than ignored**, because ignoring it is the handler remembering and a property a handler has to remember is `FM-M6-10` waiting to happen. **RULING 3: THE WITHHOLDING IS A PROPERTY OF THE RESPONSE AND NOT OF THE RENDERER**, since `api-admin` serves the bytes with no console in the path and `AS-M6-05` is that reader by name; it is a rule on the SHAPE OF THE KEY, and the control is asserted **over the serialized body** per section 5.2. **THE WITHHELD SET IS NEVER A FIELD**: a response carrying it would ship every withheld identifier to the caller. **`ADR-157` IS NOT WIDENED**: there is no `total`, and section 1's own envelope already carries both honest facts. **TWO UPSTREAM CLAIMS DID NOT SURVIVE.** `WAVE-06` section 4.2's *"the word `feed` appears in that document once"* is **seven lines**, all seven the live figure's market-data feed, so the finding stands and the measurement under it does not; and section 4.1's *"every operator route answers 503 today"* is **500 `internal_error`**, measured, and the 500 is the DESIGN because `STATUS_CODE` is closed over section 2's table and `server.ts` refuses to invent a code. **A THIRD FILE MOVED AND IT IS REPORTED RATHER THAN BURIED**: `admin-reads.test.ts`'s section 8 register assumed one module serves section 8, which `W6-e`'s own mandated new module breaks; it is repaired to the DEPLOYABLE's served set, and this wave's collision table holds that file for nobody. **THE ROUTE COUNT MOVED AND WAS MEASURED OVER A REAL `compose()` ON BOTH SIDES**: baseline `public` 46/26 and `operator` 27/45 reproduced exactly first, after is **`public` 46 registered / 27 withheld** and **`operator` 28 registered / 45 withheld**. **The wiring triple is UNMOVED at `{ declared: 23, wired: 6, blocked: 17 }`.** **NO MIGRATION NUMBER, no `SystemReason` member, no `SqlExecutorReason` member, no `pg` import, no cast past a key type, no ledger account named.** `INDEX` needed no row and the plan predicted it. **SEVEN SEEDS, SEVEN CAUGHT**, tree restored byte for byte and checked. 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean, **215 files / 5,088 passed / 6 skipped** against a `main` baseline of 33 / 16 / 214 / 5,064 / 6 reproduced before a line changed, `falsify` clean. |
| [2026-08-28 - Session 337](2026-08-28-session-337.md) | **[ADR-185](../decisions/ADR-185.md) LANDS, `status: proposed`, approval line UNSIGNED, AND IT REPAIRS NO DOCUMENT, which is a measurement rather than a shortage of time.** **THE COUNT WAS DERIVED AT THE SOURCE and the dispatch deliberately stated no number: [EVENTS section 11](../architecture/EVENTS.md) CARRIES TWELVE ROWS**, read between its heading and section 12, and the three [M10](../plans/M10-integrations.md) omits are `payout.held`, `payout.hold_released` and `identity.restriction_lifted`. **RULING 1 IS [ADR-180](../decisions/ADR-180.md)'S TEST APPLIED RATHER THAN ITS OUTCOME**: what separates two unanimities is what each is MADE OF, and here it is twelve authored rows, each with its own message and guard and each bound to its `EVENTS` line by a running test, against **one figure typed three times and re-read never**. **RULING 2 IS THE ONE THAT COSTS SOMETHING: THE REPAIR IS NOT `nine` BECOMING `twelve`.** [ADR-034:6](../decisions/ADR-034.md)'s remedy has two branches and [ALLOCATION:197](../decisions/ALLOCATION.md) records that **there is no third**; a corrected number is true for as long as nobody adds a row, **which is exactly how long `nine` was true: three days**. Generating it is refused on a cost the runner PRINTS every run: `CI-06/derivable-counts` already binds the noun **`triggers`** to a query returning **21**, the SQL triggers. **THE DEFECT IS DRIFT AND NOT A TYPO AND THE HISTORY IS THE EVIDENCE**: nine rows from `26546ed0` until **`b6bec035` added three at 2026-08-16 16:05:22**, touching neither plan, and `M10` was edited at **17:51:09 the same afternoon** about something else. **One edit to one table falsified three sentences in two other documents and nothing noticed for twelve days**, which [`gates.mjs:8417`](../../scripts/corpus/gates.mjs) says in its own header it cannot see because the recogniser is value-anchored and fires only *"while it is still RIGHT"*. **FIVE LIVE SITES AND ONLY TWO IN THE FENCE**: [M16:270](../plans/M16-notification-center.md) says nine too, and [`loops.ts`](../../apps/worker/src/integrations/loops.ts) cites both `M10` sentences and quotes one. **NOTHING IS REPAIRED AND BOTH SHAPES WERE EXECUTED**: repairing `M10:31` plainly turns [`integrations-loops.test.ts`](../../apps/worker/test/integrations-loops.test.ts)'s `test 1.3` RED, 1 failed of 34, with its designed message; repairing it under THIS CORPUS'S OWN quote-the-original convention ([M05:145](../plans/M05-payout-system.md)) leaves it **34 passed while its own failure message is false**. **That is green and wrong, and it would disable a control by satisfying its letter inside the entry whose subject is a control satisfied by its letter**, so ruling 3 forbids the quotation and the repair is registered as FOUR FILES AND ONE COMMIT with `1.3` to be RETIRED rather than inverted. **THE DISPATCH'S PREMISE OVERSTATES THE HARM IN ONE PLACE AND THE ENTRY SAYS SO: the figure never fed the generator**, because [session 328](2026-08-28-session-328.md) built `IN-M10-03` from `EVENTS` and shipped twelve. **FOUND AND NOT IN THE DISPATCH: `EVENTS` HOLDS TWO DISAGREEING DEFINITIONS OF THE M10 TRIGGER SET AND NEITHER IS NINE** -- **23 events carry `MAIL`, section 11 carries 12, they overlap in 11**, and `payout.transfer_failed` is a section 11 row with NO `MAIL` tag while being the row two plans lean on for *"always send"*; and **[M10:312](../plans/M10-integrations.md) states a second stale derivable count IN THIS FENCE**, *"twenty-nine migrations"* against **53**, in a sentence opening *"checked against the migrations rather than recalled"*. **NO ADR SIGNED, NO MIGRATION, NO CODE, NO GATE, AND `EVENTS` NOT EDITED.** 33 of 33 gates, 16 of 16 invariants, **214 files / 5,064 passed / 6 skipped**, baseline reproduced at `b3c9cf0d` first, test delta ZERO. |
| [2026-08-28 - Session 338](2026-08-28-session-338.md) | **THE DIFF IS EMPTY IN BOTH DIRECTIONS AND THAT IS THE MEASUREMENT.** `docs/architecture/API_CONTRACT.md` declares **72** distinct `METHOD /path` endpoints; `compose()` declares **72** routes and registers **72** across the two surfaces; the symmetric difference is **0**. Every contract row has a registered route and every registered route has a contract row. **THE 35 WAS THE PARSER AND NOT THE CORPUS**, exactly as the dispatch predicted, and the reader that gets 72 reads two structures and says so: an endpoint HEADING, split on commas so a segment omitting a method inherits the one to its left, and an endpoint TABLE identified by its first header cell being exactly `Endpoint`, which is a SHAPE rather than a section number. **FOUR HEADINGS CARRY MORE THAN ONE ENDPOINT AND ONE ENTRY PER HEADING LOSES FIVE OF THEM**, a failure `API_CONTRACT` section 8 names in its own words; the seed that collapses one of those headings produces two false findings and was watched doing it. **PROSE IS READ NOT AT ALL**, which is the skip that produced the 35: section 1 spells `POST /accounts/:id/reset` where the heading spells `:accountId`. **THE BASELINE WAS REPRODUCED BEFORE ANYTHING WAS BUILT** -- `public` 46 registered / 26 withheld, `operator` 27 / 45 -- and the dispatch's "73 registered routes" is a SUM rather than a distinct count: `GET /health` is registered on BOTH surfaces, so the union is 72. **THE PARSER READS THE LIVE DOCUMENT IN 0.44 ms** and states its own coverage: 59 endpoint headings read, 2 endpoint tables and 8 rows read, 22 non-method headings and 5 tables keyed on `Code`, `revocation_class`, `Surface`, `Token` and `Test` skipped with their line numbers, **0 anomalies and 0 duplicate declarations**. **ITS TEST'S SUBJECT IS A FIXTURE CONTRACT AND NEVER THE LIVE DOCUMENT**, because session 336 is adding a contract row concurrently and a suite asserting "72 endpoints" punishes the work it exists to support. **THE FIXTURE FOUND A REAL DEFECT IN THE READER**: a heading whose whole text was a bare `GET` fell into the count of non-endpoint headings, a malformed endpoint heading silently dropped, invisible on a document where every heading is well formed. **FOUR SEEDS WERE RUN AGAINST THE REAL CORPUS IN MEMORY AND ALL FOUR WERE CAUGHT EXACTLY**, with the document's checksum verified unchanged. **FINDING 1, A STALE CLAIM IN A FROZEN DOCUMENT**: [`API_CONTRACT:946`](../architecture/API_CONTRACT.md) says `GET /admin/loss-ratios` and `GET /admin/cusum` are *"registered by nothing"* and *"answer 404 on the admin origin as well as on the public one"*. Both are in `CompositionReport.registered` on `operator`, registered at [`admin-breaker.ts:211`](../../apps/api/src/routes/admin-breaker.ts) and [`:234`](../../apps/api/src/routes/admin-breaker.ts). REGISTERED, NOT REPAIRED: the document is `status: approved`, a change is an ADR rather than a commit, this session held no ADR number, and the file is session 336's this wave. **FINDING 2, THE BOUNDARY, AND IT IS BIGGER THAN THE SLICE**: `packages/tooling` cannot import `apps/api` (`ERR_MODULE_NOT_FOUND`, measured), which is the answer the dispatch anticipated -- but `apps/api` CAN import `@merit/tooling` without declaring it, because [`package.json:30`](../../package.json) declares it at the workspace ROOT and Node walks up. **THIS FALSIFIES THE REASON `RI-08` GIVES FOR ITS OWN SCOPE**: its `covers` says `node-linker=isolated` means an undeclared specifier *"resolves neither at run time nor at build time (TS2307, measured from apps/site and apps/api both)"*. Measured here: run time RESOLVED, and `tsc` resolved it too, reporting `TS7016` and not `TS2307`. **The hole is exactly nine names wide and `@merit/db` is not one of them**, so `RI-08`'s CONCLUSION about the accessor stands and only its stated reason is wrong. **NO MEASUREMENT FILE IS COMMITTED AND THAT IS FINDING 2's CONSEQUENCE**: its only legal home would be `apps/api/test/`, whose import of the reader runs through the hole this entry reports. **NO `RI-nn`, NO GATE, NO ADR NUMBER, NO CONTRACT ROW AND NO ROUTE REPAIRED**, and `repo-invariants.mjs` is byte for byte unchanged. 33 of 33 gates, **16 of 16** invariants and still 16, typecheck 0, lint 0, format clean, 215 files / 5,084 passed / 6 skipped against a `main` baseline of 33 / 16 / 214 / 5,064 / 6 reproduced exactly first, `falsify` clean with the tree clean after it. |
| [2026-08-28 - Session 339](2026-08-28-session-339.md) | **[ADR-186](../decisions/ADR-186.md) LANDS, `status: proposed`, approval line UNSIGNED, and [`0055_last_two_ledger_kinds.sql`](../../packages/db/migrations/0055_last_two_ledger_kinds.sql) IS TAKEN AND SPENT.** `psp_clearing` is an `asset`, `reserve` is an `asset`, `ledger_accounts_kind_matches_code` names all seven codes and its `ELSE true` closes to `ELSE false`. **THE CONSTRAINT IN FORCE WAS READ FROM `pg_get_constraintdef` AND NOT FROM A FILE NAME, and the hole was watched ACCEPTING first: TEN acceptances**, `psp_clearing` and `reserve` each admitted as `asset`, `liability`, `revenue`, `expense` AND `equity`, which is wider than any prior entry recorded. **THE RULING TURNS ON [`checkout.ts:1679`](../../apps/api/src/routes/checkout.ts), ONE LINE ABOVE THE SENTENCE [ADR-177](../decisions/ADR-177.md) AND [ADR-181](../decisions/ADR-181.md) EACH QUOTED**: *"There is no processor in this transaction, so there is nothing IN clearing."* **That IS [ADR-181](../decisions/ADR-181.md) section 8's *"funds held at a payment provider for onward payment"*, which that entry called the coherent alternative NOT written in this tree and put to the founder as what would overturn its refusal of [ADR-174](../decisions/ADR-174.md) section 3 shape (iii).** It does not overturn it: **both readings are ASSETS and the role shape (iii) needs is a firm-scoped `liability`**, so the alternative reaches the same answer as the reading already on the table. **`reserve` IS THE WEAKER RULING AND THE ENTRY SAYS SO FIRST**: both of `ADR-177`'s grounds STILL HOLD and were re-derived at source, and what changed is the ground for DISCOUNTING the prose that remains, because [`0052:114-116`](../../packages/db/migrations/0052_chart_of_accounts.sql) distrusted GLOSSARY's *"funds set aside"* as *"asset-shaped prose of exactly the kind that turned out to be wrong for firm_treasury"* and [ADR-180](../decisions/ADR-180.md) ruled that prose RIGHT. **The `equity` reading is NAMED and refused on the anchor**: `treasury_balances.source` is CHECKed to `('provider_api','manual_attestation')` and an appropriation of retained earnings has no rail balance. **RULING 3 IS THE ONE THAT BINDS A FUTURE SESSION AND THE ENTRY SAYS SO**: `ELSE false` refuses an eighth code until its kind is ruled in the migration that mints it, `0009:46`'s rule one column over, and **deleting the ELSE would not be the same closure** because a CASE with no ELSE returns NULL and a CHECK passes on NULL. **WATCHED IN BOTH STATES**: with `code_is_declared` dropped inside a transaction the eighth code is REFUSED under `ELSE false` and LANDS under `0053`'s `ELSE true`. **WHAT IT COSTS THE EIGHTH CODE IS THAT THE EIGHTH CODE GETS MORE LIKELY**: after `0055` no firm code can hold a `liability`, so shape (iii) is UNREPRESENTABLE rather than refused, and shape (i) or (ii) is what is left. **NO ROW IS SEEDED AND THAT IS ARGUED**, because `0052` seeded for postings that resolve and nothing posts against either, and a `reserve` ROW is the first object a reader could sum and mistake for the figure `SD-M5-03` anchors outside this ledger. **FOUR FINDINGS NOT IN THE DISPATCH.** (1) The `checkout.ts:1679` sentence. (2) **[ADR-181:42](../decisions/ADR-181.md)'s stated MECHANISM IS FALSE as of this tree**: the RCR numerator cannot see a ledger balance, executed with **777,000c** in a `reserve` ledger account and `rcr_bp` unmoved at **8000**, and **the only `treasury_balances.account_code` value anywhere in this tree is `'payout_wallet'`**, which is not one of the seven; the CONCLUSION survives on `SD-M5-03`'s other ground. (3) **[`0054:62-64`](../../packages/db/migrations/0054_identity_ledger_accounts.sql)'s named hole is NARROWED AND NOT CLOSED**: identity-scoped `reserve` as `liability` refused, as `asset` still ACCEPTED, because the DDL never ties `code` to `scope`. (4) **`ledger_accounts_kind_matches_code` HAD NEVER BEEN WATCHED FIRING BY ANYTHING THAT RUNS**, only by a text reader that would stay green against a constraint that failed to install; `LEDGER-K1` a/b/c is the repair and all three were watched FAILING under their own seeded defect. **AND IT REPORTS PRESSURE ON [ADR-180](../decisions/ADR-180.md)'S HIGHEST-WEIGHTED GROUND RATHER THAN HIDING IT.** **THIS ENTRY HAD TO EDIT `packages/rail`, the fourth in this sequence to.** `0009`, `0027`, `0038`, `0052`, `0053` and `0054` byte for byte unchanged, no eighth code, no name, vocabulary closed at seven, `M05` section 2.1 untouched. 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean, **218 files / 5,139 passed / 6 skipped both before and after**, baseline reproduced at `5d1d28b9` first, `falsify` clean with the tree clean after. |
| [2026-08-28 - Session 340](2026-08-28-session-340.md) | **[WAVE-06](../plans/WAVE-06-admin-console-transport.md)'s `W6-d` LANDS AND IT IS THAT PLAN'S WAVE 2 DONE-GATE.** `apps/admin/src/app/layout.tsx`, the liability home at `/`, and the module that turns a `LiabilityHomePage` VALUE into a document. `next build` compiles ONE ROUTE where the manifest's own note recorded it exiting 1 on *"Couldn't find any `pages` or `app` directory"*. No ADR number and no migration number. **THE VERDICT IS INHERITED AND THE CHECK IS MECHANICAL**: the document imports nothing from [`data-trust.ts`](../../apps/admin/src/data-trust.ts) and `M6-A-39` asserts that by reading the module's own source with comments stripped. **`INV-M6-10` IS RE-POINTED AT WHAT THE BROWSER RECEIVES AND IT IS STRICTLY WIDER THAN THE CALL INSIDE THE BUILDER**, which is the half [WAVE-06 section 5.2](../plans/WAVE-06-admin-console-transport.md) says is most easily left half-done: `buildLiabilityHome` asserts over `panels.flatMap(lines)`, the live figure's line is not in that array, and its `source` is a string a FEED supplies through `movement.feed`. **The seed is a subject name arriving that way: assembled without complaint by the builder, refused on the served bytes.** `renderLiabilityHome`'s lines are neither deleted nor the page. **THE SHIPPED GUARD WALKS THE ELEMENT TREE AND NOT `renderToStaticMarkup`'s STRING, WHICH IS THE FRAMEWORK'S RULING RATHER THAN A CHOICE**: the markup shape was written first and `next build` REFUSED it, naming the file and the import trace, *"You're importing a component that imports react-dom/server"*. `M6-A-42` binds the two by rendering the real markup in the suite, where the restriction does not apply, and asserting every string the shipped walk collects is in it; the walk THROWS on a node it cannot resolve rather than skipping it. **TEN SEEDED DEFECTS, ALL TEN CAUGHT, each by the case named for it.** **THE ROUTE PERFORMS NO READ AND INVENTS NO NUMBER**: it renders the 503 with its reason in `PendingPanel` shape, which is [WAVE-06 section 8.1](../plans/WAVE-06-admin-console-transport.md) blocker 1 verbatim, and names three things that block a supplier. **THE THIRD IS THIS SESSION'S LARGEST FINDING AND IT IS IN NO PLAN: `GET /admin/liability`'s CONTRACTED RESPONSE CANNOT PRODUCE THIS PAGE'S INPUT.** [`0009_ledger.sql:164`](../../packages/db/migrations/0009_ledger.sql) carries five figures the page reads and [`API_CONTRACT:894`](../architecture/API_CONTRACT.md) carries ONE of them, so `P-M6-01` cannot show *"the two components separately as well as summed"*, `INV-M6-11` cannot include wallet balances, and `P-M6-02`, `AS-M6-04` and `P-M6-10` have no field at all; the one shared name, `open_liability_cents`, is the exact ambiguity [`liability.ts`](../../apps/admin/src/liability.ts) renames on arrival to prevent. **REPORTED AND NOT REPAIRED**: both files are other slices'. **SECOND FINDING, A CONTROL THAT STOPS COVERING THIS PACKAGE ON THIS COMMIT AND SAYS IN ITS OWN HEADER THAT IT DOES NOT**: [`surface.test.ts`](../../apps/admin/test/surface.test.ts)'s transport sweep walks `sourceFiles()`, which is `.ts` ONLY, while its header names this slice, *"`W6-d` adds `.tsx` ... none of them would be covered by a `.ts` filter"*. **Measured in both directions: one seeded second `fetch(` call site is INVISIBLE as `.tsx` (20 of 20 green) and fails three cases as `.ts`.** Its origin and `NEXT_PUBLIC_` sweeps walk every file and are unaffected. `W6-d` holds neither that file nor `service.test.ts`, whose `B.2` reads `.ts` too, so the repair is reported; `test/render.test.ts` sweeps `src/app/` for all four transport needles in the meantime. **THAT PAIR ALSO FORCED THE SHAPE OF THIS SLICE**: `B.2` fails a `.ts` module in neither barrel list and `surface.test.ts` asserts the absent list holds EXACTLY TWO entries, so a `.ts` file under `src/app/` could not land without turning another slice's suite red, and every file added here is `.tsx`. **THE TWO STALE SENTENCES ARE FALSE IN FACT AND ARE CORRECTED**: `next start` runs a server that serves a route, so [`index.ts`](../../apps/admin/src/index.ts)'s *"there is no server here to receive a request carrying them"* and *"no server yet"* are gone, with the half that was never stale kept verbatim. **`apps/admin/tsconfig.json` TAKES [ADR-095](../decisions/ADR-095.md) `F7`'s THREE KEYS AND CARRIES ITS PROSE IN A `//` KEY**, because `surface.test.ts` reads every `.json` in this package through `JSON.parse` and a JSONC tsconfig fails two of its cases with a syntax error rather than a finding; a SECOND, narrower `src/app/tsconfig.json` gives Vitest `react-jsx`, which is `apps/portal/src/app/calendar/tsconfig.json`'s measured precedent. **ALSO FOUND: `apps/admin` HAS NO `next.config.mjs`, so `ADR-095` `F5`'s telemetry remedy is undischarged for this app, AND `ci.yml`'s `CI-07` JOB BUILDS `apps/portal` AND `apps/site` BY NAME, so this deployable's build runs in no CI stage on the commit that makes it buildable.** `format:check`'s glob has never included `.tsx`. All three files are outside the fence. **NOT TOUCHED: `page.ts`, `liability.ts`, `live-liability.ts`, `service.test.ts`, `surface.test.ts`, `apps/api/**`, `packages/**`, `API_CONTRACT`, `ci.yml`, `playwright.config.ts`, `package.json`, `pnpm-workspace.yaml` and `pnpm-lock.yaml`.** 33 of 33 gates, 16 of 16 invariants with `RI-04` at five separate deployables and `RI-09` and `RI-11` unmoved, typecheck 0, lint 0, format clean, **219 files / 5,161 passed / 6 skipped** against a `main` baseline of **218 / 5,139 / 6** reproduced exactly first, `falsify.mjs` clean with the tree clean after it. |
| [2026-08-28 - Session 341](2026-08-28-session-341.md) | **[ADR-184](../decisions/ADR-184.md) RULING 1'S CODE LANDS. `AdminReadSource` declares `listEvents` at [`admin-reads.ts:726`](../../apps/api/src/routes/admin-reads.ts), `composeAdminReadSource` carries its arm, and NO PORT IS DECLARED, NO SETTER ADDED AND NO HANDLER WIRED.** **THE LARGEST FINDING IS A COUNT AND IT IS THE SAME FENCE ERROR ONE LEVEL OUT: ADR-184 section 5's _"Three files must move together"_ IS FALSE AND THE NUMBER IS SIX**, measured by seeding the method and reading what went red rather than by grepping. The three that entry named are real; the three it did not are [`admin-reads.test.ts:180`](../../apps/api/test/admin-reads.test.ts) (`const base: AdminReadSource`, **TS2741**), [`admin-breaker.test.ts:102`](../../apps/api/test/admin-breaker.test.ts) (`sourceOf`, **TS2322**) and [`admin-source-evidence.test.ts:1166`](../../apps/api/test/admin-source-evidence.test.ts), **A THIRD HARDCODED SIX-NAME LIST whose own comment predicted this exact red** and which no fence in this wave or the last names. **BOTH REFUSED SHAPES WERE RE-SEEDED AND BOTH WENT RED BEFORE ANYTHING WAS FIXED**: a port declared in `admin-feed.ts` fails `wiring.test.ts`'s unaccounted check, its triple (**`declared` 23 to 24**) and that module's own fence test, THREE; a seventh method with the lists unmoved fails **two** cases in `admin-source-flags.test.ts`. **THE TWO DEFENCES IN THE COMPOSITION STILL FAIL AT DIFFERENT TIMES AND EACH WAS SEEDED ALONE**: dropping the new arm is **TS2741** in `composeAdminReadSource`, dropping a name from `IMPLEMENTED_ADMIN_READS` is **TS2353** in `composeImplementedAdminReads` over a different type in a different function, and an uncomposed leg throws **`AdminSourceNotComposed('listEvents')` at RUN TIME over a green typecheck**. Neither replaces the other and neither was collapsed. **`IMPLEMENTED_ADMIN_READS` IS UNTOUCHED AND STAYS AT TWO**, exactly as the ruling predicted, so `listEvents` joins the missing list rather than the implemented one. **THE WIRING TRIPLE IS UNMOVED AT `{ declared: 23, wired: 6, blocked: 17 }`, DERIVED AT THE MOMENT OF REPORTING** by re-running `DECLARES` over `src/routes/` and `CALLS` over `start.ts`: **a method is not a port.** Route counts unmoved over a real `compose()`: **`public` 46 registered / 27 withheld, `operator` 28 / 45**, reproduced on both sides. **A CONTROL NOBODY PREDICTED FIRED AND THE PROSE MOVED RATHER THAN THE CONTROL**: [`admin-source-flags.test.ts:1222`](../../apps/api/test/admin-source-flags.test.ts) asserts `admin-reads.ts` contains the substring `admin-source` nowhere, so a citation written into the new method's doc comment turned it red TWICE, once for the directory and once for the suite's own filename. **The assertion was not loosened**; the sentence was rewritten to name the composition without its path, and the reason is recorded in the file so the next editor meets it before the red. **TWO STALE CLAIMS ARE REPORTED AND NOT REPAIRED, BOTH IN `test/wiring.test.ts`, WHICH IS OUTSIDE THIS FENCE BECAUSE THE TRIPLE DOES NOT MOVE**: `setAdminReadSource`'s `BLOCKED` reason says _"None of the six methods is a projection of one table"_ where the port now declares seven and `AdminEventRow` IS a projection of one, the THIRD instance after `listFlags` and `readIdentityGraph`; and its two `routes/admin-reads.ts:667` pointers now land one line above the block they name, because the type-only import is one line. **RI-15 stays green on both** and the narrowing is recorded where the precedent puts it, in the composition file. **NOTHING IN `ADR-184` WAS FOUND FALSE EXCEPT SECTION 5's COUNT**, and its route-count and triple predictions were re-derived and hold. **`AdminEventFeedResponse` IS DECLARED AND REFERENCED NOWHERE**, which is the handler that still waits. **No new ADR number, no migration, no `SystemReason` member, no `SqlExecutorReason` member, no `pg` import, no cast past a key type, no ledger account named, `ADR-184` and `API_CONTRACT` not edited.** 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean, **218 files / 5,139 passed / 6 skipped with a TEST DELTA OF ZERO**, against a `main` baseline of 33 / 16 / 218 / 5,139 / 6 reproduced exactly at `5d1d28b9` before a line changed, `falsify` clean and the tree clean after it. |
| [2026-08-28 - Session 342](2026-08-28-session-342.md) | **THE DIFF WAS RE-DERIVED BEFORE ANYTHING WAS PINNED AND IT IS STILL EMPTY IN BOTH DIRECTIONS: 73 declared, 73 routed, symmetric difference 0**, over 27 route modules at `5d1d28b9`. The move from session 338's **72** is session 336 landing a contract row and its route between the two measurements, which is [session 338](2026-08-28-session-338.md)'s own landmine coming true rather than a defect. **`RI-17` IS A GATE NOW AND IT IS NOT IN `repo-invariants.mjs`**, so `check:invariants` still reports **16 of 16** and the dispatch's "17 of 17" could not be met. **THE FENCE DID NOT MOVE AND THAT IS THE REPORT**: three measurements on this branch each disqualify a `packages/tooling` home on their own, `@merit/api` (`ERR_MODULE_NOT_FOUND`) and `fastify` (same) are both unresolvable from `packages/tooling/checks/`, and [`Invariant.run`](../../packages/tooling/checks/repo-invariants.mjs) is SYNCHRONOUS while `discoverRouteModules` is not. The only shape that keeps the number in that file reads `routes/*.ts` as TEXT, which is the half [`account-reads.test.ts`](../../apps/api/test/account-reads.test.ts) says has been wrong twice here, so it lands as [`apps/api/test/api-contract-coverage.test.ts`](../../apps/api/test/api-contract-coverage.test.ts) and the `RI-17` row is amended IN PLACE to say so. **THE COST IS REAL AND IS NOT A FORMALITY: it runs in `CI-02` with the suite rather than in `CI-01` with the invariants**, so a change that skips the test run is not refused by it. **NEITHER HALF IS STORED**: the contract is read live through `api-contract-endpoints.mjs`, the routes through a real `compose()` over `discoverRouteModules()`, unioning `registered` AND `withheld` across `API_SURFACES` because a withheld route is DECLARED and merely not served there. **`API_SURFACES` IS READ OUT OF [`surface.ts:57`](../../apps/api/src/surface.ts) RATHER THAN TYPED**, so a third surface is composed the day it is declared. **THE READER IS LOADED BY COMPUTED PATH AND ITS SHAPE VALIDATED AT RUN TIME**, because a static import of a `.mjs` with no declaration file is `TS7016` from this project, measured; the two alternatives were `allowJs` on a deployable's compiler settings (outside the fence) and a hand-written `.d.mts` (INSIDE the fence and refused anyway, being a second copy of the reader's type surface that nothing checks against the reader). **FIVE SEEDS WATCHED FAILING against the REAL document mutated in memory, with its SHA-256 verified unchanged**: a contract row with no route, a route with no contract row, an endpoint table the reader stops seeing, a heading it enters and cannot parse, and a fifth that watches a LIMIT rather than a defect. **FINDING 1, FOUND BY SEEDING AND NOT IN THE DISPATCH: "assert the reader's own coverage statement" IS WEAKER THAN IT SOUNDS.** A `count > 0` floor sees a structure the reader stopped reading ENTIRELY and CANNOT see one it stopped reading in PART: this document declares endpoints in TWO tables, so renaming one `Endpoint` header leaves the count at 1 and the coverage report reads as intact. The diff catches it, the division is watched by the fifth seed, and the floor is deliberately NOT strengthened into a stored count of tables, which is the very thing `RI-17` exists to remove. **FINDING 2, REGISTERED NOT REPAIRED: [`ALLOCATION:412`](../decisions/ALLOCATION.md) now carries a universal `RI-17` falsifies**, _"THE ROWS ARE READ OUT OF `repo-invariants.mjs`'s `CHECKS` ARRAY, NEVER TYPED"_; `RI-17`'s third column is typed and its own row says so, but the paragraph above still states the rule without exception. The fence is that row only. **FINDING 3: SESSION 338'S FINDING 1 STILL STANDS**, [`API_CONTRACT:946`](../architecture/API_CONTRACT.md) still says two routes are _"registered by nothing"_ while [`admin-breaker.ts:211`](../../apps/api/src/routes/admin-breaker.ts) and [`:234`](../../apps/api/src/routes/admin-breaker.ts) register them, and **this check is blind to it BY CONSTRUCTION**: it compares `METHOD /path` and reads prose not at all. **FINDING 4: SESSION 338'S FINDING 2 IS UNREPAIRED AND THIS CHECK DOES NOT LEAN ON IT.** `apps/api` still resolves `@merit/tooling` through the ROOT `node_modules` it does not declare, measured again here by the resolver naming that path; the computed-path load means the repair, when it comes, cannot break this file. **RUNTIME: the ten cases run in 15 ms, 15 to 17 ms over three runs**, the reader parses the live document in **0.488 ms minimum over twenty reads**, and the file's 2.0 to 2.9 s wall is importing 27 route modules and `fastify`, which is SHARED with the twenty-odd other `apps/api` suites: the whole suite is **45.74 s against a 45.93 s baseline**, so the check adds no measurable cost to it. **NO CONTRACT ROW AND NO ROUTE REPAIRED, NO ADR NUMBER TAKEN, `repo-invariants.mjs` BYTE FOR BYTE UNCHANGED, `STRATEGY.md` UNTOUCHED** because no rule requiring a row there could be cited and it is `status: approved` in a frozen corpus. 33 of 33 gates, **16 of 16** invariants and still 16, typecheck 0, lint 0, `format:check` clean, **219 files / 5,149 passed / 6 skipped** against a `main` baseline of **218 / 5,139 / 6** reproduced exactly first, `falsify` clean with the tree clean after it. |
| [2026-08-28 - Session 343](2026-08-28-session-343.md) | **[ADR-187](../decisions/ADR-187.md) LANDS, `status: proposed`, approval line UNSIGNED, and [`0056_eighth_ledger_code.sql`](../../packages/db/migrations/0056_eighth_ledger_code.sql) MINTS `withdrawals_in_flight`**, a firm-scoped `liability` for the external leg's in-flight obligation, `E2 READ: MONEY PATH` owed and NOT discharged by the merge. **THE SHAPE WAS NOT CHOSEN BUT LEFT, and all three of [ADR-181](../decisions/ADR-181.md) section 4's grounds were re-derived at source rather than inherited**: shape (iii) is UNREPRESENTABLE (watched -- `0055`'s `ELSE false` refuses `psp_clearing` and `reserve` as `liability` by name), shape (ii) inverts [M20:156](../plans/M20-wallet.md) and [STATE_MACHINES:201-202](../architecture/STATE_MACHINES.md), and the "unblocks nothing" ground died with [ADR-183](../decisions/ADR-183.md). **THE SPELLING IS THE LARGEST JUDGEMENT AND IT TURNS ON A THIRD REFUSED CLASS NO ENTRY LISTS**: [`lt-07.test.ts:199`](../../packages/rail/test/lt-07.test.ts) asserts no declared code `includes('payable')` or `includes('payout')`, which **REFUSES `withdrawals_payable` -- the spelling [ADR-181](../decisions/ADR-181.md) section 5 row 1 and [ADR-186](../decisions/ADR-186.md) section 7 row 6 EACH FIRED AT A DATABASE as the eighth code** and which [`probe_ledger_constraints.sql:299`](../../scripts/db/probe_ledger_constraints.sql) still uses as `K1c`'s live undeclared fixture. **The NAME moved rather than the assertion**, so the probe needed no repair and still asserts what it says. **THE PRICE WAS MEASURED WITH BOTH TOOLS AND `tsc` SAW NOTHING**: the code seeded into [`accounts.ts`](../../packages/ledger/src/accounts.ts) alone gives **0 typecheck errors** and **3 files / 10 cases** red, the OPPOSITE asymmetry to [session 341](2026-08-28-session-341.md)'s, because [`pt-03`](../../packages/ledger/test/pt-03-ledger-zero-sum.property.test.ts)'s generator input is typed `readonly FirmAccountCode[]` and a list short of a union member is a well typed shorter list. **THE LARGEST FINDING IS THAT THE REGISTRY WHICH PRICED THIS MINT COULD NOT BE SATISFIED WITHOUT BREAKING `E2`**: `ENUMERATES_ALL` held FIVE merged migrations and asserted each contains every current code, while `0009`'s own `why` string read *"Superseded, never edited"* one line above. **Repaired by a partition** -- a `SUPERSEDED` list asserts each still says what it said and names NO code minted after it, which catches an edit the old shape could not see at all. **SECOND REGISTRY DEFECT: THE SCAN IS BLIND TO A NEGATIVE FIXTURE**, which enumerates neither the whole vocabulary nor the firm subset, so nothing would have said that minting `withdrawals_payable` inverts a live probe; `NEGATIVE_FIXTURES` is the new third category. **The registry is now 7 live / 5 superseded / 3 firm / 2 negative fixtures / 6 cardinality claims, 23 entries over 18 files, asserted by a case in the file**, and **the price FELL by one site** because [`accounts.test.ts`](../../packages/ledger/test/accounts.test.ts)'s `.toBe(7)` became `.toBe(LEDGER_ACCOUNT_CODES.length)`. **AN ARMED CONTROL NOBODY PREDICTED FIRED AND THE MIGRATION MOVED RATHER THAN THE CONTROL**: [`chart-of-accounts-kinds.test.ts`](../../packages/ledger/test/chart-of-accounts-kinds.test.ts) refuses `ON CONFLICT DO NOTHING` on a FIRM seed, the first draft of `0056` carried it copied from `0054`, and a migration runs once so the clause could only hide a duplicate. **TWO TEST FILES READ THE VOCABULARY OUT OF A MIGRATION BY NAME AND WOULD HAVE STAYED GREEN AGAINST A SUPERSEDED CONSTRAINT** -- and the `payable` refusal is INSIDE the case that read `0009`, so unrepaired it would have refused nothing and the spelling argument would have been unwatched. **`0056`'s OWN HEADER BROKE A TEXT READER** by quoting its `IF acct_code NOT IN` anchor, the fifth instance of one documented comments-come-off-first ordering bug, watched returning an empty list and failing loudly. **[ADR-181](../decisions/ADR-181.md)'s `probe_ledger_constraints.sql:84` citation is STALE**; `firm_payable` is at **:128**, moved by [ADR-183](../decisions/ADR-183.md) section 8.1, and the claim is true. **EXECUTED: PostgreSQL 16, `0001` through `0056` forward-only from empty under `ON_ERROR_STOP`, 56 files, 114 base tables UNCHANGED, 22 non-internal triggers UNCHANGED, `ledger_accounts` 2 rows to 3**, counts from `information_schema`, `pg_trigger` and `pg_constraint` rather than a grep. **ALL THREE GUARDS WATCHED REFUSING THE EIGHTH CODE INDEPENDENTLY BEFORE THE FILE WAS WRITTEN**, each with the others dropped inside a transaction, and refusing a NINTH after. **THE WHOLE EXTERNAL LEG POSTED AGAINST REAL PER-IDENTITY ROWS** rather than a fixture, which is further than [ADR-181](../decisions/ADR-181.md) section 5 row 5 went: at 25,000c, `trader_wallet` **+25000**, `firm_treasury` **-25000**, the obligation **-25000 after `LT-06`** and **0 after `LT-07`**, global sum **0**, every trigger passing. The eighth arm refused as `asset`, `revenue`, `expense` and `equity` and ACCEPTED as `liability` with the seed deleted inside the transaction first. **`LEDGER-K2` and `LEDGER-K3` are new probe blocks and both were watched failing under FOUR seeded defects**, with a clean `0056` database exiting 0. **THE ROW IS SEEDED AND IT IS ARGUED**: `0052` header item 4's RULE admits it and, unlike [ADR-186](../decisions/ADR-186.md)'s two codes, its ARGUMENT does too, because two postings the corpus STATES resolve against it and [`chart.ts:149`](../../packages/ledger/src/chart.ts) throws rather than opening an account. **[M05](../plans/M05-payout-system.md) section 2.1's `LT-06` and `LT-07` are AMENDED ADDITIVELY and are POSTABLE; `LT-02`'s slot is NOT filled and the amendment says so.** **This entry had to edit `packages/rail`, the fifth in the sequence to.** No ninth transaction, no new column, no new table, no new trigger, no grant, no revoke, and **`0009`, `0027`, `0038`, `0052`, `0053`, `0054` and `0055` are byte for byte unchanged**. 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, `format:check` clean, **220 files / 5,177 passed / 6 skipped** against a `main` baseline of **220 / 5,171 / 6** reproduced exactly first, `falsify` clean with the tree clean after it. |
| [2026-08-28 - Session 344](2026-08-28-session-344.md) | **[WAVE-06](../plans/WAVE-06-admin-console-transport.md)'s `W6-f` AND `W6-g` BOTH LAND, SERIAL, IN ONE SESSION BECAUSE THEY SHARE TWO FILES.** The flags queue at `/flags` and the identity drill-down at `/identities/[identityId]`, `FlagListItem` and `IdentityGraph` transcribed into [`api/types.ts`](../../apps/admin/src/api/types.ts), and `M6-A-43` through `M6-A-54` as **43 cases in two suites**. `next build` compiles **three routes** where `W6-d` left one. No ADR number and no migration number. **THE LARGEST FINDING IS A STATUS THE CONSOLE WAS TOLD TO RENDER AND NO OPERATOR ROUTE PRODUCES.** [WAVE-06 section 8.1](../plans/WAVE-06-admin-console-transport.md) says _"every one of the 26 operator routes above answers 503 today"_ and [`src/app/page.tsx`](../../apps/admin/src/app/page.tsx) renders `toAdminErrorKind(503)` on that basis. **MEASURED over a real `compose()` and Fastify's own `inject`, on both branches: with no admin session cookie `adminHandler` reaches `authorizeAdmin` with `{ kind: 'unknown' }` and answers 401 `unauthenticated`; with a cookie present `currentSessionSource()` throws and the answer is 500 `internal_error`.** [`admin-reads.ts:109`](../../apps/api/src/routes/admin-reads.ts) says so itself at the declaration of `AdminReadError`, _"IT IS A 500 AND NEVER A 404 OR A 503"_, and [`server.ts:155`](../../apps/api/src/server.ts) is the mapping. **Both routes this session adds therefore name NO error kind** and carry the two real answers in their blocker prose instead; the plan sentence and `W6-d`'s file are outside this fence and are REPORTED. **SECOND FINDING, FOUND BY A SEED FAILING TO FIRE: `assertNamesNoSubject`'s UUID PATTERN IS `\b`-ANCHORED, SO A SUBJECT ID GLUED TO A WORD CHARACTER IS NOT A MATCH.** `linked_to_<uuid>` passes and `linked to <uuid>` throws, run in both directions. That regex is [`page.ts:184`](../../apps/admin/src/page.ts)'s and `P5-l` holds the file, so the flags queue grew a SECOND leg that fails at a different time: the pattern leg covers an id from any field including one in no field of the response, the value leg asserts the three identifiers the document declines to render are absent from what it served. **THIRD FINDING: `GET /admin/identities/:identityId/graph` DECLARES THREE `_cents` FIELDS AND NO `as_of` AND NO SOURCE FOR ANY OF THEM**, where `GET /admin/liability` declares `as_of` as its first field; and [`figure.ts:70`](../../apps/admin/src/figure.ts)'s `ORIGIN_ID` closes the admissible roster at `P-M6-01` to `P-M6-10` and `AS-M6-04`, which are M06 section 3.1's panels, so a figure raised on section 3.2a has no origin to declare either. **Two independent mechanisms refuse the same render**, the drill-down states all three as a named absence in `PendingPanel` shape, and neither repair is in any WAVE-06 fence. **THE ORDERING IS THE SERVER'S AND IS NOT RECOMPUTED**: the queue maps `rows` in wire order, the module is asserted to sort nothing by a read of its own source, and **a queue that arrives out of order is rendered out of order rather than repaired**, which is the case that makes the claim mean something. `corroboration_depth` is beside the severity **in the words and not only in an attribute**, which is `FM-M6-01` rather than style. **THE DRILL-DOWN IS THE ONE SCREEN HOLDING AN `INV-M6-10` LICENCE AND ITS RULE IS A CLOSURE RATHER THAN A REFUSAL**: `assertNamesNoSubject` is deliberately NOT called there, because it would refuse the page's own subject; every id served must be one the query reached, and the root the server answered with is checked against the path FIRST. **THERE IS NO LIST BEHIND IT AND THAT IS ASSERTED OVER THE DIRECTORY**, not over an intention: `src/app/identities/` publishes one route carrying a dynamic segment, with no `identities/page.tsx`, no input, no form and no history affordance, and **a seeded index route and a seeded search box each turned it red**. **NO RESTRICTION AFFORDANCE AND NO RESTORE AFFORDANCE.** **FOURTH FINDING, AND IT IS WHY THE PLAN'S OWN FILENAMES COULD NOT BE USED: `src/view/flags.ts` AND `src/view/identity.ts` CANNOT LAND AS `.ts` WITHOUT TWO FILES THIS FENCE DOES NOT HOLD.** `service.test.ts`'s `B.2` fails a `.ts` module in neither barrel list, `B.7` pins the surface at 74 names over 8 legs, `B.5` derives its coverage from its own source and `surface.test.ts` pins the absent list at exactly two entries, so a ninth leg is fenced out of this whole wave. Every file added here is `.tsx` under `src/app/`, which is session 340's constraint inherited. **FOURTEEN SEEDED DEFECTS, ALL FOURTEEN CAUGHT**, each against the committed shape and each reverted, with the tree clean after every one. **THE WALK IS `W6-d`'s AND IS IMPORTED RATHER THAN COPIED**: `collectServedStrings` takes a node, throws on one it cannot resolve, and each screen writes its own entry point and its own rule. **NOT TOUCHED: `page.ts`, `figure.ts`, `liability.ts`, `live-liability.ts`, `service.test.ts`, `surface.test.ts`, `render.test.ts`, `src/app/page.tsx`, `src/app/layout.tsx`, `apps/api/**`, `packages/**`, `API_CONTRACT`, `ci.yml`, `next.config.mjs`, `playwright.config.ts`, `package.json` and `pnpm-lock.yaml`.** 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean with the four `.tsx` formatted by hand, **222 files / 5,214 passed / 6 skipped** against a `main` baseline of **220 / 5,171 / 6** reproduced exactly first, `falsify.mjs` clean with the tree clean after it. |
| [2026-08-28 - Session 345](2026-08-28-session-345.md) | **[ADR-188](../decisions/ADR-188.md) LANDS, `status: proposed`, APPROVAL LINE UNSIGNED, and it is a DOCUMENT RULING WITH NO CODE IN IT BY MEASUREMENT RATHER THAN BY PREFERENCE.** [Session 340](2026-08-28-session-340.md)'s finding 1 is real and **its statement is the first correction**: [API_CONTRACT](../architecture/API_CONTRACT.md)'s `LiabilityResponse` did NOT carry *"`open_liability_cents` and nothing else"*, it carried `as_of`, `open_liability_cents` and `funded_accounts` of the snapshot row while FOUR figures were missing, and **`funded_accounts` is on `liability_snapshots` because [ADR-128](../decisions/ADR-128.md) put it there by `ALTER TABLE` in [`0049:134`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql)**, which the dispatch and the allocation row both miss. **THE COMPLETENESS RULE IS A PROJECTION AND NOT A LIST**: one [`liability_snapshots`](../../packages/db/migrations/0009_ledger.sql) row column for column, less `id` and `computed_at`, so the next column on that table is on the response by the rule rather than by memory; a list would be the second enumeration of a table [ADR-034](../decisions/ADR-034.md) deletes wherever it finds it. The four added are `wallet_balances_cents`, `bounded_near_term_cents`, `remaining_ladder_exposure_cents` and `absorbed_corrections_cents`, the last SIGNED and now stated so. **`open_liability_cents` KEEPS ITS NAME ON FOUR GROUNDS AND THE FOURTH IS THE ONE THAT DECIDES IT**: a wire rename would DELETE the console's one stated mapping site, turning a marked boundary rename into an identity assignment nobody reads twice, so the place where the column stops being the panel would be marked nowhere. **NO TOTAL IS SENT, under a rule stated rather than a preference**: a value is SENT when the server knows something the reader does not and COMPUTED by the reader when it is a pure function of fields already on the response; the sum is two fields of one row at one `as_of`, a handler-computed total is the drift [`0049`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql)'s `GENERATED` column exists to remove with no generated column behind it, and it is the single convenient figure [`liability.ts`](../../apps/admin/src/liability.ts) removed structurally one layer down. **WHAT IS GIVEN UP IS STATED: neither design is enforced by the payload, so the choice is which failure is visible.** **A FINDING NOT IN THE RESERVATION BECAME CLAUSE 4**: `reserve` carried a ratio with **no `as_of` and no anchor**, so `P-M6-07` was as unrenderable as `P-M6-02`, and [`liability.ts`](../../apps/admin/src/liability.ts)'s `ReserveCoverageSnapshot` had no supplier for four of its members; it gains `as_of`, `treasury_account_code`, `treasury_as_of` and `treasury_source`, the last being the only field that answers `P-M6-07`'s attestation clause. **THE DISPATCH'S OTHER PREMISE IS TRUE OF EVERYTHING RULED AND FALSE IN GENERAL**: `SD-M6-01`'s `eligible_next_7d_identity_max_cents` and `eligible_next_7d_identity_max_id` appear in NO migration, so `P-M6-03`'s identity maximum is unproducible and gains no field, which is a schema item with no reserved number. **[M06:247](../plans/M06-admin-ops-console.md) ALREADY STATED THIS DELTA AND THE CONTRACT NEVER TOOK IT**, under a THIRD spelling, `open_liability_bounded_cents`. **NO CODE WAS EDITED AND THE REASON IS A SEED**: the four fields on `LiabilityResponse` are **three `TS2739` sites in three files**, `projectLiability` plus a typed fixture in each of two suites, **two of the three outside this fence**, and **the whole suite stays GREEN under that seed at 220 / 5,171 / 6**, so `CI-01` is the only thing between a half-applied field addition and silence. **`LiabilityResponse` EXISTS IN THREE COPIES AND NOTHING BINDS ANY PAIR**, the contract block, [`admin-reads.ts:464`](../../apps/api/src/routes/admin-reads.ts) and [`api/types.ts:127`](../../apps/admin/src/api/types.ts); `RI-17` compares `METHOD /path` and reads no field. **SEVEN ITEMS REGISTERED RATHER THAN REPAIRED**, including `breaker_armed` being SENT and RECOMPUTED with nothing comparing them, and [`api/types.ts`](../../apps/admin/src/api/types.ts) attributing to `INV-M6-12` a rule it does not state. **`RI-17` UNMOVED and `CI-06k` UNMOVED, both measured after the amendment.** The [ALLOCATION](../decisions/ALLOCATION.md) row is amended IN PLACE under [ADR-065](../decisions/ADR-065.md) T3. **No endpoint added, no migration number taken, no `SystemReason` or `SqlExecutorReason` member, no `pg` import, no cast, no ledger account named, and NO FILE UNDER `apps/` OR `packages/` TOUCHED.** 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean, **220 files / 5,171 passed / 6 skipped with a TEST DELTA OF ZERO** against a `main` baseline of 220 / 5,171 / 6 reproduced exactly at `3c456173` first, `falsify` clean and the tree clean after it. |
| [2026-08-28 - Session 346](2026-08-28-session-346.md) | **ALL FOUR PARTS LANDED, EACH IN ITS OWN COMMIT.** `CI-08` is at **3 of 3** projects (`site`, `portal`, `admin`, derived from [`playwright.config.ts`](../../playwright.config.ts) at reporting time) and **24 Playwright tests pass**, up from 16. **THE `CI-08` ROW MOVED IN THE SAME COMMIT AS THE PROJECT**, which is `W6-i`'s own condition: `CI-06/gate-inventory` never asks whether a `Contents` cell is TRUE, so nothing mechanical would have reported a register saying two. **THE COMPLIANT FIXTURE'S DOM IS NOT INVENTED**: it is what [`page.tsx`](../../apps/admin/src/app/page.tsx) actually serves at `/`, lifted out of `.next/server/app/index.html` after a real build, because `apps/admin/src/**` is session 344's and this slice may not write a page. **WHAT THIS SESSION ADDED TO IT IS A STYLESHEET, AND THAT IS THE LIMIT STATED RATHER THAN LEFT TO BE FOUND**: [`layout.tsx`](../../apps/admin/src/app/layout.tsx) ships NO stylesheet, `SS-01` to `SS-08` read COMPUTED STYLE, and the served page therefore computes Chromium's defaults, pure black on pure white, neither of which is in the token set. So the pass asserts this console's MARKUP is compliant when the design system is applied and does NOT assert the shipped page applies one. **BOTH DIRECTIONS WERE WATCHED FAILING**: the SS-02 stripe narrowed to 1px takes the seeded direction to 4 failed, and a non-token `--accent` on the compliant fixture takes it to 2 failed naming `SS-04 main > article > p > code`. **`apps/admin/package.json` TOOK NO `@playwright/test` LINE AND NONE WAS ASKED FOR**, verified rather than assumed: [`e2e/pass.ts:17`](../../e2e/pass.ts) is the only importer in the repository. **FINDING 1, AND IT FALSIFIES A FROZEN ADR: [`e2e/tsconfig.json`](../../e2e/tsconfig.json)'s `//include` REASON WAS FALSE AND [ADR-182](../decisions/ADR-182.md) SECTION 8 ITEM 4 REPEATS IT.** Both say `apps/admin/e2e` would otherwise be typechecked by the APP's project, whose lib has no DOM. **NO APP TSCONFIG READS ITS OWN `e2e/` DIRECTORY**: `apps/site`, `apps/portal` and `apps/admin` each include `src/**/*.ts`, `src/**/*.tsx` and `test/**/*.ts` and nothing else. Measured with the entry removed, `tsc -p e2e --listFiles` names **no file at all** under `apps/admin/e2e`, and the app's own `tsc --noEmit --listFiles` names none either. The entry buys COVERAGE AT ALL rather than a project swap, and the file's prose is corrected to say so; the ADR is frozen and outside this fence. **FINDING 2, THE DELICATE ONE, ANSWERED WITH THE BYTES: `VG-2` WOULD NOT READ THIS APP'S OUTPUT, SO PART 2 OPENS A REPORTING OBLIGATION AND CLOSES NO HOLE.** [`vg2-no-secrets-in-bundle.mjs:37`](../../scripts/ci/vg2-no-secrets-in-bundle.mjs) hard-codes `BUNDLE_DIRS` to `apps/portal/.next/static` and `apps/portal/.next/server`, and its own comment beside that list still calls the portal *"the one deployable that builds a bundle"*, which stopped being true when the site's build step landed on 2026-08-27. **Derived after all three builds on this tree: the scan reads 249 emitted file(s) and 5,831,402 byte(s), while `apps/site` emits 219 file(s) and 4,739,271 byte(s) and `apps/admin` emits 92 file(s) and 3,798,346 byte(s) that nothing greps.** One of three bundles, 249 of 560 files, 5.8 of 14.4 MB. `scripts/ci/**` is in no fence here, so the gap is written into the job and left. **ADR-012 HOLDS BY A CHAIN THAT DOES NOT RUN THROUGH `VG-2`**: `W6-c`'s two sweeps refuse an absolute origin and any inlining prefix in EVERY file of the package, and the emitted `.js.map` files carry those same files verbatim, which is measurable, the identifier `ADMIN_ORIGIN` appears 6 times in `.next` and every occurrence is inside a source map as the source's own prose. **No domain was written anywhere, in any form.** **FINDING 3: `CI-07` GAINING A THIRD APP IS AN EVENT NO GATE IN THIS REPOSITORY CAN SEE.** That row's activation condition was satisfied by `apps/portal` a day earlier, the row is Implemented and the probe is retired, so `W6-d` making a THIRD deployable buildable turned nothing red. Session 340 named it as a landmine and it is now a step. **FINDING 4: THE FORMAT GLOB'S WIDENING WOULD HAVE BEEN RED ON LANDING.** Derived at the moment of the change: **27 `.tsx` newly covered** (`apps/admin` 3, `apps/portal` 12, `apps/site` 12), of which **5 were unformatted, all five in `apps/site/src/app/`**. **THE WHOLE SWEEP WAS TAKEN AND IT COSTS A REVIEWER 5 FILES, 14 INSERTIONS AND 17 DELETIONS**, entirely JSX text rewrapping inside element children where the framework collapses the whitespace either way, reproducible in one command, and the test count is UNCHANGED at 5,171 which is the assertion that no rendered string moved. **`.html` IS DELIBERATELY NOT ADDED**: the six slop-score fixtures are hand-written DOM whose whitespace is part of the fixture, `SS-06` splits RENDERED TEXT on sentence boundaries and `SS-08` reads rendered text, so a formatter between a seeded violation and the check it exists to fire is a control with a rewriter in the middle of it. **FINDING 5, MADE FALSE BY THIS SESSION AND OUTSIDE ITS FENCE**: the root [`package.json`](../../package.json)'s `//typecheck` key says the `e2e` project *"OWNS the two apps' e2e directories"* and it now owns three; the fence is the `format` and `format:check` globs only. **FINDING 6: `apps/admin` PRERENDERS TWO ROUTES AND SESSION 340'S LOG SAYS ONE**, `/` and the framework's own `/_not-found`. **TWO STALE PARAGRAPHS WERE REPLACED RATHER THAN KEPT**: `playwright.config.ts`'s `webServer` refusal argued that `apps/site/src/app/` and `apps/portal/src/app/` do not exist, and STRATEGY's `CI-08` row argued there is no page to render. All three UI apps render routes, so both are false in fact; what holds instead is that `webServer` is a TOP-LEVEL key, so serving one project costs a `next dev` for every run of a merge blocker whose eight checks read a static document. **`scripts/corpus/gates.mjs` NEEDED NO EDIT AND NONE WAS MADE**: `INVENTORY_PROBES` is an empty Map and `CI-08`'s row carries no activation condition, so the register has nothing to shrink. **NOT TOUCHED: `apps/admin/src/**`, `apps/admin/test/**`, `apps/api/**`, `packages/**`, `API_CONTRACT`, `scripts/ci/**`, `pnpm-workspace.yaml` and `pnpm-lock.yaml`.** 33 of 33 gates, 16 of 16 invariants with `RI-11` passing on a `next.config` that now exists rather than on one that does not, typecheck 0, lint 0, `format:check` clean under the WIDENED glob, **220 files / 5,171 passed / 6 skipped with a test delta of ZERO** against a `main` baseline of 220 / 5,171 / 6 reproduced exactly at `3c456173` first, **24 Playwright tests across 3 projects run and reported rather than asserted**, and `falsify.mjs` clean with the tree clean after it. |
| [2026-08-28 - Session 347](2026-08-28-session-347.md) | **[ADR-189](../decisions/ADR-189.md), `proposed`, approval line UNSIGNED. MONEY PATH, `E2` READ OWED ON [`0057`](../../packages/db/migrations/0057_terminal_withdrawal_obligation.sql) AND NOT DISCHARGED BY THE MERGE.** **THE NINTH LEDGER TRANSACTION IS `LT-09`, `wallet_withdrawal_failure`**: the exact negation of `LT-06` with `reversal_of` set, posted in the same database transaction as `transferring --> failed`. `SD-M5-05`'s mechanism is USED and not redesigned. **EVERY PREMISE OF THE DISPATCH WAS RE-DERIVED AT SOURCE AND ALL OF THEM HOLD, BUT THE ARGUMENT IS NARROWER THAN IT READS**: `failed` is not special because the rail gave up, it is the ONLY terminal state reachable from at or past `approved` that does not itself discharge the obligation. **THE TERMINAL STATES WERE ENUMERATED AND CHECKED ONE AT A TIME**: `settled` discharges through `LT-07`, `failed` through nothing, and `cancelled` is drawn ONLY from `requested` and `cooling`, both BEFORE approval, so `LT-06` never posted. **So `WD-C1` is stated as the PROPERTY and not the PROCEDURE** -- a terminal wallet withdrawal leaves nothing standing in `withdrawals_in_flight` -- and it holds on all three, three different ways, and would hold on a fourth. **`0057` IS TAKEN AND THE DISPATCH'S BINARY IS REFUSED WITH ITS REASON**: a builder and a trigger prove DIFFERENT things. [`reversalPosting`](../../packages/ledger/src/reversal.ts) makes an inexact reversal UNREPRESENTABLE by swapping each transfer's two sides and negating nothing, so a partial reversal has no spelling; it cannot prove a withdrawal that reached `failed` was ever handed to it, and it cannot prove the reversal was built once. **The defect being closed is a state transition that happened with no posting after it, and a builder proves only what a caller who called it built.** **`WD-C1` CARRIES A SECOND ASSERTION UNDER ONE ID, `0038`'s `ADJ-C1` SHAPE**, because a handler that posts nothing sums over zero rows and gets 0, which is `0001:90`'s gate that silently stops gating. **A SECOND CONTROL WAS MEASURED BEFORE THE MIGRATION WAS WRITTEN**: `ledger_transactions_reversal_of_idx` is NOT UNIQUE in `0009`, a second reversal of one `LT-06` under a different key LANDS, drives the obligation to a debit balance and `trader_wallet` negative, **and leaves the global sum at ZERO so nothing in this database sees it**; superseded UNIQUE under its own name, binding `LT-03` and every future reversal. **THE WHOLE FAILURE PATH POSTED END TO END** against real per-identity rows: after `LT-01` of 30,000c split 25,000 / 5,000, `trader_wallet` **-25000**; after `LT-06`, **0** with the obligation **-25000**; the transition to `failed` **REFUSED** while it stood; after `LT-09` the obligation **0**, the claim back at **-25000**, global sum **0**. **FIVE FINDINGS NOT IN THE DISPATCH.** `0009:103-104`'s comment claims a reversal *"may not chain onto another reversal"* and the CHECK beneath it enforces only self-reversal, EXECUTED and reported rather than closed; **[ADR-187](../decisions/ADR-187.md) section 9 nominates the failed-withdrawal case as the discriminator for whether `LT-06`'s and `LT-07`'s slots are one account AND IT IS NOT ONE**, because `LT-09` touches `LT-06`'s slot and no other, and this ruling does not depend on the answer; `M05` section 2.1's lead-in says *"five shapes"* above a table that held eight rows and now holds nine, reported and not corrected because the fence is additive; `wallet_withdrawals` has ZERO transition enforcement, measured, so `cancelled` is safe by the drawing alone; and `wallet_withdrawals_approved_has_provenance` exempts `failed`, which is what makes `WD-C1`'s second assertion a proxy. **`0009`, `0027`, `0038`, `0052`, `0053`, `0054`, `0055` AND `0056` ARE BYTE FOR BYTE UNCHANGED, NONE OF `0056`'s THREE GUARDS IS TOUCHED, the vocabulary stays closed at EIGHT, and [M20](../plans/M20-wallet.md) and [STATE_MACHINES](../architecture/STATE_MACHINES.md) are NOT amended and neither needed to be.** 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean, **223 files / 5,240 passed / 6 skipped** against a `main` baseline of 222 / 5,220 / 6 reproduced at `9dca218d` first, `falsify` clean with the tree clean after it. **Eight defects seeded across the TypeScript, the migration and the plan and all eight caught; five more seeded into `0057` and caught by the CI probe.** |
| [2026-08-28 - Session 348](2026-08-28-session-348.md) | **BOTH GUARDS LANDED, EACH IN ITS OWN COMMIT, AND EACH FAILURE WAS WATCHED HAPPENING BEFORE A LINE MOVED.** **GUARD ONE: [`assertNamesNoSubject`](../../apps/admin/src/page.ts)'s uuid was `\b`-ANCHORED, so a word character on either side removed the boundary and the token stopped matching.** Session 344 found that by a SEED FAILING TO FIRE rather than by reading. `M6-A-55` was written first and **4 of its 7 cases were red on the committed regex**; all 7 are green after. **BOTH BOUNDARIES ARE DROPPED RATHER THAN REPLACED BY A LOOKAROUND, AND THE REASON IS A PROPERTY RATHER THAN A PREFERENCE: removing an assertion from a regex can only ADD matches, so the new pattern refuses a STRICT SUPERSET of the old one and nothing that used to throw now passes**, asserted over a generated corpus rather than argued. A hex-digit lookbehind was the obvious alternative and is REFUSED for a residue it leaves: `d` is a hex digit, so `id<uuid>` would still pass, and `id` is exactly the prefix an operator screen glues on. **WHAT THE WIDENING NEWLY REJECTS: every one of the 94 printable neighbour characters on either side, derived over the range rather than over three hand-picked cases, plus a uuid shape sitting inside a longer hex-and-dash run (a 16-hex first group, a 13-hex last group). WHAT IT NEWLY ACCEPTS: NOTHING, which is the superset property. The widening is in the NEIGHBOURS and never in the SHAPE**, pinned by four near misses that are still not uuids. **[`flags-render.test.ts`](../../apps/admin/test/flags-render.test.ts)'s `M6-A-45` case PINNED THE DEFECT with a `.not.toThrow()` and could not stay as written**; the fence named two test files and a third carried the case, so it is REWRITTEN rather than deleted and now asserts the two legs still fail at different times with neither subsuming the other. **GUARD TWO: `VG-2` SCANNED ONE OF THE THREE BUNDLES `CI-07` NOW BUILDS AND PRINTED `PASS` IN THE SAME WORDS EITHER WAY.** [`vg2-no-secrets-in-bundle.mjs`](../../scripts/ci/vg2-no-secrets-in-bundle.mjs) hard-coded `BUNDLE_DIRS` to the portal's two directories. **THE HOLE WAS REPRODUCED BEFORE THE REPAIR WAS WRITTEN: a real AWS access key id appended to one emitted file in EACH of the two unscanned bundles, and the committed check exited 0 reporting _"no key-shaped string in client output"_.** **RE-DERIVED AFTER ALL THREE BUILDS ON THIS TREE, and two of the three figures had moved since session 346: portal 249 file(s) / 5,831,402 byte(s) SCANNED, site 219 / 4,739,239 NOT, admin 121 / 4,024,910 NOT. 249 of 589 files and 5,831,402 of 14,595,551 bytes, 42 per cent by file and 40 per cent by byte.** **THE REPAIR IS A DERIVATION AND THE SOURCE IT IS DERIVED FROM IS THE DECISION**: the job's steps are REFUSED because a mirror can never notice the step that is missing, which is exactly how this hole opened; the directories that exist after a build are REFUSED because the scan then silently SHRINKS when a build is dropped, which is the same partial-green shape rebuilt from a different mechanism; **the workspace manifests are TAKEN, because they answer the question from the DECLARATION rather than from the artifact, so the expected set is known before the build and a missing bundle is RED rather than a smaller number.** **THAT GIVES `VG-2` A SECOND JOB ON PURPOSE: session 346's finding 3 was that a third app joining `CI-07` is an event no gate in this repository can see, and a fourth UI deployable landing with no build step now turns this check red.** **FINDING 5, AND IT COST A RED `CI-05`: `VG-1` CAUGHT THIS SESSION'S OWN LOG.** The first draft wrote the seeded key-shaped string out in full, `gitleaks git .` reported `leaks found: 1` over 1,987 commits, and **the repair is the commit REWRITTEN rather than a `.gitleaksignore` entry**, on that file's own stated ground: session 254's four fingerprints are defended only because their commits are in `main`, and this one was on an unmerged branch nothing is based on. **A document that describes a secret-scanner falsification is itself scanned by the secret scanner.** **SIX LEGS FALSIFIED AGAINST A SCRATCH WORKSPACE, EACH WATCHED FAILING**, plus the clean tree passing. **THE WIDENED SCAN IS CLEAN ON THE REAL ESTATE: 589 file(s), 14,595,551 byte(s), exit 0, and nothing was narrowed to get there.** **`ADR-012` HOLDS AND NO DOMAIN IS WRITTEN ANYWHERE, IN ANY FORM.** **FINDING 1, AND THIS SESSION'S OWN COMMIT IS WHAT MADE IT TRUE: [`flags-queue.tsx:239`](../../apps/admin/src/app/flags/flags-queue.tsx) still calls the pattern _"a `\b`-anchored uuid"_ and [`ci.yml:325`](../../.github/workflows/ci.yml) still reports the one-of-three scan as an open gap. Both are false now and BOTH ARE FENCED OUT**, the first as session 350's and the second under this dispatch's own NOT YOURS list, so both are REPORTED with their lines rather than repaired. **FINDING 2: `scripts/**` IS OUTSIDE `format:check`'s GLOB ENTIRELY**, so the directory holding every gate runner is unformatted-unchecked and the committed `VG-2` file was NOT prettier-clean; the rewrite is, and nothing enforces that. **FINDING 3: NO CITATION INVARIANT REACHES A SESSION LOG.** Session 344's log cites `page.ts:184` for this pattern, which held a CUSUM blocker string at `9dca218d` and holds nothing of the kind now, and `RI-15` and `RI-16` are 16 of 16 across the change. **FINDING 4: `apps/site` RE-DERIVES 32 BYTES BELOW SESSION 346's FIGURE and `next build` is byte-reproducible on this tree across three consecutive builds**, so the difference is a tree difference and not build noise; the site depends only on `@merit/rules-engine` and `@merit/ledger` appears nowhere in its emitted bundle, so the likely reading is that the table was derived before that session's own `.tsx` reformat landed, which is REPORTED as a difference and not as a cause. **NOT TOUCHED: the rest of `apps/admin/src/page.ts`, `liability.ts`, `live-liability.ts`, `figure.ts`, `apps/admin/src/app/**`, `.github/workflows/ci.yml`, `playwright.config.ts`, `packages/**`, `apps/api/**`, `docs/testing/STRATEGY.md`.** **`STRATEGY`'s `VG-2` row was in the fence CONDITIONALLY and the condition was not met**: nothing that row says became false, so it is left. 33 of 33 gates with `CI-06/vg-inventory` unmoved, 16 of 16 invariants, typecheck 0, lint 0, `format:check` clean, **222 files / 5,227 passed / 6 skipped against a `main` baseline of 222 / 5,220 / 6 reproduced exactly at `9dca218d` first**, a delta of **+7 cases and zero files**, and `falsify.mjs` clean with the tree clean after it. |
| [2026-08-28 - Session 349](2026-08-28-session-349.md) | **I AM AT 1 OF 2. `W6-h` LANDS IN FULL AND THE FEED ADAPTER DOES NOT EXIST, BECAUSE THE TABLE IT WOULD READ IS UNREGISTERED.** The event feed screen at `/feed`, both `INV-M6-10` modes, `GET /admin/events`'s query, item and envelope transcribed into [`api/types.ts`](../../apps/admin/src/api/types.ts), and `M6-A-55` through `M6-A-61` as **27 cases**. `next build` compiles **four routes** where session 344 left three. **THE LARGEST FINDING IS THE HALF THAT DID NOT LAND AND IT IS A COMPILE ERROR RATHER THAN AN OPINION: `events` IS NOT A `TableKey`.** [`scope.ts`](../../packages/db/src/scope.ts) refuses to register it in an argued paragraph -- `identity_id` and `account_id` both nullable with no CHECK tying them, so no member of the five-name scope vocabulary covers it and what the table needs is a SIXTH CLASS on ADR-106's precedent -- and `packages/db/src/schema.ts` declares no drizzle table for it at all. **Derived at reporting time: `TABLE_KEYS` holds 106 names and `events` is none of them.** So a `FeedTx` naming `'events'` is **`TS2322`, `Type 'SystemTx' is not assignable`**, seeded and watched, which is the assertion at [`admin-source-flags.test.ts`](../../apps/api/test/admin-source-flags.test.ts) that `SystemTx` satisfies `AdminSourceTx` refusing the shape. **`ADR-184` SECTION 3's THIRD-INSTANCE NARROWING DOES NOT REACH `listEvents` AND THAT ENTRY'S OWN SENTENCE IS THE ONE THAT FAILS.** It reasons that the port's `BLOCKED` reason is measured false for the feed because it is *"a keyed range read over ONE table"*; it is a keyed range read over a table the accessor **cannot key**, so the reason stands for this method where it fell for `listFlags` and `readIdentityGraph`. **`IMPLEMENTED_ADMIN_READS` IS THEREFORE UNMOVED AT TWO and `listEvents` stays in the missing list**, which is not the ruling's prediction executed but its prediction outliving the slice that was to end it. **BOTH DEFENCES WERE SEEDED SEPARATELY AND STILL FAIL AT THREE DIFFERENT TIMES**: the arm deleted is **`TS2741`** in `composeAdminReadSource` over `AdminReadSource`; a name deleted from `IMPLEMENTED_ADMIN_READS` is **`TS2353`** in `composeImplementedAdminReads` over `PartialAdminReadSource`, in a different function over a different type; an uncomposed leg is **`AdminSourceNotComposed('listEvents')` at RUN TIME over a green typecheck**. Neither was collapsed and nothing was weakened to land. **`WAVE-06` SECTION 5.2's OBLIGATION IS DISCHARGED AT THE CALL SITE AND NOT IN `src/feed.ts`**: `assertWithheld` already takes a string array and `collectServedStrings` already produces one from a rendered node, so the control is re-pointed at the served bytes without editing a barrel leg this fence does not hold, which is `W6-d`'s move for `assertNamesNoSubject`. **TWO LEGS THAT FAIL AT DIFFERENT TIMES**: a CLOSURE refusing any subject-shaped identifier the query did not name, with the licensed set **empty** under `operational` so the invariant's two modes are one rule with two licences; and `assertWithheld` refusing by VALUE, which catches a leaked identifier that is not uuid-shaped where the closure's pattern cannot. **THE SENTINEL IS DROPPED FROM THE SEARCHED SET AND THAT IS LOAD BEARING**: a withheld field arrives as the word `withheld` and the word is rendered on purpose (`FM-M6-01`), so a set carrying it would refuse every page that obeyed the rule. **THE `Feed` THIS SCREEN BUILDS IS A CONTROL AND NEVER THE RENDER SOURCE**: `buildFeed` sorts and the flags queue refuses to sort in terms, so its order is discarded and only `withheldValues` is read. **AND ITS `complete` IS DISCARDED FOR A SHARPER REASON: `rows.length < limit` AND `next_cursor === null` DISAGREE** on a full page that exhausted the query, so the server's fact is rendered and the inference is not. **THE PAYLOAD IS NOT RENDERED AT ALL**, which is `identity-graph.tsx`'s refusal of `edge.evidence` and `feed.ts`'s own `renderRow`, on the surface where `scope.ts` says the third-party uuid lives. **THE ROUTE NAMES NO ERROR KIND AND THE TWO ANSWERS WERE MEASURED IN THIS SLICE** over a real `compose()` and Fastify's `inject`: `GET /api/v1/admin/events` answers **401 `unauthenticated`** with no admin session cookie and **500 `internal_error`** with one, so `WAVE-06` section 8.1's 503 is refuted for a third time and reported rather than repaired. **AND THE 401 ARRIVES BEFORE THE SCOPE PARSE**, so `ADR-184`'s required `?scope=` answers 400 only on a deployment that has a session source. **NINE SEEDED DEFECTS, ALL NINE CAUGHT**, each reverted with the tree clean after every one. **THE WIRING TRIPLE IS UNMOVED AT `{ declared: 23, wired: 6, blocked: 17 }`, DERIVED AT THE MOMENT OF REPORTING**, and route counts unmoved over a real `compose()`: `public` 46 registered / 27 withheld, `operator` 28 / 45, reproduced on both sides. **No ADR number, no migration number, no `SystemReason` member, no `SqlExecutorReason` member, no `pg` import, no cast past a key type, no ledger account named, `ADR-184` and `API_CONTRACT` not edited, `apps/api/**` and `packages/**` not touched at all.** 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean, **223 files / 5,247 passed / 6 skipped** against a `main` baseline of **222 / 5,220 / 6** reproduced exactly at `9dca218d` before a line changed, `falsify` clean and the tree clean after it. |
| [2026-08-28 - Session 350](2026-08-28-session-350.md) | **[ADR-190](../decisions/ADR-190.md) LANDS, `status: proposed`, approval line UNSIGNED**, [ALLOCATION](../decisions/ALLOCATION.md) row `190` amended in place ([ADR-065](../decisions/ADR-065.md) T3). **NO MIGRATION NUMBER AND NO FILE UNDER `apps/api/src` EDITED.** **THE WHOLE OPERATOR SURFACE WAS INJECTED ROUTE BY ROUTE OVER A REAL `compose()`, WHICH NOBODY HAD DONE, AND IT ANSWERS TWO DIFFERENT THINGS**: 28 registered, 45 withheld, 23 of them `/admin/*`; the **ten** served by `admin-reads.ts`'s shared `adminHandler` (its seven plus `admin-breaker.ts`'s two and `admin-feed.ts`'s one) answer **401 `unauthenticated`** with no admin session cookie and **500 `internal_error`** with one, and the **thirteen** served by the four write backends answer **503 `service_unavailable`** BEFORE AUTHENTICATING AT ALL. **SO [WAVE-06](../plans/WAVE-06-admin-console-transport.md) SECTION 4.1 GENERALISED FROM THE MAJORITY OF ITS OWN SURFACE**, which is why two sessions reported it false and it still read as plausible. **TWO OF THE DISPATCH'S PREMISES DID NOT SURVIVE**: the sentence is section **4.1**'s and the fence holds 8.1 only; and *"a new code is a change to `STATUS_CODE`'s closed table"* is **FALSE**, [`API_CONTRACT:82`](../architecture/API_CONTRACT.md) declaring `service_unavailable` at 503 in section 2's canonical table, which refutes session 336's *"section 2's canonical table, which carries no 503"* at its own line. **THE FENCE'S CONDITIONAL IS MEASURED UNUSABLE**: `AdminReadError` given `readonly statusCode = 503` left **44 of 44 cases passing with the answer still 500**, because [`server.ts:67`](../../apps/api/src/server.ts)'s `STATUS_CODE` has no 503 key, so that file is byte for byte unchanged. **RULING: THE 500 STAYS, ON A GROUND THE RESERVATION DID NOT CARRY AND EXECUTED RATHER THAN ARGUED.** `AdminReadError` has **fifteen** throw sites and only **two** are *"no source is wired"*; twelve refuse the DATA a wired source returned, and with `STATUS_CODE` and `TITLE` patched so a 503 really reached the wire **12 cases failed across 2 files**, among them *"a liability body carrying a float in cents is refused"* and *"a pack whose digest is not a SHA-256 is refused"*. **RULING: THE THREE 500 STATES ARE ONE DOCUMENT ON THE WIRE, DELIBERATELY**, because a response distinguishing them tells an unauthenticated caller which ports this deployment did not compose. **THE ANSWER IS EIGHT STATES AND NOT TWO**, and state **F**, an admitted operator meeting a SECOND uncomposed port behind the first, was in no document. **RULING: A SCREEN RENDERS NO ERROR KIND IT DID NOT RECEIVE**, which is narrower than correcting the number and is why `toAdminErrorKind(401)` was refused as the repair: [`page.tsx`](../../apps/admin/src/app/page.tsx) drops `NOT_YET`, the `AdminErrorKind` import and `data-error`, and `M6-A-60` sweeps the whole `src/app/` directory with a counterfactual over `http/client.ts` so a sweep for a vanished name cannot pass by naming nothing. **`unavailable` STAYS AND NOW HAS THIRTEEN NAMED PRODUCERS.** **A DISCLOSURE IS FOUND AND REGISTERED**: the thirteen 503 routes confirm their own existence to an anonymous caller, which is the disclosure [`admin-writes.ts:823`](../../apps/api/src/routes/admin-writes.ts)'s own comment refuses one line before it happens, and `admin-reads.ts` escapes it only because it reads the cookie first. **AND `server.ts` NAMES ONLY 7 OF SECTION 2's 16 CODES**, so nine cannot be produced by a throw and 18 call sites spell the 503 title by hand; latent, because every 503 in this tree is built and sent rather than thrown. **ONE OF THE SENTENCE'S SIX HOMES IS REPAIRED AND FIVE ARE REGISTERED WITH LINE NUMBERS.** **FIVE SEEDS, FIVE CAUGHT**, tree clean after each. 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean, **222 files / 5,234 passed / 6 skipped** against a `main` baseline of 222 / 5,220 / 6 reproduced first, `falsify` clean, `@merit/admin` still three routes. |
| [2026-08-28 - Session 351](2026-08-28-session-351.md) | **[ADR-191](../decisions/ADR-191.md) LANDS, `status: proposed`, approval line UNSIGNED**, [ALLOCATION](../decisions/ALLOCATION.md) rows `191` and `0058` amended in place ([ADR-065](../decisions/ADR-065.md) T3), and **MIGRATION `0058` IS RETURNED TO THE POOL UNSPENT**. **RULED: A ROW THAT REACHES AN IDENTITY TWO DIFFERENT WAYS IS SCOPED BY BOTH, AND THE SIXTH CLASS IS `either`** -- one nullable column declared `REFERENCES identities(id)` beside one nullable foreign key to a table that itself reaches an identity, with the predicate their **DISJUNCTION**. Registry **106 to 107 of 114**, **63** scoped-reachable, at 1 root / 41 owned / 20 derived / 3 pair / 1 either / 41 firm. **THE HARD QUESTION IS ANSWERED WITH A COUNT AND NOT A PREFERENCE: NEITHER HALF MAY BE LOST.** Of the **29** `TL` rows in the catalogue, **10** name an account and no identity, **9** an identity and no account, **6** both, **4** unparseable and excluded rather than assumed, so `owned` loses at least ten of twenty-nine and `derived` at least nine. **THE CLASS IS NARROW AND IT IS MEASURED**: **8** tables declare both an identities edge and an accounts edge and `events` is the **ONLY ONE WHOSE IDENTITY COLUMN IS NULLABLE**, the other seven being correctly `owned`, and the suite reads that discrimination out of the DDL in BOTH directions. **PRECEDENCE BETWEEN THE LEGS IS REFUSED AND IT IS THE CLAUSE A LATER SESSION WILL TRY TO HARDEN**: the rows the legs disagree on are the rows a **HARD MERGE** produces, `accounts.identity_id` being repointed into the survivor while this table is APPEND-ONLY, so the guard hands the row to the identity that no longer logs in and hides it from the person who owns the account -- `purchases`' own refusal one table over. **`events` IS IN `ScopedTableKey` WHERE `pair` IS NOT, AND THE GROUND IS ABSENT RATHER THAN OUTWEIGHED**: no row this predicate returns discloses a second party through a TENANCY COLUMN. **THE PAYLOAD OBJECTION IS RULED ON RATHER THAN RESURRECTED AND A MEASUREMENT DISPOSES OF IT**: exactly **two** payloads name a second identity, `kyc.dedupe_hit`'s `matched_identity_id` and `identity.merged`'s `merged_identity_id`, and **NEITHER IS CONSUMED BY `TL`**, so the catalogue already excludes both from the surface `INV-M4-06` is about -- by a PROJECTION, which is the division the entry rules. **A ROW REACHING NEITHER LEG BELONGS TO NO IDENTITY**, which makes this the first class whose `firm` half is a property of the ROW. **`0058` RETURNS ON THREE CANDIDATE CONSTRAINTS WRITTEN OUT AND REJECTED**: the disjunction CHECK forbids the firm rows the class admits; an agreement trigger is FALSE ON PURPOSE after every hard merge; the `subject_kind` discriminator does not catch the actual counterexample. **THE COUNTEREXAMPLE `apps/api/src/events.ts` HANDED THIS ENTRY IS ANSWERED**: `payout.freeze_expiring`'s payload names neither tenancy column, and a row's tenancy and its payload are different things (`wallet.credited` resolves `account_id` at write time for exactly this reason), so it is a **PRODUCER defect** whose failure is SILENT -- the entry's sharpest open item. **TWO FENCES CROSSED, BOTH FORCED BY A HARD CONTROL**: seven line citations drifted under `RI-15`/`RI-16`, six in `apps/api/**` and one in ALLOCATION row `168`, each repaired as a **PURE REPOINT**, one of them in a file session 352's open branch holds, checked across 395 refs; and `apps/api/src/events.ts` told an operator **in a thrown message** that the scope registry refuses this table. **THE SINK WAS NOT WIRED** and the admin feed adapter is **unblocked and unwritten**. **REGISTERING THE TABLE MAKES A `derived` RULE THROUGH IT WRITABLE ON TWO COLUMNS FOR THE FIRST TIME**, `integration_dispatches.event_id` and `discord_announcements.event_id`, both refused on grounds that never depended on the type. **A MERGED IDENTITY'S IDENTITY-LEVEL EVENTS STAY UNREACHABLE AND NO CLASS FIXES IT.** **EIGHT NEW ASSERTIONS, THREE WIDENED AND EACH WIDENING STRICTLY STRONGER; SEVEN SEEDS, ALL SEVEN CAUGHT**, and seed 7 compiles at exit 0 with ZERO `error TS` while turning two assertions red, which is what shows the DDL reader refusing rather than `tsc`. **ADR-106's CLOSURE CLAIM IS FALSIFIED AND ITS MEASUREMENT IS NOT**, and what a SEVENTH would be is registered as UNMEASURED rather than asserted at zero. 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean, **224 files / 5,298 passed / 6 skipped** against a `main` baseline of 224 / 5,288 / 6 reproduced exactly at `d86a029a` before a line changed, `falsify` clean and the tree clean after it. |
| [2026-08-28 - Session 352](2026-08-28-session-352.md) | **[ADR-192](../decisions/ADR-192.md) LANDS, `status: proposed`, approval line UNSIGNED**, [ALLOCATION](../decisions/ALLOCATION.md) row `192` amended in place ([ADR-065](../decisions/ADR-065.md) T3). **NO MIGRATION NUMBER, AND `server.ts` AND [API_CONTRACT](../architecture/API_CONTRACT.md) ARE BOTH IN THE FENCE AND BOTH BYTE FOR BYTE UNCHANGED, WHICH IS A RULING RATHER THAN AN OMISSION.** [ADR-190](../decisions/ADR-190.md)'s measurement reproduced EXACTLY at `d86a029a` over a real `compose()` and Fastify's `inject`, every registered operator route one at a time: 28 registered, 45 withheld, 23 `/admin/*`, **ten answering 401 then 500 and thirteen answering 503 in both states**. **RULED: [ADR-190](../decisions/ADR-190.md)'s GROUND IS TWO GROUNDS AND ONLY ONE OF THEM REACHES THE THIRTEEN, AND THAT IS A COUNT RATHER THAN A READING.** Its clause 2 refused a 503 for `AdminReadError` because that class carries two facts across **15** throw sites of which only **2** are about wiring; the four write modules carry four DEDICATED classes with **17 throw sites, 17 of 17 inside their `UNWIRED_*` default constant and NOT ONE refusing data a wired backend returned**, derived at reporting time against constants spanning `admin-writes.ts:329-341`, `admin-wallet.ts:653-661`, `admin-payouts.ts:415-421` and `admin-certificates.ts:386-395`. So *"a 503 answers try again shortly to a float in a cents field"* has no instance here, **the status does not move and no code is minted**, and [ADR-190](../decisions/ADR-190.md) section 8 had already named this reversal and its condition. **A CLASS SPLIT IS PRICED AND REFUSED**: the split already exists at the MODULE boundary and a fifth class would be a second thing four handlers must choose between correctly, to separate facts that are not mixed. **RULED: THE 503 MOVES BEHIND THE 401, AND WHAT REACHES THE THIRTEEN IS CLAUSE 3, WHICH IS ABOUT ORDER RATHER THAN STATUS.** `principal`'s refusal by an unwired backend answers **401**, because a deployment that cannot authenticate anyone has authenticated nobody, which is true of every caller and needs no guess at credential shape; every other port member still answers 503, behind authentication, and each of the four suites asserts it there. **MEASURED AFTER: ALL 23 `/admin/*` ROUTES ANSWER 401 TO AN ANONYMOUS CALLER AND NO REGISTERED OPERATOR ROUTE ANSWERS 503 BEFORE AUTHENTICATING.** **THE DISCLOSURE IS NOT WHAT THREE DOCUMENTS SAID IT WAS AND THE REAL ONE IS WORSE**: *"confirm their own existence"* cannot be the harm, since the ten answer 401 anonymously and confirm existence just as well and an unregistered path answers 404; what the 503 added is **DEPLOYMENT STATE**, an unauthenticated live readout of which operator ports this deployment has not finished wiring, shrinking as slices land, which is exactly the fact [ADR-190](../decisions/ADR-190.md) clause 3 refused to put on the wire for the read side. **AND THE COMMENT THAT ENTRY QUOTED ONCE IS WRITTEN FOUR TIMES**, at `admin-writes.ts:844`, `admin-wallet.ts:1340`, `admin-payouts.ts:1030` and `admin-certificates.ts:700`: the argument the disclosure violated was stated four times in the four files that violated it. **TWO OF THE THREE FIGURES MOVED.** The 7 of 16 holds (`STATUS_CODE` 6 keys, `TITLE` 7, section 2 sixteen codes); **the hand-written 503 titles are 21 and not 18**, 11 declarative and 10 positional. **THE VOCABULARY IS REGISTERED RATHER THAN REPAIRED AND THE REFUSAL IS ARGUED FROM [ADR-190](../decisions/ADR-190.md)'s OWN EXPERIMENT**: `admin-writes.ts:384` states the hand-built title as the DESIGN, and adding the `STATUS_CODE` entry is precisely what let a class-level status reach the wire there for twelve cases that should not have had it; **nothing throws a 503 today, so the entry would change no response and would arm the next class that sets one.** **NO OPERATOR ENDPOINT DECLARES `service_unavailable` IN THE CONTRACT AT ALL**, the only per-endpoint declaration anywhere being `API_CONTRACT:341`'s public checkout, so the fence's conditional is unexercised and the gap is registered. **AND [ADR-190](../decisions/ADR-190.md) WROTE A CONTROL THAT PINS THE DEFECT IT REGISTERED FOR REPAIR**: `admin-reads.test.ts`'s `unavailable.length > 0` refused to pin the count 13 for a stated reason and pinned the set's non-emptiness for none, so it could not survive its own item 3 being discharged; it becomes `toEqual([])`, a bound made an equality, and it is the ONE file outside the dispatch's list this session touched, argued in section 7 rather than buried. `apps/api/src/routes/admin-reads.ts` is byte for byte unchanged. **FIVE SEEDS, FIVE CAUGHT**, tree clean after each. 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean, **224 files / 5,294 passed / 6 skipped** against a `main` baseline of **224 / 5,288 / 6** reproduced exactly first, `falsify` clean. **No migration number, no ADR number beyond 192, no `SystemReason` or `SqlExecutorReason` member, no `pg` import, no cast past a key type, no ledger account named.** |
| [2026-08-28 - Session 353](2026-08-28-session-353.md) | **I AM AT 1 OF 2, AND IT IS THE SAME TABLE THAT STOPPED SESSION 349.** `W6-j`'s SCREEN lands in full at `/accounts/[accountId]`, `AdminAccountSearchItem` and the drill-down's section roster are transcribed into [`api/types.ts`](../../apps/admin/src/api/types.ts), and `M6-A-62` through `M6-A-69` land as **31 cases**. `next build` compiles **five routes** where session 349 left four. **THE ADAPTER CANNOT BE WRITTEN AND THE MEASUREMENT IS A COMPILE ERROR RATHER THAN AN OPINION.** `readAccount` must fill EIGHT sections; seven of them read tables the registry holds and the eighth is `events`. **Derived at reporting time by importing [`scope.ts`](../../packages/db/src/scope.ts) rather than by grepping: `TABLE_KEYS` holds 106 names, `accounts`, `identities`, `dailyMarks`, `ruleStates`, `riskFlags`, `payoutRequests` and `adminActions` are all in it, and `events` is not.** An `AccountTx` naming all eight was seeded under `src/admin-source/` and is **`TS2322`, `Type '"events"' is not assignable`**; the same tuple with `'events'` removed typechecks at **exit 0**, so the blocker is that one table and nothing else. **AND SEVEN OF EIGHT IS NOT A SMALLER SCREEN, WHICH IS WHERE THIS BLOCKER IS HARDER THAN THE FEED'S**: [`admin-reads.ts:1224`](../../apps/api/src/routes/admin-reads.ts)'s `projectAccountDetail` REFUSES a drill-down that omits a section the contract names, so a partial adapter is a rejected response rather than a partial one. **`IMPLEMENTED_ADMIN_READS` IS UNMOVED AT TWO** and both `searchAccounts` and `readAccount` stay missing. **SECOND FINDING, AND IT IS INDEPENDENT OF THE FIRST: `searchAccounts` IS BLOCKED BY THE ACCESSOR'S VOCABULARY AND NOT BY THE REGISTRY.** API_CONTRACT section 8 makes its term "account id, platform ref, email, identity id, NAME FRAGMENT, coupon, or payout id"; a fragment is a substring predicate, `ADR-112`'s vocabulary is a typed equality and `ADR-157` adds a RANGE term and an `IS NULL` and refuses the rest. Six of the seven terms are keyed reads and the seventh is not, so an adapter serving six would narrow a contract behaviour behind a green suite. **THIRD FINDING, AND IT SHAPES THE WHOLE SCREEN: `GET /admin/accounts/:accountId` IS THE ONE ADMIN READ THE CORPUS DOES NOT TYPE.** [API_CONTRACT:1004](../../docs/architecture/API_CONTRACT.md) gives it a SENTENCE where every other section 8 row gives a `ts` block. So the console transcribes the eight section NAMES and no field, the document renders the roster and what arrived in each and never a member of one, and **the root cannot be checked against the path the way `W6-g`'s drill-down checks its own**: there is no declared `account_id` to compare and a check reaching for an undeclared field would pass VACUOUSLY the day a server spelled it differently. That absence is rendered with its owner rather than papered over. **THE LICENCE IS A CLOSURE ONE MEMBER WIDE**, where the identity drill-down's grows with the graph its query resolved, and the reason is the contract rather than the screen: no declared field can widen it. **NOTHING IS RECOMPUTED, AND THE MECHANICAL FORM OF THAT CLAIM IS A PROPERTY-ACCESS SWEEP**: every access on a section value is collected from the module's own source with comments stripped and the whole set must be `length`. `gate_results` is not a table in this tree at all -- `SD-06` split it into [`rule_states.engine_gates`](../../packages/db/src/schema.ts) and `context_gates`, `schema.ts:720` and `:721` -- and neither is spelled here. **A SEEDED `value[0]` WALKED PAST THE FIRST DRAFT OF THAT SWEEP AND PAST ITS RUN-TIME LEG TOO, AND THE REPAIR IS ITS OWN COMMIT.** The sweep matched `\bvalue\.\w+` and missed index access; the run-time leg looked only at values nested inside section objects, so `String(value[0])` rendered `[object Object]` and passed. **Both are repaired and the same seed now turns two cases red.** **NO FIGURE, ON THE TWO MECHANISMS `W6-g` MET ONE SECTION OVER**: this response carries no `as_of`, and [`figure.ts:70`](../../apps/admin/src/figure.ts)'s `ORIGIN_ID` closes at `P-M6-01` to `P-M6-10` and `AS-M6-04`, which are M06 section 3.1's panels, so a figure raised on section 3.2 has no origin to declare. Neither is widened. **NO INDEX ROUTE, FOR TWO REASONS AND THE SECOND OUTLIVES THE FIRST**: the search surface is a different contract row whose adapter nobody owns, and an index with no query behind it is `FM-M6-10`, which the registered endpoint already refuses by making `?query=` a validation failure when absent. **THE ROUTE NAMES NO ERROR KIND AND BOTH ACCOUNT ROUTES WERE MEASURED IN THIS SLICE** over a real `compose()` and the server injector: `GET /api/v1/admin/accounts/:accountId` and `GET /api/v1/admin/accounts?query=` each answer **401 `unauthenticated`** with no admin session cookie and **500 `internal_error`** with one, so `WAVE-06` section 8.1's 503 is refuted for a fourth and fifth route and `ADR-190`'s ruling is reproduced rather than inherited. **NINE SEEDED DEFECTS AND ONE OF THEM WAS NOT CAUGHT ON THE FIRST RUN**, which is the finding above; all nine are caught after the repair, each reverted with the tree clean. **THE COMPOSITION'S TWO DEFENCES WERE SEEDED SEPARATELY AND STILL FAIL AT THREE DIFFERENT TIMES**: an arm deleted from `composeAdminReadSource` is **`TS2741`** at `index.ts:288` over `AdminReadSource`; a name deleted from `IMPLEMENTED_ADMIN_READS` is **`TS2353`** at `:226` in `composeImplementedAdminReads` over `PartialAdminReadSource`, a different function over a different type; an uncomposed leg is **`AdminSourceNotComposed('readAccount')` at RUN TIME over a green typecheck**, and it throws **SYNCHRONOUSLY** where the port's signature returns a promise. **THE WIRING TRIPLE IS UNMOVED AT `{ declared: 23, wired: 6, blocked: 17 }`, derived at the moment of reporting**, and route counts unmoved over a real `compose()`: `public` 46 registered / 27 withheld, `operator` 28 / 45, reproduced on both sides. **No ADR number, no migration number, no `SystemReason` member, no `SqlExecutorReason` member, no `pg` import, no cast past a key type, no ledger account named, and `apps/api/**` and `packages/**` not touched at all.** 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean, **225 files / 5,319 passed / 6 skipped** against a `main` baseline of **224 / 5,288 / 6** reproduced exactly at `d86a029a` before a line changed, `falsify` clean and the tree clean after it. |
| [2026-08-28 - Session 354](2026-08-28-session-354.md) | **FIVE OF THE SIX ARE REPAIRED, ITEM 3 IS REPORTED-ONLY WITH ITS GROUND, AND NO ADR NUMBER AND NO MIGRATION NUMBER WERE TAKEN.** Seven commits, one per item plus two corrections this session made to its own work. **ITEM 1 IS THE ONE THAT COST SOMETHING AND IT IS NOT THE SWEEP.** Widening `format:check`'s directory term to `{apps,packages,e2e,scripts}` newly covers **29** file(s) under `scripts/`, of which **10** were unformatted, and the sweep is **360 insertion(s) and 179 deletion(s)** of quote style and line wrapping. **THAT SWEEP SILENTLY BLINDED `falsify.mjs` AND 33 OF 33 GATES STAYED GREEN THROUGH IT.** Prettier splits `['NAME', 'a long detail']` onto three lines once it passes the print width; `seriesTable` read `^\s*\['NAME', '` and then sliced to the end of that line, so `PENDING_SERIES` fell from **36 rows to 13** and `WITHHELD_MEMBERS` from **1 to 0**, and `falsify` went from clean to **3 problems**, two `SEED IS STALE` and one `CONTROL DID NOT FIRE` from a second anchor of the same shape. **THE GATE'S OWN READER NEVER STOPPED WORKING**, which is the independence [`falsify.mjs`](../../scripts/corpus/falsify.mjs) documents one comment above the break and is why both anchors were repaired rather than replaced by an import of the gate's parser. **ITEM 3 IS NOT A REPAIR AND IS NOT AN ADR EITHER.** [`0009:103-104`](../../packages/db/migrations/0009_ledger.sql)'s comment claims a reversal *"may not chain onto another reversal"* and `ledger_transactions_no_self_reversal` is `CHECK (reversal_of IS NULL OR reversal_of <> id)`. **EXECUTED HERE rather than inherited**: all 57 migrations applied to a real PostgreSQL 16 and a three-deep chain LANDS with every trigger installed. **`account_adjustments` DOES NOT CLOSE IT**: `ADJ-C1` is a trigger on `account_adjustments` and the chain was written straight into `ledger_transactions` with no adjustment row anywhere, so [`0057:156-157`](../../packages/db/migrations/0057_terminal_withdrawal_obligation.sql)'s *"already enforces it where reversals are actually written today"* holds only while `LT-09` has no writer, which is the next slice. **A CHECK CANNOT SEE ANOTHER ROW, so this needs a migration number nobody has and it is left.** **ITEMS 4 AND 5 BOTH DELETE THEIR CARDINAL RATHER THAN CORRECTING IT.** `M05` section 2.1's *"five shapes"* is a count of ROWS, checked before it was changed at [`M05:19`](../plans/M05-payout-system.md)'s *"`LT-nn` ledger transaction shapes"* and at [`ADR-189:23`](../decisions/ADR-189.md)'s *"declared eight transactions"*, so it tracked the table and drifted 5 to 8 to 9; the root `//typecheck` key's *"the two apps' e2e directories"* is three in `e2e/tsconfig.json`'s own `include`. **Both take [ADR-185](../decisions/ADR-185.md)'s ruling and [ALLOCATION:204](../decisions/ALLOCATION.md)'s closed set: generate it or delete it and point at the source, and there is no third.** Derived across item 4: `CI-06/derivable-counts` reads **94,139** cardinals where it read **94,140**. **ITEM 6 TRANSCRIBES [ADR-190](../decisions/ADR-190.md) INTO ITS OTHER FIVE HOMES AND TAKES NO NUMBER**, every one re-derived at source first because three of the six registered line numbers had already moved (`WAVE-06:581` was `:601`, `index.ts:345` was `:354`). **NO REPAIRED SENTENCE COUNTS EITHER SIDE OF THE PARTITION**, because ADR-190 section 5 pins no cardinal on purpose. **NO LINE MOVED UNDER A CITATION, AND THAT COST TWO REWRITES.** The first `ci.yml` repair ran six lines long and shifted five cited regions; it is now sixteen lines and the file is **708 against 708**. `client.ts` is **417 against 417**, so ADR-190's `client.ts:228` and `:244` still hold `toAdminErrorKind` and `AdminApiFailure`. **FIVE FINDINGS NOT IN THE DISPATCH.** [`WAVE-06`](../plans/WAVE-06-admin-console-transport.md) section 4.1 carried TWO FALSE CITATIONS inside the very sentence item 6 repairs, `admin-reads.ts:694` against `AdminReadSource` at **695** and `setAdminReadSource:706` against **739**, and **RI-16 IS BLIND TO BOTH**: seeded `:99999` turns it red, so the pointer is in scope and it is the NAME half that does not reach a name written inside its own backticks. [`ADR-080:25`](../decisions/ADR-080.md)'s `ci.yml:422` and [`P4:307`](../plans/P4-portal-and-site.md)'s `ci.yml:476` were **already false on `main`**. `e2e/tsconfig.json`'s `//include` states *"ALL THREE APPS'"* three lines above the list it counts, which is items 4 and 5's class. And `WAVE-06:264`'s *"against a 503"* was checked and deliberately NOT touched: it is the design argument and its referent survives, the write backends answering 503. **33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, `format:check` clean over the widened glob, `falsify` exit 0 with all 33 gates passing clean and failing dirty, 80 scope cases and 10 loader cases, tree clean after it, and 224 files / 5,288 passed / 6 skipped, which is the `main` baseline at `d86a029a` reproduced first and MOVED BY ZERO.** |
| [2026-08-28 - Session 355](2026-08-28-session-355.md) | **[ADR-193](../decisions/ADR-193.md) proposed, `status: proposed`, approval line UNSIGNED, MONEY PATH, `E2` READ OWED ON EVERY LINE OF [`0059`](../../packages/db/migrations/0059_reversal_chain.sql), NOT MERGED. MIGRATION `0059` IS TAKEN.** [`0009:103-104`](../../packages/db/migrations/0009_ledger.sql) has promised since the ledger was created that a reversal *"may not chain onto another reversal"* and `ledger_transactions_no_self_reversal` is `CHECK (reversal_of IS NULL OR reversal_of <> id)`, which is the whole of what a row-level `CHECK` can say. **THE FIRST QUESTION WAS WHETHER AND THE ANSWER IS THAT A CHAIN IS WRONG AS A LINK AND LEGITIMATE AS AN OPERATION.** A reversal of a reversal is a RE-APPLICATION and the corpus rules its shape in the SAME SENTENCE as the prohibition -- `0009:104`'s *"a reversal of a reversal is an adjustment and should be posted as one"* and [`0038:293-296`](../../packages/db/migrations/0038_account_adjustments.sql)'s *"not a hard case, it is a new credit with its own reason and its own second key"* -- so `LEDGER-C3` refuses the `reversal_of` LINK and ACCEPTS the same movement posted as a new transaction with its own kind, key and reason, which `K6` watches it accept. **`docs/`, `packages/` and `apps/` were searched for a corpus requirement that a reversal be reversed and EVERY HIT IS A MIGRATION, A PATCH OR A SEED**, so the re-application has no caller waiting for it. **THE CHAIN IS WORSE THAN THE DISPATCH DESCRIBED AND THAT IS THE FINDING.** Executed on the 57-file schema against a real `wallet_withdrawals` row: `LT-06` posts, the row transitions to `failed`, `LT-09` reverses it and the obligation is 0; the third row LANDS and drives `withdrawals_in_flight` back to **-25000** and `trader_wallet` to **+25000** with the global sum still **0**. **`WD-C1` IS SILENT AND NOT BY A DEFECT IN IT**: it is armed on `wallet_withdrawals` and that row never moves again, so **the chain RESTORES the exact defect [ADR-189](../decisions/ADR-189.md) and [`0057`](../../packages/db/migrations/0057_terminal_withdrawal_obligation.sql) exist to close**, wearing a correction's clothes. **THE RULE LIVES IN A TRIGGER BECAUSE THE BUILDER IS NOT WEAK HERE BUT INCAPABLE**: `reversalPosting` receives `reversalOf` as a `string` ([`reversal.ts:122`](../../packages/ledger/src/reversal.ts)), whether it names a row that is itself a reversal is a fact about a row, and `packages/ledger` *"opens no transaction and cannot"* in its own words at [`reversal.ts:259`](../../packages/ledger/src/reversal.ts). **AND THERE IS A SECOND DOOR**: [`posting.ts:136`](../../packages/ledger/src/posting.ts)'s header carries `reversalOf?: string`, so a rule in `reversalPosting` is bypassed by `posting()` one file over without a cast and without a lint, and [`posting.test.ts:130`](../../packages/ledger/test/posting.test.ts) builds one that way in the suite today. **`packages/ledger` GAINS A COMMENT AND NO CODE, AND THE ABSENCE IS THE RULING.** **`0009`'s CONSTRAINT IS NOT SUPERSEDED AND `0057` IS NOT TOUCHED**: two statements, two objects, no `DROP`, and the interaction is stated in `0059`'s header rather than left to a reader -- `0057`'s UNIQUE index refuses two reversals OF ONE transaction and `LEDGER-C3` refuses a reversal OF a reversal, different rows and different columns. **`OR UPDATE` IS THE ONE CHOICE WITH NO PRECEDENT IN THE TWO NEAREST FILES AND IT WAS MEASURED**: [`0026:88-107`](../../packages/db/migrations/0026_roles_and_grants.sql) revokes `UPDATE` from `merit_app` and `PUBLIC` and NOT from the owner, [`0026:109-115`](../../packages/db/migrations/0026_roles_and_grants.sql) designs a `SECURITY DEFINER` UPDATE door for exactly this class of table, and with `OR UPDATE` removed the chain is built by `UPDATE` and lands. **EXECUTED: 58 files forward-only from empty on PostgreSQL 16 under `ON_ERROR_STOP` with the 57-file baseline built FIRST and the re-apply rejected; tables 114, `CHECK` constraints 484 and indexes 397 ALL UNCHANGED, non-internal triggers 23 to 24 and functions 109 to 110. The chain WATCHED LANDING AND THEN WATCHED REFUSED, the legitimate first reversal still ACCEPTED, the "cannot see" branch REACHABLE and failing closed at READ COMMITTED and at REPEATABLE READ with two concurrent sessions, FOUR seeded defects watched turning `K6` and `K6b` red, and all 16 existing database probes passing on a `0059` schema.** **THREE FINDINGS NOT IN THE DISPATCH.** The dispatch and [ALLOCATION](../decisions/ALLOCATION.md) row `193` both cite *"ADR-189 section 5"* for the trigger-versus-builder argument and **it is section 4**, at `ADR-189:78`. The chain DEFEATS `WD-C1` rather than merely evading `0009`. And **`WD-C1` IS BLIND TO EVERY LEDGER POSTING WRITTEN AFTER A WITHDRAWAL REACHES A TERMINAL STATE**, of which the chain is only the shape that looks like a correction: `LEDGER-C3` does NOT close that and it is reported open for the slice that wires `LT-06` and `LT-07`. **ONE ERROR THIS SESSION MADE AND CAUGHT, RECORDED BECAUSE IT IS `LEDGER-C1`'s OWN CLASS**: the first draft of the probe function declared a local `found boolean`, which SHADOWS PL/pgSQL's special `FOUND`, so `IF NOT FOUND` read an uninitialised local, the branch silently never fired, and the FK quietly covered for it. It cost two mis-read race measurements and was caught by instrumenting the function rather than by reading it; **the shipped function declares one variable and it is not called `found`.** **`0059` IS TAKEN AND THE POOL IS EMPTY, `0060` IS NEXT**; `0058` stays returned-unspent under [ADR-191](../decisions/ADR-191.md) and this entry neither fills nor widens that hole. **No `SD-nn`, no `DELTA_MANIFEST` section, no table, column, enum value, ledger code, index, grant or revoke, no [M05](../plans/M05-payout-system.md) amendment and no edit to any merged migration.** Measured with `pnpm install` first, each command run separately and `pnpm run verify` never run, against a `main` baseline at `eccbee9d` reproduced before a line changed: **33 of 33 gates, 16 of 16 invariants, `typecheck` 0, `lint` 0, `format:check` clean, `falsify.mjs` clean with the tree clean after it, and 225 files / 5,335 passed / 6 skipped, MOVED BY ZERO**, which is the right answer for a slice whose whole content is unreachable from TypeScript. |
| [2026-08-28 - Session 356](2026-08-28-session-356.md) | **I AM AT 2 OF 2. BOTH ADAPTERS SESSIONS 349 AND 353 MEASURED AS UNWRITABLE ARE WRITTEN, IN TWO COMMITS, AND THE THING THAT CHANGED IS ONE ENTRY.** `TABLE_KEYS` holds **107** names with `events` among them, derived at reporting time by importing [`scope.ts`](../../packages/db/src/scope.ts) rather than by grepping, where session 353 read 106 and found it absent. **`listEvents` IS `admin-source/events.ts` AND ITS IDENTITY SCOPE IS THE DISJUNCTION RATHER THAN THE EQUALITY**, which is `ADR-191` clause 2 arriving at the read layer: an identity feed on `identity_id = $1` alone COMPILES, returns rows, and drops every account-level row, and that entry's section 4 counted the loss out of EVENTS.md at ten of the 29 `TL` rows. The accessor offers equality, so the disjunction is TWO READS unioned in memory on `graph.ts`'s precedent, `accounts` is the second leg's table, and a row reachable both ways is deduplicated and counted. **`readAccount` IS `admin-source/account.ts` AND IT IS ALL EIGHT SECTIONS**, because [`admin-reads.ts:1224`](../../apps/api/src/routes/admin-reads.ts)'s `projectAccountDetail` refuses one that omits a section the contract names. **`gate_results` NEEDED NO RULING AT ALL**: `SD-06` split it into [`schema.ts:733`](../../packages/db/src/schema.ts)'s `engine_gates` and `:734`'s `context_gates`, both carried verbatim, and a case reads the module's own logic and admits exactly two appearances of those names so nothing is derived. **THE LARGEST FINDING IS NOT IN THE DISPATCH AND IT IS A COLLISION BETWEEN THE SCHEMA AND A CONTROL: [`admin-reads.ts:326`](../../apps/api/src/routes/admin-reads.ts)'s `TRADING_DAY_KEY` REFUSES ANYTHING UNDER A `_day` OR `_on` NAME THAT IS NOT A `YYYY-MM-DD`, AND [`schema.ts:670`](../../packages/db/src/schema.ts)'s `traded_day` AND `:671`'s `win_day` ARE BOOLEANS.** Carrying them is a 500 on every drill-down that has a mark, measured through the real route; renaming them invents a field no column has. **They are OMITTED, the omission is asserted, and the sweep is `admin-reads.ts`'s to rule.** The same rule refuses `null` under a day-shaped name, so a nullable trading day is an ABSENT KEY and eleven nullable date columns feed these sections. **SECOND FINDING: THE DRILL-DOWN HAS NO `INV-M6-10` PROJECTION WHERE THE FEED HAS ONE.** `withholdForScope` ([`admin-feed.ts:330`](../../apps/api/src/routes/admin-feed.ts)) gates every key ending `identity_id` against the scope a query named; `GET /admin/accounts/:accountId` names its subject in the PATH, gates nothing, and its `events` section carries `kyc.dedupe_hit`'s `matched_identity_id` verbatim. Measured with a case and REPORTED rather than repaired inside an adapter, which is the shape `ADR-184` ruling 3 refused. **THIRD FINDING, AND IT CORRECTS THE DISPATCH: WIRING AN ADAPTER DOES NOT MOVE THE TRIPLE AND CANNOT.** `wiring.test.ts` counts SETTERS in `src/routes/`, `ADR-184` ruling 1 put the feed on the port that already has one, and this directory supplies four of seven methods so no value satisfies `AdminReadSource` and `start.ts` calls nothing. **`{ declared: 23, wired: 6, blocked: 17 }`, unmoved and re-derived**, and route counts unmoved over a real `compose()`: `public` 46 registered / 27 withheld, `operator` 28 / 45, both sides. **`IMPLEMENTED_ADMIN_READS` GOES FROM TWO TO FOUR** and the missing list goes from five to three, both by turning a case RED rather than by anyone remembering. **`searchAccounts` WAS NOT TOUCHED AND ITS REASON IS RECORDED IN THE CASE THAT NAMES IT**: a name fragment is a substring predicate and no registration decision reaches it. **FOUR CASES SERVE A REAL DRILL-DOWN THROUGH THE REAL ROUTE over a real `compose()` and the server injector, so the section allowlist and the scalar sweep run on real projected rows: `GET /api/v1/admin/accounts/:accountId` answers 200 where session 353 measured 500.** **TEN SEEDED DEFECTS, ALL TEN CAUGHT**, each reverted from a copy with `git status --porcelain` empty after every one, and the two sweep seeds turn the real-route cases red as well as the unit ones. **THE COMPOSITION'S THREE DEFENCES WERE SEEDED SEPARATELY AND STILL FAIL AT THREE DIFFERENT TIMES**: a name dropped from `IMPLEMENTED_ADMIN_READS` is **`TS2353`** at [`index.ts:236`](../../apps/api/src/admin-source/index.ts) in `composeImplementedAdminReads` over `PartialAdminReadSource`; the arm dropped from `composeAdminReadSource` is **`TS2741`** at `:301` over `AdminReadSource`, a different function over a different type; an uncomposed leg is `AdminSourceNotComposed` at **RUN TIME over a typecheck of ZERO `error TS`**, and it throws SYNCHRONOUSLY on both new methods. **FOUR SCREENS' BLOCKER PROSE IS NOW FALSE AND NONE OF IT WAS EDITED**, which is the fence: `apps/admin`'s feed page says `listEvents` is one "which no module in the tree supplies" and the account screens say the same of `readAccount`, and [`API_CONTRACT:1134`](../../docs/architecture/API_CONTRACT.md) still says `scope.ts` records why `events` "cannot be scoped at all". **One more control was widened rather than left**: `admin-source-flags.test.ts`'s no-`@merit/db` sweep read a hand-kept list of three files out of four and now reads the directory. **No ADR number, no migration number, no `SystemReason` member, no `SqlExecutorReason` member, no `pg` import, no cast past a key type, no ledger account named, `packages/**` and `apps/admin/**` not touched at all, and `admin-reads.ts` and `admin-feed.ts` not edited.** 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean, **227 files / 5,397 passed / 6 skipped** against a `main` baseline of **225 / 5,335 / 6** reproduced exactly at `eccbee9d` before a line changed, `falsify` read rather than exit-checked and the tree clean after it. |
| [2026-08-28 - Session 357](2026-08-28-session-357.md) | **[ADR-194](../decisions/ADR-194.md) SHIPS `proposed` WITH AN UNSIGNED APPROVAL LINE AND RULES SHAPE 1: A SEARCH TERM IS A VALUE THE ESTATE HOLDS AND NEVER A PATTERN THE OPERATOR COMPOSED, so `GET /admin/accounts`'s name fragment is REMOVED FROM THE CONTRACT.** **No vocabulary member added, `packages/db` NOT OPENED, no `SqlExecutorReason` member, no migration and no migration number.** **THE REFUSAL IS ON EVIDENCE AND NOT ON COST, AND THE EVIDENCE IS THAT THE TERM POINTS AT NOTHING.** `grep -rhoE "^  [a-z_]*name[a-z_]* +[a-z]"` over every migration returns `display_name`, `plans.name`, `carrier_name`, `event_name`, `file_name` and four `*name_match*` columns, and **`identities.display_name` is the only one that holds a person's name**: [`0002:37`](../../packages/db/migrations/0002_identity.sql) declares it `text NULL` *"Nullable because v1 never shows it"*, it is reserved for a leaderboard [ADR-170](../decisions/ADR-170.md) finding 13 measured as having NO TABLE, and [`M11:59`](../plans/M11-certificates-social-proof.md)'s `INV-M11-10` requires it to be a handle that is **not the trader's legal name**. **`grep -rn "legal_name\|full_name\|given_name\|family_name"` over the migrations and `schema.ts` returns NOTHING**: [`0003:49`](../../packages/db/migrations/0003_kyc.sql) keeps `provider_applicant_id`, *"the only pointer we keep"*, and [`0010:246`](../../packages/db/migrations/0010_payouts.sql) keeps `destination_name_match` as a BOOLEAN beside a score, which is a comparison outcome and not the names compared. **And `AdminAccountSearchItem` declares no name field**, so a fragment that matched would render no name back. **`INV-M6-10` IS THE SECOND REASON AND WOULD HAVE BEEN SUFFICIENT ALONE, AND NOBODY HAD READ IT AGAINST THE TERM**: [`M06:284`](../plans/M06-admin-ops-console.md)'s `FM-M6-10` states in its own mitigation column that *"Search requires a specific subject term"*. **THE DISCRIMINATOR IS NOT THE ROW COUNT**, which is the objection this ruling had to answer: [`M06:435`](../plans/M06-admin-ops-console.md) already ruled that naming a signal and listing the identities that share it IS a specific-subject query, so a coupon shared by thousands STAYS. **A coupon cannot be shortened into a wider coupon and a fragment can always be shortened**, and a term that returns more rows the LESS the operator knows is an enumeration control with a text box in front of it. **BOTH HALVES OF [ADR-157](../decisions/ADR-157.md)'s `LIKE` REFUSAL WERE RE-MEASURED AND BOTH SURVIVE**: a pattern is still a shape a caller composes, and [`admin-reads.ts:686`](../../apps/api/src/routes/admin-reads.ts) still states that `AdminAccountSearchItem` *"joins accounts to identities to flags to reconciliation state"*, so **a substring term admitted today would not have made `searchAccounts` writable** and would have been that entry's own *"widening that buys nothing and looks like it bought something"* a second time. **SHAPE 2 AND SHAPE 3 ARE PRICED RATHER THAN NAMED.** A substring term has no index to use (`identities` carries two indexes, both partial, on `status` and `payouts_frozen`), needs `%` and `_` escaped inside a bound parameter where a reviewer cannot see them, and **passes `ADR-157` clause 1's reach test while failing the enumeration one**, which no admitted term had to. A second surface needs a `SqlExecutorReason` member that `ADR-157` clause 7 and two phase rules foreclose by name, and its search-index reading is a **SECOND COPY of trader-identifying data outside every control `ADR-008`, `ADR-102`, `ADR-112` and `ADR-157` built**. **WHAT THIS DOES NOT UNBLOCK IS SAID RATHER THAN IMPLIED**: `searchAccounts` is still unimplemented and still claimed by no plan, and no adapter was written. **What changed is affordability** -- the fan-out is assembled in the runner on `ADR-157` section 5's own precedent, which an exact subject bounds by the page cap `INV-M6-10` already imposes and a fragment bounds by nothing. **ONE ASSERTION BINDS THE TWO TRANSCRIPTIONS, WHICH IS THE REPAIR FOR HOW THIS RESERVATION WAS CREATED**: the contract sentence and the route's validation message were two transcriptions of one list with nothing holding them together, and `grep -rln "name fragment"` found no test, no fixture and no gate on either. **FOUR SEEDS, ALL FOUR CAUGHT, ALL FOUR THIN AND RECORDED AS THIN**, the fourth being `searchTermsOf` returning `[]`, because two empty lists compare equal. **A FINDING NOT IN THE DISPATCH AND IT IS SESSION 354'S FROM THE OTHER SIDE**: `format:check`'s glob is `{apps,packages,e2e,scripts}` and does NOT read `docs/`, so running `prettier --write` by hand over two frozen documents reformatted `API_CONTRACT`'s frontmatter and every fenced block in it; both were restored with `git checkout` and the edits re-applied, and `ADR-157` still fails `prettier --check` today. **A SECOND: `docs/INDEX.md` NEEDED NO ROW**, its `decisions/` row stating that entry files are indexed by `decisions/README.md` under `CI-06n` and not by it. **`RI-17` UNMOVED**: no endpoint added, none removed, the heading unchanged, and that file's contract digest computes at run time rather than being pinned. **33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` clean with its OUTPUT read rather than its exit code, and 225 files / 5,336 passed / 6 skipped against a `main` baseline at `eccbee9d` of 225 / 5,335 / 6 reproduced exactly before a line changed.** |
| [2026-08-28 - Session 358](2026-08-28-session-358.md) | **ALL THREE ARE REPAIRED, NO ADR NUMBER AND NO MIGRATION NUMBER WERE TAKEN, AND EVERY ITEM WAS WATCHED FAILING BEFORE A LINE MOVED.** Four commits, one per item plus one correction this session made to its own work. **ITEM 1: `RI-16` BOUND ONLY A NAME WRITTEN IN FRONT OF A POINTER AND MARKDOWN WRITES IT INSIDE.** Session 354's own case was seeded and watched: rewritten to `setAdminReadSource:706` the citation now turns the check RED at **33 lines off**, where 16 of 16 held before. `citedInToken` tells the two apart mechanically rather than guessing: a prefix that is a PATH names the file, a prefix the target basename BEGINS WITH names the file by this corpus's nickname (``[`EVENTS:396`](../architecture/EVENTS.md)``), and a prefixed token whose path was INHERITED is a registry shorthand. **THAT LAST ROW IS A MEASUREMENT AND IT DECIDED THE RULE**: all seven prefixed tokens in the old input were `M07:nnn` or `M20:62`, so the in-token name is read ONLY where a markdown link STATES the path. Bindable names go **5 to 19** and all 14 added carry their own link; the corpus is re-derived at **916 documents, 2,950 path-bearing citations, 2,288 under a dated heading, 662 in scope and 1,284 bare**, against a `covers` line that still said 879, 2,340, 1,737, 603 and 1,123. It found **SIX drifted citations, four distinct, every one exactly THIRTY lines short**: [`WAVE-06`](../plans/WAVE-06-admin-console-transport.md) cites `renderLiabilityHome:630`, `assertNamesNoSubject:219`, `assertFloatIsNotReserve:263` and `buildLiabilityHome:391` and they are at `:660`, `:249`, `:293` and `:421`. **NEITHER FILE IS THIS FENCE'S, SO ALL FOUR ARE REGISTERED.** **ITEM 2: `RI-15`'s INPUT WAS SIX FILES SOMEBODY TYPED, WHICH IS THE HAND-MAINTAINED COUNT `RI-05`'s OWN `covers` NAMES**, and it drifted exactly that way when session 351 found `routes/verify.ts` outside it carrying the same two drifted pointers. **THE SET IS THE WALK NOW, MINUS THIS CHECK'S OWN PACKAGE, WALKED UP TO RATHER THAN WRITTEN DOWN**, and the one exclusion is measured: **all 78 findings inside `packages/tooling/` sit in the two files whose subject IS this grammar and the other four carry none.** **6 files to 584, 94 citations to 207, ONE finding to EIGHT.** Two reader defects the wider set exposed are FIXED rather than registered: a prefixed token now inherits NO path, because all **152** in this input are `0017:82`, `M07:111`, `EVENTS:407` or `+00:00` and **NOT ONE is a symbol in the file above it**; and `citationTargets` resolves a `../..` path against the citing file, which RI-16 already did for its links, which is **1 citation in 207** and was hiding a real finding. **ALL EIGHT ARE REGISTERED WITH THEIR OWNERS AND NONE IS IN A FILE THIS FENCE HOLDS: FOUR ARE A POINTER THAT NO LONGER LANDS** (`IMPLEMENTED_ADMIN_READS` cited `:178` and at `:193`; `firmTx.update` cited `scoped-db.ts:720`, nearest `update` 177 away; `atMost`'s `FilterTerm` cited `:662`, declared `:687`; `martingale` bound to the first of three parallel pointers where the citation is TRUE at `:1059`) **AND THREE ARE THE WRONG NOUN BESIDE A TRUE POINTER**, all exactly THREE lines off, which is the narrowest FALSE citation on record, so **widening the window from two to three is refused and stated rather than taken quietly.** `RI-15`'s register now SHRINKS ONLY on RI-16's rule and the fixture reproduces all eight at their real distances; the rename guard is replaced by the emptiness guard a derived set actually needs. **ITEM 3: `payout.freeze_expiring` WROTE AN UNTENANTED ROW AND NOTHING RAISED, AND THE SILENCE IS WHAT IS CLOSED.** [ADR-191](../decisions/ADR-191.md) ruled the class `either` correct and a row reaching neither leg correctly out; what it registered as its sharpest open item is that the producer writes one anyway, SILENTLY, on an append-only table. **NO CONSTRAINT CAN CATCH IT** (both columns nullable, `0058` returned to the pool over three failed candidates), so `assertTenanted` catches it at the WRITE PATH, inside `emit` and therefore inside the caller's transaction, and **the refusal is on the ROW rather than the NAME**: a payload arriving without the field its row declares refuses identically. **THE REPAIR IT NAMES IS THE PRODUCER'S AND NOT THIS FILE'S** and no field EVENTS.md does not carry is invented. **THE WHOLE CATALOGUE WAS READ AND THE MEASUREMENT IS BOUND IN THE SUITE RATHER THAN LEFT IN PROSE: 105 rows, 3 whose payload is given by reference and EXCLUDED rather than assumed, 34 of the remaining 102 naming neither column, and EXACTLY ONE of the 34 read by the trader timeline.** **THAT ONE IS NOT `payout.freeze_expiring`.** Its consumers are ALERT and FEED, so ADR-191's sharpest sentence, the row never appearing in the trader's TIMELINE, belongs to **`payout.settled`**, which has no producer in this tree yet. **THREE FINDINGS NOT IN THE DISPATCH.** [`39-fixture-status-and-blockers.md`](../testing/golden-scenarios/39-fixture-status-and-blockers.md) cites `events.test.ts:608` and `:617` and **BOTH WERE ALREADY FALSE ON `main`** (the PII floor at `:612`, the name-fields payload at `:628`) and this session moved them further, to `:716` and `:732`; **`RI-16` CANNOT SEE EITHER, and that is the residue item 1's own rule states**: the token names its own FILE and binds no name. The `RI-15` `covers` line read "32 of the 94" the moment the set widened and is corrected in its own commit, at **47 of 207**. And **every cardinal in `RI-16`'s header had drifted before this session touched it.** **NO LINE CITED INTO `repo-invariants.mjs` MOVED**: `:359`, `:363`, `:937`, `:951`, `:1318` and `:1334-1335` read byte for byte what they read on `main` across 347 added lines. **33 of 33 gates, 16 of 16 invariants in 0.8s, typecheck 0, lint 0, format clean, `falsify` exit 0 with all 33 gates clean and dirty, 80 scope cases and 10 loader cases and the tree clean after it, and 225 files / 5,345 passed / 6 skipped against a `main` baseline of 225 / 5,335 / 6 reproduced exactly at `eccbee9d` before a line changed. AND SESSIONS 355, 356 AND 357 LANDED ON `main` WHILE THIS BRANCH WAS OPEN**, bringing `ADR-193`, `ADR-194`, `0059` and both admin read adapters into a directory `RI-15` had started reading three commits earlier. **`RI-15` HELD AT 16 OF 16 ACROSS THE MERGE**, which is the widened input reading four source files nobody had shown it before and finding nothing, and **every cardinal in the file is re-derived on the merged head**: 588 files where it says 584, 154 prefixed tokens where it says 152, 922 documents, 3,055 path-bearing citations, 2,387 under a dated heading, 668 in scope, 1,414 bare and 157 resolved by a link alone. **`main` at `0306971` is 227 files / 5,398 passed / 6 skipped, measured in a worktree rather than inferred, and this branch is 227 / 5,408 / 6, a delta of exactly the ten cases this session wrote.** |
| [2026-08-28 - Session 359](2026-08-28-session-359.md) | **THE PROJECTION IS IN [`admin-reads.ts`](../../apps/api/src/routes/admin-reads.ts) ON THE RESPONSE, AND [ADR-184](../decisions/ADR-184.md) RULING 3 IS THE CITATION IN ITS OWN WORDS**: [`ADR-184:48`](../decisions/ADR-184.md), the withholding is *"A PROPERTY OF THE RESPONSE AND NOT OF THE RENDERER"* with the control *"run over the serialized body and not over the rows"*, so not the adapter, and [`scope.ts:237`](../../packages/db/src/scope.ts) draws the same line about this exact pair of payload keys, *"that exclusion is a PROJECTION and not this rule"*. **THE COUNT RE-DERIVED AT REPORTING TIME: `withholdForScope` appears TWICE in [`admin-source/events.ts`](../../apps/api/src/admin-source/events.ts), at `:20` and `:452`, both in prose, and ZERO times in [`admin-source/account.ts`](../../apps/api/src/admin-source/account.ts).** **IT IS A LICENCE AND NOT A REFUSAL AND THE LICENCE IS TWO IDS**: [`accountDetailLicence:1433`](../../apps/api/src/routes/admin-reads.ts) admits the account the PATH named and the identity that account row names, because [`0007:39`](../../packages/db/migrations/0007_accounts.sql) declares `accounts.identity_id uuid NOT NULL REFERENCES identities(id)` and `identity` is one of the eight sections the contract names, so withholding the owner would blank a section section 8 asks for. It admits nothing else. **THE ROOT IS CHECKED AGAINST THE PATH INSIDE THAT SAME FUNCTION**, because a licence taken from a response nobody checked licenses whatever the port returned, and separating the two leaves an order a later caller can get wrong, which is `FM-M6-10`'s shape. **THE LARGEST FINDING IS NOT IN THE DISPATCH AND IT IS WHY THE FEED'S CODE IS NOT REUSED: A VALUE IMPORT FROM `admin-reads.ts` BACK TO `admin-feed.ts` REGISTERS `GET /admin/events` WITH `roles: undefined` UNDER VITEST AND A GREEN SUITE OVER IT.** `admin-feed.ts` already imports six names from `admin-reads.ts`, so the back-import is a cycle; measured on this branch in both runtimes rather than reasoned about, real Node ESM throws `ReferenceError: Cannot access 'ADMIN_READ_ROLES' before initialization` and Vite's transform does not throw at all, evaluating `ADMIN_FEED_ENDPOINTS` with `roles: undefined` and `ADMIN_FEED_ROLE_TABLE` as `{ 'GET /admin/events': undefined }`. **The silent outcome is why the import is refused rather than ordered around**; [`admin-reads.ts:99`](../../apps/api/src/routes/admin-reads.ts)'s existing `import type` from the same module is not that cycle because it is erased. **SO THE RULE IS RESTATED AND THE TWO TRANSCRIPTIONS ARE BOUND BY AN ASSERTION**, session 357's repair for the same shape, both halves non-empty before they compare. **SECOND FINDING: `admin-source-flags.test.ts:1229` SWEEPS `admin-reads.ts` FOR THE RAW STRING `admin-source`, SO NO COMMENT IN THAT ROUTE MODULE MAY EVER NAME THE ADAPTER DIRECTORY.** It is a control over PROSE and not over imports; it was obeyed rather than weakened, and the prose names the adapter by what it supplies. **THIRD: THE LICENCE PLACES THE FIRST FIELD-LEVEL REQUIREMENT ANYTHING HAS PLACED ON THE ONE ROUTE THE CORPUS DOES NOT TYPE.** The `account` section must be a record carrying `account_id`, because the root has to be checkable; `admin-reads.test.ts`'s `DETAIL` stub was eight empty lists and its two 200 cases went red, which is the control working. **ELEVEN SEEDS AND TWO SURVIVED THE FIRST PASS**, both closed with cases rather than recorded as gaps: the wire control made a no-op passed everything because no fixture produced a withheld value that survived elsewhere, and the prototype guard removed passed because no fixture carried a `Date` inside a stored bag. All eleven fail now. **FOUR SCREENS REPAIRED AND ONE DOCUMENT REGISTERED.** Derived at reporting time by importing rather than grepping: `TABLE_KEYS` holds **107** names with `events` among them and `IMPLEMENTED_ADMIN_READS` holds **FOUR**, where four panels said `events` was unregistered and the list held two. **The blocker MOVED rather than cleared and that is what the panels now say**: `exportEvidence`, `readLiability` and `searchAccounts` have no module, so no value satisfies `AdminReadSource`. **TWO RENDER CASES PINNED THE STALE SENTENCES THEMSELVES**, one requiring the words *"unregistered in `packages/db`"* and one *"rejected response"*, and a sentence a case asserts cannot be repaired without the case. **[`API_CONTRACT:1157`](../architecture/API_CONTRACT.md) IS REPORTED AND NOT EDITED**, frozen, and only its middle clause is false: `events` is `either` and a `ScopedTableKey`, while the same line's *"a scope rule cannot express that and a projection can"* is the ruling this session applied. **NO ADR NUMBER, NO MIGRATION NUMBER, NO `SystemReason` MEMBER, NO `SqlExecutorReason` MEMBER, NO `pg` IMPORT, NO CAST PAST A KEY TYPE, `packages/**` NOT TOUCHED, AND `TRADING_DAY_KEY` AND `assertContractScalars` NOT OPENED.** **A projection moved none of the wiring**: `{ declared: 23, wired: 6, blocked: 17 }` and, over a real `compose()`, `public` 46 registered / 27 withheld and `operator` 28 / 45, both unmoved. 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean, `falsify.mjs` READ rather than exit-checked, and **227 files / 5,415 passed / 6 skipped** against a `main` baseline at `03069710` of **227 / 5,398 / 6** reproduced exactly before a line changed. |
| [2026-08-28 - Session 360](2026-08-28-session-360.md) | **THE RULE'S SHAPE IS RIGHT AND ITS EXTENT WAS WRONG, AND THE EVIDENCE IS API_CONTRACT ITSELF RATHER THAN THE SCHEMA.** Parsing every `ts` block of the frozen contract returns **22 member declarations under a `_day` or `_on` name over 14 distinct keys, in FOUR forms**: seven keys are a `YYYY-MM-DD` string, five are `string \| null`, one is a container, and **TWO ARE `boolean` -- [`API_CONTRACT:418`](../architecture/API_CONTRACT.md)'s `MarkListItem` writes `traded_day: boolean; win_day: boolean;` on ONE LINE of the same document whose section 1 Time rule the sweep was enforcing**. So `assertContractScalars` admitted one of the three scalar forms its own primary source declares. **SESSION 356's SENTENCE THAT THE RULE IS *"correct for every typed response in API_CONTRACT section 8"* IS TRUE AND IRRELEVANT: the collision is with SECTION 6**, and [`account-reads.ts:170-171`](../../apps/api/src/routes/account-reads.ts) already declares both booleans and `:643-644` already ships them on `GET /accounts/:accountId/marks`, a live trader route in this tree. **THE ADMISSION IS BY TYPE AND NOT BY AN EXEMPTED LIST, AND THE LIST WOULD ALREADY BE STALE**: [`0004_catalog.sql:323`](../../packages/db/migrations/0004_catalog.sql)'s `is_half_day boolean NOT NULL` is a THIRD boolean under a day-shaped name, on the trading calendar table itself, which is `RI-05`'s *"hand-maintained count in a different costume"* arriving before the list is written. Of **29 distinct day-shaped columns across the migrations, 3 are `boolean`, 1 is `text` (`breaks_on text[]`) and the rest are `date`.** **WHAT ADMITTING A BOOLEAN COSTS WAS PRICED RATHER THAN ASSERTED: nothing. A boolean is not a failed day**, because no date rendering path produces `true`, and the three forms a wrong trading day actually takes -- a UTC instant, an epoch number, a `Date` -- are all still refused, **seeded and watched being refused THROUGH THE REAL ROUTE at 500**. **THE REPAIR TIGHTENS AS WELL AS WIDENS, AND THAT HALF WAS NOT IN THE DISPATCH: A `Date` UNDER A DAY-SHAPED NAME USED TO PASS.** The container exemption written for `eligible_next_7d.by_day` was `typeof member === 'object' && member !== null`, and `Object.entries(new Date())` is `[]`, so a `Date` was walked, found to carry nothing, and ADMITTED; it serialises `"2026-08-27T00:00:00.000Z"`, which is section 1's forbidden UTC instant reaching an operator THROUGH the control that exists to stop it. `isWalkableContainer` now descends into a plain object or an array and nothing else. **A NULLABLE TRADING DAY SERVES AN EXPLICIT `null`, BECAUSE THE CONTRACT WRITES `string \| null` FIVE TIMES** (`funded_on`, `closed_on`, `covered_through_day`, `next_eligible_trading_day`, `TimelineItem.trading_day`), so an absent key was a wire shape the document never declares; **nine `optionalDay` call sites across three projectors change shape and NOT ONE CALL SITE IS EDITED**. **THE BOOLEANS ARE CARRIED AND NO CONTRACT MOVED, SO THERE IS NO ADR**: the drill-down's field-level schema is still the DEBT [`admin-reads.ts:672`](../../apps/api/src/routes/admin-reads.ts) records, and the two columns are carried under section 6's own declaration of the same two names on the same table, in the DDL's own names. **THE ADMITTED FORMS ARE DERIVED FROM THE FROZEN DOCUMENT IN THE SUITE RATHER THAN RESTATED**, with both halves asserted non-empty first on session 357's landmine, so a fifth form declared later turns a case red instead of being silently admitted; the refusal case sweeps five wrong values across all 14 declared names. **SESSION 356's TWO OMISSION ASSERTIONS WERE READ BEFORE ANYTHING CHANGED AND BOTH TURNED RED ON THE REPAIR, WHICH IS WHAT THEY WERE FOR.** **A FINDING NOT IN THE DISPATCH AND IT COST A COMMENT: [`admin-source-flags.test.ts:1229`](../../apps/api/test/admin-source-flags.test.ts) forbids the STRING `admin-source` anywhere in `admin-reads.ts`**, so the sweep cannot cite the modules that feed it even in prose. The control is right by its own terms, it was not weakened, and the citation was dropped instead. **A SECOND FINDING NOT IN THE DISPATCH AND IT IS A CONTROL GAP: THIS COMMIT SHIFTS TWELVE LINE CITATIONS INTO `admin-reads.ts` BY 65 AND NEITHER `RI-15` NOR `RI-16` REPORTS IT**, because eleven of the twelve name no symbol beside the path, so both checks fall back to resolution-and-range only and a file that got LONGER always satisfies that. Section 8 lists twelve with their new lines and **section 9 corrects that figure: the real number is 105 citation SITES**, because the sweep behind it deduplicated to one per file and its regex could not see the second citation form at all, a link whose TEXT is `Symbol:NNN`. **The correction was forced by the founder's own merge commit, which repaired a citation of that form the entry never listed**, and it left the OTHER half of the same line stale. **That one is repointed here, and so are session 359's four `accountDetailLicence:1368` sites, whose target this same diff moved 65 lines to `:1433` while `RI-16` read 16 of 16 over all four.** **SEVEN SEEDS, ALL SEVEN CAUGHT**, each applied to the committed shape and restored from a copy with `git status --porcelain` empty after every one. 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` READ rather than exit-checked, and **227 files / 5,402 passed / 6 skipped** against a `main` baseline at `03069710` of **227 / 5,398 / 6** reproduced exactly before a line changed. **No ADR, no migration, no migration number, `packages/db/**` not opened, `API_CONTRACT` not edited, `apps/admin/**` not touched, and `TRADING_DAY_KEY` itself is unchanged.** |
| [2026-08-28 - Session 361](2026-08-28-session-361.md) | **[ADR-195](../decisions/ADR-195.md) SHIPS `proposed` WITH AN UNSIGNED APPROVAL LINE AND RULES A TERM RATHER THAN A FIGURE: `P-M6-01` HAS THREE COMPONENTS AND THE THIRD IS THE FIRM-SCOPED `withdrawals_in_flight` OBLIGATION.** **No migration number reserved or taken, `packages/db` not opened, no code edited, no test moved, `INV-M6-11` not touched, `M05` not opened at all.** **THE DEFECT IS LARGER THAN THE DISPATCH AND [ADR-181](../decisions/ADR-181.md) SECTION 7 BOTH STATE IT, AND THE EXTRA HALF IS WHAT DECIDES TERM VERSUS FIGURE.** Under the two-term panel `LT-06` moves only `wallet_balances_cents` and **`LT-07` moves NEITHER term**, so the reported liability falls at the APPROVAL and does not move at the SETTLEMENT; and [ADR-189](../decisions/ADR-189.md)'s `LT-09` makes it three, because a rail-exhausted withdrawal would make it RISE at a moment when nothing new is owed. **One missing term, three wrong movements, and a figure beside cannot repair any of them because the defect is in the TOTAL.** **A FOURTH SHAPE WAS AVAILABLE, COSTS NO COLUMN AND NO MIGRATION, AND IS REFUSED ON A MEASUREMENT**: folding the obligation into `wallet_balances_cents` breaks the deliberate shared read at [`liability.ts:534`](../../apps/admin/src/liability.ts), *"one quantity read from one column and shown in two places is a quantity that cannot drift between them"*, so it would overstate `P-M6-07`'s float by every cent in transit and contradict [`M20:64`](../plans/M20-wallet.md)'s `INV-M20-08` segregation. **`INV-M5-15` DOES NOT MOVE AND ITS WORDS DO NOT COVER THE OBLIGATION, WHICH ARE TWO ANSWERS AND NOT ONE**: after `LT-06` the amount is not a wallet balance, and the invariant still does not move because nothing in it is wrong and what was missing is a rule about a PANEL, which [`M06:35`](../plans/M06-admin-ops-console.md) and [`M20:50`](../plans/M20-wallet.md) both route to M6. **THE DISPATCH'S CONDITIONAL IS UNSOUND IN BOTH DIRECTIONS** and the entry says so: whether the repair is a term or a figure is decided by what the panel means, not by whether a sentence in another module stretches. **THE DISPATCH'S "no migration number is needed" PREMISE IS HALF FALSE AND THE FALSE HALF DECIDES**: the LEDGER holds the obligation and **`liability_snapshots` does NOT**, so under [ADR-188](../decisions/ADR-188.md) clause 1's column-for-column projection the term needs a column that does not exist, and section 6 lists the four things owed rather than implying them. **The delta is named BY POSITION** as [`M06:80`](../plans/M06-admin-ops-console.md)'s single unclaimed number between `SD-M6-07` and `SD-M6-09`, because spelling it would demand a `DELTA_MANIFEST` row for a delta nobody has designed. **THE ORDERING CONSTRAINT IS THE SENTENCE A SEQUENCING SESSION NEEDS AND IT IS FAVOURABLE**: nothing posts `LT-06` today, so the panel is incomplete rather than wrong, and [`0049:97-98`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql)'s `NOT NULL` with no default is still available because that table still has no producer, re-measured rather than inherited. **`INV-M6-15` IS MINTED AND THE MEASUREMENT AROUND IT IS THE SESSION'S SHARPEST FINDING.** The prediction was that minting it would FAIL `CI-06/identifier-series` by emptying `INV-M6`'s pending set; run as a trial edit and again on the real one, **it PASSES both times**, and the reason is that [`M06:218`](../plans/M06-admin-ops-console.md)'s `#### INV-M6-10` HEADING is a second definition site, so **the register's stated reason (*"some have no row"*) and the live defect (a duplicate) are two different defects.** Reported and not repaired: `gates.mjs` is not this fence and rewording an approved heading to satisfy a census should be ruled rather than slipped in. **A SECOND FINDING NOT IN THE DISPATCH, ON THE MONEY PATH**: [`wallet-withdrawals.ts:78`](../../apps/api/src/routes/wallet-withdrawals.ts) states that `LT-06` *"credits `firm_treasury` (M05 section 2.1)"*, and [`M05:135`](../plans/M05-payout-system.md) says in terms that it is **NOT** `firm_treasury`; `RI-15` and `RI-16` bind a cited LINE to the name beside it and this citation names a SECTION, so nothing checks it. **THE ASSERTION IS WRITTEN OUT AND NOT TAKEN**: `theThreeNumbers` gaining a third component, a third addend at [`liability.ts:184`](../../apps/admin/src/liability.ts) and a case proving the sum is unchanged when `amount_cents` moves between two terms, left for `P5-l` because those three files have blocked registered repairs across five waves and a ruling that cannot yet be rendered should not be what finally opens them. **`docs/INDEX.md` NEEDED NO ROW** ([`INDEX:46`](../INDEX.md)), which is [session 357](2026-08-28-session-357.md)'s finding holding a second time. **33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` clean with its OUTPUT read rather than its exit code, and 227 files / 5,398 passed / 6 skipped against a `main` baseline at `03069710` of 227 / 5,398 / 6 reproduced exactly before a line changed.** |
| [2026-08-28 - Session 362](2026-08-28-session-362.md) | **`RI-18` LANDS AS A ROW OF `CHECKS` IN [`response-shape-copies.mjs`](../../packages/tooling/checks/response-shape-copies.mjs), SO `check:invariants` REPORTS **17**, AND IT LANDS **RED ON 8 FINDINGS** WHICH IS THE CHECK WORKING RATHER THAN A DEFECT.** **THE DISPATCH'S THREE LINE NUMBERS WERE ONE ENTRY OLD AND ONE HAD MOVED**: the copies are [API_CONTRACT:896](../architecture/API_CONTRACT.md), [`admin-reads.ts:529`](../../apps/api/src/routes/admin-reads.ts) and [`api/types.ts:144`](../../apps/admin/src/api/types.ts), where the dispatch read `:127`. **THE THREE DISAGREE TODAY AND THE DISAGREEMENT IS EXACTLY `ADR-188`'s DELTA AND NOTHING ELSE**: the contract carries **50** field paths and each code copy **42**, and **the two code copies agree with each other EXACTLY, path for path**, so the divergence is one document ahead of two files rather than three files drifting. The eight are `wallet_balances_cents`, `bounded_near_term_cents`, `remaining_ladder_exposure_cents`, `absorbed_corrections_cents`, and `reserve`'s `as_of`, `treasury_account_code`, `treasury_as_of` and `treasury_source`. **`API_CONTRACT.md` WAS NOT EDITED AND `apps/admin/src/api/types.ts` WAS NOT EDITED**: none of the eight is a transcription slip, all eight are `ADR-188`'s code half, and that half is one atomic change in another fence. **THE BOUNDARY PERMITTED THE HOME `RI-17` COULD NOT TAKE, AND SESSION 342's THREE DISQUALIFIERS WERE EACH RE-MEASURED AS ABSENT**: this check imports no deployable, needs no `fastify`, and its parse is synchronous. **AND THAT SESSION'S REFUSAL OF A TEXT READ DOES NOT REACH IT, ON A DIFFERENCE IN KIND**: it refused a regex over route files because a real `compose()` was a strictly better input, and **a TypeScript type is ERASED before anything runs**, so no composition, import or runtime value can be asked what fields a type has and the declaration is the only source there is. **SO THE READING IS A PARSE AND NOT A GREP**: the TypeScript compiler's own parser reads the markdown fence, the `interface` and the `type` alias under one grammar, follows a type reference rather than comparing it as a leaf, and **reports every position it could not read as a FINDING with its `file:line`**. **NO STORED LIST OF FIELDS EXISTS ANYWHERE IN IT.** **A PREMISE OF `ADR-188` AND OF THIS DISPATCH IS NARROWER THAN STATED, AND IT IS THE SESSION'S SHARPEST FINDING**: both recorded `CI-01`'s `tsc` as *"the only thing standing between a half-applied field addition and silence"*, and a field seeded on the **`apps/admin` copy alone** leaves `pnpm run typecheck` at **exit 0 with zero `error TS`** and the whole suite unmoved, because `tsc` fires where a copy is CONSTRUCTED and never where two declarations in two packages merely disagree. **The console's copy was covered by nothing at all.** **A SECOND FINDING NOT IN THE DISPATCH IS A LATENT DEFECT IN `RI-11`'s OWN PRECEDENT**: `ui-server-endpoints.mjs` and `repo-invariants.mjs` form a cycle whose header says it links because neither side touches the other at module-evaluation time, and the `CHECKS` array literal does, so importing that module FIRST throws `ReferenceError: Cannot access 'ri11' before initialization`, measured. `RI-18` takes its package lister as an ARGUMENT and has no cycle; `ri11`'s instance is reported rather than repaired because two sessions are live in that file. **A THIRD: `API_CONTRACT.md` DECLARES `VerifyResponse` TWICE**, at `:117` and `:787`. **THE POPULATION IS ONE SUBJECT ON A MEASUREMENT RATHER THAN A PREFERENCE**: run general over the contract's **86** type declarations across **56** ` ```ts ` fences and **509** workspace `.ts` files, **70** are declared somewhere under `apps/` or `packages/` and **10** of those diverge over **76** paths, in three classes each needing a ruling nobody holds (a name collision is not a copy, a union is not a field set, and a real divergence outside this fence is still a real divergence). **RUNTIME: `RI-18` alone 0.44s, the full invariant run 1.17s against a 0.83s baseline**, the cost being one lazy `require('typescript')` at 465ms that only the run reaching it pays. **TWENTY-ONE CASES, WITH A SEED IN EACH OF THE THREE COPIES SEPARATELY AND ALL THREE CAUGHT.** [ALLOCATION](../decisions/ALLOCATION.md)'s `RI-18` row is written in the commit that writes the check, per that table's own rule, and `RI-19` is now the next free number. 33 of 33 gates, **16 of 17 invariants**, typecheck 0, lint 0, `format:check` clean, **228 test files / 5,450 passed / 6 skipped / 1 FAILED**, the one failure being `repo-invariants.test.ts`'s real-tree case for `RI-18`, against a `main` baseline of **227 / 5,429 / 6 measured at `63b73fc` after the merge** (and 227 / 5,398 / 6 reproduced exactly at `03069710` before a line changed): the delta is **+1 file and +22 cases, 21 of them passing and the 22nd being the intended red**. `falsify.mjs` clean, read as output rather than as an exit code. **No ADR number taken, no migration number taken, `API_CONTRACT.md` byte for byte unchanged, and no file under `apps/` touched.** |
| [2026-08-28 - Session 363](2026-08-28-session-363.md) | **`ADR-188`'s CODE HALF LANDS AND `RI-18` REPORTS ZERO ON THE MERGE OF THIS BRANCH AND [#479](https://github.com/lam2-coder/meritfutures/pull/479), WHICH IS THE WHOLE POINT OF THE SLICE.** All three declarations of `LiabilityResponse` now carry **50** field paths and are **identical path for path**, derived at reporting time by importing session 362's own check rather than by counting: [API_CONTRACT:896](../architecture/API_CONTRACT.md), [`api/types.ts:144`](../../apps/admin/src/api/types.ts) and [`admin-reads.ts:529`](../../apps/api/src/routes/admin-reads.ts), where each code copy carried 42 before. **The eight are `ADR-188`'s and none was a transcription slip**: clause 1's four `liability_snapshots` columns and clause 4's four `reserve` members. `open_liability_cents` keeps its name, no total is sent, and `absorbed_corrections_cents` is carried **negative in both fixtures** so a clamp at zero fails rather than passes. **`withdrawals_in_flight_cents` DID NOT LAND AND THE REASON IS A COLUMN CENSUS RATHER THAN A JUDGEMENT**: `ADR-195` section 6 row 1 owes the column and takes no migration number, its row 3 says the field follows *"the moment the column exists"*, and `ADR-188` clause 5 refuses it before then. Re-derived: **58 migrations declare 735 distinct column names and that is not one of them.** **THE ADAPTER IS NOT WRITTEN AND THAT IS THE SESSION'S LARGEST FINDING, MEASURED IN FOUR PARTS.** `LiabilityResponse` projects **six groups and exactly one has a producible source**: `liability_snapshots` is a `TableKey`, so `ADR-188` clause 1's seven top-level fields are a keyed read plus ordinary code. **`reserve` is a COMPILE ERROR and not a design gap**: [`0049:154`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql) creates `reserve_coverage_snapshots` and **no file of `packages/db/src/` registers it**, so a `Tx` naming it is `TS2322` against `TABLE_KEYS`, seeded and watched, which is **session 349's refusal on a different table and took `ADR-191` to clear**. **`per_plan[].cusum`, `integrations.batch` and `eligible_next_7d` need columns no migration declares at all**, so that code would compile and have nothing to read. So the remaining work is a migration and a registration, both in `packages/db`, and **a module that could not be composed would have been an unimported file**: `IMPLEMENTED_ADMIN_READS` stays at **four** and `setAdminReadSource` stays BLOCKED. **[`admin-source-liability.test.ts`](../../apps/api/test/admin-source-liability.test.ts) IS THE SUITE OF THAT ABSENCE, TWELVE CASES, EACH WITH A STATED CLEARING CONDITION**, so the day a blocker lifts a case goes red and names it instead of the next session re-deriving all four. **Its census asserts its own non-vacuity FIRST, through an `ALTER`-added column**, because a parse matching nothing would make every absence pass for the wrong reason. **Five seeds, five reds**: the census with `ADD COLUMN` removed, the column seeded into a migration, the table registered, the method composed, the refusing arm replaced by a resolved null. **THE THREE DEFENCES WERE SEEDED SEPARATELY AND FAIL AT THREE SITES**: `TS2353` (excess property on the `Pick`), `TS2741` (missing property on the `Pick`), `TS2741` (missing property on `AdminReadSource`), plus the synchronous `AdminSourceNotComposed('readLiability')` already held at [`admin-source-evidence.test.ts:1162`](../../apps/api/test/admin-source-evidence.test.ts). **TWO FINDINGS THE DISPATCH DID NOT CARRY, AND BOTH ARE CITATION DEBT ON `admin-reads.ts`'s OWN LANDMINE.** The code half moved `setAdminReadSource` by 44 lines and `RI-16` went red on [WAVE-06:257](../plans/WAVE-06-admin-console-transport.md); it is repointed in a commit of its own. **AND [#479](https://github.com/lam2-coder/meritfutures/pull/479) LANDS `RI-16` RED ON ITS OWN BRANCH, WHICH ITS LOG DOES NOT RECORD**: its edits moved `DEPLOYABLES` from `359` to `385` in `repo-invariants.mjs` and [WAVE-06:174](../plans/WAVE-06-admin-console-transport.md) still cites `359`. **It is repairable on neither branch alone** and is one integer at merge time. 33 of 33 gates, **16 of 16 invariants on this branch**, **16 of 17 on the merge with `RI-18` GREEN and `RI-16` the one red**, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` clean read as output, **228 files / 5,441 passed / 6 skipped** against a `main` baseline of **227 / 5,429 / 6** reproduced first at `087fa570`. **No ADR number, no migration number, no `SystemReason` member, no `SqlExecutorReason` member, no `pg` import, no cast past a key type, no ledger account named, `API_CONTRACT.md` byte for byte unchanged, `packages/**` untouched, and no comment in `admin-reads.ts` names the adapter directory.** |
| [2026-08-28 - Session 364](2026-08-28-session-364.md) | **I AM AT 1 OF 2 ON `P5-l` AND THE OTHER HALF IS NOT MINE TO TAKE.** [ADR-195](../decisions/ADR-195.md) section 9's assertion is TRANSCRIBED rather than approximated: `theThreeNumbers` gains a third component, the sum at [`liability.ts:303`](../../apps/admin/src/liability.ts) gains a third addend, and `M6-A-70` is [ADR-195](../decisions/ADR-195.md) section 4's table over `LT-06`, `LT-07` and `LT-09` rather than a paraphrase of it, including the two-term panel FAILING the approval case. **THE FIELD IS OPTIONAL AND THE ABSENCE IS THE HONEST SHAPE**: no column of `liability_snapshots` holds the obligation, re-derived at `0009` and `0049`, so an unsupplied term renders ABSENT with that reason and the total says it is INCOMPLETE. **A SECOND FINDING WAS FOUND BY READING RATHER THAN BY THE DISPATCH AND IT IS IN THIS FENCE**: `floatCoverage`'s absence said `GET /admin/wallet/reconciliation` "is registered by no route module in this tree" and that has gone FALSE, so the reason is repaired and BOUND to the route module by a test that reads it. **`P5-l`'s DATA-SOURCE HALF CANNOT LAND HERE AND THE MEASUREMENT SAYS WHY**: `LiabilityResponse` carries `open_liability_cents` and none of the other four figures `LiabilityHomeInput` reads, and `readLiability` is not one of `IMPLEMENTED_ADMIN_READS`' four. **NINE REGISTERED DEBTS COLLECTED WITH A DISPOSITION EACH.** Seven seeded defects, each watched failing and reverted from a byte-identical copy with the hash checked both ways. **227 files / 5,451 passed / 6 skipped** against a `main` baseline of **227 / 5,429 / 6** reproduced exactly at `087fa570` before a line changed; 33 of 33 gates, 16 of 16 invariants, `falsify` clean read at its output, typecheck 0, lint 0, format clean, `@merit/admin` **five routes**, unmoved. **No ADR number, no migration number, no `SD-nn`, no new export, no barrel leg, `figure.ts` not opened, `live-liability.ts` not opened, `apps/admin/src/app/**` not opened, `apps/api/**` and `packages/**` not touched at all.** |
| [2026-08-28 - Session 365](2026-08-28-session-365.md) | **[ADR-196](../decisions/ADR-196.md) SHIPS `proposed` WITH AN UNSIGNED APPROVAL LINE AND RULES THE MOMENT, THE UNIT OF WORK AND THE ROW'S CONTENTS. `0060` RETURNS TO THE POOL UNSPENT, THE SEVENTH TIME.** **RULED AND PRICED, NOT BUILT**, and the entry says so in advance rather than half-shipping a route. **THE ABSENCE HOLDS ON RE-DERIVATION**: over every non-test `.ts` file in `apps/` and `packages/`, in all three spellings this tree uses -- raw `INSERT INTO identities`, Drizzle's `insert(identities)`, the accessor's `insert('identities', ...)` -- **ZERO writes**, against 48 files that mention the table, and it is a TYPE rather than an oversight because [`scope.ts:591-594`](../../packages/db/src/scope.ts) registers `identities` as `class: 'root'` and [`scoped-db.ts:365-368`](../../packages/db/src/scoped-db.ts)'s `OwnedTableKey` admits only `owned`. **THE DISPATCH'S CENTRAL PREMISE IS HALF REFUTED AND THE REFUTATION FAVOURS IT: THE TIMING WAS ALREADY RULED IN A FROZEN DOCUMENT.** [`API_CONTRACT:120`](../architecture/API_CONTRACT.md)'s `is_new: boolean` on `VerifyResponse` is meaningless at an endpoint that does not create the row; [`:104`](../architecture/API_CONTRACT.md)'s `OtpResponse` carries no identity; [`:91`](../architecture/API_CONTRACT.md) says `POST /auth/otp` deliberately does not reveal whether the destination exists; [`SECURITY:297`](../architecture/SECURITY.md) states the tripped-breaker residual as *"identities exist that have not proven they hold their number"*, which puts the row BEFORE phone verification; and [`0002:305-307`](../../packages/db/migrations/0002_identity.sql) refuses the earlier moment in a merged migration. **`is_new` has ONE other occurrence in the workspace, `is_new: false` in a fixture: nothing has ever been able to answer it `true`.** **AT FIRST PURCHASE IS NOT LATE, IT IS UNREACHABLE**: both checkout endpoints declare `required: 'session'` ([`checkout.ts:2437`](../../apps/api/src/routes/checkout.ts), [`:2501`](../../apps/api/src/routes/checkout.ts)) and `sessions -> users -> identities`, **which DISCHARGES [ADR-183](../decisions/ADR-183.md)'s own "most likely wrong" paragraph and confirms `0054`'s instant as merged.** **AT OTP REQUEST COSTS FOUR PERMANENT ROWS PER TYPED ADDRESS** -- one `identities` plus three `ledger_accounts` from [`0054:144-146`](../../packages/db/migrations/0054_identity_ledger_accounts.sql)'s trigger -- with every referencing table `ON DELETE RESTRICT` and retention *"forever (financial counterparty record)"* ([`0002:82-85`](../../packages/db/migrations/0002_identity.sql)). **WHAT THE ROW CARRIES IS NOTHING, MEASURED FROM `0002` RATHER THAN CHOSEN**: every column defaulted or nullable, so the statement is `INSERT INTO identities DEFAULT VALUES`, and **the root of every scope rule in this corpus holds no personal data at all**; email on `users`, phone a HASH on `identity_phones`, signals digests, and `display_name`, `max_accounts_override` and `support_contact_ref` NULL deliberately. **The emptiness is ruled a CONTROL rather than an accident of Wave 1**, which binds `M13`, `M14` and `M19`. **FOUND, IN NO DOCUMENT IN THIS CORPUS, ON THE MONEY PATH: THE `sms` CHANNEL CANNOT REGISTER ANYBODY.** [`0002:248`](../../packages/db/migrations/0002_identity.sql)'s `users.email citext NOT NULL UNIQUE` meets a `VerifyRequest` presenting two symmetric channels, so `is_new` is ALWAYS false on `sms` and that channel is login-only. **EXECUTED END TO END, WHICH NO SESSION HAD DONE**: against the 58-file schema on PostgreSQL 16, an identity created the ruled way, then the SHIPPED [`lt01`](../../apps/api/src/routes/payouts.ts) builder and the SHIPPED [`postTransaction`](../../packages/ledger/src/post.ts) over a real connection -- **the trigger opens all three positions, `resolve` finds them instead of throwing, 4 entries post, 40,000c to the wallet, 10,000c recognized at approval, withdrawable drained to 0, global sum 0.** **`0060` REFUSED ON A MEASUREMENT AND NOT A PREFERENCE**: the candidate `DEFERRABLE INITIALLY DEFERRED` constraint trigger was written and executed, it ACCEPTS the ruled shape and REFUSES the OTP-time shape at COMMIT, and a clean A/B over all 15 `probe_*.sql` scripts is **15 of 15 pass without it and 14 with it**, the casualty being [`probe_ledger_constraints.sql`](../../scripts/db/probe_ledger_constraints.sql), the acceptance script for `0054`'s own trigger. It also has no caller, and an `AFTER INSERT` trigger never sees the `UPDATE` a merge uses. **THE BUILD IS FOUR RULINGS DEEP AND THE HONEST COUNT IS 1 OF 4**: the OTP digest (a keyed MAC with nowhere to keep the key, so a deployment secret or a migration), the establishment door in `packages/db`, delivery, then the handler. **THIS ENTRY IS THE ONE [ADR-126](../decisions/ADR-126.md) SECTION 7 ASKED FOR BY NAME**, and it supplies what that section said was missing: a named caller. **TWO FURTHER FINDINGS, BOTH OUTSIDE THE FENCE AND REPORTED**: [`auth-backend.ts:291-297`](../../apps/api/src/auth-backend.ts)'s `NO_PRE_IDENTITY_READ` is **STALE**, telling an operator in a live 503's log line that a pre-identity read has no door, where [ADR-126](../decisions/ADR-126.md) built that door as `resolutionDb`; and **`resolutionDb` has no caller anywhere in `apps/`**. **The dispatch's "sixth time the pool absorbed one" is off by one against its own `0060` row**, which names three precedents as the fourth, fifth and sixth. **33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` clean with its OUTPUT read, and 227 files / 5,429 passed / 6 skipped against a `main` baseline at `087fa570` of 227 / 5,429 / 6 reproduced exactly before a line changed.** |
| [2026-08-28 - Session 366](2026-08-28-session-366.md) | **THE WRITER IS COMPOSED AND THE FIRST EVENT THIS SYSTEM HAS EVER RECORDED WAS WRITTEN, AGAINST A LIVE POSTGRESQL WITH ALL 58 MIGRATIONS APPLIED, AND READ BACK THROUGH ADR-191's `either` PREDICATE.** Two commits, no ADR number, no migration number. **`TRANSACTION_EVENT_WRITER` LIVES IN [`events.ts`](../../apps/api/src/events.ts) AND NOT IN `db.ts`, BECAUSE IT TAKES NO HANDLE**: it receives the transaction the caller opened, which is `admin-source/events.ts`'s disposition three files over, and the suite binds its two retyped strings where `@merit/db` is reachable. `db.test.ts`'s map is untouched and still reads `{'src/db.ts': [firmDb, scopedDb, transaction], 'src/routes/account-reads.ts': [atMost]}`. **THE DOOR IS NARROWER THAN THE READ AND THAT IS THE SLICE'S FIRST FINDING.** ADR-191 clause 5 put `events` in `ScopedTableKey`, which is what `rows` and `rowsWhere` take; `ScopedTx.insert` is generic over `OwnedTableKey` and always was, because a scoped insert STAMPS the tenancy column and an `either` row has two and may carry neither; `FirmTx.insert` takes `FirmTableKey`; `insertUnder` is `Extract<DerivedTableKey, 'sessions'>`, a closed list of one whose run-time guard names this exact class. **So `SystemTx` is the ONE handle in this workspace whose insert reaches this table, and `apps/api` opens no system door on purpose (ADR-165), so THIS DEPLOYABLE HOLDS NO TRANSACTION THAT CAN CARRY AN EVENT.** The writer refuses every other handle BY BRAND rather than by shape, because all three carry `insert(key, values)` and the difference is a type parameter that is gone at run time; a duck-typed guard admits a `ScopedTx` and lands the caller in `refuseTenancyColumn`'s message about scoped writes, which is true and describes a rule they were never allowed to use. **THE SECOND FINDING WAS FOUND BY WRITING A REAL ROW AND NO UNIT TEST COULD HAVE SEEN IT. Of the eight names this producer carries, FOUR write, ONE is `payout.freeze_expiring` refused by `assertTenanted`, and THREE fail at the DRIVER with `TypeError: Do not know how to serialize a BigInt`.** `events.payload` is `jsonb`, drizzle's jsonb mapping is `JSON.stringify`, `JSON.stringify` refuses a bigint outright, and `assertPayloadRules` REFUSES a `_cents` value that is NOT a bigint. **The two rules together make every money payload in the catalogue unwritable, and 33 of the 102 readable catalogue rows carry a `_cents` field.** Every writer double in every suite records what it is handed and none of them serialises, which is why the producer and its fixtures were green over a payload the storage layer cannot take. **`assertSerialisablePayload` refuses it by name and states the RULING it needs, which is EVENTS' to give and not this file's**: a JSON number loses digits above 2^53 and a JSON string is exact but is a format no document states. **It breaks no caller because all three already failed at the driver.** **THE 34 ARE NOT 34 PRODUCERS AND THAT WAS THE DISPATCH'S OPEN QUESTION.** 105 catalogue rows, 3 by reference, 34 of the remaining 102 naming neither tenancy column and exactly one of the 34 carrying `TL` (`payout.settled`), all four re-derived independently and unmoved from session 358. But only EIGHT of the 105 have a producer in this tree, and exactly ONE of those eight declares neither column. **So wiring the sink makes ONE producible name throw, not 34, and it already throws today**, because `UNWIRED_EVENT_SINK.emit` rejects before `buildEvent` ever runs. **NOTHING IN `apps/api/src` CALLS `emit`. ZERO, measured.** The seven call sites are all in `apps/worker/src`, across three ports each independently unwired, in a deployable `RI-04` forbids importing `apps/api`, and `ExpirySweepIo` has no composition either. **A sink wired to nothing is what this is, and the entry says so rather than implying a route now records events.** **THE DISPATCH'S OWN CORRECTED PREMISE IS ALSO STALE AND IS RE-DERIVED**: `grep -rln '@merit/db' apps/api/src` returns **15**, not 1 and not 10; **2** files IMPORT it and **1** takes a handle, which is `db.ts`'s own header's sentence and not ADR-191's. **NINE SEEDS, ALL NINE CAUGHT**, each restored with `git status --porcelain` empty after, including `packages/db` renaming its `SystemTx` brand. **33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, format clean, `falsify.mjs` READ rather than exit-checked with all 33 clean and dirty and 80 scope and 10 loader cases, and 227 files / 5,453 passed / 6 skipped against a `main` baseline at `087fa570` of 227 / 5,429 / 6 reproduced exactly before a line changed.** The delta is **+24** and it is exactly the twenty-four cases this session wrote. |
| [2026-08-28 - Session 367](2026-08-28-session-367.md) | **[ADR-197](../decisions/ADR-197.md) SHIPS `proposed` WITH AN UNSIGNED APPROVAL LINE. MONEY PATH (auth), `E2` OWED. `0061` RETURNS TO THE POOL UNSPENT, THE FOURTH TIME IN THIS REGISTRY.** 2 and 3 of the four [ADR-196](../decisions/ADR-196.md) priced, and unlike that entry it BUILDS. **RULING 1 IS SETTLED BY ONE MERGED `GRANT` RATHER THAN BY JUDGEMENT**: [`0026:75`](../../packages/db/migrations/0026_roles_and_grants.sql) grants `merit_app` SELECT on ALL TABLES, so the role that verifies an OTP must read the key and is the same role that reads `otp_challenges` -- a key table is readable by exactly the principal it is withheld from and **one dump yields the digests and the key together**, which is [ADR-046](../decisions/ADR-046.md)'s ruling one table over. **The cost that argued for the database is bounded to ten minutes**: no `key_id` is needed because [`0002:313`](../../packages/db/migrations/0002_identity.sql) is a *"short TTL, 10 minutes"* and a challenge past it cannot verify under any key, so a rotation admits two keys for one window. **A THIRD CANDIDATE THE DISPATCH DID NOT NAME WAS PRICED AND REFUTED BY MEASUREMENT**: key-free `scrypt` is **49.0 ms per guess**, **82 cores** to exhaust a six-digit space inside one TTL, against HMAC's **5.16 microseconds** on the honest path. **THE SHARPEST FINDING IS A SECURITY ONE PRODUCED BY TWO CORRECT MIGRATIONS**: `otp_challenges` keys off `email_normalized` ([`0002:306-308`](../../packages/db/migrations/0002_identity.sql)) and `users.email_normalized` is *"deliberately NOT unique. Two people can legitimately share a normalized form"* ([`0002:250-253`](../../packages/db/migrations/0002_identity.sql)), so **the challenge row cannot separate two people the normalizer merged and carries no copy of the address the code was mailed to** -- without a binding, `bob.smith@`'s code answers a challenge presented as `bobsmith@`. **The digest is the only place that separation can live and it costs no column.** Executed: `false`. **THE DOOR IS ONE VERB, WHICH IS CLAUSE 2 WITH ENFORCEMENT BEHIND IT AND NO DDL**: `EstablishmentTx` carries `establish` and no `insert`, `updateAt`, `deleteAt`, `rowAt` or `sqlExecutor`, so the statement that writes the identity IS the statement that writes the login. **AND CLAUSE 2 IS THE RACE'S ANSWER RATHER THAN MERELY ITS TIDINESS, MEASURED BOTH WAYS AGAINST POSTGRESQL 16**: the unique violation is ALLOWED TO RAISE, so the rollback takes the identity row and `0054`'s three `ledger_accounts` with it and **a racing loser pays ZERO permanent rows**; `ON CONFLICT (email) DO NOTHING` commits **4 permanent rows and one `identities` row with no login**. **EXECUTED END TO END**: a real OTP through the shipped digest, an identity through the door, the trigger opening all three positions, and `LT-01` posted over the shipped builder and the shipped `postTransaction`, 4 entries, net 0c. **FOUR FINDINGS NOT IN THE DISPATCH.** (1) **`resolutionDb` was never EXPORTED**, which is why [ADR-196](../decisions/ADR-196.md) measured no caller in `apps/`: there could not be one, and the refusal was **true of the package SURFACE while false of the package**. (2) **`NO_DERIVED_INSERT` was stale beside `NO_PRE_IDENTITY_READ` and no entry had said so**, and the suite **PINNED both literal strings, so it was green while holding the drift in place** -- [ADR-185](../decisions/ADR-185.md)'s shape one deployable over. (3) **`RI-15` and `RI-16` do not read the bare `` `:NNN` `` continuation citation**: `ALLOCATION:164` cited `ParentedTableKey` at `:1540` while it stood at `1609`, **69 lines adrift at `63b73fcc` with every gate green**, while the `` `file.ts:NNN` `` form in the same sentence was caught the moment an insertion moved it by four. **Registered, not repaired.** (4) `Buffer.from(_, 'base64')` silently ignores unrecognised characters, so a corrupted secret decodes to a **shorter working key**. **A DEFECT THE MEASUREMENT CAUGHT AND A REVIEW WOULD NOT HAVE**: the first translator read `code` and `constraint` off the top-level error and **Drizzle wraps the driver's error in `cause`**, so it matched nothing -- **every row count stayed correct because the rollback happens regardless, and only the error TYPE was wrong**, which would have answered 500 where clause 4 requires `is_new: false`. **TEN SEEDED DEFECTS, ALL TEN RED**, each naming its own case. **NO HANDLER IS WRITTEN**: `verifyOtp` is now blocked on [ADR-196](../decisions/ADR-196.md) item 4 alone and on no construction. **33 of 33 gates, 17 of 17 invariants, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` clean with its OUTPUT read, 15 of 15 probes, and 230 files / 5,598 passed / 6 skipped ON THE MERGED HEAD (`origin/main` at `8b7a448`).** At the branch's base `63b73fcc`: **16 of 16** and **227 / 5,460 / 6** against a reproduced **227 / 5,429 / 6**; **this branch contributes +31 cases and 0 files** and the rest arrived with `origin/main`. |
| [2026-08-28 - Session 368](2026-08-28-session-368.md) | **THE DISPATCH'S CENTRAL PREMISE IS REFUTED AT ITS SOURCE: `SC-M4-02` AND `SC-M4-03` WERE BOTH BUILT IN PULL REQUEST #434 AND HAVE BEEN SINCE SESSION 259, AND [ADR-147](../decisions/ADR-147.md) IS THAT SESSION'S ENTRY.** The dispatch also states the portal serves **four** routes; `pnpm --filter @merit/portal build` prints **TWELVE**, both sides of this diff, because no route was added. **EVERY REQUIREMENT THE DISPATCH NAMED AS BINDING WAS ALREADY SATISFIED, CHECKED ONE AT A TIME AGAINST THE SHIPPED SOURCE**: floor distance is the first row emitted at [`figures.ts:108`](../../apps/portal/src/app/accounts/figures.ts), `FundedProgress` at [`account-detail.ts:185`](../../apps/portal/src/app/accounts/account-detail.ts) renders every gate with no bar, no ring, no percentage and no aggregate, `INV-M4-05`'s skipped gate has its own rendering at [`:91`](../../apps/portal/src/app/accounts/account-detail.ts) sharing no markup with the satisfied one, section 3.3's meter is [ADR-147](../decisions/ADR-147.md)'s ruled and open subject, and section 3.6's indicative layer is **vacuously satisfied** because these screens render no live number for `INV-M4-11` to keep apart. **REBUILDING EITHER WOULD HAVE BEEN A REGRESSION RISK AGAINST ZERO GAIN, SO NEITHER WAS TOUCHED**; the session landed the two defects the measurement exposed instead, and made both mechanical. **FINDING 1, WHICH IS THE DISPATCH'S OWN POINTED INSTRUCTION ANSWERED: THE BARREL WAS AT 18 OF 21.** [`api/types.ts`](../../apps/portal/src/api/types.ts) exports 21 names and [`index.ts`](../../apps/portal/src/index.ts) re-exported 18, the missing three being `CursorPage` and the two `PAGE_LIMIT_*` bounds. **THE SHAPE OF THE FAILURE IS WHY IT SURVIVED: A NAME ABSENT FROM A BARREL TYPE CHECKS, LINTS AND TESTS GREEN**, because it is simply a name nobody outside the package can write. `CursorPage` was used by THREE modules inside this application and nameable from nowhere outside it, so the only way to type a paginated response was to REDECLARE THE ENVELOPE, which is a second transcription of a contract shape and is the exact defect [`test/api-types.test.ts`](../../apps/portal/test/api-types.test.ts) exists to prevent. **The repair is the gate and not the three names**: that file now derives both lists and asserts set equality, where [`test/surface.test.ts`](../../apps/portal/test/surface.test.ts)'s existing check is twenty transcribed names that drift the same way the barrel does. **FINDING 2: `/payouts` WAS PRERENDERING STATIC, ON THE ONE SCREEN [M04](../plans/M04-trader-portal.md) SECTION 3.6 ROWS AS "authoritative, always" (`INV-M4-13`).** Nine of the ten data-reading routes declare `force-dynamic` and the tenth was the payout centre. **THE MECHANISM IS AN ORDERING INSIDE THE TRANSPORT AND THE BUILD EXITS 0 EITHER WAY**: the only request-scoped API here is `cookies()`, which [`serverApiClient`](../../apps/portal/src/http/client.ts) reaches in its last line, and [`resolveApiOrigin`](../../apps/portal/src/http/client.ts) throws first when `MERIT_API_ORIGIN` is unset, so a build environment without that variable never reaches the cookie and Next bakes the `unavailable` screen into an artifact. **SO THE RENDER MODE OF THE PAYOUT CENTRE WAS DECIDED BY WHETHER AN ENVIRONMENT VARIABLE HAPPENED TO BE SET IN THE BUILD ENVIRONMENT.** [`accounts/page.ts`](../../apps/portal/src/app/accounts/page.ts) had already written the argument the payout segment was missing -- section 1.2's *"no client-side cache of a money number survives a navigation"* and *"`FM-M4-03`'s shape arriving through a build step rather than through a query"* -- **and nothing connected the two.** [`test/route-rendering.test.ts`](../../apps/portal/test/route-rendering.test.ts) makes it mechanical with a **DERIVED** predicate rather than an allowlist: dynamic is required exactly when a page's transitive relative-import closure reaches [`src/http/client.ts`](../../apps/portal/src/http/client.ts), which exempts the root page on a measurement rather than on a name in a list where the next static money screen could be added quietly. **SIX SEEDED DEFECTS FIRED ACROSS THE TWO GATES AND EACH WAS WATCHED**, the valuable one being the **vacuous pass**: a blinded closure walk makes every page look like it reads nothing and assert nothing at all, and the acceptance count of routes reaching the client is what catches it. **RI-15 CAUGHT A DRIFTED CITATION IN THIS SESSION'S OWN NEW COMMENT BEFORE IT LANDED**, which is the protocol's *"derive a line citation LAST"* arriving as a control rather than as advice. **THREE STALE CLAIMS IN THE BARREL'S HEADER CORRECTED AGAINST MEASUREMENTS**: *"SEVEN OF ... ELEVEN SCREENS EXIST ... The four that do not are ... SC-M4-04 the payout centre"* (it is EIGHT and THREE, and the three are `SC-M4-01`, `SC-M4-10`, `SC-M4-11`), *"`app/payouts/` is the one segment wired to it. The other five ... reach nothing"* (ALL SIX reach the client), and `main`'s *"no surface yet"*, defended by its own comment as *"accurate rather than stale ... nothing here listens on a port"* against a build that prints twelve routes. **REPORTED AND NOT REPAIRED**: `GET /accounts/:accountId/marks` is still unregistered so `SC-M4-03`'s `ready` arm is unreachable from a browser; [ADR-147](../decisions/ADR-147.md)'s three `ConsistencyGate` fields are named and unwritten, so an approved plan requires a meter at all times that shows two of five figures; and [`payouts/source.ts`](../../apps/portal/src/app/payouts/source.ts) still renders a 500 identically to a missing endpoint. **THE ONE GENEROUS FENCE READING IS STATED PLAINLY RATHER THAN TAKEN QUIETLY**: `payouts/page.ts` is not a *new* route, the parenthetical is read as the dispatcher's expectation rather than a carve-out, and the ground is that a guard firing on a file the session declines to fix leaves the suite red, so it was fix-and-gate or report-only. **LANDMINE, AND IT IS THE ONE THIS SESSION EXISTS TO RECORD: NO CONTROL IN THIS REPOSITORY COMPARES A DISPATCH'S PREMISES TO THE TREE.** The gates read documents and the suite reads source; a stale route count and a stale screen inventory reached a session unchallenged, and this is the FIFTEENTH premise refuted by the session checking it against the protocol's own running count of fourteen. **33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` clean with its OUTPUT read (33 gates pass clean and fail dirty, 80 scope cases, 10 loader cases) and the tree clean after it, and 228 files / 5,431 passed / 6 skipped against a `main` baseline at `63b73fcc` of 227 / 5,429 / 6 reproduced exactly before a line changed.** |
| [2026-08-28 - Session 369](2026-08-28-session-369.md) | **THE DISPATCH'S CENTRAL PREMISE IS REFUTED IN THE DIRECTION THAT MAKES THE SLICE BUILDABLE.** `exportEvidence` is not "the last `AdminReadSource` method with no module": [`evidence.ts`](../../apps/api/src/admin-source/evidence.ts) has been that module since session 319. What it had no adapter for was the READ, and this lands it: `EvidenceReadPort` over ADR-112's keyed accessor, in `flags.ts`'s, `graph.ts`'s, `events.ts`'s and `account.ts`'s shape unaltered -- a closed tuple of ELEVEN tables, an `EvidenceTx` that `SystemTx` satisfies structurally, a cost object beside the answer, and no `@merit/db` import under the directory. **EVERY TABLE IT NEEDS WAS ALREADY A `TableKey`, WHICH IS THE OPPOSITE OF WHAT SESSIONS 349 AND 353 FOUND FOR `events`**, checked by importing [`scope.ts`](../../packages/db/src/scope.ts) rather than by grepping. **SO `exportEvidence` STILL DOES NOT JOIN `IMPLEMENTED_ADMIN_READS` AND THE REASON IS NOT A READ**: `EvidencePackStore` is object storage and **ONE file in the whole deployable mentions that port, the file that declares it**, so no table registration reaches it and a `download_url` invented here is a pack an operator cites and nobody can open; `EvidencePackWriter` is an INSERT whose home **ALREADY EXISTS one directory over**, since [`admin-writes.ts:220`](../../apps/api/src/routes/admin-writes.ts) names `evidencePacks` in `ADMIN_WRITE_TABLES` and `AdminWriteTx` carries `insert`, while this directory's stated property is a handle it CANNOT WRITE THROUGH. **`admin-source/index.ts` IS THEREFORE NOT TOUCHED AT ALL**, which also makes the merge with session 363 empty rather than mechanical. **THE DISPATCH'S SECOND PREMISE IS ALSO REFUTED AND THIS ONE MATTERS TO A LATER SESSION**: it said the missing list would be `searchAccounts` alone after 363 and this session, and re-derived at reporting time it is **THREE** -- `exportEvidence`, `readLiability`, `searchAccounts` -- because what landed on `main` as PR #485 was session 364's liability RENDERERS in `apps/admin`, not `readLiability`'s adapter. `setAdminReadSource` is not wireable and the triple `{ declared: 23, wired: 6, blocked: 17 }` does not move. **THREE CORPUS FINDINGS, EACH READ AT SOURCE.** `GS-112` requires "the plan's rule text" and `plan_versions` HAS NO `rule_text` COLUMN: [`0028:24`](../../packages/db/migrations/0028_supersede_plan_version_immutability.sql) names `copy_blocks` as "the published rule TEXT" and `INV-M4-08` says the same from the reading end, so the map carries `copy_blocks` and invents nothing, and the existing suite's fixture key is a fixture's shape. `risk_flags` carries NO `detector` column, so `detector` and `detector_version` come off `detector_runs` through `detector_run_id`. And `risk_flags` carries no ToS clause, so two keyed reads find it on the enforcements that cite the flag and **two enforcements citing one flag under DIFFERENT clauses is a REFUSAL** rather than a choice between rules the firm did and did not cite. **THREE VALUES THE ACCESSOR RETURNS THAT `canonicalJson` CANNOT RENDER, AND ONLY ONE FAILS LOUDLY.** A `bigint` throws. A `Date` reaches `asRecord` through the same branch every record takes, has no own keys, and renders as `{}` **under a digest that makes it official**. A `Uint8Array` renders as an object keyed by byte index. Each is demonstrated FAILING before it is demonstrated fixed. **THE SUITE FOUND A REAL DEFECT IN THIS SESSION'S OWN GUARD**: `jsonSafe` was first written on `asRecord`, which accepts any non-array object and is the exact blind spot it exists to close, so it let a `Date` through; `plainRecord` checks the prototype and the case that caught it is kept. **THE COLUMN MAPS ARE THE TRADER PACK'S ALLOWLIST FOR SIX OF ITS EIGHT SECTIONS**, because `buildEvidenceDocument` carries those through WHOLE at every audience, so they name every column rather than spreading a row and the suite reads [`schema.ts`](../../packages/db/src/schema.ts) for both the property name and the column name and asserts each map IN BOTH DIRECTIONS: a new column is a red suite, not a disclosure. **NINE SEEDED DEFECTS, ALL NINE CAUGHT AND THE THREE `index.ts` DEFENCES WATCHED FAILING DIFFERENTLY**: `TS2741` for a name with no key, `TS2353` for a key with no name, `TS2741` for the dropped arm, and the run-time refusal red **over a typecheck reporting 0 errors**. A twelfth table name seeded into `EVIDENCE_READ_TABLES` reproduced `TS2322` against `SystemTx`, which is the refusal two sessions were stopped by. **No ADR number, no migration number, no `SystemReason` member, no `SqlExecutorReason` member, no `pg` import, no cast past a key type, `packages/**`, `apps/api/src/routes/**`, `apps/portal/**`, `apps/admin/**` and [API_CONTRACT](../architecture/API_CONTRACT.md) all untouched, and no comment naming `admin-source` added to any route module.** 33 of 33 gates, 16 of 16 invariants, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` clean with its OUTPUT read, and **227 files / 5,485 passed / 6 skipped** against a `63b73fcc` baseline of **227 / 5,429 / 6** reproduced exactly before a line changed, and, after two merges with `main` (the second on the PR's own `mergeable_state: dirty`, conflicting only in two append-only documents), **229 / 5,565 / 6** on the branch against `main` at `f35720d`'s **229 / 5,509 / 6** measured in a worktree -- the same **+56 cases and no new test file** against both bases, with invariants now **17 of 17** because `RI-18` arrived with that merge and not with this diff. **SESSION 363 LANDED AS PR #486 AND `IMPLEMENTED_ADMIN_READS` STILL DID NOT MOVE**, because its own title is `readLiability` measured as unwritable: it composed nothing, so the missing list is still three. |
| [2026-08-28 - Session 370](2026-08-28-session-370.md) | **[ADR-198](../decisions/ADR-198.md) RULES A `_cents` VALUE INSIDE AN `events.payload` A DECIMAL STRING, `/^(0\|-?[1-9][0-9]*)$/`, AND [EVENTS](../architecture/EVENTS.md) SECTION 13 STATES IT. THE THREE NAMES THAT FAILED AT THE DRIVER NOW WRITE, AGAINST A LIVE POSTGRESQL WITH ALL 58 MIGRATIONS APPLIED.** Three commits, `ADR-198` spent, **no migration number reserved and none taken**. **THE MAGNITUDE ARGUMENT IS REPORTED AS THEORETICAL RATHER THAN LEANED ON**: `Number.MAX_SAFE_INTEGER` is 9,007,199,254,740,991 cents, the largest payout cap is `450,000c` over a ladder of 5, and the largest firm-level payload figure would need about four billion funded accounts to reach 2^53. **WHAT DECIDES IT IS THE TYPE A CONSUMER HOLDS FOREVER**: `JSON.parse` yields an IEEE-754 double, which is the one type `assertPayloadRules` refuses on the way IN, so a JSON number would make that check undecidable at every reader of an append-only table, and producing one requires the `Number(value)` coercion that check declines from a caller. **A FOURTH OPTION EXISTED THAT NEITHER THE DISPATCH NOR THE ALLOCATION ROW NAMED, AND IT IS ALREADY IMPLEMENTED IN THIS WORKSPACE**: `asJsonNumber` at [`graph.ts:394`](../../apps/worker/src/detectors/graph.ts) encodes a `bigint` into `risk_flags.evidence`, a `jsonb` column, as a number when exactly representable and a decimal string when not. **It is REFUSED because the type would vary with the value**, so the string branch is code no fixture, suite or deployment ever runs and the first row that runs it is the largest figure the firm has ever computed; and it cannot be versioned, because EVENTS makes shape a function of `schema_version` and a magnitude-conditional shape is a function of the VALUE. **It is not overturned and not touched**, `apps/worker` being another fence, **so the two `jsonb` columns now disagree and that is registered rather than repaired**. **THE DISPATCH'S THIRD PREMISE IS FALSE AND IT CHANGED WHICH OPTION WON**: a decimal string is NOT a format no document states, because [ADR-158](../decisions/ADR-158.md) clause 3 states it for `entry_id`, `API_CONTRACT:681` states the reason in the same words, and `graph.ts` states it for a `bigint` reaching a `jsonb` column. **THE ACCEPTANCE EVIDENCE IS A ROW IN A DATABASE**: seven of the eight names write where four did, `payout.freeze_expiring` is still refused by `assertTenanted` unchanged, and a `withdrawable_cents` of **9007199254740993** reads back with its digits intact and casts exactly under `(payload->>'field')::bigint`, where the same value through `::double precision` is **9007199254740992**, one cent short and silently. **THE ENCODING IS THE WRITER'S AND `assertPayloadRules` IS UNTOUCHED**, so the domain form stays `bigint` up to the column; `assertSerialisablePayload` survives narrowed to clause 6, refusing a `bigint` under a key that does not end `_cents`. **A COUNT INSIDE THE FENCE WAS OFF BY ONE**: the rows naming neither tenancy column are **35** and not 34, the claim resting on it is unmoved, and both are cases now. **EVENTS WAS AMENDED WITHOUT MOVING A LINE** because **104 `EVENTS:N` citations in 24 files at 37 distinct line numbers** are all outside this fence. **THE SINK IS STILL WIRED TO NOTHING and this entry says so.** **TEN SEEDS, ALL TEN CAUGHT.** 33 of 33 gates, **17 of 17** invariants, `falsify.mjs` READ rather than exit-checked with all 33 clean and dirty, 80 scope and 10 loader cases, typecheck 0, lint 0, format clean, and **229 files / 5,529 passed / 6 skipped** against a `main` baseline at `b10b3bc2` of **229 / 5,509 / 6 reproduced exactly before a line changed**. The delta is **+20**. |
| [2026-08-28 - Session 371](2026-08-28-session-371.md) | **`searchAccounts` IS COMPOSED AND `GET /admin/accounts` ANSWERS `200` WHERE IT ANSWERED `500`, BOTH MEASURED THROUGH THE REAL ROUTER RATHER THAN REASONED ABOUT.** Before: `report.registered` over a real `compose()` holds **28** operator paths including `GET /admin/accounts`, `IMPLEMENTED_ADMIN_READS` names **four**, and the route answers **500 `internal_error`** because `AdminSourceNotComposed('searchAccounts')` throws synchronously. After: **28** paths unchanged, `IMPLEMENTED_ADMIN_READS` names **five**, and the route answers **200** with `{data, next_cursor}`. **The wiring triple is `{ declared: 23, wired: 6, blocked: 17 }` and DID NOT MOVE, re-run rather than predicted: a method is not a port.** **[`search.ts`](../../apps/api/src/admin-source/search.ts) IS SIX EQUALITIES FANNING IN TO KEYS AND FOUR READS FANNING OUT OVER A PAGE, AND THERE IS STILL NO JOIN**, which is `ADR-157` section 5's own answer and is what [`ADR-194`](../decisions/ADR-194.md) section 9 meant by turning the adapter from blocked into merely unwritten. Eleven registered tables, no `pg` import, no `@merit/db` import, no `sqlExecutor` on the handle to reach, no `SystemReason` or `SqlExecutorReason` member, no cast past a key type. **THE ORDER IS `(identity_id, account_id)` ASCENDING AND BOTH KEYS ARE IMMUTABLE**, which is the property `readFlagQueue`'s cursor cannot have: a page boundary cannot move under a concurrent write, so this ordering can neither repeat a row nor drop one. **`ADR-194` SECTION 9's AFFORDABILITY CLAIM IS REFUTED FOR ONE TERM OF THE SIX AND THE REFUTATION IS THIS SLICE'S LARGEST FINDING.** It reads *"under an exact subject that price is bounded by the page limit `INV-M6-10` already caps"*; **the page limit caps what is RETURNED and caps nothing that is READ**, and the coupon is the term where the two diverge without bound, because that entry's own clause 3 defends a coupon precisely BECAUSE many accounts share one and the accessor has no `IN`. **The repair is in the ordering rather than in a cap**: the expansion walks identities in the page's own order and stops once more than `limit` eligible rows sit at or before the identity just walked, so nothing unread can reach the window. **Forty identities on one coupon expand SIX on page one and SEVEN on page two**, and the seventh is the identity the cursor sits on. **NOTHING IS TRUNCATED AND THE SUITE PROVES IT BY PAGING THE WHOLE SET AND COMPARING IT TO THE UNSTOPPED ANSWER.** **EXECUTED END TO END AGAINST A LIVE POSTGRESQL 16 WITH ALL 58 MIGRATIONS AND A SEEDED ESTATE**, through `transaction(systemDb('operator-console'))`, and every term was watched resolving: an account id, an identity id, a LIVE platform ref, a RETIRED one off `platform_account_refs` where `accounts.platform_account_ref` is NULL, an email in the WRONG CASE against a `citext` column, the `email_normalized` key reaching a login stored as `A.lice+news@Example.com`, a coupon code in lower case, a payout id that matches nothing, and the fragment `jo`, which returns an EMPTY PAGE rather than the `22P02` it returns with the type guard removed. **`payouts_frozen` CAME BACK `true` ON A LIVE ROW WHOSE ACCOUNT COLUMN IS `false`**, which is the money path's own definition rather than this module's choice: `ExternalGates.payoutsFrozen` is *"account level OR identity level, RESOLVED BY THE CALLER"* ([`types.ts:901`](../../packages/rules-engine/src/types.ts)) transcribed from `M01` section 2.1, and `payout/evaluate.ts` refuses to guess the level because *"inventing 'account' here would be a wrong word on a support screen"*. **AN ACCOUNT THE ENGINE HAS NOT EVALUATED HAS NO BALANCE IN THIS ESTATE, AND THAT IS PRICED RATHER THAN PAPERED**: nothing in this tree writes a `rule_states` row yet, `API_CONTRACT` types both money members `number` with no null, a throw is `FM-17`'s shape arriving on a console and `0015`'s own header is where that failure mode is written down, and `accounts.size_cents` as a stand-in would be a rules-engine claim this module may not make. **Both are zero, with a stated clearing condition and its own non-vacuity read.** **A SECOND FINDING NOBODY WENT LOOKING FOR: `coupons.code` IS `UNIQUE` IN THE DATABASE AND NOT IN THE TRANSCRIPTION.** [`0006_commerce.sql:38`](../../packages/db/migrations/0006_commerce.sql) declares `code citext NOT NULL UNIQUE`; [`schema.ts`](../../packages/db/src/schema.ts) carries the column without `.unique()`, `refuseUnaddressed` reads uniqueness out of `schema.ts` through Drizzle's own table config, so **an addressed read the database would honour throws at run time** and the coupon term uses `rowsWhere`. `packages/db` is session 372's fence, so it is registered with a clearing-condition case rather than repaired. **THE THREE DEFENCES WERE SEEDED SEPARATELY AND FAIL AT THREE SITES**: `TS2353` excess property on the `Pick` at `:256`, `TS2741` missing property on the `Pick` at `:249`, `TS2741` missing property on `AdminReadSource` at `:318`, each restored from a byte-identical copy with the hash checked both ways and `git status --porcelain` empty after every one. **SIX MORE SEEDS AND ALL SIX FIRED**: the uuid guard removed (live `22P02`), the early stop one row too soon (4 red), `payouts_frozen` account-only (1 red), the latest rule state read as the first (5 red), `open_flags` counting `investigating` (1 red), and `.unique()` added to `coupons.code` (the census clearing condition). **`admin-source-flags.test.ts`'s LEAVE-THE-LIST CASE WENT RED AS DESIGNED AND THE PARAGRAPH IT CARRIED WAS OVERTURNED BY A RULING RATHER THAN BY A MODULE**: it said the seventh term is a substring predicate so an adapter serving six *"would narrow a contract behaviour behind a green suite"*, and `ADR-194` removed the seventh form FROM THE CONTRACT, so six of six is the whole row. **THE `IMPLEMENTED_ADMIN_READS` HEADER NO LONGER CARRIES A NUMERAL**, because two concurrent slices bumping the same word is a merge conflict over a count the array beside it already states as data. **33 of 33 gates, 17 of 17 invariants** (the dispatch said sixteen and `RI-18` has landed since), typecheck 0, lint 0, `format:check` clean, `falsify.mjs` clean read as OUTPUT with all 33 clean and dirty plus 80 scope and 10 loader cases, and **231 files / 5,643 passed / 6 skipped RE-DERIVED ON THE MERGED HEAD**, against **230 / 5,554 / 6** at this branch's base `b10b3bc` over a reproduced baseline of **229 / 5,509 / 6**: the delta is **+1 file and +45 cases at both bases**, which is exactly the suite this session wrote. **`origin/main` moved to `1ad5f90` while the branch was open**, carrying sessions 367, 368 and 369, and every figure above is measured after that merge rather than carried across it. **No ADR number, no migration number, no `SD-nn`, no `RI-nn`, no `CI-06` letter, no `SystemReason` member, no `SqlExecutorReason` member, no `pg` import, no ledger account named, `API_CONTRACT.md` byte for byte unchanged, `packages/**` untouched, and `apps/api/src/routes/admin-reads.ts` NOT OPENED, so `RI-15` and `RI-16` had no line to follow.** |
| [2026-08-28 - Session 372](2026-08-28-session-372.md) | **THE REGISTRATION LANDS AND `0062` RETURNS TO THE POOL UNSPENT, AND THE ORDINAL ITS RESERVATION CARRIED IS CORRECTED RATHER THAN REPEATED**: that row and `0061`'s each predicted *"the **eighth** time"*, two rows claiming ONE ordinal, and `0061` returned first when [ADR-197](../decisions/ADR-197.md) landed on `main` while this branch was open. Counted at reporting time over the migration table, **five** rows read as returned unspent -- `0051`, `0058`, `0060`, `0061` and this one -- and the larger figure the corpus carries is not derivable from that register at all. [ADR-199](../decisions/ADR-199.md), `status: proposed`, approval line UNSIGNED. **`reserve_coverage_snapshots` IS `firm` AND THE CLASS WAS SETTLED BY THE DDL RATHER THAN BY THE SHAPE OF THE NAME**: the row declares NO column against `identities(id)` and none against `accounts(id)`, so `root`, `owned`, `pair` and `either` have nothing to name, and its ONE foreign key is a COMPOSITE edge to `treasury_balances(account_code, as_of)` -- which `DerivedRule` cannot express, which MULTIPLIES ROWS if narrowed to one column because `account_code` is half the anchor's key, and which terminates at a `firm` table besides. **All three refusals were seeded and watched**, at six sites and four sites respectively. **THE DISPATCH'S PREMISE THAT THIS TOUCHES `scope.ts` ALONE DID NOT SURVIVE AND IT IS A PROPERTY OF THE TWO TABLES RATHER THAN THE TWO ENTRIES**: [ADR-191](../decisions/ADR-191.md) needed one file because `events` was ALREADY transcribed in [`schema.ts`](../../packages/db/src/schema.ts), and this table was not, so `TABLES` had nothing to import. `index.ts` and `scoped-db.ts` were not opened. **THE CENSUS WAS RE-DERIVED AND CONFIRMED AND ITS INFERENCE WAS REFUSED, WHICH IS THE SESSION'S RULING**: **58** migrations declare **735** distinct column names and none of `cusum`, `last_success_at`, `last_duration_ms` or the two `eligible_next_7d_identity_max` names is among them -- and **all three figures are DERIVABLE**, so the absence is correct rather than owed. *No column of that name exists* and *the figure has no producible source* are two claims, and they come apart here three times. **`per_plan[].cusum` WAS ALREADY RULED, A SESSION BEFORE THIS ONE**: [ADR-167](../decisions/ADR-167.md) clause 1 folds `S_t` at read time and *"no column, table or row anywhere holds it"*, so a migration for it would overturn a ruling by writing DDL; its remaining blocker is `DEP-M6-05`'s CALIBRATION and a calibration is not a column. **`integrations.batch` IS AN EVENT AND THE TABLE HAS BEEN REGISTERED SINCE ADR-191, WHICH IS THE DERIVATION NOBODY HAD LOOKED FOR**: [EVENTS section 5](../architecture/EVENTS.md) declares `batch.completed` with `duration_ms` IN ITS PAYLOAD, and [RB-01](../ops/runbooks/RB-01-nightly-batch-failure.md) already reads the batch's success off that event's absence. **The tempting alternative is named and REFUSED**: `max(rule_states.computed_at)` reports a SUCCESS for a run that crashed, because [OVERVIEW section 5.2](../architecture/OVERVIEW.md) makes the batch resumable at the account boundary with prior accounts committed. **`eligible_next_7d` FOLDS `rule_states`, `accounts`, `plan_versions.rules` AND `trading_calendar`, EVERY INPUT LANDED**, and [EC-074](../edge-cases/EC-074.md)'s `bounded_near_term_cents` is a STORED column over the same seven-trading-day horizon, so the schema already commits to the predicate. **`SD-M6-01`'s TWO ABSENT COLUMNS ARE A DIFFERENT FIGURE** -- `P-M6-03`'s identity maximum, on no field of this response -- **and conflating them is what made this group look blocked**. **`withdrawals_in_flight_cents` WAS NOT TAKEN AND THE STANDING QUESTION IS ANSWERED: NO**, one migration should not carry both, because three of the four columns that made bundling worth anything are dissolved and one column alone is not improved by waiting. **NO CONSTRAINT WAS PROPOSED, SO THE A/B HAS ONE ARM BY CONSTRUCTION**: **15 of 15** `probe_*.sql` pass against a schema installed from `0001` forward under `ON_ERROR_STOP`, and the counterfactual is that schema byte for byte, this diff containing no SQL. `probe_reserve_coverage.sql` already exists and is already run, so `CI-06s` counts nothing new. **THE FENCE WAS WIDENED TWICE AND BOTH ARE NAMED RATHER THAN PERFORMED SILENTLY.** [`admin-source-liability.test.ts`](../../apps/api/test/admin-source-liability.test.ts)'s blocker-1 case WENT RED, which is exactly the news session 363 wrote its clearing condition to deliver; the assertion is inverted rather than deleted and the three sibling cases, still green, had their COMMENTS become false and are repointed with a positive assertion each. And **EIGHT line citations into `scope.ts` and `schema.ts` drifted** under this diff's **35** and **72** added lines, across four files in `apps/`; `RI-15` caught four and the other four were found by reading every citation into both files. **TWO MORE FINDINGS THE DISPATCH DID NOT CARRY**: the protocol's verification table says **16 of 16** invariants and the tree reports **17 of 17** since `RI-18`; and [DELTA_MANIFEST:133](../../packages/db/DELTA_MANIFEST.md) dispositions `SD-M6-01` as **landed** against `0009` while the identity-max half never landed, which is [ADR-167](../decisions/ADR-167.md) finding 4's shape on the neighbouring row of the same register, reported and not repaired because the fence held that file only if `0062` landed. **AND THE ALLOCATION ROW'S OWN HEADER SAID `MIGRATION 0060 IS CONDITIONAL`**, which is session 365's returned number. Counts derived at reporting time: **108** declared tables of **114** created, **108** rules, **42** `firm`, **3** `pair`, **63** served by `scopedDb`; the catalog on a fresh install reports **114** tables, **397** indexes, **484** checks, **167** foreign keys and **24** triggers. **SIX SEEDS, SIX REDS**, tree clean after each. 33 of 33 gates, **17 of 17 invariants**, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` clean READ AS OUTPUT with all 33 gates clean and dirty plus 80 scope and 10 loader cases, **230 files / 5,606 passed / 6 skipped ON THE MERGE WITH `main` AT `1ad5f90`**, against that same base measured at **230 / 5,598 / 6**, and **229 / 5,517 / 6** against **229 / 5,509 / 6** at `b10b3bc` reproduced before a line changed -- the delta is **+8** on both bases, the six cases written plus the two the per-table drift loops generate for a newly registered table. **No migration written, no migration edited, `0049` byte for byte unchanged, no `SystemReason` member, no `SqlExecutorReason` member, no `pg` import, no cast past a key type, no ledger account named, and `API_CONTRACT.md` untouched.** |
| [2026-08-28 - Session 373](2026-08-28-session-373.md) | **THE DISPATCH'S CENTRAL PREMISE HELD, RE-MEASURED THE WAY IT WAS MADE, AND BOTH SCREENS WERE BUILT.** `pnpm --filter @merit/portal build` printed **twelve** routes covering `SC-M4-02` through `SC-M4-09` before a line changed and prints **fourteen** after, with `/wallet` and `/security` both DYNAMIC. **ALL FOUR ENDPOINTS CONFIRMED THROUGH `CompositionReport.registered` OVER A REAL `compose()`, AND THEN A SECOND TIME FOR WIRING, WHICH IS A DIFFERENT QUESTION AND IS THE ONE THAT MATTERED**: `GET /wallet`, `GET /wallet/entries` and `GET /sessions` are registered AND wired, `POST /sessions/:id/revoke` is registered AND wired, and **`GET /me` and `GET /phone/change` are registered and BLOCKED** -- `readMe` raises `AuthBackendUnwired` because *"`Me.max_accounts` has no source ... the BASE cap appears in no table in any of the 47 migrations, so it is a plan parameter"*. **`GET /wallet` IS THE FIRST SCREEN IN THIS APPLICATION WHOSE EVERY READ ENDPOINT IS BOTH REGISTERED AND WIRED.** **FINDING 1, AND IT IS THE ONE WORTH THE SESSION: `SC-M4-10`'s REQUIRED COPY HAS NO ROW IN THIS SCHEMA TO BE PUBLISHED ON.** [M04 section 3.5](../plans/M04-trader-portal.md) requires the withdraw exit to state *"KYC, the 48 hour destination-cooling window, a $100 minimum, and 2 to 3 business days"* plus *"No withdrawal fee"*, then rules the wording *"a `copy_blocks` entry (INV-M4-08) and a counsel-review item"* -- **every one of those is a parameter the portal may not state**, and **`copy_blocks` IS A COLUMN ON `plan_versions` WHILE A WALLET IS PER-IDENTITY AND HAS NO PINNED PLAN VERSION AT ALL**, so there is nowhere to publish them and `GET /wallet` carries no copy field. The screen renders the ABSENCE by name (`data-copy="wallet.balance_framing"` and two more) rather than authoring the sentence or leaving a blank, **enforced by a brand rather than by a reviewer**: `CopyBlock`'s only constructor is `copyBlock()` over a published plan version, so **a literal cannot be assigned to that field and the file would not compile**. Reported and not repaired; it is a schema and contract question. **FINDING 2: `POST /sessions/:id/revoke` IS REGISTERED AND WIRED AND THE PORTAL HAS NO VERB TO CALL IT WITH.** `ApiClient` declares `get` and nothing else, `surface.test.ts` fails on a second `fetch(`, and ADR-083 section 3 with ADR-095 ruling 3 forbid a route handler or a Server Action here, so **this application has no write path of any kind**. **THE WIDENING WAS REFUSED RATHER THAN TAKEN**: a first write verb is a transport ruling with a CSRF posture, an unsafe-method cookie policy and an idempotency question attached, **on a path that revokes credentials**. `revokes_at` is the literal `null`, the control renders disabled and the screen says so, because the promise an enabled-but-dead control would make **is the worst one in the product: a trader who believes they have just thrown an attacker out and has not**. **FINDING 3: `AS-M4-05` COUNTER 2 PROMISES A CREATION IP THAT `API_CONTRACT`'s `SessionRow` DOES NOT CARRY**, although `SD-M4-03` added `created_ip inet` to `sessions` for it; two approved documents disagree and M04 section 4 binds the portal to the contract, so the field is absent from the transcription and the divergence is reported rather than closed by inventing a column onto a response. **AND A SECOND OF COUNTER 2's THREE FIELDS CARRIES NO INFORMATION**: `listSessions` falls back to `created_at` because *"NOTHING STAMPS `last_seen_at` IN THIS TREE"*, ADR-120's owed write-amplification ruling, so last-seen renders as a distinct labelled field and is always the creation time. **`INV-M20-08` IS HONOURED BY AN ABSENCE AND THE ABSENCE IS ASSERTED**: the float is the FIRM's aggregate and belongs on M06's dashboard with the RCR computed from reserve alone, so **no aggregate, total, float or reserve exists anywhere in the wallet view model** and the suite fails on each word. The balance's two components are **read and never summed**, on API_CONTRACT's own reason for stating the identity server-side. **THERE IS NO PASSWORD ROW AND NO RESET LINK** (`0002:280`, ADR-039), and because **a security screen is exactly where one gets added without thought** the absence is asserted rather than trusted; **no factor is ranked or called a fallback** (`M04:263`), and **elevation carries no countdown** (section 3.7, *"a prompt to hurry"*). **TWO DEFECTS THE SUITES FOUND WERE FIXED IN THE SOURCE RATHER THAN BY TRIMMING THE ASSERTION** -- `entry_id` reached the view model and never the markup, so *"a client must not parse it"* was unobservable end to end; and the inert-control check searched for `action=` and **fired on this screen's own `data-ledger-transaction` attribute**, so it was made precise rather than deleted. **AND `eslint` CAUGHT A THIRD IN THIS SESSION'S OWN TEST**: `no-loss-of-precision` failed on the literal `9007199254740993`, correctly and in a way that made the test's point, so the assertion now DEMONSTRATES the lost digit and adds the case that matters more, that a SMALL numeric id is refused too because the guard checks the type rather than the magnitude. **FOURTEEN SEEDED DEFECTS, FOURTEEN REDS**, tree clean after each; the valuable ones are **the session guard blinded in BOTH directions**, each caught by the half the other could not see, and **`is_current` coerced rather than checked** -- `undefined` is falsy, so a response that dropped it would offer a trader a revoke control for the very session they were reading it on. **ONE GATE NUMBER MOVED AND MOVING IT IS NOT WEAKENING IT**: `route-rendering.test.ts`'s acceptance count goes 10 to 12 because the walk now resolves twelve routes reaching the client; **the control is the loop above it and it passed for both new pages before that line was touched**. Session 372's finding that the protocol's verification table says **16 of 16** invariants while the tree reports **17 of 17** is CONFIRMED at reporting time. 33 of 33 gates, **17 of 17 invariants**, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` clean READ AS OUTPUT (33 gates clean and dirty, 80 scope cases, 10 loader cases) with the tree clean after it, and **235 files / 5,726 passed / 6 skipped**, against a `main` baseline at `70c3e5b` measured at **231 / 5,671 / 6** in a worktree. **The delta is +4 files and +55 tests, which is exactly the four suites this session wrote** (18, 12, 15 and 10), so nothing else in the repository moved. **`main` then moved to `54e2faa` and the branch was merged with it, so both figures were re-measured on the head that will actually land**: **236 / 5,770 / 6** against that base at **232 / 5,715 / 6**. **The delta is +4 and +55 on BOTH bases**, which is the check worth having rather than the second measurement itself -- a diff whose delta is invariant across two bases has not absorbed anything from either. **No ADR, no migration number, no `SD-nn`, no `SystemReason` or `SqlExecutorReason` member, no `pg` import, no cast past a key type, no ledger account named, no gate weakened, and M04, API_CONTRACT and every file under `apps/api/`, `apps/admin/` and `packages/` byte for byte unchanged.** |
| [2026-08-28 - Session 374](2026-08-28-session-374.md) | **THE DISPATCH'S CENTRAL PREMISE IS REFUTED AND IT IS REFUTED BY [ADR-199](../decisions/ADR-199.md)'s OWN TEXT.** The dispatch reads that ADR-199 "cleared what blocked" `readLiability` and that the twelve cases of [`admin-source-liability.test.ts`](../../apps/api/test/admin-source-liability.test.ts) would now go red and name the lifted blockers. **RUN FIRST, BEFORE A LINE CHANGED: 12 of 12 GREEN.** The one case written to expire had already expired -- session 372 inverted it in its own diff -- so the starting measurement carried no news at all, and the news had to be derived rather than read off a red. *No column of that name exists* and *the figure has no producible source* are two claims ADR-199 correctly separates; ***the figure is derivable*** and ***this tree can read it today*** are a THIRD pair, and they come apart on two of the same three figures. **`0062` STAYS RETURNED: not one of the four blockers below is a column and none takes a migration number, which is the one part of ADR-199 this session confirms rather than narrows.** **THE ADAPTER IS WRITTEN, RUNS, AND IS NOT COMPOSED.** [`apps/api/src/admin-source/liability.ts`](../../apps/api/src/admin-source/liability.ts) produces **27 of `LiabilityResponse`'s 40 leaf paths** from live rows over eight `TableKey`s, with no join, no aggregate, no `sqlExecutor`, no `pg` import and no vocabulary member added -- so [`wiring.test.ts`](../../apps/api/test/wiring.test.ts)'s *"a live adapter today would have to reach `sqlExecutor`"* is measured FALSE for a FOURTH method, and that entry is still not this fence's. **`IMPLEMENTED_ADMIN_READS` STAYS AT FIVE, DERIVED AND NOT TRUSTED, and `composeAdminReadSource` still answers `AdminSourceNotComposed('readLiability')`**: a method that cannot fill 13 of its own paths would turn a named synchronous "this deployment is not finished" into a 500 on `GET /admin/liability` and `GET /admin/eligible-forecast`, both of which `CompositionReport.registered` holds today over a real `compose()`. **THE FOUR BLOCKERS, EACH AT ITS PRIMARY SOURCE AND EACH WITH A CLEARING CONDITION.** **B1, `eligible_next_7d` (5 leaves): `trading_calendar` IS NOT A `TableKey`**, which ADR-199 section 6 named and left to "the session that NEEDS it"; this is that session and the registration is `packages/db` plus an ADR-092-shaped entry, neither of which this fence or this dispatch holds. **AND THE TREE CARRIES A STALE REFUSAL BESIDE IT**: [`schema.ts`](../../packages/db/src/schema.ts)'s `trading_calendar_loads` header still reads that the neighbour *"cannot be registered"* because `0032` carries an `ALTER COLUMN ... DROP NOT NULL` and [ADR-094](../decisions/ADR-094.md) clause 3 closed the drift fold at `ADD COLUMN`; **[ADR-103](../decisions/ADR-103.md) clause 2 superseded that and names this very table as one of the two the widening makes REGISTRABLE**, so the blocker is a session's work and not a ruling's. Reported, not repaired: another fence. **B2, `payout_velocity` (4 leaves): THE THRESHOLD IS FIXED IN THREE DOCUMENTS AND THE WINDOW IN NONE.** [MERIT_BUILD_MASTER_PROMPT:166](../../MERIT_BUILD_MASTER_PROMPT.md), [M06:95](../plans/M06-admin-ops-console.md) and [INFRA:160](../architecture/INFRA.md) all fix 2.5x, and no document says whether `avg_30d_cents` is the 30-day total, its daily mean, or that mean scaled to seven days. **The readings are not a rounding difference**: against a daily mean a seven-day total sits near 7.0 in steady state and the pager fires every day forever; scaled to seven days it sits near 1.0 and the threshold means what the constitution says. `FM-M6-07`'s words for the CUSUM, *"either constant alarms or none, which is the same as no chart"*, on a control that PAGES. The numerator is producible and the group is not. **B3, `per_plan[].cusum` (3 leaves): TWO RULINGS EACH RIGHT ALONE WHOSE CONJUNCTION IS EMPTY**, which is session 366's shape on a different pair. [ADR-167](../decisions/ADR-167.md) clause 5 renders the field ABSENT until `DEP-M6-05` and says the wire shape for that absence is *"`P7-b`'s to carry, not this entry's to invent"*; [ADR-188](../decisions/ADR-188.md) landed `cusum` as a REQUIRED object of two numbers and a boolean in all three copies. So the only two answers are a statistic clause 5 refuses or a shape `RI-18` binds in three copies, and **the shape is REPORTED rather than changed** as the dispatch required. **B4, `integrations.recon.last_run_at` (1 leaf): NOTHING RECORDS A RECONCILIATION RUN.** [EVENTS section 5](../architecture/EVENTS.md) carries `recon.mismatch_detected` and `recon.resolved`, both per account, and no `recon.completed`; the available fold, `max(reconciliations.created_at)`, is **the fold ADR-199 section 5 refuses one field to the left** for the batch, because a sweep resumable at the account boundary reports a success for a run that crashed. `mismatches_open` is a count of a state and is produced. **THE SUBTRACTION IS ARITHMETIC THE SUITE PERFORMS AND NOT A NUMBER THIS ENTRY WROTE DOWN.** `LiabilityBook` is `LiabilityResponse` minus exactly those paths, written as `Omit` over the served type rather than as a fourth copy of it, and [`admin-source-liability-book.test.ts`](../../apps/api/test/admin-source-liability-book.test.ts) reads the contract's paths through `RI-18`'s own `copiesOf`, folds them to **40** leaves, runs the reader, and asserts produced + blocked = declared. **A CONTAINER IS NOT A FIELD AND THE FIRST FOLD GOT IT WRONG**: a rule reading only the dotted extension leaves `per_plan`, `integrations.mid_health` and `eligible_next_7d.by_day` in the set and reports **43** fields this response does not have. **EXECUTED END TO END AGAINST A LIVE POSTGRESQL 16 WITH ALL 58 MIGRATIONS**, through `transaction(systemDb('operator-console'))`: the later of two snapshots chosen, `absorbed_corrections_cents` back **NEGATIVE** off a real signed `bigint`, `rcr_bp` **12000** off `0049`'s GENERATED column with `breaker_armed` false, the composite treasury anchor resolved, the `2026-08-27` breaker row taken over `2026-08-26` with `sales_paused` true, the later `psp_b` window taken and a `degraded` PSP rendered NOT healthy, 2 open mismatches of 4 rows, and the `06:04` `batch.completed` taken while a **LATER `batch.failed` at `06:30` was correctly ignored**. **AND THE MODULE'S ONE DESIGN CHOICE WAS MEASURED RATHER THAN ARGUED.** The treasury anchor is read by `rowsWhere` and never by `rowAt`, because `refuseUnaddressed` names `treasury_balances` as *"the only registered table with no addressable key at all"*: run live, `rowAt` is REFUSED on a predicate the database honours and the equality pair returns exactly 1 row. `LiabilityTx` therefore carries no `rowAt` at all. **THIRTEEN SEEDS, THIRTEEN REDS**, each restored from a byte-identical copy with the hash checked both ways and `git status --porcelain` empty after every one: the three defences separately at three sites (`TS2353` on the `Pick`, `TS2741` on the `Pick`, `TS2741` on `AdminReadSource`), the four clearing conditions (registering `tradingCalendar`, a `recon.completed` row in EVENTS, `cusum` made optional in the admin copy, a ruling defining `avg_30d` landing in `docs/decisions/`), and six on the module (a dropped projection field, `breaker_armed` as `<=`, an unfiltered `events` read, the tie refusal removed, `sales_paused` widened, and one entry deleted from the blocked list). **ONE STALE COMMENT INSIDE THE FENCE, REPAIRED**: a green case titled *"stays at the four that are written"* over an array session 371 moved to five. **No numeral replaces it**, on `admin-source/index.ts`'s own stated reason. 33 of 33 gates, **17 of 17 invariants**, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` clean read as OUTPUT, `CompositionReport.registered` **28** operator and **46** public paths over a real `compose()`, the wiring triple `{ declared: 23, wired: 6, blocked: 17 }` UNCHANGED and re-run rather than predicted, and **232 files / 5,715 passed / 6 skipped** against a `main` baseline at `70c3e5b` of **231 / 5,671 / 6 reproduced before a line changed**; the delta is **+1 file and +44 cases**, which is exactly the 33 written plus the 11 added to session 363's suite. **No ADR number, no migration number, no `SD-nn`, no `SystemReason` member, no `SqlExecutorReason` member, no `pg` import, no cast past a key type, no ledger account named, `API_CONTRACT.md` byte for byte unchanged, `packages/**` untouched, `apps/api/src/routes/admin-reads.ts` not opened, and `exportEvidence` not composed.** |
| [2026-08-28 - Session 375](2026-08-28-session-375.md) | **HALF THE DISPATCH'S CENTRAL PREMISE IS REFUTED AT ITS SOURCE, AND IT IS THE HALF ITS OWN ARITHMETIC PARAGRAPH IS ABOUT: THE LIABILITY DASHBOARD HAS A ROUTE AND IT IS `/`.** The dispatch asks for "the two screens whose renderers and adapters already exist and whose ROUTES do not". `pnpm --filter @merit/admin build` printed **six** routes before a line changed, exactly as the dispatch says, and `/` is one of them: [`src/app/page.tsx`](../../apps/admin/src/app/page.tsx) is M06 section 3.1's liability home, its own header says so in its first line, and the chain from session 364's arithmetic to the served bytes is unbroken -- `theThreeNumbers` is imported at [`page.ts:60`](../../apps/admin/src/page.ts), reaches `buildLiabilityHome`, reaches `renderLiabilityHomeDocument` in [`liability-home.tsx`](../../apps/admin/src/app/liability-home.tsx), and reaches the route at [`page.tsx:201`](../../apps/admin/src/app/page.tsx). `M6-A-70`, `M6-A-71` and `M6-A-72` already gate `INV-M6-15`, the absent third component and the three components under the total. **THE RENDERERS ARE NOT ROUTELESS; THE ROUTE IS READLESS**, and rebuilding it would have been session 368's regression risk against zero gain. **THE OTHER HALF HELD EXACTLY**: nothing under `apps/admin` held a search screen, so that one is built. **`/search`, AND THE TERM IS THE WHOLE PRECONDITION.** `FM-M6-10` is refused rather than described: an empty term throws in `assertQueryNamesASubject` before a byte is built, the route's no-term arm renders the box and NO ROW, and a repeated `?query=a&query=b` is refused rather than resolved to either half, because picking one is the route deciding which subject an operator meant. **IT IS ITS OWN SEGMENT AND NOT `/accounts`**, because `M6-A-66` asserts that directory holds exactly two entries and its SECOND reason -- "an index with no query behind it is FM-M6-10" -- is an invariant that does not expire; its FIRST reason, a fence reading "`AdminReadSource.searchAccounts` is owned by no plan at all", expired in both halves and is corrected where it is written. **`INV-M6-10` IS A CLOSURE HERE AND NOT A REFUSAL**: `assertNamesNoSubject` is deliberately not called, because it would refuse the accounts the operator asked about, and every subject-shaped id in the served bytes must instead be the TERM or an `account_id` or `identity_id` of a row this page carries. A coupon fanning across identities is inside the licence on M06 section 7.10's ruling and `ADR-194` clause 3's, and the fixture is three rows over TWO humans so a closure keyed on the wrong one is caught by the two counts disagreeing. **THE CURSOR IS NEVER SERVED**, which removes a class of leak rather than guarding it: session 371 records both its components as primary keys, so the token is a subject id in an encoding no closure can read. **`INV-M6-04` WITHHOLDS `size_cents`, `balance_cents` AND `withdrawable_cents`** on the same two independent mechanisms `W6-g` and `W6-j` met -- no `as_of` on the response, and [`figure.ts`](../../apps/admin/src/figure.ts)'s `ORIGIN_ID` closed at section 3.1's panels -- both REPORTED again rather than repaired; `open_flags` renders, because a count is not a figure. **AND `INV-M6-10`'s SECOND HALF IS MADE A TYPE OBLIGATION**: M06 section 7.9 requires every view of trader-identifying data to be logged as an access, `ADR-171` means there is no actor to write one, so `accessRecord` is a required field refused blank and the read arm cannot be filled without stating what was recorded. **THE SESSION'S SECOND FINDING IS A COUNT EVERY SCREEN IN THE CONSOLE WAS QUOTING INTO A PANEL AN OPERATOR READS: "`exportEvidence`, `readLiability` and `searchAccounts` have no module" IS THREE AND IS ONE.** `searchAccounts` is session 371's [`admin-source/search.ts`](../../apps/api/src/admin-source/search.ts) and `exportEvidence` reaches `AdminReadSource` through `adminReadSourceParts` rather than through `IMPLEMENTED_ADMIN_READS`, **so a reader counting implemented methods off that array alone undercounts by one**, which is session 371's own Next item arriving as a live defect in five files. `IMPLEMENTED_ADMIN_READS` holds **five** where three files said two or four. **`readLiability` IS THE ONE METHOD LEFT.** **AND THE LIABILITY HOME'S THIRD BLOCKER IS RETIRED ON A MEASUREMENT RATHER THAN REWORDED**: it read that four of the five figures `buildLiabilityHome` reads have no field on the response, and `LiabilityResponse` declares **twelve** top-level fields with all four among them, with `RI-18` binding all three declarations to one field set. **WHAT SURVIVED THAT ENTRY IS NOT A BLOCKER AND IS NOW SERVED IN ITS OWN SECTION**: `ADR-195`'s third component of `P-M6-01` has no column and so no field, [`liability.ts`](../../apps/admin/src/liability.ts) renders it ABSENT with its reason and marks the total INCOMPLETE, and a term the page renders honestly is not a term the page waits for. `M6-A-61` is updated to the measurement rather than weakened, deriving the four field names out of the console's own transcription with a non-vacuity guard asserted first. **ELEVEN SEEDS, ALL ELEVEN FIRED**, `git status --porcelain` empty after every one, the valuable ones being the blinded sweep (5 red, the vacuous pass) and the dropped `LiabilityResponse` field, which `RI-18` caught exactly as the dispatch predicted. **REPORTED AND NOT REPAIRED**: `figure.ts`'s origin roster and `AdminAccountSearchItem`'s missing `as_of` are in no fence here; `GET /admin/accounts` types its ITEM and not its envelope, as `GET /admin/flags` does and `GET /admin/events` does not; and `/`, `/feed` and `/flags` still PRERENDER STATIC, which is session 368's landmine measured on this application for the first time and harmless only while no admin route reads anything. **NO ADR NUMBER, NO MIGRATION NUMBER, NO `SD-nn`, NO EDIT TO `apps/api`, API_CONTRACT, M06 OR `figure.ts`.** **Seven routes where the build printed six, `/search` DYNAMIC. 33 of 33 gates, 17 of 17 invariants, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` read as OUTPUT (33 gates pass clean and fail dirty, 80 scope cases, 10 loader cases) with the tree clean after it, and 232 files / 5,709 passed / 6 skipped against a `main` baseline at `70c3e5b` of 231 / 5,671 / 6 reproduced exactly before a line changed.** |
| [2026-08-28 - Session 376](2026-08-28-session-376.md) | **THE FOURTH AND LAST OF [ADR-196](../decisions/ADR-196.md)'s HONESTLY PRICED FOUR LANDS AND `POST /auth/verify` ANSWERS.** [ADR-200](../decisions/ADR-200.md), `status: proposed`, approval line UNSIGNED, and **migration [`0063`](../../packages/db/migrations/0063_otp_challenge_consumption.sql) IS TAKEN AND SPENT WHILE THE LIMB ITS RESERVATION NAMED FAILED**, which is said plainly rather than folded in. `verifyOtp` reads the newest live challenge, matches under every admitted key, consumes, resolves, establishes when nobody answered, and mints. **A TRADER STILL CANNOT SIGN UP AND THE ENTRY SAYS SO FIRST RATHER THAN LAST**: `requestOtp` is blocked on DELIVERY, so nothing in this deployable writes the `otp_challenges` row the handler reads. **TWO MERGED SENTENCES CLAIM A CONTROL NO OBJECT CARRIES**: [`0002:328-330`](../../packages/db/migrations/0002_identity.sql) writes *"Single use, enforced by the database rather than by the handler"* over `otp_challenges_unconsumed_uq ON otp_challenges (id) WHERE consumed_at IS NULL` and **`id` is the PRIMARY KEY**, so that index is unique whatever its predicate says; [SECURITY section 2](../architecture/SECURITY.md) states the same control twice as *"short TTL, single use, 5-attempt lock"*. **EXECUTED THROUGH THE SHIPPED HANDLER AGAINST POSTGRESQL 16, ONE FILE APART**: with `0063`, two concurrent verifications of one code answer `is_new=true` and `null (401)` and mint **ONE** session; with it dropped they answer `is_new=true` and `is_new=false` and mint **TWO SESSIONS FROM ONE CODE**. **[ADR-196](../decisions/ADR-196.md) SECTION 8's THREE GROUNDS WERE EACH RETESTED**: the A/B is **15 of 15 BOTH WAYS** where that entry's candidate went 15 without and 14 with, `verifyOtp` is the caller `identities` did not have, and `BEFORE UPDATE` sees every statement that could move the column. **A HANDLER CANNOT CLOSE IT AND THAT IS THREE MERGED RULINGS**: an address is equality only ([ADR-112](../decisions/ADR-112.md) clause 1), `isNull()` is refused in a write address ([ADR-157](../decisions/ADR-157.md), reported rather than proposed for widening), and `FirmTx` carries no `FOR UPDATE` read where `ScopedTx` carries `rowAtForUpdate` -- **the smaller repair, refused in favour of the trigger because it puts single use back in the handler, which is exactly where `0002`'s comment already assumed it did not need to be.** **`ApiDb` GAINS `resolution(fn)` AND `establishment(fn)` AND [ADR-171](../decisions/ADR-171.md) CLAUSE 1 IS NOT OVERTURNED**: that clause refuses `operator(fn)` over `SystemTx` and its section 5 argues that a door narrowed at the PORT arrives on the object `databaseAuthBackend` already holds; these two are narrowed **at the door**, one table at one address and one verb with no executor, and its own section 9 states the condition they meet. **THREE FURTHER RULINGS THE TWO LANDED ENTRIES NAME AS OPEN**: a verification answers the NEWEST live challenge and only that one, because `attempts` is per challenge and walking every live row multiplies the lockout budget by a number the ATTACKER chooses; consumption precedes establishment, on the price [ADR-126](../decisions/ADR-126.md) already stated for the two-unit-of-work split; and `SESSION_LIFETIME_DAYS = 30`, ruled at its only writer because **the contract declares NO REFRESH ENDPOINT**, so `expires_at` is the whole login lifetime rather than the access half of a pair. **SIX OTHER QUESTIONS WERE PURE TRANSCRIPTION AND THE ENTRY NAMES THE CLAUSE THAT ANSWERED EACH.** **THE `sms` CHANNEL CANNOT LOG ANYBODY IN EITHER, WHICH IS THE SECOND HALF OF A RULING THAT ONLY HAD ITS FIRST**: `RESOLUTION_ADDRESS` is `{ users: ['email'] }` and a phone has no address in that vocabulary. **THE CENSUS WAS RE-DERIVED AND IT HAS MOVED**: zero hits in all three of [ADR-196](../decisions/ADR-196.md)'s spellings over **53** non-test `.ts` files that mention the table, and **there is a writer now in a FOURTH spelling the census does not look for** -- `unscopedInsertStatement(source, 'identities', {})` inside `establishmentTx` -- so *"nothing writes `identities`"* was true of the greps and false of the tree from the moment [ADR-197](../decisions/ADR-197.md) merged. **THE DISPATCH'S ONE STALE ITEM WAS ALREADY REPAIRED BY SESSION 367 AND TWO MORE WERE RETIRED HERE**: `NO_VERIFY_HANDLER`, which this diff falsifies, and `NO_OTP_KEY_WIRED`, which it wires. **THE FENCE WAS WIDENED TWICE AND BOTH ARE NAMED**: [`apps/api/src/db.ts`](../../apps/api/src/db.ts) and six suites that build an `ApiDb` by hand. **AND `apps/api/src/routes/verify.ts` WAS IN THE FENCE AND IS NOT `POST /auth/verify`**: it is `GET /verify/:code`, [ADR-170](../decisions/ADR-170.md)'s public certificate oracle. It was not opened. **THREE THINGS REPORTED AND NOT TAKEN, ALL IN `packages/db/src/**` WHICH IS NOBODY'S THIS WAVE**: `rowAtForUpdate` on `FirmTx`, an expression in an `UPDATE`'s `SET`, and a phone address in `RESOLUTION_ADDRESS`. Counts derived at reporting time: **59** migrations, **114** tables, **397** indexes, **484** checks, **25** triggers and **111** functions, `0063` being the whole of the last two moving. **FOURTEEN SEEDS, FOURTEEN REDS**, tree clean after each. 33 of 33 gates, **17 of 17** invariants, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` read as OUTPUT, and **231 files / 5,685 passed / 6 skipped** against a base at `70c3e5b` of **231 / 5,671 / 6** reproduced before a line changed; **sessions 373, 374 and 375 landed on `main` mid-branch**, so the merged head is re-measured at each merge: **232 / 5,729 / 6** against `origin/main` at `54e2faa` measured in a worktree at **232 / 5,715 / 6**, and **237 / 5,822 / 6** against `1c6c44b` at **237 / 5,808 / 6**, a delta of **+14 on every base**. |
| [2026-08-28 - Session 377](2026-08-28-session-377.md) | **THE TABLE IS REGISTERED AND IT NEEDED NEITHER A RULING NOR A CLASS ARGUMENT, WHICH IS WHY NOBODY TOOK IT FOR TEN WAVES.** `trading_calendar` is `TableKey` **109 of 114**, scope class `firm`, and the class is the DDL's own answer: `trading_day date PRIMARY KEY` is a DAY, the row declares **NO foreign key at all** and no column against `identities(id)` or `accounts(id)`, so `owned`, `pair` and `either` have nothing to name, `derived` has nothing to traverse, and `root` is `identities`' alone. **THE DISPATCH'S CENTRAL PREMISE HELD AT SOURCE AND ONE OF ITS SUPPORTING ONES DID NOT.** [`schema.ts`](../../packages/db/src/schema.ts) did say the table "cannot be registered" on [ADR-094](../decisions/ADR-094.md)'s one-member fold and [ADR-103](../decisions/ADR-103.md) clause 2 did supersede exactly that clause, naming this table REGISTRABLE; but the dispatch's *"[ADR-191](../decisions/ADR-191.md) ... its registration touched `packages/db/src/scope.ts` alone"* is **REFUTED**: `5333b45` touched **six** files including `scoped-db.ts`, and `0bec092`, the `reserve_coverage_snapshots` registration, touched **three** and was followed by `71150bb`, a citation commit in `apps/api` and `apps/worker`. That precedent is what decided this session's shape. **THE PERMISSION WAS NOT TAKEN ON THE RULING'S WORD.** ADR-103 clause 3 keeps the sub-vocabulary closed at one shape with a default of FAIL, and every `ALTER TABLE trading_calendar` in the tree was re-read: two `ALTER COLUMN ... DROP NOT NULL`, one `DROP CONSTRAINT` and two `ADD CONSTRAINT`, no `ADD COLUMN`, no `SET DATA TYPE`, no `SET NOT NULL`, no `DROP COLUMN`, no `RENAME`. So `0004`'s CREATE body IS the column set with exactly two nullabilities moved, and the transcription follows the FOLD rather than the CREATE. **FIVE STALE SENTENCES, NOT THE ONE THE DISPATCH NAMED.** Two headers in `schema.ts` and two reasons in [`scope.ts`](../../packages/db/src/scope.ts) carried the superseded refusal, and a FIFTH claim was found while repairing them and is a different error: both the `trading_calendar_revisions` header and its `why` said that table *"carries no `ALTER TABLE` of any shape"*, and [`0048`](../../packages/db/migrations/0048_audited_writes_on_append_only_tables.sql) carries two, the pair that makes its FK `DEFERRABLE INITIALLY DEFERRED`. **The conclusion survives and the stated reason does not**, which is also why `trading_day` still carries no `.references()`: the constraint standing as of the last migration is ALTER-added, and this file leaves those to the database. **EXECUTED AGAINST A LIVE POSTGRESQL 16 WITH ALL 59 MIGRATIONS AND 114 TABLES.** Through `transaction(firmDb())`: four seeded days read back whole, `rowsWhere({ isHoliday: true })` returning the holiday with **both session columns NULL**, `rowsWhere({ isHalfDay: true })` returning the per-group closes out of `notes`, `rowAt({ tradingDay })` resolving one day on the transcribed primary key and `undefined` on a day the calendar does not know, and `scopedDb` refusing the key with the rule's own reason. **THE FOUR GUARDS WERE WATCHED FIRING BESIDE THE ACCEPTANCES**: a sessionless non-holiday and a holiday with a session both refused by `trading_calendar_holiday_has_no_session`, DELETE refused by CALENDAR-C2 and an UPDATE with no prior image refused by CALENDAR-C1. **THE FENCE WAS SELF-CONTRADICTORY AND THE OWNER RULED ON IT.** Registering a table moves lines in `scope.ts` and `RI-15` binds citations to line numbers, so *"`apps/**` is not yours at all"* and *"end mergeable"* could not both hold; the session stopped, reported, and was told to take both repairs in two commits. **EIGHT CITATION INTEGERS REPOINTED IN `apps/api`, NO CLAIM CHANGED, AND SEVEN MORE REPORTED RATHER THAN TAKEN**: `RI-15` cannot see a comma pair (`675,684`), a name whose backtick abuts an apostrophe or bold marks (``daily_marks`'`` and `COLUMN**`), or a citation whose name reappears within its two-line window on an unrelated table, which `schema.ts:3005` now does. **TWO CLEARING CONDITIONS FIRED AND WERE SPENT IN THE DIFF THAT FIRED THEM**, which is session 372's precedent: session 374's blocker **B1 is lifted**, and both cases now read `toContain`. **WHAT LIFTED IS THE BLOCKER AND NOT THE WORK** and both cases say so: `LIABILITY_READ_TABLES` still does not name the calendar, `readLiability` still produces 27 of 40 leaves, and the five `eligible_next_7d` leaves are ordinary code in another fence now instead of a `TS2322` against this one. **SIX SEEDS, SIX REDS**, two of them at `tsc` rather than at `vitest`, each restored from a byte-identical copy with `git status --porcelain` empty after every one: the wrong class, the nullability transcribed from the CREATE, the primary key left off, an `ALTER COLUMN ... SET NOT NULL` seeded as a migration, the FK reader blinded (caught by the non-vacuity case alone), and the key removed from `TABLES`. Counts derived at reporting time: **109** declared tables of **114** created, 109 rules, **43** firm, 3 pair, **63** served by `scopedDb`, **59** migrations, **114** live tables. **33 of 33** gates, **17 of 17** invariants, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` read as OUTPUT, and **239 files / 5,856 passed / 6 skipped** against `origin/main` at `9e02145` measured in a worktree at **239 / 5,846 / 6**, and against the `dbe7ecb` baseline of **237 / 5,822 / 6** reproduced before a line changed. **The delta is +10 where 8 cases were written, and the other two are the point**: `packages/db` goes 616 to 626 because the per-table drift and type-and-nullability comparisons are GENERATED from `TABLE_KEYS`, so a registration that failed to add its own comparisons would show as a count that did not move. **No ADR number, no migration number, no `SD-nn`, no `SystemReason` or `SqlExecutorReason` member, no `pg` import, no cast past a key type, `packages/db/src/{index,scoped-db}.ts` untouched, `apps/portal/**` and `apps/worker/**` untouched, and every merged migration byte for byte unchanged.** |
| [2026-08-28 - Session 378](2026-08-28-session-378.md) | **THE LAST ROW OF `M04` SECTION 3.1 WITH NO ROUTE NOW HAS ONE, AND NOBODY CAN SIGN IN THROUGH IT.** `/sign-in`, static, and the dispatch's two premises were both re-derived at source: the build printed fourteen routes before a line changed, exactly as dispatched, and **every page file behind every one of them was read** before concluding `SC-M4-01` was absent, which is session 375's landmine applied rather than quoted. **ALL FOUR ROUTES BEHIND THE SCREEN ARE REGISTERED AND NOT ONE IS WIRED**, measured through `CompositionReport.registered` over a real `compose()` and then through the backend each route calls: `requestOtp` is blocked on `NO_DELIVERY`, both passkey login methods on `NO_WEBAUTHN`, and `verifyOtp`'s `sms` arm raises separately because a phone has no address in `RESOLUTION_ADDRESS` ([ADR-200](../decisions/ADR-200.md) section 4.4). **A ROUTE TABLE OR A GREP WOULD HAVE REPORTED THIS SCREEN AS FULLY SERVED.** **THREE THINGS THE SCREEN MUST NOT DO, EACH ASSERTED RATHER THAN INTENDED**: no password field, no reset link and no recovery step, stated BY SHAPE and carrying no sentence about it, which is what keeps the assertion blunt enough to fail on the word itself; **`sms` is never offered as a way to sign up** ([ADR-196](../decisions/ADR-196.md) clause 5, derived from `users.email citext NOT NULL UNIQUE`), so email is the only door into Merit and the screen says which factor is which; and **no separate create-account control**, because `ADR-196` section 3 refuses that surface and two controls would disclose what `POST /auth/otp` withholds. **EXISTENCE IS UNDISCLOSABLE STRUCTURALLY AND NOT MERELY UNDISCLOSED BY COPY**: `toSignInView` takes no address and no submitted value of any kind, so there is no branch an existence check could enter, no sentence that can vary with a destination, and no work whose duration could differ. **THE TWO ABSENCES ARE KEPT INDEPENDENT AND BOTH ARE STATED**: `served` is `apps/api`'s and `submits_to`, typed as the literal `null`, is this application's, and the acceptance case drives the first to `true` and watches the second survive. **TWO STALE PARAGRAPHS WERE FOUND AND REPAIRED, NEITHER IN THE DISPATCH**: `index.ts` still read "EIGHT OF SECTION 3.1's ELEVEN SCREENS EXIST ... THE THREE THAT DO NOT ARE SC-M4-01, SC-M4-10 AND SC-M4-11" while its own export blocks at the foot of the same file had been updated for the two that landed, and `app/page.tsx` carried "Sign-in is not built here yet" on a screen whose header argues that this application's whole subject is not making false statements on a screen. **ONE DISPATCH PREMISE IS REFUTED**: repo-invariants is **17 of 17** and the dispatch says sixteen, `RI-18` having landed. **SEVEN SEEDS, SEVEN REDS**, each restored from a copy taken before it. **NO API CHANGE, NO ADR, NO MIGRATION, NO `SD-nn`, AND `apps/api/**` NOT OPENED FOR WRITING.** Counts derived at reporting time: **fifteen** routes in the portal build table where it printed fourteen, `/sign-in` STATIC and correctly so; **33 of 33** gates, **17 of 17** invariants, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` read as OUTPUT with all 33 gates clean and dirty plus 80 scope and 10 loader cases and the tree clean after it, and **238 files / 5,841 passed / 6 skipped** against a `main` baseline at `dbe7ecb` of **237 / 5,822 / 6** reproduced before a line changed, a delta of **+1 file and +19 cases** which is exactly the cases this session wrote. |
| [2026-08-28 - Session 379](2026-08-28-session-379.md) | **THE SLICE IS BLOCKED AND THE MEASUREMENT IS THE DELIVERABLE.** No wiring landed and none could: **`apps/worker` HOLDS EVERY `emit` CALL IN THE WORKSPACE AND NO PRODUCER, AND `apps/api` HOLDS THE ONLY PRODUCER AND NO DOOR THAT CAN CARRY THE WRITE.** Seven call sites re-derived at this commit, in three files across three independently unwired ports, and **zero in `apps/api/src`**. **THE DOOR HALF OF SESSION 366's FINDING IS ANSWERED AND IT IS ANSWERED YES**: [`apps/worker/src/db.ts`](../../apps/worker/src/db.ts)'s `LIVE_DB.batch` yields a `SystemTx` at `systemDb('nightly-batch')`, which is the ONE handle whose `insert` is generic over `TableKey`, so this deployable has held the door `apps/api` does not since ADR-165. **WHAT IT HAS NEVER HELD IS THE PRODUCER**, and `RI-04` plus `node-linker=isolated` make that unreachable in both spellings: the manifest line is refused by the invariant and a relative specifier is refused by nothing, which is why a case now refuses it. **THE SECOND FENCE WAS NOT IN THE DISPATCH AND IT IS THE ONE THAT DECIDES THE SLICE'S SIZE. WIRING THE SINK WOULD MAKE TWO CALL SITES WRITE, NOT SEVEN.** `buildEvent` refuses a name that is not a row in `EVENT_CATALOGUE` on ADR-159 clause 1, and **FOUR of the seven names are not rows in it**: `flag.raised` and `detector.run_completed` ARE rows in [EVENTS](../architecture/EVENTS.md) at `:354` and `:358` and are absent from the producer's transcription of it, and `detector.run_degraded` and `breaker.state_changed` **have no row in EVENTS at all** -- their payloads are [`M07:250`](../plans/M07-risk-abuse.md)'s and [`M06:266`](../plans/M06-admin-ops-console.md)'s, both absences already registered and pinned by sessions 300 and 320. The fifth is `payout.freeze_expiring`, refused by `assertTenanted` exactly as the dispatch predicted. **SO THE SEVEN ARE 2 WRITE / 4 REFUSED BY NAME / 1 REFUSED BY TENANCY, and every one of those five repairs lives outside this fence.** **THE DISPATCH'S CENTRAL PREMISE ABOUT `ADR-198` IS REFUTED, AND IT IS REFUTED BY MEASUREMENT RATHER THAN BY ARGUMENT: NOT ONE OF THE SEVEN PAYLOADS CARRIES A `_cents` FIELD.** That ruling unblocked `payout.requested`, `payout.approved` and `payout.held`, which are `apps/api`'s three, and `apps/api` has zero call sites, so **`ADR-198` is what makes this slice possible for nobody yet** and is correct anyway. **THE EVIDENCE IS ROWS IN A DATABASE AND NOT A GREEN SUITE.** PostgreSQL 16, all **59** migrations under `ON_ERROR_STOP=1`, **114** tables, and the REAL `runExpirySweep` run over the worker's OWN door with the api producer behind the port: **it released a held payout and a halted withdrawal and wrote TWO real `events` rows**, `payout.hold_released` under the identity and account legs and `wallet.withdrawal_halt_released` under the identity leg alone, `actor_kind` `system` read out of `released_by: 'expiry'`, `occurred_at` the injected clock and `recorded_at` the database's. The freeze warning failed inside the sweep with `assertTenanted`'s message and the sweep reported it as one `failed` outcome rather than dying, which is `FM-M5-13`'s design working. **THE FOUR OFF-CATALOGUE NAMES WERE THEN RUN THROUGH THE SAME SINK AGAINST THE SAME DATABASE AND ALL FOUR THREW `EventError` AT THE NAME**, before the payload was consulted. **`ADR-198`'s FORMAT WAS ROUND-TRIPPED THROUGH THAT DOOR ANYWAY**: `9007199254740993` stored as the string `"9007199254740993"` and read back exact through `centsFromPayload`, where a JSON number renders `...992`. **WHAT LANDED IS ONE TEST FILE AND THE MEASUREMENT.** [`event-sink.test.ts`](../../apps/worker/test/event-sink.test.ts), five cases: the call-site count and its per-file map derived from source, the seven names EXHAUSTIVE AT THE TYPE LEVEL over the three ports' unions so a name added to any of them is a compile error here, the catalogue parsed out of `apps/api/src/events.ts` as text with the parse asserted before anything rests on it, the relative-specifier fence `db.test.ts` cannot see, and the freeze warning's missing tenancy as a property of its payload. **EIGHT SEEDS, EIGHT REDS**, two of them at `tsc` rather than at `vitest`, tree clean after each. **A SECOND AND INDEPENDENT BLOCKER WAS FOUND WHILE COMPOSING THE PROBE AND IS REPORTED**: `ExpirySweepIo.ledger` is unwirable here too, because `@merit/ledger` is not in `apps/worker/package.json` and `postLt01` has nowhere to come from. **NO ADR NUMBER, NO MIGRATION NUMBER, NO `SD-nn`, no `SystemReason` or `SqlExecutorReason` member, no `pg` import, no cast past a key type, no door widened and no gate weakened.** `apps/api/**`, `packages/**` and [`apps/worker/src/index.ts`](../../apps/worker/src/index.ts) are all untouched. 33 of 33 gates, **17 of 17** invariants (not 16; the protocol's figure is stale and this is derived), typecheck 0, lint 0, format clean, `falsify.mjs` read as OUTPUT, and **238 files / 5,827 passed / 6 skipped** against a `main` baseline at `dbe7ecb` of **237 / 5,822 / 6** reproduced exactly before a line changed. The delta is **+5** and it is exactly the five cases this session wrote. |
| [2026-08-28 - Session 380](2026-08-28-session-380.md) | **B1 IS SPENT, THE HORIZON IS BUILT AND EXECUTED, AND `eligible_next_7d` IS STILL NOT PRODUCED.** The dispatch's premise is that `B1` "was worth 5 of the 13 leaves and `eligible_next_7d` is the field it unblocks", and that premise is **HALF RIGHT AND THE HALF THAT FAILS IS THE FIELD**. `eligible_next_7d` is TWO folds: the next seven TRADING days, which B1 unblocked and which this session built, executed and pinned; and a **PER-ACCOUNT ELIGIBILITY FORECAST** over them, which nobody had looked at and which has no source. **THE BLOCKED-LEAF COUNT IS UNCHANGED AT 13 OF 40 AND THAT IS THE FINDING**, not an absence of one. **THE STARTING MEASUREMENT WAS GREEN FOR THE SECOND SESSION RUNNING AND THE DISPATCH IS READING THE MECHANISM BACKWARDS.** Run first, before a line changed: **56 of 56 GREEN**, because [session 377](2026-08-28-session-377.md) section 6 spent both of B1's clearing conditions in its own diff and said so in terms. Session 374 was dispatched to read a red and found 12 of 12 green for the identical reason. **A CLEARING CONDITION IS SPENT BY THE SESSION THAT LIFTS THE BLOCKER**, so the next session inherits a green suite and an accurate map; that is [session 363](2026-08-28-session-363.md)'s design working, and a dispatch that plans to read a red should check whether the lifting session already consumed it. **THE TRADING-DAY QUESTION IS ANSWERED AND THE ANSWER IS A VALUE RATHER THAN AN ARRAY.** [`readTradingHorizon`](../../apps/api/src/admin-source/liability.ts) reads `trading_calendar` and `trading_calendar_loads` **together**, and `LIABILITY_READ_TABLES` names both, which it could not do at all while a `Tx` naming the calendar was `TS2322`. Anchored on `2026-11-24`, seven **CALENDAR** days ends `2026-12-01` and seven **TRADING** days ends **`2026-12-04`**: the two answers differ by **three days on one ordinary late-November week**, which is `AS-06`'s "five trading days is 7 calendar days in June and 9 to 10 across the year-end cluster" arriving on this field. **NO DAY IS COMPUTED**: every returned day is a ROW, filtered to `is_holiday = false` and taken in ascending order, because `R-02` fixes gap counting as calendar subtraction and "never date arithmetic". **THE HALF DAY AND THE HALTED DAY BOTH STAY IN THE HORIZON** (`0004` B4 #2 and #3) and only `is_holiday` removes a day. **AN EXHAUSTED CALENDAR SAYS SO, WHICH IS THE WHOLE OF [ADR-042](../decisions/ADR-042.md) F-4.** `TradingHorizon` is a discriminated union: `exhausted` carries the days found, the day coverage runs through and **how many short it is**, and `uncovered` is the answer when no load declares coverage at all or the anchor sits outside every interval. **FIVE SESSION ROWS PAST THE COVERAGE EDGE WERE PRESENT IN THE LIVE FIXTURE AND NOT ONE WAS TAKEN**, on `nextTradingDayAfter`'s own ruling that "THE LAST DAY IN `days` IS A MISS EVEN WHEN COVERAGE EXTENDS PAST IT". Two disjoint loads do NOT bridge their gap; two that overlap merge. **THE ANCHOR IS THE LAST CLOSED SESSION AND IT IS RESOLVED THROUGH THE CALENDAR RATHER THAN OFF THE INSTANT'S UTC DATE**: `liability_snapshots` carries `as_of timestamptz` and **no `trading_day` column**, and `P-M6-01` dates the figure at "the last closed day" (`INV-M6-11`), so a session still trading at the instant is day ONE of the horizon and never the anchor. Watched live: at `20:00Z` on `2026-11-25` the anchor is `2026-11-24`; two hours later it is `2026-11-25` and the whole window moves. **BLOCKER B5 IS THE OTHER HALF AND IT HAS TWO INDEPENDENT LEGS, EITHER OF WHICH ALONE BLOCKS THE GROUP.** **LEG 1: SIX GATE GROUPS DECIDE ELIGIBILITY AND EXACTLY ONE PUBLISHES A FORWARD-LOOKING DATE.** `cadenceGap.nextEligibleTradingDay` is `AS-06`'s resolved date; `tradedDays`, `winDays`, `buffer`, `consistency` and `minimumAmount` clear only when the trader TRADES, so no stored row says when. That one date lives in `rule_states.engine_gates`, and **NOTHING IN THIS TREE WRITES IT**: `writeRuleState` is a port whose only implementations are a test double and [`scripts/demo/world.ts`](../../scripts/demo/world.ts), which **REFUSES**. Its JSON encoding is declared by no primary source either -- `EngineGateResults` types every cents member `bigint`, which JSON cannot carry, API_CONTRACT carries no `engine_gates` shape at all, and **`0015`'s own column comment names EIGHT gates where the engine produces SIX**. **THIS IS NOT `integrations.batch`'s CASE AND THE CONTRAST IS THE POINT**: [ADR-199](../decisions/ADR-199.md) clause 4 could rule the batch's two figures readable off an event nothing has emitted **because EVENTS section 5 DECLARES that body in the approved catalogue**. **LEG 2: `R-37` COUNTS BY `sequence` SUBTRACTION AND `trading_calendar` DECLARES NO SUCH COLUMN.** ADR-199 section 6's other route ends at a missing column: the engine takes its slice from a PORT the caller supplies, `0004`'s CREATE body has no `sequence`, no `ALTER TABLE trading_calendar ADD COLUMN` exists in any of the 59 migrations, and `packages/db/src/seed/calendars/generate.mjs` assigns none. The only substitute available is the date arithmetic `AS-06` says publishes "a rule its own traders cannot evaluate". **AND THE FIRST DRAFT OF THAT ASSERTION WAS WRONG AND THE SUITE CAUGHT IT**: `expect(migrationColumnNames()).not.toContain('sequence')` went RED, because `0050` declares `live_account_state.sequence`, a tick ordinal "1-based PER ACCOUNT PER DAY". **A census over 735 column names cannot make a claim about one of 114 tables**, so the assertion is table-scoped and the near-miss is pinned beside it. **THE GROUP GOES WHOLE OR NOT AT ALL, ON `B2`'s STATED REASON**: producing only the accounts eligible TODAY would understate `total_cents`, which is the figure the payout wallet is funded against (`EC-074`, `P-M6-02`, `0009`'s own column comment, [ADR-011](../decisions/ADR-011.md)'s top-up trigger). **AND RECOMPUTING THE GATE WOULD BE THE WRONG SHAPE EVEN IF THE COLUMN EXISTED**, on this directory's own rule in [`account.ts`](../../apps/api/src/admin-source/account.ts): "Nothing in this module derives an eligibility, recomputes a gate or summarises one". **THIRTEEN SEEDS, THIRTEEN REDS, AND ONE OF THEM ONLY AFTER THE REPAIR ITS FAILURE EXPOSED.** Seed 4 seeded `0032`'s two-way CHECK as one-directional and **ALL 82 CASES STAYED GREEN**: the row fell through to `instant()`, which refuses a null timestamp for its own reason and throws the same class, so three cases were pinning that SOMETHING refused rather than that THIS control did. Each now asserts the **constraint name**, and both one-directional seeds go red. **A seed that fails to fire is the most valuable result available and it caught a second thing in the same session**: the fixture's own session interval was inverted, and `trading_calendar_session_ordered`'s re-assertion is what said so. **EXECUTED END TO END AGAINST A LIVE POSTGRESQL 16 WITH ALL 59 MIGRATIONS AND 114 TABLES**, over 34 seeded calendar rows through `transaction(firmDb())`, and **the write guards fired unbidden beside the acceptances**: `CALENDAR-C2` refused a `TRUNCATE` and `CALENDAR-C1` refused two `UPDATE`s with no prior image, each with its own migration's message. **`IMPLEMENTED_ADMIN_READS` STAYS AT FIVE, DERIVED AND NOT TRUSTED** (`listEvents`, `listFlags`, `readAccount`, `readIdentityGraph`, `searchAccounts`), `composeAdminReadSource` still answers `AdminSourceNotComposed('readLiability')`, and **`readLiability` IS NOT COMPOSED**, on session 374's judgement that composing against a source it cannot fully serve turns an honest 500 into a wrong answer. **ADR-188's "NO TOTAL IS SENT" WAS OBEYED AND NO TOTAL IS COMPUTED ANYWHERE IN THE DIFF.** Counts derived at reporting time: **40** declared leaves, **13** blocked, **27** produced, all three asserted by the suite against API_CONTRACT rather than written down; `CompositionReport.registered` **28** operator and **46** public over a real `compose()`; the wiring triple `{ declared: 23, wired: 6, blocked: 17 }` unchanged; **59** migrations and **114** live tables. **33 of 33** gates, **17 of 17** invariants, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` read as OUTPUT with all 33 gates clean-and-dirty plus 80 scope and 10 loader cases and `git status --porcelain` empty after it, and **239 files / 5,882 passed / 6 skipped** against a `main` baseline at `4bd102e` of **239 / 5,856 / 6 reproduced before a line changed**; the delta is **+0 files and +26 cases**, which is exactly the 26 written. **No ADR number, no migration number, no `SD-nn`, no `SystemReason` member, no `SqlExecutorReason` member, no `pg` import, no cast past a key type, no ledger account named, no join and no aggregate, API_CONTRACT byte for byte unchanged, `packages/**` untouched, and `apps/api/src/events.ts` and `apps/api/src/routes/admin-reads.ts` not opened.** |
| [2026-08-28 - Session 381](2026-08-28-session-381.md) | **THE DISPATCH'S PREMISE IS TRUE IN SUBSTANCE AND FALSE AS SPELLED, AND THE CORRECTION CHANGED WHAT THE RULING HAD TO PRODUCE.** "The threshold is fixed in three documents and the window in NONE" is refuted at source: [M06:95](../plans/M06-admin-ops-console.md) states BOTH windows, in words, on the row that defines the panel. What is stated in none of the three is **what the average averages**, and session 374's longer text says exactly that where its own summary row does not. Derived per document rather than as a group: the numerator window is stated in **one** of three, the denominator's period in **all three**, and the averaging basis and the day unit in **NONE** -- those two together being the half without which a ratio is not a number. **THE THRESHOLD IS STATED BY FOUR DOCUMENTS, NOT THREE, AND ALL FOUR AGREE INCLUDING ON STRICTNESS.** [MERIT_BUILD_MASTER_PROMPT:166](../../MERIT_BUILD_MASTER_PROMPT.md), [M06:95](../plans/M06-admin-ops-console.md), [INFRA:160](../architecture/INFRA.md), and non-normatively [ADVERSARY_DOSSIER:49](../../research/ADVERSARY_DOSSIER.md): one value, 2.5x, every one of them *"above"*, *"over"* or *">"*, so `25000` bp compared with `>` and nothing alarms at exactly 25000. **Zero disagreements, so the second finding the dispatch provided for did not arise, and it is said plainly rather than left as an absence.** **THE READING IS PROVED AND NOT CHOSEN, AND ONE OF THE TWO PROOFS NEEDS NO DATA AT ALL.** Of the three readings `avg_30d_cents` admits, **the 30-day TOTAL is refuted arithmetically**: the trailing 7 days are a SUBSET of the trailing 30, so `last_7d <= total_30` on every possible history, so `ratio_bp <= 10000`, so **the alarm can never fire, on any input, ever** -- reading A makes INFRA's pager a decoration. **The mean DAILY figure is refuted by one division**: steady state sits near `70000` bp, `2.8x` the threshold, so the pager fires every day and goes quiet only on a roughly 64 percent collapse in payouts, which is `FM-M6-07`'s *"either constant alarms, which become noise, or none, which is the same as no chart"* arrived at from the other direction. **The mean scaled to seven days is the only survivor**, sits near `10000` bp in steady state, and is the sentence the constitution already says out loud. **EIGHT RULINGS, ONE OF WHICH IS THE MISSING HALF AND SEVEN OF WHICH ARE WHAT MAKE IT EVALUABLE.** `25000` bp strict; `avg_30d_cents = floor(total_30_cents * 7 / 30)`; both windows counted in **TRADING DAYS**; **every settled payout attributed to the first trading day ON OR AFTER its settlement date**; the integer arithmetic entire, with the two `floor`s' opposite directions stated rather than hidden; `avg = 0` gives `ratio_bp = 0` and `alarm = false`; the window a **structural ruling**; carried as **one named exported constant in one module**. **THE TRADING-DAY QUESTION IS ANSWERED ON STATIONARITY, NOT ON THE FACT THAT A CALENDAR EXISTS, AND THE CORPUS'S OWN STRONGEST COUNTER-SOURCE IS QUOTED IN FULL RATHER THAN SUMMARISED.** [GLOSSARY:47](../GLOSSARY.md) warns that whoever *"reaches for the only calendar in the database reaches for a different set of days, and that substitution is wrong on roughly 104 days a year while looking exactly right in review"*, and a payout SETTLES on the rail's clock. It is answered by separating two questions the objection folds together: the trading calendar is never asked whether a settlement happened, only **which snapshot first sees it**, which is `last closed day` semantics already in force on every other figure of this response. **The attribution rule is what keeps the two apart and it is also what makes the arithmetic exact**: without it a 7-trading-day window spans 9 calendar dates and a 30-trading-day window about 42, and `9/42` is not `7/30`, so the scaling would carry a standing 8 percent bias in steady state. **The positive case is that payout SUPPLY is generated by gates counted in trading days** (minimum trading days, win days, the cadence gap) so no account becomes newly eligible over a weekend, **and the snapshot's own clock is already the last closed day, a trading day**, so a calendar window imposes a THIRD clock on a quantity that has two; the cost is measured rather than asserted, out of [M01:941](../plans/M01-rules-engine.md)'s own *"Five trading days is 7 calendar days in June and can be 9 to 10 across the year-end cluster"*: a genuine surge inside the cluster is MISSED and the first full week back PAGES on a calendar artifact. **WHERE THE NUMBER LIVES WAS PRICED FOUR WAYS AND ONE OF THE FOUR TURNED OUT NOT TO EXIST.** A column REFUSED, on [`0049`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql)'s own reason for storing no `breaker_armed`. **A config value an operator sets REFUSED loudest, because it is the option that looks most responsible**: `INV-M6-13` already refuses the console the exact analogue, *"an 'extend' button is the one control-shaped affordance that would delete the control"*, and a settable velocity window is that button on the alarm that watches the firm's own money leaving. **A launch candidate NOT AVAILABLE rather than declined**: the [parameter-status ruling](../decisions/gates/parameter-status-launch-candidates-versus-structural-rulings-founder-ruling-2026-08-14.md) scopes launch candidates to PLAN parameters *"because each is a row in `plan_version_sizes` rather than a constant in code"*, and this window is firm-scoped with no plan and no `plan_version` to pin it to. A structural ruling RULED, on the RCR breaker's `1.0` precedent. **AND THE SHAPE THAT RULING ASKS FOR IS ALREADY BUILT, WHICH IS THE FIND THAT MADE IT CONCRETE**: `apps/worker/src/breaker/ports.ts` declares `PolicyNumber` as `stated` or `unstated`, each carrying a `cite` and a `quote`, with a suite asserting the `unstated` ones stay unstated *"so filling one in is a red suite rather than a quiet commit"* -- **and before this entry every number in a `PAYOUT_VELOCITY_POLICY` would have had to be `unstated`, because there was no document to cite**. **ONE OF THE THREE THRESHOLD DOCUMENTS MOVED AND TWO DID NOT, EACH FOR A REASON THAT DOCUMENT STATES ABOUT ITSELF.** [M06:95](../plans/M06-admin-ops-console.md) grew **on the line it already occupied, adding no line**, because section 3.1's charter is *"the DEFINITION of each number, because an undefined number is what liability blindness actually looks like"* and because two `M06:95` citations in `apps/worker` are bound by `RI-15` to line numbers. INFRA section 8 is an alarm inventory whose every row is one terse trip point and none of which carries a definition; the constitution is read-only and this entry is its amendment. **TWO FINDINGS IN FILES THIS FENCE COULD NOT TOUCH, BOTH REPORTED AND NEITHER REPAIRED.** `LOSS_RATIO_POLICY`'s two `stated` numbers at [`ports.ts:320`](../../apps/worker/src/breaker/ports.ts) and `:326` both cite `M06:95 (P-M6-05)`, and **M06:95 is `P-M6-04`, this session's own row**; `P-M6-05` is `:96` and both quotes are taken from 96, so the quotes are right and the integers are one short. **And `RI-15` cannot see it**: its `CITATION_WINDOW` is **two** lines, tuned against declarations in source files where the widest true drift measured was one, and over a markdown TABLE whose consecutive lines are consecutive identifiers a two-line window spans **five** distinct panels, so any `M06:9n` citation into that table can name a panel two rows away and pass green. **THE EMPTY DENOMINATOR IS RULED AND THE GAP IT LEAVES IS NAMED RATHER THAN CLOSED.** `INV-M6-07`'s *"a ratio computed on three transactions is not a ratio"* binds circuit BREAKERS, and [GLOSSARY:257](../GLOSSARY.md) defines those as the controls that pause new sales and names exactly two, RCR and loss ratio; **payout velocity pauses nothing, it PAGES**, so the invariant does not reach it and `plan_breaker_state`'s `min_sample` and `insufficient_data` state have no row here. `0 / false` is ruled, and that it is indistinguishable on the wire from a quiet week is finding 3 rather than a field added in passing. **`B2` IS NOT FULLY CLEARED AND THE COUNT IS STATED HONESTLY**: the definition exists, the **four leaf paths still have no producer**, `readLiability` is still not composed, and what changed is that the evaluator is ordinary code in somebody's fence instead of a number nobody is allowed to invent -- session 377's shape on `B1`. **`B3` AND `B4` NAMED OUT OF SCOPE AND LEFT UNTOUCHED.** **NO MIGRATION NUMBER WAS RESERVED, NONE WAS NEEDED AND NONE WAS TAKEN.** **`apps/**` AND `packages/**` ARE NOT EDITED AT ALL**: two files were READ and both reads are reported as findings, no evaluator, no `PolicyNumber`, no constant, no type and no test is written, and the entry says so plainly rather than half-shipping one. **SIX SEEDS, THREE REDS AND THREE THAT DID NOT FIRE, AND THE THREE THAT DID NOT FIRE ARE THE VALUABLE ONES**, each restored from a byte copy with `git status --porcelain` empty after every one. RED: a link repointed at a file that does not exist (`CI-06a`), and the `What a founder read adds` block deleted from an unsigned entry (`RI-13`). **DID NOT FIRE**: this entry's `GLOSSARY:47` rewritten to `GLOSSARY:9`, which is finding 3a -- **`RI-16` checks a `file:line` pointer only when a BACKTICKED IDENTIFIER sits beside it, so every cross-document pointer in this entry is checked by nothing**, which is why each was re-derived by hand; the `ALLOCATION` row's `ADR-201` link replaced by plain text, since `CI-06w` reads the row's allocation KEY and no gate binds its prose; and **both directions of finding 2** -- correcting `ports.ts`'s two citations to `M06:96` leaves 17 of 17, and inserting a row that puts `P-M6-05` at `:97` ALSO leaves 17 of 17, so `RI-15` returns the identical verdict for the right citation and the wrong one. **That second measurement corrected this entry's own text**: growing the M06 line rather than inserting one is a real caution, but ONE inserted line would not have broken those pointers and a THIRD would, so `RI-15` was never the thing that would have caught it. 33 of 33 gates, **17 of 17 invariants**, no ADR number beyond `201`, no `SD-nn`, no `GS-nnn`, no `SystemReason` or `SqlExecutorReason` member, no `pg` import, and every merged migration byte for byte unchanged. |
| [2026-08-28 - Session 382](2026-08-28-session-382.md) | **THE TWO ROWS ARE NOT ONE PROBLEM AND THE DISPATCH TREATED THEM AS ONE. ONE LANDS, THE OTHER CANNOT BE TRANSCRIBED FROM THE DOCUMENT AT ALL.** Both line numbers and both spellings were re-derived before a character was copied and both held: [`EVENTS:354`](../architecture/EVENTS.md) is `flag.raised` and `:358` is `detector.run_completed`, neither was a row in the producer's catalogue, and `buildEvent` was refusing both on `ADR-159` clause 1 exactly as designed. **`flag.raised` IS TRANSCRIBED AND IT WRITES.** `subjectKind` `risk_flag` (the singular of `risk_flags`, the spelling `admin-writes.ts` already puts on `admin_actions`), `subjectField` `flag_id`, both tenancy legs, `actorKind` `system` because `ACTOR_KINDS` is closed by `0017`'s CHECK and carries no `detector` member. **`detector.run_completed` CANNOT BECOME A ROW AND THAT IS THE SESSION'S FINDING RATHER THAN ITS SECOND HALF LEFT UNDONE.** `EVENTS:358` gives its payload as `{ detector, detector_version, trading_day, rows_scanned, flags_raised, duration_ms }` and **THAT LIST CARRIES NO `_id` FIELD AT ALL**, while `events.subject_id` is `uuid NOT NULL` and `CatalogueRow.subjectField` names the payload field it is read from; naming one here would be the producer inventing a field the registry does not carry, which is the same move as the omission that made this session necessary. **A SECOND AND INDEPENDENT BLOCKER SITS BEHIND IT**: the same payload names neither tenancy column, so even given a subject `assertTenanted` refuses it, for `payout.freeze_expiring`'s reason; its consumers are BI and ALERT, so the fact is FIRM-level and the repair is the one `assertTenanted`'s message already names, a row in EVENTS saying so. Both repairs are `EVENTS`' and `EVENTS` is frozen, so each is an ADR. **THE SUBSTRATE FOR THE FIRST REPAIR EXISTS AND IS NAMED, WHICH IS NOT PERMISSION TO TAKE IT**: `detector_runs.id` is a `uuid` primary key and the runner already holds it as `runId` one line above the emit, so an amendment adding `detector_run_id` to that payload has a real column to point at. **ONE PREMISE THE DISPATCH CARRIED FORWARD IS REFUTED**: session 379 wrote that `flag.raised` "also needs the `evidence_summary` definition session 300 registered as owed". It does not. `CatalogueRow` names a subject field, two tenancy fields and an actor, `evidence_summary` is none of them, and the producer never reads it; session 300's registration is still open and is not on the critical path of this row. **THE EVIDENCE IS ROWS IN A DATABASE.** PostgreSQL 16, all **59** migrations under `ON_ERROR_STOP=1`, **114** tables, `events` empty before the run, and `makeEventSink({ writer: TRANSACTION_EVENT_WRITER, clock })` over a real `SystemTx` from `transaction(systemDb('nightly-batch'))`: **TWO REAL `flag.raised` ROWS**, one under both tenancy legs and one under the identity leg alone with `account_id` NULL, `subject_kind` `risk_flag`, `actor_kind` `system`, `actor_id` NULL, `schema_version` 1, `occurred_at` the injected clock and `recorded_at` the database's, **4 hours 7 minutes apart in the direction that proves they are two clocks**. **THE ACCEPTANCE CASE WAS WATCHED FIRING AND IT IS THE HALF A PROBE OF REFUSALS NEVER SEES**: a TRADER-SCOPED `scopedDb(identity).rows('events')` reaches **BOTH** rows and another identity reaches **ZERO**, which is `ADR-191`'s `either` disjunction working on the identity leg for the row that has no account, and is exactly what `payout.freeze_expiring` cannot do. `events_identity_id_fkey` refused an identity naming no row, so the column is a reference and not a string. `detector.run_completed` and `detector.run_degraded` were then run through the same sink against the same database and **both threw `EventError` at the NAME**, and an untenanted `flag.raised` threw `assertTenanted`'s message. **NEITHER ROW CARRIES MONEY AND THIS WAS CHECKED RATHER THAN ASSUMED**, so `ADR-198` does not reach either: no `_cents` and no `_bp` field on either payload, `encodeCentsForStorage` is a no-op over the one that is a row, and session 379's measurement over the worker's seven holds for these two. **ONE EDIT LANDED OUTSIDE THE FENCE AND IT IS THE CONTROL FIRING RATHER THAN THE SLICE WIDENING.** `apps/worker/test/event-sink.test.ts` section 4 parses the producer's catalogue as text and session 379 wrote it to go red on exactly this event: "it goes red the day a catalogue row lands, which is the day the count needs re-deriving." It did, on the first commit. The three figures are re-derived rather than adjusted -- parse 8 to **9**, `accepted` 3 to **4**, `refused` 4 to **3** -- in a commit of its own, on PR #503's precedent that clearing-condition cases are inverted in the diff that clears them. **No `src/` file in `apps/worker` is touched and the blocked-slice measurement is untouched.** The seven are now **2 write / 1 accepted-and-untested-here / 3 refused by name / 1 refused by tenancy**, and the three refusals are one kind of gap where they were two: every remaining name needs an amendment to a frozen document. **EIGHT SEEDS, EIGHT REDS**, tree clean after every one, the valuable one being the eighth: `EVENTS:358` seeded with a `detector_run_id` turns the clearing-condition case red, so the pin is a control and not a comment. **NO ADR NUMBER, NO MIGRATION NUMBER, NO `SD-nn`, no `SystemReason` or `SqlExecutorReason` member, no `pg` import, no cast past a key type, no door widened and no gate weakened. `docs/architecture/EVENTS.md` is byte for byte unchanged.** 33 of 33 gates, **17 of 17** invariants, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` read as OUTPUT, and **239 files / 5,868 passed / 6 skipped** against a `main` baseline at `4bd102e` of **239 / 5,856 / 6** reproduced exactly before a line changed. The delta is **+12**: the 8 cases this session wrote plus the 4 that four `test.each(EVENT_NAMES)` blocks add when the record gains its ninth key. |
| [2026-08-28 - Session 383](2026-08-28-session-383.md) | **THE ACCEPTANCE PROPERTY WAS NOT "IT COMPILES" AND THE RESULT IS A MEASUREMENT RATHER THAN A CLAIM.** [ADR-201](../decisions/ADR-201.md) exists because two of the three readings of `avg_30d_cents` are not controls at all, so this session computed **BOTH REFUTED READINGS ON THE SAME ROWS** and asserted them. Over one steady state: ruling 2's reading gives **10000 bp and is SILENT**, the daily mean gives **70000 bp and PAGES**, and the 30-day total gives **2333 bp** and is bounded by 10000 on every history the suite can build. The entry's proof is arithmetic; this is that arithmetic run against a database. **EXECUTED AGAINST A LIVE POSTGRESQL 16 WITH ALL 59 MIGRATIONS AND 114 TABLES**, through `transaction(systemDb('operator-console'))` and the real keyed accessor over `payout_transfers`, `trading_calendar` and `trading_calendar_loads`: steady state **10000 bp, no alarm**; a 5x week **25862 bp and the pager FIRES**; exactly **25000 bp SILENT** and **25001 bp LOUD** on the same 30-day total with one dollar moved across the numerator edge; an empty denominator **0 and false**; a `failed` transfer carrying a `settled_at` **not counted**; an exhausted calendar and an uncovered one each **saying so** with `transferRowsScanned` at zero. The unit fixture is the live run's calendar, so every figure a case asserts is a figure a database produced. **THE SEVEN-TRADING-DAY HORIZON WAS REUSED RATHER THAN RE-DERIVED, AND THE REUSE IS BY IDENTITY.** Session 380's `readTradingHorizon` walks FORWARD; `ADR-201` ruling 3 needs a TRAILING window, and a second transcription of "which day is the snapshot's" would be two answers to one question inside one module -- `B2`'s own shape one level down. So `anchorCalendar` is EXTRACTED unchanged, both walks share the last-closed-day rule, the coverage lookup and all three `uncovered` answers, and `readTradingLookback` differs in exactly three ruled ways: **inclusive of the anchor** where the horizon is strictly after it, bounded by `covering.from` where the horizon is bounded by `covering.to`, and `exhausted` as a VERDICT rather than a short array, because a short forward horizon understates a forecast while a short trailing window understates the **DENOMINATOR** of a ratio. **ONE SEED STAYED GREEN AND IT IS THE MOST VALUABLE RESULT IN THE SESSION.** Rewriting attribution to take the LAST trading day ON OR BEFORE a settlement -- the exact opposite of ruling 4 -- left the whole suite passing, because in every fixture both directions landed inside the same window and **the TOTALS are identical**. A case asserting a sum asserts that SOMETHING attributed the row, never that ruling 4 did; this is [session 380](2026-08-28-session-380.md)'s seed 4 finding arriving on a different rule. The discriminating fixture puts a weekend settlement across the NUMERATOR edge: anchored at `2026-11-24` the seven-day window opens on Monday `2026-11-16`, so a Saturday `2026-11-14` settlement attributes FORWARD to the 16th and is inside the numerator, while the backward reading puts it on Friday the 13th, inside the thirty and outside the seven. The 30-day total is `3210000` either way and `last_7d_cents` is **910000 forward against 700000 backward**. Re-seeded, it goes red. **THREE THINGS WERE FOUND RATHER THAN TRANSCRIBED.** **1. `payout_transfers_settled_has_timestamp` IS ONE-DIRECTIONAL.** `CHECK (status <> 'settled' OR settled_at IS NOT NULL)` forbids a settled row with no instant and **PERMITS a failed or retrying row that carries one**, so selecting on `settled_at IS NOT NULL` would count money the rail never moved and OVERSTATE the numerator of a pager. Selection is on `status`, which the CHECK makes safe in the other direction, and the permitted rows are counted and reported. Watched live: a `failed` transfer of `90000000` cents inside the numerator window moved nothing and appeared as `settledInstantsOnUnsettledRows: 1`. **2. RULING 4 FORCES A THIRTY-FIRST DAY TO BE READ.** A settlement attributes to the window's OLDEST day only if its date is also strictly after the trading day BEFORE it; without that bound "the first trading day on or after" a date in 2019 is, for a walk that can see thirty days, the first day it can see, so every settlement in recorded history lands on the oldest window day. The boundary is a READ FACT and never an assumption, and its cost is that the control needs 31 trading days inside one covered interval rather than 30. **3. RULING 6's ARM IS REACHABLE WITH A NON-ZERO NUMERATOR.** Section 7 reasons about "no payout settled in the trailing 30 trading days" and RULES on the VALUE, and the two differ for a 30-day total under **five cents**, where `floor(total * 7 / 30)` is 0 beside a numerator that is not. The transcription follows the ruling's own words and the case is pinned so a later reader finds it rather than discovers it. **THE THRESHOLD WAS RE-DERIVED AT FOUR SOURCES AND ALL FOUR ARE STRICT**: [MERIT_BUILD_MASTER_PROMPT:166](../../MERIT_BUILD_MASTER_PROMPT.md) *"alarm >2.5"*, [M06:95](../plans/M06-admin-ops-console.md) *"alarming above 2.5x"*, [INFRA:160](../architecture/INFRA.md) *"Over 2.5 times the 30 day average pages"*, [ADVERSARY_DOSSIER:49](../../research/ADVERSARY_DOSSIER.md) *"trips the payout-velocity alarm (>2.5"*. Not one says "at or above", so `alarm` is `>` and exactly 25000 does not page, and a case reads all four files rather than restating the number. **RULING 8 IS ENFORCED MECHANICALLY RATHER THAN PROMISED.** Every number lives in `PAYOUT_VELOCITY_POLICY`, in `apps/worker/src/breaker/ports.ts`'s `PolicyNumber` shape, all four `stated` because `ADR-201` supplied the cite and the quote that made them so. A case strips comments and string literals in ONE pass over all three quote characters and asserts that `7`, `30`, `25000` and `10000` appear NOWHERE in the module's code outside that object. **AND `ELIGIBLE_HORIZON_TRADING_DAYS` IS DELIBERATELY NOT REUSED FOR THE NUMERATOR, WHICH IS THE TRAP RULING 8 LOOKS LIKE IT ASKS FOR**: `liability.ts` already exports a `7`, it is `EC-074`'s FORWARD horizon over accounts that will become eligible, and this one is `ADR-201`'s TRAILING window over money that has already left. A constant shared between two independent quantities that happen to be equal today would move this pager's window the day `EC-074` moved its forecast, silently and with no ADR, deleting exactly the property ruling 7 creates. A case asserts the import does not name it. **MONEY IS INTEGER CENTS AND THE RATIO INTRODUCES NO FLOAT, SAID PRECISELY.** Every term is `bigint`: `amount_cents` is `bigint NOT NULL` in `0010`, `total * 7` is formed BEFORE the division on ruling 5's own ordering, and bigint `/` TRUNCATES where the ruling says `floor`. **The two agree only on non-negative operands**, so `0010`'s `CHECK (amount_cents > 0)` is re-asserted per row and the divide refuses a negative outright, which is what makes the word `floor` true of this code rather than nearly true. A pinned case names a 30-day total where `Math.floor(total * 7 / 30)` in IEEE doubles is **ONE CENT HIGHER** than the exact answer. **`readLiability` STAYS UNCOMPOSED AND THE FOUR LEAVES STAY BLOCKED, AND THE REASON MOVED RATHER THAN LIFTED.** Not because the numbers are unavailable -- every case produces them -- but because the evaluator answers THREE ways and `LiabilityResponse` carries ONE: `ratio_bp` is a non-nullable number and `alarm` a non-nullable boolean, so an uncovered calendar would render `0 / false`, indistinguishable from a quiet week. That is `ADR-201` finding 3's gap with a pager attached, and session 374's judgement that composing against a source it cannot fully serve turns an honest 500 into a wrong answer. `B3`, `B4` and `B5` untouched. **EIGHTEEN SEEDS**, each applied to the committed shape and restored from a byte copy with `git status --porcelain` empty after every one: **17 red on first application and one green**, the green one being the attribution direction above, which goes red after the case it exposed. Counts derived at reporting time: **33 of 33** gates, **17 of 17** invariants, typecheck 0, lint 0, `format:check` clean, `falsify.mjs` read as OUTPUT and the tree clean after it, **59** migrations and **114** live tables, `IMPLEMENTED_ADMIN_READS` still **5** of `AdminReadSource`'s **7** with `readLiability` and `exportEvidence` uncomposed, and **240 files / 5,925 passed / 6 skipped** against a baseline of **239 / 5,894 / 6** reproduced before a line changed. The delta is **+1 file and +31 cases**, which is exactly the 31 written; the two cases repaired in `admin-source-liability.test.ts` were inverted in place and add none. **No ADR number, no migration number, no `SD-nn`, no `SystemReason` or `SqlExecutorReason` member, no `pg` import, no cast past a key type, no ledger account named, no join and no aggregate, every merged migration byte for byte unchanged, and `packages/**`, `apps/worker/**` and `apps/admin/**` not opened.** |
<!--/gen-->
