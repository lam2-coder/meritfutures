---
status: approved
depends_on: [README.md]
last_updated: 2026-08-22
---

## 2026-08-22 - Session 139: three repairs owed by landed sessions, and `writable` empties

- **Objective:** repair section 39's stale `plan-validate.test.ts` citations, move `GS-071` off `writable` on an assertion that now EXECUTES, and re-quote [ADR-077](../decisions/ADR-077.md)'s struck clause in the fixture README.
- **Done.** All three. **`writable` holds zero rows** and `covered-elsewhere` holds seven. Every citation was re-derived by grep against the file rather than by arithmetic on the old number.
- **The count:** `node scripts/corpus/gates.mjs check` **28 of 28**, `CI-06/fixture-inventory` reporting `43 written, 0 writable, 266 blocked, 7 covered-elsewhere` and **7 covered-elsewhere rows read for EXECUTION**, `node scripts/corpus/falsify.mjs` green over 55 scope cases and 10 loader cases, and `pnpm run verify` clean end to end.
- **Next:** three things are named below under **Reported, not touched**, and none is this fence's.
- **Blockers:** none.
- **Files touched:** [section 39](../testing/golden-scenarios/39-fixture-status-and-blockers.md), [`fixtures/README.md`](../../packages/rules-engine/fixtures/README.md), [`falsify.mjs`](../../scripts/corpus/falsify.mjs), this file, [README](README.md) and one appended section in [STATE](../STATE.md). **[`gates.mjs`](../../scripts/corpus/gates.mjs) was in the fence and needed no edit**, which is the next heading.

### The inventory assertion did not have to move, and that was the fence's question

The fence held [`gates.mjs`](../../scripts/corpus/gates.mjs) deliberately, on the memory of session 127 being fenced out of exactly this file. **It was not needed.** `covered-elsewhere` entered `FIXTURE_STATUSES` in [session 131](2026-08-22-session-131.md); both summary tables are derived from the rows and compared term by term; assertion 4 already runs on `else if (r.blocker !== '')`, so a `covered-elsewhere` row with an empty blocker passes without a special case; and assertion 6 resolves `apps/worker/test/replay.test.ts` through `CITED_TEST_FILE`, finds `GS-071` named outside any disabled construct, and passes. **A vocabulary term with a count of zero is not a special case for this runner**, because `fixtureSummaryCounts` requires a summary row for every declared term and compares its number, whatever the number is.

### `falsify.mjs` DID have to move, and it was outside the fence

**Emptying `writable` starved a falsification seed, and the stop condition names `falsify.mjs` green.** `CI-06/fixture-inventory/writable-with-a-blocker` picked a row that already read `writable` and gave it a blocker. `GS-071` was the last such row, so after this edit the predicate matched nothing and the case reported `SEED IS STALE`. **That is the harness reporting correctly and it is still a case asserting nothing**, which is the whole property `falsify.mjs` exists to prevent.

**The seed now CREATES its anchor**, which is the move `register-names-no-defect` one entry above it made when session 131 emptied the register, for the same reason and recorded in its own comment as *"an anchor that cannot go stale because the seed creates it"*. A `blocked` row is moved to `writable` with its blocker left in place: the status means every [ADR-072](../decisions/ADR-072.md) condition holds and the row still names one that fails, which is assertion 4's contradiction on the branch the old seed reached. The summary is bumped in the same edit through the existing `bumpFixtureSummary`, because a seed that moves a row and leaves the counts alone fires assertion 5 as a second finding it did not aim at. **A `blocked` row and not a `written` one, deliberately**: a `written` row has its fixture on disk and moving it would fire assertion 2 as well.

**The change is one seed and its comment. No gate moved, no assertion moved, and the case is watched failing with exactly the finding it names.**

### `GS-071`: the status was taken by executing the citation, not by reading it

`GS-071` reads `covered-elsewhere` on **both** conditions section 39 states, checked separately.

| | |
|---|---|
| **no golden fixture** | no `GS-071-*.yaml` in [`packages/rules-engine/fixtures`](../../packages/rules-engine/fixtures/README.md); the directory is 43 yaml over 43 expectations over 43 distinct ids |
| **a named suite EXECUTES it** | the `describe` at [`replay.test.ts:1076`](../../apps/worker/test/replay.test.ts), which names the row in its own header, and the `it` at `:1077` |

**The second was executed and not read**, which is the distinction [session 123](2026-08-21-session-123.md) found on `GS-072` and [session 131](2026-08-22-session-131.md) built the gate assertion for. `pnpm exec vitest run apps/worker/test/replay.test.ts` reports **62 passed and 0 skipped**, and the file carries no `skipIf`, no `.skip` and no `.only` anywhere. `GS-072`'s failure mode is a named skip that leaves every grep in the tree passing; a passing grep was not accepted as the evidence here.

**The row was `writable` for a scale and not a shape.** It named `apps/worker/test` as the place its assertion could be written while the suite there asserted four days and the row asks 250. [Session 129](2026-08-22-session-129.md) wrote it at 250 as WAVE-05 `X5`, and [ADR-076 section 1](../decisions/ADR-076.md)'s governing rule finished the move: a row is discharged when its assertion is EXECUTED, wherever it runs.

**`writable` is not retired and its vocabulary does not move.** It is a term with a count of zero, the summary row stays, and the next scenario whose assertion is writable and unwritten reads `writable` again. Adding or removing a status word is a different change and was not this session's.

### Every `plan-validate.test.ts` citation was re-derived by grep

[Session 128](2026-08-22-session-128.md) landed WAVE-05 `X3` and `X4` in that file and moved twelve of the thirteen distinct line numbers section 39 cites into it. **Not one was recomputed by adding an offset to the old number.**

| What the cell points at | Whose row cites it | Was | Is |
|---|---|---|---|
| `RE-C-10`, the `it` | GS-076 | 373 | **405** |
| `SEEDS['CV-10']` | GS-076 | 757 | **789** |
| the `GS-076` comment | GS-076 | 89 | **91** |
| `RE-C-06`, the `it` | GS-077 | 282 | **314** |
| `SEEDS['CV-06']` | GS-077 | 748 | **780** |
| the `GS-077` comment | GS-077 | 82 | **83** |
| `RE-C-01`, the `it` | GS-078 | 208 | **240** |
| `SEEDS['CV-01']` | GS-078 | 733 | **765** |
| the `GS-078` comment | GS-078 | 74 | **74**, the one that did not move |
| the oracle `it.each` | GS-076 to GS-078 | 801 | **833** |
| the `GS-083` `describe` | GS-083 | 905 | **964** |
| the three-plan `it.each` | GS-141 | 166 | **169** |
| the `GS-141` comment | GS-141 | 154 | **157** |

**Every other line citation in section 39 was checked and none had moved**: `determinism.test.ts:232` and `:245`, `floor-monotonicity.property.test.ts:259`, `win-days-monotonicity.property.test.ts:153`, and `GS-072`'s `replay-determinism.property.test.ts:231` and `:76`. Session 128's diff is the whole of the drift.

**Two claims in those cells were stale in substance and not only in position**, which a pure line-number repair would have left standing.

- **`GS-141` no longer reads `EXECUTED IN PART`.** The cell said the row's last sentence *"is unasserted and is WAVE-05 `X3`"*. `X3` landed: the `it` at `:190` pins both severities and `textsDiffer` in one `expect`. The cell now records the residual as closed rather than still claiming it.
- **`GS-076` to `GS-078` gained the `X4` strengthening**, the `it.each` at `:859` that pins each seed's error set EXACTLY rather than by containment. It was in the file and in no cell.

### The fixture README's third recitation

[ADR-077](../decisions/ADR-077.md) section 8 recorded that [`fixtures/README.md`](../../packages/rules-engine/fixtures/README.md) recites the falsified *"23 hour and 25 hour"* clause a third time and could not repair it, because `packages/` was outside [session 130](2026-08-22-session-130.md)'s fence. **Only the quotation moved**, exactly as that entry said it should: the surrounding sentence, *"is a claim about the ingest boundary, not about the fold"*, survives the repair and is correct, and the row's disposition does not change.

**The replacement is `GS-030`'s wording and not `EC-012`'s**, and that is a second repair inside the first. ADR-077 section 1's opening finding is that this line *"quotes `EC-012`'s wording while citing `GS-030`'s row"*, so the corpus was carrying two different obligations under one quotation: `GS-030` obligates a mark and `EC-012` does not. The line's row is `GS-030`. Quoting `EC-012`'s amended text would have carried the mis-attribution forward in corrected form.

### Reported, not touched

- **[`gates.mjs`](../../scripts/corpus/gates.mjs) says *"the two `writable` rows"* twice**, in the assertion-4 comment at `:4813` and in the gate's own `covers` string at `:5090`. **Both were already stale before this session** (the count was one, not two) and are now zero. Neither is an assertion and neither can fail; the fence admitted this file only if the inventory assertion had to move, and it did not, so the file is untouched. It is a two-word repair.
- **Section 39's `GS-030` blocker cell is the FOURTH recitation of ADR-077's struck clause**, and it is still there: *"so the 23 hour and 25 hour halves are not statable"*. It is [session 127](2026-08-22-session-127.md)'s by ADR-077 section 8, **and it owes more than a re-quote**: that entry records both halves of the cell as wrong, since the count half IS stated in [`trading-calendar-generator.test.ts`](../../packages/db/test/trading-calendar-generator.test.ts) and the duration half is not unstatable but false. Re-quoting it here would have looked like the repair and been a third of it.
- **[WAVE-04 section 2](../plans/WAVE-04-fixture-backlog-and-gate-inventory.md) now disagrees with the table at 16 writable against zero**, where it disagreed at 16 against one. Section 39 already rules that the table is the answer and the plan is the error, and the plan is out of this fence.
