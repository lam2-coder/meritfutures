---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 239: `CI-09`'s Stryker leg, a DELEGATED `VG-12` grant, and a threshold expressed by not writing one (not money)

<!--index: `CI-09`'s third leg of four, which had waited since 2026-08-20 on an artifact that was never a file: "the `VG-12` admission, which a session cannot grant itself". **The founder DELEGATED the grant and [ADR-127](../decisions/ADR-127.md) RECORDS the delegation** rather than proceeding as though the gate did not apply, which is [ADR-116](../decisions/ADR-116.md)'s shape one leg over and the only form the clause survives. **THE ENTRY'S CENTRAL RULING IS A BLOCK THAT IS NOT WRITTEN**: [ADR-073](../decisions/ADR-073.md) section 6 measured seven days ago that a Stryker score threshold spelled in `.mjs` fires `RI-02` at `CI-01` and the same values in JSON do not, and said neither outcome is a ruling anybody made; both the JSON spelling and an edit to `RI-02`'s needle are REFUSED, and writing no such option at all is the literal form of STRATEGY's *"reported as a trend rather than a threshold"*. **The default that ruling now rests on is asserted out of the installed tool's own shipped schema and throws before the run starts.** **THIS ROW COULD NEVER HAVE SPENT A DAY AS A FINDING** the way the replay leg one sentence up did, because an unprobeable artifact arrives silently: the register shrinking is the only observable event there ever was, and it fired at 29 of 32 before it was repaired -->

- **Objective:** land `CI-09`'s Stryker leg on the founder's delegated `VG-12` grant, record the delegation in `ADR-127` rather than assume it, and establish the threshold and the scope from [STRATEGY](../testing/STRATEGY.md) rather than from Stryker's defaults.
- **Done:** **[ADR-127](../decisions/ADR-127.md), `status: proposed`, UNSIGNED, six rulings.** `@stryker-mutator/core@10.0.0` and `@stryker-mutator/vitest-runner@10.0.0` admitted under the delegated grant, declared on `packages/rules-engine` and no other manifest; [`stryker.conf.mjs`](../../packages/rules-engine/stryker.conf.mjs) and [`vitest.config.ts`](../../packages/rules-engine/vitest.config.ts) beside it; the `stryker-mutation` job in [`nightly.yml`](../../.github/workflows/nightly.yml); STRATEGY's `CI-09` row moved; one `UNPROBEABLE_ARTIFACTS` entry retired. **THE RUN WAS EXECUTED**: 1921 mutants over 19 source files, 28 minutes 31 seconds, score **80.90%**. **32 of 32 gates, 10 of 10 invariants, `pnpm run verify` exit 0.**
- **Next:** **the fourth leg.** `CI-09`'s detector canary still waits on [M07](../plans/M07-risk-abuse.md)'s detector code, and `canary` appears in no `.ts` file in this tree. Then the four things `ADR-127` section 8 rows: `VG-12`'s branch-protection action, `OQ-TS-02`'s second half, `gates.mjs`'s `walk` reading gitignored directories, and a `CI-09` falsification case for this leg.
- **Blockers:** **`ADR-127` is UNSIGNED.** Two things in this diff want a founder decision rather than a session's and both are named in the entry: **the admission costs `+144` packages**, the largest any `VG-12` admission in this corpus has carried, and **one `pnpm.overrides` entry overrules an upstream EXACT pin** to clear a moderate advisory that turned `CI-05` red. **`OQ-TS-02` now has a deadline**: the day the score becomes a gate, `RI-02` and this config collide and only the founder can say which moves.
- **Landmines:** **`scripts/demo/seed-world.ts` is still not the replay runner** and this session did not touch that job. **`gates.mjs`'s `walk` reads directories `.gitignore` names**, so a crashed mutation run leaves `pnpm run verify` red over copies of the tree; the sandbox is configured into `node_modules/` to route around it and the fix is a gate edit nobody has taken. **`stryker.conf.mjs` must never acquire a score threshold block** without `RI-02` moving first; the file says so at length and the assertion at its head is the mechanical half. **And its sandbox location must not be "tidied"**: `node_modules/.stryker-tmp` and an absolute `os.tmpdir()` path were both tried and both break the run, for two different reasons, and the config records both.
- **Files touched:** [`docs/decisions/ADR-127.md`](../decisions/ADR-127.md) (new), [`.github/workflows/nightly.yml`](../../.github/workflows/nightly.yml), [`packages/rules-engine/stryker.conf.mjs`](../../packages/rules-engine/stryker.conf.mjs) (new), [`packages/rules-engine/vitest.config.ts`](../../packages/rules-engine/vitest.config.ts) (new), [`packages/rules-engine/package.json`](../../packages/rules-engine/package.json), [`pnpm-workspace.yaml`](../../pnpm-workspace.yaml), [`pnpm-lock.yaml`](../../pnpm-lock.yaml), [`package.json`](../../package.json), [`.gitignore`](../../.gitignore), [`scripts/corpus/gates.mjs`](../../scripts/corpus/gates.mjs), [`docs/testing/STRATEGY.md`](../testing/STRATEGY.md) (the `CI-09` row only), [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md), [`docs/decisions/README.md`](../decisions/README.md), [`docs/INDEX.md`](../INDEX.md), [`docs/sessions/README.md`](README.md), this file. **[`docs/STATE.md`](../STATE.md) IS NOT TOUCHED AND THAT IS THE FENCE RATHER THAN AN OMISSION**: the dispatch enumerated this session's files and STATE was not among them, which reads as deliberate in a wave where fourteen sessions would each be appending to the same document. The end ritual's STATE update is therefore OWED and is reported here rather than taken.

---

### 1. What was read, and the five facts the work rests on

| # | Fact | Source |
|---|---|---|
| 1 | Mutation testing is ruled **on `packages/rules-engine` only**, and **"reported as a trend rather than a threshold"** | [STRATEGY section 2](../testing/STRATEGY.md), the Mutation testing row |
| 2 | `OQ-TS-02` proposes **trend only** and gives the reason: a threshold on a young codebase produces tests written to kill mutants rather than to pin behaviour, which is `TR-01` inverted | [STRATEGY:467](../testing/STRATEGY.md) |
| 3 | Stryker is **an activation condition and not a purchase**; a session that installed it would be granting itself the admission the gate exists to withhold | [ADR-073](../decisions/ADR-073.md) section 6 |
| 4 | A Stryker score threshold **in `.mjs` fires `RI-02`** and the same values **in JSON do not**, and neither outcome is a ruling anybody made | [ADR-073](../decisions/ADR-073.md) section 6, reproduced on this branch |
| 5 | The artifact this leg waited on is **unprobeable by construction**: *"a job can see the dependency surface changed and cannot see that a human agreed"* | `UNPROBEABLE_ARTIFACTS` in [`gates.mjs`](../../scripts/corpus/gates.mjs), quoting session 23 |

**Facts 4 and 5 are the two the work turned on**, and neither was in the dispatch. The first decided how the ruling is expressed; the second decided which direction of `CI-06/gate-inventory` this leg would ever be visible to.

---

### 2. The admission, and the two things in it a founder has to weigh

**THE GRANT WAS DELEGATED AND THE ENTRY RECORDS IT.** That is the whole of section 1 of [ADR-127](../decisions/ADR-127.md) and it is not a formality: an admission recorded as though the gate did not apply and an admission recorded as delegated are identical in the lockfile and opposite in the record.

**FIRST: THE COST IS `+144` PACKAGES AND NOT ONE OF THEM IS PER-PLATFORM.** 283 to 427 `packages:` keys. [ADR-116](../decisions/ADR-116.md) measured Playwright at `+4`, one of them `darwin`-only; [session 183](2026-08-24-session-183.md) measured Next.js at `+51`, 36 of them per platform. Here the two figures do not diverge at all: every one of the 144 installs on every host. **37 are `@babel/*`**, which is the instrumenter's parser, and **16 are `@inquirer/*`**, which is an init wizard this repository will never run and which is a direct dependency of the core rather than an optional one. This is the largest cost-per-consumer any `VG-12` admission in this corpus has carried and the entry says so at that framing rather than as a package list.

**SECOND: IT FAILED `CI-05` BEFORE IT PASSED ANYTHING.** `@stryker-mutator/core@10.0.0` depends on `typed-rest-client@2.3.1`, which pins `qs` **exactly** at `6.15.1`, and `GHSA-q8mj-m7cp-5q26` rates `>=6.11.1 <=6.15.1` **moderate** against a `security-static` job running `pnpm audit --audit-level=moderate`. On the install this branch admits, `pnpm audit` reported `1 vulnerabilities found. Severity: 1 moderate`.

**THE REPAIR REMOVES THE PACKAGE RATHER THAN SILENCING THE CHECK**, which is the distinction session 232 drew when it refused a `.gitleaksignore` fingerprint: `--audit-level=high` or an audit exclusion buys green by keeping the thing that should not be there. One path-scoped `pnpm.overrides` entry, `typed-rest-client>qs` at `6.15.3`, the advisory's own patched range. **It is called out as a SECOND supply-chain decision and not as a detail of the first**, because an override is a version nobody upstream chose and overruling an exact pin is exactly the class `VG-12` exists to put in front of a human.

---

### 3. The ruling that is a block NOT written, which is the part worth reading

**[ADR-073](../decisions/ADR-073.md) SECTION 6 SAW THIS COMING SEVEN DAYS AGO AND LEFT THE QUESTION WITH THE FOUNDER.** `RI-02` asserts that no coverage gate exists anywhere in the tree, and one of its needles reads a bare object key followed by a colon. Reproduced on this branch rather than quoted:

> `FAIL   RI-02  No coverage threshold exists anywhere in the tree  (1)`
> `packages/rules-engine/stryker.conf.mjs: reads as a Vitest coverage thresholds block`

**TWO MOVES WERE AVAILABLE AND BOTH ARE REFUSED.**

| Move | Why not |
|---|---|
| Write the config in **JSON** | The quote between the key and the colon defeats the needle. That keeps the value and hides it from the gate, which is the `.gitleaksignore` shape session 232 refused |
| **Narrow `RI-02`'s needle** | ADR-073 handed the founder exactly that question: whether `RI-02`'s vocabulary should reach a mutation threshold at all, given that its argument is about line coverage and mutation score is the one metric generated tests do not inflate for free. A session editing the needle answers `OQ-TS-02`'s second half on its own authority, one file over from the gate it is quietening |

**SO THE THIRD MOVE IS THE ONE TAKEN AND IT IS NOT A DODGE: THERE IS NO SUCH OPTION IN THE FILE, BECAUSE THERE IS NO THRESHOLD.** STRATEGY ruled the score *"reported as a trend rather than a threshold"*. A config carrying no such block is the literal form of that, and `RI-02` then passes because there is genuinely nothing to find. **The difference between this and the JSON file is the difference between not having one and having one the gate cannot see.**

**WHAT IT COSTS IS PAID RATHER THAN ABSORBED.** The property now rests on Stryker's **default**, which is `null`, rather than on a line here. So the default is read out of the shipped JSON schema of the installed `@stryker-mutator/core` and the config **throws before the run starts** if it is anything else. That is a check on every nightly and every local invocation, and it is the corpus's own standing preference: prefer a mechanical assertion over a sentence whenever the error is checkable.

---

### 4. What the leg fails on, watched rather than argued

**THE SCORE FAILS NOTHING AND THE LEG IS STILL A CONTROL.** Three things turn it red.

| # | Failure | Watched |
|---|---|---|
| 1 | The initial test run failing | Stryker runs the suite unmutated first; a red baseline stops the run |
| 2 | The runner crashing | Observed for real: under `.npmrc`'s `node-linker=isolated` the default plugin glob loaded the core and found **no test runner**, and the run died on `Cannot find TestRunner plugin "vitest"` and exit `1`. `plugins` is written out because of it |
| 3 | **ZERO MUTANTS** | **Watched firing before the job was written.** `--mutate 'src/nonexistent-file.ts'` gives `Instrumented 0 source file(s) with 0 mutant(s)`, then `No tests were executed. Stryker will exit prematurely`, exit **`1`** |

**THE THIRD IS [`nightly.yml`](../../.github/workflows/nightly.yml)'s OWN LESSON IN A DIFFERENT COSTUME.** The `replay-self-audit` job carries a runner note precisely because `scripts/demo/seed-world.ts` exits `0` writing **zero bytes**, and its sentence is the one this leg inherits: a green check over zero output is worse than a red one. A `mutate` glob that matched nothing would be exactly that.

---

### 5. The run, executed

| | |
|---|---|
| Source files mutated | **19** of 162 in the package |
| Mutants | **1921** |
| Wall clock, 4-core host | **28 minutes 31 seconds** |
| **Mutation score** | **80.90%** |
| Killed / timed out | **1551** / **3** |
| Survived / no coverage | **307** / **60** |
| Errors | **0** |

**THE SUITE THAT DOES THE KILLING IS THE PACKAGE'S WHOLE SUITE AND NOT ONE CI STAGE**, which is why [`packages/rules-engine/vitest.config.ts`](../../packages/rules-engine/vitest.config.ts) exists at all. The root config splits tests into projects because **the projects map to CI stages** and `CI-02` and `CI-03` block independently; a mutant is killed by whichever test notices, so mutating against one project reports a mutant as surviving because the file that kills it is in the other. **A false survivor is the worst possible row in a report whose entire content is survivors.**

**NOT ONE LINE UNDER `src/` OR `test/` CHANGED**, so the score is a measurement of the suite as it stands rather than of a suite this session tuned. That is also why this is not money path despite the subject being the engine.

---

### 6. `CI-06/gate-inventory` went RED, and NOT in the direction the dispatch predicted

**THE DISPATCH ASKED WHICH DIRECTION WOULD FIRE, AND THE HONEST ANSWER IS NEITHER OF THE TWO IT NAMED.** The arrival direction, *"the day the artifact arrives the row is a finding until the leg is registered"*, **could not fire here and could never have fired**: this leg's artifact was in `UNPROBEABLE_ARTIFACTS`, so no probe over this tree watches for it. **An artifact nothing watches for arrives silently.**

**WHAT FIRED IS THE STALE DIRECTION, WHICH IS THE OTHER HALF OF THE SAME GATE**, and it fired on the commit that moved the row, before the register was touched:

> `29 of 32 gates pass.`
> `scripts/corpus/gates.mjs: UNPROBEABLE_ARTIFACTS registers "the VG-12 admission" and no condition in docs/testing/STRATEGY.md section 4.1 names it. A register entry that no longer names a real condition is a finding, which is what keeps this register from becoming furniture`

**THE COMPARISON WITH THE REPLAY LEG IS THE THING WORTH KEEPING.** That leg's artifact was re-ruled into a **path** by [ADR-119](../decisions/ADR-119.md), landed, was probed, and the row was red for a day. This leg's arrival was invisible. A reader comparing the two histories would conclude this one arrived more cleanly, when in fact it arrived **unobserved**. So: **the unprobeable register is not a list of things that are hard to check, it is a list of arrivals nothing will announce**, and the register shrinking is the only event a gate ever gets to see.

The entry is retired with a note and the register goes three entries to two.

---

### 7. Fence findings, reported rather than resolved quietly

**`scripts/corpus/gates.mjs` WAS TAKEN AGAINST A FENCE THAT SAID NOT TO, AND THIS IS THE LOUD VERSION.** The dispatch's own clause permits it -- *"or take it and say loudly that you did"* -- and section 6 is why the stop condition is unreachable without it. The edit is **one `Map` entry removed and a retirement note in its place**, in `UNPROBEABLE_ARTIFACTS` and nowhere else. **Sessions 243 and 244 hold that file in this wave**; this touches no probe, no gate body and no register they were dispatched against, and whichever lands second merges the other first.

**FOUR MORE PATHS WERE TAKEN THAT THE FENCE DOES NOT NAME**, each because the tool requires it: [`pnpm-workspace.yaml`](../../pnpm-workspace.yaml) and [`pnpm-lock.yaml`](../../pnpm-lock.yaml), because an admission that fitted inside a fence naming neither would not be an admission; [`package.json`](../../package.json) beyond its `scripts`, for the `pnpm.overrides` block, without which `CI-05` is red; [`packages/rules-engine/package.json`](../../packages/rules-engine/package.json), for the two `devDependencies` and the `mutation` script that ARE the scope; and [`packages/rules-engine/vitest.config.ts`](../../packages/rules-engine/vitest.config.ts) plus [`.gitignore`](../../.gitignore), on [ADR-116](../decisions/ADR-116.md)'s precedent for a runner's output.

**AND ONE FINDING THAT IS ABOUT THE GATES THEMSELVES.** [`gates.mjs`](../../scripts/corpus/gates.mjs)'s `walk` skips `node_modules` and `.git` **and nothing else**, so a directory `.gitignore` names is still read by every `CI-06` gate. Measured with a mutation run in flight: **`CI-06a` at 427 findings and `CI-06/retired-constraints` at 35**, every one of them a copy of a file already in the tree. **`pnpm run verify` after a crashed mutation run is red over files that are about to be deleted**, and that is measured rather than predicted: `cleanTempDir` removes the sandbox after a run that finishes and a run that CRASHES leaves it, both watched. **THE TWO PLACES THAT WOULD HAVE ROUTED AROUND IT WERE TRIED AND BOTH BROKE THE RUN**: `node_modules/.stryker-tmp` is excluded by Vitest's own default so the dry run reports `No tests were found`, and an absolute path in `os.tmpdir()` cannot resolve the package's dependencies and dies in the initial test run. **So the default stands, the landmine is named, and the fix is a gate edit that is not this session's**; the `docs/edge-cases/` entry `TR-04` requires is owed and outside every fence in this wave.

**ONE STALE SENTENCE PREDATES THIS BRANCH AND IS LEFT ALONE.** [STRATEGY:262](../testing/STRATEGY.md) and the runner's own `covers` string both read *"Four of the six conditions name neither a path nor a manifest key"*, while on `main` at `cce89de` the runner's note already printed `4 activation condition(s) ... 3 registered as unprobeable`. This branch moves it again, to three and two. Neither site is in this fence.

---

### 8. The approval clause, run

> **With `mutate` pointed at a path that does not exist, `pnpm --filter @merit/rules-engine run mutation` exits NON-ZERO reporting `No tests were executed`. With `mutate` at its configured glob it exits ZERO, whatever the mutation score is. And with the breaking default in `@stryker-mutator/core`'s shipped schema anything but `null`, it refuses to start at all.**

All three were executed. **It is a clause about this configuration and not about a score**, because a score is a fact about the whole engine on a day and any session that adds a test moves it; six approval clauses in this corpus have drifted for exactly that reason, and every time the ruling held while the enumeration did not.
