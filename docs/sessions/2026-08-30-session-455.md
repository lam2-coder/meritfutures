---
status: approved
depends_on: [README.md]
last_updated: 2026-08-30
---

## 2026-08-30 - Session 455: the fold was run rather than reasoned about, the row it wrote is read back, and the day that decides which row is on the wrong door

<!--index: **THE RULING IS [`ADR-264`](../decisions/ADR-264.md), `proposed` and UNSIGNED, MONEY PATH, `E2` READ OWED, and it INHERITS FOUR unsigned debts** ([ADR-241](../decisions/ADR-241.md), [ADR-250](../decisions/ADR-250.md), [ADR-258](../decisions/ADR-258.md), [ADR-260](../decisions/ADR-260.md)), because the measurement stands on all four. **NO MIGRATION NUMBER TAKEN OR RESERVED**, no file under `apps/worker/src/` edited, no `packages/**` file edited. **THE ROW'S FIRST QUESTION WAS ANSWERED BY RUNNING AND ITS PREMISE DID NOT HOLD**: `0001` through `0074` applied forward-only to an empty PostgreSQL 16.13 under `ON_ERROR_STOP=1` (118 base tables), one identity, one `users`, one plan, one `plan_versions` carrying Appendix A.1's `PlanRulesJson`, one 50K `plan_version_sizes`, one purchase, one `eval`-phase account and **ONE LIVE `daily_marks` ROW** seeded, then `node --experimental-strip-types src/start.ts` in `apps/worker` at `MERIT_BATCH_TRADING_DAY=2026-08-28` -- and **`rule_states` WENT FROM ZERO ROWS TO ONE**, `{"outcome":"completed","accountsConsidered":1,"written":1,"refused":0,"absent":0}`, exit 0. **Completing and persisting were different claims and the second is now MEASURED**, through `writeRuleStateVia` on `encodeEngineGates`, the `jsonb` round trip, `0035`'s watermark, `0065`'s three columns and `rule_states_hash_is_sha256`. **`context_gates` CAME BACK POPULATED**, which is `resolveExternalGates` reached through four real table reads rather than a fixture: `kycVerified.pass=false, state=kyc_required` for an identity with no `kyc_verifications` rows. **THE READ HALF IS BUILT AND IT IS [`apps/api/src/rule-state-reader.ts`](../../apps/api/src/rule-state-reader.ts)**: `readRuleState` rebuilds twenty-two fields with **no default anywhere**, refuses a cents column arriving as a `number` (`INV-02`), checks `0065`'s `breached = (breach_kind IS NOT NULL)` pairing on the way past, reads the `jsonb` leaf through `decodeEngineGates`, and does not read the three columns a `RuleState` has no field for. **AN ABSENT ROW IS A REFUSAL AND NEVER A DEFAULT VERDICT**: `RuleStateAbsent` is a CLASS and not an arm, distinct from `RuleStateUnreadable` because a fold that has not run and a malformed row are different operational days; and **`R-06` IS WHY THE DAY IS AN ARGUMENT** -- a row for a NEIGHBOURING day is an absence for this one, there is no `latest` function, and link 6 sweeps for one. **THE ASSERTION IS A DIGEST AND NOT A FIELD COMPARISON**: the folded row is the fixture column for column, `SD-08`'s `state_hash` is what PostgreSQL holds and nothing in the suite computes, and `stateHash` over the REBUILT state reproduces those thirty-two bytes. **WATCHED RED BEFORE GREEN FOUR TIMES**: red as an absent module, green, **red on a one-cent perturbation of one of twenty-two fields** (`9e8b0a45...` against `68d7303f...`), green on a byte-copy restore. Thirteen cases. **THE PORT DOES NOT BECOME WIREABLE AND THE REMAINDER CHANGED CHARACTER**, which is said explicitly because the row required it: sixteen revisions of that reason each named something nobody had BUILT and every one of those is now built. What stands is that **NO DEPLOYMENT HAS RUN THE JOB** -- an operator fact [ADR-241](../decisions/ADR-241.md) ruled EXTERNAL, and **no in-process timer was built**, which was trap two -- plus **A CLAUSE NO REASON ON THIS PORT HAD EVER NAMED**: `R-06` permits ONE day, `tradingCalendar` is scope class `firm`, and `CATALOG_TABLE_KEYS` is a **CLOSED LIST OF FIVE** that excludes it, so **the transaction that reads the state cannot read the day that decides which state it may read.** `databaseEconomicCalendar` reads that same table on `ApiDb.firm`, so the two ways out are [ADR-211](../decisions/ADR-211.md) clause 2's two-transaction remedy needed again on the CALENDAR half after [ADR-233](../decisions/ADR-233.md) removed it on the catalogue half, or a sixth catalogued key; **`packages/db` is not in this fence and NEITHER WAS TAKEN.** **`FM-16` IS REGISTERED RATHER THAN CREATED QUIETLY**: `toRuleState` in `apps/worker` and `readRuleState` here are two transcriptions of one row, the home is `packages/rules-engine` beside `gates-codec.ts`, and **what FM-16 costs is "with nothing comparing them", which the digest pays** -- a shared module would make the two agree by construction and prove nothing about the STORED row. **`usePayoutBackend` IS CALLED BY NOTHING, `start.ts` IS UNTOUCHED, THE ROUTES STILL ANSWER 503, THE WIRED COUNT DOES NOT MOVE.** **`CRON_INVENTORY`'s nightly row carried a sentence that had gone false** -- *"IT RUNS AND IT DOES NOT YET WRITE A ROW"* -- and it is amended with the retired wording quoted and marked; the SCHEDULE does not move. Three citations into `payouts.ts` were re-derived after this diff moved them. Counts derived at reporting time, each command run separately: **33 of 33** gates after `generate`, **23 of 23** invariants, **287 files / 6,899 passed / 6 skipped** against a baseline of **286 / 6,884 / 6** measured on this same tree with the diff stashed, a delta of **+1 file and +15 cases**, typecheck 0, lint 0, `format:check` clean. **`pnpm run verify` NOT run**, per the row. -->

**Objective.** Establish by RUNNING whether the nightly fold writes a `rule_states` row, build the
reader that turns one into a `RuleState`, and say explicitly whether `usePayoutBackend` becomes
wireable.

**Outcome.** It writes. The reader is built and the digest closes. The port does not become
wireable, and the half that refuses is one nobody had named.

---

### 1. What the row claimed, and what the tree says

| Row `264` and the dispatch | Derived before a line changed |
|---|---|
| `plan` waits on nothing (ADR-233), `gates` waits on nothing (ADR-260) | **HOLDS** |
| `decodeEngineGates` already exists for the hard half of the read | **HOLDS.** `packages/rules-engine/src/gates-codec.ts`, and `apps/api` declares that dependency |
| Establish by RUNNING whether the fold writes a row. Completing and persisting are different claims | **THE RIGHT QUESTION AND THE PREMISE DID NOT HOLD.** It writes. Section 2 |
| `rule_states` holding no rows is not a reason to install a backend that computes a verdict off an empty table | **HOLDS AND IS OBEYED.** `RuleStateAbsent` is a class with no fallback arm beside it |
| The schedule is external; do not build an in-process timer | **HOLDS AND IS OBEYED.** Nothing in this diff schedules anything |
| Something must RUN the fold and `apps/api` must READ the row: two halves, neither a decision | **BOTH HELD AND NEITHER WAS THE BLOCKER.** Section 5 |

**The row's second sentence is the one that did not survive.** *"Something must RUN the fold so a
row exists"* reads as work owed; the work was finished by [ADR-260](../decisions/ADR-260.md) and
what was owed was a MEASUREMENT. That is recorded because the row was right to demand the
measurement and wrong about what it would find, and both halves of that are useful to the next
reader.

---

### 2. The measurement

Full record in [ADR-264](../decisions/ADR-264.md) section 2. **Zero rows before, one after**, with
`state_hash` `68d7303f2d7282a3f310360a3470e9e6fa2d40025683cd5d80c437b2c5f3d090`, `phase` `eval`,
`floor_cents` `4760000`, `engine_eligible` `false` and `calendar_revision_id` `null`, which is a
pristine calendar recorded correctly rather than an unknown.

**IT IS NOT A CI GATE AND MUST NOT BE REPORTED AS ONE.** `ci.yml`'s `integration` job has no
database, so nothing in `CI-01` re-runs it. What is carried into `CI-01` is the ROW, as the
fixture the reader is asserted against, and its digest.

---

### 3. What was built

| Artefact | What it holds |
|---|---|
| [`rule-state-reader.ts`](../../apps/api/src/rule-state-reader.ts) | `readRuleState`, `ruleStateOn`, `RuleStateAbsent`, `RuleStateUnreadable`, and the two rulings at their own sites |
| [`rule-state-reader.test.ts`](../../apps/api/test/rule-state-reader.test.ts) | 13 cases. The folded row as the fixture, the digest as the comparator, and every refusal executed |
| [`rule-state-producibility.test.ts`](../../apps/api/test/rule-state-producibility.test.ts) | **Link 6.** The rebuilder census, the sweep for a `latest` arm and for a `??`, and the calendar derived from two closed lists |
| [`wiring.test.ts`](../../apps/api/test/wiring.test.ts) | `usePayoutBackend`'s **sixteenth** revision, three citations re-derived |
| [`payouts.ts`](../../apps/api/src/routes/payouts.ts) | Two docstrings. Nothing executable, and no backend installed |
| [`CRON_INVENTORY`](../ops/runbooks/CRON_INVENTORY.md) | The nightly row's stale sentence amended, retired wording quoted and marked |
| [`ADR-264`](../decisions/ADR-264.md) | The ruling |

---

### 4. Where this session is most likely wrong

**THE SEED IS THE NARROWEST BOOK A FOLD CAN HAVE.** One account, one day, phase `eval`, `prior:
null`, an empty KYC chain, no settlements, no payout requests, a pristine calendar. The chain from
`daily_marks` to a stored row is established unbroken; **every branch inside it is not.** A funded
account with a stored prior and a settled payout is a different path through `resolveAccountDay`
and nobody has run it.

**`ruleStateOn` SHIPS WITH A CORRECT SIGNATURE AND NO CORRECT CALLER**, because section 5's finding
is that no caller on the payout transaction knows the last closed day. The one function here that
could still be designed wrong has not met its call site.

**THE `FM-16` TRADE IS A JUDGEMENT.** A second transcription of a money-path row was written
deliberately while its home was out of fence. The counter is that the digest observes this pair
better than a shared module would, and it is one module either way.

---

### 5. What is owed

- **The `E2` read on [ADR-264](../decisions/ADR-264.md)**, MONEY PATH, and the four it inherits.
- **The calendar decision.** A sixth `CATALOG_TABLE_KEYS` member or [ADR-211](../decisions/ADR-211.md)
  clause 2's remedy on the calendar half. **It is the only code-level blocker left on
  `PayoutSubject.state`** and it is a `packages/db` or a `routes/payouts.ts` diff depending which
  way it goes.
- **The `FM-16` merge.** One module in `packages/rules-engine`, both call sites repointed, the
  digest comparator kept. Small, and not urgent.
- **A fold against a wider book**, section 4.
- **The other five members of `PayoutBackend`.** This session established `state` and audited none
  of `identityStatus`, `holdFlag`, `insertPayoutRequest`, `listPayouts` or `idempotency`.

---

### 6. Landmines for the next session

- **A LOCAL POSTGRESQL NEEDS THE `postgres` USER AND THE SEED NEEDS SEVEN TABLES BEFORE `accounts`.**
  `initdb` refuses to run as root. And `accounts` reaches `identities`, `users`, `purchases` and
  `plan_versions`, `purchases` has a `psp CHECK IN ('psp_a','psp_b')` and a `kind CHECK IN
  ('new','reset')`, and `plan_versions.rules` must be the ENGINE's `PlanRulesJson` rather than
  `fixtures/plans/CORE-50K.json`'s shape, which is a different document for a different reader.
- **`JSON.stringify` CALLS `toJSON` BEFORE THE REPLACER**, so a `Buffer` column dumped through a
  replacer that tests `instanceof Uint8Array` comes back as `{type:'Buffer',data:[...]}` and never
  reaches the branch. Read a `bytea` through `psql`'s `encode(...,'hex')` if the shape matters.
- **A `decodeEngineGates(` CENSUS FINDS THE ENGINE'S OWN DECLARING FILE**, which is link 5's shape
  met again one field over. Assert the whole list AND the `apps/` filter, never only the count.
- **THE `state_hash` CONSTANT IN `rule-state-reader.test.ts` IS THE COMPARATOR.** If that case goes
  red, the repair is to re-run section 2 against a database. **A session that edited the constant to
  make the suite pass would have deleted the control**, and nothing but this line stands in the way.
- **`pnpm vitest` IS NOT A SCRIPT IN THIS WORKSPACE.** `npx vitest run` or `pnpm test`.
- **DEPENDENCIES ARE NOT INSTALLED IN A FRESH CONTAINER.** `pnpm install` first.

---

### 7. Files touched

**The reader.** `apps/api/src/rule-state-reader.ts` (new).

**Assertions.** `apps/api/test/rule-state-reader.test.ts` (new),
`apps/api/test/rule-state-producibility.test.ts`, `apps/api/test/wiring.test.ts`.

**The port's shape only.** `apps/api/src/routes/payouts.ts` (two docstrings).

**Corpus.** `docs/decisions/ADR-264.md` (new), `docs/decisions/ALLOCATION.md` (row `264`),
`docs/ops/runbooks/CRON_INVENTORY.md`, `docs/decisions/README.md` and `docs/sessions/README.md`
**by generated span**, `docs/STATE.md` (append), `docs/sessions/` (this file).

**`docs/INDEX.md` IS UNCHANGED AND THAT WAS CHECKED RATHER THAN ASSUMED.** Its `decisions/` and
`sessions/` rows say in terms that entry files are indexed by those READMEs under `CI-06n`, this
diff adds no other document, and its one `apps/api/src` mention is inside a `P7` plan cell rather
than a file census. `CI-06a` and `CI-06n` pass.

**No migration, no `packages/**` file, and no file under `apps/worker/src/`.**
