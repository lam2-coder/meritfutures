---
status: approved
depends_on: [README.md]
last_updated: 2026-08-25
---

## 2026-08-25 - Session 212: the scope-rule ruling, and the trap `scope.ts` names in its own header turned out to be writable
<!--index: **[ADR-100](../decisions/ADR-100.md) lands, `status: proposed`, approval line UNSIGNED, MONEY PATH and the `E2` read owed. RULED: `derived` is unavailable where the row itself declares a column `REFERENCES identities(id)`, and `derived` may not traverse a NULLABLE edge. `DerivedRule` gains NO `nullable` field and `OwnedRule.nullable` is CHECKED against the DDL instead.** **THE RULING IS FIVE ASSERTIONS IN [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) AND NOT A PARAGRAPH**, on [ADR-042](../decisions/ADR-042.md) and on [CLAUDE.md](../../CLAUDE.md)'s *"prefer a new CI gate over a bigger model whenever the error is checkable"*. **[`packages/db/src/scope.ts`](../../packages/db/src/scope.ts) IS NOT EDITED and no table's rows are touched** -- `git status --porcelain packages/db/src` reports zero -- which is deliberate while sessions 205 through 208 transcribe into that file concurrently. **BOTH OF [SESSION 202](2026-08-25-session-202.md)'s FINDINGS WERE REPRODUCED BEFORE EITHER WAS RULED ON, AND A THIRD WAS FOUND THAT NOBODY HAD**: `wallet_entries` `derived` through `ledger_transactions`, `wallet_withdrawals` `derived` through `risk_flags` on the NULL `freeze_flag_id`, and -- the one that decides the shape of the remedy -- **`sessions` `derived` through `identity_signals` on `device_fingerprint_id`, which is [`scope.ts`](../../packages/db/src/scope.ts)'s OWN NAMED TRAP from its first twenty lines written as a rule.** All three seeded on `main` at `fe05a2e`, all three accepted by the whole estate: `pnpm run typecheck` at exit 0 with **zero `error TS`** and `scoped-db.test.ts` at **101 of 101** on each. **[Session 188](2026-08-25-session-188.md)'s bounded-chain assertion, the derived-FK assertion, the `traversal` assertion and `scopePredicate` all AGREE with all three and none of them is defective**: the chain terminates, the FK is declared, the hop is single-valued, and none of them was ever asking whether the identity it terminates at is the right one. **A HEADER PARAGRAPH IS NOT A CONTROL, and that is measured rather than argued.** **THE CANDIDATE WAS MEASURED AGAINST ALL EIGHTY RULES BEFORE IT WAS ADOPTED AND IT TURNS NOTHING RED**: clause 1 refuses **0 of 14** `derived` rules, clause 2 refuses **0 of 14**, clause 3 passes **34 of 34** `owned` flags, so there is no exemption list because there is nothing to exempt. **CLAUSE 2 IS ALREADY THE REGISTRY'S PRACTICE, ARGUED EIGHT TIMES IN PROSE AND ENFORCED NOWHERE**: `risk_flags`, `coupons`, `coupon_redemptions`, `psp_webhook_events`, `account_adjustments`, `journal_entries`, `promotional_credit_grants` and `wallet_withdrawals` each refuse a derivation on exactly this ground, naming the column and the rows it would drop, and the ninth author wrote the rule anyway. **`OwnedRule.nullable` WAS STATED BY 34 RULES, READ BY NOTHING AND CHECKED BY NOTHING** -- [`scoped-db.ts:84-89`](../../packages/db/src/scoped-db.ts) declines it in its own comment and `grep -n nullable` over the suite returned ZERO over 101 tests -- **which is why a second such field on `DerivedRule` is refused and the existing one is made checkable instead**, four lines, passing on all 34 including `wallet_dormancy` whose `NOT NULL` is carried by `PRIMARY KEY`. **A COMPILE-TIME REFUSAL IS IMPOSSIBLE FOR CLAUSE 1 AND THE REASON IS STRUCTURAL**: the predicate is a fact about the DDL, `tsc` cannot read a migration, and the only encoding is [ADR-092](../decisions/ADR-092.md) section 5's measured two-statements-of-one-fact hazard plus [session 145](2026-08-23-session-145.md)'s rule-asserted-against-itself. **A LIVE FINDING IS RECORDED AND DELIBERATELY NOT REPAIRED**: `no firm table carries a column referencing identities` states a claim that is FALSE about this tree and is green because its reader stops at `CREATE TABLE`, and `admin_actions.on_behalf_of_identity_id` is `ALTER`-added in [`0043`](../../packages/db/migrations/0043_admin_attributed_actions.sql), is the ONLY one of the 31 firm tables that reaches `identities`, and repairing it is a ruling about what the `firm` class PROMISES. **Each seed watched being refused by the clause that NAMES it, and clause 2 fires ALONE on `sessions`**, which is what makes it independently load bearing rather than a refinement of clause 1; two mutations checked, the `PRIMARY KEY` leg failing on `wallet_dormancy` BY NAME and a `CREATE`-only fold failing on `sessions`. **The approval clause is a COMMAND rather than a COUNT**, on the seven that have drifted, and it carries the direction it fails in: a reader that silently stops matching looks exactly like a refusal with nothing to refuse, so the third command must fail **34 times**. `scoped-db.test.ts` **101 to 106**, suite **1,611 to 1,616 passed / 1 skipped**, `typecheck` exit 0, `eslint packages/db` exit 0, 30 of 30 gates, `verify` exit 0 -->

- **Objective:** rule on [session 202](2026-08-25-session-202.md)'s two landmines, both of which are a `derived` rule the registry could write and should not. **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **One ADR: [ADR-100](../decisions/ADR-100.md).**
- **Done:** [ADR-100](../decisions/ADR-100.md) landed at `status: proposed` with the approval line UNSIGNED, and the ruling is **mechanical**: five assertions appended to [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) under `a class is REFUSED as well as declared`. **Both reported findings reproduced first, a third found, and every clause measured against all eighty rules before it was adopted.** `scoped-db.test.ts` **101 to 106**; suite **1,611 to 1,616 passed / 1 skipped**.
- **Next:** **The `E2` line-by-line read is owed on [ADR-100](../decisions/ADR-100.md) and on the assertions**, and the approval clause is written as three commands so the read can execute it rather than trust it. **The `firm` assertion's blind reader is dispatchable and needs an ALLOCATION row**: the repair is one line -- point it at `identityColumnsOf(...)`, which this session adds -- plus a restatement of what the `firm` class promises about a NULLABLE identity column, and it is a ruling rather than a transcription. **Sessions 205 through 208 will merge into this**, and the merge is expected to be textual and keep-both.
- **Blockers:** none for this session.
- **Landmines:**
  - **[`scope.ts`](../../packages/db/src/scope.ts)'s OWN NAMED TRAP WAS WRITABLE AS A RULE AND THE ESTATE STAYED GREEN.** The header names `sessions.device_fingerprint_id -> identity_signals` in its first twenty lines as one of three reasons the registry is declared rather than derived. Seeded as a rule: `typecheck` exit 0 with zero `error TS`, `scoped-db.test.ts` **101 of 101**. **This is the strongest of the three seeds and neither session 202 nor the dispatch had it**, and it is what makes the case that the instrument had to be an assertion rather than a paragraph -- the paragraph already existed.
  - **CLAUSE 1 DOES NOT CATCH THE `sessions` SEED AND THAT IS WHY THERE ARE TWO CLAUSES.** `sessions` carries no identity column of its own, so the candidate ruling as dispatched refuses it not at all. **Clause 2 fires alone.** A ruling that had adopted only the candidate would have closed two of the three holes and read as if it had closed all of them.
  - **`OwnedRule.nullable` IS READ BY NOTHING AND WAS CHECKED BY NOTHING.** `scopePredicate`'s `owned` branch is `eq(column, identityId)` whichever the flag says, and its own comment says adding `IS NOT NULL` *"would look careful and assert nothing"*. `grep -n nullable packages/db/test/scoped-db.test.ts` returned **zero** before this session. **The field was a comment with a type for as long as the registry has existed**, which is the whole argument against giving `DerivedRule` a second one.
  - **THE EXISTING `firm` ASSERTION STATES A CLAIM THAT IS FALSE ABOUT THIS TREE AND IS GREEN.** `no firm table carries a column referencing identities, so the class is not hiding one` reads one `CREATE TABLE`; `admin_actions.on_behalf_of_identity_id uuid NULL REFERENCES identities(id)` is added by `ALTER TABLE` in [`0043`](../../packages/db/migrations/0043_admin_attributed_actions.sql). Folded over all 47 migrations it is the **only** one of the 31 firm tables that reaches `identities` directly. **REPORTED AND NOT REPAIRED**: pointing the assertion at the fold turns it RED on a row that is CORRECTLY `firm`, so the repair is a ruling about the `firm` class and not this session's subject. **The `owned` column assertion has the same reader and is sound only because zero of the 34 `owned` columns is `ALTER`-added, measured.**
  - **A SECOND READER OF THE MIGRATIONS NOW EXISTS AND IS BOUND TO THE FIRST BY AN ASSERTION.** `foldTableDefs` folds definitions where `foldTable` folds names, which is [ADR-092](../decisions/ADR-092.md) section 5's `SQL_NAME` / `DDL_NAMES` hazard in a new place. The first of the five tests compares the two key sets on **every** registered table, and a `CREATE`-only mutation was watched failing it on `sessions`.
  - **THE `PRIMARY KEY` LEG OF THE NULLABILITY READER IS LOAD BEARING AND `wallet_dormancy` IS WHY.** `identity_id uuid PRIMARY KEY REFERENCES identities(id)` carries no `NOT NULL` text at all. Removing the leg was watched failing on that row by name, which is [session 202](2026-08-25-session-202.md)'s own landmine arriving as a mutation result.
  - **THE REFUSAL COULD PASS BY READING NOTHING, AND ONE ASSERTION EXISTS ONLY TO REFUSE THAT.** Clause 1 passes today by finding nothing on fourteen tables, and a reader that had stopped matching would find nothing on all eighty and stay just as green. `the identity-column reader finds the column every owned rule names` fails **34 times** in that case.
  - **NOTHING IN [`packages/db/src/`](../../packages/db/src/scope.ts) WAS EDITED**, so the three seeds and the flag-flip seed all reverted to a byte-identical file and `git status --porcelain` was read at zero before each commit. **`falsify:ci` was never backgrounded and `git add -A` was never used in this session.**
- **Files touched:** [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) (append only, 220 lines), [ADR-100](../decisions/ADR-100.md) (new), [ALLOCATION](../decisions/ALLOCATION.md) (its row, reserved in the first commit and amended in place when the file landed), [decisions/README](../decisions/README.md) (generated row), [INDEX](../INDEX.md), [STATE](../STATE.md) (one appended `##` section plus a generated count span), [sessions/README](README.md) (row 212 struck, entry row generated), this file. **Nothing outside the fence.** [ADR-092](../decisions/ADR-092.md), [ADR-094](../decisions/ADR-094.md), [ADR-008](../decisions/ADR-008.md), [ADR-042](../decisions/ADR-042.md), [`scope.ts`](../../packages/db/src/scope.ts), [`scoped-db.ts`](../../packages/db/src/scoped-db.ts), [session 202](2026-08-25-session-202.md) and all 47 migrations were **read and none was written**.

### What was NOT done, named rather than implied

**No migration, no migration number claimed, no `CI-06` letter, no gate register row, no `VG` row, no `RI-nn`, no `SD-nn`, no `OI-nn`, no fixture, no edge-case entry, and no edit to [`docs/plans/`](../plans/M20-wallet.md), which this fence makes read only.**

**[`packages/db/src/scope.ts`](../../packages/db/src/scope.ts) IS NOT EDITED AT ALL** and that is a ruling rather than an omission. A comment in the file naming the refusal would be the prose [ADR-042](../decisions/ADR-042.md) refuses, and section 1 of [ADR-100](../decisions/ADR-100.md) is the evidence in this specific case: the header already names one of the three traps and the trap was seeded anyway. **The refusal a future author meets is a red test naming their own row and quoting their own DDL back.** It also keeps this diff off the file [sessions 205 through 208](README.md) are transcribing into concurrently.

**NO TABLE'S ROWS ARE TOUCHED AND NO REGISTRATION IS RE-ARGUED.** `wallet_entries` and `wallet_withdrawals` stay `owned` exactly as [session 202](2026-08-25-session-202.md) registered them; this session rules what could have been written there and was not. The four seeds and the flag-flip were reverted and the tree confirmed clean before every commit.

**THE `firm` ASSERTION IS NOT REPAIRED AND THE `owned` COLUMN ASSERTION IS NOT REPOINTED.** Both are named in [ADR-100](../decisions/ADR-100.md) section 7 with the one-line remedy written out. Repairing the first turns it red on `admin_actions`, which is correctly `firm`, so the green requires a restatement of what the `firm` class promises -- a ruling about a different class made in passing, in the same commit, on the money path, and not the subject the founder's `E2` read is being asked for.

**COLUMN TYPE REMAINS ASSERTED NOWHERE**, which is [ADR-094](../decisions/ADR-094.md) section 3's unclosed axis. This session closes the NULLABILITY half for two classes and leaves type open over all 80 registered tables.

**`identity_links`, `attributions`, `events` AND `affiliate_commissions` ARE NOT REGISTERED**, and clause 1 makes `events`' absence structural rather than a judgement nobody re-checks: its only two paths are `identity_id uuid NULL` and `account_id uuid NULL`, so clause 1 refuses a hop through `accounts` and clause 2 refuses it twice over.

### The merge, and what it changed

**[Session 205](2026-08-25-session-205.md) merged first**, so `main` moved from `fe05a2e` to `7f8215f` while this branch was open, and the registry went from **80 to 87 of 111** at 1 root / 34 owned / **19** derived / 33 firm.

**THE RULING MET A SLICE NOBODY COORDINATED WITH AND REFUSED NONE OF IT.** Session 205 added **five new `derived` rules**, written by an author who had never read [ADR-100](../decisions/ADR-100.md): `account_status_history`, `platform_account_refs`, `provisioning_queue`, `platform_entitlements` and `reconciliations`, each hopping `account_id` to `accounts`. **All five satisfy both clauses and not one line of session 205's work was edited to make them.** Re-measured on the merge result rather than reasoned about: **clause 1 refuses 0 of 19, clause 2 refuses 0 of 19, clause 3 passes 34 of 34.**

**ALL THREE SEEDS WERE RE-RUN ON THE MERGE RESULT AND ALL THREE ARE STILL REFUSED BY THE SAME CLAUSES** -- 1 failed / 112 passed, 2 failed / 111 passed, 1 failed / 112 passed against a merged baseline of **113** -- with the file restored and `git status --porcelain` read at zero after each.

**THE CONFLICT WAS IN [STATE](../STATE.md) AND [sessions/README](README.md) ONLY, BOTH APPENDED SECTIONS AND BOTH KEEP-BOTH, WITH SESSION 205's SIDE PLACED FIRST.** The three code files auto-merged, because this session appends 220 lines to the END of one test file and touches no table's rows -- which is the reason the diff was kept to the mechanism in the first place. **Neither session 205's STATE section nor its log was edited**, and the `<!--gen-->` spans in both READMEs were regenerated rather than hand-merged.

**Every figure in this log above the merge is true at this session's BASE and is left standing rather than rewritten**, because a count is a fact about a tree at a commit. `scoped-db.test.ts` **108 to 113** merged; full suite **1,618 to 1,623 passed / 1 skipped** over 106 files; `typecheck` exit 0 with zero `error TS`; 30 of 30 gates; `verify` exit 0.
