---
status: approved
depends_on: [README.md]
last_updated: 2026-08-30
---

## 2026-08-30 - Session 451: the resolver nobody had written, the seventh status refused rather than admitted, and the fold that completes because of it

<!--index: **THE RULING IS [`ADR-260`](../decisions/ADR-260.md), `proposed` and UNSIGNED, MONEY PATH, `E2` READ OWED, AND THREE `E2` DEBTS ARE INHERITED** ([ADR-248](../decisions/ADR-248.md), [ADR-254](../decisions/ADR-254.md), [ADR-258](../decisions/ADR-258.md), all `proposed` and all UNSIGNED). **NO MIGRATION NUMBER IS RESERVED, TAKEN OR SPENT**, `0074` is still the highest, and `git diff origin/main -- packages/db/` is EMPTY. Row `260` sent one session to write the `ExternalGates` resolver [ADR-248](../decisions/ADR-248.md) ruled unbuildable and [ADR-254](../decisions/ADR-254.md) unblocked. **IT IS WRITTEN AND ITS HOME IS THE ENGINE.** [`packages/rules-engine/src/external-gates.ts`](../../packages/rules-engine/src/external-gates.ts) is PURE: it takes the RAW column values four tables hold and returns an `ExternalGates` or throws an `ExternalGatesRefusal` naming the ACCOUNT and every leg it could not derive. **THE HOME IS [ADR-239](../decisions/ADR-239.md) SLICE A's ARGUMENT UNCHANGED**: `apps/worker` needs `AccountDay.external`, `apps/api` needs `PayoutSubject.gates`, neither can import the other, and that package declares no workspace dependency at all, so a resolver in each is `FM-16` by name. **AND THE COST OF THE OTHER CHOICE IS ALREADY PAID ONE LEG DOWN RATHER THAN PREDICTED**: `currentKycState` is written out TWICE under `apps/api/src/routes/` because a route may not import a route, which is also why [ADR-248](../decisions/ADR-248.md) section 4's *"a resolver would call it rather than write a second one"* could not be honoured -- that entry named this as its most likely error and it was. **THE TRAP IS REFUSED RATHER THAN PAPERED OVER**: `account_status` declares SEVEN members and `AccountStatus` takes SIX, `M01:203` carries the same six so the engine transcribed its source correctly, and `provisioning_pending` is **REFUSED BY NAME** -- widening the union would amend a frozen plan through a type and would decide, in a narrowing function nobody reads twice, what a half-provisioned account is worth to `R-40`. **NO PERMISSIVE DEFAULT ON ANY LEG AND NO REFUSING ONE EITHER**: `R-41` conjoins all five as VETOES, an unknown `accounts.status` refuses, a `payout_requests.status` outside the FIVE refuses (the vocabulary has moved twice on that table), and a value that cannot be derived is a THROW because a refusal is visible and a conservative value is a silent denial with a plausible shape. **THE KYC LEG GOES FURTHER THAN THE ESTATE DOES, ON A JUDGEMENT THAT IS DECLARED**: an ambiguous chain REFUSES here where both shipped route readers fail closed to `kyc_required`, because on those doors the value is DISPLAYED to somebody who can act on it and here it is folded into a stored money row where `kyc_required` is indistinguishable from *we could not tell*. An EMPTY chain is still `kyc_required`, which is a reading. **THE CONSEQUENCE IS BIGGER THAN THE FIELD AND IT IS SAID OUT LOUD**: `AccountDay.external` was the last port `runNightlyBatch` could not resolve, so **THE NIGHTLY FOLD NOW COMPLETES** on `apps/worker` and writes a `rule_states` row per account where it stopped at the first one. `accountDaysFrom` and `storedRuleStates` still refuse and **NEITHER IS ON THAT PATH**; the split [ADR-258](../decisions/ADR-258.md) made between the two blocker constants is what kept that honest. **`usePayoutBackend` IS NOT WIRED AND THE ROW REQUIRED THAT BE EXPLICIT**: `PayoutSubject.state` still needs a `rule_states` row no scheduled run has written, [`payouts.ts`](../../apps/api/src/routes/payouts.ts) gains a field docstring and no executable line, `start.ts` is untouched, and the route still answers 503. **FOUR CASES WENT RED WITHOUT BEING SEEDED**, each written by an earlier entry to pin an ABSENCE -- [ADR-248](../decisions/ADR-248.md)'s census, [ADR-258](../decisions/ADR-258.md)'s refusal list, [ADR-241](../decisions/ADR-241.md)'s eligibility clause and [ADR-078](../decisions/ADR-078.md)'s export surface -- and all four are rewritten onto the resolved side rather than deleted; the census now asserts the STRONGER property, that exactly one producer ships under any `src/` and **no `apps/*` file is on the list**. **FIFTEEN SEEDED DEFECTS WATCHED RED**, each restored and `sha256sum` verified, the two that matter being the widened union (2 cases) and the admitted seventh member (8). **ONE SEED FOUND THE ASSERTION RATHER THAN THE TREE**: `M01`'s `SD-09` row carries the RETIRED status set on the same line under `THIS ROW READ`, so a slice to end-of-line reads six statuses and would have been green on a row saying two different things. **ONE FENCE WIDENED AND DECLARED**: one clause of `ELIGIBILITY_BLOCKER` in [`routes/account-reads.ts`](../../apps/api/src/routes/account-reads.ts), which this diff falsified inside a live 503 that now sends an operator to build a port that is built; the retired wording is PARAPHRASED rather than quoted. **REGISTERED RATHER THAN REPAIRED**: two OTHER clauses of that same constant were already false on `origin/main` ([ADR-250](../decisions/ADR-250.md)'s remainder) and the case guarding one of them is green on a false sentence, which is the more useful half of the finding; and `currentKycState` now exists in three shapes with nothing comparing them, the repair being that both routes call the engine's. Counts derived at reporting time: **33 of 33** gates, **23 of 23** invariants, **284 files / 6,833 passed / 6 skipped** against a baseline of **282 / 6,779 / 6** reproduced on `origin/main` in a separate worktree, a delta of **+2 files and +54 cases**, typecheck clean over twelve projects, lint 0, `format:check` clean. **`falsify.mjs` NOT run**: forbidden by this row and it mutates the working tree. -->

**Objective.** Write the `ExternalGates` resolver, refuse rather than widen on the seventh
`account_status` member, take no permissive default on any leg, and say out loud if wiring a port
becomes legitimate.

**Outcome.** Done. **And the last clause is where the session earns its keep**: no port was wired,
and resolving the sixth field of an `AccountDay` turned the nightly batch on anyway.

---

### 1. Done

| # | |
|---|---|
| 1 | **[`packages/rules-engine/src/external-gates.ts`](../../packages/rules-engine/src/external-gates.ts).** NEW. `resolveExternalGates`, `ExternalGatesRefusal`, three closed vocabularies, no I/O and no import but a type import |
| 2 | **[`packages/rules-engine/src/index.ts`](../../packages/rules-engine/src/index.ts).** Three names exported, with the `M01` section 1.3 argument written out. Twenty-eight names to thirty-one, seventeen functions to eighteen, five error classes to six |
| 3 | **[`apps/worker/src/batch/adapter.ts`](../../apps/worker/src/batch/adapter.ts).** `externalGates()` reads `identities`, `kyc_verifications` and `payout_requests` and hands RAW values over; `loadAccountDay` serves six of six; `AccountDayInputs` and `witness()` deleted with the refusal they served |
| 4 | **[`apps/api/src/routes/payouts.ts`](../../apps/api/src/routes/payouts.ts).** The `gates` field's docstring. **Comment only** |
| 5 | **Three suites**: 27 pure cases in the engine, 12 comparator cases in `apps/api` against the migrations and `M01`, and 15 column-by-column cases in `apps/worker` |
| 6 | **[`apps/api/test/wiring.test.ts`](../../apps/api/test/wiring.test.ts).** The `usePayoutBackend` entry narrowed to ONE blocker, and two citations repaired |
| 7 | The record: `ADR-260`, row `260`, `decisions/README.md`, `STATE`, this log |

**`docs/INDEX.md` NEEDED NO ROW AND THAT IS DERIVED RATHER THAN SKIPPED.** Its own row 48 states
that ADR entry files "are fragments rather than corpus documents and are indexed by that README
under `CI-06n`, not by this row", and no source file under any `packages/*/src` is indexed there:
`gates-codec.ts`, the closest precedent, has no row either. No corpus DOCUMENT was added.

### 2. Next

1. **The `E2` read on four entries in order**: [ADR-248](../decisions/ADR-248.md),
   [ADR-254](../decisions/ADR-254.md), [ADR-258](../decisions/ADR-258.md), then
   [ADR-260](../decisions/ADR-260.md). A founder who moves any of the first three moves this one.
2. **The three judgements in section 11 of the entry**, one of which is not the resolver at all: the
   nightly fold completing is the largest thing in the diff and it is a side effect of a field.
3. **`usePayoutBackend` waits on ONE thing now.** A scheduled run that writes a `rule_states` row,
   and a `RuleStateRow` reader in `apps/api`. The next row on this port should be sent at those and
   at nothing else on that list.
4. **`currentKycState`'s three shapes.** Section 9 finding 1 of the entry.

### 3. Blockers

**None taken and none met.** The row's fence held everywhere except one clause, declared below.

### 4. Landmines

1. **THE FOLD IS LIVE ON THE WORKER AS OF THIS DIFF.** `job.ts` installs `postgresBatchPorts` and
   `start.ts` awaits `main`, so this is not a capability the deployable has, it is behaviour it
   performs. A reader who takes "the resolver landed" as a comment change will be wrong about what
   changed.
2. **A REFUSING ACCOUNT STOPS THE WHOLE BATCH.** One account whose `accounts.status` is
   `provisioning_pending` and which has a live daily mark throws, and nothing catches between the
   resolver and the process. That is the ruled behaviour and it is recoverable by a human in the
   morning; a wrong `context_gates` row is not.
3. **`ELIGIBILITY_BLOCKER` STILL CARRIES TWO FALSE CLAUSES**, both `ADR-250`'s remainder rather than
   this ruling's, and **the case that guards one of them is green on a false sentence** because it
   measures `RuleStateWriterIo` VALUES rather than the claim. Registered, not repaired.
4. **`DELTA_MANIFEST.md:2043`'s `routes/payouts.ts:438-439` pointer moved to `468-469`.** It is a
   historical record in a frozen file and outside the fence. Registered.
5. **The `gen:adr_count` landmine is still not there.** Session 449 re-derived it: that PARAGRAPH
   appears once in `docs/STATE.md` and the other occurrences are prose ABOUT the duplication. This
   append adds no copy of either.

### 5. Files touched

```
NEW  packages/rules-engine/src/external-gates.ts
NEW  packages/rules-engine/test/external-gates.test.ts
NEW  apps/api/test/external-gates.test.ts
NEW  docs/decisions/ADR-260.md
NEW  docs/sessions/2026-08-30-session-451.md
     packages/rules-engine/src/index.ts
     packages/rules-engine/test/evaluate.golden.test.ts
     apps/worker/src/batch/adapter.ts
     apps/worker/test/account-day.test.ts
     apps/worker/test/fixtures.ts
     apps/worker/test/entrypoint-harness.ts
     apps/worker/test/entrypoint.test.ts
     apps/api/src/routes/payouts.ts          (comment only)
     apps/api/src/routes/account-reads.ts    (ONE clause, fence widened, declared)
     apps/api/test/wiring.test.ts
     apps/api/test/rule-state-producibility.test.ts
     apps/api/test/account-reads.test.ts
     docs/decisions/ALLOCATION.md
     docs/decisions/README.md                (generated span)
     docs/STATE.md                           (append)
```

**Not touched, and named because sessions 452, 453 and 454 hold them**:
`apps/api/src/routes/wallet-withdrawals.ts`, `routes/certificates.ts`, `routes/checkout.ts` and
`packages/affiliate`. `git status --porcelain` reports zero on all four. No migration, no
`MERIT_BUILD_MASTER_PROMPT.md`, no `status` field anywhere in the corpus.

### 6. Verification

Each command run separately, and the baseline reproduced on `origin/main` in a separate worktree
rather than remembered.

| Check | Result |
|---|---|
| `node scripts/corpus/gates.mjs generate` then `check` | **33 of 33** |
| `node packages/tooling/checks/repo-invariants.mjs` | **23 of 23** |
| `pnpm install` | clean |
| `pnpm vitest run` | **284 files, 6,833 passed, 6 skipped** (baseline **282 / 6,779 / 6**) |
| `pnpm run typecheck` | clean, twelve projects |
| `pnpm run lint` | clean |
| `pnpm run format:check` | clean |

**`falsify.mjs` was NOT run.** Forbidden by this row and it mutates the working tree.
