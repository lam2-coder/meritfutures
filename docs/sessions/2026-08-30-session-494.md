---
status: approved
depends_on: [README.md]
last_updated: 2026-08-30
---

## 2026-08-30 - Session 494: the worker has no ledger, and the three absences are five

<!--index: **THE RULING IS [`ADR-305`](../decisions/ADR-305.md), `proposed` and UNSIGNED. MONEY PATH, PLANNING ONLY, `E2` READ OWED.** **ALL THREE DISPATCHED ABSENCES WERE RE-DERIVED AT SOURCE AND ALL THREE HOLD**: [`apps/worker/package.json`](../../apps/worker/package.json) declares exactly `@merit/db` and `@merit/rules-engine` and the specifier `@merit/ledger` returns **zero lines** across that whole tree; `postLt01` is declared at [`sweeps/ports.ts:230`](../../apps/worker/src/sweeps/ports.ts), called at [`sweeps/expiry.ts:671`](../../apps/worker/src/sweeps/expiry.ts) and occurs at exactly two lines in `ports.ts`, the declaration and **the rejector at [`:336`](../../apps/worker/src/sweeps/ports.ts)**; and **no migration names `pgboss`**. **THE SIZING IS NINE SLICES AND THE THREE ABSENCES ARE FIVE.** **`@merit/ledger` EXPORTS NO `LT-06` BUILDER** (`F1`): `walletWithdrawalApprovalPosting` is module-scoped at [`reversal.ts:218`](../../packages/ledger/src/reversal.ts) and occurs **zero times** in the package's public surface, so a session taking absence 1 as a one-line manifest fix lands the line, imports the package and **finds no function to call**; the privacy's stated ground, [`lt06-posting-timing.test.ts:72`](../../apps/api/test/lt06-posting-timing.test.ts)'s *"nothing in this tree rules `LT-06`'s writer"*, was **discharged by [ADR-270](../decisions/ADR-270.md) clause 2**. **`lt01()` IS IN THE DEPLOYABLE THE WORKER CANNOT IMPORT** (`F2`): one definition, [`payouts.ts:933`](../../apps/api/src/routes/payouts.ts), and **the copy-the-arithmetic route is refused by the port itself BY NAME** at [`ports.ts:216-220`](../../apps/worker/src/sweeps/ports.ts) as [ADR-092](../decisions/ADR-092.md) section 5's hazard on the money path, so `lt01` moves or nothing posts. **AND THE ADAPTER THAT PORT SPECIFIES DOES NOT COMPILE AGAINST ITS OWN PORT** (`F3`): [`ports.ts:222`](../../apps/worker/src/sweeps/ports.ts) says `postTransaction(tx.ledger, ...)` and `ExpiryTx` declares **three** members, none of them `ledger`, both ledger keys being excluded from `EXPIRY_TABLES` **DELIBERATELY**. **AND ABSENCE 3 IS TWO LINES AND NOT ONE: NO DEPLOYABLE DECLARES `@merit/queue` AT ALL**, every occurrence in both trees being a comment recording the absence. **THE AUTHORITY QUESTION IS ANSWERED IN WRITING AND BOTH REFUSALS STAND, RE-DERIVED AT SOURCE AND NOT SOFTENED**: [`scoped-db.ts:271`](../../packages/db/src/scoped-db.ts) is still two members and `ApiDb` still declares no system door. **[ADR-165](../decisions/ADR-165.md) AND [ADR-176](../decisions/ADR-176.md) ARE NOT REOPENED.** **THE DRIVER IS NOT `driveApprovals` MOVED, WHICH IS THE LARGEST SINGLE FINDING**: `WithdrawalTx` ([`:931-936`](../../apps/api/src/routes/wallet-withdrawals.ts)) says *"EVERY METHOD IS SCOPED TO THE CALLER'S IDENTITY ... No method takes an identity"* and **a clock has no identity**, and `decideApproval` is pure and homeless, **the same question [ADR-286](../decisions/ADR-286.md) met and refused every candidate home for**. **AND THE INSTALLATION IS NOT MERIT'S TO SCHEDULE, MEASURED AT THREE SOURCES**: `0072`'s **`WD-C2` refuses `approved --> cancelled`**, `approved --> transferring` is the only other arrow out, and `transferring` is unreachable with no rail adapter and no importer, **so the day `LT-06` posts an approved withdrawal has no exit at all** and `0072` wrote that down one migration in advance in its own words. **A 503 SAYS MERIT CANNOT DO THIS TODAY. A WIRED DEPLOYMENT WOULD SAY YES AND THEN NEVER PAY.** **EIGHT OF NINE SLICES ARE DISPATCHABLE AND FIVE MAY RUN AT ONCE**; **slice 9 joins 7, 8 AND THE RAIL**, which is **named out of scope and NOT SIZED** because it is vendor work and FOUNDER-OWED. The one move that shortens the plan is **named as a founder choice and NOT proposed**. **NO `src/` LINE MOVED, NO TEST CHANGED, NO MIGRATION WRITTEN, NO `package.json` EDITED, NO `RI-nn` MINTED, AND NO MIGRATION NUMBER TAKEN OR RESERVED**: `0077` is [ADR-297](../decisions/ADR-297.md)'s and **`0078` is the first free number and slice 4's own row spends it**. **NOTHING WIRED AND THE COUNTS DO NOT MOVE: TEN WIRED OF TWENTY-FOUR DECLARED, FOURTEEN BLOCKED.** **`useWithdrawalBackend`'s ENTRY WAS NOT REWRITTEN**, `apps/api/test/**` being out of fence, so **`F1`, `F2` and `F3` ARE OWED TO IT**. Counts derived at reporting time, each command run separately: **33 of 33** gates after `generate`, **31 of 31** invariants off the runner's own last line, **298 files / 7,249 passed / 6 skipped** with a delta of **ZERO**, typecheck 0, lint 0, `format:check` clean. **`falsify.mjs` was NOT run**, and the fence is measured rather than intended: `git status --porcelain` names **six files and all six are under `docs/`**, two of them the generated registry READMEs. **`pnpm run verify` was NOT run**, forbidden by the row. **`node_modules` was absent on arrival and was restored by `pnpm install --frozen-lockfile`; its absence was never committed. AND THE REPOSITORY ITSELF WAS ABSENT**: the container held no clone at all and it was attached and verified against this row before a byte was written. **NO GITHUB API ACCESS: THE BRANCH IS PUSHED AND MERGEABLE AND NO PR WAS OPENED.** `gh` is not installed in this container and the REST call answers *"GitHub access is not enabled for this session. An org admin must connect the Claude GitHub App for this organization."* The push itself succeeds through the session's git proxy, so the branch is on `origin` and the pull request is one click for the founder. This is the same refusal sessions **485**, **487** and **291** reported. -->

---

### 1. The objective

**[ALLOCATION](../decisions/ALLOCATION.md) row `305`: size the three absences standing between the ruled `LT-06` authority and a posting, the way [ADR-287](../decisions/ADR-287.md) sized the payout backend into nine slices.** One objective, money path, **planning only**. The second of the two dispatchable leaf ports from row `304`'s finding.

### 2. What was read before anything was written

Row `305` in full, then `CLAUDE.md`, [STATE](../STATE.md), the last two session logs. Then **`useWithdrawalBackend`'s entry in the `BLOCKED` map in [`wiring.test.ts`](../../apps/api/test/wiring.test.ts) in full**, all eight corrections, as the row directed. Then [ADR-270](../decisions/ADR-270.md) in full and [ADR-287](../decisions/ADR-287.md) section 7 as the format to copy.

**Then, at source rather than through those entries**: `apps/worker`'s manifest and every occurrence of `@merit/ledger` and `@merit/queue` in both `apps/worker` and `apps/api`; `sweeps/ports.ts`'s `ExpiryTx`, `Lt01Values`, `ExpiryLedgerPort`, `ExpirySweepIo` and `UNWIRED_EXPIRY_SWEEP_IO`; `sweeps/expiry.ts`'s `postLt01` call site; `packages/ledger`'s whole `index.ts` export surface and `reversal.ts`'s four functions; `packages/queue`'s manifest and `pg-boss-queue.ts`'s `migrate: false` paragraph; `wallet-withdrawals.ts`'s `decideApproval`, `driveApprovals`, `WithdrawalTx` and `TERMINAL_EDGE_FINDINGS`; `payouts.ts`'s `lt01`; `scoped-db.ts`'s `SystemReason`; `worker/src/db.ts`, `job.ts` and `start.ts`; `lt06-posting-timing.test.ts` in full; `ledger-posting-authority.test.ts`'s case list; and **`0072_terminal_withdrawal_transitions.sql`'s header, its `WD-C2` body and its list of the arrows it does not bind**.

**That last read is what found section 5**, which the dispatch did not name and which changes what slice 9 is blocked on.

### 3. Done

- **[ADR-305](../decisions/ADR-305.md)**, ten sections, `proposed`, **UNSIGNED**. Sizes the work into nine slices with a fence, a blocker and a stop condition each, states which may run in parallel, answers the authority question in writing, and names the rail out of scope.
- **[ALLOCATION](../decisions/ALLOCATION.md)**: row `305` marked taken and landed. **No migration number taken or reserved and no `RI-nn` minted or reserved.**
- **[STATE](../STATE.md)**: this session's entry.

### 4. Next

**Row `305` is complete and the branch is left mergeable.** **Five slices are dispatchable today with no blocker and no dependency on each other** and may run in parallel on five branches: the `LT-06` builder export, the `lt01` code motion, the `ExpiryTx` ledger-handle ruling, the job-store migration, and the driver-port-and-home ruling. **Slice 6 closes `ExpiryLedgerPort.postLt01` and joins slices 2 and 3. Slice 7 is the driver and joins 1 and 5.** **Slice 9, the installation, must not be dispatched before the rail exists**, and section 5 of the ADR is why.

**`F1`, `F2` and `F3` are owed to `useWithdrawalBackend`'s entry** by a row whose fence reaches `apps/api/test/**`. `RI-38` and every number above it are untouched.

### 5. Blockers

**None for this row.** [ADR-305](../decisions/ADR-305.md) ships `proposed` with an UNSIGNED approval line and merging is not signing. Section 10 names the three things a founder read decides: **whether to commission the rail or to rule a new arrow out of `approved` with `LT-09` attached**, which is the only thing here no session can move; whether `F1` and `F2` are detail or are a correction to how [ADR-270](../decisions/ADR-270.md) clause 6 should be read; and whether slice 4 is money path.

### 6. Landmines

- **THE THREE ABSENCES READ AS THREE ONE-LINE FIXES AND NOT ONE OF THEM IS ONE.** The manifest line grants an exported surface that does not contain `LT-06`. The adapter the port specifies in its own docblock does not compile against its own port. The job store is a migration **and** a manifest line nobody has written either half of.
- **THE DAY `LT-06` POSTS, AN APPROVED WITHDRAWAL HAS NO EXIT AT ALL, AND THE CONTROL THAT MAKES THAT TRUE IS CORRECT.** `0072`'s `WD-C2` closed `approved --> cancelled` deliberately, because a cancel from `approved` would leave the obligation standing. It could not also produce a rail. **So the estate is correct and stuck, and the stuckness is invisible until a driver lands.**
- **`decideApproval` IS PURE, SHARED-LOOKING, AND HOMELESS, AND THE TEMPTATION IS TO COPY IT.** [ADR-286](../decisions/ADR-286.md) met this exact shape on the size decoder and refused every candidate home. A driver session that copies a money-path decision function into `apps/worker` has made two statements of one fact on the money path.
- **`SystemReason` GAINING A THIRD MEMBER IS THE TRAP AND IT IS ALREADY INSTRUMENTED.** [ADR-270](../decisions/ADR-270.md) section 4 seed 1 named `'withdrawal-driver'` and watched the case fire. The case lives in `apps/api/test/`, where a wiring session runs it.
- **SLICE 6 WILL TURN TWO GREEN CASES RED BY SUCCEEDING.** `ledger-posting-authority.test.ts`'s two "the worker does not post" cases are correct today and become false the moment `postLt01` has an implementation. **They are to be rewritten rather than deleted.**

### 7. Fence

**Held.** `docs/decisions/ADR-305.md` (new), `docs/decisions/ALLOCATION.md`, `docs/STATE.md`, this log, and the two registry READMEs, **which are GENERATED by `node scripts/corpus/gates.mjs generate` and not hand-written**.

**`docs/INDEX.md` was NOT edited** though the fence named it: an ADR is a registry entry and needs no INDEX row ([ADR-287](../decisions/ADR-287.md) section 9), and **no `docs/plans/` document was written** though the fence permitted one, because a second document holding the same nine rows is two statements of one fact.

**`apps/**` and `packages/**` were read and not edited**, which is measured rather than intended: `git diff origin/main --stat -- 'apps' 'packages'` is **EMPTY**, and that sweep covers every `src/`, every `test/`, every `package.json` and `packages/db/migrations`.
