---
status: approved
depends_on: [README.md]
last_updated: 2026-09-05
---

## 2026-09-05 - Session 515: a refusal outlived its reason, and the assertions on it were pinning its words

<!--index: **THE RULING IS [ADR-324](../decisions/ADR-324.md), `proposed` and UNSIGNED. NOT A MIGRATION AND NO SQL IS WRITTEN; an `E2` READ IS OWED ANYWAY, because it rewrites what an operator is told when a money route refuses**, and three reads are inherited ([ADR-321](../decisions/ADR-321.md), [ADR-322](../decisions/ADR-322.md), [ADR-323](../decisions/ADR-323.md), all `proposed` and UNSIGNED). **THE BASE WAS RE-DERIVED BEFORE A LINE WAS WRITTEN**, because the previous attempt at this row was dispatched on a base without `0078` and correctly refused to build: the file is on disk, `git log --oneline origin/main -1` on it returns `7e4c6f9a` so it is MERGED and not merely present, `grep -c affiliate_commissions packages/db/src/scope.ts` returns 6, `pgTable('affiliate_commissions'` appears once in `schema.ts`, `origin/main` is an ancestor of `HEAD`, and [ALLOCATION](../decisions/ALLOCATION.md) carries rows 320 through 323. **FOUR CLAUSES OF `COMMISSIONS_UNREACHABLE` WENT FALSE THE DAY `0078` MERGED AND NOTHING WENT RED**, because the assertions matched `/UNREGISTERED/`, `/one COLUMN away/` and `/NOT WRITTEN/`, which are the WORDS and not the facts; **a fifth clause, `undeclared in packages/db/src/schema.ts`, was pinned by NOTHING AT ALL**. **THE REPAIR IS THAT THE EXPECTATIONS ARE NOW COMPUTED FROM THE TREE**: the registry rule with its `via` and `localColumn` out of [`scope.ts`](../../packages/db/src/scope.ts), the owner column out of [`schema.ts`](../../packages/db/src/schema.ts), the migration NUMBER and the column DDL out of the migration directory, the `attributions` class out of the registry, the counting door out of the `ScopedTx` INTERFACE BODY rather than the file text, and the cited ADRs out of `docs/decisions/`; each requires the SERVED message to agree in BOTH directions, and the list of retired clauses is ASSEMBLED FROM THE TREE and asserted absent from all SIX messages rather than from `stats` alone. **THE FOUR STALE PINS WERE EACH WATCHED RED SEPARATELY** after being split out of the one case that would have hidden three of them, and **FIVE DERIVATIONS WERE EACH WATCHED RED AGAINST A SEEDED TREE** (registration renamed out of `scope.ts`, owner column deleted from `schema.ts`, `0078` moved off the migration directory, a `conversionsInLast30Days()` member added to `ScopedTx`, `attributions` moved from `pair` to `firm`), every probe reverted and `git status --porcelain` checked clean before anything was committed. **THE SECOND OBSTRUCTION SURVIVES AT FULL STRENGTH AND GAINS ONE SENTENCE**: `conversions_30d` is a count over `attributions`, which is `pair`, excluded from `ScopedTableKey` AND `FirmTableKey`, `scopePredicate` throws on it, no scope class fixes it, **and no migration reaches it either**, which is the wrong inference the old message's shape invited. **THE FIFTH STALE CLAUSE WAS REAL AND SO WERE THREE CITATIONS UNDER IT**: `wiring.test.ts`'s `useAffiliateDeps` entry warned that a session dispatched there would take a number the ruling gates on a read that has not happened, and `0078` has been dispatched and merged, so it is discharged as a DISPATCH condition while the `E2` read it names is not; the same paragraph cited ADR-304 `section 10` for a judgement that lives in ADR-304's APPROVAL block, quoted ALLOCATION as carrying `0078` as "RESERVED, NOT WRITTEN, AND NO FILE EXISTS" when that row reads TAKEN AND SPENT, cited `affiliate-commissions-is-a-column-away.test.ts` as asserting the column's ABSENCE when ADR-321 rewrote it to assert what `0078` did, and pointed at `affiliate.ts:478` for a declaration that was at `:590` on this branch's base. **NOTHING IS WIRED**: `unwired('stats', ...)` stands, no adapter and no counting door is written, `scopePredicate` and the six scope classes are untouched, `packages/db/**` is untouched, and no migration, `RI-nn` or `SD-nn` number is taken. **FOUR LANDMINES REPORTED AND NONE FIXED**, the first being that `wiring.test.ts`'s fourteen `BLOCKED` reasons are asserted for LENGTH and for not saying "not wired" and for nothing else, which is why five false clauses sat in one of them for two waves. Counts derived at reporting time off each runner's own last line: suite **305 files / 7,443 passed / 6 skipped and 0 failed**, against a base-ref baseline reproduced before a line was changed of **305 files / 2 failed / 7,430 passed / 6 skipped**, both baseline failures 5000ms timeouts under full-suite load; the delta is **+11 cases and zero files**, all eleven of them this row's own (`affiliate-stats-obstructions.test.ts` goes from 8 cases to 19), and **both baseline timeouts passed on this run**; gates **33 of 33** after `generate`, invariants **32 of 32**, lint clean, `format:check` clean, typecheck clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `324`: a shipped refusal outlived its reason and nothing went red. Rewrite `COMMISSIONS_UNREACHABLE` at [`affiliate.ts`](../../apps/api/src/routes/affiliate.ts) so it says what is true after `0078`, keep the obstruction `0078` could not touch at full strength, and **rewrite the assertions on it to pin facts DERIVED FROM THE TREE rather than phrases**.

**One objective.** The message is one edit; the assertions are the row.

---

### 2. The base, re-derived rather than inherited

The previous attempt at this row was dispatched on a base that lacked `0078`, correctly refused to build, and stopped. The dispatch said to re-derive all of it, and that instruction was right the first time, so it was re-derived:

```
$ ls packages/db/migrations/0078*
packages/db/migrations/0078_affiliate_commission_owner.sql
$ grep -c affiliate_commissions packages/db/src/scope.ts
6
$ grep -c "pgTable('affiliate_commissions'" packages/db/src/schema.ts
1
$ git merge-base --is-ancestor origin/main HEAD && echo ANCESTOR_OK
ANCESTOR_OK
$ git log --oneline origin/main -1 -- packages/db/migrations/0078_affiliate_commission_owner.sql
7e4c6f9a feat(db): 0078 gives a commission the affiliate it is owed to (ADR-321)
```

**The last one is the one the dispatch did not ask for and it is the one that matters**: the file being present on this branch would also be true if it were only unmerged work below it. It is on `origin/main`.

[ALLOCATION](../decisions/ALLOCATION.md) carries rows `320`, `321`, `322` and `323`, so `324` is the next integer and opens no `CI-06f` hole; the base is [PR #677](https://github.com/lam2-coder/meritfutures/pull/677)'s head, which carries `0080`/ADR-322 and `0081`/ADR-323 unmerged but present.

---

### 3. What was false, measured at the constant

`COMMISSIONS_UNREACHABLE` served five claims that `0078` retired:

| Clause | What the tree says |
|---|---|
| *"`affiliate_commissions`, which is UNREGISTERED in `packages/db/src/scope.ts`"* | registered `derived` via `affiliates` on `affiliate_id` as a `hop` |
| *"and undeclared in `packages/db/src/schema.ts`"* | `affiliateId: uuid('affiliate_id').notNull()` inside the `pgTable` body |
| *"it is one COLUMN away, `affiliate_id uuid NOT NULL REFERENCES affiliates(id)`"* | `0078:126` declares exactly that, with `ON DELETE RESTRICT` |
| *"Migration `0078` is RESERVED for it ... and is NOT WRITTEN"* | `0078_affiliate_commission_owner.sql`, merged at `7e4c6f9a` |
| *"a session dispatched at this entry today would be taking a number the ruling gates on a read"* (`wiring.test.ts`) | the number was dispatched and merged |

**Four of those were asserted somewhere and the fifth was asserted nowhere.** The `undeclared` clause was written into the message by ADR-304 and never pinned by anything, so it could not even go stale loudly.

---

### 4. Watched RED, twice

**First the four stale pins, split so no failure could hide another.** The single case asserted them in one block; against the rewritten message it was split into four and each was watched failing on its own:

```
     × WATCH-1 UNREGISTERED 5ms
     × WATCH-2 undeclared in schema.ts 1ms
     × WATCH-3 one COLUMN away 1ms
     × WATCH-4 NOT WRITTEN 1ms
      Tests  4 failed | 7 passed (11)
```

`WATCH-2` is the clause nothing had ever asserted, written here only so it could be watched failing like the others.

**Then the five derivations, each against a seeded change to the thing it derives from:**

```
### PROBE A: the registration removed from scope.ts
     × what it says about `scope.ts` is what `scope.ts` says 7ms
      Tests  1 failed | 18 passed (19)
### PROBE B: the owner column removed from schema.ts
     × what it says about `schema.ts` is what `schema.ts` says 6ms
      Tests  1 failed | 18 passed (19)
### PROBE C: 0078 renamed off the migration directory
     × what it says about the migration is what the migration directory says 7ms
     × the column it quotes is the column the DDL declares, character for character 1ms
      Tests  2 failed | 17 passed (19)
### PROBE D: a counting door added to ScopedTx
     × it waits on a counting door for exactly as long as no counting door exists 6ms
      Tests  1 failed | 18 passed (19)
### PROBE E: attributions moved off pair
     × it names the class `attributions` is actually registered under 11ms
     × it says no class reaches it, and says so only while that is what the registry does 2ms
      Tests  2 failed | 17 passed (19)
### CLEAN TREE
      Tests  19 passed (19)
```

**Every probe was reverted and `git status --porcelain` showed only this row's three files before anything was committed.** Nothing was deleted and nothing was loosened; the file goes from 8 cases to 19 and all eight original claims are still asserted, three of them now computed.

---

### 5. What the message says now

**TWO obstructions, one DISCHARGED and one LIVE, and neither half deleted.** It names the registration and the declaration that closed the first, quotes the column the DDL actually declares, records that the class question is settled as `derived` and **not** `firm` while keeping the sentence saying why `firm` would have been wrong, and then carries the second obstruction unchanged in substance: `conversions_30d` over `attributions`, `pair`, excluded from both narrow keys, `scopePredicate` throws, no scope class fixes it, a NAMED DOOR returning one integer is what serves it.

**One sentence is added to the second half rather than to the first**: *no migration reaches it either, so `0078` neither helped it nor could have*. That is the inference the old message's ordering invited and the reason this row exists at all.

**The four-step closing sentence is updated and not replaced.** It still names a migration, a registration, a counting door and an adapter in that order, and says the first two have landed.

**`unwired('stats', COMMISSIONS_UNREACHABLE)` is untouched.** The endpoint refuses exactly as it did.

---

### 6. Which assertions became derivable, and which stayed textual

**Derivable, in [`affiliate-stats-obstructions.test.ts`](../../apps/api/test/affiliate-stats-obstructions.test.ts)**: the registered class with its `via` and `localColumn`; whether the owner column is declared in `schema.ts`; which migration file adds it, its NUMBER, and the column DDL with the `ON DELETE` tail stripped; the class `attributions` is registered under; whether a counting door exists, read off the `ScopedTx` interface BODY so that a comment using the word is not mistaken for a door; whether every `ADR-nnn` the message cites exists under `docs/decisions/`; and the LIST of retired clauses, assembled from the first three of those and asserted absent from **all six** messages.

**Textual, and named as textual in [ADR-324](../decisions/ADR-324.md) section 4 rather than left to look derived**: that `firm` is FALSE because a commission is what Merit owes a named affiliate; that ADR-106's exclusion is about what a ROW read returns; and that a NAMED DOOR is the right construction for a count. **Each is a RULING, nothing in this tree computes it, and each moves by an ADR rather than by a file edit.** The derivable half of the `firm` sentence is asserted beside it: `firm` is a class the registry really has, so the mistake is available rather than hypothetical.

**Every derivation is guarded for non-vacuity in its own describe block**, because a parser that silently matched nothing would reproduce this row's defect one layer down.

---

### 7. The `useAffiliateDeps` entry, and why nothing there could have gone red

`wiring.test.ts`'s four `BLOCKED` cases assert that every declared port is wired or blocked, that no blocked port is also wired, that no blocked port was renamed away, and that **every reason is longer than 80 characters and does not merely say `not wired`**. That is the whole of it. **The five false clauses in this entry were pinned by nothing, so there was no red to watch and none is claimed.** The entry is rewritten as prose: the retired clauses are quoted rather than deleted, the live obstruction is named, the founder gate is discharged as a dispatch condition while the `E2` read it names is not, and its three stale citations are repointed. The port stays BLOCKED and the wired count does not move.

---

### 8. Landmines found and left

1. **`wiring.test.ts`'s fourteen `BLOCKED` reasons are asserted for LENGTH and for nothing else.** Each is a paragraph of checkable claims with file-and-line citations and no gate reads any of them. The generalisation of this row is a case that resolves those citations and fails on the ones that no longer do; it is not written here because it would touch all fourteen entries.
2. **[`affiliate.ts`](../../apps/api/src/routes/affiliate.ts)'s module header restates the port's refusals in prose a second time.** ADR-253 section 5 found the same duplication and repaired both copies; ADR-304 wrote a fresh copy into the header anyway; this row repaired both again. Two statements of one fact drift, and the header copy is asserted by nothing.
3. **[STATE](../STATE.md)'s session 512 section still says this repair "should be the next row on this path".** It is a dated record and is left, but a reader scanning STATE for what is next reads a resolved instruction.
4. **Two full-suite timeouts, both on the base ref before a line was changed.** `RI-12` is the known one, recorded by ADR-322; `apps/api/test/checkout.test.ts`'s *produces THE PRICE THE SERVER COMPUTED on the honest path* is a second of the same class and is new. Both fail at vitest's 5s default under full-suite load and both pass alone.

---

### 9. Files touched

[`apps/api/src/routes/affiliate.ts`](../../apps/api/src/routes/affiliate.ts), [`apps/api/test/affiliate-stats-obstructions.test.ts`](../../apps/api/test/affiliate-stats-obstructions.test.ts), [`apps/api/test/wiring.test.ts`](../../apps/api/test/wiring.test.ts), [`ADR-324`](../decisions/ADR-324.md), [ALLOCATION](../decisions/ALLOCATION.md), [STATE](../STATE.md), this log, and the two generated registry READMEs.

**No SQL. No migration. `packages/db/**` untouched. Nothing wired. Nothing signed.**

---

### 10. Next

The founder's `E2` read of [ADR-324](../decisions/ADR-324.md), and the three questions its approval block names: whether the message should lead with the discharged half or the live one, whether a settled class question belongs in a live refusal at all, and whether refusal TEXT is inside `E2` for every row of this class.

Then the path this refusal now describes, in its own order: **the counting door for `conversions_30d`**, a named door returning one integer on ADR-262's and ADR-265's construction, and then **the `stats` adapter**.
