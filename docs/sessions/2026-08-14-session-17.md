## 2026-08-14 - Session 17: the ADR collision resolved, and two gates that make it structural
<!--index: the ADR collision resolved, and two gates that make it structural -->
- **Done.** Three branches now hold disjoint ADR numbers, and the mechanism that let them collide is closed rather than cleaned up after.
  - **[ADR-034](../decisions/ADR-034.md), and the number allocation table at the top of [DECISIONS](../decisions/README.md).** A number is **claimed before the ADR is written**. `001-025` merged, `026-032` reserved by PR #4, `033` by PR #5, `034` by this branch.
  - **[CI-06f](../testing/STRATEGY.md): ADR headings unique, and gapless over allocated PLUS reserved.** The union is the subtle half. **A branch cannot see the numbers its siblings hold**, so a naive gapless check fails a branch legitimately holding a reserved-but-unmerged number. Under this one, a hole in `DECISIONS.md` matching a reservation passes and an unreserved number fails.
  - **[CI-06g](../testing/STRATEGY.md), the COUNT GATE.** No document states a quantity a script can derive unless it sits in a generated span the script rewrites. Two ways to comply, no third: **generate it in, or delete it and point at the script.** `INDEX`'s three count claims are converted as the worked example.
  - **PR #5 renumbered in place, ADR-031 to ADR-033**, eight references across four files including `.claude/agents/reviewer.md`. **PR #4 keeps 031 and 032** because those numbers are cited inside migration comments, `DELTA_MANIFEST` and `DATA_MODEL`, and **a migration is sacred once merged**: renumbering it would mean editing files that are about to become uneditable.
- **Next.** The founder's E2 read on PR #4. Then the CI setup, where CI-06a through CI-06g stop being documents and start being jobs, and where **the COUNT GATE's first run is a corpus sweep** that will find more than the five known instances.
- **Blockers.** None.
- **Landmines.**
  - **Both gates are documented and not running, and so are CI-06a to CI-06e.** There is no CI in this repository at all. **A gate inventory is not a gate**, and this corpus now has seven corpus-integrity checks that exist only as prose describing checks. The gap between "specified" and "wired" is where CI-06d's own ancestors already failed.
  - **The renumber rewrote references inside the append-only SESSION_LOG**, against the usual annotate-in-place rule. Deliberate: **an ADR number is an identifier, and a stale one now resolves to a different ruling** rather than merely reading wrong. The entry carries an annotation saying so. Anyone applying the append-only rule mechanically to a future identifier change should read that entry first.
  - **Three branches append to this file and to `INDEX`, and all three will conflict at merge.** Trivially resolved (keep both), but the resolver must not drop an entry, and **the merge order determines whether `INDEX`'s generated spans are correct on arrival** since PR #4 adds seven ADRs. The spans are the fix for exactly this and the generator does not exist yet, so **the first merge after PR #4 lands must re-derive them by hand, once.**
  - **PR #4 merged while this session was still open, and the prediction above fired immediately.** `main` went to 32 numbered ADRs; its `INDEX` said **30**, corrected on that branch to its own truth before two further ADRs landed in the same pull request. **That is the sixth hand-maintained count found wrong, on the day CI-06g was ruled, in the file the ruling had just edited.** Resolved here: three conflicts merged keeping both sides, the allocation table moved to `001-032 merged / 033 reserved / 034 reserved`, and the `adr_count` span re-derived to **33** for this branch. **The re-derivation was still done by hand, because the generator does not exist**, which is the whole of what CI-06g is buying and the reason it is worth buying.
  - **A derivable count still needs its query named.** Counting registry table rows gives 22 edge cases and 301 golden scenarios; counting **distinct identifiers** gives the correct 140 and 257. Both are scripts. **"A script can derive it" is not a specification**, and the first draft of this gate implied it was.
  - **`main` moved mid-session** (PR #3, the SessionStart hook fix and the post-FREEZE cadence). Merged in; no overlap with these three files.
- **Files touched:** docs/testing/STRATEGY.md; docs/DECISIONS.md; docs/INDEX.md; docs/SESSION_LOG.md. On `claude/builder-reviewer-loop-rykvhs`: docs/{DECISIONS,INDEX,SESSION_LOG}.md and .claude/agents/reviewer.md.

---
