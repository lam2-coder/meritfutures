---
status: approved
depends_on: [README.md]
last_updated: 2026-09-05
---

## 2026-09-05 - Session 533: the adapter was complete, the install is refused, and the door turns out to be shut by something no entry had named

<!--index: **THE ROW IS [ADR-342](../decisions/ADR-342.md), `proposed` and UNSIGNED. MONEY PATH, `E2` READ OWED.** The row was dispatched to install `databaseWithdrawalBackend` and **the install is REFUSED with the argument**. **THE ADAPTER IS NOT WHAT REFUSES**: it is whole, **eleven of `WithdrawalTx`'s eleven members served**, lock first, every arm through `db.scoped`, and `parseFloat`, `toFixed`, `Math.round`, `Math.floor` and `Math.ceil` occur **zero** times as calls in the module. **EVERY CLAIM THE DISPATCH MADE REPRODUCES**: both rows answer through `UNWIRED_WITHDRAWAL_BACKEND`, `start.ts` has exactly **10** install calls and `useWithdrawalBackend` is not among them, and `wiring.test.ts` asserts `{declared: 24, wired: 10, blocked: 14}`. **THE HEADLINE IS THE ONE BLOCKER NO ENTRY HAD EVER NAMED AND IT IS THIS DEPLOYABLE'S OWN**: `C-27` refuses every caller of both rows BEFORE any handler body runs, and `databaseAuthBackend` declares `elevate` **blocked on BOTH arms**, so no session in a deployment can be elevated, no request reaches this port, and **the 503 it is famous for is itself unreachable**. **THE DISPATCH READ THAT AS A CAVEAT AND IT IS THE DECIDING FACT IN THE OPPOSITE DIRECTION**: an install would change no observable response, would be exercised and falsified by nothing, and would **arm itself silently** on the day an unrelated auth row lands a WebAuthn ceremony or an SMS sender. An install nobody notices is this branch's own defect with the sign flipped, and it is the worse one. **PAST THE INSERT THE ESTATE STILL CANNOT PAY**: the approval sweep now has a driver, an exported `LT-06` builder, a posting site and a manifest line, and it has **no IO adapter and no clock**, held by a rail `apps/worker/src/index.ts` says in terms is FOUNDER-OWED. **FOUR SENTENCES IN `wiring.test.ts`'s BLOCKED ENTRY WERE FALSE** and are corrected in place as its **tenth** correction, all four kept where they were made: no door drives `requested --> approved`, `@merit/ledger` publishes no `LT-06` builder, the manifest line is owed, and the entry is no longer waiting on a slice that could arrive here. **FOUR BLOCKERS DISSOLVED AND THE PORT DID NOT MOVE**, because what actually refuses was never any of them. **THE REFUSAL IS NO LONGER PROSE**: `INSTALL_BLOCKING_FINDINGS` carries three findings with sources and the suite **RUNS** each, so the day the IO adapter, the clock, `elevate` or the rail lands, the suite goes RED and the install is due a re-decision. **SEVEN SEEDS WATCHED RED AND RESTORED GREEN, AND SEED 4 FOUND A DEFECT IN THIS ROW'S OWN CHECK**: `toContain` over the whole ledger index read a **commented-out export as an export**, so the case held while the package published nothing; the assertion now extracts the export block and requires a live specifier. **SEED 2 FAILED AS A SEED RATHER THAN AS A CHECK** and is recorded, because a seed that does not reproduce the violation proves nothing. **THREE CITATIONS DRIFTED BY THIS ROW'S 106 INSERTED LINES AND `RI-15` AND `RI-16` CAUGHT ALL THREE**; a **fourth was already wrong at baseline** and passed only because its line was non-blank, and it is repointed at the line that holds the claim rather than at 1527 plus 106. **ONE BASELINE READING WAS AN ARTEFACT AND IS RECORDED**: the first invariants run reported `RI-18` ERRORED and *"this run did not measure the tree"* because `node_modules` was absent, not because of the tree. **NOTHING WAS WEAKENED**: `required` is untouched on both rows, no second refusal is added, and `{declared: 24, wired: 10, blocked: 14}` is UNCHANGED because the wired count did not move. **THREE RESERVATION ROWS WRITTEN FOR `339`, `340` AND `341`, EACH CLAIMING NOTHING**, one naming its branch and two recording that they could not. **A DISPATCHER ADDITION ARRIVED AFTER THE WORK AND NAMED THE RAIL TRAP: does an installed door strand money?** Answered at the migrations rather than trusted, and its standing note is **wrong about the mechanism** while its conclusion stands. **The request path debits nothing**: the adapter's only writes are one `payoutDestinations` insert, one `walletWithdrawals` insert and two `walletWithdrawals` updates, there is **no `insert('walletEntries')` anywhere under `apps/api/src`**, and the module posts no ledger transaction. **The debit lives in the approval sweep**, in one transaction with the `approved` transition. **And `0072`'s `WD-C2` refuses a cancel only from outside `requested` and `cooling`**, which are exactly the two states this door can create, so every row it makes is cancellable. **The stranding scenario does not exist on the request path**, and the install stays refused on sections 4 and 5 instead. **The approval sweep was NOT scheduled and no rail adapter was written**, both out of fence. **NO SQL, NO MIGRATION WRITTEN AND NONE EDITED.** Counts at reporting time: suite **312 files / 7,671 passed / 10 skipped / 0 failed** against a `c0d15f0c` baseline reproduced BEFORE the first edit of **312 / 7,666 / 10 / 0**, a delta of **+5 cases in one existing file with SKIPS UNCHANGED and no file added**; invariants **35 of 35** and gates **33 of 33**, both unchanged; lint, `format:check` and typecheck clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `342`: read `databaseWithdrawalBackend` adversarially **first**, install it only if it is sound, and report plainly if it is not.

**One objective.** The reading was the work and the answer was not the one the row's shape anticipated: the adapter is sound and the install is still wrong.

---

### 2. What was re-derived before the first edit

**Every claim the dispatch made reproduces**, and two facts it did not name are sections 4 and 5 of the ADR.

| Claim | Held |
|---|---|
| Both rows answer through `UNWIRED_WITHDRAWAL_BACKEND` | **YES** |
| `databaseWithdrawalBackend` is fully written | **YES, AND STRONGER**: eleven declared `WithdrawalTx` members, **eleven served**, none stubbed |
| `start.ts` contains exactly **10** install calls, without `useWithdrawalBackend` | **YES** |
| `wiring.test.ts` asserts `{declared: 24, wired: 10, blocked: 14}` | **YES** |
| Something asserts this port's absence | **NO.** `absence-claims.mjs` names no withdrawal artifact; the claim lives in `wiring.test.ts`'s `BLOCKED` entry, which `RI-14` reads for exports only |
| `POST /auth/elevate` is blocked, so the route may be unreachable | **YES, AND IT IS DECISIVE** |

**Baseline on `c0d15f0c`, before the first edit:** suite **312 files / 7,666 passed / 10 skipped / 0 failed**; invariants **35 of 35**; gates **33 of 33**; lint, `format:check`, typecheck clean; `git diff --stat origin/main -- packages/db/migrations scripts/db` **EMPTY**.

**The first invariants run said `RI-18` ERRORED and that the run did not measure the tree.** The cause was an uninstalled workspace in a fresh container. After `pnpm install --frozen-lockfile` the same command reported **35 of 35**, and that is the figure above. A session that had quoted the first run would have carried an unknown into every number under it.

---

### 3. What landed

1. **`INSTALL_BLOCKING_FINDINGS`** in [`routes/wallet-withdrawals.ts`](../../apps/api/src/routes/wallet-withdrawals.ts), three entries on `TERMINAL_EDGE_FINDINGS`' `{id, claim, ruled, sources}` shape.
2. **Five cases in [`wallet-withdrawals.test.ts`](../../apps/api/test/wallet-withdrawals.test.ts)** that **RUN** those findings against the tree, including the discharged halves, so the retired claims cannot return without landing red.
3. **The tenth correction to `wiring.test.ts`'s `useWithdrawalBackend` entry**, four false sentences kept beside their corrections.
4. **Four repointed citations**, three drifted by this row and one already wrong.
5. **[ADR-342](../decisions/ADR-342.md)**, `proposed` and UNSIGNED, plus row `342` and three reservation rows.

**What did NOT land: the install.** `start.ts` is untouched and still has ten install calls.

---

### 4. The falsification transcripts

Seven seeds, each watched RED, restored, watched GREEN, at **119 passed** in the file when green.

| # | Seeded violation | Result |
|---|---|---|
| 1 | `start.ts` calls `useWithdrawalBackend(...)` | **RED**, restored **GREEN** |
| 2 | the approval job's row becomes `disposition: 'scheduled'` | **RED** on the second attempt, restored **GREEN** |
| 3 | a second `WithdrawalApprovalSweepIo` is declared | **RED**, restored **GREEN** |
| 4 | `walletWithdrawalApprovalPosting` commented out of the ledger index | **GREEN, and the check was wrong** |
| 5 | the worker loses its `@merit/ledger` manifest line | **RED**, restored **GREEN** |
| 6 | `POST /wallet/withdrawals` drops to `required: 'session'` | **RED** at three cases, restored **GREEN** |
| 7 | `databaseAuthBackend` unblocks `elevate` | **RED**, restored **GREEN** |

**Seed 4 is the one worth reading.** The assertion was `toContain` over the whole file, and commenting the export line **out** leaves the identifier in the file, so the case was reading a comment as an export. It now extracts the `export { ... } from './reversal.ts'` block and requires a live specifier. **Seed 2's first failure was the seed and not the check**: the script took the first occurrence of `runWithdrawalApprovals`, which is an import, and corrupted an unrelated job's row.

---

### 5. Blockers

**The install itself, and it is not an engineering blocker.** The port waits on a payment rail that `apps/worker/src/index.ts` and `schedule.ts` both record as FOUNDER-OWED, and on an elevation factor that is a different row's subject. **Neither is a slice anybody can dispatch here.**

---

### 6. Landmines

1. **FOUR OF THE `BLOCKED` ENTRY'S SENTENCES WERE DISCHARGED BY ROWS IN OTHER DEPLOYABLES AND NOTHING NOTICED.** `wiring.test.ts` records why `apps/api` ports are unwired, and nothing in this repository reads an `apps/api` reason against `apps/worker`'s tree. This row closes that for **one** port by executing its claims. **Every other `BLOCKED` entry has the same exposure**, and the general repair is a check that reads each entry's cited artifacts the way `RI-35` reads absence claims. `packages/tooling`'s subject, owed.
2. **A CITATION CAN BE WRONG AND PASS `RI-15` BY LANDING ON A NON-BLANK LINE.** `adr-172-transaction-dependencies.test.ts:45` pointed at the wrong line for an unknown number of sessions and was exposed only because this row's insertion made that line blank. The check's other citation forms match a NAME; this form matches only emptiness.
3. **SECTION 4'S UNREACHABILITY CLAIM IS ABOUT THE ADAPTER THIS REPOSITORY SHIPS**, not about every `AuthBackend` that could be installed. The test reads `auth-backend.ts` for `elevate: blocked(`. A deployment installing a different auth backend is outside anything asserted here.
4. **FINDING `D`'s IO SCAN EXCLUDES TEST FILES.** An adapter constructed inside a suite and exported from it would not be seen. Judged acceptable because a value no deployment can reach is not a wiring, and stated rather than left to be found.

---

### 7. Files touched

- [`apps/api/src/routes/wallet-withdrawals.ts`](../../apps/api/src/routes/wallet-withdrawals.ts) - `INSTALL_BLOCKING_FINDINGS`, 106 lines. No arm of the adapter changed.
- [`apps/api/test/wallet-withdrawals.test.ts`](../../apps/api/test/wallet-withdrawals.test.ts) - five cases that run the findings.
- [`apps/api/test/wiring.test.ts`](../../apps/api/test/wiring.test.ts) - the tenth correction.
- [`apps/api/test/adr-172-transaction-dependencies.test.ts`](../../apps/api/test/adr-172-transaction-dependencies.test.ts) - one citation, already wrong.
- [`apps/worker/src/withdrawals/approval-sweep.ts`](../../apps/worker/src/withdrawals/approval-sweep.ts) - two citations, drifted by this row.
- [`docs/decisions/ADR-342.md`](../decisions/ADR-342.md), [`ALLOCATION.md`](../decisions/ALLOCATION.md), [`README.md`](../decisions/README.md), [`docs/STATE.md`](../STATE.md), this log.

**`apps/api/src/start.ts` is NOT in this list, and that is the row's outcome rather than an omission.**
