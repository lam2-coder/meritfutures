---
status: approved
depends_on: [README.md]
last_updated: 2026-08-28
---

## 2026-08-28 - Session 320: `P7-k`, and `INV-M5-12` turns out to be expressible as a write union with one member

<!--index: **[P7](../plans/P7-risk-and-abuse.md) section 8's `P7-k`, the THIRD DONE-GATE. `plan_breaker_state` landed in [`0016`](../../packages/db/migrations/0016_treasury_controls.sql) and [`apps/admin/src/page.ts`](../../apps/admin/src/page.ts) has said *"nothing writes it"* ever since; it has a writer now.** `GS-113` is asserted with its NEAR-MISS beside it. **`INV-M5-12` IS THE WRITE UNION AND NOT A COMMENT**: `BREAKER_WRITE_TABLES` has EXACTLY ONE MEMBER, so no payout, wallet, halt or restriction table is reachable through the port, while the READ side carries `payoutRequests` because the loss ratio's numerator IS settled payouts. **THE NAMED SEED WAS RUN IN BOTH DIRECTIONS AND THE SECOND IS THE ONE WORTH READING**: written straight, a payout insert is a COMPILE ERROR (`TS2345`); written with the cast that gets past `tsc`, **the typechecker reports ZERO errors** and ten cases go red, which is why the invariant is enforced at four levels rather than one. **`insufficient_data` IS THE STATE BELOW THE MINIMUM AND SALES ARE NOT PAUSED THERE, AND THE LADDER'S ORDER IS THE CONTROL**: the floor is tested BEFORE the ratio and the ratio is not consulted below it, because a ladder that computed an opinion and discarded it is one edit from reporting it. `AS-M6-02`'s own scenario is the fixture: one purchase, one 150,000c payout, **151,515bp against a 6,000bp threshold**, and `salesPaused` FALSE. **`sample_size` IS WRITTEN BESIDE `min_sample` ON EVERY ROW AND EVERY STATE** and `breaker.state_changed` carries both as REQUIRED fields, transcribed from [M06:265](../plans/M06-admin-ops-console.md) parsed out of the document; **that event has NO row in [EVENTS](../architecture/EVENTS.md)** and the absence is PINNED. **`ADR-167` SECTION 5 IS APPLIED AND THE TERM IT COULD NOT PLACE IS APPLIED WHERE THE ENTRY PUT IT**: `sample_size` is the DENOMINATOR's count, and `OQ-M6-02`'s second term reaches `decideState` and NO COLUMN AT ALL, because a row holding the payout count *"would satisfy every CHECK in `0016` and describe the wrong population"*. **NO NUMBER IN THIS SLICE IS THIS SESSION'S**: both `OQ-M6-02` terms ship `unstated`, so **the evaluator DECLINES**, a floor of zero being refused by `min_sample > 0` and a floor of one being `AS-M6-02` produced deliberately. **THE CUSUM IS FOLDED IN INTEGER BASIS POINTS AND RENDERS ABSENT** (`ADR-167` clauses 4 and 5), with the ONE remaining division declared rather than hidden: the pass rate truncates downward, at most 1bp per day, and the bias accumulates toward NOT alarming. **THE API HALF DECLARES NO PORT AND THAT IS A RULING**: `CompositionReport` moves operator `registered` 24 to 26 and public `withheld` 23 to 25, and [`wiring.test.ts`](../../apps/api/test/wiring.test.ts)'s `{declared: 21, wired: 6, blocked: 15}` triple DOES NOT MOVE, because both endpoints are section 8's *"focused projections of the same underlying data"* and compose with `admin-reads.ts`'s handler. **A POPULATED CUSUM ON THE SOURCE RENDERS AS `null`**, because `DEP-M6-05` is Wave 4 and a second surface drawing the chart `apps/admin` refuses to draw is the same failure with a different renderer. **THREE ABSENCES ARE REPORTED AND NOT REPAIRED**: `per_plan[].sample_size` and the three-state distinction behind `sales_paused` both need a field on `LiabilityResponse.per_plan`, which is `P7-b`'s file; and [`admin-reads.test.ts`](../../apps/api/test/admin-reads.test.ts)'s unserved register is **STILL GREEN while two of its three rows are now SERVED**, because it derives `served` from `ADMIN_READ_ENDPOINTS` rather than from the composition report. **THE BARREL BIT EXACTLY AS THE DISPATCH WARNED**: deleting BOTH `detectors/graph.ts` legs leaves `typecheck` at **ZERO ERRORS**, and one assertion catches it, the barrel's **17** legs enumerated. **A LATENT DEFECT IN [`db.test.ts`](../../apps/worker/test/db.test.ts) WAS FOUND BY WALKING INTO IT**: its bare-specifier scan reads a WRAPPED message ending in the word *"from"* as an import, the exact hazard `admin-reads.test.ts` anchors against and this one does not. **HALF of [CRON_INVENTORY](../ops/runbooks/CRON_INVENTORY.md)'s numbered finding is closed and the row says which half.** **Fifteen defects seeded and fifteen caught.** 33 of 33 gates, 13 of 13 invariants, typecheck 0, lint 0, format clean, **206 files / 4,515 passed / 6 skipped** against a `main` baseline of 204 / 4,443 / 6 reproduced exactly first, `falsify` clean. **NO ADR NUMBER, NO MIGRATION NUMBER, no `packages/db` change, no contract change, no `SqlExecutorReason` or `SystemReason` member, no `pg` import and no cast past a key type.** -->

- **Objective:** [P7](../plans/P7-risk-and-abuse.md) section 8's `P7-k`, the **third done-gate**: the
  breaker and CUSUM evaluator. `plan_breaker_state` gains its daily producer, `sample_size` is written
  beside `min_sample`, `insufficient_data` is the state below the minimum and sales are NOT paused,
  and `breaker.state_changed` carries the sample size into the alert. `GS-113`. **`INV-M5-12` binds:
  the breaker pauses SALES and can never pause payouts.**

- **Done:** **Five files, 72 new cases.**
  [`apps/worker/src/breaker/ports.ts`](../../apps/worker/src/breaker/ports.ts) (the read and write
  unions, the state vocabulary transcribed from `0016`'s `CHECK`, `ADR-157`'s two terms, the policy
  with three numbers cited and two `unstated`, `breaker.state_changed`'s payload, the event port, the
  unwired default), [`evaluate.ts`](../../apps/worker/src/breaker/evaluate.ts) (row readers that
  refuse rather than coerce, the `bigint` fold, the exact integer ratio, the ladder, `salesPaused`,
  the override carry-forward, the row builder, the event builder, the CUSUM fold and its absence, and
  the run), [`test/breaker.test.ts`](../../apps/worker/test/breaker.test.ts) (**52 cases in eight
  sections**), [`apps/api/src/routes/admin-breaker.ts`](../../apps/api/src/routes/admin-breaker.ts)
  (`GET /admin/loss-ratios` and `GET /admin/cusum`, declaring **no port**) and
  [`test/admin-breaker.test.ts`](../../apps/api/test/admin-breaker.test.ts) (**20 cases**). Plus one
  row in [CRON_INVENTORY](../ops/runbooks/CRON_INVENTORY.md) and the barrel's export block.
  **Every claim above was measured on this branch with each command run separately**, and the `main`
  baseline was reproduced before a line changed.

- **Next:** **The adapter, and it is one manifest line and one call.** `BreakerIo` is satisfied
  structurally by [`src/db.ts`](../../apps/worker/src/db.ts)'s `LIVE_DB.batch` with no `SystemReason`
  member added, so what is missing is the scheduler and the `tradingDayOf` supplier rather than a
  door. **`OQ-M6-02` goes to the founder and nothing runs until it comes back**: the evaluator
  declines by design. **`LiabilityResponse.per_plan` needs `sample_size` and the breaker state**,
  which is `P7-b`'s file and closes both API gaps at once. **`breaker.state_changed` needs its
  [EVENTS](../architecture/EVENTS.md) row**, which `P5-n` reaches. **`P7-l`'s
  `weekly_loss_ratio_cusum` digest inherits this slice's absence and its reason**: a digest that
  renders an uncalibrated CUSUM is `FM-M6-07` delivered weekly by email.

- **Blockers:** **`OQ-M6-02` is unanswered and it is the founder's**, so no evaluation runs today and
  the two minimum terms ship `null`. **`DEP-M6-05` is M06 Wave 4**, so the CUSUM has a fold and no
  parameters, and the endpoint renders absence. **The two `admin-breaker` gaps cannot be closed
  inside this fence**: both need a field on `LiabilityResponse.per_plan`, and that type lives in
  `admin-reads.ts`, which `P7` section 9 gives to `P7-b` alone. **A port of this module's own would
  have closed them and would have moved `wiring.test.ts`'s triple**, whose file is session 316's this
  wave, so the gaps are stated on the wire rather than the fence widened.

- **Landmines:** **`admin-reads.test.ts`'s unserved register is now false by implication and is
  GREEN.** Two of its three rows are served by `admin-breaker.ts` and it cannot see them, because
  `served` comes from `ADMIN_READ_ENDPOINTS` and not from `CompositionReport`. **`db.test.ts`'s
  bare-specifier scan reads a wrapped message ending in the word "from" as an import**, and this
  slice worked around it by rewording rather than by widening the scan. **The barrel's legs are now
  asserted by count and by name in `breaker.test.ts`**, which is a control in a file `P7-l` and
  `P7-m` do not hold: a slice that adds a leg must add it to that list or go red, and that is
  deliberate, because the alternative is the merge that deleted both sides of a hunk and typechecked.
  **`ratio_bp` is `0` in the column when there is no ratio**, with `insufficient_data` beside it and
  the `null` preserved on `BreakerDecision`; a reader taking the column alone will read a plan with no
  fees as the healthiest plan Merit sells. **`applyOverride`'s expiry comparison is STRICT** and an
  override expiring exactly now has expired.

- **Files touched:** [`apps/worker/src/breaker/**`](../../apps/worker/src/breaker/evaluate.ts) (new,
  2 files), [`apps/worker/test/breaker.test.ts`](../../apps/worker/test/breaker.test.ts) (new),
  [`apps/worker/src/index.ts`](../../apps/worker/src/index.ts) (one export block appended, and the
  `main()` line), [`apps/api/src/routes/admin-breaker.ts`](../../apps/api/src/routes/admin-breaker.ts)
  (new), [`apps/api/test/admin-breaker.test.ts`](../../apps/api/test/admin-breaker.test.ts) (new),
  [CRON_INVENTORY](../ops/runbooks/CRON_INVENTORY.md) (one row), [STATE](../STATE.md),
  [sessions/README](README.md) (generated span), this file. **`packages/db`, `API_CONTRACT`,
  `admin-reads.ts`, `wiring.test.ts` and `ALLOCATION` untouched.**

---

### 1. The baseline was reproduced before anything moved

`pnpm install` first, then each command separately, on `main` at `bdc99d7`:

| Command | `main` | After |
|---|---|---|
| `node scripts/corpus/gates.mjs generate && check` | **33 of 33** | **33 of 33** |
| `node packages/tooling/checks/repo-invariants.mjs` | **13 of 13** | **13 of 13** |
| `pnpm run typecheck` | exit 0 | exit 0 |
| `pnpm run lint` | exit 0 | exit 0 |
| `pnpm run format:check` | clean | clean |
| `pnpm vitest run` | **204 files / 4,443 passed / 6 skipped** | **206 files / 4,515 passed / 6 skipped** |
| `node scripts/corpus/falsify.mjs` | exit 0 | exit 0 |

`pnpm run verify` was never run: the dispatch names it as exceeding the timeout.

### 2. The fifteen seeds, and what caught each

| # | Seeded | Caught by |
|---|---|---|
| **A** | `salesPaused` widened to `state !== 'armed'` | 5 cases, including `1.3` and `3.1` |
| **B** | the ratio tested BEFORE the floor | 5 cases, all of section 3 |
| **C** | `sample_size` written as the settled-payout count | 8 cases, including `3.6` |
| **D** | `min_sample` dropped from the row | 3 cases |
| **E** | `sample_size` dropped from the alert | `3.5` |
| **F** | the ratio computed as a float and rounded | `4.1`, `4.2` |
| **G** | a CUSUM alarm reaching `decideState` | 4 cases, including `6.8` |
| **H** | a breaker barrel leg lost | 52 cases and 14 typecheck errors |
| **I** | `OQ-M6-02` quietly answered with `20` | `7.1`, `7.2` |
| **J** | **BOTH `detectors/graph.ts` legs lost** | `8.1` **alone**, with `typecheck` at **zero errors** |
| **K** | the uncalibrated CUSUM passed through to the wire | `4.1` |
| **L** | `sample_size` omitted rather than stated | `5.2` |
| **M** | `sales_paused` derived from the ratio | `5.4` |
| **N** | a gap field named `blocked_on` | 7 cases, every 200 becoming a 500 |
| **O** | **`tx.insert('payoutRequests', ...)` in the paused branch** | **uncast: `TS2345`. Cast: typecheck ZERO, and TEN cases red** |

**Seed O is the one the dispatch named and seed J is the one it warned about.** Both were run twice,
once in the form a type checker refuses and once in the form it accepts.
