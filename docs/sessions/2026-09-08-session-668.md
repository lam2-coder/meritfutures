---
status: approved
depends_on: [README.md]
last_updated: 2026-09-08
---

## 2026-09-08 - Session 668: the eligibility test reproduces row 471's hand adjudication exactly, a published index and the site it named agree 88.9 percent of the time, and the gate is refused on the first of ADR-450's grounds rather than the second

<!--index: **THE ROW IS [ADR-473](../decisions/ADR-473.md), `proposed` and UNSIGNED. NON-MONEY DIFF, TWO NEW FILES AND NO EXISTING SOURCE FILE EDITED, `E2 READ OWED`.** [ADR-471](../decisions/ADR-471.md) section 9 item 1's price built: an eligibility pass over any `file:line` list a decision entry publishes as repairable, run when the list is COMPILED. **THE INSTRUMENT REPRODUCES ROW 471's HAND ADJUDICATION EXACTLY AND MECHANICALLY**: `ADR-469` section 10 item 1's thirteen, read `--at 7f7ba904`, come back **6 `COMMENT` and 7 `CODE`**, which is that row's seven string literals and six comments in one command. **The same thirteen indexes against this worktree give 7 and 6 and are a different set of lines entirely.** **THE GATE IS REFUSED AND `ADR-471`'s OWN GROUND IS THE WEAKEST OF FOUR.** That entry refused it on [ADR-450](../decisions/ADR-450.md) section 6's SECOND ground, an undischargeable red in a dated record. **The decisive ground is the FIRST: a gate cannot see its own motivating case.** The corpus publishes a repair list as one path and a run of bare `` `:NNN` ``, so a path-keyed harvester sees `ADR-469`'s thirteen as ONE site; measured over `docs/decisions/`, **3,114 path-keyed sites against 1,357 reachable only by inheritance**, 30 percent invisible, and **15 against 37 in `ADR-469` itself**. **The second new ground is structural: a gate reads the working tree and has no ref**, and **11.1 percent of sites classify differently at the worktree than at the ref their entry names**, 19 `COMMENT` to `CODE` and 20 the other way. **The third is scale**: the gate would ship with **1,678** undischargeable findings, four times the 402 `ADR-450` reported. **THE POPULATION, DERIVED: 280 of 455 entries publish a source `file:line`, 2,894 sites, 1,216 ELIGIBLE and 1,678 INELIGIBLE, `MIXED` ZERO.** Over the 52 entries naming a resolvable base ref, **718 sites read at the ref: 267 `COMMENT`, 410 `CODE`, 27 `RANGE`, 9 `BLANK`, 5 `ABSENT`; 638 of 718 identical either reading.** **TWENTY-SEVEN PUBLISHED POINTERS WERE ALREADY OUT OF RANGE ON THE DAY THEY WERE WRITTEN AND ONLY ONE STILL LOOKS IT.** **The owed subset is 240 sites, 101 eligible: 58 percent of everything this corpus publishes as owed is a site no repair may touch.** **THE CONTROL WATCHED FIRING AND NOT OVER-FIRING ON THE REAL TREE**: the site derived by grep, the pointer text re-wrapped in a string literal one line for one, `INELIGIBLE CODE` at that index and **EXACTLY ONE of 2,378 indexes moving verdict**, restored, `git status --porcelain` EMPTY. **THE FOURTEENTH SITE IS DISCHARGED AND NOTHING WAS EDITED, AND THE DISPATCH'S HYPOTHESIS IS WRONG TWICE.** `absence-claims.test.ts:151` is `claim: OCCURRENCE_1,` at this base and byte-identically on `origin/main`; the comment is at **`:138`**, thirteen lines up. **It is NOT a seed, it is a comment and ELIGIBLE**, and applying the test at the published index without a ref returns INELIGIBLE and confirms the wrong hypothesis with a correct procedure. **AND ELIGIBLE IS NOT WARRANTED**: the sentence is a REF-QUALIFIED USE into an immutable blob, checked at primary source after unshallowing this clone, and `OCCURRENCE_1` is **92 bytes byte-identical** to line 525 of `git show cd5dc8f9^:apps/api/src/routes/affiliate.ts`. A pointer into an immutable blob cannot go stale. **`:151` IS A CITED LINE**, 4 of 18 citations into that file on `origin/main`, and nothing moved because nothing was edited. **THREE OF `ADR-471`'s FIGURES DO NOT RE-DERIVE AND ONE CHANGES A PRICE.** `fc.assert` sites are **53 and not 56**, at that entry's own base and at this one, over the 14 files it names; **its "not one of which pins a seed" is FALSE**, `win-days-monotonicity.property.test.ts` pins `REACHABILITY_SEED` on an `fc.assert` and `engine-eligible-conjunction.property.test.ts` pins one on an `fc.sample`, two files, one name, two unrelated values; and **thirteen of thirteen line numbers moved, not twelve**, 11 of 11 by distinct site, twelve being neither count. **NAMED AND NOT REPAIRED**, `ADR-386`:169, and not a sixth instance of the stacked founder's question. **`RE-P-01` DID NOT FIRE** in either full run. **THE ROW RAN ITS OWN INSTRUMENT ON ITS OWN PUBLISHED LIST**: 8 sites, **1 ELIGIBLE, 6 INELIGIBLE, 1 UNCLASSIFIED**, the last being the first `OPAQUE` any published list here has been shown to contain. **NOTHING OF ROW 472's WAS TOUCHED**: `tree-input-budget.mjs` reports **38 budgeted cases at base and 38 at head**, the same 38, and all 21 new cases sit in the unbudgeted population because not one walks a directory. **ONE PRICE DISCHARGED, THREE SET, `PRICE_REGISTER.md` UNTOUCHED, SO THIS BRANCH LANDS `price-register.mjs` RED EXPECTEDLY**, `ADR-464.md:5` coming true a fourth time. **NO GATE ADDED OR WEAKENED, NO TEST SKIPPED, DISABLED, QUARANTINED OR DELETED, NO `RI-nn` MINTED, NO `CI-06` LETTER SPENT, NO MIGRATION, NO DATED RECORD EDITED, NO MONEY-PATH FILE EDITED, NOTHING FROM THE HARVEST COMMITTED.** `pnpm run verify` and [`falsify.mjs`](../../scripts/corpus/falsify.mjs) **NOT run**. -->

### 1. The objective

Build [ADR-471](../decisions/ADR-471.md) section 9 item 1's price, engage rather than inherit that entry's argument for an instrument over a gate, run the result against every `file:line` list this corpus has published, and settle the fourteenth site at [`absence-claims.test.ts`](../../packages/tooling/test/absence-claims.test.ts).

### 2. Done

- **[`packages/tooling/checks/repairable-sites.mjs`](../../packages/tooling/checks/repairable-sites.mjs) and [its suite](../../packages/tooling/test/repairable-sites.test.ts)**, 21 cases. Seven verdicts, one of which permits a repair; `OPAQUE` is neither eligible nor ineligible, because an absence check over a file it cannot parse reports PASS in silence.
- **The verdict on instrument against gate**, argued from four grounds of which two are measurements taken here. [ADR-473](../decisions/ADR-473.md) section 4.
- **The population**: 280 of 455 entries, 2,894 sites, 1,216 eligible. `ADR-473` section 5.
- **The control watched firing and not over-firing** on the real tree, seeded and restored, `git status --porcelain` empty after. `ADR-473` section 6.
- **The fourteenth site discharged with nothing edited**, and the reason is not the reason the dispatch predicted. `ADR-473` section 7.

### 3. Next

- The founder's read on `ADR-473` section 4's first ground, which is the one that decides whether the gate is genuinely unavailable or merely hard.
- `ADR-473` section 10 item 2, the fast-check seeding decision, against the corrected figures. **Money path, and a new session.**
- `ADR-473` section 10 item 3, the convention that a published list names the ref it was compiled against.

### 4. Blockers

None. `price-register.mjs` is red on this branch by design and by precedent, `ADR-473` section 10 item 6.

### 5. Landmines

- **A `file:line` published by an entry is a claim about a file at an instant, and reading it against today's tree is wrong 11.1 percent of the time.** Always pass `--at <the entry's base>`. Section 7 is the near miss this rule exists for.
- **`ADR-471` section 7 item 2's figures do not re-derive.** 53 `fc.assert` sites, not 56, and one of them pins a seed.
- **`ADR-469` section 10 item 1's fourteenth site is at `:138`, not `:151`.** The published index now lands on code.

### 6. Files touched

- `packages/tooling/checks/repairable-sites.mjs` (new)
- `packages/tooling/test/repairable-sites.test.ts` (new)
- `docs/decisions/ADR-473.md` (new)
- `docs/decisions/README.md`, `docs/INDEX.md`, `docs/decisions/ALLOCATION.md` (row 473 only), `docs/STATE.md` (generator output only)
- `docs/sessions/2026-09-08-session-668.md` (new), `docs/sessions/README.md`
