---
status: approved
depends_on: [README.md]
last_updated: 2026-08-20
---

## 2026-08-20 - Session 81: STATE's P1-item table repaired, and OI-10's own record was one of the false claims (non-money)
<!--index: STATE's P1-item table repaired: nine duplicated keys, the span-carrying row kept in each, and OI-10's "Deduplicated 2026-08-16" was false -->

- **Objective:** repair the nine duplicated first-cell keys in [STATE](../STATE.md)'s P1-item table under [ADR-061](../decisions/ADR-061.md), correct `OI-10`'s record of itself, and remove `docs/STATE.md` from `CI06U_REGISTER` entirely. Fence: [`docs/STATE.md`](../STATE.md), this session's entry in `CI06U_REGISTER`, this file.

- **Done. The table heads each of its nine keys once.** It had carried **two whole copies of itself**, the second column-padded by prettier, which is why the review desk's byte-identical dedupe never reached it. **All eleven padded rows are character-identical to their twins once the padding is stripped**, checked mechanically, which is ADR-061's agreement test satisfied by evidence rather than by reading eleven pairs of prose.

- **The two pairs that did not agree were resolved on evidence, and the fact that ADR-061 would escalate them is recorded rather than skipped.** `CI-06, corpus integrity` stood **four** times: twice carrying a generated `gate_count` span, twice reading the bare word "Eleven" against a runner that prints 22. `The reconciled schema and migrations` stood four times: twice with generated spans, twice hand-typing `98 tables, 332 indexes, 359 check constraints, 6 triggers` beside the claim that `0029` to `0031` were "reserved and unwritten" **after all three had merged**, which `OI-10` already records as false. **The span-carrying row survived in both**, per the review desk's instruction of 2026-08-20. Both are `T1` shapes under [ADR-061](../decisions/ADR-061.md) section 5, which routes `T1` to a founder signature; the instruction is that signature and it is named in the commit message so the commit does not read as a session asserting agreement.

- **The generated-span count moved and here is the arithmetic.** `gen:gate_count` occurrences in `docs/STATE.md`: **10 before, 9 after**, and the one lost is the second copy of the `CI-06` row. `gen:migration_files` 11 to 7, `gen:sql_tables` 5 to 4, `gen:sql_triggers` 5 to 4. Every one of them sat inside a deleted duplicate row. **No span was edited**, and `gates.mjs generate` reports "every span already matches its query" with no diff.

- **`OI-10` was reporting itself cleared.** Its State cell read **"Deduplicated 2026-08-16"** and had read it for four days while the table stood doubled; that pass cleared the schema-delta paragraph and nothing else. Its Finding cell said the `CI-06` row stood **three** times when the fourth copy was already in the file. Both corrected in place. The cell now separates what is repaired from what is not: **action item 3 still stands three times**, left as found by its own note, and it is not a table so no gate can reach it.

- **A count correction the brief asked for at two sites and the tree carried at four.** `105` where the gate prints `106`, at the session 75 heading, at two cells of its findings table, and in the sessions-index paragraph, which also said 58 of them were the session index when the register holds **59**. All four corrected. The sentence that counted those sites said "in this file at two" and "in `gates.mjs` at four"; the tree says **four** and **five**, and that sentence is corrected too. `gates.mjs` prose and [STRATEGY](../testing/STRATEGY.md) are `S8`'s and are untouched.

- **The register shrank by exactly this session's fence.** `docs/STATE.md` is gone from `CI06U_REGISTER`, removed in the same commit as the repair as ADR-061 requires, and the gate now reports **97 known duplicate key(s) registered across 7 file(s)**, down from 106 across 8. The `CI-06u/a-repaired-register-entry-is-itself-a-finding` scope case still holds, which is what proves the shrink was real rather than an exemption.

- **Verification.** `node scripts/corpus/gates.mjs check`: **22 of 22**, `CI-06g` and `CI-06t` included. `node scripts/corpus/gates.mjs generate`: no diff. `node scripts/corpus/falsify.mjs`: every gate clean and dirty, **30 scope cases**, **10 loader cases**. `pnpm vitest run`: **939 passed, 42 skipped, 62 files**.

- **Next:** `S3`'s M05 renumbering, then whichever WAVE-03 session `S8` is, which owns the `105` sites in [`gates.mjs`](../../scripts/corpus/gates.mjs) and [STRATEGY](../testing/STRATEGY.md).

- **Blockers and landmines.**
  - **`STATE:286` cites `INV-M5-17` and `INV-M5-18` for M05's SET B invariants and those numbers are being renumbered by `S3`, which has NOT merged.** [M05](../plans/M05-payout-system.md) carries `INV-M5-17`, `INV-M5-18` and `INV-M5-19` **twice each with entirely different meanings** on this ref (lines 90 to 92 and 101 to 103), and STATE's citation matches the second set. **No number was guessed and nothing was re-pointed**: the citation stands exactly as found and the correction belongs to whoever lands after `S3`.
  - **Action item 3's triple copy is untouched and is the same defect one level down**, in prose rather than in a table, which is precisely where `CI-06u` cannot see it. `OI-10`'s cell now says so.
  - **This session held the only permission to edit existing lines of [STATE](../STATE.md)** while six others appended `##` sections to it concurrently. Every edit here is inside the P1-item table, `OI-10`'s row, or a `105` site, plus one appended section of its own.
