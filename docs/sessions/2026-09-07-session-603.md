---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 603: the producer exists and has since ADR-348, the handle is what has no producer, the refusal named the wrong kind of blocker, and the relocation costs no manifest edge

<!--index: **THE ROW IS [ADR-408](../decisions/ADR-408.md), `proposed` and UNSIGNED. NON-MONEY DIFF, MONEY-ADJACENT SUBJECT, `E2` READ OWED.** **THE DISPATCH'S HEADLINE PREMISE IS REFUTED AT PRIMARY SOURCE: `EventSink` HAS A PRODUCER AND HAS HAD ONE SINCE [ADR-348](../decisions/ADR-348.md).** [`apps/api/src/events.ts`](../../apps/api/src/events.ts) declares `makeEventSink` and `TRANSACTION_EVENT_WRITER`, its own header says so in terms, and `EVENT_CATALOGUE` parses mechanically to the **ten** rows that header claims. **WHAT HAS NO PRODUCER IS THE HANDLE**, and every clause of that reason was put back to primary source rather than inherited: `events` is class `either`, `ScopedTx.insert` is generic over `OwnedTableKey`, `FirmTx.insert` over `FirmTableKey`, `SystemTx.insert` over `TableKey`, `insertUnder` is a closed list of one, **eleven** brands are minted in `packages/db` of which **three** transaction handles carry an `insert`, and `systemDb` is imported by exactly **one** non-test source in the workspace, which is `apps/worker`'s. **ONE CLAUSE IS STALE AND ITS CONCLUSION IS UNMOVED**: *"`apps/api` opens only `scoped` and `firm` doors"* survives in two `apps/worker` files this row may not write, and `src/db.ts` declares **five**, none of them a system door. **THE REFUSAL MESSAGE HAD THE BLOCKER BACKWARDS ON BOTH HALVES AND IT IS THE FIRST THING A WIRING SESSION READS**: `EventSinkUnwired` said what is missing *"is a decision about a deployment rather than a file on disk"*, when no deployment in this tree can take that decision and the missing thing is an address. Corrected in place, kept beside its correction on `RI-14`'s rule, with `events.test.ts`'s own stated discipline that the assertion follows the message. **THE REMEDY IS COSTED FOR THE FIRST TIME**: every record calls it *"a package both arrows already reach"* and none checks, and **three** workspace dependencies sit in BOTH deployables' manifests, so the relocation needs no new manifest edge and no `VG-12` admission. **NOT TAKEN, because `packages/**` is outside the fence, and BOUND so it cannot rot**: a new section 4 in [`event-placement.test.ts`](../../apps/api/test/event-placement.test.ts) asserts both halves as separate cases, the absence half failing on GOOD NEWS. **NO PORT WIRED, NO SINK INSTALLED, NO TRIPLE MOVED, `apps/worker` BYTE-IDENTICAL.** -->

### 1. The objective

Derive at primary source whether a real producer for `EventSink` can be built today, build it if it can, and state precisely what it waits on if it cannot. One objective. The diff is non-money; the subject is the append-only record of money movements, so it is treated as money-adjacent throughout.

### 2. Done

- **The container measured and reported.** No repository on arrival, seventh row running. Cloned out of this row's own budget in **4.289s**, base `43ae94a` confirmed as the tip of `claude/wave-404s` and as the dispatched commit.
- **The base reproduced before the first edit** on an empty `git status --porcelain`: gates **33 of 33**, invariants **35 of 35**, suite **336 / 8,109 / 10 / 0**, typecheck, lint and `format:check` clean, and `gates.mjs generate` reporting every span already matching.
- **[`apps/api/src/events.ts`](../../apps/api/src/events.ts) read in full** and the producer found already built, with the reason it records reported verbatim in [ADR-408](../decisions/ADR-408.md) section 5.
- **Ten of ten reasons re-derived at primary source**, one found stale in its wording and unmoved in its conclusion, in files this row may not write.
- **`EVENT_CATALOGUE` parsed mechanically** and the header's own count reproduced.
- **`EventSinkUnwired`'s message corrected**, the false clause kept beside its correction.
- **The remedy address measured** across both deployables' manifests and priced at no new manifest edge.
- **Six new cases written** in [`event-placement.test.ts`](../../apps/api/test/event-placement.test.ts) sections 4 and 5, four of them seeded and watched RED before being restored.
- **The dispatcher's supplement answered, and its question has a worse answer than the one it supplements.** [`apps/worker/test/event-sink.test.ts`](../../apps/worker/test/event-sink.test.ts) read as instructed, read-only. **Of the nine names this estate emits, THREE would build through this producer and SIX would not**, the six splitting into four different kinds of refusal. **The vocabulary is a bigger obstruction than the handle**, and a row that landed the relocation and stopped would have delivered a third of the estate's emits with nothing in either tree saying so. [ADR-408](../decisions/ADR-408.md) section 9.
- **The composition is the one measurement only `apps/api` can take**, because `RI-04` forbids the worker's suite the import of `buildEvent`. The name split is deliberately NOT re-asserted, being that suite's section 4.
- **`RI-13` and `RI-15` were both turned RED by this row's own first draft and both are repaired rather than worked around.** `RI-13` because the entry shipped unsigned without a founder block; `RI-15` because the first placement of the new source comment was in the header, which moved `buildEvent` by the exact size of the block and stale a live pointer in [`schedule.ts`](../../apps/worker/src/schedule.ts), a file this row may not write. **The block was relocated below the last cited line rather than shrunk**, and both cited anchors are byte-identical at their original coordinates.

### 3. Not done, and why

- **NO PRODUCER BUILT**, because one exists. Building a second would be the dispatch's own forbidden shape, an install nothing can exercise, arriving as a duplicate nothing falsifies.
- **NO SINK INSTALLED AND NO TRIPLE MOVED.** The dispatch forbids wiring a sink against nothing and holds that argument on record in `wiring.test.ts`, which this row does not own. `apps/worker/src/schedule.ts` and `apps/worker/src/sweeps/**` were read, cited by name and never opened for writing.
- **THE RELOCATION NOT TAKEN.** `packages/**` and both app manifests are outside this row's fence. Reported as a finding with its price rather than performed.
- **THE STALE CLAUSE IN TWO `apps/worker` FILES NOT REPAIRED.** Same fence. Named in [ADR-408](../decisions/ADR-408.md) section 4.

### 4. Falsification

A passing case that was never watched fail proves nothing, so the two new cases that assert a state of the tree were seeded.

| seed | expected | observed |
|---|---|---|
| a `packages/ledger/src` file carrying the producer's name | the absence case turns RED | **RED**, naming the seeded path, **1 failed / 11 passed** |
| `@merit/ledger` renamed in `apps/api/package.json` | the shared-address case turns RED | **RED**, **1 failed / 11 passed** |
| `detector_run_id` added to the runner's emit, which is [ADR-205](../decisions/ADR-205.md) section 7's one-line repair | the three-of-nine case turns RED | **RED**, **1 failed / 13 passed** |
| `identity_id` added to the freeze warning's payload alone | the three-of-nine case turns RED | **GREEN, and that is the FINDING.** The producer reads the field the CATALOGUE ROW names and that row names none, so the refusal cannot be lifted from `apps/worker` under any edit |
| the catalogue row given a tenancy field to read, beside the payload field | the three-of-nine case turns RED | **RED**, **1 failed / 13 passed**, so the clearing condition is the catalogue's and the document behind it |

Both seeds were removed and `git status --porcelain` returned to exactly the three source files this row edits. **A third RED was not seeded and is reported anyway**: `RI-15` at **34 of 35**, caused by this row's own first draft and named in [ADR-408](../decisions/ADR-408.md) section 7. The two remaining new cases assert a message substring and a manifest read, and both would fail on an empty read by construction: the manifest guard names `@merit/db` on both sides so an unparsed manifest cannot pass silently.

### 5. Counts at reporting time

| leg | base at `43ae94a` | after this row |
|---|---|---|
| gates | **33 of 33** | **33 of 33** |
| invariants | **35 of 35** | **35 of 35** |
| suite files | **336** | **336** |
| suite passed | **8,109** | **8,115** |
| suite skipped | **10** | **10** |
| suite failed | **0** | **0** |
| typecheck | clean | clean |
| lint | clean | clean |
| `format:check` | clean | clean |

**The delta is six cases in one existing file and no new file.** `pnpm run verify` and [`falsify.mjs`](../../scripts/corpus/falsify.mjs) were **NOT run**, both forbidden by the row, and every leg was invoked directly.

### 6. Owed

- **The producer's relocation to a shared `packages/*` address.** A founder's. Priced in [ADR-408](../decisions/ADR-408.md) section 6.
- **A tenancy field on `payout.freeze_expiring`'s catalogue row.** `EVENTS.md`'s, registered by [ADR-191](../decisions/ADR-191.md) section 9 and unmoved here.
- **The stale door clause in `apps/worker/src/schedule.ts` and `apps/worker/src/sweeps/expiry-adapter.ts`.** A later row holding that fence.
- **`E2` read.** Owed by this entry as by every unsigned entry in this wave.
