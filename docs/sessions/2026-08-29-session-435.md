---
status: approved
depends_on: [README.md]
last_updated: 2026-08-29
---

## 2026-08-29 - Session 435: the payout port still refuses, the worker landing closed two of its four links, and `PayoutSubject`'s third field has never had a clause

<!--index: **THE RULING IS [`ADR-245`](../decisions/ADR-245.md), `proposed` and UNSIGNED, and NO MIGRATION AND NO `RI-` NUMBER WERE TAKEN.** The row asked whether `usePayoutBackend` still refuses now that the worker runs. **IT DOES, AND WHAT REFUSES IS NO LONGER THE WORKER.** **LINK 1 IS MEASURED CLOSED RATHER THAN READ CLOSED**: `pnpm --filter @merit/worker start` exits **1** on this branch with a stack running from `start.ts`'s top-level statement through `main` and `resolveTradingDay` into `readLastClosedTradingDay` and out at the door, where [ADR-239](../decisions/ADR-239.md) measured no output and exit **0**. **LINK 2 IS PARTIAL** at four of ten with `loadAccountDay` refusing by name, so `runNightlyBatch` reaches its per-account loop and throws; **LINKS 3 AND 4 DID NOT MOVE AT ALL**. **THE STANDING `BLOCKED` ENTRY IS NEITHER DELETED NOR REWRITTEN FOR THE WORKER AND THAT IS RULING 1**: session 431 had already rewritten it and **all five of its clauses re-derive true**, each checked at its own source, so restating them would have been motion sold as progress. **THE FINDING IS UNDERNEATH THE FOUR LINKS AND IS DOWNSTREAM OF NONE OF THEM**: `PayoutSubject` carries `state`, `plan` and `gates`, links 1 to 4 are the first, [ADR-233](../decisions/ADR-233.md) discharged the second, and **eleven revisions of this reason have never carried a clause about the third**. **NO `ExternalGates` VALUE IS RESOLVED FROM A ROW ANYWHERE UNDER A `src/`**, measured comment-stripped over every `.ts` under every `apps/*/src` and `packages/*/src`: the key `hasPayoutInFlight:` appears in the interface that declares it and in a Monte Carlo loop that raises one member on a value its caller supplied, and every constructed value in the repository is a fixture or `scripts/demo/fold.ts`. **THE FIVE FACTS ALL HAVE COLUMNS AND EVERY TABLE IS REGISTERED**, so what is missing is a function and not a schema. **AND ONE LEG CANNOT BE A TOTAL MAP**: `account_status` declares **seven** members and `AccountStatus` takes **six**, the difference being `provisioning_pending` in that direction only, and `M01:203` carries the same six, so the engine is faithful and the gap is the corpus's; the resolver owes a REFUSAL on `IdentityStatus`'s precedent one file over and **that ruling is registered rather than made here**. **SO ADR-239's THREE SLICES ARE FOUR** and the new one is the cheapest, is independent of the others, and is the one nobody would have found until the other three were spent. **BOTH HALVES ARE PREDICATES MEASURED IN BOTH DIRECTIONS**, each seed restored with `git status` clean after it. **THE PORT IS NOT WIRED**: `usePayoutBackend` is not called, `start.ts` and `payouts.ts` are unchanged, the route answers 503, and `git status --porcelain apps/worker` reports zero. **ONE CITATION REPAIR TAKEN AT A FOURTH SITE ADR-239's TABLE OF THREE DID NOT NAME**, `packages/db/src/schema.ts`, **and it is LINE-NEUTRAL because the first version of it drifted a citation seventeen lines and turned `RI-15` red**. **`RI-15` ALSO CAUGHT THIS SESSION'S OWN COMPOUND CITATION** and it is split rather than argued with. **ADR-239 finding 2 is still open and reported again.** **A THIRD FINDING**: the batch completes and exits 0 on a database with no live mark, so a dead-man switch reading only for a completion line sees a healthy night on an estate that folded nobody. Counts derived at reporting time: **33 of 33** gates, **21 of 21** invariants off the runner's last line, **268 files / 6,570 passed / 6 skipped** against an `origin/main` baseline of **268 / 6,568 / 6** reproduced before a line changed, a delta of **+2 cases and no new file**, typecheck 0, lint 0, `format:check` clean. **`falsify.mjs` was NOT run**: it is not in this row's verification list and it mutates the working tree. -->

**Objective.** Find out whether `usePayoutBackend` still refuses now that the worker actually runs,
and answer it by reading rather than by hoping.

**Outcome.** **It still refuses, and the honest answer is the row's outcome 3.** Two of the four
links closed, two did not move, and the clause that matters most was never one of the four.

### 1. What the row claimed, and what the tree says

| Row `245` and the dispatch | Derived before a line changed |
|---|---|
| `ADR-241` landed the entrypoint, a `BatchPorts` adapter and an external schedule | **HOLDS.** Link 1 measured closed, link 2 real and partial |
| Re-derive the blocker from the current tree first | **DONE, AND IT CHANGED THE OUTCOME.** The entry was already correct, so outcome 2 was not available honestly |
| The clause is discharged, narrowed, or something else refuses | **THE THIRD.** Sections 3 and 4 |
| `apps/worker/src/**` is READ ONLY | **HELD IN FACT.** `git status --porcelain apps/worker` reports zero |
| Stop and report if the port needs a shape the worker does not export | **NOT REACHED.** The blocker is in `apps/api`'s own subject, not in the worker's exports |

### 2. Link 1, measured

`ADR-239` recorded `pnpm --filter @merit/worker start` printing nothing and exiting **0**. On this
branch, with `pnpm install` run first and no `DATABASE_URL` set, it exits **1** and the stack runs
`start.ts:43` to `job.ts:189` to `job.ts:151` to `adapter.ts:334` to `db.ts:152`. **The stack is the
evidence and the exit code is the property**: the process reached the first thing the job needs and
refused there, rather than exiting non-zero for an unrelated reason.

### 3. The four links, re-derived

| Link | This tree |
|---|---|
| 1. A process that runs the job | **CLOSED**, measured above |
| 2. A `BatchPorts` value over Postgres | **PARTIAL.** One value, four of ten, `loadAccountDay` refuses by name |
| 3. An `engine_gates` codec | **UNMOVED.** Still `UNWIRED_RULE_STATE_WRITER_IO.encodeEngineGates` |
| 4. Rows | **UNMOVED.** None |

### 4. The clause nobody had named

`PayoutSubject` carries three fields and the entry has always named two. `gates` is an
`ExternalGates`; **no value of one is resolved from a row anywhere under a `src/`**, and the census
is in the suite rather than in prose. It did not move when the worker landed, because nothing the
worker landed touches it, so a session that lands all three of `ADR-239`'s slices still arrives at
`subject()` with a `RuleState` in hand and no third argument for it.

**And `accountStatus` cannot be a total map**: seven members in the DDL, six in the engine, the
difference `provisioning_pending`, and `M01:203` carries the engine's six. The resolver owes a
refusal, and the ruling is registered rather than made from here.

### 5. Files touched

`apps/api/test/rule-state-producibility.test.ts`, `apps/api/test/wiring.test.ts`,
`packages/db/src/schema.ts` (one comment, line-neutral), `docs/decisions/ADR-245.md`,
`docs/decisions/ALLOCATION.md` row `245`, `docs/decisions/README.md`, `docs/STATE.md`, this log.

**Not touched**: `apps/api/src/**`, `apps/worker/**`, `packages/db/migrations/**`,
`routes/account-reads.ts`, `MERIT_BUILD_MASTER_PROMPT.md`.

### 6. Next

- **The `ExternalGates` resolver**, sized in `ADR-245` section 5. Cheapest of the four slices and independent of the other three.
- **`ADR-239` slice A**, the `engine_gates` codec in `packages/rules-engine`. Unstarted.
- **`ADR-239` finding 2**, still open: `ELIGIBILITY_BLOCKER` clause 2 carries a retired price.
- **`CRON_INVENTORY`'s `S2` switch**, which cannot tell a night that folded nobody from a night with nobody to fold.

### 7. Blockers and landmines

- **`E2` READ IS OWED** on `ADR-245`. It ships `proposed` and UNSIGNED, and merging is not signing.
- **A comment that grows in `packages/db/src/schema.ts` drifts every citation below it.** Forty distinct line numbers point into that file and most are read by no check. Edit it line for line or repoint them.
- **The batch exits 0 on an empty estate.** That is correct and it is also the exit-0 shape one layer in.
