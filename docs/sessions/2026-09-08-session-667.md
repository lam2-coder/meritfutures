---
status: approved
depends_on: [README.md]
last_updated: 2026-09-08
---

## 2026-09-08 - Session 667: the roster is measured by running it, eight of nine walkers enumerate and the ninth is inconclusive rather than clean, and the first control found the instrument refusing a named read and calling that a finding

<!--index: **THE ROW IS [ADR-472](../decisions/ADR-472.md), `proposed` and UNSIGNED. NON-MONEY DIFF, TWO CODE FILES, BOTH OF THEM ROW 470's OWN AND NEITHER ON `main` YET, `E2 READ OWED`.** [ADR-470](../decisions/ADR-470.md) section 10 item 2 taken: *"THE ROSTER IS DERIVED FROM A PARAMETER REACHING AN ENUMERATOR AND NEVER FROM A RUN."* **THE MEASURED ROSTER IS BUILT AND IT IS GREEN.** [`tree-input-budget.mjs`](../../packages/tooling/checks/tree-input-budget.mjs) reports on every run that **8 of 9 rostered walkers enumerate a planted tree when they are RUN, 1 is INCONCLUSIVE, 6 read more of the larger planted tree than of the smaller, 1 enumerates by spawning a process**, and that **7 of the 103 exported functions of `packages/tooling/checks/` are callable with no argument at all, were run, and none of them enumerates unrostered.** Every one of those figures is derived on the run and none is written in the check. **THE HARD HALF THE ENTRY NAMED IS ANSWERED BY WATCHING THE ENUMERATION AND NEVER THE RESULT.** A checker handed placeholder files instead of its corpus throws, and it throws AFTER it has enumerated, which is the only thing being asked, so **the planted tree carries no fidelity to any checker's inputs and needs none**: one placeholder line, eight extensions, and a directory shape MATERIALISED ON DEMAND out of the paths the checker itself asks for. There is no second copy of anything. **TWO LEGS.** **Leg D**, every rostered walker is measured enumerating, over two rounds differing only in how many files are planted, which is the comparison the price asked for. **Leg E**, anything that enumerates when called with no argument at all is in the roster, which is the roster's own definition run backwards. **Leg A over the measurement THROWS** when nothing was measured or when no walker distinguishes the two rounds. **THE VERDICT HAS THREE STATES AND NOT TWO**: measured, a finding, or INCONCLUSIVE. The one inconclusive is [`dependants.mjs`](../../packages/tooling/checks/dependants.mjs)`#main`, whose first statement reads `argv[0]` and which prints its usage and returns; **collapsing it into either of the others would be the check lying about its own population**, so it is counted and named. **[ADR-470](../decisions/ADR-470.md) SECTION 3.4 ITEM 3's HYPOTHETICAL IS ALIVE IN THIS TREE.** `absence-grammar-census.mjs#main` enumerates by spawning `git -C <repo> ls-files -z`, which no `ENUMERATORS` register of `fs` calls can ever see, and **it is in the static roster only because its FALLBACK is a `readdirSync` walk**. A checker written the same way without an fs fallback would never be rostered, and leg E is now what would name it. **THE INSTRUMENT IS A DORMANT PATCH OVER `node:fs` AND `node:child_process`**, installed by `--import` as a `data:` module before any module of this repository is instantiated, ARMED around one call and a pass-through otherwise. **While armed every write, every spawn and `process.exit` throws**, and a suite case asserts that the path a fixture aimed at a real directory of this repository is not there afterwards. **The ordering was measured rather than assumed**: a probe confirmed that an ESM `import { readdirSync }` binds at INSTANTIATION and that patching afterwards reaches nothing, even after the builtin's exports are re-synchronised. **THE CONTROL WAS WATCHED FIRING AND WATCHED NOT OVER-FIRING, AND IT CHANGED THE INSTRUMENT.** The first `seedStale` actually walked and leg D named it anyway: the planted tree did not hold a file the seed asked for BY NAME, the read threw `ENOENT`, and the walk behind it never ran. **A measurement that refuses a named read is measuring its own gaps and reporting them as findings.** A named read is now ANSWERED and counted apart from the planted tree. With both seeds in, exit 1 and **two findings, one per leg, naming the seeds and nothing else**; with **one line changed in each**, exit 0 with both functions still present and the roster at ten of which nine measured; restored **BYTE-IDENTICAL**, verified by `cmp` and by `grep`, `git status --porcelain` clean. **AND LEG C NAMED SEVEN CASES OF THIS ROW'S OWN NEW SUITE ON THE FIRST RUN AND WAS RIGHT ABOUT ALL SEVEN.** They carried `CORPUS_SCAN_MS` out of habit; their input is a fixture module and one child process, and the repository reaches them only inside a template literal, which the fold blanks. **The budget came off the seven rather than the leg coming off the check.** This is leg C working on the diff that added leg D. **FOURTEEN NEW CASES**, each leg watched naming a fixture shape and watched silent on the shape beside it in the same measurement, plus two controls on the instrument itself. **THE BASE RUN WAS NOT CLEAN AND THE CONTAINER WAS THE REASON**: no `node_modules` at all, so `repo-invariants.mjs` reported **34 passed, 0 failed, 1 ERRORED of 35** with `RI-18` unable to resolve `typescript`. **A session reading the pass count would have recorded a green baseline over an unmeasured invariant**; after `pnpm install --frozen-lockfile` it is **35 of 35**. **THE BLAST RADIUS ON `origin/main`'s CITATION SET IS ZERO AND VACUOUSLY SO**: neither file exists on `main`, because wave 470s is open as PR #748 and unmerged, verified through the API rather than assumed. **The one citation that exists at all is [`ADR-470.md:211`](../decisions/ADR-470.md), which cites `tree-input-budget.test.ts:608` in a control transcript when the file was 605 lines long**, so it did not resolve before this row and does not now: **a dated record, NAMED and NOT REPAIRED** ([`ADR-386.md:169`](../decisions/ADR-386.md)). **[`price-register.mjs`](../../packages/tooling/checks/price-register.mjs) IS RED ON THIS BRANCH AND THAT IS THE CHECK WORKING**, for the fifth time: this entry sets four prices and joins the population it describes ([`ADR-464.md:5`](../decisions/ADR-464.md)). **[PRICE_REGISTER.md](../decisions/PRICE_REGISTER.md) is out of fence and was not opened** and the integrator registers the anchors as `OPEN` / `UNASSESSED`. **FOUR PRICES SET**: whether an unreachable rostered walker should become a NAMED exception with a declared reason rather than an anonymous count; whether leg E's argument values should come from the call sites the suites already hold, since it reaches 7 of 103 exported functions; whether a spawn should be ANSWERED with a planted listing instead of refused, since a refused spawn means the measured path is a fallback; and deriving `PLANTED_EXTENSIONS` from the checkers' own literals instead of writing it down. **`RE-P-01` WAS NOT SEEN** in either full suite run and nothing in that file was touched. **NO FILE OUTSIDE THE FENCE WAS EDITED**, [`absence-claims.test.ts`](../../packages/tooling/test/absence-claims.test.ts) is row `473`'s and is byte-identical, **NO CASE SKIPPED, DISABLED, QUARANTINED OR DELETED** (skip count 11 before and after), **NO LEG NARROWED TO PASS, NO GATE ADDED OR WEAKENED, NO `RI-nn` MINTED, NO `CI-06` LETTER SPENT, NO WORKFLOW, NO MIGRATION, NO MONEY-PATH FILE, NO `PRICE_REGISTER.md` ROW, NO `ENUMERATORS` ENTRY ADDED, NO SEED LEFT IN THE TREE, NO PULL REQUEST OPENED, NOTHING SIGNED.** `pnpm run verify` and [`falsify.mjs`](../../scripts/corpus/falsify.mjs) **NOT run**. Counts at reporting time: base **348 files, 8745 PASSED, 11 skipped, 8756 TOTAL, 0 failed**; head on the code alone **348 files, 8759 PASSED, 11 skipped, 8770 TOTAL, 0 failed**, the growth being exactly this row's fourteen cases in one file; gates **33 of 33**; invariants **35 of 35**; `write-guard-set.mjs` PASS; `tree-input-budget.mjs` PASS at **41 case(s) over 9 suite(s)**; typecheck, lint and `format:check` clean. The check costs **944ms, 995ms, 992ms** with the measurement and **746ms, 798ms, 765ms** without it, so **the measured roster costs about 210ms**, one child process running eighteen calls over two rounds.-->

### 1. The objective

Take [ADR-470](../decisions/ADR-470.md) section 10 item 2, which is the last gap that entry left open: *"THE ROSTER IS DERIVED FROM A PARAMETER REACHING AN ENUMERATOR AND NEVER FROM A RUN. A rostered function that stops walking keeps its entry and nothing notices; a function that starts walking through a primitive outside `ENUMERATORS` never gets one."* **Build the assertion the entry priced, which is that each rostered walker actually enumerates, taken by RUNNING it against a tree with a counted number of files and comparing what it reads, and answer the hard half the entry named itself: doing that without giving the check a second copy of every checker's inputs.** Refusing with an argument from a measurement was an accepted outcome. It was not needed.

### 2. Done

- **The measured roster is built and it is green.** [`tree-input-budget.mjs`](../../packages/tooling/checks/tree-input-budget.mjs) reports on every run that **8 of 9 rostered walkers enumerate a planted tree when they are run, 1 is inconclusive, 6 read more of the larger planted tree than of the smaller, 1 enumerates by spawning a process**, and that **7 of the 103 exported functions of that directory are callable with no argument at all, were run, and none of them enumerates unrostered.** Every figure is derived on the run.
- **The hard half is answered by watching the enumeration and never the result.** A checker handed placeholder files throws AFTER it has enumerated, so the planted tree needs no fidelity: one placeholder line, eight extensions, and a directory shape materialised on demand out of the paths the checker itself asks for.
- **Leg D** names a rostered walker that reads the planted tree and enumerates none of it. **Leg E** names anything that enumerates when called with no argument at all and is not in the roster. **Leg A over the measurement THROWS** when nothing was measured or when the two rounds are indistinguishable.
- **[ADR-470](../decisions/ADR-470.md) section 3.4 item 3's hypothetical was found live in the tree**: `absence-grammar-census.mjs#main` enumerates by spawning `git ls-files` and is rostered only because its fallback is a `readdirSync` walk.
- **Fourteen new suite cases**, both legs watched firing and watched silent on the shape beside them, plus two controls on the instrument itself.
- **The control changed the instrument rather than the leg.** Section 6.2 of the entry.
- **[ADR-472](../decisions/ADR-472.md) written**, `status: proposed`, approval line UNSIGNED, four prices set.

### 3. Next

- The integrator registers [ADR-472](../decisions/ADR-472.md) section 10 items 1 to 4 in [`PRICE_REGISTER.md`](../decisions/PRICE_REGISTER.md) as `OPEN` / `UNASSESSED`. **This row did not touch that file and `price-register.mjs` goes red naming this entry's own anchors, which is the check working.**
- **The `E2` read on [`tree-input-budget.mjs`](../../packages/tooling/checks/tree-input-budget.mjs) and [`tree-input-budget.test.ts`](../../packages/tooling/test/tree-input-budget.test.ts).** Both are row 470's files and neither is on `main` yet.
- [ADR-470](../decisions/ADR-470.md) section 10 items 1, 3 and 4 are still unowned, and item 3 now carries a second property on its back.

### 4. Blockers

None. Nothing in the dispatch was refused and nothing inside the fence was left unfinished.

### 5. Landmines

- **THE CONTAINER ARRIVED WITH NO `node_modules` AND `repo-invariants.mjs` REPORTED `34 passed, 0 failed, 1 ERRORED` UNTIL `pnpm install` RAN.** A session that reads the pass count and moves on records a green baseline over an unmeasured invariant. The checker's own closing sentence is what caught it.
- **A CONTROL WATCHED ONLY FIRING WILL SHIP A DEFECT AS A FEATURE.** The first seed for leg D actually walked, and the leg named it, because the planted tree could not answer a file the seed asked for by name. The pair that separates them is now two cases one line apart.
- **AN ESM `import { readdirSync }` BINDS AT INSTANTIATION.** Patching a builtin after a module is loaded reaches nothing, re-synchronising the builtin's exports does not help, and this was measured with a probe before a line of the check was written. `--import` is the only ordering guarantee.
- **A TOP-LEVEL `await` IN A MODULE THE CHILD IMPORTS BACK NEVER SETTLES.** `run` and `census` are exports of the same file the child re-enters; Node reports *"Detected unsettled top-level await"* and the check exits 2.
- **THE BUDGET IS EASY TO PASTE ON.** Seven of this row's own new cases carried `CORPUS_SCAN_MS` out of habit and leg C named all seven. Their input is a fixture and one child process. The budget came off, not the leg.

### 6. Files touched

- [`packages/tooling/checks/tree-input-budget.mjs`](../../packages/tooling/checks/tree-input-budget.mjs) - the measured roster, legs D and E, the dormant patch and the child mode.
- [`packages/tooling/test/tree-input-budget.test.ts`](../../packages/tooling/test/tree-input-budget.test.ts) - fourteen cases, and seven budgets removed from cases that had not earned them.
- [`docs/decisions/ADR-472.md`](../decisions/ADR-472.md) (new), [`docs/decisions/README.md`](../decisions/README.md) (generated span), [`docs/INDEX.md`](../INDEX.md), [`docs/sessions/README.md`](README.md), this file, [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) at row `472` only, and [`docs/STATE.md`](../STATE.md) for generated output only.
