---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 637: the survey nobody had run, and it says the class gets no control and the one claim that was priced cannot land

<!--index: **THE ROW IS [ADR-442](../decisions/ADR-442.md), `proposed` and UNSIGNED. NON-MONEY DIFF, MONEY-ADJACENT SUBJECT, `E2` READ OWED.** [ADR-440](../decisions/ADR-440.md) section 12 item 3 asked how many sentences in this tree claim that no check does X and said the answer decides whether the control is one artifact or a family. **IT IS NEITHER.** Derived over **2,499** files by an instrument this row ships: NARROW (a checking noun, negated, with a verification verb) is **85 sentences over 65 source files** and **861 over 384 documents**; WIDE (plus the universal negatives, which is how all three repaired claims are actually spelled) is **1,468 over 507 source files** and **4,773 over 1,061 documents**. **THE 85 WERE CLASSIFIED EXHAUSTIVELY BY HAND: 50 ARE PRESENT-TENSE COVERAGE CLAIMS**, and a deterministic 1-in-34 sample of the 1,380 WIDE-only live source sentences puts the class at **roughly 430 to 600 in live source**. **THE CLAIMS SHARE A GRAMMAR AND NOT A PROBE**, which is what settles it: of nine tabled, three are decidable by a short probe, three need a reader over every gate's body, three name a fact outside the tree. **THREE COUNTS CORRECTED UPWARD.** [ADR-440](../decisions/ADR-440.md)'s claim has **13 live source sites and not five**, eight of them invisible to its single-substring line search, two by a comment wrap in that exact substring; **row 441's five `apps/worker` sites are ten**; and [`scoped-db.ts:4191`](../../packages/db/src/scoped-db.ts) is a thirteenth site inside that row's own file and own fence. **THE FOURTH INSTANCE OF THE CLASS IS INSIDE THE THIRD INSTANCE'S REPAIR**: both [ADR-440](../decisions/ADR-440.md) blocks close on *"DEFAULT is compared nowhere"* and [ADR-438](../decisions/ADR-438.md) landed that comparison **29 seconds earlier** on a branch it could not see, so [ADR-440](../decisions/ADR-440.md) section 12 item 6 is superseded and a third site is **the first genuinely STALE instance in this chain**. **THE PRICED CONTROL WAS BUILT AS CANDIDATE A DESCRIBES AND RUN OUTSIDE THE TREE AGAINST THE SHIPPED `checkAbsenceClaims`: both probes `present`, 16 sites, 0 leg-1 findings, 16 leg-2 findings, `repo-invariants.mjs` to 34 of 35.** It is SEQUENCING-BLOCKED and no fence in this wave holds half its sites, so there was no first member to build. **WHAT LANDED IS AN INSTRUMENT AND ITS SUITE**, on [`covers-census.mjs:14`](../../scripts/corpus/covers-census.mjs)'s own words, with no count written into it. **NO SENTENCE REPAIRED, NO GATE ADDED OR WEAKENED, NO TEST SKIPPED OR DISABLED, NO CASE INVERTED, NO `RI-nn` MINTED, NO `CI-06` LETTER SPENT, NO REGISTER TOUCHED, NO MIGRATION, NO WORKFLOW, NO `apps/` FILE, NO `packages/db` FILE.** Suite **base 8440, mine 8460, delta +20** over **343 files / 11 skipped both sides / 0 failed**; typecheck, lint and `format:check` clean; **35 of 35 invariants**. **`CI-06f` red at `441`, reported rather than repaired.**-->

### 1. The objective

Run the survey [ADR-440](../decisions/ADR-440.md) section 12 item 3 commissions, **first**, and let it decide the second deliverable: one artifact, a family with its first member built, or neither. Report the count, the files and the method so a later row can reproduce it rather than rebuild it. Re-derive rather than trust.

### 2. Done

- **The container measured and reported.** No repository on arrival. `git push` refused `403` and succeeded after `add_repo` with `access: "push"`; the remote was **not** pre-checked.
- **The base reproduced at `9847bcd0`** on a clean tree before the first edit and **it is green**: gates **33 of 33**, invariants **35 of 35**, suite **342 files / 8,429 passed / 11 skipped / 0 failed**, typecheck, lint and `format:check` clean.
- **The survey run and the population derived**, in two grammars rather than one number, on `covers-census.mjs`'s discipline. [ADR-442](../decisions/ADR-442.md) section 4 is the table.
- **The 85 NARROW source sentences classified exhaustively by hand**, one at a time: 50 present-tense coverage claims, 15 historical, 11 outside the class, 7 undecided, 2 the [ADR-440](../decisions/ADR-440.md) corrections. **The floor is a floor and not an estimate.**
- **The wider grammar sampled deterministically**, every 34th of the 1,380 WIDE-only live source sentences starting at the 7th, so a later row can draw the same 40.
- **Three counts corrected upward, each re-derived at source**, [ADR-442](../decisions/ADR-442.md) section 5.
- **The fourth instance of the defect class found, inside the third instance's repair**, with `git log` author times and `git merge-base --is-ancestor` in both directions. [ADR-442](../decisions/ADR-442.md) section 6.
- **The priced control BUILT and RUN**, exactly as [ADR-440](../decisions/ADR-440.md) section 9.1 candidate A describes it, outside the tree, against the shipped `checkAbsenceClaims` whose register is a parameter. **16 of 16.** That measurement is what decides the shape, and it is forty lines a later row can re-run rather than re-read.
- **The instrument and its suite landed**, [`absence-grammar-census.mjs`](../../packages/tooling/checks/absence-grammar-census.mjs) and [`absence-grammar-census.test.ts`](../../packages/tooling/test/absence-grammar-census.test.ts), 20 cases, all but three over synthetic trees.

### 3. Not done, and why

- **NO SENTENCE WAS REPAIRED.** All 16 false sites are outside this fence, in `apps/worker/**` (10, row 441's), `apps/api/**` (3) and `packages/db/src/**` (3, row 443's). Reaching any of them is the widening the dispatch forbade. They are named by SUBJECT for `apps/worker` and by line elsewhere.
- **NO CONTROL WAS BUILT AND NO FIRST MEMBER EITHER**, and the reason is a measurement rather than a preference: the best-specified candidate in the corpus lands 16 findings red on this base, and a register offers no honest disposition for a sentence that is false and not yet repaired. [ADR-442](../decisions/ADR-442.md) sections 8 and 9.
- **NO `RI-nn` MINTED AND NO `CI-06` LETTER SPENT.** What landed is an instrument, registered in no `GATES` array and asserting nothing.
- **NO RESERVATION WAS WRITTEN FOR `441`**, so `CI-06f` is red at one number. This fence grants row 442's cell and no other row's, and a reservation's subject is row `423`'s *"to be set at dispatch and NOT invented here"*. Reported rather than repaired, on the precedent rows `434`, `436`, `437` and `440` set.
- **THE DOCUMENT HALF OF THE POPULATION IS UNCLASSIFIED.** 1,292 live document sentences under the wider grammar, no sample drawn, no rate claimed.

### 4. Landmines

- **A LINE-ORIENTED SEARCH UNDERCOUNTS THIS CLASS BY MORE THAN HALF**, and every entry in this chain has used one. A claim wraps across two comment lines and a full stop inside `` `schema.ts` `` splits it at its own subject.
- **A CLAIM ABOUT THE WHOLE TREE, WRITTEN ON A BRANCH, CAN ARRIVE FALSE AT THE MERGE WITH NOBODY WRONG.** Two of the three DEFAULT sites are exactly that, 29 seconds apart on concurrent branches. Neither *"false when written"* nor *"stale"* names it, and no control that reads one branch can see it.
- **THE CONTROL [ADR-440](../decisions/ADR-440.md) RECOMMENDS LANDS RED ON ARRIVAL.** A row that takes it without repairing first ships `repo-invariants.mjs` at 34 of 35, and [STATE](../STATE.md)'s own sentence is that a gate which fails on arrival is a gate somebody switches off.
- **`rule` AND `case` MUST STAY OUT OF THE NARROW GRAMMAR.** This corpus owns both words for the domain, and admitting either raises the floor by more than half with nothing in the class.

### 5. Files touched

[`packages/tooling/checks/absence-grammar-census.mjs`](../../packages/tooling/checks/absence-grammar-census.mjs), [`packages/tooling/test/absence-grammar-census.test.ts`](../../packages/tooling/test/absence-grammar-census.test.ts), [ADR-442](../decisions/ADR-442.md), [ALLOCATION](../decisions/ALLOCATION.md) row `442` only, [`docs/decisions/README.md`](../decisions/README.md), [`docs/sessions/README.md`](README.md), this file, and the generated spans in [`docs/STATE.md`](../STATE.md). **No migration. No workflow. No `apps/` file. No `packages/db` file. No existing check file. No register.**

### 6. Next

[ADR-442](../decisions/ADR-442.md) section 15. The two that are ready to pick up: **repair the thirteen TYPE sites and the three DEFAULT sites**, which needs `apps/worker`, `apps/api` and `packages/db` in one fence or three rows and a fourth to register; and **the convention question nobody has put**, which is whether a claim about the suite should be written once and cited thereafter, costs no runner, and would have stopped twelve of the sixteen.
