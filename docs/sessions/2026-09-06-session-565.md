---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 565: eleven jobs and nothing runs, ten reasons hold, and the one that expired was discharged seven rows ago by a row that was not about it

<!--index: **THE ROW IS [ADR-370](../decisions/ADR-370.md), `proposed` and UNSIGNED. MONEY-ADJACENT (the worker runs ledger and payout work), `E2` READ OWED.** **RULED: TEN OF THE ELEVEN RECORDED DISPOSITION REASONS HOLD AT THEIR PRIMARY SOURCE, THE ELEVENTH EXPIRED AND NOBODY RECORDED IT, AND NOT ONE OF THE ELEVEN IS SCHEDULABLE TODAY.** Counts derived by importing the module rather than counted by eye: **13** entry points, **2** scheduled, **11** unscheduled over **10** distinct `CRON_INVENTORY` rows, because `digests/alarm.ts` and `digests/produce.ts` share one row; **so this branch`s own name is imprecise in the direction the file warns about**, eleven ENTRY POINTS and ten ROWS and one job running. **THE DISPATCH`S 2 AND 11 REPRODUCE EXACTLY AND ITS CONCLUSION DOES NOT.** **THE EXPIRED ONE IS THE REPLAY SELF-AUDIT AND ITS BLOCKER WAS DISCHARGED BY A ROW THAT WAS NOT ABOUT IT**: `runReplayAudit` takes `BatchPorts` (`batch/replay.ts:609`), `postgresBatchPorts` returns it (`batch/adapter.ts:940`) and `job.ts:206` builds one on every nightly run, so a `src/` file has supplied exactly this audit`s port since [ADR-346](../decisions/ADR-346.md), while the register said it had none and **`CRON_INVENTORY` still does**. **A WHOLE WAVE OF ADAPTER WORK PASSED OVER IT AND EACH ROW CORRECTED ONLY THE CLAUSE IT WAS DISPATCHED ABOUT**, which is [ADR-358](../decisions/ADR-358.md)`s shape in the file written about exactly this. **AND THE CENSUS THAT HEADER HAS REPEATED FIVE TIMES COUNTS A NAME SUFFIX RATHER THAN A FACT**: there are **ELEVEN** `UNWIRED_*` defaults under `apps/worker/src` and not nine, the two the `_IO` suffix drops are `UNWIRED_BREAKER_EVENT_SINK` (`breaker/adapter.ts:207`) and `UNWIRED_DETECTOR_EVENT_SINK` (`detectors/adapter.ts:217`), and **those are blocker ONE on the breaker row and blocker ONE on the detector row**, so the count excluded precisely the blocker it most often names. **THE ADAPTER WAVE`S CONCLUSION IS FALSE ON THREE ROWS**: `LiveIngestIo` and `WithdrawalApprovalSweepIo` have no inhabitant of any kind, and the provisioning gap is inside `SagaIo` where no port census can see it, `ProvisioningTx` and `PlatformProvisioningPort` existing only in a suite. **A FOURTH FAILS DIFFERENTLY AND IS NOT COUNTED TWICE**: the expiry adapter EXISTS and CANNOT BE CALLED, because `expirySweepIo` (`sweeps/expiry-adapter.ts:193`) takes the sink as a required argument with no default. **THE TWO SPECIALLY FLAGGED DISPOSITIONS WERE BOTH READ AGAIN**: the detector row`s non-adapter reason **STANDS** in all three blockers, with one clause wrong by one (three declared event names, ONE outside the catalogue, not two) which `event-sink.test.ts` has derived in an executed case since [ADR-325](../decisions/ADR-325.md) **while nothing connected it to the register**; and the provisioning row`s `ADR-102` system-write-path reason **DOES NOT STAND FOR THAT ROW** and the file already carries the correction beside it ([ADR-355](../decisions/ADR-355.md) section 7), the obstruction being real and attached to two ports no function in this workspace takes. **`ADR-355`S THREE OPEN QUESTIONS ARE NAMED AND NOT ANSWERED**, and the provisioning row depends on all three; **`JobQueue` still publishes five methods and none is a pull**, derived. **SIX OF THE ELEVEN WAIT ON SOMETHING FOUNDER-OWED**, each cited: `OQ-M6-02` (still *"Proposed"* at [M06:539](../plans/M06-admin-ops-console.md), while `0016:139` makes `min_sample` NOT NULL), `OQ-M7-02` (**eleven of the eighteen seeded detector rows state no number at all**, derived from the seed), `OQ-F3-04` and a vendor ruling on the digest transport, the payment rail, the Rithmic call, and two statistics facts no column carries and no document rules. **THE THIRD BUCKET IS EMPTY AND THAT IS STATED RATHER THAN INFERRED.** **NOTHING WAS SCHEDULED AND NOTHING WAS WIRED**: section 4.3 states what scheduling the closest one would require in four items and then takes none of them, because wiring and scheduling are two decisions and this file is where the second is recorded. **NO CRON EXPRESSION, INTERVAL, BATCH SIZE, CONCURRENCY, RETRY COUNT, BACKOFF OR TIMEOUT WAS INVENTED.** **THREE CASES DERIVE ALL OF IT FROM THE TREE; SIX FALSIFICATION SEEDS, EVERY ONE WATCHED RED AND EVERY ONE RESTORED BYTE-IDENTICAL under `sha256sum -c`**, two in the GOOD NEWS direction (an adapter landing for each of two bare ports) and one in the HARM direction (a refusing `ExpiryEventPort` default, which that row argues against and nothing else refuses). **ONE OF THIS ROW`S OWN CASES WAS WRONG ON ITS FIRST RUN AND ITS OWN RUN FOUND IT**: it asserted the header`s nine and reported eleven, so the figure this row was dispatched to check was inside the file it was checking. **AND `RI-15` WENT RED ON THIS ROW`S OWN FIRST DRAFT AT 34 OF 35**, on a citation naming a method that is not a top-level declaration; it is repointed so the enclosing declaration is a subject of the sentence, which is the invariant catching the defect it was minted for inside the diff that introduced it. **`368` AND `369` ARE RESERVED RATHER THAN LEFT AS HOLES THIS ROW`S OWN LANDING CREATES**, naming no subject and claiming nothing, on [session 558](2026-09-06-session-558.md) section 7`s precedent. **NO PORT WIRED, NO JOB SCHEDULED, NO DISPOSITION MOVED, no gate weakened, no test skipped, disabled or quarantined, no invariant number minted, no `CI-06` letter spent, no migration number taken or reserved, no `status` field moved, `apps/api` BYTE-IDENTICAL to `origin/main` and `apps/api/test/wiring.test.ts` never opened for writing.** **THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL** and the tree was attached and cloned by hand, the condition sessions 550, 553, 557, 558, 561 and 562 recorded. **NO `mcp__github__*` TOOLS, NO `gh` ON PATH, `api.github.com/repos/lam2-coder/meritfutures` ANSWERS 403 while `git push` AUTHENTICATES**; the branch is pushed and **no pull request was opened, by instruction**. Counts derived at reporting time: suite **331 files / 8,022 passed / 10 skipped / 0 failed** against a base reproduced on `8ff3a2a4` BEFORE the first edit of **331 / 8,019 / 10 / 0**, a delta of **+3 cases in ONE EXISTING FILE, no file added, skips unchanged**, attributed BY REMOVAL (`schedule.test.ts` runs **14** alone on this tree and **11** alone on the base); gates **33 of 33**, invariants **35 of 35**, typecheck, lint and `format:check` clean, SQL diff **EMPTY**. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `370`: read every one of the eleven unscheduled dispositions in [`apps/worker/src/schedule.ts`](../../apps/worker/src/schedule.ts) against its primary source and rule on whether the recorded reason still holds.

**One objective.** It resolved into one expired reason, one falsified census, one figure wrong by one, and an empty third bucket.

---

### 2. What was derived before the first edit

**The baseline was measured on `8ff3a2a4` with a clean tree, before anything was touched: 331 files / 8,019 passed / 10 skipped / 0 failed.** Gates **33 of 33** and invariants **35 of 35**, both green on the untouched tree.

**The counts were derived by importing the module**, not read off the dispatch and not counted by eye:

| | |
|---|---|
| Total entry points | **13** |
| `scheduled` | **2** |
| `unscheduled` | **11** |
| Distinct unscheduled `CRON_INVENTORY` rows | **10** |

**THE DISPATCH'S TWO AND ELEVEN REPRODUCE.** What does not reproduce is the wave's conclusion about adapters, which is section 3 of the entry.

---

### 3. THE RULING

[ADR-370](../decisions/ADR-370.md) section 2 holds it in five clauses. The three that carry the row:

**RULED: TEN OF THE ELEVEN REASONS HOLD AT THEIR PRIMARY SOURCE**, each cited in section 5 of the entry.

**RULED: THE REPLAY SELF-AUDIT'S REASON EXPIRED IN HALF, AND THE HALF THAT EXPIRED IS THE ADAPTER.**

**RULED: NOT ONE OF THE ELEVEN IS SCHEDULABLE TODAY**, and the third bucket being empty is stated rather than left to be inferred.

---

### 4. What landed

| file | what |
|---|---|
| [`apps/worker/src/schedule.ts`](../../apps/worker/src/schedule.ts) | the expired clause corrected beside its original, the census paragraph, and the detector figure. **No disposition moved** |
| [`apps/worker/test/schedule.test.ts`](../../apps/worker/test/schedule.test.ts) | **+3 cases.** The unwired-default and constructor census, the replay port identity, the detector event-name derivation |
| [`docs/decisions/ADR-370.md`](../decisions/ADR-370.md) | **new.** The ruling, `proposed`, UNSIGNED |
| [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) | row `370` taken, and `368` and `369` reserved. Section 6 |
| `docs/decisions/README.md`, `docs/STATE.md`, `docs/sessions/README.md` | the registry row and the generated spans |

**No file under any `src/` outside `apps/worker/src/schedule.ts` was changed**, and every falsification seed was restored from a byte copy with `sha256sum -c` verifying it.

**`docs/INDEX.md` was NOT changed**, on the disposition [session 557](2026-09-06-session-557.md) recorded and [session 561](2026-09-06-session-561.md) followed: entry files are fragments indexed by their own README under `CI-06n`.

---

### 5. Falsification

**Six seeds, every one watched RED and every one restored byte-identical.** Three worth keeping here:

- **Seeds 1 and 6 run in the GOOD NEWS direction**, which is the only kind of check still present when the good news arrives. A detector event-sink adapter landing and a live ingest adapter landing each redden the census, so the row that lands the tenth adapter is made to re-read that job's row and find out whether the adapter was ever the binding blocker. **For four of them it was not.**
- **Seed 5 runs in the HARM direction.** A refusing `ExpiryEventPort` default is the convenient thing to add and the expiry row argues against it in its own words: every leg of that sweep emits inside its own release transaction, so a live io over a rejecting sink is an hourly job that releases nothing while the S1 switch reports it present. **Nothing else in this tree refuses that edit.**
- **Seed 4 was rewritten after its first run.** It fired on the parse guard rather than on the finding, because the guard duplicated `event-sink.test.ts`'s pin of the catalogue size and one transcription would have reddened two files for one fact. The guard now proves the parse and the assertion carries the finding.

---

### 6. The fence widening, reported rather than quiet

**Row `370`'s fence names only its own row in `ALLOCATION.md`, and this row wrote three.** `CI-06f` is gapless over allocated PLUS reserved, the highest number on the merged head is `367`, and `370` landing alone opens two holes and turns that gate red with nothing wrong. **The reservation cells name no subject and claim nothing**, because this row was told not to guess at its siblings, and a sibling taking one replaces the cell whole.

**THIS IS THE THIRD CONSECUTIVE WAVE DISPATCHED WITHOUT A RESERVATION BLOCK**, which [session 561](2026-09-06-session-561.md) section 7 recorded as the second and [session 562](2026-09-06-session-562.md) section 6 recorded again. It is cheap to write once at dispatch and it costs a paragraph in every row that lands without it.

---

### 7. Blockers and landmines

- **The founder's `E2` read of [ADR-370](../decisions/ADR-370.md) is owed.** Its approval block names three decisions, the first of which is whether the replay self-audit is now the next slice.
- **OWED AND OUTSIDE THIS FENCE: [`docs/ops/runbooks/CRON_INVENTORY.md`](../ops/runbooks/CRON_INVENTORY.md)'s Replay self-audit row carries the expired half too.** It reads that no `src/` file supplies its ports and no `src/` file calls it. **The first clause is false and the second is true.** The page already knows the shape of the repair: the Statistics run row two rows down says the `against ports` half is discharged while the marker stays. **The marker itself must NOT move**, and `schedule.test.ts` case 4.2 asserts that in both directions. ADR-370 section 7 states it precisely enough to be picked up.
- **LANDMINE: the replacement census counts a PREFIX where the old one counted a suffix.** A refusing default named anything but `UNWIRED_*` is invisible to both, and nothing in this tree requires the prefix. The direction of the error is the safe one and it is the same class of defect one letter along. Recorded in ADR-370 section 9 rather than repaired, because fixing it properly means a type-level census and that is a different row.
- **LANDMINE: the constructor census reads RETURN types and `RuleStateWriterIo` proves it misses things.** A second value composed inline at a call site would be counted as absent, in the direction that invents work.
- **LANDMINE: `apps/worker/src/index.ts:450` records that `replay.audit_completed` has no producer.** True, and named only so the row repairing the inventory knows the second site exists. The barrel was not opened, so no barrel-leg count was re-derived and none needed to be.
- **Concurrency: sibling rows are running now** and this row reserved two `ALLOCATION` numbers for them. A registry conflict at merge is expected and is the designed workflow.

---

### 8. Verification, derived at reporting time

| runner | result |
|---|---|
| `pnpm vitest run` | **331 files / 8,022 passed / 10 skipped / 0 failed** |
| baseline on `8ff3a2a4`, before the first edit | **331 files / 8,019 passed / 10 skipped / 0 failed** |
| delta | **+3 cases in ONE EXISTING FILE. No file added, skips unchanged.** Attributed **by removal**: `schedule.test.ts` runs **14** alone on this tree and **11** alone on the base |
| `scripts/corpus/gates.mjs generate` then `check` | **33 of 33** |
| `packages/tooling/checks/repo-invariants.mjs` | **35 of 35**, no number minted |
| `pnpm run typecheck` | clean |
| `pnpm run lint` | clean |
| `pnpm run format:check` | clean |
| `git diff --stat origin/main -- packages/db/migrations scripts/db` | **EMPTY** |
| `git diff --stat origin/main -- apps/api` | **EMPTY** |

**`RI-15` WENT RED ON THIS ROW'S OWN FIRST DRAFT**, at 34 of 35, on a citation pointing at `batch/adapter.ts:802` for `accountsWithStoredState`. That line is a method inside `readPort` rather than a top-level declaration, so the pointer resolved and anchored on nothing. **It is repointed so the enclosing declaration is a subject of the sentence, which is what the invariant asks for and is not a weakening of it.** That is the invariant catching the defect it was minted for, inside the diff that introduced it, which is the second consecutive wave in which that has happened.

**`pnpm run verify` was NOT run and `scripts/corpus/falsify.mjs` was NOT run**, both forbidden by the row and both mutate the working tree.

---

### 9. This session's own tooling, measured rather than assumed

```
$ command -v gh                                            -> not found
$ curl -s -o /dev/null -w '%{http_code}' \
    https://api.github.com/repos/lam2-coder/meritfutures   -> 403
```

**No `mcp__github__*` tool exists in this session's surface.** `git push` authenticates through the session's git proxy and did so on every commit.

**THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL.** `/home/user` was empty and the tree had to be attached to the session's authorized set and cloned by hand before a single file could be read, the same condition sessions 550, 553, 557, 558, 561 and 562 recorded. **The clone landed exactly on `8ff3a2a4`, which is the base the row named.**

**Every Bash command was run bare from the working directory**, per the row's operational note. **No permission stall occurred**, which is the seventh consecutive row to report that.

**THE BRANCH IS `claude/eleven-jobs-nothing-runs`, PUSHED. NO PULL REQUEST WAS OPENED, BY INSTRUCTION.**

---

### 10. Next

The founder's `E2` read of [ADR-370](../decisions/ADR-370.md) and the three decisions its approval block names. Then, in order of cheapness: the `CRON_INVENTORY` replay-row repair, which is prose in one row and is now owed by this one; the replay self-audit's four items in section 4.3, of which two are `src/` slices, one is `packages/db`'s and one needs a row on a frozen document; and `ADR-355`'s three open questions, which are the founder's and which hold the only one of the eleven with a paying trader on the other side of it.
