---
status: approved
depends_on: [README.md]
last_updated: 2026-08-25
---

## 2026-08-25 - Session 211: P3 wave 3 planned, and the axis the brief asked for is not the one the six collide on

<!--index: **[P3 wave 3](../plans/P3-wave3-modules.md) dispatched at last, `status: draft`, eleven slices over four waves, sessions `213` to `223`.** [P3](../plans/P3-ledger-billing-identity.md) section 6 refused it because its fences were paths inside a deployable `ADR-083` had not named; `ADR-083` is signed, [`apps/api`](../../apps/api/src/index.ts) exists, `P4-d` is dispatched, and the fences are real. **THE BRIEF ASKED FOR THE ROUTE AXIS AND ROUTES ARE NOT THE AXIS.** Every one of P3's contents is a WRITE and [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) gives both accessors `rows()` and nothing else; **[ADR-092](../decisions/ADR-092.md) section 9 says so about itself**, and **sessions [157](2026-08-24-session-157.md) and [168](2026-08-24-session-168.md) proposed the same write slice from inside two different fences without either being able to see the other**, which is ADR-092's own shape one interface over. [`packages/queue`](../../packages/queue/src/job-queue.ts)'s `enqueue` takes a transaction **nothing in the workspace can produce**, so the saga is blocked on the accessor rather than on a queue. **A THIRD ABSENCE NEITHER MEASUREMENT NAMED**: `SystemReason` is closed at two, and `psp_webhook_events`, `coupons` and `integration_contracts` are all `firm`, so three contents have no legitimate reader and [ADR-096](../decisions/ADR-096.md)'s remedy is unavailable because `apps/api` is the process the site reads THROUGH. **`otp_challenges` is refused by SIGNED [ADR-094](../decisions/ADR-094.md) and the remedy is a session that entry SPECIFIES and nobody numbered**; its "seven" is now **80**. **AUTH IS THIRD**: not last because `POST /checkout` is `Auth: session` and section 12 requires its rows before the endpoint ships, not first because of those two preconditions. **[ADR-093](../decisions/ADR-093.md)'s cell edits are APPLIED**, so P3's own six-content table is stale by one row. **The concurrency answer is CONDITIONAL and both branches are dispatchable**: the axis is the ENDPOINT for routes and the PACKAGE for libraries, and ADR-092's third part transfers only if `P4-d` declares the registry total over a closed endpoint union. **The brief's "209 is writing it right now" is false against the tree**: zero branches, zero pull requests, which changes the allocation and not the fences. **Claims sessions 213 to 223 and NO ADR number**, because 209 holds an unclaimed row. **Eight of eight fenced, and the phase's first line of application code is three sessions away rather than one.** **A FIGURE THIS SESSION MEASURED MOVED WHILE THE BRANCH WAS OPEN**: [session 205](2026-08-25-session-205.md) merged at `7f8215f`, the registry went **80 to 87 of 111**, `provisioning_queue` was registered by somebody else, and **`P3-l`'s fence lost all three of its `packages/db` files** while `P3-g`'s cost grew. Both re-derived on the merged tree rather than carried. **THEN [SESSION 209](2026-08-25-session-209.md) MERGED AT `4fd4a8a` AND ANSWERED THE CONDITIONAL THIS PLAN WROTE, BY A MECHANISM IT DID NOT PROPOSE**: section 6.2 asked for a registry total over a closed endpoint union, and [ADR-100](../decisions/ADR-100.md) **removed the shared file instead**, making the module list a DIRECTORY LISTING. **Four route slices are four branches unconditionally**, four fences lost their registry edit, and the `routes/index.ts` collision row is **struck**. **Session 209 also took ADR `100`**, which is this plan's refusal to claim it arriving as an outcome. 30 of 30, verify exit 0 at **111** files / **1,665** passed. No code, no migration, no ADR, nothing signed -->

- **Objective:** plan `P3` wave 3, the six module slices [P3 section 6](../plans/P3-ledger-billing-identity.md) deliberately did not dispatch, consuming its ordering table as input rather than re-deriving it. One plan document. **No application code, no migration, no ruling.**

- **Done:** **[P3-wave3-modules](../plans/P3-wave3-modules.md)**, `status: draft`. **Eleven slices over four waves**, each with a one-line objective, a fence **BY FILE**, a money-path flag, a depends-on **BY FILE** and a complete prompt, every fence cross-checked against every other **and against the five sessions already in flight**. Collisions stated **PER FILE**. **Sessions `213` to `223` claimed** in [sessions/README](README.md); **no ADR number and no migration number claimed**, and section 8 is why. **30 of 30 gates, `pnpm run verify` exit 0 at 8 of 8 invariants, 111 test files, 1,665 passed, 1 skipped** on the tree merged through `14dc406`, all measured on this branch and all unmoved by it: it was **106 files and 1,611** before any of the three merges, and the difference is session 205's seven drift assertions, session 209's three test files and session 210's two.

- **Next:** **six items go to the founder**, and the first changes what wave 3.0 costs: `P3-g`'s size is unmeasured by construction and auth waits behind it either way. Then **`P3-f`, which nothing blocks and which may open today**.

- **Blockers:** none for this session. For wave 3 itself: **four preconditions, three dispatched here and one (`P4-d`, session 209) not this plan's**, and no stated content can open until all four land.

---

### The two decisions the brief asked for, and the first one came back a different shape

**Decision one was "how many of the six can run concurrently, and on what axis do they collide", with [ADR-092](../decisions/ADR-092.md) offered as the precedent.** The precedent is exactly right and the subject is not routes.

| The question | The answer, measured |
|---|---|
| What do the six actually collide on? | **`packages/db/src/scoped-db.ts`.** Every P3 content is a WRITE and both accessors carry `rows()` and nothing else |
| Who says so? | **[ADR-092](../decisions/ADR-092.md) section 9, about itself**: *"This entry rules NOTHING about the write accessor"*, and [ADR-094](../decisions/ADR-094.md) section 5 repeats it |
| Had anyone else found it? | **Two module measurements, independently.** [Session 157](2026-08-24-session-157.md)'s `M3-a` and [session 168](2026-08-24-session-168.md)'s `M19-0` |
| Is the route registry still an axis? | **Yes, second**, and the transferable part of ADR-092 is the third clause: the queue is the TYPE CHECKER. It transfers **only if** `P4-d` declares the registry total over a closed endpoint union with a per-endpoint disposition and a count assertion |
| So how many run concurrently? | **`P3-f` alone first**, then **two library slices always concurrent**, then **four route slices that are four branches under a total registry and one queue without it**. Both branches are dispatched |

**Decision two was where auth goes.** Third. **Not last**, because `POST /checkout` at [API_CONTRACT:285](../architecture/API_CONTRACT.md) reads `Auth: session`, `POST /kyc/session` at `:488` reads the same, and section 12's first sentence is *"Every row is a named test that must exist **before the endpoint ships**"*. **Not first**, because its front door writes a table the registry cannot hold. The brief's paragraph was checked at the source and it holds, and the half it did not have is the second one.

---

- **Landmines:**

  - **THE ACCESSOR CANNOT WRITE, AND THE TWO SESSIONS THAT FOUND IT WERE FENCED SO THEY COULD NOT SEE EACH OTHER.** [`scoped-db.ts:131-156`](../../packages/db/src/scoped-db.ts) gives `ScopedDb` `__brand`, `identityId` and `rows()`, and `SystemDb` `__brand`, `reason` and `rows()`. [`index.ts:41-71`](../../packages/db/src/index.ts) exports no writer and no transaction, and [ADR-084](../decisions/ADR-084.md) section 9 rules `client()` unexported **permanently**. **[`packages/queue`](../../packages/queue/src/job-queue.ts) makes the caller's open transaction `enqueue`'s FIRST argument with no overload that omits it**, and its own header says `packages/db` *"can satisfy it without exporting its client"*. It does not. So **nothing in this workspace can enqueue a job**, and [session 147](2026-08-24-session-147.md) recorded exactly that and could not fix it from inside its fence.

  - **`SystemReason` IS CLOSED AT TWO AND A REQUEST HANDLER IS NEITHER, WHICH TAKES OUT THREE CONTENTS AT ONCE.** `psp_webhook_events`, `coupons` and `integration_contracts` are all **`firm`** in [`scope.ts`](../../packages/db/src/scope.ts), therefore excluded from `ScopedTableKey` by construction, therefore readable only through `systemDb('nightly-batch' | 'operator-console')`. **The webhook receiver does not know whose purchase it is until after it has persisted the event**; checkout recomputes the coupon server side; the enrichment adapter reads its contract row. **[ADR-096](../decisions/ADR-096.md) is the precedent and it went the OTHER way**, refusing a third member for `apps/site` and ruling the site *"is not a reader of this database at all"*. **That remedy is unavailable here, because under ADR-096 `apps/api` is the process the site reads THROUGH.**

  - **A SIGNED ADR REFUSES `otp_challenges` AND ITS OWN SECTION 3 WROTE THE REMEDY'S SPECIFICATION WITHOUT NUMBERING IT.** [`0029:526`](../../packages/db/migrations/0029_phone_identity_and_auth.sql) is `ALTER COLUMN email_normalized DROP NOT NULL`. [ADR-094](../decisions/ADR-094.md) closes the drift fold at one member with a default of FAIL, forecloses a per-table exemption list **permanently** and forecloses widening by regex, and its consequences name the cost: *"`otp_challenges` and `trading_calendar` stay refused"*. **Section 3 names the third door**: column TYPE and NULLABILITY are transcribed and asserted nowhere, closing it is a second comparison against `ddlColumnDefs`, and *"measuring that is its own session"*. **That session is P3's critical path and it has never been numbered.** It is `P3-g` now.

  - **ADR-094 SAID SEVEN AND THE ANSWER IS 80, SO THE UNNUMBERED SESSION'S COST MOVED WITH THE REGISTRY.** `grep -c 'pgTable(' packages/db/src/schema.ts` returns **87**, and it returned **80** when this plan was first written. The comparison lands on 87 transcriptions rather than 7, **and it is still growing**, and **whether the session repairs what it finds or reports it and stops is a scope decision nobody can size before it runs**. Recorded in the plan rather than left for the session to discover, which is the class [CLAUDE.md](../../CLAUDE.md) says escalating a model does not fix.

  - **THREE PER-TABLE REFUSALS ARE ONE RULING AND [ADR-092](../decisions/ADR-092.md) NAMED IT THREE TIMES AND TOOK NONE.** [`scope.ts:120-152`](../../packages/db/src/scope.ts) records `identity_links`, `attributions` and `affiliate_commissions` in its own words, `dedupe_matches` was refused by [session 194](2026-08-25-session-194.md) on the same ground, and all four are the same shape: **a row whose subject is a PAIR against an `owned` rule that names ONE column**, where each wrong choice **returns rows rather than erroring**. **`attributions` is the table [M08](../plans/M08-affiliate-system.md) section 3.1 resolves INSIDE the checkout transaction**, so P3's third and fourth stated contents both stop at it. ADR-092's own section 10 rejected a holder document because a second hand-maintained copy is the defect; **three entries for one shape is that defect in a different costume**, so `P3-h` takes the class once.

  - **[ADR-093](../decisions/ADR-093.md)'S CELL EDITS HAVE LANDED AND P3'S OWN SECTION 2 TABLE IS STALE BY ONE ROW.** [DELIVERY_PLAN](../DELIVERY_PLAN.md)'s P3 cell now reads *"the authentication surface: sessions, the OTP challenge lifecycle, the two passkey ceremonies and `C-27` elevation"* and its done-condition carries a fifth clause naming [API_CONTRACT section 12](../architecture/API_CONTRACT.md)'s `C-27` rows **in both directions**. [Session 180](2026-08-24-session-180.md) wrote both edits out verbatim and deliberately did not make them; somebody did. **P3's contents are SEVEN and its wave-3 ordering table carries six**, which is not a defect in the table: the table was written on 2026-08-23 and the cell moved on 2026-08-24. Both plans are `draft` and outside this session's subject, so **neither is edited here** and the discrepancy is recorded instead.

  - **THE BRIEF'S "SESSION 209 IS WRITING THE ROUTE REGISTRY RIGHT NOW" IS FALSE AGAINST THE TREE, AND THE USEFUL HALF IS WHAT IT CHANGES.** `git ls-remote --heads origin | grep -c 's209'` returns **0** and the four open pull requests are sessions 205, 206, 207 and 208. **It does not change the fences**: `apps/api/src/routes/index.ts` is named by path in [P4 section 8](../plans/P4-portal-and-site.md)'s `P4-f` fence and again in section 9's collision table as *"new in `P4-d`"*, and a path a dispatched slice's own fence names is the standard [P3 section 6](../plans/P3-ledger-billing-identity.md) refused to fence below. **It changes the allocation.** Session 209's fence holds `ALLOCATION` (its row) and it has claimed nothing, so a planning session taking `100` would hand it a collision it cannot see from inside its own fence. **This plan claims eleven session numbers and no ADR number**, on [P4 section 7](../plans/P4-portal-and-site.md)'s precedent, and states the lowest free number as a measurement instead.

  - **`P3-f`'S TEST FILE IS NEW AND THAT IS THE WHOLE CONCURRENCY CLAIM.** [ADR-092](../decisions/ADR-092.md) section 9 makes [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) *"the pair's third file for fencing purposes"*, and five sessions hold it: 205 to 208 in flight, plus `P3-g` and `P3-h`. **`P3-f` holds `scoped-db.ts`, `index.ts` and a NEW file, and no file is in both sets**, which is why it can open today with six other branches live. **Its verification line is that `scoped-db.test.ts` is byte-identical to `origin/main`'s at the end**, because a concurrency claim with no mechanical check is a hope.

  - **THE INTEGRATION PROJECT STILL HAS NO DATABASE, SO A WRITE SUITE CANNOT EXECUTE A WRITE IN CI.** [Session 172](2026-08-24-session-172.md) measured that [`ci.yml`](../../.github/workflows/ci.yml)'s `integration` job runs on bare `ubuntu-latest` with **no `services:` block**, and that reading survives `ADR-085`. So `P3-f`'s suite asserts the SQL the accessor **builds**, the way [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) asserts the predicate, and **its entry must say so**, because a suite that looks like it exercised a write and did not is worse than one that says it did not. That is [session 145](2026-08-23-session-145.md)'s non-discriminating-suite finding aimed at the session most likely to repeat it.

  - **P3's THIRD DONE-CLAUSE IS STILL UNDISCHARGEABLE AND THIS PLAN DOES NOT PRETEND OTHERWISE.** All fourteen M2 fixture rows are `blocked / vendor-call`, `GS-138` is the one [M02](../plans/M02-rithmic-bridge.md)'s `INV-M2-13` cites, and *"fail-closed provisioning holding an unconfirmed setpoint out of trading"* is that invariant verbatim. **`P3-l` writes the assertion against the simulator anyway and reports that whether it discharges the row is a ruling nobody has taken**, which is [P3 section 9](../plans/P3-ledger-billing-identity.md) item 3 inherited unchanged rather than quietly resolved.

  - **A FIGURE MEASURED IN THIS SESSION MOVED WHILE THE BRANCH WAS OPEN, AND IT MOVED A SLICE IN THE GOOD DIRECTION.** [Session 205](2026-08-25-session-205.md) merged at `7f8215f` between this plan's first commit and its merge of `main`, registering M02's seven tables. **The registry went 80 to 87 of 111 and `provisioning_queue` came with it**, `derived` through `account_id`. **`P3-l` was written holding `schema.ts`, `scope.ts` and `scoped-db.test.ts` to register that one table and now holds no `packages/db` file at all**, so it depends on `P3-f` alone and is the least blocked slice in the wave. **`P3-g` moved the other way**: its comparison lands on every registered transcription, so **every merge before its dispatch makes it larger**, and 206, 207 and 208 are still open. **Both figures were RE-DERIVED on the merged tree rather than carried**, and section 5.3 of the plan is the record. This is [ADR-092](../decisions/ADR-092.md)'s own stated cost, *"a session cannot know its own slice size before it runs"*, arriving on a PLAN rather than on a session, and [P4 section 11](../plans/P4-portal-and-site.md) rule 3 is the twelve-of-fourteen precedent.

  - **THE CONDITIONAL THIS PLAN WROTE WAS ANSWERED WHILE THE BRANCH WAS OPEN, AND THE ANSWER WAS BETTER THAN THE QUESTION.** Section 6.2 was written with two dispatchable branches because `P4-d`'s registry shape was inside [session 209](2026-08-25-session-209.md)'s fence and not this plan's. **209 merged at `4fd4a8a`** carrying [ADR-100](../decisions/ADR-100.md). This plan asked for a registry **total over a closed endpoint union with a count assertion**, the shape `SCOPE_RULES` has over `TableKey`; `ADR-100` **removed the shared file altogether**, so `discoverRouteModules` reads `apps/api/src/routes/` and *"the module list is the directory listing and is never written down"*. **A total registry still has one shared file and this has none**, so the resolution is strictly stronger than the property that was asked for, and the plan records the diff in section 5.4 rather than absorbing it. **Four slice fences lost the `apps/api/src/routes/index.ts` edit they carried and section 6.3's row for that file is STRUCK**, with the prompts stating in terms that creating it re-creates the collision `ADR-100` removed. **`compose` refuses a duplicate `METHOD /path` across the whole module set**, which is the two-slices-one-endpoint collision an array would have merged silently.

  - **THE ALLOCATION REFUSAL STOPPED BEING AN ARGUMENT AND BECAME AN OUTCOME.** Section 8 measured `100` as the lowest free ADR and deliberately claimed nothing, because session 209 was dispatched holding `ALLOCATION` (its row) and had claimed no number. **Session 209 took `100`.** Had this plan claimed it, `CI-06w` would have read two rows over one key and the resolution would have cost a cycle rather than a sentence. The lowest free number is now `101`, and section 8 says so.

  - **A SECOND SESSION MERGED `main` INTO THIS BRANCH CONCURRENTLY, AND THE RECONCILIATION IS THE SAME LESSON A THIRD TIME.** While this session was merging `14dc406`, another push landed `98b0392` on the same branch doing the same merge. **The push was refused as non-fast-forward and the resolution was a MERGE and never a force-push**, because a branch under review is not a branch to rewrite. The two differed only where this session had gone further: two STATE paragraphs and the corrected test counts. **Here keep-both is the WRONG resolution and taking OURS is the right one**, because the two sides are the same `##` section and the same registry row at two ages rather than two appends, and keeping both would duplicate exactly what `CI-06u` refuses. **The rule that generalises: keep-both is right for an APPEND and wrong for a REVISION**, and which one a conflict is cannot be read off the diff.

  - **A BLANKET KEEP-BOTH MERGE RESOLVER IS WRONG FOR THE CLAIM TABLE, AND `CI-06u` CAUGHT IT BEFORE THE PUSH.** Three merges of `origin/main` landed on this branch and every conflict was in [STATE](../STATE.md) or [sessions/README](README.md). **STATE is append-only and keep-both is always right there**; the CLAIM table is not, because a row exists on BOTH sides and the merge is about which STRIKE survives. The third merge produced two rows keyed `210` and two keyed `211`, and [`CI-06u`](../testing/STRATEGY.md) reported both at 29 of 30: *"two rows for one key is the keep-both merge, and the registry can no longer answer the question it exists to answer"*. **Resolved by keeping one row per key**, main's struck `210` because 210 has landed and this branch's struck `211` because this session wrote it. **The gate is the only thing that would have said so**: both duplicate pairs render as plausible table rows and neither breaks a link. This is [session 105](2026-08-20-session-105.md)'s `OI-10` shape arriving on the registry `CI-06u` was written for.

  - **THE PULL REQUEST MERGED AT THE SIBLING'S HEAD AND FOUR RECORDS DID NOT LAND WITH IT.** [#272](https://github.com/lam2-coder/meritfutures/pull/272) merged at `98b0392`, the concurrent session's merge of `main`, rather than at this session's final commit `f3ee248`. **The two landmines above, the plan's `ADR-096` paragraph and the position line's figure were all in the difference**, and the figure was FALSE rather than merely old: it read 109 files and 1,645 tests where the merged tree measures **111 and 1,665**. **A merged pull request cannot track new work**, so the follow-up restarted this branch from `origin/main` and carried the unmerged delta forward rather than stacking on merged history. **The lesson is the one this session already recorded twice, arriving a fourth time**: a concurrent writer on a shared branch means the head that merges may not be the head you last verified, so **verify what LANDED and not what you pushed**.

- **Files touched:** [`docs/plans/P3-wave3-modules.md`](../plans/P3-wave3-modules.md) (new), [`docs/INDEX.md`](../INDEX.md) (its row), [`docs/sessions/README.md`](README.md) (this row struck, eleven claimed), [`docs/STATE.md`](../STATE.md) (appended), this log. **Plus THREE merges of `origin/main`, at `7f8215f`, `4fd4a8a` and `14dc406`**, six conflicts in [STATE](../STATE.md) and [sessions/README](README.md) alone. **Five were pure appends and the sixth was not**: the claim table's `210` and `211` rows exist on both sides and keep-both duplicated them, which `CI-06u` refused. `packages/db`, `apps/api` and `apps/site` all came through untouched, and `git diff origin/main --name-only` returns this session's five documents and nothing else. **No code, no migration, no schema, no gate, no ADR.**
