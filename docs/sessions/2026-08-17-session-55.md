---
status: approved
depends_on: [README.md]
last_updated: 2026-08-17
---

## 2026-08-17 - Session 55: golden fixtures batch 7, the forty-three re-derived, and three that had come unblocked without anyone noticing (non-money)

- **Done:** **All forty-three held-back rows were re-derived against their own primary sources and three no longer bind.** [GS-070](../../packages/rules-engine/fixtures/GS-070-funded-start-balance-does-not-equal-size.yaml), [GS-065](../../packages/rules-engine/fixtures/GS-065-settled-payout-drops-the-balance-toward-a-floor-that-does-not-move.yaml) and [GS-056](../../packages/rules-engine/fixtures/GS-056-locked-floor-converts-the-account-into-a-free-option.yaml) are written and green on the first fold. **Every one of the three turned on the same field**, `days[].adjustment_cents`, which stopped being pinned at zero when `AWAITING_M01_INPUT` emptied and which nobody had cashed. **Four further reasons were corrected without being unblocked** and two of them would have sent a reader at the wrong thing. **17 of 17 gates pass, 746 tests pass**, and the only two failures are `GS-024` and `GS-064`, pre-existing, ruled by [ADR-052](../decisions/ADR-052.md) and [ADR-054](../decisions/ADR-054.md), awaiting one signature each. **The count is 33 of 73 and 33 of 284.** Nothing outside [`packages/rules-engine/fixtures/`](../../packages/rules-engine/fixtures/README.md) was touched except this log and its registry row.
- **Next:** **`L-11` is now the biggest single blocker in the table and it is a FORMAT change rather than an engine one.** Eight rows (GS-052, GS-053, GS-066, GS-067, GS-068, GS-081, GS-082, GS-241) are behind a `settlements:` block carrying `payout_request_id`, `ordinal`, `approved_cents`, `basis_trading_day` and `effective_trading_day`; `DayInput.settlements` has taken `SettlementFact` since the fold landed. **`compare.ts`'s flatness is the second**, holding GS-060 and GS-080 and half of GS-042. Both are one session's work in `packages/golden-loader`, and neither needs a ruling.
- **Blockers:** **None taken, and one raised for the founder that is a documentation defect rather than a decision.** [EC-157](../edge-cases/EC-157.md) is **ruled** (Repair A, 2026-08-16, migration `0036` merged) and its file still says `status: open`, `NOT RULED`, and "GS-065's fixture cannot be written until this is ruled"; [STATE](../STATE.md)'s open-items table carries the same entry. Both are outside this session's fence. The calendar transcription still blocks GS-003, GS-030, GS-032 and half of GS-049 and is still a founder item. `engine_eligible: true` is still unpinnable and that reason was re-checked rather than assumed: M01 lists R-38 under **Group F: funded gates** while declaring `hasPayoutInFlight` on `ExternalGates`, and R-41 says nothing about which gates are in the left operand.
- **Landmines:**

  - **THE THREE FIXTURES WERE WITHIN ONE COMMIT OF BEING WITHDRAWN ON A BLOCKER THAT WAS LIFTED TWO DAYS BEFORE THEY WERE WRITTEN, AND THIS IS THE FINDING OF THE SESSION.** [EC-157](../edge-cases/EC-157.md)'s golden-scenario line reads "**GS-065's fixture cannot be written until this is ruled**", its header reads `status: open`, and its Correct-behavior line opens "**NOT RULED**". Every mark in all three files carries a non-zero adjustment, which is precisely what that entry calls unwritable, so on its face batch 7 was another zero and I had begun writing it up as one. **It is ruled.** [ALLOCATION](../decisions/ALLOCATION.md) records `0036` as "[EC-157](../edge-cases/EC-157.md)'s **Repair A** as ruled 2026-08-16", and [`0036_supersede_daily_marks_balance_arithmetic.sql`](../../packages/db/migrations/0036_supersede_daily_marks_balance_arithmetic.sql) is merged: `daily_marks_balance_arithmetic` is dropped and `closing = opening + realized_pnl` survives. **All eleven marks satisfy it, checked field by field rather than assumed.** [STATE](../STATE.md)'s open-items table carries the stale entry too, verbatim including the GS-065 sentence. **This is the fifth instance of the leading-clause shape in three batches and the first one that would have destroyed correct work rather than merely wasting a reader's time**: the other four sent someone to unblock something already unblocked, and this one says stop. Neither file is inside this session's fence and both are named here and in the fixtures README.

  - **A HELD-BACK REASON IS A CLAIM WITH A SHELF LIFE, AND THIS BATCH IS THE PROOF THAT SAYING SO IS NOT ENOUGH.** [Session 51](2026-08-17-session-51.md) wrote that sentence, and then three of the rows it wrote it about expired within the day. `AWAITING_M01_INPUT` emptied in the very next session and its log recorded the emptying as a fact about the LOADER; nobody asked which held-back rows had been standing on it. **The list was re-derived and the answer was three.** The generalisation this batch adds is narrower and more useful: when a session records that an input became available, the held-back table is a downstream consumer of that fact and has to be re-read in the same session, not merely marked as re-readable.

  - **"IT NEEDS A SETTLEMENT" WAS TRUE OF THE SCENARIO AND FALSE OF THE FIXTURE, AND M01 SAID SO IN A ROW THAT CITES THE FIXTURE BY NAME.** GS-065 and GS-056 were both filed under `L-11`. R-10's row reads "non-trading balance movements are applied between sessions and carried in `adjustment_cents`", and its `Pinned by` column names **GS-065**. So the corpus had already stated which mechanism the scenario runs on, one column to the right of the rule the row is about. What `applySettlement` adds is R-46's anchors, R-47's reset, R-49's ladder and R-50's lifetime total, and **neither fixture pins one of them or cites a group H rule**. The reason had generalised from "the scenario narrates a payout" to "the fixture needs a `SettlementFact`", and the two are different claims.

  - **THE SHAPE THAT MADE GS-070 WRITABLE IS THE ONE ITS OWN BLOCKER DESCRIBED.** The row read that `funded_start_not_size` "cannot be reached independently of `opening_mismatch`: DO-3 tests `opening == prior.balance + adjustment` first, **`L-11` fixes `adjustment_cents` at 0**, and the prior balance after R-31's reset IS `size_cents`". Every clause is a statement about the adjustment, and the middle one stopped being true. With a +300,000c credit stated, INV-18 holds and INV-20 fails on the same mark. **The blocker was written precisely enough to expire visibly and it still took a re-derivation to notice**, because nobody re-reads a row they already know the conclusion of.

  - **THE LITERAL GS-070 IS THE WEAKER FIXTURE AND IT IS THE ONE A READER WOULD WRITE.** Stating `adjustment_cents: 0` beside an opening of 5,300,000 is exactly "the platform failed to reset", and it raises **both** INV-18 and INV-20: `markIdentityFailures` collects failures rather than returning on the first. The day is refused either way and the end state is identical, so that fixture passes just as well against an engine with no INV-20 check at all. **It asserts "something refused" where AS-14 asserts "INV-20 refused"**, and the whole reason INV-20 exists is that it is not implied by INV-18. The declined shape is recorded in the fixture's own header rather than in this log alone.

  - **A REFUSAL IS ASSERTABLE THROUGH A FIELD DIFF EVEN THOUGH THE SIBLING HAS NO SHAPE FOR AN `AssertionFailure`, AND THAT HALF OF THE ROW WAS NEVER RE-CHECKED EITHER.** The fold stops on a refusal and the diff is taken against **the last state the engine wrote**, so GS-070's end state is the account-open state and `trading_day` is still `2026-10-30`. An engine that folded the day moves five pinned fields at once. What the fixture still cannot state is **which** assertion refused it, which it reaches by construction instead: the stated adjustment leaves INV-20 as the only identity on the mark that can fail.

  - **AS-10 CARRIES A PRE-ADR-014 SENTENCE AND A FIXTURE AUTHOR READING IT WOULD GET THE FLOOR BACKWARDS.** Its counter reads "the floor is recomputed in the same step as the balance drop, so balance and floor move together (R-48)". R-19's own row says the opposite in bold: "there is no post-payout floor recompute ... `floorCents`, `highWaterBalanceCents`, and `floorLocked` all carry through untouched", and GS-065's registry row was **rewritten by ADR-014** to say the floor is "**not** recomputed to compensate". The rule row and the registry row agree and the narrative paragraph is stale. **Not edited: a frozen document moves by ADR.** GS-065 follows R-19 and its sibling says which of the two it followed.

  - **GS-004 AND GS-031 WERE FILED BESIDE GS-003 AS IF ONE THING BLOCKED ALL FOUR, AND THE CALENDAR TRANSCRIPTION MOVES ONLY TWO OF THEM.** `kind` maps to `CalendarDay.isHalfDay` now, so the half-day rows wait on the transcription alone. **`halted` has no key in the calendar record at all** and [`calendarRowsFromRecord`](../../packages/golden-loader/src/calendar.ts) writes the constant `false`, so R-04 is declared, `isWinDay` reads the flag, and no fixture graded through this loader can ever set it. A transcribed CME year unblocks GS-003 and GS-032 and leaves GS-004 and GS-031 exactly where they are. **The same shape hit GS-059**, whose recorded blocker was the calendar and which also needs a `cadenceAnchorDay` only a settlement sets.

  - **A RETRACTION LEFT ONE COPY STANDING, TWENTY-EIGHT LINES BELOW ITSELF.** [Session 51](2026-08-17-session-51.md) reported a founder ruling of 2026-08-17 living in one source comment and in no document. It has since been retracted in [`coverage.ts:125`](../../packages/golden-loader/src/coverage.ts) and in [`fixtures.golden.test.ts:174`](../../packages/golden-loader/test/fixtures.golden.test.ts) — "no such ruling exists and none is needed" — and **`fixtures.golden.test.ts:202` still opens `FOUNDER RULING, 2026-08-17`**, in the same file as one of the two retractions. `CI-06q` passes, because it checks that cited rulings resolve rather than that retracted ones are gone. **A retraction that leaves one copy standing is worse than the original claim**: the next reader finds both and cannot tell which is current. Outside this session's fence, recorded in the fixtures README beside the paragraph it contradicts.

  - **THE MECHANICAL RECONCILIATION REPORTED FORTY-ONE ROWS AND THE NAIVE FIX WOULD HAVE BEEN TO EDIT THE COUNT.** GS-056's row was left in the table marked `WRITTEN`, which is a leaving-record rather than a claim that the file is missing, and the sweep of leading cells counted it. The row was deleted instead: it is already in the batch-7 table above with its expired clause, and a table of things that are not here should not contain a thing that is. **Batch 6 recorded the five-false-positive version of this**, from sweeping the `Why` cells; this is the same defect one column over.

- **Files touched:** [`fixtures/GS-070-*`](../../packages/rules-engine/fixtures/GS-070-funded-start-balance-does-not-equal-size.yaml) (new pair), [`fixtures/GS-065-*`](../../packages/rules-engine/fixtures/GS-065-settled-payout-drops-the-balance-toward-a-floor-that-does-not-move.yaml) (new pair), [`fixtures/GS-056-*`](../../packages/rules-engine/fixtures/GS-056-locked-floor-converts-the-account-into-a-free-option.yaml) (new pair), [`fixtures/README.md`](../../packages/rules-engine/fixtures/README.md), [sessions/README](README.md), this file. **Each fixture committed and pushed as it landed.**

### The count, reported rather than implied

**33 of 284, and 33 of M1's 73.** Three fixtures, forty rows re-derived and still binding, four reasons corrected, and one instance of the leading-clause rot the last two batches both found. **The three that unblocked shared one cause**, which is the useful part of the result: a batch that finds three unrelated unblocks has been lucky, and a batch that finds three behind one field has found where to look next.
