---
status: approved
depends_on: [README.md]
last_updated: 2026-08-17
---

## 2026-08-17 - Session 55: ADR-052 applied to `floor.ts`, the locked floor becomes an assignment (money path, plan mode)

- **Done:** **[ADR-052](../decisions/ADR-052.md) is applied.** `floor.ts` takes the **branch** (`floor = floorLocked ? floor_lock_floor_at_cents : max(hwb - drawdown, size - drawdown)`); the sub-step `neverRetreats('R-15', ...)` call and `trailedFloorCents` are **deleted**, and the `R-13` sub-step check stays. All three comment blocks that argued for the `max` are replaced with the ruling rather than left beside it. `RE-U-015`'s overshoot case and `RE-U-031`'s two pins move `5,050,000 -> 5,010,000`. `CI-02/engine-R-15` **inverts**: the `max` is now the mutant, watched failing on `RE-U-015 + R-15`. M01 amended at the three in-fence sites ADR-052 names. The fixtures README's section 2 becomes the record of the ruling, on section 1's ADR-050 precedent. **Result: `GS-024` GREEN, `CI-03` 2 -> 1, suite 743/2 -> 744/1**, deterministic over repeated runs; the one remaining failure is `GS-064`, which is ADR-054's. 17 of 17 gates, typecheck, lint, format clean, and **50 of 50 CI-02 mutants watched failing**.
- **Next:** `INV-07` versus the funded reset is the successor founder item and is now the only thing keeping a post-pass fixture from pinning `floor_locked` (fixtures README section 1). ADR-052's fourth named amendment site, **ADR-014's decision block**, is owed and was out of this session's fence.
- **Blockers:** `GS-064` (ADR-054's, pre-existing). `INV-07`'s scope, founder-owned.
- **Landmines:**
  - **NEVER RUN `falsify-ci.mjs` IN THE BACKGROUND WHILE TOUCHING THE SAME WORKING TREE.** It mutates source files in place and restores them. This session backgrounded it and then ran the suite and several `git checkout`s alongside it; every measurement taken during that window was garbage, the R-50 mutant (`lifetimeSettledCents: fact.approvedCents`) was left in the tree and rode across checkouts, and it produced a **completely false reading that `main` was red on two settlement tests**. It is not. Diagnosis was `git diff` on a file this session never touched. Run it in the foreground, or in a separate worktree with its own `node_modules`.
  - **A worktree symlinked to the main checkout's `node_modules` is not an independent tree.** The first attempt to measure a baseline that way resolved `@merit/rules-engine` back into the changed source, so `GS-024` "passed" at a commit that predates the fix.
  - **The survey in the approved plan said "nothing else in the suite pins it" and that was wrong.** `RE-U-031` pinned the `max`'s number twice, at the `DO-7` witness and in the `rule.floor_locked` payload, and described it in two comments. Every argument that test exists for survives; only the transcribed literal moved. A grep for the literal `5_050_000` would have found it and the plan's survey went by file instead.
  - **`CI-06q` cannot see a misattributed ruling on a date that has any declared ruling.** Executed rather than assumed this session: seeding `founder ruling, 2026-08-17` into a file **PASSES**; seeding an undeclared date **FAILS** with a precise message. That is the limitation `CI-06q`'s own header declares, and the stronger assertion it names (a citation must name `ADR-nnn`, not merely a date) is still blocked on the measured cleanup of 28 dated citations in frozen documents.
- **Files touched:** [`floor.ts`](../../packages/rules-engine/src/day/floor.ts), [`rules-c-floor.test.ts`](../../packages/rules-engine/test/rules-c-floor.test.ts), [`rules-e-progression.test.ts`](../../packages/rules-engine/test/rules-e-progression.test.ts), [`M01`](../plans/M01-rules-engine.md), [`falsify-ci.mjs`](../../scripts/ci/falsify-ci.mjs), [fixtures README](../../packages/rules-engine/fixtures/README.md), this log, [STATE](../STATE.md).
