---
status: approved
depends_on: [README.md]
last_updated: 2026-08-25
---

## 2026-08-25 - Session 202: M20's four wallet tables enter the transcription, and the one known collision cost nothing because nobody coordinated
<!--index: **[M20](../plans/M20-wallet.md)'s table set under [ADR-092](../decisions/ADR-092.md) and [ADR-094](../decisions/ADR-094.md). FOUR OF FOUR, and the four are the same four [session 196](README.md) was dispatched to claim.** `wallet_entries`, `wallet_withdrawals`, `wallet_spend_limits` and `wallet_dormancy` are in [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts), each `owned` on an `identity_id` the DDL declares against `identities(id)`; **53 of 111 becomes 57 of 111**, at 1 root / 24 owned / 7 derived / 25 firm, so **32 of the 57 are reachable through the scoped accessor**. **No ADR, no migration, no new document.** **[ADR-092](../decisions/ADR-092.md) SECTION 8 ITEM 4'S ONE INTERSECTING PAIR IS THIS ONE AND THE RULING'S FIRST REAL TEST RESOLVED BY NOBODY DOING ANYTHING**: [session 159](2026-08-24-session-159.md)'s `M5-1` and [session 169](2026-08-24-session-169.md)'s `M20-a` claim the same four, 159 wrote the collision down and 169 called the two files *"M20-a's alone"*, and `git fetch origin main` at open and again immediately before the claim both return **`3c9c2d3`**, so all four were unregistered when looked at and the first writer wrote them. **No roster was read because there is no roster**, and the four `why` fields state the TABLE's tenancy rather than M20's use, which is what makes session 196's smaller slice costless rather than a loss. **THE FINDING IS A TRAP THAT IS CAUGHT BY NOTHING, AND IT IS NOT THE ONE THE DISPATCH PREDICTED.** `wallet_entries.ledger_transaction_id uuid NOT NULL REFERENCES ledger_transactions(id)` reads exactly like `ledger_entries`' hop, and because `ledger_transactions` is registered `derived` rather than `firm` the chain **TERMINATES**: seeded as `derived` through it, **`pnpm run typecheck` exits 0 with zero `error TS` and the suite passes at 78 of 78**. The declared FK exists, [session 188](2026-08-25-session-188.md)'s bounded chain assertion is satisfied, and every mechanical check agrees with a rule answering a DIFFERENT QUESTION -- whose ledger accounts appear on this transaction, rather than whose wallet holds this entry -- which agree only while no transaction touches two identities' accounts, a thing nothing enforces and double entry makes ordinary. **This is [session 194](2026-08-25-session-194.md)'s `dedupe_matches` class in a second shape, reported and NOT repaired, because an assertion that `derived` is unavailable where an `owned` column exists is a ruling and a transcription rules nothing.** **The second trap is NULLABILITY rather than target and is equally uncaught**: `freeze_flag_id uuid NULL REFERENCES risk_flags(id)` reaches the CORRECT identity through a registered `owned` table and would still return a person only the withdrawals HELD AGAINST THEM, because the column is NULL on every withdrawal nobody froze. **[ADR-094](../decisions/ADR-094.md)'S FOLD APPLIES NOTHING TO ANY OF THE FOUR, RE-DERIVED RATHER THAN INHERITED**: no `ADD`, `DROP` or `ALTER COLUMN` and no `RENAME` names any of the four over all 47 migrations, and the two later `ALTER TABLE`s that do touch the set -- [`0028`](../../packages/db/migrations/0028_supersede_plan_version_immutability.sql) superseding `wallet_dormancy_review_was_noticed` and [`0031`](../../packages/db/migrations/0031_payout_hold_and_identity_restriction.sql) adding `wallet_withdrawals_live_freeze_blocks_settlement` -- are both constraint work the fold reads and discards; neither of ADR-094's two unregistrable tables is M20's, so the fold was not widened. **`wallet_dormancy` IS THE FIRST REGISTERED TABLE WHOSE `nullable: false` IS READ OFF `PRIMARY KEY` RATHER THAN OFF THE WORDS `NOT NULL`**, and its `why` says so, which matters precisely because nullability is the axis nothing checks. **Two seeds watched failing in the direction each names**, both at 1 failed / 77 passed: `wallet_withdrawals` scoped by `destination_ref` fails **quoting the DDL back**, and `earliest_credit_at` deleted from the transcription fails at 18 columns against 19; the loud registration hazard was watched too, the four registered without their `SQL_NAME` entries being **`TS2739` naming all four**. **`fills`, `offers`, `payout_requests` and `promotional_credit_grants` are named by M20 and deliberately NOT registered** -- all four are other modules' tables M20 reads and none is in session 169's `M20-a` -- and `identity_links`, `dedupe_matches`, `otp_challenges` and `trading_calendar` were not declared, not registered and not touched. **COLUMN TYPE AND NULLABILITY REMAIN ASSERTED NOWHERE, NOW OVER 57 TABLES**, four new `nullable` flags among them, `notified_at` transcribed as a `timestamptz` ARRAY and `wallet_withdrawal_status` as a `pgEnum` in `0001`'s member order because a Postgres enum is ORDERED. 74 tests to **78**, suite **1,584 to 1,588**, both baselines measured on this tree; `verify` exit 0 at 8 of 8 invariants and 106 files; 30 of 30 gates; `falsify:ci CI-01` **12 of 12** with `git status --porcelain` read immediately after it at zero -->

- **Objective:** M20's tables enter [`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts), minus whatever is already registered, under [ADR-092](../decisions/ADR-092.md) and [ADR-094](../decisions/ADR-094.md). **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict. **No ADR.**
- **Done:** **4 of 4.** `wallet_entries`, `wallet_withdrawals`, `wallet_spend_limits` and `wallet_dormancy` registered, all four `owned` on `identity_id` with `nullable: false`. The registry goes from **53 of 111 to 57 of 111**, at 1 root / 24 owned / 7 derived / 25 firm, **32 of the 57 reachable through the scoped accessor**. Three seeds executed rather than predicted, one of which found a real hole and is reported below.
- **Next:** **`M20-a` is complete and `M20-b` is dispatchable and blocked by nothing** ([session 169](2026-08-24-session-169.md) section 4): the wallet probe, watching four `0011` constraints reject. **`M20-c` and `M20-d` each still need an `ALLOCATION` row claimed before dispatch**, and both are the founder's questions rather than a session's -- which uniqueness `wallet_entries` gains, and whether `DEP-M20-04`'s missing input is a widened `payment_method` CHECK or a new table. **Session 196 (`M5-1`) will find these four already registered** and should take them as they stand and report its slice as that much smaller; nothing in this branch needs it to do anything else. **Registering a table makes it READABLE and nothing else**: `ScopedDb` is still `__brand`, `identityId` and `rows()`, so every M20 slice that WRITES still waits on the write accessor.
- **Blockers:** none for this session. **The `E2` line-by-line read is owed these four registrations**, which makes fifty-seven money-path tables merged or merging on delegated authority without it.
- **Landmines:**
  - **A `derived` RULE ON `wallet_entries` THROUGH `ledger_transactions` PASSES EVERY ASSERTION IN THE SUITE.** This is not the trap the dispatch describes. That one reaches a **firm** table and throws at read time, and [session 188](2026-08-25-session-188.md)'s bounded chain assertion catches it. This one reaches a **registered `derived`** table, so the chain terminates at an identity: `pnpm run typecheck` exits **0 with zero `error TS`** and the suite is **78 of 78 green**. `ledger_transaction_id` is `NOT NULL`, the FK is declared inline in [`0011`](../../packages/db/migrations/0011_wallet.sql), and nothing in the file compares the answer that rule gives against the answer `identity_id` gives. **The only thing standing between a future session and it is reading the `CREATE TABLE` and seeing `identity_id uuid NOT NULL` on the row.**
  - **`freeze_flag_id` IS THE SAME HOLE THROUGH THE OTHER DOOR.** `uuid NULL REFERENCES risk_flags(id)`, and `risk_flags` is registered `owned` on its own `identity_id`, so a derived rule terminates at the **right** identity and is still wrong: NULL on every unfrozen row, so it returns a person only the withdrawals held against them. **A nullable FK to a correctly-scoped table is not a milder error than a nullable FK to a firm one.**
  - **`wallet_dormancy.identity_id` HAS NO `NOT NULL` TEXT AND IS `NOT NULL` ANYWAY.** It is `uuid PRIMARY KEY REFERENCES identities(id) ON DELETE RESTRICT`. `nullable: false` is therefore read off the key rather than off the words, the `why` says so, and **the suite reads only the `REFERENCES` clause**, so it would have accepted either flag. This is [ADR-094](../decisions/ADR-094.md) section 3's unclosed axis arriving on a specific column rather than in the abstract.
  - **`provenance` AND `state` ARE CLOSED LISTS ENFORCED BY CHECK CONSTRAINTS AND ARE TRANSCRIBED AS `text`, WHICH IS CORRECT AND LOOKS WRONG.** A CHECK is not a Postgres enum, and declaring either as a `pgEnum` would be a false statement about the column's type -- on the axis nothing compares. `wallet_withdrawal_status` **is** a real type in [`0001`](../../packages/db/migrations/0001_extensions_and_enums.sql) and is transcribed as a `pgEnum` **in `0001`'s member order**, because a Postgres enum is ORDERED and a reordered transcription would be a false statement about a type this file does not create.
  - **`reference_id` IS POLYMORPHIC AND CARRIES NO `.references()` ON PURPOSE.** `provenance` decides whether it names a payout request, a purchase or the entry this one corrects. It names no one table, so it is untraversable by a vocabulary whose `derived` member names one table against one column, and `0011` declares no FK for it either.
  - **`node_modules` WAS ABSENT** and `pnpm install --frozen-lockfile` was run before anything could be verified; the lockfile is unchanged. **`falsify:ci` was never backgrounded and `git add -A` was never used in this session**; `git status --porcelain` was read immediately after the `CI-01` run and reported zero.
  - **`scope.ts` AND `scoped-db.test.ts` LANDED IN ONE COMMIT AND THAT WAS DELIBERATE**, for [session 194](2026-08-25-session-194.md)'s reason: `SQL_NAME` is `Readonly<Record<TableKey, string>>` and total, so either file alone is red. The per-file commit rule is served better by a green pair than by two commits neither of which compiles. **`schema.ts` went first and alone**, which compiles because a declared-but-unregistered table is [ADR-092](../decisions/ADR-092.md) section 4's third row.
  - **EVERY STATED COUNT WAS RECOMPUTED FROM `TABLE_KEYS.length` AND NONE WAS INCREMENTED.** Five sites: [`schema.ts`](../../packages/db/src/schema.ts)'s header three times as a spelled word plus the unreachable remainder, [`scope.ts`](../../packages/db/src/scope.ts)'s registry docblock once, and [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) four times -- two comments, the test NAME and two `expect(...).toBe(57)` assertions. **The diff into all three files is APPEND-ONLY apart from those recomputed lines**, so a concurrent sibling's conflict resolves by keeping both sides and re-deriving one number.
- **Files touched:** [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts), [`packages/db/src/scope.ts`](../../packages/db/src/scope.ts), [`packages/db/test/scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts), [STATE](../STATE.md) (one appended `##` section), [sessions/README](README.md) (row 202 struck, entry row generated), this file. **Nothing outside the fence.** [M20](../plans/M20-wallet.md), [M05](../plans/M05-payout-system.md), [`0001_extensions_and_enums.sql`](../../packages/db/migrations/0001_extensions_and_enums.sql), [`0011_wallet.sql`](../../packages/db/migrations/0011_wallet.sql), [`0028`](../../packages/db/migrations/0028_supersede_plan_version_immutability.sql), [`0031`](../../packages/db/migrations/0031_payout_hold_and_identity_restriction.sql), [ADR-092](../decisions/ADR-092.md), [ADR-094](../decisions/ADR-094.md) and [session 169](2026-08-24-session-169.md) were **read and none was written**.

### What was NOT done, named rather than implied

**No ADR, no migration, no new document, no `INDEX.md` row, no gate, no fixture, and no edit to [`docs/plans/`](../plans/M20-wallet.md), which this fence makes read-only.** **No number was allocated**: the `wallet_entries` finding wants a ruling and wanting one was the signal to stop and report it, here and in the pull-request body.

**`fills`, `offers`, `payout_requests` AND `promotional_credit_grants` WERE NOT REGISTERED.** All four are unregistered on `main`, all four are named by [M20](../plans/M20-wallet.md), and all four are other modules' tables that M20 READS rather than owns; [session 169](2026-08-24-session-169.md)'s measurement puts none of them in `M20-a`. Each carries a tenancy argument that belongs to the session holding its module, and `payout_requests` in particular is [M05](../plans/M05-payout-system.md)'s -- session 196's, which is the very slice this session was told not to coordinate with. **The honest report is four of four against session 169's measured slice, and four of eight against a whole-word grep of M20 for unregistered table names.**

**`identity_links`, `dedupe_matches`, `otp_challenges` AND `trading_calendar` WERE NOT DECLARED IN [`schema.ts`](../../packages/db/src/schema.ts) AND NOT REGISTERED**, which is [session 192](2026-08-25-session-192.md)'s handling followed rather than merely its outcome: a declared-but-unregistered `pgTable` is a table no drift assertion compares. None of the four is M20's in any case.

**NO LANDED RULE WAS RE-ARGUED, RESTATED OR IMPROVED**: the fifty-three rows that were there when this branch opened are byte-identical on it, and the only pre-existing line this session edited in any of the three files is a count. **The write accessor was not touched**, so registering these four makes them READABLE and nothing else, which is [ADR-092](../decisions/ADR-092.md) section 9's boundary unchanged.

### The merge, and what it changed

**[Session 197](2026-08-25-session-197.md) merged first**, so `main` moved from `3c9c2d3` to `92ea3d9` while this branch was open and [ADR-092](../decisions/ADR-092.md) section 4's fourth row arrived: **a textual conflict in all five files** -- [`schema.ts`](../../packages/db/src/schema.ts), [`scope.ts`](../../packages/db/src/scope.ts), [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts), [STATE](../STATE.md) and [README](README.md). **Every one was keep-both**: M08's three tables and M20's four are disjoint, and neither session touched a line that was not its own.

**Every figure in this log above is true at this session's BASE and the merge result is different.** 53 of 111 to 56 by session 197, to **60 of 111** here, at **1 root, 25 owned, 9 derived and 25 firm**, **35 of the 60 reachable through the scoped accessor**, the other 51 through neither. `scoped-db.test.ts` **77 on `main` to 81 merged**, +4, one per registered table; suite **1,587 to 1,591**, +4; `verify` exit 0 at 8 of 8 invariants and 106 files; 30 of 30 gates. **The base figures are left standing rather than rewritten**, because an absence is a fact about a file at a commit and this session's was `3c9c2d3`.

**EACH CONFLICTED SOURCE FILE WAS REBUILT FROM `main`'s COPY PLUS WHOLE DECLARATIONS LIFTED FROM THIS BRANCH**, never a line-aligned keep-both. Every `pgTable` ends in the same `createdAt` / `updatedAt` / `});` tail, git aligns the hunk boundary on it, and a keep-both there splices one table's fields into another's object literal. All five count sites were recomputed from `TABLE_KEYS.length` on the merge result rather than added up from the two branches.

**ONE LINE THAT WAS NOT THIS SESSION'S IS CORRECTED IN PASSING AND IS NAMED HERE RATHER THAN LEFT.** [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts)'s `identity_links` comment still read *"ONE OF THE 58"* on `main` where the line directly above it had moved to 55; the conflict landed in that hunk, both now read **51**, and 51 is what `TABLE_KEYS.length` gives. **Neither session 197's STATE section nor its log was edited.**

**THE `wallet_entries` UNIQUENESS GAP WAS NOT CLOSED AND IS NOT THIS FENCE'S.** [Session 169](2026-08-24-session-169.md) measured that `wallet_entries` carries four plain indexes and no uniqueness of any kind, `ledger_transaction_id` among them, so nothing structurally refuses a second `payout` credit for one settlement. That is `M20-c`, it needs a ruling and then a migration, and this session holds neither.
