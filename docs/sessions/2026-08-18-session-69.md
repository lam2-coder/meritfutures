---
status: approved
depends_on: [README.md]
last_updated: 2026-08-18
---

## 2026-08-18 - Session 69: `CI-06t`, and the four live sites the tree scan found before anything was seeded (non-money)
<!--index: `CI-06t`, and the four live sites the tree scan found before anything was seeded -->

**This log names the span tokens rather than spelling them**, as the gate, its `covers` line, the STRATEGY row and every commit message here do. A document describing this gate is a document carrying the defect unless it is careful, and the ALLOCATION row reserving this very letter proved it within a minute of being written.

- **Done:** **[`CI-06t`](../testing/STRATEGY.md) is written, seeded twice and watched firing on both**, with two scope cases passing out of scope and their controls firing. **Four real sites in three documents were found by running the check across the tree before anything was seeded, and all four are repaired.** **19 of 19 gates pass, `falsify.mjs` is green with 23 scope cases, and `pnpm vitest run` is 60 files and 908 passing.**
- **Next:** `CI-06o` and `CI-06s` are sessions 67 and 68 and are not written here. Session 70 owns `## Next 3 actions`, which this session did not touch.
- **Blockers:** None. One repaired file sits outside the stated fence and is in the landmines below.
- **Landmines:**

  - **A TOTAL COUNT OF OPENERS AGAINST CLOSERS IS NOT THIS CHECK, AND A FIRST IMPLEMENTATION WOULD NOT LEARN THAT FROM THE FIRST SEED.** The simple seed, one opener with nothing after it, fires under either reading. **The second seed is the one that separates them**: an unclosed opener early with a legitimate closed span later, which is two openers and one closer. A counter sees an imbalance it cannot locate; the pairing `CI-06g` actually performs sees one tidy span whose content happens to be everything in between. If a future edit turns this gate into a counter, seed one still fires and seed two stops.

  - **THE GATE'S OWN FIRST DRAFT WAS THE FIFTH INSTANCE OF THE CLASS IT EXISTS FOR, AND THE RECORD IS KEPT RATHER THAN TIDIED AWAY.** It sorted span tokens by **line** with an opener-first tiebreak, and reported **fifty findings against a clean tree**: [INDEX](../INDEX.md) carries three spans on one line and [STRATEGY](../testing/STRATEGY.md) carries two, which a line-keyed sort reads as three openers followed by three closers. Sorted by character offset it is clean. A scope case now carries two spans on one line specifically so the regression cannot return quietly, and the comment in the gate says what happened.

  - **FOUR REAL SITES, AND THE SCAN THAT FOUND THEM RAN BEFORE ANY SEED.** [`unsigned-adr-audit`](../decisions/gates/unsigned-adr-audit-2026-08-18.md) line 263, [session 25](2026-08-15-session-25.md) line 14, and [session 30](2026-08-15-session-30.md) lines 29 and 48. All four spell an opener out in prose while describing a span. **Session 30 is the sharpest**: line 48 opens twice on one line with neither closed, while line 29 had already left one open above it. The repair is the one STATE took: name the span rather than spelling it. No claim in any of the four sentences changed.

  - **ONE REPAIRED FILE IS OUTSIDE THE FENCE.** The audit sits under `docs/decisions/gates/` and the fence was `gates.mjs`, `falsify.mjs`, STRATEGY, STATE append-only and `docs/sessions/`. It was repaired on the brief's explicit instruction to fix a real unbalanced document found on arrival, and it is named here, in the commit and in the PR rather than slipped in.

  - **THE DOCUMENT SET IS `markdownFiles()` AND THE OBVIOUS REUSE WOULD HAVE BEEN WRONG.** `isCorpusDocument` is `CI-06b` and `CI-06c`'s shared reader under `OQ-P1-04`, and it answers "is this a thing with a gateable status and an INDEX row". **This gate protects one parser**, so the population it guards must be the population that parser reads, which is every markdown file. **The audit file above is the proof**: `isCorpusDocument` excludes it as a registry entry, so a gate built on the tidier helper would have been green over one of the four real sites.

  - **`falsify.mjs` IS THE ONE PLACE A LITERAL TOKEN IS UNAVOIDABLE AND IT IS STILL NOT WRITTEN OUT.** Two builders assemble the opener and the closer, on `RI-02`'s idiom and the same reasoning `CI-06q`'s seed states: the harness lives inside the tree the gate scans, and a literal there would make the harness a finding of the gate it is proving.

  - **ONE CONTROL WAS WRONG AND THE HARNESS IS WHAT SAID SO.** It asserted the interleaving message where its seeded arrangement produces the end-of-file one, and the run reported `CONTROL DID NOT FIRE`. **A control whose expectation is wrong proves nothing and the `PASS` beneath it asserts nothing**, which is this harness working rather than a nuisance. Corrected against what the runner reported rather than argued with.

  - **THE GATE IS DELIBERATELY NARROWER THAN "THE SPANS ARE CORRECT".** It says nothing about a span's name or its content, which is `CI-06g`'s half. A perfectly balanced file whose every span holds a stale number passes here and is `CI-06g`'s finding. The two are not merged, so neither is taken on trust for the other.

- **Files touched:** [`gates.mjs`](../../scripts/corpus/gates.mjs) (block inserted after `ci06r`, immediately before the `GATES` array; `ci06t` added as the array's last entry), [`falsify.mjs`](../../scripts/corpus/falsify.mjs) (seed key last in `SEEDS`; three cases before `CI-06p/unclaimed-letter`), [`STRATEGY`](../testing/STRATEGY.md) (row last in section 4.4), [STATE](../STATE.md) (one appended section), [session 25](2026-08-15-session-25.md), [session 30](2026-08-15-session-30.md), this log and [`sessions/README.md`](README.md). **Outside the fence and named:** the [unsigned-ADR audit](../decisions/gates/unsigned-adr-audit-2026-08-18.md). **Nothing was reordered, reformatted or renumbered**, so sessions 67 and 68 keep their anchors.
