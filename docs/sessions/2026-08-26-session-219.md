---
status: approved
depends_on: [README.md]
last_updated: 2026-08-26
---

## 2026-08-26 - Session 219: the idempotency layer and the PSP webhook receiver

- **Objective:** `P3-k`. The idempotency layer every mutating endpoint uses, and `POST /webhooks/psp/:provider`. [P3 wave 3](../plans/P3-wave3-modules.md) section 9's `P3-k` row and section 15's prompt at line 988, as amended in four places by the dispatch. **MONEY PATH**, [ADR-003](../decisions/ADR-003.md) strict.

---

### Plan

**This section is the plan gate.** [ADR-003](../decisions/ADR-003.md) asks for plan mode on a money path; this is a remote autonomous session and the harness's plan mode blocks on an approval prompt nobody is watching, so the discipline is discharged in writing instead. **It is committed and pushed before any source file is touched.** If the plan moves while the slice is built, this section is amended in place and the move is named.

#### 1. What was read and measured before anything was planned

Every clause of the dispatch's four amendments was checked against the tree at `f550d9f` rather than taken on trust, which the dispatch asked for by name.

| # | Claim | Verified |
|---|---|---|
| 1 | **`idempotency_keys` IS already registered.** AMENDMENT 2 holds and the plan's prompt is stale | [`scope.ts:1032`](../../packages/db/src/scope.ts), `class: 'owned'`, `column: 'identity_id'`, `nullable: true`. Session 215 landed it. **No `packages/db` file is in this fence and none is touched** |
| 2 | **`psp_webhook_events` is `firm`** | [`scope.ts:601`](../../packages/db/src/scope.ts). Its `(psp, provider_event_id)` uniqueness is [`0006_commerce.sql:310`](../../packages/db/migrations/0006_commerce.sql), already a constraint |
| 3 | **AMENDMENT 3's hole is real, and it is WIDER than the amendment states** | `scopedDb` needs an identity a webhook does not have; `firmDb()` is `FirmTableKey` and `idempotencyKeys` is `owned`; `SystemReason` is `'nightly-batch' \| 'operator-console'` ([`scoped-db.ts:174`](../../packages/db/src/scoped-db.ts)). **But admitting a third reason would not fix it**, which is item 4 |
| 4 | **AMENDMENT 4's defect is real and it is not only `update`.** The accessor CANNOT NAME A ROW, at any authority | [`scoped-db.ts:694,701,704`](../../packages/db/src/scoped-db.ts) and `714,718,721`. `ScopedTx` narrows by tenancy and by nothing else: `rows(key)`, `update(key, values)` and `delete(key)` take no predicate either. **So the identity-bearing path is blocked for the same reason as the identity-free one** |
| 5 | **AMENDMENT 4's prescribed remedy is itself unavailable inside this fence** | `sqlExecutor(reason)` takes `SqlExecutorReason`, which is the single member `'job-enqueue'`, and [`scoped-db.ts:812`](../../packages/db/src/scoped-db.ts) THROWS on anything else. Widening it is a `packages/db` edit, which AMENDMENT 2 says to **report rather than take** |
| 6 | **Fastify 5.12.1 destroys the raw body before any handler runs** | Measured, not assumed. Probe run inside `apps/api`: the default `application/json` parser hands the handler a plain `Object`, `request.raw.readable` is `false`, and an unknown content type is `415` before the handler. `addContentTypeParser('application/json', { parseAs: 'buffer' })` delivers the exact bytes, **and it applies even when added after the route is registered** |
| 7 | **`RouteDefinition` has no seam for a content-type parser** | [`registry.ts`](../../apps/api/src/registry.ts) is `{ method, path, handler }` and `compose` calls `app.route` with exactly those three. `registry.ts` and `server.ts` are **outside this fence** |
| 8 | **`classifyPath('/webhooks/psp/:provider')` is `public`** | [`surface.ts:139`](../../apps/api/src/surface.ts). `OPERATOR_PREFIXES` is `/admin` and `/internal`. The route is served on the public deployment, which is right: a PSP posts to the public origin |

**Baseline on this branch's base:** suite **122 files / 2,028 passed / 1 skipped**, gates **30 of 30**, invariants **9 of 9**.

#### 2. The shape the two holes force, and it is not a preference

The receiver's storage divides cleanly and the division is what decides this slice.

**What IS reachable today: the `psp_webhook_events` INSERT.** The table is `firm`, so `transaction(firmDb(), tx => tx.insert('pspWebhookEvents', ...))` runs `unscopedInsertStatement`, which carries no `WHERE` and needs none. **This is why the receiver never updates a row**: the business result is decided before the insert and written IN it, and the duplicate is the unique index refusing the second insert. That is the dispatch's own instruction ("your dedupe RIDES ON IT rather than re-checking it in application code") arriving as the reason the defect in item 4 cannot bite this path.

**What is NOT reachable today: naming one row.** The idempotency layer's whole job is to find ONE row by its key and stamp THAT ROW with a response. No accessor in [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) takes a predicate. `scopedTx.update('idempotencyKeys', values)` would stamp **every idempotency record this identity owns** with one response body, and the next replay of any key on any endpoint would serve it. `systemTx.update` would do it to every identity at once.

**So this session writes no `packages/db` implementation of the layer, and says so rather than routing around it.** Both halves are declared as PORTS, the protocol over them is pure and fully asserted, and the ruling that unblocks them is [ADR-109](../decisions/ADR-109.md).

#### 3. Files, in the order they land, one commit and one push each

1. **This log's Plan section.** The gate.
2. **[`apps/api/src/idempotency.ts`](../../apps/api/src/idempotency.ts)** (new). API_CONTRACT section 1's Idempotency paragraph as a protocol over an `IdempotencyStore` port.
   - **The scope is TWO TYPES and not one nullable field**, which is AMENDMENT 3's third option taken deliberately: `IdentityScope` and `UnownedScope` are distinct, the union is discriminated, and an implementation cannot serve one and silently answer for the other. The DDL's own reasoning is the argument: `identity_id` is nullable because `identity_id = $1` must DROP the unowned replay, so the two populations are already separate in the database and a single accessor over both is the thing the schema refuses.
   - Four outcomes, from the contract: `fresh`, `replay` (identical body, stored response returned verbatim), `reuse` (same key, different body, `409 idempotency_key_reuse`) and `in_flight` (same key, identical body, no response stored yet). **The fourth is not in the contract and is named as this session's addition** with its reasoning stated at the declaration.
   - `request_hash` is SHA-256 over the raw request bytes, `node:crypto`.
   - The stored `key` is the endpoint-prefixed key, which is [`0017_events_and_audit.sql:103`](../../packages/db/migrations/0017_events_and_audit.sql)'s own comment ("scoped by endpoint prefix") rather than an invention.
   - A comment AT the store boundary naming AMENDMENT 4's defect by file and line, so it is a grep target when the fix lands.
3. **[`apps/api/src/routes/webhooks-psp.ts`](../../apps/api/src/routes/webhooks-psp.ts)** (new). The receiver.
   - `receivePspWebhook(...)` is a pure function over raw bytes, headers, a clock, a scheme, a secret resolver, an `apply` port and a `PspWebhookLog` port. It returns `{ status, body }` and touches no framework.
   - The order is section 10's: verify on the RAW BYTES through [`packages/psp`](../../packages/psp/src/webhook.ts)'s `verifyHmacWebhook`, which is the only parse in the tree, then the insert.
   - A refusal writes the `signature_verified = false` row and returns **401**, and the `apply` port is never reached. Asserted in that direction, not only in the passing one.
   - A duplicate returns **200** and is not an error path.
   - Out-of-order is **DEFERRED AND RE-EVALUATED**: the row lands with `processing_result = 'out_of_order_deferred'`, a `deferred_until` and `defer_attempts = 0`. The re-drive is the batch's and is not written here.
   - `rawBodyOf(request)` accepts a `Buffer` or a `Uint8Array` and **REFUSES a parsed object rather than re-serialising it**. A re-serialisation changes the bytes the MAC covered, which turns every legitimate webhook into a 401 for the wrong reason.
   - The module named-exports the one-line content-type parser installer the seam needs. `discoverRouteModules` reads only `.default`, so a named export beside it composes exactly as before. **This is the fence-legal half of item 7**; the permanent seam is ADR-109's second ruling.
   - **No `apps/api/src/routes/index.ts`.** ADR-100 made the module list a directory listing.
4. **[`apps/api/test/idempotency.test.ts`](../../apps/api/test/idempotency.test.ts)** (new). The protocol against an in-test store, both directions on every outcome, and the two scope types asserted as separate populations.
5. **[`apps/api/test/webhooks-psp.test.ts`](../../apps/api/test/webhooks-psp.test.ts)** (new). Both fakes from `packages/psp`; the approval line's two watched properties; the receiver composed onto a real Fastify instance through `compose` with the parser installed, and the same instance WITHOUT it, so the seam is a test somebody reads rather than a paragraph.
6. **[`apps/api/package.json`](../../apps/api/package.json)**: `@merit/psp` as a workspace dependency. `node-linker=isolated` makes an undeclared import unresolvable. **No catalog entry, no registry package, no `pnpm-workspace.yaml` change.**
7. **[ADR-109](../decisions/ADR-109.md)** (new), `status: proposed`, approval line UNSIGNED.
8. **[ALLOCATION](../decisions/ALLOCATION.md)**, row `109` amended in place ([ADR-065](../decisions/ADR-065.md) T3), never joined by a second row. **[INDEX](../INDEX.md)** and **[decisions/README](../decisions/README.md)** regenerated. **[STATE](../STATE.md)** appended. **[sessions/README](README.md)** row.

#### 4. What ADR-109 will rule, stated before it is written so the entry can be held to it

> **1. `SystemReason` DOES NOT GAIN A THIRD MEMBER, and the reason is arithmetic rather than taste.** Even if it did, `systemTx.update` still renders no `WHERE`. A door that reaches the row and cannot name it is not the missing door.
>
> **2. THE MISSING CONSTRUCTION IS A PREDICATE, NOT AN AUTHORITY.** Every method on all three transaction handles narrows by tenancy or by nothing, and an idempotency layer, a saga's status advance ([session 222](2026-08-26-session-222.md) found the same wall) and a webhook re-drive all need to name one row. This is a `packages/db` ruling and it is the founder's.
>
> **3. UNTIL IT LANDS, THE LAYER IS A PORT AND THE UNOWNED REPLAY IS A SECOND TYPE.** Not a nullable field on one type: the schema's own index is partial on `identity_id IS NOT NULL`, so the database already holds two populations.
>
> **4. THE RAW BODY IS A ROUTE-SCOPED CONTENT-TYPE PARSER AND NEVER A RE-SERIALISATION.** `RouteDefinition` needs a way to say a route is served raw, and `compose` should register those routes inside an encapsulated context carrying the buffer parser. Measured: the parser applies even when registered after the route, so the seam is small. **A handler that re-encodes `request.body` to recover bytes has already lost API_CONTRACT section 10's ordering**, and the loss is silent.

#### 5. Approval line, unsigned, and the two commands that check it

**"The same provider event delivered twice produces exactly one business effect and two 200s, watched against a seeded receiver whose uniqueness check is removed, and an unverified signature is watched never reaching the handler."**

Cost if wrong: a PSP retry double-credits a purchase, which is P3's first done-clause failing silently.

#### 6. Stop condition

The layer, the receiver, both directions asserted, **30 of 30 gates**, `pnpm run verify` exit 0, full vitest green. Pull request opened. **DO NOT MERGE**: the `E2` read is owed and it is the founder's.
