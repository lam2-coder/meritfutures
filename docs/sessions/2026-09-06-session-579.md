---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 579: the line split three rows queued and none could reach, refused with a measurement, and the registration landing on the row unsplit ([ADR-384](../decisions/ADR-384.md), proposed)

<!--index: **THE ROW IS [ADR-384](../decisions/ADR-384.md), `proposed` and UNSIGNED. CONTROL OVER AN OPERATIONS DOCUMENT AND OVER `RI-35`'s SCOPE, `E2` READ OWED.** [ADR-375](../decisions/ADR-375.md) section 7, [ADR-377](../decisions/ADR-377.md) section 9 and [ADR-380](../decisions/ADR-380.md) section 10 each queued the same two steps, split `CRON_INVENTORY.md`'s replay row then register the claim in `RI-35`, and each recorded `docs/ops/runbooks/**` outside its fence. **RULED: THE SPLIT IS NOT THE REPAIR AND NEVER WAS.** `RI-35`'s leg 1 anchors a VERBATIM SUBSTRING and stores no line number, so the anchor selects the live caller half of a 2,576-character table row that also carries a retired half, occurs on exactly ONE line of that file, and the register returns **zero findings with the row unsplit**, measured before the first edit. **THE OBSTACLE HAD A REASON AND THE REASON EXPIRED**: [ADR-375](../decisions/ADR-375.md) said a registration bound to that line "breaks the moment the owed repair rewrites the row", [ADR-376](../decisions/ADR-376.md) landed that repair, and the two rows after it re-derived the CHARACTER COUNT, which was never the mechanism. **THE SPLIT IS REFUSED BY A GATE RATHER THAN BY TASTE.** Seeded as described, it turns `CI-06v` RED at `CRON_INVENTORY.md:23-40`: a markdown table row is one line, so the split ends the table and orphans EIGHTEEN scheduled-job rows from their header, and `31 of 33` gates pass under it. Written instead as a second table row it passes **32 of 33** and fabricates a job in a table whose own rule is that a job without a dead-man switch does not exist. **FIFTEEN `CRON_INVENTORY:NN` citations exist and every one sits at or below line 27, so a split moves all fifteen; TWELVE ARE ALREADY STALE and `35 of 35` invariants stayed green under the seed.** This diff moves no line in that file: `last_updated` and one clause inside line 21, which grows 2,576 to 2,975 and stays the fourth longest of nine rows over 2,000 characters. **ONE ARTIFACT AND ONE `live` CLAIM SHIPPED**, `replay-audit-src-caller`, the FIRST `docs/` site in the register, which answers [ADR-375](../decisions/ADR-375.md) section 13 question 2 with its own option (b): a LIVE runbook may be a claim site and a DATED RECORD may not, **and leg 6's sweep is NOT lifted**, asserted both ways by a case. **THE PROBE READS CODE AND COUNTS AN ARGUMENT POSITION, AND THE CASE FOUND THE HOLE FIRST**: the first version failed a registry written `[runNightlyBatch, runReplayAudit]`, where the entry point is last and the next character is `]`. **OWED AND NAMED**: `apps/worker/src/schedule.ts:407` states the same absence unregistered, a needle would reach it and nothing else, and it is another row's fence. Inherited figures re-derived: 2,576 exact, `replay.ts:608` exact, `adapter.ts:870` and `:887` exact lines but **imprecisely attributed**, since both sit inside `readPort` at `:721` and `postgresBatchPorts` at `:940` composes it. **Eight seeds, five red and three green, every file restored byte-identical under `sha256sum -c`, no restore by `git checkout --`.** 33 of 33 gates, 35 of 35 invariants, typecheck 0, lint 0, format clean, **336 files / 8,096 passed / 10 skipped** against a base of 336 / 8,085 / 10 reproduced on `0fc59a22` before the first edit, +11 by ADDITION in one file. **No `RI-nn` minted, no leg added or removed, no gate weakened, no test skipped, no `CI-06` letter spent, no migration number, and `repo-invariants.mjs`, `apps/**`, `scripts/**` and `playwright.config.ts` all UNEDITED.** -->

---

### 1. The objective

Row `384`: settle whether the line split three consecutive rows queued for `RI-35` is the right shape of repair, take it in the stated order if it is, and register the replay-audit absence claim either way.

**One objective.** It resolved into one refusal with two seeds behind it, one registration, eleven cases and eight seeds.

---

### 2. What was measured before the first edit

The suite base on `0fc59a22`: **336 files / 8,085 passed / 10 skipped / 0 failed**, with `packages/tooling/test/absence-claims.test.ts` at **48** alone. Gates **33 of 33**, invariants **35 of 35**, `typecheck`, `lint` and `format:check` all exit 0.

Then the four numbers the ruling turns on, derived before anything was written: line 21 at **2,576 characters**, the anchor clause on **exactly one line** of that file, the caller probe reporting **`absent`**, and the whole shipped register **plus the new entry** returning **zero findings** with the row untouched.

---

### 3. The ruling, and why the obstacle survived two re-derivations

[ADR-375](../decisions/ADR-375.md) section 7 gave obstacle 4 a reason: a registration bound to that line would break when the owed repair rewrote the row. **That repair landed one row later.** [ADR-377](../decisions/ADR-377.md) restated the obstacle as the bare fact and marked it STANDS; [ADR-380](../decisions/ADR-380.md) inherited the restatement and measured the character count.

**A re-derivation that measures the fact and not the reason is how a wrong obstacle survives being checked twice.** Both later rows were careful and both re-derived what was in front of them. What neither did was re-read the sentence that made the fact an obstacle.

---

### 4. What the split costs, applied rather than argued

Seeded as [ADR-375](../decisions/ADR-375.md) described it, the split turns **`CI-06v`** red: eighteen consecutive pipe lines with no delimiter row, which is the runbook's remaining eighteen scheduled jobs rendering as prose. Seeded as a second table row, every gate passes and the table gains a job with no schedule, no expected-by and no dead-man switch.

**The gate that caught it was written for something else**, which is the strongest argument in this session's record for structural gates nobody has a case for yet.

---

### 5. What shipped

One artifact, one `live` claim, one clause inside the runbook cell naming `RI-35` as the binding, and eleven cases. The clause is the cost written where the person who pays it will be: reword the sentence and leg 1 goes red.

**The register's own reasoning was read before a row was added to it.** It says a register a session can regenerate is a register nobody decided, and this entry is a decision: a `live` disposition, a probe over `src/` because the claim says `src/`, no needle because the one line a needle reaches is another row's to repair, and a written rule for which `docs/` files may ever be sites.

---

### 6. Falsification

Eight seeds, five red and three green. The two that produced findings are **E**, which turned a preference into a gate failure, and **F**, the case that failed against this session's own first probe because a registry entry written last is followed by `]` rather than `,`.

Every seed restored byte-identical and verified with `sha256sum -c` after each restore. No restore used `git checkout --`. `apps/**` was opened for reading and for four seeds and is unedited.

---

### 7. Blockers and landmines

- **`apps/worker/src/schedule.ts:407` is an unregistered claim site for the artifact this row registered**, and the needle that would reach it must land in the same commit as the entry. Another row's fence.
- **Twelve of fifteen `CRON_INVENTORY:NN` citations are stale today** and eleven of those sit in dated records that may not be rewritten.
- **`docs/sessions/README.md:141` is the one table row in `docs/` that opens and does not close**, out of 31,679.
- **`ADR-352:30` cites `absence-claims.mjs:822` for an artifact that has never been on that line**, thirty lines short when written and fifty-three after this diff. Dated record, reported and not repaired.

---

### 8. Verification, derived at reporting time

Suite **336 files / 8,096 passed / 10 skipped / 0 failed** against the base above, **+11 by addition in one file**, attributed by removal: `absence-claims.test.ts` runs 59 alone here and 48 alone on the base. Gates **33 of 33** after `generate`, invariants **35 of 35**, `typecheck` 0, `lint` 0, `format:check` clean.

**`pnpm run verify` was NOT run and `scripts/corpus/falsify.mjs` was NOT run.** Both mutate the working tree and both are forbidden to this row.

---

### 9. Next

The founder's `E2` read of [ADR-384](../decisions/ADR-384.md), and the first of the three decisions its approval block names: whether a runbook sentence may be machine-bound at all.
