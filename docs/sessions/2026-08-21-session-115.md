---
status: approved
depends_on: [README.md]
last_updated: 2026-08-21
---

## 2026-08-21 - Session 115: WAVE-04 `W8`, three `CI-06` slug gates, and three rulings the tree corrected
<!--index: WAVE-04 `W8`: three `CI-06` slug gates, `gate_count` 24 to 27. **Each gate corrected the record that commissioned it.** `<<<<<<< HEAD` never stood in a commit, so the conflict-markers case SEEDS its violation; `CI-06/fixture-inventory` found 43 fixtures on disk against 40 `written` rows and registers the three by name rather than repairing a file outside the fence; and `ADR-074`'s stated 118 series and 1,086 members do not reproduce from its own four classes, which is the check section 3 asked for. The `CI-06l` trap was walked into once, by the scope case written to avoid it -->

- **Objective:** ship `CI-06/conflict-markers`, `CI-06/fixture-inventory` and `CI-06/identifier-series`, each with its `falsify.mjs` case and its [STRATEGY](../testing/STRATEGY.md) section 4.4 row, in three commits.
- **Done:** **three of three.** `gate_count` 24 to 27, 27 of 27 gates pass clean and fail dirty, 45 scope cases and 10 loader cases hold. Each gate landed in its own commit and was pushed before the next was started.
- **Next:** `W9`, session `121`, writes `CI-06/gate-inventory` after `114` and `115` have merged, and `gate_count` moves 27 to 28 there.
- **Blockers:** none. Three findings for the founder are in the pull request and in the gate blocks; none of them blocks a merge and none was repaired here, because the file each one lives in is outside this session's fence.

**Scope was three gates and not the four [WAVE-04 section 7](../plans/WAVE-04-fixture-backlog-and-gate-inventory.md)'s `W8` brief lists.** `CI-06/gate-inventory` moved to `W9` at the `W1` to `W6` merge, because it must see `CI-09` implemented in a workflow and the session that implements `CI-09` ran beside this one. **No softened version of it was written**, which was the whole reason it moved rather than staying.

### The record that commissioned gate 1 was wrong about its own evidence

**`OI-19` says `<<<<<<< HEAD` stood in [INDEX](../INDEX.md) and [STATE](../STATE.md) while the gates passed. It never stood in a commit**, and the check was re-run on this branch rather than taken from the brief: `git log --all -G` over `.md`, `.ts`, `.mjs` and `.json` returns nothing on any branch head or pull-request head. It stood in a **working tree** during a merge, and the record disagrees with itself on the count besides, 22 in [session 105](2026-08-20-session-105.md) and in STATE against 24 in [session 106](2026-08-20-session-106.md).

**The gate is still worth writing because the boundary it protects is the push and not the history**, and its falsification case **seeds** a marker rather than anchoring on a historical one. That is `OI-21` applied rather than quoted: a harness anchored to corpus state decays as the corpus is repaired, and the two `0029` seeds are the recorded case. A seeded anchor cannot go stale, because the tree it needs is one it creates.

**The live complication was re-checked rather than trusted.** STATE carries the opener three times in prose and all three are mid-line, so the assertion is "a line BEGINNING with a marker" and **no exemption is registered because none is needed**. `CI-06/conflict-markers/mid-line` asserts that boundary with a control on the far side of it.

Three things were measured rather than assumed, and two changed the implementation:

| Measured | Result | What it changed |
|---|---|---|
| The runner walk against `git ls-files` | **1,022 each, no file on either side of the difference** | The walk is what "tracked" means here. `git` is not used, because `falsify.mjs` copies the tree WITHOUT `.git` and a gate shelling out to git could never be watched failing |
| Lines beginning with any of the four marker shapes | **zero**, including diff3's seven pipes | The gate ships with no exemption and no register |
| A UTF-8 round trip against a NUL-byte test | **UTF-8 drops exactly the six `.xlsx`; a NUL test also drops two `.ts` sources** | `packages/rithmic/src/simulator/session.ts` and `stream.ts` embed a literal NUL as a composite-key separator. A NUL test would have been the sixth parser narrower than the property it claims |

**The separator is matched as a WHOLE line where the outer two are prefix matches**, because a markdown setext H1 underline of exactly seven `=` is textually identical to it. **diff3's base marker is not asserted, in writing rather than by omission**: seven `|` is also a table row of empty cells, and diff3 writes the outer two markers anyway, so that conflict is caught twice over.

**The markers are spelled by repetition and never as literals**, so no line of the runner can begin with one. `CI-06t` exists because a line of prose spelled a generated-span opener out.

### Gate 2's first run was red, and the three findings are its first catch

**`CI-03` has reported this direction as unimplemented on every run since it was written, in its own words**: *"The inventory check for a registry row with no fixture is `CI-06`'s and is not switched on."* A sentence that reports its own hole on every run, for as long as nobody writes the gate, is the most polite form of a silent failure there is.

**The counts came from the tree and not from [ADR-072](../decisions/ADR-072.md), and that is the finding.** 43 `.yaml` fixtures are on disk against 40 `written` rows:

| Scenario | The row says | Written by | The repair |
|---|---|---|---|
| **`GS-080`** | `writable` | `W2`, [session 109](2026-08-20-session-109.md) | The row moves to `written` with its citation pointing at the fixture |
| **`GS-059`** | `writable` | `W4`, [session 117](2026-08-20-session-117.md) | As above |
| **`GS-049`** | `blocked / format-cannot-express` | `W4`, [session 117](2026-08-20-session-117.md) | As above, **and** a disposition: whether ONE fixture discharges a row arguing three probe shapes |

**They are REGISTERED, not exempted**, because the repair is in `W1`'s file and outside this session's fence. `CI06FIXTURE_REGISTER` names each row and the repair it waits for and carries `CI06U_REGISTER`'s defining property: **an entry that no longer names a real defect is itself a finding**. So it shrinks as the repair lands, it cannot become furniture, and **a fourth such fixture fails on the day it lands**. `CI-06/fixture-inventory/register-repaired` asserts exactly that, and it is what separates a register from an exemption list.

**Assertion 4 is narrowed against the brief, in writing.** The brief asks for a blocker on every NON-WRITTEN row, which would flag the two `writable` rows; the document defines `writable` as all three `ADR-072` conditions holding, so a blocker there is a contradiction. **Those rows get the opposite assertion rather than none**, and the `writable-with-a-blocker` case proves they are still read. A narrowing tested only from the quiet side is indistinguishable from a gate switched off.

**`CI-06d` caught this session's own prose.** The STRATEGY row cited an unregistered `GS` identifier in an illustration and the gate reported it. Reworded rather than exempted; the gate was right.

### Gate 3 implements ADR-074 as ruled, and reports four disagreements rather than re-scoping

**[ADR-074](../decisions/ADR-074.md) section 2 makes choosing scope the ruling's job and not the runner's**, so where the ruling and the tree disagree the disagreement is reported. Four places, all four in the gate block, the STRATEGY row and the pull request:

| # | The disagreement | What was done |
|---|---|---|
| **1** | Section 3 states **118 series and 1,086 members** and says the count is stated *"so `W8` can check that its transcription reproduced it"*. **It does not reproduce**: its own four classes give **117 and 1,083**. The module row says 96 against the 93 its rule enumerates; the design row says 7 against the **nine series it lists by name**. `+3` and `-2`, so the total is right by one | **Nothing added to close the gap.** Adding a series the ruling does not name would be choosing scope |
| **2** | **`OQ-F6` is ruled in scope and has no register section 1 admits.** Its only definition sites are table rows in [session 94](2026-08-20-session-94.md), and a session log is a registry ENTRY rather than a corpus document ([ADR-043](../decisions/ADR-043.md)) | **The rule governs the list that applies it.** `OQ-F6` is pending, with the artifact named |
| **3** | **`P-M6` is ruled in scope and `P-M6-11` was deliberately not minted.** [Session 112](2026-08-21-session-112.md) records it: the panel table gains a row, or the third number lives inside `P-M6-01`'s block | A disposition and not a parse. `P-M6` is pending with that decision named |
| **4** | **Section 5 names ten of the eighteen** series in its first pending class. The other eight are identified nowhere in the ruling or the survey | The register holds **38** and the shortfall against 44 is reported. Picking eight of the twelve candidates in the tree would be this session choosing scope |

**It arrives at 115 series, 1,070 members and zero findings**, which is the state section 6 rules it must arrive in, reached at three fewer series than stated and with every difference written down.

**Three things were measured and each changed the implementation.**

- **Section granularity is load bearing rather than a convenience**, exactly as section 1 claims. M01 section 1 holds `INV`'s 24 definition rows and Appendix A holds a **coverage** row for the same identifiers. A file-wide register reports `INV`, `CV` and `RB` as broken; section scoping takes all three from failing to clean.
- **`M12` has TWO plan files** and the `M12` series live in the transparency one. A register derived from the series name picks the wrong file and reports four series with **every member undefined**. The register is declared, which section 2 required anyway.
- **All 38 pending entries were checked still broken.** Not one is furniture today, and the predicate can fail: `CI-06/identifier-series/pending-repaired` gives every member of one pending series a definition site and the entry becomes the finding.

### The `CI-06l` trap was walked into once, by the case written to avoid it

**`pending-repaired`'s selector is "the smallest pending series all of whose members have zero definition sites". Its seed gives every member a site.** `expect` resolves against the **seeded** tree, so re-deriving named `DT` while the gate correctly named `RE-R`, and `falsify.mjs` reported `FAILED OFF-TARGET`. **That is the harness working**: the gate was right and the case's description of its own target was wrong.

It now reads its mark back out of the rows the seed wrote. **The general rule, which the `CI-06/fixture-inventory` seeds were already written under and this one was not:** a seed that changes the property its own `expect` selects on must read a mark back, never re-derive.

### Landmines

- **`node scripts/corpus/gates.mjs generate` writes outside this session's fence, and that is the ruled workflow**, not a fence breach: `gate_count` spans live in [STATE](../STATE.md), [INDEX](../INDEX.md) and [session 38](2026-08-16-session-38.md). [Session 119](2026-08-21-session-119.md) records that `generate` is the only correct resolution of a generated span. **Nothing else in those files was touched**, and the diff carries `gate_count` and nothing besides.
- **STATE was NOT updated with this session's position**, and the omission is deliberate: the fence is `scripts/corpus/`, [STRATEGY](../testing/STRATEGY.md) and this log. **Whoever merges this owns the STATE edit**, along with the three findings above.
- **`falsify.mjs` takes 32 seconds on the runner and several minutes in a session sandbox, and the first number is the one that matters.** This entry originally claimed "several minutes" from the sandbox timing alone. **The CI log refutes it**: on [the `CI-06 corpus integrity` job for `5d4be98`](https://github.com/lam2-coder/meritfutures/actions/runs/32509634267/job/96857483482) the step ran `17:43:37` to `17:44:09`, and the whole job finished in 39 seconds including install. **A landmine measured on the wrong machine is exactly the class this session found three of**, so it is corrected here rather than left to send a future session optimising a cost the runner does not pay. Three gates were added and each walks the whole tree; the harness runs every gate in its own process, twice, plus 45 scope cases. If it looks hung locally, it is not.
- **`scripts/` is outside the repository's prettier scope** (`format:check` covers the root and `{apps,packages}`). Both runner files were unformatted before this session and were left that way; reformatting them would bury a 771-line diff in a whole-file rewrite.

### Files touched

| File | What |
|---|---|
| [`scripts/corpus/gates.mjs`](../../scripts/corpus/gates.mjs) | Three gates, their two hardcoded tables and two registers, and the three `GATES` registrations |
| [`scripts/corpus/falsify.mjs`](../../scripts/corpus/falsify.mjs) | Three seeded violations, eight scope cases, and the readers they need |
| [`docs/testing/STRATEGY.md`](../testing/STRATEGY.md) | Three section 4.4 rows |
| [`docs/sessions/README.md`](README.md) | This entry, and the `115` claim row struck |
| STATE, INDEX, session 38 | `gate_count` spans only, by `generate` |
