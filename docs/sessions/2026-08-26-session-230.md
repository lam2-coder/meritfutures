---
status: approved
depends_on: [README.md]
last_updated: 2026-08-26
---

## 2026-08-26 - Session 230: the two owed gates, and the discriminator the receipt proposed was measured and replaced
<!--index: **Two gates, `gate_count` 30 to 32, and [ADR-118](../decisions/ADR-118.md) at `status: proposed`, unsigned.** `CI-06/table-row-width` catches [#299](https://github.com/lam2-coder/meritfutures/pull/299)'s corruption, which all 30 gates passed over: a row whose cell count disagrees with its table's delimiter row. **The first-cell-shape rule #299 proposed was measured and rejected** -- at an 80% homogeneity threshold it reaches 593 of 1,593 tables and MISSES #299, at 50% it catches #299 and costs 155 registered rows; cell count reaches 1,604 of 1,604 with NO shape exemption and 17 registered rows. `CI-06/derivable-counts` runs the sweep `CI-06g` has declared owed in its own covers line since it was written. **ADR-118 was reserved conditionally and the condition was met**: enforcing ADR-034 forced a ruling on WHICH documents its count rule reaches, and a section headed by a date or a session number is a RECORD it does not reach. 32 of 32 gates, 80 scope cases, `verify` exit 0 -->

- **Objective:** the two gates this corpus has named as owed and never written. Both `CI-06` slugs, no gate letter, not a money path.

- **Done:**
  - **`CI-06/table-row-width`.** Every markdown table row under `docs/` carries the cell count its DELIMITER row declares, in both directions. That is GitHub-flavoured markdown's own rule: the delimiter declares the column count, a short row is padded with empty cells and a long row has its excess SILENTLY DISCARDED.
  - **`CI-06/derivable-counts`.** No prose sentence under `docs/` states a count this runner derives. This is the sweep `ADR-034` deferred (_"the sweep is the gate's first run and is not done here"_) and `CI-06g` has reported owed on every run since, in its own words: _"the corpus-wide sweep for bare numerals adjacent to a registry noun needs the generator and is NOT run here."_
  - **[ADR-118](../decisions/ADR-118.md), `status: proposed`, approval line UNSIGNED**, one clause, executable, scoped to this branch's files. The clause is #299's corruption replayed on a scratch copy and caught by the new gate while the other 31 report PASS on the same seeded tree.
  - **`gate_count` 30 to 32**, rewritten by `gates.mjs generate` across 196 spans in eight files. No span was edited by hand.
  - `falsify.mjs` gains two seeded violations and eleven scope cases. **32 of 32 gates pass clean and fail dirty, 81 scope cases hold** (70 before), 10 loader cases.

- **Next:**
  - **A `SPAN_QUERIES` entry for the receipt's own number, and it is the more valuable of the two owed items.** "How many registered tables carry a later `ADD COLUMN`" is derived by [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts)'s replay and by nothing in `gates.mjs`, so `CI-06/derivable-counts` reaches the CLASS the receipt belongs to and not that INSTANCE. It needs a query reading the migrations, which is `packages/`-adjacent and was outside this fence.
  - **A comment-span generator, so source files come into scope.** The same reader over every `.ts`, `.tsx`, `.mjs` and `.sql` finds **17 sites**, eleven under `packages/` -- four in [`schema.ts`](../../packages/db/src/schema.ts) and three in [`scope.ts`](../../packages/db/src/scope.ts) -- and six in the runner and its own harness. They are out of scope today because `generate()` rewrites markdown only, and a finding with no remedy is how a gate gets switched off.
  - **The `session_entries` generator should escape pipes as it builds a cell.** Three of the 17 registered ragged rows are generated rows whose `<!--index:` source carries a pipe inside an inline code span, so the generated row splits and its tail is dropped. Fixing the generator fixes all three and every one after them.

- **Blockers:** none.

- **Landmines:**
  - **SIX ROWS OF [`ALLOCATION`](../decisions/ALLOCATION.md)'S OWN ADR TABLE CARRY A FOURTH CELL IN A THREE-COLUMN TABLE, AND EVERY MARKDOWN RENDERER DISCARDS IT.** Rows `079`, `088`, `095`, `104`, `112` and `0046`. The discarded cell is a whole disposition paragraph, in the registry a sibling branch reads to decide whether a number is free. **This session held ONE row of that file and took only that row**, so they are registered rather than repaired. The repair is one escaped pipe per row.
  - **[`STRATEGY`](../testing/STRATEGY.md) CARRIES A BYTE-IDENTICAL DUPLICATED PROSE LINE AT 271 AND 272**, and nothing in this runner can see it. `CI-06u` reads TABLE rows only, `CI-06v` reads pipe runs, and a repeated paragraph is neither. It is a keep-both merge artifact of exactly the class `CI-06u` was written for, one layer out. **Not repaired: session 231 holds that file tonight.** It is a candidate for a third gate and is recorded rather than fixed in passing.
  - **`CI-06/derivable-counts` CANNOT SEE A COUNT THAT HAS ALREADY DRIFTED, AND THAT IS DELIBERATE.** Recognition is value-anchored: the cardinal must equal what its bound query returns. On the merged tree the receipt read NINE against a truth of TEN and this gate would have been silent. **On either branch alone it read NINE against a derivation of NINE and is named there**, while it is still right, which is the only moment a span can be installed without anybody adjudicating the truth. A future session that "strengthens" this by dropping the value clause takes the gate from zero findings to **206**, almost all of them local subsets like `1 gate` and `Six deltas`.
  - **THREE OF `CI-06/derivable-counts`'s EIGHT VOCABULARY ENTRIES COULD NEVER MATCH ANYTHING, AND THE HARNESS COULD NOT SEE IT.** The first reader captured the noun with a lazy `[A-Za-z ]*?` and a word-boundary lookahead, which stops at the FIRST word: `edge cases` was read as `edge`, so `ec_count`, `gs_count` and `manifest_changes` were dead. The gate reported PASS on a clean tree, correctly, while asserting five eighths of what its `covers` line claimed. **Every scope case passed too, because every one of them used the single word `gates`.** Found by re-reading the regex, not by the harness -- which is this project's own recorded lesson about the class of error a bigger model does not fix. The repair anchors each vocabulary pattern against the text instead of capturing a noun and comparing it after, and `CI-06/derivable-counts/a-two-word-noun-is-one-noun` is the case that now watches it. **A vocabulary of eight entries is not one code path watched eight times.**
  - **THE REGISTER FOR `CI-06/table-row-width` IS KEYED BY (file, first cell) AND NOT BY LINE, BECAUSE FOUR OF ITS ENTRIES LIVE IN A GENERATED TABLE.** `docs/sessions/README.md`'s index moves on every regeneration. A line-keyed register would go stale on every session.
  - **BOTH REGISTERS SHRINK ONLY.** An entry that no longer names a real defect is itself a finding. A repair that lands without the register following it turns the gate red, which is the friction that assertion exists to create.
  - **`CI-06/table-row-width`'s SEED PERFORMS #299's EDIT RATHER THAN PASTING ITS OUTPUT.** It locates the paragraph after the reservation table in the tree copy and rewrites its continuation lines as rows, the way prettier did. A frozen copy of #299's nine corrupted lines would have gone stale the day that paragraph was reworded, and would have gone stale SILENTLY, because a literal block appended to a file needs no anchor and always seeds something. The anchor assertion throws instead.
  - **THE SEEDS FOR `CI-06/derivable-counts` DERIVE THE GATE COUNT FROM `gates.mjs list` AND NOT FROM A `gen:` SPAN.** Reading a span makes every one of them depend on `CI-06g` being green in the copy: on a branch that has added a gate and not yet run `generate`, the span says one number and the query says another, the seed writes a sentence the gate correctly declines to match, and the harness reports DID NOT FAIL -- which reads as a gate problem -- for what is a stale span. **This was watched happening before it was fixed.**

- **Files touched:** [`scripts/corpus/gates.mjs`](../../scripts/corpus/gates.mjs), [`scripts/corpus/falsify.mjs`](../../scripts/corpus/falsify.mjs), [`docs/decisions/ADR-118.md`](../decisions/ADR-118.md), [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) (row `118` only), [`docs/decisions/README.md`](../decisions/README.md) and [`docs/sessions/README.md`](README.md) and [`docs/INDEX.md`](../INDEX.md) and [`docs/testing/STRATEGY.md`](../testing/STRATEGY.md) and five session logs (**generated spans only**, rewritten by `gates.mjs generate`), [`docs/STATE.md`](../STATE.md) (append plus its `gate_count` spans), this file.

- **Not touched, deliberately:** `.github/workflows/ci.yml` (session 228 holds it, and no new workflow step is needed: `gates.mjs check` already runs every gate in one step). `STRATEGY`'s section 4.4 inventory: `CI-06p` reads LETTERS and explicitly does not require an implemented gate to have a row, and nothing else in the runner demands one for a slug. Nothing under `packages/` or `apps/`. No migration. No `CI-06` letter, and the letter allocation table is untouched.
