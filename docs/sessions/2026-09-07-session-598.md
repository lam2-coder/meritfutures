---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 598: the four stay wrong and now say why, the binding nobody checked has been checked all along, and one coordinate that was exact on the day it was written

<!--index: **THE ROW IS [ADR-403](../decisions/ADR-403.md), `proposed` and UNSIGNED. NON-MONEY, DATED RECORDS AND ONE TEST FILE'S HEADER COMMENT, `E2` READ OWED.** **THE FOUR `:35` POINTERS INTO [`CRON_INVENTORY.md`](../ops/runbooks/CRON_INVENTORY.md) ARE CLASS TWO FOR THE SECOND TIME AND ALL FOUR NOW CARRY THE FINDING [ADR-388](../decisions/ADR-388.md)'s OUTCOME TWO REQUIRES.** The dispatch instructed this row to test whether *"these four must stay wrong"* was itself an inherited claim of the kind [ADR-399](../decisions/ADR-399.md) demolished for the eight, and to repair them if it was. **It is not, and they are not.** `G2` was re-derived at the blob at both write commits, `a21bfb8e` and `4b3e8e57`: runbook line **35** held the **Tier-1 economic calendar staleness check** row and the **Scheduled digest delivery** row that is the subject sat at **36**, one below. All four were **one short on the day they were written**. **The mechanism is sharper at the blob than in prose**: read whole, the two rows are the same sentence with one noun changed, *"whether any loader reported success"* against *"whether any delivery run reported success"*, and the records quote the second. **[ADR-399](../decisions/ADR-399.md) SECTION 12 ITEM 4 IS REFUTED AND THERE WAS NOTHING TO RULE**: it says *"nothing checks the target exists"* and **`CI-06a` checks it**, reads the runbooks while doing so, and has been one of the 33 gates every wave has run. **Seed G watched it go RED** on a renamed `RB-01` target and name the site. **This row did not refuse a gate. It found the gate running**, which is the ninth consecutive wave to reach a gate question and the first to answer it that way. Priced anyway: **zero renames and zero deletions** under `docs/ops/runbooks/` in 4,320 commits, **zero of 225** links in that scope broken, and a naive check that failed to strip inline code would redden **10 metasyntactic placeholders** it must not touch. **The document carries SIX `RB-nn` links and not three.** **[ADR-337](../decisions/ADR-337.md):202 IS CLASS ONE**, not class two: at `7bfd86f6` line **8412** of [`gates.mjs`](../../scripts/corpus/gates.mjs) held the `RETIRED_REGISTER` key for exactly the document that row was editing with `sites: 7` two lines below, so **both halves of the token were exact when written**. Cardinal untouched and tense dated, coordinate replaced by the entry **NAMED** because the number is knowable at **8667** and row `402` is restructuring that file this wave. **RULED: the annotation convention's site is the SOURCE line**, so re-rendering a generated copy is not overwriting a record, which is [ADR-400](../decisions/ADR-400.md) section 17 item 3's scope question met on its generated-line edge. **ONE FILE COULD NOT BE LINE-COUNT NEUTRAL AND THE ROW SAYS SO**: fourteen comment lines in [`digests.test.ts`](../../apps/worker/test/digests.test.ts) moved [ADR-320](../decisions/ADR-320.md):75's `:1155`, exact for its entire life, repaired to `:1169` with its class derived rather than waived. **[ADR-399](../decisions/ADR-399.md)'s population table sums to 20 through two offsetting grain shifts**, neither stated. **Seeds G and H restored byte-identical under `sha256sum -c`**, neither with `git checkout --`. **`CI-06f` watched RED** naming both sibling holes before the reservations closed it. Suite **336 / 8,109 / 10 / 0** against a base reproduced on `28e2b0ee` before the first edit of **336 / 8,109 / 10 / 0**, gates **33 of 33**, invariants **35 of 35**, typecheck, lint and `format:check` clean. **No clone on arrival, five waves running, and the one made here is FULL**, `is-shallow` **`false`** at 4,320 commits, against a dispatch reporting `true` on its own container -->

### 1. The objective

Write the finding beside the four `:35` pointers [ADR-399](../decisions/ADR-399.md) section 12 item 1 left standing, rule on the `RB-nn` binding its item 4 raised, and derive and discharge the `:8412` coordinate [ADR-398](../decisions/ADR-398.md) section 13 item 1 found missed at [ADR-337](../decisions/ADR-337.md) line 202. **Every figure handed over was to be re-derived, and the claim that the four must stay wrong was to be tested rather than accepted.**

### 2. Done

- **The four `:35` sites annotated**, at [STATE](../STATE.md):6095, [session 323](2026-08-28-session-323.md):9, [`sessions/README.md`](README.md):556 and [`digests.test.ts`](../../apps/worker/test/digests.test.ts):60. Class two re-derived at the blob rather than inherited. Three of the four are line-count neutral.
- **[ADR-399](../decisions/ADR-399.md) section 12 item 4 refuted**, with `CI-06a` read at its own definition and watched red on seed G.
- **[ADR-337](../decisions/ADR-337.md):202 derived CLASS ONE and repaired to a name**, cardinal untouched and tense dated, per [ADR-398](../decisions/ADR-398.md) section 6.
- **[ADR-320](../decisions/ADR-320.md):75 repaired** under the citation-repair exception, because this row's own diff moved it.
- **[ADR-403](../decisions/ADR-403.md) written**, `proposed`, approval line unsigned.

### 3. Landmines

- **A GREEN RUN SAYS NOTHING ABOUT THREE OF THE FOUR ANNOTATIONS.** `RI-16` masks every dated record and `RI-15` reads no `.md`. Seed H moved an annotated pointer 9,803 lines past the end of its file and nothing went red. **Only reading says anything here, and every annotation and every repair was read back at its own line.**
- **`CI-06a` IS A LINK CHECK AND THE CORPUS DID NOT KNOW IT.** Two entries recorded that nothing checks a thing it checks. Row `402` is restructuring 30 of 33 `covers` strings this wave, and whether anybody reads them before stating an absence is the question that makes urgent.
- **THE FOURTEEN-LINE INSERTION IS A TASTE JUDGEMENT WITH A COST.** Neutrality was available as a single comment line of about a thousand characters in a file whose longest comment is 86, and it was rejected on Appendix F2 grounds. It broke a pointer that had been exact for its entire life.

### 4. Blockers

None. **The `E2` read is owed and is not a blocker to merging**, and merging is not signing.

### 5. Files touched

[`docs/decisions/ADR-403.md`](../decisions/ADR-403.md) (new), [`docs/decisions/ADR-337.md`](../decisions/ADR-337.md), [`docs/decisions/ADR-320.md`](../decisions/ADR-320.md), [`docs/STATE.md`](../STATE.md), [`docs/sessions/2026-08-28-session-323.md`](2026-08-28-session-323.md), [`docs/sessions/README.md`](README.md), [`apps/worker/test/digests.test.ts`](../../apps/worker/test/digests.test.ts), [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md), [`docs/decisions/README.md`](../decisions/README.md), and this file. **[`CRON_INVENTORY.md`](../ops/runbooks/CRON_INVENTORY.md) was read, seeded, restored byte-identically and never left edited.**

### 6. Next

- **[ADR-392](../decisions/ADR-392.md) section 11 item 5 still carries the refuted clause.** Whether a false non-numeric claim in a dated record is annotated at its site is [ADR-400](../decisions/ADR-400.md) section 17 item 3's open question, now with a second instance.
- **`G1` should require a token UNIQUE in the cited file**, [ADR-399](../decisions/ADR-399.md) section 12 item 2. This row is a second and sharper receipt: the two candidate lines differ by one noun.
- **[ADR-388](../decisions/ADR-388.md)'s census of 289 has still never been re-run under `git log -S`.**
