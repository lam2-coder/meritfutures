---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 278: `P6-c`, the live cache, its role and its grant

<!--index: **[ADR-164](../decisions/ADR-164.md), `status: proposed`, UNSIGNED. Migration [`0050`](../../packages/db/migrations/0050_live_cache_and_role.sql) SPENT: `live_account_state` and a fifth role `merit_live`, `SD-M2-07`.** **[P6 section 10 item 1](../plans/P6-live-tier.md) held this slice for a founder answer, the answer was DELEGATED, and reading 1 of three is ARGUED AND TAKEN.** **THE SENTENCE THE SLICE STARTS FROM WAS EXECUTED RATHER THAN BELIEVED**: on the `0001`..`0049` tree a bare `CREATE TABLE zz_hazard_demo (id int)` returns `t` on all four verbs for `merit_app`, with nothing granting it, so [`0026`](../../packages/db/migrations/0026_roles_and_grants.sql)'s `ALTER DEFAULT PRIVILEGES` really does move the separation further away when a table is added. **THE DEPARTURE FROM SIX PRECEDENTS IS ONE VERB**: `0032`, `0039` and `0049` each revoke `UPDATE, DELETE` because each implements append-only, and `FM-M12-08` is about READING, so `0050` revokes `ALL` from `merit_app` AND `PUBLIC`. **A PROCESS-LOCAL STORE IS REFUSED ON `GS-132`**, the phase's own done-condition, which requires the cache populated from OUTSIDE the process that writes it; **A SECOND STATEFUL SERVICE IS REFUSED ON A GROUND [OVERVIEW](../architecture/OVERVIEW.md) SECTION 3 DOES NOT SUPPLY**, because neither limb of the pg-boss sentence transfers to a discardable cache with one writer, and what actually refuses Redis is that it makes the separation a CONVENTION again. **THE COST IS NAMED AND NOT SPENT**: [`client.ts`](../../packages/db/src/client.ts) opens one pool from one `DATABASE_URL` and [INFRA section 5](../architecture/INFRA.md) gives `merit_app` to *"API and worker at runtime"*, so no grant can separate the API's read from the stats worker's, and clause 4 rules the live path a principal the database can name while refusing to say WHICH process -- `P6-b`'s address and `P6-g`'s mechanism. **`F1` forecloses the cheap escape**, granting `merit_app` `SELECT`, which makes `FM-M12-08` false with nothing failing. **THREE COLUMNS REFUSED AND NO MONOTONICITY TRIGGER**, so triggers move 20 to 20. **THE STORE IS NOT APPEND-ONLY**, which resolves [P6 section 3.5](../plans/P6-live-tier.md)'s range-term question to NO. **THE FENCE HAS TWO CELLS IT SHOULD NOT HAVE AND ONE IT LACKS, BOTH EXECUTED**: the table is NOT registered on [ADR-092](../decisions/ADR-092.md)'s own rule quoted in [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts), and that same unfenced file's `expect(createdTables).toBe(112)` counts `^CREATE TABLE` over the whole migration set and was watched failing at 113. **Applied from empty against PostgreSQL 16.13 under `ON_ERROR_STOP`, 50 of 50**; tables 112 to 113, indexes 395 to 396, `CHECK`s 477 to 479, constraints 773 to 777, triggers 20 to 20, read from the catalogue rather than from a grep; **grant probe in BOTH directions and five rejections watched firing**. 33 of 33 gates, 11 of 11 invariants, typecheck 0, lint 0, 170 files / 3,115 passed / 6 skipped, `falsify` clean in 5m20s. **NOT PUSHED AND NO PULL REQUEST: the repository is not in this session's authorized set** -->

- **Objective:** [P6](../plans/P6-live-tier.md) `P6-c`. The live cache's medium, its role and the grant that makes `INV-M2-14` structural. [P6 section 10 item 1](../plans/P6-live-tier.md) held this slice for a founder answer and that answer is DELEGATED to this session by [ALLOCATION](../decisions/ALLOCATION.md)'s `ADR-164` row, which requires one of three readings ARGUED rather than assumed.
- **Done:** [ADR-164](../decisions/ADR-164.md), seven ruling clauses, six foreclosures, eighteen findings of which the first three are EXECUTIONS against PostgreSQL 16.13 rather than readings. [`0050_live_cache_and_role.sql`](../../packages/db/migrations/0050_live_cache_and_role.sql), the [`live_account_state`](../architecture/data-model/live_account_state.md) design record and its index row, `SD-M2-07` and section 27 of [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md) carrying both transcripts, [ALLOCATION](../decisions/ALLOCATION.md) rows `164` and `0050` amended in place, [STATE](../STATE.md) appended, the generated spans re-run and committed. **33 of 33 gates, 11 of 11 invariants, typecheck exit 0, lint exit 0, 170 test files / 3,115 passed / 6 skipped, `falsify.mjs` clean.** The baseline was reproduced on the dispatch SHA `552c918` before anything changed and it matched the dispatch exactly.
- **Next:** `P6-f`, `P6-g` and `P6-j` all cited `P6-c` and all now have it. **`P6-f` inherits two things it should not rediscover**: the upsert's monotonicity predicate (`F6`, owed) and the fact that **its ingest cannot run inside the batch worker's process**, because that process is `merit_app` and clause 4 puts the cache behind `merit_live`. **`P6-g` and `P6-j` inherit the registration**, which is a THREE-file act and not the two this fence named, and the read path's principal. **`P6-b` inherits clause 4's open half**, which is which process holds `merit_live`, and it is the same question as its address ruling seen from the database.
- **Blockers:** **THE BRANCH IS NOT PUSHED AND THERE IS NO PULL REQUEST, AND THAT IS AN ENVIRONMENT BLOCKER RATHER THAN A WORK ONE.** `git push` returns `403`: *"lam2-coder/meritfutures is not in this session's authorized repository set, so the proxy will not inject a credential for it."* The sanctioned repair is attaching the repository to the session's sources, and both attempts at it were denied by the permission classifier. **Six commits sit on `claude/p6c-live-cache` locally and the container is ephemeral.** Nothing about the slice is blocked; the delivery of it is. **No other blocker.** `P6` section 10 item 1 is answered and the four slices behind it are unblocked by this branch's content the moment it lands.
- **Landmines:** **`F1` IS THE ONE.** The live read path is blocked today by design, and the smallest diff that unblocks it is `GRANT SELECT ON live_account_state TO merit_app`, which costs nothing visible and makes `FM-M12-08` and `DEP-M12-08` false. Nothing in this tree would fail. **A gate for it does not exist and is OWED**: [`assert_append_only_grants.mjs`](../../scripts/db/assert_append_only_grants.mjs) reads the revoke list against the document for append-only tables and has no clause for a table that must be unreachable, and the file is outside this fence. **`scoped-db.test.ts:386` will collide with session 281**, which reserves migration `0051` conditionally: if `P7-c`'s reading also needs a table, both branches move `createdTables` and a keep-both merge produces `113` beside `114`. **`DELTA_MANIFEST` section 27 is a heading number 281 may also take**, in a different part of the file. **And [INFRA section 5](../architecture/INFRA.md)'s role table is already stale in a way this entry did not repair**: it names `merit_migrate`, `merit_readonly` and `merit_admin` while `0026` created `merit_migrator` and `merit_analytics`, so a reader reconciling the fifth role against that table finds three of five names wrong before they start. That file is `P6-b`'s.
- **Files touched:** [`0050_live_cache_and_role.sql`](../../packages/db/migrations/0050_live_cache_and_role.sql), [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md), [`live_account_state.md`](../architecture/data-model/live_account_state.md), [data-model/README](../architecture/data-model/README.md) (its index row), [`scoped-db.test.ts`](../../packages/db/test/scoped-db.test.ts) (**one count and its comment, outside the fence, reported**), [ADR-164](../decisions/ADR-164.md), [ALLOCATION](../decisions/ALLOCATION.md), [STATE](../STATE.md), [sessions/README](README.md), the generated spans, this file. **[`schema.ts`](../../packages/db/src/schema.ts) and [`scope.ts`](../../packages/db/src/scope.ts) were in the fence and are UNTOUCHED**, deliberately, and section 6 of the entry is why.

---

### 1. The plan, written before the work (ADR-003 strict, no interactive plan mode)

**This session has no human watching, so the plan is committed rather than approved.** The dispatch
delegates the founder's answer and requires the ruling to be argued; what follows is the argument as it
stood after the sources were read and before any file was written.

**THE READING THAT WINS IS 1: A POSTGRES TABLE PLUS A FIFTH ROLE.** The table is `live_account_state`,
one row per account, and the role is `merit_live`.

**The four load-bearing arguments, each from a source rather than from preference.**

1. **`GS-132` is `P6`'s own done-gate and only reading 1 keeps it executable.** [DELIVERY_PLAN section 4](../DELIVERY_PLAN.md)
   states the done-condition as *"`GS-132` byte-identical with the cache poisoned"*, and `GS-132` itself
   reads *"the live cache is **populated** with values that would flip eligibility, a breach, and a payout
   clamp"*. A process-local store inside the socket server cannot be populated by a test outside that
   process, which is the cost [P6 section 10 item 1](../plans/P6-live-tier.md) names, and it is decisive
   rather than merely expensive: a medium that makes the phase's stated done-condition unwritable has
   failed the phase.
2. **`INV-M2-14`'s own words are *"a permission rather than a convention"*, and only a Postgres grant is
   one on this tree.** The apparatus already exists and is landed: [`0026`](../../packages/db/migrations/0026_roles_and_grants.sql)'s
   grant-then-revoke idiom, [`0034`](../../packages/db/migrations/0034_reversible_contact_addresses.sql)'s
   enumerated-role idiom, and [`assert_append_only_grants.mjs`](../../scripts/db/assert_append_only_grants.mjs).
3. **[`0034`](../../packages/db/migrations/0034_reversible_contact_addresses.sql) is the precedent and its
   sentence is exactly on point:** *"you cannot withhold DELETE from the send path until the send path is a
   principal the database can name."* The live path needs to be a principal the database can name, and that
   is a fifth role, minted the way the fourth was.
4. **Coherence with a fence the plan already wrote.** `P6-f`'s ingest is fenced to `apps/worker/src/live/ingest.ts`,
   a process that is not `apps/api`. A process-local store in the socket server makes that fence unbuildable.

**What reading 2 costs, named rather than waved at.** `GS-132`'s poisoning step becomes untestable from
outside the process. [INFRA section 2.1](../architecture/INFRA.md) deploys `api` and `api-admin` as **two
services**, so a process-local cache is two caches that diverge, and the trader's dashboard and the
operator's live Open Liability would read different numbers from the same feed. Each additional instance
of either service is a third and a fourth. And `P6-f`'s ingest would have to leave `apps/worker`.

**What reading 3 costs, and the honest part is that [OVERVIEW section 3](../architecture/OVERVIEW.md)'s
sentence reaches it only halfway.** The sentence is *"pg-boss (Postgres-only) rather than BullMQ plus
Redis: one fewer stateful service to back up, restore, and reason about, and the job store participates in
the same transactions and the same PITR as the money data."* **Neither limb transfers cleanly.** A cache
whose entire content is discardable by design needs no backup and no restore, so the first limb reaches
only the words *"reason about"*; and `INV-M2-14` makes the streaming ingest write **nothing but the
cache**, so there is no second write for a transaction to be atomic with and the PITR limb does not reach
at all. **The queue's argument therefore does not decide this, and this session does not pretend it does.**
What decides it is that **Redis would make the separation a convention again**: the engine's inability to
read the cache would rest on the worker not importing a client, which is an absent import rather than a
permission, and `INV-M2-14`, `C-26`, `FM-M12-08` and `FM-M13-07` all say permission. A new stateful service
is additionally a `VG-12` admission and an [INFRA](../architecture/INFRA.md) and
[OVERVIEW](../architecture/OVERVIEW.md) row, all three of which are `P6-b`'s and none of which is in this
fence.

### 2. The finding that makes reading 1 cost something, stated before it is paid

**[INFRA section 5](../architecture/INFRA.md) says `merit_app` is held by *"API and worker at runtime"*,
and [`client.ts`](../../packages/db/src/client.ts) opens exactly one pool from one `DATABASE_URL`, read
*"here and nowhere else"*.** So **one process is one role**, and while the API and the worker share
`merit_app` **no Postgres grant can separate "the API may read the cache" from "the stats worker may
not"** -- which is precisely what `FM-M12-08` and `DEP-M12-08` require.

**This session does not resolve that, because the address is `P6-b`'s and the mechanism is `P6-g`'s.** It
does two things instead: it states the requirement (**any process that reads or writes the live cache
connects as `merit_live`**), and it **forecloses the one cheap escape**, which is granting `merit_app`
`SELECT` on the cache. That grant is the move that makes `FM-M12-08` false silently, on the day somebody
finds the read path blocked and reaches for the smallest diff.

### 3. What the table stores, and the three columns it deliberately refuses

Columns are `account_id` (grain and scope hop), `trading_day`, `sequence`, `opening_equity_cents`,
`equity_cents`, `intraday_movement_cents` **generated** as the difference of the two beside it
([`0049`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql)'s `rcr_bp` precedent), `feed`,
`as_of_instant`, `indicative` and `created_at`. Every money column is integer cents in `bigint`.

**Three refusals, each with its reason, because a merged migration may never be edited (`E2`).**

- **`kind` is not stored.** It is `SimWaypoint['kind']`, simulator vocabulary, and `V-M2-16` is
  unanswered. [`stream.ts`](../../packages/rithmic/src/simulator/stream.ts) states that the tick shape is
  one of the two things the mechanism decides. A merged `CHECK` over an unconfirmed vendor vocabulary is a
  constraint that cannot be corrected.
- **`at_utc` is not stored.** Nothing in the landed contract reads it, and storing the feed's own clock
  beside ours invites staleness to be computed from the wrong one, which is what
  [ADR-152](../decisions/ADR-152.md) clause 1 and `LiveFreshness.stale` (*"the SERVER's own answer against
  its own threshold"*) forbid.
- **`projected_floor_distance_cents` is not stored.** The floor is a rule threshold the engine owns.
  Storing it here would put a rule input in the indicative tier, and computing it here would need a grant
  on `rule_states` that `INV-M2-14` forbids by name. The contract's field is composed at the read layer.

**No monotonicity trigger, and the refusal is deliberate.** An out-of-order tick that rewound the row would
be a display defect on a labeled indicative number, not a money defect, and the guard belongs in `P6-f`'s
`ON CONFLICT ... DO UPDATE ... WHERE excluded.sequence > ...` predicate. It is named as OWED rather than
added to a money-path migration this slice was not asked to widen.

### 4. What the ruling settles for other slices

[P6 section 3.5](../plans/P6-live-tier.md) left open whether the accessor needs an expiry sweep. **It does
not**: the table is one row per account, bounded, and `merit_live` is granted no `DELETE`. So `P6-c` does
not depend on `P5-a`'s range term and that row of section 3.5 resolves to NO.

### 5. The order of work

The migration, then `DELTA_MANIFEST`'s delta row, then the design record and its `INDEX` row, then
`schema.ts` and `scope.ts` together (they do not compile apart), then `ADR-164`, then `ALLOCATION`, then
`STATE`, then this log. Commit and push after each. Verification runs in pieces, `falsify.mjs` last and
alone.
