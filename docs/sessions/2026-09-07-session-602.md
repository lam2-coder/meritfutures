---
status: approved
depends_on: [README.md]
last_updated: 2026-09-07
---

## 2026-09-07 - Session 602: all four blockers on `readLiability` are spent, a fifth stands that the dispatch does not name, and the composition file stated the four as live under a green invariant for four sessions

<!--index: **THE ROW IS [ADR-407](../decisions/ADR-407.md), `proposed` and UNSIGNED. MONEY PATH, THE ADMIN LIABILITY READ, `E2` READ OWED. NOTHING MINTED, NO MIGRATION NUMBER RESERVED OR TAKEN.** **THE DISPATCH SUSPECTED TWO OF FOUR MAY HAVE CLEARED. ALL FOUR HAVE, NONE OF THEM BY THIS ROW, AND `AdminReadSource.readLiability` IS STILL NOT COMPOSABLE.** Each derived at its primary source rather than inherited: `trading_calendar` is a registered key in `packages/db`'s scope registry (session 377), [ADR-201](../decisions/ADR-201.md) ruling 2 states the velocity window as the trailing thirty-day settled total scaled to seven days, [ADR-202](../decisions/ADR-202.md) ruling 3 and [ADR-203](../decisions/ADR-203.md) gave `per_plan[].cusum` its absent form across the three copies `RI-18` binds, and `0064_reconciliation_runs.sql` plus session 387's producer plus `readRecon` record and read a reconciliation run. **A FIFTH BLOCKER STANDS AND THE DISPATCH DOES NOT NAME IT**: `eligible_next_7d`'s per-account half, whose two terms are `EligibleFoldIo.resolvePinnedPlan`, injected and refusing by name, and a wire that can say the figure is a FORECAST, which `EligibleNext7d` cannot. **THE COMPOSITION IS REFUSED ON A RULING RATHER THAN BLOCKED BY A TYPE**: the response can carry `eligible_next_7d: null` and `assertLiabilityGapsPaired` does not constrain which cause a field may take, but [ADR-203](../decisions/ADR-203.md) ruling 8 and [ADR-204](../decisions/ADR-204.md) ruling 9 fix this field's two causes in three copies and neither is true of an unwired port, so both routes are the widening the dispatch forbids. **THE DEFECT FOUND IS IN THE FILE THE DISPATCH POINTED AT**: `apps/api/src/admin-source/index.ts` stated all four spent blockers in the present tense, did not name the one that stands, and carried two stale hand-typed counts, **and `RI-14` cannot see any of it because none of the four subjects is an export**. Six stale numerals deleted across two files on [ADR-034](../decisions/ADR-034.md)'s remedy, and a seven-case control added that asserts each of the four spent AT ITS PRIMARY SOURCE and was watched RED on a seeded violation. -->

### 1. The objective

Derive each of the four blockers `apps/api/src/admin-source/index.ts` records against `AdminReadSource.readLiability` at its primary source, and compose the method if they have cleared. One objective, money path, the read the admin liability home is funded against.

### 2. Done

- **The container measured and reported.** **No repository on arrival**, the seventh consecutive row to record it. `git status` answered `fatal: not a git repository`; no `.git` existed anywhere under `/home/user` to depth four; `gh` is not installed. One shallow clone taken out of this row's budget, `.git` at 19M, `git rev-list --count HEAD` of **1**, tip `13f5a3f0`. `claude/wave-404s` fetched separately at depth 50 and `43ae94a0` confirmed as its tip.
- **The base reproduced on `43ae94a0`** before the first edit, on an empty `git status --porcelain`: `gates.mjs generate` reported **every span already matches its query**, gates **33 of 33**, invariants **35 of 35**, suite **336 files / 8,109 passed / 10 skipped**, typecheck, lint and format all exit `0`. `pnpm run verify` and `falsify.mjs` were NOT run; every leg was invoked directly.
- **All four blockers derived at their primary sources and all four are SPENT**, which refutes the dispatch's suspicion in the generous direction: it flagged blockers 3 and 4 as the pair that may have lifted, and 1 and 2 had lifted too.
- **The fifth blocker read out of `liability.ts`'s own enumeration and confirmed standing**, with both terms checked: `UNWIRED_ELIGIBLE_FOLD_IO` still throws `EligibleFoldUnwired('resolvePinnedPlan')` and nothing under any `src/` in this deployable supplies the port; `EligibleNext7d` still declares no measured term.
- **The composition REFUSED and the reason written down.** `IMPLEMENTED_ADMIN_READS` is unchanged, the wiring triple does not move, and `composeAdminReadSource` already answers `AdminSourceNotComposed('readLiability')`, which is the named synchronous refusal the dispatch's middle branch requires. Nothing was built for it because it was already there.
- **`test/wiring.test.ts` read and NOT edited.** Its `setAdminReadSource` entry already states the position correctly, names `EligibleFoldIo.resolvePinnedPlan` as the standing term, and cites [ADR-269](../decisions/ADR-269.md) and [ADR-283](../decisions/ADR-283.md). **A file in the fence that needs no repair is a measurement and not an omission.**
- **`apps/api/src/admin-source/index.ts` repaired.** The four retired clauses are PARAPHRASED rather than quoted, on `RI-14`'s rule that a verbatim sentence reads as live to every grep; `B5` is named with both terms; both hand-typed counts are deleted and the deriving suite named.
- **Four more stale numerals deleted in `apps/api/src/admin-source/liability.ts`**, on [ADR-034](../decisions/ADR-034.md)'s remedy: two statements of how many tables the module reads against an array that has grown twice, one of how many leaves the book subtracts, one of how many it produces.
- **A control added** to `test/admin-source-liability.test.ts`, holding the property from both ends, and **watched RED on a seeded violation** before it was believed. The seed was reverted and the file restored byte for byte.
- **[ADR-407](../decisions/ADR-407.md) written**, `proposed`, approval line unsigned, and [ALLOCATION](../decisions/ALLOCATION.md) row `407` claimed with `408` and `409` RESERVED for the two rows running beside this one.

### 3. Landmines

- **A BLOCKER LIST GOES STALE IN THE FILE THAT EXISTS TO EXPLAIN IT, BECAUSE NO SESSION OWNS THAT FILE.** Each of the four was lifted correctly, by a session that spent its clearing condition in the file that HELD the blocker, and each of those sessions was right that the composition file was not its fence. **The convention worked and the outcome was still wrong.** A reader of `index.ts` on this base would have gone to register a key that is registered.
- **`RI-14` IS WRITTEN FOR THIS DEFECT AND PASSED OVER FOUR INSTANCES OF IT.** It asks whether a reason claims a named thing does not exist while the tree EXPORTS it. A `TableKey` is a key of a registry object, a ruling is a document, a migration is a file, and a wire form is a union arm. **None of the four subjects is an export**, so the invariant was green throughout. Widening it is `packages/tooling` and is outside this fence.
- **THE COMPOSITION WOULD HAVE PASSED EVERY CHECK IN THIS REPOSITORY.** `assertLiabilityGapsPaired` enforces one entry per absent figure, a non-blank detail and the `awaiting` pairing, and does NOT constrain which cause a field may take. The field's causes are ruled in three copies instead. **A gate that would not have caught it is the reason to be careful and never the licence**, and the next row to reach this file can take what this one refused.
- **A FALLING BLOCKED-LEAF COUNT DOES NOT MEAN FIGURES ARRIVED.** [ADR-208](../decisions/ADR-208.md) moved the DECLARED count without moving production, because `RI-18`'s reader does not walk into a union arm. Only the three numbers together are honest, which is why `admin-source-liability-book.test.ts` asserts all three and why this row deleted the copies rather than updating them.

### 4. Blockers

- **`B5` TERM 1 is a slice**: the composition of `decodePlanRules` with the account's `plan_version_sizes` read, on a transaction the fold holds. The decoder move it used to wait on landed in [ADR-283](../decisions/ADR-283.md).
- **`B5` TERM 2 is a ruling**: `EligibleNext7d` needs a term a basis can ride on, in three copies, atomically, under `RI-18`.
- **THE GAP CAUSE FOR AN UNWIRED PRODUCER IS UNRULED**, and it is the one thing between this method and an honest wire. `LiabilityGapCause` is closed at three kinds of absence and none of them is "a producer this deployment did not install".
- **`DEP-M6-05` is unchanged.** Blocker 3's SHAPE lifted; its calibration did not, and no statistic is manufactured anywhere in this diff.

### 5. Files touched

- [`apps/api/src/admin-source/index.ts`](../../apps/api/src/admin-source/index.ts)
- [`apps/api/src/admin-source/liability.ts`](../../apps/api/src/admin-source/liability.ts)
- [`apps/api/test/admin-source-liability.test.ts`](../../apps/api/test/admin-source-liability.test.ts)
- [`docs/decisions/ADR-407.md`](../decisions/ADR-407.md)
- [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md), [`docs/decisions/README.md`](../decisions/README.md), [`docs/sessions/README.md`](README.md), [`docs/STATE.md`](../STATE.md), [`docs/INDEX.md`](../INDEX.md)

### 6. Next

Dispatch `B5`'s two terms, which are one slice and one ruling, or rule the gap cause an unwired producer takes and let this method compose against it. Either unblocks the admin liability home, the worker's digests and `P5-l`; neither is this row's fence.
