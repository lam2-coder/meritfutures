---
status: approved
depends_on: [README.md]
last_updated: 2026-08-26
---

## 2026-08-26 - Session 228: the slop-score pass
<!--index: **[ADR-116](../decisions/ADR-116.md) lands, `status: proposed`, approval line UNSIGNED, NOT money path.** Playwright entering the catalog is a **`VG-12` admission** which [STRATEGY:192](../testing/STRATEGY.md) says in its own words *"a session cannot grant itself"*; **the founder DELEGATED it for this slice and section 1 of the entry records the delegation rather than proceeding as though the gate did not apply**, which is the one sentence separating this from the failure mode `VG-12` exists to catch. **THE VERSION IS CHOSEN ON A MEASUREMENT AND `latest` IS REFUSED**: `playwright-core`'s `browsers.json` pins a chromium REVISION rather than a range, `1.56.1` is the newest version pinning `1194`, and `1194` is the build every Merit container carries at `/opt/pw-browsers`, so the gate RUNS here; `1.62.1` would have pinned a revision no container holds and produced a check that cannot run, which `repo-invariants.mjs` calls not a check that passed. **THE CLOSURE ARITHMETIC IS RUN IN BOTH FORMS BECAUSE SESSION 183 PROVED THEY DIVERGE**: `279 -> 283`, **`+4`**, of which ONE is a per-platform binary (`fsevents`, `os: [darwin]`, optional) and **THREE are the real admission**, against Next.js's `+51` with 36 per-platform and fifteen real. `pnpm audit --audit-level=low` reports no known vulnerabilities, all four entries carry a `sha512` integrity hash, no build script is ignored, and **the BROWSER is not in the lockfile and is not in that count**, which is where a supply-chain reading of this admission would otherwise stop one step early. **`CI-08` MOVES TO IMPLEMENTED IN THE SAME COMMIT AS ITS ARTIFACT**, P4 rule 10, naming [`ci.yml`](../../.github/workflows/ci.yml) job `e2e`. **THIS SESSION IS THE FIRST TO MAKE THE REPAIRED NEEDLE FIRE FOR REAL AND IT FIRED FOR THE RIGHT REASON**: the finding named `'@playwright/test@1.56.1':` at two spaces and NOT `next@16.3.2`'s optional peer declaration at six, which is still in the file, so [ADR-095](../decisions/ADR-095.md) section 8's repair is proved by the arrival it was repaired for. **AND THEN IT RETIRES, BY THE REGISTER'S OWN RULE**: with no condition naming that artifact, `playwrightInLockfile` is an entry naming nothing, which the stale-direction loop calls a finding; **`falsify.mjs`'s PAIR DOES NOT SURVIVE AND THAT WAS MEASURED RATHER THAN ASSUMED**, both reported `CONTROL DID NOT FIRE` before the edit, one because its seed asserts the absence it was written against and one because no row can produce `HAS ARRIVED` any more. **THE COST OF THAT RETIREMENT IS NAMED**: the only EXECUTABLE statement in this repository that a peer declaration is not an install becomes prose, and [ADR-042](../decisions/ADR-042.md) ruled prose is not a control. **THE PASS RENDERS FIXTURES AND NOT PAGES, MEASURED RATHER THAN CHOSEN**: `ls apps/*/src/app` returns `No such file or directory`, `CI-07` is still open on exactly that path and this gate probes it ABSENT, so there is no page to render; the honest half is that the COMPLIANT direction is self-referential today and **the SEEDED direction is what makes it a control**, and that half is about the checks rather than the fixtures. **FALSIFICATION IS IN BAND RATHER THAN IN `falsify-ci.mjs`**: each surface ships a compliant fixture AND one built to break every check, and the stage fails unless the first is silent and the second fires ALL EIGHT, so both directions run in one job on one commit. **PROVED BY BLINDING**: each of the eight check functions replaced by `return [];` in turn, eight runs, each reporting `8 failed, 8 passed` and naming exactly the blinded check; and in the other direction a 4px `#3b82f6` left border added to the compliant fixture turned four tests red naming `SS-02` AND `SS-04` at one selector. **TWO OF THREE PROJECTS**, `apps/admin` named rather than rounded up: it is in no P4 slice and ADR-095 declined even its framework. **THE DISPATCH FENCE NAMED NEITHER `scripts/corpus` FILE AND THE STOP CONDITION IS UNREACHABLE WITHOUT BOTH**; [P4](../plans/P4-portal-and-site.md) section 8's `P4-i` row names both and section 9 makes shrinking the register this slice's obligation, so the edits are taken at the PLAN's scope and reported. **`CI-07` and `VG-2` ARE UNTOUCHED AND THAT WAS CHECKED**: the `e2e` job builds nothing, so `CI-07` acquires no implemented leg and `VG-2` keeps its chain, with `CI-06/vg-inventory` unmoved at 12 rows. Gates **30 of 30**, invariants **9 of 9**, `verify` exit 0, suite **134 files / 2,244 passed / 1 skipped** unmoved, Playwright **16 passed** -->

- **Objective:** The slop-score pass, `SS-01` to `SS-08`, with `CI-08`'s cell moving in the same commit as the artifact that activates it. [P4](../plans/P4-portal-and-site.md) section 8's `P4-i`. **NOT money path.** **One ADR: [ADR-116](../decisions/ADR-116.md), reserved before dispatch and amended in place per [ADR-065](../decisions/ADR-065.md) T3.**

- **Done:** [ADR-116](../decisions/ADR-116.md) at `status: proposed`, approval line **UNSIGNED** and scoped to the two files it rules rather than to a package-wide count. `@playwright/test@1.56.1` admitted to the catalog and to three manifests. The pass: [`e2e/tokens.ts`](../../e2e/tokens.ts), [`e2e/extract.ts`](../../e2e/extract.ts), [`e2e/slop-score.ts`](../../e2e/slop-score.ts), [`e2e/pass.ts`](../../e2e/pass.ts), [`playwright.config.ts`](../../playwright.config.ts), and four fixtures across the two apps. `CI-08`'s `Closure` cell moved to **Implemented**, naming job `e2e` in [`ci.yml`](../../.github/workflows/ci.yml). The retired probe and the two retired seeded cases, each with its note. **Sixteen Playwright tests, eight compliant and eight seeded, each of the eight checks watched failing when blinded.**

- **Next:** **The token set is transcribed twice and no gate reconciles the two.** [`e2e/tokens.ts`](../../e2e/tokens.ts) holds the twenty color values because `SS-04` compares a COMPUTED color against the set and a computed color is `rgb(7, 12, 10)` rather than `ink-950`; [DESIGN_SYSTEM section 2](../design/DESIGN_SYSTEM.md) holds the same values as a table. If the palette moves and the file does not, **the pass enforces a palette nobody chose and the failure is silent in the direction that reads as working.** A `CI-06` gate reconciling the two is one registry over from `CI-06i`'s shape and is the cheapest repair on this list. Second: **`CI-07` is implementable and unimplemented**, it needs a page under `apps/*/src/app/`, and the day one lands the pass takes a URL and a `webServer` and **no check changes**.

- **Blockers:** none for this slice. One thing reported and not taken: **`P4-i`'s dispatch fence omitted [`scripts/corpus/gates.mjs`](../../scripts/corpus/gates.mjs) and [`scripts/corpus/falsify.mjs`](../../scripts/corpus/falsify.mjs), which [P4](../plans/P4-portal-and-site.md) section 8's own `P4-i` row names.** Session 230 holds `scripts/corpus` tonight and session 231 will touch `UNPROBEABLE_ARTIFACTS`; the three edits sit in different regions and P4 section 9 already orders this file rather than parallelising it, so whichever lands second merges the other first.

- **Landmines:**
  - **THE COMPLIANT FIXTURES ARE DOCUMENTS THIS SESSION WROTE, SO THE COMPLIANT DIRECTION ALONE PROVES NOTHING ABOUT `apps/site`.** It proves the checks accept what this session believes compliance looks like. **What makes `CI-08` a control today is the SEEDED direction**, which is about the CHECKS rather than about the fixtures and which is the half that keeps working unchanged when a real page arrives. A future reader who sees a green `CI-08` and concludes the site is compliant has read the wrong half.
  - **DO NOT REPAIR THE SEEDED FIXTURES.** `plans.seeded.html` and `dashboard.seeded.html` are deliberately every tell Appendix F names. A well-meant fix to any one of them disables the half of the pass that proves the other half is alive, and the stage will go red naming the check that stopped firing rather than naming the fixture that changed. Both files say so at the top.
  - **A PEER DECLARATION IS NOT AN INSTALL, AND FROM THIS COMMIT THAT IS PROSE.** `next@16.3.2` declares `@playwright/test` as an optional peer and pnpm writes the block into the lockfile verbatim at six spaces; a lockfile v9 ENTRY KEY is `name@version:` at exactly TWO. The needle that encoded this and the two seeded cases that proved it both retired with the condition they watched. The distinction survives at the retirement note in `gates.mjs` and in the catalog comment, and **nothing re-derives it**: a future probe that gets it wrong again will not be caught by anything here.
  - **`SS-05`'s LIMIT OF THREE IS A JUDGMENT AND EVERY OTHER NUMBER IN THE PASS IS A TRANSCRIPTION.** DESIGN_SYSTEM deliberately leaves it as "a configured number". Three, because section 4.2 has exactly one shadow and it is for overlays, so a compliant viewport carries a shadowed rounded element only while an overlay is open: zero would fail the surface the system permits and three refuses the fourth. **It is the only parameter in the pass a founder may want to move.**
  - **`extract.ts` READS `fill` AND `stroke` ON SVG SHAPES AND NOWHERE ELSE, AND WIDENING THAT WOULD LOOK LIKE COVERAGE.** On an HTML element Chromium computes `fill` as `rgb(0, 0, 0)` whether or not anything is filled, so reading it everywhere reports pure black on every element in the tree. The same is true of border, outline and text-decoration colors on elements that paint none: each defaults to the used `color`. **A gate whose findings are mostly noise is a gate somebody turns off**, and each narrowing is stated in that file's header with the value Chromium actually returns.
  - **`PICTOGRAPH_ONLY` IS AN ALTERNATION RATHER THAN A CHARACTER CLASS AND THE REASON IS A LINT RULE THAT WAS RIGHT.** `no-misleading-character-class` refuses a class holding a pictograph and `U+FE0F` together, because inside a class they are two members and on screen they are one grapheme. `(?:a|b)+` accepts exactly what `[ab]+` accepts, so the property is unchanged. **A disable comment would have silenced a rule that was correct about the shape**, which is the move [STRATEGY section 4.5](../testing/STRATEGY.md) bans one rule over.
  - **THE `e2e` JOB RUNS ON EVERY PUSH, WHICH IS MORE THAN `CI-08`'s `Runs on` CELL ASKS FOR, AND IT IS NOT A DRIFT.** Section 4.1 gives the stage "every push to `main`, and on demand" while section 4.5 makes the same pass a MERGE blocker; **a job gated to `main` runs after the merge it was supposed to block.** Every push to `main` is a subset of every push, so the narrower cell is satisfied rather than contradicted, and `workflow_dispatch` was added to the workflow to make the cell's second clause true at all. **The cell was not edited.**
  - **THE PASS IS AT THE ROOT AND THAT IS A RULING, NOT A CONVENIENCE.** `RI-04`'s prose refuses a deployable importing a deployable, and two copies of a twenty-value palette is the drift every register here exists to prevent, so `e2e/` sits beside `vitest.config.ts` and `eslint.config.js`. **The consequence is `scripts/demo`'s verbatim**: the root `typecheck` script names `tsc -p e2e` and the `format` globs gained the directory, because a TypeScript directory no `tsc` invocation reaches is a directory that rots. `e2e/tsconfig.json` is the ONLY project in this tree with the `DOM` lib, deliberately, so no Node path can reference `window` and typecheck.

- **Files touched:** [`pnpm-workspace.yaml`](../../pnpm-workspace.yaml), `pnpm-lock.yaml`, [`package.json`](../../package.json), [`apps/site/package.json`](../../apps/site/package.json), [`apps/portal/package.json`](../../apps/portal/package.json), [`playwright.config.ts`](../../playwright.config.ts) (new), [`e2e/tokens.ts`](../../e2e/tokens.ts) (new), [`e2e/extract.ts`](../../e2e/extract.ts) (new), [`e2e/slop-score.ts`](../../e2e/slop-score.ts) (new), [`e2e/pass.ts`](../../e2e/pass.ts) (new), [`e2e/tsconfig.json`](../../e2e/tsconfig.json) (new), `apps/site/e2e/` (new, one spec and two fixtures), `apps/portal/e2e/` (new, one spec and two fixtures), [`.github/workflows/ci.yml`](../../.github/workflows/ci.yml), [`.gitignore`](../../.gitignore), [`docs/testing/STRATEGY.md`](../testing/STRATEGY.md) (`CI-08`'s row only), [`scripts/corpus/gates.mjs`](../../scripts/corpus/gates.mjs) (the inventory register only), [`scripts/corpus/falsify.mjs`](../../scripts/corpus/falsify.mjs) (two cases retired), [`docs/decisions/ADR-116.md`](../decisions/ADR-116.md) (new), [ALLOCATION](../decisions/ALLOCATION.md), [decisions/README](../decisions/README.md), [INDEX](../INDEX.md) (generated spans only), [STATE](../STATE.md), this log.

### What was NOT done, named rather than implied

**No migration, no migration number, no scope-registry row, no route, no `CI-06` letter, no `VG` row, no `RI-nn`, no `SD-nn`, no fixture-registry row and no edge-case entry.** `packages/db` and `apps/api` were not opened at all, which the dispatch excluded by name.

**NO `INDEX.md` ROW WAS ADDED AND THE FENCE ANTICIPATED ONE.** [INDEX:45](../INDEX.md) states the rule this follows: ADR entry files "are fragments rather than corpus documents and are indexed by that README under `CI-06n`, not by this row", and `CI-06n` is what `ADR-116`'s row in [decisions/README](../decisions/README.md) satisfies. The pass itself takes no row either, on the same reading that leaves `vitest.config.ts`, `eslint.config.js`, `ci.yml` and `repo-invariants.mjs` unrowed today: `grep` finds none of the four in that file. **`CI-06c` reads INDEX completeness in both directions and passes**, so this is the existing convention rather than an omission, and the INDEX diff on this branch is generated spans only.

**NO `falsify-ci.mjs` CASE FOR `CI-08`, AND IT IS A RULING RATHER THAN AN OMISSION.** [ADR-116](../decisions/ADR-116.md) ruling 4: the seeded direction is IN BAND, so [P1](../plans/P1-monorepo-scaffold.md) section 6's proof runs inside the stage on every commit rather than in a harness somebody has to remember. That is strictly earlier than a `falsify-ci.mjs` case, and it also keeps this session out of `scripts/ci/`.

**`apps/admin` HAS NO FIXTURE AND `CI-08` SAYS SO.** It is the third UI surface, it is in no P4 slice, and `ADR-095` declined to rule even its framework for that reason. `CI-06/gate-inventory` never asks whether a `Contents` cell is TRUE, which is `ADR-073`'s own second limit and which `CI-02` already exercises against a tree with no `PT-nn` suites, so the row is implemented at two of three and states it.

**`CI-07` AND `VG-2` WERE NOT TOUCHED AND THAT WAS CHECKED RATHER THAN INTENDED.** `CI-07` is implementable and unimplemented and `VG-2` chains to it. The `e2e` job runs `playwright test` and builds nothing, so `CI-07` acquires no implemented leg. `CI-06/vg-inventory` reports 12 rows unchanged: 3 wired, 2 chained, 4 waiting legs over 3 artifacts, 4 discharged, 1 finding.

### The dispatch's five instructions, and where each is answered

| Asked | Answered |
|---|---|
| **Record the DELEGATION rather than proceed as though the gate did not apply** | [ADR-116](../decisions/ADR-116.md) section 1, which quotes `STRATEGY:192`, names what was admitted, the exact version and why it was needed, and states that the grant was delegated rather than assumed |
| **Add it exactly, and measure the closure delta in BOTH forms** | Section 5. `279 -> 283`, `+4`, one per-platform and three real, against session 183's `+51` / 36 / fifteen. The audit, the integrity check and the ignored-build-script check are quoted from their commands |
| **`CI-08` moves in the same commit as its artifact** | One commit carries `pnpm-lock.yaml`, the `e2e` job and `CI-08`'s cell. P4 rule 10 |
| **Confirm the needle fires for the right reason, and confirm `falsify.mjs`'s pair still holds** | Section 7. It fired for the right reason, naming the entry key at two spaces and not the peer line at six. **The pair does NOT hold**, both controls reported `CONTROL DID NOT FIRE` before any edit, and both retire with the probe |
| **Report a collision rather than resolving it quietly** | Section 9, and the Blockers line above. The dispatch fence omitted two files P4's own `P4-i` row names, and session 230 holds one of them tonight |

### What the eight checks actually read, so a reviewer does not have to infer it

| Check | Read from | Fires on |
|---|---|---|
| `SS-01` | computed `background-image` | a gradient with one stop in hue `[195, 258)` and another in `[258, 320)`, which straddles indigo `#4f46e5` at 243 and violet `#a855f7` at 271 |
| `SS-02` | `border-left-{width,style,color}` | wider than 2px, painted, and outside the ELEVEN ink values. A brass left border is `SS-02` and is inside `SS-04`'s set, which is two different questions |
| `SS-03` | computed `font-family` | one of the five banned faces by name, or a stack opening `ui-sans-serif, system-ui` |
| `SS-04` | `color`, `background-color`, painted border colors, drawn outline and decoration colors, and `fill` / `stroke` on SVG shapes | any painted color not among the twenty tokens. **A token at ANY alpha is the token**, because the hairline on dark is `ink-300` at 30 percent; alpha `0` is skipped as unpainted |
| `SS-05` | `border-*-radius` and `box-shadow` | three or more elements per viewport carrying both a radius above 2px and a shadow |
| `SS-06` | rendered text nodes | a SENTENCE opening with a banned verb, which is the copy rule the section 8 row approximates and is strictly the stronger reading |
| `SS-07` | rendered text nodes | a pictograph in a declared icon slot (`data-icon`, `class~=icon`, `role=img`), or a text run that is nothing but a pictograph |
| `SS-08` | rendered text nodes | `U+2014` or `U+2015`. **The en dash is deliberately NOT banned**: it is the correct character for a numeric range |
