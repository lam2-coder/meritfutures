## 2026-08-16 - Session 44: `advanceDay`, DO-1 to DO-7, groups B, C and D (money path)

- **Done:** **The fold exists.** [`packages/rules-engine/src/day/advance.ts`](../../packages/rules-engine/src/day/advance.ts) evaluates one day in [M01 section 3.1](../plans/M01-rules-engine.md)'s order and no other, over [ADR-049](../decisions/ADR-049.md)'s `CalendarSlice` **value** built by [`calendar.ts`](../../packages/rules-engine/src/calendar.ts)'s pure constructor. **Sixteen of M01's fifty rules**: R-03, R-04, R-07, R-08, R-09, R-12, R-13, R-14, R-15, R-16, R-18, R-21, R-22, R-23, R-24, R-25, each with an `RE-U-nn` asserting its operator at the boundary on both sides. `IMPLEMENTED_RULES` is exported, which is [ADR-048](../decisions/ADR-048.md)'s engine half. **`Cents` becomes `bigint`** (M01 section 2.1, `INV-02`). **`pnpm run verify` green: 312 tests, 15 gates clean and dirty, 6 CI-02 seeded violations, 16 scope cases.**
- **Next:** Group E (`DO-8`, R-26 to R-31) or group F (`DO-9`'s gates, R-33 to R-41), each its own session under ADR-003. **The fixture-wiring session is a third and it is the one ADR-048 waits on**: the `L-nn` resolvable-citation rule, the loader's bigint comparison, then the per-fixture polarity derivation, in that order.
- **Blockers:** None for the engine. Groups A, F and H still wait on the calendar transcription, which is a founder item (P2 section 6).
- **Landmines:**
  - **M01 DISAGREES WITH ITSELF ON R-22's OPERATOR AND THE CODE FOLLOWED THE TABLE.** Section 3.6's pseudocode writes `-realized_pnl >= limit`; **R-22's operator column, `OQ-6`'s ruling and section 10.1 all write `>`**, "exactly at the limit survives". Section 3.5 settles it ("the operator column is the contract"), so the engine implements `>`. **This is a defect in a frozen document and moving it is an ADR, not a commit**, so nothing in M01 was edited. **No v1 plan configures a daily loss limit**, which is why it had to be right now: nothing exercises this line until a plan enables one, and by then it will not be re-read.
  - **A refusal is not a skip, and the difference is the whole design of the unfinished steps.** `DO-2` refuses when a settlement is effective today and `DO-8` refuses on an eval-phase day, each returning a typed `AssertionFailure` naming the missing group. **Folding either anyway would return a confident wrong number on a money path**: a skipped settlement leaves the balance 150,000c too high and pays a second time against it. The reader tempted to "just carry the state through" for convenience is the reader this note is for.
  - **`DO-8`'s refusal happens AFTER the day is computed, on purpose.** The refusal names the missing step rather than the phase, and `DO-4` to `DO-7` having already run is what makes the returned state worth inspecting when group E lands. It is still a refusal: the caller writes none of it.
  - **THE GOLDEN LOADER STILL CANNOT FOLD THIS ENGINE, AND THE REASON IS A COMPARISON.** `diffEndState` uses `Object.is`, the expectations are JSON numbers and the state fields are `bigint`, so `Object.is(4750000, 4750000n)` is **false** and every fixture would diff on every field. That conversion belongs to the fixture-wiring session, with the `L-nn` citation rule and the polarity derivation. **CI-03's polarity was NOT touched here**, per ADR-048's stated prerequisite and the session brief.
  - **`RuleState` is a deliberate subset of M01 section 2.2.** `withdrawableCents` (R-35), `engineEligible` and `engineGates` (R-33 to R-41), and `stateHash` (`SD-08`, and `hash.ts`, which replay needs and the day fold does not) are absent, each named in `types.ts` beside the rule that fills it. **A field the engine cannot fill is worse than a field it does not declare**: the first is a number a caller will read.
  - **`ResolvedPlan` uses discriminated unions where M01's sketch uses non-null assertions.** `floorLockAtProfitCents!` and `dailyLossLimitCents!` stand in for a rule `CV-11`, `CV-12` and `CV-16` state exactly, which is that the value is present precisely when the feature is enabled. The union says it in the type system, so the engine never asserts non-null on a money field. **`resolvePlan` itself is NOT written** and remains `P2-1`'s.
  - **`winDayFloorCents` is carried on BOTH phase records and `MaterializedPlan` carries it on one.** M01 section 3.6 reads it off whichever phase record the day selected; `plan_versions.rules.win_days` is a plan-level block and Appendix A lists one win-day floor per plan. Resolution copies the single published value onto both, and the alternative, reaching into `plan.funded` from an eval-phase day, would put a funded parameter on the eval path where no rule says it belongs.
  - **The `RE-U` cross-check in this package proves less than ADR-048's stage version will, and it says so.** `test/rule-coverage.ts` is the single source of the test titles and of the comparison against `IMPLEMENTED_RULES`, so a claimed rule with no test cannot land. **It cannot prove a test asserts what its title says**, which is what the four seeded mutants are for and why ADR-048 puts the real cross-check in the stage, against the PASSING set.
  - **Engine tests cannot read files, and that is the same boundary as the engine's.** `types: []` means `node:fs` does not exist in `test/` either, so the plan and the calendar are transcribed into `test/fixtures-in-code.ts` from Appendix A.1 and from `fixtures/calendars/cme-2026.json`. The window's `sequence` values start at 4021 rather than 0 **so a test cannot pass by confusing a window offset for a calendar index**, which is the confusion R-37's gap subtraction cannot survive.
  - **`JSON.stringify` throws on a `bigint`**, which broke the scaffold's mutation property the moment `Cents` moved. That is `FM-12` arriving on a test instead of on a payload, and it fails in the safe direction: money is now rendered as a base-10 string, which is Appendix B.2's canonical rule one file early.
- **Files touched:** `packages/rules-engine/src/{types,index,calendar,errors,rules}.ts` and `src/day/{advance,floor,breach,counters}.ts` (four new), `packages/rules-engine/test/` (seven new files, two scaffold tests updated), `packages/golden-loader/src/loader.ts` (the bigint boundary), `scripts/ci/falsify-ci.mjs` (four engine mutants), `docs/{STATE}.md`, `docs/sessions/`.

### The count, reported rather than implied

**Sixteen of fifty.** The thirty-four that are not implemented are not silent: `src/rules.ts` names the groups they belong to and why each is absent, `advance.ts`'s header maps every `DO-n` to implemented or refusing, and `implemented-rules.test.ts` asserts the number **16** so a session that adds a rule without noticing cannot land.

Four are worth naming individually, because they are group A rules that look cheap and are not: **R-01, R-02, R-05 and R-06 need the real calendar.** R-02's gap counting is `sequence` subtraction across a range, R-01 and R-05 are session containment over stored UTC instants, and there is not one calendar row in the repository. Writing them against a hand-built five-session window would be transcription from recollection, which is what `TR-01` forbids.

### What was NOT done, named rather than implied

**CI-03's polarity is untouched.** `engineIsIdentityStub()` still governs and every golden fixture is still asserted to fail, which is correct while the loader cannot fold `advanceDay`. ADR-048's derivation lands when the first fixture genuinely passes.

**No ADR was written for the R-22 disagreement.** It is a defect in a frozen document, the founder rules those, and a session that writes an ADR to resolve a contradiction it found in its own path is a session grading its own work.

**No `EC-nnn` was claimed** for the same reason a `CI-06` letter was not claimed in session 42: an edge case arrives with a number from the allocation table and a regenerated count, and both belong to the session that rules it rather than to the one that found it.
