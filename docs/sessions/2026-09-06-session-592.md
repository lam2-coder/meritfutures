---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 592: there is no fourth outcome, the missing thing is a precondition on outcome one's remedy, it is priced at zero over every pointer the corpus can decide, and both `schedule.ts` header clauses are repaired without moving a line

<!--index: **THE ROW IS [ADR-397](../decisions/ADR-397.md), `proposed` and UNSIGNED. MONEY-ADJACENT, ADR-003 STRICT, `E2` READ OWED.** [ADR-393](../decisions/ADR-393.md) section 9 item 4 asked for a FOURTH OUTCOME or an explicit statement that there is none. **RULED: THERE IS NONE.** Two gates partition the population into three cells and admit no fourth. **What that row found is real and is a missing PRECONDITION on outcome one's remedy**: *only the coordinate moves* presumes a coordinate to move to, and where the named subject has left the cited file there is none. It is amended into [ADR-388](../decisions/ADR-388.md) at outcome one's own line with that sentence kept whole. **AND THE PRICE IS ZERO**: over the **420** pointers this corpus can decide the precondition fails **8** times, all 8 where `G2` had already failed and **not once in outcome one**, so **nothing moves between classes**. **[ADR-388](../decisions/ADR-388.md) section 4's table reproduces figure for figure on a different base**, 169 / 76 / 42 / 2 over 290, and its 363, 393 and 130 reproduce exactly. **[ADR-393](../decisions/ADR-393.md)'s two instances re-derive at primary source and both sit OUTSIDE the 420**, because the binder returns no name for either. **RULED: a wrong figure in a dated record is annotated and never overwritten**, and [ADR-391](../decisions/ADR-391.md)'s figure is wrong under all six readings taken at three commits. **Both `schedule.ts` clauses repaired, both verdicts unchanged, the standing bar untouched for the seventh consecutive row.** **STRICTLY LINE-COUNT NEUTRAL AND DERIVED**: 33 pointers at seven lines below the block, all seven exact, five in files this row may not write. **SIX CITATIONS ARE MOVED BY THIS ROW'S OWN THREE REGISTRY ROWS AND ALL SIX ARE REPAIRED TO NAMES AND LISTED**, and those same six were repaired by [ADR-389](../decisions/ADR-389.md) and again by [ADR-393](../decisions/ADR-393.md), each time falsified by the repairing wave's own rows, which answers [ADR-394](../decisions/ADR-394.md)'s first approval question with a receipt. Suite **336 / 8,109 / 10 / 0** against the same base reproduced before the first edit, gates **33 of 33**, invariants **35 of 35**.-->

---

### 1. The objective

Decide whether [ADR-388](../decisions/ADR-388.md)'s classification needs a fourth outcome, price the answer, and repair the two `ADR-376` header clauses in [`schedule.ts`](../../apps/worker/src/schedule.ts) plus [ADR-391](../decisions/ADR-391.md)'s wrong figure. One objective, money-adjacent, ADR-003 strict.

---

### 2. What was measured before the first edit

**The container arrived with no repository at all.** `/home/user` was empty and `git rev-parse` failed in the directory the dispatch called the repository root. The tree was attached to this session's authorized set and cloned by hand, which is the condition [ADR-382](../decisions/ADR-382.md), [ADR-393](../decisions/ADR-393.md) and [ADR-394](../decisions/ADR-394.md) each recorded before this one.

**The clone was shallow and it was deepened before one history question was asked.** `git rev-parse --is-shallow-repository` returned **`true`** on arrival and **`false`** after `git fetch --depth=1000 origin claude/wave-392s`, with `.git/shallow` absent and **4,309** commits reachable including the root commit. Every `G2` verdict below was derived only after that was true.

**The base was reproduced on `ebc21238` before the first edit**, on a clean tree with `HEAD` confirmed rather than assumed: gates **33 of 33**, invariants **35 of 35**, suite **336 files / 8,109 passed / 10 skipped / 0 failed**, `typecheck`, `lint` and `format:check` clean. **`pnpm run verify` and `falsify.mjs` were never run**, both forbidden to this row and both mutating.

---

### 3. What landed

1. **The ruling.** No fourth outcome, stated explicitly because [ADR-393](../decisions/ADR-393.md) asked for that statement by name. The two gates are a partition and a partition of two gates has three cells.
2. **The amendment**, written into [ADR-388](../decisions/ADR-388.md) at outcome one's own line, one line replaced by one longer line, keeping the original sentence whole.
3. **The price**, derived through an export-added copy of the checkers' own reader that never entered the repository: 8 failures in 420, all 8 already forbidden, 0 in outcome one, and [ADR-388](../decisions/ADR-388.md)'s four bucket figures reproducing exactly on a different base.
4. **Both `schedule.ts` clauses repaired**, 16 comment lines replaced by 16, with each original kept whole and what is retired named rather than reproduced.
5. **The convention ruled and applied**: a wrong figure in a dated record is annotated at its site and never overwritten, and [ADR-391](../decisions/ADR-391.md)'s is annotated there.

---

### 4. Landmines

- **`apps/worker/src/schedule.ts` is now effectively frozen against any edit that adds a line.** 33 pointers aim below the `ADR-376` header at seven distinct lines, **all seven exact**, and **five of them sit inside `packages/tooling/`**, which no citation-repair right reaches while that directory is another row's. The only reason this repair was possible is that a comment block can be re-wrapped wider.
- **The re-wrap is visible and it touches a convention.** The two repaired paragraphs run to at most 122 columns against the file's habit of about 78. Nothing was compressed, no paragraph was dropped, and `format:check` passes because prettier does not reflow comments.
- **The price of the precondition is zero over a population that cannot see either known instance of it.** Both of [ADR-393](../decisions/ADR-393.md)'s sit in the 8,101 [ADR-388](../decisions/ADR-388.md) calls undecidable, because the binder returns no name for a pointer written inside a markdown link whose token already carries a path.
- **[ADR-388](../decisions/ADR-388.md)'s 289 reproduces and its stated reason does not.** Every one of the 290 has a recoverable write commit on full history; the one that is still undecidable is undecidable because its cited path resolves to no file in this tree.
- **Three consecutive waves have repaired the same six `ALLOCATION` pointers and each repair was falsified by the repairing wave's OWN registry rows.** [ADR-312](../decisions/ADR-312.md)'s four tokens carried `(was :472, and :393 before that)` on arrival. **A fourth coordinate is refused and all six are repaired to names.**
- **This entry dates itself 2026-09-06 and the container clock reads 2026-09-07.** The wave was followed rather than the clock.

---

### 5. Next

The founder's `E2` read of [ADR-397](../decisions/ADR-397.md) and the three questions its approval block names, of which the first is whether a refusal with a zero in it was the right deliverable and the second is whether `G1` means what a person can read or what `citationsIn` can bind. **Those two give 0 and at-least-2 for the same question and both are honest under their own definition.**

Counts derived at reporting time off each runner's own last line: suite **336 files / 8,109 passed / 10 skipped / 0 failed** against the same base reproduced before the first edit, gates **33 of 33**, invariants **35 of 35**, `typecheck`, `lint` and `format:check` clean.
