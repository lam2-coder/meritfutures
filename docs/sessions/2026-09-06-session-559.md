---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 559: the operator console's port declares four members, so the half install ADR-360 could not build is built here, and the row it brings to life is the one that triggers the nightly batch

<!--index: **THE ROW IS [ADR-364](../decisions/ADR-364.md), `proposed` and UNSIGNED. MONEY-ADJACENT (internal ops), `E2` READ OWED.** **RULED: `setInternalOpsSource` is DECLINED rather than BLOCKED, [ADR-360](../decisions/ADR-360.md)'s "no half install exists to get wrong" DOES NOT CARRY, and [ADR-356](../decisions/ADR-356.md)'s harm ruling DOES.** `InternalOpsSource` declares **FOUR** members (`routes/internal.ts:807`), sliced to its closing brace by a brace-balancing scanner that skips comments and string literals **DURING** the scan, corroborated on `AdminReadSource` at **SEVEN** (which reproduces ADR-360's independent answer) and on a fixture carrying an unbalanced `{` inside a doc comment. **ADR-360's impossibility was a fact about a ONE-member interface**, and what survives here is only the narrower claim that a `null` DEFAULT has no arms to present: **the default is not the install.** **THE HALF INSTALL IS WELL-TYPED, TAKES NO CAST, GOES IN THROUGH THE SINGLE SETTER, AND ANSWERS 500/500/200/202 WHERE UNWIRED ANSWERS 500 ON ALL FOUR.** Three of the four rows are reads and a read whose member throws is **byte-identical on the wire to no port at all**, so half-installing those is invisible rather than harmful; the exception is `POST /internal/batch/run`, **the row that COMMANDS**, which turns from a refusal into a **202 Accepted** naming a job id. **THE HARM IS ALREADY BOUNDED BY THIS FILE IN THIS FILE'S OWN WORDS**: `renderBatchRun` (`:759`) refuses `started` with a null job id as *"a trigger that reports success and does no work"*, so the HONEST stub is a 500 and the entire residue is a stub that FABRICATES an id, which nothing inside a deployable declaring no queue can catch. **THE PORT'S DOCBLOCK IS NOT WRONG AND IT IS NOT COMPLETE**: *"this file answers that the same way whichever method is MISSING"* is TRUE and measured; what neither it nor the single setter reaches is a member that returns a PLAUSIBLE value, so `src/` is not edited and the correction is executed beside the claim (`RI-14`). **THE CHANNEL IS THE OTHER FINDING AND IT IS [ADR-359](../decisions/ADR-359.md)'s ARRIVING ON AN OPERATOR SURFACE.** `internal.test.ts` already held the STATUSES; nothing held the DOCUMENT. The 500 carries API_CONTRACT section 2's **FIVE KEYS AND NOTHING ELSE**, `STATUS_CODE` (`server.ts:98`) not being closed over 500 at all, so the sentence `wired` throws reaches no caller; all four rows answer the same bytes once the request id is removed; and **the operator who typed the trigger WRONG is told THREE FIELD-LEVEL ERRORS while the one who typed it RIGHT is told five keys**. **THE MUTENESS IS RULED CORRECT ON THE CONTRACT RATHER THAN ON PRECEDENT**: `problem`'s own docblock (`server.ts:135`) states that `detail` is deliberately never set, quoting section 2, so a detail field is **not** the remedy and the options are open questions. **THE LOG IS THE ONLY CHANNEL THAT CARRIES THE SENTENCE AND NO SUITE HERE CAN OBSERVE IT**: `ServerOptions.logger` is `boolean` (`:154`) and `buildServer` (`:169`) hands it to Fastify, which is ADR-360's structural hole **re-measured rather than carried**. **THE SECOND-ORDER FINDING IS ABOUT A CONTROL AND NOT ABOUT THIS PORT.** `internal-ops-constructibility.test.ts` states that *"a fifth method added to `InternalOpsSource` arrives on this array without anybody remembering to add it"*; seeded with a REAL fifth member in PROPERTY form behind a doc comment it stayed **GREEN AT 14 OF 14**, its both-directions partition included, while this row's slice went red. **The claim is true of a METHOD and false of a PROPERTY**, the two derivations are now asserted equal on every run, and **that file was not edited**. **THE AUDIENCE CHANGES THE ANSWER TWICE**: a log-only channel is legitimate for an operator in a way it was not for the trader surfaces ADR-359 and ADR-360 ruled, and **the boot is the one refusal path silent to the only person who could act on it** -- the operator surface registers all four `/internal/*` rows, every one answers 500, and nothing in the composition report, the startup line or anywhere else names the port. **CONSTRUCTIBILITY IS ADR-242's AND IS RE-MEASURED RATHER THAN RE-EXECUTED**: one read out of four is constructible in SHAPE, zero in full, `reconciliations` is scope class `derived` (`scope.ts:1433`) so `FirmTx` refuses it at COMPILE TIME and [ADR-171](../decisions/ADR-171.md) clause 1 refuses the `SystemTx` door, and `runBatch` is blocked on a MANIFEST AUTHORITY and not a shape. **THREE ITEMS ARE OWED TO THE SIBLING HOLDING `wiring.test.ts`, WHICH WAS NOT OPENED FOR WRITING**, including that this port's entry sentence *"this port's OWN docblock refuses it harder"* is OVERSTATED; **nothing here moves `BLOCKED`, `DEFAULTS`, `LIVE_DEFAULT`, `UNPROVEN_RULING` or the triple**, and `wiring.test.ts` was re-derived green on this branch. **EIGHT FALSIFICATION SEEDS, EVERY ONE WATCHED RED AND EVERY ONE RESTORED BYTE-IDENTICAL under `sha256sum -c`**, covering all thirteen cases. **NO PORT WIRED, NO `src/` LINE MOVED, NO MIGRATION TAKEN OR RESERVED, NO GATE WEAKENED, no test skipped, disabled or quarantined, no invariant number minted, no `CI-06` letter spent, no `status` field moved, and no threshold, timeout, digest, page size or boot posture invented** -- four open questions recorded as OPTIONS. **THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL** and the tree was attached and cloned by hand, the condition sessions 550, 553 and 557 recorded. **NO `mcp__github__*` TOOLS, NO `gh` ON PATH, `api.github.com/repos/lam2-coder/meritfutures` ANSWERS 403 while `git push` AUTHENTICATES**; the branch is pushed and **no pull request was opened, by instruction; the parent session opens it.** Counts derived at reporting time: suite **329 files / 7,979 passed / 10 skipped / 0 failed** against a base reproduced on `ee7ce5a4` BEFORE the first edit of **328 / 7,966 / 10 / 0**, a delta of **+13 cases in ONE NEW FILE measured by removal**, skips unmoved; gates **33 of 33**, invariants **35 of 35**, typecheck, lint and `format:check` clean, and `git diff --stat origin/main -- apps/api/src packages/db/migrations scripts/db` **EMPTY**. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `364`: rule `setInternalOpsSource`, the third of the three `null` defaults in `wiring.test.ts`'s `BLOCKED` map, against the five questions the dispatch put and against [ADR-360](../decisions/ADR-360.md), which is the closest prior row in shape.

**One objective. It resolved into a ruling that agrees with ADR-360 on the register and contradicts it on the reasoning.**

---

### 2. What was re-derived before the first edit

**The baseline was measured on `ee7ce5a4` with a clean tree, before anything was touched: 328 files / 7,966 passed / 10 skipped / 0 failed.** It is not the `aa7ab356` figure sessions 555, 557 and 558 recorded; this base is the merge that landed all four of those rows. Gates **33 of 33** and invariants **35 of 35**, both green on the untouched tree.

**Every dispatched claim reproduces.**

| claim | verdict |
|---|---|
| `setInternalOpsSource` declared at `routes/internal.ts:825` | **REPRODUCES**, exact |
| its module-scope default is `null` | **REPRODUCES**, `:822` |
| it is one of the fourteen entries in `BLOCKED` | **REPRODUCES** |
| it is one of only three `null` defaults, the rest refusing objects | **REPRODUCES**, `DEFAULTS` reads three `null` and eleven others |
| ADR-360 ruled its sibling DECLINED rather than blocked | **REPRODUCES** |

**Nothing was refuted in the dispatch. What was refuted is a sentence in ADR-360 and a claim in a test file, and neither was on the list.**

---

### 3. THE RULING

[ADR-364](../decisions/ADR-364.md) section 2 holds it in full.

**RULED: the port stays uncalled and is DECLINED rather than BLOCKED.**

**RULED: ADR-360's "no half install exists to get wrong" DOES NOT CARRY.** It was a fact about a ONE-member interface. This port declares four, and a half install is well-typed and installable through the single setter.

**RULED: ADR-356's harm ruling DOES carry, and it lands on ONE row of the four.**

**RULED: the module's own guard already bounds that harm, and the residue is not detectable from inside this process.**

**RULED: the mute wire is correct and stays, on API_CONTRACT section 2 as `problem`'s own docblock states it.**

---

### 4. The measurement the row turns on

Installed: two members that throw, one real read, and a `runBatch` that returns `{ outcome: 'started', jobId: 'job-2026-08-26-1' }` and enqueues nothing.

```
GET  /internal/health/deep   500   (member throws)
GET  /internal/jobs          500   (member throws)
GET  /internal/recon/status  200   {"as_of":"...","open_mismatches":[]}
POST /internal/batch/run     202   {"trading_day":"2026-08-26","run_id":"...",
                                    "outcome":"started","job_id":"job-2026-08-26-1"}
```

Unwired, the same four requests are **500, 500, 500, 500**.

**The two 500s in the half-installed column are byte-identical to the unwired ones**, so the wire reports nothing about the deployment being half finished. **The 202 is the whole of the harm**, and it is a success answer on the row that triggers the nightly batch.

---

### 5. Where this row found something its own dispatch did not ask about

**`internal-ops-constructibility.test.ts` DOES NOT SEE A MEMBER WRITTEN IN PROPERTY FORM.** Its docblock states the property it exists for: *"a fifth method added to `InternalOpsSource` arrives on this array without anybody remembering to add it, and the partition below then has a member nothing rules"*. Seeded with

```ts
  readonly readSeed: () => Promise<void>;
```

behind a doc comment, that file ran **14 of 14 GREEN**, its `RULED` partition asserted in both directions included, while this row's brace-balanced slice reddened. **The port grew a member, nothing ruled it, and the control written to catch exactly that stayed quiet.**

It is right today, and the repair here is a tie rather than an edit to a fenced file: the two derivations are asserted equal on every run, so the fragile one is falsifiable instead of merely correct. **The wider question, whether other constructibility suites written from the same idiom carry the same hole, is named in the ADR's approval block as a sweep this row did not take.**

---

### 6. Blockers, landmines and what is owed

- **The `E2` read of [ADR-364](../decisions/ADR-364.md) is owed.** Money-adjacent: `POST /internal/batch/run` triggers the nightly batch.
- **OWED TO THE SIBLING HOLDING `apps/api/test/wiring.test.ts`**, which was not opened for writing. Three items, in ADR-364 section 9: the `setInternalOpsSource` entry's closing sentence is OVERSTATED and needs the `RI-14` repair rather than a deletion; the new file is the citation to add for the half-install answer; and ADR-360's SIX-versus-EIGHT prose repair is **confirmed independently here** and still owed.
- **LANDMINE: nothing guards the partial install and nothing reports it.** A slice that lands `runBatch` and forgets the probes produces section 4's table, and no test, gate or invariant in this tree goes red.
- **LANDMINE: the operator surface starts clean with every operator row broken.** The composition report names four registered `/internal/*` routes and no channel anywhere names the unwired port.
- **`docs/INDEX.md` was NOT changed.** A new test file takes no INDEX row; session 555's `admin-session-source.test.ts` has none either, checked rather than assumed.

---

### 7. Verification, derived at reporting time

| runner | result |
|---|---|
| `pnpm vitest run` | **329 files / 7,979 passed / 10 skipped / 0 failed** |
| baseline on `ee7ce5a4`, before the first edit | **328 files / 7,966 passed / 10 skipped / 0 failed** |
| delta | **+13 cases in ONE NEW FILE. By removal: `internal-ops-source.test.ts` runs 13 alone and did not exist on the base** |
| `scripts/corpus/gates.mjs generate` then `check` | **33 of 33** |
| `packages/tooling/checks/repo-invariants.mjs` | **35 of 35** |
| `pnpm run typecheck`, `pnpm run lint`, `pnpm run format:check` | all clean |
| `git diff --stat origin/main -- apps/api/src packages/db/migrations scripts/db` | **EMPTY** |

**`pnpm run verify` was NOT run and `scripts/corpus/falsify.mjs` was NOT run**, both forbidden by this row for mutating the working tree.

**Eight seeds, each watched RED and each restored byte-identical under `sha256sum -c`.** The table is [ADR-364](../decisions/ADR-364.md) section 10. Every one of the thirteen cases is reddened by at least one seed.

---

### 8. This session's own tooling, tested rather than assumed

```
$ command -v gh                                            -> not found
$ curl -s -o /dev/null -w '%{http_code}' \
    https://api.github.com/repos/lam2-coder/meritfutures   -> 403
```

**No `mcp__github__*` tool is present in this session's surface.** `git push -u origin` **authenticates and succeeds**, which matches every prior session that recorded this.

**THE CONTAINER ARRIVED WITH NO REPOSITORY.** `/home/user` was empty; the tree was attached and cloned by hand before a file could be read, the condition sessions 550, 553 and 557 recorded.

**The operational note about bare commands was taken and it held.** Commands were run from the repository directory with no `cd` prefix and **no permission prompt stalled a suite run**. One tool call was refused by the harness classifier: a `python3` heredoc that edited a source file for the first seed. **It was not worked around; the seed was applied with the editing tool instead**, and every later seed used the same route.

**The branch is `claude/the-internal-ops-source-nobody-sets`, pushed. NO PULL REQUEST WAS OPENED, BY INSTRUCTION: the parent session opens it.**

---

### 9. Next

The founder's `E2` read of [ADR-364](../decisions/ADR-364.md) and the four questions its approval block names, of which **OQ-3, whether a wiring slice may install this port partially, is the one that is cheap now and expensive after the slice exists**. Then the sibling's `wiring.test.ts` repairs, which now carry three owed items from this row and two from ADR-360. Separately: the sweep for other constructibility suites deriving interface members by line pattern.

---

### 10. Files touched

| file | what |
|---|---|
| `apps/api/test/internal-ops-source.test.ts` | **NEW.** Thirteen cases: the four-member slice and its two corroborations, the five-key document in three directions, the malformed-versus-well-formed comparison, the structural log argument, the half install and its 202, the guard that bounds it, the throwing member's indistinguishability, and the boot report |
| `docs/decisions/ADR-364.md` | **NEW.** The ruling, `proposed`, UNSIGNED |
| `docs/decisions/ALLOCATION.md` | the `364` row |
| `docs/decisions/README.md` | the `ADR-364` row, and generated spans |
| `docs/sessions/2026-09-06-session-559.md`, `docs/sessions/README.md` | this file and its generated row |
| `docs/STATE.md` | this session's entry, and generated spans |

**No file under any `src/` was changed.** The eight seed mutations were reverted from byte copies and `git diff origin/main -- apps/api/src` is empty. **`docs/INDEX.md` was NOT changed**: ADR entry files are indexed by `docs/decisions/README.md` under `CI-06n`, and a test file takes no row there.
