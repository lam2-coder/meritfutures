---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 275: `P5-g`, the wallet reads

<!--index: **[API_CONTRACT](../architecture/API_CONTRACT.md) section 6.2 is SERVED**: [`wallet.ts`](../../apps/api/src/routes/wallet.ts) and [`wallet.test.ts`](../../apps/api/test/wallet.test.ts) are the whole diff besides `STATE` and this log. No ADR, no migration, no `SqlExecutorReason` member, no `pg`, [`scoped-db.ts`](../../packages/db/src/scoped-db.ts) untouched. **THE FINDING IS THAT `held_cents` IS 0 ON EVERY RESPONSE THIS TREE CAN PRODUCE AND IT IS THE FIELD THE SCREEN EXISTS FOR**: [ADR-158](../decisions/ADR-158.md) clause 6 makes `available_at` null because no landed column carries a purchase's chargeback-window end, and **the same absence decides MEMBERSHIP of the hold and not only its release**, so `cents` and `since` are equally uncomputable and a `WalletHold` cannot be built truthfully today. `holdsToday` is one function returning one empty array so the day `DEP-M20-03` lands there is one place to change. **The biconditional does NOT reach this route and the paragraph says why it must not**: a payout under hold has not settled, `LT-01` has not posted, and there is no `wallet_entries` row for it at all, so summing it would make `balance = withdrawable + held` false. **THE BALANCE IS `max(id)` AND NOT `max(occurred_at)`**, because `balance_after_cents` is computed at APPEND time and a backdated row is the one world where the two disagree. **[ADR-157](../decisions/ADR-157.md)'s cost is paid in full**: both endpoints read the whole statement, and `atMost` is this cursor's term BY NAME but unreachable, because [`db.ts`](../../apps/api/src/db.ts) is the one door onto `@merit/db` and re-exports nothing -- a two-line change in a file this fence does not hold, reported rather than reached. **THE DISPATCH'S REGISTRY INSTRUCTION IS REFUTED BY THE APPLICATION**: [ADR-100](../decisions/ADR-100.md)'s registry is the DIRECTORY LISTING, there is no row and no list, and two routes were registered by adding one file and editing nothing. Measured on `CompositionReport`: public 36 to 38, operator withheld 35 to 37, union 54 to 56. **NINE DEFECTS SEEDED AND THE NINTH ESCAPED THE FIRST SUITE** -- a lexical tie-break in `isAfter`, reached only when a page boundary falls INSIDE one `occurred_at`, which silently drops an entry and ends the walk one movement short; two cases added and both watched failing on the reseed. `typecheck` and `lint` caught two more `vitest` could not. **33 of 33 gates, 11 of 11 invariants, typecheck 0, lint 0, 171 files / 3,153 passed / 6 skipped, `falsify` clean**, against a baseline of 170 / 3,115 / 6 that reproduced exactly -->

- **Objective:** [P5](../plans/P5-payouts-and-wallet.md) `P5-g`. [API_CONTRACT](../architecture/API_CONTRACT.md) section 6.2's `GET /wallet` and `GET /wallet/entries` are served by a new [`apps/api/src/routes/wallet.ts`](../../apps/api/src/routes/wallet.ts), transcribed from the rows [ADR-158](../decisions/ADR-158.md) wrote from the CHECK, with a suite in [`apps/api/test/wallet.test.ts`](../../apps/api/test/wallet.test.ts).
- **Done:** [`apps/api/src/routes/wallet.ts`](../../apps/api/src/routes/wallet.ts) serving `GET /wallet` and `GET /wallet/entries`, and [`apps/api/test/wallet.test.ts`](../../apps/api/test/wallet.test.ts), **38 cases through Fastify's real router over the modules discovered from disk, watched failing on nine seeded defects**. [STATE](../STATE.md) appended, the generated span re-run. **Measured separately, each with its own command**: 33 of 33 gates, 11 of 11 invariants, `typecheck` exit 0, `lint` exit 0, **171 test files / 3,153 passed / 6 skipped**, `falsify.mjs` exit 0. The dispatch's baseline of 170 / 3,115 / 6 reproduced exactly before anything was written.
- **Next:** **`P5-h` (`POST /wallet/withdrawals`) inherits three things it should not have to rediscover.** P-1 is `provenance_review` on the withdrawal and subtracts nothing from `withdrawable_cents` ([ADR-158](../decisions/ADR-158.md) clause 5), so the two legs do not double-count it. **P-3 is unevaluable on that leg for the same reason it is unevaluable here**, so a withdrawal cannot refuse what this read did not hold, and the two surfaces are consistent by accident of the same absence rather than by agreement. And `composition` is `null` at `requested`, which the CHECK permits and the sequence in [M20](../plans/M20-wallet.md) section 3.3 does not. **Whoever wires this deployable's persistence installs `databaseWalletBackend(db)`**; until then both routes answer 503 and neither renders a figure it did not read.
- **Blockers:** none for this slice; it landed whole. **`held_cents` is blocked for everybody and the block is a schema absence rather than a slice**: `OQ-M20-02` (how long is P-3's hold) is open and goes to the founder, and `DEP-M20-03` (M3 records the chargeback-window state of every purchase) is unbuilt. Neither is `P5-g`'s to answer and both are named in [ADR-158](../decisions/ADR-158.md) already.
- **Landmines:** **`holdsToday` returning an empty array is NOT "no hold applies" and a later reader must not read it as one.** It takes the rows it cannot yet use precisely so the day the chargeback window lands the change is one function. **The `held > balance` guard in `renderWallet` is unreachable today and therefore UNWATCHED**, and the suite's own case name says so rather than implying a refusal it does not exercise. **The `id` tie-break is a `bigint` comparison in TWO places**, `newestFirst` and `isAfter`, and a lexical one is green in both until a page boundary falls inside a shared `occurred_at` across a power of ten; the second of those escaped this suite's first draft. **[`db.ts`](../../apps/api/src/db.ts)'s header asks that `grep -rln '@merit/db' apps/api/src` return exactly one file and it returns FOUR**, three being route files that discuss the package in prose rather than import it: the convention is intact, its stated measurement is not, and the file is outside this fence.
- **Files touched:** [`apps/api/src/routes/wallet.ts`](../../apps/api/src/routes/wallet.ts) (new), [`apps/api/test/wallet.test.ts`](../../apps/api/test/wallet.test.ts) (new), [STATE](../STATE.md) (one appended section at the END), this file, and [sessions/README](README.md) inside the generated `session_entries` span. **Nothing else, and the fence was not widened once.**
- **THE DISPATCH WAS WRONG ABOUT THE REGISTRY AND THE APPLICATION IS WHY ANYBODY KNOWS.** It said *"the route REGISTRY entry is an APPEND -- add your row, order by path, and let the type checker catch a bad merge resolution"*. [ADR-100](../decisions/ADR-100.md)'s registry is the DIRECTORY LISTING and [`registry.ts`](../../apps/api/src/registry.ts) says so in capitals; there is no row, no list and no merge to resolve. **Two routes were registered by adding one file and editing nothing**, which is that entry's whole return and is what makes twenty concurrent route branches merge in any order.
- **NO CLAIM ROW EXISTS FOR THIS SESSION.** [sessions/README](README.md)'s CLAIM table ends at **272** and carries no row for 273, 274 or 275, so [P5](../plans/P5-payouts-and-wallet.md) section 11 rule 1's strike had nothing to strike. **None was invented**: that table records what was dispatched and writing my own row into it would fabricate a dispatch record rather than satisfy a rule.

---

### THE PLAN, written into the first commit rather than into plan mode

**This session has no human watching, so [P5](../plans/P5-payouts-and-wallet.md) section 11's plan-mode clause is discharged HERE**, in the stub commit rule 1 requires, and the plan is the thing the rest of the branch is measured against.

**Baseline reproduced on the dispatch SHA `552c918` BEFORE anything was written**, which rule 11 requires and which is the only reason any figure below will mean anything: 33 of 33 gates, 11 of 11 invariants, `typecheck` exit 0, `lint` exit 0, **170 test files / 3,115 passed / 6 skipped**. It matches the dispatch's stated baseline exactly.

#### 1. The shape, and it is `accounts.ts`'s rather than a new one

A `WalletBackend` PORT, an `UNWIRED_WALLET_BACKEND` that rejects every call and is answered **503**, a `databaseWalletBackend(db: ApiDb)` that reads through the accessor, and `withSessionContext` for the auth. That is [`accounts.ts`](../../apps/api/src/routes/accounts.ts)'s and [`payouts.ts`](../../apps/api/src/routes/payouts.ts)'s idiom unchanged, and its reason is theirs: *"a backend that returned plausible values would be a fixture serving real traffic"*. The route is REGISTERED either way, because the contract row exists and a missing route answers 404 and reads as a contract Merit never wrote.

**The projection is an allowlist built field by field**, section 1's API3 control, and never the accessor's row spread.

#### 2. The balance is the row with the greatest `id` and NOT the greatest `occurred_at`

`wallet_entries.balance_after_cents` is a STORED running balance, computed at APPEND time, so the current balance is the last row APPENDED. Ordering is `id`, which is `bigint GENERATED ALWAYS AS IDENTITY`. **The statement's ORDER is `occurred_at DESC` and that is a different question**, which the contract answers separately for `GET /wallet/entries`; a backdated `occurred_at` would make the two disagree, and only one of them is the balance.

#### 3. The cursor is composed in the ROUTE, and the inclusive bound is the argument `P5-a` invited

[ADR-157](../decisions/ADR-157.md) refused the aggregate and gave a RANGE term, so the page is composed from rows the accessor returns. `scoped-db.ts` names this slice by name: *"a cursor over `wallet_entries` is `P5-g`'s and an inclusive bound re-reads its boundary row, so if that is unacceptable it is an argument `P5-g` makes rather than one this entry makes for it."* **It is acceptable and this session makes no such argument.** The cursor carries `occurred_at` and `entry_id`, `atMost(occurredAt)` renders the inclusive bound, and the route drops the rows at or after the anchor's position under the full `(occurred_at DESC, id DESC)` order. A strict inequality would still not be enough, because the tie-break is a second column and no term in the vocabulary is a tuple comparison.

#### 4. THE COST ADR-157 SECTION 5 NAMES IS PAID HERE AND ITS SIZE IS RECORDED

*"The rows crossing the boundary are the window's rather than the match's."* Concretely: `GET /wallet` reads the identity's WHOLE statement to render one balance, and `GET /wallet/entries` reads every entry at or before the cursor's instant to render one page of at most 100. There is no `LIMIT` and no `ORDER BY` in the accessor and this slice adds neither.

#### 5. What is expected to be a FINDING rather than a transcription

**`held_cents` under `chargeback_window` has no input in this tree.** [ADR-158](../decisions/ADR-158.md) clause 6 makes `available_at` null because no landed column carries a purchase's chargeback-window end; the same absence decides MEMBERSHIP of the hold and not only its release. This is measured before it is written up, and it is reported rather than papered.

#### 6. The foreclosures this branch carries

No `SqlExecutorReason` member, no `SystemReason` member, no `pg` import, no cast past a key type, no edit to [`scoped-db.ts`](../../packages/db/src/scoped-db.ts), no ADR, no migration. Money is `bigint` cents inside the file and a JSON integer on the wire, refused rather than rounded. The fence is [`apps/api/src/routes/wallet.ts`](../../apps/api/src/routes/wallet.ts), [`apps/api/test/wallet.test.ts`](../../apps/api/test/wallet.test.ts), [STATE](../STATE.md) appended at the END, and `docs/sessions/`.
