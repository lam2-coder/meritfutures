---
status: approved
depends_on: [README.md]
last_updated: 2026-08-29
---

## 2026-08-29 - Session 441: an instant is not a trading day, the gap was narrower than the sentence said, and the port that named it asked a three-armed question with one arm

<!--index: **THE RULING IS [`ADR-251`](../decisions/ADR-251.md), `proposed` and UNSIGNED, and NO MIGRATION NUMBER AND NO `RI-` NUMBER WERE TAKEN.** Row `251` asked whether the smallest of `useAdminWriteBackend`'s three suppliers is buildable and said to build it if so. **IT IS BUILDABLE AND IT IS BUILT**: `packages/rules-engine/src/calendar.ts` gains `buildSessionCalendar` and `tradingDayAt`, which answer by CONTAINMENT over `trading_calendar`'s stored session bounds and `trading_calendar_loads`' coverage, comparing an instant only with an instant and reading the day off the row that comparison selected. **THE CLAUSE IS NOT NARROWED, IT IS FALSE, AND IT WAS ALREADY FALSE IN TWO PLACES BEFORE THIS SESSION CHANGED A BYTE**: `readLastClosedTradingDay` (`apps/worker/src/batch/adapter.ts:333`) and the PRIVATE `anchorCalendar` (`apps/api/src/admin-source/liability.ts:1605`) both map an instant to a day today, and [ADR-145](../decisions/ADR-145.md) finding 10's EVIDENCE was only ever about `calendar.ts`'s exports while its WORDING was about the whole workspace. **THE TRAP WAS NOT SPRUNG AND IT WAS MEASURED IN BOTH DIRECTIONS RATHER THAN AVOIDED BY INTENTION**: three paper-overs were seeded into the shipped function and each watched RED, the nearest-loaded-day costume the row named by name, the collapse of `outside_coverage` into `not_a_session`, and the UTC calendar date. **SEED C IS THE RESULT WORTH READING**: the UTC-date paper-over CANNOT BE WRITTEN inside `packages/rules-engine/src` at all, because `merit/engine-purity` and `no-restricted-globals` both fire on the `new Date(atMs)` it needs, which is a control this session did not build and did not know it was relying on. **THE PORT IS NOT WIRED AND THE WIRED COUNT DID NOT MOVE**, and that is the row's own instruction: `useAdminWriteBackend` stays blocked on `principal(request)` behind the C-08 purchase and on a `ValidationResult` projection nothing in this tree performs, so the entry goes from three clauses to two. **WHAT REPLACES THE THIRD IS A SHAPE RATHER THAN AN ABSENCE**: `tradingDay(): string` has ONE arm and the resolver answers THREE, and no ruling says which day an operator close takes when the instant is in no session while `accounts_terminal_has_close_date` requires `closed_on` on every `closed_admin` row, so A DECISION IS OWED AND A FUNCTION IS NOT. **A FINDING NOBODY WENT LOOKING FOR AND IT IS THE MOST CONSEQUENTIAL THING HERE**: `apps/worker` NEVER READS `trading_calendar_loads`, measured at zero occurrences under its `src/`, so `toCalendarSlice` declares the nightly fold's coverage as the EXTENT OF THE ROWS IT LOADED (`adapter.ts:315`) while its own docblock says "COVERAGE IS EXACTLY WHAT WAS LOADED", and a bug in the load therefore reports as `not_a_session`, which is the exact substitution `ADR-042` F-4 exists to prevent living in the deployable that folds every account every night. **REPORTED AND NOT FIXED**: `apps/worker/**` is outside this row's fence. **`RI-15` CAUGHT THIS SESSION'S OWN CITATIONS** at three sites and is obeyed rather than argued with, which is the second consecutive wave it has caught the entry written to satisfy it. **THREE CITATION REPAIRS TAKEN** under the row's up-front grant, one of them a line this session's own export moved. **ONE CASE OF THIS SESSION'S OWN WAS WRONG AND IS RECORDED** rather than quietly corrected. Counts derived at reporting time: **33 of 33** gates, **22 of 22** invariants off the runner's last line, **271 files / 6,598 passed / 6 skipped** against an `origin/main` baseline of **269 / 6,583 / 6** reproduced before a line changed, a delta of **+15 cases across 2 new files**, typecheck 0, lint 0, `format:check` clean. **`falsify.mjs` was NOT run**: it is not in this row's verification list and it mutates the working tree. -->

**Objective.** Establish whether an instant can be mapped to an exchange trading day in this
workspace, and build it if it can.

**Outcome.** **It can, it is built, and the clause that said otherwise was false rather than
narrow.** The port it was blocking is no closer to being wired, which the row predicted and
required.

### 1. What the row claimed, and what the tree says

| Row `251` and the dispatch                                                  | Derived before a line changed                                                                            |
| --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| No `tradingDay` function is exported under `apps/**` or `packages/**`       | **HOLDS.** The name is an injected PORT in three files and every one throws                              |
| `CalendarSlice`'s functions take a day or a sequence, never an instant      | **HOLDS, AND IT IS THE VALUE RATHER THAN THE FUNCTIONS.** `CalendarDay` carries no session bounds at all |
| `trading_calendar` is `firm` at `scope.ts:1485`, `apps/api` holds `db.firm` | **HOLDS, CITATION EXACT**                                                                                |
| "Nothing in this workspace maps an instant to a trading day"                | **FALSE ALREADY, IN TWO PLACES.** Section 2                                                              |
| Consult BOTH tables and answer UNKNOWN where the calendar is not loaded     | **DONE, and the coverage test is instant against instant.** Section 3                                    |
| Do not wire the port                                                        | **HELD.** `start.ts` is untouched and the port still throws by name                                      |

### 2. The sentence was already false, and only reading for it finds that

[ADR-145](../decisions/ADR-145.md) finding 10 enumerates `calendar.ts`'s exports and observes that
none takes an instant. **That is true and it is still true.** The sentence built on it was about the
whole workspace, and two functions already did the mapping: the worker's `readLastClosedTradingDay`
in another deployable, and `liability.ts`'s `anchorCalendar`, private, in this one.

**So the honest gap was never that nothing maps an instant to a day.** It was that both functions
that do answer a different question in the wrong place, and the general one had no home.

### 3. What was built

| Artefact                                                                                 | What it holds                                                                                                                                                                                               |
| ---------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [`calendar.ts`](../../packages/rules-engine/src/calendar.ts)                             | `buildSessionCalendar` and `tradingDayAt`. Containment, three answers, and five refusals of which OVERLAP is the load-bearing one: containment is an answer only while at most one session holds an instant |
| [`index.ts`](../../packages/rules-engine/src/index.ts)                                   | The export lines. The package declares one door and no subpath, so a function not named there is a function no caller can reach                                                                             |
| [`session-calendar.test.ts`](../../packages/rules-engine/test/session-calendar.test.ts)  | Ten cases. The premise asserted BEFORE anything rests on it, on `ADR-146` section 3's discipline                                                                                                            |
| [`admin-write-trading-day.test.ts`](../../apps/api/test/admin-write-trading-day.test.ts) | Five cases. Every clause of the narrowed entry derived from source, including that the port still refuses                                                                                                   |
| [`wiring.test.ts`](../../apps/api/test/wiring.test.ts)                                   | The entry narrowed to two clauses, three citations repointed                                                                                                                                                |
| [`evaluate.golden.test.ts`](../../packages/rules-engine/test/evaluate.golden.test.ts)    | **THE EXPORT-SURFACE GATE WENT RED ON THE TWO NEW EXPORTS AND IS UPDATED WITH ITS ARGUMENT RATHER THAN MADE TO PASS.** `ADR-078`'s test a fourth time, and the first whose two new names are both functions |
| [`admin-writes.ts`](../../apps/api/src/routes/admin-writes.ts)                           | Two stale census sentences repaired at the source. **Docblocks only, no behaviour**                                                                                                                         |

**`trading_calendar_holiday_has_no_session` IS A TYPE RATHER THAN A CHECK.** `SessionDay` is a
discriminated union on `is_holiday`, so a holiday carrying a session cannot be spelled.

**AND NO READ HELPER WAS TAKEN IN `packages/db/src/**`.** The fence permitted one if genuinely owed.
It is not: `FirmTx` already reads both tables and `liability.ts` proves it in this same deployable.

### 4. The transcript

RED on the tree before the change, with the two source files stashed and the new test present:

```
FAIL  |unit| packages/rules-engine/test/session-calendar.test.ts
TypeError: buildSessionCalendar is not a function
```

GREEN after: ten passed. **A red that is only an absence is a weak red**, so three paper-overs were
seeded into the shipped function and each watched failing, each restored with `git status` reporting
the intended paths and nothing else. **Seed C, the UTC calendar date, is refused by the test AND by
`merit/engine-purity` and `no-restricted-globals` independently.**

### 5. Files touched

`packages/rules-engine/src/calendar.ts`, `packages/rules-engine/src/index.ts`,
`packages/rules-engine/test/session-calendar.test.ts`,
`apps/api/src/routes/admin-writes.ts` (docblocks only),
`apps/api/test/admin-write-trading-day.test.ts`, `apps/api/test/wiring.test.ts`,
`packages/rules-engine/test/evaluate.golden.test.ts`,
`docs/decisions/ADR-251.md`, `docs/decisions/ALLOCATION.md` row `251`,
`docs/decisions/README.md`, `docs/STATE.md`, this log.

**Not touched**: `apps/worker/**`, `packages/db/**`, `packages/db/migrations/**`, `start.ts`,
`MERIT_BUILD_MASTER_PROMPT.md`, and every `status` field in the tree.

**[`docs/INDEX.md`](../INDEX.md) NEEDED NO CHANGE AND THAT IS DERIVED RATHER THAN ASSUMED**, which is
session 436's ruling re-checked here. An `ADR-*.md` entry file is a fragment indexed by
[`decisions/README.md`](../decisions/README.md) under `CI-06n`, a session log is a fragment indexed
by [`sessions/README.md`](README.md), **both of those tables are generated spans and both regenerated
in this commit**, and `INDEX.md` carries no ADR-count span for a new entry to move. `STATE`'s
`gen:adr_count` moved on its own, by the runner.

### 6. Next

- **The ruling `tradingDay(): string` is owed**, which is which day an operator close takes when the instant is in no session. Sized in `ADR-251` section 5 and in the founder block.
- **The worker's missing coverage read**, `ADR-251` section 7. A money-path slice of its own and the most consequential thing this session found.
- **The `ValidationResult` to `PlanValidation` projection**, which is now one of the two clauses left on this port.
- **`ADR-245`'s `ExternalGates` resolver** and **`ADR-239` slice A**, both still unstarted.

### 7. Blockers and landmines

- **`E2` READ IS OWED** on `ADR-251`. It ships `proposed` and UNSIGNED, and merging is not signing.
- **`packages/rules-engine/src/index.ts` IS A CITED FILE AND THIS SESSION MOVED ITS LINES.** One citation into it was repaired; a future export addition moves them again and most are read by no check.
- **`tradingDayAt` IS EXPORTED AND NO ADAPTER CALLS IT.** Nothing has run it against a database row, so that `timestamptz` arrives through the keyed accessor in a shape `Date.parse` accepts is asserted by `liability.ts` doing it and by nothing in this diff.
- **THE ADJACENT-LOAD SEAM ANSWERS UNKNOWN.** Two abutting loads leave the overnight gap between them `outside_coverage`. It is the safe direction and it is a judgement, named in `ADR-251` section 8 as the first thing to read hardest.
- **`docs/STATE.md` STILL CARRIES THE `gen:adr_count` PARAGRAPH FOUR TIMES**, which session 436 recorded at two and which has grown. **The generator rewrote all four in this commit**, so the duplication is visible in this session's diff without being this session's doing, and `RI-12`'s ceiling of eight identical lines is now half spent. Outside this row's fence and not collapsed here.
