---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 281: where the CUSUM lives

<!--index: placeholder -->

- **Objective:** rule `P7-c`, [P7 section 10 item 3](../plans/P7-risk-and-abuse.md): where the pass-rate CUSUM lives. One of [section 5.3](../plans/P7-risk-and-abuse.md)'s three readings, argued rather than assumed, with the cost of the other two stated. [`0049`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql)'s disposition of `per_plan` corrected in [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md) and never in `0049`.
- **Done:** placeholder
- **Next:** placeholder
- **Blockers:** placeholder
- **Landmines:** placeholder
- **Files touched:** placeholder

---

### THE PLAN, WRITTEN BEFORE THE WORK (ADR-003 strict, no interactive plan mode)

**This session has no human watching, so the plan is committed here rather than approved in a mode.**

1. **Reproduce the dispatch baseline before changing anything.** 33 gates, 11 invariants, typecheck 0, lint 0, 170 files / 3,115 passed / 6 skipped.
2. **Re-derive section 5.3 rather than trust it**: `plan_breaker_state`'s primary key, its columns, `0049`'s per_plan paragraph, `API_CONTRACT`'s `per_plan` shape, `P-M6-06`'s recurrence, and whether the series the recurrence runs over exists.
3. **Argue the three readings and take one.** State what the other two cost.
4. **Write [ADR-167](../decisions/ADR-167.md)**, stating what it FORECLOSES and not only what it chooses.
5. **Correct `per_plan`'s disposition in DELTA_MANIFEST**, in a new dated section, on section 22's precedent: a merged artifact is not edited and the correction is recorded where a reader looks.
6. **Amend the `plan_breaker_state` design record** with what the ruling makes true of it.
7. **Amend `ALLOCATION`'s `167` row and its `0051` row IN PLACE** ([ADR-065](../decisions/ADR-065.md) T3).
8. **Append one `##` section to STATE at the END**, finish this log, re-run `gates.mjs generate`.
9. **Verify by running, in pieces, and paste every output into the pull-request body.** `falsify.mjs` last and alone.

**The migration number is conditional and it is not taken to justify the reservation.** If the reading that wins needs no migration, `0051` returns to the pool and its row says so, on `ADR-160`'s precedent.
