---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 281: the CUSUM's home is nowhere, and the reason the plan gave for that answer was not the reason

<!--index: **[ADR-167](../decisions/ADR-167.md) proposed, `status: proposed`, approval line UNSIGNED, MONEY PATH BY CONTENT, and `0051` RETURNS TO THE POOL UNSPENT.** [P7 section 10 item 3](../plans/P7-risk-and-abuse.md) held `P7-c` for a founder answer, the answer was delegated, and what was delegated was the authority to CHOOSE rather than permission to assume: **reading 3 of three, `S_t` folded at read time from a series [`0007`](../../packages/db/migrations/0007_accounts.sql) already carries, and no column, table or row holds it.** **Reading 1 is refused on a MONEY-PATH DEFECT rather than on the primary-key change**: with `metric` in the key, `state`'s `'paused'` value governs the added rows and [API_CONTRACT:798](../architecture/API_CONTRACT.md)'s `per_plan.sales_paused` derives from it, so an ADVISORY statistic whose own panel gloss is *"inspect before the funded wave"* gets a column spelling a REVENUE PAUSE one `UPDATE` away, on the table `INV-M5-12` is written about; and `min_sample integer NOT NULL CHECK (min_sample > 0)` forbids even the honest zero, so such a row must invent a minimum sample it does not have. **Reading 2 is refused on [`0049`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql)'s OWN objection with `0049`'s own remedy structurally unavailable**: its founder-read item 1 is *"a ratio stored beside its own numerator invites recomputation drift ... a ratio the database computes cannot disagree with its own inputs"*, answered there by a `GENERATED` column -- **but a generated column is a row-local expression and `S_t` is a recurrence over the row before it**, so the drift returns unpoliceable one migration later on the same rail; and its parameter columns have no source, `mu_0` and `sigma` being `DEP-M6-05`'s and `DEP-M6-05` being **Wave 4**. **THE PLAN'S OWN STATED REASON FOR THE WINNING READING IS CORRECTED RATHER THAN REPEATED**: *"nothing about storing it makes more reproducible"* is too weak, because storing `S_t` makes the value RETRIEVABLE and not CHECKABLE, and **a stored `S_t` that disagrees with a replay yields two answers and no rule for which is right.** **P7 RULE 17's SECOND CLAUSE DOES NOT SURVIVE THE ARITHMETIC AND GETTING IT RIGHT IS WHAT REMOVES THE FLOAT**: every operand of `S_t = max(0, S_(t-1) + (x_t - mu_0 - 0.5*sigma))` is a RATE, so the statistic **IS** commensurable with a basis point and is **NOT** commensurable with `ratio_bp`, which is a LOSS ratio in the same unit -- **a column carrying the right unit and the wrong quantity is exactly what "column for column" mistook for a fit.** `integer` suffices with the bound a multiplication rather than an assumption (`10000` bp per day, `int4` unreached for **588 years**), and **the slack is configured as `k_bp` so `sigma` is never halved on the evaluation path**, because a per-step truncation with a constant sign accumulates in a running sum. **[ADR-157](../decisions/ADR-157.md) finding 8's numeric-transport ruling is NOT reached and the line that would reach it is named**: the moment `x_t` is computed with a SQL `COUNT` or `SUM` rather than by counting rows. **THE FALSE DISPOSITION IS IN TWO PLACES AND THE DISPATCH NAMED ONE**: [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md) section 26's `OI-01` register row says **"column for column"**, enumerates nothing where `0049` at least enumerates the five columns it checked, and is in the register rather than in a migration header, so it is the copy a reader reaches first; corrected in **section 27** and never in `0049`, on section 22's precedent. **THE UNDERLYING GAP IS NOT CLOSED AND THE ENTRY SAYS SO**: the CUSUM had neither storage nor inputs and now has a ruling about the first and still nothing about the second, [`apps/admin/src/page.ts:156`](../../apps/admin/src/page.ts) already listing `P-M6-06` pending on `DEP-M6-05` with `FM-M6-07`'s *"either constant alarms or none, which is the same as no chart"*; **what needs a versioned home when the harness lands is the PARAMETER SET and not the statistic**, and [`detector_definitions`](../../packages/db/migrations/0008_risk.sql) is named as the shape that already exists and deliberately NOT taken, because its `is_sensitive` column is `P7-j`'s strip list. **`OQ-M6-02`'s NUMBER IS UNTOUCHED and only the schema half is ruled**: `sample_size` is the DENOMINATOR's count, sourced from [`0016:116`](../../packages/db/migrations/0016_treasury_controls.sql)'s own *"a two-transaction denominator"*. **NO MIGRATION, NO TABLE, no `schema.ts` registration, no `scope.ts` change, no design record, no `SqlExecutorReason` member, no `SystemReason` member, no `pg` import and no cast**; `schema.ts` receives **one comment and no code**, because it is where a later session would add the column the entry forecloses, and `scope.ts` is not touched even by a comment on its own tenancy-only rule. **NO `E2` READ IS OWED, because nothing was written that a founder must read line by line.** **THE REPOSITORY WAS NOT IN THIS CONTAINER AND WAS ATTACHED AND CLONED BEFORE ANY WORK BEGAN**, `HEAD` landing exactly on the dispatch SHA `552c918`. **Sessions 274 and 278 were not merged into and the union is mechanical**: `schema.ts` gains a comment block above one existing table and no declaration, `scope.ts` is untouched, and `DELTA_MANIFEST` gains a new trailing section. Gates **33 of 33**, invariants **11 of 11**, `typecheck` exit 0, `lint` exit 0, suite **170 files / 3,115 passed / 6 skipped**, `falsify.mjs` exit 0 at 33 gates, 80 scope and 10 loader cases -- the dispatch baseline reproduced BEFORE anything changed and unmoved by all of it -->

- **Objective:** rule [`P7-c`](../plans/P7-risk-and-abuse.md), [P7 section 10 item 3](../plans/P7-risk-and-abuse.md): where the pass-rate CUSUM lives. One of section 5.3's three readings, **argued rather than assumed**, with the cost of the other two stated, and [`0049`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql)'s disposition of `per_plan` corrected in [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md) and never in `0049`.
- **Done:** [ADR-167](../decisions/ADR-167.md) (new, `status: proposed`, UNSIGNED), [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md) section 27, the [`plan_breaker_state`](../architecture/data-model/plan_breaker_state.md) design record's prose, one comment block in [`schema.ts`](../../packages/db/src/schema.ts), [ALLOCATION](../decisions/ALLOCATION.md)'s `167` and `0051` rows amended in place ([ADR-065](../decisions/ADR-065.md) T3), [STATE](../STATE.md) appended, this log. **`0051` NOT TAKEN.**
- **Next:** **[`P7-k`](../plans/P7-risk-and-abuse.md) is unblocked and inherits a smaller thing than it asked for**: it writes `plan_breaker_state` for the loss ratio, writes no CUSUM row anywhere, folds `S_t` when something asks, and **renders `per_plan[].cusum` as absent until `DEP-M6-05` lands.** **What absence looks like on the wire is [`P7-b`](../plans/P7-risk-and-abuse.md)'s**, `API_CONTRACT` being that slice's file and not this one's. **[`P7-l`](../plans/P7-risk-and-abuse.md)'s `weekly_loss_ratio_cusum` digest inherits the same absence for the same reason** -- a digest rendering an uncalibrated CUSUM is `FM-M6-07` delivered weekly by email. **The parameter set's versioned home is OWED and is not `0051`'s**, section 6 of the entry.
- **Blockers:** none for this slice. **The standing one is `DEP-M6-05`**, Wave 4: the CUSUM has a ruling about storage and still no `mu_0` and no `sigma`.
- **Landmines:** **`ratio_bp` and the CUSUM statistic are both basis points and are different quantities**, which is the whole trap: a later reader who greps for a `_bp` column will find one that fits the unit and not the meaning, and that is precisely the inference `0049` made. **`min_sample` and `sample_size` are the DENOMINATOR's**, and a row holding the settled-payout count instead satisfies every `CHECK` in `0016` and describes the wrong population, which no gate can see. **`sigma` must never be halved on the evaluation path**; the configured term is `k_bp`. **`schema.ts`'s comment is the only thing standing between the next session and reading 1**, because a CUSUM column added to that declaration type-checks and the suite's drift assertion would then demand a matching migration rather than refuse the idea. **The generated ADR-count span moved 152 to 153 across 4,672 duplicated paragraphs in STATE**, which is [session 267](2026-08-27-session-267.md)'s reported defect surfacing as diff noise; it is `gates.mjs generate` output and not a hand edit, and it is NOT repaired here.
- **Files touched:** [ADR-167](../decisions/ADR-167.md) (new), [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md) (section 27, appended), [`plan_breaker_state`](../architecture/data-model/plan_breaker_state.md) (prose below the column table; the table itself unchanged), [`packages/db/src/schema.ts`](../../packages/db/src/schema.ts) (one comment block, no code), [ALLOCATION](../decisions/ALLOCATION.md) (the `167` row and the `0051` row, both in place), [decisions/README](../decisions/README.md) and [sessions/README](README.md) (generated spans), [STATE](../STATE.md) (one appended section), this file.

---

### 1. The repository was not in this container, and that is recorded because it is the first thing a reader will not expect

**The dispatch named a branch off `origin/main` at `552c918` and the container's working directory was empty**: no clone, no `.git` anywhere but `/opt/rbenv` and `/opt/nvm`, and `git status` in `/home/user` reporting *"not a git repository"*. The session had no attached source. `lam2-coder/meritfutures` was attached and cloned before any work began, and **`git rev-parse HEAD` returned `552c9186db68ae9a951d1d7df511d09b80a209a0`**, which is the dispatch SHA exactly. `git fetch origin main` was re-run immediately before every assertion about a registry, per rule 3, and `main` was unmoved at `552c918` throughout.

**The baseline was then reproduced before anything was changed**, per the dispatch, and it reproduced exactly: 33 of 33 gates, 11 of 11 invariants, `typecheck` exit 0, `lint` exit 0, **170 files / 3,115 passed / 6 skipped.**

---

### 2. Section 5.3 was re-derived rather than trusted, and it holds -- and it is missing one thing

**Every figure in [P7 section 5.3](../plans/P7-risk-and-abuse.md) reproduces at source.** The primary key is `(plan_id, evaluated_on)` at [`0016:150`](../../packages/db/migrations/0016_treasury_controls.sql); `metric text NOT NULL` at [`0016:130`](../../packages/db/migrations/0016_treasury_controls.sql) is outside it; the table's numeric columns are cents, basis points of a loss ratio, and counts; and [`0049:47`](../../packages/db/migrations/0049_reserve_coverage_snapshots.sql) dispositions `per_plan` while enumerating five columns that do not include the `cusum` object. **Four of `API_CONTRACT`'s five `per_plan` fields ARE this table's and the disposition is right about all four.**

**What section 5.3 does not name is that the false disposition is written in TWO places, and the second is the stronger claim.** [DELTA_MANIFEST](../../packages/db/DELTA_MANIFEST.md) section 26's `OI-01` register row reads *"that is `plan_breaker_state` **column for column**. Thirty-three migrations of an orphan that was not orphaned."* `0049`'s header at least lists what it checked, so a reader can count five names against six fields and see the gap. **This file's row enumerates nothing and asserts a total fit in three words, and it sits in the register rather than in a migration header, which makes it the copy the next session finds first.**

**Two things section 5.3 asserts in passing were checked because the ruling turns on them.** First, **the series the recurrence runs over does exist**: [`0007:37`](../../packages/db/migrations/0007_accounts.sql)'s `accounts` carries `plan_version_id`, `opened_on`, `funded_on`, `closed_on` and `phase`, [`0007:187`](../../packages/db/migrations/0007_accounts.sql)'s `account_status_history` carries every transition with `changed_at`, and [`0004:59`](../../packages/db/migrations/0004_catalog.sql)'s `plan_versions.plan_id` reaches `plans`. Second, **nothing has ever written `plan_breaker_state`**, so there is no stored `S_t` to migrate and this ruling moves no data.

---

### 3. The two readings that lose, and neither loses for the reason it looks like it loses for

**Reading 1 does not lose on the primary-key change.** It loses because after the widening `state`'s `'paused'` value governs the added rows, and `per_plan.sales_paused` derives from it. **A pass-rate CUSUM is advisory** -- [API_CONTRACT:798](../architecture/API_CONTRACT.md) carries `sales_paused` and `cusum.alarm` as separate fields, and [M06:96](../plans/M06-admin-ops-console.md)'s gloss is *"inspect before the funded wave"* -- **so reading 1 hands an advisory signal a column that spells a revenue pause on the table [`INV-M5-12`](../plans/M05-payout-system.md) exists to keep narrow.** That invariant's evidence column reads *"Structural: the breaker's only effect is a flag read by M3's checkout"*, and reading 1 makes the flag settable by a second, weaker signal.

**Reading 2 does not lose on the cost of a migration**, and its steelman is recorded in the entry rather than dismissed: `detector_runs`' own header is *"provenance for every flag, so 'why did this not fire in March' is answerable"*, and a breaker is at least as consequential as a detector. **It loses on `0049`'s own objection with `0049`'s own remedy structurally unavailable** -- a `GENERATED` column is row-local and `S_t` is a recurrence -- **so the migration that got `per_plan` wrong reasoned correctly about exactly this hazard four items earlier in its own header, and applying its reasoning to the field it mis-dispositioned refuses the table.**

---

### 4. What was NOT done, and each absence is a decision

- **No migration and no table.** `0051` returns to the pool, its row amended in place to say so, on `ADR-160`'s precedent and on the reservation's own stated condition.
- **[`scope.ts`](../../packages/db/src/scope.ts) is not touched even by a comment.** That file's own rule is that every `why` states the TABLE's tenancy and never the reader's use, and a CUSUM is a use. **The fence named the file; the file's rule said no.**
- **The [`plan_breaker_state`](../architecture/data-model/plan_breaker_state.md) record's COLUMN TABLE is unchanged**, only the prose below it, so `CI-06i` reconciles the same column set in both directions after this change as before.
- **`detector_definitions` is named and not used.** Right shape, wrong table: it is `P7-d`'s file, and `is_sensitive` is `P7-j`'s evidence-pack strip list, so a breaker parameter landing there would join a redaction policy by accident.
- **`API_CONTRACT` is not edited.** It is `P7-b`'s, and what an absent `cusum` looks like on the wire is a contract question left where the contract lives.
- **STATE's 4,672 duplicated `gen:adr_count` paragraphs are not repaired.** [Session 267](2026-08-27-session-267.md) reported them; they are outside this fence and `CI-06g` rewrites all of them and passes.

---

### 5. Two fence observations, reported rather than reached around

**The fence names `INDEX` (its row) and the gate that enforces an ADR's index row reads a different file.** [ADR-043](../decisions/ADR-043.md) exempts registry ENTRY files from `CI-06c`, and `CI-06n` buys that exemption back by requiring every entry to have a ROW in its registry README. **Zero of the 152 ADR files on `main` have a `docs/INDEX.md` row**, and `ADR-167`'s row is in [decisions/README](../decisions/README.md), where `CI-06n` looks -- **and that row is inside a `<!--gen:-->` span, so it was produced by `gates.mjs generate` rather than written by hand**, which also means it cannot collide with the concurrent sessions that need one. `docs/INDEX.md` is unedited and needed no edit: no non-ADR document was created.

**There is no session-281 row in [sessions/README](README.md) to strike.** The dispatch table's last unstruck row is **267**, and rule 1's *"strike your row"* has no subject here. The entry span at the bottom of that file is generated and carries this log's `<!--index: -->` line.

---

### 6. Verification, run in pieces, with `falsify.mjs` last and alone

Every command was run separately with its own timeout, `pnpm install` first, and **the working tree was confirmed clean before `falsify.mjs` and confirmed unmodified after it.** Outputs are in the pull-request body.

| Command | Result |
|---|---|
| `node scripts/corpus/gates.mjs generate && ... check` | `every span already matches its query.` / **33 of 33 gates pass** |
| `node packages/tooling/checks/repo-invariants.mjs` | **11 of 11 invariants hold** |
| `pnpm run typecheck` | exit **0** |
| `pnpm run lint` | exit **0** |
| `pnpm vitest run` | **170 files / 3,115 passed / 6 skipped** |
| `node scripts/corpus/falsify.mjs` | exit **0**, *"All 33 gates pass clean and fail dirty, 80 scope case(s) hold, and 10 loader case(s) land on the side of the CI-03 boundary they name"* |

**Every figure is the dispatch's `main` baseline unmoved, which is the expected and correct result**: this session writes a ruling, a manifest correction, two comment blocks and four registry rows, and **not one line of executable code.** `pnpm run verify` was not run as a unit, on the dispatch's own measurement that it exceeds 560s and is killed.
