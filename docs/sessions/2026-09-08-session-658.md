---
status: approved
depends_on: [README.md]
last_updated: 2026-09-08
---

## 2026-09-08 - Session 658: a conforming name is now held to a refusal, the wider throw is forced by the polarity rather than chosen, and RI-06's header stops pinning a count its own check never pinned

<!--index: **THE ROW IS [ADR-463](../decisions/ADR-463.md), `proposed` and UNSIGNED. NON-MONEY DIFF, MONEY-ADJACENT SUBJECT, `E2` READ OWED.** [ADR-459](../decisions/ADR-459.md) section 9 items 3 and 4 taken, and **BOTH HELD ON RE-DERIVATION**. Item 3's third leg for [`merit/refusal-naming`](../../packages/eslint-plugin-merit/rules/refusal-naming.js) lands **green with no exemption**: all **9** `refuse[A-Z]` declarations in `packages/db/src` throw in their own top-level body, at `:473`, `:771`, `:946`, `:974`, `:1056`, `:1528`, `:1728`, `:3482` and `:4685`. **THE LEG READS A WIDER THROW THAN LEGS 1 AND 2 AND THE POLARITY FORCES IT**: leg 1 reports a function that DOES throw, so attributing a callback's throw to its parent would manufacture reports; leg 3 reports one that does NOT, so the same attribution suppresses them. **THREE SHAPES RULED IN THE ENTRY RATHER THAN LEFT TO THE CODE**: a throw inside a callback counts, delegation to another `refuse[A-Z]` name counts, a call to itself does not, and `async` is covered because leg 3 keys on a name the author already chose. **WATCHED FIRING BY NAME** on a seeded `refuseNothingAtAll` in `packages/db/src`, with the callback-throw and delegating shapes silent in the same run; seed deleted, `git diff -- packages/db` empty. **THE HOLE IS MEASURED, NOT REASONED**: two mutually recursive conforming names that throw nowhere produce **no lint output at all**, and it is priced rather than closed because closing it needs a cross-file call graph. Item 4's halves both hold: `:526` carried "The three merit rules" verbatim, `rules/` holds **four** files all imported and registered by `index.js`, and **`RI-06`'s `run` really does pin no number** (it iterates `registered` and `attached` and throws only if either is EMPTY, at `:571` and `:581`). Repaired **number-free** rather than as "four", because the comment went stale by pinning a cardinal the check deliberately does not. **TWO STALE POINTERS FOUND AND NAMED, NEITHER REPAIRED**: `repo-invariants.mjs:539` says "all three rules" and is outside the one-line fence, and [ADR-081](../decisions/ADR-081.md) cites `:526` TWICE for `RI-07`, whose header is at `:609`. Suite **346 files / PASSED 8,659 -> 8,669 / skipped 11 unchanged / TOTAL 8,670 -> 8,680**, the rule's own file **25 -> 35 PASSED and TOTAL**, and a count of added `it(` lines reads **0** because `RuleTester` generates cases at runtime. `CONVENTION` is byte-identical and absent from the diff, moved from `:114` to `:157`. Invariants **35 of 35**, gates **33 of 33**, `write-guard-set.mjs` identical before and after at 9 guards and 6 builders. `pnpm run verify` and `falsify.mjs` **NOT run**.-->

### 1. The objective

Take [ADR-459](../decisions/ADR-459.md) section 9 items 3 and 4. Item 3 priced a third leg for `merit/refusal-naming` reporting a `refuse[A-Z]` function with no throw of its own, called it cheap and unowned, and declined it only because taking it would have been that row inventing its own subject. Item 4 recorded that `repo-invariants.mjs:526` miscounts the merit rules in its own comment. **Re-derive both before acting on either**, rule the shape questions in the entry rather than in the implementation, and report a red on a legitimately throw-free function as the result if one appears.

### 2. What the container arrived with

Repository present, working tree clean at `50dc4cb`, **`node_modules` absent**. That is not cosmetic: on the tree as delivered `repo-invariants.mjs` reported `ERROR  RI-18 ... Cannot find module 'typescript'` and refused to call the run 34 of 35, saying `1 of 35 check(s) COULD NOT RUN, so this run did not measure the tree.` `pnpm install --frozen-lockfile` fixed it, changed no tracked file, and the runner then read **35 of 35**. Every baseline in [ADR-463](../decisions/ADR-463.md) section 1 is measured after that install.

### 3. What was derived before the first edit

**9** `refuse[A-Z]` declarations in `packages/db/src`, all in `scoped-db.ts`, all throwing in their own top-level body, so leg 3 is green on 9 of 9 with nothing excluded. **4** rule files in `packages/eslint-plugin-merit/rules/`, all four imported at `index.js:1` to `:4` and registered at `:31` to `:34`. `RI-06`'s `run` parses both sides and pins no cardinal; its `title` at `:546` is already number-free. Baseline suite **346 files / 8,659 passed / 11 skipped / 8,670 total**, the rule's own file **25 / 25**, gates **33 of 33**, `write-guard-set.mjs` PASS at 9 guards and 6 builders.

### 4. The rulings

A throw inside a callback counts for leg 3 and does not count for leg 1, and the two fields are separate because the two legs point opposite ways. Delegation to another `refuse[A-Z]` name is not emptiness, because every delegate is a name legs A and D already read; a call to itself is. `async` is covered, and covering it does not decide [ADR-459](../decisions/ADR-459.md) section 9 item 5, which stays unowned and is now given a trigger instead.

### 5. Verification

Watched firing by name on a seeded `refuseNothingAtAll` under the rule's real glob, with the two non-empty shapes silent in the same output; the seed was a new file, so the tracked tree was never modified and `git diff --stat -- packages/db` is empty after deleting it. The message was corrected before that run: its first draft claimed `write-guard-set.mjs` counts a guard anywhere in the glob, and `derive()` at `:259` defaults to `SCOPED_DB` and reads `scoped-db.ts` alone. Mutual recursion between two conforming names produced no output at all and is recorded as an open hole. After the last edit: gates **33 of 33**, invariants **35 of 35**, `write-guard-set.mjs` unchanged, suite **346 / 8,669 / 11 / 8,680**, typecheck, lint and `format:check` clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` **not run**.

### 6. Next

Four items are owed and named in [ADR-463](../decisions/ADR-463.md) section 9: the second stale "three rules" line at `repo-invariants.mjs:539`, the `"line 270"` pointer at `refusal-naming.js:152` that should read `:264` and is deliberately left until row 462 lands, the mutual-recursion hole with a cheap same-file partial priced beside it, and the `async` guard ruling that leg 3 now has a trigger for. The `E2` read is owed on the ruling rather than on the diff.
