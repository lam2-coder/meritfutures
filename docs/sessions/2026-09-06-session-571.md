---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 571: the census that counted a name suffix was already derived by the row that found it, and the one finding still outstanding was outstanding because no fence reached it

<!--index: **THE ROW IS [ADR-376](../decisions/ADR-376.md), `proposed` and UNSIGNED. MONEY-ADJACENT, ADR-003 STRICT, `E2` READ OWED.** [ADR-370](../decisions/ADR-370.md) read the worker's job register and left three things behind; this row was dispatched to act on all three and to say plainly which somebody had already fixed. **RULED: ONE WAS ALREADY REPAIRED BY THE ROW THAT FOUND IT, ONE WAS STILL OUTSTANDING IN THE REGISTER THAT ROW'S FENCE COULD NOT REACH, AND THE THIRD HAD TRAVELLED FURTHER THAN [ADR-371](../decisions/ADR-371.md) RECORDED.** **THE CENSUS RE-DERIVES AT ELEVEN AND NINE**, both read off the tree rather than carried, and **it was already DERIVED rather than counted by hand**: ADR-370 moved it into `test/schedule.test.ts` case `6.1`, which matches the `UNWIRED_` PREFIX and not the `_IO` suffix and asserts the two readings side by side. **A row arriving to make a census derived and finding it derived says so and stops**, and no figure is re-typed in this diff. **`CRON_INVENTORY`'s replay self-audit row still recorded the audit as having no `src/` file supplying its ports and it is repaired here**, in the shape the Statistics run row two rows down already used, with the retired half NAMED rather than re-quoted: **WHAT EXPIRED IS THE SUPPORT AND NOT THE BLOCKER** (ADR-371), so the marker, the dead-man switch, the **S1** and the `unscheduled` disposition all stay exactly where they were. **THE VOCABULARY COLLAPSE ADR-371 FOUND IN AN ADR BODY ALSO STANDS IN `schedule.ts`'s HEADER**, a few lines below the partition that draws the distinction and a few above the paragraph reserving the phrase for `ExpiryEventPort`, and it is corrected there under `RI-14`; **the harm runs in the direction that invents work**, since a port with a refusing default needs an adapter and a port with nothing reads as needing a declaration first. **THE EXPIRY CONSTRUCTOR IS STILL UNCALLABLE AND NEITHER REPAIR IS THIS ROW'S**: passing a sink is wiring, defaulting it means the refusing `ExpiryEventPort` that case `6.1` asserts the ABSENCE of, and it was already bound anyway by `expiry-adapter.test.ts` asserting `expirySweepIo.length` is 2. **[ADR-370](../decisions/ADR-370.md) SECTION 9'S NAMED HOLE IS REAL, NARROWER THAN THAT ENTRY STATES, AND ITS DIRECTION OF ERROR IS BACKWARDS FOR THE CASE THAT OPENS**: `constructorsOf` excludes only `UNWIRED_` names, so a refusing value under another name counts as a CONSTRUCTOR and its port reads as SERVED, measured in three seeds; **no census grammar was widened and no checker was opened for writing**. **ELEVEN SEEDS, TEN WATCHED RED AND ONE WATCHED GREEN, every one restored byte-identical under `sha256sum -c`** against an eight-file manifest re-verified after each restore. **A THIRD SITE CARRYING AN IMPRECISE REPLAY CLAIM IS REPORTED AND NOT REPAIRED** (`batch/adapter.ts:76` calls one of four blockers the ONLY one), **and it was in neither ADR-370 section 7's enumeration nor ADR-371 section 6's**. **THREE `docs/` CITATIONS INTO `schedule.ts` WERE ALREADY STALE ON THE BASE TREE**, nothing reddens on them, and this row's insertion moves them further and cannot reach them. **THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL**, the ninth consecutive session to record it. **NO `mcp__github__*` TOOLS, NO `gh` ON PATH, `api.github.com/repos/lam2-coder/meritfutures` ANSWERS 403 while `git push` AUTHENTICATES**; the branch is pushed and **no pull request was opened, by instruction**. **No job scheduled, no port wired, no disposition moved, no schedule parameter invented, no gate weakened, no test skipped, disabled or quarantined, no invariant minted, no `CI-06` letter spent, no migration taken or reserved, and `apps/api/**` was never opened for reading or writing.** Suite **335 files / 8,062 passed / 10 skipped / 0 failed** against a base reproduced on `c7987c3c` BEFORE the first edit of **335 / 8,060 / 10 / 0**, a delta of **+2 cases in ONE EXISTING FILE entirely by ADDITION with zero removals**, files and skips both unmoved, attributed BY REMOVAL; gates **33 of 33** after `generate`, invariants **35 of 35**, typecheck, lint and `format:check` clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `376`: verify each of [ADR-370](../decisions/ADR-370.md)'s three outstanding findings at primary source, repair what this fence holds, report what it does not, and rule on whether the expiry adapter's defect is this row's.

**One objective.** It resolved into two already-fixed findings, one real repair in a document, one real repair in a `src/` register, one refusal to repair, one measured correction to another entry's account of its own hole, and eleven seeds.

---

### 2. What was measured before the first edit

**THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL.** `/home/user` was empty, with no clone and no `node_modules`. The tree was attached to this session's authorized set and cloned by hand, and `HEAD` was confirmed to be the `c7987c3c` the row names rather than assumed.

**Base, on a clean tree, before anything was touched: 335 files / 8,060 passed / 10 skipped / 0 failed.** Gates **33 of 33**, invariants **35 of 35**, typecheck, lint and `format:check` clean.

**THE CENSUS, DERIVED BEFORE ANYTHING WAS CHOSEN AND BEFORE ANYTHING WAS EDITED:**

```
$ grep -rn "export const UNWIRED_[A-Z0-9_]*:" apps/worker/src --include=*.ts   ->  11
$ ... the same, restricted to names ending _IO                                 ->   9
```

**BOTH OF THE DISPATCH'S FIGURES REPRODUCE EXACTLY**, and both belong to other rows rather than being measurements this session inherited.

---

### 3. The three findings, dispositioned

| # | Finding | At primary source | Action |
|---|---|---|---|
| 1 | the census counts a name suffix | **ALREADY REPAIRED** by ADR-370, in the header AND in `test/schedule.test.ts` case `6.1` | reported; falsified in both directions; **no figure re-typed** |
| 2 | the replay row's support expired | **STILL OUTSTANDING** in `CRON_INVENTORY` and nowhere else | **REPAIRED.** Section 4 |
| 3 | the vocabulary collapse | **STILL OUTSTANDING** in `ADR-370.md`, and it had also reached `schedule.ts` | repaired in `src/`, reported in the ADR |
| 4 | the expiry adapter cannot be called | **STILL TRUE**, and already bound by `expiry-adapter.test.ts` | **NOT this row's to repair.** Section 5 |

**FINDING ONE ARRIVING FIXED IS THE RESULT AND NOT A NON-RESULT.** The row that found the defect landed the correction with it, so the number moved out of prose and into a case that reads it off the tree on every run. **What this row added is falsification the case had not had**, including seeding the historical defect back into the census helper and watching it RED.

---

### 4. What landed

| file | what |
|---|---|
| [`docs/ops/runbooks/CRON_INVENTORY.md`](../ops/runbooks/CRON_INVENTORY.md) | the **Replay self-audit** row's fifth cell only. The `supplies its ports` half recorded DISCHARGED since [ADR-346](../decisions/ADR-346.md), the sentence kept beside its correction under `RI-14` with the retired half NAMED, the four blockers restated where the retired half stood. **The job-name cell was not touched, so `CI-06l`'s `normJob` reads exactly what it read before**, and the file's line count is unchanged at 106 |
| [`apps/worker/src/schedule.ts`](../../apps/worker/src/schedule.ts) | **one header section, comment only.** The census re-derived and named as already derived, the collapsed clause corrected beside itself, the replay repair recorded, and the expiry constructor's status ruled not this row's |
| [`apps/worker/test/schedule.test.ts`](../../apps/worker/test/schedule.test.ts) | **section 7, two cases.** `7.1` holds the inventory repair fact-first and cannot be satisfied by moving a marker; `7.2` makes ADR-371 section 6 item 4 mechanical and goes RED on good news |
| [`docs/decisions/ADR-376.md`](../decisions/ADR-376.md) | **new.** The ruling, `proposed`, UNSIGNED |
| [`docs/decisions/ALLOCATION.md`](../decisions/ALLOCATION.md) | row `376` taken; `374` and `375` RESERVED so `CI-06f` stays gapless. Section 6 |
| `docs/decisions/README.md`, `docs/INDEX.md`, `docs/sessions/README.md`, `docs/STATE.md` | the registry rows and the generated spans |

**NO `src/` FILE HAD ITS BEHAVIOUR CHANGED.** `git diff origin/main -- apps/worker/src` is one comment block and nothing else. **`apps/api/**` was never opened**, for reading or for writing, and `git diff origin/main -- apps/api` is **EMPTY**.

---

### 5. The one repair this row refused

**`expirySweepIo` STILL TAKES THE SINK AS A REQUIRED POSITIONAL ARGUMENT WITH NO DEFAULT** ([`sweeps/expiry-adapter.ts:193`](../../apps/worker/src/sweeps/expiry-adapter.ts)), re-derived at its own declaration. **There are exactly two repairs and this row may make neither.**

- **Passing a sink is WIRING**, forbidden by the dispatch and by the register's own header.
- **Defaulting the sink means adding a refusing `ExpiryEventPort`**, which the expiry row argues against in its own words and which case `6.1` asserts the ABSENCE of. **A row that made it would be failing a case written to catch exactly it**, which is not an argument this row constructed: it is seed 8, watched RED.

**AND IT WAS ALREADY BOUND**, which no dispatch had noticed: [`apps/worker/test/expiry-adapter.test.ts:293`](../../apps/worker/test/expiry-adapter.test.ts) asserts `expirySweepIo.length` is **2**. No case was added, because a second pin means one edit reddens two files for one fact.

---

### 6. Falsification

**Eleven seeds. Ten watched RED, one watched GREEN, every one restored byte-identical under `sha256sum -c` against an eight-file manifest re-verified after each restore.** Six edited `src/`, two edited the inventory, one edited the test's own census helper. Four worth keeping here:

- **Seed 2 is the historical defect itself.** The census helper restricted back to the `_IO` suffix, watched RED at `6.1`. **The repair is falsifiable rather than merely present.**
- **Seed 9 is the degradation this row is about.** The live ingest row's `why` rewritten from the precise form into the collapsed one, which is exactly the edit ADR-371 measured a restatement making, watched RED at `7.2`.
- **Seed 8 runs in the direction somebody would call good news.** `ExpiryEventPort` gaining the convenience default reddens two cases, which is why section 5 refuses that repair rather than merely declining it.
- **SEEDS 10a AND 10c ARE A MEASUREMENT AND NOT A CONTROL**, and they correct [ADR-370](../decisions/ADR-370.md) section 9's account of its own hole. `10a` added a refusing default under another NAME for a port with no inhabitant and `6.1` went RED reporting that port as having a **constructor**, because `constructorsOf` excludes only names beginning `UNWIRED_`. **So the renamed default is not invisible to both censuses, and the error runs in the UNSAFE direction rather than the safe one that entry predicts.** `10c` declared the same value for a port that ALREADY has a constructor and the suite stayed entirely **GREEN**: that is the hole, and it is narrower than the entry gives it. **No grammar was widened and no checker was opened for writing.**

---

### 7. The fence, reported rather than quiet

**ROWS `374` AND `375` ARE RESERVED.** `CI-06f` is gapless over allocated PLUS reserved, `373` is the highest on the merged head, and `376` landing alone opens two holes and turns that gate red with nothing wrong. **The cells name no subject and claim nothing**, because this row was told two siblings are running now and told not to guess at them; a sibling taking one replaces the cell whole. **THIS IS THE FIFTH CONSECUTIVE WAVE DISPATCHED WITHOUT A RESERVATION BLOCK.**

**NO OTHER FENCE WAS WIDENED AND THE CITATION-REPAIR RIGHT WAS NEVER EXERCISED.** The dispatch granted line-number repairs across `apps/**` for citations this diff breaks. **This diff breaks none inside `apps/**`**: the three citations it moves are all under `docs/`, which the grant does not reach, and all three were already stale before it. Section 8.

---

### 8. Blockers and landmines

- **OWED AND OUTSIDE THIS FENCE: [`ADR-370.md`](../decisions/ADR-370.md) section 3.1's two cells.** ADR-371 section 6 item 4 named them; they are still uncorrected and this row confirms it. **The remedy is the precise form both job rows and `CRON_INVENTORY` already use.**
- **OWED AND REPORTED FOR THE FIRST TIME: [`apps/worker/src/batch/adapter.ts:76`](../../apps/worker/src/batch/adapter.ts) calls one of four blockers the ONLY thing keeping the replay audit off a clock**, and the read it names in that same sentence is itself the third of the four, declared owed at `:802` in that same file. **It was in NEITHER ADR-370 section 7's enumeration NOR ADR-371 section 6's**, which is [ADR-367](../decisions/ADR-367.md)'s shape recurring: a collection of owed items that is itself incomplete. Not repaired: this row's diff does not break it and repairing it is a second objective.
- **LANDMINE: three `docs/` citations into `schedule.ts` were ALREADY STALE ON THE BASE TREE and nothing reddens on them.** [ADR-353](../decisions/ADR-353.md) line 24 cites `schedule.ts:309` for a registry row and that line was **BLANK** before this diff; [ADR-355](../decisions/ADR-355.md) line 61 and [ADR-361](../decisions/ADR-361.md) line 124 both land in unrelated text. **This row's insertion moves all three further and cannot repair any of them**, and `already broken` is a reason rather than a licence.
- **LANDMINE: the header's correction is prose beside prose and is held by nothing.** `7.2` asserts the ROWS and the tree, deliberately, because a row is what a reader meets and `RI-14` requires the header's clause to stay. **The fact cannot drift; the header's account of it can.**
- **LANDMINE: `apps/worker/src/index.ts:450` was checked and needs nothing.** ADR-370 section 7 item 2 named it for the row repairing the inventory. Its two claims are both true on this tree. **The barrel was not opened for writing and no barrel-leg count was re-derived, because none needed to be.**
- **Concurrency: two sibling rows are running now**, one owning `apps/api/src/start.ts` and one owning `apps/api/test/wiring.test.ts`. **Neither is owed anything by this row** and neither file was opened. A registry conflict at merge is expected and is the designed workflow.

---

### 9. Verification, derived at reporting time

| runner | result |
|---|---|
| `pnpm vitest run` | **335 files / 8,062 passed / 10 skipped / 0 failed** |
| baseline on `c7987c3c`, before the first edit | **335 files / 8,060 passed / 10 skipped / 0 failed** |
| delta | **+2 cases in ONE EXISTING FILE, entirely by ADDITION with zero removals**, files unmoved at 335 and skips unmoved at 10. Attributed **by removal**: `schedule.test.ts` runs **16** alone on this tree and **14** alone on the base |
| `node scripts/corpus/gates.mjs generate` then `check` | **33 of 33** |
| `node packages/tooling/checks/repo-invariants.mjs` | **35 of 35**, no number minted |
| `pnpm run typecheck` | clean |
| `pnpm run lint` | clean |
| `pnpm run format:check` | clean |
| `git diff --stat origin/main -- packages/db/migrations scripts/db` | **EMPTY** |
| `git diff origin/main -- apps/api` | **EMPTY** |

**`CI-06l` WAS CHECKED BECAUSE `CRON_INVENTORY` IS IN THIS FENCE.** The repair touches the Replay self-audit row's fifth cell only; the job-name cell is untouched, so `normJob` reads exactly what it read before, and no coverage or exemption row moved. The gate passes and the file's line count is unchanged.

**`pnpm run verify` was NOT run and `scripts/corpus/falsify.mjs` was NOT run**, both forbidden by the row and both mutate the working tree.

---

### 10. This session's own tooling, measured rather than assumed

```
$ command -v gh                                            -> not found (exit 1)
$ curl -s -o /dev/null -w '%{http_code}' \
    https://api.github.com/repos/lam2-coder/meritfutures   -> 403
```

**No `mcp__github__*` tool exists in this session's surface**, checked by search rather than by inspection of a list. `git push` authenticates through the session's git proxy and did so on every commit. **This matches sessions 554, 558, 562, 564, 565 and 568 exactly.**

**THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL**, the ninth consecutive session to record it. **Every Bash command was run bare from the working directory**, per the row's operational note, and **no permission stall occurred**, which is the tenth consecutive row to report that.

**THE BRANCH IS `claude/the-census-that-counts-a-suffix`, PUSHED. NO PULL REQUEST WAS OPENED AND NONE WAS MERGED, BY INSTRUCTION.**

---

### 11. Next

The founder's `E2` read of [ADR-376](../decisions/ADR-376.md) and the three decisions its approval block names, of which **the first is the one with a cost already paid**: two of three findings arrived already fixed, and the one that did not was outstanding through two rows that both found it and neither of which could reach it. **That is a fence problem rather than a diligence problem.** Then [ADR-370](../decisions/ADR-370.md)'s own second approval question, whether the census should be a gate rather than a case, on which section 8 of this row's entry is new evidence that cuts both ways.
