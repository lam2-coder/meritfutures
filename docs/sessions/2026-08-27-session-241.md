---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 241: two register rows that were both already true, and one hole neither guard sees

<!--index: **BOTH ROWS WERE STALE RATHER THAN OPEN, AND THE SECOND ONE NOBODY HAD NOTICED.** `OI-14` is satisfied and `OI-30` was repaired on 2026-08-24 by [session 151](2026-08-24-session-151.md), three days before the dispatch called it open, so the blind spot [ADR-117](../decisions/ADR-117.md) names in its own register has now produced TWO instances in one wave. **The dispatch's premise about `OI-14` is refuted at source**: [#315](https://github.com/lam2-coder/meritfutures/pull/315) did not move `OI-14`'s refusal, it added a second one for the EMPTY BOOK, and [ADR-123](../decisions/ADR-123.md) section 4 says so itself. **Both guards were falsified independently** and each case pinned to its own message. **AND THERE IS A THIRD SCALE NEITHER GUARD SEES, EXECUTED**: an account named by `accountsWithStoredState()` with no rows and no days returns `{accountsAudited: 1, storedRows: 0, inScope: 0, diverged: 0}`, which is `FM-17` whole. It is `ADR-123` section 7's rejected alternative, whose stated reason is true of one half of that input class and false of the other. **Pinned as a hole, not fixed**: closing it reverses an argued rejection on a money-path-by-position file. All three `0015` citations re-derived from `0015`, `0037`, `0046` and the two live predicates rather than taken on session 151's word, and none is a case where the test depends on the retired property. **`ADR-129` NOT USED, condition did not fire, number returns to the pool** -->

- **Objective:** close `OI-14` on an EXECUTED refusal rather than on a diff, and establish for each of `OI-30`'s three citations what the test asserts, what retired the constraint it names, and what holds the property now.
- **Done.** Both, and neither the way the dispatch expected. **`OI-14` is closed and it was never `#315` that satisfied it.** **`OI-30` was already repaired three days before it was dispatched as open.** **No ADR:** the `ADR-129` condition did not fire and the number is returned. **One new finding, executed:** a path into `runReplayAudit` that returns a clean report over nothing, pinned in the suite and reported rather than fixed.
- **The count:** `pnpm run typecheck` exit 0; `node packages/tooling/checks/repo-invariants.mjs` 10 of 10; `node scripts/corpus/gates.mjs check` 32 of 32; `falsify.mjs` clean; `pnpm run verify` exit 0; `pnpm vitest run` 150 files / 2,658 passed / 1 skipped, against 2,656 on `main` and the two new cases are the difference.
- **Next:** the third-scale hole needs an entry that re-rules [ADR-123](../decisions/ADR-123.md) section 7's fourth alternative. `RE-P-02` and `RE-P-18` are named by `0037` and `0046` as what holds what those supersessions stopped asserting, and neither exists.
- **Blockers:** none. **Not merged.**
- **Landmines:**

  - **A REGISTER ROW SAYING `Open` IS NOT EVIDENCE THAT ANYTHING IS OPEN, AND THAT IS NOW TWO FOR TWO IN ONE WAVE.** Both rows this session was dispatched to work were already true in the tree. `OI-30`'s repair is a merged commit from 2026-08-24 with a commit message that names all three sites. **Nothing reconciles a register row's prose against the code it points at**, which is exactly the finding [ADR-117](../decisions/ADR-117.md) records against its own register, and the cost is a session dispatched at work that was done.

  - **THE DISPATCH'S SUMMARY OF `#315` IS WRONG AND THE ENTRY IT CITES SAYS SO.** The brief reads *"`OI-14` reads 'the replay job must refuse an empty in-scope set' and #315 DID THAT"*. #315 added the `accountsAudited === 0` guard, which is `ADR-073` section 5's empty book. `OI-14`'s guard is `storedRows > 0 && inScope === 0`, it predates #315, and [ADR-123](../decisions/ADR-123.md) section 4 states in its own words that the entry *"does not close `OI-14`'s row"*. **Taking the summary as evidence would have closed the row citing the wrong test.**

  - **`ADR-123` SECTION 7 PRICED ONE HALF OF A TWO-HALF INPUT AND THE OTHER HALF IS A HOLE.** Its rejection of `storedRows === 0` reads *"a port that returns an account id and no rows for it. On that input `accountsAudited` is 1 and the audit has something to say about the account."* With days present that is TRUE and executed here. With days absent the audit has nothing to say and says so as `diverged: 0`. **The entry is not wrong about the case it considered; it did not consider the other one.**

  - **A TEST THAT PINS A DEFECT IS ONE `git blame` AWAY FROM READING LIKE A PROMISE.** The new `PINS A HOLE` case asserts today's wrong behaviour on purpose, and its comment says the day it goes red is the day the hole closed and that the fix should DELETE it rather than satisfy it. This is `ADR-123` finding 4's shape deliberately inverted, and it is the reason the words are in the test rather than only here.

- **Files touched:** [`apps/worker/test/replay.test.ts`](../../apps/worker/test/replay.test.ts), [`DELTA_MANIFEST`](../../packages/db/DELTA_MANIFEST.md) (the `OI-14` row), [ALLOCATION](../decisions/ALLOCATION.md) (the `OI-14` row, the `OI-30` row, the `129` row), [STATE](../STATE.md) (one appended section), [README](README.md), this file. **No migration, no ADR, no application source, no gate.**

---

### 1. `OI-14`, verified by execution and closed on a different citation than the one dispatched

**The row is [`DELTA_MANIFEST`](../../packages/db/DELTA_MANIFEST.md)'s and it reads *"the replay job must refuse an empty in-scope set"*.** Its own fuller prose names the trigger: rows exist, every one reads out of scope, the audit compares nothing.

| # | Claim | How it was established |
|---|---|---|
| 1 | **`OI-14`'s guard is `report.storedRows > 0 && report.inScope === 0` and it throws `ReplayAuditRefusal`** | Read at [`replay.ts`](../../apps/worker/src/batch/replay.ts), and it is the row's predicate transcribed |
| 2 | **It fires, on a real fold rather than a stub** | `it('REFUSES a run that compared nothing while rows exist (OI-14)')` passes. Four stored rows written by `foldAccountDay`, every one stamped `engine-older` |
| 3 | **The case is pinned to THAT guard and not to the class** | **Falsified.** With `storedRows > 0 && inScope === 0` neutered and nothing else touched: `Tests 1 failed`, `1 passed`, the failure being this case. With `accountsAudited === 0` neutered instead: the two swap. **Both directions run** |
| 4 | **`#315` did not satisfy this row** | [ADR-123](../decisions/ADR-123.md) finding 1 records `OI-14`'s guard as pre-existing and section 4 says the entry *"does not close `OI-14`'s row"*. The guard's commit is not #315's |
| 5 | **The message is now asserted and not only the class** | Three guards throw `ReplayAuditRefusal`. The case asserted the class alone, which is the weakness `ADR-123` names one case below it and did not apply to this one |

**So the row closes, and the citation is `replay.test.ts`'s `OI-14` case rather than the empty-book case the dispatch pointed at.** The two are adjacent in the file and they are not the same assertion.

### 2. THE FINDING: a path into `runReplayAudit` that returns rather than rejects

**Executed, through the demo's own `ts-resolve` hook so the code path is a runner's:**

```
accountsWithStoredState() -> ['account-a']
storedRuleStates()        -> []
accountDaysFrom()         -> []

RETURNED: {"accountsAudited":1,"storedRows":0,"inScope":0,"matched":0,"diverged":0}
```

**Neither guard sees it.** `accountsAudited` is 1, so `ADR-073` section 5's guard is silent. `storedRows` is 0, so `OI-14`'s conjunct is false and its guard is silent. **The audit compared nothing and reported clean**, which is `FM-17` stated at the widest: an audit that has stopped looking reports exactly like one that found nothing.

**IT IS NOT `OI-14`'s AND THIS SESSION DOES NOT CLOSE IT AS ONE.** That row is about an empty in-scope set while rows exist; here there are no rows. This is the empty-book failure at a THIRD scale, one account wide.

**The negative half was executed too, and it vindicates `ADR-123`'s argument on the input the entry actually considered.** Same port with `accountDaysFrom` returning a real four-day history: `inScope` 4, `diverged` 4, four findings handed to the port. **A `storedRows === 0` refusal would have thrown over a book whose real finding is four missing rows**, which is precisely why section 7 rejected it. **The rejection is sound and its stated reason covers one of the two shapes that input takes.**

**Not fixed here, and the reason is the fence rather than the difficulty.** Closing it means reversing an alternative [ADR-123](../decisions/ADR-123.md) considered, argued and rejected in writing, in a function that is `INV-04`'s right-hand side. That is a ruling. `ADR-129` was reserved against a different condition and taking it for this would be minting a number the reservation does not cover.

**It is pinned rather than only described.** `it('PINS A HOLE: an account named with no rows AND no days returns a clean report')` asserts the defective report field by field, and says in its own comment that it records a hole rather than a promise and that the fix should delete it.

**What lowers the likelihood without changing the shape:** a port behaving this way is breaking `accountsWithStoredState`'s own contract, which promises accounts WITH stored state. So it is a lying adapter or a race between two queries rather than a routine input. **`OI-14`'s row answers that same objection about itself**: no per-row constraint can see its case either, and the answer was that it belongs to the job.

### 3. `OI-30`: three citations, all three already repaired, and all three re-derived rather than believed

**The row was repaired on 2026-08-24 by [session 151](2026-08-24-session-151.md), commit `35a7847`, *"retense the three stale `0015` citations in `replay.test.ts`"*.** The dispatch, three days later, describes them as live. **Found by `git log -S` on the file rather than by reading the row.**

**Session 151's word was not taken for any of it.** Each citation was re-derived from the migration text and the live predicate:

| Site | What the test is actually asserting | What retired the name | What holds the property now | Verdict |
|---|---|---|---|---|
| **`:875`** | The 250-day stream is capped at 219,000c of profit so `R-15`'s lock never fires | [`0037`](../../packages/db/migrations/0037_supersede_rule_states_high_water_bounds_balance.sql)`:124` drops `rule_states_high_water_bounds_balance`; `:143` adds `rule_states_high_water_bounds_balance_unlocked`, `floor_locked OR high_water_balance_cents >= balance_cents` | **The file's own two assertions**: `floorLocked` empty on all 250 rows and `hwb >= balance` on all 250. The comment says exactly that | **Clean repoint.** The test never leaves the live constraint's BINDING branch, so it does not depend on the retired unconditional form |
| **`:1005`** | A settlement fact whose basis day is the previous session and whose effective day is this one | [`0046`](../../packages/db/migrations/0046_supersede_rule_states_consistency_period_started.sql)`:104` drops `rule_states_consistency_period_started`; `:130` adds `rule_states_consistency_period_after_anchor`, start `>` `payout_anchor_day` | Nothing in the schema, by design. `0046`'s own column comment: *"NOTHING here bounds this column against `trading_day`"* | **Clean repoint.** The file asserts nothing about `consistency_period_start_day` against `trading_day`, so the property the supersession dropped is not one this test rests on |
| **`:1245`** | `floor_locked` false on all 250, so `hwb >= balance` is the whole assertion | `0037`, as above | The same two assertions | **Clean repoint**, and it was the mildest of the three from the start |

**The conditional `ADR-129` was reserved for did NOT fire.** It reads: a constraint retired for a reason the test still depends on. **None of the three is that.** Both supersessions removed a bound the test stays clear of, in one case by asserting the opposite condition on every row and in the other by asserting nothing about the columns at all.

**Reported, and it is not this row's to close.** `0037:93` names *"the fixture layer, `GS-016` and `RE-P-02`"* as what holds the post-lock behaviour it stopped asserting; `0046:89` and [ADR-079](../decisions/ADR-079.md)`:73` name `GS-068` and `RE-P-18`. **Both fixtures exist. Neither property test does**: `grep -o 'RE-P-[0-9]*' packages/rules-engine/test/` returns `RE-P-01`, `RE-P-05`, `RE-P-13` and `RE-P-15` and nothing else. **`RE-P-02`'s absence is on record and blocked on [ADR-056](../decisions/ADR-056.md)'s signature** ([STATE](../STATE.md)). **`RE-P-18`'s absence is recorded nowhere found here**, and two documents state it as holding a shape. `PT-02` in [`win-days-monotonicity.property.test.ts`](../../packages/rules-engine/test/win-days-monotonicity.property.test.ts) asserts `R-47`'s reset-to-zero formulation; `RE-P-18` is the EQUIVALENCE of that formulation with the count-strictly-after-the-anchor one, and the equivalence is not asserted.

### 4. Why no ADR, stated so the returned number reads as the mechanism rather than as an omission

**`ADR-129` was `RESERVED BEFORE DISPATCH, CONDITIONAL`.** The condition was `OI-30`'s: a citation naming a constraint retired for a reason the test still depends on. **Section 3 establishes that none of the three is that case**, and the [ALLOCATION](../decisions/ALLOCATION.md) row is amended in place to say so, per [ADR-065](../decisions/ADR-065.md) T3.

**The one thing in this session that DOES need a ruling is not `ADR-129`'s.** Section 2's hole is a reversal of [ADR-123](../decisions/ADR-123.md) section 7, on a money-path-by-position file, and it belongs to a session dispatched for it with the `E2` read claimed. **Taking a number reserved for a different question in order to have somewhere to put a finding is how a registry stops meaning anything.**

### 5. The fence, and what it was not widened to

**Held exactly:** [`apps/worker/test/replay.test.ts`](../../apps/worker/test/replay.test.ts), the `OI-14` and `OI-30` register rows, the `129` row, [STATE](../STATE.md), [README](README.md), [INDEX](../INDEX.md)'s generated span, this file.

**`apps/worker/src/` was granted conditionally, on `OI-14` turning out to be incomplete. It is complete on its row's own words and the directory is UNEDITED.** `git status --porcelain apps/worker/src packages/db/migrations` reports zero on this branch, and the two guard-neutering falsifications in section 1 were performed against a copy restored immediately after, with a clean `git status` between each.

**Session 240 holds every migration number in this wave and no number was taken here.** `OI-30` needed no schema; the finding in section 2 needs no schema either, only a guard and an entry.
