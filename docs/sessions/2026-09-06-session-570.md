---
status: approved
depends_on: [README.md]
last_updated: 2026-09-06
---

## 2026-09-06 - Session 570: everything the wiring test owed, where `wired` becomes the set a RUN installs, the support that expired is repaired on a narrower ground than it named, and the one registration a prior row asked for cannot land until a sentence outside this fence is repaired

<!--index: **THE ROW IS [ADR-375](../decisions/ADR-375.md), `proposed` and UNSIGNED. CONTROL OVER THE MONEY PATH'S HONESTY, `E2` READ OWED.** Four consecutive waves fenced [`apps/api/test/wiring.test.ts`](../../apps/api/test/wiring.test.ts) so it had a single owner and each reported what it found instead of repairing it; **the fence turned findings into a QUEUE rather than losing them** and this row drains it. **THE OWED SET WAS ENUMERATED AT PRIMARY SOURCE RATHER THAN ACCEPTED**: ten items over [ADR-369](../decisions/ADR-369.md), [ADR-371](../decisions/ADR-371.md) and [ADR-372](../decisions/ADR-372.md), of which one is two items counted twice under different headings, one was already discharged as a control by the row that recorded it, and one cannot be done at all. **RULED: THE STANDING QUESTION IS ANSWERED YES.** `wired` is derived from the SYNTAX TREE and is now the set of ports a run of `start.ts` would install before the process begins serving. The file still never imports that module, because importing it binds a port; **parsing is not executing**, which is ADR-372's measurement and not a new claim. **FOUR OF THE SIX OWED ITEMS ARE RETIRED OUTRIGHT AND NOT MITIGATED**: a parser refuses a BLOCK comment, a template literal, a nested call and a call below `main()` **without being taught any of them**, because three of the four are not statements and the fourth is a position. **THE PATTERN IS KEPT AND STILL RUN**, so the two derivations are bound to agree and `start-program.test.ts` can still read `CALLS` out of this file. **THE FIFTH FIXTURE IS AN UNINDENTED NESTED CALL**, which is the FORMATTER reliance ADR-372 could name and could not exercise: `prettier` would never write it, so no run of `format:check` can hold the answer up. **`wired` IS STILL A `Set` AND THAT IS STILL LOSSY**, so the install LIST is kept beside it and a case reads it: a port installed twice is now a red bar rather than a silent choice about which backend serves. **THIS TAKES A DECISION ADR-372 SECTION 15 RECORDED AS OPEN AND THE ENTRY SAYS SO**: option (b) shipped with option (a)'s agreement bar beside it, and [ADR-367](../decisions/ADR-367.md) section 5's reason was tested against it and does NOT apply, because nothing outside this file changes meaning. **THE EXPIRED SUPPORT IS REPAIRED AND THE VERDICT DOES NOT MOVE** (ADR-371 section 6 item 1): the claim was FALSE IN HALF, the per-IP dimension is built and installed, the per-ASN dimension is absent, and the ground survives on `RATE_LIMITED_ROUTES`, which is two PUBLIC certificate rows and has never reached `/admin/*`. **THE RETIRED CLAUSE IS NAMED AND NEVER REPRODUCED**, because `start.ts` keeps it beside its correction in three places and a fifth copy here would be ADR-367's measured collision one register down. **AND THE REPAIR MOVES THE SENTENCE FROM `RI-15`'s THREE WEAK LEGS INTO ITS STRONG ONE**: both new citations carry a backticked NAME, both were seeded past `CITATION_WINDOW` and both turned `RI-15` red at 34 of 35. **THE REMEDY FOR A CITATION WHOSE SUBJECT IS PROSE IS TO CITE A NAME, NOT TO WIDEN THE CHECK.** **THE THREE `operator` OMISSIONS [ADR-369](../decisions/ADR-369.md) LEFT ARE REPAIRED AND THE FOUR ENTRIES NOW REST ON ONE DERIVED FACT**: `ApiDb` declares five doors and NONE yields a `SystemTx`, sliced out of the interface rather than grepped over `db.ts`, which argues in its own comments that the door does not exist. **THE CASE FAILS ON GOOD NEWS**, naming all four the day that door opens. `useAdminPayoutBackend` is the sharpest of the three: it enumerated `AdminPayoutTx`'s four methods and called itself the closest of the five to wireable while never naming the member that opens the transaction those four run inside. **`now` STAYS IN THE PINNED GAP ON THE WALLET ENTRY'S OWN PRECEDENT**, because a clock needs no door. **THE `RI-35` REGISTRATION ADR-371 ASKED FOR CANNOT LAND, AND THE REASON IS ORDER RATHER THAN DIFFICULTY.** `postgresBatchPorts` serves both replay-audit reads (`apps/worker/src/batch/adapter.ts:870` and `:887`), verified at primary source rather than inherited, so the artifact is PRESENT: a `live` registration turns leg 2 red at a sentence in `CRON_INVENTORY.md`, which this wave fences to a sibling, and a `retired` one would use the register to CONCEAL a live false sentence. **BOTH HALVES OF THAT SENTENCE ARE ON ONE LINE**, and leg 1 anchors to a verbatim substring of a single line, so any binding written today breaks on the owed repair. **A `docs/` claim site would also be the register's FIRST**, which is a change to what `RI-35` is for and exactly the widening ADR-371 declined. **`absence-claims.mjs` WAS NOT OPENED FOR WRITING, no `RI-nn` was minted and no invariant was extended.** **FIFTY CITATIONS POINT INTO THIS FILE ACROSS 183 SITES IN 74 FILES, THIS ROW'S DIFF MOVED EVERY ONE OF THEM, AND `RI-15` AND `RI-16` STAYED GREEN AT 35 OF 35**, because not one of the fifty carries a name they can anchor to. **That was measured BEFORE the first edit**, by inserting thirty lines into the untouched file and running the invariants. Three of the 183 sites are outside `docs/`; **one is this file citing itself and it is repaired**, and the other two are in `packages/tooling`, outside this fence and now wrong. **FOURTEEN FALSIFICATION SEEDS, TWELVE RED AND TWO GREEN, AND BOTH GREENS CHANGED THE WORK.** Seed 7 reverted `wired` to the line pattern and **every case in the file passed**, because the agreement case then compares the text derivation with itself; the bind is structural now, anchored at the start of a line on ADR-371 section 7's own lesson about `toContain`. Seed 8 was a bad seed, editing both sides of an equality, and is red when aimed properly. **ONE RESTORE WAS DONE WRONG AND IT IS THE SAME MISTAKE IN THE THIRD CONSECUTIVE WAVE**: `git checkout --` on a file whose repair was still UNCOMMITTED reverted the repair with the seed, `sha256sum -c` caught it at once, and every later seed ran against a committed tree. **Five seeds edited `src/` and every one was restored byte-identical.** **NO `src/` FILE CHANGED, no port wired, no door opened, no triple number adjusted, no gate weakened, no test skipped, disabled or quarantined, no invariant minted, no `CI-06` letter spent, no migration number taken or reserved, no `status` field moved.** **`374` IS RESERVED so `CI-06f` stays gapless**, naming no subject, and the gate was RUN with `375` alone to measure it rather than assumed, at **32 of 33**. **THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL**, the ninth consecutive session to record it, and the clone landed exactly on the `c7987c3c` the row names. **NO `mcp__github__*` TOOLS, NO `gh` ON PATH, `api.github.com/repos/lam2-coder/meritfutures` ANSWERS 403 while `git push` AUTHENTICATES**; the branch is pushed and **no pull request was opened, by instruction**. Suite **335 files / 8,064 passed / 10 skipped / 0 failed** against a base reproduced on `c7987c3c` BEFORE the first edit of **335 / 8,060 / 10 / 0**, a delta of **+4 cases in ONE EXISTING FILE entirely by ADDITION with zero removals**, files and skips unmoved, attributed BY REMOVAL: `wiring.test.ts` runs **27** alone here and **23** alone on the base. Gates **33 of 33** after `generate`, invariants **35 of 35**, typecheck, lint and `format:check` clean. `pnpm run verify` and `scripts/corpus/falsify.mjs` **NOT run**, both forbidden by the row. -->

---

### 1. The objective

Row `375`: enumerate everything [`apps/api/test/wiring.test.ts`](../../apps/api/test/wiring.test.ts) owes across four waves of collection rows, verify every item at primary source rather than accepting the dispatch's list, answer for the fourth time whether the remaining items can be DERIVED rather than repaired, and, if it can be done without importing the module the file reads as text, make its `wired` set SOUND rather than accidentally correct.

**One objective.** It resolved into ten enumerated items, four repairs, four new cases, one refusal with its reason measured, and fourteen seeds.

---

### 2. What was measured before the first edit

**THE CONTAINER ARRIVED WITH NO REPOSITORY AT ALL.** `/home/user` was empty, with no clone and no `node_modules`. This is the ninth consecutive session to record it. The tree was attached to this session's authorized set and cloned by hand, and `HEAD` was confirmed to be the `c7987c3c` the row names rather than assumed.

**Base, on a clean tree, before anything was touched: 335 files / 8,060 passed / 10 skipped / 0 failed.** Gates **33 of 33**, invariants **35 of 35**, typecheck, lint and `format:check` clean.

**AND ONE MORE THING WAS MEASURED BEFORE THE FIRST EDIT, BECAUSE THE ROW'S OWN FENCE PREDICTED A COLLISION.** `RI-15` and `RI-16` bind citations to line numbers and this row was going to shift lines in a heavily cited file. Thirty lines were inserted into the untouched file and the invariants run: **35 of 35**. The probe was restored and verified with `sha256sum -c`. **Nothing in this repository can see a citation into this file drift**, which is what made the change affordable and what section 8 of [ADR-375](../decisions/ADR-375.md) reports as a finding in its own right.

---

### 3. The owed set

Ten items, each opened at the file and line it names. The table is [ADR-375](../decisions/ADR-375.md) section 1. Two of them are not what they look like: item 10 is items 1, 3, 4 and 6 counted a second time under a different heading, and item 5 was owed as a control and had already been discharged by the row that recorded it.

---

### 4. What shipped

| commit | what |
|---|---|
| `wired` from the syntax tree | the derivation, the duplicate case, the agreement case with five named shapes |
| the structural pin | found by a seed that went green: nothing held that `wired` USES the derivation |
| the expired support | ADR-371 section 6 item 1, repaired with the verdict unmoved |
| the three `operator` omissions | ADR-369's owed repair, plus one case binding the shut door for all four entries |
| the self-citation | the one citation into this file that this row's own diff broke and could reach |

---

### 5. What was NOT done, and why

**THE `RI-35` REGISTRATION.** [ADR-375](../decisions/ADR-375.md) section 7. The artifact is present, the register has no disposition that fits, the repair it is downstream of belongs to another row this wave, and both halves of the sentence sit on one line so any binding breaks on that repair. **`packages/tooling/checks/absence-claims.mjs` was not opened for writing.**

**`apps/api/src/start.ts` WAS NOT EDITED**, nor was any other `src/` file. The imprecision [ADR-372](../decisions/ADR-372.md) section 9 reported at `start.ts:311` is still there and is still owed; what has changed is that the append it invites is now caught by `wiring.test.ts` itself and not only by its neighbour.

---

### 6. Verification

Every figure in the index block above was derived at reporting time off each runner's own last line. `pnpm run verify` and `scripts/corpus/falsify.mjs` were **NOT** run, both forbidden by the row for mutating the working tree.
