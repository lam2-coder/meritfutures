---
status: approved
depends_on: [README.md]
last_updated: 2026-08-24
---

## 2026-08-24 - Session 157: M03 measured, and its schema is the only complete half of it
<!--index: **M03 measured against the tree. Its SIX schema deltas ALL LANDED in [`0006_commerce.sql`](../../packages/db/migrations/0006_commerce.sql) and its application code is EMPTY: zero `PspAdapter`, zero routes, zero `coupon`, zero `refund`, zero `mid_health` in any `.ts` in the repository.** So M03 is not [session 143](2026-08-23-session-143.md)'s "schema only" uniformly, and **three specified enforcements are absent rather than unbuilt**. `INV-M3-01` says the purchase's `plan_version_id` is write-once BY TRIGGER; **the tree holds ten triggers and the pin one is on `accounts`, not on `purchases`**. `INV-M3-16`'s `marketed_size_label` **exists at 20 TypeScript sites in [`apps/site`](../../apps/site) and in ZERO SQL**, and [DELTA_MANIFEST:156](../../packages/db/DELTA_MANIFEST.md) rows `SD-M9-04` against migration `0044`, which [ALLOCATION:136](../decisions/ALLOCATION.md) records as **SPENT UNDER A DIFFERENT NAME**. **Both of M03's events marked NEW are registered nowhere and one has two spellings**: [EVENTS section 4](../architecture/EVENTS.md) carries nine M3 rows and neither `mid.health_changed` nor `purchase.first_trade_recorded`, while [M10:165](../plans/M10-integrations.md) writes `psp.mid_health_changed`. **`ScopedDb` is READ-ONLY** (`rows(key)` and nothing else) and `INV-M3-07`, `INV-M3-12` and `INV-M3-13` each require a transaction, so all three are inexpressible. **Seven tables of 111 are in [`schema.ts`](../../packages/db/src/schema.ts) and none is M03's**, which is the cross-module collision rather than an M03 gap. **`CI-06o` CANNOT DISCOVER CHECKOUT AS A MONEY PATH**: `MONEY_PATH_WORDS` is `rules-engine, payout, ledger, auth`. **All 18 golden scenarios M3 touches are `blocked / no-fixture-format`, one cause.** Ten slices, six collision files. **Session 157 HAS NO CLAIM ROW** and the dispatching brief says it does. Nothing built, nothing ruled, nothing signed -->

- **Objective:** measure [M03 billing and checkout](../plans/M03-billing-checkout.md) against the tree and produce an inventory. No document, no ruling, no code.

- **Done:** M03's **16 invariants, 6 schema deltas, 12 failure modes, 6 adversarial scenarios, 6 endpoint rows, 10 emitted events, 4 open questions and 7 dependencies** measured one at a time against the tree by command. Every count in this log was produced by a command run here. Sections 1 to 6 below are the deliverable. **29 of 29 gates, 7 of 7 invariants, `pnpm run verify` green at 104 files / 1,512 passed / 1 skipped, `falsify` 62 scope and 10 loader cases**, all measured on this branch and all unmoved by it, because this session writes no code.

- **Next:** four items are the founder's rather than a session's, and they are section 6. The ten slices in section 4 are dispatchable in the order section 5's collision graph permits, and **none of them may be dispatched until `packages/db/src/schema.ts` has an owner**, because thirteen sibling module measurements are running against the same two files.

- **Blockers:** none for this session. For M03 itself: the accessor cannot write, no HTTP framework is installed, and every one of M03's 18 golden rows is blocked on one corpus-wide cause.

---

### 1. What M03 specifies

[M03](../plans/M03-billing-checkout.md) is `approved`, `last_updated: 2026-08-20`, 510 lines, and it is a **money path under the [ADR-003](../decisions/ADR-003.md) strict regime** by its own seventh line. Section by section:

| Section | What it commits to |
|---|---|
| **1.1 What it is** | `POST /checkout`, `POST /accounts/:id/reset`, `POST /webhooks/psp/:provider`, the coupon claim, the plan-version publish path, the chargeback handler. One traceable saga with **compensation at every step** |
| **1.2 What it is not** | Six boundaries. It never reads a rule, never runs a KYC check, never talks to Rithmic, never pays anybody. It enforces **hard limits only**, and `restricted` is ruled onto the hard-limit side because it is *"a recorded enforcement decision a human already made"* rather than a score |
| **1.3 Invariants** | **16**, `INV-M3-01` to `INV-M3-16`. Section 2 below measures each one |
| **2 Schema deltas** | **Six**, `SD-M3-01` to `SD-M3-06`, and the document says *"Six deltas"* after [ADR-034](../decisions/ADR-034.md)'s hand-maintained-count defect was found in this very line |
| **2.1 The PSP abstraction** | A five-method `PspAdapter` interface, with three rules each traced to a documented failure: **`verifyWebhook` throws rather than returning a boolean**, **nothing in the interface returns a decision**, **no adapter method takes a price** |
| **3.1 Saga** | Six break-and-compensation rows with an alarm and an owner each. **The account row is created BEFORE provisioning confirms**, deliberately, so paid-not-provisioned is a queryable state rather than an absence |
| **3.2 MID failover** | A four-state machine on `mid_health`. **Failover is per-attempt routing, never mid-transaction**, which the document calls *"the single most important sentence in this section"* |
| **3.3 Plan version publish** | `copy_blocks` keyed by rule path; the publish diff shows `PW-01` to `PW-04` **typed by severity**; the diff shows each size's `marketed_size_label` beside its `size_cents`. **It carries no `CV-nn` and no `PW-nn`, deliberately** |
| **3.4 The wallet leg** | [ADR-019](../decisions/ADR-019.md). One transaction, no third party, no webhook. **Mixed funding applies the wallet leg first**, a wallet refund returns to the wallet, and **MID health thresholds are computed against CARD volume** |
| **3.5 / 3.5.1 The restriction refusal** | [ADR-041](../decisions/ADR-041.md), `INV-M3-15`. The finding it records is **a proven asymmetry inside one endpoint**: the wallet leg has refused a restricted identity since ADR-019 and the card leg never has, so *"a restricted trader could buy with a card and could not buy with money Merit already owes them"* |
| **3.6 What checkout shows** | `INV-M3-16`. The label is pinned by the write that pins the price, and **where the label is absent checkout renders the capital figure derived from `size_cents`** |
| **4 Endpoints** | Six rows. Four owned, one served, one read-only |
| **5 Events** | **10 emitted**, two marked **NEW**; **five consumed**. `identity.restricted` is consumed **for cache invalidation only**, because *"a gate driven by an event is a gate with a replication lag on the money path"* |
| **6 Failure modes** | **12**, `FM-M3-01` to `FM-M3-12` |
| **7 Adversarial** | **Six**, five novel. `AS-M3-03`'s counter is explicitly **not in M3** |
| **7.9 Enrichment** | [ADR-023](../decisions/ADR-023.md). Observe mode, **fail-open on timeout**, and 7.9.1's procurement condition that **portability history is disqualifying** |
| **8 Test plan** | Five named suites totalling **50 tests** (`M3-U` 18, `M3-W` 11, `M3-G` 9, `M3-N` 7, `M3-P` 5) plus six owned golden scenarios. Section 8.3: **every hard limit is tested on all three payment methods** |
| **9 Observability** | 11 metrics, 7 alerts, one dashboard |
| **10 Open questions** | **Four.** `OQ-M3-04` is RULED; `OQ-M3-01`, `02` and `03` are open and **two of them change what a slice builds** |

---

### 2. What EXISTS, measured by command

**The schema is complete. Every one of the six deltas landed**, and that is the single largest correction this measurement makes to the record.

| Delta | Where it is | Verdict |
|---|---|---|
| `SD-M3-01` | [`0006:296-303`](../../packages/db/migrations/0006_commerce.sql): `purchase_id`, `deferred_until`, `defer_attempts`, plus `psp_webhook_events_deferred_idx` | **LANDED** |
| `SD-M3-02` | [`0006:139-140`](../../packages/db/migrations/0006_commerce.sql): `refundable_until`, `first_trade_at`, plus `purchases_refundable_idx` | **LANDED** |
| `SD-M3-03` | [`0006:343-372`](../../packages/db/migrations/0006_commerce.sql): the whole `mid_health` table | **LANDED, AND WIDER THAN THE DELTA TEXT.** The delta names seven columns; the migration writes **nine**, adding `window_end` and `card_settled_count`, and a `COMMENT ON COLUMN` stating that `chargeback_rate_bp` is computed against `card_settled_count` and never total volume. **The denominator rule of section 3.4 is enforced in the column names**, which the delta row does not say |
| `SD-M3-04` | [`0006:66-68`](../../packages/db/migrations/0006_commerce.sql): `applies_to_kind`, `first_purchase_only` | **LANDED**, and the header records that the `'any'` default *"exists only so the constraint is total; the create path supplies it"* |
| `SD-M3-05` | [`0006:145-150`](../../packages/db/migrations/0006_commerce.sql): `checkout_ip_country`, `card_country`, `geo_decision` | **LANDED** |
| `SD-M3-06` | [`0006:158-167`](../../packages/db/migrations/0006_commerce.sql): `payment_method`, `wallet_debit_cents`, `wallet_ledger_transaction_id`, **plus two CHECK constraints the delta row does not name** | **LANDED, AND STRONGER THAN SPECIFIED.** `purchases_wallet_leg_matches_method` makes the three methods' funding arithmetic total, and `purchases_wallet_debit_is_posted` makes *"a wallet debit that posted no ledger transaction"* unrepresentable |

**Sixteen invariants, one at a time.**

| Invariant | State in the tree |
|---|---|
| `INV-M3-01` | **HALF.** [`0028`](../../packages/db/migrations/0028_supersede_plan_version_immutability.sql) plus `plan_versions_published_immutable` gives the immutability half; **the write-once trigger on `purchases.plan_version_id` does NOT exist**. Section 3 |
| `INV-M3-02` | **NO ENFORCEMENT AND NO LIBRARY.** The stated mechanism is *"Zod schema has no price field at all"* and `zod` appears in no manifest and no source file in the repository |
| `INV-M3-03` | **STRUCTURAL, LANDED.** `psp_webhook_events_provider_event_uq` at [`0006:310`](../../packages/db/migrations/0006_commerce.sql) and `processing_result`'s four-value CHECK. No handler reads it |
| `INV-M3-04` | **SCHEMA LANDED, DRIVER ABSENT.** `SD-M3-01`'s three columns and the re-drive index exist; nothing enqueues or re-drives |
| `INV-M3-05` | **COLUMN ONLY.** `signature_verified boolean NOT NULL` at [`0006:281`](../../packages/db/migrations/0006_commerce.sql). No verification code |
| `INV-M3-06` | **STRUCTURAL, LANDED, EXACTLY AS SPECIFIED.** `coupon_redemptions_live_claim_uq ON (coupon_id, identity_id) WHERE released_at IS NULL`, [`0006:264`](../../packages/db/migrations/0006_commerce.sql) |
| `INV-M3-07` | **INEXPRESSIBLE.** Requires claim release and count decrement *"in the same transaction"* and the accessor has no transaction. Section 3 |
| `INV-M3-08` | **INPUT ONLY.** `identities.max_accounts_override` at [`0002:47`](../../packages/db/migrations/0002_identity.sql). No cap check anywhere |
| `INV-M3-09` | **PARTIAL, AND THE GAP IS THE WORD "SET".** [`tos_acceptances`](../../packages/db/migrations/0004_catalog.sql) at `0004:250` records identity, version, `ip inet NOT NULL` and timestamp, unique per identity per version. **`purchases` carries no reference to it**, so the tree records what an identity accepted and not *"the exact `tos_version_id` set"* a given purchase was made under |
| `INV-M3-10` | **ACCOUNTS EXIST, PATH DOES NOT.** `psp_clearing` and `trader_wallet` are both in [`0009:52,56`](../../packages/db/migrations/0009_ledger.sql)'s declared code list. No posting path, which [session 143](2026-08-23-session-143.md) already recorded |
| `INV-M3-11` | **DECISION RECORD ONLY.** `mid_health` exists; no routing code, no health check, no `service_unavailable` |
| `INV-M3-12` | **HALF, AND THE HALF THAT EXISTS IS M1's.** `validatePlan` is real and exported at [`packages/rules-engine/src/index.ts:163`](../../packages/rules-engine/src/index.ts), from `src/plan/validate.ts`, whose own header quotes M01's *"`validatePlan` runs in `POST /admin/plans/versions/:id/publish`"*. **There is no publish path to run it in**, and the same-transaction materialization is inexpressible |
| `INV-M3-13` | **INEXPRESSIBLE.** Section 3 |
| `INV-M3-14` | **LANDED.** `wallet_debit_cents bigint NOT NULL DEFAULT 0 CHECK (>= 0)` plus `purchases_wallet_leg_matches_method`. The no-credit-facility property is in the schema |
| `INV-M3-15` | **NOTHING.** No code reads `identities.status` at any checkout point, because there is no checkout |
| `INV-M3-16` | **THE COLUMN DOES NOT EXIST.** Section 3 |

**The runtime precondition [session 143](2026-08-23-session-143.md) called *"the finding"* is now PARTLY MET, and this is measured rather than inherited.** Sessions 144, 145 and 146 landed between that measurement and this one.

| What 143 measured | What is true today |
|---|---|
| *"the workspace declares ZERO runtime dependencies"* | **False now.** `packages/db` declares `drizzle-orm: catalog:` and `pg: catalog:`; the catalog pins `drizzle-orm 0.45.2` and `pg 8.23.0`. **Every other manifest is still empty or `workspace:*`**, read across all 13 |
| *"`ScopedDb` is an interface with ONE field"* | **False now.** [`scoped-db.ts:131`](../../packages/db/src/scoped-db.ts) declares three members and `scopedDb()` returns a working accessor over a real Drizzle handle. [`client.ts`](../../packages/db/src/client.ts) is the one file that imports `pg`, the pool is lazy, and `merit/no-raw-db-client` makes that mechanical |
| *"the `API` container is drawn and not rowed"* | **Closed by [ADR-083](../decisions/ADR-083.md).** [`apps/api`](../../apps/api) exists: `package.json`, `src/index.ts` (95 lines), `src/surface.ts` (185), `test/surface.test.ts` (135) |

**What `apps/api` actually is.** Its own header says it: *"NO ROUTE IMPLEMENTS A BEHAVIOUR AND THAT IS NOT AN OVERSIGHT."* What is real is `surface.ts`, the startup partition that makes `/internal/*` answer 404 by registering nothing. `classifyPath`'s doc comment names `/checkout` and `/webhooks/psp/:provider` as its examples, and [`test/surface.test.ts:65`](../../apps/api/test/surface.test.ts) asserts `/checkout` classifies `public`. **That is the entire presence of the word `checkout` in the API deployable: one argument to a path classifier.**

---

### 3. What does NOT exist

**Nine absences, each with the widest grep that establishes it.**

**3.1 The `PspAdapter` does not exist, and neither does anything downstream of it.**
`grep -rn 'PspAdapter\|createSession\|verifyWebhook\|PurchaseIntent\|PaymentSession'` over every `.ts`, `.mts`, `.js` and `.mjs` outside `node_modules`: **zero hits, of any of the five names.** Widening to the bare nouns, `grep -rli` over `apps packages scripts`:

| Noun | Files in code |
|---|---|
| `coupon` | **ZERO** |
| `refund` | **ZERO** |
| `mid_health` | **ZERO** |
| `psp` | 5, and **not one is an implementation**: `apps/api/src/surface.ts` and its test (the `/webhooks/psp/:provider` path string), `packages/db/src/schema.ts` and `scope.ts` (the `psp_clearing` ledger code in a comment, and `closed_chargeback`), `scripts/corpus/falsify.mjs` |
| `checkout` | 24 lines over 20 files, **every one a comment, a doc block or a test path string.** The nearest thing to code is `apps/site/src/routes/geo.ts:19`, which says *"the notice is courtesy; the control is server side"* about a control that is not there |

**3.2 There is no HTTP framework and no server.** No `fastify`, `express`, `createServer` or `listen(` in any manifest or non-test source. [ADR-083](../decisions/ADR-083.md) rules Fastify 5 and [`apps/api/package.json:14`](../../apps/api/package.json) states in terms why it is absent: the version belongs in the `catalog:` block, which that session did not hold. **The catalog holds 12 entries and `fastify` is not one of them.** So M03's four owned endpoints have a deployable, a surface partition and no way to register a route.

**3.3 `marketed_size_label` exists in TypeScript and in no SQL, and its manifest row points at a SPENT number.**
This is the sharpest finding in the measurement and it took three commands.

| Fact | Command and result |
|---|---|
| The column is nowhere in the DDL | `grep -rn 'marketed_size_label' packages/db/migrations/` -> **zero** |
| It is read in 20 places | `grep -rn` repo-wide -> **37 hits**, 20 of them in [`apps/site`](../../apps/site): `catalog/types.ts` declares `readonly marketed_size_label: MarketedSizeLabel \| null`, [`render/size-label.ts:55`](../../apps/site/src/render/size-label.ts) implements the stated default as `size.marketed_size_label ?? money(size.row.size_cents)`, and four test files exercise both branches |
| Its delta row names migration `0044` | [`DELTA_MANIFEST:156`](../../packages/db/DELTA_MANIFEST.md): `SD-M9-04 \| plan_version_sizes \| add marketed_size_label ... \| 0044 \| **reserved**` |
| **`0044` is spent** | [`ALLOCATION:136`](../decisions/ALLOCATION.md): *"**SPENT, and under a DIFFERENT NAME than it was reserved for**... Written as `0044_fee_back_and_ladder_unlock.sql`, **not** as `0044_plan_config_completeness.sql`. Reserved for the four gaps as schema: ... **the decoupled display name and marketed size label** beside the internal size"* |
| The file on disk confirms it | `grep -n 'marketed_size_label' packages/db/migrations/0044_fee_back_and_ladder_unlock.sql` -> **zero**; the file's three `plan_version_sizes` mentions are all about the ladder unlock |

**So `SD-M9-04` is a reserved delta whose reservation was consumed by a different migration, and no gate can see it.** `CI-06h` asserts migration numbers are gapless over allocated plus reserved and it passes, because `0044` **is** allocated: the number is not missing, the delta that was riding on it is. `ADR-026`'s manifest-completeness gate requires every `SD-nn` to have exactly one row with a disposition, and this row has one. **The defect is that the disposition is `reserved` against a number nothing can still write to**, and it is M03's problem because `INV-M3-16` and section 3.6's three named surfaces all rest on the column.

**3.4 `INV-M3-01`'s trigger is on the wrong table.**
`grep -rn 'CREATE TRIGGER' packages/db/migrations/` returns **ten triggers in the whole tree**, and here they all are: `account_adjustments_reversal_is_sound`, `accounts_plan_version_pinned`, `impersonation_page_view_within_box`, `impersonation_sessions_token_is_not_a_trader_token`, `ledger_entries_class_declared`, `phone_change_requests_evidence_is_this_identitys`, `plan_versions_published_immutable`, `sessions_token_is_not_an_impersonation_token`, `trading_calendar_no_delete`, `trading_calendar_no_truncate`. **`grep -rn 'ON purchases' packages/db/migrations/` returns five lines and all five are `CREATE INDEX`.**

`assert_account_plan_version_pinned` at [`0027:270`](../../packages/db/migrations/0027_triggers_invariants.sql) raises *"account % has plan_version pinned at purchase and it may never change (B4 #12, GS-041)"*, **citing INV-M3-01's own two references**. It is the right control on the wrong row. `INV-M3-01`'s subject is the purchase, which is the contract; the account is downstream of it. **A `purchases` row whose `plan_version_id` is updated after the fact repins the contract and leaves the account's trigger untouched**, because the account already holds its own copy.

**3.5 The accessor is read-only, and three invariants say "in the same transaction".**
[`scoped-db.ts:131-139`](../../packages/db/src/scoped-db.ts) is the whole `ScopedDb` interface: `__brand`, `identityId`, and `rows<K extends ScopedTableKey>(key: K): Promise<unknown[]>`. `systemDb` is the same shape. `grep -n 'transaction' packages/db/src/*.ts` returns nine lines and **every one is a comment or the `ledger_transactions` table name**; `client.ts:15` mentions transactions only to explain why the driver is `pg`.

`INV-M3-07` (claim release and count decrement), `INV-M3-12` (publish and `plan_version_sizes` materialization) and `INV-M3-13` (wallet debit and purchase insert) each require one. **None is expressible against this accessor, and neither is any INSERT at all.**

**3.6 Seven tables of 111 are reachable, and none of them is M03's.**
[`schema.ts`](../../packages/db/src/schema.ts) declares `identities`, `accounts`, `ledgerAccounts`, `ledgerTransactions`, `ledgerEntries`, `treasuryBalances`, `liabilitySnapshots`, and its header says so: *"SEVEN TABLES OF 111, AND THAT IS REPORTED RATHER THAN ROUNDED UP."* `SCOPE_RULES` in [`scope.ts`](../../packages/db/src/scope.ts) is declared against a mapped type over `TableKey`, so **a table absent from `schema.ts` is a compile error at the call site rather than an unscoped read**. `purchases`, `coupons`, `coupon_redemptions`, `psp_webhook_events`, `mid_health`, `plan_versions`, `plan_version_sizes` and `tos_acceptances` are all absent. `accounts.purchaseId` is present as a bare `uuid` with `.notNull().unique()` and nothing on the other end of it.

**3.7 Both events marked NEW are unregistered, and one of them has two spellings.**
`grep -rn 'mid.health_changed'` repo-wide: **three hits, two of them in M03 itself** (sections 5.1 and 6), and the third is [`M10:165`](../plans/M10-integrations.md), which writes **`psp.mid_health_changed`**. `grep -rn 'purchase.first_trade_recorded'`: **one hit, M03 section 5.1.**

[EVENTS section 4](../architecture/EVENTS.md) carries nine M3 rows at lines 86 to 94 (`checkout.started`, `coupon.claimed`, `coupon.claim_released`, `purchase.paid`, `purchase.failed`, `purchase.refunded`, `purchase.charged_back`, `plan_version.published`, `plan_version.retired`) and `account.provision_requested` at line 104. **Ten of M03's twelve emitted events are registered; the two the document marks NEW are not.** `grep -in 'mid_health\|health_changed'` over EVENTS.md returns exactly one line, and it is the parenthetical *"ALERT (MID health)"* on `purchase.failed`.

**Two approved plans name the same event with two different names and neither name is in the register.** `FM-M3-05`'s detection is *"`mid.health_changed` to `unhealthy` on both"*, so the page for the both-MIDs-down case fires on an event that does not exist under either spelling.

**3.8 No test, no fixture, no probe.**
`grep -rn 'M3-U-\|M3-W-\|M3-G-\|M3-N-\|M3-P-'` repo-wide, excluding the M03 plan itself: **zero.** All 50 named tests are unwritten and the suite prefixes are unused identifiers.

The fixture side is uniform, and the uniformity is the finding. [Section 33.1](../testing/golden-scenarios/33-ownership-index-and-coverage-reconciliation.md) owns **15** scenarios to M3: `GS-038` to `GS-041`, `GS-094` to `GS-099`, `GS-239`, `GS-250`, `GS-252`, `GS-272`, `GS-280`. Adding `GS-046` (which M03 section 5.2 and `FM-M3-12` both cite) and `GS-309`/`GS-310` (M9's, asserted at checkout by section 8.2) gives 18. **All eighteen are `blocked / no-fixture-format` in [section 39](../testing/golden-scenarios/39-fixture-status-and-blockers.md)**, each with the identical reason: *"the only fixture format in the tree is M1's day stream folded through `advanceDay`"*. `ls packages/rules-engine/fixtures/*.json` is empty; the 43 fixtures the inventory counts are elsewhere in that tree and none is M3's.

**No probe asserts an M03 constraint.** `grep -rn 'purchases_\|coupon_redemptions_\|psp_webhook_events_\|mid_health_' scripts/db/` returns **zero**. Six of the twelve probes `corpus.yml` runs insert `purchases` rows, and in all six the row is setup for another module's assertion. `INV-M3-03`'s and `INV-M3-06`'s unique indexes are the two strongest controls M03 has and **neither has ever been watched refusing anything.**

**3.9 `CI-06o` cannot discover checkout as a money path.**
[`gates.mjs:3015`](../../scripts/corpus/gates.mjs): `const CI06O_SCOPE = ['packages/rules-engine']`, one entry. [`gates.mjs:3025`](../../scripts/corpus/gates.mjs): `const MONEY_PATH_WORDS = ['rules-engine', 'payout', 'ledger', 'auth']`. Assertion 2 walks `packages/` and `apps/` and flags any directory whose **name** carries one of those four words and is not in the scope list.

**`billing`, `checkout`, `commerce` and `psp` are none of the four.** The gate says so about itself, in its own `covers` string: *"discovery is BY NAME, so a money path whose directory name does not say so -- apps/worker runs the nightly batch today -- is not found by assertion 2 and must be added by hand."* M03 is a money path by its own seventh line, by `ADR-003` and by `CLAUDE.md`'s session-length regime. **Every slice in section 4 that lands checkout code lands it where `ADR-044` prohibition 1 is not enforced**, silently, with a green gate.

**3.10 Two things M03 says are missing have since landed, and M03 is stale about both.**
Named here because a slice reading M03 would rebuild them.

- **Section 3.5 says the `identity_restricted` error code is *"proposed and not claimed here"* and *"stated in this document only".* It is defined.** [API_CONTRACT:69](../architecture/API_CONTRACT.md) rows `identity_restricted \| 422 \| The identity is restricted...`, and line 76 states why it is distinct from `payouts_frozen`, naming M03 as the document that had it in one direction. [Session 38](2026-08-16-session-38.md) landed it as FOLD-02 session 6.
- **Section 8.3 says *"One golden scenario is owed and no `GS-nnn` is claimed here".* It is claimed: [`GS-280`](../testing/golden-scenarios/35-gs-273-to-gs-284-the-enforcement-window-and-identity-restriction.md)**, whose text is M03 section 8.3's sentence almost verbatim, down to *"which is GS-094's assertion with a different hard limit behind it"*, and which names *"M03 section 8's owed scenario, `EC-155`"* in its own last cell.

---

### 4. The slices

Ten. Each carries an objective, a fence **by file**, a money-path flag and a depends-on **by file**. **No number is claimed by this log**: `ALLOCATION.md` is outside this session's fence, so the ADR, migration and session numbers below are named as *needed* and reserved by nobody. Section 6 says why that is a finding rather than an omission.

**Wave A: the preconditions. Nothing in M03 is buildable before these and none of them is M03's alone.**

| # | Slice | Fence, by file | Money | Depends on, by file |
|---|---|---|---|---|
| **M3-a** | `schema.ts` and `scope.ts` learn M03's five commerce tables, and the accessor learns to WRITE and to run a TRANSACTION | `packages/db/src/schema.ts`, `packages/db/src/scope.ts`, `packages/db/src/scoped-db.ts`, `packages/db/test/scoped-db.test.ts`, ADR (new), `ALLOCATION` (its row), `INDEX` (its row), `STATE` (append), `sessions/` | **yes** | `packages/db/src/**` from **145**, landed |
| **M3-b** | `CI-06o` learns that checkout is a money path, before any checkout code exists | `scripts/corpus/gates.mjs` (`CI06O_SCOPE` and `MONEY_PATH_WORDS` only), `scripts/corpus/falsify.mjs` (its seed), ADR (new), `ALLOCATION` (its row), `INDEX`, `STATE`, `sessions/` | no | nothing. **It must land BEFORE M3-e** |
| **M3-c** | Fastify 5 enters the catalog and `apps/api` registers its first route | `pnpm-workspace.yaml` (`catalog:` only), `pnpm-lock.yaml`, `apps/api/package.json`, `apps/api/src/**`, `apps/api/test/**`, `sessions/` | no | **147** via `pnpm-lock.yaml` |

**Wave B: the two schema repairs. Each carries a migration, so each is E2.**

| # | Slice | Fence, by file | Money | Depends on, by file |
|---|---|---|---|---|
| **M3-d** | `SD-M9-04` gets a migration and its manifest row leaves the spent `0044`. `INV-M3-16`'s column becomes real | `packages/db/migrations/00NN_marketed_size_label.sql` (new), `packages/db/DELTA_MANIFEST.md` (the `SD-M9-04` row only), `scripts/db/probe_plan_version_size_label.sql` (new), `.github/workflows/corpus.yml`, `scripts/corpus/gates.mjs` (`CI-06h`'s needle list only), `docs/architecture/data-model/` (the `plan_version_sizes` entry), ADR (new), `ALLOCATION` (its two rows), `INDEX`, `STATE`, `sessions/` | no, **but it carries a migration and an `E2` read** | nothing |
| **M3-e** | `INV-M3-01`'s missing half: `purchases.plan_version_id` becomes write-once by trigger | `packages/db/migrations/00NN_purchases_plan_version_pinned.sql` (new), `packages/db/DELTA_MANIFEST.md`, `scripts/db/probe_purchase_plan_version_pinned.sql` (new), `.github/workflows/corpus.yml`, `scripts/corpus/gates.mjs` (`CI-06h` needles only), ADR (new), `ALLOCATION` (its two rows), `INDEX`, `STATE`, `sessions/` | **yes, `E2`** | **M3-d** via the migration sequence and `DELTA_MANIFEST` |

**Wave C: the corpus repairs. No code, and none of them needs the runtime.**

| # | Slice | Fence, by file | Money | Depends on, by file |
|---|---|---|---|---|
| **M3-f** | `EVENTS` gains `mid.health_changed` and `purchase.first_trade_recorded`, and the two-spelling divergence is RULED rather than picked | `docs/architecture/EVENTS.md`, `docs/plans/M03-billing-checkout.md` (section 5.1's two rows only), `docs/plans/M10-integrations.md` (line 165 only), ADR (new, **required**: both plans are `approved`), `ALLOCATION` (its row), `INDEX`, `STATE`, `sessions/` | no | nothing |
| **M3-g** | M03's two stale sentences are repaired: section 3.5's forward reference and section 8.3's owed scenario | `docs/plans/M03-billing-checkout.md` (sections 3.5 and 8.3 only), ADR (new, **required**), `ALLOCATION` (its row), `INDEX`, `STATE`, `sessions/` | no | nothing |

**Wave D: the module itself. Every one of these is money path and every one needs all of wave A.**

| # | Slice | Fence, by file | Money | Depends on, by file |
|---|---|---|---|---|
| **M3-h** | The `PspAdapter` interface and two fakes. **No routes, no network** | `packages/psp/**` (new), `packages/psp/package.json`, `pnpm-workspace.yaml` (`catalog:` only), `pnpm-lock.yaml`, `scripts/corpus/gates.mjs` (`CI06O_SCOPE` only), `vitest.config.ts` (if a project include moves), ADR (new), `ALLOCATION`, `INDEX`, `STATE`, `sessions/` | **yes** | **M3-b** via `gates.mjs`; **M3-c** via `pnpm-lock.yaml` |
| **M3-i** | `POST /checkout` and `POST /accounts/:id/reset`: the server-authoritative pipeline, `INV-M3-02`, `08`, `09`, `13`, `14`, `15`, `16`, and the `M3-U` and `M3-N` suites | `apps/api/src/routes/checkout.ts` (new), `apps/api/src/routes/index.ts`, `apps/api/test/checkout.test.ts` (new), `apps/api/package.json`, `pnpm-lock.yaml`, `sessions/` | **yes, `E2`** | **M3-a** via `packages/db/src/**`; **M3-h** via `packages/psp`; **M3-c** and **M3-e** |
| **M3-j** | `POST /webhooks/psp/:provider`: verify, persist, dedupe, dispatch, and the deferral driver. `INV-M3-03`, `04`, `05`, and the `M3-W` suite | `apps/api/src/routes/webhooks.ts` (new), `apps/api/src/routes/index.ts`, `apps/api/test/webhooks.test.ts` (new), `apps/worker/src/**` (the re-drive job only), `sessions/` | **yes, `E2`** | **M3-a**, **M3-h**, **147** via the queue; **M3-i** via `apps/api/src/routes/index.ts` |

**Not a slice, and named so it is not lost.** The publish path (`INV-M3-12`, section 3.3, the `M3-P` suite) is **not decomposed here** because [session 148](../plans/P3-ledger-billing-identity.md)'s `ADR-087` decides between a trigger and an application publish path for `OI-29`, and *that is the same publish transaction*. A slice written before 148 lands would either duplicate its decision or contradict it. **The eighteen blocked fixtures are also not a slice**, for the reason section 6 gives.

---

### 5. The collisions, BY FILE

A per-slice depends-on column cannot express a per-file collision, and this corpus has made that error twice. So: **six files are held by more than one slice**, and two of them are held by slices in different waves.

| File | Held by | Why it collides |
|---|---|---|
| **`packages/db/src/schema.ts`** and **`scope.ts`** | **M3-a**, and **every one of the thirteen sibling module measurements running right now** | This is the largest collision in the set and **it is not visible from inside M03.** `SCOPE_RULES` is total over `TableKey` by construction, so M04, M05, M08, M19 and M20 each need their own tables in the same two files, and **a mapped type over a table set cannot be appended to per branch** any more than a lockfile can. This is [P3](../plans/P3-ledger-billing-identity.md)'s `pnpm-lock.yaml` lesson with a different file: four branches would each merge cleanly alone and none of them together. **`packages/db/test/scoped-db.test.ts` collides with it identically**, because that suite reads the `CREATE TABLE` bodies out of the migrations and asserts column-set agreement, so every added table adds a transcription to the same file |
| **`packages/db/DELTA_MANIFEST.md`** | **M3-d**, **M3-e**, and **session 148** (already dispatched, `0047`) | Three rows in one table. 148 holds it first |
| **`.github/workflows/corpus.yml`** | **M3-d**, **M3-e**, **148** | Each adds a `psql -f scripts/db/probe_*.sql` step, and `CI-06s` reads the workflow and the directory in both directions, so a probe on disk with no step is a finding and the reverse is too |
| **`scripts/corpus/gates.mjs`** | **M3-b** (`CI06O_SCOPE`, `MONEY_PATH_WORDS`), **M3-d** and **M3-e** (`CI-06h`'s needle list), **M3-h** (`CI06O_SCOPE` again), and **146 and 148** already | **Four of the ten slices, plus two dispatched sessions.** M3-b and M3-h touch the same constant, so they are strictly serial with each other regardless of what their depends-on columns say |
| **`docs/decisions/ALLOCATION.md`** | **M3-a**, **M3-b**, **M3-d**, **M3-e**, **M3-f**, **M3-g**, **M3-h** | Seven of ten. `CI-06w` reads the table as a multiset and a keep-both merge leaves one row per key, so an expected collision costs a resolution and a discovered one costs a cycle ([session 143](2026-08-23-session-143.md)'s wording, and its case) |
| **`apps/api/src/routes/index.ts`** | **M3-i**, **M3-j** | Both register into one router. **`M3-i` and `M3-j` are serial**, and their depends-on columns would read as parallel if this row did not exist |
| **`pnpm-lock.yaml`** | **M3-c**, **M3-h**, and **147** | The lesson P3 already paid for. **Serial, three ways** |
| **`docs/plans/M03-billing-checkout.md`** | **M3-f** (section 5.1), **M3-g** (sections 3.5 and 8.3) | Different sections of one frozen document. They do not overlap textually and they **both require an ADR**, so they collide on `ALLOCATION` even where they do not collide in the file. **Merging them into one slice is the cheaper answer and it is recommended** |

**The migration NUMBER is a collision the file list does not show.** M3-d and M3-e each need one; `0047` is reserved to session 148 and **released if `ADR-087` takes the application path**. So the two wave-B slices are serial against each other *and* their numbers depend on a ruling neither of them holds.

---

### 6. What could not be determined, named rather than guessed

1. **Whether M03's slices may claim numbers at all.** [Session 143](2026-08-23-session-143.md) allocated seven rows in its first commit, *"before anything else was written"*, and its fence included `ALLOCATION.md`. **Mine does not.** So section 4's ten slices name no ADR, no migration and no session number, and **seven of them need one.** Under [ADR-055](../decisions/ADR-055.md)'s reasoning, and 143's, an unreserved number at the top of the sequence opens no hole and is therefore invisible until something below it is reserved. **This is the same shape as `ADR-082` and it is reported rather than repaired**, because repairing it means writing in a file this session was told not to touch. Whoever dispatches these slices allocates first.

2. **Whether the `no-fixture-format` blocker is closable for M03 at all.** All 18 rows carry it, and the cause named is corpus-wide rather than M3-specific: the only fixture format in the tree is M1's day stream. **Whether a second format is written, and what shape it takes, is [WAVE-04](../plans/WAVE-04-fixture-backlog-and-gate-inventory.md)'s and [WAVE-05](../plans/WAVE-05-tier2-fixture-shapes.md)'s territory and not M03's**, and this session did not take a view. What is determinable and is stated: **M03 cannot discharge a single golden row until that ruling exists**, so its section 8.2 is unreachable independently of how much of section 4 gets built.

3. **Which sibling sessions collide with me.** Thirteen other modules are being measured concurrently and **I can see none of their branches.** Section 5's first row is my best reading of the shared surface from M03's side alone. **If any sibling proposes a `packages/db/src/schema.ts` slice, theirs and mine are one slice and not two**, and that has to be decided above all fourteen of us.

4. **`OQ-M3-01`, `OQ-M3-02` and `OQ-M3-03` are open and two of them change what `M3-i` builds.** `OQ-M3-01` (is the reset-diff acknowledgement blocking or a notice) decides whether `M3-i` can complete a reset without a second round trip. `OQ-M3-02` (the refund window's time bound and whether the cap slot releases immediately) decides what `refundable_until` is set to at checkout, and **`SD-M3-02` landed the column without the policy**, so today the column exists and there is no number to write into it. `OQ-M3-03` does not block `M3-i` and is sharper than it was: section 10 records that a restored restriction leaves `identities.status = 'active'` with an episode row that *"will still be there in five years"*, so the question is now which past enforcements count. **All three go to the founder, and the first two go BEFORE `M3-i` is dispatched.**

5. **Whether `mid.health_changed` or `psp.mid_health_changed` is the name.** M03 and M10 are both `approved`. This session established that neither is registered and did **not** pick between them, because picking is an amendment to one of two frozen documents and that is an ADR.

- **Landmines:**

  - **SESSION 157 HAS NO CLAIM ROW, AND THE DISPATCHING BRIEF SAYS IT DOES.** The brief instructed this session to **strike** its row per [ADR-065](../decisions/ADR-065.md) T3, *"amend in place, never add a second row"*. `grep -n '157' docs/sessions/README.md` on `main` at `ef4cccc` returns **one line and it is `EC-157` inside session 43's entry**. The CLAIM table runs `114` to `156` and holds no `157`; the live rows are `147`, `148`, `156` and nothing else. **None of the thirteen sibling numbers is there either.** The row was written here as **already struck**, which is the state a written session's row is supposed to reach, on session 143's `ADR-082` precedent of writing a missing registry row on the absent session's behalf and saying so. **This is `OI-27`'s shape and session 154's**: a dispatcher taking numbers without reserving them, and fourteen concurrent sessions is the worst possible configuration for it. **A number is free when no row claims it, and fourteen sessions each reading the highest row is exactly the 110/111/112 collision the CLAIM table was created to end.**

  - **THE COLLISION THAT MATTERS IS NOT INSIDE M03 AND NO SINGLE MODULE MEASUREMENT CAN SEE IT.** `packages/db/src/schema.ts` holds seven tables of 111 and `SCOPE_RULES` is **total over its keys by compile-time construction**. Every module slice in this corpus that touches a database needs its own tables in that file and in `scope.ts`. **Fourteen concurrent measurements will each propose that same edit, correctly, from inside their own fence, and no two of them will merge.** This is [P3](../plans/P3-ledger-billing-identity.md)'s serial-on-`pnpm-lock.yaml` finding with a worse file, because a lockfile at least conflicts loudly and a mapped type conflicts as a type error in a package nobody's branch is testing.

  - **`CI-06o` IS GREEN AND WILL STAY GREEN OVER EVERY LINE OF CHECKOUT CODE ANYONE WRITES.** `MONEY_PATH_WORDS` is `['rules-engine', 'payout', 'ledger', 'auth']` and `CI06O_SCOPE` is `['packages/rules-engine']`, one entry. **`billing`, `checkout`, `commerce` and `psp` are none of the four**, so `ADR-044` prohibition 1 goes unenforced over M03 silently. The gate's own `covers` string admits the limit in terms and names `apps/worker` as the standing instance. **The fix is two array literals and it must land BEFORE the code**, because a gate widened after the fact has never watched the code it now covers.

  - **`SD-M9-04` IS A RESERVED DELTA WHOSE NUMBER WAS SPENT ON SOMETHING ELSE, AND `apps/site` ALREADY SHIPS THE READ PATH.** `DELTA_MANIFEST:156` rows it against `0044`; `ALLOCATION:136` says `0044` is **SPENT under a different name** and lists *"the decoupled display name and marketed size label"* among what the reservation was for. `0044_fee_back_and_ladder_unlock.sql` is on disk and carries no such column. **Twenty TypeScript sites read a column no database has**, including a rendering default implemented at [`size-label.ts:55`](../../apps/site/src/render/size-label.ts). **No gate can see this**: `CI-06h` passes because the *number* is allocated, and `ADR-026`'s completeness gate passes because the *row* exists with a disposition. The thing that is wrong is the disposition, and nothing reads dispositions for plausibility.

  - **A HAND-MAINTAINED COUNT IN M03's OWN DELTA TABLE WAS CAUGHT ONCE AND THE SAME CLASS SURVIVES IN ITS EVENT TABLE.** Section 2's *"Six deltas"* line records that it read *"Five deltas"* over six rows and calls it [ADR-034](../decisions/ADR-034.md)'s defect. **Section 5.1's two `NEW` rows are the same defect one table down**: a plan asserting an event exists, with nothing reconciling the assertion against [EVENTS](../architecture/EVENTS.md). There is no `CI-06` gate over event names, which is why two approved plans can carry two spellings of one unregistered event indefinitely. **A gate here is cheaper than a bigger model**, which is `CLAUDE.md`'s own standing instruction on this exact class.

  - **`INV-M3-01`'s TRIGGER EXISTS AND GUARDS THE WRONG ROW, WHICH IS WORSE THAN ITS ABSENCE.** `accounts_plan_version_pinned` raises an error citing **B4 #12 and GS-041**, which are `INV-M3-01`'s own two references, so a reader checking whether the invariant is enforced finds a trigger, finds the right citations, and stops. The subject of `INV-M3-01` is the **purchase**, which is the contract; the account holds a copy made downstream of it. **Ten triggers in the tree and five index definitions on `purchases`, and not one trigger.**

  - **THREE INVARIANTS SAY "IN THE SAME TRANSACTION" AND THE ACCESSOR CANNOT OPEN ONE.** `ScopedDb` is `__brand`, `identityId`, `rows()`. There is no insert, no update and no transaction anywhere in `packages/db/src`. **`INV-M3-13` is the one to watch**, because its whole argument for why the wallet path is safe is that the debit and the purchase *"commit together or neither does"*, and a wallet leg written against a read-only accessor would have to be built some other way and would then be a second money movement path nobody ruled on.

- **Files touched:** [`docs/sessions/2026-08-24-session-157.md`](2026-08-24-session-157.md) (new, this file) and [`docs/sessions/README.md`](README.md) (the `157` CLAIM row, written already-struck because none existed, plus the regenerated `session_entries` span, which is [ADR-088](../decisions/ADR-088.md)'s generated table and not a hand edit). **No code file, no migration, no gate, no fixture, no ADR, no `INDEX` row, no `STATE` append. Nothing was ruled and nothing was signed.**

### What was NOT done, named rather than implied

**No document was created and none was edited outside `docs/sessions/`.** The two stale sentences in M03 sections 3.5 and 8.3 were **found and left**, as `M3-g`; `EVENTS.md` was **read and not edited**, as `M3-f`; `DELTA_MANIFEST`'s `SD-M9-04` row was **measured and left**, as `M3-d`; `CI06O_SCOPE` was **read and left**, as `M3-b`. Each is an edit this session could have made in one line and each is outside the fence, which is the constraint that lets fourteen sessions run at once. **No number was claimed in `ALLOCATION.md`** and section 6 item 1 says why that is itself a finding. **The `147` and `148` rows were not touched**, and `148`'s `0047` is named in section 5 as a dependency rather than resolved.
