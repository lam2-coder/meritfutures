---
status: approved
depends_on: [README.md]
last_updated: 2026-08-30
---

## 2026-08-30 - Session 482: the first real payout backend, and it is two members of seven on purpose

<!--index: **THE RULING IS [`ADR-291`](../decisions/ADR-291.md), `proposed` and UNSIGNED, `E2` READ OWED AND NOT PERFORMED. A BUILD ROW ON THE MONEY PATH: [`ADR-287`](../decisions/ADR-287.md) SLICE 3, THE FIRST IMPLEMENTATION OF `PayoutTx` THIS TREE HAS EVER HELD.** `postgresPayoutBackend` (`apps/api/src/payout-backend.ts`) implements **`transact` and `identityStatus()`** and **its other five members reject with `PayoutBackendUnwired`**. **THE PORT STAYS BLOCKED AND THE COUNTS DO NOT MOVE AT TEN WIRED OF TWENTY-FOUR DECLARED WITH FOURTEEN BLOCKED**, because `wired` is the set of ports `start.ts` CALLS and **`start.ts` IS UNTOUCHED**: installing the backend is slice 9, a separate money-path session behind a founder read. **NO MIGRATION WAS WRITTEN, NO NUMBER TAKEN OR RESERVED, AND `packages/db/migrations/**` IS UNTOUCHED. NO `RI-nn` WAS MINTED AND `RI-32`, RESERVED TO THIS ROW BY NAME, IS LEFT FREE**, with the three candidates enumerated and each shown already served (ADR-291 section 8): the `start.ts` property is asserted in two live places already, "no member answers plausibly" is behaviour that a static check cannot see, and a token-level ban on casts would fire on `asRow`'s own legitimate narrowing. **THE RE-DERIVATION ROW 291 DEMANDED WAS PERFORMED AND FOUND NO CONTRADICTION**: `identity_status` is `('active','restricted','closed')` (`0001_extensions_and_enums.sql:27`), `identities.status` is `identity_status NOT NULL DEFAULT 'active'` (`0002_identity.sql:42`), `identities` is scope class `root` on `id` and `ScopedTableKey` excludes only `firm` and `pair`, and `IdentityStatus` transcribes the three. **AND ONE LEG WAS CHECKED THAT ADR-287 DID NOT STATE**: a recursive read of the migrations for `ALTER TYPE identity_status` and for an `ALTER TABLE identities` touching `status` returns NOTHING, so the decoder's list and the column's CHECK are one fact and not two. **`transact` IS THE LOAD-BEARING DECISION AND FIVE PROPERTIES ARE STATED AS DECISIONS RATHER THAN LEFT AS CODE**: the identity binds ONCE from the session so `PayoutTx` needs no identity parameter and a foreign account reads EMPTY into section 1's 404; it commits only if `fn` returns and this file adds no second commit rule, re-derived at `transaction()` in `scoped-db.ts` rather than assumed; NO second uuid predicate, because `db.scoped` already refuses a non-uuid before the accessor; **NO LOCK IS TAKEN AND THAT IS REPORTED RATHER THAN DECIDED**, registered as slice 6's question with the current call list asserted so adding one is visible; and the handle's non-escape is held BY SHAPE on `db.ts`'s own argument, a runtime validity flag having been considered and REFUSED as a second mechanism for a property the signature carries. **THE EXISTING SCOPED-DOOR IDIOM IS USED AND NO NEW ONE INVENTED**: `databaseWithdrawalBackend` is followed line for line, same door, same `tx.rows('identities')`, same exactly-one-row refusal. **TWO REFUSAL VOCABULARIES, AND THE DIFFERENCE IS THE STATUS CODE**: `PayoutBackendUnwired` is caught by `unwiredOrThrow` and answers 503, and **`PayoutRowError` IS DELIBERATELY NOT ONE**, so a row disagreeing with the schema that wrote it stays a 500, which is `RuleStateUnreadable`'s ruling one door over. **NO SYNTHESISED DEFAULT ANYWHERE**: a FOURTH status RAISES rather than falling back to `restricted`, which would deny a trader on a value nobody wrote, or to `active`, which would open the money door on one. **NO CAST ONTO AN UNVALIDATED ROW**: the accessor's `unknown` is narrowed by a shape test that throws and a closed-list membership test that throws. **F1, THE MEMBER THIS SESSION WAS TEMPTED TO FILL, IS WRITTEN AS A FINDING RATHER THAN TAKEN**: `idempotency` could answer TODAY (`databaseIdempotencyStore`, `idempotency-store.ts:144`) and is left refusing on `usePayoutBackend`'s own closing sentence, that installing it beside a `transact` whose `subject` rejects would put a live-looking route in front of the arm that approves payouts. **F2 IS A LANDMINE AND IT WAS THIS SESSION'S OWN NEAR MISS**: the backend was first written with NO `: PayoutTx` annotation, contextual typing checking it just as well, and `rule-state-producibility.test.ts`'s census stayed GREEN over a module that implements the interface. The annotation was restored, the census went RED naming the file, and only then was it inverted; the census now holds at EXACTLY ONE implementing file and still asserts `start.ts` does not call `usePayoutBackend(`. **F3**: the lock is slice 6's open question, named with the constraint that would arbitrate it. **F4**: `PayoutRowError` is caught by nothing by design and must not be made a `PayoutBackendUnwired` to change that. **`usePayoutBackend`'s ENTRY IS REWRITTEN RATHER THAN SHRUNK** and clause FIVE, which has outlived every other clause there, **NARROWS FOR THE FIRST TIME RATHER THAN CLOSING**: it names five members and not a port, the `RuleState` no-fold ruling inside it is unchanged, and its closing partial-backend sentence stopped being a warning and became a property. Retired wordings are paraphrased and not quoted, on that file's standing rule. **NO PORT WIRED, NO DOOR WIDENED (`CATALOG_TABLE_KEYS` is still five, `SystemReason` gains nothing, `PayoutTx` gains no member), NO GATE WEAKENED, NO FENCE WIDENED, NO STUB, NO CAST, NO SYNTHESISED DEFAULT, NO FLOAT (this slice reads one enum column and no money column at all), NO SECRET, AND MERIT STAYS PASSWORDLESS.** **FENCE HELD.** The files outside the stated fence are GENERATED: `docs/decisions/README.md` and `docs/sessions/README.md` carry spans rewritten by `gates.mjs generate`, which `CI-06n` requires. Counts derived at reporting time, each command run separately: **33 of 33** gates after `generate`, **30 of 30** invariants off the runner's own last line, **298 files / 7,242 passed / 6 skipped**, against a baseline taken by checking out `main` on this same tree and re-running: **297 / 7,223 / 6**, a delta of **exactly one file and nineteen cases**, which is the new suite and nothing else, with **no test deleted and the skip count unmoved**. Typecheck 0, lint 0, `format:check` clean, `falsify.mjs` clean and FOREGROUND. **`pnpm run verify` was NOT run**, forbidden by the row. **THE CONTAINER STARTED WITH NO REPOSITORY AT ALL** and the clone was attached and verified against row `291` before a byte was written; reported here because it is a dispatch fact rather than a code one. -->

---

### 1. The objective

**[ALLOCATION](../decisions/ALLOCATION.md) row `291`: [ADR-287](../decisions/ADR-287.md) slice 3, the module.** A `postgresPayoutBackend` under `apps/api/src/` implementing `transact` and `identityStatus()` only, whose other five members throw `PayoutBackendUnwired`. One objective, money path, [ADR-003](../decisions/ADR-003.md) strict regime.

### 2. What was read before anything was written

Row `291` in full, then `CLAUDE.md`, then [ADR-287](../decisions/ADR-287.md) end to end because it is this session's plan, then `PayoutTx` and `PayoutBackend` at their declarations, then the two migrations that carry `identity_status`, the scope registry, `transaction()` in `scoped-db.ts`, `databaseAuthBackend` and `databaseWithdrawalBackend` as the two precedents, and `usePayoutBackend`'s entry in full.

### 3. Done

- **`apps/api/src/payout-backend.ts`**, new. Two members answer, five refuse.
- **`apps/api/test/payout-backend.test.ts`**, new. Nineteen cases over the recorder.
- **`apps/api/test/wiring.test.ts`**, clause FIVE and the closing summary rewritten rather than shrunk.
- **`apps/api/test/rule-state-producibility.test.ts`**, the `PayoutTx` census inverted rather than deleted.
- **[ADR-291](../decisions/ADR-291.md)**, `proposed`, UNSIGNED.

### 4. Next

**Row `291` is complete.** [ADR-287](../decisions/ADR-287.md) slice 4 (`subject()`'s `null` arm, `gates`, and the `plan` leg short of the size decode) is unblocked and is the next build. Slices 6 and 7 wait on rows `289` and `290`, in flight beside this one. **Slice 9 does not become closer**, because it depends on slice 8 and slice 8's supplier is counsel.

### 5. Blockers

**The `E2` read on [ADR-291](../decisions/ADR-291.md) is OWED and has not happened.** Three things in it are decisions rather than transcriptions and are worth the founder's disagreement: `transact`'s handle-escape property held by shape, the deliberate refusal of an `idempotency` store that could answer today, and a malformed identity row answering 500 rather than 503.

### 6. Landmines

- **A PORT IMPLEMENTATION WITH NO TYPE ANNOTATION IS INVISIBLE TO ITS OWN CENSUS.** F2 in [ADR-291](../decisions/ADR-291.md). This session hit it and caught it. The pattern is general: any census keyed on `: PortName` reports PASS over an adapter satisfied by contextual typing alone.
- **`idempotency` LOOKS LIKE A FREE WIN AND IS NOT.** The real store is one import away and installing it is what `usePayoutBackend`'s entry refuses in writing.
- **THE LOCK QUESTION IS OPEN AND BELONGS TO SLICE 6**, not to whoever notices it first.

### 7. Files touched

`apps/api/src/payout-backend.ts` (new), `apps/api/test/payout-backend.test.ts` (new), `apps/api/test/wiring.test.ts`, `apps/api/test/rule-state-producibility.test.ts`, `docs/decisions/ADR-291.md` (new), `docs/decisions/ALLOCATION.md`, `docs/STATE.md`, this log, and the two generated registry READMEs.
