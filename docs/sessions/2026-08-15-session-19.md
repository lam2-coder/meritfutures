## 2026-08-15 - Session 19: PR #7 and PR #8 reconciled into one branch, ADR-035 accepted, P1 measured
<!--index: PR #7 and PR #8 reconciled into one branch, ADR-035 accepted, P1 measured -->

- **Done.** Two open pull requests overlapping on 11 of 13 files, both carrying an independently written `scripts/corpus/gates.mjs`, are one mergeable branch. Nothing was dropped and what was dropped is in writing.
  - **The founder's ruling on the runner, and the criterion is the part worth carrying.** PR #8's `gates.mjs` is the base **because it had been falsified**: 109 phantom broken anchors and 119 phantom refless edge cases, both traced to bugs in the runner rather than to the corpus, both fixed, and only then did it find 27 real broken anchors. PR #7's had not been watched fail correctly. PR #7's `.github/workflows/corpus.yml` was taken **unchanged**, as ruled.
  - **Ported from PR #7 into the base**, each one real coverage the base lacked: **[ADR-026](../decisions/ADR-026.md)'s manifest completeness gate** (PR #8 had no equivalent, and it is the gate that found `U-06`); **CI-06h's tree half** plus the `sql_tables` / `sql_triggers` / `manifest_changes` / `index_entries` span queries; CI-06d contiguity; CI-06b `depends_on` resolution; CI-06a duplicate-heading and `<a name>` anchors; the `anchors` subcommand.
  - **The merge failed CI-06g on arrival**, and correctly: PR #7 wrote four generated spans into `STATE` and PR #8's runner had no queries for them. **A gate catching a merge defect in the one hour it existed as a merge** is the cleanest argument for the gate that exists in this log.
  - **[CI-06j](../testing/STRATEGY.md), by neither branch.** Every `NEW.`/`OLD.` column a PL/pgSQL trigger body names must resolve against the columns the migrations declare. **It found [ADR-035](../decisions/ADR-035.md)'s defect from the tree, with no database, on its first run.** LEDGER-C2's idea applied to columns.
  - **[`scripts/corpus/falsify.mjs`](../../scripts/corpus/falsify.mjs).** Every gate is run against the tree, where it must PASS, and against a copy carrying one seeded violation aimed at it, where it must FAIL **on that finding**. **All 11 gates pass clean and fail dirty.**
  - **[ADR-035](../decisions/ADR-035.md) ACCEPTED**, fixed by **[`0028_supersede_plan_version_immutability.sql`](../../packages/db/migrations/0028_supersede_plan_version_immutability.sql)**. `0027` is not edited. Verified on a clean PostgreSQL 16 install of all 28: **96 tables, 326 indexes, 347 check constraints, 6 triggers**, unchanged from 27 because `0028` adds no object.
  - **[`scripts/db/probe_plan_version_immutability.sql`](../../scripts/db/probe_plan_version_immutability.sql), 14 assertions, all passing**, and it **leads with the permitted transition succeeding**.
  - **TASK 4 reported in [STATE](../STATE.md)** rather than only in chat: what remains of P1, item by item.
- **Next.** The founder's E2 read, now on **18** money-path migration files including `0028`. Then the **monorepo scaffold**, which is the whole of what P1 has left and what every remaining VG gate is blocked on.
- **Blockers.** None. **`OI-01` is open and stays open**: the session was instructed not to decide `liability_snapshots` and did not.
- **Landmines.**
  - **`0028` widens a money-path guard beyond what ADR-035 proposed, in two places, and the E2 read must cover both.** (1) **A retired row is now frozen absolutely.** `0027`'s guard fired only when `OLD.status = 'published'`, so a retired row was completely unguarded: **retire a version, rewrite its `rules`, and every account pinned to that `plan_version_id` silently trades under new rules.** That is the two-step form of the exact retroactive change B4 #12 exists to prevent. [STATE_MACHINES section 9](../architecture/STATE_MACHINES.md) already rules retirement terminal, so this enforces the approved design rather than extending it, but **it is enforcement that did not exist before**. (2) **The pinned set is derived from the row rather than listed**, so a future `ALTER TABLE ADD COLUMN` is pinned automatically. Both are argued in the ADR's amendment table and in the migration header.
  - **`public_visible` had to be permitted to move, and finding that out required executing the fix.** `plan_versions_visible_implies_published` forbids a visible non-published row, so pinning `public_visible` would have made retiring a version that is on sale impossible: **a second undelistable-plan bug, against a different constraint, produced by the fix for the first one.** A reading would not have caught it.
  - **"Six further `CHECK` constraints" was wrong: there are SEVEN.** [DELTA_MANIFEST section 12](../../packages/db/DELTA_MANIFEST.md) said six above its own list of seven, and **the reconciliation brief quoted the six onward**, so the wrong count propagated into the instruction. Corrected and line-cited. **Eighth hand-maintained count found wrong, in the section recording the seventh.** The migration header then said "Three items" above a list of four; ninth, caught in the same file.
  - **`falsify.mjs` found three real problems on its first run and two of them were in the harness itself.** CI-06a and CI-06c "failed" on a tree the harness had copied incompletely, and a gate failing for a reason nobody planted proves nothing; every seed now names a substring its finding must contain. CI-06e's seed landed on the convention paragraph above EC-001, which the gate correctly ignores: **the seed was wrong and the gate was right**, the same shape as the 109 phantom anchors. **A falsification harness needs falsifying too**, which is the recursion this note exists to flag rather than resolve.
  - **CI-06j was wrong about `0028` before it was right.** A superseding migration rebinds a guard with `CREATE OR REPLACE FUNCTION` and does **not** recreate the trigger, and the single-pass parser let the later definition wipe the earlier attachment, so a live guard read as orphaned. Definitions and attachments are now collected in separate passes. **Every superseding migration from here will hit this path**, and it is now covered.
  - **TWO artifacts are NOT wired into `corpus.yml`, and the entry first recorded only one.** The ruling was to take PR #7's workflow unchanged and adding a step is a change, so neither `falsify.mjs` (three lines in `integrity`) nor **`probe_plan_version_immutability.sql`** (one line in `migrations`, which today runs only `probe_ledger_constraints.sql`) was added. **The second matters more than the first**: ADR-035's guard is verified by hand in this session and by nothing after it, which is exactly the condition that let the `NEW.config` defect live inside `0027`. **A probe that ships beside a fix and never runs again is the same object as the golden test that was missing.** Caught by reading the CI log rather than by re-reading the entry.
  - **`CI-06h` ran in GitHub Actions for the first time on PR #9 and PASSED.** The runner's own database reported 96 tables, 326 indexes, 347 check constraints, 6 triggers; the re-apply was rejected (`type "identity_status" already exists`); the ledger probes fired 3/3. The prediction that a services block working on a laptop is not one working on a runner was worth making and did not fire. **Cosmetic, recorded so nobody chases it:** the Postgres container logs one `FATAL: role "root" does not exist` from the `pg_isready` health check, which is the health check and not the schema.
  - **`DATA_MODEL` cited the immutability trigger as `plan_versions_immutable_when_published`; `0027` has always called it `plan_versions_published_immutable`.** A citation that does not resolve, in the paragraph describing the corpus's most valuable promise, one line from the trigger that was reading a column that did not exist. Corrected. **No gate checks constraint and trigger names cited in prose**; CI-06j checks columns. That is the next gate of this shape and it is not written.
  - **Local `main` in a fresh clone can point at an unrelated `Create README.md` commit.** `git diff main <branch>` then reports every file as new and hides the real diff entirely. Use `origin/main`.
- **Files touched:** `scripts/corpus/{gates.mjs,falsify.mjs}`; `scripts/db/probe_plan_version_immutability.sql` (new); `packages/db/migrations/0028_supersede_plan_version_immutability.sql` (new); `packages/db/DELTA_MANIFEST.md`; `docs/{STATE,DECISIONS,INDEX,SESSION_LOG}.md`; `docs/testing/STRATEGY.md`; `docs/architecture/DATA_MODEL.md`; `.github/workflows/corpus.yml` (merged from PR #7, unchanged).

**Amended 2026-08-15, after the P1 planning session read this branch.** INDEX's migration row stated **"27 files, all 93 schema deltas"** and **"Sixteen carry an `E2 READ: MONEY PATH` header"** against 28, 94 and 18 on disk. **The third is not a cosmetic count: it states the scope of the founder's E2 read, two files short.** Following it would have left `0028` and one other money-path file unread, on the branch whose entire remaining gate is that read. Converted to `migration_files`, `manifest_changes` and `e2_files` spans, all three of which already had queries. **The tenth hand-maintained count found wrong, and the first one found by a session other than the one that wrote it.**

**All eleven gates passed over it, correctly.** [CI-06g](../testing/STRATEGY.md) checks the spans that exist and **does not sweep for bare numerals adjacent to a registry noun**; the runner's own `covers` line says so and STRATEGY names it as one of four stated coverage gaps. **A documented gap is still a gap, and this one cost a real defect within a day of being documented.** The sweep is the next thing CI-06g needs and it is not written.

---
