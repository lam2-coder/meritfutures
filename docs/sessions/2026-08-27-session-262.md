---
status: approved
depends_on: [README.md]
last_updated: 2026-08-27
---

## 2026-08-27 - Session 262: SC-M4-07 rendered, and the no-proxy clause asserted by trying to break it

<!--index: THE FIRST `apps/portal` BUILD THAT PRODUCES A ROUTE. `apps/portal/src/app/kyc/` renders `SC-M4-07` from `view/kyc.ts` and `view/disclosure.ts` and rewrites neither, and `pnpm --filter @merit/portal build` exits **0** with `/kyc` listed **dynamic**. **[ADR-114](../decisions/ADR-114.md) clause 6 is enforced in BOTH of its places and the clause is asserted by TRYING**: a payload carrying a passport, a nested selfie URL, `INV-M19-09`'s provider reason code and a birth date is handed to the render path, and the emitted bytes are searched for the VALUES with every needle proven findable first. It refuses, names KEY PATHS, and quotes no value. **M04 section 7.9's string catalogue now EXISTS and its lint runs**, which [`view/kyc.ts`](../../apps/portal/src/view/kyc.ts) reported as owed and unbuildable because a view model authors no sentences. **THREE DISPATCH CLAIMS CHECKED AND ONE IS WRONG**: the build does not need session 250. **`CI-07` reopened on the artifact this page IS**, reported with a written-out patch rather than weakened, and **session 250 then landed that patch and every other item this session reported as owed**, so the segment is now WIRED to the real root layout and `pnpm run verify` exits **0**: 11 of 11 invariants, 33 of 33 gates, falsify clean. `ADR-150`'s condition did NOT fire and the number stays free -->

- **Objective:** render the KYC status and disclosure screens as a real page under `apps/portal/src/app/`, with `ADR-114` clause 6 asserted rather than described.
- **Done:** **`apps/portal/src/app/kyc/`, four files, and `apps/portal/test/kyc-page.test.ts` at 22 passed**, shipped unwired and then **WIRED to session 250's root layout** when it landed mid-session (section 9). `pnpm run verify` **exit 0**: typecheck, lint and `format:check` clean, **11 of 11 invariants**, **2,772 passed / 6 skipped over 154 files**, **33 of 33 gates**, `falsify` clean. `pnpm --filter @merit/portal build` **exit 0**, with `/` static and `/kyc` **dynamic**. **NO ADR: the `150` condition did not fire.**
- **Next:** the wiring slice that gives `KycScreenSource.status()` an implementation reading `GET /kyc/status` on the API origin, and the `verify` control's handler, which must be a client component and never a Server Action. Then the `jsx: preserve` question in section 9, without which no `.tsx` component in this workspace can be rendered by a test.
- **Blockers:** **NONE on this branch.** `CI-07` was red here for three hours and is CLOSED, by session 250 writing the `build-and-bundle` stage this session had proposed as a patch it could not take. One repository-wide red remains and it is NOT this branch's: `CI-05 security static` fails on three gitleaks findings that live only on `origin/claude/routes-affiliate`, **confirmed red on `main` itself**, section 10.
- **Landmines:** **the `verify` button carries no handler**, and the thing it must never grow is a Server Action. **No test in this repository can import a `.tsx` file**, so session 250's `layout.tsx` and `page.tsx` are unreachable by the suite; section 9 measures it. The page's own throw-when-unwired behaviour is GONE and that is deliberate, not a relaxation: the obligation moved to a file that cannot skip it.
- **Files touched:** [`apps/portal/src/app/kyc/copy.ts`](../../apps/portal/src/app/kyc/copy.ts), [`source.ts`](../../apps/portal/src/app/kyc/source.ts), [`screen.ts`](../../apps/portal/src/app/kyc/screen.ts), [`page.ts`](../../apps/portal/src/app/kyc/page.ts), [`apps/portal/test/kyc-page.test.ts`](../../apps/portal/test/kyc-page.test.ts), [ALLOCATION](../decisions/ALLOCATION.md), [sessions README](README.md), this file. **`view/kyc.ts` is UNCHANGED. `view/disclosure.ts` is UNCHANGED and, after the wiring, is no longer rendered by this segment at all**, which is section 9, and section 3 says what rendering asked of each and why neither needed an edit.

---

### 1. What the page is, and the two files it renders rather than rewrites

| File | What it is |
|---|---|
| [`page.ts`](../../apps/portal/src/app/kyc/page.ts) | The App Router page at `/kyc`. `export const dynamic = 'force-dynamic'`, because the App Router's default is to prerender a page with no dynamic input and a prerendered KYC status is one trader's verification state served to every trader from a cache |
| [`screen.ts`](../../apps/portal/src/app/kyc/screen.ts) | The pure render. Synchronous, no `Promise`, no IO, which is what lets the suite hand it a payload and read the bytes |
| [`source.ts`](../../apps/portal/src/app/kyc/source.ts) | The port, the runtime screen, and a default that refuses every method |
| [`copy.ts`](../../apps/portal/src/app/kyc/copy.ts) | M04 section 7.9's string catalogue |

**`view/kyc.ts` decides what the screen may say and this page did not second-guess it.** `toKycStatusView` carries `SC-M4-07`'s two refusals, an unknown `kyc_status` member and section 7.9's vocabulary over the server's own sentence, and the page routes the payload through it rather than around it. A page that projected the wire shape itself would be a second, weaker copy of a check that already exists.

**`view/disclosure.ts` is what makes the footer impossible to omit.** `PublishedDisclosure.text` is a `DisclosureBlock`, so a screen cannot be assembled from a literal, and `disclosureBlock()` refuses a blank one. The footer element is a SIBLING of the content branch rather than a child of it, so no content state can drop it.

---

### 2. THE CLAUSE, ASSERTED BY TRYING

> [ADR-114](../decisions/ADR-114.md) clause 6: **"MERIT NEVER PROXIES A DOCUMENT, AND THE INVARIANT IS ENFORCED IN TWO PLACES BECAUSE ONE OF THEM CANNOT SEE INSIDE A JSONB BLOB.** The port has no method returning a document and no field one could be assigned to. The receiver SCREENS a verified payload before anything stores it, **REFUSES rather than redacts**, and records **key paths and never values**."

**Both places are here, one deployable over, and neither alone is the control.**

| Place | What it is | What it cannot see |
|---|---|---|
| **The type** | `KycScreenSource.status()` returns `KycStatus`, API_CONTRACT section 7's five fields. There is no `documents()`, no `hostedUrl()` and no `applicantRef()`, and no field an image could be assigned to | A wire payload. TypeScript is structural and JSON is not typed at all, so an object carrying `document_image` beside the five fields satisfies the type at compile time and arrives intact at run time |
| **The runtime screen** | `screenKycStatus` walks the payload that actually arrived, REFUSES on a document-shaped or screened-value key, and projects five fields by name | A field nobody has sent yet |

**A test that checks the status field renders passes on a page that also leaks a passport.** So the assertion is the other way round: the render path is handed a payload carrying four different shapes of leak, and the bytes it produced are searched for the VALUES.

| Shape | Key | Value planted |
|---|---|---|
| A document at the top level | `document_image` | a `data:image/jpeg;base64,` string |
| A screened value that is not a document at all | `rejection_reason` | `INV-M19-09`'s provider reason code |
| A PII attribute the surface has no field for | `date_of_birth` | a birth date |
| **A document NESTED inside the provider's own blob** | `provider.raw_result.selfie_url` | a vendor URL |

**Every needle is proven findable before it is proven absent**, on `falsify.mjs`'s rule that a check which cannot fail is not a check. What the page emitted, in full:

```html
<article data-screen="SC-M4-07"><main><h1>Identity verification</h1><section data-content="server_error"><p>We could not load this just now. Try again shortly.</p></section></main><footer data-disclosure="simulated-environment"><h2>About Merit</h2><p>Merit evaluations and funded accounts trade in a simulated environment. No order reaches a live market and no customer funds are traded.</p></footer></article>
```

**None of the four values, and none of the five KEYS either**, because a field name is a statement that the field exists and `data-field="document_image"` on an empty element would be a page announcing that Merit holds one. **The refusal itself names the four key paths, including the nested one, and quotes no value**, which is asserted the same way: each planted value is proven findable, then proven absent from `ScreenedFieldError.message`. That message is written to a log, and a message quoting what it screened out would be the leak it exists to stop.

**And the control for it**, without which "a passport is absent" is satisfied by a page that renders nothing: the same payload with the four keys removed renders `data-state="verified"`, `data-badge="verified"` and the state label.

**Two tiers, because "unknown" and "document-shaped" are different facts.** An unknown key is DROPPED by the five-field projection, since API_CONTRACT can add a field and a portal that refused every response carrying one would break on an additive change. A screened key is REFUSED, because a screen that quietly dropped the field would render correctly forever while the response kept carrying a passport to every other consumer of that endpoint.

**One assertion runs in the direction this check must never fail in**: no member of `SCREENED_KEY_TERMS` matches any member of `KYC_STATUS_FIELDS`. A term that matched a contract field would refuse every well-formed response, and the screen would look like a broken endpoint rather than like an over-broad list.

---

### 3. What rendering forced, and why neither view module changed

**Rendering found one real defect, and it was found by reading the bytes rather than by reasoning.** The first render put section 7.9's own prompt, *"You passed. One quick step to activate your funded account, about 2 minutes"*, under a heading reading *"Why you were asked"* on a verification that was **already verified**, and again on one that had **failed**. The corpus sentence is a call to action and it is only true at the trigger moment.

**The repair is in this segment's catalogue and not in `view/kyc.ts`.** `KYC_PLACEMENT_PROMPT` is the contextual prompt and renders only where the control shape is `verify`; `KYC_PLACEMENT_REASON` is a noun phrase that stays true after the check is over. **The condition reads the CONTROL SHAPE and not the state**, so a second copy of `NEXT_STEP` cannot drift out of step with the first.

| What rendering asked for | Whose it turned out to be |
|---|---|
| A trader-facing label for each `kyc_status` member | This segment's. `view/kyc.ts` says so itself: it decides the SHAPE of the next step and never its words |
| A sentence per `placement` member | This segment's, and it needed TWO per member, above |
| A refusal on a `placement` the CHECK cannot produce | This segment's. `view/kyc.ts` deliberately passes `placement` through as the server sent it, which is right for a value and wrong for copy: an unmapped member would render the raw wire token |
| The vocabulary lint section 7.9 asks for | This segment's, and section 4 below is not where it went |

**So `view/kyc.ts` and `view/disclosure.ts` are byte-identical to `main`.** Both were built to be rendered and both were.

**M04 SECTION 7.9's LINT NOW HAS SOMETHING TO READ.** [`view/kyc.ts`](../../apps/portal/src/view/kyc.ts) reported it owed and unbuildable in its own words: *"Section 7.9 asks for a lint over the portal's string catalogue. THERE IS NO STRING CATALOGUE HERE AND THE STRING IS NOT THIS MODULE'S."* That was right on both counts, and the second half is why: a view model that projects a wire shape authors no sentences, so there was nothing to lint. **A page authors sentences.** `traderFacingStrings()` gathers every one of them, derived from the catalogues rather than maintained beside them, and the suite runs `INTERNAL_TIER_TERMS` over all of them, reusing `view/kyc.ts`'s list rather than keeping a second copy. A second assertion runs `forbidden`, `not allowed`, `denied` and `permission` over the same set, because `INV-M4-07` keeps that vocabulary off the screen by TYPE and copy could reinstate by wording what the type refuses to carry.

**THERE IS NO JSX IN THIS SEGMENT AND THAT WAS MEASURED RATHER THAN PREFERRED.** [ADR-095](../decisions/ADR-095.md) foreclosure `F7` assigns the `jsx` and `dom` additions in `apps/portal/tsconfig.json` to the slice that writes the first page. **Six slices are writing a first page at once** and that file is in none of their fences, which is [ADR-114](../decisions/ADR-114.md) section 6's manifest collision in a different file. Executed on this tree:

| Spelling | `pnpm --filter @merit/portal run typecheck` |
|---|---|
| A `.tsx` component imported from a `.ts` file, tsconfig untouched | **exit 2.** `error TS6142: Module './probe.tsx' was resolved to ... but '--jsx' is not set` |
| The same component written with `createElement` in a `.ts` file | **exit 0**, unchanged |

JSX compiles to exactly these calls and nothing about the rendered output differs, so this segment is written in the spelling that needs no shared edit and the tsconfig line is reported to whoever holds it.

---

### 4. `CI-07` REOPENED ON THE ARTIFACT THIS PAGE IS, WAS NOT THIS SESSION'S TO CLOSE, AND WAS CLOSED BY THE SESSION THAT HELD THE FILE

**`pnpm run gates` is 31 of 32 on this branch.** The failure, verbatim:

```
FAIL   CI-06/gate-inventory
       docs/testing/STRATEGY.md:190 CI-07: the artifact "a page, layout or route file under
       apps/*/src/app/" HAS ARRIVED (apps/portal/src/app/kyc/page.ts is an App Router file),
       so the row reopened on the commit that added it and the stage is not written.
```

**`falsify.mjs` SEEDS THIS EXACT FILE SHAPE TO PROVE THE GATE FIRES**, which is as strong as evidence of design intent gets. Its case for this gate reads, in its own words:

```
seeded: an activation condition's artifact ARRIVING: an App Router file under
apps/*/src/app/ reopens CI-07
```

So the red is the gate doing the one thing a suite was written to prove it does. Its two reported problems are that same failure counted twice: `CI-06/gate-inventory` fails on the CLEAN tree, and its seeded case is therefore reported `FAILED OFF-TARGET` rather than as a fresh defect. **Nothing else in `falsify` moved**, and the base tree at `c947e39` reports `All 32 gates pass clean and fail dirty, 80 scope case(s) hold`.

**This is ADR-073's half that fails on good news, working exactly as designed**, and [ADR-095](../decisions/ADR-095.md) section 6 predicted this exact line: it moved `CI-07`'s artifact to *"a `page`, `layout` or `route` file under `apps/*/src/app/`"* and wrote that *"the row moves in this commit or the gate lies in the direction that looks like success."*

**AND IT IS NOW CLOSED, BY THE PATCH THIS SESSION WROTE OUT AND DID NOT TAKE.** Session 250 landed [`ci.yml`](../../.github/workflows/ci.yml)'s `build-and-bundle` job while this branch was open, running `pnpm --filter @merit/portal build` and, in the same job, `VG-2` the moment its chain expired. `gates.mjs check` on the merge result is **33 of 33** and `falsify` reports `All 33 gates pass clean and fail dirty`. **The row was red on this branch for three hours and no gate was weakened to make it green**, which is the outcome the fence rule exists to produce: the session that could not fix it wrote down exactly what the fix was, and the session that held the file applied it.

**The remedy the corpus names is out of this fence, and that is the finding.** An implemented leg must name a workflow under [`.github/workflows/`](../../.github/workflows/) and a top-level job key in it, and `.github/workflows/ci.yml` holds no `build` job today. Writing one means editing that file and moving the row in [STRATEGY section 4.1](../testing/STRATEGY.md), a frozen document. **This session holds neither**, and P4 section 11 rule 13 makes widening a fence and weakening a gate the same move.

**WHAT IS NEW IS THAT THE STAGE IS FINALLY WRITABLE, and it was executed rather than argued.** ADR-095's reason for not writing it was that *"a `CI-07` written today could only fail forever"*, because `next build` had nothing to build. Measured on both trees, with `NEXT_TELEMETRY_DISABLED=1`:

| Tree | `next build` in `apps/portal` |
|---|---|
| `main` at `c947e39` | **exit 1.** `Error: > Couldn't find any` `pages` `or` `app` `directory. Please create one under the project root` |
| this branch | **exit 0.** `✓ Compiled successfully`, and the route table reads `┌ ○ /_not-found` and `└ ƒ /kyc` |

**So the argument that kept the stage unwritten has expired, and a `build` job added today would PASS.** That is one job key and one row, and it belongs to session 250 or to a slice the founder assigns. Six branches land this artifact and whichever merges first turns `main` red the same way.

---

### 5. THREE DISPATCH CLAIMS CHECKED, AND ONE IS WRONG

A dispatch prompt is not a source.

| Claim | Verdict |
|---|---|
| *"`apps/portal` has 18 view modules and 3,484 lines that render nothing"* | **The lines are exact and the noun is loose.** `find apps/portal/src -name '*.ts'` at base returns **18** files totalling **3,484** lines, of which **12** are under `src/view/`; the other six are `index.ts`, `api/types.ts`, `format/money.ts`, `copy/copy-block.ts` and the two `shell/` modules |
| *"no `app/` directory, `next build` exits 1"* | **True at base**, reproduced above |
| *"if 250 has not landed, ship your segment honestly unwired rather than inventing a layout"* and *"if 250 landed: `pnpm --filter @merit/portal build` exits 0"* | **THE CONDITIONAL IS WRONG IN THE HELPFUL DIRECTION AND THE INSTRUCTION IS STILL FOLLOWED.** 250 has not landed and the build exits **0** anyway: `next@16.3.2` synthesises a root layout for an `app/` directory that has none, so a segment alone is buildable. **No layout was invented and none is committed**; the finding is that the build gate does not wait on one |
| *"`ADR-095` ruling 3", "`ADR-083` section 3", "`ADR-114` clause 6", "`M04` 1.1 mobile first", "`apps/api/src/routes/kyc.ts` (`GET /kyc/status`, `POST /kyc/session`)"* | **All five resolve.** ADR-083 section 3 is headed *"Why the API is its own deployable"* and carries the *"no privileged back door"* sentence ADR-095 ruling 3 governs on |

---

### 6. What was reported rather than resolved, and what happened to it

**Five of the six items were closed by session 250 within three hours**, which is the fence rule paying for itself: each was written down precisely enough to be applied by whoever held the file.

| # | Item | Outcome |
|---|---|---|
| **1** | **`CI-07`'s stage** | **CLOSED by session 250.** `build-and-bundle` in [`ci.yml`](../../.github/workflows/ci.yml), which is the patch this session wrote out. `VG-2`'s leg lands in the same job |
| **2** | **`apps/portal/tsconfig.json` has no `jsx` and no `dom` lib** | **CLOSED by session 250**, quoting `ADR-095` `F7` in the file. It chose `jsx: preserve`, which has a consequence nobody has hit yet: section 9 |
| **3** | **`next build` writes `next-env.d.ts` and `.gitignore` does not cover it** | **CLOSED by session 250.** `.gitignore:43` |
| **4** | **`NEXT_TELEMETRY_DISABLED`** (raised in the pull request's patch comment) | **CLOSED by session 250**, in [`next.config.mjs`](../../apps/portal/next.config.mjs), discharging `ADR-095` `F5` |
| **5** | **`ShellView.simulated_environment_disclosure` is a bare `string` where `DisclosureBlock` exists** | **ANSWERED rather than closed, and the answer is better than the finding.** [`layout.tsx`](../../apps/portal/src/app/layout.tsx) uses a plain string deliberately and says why: no contract row serves `content_documents`, so minting a `DisclosureBlock` there "would assert a provenance that does not exist". `ADR-138` section 6 carries the endpoint as owed |
| **6** | **The `verify` control has no handler**, and must never grow a Server Action | **STILL OPEN.** The wiring slice's |
| **7** | **No stylesheet** | **STILL OPEN.** Session 250's layout ships none either |

---

### 7. THE `jsx: preserve` CONSEQUENCE: NO TEST IN THIS REPOSITORY CAN RENDER A `.tsx` COMPONENT

**Found by trying to render this page inside the real root layout**, which is the composition check this session owed once session 250 landed. It cannot be written:

```
Error: Failed to parse source for import analysis because the content contains
invalid JS syntax. If you use tsconfig.json, make sure to not set jsx to preserve.
  Plugin: vite:import-analysis
  File: apps/portal/src/app/layout.tsx:92:10
```

**Both settings are individually right and together they close a door.** `jsx: preserve` hands JSX to the framework's bundler, which is `tsconfig.base.json`'s *"nothing in this repository is built by `tsc`"* applied one level down and is correct for `next build`. Vitest reads the same `tsconfig.json` and leaves JSX untransformed, so **any `import` of a `.tsx` file fails before one assertion runs.**

**Nothing has hit it yet because nothing else imports one.** `grep` over every `test/` directory in the workspace returns one file referencing a `.tsx` path, and it is this session's, which reads [`layout.tsx`](../../apps/portal/src/app/layout.tsx) as TEXT. That was luck and is now deliberate, with the reason written at the assertion.

**The consequence is the one that matters for M04.** Section 8.1 requires *"Visual and layout, 375px and 1280px, one per screen in section 3.1"*, and `DESIGN_SYSTEM` section 8's slop-score pass *"renders each page at 375px and 1280px"*. **A screen written as `.tsx` cannot be rendered by the suite at all today**, so every future component test in this workspace meets this first. It is one line in a Vitest config or one word in a `tsconfig`, and it belongs to whoever holds `apps/portal/tsconfig.json`. **Reported rather than taken**, on the same rule that left `CI-07` red rather than reaching for `ci.yml`.

### 8. The rendered page, with real data, AFTER the wiring

**No `<main>`, no `<footer>`, no band and no `<html>`: all four are the root layout's.** What this segment emits is the content and its own heading.

`kyc_required`, the `pre_funded` trigger, leading with section 7.9's own sentence:

```html
<article data-screen="SC-M4-07"><h1>Identity verification</h1><section data-state="kyc_required"><p data-field="prompt">You passed. One quick step to activate your funded account, about 2 minutes.</p><p data-field="state">Not started</p><dl><dt>Why you were asked</dt><dd>Activating your funded account.</dd></dl><p data-field="action_required">Start your identity check to activate your funded account.</p><button type="button" data-next-step="verify">Start verification, about 2 minutes</button></section></article>
```

`verified`, with the permanent badge and no prompt, because the check is over:

```html
<article data-screen="SC-M4-07"><h1>Identity verification</h1><section data-state="verified"><p data-field="state">Verified</p><p data-badge="verified">Verified</p><dl><dt>Why you were asked</dt><dd>Activating your funded account.</dd><dt>Verified on</dt><dd><time dateTime="2026-08-20T14:02:11Z">2026-08-20T14:02:11Z</time></dd><dt>Verify again by</dt><dd><time dateTime="2028-08-20T14:02:11Z">2028-08-20T14:02:11Z</time></dd></dl></section></article>
```

**THE CLAUSE still holds after the wiring**, on the same payload carrying a passport, a nested selfie URL, the provider reason code and a birth date:

```html
<article data-screen="SC-M4-07"><h1>Identity verification</h1><section data-content="server_error"><p>We could not load this just now. Try again shortly.</p></section></article>
```

**The badge follows the STATE and not `verified_at`**, asserted over a fixture that carries a verification date in every state including `expired`.

---

### 9. THE WIRING, WHICH ARRIVED MID-SESSION AND WAS THREE DEFECTS RATHER THAN ONE TIDY-UP

**Session 250 landed [`app/layout.tsx`](../../apps/portal/src/app/layout.tsx) while this branch was open**, and it owns `<html>`, `<body>`, the impersonation band, `<main>` and the `INV-M4-09` footer. This segment rendered the band, a `<main>` and the footer itself, because none of that existed when it was written, and [`screen.ts`](../../apps/portal/src/app/kyc/screen.ts)'s header said at the time that the page would compose the layout the moment one arrived.

**Merging without the repair would have shipped three defects**, and they are not equally serious:

| Defect | Weight |
|---|---|
| A second impersonation band | Wrong, and visible |
| A `<main>` nested inside the layout's `<main>` | Invalid document structure |
| **The `INV-M4-09` disclosure printed TWICE** | **The one that matters.** A doubled compliance obligation is not a safer failure than a missing one; it is a screen nobody can quote |

**So the port lost two of its three methods and that is the wiring rather than a narrowing.** `KycScreenSource` is now one method, `status()`. The band and the footer are the layout's, so a page that still fetched them could only render a second copy, and there is now no field on the port a second footer could arrive through, which is the same shape as the no-document argument one level over.

**The page no longer throws when unwired, and the new behaviour is the better one.** While this segment owned the footer, an unavailable disclosure had to refuse the render: a screen that cannot render a required disclosure must not render. The layout renders the footer around every page now and outside every branch this page can take, so an unavailable status is **content that failed** rather than **an obligation that failed**, and the honest answer is the error state inside chrome that is already there. **Nothing was relaxed; the obligation moved to a file that cannot skip it**, which is the layout's own argument about itself.

**The old chrome test is replaced by its INVERSE**, and that is the control worth keeping: this segment must emit no `<main>`, no `<footer>`, no `<html>`, no `<body>`, no band and no disclosure, in all five content states, while still emitting `data-screen="SC-M4-07"` so that "no chrome" cannot be satisfied by "no output". A second assertion reads the layout's source for all three, because a segment emitting no footer is also what a portal with no disclosure anywhere looks like.

---

### 10. THE ONE RED THAT IS NOT THIS BRANCH'S, ESTABLISHED BY EXECUTION

`CI-05 security static` fails at step **`VG-1 secret scan, full history`** on **3** gitleaks `generic-api-key` findings. Reproduced locally with the pinned binary, its digest checked against [`ci.yml`](../../.github/workflows/ci.yml)'s own `GITLEAKS_SHA256` before it was run, then isolated ref by ref:

| `--log-opts` | Commits | Result |
|---|---|---|
| `HEAD`, this branch's ancestry | 201 | **no leaks found** |
| `origin/main` | 214 | **no leaks found** |
| `origin/claude/routes-affiliate` | 195 | **leaks found: 3** |

All three are a `click_token` literal, in `apps/api/test/affiliate.test.ts` and one session log, on commits `6c1bb83` and `818e875`. **Neither is an ancestor of this branch and neither is on `main`**, and that test file does not exist in this working tree.

**`main`'s own run confirms it**: [33041172374](https://github.com/lam2-coder/meritfutures/actions/runs/33041172374) at `3f32575` is **red on `CI-05` at the same step**, with every other job green.

**The mechanism is worth a ruling and it is not this session's to take.** `VG-1` is *"gitleaks on the diff and the full history"*, `gitleaks git` scans **every ref in the checkout**, and `actions/checkout@v4` with `fetch-depth: 0` brings down every branch anyone has pushed. **One session's unmerged fixture therefore turns the gate red for every other session at once**, which is a much wider blast radius than the row's wording suggests. Corroborated incidentally: two runs eight minutes apart scanned 1,470 and then 1,472 commits, the difference being commits pushed to somebody else's branch in between.

---

### 11. Why no ADR

**`ADR-150` was reserved CONDITIONALLY**, for *"the case where rendering forces a ruling on what a KYC screen may show"*. **It did not fire, and the number stays free.** Every refusal this page makes transcribes a ruling that is already taken: [ADR-114](../decisions/ADR-114.md) clause 6 rules the no-proxy invariant and both of its enforcement points, `INV-M19-09` already keeps the provider's reason code off every response, `INV-M4-09` and constitution section 6 already make the disclosure an obligation, and M04 section 7.9 already rules the vocabulary. Enumerating `SCREENED_KEY_TERMS` and splitting a prompt from a reason are implementation inside a settled rule, and an ADR for either would spend a founder gate relitigating a sentence the corpus has already written.

**What did arrive is a finding rather than a ruling**, and it is `CI-07`'s row rather than a KYC question. [ALLOCATION](../decisions/ALLOCATION.md)'s `150` row is amended in place per [ADR-065](../decisions/ADR-065.md) T3 to say so.
