---
status: approved
depends_on: [README.md, ../../decisions/ADR-072.md, ../../decisions/ADR-076.md, ../../decisions/ADR-125.md]
last_updated: 2026-08-27
---

## 39. Fixture status and blockers

**One row per registered scenario, and the reason lives in a column rather than in prose.** [ADR-072](../../decisions/ADR-072.md) is the ruling: a fixture is writable when three checkable conditions hold, and **every row without a fixture carries a stated blocker from a closed vocabulary with a citation that supports it**. [Section 33.4](33-ownership-index-and-coverage-reconciliation.md) is the same idea at three rows; this is it at 276.

**`status` is derived from the directory and not from this file.** `written` means a `GS-NNN-*.yaml` and its `.expected.json` sibling are on disk in [`packages/rules-engine/fixtures`](../../../packages/rules-engine/fixtures/README.md). `writable` means the assertion can be written today, and since [ADR-076](../../decisions/ADR-076.md) it names WHERE. `covered-elsewhere` means it is already written and RUNS, in a suite the citation names. `blocked` means at least one of `ADR-072`'s conditions fails, and the `blocker` names **the one that would still hold if every other were cleared**.

### `covered-elsewhere`, and the word that carries it is EXECUTED

[ADR-076 section 1](../../decisions/ADR-076.md) is the ruling and this is its rule in its own terms: **a registry row is discharged when its assertion is EXECUTED somewhere a gate can read, not when a file appears in [`packages/rules-engine/fixtures`](../../../packages/rules-engine/fixtures/README.md).** So a row is `covered-elsewhere` when **both** of these hold, and neither alone is enough:

1. the scenario has **no golden fixture**, and
2. a named suite outside that directory **runs an assertion for it**, in an ordinary `pnpm exec vitest run`, and the case is reported as a PASS.

A row in this status carries **no blocker**, because nothing is blocked. It is a status and not a seventh blocker term for a checkable reason: a blocker names which of `W1`, `W2`, `W3` fails, and on these rows none has.

**THREE THINGS LOOK LIKE COVERAGE AND ARE NOT, and the reason this list is here rather than in the ruling is that all three are true of a row this document nearly moved.**

- **A path that resolves is not a running assertion.** A file exists whether or not anything in it executes.
- **A file that contains the `GS-nnn` is not a running assertion.** Those ids were written into the suites by [session 123](../../sessions/2026-08-21-session-123.md) as comments, on purpose, so a gate could find them. A comment is a claim about a file, and a refactor can leave one behind.
- **A `describe` that names the row is not a running assertion when the block is skipped.** `describe.skipIf(condition)` with the condition false reports a named skip, never enters its body, and leaves every grep in the tree passing.

**The third is not hypothetical, it is `GS-072`.** [ADR-076 section 6](../../decisions/ADR-076.md) counts it among seven rows moving to this status; its cited block is `describe.skipIf(!replayExists)` at [`replay-determinism.property.test.ts:231`](../../../scripts/demo/test/replay-determinism.property.test.ts), `replayExists` is `Object.keys(engine).includes('replay')` at [`:76`](../../../scripts/demo/test/replay-determinism.property.test.ts) and is false, and the body **throws** rather than asserting. **Six rows moved and not seven**, and the difference is one run of the suite. **The test for this status is therefore watched rather than read**: the citation is checked by executing it and seeing a PASS, not by opening the file.

| Status | Rows | |
|---|---|---|
| **written** | **43** | Every one M1-owned. Not one fixture on disk belongs to any other module |
| **writable** | **0** | **The term stays in the vocabulary and holds no row.** `GS-071` was the last one and it left for `covered-elsewhere` when its assertion landed |
| **covered-elsewhere** | **16** | `GS-071`, `GS-073`, `GS-076`, `GS-077`, `GS-078`, `GS-083`, `GS-100`, `GS-115`, `GS-141`, `GS-143`, `GS-144`, `GS-148`, `GS-285`, `GS-309`, `GS-310`, `GS-313`, each cited to the suite that executes it. **The last seven arrived together** and are [ADR-125](../../decisions/ADR-125.md) section 3.1: seven rows that read `blocked / no-fixture-format` while a suite in this tree asserted them and passed |
| **blocked** | **257** | By vocabulary term below |

| Blocker | Rows | The condition that fails |
|---|---|---|
| `no-fixture-format` | **220** | W2 as [ADR-072](../../decisions/ADR-072.md) minted it. **[ADR-125](../../decisions/ADR-125.md) rules the reason these rows STATED false**: the tree holds four fixture formats and all 227 citations were one sentence. The term is kept, and only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that entry's fence; on these 220 rows what binds is that the code has not been written |
| `format-cannot-express` | **16** | W2. The format exists and cannot state this input or this expectation |
| `vendor-call` | **14** | W3. `M02` is at `status: review`, the one module plan of twenty-one that is not approved |
| `outside-loader-boundary` | **4** | W1. The code exists and lives outside `@merit/rules-engine` |
| `open-question` | **2** | W3. `OI-16`, on `GS-004` and `GS-031` |
| `no-plan-record-value` | **1** | W2. `GS-079`, a value no plan document states |

**EVERY COUNT ABOVE WAS RECOMPUTED BY READING THE TREE AND NOT BY ADJUSTING THE PREVIOUS ONE.** The 43 is a directory listing: [`packages/rules-engine/fixtures`](../../../packages/rules-engine/fixtures/README.md) holds **43 `GS-nnn-*.yaml` and 43 `.expected.json`, over 43 distinct scenario ids**, and it held 43 against 40 `written` rows before this edit, which is the three-row gap `CI-06/fixture-inventory` was carrying in a register. The four status counts and the six blocker counts are counts of the rows below, taken from the rows below after they were edited.

**`writable` NOW HOLDS NO ROW, AND THE WAY THE LAST ONE LEFT IS WORTH SAYING OUT LOUD.** It means *"all three of [ADR-072](../../decisions/ADR-072.md)'s conditions hold"*, and `W1` is loader reachability: *"a symbol in `apps/worker` or `packages/rithmic` fails W1 whether or not it is implemented"*. **`GS-071` fails `W1` read that way and always will**, because replay runs in [`apps/worker`](../../../apps/worker/src/batch/replay.ts). It sat `writable` for one edit, naming the suite where its assertion could be written rather than the directory that cannot hold it; [session 129](../../sessions/2026-08-22-session-129.md) then wrote that assertion at the scale the row names, and [ADR-076 section 1](../../decisions/ADR-076.md)'s governing rule finished the move: **a row is discharged when its assertion is EXECUTED, wherever it runs**, so `W1` is a condition on the fixture directory rather than on the scenario. **`writable` is a term in the vocabulary with a count of zero**, which is what it should read when every row that could name a suite has one that runs. It is not retired and the vocabulary does not move: the next scenario whose assertion is writable and unwritten reads `writable` again.

**These counts are derived from the table below and they disagree with [WAVE-04 section 2](../../plans/WAVE-04-fixture-backlog-and-gate-inventory.md) on three of five.** The plan claims 16 writable against 0, 14 needing a ruling against 23 M1 rows blocked, and 3 held against 2. It agrees at 243 with no code, and it no longer agrees at 40 written, which is 43. **[ADR-072 section 3](../../decisions/ADR-072.md) is why**: the plan partitions on whether the code exists, which is W1, and the condition that binds M1 is W2. **The table is the answer and the plan is the error.**

**The M1 partition closes exactly.** [Section 33.1](33-ownership-index-and-coverage-reconciliation.md) owns 73 scenarios to M1; 43 are written, none is writable, 7 are covered elsewhere, and 23 are blocked. The other 243 rows are the 14 M2 rows and the 229 owned by M3 through M20 and INFRA.

### `GS-049` carries a disposition and not only a status, and this is it

**One fixture discharges the row and the row is NOT split.** [Its registry row](04-gs-030-to-gs-051-the-appendix-b4-battery.md) names three probe shapes and adds *"runs as a property suite as well as a fixture"*. [`GS-049-fuzz-adversarial-day-sequences.yaml`](../../../packages/rules-engine/fixtures/GS-049-fuzz-adversarial-day-sequences.yaml) carries **all three shapes** in one 21-day stream: the alternating 15,001c and 14,999c run on days 1 to 8, a flat grind on days 9 to 20, and the single 1,000,000c day into consistency math last, so the whole stream is the period it is measured against. **Two of the three are pinned whole**, including the assertion the file exists for, `profit_needed_to_dilute_cents: 2213334` as a CEILING.

**What is not in the stream is the third probe's LENGTH**, twelve flat days against the row's hundred, and the reason it is not a residual is arithmetic rather than effort: **a sibling pins one `end_state`, and the end state after twelve flat days and after one hundred is the same state, field for field**, because a flat day moves no balance, no floor and neither counter. A longer grind buys a golden file a longer `events` list and nothing else. The trajectory in between is what a property suite asserts, and the row itself names one.

**One thing was checked rather than accepted, and it did not hold.** The sibling names *"`RE-P-01`, `RE-P-03` and `RE-P-04`"* as where that trajectory is asserted. `RE-P-01` executes, at [`floor-monotonicity.property.test.ts:259`](../../../packages/rules-engine/test/floor-monotonicity.property.test.ts). `RE-P-03`'s content executes as `PT-02`, at [`win-days-monotonicity.property.test.ts:153`](../../../packages/rules-engine/test/win-days-monotonicity.property.test.ts). **`RE-P-04` executes nowhere in this tree**: no property suite asserts that `tradedDaysCount` is non-decreasing, which is [M01](../../plans/M01-rules-engine.md)'s `INV-09`. So the floor and the win-day count have the length and **the traded-day count does not**.

**That is a residual on `RE-P-04` and it is not a residual on this row.** The fixture pins `traded_days_count: 9` across the grind, so a counter that moved on a zero-fill day fails there; what is missing is the generated-sequence form, whose owner is the property suite and not a golden file. **The row moves to `written` on the directory**, per [ADR-072](../../decisions/ADR-072.md)'s rule that the status is a fact about the directory, and the missing `RE-P-04` suite is named here so it is not discovered a third time.

| id | status | blocker | citation |
|---|---|---|---|
| GS-001 | blocked | format-cannot-express | [`types.ts`](../../../packages/rules-engine/src/types.ts) declares `DailyMark` with nine fields and no instant, so a fill timestamp has no home in the day stream; [`rules.ts`](../../../packages/rules-engine/src/rules.ts) files `R-01` among the four the engine does not declare, discharged at ingest |
| GS-002 | written |  | [`GS-002-counters-advance-on-a-day-the-trader-did-not-trade.yaml`](../../../packages/rules-engine/fixtures/GS-002-counters-advance-on-a-day-the-trader-did-not-trade.yaml) |
| GS-003 | written |  | [`GS-003-half-day-counts-as-a-full-trading-day.yaml`](../../../packages/rules-engine/fixtures/GS-003-half-day-counts-as-a-full-trading-day.yaml) |
| GS-004 | blocked | open-question | `OI-16` asks whether any feed Merit will hold reports a whole-session halt at the granularity `R-04` consumes; [ADR-059](../../decisions/ADR-059.md) recommendation 3 was granted and the item is recorded in [STATE](../../STATE.md) |
| GS-005 | written |  | [`GS-005-day-with-fills-and-exactly-zero-pnl.yaml`](../../../packages/rules-engine/fixtures/GS-005-day-with-fills-and-exactly-zero-pnl.yaml) |
| GS-006 | written |  | [`GS-006-win-day-exactly-at-the-floor-counts.yaml`](../../../packages/rules-engine/fixtures/GS-006-win-day-exactly-at-the-floor-counts.yaml) |
| GS-007 | written |  | [`GS-007-win-day-one-cent-below-the-floor-does-not-count.yaml`](../../../packages/rules-engine/fixtures/GS-007-win-day-one-cent-below-the-floor-does-not-count.yaml) |
| GS-008 | written |  | [`GS-008-initial-floor-at-account-open.yaml`](../../../packages/rules-engine/fixtures/GS-008-initial-floor-at-account-open.yaml) |
| GS-009 | written |  | [`GS-009-floor-trails-a-new-closing-high.yaml`](../../../packages/rules-engine/fixtures/GS-009-floor-trails-a-new-closing-high.yaml) |
| GS-010 | written |  | [`GS-010-floor-never-retreats-after-a-losing-day.yaml`](../../../packages/rules-engine/fixtures/GS-010-floor-never-retreats-after-a-losing-day.yaml) |
| GS-011 | written |  | [`GS-011-trailing-floor-ignores-the-intraday-high.yaml`](../../../packages/rules-engine/fixtures/GS-011-trailing-floor-ignores-the-intraday-high.yaml) |
| GS-012 | written |  | [`GS-012-breach-compares-against-the-floor-at-open.yaml`](../../../packages/rules-engine/fixtures/GS-012-breach-compares-against-the-floor-at-open.yaml) |
| GS-013 | written |  | [`GS-013-low-exactly-at-the-floor-survives.yaml`](../../../packages/rules-engine/fixtures/GS-013-low-exactly-at-the-floor-survives.yaml) |
| GS-014 | written |  | [`GS-014-low-one-cent-below-the-floor-breaches.yaml`](../../../packages/rules-engine/fixtures/GS-014-low-one-cent-below-the-floor-breaches.yaml) |
| GS-015 | written |  | [`GS-015-floor-lock-engages-continuously-with-no-jump.yaml`](../../../packages/rules-engine/fixtures/GS-015-floor-lock-engages-continuously-with-no-jump.yaml) |
| GS-016 | written |  | [`GS-016-locked-floor-does-not-move-on-a-later-high.yaml`](../../../packages/rules-engine/fixtures/GS-016-locked-floor-does-not-move-on-a-later-high.yaml) |
| GS-017 | written |  | [`GS-017-eval-passes-at-exactly-the-profit-target.yaml`](../../../packages/rules-engine/fixtures/GS-017-eval-passes-at-exactly-the-profit-target.yaml) |
| GS-018 | written |  | [`GS-018-eval-one-cent-below-target-does-not-pass.yaml`](../../../packages/rules-engine/fixtures/GS-018-eval-one-cent-below-target-does-not-pass.yaml) |
| GS-019 | written |  | [`GS-019-eval-pass-resets-the-funded-phase-to-the-account-size.yaml`](../../../packages/rules-engine/fixtures/GS-019-eval-pass-resets-the-funded-phase-to-the-account-size.yaml) |
| GS-020 | written |  | [`GS-020-eval-consistency-defers-the-pass-and-dilution-clears-it.yaml`](../../../packages/rules-engine/fixtures/GS-020-eval-consistency-defers-the-pass-and-dilution-clears-it.yaml) |
| GS-021 | written |  | [`GS-021-consistency-skipped-when-period-profit-is-zero.yaml`](../../../packages/rules-engine/fixtures/GS-021-consistency-skipped-when-period-profit-is-zero.yaml) |
| GS-022 | written |  | [`GS-022-consistency-skipped-when-period-profit-is-negative.yaml`](../../../packages/rules-engine/fixtures/GS-022-consistency-skipped-when-period-profit-is-negative.yaml) |
| GS-023 | written |  | [`GS-023-consistency-at-exactly-the-threshold-passes.yaml`](../../../packages/rules-engine/fixtures/GS-023-consistency-at-exactly-the-threshold-passes.yaml) |
| GS-024 | written |  | [`GS-024-consistency-one-cent-over-the-threshold-defers.yaml`](../../../packages/rules-engine/fixtures/GS-024-consistency-one-cent-over-the-threshold-defers.yaml) |
| GS-025 | written |  | [`GS-025-withdrawable-is-balance-minus-size-minus-buffer-floored-at-zero.yaml`](../../../packages/rules-engine/fixtures/GS-025-withdrawable-is-balance-minus-size-minus-buffer-floored-at-zero.yaml) |
| GS-026 | blocked | format-cannot-express | the row pins `approved 150,000` and `clamp_reason: cap`, which live on `PayoutEvaluation`; [`run.ts`](../../../packages/golden-loader/src/run.ts) folds `advanceDay` and diffs a `RuleState`, and [`types.ts`](../../../packages/rules-engine/src/types.ts) gives `RuleState` neither field |
| GS-027 | blocked | format-cannot-express | the row pins `approved 120,000` and `clamp_reason: withdrawable`, both on `PayoutEvaluation`, which no fixture in this format ever sees ([`types.ts`](../../../packages/rules-engine/src/types.ts), [`run.ts`](../../../packages/golden-loader/src/run.ts)) |
| GS-028 | blocked | format-cannot-express | the row pins the tie resolving with `clamp_reason: none`, a `PayoutEvaluation` field absent from `RuleState` ([`types.ts`](../../../packages/rules-engine/src/types.ts)) |
| GS-029 | blocked | format-cannot-express | R-44's trader and firm split is on `PayoutEvaluation` and never reaches `RuleState`, so the row's `trader 90,001, firm 10,000` cannot be pinned ([`types.ts`](../../../packages/rules-engine/src/types.ts)) |
| GS-030 | blocked | format-cannot-express | the calendar record carries two keys per session, `trading_day` and `kind`, and states no session open and no close, so the 23 hour and 25 hour halves are not statable ([`cme-2026.json`](../../../packages/rules-engine/fixtures/calendars/cme-2026.json), `what_this_format_cannot_carry`) |
| GS-031 | blocked | open-question | `OI-16`, the same item: `halted` has no publisher, and [ADR-059](../../decisions/ADR-059.md) recommendation 3 granted the hold ([STATE](../../STATE.md)) |
| GS-032 | written |  | [`GS-032-half-day-the-trader-sat-out.yaml`](../../../packages/rules-engine/fixtures/GS-032-half-day-the-trader-sat-out.yaml) |
| GS-033 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-034 | blocked | outside-loader-boundary | the row needs a replay forward; [`replay.ts`](../../../apps/worker/src/batch/replay.ts) lives in `apps/worker` and the [golden-loader](../../../packages/golden-loader/README.md) imports the engine entry point and nothing else |
| GS-035 | blocked | format-cannot-express | the row turns on a request at 23:59:59 against a batch at 00:05; the day stream carries no time of day, and `INV-01` forbids the engine reading a clock ([ADR-059](../../decisions/ADR-059.md), which excludes R-06 for this reason) |
| GS-036 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"Two accounts, same identity, payout the same second"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-037 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"Rise webhook replayed 50 times"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-038 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, its subject is *"PSP duplicate and out-of-order delivery"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-039 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, its subject is *"Chargeback after a settled payout"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-040 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, its subject is *"Coupon race across two tabs"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-041 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, its subject is *"Plan v2 published while checkout is open on v1"*, and **shipped source CITES it as a reason and not as an assertion**: [`replay.ts:403`](../../../apps/worker/src/batch/replay.ts) names this row as why `plan_version_id` is an INPUT the engine never chooses and no config migration touches. A reason a file gives for its own shape is not the row's assertion executing. [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-042 | blocked | format-cannot-express | three request amounts on one row, and the format carries no payout request at all: `SETTLEMENT_KEYS` admits five fields of an already-approved fact ([`loader.ts`](../../../packages/golden-loader/src/loader.ts)) |
| GS-043 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-044 | written |  | [`GS-044-passes-eval-while-payouts-are-frozen.yaml`](../../../packages/rules-engine/fixtures/GS-044-passes-eval-while-payouts-are-frozen.yaml) |
| GS-045 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M8**, its subject is *"Affiliate buys through their own code"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-046 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, its subject is *"Identity merge after both identities were funded"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-047 | blocked | format-cannot-express | the row needs the same day applied twice, and `L-10` refuses a `trading_day` that is not after its predecessor ([`loader.ts`](../../../packages/golden-loader/src/loader.ts)); the loader rule that blocks the fixture is the claim the scenario exists to assert |
| GS-048 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"Restore from backup with payouts mid-queue"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-049 | written |  | [`GS-049-fuzz-adversarial-day-sequences.yaml`](../../../packages/rules-engine/fixtures/GS-049-fuzz-adversarial-day-sequences.yaml), on disk since session 117 with its `.expected.json` sibling. **The disposition is stated above this table**: one stream carries all three probe shapes, two are pinned whole, and the third probe's LENGTH is the property suite's because the end state after twelve flat days and after one hundred is the same state |
| GS-050 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, its subject is *"Six-account hedged syndicate rehearsal"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-051 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"500 simultaneous payout requests in one minute"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-052 | blocked | format-cannot-express | neither half is one account's day fold: three requests in one transaction window, and three wallet-to-rail withdrawals under `G-NO-IN-FLIGHT` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-053 | written |  | [`GS-053-stacking-attempt-across-the-settlement-boundary.yaml`](../../../packages/rules-engine/fixtures/GS-053-stacking-attempt-across-the-settlement-boundary.yaml) |
| GS-054 | written |  | [`GS-054-manufactured-dilution-days-from-a-hedged-pair.yaml`](../../../packages/rules-engine/fixtures/GS-054-manufactured-dilution-days-from-a-hedged-pair.yaml) |
| GS-055 | written |  | [`GS-055-minimum-variance-path-to-a-full-cap-extraction.yaml`](../../../packages/rules-engine/fixtures/GS-055-minimum-variance-path-to-a-full-cap-extraction.yaml) |
| GS-056 | written |  | [`GS-056-locked-floor-converts-the-account-into-a-free-option.yaml`](../../../packages/rules-engine/fixtures/GS-056-locked-floor-converts-the-account-into-a-free-option.yaml) |
| GS-057 | blocked | format-cannot-express | a correction needs a superseding mark (`R-11`, undeclared) and the absorbed amount needs a field: [`types.ts`](../../../packages/rules-engine/src/types.ts) gives `RuleState` neither an absorbed delta nor `corrected_days_in_period` |
| GS-058 | blocked | format-cannot-express | the same three clauses as GS-057: a superseding mark, a settlement to land after, and an absorbed delta `RuleState` does not carry ([`types.ts`](../../../packages/rules-engine/src/types.ts), [`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-059 | written |  | [`GS-059-holiday-cluster-compresses-the-cadence-gap-in-calendar-time.yaml`](../../../packages/rules-engine/fixtures/GS-059-holiday-cluster-compresses-the-cadence-gap-in-calendar-time.yaml), written by session 117 with its `.expected.json` sibling. The row read `writable` for five sessions after the fixture landed, which is the gap `CI-06/fixture-inventory` was holding in a register rather than reporting |
| GS-060 | written |  | [`GS-060-zero-risk-day-farming-buys-nothing-against-the-real-gates.yaml`](../../../packages/rules-engine/fixtures/GS-060-zero-risk-day-farming-buys-nothing-against-the-real-gates.yaml) |
| GS-061 | written |  | [`GS-061-peak-picking-requesting-on-a-local-maximum.yaml`](../../../packages/rules-engine/fixtures/GS-061-peak-picking-requesting-on-a-local-maximum.yaml) |
| GS-062 | blocked | format-cannot-express | ten copy-traded accounts under one identity; the engine is per account by design, and no per-account fold produces the identity-level forecast ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-063 | written |  | [`GS-063-breach-and-profit-target-on-the-same-day.yaml`](../../../packages/rules-engine/fixtures/GS-063-breach-and-profit-target-on-the-same-day.yaml) |
| GS-064 | written |  | [`GS-064-breach-and-payout-eligibility-on-the-same-day.yaml`](../../../packages/rules-engine/fixtures/GS-064-breach-and-payout-eligibility-on-the-same-day.yaml) |
| GS-065 | written |  | [`GS-065-settled-payout-drops-the-balance-toward-a-floor-that-does-not-move.yaml`](../../../packages/rules-engine/fixtures/GS-065-settled-payout-drops-the-balance-toward-a-floor-that-does-not-move.yaml) |
| GS-066 | blocked | format-cannot-express | the row needs a FAILED transfer that consumes no ordinal, and a failed transfer produces no `SettlementFact`: `L-11` admits five fields and none of them is a failure ([`loader.ts`](../../../packages/golden-loader/src/loader.ts)) |
| GS-067 | written |  | [`GS-067-graduation-fires-exactly-on-the-ladder-count.yaml`](../../../packages/rules-engine/fixtures/GS-067-graduation-fires-exactly-on-the-ladder-count.yaml) |
| GS-068 | written |  | [`GS-068-consistency-period-boundary-at-the-settlement-day.yaml`](../../../packages/rules-engine/fixtures/GS-068-consistency-period-boundary-at-the-settlement-day.yaml) |
| GS-069 | written |  | [`GS-069-adding-profit-on-the-best-day-breaks-a-passing-consistency-gate.yaml`](../../../packages/rules-engine/fixtures/GS-069-adding-profit-on-the-best-day-breaks-a-passing-consistency-gate.yaml) |
| GS-070 | written |  | [`GS-070-funded-start-balance-does-not-equal-size.yaml`](../../../packages/rules-engine/fixtures/GS-070-funded-start-balance-does-not-equal-size.yaml) |
| GS-071 | covered-elsewhere |  | EXECUTED by the `describe` at [`replay.test.ts:1076`](../../../apps/worker/test/replay.test.ts), which names this row in its own header, and the `it` at `:1077`: a **250-day funded life**, asserted the hash first as one array of 250 hex digests and then field by field at 43 comparisons a day, with the audit itself reporting `diverged: 0` against `matched`, `inScope` and `storedRows` each equal to 250. **THE STATUS WAS TAKEN BY EXECUTING THE CITATION AND NOT BY READING IT**, which is the distinction [session 123](../../sessions/2026-08-21-session-123.md) found on `GS-072` and [session 131](../../sessions/2026-08-22-session-131.md) built the gate assertion for: the file carries no `skipIf`, no `.skip` and no `.only`, and `pnpm exec vitest run apps/worker/test/replay.test.ts` on 2026-08-22 reports **62 passed and 0 skipped**. **THE ROW LEFT `writable`, WHICH NOW HOLDS NONE.** It read `writable` while the suite asserted four days and the row asks 250, which was a SCALE and not a shape; [session 129](../../sessions/2026-08-22-session-129.md) wrote it at the row's own scale as WAVE-05 `X5`, and [ADR-076 section 1](../../decisions/ADR-076.md)'s governing rule discharges a row whose assertion is EXECUTED wherever it runs. Line numbers read 2026-08-22 |
| GS-072 | blocked | outside-loader-boundary | arrival order across a replay, which runs in `apps/worker` ([`replay.ts`](../../../apps/worker/src/batch/replay.ts)) outside the loader import boundary ([golden-loader](../../../packages/golden-loader/README.md)). **THIS ROW DID NOT MOVE TO `covered-elsewhere` AND [ADR-076 section 6](../../decisions/ADR-076.md) SAYS IT DOES.** Its cited site is `describe.skipIf(!replayExists)` at [`replay-determinism.property.test.ts:231`](../../../scripts/demo/test/replay-determinism.property.test.ts), whose condition is `Object.keys(engine).includes('replay')` at [`:76`](../../../scripts/demo/test/replay-determinism.property.test.ts) and is false; the block is a named skip and its body throws. Re-run on 2026-08-22, printing *"PT-06: arrival-order permutation is UNEXPRESSIBLE (the engine exports no replay; P2-8)"*. **The block switches itself on the day the engine exports `replay`**, which [WAVE-05 section 3](../../plans/WAVE-05-tier2-fixture-shapes.md) calls self-clearing, and on that day this row moves without a ruling |
| GS-073 | covered-elsewhere |  | EXECUTED by `RE-D-02`, the `describe` at [`determinism.test.ts:245`](../../../packages/golden-loader/test/determinism.test.ts), which names this row at `:232` above it. Five cases run and pass, `TZ=Asia/Kolkata` with `LC_ALL=ja_JP.UTF-8` first, which is the row's own pair, and one of the five asserts the spawned locale genuinely changed so the comparison meant something. [STRATEGY section 3.1](../STRATEGY.md) rows `PT-06` as *"the executable form of GS-071 to GS-073"* ([ADR-076 section 3](../../decisions/ADR-076.md)) |
| GS-074 | blocked | outside-loader-boundary | a supersession replayed forward, in `apps/worker` ([`replay.ts`](../../../apps/worker/src/batch/replay.ts)), outside the loader import boundary ([golden-loader](../../../packages/golden-loader/README.md)) |
| GS-075 | blocked | outside-loader-boundary | a cross-version diff report, produced in `apps/worker` ([`replay.ts`](../../../apps/worker/src/batch/replay.ts)), outside the loader import boundary ([golden-loader](../../../packages/golden-loader/README.md)) |
| GS-076 | covered-elsewhere |  | EXECUTED by `RE-C-10`, the `it` at [`plan-validate.test.ts:405`](../../../packages/rules-engine/test/plan-validate.test.ts), and a second time through `SEEDS['CV-10']` at `:789` under the oracle `it.each` at `:833`, which runs the seeded violation past `validatePlan` AND the independently transcribed oracle. This row is named at `:91`. **Its seed's error set is pinned EXACTLY**, at the `it.each` at `:859`, which is WAVE-05 `X4`. Line numbers re-derived by grep 2026-08-22 after [session 128](../../sessions/2026-08-22-session-128.md) moved every one of them; the stable locators are the two test ids ([ADR-076 section 2](../../decisions/ADR-076.md)) |
| GS-077 | covered-elsewhere |  | EXECUTED by `RE-C-06`, the `it` at [`plan-validate.test.ts:314`](../../../packages/rules-engine/test/plan-validate.test.ts), which asserts BOTH of this row's halves, 0 bp and 10001 bp refused against 1 and 10000 passing, and again through `SEEDS['CV-06']` at `:780` under the oracle `it.each` at `:833`. This row is named at `:83`. **Its seed's error set is pinned EXACTLY**, at the `it.each` at `:859`, which is WAVE-05 `X4`. Line numbers re-derived by grep 2026-08-22 after [session 128](../../sessions/2026-08-22-session-128.md) moved them ([ADR-076 section 2](../../decisions/ADR-076.md)) |
| GS-078 | covered-elsewhere |  | EXECUTED by `RE-C-01`, the `it` at [`plan-validate.test.ts:240`](../../../packages/rules-engine/test/plan-validate.test.ts), which refuses `intraday_trailing` on the funded phase AND on the eval phase and pins the reported `path`, and again through `SEEDS['CV-01']` at `:765` under the oracle `it.each` at `:833`. This row is named at `:74`, the one citation in this block [session 128](../../sessions/2026-08-22-session-128.md) did not move. **Its seed's error set is pinned EXACTLY**, at the `it.each` at `:859`, which is WAVE-05 `X4`. Line numbers re-derived by grep 2026-08-22 ([ADR-076 section 2](../../decisions/ADR-076.md)) |
| GS-079 | blocked | no-plan-record-value | the row turns on a hard daily loss limit and all three plan records carry `"daily_loss_limit": null` ([`CORE-50K.json`](../../../packages/rules-engine/fixtures/plans/CORE-50K.json)), so a fixture would state a value no plan document states, which `TR-01` forbids |
| GS-080 | written |  | [`GS-080-a-gate-configured-to-zero-renders-as-disabled-not-as-satisfied.yaml`](../../../packages/rules-engine/fixtures/GS-080-a-gate-configured-to-zero-renders-as-disabled-not-as-satisfied.yaml), written by session 109 with its `.expected.json` sibling. It is the first of the three rows a fixture landed under while the registry did not move, and the oldest by eight sessions |
| GS-081 | written |  | [`GS-081-settlement-leaves-the-floor-the-high-water-balance-and-the-lock-untouched.yaml`](../../../packages/rules-engine/fixtures/GS-081-settlement-leaves-the-floor-the-high-water-balance-and-the-lock-untouched.yaml) |
| GS-082 | blocked | format-cannot-express | a per-day table of which gate binds on each of three days, and the expectation carries exactly one `end_state` ([`loader.ts`](../../../packages/golden-loader/src/loader.ts), `EXPECTATION_KEYS`) |
| GS-083 | covered-elsewhere |  | EXECUTED by its own `describe` at [`plan-validate.test.ts:964`](../../../packages/rules-engine/test/plan-validate.test.ts), four cases, all passing. The first asserts `idsOf(result)` **EQUALS** `['CV-17']` rather than containing it, so the file states that `CV-17` blocked the publish and not merely that something did; the second EXECUTES the row's arithmetic through `resolvePlan` and the day fold rather than reciting it. This site is the one of the eight that named its own row before [session 123](../../sessions/2026-08-21-session-123.md), three times, and the file's own header says it lives there because it cannot live in `fixtures/` ([ADR-076 section 2](../../decisions/ADR-076.md)) |
| GS-084 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-085 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-086 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-087 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-088 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-089 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-090 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-091 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-092 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-093 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-094 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, its subject is *"Account cap enforced per identity, not per email"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-095 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, its subject is *"Failover never retries a purchase at the second MID"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-096 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, its subject is *"Chargeback lands after a settled payout"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-097 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, its subject is *"Coupon restricted by purchase kind"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-098 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, its subject is *"Reset onto a changed plan version renders the rule diff"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-099 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, its subject is *"Webhook citing an unknown purchase reference"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-100 | covered-elsewhere |  | EXECUTED by the `test` at [`eligibility.test.ts:77`](../../../apps/portal/test/eligibility.test.ts), which names this row in its own title and asserts the WHOLE pin on a PASSING account: `consistency_meter.state` is `pass`, and `best_day_share`, `max` and `profit_needed_to_dilute` all render there rather than only on a failing one. The account-card half runs again at [`accounts.test.ts:139`](../../../apps/portal/test/accounts.test.ts). **The status was taken by EXECUTING the citation and not by reading it**: PASS, 0 skipped (2026-08-27). [ADR-125](../../decisions/ADR-125.md) |
| GS-101 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, its subject is *"Eligibility moves between dashboard render and confirm"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-102 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, its subject is *"Certificate verification: valid, unknown, revoked"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-103 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, its subject is *"Breach screen ordering at every breakpoint"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-104 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, its subject is *"Payout destination change enters a 48 hour cooling window"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-105 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, its subject is *"Eligibility notification names its trading day and links to the gates screen"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-106 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"A settled payout never appears as an adjustment on any mark"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-107 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"Name match scored across a realistic set"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-108 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"Ten correlated accounts under one identity approve on the same day"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-109 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"A freeze reaches its expiry with no decision made"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-110 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"A one cent per-transaction ledger imbalance"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-111 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"Settlement rail outage during a payout wave"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-112 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"Evidence pack redaction by declared audience"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-113 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"Loss ratio computed on a sample below the minimum"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-114 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"Alarm suppression expires and the alarm returns by itself"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-115 | covered-elsewhere |  | EXECUTED by the `describe` at [`liability.test.ts:51`](../../../apps/admin/test/liability.test.ts), `M6-A-06: GS-115 book`, five cases over THIS ROW'S OWN BOOK: 500,000c withdrawable, a 150,000c cap and 6 rungs producing 500,000c open liability, 150,000c bounded near-term and 900,000c remaining ladder exposure, with no two equal and each carrying a definition of what it is NOT. A sixth case at `:134` asserts that the pair this row exists for is not refused. **The file's own header declined this status on a ground [ADR-076](../../decisions/ADR-076.md) removed**, that a session fenced to `apps/admin` cannot register a fixture; a row is discharged when its assertion is EXECUTED, wherever it runs. PASS, 0 skipped (2026-08-27). [ADR-125](../../decisions/ADR-125.md) |
| GS-116 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"Evidence pack export burst"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-117 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"Reversing a protective state requires the reason typed first"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-118 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, its subject is *"Detection cadence beats extraction on the minimum-variance path"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-119 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, its subject is *"Three-leg rotation defeats pairwise correlation and not group variance"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-120 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, its subject is *"Queue ordering under manufactured noise"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-121 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, its subject is *"Household signals produce a soft link and never a merge"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-122 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, its subject is *"A detector run that finds none of its own canaries"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-123 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M8**, its subject is *"Chargeback lands after the commission was paid"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-124 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M8**, its subject is *"An affiliate whose referred buyers cluster on shared signals"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-125 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M8**, its subject is *"Ten thousand clicks with a near-zero conversion rate"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-126 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M8**, its subject is *"A required disclosure version is superseded"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-127 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M8**, its subject is *"An affiliate destination also receives trader payouts from unrelated identities"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-128 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"A payout credits the wallet in the same transaction it is approved"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-129 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"A wallet-to-rail withdrawal carries every external control"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-130 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"Wallet balances appear in Open Liability and reserve coverage"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-131 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"Account takeover against a funded wallet, both directions"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-132 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, its subject is *"Indicative data never reaches a money decision"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-133 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, its subject is *"Streaming feed loss degrades to the last closed session"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-134 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, its subject is *"A ring is a watched cluster on day 0, before any fill exists"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-135 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, its subject is *"Young-account fast path, positive and near-miss"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-136 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, its subject is *"Clique position-sum detects third-leg rotation intraday"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-137 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, its subject is *"Copy trading, allowed and prohibited"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-138 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-139 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"A ledger imbalance scopes only when locality is proven"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-140 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M8**, its subject is *"An affiliate destination change enters the 48 hour cooling window"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-141 | covered-elsewhere |  | EXECUTED by the three-plan `it.each` at [`plan-validate.test.ts:169`](../../../packages/rules-engine/test/plan-validate.test.ts), three cases, all passing: an EXACT-SET equality of the diff ids each v1 plan emits, `PW-02a` on Core EOD and Direct and `PW-02b` on Merit Rapid, with nothing else emitted. This row is named at `:157`. **THE RESIDUAL THIS CELL RECORDED IS LANDED AND THE CELL SAYS SO RATHER THAN STILL CLAIMING IT**: the row's last sentence, *"asserts the two are never rendered identically"*, is the `info` against `warning` severity and the distinct text, which `diffIds` cannot reach because it compares ids, and WAVE-05 `X3` wrote it as the `it` at `:190`, which pins both severities and `textsDiffer` in ONE `expect` because no two of the three clauses are the row's claim. Line numbers re-derived by grep 2026-08-22 after [session 128](../../sessions/2026-08-22-session-128.md) moved them ([ADR-076 section 2](../../decisions/ADR-076.md)) |
| GS-142 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M9**, its subject is *"A plan version publishes and revalidation fails"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-143 | covered-elsewhere |  | ADR-097's stated exit, reached: [`content-lint.test.ts`](../../../apps/site/test/content-lint.test.ts) runs the row's own assertion, "MDX content containing a bare parameter value. The build fails", against `assertAuthoredContentIsClean` in [`lint.ts`](../../../apps/site/src/content/lint.ts), and asserts the clean twin BUILDING in the same file, because a lint tested only where it refuses is indistinguishable from one that refuses everything. 19 case(s) PASS in `pnpm exec vitest run` (2026-08-26), and 16 specimen(s) run again as a CI-01 step. Never `written`: the subject is a compiler refusal rather than a day stream |
| GS-144 | covered-elsewhere |  | ADR-097 section 6 held this row `blocked` until the OG leg ran, and it runs. [`og-card.test.ts`](../../../apps/site/test/og-card.test.ts) builds the social card through [`og.ts`](../../../apps/site/src/routes/og.ts) and fails the build on a statistic whose window is empty and on a bare figure in the card title, description or alt text; [`content-lint.test.ts`](../../../apps/site/test/content-lint.test.ts) fails it on an `og_*` frontmatter surface while the body is clean. Both legs PASS in `pnpm exec vitest run` (2026-08-26), against the four comments and no implementation session 186 measured. The first leg, `assertWindowAttached`, now has the call site the row names |
| GS-145 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M9**, its subject is *"A restricted-country visitor, with and without a VPN"*, and **HALF of it is asserted and passing**: the notice half runs at [`geo.test.ts:15`](../../../apps/site/test/geo.test.ts) and `:26`, PASS 2026-08-27. The other half, *"checkout refuses server side in BOTH cases"*, has its code at [`checkout.ts`](../../../apps/api/src/routes/checkout.ts) (`GEO_RESTRICTED`, `geoDecision`) and no assertion in [`checkout.test.ts`](../../../apps/api/test/checkout.test.ts). [ADR-125](../../decisions/ADR-125.md) section 4 names this a `writable` candidate it does NOT take, because the paragraph declaring `writable` empty is prose above this table and outside that entry's fence. **The term is kept for [ADR-125](../../decisions/ADR-125.md) clause 3's reason and its stated one is FALSE**: the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that entry's fence |
| GS-146 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M9**, its subject is *"A `copy_block` whose wording contradicts its rule's operator"*, and **shipped source NAMES it and puts the check elsewhere**: [`rules.ts:22`](../../../apps/site/src/routes/rules.ts) states *"GS-146 IS NOT ENFORCED HERE AND THAT IS THE RIGHT BOUNDARY"*, because the operator check is the PUBLISH path's ([M01](../../plans/M01-rules-engine.md) section 2.4). [`validate.ts`](../../../packages/rules-engine/src/plan/validate.ts) carries no `copy_block` check, so the publish path this row asserts against does not exist yet. [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-147 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M9**, its subject is *"Payout copy with one leg omitted"*, and **THREE of its four surfaces are asserted and passing**: a headline, a social card and an email subject, at [`disclosure.test.ts:70`](../../../apps/site/test/disclosure.test.ts) to `:85`, PASS 2026-08-27. The fourth is the OG card and it is not wired: `payoutCopyOmitsALeg` is called from neither [`lint.ts`](../../../apps/site/src/content/lint.ts) nor [`og.ts`](../../../apps/site/src/routes/og.ts). [ADR-125](../../decisions/ADR-125.md) section 4 names this a `writable` candidate it does NOT take, for the reason `GS-145` carries. **The term is kept for [ADR-125](../../decisions/ADR-125.md) clause 3's reason and its stated one is FALSE**: the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that entry's fence |
| GS-148 | covered-elsewhere |  | EXECUTED by four cases naming this row in their own titles, covering all four clauses of the pin: [`paths.test.ts:67`](../../../apps/site/test/paths.test.ts) (resolves, names its successor, leaves index and nav) and `:87` (exclusion is from indexing AND from navigation, never one of the two), and [`rules-page.test.ts:144`](../../../apps/site/test/rules-page.test.ts) (labeled, names its successor, excluded) and `:160` (the notice that the superseded rules still govern accounts opened under them). PASS, 0 skipped (2026-08-27). [ADR-125](../../decisions/ADR-125.md) |
| GS-149 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M10**, its subject is *"A support agent attempts to address an unassigned identity"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-150 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M10**, its subject is *"An internal analytical question diverges from the published metric"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-151 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M10**, its subject is *"Breach at 00:20, detector flag at 00:40, restriction at 09:15"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-152 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M10**, its subject is *"An unhandled exception on the payout path"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-153 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M10**, its subject is *"An operational alert dispatched to a mis-set Discord channel"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-154 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M10**, its subject is *"Every vendor returns 500, then times out"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-155 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M11**, its subject is *"A per-trade certificate is requested"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-156 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M11**, its subject is *"A shared payout card is revoked after enforcement"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-157 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M11**, its subject is *"Key rotation with historical certificates outstanding"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-158 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M11**, its subject is *"Enumeration attempt against the verification endpoint"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-159 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M11**, its subject is *"Enforcement on an account holding a pass card and a payout card"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-160 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M11**, its subject is *"A leaderboard participant opts out"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-161 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M11**, its subject is *"A public surface attempts a cross-account aggregate"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-162 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, its subject is *"Pass rate computed under all five denominator choices"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-163 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, its subject is *"A published settlement address accumulates past its ceiling"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-164 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, its subject is *"Review invitations across a mixed-outcome population"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-165 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, its subject is *"Loss ratio and reserve coverage requested from the public API"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-166 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, its subject is *"Approval rate published for a window containing freezes"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-167 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, its subject is *"A backdated correction lands on an already-published window"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-168 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, its subject is *"Sample below the published minimum"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-169 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, its subject is *"A comparative claim in marketing copy"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-170 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, its subject is *"The statistics run executes on a day whose replay self-audit diverged"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-171 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, its subject is *"A definition change is written with a backdated `effective_from`"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-172 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M13**, its subject is *"Consistency share rendered on analytics against the engine's value"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-173 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M13**, its subject is *"A backdated correction lands on a day already snapshotted"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-174 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M13**, its subject is *"Journal content requested from the risk, admin, evidence, and support paths"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-175 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M13**, its subject is *"An equity series with a live final point appended"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-176 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M13**, its subject is *"An R-multiple requested with no declared risk"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-177 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M13**, its subject is *"A percentile or population comparison requested"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-178 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M13**, its subject is *"Analytics load concurrent with a payout wave"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-179 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, its subject is *"A progressive cap release is proposed. Rewritten by [ADR-025](../../decisions/ADR-025.md)"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-180 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, its subject is *"A hedged pair accumulates a perfect streak"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-181 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, its subject is *"An earned free spin awarding a randomized reset discount"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-182 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, its subject is *"Win-back scoring across a population containing serial resetters"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-183 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, its subject is *"A high-milestone identity reaches the flags queue and the payout path"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-184 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, its subject is *"The loyalty service attempts to write a payout cap"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-185 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, its subject is *"A streak spans a half day, a holiday, and a limit-locked session"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-186 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M15**, its subject is *"Role sync for a trader opted into one role and not another"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-187 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M15**, its subject is *"A Discord identity presented to auth, recovery, and support verification"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-188 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M15**, its subject is *"The bot token used to post an unknown template and a free-text message"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-189 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M15**, its subject is *"Prohibited-arrangement solicitation observed in the community"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-190 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M15**, its subject is *"An enforcement closes an account holding a synced role"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-191 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M15**, its subject is *"An account-specific question asked in a public channel"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-192 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, its subject is *"A freeze notice for an account inside an active investigation"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-193 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, its subject is *"Contact change, then preference mute, then destination change"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-194 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, its subject is *"The nightly batch is replayed after a mid-run crash"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-195 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, its subject is *"A template referencing a detector name and a population comparison"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-196 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, its subject is *"A notice disputed with `read_at` null, and again with `read_at` set"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-197 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, its subject is *"A migration adds a new notification kind"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-198 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M17**, its subject is *"A campaign attempts a temporary cap or a waived consistency gate"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-199 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M17**, its subject is *"A free-trial cohort sharing device and ASN signals"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-200 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M17**, its subject is *"A price experiment runs while the public pricing page renders"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-201 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M17**, its subject is *"Coupon plus bundle plus promotional credit on a Direct plan"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-202 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M17**, its subject is *"A campaign code posted to a public aggregator"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-203 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M17**, its subject is *"Credit granted, spent, then the funding purchase charged back"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-204 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M17**, its subject is *"An experiment proposed with a rule-varying arm"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-205 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, its subject is *"Any surface using live-program language with no contracted program"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-206 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, its subject is *"An account reaches the final ladder ordinal"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-207 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, its subject is *"A graduating account carrying an unresolved correlation signal"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-208 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, its subject is *"A vault display with a benefit not yet accrued"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-209 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, its subject is *"Graduation with withdrawable balance remaining"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-210 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, its subject is *"Graduation with no human in the path"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-211 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, its subject is *"A third-party introduction is presented to a graduate"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-212 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"Thirty purchases under `pre_funded`, four of which pass"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-213 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"Provider outage during a payout wave"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-214 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"Geo triangle mismatch on an expatriate profile"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-215 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"Sanctions possible match on a common name"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-216 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"A dedupe match between two unrelated legitimate traders"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-217 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"A destination change against a stored verified status"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-218 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"An enforcement pack built on a dedupe hit after a provider change"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-219 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"A funnel query for drop-off by placement"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-220 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"A plan with no evaluation phase published with a placement other than `direct_purchase`"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-221 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"The placement config changes after an account was purchased"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-222 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, its subject is *"Promotional credit buys an evaluation that passes and pays out"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-223 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, its subject is *"A payouts-frozen identity attempts a wallet-funded purchase"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-224 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, its subject is *"A card-funded purchase is refunded"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-225 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, its subject is *"Every conceivable inbound funding attempt"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-226 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, its subject is *"Direct purchase, fast payout, then the funding card charges back"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-227 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, its subject is *"Wallet spend targeting an account owned by another identity"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-228 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, its subject is *"A balance reaches the jurisdictional dormancy period"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-229 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, its subject is *"Reserve coverage computed while the wallet float is large"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-230 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, its subject is *"Simultaneous withdrawal and checkout spend against one balance"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-231 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, its subject is *"Per-identity reconciliation against a globally balanced ledger"*, and **a session already checked this row and ruled it stays**: [`pt-03-ledger-zero-sum.property.test.ts:24`](../../../packages/ledger/test/pt-03-ledger-zero-sum.property.test.ts) says *"`GS-231` IS `blocked / no-fixture-format` AND STAYS BLOCKED. It is `PT-03`'s golden pair and carries the per-identity assertion; this is the property and the two are not the same artifact."* **The disposition holds and the stated reason does not**. [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-232 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"The composite trigger fires at the earliest reached trigger"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-233 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"`payout_request` never fires as a sole trigger"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-234 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"Resets inflate `second_purchase_any`"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-235 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, its subject is *"v1 tier: a hard link auto-enforces"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-236 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, its subject is *"v1 tier: a soft link never auto-enforces"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-237 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, its subject is *"v1.x tier: the signal-weight table is config, not code"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-238 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, its subject is *"v1.x tier: graph explorer packs are audience-scoped"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-239 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, its subject is *"Enrichment failure never blocks checkout"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-240 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, its subject is *"Ladder completion graduates without inviting"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-241 | blocked | format-cannot-express | the row's two trader figures are R-44's split on `PayoutEvaluation`, and its gross half is quantified over sequences while one fixture is one stream ([`types.ts`](../../../packages/rules-engine/src/types.ts), [`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-242 | written |  | [`GS-242-percent-of-size-scaling-holds-at-150k.yaml`](../../../packages/rules-engine/fixtures/GS-242-percent-of-size-scaling-holds-at-150k.yaml) |
| GS-243 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, its subject is *"INV-17's bound computed for a first-time buyer and for an identity on its tenth completed ladder"*, and **a test file NAMES it and states its own ceiling in the failure message**: [`lifetime-bound.property.test.ts:275`](../../../packages/rules-engine/test/lifetime-bound.property.test.ts), *"GS-243 needs a real two-identity fixture from here on"*. The four cases at `:237` assert the structural half, that neither the plan the bound is computed from nor the state the fold carries has a loyalty dimension; the row asks for the bound compared across two identities. [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-244 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, its subject is *"An identity completes its Nth ladder while under an open severity 4+ flag"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-245 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, its subject is *"Loyalty bonus credit funds an evaluation that passes and pays out"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-246 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, its subject is *"Credential-stuffing storm: 50,000 login attempts across 5,000 emails in ten minutes"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-247 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, its subject is *"IDOR sweep: an authenticated trader enumerates accounts, marks, timelines, and payouts they do not own"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-248 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"Payout destination swap from a hijacked session, then a payout request"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-249 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"Forged settlement webhook, plus a valid one replayed 50 times"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-250 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, its subject is *"Spoofed PSP `payment.success` for an order that was never paid"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-251 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"A trader-scoped token calls admin routes and the internal batch trigger"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-252 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, its subject is *"A compromised operator credential edits a live plan version's cap, split, or gap"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-253 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M17**, its subject is *"Mass redemption of a single-use code across tabs and bots, with client-set price fields probed in the same run"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-254 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M10**, its subject is *"An admin or affiliate field accepting a URL is set to an internal metadata address"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-255 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **INFRA**, its subject is *"`/docs`, `/openapi.json`, `/swagger`, and undocumented internal routes requested from the public origin"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-256 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"A verification is abandoned mid-flow and resumed later"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-257 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"Every verification surface is rendered and linted"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-258 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"(a) A registration supplies a `voip` number with a fresh email, a datacenter IP and no digital footprint"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-259 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"(a) The lookup vendor times out at registration"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-260 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"(b) One identity attempts to verify a second phone while its first is live"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-261 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"(b) A second identity verifies a number already live on another identity"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-262 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"Amendment 3 A banned identity's number is reassigned by the carrier and its new owner registers honestly"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-263 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"Amendment 3 The same reassignment with no portability record at all"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-264 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"(c) A phone change runs the full D4 ceremony"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-265 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"(c) A change request reaches application with the prior number notified and nothing sent"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-266 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, its subject is *"(d) and C-27 A session established by SMS OTP alone requests a payout destination change"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-267 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, its subject is *"C-27 The same session loads the portal"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-268 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, its subject is *"C-27 A SIM-swapped session is used end to end"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-269 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, its subject is *"(c) and C-27 The phone-change ceremony is visible on the security screen while its hold runs"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-270 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, its subject is *"Amendment 2 A pumping run drives registration OTP volume at attacker-controlled premium-rate numbers"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-271 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, its subject is *"Amendment 2, the degraded path The cost breaker trips mid-registration"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-272 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, its subject is *"(e) SMS delivery and lookup costs land in the Cost Stack"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-273 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"Ruling A A payout is requested while an unresolved severity-5 flag stands, and the flag resolves inside the window"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-274 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"Ruling A The same hold ends in enforcement rather than release"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-275 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"Ruling A A held request reaches its 48 hour expiry and the account breached during the hold"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-276 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"Ruling A, the negative half The hourly sweep runs, completes and emits its completion signal, and a held request sits past `hold_expires_at` anyway"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-277 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, its subject is *"Ruling A A flag is raised after wallet credit and the withdrawal is halted before settlement"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-278 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, its subject is *"Ruling A A second payout request is opened on an account whose first request is held"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-279 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"Ruling B An identity is restricted, and every surface [ADR-041](../../decisions/ADR-041.md) enumerates is exercised"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-280 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, its subject is *"Ruling B A restricted identity attempts to buy, and to reset, on each of the three payment methods"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-281 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M8**, its subject is *"Ruling B An affiliate whose identity is restricted reaches a settlement cycle, and is later restored"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-282 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"Ruling B A restore is attempted while the account's `set_risk` is unconfirmed"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-283 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, its subject is *"Ruling B A restore succeeds, and is then contested"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-284 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"Both rulings A restriction is opened over an account that already has a held payout"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-285 | covered-elsewhere |  | EXECUTED by the `test` at [`economic-calendar.test.ts:35`](../../../apps/portal/test/economic-calendar.test.ts), which names this row in its own title and asserts the whole pin: ONE occurrence at 12:30 UTC rendering 07:30 in Chicago and 21:30 in Tokyo, both carrying the same `scheduled_release_at`, with `INV-M4-16` asserted structurally in the same file by walking the built view for any key an origin could be assigned to. **The db-side suite named the condition and the condition is met**: [`economic-calendar.test.ts:26`](../../../packages/db/test/economic-calendar.test.ts) says this row gets its executable half *"when D-04 and the panel are built"*, and [`view/economic-calendar.ts`](../../../apps/portal/src/view/economic-calendar.ts) is the panel. PASS, 0 skipped (2026-08-27). [ADR-125](../../decisions/ADR-125.md) |
| GS-286 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, its subject is *"A scheduled release time is revised after publication"*, and **a test file NAMES it and that file says what it does not assert**: [`economic-calendar.test.ts:26`](../../../packages/db/test/economic-calendar.test.ts) gives this row its executable half *"when D-04 and the panel are built"*. The panel is built and `D-04` is not, so the re-evaluation half has no detector to run through. What executes asserts that the migration still says what [ADR-066](../../decisions/ADR-066.md) ruled. [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-287 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, its subject is *"The calendar passes its staleness threshold"*, and **a test file NAMES it and that file says what it does not assert**: [`economic-calendar.test.ts:26`](../../../packages/db/test/economic-calendar.test.ts) gives this row its executable half when `D-04` is built, and `D-04` does not exist, so nothing can decline to fire. The coverage bound that makes declining possible is asserted at `:224`. [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-288 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"A scheduled digest fails to deliver while the job reports success"*, and **a test file NAMES it and that file says what it does not assert**: [`report-schedules.test.ts:18`](../../../packages/db/test/report-schedules.test.ts) states *"There is no delivery job, no renderer and no transport ... NOTHING HERE SENDS ANYTHING."* The five cases at `:129` assert that `0040` still says what [ADR-066](../../decisions/ADR-066.md) ruled about the alarm's source. [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-289 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"The daily liability digest is generated for an estate with named traders"*, and **a test file NAMES it and that file says what it does not assert**: [`report-schedules.test.ts:18`](../../../packages/db/test/report-schedules.test.ts), *"NOTHING HERE SENDS ANYTHING"*. The three cases at `:181` pin the column set of `report_deliveries` so a body column cannot be added quietly; no digest has been generated for any estate. [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-290 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"A digest schedule names a recipient who has been removed"*, and **a test file NAMES it and that file says what it does not assert**: [`report-schedules.test.ts:18`](../../../packages/db/test/report-schedules.test.ts), *"NOTHING HERE SENDS ANYTHING"*. The four cases at `:224` pin that an omission with no stated reason is unwritable; no delivery has degraded to anybody. [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-291 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"A shared IP across three identities and a shared payment fingerprint across two, the second carrying higher open liability"*, and **a test file NAMES it and that file says what it does not assert**: [`duplicate-signal-views.test.ts:39`](../../../packages/db/test/duplicate-signal-views.test.ts), *"What is deliberately NOT claimed: that any row exists, that any ordering has ever been executed, or that the aggregate is correct against a book. GS-291's ranking ... gets their executable half when the console is built."* [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-292 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"A soft link appears in a standing duplicate-signal view"*, and **a test file NAMES it and that file says what it does not assert**: [`duplicate-signal-views.test.ts:39`](../../../packages/db/test/duplicate-signal-views.test.ts) gives *"GS-292's soft link"* its executable half *"when the console is built"*. What executes is the view's declared shape. [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-293 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, its subject is *"A security-class message bounces"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-294 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, its subject is *"An admin resends a notice after its template has changed"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-295 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, its subject is *"A trader files a spam complaint"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-296 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"`ADR-067` A goodwill credit is posted, then the trader requests a payout"*, and **a test file NAMES it and reports this row STALE against the ruling it would be checked by**: [`account-adjustments.test.ts:38`](../../../packages/db/test/account-adjustments.test.ts) states *"GS-296 AND GS-298's REGISTRY ROWS DO NOT MATCH THIS RULING and are owed an amendment (ADR-067 section 9)"*. A row whose registry entry is stale cannot be discharged against a suite asserting the ruling that superseded it, and the amendment is [ADR-067](../../decisions/ADR-067.md) section 9's rather than this table's. [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-297 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"`ADR-067` An adjustment is reversed"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-298 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"`ADR-067` An adjustment is attempted against a restricted identity"*, and **a test file NAMES it and reports this row STALE against the ruling it would be checked by**: [`account-adjustments.test.ts:38`](../../../packages/db/test/account-adjustments.test.ts), the same sentence. What executes at `:253` asserts that `0038` gates on NO identity status, which is the opposite direction from the row's *"refused or held"*, and is why the amendment is owed before the row can be read. [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-299 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"`ADR-067` An adjustment crosses the configurable dual-control threshold"*, and **a test file NAMES it and the control it turns on is INERT**: [`account-adjustments.test.ts:291`](../../../packages/db/test/account-adjustments.test.ts) asserts that `OQ-F6-01` is still open, so `account_adjustments_dual_control_above_threshold` is specification rather than enforcement until a number is written into the column. Nothing has crossed a threshold that has no value. [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-300 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"An impersonation session issues a payout request"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-301 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"A session reaches its 30 minute expiry while a page is open"*, and **shipped source NAMES it and says the carrier is not declared**: [`api/types.ts:311`](../../../apps/portal/src/api/types.ts) records that `Me.impersonation` *"is NOT declared yet and this type is a shape waiting for its field"*, and [`impersonation-banner.ts:52`](../../../apps/portal/src/shell/impersonation-banner.ts) quotes this row as the failure it renders the instant for. The refusal on the next request is the auth surface's and has no route. [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-302 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"Impersonation of a `restricted` identity"*, and **its assertion IS executed and this document's own definition excludes it**: [`probe_impersonation_session_type.sql`](../../../scripts/db/probe_impersonation_session_type.sql) asserts that a `restricted` identity is still impersonable, and [`gates.mjs`](../../../scripts/corpus/gates.mjs) pins the step by name and cites this row as what deleting it would cost. **It runs in the install job against PostgreSQL**, and `covered-elsewhere` above requires an ordinary `pnpm exec vitest run` reporting a PASS. [ADR-125](../../decisions/ADR-125.md) section 7 item 3 rows that question and does not take it, because the definition is prose above this table. **The term is kept for [ADR-125](../../decisions/ADR-125.md) clause 3's reason and its stated one is FALSE**: the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that entry's fence |
| GS-303 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"Negative: an impersonation token is replayed against a trader route as a trader token"*, and **its assertion IS executed and this document's own definition excludes it**: [`probe_impersonation_session_type.sql`](../../../scripts/db/probe_impersonation_session_type.sql) asserts the session-type boundary in BOTH directions, which [`gates.mjs`](../../../scripts/corpus/gates.mjs) calls the only assertion that the mirror closes the ordering hole this row names. **It runs in the install job against PostgreSQL**, not in `pnpm exec vitest run`. [ADR-125](../../decisions/ADR-125.md) section 7 item 3 rows that question and does not take it. **The term is kept for [ADR-125](../../decisions/ADR-125.md) clause 3's reason and its stated one is FALSE**: the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that entry's fence |
| GS-304 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"The parity matrix is checked against the enumerated trader-side action set"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-305 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, its subject is *"The Nth payout settles on a plan with fee-back configured"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-306 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, its subject is *"A trader reaches `N` on a plan whose fee-back amount is configured to zero"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-307 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, its subject is *"A ladder completes on a plan configuring an unlock"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-308 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, its subject is *"A ladder completes on a plan configuring no unlock"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-309 | covered-elsewhere |  | EXECUTED by five cases naming this row in their own titles: [`size-label.test.ts:23`](../../../apps/site/test/size-label.test.ts), `:48` and `:63`, [`plans-page.test.ts:98`](../../../apps/site/test/plans-page.test.ts) and [`rules-page.test.ts:102`](../../../apps/site/test/rules-page.test.ts). Both halves of the pin, including the case the row exists for: a label that READS as money still reaches no figure, and the segment moves only with `size_cents`. PASS, 0 skipped (2026-08-27). [ADR-125](../../decisions/ADR-125.md) |
| GS-310 | covered-elsewhere |  | EXECUTED by four cases naming this row in their own titles: [`size-label.test.ts:80`](../../../apps/site/test/size-label.test.ts), `:87` and `:99`, and [`plans-page.test.ts:112`](../../../apps/site/test/plans-page.test.ts). The stated default is asserted as the capital figure derived from `size_cents`, and the three things it may never be (an empty string, a placeholder, the plan name) are asserted separately, with the empty string made unwritable so `null` is the only absent case. PASS, 0 skipped (2026-08-27). [ADR-125](../../decisions/ADR-125.md) |
| GS-311 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-312 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"A new plan version is published from the console"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-313 | covered-elsewhere |  | EXECUTED by the `it` at [`aggregate.test.ts:68`](../../../packages/harness/test/aggregate.test.ts), which names this row in the comment run immediately above it and asserts the pin on EVERY output of a real run: `calibrationId`, `calibrationObservedAt`, a 64-hex `calibrationDigest`, the seed, and `runSampleSize` equal to the trial count. [`provenance.ts:17`](../../../packages/harness/src/provenance.ts) quotes this row and makes provenance a REQUIRED property of every output record, so the absent-field case is unrepresentable rather than untested. **The residual is stated rather than implied**: there is no renderer, so the row's *"displayed"* is carried by `FM-M21-02`'s shape and not by a rendering. PASS, 0 skipped (2026-08-27). [ADR-125](../../decisions/ADR-125.md) |
| GS-314 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"One owner publishes and a second has not approved"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-315 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"AS-M21-01. A config is published against calibration that was already stale"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
| GS-316 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, its subject is *"A sensitivity sweep runs over price"*, and **no test file in this tree names it**: no `.test.ts` or `.spec.ts` under `apps`, `packages`, `scripts` or `e2e` (measured 2026-08-27). [ADR-125](../../decisions/ADR-125.md) clause 4: the term's corpus-wide reason is FALSE, on four fixture formats measured in this tree, and the term is kept only because the closed vocabulary is enforced in [`gates.mjs`](../../../scripts/corpus/gates.mjs), outside that fence |
