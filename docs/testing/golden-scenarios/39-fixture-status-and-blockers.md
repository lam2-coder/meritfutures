---
status: approved
depends_on: [README.md, ../../decisions/ADR-072.md, ../../decisions/ADR-076.md]
last_updated: 2026-08-26
---

## 39. Fixture status and blockers

**One row per registered scenario, and the reason lives in a column rather than in prose.** [ADR-072](../../decisions/ADR-072.md) is the ruling: a fixture is writable when three checkable conditions hold, and **every row without a fixture carries a stated blocker from a closed vocabulary with a citation that supports it**. [Section 33.4](33-ownership-index-and-coverage-reconciliation.md) is the same idea at three rows; this is it at 276.

**`status` is derived from the directory and not from this file.** `written` means a `GS-NNN-*.yaml` and its `.expected.json` sibling are on disk in [`packages/rules-engine/fixtures`](../../../packages/rules-engine/fixtures/README.md). `writable` means the assertion can be written today, and since [ADR-076](../../decisions/ADR-076.md) it names WHERE. `covered-elsewhere` means it is already written and RUNS, in a suite the citation names. `blocked` means at least one of `ADR-072`'s conditions fails, and the `blocker` names **the one that would still hold if every other were cleared**.

### `covered-elsewhere`, and the word that carries it is EXECUTED

[ADR-076 section 1](../../decisions/ADR-076.md) is the ruling and this is its rule in its own terms: **a registry row is discharged when its assertion is EXECUTED somewhere a gate can read, not when a file appears in [`packages/rules-engine/fixtures`](../../../packages/rules-engine/fixtures/README.md).** So a row is `covered-elsewhere` when **both** of these hold, and neither alone is enough:

1. the scenario has **no golden fixture**, and
2. a named suite outside that directory **runs an assertion for it**, in an ordinary `pnpm exec vitest run`, and the case is reported as a PASS.

A row in this status carries **no blocker**, because nothing is blocked. It is a status and not a seventh blocker term for a checkable reason: a blocker names which of `W1`, `W2`, `W3` fails, and on these rows none has.

**THREE THINGS LOOK LIKE COVERAGE AND ARE NOT, and the reason this list is here rather than in the ruling is that all three are true of a row this document nearly moved.**

- **A path that resolves is not a running assertion.** A file exists whether or not anything in it executes.
- **A file that contains the `GS-nnn` is not a running assertion.** Those ids were written into the suites by [session 123](../../sessions/2026-08-21-session-123.md) as comments, on purpose, so a gate could find them. A comment is a claim about a file, and a refactor can leave one behind.
- **A `describe` that names the row is not a running assertion when the block is skipped.** `describe.skipIf(condition)` with the condition false reports a named skip, never enters its body, and leaves every grep in the tree passing.

**The third is not hypothetical, it is `GS-072`.** [ADR-076 section 6](../../decisions/ADR-076.md) counts it among seven rows moving to this status; its cited block is `describe.skipIf(!replayExists)` at [`replay-determinism.property.test.ts:231`](../../../scripts/demo/test/replay-determinism.property.test.ts), `replayExists` is `Object.keys(engine).includes('replay')` at [`:76`](../../../scripts/demo/test/replay-determinism.property.test.ts) and is false, and the body **throws** rather than asserting. **Six rows moved and not seven**, and the difference is one run of the suite. **The test for this status is therefore watched rather than read**: the citation is checked by executing it and seeing a PASS, not by opening the file.

| Status | Rows | |
|---|---|---|
| **written** | **43** | Every one M1-owned. Not one fixture on disk belongs to any other module |
| **writable** | **0** | **The term stays in the vocabulary and holds no row.** `GS-071` was the last one and it left for `covered-elsewhere` when its assertion landed |
| **covered-elsewhere** | **7** | `GS-071`, `GS-073`, `GS-076`, `GS-077`, `GS-078`, `GS-083`, `GS-141`, each cited to the suite that executes it |
| **blocked** | **266** | By vocabulary term below |

| Blocker | Rows | The condition that fails |
|---|---|---|
| `no-fixture-format` | **227** | W2. No fixture format exists for the owning module at all |
| `format-cannot-express` | **16** | W2. The format exists and cannot state this input or this expectation |
| `vendor-call` | **14** | W3. `M02` is at `status: review`, the one module plan of twenty-one that is not approved |
| `outside-loader-boundary` | **6** | W1. The code exists and lives outside `@merit/rules-engine` |
| `open-question` | **2** | W3. `OI-16`, on `GS-004` and `GS-031` |
| `no-plan-record-value` | **1** | W2. `GS-079`, a value no plan document states |

**EVERY COUNT ABOVE WAS RECOMPUTED BY READING THE TREE AND NOT BY ADJUSTING THE PREVIOUS ONE.** The 43 is a directory listing: [`packages/rules-engine/fixtures`](../../../packages/rules-engine/fixtures/README.md) holds **43 `GS-nnn-*.yaml` and 43 `.expected.json`, over 43 distinct scenario ids**, and it held 43 against 40 `written` rows before this edit, which is the three-row gap `CI-06/fixture-inventory` was carrying in a register. The four status counts and the six blocker counts are counts of the rows below, taken from the rows below after they were edited.

**`writable` NOW HOLDS NO ROW, AND THE WAY THE LAST ONE LEFT IS WORTH SAYING OUT LOUD.** It means *"all three of [ADR-072](../../decisions/ADR-072.md)'s conditions hold"*, and `W1` is loader reachability: *"a symbol in `apps/worker` or `packages/rithmic` fails W1 whether or not it is implemented"*. **`GS-071` fails `W1` read that way and always will**, because replay runs in [`apps/worker`](../../../apps/worker/src/batch/replay.ts). It sat `writable` for one edit, naming the suite where its assertion could be written rather than the directory that cannot hold it; [session 129](../../sessions/2026-08-22-session-129.md) then wrote that assertion at the scale the row names, and [ADR-076 section 1](../../decisions/ADR-076.md)'s governing rule finished the move: **a row is discharged when its assertion is EXECUTED, wherever it runs**, so `W1` is a condition on the fixture directory rather than on the scenario. **`writable` is a term in the vocabulary with a count of zero**, which is what it should read when every row that could name a suite has one that runs. It is not retired and the vocabulary does not move: the next scenario whose assertion is writable and unwritten reads `writable` again.

**These counts are derived from the table below and they disagree with [WAVE-04 section 2](../../plans/WAVE-04-fixture-backlog-and-gate-inventory.md) on three of five.** The plan claims 16 writable against 0, 14 needing a ruling against 23 M1 rows blocked, and 3 held against 2. It agrees at 243 with no code, and it no longer agrees at 40 written, which is 43. **[ADR-072 section 3](../../decisions/ADR-072.md) is why**: the plan partitions on whether the code exists, which is W1, and the condition that binds M1 is W2. **The table is the answer and the plan is the error.**

**The M1 partition closes exactly.** [Section 33.1](33-ownership-index-and-coverage-reconciliation.md) owns 73 scenarios to M1; 43 are written, none is writable, 7 are covered elsewhere, and 23 are blocked. The other 243 rows are the 14 M2 rows and the 229 owned by M3 through M20 and INFRA.

### `GS-049` carries a disposition and not only a status, and this is it

**One fixture discharges the row and the row is NOT split.** [Its registry row](04-gs-030-to-gs-051-the-appendix-b4-battery.md) names three probe shapes and adds *"runs as a property suite as well as a fixture"*. [`GS-049-fuzz-adversarial-day-sequences.yaml`](../../../packages/rules-engine/fixtures/GS-049-fuzz-adversarial-day-sequences.yaml) carries **all three shapes** in one 21-day stream: the alternating 15,001c and 14,999c run on days 1 to 8, a flat grind on days 9 to 20, and the single 1,000,000c day into consistency math last, so the whole stream is the period it is measured against. **Two of the three are pinned whole**, including the assertion the file exists for, `profit_needed_to_dilute_cents: 2213334` as a CEILING.

**What is not in the stream is the third probe's LENGTH**, twelve flat days against the row's hundred, and the reason it is not a residual is arithmetic rather than effort: **a sibling pins one `end_state`, and the end state after twelve flat days and after one hundred is the same state, field for field**, because a flat day moves no balance, no floor and neither counter. A longer grind buys a golden file a longer `events` list and nothing else. The trajectory in between is what a property suite asserts, and the row itself names one.

**One thing was checked rather than accepted, and it did not hold.** The sibling names *"`RE-P-01`, `RE-P-03` and `RE-P-04`"* as where that trajectory is asserted. `RE-P-01` executes, at [`floor-monotonicity.property.test.ts:259`](../../../packages/rules-engine/test/floor-monotonicity.property.test.ts). `RE-P-03`'s content executes as `PT-02`, at [`win-days-monotonicity.property.test.ts:153`](../../../packages/rules-engine/test/win-days-monotonicity.property.test.ts). **`RE-P-04` executes nowhere in this tree**: no property suite asserts that `tradedDaysCount` is non-decreasing, which is [M01](../../plans/M01-rules-engine.md)'s `INV-09`. So the floor and the win-day count have the length and **the traded-day count does not**.

**That is a residual on `RE-P-04` and it is not a residual on this row.** The fixture pins `traded_days_count: 9` across the grind, so a counter that moved on a zero-fill day fails there; what is missing is the generated-sequence form, whose owner is the property suite and not a golden file. **The row moves to `written` on the directory**, per [ADR-072](../../decisions/ADR-072.md)'s rule that the status is a fact about the directory, and the missing `RE-P-04` suite is named here so it is not discovered a third time.

| id | status | blocker | citation |
|---|---|---|---|
| GS-001 | blocked | format-cannot-express | [`types.ts`](../../../packages/rules-engine/src/types.ts) declares `DailyMark` with nine fields and no instant, so a fill timestamp has no home in the day stream; [`rules.ts`](../../../packages/rules-engine/src/rules.ts) files `R-01` among the four the engine does not declare, discharged at ingest |
| GS-002 | written |  | [`GS-002-counters-advance-on-a-day-the-trader-did-not-trade.yaml`](../../../packages/rules-engine/fixtures/GS-002-counters-advance-on-a-day-the-trader-did-not-trade.yaml) |
| GS-003 | written |  | [`GS-003-half-day-counts-as-a-full-trading-day.yaml`](../../../packages/rules-engine/fixtures/GS-003-half-day-counts-as-a-full-trading-day.yaml) |
| GS-004 | blocked | open-question | `OI-16` asks whether any feed Merit will hold reports a whole-session halt at the granularity `R-04` consumes; [ADR-059](../../decisions/ADR-059.md) recommendation 3 was granted and the item is recorded in [STATE](../../STATE.md) |
| GS-005 | written |  | [`GS-005-day-with-fills-and-exactly-zero-pnl.yaml`](../../../packages/rules-engine/fixtures/GS-005-day-with-fills-and-exactly-zero-pnl.yaml) |
| GS-006 | written |  | [`GS-006-win-day-exactly-at-the-floor-counts.yaml`](../../../packages/rules-engine/fixtures/GS-006-win-day-exactly-at-the-floor-counts.yaml) |
| GS-007 | written |  | [`GS-007-win-day-one-cent-below-the-floor-does-not-count.yaml`](../../../packages/rules-engine/fixtures/GS-007-win-day-one-cent-below-the-floor-does-not-count.yaml) |
| GS-008 | written |  | [`GS-008-initial-floor-at-account-open.yaml`](../../../packages/rules-engine/fixtures/GS-008-initial-floor-at-account-open.yaml) |
| GS-009 | written |  | [`GS-009-floor-trails-a-new-closing-high.yaml`](../../../packages/rules-engine/fixtures/GS-009-floor-trails-a-new-closing-high.yaml) |
| GS-010 | written |  | [`GS-010-floor-never-retreats-after-a-losing-day.yaml`](../../../packages/rules-engine/fixtures/GS-010-floor-never-retreats-after-a-losing-day.yaml) |
| GS-011 | written |  | [`GS-011-trailing-floor-ignores-the-intraday-high.yaml`](../../../packages/rules-engine/fixtures/GS-011-trailing-floor-ignores-the-intraday-high.yaml) |
| GS-012 | written |  | [`GS-012-breach-compares-against-the-floor-at-open.yaml`](../../../packages/rules-engine/fixtures/GS-012-breach-compares-against-the-floor-at-open.yaml) |
| GS-013 | written |  | [`GS-013-low-exactly-at-the-floor-survives.yaml`](../../../packages/rules-engine/fixtures/GS-013-low-exactly-at-the-floor-survives.yaml) |
| GS-014 | written |  | [`GS-014-low-one-cent-below-the-floor-breaches.yaml`](../../../packages/rules-engine/fixtures/GS-014-low-one-cent-below-the-floor-breaches.yaml) |
| GS-015 | written |  | [`GS-015-floor-lock-engages-continuously-with-no-jump.yaml`](../../../packages/rules-engine/fixtures/GS-015-floor-lock-engages-continuously-with-no-jump.yaml) |
| GS-016 | written |  | [`GS-016-locked-floor-does-not-move-on-a-later-high.yaml`](../../../packages/rules-engine/fixtures/GS-016-locked-floor-does-not-move-on-a-later-high.yaml) |
| GS-017 | written |  | [`GS-017-eval-passes-at-exactly-the-profit-target.yaml`](../../../packages/rules-engine/fixtures/GS-017-eval-passes-at-exactly-the-profit-target.yaml) |
| GS-018 | written |  | [`GS-018-eval-one-cent-below-target-does-not-pass.yaml`](../../../packages/rules-engine/fixtures/GS-018-eval-one-cent-below-target-does-not-pass.yaml) |
| GS-019 | written |  | [`GS-019-eval-pass-resets-the-funded-phase-to-the-account-size.yaml`](../../../packages/rules-engine/fixtures/GS-019-eval-pass-resets-the-funded-phase-to-the-account-size.yaml) |
| GS-020 | written |  | [`GS-020-eval-consistency-defers-the-pass-and-dilution-clears-it.yaml`](../../../packages/rules-engine/fixtures/GS-020-eval-consistency-defers-the-pass-and-dilution-clears-it.yaml) |
| GS-021 | written |  | [`GS-021-consistency-skipped-when-period-profit-is-zero.yaml`](../../../packages/rules-engine/fixtures/GS-021-consistency-skipped-when-period-profit-is-zero.yaml) |
| GS-022 | written |  | [`GS-022-consistency-skipped-when-period-profit-is-negative.yaml`](../../../packages/rules-engine/fixtures/GS-022-consistency-skipped-when-period-profit-is-negative.yaml) |
| GS-023 | written |  | [`GS-023-consistency-at-exactly-the-threshold-passes.yaml`](../../../packages/rules-engine/fixtures/GS-023-consistency-at-exactly-the-threshold-passes.yaml) |
| GS-024 | written |  | [`GS-024-consistency-one-cent-over-the-threshold-defers.yaml`](../../../packages/rules-engine/fixtures/GS-024-consistency-one-cent-over-the-threshold-defers.yaml) |
| GS-025 | written |  | [`GS-025-withdrawable-is-balance-minus-size-minus-buffer-floored-at-zero.yaml`](../../../packages/rules-engine/fixtures/GS-025-withdrawable-is-balance-minus-size-minus-buffer-floored-at-zero.yaml) |
| GS-026 | blocked | format-cannot-express | the row pins `approved 150,000` and `clamp_reason: cap`, which live on `PayoutEvaluation`; [`run.ts`](../../../packages/golden-loader/src/run.ts) folds `advanceDay` and diffs a `RuleState`, and [`types.ts`](../../../packages/rules-engine/src/types.ts) gives `RuleState` neither field |
| GS-027 | blocked | format-cannot-express | the row pins `approved 120,000` and `clamp_reason: withdrawable`, both on `PayoutEvaluation`, which no fixture in this format ever sees ([`types.ts`](../../../packages/rules-engine/src/types.ts), [`run.ts`](../../../packages/golden-loader/src/run.ts)) |
| GS-028 | blocked | format-cannot-express | the row pins the tie resolving with `clamp_reason: none`, a `PayoutEvaluation` field absent from `RuleState` ([`types.ts`](../../../packages/rules-engine/src/types.ts)) |
| GS-029 | blocked | format-cannot-express | R-44's trader and firm split is on `PayoutEvaluation` and never reaches `RuleState`, so the row's `trader 90,001, firm 10,000` cannot be pinned ([`types.ts`](../../../packages/rules-engine/src/types.ts)) |
| GS-030 | blocked | format-cannot-express | the calendar record carries two keys per session, `trading_day` and `kind`, and states no session open and no close, so the 23 hour and 25 hour halves are not statable ([`cme-2026.json`](../../../packages/rules-engine/fixtures/calendars/cme-2026.json), `what_this_format_cannot_carry`) |
| GS-031 | blocked | open-question | `OI-16`, the same item: `halted` has no publisher, and [ADR-059](../../decisions/ADR-059.md) recommendation 3 granted the hold ([STATE](../../STATE.md)) |
| GS-032 | written |  | [`GS-032-half-day-the-trader-sat-out.yaml`](../../../packages/rules-engine/fixtures/GS-032-half-day-the-trader-sat-out.yaml) |
| GS-033 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-034 | blocked | outside-loader-boundary | the row needs a replay forward; [`replay.ts`](../../../apps/worker/src/batch/replay.ts) lives in `apps/worker` and the [golden-loader](../../../packages/golden-loader/README.md) imports the engine entry point and nothing else |
| GS-035 | blocked | format-cannot-express | the row turns on a request at 23:59:59 against a batch at 00:05; the day stream carries no time of day, and `INV-01` forbids the engine reading a clock ([ADR-059](../../decisions/ADR-059.md), which excludes R-06 for this reason) |
| GS-036 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-037 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-038 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-039 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-040 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-041 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-042 | blocked | format-cannot-express | three request amounts on one row, and the format carries no payout request at all: `SETTLEMENT_KEYS` admits five fields of an already-approved fact ([`loader.ts`](../../../packages/golden-loader/src/loader.ts)) |
| GS-043 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-044 | written |  | [`GS-044-passes-eval-while-payouts-are-frozen.yaml`](../../../packages/rules-engine/fixtures/GS-044-passes-eval-while-payouts-are-frozen.yaml) |
| GS-045 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M8**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-046 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-047 | blocked | format-cannot-express | the row needs the same day applied twice, and `L-10` refuses a `trading_day` that is not after its predecessor ([`loader.ts`](../../../packages/golden-loader/src/loader.ts)); the loader rule that blocks the fixture is the claim the scenario exists to assert |
| GS-048 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-049 | written |  | [`GS-049-fuzz-adversarial-day-sequences.yaml`](../../../packages/rules-engine/fixtures/GS-049-fuzz-adversarial-day-sequences.yaml), on disk since session 117 with its `.expected.json` sibling. **The disposition is stated above this table**: one stream carries all three probe shapes, two are pinned whole, and the third probe's LENGTH is the property suite's because the end state after twelve flat days and after one hundred is the same state |
| GS-050 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-051 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-052 | blocked | format-cannot-express | neither half is one account's day fold: three requests in one transaction window, and three wallet-to-rail withdrawals under `G-NO-IN-FLIGHT` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-053 | written |  | [`GS-053-stacking-attempt-across-the-settlement-boundary.yaml`](../../../packages/rules-engine/fixtures/GS-053-stacking-attempt-across-the-settlement-boundary.yaml) |
| GS-054 | written |  | [`GS-054-manufactured-dilution-days-from-a-hedged-pair.yaml`](../../../packages/rules-engine/fixtures/GS-054-manufactured-dilution-days-from-a-hedged-pair.yaml) |
| GS-055 | written |  | [`GS-055-minimum-variance-path-to-a-full-cap-extraction.yaml`](../../../packages/rules-engine/fixtures/GS-055-minimum-variance-path-to-a-full-cap-extraction.yaml) |
| GS-056 | written |  | [`GS-056-locked-floor-converts-the-account-into-a-free-option.yaml`](../../../packages/rules-engine/fixtures/GS-056-locked-floor-converts-the-account-into-a-free-option.yaml) |
| GS-057 | blocked | format-cannot-express | a correction needs a superseding mark (`R-11`, undeclared) and the absorbed amount needs a field: [`types.ts`](../../../packages/rules-engine/src/types.ts) gives `RuleState` neither an absorbed delta nor `corrected_days_in_period` |
| GS-058 | blocked | format-cannot-express | the same three clauses as GS-057: a superseding mark, a settlement to land after, and an absorbed delta `RuleState` does not carry ([`types.ts`](../../../packages/rules-engine/src/types.ts), [`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-059 | written |  | [`GS-059-holiday-cluster-compresses-the-cadence-gap-in-calendar-time.yaml`](../../../packages/rules-engine/fixtures/GS-059-holiday-cluster-compresses-the-cadence-gap-in-calendar-time.yaml), written by session 117 with its `.expected.json` sibling. The row read `writable` for five sessions after the fixture landed, which is the gap `CI-06/fixture-inventory` was holding in a register rather than reporting |
| GS-060 | written |  | [`GS-060-zero-risk-day-farming-buys-nothing-against-the-real-gates.yaml`](../../../packages/rules-engine/fixtures/GS-060-zero-risk-day-farming-buys-nothing-against-the-real-gates.yaml) |
| GS-061 | written |  | [`GS-061-peak-picking-requesting-on-a-local-maximum.yaml`](../../../packages/rules-engine/fixtures/GS-061-peak-picking-requesting-on-a-local-maximum.yaml) |
| GS-062 | blocked | format-cannot-express | ten copy-traded accounts under one identity; the engine is per account by design, and no per-account fold produces the identity-level forecast ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-063 | written |  | [`GS-063-breach-and-profit-target-on-the-same-day.yaml`](../../../packages/rules-engine/fixtures/GS-063-breach-and-profit-target-on-the-same-day.yaml) |
| GS-064 | written |  | [`GS-064-breach-and-payout-eligibility-on-the-same-day.yaml`](../../../packages/rules-engine/fixtures/GS-064-breach-and-payout-eligibility-on-the-same-day.yaml) |
| GS-065 | written |  | [`GS-065-settled-payout-drops-the-balance-toward-a-floor-that-does-not-move.yaml`](../../../packages/rules-engine/fixtures/GS-065-settled-payout-drops-the-balance-toward-a-floor-that-does-not-move.yaml) |
| GS-066 | blocked | format-cannot-express | the row needs a FAILED transfer that consumes no ordinal, and a failed transfer produces no `SettlementFact`: `L-11` admits five fields and none of them is a failure ([`loader.ts`](../../../packages/golden-loader/src/loader.ts)) |
| GS-067 | written |  | [`GS-067-graduation-fires-exactly-on-the-ladder-count.yaml`](../../../packages/rules-engine/fixtures/GS-067-graduation-fires-exactly-on-the-ladder-count.yaml) |
| GS-068 | written |  | [`GS-068-consistency-period-boundary-at-the-settlement-day.yaml`](../../../packages/rules-engine/fixtures/GS-068-consistency-period-boundary-at-the-settlement-day.yaml) |
| GS-069 | written |  | [`GS-069-adding-profit-on-the-best-day-breaks-a-passing-consistency-gate.yaml`](../../../packages/rules-engine/fixtures/GS-069-adding-profit-on-the-best-day-breaks-a-passing-consistency-gate.yaml) |
| GS-070 | written |  | [`GS-070-funded-start-balance-does-not-equal-size.yaml`](../../../packages/rules-engine/fixtures/GS-070-funded-start-balance-does-not-equal-size.yaml) |
| GS-071 | covered-elsewhere |  | EXECUTED by the `describe` at [`replay.test.ts:1076`](../../../apps/worker/test/replay.test.ts), which names this row in its own header, and the `it` at `:1077`: a **250-day funded life**, asserted the hash first as one array of 250 hex digests and then field by field at 43 comparisons a day, with the audit itself reporting `diverged: 0` against `matched`, `inScope` and `storedRows` each equal to 250. **THE STATUS WAS TAKEN BY EXECUTING THE CITATION AND NOT BY READING IT**, which is the distinction [session 123](../../sessions/2026-08-21-session-123.md) found on `GS-072` and [session 131](../../sessions/2026-08-22-session-131.md) built the gate assertion for: the file carries no `skipIf`, no `.skip` and no `.only`, and `pnpm exec vitest run apps/worker/test/replay.test.ts` on 2026-08-22 reports **62 passed and 0 skipped**. **THE ROW LEFT `writable`, WHICH NOW HOLDS NONE.** It read `writable` while the suite asserted four days and the row asks 250, which was a SCALE and not a shape; [session 129](../../sessions/2026-08-22-session-129.md) wrote it at the row's own scale as WAVE-05 `X5`, and [ADR-076 section 1](../../decisions/ADR-076.md)'s governing rule discharges a row whose assertion is EXECUTED wherever it runs. Line numbers read 2026-08-22 |
| GS-072 | blocked | outside-loader-boundary | arrival order across a replay, which runs in `apps/worker` ([`replay.ts`](../../../apps/worker/src/batch/replay.ts)) outside the loader import boundary ([golden-loader](../../../packages/golden-loader/README.md)). **THIS ROW DID NOT MOVE TO `covered-elsewhere` AND [ADR-076 section 6](../../decisions/ADR-076.md) SAYS IT DOES.** Its cited site is `describe.skipIf(!replayExists)` at [`replay-determinism.property.test.ts:231`](../../../scripts/demo/test/replay-determinism.property.test.ts), whose condition is `Object.keys(engine).includes('replay')` at [`:76`](../../../scripts/demo/test/replay-determinism.property.test.ts) and is false; the block is a named skip and its body throws. Re-run on 2026-08-22, printing *"PT-06: arrival-order permutation is UNEXPRESSIBLE (the engine exports no replay; P2-8)"*. **The block switches itself on the day the engine exports `replay`**, which [WAVE-05 section 3](../../plans/WAVE-05-tier2-fixture-shapes.md) calls self-clearing, and on that day this row moves without a ruling |
| GS-073 | covered-elsewhere |  | EXECUTED by `RE-D-02`, the `describe` at [`determinism.test.ts:245`](../../../packages/golden-loader/test/determinism.test.ts), which names this row at `:232` above it. Five cases run and pass, `TZ=Asia/Kolkata` with `LC_ALL=ja_JP.UTF-8` first, which is the row's own pair, and one of the five asserts the spawned locale genuinely changed so the comparison meant something. [STRATEGY section 3.1](../STRATEGY.md) rows `PT-06` as *"the executable form of GS-071 to GS-073"* ([ADR-076 section 3](../../decisions/ADR-076.md)) |
| GS-074 | blocked | outside-loader-boundary | a supersession replayed forward, in `apps/worker` ([`replay.ts`](../../../apps/worker/src/batch/replay.ts)), outside the loader import boundary ([golden-loader](../../../packages/golden-loader/README.md)) |
| GS-075 | blocked | outside-loader-boundary | a cross-version diff report, produced in `apps/worker` ([`replay.ts`](../../../apps/worker/src/batch/replay.ts)), outside the loader import boundary ([golden-loader](../../../packages/golden-loader/README.md)) |
| GS-076 | covered-elsewhere |  | EXECUTED by `RE-C-10`, the `it` at [`plan-validate.test.ts:405`](../../../packages/rules-engine/test/plan-validate.test.ts), and a second time through `SEEDS['CV-10']` at `:789` under the oracle `it.each` at `:833`, which runs the seeded violation past `validatePlan` AND the independently transcribed oracle. This row is named at `:91`. **Its seed's error set is pinned EXACTLY**, at the `it.each` at `:859`, which is WAVE-05 `X4`. Line numbers re-derived by grep 2026-08-22 after [session 128](../../sessions/2026-08-22-session-128.md) moved every one of them; the stable locators are the two test ids ([ADR-076 section 2](../../decisions/ADR-076.md)) |
| GS-077 | covered-elsewhere |  | EXECUTED by `RE-C-06`, the `it` at [`plan-validate.test.ts:314`](../../../packages/rules-engine/test/plan-validate.test.ts), which asserts BOTH of this row's halves, 0 bp and 10001 bp refused against 1 and 10000 passing, and again through `SEEDS['CV-06']` at `:780` under the oracle `it.each` at `:833`. This row is named at `:83`. **Its seed's error set is pinned EXACTLY**, at the `it.each` at `:859`, which is WAVE-05 `X4`. Line numbers re-derived by grep 2026-08-22 after [session 128](../../sessions/2026-08-22-session-128.md) moved them ([ADR-076 section 2](../../decisions/ADR-076.md)) |
| GS-078 | covered-elsewhere |  | EXECUTED by `RE-C-01`, the `it` at [`plan-validate.test.ts:240`](../../../packages/rules-engine/test/plan-validate.test.ts), which refuses `intraday_trailing` on the funded phase AND on the eval phase and pins the reported `path`, and again through `SEEDS['CV-01']` at `:765` under the oracle `it.each` at `:833`. This row is named at `:74`, the one citation in this block [session 128](../../sessions/2026-08-22-session-128.md) did not move. **Its seed's error set is pinned EXACTLY**, at the `it.each` at `:859`, which is WAVE-05 `X4`. Line numbers re-derived by grep 2026-08-22 ([ADR-076 section 2](../../decisions/ADR-076.md)) |
| GS-079 | blocked | no-plan-record-value | the row turns on a hard daily loss limit and all three plan records carry `"daily_loss_limit": null` ([`CORE-50K.json`](../../../packages/rules-engine/fixtures/plans/CORE-50K.json)), so a fixture would state a value no plan document states, which `TR-01` forbids |
| GS-080 | written |  | [`GS-080-a-gate-configured-to-zero-renders-as-disabled-not-as-satisfied.yaml`](../../../packages/rules-engine/fixtures/GS-080-a-gate-configured-to-zero-renders-as-disabled-not-as-satisfied.yaml), written by session 109 with its `.expected.json` sibling. It is the first of the three rows a fixture landed under while the registry did not move, and the oldest by eight sessions |
| GS-081 | written |  | [`GS-081-settlement-leaves-the-floor-the-high-water-balance-and-the-lock-untouched.yaml`](../../../packages/rules-engine/fixtures/GS-081-settlement-leaves-the-floor-the-high-water-balance-and-the-lock-untouched.yaml) |
| GS-082 | blocked | format-cannot-express | a per-day table of which gate binds on each of three days, and the expectation carries exactly one `end_state` ([`loader.ts`](../../../packages/golden-loader/src/loader.ts), `EXPECTATION_KEYS`) |
| GS-083 | covered-elsewhere |  | EXECUTED by its own `describe` at [`plan-validate.test.ts:964`](../../../packages/rules-engine/test/plan-validate.test.ts), four cases, all passing. The first asserts `idsOf(result)` **EQUALS** `['CV-17']` rather than containing it, so the file states that `CV-17` blocked the publish and not merely that something did; the second EXECUTES the row's arithmetic through `resolvePlan` and the day fold rather than reciting it. This site is the one of the eight that named its own row before [session 123](../../sessions/2026-08-21-session-123.md), three times, and the file's own header says it lives there because it cannot live in `fixtures/` ([ADR-076 section 2](../../decisions/ADR-076.md)) |
| GS-084 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-085 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-086 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-087 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-088 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-089 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-090 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-091 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-092 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-093 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-094 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-095 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-096 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-097 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-098 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-099 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-100 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-101 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-102 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-103 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-104 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-105 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-106 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-107 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-108 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-109 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-110 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-111 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-112 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-113 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-114 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-115 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-116 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-117 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-118 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-119 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-120 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-121 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-122 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-123 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M8**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-124 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M8**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-125 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M8**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-126 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M8**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-127 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M8**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-128 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-129 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-130 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-131 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-132 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-133 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-134 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-135 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-136 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-137 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-138 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-139 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-140 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M8**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-141 | covered-elsewhere |  | EXECUTED by the three-plan `it.each` at [`plan-validate.test.ts:169`](../../../packages/rules-engine/test/plan-validate.test.ts), three cases, all passing: an EXACT-SET equality of the diff ids each v1 plan emits, `PW-02a` on Core EOD and Direct and `PW-02b` on Merit Rapid, with nothing else emitted. This row is named at `:157`. **THE RESIDUAL THIS CELL RECORDED IS LANDED AND THE CELL SAYS SO RATHER THAN STILL CLAIMING IT**: the row's last sentence, *"asserts the two are never rendered identically"*, is the `info` against `warning` severity and the distinct text, which `diffIds` cannot reach because it compares ids, and WAVE-05 `X3` wrote it as the `it` at `:190`, which pins both severities and `textsDiffer` in ONE `expect` because no two of the three clauses are the row's claim. Line numbers re-derived by grep 2026-08-22 after [session 128](../../sessions/2026-08-22-session-128.md) moved them ([ADR-076 section 2](../../decisions/ADR-076.md)) |
| GS-142 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M9**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-143 | blocked | outside-loader-boundary | the assertion is a BUILD FAILURE and not an end state, so no fixture format for M9 would discharge it (ADR-097 B2), and an MDX pipeline in `apps/site` is unreachable from the golden loader, which imports `@merit/rules-engine` and nothing else ([ADR-072](../../decisions/ADR-072.md) W1, "whether or not it is implemented"). `grep -rniE "parameter.?lint" apps packages scripts` returns 0 and `<PlanValue>` exists only as a comment at [`apps/site/src/index.ts:42`](../../../apps/site/src/index.ts). Exits to `covered-elsewhere` when `P4-j` lands the lint, never to `written` |
| GS-144 | blocked | outside-loader-boundary | same condition as GS-143 and PARTLY COVERED ALREADY: `assertWindowAttached` at [`apps/site/src/routes/stats.ts:145`](../../../apps/site/src/routes/stats.ts) is the row's first leg executable, and two tests naming this row at [`stats-page.test.ts:101`](../../../apps/site/test/stats-page.test.ts) and `:110` PASS in `pnpm exec vitest run` (21 passed, 2026-08-24). **The row's second leg is uncovered**: [section 16](16-gs-142-to-gs-148-marketing-site-m9.md) says "including on the OG image path", and a case-insensitive grep for "og image" and for "opengraph" over `apps` and `packages` returns four sites between them, all comments. A status is not partial, so this stays `blocked` until the OG leg runs (ADR-097 section 6) |
| GS-145 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M9**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-146 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M9**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-147 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M9**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-148 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M9**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-149 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M10**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-150 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M10**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-151 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M10**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-152 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M10**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-153 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M10**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-154 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M10**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-155 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M11**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-156 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M11**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-157 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M11**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-158 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M11**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-159 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M11**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-160 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M11**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-161 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M11**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-162 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-163 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-164 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-165 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-166 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-167 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-168 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-169 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-170 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-171 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M12**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-172 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M13**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-173 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M13**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-174 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M13**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-175 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M13**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-176 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M13**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-177 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M13**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-178 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M13**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-179 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-180 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-181 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-182 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-183 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-184 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-185 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-186 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M15**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-187 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M15**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-188 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M15**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-189 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M15**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-190 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M15**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-191 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M15**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-192 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-193 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-194 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-195 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-196 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-197 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-198 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M17**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-199 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M17**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-200 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M17**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-201 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M17**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-202 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M17**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-203 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M17**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-204 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M17**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-205 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-206 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-207 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-208 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-209 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-210 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-211 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-212 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-213 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-214 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-215 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-216 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-217 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-218 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-219 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-220 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-221 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-222 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-223 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-224 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-225 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-226 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-227 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-228 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-229 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-230 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-231 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-232 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-233 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-234 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-235 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-236 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-237 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-238 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-239 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-240 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-241 | blocked | format-cannot-express | the row's two trader figures are R-44's split on `PayoutEvaluation`, and its gross half is quantified over sequences while one fixture is one stream ([`types.ts`](../../../packages/rules-engine/src/types.ts), [`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-242 | written |  | [`GS-242-percent-of-size-scaling-holds-at-150k.yaml`](../../../packages/rules-engine/fixtures/GS-242-percent-of-size-scaling-holds-at-150k.yaml) |
| GS-243 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-244 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-245 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M14**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-246 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-247 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-248 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-249 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-250 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-251 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-252 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-253 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M17**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-254 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M10**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-255 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **INFRA**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-256 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-257 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-258 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-259 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-260 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-261 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-262 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-263 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-264 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-265 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-266 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-267 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-268 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-269 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-270 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-271 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-272 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-273 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-274 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-275 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-276 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-277 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-278 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M5**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-279 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-280 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M3**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-281 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M8**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-282 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-283 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M19**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-284 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-285 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M4**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-286 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-287 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M7**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-288 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-289 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-290 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-291 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-292 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-293 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-294 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-295 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M16**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-296 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-297 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-298 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-299 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-300 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-301 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-302 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-303 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-304 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-305 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-306 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M20**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-307 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-308 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M18**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-309 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M9**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-310 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M9**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-311 | blocked | vendor-call | [M02](../../plans/M02-rithmic-bridge.md) stands at `status: review` under [ADR-005](../../decisions/ADR-005.md), the one module plan of twenty-one that is not approved, pending the Rithmic vendor call |
| GS-312 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-313 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-314 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-315 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
| GS-316 | blocked | no-fixture-format | [33.1](33-ownership-index-and-coverage-reconciliation.md) owns the row to **M6**, and the only fixture format in the tree is M1's day stream folded through `advanceDay` ([`fixtures/README`](../../../packages/rules-engine/fixtures/README.md)) |
