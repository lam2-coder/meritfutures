{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git pull --ff-only origin main 2>&1 || echo 'PULL FAILED: resolve divergence with origin/main before writing anything. Do not commit on top of a stale tree.'"
          },
          {
            "type": "command",
            "command": "echo '--- docs/STATE.md ---'; cat docs/STATE.md 2>/dev/null || true"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node .claude/hooks/block-dangerous-shell.mjs"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "git push origin HEAD 2>&1 || echo 'PUSH FAILED: work is committed locally but NOT on origin. This container is ephemeral. Push before the session ends.'"
          }
        ]
      }
    ]
  }
}
