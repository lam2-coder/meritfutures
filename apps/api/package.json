{
  "name": "@merit/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "The /api/v1 surface API_CONTRACT specifies. Railway services: api (public origin) and api-admin (ADMIN_ORIGIN), one codebase deployed twice per ADR-083.",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "start": "node --experimental-strip-types src/start.ts"
  },
  "//": "IT DECLARED NO RUNTIME DEPENDENCY UNTIL ADR-100 AND FASTIFY'S ABSENCE WAS THE FENCE RATHER THAN THE RULING. ADR-083 ruling 3 chose the framework and deliberately did not install it: the version lives in `pnpm-workspace.yaml`'s `catalog:` block, which session 144 did not hold, and `.npmrc`'s `save-exact` plus VG-12 make a version a human approves once rather than a caret. ADR-100 is that admission -- every package named, every resolution checked for a registry entry carrying a `sha512` integrity hash, `pnpm audit --audit-level=low` run and the closure delta stated -- and it is why `\"fastify\": \"catalog:\"` is below. THERE IS NO `build` SCRIPT AND THERE MUST NOT BE ONE. `CI-06/gate-inventory` probes every apps/*/package.json for that key and requires it ABSENT, because CI-07's row reads through an artifact that does not exist; a build script makes that gate fail on good news. Fastify 5 needs none, which ADR-083 section 5 measured and which `pnpm run start` now demonstrates rather than predicts: `node --experimental-strip-types src/index.ts` serves the registry with nothing compiled.",
  "//dependencies": "`@merit/affiliate` AND `@merit/psp` ARE WORKSPACE DEPENDENCIES AND NOT CATALOG ONES, so `pnpm-workspace.yaml` is untouched and VG-12 is asked to admit nothing: the only lines `pnpm-lock.yaml` gains are this importer's own. `.npmrc` sets `node-linker=isolated`, which makes an undeclared import unresolvable at run time while `tsc` resolves it happily through the workspace, so the declaration is what separates a route that type-checks from one that starts. `ls apps/api/node_modules/@merit` is how that was measured rather than assumed. THERE IS STILL NO `@merit/db` HERE and its absence is now session 220's ruling as well as session 219's finding: ADR-112 removed the obstacle ADR-109 clause 3 named, and an implementation of `IdempotencyStore` still needs this manifest to declare the accessor, which is an authority change on the deployable that serves the whole public surface and not a side effect of a checkout slice. See `src/idempotency.ts`'s header, ADR-109 and ADR-113.",
  "dependencies": {
    "@merit/affiliate": "workspace:*",
    "@merit/psp": "workspace:*",
    "fastify": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
