# GS-011  Trailing floor ignores the intraday high
#
# THIS IS THE FIXTURE GOLDEN_SCENARIOS SECTION 2 PRINTS. Every input below is
# transcribed from that block, which is why it is the first one written: the
# format has an worked example in the corpus and a loader that cannot read it is
# a loader that has redefined the format.
#
#   hwb' = max(hwb, closing_balance_cents), the CLOSING balance only; the
#   intraday high never raises it                              M01 R-13
#   "high 5,090,000 with close 5,020,000 leaves hwb at 5,020,000"
#                                                              GOLDEN_SCENARIOS GS-011 row
#   4,770,000 = 5,020,000 - 250,000                            M01 Appendix A.1 at 50K
#
# TWO DEPARTURES FROM THE PRINTED BLOCK, both stated rather than quiet:
#   - `traded_day` is supplied. R-08 derives it from `fill_count > 0`, and a
#     loader deriving it would be a loader that has implemented a rule the
#     fixtures exist to check. The engine's DayMark declares the field, so the
#     fixture states it.
#   - `expect` is not here. It is the .expected.json sibling, which is the
#     format STRATEGY section 2 and GOLDEN_SCENARIOS section 2 both rule.
id: GS-011
name: trailing floor does not trail on an intraday spike
source: M01 R-13, R-18
plan: CORE-50K
calendar: cme-2026
account:
  phase: funded
  opened_on: 2026-11-02
  size_cents: 5000000
days:
  - trading_day: 2026-11-03
    opening_balance_cents: 5000000
    closing_balance_cents: 5020000
    high_balance_cents: 5090000
    low_balance_cents: 4995000
    realized_pnl_cents: 20000
    fill_count: 4
    traded_day: true
    adjustment_cents: 0
settlements: []
