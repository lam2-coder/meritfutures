# GS-006  Win day exactly at the floor counts
#
# DERIVED FROM THE PLAN DOCUMENTS, NEVER FROM IMPLEMENTATION OUTPUT (TR-01).
#
#   win day  `realized_pnl_cents >= win_day_floor_cents` (`>=`, so exactly at
#   the floor counts)                                         M01 R-09
#   win day floor 15,000c at 50K, 30bp                        M01 Appendix A.1
#   "`realized_pnl_cents == 15000` satisfies `>= 15000`"      GOLDEN_SCENARIOS GS-006 row
#
# THIS FILE AND GS-007 ARE ONE ASSERTION IN TWO FILES. The registry calls them
# "the published boundary", so every measurement below is identical to GS-007's
# except the realized P&L and the closing balance it implies. A reader diffing
# the pair sees one cent and nothing else, which is the whole point: an engine
# carrying `>` instead of `>=` fails here and passes GS-007, and no other
# difference between the two files can be blamed for it.
id: GS-006
name: win day exactly at the floor counts
source: M01 R-09
plan: CORE-50K
calendar: cme-2026
account:
  phase: eval
  opened_on: 2026-11-02
  size_cents: 5000000
days:
  - trading_day: 2026-11-03
    opening_balance_cents: 5000000
    closing_balance_cents: 5015000
    high_balance_cents: 5020000
    low_balance_cents: 4995000
    realized_pnl_cents: 15000
    fill_count: 2
    traded_day: true
settlements: []
