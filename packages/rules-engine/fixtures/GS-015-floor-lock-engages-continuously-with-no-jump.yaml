# GS-015  Floor lock engages continuously with no jump
#
# DERIVED FROM THE PLAN DOCUMENTS, NEVER FROM IMPLEMENTATION OUTPUT (TR-01).
#
#   trigger  `closing_balance_cents - size_cents >= floor_lock_at_profit_cents`
#   effect   `floor = floor_lock_floor_at_cents`, `floorLocked = true`, `hwb`
#            frozen, all permanently                          M01 R-15
#   floor lock at profit  260,000c at 50K                     M01 Appendix A.1
#   locked floor          size + 10,000c = 5,010,000c         M01 Appendix A.1
#   trailing floor on the day  5,260,000 - 250,000 = 5,010,000 M01 R-13, Appendix A.1
#   "CV-12 forces the trigger to sit exactly where the trailing floor already
#   is, so the floor never jumps"                             M01 R-15, CV-12
#   "260,000 = 250,000 + 10,000"                              M01 Appendix A.4, CV-12 row
#   "At profit 260,000 the trailing floor reaches exactly `size + 10,000` and
#   locks there. Asserts the lock introduces no discontinuity"
#                                                             GOLDEN_SCENARIOS GS-015 row
#
# THE CLOSE IS THE TRIGGER TO THE CENT, WHICH IS WHY THIS SCENARIO CAN PIN
# CONTINUITY AT ALL. Profit of exactly 260,000c satisfies R-15's `>=` on its
# first true value, and at that value the two expressions for the floor agree:
# trailing gives 5,260,000 - 250,000 and the lock gives size + 10,000, and both
# are 5,010,000c. CV-12 is the reason they agree rather than an arithmetic
# coincidence, and Appendix A.4 walks the check for this plan.
#
# THE ACCOUNT IS FUNDED, so no eval progression can reach the outcome and the
# only rules with anything to do on this day are R-13 and R-15. The first funded
# mark opens at exactly `size_cents`, which is INV-20.
id: GS-015
name: floor lock engages continuously with no jump
source: M01 R-13, R-15
plan: CORE-50K
calendar: cme-2026
account:
  phase: funded
  opened_on: 2026-11-02
  size_cents: 5000000
days:
  - trading_day: 2026-11-03
    opening_balance_cents: 5000000
    closing_balance_cents: 5260000
    high_balance_cents: 5265000
    low_balance_cents: 4995000
    realized_pnl_cents: 260000
    fill_count: 5
    traded_day: true
settlements: []
